---
type: Spec Mapping
title: Step 1b の CR に同梱すべき要素の洗い出し —— 仕様案の表が覆っていないもの
description: 新方式の仕様案（spec-draft-ja.md）を 1 本の CR で当てるとき、案の表がまだ覆っていないのに同じ CR へ入れなければならない要素を、今の仕様・見本・アプリのコードと突き合わせて数えた表。重い順に 23 件。利用者の指示（2026-09-19「他にも一緒のCRに入れておくべき要素が無いか、洗い出せ。」）で体が調べた。
tags: [spec-mapping, step-1b, grab-area, hit-test, label-placement, selection, milestone-glyph, chevron, endpoint-span]
phase: design
status: draft
---

# Step 1b の CR に同梱すべき要素の洗い出し（仕様案の表が覆っていないもの）

- 調べた日: 2026-09-19。読むだけ（リポジトリは 1 文字も書き換えていない）。再開アイコン（GR-8 ほか）は対象外（依存する所だけ触れる）。
- 読んだもの: `previous-project-result/16-grab-area-sizing/spec-draft-ja.md`（全文。読んでいる途中で前に立つ者が再開アイコンを足した —— 行番号は足した後の 473 行版）、`old-to-new-map-ja.md`（A 全体・B の §1.4〜§6）、見本 `grab-area-sizing-sample.html`（これも作業中に動いた。行番号は最後に取り直した値）、`rulings.md` の JDG-176〜221、`docs/spec/01-04-requirements.md`・`05-07-design.md`・`_assets/tbl-settings.md` の該当行。アプリ（`src/`）は別の体に読ませ、効く所は自分で開いて確かめた（【確】）。
- 略号: 案 ＝ spec-draft-ja.md、R ＝ 01-04-requirements.md、D ＝ 05-07-design.md、S ＝ tbl-settings.md、見本 ＝ grab-area-sizing-sample.html、HIT ＝ src/entity/layout-engine/item-hit-area/item-hit-area.ts、ICT ＝ src/adapter/input-command-translator/input-command-translator.ts、FL ＝ src/framework/single-html-shell/frame-loop.ts。
- 判定: **必須** ＝ CR が消す／書き直す行に寄りかかっており、外すと規則が黙って落ちるか食い違う ／ **入れるべき** ＝ 同じ行を触るので今入れるのが安い ／ **後で** ＝ 独立している。

## 1. 一覧（重い順）

| # | 候補 | 今の仕様の在り処 | 案で探した所と欠け | 載せる表 | 判定と理由 | 見本で示せるか |
|---|---|---|---|---|---|---|
| 1 | **`FR-108` の範囲が、置き換える T-023a の結びより狭い** | R:3273〜3276「当たり判定の対象はそのフレームで描いた編集対象に限る」—— LOD で落とした Task・非表示の行・畳んだコメントボックス・変更前の予定（`FR-015`）も外す | 案 §2 `FR-108`（案:74〜76）は「表示していない予定・実績・依存線」だけ。GA の注（案:222）も同じ。群 D（`JDG-211`、rulings:337）は「`FR-108` を正とし T-023a の結びはそれを指す」 | `FR-108` の STATEMENT（GA・PE はそれを指す） | **必須** —— 指す側に縮めると、LOD・畳み・隠した行・`S-63` で隠したマーカー・選んでいないタスクのフェード掴み点・`FR-015` の重ねの除外が落ちる。STATEMENT を「そのフレームで描いていない編集対象」に広げる | 一部（予実・マーカー・依存線の切替はある。LOD・畳みは無い） |
| 2 | **ハイライトボックスの「本体」が囲んだ内側全部** | R:3354（`GR-14` 本体・アンカー・四隅）、R:3435〜3450。本体が枠か内側かは書いていない。アプリは内側全部【確 HIT:397〜400】。`FR-019` R:4396「枠線が唯一の表現」 | 案 §4.5 の T-023d の新しい並び（案:268）が `GR-14` を日程の形（行 4）より先へ上げる。理由は「形の上に描くもの」だが、表 ZO（案:330）はハイライトボックスの枠をマーカー・札より**奥**（順 6）に描く | T-023d の新しい並び ＋ GA の外（`GR-14` の場所の欄） | **必須** —— このまま上げると、範囲を囲う箱（`UC-008`）の中のタスクが 1 つも掴めなくなる（今は `GR-3`/`GR-4`/`GR-7`/`GR-12` だけが負ける）。「本体 ＝ 枠線 ± `S-230`」と書くか、ハイライトボックスは下に残す。コメントボックスのアンカー（`S-230` の正方形、行の帯の中央 `LF-15`）も上げるとその下のタスクから 12px 角を奪う —— 同じ所で断る | 無い（見本に注記が無い。群 B ⑫ の記録どおり） |
| 3 | **線だけの形（`SH-3`/`SH-4`）のダミーに掴み代の行が無い** | `DM-10`（R:2811）「バーの形状は例外ではない」、`GR-9`/`GR-17` は形状を問わない | GA のダミーは 5・6（`===`）と 17（◆）だけ（案:205〜206、217）。XS の `--->`（案:130〜158）にもダミーの段が無い。見本も `--->` のダミーを描かない（`useArrowAct`、見本:853） | XS（3 段目に 1 日の実績の矢 ＝ 印）・GA（`--->` のダミーの開始／終了）・LP 5・6 は RF 2 で足りる | **必須** —— `GR-9`/`GR-17`/T-240 を消すと、未着手の `--->` に実績の入口が無くなり、書き直す `FR-043`（案:89）が `SH-3`/`SH-4` で成り立たない。印の幅は マーカー径（＝字 f）× 0.5 ≒ 3.75px なので、中央合わせ 12 の 2 つの掴み代はほぼ重なる（HT 段 4 で割る）—— 値を決める要る | 示せる（④ の行を未着手にする切替を足す） |
| 4 | **マイルストーンの 15 の図形（`SH-5`）を ◆ だけで書いている** | R:1073・1075（15 の図形）、`LF-10`（D:1020 —— どの図形も一辺 h の正方形）、`GR-15`「実績の図形の上」、`GR-18`「描いた**正方形**そのもの」（R:3351〜3352）、R:3408「`SH-5` では描いた図形そのもの」 | XS 8・9、LP 7・8（「菱形の右端」「菱形の幅の 25%」）、GA 15〜17（「菱形そのもの」）、HT「◆ の予定」、PE 6、PK 5・6 —— どれも ◆ だけ。見本も ◆ だけで、しかも掴み代は外接の箱（見本:786〜794）、形の上の判定だけ `|dx|+|dy|`（見本:477）。アプリは形の上 ＝ 輪郭の多角形、掴み代 ＝ 外接の箱【サブ調査】 | XS・LP・GA・HT・PE・PK の「◆」を「`SH-5` の図形」に。基準は外接正方形（一辺 h ／ h × `actualOfPlan`） | **必須** —— `GR-15`/`GR-18` を消すと 14 の図形の掴み代が定義を失う。直すのは語だけで済む：掴み代 ＝ 外接正方形（115% も正方形の幅に）、形の上 ＝ 図形の輪郭（R:3408 のまま）、札の基準 ＝ 外接正方形の右端と幅の 25%。☆・五角形は右の頂点が正方形の右端より内（0.95r）で、△ は 25% の位置で文字が斜辺を横切る —— 了承の要る見た目の代償 | 示せる（図形の選び欄を足すだけ） |
| 5 | **矢羽根（`SH-2`）に行が無い** | R:1070、`GR-1`/`GR-2`「矩形と矢羽根のみ」（R:3342〜3343）、`FD-5`「矢羽根ではフェード長が切り込みの深さを置き換える」（R:1298）、`LF-6`（D:1016、実績の切り欠きは予定 × `actualOfPlan`） | XS・LP・GA は `===`・`--->`・◆ の 3 つだけ（案で「SH-2」「矢羽根」は 0 件）。GA 7・8 の基準点は「台形の点 4 ／ 2」 | XS・LP・GA の見出しを「`===`（`SH-1`・`SH-2`）」にし、GA に注を 1 つ：端の基準点 ＝ 外接矩形の左右端（アプリはそう取る【サブ調査】）、フェードの掴み点 ＝ T-012a の点 4 ／ 2 の日と上端・下端 | **必須** —— `GR-1`〜`GR-6`・`GR-12` を消すと矢羽根の掴み代とフェードの掴み点の場所が消える。「入る」を矢羽根の尖りで減らすかも決める（今は減らさない） | 示せる（⑤ の 1 本を矢羽根にする） |
| 6 | **端点スパン（`SH-4`）に行が無い** | R:1072、`LF-7`（D:1017、点の半径 ＝ 線 × `S-47`）、`OC-6`/`OC-10` は「矢印・端点スパン」 | 案は `SH-4` を §4.1 の注（案:149 付近）と ㉕ でだけ触れる。GA 10〜13 の基準点は「軸の左端」「`>` の中心」。XS の縦幅の式の注「矢じりがはみ出すのは矢の終わりの 1 か所だけ」は `SH-4`（両端に点）では偽 | XS（点の径 ㉕、両端のはみ出し）・LP 5・6 と GA 10〜14 の見出しを「`--->`（`SH-3`・`SH-4`）」に、基準点 ＝ 開始の点・終了の点の中心 | **必須** —— `SH-3` と同じ段に積む形なので、XS の式・中線で割る掴み代（`JDG-185`）をそのまま当てると書くだけで足りるが、書かないと `GR-3`〜`GR-6` を消した後に定義が無い。㉕ の答えが要る | 示せる |
| 7 | **押すと選ぶこと（`SL-2`）と複数選択が表 PE に無い** | `SL-2`「クリックで選び、置き換える」、`SL-7`「選択の本体を引けば全部動く」、`SL-7a`「端を引けば 1 つに絞る」（R:2202〜2208）、`FR-075`「掴み点は選択中のタスクだけ」（R:1270）。アプリは離した時点で当たったタスクを選ぶ（引いた後も・マーカーでも）【確 ICT:3967〜3984】 | PE（案:276〜289）の「押して離す」は、本体・端・フェード・◆ が「—」、ダミーが「何も起きない」。「選ぶ」は依存線（PE 10）だけ。GA 7・8 に「選んでいるときだけ」が無い（理由は HT の注 案:255 にだけある） | PE の前文に 1 文（どの行も押して離せばそのタスクを選ぶ、`SL-2`）、PE 1 に `SL-7`、端・マーカー・フェードに `SL-7a`、GA 7・8 に「選択しているタスクだけ（`FR-075`）」 | **必須** —— 表のとおり読むと「ダミーを押しても何も起きない ＝ 選ばない」になり `SL-2` と衝突する（仕様だけを読む試験の体がそのまま赤を書く）。⚠️ 決める要る所: 複数を縦に引いたとき全部が行を移るか（`SL-7` は全部、アプリは掴んだ 1 つだけ ICT:3014〜3017）、マーカーを引くのは `SL-7a` の「端」か | 一部（見本は選択を持たない。依存線の選択だけ） |
| 8 | **マイルストーンを縦に引いて行を移せなくなる** | 予定の図形は今 `GR-12`（`GR-15` が「`GR-12` より上」R:3351）⇒ 縦で行の載せ替え（`HM-3`）。アプリも同じ【確 ICT:2994〜3018】 | PE 6「◆ の予定 ／ 実績」の「縦に引く」が「—」（案:284）。`JDG-188` の縦は「タスク」—— マイルストーンを外す裁定は無い | PE 6 の縦の欄 | **必須** —— このまま当てると、今できる操作（マイルストーンを別の行へ）が理由なく消える。PE 1 と同じ「予定と実績を一緒に移す。日付は変わらない」にする | 示せる（見本の ◆ は横だけ） |
| 9 | **マーカーを引く行（PE 7）の主語があいまい** | `JDG-187`「マーカーに実績が追従するのは、マーカーと `===` の名前が実績の右にあるときだけ」（rulings:313）、`--->` は追従しない | PE 7「進捗マーカー（実績の右に立つ）」。RF 2 で未着手の札の基準はダミーなので、マーカーは「ダミーの右」に立つ（ダミー幅は径 × 0.5 なので必ず「入らない」）。◆ のマーカーも常に「図形の右」（LP 7）。見本はダミーの右のマーカーを引くと予定の開始から実績を作り（見本:1114〜1118）、◆ のマーカーを引くと ◆ の実績を動かす（見本:1081） | PE 7・8 の行の分け方と、T-245 に足す「マーカーを引く」行の値 | **必須** —— 形（`===`・`SH-2` だけ）と基準（実績だけか、ダミーも含むか）を書かないと、見本の 2 つの振る舞いが裁定なしに仕様へ入る。◆ は `JDG-187` の「`===` だけ」から外れる | 示せる（すでに振る舞いはある。直すだけ） |
| 10 | **HT 段 1 の「描いた形」が形ごとに定まっていない** | R:3408「`SH-1`〜`SH-4` は予定と実績を描いた範囲の矩形、`SH-5` は図形そのもの。矢羽根の欠けた角も、線だけの形の細い範囲もその矩形で読む」 | HT 段 1（案:238）は「バー・矢・菱形・ダミー・マーカー」。対応表は R:3404〜3426（C7）を「削除」にし、R:3408 の文の行き先が無い。見本は `===` を予定の矩形（フェードの台形でなく）、`--->` を線ごとの帯（予定と実績のあいだは形の外）で取る（見本:678、871、875）—— 今の仕様の「範囲の矩形」と違う | HT 段 1 の注（形ごとの「形の上」の範囲）。段 2（`GS-2` の縦の距離）も同じ定義を読む | **必須** —— 消すと段 1・段 2 の境が実装ごとに割れる。`--->` を線ごとにするなら R:3408 を覆す 1 文が要る（裁定は無い） | 示せる（見本がすでに 1 つの答えを持つ） |
| 11 | **形の上の依存線 ＝ 本線 ＋ 矢じり、選ばれたら `S-178` 倍、縁は含まない** | R:3410〜3411。`SL-8`（R:2210）。アプリは選んだ線を太いインクで取る【サブ調査 HIT:353〜371】 | HT 段 1（案:238）は「線の中心から 線の太さ ÷ 2」だけ。GA 19 も矢じりを言わない。見本の掴み代は線分だけで、選んだ線も元の太さで取り（見本:982）、描くのは ＋1px（`S-178` の 2 倍でない） | HT 段 1・GA 19 の注 | **必須** —— C7 を消すと 3 つの規則（矢じり・選択の太さ・縁を除く）が行ごと落ちる | 示せる |
| 12 | **`--->` の 1 段目を字 f で数えると、行の帯と隙間の規則がずれる** | `OC-10`（R:1541）・`S-233`（S:292「字の大きさそのものを数えてはならない」）・`VG-5`（R:1250「描いた端は `OC-10` を含む」）・`LF-2`（D:1012）・T-012 の注（R:1090「最上段では目盛の下に隠れる」） | 案 §4.1 の注は `OC-10` と `S-233` の書き直しを挙げ、「上は行と段の隙間が受ける」と書く。`VG-5`・`LF-2` は挙げていない。最初の行の最初の段の上には隙間が無い（`VG-1` は段の下にしか空けない）ので、字の箱が約 1.25px 目盛の下へ出る | XS の注と `VG-5`（数えた端か描いた端か） | **入れるべき**（高）—— `OC-10` を書き直す CR で `VG-5` が古い端を指したまま残る。最上段の字の欠けは、`OC-10` が防いでいた事故そのもの。⚠️ ついでに: `LF-3` の床（矩形の高さと操作子の格子）で、1 段だけの行は `--->` でも `===` と同じ高さのまま —— `JDG-212` の「縦幅が狭くなる」が効くのは複数段の行だけ（利用者の期待とずれうる。要確認） | 一部（見本は最上段を持たない） |
| 13 | **引いているあいだの絵と「書かない」の散文（C19〜C24）の付け替え** | R:3524〜3548（`GR-*` の列挙で持つ） | 案は「本表の外に残す」（案:228）とだけ言う。対応表 §5 の 3 は「ID を GA の行へ付け替える」。C21 は `GR-7`（マーカー）を「追従しない」に数える —— PE 7 で引けば実績の終了が動くので偽 | FR-016 の散文（GA の行 ID を列挙） | **必須** —— `GR-*` を消せば列挙がすべて宙に浮く。マーカーを引くときの絵（実績の終了の予告）と「書かない」を足す | 示せる（見本は引くたびに書き換えて描く ＝ 予告と同じ見え） |
| 14 | **依存線の道具が「当たった」と読む範囲** | `PTD-3`（R:3251）「何かに当たった（`MK-9a`）」、`FR-009` R:2484〜2490「左半分／右半分、端の掴み代で判じない、予定が無ければ実績に落ちる」、`PI-7` の `dependencyStartOfHit`（D:427「`itemAtPointer` の答えから」） | 案の PE の注（案:296）は「予定の左半分と右半分で結ぶ」だけ。見本は結ぶ元も先も**描いた予定の形**だけ（`taskAt` 見本:994）。アプリは元 ＝ T-023d で当たったタスク（マーカー・外の札でもよい）、先 ＝ 予定の箱、無ければ実績の箱【確 HIT:594〜609】 | `FR-009` の書き直し（案:93〜）と `PTD-3`・`PI-7` の文 | **入れるべき**（高）—— 同じ CR で `FR-009` の「実績に落ちる」を消すので、アプリの落ち先がそのまま裁定違反になる。描いた予定だけにすると、`--->` の予定の矢は 3.5px の帯しか無い（どれも指せる、と逆向き）—— 決める要る | 示せる（見本が 1 つの答えを持つ） |
| 15 | **基準日線（`GR-16`）の掴み代** | `S-137`（S:305）が `GR-13` と `GR-16` の両方、`S-230`（S:306）の既定が `S-137` | 案 §5 は `S-137` を依存線の分で「割る」、`S-230` に自分の値。T-023d の並び 6（案:271）は「6px のまま。要確認」 | §5 の定数（`S-137` を基準日線だけの行として残すと明記） | **入れるべき** —— 既に案が気づいている。確かめるのは利用者の答え（6px のまま）だけ | 無い（見本に基準日線が無い） |
| 16 | **打ち切った名前のツールチップ** | `EZ-6`（R:5861）「どのタスクの上かは T-023d の順」、`UC-001` 拡張 4a（R:582）。`GR-10` は T-023d の行なので、名前の上に乗れば説明が出るはず | 案は触れない（対応表は `FR-092` を参照の付け替えだけとする）。LP で名前が形の外に出る場面が増える（LP 2・4・7） | `EZ-6` の参照を「表 HT と T-023d」へ ＋「素の押下で成立しない名前・担当も説明の引き金に数える」1 文 | **入れるべき** —— 参照は必ず直す。アプリは押しの表で判じるので外の名前で説明が出ない（サブ調査の読み。アプリの欠陥） | 無い |
| 17 | **乗ったときに濃くするもの（未着手のマーカー・ダミー）** | `FR-013` R:2942、`FR-048` R:4447 | 案は「乗る」を言わない。HT で実績の開始がマーカーの左半分を取る場面など、どの掴み代が応えていれば「乗っている」かが決まらない | PE に「乗せる」の欄か注 1 つ（HT が応えている掴み代のものを濃くする） | **入れるべき**（低） | 示せる |
| 18 | **ピン止めの帯の境で掴み代を切る** | `FR-098`（R:4112〜）「帯の場所に描いてはならない」—— 描くものだけ | 案は触れない（対応表 B N-22 は ZO だけ）。フェードの掴み点（角の 4px 外）、線の掴み代（縁から 2px）、外側 12 は描いた形の外へ出る | GA の前文 1 文 | **後で**（低）—— アプリは帯の中身を切らずに描き、当たりも帯を先に見るかは未確認 | 無い |
| 19 | **依存線の道具を構えているときのポインタ** | `IN-2`（R:6429）「既定の矢印」 | PK に行が無い（`IN-2` に残す前提）。見本は予定の上で十字（見本:1044） | `IN-2` のまま（見本を直す） | **後で** —— 仕様は変えない。見本を仕様へ戻すだけ。基準日線・注記の四隅のポインタも `IN-2`「環境が持つ」のまま | — |
| 20 | **進捗マーカーの記号（T-021 の `PM-1`〜`PM-4`）** | R:2961〜2965、遅れは `PM-4` が勝つ | 掴み代は GA 18（描いたマーカー ＝ `LF-11` の正方形）で記号に依らない。見本は 4 つの状態を描き `PM-4` を持たない | — | **後で** —— どの表も記号に寄りかからない。残る問いは ㉔（`S-22` の上限）だけ | 示せる（要らない） |
| 21 | **LOD（`FR-018`、表 T-005a）** | タスク LOD は幅 ＜ `S-86` × 描く比（既定 24 × 0.625 ＝ 15px）の Task を描かない（S:256、`DS-2` R:6388）。札・マーカーを個別に落とす LOD は仕様にもアプリにも無い | 案の低倍率の測り（「1 日 2px・5 日の予定」＝ 10px、案:225）は、製品では描かれない幅である。見本は `S-86` を持たない | — | **後で** —— 掴み代が消えることは #1 の一般則で足りる。Step 4 の試験の範囲を決めるとき、`S-86` より狭い予定を入れない | 示せる（`S-86` を足せば見本が誤解を招かない） |
| 22 | 範囲選択が「囲んだ」と読むタスクの外形、`--->` の選択の枠 | `SL-3`・`SL-8`（R:2203、2210） | 案は触れない | — | **後で** —— 掴み代と独立 | — |
| 23 | キーボードで選択を動かす | 仕様にもアプリにも無い（`SK-*` は Delete・全選択・写しだけ） | — | — | **対象外** | — |

## 2. 見落としやすい食い違い（案 ↔ 今の仕様 ↔ 見本 ↔ アプリ）

1. **案:81「隣のタスクどうしの札のぶつかりは LOD の受け持ち」は誤り** —— 今の仕様では段割当が解く（R:1380〜1382: 外へ出した名前は `OC-1` で占有に入り、`ST-3` が干渉しない段を選ぶ）。LOD（T-005a）は札を扱わない。`FR-109` の注の語を「段割当（表 T-038 → `ST-3`）」に直す。
2. **`GR-14` を上げる理由「形の上に描くもの」は、ハイライトボックスには当たらない**（#2）—— 案の表 ZO 自身が枠を順 6（マーカー・札より奥）に置いている。
3. **PE 6 の「◆ 縦 —」は `GR-12`/`HM-3` と逆**（#8）。**PE 4・5 の「何も起きない」は `SL-2` と逆**（#7）。
4. **GA 16・17「菱形そのもの」は見本（外接の箱）とも `JDG-189`「グリフのサイズ」とも `GR-18`「正方形そのもの」とも違う**（#4）。
5. **見本 ⑤ は本体を縦に引くと同じ行の段が入れ替わる**（見本:1065）—— `ST-6`（R:1519）「人が段を手で指定する手段を設けない（MUST NOT）」と逆。PE 1 の「行を移る」は正しいので、見本だけの簡略と注すること（試験の体が写さないように）。
6. **見本の選んだ依存線**は掴み代が元の太さのまま（見本:982）、描く太さは ＋1px（見本:733）—— R:3410 と `SL-8` の `S-178`（2 倍）と違う（#11）。
7. **見本のマーカーの引き**（ダミーの右 → 実績を作る、◆ → 実績を動かす）は `JDG-187` の「`===` だけ」を越える（#9）。
8. アプリの観察（仕様ではなく欠陥の候補）: 複数を選んで本体を引くと、離した時点で選択が掴んだ 1 つに縮む（ICT:3983 が PTD-3 の当たりで選択を置き換える）。`SL-7` は引いた後の選択を言わないので、#7 で一緒に決めると安い。

## 3. 同じ CR に入れる順の提案

- **語の置き換えで済むもの**（#4・#5・#6 の見出し、#15、#16）—— 表の見出しを形の群（`SH-1`・`SH-2` ／ `SH-3`・`SH-4` ／ `SH-5`）にし、「◆」を「`SH-5` の図形（外接正方形）」にする。
- **削る行の文を運ぶだけのもの**（#1・#10・#11・#13）—— 対応表の「削除」の行のうち、R:3273〜3276・R:3408・R:3410〜3411・R:3524〜3548 の中身を、行き先の表の注へ写してから消す。
- **利用者の答えが要るもの**（#2 ハイライトボックスの本体 ／ #3 `--->` のダミーの掴み代の値 ／ #7 複数を縦に引く ／ #9 マーカーを引く主語 ／ #10 `--->` の「形の上」を線ごとにするか ／ #14 依存線の道具の当たり ／ #6 の ㉕）—— #2 以外は、見本に切替を 1 つ足せば触って決められる（#14 は見本の道具がすでにある）。#2 は見本に注記が無いので、文で問う。
