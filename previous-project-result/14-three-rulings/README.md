---
type: Design Sample
title: 掴みシロの裁定と、新しい不具合 8 件
description: 2026-09-11 の巡が閉じられなかった 3 件は 2026-09-10 に裁定が下りた。裁定 ① は同時にいただいた不具合 2 と 8 に影響したので案 ①′ に替えて承認された。不具合 1 も同日に「ダミーを実績にして幅を変更せず」で解けた。裁定待ちは 0 件で、残るのは直す順だけである。
tags: [grab-area, hit-area, dummy, click, milestone, cursor, progress-marker, sample]
phase: design
status: fixed
---

# 掴みシロの裁定と、新しい不具合 8 件

- 初版: 2026-09-11 ／ **改訂: 2026-09-10（裁定を反映）**
- 見本: [`rulings-sample.html`](rulings-sample.html)
  （**1 ファイルで完結。ダブルクリックすれば開く。**サーバーも通信も要らない）

⛔ **これは仕様書ではない。**⛔ **これは動く見本でもない** —— **図と表だけ**である。
⭐ **裁定待ちは 0 件である。**
⭐⭐ **8 件はすべて直り、2026-09-10 に出荷ビルドを押して測った** —— 数は見本の「結果」の節に在る。

---

## 1. 下りた裁定（2026-09-10）

| # | 逐語 | 結論 |
|---|---|---|
| ① | 推奨通り案 ① | ⭐ **案 ①′**（インク ＋ 1 稼働日の下限）。⛔ 案 ① 単体では不具合 2 を解けないため |
| ② | 提案通り 裁定済 ＝ 現状を是とする | 引かないと書かない |
| ③ | 裁定済 ＝ 数を取り下げる | 条項は立てない |
| 不具合 1 | ステータスマークだけを変更して、実績の幅を変更せず **or ダミーを実績にして幅を変更せず** でよいのでは？ | ⭐ **2 つ目を採る** |

---

## 2. 案 ①′ —— 掴み代の最終形

```
1  掴み代は、描かれたインクそのものとする。インクの中央で左右に割る（＝ 案 ①、R-36）
2  ⭐ 着手済の実績の長さの下限を 1 稼働日（S-129）とする。0 日には縮められない
3  S-93（30px の箱）は廃する。S-180（描く幅の上限）は残す
```

| 効くところ | なぜ |
|---|---|
| **裁定 ①** | 未着手のダミーと着手済の実績が**どの倍率でも同一の形**になる |
| **不具合 2** | インクが 0 にならないので掴み代も 0 にならない。⭐ **見えない掴み代を作らずに済む** |
| **不具合 8** | 30px の箱が消えるので、短いタスクでも予定の端点（`GR-3` / `GR-4`）が塞がれない |

⛔ **代償**: 実績を「0 日」として持つ道が、掴んで縮める側からは無くなる。
⚠️ **プロパティパネル（`PR-4` / `PR-5`）から空にできることを、直す前に確かめる。**

---

## 3. ⭐ 不具合 1 —— なぜ「2 つ目」だけが成り立つのか

**1 つ目（実績を書かずに印だけ変える）は成り立たない。**
この造りは**状態を保存していない**。**列の組み合わせから毎回導いている**（表 T-019a）。

| | 条件 | 導かれる状態 |
|---|---|---|
| `PS-1` | `actualStart` が空 | **未着手** |
| `PS-2` | `actualFinish` がある | **完了** |

⇒ 実績を 1 つも書かなければ `PS-1` で「未着手」に導かれ続け、**印だけ変えても次に描いた瞬間に戻る。**
⛔ 状態を別に保存すると、列と印が食い違ったまま保存できる文書ができる（`FR-041` が禁じる導出値の保存）。

**2 つ目（ダミーを実績にして幅を変更せず）は成り立つ。**
表 T-021a の `PV-1`（未着手 → 完了）が置く値のうち、**1 語だけを変える。**

| | `actualDuration` |
|---|---|
| いま | 予定の期間 |
| 直したあと | ⭐ **`S-129`（1 稼働日）** |

⭐⭐ **同じ数が 3 つの道で使われることになる** —— `S-129` は
① ダミーを掴んだときに置く実績期間（`FR-043`）、
② 案 ①′ が置く実績の長さの下限、
③ ステータスマークの 1 押しが置く実績期間。
⇒ **どの道から入っても、最初に立つ実績は同じ 1 日である。**

⭐ **`FR-013` の理由の側とも合う** —— 同要求は「**1 日で終わる作業は日程表で頻出する**ので、2 手を要求すると道具として重い」と書いている。
⛔ **いまの `PV-1` は、自分の要求の理由と食い違っていた。**

⛔ **代償**: 「10 日の予定が、予定どおり 10 日かかって終わった」を 1 押しで言う道は無くなる。
実績の右端（`GR-6`）を掴んで引くか、`PR-5` に日数を入れる。

---

## 4. ⭐ 直す順 —— 3 波

| 波 | 件 | 直すこと | 動かす原典 |
|---|---|---|---|
| **1** | 5 | ダミーの上でカーソルを**スライド**に | 表 T-028 の `IN-2` に 1 行 |
| **1** | 6 | ダミーの `y` を本物の実績と**同じ式**に | なし（造りだけ） |
| **1** | 7 | マイルストーンのダミーの箱を**正方形**に | `FR-043` の「大きさは例外ではない」を撤回 |
| **1** | 3 | `Enter` で確定してアクティブを解く | 表 T-036 に 1 行 |
| **2** | ① | 掴み代をインクに合わせ、`S-93` を廃す | 表 T-023d の結び、`S-93` の行 |
| **2** | 2 | 着手済の実績の下限を `S-129` に | `FR-043` / `FR-011` に下限を 1 行 |
| **2** | 8 | `*---*` の予定の端点が掴めるようになる | ①・2 の結果 |
| **3** | 1 | `PV-1` の `actualDuration` を `S-129` に | 表 T-021a の `PV-1` の 1 語 |
| **3** | 4 | ⚠️ **切り分けてから**直す（1 と同根の疑い） | 1 の結果しだい |

⭐ 各波の頭で**まず赤を出す**（いまの造りが本当に間違っていることを測る）。
⭐ **ブラウザの実測は、すべての実装が終わってから 1 度にまとめて**行う。

---

## 5. ⭐ 8 件の根っこ（実物の行番号）

| # | 根っこ |
|---|---|
| 1 | 表 T-021a の `PV-1` が「予定の期間」を置く（⛔ 造りの誤りではなかった） |
| 2 | `schedule-layout.ts:1262` が幅 0 を返し、`reach = min(S-91, 幅÷2)` が 0 になる |
| 3 | `input-command-translator.ts:4013-4029` が `UNASSIGNED` を返す |
| 4 | ⭐ **切り分けた。1 とは別根であった** —— 記録の `down` の直後の `done` 行が `grab=GR-17` と告げており、押下点（x=1092）は印の 2px 右、`S-93` の 30px の箱の中であった。⇒ 裁定 ① で閉じる |
| 5 | `frame-loop.ts:774-809` で `GR-9` / `GR-17` / `GR-18` が `null` |
| 6 | `schedule-geometry.ts:1310` が `middle` を無条件で使う |
| 7 | 同 `1320` の箱が `min(1日, S-180) × 実績の帯` の縦長 |
| 8 | 表 T-023d の順 ＋ `S-93` の 30px（既定の倍率で 5 日ぶん。3 日のタスクでは予定の終了点 `GR-4` を丸ごと飲んでいた） |

---

## 6. ⭐ この巡で値打ちのあった 1 手

⚠️ **前に立つ者は最初、不具合 1 を「造りの誤り」として直しにかかろうとした。**
⛔ **原典を読んだら、`FR-013` の MUST と 表 T-021a の `PV-1` が、まさにその挙動を求めていた。**
⇒ **直していたら、MUST を黙って 1 つ消していた。**
⭐ **問うたら、`PV-1` の 1 語だけを替える道が返ってきた。**

---

## 7. ⭐ 直した結果（2026-09-10 の実測）

⭐ **自動試験** —— 202 ファイル・7478 件が緑（1 件 skip）、`npx tsc --noEmit` 0 件、
`npm run layers` 299/299、`check.sh` 終了コード 0。

⭐ **出荷ビルド** —— `dist/index.html` を `file://` で 1600 × 1000、既定の倍率（1 日 6px）。
探り針は `scratch/d2026-09-10-eight.mjs`。

| # | 押して測った値 |
|---|---|
| 1 | ステータスマーク 1 回で `actualDuration` ＝ **1 稼働日**（予定は 23 稼働日） |
| 2 | `actualDuration` に 0 を書くと **1** で返り、実績バーは **6px** 幅で描かれる |
| 3 | 予定バーを押すと選択 1 件、`Enter` で **0 件** |
| 4 | 印（x ＝ 1046〜1052）の **2px 右**を押して引いても実績は入らない。印の右半分なら入る |
| 5 | 印の上は **`ew-resize`**、予定の端も `ew-resize`、予定の本体は `grab` |
| 6 | `*---*` と `--->` のダミーの上端 **141** ／ 予定の下端 **132** ⇒ 下に出た |
| 7 | マイルストーンのダミーの箱は **24 × 24（縦横比 1）**。○・◇・△・□ で同じ |
| 8 | 予定の終点を掴んで引くと `finish` が **2028-01-20 → 2028-02-09**、実績は動かない |

⛔ **`S-93` の行は廃した** —— `_source/settings.json` からも `PointerSlop.dummyWidth` からも消えた。
⭐ **掴み代が印そのものになり、読む者が 1 人も残らなかったからである。**

⚠️ **意図して上げた基準が 2 つある**（理由はコミットに書いた）——
重複検出の基準を 3 群ぶん（廃した文を撤回の記録として逐語で残したため）と、
検査 37 の対応表（`SK-19` と `GR-9` / `GR-17` / `GR-18` を読み直した）。

---

## 8. ⛔ この見本が省いていること

⛔ **タッチ（指）とキーボードだけでの操作は考えていない。**
⛔ **`D-311`（第 8・9・10 章が空の器）は触れていない** —— 2026-09-06 に「試験の章は一旦そのまま」と裁定されている。
