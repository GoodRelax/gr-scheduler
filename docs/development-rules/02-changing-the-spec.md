# 02 仕様を変える手順 —— 影響をグラフで測ってから書く

⛔ **仕様書は文書ではなくグラフである。** 要求は表を指し、表は行を持ち、行は別の要求を指す。
**その指し先をたどらずに 1 か所を直すと、指している側が黙って偽になる。**

⚠️ **この手順は 4 セッション連続で飛ばされた。** 記憶にも技能書にも書いてあった。
⭐ **「打つコマンド付きの手順」として書いた瞬間から走り出した。** だから手順の形で書いてある。

---

## 1. ⛔ 手順（飛ばすな）

```
1. 調査   —— 仕様を読み切る。⭐ 半分以上は「実は在る」

2. 測る   —— 2 本とも走らせる（片方だけは不可）
     PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <対象>
     PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py <対象> <対象> ...

3. 計画   —— 閉路に入る対象を 2 つ以上触るなら、その閉路は 1 つの計画で 1 度に書く

4. 変更要求 —— change-request/ に書く。数の予測（tables / rows / uids / figures）を必ず入れる

5. 編集   —— オブジェクトごとに 1 回。⛔ 同じ行を 2 度書かない

6. 検査   —— 全部走らせる（04 を見よ）。数の予測と実測を突き合わせる
             ⛔ 仕様だけのコミットでも vitest を回す —— 仕様の試験は原稿を読む（npm run guard:spec が含む。09 の 1）
             ⚠️ 実測（2026-09-14）: LF-15 を足した仕様の波を check.sh だけでコミットし、T-221 の網羅の試験が赤くなった
             ⛔ 表・原稿を触ったら、同じコミットで npm run gen（04 の 6.13）

7. 変更履歴 —— A-appendix に 1 行足す。⛔ 過去の行を書き換えない

8. ⭐ 立ちどまる —— 変更要求の第 0 節に 3 つ答える（01 の 8 条 ①②⑧。機械検査が空欄を許さない）
```

### 測るときの注意

- ⚠️ **`impact.py` は 2 次まで（深さ 2）を答える。** 実測で 94% を捉え、深さ 3 は 4 倍の対象を集めて 1 ポイントしか増えない
- ⚠️ **`graph.py --cycles` は代用にならない** —— 全体を答え、最大の閉路が 180 対象なので編集の役に立たない
- ⚠️ **`induced.py` は種が 1 つも解決しないと拒否して止まる** —— **空のグラフの閉路 0 件は「合格」に見えるが合格ではない**
- ⭐ **グラフは DAG ではない。** 作業は「共有する対象」で分ける。順番では分けられない
- ⭐ **値の行と、その規則を持つ要求との間の小さな閉路は作法であり、直してはならない**
- ⛔ **導いた条項を着地させる前に、その条項で `impact.py` を打ち、届いた行ごとに `docs/development-records/rulings.md` を grep する。**
  **裁かれた行が勝つ。変わるのは導いた条項のほうである**
  ⚠️ 実測（2026-09-14）: 導いた条項（対話欄の `IN-5a`）が利用者の裁いた行（`IF-9`、`JDG-68`）とぶつかり、仕様だけを読む体しか捕まえなかった

### ⭐ 原稿だけを変えて刊行物が動かないとき

**グラフに種が無い。** そのときは **生成物のバイト一致が「仕様のオブジェクトが 1 つも動いていない」証明になる。**
⛔ **ただし「書かれていないファイル」と比べても何も証明しない** —— **生成物を先に消してから生成し、書かれたことを確かめてから比べる。**

---

## 2. ⭐ 数の予測は当たる（**外れたら数え落としが出ている**）

**変更要求に `tables` / `figures` / `rows` / `uids` の予測を書く。** 実測と突き合わせる。
⭐ **10 件以上の変更要求で予測どおりだった。** ⚠️ **外れたときは、たいてい「行を足したつもりが無かった」か「セルの編集を行の追加と数えていた」。**

---

## 2.5 ⛔ 予約した識別子は、草案を書いた時ではなく**当てる時**に空いていなければならない

⛔ **「いまの最大の次」で選んだ番号は、賞味期限が 1 コミットの主張である。**

⚠️ **実測（2026-09-21）** —— 2 つのセッションが同時に変更要求を書いた。
どちらも木を読んで「木が使う表の最大はこれである」と結論し、**同じ表番号を提案した。**
片方が着地した瞬間に、もう片方の根拠は偽になっていた。
⭐ **番号の帯を依頼文で配る手は、1 つのセッションの中の体までしか届かない** ——
**別のセッションの草案は互いに見えない。だから帯では防げない。**

⛔ **規則は 3 つである。**

1. **変更要求が「新たに作る」と言う識別子** —— 表の番号・行 ID の接頭辞・要求 ID ほか、
   名を 1 つ占めるもの一切 —— は、**草案を書いた時ではなく、それを仕様に当てる時に
   空いていること（MUST）。**
2. **その選択の根拠として引いた数**（登録簿の件数・木が使う最大の番号など）は、
   **当てる直前に測り直すこと（MUST）。**
   ⛔ **草案時の測定をそのまま当ててはならない（MUST NOT）。**
3. **測り直した日付を、その数のそばに書くこと（MUST）。**
   ⚠️ **日付の無い数は、いつのものかが書かれていないので、読んだ側が反証できない。**

⛔ **上の第 2 節の「数の予測」と取り違えるな。** ⭐ **予測は凍らせることが目的であり、
当時の測定がそのまま正しい。**
⭐ **本節が言うのは「いまの木についての主張」だけである** ——
**空いている・最大である・登録簿に何件ある。この 3 つは、当てる日に測り直す。**

⭐ **日付と一緒に、測った木（commit の sha）を書け。** ⚠️ 実測（2026-09-24）: 依頼文の「`JDG-400`・`JDG-401` が在る」は、起草した木 `ebc71984` では偽、1 回載せ直した `61bbd572` では真だった —— 同じ主張が載せ直しで入れ替わる。

**新しい接頭辞と新しい表を受け入れる前に、2 つを見よ:**

1. **新しい接頭辞が、既にある接頭辞と見間違えないか**（`DLM` と `DL`。Delay Diagnostics の頭字の `DD` も退けた）。⭐ 違う順序・違う表には、新しい接頭辞を `docs/spec/_source/row-id-prefixes.json` に登録する —— 1 つの接頭辞に 2 つの意味を持たせない（`JDG-274`）。
2. **同じ意味の表が既に無いか**（掴み点は新しい `HP` の表ではなく 表 T-246 の `HB` の行へ、見た目の既定は 表 T-217 へ入れた）。

⛔ **着地した変更要求に追補を書くな。** 変更要求のファイルが変わると、検査 62 がその第 2 節の「空いている」の主張を今の木で測り直して赤にする（2026-09-24 に 2 回）。⇒ **続きの仕様の直しは、新しい変更要求の番号で書け**（`CR-566` がその形）。予約した席は古い変更要求の中にそのまま残す。

---

## 3. ⛔ 穴に当たったときの作法

⛔ **その場で値を決めない。** ⭐ **ただし「止まる」は「丸投げ」ではない。**

1. **仕様書を先に読み切る** —— ⭐ **要求は `STATEMENT` の後の段落に MUST を置くことが多い。`RATIONALE` の後まで読む**
2. **確定した参考資料を当たる**（[03](03-implementation.md) の順序）
3. **それでも無ければ、根拠つきの推奨を添えて問う**
4. **裁定を受けたら 1. の手順で当てる**

⚠️ ⭐ **参考が仕様と食い違うことがある。仕様が勝つ。**
⚠️ ⭐ **参考が「正」を自称していても信じない** —— 冒頭で「決定ではない」と書いている草案を、仕様書が MUST で正としていた実例がある。**在るかどうかだけでなく、指し先の資格を見る。**
⭐ **新しい規則を書く前に、既にある行の値を疑え** —— 法則は仕様に在り、値だけがそれを殺していることがある。

### 実装中に穴を見つけたら

⭐ **コードに ⛔ を付けて「何が無いか」を書き、報告する。** 値を推測して埋めない。
⚠️ **これは実際に効いている** —— アンカーの 9 点がどの表にも無いことを ⛔ 付きで書いて推測しなかったコードがあり、後の変更要求がその穴を閉じられた。
⛔ **逆に、注記に値を写すと腐る** —— 掴み代の注記が数を写しており、原稿を変えても古い数を言い続けた。**注記は行 ID を名乗る。**

---

## 3.5 ⛔⛔ 原稿の行は、それを読むコードと同じ巡で着地させろ

⛔ **`_source/` の原稿に 1 行足しただけで、単体試験が 54 ファイル落ちた。**

```
Error: table T-016 holds PR-21, and nothing says which entity holds its value
  heldByOf  src/adapter/screen-renderer/properties-panel.ts:488
```

⭐ **原因は、生成された名簿を読む側が「知らない行」に対して**読み込みの時点で投げる**ことである。**
⇒ **その 1 ファイルを import する試験は 1 件も走らず、`(0 test)` として落ちる。**
⚠️ **落ちた 54 ファイルのうち、その仕事に関係するのは 1 つも無い。**

⛔ **投げるのが悪いのではない** —— **黙って飛ばせば、足した行が画面に出ないまま緑になる。**
⭐ **悪いのは着地の順である。**

```
⛔ やってはならない  原稿に行を足して commit し、読む側は次の波に回す
⭐ やること          原稿の行と、それを読む側の直しを 1 つの波にする
                     ⇒ 波を分けるなら、⛔ 分ける前に「読む側は投げるか」を測れ
```

⚠️ **どの生成物が「投げる側」かは、生成器ではなく**読む側**に書いてある。**
⭐ **測り方**: 原稿に 1 行足して `npm run gen` し、`npx vitest run` を 1 ファイルだけ走らせる。
**`(0 test)` が出たら、その名簿は投げる側である。**

⛔ **逆の形もある —— 設定値の行（`settings.json`）は、生成器の群に載るまで src に出ない。** `tools/generate_entity_types.py` は群ごとに手で並べた鍵しか書き出さないので、gen:check は緑のまま、行はどこにも届かない。
⚠️ 実測（2026-09-23）: `CR-551` の波 0 が `S-325`〜`S-340` を足して緑だったが、1 行も src に無く、並べた体 2 つが止まり、1 つは値を手で写した。
⇒ **設定値の行を足す変更要求は、仕様の波で生成器の群にも足し、コードの体には生成された定数の名を渡す**（`NOT_STORED_DELAY_MARK_SIZES['S-328']` の形）。

---

## 4. ⚠️ 文書を編集するときに踏んだ罠（繰り返すな）

- ⚠️ **`****` は片方が生の `**` になる**（空白を 1 つ入れる）。⛔ **表の検査は見えない。刊行物の描画検査だけが見る**
- ⚠️ **CJK の直後に `**` を開き、その直後にコードスパンを置かない**（句読点や ⚠️ の直後は安全）
- ⚠️ **変更履歴の行にリテラルの `|` を書くと列が割れる**
- ⚠️ **「表 X の N 行」と書くと検査が「X は N 行を持つ」と読む** —— 生成物の何が動いたかは**行 ID を名指す**
- ⚠️ **値の表・用語集に `（MUST）` を書かない** —— **値は表、規則は要求**
- ⛔⛔ ⭐ 新: **書き換える文字列が 1 度しか現れないことを、書き換える前に数えろ**
  —— **`S-209` の注記を書き換えたつもりが、同じ語（「⛔ 0 にしてはならない」）を先に持つ
  `S-208` に着地し、そちらの MUST NOT を丸ごと消していた。**
  ⭐ **捕まえたのはその表を読む試験である**（`t-051-hf-15-grabbing-a-row.test.ts`）——
  **仕様の逐語を主張する試験は、こういう取り違えを捕まえるために在る。**
  ⛔ **`find` で最初の 1 件を取るな。`count(...) == 1` を主張してから `replace` しろ。**
  ⭐ **直したら、元の内容を commit から取り直して**バイトで突き合わせろ** ——
  記憶から書き戻すと、消えたことに気づけないまま似た文が残る。
- ⛔⛔ ⭐ 新: **「同・」で前の行を継ぐ表から、頭の行を退役させるな**
  —— **表 T-109 の `IC-46` を退けたところ、`IC-47` / `IC-48` の「同・」が継ぐ先を失い、
  1 つ上に来た無関係の行（基準日線）を継ぐようになった。**
  ⚠️ **`grep` で `S-66` を数えると 0 件になる** —— **その表から設定値の名が消える。**
  ⭐ **捕まえたのは、その表を歩いて設定値を引く試験である。**
  ⛔ **退役させるときは、継ぎの頭を次の行へ移し替えてから退けろ。**
- ⛔ **検査 39・42 が照らす日本語の逐語は、途切れずに 1 行で書け** —— 検査は文字の並びで照らすので、折り返しで 2 行に割れると黙って効かなくなる
- ⛔ **仕様に行を足すときは段落の終わりに足せ。** 2 つの文のあいだに挟むと、両方を続けて引く試験の注が割れる
  —— ⚠️ 実測（2026-09-24）: 表 T-017a の結びの 2 文のあいだに 1 行挟み、`tests/unit/uf-32.test.ts` の引用が割れて検査 42 が 354 → 355。
  ⭐ **検査 42 が動いたら、自分のファイルではなく、自分が触った文を `check-quoted-source.py --list` で探せ**
- ⛔ **設計の数を丸めて書くな。導き方を書け** —— 丸めは不変条件を割る。⚠️ 実測（2026-09-10）: `S-5` の 0.57 は 16 ÷ 28 の丸めで、`S-4` の既定が自分の下限を割った
- ⚠️ **退役した行 ID の語は、`change-request/` と検査の基準線のファイルの中では履歴である。⛔ 消すな**（`S-93` の例。退役した行の名簿 `.claude/skills/spec-graph-check/retired.py` が黙らせている）
- ⛔ **行を退役させたら、`retired.py` に席を焼け。理由も書け**
- ⛔ **原稿の置き場を取り違えるな** —— 生成物を手で直すと、次の `npm run gen` が黙って戻す:

  | 足すもの | 書く所（原稿） | ⛔ 書かない所（生成物） |
  |---|---|---|
  | 設定値 | `docs/spec/_source/settings.json` | `tbl-settings.md` |
  | 表示語 | `docs/spec/_source/display-words.json` | —— |
  | プロパティ項目 | `docs/spec/_source/property-items.json`（＋ `property-items.schema.json` の enum）。読む側は `properties-panel.ts` の 1 つだけ | `src/adapter/screen-renderer/property-items.json` |
  | コンポーネントの辺 | `docs/spec/_source/components.json` | 部品図 |
  | MSPDI の子の並び順 | `tools/generate_mspdi_child_order.py` が XSD から作る（`JDG-251`） | `mspdi-child-order.json`（`sourceXsdSha256` と `bodySha256` が見張る） |
- ⚠️ **`docs/review/duplication-baseline.txt` の 表 T-023d まわりの 3 群は、撤回の記録を逐語で残した意図した重複である。⛔ 畳むな**
- ⛔ **`tools/fix_line_breaks.py` は仕様と変更要求のファイルにだけ当てよ** —— 2026-09-21、`rulings.md`・`defects.md` の既存の行を数百行書き換えた
- ⚠️ **番号を振り直さない。** 欠番はそのまま残す
- ⚠️ **仕様書は機械検査の番号を 1 度も引いていない。** 規則だけ書き、検査番号は harness 側に置く

### ⭐⭐ `RATIONALE` の中の `（MUST）` は違反ではない

⛔ **`RATIONALE` に規則を書いてはならない、という規則は無い。作るな。**

**実測（2026-09-05）**: **`docs/spec/` の `RATIONALE` の段落は 120。
うち `（MUST）` か `（MUST NOT）` を持つものが 21、「〜すること」で終わるものが 8、合わせて 25。**
⇒ ⭐ **これは違反の 25 件ではなく、いまの流儀そのものである。**

⭐ **上の §3 の 1 番が既にそう述べている** —— 「**要求は `STATEMENT` の後の段落に MUST を
置くことが多い。`RATIONALE` の後まで読む**」。⛔ **禁じる側の規則を足すと、この 1 行と食い違う。**

⛔⛔ **これを禁じる機械検査を作るな。**⚠️ **一度、`RATIONALE` の中の MUST を数える検査が
提案されたことがある。⛔ 数えたら 25 件出たが、**その 25 件が破っている規則がどこにも無かった**。
⇒ ⭐⭐ **検査を作る前に、その検査が守らせる規則が書かれているかを確かめよ。**
**書かれていないなら、作るのは検査ではなく規則の捏造である。**

⚠️ **`（MUST）` を置いてよい場所と、置いてはならない場所は別の話である** ——
**値の表と用語集には置かない**（上の箇条書き）。⭐ **`RATIONALE` は要求の一部なので、置いてよい。**
