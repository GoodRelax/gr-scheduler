# 05 進め方 —— 記録・順番・並行・受け入れ・止まり方

⛔ **8 条の ⑥ と ⑦ は原則としては既にある。** ⚠️ **原則のままでは走らないことを実測しているので、本書は手順として書く。**

---

## 1. ⛔ 止まっても再開できるように記録する

⚠️ **止まる理由は選べない** —— 停電・枠切れ・要約による文脈の消失・単に日をまたぐ。
⭐ **どれも「作業中の頭の中」だけが消える。** ディスクの中身は残る。**だから頭の中をディスクへ落とす。**

### ⭐ 記録は `docs/development-records/` に置く

⛔ **git が答えられることを書かない。** 何を変えたかは `git log` と `git diff` が持つ。
⭐ **記録が持つのは、git が答えられない 3 つだけである:**

```
1. 現在地   いまどのユニットのどの段にいるか（次の一手が読める形で）
2. 状態表   各ユニットが 未着手 / 実装済 / 試験済 / 受入済 のどれか
3. 決めた理由と踏んだ罠   ⭐ 変更要求に載らない、作業中の判断
```

### ⛔ 書き足す時機（**あとでまとめて書かない**）

| いつ | 何を |
|---|---|
| **ユニットの段が 1 つ進んだ直後** | 状態表の 1 マスを書き換える |
| **裁定を受けた／罠を踏んだ／自分で決めた直後** | 1 行足す |
| **危ない操作の前** | 何を壊しうるか、どう戻すかを 1 行 |
| **枠が細ってきたとき** | ⭐ **要約が入って検討事項が消える前に。** 消えてからでは書けない |

⚠️ **「終わってから書く」は失敗する。** ⛔ **実測**: 引継ぎを最後にまとめて書いた回で、古い節が残って次の担当を誤らせた。

### ⭐ 記録は機械が突き合わせる（**腐らせない**）

⛔ **状態表は放っておけば腐る。** ⭐ **機械検査が、記録と実物のズレを落とす:**

```
・その波のユニットが 1 つでも表から漏れていれば落ちる
・表に無いファイルが並んでいれば落ちる
・⬜ 未着手 と書いてあるのにファイルが stub でなければ落ちる
・段が進んだと書いてあるのにファイルが stub のままなら落ちる
```

⚠️ **「未着手」は行数や export では判定していない** —— **stub 生成器が書くはずの全文と 1 バイトでも違うか**で判定する。
⭐ **記録は「波を宣言する」** —— 先頭に `**対象**: \`src/adapter\`` の 1 行を置く。検査はそれを読む。

### ⛔ 記録の書き方（機械が読むので守る）

```
⛔ 数を書くなら、そばに測り方（何を何で割ったか・どのビルド・窓・倍率で）と日付と木の sha を書け
   ⇒ 書いていない数は反証できず腐る。前の数と食い違ったら、消さずに両方残せ
   ⚠️ 実測（2026-09-05）: 引継書の「61.73 日」を但し書きごと落として写した。直に測ると 64.17 日であった
⛔ docs/development-records/ の文書（引継書を含む）に、頭の欄が行 ID だけの表の行を書くな
   ⇒ docs/spec/_source/row_id_prefixes_json_to_md.py は records も歩き、その行を数える。
     登録の無い接頭辞も仕様の接頭辞（FR ほか）も、gen:check と検査 27 を落とす。ID の後ろに語を添えよ
⛔ 台帳・裁定の欄に「」で引く言葉は、逐語でなければ「」に入れるな
⛔ rulings.md の状態の欄の書き方は 06 の 7
```

### ⛔ 引継書は状態だけを持つ（検査 72）

⭐ **引継書（`docs/development-records/handoff.md`）が持つのは、いまの状態だけである** —— 着地したもの（commit の sha）・止めたもの・次にやること・利用者の確認待ち。
⛔ **学びと規則を引継書に置くな。** ⚠️ 引継書に置いた学びは 1 巡で消える（`README.md` の 2 節の梯子の一番上の段である）。

```
巡の終わりの手順
1. その巡の学びを、本フォルダのどこへ下ろすかを決める
   ⭐ 手順（打つコマンドつき）か機械検査を先に考える。原則の文は最後の手段
2. 下ろす前に、同じことが既に規則に無いかを grep する。在れば足さずに、その項に実測を 1 行足す
3. 下ろしたら、引継書からその項を消す。古い巡の節は、sha を並べた 1 行の履歴へ縮める
4. ⛔ 引継書に「裁定を呼ぶ」と書く前にも、10 の最後の 3 手を打て
   ⚠️ 実測（2026-09-22）: 前の版が「裁定を呼ぶ」とした 3 件は、並行セッションに全部「既存の裁定から導ける」と反証された
```

⭐ **検査 72（`check-handoff-holds-state.py`）が機械で守らせる** —— 最新の日付の節（`> 🆕` で始まる最初の節）の外に「学び」の行が残るか、「規則へ下ろす前の控え」の見出しが在れば赤である。
⇒ **次の巡が自分の節を書いてコミットするとき、前の巡の学びが下りていなければ通らない。**

### ⛔ 止まるときは緑で止まる

⭐ **中断する前に、機械検査と型検査を通しておく。** ⚠️ **赤いまま止めると、次に開いた者が
「自分が壊したのか、前から壊れていたのか」を切り分けるところから始めることになる。**
⭐ **通せないなら、記録に「いまここが赤い。原因はここまで絞れている」と書いてから止まる。**

---

## 2. ⛔ 実装の順番は依存で決める（**好みで決めない**）

⭐ **順番は既に半分決まっている** —— 層の規則（表 T-061）が依存を内向きだけに限っているので、
**内側の層は外側を待たない。**

```
documentModel → layoutEngine → UseCase → Adapter → Framework
```

### ⭐ 波の中の順番は、継ぎ目と公開エントリで決める

1. **その波が実装する継ぎ目**（表 T-065）**を宣言している側が先** —— 宣言が無ければ実装できない
2. **コンポーネントの公開エントリが先、内部ユニットが後** —— 外から見える形が決まらないと、他が書けない
3. ⛔ **依存の向きに逆らって始めない。** 生煮えのものに依存したコードを書かない（8 条の ⑥）

### ⚠️ 内側から順に作ると、最後の波まで実物が見えない（**実測**）

⛔ **`index.html` が読むのは `single-html-shell.ts` であり、いまそれは中身の無いユニットである。**
**つまり全部そろうまでブラウザには何も出ない。** ⚠️ **[04](04-verification.md) が「緑の試験は動くアプリを意味しない」と言う一方で、
実物を確かめる手段が最後まで存在しないことになる。**

⭐ **だから波の中で、まず 1 本の縦線を通す。** 依存の順を守ったまま、**「画面に何かが出る」最小の集合を先に選ぶ。**
⚠️ **そこから先は、実物を見ながら進められる。** ⛔ **波の全部を書き終えてから初めて起動する、という順番を採らない。**

### ⭐ 縦線のために、次の波へ先に手を出してよい

⛔ **波は「順番」であって「柵」ではない。** ⭐ **縦線を通すのに必要なら、次の波のユニットを先に書いてよい。**

```
許される     縦線に必要な最小限だけを、次の波から先に書く
             ⭐ 例: Adapter を書いている途中で、画面に出すために
                    Framework のシェルを先に動く形にする
許されない   ⛔ 依存の向きに逆らうこと（外側の層に内側が依存する）
             ⛔ 段を飛ばすこと（4. の ✅ を経ずに他が依存する）
```

⚠️ **先に手を出したら、記録に「どの波のどれを、なぜ先にやったか」を書く** ——
**状態表は波ごとのファイルなので、書かないと「その波は未着手」と読まれる。**
⭐ **機械検査が状態表と実物を突き合わせるので、書き忘れれば落ちる。**

---

## 3. ⭐ インターフェースが決まったら並行してよい

⛔ **「決まった」は「実装が動く」ではない。** ⭐ **並行を許す条件はこれだけである:**

```
公開エントリに、公開する型と署名が書かれ、型検査が通る
```

⚠️ **そこから先、それに依存する側は中身を待たずに書ける** —— 型が契約だからである。
⛔ **中身が未実装のまま依存側の「試験」を緑にしてはならない**（4. を見よ）。
⭐ **公開エントリにまだ無い新しい関数を、並行する 2 体が両側から使うなら、その名前と引数の順を両方のブリーフに同じ字面で書け。**
⚠️ **実測（2026-09-14）: 新しい 3 つの関数でそうしたところ、呼ぶ側の体は tsc でその 3 つの名前だけが欠けた状態で書き終えた。**
⭐ **欄・引数も同じである**（2026-09-16、`RowTitle.fontPx` と `frameLoop` の 12 番目の引数で、2 波とも合流の衝突 0）。⛔ **試験の体には型の名を import させず、構造の型で渡させよ** —— その型を export するかは検査 26b が決める。

### ⛔ 並行の単位はファイルである。⭐ **隔離の単位は worktree である**

⛔ **同じ作業木で複数の体を走らせない。** ⚠️ **実測（11 体を同じ木で走らせた回）:**

```
各体が npm test と check.sh を何十回も走らせ、互いの編集で数字が動くたびに
「これは自分のか」を調べ直した —— 報告の半分がその説明で埋まった
1 体が共有の scratch/ を rm -rf した
1 体が死に、報告ごと失われた（作業はディスクに残っていたが、何が未達かの申告が消えた）
```

⭐ **実装する体は `isolation: "worktree"` で隔離する。** ⛔ **体には検証をさせない。**

| 誰が | 何を走らせるか |
|---|---|
| 実装する体 | ⛔ **走らせない。** 書いて、**変えた点と、できなかった点**だけを報告する |
| 前に立つ者 | ⭐ **合流点で 1 回だけ** —— `precheck` → `typecheck` → `test` → `layers` → `check.sh` → 実物 |

⚠️ **体に基準の数字を渡さない。** ⛔ **渡すと、走っているあいだに動いて必ず食い違う。**
⭐ **「自分で先に測れ」とだけ言う。**

⛔ **走っている体の途中結果を、確定した数字として報告しない。** ⚠️ **実測**: 型検査を「0 エラー」と
報告した後、まだ生きていた体が書き足して 7 件に戻った。**体が終わったと確定するまで、その領域の数字は言わない。**

### ⛔ 並行の単位はファイルである

⚠️ **同じファイルを 2 人（2 体）で書かない。** 衝突する。
⭐ **実測で効いた形**: 1 体 1 ファイル。集約 6 つを並行させて 1 巡で揃った。
⛔ **効かない形**: 仕様書への適用。**必ず 1 体でやる**（同じ文書を並行に書けば壊れる）。

---

## 4. ⛔ 試験が通ってから、他所での参照を許す

⭐ **ユニットには段がある。** ⛔ **「受入済」になるまで、他のユニットがそれに依存してはならない。**

```
⬜ 未着手    stub のまま
🔧 実装済    製品コードを書いた。⛔ まだ誰も依存してよくない
🧪 試験済    別の者が docs/spec だけを読んで試験を書いた（04 の 1.）
✅ 受入済    その試験が緑。型検査・層の検査も緑
             ⭐ ここで初めて、他のユニットがこれに依存してよい
```

⚠️ **段を飛ばさない。** ⛔ **「あとで試験を書く」は、依存が積み上がった後では書けなくなる** ——
落ちたときに、どのユニットの欠陥かを切り分けられなくなるからである（8 条の ⑥ の「不具合範囲を局所化し」）。

⭐ **例外は 3. の「型だけ先に決める」だけ** —— 型に依存してよいが、**振る舞いには依存しない。**

---

## 5. ⭐ 1 巡の形（**まとめ**）

⭐ **巡の頭（毎回、この順）:**

```
0. ListAgents で、動いている相手を確かめる（13）
1. docs/development-records/rulings.md を読む          ⛔ 問う前に。06 の 1 の 0
2. python tools/ledger_metrics.py --start             基準を刻む。⛔ 手で書くな
3. npm run guard:start、npm run gen:check              ⛔ 赤い試験を疑う前にこれ
4. 引継書（docs/development-records/handoff.md）の冒頭の節を読み、書かれた数を git log で測り直す
```

```
1. 記録の「現在地」を読む                     ← 停電から戻ってきてもここから
2. 次のユニットを依存の順に選ぶ                （2.）
3. 公開エントリの型を書く → 型検査が通る       ← ここで並行を解禁（3.）
4. 製品コードを書く                            🔧
5. 別の者が仕様だけを読んで試験を書く           🧪（04 の 1.）
6. 緑にする。型検査・層の検査も通す             ✅ 受入済
7. 記録の状態表を書き換える                     ← 段が進むたび。あとでまとめない
8. 緑のまま止まる                               （1. の最後）
```

---

## 6. ⛔ 前に立つ者はオーケストレーションに徹する

⛔ **前面のセッションが製品コードと試験を自分で書かない。** ⭐ **書くのはサブエージェントである。**

```
前に立つ者がやること     読む・測る・決める・分ける・検証する・記録する
                         仕様への適用と原稿の移送（⛔ これは 1 体でやる。3. を見よ）
サブエージェントがやること 製品コードを書く／試験を書く／踏査する
```

### ⭐ なぜか（**実測**）

| | |
|---|---|
| **文脈が保つ** | ⚠️ **試験を別の者に書かせても、親の文脈はほとんど減らない**（195 万 → 35k）。⛔ **前に立つ者が自分で書くと、検証に使える文脈が残らない** |
| **04 の 1. がただで手に入る** | ⭐ **書いた者に試験を書かせない**が、分業の副産物として成立する。⚠️ **実測: 仕様だけを読ませた 3 体が、本物の欠陥を 8 件見つけた** —— 型検査も 684 件の試験も 1 件も捕まえていない |
| **並列が効く** | ⭐ 1 体 1 ファイルなら並行できる（3.）。⛔ **同じファイルを 2 体で書かない** |

### ⭐ 仕様への適用は 1 巡にまとめる

⛔ **変更要求ごとに「測る → 書く → 当てる → 検査 → 変更履歴」を回さない。**
⚠️ **実測**: 5 本を 1 本ずつ回して 5 周した。⭐ **閉路が無いことは最初の 1 回で分かっていた。**

```
1. 種を全部集めて induced.py を 1 回      ⭐ 閉路の有無がここで出る
2. 閉路に入る対象を 2 つ以上触るなら、その閉路は 1 つの計画で 1 度に書く（規則 02 の 3.）
3. 閉路に入らない分は、変更要求を全部書いてから、まとめて当てる
4. 検査は最後に 1 回
```

⚠️ **数の予測は変更要求ごとに書く** —— まとめて当てても、外れたのがどれかは分かる必要がある。

### ⛔ それでも前に立つ者が手を動かす場面

```
1. 仕様書への適用と原稿の移送   ⛔ 必ず 1 体（同じ文書を並行に書けば壊れる）
2. 変更要求を書くこと           ⛔ 影響を測った者が書く
3. 分けようがない小さな直し     ⚠️ 1 ファイルの数行。⭐ 迷ったら分ける
```

### ⛔⛔ 台帳の刈り取りも体に出す

> 「**実測済みは、当然エージェントを使って移させろ**」

⛔ **`defects.md` から `fixed-defects.md` へ行を移すのは、上の 3. の「小さな直し」ではない。**
⭐ **見分け方は行数ではなく、作業が機械的かどうかである** —— **機械的なら、それは体の仕事である。**
⚠️ **行数で線を引くと、機械的な移送まで前に立つ者が自分で抱え込んでしまう。**
⇒ **1 ファイルの数行であっても、小さな script で片づく形をしているなら、体に出せ。**

⭐ **代わりに前に立つ者がやるのは、体が出した結果を自分で測り直すことである**（下の「受け取り方」）。
⛔ **体の報告の数を写すな。**⭐ **実測**: 移す前と後で 400 行の md5 が一致することを、
前に立つ者が自分で取り直して初めて「1 バイトも変えていない」と言える。

⛔ **体に渡すときに必ず書く 1 行**: **件数表の `<!-- ledger-metrics -->` ブロックを手で書き換えるな** ——
**巡の開始時の基準行がそこに刻まれており、消すと巡の基準が失われる。**
⛔ **同じ理由で、`python tools/ledger_metrics.py --start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな** —— checkout は基準ごと消す。

⭐ **巡を閉じる前に刈り取る**（`JDG-32`）。⛔ **`defects.md` に `実測済` と `取下げ` を 1 行も残さない。** 残りを数える 1 行:

```bash
awk -F'|' '/^\| D[A-Z]*-[0-9]+ /{ if ($7 ~ /実測済/ || $7 ~ /取下げ/) n++ } END{ print n+0 }' docs/development-records/defects.md
```

⇒ **0 でなければ、まだ閉じていない。**

### ⚠️ 受け取り方（**鵜呑みにしない**）

⛔ **サブエージェントの報告を鵜呑みにしない。** ⭐ **必ず自分で開いて確かめ、検査も自分で走らせる。**
⚠️ **実測: 行番号は 1〜25 行ずれて報告される。逐語の中身は正しいことが多い。**
⛔ **落ちた試験を自分で直さない** —— 取り出し方が怪しいと思ったら**書いた側へ差し戻す。**
⚠️ **下書き（`zz-*.test.ts` など）を残すことがある。終わったら `git status` を見る。**

---

## 7. ⛔ 5 段のパイプライン

⛔ **前に立つ者が、体の報告を自分の推論の入力にしてはならない。** ⚠️ **実測: そうした結果、
存在しない二者択一を利用者に出した。**⭐ **本節はそれを起こさない順序である。手順であって心構えではない。**

```
段 1  前に立つ者が「大雑把に」指示する        ⭐ 詳細を作り込まなくてよい
段 2  ⛔ 体が仕様を読み、実装方針を提案する    ⛔ ここでコードを書かせない
段 3  前に立つ者がその方針の可否を決める      ⛔ 数字と file:line が無い方針を通さない
段 4  実装の体と試験の体を「同時に」走らせる  ⛔ 試験の体は docs/spec だけを読む
段 5  実装が入ってから試験を当て、前に立つ者が最終報告をまとめる
```

### ⛔ 段 2 を飛ばしてはならない

⚠️ **飛ばすと、前に立つ者の思い込みがそのまま体の台本になる。**
⭐ **実測: 段 2 を踏んだ回は、体が台本の誤りを 7 件返した** —— そのうち 5 件は
「仕様に無い」と `src/` の `STOP` コメントが書いていたのに**実際には在った**ものである。
⛔ **`STOP` コメントを根拠に「仕様に無い」と言わない。必ず自分で当たる。**

### ⛔ 段 3 で通してよい方針の条件

```
1. 触る要求・表・行を行 ID で名乗っている
2. file:line が付いている（⚠️ 行番号は 1〜25 ずれる。逐語で確かめる）
3. ⛔ 値を選ぶ提案なら、選択肢ごとの帰結が数で出ている
4. 仕様に行が無いなら、そう申告している（⛔ 発明していない）
```

### ⛔ 波を出す前に commit する

⛔ **作業木はセッションの開始コミットで切られる。HEAD ではない。**
⚠️ **実測**: 設定値の行を書いて commit せずに波を出したところ、体の木にその行が無く、
⭐ **体は発明を拒んで STOP を残した（正しい判断）。誤っていたのは波の出し方である。**

```
1. commit する
2. その sha を five-stage へ baseCommit として渡す
3. ⭐ 体は `git merge --ff-only <sha>` で治してから始める
```

⭐ **`baseCommit` を渡さなければ `five-stage.js` が拒む。**⛔ **覚えている必要がないようにしてある。**

### ⛔ 段 4 の並行は「実装 ‖ 試験」であって「実装 → 試験」ではない

⚠️ **試験を後から書くと、実装に合わせた期待値になる。**
⭐ **同時に走らせれば、試験の体は実装を見られないので、仕様からしか書けない。**
⛔ **前に立つ者が、自分が動かした値の試験を自分で直してはならない** ——
⚠️ **実測: `S-85` を動かした当人が試験を直し、3 か所のうち 2 か所しか直さず、
しかも直した 1 つは `FR-017` のフォント補正を落としていた。緑がそれを隠した。**
⭐ **仕様だけを読む試験の体は、実装の体の読み違いを止める**（2026-09-24、H2 のラベルとマーカーの数え方）。逆に、試験の体の見本が実物と違うこともある ⇒ **食い違ったら、仕様か実物の生成で裁け。**
⛔ **壊し試験は合流した木で数える**（04 の 2）。実装の体の木では、試験の体の試験がまだ無いので 0 になる。

### ⭐ 直しの巡の波（`JDG-12`。2026-09-08 に 15 体で回した形。⛔ 順を入れ替えるな）

> 利用者「エージェントに実装と単体テストをやらせて、すべて終わったら、一気にブラウザを操作する実測テストをやればいいだろ？」

```
波0 反証   1 体につき 5〜8 行。⛔ 直すことを禁じる。FALSE / TRUE / 誤診 の 3 択で裁かせる
波1 裁定   波0 が挙げた問いを 1 通にまとめて利用者へ。⛔ 小出しにしない
波2 仕様   ⛔ 1 体だけ。直列（6）。体に並行で書かせると同じ文書が壊れる
波3 修理   ファイルで割って並列。⛔ 同じファイルを 2 体に渡すな
波4 試験   仕様が変わって古くなった逐語を持つ試験を直す。⭐ 波3 と同じ巡に必ず来る
波5 実測   出荷ビルドを 1 回起動して、まとめて押す
波6 台帳   機械的なので体に出す（6）
```

⛔ **波ごとにコミットしろ**（下の「波を出す前に commit する」）。そのうえで:

- ⭐ **並べた体の継ぎ目には、必ず借りが残る**（型検査の赤、試験が知らない署名）。⇒ **並行の代償として、合流の後に締めの体を 1 体見積もれ** —— 赤ごとに `docs/spec` でどちらが正しいかを決め、試験を署名に合わせる（2026-09-17、`cdca63ac`）。
- ⛔ **実装と試験が食い違った赤は、直す前に反証の体へ渡し、FALSE / TRUE / 誤診 で裁かせよ。** 誤診（仕様の穴）は小さな仕様の波で閉じ、実装と試験の体へは署名と読みだけを伝える（2026-09-15、`CR-380` の穴を両側の体が別々に見つけた）。
- ⛔ **仕様だけを読む試験の体の `it.fails` も仮説である** —— 5 つのうち 3 つは試験の誤り（場面を組んでいない）だった（2026-09-22）。台帳の行を書く前に反証させよ。
- ⛔ **反証する体の「閉じた」も仮説である** —— 2 体目に反証させると 94 件のうち 18 件が覆った（2026-09-22）。台帳の行を閉じる判定は、別の体に反証させてから閉じる。
- ⭐ **基準線を超える変更要求を利用者の留守に当てるなら、`refactor` ではなく合流用の枝に止め、戻ってから問え** —— 関門を割らずに次の変更要求を積める（13 の「枝の見せ方」も守る）。

### ⭐ 割る波の手順（段 4〜7.5、`CR-432`・`CR-438`・`CR-554` で効いた形）

```
1. 割る前に、計画したユニットすべての名と責務の文を 07 の R2（命名・品詞・R2.2a の 5 段・R2.2b・POLA・CQS・依存の向き）で
   別の体に審査させる。⚠️ 1 つ見つかれば他にもいる（2026-09-24、共変の数 0.60 が名を嘘にしかけた）
   ⇒ 名と責務の文の確定表を利用者に見せてから、最初の src の波を出す（JDG-548・JDG-550）
2. 割る順は大きさではなく「変わる頻度 × 大きさ」で決める
   ⚠️ 書かれて以来 0 回しか変わっていない builtMerge を割っても、直す時間は 1 秒も減らない
   継ぎ目は 3 つの証拠で決める（CR-432 の 4 節）: 要求の所有（表 T-075 の「負う要求」）が提案し、
   共変更が拒み（04 の 6 の数え方で）、R2.2a の 5 段が確かめる
   ⛔ 出口は行数で判じない（JDG-284）。起点を別の要求と共有する要求の数で判じる
3. 照合器は、分割前の木だけを読む別の体に作らせる（公開エントリを動的 import し、多数の入力の出力の sha256 を並べる台本）
   ⛔ 作ったら壊して感度を測れ（10 件）、照合器自身の分岐網羅を報告させよ
     （NODE_V8_COVERAGE ＋ v8-to-istanbul。c8 の CLI はファイルを黙って落とした）
   ⚠️ 最初の版は 3 件中 2 件を取り逃がした。境界の ±1・上下限・掴みの連なりを足して 10 中 8
   ⭐ 試験が 0 件しか赤くならない入れ替え（層の中の順）を、照合器は捕まえる
4. 波ごとに、前に立つ者が合流した木で回す: 照合器（バイト一致）・vitest・check.sh・gen:check・tsc
   ⛔ 検査 55（コメントの密度）: 小さな兄弟に TRAP・WHY が集まり 10% を破る ⇒ コメントの規則の 5 節どおり台帳へ起こし、消した文を報告させよ
   ⛔ 検査 45（同じ式の繰り返し）: export を足すと、下限の下にいた重複が 20 字句を超える
     ⇒ 型なら既存の名（Point など）で解け。コードなら理由を書いて上げる（04 の 6.3）
   ⛔ 関数を動かすと、ほかのファイルのコメントの「どのファイルにあるか」が腐る ⇒ 波ごとに掃け
   ⛔ 体の「この試験が TRAP を守っている」は 8 件中 2 件が偽だった ⇒ コメントを消す前に 1 件ずつ壊して確かめよ
5. 新しい兄弟のファイルは、表 T-075 の行が入るまで検査 26b・18・44 を赤くする
   ⇒ 割る体は、前に立つ者が行を書くまで、26b を「名の一覧（すべて入口と兄弟のあいだ）」で判じる
6. 出口の赤が分割のせいかを知るには、同じ作業木で src を分割前の sha に戻して同じ試験を走らせる
```

### ⭐ UI の数は触れる見本で決める（`JDG-180`〜`JDG-193`）

⭐ **大きさ・重なり・掴み代・札の置き方は散文では決まらない。** 2026-09-18〜19 は 1 ファイルの見本で、散文で決まらなかった 14 の問いが 2 日で測った規則になった（`previous-project-result/16-grab-area-sizing/`）。

```
形       1 ファイルの HTML。サーバ無しでダブルクリックで開く。大きさはすべてつまみ（スライダー）
         ポインタの下で答えた掴みを名指す表示と、いまの値の一覧（利用者が貼り返せる）
値       操作対象 × 向きごとに独立させる。⛔ 1 つのつまみがほかの対象を動かすのは密結合（利用者「蜜結合となって悪手」）
掴み代   縁取りなし。掴む物と離れた色相の薄い面を、形の上に重ねる（破線は「見にくい」）
画面     絵と設定は別のペイン。どちらも縦横に別々にスクロールし、あいだの棒で設定の幅を変えられる
確かめ   変えるたびに、ポインタを行に沿って 0.5〜1px 刻みで掃き、どの掴みがどこで答えるかを刷る Playwright の探り針で数を出す
記録     1 回ごとに、規則と測った数を commit の文に書く。その履歴が裁定の記録になった
```

⛔ **図に数を書くなら、その数で描け。比は内容である** —— 違う比で描くと、利用者は設計ではなく図を叩く。
⭐ **言葉で立たない設計は、図で立てる。却下の理由が次の案の設計条件である。**

### ⛔ 問う前に、そもそも問ってよいかを見る

```
① 直すときに触るのは 1 コンポーネントの中だけか
② 公開された API（表 T-064 ・ 表 T-065）は動かないか
⇒ 両方「はい」なら `A`〜`C` であることが多い。⛔ **問わずに推奨値で進める**
```

⛔ **`A`〜`C` を巡の途中で問わない。実装と試験を止めない。**
⭐ **推奨値で実装し、`// @provisional PD-n` の印を付け、一覧に溜めて巡の最後か次の先頭でまとめて問う。**
⚠️ **実測: 1 巡で 14 件の裁定を並べ、そのうち 8 件は `A`〜`C` であった。**
⛔ **問わずに進められたものを問い、その間下流を待たせた。規則 06 は最初からそう書いてあった。**

### ⛔ 利用者へ裁定を問う前に埋める欄（**空欄のまま問わない**）

```
機構      症状ではなく、なぜそうなるかを file:line と数で 1 文
実測      自分の手で走らせた数字（⛔ 体の報告の引き写しは不可）
選択肢    2 つ以上。⛔ それぞれの帰結を数で
推奨      1 つ。根拠は仕様の逐語
覆す費用  規則 06 の分類と、落ちる試験
```

⛔ **「測れなかった」と書くのはよい。⭐ 測っていないことを設計判断のふりにするのが悪い。**

### ⭐ 不具合は台帳で追う

⛔ **利用者が指摘した不具合を、頭の中と引継書だけで追わない。**
⭐ **`docs/development-records/defects.md` に 1 行立て、直るまで消さない。**
⚠️ **「実装済」は試験が緑という意味であって、実物で動いた証拠ではない**（規則 04）。

### ⛔⛔ 「塞がっている」を信じるな。反証しに行け

⚠️ **台帳に「裁定を待っている」「仕様に行が無い」と書かれた 13 行を反証しに行ったところ、
11 行は実際には塞がっていなかった。**

```
7 行  仕様に行が最初から在った（1 行は 3 か所に逐語で在った）
4 行  裁定は既に下りており、書けばよいだけだった
2 行  本当に利用者を待っていた
```

⛔⛔ **いちばん高くついた 1 行は、4 つの行を止めていた。**
**反証に要したのはブラウザ 1 回の起動であり、選べないはずの案は設計の表が最初から禁じていた。**

⭐ **順に当たれ。⛔ 3 つとも飛ばすな:**

```
① 実物で押す      「入口が無い」「図形が無い」は、たいてい在る
② 仕様書を引く    ⛔ 全文を読め。要求は STATEMENT の後の段落に MUST を置く。表の前後も見ろ
③ 台帳の欄を末尾まで読む   ⛔ 冒頭ではない。裁定は書き足しで末尾に付く
```

⛔ **体へ出すときは「確かめてこい」ではなく「反証してこい」と書け。**⭐ **判定を
「塞がっていない／原因が割れた／本当に塞がっている／別の行と同じ原因」の 4 つに分けさせると、
当て推量が混ざる余地が無くなる。**

### ⛔ 前に立つ者のブリーフは間違う。⭐ 体に原典で訂正させろ

⚠️ **1 巡で 8 つ間違え、8 つとも体が捕まえた。**⛔ **半分は数と在り処である:**

```
設定値を古い数で書いた（12 と書いたが 16 であった）
表の番号を間違えた（T-028 と書いたが T-023 に在った）
条文に偽を書いた（「読む人の文字サイズで動く」を、別の行が逐語で禁じていた）
要求の引用が的外れだった（幅の見積もりの要求を、綴りの要求として引いた）
台帳の実測を古いまま渡した（前の変更で数が変わっていた）
対応案そのものが誤りだった（入れると画面が永久に空になることを、体が測って示した）
```

⇒ ⛔ **ブリーフに数・行 ID・在り処を書いたら、必ずこの 1 文を添えること:**
**「⛔ この数と在り処は前に立つ者が書いた。原典で確かめてから使え。違っていたら訂正して報告しろ」**
⭐ **訂正は成果である。⛔ 報告を受けたら台帳と条文の両方を直すこと。**

### ⛔ 行は症状ではなく原因で立てる

⛔⛔ **1 つの原因が複数の症状を割る。症状の数を不具合の数として数えるな。**
⚠️ **実測 2026-09-03: `npm run parity` は 25 手のうち 14 手が食い違ったが、
どちらの名簿にも説明が無く、14 件の不具合ではなかった。**

```
① 症状を全部集める（⛔ ここで台帳の行を立てない）
② 実物で押して二分し、原因を file:line で名指す
③ ⭐ 原因 1 つにつき台帳の行 1 つ。症状はその行の中に並べる
```

⛔ **門で止まる不具合は、その先を全部隠す。** ⚠️ **実測: 板の門が止まっていたあいだ、
14 手は一度も走っていなかった。**⇒ ⛔ **門で止まったら「直った」ではなく
「ここから先は一度も走っていない」と読め。**

#### ⛔⛔ 症状を別の行に吸収するな。⭐ 吸収したなら、取り下げる前に説明がついたかを見よ

⚠️ **実測**: `DFC-342`は「単体試験が 1 本、走らせるたびに結果が変わる」を記録し、
原因を**「ファイルをまたぐ状態（表示言語らしい）」と推測して** `DFC-352` に吸収された。
`DFC-352` は「`--sequence.concurrent` はこの製品の走らせ方ではない」として取り下げられ、
⛔ **`DFC-342` の症状はその機構では起きないのに、一緒に消えた。**
⇒ **6 日後、同じ症状が別の巡を止めた**（`DFC-506`。原因は絵が持つ壁時計であって、表示言語でも並行でもなかった）。

```
⛔ 吸収してよいのは「原因が同じ」ときだけである
⛔ 推測で吸収するな —— 推測は原因ではない
⭐ 吸収先を取り下げるときは、抱えている症状の 1 つずつについて
   「これは説明がついたか」を問う。⇒ ついていない症状は、行として残す
```

⚠️ **この失敗は 2 度起きうる形である** —— **症状を吸収した側が取り下げられると、
台帳には「取下げ」しか残らず、症状が説明されなかったことは誰にも見えない。**

### ⭐ 台帳の数は 5 つの束で読む

⛔ **見出しの 1 つの数に、種類の違う仕事が混ざる。**
⚠️ **実測 2026-09-03: 開いていた 65 行のうち、コードを直す仕事は 12 行しかなく、
26 行は仕様を書く仕事、16 行は押しに行く仕事であった。**
⇒ **「減らない不具合」に見えていたものの大半は、不具合ではなかった。**

| 束 | 意味 | ステータス |
|---|---|---|
| `未仕分け` | まだ調べていない | `未検討` |
| `仕様の穴` | ⛔ 利用者の裁定か、仕様の行が要る | `裁定待ち` ＋ `仕様待ち` |
| **`製品の仕事`** | ⭐ **「不具合」と呼んでよいのはここだけ** | `実装待ち` ＋ `試験待ち` |
| `検証の借り` | 緑だが出荷ビルドを押していない | `実測待ち` |
| `決着` | 終わっている | `実測済` ＋ `取下げ` |

⭐ **`tools/ledger_metrics.py` がこの束を刷る。**⛔ **手で書くな。**

## 8. ⛔ 利用者の発言を、巡の頭で台帳へ写す

⛔ **指示を受けたら、話を進める前にまず defects.md へ要点を書くこと。
話は、それを書いてからである。**

⚠️ **実測: 利用者の指摘 10 件が台帳から落ちていた。**
⛔ **原因は、台帳が「前の巡が書いた引継書」からしか作られていなかったことである。**
引継書に載らなかった指摘は、記録のどこにも残らずに消える。

⭐ **落ちた 10 件のうち 4 件は既に直っていた。**
⛔ **「直っていないから消えた」のではなく、「直ったかどうかを誰も確かめられない」状態だった。**
⚠️ **だから利用者は同じ指摘を繰り返す羽目になった。**

### ⛔ 手順（心構えではない）

```
1. 巡の頭で、セッションの逐語記録から利用者の発言だけを抜き出す
     python previous-project-result/temp/extract_user.py <transcript>.jsonl
   記録は ~/.claude/projects/<プロジェクト>/*.jsonl に在る
2. 抜き出した指摘・指示を 1 つずつ台帳と突き合わせる
3. ⛔ 台帳に無いものを、その場で全部立てる。調べる前に立てる
4. ⭐ 既に直っているものも立てる ── 状態は `実測待ち` とし、
   実測の根拠（自分で測った数）を「詳細状況」に書く
5. そのうえで作業に入る
```

⛔ **巡の途中で指示を受けたときも同じである。まず台帳へ写し、それから着手する。**

### ⚠️ 抜き出すときに落としてはならないもの

⭐ **道具は利用者の発言だけを刷り、道具の出力・システムの注記・スラッシュコマンドの控えを捨てる。**
⛔ **要約するな。逐語のまま台帳へ運ぶこと** —— 要約は、次の巡で「何を言われたか」を失う。

---

## 9. ⛔ 体（サブエージェント）の使い方

⭐⭐ **本節の教訓は、引継書のどの版にも同じ形で現れたので、引継書ではなく規則に置く。**
⚠️ **新しく測れたことは、引継書ではなく本節に足すこと。**

```
⭐⭐ 「反証してこい」と書け。「確かめてこい」ではない（§2.1。歩留まりが 1 桁違った）
⭐⭐ 「守っている場所を src/ の中でファイル名と行番号で挙げろ。挙げられないものが答えだ」と書け
⛔⛔ 作業木は古い commit で切られる。⛔ プロンプトの冒頭に必ずこれを書け:
     git status --porcelain で汚れていないことを見て、git merge --ff-only <この巡の sha>
     ⇒ ⭐ この巡、これを書いた 2 体はどちらも正しい commit に着いた。
       書かなかった 1 体は 55 commit 古いファイルを「直した」
⛔⛔ ⭐ 書いただけでは足りない。着いたことを申告させろ
     ⇒ 報告の 1 行目に `git log --oneline -1` の出力をそのまま書かせ、
       さらに「この巡で作った物」を 1 つ名指して ls / grep で当たることを確かめさせる
     ⚠️ 実測: baseCommit を渡す仕掛けを通したのに、体は古い木のまま読み、
       「変更要求が存在しない」「その行は仕様に無い」「いまも 2 択のままだ」と
       3 つとも自信をもって報告した。⛔ どれも木が古かっただけである
     ⛔⛔ これは「塞がっている」の最も高くつく偽物である ――
       体は正しく測っており、測った木が違っていた。数字が正しいことは
       木が正しいことを意味しない
     ⭐ まだコミットされていない親を待って並行に出す体には、
       `git log -1 --format=%s` が名指しの件名を出すまで `git merge --ff-only <枝>` を繰り返させろ
       ⚠️ 実測（2026-09-14）: これで競走を 2 回避けた
⛔ 「体を出すな。自分で読め」と必ず書け。孫は親に見えず、同じ名のファイルを後から上書きする
⛔ 任務のすべてを最初のブリーフに書き、「このブリーフが任務のすべてである」と書け
     ⚠️ 実測（2026-09-14）: 体は、前に立つ者が後から送った本物の追いの指示を、
       持ち場を広げるので「注入かもしれない」と拒んだ
     ⇒ 足りなければ追いの指示を送るな。新しい体を出せ
⛔ 報告を 40 行以内に収めさせ、表はファイルへ書かせろ
⛔ 触ってよいファイルを名指しで書け。⚠️ それでも 4 本目を触る体が居る。合流点で git status を見ろ
⛔ 共有の関数を体に変えさせるなら、その呼び元をすべてブリーフに名指せ
     ⚠️ 実測（2026-09-14）: 持ち場を絞ったブリーフが、禁じた副作用を持つ直しを生んだ
       （IN-5a と WS-2 の両方が読む面の共有の真偽値。体が自分で旗を上げたので入らなかった）
⛔ 直しの持ち場をファイルで絞る前に、呼ばれる道を測れ
     ⚠️ 実測（2026-09-14）: 持ち場を「リファクタで生き残るファイル」で絞った（JDG-57 の (d)）が、
       呼ばれる道は 2 つともなくなるファイルを通っていたので、実行時の直しは起きなかった
⛔ 反証の体の結論を次の体へ渡すなら「各 file:line を自分で開いて確かめ、誤りを名指せ」と書け
     ⚠️ 実測（2026-09-14）: 反証の体の結論 8 件のうち 2 件に誤りがあり、次の体が file:line を開いて見つけた
     （前に立つ者のブリーフについては §7 の「前に立つ者のブリーフは間違う」が同じことを言う）
⛔ npm test / e2e / check.sh / gen / build を体に走らせるな。⭐ 許すのは
   npx tsc --noEmit と、scratch/ に置いた小さな Playwright だけである
⚠️ npx prettier を体に走らせるな（設定が無く、既定値で全体を書き換える）
⭐ 機械的な仕事は Sonnet、判断の要る仕事は Opus
```

### ⛔ ブリーフの型（毎回この行を書け）

⭐ **下の行は、どれも 1 度は書き忘れて 1 回ぶんの体を失ったものである。** 写して、`<…>` を埋めて渡せ。

```
1  git status --porcelain が空であることを見て、git merge --ff-only <この巡の sha>。報告の 1 行目に git log --oneline -1
2  編集してよいファイル: <名指しで>。⛔ 同時に走っている体の持ち場 <名指しで> には触るな
3  書く前に、同じ形の枝が他にいくつあるかを grep で数えよ（読むのではなく数える）
   型を広げるなら、as unknown as で手組みした試験の値も数えよ（型検査では落ちず、描く所で落ちる）
4  わざと壊して何件赤くなるかを測れ。⛔ 0 なら 0 と正直に書け。対照も書け ——「逆に壊れていたら何が通ってしまうか」
5  検査は終了コードで判じよ。⛔ FAIL を grep するな
6  基準線のファイルに触るな。⭐ ただし錨が条項を逐語で持てば、そのぶん借りが返る
7  ⛔ 条項が無いなら発明するな。止まって報告せよ。⭐ 止まるのは失敗ではない
   ⛔ 元の試験が主張していなかったことを足すな。足すなら、それを名指す条項を書け（DFC-105）
8  許可の確認や分類器に止められたら、止まって報告せよ。⛔ どの道具でも、どの殻でも回り道するな
9  ⛔ 作業木・junction・symlink を作るな。⛔ git stash・checkout・restore をするな（一瞬でも）。比べるなら git show HEAD:<file>
   ⛔ dist/index.html に触るな（前に立つ者が作り直した dist を体が git checkout で戻した）
10 一時ファイルは scratchpad に、自分の名の接頭辞を付けて置け（<名>.move_rows.py）。前に立つ者も同じ場所に書く
11 前に立つ者は commit と push をする。あなたの足の下に現れた commit は前に立つ者のものである
12 ⛔ 体を出すな。自分で読んで自分で答えよ。このブリーフが任務のすべてであり、追いの指示は来ない
13 報告は 40 行以内。表はファイルへ。主張してよいのは自分が触ったファイルの数だけ。
   「検査 N は緑」と書くなら、走らせたコマンドを添えよ
14 npm run gen を <許す／禁じる>（原稿 _source/ に触れる仕事なら許す）
15 docs/spec/output/ が要る検査を走らせるなら、親から写せ（04 の 6.7）。XSD を読む試験は根の関門に任せよ（04 の 3.9）
```

⛔ **この 15 行の外に、仕事ごとの行を足せ**: 反証なら「反証してこい。守っている場所を file:line で挙げよ」、数・行 ID・在り処を渡すなら「原典で確かめてから使え」（7）、修理なら「錨が条項を逐語で持てば借りが返る」。

### ⛔ 受け取り方（報告は仮説である。ディスクを数えよ）

```
⛔ 報告の最後が「走行が続いている」「戻す」で終わっていたら、完了ではない
   ⚠️ 実測（2026-09-14）: 「終わった」と返した体が、わざと壊した版を作業木に残し、裏で Playwright を 2 本走らせ続けていた
   ⇒ TaskStop で止め、その作業木の名を含む node の走行が残っていないことを数えてから e2e を回す。写す前に、体の作業木の現物と写す版を diff する
⛔ 「裏の走行を待つ」と書いて止まった体は、自分では起きない（2026-09-15、4 回）
   ⇒ その作業木の名を含むプロセスが消えるまで裏で見張り、消えたら「走行は終わった、報告を仕上げよ、もう走行を始めるな」と送る
   ⚠️ 走行中の体は追いの指示を注入と読んで拒むことがある（2026-09-26）。拒まれたら、作業木のファイルを自分で数えて受け取れ
   ⭐ 報告が無くても、仕事は作業木に在ることがある ⇒ 出し直す前に git -C <作業木> status --porcelain を見よ
⛔ 数は報告の文ではなく、体が書いたファイルから数え直せ
   ⚠️ 実測: 報告の合計 61 が同じ報告の群ごとの数 56 と合わなかった／98 本の仕分けを頼んだ体が 20 本だけ書き、残りを孫に出して終わった
   ⇒ 1 から N まで欠けも重なりも無いかを数えよ
⛔ 数を比べる検査（45・55・60）は、合流した木で数え直せ。体ごとの数を足し合わせるな
   ⚠️ 実測（2026-09-21）: 2 体が別々に 89 と測ったが、重ねると 90 —— 2 体がそれぞれ export を付け、字面が揃って 1 組になった
⛔ 「検査 N は緑」は走らせたコマンドを確かめよ。npm run gen:check は自分で回せ
   ⚠️ 実測（2026-09-21）: 体が「検査 27 は緑」と報告したが、走らせたのは別の道具であり、実際は DRIFTED だった
⛔ 体が書いた台帳の行は、前に立つ者が開いて読め。裁定待ち に 10 の 3 手の跡が無ければ差し戻せ
   ⚠️ 実測（2026-09-14）: 体は禁じている import を「禁じていない」と書き、2 行とも 裁定待ち にした
⛔ commit の前に、報告の中の「拒んだ指示」「安全の注記」を読め
⛔ 体が終わったら、根の node_modules/.bin の数と vitest の版を確かめよ（04 の 6.7）
```

---

## 10. ⛔⛔ 前に立つ者にも「反証してこい」を適用しろ

⭐ §2.1 は体に向けて書かれている。⛔ **前に立つ者はそこから除外されていない。**

### ⛔ 台帳の行が「無い」と言ったら、まず探せ

```
⛔ 実測: DFC-273 が「表 T-233 に担当者名の波及の行が 1 つも無い」と言う。
   信じて、同じ場面の行をもう 1 つ書いた。RS-49 が同じ日に既に立っていた。
⭐ 捕まえたのは契約試験である —— 語と行の対の基準線が、無い指紋を拒んだ。
⇒ ⛔ 機械が捕まえなければ、重複した行が仕様に残っていた。

⭐ 手順（1 分で済む）:
   ① 行が名指す表を開き、近い場面の行を目で読む
   ② その巡の git log を見る —— 同じ日に誰かが立てていることがある
   ③ 辞書（_source/display-words.json）を grep する
   ④ 赤に台帳の番号を振る前に、fixed-defects.md を grep する
      ⭐ 台帳には戻り道が在る —— 実物で落ちた行は fixed-defects.md から defects.md へ戻す。新しい番号を立てない
      ⚠️ 実測（2026-09-21）: DFC-374 の赤に新しい番号を宣言しかけ、台帳の頭を読んで訂正した
⛔ ①〜④ を飛ばして行を書くな。
```

### ⛔⛔ 前に立つ者の見立ても仮説である

```
⚠️ 実測（2026-09-20〜21）: 前に立つ者が「開いて測る前に書いた」ことが 6 回あり、6 回とも体の実測が覆した
   パリティの赤を CR-430 の回帰と疑った          ⇒ 2 つの古い赤が積み重なっていた
   2 つの行の合流を「問わずに進めてよい」とした  ⇒ 利用者の裁定が禁じる形の軸をまたいでいた
   門 3・4 の道具を「無い」とした                ⇒ 483b666b から在った
   「仕様は R2.2 を 1 度も書かなかった」とした    ⇒ 設計の章に 2 か所在った
   数を比べる検査を提案した                      ⇒ 着地済みの変更要求がすべて落ちる形だった
   ユニットの番号の最大を行の数から推し量った    ⇒ 欠番が 3 つあり、既存の行と帯が重なった
⇒ ⭐ 体を「前に立つ者の見立てを反証させる相手」として使え。渡した数は、体にいつも測り直させよ
⛔ 体の心配を「前例がある」で退ける前に、その前例が基準線（借り）に載っていないかを grep せよ
   ⚠️ 実測（2026-09-21）: 「入口は既に Hit・Item を取り込んでいて緑だ」と退けたが、その 2 つは
     crossing-names-baseline.txt に借りとして載っていただけで、検査 26b が赤になった
```

### ⛔⛔ 直したら、同じコミットで台帳の行を動かせ

```
⛔ 実測: 巡の頭に開いていた行の中に、既に直っていた状態を述べる行が混ざっていた。
⇒ 直っている行は、次の巡に実測 1 回ずつを負わせる。⛔ それは巡を丸ごと 1 つ食う。
⭐ 合流したら、造りと台帳を 1 つのコミットに入れろ。
⚠️ ステータスだけでなく、詳細状況の欄も書き換えろ —— 古い欄は次の読み手に古い世界を見せる。
```

### ⭐ 体のブリーフに必ず書く 2 行

```
⭐ npm run gen を許すか禁じるかを書け
   ⇒ 判断は単純: 原稿（_source/）に触れる仕事なら許せ。触れないなら禁じろ
   ⛔ 書かなかった巡、体は生成物を手書きして「次巡で畳んでよい」と注を残した

⛔ 逐語の grep 文字列で門を作るな。概念を言え
   ⇒ 「基準隅は左下」で確かめさせたが、仕様は「基準隅を左下」だった
   ⭐ 体は 0 件を報告し、実際の文言を探して続けた。正しい振る舞いである
   ⛔ 脆い文字列で門を作ると、正しい体を止める
```

### ⭐ 波は「いまどのファイルが空いているか」で決める。段割りを先に引くな

```
⛔ 実測: 前の巡の引継書が「3 段・のべ 18 体」の段割りを測って載せた。
   次の巡は一度も使わなかった。実際にやったのは 3 波・のべ 6 体である。
⇒ ⭐ 段割りは 1 波目で崩れる —— 体が行を反証して戻すからである。
⭐ 生きているのは 1 つだけ: 同じファイルを 2 体に渡すな。
⛔ 合流の前に必ず重なりを数えろ（0 件を見てから写せ）:
   comm -12 <(git diff --name-only <base> HEAD | sort) \
            <(git -C <worktree> status --porcelain | awk '{print $2}' | sort)
⛔ 体の作業木から「全部」を写すな。その体が書いたファイルだけを写せ。
   ⚠️ 作業木の「変更あり」は、その体の基準に対する差である。丸ごと写すと幹の仕事が消える
     （2026-09-05、渡したあとで幹が進んだファイルを写し戻し、変更要求 1 本ぶんの仕様の編集を戻した）
   ⭐ 写す前に 1 ファイルずつ数えよ（2026-09-08 は 15 回、2026-09-14 も全部 SAFE で事故 0）:
     for f in $(git -C <wt> status --porcelain | grep -v '^?? ' | awk '{print $2}'); do
       git diff --quiet <体の基準 sha> HEAD -- "$f" && echo "SAFE $f" || echo "PATCH $f"
     done
     SAFE なら cp。PATCH なら git -C <wt> diff -- <file> > <scratchpad>/x.patch ; git apply -3 <scratchpad>/x.patch
   ⭐ 体の持ち場をファイルで割っておけば、ほぼ全部 SAFE になる ⇒ 割り方が合流の費用を決める
⛔⛔ コメントだけを書き換える波では、体の「コードは同一」を証拠にするな。
   前に立つ者が、自分の同一性の script を波の全ファイルに打ち直せ。
   ⚠️ 実測（2026-09-14）: 6 体がそれぞれ字句解析器の比較で同一を「証明」した。
     1 体の比較は正規表現の中のエスケープの変化（`×` が文字そのものに変わった。tests/system/live-app.ts）を
     見落として IDENTICAL と報告した
   ⭐ 捕まえたのは前に立つ者の script —— 生の本文から字句解析器のコメントの範囲だけを抜いて比べ、
     わざと入れたエスケープの変化と題名の変化で赤くなることを確かめてあった（規則 04 の 2）
⛔ 写すのは、その script が IDENTICAL と刷ったファイルだけである。判定より先に写すな
   ⚠️ 実測（2026-09-14）: 合流の繰り返しが判定の前に幹へ写したので、壊れたファイルがそのまま入った
```

### ⛔ 体に docs/ を書かせるな。代わりに「行が無いならそう報告しろ」と書け

```
⭐ 実測: これで 2 件が正しく止まった。
   DFC-276 —— 表 T-206 / T-207 に当たる行が無いことを測って、造りを変えずに戻した
   DFC-278 —— 置き場を仕様が決めていないことを測って、造りを変えずに戻した
⇒ ⭐ 止まった報告は失敗ではない。⛔ 体が値を発明していたら、
     その値は次の巡に「原稿から切れた写し」として現れる。
```

### ⛔⛔ 体が「覆う条項が無い」と言っても、そのまま `裁定待ち` にするな

> **「なんで同じような裁定をさせるのか？ なぜ一度に裁定が終わらないのか？ 新規の不具合出たのか？ 俺が何度ども指示しているのは既存の不具合を0にしろ、新規の不具合を探すテストはするな、必要なのは修正確認テストだけだ。」**

⛔ **体は「条項が無い」と言った当人である。**⭐ **反証するのは前に立つ者である。**

```
⭐ 実測: `裁定待ち` に置かれていた 4 件のうち 3 件は裁定案件ではなかった
   ① 進捗マーカーを掴みシロの外へ出す  ⇒ 表 T-023d の `GR-7` が既に定めていた（grep 1 回）
   ② 再開アイコンと予定バーの優先  ⇒ 既に下りている裁定から導けた
   ③ ダミーの印の掴み分け              ⇒ 利用者は既に 2 度「終了を優先」と裁いていた
⇒ ⛔ 問う前にこの 3 つを打て。⭐ 打った跡を行に書け
   1  grep -n "<その語>" docs/development-records/rulings.md
      ⛔ 語が当たって終わりにするな。当たった裁定の着地先の条項の本文を開き、
        推しと代償の両方をその本文で反証しろ
      ⚠️ 実測（2026-09-14）: 起草の体の推しが JDG-04 の着地先 FR-011 の MUST を割っていた。
        退けた案に付けた代償（完了率が 100 を超える）は、FR-012 の RATIONALE が意図と定めていた
   2  python .claude/skills/spec-graph-check/impact.py <関係する行 ID>
   3  その所作が 他の道 でも行えるか（行えるなら「他の道が無いほう」が勝つ）
⭐ 3 つとも空振りしたときだけ、`裁定待ち` にしてよい。
```

⛔ **同時に、新規の不具合を探す試験を足さないこと。**⭐ **書いてよいのは修正確認とデグレの確認だけである（`JDG-13`）。**

---

## 11. ⛔⛔ 波の幅を先に測れ

⛔⛔ **引継書 5 版を読み返して分かったこと** —— **どの版の「学び」も「体がどう誤ったか」であり、**
**「一度に何体出せるか」を一度も問うていない。⇒ どの巡も 1 波 2〜4 体で回していた。**
⭐ **一度に何体出せるかを問わないと、1 回で終わる仕事を何巡にも分けてしまう**。

### ⭐ 行は 3 種類にしか分かれない。⛔ 幅を決めるのは 1 種類だけである

```
試験だけが無い行  → tests/ しか触らない。⛔ 誰とも衝突しない。何体でも同時に出せる
                    ⭐ 体ごとに「新しい試験ファイル 1 本の名前」を指定すれば
                      試験の体どうしも衝突しない
実物を押すだけの行 → ⛔ 体は要らない。Playwright を 1 回起動して前に立つ者が閉じる
造りを直す行      → src のファイルで割る。⛔ ここだけが幅の上限を決める
```

⛔ **上限を測る前に体を出すな。**⭐ **測るのは 3 分である**（開いている行から `src/…\.ts` を
拾って、2 行以上が名指すファイルを数えるだけ）。

### ⛔ 惜しむ理由が無いことを、数で押さえておけ

```
体 1 つが自分で使う文脈        120k 〜 370k   ⇒ ⭐ 親の文脈には乗らない
親が 1 体を出して合流する費用    3k 〜 5k     ⇒ ⭐ 9 体でも 40k に収まる
```

⚠️ **合流は 1 体ずつ検査を回すな。**⛔ **全部写してから `npm run gen` → `tsc` →
`check.sh` → `vitest` 全体 を 1 回だけ回せ。**

⭐ **体が走っているあいだ、前に立つ者は仕様を書け** —— **`docs/` は体が触らないので衝突しない。**

---

## 12. ⭐⭐ 幅は測ったら本当に出た（**11 の続き**）

⭐ **11 が「惜しむ理由が無い」と書いた翌日、**14 体を 1 波で出した。**⛔ 幅は問題ではなかった。**

```
1 波目   14 体（試験 5 ／ 造り 4 ／ 踏査 4 ／ 掃引 1）
2 波目    4 体
親の文脈  1 体あたり 3〜5k。⛔ 14 体でも 60k に収まった（見積りどおり）
```

### ⛔ 詰まったのは幅ではなく、**合流の順序**であった

```
⛔⛔ 体は「いま木に在るもの」を見られない。作業木はセッションの始点で切られる。
   ⇒ 2 波目が 1 波目の成果（新しい表の行・新しい型）に依存したので、
     ⭐ 波と波のあいだで 1 度コミットする必要があった。
   ⛔ コミットせずに出すと、体は「その行は原稿に無い」と正しく報告して止まる。
⭐ 手順: 1 波を合流 → 検査を 1 回 → コミット → その sha を 2 波目のブリーフに書く
```

### ⭐⭐ いちばん高く付いたのは「造り」ではなく「前提」であった

```
⭐ 実測: 体に渡した行のうち、少なからぬ行の前提が偽であった。
   DFC-280 と DFC-282 は「仕様に行が無い」と書いてあったが、
   既に仕様へ書き込まれていた。⛔ 誰も読み直していない。
   DFC-269 は 7 行を挙げるが、7 行とも既に直って 裁定済 であった。
   DFC-310 は要求そのものを取り違えていた（NFR-008 は焦点、コントラストは NFR-007）。
   DFC-338 は原因も対応案 2 つも外していた（押下ではなく解放が拒まれている）。
⇒ ⭐⭐ 「反証してこい」と書いた体が、いちばん多くを返した。
⇒ ⛔ 直す体より先に、踏査の体を出せ。造りを直す前に前提を潰すほうが安い。
```

### ⛔ 前に立つ者は、同じ巡で 2 度 ①（§2 の教訓）を踏んだ

```
⛔ 実測: 掃引の報告が戻る前に、掃引が測っていたのと同じ場面で
   新しい行（DFC-362）を立てた。DFC-283 がそのまま同じことを言っていた。
⭐ 捉まえたのは、報告と自分の行を突き合わせたことである。
⇒ ⛔⛔ 行を立てる前に必ず defects.md を grep しろ。
     ⚠️ 走っている体が同じ場所を測っているなら、その報告を待て。
```

---

## 13. ⛔⛔ 並行するセッション —— 調整役が 1 人で持つ

⭐ **体（サブエージェント）は 1 つのセッションの中の並行である。本節は、セッションを 2 つ以上同時に走らせるときの規則である**（開発する人の手引きは [`parallel-agents-ja.md`](parallel-agents-ja.md)）。
⚠️ **帯・番号・push の口をセッションどうしで宣言し合う形は、2026-09-21 夜に 5 つのセッションで「確定した表」が 4 回食い違った。** 1 つのセッションが 1 つの登録簿で配ると止まった。

### ⛔ 誰が何を持つか

```
調整役（1 つのセッション）  帯・持ち場・順・合流・push の口・関門を回す口・基準線の下げ（04 の 6.3）・裁定の問い
                          ⛔ 調整だけをする。仕事は提案チップ（別のセッション）と体に配る（利用者 2026-09-26）
ほかのセッション          自分の枝でだけ commit する。push・refactor への合流・main への合流・タグはしない
                          終わったら調整役に 5 行以内で送る: 枝・commit の sha・着地したもの・検査の終了コード・問い
```

⭐ **並べる数に上限は置かない**（〜5 セッションで回した。利用者 2026-09-26）。⛔ **詰まりは数を絞らず、仕組みで直す。**
⛔ **計画なしにセッションを増やすと散らかる** —— 整理 → 計画 → 並行の順（2026-09-26）。
⛔ **チップ（別のセッション）の名に記号（`@` など）を付けるな** —— 名で宛てられなかった。

### ⛔ 番号は調整役から受ける

⛔ **取る番号はすべて、使う前に調整役に宣言する** —— 表・行 ID の接頭辞・要求・裁定（`JDG`）・台帳（`DFC`）・ユニット（`UF` / `UT`）・図・変更要求・検査の番号。
⚠️ **検査 62 が捕まえるのは、散文で「空いている」と主張した表番号だけである** —— 裁定とユニットの番号の衝突は、宣言でしか防げなかった（2026-09-21）。

```
起草のあいだ   仮の番号で書く（帯を配るのは体どうしを分けるためだけ）
commit の前    1 度だけ、行の順に 最大 ＋ 1 へ詰め直す。⭐ 2 段で —— まず一意な仮の字へ、次に本番の番号へ（連鎖してぶつからない）
              ⛔ 帯の穴は二度と埋まらない（公開なので、push した穴は残る）
詰めた後      確定した番号の一覧を調整役に送ってから commit する
```

⛔ **並べる体・セッションの補助ファイルには、名の接頭辞を分けて付けさせよ** —— 2026-09-20、仕様だけを読む試験の体 2 つが、どちらも `cr-430-scene.ts` を作った。

### ⛔ push と載せ直し

```
push の口は 1 つ（調整役）。push の前に、生きている相手に「未 push のコミットがあるか」を尋ね、返事を待つ
   ⚠️ 実測（2026-09-21）: 相手の未 push の 3 コミットが non-fast-forward になった
   ⭐ 調整役が ListAgents に居ないとき（立ち直っている間）は、生きている相手すべての「なし」を得てから push し、あとで sha を伝える
上流が進んだら載せ直し、合流した木で npm run gen と python tools/ledger_metrics.py を回し直す
   ⛔ 共有の数は測り直せ —— 両側が同じ新しい値を書くと、衝突せずに誤る
⛔ 台帳の衝突を git checkout --ours で解くな —— ファイル丸ごとを取る
   ⚠️ 実測（2026-09-22）: 件数の塊の衝突を --ours で解き、新しい 7 行（DFC-750〜756）が黙って消えた
   ⇒ 解くのは衝突した塊だけ。自分の差分は git diff <古い基準> <古いコミット> -- <file> | git apply --3way で当て直し、
     件数を作り直し、自分の行を数える。台帳は行 ID の和をとれば機械的に解ける
```

### ⛔ 枝の見せ方 —— 利用者は根の dist を試す

⚠️ **利用者は根の作業木の `dist/index.html` を `file://` で開いて試す。** 根で走ったビルドや e2e は、利用者が試しているものを黙って変える（04 の 6.12）。

```
⭐ 合流は別の作業木で組み、根は refactor のまま置く
⭐ 「試せる」と言うときは、枝・commit・dist/index.html の sha256（git show HEAD:dist/index.html と一致）を添える
⛔ 報告の 1 行目に、いま作業している枝の名を書く
   ⚠️ 実測（2026-09-15）: 根が合流用の枝に居たことに、利用者は翌朝まで気づかなかった
⭐ commit・push と、早送り・push は別のコマンドに分ける（1 本に並べたものを自動モードの分類器が止めた）
```

### ⛔ commit と push の前に数える

⛔ **commit の前ごとに、利用者名・絶対パス・メールアドレス・秘密・`.gitignore` の覆いを数えて報告する。push の前にも数える** —— 2026-09-26、1 回 push が先に出た（当たりはリポジトリ自身の公開 URL だった）。
⛔ **0 と言う前に、当たった行を開いて読め。** この木の「秘密」の当たりは誤検知が多い:

```
表示語の watermark unlock password ／ type="password" の入力欄 ／ 雛形の作業名 token exchange ／ 試験の GLYPH_TOKEN
dist/index.html の縮めた束の中の token と、本リポジトリ自身の著作権表示の名
file:// と http:// は絶対パスではない ／ リポジトリ自身の公開 URL
```

⛔ **MSPDI の XSD（`docs/reference/mspdi/`）と SDK から取り出したファイルは、コミットも push もしない。**
⚠️ **公開の push は、後から消しても履歴に残る。**
