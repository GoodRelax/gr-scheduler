# 03 実装の規則 —— 識別子・命名・コメント・読みやすさ

⛔ **本書は「レビュー観点の規約」を書き写さない。** 正は
`docs/development-rules/07-review-standards.md` にあり、条項番号で引く。
⭐ **本書が持つのは、あちらに無い規則と、この製品に固有の作法だけである。**

---

## 1. ⛔ 仕様にある名前を使って実装する

⭐ **仕様書は名前の正である。** 確定名は `_assets/tbl-glossary.md`、列は 表 T-058、
公開してよい名前は 表 T-064、設定値は `_assets/tbl-settings.md` が持つ。

- ⛔ **仕様が名前を持っている概念に、別名を与えてはならない（MUST NOT）** —— 同じものが 2 つの名で呼ばれた瞬間、
  読む者は「違うものかもしれない」と考えなければならなくなる
- ⛔ **仕様の名前を「言い換えて」はならない** —— `Task` を `Item`、`TaskGroup` を `Row` と呼び替えない
- ⭐ **綴りまで写す。** 仕様が `milestoneGlyph` と綴るなら、コードも `milestoneGlyph` である
- ⚠️ **例外は 1 つだけ** —— **仕様がまだ名前を決めていないもの。** そのときは 2. に従う

### ⭐ 名前を写すのではなく、値を生成する

⛔ **仕様が持つ「値」は、写さずに生成する。** 打ち直した値は、仕様が変わっても黙って古いままになる。
⚠️ **実例**: ある設定値を 2 から 6 へ変えたのに、生成物も型も試験も 1 つとして知らせなかった。
**その値を持っていたのは、試験が組み立てる部品ただ 1 か所だった。**

⭐ **いま原稿から生成されている定数**（増えたらここに足す）:

```
COLUMN_DEFAULTS                 列の既定値（表 T-058 が刷る）
NOT_STORED_SIZES                掴み代・当たり判定（表 T-206）
NOT_STORED_LIMITS               取り消しの段数と上限（表 T-206）
NOT_STORED_PANEL_DIVIDER_SIZES  `Panel Divider` の掴み帯（表 T-206 の `S-134`）
DEFAULT_CALENDAR_VALUES         既定の暦（表 T-209）
DATE_COLUMNS                    日付列の全数（表 T-058）
SETTINGS_DEFAULTS               見せ方の群の既定値
SETTINGS_BOUNDS                 その下限・上限
SETTINGS_DERIVED                他の鍵の式で書かれた既定値（⭐ 答えではなく式そのものを刷る）
NOT_STORED_ZOOM_STEP            1 ノッチの倍率（表 T-206 の `S-96`）
NOT_STORED_ZOOM_BOUNDS          倍率の下限・上限（`S-97` / `S-98`）
```

⚠️ **下の 2 つは、表 T-206 の行が値を持たず 表 T-201 の行を名指している初めての例である**（`S-96` → `S-53`）。
⛔ **これを運ぶ道が無いあいだ、ズームの 3 値は `src/` のどこにも届いておらず、`InputContext.zoomStep` が作れないために `FT-1`（ポインタと打鍵の経路）が丸ごと塞がっていた。**

⚠️ **`SETTINGS_DEFAULTS` が持つのは「その式を、指し先も既定値のままで解いた答え」だけである。** ⛔ **指し先が動いた瞬間に古くなる。**
⭐ **だから式を要る側は `SETTINGS_DERIVED` を読む** —— `S-2`（目盛の帯の高さ）は `S-3` に追随するので（`FR-039`）、文字が変わるたびに解き直す。
⚠️ **実例**: これが無かったあいだ、`edit-document-settings.ts` が `rulerFont * 3 + 6` と打ち直しており、**その `6` は後に `S-136` という 1 行になった**（`CR-200`）。⭐ **鍵の名はリテラル型で刷るので、原稿で鍵を改名すると型検査が落ちる。**

⚠️ **表 T-206 の行は、値の種類ではなく**使うユニット**で分ける。** ⭐ 同じ掴み代でも
`S-90` 〜 `S-93` は `item-hit-area.ts`（`Entity`）へ、`S-134` は `screen-frame.ts`（`Adapter`）へ行く ——
⛔ **1 つの定数に相乗りさせると、片方のユニットが他方の値を持つことになる**（1. の重複）。
**どの行がどのユニットへ行くかは `tools/generate_entity_types.py` が決める。**

---

## 2. ⛔ 名前を足すときは、名が体を表すこと

⭐ **必要なら名前を足してよい。** ⛔ **ただし、その識別子の意味と責務が読んで分かること。**

⚠️ **品詞・接頭辞・単位の規約は既にある。書き写さない:**

| 何を決めたいか | どこ |
|---|---|
| クラス・コマンド・クエリ・イベントの品詞 | `R2` の品詞の規約 |
| Boolean は `is` / `has` / `can` / `should` で始める | `R2` |
| コレクションは複数形 | `R2` |
| 単位が固定の量は名前に単位を入れる（`timeoutMs`）| `R2` |
| 層ごとのコンポーネント名（Entity は名詞、UseCase は動詞句 …）| `R2` の層別表 |
| 純粋性の 4 値と `@purity` タグ | `R7` |

### ⭐ 足す前に自問する 3 つ

1. **仕様に同じ概念の名前が既に無いか**（1. に戻る）
2. **その名前は「何であるか」を言っているか、「どう作ったか」を言っていないか** ——
   ⛔ `parsedDataList` ではなく `acceptedTasks`
3. ⚠️ **範囲・限界を含む名前は、境界を含むかどうかが名前から分かるか** ——
   `min` / `max` は**含む**、`begin` / `end` は**終端を含まない**、`first` / `last` は**含む**。
   ⭐ **この 3 組を混ぜない**

⛔ **`data` / `info` / `tmp` / `obj` / `manager` / `helper` / `util` は名前ではない。**

---

## 3. ⛔ コメントには「なぜ」を書く。「どう動くか」を書かない

⭐ **コードを見れば分かることをコメントに書かない。** ⛔ **コードを見ても分からない意図を書く。**
⚠️ 規約としては `R2.15`（名前・コメントが「何を」でなく「なぜ」を表す）が既に持つ。**以下はこの製品に固有の作法である。**

### 書くべきこと

| | 例 |
|---|---|
| ⭐ **なぜ他の選び方を採らなかったか** | 「行ごとに回すと重ね順で勝者が変わるので、行を外側の繰り返しにしている」 |
| ⭐ **その値・規則を持っている仕様書の行 ID** | 「`GR-9` の掴み代。値は 表 T-206 の `S-93`」 |
| ⭐ **危険の理由** | 「ここで `null` と `undefined` を同一視すると、一度も完了していないタスクが完了と判定される」 |
| ⛔ **無いもの・未決のもの** | 「⛔ アンカーの 9 点がどの表にも無い。範囲だけを検査し、推測しない」 |

### 書いてはならないこと

- ⛔ **コードの言い換え**（`// i を 1 増やす`）
- ⛔ **仕様の本文の写し** —— **行 ID を書く。** 写すと 2 か所になり、片方が腐る
- ⛔ **値の写し** —— ⚠️ **実例**: 掴み代の注記が `6px` / `15 x 15` / `30 x 20` と数を写しており、
  原稿を変えても古い数を言い続けた。**注記は行 ID を名乗る**
- ⛔ **嘘** —— ⚠️ **実例**: 「これは DOM ではない」と書いたコメントを型検査が拒んだ。
  **注記が事実かどうかは、機械が確かめられる形にしておくほうがよい**

### ⭐ 印の使い分け（仕様書・コード・変更要求で共通）

```
⛔  禁止・欠落・本物の欠陥
⚠️  注意・過去に踏んだ罠
⭐  要点
```

---

## 4. ⭐ 読みやすいコードの常識

⛔ **既に規約が持つものは、ここでは指すだけにする**（`review-standards.md`）:

| 常識 | どこ |
|---|---|
| 繰り返しを避ける／マジックナンバーを定数にする | `R4`（DRY）|
| ネストを 4 段以上にしない／早期 return で平坦にする | `R4`（KISS）|
| 今の要求に無い機能を先回りで作らない | `R4`（YAGNI）|
| 1 つの関数に複数の抽象度を混ぜない | `R4`（SLAP）|
| 関心を分ける（UI と業務、データ取得と業務）| `R4`（SoC）|
| 状態を変える操作と値を返す操作を分ける | `R4`（CQS）|
| チェーン呼び出しで他人の内部に触らない | `R4`（LOD）|
| 驚かせない | `R4`（POLA）|
| エラーを握り潰さない／境界とインデックス／資源の生存期間／時刻 | `R3` |
| アルゴリズムとデータ構造・毎フレームの経路 | `R5` |

### ⛔ 規約に無いので、ここで定めるもの

| | 規則 |
|---|---|
| **説明変数・要約変数** | ⭐ **複雑な式には名前を付ける。** `if (task.actualFinish !== null && task.percentComplete === 100)` より `const isComplete = …` を挟む。**名前がその式の説明になる** |
| **変数のスコープを縮める** | ⭐ **変数は使う直前で宣言し、寿命を短くする。** 上で宣言して下で使う形は、間の全行が「この変数に触るかもしれない」場所になる |
| **短さより明確さ** | ⛔ **行数を減らすために意味を潰さない。** 三項演算子の入れ子・1 行に詰めた副作用は短いが読めない |
| **条件は肯定形で書く** | ⭐ `if (!isNotReady)` を書かない。**否定の否定は読む速度を半分にする** |
| **似た処理は似た形で書く** | ⭐ **並んだコードは、並んで見えるように書く。** 見た目の違いは意味の違いだと読まれる |
| **意味のある順序を保つ** | ⭐ 仕様の表が持つ順序（印刷順）を、コードの順序にも保つ。⚠️ **実例**: 掴みの優先順を行 ID の数字順に並べ替えると、表の裁定を 3 つ黙って逆にする |
| **無関係の下位問題を抽出する** | ⭐ その関数の主題でない汎用処理は、別の関数へ出す |
| **試験も読み手のために書く** | 詳細は [04](04-verification.md) |

---

## 5. ⛔ コードは英語・ASCII

⭐ **識別子・コメント・文字列・ログ出力を含む。** 世界に公開する前提である。
⚠️ **日本語の仕様書を引くときは行 ID を書く**（`FR-094` / `GR-9` / `S-93`）—— **本文を訳して写さない。**
⚠️ **例外は、日本語そのものを扱う処理**（分類の欄を解析するなど）。**その場合も、なぜ日本語が要るかを書く。**

⛔ **文字列の鍵は ASCII だけにする。** ⚠️ 制御文字が 1 つ混じると、成果物のハッシュが変わって
**アプリ全体が読み込まれなくなる**（[04](04-verification.md)）。

---

## 6. ⭐ 参考資料を当たる順序（8 条の ⑤）

```
1. docs/spec                                      ⛔ 仕様が勝つ
2. docs/reference/mspdi                           交換相手の正典（XSD ＋ 公式文書）
   docs/reference/wcag                            アクセシビリティの正典
3. previous-project-result/03-ui-naming/          前プロジェクトの確定文書
4. previous-project-result/08-poc/                動く試作（値を発明せずに済む）
```

⛔ **参考のコードを流用しない。** ⭐ **値と規則だけを採る。**
⚠️ **`previous-project-result/01-mspdi/` は README だけである。** 正典は `docs/reference/` にある。
