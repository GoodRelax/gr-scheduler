# 04 確かめ方 —— 試験・実物・機械検査

⛔ **「通った」は「正しい」ではない。** 本書は、何をもって確かめたと言えるかを定める。

---

## 1. ⛔ 書いた者に、その試験を書かせない

⛔ **ユニットを書いた者が、そのユニットの試験を書いてはならない。**
**実装を読んで書いた試験は、実装の読み違いごと緑になる。**

```
A  製品コードだけを書く。「別の者が仕様から試験する」と伝えられている
   ⭐ だから公開する形を、それだけで意味が通るように作る
B  docs/spec だけを読んで試験を書く。A の本体ロジックは読まない
   （読んでよいのは冒頭の宣言・公開する型・署名まで）
```

- ⭐ **落ちた試験は「手間」ではなく「発見」である**
- ⛔ **期待値をコードに合わせて書き換えてはならない。** 仕様が明確ならコードを直す（その文を引用する）。
  仕様が曖昧ならその件を落とし、**食い違いとして報告する**
- ⭐ **表を指す要求の試験は、表を写した固定データで駆動する**（仕様書 1.9）

⚠️ **実測**: 415 件が緑のまま、ある関数が仕様の下限を落としていた（見つけたのは監査で、試験ではない）。
⭐ **仕様から書かせた 2 回の走りは、実装者自身の試験には出せない欠陥を 2 件捕まえた** ——
既に否定されていた古いコメントと、`@purity pure` を名乗りながら引数を書き換えていた関数。

---

## 2. ⛔ 通るだけの試験は何も証明しない

⭐ **検査・契約・免除は、わざと壊して落ちることを確かめるまで、確かめたことにならない。**

```
1. 期待どおり通ることを見る
2. ⛔ 壊す。落ちることを見る          ← ここまでやって初めて「効いている」
3. 戻す。ファイルが元に戻ったことを確かめる
```

⚠️ **控えを取ったら `ls -la` で存在と大きさを見る。** ⛔ **失敗した `cp` を信じて復元したつもりになり、原稿を壊した実例がある。**

⭐ **原稿から値が届く仕組みの受け入れ試験は「原稿の値を 1 つ変えると試験が落ちるか」とする。**
⚠️ **落ちなければ、その値はどこにも届いていない。**

---

## 3. ⛔ 緑の試験は「動くアプリ」を意味しない

⚠️ **描画・入出力を触ったときは、開発サーバの実 DOM を見る。** 過去に何度も刺さった:

- **rAF だけの初回描画** → 入力が来るまで白い画面
- ⛔ **文字列の鍵に制御文字が 1 つ混じる** → ブラウザが書き換える → 成果物のハッシュが合わなくなり
  **アプリ全体が止まる。** ⭐ **鍵は ASCII だけ**
- **収める表示のはみ出し**（印とラベルを数えていない）／ヘッダーが日程表の高さを奪う

⭐ **見るもの**: 要素数・色・`data-role` の節点・コンソールとセキュリティ方針の誤り。
⚠️ **プレビュー枠は幅も高さも 0 で冷間起動することがある** —— **そのときの崩れは退行ではない。**
1280×800 にして読み直してから結論を出す。

---

## 4. ⭐ 機械検査（全部走らせる）

| | |
|---|---|
| **機械検査（26 本）** | `bash .claude/skills/spec-graph-check/check.sh`。⭐ **検査 0 が最初に走り、`docs/development-rules/` の索引を刷る** —— この規則が読まれるのは、読めと書いたからではなく、**最初の命令が出すから**である |
| Chapter 5 の自己検算 | `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/audit-ch5.py` |
| 刊行物の描画 | ⚠️ **先に** `strictdoc export docs/spec --formats=html --output-dir scratch/spec-html-probe`、その後 `check-render.py` |
| 影響範囲 / 閉路 | `impact.py` ／ `induced.py`（[02](02-changing-the-spec.md)）|
| 再生成 | `npm run gen`（ズレの検出は `npm run gen:check`）|
| その他 | `npm run typecheck` ／ `test` ／ `e2e` ／ `build` ／ `layers` ／ `tree:check` ／ `types:check` |

### ⭐ 何が何を強制するか

```
型検査        層の規則（Entity と UseCase は DOM ライブラリ抜きでコンパイルされる）
              生成された既定値が、その列の値でないこと
機械検査      参照が解決すること／生成物が原稿と一致すること
              変更要求が 8 条の ①②⑧ に答えていること
              原稿が刊行する散文を言語ごとに持っていること
              開発の記録が実物と合っていること（05 の 1.）
              暫定の印と裁定待ちの一覧が一致すること（06 の 6.）
描画検査      刊行物が実際に表として描かれること（記法の崩れ）
試験          仕様の値が実際にコードへ届いていること
人            答えの質。⛔ ここは機械に下ろせない
```

⚠️ **緑は「参照が解決する」ことの証明であって、「仕様が自分と整合している」ことの証明ではない。**
⭐ **過去 8 巡の重大な欠陥は、すべて全部が緑の状態で見つかっている。**

---

## 5. ⛔ 性能は「後で測る」ではなく規則である

⚠️ **毎フレーム走る経路に `O(n²)` を 4 本書いていた実例がある**（`NFR-013` の MUST NOT 違反）。
⭐ **毎フレームの経路（レイアウト・幾何・当たり判定・描画）では、配列の線形探索を書く前に `Map` を作る。**
⭐ **節目ごとに測るものと合否は仕様書 Chapter 7（表 T-042 / T-043）が持つ。** 本書に数値を書かない。
