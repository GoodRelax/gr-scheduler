---
type: Handover Prompt
title: 引継 — `PD-` の改名（巡 1・2・3）と、中断している掃除
description: 2026-09-12〜13 のセッションからの引継。掃除は中断した。先に `PD-` の ID 衝突を片付ける。分類は済んでいる（1,918 箇所・`unknown` 0 件）ので、次のセッションは判定をやり直さず置換から始められる。
tags: [handover, id, rename, cleanup]
status: ready
---

# 引継 — `PD-` の改名と、中断している掃除

このファイルの `>` から下を、そのまま次のセッションの最初の指示として貼ってよい。

---

> # ⛔⛔ 止まれ — 3 つの文書を先に読め
>
> | 読むもの | なぜ |
> |---|---|
> | [`change-request/CR-371-…md`](../../change-request/CR-371-row-ids-get-a-registry-and-pd-is-abolished.md) | **本件の仕様。**§3 がスコープ、§4 が巡の割り |
> | [`pd-id-split-plan.md`](pd-id-split-plan.md) | 実測と、単純置換が壊す 5 つ |
> | [`pd-sites-summary.md`](pd-sites-summary.md) | ⭐ **分類済みの一覧の読み方。**1 件ずつ判定し直すな |
>
> ⛔ **判定基準を写した文書は 1 つも無い。**原典を読め。
>
> ---
>
> ## 0. 現在地（2026-09-13 実測）
>
> | | 値 |
> |---|---|
> | 最新コミット | `fcd6164`（⭐ **push 済み**。ブランチ `restart`）|
> | ブランチ | `restart` |
> | **掃除** | ⛔ **中断中。**9 ファイル済み（注釈 48,684 → 47,155）|
> | **`PD-` 改名** | ✅ 巡 A（登録簿）／ ✅ 巡 0（分類）／ ⬜ 巡 1・2・3 |
> | 試験 | `vitest` 7,479 緑 / 1 skip、202 ファイル |
> | 門 | 42 = 412・44 = 153・45 = 91・46 = 2。⛔ **どれも余裕ゼロ** |
>
> ```bash
> npm run guard:spec     # 手ごと
> npm run guard:commit   # コミット前
> npm run guard:publish  # push 前。公開する commit まで読んで個人情報を点検する
> ```
>
> ---
>
> ## 1. ⭐ 分類は済んでいる。やり直すな
>
> `docs/review/pd-sites.jsonl` —— **1,918 箇所 / 208 ファイル、1 箇所 1 行。**
>
> ```
> PND-  1,237      certain  1,912
> PTD-    535      likely       6
> null    146      unknown      0   ⭐ 巡 0 の合否はこれである
> ```
>
> `null` の内訳: **META 125**（接頭辞そのもの・正規表現・ひな型）、
> `previous-project-result/` 20（⛔ 裁定により範囲外）、事実誤りの記録 1。
>
> ⛔ **1 行が 2 つの名前空間を持つことがある** —— 6 行で実測。
> 例: `CR-243:47` は 1 文の中に `@provisional PD-1`（台帳）と 「仕様の `PD-1`」（仕様）を持つ。
> ⇒ ⭐ **行ではなく `col` で当てよ。**一覧はそのために `col` を持っている。
>
> ---
>
> ## 2. ⛔ 巡 0 が反証したこと —— ここが次の巡の近道である
>
> | 前のセッションが信じていたこと | 実測 |
> |---|---|
> | 曖昧なのは `PD-1`〜`PD-5`・`4a` の **518 箇所** | ⭐ **438 箇所。**台帳に **`PD-5` は存在しない**（`PD-4` の次は `PD-10`）。⇒ **`PD-5`（113）と `PD-4a`（42）は無条件に `PTD-`** |
> | `pending-decisions.md` の行は全部 `PND-` | ⛔ **287 箇所のうち 15 は `PTD-`。**関連欄とセルの散文が表 T-023a を直に引いている。**行の第 1 セルだけが その行の ID である** |
> | スコープは 1,684 / 181 | **1,918 / 208。**差は CR-371 自身が生んだ文書と、`PD-\d+` では見えない 103 箇所（`PD-n` `PD-x` `pd-` 等）|
> | `change-request/` 254 ／ `previous-project-result/` 14 | **288 ／ 20** |
> | `dist/index.html` は範囲外 | ⭐ **範囲外で正しい。**追跡され 37 箇所持つが、**生成物である** —— ① 門に入っていない（`npm run build` は `guard:commit` に無い）、② **既に 2 日ぶん古い**（ビルド 2026-09-10 対 `src` 2026-09-12）。⇒ ⛔ **改名のついでにビルドするな** —— 無関係な 2 日ぶんの差分が混ざり、改名の差分が読めなくなる |
>
> ---
>
> ## 3. 巡 1 — `PTD-`（仕様側、535 箇所）
>
> ⛔⛔ **検査を先に足す。これが肝である。**
>
> ```
> 1. ✅ **検査 50 は 2026-09-13 に入れてある**（`check-press-row-ids.py`、緑）
>                    ⛔ 作り直すな。片側だけ改名すると赤になることは実測済み
> 2. 原稿 表 T-023a を PTD- に       → 1 も gen:check も赤になる
> 3. type PressRow を PTD- に        → ⭐ tsc が残りを全部名指しする
> 4. tsc が指した箇所を直す
> 5. _source/display-words.json の rowId → gen:check が緑に戻る
> 6. tests/ の該当分 ＋ git mv 2 本
> 7. change-request/ の該当分
> → npm run guard:commit
> ```
>
> ⭐ **1 を後に足すと、「赤にならない改名」を一度通してしまう** ——
> 検査が守るべきものが、既に壊れた状態で生まれる。
>
> **いま結んでいる機械と、結んでいない機械:**
>
> | | 結ぶか |
> |---|---|
> | `generate_display_words.py`（`PRESS_ORDER_TABLE = 'T-023a'`）⇄ `display-words.json` の `rowId` | ⭐ **強制する** |
> | 表 T-023a ⇄ `type PressRow` の値 | ⭐ **検査 50 が結んだ**（2026-09-13。それまでは何も無かった）|
>
> **`git mv` する 2 本:**
> ```
> tests/unit/t-023a-pd-1-pan-follows-the-pointer.test.ts
> tests/unit/fr-009-pd-3-the-two-halves-of-a-bar.test.ts
> ```
>
> ---
>
> ## 4. 巡 2 — `PND-`（台帳側、1,237 箇所）
>
> **1 つの不可分な手として行う:**
>
> 1. `pending-decisions.md` の行 ID（⛔ **第 1 セルだけ。**15 の `PTD-` を巻き込むな）
> 2. `src/` と `tests/` の `@provisional PD-n` の印
> 3. ⛔⛔ **`.claude/skills/spec-graph-check/check-pending-decisions.py` の正規表現**
>    —— **同じ手でやらないと検査 25 が赤になる**
> 4. 他の台帳（`fixed-defects.md` `defects.md` ほか）
> 5. `docs/development-rules/05-working-method.md:301` の `// @provisional PD-n`
> 6. `docs/spec/_source/*.json` の `$comment` の `PD-51` `PD-160`
> 7. `change-request/` の該当分
>
> ⚠️ **`PD-7` `PD-100` `PD-103` `PD-120` `PD-161a` `PD-161b` `PD-213` は台帳に行が無い。**
> ⇒ 改名すると**宙に浮いた `PND-` の指し先**になる。⛔ **別件として報告せよ。直すな。**
>
> ---
>
> ## 5. 巡 3 — 終端を機械にする
>
> ⛔⛔ **「`PD-\d+` は 0 件」とは書けない。**巡 0 が反証した:
>
> - **META が 125 箇所ある** —— 接頭辞そのものを論じる文、ひな型、`PD-n` のような書き方
> - ⭐ **検査自身の正規表現が `PD-\d+` を literal で持っている** ⇒ 検査が自分を捕まえる
>
> ⇒ **こう綴る:**
>
> ```
> PD- のあとに数字が続く綴りは 0 件であること
>   除外 1  previous-project-result/     （裁定 2026-09-12）
>   除外 2  接頭辞そのものを論じる文と正規表現   ⛔ 一覧で名指しし、理由を書く
>   除外 3  `dist/`                              ⭐ 生成物である。ソースを直せば次のビルドで消える
> ```
>
> ---
>
> ## 6. ⛔ 別 CR へ送ったもの（裁定 2026-09-13）
>
> **登録簿が `PD-` 以外に 2 つの真の衝突を見つけた。**
>
> ```
> D  仕様 5 行（機器・表 T-007）   対 台帳 527 行（不具合）   494/494 が台帳だけのもの
> R  仕様 9 行（経路・表 T-008）   対 台帳  44 行（裁定）      44/44 が台帳だけのもの
> ```
>
> ⭐ **綴りが今日ぶつかっていないのは台帳が 0 詰めだからにすぎない**（`DFC-05` 対 `D-5`）。
> ⛔ **後回しと裁定された。順を決めたのであって、方針を送ったのではない。**
> ⭐ **抑止として登録簿に残り、生成物にも印刷されるので忘れられない。**
>
> ⚠️ 確かめてある: **`D-` の衝突は掃除に噛んでいない** —— 消した `D-1`〜`D-9` は 0 件、
> `src/` に元から存在しない（機器は Chapter 2 の話でコードは引かない）。**`PD-` は噛んだ。運である。**
>
> ---
>
> ## 7. 掃除の再開（⛔ `PD-` が終わってから）
>
> **到達点**: 9 ファイル（丸ごと 6 本＋巨大 3 本の第 1 節）。注釈 **48,684 → 47,155**。
>
> ⛔⛔ **同じやり方で再開するな。**前のセッションは**処理済み領域の段落の 77% を素通りした。**
> 原因と対策は [`comment-removal-logic.md`](comment-removal-logic.md) が持つ。**先に読め。**
>
> 要点だけ（⛔ 基準ではない。基準は `cleanup-2026-09-11-prompt.md` 第 6 節）:
>
> - ⭐⭐ **中心の手順が変わった** —— コメントを剥いだコードを体に読ませ、
>   **問われた／問われない／穴**の 3 つに仕分ける。⛔ **「問われない ＝ 消す」にはしない**
> - **粒度は文であって段落ではない**（残存の 68% は段落ごと生き残ったもの）
> - **覆いが合否である。産出ではない**（前回 23%）
> - **切った体に、切ってよかったかを判定させない**（⛔ 刈りすぎは一度も測られていない）
>
> **巨大 3 本の残りの節は、目印で切ってある**（⛔ 行番号は最初の編集で動く）:
>
> ```
> frame-loop.ts              S2 interface DecodedIntake { → function turnInteractionRecord()
>                            S3 function turnInteractionRecord() → async function takeInHandedDocument()
>                            S4 async function takeInHandedDocument() → EOF
> input-command-translator   S2 function dayAnchorAt( → const entry = on.entry
>                            S3 const entry = on.entry → const uid = item.taskUid
>                            S4 const uid = item.taskUid → EOF
> dom-screen-surface         S2 function commandEntry(host → function confirmationAnswerElement(
>                            S3 function confirmationAnswerElement( → EOF
> ```
>
> ⚠️ **改名のあとは節がずれる。引き直せ。**
>
> ---
>
> ## 8. ⭐⭐ 前に立つ者へ —— これが唯一いちばん効いた仕掛けである
>
> **2026-09-12〜13 の 9 巡すべてで、体が前に立つ者の誤りを 1 つ以上、実測で潰した。**
>
> | 前に立つ者が出した数 | 実測 |
> |---|---|
> | `src/` を読む門は 2 本 | **7 本**、余裕ゼロは 4 本 |
> | 検査 46 は逐語引用を消すと上がる | ⛔ **`src/` からは動かせない**（免除が計算されて一度も引き算されていない）|
> | 「原本が他所にある」が最多 | **9%。88.8% は地の散文** |
> | `PD-160` は開いている | **閉じている**（`裁定済` を探していなかった）|
> | 裁定 13 の的は 127 箇所 | **17 箇所**（`PD-1`〜`5` は仕様の表の行）|
> | 節の外を拒む番人（固定の行番号）| ⛔ **その番人自体が危険**（境界は編集で動く）|
> | 衝突は 1 つ | **3 つ**（`PD` `D` `R`）|
> | 登録簿は `docs/spec` を歩けばよい | ⛔ **3 つの木を歩かないと、作られた原因を捕まえられない** |
> | 台帳にも `PD-5` がある | **無い。`PD-4` の次は `PD-10`** |
>
> ⇒ **体に渡す指示には必ず書け:**
> **「⭐⭐ このブリーフの誤りを 1 つ挙げよ。見つからなければ『測っていない』と書け」**
> —— **「誤りは無かった」とは違う主張である。**
>
> ⚠️ そして **`PD-` の事故は、2 体が独立に、編集する前に見つけていなければ、
> 126 箇所の生きた仕様の指し先が静かに消えていた。**
>
> ### ⛔ 体が「攻撃か」と読む 2 つの原因（実測）
>
> - **scratchpad のファイル名を体ごとに分けよ** —— 親が同じ名前で上書きすると、体は「走らせたスクリプトがすり替わった」と読む
> - **「commit と push は前に立つ者が行う」と書け** —— 書かないと、体は足元で増える commit を「許可のない公開」と読む
>
> ---
>
> ## 8a. ⚠️ 別件として見つかったもの
>
> **`dist/index.html` は追跡された生成物であり、出来たのは 2026-09-10、`src/` の最終変更は 2026-09-12 である。**
> ⇒ ⛔ **古い生成物が公開されたままである。**
> ⚠️ `PD-` とは無関係であり、**この巡で直さない**。何が `dist/` を新しく保つのか（門に入れるのか、追跡をやめるのか）は**裁定案件**である。
>
> ---
>
> ## 9. この巡でやらないこと
>
> - ⛔ **掃除の再開** —— `PD-` が終わってから
> - ⛔ **`D` と `R` の改名** —— 別 CR、`PD-` のあと
> - ⛔ **`previous-project-result/`** —— 裁定により範囲外
> - ⛔ **タグ** —— 利用者が打つ
