---
type: Handover Prompt
title: 引継 — 掴めるのに要求が無い 8 行と、行の無い `PND-` の指し先
description: 2026-09-13 のセッションからの引継。CR-371 / CR-372 / CR-373 と CR-265 / CR-266 の訂正は着地して push 済み。残るのは 2 件、どちらも裁定が先に要る。表 T-023d の 8 行（実測済み・行 ID つき）と、台帳に行の無い `PND-` の指し先 3 つ。
tags: [handover, ruling-wanted, requirements, ledger]
status: ready
---

# 引継 — 掴めるのに要求が無い 8 行と、行の無い `PND-` の指し先

このファイルの `>` から下を、そのまま次のセッションの最初の指示として貼ってよい。

---

> # 現在地（2026-09-13 実測）
>
> | | 値 |
> |---|---|
> | 最新コミット | `b5008dc`（⭐ **push 済み**）|
> | ブランチ | `restart` |
> | 試験 | `vitest` 202 ファイル / 7,479 passed ＋ skipped 1 |
> | 門 | `npm run guard:commit` 終了コード 0、検査 46 本すべて緑 |
>
> ```bash
> npm run guard:spec     # 手ごと
> npm run guard:commit   # コミット前（precheck → tsc → gen:check → test → check）
> ```
>
> ⛔ **`git tag` は打つな。**タグは利用者が打つ。commit と push はこのリポジトリでは行ってよいが、**毎回コミット前に個人情報の点検を実測して報告すること**（利用者名・絶対パス・メールアドレス・秘密が 0 件であること）。
> ⛔ **一時ファイルはセッションの scratchpad へ置け。**リポジトリの外は読むだけ。
> ⛔ **絶対パスを 1 つも書くな**（precheck の罠 5）。
>
> ---
>
> ## 直前のセッションが閉じたもの（読み返す必要はない）
>
> | | |
> |---|---|
> | `CR-371` | `PD-` を廃し `PTD-` / `PND-` に割った。検査 51 が門 |
> | `D-` / `R-` 改名 | `DEV-` / `CHN-` / `DFC-` / `JDG-` へ。検査 52・53 が門 |
> | `CR-372` | `GL-008`「描いたものを直接掴んで日程を変えられる」を新設し、`CH-1` に載せた |
> | `CR-373` | 文書を変える 6 つの掴みを `GL-008` の下へ置いた（`GL-008` の子 1 → 7）|
> | `b5008dc` | `CR-265` / `CR-266` の問い ① を宣言された木で測り直して訂正した |
>
> ---
>
> # ⛔ 件 1 — 掴めるのに、その結果を定める要求が 1 つも無い 8 行
>
> ## 何が起きているか
>
> 表 T-023d（`docs/spec/01-04-requirements.md:3190`）は**掴める場所 21 通り**を優先順に並べた表である。
> ⛔ **そのうち 10 行が、行のどのセルでも要求（`FR-nnn`）を 1 つも引いていない。**
> ⇒ **掴んだら何が起きるかが、表の中の散文だけで決まっている。**
>
> `GR-10`（名称ラベル）と `GR-11`（担当ラベル）の 2 行は ⛔ **掴んで動かさない（MUST NOT）**と表自身が定めているので、掴みが無い以上それで正しい。
>
> ⇒ **閉じるべきは 8 行である。**
>
> | 行 | 掴む場所 |
> |---|---|
> | `GR-3` | **予定の開始点** |
> | `GR-4` | **予定の終了点** |
> | `GR-6` | 実績の終了点 |
> | `GR-13` | 依存線 |
> | `GR-14` | コメントボックス／ハイライトボックス |
> | `GR-15` | 実績のマイルストーン |
> | `GR-1` | フェードイン |
> | `GR-2` | フェードアウト |
>
> ⛔⛔ **`GR-3` / `GR-4` がここに居ることが、いちばん重い。**
> **予定バーの端を掴んで開始日・終了日を変える** —— この道具でもっとも普通の操作の結果を定める要求が、1 つも無い。
> ⭐ 隣は引いている: `GR-12`（予定バー本体）と `GR-5`（実績の開始点）はどちらも `FR-011` を引き、`GR-7`（進捗マーカー）は `FR-013`、`GR-8`（再開アイコン）は `FR-044`、`GR-16`（基準日線）は `FR-046` を引く。**端だけが抜けている。**
>
> ## ⭐ 測った方法（写すな、走らせ直せ）
>
> 表 T-023d の 21 行それぞれの**行そのもの**を読み、どのセルにも `FR-\d+` を 1 つも引いていない行を数えた。
>
> ```
> 表 T-023d の行                21
>   行の中で要求を引いている      11
>   引いていない                 10
>   うち MUST NOT（掴みが無い）  −2   GR-10 / GR-11
>   ⇒ 閉じるべき                  8
> ```
>
> ⚠️⚠️ **この測り方は行そのものしか読んでいない。**
> ⛔ **最初にやることは、表の下の閉じの散文（`:3218` 以降）が 8 行のどれかを名指していないかを確かめることである。**名指していれば、その行はこの件から落ちる。⭐ **落ちた数を報告せよ** —— 8 のまま進めるな。
>
> ## ⛔ 裁定が要る（勝手に決めるな）
>
> 1. **別 CR として立てるか。**`CR-373` は「いずれ裁定が要る」と書いて閉じなかった
> 2. 立てるなら **8 行を 1 本の CR で閉じるか、`GR-3` / `GR-4` だけ先に閉じるか**
>
> ⭐ **案を複数挙げ、比較し、根拠つきで最適案を示してから問え。**⛔ **説明に `GR-3` のような記号だけを並べるな** —— 「予定バーの右端を掴んで終了日を延ばす」のように、**ユースケースと具体名**で書け（利用者の指示）。
>
> ## ⛔ やってはならないこと
>
> - ⛔ **裁定の前に仕様書へ要求を書き足すな。**
> - ⛔ **`GR-10` / `GR-11` を「抜けている」と数え直すな** —— `CR-266` が `MUST NOT` を置いたので、無いのが正しい。
> - ⛔ **`CR-373` の第 2 節を読んで数を写すな。**当初そこは「13 通り」と書いており、**`端のぼかし 2 つ` と `フェード 2 つ` を二重に数えた誤りであった**（同日訂正済み）。⭐ **散文の数は主張である。測り直せ。**
>
> ---
>
> # ⛔ 件 2 — 台帳に行が無い `PND-` の指し先
>
> ## 何が起きているか
>
> `PND-` は欠陥台帳 `docs/development-records/pending-decisions.md`（実測 **212 行**）の行番号である。
> ⛔ **行が無いのに指している綴りが残っている。**
>
> ⭐ **`CR-371` が壊したのではない。**改名前から `PD-5` `PD-7` … として宙に浮いており、`CR-371` は綴りを変えただけである。検査 40 の基準線（`.claude/skills/spec-graph-check/spec-id-references-baseline.txt:56`）が既にそう書いている ——「`PND-100 .. PND-103` は取り下げられたのではない。**一度も書かれていない**。台帳は 95・104・105 と続き、`git log -S` は 4 つとも 0 コミットを返す」。
>
> ## ⭐ 実測し直した（2026-09-13）—— 引き継ぎ元の「7 件」はもう古い
>
> | id | いま指している場所 | 何か |
> |---|---|---|
> | **`PND-120`** | `tests/unit/uf-53.test.ts:37`「The gap is PND-120」| ⛔ **本物。**試験が穴の番号を名指しているのに行が無い |
> | **`PND-161a`** | `docs/review/rulings-2026-08-23/RULINGS.md:149` | ⚠️ **枝番。**台帳の `PND-161` は 1 行で在る |
> | **`PND-161b`** | 同 `:163` | ⚠️ 同上 |
> | `PND-7` | `docs/development-rules/06-pending-decisions.md:84` | ⭐ **規則書の書き方の例**（`// @provisional PND-7`）。指し先ではない。**触るな** |
> | `PND-100` | `docs/development-records/fixed-defects.md:286` | ⭐ **歴史の記述**。`src/` に注は 1 件も残っていない。**触るな** |
> | `PND-103` | 基準線自身の説明文 | 説明のための引用。**触るな** |
> | `PND-5` | **どこからも指されていない** | 消えた |
>
> ⇒ **閉じるのは 3 件だけである。**
>
> ## 各件の中身
>
> **`PND-120`** —— 表 T-024 の `IO-6` は「いまの画面を画像として別のアプリへ渡す」と定めるが、継ぎ目は SVG の文字列を渡しており、`CN-2` が認めるどのブラウザも SVG をクリップボードの画像として受けない。⭐ **その穴の番号として `PND-120` が試験の頭注に書かれている。**⛔ **台帳にその行は無い。**
>
> **`PND-161a` / `PND-161b`** —— 台帳の `PND-161`（`:87`）は **1 行で 2 つの主題**を持つ:「座標の丸めの許容差**と**、SVG の正規化の規則」。`docs/review/rulings-2026-08-23/RULINGS.md` はそれを §08（丸めの許容差）と §09（正規化の規則）に割り、**割るときに枝番を発明した。**⚠️ `PND-162` は別に台帳に在る。
>
> ## ⛔ 裁定が要る（案を挙げて比較し、根拠つきで問え）
>
> | | `PND-120` |
> |---|---|
> | 案 ① | 台帳に行を立てて番号を与え、試験の頭注をその番号に向ける |
> | 案 ② | 試験の頭注から番号を外し、穴を散文だけで述べる |
>
> | | `PND-161a` / `PND-161b` |
> |---|---|
> | 案 ① | `PND-161` を 2 行に割り、枝番を台帳の正規の行にする |
> | 案 ② | 枝番を捨て、`RULINGS.md` の 2 つの節をどちらも `PND-161` に寄せる |
>
> ⭐ **どちらの件も、決める前に測れ**: その番号を読んでいる検査が在るか（検査 25 は `@provisional PND-nnn` を読む）、台帳の行を増減させると門の数がどう動くか。
>
> ## ⛔ やってはならないこと
>
> - ⛔ **`PND-7` / `PND-100` / `PND-103` を「直す」な。**例文・歴史・説明文であり、直す対象ではない。
> - ⛔ **基準線の数を、測らずに下げるな。**
>
> ---
>
> # ⚠️ ついでに見つけていて、まだ誰も触っていないもの
>
> 検査 40 が **「`src/` と `tests/` に 152 件（前は 153）。基準線を下げて地歩を固めよ」**と言っている（`.claude/skills/spec-graph-check/spec-id-references-baseline.txt` の 1 行目が `153`）。
> ⛔ **緑であり、失敗ではない。**⚠️ **下げるかどうかは別件。**件 1・件 2 のついでに黙って下げるな。
>
> ---
>
> # ⭐ 先に読め
>
> | 読むもの | なぜ |
> |---|---|
> | `change-request/CR-373-the-six-grabs-that-change-the-document.md` | 件 1 の前段。⛔ **第 2 節の訂正を必ず読め** |
> | `change-request/CR-372-the-goal-that-was-missing-under-the-first-challenge.md` | `GL-008` がなぜ立ったか |
> | `docs/spec/01-04-requirements.md:3190`〜 | 表 T-023d 本体と、その下の閉じの散文 |
> | `docs/development-rules/06-pending-decisions.md` | `PND-` の作法（件 2） |
>
> ⛔ **判定基準を写した文書は 1 つも無い。原典を読め。**
