# 見本 ／ 仕様書 ／ 実装 —— 全数の突き合わせ（行の操作子）

- 日付: 2026-08-31
- 利用者の指示（2026-08-31、逐語）: **「引き継ぎ書にサンプルと仕様書と今の実装を徹底的に比較して、サンプルに合わせて仕様書を書き、仕様書に合わせて実装するようにしろ。」**
- 突き合わせる 3 つ:
  1. **見本** `previous-project-result/11-row-controls/row-controls-sample.html`
  2. **仕様書** `docs/spec`
  3. **実装** `src/` を出荷ビルド（`dist/index.html`）で実測

⛔ **見本から実装へ直接跳ばない。**⛔ **仕様書を飛ばした分は、次の巡で必ず食い違う。**

---

## 0. ⭐ どうやって測ったか（**両方とも実物である**）

| | 何を | どう |
|---|---|---|
| 見本 | `row-controls-sample.html` | ⭐ **Chromium で開いて `getBoundingClientRect` で測った**（`scratch/probe/measure-sample.mjs`）。⛔ **CSS を読むだけでは形が出ない** —— 例: `.cnt` の位置は `.nm{flex:1}` が決めるので、`.cnt` の規則をいくら読んでも「行の右端に立つ」ことは出てこない |
| 実装 | 出荷ビルド 1920 × 1080 | ⭐ `tools/probe/harness.mjs`。実ポインタで押し、`getComputedStyle` と `elementFromPoint` で測った |
| 仕様書 | `docs/spec` | ⭐ 行 ID で当てた。⛔ **当たる行が無ければ「無い」と書いた** |

⚠️ **この節の数値はすべて実測である。**⛔ **絵から読んだものは 1 つも無い。**

---

## 1. ⛔⛔ 前巡の記録 `D-162` は **両側とも偽であった**

前巡はこう書き残していた:

> ⚠️ `D-162` 件数が「行の名前の隣」でなく、行の右端に寄っている（実測）。
> ⭐ 見本は grip → nm → cnt の順の流れで、名前の直後である

**実測は 2 つとも違った。**

| | 前巡の記録 | ⭐ 実測 |
|---|---|---|
| 見本の件数の場所 | 「名前の直後」 | ⛔ **行の右端である。**`x=670`・幅 18・行の右端は 699。`.nm{flex:1}` が名前を伸ばすので、`.cnt` は右端へ押し出される |
| 実装の件数の場所 | 「行の右端に寄っている」 | ⛔ **右端ではない。**`x=46`（パネルの右端は 170）。**行の名前の文字の上に重なっている** |

⭐⭐ **教訓は 1 つ**: **見本の CSS を読んで「順序」を語ると、`flex:1` を見落とす。**⛔ **見本も実物として測れ。**

---

## 2. ⭐⭐ 3 列の表

**判定の読み方** —— ✅ 3 つが一致／⚠️ 仕様に行が無い（実装が発明している）／⛔ 3 つのどれかが食い違う。

### A. パネルの頭（段 0）

| # | 見本（実測）| 仕様書の行 | 実装（実測）| 判定 |
|---|---|---|---|---|
| A1 | 入口は左から `∨` `∧∧` `∨∨` `⊞` の 4 つ | `HF-10` / `HF-12` / `HF-16` / `HF-17` | `IC-92`@66 `IC-78`@92 `IC-74`@118 `IC-93`@144 | ✅ |
| A2 | 開くものが無いとき `∨` は薄い | `HF-16` ／ `FR-029` | `IC-92` は `rgb(217,221,226)` ＝ `S-149`、`aria-disabled="true"` | ✅ |
| A3 | 畳み込みが無いとき `∨∨` は薄い | `HF-10` ／ `FR-029` | `IC-74` 同上 | ✅ |
| A4 | 薄い入口も押下は届く（`disabled` にしない）| `FR-029`（⛔ 無効にしてはならない）| `disabled` は付いていない。`aria-disabled` だけ | ✅ |
| A5 | 全畳みは段 0 自身を畳む ⇒ **行が 1 つも描かれない** | `HR-2`（MUST）／ `S-211` | `IC-78` を押すと **描かれる行 0** | ✅ |
| A6 | 頭は畳み込み数を **語で**示す —— 「**・4 行を畳み込み中**」 | `HF-12`「示すこと（MUST）」⛔ **形を定めていない** | **`▾ 27`**（`x=37, y=37`、`S-153`、12px。⭐ **行と同じ形にそろえた**）| ⚠️ **裁定待ち `PD-415`**（語にするか）|

### B. 行の描かれ方

| # | 見本（実測）| 仕様書の行 | 実装（実測）| 判定 |
|---|---|---|---|---|
| B1 | 流れは **掴み代 → 名前 → 件数**、操作子は右端に重ねる | `HF-4`（右端に揃える）／ `GR-20`（掴み代は名前の直前）| 掴み代 → 名前（操作子と件数は重ね）| ✅ |
| B2 | 掴み代は `⋮⋮`。字下げの内側、名前と同じ行、地は塗らない、罫の色 | ✅ `HF-15`「**印は縦に並べた 2 本の三点リーダ（`⋮⋮` U+22EE を 2 つ）とすること（MUST）**」（CR-321 で足した）| `⋮⋮`、`x=12/24/36`（`S-37`＝12px の字下げに追随）、幅 16（`S-138`）、`color:var(--gr-rule)`、地は透明 | ✅ **決着**（実装は 1 文字も動かしていない）|
| B3 | 名前は伸び、収まらなければ `…` | `FR-085` | `Phone Settings Scr…` | ✅ |
| B4 | 件数は **`▾ N`**。**行の右端**、11px、`--note`、地は塗らない | ✅ `HF-18`「**置く先は行の右端とすること（MUST）**」「**数の前に…下向きの三角（`▾` U+25BE）**」（CR-321 で足した）| **`▾ 27`**。`x=138`（パネルの右端は 170）。12px、`S-153`、地は透明 | ✅ **決着**（台帳 `D-162`。実物で確認済み）|
| B5 | 抱えている行の**左辺に 3px の帯**（`--note` 70%）| ✅ `HF-18`「**太さは…`S-213` とすること（MUST）**」＋ 表 T-206 に `S-213` ＝ 3px を新設 | `box-shadow: inset 3px 0 0 0 rgb(168,96,15)`（濃さ 100%）| ✅ **太さは決着**（台帳 `D-163`）。⚠️ **濃さは仕様に行が無いまま** |
| B6 | 行の操作子は左から `∧` `∨` `∧∧` `∨∨` `⊞` `⊠` `📌` | `HF-1`（2 × 2 の格子）／ `HF-4`（足す・消す・ピンの順）| `IC-59`@26 `IC-90`@46 `IC-77`@66 `IC-58`@86 `IC-91`@106 `IC-82`@126 `IC-60`@146 | ✅ |
| B7 | 操作子はポインタが乗っているあいだだけ。下に地を 1 枚 | `HF-6`（MUST）色は `S-150` | `visibility:hidden` → `visible`、地は 1 枚 `x=26..170`、`S-150` | ✅ |
| B8 | 留めた行の `📌` は乗っていなくても描き、塗る | `HF-6` の唯一の例外 ／ `EN-3`（`S-151`）| 留めた行だけ `visibility:visible`、地 `rgb(33,75,130)` ＝ `S-151` | ✅ |
| B9 | 留めた行は**行の地も薄く塗る**（`--pin` 9%）| ⛔ **無い。**`FR-098` は「行を一覧しただけで読めること」を `HF-6` の常時描画で満たす | 行の地は他の行と同じ `rgb(246,247,249)` | ⚠️ **裁定待ち `PD-414`** |
| B10 | 留めた行はパネルの先頭へ上げる | `FR-098` | `Phone App` を留めると `y=85`（先頭）へ | ✅ |
| B11 | 行の高さは一定（34px）| `FR-042`（行ごとに違う）| 64 / 96 / 101 / 108 / 148px | ✅ **見本が簡略なだけ。仕様が正** |
| B12 | 字下げの刻みは 16px | `S-37`＝12px | 12px | ✅ **設定値。見本は自分の値と断っている** |

### C. 折り畳みの振る舞い

| # | 見本（実測）| 仕様書の行 | 実装（実測）| 判定 |
|---|---|---|---|---|
| C1 | `∧` はその行を隠す（配下ごと）| `HF-3` ＝ `HR-6` | `IC-59` | ✅ |
| C2 | `∨` は直下の子だけを開く | `HF-13` ＝ `HR-7` | `IC-90` | ✅ |
| C3 | ⛔ **`∧∧` は配下を残らず畳み、`直下の子も描かれなくなる`** —— 押すと `Mobile Client` だけが残る | `HR-4`「配下をすべて閉じる」／ `HF-11`「その行自身を畳んではならない」⇒ **直下の子は描かれたまま** | ⛔ **`IC-77` を押しても `Phone App` と `Tablet App` は描かれたまま。**消えるのは孫から下 | ⛔⛔ **裁定待ち `PD-413`** |
| C4 | `∨∨` は配下をすべて開く（隠した行も戻す）| `HF-2` ＝ `HR-3` | `IC-58` | ✅ |
| C5 | 畳んだ親の下に「開いている」行が残らない | `HR-1a`（MUST）| 画面上の反例は作れず | ✅ |
| C6 | 件数は**その下に畳み込まれている行の総数**（`Mobile Client` ＝ 4）| `HF-18` ⛔ **2 通りに読める**（`PD-412`）| 総数（`Mobile Client` 27 ＝ `Phone App` 15 ＋ `Tablet App` 12）| ⚠️ **裁定待ち `PD-412`** |
| C7 | 頭の `∧∧` と行の `∧∧` は**同じ論理**（`headfold=same` が既定）| `HR-2`「本行と `HR-4` は同じ動作の範囲違い」 | ⛔ **頭は段 0 を畳んで空にする。行は直下の子を残す。**⇒ **1 階層ずれている** | ⛔ **`C3` と同じ根（`PD-413`）** |

### D. 掴んで動かす

| # | 見本（実測）| 仕様書の行 | 実装 | 判定 |
|---|---|---|---|---|
| D1 | 軸は最初に閾値を超えた向きで決まり、離すまで変わらない | `HF-15`／ `S-208`＝6px | 前巡に実測（引継書 §1）| ✅ |
| D2 | 上下＝位置。親をまたぐ。段は変わらない | `HF-15` | 同上（`Phase Gates` が親をまたいだ）| ✅ |
| D3 | 左右＝段。右は直前の兄弟の末子、左は親の次の兄弟 | `HF-15` | 同上 | ✅ |
| D4 | どちらも配下ごと動く | `HF-15` | 同上 | ✅ |
| D5 | 留めた行は掴めない | `GR-20`（MUST NOT）| 同上。⚠️ **読む側の再確認は無い（`PD-410`）** | ⚠️ **裁定待ち** |
| D6 | 生きている軸を掴んでいる行に描く（上下＝左右の辺、左右＝上下の辺）| `HF-15`／ `S-151`・`S-152` | 前巡に実測 `rgb(33,75,130)` / `rgb(31,122,61)` | ✅ |
| D7 | 拒まれた向きへの追従は途中で止める | `HF-15`／ `S-212`＝0.4 | 前巡に実測（200px 引いて 5px）| ✅ |
| D8 | 掴んでいる行に地を敷く | `HF-15`（MUST）⛔ **色の行が無い** | ⛔ **未確認** | ⚠️ **仕様に色が無い** |
| D9 | 畳まれた群の中の場所は選ばない | `HF-15`（MUST NOT）| 前巡に実測 | ✅ |
| D10 | 掴む足場が消えたらドラッグを終える | ⛔ **無い** | ⛔ **未確認** | ⚠️ **仕様に行が無い** |

### E. 足す ／ 消す

| # | 見本（実測）| 仕様書の行 | 実装 | 判定 |
|---|---|---|---|---|
| E1 | `⊞` は配下の末子に、名前を空で立てて打たせる | `HF-14`（MUST）| 前巡に実測 | ✅ |
| E2 | `⊞` も深さの上限で止まる | `FR-085` | 前巡に実測 | ✅ |
| E3 | `⊠` は配下ごと消す | `FR-032` ／ `CD-2` | 前巡に実測 | ✅ |
| E4 | 名前を打ちかけの行は 1 つだけ | ⛔ **無い** | ⛔ **未確認** | ⚠️ **仕様に行が無い** |
| E5 | 頭の `⊞` は段 0 の末子へ | `HF-17` | 前巡に実測 | ✅ |

---

## 3. ⛔ 出てきた仕事（**この表から落ちたものだけ**）

### 3.1 ✅ **直した**（CR-321、版 1.75。**見本が形を持っていたので裁定を待たなかった**）

| # | 何が | 直す順 |
|---|---|---|
| **①** | **件数が行の名前の文字に重なっている**（`B4`）—— ポインタを乗せると `IC-90` に完全に覆われる（`elementFromPoint` が `BUTTON IC-90` を返す）| 仕様に「行の右端」を書く → 実装 |
| **②** | **件数に `▾` が無い**（`B4`）| 仕様に字形を書く → 図形 → 実装 |
| **③** | **掴み代の字形 `⋮⋮` が仕様に無い**（`B2`）—— 実装は既に `⋮⋮` を描いており、**仕様だけが黙っている** | 仕様に書く（実装は動かない）|
| **④** | **抱えている帯の太さ（3px）と濃さが仕様に無い**（`B5`）| 仕様に書く（実装は動かない）|

### 3.2 ⛔⛔ 裁定が要るもの

| # | 何が | なぜ裁定が要るか |
|---|---|---|
| **⑤** | **`∧∧` が 1 階層浅い**（`C3` / `C7`）| ⛔ **`HR-4` と `HF-11` の MUST を書き換えることになる。**⭐ **仕様の側に既に 2 つの矛盾がある** —— `HR-2` は「頭の入口は `HR-4` の段 0 である」と言い、`HF-1` は「`HR-4` を 1 度押せば `HR-5` と同じ絵になる（版 1.67 の実測）」と言う。**どちらも今のビルドでは偽である**（実測）|
| **⑥** | **留めた行の地を塗るか**（`B9`）| 見本は塗る。`FR-098` は塗らずに済ませている |
| **⑦** | **頭の件数を語にするか**（`A6`）| 見本は「N 行を畳み込み中」。パネルは 170px しかない |
| **⑧** | `PD-412`（件数の射程）| 既出 |

---

## 4. ⭐ この表の使い方

⛔ **1 行ずつ潰したら、この表の判定を書き換えること。**
⛔ **表を消してはならない** —— **次に「サンプルと合っているか」を問われたとき、測り直す代わりにここを読む。**

---

## 5. ✅ この巡で閉じたもの（2026-08-31、CR-321 / 版 1.75）

| # | 何を | 台帳 |
|---|---|---|
| ① | 件数を行の右端へ移した（**名前の 2 文字目の上に描かれていた**）| `D-162` |
| ② | 件数に `▾` を付けた | `D-164` |
| ③ | 掴み代の `⋮⋮` を仕様に書いた（**実装は動かしていない**）| `D-164` |
| ④ | 2 本の帯の太さを 1 つの値（`S-213` ＝ 3px）にした | `D-163` |
| ⑤ | e2e の赤 2 件を直した（**試験が `IC-3` と `IC-2` を取り違えていた**）| `D-165` |

## 6. ⛔ この巡で積んだ裁定（**利用者へ**）

| ID | 問い | 推奨 |
|---|---|---|
| `PD-413` | **`∧∧` が見本より 1 階層浅い** —— 直下の子が残る | ⭐ **見本に合わせる**（`HR-2` と `HF-1` が既にそれを前提に書かれている）|
| `PD-414` | 留めた行の地を塗るか | ⭐ 現状維持（塗らない）|
| `PD-415` | 頭の件数を語にするか | ⭐ 現状維持（`▾ N`）|
