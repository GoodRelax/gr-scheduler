# 裁定の結果（2026-08-23 第 2 巡）

⭐ **利用者の裁定を受けた。全 14 問。** ⛔ **本書は決定の記録である。仕様はまだ 1 文字も動いていない。**
⚠️ **当てるのは変更要求（`CR-215` 以降）である。** 手順は規則 02。

⭐ **前の巡は [`../rulings-2026-08-22/RULINGS.md`](../rulings-2026-08-22/RULINGS.md)（16 問）。あちらは仕様も実装も当て終えている。**

---

## ⛔ まず —— 問う前に 8 件を読み切った結果、**2 件は裁定が要らなかった**

⭐ **規則 06 の 1.**「載せる前に必ず引く」を実行したところ、`PD-171` と `PD-174` は**既に答えが在った。**
⚠️ **裁定待ちの一覧に「未裁定」と書かれていることは、未決であることの証明ではない。**

| PD | 何が既に在ったか |
|---|---|
| **`PD-171`** | ① `FR-055` STATEMENT が「**縦横の倍率と表示位置**を…合わせること」と既に定めている ② `BO-3` が「`OP-10` に当たるときは、**この 2 つ**を `BO-4` が決める」 ③ **A-appendix 版 0.47（`CR-160`、利用者の裁定 2026-08-17）が `OP-10` を倍率と表示位置の両方へ広げたと記録している** ④ 表 T-064 の前書き（`:325`）が「**引数・戻り値は `src/` の公開エントリが持ち**」と定めるので、署名は仕様のオブジェクトではない ⑤ **式は前プロジェクトの確定文書が持つ**（`03-ui-naming/ui-detail-spec-ja.md:1400` /`:1465` /`:1478`）|
| **`PD-174`** | **前プロジェクトが 6 案を比較して確定していた**（`appheader-palette-icons.html:1299`）—— 「**ヘッダーとモーダルの両方**に置く」「6 案を比較して**地球儀＋`ja`** を採用」。⭐ 図 F-019 の `IC-21` は既に地球儀なので、**新しい図形を 1 つも要求しない** |

---

## 01 —— `PD-169` 押下がどの行で始まったかを、書き込みの側から問う道

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 行 ID をどこに置くか | ⭐ **`PointerPress` が運ぶ。** 押下値に 表 T-023a の行 ID の欄を足す |

**根拠**: `CS-2` が一貫性の単位を「**身振り 1 回（掴む）／ポインタを押した時点**」と定めており、**行 ID の寿命は `PointerPress` の寿命と一致する。**
⭐ `PointerPress` は既に `hit` と `on` を**呼び手が解決して**運んでおり、3 つ目として自然である。

**退けた案**:

| 案 | なぜ退けたか |
|---|---|
| `PI-18` に `pressRowOf` を公開する | ⛔ シェルが `pressed` の隣にもう 1 つ値を持つことになる。表 T-064 の `PI-18` が 1 行動く |
| 表 T-023a をシェル側で 2 度読む | ⛔ `R2.7`（DRY）の重複。しかも `:2252` が「判定順序を適用するのは日程の描画領域だけとすること（MUST）」と適用範囲を縛っている |

⚠️ **帰結として `IF-7` も追随が要る**（代替案ではない）—— 表 T-065 の `IF-7` はいま「身振りの最中か**どうか**」という真偽だけを運ぶ。`AG-9a` が拒否の値に「**理由の区分**」を求める以上、「パンだった」と「本物のドラッグだった」を区別できなければならない。

⛔ **落ちる試験**: `tests/unit/uf-48-write-moment.test.ts` の 2 件（いま赤で立っている）。

---

## 02 —— `PD-170` 全体表示を取り消すと倍率と表示位置まで戻ること

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | どう直すか | ⭐ **押下を 2 つの書き込みに割る。** ①倍率と表示位置＝段を積まない ②畳みを捨てる＝段を 1 つ積む |
| **Q2**（副裁定）| 1 回の押下で 2 回書いてよいか | ⭐ **よい。** 表 T-067 の `:473`「差し替えは 1 つの参照の置き換えとすること（MUST）」は**個々の書き込みの原子性**を求めるものであり、1 つの身振りが 2 回書くことを禁じていない |

**根拠**: `WS-4` は「**その書き込みの前の文書**」を積む。だから②の段は**既に新しい倍率を含む。**
⭐ **取り消すと「新しい倍率＋古い畳み」になり、これは `UN-17` が約束したそのものである。**
⛔ **`FR-031` の段の定義を 1 文字も動かさない。**

**退けた案**:

| 案 | なぜ退けたか |
|---|---|
| 現状を認めて `UN-17` の注を直す | ⭐ 仕様 2 セルで済むが、`UN-17` の約束を下げることになる |
| 段を枝で絞る（`schedule` だけ戻す）| ⛔ `UN-13`（テーマ・文字サイズ・暦・文書名…）は**対象**なのに見せ方の群に居るので、それが戻らなくなる。**地雷** |
| 全体表示を履歴の対象から外す | ⛔ `UN-17` が「対象」と明文で書いており、仕様の反転になる。`HF-7`/`HF-8` の対の注ごと約 4 行動く |
| 段を列で絞る | ⛔ 最も高い。`FR-031` の段の定義と `S-95` の測り方（詰めた `GRS JSON` へ直列化）が崩れる。**表 T-027 は列でなく操作を分類している** |

---

## 03 —— `PD-171` 全体表示が表示位置も選ぶこと

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 分類 | ⭐ **`E` → `B` へ直す。** 仕様のオブジェクトは 0 個動く |
| **Q2** | 式 | ⭐ **前プロジェクトの確定文書のとおり** —— `scrollDate` ＝ 描画矩形の左端の日付、`scrollGroupId` ＝ 最上行の `TaskGroup.id`、**中央寄せはしない** |

**根拠**: 規則 03 の 6. が参考の順序を「`docs/spec` → `docs/reference/` → `03-ui-naming/` → `08-poc/`」と定める。
⭐ **仕様は「位置も決める」とは言うが式を言っていない。** その穴を `03-ui-naming/ui-detail-spec-ja.md` §6-7（**確定 2026-08-02**）が持っている。

⚠️ **`08-poc` は採らない** —— 試作は `scrollLeft = 0` としており、これは「描画矩形の左端」ではなく「原点」である。**確定文書のほうが強い。**

⚠️ **未解決が 1 つ残る**（本裁定の射程外）: `scrollGroupId` が**この回で描かなかった行**を指すとき。`OP-10` の 2 条件のどちらでもない。実装は ⛔ 印を付けて推測しないこと。

---

## 04 —— `PD-172` 確認を立てる者

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 誰が持つか | ⭐ **`CP-25`（`SingleHtmlShell`）が現在値として持つ。** 仕様のオブジェクトは 0 個動く |

**根拠**: 表 T-060 の `LY-5`「**現在値を保持するのはこの層だけである**」／ 表 T-062 の `CP-25`「**現在値を保持する。**」
⭐ **立った問いと、その答えを待つあいだの削除命令は、現在値である。**
⚠️ `PI-25` の「他のコンポーネントから呼ばれるメンバを持たない」は破らない —— **公開しない状態だからである。**

**退けた案**:

| 案 | なぜ退けたか |
|---|---|
| 表 T-066 に `CS-5` を足す | 1 行＋入れ子の数え直し 3 文＋`CA-3` の追随 |
| `CS-4` を広げる | ⛔ `CS-4` の着地は `replaceDocument`（MUST）だが、削除は `applyDocumentChange` である。**着地の MUST ごと書き換えが要る** |
| `ScreenState` に持たせる | ⛔ 最も高い。`PI-36` の中身は構えと `S-99e`/`f`/`g` で**閉じた列挙**であり、表 T-206 に新しい行も要る |

⚠️ **開いたまま**: `Confirmation` が `S-99g` にいう「面」かどうか。面なら `Esc` の第 1 階層が `IC-70` と競合する。⛔ **仕様は言っていない。**

---

## 05 —— `PD-173` 選ばれた表示言語が次の起動まで残らないこと

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 誰が運ぶか | ⭐ **`CP-25` が持つ**（`PD-172` と同じ形）。仕様のオブジェクトは 0 個動く |

⭐ **運ぶ行は 4 つで確定していた** —— `S-99` / `S-99a` / `S-99b` / `S-99c`。
**根拠は `LM-14` 自身**:「**全数は表 T-206 の別枠の行、すなわち「別枠。`localStorage` に置く。」と明記した行である**」。
⛔ `S-99e` / `S-99f` / `S-99g` は**該当しない** —— 「同上」が指すのは別の理由（画面の使い方）であり、あの 3 行は `PI-36 ScreenState` が既に持ち主である。

⚠️ **制約**: `FR-026` の RATIONALE が `localStorage` の鍵に接頭辞を求める。**自動保存と接頭辞を共有すること。**

**退けた案**: `CP-29` を広げて改名（表 5 行＋木＋フォルダ名）／ 新コンポーネント `CP-39`（38→39 の数え直しが `SU-1`・5.3・継ぎ目 9→10・図 F-013 へ波及）。

---

## 06 —— `PD-174` 押す前に現在の言語が読めること

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 見せ方 | ⭐ **前プロジェクトの確定形。** ヘッダー＝**地球儀（既存の `IC-21`）＋小文字の言語コード**、モーダル＝`[ja][en]` を `×` の左 |
| **Q2** | 運ぶ欄 | ⭐ **`AppHeaderItems.language`**（`HelpModal.language` と対称）。仕様のオブジェクトは 0 個動く |

**根拠**: `appheader-palette-icons.html:1299` /`:1053` /`:1070`。大文字を退けた理由は ISO 639-1（言語コードは小文字、`JP` は国コードの書式で言語と国を混同させる）。
⛔ **`CommandItem.isPressed` は使わない** —— 「入っているトグル」であり、**2 言語の選択に「切」は無い。**

⚠️ **`ja` / `en` は「語」ではなく「値」として扱う** —— 表 T-206 の `S-99` が「`language`（`ja` / `en`）」と**表に literal を刷っている**。`FR-038` の「要求にも表にも語そのものを書いてはならない（MUST NOT）」が語であれば、その行自体が違反になる。

---

## 07 —— `PD-175` 「他の行に表示されている」印

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 媒体 | ⭐ **語で示す。** `display-words.json` に新しい鍵を立て、項目ごとの注釈語を置く |

⭐ **何の印かは調査で確定した** —— **行の削除確認ダイアログの中で、消える `Task` のうち「別の行に描かれているもの」に付ける印**である。画面の印ではない。
根拠: `FR-032`（`:1678`）「**消える `Task` のうち他の行に表示されているものには、その旨を示すこと（MUST）** —— 画面上その行に見えていないためである。**別の行に見えるのは 表 T-015a の `HM-10` による**」。

⭐ **語なら図形を 1 つも発明しないので、表 T-026 の `RC-13`（図形の決定は利用者の裁定）に触れない。** `NT-7` はそもそも文字の面である。

**退けた案**: 図形を新設（`IC-71`。表 T-109 の「70 行ある」の数え直し＋図 F-019＋`RC-13` の一覧へ波及。⛔ `RC-13` が「本仕様書の中で発明しない」と定める）／ 組版だけで示す（⛔ `FR-030` の同じ理屈に触れるおそれ）。

---

## 08 —— `PD-161a` 座標の丸めの許容差（＋ `PD-162`）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 書き出す桁（`PD-162`）| ⭐ **0.01 px の格子**（`Math.round(value * 100) / 100`）。いまの実装のまま |
| **Q2** | `WY-3` の許容差 | ⭐ **両側を同じ格子へ丸めてから、外接矩形の各辺で `|Δ| < 1.0 px` なら一致** |

**根拠**: `WY-3` は既に除外として「**丸めによる 1 px 未満の差**」と桁を書いている。⭐ **未決だったのは境と手続きだけである。**
⭐ **`CR-148:135` が残した指示**「Chapter 6.1 が丸め式を書いた時点で、この除外が許容差として残るのか不要になるのかを判定し直すこと」に直接答える —— **残す。**

**退けた案**: 丸めずに書き、判定側だけが丸める（⛔ 綴りが膨らむ。`0.30000000000000004` 形）／ 許容差 0 の厳密一致（⛔ `WY-3` 自身が「縮める順が両側で違うので端数の処理が一致しない」と書いており、見込みで落ちる）。

---

## 09 —— `PD-161b` SVG の正規化の規則

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | どこに何を書くか | ⭐ **表 T-228 に倣って Chapter 6.1 に新表を立て、`FR-080` を親に取る `SWS-7` を置く** |

⭐ **行の骨子**: ① C14N 1.1 を当てる ② 要素の間の空白のみのテキストを落とす ③ 数の綴りを両側で `PD-162` の格子に揃える ④ `id` は定数に限る。
⭐ **`FR-080` は 1 文字も動かない** —— `:3096` が既に「正規化と丸めの規則そのものは Chapter 6.1 が持つ」と委任している。
⚠️ **表 T-228 は自分で「本表が持つのは規格に足す分だけである」と XML に限定しているので、あちらを広げてはならない。**

**退けた案**: `SWS-6` を広げる（⛔ 親が `FR-021`＝MSPDI 往復で衝突。`NR-4`/`NR-5` は MSPDI 固有）／ 正規化せずバイト一致（⛔ `FR-080:3096` の MUST に正面から反する）。

⚠️ **同じ回で掃くこと**: `05-07-design.md:892` の `> 未記入。` は、その後に `SWS-1`〜`SWS-6` が 6 本入った時点で偽になっている。

---

## 10 —— `WY-2` の「同じ」の相手

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 何と比べるか | ⭐ **同じ走りの中の 2 回の書き出し同士** |

**根拠**: ⭐ **`WY-2` が測っているのは「書き出しの経路が決定的か」であり、桁数の問題ではない** —— 桁が何桁でも、決定的なら一致する。
⛔ **機や版をまたぐ浮動小数の再現性を判定に入れない。**
⚠️ **`CR-202` §7 が自分で「開いたまま」と挙げていた 3 件目**であり、引継書の開いた項目 (5) でもある。

**退けた案**: 版元に置いた見本と比べる（⛔ `MC-5` の基準ブラウザの版が上がるたびに見本を作り直すことになる）。

---

## 11 —— `CN-8` の内容セキュリティ方針（**新規。本巡で見つけた**）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 何を書くか | ⭐ **`default-src 'none'` を土台に、`img-src data:`（`CN-8` の指定）、`style-src 'unsafe-inline'`、`script-src` は埋め込みスクリプトの `sha256` をビルド時に算出して固定** |

⛔ **成果物に方針が 1 つも無かった。** ⭐ **実証済み**: 外部 `img` を仕込むと**実際にネットワークへ要求が出た**。`CN-8` の方針を入れた版では Chromium が要求前に遮断する。
**根拠**: `NFR-009`（不正な入力によって任意のコードが実行される状態にならないこと、MUST NOT）に最も強い。`nonce` はサーバーが要るので `file://` では採れない。

**退けた案**: `script-src 'unsafe-inline'`（⛔ 外部通信は止まるが、注入されたスクリプトの実行は止まらない）／ 今回は入れない（⛔ `NFR-004` の RATIONALE が「その開き方でだけ落ちる形がある」と名指しており、後回しにするほど見つかりにくい）。

⚠️ **過去の罠**: 文字列の鍵に制御文字が 1 つ混じるとハッシュが合わずアプリ全体が止まる。**鍵は ASCII だけ。**

---

## 12 —— 表示語 176 枠（`PD-160` / `PD-3` / `PD-4`）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | どう進めるか | ⭐ **agent が案を作ってよい。** 群ごとに承認を取る |

⛔ **これは 2026-08-21 の裁定（案 (a)「空のまま置く」）を、本巡に限り覆す指示である。**
⚠️ **その裁定の理由は「表 T-109 が明文で拒む確定名の新設を、機械が行うことになる」であった。** ⭐ **利用者がそのうえで案の作成を許可した。**
⭐ **案は 表 T-109 の「何の入口か」欄・表 T-037・表 T-023・`FR-072` の 3 見出しを根拠に作る。** ⛔ **発明ではなく引き写しに留める。**

---

## 13 / 14 —— 実装まわり

| # | ⭐ 裁定 |
|---|---|
| **`tests` を型検査に入れる** | ⭐ **やる。** `tsconfig.json` の `include` に `tests` を足す。⛔ **実測 60 件のエラーが 23 ファイルで出る**（`uf-51` の `askToWriteOver` 欠落 11 か所ほか）。⚠️ **直すのは試験台であって期待値ではない**（規則 04 の 1.）|
| **`modulepreload` ポリフィルを切る** | ⭐ **やる。** `build.modulePreload: { polyfill: false }`。⛔ これが成果物中で唯一の `fetch(` である |

---

## ⛔ 本巡で確認した仕様書の取りこぼし（裁定は要らない。同じ回で掃く）

```
tbl-glossary.md:436   「68 個の確定名」—— CR-211 で表は 70 行になった（433 行目と 437 行目は 70）
tbl-settings.md:326   「表 T-206 の注」を引くが、その注は存在しない
                      —— 当該の文言は S-90〜S-93 の「保存しない理由」の欄に在る。行 ID を名指すこと
05-07-design.md:892   「> 未記入。」—— SWS-1〜SWS-6 が 6 本入った時点で偽
```

---

# 裁定の結果（2026-08-23 第 3 巡）

⭐ **4 問。規則 06 の 3b「裁定は巡の先頭で取る」に従い、巡の最初に取った。**
⛔ **本節は決定の記録である。仕様も原稿も、まだ 1 文字も動いていない。**

---

## ⛔ まず —— 問う前に引いたら、承認が要ると見込んでいた 3 点のうち 1 点は既に裁定済みだった

⚠️ **引継書 28 の §5 は「承認が要る 3 点」の筆頭に「マイルストーン 8 図形の英名」を挙げ、
表 T-109 前文が「未決」と名指すことを根拠にしていた。** ⛔ **その一句が古い。**

| 何が既に在ったか | どこ |
|---|---|
| `milestoneGlyph` の 8 つの綴りは **`CR-172`（利用者の裁定）が決めている** —— `circle` / `hexagon` / `pentagon` / `diamond` / `square` / `star` / `triangleUp` / `triangleDown`、既定 `diamond` | `docs/spec/_source/erd.json` の `Task` の `milestoneGlyph` 列 |
| その裁定の記録 | A-appendix 版 0.53（2026-08-17）「**図形も意味も仕様書が既に決めており、決まっていなかったのは保存する英語の綴りだけだった**」 |

⛔ **`tbl-glossary.md:437` の「`milestoneGlyph` の 8 つの綴りは未決である」は、`CR-172` が当たった同じ日に偽になり、そのまま残った。**
⚠️ **版 0.52（`CR-169`）が「先に綴ると裁定を奪う」と書いて英名の欄を作らなかった、その理由が消えている。**
⭐ **承認対象は英名 50 語ではなく 42 語である。**

---

## 15 —— 進め方（4 継ぎ目をどう回すか）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 実装をどう分けるか | ⭐ **ワークフローで回す**（`.claude/workflows/wire-the-remaining-seams.js`）|

⛔ **走らせる前に台本の欠陥を 1 つ見つけ、直した。**

```
⛔ pipeline の第 2 段（試験体）も isolation: 'worktree' なので、実装体とは別の、
   基点から切り直した木で走る
   ⇒ 試験体は scheduleViolations も広げた IF-7 も「存在しない木」で試験を書く
   ⇒ 台本自身が命じる「壊して赤くなることを見よ」（規則 04 の 2.）が実行できない
⭐ 直し   台本に args.stage の門を足し、2 回の走りに割った
          実装 → 前に立つ者が合流点で 1 回検証 → 試験
          ⭐ これは規則 05 の「合流点で 1 回」とも一致する
```

⚠️ **走らせる前提が 1 つ足りない** —— **worktree は HEAD から切られるので、未コミットの 9 件が体から見えない。**

```
src/framework/single-html-shell/frame-loop.ts   HEAD 1248 行 / 作業木 1573 行
tools/precheck.py                                未追跡。worktree に来ない
```

⛔ **4 継ぎ目のうち `file-gateway` と `am-13-picture` は、まさにその `single-html-shell/**` を所有する。**
⭐ **よってコミットが先である**（利用者が行う。推奨文は引継書 28 の §9）。

---

## 16 —— 面の見出し 4 枠（`surfaces`）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 確定名を短縮するか | ⭐ **丸ごと刷る。短縮しない** |

```
Help Modal / AI Export Modal / Resource Roster / Export Chooser
```

**根拠**: 規則 03 の 1.「**仕様が名前を持っている概念に、別名を与えてはならない（MUST NOT）**」。
⚠️ **案（D-4）は `Help Modal` → `Help`、`Export Chooser` → `Export` と構造の語を落としていたが、
`Resource Roster` は種類の接尾語を持たないので落とせず、案自身が「4 枠のうち 3 枠だけ短縮する形になる」と内部不整合を申告していた。**

⭐ **帰結**: 案の「新しく起こした英語」の **F 群（面の見出しの短縮 2 語）が丸ごと消える。**
⚠️ **ja 側は動かない** —— 「ヘルプ」「AI 出力」「名簿」「書き出し」は 表 T-103 の `U-30` / `U-49` の逐語であり、短縮ではない。

---

## 17 —— `notices` 8 枠の刷り先

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 何を書くか | ⭐ **表 T-037 の「場面」欄を、作法の呼び名として転記する** |

⛔ **仕様は刷り先を述べていない。** `notices.ts` は `Notice.manner` を「行 ID を運ぶ継ぎ目であって語ではない」「見出しさえ書かない」と明記している。
⭐ **それでも原稿に節がある以上、語は要る。** ⭐ **転記であって創作ではない**ので、表 T-109 が禁じる「確定名の新設」に当たらない。

⚠️ **同時に決まったこと**: 案が `NT-2` / `NT-3a` で「通知」を「知らせ」へ寄せていた 2 枠は、**転記なので仕様の語（「通知」）に戻る。**

---

## 18 —— 表示語 174 枠の案そのもの

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | いつ書き込むか | ⭐ **先に案を読む。読んでから承認する** |

⛔ **したがって `_source/display-words.json` は本巡でもまだ空である。** `PD-160` / `PD-3` / `PD-4` は **未裁定のまま。**
⭐ **案は 2 か所に在る**:

```
previous-project-result/temp/display-words-proposal-2026-08-23.md   原本 51.8KB
                                                                    ⚠️ セッションの scratchpad から退避した
Artifact                                                            読む形。裁定 16 / 17 を反映済み
```

⚠️ **`IC-54` の 2 枠は案が「未決」と申告しており、本裁定でも埋まらない** —— 原因は仕様の欠陥である（下記）。

---

## ⛔ 本巡で原典に当たって確認した欠陥（裁定は要らない。変更要求で閉じる）

```
tbl-glossary.md:437   「milestoneGlyph の 8 つの綴りは未決である」
                      ⛔ CR-172（版 0.53、2026-08-17）が 8 つとも決めている。同じ日に偽になった

tbl-glossary.md:497   IC-54「構えた図形を置く打鍵を示す」、正は 表 T-036 の SK-19
  requirements:2440   ⛔ SK-19 は「その場の編集を確定する」（Enter）である
  requirements:2438   ⛔ SK-1 は「キーボードだけで図形を置く経路は持たない」と明文で定める
                      ⇒ 置く打鍵は存在しない。IC-54 の正が誤っているか、行そのものが古い
                      ⚠️ 引継書 28 の開いた項目 (27)。語を当てると誤りを画面に刷ることになる
```

⭐ **第 2 巡の末尾が挙げた 3 件（`tbl-glossary.md:436` の「68 個」・`tbl-settings.md:326` の在らぬ注・`05-07-design.md:892` の「> 未記入。」）と同じ回で掃くこと。**

---

# 裁定の結果（2026-08-23 第 4 巡）

⭐ **9 問。利用者はすべて提案どおりで裁可した。** ⛔ **本節は決定の記録である。仕様はまだ 1 文字も動いていない。**
⭐ **出どころは `4586e0a` で入った 4 継ぎ目の実装と、その体が返した `blocked` / `needsSpecChange` である。**

⚠️ **逐語は私が原典で引き直した。** ⛔ **2 件で体の申告と食い違い、引いた結果を採った**（19 と 23）。

⭐ **測った**（規則 02 の 2.、規則 05 の 6.）:

```
induced.py  種 39 / 39 が解決  ／  種の中の辺 14  ／  閉路 0
⇒ 1 つずつ書いてよい。⭐ ただし当てるのは 1 巡にまとめる
impact.py   T-103 56 行 ／ T-109 70 行 ／ T-220 17 行 ／ T-078 5 行 ／ T-006a 11 行
```

---

## 19 —— `OP-3` の 3 択を問う面（**最大の閉塞**）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | どこに面を立てるか | ⭐ **「開き方を選ぶ面」を 表 T-103 に立て、表 T-109 に 3 入口を置く** |

**根拠**: `OP-3`（`:3182`）が「**読んだ内容をどう扱うかを人に選ばせること（MUST）**」「**3 つとする**」「**`GRS` が勝手に決めてはならない（MUST NOT）**」と定める。
⛔ **表 T-103 に 3 択を問う面が無く、表 T-109 は自ら網羅と名乗るので `src/` で入口を発明できない。**
⭐ **先例が在る** —— `U-54` `Export Chooser`（「書き出しの形式を選ぶ面」）が**まったく同じ形**である。兄弟として立てれば新しい概念を作らない。

**退けた案**: `Confirmation` を 3 択へ広げる（⛔ `NT-7` が「続けるか取りやめるか」と 2 択で書き、`IC-69`/`IC-70` の対がその具体。定義ごと動く）／ 問いを 2 段に割る（⛔ `OP-3` が「3 つとする」と書いており、要求の書き換えになる）。

⭐ **下流は既に用意できている** —— `importDocument`（`PI-10`）は答えを引数で取り、表 T-230 の `RD-3`/`RD-4` が着地を持つ。**面と入口だけが欠けている。**

---

## 20 —— `data-role` の記法（**私が引き直して見つけた**）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 仕様と実装のどちらに寄せるか | ⭐ **表 T-006a の `W-4` に但し書きを足す** ——「ただし UI パーツの名を運ぶ `data-role` は `W-6` の形とする」 |

⚠️ **体は「UI 部品を頁の中でどう識別するかを決めた仕様が無い」と報告した。** ⛔ **引いたら、無いのではなく二重に決まっていた:**

```
表 T-006a の W-4   文字列判別値・`data-role`・CSS クラス ＝ kebab-case（例 `toggle-plan`）
表 T-006a の W-6   UI パーツ（散文で指すとき） ＝ PascalCase の複合語（空白あり）
dom-screen-surface.ts:1123   「data-role takes the surface's own name」＝ W-6 の形を書いている
tests/system/live-app.ts:38  `[data-role="Schedule Canvas"]` ⛔ kebab-case ではない
```

⚠️ **表 T-006a の前文が「同表が縛るのは `src/` のコードと本ソフトウェアが読み書きする鍵である（MUST）」と書いており、`data-role` の値は射程内である。**
**根拠**: 規則 03 の 1. —— **確定名を kebab へ訳すと、同じものに 2 つ目の綴りができる。** 行 ID で繋ぐ設計と同じ理屈で、名前そのものを運ぶほうが継ぎ目が細い。

**退けた案**: 実装を `W-4` に合わせる（⛔ `data-role` を書く 11 か所・当たり判定の走査・`tests/system`・表 T-103 との突き合わせが動き、**訳語表がどこにも無い**）。

---

## 21 —— 待ちのあとに立った問いを描く契機

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | どの契機か | ⭐ **`FT-1` の文言を広げる** ——「人の入力、および**その入力の、待ちをまたいだ続き**」 |

**根拠**: 表 T-078 は契機を 5 つに閉じ「**本表に無い契機でフレームを起こしてはならない（MUST NOT）**」と定める。
⚠️ **仕様は自分で `05-07-design.md:449` に「待つことそのものは画面を何も変えない ——「表 T-078 に契機が無いからである」」と書いており、待ちの終わりを詰め忘れていた。** `CR-210` は**着地**（`FT-2`）だけを論じた。
⭐ **事実としても正しい** —— そのフレームは `SK-11` を押した本人の押下の、遅れて来た残りである。⭐ **1 セル。`rows` は動かない。**

**退けた案**: `FT-6` を新設（⚠️ `rows` ＋1。契機の粒度が「入力」から「入力の一部」へ落ちる）／ `FT-2` に寄せる（⛔ 括弧が `WS-6`＝**文書**の差し替えに固定。問いは文書ではない）。

---

## 22 —— `erd.json` の関係が外部キーの列を持たない

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 機械可読な列名を足すか | ⭐ **足す。** 22 本すべてに、参照を持つ列と着地する列を書く |

⛔ **足さないと 表 T-220 の `IV-2` は永久に「表にはあるが誰も答えない行」になる。** 表 T-220 が 17 行と数えている以上、答えない行が残るほうが高い。
⭐ **`IV-1` の側は裁定が要らない** —— 列は `key`（`PK`/`FK`/`PK/FK`）を**既に持っており**、`generate_entity_types.py` が出していないだけである（`DATE_COLUMNS` は同じ仕組みで出ている）。

---

## 23 —— ε に値が無い（**私が数え直した**）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | ε をどうするか | ⭐ **ε をやめ、境界を「開いている」と綴る** —— `max: 1` ＋ `exclusive: true` |

⚠️ **体は「ε に値が無い」と報告した。** ⭐ **機械で数えたら 14 回現れて 1 度も値を持たないのは事実だが、使い方は `1 − ε`（4 行）と `1 + ε`（2 行）の 2 通りだけで、言いたいのは「1 を含まない」であって特定の数ではない。**
⭐ **だから値を発明する必要が無い。**

**退けた案**: ε に具体的な数を与える（⛔ 根拠が無く、分類 G —— この数に他の規則の論証が乗ると覆せなくなる）／ 「表せる最小の刻み」と定義する（⚠️ 機や版で答えが変わる。`WY-2` が同じ理由で退けた形である）。

⚠️ **他の行を名指す境界が約 30 本ある**（`` `fontMin` `` `` `zoomMax` `` `` `thinStrokeMax` `` ほか）。⭐ **こちらは値ではなく読み方の問題であり、`IV-16` はそのための名簿を要る。**

---

## 24 —— 表 T-220 に `parentId` の輪を止める行が無い

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 行を足すか | ⭐ **足す。** 表 T-220 に `IV-18`（`TaskGroup.parentId` の輪を禁じる）|

⛔ **3 つの網の目のどれにも掛からない:**

```
IV-4     輪を見るのは wbsParentUid だけ
HM-4     禁じるのは画面の上での移動だけ
FR-023   見るのは取り込み時の WBS の輪だけ
```

⚠️ **分類 F**（受理するか拒むかの境界）—— **緩く受理した文書は後から厳しくしても回収できない。初回リリース前のいまが最も安い。**

---

## 25 / 26 / 27 —— 推奨で進めてよい 3 件（分類 A〜C）

| # | 何が未決だったか | ⭐ 裁定 |
|---|---|---|
| **25** | 閉じたプロパティ面の幅 | ⭐ **`0`。** `FR-080` の基準環境が「面は閉じている」「その場所は日程へ渡る」と言うのと同じである。⚠️ `S-80` は**開いているときの幅**であり、`regionsFromScreen`（`PI-35`）はその保存値だけから矩形を切っていた |
| **26** | 保存と書き出しが提案するファイル名 | ⭐ **文書名 ＋ 表 T-024 の拡張子。** ⛔ `FR-096`/`FR-060`/表 T-227/表 T-024/表 T-024a/設定値の表のどれも提案名を名指していない |
| **27** | 表 T-006b は画面に刷る語にも掛かるか | ⭐ **掛からない。** 表 T-006b は 1.9 の**仕様書の表記規約**であり、縛るのは仕様書の散文である。⛔ 掛かるとすると `A-2`（「行」）が画面で守れず、例外を書くことになる |

---

## ⛔ 裁定は要らない。同じ巡で当てる 6 件

```
IC-54                正を 表 T-036 から 表 T-023b（構え）へ付け替え、「何の入口か」を構えの表示へ
                     ⭐ tables / rows / uids は動かない
MK-2 の（Cmd）        表 T-023 から落とす。Cmd は仕様書全体でそこ 1 か所、macOS も ⌘ も 0 件、
                     表 T-036 は Ctrl+A / Ctrl+O / Ctrl+S … と全部素の Ctrl
tbl-glossary.md:437  「milestoneGlyph の 8 つの綴りは未決である」—— CR-172（版 0.53）が決めている
tbl-glossary.md:436  「68 個の確定名」—— 表は 70 行（433 行目と 437 行目は 70）
tbl-settings.md:326  「表 T-206 の注」を引くが、その注は存在しない。行 ID を名指すこと
05-07-design.md:892  「> 未記入。」—— SWS-1〜SWS-6 が 6 本入った時点で偽
```

⭐ **Chapter 9.1 / 9.2 と `SWS-6` は本巡の対象外** —— 前者は `TW-2` が生成物と定めるので生成器の仕事、
後者は**製品側が無い**ので試験を書けない（表 T-231 の `NS-1`/`NS-2`/`NS-6` も同じ理由）。

---

## ⭐ 方式についての裁定（**2026-08-23、本巡の途中**）

⛔ **「これから裁定はすべてブラウザ画面で」を、同じ日に狭めた。**

```
簡単な裁定        既存の方法（会話）でよい
ブラウザの画面    ⭐ 大量の文言レビューのときだけ（表示語 174 枠のような）
```

⚠️ **理由は実測である** —— 9 件の裁定に 1 枚を起こしたのは、決定の重さに対して仕掛けが大きかった。
⭐ **174 枠のほうは、行に貼り付くコメントでなければ精密に review できないので、あちらは残す。**

---

## ⭐ アーキテクチャの巡が取った裁定（**2026-08-23、監査のあと**）

⛔ **監査（`../audit-cadence-and-architecture-2026-08-23.md` の 4.「測れなかったこと」）が唯一の裁定待ちとして挙げた件である。**
⭐ **規則 06 の 3b に従い、巡の先頭で問うた。** ⛔ **仕様への適用はまだである**（`PD-178`）。

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q15** | 表 T-014 の `ST-7`（安全弁に達したらエラーを出して処理を止める MUST）と `FR-028`（例外を投げてはならない MUST NOT）が本文で調停されていない。`StackSafetyCapReached` をどの形にするか | ⭐ **値で返す。** `layoutFromSchedule` は throw をやめ、判別子つきの値で「安全弁に達した」を返す |
| **Q16** | 通知が運ぶ理由（表 T-233）に、この安全弁の行を新設するか | ⭐ **新設する。** 作法は `NT-3a`、正は 表 T-014 の `ST-7`。⛔ **辞書の原稿にも項を足すこと**（表 T-233 の前文の MUST）|

**根拠（問う前に引いた結果）**:

- ⭐ **前プロジェクトの確定文書が、この安全弁そのものについて 2 か所で「到達したら人に知らせる（黙って捨てない）」と書いている** —— `previous-project-result/03-ui-naming/ui-detail-spec-ja.md` の 1105 行、`07-plan-actual/plan-actual-decisions-ja.md` の 925 行。⛔ **どちらも「例外を投げる」とは書いていない。**
- ⛔ **いまの throw は、その「知らせる」の正反対に着地している** —— 監査が経路を追ったとおり、`runFrame` が死に、`owed` が既に落ちているので画面が黙って止まり、人は何も告げられない。
- ⚠️ **`FR-033` の貼り付けの拒否は値でしか答えられない** —— `edit-task.ts` の ⛔ MISSING が「段数は文書だけの関数では数えられない」と自分で書いている。**throw を残すと、同じ安全弁が 2 つの機構を持つことになる。**
- ⭐ **実費が小さい** —— `layoutFromSchedule` の呼び手は `frame-loop.ts` の 3 か所だけである。

⚠️ **`ST-7` の「エラーを出して処理を止めること」は残る** —— 変わるのは**どう告げるか**であって、**黙って切り捨ててよくなったわけではない**（同行の MUST NOT）。
