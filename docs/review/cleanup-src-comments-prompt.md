---
type: Handover Prompt
title: 掃除フェーズ 2 の本命 — `src/` と `tests/` の注釈
description: 仕様書・道具・規則から履歴を消し終えた（CR-375）。残るのは製品コードと試験コードの注釈であり、フェーズ 2 でいちばん重い。中断していた `src/` の巡を、`tests/` まで広げて再開する。
tags: [cleanup, phase-2, src, tests, comments, handover]
phase: cleanup-p2-code
status: ready
---

# 掃除フェーズ 2 の本命 — `src/` と `tests/` の注釈

このファイルの `>` から下を、そのまま次のセッションの最初の指示として貼ってよい。

---

> # ⛔⛔ 止まれ — 判定基準はこの文書に無い
>
> **1 文字も消す前に、次の 3 つを全文読め。**
>
> | 読むもの | 何を持つか |
> |---|---|
> | [`cleanup-2026-09-11-prompt.md`](cleanup-2026-09-11-prompt.md) の **第 6 節**（判定 — 5 つの箱） | ⭐ 基準。**6-3 が `src/`、6-4 が `tests/`** |
> | 同じ文書の **第 5 節**（既に下りている裁定 1〜15） | ⛔ 問い直すな。**とくに 3・7・10・12・13・14・15** |
> | [`comment-removal-logic.md`](comment-removal-logic.md) | ⭐ **基準をどう当てるか**（文単位で裁く／覆いを報告させる／コードに決めさせる） |
>
> ⛔ **要約を置かない理由は 6-1 ① である** —— 原本が他所にあれば、ここに在るのは写しであり、写しは腐る。
> ⛔ **体に渡すときも同じ。場所を教えて原典を読ませよ。**
>
> ---
>
> ## 1. 現在地（2026-09-13 実測）
>
> | | 値 |
> |---|---:|
> | 最新コミット | `dbd5a54`（ブランチ `restart`、push 済み）|
> | **仕様書** | ✅ 手で刈れる節は終わった。⭐ **CR-375 で出どころ・日付・逐語・覆った履歴も消した**（原稿で 1,261 回）|
> | **道具・規則・基準線** | ✅ 同じ規則を当てた（CR-375 §3.3）。目録は [`09-tools.md`](../development-rules/09-tools.md) |
> | **`src/`** | **68 ファイル ／ 75,005 行、うち注釈 47,155 行（62.9%）** |
> | **`tests/`** | **227 ファイル ／ 192,045 行、うち注釈 59,904 行（31.2%）** |
> | 台帳の開いている行 | **51 行** —— `仕様待ち` 26、`実装待ち` 3、`試験待ち` 22 |
> | 検査 | **番号付き 51 本、すべて緑**。⭐ 終わりに赤の検査番号を刷り、`scratch/spec-check/last-run.txt` に残す |
> | 試験 | `vitest` **7,480 件中 47 件が赤**（22 ファイル）—— すべて `DFC-510`〜`DFC-531`（第 7 節）|
>
> ⭐ **打つのは束である。1 本ずつ思い出すな:**
>
> ```bash
> npm run guard:spec     # 手ごと。precheck → vitest → check.sh
> npm run guard:commit   # コミット前
> npm run guard:publish  # push 前。公開する commit まで読んで個人情報を点検する
> ```
>
> ⚠️ **赤の試験 47 件がある間は `guard:spec` と `guard:commit` が vitest で止まる**（連鎖は `&&`）。
> ⇒ `check.sh` を別に走らせ、赤が 47 件の集合から増えていないことを数えて比べよ。
>
> ---
>
> ## 2. ⭐ この巡の範囲と順（⛔ 入れ替えるな）
>
> | 順 | 何を | なぜこの順か |
> |---|---|---|
> | **1** | **`src/` の注釈** —— まだ手が入っていない 59 本と、既に処理した 9 本の再訪（第 4 節）| 6-3。重さがここに集まっている |
> | **2** | **裁定 3 を済ませる**（検査 39 がコメントを保持に数えないようにする） | ⛔ **これより前に `tests/` のコメントを消すと、消した引用の分だけ保持が落ちて検査 39 が赤くなる**（6-4 の末尾） |
> | **3** | **赤の試験 47 件**（第 7 節）| 試験の窓を今の原稿で切り直す。コメントではなく試験の主張の直し |
> | **4** | **`tests/` の注釈** | 6-4。規則が `src/` と違う —— **指し先は消すな** |
>
> ⭐ **CR-375 の規則はコードの注釈にも同じく当たる。** 誰がいつ裁定したか・利用者の逐語・覆った経緯・日付は消し、
> **なぜそう書いたか（箱 D）だけを現在形で残す**。これは裁定 7（日付と旧値を落とし、理由を現在形で書き直せ）と同じことを言っている。
>
> ---
>
> ## 3. ⭐ 重さの分布（2026-09-13 実測）
>
> **`src/` —— 上位 8 本が注釈の 60% を持つ**（注釈 ／ 全行）:
>
> ```
> 7825 / 11097  src/framework/single-html-shell/frame-loop.ts
> 6555 /  9670  src/adapter/input-command-translator/input-command-translator.ts
> 6195 /  8693  src/framework/dom-screen-surface/dom-screen-surface.ts
> 2377 /  2836  src/adapter/screen-renderer/screen-renderer.ts
> 1751 /  2903  src/adapter/svg-renderer/svg-renderer.ts
> 1616 /  2594  src/entity/layout-engine/schedule-layout/schedule-layout.ts
> 1050 /  2756  src/adapter/document-codec/mspdi-codec.ts
> 1015 /  2030  src/entity/layout-engine/schedule-geometry/schedule-geometry.ts
> ```
>
> ⚠️ 注釈が 100 行に満たないファイルが **13 本**。⛔ **68 本を均等に配るな。**
>
> **`tests/` —— 分布が平たい。上位 8 本で 15% しかない**（注釈 ／ 全行）:
>
> ```
> 1736 / 5599  tests/unit/uf-72-screen-part.test.ts
> 1359 / 4036  tests/system/measured-sweep.test.ts
> 1094 / 3170  tests/unit/uf-47-48-choosers.test.ts
> 1032 / 3730  tests/unit/uf-71.test.ts
>  987 / 2187  tests/contract/display-words.contract.test.ts
> ```
>
> ⚠️ 注釈が 100 行に満たないファイルが **32 本**。⇒ **`tests/` は大物を 1 体ずつではなく、似た試験を束ねて配る。**
>
> **機械で見える履歴の形**（注釈の行だけを数えた）:
>
> | 形 | `src/` | `tests/` |
> |---|---:|---:|
> | 日付 `YYYY-MM-DD` | 547 | 1,205 |
> | 台帳の番号（`DFC-` `PND-` `JDG-` `CR-`）| 653 | 1,100 |
> | 出どころ（「利用者の裁定」ほか）| 180 | 329 |
> | 逐語 ／ verbatim | 21 | 135 |
>
> ⛔ **これは的の一部でしかない。** 2026-09-12 に `src/` の注釈を歩いて分類すると、**88.8% は引用も番号も日付も無い地の散文**であった。
> ⇒ **grep で刈れる仕事ではない。**1 文ずつ 6-3 の「WHAT ／ 約束 ／ WHY」に当てる判断仕事である。
> ⚠️ **台帳の番号は一律に消すな** —— 裁定 13。閉じた行への帰属は消し、**開いている行への指し先と `@provisional PND-nnn` は残す**。
>
> ---
>
> ## 4. ⭐ 既に処理したファイル（中断した巡）と再訪
>
> | 段 | ファイル | コミット |
> |---|---|---|
> | 0. 試し撃ち | `svg-renderer.ts` | `41e6eb2` |
> | 1. 大物 5 本 | `screen-renderer.ts` ／ `properties-panel.ts` ／ `row-title-panel.ts` ／ `schedule-layout.ts` ／ `schedule-geometry.ts` | `5970519`、`ad369a9` |
> | 2. 巨大 3 本の **第 1 節だけ** | `frame-loop.ts` ／ `input-command-translator.ts` ／ `dom-screen-surface.ts` | `af13081` |
>
> ⛔ **この 9 本も「終わった」と読むな。** 理由は 2 つ:
>
> 1. **覆いが 23% しかなかった**（`comment-removal-logic.md` 第 0 節）—— 処理済みの段落の 77% は読まれずに残った
> 2. **その後に基準が増えた** —— 裁定 12〜15 と CR-375。`comment-removal-logic.md` 第 7 節の「裁定の遡及」を当て、再訪の要否をファイルごとに記録せよ
>
> ⚠️ `af13081` が他に触った 10 本（`app-shell-source.ts` など）は、誤った掃きを戻した**小さな直し**であり、処理ではない。
>
> ---
>
> ## 5. ⛔ 並行か直列か —— **ファイルをまたいで並行、ファイルの中は直列**
>
> ⛔⛔ **裁定 D が禁じているのは「1 つのファイルを 2 体が同時に編集すること」だけである。**
> 行範囲が交わらなくても片方の編集が消え、⚠️ 消えても赤にならない。⭐ 別のファイルなら並行してよい。
>
> ### 進め方（`src/`）
>
> | 段 | 体の数 | 何を |
> |---|---|---|
> | **0. 試し撃ち** | ⭐ **1 体だけ** | `comment-removal-logic.md` の手順（コードに決めさせる・文単位・覆いの報告）が本当に効くかを、**再訪の 1 本**（`svg-renderer.ts`）で測る。ブリーフを直す |
> | **1. 大物** | 1 体 × 3 本を並行 | `mspdi-codec.ts` と、再訪する大物 |
> | **2. 巨大 3 本** | ⛔ **ファイル内は直列** | 節で切って 1 体 1 節。⛔⛔ **行番号で節を切るな** —— 最初の編集で動く。目印で切れ |
> | **3. 小物** | 1 体が数本まとめて | 注釈 100 行未満の 13 本 |
>
> ⛔ **段 0 を飛ばすな。** 中断した巡の 7 巡すべてで、体がブリーフの誤りを実測で潰している。
>
> ### 体に必ず書くこと
>
> - ⛔ **scratchpad のファイル名を体ごとに分けよ** —— 親が同じ名前で上書きすると、体は「走らせたスクリプトがすり替わった」と読む
> - ⛔ **「commit と push は前に立つ者が行う」と書け** —— 書かないと、体は足元で増える commit を「許可のない公開」と読む
> - ⛔ **迷ったら消さずに報告せよ**（6-5）—— 体は判断を求められると消す方へ倒れる
> - ⭐ **見た段落の総数と、残した段落の総数を報告させよ**（覆い）—— 消した行数は合否ではない
> - ⛔ **`docs/spec/output/` を見るな**（裁定 11）
> - ⭐⭐ **ブリーフを反証させよ** —— 「誤りが見つからなければ『測っていない』と書け」
> - ⛔ **刈りすぎは、切った体に判定させない**（`comment-removal-logic.md` 第 6 節）—— 別の体に削除行だけを読ませ、箱 D の巻き添えを数えさせよ
>
> ---
>
> ## 6. ⛔⛔ `src/` と `tests/` を読む門（2026-09-13 実測、すべて緑）
>
> | 門 | いま（基準線） | 何をすると赤になるか |
> |---|---|---|
> | **42** `check-quoted-source.py` | **611** | ⛔ 引用を短縮・接ぎ・言い換えると上がる。⭐ **指し先に畳むと下がる** |
> | **44** `check-spec-id-references.py` | **153**（廃番 136 ／ 未定義 17）| ⭐ 廃番を名指す注釈を消すと下がる。⛔ 畳むときに未定義の ID を書くと上がる |
> | **45** `check-repeated-expressions.py` | **91** | 注釈だけを触る限り動かない |
> | **46** `check-line-breaks.py` | **2** | ⛔ 下の注意を読め |
> | **39** `check-must-clause-coverage.py` | **1348** | ⛔ **`tests/` しか読まない。** 裁定 3 の前に `tests/` の引用を消すと上がる |
> | 25 `check-pending-decisions.py` | 213 行、開 131 | 暫定の印（`@provisional`）と台帳の行が合わなくなると赤 |
> | 26b `check-published-members.py` | new 0 | 注釈だけを触る限り動かない |
> | 30 `check-generated-constants.py` | 完全一致 | 生成柵の中を触ると赤 |
> | 50 ／ 52 | 完全一致 | `PressRow` の値と、試験が値として持つ `DEV-` `CHN-` を触ると赤 |
>
> ⭐ **どの門も、基準線を下回るのは緑であり、上回るのだけが赤である。**
> ⛔ **下がったら、同じコミットで基準線を締めよ**（基準線ファイルの頭に、何を数えどう動かしてよいかが書いてある）。
>
> ### ⚠️ 検査 42 の約 200 件は、この巡の味方である
>
> 基準線 611 のうち約 200 件は、**CR-375 が仕様書から消した出どころ・日付つき引用・覆った文を、コメントがまだ引いている**ものである。
> ⇒ 箱 A として消すか指し先に畳むたびに下がる。`python .claude/skills/spec-graph-check/check-quoted-source.py --list` が一覧を出す。
>
> ### ⛔ 検査 42 の 120 字の窓は両向きに働く
>
> 同じ注釈塊の 120 字以内に `DFC-` ／ `CR-` ／ `規則 N` ／ `利用者の` があると、「…」が免除される。
> ⇒ **台帳番号を消しただけで、隠れていた捏造引用が露出して上がる**ことがあり、逆に**注釈を短くすると働いていないのに下がる**ことがある。
> ⇒ ⛔ **並行するなら、体に自分のファイルだけを始める前と後に測らせよ。** 木全体の数は誰のものでもない。
>
> ### ⛔ 検査 46 は `src/` からは動かせない
>
> `check-line-breaks.py` は免除を計算して刷るだけで、数から引いていない。
> ⇒ **検査 46 を避ける労力を払うな。** コードと docstring のどちらが意図かは裁定案件であり、掃除の中で直すな。
>
> ---
>
> ## 7. ⛔ 赤の試験 47 件（`DFC-510`〜`DFC-531`、22 ファイル）
>
> **何か**: 試験が仕様書の文を逐語で持っており、その窓が CR-375 で消した出どころ・日付・逐語・覆った文まで含んでいた。
> ⭐ **規則の文（MUST ／ MUST NOT ／ 値）は変わっていない**（HEAD と同じ場所で突き合わせてある）。⇒ **仕様書は正であり、直すのは試験である。**
>
> **直し方**:
>
> 1. 各行の `対応方針・決定仕様` の欄が、その試験が読む要求や表の行を名指している
> 2. 試験の窓を、**今の原稿の同じ規則の文**で切り直す（出どころ・日付・逐語を窓に入れない）
> 3. 出どころや覆った文そのものを在ると確かめているだけの件（`fr-021` ／ `t-024a` ／ `fr-053` ／ `fr-036` ほか）は、**その件を試験から消す**
> 4. `t-233-reason-words-tell-the-row.contract.test.ts` は指紋の掛け金（`DFC-166`）である —— ja/en の表示語が場面と合うことを読み直してから指紋を刷り直す
>
> ⛔ **`tools/follow_the_manuscript.py` ではこの 47 件は直らない。** あの道具は、試験の文字列が原稿の 1 か所に**まだ見つかる**ときだけ書き戻す。
> この 47 件の窓は消えた語を含むので、原稿のどこにも当たらない。⇒ **手で切り直す。**
>
> ⭐ 窓が戻ると**検査 39 が 1348 から下がる**（1 条項ぶん）。⇒ 同じコミットで基準線を締め、台帳の行を台帳の頭の状態の定義に従って進めよ。
>
> ---
>
> ## 8. ⛔ 前の巡が踏んだ罠（全部実測）
>
> - ⛔⛔ **原稿を実行時に読む試験が在る。** ⇒ `check.sh` だけでは足りない。`vitest` も走らせよ
> - ⛔ **書き戻しの改行を「CRLF が 1 つでも在れば CRLF」で決めるな。** 行末は多数決で決め、編集の後に HEAD と CRLF の数を比べよ ——
>   ある巡で LF のファイルを丸ごと CRLF で書き戻し、文言の同じ試験が 35 件赤くなった
> - ⛔ **`check.sh` の赤は「FAIL」と刷らないものもある**（検査 11 の `[NEW]`、検査 12 の `12  path:line`、検査 33 の `MISMATCH`）。
>   ⭐ いまは終わりに赤の検査番号を刷り、`scratch/spec-check/last-run.txt` に残す —— **それを読め**
> - ⛔ **`git checkout HEAD -- <file>` は自分の未コミットの直しも消す**
> - ⛔ **`bash` の heredoc が escape を壊す。** 日本語を Python で触るなら、scratchpad にファイルを書いてパスで走らせろ
> - ⛔ **検査 11 は非単調である** —— 触っていない 2 行が、別の場所を消した副作用で赤くなる
> - ⛔⛔ **「再現しないから無い」と読むな。** `DFC-506`（揺れる試験）は自然な全件実行 36 回で 1 度も出なかった。
>   ⚠️ 全件 `vitest` を 2 本同時に走らせると時計に依る試験を増幅する ⇒ 体が説明できない赤を見たら、戻す前にもう一度走らせよ
>
> ---
>
> ## 9. この巡でやらないこと
>
> - ⛔ **`docs/spec`** —— 手で刈れる節はもう無い。仕様の問題を見つけたら台帳へ
> - ⛔ **`CR-374`（日付を変える 4 つの掴み）と `DFC-507` ／ `DFC-508` の実装** —— 掃除が終わってから
> - ⛔ **試験名・アサーションが廃番を名指すもの** —— 裁定 10。直さずに台帳へ
> - ⛔ **部品図の検査**（`build.py` の `.drawio` ／ `.svg`）—— Graphviz と draw.io が要る。目録の第 9 節が持つ
> - ⛔ **フェーズ 3 と `After-Cleanup-P2` のタグ** —— 掃除が終わってから。**タグは利用者が打つ**
