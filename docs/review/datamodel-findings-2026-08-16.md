# データモデルの取り込みで出た、仕様書側の宿題（2026-08-16）

**これは指摘の置き場であって、仕様書ではない。** データモデルを `_assets/tbl-datamodel.md` へ抽出する作業のあいだに見つかった、
**Chapter 1 〜 4 を直さないと辻褄が合わない件**をここに積む。**その場で直さない** —— 抽出の途中で要求を触ると、
1 か所直して他所を壊す型（自作率）を踏むためである。**抽出が終わってから、塊に割って一度に直す。**

⚠️ **2026-08-13 追記 —— 「どう直すか」欄は保留である。**
この欄が書かれた当時の `_assets/tbl-datamodel.md` と行 ID（`DM-xx`）は、**Chapter 5 / 6 の設計ごと破棄された。**
**「何が食い違っているか」欄は破棄と無関係に生きている**（要求どうしの食い違いであり、設計の産物ではない）。
**処方は、新しいデータモデルが決まってから引き直すこと。** 破棄されたモデルの列名・行 ID をそのまま復活させてはならない（MUST NOT）。

**状態**: `未着手` / `対応済` / `見送り` / `保留（処方の再導出待ち）`

⚠️ **2026-08-14 追記 —— 新しいデータモデルが決まったので、保留を解除した。**
受け皿の名前は 表 T-052 の `DR-4`（ルート直下に置く 3 つ）と 表 T-058（その列）が持つ。
**残るのは `D-2` と `D-3` の 2 件である。**

| # | 状態 | 場所 | 何が食い違っているか（**生きている**） | どう直すか（**⚠️ 再導出待ち**） |
| --: | --- | --- | --- | --- |
| D-1 | 対応済 | `FR-063` | 「最後に書いた者と時刻を文書に持たせ」と求めているが、**仕様書はその受け皿の名前を持たない**（`revisionStamp` / `changeLog` の出現が 0 件）。**MSPDI にも対応要素が無い**（`mspdi_pj12.xsd` を検索して確認。`Author` は作成者であって最後に書いた者ではない） | ⚠️ **受け皿は前プロジェクトで確定済みである**（2026-08-13 に原典で確認）—— `revisionStamp` の 3 キー `revision` / `lastEditedBy` / `updatedAt`。出典 `DECIDED-ja.md:312`（決定-3「`revisionStamp` は保存する」）・`NEXT-STEPS-ja.md:336`・機械可読な実例 `10-agent-interface/samples/grs-document-with-revision-stamp.json:4-8`。⚠️ **`revisionStamp` は MSPDI へ書き出さない**（`NEXT-STEPS-ja.md:340`）。データモデルにこの 3 列を立て、`FR-063` からそこを指す 1 文を足す |
| D-2 | 対応済 | `FR-024` | 「設定値は既定値と一致していても省略せず、常に全項目を書き出すこと（MUST）」は**設定値についての規則**であり、**エンティティの列を `null` のまま鍵ごと書き出すこと**には持ち主がいない。往復で情報を失わないための要なのに、規則がどこにも無い | `FR-024` に 1 文足すか、行を 1 つ足して受ける。**データモデル側に書いてはならない** —— 規則の正は要求である。**この処方は破棄と無関係に有効** |
| D-3 | 対応済 | 表 T-005 / `tbl-glossary.md` の表 T-101 | `Dependency` / `Project` / `Calendar` / `WeekDay` / `Exception` / `Resource` / `Assignment` / `TaskVisual` / `TaskOrigin` / `Comment` / `HighlightBox` は**仕様書に 1 度も出てこない**（バッククォート付きの出現が 0 件）。画面に出る語は用語辞書が受ける必要がある | 用語辞書の表 T-101 に、**画面や散文で指す必要があるものだけ**を足す。**全数を写さない**。⚠️ **どの名前が残るかは新しいデータモデルが決めるので、それを待つ** |
| D-4 | 対応済 | 表 T-005 の `G-1` ／ `tbl-glossary.md` の `N-1` | どちらも **`Task.shapeKind`** と書いているが、**継承したデータモデルでは列が `TaskVisual` に載っている**。表記の規約 `W-7` は散文で属性を指すとき `Entity.field` の形にせよと定めているので、いまの綴りは実在しない場所を指している | **どのエンティティが `shapeKind` を持つかを新しいデータモデルで決め、その綴りに直す。** ⚠️ **破棄された設計は `TaskVisual` 側に置いた**（理由: `Task` を MSPDI の `Own` だけを持つ器に保つと書き出しに除外一覧が要らなくなる）。**理由は評価に値するが、決定としては引き継がない** |
| D-5 | 対応済 | `FR-063` | 「時刻」の受け皿が無い（`D-1` と同じ要求の別の穴） | `D-1` と同じ塊で直す。受け皿は **`revisionStamp.updatedAt`**。⚠️ **実例は `"2026-08-02T13:24:51.117Z"` とミリ秒まで書いており、透かしの決定（UTC・`ISO 8601`・**秒**精度）と精度が食い違う。どちらに揃えるかを決めること** |
| D-6 | 対応済 | `UC-013` | `UC-013` は「変更の理由を文書に残す」ことを求め、`FR-063` の理由欄も「どの版に対する理由か」に触れているが、**仕様書はその受け皿の名前を持たない** | 受け皿は前プロジェクトで確定済み —— **`changeLog[]`**（`revision` / `editedBy` / `explanation` / `changedAt`）。出典 `DECIDED-ja.md:297`・`NEXT-STEPS-ja.md:336`・実例 同 JSON `:11-18`。⚠️ **会話そのものは保存せず、変更の理由だけを保存する**（`DECIDED-ja.md:280`）。`D-1` と同じ塊で直す |
| D-7 | 未着手 | `FR-033` ／ タスクを作る要求 | **新しい `Task` の `UID` の採番に持ち主がいるのは複製（`FR-033`）だけである。**ドラッグでタスクを作る経路には採番の規則が無い（`採番` の出現は `docs/spec` 全体で 1 か所。自分で数えた） | 採番の規則を、複製と作成の両方が指せる場所へ置く。値の受け皿は `Project.uidHighWaterMark`（表 T-058）で既にある |
| D-8 | 未着手 | `UN-15` ／ `FR-008` | **取り消しの表 `UN-15` は「担当者（資源）と割当の追加・改名・削除（`FR-008`）」と書くが、`FR-008` が定めているのは「追加・編集・解除」であり、担当者そのものを削除する規則は仕様書のどこにも無い**（`参照されていない担当者` の出現 0 件。自分で数えた） | `FR-008` に削除を足すか、`UN-15` の語を `FR-008` に揃えるかを決める。⚠️ **表 T-050（削除の連鎖）にも担当者の行は無い** —— 足すなら 3 か所を 1 回で書く |

## 数

| | |
| --- | --: |
| 積んだ件数 | 8 |
| 対応済 | 6 |
| 未着手 | 2 |

**対応済の 4 件がどこで解けたか** —— `D-1` / `D-5` / `D-6` は受け皿の名前が
表 T-052 の `DR-4`（`schemaVersion` / `revisionStamp` / `changeLog`）と 表 T-058（その列）に入った。
⚠️ **`D-5` の精度は秒に揃えた**（透かしの決定に合わせ、前プロジェクトの実例 JSON のミリ秒を採らない。利用者判断、2026-08-14）。
`D-4` は `CR-104` が用語辞書 `N-1` と 表 T-005 の `G-1` を直し、置き場が `TaskVisual.shapeKind` に決まった（表 T-058）。

**2026-08-14 に閉じた 2 件** —— `D-2` は `FR-024` に「日程データの群では `null` の列も鍵ごと書き出す」を足した。
`D-3` は用語辞書の表 T-101 に `N-8` 〜 `N-14` を足した。⚠️ **全数は写していない** ——
`WeekDay` / `Exception` / `CarryElement` は Chapter 1 〜 4 が日本語でも英語でも呼んでいないので入れず、
表 T-056 が持つ。**足したのは、`担当者`（30 回）`割当`（40 回）`暦`（21 回）のように
Chapter 1 〜 4 が繰り返し使いながら英語名を持たなかった語である**（自分で数えた）。

**残る 2 件は `D-7` と `D-8`** —— どちらも本作業のあいだに新しく見つけた。
⚠️ **`D-8` は `Y3` の `F-16` が「`FR-008` が担当者の削除を持つ」と書いていたのを確かめて出てきたものである。その引用文は仕様書に存在しなかった。**

⚠️ **この表はまだ増える。** 2026-08-13 に、引継書の資産地図に無かった約 6,000 行
（`10-agent-interface/` ほか）の走査を掛けており、そこから出る指摘は未反映である。
