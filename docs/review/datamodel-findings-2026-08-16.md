# データモデルの取り込みで出た、仕様書側の宿題（2026-08-16）

**これは指摘の置き場であって、仕様書ではない。** データモデルを `_assets/tbl-datamodel.md` へ抽出する作業のあいだに見つかった、
**Chapter 1 〜 4 を直さないと辻褄が合わない件**をここに積む。**その場で直さない** —— 抽出の途中で要求を触ると、
1 か所直して他所を壊す型（自作率）を踏むためである。**抽出が終わってから、塊に割って一度に直す。**

**状態**: `未着手` / `対応済` / `見送り`

| # | 状態 | 場所 | 何が食い違っているか | どう直すか |
| --: | --- | --- | --- | --- |
| D-1 | 未着手 | `FR-063` | 「最後に書いた者と時刻を文書に持たせ」と求めているが、**識別子を持たない**。継承したデータモデルにも受け皿が無く、**MSPDI にも対応要素が無い**（`mspdi_pj12.xsd` を検索して確認） | `Project.lastWriter`（`DM-49`）を新設した。`FR-063` から `tbl-datamodel.md` の `DM-46` / `DM-48` / `DM-49` を指す 1 文を足す |
| D-2 | 未着手 | `FR-024` | 「設定値は既定値と一致していても省略せず、常に全項目を書き出すこと（MUST）」は**設定値についての規則**であり、**エンティティの列を `null` のまま鍵ごと書き出すこと**には持ち主がいない。往復で情報を失わないための要なのに、規則がどこにも無い | `FR-024` に 1 文足すか、行を 1 つ足して受ける。**`tbl-datamodel.md` 側に書いてはならない** —— 規則の正は要求である |
| D-3 | 未着手 | 表 T-005 / `tbl-glossary.md` の表 T-101 | `Dependency` / `Project` / `Calendar` / `WeekDay` / `Exception` / `Resource` / `Assignment` / `TaskVisual` / `TaskOrigin` / `Comment` / `HighlightBox` は**仕様書に 1 度も出てこない**（バッククォート付きの出現が 0 件）。データモデルが名前の正になった以上、画面に出る語は用語辞書が受ける必要がある | 用語辞書の表 T-101 に、**画面や散文で指す必要があるものだけ**を足す。**全数を写さない** —— 列名の正は `tbl-datamodel.md` である |

| D-4 | 未着手 | 表 T-005 の `G-1` ／ `tbl-glossary.md` の `N-1` | どちらも **`Task.shapeKind`** と書いているが、**列は `TaskVisual` に載っている**（`DM-96`）。表記の規約 `W-7` は散文で属性を指すとき `Entity.field` の形にせよと定めているので、いまの綴りは実在しない場所を指している | `TaskVisual.shapeKind` に直す。⚠️ **列を `Task` へ移す案は採らない** —— `Task` を「MSPDI の `Own` だけを持つ器」に保つと書き出しに除外一覧が要らなくなるためで、その利点のほうが大きい |
| D-5 | 未着手 | `FR-063` | 「時刻」の受け皿として `Project.lastWrittenAt`（`DM-119`）を新設した。要求側からそれを指す記述が無い | `FR-063` から `DM-46` / `DM-49` / `DM-119` を指す 1 文を足す（`D-1` と同じ塊で直す） |

## 数

| | |
| --- | --: |
| 積んだ件数 | 5 |
| 対応済 | 0 |
