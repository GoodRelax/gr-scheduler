# U2 — ui-detail-spec-ja.md の名前の全数抽出

- 担当ファイル: `previous-project-result/03-ui-naming/ui-detail-spec-ja.md`
- 行数: **1,556 行**（`wc -l` で確認）。**全文を 300 行ずつ 6 回に分けて読んだ**
- 突合先: `docs/spec/_assets/tbl-glossary.md`（**名前の正**。185 行）
- 参考: `previous-project-result/03-ui-naming/ui-parts-ja.md`（741 行・全文を読んだ）、
  `docs/spec/01-04-requirements.md` 1.9（表 T-006a / T-006b・214〜323 行）
- 抽出日: 2026-08-14

## 0. 数（自分で数えた）

| 事項 | 数 |
|---|--:|
| 抽出した名前（下表 A〜H の行数の合計） | **191** |
| うち用語辞書 `tbl-glossary.md` に語として在るもの | **103** |
| 辞書に無いもの | **88** |
| 判定「一致」 | **99** |
| 判定「辞書に無い（追加が要る）」 | **57** |
| 判定「要改名」 | **23** |
| 判定「衝突」 | **10** |
| 判定「廃止語」 | **2** |

> 行数の内訳: A=37 / B=23 / C=30 / D=15 / E=13 / F=9 / G=43 / H=21。**合計 191**。
> 判定の内訳 99 + 57 + 23 + 10 + 2 = **191**（一致する）。
> **数はいずれも表の行を機械で数え直して確かめた**（目視の集計ではない）。
> 「辞書に在る」は `tbl-glossary.md` の中に**その綴りが実在すること**で数えた。**概念が別名で在る場合は数えていない**
> （例: `Today Line` は辞書に無い。在るのは `Status Line` である）。
> 表記規約側（`01-04-requirements.md` 表 T-006b）にしか無い語（「ポインタ」など）も**辞書に在るとは数えていない**。

---

## 1. 最優先 —— ui-parts-ja.md との食い違い（同じ資産の中の不一致）

**11 件ある。うち 6 件は「ui-parts が改名を決めたのに ui-detail に取り込まれなかった取りこぼし」である。**
これは推測ではなく、ui-parts-ja.md §2-1-6 の改名表に**改名の事実が書かれている**のに、
ui-detail-spec-ja.md に旧綴りが**残っている**という照合の結果である。

| # | 名前 | ui-parts の決定 | ui-detail の現状 | 深刻度 |
|:--:|---|---|---|---|
| **1** | `Today Line` / 本日線 | §2-1-3 は UI パーツとして掲載。§2-1-6 は「**`todayLineVisible` は廃止した（確定 2026-07-31）**」、`todayDay` → `statusDate` の理由を「**中身は基準日であって本日ではなかった**」と記す | §3 に **`[本日線]` 独立トグル**（122 行）、§5-2 に **`Cursors`（`Today Line` / `Dual Cursor` / `Guide Cursor`）**（955 行）。一方 §6-6 は「**基準日**の縦線」と書く（1387 行）—— **同じ線を同じ文書が 2 通りに呼んでいる** | **最大**。辞書 U-11 は `Status Line`／基準日線とし、**「本日線」と呼んではならない（MUST NOT）**と定める |
| **2** | `grid-date` / `grid-group` | §2-1-6（2026-08-02）で `gridDateLinesVisible` / `gridGroupLinesVisible` → **`dateGridLinesVisible` / `groupGridLinesVisible`**。理由は「UI パーツ名 `Date Grid Lines` / `Group Grid Lines` に対し**語順が反転**していた」 | §1（64 行）と §3（111 行）に **`grid-date` / `grid-group`** が残る。**同じ語順反転を潰し損ねている** | 大 |
| **3** | `comment`（パレットのコマンド名） | §2-1-6 で `add-box` → **`highlight-box`**。理由は「パレットのコマンド名が UI パーツ名 `Highlight Boxes` と繋がっていなかった」 | §3（111 行）に **`comment`** が残る。**兄弟の `highlight-box` だけ直り、`comment` は `Comment Boxes` と繋がらないまま** | 大。辞書 U-14 は「**「コメント」と略さない（MUST NOT）**」 |
| **4** | `markText` | §2-1-6（2026-07-31）で `markGap` / `markMin` / `markStroke` / `markOfText` / `markTextOfFont` … → **`marker*`** に統一 | §4-10-1 の mermaid（745 行）に **`markText["マーカーの数字"]`** が残る | 中 |
| **5** | `labelW` / `occW` / `rowH` | §1-2（2026-08-02）「**裸の `H` / `W` も同じ。`Height` / `Width` と書く**」 | §4-10-1 の mermaid（746〜747, 759 行）に **`labelW` / `occW` / `rowH`** が残る | 中 |
| **6** | 「行名ペイン」 | §2-1-6（2026-08-02）で `leftPaneWidth` → **`rowTitlePanelWidth`**。理由に「**`Pane` / `Panel` の綴りも不一致だった**」と明記 | §6-7（1411〜1412 行）に「**行名ペインは不透明な固定ペイン**」「**行名ペイン**と日程本体の間の余白」が残る | 中 |
| **7** | `milestone`（真偽値） | §2-1-1 / §4-2(a) は **`Task.milestone`(bool)** を確定としている | §6-1（1084, 1111, 1119 行）が「`milestone` 優先」の**廃止規則**として言及 | **辞書と衝突**。辞書 N-1 は「⚠️ **真偽値の `milestone` という列は持たない**」——`shapeKind` が `'milestone'` かで判定する。**ui-parts 側が古い** |
| **8** | 「Resume アイコン」 | §2-1-3 は **`Resume Icon` / 再開アイコン**。ただし §2-1-6 の日本語欄は「**Resume の腕の長さ**」と英日混在 | §4-1（208 行）・§4-5 見出し（370 行）・§4-5 本文（373 行）・§4-6（397 行）が**「Resume アイコン」**。一方 §1 冒頭（26 行）だけ `Resume Icon` | 中。**両資産が同じ混在を持つ** |
| **9** | `[予定]` `[実績]` の 2 トグル ↔ `planActualDisplay` | §5-4（713 行）「表示状態は `documentSettings.planActualDisplay`（`both` / `plan-only` / `actual-only` の **3 値**）**1 つ**で表す」 | §2 / §3 / §4-4 は「**独立トグル 2 つ**」。両方 OFF は作らせないので有効な組は 3 通りで**値としては一致するが、UI の操作子 2 個とデータの列 1 個の対応を書いた場所が無い** | 中。**名前の衝突ではなく面の対応の欠落** |
| **10** | `Row Title Tree` を操作の入口として使う | §3 の木は `Row Title Panel` > `Row Title Tree` と親子で持つ | §5-4 見出し「**Row Title Tree**」（1004 行）、§6-1-1 見出し「**Row Title Tree の操作が何を動かすか**」（1131 行） | **辞書と衝突**。辞書 U-23 は「⚠️ **操作の入口を指すときは「行見出しパネル」と書くこと（MUST）**」（確定 2026-08-13）。**この決定は資産より後**なので、資産側が追随していないのは当然だが、**次の工程で直す対象**である |
| **11** | `Grid Lines`（総称） | §3 の木（550 行）に中間節点として実在 | §1（49 行）「**`Grid Lines` の表示切替**」 | 小。**辞書に総称の行が無い**（U-17 / U-18 は子だけ）。追加が要る |

---

## 2. 抽出表

判定の語: **一致 / 辞書に無い（追加が要る）/ 綴りが違う / 衝突 / 要改名**

### 表 A — UI パーツ（画面の部品）37 行

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `Rows` | 行 | §1, §4-5 | ○ | U-1 | 一致 | |
| `Task Bars` | タスクバー | §1, §4-5 | ○ | U-2 | 一致 | |
| `Plan Bar` | 予定バー | §4-1, §4-4 | ○ | U-3 | 一致 | 本書は**日本語のみ**で書く。英名は §1 冒頭の参照だけ |
| `Actual Bar` | 実績バー | §4-1, §4-4 | ○ | U-4 | 一致 | 同上 |
| `Progress Marker` | 進捗マーカー | §4-5, §4-6, §6-7 | ○ | U-5 | 一致 | |
| `Resume Icon` | 再開アイコン | §1（26 行）のみ | ○ | U-6 | 一致 | 本文 4 か所は下行の混在形 |
| （`Resume アイコン`） | — | §4-1, §4-5 見出し, §4-6 | ✗ | U-6 | **要改名** | **英日混在**。`Resume Icon`（散文）／「再開アイコン」（日本語）に直す |
| `Name Label` | 名称ラベル | §4-1, §4-2, §4-3 | ○ | U-7 | 一致 | 日本語のみ |
| `Assignee Label` | 担当ラベル | §4-1, §4-2, §6-7 | ○ | U-8 | 一致 | 日本語のみ |
| `Progress Line` | イナズマ線 | §3, §6-6 | ○ | U-9 | 一致 | 直訳しない例外 X-1 |
| `Cursors` | カーソル | §5-2（955 行） | ○ | U-10 | 一致 | 総称。中身の 1 つが下行 |
| `Today Line` | 本日線 | §3（122 行）, §5-2（955 行） | ✗ | U-11 | **要改名** | 正は **`Status Line` / 基準日線**。辞書 U-11 が MUST NOT。§6-6 は同じ線を「基準日の縦線」と書いており**文書内で不一致** |
| `Dual Cursor` | デュアルカーソル | §5-2 | ○ | U-12 | 一致 | |
| `Guide Cursor` | ガイドカーソル | §3, §5-2 | ○ | U-13 | 一致 | 4 モード排他 |
| `Comment Boxes` | コメントボックス | §3（`comment`）, §4-1（「コメント本体」） | ○ | U-14 | **要改名** | **本書に完全形が 1 度も出てこない**。辞書 U-14 が「コメント」と略すことを MUST NOT にしている |
| `Highlight Boxes` | ハイライトボックス | §3 | ○ | U-15 | 一致 | |
| `Annotations` | 注記 | §2（91 行）, §4-1（221 行） | ○ | U-15a | 一致 | 日本語のみ |
| （`注釈`） | — | §5-1（895 行） | ✗ | U-15a | **要改名** | **同じ文書が §4-1 では「注記」と書いている**。1 か所だけ「注釈」 |
| `Dependency Lines` | 依存線 | §1, §4-3, §4-9 | ○ | U-16 | 一致 | |
| `Grid Lines` | グリッド線（総称） | §1（49 行） | ✗ | — | **辞書に無い（追加が要る）** | ui-parts §3 の木には中間節点として実在。辞書は子（U-17/U-18）だけ持つ |
| `Date Grid Lines` | 日付罫線 | §1 | ○ | U-17 | 一致 | |
| `Group Grid Lines` | グループ罫線 | §1 | ○ | U-18 | 一致 | |
| `Time Ruler` | タイムルーラー | §1, §6-3 | ○ | U-19 | 一致 | |
| `Watermark` | 透かし | §1, §3（`watermark`） | ○ | U-20 | 一致 | |
| `Scrollbars` | スクロールバー | §5-2, §5-5 | ○ | U-21 | 一致 | ヒントの出しどころ |
| `Row Title Panel` | 行見出しパネル | §1 | ○ | U-22 | 一致 | |
| （`行名ペイン`） | — | §6-7（1411〜1412 行） | ✗ | U-22 | **要改名** | ui-parts が `Pane` / `Panel` の綴り不一致として潰したはずの語 |
| `Row Title Tree` | 行見出しツリー | §5-4, §6-1-1 | ○ | U-23 | **衝突** | 辞書 U-23（確定 2026-08-13）「**操作の入口を指すときは「行見出しパネル」と書くこと（MUST）**」。§5-4 / §6-1-1 は**操作の入口**を指している |
| `Panel Divider` | パネル境界 | §1（46 行） | ○ | U-24 | 一致 | |
| `Properties Panel` | プロパティパネル | §4-6 | ○ | U-25 | 一致 | WCAG 2.1.1 のキーボード等価物 |
| `Command Palette` | コマンドパレット | §1, §2, §3, §6-2 | ○ | U-26 | 一致 | |
| `Schedule Title` | 文書名 | §5-1（901 行） | ○ | U-27 | 一致 | `F2` で編集 |
| `Help Modal` | ヘルプ | §5-5（1034 行） | ○ | U-30 | 一致 | |
| `App Header` | （日本語を当てない） | §2 | ○ | U-31 | 一致 | |
| `Schedule Canvas` | （同上） | §5-4（1020 行） | ○ | U-32 | 一致 | |
| `Canvas Overlays` | （同上） | §1（30 行） | ○ | U-33 | 一致 | スクリーン空間の別レイヤ |
| `Header Commands` | （同上） | §2 見出し | ○ | U-35 | 一致 | |

> **本書に一度も出てこない辞書語**（次工程で置き場所を決める必要がある）:
> `Autosave Status`(U-28) / `Hidden Group Tab`(U-29) / `AI Export Modal`(U-30 の片方。本書は `AI` ボタンだけ) /
> `Palette Groups` `Palette Commands`(U-34) / `Branding`(U-35) / `Agent API`(U-36) /
> `WatermarkPassword`(U-37) / `Selection`(U-39) / `Marquee`(U-40)。
> 逆に **ui-parts §3 の木にある `App Shell` と `Modals` は辞書に無い**（本書にも無い）。

### 表 B — 操作・ジェスチャ・掴み 23 行

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `hit region` | 掴み領域 | §4-1（168 行, 全 11 回） | ✗ | — | **辞書に無い（追加が要る）** | 本書が「掴み領域」と「掴み点」を**明示的に 2 語に分けている**。辞書はこの区別を持たない |
| `handle` | 掴み点 | §4-1（169 行, 全 3 回） | △ | K-94 / K-95 | **辞書に無い（追加が要る）** | 辞書に在るのは「**フェード掴み点**」の 2 キーだけ。**総称としての「掴み点」の行が無い** |
| — | 掴み代 | §4-1（237 行の見出し） | ✗ | — | 辞書に無い（追加が要る） | 「見た目より大きくとる」量 |
| — | 平行移動 | §4-1（ジェスチャ 4 種） | ✗ | — | 辞書に無い（追加が要る） | ジェスチャ名。英名が本書に無い |
| — | リサイズ | §4-1 | ✗ | — | 辞書に無い（追加が要る） | 同上 |
| — | ラベル移動 | §4-1 | ✗ | — | 辞書に無い（追加が要る） | 同上 |
| — | アンカー移動 | §4-1 | ✗ | — | 辞書に無い（追加が要る） | 同上 |
| — | 開始点 | §4-1（予定バー / 実績バー） | ✗ | — | 辞書に無い（追加が要る） | ⚠️ 表 T-006b `A-13`「端点」の下位。ui-parts §4-2(c)「**日付 vs 掴み点**」の区別を担う語 |
| — | 終了点 | §4-1 | ✗ | — | 辞書に無い（追加が要る） | 同上 |
| — | バー本体 | §4-1 | ✗ | — | 辞書に無い（追加が要る） | 端点を除いた中間。平行移動 |
| — | マイルストーン点 | §4-1（204 行） | ✗ | — | 辞書に無い（追加が要る） | 幅がないので平行移動のみ |
| — | フェード掴み点 | §4-1（189〜190 行） | ○ | K-94 / K-95 | 一致 | 表 T-006b `A-13` が「端点」と別物と明記 |
| — | 枠本体 | §4-1（注記） | ✗ | — | 辞書に無い（追加が要る） | 角丸囲みの内部 |
| — | 枠の角 | §4-1（注記） | ✗ | — | 辞書に無い（追加が要る） | 4 隅・リサイズ |
| — | コメント本体 | §4-1（227 行） | ✗ | U-14 | **要改名** | 辞書 U-14「本文は『**コメントボックスの本文**』と書く」 |
| — | 引出しアンカー | §4-1（228 行） | ✗ | — | 辞書に無い（追加が要る） | 引出し線の先端。アンカー移動 |
| — | 依存アンカー | §4-1（234 行） | ✗ | — | 辞書に無い（追加が要る） | 外接矩形の **9 点**（表現力）。自動配線が使うのは 2 点 |
| — | 折れ点 | §4-1, §4-9 | ✗ | — | 辞書に無い（追加が要る） | **表示のみ・人は動かさない** |
| — | ダミーの実績線 | §4-0（157 行）, §4-1（245 行） | ✗ | — | 辞書に無い（追加が要る） | 着手の入口。**11×10px では小さすぎた**という実測が付く |
| — | ポインタ | §5-2（13 回） | ✗ | — | 一致 | 辞書ではなく**表 T-006b `A-15`** が持つ。`Cursors` と別物 |
| — | 開いた手 / 握った手 | §5-2（950〜953 行） | ✗ | — | 辞書に無い（追加が要る） | ポインタの形 2 種 |
| — | パン | §5-2 | ✗ | — | 辞書に無い（追加が要る） | 背景ドラッグ / `Ctrl`+ドラッグ / 中ボタンドラッグ。**等倍** |
| — | キャレット | §5-1（893 行） | ✗ | — | **辞書に無い（追加が要る）** | ⚠️ **本書のこの 1 か所にしか無く、何を指すか本書内で定義されていない**（`Cursors` なのか別の点なのかは**未検証**） |

### 表 C — ボタン・コマンド（アイコンと操作の対応）30 行

**次の工程の入力になる部分である。** ⚠️ ただし本書が持つのは**役割の対応**であって、
**アイコンの絵の全数は本書に無い**（§3 が「前プロジェクトのアイコン定義が参考になる」と外に投げている）。
同フォルダの `appheader-palette-icons.html` が絵を持つが、**本調査では読んでいない**（担当外）。

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `Fit` | — | §2, §5-2, §6-7 | ✗ | — | 辞書に無い（追加が要る） | **縦横とも**収める。キーは `F`。⚠️ **収まることは保証しない** |
| `Cmd` | — | §2 | ✗ | U-26 | **要改名** | `Command Palette` の表示/非表示。**略語**。ui-parts §5-2 も「画面に出す文字列は未決」 |
| `SS` | — | §2 | ✗ | — | **要改名** | 画面のコピー。**本書自身が「§6-6（言葉ではなくアイコンで伝える）に反する」と書いている**（89 行） |
| `Load` / `Save` | — | §2 | ✗ | — | 辞書に無い（追加が要る） | ファイル読込 / 保存 |
| `Theme` | — | §2 | ✗ | K-59 | 辞書に無い（追加が要る） | 明暗テーマ切替。値の名は `themePreference` |
| `Base` | — | §2 | ✗ | K-84 | **要改名** | 中身は「**変更前の予定**」（`baselineVisible`）。**表 T-006b `A-7` が「ベースライン」を単独禁止**にしている |
| `Undo` / `Redo` | — | §2 | ✗ | — | 辞書に無い（追加が要る） | `Ctrl+Z` / `Ctrl+Y`・`Ctrl+Shift+Z` |
| `AI` | — | §2 | ✗ | U-30 | 辞書に無い（追加が要る） | 開くのは `AI Export Modal`。ボタン名が部品名と繋がっていない |
| `?` | — | §2, §5-5 | ✗ | U-30 | 辞書に無い（追加が要る） | 開くのは `Help Modal`。同上 |
| `[予定]` | 予定 | §2, §3, §4-4 | ✗ | K-74 | **衝突** | **アイコン = 破線 1 本**。2 トグルと `planActualDisplay`（3 値）の対応が未定義 |
| `[実績]` | 実績 | §2, §3, §4-4 | ✗ | K-74 | **衝突** | **アイコン = 実線 1 本**。押下状態＋形状の**二重符号**（WCAG 1.4.1） |
| `[分離]` | 分離 | §2, §4-4, §7 | — | — | 廃止語 | **廃止済み**（`user-order.md` 項 52 は欠番）。次期に持ち込まない |
| `progress-line` | イナズマ線 | §3 | ✗ | U-9 | 辞書に無い（追加が要る） | **アイコンは絵文字ではなくジグザグの線画**（§3・114〜116 行）。語幹は `Progress Line` と一致 |
| `progress-marker` | 進捗マーカーの表示切替 | §3 | ✗ | U-5 / K-78 | 辞書に無い（追加が要る） | 語幹一致 |
| `comment` | — | §3 | ✗ | U-14 | **要改名** | → **`comment-box`**。辞書 U-14 が略称を MUST NOT |
| `highlight-box` | ハイライトボックス | §3 | ✗ | U-15 | 辞書に無い（追加が要る） | 語幹一致。ui-parts が `add-box` から直した |
| `watermark` | 透かし | §3 | ✗ | U-20 | 辞書に無い（追加が要る） | 語幹一致 |
| `grid-date` | 日付罫線 | §1（64 行）, §3 | ✗ | U-17 | **要改名** | → `date-grid-lines`。**語順反転**。ui-parts が設定キー側で潰した同型 |
| `grid-group` | グループ罫線 | §1（64 行）, §3 | ✗ | U-18 | **要改名** | → `group-grid-lines`。同上 |
| `guide-cursor-none` / `-crosshair` / `-single-vertical` / `-double-vertical` | なし / 十字 / 縦1本 / 縦2本 | §3 | ✗ | U-13 / K-81 | 辞書に無い（追加が要る） | **4 モード排他**。語幹は `Guide Cursor` / `guideCursorMode` と一致 |
| `[本日線]` | 本日線 | §3（122 行） | ✗ | U-11 | **要改名** | → `[基準日線]`。**独立トグル**。辞書 U-11 が MUST NOT |
| `[デュアル]` | デュアル | §3（123 行） | ✗ | U-12 | **要改名** | → `[デュアルカーソル]`。**略すと `Dual Cursor` と繋がらない** |
| `[ガイド]` | ガイド | §3（124 行） | ✗ | U-13 | **要改名** | → `[ガイドカーソル]`。同上 |
| — | 選択中の図形 | §3（110 行） | ✗ | U-38 / U-39 | **要改名** | 辞書 U-38 `ArmedShape`（構え）は「**「選択」と呼んではならない（MUST NOT）**」。**同じ行の説明文は「構え」と正しく書いている**ので、行ラベルだけが違反 |
| — | 整列コマンド | §6-2（1230 行） | ✗ | — | 辞書に無い（追加が要る） | 選択した `Task` の `start` または `finish` を揃える。**横は人が実行する** |
| — | 矩形 `===` / 矢羽根 `>===>` / 矢印 `--->` / 端点スパン `*----*` | §3（108 行） | ○ | P-11〜P-14 | 一致 | パレットの図形（タスク）。**両端が縦棒の細線は廃止** |
| — | 〇 六角形 五角形 ◇ □ ☆ △ ▽ | §3（109 行） | ○ | P-16 | 一致 | パレットの図形（マイルストーン）。**畳んで隠れており展開して選ぶ** |
| — | 明暗テーマ切替 | §2 | ✗ | K-59 | 辞書に無い（追加が要る） | ボタン `Theme` の役割側 |
| — | 進捗マーカーのクリック（3 状態を巡る） | §4-6 | ✗ | U-5 | 辞書に無い（追加が要る） | **完了時の入口**。`actualFinish` に右端の日付が入る |
| — | 再開アイコンのクリック / ドラッグ | §4-6 | ✗ | U-6 | 辞書に無い（追加が要る） | **中断のときだけ出る L 字の折れ矢印**。`resume` を変える |

> **アイコンについて本書が確定していること（4 点）**
> 1. **全ボタンをアイコン化する**（テキストラベルをやめる）—— §2（87 行）
> 2. **アイコンは全て線画。20×20 のデザイングリッドで作画し太さを揃える** —— §3（114 行・CR-014）
> 3. **責務で置き場所を分ける** —— `App Header` = 文書に対する操作 / `Command Palette` = 描画に対する操作（§2・90〜92 行）
> 4. **ドロップダウンにまとめない**（1 クリック増える）—— §2（93 行）
>
> ⚠️ **同じ機能を 2 か所に置かない**（DEF-013 の再発防止）。`App Header` に予実の状態を出すなら**表示のみ・操作不可**（§2・96〜98 行）。

### 表 D — 状態・モード・分類の語 15 行

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `ArmedShape` | 構え | §3（110 行）, §5-1（893 行） | ○ | U-38 | 一致 | 本書は日本語「構え／構えている図形」で書く |
| `GuideCursorMode` | — | §7（1543 行） | ✗ | K-81 | **辞書に無い（追加が要る）** | 辞書は設定キー `guideCursorMode` だけ持ち、**型名の行が無い** |
| `CursorMode` / `CursorGuideMode` | — | §7（1543 行） | — | — | 廃止語 | 打ち消し線付きで**決着済み**と記録。次期に持ち込まない |
| `Item` | アイテム | §5-1（894〜895 行ほか 9 回） | ○ | N-4a | 一致 | 本書は日本語のみ。⚠️ 辞書 N-4a「**`Task` の別名ではない**」 |
| — | FS / SF / FF / SS | §4-9（431〜436 行） | ✗ | — | **辞書に無い（追加が要る）** | `link_type` の 4 値。**FS=1 / SF=2 / FF=0 / SS=3**（値は MSPDI 正本 XSD で照合済みと本書が記す） |
| — | 対向（族）/ 同側（族） | §4-9（456〜459 行） | ✗ | — | 辞書に無い（追加が要る） | 経路を分ける最上位の分類。**対向 = FS/SF、同側 = FF/SS** |
| — | ① 〜 ⑧（経路パターン） | §4-9 | ✗ | — | **衝突** | **表 T-006a 直後の「概念に記号のラベルを与えてはならない（MUST NOT）」に当たる。** 本書自身「丸囲み数字は画面に出さない」と書くが、**散文が丸数字で議論している**（②③ と ④⑤ の切替 等） |
| — | `FS-1` 〜 `FS-5` | §4-9（505〜509 行） | ✗ | — | 辞書に無い（追加が要る） | **PoC の標本データに付けた名前**。⚠️ **SF / FF / SS の標本は無い**（本書が明記） |
| — | 時間軸 LOD / タスク LOD / グループ LOD | §6-3 / §6-4 / §6-5 | ✗ | K-52〜K-57 | **辞書に無い（追加が要る）** | 辞書は「LOD」群のキーを持つが**3 種の呼び名の行を持たない**。ui-parts §1-2 は「必ず言い分ける」と定める |
| — | 段間の通路 | §4-9（584 行〜, 全 10 回） | ✗ | — | 辞書に無い（追加が要る） | ④⑤⑧ の水平部分が通る高さ |
| — | 入口の走り | §4-9 | ○ | K-20 | 一致 | `dependencyRunOfArrow` の日本語欄が「入口の走り ÷ 三角形」 |
| — | 出口の走り | §4-9 | ✗ | — | **辞書に無い（追加が要る）** | **入口の走り − 三角形**。独立した定数ではなく導出値 |
| `milestone`（真偽値） | — | §6-1（1084, 1111, 1119 行） | ✗ | N-1 | **衝突** | 辞書 N-1「⚠️ **真偽値の `milestone` という列は持たない**」。本書の言及は**廃止した規則の記録**なので実害は小さいが、ui-parts §2-1-1 は今も bool を確定と書いている |
| — | マルチバー | §4-2, §6-1, §6-7 | ✗ | — | **辞書に無い（追加が要る）** | 「**本ツール最大の差別化**」と繰り返し書かれる。ui-parts §2-0 は「機能名」と位置づける |
| `Frozen Record` | — | §6-1（1129 行）, §4-9（690 行） | ✗ | — | 辞書に無い（追加が要る） | PoC の扱いを示すプロセス語。**PoC の絵で段割当を検算しないこと** |

### 表 E — 幾何の導出値（§4-10-1 の図のノード名）13 行

**これは「設定値」ではなく「計算の途中に出る値」の名前である。辞書は 1 つも持っていない。**
UI 実装がそのまま変数名にする層なので、**次の工程で名前を決めないと必ずずれる。**

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `planHeight` | その形状の予定の縦幅 | §4-10-1（737 行） | ✗ | — | 辞書に無い（追加が要る） | `max(planFloor, basePlanHeight × zoomY × shapeHeightOf)` |
| `actualHeight` | 実績の縦幅 | §4-10-1 | ✗ | — | 辞書に無い（追加が要る） | `planHeight × actualOfPlan`。**クランプを掛けない**（§4-10-3） |
| `planFloor` | 予定の下限 | §4-10-1 | ✗ | — | 辞書に無い（追加が要る） | `actualMin ÷ actualOfPlan`。**縦の下限は 1 か所だけに置く** |
| `fontRaw` | 素のフォント | §4-10-1 | ✗ | — | 辞書に無い（追加が要る） | |
| `fontSize` | フォントサイズ | §4-10-1 | ✗ | — | 辞書に無い（追加が要る） | `max(fontRaw, fontMin)` |
| `markerSize` | マーカー径 | §4-10-1 | ✗ | — | 辞書に無い（追加が要る） | `fontSize × markerOfFont` |
| `markText` | マーカーの数字 | §4-10-1（745 行） | ✗ | — | **要改名** | → `markerText`。ui-parts が 2026-07-31 に `mark*` → `marker*` へ統一済み |
| `labelW` | ラベルの幅 | §4-10-1（746 行） | ✗ | — | **要改名** | → `labelWidth`。**裸の `W` は表 T-006a 直後の規則に反する** |
| `occW` | 占有幅 | §4-10-1（747 行） | ✗ | — | **要改名** | → `occupiedWidth`。同上。⚠️ 表 T-006b `A-11` が「幅」の単独使用を禁じている概念 |
| `rowH` | 行の高さ | §4-10-1（759 行） | ✗ | — | **要改名** | → `rowHeight`。同上 |
| `stackCount` | 積み段の割当 | §4-10-1 | ✗ | — | 辞書に無い（追加が要る） | ⚠️ 名前は「数」だが日本語欄は「割当」。**どちらの意味か本書からは決まらない（未検証）** |
| `dependencyRunEntry` | 入口の走り | §4-10-1（772 行） | ✗ | — | 辞書に無い（追加が要る） | `dependencyArrowLength × dependencyRunOfArrow` |
| `dependencyRunExit` | 出口の走り | §4-10-1（773 行） | ✗ | — | 辞書に無い（追加が要る） | `dependencyArrowLength × (dependencyRunOfArrow − 1)` |

### 表 F — 交換相手（MSPDI）由来の綴りと snake_case 3 語 9 行

**2026-08-14 の決定「MSPDI の綴りが現れてよいのは `carry` / `carryElements` の内側だけ」に照らすと、
本書には carry の外に置かれた MSPDI 綴りが 6 件ある。**
⚠️ **ただし本書は前プロジェクトの資産であり、この決定より前に書かれている。**
判定は「次期の仕様へ写すときに直す対象」という意味で付けた。

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `wbs_parent_uid` | WBS の親 | §6-1-1（1168 行）, §6-4（1303 行） | ○ | P-22 | **要改名** | → **`wbsParentUid`**（2026-08-14 決定で snake_case 例外 3 語を廃止）。**辞書 P-22 と表 T-006a `W-8` の側も直す必要がある** |
| `link_type` | 依存の種別 | §4-9（429, 431, 682 行） | ✗ | — | **要改名** | → **`linkType`**。⚠️ **辞書に行が無い**ので、改名と同時に**追加**も要る |
| `status_date` | 基準日 | 本書は「基準日」と日本語でのみ書く（§4-0, §6-6） | ○ | U-11 / キーの規約 | **要改名** | → **`Project.statusDate`**。本書に綴りは出てこないが、**指している値は同じ** |
| `PredecessorLink` / `PredecessorLink/Type` | — | §4-9（429, 438, 661 行） | ✗ | — | **衝突** | carry の外に MSPDI の要素名が露出している |
| `LinkLag` | ラグ | §4-9（669 行） | ✗ | K-104 | **衝突** | 辞書が持つのは `dependencyLagDefault`（既定値）だけ。**MSPDI 側の綴りは carry 内に限る** |
| `OutlineLevel` | — | §6-1-1（1140 行）, §6-4（1303 行） | ✗ | — | **衝突** | 表 T-006b `A-3` が「レベル」の明示先として挙げる語。carry 外 |
| `Project/WeekStartDay` | 週の始まり | §6-3（1247 行） | ✗ | — | **衝突** | carry 外 |
| `Summary` | サマリタスク | §6-7（1426 行）, §6-1-1（1166 行） | ✗ | — | **衝突** | carry 外 |
| `UID` / `uid` | — | §6-1-1（1141 行）, §6-1（1073, 1082 行） | ✗ | — | **辞書に無い（追加が要る）** | ⚠️ **大文字 `UID`（MSPDI）と小文字 `uid`（並べ替えのタイブレーク）が同じ文書に混在**。同じものかは**本書からは決まらない（未検証）** |

### 表 G — 設定値のキー（辞書と照合済み）43 行

**本書に現れた設定値キーは 41 個で、いずれも辞書と綴りが一致していた。**
（`documentSettings` と `SPEC` は設定値そのものではないので別扱い。）

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `pxPerDayAt1x` | 1 日の幅 | §6-7 | ○ | K-1 | 一致 | |
| `rulerHeight` | 目盛の帯の高さ | §6-3, §6-7 | ○ | K-2 | 一致 | **段階によらず固定**（Fit の算出が閉じる） |
| `rulerFont` | 目盛の文字 | §6-3, §4-10-2 | ○ | K-3 | 一致 | |
| `basePlanHeight` | 予定の縦幅 | §4-10-1, §4-10-2 | ○ | K-4 | 一致 | 根の 1 つ |
| `actualOfPlan` | 実績 ÷ 予定 | §4-10-2, §4-10-3 | ○ | K-5 | 一致 | |
| `actualMin` | 実績の縦幅の下限 | §4-10-1〜3 | ○ | K-6 | 一致 | 根の 1 つ |
| `fontOfActual` | フォント ÷ 実績 | §4-10-2 | ○ | K-7 | 一致 | |
| `fontMin` | 最小フォント | §4-10-1, §4-10-2 | ○ | K-8 | 一致 | 根の 1 つ。**12px** |
| `stackGap` | 段の間隔 | §4-10-1, §4-10-2, §6-7 | ○ | K-11 | 一致 | `≧ dependencyWidth × 2` |
| `rowGap` | 行の間隔 | §6-7 | ○ | K-12 | 一致 | |
| `shapeHeightOf.arrow` | 矢印 | §4-10-2 | ○ | K-15 | 一致 | |
| `shapeHeightOf.milestone` | マイルストーン | §4-9, §4-10-2 | ○ | K-17 | 一致 | |
| `dependencyWidth` | 依存線の太さ | §4-9, §4-10-1, §4-10-2 | ○ | K-18 | 一致 | **固定の定数** |
| `dependencyArrowLength` | 矢印の三角形の長さ | §4-9, §4-10-1 | ○ | K-19 | 一致 | |
| `dependencyRunOfArrow` | 入口の走り ÷ 三角形 | §4-9, §4-10-1, §4-10-2 | ○ | K-20 | 一致 | **`> 1` の制約が両端を同時に担保** |
| `markerOfFont` | マーカー径 ÷ フォント | §4-10-1 | ○ | K-21 | 一致 | |
| `markerGap` | 実績の右端からの隙間 | §4-10-1 | ○ | K-23 | 一致 | |
| `labelCoef` | 幅の概算係数 | §4-2 | ○ | K-30 | 一致 | **キャッシュしない** |
| `labelHaloOfFont` | 縁取りの太さ ÷ フォント | §4-3, §6-1-3 | ○ | K-34 | 一致 | **0 にすれば縁取りは消える** |
| `thinStrokeMin` | 細線の太さの下限 | §4-10-2 | ○ | K-41 | 一致 | `thinStrokeMax` と相互に縛る |
| `thinStrokeMax` | 細線の太さの上限 | §4-10-2 | ○ | K-42 | 一致 | 同上 |
| `chevronNotchOfWidth` | 矢羽根の切り欠き ÷ 幅 | §4-10-2 | ○ | K-44 | 一致 | `≦ 0.5` |
| `minShapeWidth` | ゼロ期間でも残す最小幅 | §4-9 | ○ | K-49 | 一致 | |
| `rulerTierPxPerDayMonth` | 年 → 年＋月 | §6-3 | ○ | K-52 | 一致 | 既定 **1.4**（本書 1279 行） |
| `rulerTierPxPerDayWeek` | 年＋月 → ＋週 | §6-3 | ○ | K-53 | 一致 | 既定 **4.3** |
| `rulerTierPxPerDayDay` | ＋週 → ＋日＋曜日 | §6-3 | ○ | K-54 | 一致 | 既定 **30** |
| `taskLevelOfDetailReadablePx` | この幅を割った深さは描かない | §6-4, §6-7 | ○ | K-55 | 一致 | **24px**（本書 1521 行） |
| `zoomMin` | 下限 | §4-10-2, §6-7 | ○ | K-63 | 一致 | **0.02** |
| `zoomMax` | 上限 | §4-10-2, §6-7 | ○ | K-64 | 一致 | **64** |
| `canvasPadding` | キャンバスの余白 | §6-7 | ○ | K-65 | 一致 | |
| `svgPadding` | SVG の縁の余白 | §6-7 | ○ | K-66 | 一致 | **画面の Fit には効かない** |
| `zoomX` | 横のズーム倍率 | §5-2, §6-3, §6-7 | ○ | K-67 | 一致 | |
| `zoomY` | 縦のズーム倍率 | §4-10-1, §6-5, §6-7 | ○ | K-68 | 一致 | |
| `scrollDate` | 表示の左端が指す日付 | §6-7 | ○ | K-69 | 一致 | **中央寄せはしない**（データ形式が表現できない） |
| `scrollGroupId` | 表示の上端が指す行 | §6-7 | ○ | K-70 | 一致 | |
| `rowTitlePanelWidth` | `Row Title Panel` の幅 | §6-7（1433 行） | ○ | K-71 | 一致 | |
| `propertyPanelWidth` | `Properties Panel` の幅 | §6-7（1433 行） | ○ | K-72 | 一致 | |
| `stackDirection` | 積む向き | §6-1（1094 行） | ○ | K-73 | 一致 | 既定 `'up'`。**写像だけが変わる** |
| `dependencyVisible` | 依存線 | §4-3, §4-2 | ○ | K-77 | 一致 | 担当/完了率と**独立の 3 トグル** |
| `guideCursorMode` | ガイドカーソル | §7（1543 行） | ○ | K-81 | 一致 | |
| `fontScale` | 文字サイズ | §6-3 | ○ | K-85 | 一致 | ⚠️ 本書は「**S / M / L が何 px かは未定義**」と書くが、**辞書 K-105 `fontScaleSizes.S/M/L` が既に在る**。この未決は閉じている |
| `documentSettings` | — | §4-10, §6-1-3, §6-3 | ✗ | — | 辞書に無い（追加が要る） | 設定の入れ物の名前。辞書は「`documentSettings` が持つ」と本文で使うだけで**行を持たない** |
| `SPEC` | — | §4-10-2（850 行） | ✗ | — | 辞書に無い（追加が要る） | **PoC の定数の置き場**。製品の名前ではない |

### 表 H — データの語・プロパティ（辞書と照合済み）21 行

**本書に現れたデータ側の語 21 個は、いずれも辞書と一致していた。**（`wbs_parent_uid` は表 F、`milestone` は表 D。）

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
|---|---|---|---|---|---|---|
| `Task` | タスク | 全編（13 回） | ○ | N-1 | 一致 | |
| `TaskGroup` | タスクグループ | 全編（11 回） | ○ | N-2 | 一致 | 器・≤Lv5 |
| `TaskGroupMember` | タスクグループメンバー | §6-1-1（1167 行） | ○ | N-3 | 一致 | **export しない** |
| `stackOrder` | 積み順 | §6-1（11 回） | ○ | N-4 | 一致 | **人の指定＝疎な上書き**（`null` = 自動） |
| `name` | 名称 | §4-2（280 行） | ○ | P-1 | 一致 | **略称を廃止したのでそのまま描く** |
| `start` / `finish` | 開始日 / 終了日 | §4-1, §6-2, §4-9 | ○ | P-3 | 一致 | |
| `actualStart` / `actualFinish` | 実績開始日 / 実績終了日 | §4-0, §4-1 | ○ | P-4 | 一致 | **完了にしても右端は 1 日も動かない** |
| `actualDuration` | 実績期間 | §4-0, §4-1 | ○ | P-5 | 一致 | **日常的に触るのはここ 1 つだけ** |
| `resume` | 再開予定日 | §4-1（212 行） | ○ | P-6 | 一致 | 中断のときだけ出る |
| `percentComplete` | 完了率 | §4-0（148 行） | ○ | P-8 | 一致 | **人が直接入力しない導出値** |
| `shapeKind` | タスク形状 | §1, §3, §4-4 | ○ | P-10 | 一致 | |
| `'rectangle'` | 矩形（`===`） | §3 | ○ | P-11 | 一致 | |
| `'chevron'` | 矢羽根（`>===>`） | §3, §4-4 | ○ | P-12 | 一致 | 直訳しない例外 X-2 |
| `'arrow'` | 矢印（`--->`） | §3, §4-4 | ○ | P-13 | 一致 | 幅がないので実績を下にずらす |
| `'endpointSpan'` | 端点スパン（`*----*`） | §3, §4-4 | ○ | P-14 | 一致 | **「端点」と略さない** |
| `'milestone'` | マイルストーン | §3, §4-4, §4-9 | ○ | P-15 | 一致 | **実績は実績日の位置へ横にずらす** |
| `milestoneGlyph` | マイルストーン形状 | §3, §4-9 | ○ | P-16 | 一致 | 8 種すべてで外接矩形の左右中央が輪郭に触れる |
| `strokeColor` / `fillColor` | 線色 / 塗り色 | §6-1-2 | ○ | P-18 | 一致 | **独立に判定する** |
| `'transparent'` | 透明 | §6-1-2 | ○ | P-19 | 一致 | **塗りと輪郭を同時に透明にはできない** |
| `TaskGroup.color` | 行の色 | §6-1-2 | ○ | P-19 | 一致 | 同じ 1 段の分岐で解く |
| `fadeInDays` / `fadeOutDays` | フェードイン / フェードアウト日数 | §4-1（189〜190 行） | ○ | P-21 | 一致 | **矩形と矢羽根のみ** |

---

## 3. 衝突の全数（10 件）

| # | 衝突の型 | 内容 |
|:--:|---|---|
| 1 | 同じものに 2 名 | **`Today Line`（本日線）↔ `Status Line`（基準日線）**。本書の §3 / §5-2 と §6-6 が同じ縦線を別名で呼ぶ |
| 2 | 同じものに 2 名 | **「Resume アイコン」↔ `Resume Icon` / 再開アイコン**。本書内で 4 : 1 |
| 3 | 同じものに 2 名 | **「行名ペイン」↔ `Row Title Panel` / 行見出しパネル**。`Pane` / `Panel` の綴り不一致 |
| 4 | 同じものに 2 名 | **「注釈」↔「注記」（`Annotations`）**。本書内で 1 : 3 |
| 5 | 同じものに 2 名 | **`grid-date` / `grid-group` ↔ `Date Grid Lines` / `Group Grid Lines`**。語順が反転 |
| 6 | 同じ語に 2 義 | **「注記」** —— `Annotations`（U-15a・コメントボックス＋ハイライトボックス）と、**一般語の注記**（§6-1・1084 行「本節末尾の注記」）。**辞書に載せた瞬間に一般語として使えなくなる語である** |
| 7 | 同じ語に 2 義 | **「選択」** —— `Selection`（U-39・既にある対象を選ぶ）と、§3 の行ラベル「**選択中の図形**」（実体は `ArmedShape`＝構え）。**辞書 U-38 が MUST NOT で禁じている取り違え** |
| 8 | 同じ語に 2 義 | **「マーカー」** —— `Progress Marker`（U-5）と、§4-10-1 の `markText`「マーカーの**数字**」/ `markerSize`「マーカー**径**」。表 T-006b `A-5` は「単独で書いたら `Progress Marker`」と定める |
| 9 | 同じ語に多義（無併記） | **「段」** —— §4-9 の「別の段」（積み順）／ §6-3 の「4 段階」「3 段」（目盛の帯の中の段）／ §6-5 の「深い段」（`TaskGroup` の深さ）が**同一文書内で併記なしに混在**。表 T-006b `A-1` が「6 義ある。必ずどれかを併記する」と定める |
| 10 | 面の対応が未定義 | **`[予定]` `[実績]` の 2 トグル ↔ `planActualDisplay` の 3 値**。操作子 2 個と列 1 個の写像を書いた場所が無い |

---

## 4. 要改名の全数（23 件）

| # | 現在の名前 | 直す先 | 根拠 |
|:--:|---|---|---|
| 1 | `Today Line` / 本日線 | **`Status Line` / 基準日線** | 辞書 U-11（MUST NOT） |
| 2 | `[本日線]`（トグル） | **`[基準日線]`** | 同上 |
| 3 | 「Resume アイコン」 | **`Resume Icon` / 再開アイコン** | 辞書 U-6・英日混在 |
| 4 | 「行名ペイン」 | **行見出しパネル（`Row Title Panel`）** | 辞書 U-22・ui-parts §2-1-6 |
| 5 | 「注釈」（§5-1） | **注記（`Annotations`）** | 辞書 U-15a |
| 6 | 「コメント本体」 | **コメントボックスの本文** | 辞書 U-14（MUST NOT） |
| 7 | `comment`（コマンド） | **`comment-box`** | 辞書 U-14・`highlight-box` と揃える |
| 8 | `grid-date` | **`date-grid-lines`** | 辞書 U-17・語順反転 |
| 9 | `grid-group` | **`group-grid-lines`** | 辞書 U-18・語順反転 |
| 10 | `[デュアル]` | **`[デュアルカーソル]`** | 辞書 U-12 |
| 11 | `[ガイド]` | **`[ガイドカーソル]`** | 辞書 U-13 |
| 12 | 「選択中の図形」 | **「構えている図形」（`ArmedShape`）** | 辞書 U-38（MUST NOT） |
| 13 | `Cmd`（ボタン） | 未定（`Command Palette` と繋がる名前へ） | 略語。ui-parts §5-2 が未決と記す |
| 14 | `SS`（ボタン） | 未定（アイコン化） | **本書自身が §6-6 違反と記す** |
| 15 | `Base`（ボタン） | **「変更前の予定」に繋がる名前** | 表 T-006b `A-7`（「ベースライン」単独禁止） |
| 16 | `markText` | **`markerText`** | ui-parts §2-1-6（2026-07-31 `mark*`→`marker*`） |
| 17 | `labelW` | **`labelWidth`** | 表 T-006a 直後の「裸の `W` 禁止」 |
| 18 | `occW` | **`occupiedWidth`** | 同上 |
| 19 | `rowH` | **`rowHeight`** | 同上 |
| 20 | `wbs_parent_uid` | **`wbsParentUid`** | 2026-08-14 決定（snake_case 例外 3 語の廃止）。**辞書 P-22 と表 T-006a `W-8` の側も要修正** |
| 21 | `link_type` | **`linkType`** | 同上。⚠️ **辞書に行が無いので追加も要る** |
| 22 | `status_date`（本書では「基準日」） | **`Project.statusDate`** | 同上。⚠️ **本書に綴りは現れない** —— 日本語で「基準日」と書くだけである（§4-0 / §6-6）。直す先は辞書 U-11 とキーの規約の側 |
| 23 | `Comment Boxes` の完全形が本書に無い | **「コメントボックス」を使う** | 辞書 U-14。本書は `comment`（§3）と「コメント本体」（§4-1）としか書かず、**部品名を一度も名乗っていない**。#6 #7 と根は同じだが、直す場所が 3 か所に分かれる |

---

## 5. アイコンと操作の対応（次の工程の入力）

**本書が確定させている対応は 20 組である。** 絵の形を決めているのは 3 件だけで、残り 17 件は**役割だけ**が決まっている。

| 置き場 | 操作子 | 何をするか | 絵の形は決まっているか |
|---|---|---|---|
| `App Header` | `Fit` | 縦横とも全体を収める（算出は §6-7） | ✗ |
| `App Header` | `Cmd` | `Command Palette` の表示/非表示 | ✗ |
| `App Header` | `SS` | 画面のコピー | ✗（**要改名**） |
| `App Header` | `Load` / `Save` | ファイル読込 / 保存 | ✗ |
| `App Header` | `Theme` | 明暗テーマの切替 | ✗ |
| `App Header` | `Base` | 変更前の予定を別ファイルとして読み込む/外す | ✗（**要改名**） |
| `App Header` | `Undo` / `Redo` | 元に戻す / やり直す | ✗ |
| `App Header` | `AI` | `AI Export Modal` を開く | ✗ |
| `App Header` | `?` | `Help Modal` を開く | ✗ |
| `Command Palette` | 図形（タスク）4 種 | 構える → `Enter` で配置 | **○** `===` / `>===>` / `--->` / `*----*` |
| `Command Palette` | 図形（マイルストーン）8 種 | 同上。**畳んで隠れており展開して選ぶ** | **○** 〇 六角形 五角形 ◇ □ ☆ △ ▽ |
| `Command Palette` | `progress-line` | イナズマ線の表示切替 | **○** **ジグザグの線画**（絵文字ではない） |
| `Command Palette` | `progress-marker` | 進捗マーカーの表示切替 | ✗ |
| `Command Palette` | `comment` | コメントボックスを置く | ✗（**要改名**） |
| `Command Palette` | `highlight-box` | ハイライトボックスを置く | ✗ |
| `Command Palette` | `watermark` | 透かしの表示切替 | ✗ |
| `Command Palette` | `grid-date` / `grid-group` | 日付罫線 / グループ罫線の**独立トグル 2 つ**（4 通り） | ✗（**要改名**） |
| `Command Palette` | `guide-cursor-*` 4 値 | ガイドカーソルの**4 モード排他** | ✗ |
| `Command Palette` | `[予定]` `[実績]` | 予実の表示切替（**両方 OFF は作らせない**） | **○** 予定=**破線 1 本** / 実績=**実線 1 本** |
| `Command Palette` | `[本日線]` `[デュアル]` `[ガイド]` | カーソル系 1 グループ | ✗（3 件とも**要改名**） |

**キャンバス上の操作にアイコンは無く、形とジェスチャで示す（5 組）**

| 対象 | 操作 | 効果 |
|---|---|---|
| 実績バーの**右端**をドラッグ | 1 日刻み | `actualDuration` が変わる（完了率は自動追随）。**日常的に触るのはここだけ** |
| **進捗マーカー**をクリック | 3 状態を巡る | 完了時に `actualFinish` へ右端の日付が入る |
| **再開アイコン**をクリック / ドラッグ | — | `resume` を変える。**中断のときだけ出る** |
| キャンバスの背景 | ポインタが**開いた手** | 掴めることを示す。ドラッグでパン（**等倍**） |
| パン中 | ポインタが**握った手** | — |

⚠️ **不採用が 2 件ある**（次の工程で再提案しないこと）: **右クリックのクイック設定** / **キーボードでの進捗増減**（§4-6）。

⚠️ **掴めない端点は薄く描き、理由をツールチップで示す**（§4-4・344 行）。無反応だと故障に見える。

---

## 6. 未検証として残すもの

**確かめられなかったことを断定で書かない。以下は「本書からは決まらない」と記録する。**

1. **「キャレット」が何を指すか。** §5-1（893 行）「構えている図形を**キャレット位置**に配置」の 1 か所にしか現れず、
   本書内に定義が無い。`Cursors`（`Guide Cursor` 等）なのか、別の挿入点なのかは**未検証**。
2. **`UID`（大文字・MSPDI）と `uid`（小文字・並べ替えのタイブレーク）が同じものかどうか。**
   本書は両方を使うが、対応を書いていない。**未検証**。
3. **`stackCount` の意味。** ノード名は「数」だが日本語欄は「積み段の**割当**」。どちらか**未検証**。
4. **`Today Line` と `Status Line` が同一の部品かどうか。** ui-parts §2-1-6 が `todayDay` → `statusDate` の理由を
   「中身は基準日であって本日ではなかった」と記し、辞書 U-11 が「本日線」を MUST NOT にしていることから
   **同一と読むのが自然だが、「UI パーツとして統合した」と書いた文は見つけられなかった**。
   ui-parts §2-1-3 は今も `Today Line` を独立のパーツ行として持つ。**統合の是非は次工程で確定させること。**
5. **辞書が参照する表 T-023b / T-023c / T-023d の中身。** 辞書 U-38 / U-39 / 表 T-006b `A-13` がこれらを指しているが、
   **本調査では `tbl-glossary.md` しか読んでいない**ので、掴み領域名（開始点・終了点・枠の角 等）が
   既にそこに在るかどうかは**未検証**。表 B の「辞書に無い」は **`tbl-glossary.md` の中に無い**という意味に限る。
6. **アイコンの絵の全数。** 同フォルダの `appheader-palette-icons.html` が持つが、**担当外なので読んでいない**。
   表 C の「絵の形は決まっているか」は **ui-detail-spec-ja.md の中だけ**を見た結果である。

---

## 7. 次の工程への申し送り

1. **表 B（掴み領域・ジェスチャ 23 語）と表 E（幾何の導出値 13 語）は、辞書がまるごと持っていない層である。**
   UI 実装がそのまま変数名にする層なので、**ここを決めずに UI 設計を始めると必ずずれる。**
2. **「§1 の食い違い 11 件」のうち 6 件は、ui-parts が改名を決めたのに ui-detail へ届かなかったものである。**
   ui-parts §2-1-6 自身が「**改名は『新しい名前を決める』ことではなく『古い名前を全部潰す』ことである。
   改名表を書いたら、旧名の全出現を機械で走査して残りゼロを確かめること**」と書いており、
   **その戒めが同じフォルダの隣のファイルで守られていない。** 次期は機械走査を必ず回すこと。
3. **丸囲み数字 ① 〜 ⑧ による経路パターンの呼び名は、表 T-006a 直後の「概念に記号のラベルを与えてはならない」に当たる。**
   §4-9 は依存線の幾何の**正**なので、次期へ写すときに**中身の名前**（例: 水平 1 本 / 中点 2 折れ / 通路経由 4 折れ）へ置き換える判断が要る。
   本書自身「形は 3 種類しかない」と書いている（633 行）ので、置き換え先の候補は本書の中にある。
4. **`fontScale` の S / M / L は本書では未決だが、辞書 K-105 `fontScaleSizes.S/M/L` で既に閉じている。**
   §6-3 の「未定義・`NEXT-STEPS-ja.md` 2-7 で決める」という注記は**古い**。
