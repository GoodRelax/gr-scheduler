# U4 — 裁定と、用語辞書に足す行

- **担当**: U1 / U2 / U3 の 3 枚を全文読み、衝突を裁定し、`docs/spec/_assets/tbl-glossary.md` に足す行を作る
- **読んだもの**: `U1-ui-parts.md`（258 行）／ `U2-ui-detail.md`（445 行）／ `U3-glossary-reverse.md`（271 行）——**すべて全文**。
  原典に当たり直したもの: `docs/spec/_assets/tbl-glossary.md`（259 行）／ `docs/spec/01-04-requirements.md` 1.9（214〜322 行）と表 T-018 / T-023b / T-023c / T-023d / T-034 相当の NT 行 ／ `docs/spec/05-07-design.md`（115 行）／ `docs/spec/_assets/tbl-settings.md`
- 作成日: 2026-08-14
- ⚠️ **`docs/` は 1 文字も編集していない。** 本書は「足す行の案」であり、辞書そのものではない。
- ⚠️ **`previous-project-result` のコード・HTML は 1 行も写していない。名前だけを取った。**

**原典で確かめた事実（U1 / U2 / U3 の記述をそのまま信じていない）**

| 確かめたこと | 実測 | 数え方 |
| --- | --- | --- |
| 辞書の行 ID の全数 | **175**（`N` 5 ／ `P` 22 ／ `U` 41 ／ `K` 105 ／ `X` 2） | `grep -cE '^\| (N\|P\|U\|K\|X)-[0-9]+[a-z]? '` |
| 各表の最大の番号 | `N-4`（＋`N-4a`）／ `P-22` ／ `U-40`（＋`U-15a`）／ `K-106`（**`K-89` は欠番**）／ `X-2` | 行 ID を全数書き出して目視 |
| 辞書の行に MUST / MUST NOT が入っているもの | **7 行**（`U-11` `U-14` `U-15` `U-23` `U-27` `U-37` `U-38`） | 行 ID で始まる行のうち `MUST` を含むもの |

> ⚠️ **利用者の指示「辞書は名前だけを持つ。規則を書いてはならない」に対し、既存の辞書は 7 行が規則を持っている。**
> 本書が足す行には規則を 1 つも書かない。**既存 7 行をどうするかは本書の担当外**（`docs/` を編集しないため）。

---

## 1. 衝突の裁定

**A 群＝同じ概念に別名（12 件）／ B 群＝別概念に同名（8 件）。合計 20 件を 1 件ずつ裁定した。**
判定の根拠は、**辞書（名前の正）＞ 要求 1.9（表記の正）＞ 2026-08-14 の決定 ＞ 前資産**の順で見た。
ただし **2026-08-14 の決定は辞書より新しいので、決定が名指ししたものは決定が勝つ**（A-11 / B-5）。

### A 群 —— 同じ概念に別名

| # | 争っている名前 | **正（断定）** | 捨てる側 | 根拠 |
| --- | --- | --- | --- | --- |
| A-1 | `Bar`（前資産 §2-0 の総称）↔ `Task Bars` | **`Task Bars` が総称** | `Bar` 単独を捨てる | 辞書 `U-2` と表 T-006b `A-14` が一致して `Task Bars` を総称としている。**1 タスク分の横帯を指す語は立てない** —— 画面側は `Plan Bar` / `Actual Bar`、データ側は `Task` で足り、掴み領域は表 T-023d が `予定バー本体` として名を持つ。⚠️ 前資産は同じ文書の中で既に食い違っていた（§2-0 と §2-1-5）ので、**前資産に決着を求めない** |
| A-2 | `Today Line` / 本日線 ↔ `Status Line` / 基準日線 | **`Status Line` / 基準日線** | 本日線 | 辞書 `U-11` が MUST NOT。⚠️ **`docs/spec` 側は既に片付いている** —— 「本日線」は実測 **5 箇所**で、内訳は `FR-046` の統合理由（1951 行）・同じく旧称の説明（1953 行）・`A-appendix` の版数 0.6（34 行）・辞書 `U-11` の禁止文そのもの・`tbl-settings` `S-99d` の**取り消し線付きの廃止行**。**生きた用法は 0 である。** 直すのは前資産側だけ |
| A-3 | 「Resume アイコン」（英日混在）↔ `Resume Icon` / 再開アイコン | **散文の英名は `Resume Icon`、日本語は「再開アイコン」** | 「Resume アイコン」 | 辞書 `U-6` ＋ 表 T-006a `W-6`。混在形は**前資産の 2 ファイルの両方に**あり、どちらも正ではない |
| A-4 | 「行名ペイン」↔ `Row Title Panel` / 行見出しパネル | **`Row Title Panel` / 行見出しパネル** | 行名ペイン | 辞書 `U-22`。`Pane` / `Panel` の綴り不一致は前資産 `ui-parts` §2-1-6 が既に潰したもの。⚠️ `docs/spec` の「行名ペイン」は実測 **0 箇所** |
| A-5 | 「注釈」↔ 「注記」（`Annotations`） | **注記** | 注釈 | 辞書 `U-15a`。⚠️ **`docs/spec` に「注釈」が 2 箇所ある** —— 100 行「型注釈」（TypeScript の語であり無関係）と **1921 行「注釈が本体より大きくなり」（指しているのは進捗マーカー）**。後者は `Annotations`（注記）と読み違えうる。**要改名の候補として §3 に載せた** |
| A-6 | `grid-date` / `grid-group` ↔ `Date Grid Lines` / `Group Grid Lines` | **辞書の語順が正。コマンド名は `date-grid-lines` / `group-grid-lines`** | `grid-date` / `grid-group` | 辞書 `U-17` / `U-18` ＋ 表 T-006a `W-4`（kebab-case）＋「語幹は必ず一致させること（MUST）」。設定値側は既に `dateGridLinesVisible` / `groupGridLinesVisible`（`K-82` / `K-83`）で直っており、**コマンド名だけが取り残されている** |
| A-7 | `comment`（パレットのコマンド）↔ `Comment Boxes` | **`comment-box`** | `comment` | 辞書 `U-14`（「コメント」と略さない）。兄弟の `highlight-box` は既に直っている |
| A-8 | 「チャット欄」↔ 「対話欄」 | **対話欄** | チャット欄 | `docs/spec` が「対話欄」を **2 箇所**で使う（`UC-013` 手順 2 ＝ 874 行、表の `AG-11` ＝ 3153 行）。「チャット欄」は `docs/spec` に **0 箇所**。⚠️ **英名は前資産にも仕様書にも無いので新設する**（§2 の `U-44`） |
| A-9 | 操作の入口は `Row Title Tree` か `Row Title Panel` か | **`Row Title Panel`** | 入口としての `Row Title Tree` | 辞書 `U-23` の MUST（確定 2026-08-13）が前資産より新しい。`Row Title Tree` は**中身（木の表示）を指すときだけ**使う |
| A-10 | `Assignee Label` が「担当と完了率」を 1 名で覆う ↔ 2 つのラベル | **2 つのラベル。`Assignee Label` は担当だけ** | 1 名で覆う書き方 | 仕様書が 2 つとして扱っている —— `UC-003` 手順 4（542 行）・表 T-038 `OC-2`（1201 行）・`FR`「完了率ラベルに出すものを決める」（2748 行〜）・設定値が `assigneeVisible`（`K-75`）と `percentCompleteVisible`（`K-76`）で別。表 T-006b `A-6` も「ラベルを指すなら『完了率ラベル』」と名指ししている。⚠️ **完了率ラベルの英名が辞書に無いので新設する**（§2 の `U-41`） |
| A-11 | 基準日の綴り `Project.status_date` ↔ `statusDate` | **`Project.statusDate`** | `Project.status_date` | 2026-08-14 の決定（snake_case 例外 3 語の廃止）。⚠️ **辞書だけ直しても足りない** —— 表 T-006a `W-8` が 3 語を例外として掲げているので、**`W-8` 行ごと廃止しないと辞書と要求が食い違う** |
| A-12 | 「変更前の予定」に英語の確定名 `Baseline` を与えるか | **与えない** | `Baseline` | `docs/spec` に単独の `Baseline` は **0 箇所**（`baselineVisible` / `labelBaseline` の中にしか現れない）。表 T-006b `A-7` が「①は『変更前の予定』と書く」と定めており、名前は `baselineVisible`（`K-84`）だけで足りている。**前資産の `Base` ボタンは名前ごと作り直す**（§3） |

### B 群 —— 別概念に同名

| # | 同じ綴りが指している 2 つのもの | **正（断定）** | 根拠 |
| --- | --- | --- | --- |
| B-1 | `Item` —— 辞書 `N-4a`（当たり判定と選択の対象の総称）／ 前資産の廃止語 `item` `ScheduleItem` `itemKind` `Canvas Items`（＝ `Task` の別名） | **辞書 `N-4a` が勝つ。衝突ではない** | **外延が違う別概念である。** 前資産が廃止したのは「`Task` の別名としての item」であり、`N-4a` は自ら「`Task` の別名ではない」と書いて外延を分けている。`docs/spec` は `Item` を 3 箇所で総称として使っている（表 T-023 `MK-8` ほか）。⚠️ **前資産の `Canvas Items` は復活させない** |
| B-2 | `milestone` —— 真偽値の列 ／ `shapeKind` の値 `'milestone'` | **`shapeKind === 'milestone'`。真偽値の列は持たない** | 辞書 `N-1` と表 T-005 `G-1` が同じ文で禁じている。前資産 `ui-parts` §2-1-1 / §4-2(a) の `Task.milestone`(bool) は**廃止扱い**。UI 設計で写すと、存在しない列を前提にした画面になる |
| B-3 | 「太さ」—— `dependencyWidth`（`K-18`）／ `progressLineWidth`（`K-50`） | **日本語名を限定形にする。英名は変えない** | 2 つの設定値の**日本語名が「太さ」の 1 語で完全に同一**であり、しかも表 T-006b `A-10` が「太さ」の単独使用を禁じている。**設定画面に「太さ」というラベルが 2 つ並ぶ。** 改名案は §3（`K-18` ＝ 依存線の太さ ／ `K-50` ＝ イナズマ線の太さ） |
| B-4 | 部品名と表示切替キーの日本語名が同じ（進捗マーカー／担当ラベル／イナズマ線／デュアルカーソル／ガイドカーソル／依存線／日付罫線／グループ罫線 の **8 組**） | **矛盾ではない（面が違う）。辞書は直さない** | 表 T-006a が「同じ概念でも面が違えば記法が違う。矛盾ではない」と定める。⚠️ **ただし画面に日本語ラベルを出した瞬間に面の違いが消える。** 「切替のラベルを部品名そのままにしない」は**規則**なので、**辞書ではなく要求（`FR-049` と表 T-202 の周辺）が持つべきである。どの要求に置くかは利用者判断が要る** |
| B-5 | `statusDate` —— 本製品の基準日 ／ `tbl-settings` `S-52` の **PoC 専用キー** `statusDate` 🅿 | **本製品の `Project.statusDate` が正。`S-52` の扱いは利用者判断が要る** | ⚠️ **本書で新たに見つけた衝突である。** いまは `Project.status_date`（本製品）と `statusDate`🅿（PoC）で綴りが分かれているが、**A-11 の改名を実施した瞬間に同じ綴りになる。** `S-52` は「前プロジェクトの PoC 専用。本製品では設定値ではない（`FR-046`）」と明記された行なので消してよいとは限らない。**残すなら 🅿 の印を必ず読ませる形にする必要がある** |
| B-6 | 「段」—— 表 T-006b `A-1` が全数と主張する 6 義 ／ 辞書 `K-105` の「文字サイズの **3 段** の px」 | **`A-1` の 6 義を崩さない。`K-105` の日本語名から「段」を外す** | `A-1` は「6 義ある」と**全数を主張している**のに、辞書自身がその 6 義のどれでもない第 7 の義を作っている。**全数の主張が崩れると `A-1` は守れない。** 改名案は §3 |
| B-7 | 「端点」—— 表 T-006b `A-13`（単独＝バーの端の掴み点）／ 辞書 `K-47` `spanDotOfStroke`「端点の点の半径」（＝ `endpointSpan` の端点） | **`A-13` が正。`K-47` の日本語名を「端点スパン」4 文字にする** | `A-13` は「形状名は必ず『端点スパン』と 4 文字で書く」と定めている。**規則どおりに読むと `K-47` は意味が逆に取れる** |
| B-8 | 「パレット」—— `Command Palette`（`U-26`）／ `Palette Groups` `Palette Commands`（`U-34`）／ 裸の「パレット」 | **裸で使わない。`Command Palette` を明示する** | 辞書の「キーに関する規約」自身が「`palette` を設定値の名前に使わないこと（MUST NOT）—— 同じ語が別のものを指すことになる」と警告している。⚠️ **警告しておきながら表 T-006b に「パレット」の行が無い。表 T-006b への追加が要るかは利用者判断**（辞書ではなく要求の表なので、本書の足す行には入れない） |

---

## 2. 用語辞書に足す行の案

**足す行は 9 行。** そのまま貼れる形にした。

**行 ID の衝突は自分で確かめた** —— 辞書の行 ID を全数（175 行）書き出し、各表の最大値の次を使った。
`N-5` `N-6` `N-7` / `P-23` / `U-41` 〜 `U-45` は**いずれも辞書に存在しない**。
⚠️ **`K-89` は欠番だが、要求 1.9 が「番号は席番号とする（MUST）」と定めているので再利用しない。** 本書は `K` 行を 1 つも足さない。

**足す行に MUST / MUST NOT を 1 つも書いていない。** 規則の正は要求である。

### 2-1. 表 T-101（データの語）に足す 3 行

```
| N-5 | `CommentBox` / `commentBoxes` | コメントボックス 1 つ分の型と、文書が持つその配列の鍵。画面の部品名は `Comment Boxes`（`U-14`） |
| N-6 | `HighlightBox` / `highlightBoxes` | ハイライトボックス 1 つ分の型と、文書が持つその配列の鍵。画面の部品名は `Highlight Boxes`（`U-15`） |
| N-7 | `carry` / `carryElements` | 取り込んだ交換形式の項目をそのまま持ち回るための入れ物。仕組みの呼び名は Carry（表 T-005 の `G-13`） |
```

- `N-5` / `N-6` の出どころは **2026-08-14 の決定**。辞書は `Comment Boxes` / `Highlight Boxes`（UI パーツ名・表 T-006a `W-6`）しか持たず、**型名（`W-1`）と JSON の鍵（`W-2`）の行が無い**。1 行に 2 面を入れたのは `P-3`（`start` / `finish`）・`U-30`・`U-34` と同じ形である。
- `N-7` の出どころも **2026-08-14 の決定**（「交換相手の綴りが現れてよいのは `carry` / `carryElements` の内側だけ」）。⚠️ **その `carry` / `carryElements` が `docs/spec` に 1 箇所も無い**（実測 0 箇所。`Carry` は表 T-005 `G-13` に概念として 3 箇所）。**規則が名指しした先が存在しない状態なので、名前を辞書に置く。**
- ⚠️ **`carryElements` の構造（何を何個持つか）は未検証である。** 設計 5.4 / 6.2 は未記入であり、`_assets/tbl-datamodel.md` は作業ツリーで削除されている。**本行は名前だけを主張し、構造を主張していない。**

### 2-2. 表 T-102（プロパティ）に足す 1 行

```
| P-23 | `linkType` | 依存の種別（4 値は表 T-018 が持つ） |
```

- 出どころは **2026-08-14 の決定**（`link_type` → `linkType`）。⚠️ **辞書には `link_type` の行すら無い**（実測 0 箇所）。`docs/spec` での `link_type` は **2 箇所**（表 T-006a `W-8` の例示 ＝ 292 行、表 T-018 の列見出し ＝ 1664 行）だけで、**依存の種別は `UC-004` の中核なのに確定名が辞書に無かった。**
- ⚠️ **4 値（FS / SF / FF / SS）は足さない。** 表 T-018 が全数と数値を持っており、さらに設計 6.2 が「交換形式の要素と列挙値は本仕様書に写さない（MUST NOT）」と定めている。**辞書に写すと二重管理になる。**
- ⚠️ **所属エンティティ名（表 T-006a `W-7` の `Entity.field` で書くための語）は未検証である。** 辞書は依存のデータ側の型名を持たない（`Dependency Lines` は UI パーツ）。**設計 5.4 が決めるまで `linkType` を裸で置く。**

### 2-3. 表 T-103（UI パーツ）に足す 5 行

```
| U-41 | `Percent Complete Label` | 完了率ラベル |
| U-42 | `Pointer` | ポインタ。マウスが指す点 |
| U-43 | `Grab Region` / `Grab Point` | 掴み領域 / 掴み点（全数と優先順位は表 T-023d が持つ） |
| U-44 | `Dialogue Field` | 対話欄 |
| U-45 | `GoodRelax Scheduler` | GoodRelax Scheduler（略称 `GRS`。日英とも訳さない。`Branding`（`U-35`）が出す文字列） |
```

| 行 | なぜ足すか（画面か散文で指す必要がある証拠） |
| --- | --- |
| `U-41` | **表 T-006b `A-6` が「ラベルを指すなら『完了率ラベル』」と名指ししているのに、その部品名が辞書に無い。** `docs/spec` に「完了率ラベル」は実測 **11 箇所**（`UC-003` 手順 4、表 T-038 `OC-2`、`FR`「完了率ラベルに出すものを決める」、`tbl-settings` `S-61` ほか）。`percentCompleteVisible`（`K-76`）が切り替える対象に名前が無い状態だった。英名は `percentComplete`（`P-8`）と語幹が一致し、`Assignee Label`（`U-8`）と同型である |
| `U-42` | **表 T-006b `A-15` が「マウスが指す点は必ず『ポインタ』と書く」と定めているのに、「ポインタ」が辞書に 1 行も無い。** `docs/spec` に「ポインタ」は実測 **41 箇所**（表 T-023 `MK-2` のズーム中心、`FR-083` の「ポインタが乗っていないあいだ」ほか）。`Cursors`（`U-10`）の対概念であり、**片方だけが辞書にある状態が最も危ない** |
| `U-43` | `docs/spec` に「掴み領域」**8 箇所**・「掴み点」**13 箇所**（実測）。表 T-023d は表題からして「掴み領域と優先順位」であり、`GR-1` 〜 `GR-16` の**個々の名前**は持つが、**2 語の総称に英名が無い。** UI 設計と実装がそのまま変数名にする層である。⚠️ `K-94` / `K-95` の「フェード掴み点」は表 T-006b `A-13` が**別物**と明記しているので、本行の「掴み点」は限定の付かない総称として読む |
| `U-44` | `UC-013` 手順 2（874 行）と `AG-11`（3153 行）が「対話欄」を要求している —— **要求が在るのに部品名が無い。** 前資産は「チャット欄」と呼んでおり（A-8 の衝突）、名前を決めないと UI 設計で 2 語に割れる。英名は「対話欄」の直訳（表 T-105 の例外を増やさない） |
| `U-45` | **2026-08-14 の決定。** 辞書に `GRS` も `GoodRelax` も **0 箇所**。⚠️ 前資産の `Branding` は **`GR Scheduler`**（GRS ではなく GR・空白あり）を画面に出しており、**そのまま写すと画面の文字列が間違う。** `docs/spec` は図 F-001（337 行）で既に `GRS` を使っている。`Agent API`（`U-36`）と同じく、日本語でも訳さない語として置く |

### 2-4. 足さないと決めたもの（U1 / U2 が「追加が要る」と挙げたもののうち）

**足す行を最小にするため、下は足さない。全数を写さないという指示に従った。**

| 足さないもの | 理由（断定） |
| --- | --- |
| `App Shell` / `Modals` / `Grid Lines`（親の器の名） | **画面に出ず、散文でも指す必要がまだ無い。** 子（`App Header` `U-31` ／ `Hidden Group Tab` `U-29` ／ `Help Modal` `U-30` ／ `Date Grid Lines` `U-17` ／ `Group Grid Lines` `U-18`）が個別に名を持ち、罫線をまとめて消す要求も存在しない（切替は `K-82` / `K-83` で別々）。**器の分割は設計 5.2（コンポーネント）が決める層である** |
| `Toast` / 通知の器 | ⚠️ **`Toast` は直訳が成り立たず、足すと表 T-105 の例外を 3 つに増やすことになる**（例外は 2 件だけと明記されている）。通知の作法は `NT-1` 〜 `NT-6` が全数を持つが、**面の形は「Chapter 5 が決める」と仕様書自身が書いている**（2517 行）。**辞書が先に器の名を作ると設計を縛る。M0（表 T-042 `MS-0`）で名づける** |
| 「起動時の面」「有効化の面」 | 同上。`NT-4`（起動時の保留中の用件を 1 枚に集約）が要求として在るので**名前は必ず要るが、それは 1 枚の形が決まってからである** |
| 開始点 / 終了点 / 枠の角 / バー本体 ほか掴み領域の個別名 | **表 T-023d が `GR-1` 〜 `GR-16` で全数と優先順位を持っている。** 辞書へ写すと二重管理になる（要求 1.9「同じことを 2 度書きたくなったら置き場を 1 つ決める（MUST）」）。総称だけを `U-43` で足した |
| `Header Commands` の個別ボタン 11 個（`Fit` `Cmd` `SS` `Load` `Save` `Theme` `Base` `Undo` `Redo` `AI` `?`） | **確定名ではなく前資産の画面文字列である。** しかも全ボタンをアイコン化すると決まっており（前資産 §2）、**文字列そのものが残らない。** `Cmd` / `SS` / `Base` は §3 で要改名として扱う |
| 幾何の導出値 13 語（`planHeight` `labelW` `occW` `rowH` ほか） | **設計と実装の中間変数であり、画面にも仕様の散文にも出ない。** 名前の規約（表 T-006a と「裸の `H` / `W` を使わない」）だけで決まる。**設計 5.5 が持つ** |
| ジェスチャ名（平行移動 / リサイズ / パン / 開いた手 ほか） | 表 T-023（`MK-1` 〜 `MK-13`）と表 T-023a が動作として全数を持つ |
| FS / SF / FF / SS ／ 対向・同側 ／ 経路パターン ①〜⑧ | 表 T-018 が持つ。丸囲み数字は要求 1.9 の「概念に記号のラベルを与えてはならない（MUST NOT）」に当たるので、**辞書に入れず設計で中身の名前に置き換える** |
| 「マルチバー」 | **既に表 T-005 `G-5` にある**（辞書ではなく要求 1.8）。二重に持たない |
| 時間軸 LOD / タスク LOD / グループ LOD | **既に表 T-005a `L-1` 〜 `L-3` にある。** 二重に持たない |
| `documentSettings` / `SPEC` / `Frozen Record` / `UID` / `uid` / キャレット | `documentSettings` は辞書 T-104 の前書きが入れ物として使っている。`SPEC` と `Frozen Record` は **PoC の語であって製品の名前ではない**。`UID` / `uid` とキャレットは **U2 が未検証と記した**ので、確かめずに足さない |

---

## 3. 要改名の一覧

### 3-1. `docs/spec` 側 —— 2026-08-14 の決定に反する名前（**本書では直していない**）

| # | いまの綴り | 改名後 | 実測の出現 |
| --- | --- | --- | --- |
| 1 | `wbs_parent_uid` | **`wbsParentUid`** | **5 箇所 / 5 行** —— `01-04-requirements.md` 195（表 T-005 `G-6`）・292（表 T-006a `W-8`）・1275（`FR` 本文）・1347（表 `PR-15`）、`_assets/tbl-glossary.md` 60（`P-22`） |
| 2 | `link_type` | **`linkType`** | **2 箇所 / 2 行** —— `01-04-requirements.md` 292（`W-8`）・1664（表 T-018 の列見出し）。⚠️ 辞書には行が無いので**改名ではなく新設**（§2-2 の `P-23`） |
| 3 | `Project.status_date` / `status_date` | **`Project.statusDate` / `statusDate`** | **13 箇所 / 9 行** —— `01-04-requirements.md` 292・1945・2139（`GR-16`）・2261（`SK-20`）・2468（表 T-029 `CU-1`）・2824（`WY-1`）、`_assets/tbl-glossary.md` 80（`U-11`）・244（キーに関する規約）、`_assets/tbl-settings.md` 234（`S-99d`） |
| 4 | 表 T-006a **`W-8` の行と直後の注記** | **廃止する** | 292 行（行）・294 行（「`W-8` を増やしてはならない」の注記）。⚠️ **ここを残したまま辞書だけ直すと、辞書と要求が正面から食い違う。** 例外 3 語が消えると `W-8` は空になる |
| 5 | `B-5` の帰結 —— `tbl-settings` `S-52` の `statusDate` 🅿 | **利用者判断が要る** | 100 行。改名後、本製品の基準日と綴りが同じになる |

### 3-2. `docs/spec` 側 —— 表 T-006b と衝突する辞書の日本語名（§1 の B 群の帰結）

| # | 行 | いまの日本語名 | 改名案 | 根拠 |
| --- | --- | --- | --- | --- |
| 6 | `K-18` `dependencyWidth` | 太さ | **依存線の太さ** | `A-10` ＋ `K-50` と同名（B-3） |
| 7 | `K-50` `progressLineWidth` | 太さ | **イナズマ線の太さ** | 同上 |
| 8 | `K-105` `fontScaleSizes.*` | 文字サイズの **3 段** の px | **文字サイズ 3 種の px** | `A-1` の 6 義に無い第 7 の義を作っている（B-6） |
| 9 | `K-47` `spanDotOfStroke` | **端点**の点の半径 ÷ 線の太さ | **端点スパンの点の半径 ÷ 線の太さ** | `A-13`（形状名は 4 文字）（B-7） |
| 10 | `K-11` `stackGap` | **段**の間隔 | **積み順の段の間隔** | `A-1`（併記が無い） |
| 11 | `K-37` `rowTitleIndent` | 行名の **1 段** のインデント | **行名の `TaskGroup` 1 段ぶんのインデント** | `A-1`（「N 段」と単独で書かない） |
| 12 | `01-04-requirements.md` 1921 行の「注釈」 | 注釈 | **印**（指しているのは進捗マーカー） | `Annotations`＝注記（`U-15a`）と読み違えうる（A-5） |

### 3-3. 前資産側 —— UI 設計で**写してはならない**名前

**22 件。** 前資産は直さない（過去の記録である）。**UI 設計が引くときにこの表で置き換える。**

| # | 前資産の名前 | 置き換え先 | 根拠 |
| --- | --- | --- | --- |
| 1 | `GR Scheduler`（`Branding` の画面文字列） | **`GoodRelax Scheduler`** | 2026-08-14 の決定 |
| 2 | `Today Line` / 本日線 | **`Status Line` / 基準日線** | 辞書 `U-11` |
| 3 | `[本日線]`（トグルのラベル） | **`[基準日線]`** | 同上 |
| 4 | 「Resume アイコン」 | **`Resume Icon` / 再開アイコン** | 辞書 `U-6` |
| 5 | 「行名ペイン」 | **行見出しパネル（`Row Title Panel`）** | 辞書 `U-22` |
| 6 | 「注釈」 | **注記（`Annotations`）** | 辞書 `U-15a` |
| 7 | 「コメント本体」 | **コメントボックスの本文** | 辞書 `U-14` |
| 8 | `comment`（パレットのコマンド） | **`comment-box`** | 辞書 `U-14` ＋ `highlight-box` と揃える |
| 9 | `grid-date` | **`date-grid-lines`** | 辞書 `U-17`・語順反転 |
| 10 | `grid-group` | **`group-grid-lines`** | 辞書 `U-18`・語順反転 |
| 11 | `[デュアル]` | **`[デュアルカーソル]`** | 辞書 `U-12` |
| 12 | `[ガイド]` | **`[ガイドカーソル]`** | 辞書 `U-13` |
| 13 | 「選択中の図形」 | **「構えている図形」（`ArmedShape`）** | 辞書 `U-38` |
| 14 | `Bar`（総称） | **`Task Bars`** | 表 T-006b `A-14`（A-1 の裁定） |
| 15 | 「チャット欄」 | **対話欄（`Dialogue Field`）** | A-8 の裁定 ＋ §2-3 の `U-44` |
| 16 | `Task.milestone`(bool) | **`shapeKind === 'milestone'`** | 辞書 `N-1`（B-2） |
| 17 | `markText` | **`markerText`** | 前資産自身が 2026-07-31 に `mark*` → `marker*` へ統一済み |
| 18 | `labelW` | **`labelWidth`** | 要求 1.9「裸の `H` / `W` は `Height` / `Width` と書く」 |
| 19 | `occW` | **`occupiedWidth`** | 同上 |
| 20 | `rowH` | **`rowHeight`** | 同上 |
| 21 | `Cmd` / `SS`（ボタンの文字列） | **未定。アイコン化する** | 前資産自身が「言葉ではなくアイコンで伝える」に反すると記す。⚠️ **`SS` が何の略かはどの資産にも書かれていない（未検証）** |
| 22 | `Base`（ボタンの文字列） | **「変更前の予定」に繋がる名前** | 表 T-006b `A-7`。⚠️ 英名 `Baseline` は採らない（A-12 の裁定） |

---

## 4. 表 T-006b に触れる名前

**表 T-006b は 15 行（`A-1` 〜 `A-15`）。うち UI パーツ名が触れるのは 8 行である**（`A-2` `A-5` `A-6` `A-8` `A-9` `A-13` `A-14` `A-15`）。
**設定値のキーの日本語名まで含めると `A-1`（段）と `A-10`（太さ）も触れ、合計 10 行**になる（§3-2 の `K-11` `K-18` `K-37` `K-50` `K-105`）。
**触れる名前は 16 件**（本書が足す 3 ＋ 既存 13。捨てる `Bar` は既存 13 の中に数えている）。

### 4-1. 本書が足す行のうち T-006b に触れるもの（3 件）

| 足す行 | 触れる行 ID | 何が起きるか | 対処 |
| --- | --- | --- | --- |
| `U-41` `Percent Complete Label` 完了率ラベル | **`A-6`（進捗）** | `A-6` が名指ししている語そのものである。「進捗」と略した瞬間に `Progress Marker` / `Progress Line` と混ざる | **「完了率ラベル」を略さない。** 値だけを指すときは `percentComplete`（`P-8`） |
| `U-42` `Pointer` ポインタ | **`A-15`（カーソル）** | `A-15` は「単独の『カーソル』＝ `Cursors`、マウスが指す点＝ポインタ」と 2 語で切り分けている。**片方が辞書に無いと切り分けが成立しない** | 足すことで `A-15` が初めて両側とも辞書で裏が取れる |
| `U-43` `Grab Region` / `Grab Point` 掴み領域 / 掴み点 | **`A-13`（端点）** | `A-13` は「単独の『端点』＝バーの端の掴み点」「フェードの掴み点は別物」と定める。**総称に名前が無いと、限定形（フェードの掴み点）と総称の区別が書けない** | 総称は限定を付けずに「掴み点」、フェードのものは必ず「フェードの掴み点」 |

### 4-2. 既存の名前で T-006b に触れるもの（13 件）

**日本語名そのものが曖昧語である（最も危険・3 件）**

| 名前 | 日本語 | 触れる行 | 何が起きるか |
| --- | --- | --- | --- |
| `Rows`（`U-1`） | **行** | `A-2` | 「行」と書いたとき `Rows`（UI）か `TaskGroup`（データ）か読めない。⚠️ 前資産の木が「`Rows` ＝ `TaskGroup` 1 つ分の横帯」と定義しているので、**両方が同時に正しい状況が常時ある** |
| `Cursors`（`U-10`） | **カーソル** | `A-15` | マウスのポインタと読まれる。`U-42` を足して初めて片側だけの状態が解消する |
| `Bar`（前資産のみ・捨てる） | **バー** | `A-14` | A-1 の裁定で捨てる |

**限定付きなので使えるが、略すと壊れる（7 件）**

| 名前 | 触れる行 | 略してはならない形 |
| --- | --- | --- |
| `Progress Marker`（`U-5`） | `A-5` / `A-6` | 「マーカー」「マーク」「進捗」と略さない |
| `Progress Line`（`U-9`） | `A-6` | 「進捗」と略さない（日本語は「イナズマ線」。表 T-105 `X-1` の例外） |
| `Comment Boxes`（`U-14`） | （`U-14` の禁止） | 「コメント」と略さない |
| `Highlight Boxes`（`U-15`） | （`U-15` の禁止） | 「囲み枠」と呼ばない |
| `Schedule Title`（`U-27`） | （`U-27` の禁止） | 「表題」「題名」と呼ばない |
| `Row Title Panel` / `Row Title Tree`（`U-22` / `U-23`） | `A-2` | 「行」だけで指さない。操作の入口は `Row Title Panel`（A-9 の裁定） |
| `Plan Bar` / `Actual Bar` / `Task Bars`（`U-2` 〜 `U-4`） | `A-14` | 「バー」と単独で書かない。⚠️ 前資産の掴み領域と重ね順の記述は**裸の「バー」で書かれている**ので、写すと予定と実績の区別が消える |

**日本語名を当てないために散文で曖昧になる（2 件）**

| 名前 | 触れる行 | 対処 |
| --- | --- | --- |
| `Schedule Canvas`（`U-32`） | `A-8` | 散文では必ず英名で書く。「キャンバス」と書くと `exportCanvas`（出力サイズ）/ `canvasPadding`（余白）と区別できない |
| `Canvas Overlays`（`U-33`） | `A-8` | 同上 |

**⚠️ 1 文字違いで別物（1 件・読み飛ばすと入れ替わる）**

| 名前 | 触れる行 | 何が起きるか |
| --- | --- | --- |
| `Status Line`（`U-11`）の日本語名「**基準日線**」 | `A-9`「**基準線**」 | `A-9` は「基準線」を**変更前の予定の訳語に限る**と定める。**「基準日線」と「基準線」は 1 文字違いで別物である。** ⚠️ `A-appendix.md` 版数 0.5 の「性能は…**基準線**は骨格で取り直す」は性能の基準値の意味であり、`A-9` の「性能の実測値は『基準値』と書く」に反している（**本書の担当外・`docs/` は編集していない**） |

### 4-3. 本書が足すことで新たに生まれる「面が違う同名」（1 組）

⚠️ **`U-41` を足すと、`K-76` `percentCompleteVisible` の日本語名「完了率ラベル」と完全に同じになる。**
これは B-4 と同じ型（部品名と表示切替キーの日本語名が同じ）であり、**表 T-006a により矛盾ではない**。
B-4 の 8 組が **9 組**になる。**画面の切替ラベルを部品名そのままにしない方針は、B-4 と一緒に要求側で決めること。**

---

## 数

**すべて自分で数えた。数え方も併記する。**

### 抽出した名前

| 事項 | 数 | 数え方 |
| --- | --- | --- |
| 3 枚の突合表の**行**（U1 51 ／ U2 191 ／ U3 24） | **266** | 7 列（`|` で区切って 9 片）の表の行だけを機械で抜き、見出し行と区切り行を除いた |
| 重複を除いた**名前** | **235** | 「名前（英）」＋「日本語名」を鍵にして重複を畳んだ（`` ` ``・`*`・括弧・空白を無視） |
| うち **前資産 → 辞書**の向き（U1 ＋ U2） | 242 行 / **214 名** | 同上 |
| うち **辞書 → 前資産**の向き（U3） | 24 行 / **24 名** | 同上。**24 名はすべて辞書にある** |

### 辞書にある数 / 無い数（前資産 → 辞書の 214 名について）

| 事項 | 数 |
| --- | --- |
| **辞書に行があるもの** | **113** |
| **辞書に行が無いもの** | **101** |

**判定の内訳（214 名。1 つの名前が複数行に出るときは重い判定を採った）**

| 判定 | 数 |
| --- | --- |
| 一致 | 108 |
| 辞書に無い | 68 |
| 要改名 | 23 |
| 衝突 | 12 |
| 廃止語 | 2 |
| 綴りが違う | 1 |
| **合計** | **214** |

### 本書が出した数

| 事項 | 数 |
| --- | --- |
| **足す行** | **9**（`N-5` `N-6` `N-7` / `P-23` / `U-41` 〜 `U-45`） |
| 足す行が持つ**名前の文字列** | **13**（`N-5` `N-6` `N-7` `U-43` が 1 行に 2 名を持つ） |
| **足さないと決めたもの**（U1 / U2 が「追加が要る」と挙げたもののうち） | **11 群**（§2-4 の表の行数） |
| **裁定した衝突** | **20**（A 群 12 ＋ B 群 8） |
| うち 3 枚が「衝突」と判定していたもの | 13（重複を除いた実測） |
| うち **本書が新たに見つけたもの** | **2**（B-5 `statusDate` の改名後の衝突 ／ A-5 の 1921 行「注釈」）。⚠️ B-3（太さ）・B-6（段の第 7 義）・B-7（端点）は **U3 が §3-1 / §5 で既に見つけていた**ものを裁定し直したのであって、本書の発見ではない |
| **要改名・要廃止として挙げた行** | **34**（`docs/spec` 側 12 ＝ §3-1 の 5 ＋ §3-2 の 7 ／ 前資産側 22） |
| **表 T-006b に触れる名前** | **16**（足す行 3 ＋ 既存 13。§4 の表の行を数えた） |
| 表 T-006b 15 行のうち触れられた行 | **10**（UI パーツ名が触れる 8 行 ＝ `A-2` `A-5` `A-6` `A-8` `A-9` `A-13` `A-14` `A-15`、設定値のキーの日本語名が触れる 2 行 ＝ `A-1` `A-10`） |
| 辞書の行 ID の全数（原典で数え直した） | **175**（`N` 5 ／ `P` 22 ／ `U` 41 ／ `K` 105 ／ `X` 2） |
| 足したあとの行 ID の全数 | **184** |

---

## 未検証 —— 断定していないこと

1. **`carryElements` の構造。** 名前は 2026-08-14 の決定で確定しているが、**何を何個持つかはどこにも書かれていない。** 設計 5.4 / 6.2 は未記入、`_assets/tbl-datamodel.md` は作業ツリーで削除されている。
2. **`linkType` の所属エンティティ名。** 表 T-006a `W-7` は散文で `Entity.field` と書くことを求めるが、**依存のデータ側の型名が辞書に無い。**
3. **2026-08-14 の決定「交換相手の綴りが現れてよいのは `carry` / `carryElements` の内側だけ」の射程。** 識別子だけを指すのか、散文での引用も含むのかが**どこにも書かれていない。** 厳しく読むと、辞書 `U-27` の「MSPDI の `Project/Title` に対応する」・表 `PR-15` の「`Task/OutlineLevel` へ導出」・表 T-018 の 4 値がすべて動く。**利用者判断が要る。**
4. **`tbl-settings` `S-100` の既定の透かし解除パスワード `gr-scheduler`。** 注記は「製品名に変更した」と書いているが、**製品名は `GoodRelax Scheduler` に決まった。** `gr-scheduler` がリポジトリ名なのか製品名の別綴りなのかは**確かめられない。** 値の正は `tbl-settings` なので本書の担当外。
5. **`Scrollbars`（`U-21`）が表 T-006b `A-14`（バー）の対象かどうか。** 複合語なので対象外と読めるが、**規約に明文が無い。**
6. **`SS` / `Cmd` / `Base` が何の略か。** `Cmd` は `Command Palette`、`Base` は `baselineVisible` と**推測できるだけ**で、前資産のどこにも書かれていない。
7. **`Status Line` が `Cursors`（`U-10`）の子かどうか。** 辞書は親子関係を書いていない。U3 も未検証としている。
8. **U2 が未検証とした 3 件**（「キャレット」が何を指すか ／ `UID` と `uid` が同じものか ／ `stackCount` が数か割当か）は、**本書でも確かめていないので足していない。**
