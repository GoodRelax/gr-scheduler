# A03 — `Agent API` の未決事項と、AI 協働トライアルの実測

- 作成: 2026-08-13 ／ 担当: A03（`10-agent-interface/` の未決 2 文書）
- **この表は在庫であって決定ではない。** 各行に出典（`ファイル:行`）を付けた。
- **推測は書かない。** 確かめられないものは「**未検証**」と明記した。

## 0. 読んだ文書と行数

| 略号 | ファイル | 行数 | 読んだ範囲 |
|---|---|--:|---|
| `OI` | `previous-project-result/10-agent-interface/agent-interface-open-items-ja.md` | **572** | **全文**（1-150 / 150-300 / 300-450 / 450-572 の 4 分割） |
| `TR` | `previous-project-result/10-agent-interface/ai-cowork-trial-findings-ja.md` | **335** | **全文**（1-170 / 170-335 の 2 分割） |
| — | 合計 | **907** | — |

**照合のために部分だけ見た文書**（全文は読んでいない。引用は自分で開いた行のみ）:

| ファイル | 見た行 | 目的 |
|---|---|---|
| `previous-project-result/10-agent-interface/agent-interface-requirements-ja.md` | 245-274 | `A-16` / `A-17` の原文確認（食い違い X-1・廃棄 K-1 の裏取り） |
| `previous-project-result/DECIDED-ja.md` | 305-320 | 「open-items §0 で 8 件決着」の指し先確認 |
| `previous-project-result/temp/inventory/A02-agent-api-requirements.md` | 190-275 | 在庫側の決定・未決・食い違い表との突合 |
| `previous-project-result/temp/inventory/A07-security-a11y.md` | 205-215 ほか機械検索 | `documentId` / 保管庫の記述の突合 |
| `previous-project-result/temp/inventory/E09-settings-blob.md` | 318 | `S-99b`（`Agent API` 有効化の記録）の突合 |

---

## 1. 未決の全数（**自分で数えた**）

`OI` 自身の集計表（`OI:564-572`）と、私の数え直しは一致した。

| 区分 | `OI` の主張 | 自分で数えた結果 | 数えた根拠（行） |
|---|:--:|:--:|---|
| 実測で決着 | 8 | **8** | `OI:38, 78, 92, 114, 137, 160, 190, 222` |
| 決定済み（D1-D6） | 6 | **6** | `OI:315, 340, 375, 402, 441, 483` |
| A まだ実測できていない | 0 | **0** | `OI:531-534`（「本節に残る項目は無い」） |
| B 設計で決める | 2 | **2** | `OI:544`（O-4）／`OI:553`（O-10） |
| **未決の合計** | **2** | **2** | 同上 |

### 1-1. 未決 2 件と、それが止めるもの

| # | 未決 | 何を決めるのか | **設計のどこが止まるか** | いつ決めるか | 出典 |
|:--:|---|---|---|---|---|
| **O-4** | 同時更新の方針 | **自動調停**（`AutomaticReconciliation`）を持つか。持つなら粒度は**タスク単位か項目単位か** | 止まらない。**基準版の照合だけで 2 者は成立済み**（6 回発火・壊れた状態 0 件）。粒度を決めると `applyCommands` の拒否の返り値が変わるので、**多人数編集を作る日まで契約を凍らせない** | 多人数編集を実装するとき | `OI:544-551`／`OI:299` |
| **O-10** | エージェント側の寿命と追いつき | 変更の履歴をどこまで保持するか（サーバ側キューの深さ）。**`revision` だけで足りるか、変更の内容も要るか** | 止まらない。**サーバが無ければ保持すべきキューも無い**。ただし「落ちて復帰しても `sinceRevision` で追いつける」は**設計上そうなるはずというだけで未検証** | サーバを立てるとき | `OI:553-560`／`OI:300`／`TR:171` |

### 1-2. 「未決 2 件」に**数えられていない**が、次期が引き取るもの（5 件・自分で数えた）

**`OI` は「決め方が書けないものは未決ですらない」（`OI:14`）という基準で数えている。** 下は
`OI` の中で**別の担当へ送られた**ため合計 2 に入っていない。**落とすと持ち主が居なくなる。**

| # | 残っているもの | `OI` での扱い | 送り先 | 出典 |
|:--:|---|---|---|---|
| 1 | **ブラウザ拡張の経路が未測定** | 「測り残しだが未決ではない」。O-2 が問うた「入れるか」は決着-7 で答えが出ている | 別の入り口の話として保留 | `OI:218-220`／`OI:536-538`／`TR:313-314` |
| 2 | **素の Chrome で 1 件も測っていない** | 対応ブラウザの確定は別担当の仕事 | `NEXT-STEPS-ja.md` 1-1 | `OI:33-36`／`OI:247-250`／`TR:199` |
| 3 | **`documentId` の受け皿がデータ構造に無い** | 「要る理由と最小の形」だけ書き、**正への追加は組込み時に change-manager を通す。ドラフト側から正へ書き足さない** | `02-data-model/grs-native-erd-ja.md` | `OI:471-474` |
| 4 | **`ImportMerge` の命名を正へ通す** | 正（ERD §5.4）の「マージ」を*狭める*提案。構造も挙動も変えないが命名の正を通す必要 | `NEXT-STEPS-ja.md` 2-8 | `OI:525-527` |
| 5 | **初期テンプレートでの `revisionStamp` の初期値** | 決定-3 が縛る先として名指しされているだけで、値は決まっていない | `NEXT-STEPS-ja.md` 2-6 | `OI:399-400` |

> **`OI` が「未決 0 件」と書いた節が 1 つある**（`OI:531`「A. まだ実測できていない（**0 件**）」）。
> **これは「測るべきことは残っていない」の意味であって、「測り終えた」ではない。**
> 上の 1・2 は測っていない（`OI:218`, `OI:33`）。**混同しないこと。**

---

## 2. `file://` について実測で決着した 8 件

**`DECIDED-ja.md:314` が「open-items §0」で指しているのはこの 8 件である**（同行の要約は
「保存の仕組み・隣のファイル・ダウンロード・外部操作・エスケープ・既定非公開・人間の画面への接続・保管庫の共有」）。
**表題の 8 語と `OI:38-245` の 8 節は 1 対 1 で対応する。自分で突き合わせた。**

### 2-1. 測定環境（**混ぜないこと**）

| 系統 | 環境 | 再現方法 | 測った決着 | 出典 |
|---|---|---|---|---|
| 人が操作 | **Microsoft Edge 151（Chromium 151）/ Windows 11**・`file://` を直接開いた | `ai-cowork-trial/file-protocol-probe.html` をダブルクリック | **決着-1 / 2 / 3** | `OI:24` |
| 外部から自動操作 | **Playwright の Chromium 1228** | `file-protocol-automation-probe.mjs` | **決着-4 / 5** | `OI:25` |
| 同上 | 同上 | `agent-api-exposure-probe.mjs` | **決着-6** | `OI:26` |
| 人が起動した本物の Edge へ外部から接続 | Edge 151 を**デバッグポート付き・使い捨てプロファイル**で人間が起動 | `cowork-live-probe.html` ＋ `cowork-live-attach.mjs` | **決着-7** | `OI:27` |
| 外部から自動操作（永続プロファイル） | Playwright の Chromium 1228 を**同じプロファイルで 2 回起動** | `startup-storage-probe.mjs` | **決着-8** | `OI:28` |

- Edge の `navigator.userAgent` 実測値は `OI:31` にある（`Chrome/151.0.0.0 Safari/537.36 Edg/151.0.0.0`）。
- **素の Chrome では 1 件も測っていない**（`OI:33`）。**唯一 `fetch` の遮断（決着-2）だけが 2 系統で同じ結果**（`OI:36`）。
- **`1228` は Playwright のビルド番号であって Chrome の版ではない**（`OI:248`）。

### 2-2. 8 件の中身

| # | 何が決着したか | 主な実測値 | **設計への効き方** | 出典 |
|:--:|---|---|---|---|
| **1** | **`file://` でもファイル保存の仕組みは使える** | `isSecureContext` = **`true`**／`showSaveFilePicker` / `showOpenFilePicker` / `showDirectoryPicker` はすべて **`function`**／呼ぶと **`AbortError`**（`SecurityError` ではない＝ダイアログは開いた）／`createWritable()`→`write()`→`close()` **成功**／**同じハンドルへの 2 回目はダイアログ無しで成功**／ハンドルを **IndexedDB に入れてリロードしても残った**／リロード直後の権限は **`prompt` に戻る**／129 バイト読取 → 196 バイト書戻し成功／**ディスク上はファイル 1 本、` (1)` は作られない** | **往復は「同じファイルへ上書き保存」で閉じる。** AI が書く → 人間が開いて編集 → 同じパスへ保存 → AI が読み直す。**代償は起動ごとの利用者 1 クリック**（復帰ダイアログに「常に許可」が無い＝**省けない**）。起動時に「編集中のファイルへのアクセスを復帰しますか」を出す設計になる | `OI:38-77`／`TR:211-235` |
| **2** | **`file://` から隣のファイルは取得できない** | `fetch('./…json')` → **`TypeError: Failed to fetch`**／同じファイルへの `XMLHttpRequest` → **network error**／`location.search` は**読める** | **URL パラメータで文書を読ませる方式は `file://` では成立しない。** パラメータは受け取れるが中身を取りに行けない。**`file://` で文書を投入する経路は埋め込み（`A-13`）だけ。** 要求 `A-13` は「文献／未実測」から**実測**に変わった | `OI:78-90`／`TR:206-208` |
| **3** | **ダウンロードは保存先を制御できない・名前は尊重される・衝突は ` (n)`** | 保存先は**既定のダウンロードフォルダ（聞かれない）**／1 回目 `grs-file-probe.json`／2 回目 **`grs-file-probe (1).json`**／中身は 2 つとも正しい | ①**「ここへ保存して」と指示する設計にしない** ②**決定的な名前（文書 id ＋ `revision`）を付ければ AI は名前だけで特定できる** ③**上書きされないので「最新を読む」は名前一致ではなく更新時刻で選ぶ必要がある** | `OI:92-110`／`TR:209` |
| **4** | **`file://` のページは外から操作できる** | `file://` への遷移**成功**／ページ内の `location.protocol`・`origin` は **`file:` / `file://`**（本物の file オリジン）／**外部からページ内の関数を呼べた**（`applyCommands` 相当が `{accepted:true, revision:1}`）／**画面も変わった**／**スクリーンショット取得可**／兄弟ファイルの `fetch` は別エンジンでも **BLOCKED** | **AI は `file://` のまま GRS を起動し、API を呼び、画面を確認できる。サーバも拡張も要らない。** これで **CLI の必要性が下がった**（決定-1 の補強材料 1） | `OI:114-135`／`TR:237-255` |
| **5** | **埋め込み時のエスケープ規約は、無いと本当に壊れる**（対照実験） | エスケープ**有り**: 読めた・往復完全一致・非 ASCII 生存／エスケープ**無し**: 読めない・`Unterminated string in JSON at position 76`・**`bold` がページ本文に出た** | **規約は経験則ではなく必須。** `</script>` が早期にタグを閉じ、**残りが HTML として解釈される**。**埋め込みは実質 HTML のテンプレート処理であり、怠れば注入が成立する**（タスク名に markup を入れた文書を渡されると生成物に混入） | `OI:137-158`／`TR:257-271` |
| **6** | **「既定で公開しない」は実際に成立する。ただし取り消しはできない** | **M1**: `typeof globalThis.grSchedulerAgentApi` = **`undefined`**／`agent` を含むグローバル名の全数は **`ondragenter` / `originAgentCluster` の 2 つだけ**（ブラウザ標準・**漏れ無し**）／**M2**: 起動前にフラグを置くと**公開された。埋込文書の入れ口は `null` のまま。ディスク上のファイルはバイト数不変**／**M3**: `file://` の URL クエリは**届く**（`"?agentApi=1"`）／**M4**: **名前を消しても渡し済みの参照は動き続けた** | ①**既定非公開は演出ではない** ②**有効化はファイルを変えずにできる**＝**埋込文書つき GRS は文書だけを運び、権限を運ばない** ③**「後から切れる」を安全性の根拠にしてはならない**。**クエリ有効化は既定手段にしない**（ダブルクリック経路では付けられない） | `OI:160-188`／`TR:273-289` |
| **7** | **人間が開いている `file://` の画面に、外から入れる** | **P1** 人間が起動したブラウザへ接続**成功**（タブ 3 枚）／**P2** `file:` / `file://`・**人間が打った `わたしは人間だ` を外から読めた**／**P3 有効化の前は届かない**（`undefined` / `TypeError`）／**P4 人間がボタンを押した後は届いた**（押下まで 115 秒）／**P5** 正しい `baseRevision` は**受理**（`revision 1 → 2`・`lastEditedBy` = `agent`・**人間の画面が実際に変わった**）／古い `baseRevision` は**拒否**（`stale-base-revision` ＋ `expectedRevision: 2` ＋ **現在の文書つき**） | ①決着-4 とは**別に**、人間が編集中のページで成立 ②**前提はデバッグポート付き起動＝後から入れない** ③**関門は二重**（起動時フラグ＋画面の有効化）で、**②が無ければ①だけでは届かない** ④**基準版の照合はこの経路でもそのまま働き、拒否時に現在の文書が返るので読み直しが要らない** | `OI:190-220`／`TR:291-314` |
| **8** | **`file://` のローカルファイルは、保管庫を 1 つしか持たない** | **S1** `file://` で localStorage は使える（`origin` は `file://`）／**S2** 同じフォルダの別ファイルから**見える**／**S3** 別フォルダからも**見える**／**S5** **IndexedDB も同じ**／**S4** ブラウザを完全に落として再起動しても**残っていた** | ①**自動保存のキーは文書の識別子で分けるしかない**（素の GRS・埋込文書つき GRS・古い複製が同じ鍵を奪い合う。**2 窓同時は想定ではなく構成上の現実**） ②**ファイルハンドルも共有される**＝**無関係な文書のハンドルが「編集中のファイル」として復帰しうる** ③**任意のローカル HTML ページが GRS の自動保存を読める**（`http(s)` 配信なら消える） | `OI:222-250`／`TR:316-335` |

> **決着-1 と決着-3 は同じ「ファイルを書き出す」で挙動が正反対だった**（`OI:70-72`）。
> ダウンロードは上書きされず ` (1)` が増え、保存の仕組みは同じ場所を上書きする。
> **「編集中のファイル」を持つなら後者しかない。**

> **決着-8 は測定の順序そのものが教訓になっている**（`TR:334-335`）——
> 「**この 1 回の測定で、起動順序より先にキー設計が決まった。測らずに決めていたら、
> 2 窓同時のときに黙って上書きし合う設計になっていた。**」

---

## 3. データモデルに効く決定

| # | 決定 | 中身（形・列・既定値・不変条件） | 出典 |
|:--:|---|---|---|
| **M-1** | **`revisionStamp` は保存する** | **`revision` / `lastEditedBy` / `updatedAt` の 3 つだけ。** 決定日 2026-08-02・決めた人はユーザー。**`DECIDED-ja.md:312` が指す決定はこれである** | `OI:375-382` |
| **M-2** | **`agentApiVersion` は入れない** | 書いた側の都合であって日程表の情報ではない。入れると**版が上がるたびに全ファイルの diff が出る**。読む側は自分の版で解釈すればよい | `OI:382`／`OI:391-392` |
| **M-3** | **版が文書に載ること自体は決定-2 で既に確定していた** | `changeLog` の各項目が `revision` を持つため。決定-3 で残っていたのは「**いまの版はいくつか**」だけ | `OI:384-385` |
| **M-4** | **保存する理由は往復の判定** | 決着-1 で**ファイルが往復の運び手**になった。版が乗っていないと AI は「自分が書いたままか、人が直した後か」を**中身の全比較でしか判定できない** | `OI:387-389` |
| **M-5** | **決定論性を捨てた（意図的）** | 「同じ日程 → 常に同じ JSON」は崩れる。ただし **`updatedAt` を入れる時点でどのみち発生**するノイズで、`revision` の 1 行で悪化しない | `OI:394-396` |
| **M-6** | **`revisionStamp` は MSPDI へ書き出さない** | 決定論性が本当に要る往復無損失の検査（`user-order.md` 56）は MSPDI の話であり、対象外 | `OI:396-397` |
| **M-7** | **会話は保存しない。`changeLog` だけ保存する** | 保存するのは「**どの `revision` で・誰が・なぜ変えたか**」。**上限は設けない**（変更 1 回につき 1 件なので**変更回数で自然に有界**） | `OI:347-348` |
| **M-8** | **`changeLog` は文書に同梱され、日程表を渡すと一緒に渡る** | **ユーザーが明示的に承諾済み** | `OI:349` |
| **M-9** | **命名で `note` / `notes` を避ける** | **`Task.notes` と衝突するため**（決定-2 が縛る先として明記） | `OI:374` |
| **M-10** | **`changeLog` に載らないものが 1 種類ある（承知の上）** | **何も変更しなかったときの発言は残らない**（「この日程は間に合いません」とだけ言って何も動かさなかった場合）。**日程表に残す価値が薄いと判断して受け入れた** | `OI:369-370` |
| **M-11** | **自動保存のキーは `grsched.autosave.<documentId>`** | 文書ごとに分ける。**選択の余地は無い**（決着-8）。素の GRS・埋込文書つき GRS・古い複製が同じ鍵を奪い合うため | `OI:449`／`OI:466` |
| **M-12** | **`documentId` はデータ構造にまだ無い** | 「**文書の識別子は現在のデータ構造に無い。これは `02-data-model/grs-native-erd-ja.md`（データ構造の正）の管轄**」。**ドラフト側は「要る理由と最小の形」だけを書き、正への追加は組込み時に change-manager を通す** | `OI:471-474` |
| **M-13** | **`AI 有効化の記憶`も文書ごとに持つ** | **文書ごとに記憶する。ただし有効な間は画面に常時表示を出す** | `OI:451`／`OI:469` |
| **M-14** | **決定的なファイル名の素材** | ダウンロード名に「文書 id ＋ `revision`」を使えば、AI は**名前だけで目的のファイルを特定できる**（決着-3 の帰結。**`documentId` を要求する用途がもう 1 つある**）。⚠️ **`OI:106` は「例」と書いており、命名規則は確定していない** | `OI:106-107` |
| **M-15** | **起動時の 4 分岐は `revision` / `updatedAt` の比較で判定する** | 決定-3 で 2 値が文書に載るので**比較で判定できる**。分岐は下表 M-16 | `OI:453` |
| **M-16** | **起動時の 4 分岐（不変条件）** | ①**同じ文書・自動保存が新しい** → **確認を出す**（これが本当のクラッシュ復旧・`user-order.md` 60） ②**同じ文書・自動保存が古いか同じ** → **黙って捨てる**（「サイレント破棄禁止」は**破損**の規定であって**追い越された**場合の規定ではない） ③**別の文書の自動保存** → **確認を出さない。消しもしない**（保管庫共有の副作用） ④**壊れている** → **必ず通知し、退避して残す** | `OI:455-460` |
| **M-17** | **語彙を 4 語に分けた（識別子は 1 つも増えない）** | `ConcurrentUpdate`＝同時更新／`BaseRevisionCheck`＝基準版の照合（**現行の方針**）／`AutomaticReconciliation`＝自動調停（**将来**）／`ImportMerge`＝取込マージ（**取込専用**） | `OI:488-493` |
| **M-18** | **使わないと決めた語（5 つ。自分で数えた）** | `Conflict`／`Collision`／`Resolution`／**裸の `Merge`**／**裸の `Lock`** | `OI:513-514` |
| **M-19** | **既存の識別子は変えない** | `baseRevision` / `stale-base-revision` / `expectedRevision` は**そのまま**。`AutomaticReconciliation` は延期中の機能なので**散文の語に留める** | `OI:516-517` |
| **M-20** | **将来コード化するときの形だけ先に決めてある** | コマンドは動詞＋目的語 —— `reconcileConcurrentUpdates()` ／ 型は `ConcurrentUpdatePolicy` | `OI:518-519` |
| **M-21** | **拒否の返り値は「現在の文書」を同梱する** | 決着-7 P5 の実測。**拒否時に現在の文書が返るので読み直しが要らない** | `OI:206`／`OI:216` |
| **M-22** | **単調増加する `revision` 1 本で 3 つの問いが片づく** | ①相手が触ったか ②どこまで自分は読んだか（`sinceRevision`） ③自分の書き込みが古い前提に立っていないか（`baseRevision` の一致）。**トライアルでは盤面の更新と会話の投稿が同じ連番に載っていた** | `TR:54-61`／`TR:39` |

> **M-22 は次期にそのままは持ち込めない。** トライアルでは**会話も保存され、会話が `revision` を進めていた**
> （`TR:39` の 104 = 盤面 62 ＋ 会話 42）。**決定-2（`OI:347`）で会話を保存しないと決めた以上、
> トライアルの「会話が版を進める」という前例は次期へ引き継がれない。**
> これは在庫 `A02:264`（X-3）が抱えている `AG-11` 対 `FR-063` / `FR-066` の衝突と同じ論点である。

---

## 4. アーキテクチャに効く決定

| # | 決定 | 中身（層・分割・依存の向き・API の形） | 出典 |
|:--:|---|---|---|
| **R-1** | **描画は DOM 非依存の純粋関数に保つ** | **`document` に触らず、SVG 文字列を返す。** 決定日 2026-08-02・決めた人はユーザー。理由（ユーザー判断）は「**純粋関数のほうが作りやすい。意図しない競合が起こらないから**」 | `OI:315-325` |
| **R-2** | **成果物は当面 単一 `.html` だけ** | **ライブラリ／CLI は出さない。** 必要になった日に足す。**純粋性さえ保てば後から足せる** | `OI:322-323` |
| **R-3** | **D1 だけ性質が違う（不可逆）** | 他の 7 件は決め直せるが、**D1 は決めそこねると選択肢が消える**。逆に言えば **D1 以外は迷ったら先に進んでよい**。**成果物は後から増やせるが、純粋性は後から取り戻せない** | `OI:308-309`／`OI:334-335` |
| **R-4** | **決定-1 が縛る先は明示されている** | `NEXT-STEPS-ja.md` **3-3 モジュール構成**と **3-4 技術スタック**。**描画層が `document` に触れない構成にすること。レビューの観点として残す** | `OI:337-338` |
| **R-5** | **公開点は `globalThis.grSchedulerAgentApi` ちょうど 1 名（フラット）** | 契約の型は **`GrSchedulerAgentApi`** | `OI:411` |
| **R-6** | **既定では公開しない** | 素の GRS を開いただけでは**公開点はどこにも存在しない**（決着-6 M1） | `OI:408` |
| **R-7** | **有効化の 2 経路** | ①**人間が起動**したとき: 画面で「**AI と一緒に編集する**」を有効にしたときだけ ②**エージェントが起動**するとき: **ブラウザを動かしている側が起動時に `shouldExposeGrSchedulerAgentApi` を立てる**。**ファイルにも文書にも載せない** | `OI:409-410` |
| **R-8** | **無効化は不完全である（設計上の制約）** | 「これ以降、名前で探す者」を止めるだけ。**渡し済みの参照は取り返せない**（決着-6 M4）。**「後から切れる」を安全性の根拠にしてはならない** | `OI:412`／`OI:183-184` |
| **R-9** | **API は信頼境界ではない** | ページ内でスクリプトを実行できる主体は API の有無に関わらず内部状態へ到達できる。**既定オフは「攻撃を防ぐ」ためではなく「断りなく口を開けない」ため。防御は取込検証（`security-design.md` §3）と CSP（同 §4）に置いたまま動かさない（`A-7`）** | `OI:425-428` |
| **R-10** | **クエリによる有効化は既定手段にしない** | 決着-6 M3 で成立は確認したが、**ダブルクリックで開く経路では付けられない**（人間が URL を手で打つことになる） | `OI:186-188`／`OI:438-439` |
| **R-11** | **起動時の投入は優先順位つき（ドラフトのまま変えない）** | **埋込文書 → URL パラメータ → 自動保存 → 初期テンプレート** | `OI:447` |
| **R-12** | **負けた自動保存は捨てずに比べる** | 埋込文書は誰かの明示的な行為、自動保存は前回セッションの事故。**表示は前者が勝つ。ただし「勝つ」と「捨てる」は別**で、後者は比較の対象として残す（4 分岐は M-16） | `OI:448`／`OI:464-465` |
| **R-13** | **起動時の面は 1 枚に集約する** | **どのみち 1 クリックは省けない**（決着-1）ので**枚数を増やさない** | `OI:450`／`OI:467-468` |
| **R-14** | **ブラウザの権限ダイアログだけは 1 枚に統合できない** | **クリックを起点にしてしか出せない**ため、「復帰」を押した**後に**ブラウザのダイアログが出る順になる | `OI:476-477` |
| **R-15** | **呼び出しの粒度を粗くする（実測が根拠）** | 1 手あたり「起床 → 状態を読む → 着手を送る → 発言を送る → 待機を張り直す」の **5 往復**が固定発生していた。「**書いて・喋って・次の待機点の `revision` を返す**」を 1 回にまとめ **3 往復**にした。**素直な CRUD（`getDocument` → `applyCommands` → `exportSvg`）は遅い** | `TR:120-131` |
| **R-16** | **監視は revision スコープでなければならない** | 「**私が最後に読んだ `revision` より後の変更だけ通知しろ**」。**「今どうなっているか」を返す監視は、再武装のたびに空振りする** | `TR:99-105` |
| **R-17** | **AI を動かすのは push ではなく「ブロックして待つプロセス」** | 画面を光らせるのは push（SSE）だが、**AI 側は待機プロセスが終わることで起きる**形にした。**61 手と 42 通を、開発チャットを一度も経由せずに回した（0 回）** | `TR:71-78`／`TR:46` |
| **R-18** | **確定の瞬間が 1 点に定まっていること** | 「マスを選ぶ」と「確定する」を分け、**確定を押すまでサーバへ何も送らない**。**選択・取消・選び直しでは `revision` が動かないことを実測**。これで「**人間の編集が終わった時刻**」が曖昧でなくなる | `TR:80-87` |
| **R-19** | **説明の置き場所が編集の邪魔をしてはならない** | 会話の文字数比 **人間 : AI = 1 : 84**（人間 173 字 / AI 14,452 字。**私の計算では 83.5 倍で、`TR:42` は切り上げて 84 と書いている**）。**説明する側の出力量は 2 桁上になる** | `TR:42`／`TR:47-48` |
| **R-20** | **埋め込みはテンプレート処理である（層の位置づけ）** | エスケープを怠れば注入が成立する。**`user-order.md` 62（信頼できない入力・`innerHTML` 直挿し禁止）と同じ土俵に載る**。契約は `agent-interface-spec-ja.md` §6 | `OI:154-158` |
| **R-21** | **試作のコードは持ち込まない** | 「**これらは使い捨ての実験である。GRS へ写すのはコードではなく、要求と契約だけ**」（`README.md` §0-1 コピペ禁止）。試作は **8 ファイル**（自分で数えた） | `TR:177-190` |

---

## 5. トライアルで**実測して分かったこと**（推測ではないもの）

**`TR` は「事実の記録である。設計の決定はしない」と自ら宣言している**（`TR:13`）。
下はサーバの最終状態から機械的に数えた値である（`TR:35`）。

| # | 指標 | 実測値 | 内訳・私の検算 | 出典 |
|:--:|---|--:|---|---|
| **T-1** | 最終 `revision` | **104** | 盤面 **62** ＋ 会話 **42**。**62 + 42 = 104。検算一致。両方が同じ 1 本の連番を進める** | `TR:39` |
| **T-2** | 盤面の更新 | **62** | 着手 61 ＋ 終局宣言 1。**61 + 1 = 62。検算一致** | `TR:40` |
| **T-3** | 会話 | **42 通** | 人間 **9** / AI **33**。**9 + 33 = 42。検算一致** | `TR:41` |
| **T-4** | 会話の文字数 | **14,625 字** | 人間 **173** / AI **14,452**。**173 + 14,452 = 14,625。検算一致。**比は `TR` が「1 : 84」と書くが**私の計算では 83.54 倍**（切り上げ表記） | `TR:42` |
| **T-5** | **基準版の照合による拒否** | **6 回** | 人間の投稿と AI の書き込みが同時更新になり、`baseRevision` 不一致で拒否 → 読み直して再送 | `TR:43`／`OI:509` |
| **T-6** | AI が自分の誤りを訂正した回数 | **7 回** | 全数が `TR:139-147` に列挙されている（**7 行。自分で数えた**） | `TR:44` |
| **T-7** | 開発チャットを経由した回数 | **0 回** | 手番の受け渡しは全て待機プロセスで行った | `TR:45` |
| **T-8** | 壊れた状態の発生 | **0 件** | 6 回の同時更新すべてで、拒否 → 読み直し → 再送が成立した | `TR:66`／`OI:299` |
| **T-9** | 選択・取消・選び直しでの `revision` の動き | **動かなかった** | 確定を押すまでサーバへ何も送らない設計を実測で確認 | `TR:83` |
| **T-10** | 有効化ボタンが押されるまでの時間 | **115 秒** | 決着-7 P4。**ブラウザの初回起動案内に対応していた時間** | `OI:204` |

> **T-5 は語の選択にまで効いた。** 業界名の **optimistic locking（楽観ロック）** を採らなかった理由は
> ①**何もロックしていない**（名が体を表さない） ②「楽観」は**衝突がめったに起きない見込み**を指すのに、
> **実測では 1 セッションに 6 回発火した**（`OI:507-511`）。
> **散文には「業界では optimistic locking と呼ばれる」と 1 行添える**と決めてある（`OI:511`）。

### 5-1. AI が間違えた 7 件（全数。`TR:139-147`）

| # | 何を誤ったか | 種類 |
|:--:|---|---|
| 1 | 手順の前後を取り違えた | 手順の誤り |
| 2 | 「今すぐこうする」と書いたが、支えるものが無く成立しなかった | 前提の欠落 |
| 3 | **相手側の影響範囲を見落とした** | 致命的な見落とし |
| 4 | 「これが一番痛い」と 2 回書いた脅威が、**そもそも成立しなかった** | 存在しない脅威 |
| 5 | 予告した手が、実行すると損になる手だった | 予告の撤回 |
| 6 | 選択肢を列挙したとき、**1 つ数え落としていた** | 数え上げの漏れ |
| 7 | **自分の手で自分の退路を塞いだ**袋小路へ進み、大きく損をした | #3 と同型 |

**#3 と #7 は同じ間違い方である**（`TR:149-150`）——
**他者の影響範囲は 1 つずつ数えるのに、自分が自分の邪魔をしていることを見落とす。**

> **GRS への含意（`TR:152-156`）**: AI が日程を動かすなら同じ種類の誤りが必ず出る
> （「**他行との衝突は見るが、自分が今動かした結果できた制約を見ない**」）。
> だから ①**AI の変更は人間が取り消せなければならない**（`A-4`）
> ②**なぜそうしたかが文書の上に残らなければならない**（`A-12`）。
> **7 件はすべて記録に残っていて、あとから読み返せた。**

---

## 6. やって失敗したこと

| # | 失敗 | 症状 → 原因 → 対処 | **残った規約** | 出典 |
|:--:|---|---|---|---|
| **F-1** | **監視が revision スコープでなくビジーループになった** | 終局検知で待機終了 → AI 起床 → 待機を張り直す → **即座に終了** → 無限ループ。原因は待機条件が「**今の状態**を見て判定する」だったこと。`--since <最後に読んだ revision>` を**必須**にして修正し、終局済み盤面で 3 秒待って正常タイムアウトすることを実測 | **監視は「私が最後に読んだ `revision` より後の変更だけ通知しろ」でなければならない** | `TR:94-105` |
| **F-2** | **日本語をシェル引数で渡して全部文字化けした（Windows）** | 盤面の漢字は正常だったので**ページ側ではない**と切り分け。**コンソールのコードページが、コマンドに渡る前にバイト列を壊していた**。AI の発言は **UTF-8 のファイルに書いてから**送る経路へ変更 | 当時の規約は「**非 ASCII をシェル引数に載せない**（GRS ではタスク名・行名・注記が該当）」。⚠️ **この要求は後に不採用**（§7 K-4） | `TR:106-118` |
| **F-3** | **API の粒度が粗くなく、体感が遅かった** | 遅さの主因は**推論時間ではなく往復回数**（1 手あたり固定 **5 往復**）。「書いて・喋って・次の待機点を返す」を 1 回にまとめ **3 往復**へ | **1 回の呼び出しで「読んで・書いて・次に待つべき点を返す」**（R-15） | `TR:120-131` |
| **F-4** | **AI が 7 回間違えた** | §5-1。#3 と #7 は同型 | **Undo（`A-4`）と理由の記録（`A-12`）が要る** | `TR:134-156` |
| **F-5** | **待機プロセスがセッションと一緒に死んだ（実際に 1 回失われた）** | AI 側の復帰は**未検証**。取りこぼしなく追いつけるかは「`sinceRevision` の設計上そうなるはず、というだけ」 | **O-10 として残っている**（§1-1） | `TR:171`／`OI:557-558` |
| **F-6** | **Undo を作ったのに一度も押されなかった** | 両者の手を 1 本の履歴スタックに載せ、画面にボタンも出したが、**このセッションでは一度も押されなかった**＝**実装済み・未検証** | 在庫 `A02:244`（O-8）と一致 | `TR:166` |
| **F-7** | **エスケープ無しで埋め込むと本文へ漏れた（対照実験）** | `Unterminated string in JSON at position 76` で読めず、さらに **`bold` がページ本文に出た** | **エスケープ規約は必須。埋め込みはテンプレート処理である** | `OI:143-152` |
| **F-8** | **測る前に決めていたら、黙って上書きし合う設計になっていた** | 保管庫の共有を測ったことで、**起動順序より先にキー設計が決まった** | **2 窓同時は想定ではなく構成上の現実** | `TR:334-335`／`OI:238-239` |
| **F-9** | **前の版に、測っていないことを「測れず」と書いていた** | `TR:167` が自ら是正を記録している ——「⚠️ **旧版はここで『人間が開いている画面に外から入れるかは測れず』と書いていたが、§7-4 で測って成立を確認した**（決着-7。2026-08-04 に本行を是正）」 | **やっていないことを、やったように書かない**（`TR:162`） | `TR:162`／`TR:167` |

### 6-1. 確かめられなかったこと（`TR:164-171`。**6 行のうち 1 行は後に決着**）

| 項目 | 状態 | 出典 |
|---|---|---|
| **Undo（取り消し）** | **実装済み・未検証**（一度も押されなかった） | `TR:166` |
| `file://` 直開きでの動作 | **決着済み**（§7 の 6 回の追測。旧版の記述は 2026-08-04 に是正） | `TR:167` |
| **多人数** | **未実施。** 2 者・認証なし・localhost 限定でしか動かしていない | `TR:168` |
| **同時更新の自動調停** | **未実装。**「後勝ちを禁止し、読み直して再試行」しかしていない | `TR:169` |
| **長時間・大文書** | **未計測。** 文書は小さく、セッションは 1 時間程度 | `TR:170` |
| **AI 側の復帰** | **未検証。** 待機プロセスはセッションと一緒に死ぬ | `TR:171` |

---

## 7. 廃棄・撤回された決定（**生きている決定と混ぜないこと**）

| # | 廃棄・撤回されたもの | 置き換え・現在の状態 | 出典 |
|:--:|---|---|---|
| **K-1** | **旧 `O-1` 〜 `O-3`・`O-5` 〜 `O-9` の「未決」という地位** | **`D1`〜`D6` は決定済み**（`OI:288-293` の表は 6 行とも取り消し線で書かれている）。**旧番号のまま引用すると未決に見える** | `OI:288-293` |
| **K-2** | **`O-2` 残件（人間が開いている画面に外から入れるか）＝「決定ではなく測定」の 1 件** | **0 件になった。** 決着-7 で成立を確認 | `OI:302-306` |
| **K-3** | **「A. まだ実測できていない」の全項目** | **0 件**（`OI:531`） | `OI:531-534` |
| **K-4** | **要求 `A-17`「非 ASCII のデータをシェル引数に載せない」** | **不採用（2026-08-04）。** 決定-1 が「成果物は当面 単一 `.html` だけ」と定めており、**シェル引数を持つ部品が存在しない**（YAGNI）。⚠️ **実測そのものは `TR:106-118` に残る。CLI を出す日が来たらそこを読む** | `agent-interface-requirements-ja.md:262-269`（自分で開いて確認） |
| **K-5** | **「後から切れるから安全」という設計** | **禁止。** 名前を消しても渡し済みの参照は動き続けた（決着-6 M4） | `OI:183-184`／`TR:289` |
| **K-6** | **クエリ（`?agentApi=1`）を既定の有効化手段にする案** | **既定にしない。** 成立はするが**ダブルクリック経路では付けられない** | `OI:186-188`／`OI:438-439` |
| **K-7** | **「サイレント破棄禁止」を追い越された自動保存にも適用する読み** | **適用範囲を狭めた。**「サイレント破棄禁止」は**破損**の規定であって**追い越された**場合の規定ではない（古いか同じ自動保存は**黙って捨てる**） | `OI:458` |
| **K-8** | **`O-4` を「競合の解消（自動マージ）」と呼ぶ言い方** | **廃止。** 正の側が「マージ」（取込の合流）と「衝突」（取込時の UID 衝突）を既に使っており、**二重に重なる**。`ConcurrentUpdate` / `BaseRevisionCheck` へ分離 | `OI:498-505` |
| **K-9** | **`Conflict` / `Collision` / `Resolution` / 裸の `Merge` / 裸の `Lock`** | **使わないと決めた語（5 つ）** | `OI:513-514` |
| **K-10** | **`optimistic locking` を名前に採る案** | **採らない**（理由は §5 の引用）。**散文で 1 行だけ添える** | `OI:507-511` |
| **K-11** | **`agentApiVersion` を保存 JSON に入れる案** | **入れない**（決定-3） | `OI:382` |
| **K-12** | **会話を丸ごと保存する案** | **保存しない。** 42 通を見返して、残す価値があったのは「**なぜその手を指したか**」と「**前の発言は間違いだった**」の **2 種類だけ** | `OI:356-358` |

> **意図的に直さなかった 2 箇所がある**（`OI:521-523`）——
> 決定-1 の理由「**純粋関数のほうが作りやすい。意図しない競合が起こらないから**」（`OI:325` と要求 `A-15`）。
> **ユーザーの判断の引用であり、語義も別**（コード同士の干渉であって同時更新ではない）。
> **記録された理由を後から書き換えない。** 次期が語彙検査を機械化するとき、**この 2 箇所を例外に入れること。**

---

## 8. 未決のまま残っている件（`OI` の 2 件に加えて、本担当で気づいたもの）

**§1-1 の 2 件（O-4 / O-10）と §1-2 の 5 件が `OI` 自身の残件である。**
下は**本 2 文書を全文読んだ結果、答えが書かれていないと確認できたもの**である。
**「無い」と言えるのは私が全文を読んだこの 2 文書についてだけで、他文書は未検証。**

| # | 未決 | なぜ設計に効くか | 出典（無いことの確認） |
|:--:|---|---|---|
| **U-1** | **チャットの発話は `revision` を進めるのか** | トライアルでは**進めた**（`TR:39` の 42 通）。しかし**そのときチャットは保存されていた**（`TR:156`）。**決定-2 で会話を保存しないと決めた以上、前例がそのまま効くとは言えない。** 本 2 文書はこの点を決めていない | `OI:340-374`（決定-2 に記述なし）／`TR:39` |
| **U-2** | **`documentId` の型と生成規則** | `OI:471-474` は「**要る理由と最小の形だけを書く**」と宣言するが、**`OI` 本文にその「最小の形」は書かれていない**（自動保存キーの綴り `grsched.autosave.<documentId>` だけ） | `OI:449`／`OI:471-474` |
| **U-3** | **ダウンロード名の命名規則** | 決着-3 は「文書 id ＋ `revision`」を**例として**挙げるだけ。**確定していない**のに、AI 側の「最新を読む」処理の前提になっている | `OI:105-109` |
| **U-4** | **決着-7 の前提（デバッグポート付き起動）を、実際の利用者にどう用意させるか** | **後から付けられない起動時フラグ**なので、運用手順が要る。**本 2 文書に手順の記述は無い** | `OI:212-213`／`TR:309-310` |
| **U-5** | **`watchChanges` の解除手段** | 本 2 文書には記述が無い。在庫 `A02:240` が同じことを別経路で指摘している | `OI` 全文／`TR` 全文に記述なし |
| **U-6** | **有効化に 115 秒かかった件の扱い** | 実測値だけがあり、**UI 設計上の含意は書かれていない**（初回起動案内に対応していた時間） | `OI:204` |

---

## 在庫表との食い違い

**⚠️ 最重要節。両側に `ファイル:行` を付けた。**

### A. 在庫の記述と、本担当の出典が食い違うもの

| # | 何が食い違うか | 在庫表の側（`file:line` ＋ 引用） | 出典の側（`file:line` ＋ 引用） | どちらが正か |
|:--:|---|---|---|---|
| **X-1** | **`file://` でライブの共同編集は成立しないのか** | `A02-agent-api-requirements.md:207`「`file://` では**ライブの共同編集は原理的に成立しない**ので、共同編集はサーバ前提である」（原典 `agent-interface-requirements-ja.md:255-256`「由来: `file://` では**ライブの共同編集は原理的に成立しない**（**2 つのビューアが状態を共有できない**）」。**私が原典を開いて確認した**） | `OI:222-245`（決着-8）**「`file://` のローカルファイルは、保管庫を 1 つしか持たない」——同じフォルダでも別フォルダでも localStorage も IndexedDB も見え、ブラウザを完全に落として再起動しても残っていた**／`TR:329-330`「**ローカルファイルはパスに関係なく 1 つの保管庫を共有する**」／`OI:190-216`（決着-7）**人間が開いている `file://` の画面へ外から入り、読み・書き・`stale-base-revision` による拒否まで通した** | **理由の部分は実測に負けている。**「2 つのビューアが**状態を共有できない**」は決着-8 と正面から食い違う（**共有してしまうことが問題だった**）。決着-7 では**人間と外部プロセスが同じ `file://` ページで実際に共同編集した**。⚠️ **ただし「タブ間のライブ通知（`storage` イベント等）が届くか」は本 2 文書のどこでも測っていない（未検証）。** 結論（窓口は薄い包みにする）は残してよいが、**根拠の文は書き換えが要る。** ⚠️ **原典の記述日と決着-7/8 の前後関係は未検証**（同じ節の隣に 2026-08-04 の不採用注記がある） |
| **X-2** | **`O-` 番号が 2 系統あり、同じ番号が別物を指す** | `A02-agent-api-requirements.md:240`「**`O-4`** `watchChanges` の引数・返り値・解除の手段が未規定」／同 `:246`「**`O-10`** `A-6` / `A-7` / `A-8` / `A-9` / `A-16` は『提案』であって実測ではない」 | `OI:544`「**`O-4`. 同時更新の方針**」／`OI:553`「**`O-10`. エージェント側の寿命と追いつき**」 | **どちらも自分の中では正しく、番号だけが衝突している。** `A02` の `O-n` は**在庫作成時に振った通し番号**、`OI` の `O-n` は**前プロジェクトの原番号**である。**合流すると `O-4` と `O-10` が二重定義になる。** 次期は**片方を必ず振り直すこと**（原典 `agent-interface-requirements-ja.md:17` も「`A-n` は前プロジェクト内だけの通し番号。次期は自分の体系で振り直す」と述べている＝`A02:251` O-15） |
| **X-3** | **「他エンジン未測定」の範囲** | `A07-security-a11y.md:174`「**`O-5` `file://` の保管庫共有は他エンジン未測定。** Playwright Chromium 1228 の実測のみ。…**基準ブラウザは Chrome 150 系**」（＝**保管庫の件だけ**が未測定に見える） | `OI:33-35`「⚠️ **素の Chrome では未測定である。** Edge も Playwright の Chromium もエンジンは Chromium だが、**推測を断定で書かない**」／`OI:36`「**なお `fetch` の遮断（決着-2）だけは 2 系統の両方で同じ結果が出ている**」 | **出典の側が正しく、範囲は広い。** 未測定なのは保管庫（決着-8）だけではなく、**8 件のうち 7 件**（決着-2 以外の全部。**自分で数えた**）が**基準ブラウザで未測定**である。`A07` は自分の担当範囲で書いたので誤りではないが、**合流時に「未測定は保管庫だけ」と読めてしまう。** 対応ブラウザ確定時に**8 件まとめて測り直すこと**（`OI:35`／`OI:249-250`） |
| **X-4** | **`documentId` を待っている機能の数** | `A07-security-a11y.md:187`「**2 つの機能**が同じ識別子を要求している」（自動保存キー ＋ `Agent API` 有効化の記録）／`A06-readme-nextsteps.md:112`「効く先は **3 つ** —— 自動保存のキー／起動時の 4 分岐の判定／埋込文書つき GRS のファイル名」 | `OI` からは **4 つ**数えられる（**自分で数えた**）: ①自動保存のキー `OI:449` ②起動時の 4 分岐の「同じ文書か別の文書か」の判定 `OI:455-459` ③**AI 有効化の記憶（文書ごと）** `OI:451` ④決定的なダウンロード名 `OI:106-107` | **`A06` の 3 つが最も近いが、それでも 1 つ足りない**（`A06` は③を挙げていない）。**`A07` の「2 つ」は最少である。** 次期は **4 用途**で `documentId` を設計すること。⚠️ ④は `OI:106` が「例」と書くので**用途としては確定していない**（→ U-3） |
| **X-5** | **チャット発話が版を進めるかの衝突（原因が特定できた）** | `A02-agent-api-requirements.md:264`（X-3）「仕様書は `AG-11`（**確定した発話は版数を上げる**）を採ったが、**覆した記録がどちらの側にも無い（未検証）**」／同 `:272`（Y-1）「**同じサンプル束の中で逆になっている**」 | `TR:39`「最終 `revision` **104** ＝ 盤面の更新 **62** ＋ **会話の投稿 42**。**両方が同じ 1 本の連番を進める**」／`TR:156`「上の 7 件は、**すべて盤の上のチャットに残っていて、あとから読み返せる**」／`OI:347`「**会話は保存しない。**『どの `revision` で・誰が・なぜ変えたか』＝ `changeLog` だけ保存する」 | **食い違いそのものは在庫の指摘どおり残る。本担当が足せるのは由来である** —— **`AG-11` はトライアルの実測（会話が版を進める）から来ている。ただしトライアルでは会話が保存されていた。** 決定-2 が保存をやめた時点で前提が変わっており、**`AG-11` が引き継いだのは「保存されない発話も版を進める」という別物になっている。** ⚠️ **その差を書いた記述は本 2 文書に無い（未検証）。→ U-1 として設計で決着させること** |

### B. 在庫が「**未検証 —— 同書は未読**」と記した点に、本担当が答えを持つもの

**`A02` は `OI` と `TR` を読まずに書かれている**（`A02:218` / `:225` / `:226` が明記）。
**下の 3 件は、今回の全文読みで確定した。**

| # | 在庫の記述 | 本担当の答え（実在を確認した行） |
|:--:|---|---|
| **B-1** | `A02:218`「`A-17` の実測は `ai-cowork-trial-findings-ja.md` §3-2 に残る（**未検証** —— 同書は未読）」 | **実在する。** `TR:106-118` に症状・切り分け・対処・規約が揃っている（**コンソールのコードページが、コマンドに渡る前にバイト列を壊していた**）。**要求としては不採用だが、実測は使える**（→ K-4） |
| **B-2** | `A02:225`「ライブラリ／CLI は当面出さない（`agent-interface-open-items-ja.md` 決定-1。**未検証** —— 同書は未読）」 | **実在する。** `OI:315-323`（決定-1・2026-08-02・決めた人はユーザー）。**補強材料 3 つ**（決着-4 で CLI の必要性が下がった／Undo の不変更新スナップショットと整合／純粋性は後から取り戻せない）も `OI:327-335` にある |
| **B-3** | `A02:226`「`agentApiVersion` を保存 JSON に入れない（同 決定-3。**未検証**）」 | **実在する。** `OI:375-382`。理由は「**入れると版が上がるたびに全ファイルの diff が出る**」（`OI:391-392`） |
| **B-4** | `A02:244`「`A-4`（Undo）は未検証。トライアルで実装したが押されなかった」 | **裏取り完了。** `TR:166`「**実装済み・未検証。** 両者の手を 1 本の履歴スタックに載せ、画面にボタンも出したが、**このセッションでは一度も押されなかった**」 |
| **B-5** | `A02:245`「『落ちて復帰しても `sinceRevision` から追いつける』は未検証」 | **裏取り完了。** `TR:171`／`OI:557-558`（**待機プロセスは実際に 1 回失われた**）。`OI:553-560` が **O-10** として正式に未決へ載せている |

### C. 在庫が落としている条件（食い違いではないが、片側にしか無い）

| # | 在庫の側 | 本担当が足すもの |
|:--:|---|---|
| **C-1** | `E09-settings-blob.md:318`「`Agent API` を有効にした文書の記録 … **非保存・`localStorage`**。文書の識別子と対にして環境側へ置く。**設定値としての鍵名が無い**」 | `OI:451`「**文書ごとに記憶する。ただし有効な間は画面に常時表示を出す**」——**常時表示は記憶とセットの条件**である。`A06-readme-nextsteps.md:154-155` は拾っているが、`E09` の行だけを見ると落ちる |
| **C-2** | `A07-security-a11y.md:39` / `:187`（`documentId` の受け皿がデータモデルに無い） | `OI:471-474` が**手続きまで決めている** ——「**正への追加は組込み時に change-manager を通す。ドラフトの側から正へ書き足さない**」。**次期が `documentId` を足すときの経路が既に指定されている** |
| **C-3** | `A07-security-a11y.md:132`（X-5・共有 PC のプライバシ注記が弱い） | `OI:242-245` が**言い切っている** ——「**任意のローカル HTML ページが GRS の自動保存を読める。**『同一オリジン JS から平文で読める』は、`file://` では**『そのマシンの全ローカルファイル』**を意味する。**`http(s)` で配信すれば本物のオリジンになり、この問題は消える**」 |

