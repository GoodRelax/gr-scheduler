# A05 — user-order（利用者の指示）

**担当**: `previous-project-result/user-order.md` の全文（637 行）から、設計に効く決定・禁止・未決を拾う。
**位置づけ**: 本書が扱うのは**要求の源泉**である。他のどの文書より重い。
**推測は断定で書かない。** 確かめられないことは「**未検証**」と明記した。**数は自分で数えた**（数え方は §1）。

---

## 0. 読んだ文書と行数

| # | ファイル | 行数 | 読んだ範囲 | 本書での役割 |
|:--:|---|--:|---|---|
| 1 | `previous-project-result/user-order.md` | **637** | **全文（1–637）** | **本書の担当。要求の源泉** |
| 2 | `docs/spec/01-04-requirements.md` | 3728 | **部分**（表 T-011 = 413–465／`ST-6` `ST-7` = 1187–1188／`HR-1a` = 1268／`FR-007` 周辺 = 1374–1400／`FR-015` = 2023–2028／`FR-046` = 1945–1953／表 T-023a = 2060–2070／`FR-062` = 2776／`FR-024` = 2795／`FR-063` = 3106–3124／`RC-6` = 3728、および §9 の grep 結果） | 受けの確認。**全文は読んでいない** |
| 3 | `docs/spec/_assets/tbl-settings.md` | 332 | **部分**（`S-35` = 83／`S-81` = 180／`S-100` = 243） | 値の正。突合用 |
| 4 | `docs/spec/_assets/tbl-glossary.md` | 259 | **部分**（表 T-101 = 20–29） | 名前の正。突合用 |
| 5 | `previous-project-result/temp/inventory/E01-task-plan.md` | 132 | **全文** | 在庫表との突合 |
| 6 | `previous-project-result/temp/inventory/E02-task-actual.md` | 314 | **全文** | 同上 |
| 7 | `previous-project-result/temp/inventory/E03-dependency-taskgroup.md` | 159 | **全文** | 同上 |
| 8 | `previous-project-result/temp/inventory/E04-project.md` | 200 | **全文** | 同上 |
| 9 | `previous-project-result/temp/inventory/E05-calendar.md` | 251 | **全文** | 同上 |
| 10 | `previous-project-result/temp/inventory/E06-resource-assignment.md` | 395 | **部分**（1–233 と 363–395。234–362 は `Resource` / `Assignment` の Carry 全数表で、user-order に対応する項が無い） | 同上 |
| 11 | `previous-project-result/temp/inventory/E07-visual-origin.md` | 186 | **全文** | 同上 |
| 12 | `previous-project-result/temp/inventory/E08-comment-highlight.md` | 232 | **全文** | 同上 |
| 13 | `previous-project-result/temp/inventory/E09-settings-blob.md` | 577 | **全文** | 同上 |
| 14 | `previous-project-result/temp/inventory/E10-carry-roundtrip.md` | 422 | **部分**（1–120 と 222–251 と grep で拾った 209/215/365/366/398 行） | 同上 |
| 15 | `previous-project-result/temp/inventory/E11-identity-and-notstored.md` | 522 | **全文** | 同上 |
| 16 | `previous-project-result/10-agent-interface/samples/grs-document-with-revision-stamp.json` | 96 | **1–20 行のみ**（`revisionStamp` の実在確認） | 在庫表どうしの食い違い（X-11）の裏取り |

**出典の書き方**: `ファイル名:行番号`。ファイル名は上表の basename を使う。
**行数は `wc -l` で自分で数えた。**

---

## 1. 数えたもの（自己申告）

| 数えたもの | 値 | 数え方 |
|---|--:|---|
| **項番（トップレベル）** | **69**（`1`〜`69`・欠番なく揃う） | `user-order.md` の行頭 `^[0-9]+\.` を機械抽出し、1〜69 を 1 つずつ突き合わせた |
| うち **欠番（取り下げ・廃止）** | **3**（項 11 / 項 14 / 項 52） | `user-order.md:140` / `:143` / `:395`。いずれも「（欠番）」と明記 |
| うち **生きている項** | **66** | 69 − 3。**仕様書 `01-04-requirements.md:436` の「要望の 66 項目」と一致する** |
| **入れ子の項（サブ項番）** | **23** | 6-1〜6-6（6）＋ 30-1〜30-8（8）＋ 55-0〜55-3（4）＋ 65-1〜65-5（5）。`user-order.md:111-116` / `:234-251` / `:431-438` / `:496-502` |
| うち **失効したサブ項** | **1**（項 30-4） | `user-order.md:238-243`。「失効 — 2026-08-02（ユーザー判断）。番号は繰り上げない」 |
| **番号を振らない決定**（「決着した仕様」） | **5** | `user-order.md:587` / `:594` / `:611` / `:620` / `:631` |
| **番号を振らない決定ブロック** | **2**（対象環境／アクセシビリティの適合範囲） | `user-order.md:547-564` / `:566-583`。`:550` が「ここは要望ではなく決定なので番号を振らない」と明記 |
| **やらないこと（MVP 非対象）** | **9** | `user-order.md:474-489` の箇条書きを数えた |
| **廃止した語** | **14** | `user-order.md:59-60` を数えた |
| **改名した語（対）** | **4** | `user-order.md:64-65` を数えた |
| **別紙（引継ぎ資産）の行** | **13** | `user-order.md:523-537` を数えた |
| **本書が拾った「やるな」** | **65** | §5 の表の行を数えた（§6 の 9 件は別表） |
| **表 T-011 が挙げる項番の和集合** | **69**（＝全数） | `01-04-requirements.md:444-459` の 15 行から項番を抽出し、1〜69 と突き合わせた。**取りこぼしは 0 件** |

> **「番号を振らない」は 3 種ある。** ①「決着した仕様」5 件（`:585-637`）②「対象環境」「a11y の適合範囲」の 2 ブロック（`:547-583`）③ 用語節・画面構成節・最優先事項の本文。
> **③ のうち最優先事項だけは 1〜5 の番号を持つ**（`:75-81`）ので、本書は項 1〜5 として数えた。

---

## 2. 指示の全数 — 項番 1〜69

**「状態」欄の凡例**: 生 = 生きている／欠 = 欠番／**改** = 仕様書が内容を変えた（§9 と §在庫表との食い違いを見よ）。

| 項 | 内容（一行） | 出典 | 状態 |
|:--:|---|---|:--:|
| 1 | ぬるサク。ズーム・パン・ドラッグが常に滑らか。**最優先** | `user-order.md:75` | 生 |
| 2 | 1 行に複数のマイルストーン/タスク（マルチバー）。**最大の差別化** | `user-order.md:78` | 生 |
| 3 | 全体日程を 1 画面でスクロールなしに見られる | `user-order.md:79` | 生 |
| 4 | 1 つの `.html` だけで動作（サーバー不要・完全オフライン） | `user-order.md:80` | 生 |
| 5 | WYSIWYG | `user-order.md:81` | 生 |
| 6 | スケジュール画面をいっぱいに使える（サブ 6 件） | `user-order.md:110-116` | 生 |
| 7 | 未選択の `Command Palette` は薄く透明表示 | `user-order.md:117` | 生 |
| 8 | 拡大しても `App Header` と `Row Title Panel` は常に表示 | `user-order.md:118` | 生 |
| 9 | `Row Title Panel` と `Schedule Canvas` は個別に連動スクロール | `user-order.md:119` | 生 |
| 10 | 既定のタスク形状（`shapeKind` 4 ＋ マイルストーン） | `user-order.md:125-139` | 生 |
| 11 | ~~絵文字~~ | `user-order.md:140` | **欠** |
| 12 | 枠の色と塗りの色を個別に選択 | `user-order.md:141` | 生 |
| 13 | 線の太さ（細/中/太）。色に頼らない識別手段として必須 | `user-order.md:142` | 生 |
| 14 | ~~アイコンのインポート（SVG / PNG）~~ | `user-order.md:143` | **欠** |
| 15 | コピー&ペースト | `user-order.md:144` | 生 |
| 16 | アイコンの移動に連動してプロパティが自動で変わる | `user-order.md:145` | 生 |
| 17 | `Task.name` をアイテムの内部/付近に表記。全角 12 / 半角 24 で打ち切り | `user-order.md:146-149` | 生 |
| 18 | 名称ラベルの位置を移動できる（9 点アンカー ＋ 整列） | `user-order.md:150-152` | 生 |
| 19 | `Task` にプロパティを設定できる | `user-order.md:158` | 生 |
| 20 | プロパティの全数表（15 行 ＋ 担当者） | `user-order.md:159-189` | 生 |
| 21 | プロパティの多国語対応（en / ja・UTF-8 BOM なし） | `user-order.md:191-196` | 生 |
| 22 | プロパティ編集の項目名は英語表記 | `user-order.md:197` | 生 |
| 23 | パレット色 11 色（透明を含む） | `user-order.md:211` | 生 |
| 24 | 色弱者対応として原色を使わない | `user-order.md:212` | 生 |
| 25 | 色以外の識別手段（線の太さ・形状） | `user-order.md:213` | 生 |
| 26 | `Task` を分類する水平線（Group Grid Lines） | `user-order.md:219` | 生 |
| 27 | `TaskGroup` は階層を持てる（推奨 3 段・最大 5 段） | `user-order.md:220-228` | 生 |
| 28 | 非表示の `TaskGroup` を親の小タブ（Hidden Group Tab）で再表示 | `user-order.md:229-231` | 生 |
| 29 | `Row Title Panel` を `Schedule Canvas` の左側に表示 | `user-order.md:232` | 生 |
| 30 | 1 行の中で時間が重なる `Task` は自動で縦に積む（サブ 8 件） | `user-order.md:233-251` | **改** |
| 31 | `Task` の位置を揃える（縦は自動・横は整列コマンド） | `user-order.md:252-256` | **改** |
| 32 | `Time Ruler` に年 / 月 / 日 / 曜日 | `user-order.md:262-263` | 生 |
| 33 | ズームで `Time Ruler` の粒度が自動で切り替わる（4 段階・しきい値 3 本） | `user-order.md:264-277` | 生 |
| 34 | 年単位から日単位まで表示 | `user-order.md:278` | 生 |
| 35 | 縦のみ / 横のみの拡大（異方性ズーム） | `user-order.md:279` | 生 |
| 36 | ズームで表示する `Task` が自動的に増減（LOD） | `user-order.md:280-287` | 生 |
| 37 | マウスとキーボードの割当（表 6 行） | `user-order.md:288-300` | **改** |
| 38 | 拡大したら上下左右にスクロール。マウス操作だけでも | `user-order.md:301` | 生 |
| 39 | 稼働日/非稼働日（土日・祝日）を区別して表示 | `user-order.md:302` | **改** |
| 40 | `Today Line`（本日線）を表示できる | `user-order.md:326` | **改** |
| 41 | `Dual Cursor`: 縦線 2 本の間の日数を測る | `user-order.md:327` | 生 |
| 42 | `Guide Cursor`: ポインタ追従の補助線（4 モード排他） | `user-order.md:328-329` | 生 |
| 43 | 本日線・各カーソルの表示/非表示 | `user-order.md:330-331` | 生 |
| 44 | コメント（Comment Boxes）。引出し四角と折れ線の 2 種 | `user-order.md:337-341` | 生 |
| 45 | 任意の色の角丸四角（Highlight Boxes）で囲う | `user-order.md:342-347` | **改** |
| 46 | `Task` の依存関係を表現 | `user-order.md:353` | 生 |
| 47 | 依存線を 9 点から引き出し／引き入れ | `user-order.md:354` | **改** |
| 48 | 依存線は自動で引く。折れ曲がり点 0〜4 | `user-order.md:355-359` | 生 |
| 49 | 依存の種別（FS / SS / FF / SF）とラグ | `user-order.md:360-362` | 生 |
| 50 | 予定と実績を表示/管理（同じ `Task` が実績を持つ・5 状態） | `user-order.md:368-392` | 生 |
| 51 | 予定のみ / 実績のみ / 両方の切替（両方 OFF は作らせない） | `user-order.md:393-394` | 生 |
| 52 | ~~重ね表示 / 上下分離表示の切替~~（**本文の決定は生きている**） | `user-order.md:395-405` | **欠** |
| 53 | イナズマ線（Progress Line）。頂点は段ごと・途切れさせない | `user-order.md:406-422` | 生 |
| 54 | 変更前の予定をグレーで表示（別ファイルとして重ねる） | `user-order.md:423-424` | 生 |
| 55 | 透かし（Watermark）。サブ 4 件（0〜3） | `user-order.md:430-441` | **改** |
| 56 | MSPDI XML の双方向 Import / Export。未編集往復で同じものが出る | `user-order.md:451-453` | **改** |
| 57 | JSON で Import / Export（AI 連携用の主データ形式） | `user-order.md:454-455` | 生 |
| 58 | 画面を SVG で出力（既定 1600×900・PNG は 1x/2x） | `user-order.md:456-460` | 生 |
| 59 | ファイル入出力による明示的な保存/読込 | `user-order.md:461` | 生 |
| 60 | localStorage への自動保存とクラッシュ復旧 | `user-order.md:462` | 生 |
| 61 | 複数の MSPDI を 1 つに合流（3 択・勝手に消さない） | `user-order.md:463-465` | 生 |
| 62 | 外部入力を信頼できない入力として厳格に検証（XXE・`innerHTML`） | `user-order.md:466-468` | 生 |
| 63 | 初期表示用のテンプレートを 1 つ。**新規に作る** | `user-order.md:469` | 生 |
| 64 | ショートカットキー | `user-order.md:470` | 生 |
| 65 | 将来の拡張（サブ 5 件）を後から足せる設計にする | `user-order.md:495-502` | 生 |
| 66 | 名は体を表す命名。1 概念 1 語 | `user-order.md:509-512` | 生 |
| 67 | 画面（GUI）とデータモデルを分離 | `user-order.md:513-514` | 生 |
| 68 | パワポ代替であると便利な機能を**提案すること** | `user-order.md:515` | 生 |
| 69 | 担当者を GRS 側で追加・編集・解除でき MSPDI へ書き戻す | `user-order.md:198-205` | 生 |

> **項番の位置は内容で決めてあり、番号順に並んでいない。** 項 69 は「プロパティ」節（`:198`）に置かれ、項 23 より前に現れる。
> **理由は `user-order.md:199` が明記している**（「項番は追加順に振る（1〜68 は動かさない）。置き場所は内容で決めたので番号が飛ぶ」）。

---

## 3. データモデルに効く決定

**在庫表**欄は、その決定を受けている在庫表の行を指す。「—」は在庫表に対応行が無いことを意味する。

| # | 決定 | 出典（user-order） | 在庫表 |
|:--:|---|---|---|
| M-1 | **`Task` が唯一の日程要素。バーもマイルストーンも `Task` で、`Task.milestone`（真偽値）で区別する** | `user-order.md:51` | `E01-task-plan.md:31` / `E02-task-actual.md:31` |
| M-2 | **`TaskGroup` が行の器。入れ子で階層（≤5 段）。`TaskGroupMember` が所属を持つ。両方 export しない** | `user-order.md:52-53` / `:56` | `E03-dependency-taskgroup.md:28` / `:67` / `:94` |
| M-3 | **WBS は `Task.wbs_parent_uid` が持ち MSPDI へ export する。マルチバーは `TaskGroup` で export しない ＝ 2 軸** | `user-order.md:55-56` | `E03-dependency-taskgroup.md:27-28` |
| M-4 | **追随は片方向**。左の階層移動は元データ（MSPDI）に反映するが、**バーを別の行へ移す操作は元データを変えない。この非対称が要** | `user-order.md:224-226` | `E03-dependency-taskgroup.md:30` / `E07-visual-origin.md:100` |
| M-5 | **階層を移動しても `UID` は保持する**（相手ツールで新規タスクにならないため） | `user-order.md:225` | `E11-identity-and-notstored.md:108` |
| M-6 | **`stackOrder` は 1 つの `TaskGroup` 内の縦の積み順。`null` = 自動** | `user-order.md:54` / `:245` | `E03-dependency-taskgroup.md:96` |
| M-7 | **テキスト列は `name` ＋ `notes` の 2 つだけ。`abbrev` / `fullName` / `description` / `remarks` を廃止** | `user-order.md:61` / `:147` / `:185-186` | `E01-task-plan.md:19` / `:28` / `:33` |
| M-8 | **表示だけ打ち切り、データは切らない**（全角 12 / 半角 24・超過はツールチップで全文） | `user-order.md:148-149` | `E01-task-plan.md:28` |
| M-9 | **名称ラベルの位置は 9 点アンカー ＋ 左詰め/中央/右詰め。既定は自動配置。ピクセル座標で持たない** | `user-order.md:151-152` | `E07-visual-origin.md:34-35` |
| M-10 | **プロパティの全数**（`name` / `notes` / `start` / `finish` / `actualStart` / `actualDuration` / `actualFinish` / `resume` / `resumeValid` / `percentComplete` / `deadline` / `shapeKind` / `strokeColor` / `fillColor` / `lineWeight` / `nameAnchor` / `nameAlign` / `fadeInDays` / `fadeOutDays` / `wbs_parent_uid`） | `user-order.md:161-178` | `E01`（予定側）／`E02:25-31`（実績側）／`E07:31-40`（視覚） |
| M-11 | **`actualDuration` が実績バーの長さそのもの。右端 = `actualStart` ＋ `actualDuration`（稼働日で加算）** | `user-order.md:167` / `:373-375` | `E02-task-actual.md:26` / `:44` |
| M-12 | **`actualFinish` は完了したときだけ入る。同じ `Task` が持つ（別アイテムにしない）** | `user-order.md:168` / `:369` | `E02-task-actual.md:27` |
| M-13 | **`resumeValid = false` が「再開日未定の中断（＝中止）」。中止に専用の概念を作らない** | `user-order.md:170` / `:387` | `E02-task-actual.md:29` / `:107` |
| M-14 | **`percentComplete` は整数 0 以上。`round(actualDuration ÷ (finish − start) × 100)` で算出して格納する。人が直接入力する値ではない** | `user-order.md:171` / `:377-381` | `E02-task-actual.md:30` / `:172` |
| M-15 | **`stop`（中断日）は保存しない。** export のときに算出する | `user-order.md:180-181` | `E02-task-actual.md:43` |
| M-16 | **拡張領域を使うのは `fadeInDays` / `fadeOutDays` の 2 つだけ** | `user-order.md:176` / `:183` | `E01-task-plan.md:35-36` / `E07-visual-origin.md:115` |
| M-17 | **色は指定しなければテーマ色。パレットから選べば上書き。透明も選んだ値。塗りと輪郭を同時に透明にはできない** | `user-order.md:174` | `E07-visual-origin.md:38-39` / `:92` |
| M-18 | **`Task` に担当者の列を持たない。担当は `Assignment` → `Resource.name` から導出する** | `user-order.md:178` / `:203` | `E06-resource-assignment.md:112-113` |
| M-19 | **編集できるのは担当者の名前と割当先だけ。資源の種類・費用資源・暦・割当率は編集しない** | `user-order.md:200-201` | `E06-resource-assignment.md:189` |
| M-20 | **どの言語で開くかは読む人の環境であり、文書には保存しない** | `user-order.md:194` | `E09-settings-blob.md:319` |
| M-21 | **文字コードは UTF-8・BOM なし**（JSON / MSPDI XML とも） | `user-order.md:195` | — |
| M-22 | **`TaskGroup` の階層は「大/中/小」という専用語を持たず、深さで表す。「セクション」は廃止** | `user-order.md:221` / `:223` | `E11-identity-and-notstored.md:430`（`sections[]` 却下） |
| M-23 | **積む順序は決定的**（`start` 昇順 → `finish` 降順 → `uid` 昇順）。重なり判定は**描画上の占有幅**（はみ出すラベルを含む） | `user-order.md:234-235` | `E11-identity-and-notstored.md:374` / `E03-dependency-taskgroup.md:96` |
| M-24 | **行の帯高は積み順の数で決まる。行高固定を前提にしてはならない** | `user-order.md:251` | `E03-dependency-taskgroup.md:74`（C-5 と衝突） |
| M-25 | **積む向き（上/下）は文書全体で 1 つ。既定は上** | `user-order.md:244` | `E09-settings-blob.md:249` |
| M-26 | **LOD の判定は WBS の階層の深さ。判定でだけ 5 で頭打ちにしてよい。書き戻す値は頭打ちにしない** | `user-order.md:281-285` | `E01-task-plan.md:26` / `:106` / `:108` |
| M-27 | **`TaskGroup` の階層も縦ズームで畳む。WBS とは別の仕組み** | `user-order.md:287` | `E03-dependency-taskgroup.md:28` |
| M-28 | **`Time Ruler` は 4 段階・しきい値は境目の数だけ 3 本。値は user-order に書かない** | `user-order.md:264-274` | `E09-settings-blob.md:291-293` / `:433`（配列を 3 キーへ分けた） |
| M-29 | **注記の位置は日付 ＋ 行。行は順番ではなく行の識別子で参照する** | `user-order.md:339` / `:344` | `E08-comment-highlight.md:99-100` / `E11-identity-and-notstored.md:431` |
| M-30 | **吹き出しのズレだけは画面上の距離（px）で持つ。角丸の R も拡大縮小で同じ大きさに見える** | `user-order.md:340` / `:343` | `E08-comment-highlight.md:111-112` |
| M-31 | **コメントと囲み枠は別のものとして持つ**（点を指す／範囲を囲む） | `user-order.md:346` | `E08-comment-highlight.md:88` |
| M-32 | **依存の経路は保存しない。人が手で調整しない** | `user-order.md:356` | `E03-dependency-taskgroup.md:55`（D-2）／`E11:354` |
| M-33 | **依存は種別（FS / SS / FF / SF）とラグを持つ** | `user-order.md:360` | `E03-dependency-taskgroup.md:44-45` |
| M-34 | **予実の重ね順は背面から「予定 → 実績 → 進捗マーカー → 依存線 → 名称ラベル」** | `user-order.md:398-400` | — |
| M-35 | **透かしの設定は文書に保存しない。開いた人の名前と日時で描く** | `user-order.md:431-433` | `E09-settings-blob.md:316` / `:394-401` |
| M-36 | **出力サイズと文字サイズは文書に保存する**（同じ JSON から同じ出力） | `user-order.md:458` | `E09-settings-blob.md:261` / `:282-285` |
| M-37 | **未編集で往復したら元と同じものが出る。GRS が使わない項目もそのまま持ち回って書き戻す** | `user-order.md:452-453` | `E10-carry-roundtrip.md:26` / `:228-234` |
| M-38 | **合流は 3 択（上書き / 別のものとして取り込む / 取込をやめる）。勝手にタスクを消さない** | `user-order.md:464-465` | `E11-identity-and-notstored.md:297-298` / `:313` |
| M-39 | **変更前の予定は別ファイルとして読み込んで重ねる** | `user-order.md:424` | `E01-task-plan.md:96`（`Baseline` は Carry・一級エンティティにしない） |
| M-40 | **実績は予定と同じ形状で描く。幅がある形状は内側、幅がない形状は下、マイルストーンは実績日へ横** | `user-order.md:135-138` | `E02-task-actual.md:193-197` / `E07-visual-origin.md:179` |

---

## 4. アーキテクチャに効く決定

| # | 決定 | 出典（user-order） | 補足 |
|:--:|---|---|---|
| A-1 | **1 つの `.html` だけで動作する**（サーバー不要・完全オフライン） | `user-order.md:80` | 最優先事項 4 |
| A-2 | **画面（GUI）とデータモデルを分離する。** 前プロジェクトで「バグが止まらない」根因が語彙の混在だった | `user-order.md:513-514` / `:36` | 命名規約（項 66）と対 |
| A-3 | **UI とデータの名が食い違ったら UI の名前を変える**（データ側に合わせる） | `user-order.md:512` | 依存の向きを一方向に固定する規則 |
| A-4 | **用語の正は 4 文書だけ**（`ui-parts-ja.md` / `grs-native-erd-ja.md` / `plan-actual-decisions-ja.md` / `grs-document-settings-ja.md`）。**3 は 1・2 の予実まわりを上書きする** | `user-order.md:38-45` | 在庫表 11 枚の「正」の順序はここが根拠 |
| A-5 | **画面は上部 / 左 / 中央 / 右の 4 部構成 ＋ 浮遊 2（パレット・モーダル）** | `user-order.md:87-97` | `Canvas Overlays` は 5 種（`:99`） |
| A-6 | **`Time Ruler` は `App Header` と別物**。横スクロール・横ズームに追従し縦スクロールでは動かない | `user-order.md:102` / `:263` | 層の分離の具体 |
| A-7 | **レイアウトの計算順序は 4 段の 1 パス**（制約 → ラベル幅概算 → 積み順割当 → 依存線の経路）。**「依存線が必要とする余地」と「経路そのもの」を分けるのが要点** | `user-order.md:620-630` | 循環を切るための層の順序 |
| A-8 | **依存線とラベルの干渉は計算しない**（やりすぎでバグの元） | `user-order.md:630` | 上と対 |
| A-9 | **ラベル幅は概算する。実測もキャッシュもしない**（実測は重く、キャッシュは古い値が残る事故を招く） | `user-order.md:609-610` | `E11-identity-and-notstored.md:376` |
| A-10 | **骨格の段階で性能を実測してから機能を積む**（手戻り削減）。**ポイントとなる UI は実装前に PoC を回して利用者と認識合わせを行う** | `user-order.md:77` / `:631-637` | PoC では **UI と性能を同時に測る**（`:633`） |
| A-11 | **JSON が AI 連携用の主データ形式**。JSON を渡せば GRS 同士で同じ見た目が再現される | `user-order.md:454-455` | ⚠️ **user-order にある「AI」はこの 1 項と `Modals` の `AI Export`（`:97`）の 2 か所だけ**（全文を読んで数えた） |
| A-12 | **外部から読み込むファイルは信頼できない入力**。対象は JSON と MSPDI XML だけ（項 14 の取り下げで SVG / PNG の取込が消え攻撃面が減った） | `user-order.md:466-467` / `:480` | |
| A-13 | **将来の拡張の余地だけ残す**（共同編集・`TaskGroup` 単位の公開/編集権限・複数 PJ 統合・サブ PJ・一覧モード） | `user-order.md:495-502` | サーバー通信・認証は「やらないこと」だが**設計だけ残す**（`:489`） |
| A-14 | **対応ブラウザは Chromium 系（Chrome / Edge）が基準。Firefox は動作確認。Safari は対象外。OS は問わない。タッチ・モバイルは対象外** | `user-order.md:552-556` | 確定 2026-08-02 |
| A-15 | **「WCAG 2.1 AA 適合」とは名乗らない。** AA を指針とし視覚と操作に絞る（全 50 基準のうち 31 件を対象、読み上げ 5 件は対象外、該当なし 14 件） | `user-order.md:566-581` | 条文に部分適合が無い（5.2.1「met in full」）ため |
| A-16 | **CSP の `img-src` は `data:` で決着** | `user-order.md:582-583` | 正は `05-security-a11y/security-design.md` §4 |
| A-17 | **状態を変える入口は Progress Marker 1 つ**（クリックで 未完了 → 完了 → 中断 → 未完了 と巡る） | `user-order.md:390-391` | `E02-task-actual.md:47` |
| A-18 | **既存コードは参考のみ・コピペ禁止** | `user-order.md:536` | 別紙 `04-performance/performance-notes-ja.md` の扱い |

---

## 5. 利用者が「やるな」と言ったこと（**65 件**・全数）

**⚠️ この節が本書で最も重い。** 明示的な禁止・非採用・「〜しない」の全数である。

| # | 禁止・非採用 | 出典 |
|:--:|---|---|
| D-1 | **項番を変えない。** 取り下げた項目は**欠番として残す**（他文書が項番で参照しているため） | `user-order.md:16` |
| D-2 | **廃止した語 14 を次期で使わない**（`item` / `itemKind` / `lane` / `laneIndex` / `section` / `activity` / `ribbon` / `classification` / `abbrev` / `fullName` / `description` / `remarks` / `progressStatus` / `importance`） | `user-order.md:59-60` |
| D-3 | **両端を縦棒で示す細線 `\|---\|` は廃止。** `*----*` に置き換える | `user-order.md:139` |
| D-4 | **略称（`abbrev`）を廃止。** MSPDI に `Name` と `Notes` しかないのでテキスト列を増やさない | `user-order.md:147` |
| D-5 | **データは切らない**（切ると往復無損失＝項 56 が壊れる）。表示だけ打ち切る | `user-order.md:149` |
| D-6 | **名称ラベルの位置をピクセル座標で持たない**（縦横独立ズームでずれる） | `user-order.md:152` |
| D-7 | **塗りと輪郭を同時に透明にはできない** | `user-order.md:174` |
| D-8 | **`stop`（中断日）は保存しない** | `user-order.md:180` |
| D-9 | **`fullName` / `description` / `remarks` の 3 語をやめる**（テキスト列を 5 つ持つと往復で必ず溢れる） | `user-order.md:185-186` |
| D-10 | **`progressStatus` と `importance` は廃止した** | `user-order.md:182` |
| D-11 | **対象言語は en / ja の 2 言語。それ以上は広げない** | `user-order.md:192` |
| D-12 | **「ロケール」は位置を連想させるので使わない**（項目名は `language`） | `user-order.md:193` |
| D-13 | **どの言語で開くかは文書に保存しない** | `user-order.md:194` |
| D-14 | **「Excel 互換のため BOM を付ける」実装を入れてはならない**（MSPDI が壊れる） | `user-order.md:196` |
| D-15 | **資源の種類・費用資源かどうか・暦・割当率は編集しない**（資源管理は引き続き対象外） | `user-order.md:201` |
| D-16 | **`Task` に担当者の列は持たない** | `user-order.md:203` |
| D-17 | **色弱者対応として原色を使わない** | `user-order.md:212` |
| D-18 | **色だけで状態を伝えてはならない**（WCAG 2.1 AA / 1.4.1） | `user-order.md:213` |
| D-19 | **「セクション」という別概念は廃止した**（`TaskGroup` の階層が兼ねる） | `user-order.md:223` |
| D-20 | **階層を移動しても `UID` は保持する**（相手ツールで新規タスクにしない） | `user-order.md:225` |
| D-21 | **バーを別の行へ移す操作（マルチバー化）は元データを変えない** | `user-order.md:226` |
| D-22 | **非表示が増えても水平線の幅を太くしない** | `user-order.md:231` |
| D-23 | **積む順序が決定的でないと再描画のたびに段が入れ替わる**（＝非決定的な順序を採らない） | `user-order.md:236` |
| D-24 | **項 30-4 は失効（2026-08-02 ユーザー判断）。番号は繰り上げない** | `user-order.md:238-239` |
| D-25 | **マイルストーンを段割当で特別扱いしない** | `user-order.md:240` |
| D-26 | **「最終段を再利用する」（重なりを許して同じ段に押し込む）挙動は廃止した。勝手に重ねてはならない** | `user-order.md:249-250` |
| D-27 | **行高固定を前提にしてはならない** | `user-order.md:251` |
| D-28 | **自動で揃うのは段割当の結果までである**（同種のマイルストーンの縦揃えは人がやる） | `user-order.md:254-255` |
| D-29 | **しきい値 3 本の値をここ（user-order）には書かない**（2026-08-02 に決め直したとき旧値が残っていた） | `user-order.md:274` |
| D-30 | **粒度は単調であること**（拡大すると細かくなる一方。粗くなる逆転を起こさない） | `user-order.md:276` |
| D-31 | **`importance`（重要度）という専用の属性は廃止した。人が付ける値が要らなくなる** | `user-order.md:283` |
| D-32 | **書き戻す値は頭打ちにしない**（階層が壊れる） | `user-order.md:285` |
| D-33 | **縮小したのに表示が増える逆転を起こさない** | `user-order.md:286` |
| D-34 | **素のホイールはスクロール**（「素のホイール＝ズーム」を採らない） | `user-order.md:288` |
| D-35 | **アイテムの上での素の左ドラッグはパンではない**（アイテムを動かす操作） | `user-order.md:298` |
| D-36 | **パンは等倍。倍率を掛けない** | `user-order.md:300` |
| D-37 | **ガイドが無効なときはポインタ移動で再描画しない** | `user-order.md:331` |
| D-38 | **注記の行は順番ではなく行の識別子で参照する**（並べ替えで別の行を指す事故を防ぐ） | `user-order.md:339` / `:344` |
| D-39 | **指している行が畳まれた/非表示になったら、コメントも一緒に隠す** | `user-order.md:341` |
| D-40 | **コメントと囲み枠は別のものとして持つ**（1 つの型に統合しない） | `user-order.md:346` |
| D-41 | **依存線の経路は完全自動。人が手で調整しない。経路を保存もしない** | `user-order.md:356` |
| D-42 | **仕様に「0〜3」と書いてはならない**（折れ点の上限は非対称で、日程が重なる場合は 4 が必要） | `user-order.md:359` |
| D-43 | **ラグは日付に効くので経路の規則を増やさない** | `user-order.md:362` |
| D-44 | **予定と実績を別アイテム・別行にはしない** | `user-order.md:369` |
| D-45 | **実績バーの両端は人が置いた日付であり、勝手には動かない** | `user-order.md:370` |
| D-46 | **完了率は人が直接入力する値ではない** | `user-order.md:377` |
| D-47 | **5 状態から外れる入力はできないようにする** | `user-order.md:383` |
| D-48 | **完了にしても実績バーの右端は 1 日も動かない** | `user-order.md:386` |
| D-49 | **「中止」に専用の概念を作らない**（中断・再開日未定と同じもの） | `user-order.md:387` |
| D-50 | **基準日を動かしても、バー・日付・完了率のどれも動かない** | `user-order.md:388` |
| D-51 | **予実トグルの「両方 OFF」は作らせない** | `user-order.md:394` |
| D-52 | **実績バーがラベルを覆ってはならない** | `user-order.md:399` |
| D-53 | **完了したタスクを尖らせない**（日本の作図慣行をそのまま実装しない） | `user-order.md:418-420` |
| D-54 | **基準日の縦線は常時表示にしない** | `user-order.md:421` |
| D-55 | **透かしの設定は文書に保存しない**（作者の名前が他人の画面に出るのは誤り） | `user-order.md:431-432` |
| D-56 | **生パスワードはコード・モデル・出力（HTML / JSON）に一切保存しない** | `user-order.md:439` |
| D-57 | **勝手にタスクを消さない**（取込側に無かったタスクは残し、知らせるだけ） | `user-order.md:465` |
| D-58 | **XML 外部エンティティ（XXE）を無効化する。`innerHTML` への直挿しを禁止する** | `user-order.md:468` |
| D-59 | **前プロジェクトのテンプレートは使わない**（廃止済みモデルで書かれている）。新規に作る | `user-order.md:469` |
| D-60 | **`type` / `data` / `info` / `value` のような無意味な汎用語は使わない** | `user-order.md:510` |
| D-61 | **1 つの概念には 1 つの語しか与えない。同じものを画面側とデータ側で別の名前で呼ばない** | `user-order.md:511` |
| D-62 | **既存コードは参考のみ・コピペ禁止** | `user-order.md:536` |
| D-63 | **Safari は対象外。タッチ・モバイルは対象外。WCAG 2.5.5（AAA）は AA 適合判定に数えない** | `user-order.md:554-560` |
| D-64 | **「WCAG 2.1 AA 適合」とは名乗らない。レベルを A に下げる案は採らない（逆効果である）** | `user-order.md:568` / `:579` |
| D-65 | **性能の基準機・基準ブラウザ、a11y の全数を user-order に複製しない**（正は別紙） | `user-order.md:563-564` / `:581` |

---

## 6. やらないこと（MVP 非対象・**9 件**）

**「意識的に作らないと決めたもの。次期でも原則として広げない」**（`user-order.md:476`）。

| # | 対象外 | 出典 | 但し書き |
|:--:|---|---|---|
| N-1 | 絵文字アイコンの対応 | `user-order.md:478` | 旧項 11・本レビューで取り下げ |
| N-2 | アイコンのインポート（SVG / PNG） | `user-order.md:479` | 旧項 14。**これで取込サニタイズが不要になり攻撃面が減った** |
| N-3 | 資源管理（工数・割当率・稼働率） | `user-order.md:481` | **担当者の表示と割当の追加・編集・解除は行う**（項 69） |
| N-4 | コスト管理（金額・単価・予算） | `user-order.md:484` | |
| N-5 | EVM / 出来高管理 | `user-order.md:485` | |
| N-6 | 資源平準化 | `user-order.md:486` | |
| N-7 | クリティカルパス計算・スケジューラ | `user-order.md:487` | **日付は人が決める** |
| N-8 | カスタムフィールドの汎用機構 | `user-order.md:488` | ただし内部的に拡張領域を使う場面はある（＝`fadeInDays` / `fadeOutDays`） |
| N-9 | サーバー通信・認証 | `user-order.md:489` | **将来拡張として設計だけ残す** |

---

## 7. 廃棄・撤回された決定（**生きている決定と混ぜない**）

| # | 廃棄・撤回されたもの | いつ・誰が | 出典 | 生きている置き換え |
|:--:|---|---|---|---|
| R-1 | **項 11 絵文字アイコン** | 本レビューで取り下げ | `user-order.md:140` / `:478` | 無し（やらないこと） |
| R-2 | **項 14 アイコンのインポート（SVG / PNG）** | 本レビューで取り下げ | `user-order.md:143` / `:479` | 無し（やらないこと） |
| R-3 | **項 52 予実の描き方をモードで切り替える（重ね/上下分離）** | 廃止 | `user-order.md:395-397` | **予定バーの高さを実績バーより大きくして幾何的に解く**（`:397`）。`planActualStyle` も廃止（`E07-visual-origin.md:54`） |
| R-4 | **項 30-4 最上段にマイルストーンを置く** | **失効 2026-08-02（ユーザー判断）** | `user-order.md:238-243` | 人が専用の行を作るか `stackOrder` を指定する |
| R-5 | **細線 `\|---\|`（両端を縦棒）** | 廃止 | `user-order.md:139` | `*----*`（`endpointSpan`） |
| R-6 | **`iconShapeKind`** | 改名 | `user-order.md:64` | **`shapeKind`** |
| R-7 | **`progressRatio`** | 改名 | `user-order.md:64` | **`percentComplete`**（整数 0 以上） |
| R-8 | **`Cursor Guides` / `Cursor Guide`**（単複しか違わない同名） | 改名（項 42） | `user-order.md:65` / `:319-320` | **`Cursors`** / **`Guide Cursor`** |
| R-9 | **`progressStatus`（自由文字列）** | 廃止 | `user-order.md:60` / `:62` | 状態が `actualFinish` / `resume` / `resumeValid` で構造化された |
| R-10 | **`importance`（重要度）** | 廃止 | `user-order.md:60` / `:62` / `:283` | LOD は WBS の階層の深さで判定 |
| R-11 | **`abbrev`（略称）・`fullName` / `description` / `remarks`** | 廃止 | `user-order.md:60` / `:185` | `name` ＋ `notes` の 2 列 |
| R-12 | **「セクション」概念** | 廃止 | `user-order.md:223` | `TaskGroup` の階層 |
| R-13 | **「最終段を再利用する」挙動** | 廃止 | `user-order.md:249` | 段は必要なだけ広げる（安全弁あり） |
| R-14 | **素のホイール＝ズーム** | 前プロジェクトで変更済み | `user-order.md:288` | 素のホイールは縦スクロール |
| R-15 | **依存線の手動経路調整** | 前プロジェクトで判断済み | `user-order.md:356` | 完全自動・経路は保存しない |
| R-16 | **依存線をバーの背面に置く** | **確定 2026-08-02** で前面へ | `user-order.md:400` | 交差部分が消えて線が途切れて見えるため |
| R-17 | **イナズマ線「最も進んだ頂点が勝つ」** | **確定 2026-08-01** で反転 | `user-order.md:415-417` | **最も遅れた頂点（最も左）が勝つ**。行内の致命的な遅れが隠れるため |
| R-18 | **マイルストーンの実績を予定の真下に積む** | **確定 2026-08-01** | `user-order.md:404` | 実績日の位置へ**横**にずらす |
| R-19 | **前プロジェクトのテンプレート** | 廃止済みモデルで書かれている | `user-order.md:469` | 新規に作る |

> ⚠️ **項 52 は「欠番」だが、その本文の決定は生きている。** 重ね順（`:398`）・依存線を前面（`:400`）・幅がない形状の局所分離（`:402-403`）・マイルストーンの横ずらし（`:404`）はいずれも生きた決定である。
> 仕様書も同じ扱いをしている（`01-04-requirements.md:448` の `V-5`「モード切替のみ取り下げ。重ね順と局所分離は `FR-011` と表 T-020 が受ける」）。

---

## 8. 未決のまま残っている件

| # | 未決の内容 | 出典（user-order） | 在庫表・仕様書側の状態 |
|:--:|---|---|---|
| O-1 | **書き出した担当者を MS Project がどう扱うか**（`OPEN-ITEMS-ja.md` 4） | `user-order.md:205` | `E06-resource-assignment.md:390`（#8。①表示されるか ②`Units`/`Work` を勝手に埋めるか ③埋めたら `Task` の `Duration`/`Work` が変わるか。**未検証・優先度 中**） |
| O-2 | **パレット 11 色の具体値**（「原色を使わない」としか決まっていない） | `user-order.md:211-212` | 仕様書は色名のみ（`01-04-requirements.md:1390`）、係数は Chapter 5 送り（`:1395`）。在庫表は値域 3 種のみ（`E07-visual-origin.md:94`） |
| O-3 | **`lineWeight`（細/中/太）が何 px になるか** | `user-order.md:142` | `E07-visual-origin.md:135` / `:176`（U-5「設定値の表・キー表の**どこにも `lineWeight` のキーが無い**」） |
| O-4 | **どの暦で稼働日を数えるか**（実績の右端 = `actualStart` ＋ `actualDuration` は稼働日加算） | `user-order.md:374` | `E05-calendar.md:237`（C-2「既定暦と読むのが自然だが原典は明言していない＝**未検証**」） |
| O-5 | **繰返し祝日（`Exception/Type` 1–8）の展開** | `user-order.md:302`（項 39） | `E05-calendar.md:120` / `:242`（C-7「展開器（`2`/`4`/`6` の 3 種）の実装は次期で再評価。警告の文面・出し方も未定」） |
| O-6 | **注記用の固定色の実値**（`HighlightBox.strokeColor = null` のとき何色で描くか） | `user-order.md:342` | `E08-comment-highlight.md:212`（C-1「未定である。次期の色 PoC で確定させる。推測で書かない」） |
| O-7 | **透明時のコントラスト判定／SVG 出力で透明をどう書くか／モノクロ時のコントラスト** | `user-order.md:174` / `:456` | `E07-visual-origin.md:181-183`（U-10 / U-11 / U-12。いずれも原典自身が未検証と明記） |
| O-8 | **`TaskGroup.height`（人の指定）と段数由来の行高の関係** | `user-order.md:251`（行高固定を前提にしない） | `E03-dependency-taskgroup.md:151`（C-5「指定値が下限なのか固定値なのか上書きなのかで、段が増えたときの絵が変わる」） |
| O-9 | **`TaskGroup.parent_id` の循環を禁じる規則が無い**（WBS の循環は禁じている） | `user-order.md:220-221` | `E03-dependency-taskgroup.md:156`（C-10。**未検証**） |
| O-10 | **将来拡張「`TaskGroup` ごとの公開/編集権限」の受け皿** | `user-order.md:497-499` | 仕様書は `NFR-012` が受ける（`01-04-requirements.md:458`）。データモデル側に列は無い（在庫表 11 枚に該当行なし。自分で確認） |
| O-11 | **担当ラベルの既定を隠す**という決定を利用者が承認した記録が無い | `user-order.md:606`（「個別に表示/非表示を切り替えられること」としか言っていない） | `E06-resource-assignment.md:142`（`assigneeVisible` の**既定は `false`**。根拠は PoC 実測） |
| O-12 | **`exportCanvas.width` / `.height` それぞれの範囲** | `user-order.md:457-459`（「収まらない場合は縮小」「文字が下限を割るときは警告」） | `E09-settings-blob.md:556`（B-5。`S-81` は 1 行しかなくメンバーごとの下限・上限が無い） |
| O-13 | **`fontScale` の 3 値が `'S'`/`'M'`/`'L'` の大文字 1 字**（他の判別値は kebab-case 小文字） | `user-order.md:113`（「大/中/小」） | `E09-settings-blob.md:568`（C-3。**利用者に見える略号でもあるので変えると UI の表記も動く**） |
| O-14 | **項 68「提案すること」をどう果たすか**（製品への要求ではなく進め方） | `user-order.md:515` | 仕様書は「どの要求も受けない。**受け手を要求側に作らないこと（MUST NOT）**」と明記（`01-04-requirements.md:461`）。⚠️ **進め方としての運用（レビューのたびに提案）が実際に回っているかは本書では確かめられない＝未検証** |

---

## 9. 仕様書 Chapter 1〜4 の受け（`grep` で確かめた）

**結論: 表 T-011（`01-04-requirements.md:442-459`）は項番 1〜69 を 1 つ残らず挙げている**（15 行から項番を抽出して 1〜69 と突き合わせた。取りこぼし 0）。
**したがって「受けていない項番」は無い。** ただし**内容を変えて受けている項が 8 件**あり、そのうち **6 件は user-order 側に変更の記録が無い**。

| # | 項 | 利用者の指示 | 仕様書が受けた形 | user-order 側の更新 |
|:--:|:--:|---|---|:--:|
| S-1 | 30-6 | **人が `stackOrder` を指定したらそれを優先する**（`user-order.md:245`） | **`ST-6`「人が段を手で指定する手段を設けない（MUST NOT）」**（`01-04-requirements.md:1187`） | **無し** |
| S-2 | 30-7 | 安全弁に**到達したら人に知らせる**（項 61 と同じ考え方。`user-order.md:246-248`） | **`ST-7`「達したらエラーを出して処理を止めること（MUST）」**（`01-04-requirements.md:1188`） | **無し** |
| S-3 | 37 | **背景の上での素の左ドラッグはパンである**（改訂 2026-08-01。`user-order.md:298`） | **`PD-5` 範囲選択**。パンは `Ctrl`・中ボタン・スクロールバーに残す（`01-04-requirements.md:2068`） | **無し** |
| S-4 | 40 | **`Today Line`（本日線）**を表示できる。今日の日付に縦線を 1 本（固定）（`user-order.md:314` / `:326`） | **本日線を廃止し基準日線に統合**（利用者判断 2026-08-13。`01-04-requirements.md:1945` / `:1951`） | **無し** |
| S-5 | 45 | **任意の色の角丸四角**で囲える（`user-order.md:342`） | 色は**枠線だけ**（塗りの列を持たない。`E08-comment-highlight.md:64`） | **無し** |
| S-6 | 47 | 依存線を**9 点**から引き出し／引き入れ（`user-order.md:354`） | **`RC-6` により自動配線のアンカーを 2 種へ縮小**（性能を理由に。`01-04-requirements.md:3728`） | **有り**（同書 `:358` が既に「アンカーは 2 種類」と書いており、**同じ文書の中で 354 行と 358 行が食い違っている**） |
| S-7 | 55-3 | **既定パスワードは `GoodRelax`**（`user-order.md:440`） | **`gr-scheduler`** へ変更（確定 2026-08-13・理由付き。`tbl-settings.md:243`） | **無し** |
| S-8 | 39 | **稼働日/非稼働日（土日・祝日）**を区別して表示（`user-order.md:302`） | 繰返し祝日（`Exception/Type` 1–8）は**MVP でグレー表示されない**（`E05-calendar.md:120`） | **無し** |

**`grep` で受けを確認できたもの**（内容が変わっていない主なもの）

| 項 | 受け手 | 出典 |
|:--:|---|---|
| 17 | `truncateUnits`（既定 24 ＝ 全角 12） | `tbl-settings.md:83` |
| 18 | 「9 点アンカーと左詰め / 中央 / 右詰め…ピクセル座標で持ってはならない（MUST NOT）」 | `01-04-requirements.md:1131` |
| 20 | 表 T-016 のプロパティ行（`PR-12` / `PR-14` ほか） | `01-04-requirements.md:1344` / `:1346` |
| 24 | 「原色を使ってはならない（MUST NOT）」・表 T-017 `CL-1` に 11 色 | `01-04-requirements.md:1390` / `:1393` |
| 54 | `FR-015`「変更前の予定を重ねる」。**グレーかつ破線**（色だけで区別しない） | `01-04-requirements.md:2023-2028` |
| 58 | `S-81` `exportCanvas` 既定 `1600 × 900`（A3 横 / A4 横 / 現在の画面） | `tbl-settings.md:180` |
| 62 | 「XML 外部エンティティを無効化すること（MUST）。`innerHTML` への直挿しを行ってはならない（MUST NOT）」 | `01-04-requirements.md:2776` |
| 63 | `FR-027` 初期表示用のテンプレート | `01-04-requirements.md:3016` |
| 65 | `NFR-012`（将来の拡張。ユースケースを立てない判断） | `01-04-requirements.md:458` / `:463` |
| 68 | **どの要求も受けない**（製品の振る舞いではないため。受け手を要求側に作らないこと＝MUST NOT） | `01-04-requirements.md:461` |

**確かめられなかったもの（未検証）**

| 項 | なぜ確かめられないか |
|:--:|---|
| 6-3 〜 6-6 | 表 T-011 の `V-13` は「`GL-006` 配下の横断要求が受ける」と書く（`01-04-requirements.md:459`）。`GL-006` は `:83` に実在し `:3265` に判定手段の要求がある。**しかし 4 つのサブ項それぞれの受け皿を 1 対 1 で突き合わせてはいない＝未検証** |
| 1 〜 4 | 「Chapter 1.3 が目標として受ける」（`01-04-requirements.md:461`）。**Chapter 1.3（`:43-93`）の全文は読んでいない＝未検証** |
| 66 / 67 | 「Chapter 1.9 と表 T-003 の `CN-9` が受ける」（`01-04-requirements.md:461`）。**当該箇所は読んでいない＝未検証** |

> ⚠️ **表 T-011 の `V-7` 行に付け先の誤りがある。** 「**素の左ドラッグは範囲選択へ変更**」という注記が**項 36 に付いている**が（`01-04-requirements.md:450`）、素の左ドラッグを定めているのは**項 37**（`user-order.md:296` / `:298`）である。項 36 は LOD（`user-order.md:280`）で、ドラッグとは無関係である。

---

## 在庫表との食い違い

**在庫表 11 枚（`previous-project-result/temp/inventory/`）と `user-order.md` を突き合わせた結果。**
**「どちらが正か」欄は判定であって決定ではない。決定は利用者が下す。**

| # | 何が食い違うか | 源泉側（`user-order.md`） | 在庫表側 | どちらが正か |
|:--:|---|---|---|---|
| **X-1** | **`Task.milestone`（真偽値の列）を持つか** | `user-order.md:51`「バーもマイルストーンも `Task` で、**`Task.milestone`（真偽値）で区別する**」 | `E01-task-plan.md:120`「⚠️ **用語辞書は「真偽値の `milestone` 列は持たない」と明記＝正面衝突**（未解決 1）」／`E02-task-actual.md:288`／`E07-visual-origin.md:173`／`E11-identity-and-notstored.md:508` | **列を持つ側が正である。** 在庫表 4 枚とも原典（ERD・plan-actual）は列を持つと読み、**利用者の指示も列を持つ側**である。食い違っている相手は仕様書の用語辞書（`tbl-glossary.md:25`）1 枚だけ。⚠️ **列を持たない設計を採るなら、export で `shapeKind` から `Milestone` を復元する規則が要るが、その規則はどの原典にも無い**（`E01:120`） |
| **X-2** | **`stackOrder` を人が指定できるか** | `user-order.md:245`（項 30-6）「**人が `stackOrder` を指定したらそれを優先**する（`null` = 自動）」／`:255`／`:603` | `E03-dependency-taskgroup.md:96`（列として在る）／`E03:153`（C-7「**仕様書 `ST-6` が『人が段を手で指定する手段を設けない（MUST NOT）』と定めたため、列そのものの要否が原典と食い違う**」） | **未決。利用者の判断が要る。** ⚠️ **`user-order.md:16` は「取り下げた項目は欠番として残す」と手続きを定めているのに、30-6 にはその印が無い。** 仕様書側だけで取り下げが進んでいる |
| **X-3** | **段数の安全弁に達したときの挙動** | `user-order.md:246-248`（項 30-7）「暴走を止める安全弁として十分に大きい上限を持ち…**到達したら人に知らせる**（項 61 と同じ考え方）」 | `E09-settings-blob.md:522`（10-4 #1）「`stackSafetyCap`…**数え方と到達時の挙動は `FR-003` の `ST-7` が持つ**」。その `ST-7` は「**エラーを出して処理を止めること（MUST）**」（`01-04-requirements.md:1188`） | **未決。** 「知らせる」と「止める」は両立しない。⚠️ 項 61（`user-order.md:465`）は「**勝手に消さない…知らせるだけにする**」であり、利用者は一貫して「止めずに知らせる」側を言っている |
| **X-4** | **ハイライトボックスの「任意の色」の範囲** | `user-order.md:342`（項 45）「任意の場所を**任意の色の角丸四角**で囲えること」 | `E08-comment-highlight.md:64`「**塗り（fill）の列は無い。** 原典は `strokeColor` だけを持つ。項 45 の『任意の色』は**枠線の色**として実装されている」／`E08:61`（`'transparent'` は選べない） | **在庫表が原典どおりで正。** ただし利用者の言葉は塗りを含みうる。**狭めたことの承認記録が user-order にも在庫表にも無い** |
| **X-5** | **祝日を表示できるか** | `user-order.md:302`（項 39）「**稼働日/非稼働日**（土日・祝日）を区別して表示できること」 | `E05-calendar.md:120`「**既知の割り切り**: 繰返し祝日（1–8）は MVP でグレー表示されない」／`E05:173`（種別 1–8 は要素まるごと Carry ＋ 警告） | **在庫表が原典どおり。** ⚠️ `E05:115` が挙げる原典の実例そのものが `Type=2`（毎年・日付指定）の「元日」である。**実務の祝日ファイルが何型で来るかは本書では確かめられない＝未検証**だが、原典の例が 1–8 側に落ちる以上、項 39 が MVP で満たされない可能性は高い |
| **X-6** | **透かしの既定パスワード** | `user-order.md:440`（項 55-3）「**既定パスワードは `GoodRelax`**。その SHA-256（16 進）を定数として `.html` に埋め込む」 | `E09-settings-blob.md:329`（`S-100` 既定の透かし解除パスワード）。値の正は `tbl-settings.md:243` で **`gr-scheduler`**（「⚠️ **要望 55-3 は `GoodRelax` を指定していたが、汎用の道具に固有名を既定値として置かないため製品名に変更した**（確定 2026-08-13）」） | **仕様書側が正**（変更の記録が値の正の側にある）。**user-order は未更新** |
| **X-7** | **本日線（Today Line）が在るか** | `user-order.md:314`（`Cursors` A = `Today Line`・今日の日付に縦線を 1 本引く（固定））／`:326`（項 40） | `E04-project.md:144`「仕様が**本日線と基準日線を 1 本に統合**し、**本日線そのものを廃止**（`S-99d` 廃止・2026-08-13）」／`E09-settings-blob.md:320`（`S-99d` 廃止） | **仕様書側が正**（利用者判断 2026-08-13・`01-04-requirements.md:1951`）。**user-order は未更新で項 40 が生きたまま残っている。** 3 種のカーソル表（`user-order.md:311-317`）も A の行が古い |
| **X-8** | **`percentComplete` の範囲の書き方** | `user-order.md:171`「**整数・0 以上**（**通常 0〜100**）」 | `E02-task-actual.md:166`「**「0〜100」と書いてはならない。** そう読んだ実装者は必ずバリデーションかクランプを書き、『予定 10 日の仕事に 15 日かかった』という業務上の事実を消す」 | **規則は在庫表（plan-actual）が正。** user-order の括弧書きは「通常」と断ってはいるが、**禁止された綴りをそのまま含む。転記するときに落とすこと** |
| **X-9** | **`item` を使ってよいか** | `user-order.md:59`「**廃止した語**（次期で使わない）: **`item`** / `itemKind` / …」 | `E03-dependency-taskgroup.md:158`／`E06-resource-assignment.md:392`（用語辞書 表 T-101 の 5 語に **`Item`** が入る） | **両立しうるが、記録が要る。** 辞書 `tbl-glossary.md:29` は「`Item` ＝**当たり判定と選択の対象となるものの総称**。⚠️ **`Task` の別名ではない**」と外延を変えて再導入している。**利用者が廃止と言った語を意味を変えて復活させたことの明示が user-order 側に無い** |
| **X-10** | **往復無損失（Drop=0）の適用範囲** | `user-order.md:452`「**未編集で往復したら元のファイルと同じものが出る**こと（情報を落とさない）」＋ `:463-465`（項 61 マージ） | `E10-carry-roundtrip.md:240-241`「適用範囲は**単一 MSPDI の未編集往復**に限る／**明示的に許容する Drop（1 件のみ）＝マージ時の取込側 Carry の欠落**」／`E11-identity-and-notstored.md:319` | **在庫表が原典どおりで正。** ただし**利用者は「情報を落とさない」としか言っておらず、合流で通貨・計算オプション・単価表・勤務時刻が落ちることの承認記録が無い**。項 61 の 3 択ダイアログの説明にもその代償が書かれていない |
| **X-11** | ⚠️ **在庫表どうしが食い違う（本書の範囲外だが A05 が裏を取れた）— `FR-063` の受け皿** | **user-order に版数・最後に書いた者・変更の理由を求める項は 1 つも無い**（項 1〜69 を全文で確認した。`user-order.md` の「AI」は `:97` の `AI Export` と `:454` の項 57 の 2 か所だけ） | `E04-project.md:117`「→ **`FR-063` は現状どこにも着地していない。** 新設が要る」（同書 `:14` の読んだ文書一覧に `10-agent-interface/` が無い）↔ `E09-settings-blob.md:41-43`「文書の根 ├─ **`revisionStamp` {…}** Agent API 用の版スタンプ ├─ **`changeLog` [ … ]**」（出典 `:57` ＝ `grs-document-with-revision-stamp.json:1-96` を全文で読んだ） | **`E09` が正。受け皿は既に在る。** 実物を自分で開いて確認した: `grs-document-with-revision-stamp.json:4-8` に `revisionStamp: { revision: 12, lastEditedBy: "agent", updatedAt: "2026-08-02T13:24:51.117Z" }`、`:11-18` に `changeLog`（`revision` / `editedBy` / `explanation` / `changedAt`）。**`E04` は `10-agent-interface/` を読んでいないため「受け皿なし」と結論している** |

---

### 食い違いの読み方（散文はここだけ）

**X-2・X-3・X-4・X-5・X-7 の 5 件は、同じ形をしている** —— 仕様書側が利用者の言葉を狭めた（または反転した）のに、**源泉である `user-order.md` にその記録が戻っていない**。
`user-order.md:16` は「レビューで取り下げた項目は**欠番として残す**（他文書が項番で参照しているため）」という手続きを自ら定めており、
項 11 / 14 / 52 と項 30-4 ではその手続きが実際に踏まれている（`:140` / `:143` / `:395` / `:238-239`）。
**踏まれていないものだけが残っている**ので、これは記法の問題ではなく**手続きの取りこぼし**である。

**X-11 は種類が違う。** 在庫表どうしの食い違いであり、原因は**読んだ原典の範囲の差**である。
`E04-project.md:11` は XSD 全 3906 行を機械検索して「MSPDI に受け皿は無い」と正しく結論したが、
**GRS 側の受け皿を探す範囲に `10-agent-interface/` が入っていなかった**（同書 `:5-17` の読んだ原典一覧に無い）。
`E09-settings-blob.md:17` はその実物を読んでいる。**`E04` の §3-3「GRS 側にも受け皿が無い」（`:111-117`）は撤回されるべきである。**
ただし **`E04` が挙げた別の論点は生きている** —— `Project.revision`（MSPDI Own・"number of times a project has been saved"）に
GRS の版数を相乗りさせてよいかは依然として未決であり（`E04:131` / `:159` の U-4）、
`revisionStamp.revision` が `Project/Revision` と**別の値**である以上、その 2 つの関係を決める必要が残る。
**本書はそこまでは確かめていない＝未検証。**
