# Y2 — 一度に直さないと終わらない束

**作成 2026-08-14。`docs/spec/` は 1 バイトも編集していない（読むだけ）。**
**行番号では指定しない。** すべてリテラル文字列か行 ID / UID で指す。
**数はすべて自分で数えた。** 数えていないものは「未検証」と明記する。

---

## 0. 走らせた道具と、その出力（実測）

```bash
python .claude/skills/spec-graph-check/graph.py --cycles
python .claude/skills/spec-graph-check/impact.py GL-003 FR-018
python .claude/skills/spec-graph-check/impact.py S-100 AG-11 U-45 T-001
bash .claude/skills/spec-graph-check/check.sh
```

**`graph.py --cycles`（全体）** —— 出力をそのまま数えた。

| | 値 |
|---|---|
| 出力辺を持つ object | **437** |
| 閉路（2 メンバー以上） | **6** |
| 内訳 | `size 195` / `size 8`（`FR-020` `FR-086` `GL-007` `LM-12` `LM-8` `S-100` `S-99a` `UC-009`）/ `MG-10`↔`MM-2` / `MG-13`↔`S-71` / `AG-11`↔`AG-6` / `EZ-2`↔`S-124` |

**`check.sh`（作業前の基準線）** —— **ALL GREEN**。

| 指標 | 値 |
|---|---|
| 検査 1 〜 12・15 | **すべて 0**（ゲート） |
| 検査 13（移送の消し忘れ・ADVISORY） | **4** |
| 検査 14（値の写し・ADVISORY） | **20** |
| 重複検出 | **A=18 / new 0** / groups=50 / pairs=118 |
| 走査した計数 | `tables=83` `figures=1` `rows=811` `uids=136` |

⚠️ **利用者の指示にあった「`T-011 V-10` ↔ `UC-014`・0.47」は、いまの基準線では `new 0` である。**
既に基準線へ取り込まれたか、要求側 CR-114 の適用時に解消されたかは**未検証**。
**本作業の意味は変わらない** —— 削除だけで新規指摘が出た事実は残るので、束ごとに `check.sh` を回す前提は保つ。

---

## 1. 未反映 6 CR が触る対象の全数

**「触る」を 2 つに分けた。** 分けないと、束が文書全体に膨らんで使い物にならなくなる
（`SKILL.md` の「195 メンバー閉路は使い物にならない」と同じ型）。

| 区分 | 定義 |
|---|---|
| **編集対象** | その object の**本文が書き換わる**もの |
| **参照対象** | 新しく**指す／指される**が、本文は変わらないもの |

### 1-1. 編集対象（既存 object のみ。新設する席は「まだ節点ではない」ので別掲）

| CR | 編集する既存 object | 新設するもの |
|---|---|---|
| **CR-105** | `T-001`・`C-1`・`C-2`・`C-3`・`C-4`（`PLM-` へ改名）・`FR-092` の注 | 0xx 表 1 枚（`CH-1`〜`CH-5`）／辞書 §6 ＋ `T-106`（`VK-1`〜`VK-3`） |
| **CR-106** | `U-45`・表 `T-007` の下の注・§1.9 の `[System]` 定義・§2.1（段落追加）・辞書の `Agent API` 注・`A-appendix` 変更履歴 ＋ **`UC` / `FR` / `NFR` の本文 121 ノード** | なし（席を採らない） |
| **CR-107** | `GL-003`・表 `T-025`（`MC-9` 追加）・表 `T-043`（`PG-13` 追加）・`05-07-design.md` 第 7 章の 1 文 | `NFR-013`／0xx 表 1 枚（`CO-1`〜`CO-7`） |
| **CR-109** | `FR-004`・`FR-018`・`FR-055`・表 `T-015a`・表 `T-036`・表 `T-103` | 0xx 表 1 枚（`HF-1`〜`HF-9`）／`HM-` 1 行／`SK-` 2 行／辞書 1 行 |
| **CR-110** | `FR-024`・`FR-021`・`FR-063`・`AG-11`・表 `T-203`・`S-73` | 0xx 表 2 枚（`DR-`・`DF-`）／`tbl-settings.md` に表 1 枚 |
| **CR-114（資産側）** | `U-23`・辞書 §1 の注・`Agent API` の注 2 か所／`S-52`(行削除)・`S-89`・`S-93`・`S-99d`(行削除)・`S-100`・`S-118`・`S-124`・表 `T-215` の注・設定値 §1 の注・凡例 | なし |

**編集対象のうち、グラフが知っている object は 32 個**（自分で数えた。`specindex.known` と突き合わせた）。

**CR-114 資産側の「除く」は 14 か所**（辞書 4 ＝ `G2` `G3` `G4` `G6`、設定値 10 ＝ `S1` `S4` `S5` `S12` `S13` `S14` `S15` `S16` `S18` `S19`）。
`S10`（`themeHue` の `210`）は**要判断**で 14 に含まない。`05-07-design.md` は**除く対象 0 件**。

### 1-2. アンカー照合（適用前の一致確認・実測）

**各 CR が「1 回だけ一致する」と表明したリテラルを、いまの作業ツリーで数え直した。48 本中 46 本が `n=1`。**

| 結果 | 件数 | 中身 |
|---|--:|---|
| `n=1`（一致） | **46** | CR-105 6 / CR-106 5 / CR-107 5 / CR-109 8 / CR-110 7 / CR-114 15 |
| `n=0`（**不一致**） | **2** | **CR-110 の `FR-063` の「変更前」**（`**版数を上げるのは、文書に保存される値を変える更新すべてとすること（MUST）。`）／**CR-110 の `AG-11` の「変更前」**（`発話の確定は版数を上げる（`FR-063`）。`） |

⚠️ **この照合は「本書を書いた時点」の事実である。作業中に別の作業者が編集を続けている。**
本書の作業中だけで `git diff --stat` は
`01-04-requirements.md` **239 行**・`_assets/tbl-glossary.md` **47 行**・`_assets/tbl-settings.md` **4 行**・
`docs/review/duplication-baseline.txt` **1 行**の変化を示した。
**束に入る直前に 48 本のリテラルを数え直すこと（MUST）。**

⚠️ **CR-110 の第 4 節（変更前の仕様）は、`CR-103` 適用前の本文を引いている。**
いまの `FR-063` は既に **「文書は日程データの群と見せ方の群に分かれる。版数を上げるのは、日程データの群を変える更新とすること（MUST）」** であり、
`AG-11` も既に **「発話は日程データではないので版数を上げない（`FR-063`）。それでも監視は起きること（`AG-6`）。」** である。
**CR-110 を反映するときは、`CR-103` 後の本文を置換元にすること（MUST）。** CR の引用をそのまま使うと 0 件一致で止まる。

---

## 2. 席番号 —— 4 つの CR が同じ席を採ろうとしている（**最優先の実測**）

**`grep` で全数を数えた**（`docs/spec/*.md` と `docs/spec/_assets/*.md` のみ。`output/` と `__pycache__/` は読んでいない）。

### 2-1. すでに埋まっている席

| 席 | 実測 | 誰が採ろうとしているか |
|---|---|---|
| **表 `T-050`** | ⚠️ **使用中。** `01-04-requirements.md` に **`**表 T-050 — 削除の連鎖**`** が定義されており、`FR-032` の `STATEMENT` が「一緒に消えるものを表 T-050 に従って消し」と指している（CR-112 が入れた） | **CR-105（`CH-`）／CR-107（`CO-`）／CR-109（`HF-`）／CR-110（`DR-`）の 4 本が全部これを採ると書いている** |
| **`U-46`** | ⚠️ **使用中。** `| U-46 | `Pinned Row` | ピン止めした行。縦にスクロールしても画面に残る |`（CR-108 が入れた） | **CR-109**（`Row Expander` を `U-46` に置くと書いている） |
| **`HM-10`** | ⚠️ **使用中。** `| HM-10 | **タスクバーを別の行へ移したとき、WBS の子は元の行に残る。**…`（CR-111 が入れた） | **CR-109**（WBS の深さ操作を `HM-10` に置くと書いている） |

**3 件とも、各 CR の第 6 節は「出現 0 件」と実測を書いている。**
**その実測は正しかったが、その後 CR-108 / CR-111 / CR-112 が反映されて席が埋まった。**
**席の実測は「起票時点の事実」であり、適用時点の事実ではない（MUST 数え直す）。**

### 2-2. いま空いている席（実測）

| 数えたもの | 値 |
|---|--:|
| 見出しとして定義されている表（`^**表 T-nnn —`） | **80** |
| うち 0xx 帯 | **60**（`T-001` 〜 `T-043` ＋ 枝番 ＋ `T-050`。`T-030` は欠番） |
| 封印（再利用禁止・`A-appendix.md`） | `T-044` 〜 `T-047` |
| **0xx 帯の空き** | **`T-048` `T-049` `T-051` `T-052` `T-053` `T-054` `T-055` …**（`T-051`〜`T-055` はいずれも 0 件と実測） |
| `T-106` / `U-47` / `HM-11`※ / `SK-21` / `SK-22` / `MC-9` / `PG-13` / `NFR-013` | **すべて 0 件** |
| 接頭辞 `PLM` / `CH` / `VK` / `CO` / `HF` / `DR` / `DF` | **すべて 0 件** |

※ `HM-11` は `HM-10` が埋まった以上の次席。**本書では「0 件」であることだけを実測しており、採ることを決めてはいない。**

### 2-3. 表番号の帯（実測。規約として明文があるかは未検証）

| ファイル | 使っている帯 |
|---|---|
| `01-04-requirements.md` | `T-001` 〜 `T-050`（0xx） |
| `05-07-design.md` | `T-042` `T-043`（0xx） |
| `_assets/tbl-glossary.md` | `T-101` 〜 `T-105`（1xx） |
| `_assets/tbl-settings.md` | `T-201` 〜 `T-215`（2xx） |

⚠️ **CR-110 は `_assets/tbl-settings.md` に「表 T-052」を新設すると書いている。**
**設定値ファイルに 0xx を置くのは、上の帯から外れる。** 帯が明文の規約かどうかは**未検証**だが、
**席を配り直すときに一緒に決めること**（2xx を採るなら `T-216`）。

### 2-4. 新設する表の総数

| CR | 0xx 帯 | 1xx 帯 | 2xx 帯（または CR の言うとおり 0xx） |
|---|--:|--:|--:|
| CR-105 | 1（`CH-`） | 1（`T-106`・`VK-`） | 0 |
| CR-107 | 1（`CO-`） | 0 | 0 |
| CR-109 | 1（`HF-`） | 0 | 0 |
| CR-110 | 2（`DR-`・`DF-`） | 0 | 1（`themeHue` の値） |
| **計** | **5** | **1** | **1** |

**0xx 帯を 5 枚、まとめて配ること。** 1 枚ずつ配ると、2 本目が 1 本目の採った番号を知らずに同じ番号を採る
—— **いま 4 本が `T-050` で衝突しているのは、まさにそれが起きた形である。**
利用者が示した対応表（CR-105→`T-054` / CR-107→`T-055` / CR-109→`T-051` / CR-110→`T-052`）は
この再配当の 4 枚ぶんに当たるが、**CR-110 の 2 枚目（`DF-`）と設定値側の 1 枚が空欄である。**
**残り 2 枚をどこに置くかは決まっていない（未決）。**

---

## 3. 対象を共有する CR を辺で結ぶ

### 3-1. 参照まで含めて結ぶと、6 本が 1 つの成分に潰れる（＝使えない）

**参照対象（122 object）まで含めて CR どうしを結んだ結果** ——

```
CR-105 <-> CR-106 : T-103        CR-106 <-> CR-109 : T-103 U-45
CR-105 <-> CR-107 : GL-003       CR-106 <-> CR-114 : U-45 XO-4
CR-105 <-> CR-109 : T-103        CR-107 <-> CR-109 : FR-003 FR-018
CR-105 <-> CR-114 : FR-092       CR-107 <-> CR-114 : FR-003
CR-110 <-> CR-114 : T-206        CR-109 <-> CR-114 : FR-003 U-45

連結成分: 1 個 —— {CR-105 CR-106 CR-107 CR-109 CR-110 CR-114}
誘導部分グラフ: object 122 / 辺 164 / 閉路 6（最大 25 メンバー）
```

⚠️ **これは束として使えない。** 「6 本を 1 回で書け」は「全部を 1 回で書け」と同じで、
`SKILL.md` が「195 メンバー閉路は使い物にならない」と言っているのと同じ失敗である。
**参照の共有は「同じものを読む」であって「同じものを書く」ではない。**

### 3-2. 編集対象だけで結ぶ（こちらを採る）

**本文が書き換わる 32 object だけで誘導部分グラフを取った。**

| 指標 | 値 |
|---|--:|
| object | **32** |
| そのあいだの辺 | **9** |
| **閉路** | **1 個だけ** —— **`FR-018` ↔ `FR-055`** |

**辺の全数（実測。これが全部である）**

```
AG-11  -> FR-063
FR-004 -> FR-018   FR-021
FR-018 -> FR-055
FR-024 -> FR-063
FR-055 -> FR-018   T-025
FR-092 -> T-025
S-124  -> FR-092
```

**この 9 本が、CR をまたぐ「書き換えどうしの依存」の全部である。**

| 辺 | またぐ CR | 意味 |
|---|---|---|
| `FR-004` → `FR-018` | CR-109 の中 | 折り畳みと LOD |
| `FR-018` ↔ `FR-055` | **CR-109 の中（閉路）** | LOD の下限と全体表示 |
| `FR-004` → `FR-021` | **CR-109 → CR-110** | `FR-004` の本文が `FR-021` を名指ししている |
| `FR-055` → `T-025` / `FR-092` → `T-025` | **CR-109 → CR-107** / **CR-105 → CR-107** | 測定条件の表を CR-107 が書き換える |
| `AG-11` → `FR-063` / `FR-024` → `FR-063` | CR-110 の中 | 版数の判定 |
| `S-124` → `FR-092` | **CR-114 → CR-105** | 待ち時間の値が `FR-092` の `EZ-2` を指す |

### 3-3. 全体の 6 閉路のうち、編集がメンバーを含むもの（実測）

| 全体閉路 | メンバー数 | 編集する CR | 編集するメンバー |
|---|--:|---|---|
| `size 195` | 195 | —（編集対象は入るが、この閉路は分割の役に立たない） | — |
| **`size 8`** `FR-020` `FR-086` `GL-007` `LM-12` `LM-8` `S-100` `S-99a` `UC-009` | 8 | **CR-114** | **`S-100`** |
| **`AG-11` ↔ `AG-6`** | 2 | **CR-110** | **`AG-11` のみ** |
| **`EZ-2` ↔ `S-124`** | 2 | **CR-114** | **`S-124` のみ** |
| `MG-10` ↔ `MM-2` | 2 | なし | — |
| `MG-13` ↔ `S-71` | 2 | なし | — |

**`size 8` 閉路の辺（実測）**

```
UC-009 -> FR-020  FR-086  GL-007
FR-086 -> FR-020  GL-007  LM-12  S-99a  UC-009
LM-12  -> FR-020  S-100
S-100  -> FR-020
LM-8   -> FR-020 ;  S-99a -> FR-020 ;  GL-007 -> UC-009 ;  FR-020 -> LM-8  UC-009
```

⚠️ **`S-100` は「入口が `LM-12` だけ・出口が `FR-020` だけ」の位置にある。**
**閉路の中で最も細い箇所であり、ここを書き換えると閉路全体の意味が動く。**

---

## 4. 束（出力）

**6 束。うち 3 束が閉路を含む。**

| 束 | 含む CR | 閉路を含むか | 束の中の順序 | 直したあとに確かめること |
|---|---|---|---|---|
| **A. 席の一括再配当**（前提） | CR-105 / CR-107 / CR-109 / CR-110 | **含まない**（席は参照グラフの節点ではない） | ① 0xx 表 **5 枚**を一度に割り当てる ② 1xx（`T-106`）と設定値側 1 枚の帯を決める ③ 行 ID（`U-47` ほか・`HM-11` ほか）を割り当てる ④ 4 本の CR 本文の番号を書き換える | **`grep` で 0 件を数え直す**（起票時の実測は古い）／検査 **5**（未定義の表への参照）・**6**（同じ行 ID が 2 か所）・**7**（存在しない行 ID / UID への参照）／`tables=` `rows=` の計数が動くこと |
| **B. 目標 `GL-003` と価値のキーワード** | **CR-105 → CR-107** | **含まない**（編集対象どうしの閉路は無い）。ただし `GL-003` は参照側の 25 メンバー閉路のメンバーであり、`FR-092` → `T-025` と `FR-055` → `T-025` の 2 辺が束をまたぐ | ① 辞書 §6 ＋ `T-106`（`VK-1` ぬるサク / `VK-2` すぐわか / `VK-3` ペライチ）② 1.1（`T-001` を移し `C-` → `PLM-`、`PLM-5`〜`PLM-7` 追加）③ 1.2（新表 `CH-1`〜`CH-5`）④ `FR-092` の注 ⑤ `GL-003` の `STATEMENT` ⑥ `NFR-013` ＋ `T-025` の `MC-9` ＋ `T-043` の `PG-13` ＋ 設計第 7 章の 1 文 | 検査 **12**（新表 `T-106` と `CO-` の表に MUST を入れない）／検査 **9**（「例外は表 T-105 の 2 件だけ」型の件数文を壊していないか）／**StrictDoc export**（表 2 枚追加＋段落削除 ⇒ 連続空行）／`impact.py GL-003 NFR-013 <新表番号>` で孤立していないこと |
| **C. 折り畳みの操作面** | CR-109 | ⚠️ **含む —— `FR-018` ↔ `FR-055`**（編集対象どうしの**唯一**の閉路）。`FR-004` → `FR-018` → `T-015` も同じ 1 回で書く | ① 新表（`HF-1`〜`HF-9`）② `FR-004` の `STATEMENT` ③ `FR-018` の 1 文（`HF-7`）④ `FR-055` の 1 文（`HF-8`）—— **③④は同じ 1 回で書く（MUST）** ⑤ `T-015a` の新行 ⑥ `T-036` の `SK-21` / `SK-22` ⑦ 辞書 `T-103` の新行 | 検査 **7**（`HF-*` / 新 `HM-` / `SK-21` / `SK-22` / 新 `U-` の定義行があるか）／検査 **8**（`表 T-015 の HR-3` 型の参照）／検査 **9**（`T-036` は 22 行・`T-015a` は 10 行と述べる文が無いか）／**export**（表 1 枚追加） |
| **D. 文書ルートと版数** | CR-110 | ⚠️ **含む —— `AG-11` ↔ `AG-6`**（全体閉路の 1 つ）。ほかに `FR-024` → `FR-063` → `WY-1`、`FR-021` ← `FR-004` | ① **`AG-6` と `AG-11` を同じ 1 回で書く（MUST。§5 参照）** ② `FR-063` の `STATEMENT`（**`CR-103` 後の本文を置換元にする**）③ `FR-024` ＋ 新表（`DR-`）④ `FR-021` ＋ 新表（`DF-`）⑤ `tbl-settings` の `S-73` 移設＋新表 ⑥ 辞書 `K-60` の確認 | 検査 **12**（新設する値の表に MUST を書かない）／検査 **9**（`DR-2` の「鍵は 11」を数え直す）／検査 **7**（`DR-` `DF-` の定義行）／`impact.py FR-063 FR-024 T-032 T-041` を再走し `MG-12`・`WY-1`・`FR-041` の 3 鍵文を読む／**export**（表 3 枚追加） |
| **E. 製品名 `GRS` の一括置換** | CR-106 | **含まない** —— 参照を 1 本も張らないので、グラフに辺が生えない | ① 定義（§1.9 → 指すだけ／§2.1 に宣言／`T-007` の下の注）② Chapter 4.2（最小の塊で RST の壊れを検出）③ Chapter 3 §3.2 前半 ④ 同後半 ⑤ Chapter 4.1 前半 ⑥ 同後半 ⑦「システムが」⑧「本ソフトウェア」＋辞書 `U-45` ⑨ `A-appendix` 変更履歴 | ⚠️ **件数を数え直してから始める（MUST。§6 参照）**／**塊ごとに export**（CJK に隣接する行内コード `` `GRS` `` が docutils を落とす型）／置換後に「システムは」「システムが」「本ソフトウェア」が **0** であることと、残る「システム」の全数が説明できることを数える |
| **F. 検討経緯の除去（資産側）と `S-100`** | **CR-114（資産 14 か所）＋ CR-106 の名称決定** | ⚠️ **含む —— `S-100` は 8 メンバー閉路の、`S-124` は `EZ-2` ↔ `S-124` の各メンバー** | ① **`S-100` の文言を、`GRS` の名称決定と同じ 1 回で決める（MUST。§6-2）** ② `S4` と `S5`（🅿 の凡例と `S-52` 行）を**対で**適用 ③ `S14`（`S-99d` 行削除。`CR-102` 適用後に回す） ④ 残り（`G2` `G3` `G4` `G6` / `S1` `S12` `S13` `S15` `S16` `S18` `S19`）を 3〜5 件ずつ ⑤ `S10`（`210`）は**判断待ちとして触らない** | 検査 **7**（`S-52` / `S-99d` を消したあと、それらを指す `` ` `` 付きの語が残っていないか）／検査 **9**（行数を述べる散文）／検査 **13**（いま **4** 件。**増えていないこと**）／**重複検出が `new 0` を保つこと**／`EZ-2` と `LM-12` を読み直す |

**束をまたぐ順序（守らないと収束しない）**

```
A（席の再配当）
   ↓
B（CR-105 → CR-107）      ←  CR-107 は自分で「CR-105 の後」と書いている
   ↓
C（CR-109）
   ↓
D（CR-110）
   ↓
E（CR-106）               ←  B・C・D のアンカーを壊すので必ず後
   ↓
F（CR-114 資産 ＋ S-100） ←  CR-114 は自分で「最後に回す」と書いている
```

---

## 5. `AG-11` ↔ `AG-6` —— 片方だけ触った跡を読み直した（利用者の指示による）

### 5-1. いまの本文（実ファイルから引いた）

> `AG-6` | 監視は「**自分が最後に読んだ版より後で、かつ自分以外の書き手が確定した変更だけ**」を通知すること。**確定した発話（`AG-11`）もこれに含める（MUST）** —— 人は「待った」「なぜそうしたか」とだけ言うこともあり、そのときも起きなければ `UC-013` の手順 2 から 3 へ進まない。**自分の書き込みで自分が起きてはならない（MUST NOT）。確定していない下書きで起きてはならない（MUST NOT）**

> `AG-11` | **対話欄で人が確定した発話を読めること（MUST）。** 発話は日程データではないので**版数を上げない**（`FR-063`）。**それでも監視は起きること**（`AG-6`）。**確定していない入力途中の文字を読めてはならない（MUST NOT）**

### 5-2. 判定

| 問い | 判定 |
|---|---|
| **明白な矛盾があるか** | **無い。** `AG-6` の「確定した発話（`AG-11`）もこれに含める（MUST）」が生きているので、`AG-11` の「それでも監視は起きること（`AG-6`）」と噛み合う。**2 閉路は規約どおり閉じている**（互いを行 ID で指すだけで、規則を書き写していない） |
| **選別の条件は評価できるか** | ⚠️ **本文からは決まらない（未検証）。** `AG-6` の選別条件は「**自分が最後に読んだ版より後**」で始まる。`AG-11` が版数を上げない以上、**発話は「版より後」を版番号では判定できない。** 「版より後」が**時刻の順**を指すのか**版番号の順**を指すのかは、`AG-6` にも `FR-063` にも `FR-064` にも書かれていない |
| **いま直すべきか** | **いまは直さない。** 上の穴は発話 1 件ぶんで、`AG-6` の MUST が受け止めている |

### 5-3. ⚠️ CR-110 を入れると、この穴が広がる

**CR-110 は版数の判定を「`schedule` 群が変わったとき」に変える。**
すると **`documentSettings` の変更が全部、版数を上げない側に回る。**
`AG-6` の選別条件は「版より後」のままなので、
**版数を持たない変更の集合が「発話 1 件」から「`documentSettings` 全体」へ広がる。**

**CR-110 は同じ帰結を `AG-2`（楽観ロック）については §6-5 ② で挙げ、別 CR へ送っている。**
**`AG-6` については挙げていない。** —— `impact.py` は `AG-11` を指す要求を **1 件 / 参照 1 箇所**としか答えないので、
**閉路の相手（`AG-6`）は「指してくる側」に現れない。** 手で閉路を辿らないと出てこない。

**したがって（本書の結論）** ——

> **束 D では `AG-6` と `AG-11` を同じ 1 回で書くこと（MUST）。**
> `AG-11` だけを書き換えると、**同じ型の作業を 3 度目に繰り返すことになる**
> （1 度目 = `CR-103` の適用、2 度目 = `CR-110` が `AG-11` だけを書き換える予定）。
> `AG-6` を書き換えないと決めるなら、**「書き換えない」と判定した記録を残すこと。**

---

## 6. 削除しかしていないのに新しい指摘が生まれる —— 同じ型を繰り返さないために

### 6-1. 仕組み（利用者が挙げた実例の一般形）

**`CR-114` は文を消すだけである。それでも重複検出が新規 1 件を出した。**
**理由は、重複検出が「似た言い回し」を測るからである。**
**区別を担っていた 1 文を消すと、残りの文どうしの類似度が上がる。**
**削除は類似度を下げない。上げる。**

→ **束ごとに `check.sh` を回す（`A=18 / new 0` を保つ）。**
**とくに束 F は 14 か所の削除であり、最も新規指摘が出やすい。**

### 6-2. ⚠️ 本作業で見つけた、同じ型の 2 件目（束 E と束 F をまたぐ）

**`S-100` の注は、いまこう書いてある** ——

> `S-100` | 既定の透かし解除パスワード | `gr-scheduler` | … ⚠️ **要望 55-3 は `GoodRelax` を指定していたが、汎用の道具に固有名を既定値として置かないため製品名に変更した**（確定 2026-08-13）

**`CR-114` の `S15` は、これをこう書き換えると定めている**（`X2-cr114-assets.md`）——

> ⚠️ **汎用の道具に固有名を既定値として置かないため、既定値は製品名とする。**

**一方 `CR-106` は「本仕様書が仕様を定めるソフトウェアの名称は `GoodRelax Scheduler`」と宣言する。**

**両方を適用すると、注は「既定値は製品名である」と述べ、値は `gr-scheduler` になる。**
**`gr-scheduler` はリポジトリ名であって、宣言された製品名（`GoodRelax Scheduler`）ではない。**
**削除と書き直しだけで、いま存在しない矛盾が生まれる。**

⚠️ **`CR-106` 自身が §6-9 の 1 番でこれを「未決」として記録している。**
**`CR-114` の `S15` はそれを知らずに「既定値は製品名とする」と書いている。**
**2 本の CR は互いを見ていない。**

**採れる道は 3 つある（本書は決めない。利用者判断）** ——

| 案 | 中身 | 触る範囲 |
|---|---|---|
| ① | **値を変える** | `S-100` の値。⚠️ **8 メンバー閉路の中**（`LM-12` が「既定値そのものが本仕様書に書いてある」と述べている） |
| ② | **注の文言から「製品名」を外す**（例:「固有名を既定値として置かないため」で止める） | `S-100` の注だけ。閉路の他のメンバーは動かない |
| ③ | **`gr-scheduler` はリポジトリ名であると明記する** | `S-100` の注 ＋ 用語辞書（`U-45` の周辺） |

**②が最も範囲が狭い。** ただし **`S15` の「除く理由」は「既定値 `gr-scheduler` を採る理由そのものである」と書いており、
理由を残すことが `S15` の主旨である。②はその主旨を保つ。** —— **これは本書の読みであり、確定ではない。**

### 6-3. `S-124` の側（`EZ-2` ↔ `S-124` の 2 閉路）

**`CR-114` の `S16` は `S-124` の注から「既定 3 秒は利用者の指定（2026-08-13）」を落とす。**
**閉路の相手 `EZ-2` はこう書いてある** ——

> `EZ-2` | **アイコンにポインタを合わせて一定時間が経ったら、そのアイコンの説明を出すこと（MUST）。** 待ち時間は `_assets/tbl-settings.md` の `S-124` が持つ

**`EZ-2` は値を持たず `S-124` を指すだけ。`S-124` は値を持ち `EZ-2` を指すだけ。**
**`SKILL.md` の言う「値の行と、その規則を持つ要求が作る 2 閉路は規約であって欠陥ではない」に当たる。**
**`S16` は値も指し先も動かさないので、この閉路は壊れない。** ただし ——
⚠️ **削除後、`3000` の由来を語る文は行頭の 🔎 だけになる。**
**`S1` / `S2`（🔎 の定義）を同じ束で触るので、🔎 の定義文が生きたままであることを確かめること。**

---

## 7. 束 E（`GRS` 置換）が、ほかの束を壊す —— なぜ最後に置くのか

### 7-1. 件数は 3 つの出どころで食い違っている（自分で数え直した）

| 語 | **本書の実測（2026-08-14）** | CR-106 の表明 | 作業指示にあった数 |
|---|--:|--:|--:|
| 「システムは」 | **179** | 180 | 215 |
| 「システムが」 | **20** | 20 | — |
| 「本ソフトウェア」 | **15**（要求 12 ／ 設計 1 ／ 辞書 2） | 10（要求 8 ／ 辞書 2） | — |
| 「システム」総数 | **206**（要求 205 ／ `A-appendix` 1） | 207 | — |

**数え方**: 6 ファイル（`01-04-requirements.md` / `05-07-design.md` / `08-10-test.md` / `A-appendix.md` /
`_assets/tbl-glossary.md` / `_assets/tbl-settings.md`）に `grep -o` を掛けて出現数を数えた。
**`docs/spec/output/` と `docs/spec/__pycache__/` は読んでいない。**

⚠️ **3 つとも違う。** CR-106 の起票後に他の CR が本文を書き換えたためである。
**とくに「本ソフトウェア」は 10 → 15 に増えており、`05-07-design.md` にも 1 件現れた**（CR-106 は同ファイル 0 件と実測していた）。
**CR-106 の「置換後は 0 件」という表明は、件数を数え直してからでないと使えない（MUST）。**

### 7-2. 束 E を先にやると壊れるアンカー（実測）

| 束 | 壊れるアンカー | 中身 |
|---|---|---|
| **B（CR-105）** | 置換 1 | `本ソフトウェアは、作図ソフトと同じ操作感で日程表を描きながら、…` —— **`本ソフトウェア` を含む**。束 E が先だと 0 件一致になる |
| **C（CR-109）** | `FR-004` の `STATEMENT` | `作成者が行見出しパネルを操作したとき、システムは、` —— **`システムは` を含む** |
| **D（CR-110）** | `FR-024` / `FR-021` / `FR-063` の `STATEMENT` | 3 文とも **`システムは` を含む** |

**逆向きの壊れ（束 E を後にしたときに起きる）** ——

| 束 | 何が起きるか | 対処 |
|---|---|---|
| **B（CR-105）** | 置換 1 が `本ソフトウェア` 1 件を**消す** | 束 E の「本ソフトウェア」の件数が 1 減る。**数え直せば足りる** |
| **B（CR-107）** | `NFR-013` の `STATEMENT` が **`システムは、` で始まる** | 束 E が後なら、この 1 件も一緒に `GRS` へ置換される。**正しい向き。** 束 E を先にすると `システムは` が 1 件だけ復活する |
| **C（CR-109）** | `FR-004` の置換後の文も `システムは、` を保つ | 同上 |
| **D（CR-110）** | `FR-024` / `FR-021` / `FR-063` の置換後の文も `システムは` を保つ | 同上 |

**結論** —— **束 E は後に置く。** 前に置くと 4 本の CR のリテラルを全部書き直すことになり、
**「置換元は 1 回だけ一致する」という各 CR の安全装置が全部無効になる。**

### 7-3. 束 E と束 F・C が同じ場所を触る

| 場所 | 触る束 | 中身 |
|---|---|---|
| `_assets/tbl-glossary.md` の `Agent API` の注（引用ブロック） | **E** と **F** | E は同ブロック内の「本ソフトウェアは AI 推論の実行系を持たない」の引用を置換／F は同ブロックの `G4`（`2026-08-12` に確定）と `G6`（識別子の確定日）を落とす。**行は違うが同じ引用ブロックである** |
| `_assets/tbl-glossary.md` の `U-45` の行 | **E** と **C** | E は `U-45` の説明文から「本ソフトウェア」を外す／C は `| U-45 |` の**直後**に新しい行を挿す。**C を先にしても `| U-45 |` は残るので E のアンカーは壊れない**（実測で `n=1`） |

⚠️ **`CR-106` は §6-5 で「辞書の引用が `XO-4` の原文とすでにずれている」と記録し、直さないと決めている。**
**`CR-114` も直さない。** —— **どちらの CR も直さないので、このずれは残る。**
**残すと決めるなら、残すと判定した記録を残すこと。**

---

## 8. 未検証・未決（本書では決めない）

| # | 中身 | 状態 |
|---|---|---|
| 1 | **0xx 表の再配当先。** 5 枚要るが、利用者の示した対応表は 4 枚ぶんしか埋まっていない（CR-110 の `DF-` と設定値側の 1 枚が空欄） | **未決** |
| 2 | **設定値ファイルに 0xx 帯の表番号を置いてよいか。** 実測では `tbl-settings.md` は `T-2xx` だけを使っている。帯が明文の規約かは**未検証** | **未検証** |
| 3 | **`AG-6` の「版より後」が時刻の順か版番号の順か。** CR-110 でこの穴が広がる（§5-3） | **未検証** |
| 4 | **`S-100` の既定値 `gr-scheduler` と、宣言される製品名 `GoodRelax Scheduler` の関係**（§6-2 の 3 案） | **未決** |
| 5 | **`GL-007`（秘密の日程を画面に出した証跡）を受ける課題行が無い。** `GL-007` は `S-100` と同じ 8 メンバー閉路のメンバーであり、束 B と束 F が同じ閉路の別のメンバーを触ることになる | **未検証**（CR-105 §6-6 の開いた問い 2 と同じ） |
| 6 | **接頭辞の総数。** 本書は数え直していない。CR-105 / CR-107 / CR-109 / CR-110 がそれぞれ **67** と実測し、指示は **76**、CR-110 は数え方を変えると **76** になると書いている。**`PLM` / `CH` / `VK` / `CO` / `HF` / `DR` / `DF` が 0 件であることは、接頭辞の集合ではなく綴りの全数検索で直接確かめた**ので、この差は席の判定に効かない | **未検証** |
| 7 | ~~重複検出の `T-011 V-10` ↔ `UC-014`（0.47）の行方~~ **→ 解決した。** `git diff docs/review/duplication-baseline.txt` に **`01-04-requirements.md/T:T-011 V-10 + 01-04-requirements.md/UC-014` が 1 行追加されている**。`dup-check.py` は基準線を**読むだけで書かない**（`open(BASELINE)` は読み、書くのは `REPORT` だけ。ソースを読んで確認）ので、**別の作業者が基準線へ取り込んだ**ものである。だから本書の測定は `new 0` になった。**指摘が消えたのではなく、基準線が動いた** | **解決** |
| 8 | **`CR-110` が別 CR へ送る 6 件**（`FR-077` の LOD と画像／`AG-2` の楽観ロック／`scrollGroupId` と `FR-032`／`Project.revision`／`importSeq`（`MG-13` ↔ `S-71` の 2 閉路）／`themePreference` と `themeMonochrome`） | **本書の 6 束の外** |

---

## 9. 一言でいうと

- **編集対象どうしの閉路は 1 個しかない（`FR-018` ↔ `FR-055`）。** 参照まで含めると 6 個・最大 25 メンバーに膨らむが、それは「同じものを読む」であって「同じものを書く」ではない。
- **本当に危ないのは閉路ではなく席番号だった。** **4 本の CR が、すでに埋まっている `T-050` を採ろうとしている。**`U-46` と `HM-10` も埋まっている。
- **`CR-110` の「変更前の仕様」は 2 か所が実ファイルと一致しない。** `CR-103` 適用前の本文を引いている。
- **`AG-6` は、いまは矛盾していない。** ただし `CR-110` を入れると穴が広がるので、**`AG-11` と同じ 1 回で書くこと。**
- **削除しかしない `CR-114` が、`CR-106` の製品名決定と噛み合って新しい矛盾を作りうる**（`S-100`）。**2 本の CR は互いを見ていない。**
