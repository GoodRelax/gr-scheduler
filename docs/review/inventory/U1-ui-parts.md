# U1 — `ui-parts-ja.md` の UI パーツ名 全数突合

- **担当ファイル**: `previous-project-result/03-ui-naming/ui-parts-ja.md`（**741 行・全文を読んだ**）
- **突合先（名前の正）**: `docs/spec/_assets/tbl-glossary.md`（**実測 259 行**。指示は「185 行」だったが、実ファイルは 259 行で `U-` 行は 41 行ある。`wc -l` と `grep -c "^| U-"` で確認した）
- **表記の規約**: `docs/spec/01-04-requirements.md` 1.9（表 T-006a / T-006b）を読んで適用した
- 作成日: 2026-08-14
- ⚠️ **本書は名前だけを取った。** `previous-project-result` のコード・HTML は 1 行も写していない。
- ⚠️ **`docs/` は 1 文字も編集していない。**

## 0. 数（自分で数えた）

| 区分 | 件数 |
| --- | --- |
| **抽出した UI パーツ名（全数）** | **51** |
| 用語辞書にあるもの | 37 |
| 用語辞書に無いもの | 14 |

**判定の内訳（合計 51）**

| 判定 | 件数 |
| --- | --- |
| 一致 | 35 |
| 衝突 | 3 |
| 要改名 | 1 |
| 綴りが違う | 1 |
| 辞書に無い（追加が要る） | 11 |

**検算**: 一致 35 ＋ 衝突のうち辞書にある 2（`Assignee Label` / `Row Title Tree`）＝ 37（辞書にある）。
衝突のうち辞書に無い 1（`Bar`）＋ 要改名 1（`Today Line`）＋ 綴りが違う 1（チャット欄）＋ 辞書に無い 11 ＝ 14（辞書に無い）。37 ＋ 14 ＝ 51。

**数え方の宣言**（数が食い違ったときに再現できるようにする）

- 1 行に 2 つの名前が入っているものは **2 件**として数えた（`Help Modal` / `AI Export Modal`、`開始点` / `終了点`）。
- `§2-1-3` の表は**表の行が 30 行**で、そこに入っている**英語名は 31 個**である（最後の 1 行が 2 名）。
- `Header Commands` の個別ボタン（`Fit` / `Cmd` / `SS` / `Load` / `Save` / `Theme` / `Base` / `Undo` / `Redo` / `AI` / `?` の 11 個）は、**確定名として与えられておらず日本語名も無い**ので、**まとめて 1 件**として数えた（内訳は備考に書いた）。
- 形状の値（`rectangle` / `chevron` / `arrow` / `endpointSpan` / `milestone`）は **UI パーツではなくプロパティの値**なので数えていない（辞書でも T-102 にある）。
- 設定値のキー（`§2-1-6` の全数）は **UI パーツではない**ので数えていない。ただし UI パーツ名と語幹が繋がるものは §5 に注記した。

---

## 1. 突合表（全 51 件）

### 1-1. `§2-1-3` 日英対応表（30 行 ＝ 31 名）

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| `Rows` | 行 | §2-1-3 / §3 木 / §4 | ある | U-1 | 一致 | ⚠️ **T-006b `A-2` に触れる。** 日本語名そのものが曖昧語「行」である。`Rows`（UI）と `TaskGroup`（データ）のどちらかを必ず明示すること |
| `Task Bars` | タスクバー | §2-1-3 / §2-0 / §3 木 | ある | U-2 | 一致 | 名前は一致。⚠️ **定義がずれている** —— §3-1 の衝突①を見よ |
| `Plan Bar` | 予定バー | §2-1-3 / §2-0 / §3 木 | ある | U-3 | 一致 | T-006b `A-14` は「バー」単独を禁じるが、本名は限定付きなので可 |
| `Actual Bar` | 実績バー | §2-1-3 / §2-0 / §3 木 | ある | U-4 | 一致 | 同上 |
| `Progress Marker` | 進捗マーカー | §2-1-3 / §3 木 | ある | U-5 | 一致 | ⚠️ T-006b `A-5`（マーカー）と `A-6`（進捗）の**両方に触れる**が、限定付きなので可。「マーカー」「マーク」と略さない |
| `Resume Icon` | 再開アイコン | §2-1-3 / §3 木 | ある | U-6 | 一致 | — |
| `Name Label` | 名称ラベル | §2-1-3 / §3 木 | ある | U-7 | 一致 | — |
| `Assignee Label` | 担当ラベル | §2-1-3 / §3 木 | ある | U-8 | **衝突** | ⚠️ **§3-1 の衝突②。** 木は「担当と完了率」を 1 パーツで描くと書くが、仕様は 2 つのラベルとして扱う |
| `Progress Line` | イナズマ線 | §2-1-3 / §3 木 | ある | U-9（例外 X-1） | 一致 | 直訳しない例外。⚠️ T-006b `A-6` に触れるが、辞書が例外として認めている |
| `Cursors` | カーソル | §2-1-3 / §3 木 | ある | U-10 | 一致 | ⚠️ **T-006b `A-15` に触れる。** 日本語名そのものが曖昧語「カーソル」である。マウスが指す点は必ず「ポインタ」と書く |
| `Today Line` | 本日線 | §2-1-3 / §3 木 | **無い**（概念は `Status Line` として在る） | U-11 が受け皿 | **要改名** | ⚠️ **辞書 U-11 が「本日線」と呼ぶことを MUST NOT で禁じている。** `Status Line` / 基準日線 へ改名する。`A-appendix.md` 0.6（2026-08-13）に「本日線と基準線を基準日線 1 本へ統合」とあり、統合は済んでいる |
| `Dual Cursor` | デュアルカーソル | §2-1-3 / §3 木 | ある | U-12 | 一致 | — |
| `Guide Cursor` | ガイドカーソル | §2-1-3 / §3 木 | ある | U-13 | 一致 | 設定値 `guideCursorMode` と語幹が一致している |
| `Comment Boxes` | コメントボックス | §2-1-3 / §3 木 | ある | U-14 | 一致 | 辞書は「コメント」と略すことを MUST NOT で禁じる。2026-08-14 決定の型名 `CommentBox` / 鍵 `commentBoxes` とは**面が違うだけで語幹は一致**（T-006a `W-1` / `W-2` / `W-6`）。⚠️ ただし**型名と鍵は辞書にまだ無い**（§4 を見よ） |
| `Highlight Boxes` | ハイライトボックス | §2-1-3 / §3 木 | ある | U-15 | 一致 | 辞書は「囲み枠」を MUST NOT で禁じる。型名 `HighlightBox` / 鍵 `highlightBoxes` は同上 |
| `Dependency Lines` | 依存線 | §2-1-3 / §3 木 | ある | U-16 | 一致 | — |
| `Date Grid Lines` | 日付罫線 | §2-1-3 / §3 木 | ある | U-17 | 一致 | — |
| `Group Grid Lines` | グループ罫線 | §2-1-3 / §3 木 | ある | U-18 | 一致 | — |
| `Time Ruler` | タイムルーラー | §2-1-3 / §3 木 | ある | U-19 | 一致 | — |
| `Watermark` | 透かし | §2-1-3 / §3 木 | ある | U-20 | 一致 | 辞書は別に `WatermarkPassword`（U-37）を持つ。ui-parts 側には無い（辞書側の増分） |
| `Scrollbars` | スクロールバー | §2-1-3 / §3 木 | ある | U-21 | 一致 | 「バー」を含むが複合語であり T-006b `A-14` の対象外と読む（**未検証** —— 規約に明文が無い） |
| `Row Title Panel` | 行見出しパネル | §2-1-3 / §3 木 / §4 | ある | U-22 | 一致 | 設定値 `rowTitlePanelWidth` と語幹が一致 |
| `Row Title Tree` | 行見出しツリー | §2-1-3 / §3 木 / §4 | ある | U-23 | **衝突** | ⚠️ **§3-1 の衝突③。** 木は操作点をツリーに置くが、辞書 U-23 は「操作の入口を指すときは行見出しパネルと書くこと（MUST）」 |
| `Panel Divider` | パネル境界 | §2-1-3 / §3 木 | ある | U-24 | 一致 | — |
| `Properties Panel` | プロパティパネル | §2-1-3 / §3 木 | ある | U-25 | 一致 | 設定値 `propertyPanelWidth` は `Propert**ies**` と綴りが揃っていない（**辞書 K-72 も同じ綴り**なので、辞書どおりなら一致。改名するかは §5 の申し送り） |
| `Command Palette` | コマンドパレット | §2-1-3 / §3 木 | ある | U-26 | 一致 | 画面に出す文字列は §5-2 で「未決」のまま。辞書も持たない |
| `Schedule Title` | 文書名 | §2-1-3 / §3 木 | ある | U-27 | 一致 | 辞書は「表題」「題名」を MUST NOT で禁じる。⚠️ 辞書 U-27 は説明として MSPDI の `Project/Title` を書いている（§4 の交換相手綴りの論点） |
| `Autosave Status` | 自動保存の状態 | §2-1-3 / §3 木 | ある | U-28 | 一致 | — |
| `Hidden Group Tab` | 非表示グループタブ | §2-1-3 / §3 木 / §4 | ある | U-29 | 一致 | — |
| `Help Modal` | ヘルプ | §2-1-3 / §3 木 | ある | U-30 | 一致 | — |
| `AI Export Modal` | AI 出力 | §2-1-3 / §3 木 | ある | U-30 | 一致 | 辞書 U-36 の `Agent API` とは別物（前者は画面、後者は口）。混ぜないこと |

### 1-2. `§2-0`（`Bar` の定義）

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| `Bar` | バー（総称） | §2-0 | **無い** | — | **衝突** | ⚠️ **§3-1 の衝突①。** T-006b `A-14` は総称の座を `Task Bars` に与えており、`Bar` という総称は辞書に存在しない。**日本語名が曖昧語「バー」そのもの**でもある |

### 1-3. `§3` UI パーツ木の構造名（画面に出ないもの）

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| `App Shell` | （当てない） | §3 木 | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** 辞書は `App Header`（U-31）を持つが、その親の器を持たない。`docs/spec` 全体で `App Shell` は **0 箇所**（`grep -o` で確認）。⚠️ 追加しないなら「木の根に名前が無い」状態のまま UI 設計に入ることになる |
| `App Header` | （当てない） | §3 木 | ある | U-31 | 一致 | 設定値 `appHeaderMaxHeight`（K-100）と語幹が一致 |
| `Branding` | （当てない） | §3 木 | ある | U-35 | 一致 | 木の責務は「製品名と著作権表示」。**製品名は 2026-08-14 に GoodRelax Scheduler (GRS) と決まった**ので、ここに入る文字列が確定した（名前の衝突ではない） |
| `Header Commands` | （当てない） | §3 木 | ある | U-35 | 一致 | — |
| `Schedule Canvas` | （当てない） | §3 木 | ある | U-32 | 一致 | ⚠️ **T-006b `A-8` に触れる。** 日本語名を当てないため、散文で「キャンバス」と書くと `exportCanvas` / `canvasPadding` と区別できない。**常に英名で書く**運用が要る |
| `Grid Lines` | （当てない） | §3 木 | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** 辞書は子（U-17 / U-18）だけを持ち、2 つをまとめる親を持たない。`docs/spec` 内の `Grid Lines` は U-17 / U-18 の一部としての 2 箇所だけ（`grep -o` で確認）。**親が無いと「罫線をまとめて消す」ような要求を書く先が無い** |
| `Canvas Overlays` | （当てない） | §3 木 | ある | U-33 | 一致 | ⚠️ 木の説明文が「`Items` から分離した重ね描き層」と**廃止語 `Items`** を使っている。辞書の `Item`（N-4a）は**当たり判定と選択の対象の総称**という別の意味なので、**読み違えないこと**（§3-1 の注意） |
| `Palette Groups` | （当てない） | §3 木 | ある | U-34 | 一致 | — |
| `Palette Commands` | （当てない） | §3 木 | ある | U-34 | 一致 | — |
| `Modals` | （当てない） | §3 木 | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** 辞書は `Help Modal` / `AI Export Modal`（U-30）と `Hidden Group Tab`（U-29）を持つが、3 つを束ねる親を持たない。`docs/spec` 内に `Modals` は **0 箇所** |
| （英名なし） | `Header Commands` の個別ボタン 11 個 | §3 木 539 行 | **無い** | — | **辞書に無い** | 内訳は `Fit` / `Cmd` / `SS` / `Load` / `Save` / `Theme` / `Base` / `Undo` / `Redo` / `AI` / `?` の **11 個**。**確定名でも日本語名でもなく、画面の文字列としてしか書かれていない。** ⚠️ **`SS` / `Cmd` / `Base` は何の略かが本文のどこにも無い（未検証）。** 1.9 の「大文字の略語を識別子に入れてはならない」は識別子の規約であって画面文字列には掛からないが、**`Cmd` は `Command Palette` を、`Base` は `baselineVisible`（変更前の予定）を指すと推測できるだけで、確かめられない。** T-103 に 11 行として足すか、`Header Commands` の 1 行の備考として足すかは判断が要る |

### 1-4. `§3` の「未収録」4 件（2026-08-04 時点で木に入っていないと明記されているもの）

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| （英名なし） | チャット欄 | §3 未収録表 | **無い** | — | **綴りが違う** | ⚠️ **仕様書は「対話欄」と書いている**（`01-04-requirements.md` `UC-013` 手順 2 と表 T-035 `AG-11` の **2 箇所**）。前資産は「チャット欄」。**同じものを 2 語で呼んでいる。** 仕様書側の「対話欄」を採り、**英名を決めて T-103 に足す**必要がある（現状 T-103 に行が無い） |
| （英名なし） | 有効化の面 | §3 未収録表 | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** 中身は仕様書の表 T-034 `NT-4`（起動時の 1 枚）に吸収されているが、**面（画面）の名前としては辞書に無い** |
| （英名なし） | 起動時の面 | §3 未収録表 | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** 仕様書 `NT-4`「起動時の保留中の用件」は**要求としては在る**が、**その 1 枚を指す UI パーツ名が無い。** UI 設計で必ず名前が要る |
| `Toast` | 通知 | §3 未収録表 | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** `docs/spec` 内に `Toast` は **0 箇所**。一方で通知の作法は表 T-034（`NT-1` 〜 `NT-6`）に全数ある。**規則だけあって器の名前が無い状態。** ⚠️ 前資産は「表示位置・滞留時間・多重時の積み方が未定」と書いているが、`NT-2`（読み終える前に消えない）は決まっているので、**未定なのは位置と積み方だけ**（滞留時間は `NT-2` が縛る） |

### 1-5. `§2-1-5` / `§4-2(c)` が名前として立てているもの

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| （英名なし） | ポインタ | §2-1-5「カーソル」の行 | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** ⚠️ **T-006b `A-15` が「マウスが指す点は必ずポインタと書く」と定めているのに、`ポインタ` という語そのものは用語辞書に 1 行も無い。** 規約（要求文書）にはあり、名前の正（辞書）には無い。`Cursors`（U-10）の対概念なので、**辞書に並べて置かないと片方だけ読んだ者が取り違える** |
| （英名なし） | 掴み点 | §2-1-5「端点」の行 / §4-2(c) | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** T-006b `A-13` が「端点＝単独で書いたらバーの端の掴み点」と定めており、**フェードの掴み点（表 T-023d `GR-1`/`GR-2`）とは別物**とも書いている。⚠️ **掴み領域の全数は `previous-project-result/03-ui-naming/ui-detail-spec-ja.md` §4-1 にあり、本 U1 の担当外**である（未読・**未検証**） |
| （英名なし） | 開始点 | §4-2(c) | **無い** | — | **辞書に無い** | **T-103 に追加が要る。** §4-2(c) は「開始日/終了日＝データ、開始点/終了点＝画面で掴む場所。**混用禁止**」と定める。**辞書はデータ側（P-3 `start`/`finish`）しか持たず、画面側の 2 語を持たない** |
| （英名なし） | 終了点 | §4-2(c) | **無い** | — | **辞書に無い** | 同上 |

---

## 2. 「辞書に無い」14 件の追加先（T-101 〜 T-105 のどこに入れるか）

| # | 名前 | 入れる表 | 理由 |
| --- | --- | --- | --- |
| 1 | `App Shell` | **T-103** | 画面に出ない構造名。U-31 〜 U-35 と同じ扱い（日本語を当てない） |
| 2 | `Grid Lines` | **T-103** | U-17 / U-18 の親。同じく構造名 |
| 3 | `Modals` | **T-103** | U-29 / U-30 の親。同じく構造名 |
| 4 | `Header Commands` の個別ボタン 11 個 | **T-103**（U-35 の備考でも可） | 画面に出る文字列だが確定名が無い。**表に入れるか、入れないと決めるかを先に決める** |
| 5 | 対話欄（旧「チャット欄」） | **T-103** | 画面に出る領域。英名を決める必要がある |
| 6 | 有効化の面 | **T-103** | 同上 |
| 7 | 起動時の面 | **T-103** | 同上。`NT-4` が要求として在る |
| 8 | `Toast` / 通知 | **T-103** | 同上。表 T-034 が作法を全数持つ |
| 9 | ポインタ | **T-103** | `Cursors`（U-10）の対概念。⚠️ **英名は前資産にも無い（未検証）。** `Pointer` にするかは判断が要る |
| 10 | 掴み点 | **T-103** | 当たり判定の対象。⚠️ 辞書 N-4a の `Item`（当たり判定と選択の対象の総称）と**関係を書き分ける**必要がある |
| 11 | 開始点 | **T-103** | 掴み点の 1 つ |
| 12 | 終了点 | **T-103** | 掴み点の 1 つ |
| 13 | `Bar`（総称） | **T-103 か、廃止** | ⚠️ **足す前に衝突①を決着させる。** `Task Bars` が総称なら `Bar` は要らない |
| 14 | `Today Line` | **足さない（要改名）** | 概念は U-11 `Status Line` として既にある。**足すのではなく、前資産側の呼び名を捨てる** |

---

## 3. 衝突と、T-006b（曖昧な日本語）に触れる名前

### 3-1. 衝突 3 件（UI 設計に入る前に決着が要る）

**衝突① `Bar` と `Task Bars` が「総称」の座を争っている**

| どちらの文書 | 何と書いてあるか |
| --- | --- |
| 前資産 `ui-parts-ja.md` §2-0 | `Bar` ＝「**1 つの `Task` が占める横帯 1 本**と、そこに描かれるもの全体を指す**総称**」／ `Task Bars` ＝「`Rows` の子。**その行に載る `Task` 全部の `Bar` をまとめた描画層**」 |
| 名前の正 `01-04-requirements.md` T-006b `A-14` | 「バー」は「**`Task Bars`（総称）**／`Plan Bar`／`Actual Bar` のどれかを明示する」 |

**外延が違う。** 前資産では `Bar` が 1 タスク分、`Task Bars` が 1 行分（複数タスク）である。仕様書はその `Task Bars` を「総称」と呼んでいる。
⚠️ **これは前資産の中で既に食い違っていた** —— 同じ `ui-parts-ja.md` の §2-1-5 が仕様書と同じ文（`Task Bars`（総称））を持っており、§2-0 の定義と噛み合っていない。**仕様書はその食い違ったほうを引き継いでいる。**
**決着の候補**: ① `Bar` を捨て、1 タスク分を指す語を別に立てる ② `Task Bars` を 1 行分の層の名前に限り、総称は `Bar` として T-103 と T-006b `A-14` を直す。**どちらを採るかは利用者の判断が要る。**

**衝突② `Assignee Label` が 2 つのラベルを 1 名で覆っている**

| どちらの文書 | 何と書いてあるか |
| --- | --- |
| 前資産 §3 木 | `Assignee Label` ＝「**担当と完了率**。バーの外側左へ右揃えで連結」 |
| 仕様書 | `UC-003` 手順 4「**担当ラベルと完了率ラベル**を表示の設定に従って出す」／ 表 T-038 `OC-2`「**担当ラベルと完了率ラベル**」／ 「**完了率ラベル**に出すものを決める」という要求が独立してある |
| 辞書 | U-8 `Assignee Label` 担当ラベル のみ。**「完了率ラベル」の行が無い** |

仕様書は **2 つのラベル**として扱い、設定値も `assigneeVisible`（K-75）と `percentCompleteVisible`（K-76）で**別々に切り替える**。
⚠️ **T-006b `A-6` は「ラベルを指すなら『完了率ラベル』」と明記しているのに、その完了率ラベルの UI パーツ名が T-103 に無い。**
**要ること**: `Percent Complete Label` 相当の英名を決め、T-103 に足す。足したうえで `Assignee Label` の責務を「担当だけ」に狭める。

**衝突③ 行見出しの「操作の入口」がパネルかツリーか**

| どちらの文書 | 何と書いてあるか |
| --- | --- |
| 前資産 §3 木 | `Row Title Tree` ＝「`TaskGroup` 木。**畳み・並べ替え・表示切替の操作点**」 |
| 辞書 U-23 | 「`Row Title Panel` の中身（パネルが画面領域、ツリーがその中の木の表示）。⚠️ **操作の入口を指すときは『行見出しパネル』と書くこと（MUST）** —— 入口を 2 つの語で呼ぶと `FR-029` に触れる（確定 2026-08-13）」 |

**辞書の MUST が新しい（2026-08-13 確定）ので辞書が勝つ。** UI 設計では**操作の入口を `Row Title Panel` と書く**。前資産の木の記述をそのまま写すと MUST 違反になる。

### 3-2. ⚠️ 追加の注意 —— 前資産 `Items` を辞書の `Item` と読み違えないこと

前資産 §3 木の `Canvas Overlays` の説明に「**`Items` から分離した重ね描き層**」とある。この `Items` は**前プロジェクトの廃止語**（`item` → `Task` へ一本化。§4 の改名表「Canvas Items → `Task Bars` に統合」）である。
一方、辞書 N-4a の **`Item` は「当たり判定と選択の対象となるものの総称」**という**別の概念**で、⚠️ 辞書自身が「**`Task` の別名ではない**」と注記している。
**綴りがほぼ同じで意味が違う。** UI 設計で前資産の木を参照するとき、ここは必ず読み替えること。

### 3-3. T-006b（単独で使ってはならない曖昧な日本語）に触れる名前

**日本語名そのものが曖昧語である（＝最も危険）**

| 名前 | 日本語名 | 触れる行 | 何が起きるか |
| --- | --- | --- | --- |
| `Rows` | **行** | `A-2` | 「行」と書いたとき `Rows`（UI）か `TaskGroup`（データ）か読めない。⚠️ **木の説明が「`Rows` ＝ `TaskGroup` 1 つ分の横帯」なので、両方が同時に正しい状況が常時ある** |
| `Cursors` | **カーソル** | `A-15` | 「カーソル」と書いたときマウスのポインタと読まれる。`A-15` は「単独＝`Cursors`」と決めて逃げているが、**`ポインタ` が辞書に無い**ので片側しか守られていない |
| `Bar`（前資産のみ） | **バー** | `A-14` | 衝突①のとおり |

**限定付きなので使ってよいが、略すと壊れるもの**

| 名前 | 触れる行 | 略してはならない形 |
| --- | --- | --- |
| `Progress Marker` 進捗マーカー | `A-5` / `A-6` | 「マーカー」「マーク」「進捗」と略さない |
| `Progress Line` イナズマ線 | `A-6` | 「進捗」と略さない（辞書 X-1 の例外で日本語は「イナズマ線」） |
| `Comment Boxes` コメントボックス | （辞書 U-14 の MUST NOT） | 「コメント」と略さない |
| `Highlight Boxes` ハイライトボックス | （辞書 U-15 の MUST NOT） | 「囲み枠」と呼ばない |
| `Schedule Title` 文書名 | （辞書 U-27 の MUST NOT） | 「表題」「題名」と呼ばない |
| `Row Title Panel` / `Row Title Tree` | `A-2` | 「行」だけで指さない。⚠️ 衝突③ |
| 掴み点 / 開始点 / 終了点 | `A-13` | 「端点」と書いたら**バーの端の掴み点**。形状名は必ず「端点スパン」と 4 文字 |

**日本語名を当てないために曖昧語になるもの**

| 名前 | 触れる行 | 対処 |
| --- | --- | --- |
| `Schedule Canvas` | `A-8` | 日本語を当てない構造名なので、**散文では必ず英名で書く**。「キャンバス」と書くと `exportCanvas`（出力サイズ）/ `canvasPadding`（余白）と区別できない |
| `Canvas Overlays` | `A-8` | 同上 |

**⚠️ 隣接する語で新たに危ないもの（見つけたので記す）**

`Status Line` の日本語名は **「基準日線」**（辞書 U-11）。一方 T-006b `A-9` は **「基準線」**を「変更前の予定（`Baseline`）の訳語に限る」と定めている。
**「基準日線」と「基準線」は 1 文字違いで別物である。** 読み飛ばすと `Status Line` と `Baseline` が入れ替わる。
⚠️ 実際に `docs/spec/A-appendix.md` の版数 0.5 の行が「**性能は目標値と測定条件のみを定め、基準線は骨格で取り直す**」と書いており、これは**性能の基準値の意味**なので `A-9` に反している（`A-9` は「性能の実測値は『基準値』と書く」）。**担当外・`docs/` は編集していない。参考として報告する。**

---

## 4. 2026-08-14 の決定に照らした「要改名」

指示にある 5 つの決定を、担当ファイルの全文に当てて調べた。

| # | 決定 | 担当ファイル内の該当 | 判定 |
| --- | --- | --- | --- |
| 1 | 製品名は **GoodRelax Scheduler (GRS)** | `Branding` ＝「製品名と著作権表示」（§3 木 537 行）。**製品名の文字列そのものは前資産に書かれていない** | 改名不要。`Branding` に入る文字列が決まっただけ |
| 2 | 識別子は lowerCamelCase。**snake_case 例外 3 語を廃止** | ① `wbs_parent_uid` —— **2 箇所**（180 行 §2-1-2、616 行 §4）② `status_date` —— **1 箇所**（376 行、`Project.status_date`）③ `link_type` —— **0 箇所**（担当ファイルに無い） | **要改名。** `wbsParentUid` / `statusDate` へ。⚠️ **辞書 P-22 も `wbs_parent_uid` のまま**、**辞書 U-11 も `Project.status_date` のまま**、**`01-04-requirements.md` の T-006a `W-8` も 3 語を例外として掲げたまま**である。UI パーツでは `Status Line`（U-11）の説明文が直接影響を受ける |
| 3 | **MSPDI の綴りが現れてよいのは `carry` / `carryElements` の内側だけ** | ① `Project.status_date`（376 行）—— MSPDI の `StatusDate` を写した綴り ② 辞書 U-27 が説明として `Project/Title` と書いている ③ §2-0 が根拠として `HideBar` を引いている | **①は要改名**（#2 と同じ）。**②③は「説明としての引用」であり識別子ではない。** 決定が禁じているのが**識別子の綴り**か**文書中の引用**かは指示文からは読み取れない —— **未検証。利用者の判断が要る** |
| 4 | 注記の 2 型は **`CommentBox` / `HighlightBox`**、鍵は **`commentBoxes` / `highlightBoxes`** | 担当ファイルは UI パーツ名 `Comment Boxes` / `Highlight Boxes` しか持たない（型名も鍵も書かれていない） | **改名不要**（語幹が一致し、T-006a の `W-6` と `W-1`/`W-2` で面が違うだけ）。⚠️ **ただし `CommentBox` / `HighlightBox` / `commentBoxes` / `highlightBoxes` は用語辞書に 1 行も無い。** T-101（データの語）と T-102 相当への**追加が要る** |
| 5 | 真偽値は GRS 固有なら **is / has / can** で始める | 担当ファイル §2-1-6 の真偽値らしきキー: `assigneeVisible` / `percentCompleteVisible` / `dependencyVisible` / `progressMarkerVisible` / `progressLineVisible` / `dateGridLinesVisible` / `groupGridLinesVisible` / `baselineVisible` / `themeMonochrome` / `resumeValid` / `dualCursor` | **UI パーツではなく設定値キー・プロパティなので U1 の判定対象外。** ⚠️ **かつ、これらが真偽値だという確証は担当ファイルにも辞書にも無い**（型は `tbl-settings.md` が持つ）—— **未検証**。UI パーツ名と語幹が繋がる（`Assignee Label` ↔ `assigneeVisible` 等）ので、改名するなら**語幹の一致が崩れないか**を U1 の表と突き合わせること |

**そのほか、担当ファイルの中で辞書と食い違っていたもの（UI パーツではないが UI 設計に直に効く）**

| 前資産の書き方 | 辞書 | 判定 |
| --- | --- | --- |
| `Task.milestone`(bool) —— **2 箇所**（100 行 §2、638 行 §4-2(a)）。§3 木にも「milestone も含む」とある | **N-1 が「⚠️ 真偽値の `milestone` という列は持たない」と明記。** 表示は `shapeKind` が `'milestone'` のとき | **要改名（実質は廃止）。** 前資産の木・§2 の記述をそのまま UI 設計へ持ち込むと、存在しない列を前提にした画面になる |

---

## 5. UI 設計に入る前に決めるべきこと（本書の結論）

1. **衝突①** —— 「バー」の総称を `Bar` と `Task Bars` のどちらにするか。**辞書 T-103 と T-006b `A-14` の両方が動く。**
2. **衝突②** —— 完了率ラベルの UI パーツ名を決め、T-103 に足す。**足すまで `percentCompleteVisible`（K-76）が切り替える対象に名前が無い。**
3. **衝突③** —— 操作の入口は `Row Title Panel`（辞書 MUST が勝つ）。**前資産の木の記述を写さない。**
4. **辞書に無い 11 件を T-103 に足す** —— とくに **`Toast` / 起動時の面 / 対話欄 / ポインタ / 掴み点・開始点・終了点** は、要求（表 T-034、`UC-013`、T-006b `A-13`/`A-15`）が既に**その物を前提にしているのに名前だけが無い**。UI 設計の最中に名前を作ると、また名前が 2 つに割れる。
5. **`Today Line` / 本日線を捨てる** —— `Status Line` / 基準日線 に統一済み。
6. **snake_case 3 語の廃止を辞書へ反映する** —— 辞書 P-22（`wbs_parent_uid`）と U-11（`Project.status_date`）、および `01-04-requirements.md` の T-006a `W-8` が旧綴りのまま残っている。**`docs/` は本作業では編集していない。**
7. **`SS` / `Cmd` / `Base` の正体を確かめる** —— `Header Commands` の 11 ボタンのうち 3 つが**何の略か分からない（未検証）**。UI 設計で必ず必要になる。

---

## 6. 本書の限界（推測を断定しないための記録）

- **担当は `ui-parts-ja.md` 1 ファイルのみ。** 同じ `03-ui-naming/` にある `ui-detail-spec-ja.md`（掴み領域の全数 §4-1、スクロールバー §5-2、ルーラー §6-3 を持つと本文が言及）は**読んでいない**。**掴み点の全数は未検証**である。
- **辞書側の増分は突合していない。** 辞書 T-103 にあって前資産に無い名前（`Annotations` U-15a、`Agent API` U-36、`WatermarkPassword` U-37、`ArmedShape` U-38、`Selection` U-39、`Marquee` U-40、`Status Line` U-11）は、**本書の 51 件には数えていない**（本書は前資産 → 辞書の向きで数えた）。逆向きの突合は別途要る。
- **`tbl-settings.md` は読んでいない。** 真偽値かどうか、既定値、型はすべて**未検証**である。
- 表 T-006b の全 15 行（`A-1` 〜 `A-15`）のうち、UI パーツ名が触れたのは `A-2` / `A-5` / `A-6` / `A-8` / `A-9` / `A-13` / `A-14` / `A-15` の **8 行**である。残る 7 行（`A-1` 段、`A-3` レベル、`A-4` 深さ、`A-7` ベースライン、`A-10` 太さ、`A-11` 幅、`A-12` 期間）は、**設定値キーとプロパティの日本語欄には触れるが、UI パーツ名には触れない。**
