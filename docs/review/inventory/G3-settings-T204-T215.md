# G3 — 表 T-204 出力 / 表 T-205 LOD / 表 T-206 〜 T-215

**担当**: 表 T-204・T-205 と、表 T-206 〜 T-215 の全鍵を、利用者判断（2026-08-14）の 2 群へ割り当てる。

> **版数（revision）を上げるのは、日程のデータが変わったときだけ。見せ方が変わっただけでは上げない。**
>
> | 群 | 何が入るか | 版数 |
> |---|---|---|
> | schedule | 個別の対象に付いた意味。日程・タスク・依存・**暦**・資源・割当・行の器（TaskGroup）・個別のタスクの色と形状・注記・出自 | **上げる** |
> | documentSettings | 全体の見せ方。ズーム・スクロール・パネル幅・表示切替・**寸法の既定**・**LOD のしきい値** | **上げない** |
>
> 例外: `themeHue` は schedule 側（プロジェクトの属性であって見る人の好みではない）。

---

## 0. 読んだ原典と、自分で数えた数

| # | ファイル | 読んだ範囲 |
|:--:|---|---|
| 1 | `docs/spec/_assets/tbl-settings.md`（332 行） | **全文**。表 T-204 〜 T-215 は 172–333 行 |
| 2 | `docs/spec/01-04-requirements.md`（3728 行） | **部分**。`FR-004` / `FR-020` / `FR-025` / `FR-026` / `FR-038` / `FR-039` / `FR-051` / `FR-053` / `FR-054` / `FR-063` / `FR-065` / `FR-071` / `FR-075` / `FR-084` / `FR-086` / `FR-088` / `FR-092` の各 STATEMENT と、表 T-005b（`ST-7`）・表 T-018（ラグ）・表 T-024（`IO-3`〜`IO-5`）・表 T-041（`WY-1`）・表 T-008（`XO-3`）の該当行のみ |
| 3 | `docs/reference/mspdi/mspdi_pj12.xsd` | **部分**。`LinkLag` / `LagFormat`（2196–2210 行）のみ |
| 4 | `previous-project-result/02-data-model/grs-document-settings-ja.md`（631 行） | **機械検索のみ**（§9 の 18 鍵が 0 件であることの確認） |
| 5 | `previous-project-result/temp/inventory/E09-settings-blob.md`（576 行） | **全文**（突き合わせ対象） |

> ⚠️ 2・3・4 は全文を読んでいない。本書がそれらに基づいて書いた主張は、読んだ行の範囲に限る。

### 0-1. 担当範囲の行数（自分で数えた）

`grep -c "^| S-"` を表ごとの行範囲に掛けて数えた。**目視ではない。**

| 表 | 行範囲（tbl-settings.md） | 行 ID | 行数 |
|---|---|---|--:|
| T-204 出力 | 178–182 | `S-81` `S-82` | **2** |
| T-205 LOD のしきい値 | 191–200 | `S-83`〜`S-89` | **7** |
| T-206 保存しないもの | 219–238 | `S-90`〜`S-99g` | **17** |
| T-207 透かし | 241–246 | `S-100`〜`S-102` | **3** |
| T-208 予実の補助線 | 323–327 | `S-103`〜`S-105` | **3** |
| T-209 既定の暦 | 313–317 | `S-106`〜`S-108` | **3** |
| T-210 フェードの掴み点 | 303–307 | `S-109`〜`S-111` | **3** |
| T-211 保存と受け入れの上限 | 263–269 | `S-112`〜`S-115` `S-125` | **5** |
| T-212 画面の寸法と待ち時間 | 256–259 | `S-124` `S-116` | **2** |
| T-213 依存のラグ | 275–278 | `S-117` `S-118` | **2** |
| T-214 受け入れる日付の範囲 | 284–287 | `S-119` `S-120` | **2** |
| T-215 文字サイズ | 291–295 | `S-121`〜`S-123` | **3** |
| | | **担当の行 合計** | **52** |
| | | ＋ 入れ子の葉（`exportCanvas.width` / `.height`） | **2** |
| | | **分類した実体 合計** | **54** |

参考: `tbl-settings.md` 全体の `S-` 行は **132**（機械で数えた。E09 §9-1 の 132 と一致する）。

### 0-2. 判定の理由に付ける印

| 印 | 意味 |
|:--:|---|
| 【規】 | **利用者規則（2026-08-14）が群を名指ししている。** 原典を要しない |
| 【明】 | **原典に明記がある。** 出典の行を挙げる |
| 【類】 | **類推である。原典に明記が無い。** 断定ではない |
| 【無】 | **原典に記述が無い。** 判定不能の理由になる |

---

## 1. 表 T-204 — 出力（2 行 ＋ 入れ子の葉 2）

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `exportCanvas` | `S-81` | **documentSettings** | 【明】`FR-025` が「出力サイズと文字サイズを**文書に保存すること**」と定め、理由を「同じ JSON から同じ出力を得るため」と書く。**出力の寸法は全体の見せ方**であって、個別のタスクに付いた意味ではない | **上げない** | tbl-settings.md:180 / 01-04-requirements.md:2843,2848 |
| `exportCanvas.width` | `S-81` | **documentSettings** | 【明】上の親の葉。プリセット（A3 横 / A4 横 / 現在の画面）は名前を保存せず、解決後の 2 値だけを保存する | **上げない** | tbl-settings.md:180 / E09-settings-blob.md:283 |
| `exportCanvas.height` | `S-81` | **documentSettings** | 【明】同上 | **上げない** | tbl-settings.md:180 / E09-settings-blob.md:284 |
| `exportPngScale` | `S-82` | **documentSettings** | 【明】「PNG の倍率。毎回手で選ばせない」。`IO-4` が倍率の出どころをこの行と定める。**倍率は絵の解像度であって日程のデータではない** | **上げない** | tbl-settings.md:181 / 01-04-requirements.md:2603 |

**この 4 実体はすべて documentSettings。schedule は 0。判定不能は 0。**

> ⚠️ **`FR-063` は現在この 4 つを「版数を上げる」側に置いている**（`01-04-requirements.md:3111`）。新しい規則と正面から衝突する。§6-1 に書く。

> ⚠️ **`exportCanvas.width` / `.height` それぞれの下限・上限は原典に無い**（`S-81` は 1 行しかない）。【無】。E09 の B-5 と同じ所見であり、こちらでも確かめた（`tbl-settings.md:178-181` に width / height を分けた行は無い）。群の判定には効かないが、`FR-023`（範囲を持たない設定値は検証できない）に触れる。

---

## 2. 表 T-205 — LOD のしきい値（7 行）

**利用者規則が「LOD のしきい値」を documentSettings と名指ししている。** したがって本表は原則として判定が済んでいる —— **ただし 1 行だけ LOD ではないものが混じっている。**

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `rulerTierPxPerDayMonth` | `S-83` | **documentSettings** | 【規】目盛が「年」→「年 ＋ 月」に変わる px/day。**目盛の段階が変わってもタスクの日付は動かない** | **上げない** | tbl-settings.md:193 |
| `rulerTierPxPerDayWeek` | `S-84` | **documentSettings** | 【規】同上（「年 ＋ 月」→「＋ 週」） | **上げない** | tbl-settings.md:194 |
| `rulerTierPxPerDayDay` | `S-85` | **documentSettings** | 【規】同上（「＋ 週」→「＋ 日 ＋ 曜日」） | **上げない** | tbl-settings.md:195 |
| `taskLevelOfDetailReadablePx` | `S-86` | **documentSettings** | 【規】この幅を割った深さのタスクを描かない。`FR-018` が「表示する `Task` と行の量を増減させる」根拠に使う。**描く / 描かないが変わるだけで、`Task` の日付・依存・割当は 1 つも変わらない** | **上げない** | tbl-settings.md:196 / 01-04-requirements.md:2357 |
| `groupLevelOfDetailBase` | `S-87` | **documentSettings** | 【規】グループ LOD の初項（`threshold(d) = base × ratio^(d − 2)`） | **上げない** | tbl-settings.md:197 |
| `groupLevelOfDetailRatio` | `S-88` | **documentSettings** | 【規】同上の公比 | **上げない** | tbl-settings.md:198 |
| `stackSafetyCap` | `S-89` | **判定不能** | 【明】**この行は LOD のしきい値ではない。** `ST-7` は「1 つの `TaskGroup` あたりの段数に安全弁を置き、**達したらエラーを出して処理を止めること（MUST）**。黙って切り捨てても、重ねて押し込んでもならない（MUST NOT）」と定める。**LOD は「描く量を減らす」、`S-89` は「受け付けない」であり、働きが違う。** 下げると、いままで編集できていた日程が編集できなくなる —— 見せ方の設定が日程データの受理可否を決める | **判定不能** | tbl-settings.md:199 / 01-04-requirements.md:1188 |

**documentSettings 6 ／ schedule 0 ／ 判定不能 1。**

> ⚠️ `S-89` が表 T-205 にあることは E09 も A-4 で指摘している（`E09-settings-blob.md:540`）。本書はそれを**群の割り当ての問題として**確かめ直した: 群の見出しの誤りにとどまらず、**2 群への割り当てそのものが決まらない。**

> **`tbl-settings.md:187` は表 T-205 の直前に「出力に含まれるタスクが変わるので保存する」と書いている。** LOD を documentSettings に置くと、**版数が同じ 2 つの文書から違う SVG が出る。** これは新しい規則が意図して受け入れる帰結だが、`WY-1`（`01-04-requirements.md:2824`）が「日程の描画領域が書き出す前と同じになること」を求め、除外を表 T-206 に限っていることと突き合わせる必要がある。§6-3 に書く。

---

## 3. 表 T-206 — 保存しないもの（17 行）— **どこに保存されるのか / されないのか**

**この 17 行は schedule でも documentSettings でもない。** 2 群はどちらも**文書の中**の話であり、表 T-206 は**文書の外**にあるものの全数だからである。
群の列には「**どちらでもない（文書に保存しない）**」と書く。**版数はどれも上げない** —— `FR-063` が「版数を上げないのは、文書に保存されない操作だけである —— …**表 T-206 が持つ値を変える操作**である」と明記している（`01-04-requirements.md:3111`）。

**置き場は 3 種類しか無い。** `XO-3` が「文書はファイル（`D-2`）と `localStorage`（`D-3`）**だけ**で持つ」と定めるので、文書外に残るのは `localStorage` か、成果物へ焼いた定数か、実行中のメモリだけである（`01-04-requirements.md:408`）。

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由（＝**どこに保存されるのか**） | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| （鍵名なし）予定の端点の掴み代 | `S-90` | どちらでもない（非保存） | 【明】文書に保存しない。理由は「掴み領域は**読む人のアクセシビリティに属する**。手が震える人には大きな掴み代が要る。文書が強制してよい値ではない」。⚠️ **置き場は原典に無い【無】** —— 読む人が変えられることを前提にした理由なのに、`localStorage` とも実装の定数とも書かれていない | 上げない | tbl-settings.md:221 / 01-04-requirements.md:3111 |
| （鍵名なし）実績の端点の掴み代 | `S-91` | どちらでもない（非保存） | 【明】同上。**置き場は原典に無い【無】** | 上げない | tbl-settings.md:222 |
| （鍵名なし）フェード掴み点の当たり判定 | `S-92` | どちらでもない（非保存） | 【明】同上（15 × 15px）。見た目の点（`S-109`）より広く取る。**置き場は原典に無い【無】** | 上げない | tbl-settings.md:223,309 |
| （鍵名なし）実績入力の入口の当たり判定 | `S-93` | どちらでもない（非保存） | 【明】同上（30 × 20px）。`GR-9` が大きさの出どころとしてこの行を指す。**置き場は原典に無い【無】** | 上げない | tbl-settings.md:224 / 01-04-requirements.md:2132,2141 |
| （鍵名なし）取り消しの段数 | `S-94` | どちらでもない（非保存） | 【明】文書に保存しない。「**履歴自体を保存しないのに回数だけ文書が指定するのは筋が通らない**」。⚠️ 理由文は「動いている端末のメモリ予算」なので**実装の定数**と読めるが、**そう書いてはいない【無】** | 上げない | tbl-settings.md:225 |
| （鍵名なし）取り消しの合計メモリ上限 | `S-95` | どちらでもない（非保存） | 【明】同上（64MB 🔎）。**置き場は原典に無い【無】** | 上げない | tbl-settings.md:226 |
| `zoomStep` | `S-96`（→`S-53`） | どちらでもない（非保存） | 【明】**置き場は仕様書＝製品の定数。** 値は表 T-201 の `S-53` が持ち、`S-53` には ⛔ が付いている。「操作の速さであって結果ではない。1 ノッチで何倍動くかは入力装置に依存する」。**JSON に現れないことを機械検査する** | 上げない | tbl-settings.md:101,227 / grs-document-settings-ja.md:617 |
| `zoomMin` | `S-97`（→`S-54`） | どちらでもない（非保存） | 【明】同上（値は `S-54`、⛔ 付き）。⚠️ **ただし保存される `zoomX` / `zoomY` のクランプ範囲としては効く** —— 非保存の定数が保存値の有効範囲を決める | 上げない | tbl-settings.md:102,228 |
| `zoomMax` | `S-98`（→`S-55`） | どちらでもない（非保存） | 【明】同上（値は `S-55`、⛔ 付き） | 上げない | tbl-settings.md:103,229 |
| （鍵名なし）透かしに出す開いた者の名前 | `S-99a` | どちらでもない（非保存） | 【明】**置き場は `localStorage`。** `FR-086` が「透かしに出す名前を入力させ、`localStorage` へ保存すること」と定める。文書に保存しない規則は `FR-020` | 上げない | tbl-settings.md:230 / 01-04-requirements.md:2543,2549 |
| （鍵名なし）利用者が設定した透かし解除パスワードの SHA-256 | `S-99c` | どちらでもない（非保存） | 【明】**置き場は `localStorage`。** 未設定なら表 T-207 の既定値を使う | 上げない | tbl-settings.md:231 |
| `Agent API` を有効にした文書の記録 | `S-99b` | どちらでもない（非保存） | 【明】**置き場は `localStorage`。** `FR-065` が「文書ごとに記憶する」と定めるが、**有効化は読む人の判断であって文書の内容ではない**ので、文書の識別子と対にして環境側へ置く | 上げない | tbl-settings.md:232 |
| `language` ⛔ | `S-99` | どちらでもない（非保存） | 【明】**置き場は `localStorage`。** 保存しない規則は `FR-038`。絵が変わらない。⚠️ **表 T-202 に行が無いので `FR-049` の切り替え対象ではない** | 上げない | tbl-settings.md:233 |
| ~~本日線の表示状態~~ | `S-99d` | **どこにも無い（廃止）** | 【明】**廃止（2026-08-13）。値そのものが存在しない。** 基準日線は `Project.status_date` が `null` かどうかで描画が決まる（`FR-046`）。⚠️ `Project.status_date` は **schedule 側**（文書のデータ）であり、`WY-1` は基準日線を判定に**含める** | — | tbl-settings.md:234 / 01-04-requirements.md:2824 |
| （鍵名なし）コマンドパレットの表示状態 | `S-99e` | どちらでもない（非保存） | 【明】文書に保存しない（「画面の使い方であって文書の内容ではない」）。⚠️ **置き場は原典に無い【無】** —— `FR-053` は浮遊と透明度を定めるだけで、表示状態の保持に触れない。既定「表示」があるので毎回戻ると読めるが、**そう書いてはいない** | 上げない | tbl-settings.md:235 / 01-04-requirements.md:3111 |
| （鍵名なし）全画面表示かどうか | `S-99f` | どちらでもない（非保存） | 【明】同上。**置き場は原典に無い【無】** —— `FR-071` は入口と解除を定めるだけ | 上げない | tbl-settings.md:236 |
| （鍵名なし）開いている面 | `S-99g` | どちらでもない（非保存） | 【明】同上。「面」＝画面の上に重ねて開き `Esc` の第 1 階層で閉じられるもの（`IN-4`）。**置き場は原典に無い【無】** | 上げない | tbl-settings.md:237 |

### 3-1. 置き場の内訳（自分で数えた）

| 置き場 | 行数 | 行 ID |
|---|--:|---|
| `localStorage`（明記あり） | **4** | `S-99a` `S-99b` `S-99c` `S-99` |
| 製品の定数（値は表 T-201 が持つ・⛔ 付き） | **3** | `S-96` `S-97` `S-98` |
| **原典に置き場の記述が無い** | **9** | `S-90` `S-91` `S-92` `S-93` `S-94` `S-95` `S-99e` `S-99f` `S-99g` |
| 廃止（値が存在しない） | **1** | `S-99d` |
| **合計** | **17** | |

> **17 行のうち 9 行は「文書に保存しない」とだけ書かれ、代わりにどこへ置くのかが書かれていない。** `XO-3` が置き場を 2 つ（ファイルと `localStorage`）に限っている以上、**「文書でない」だけでは置き場が決まらない。** §6-4 に書く。

---

## 4. 表 T-207 — 透かし（3 行・成果物に埋め込む定数）

**この 3 行も文書の外にある。** ただし表 T-206 とは置き場が違う —— **`localStorage` でも実行時のメモリでもなく、成果物（`.html`）へ焼き込む定数**である。

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由（＝どこに保存されるのか） | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| （鍵名なし）既定の透かし解除パスワード | `S-100` | どちらでもない（非保存） | 【明】**成果物にも入らない。** 「ここに書くのは仕様として値を定めるためであり、**成果物へ入るのは下の SHA-256 だけ**である」。⚠️ `LM-12` が「既定値そのものが本仕様書に書いてあるので秘密として機能しない」と明記している | — | tbl-settings.md:243 / 01-04-requirements.md:174 |
| （鍵名なし）既定の透かし解除パスワードの SHA-256 | `S-101` | どちらでもない（非保存） | 【明】**置き場は成果物（`.html`）に埋め込む定数。** UTF-8・末尾の改行を含めない | — | tbl-settings.md:244 |
| `watermarkOpacity` | `S-102` | どちらでもない（非保存） | 【明】成果物の定数。透かしの濃さ。**コントラストの適合範囲から外してある**（`NFR-007` / `LM-13`） | — | tbl-settings.md:245 / 01-04-requirements.md:176 |

> ⚠️ **`FR-063` は表 T-207 を版数の除外に挙げていない。** 除外として名指しされているのは表 T-206 だけである（`01-04-requirements.md:3111`）。§6-1 に書く。

> ⚠️ **透かしは絵に出るのに、文書のデータではない。** `WY-2` は「**透かしの層を除いた**描画が同じ SVG / PNG になること」として、透かしを判定から外している（`01-04-requirements.md:2826`）。**2 群の外に、「絵に出るが文書に無いもの」という第 3 の場所が要る**ことの実例である。

---

## 5. 表 T-208 〜 T-215 — 後から選び直す値（8 表 23 行）

`tbl-settings.md:251-252` は「**本節の表は §1 の判定では文書に保存する側に落ちるものを含む。**（中略）**各表の中で保存の可否を明記する**」と宣言している。
**明記があるのは表 T-209 の 1 表だけであり、行単位では `S-105` を足した 4 行だけである**（`grep -n "保存" tbl-settings.md` を 247 行以降に掛けて確かめた。ヒットは 251 / 252 / 261 / 265 / 319 / 327 の 6 行で、うち 261 は表題、265 は「自動保存」という別語）。

### 5-1. 表 T-208 — 予実の補助線（3 行）

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `planActualGuideWeight` | `S-103` | **documentSettings** | 【類】線の太さは全体の見せ方。「バーより目立つと、**事実（バー）より補いの線が前に出る**ので細くする」。⚠️ **保存の可否は原典に無い【無】**。群としては見せ方に落ちるが、文書が持つのか製品の定数かは決まらない | **上げない** | tbl-settings.md:325 / 01-04-requirements.md:1805 |
| `planActualGuidePattern` | `S-104` | **documentSettings** | 【類】破線の刻み。同上。⚠️ **値 `2,2` が文字列か配列かは原典から確かめられない【無】**（E09 B-4 と同じ所見。`tbl-settings.md:326` に型の記述が無いことを確かめた） | **上げない** | tbl-settings.md:326 |
| `planActualGuideColor` | `S-105` | **どちらでもない（保存しない）** | 【明】**この行だけ保存の可否が書いてある**: 「**独立した色を保存しない** —— 派生する色を保存しない規則は `FR-041` が持つ」。値は「実績バーの色に追随」。⚠️ **追随先の実績バーの色は schedule 側**（個別のタスクの色、または `themeHue`）なので、**この行は schedule を読むが自分は文書に無い** | — | tbl-settings.md:327 |

### 5-2. 表 T-209 — 既定の暦（3 行）⚠️ **本担当で唯一 schedule に落ちる表**

**利用者規則は「暦」を schedule 群に名指ししている。** さらに `FR-088` は「**変更を文書の暦へ反映すること**」、その注は「**暦を変えると期間の計算がすべて動く**ので、影響する `Task` の件数を知らせること（MUST）」と定める（`01-04-requirements.md:2291,2297`）。**日程のデータが動く。**

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| （鍵名なし）稼働する曜日 | `S-106` | **schedule** | 【規】【明】暦は schedule 群。`FR-054` が「**期間の計算に用いる『稼働日』も同じ暦に従うこと**」と定め、`FR-088` の注が「暦を変えると期間の計算がすべて動く」と書く。**稼働日が変われば `Task` の終了日が変わる** | **上げる** | tbl-settings.md:315 / 01-04-requirements.md:2291,2297,2310 |
| （鍵名なし）例外日（休業日） | `S-107` | **schedule** | 【規】【明】同上。既定は「無し」（祝日を持たない）。`FR-088` は「自社の休業日を入れる手段が無いと、**既定のままでは日数が実態と合わない**」を成立理由に挙げる | **上げる** | tbl-settings.md:316 / 01-04-requirements.md:2295 |
| （鍵名なし）週の始まり | `S-108` | **判定不能** | **2 つの原典が別の群を指す。** `tbl-settings.md:317` は「**週の目盛が書く日付の基準**」＝見せ方。`FR-054` は「週の目盛は週の始まりの日付を書き、**週の始まりも文書の暦に従うこと（MUST）**」＝暦の一部。`FR-088` も暦の編集項目として並べる。**暦（schedule）に属しながら、変えても稼働日は 1 日も動かない** —— 日程のデータが変わらないのに schedule 群にある | **判定不能** | tbl-settings.md:317 / 01-04-requirements.md:2291,2314 |

> ⚠️ **格納先は 3 行とも判定不能【無】。** 表 T-209 は「保存の可否: **保存する**」とだけ書き（`tbl-settings.md:319`）、`documentSettings` なのか `Calendar` / `WeekDay` / `Exception` のエンティティなのかを書いていない。`FR-054` は「**既定の暦は表 T-209 が持つ**」「**取り込んだ暦があればそれを文書の暦とする**」と書くので、**表 T-209 は文書の暦そのものではなく既定値**と読めるが、明記が無い。E09 の B-3 と同じ所見であり、こちらでも `FR-054` / `FR-088` を読んで確かめた。**群（schedule）は決まるが、置き場は決まらない。**

### 5-3. 表 T-210 — フェードの掴み点（3 行）

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `fadeHandleHalfPx` | `S-109` | **documentSettings** | 【類】掴み点の半辺（正方形 9 × 9px）。**寸法の既定**は利用者規則で documentSettings。⚠️ **保存の可否は原典に無い【無】** | **上げない** | tbl-settings.md:305 / 01-04-requirements.md:1076 |
| `fadeHandleStrokePx` | `S-110` | **documentSettings** | 【類】掴み点の枠線。同上 | **上げない** | tbl-settings.md:306 |
| （鍵名なし）掴み点を出す条件 | `S-111` | **判定不能** | **値が「条件」であって寸法ではない。** `FR-075` は「**掴み点は選択しているタスクにだけ出すこと（MUST）**」と要求で固定している。要求が MUST で固定しているものを設定値の表に置くと、**変えられる値なのかどうかが決まらない** —— 変えられないなら設定値ではなく、表の行であること自体が誤りになる | **判定不能** | tbl-settings.md:307 / 01-04-requirements.md:1038 |

> ⚠️ **「フェード」という 1 語が 2 群にまたがる。** フェードイン日数 / フェードアウト日数は `Task` の列＝**schedule**（`01-04-requirements.md:1038,1076`）。掴み点の寸法（`S-109` / `S-110`）は見せ方＝**documentSettings**。当たり判定の大きさ（`S-92`）は**どちらでもない（非保存）**。**同じ機能の 3 つの値が 3 つの場所に散る。**

### 5-4. 表 T-211 — 保存と受け入れの上限（5 行）⚠️ **5 行すべて判定不能**

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `autosaveIdleMs` | `S-112` | **判定不能** | **2 群のどちらでもない「振る舞い」である。** 自動保存の「操作の切れ目」（`FR-026`）。**変えても絵は 1px も変わらない**ので `tbl-settings.md:16` の第 1 段（絵が変わるか）で落ち、保存する側に来られない。それなのに §9 は「本節の表は保存する側に落ちるものを含む」と書き、この行の可否を書かない【無】 | **判定不能** | tbl-settings.md:265 / 01-04-requirements.md:2899 |
| `importMaxBytes` | `S-113` | **判定不能** | **受け入れの検証方針であって、見せ方でも対象の意味でもない。** `FR-023` が上限を超えた入力の扱いを定める。⚠️ **文書が自分の取り込み上限を持つと循環する** —— 取り込んだ文書の設定が、次の取り込みの可否を決める。保存の可否は原典に無い【無】 | **判定不能** | tbl-settings.md:266 |
| `importMaxItems` | `S-114` | **判定不能** | 同上（`Task` 件数の上限 20000。目標規模の 20 倍）。**性能の目標より広く受理し、資源が尽きる手前で止める** | **判定不能** | tbl-settings.md:267 |
| `importMaxDepth` | `S-115` | **判定不能** | 同上（WBS のネストの深さ 64）。**`maxGroupDepth` とは別物**（WBS はクランプしない） | **判定不能** | tbl-settings.md:268 |
| `maxGroupDepth` | `S-125` | **判定不能** | **1 つの値が両群にまたがる。** ① schedule 側: `FR-004` が「階層の深さに上限を課すこと（MUST）」、`01-04-requirements.md:1232` が「**上限に達している親の下に作らせてはならない（MUST NOT）**」 —— **`TaskGroup`（行の器＝schedule）の作れる形を直接に縛る。** ② documentSettings 側: `S-79` `rowTitlePanelWidth` の**下限の式に現れる**（`rowTitleIndent` × `maxGroupDepth`）。下げると既存の `TaskGroup` が規則違反になるので「見せ方だから上げない」と言い切れない | **判定不能** | tbl-settings.md:269 / 01-04-requirements.md:1232,1271,1273,1275 / tbl-settings.md:155 |

### 5-5. 表 T-212 — 画面の各部の寸法と待ち時間（2 行）

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `iconHintDelayMs` | `S-124` | **判定不能** | **待ち時間であって絵ではない。** アイコンの説明を出すまでの 3 秒（`FR-092` の `EZ-2`）。`autosaveIdleMs` と同じ理由で第 1 段（絵が変わるか）を通らない。保存の可否は原典に無い【無】 | **判定不能** | tbl-settings.md:258 / 01-04-requirements.md:3274 |
| `appHeaderMaxHeight` | `S-116` | **documentSettings** | 【類】画面レイアウトの寸法。利用者規則が「パネル幅」を documentSettings に置いており、**`App Header` の高さは同じ species の値**である。「**上部に使った高さはそのまま日程表から引かれる**」ので絵は変わる。⚠️ **保存の可否は原典に無い【無】**。⚠️ **`WY-1` の判定対象は「日程の描画領域」であり、`App Header` はその外**なので、再現性の要求からは帰属が決まらない | **上げない** | tbl-settings.md:259 / 01-04-requirements.md:2160,2824 |

### 5-6. 表 T-213 — 依存のラグ（2 行）⚠️ **2 行とも判定不能**

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `dependencyLagDefault` | `S-117` | **判定不能** | **「新しく作る対象の初期値」という第 3 の性質。** 「依存を作ったときに置く値」。**ラグの実値は `Dependency` の属性＝schedule**（`FR-009` が「依存の種別ごとにラグを保持し、作成者が編集できること（MUST）」と定める）。既定値を変えても**既存の依存は 1 つも動かない**ので日程データは変わらない。だが見せ方でもない。**雛形は 2 群のどちらにも入らない** | **判定不能** | tbl-settings.md:277 / 01-04-requirements.md:1680 |
| （鍵名なし）ラグの単位 | `S-118` | **判定不能** | **大域の設定が、個別の対象に付いた意味を書き換える。** 単位を「稼働日」から変えると、**既存のすべての `Dependency` のラグ値が指す日数が変わる**（数値は変わらないのに意味が変わる）。⚠️ **MSPDI は `LagFormat` を `PredecessorLink` ごとに持つ**（`mspdi_pj12.xsd:2201`。`LinkLag` も同 2196 行、単位は 1/10 分）ので、**往復すると 1 つの大域設定に畳めない。** ⚠️ 一方で `FR-009` は「**ラグは値として保持するだけで、日付を自動で動かさない**」と定めるので、**単位を変えても日付は動かない** —— 「日程のデータが変わったか」の判定が下せない | **判定不能** | tbl-settings.md:278 / 01-04-requirements.md:1680,1701 / mspdi_pj12.xsd:2196,2201 |

### 5-7. 表 T-214 — 受け入れる日付の範囲（2 行）

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `importMinDate` | `S-119` | **判定不能** | **受け入れの検証方針。** これより前の日付を持つ入力を受け付けない。見せ方でも個別の対象の意味でもない。保存の可否は原典に無い【無】。⚠️ **文書が持つとすると、いま入っているタスクの日付を弾く値を同じ文書が持ちうる** | **判定不能** | tbl-settings.md:286 |
| `importMaxDate` | `S-120` | **判定不能** | 同上（`2200-12-31`）。「**231 年ぶんの期間でも稼働日の数え上げが計算量に効く**」 | **判定不能** | tbl-settings.md:287 |

### 5-8. 表 T-215 — 文字サイズ（3 行）

| 鍵 / エンティティ | 行 ID | 群 | 判定の理由 | 版数を上げるか | 出典 |
|---|---|---|---|---|---|
| `fontScaleSizes.S` | `S-121` | **documentSettings** | 【規】**寸法の既定**は利用者規則で documentSettings。⚠️ **保存の可否は原典に無い【無】**。`FR-039` は「文字サイズの 3 段階が何 px かは**表 T-215 が持つ**」と書いており、**仕様書側の定数**とも読める | **上げない**（⚠️ §6-2 参照） | tbl-settings.md:293 / 01-04-requirements.md:3420 |
| `fontScaleSizes.M` | `S-122` | **documentSettings** | 【規】同上 | **上げない**（⚠️ §6-2） | tbl-settings.md:294 |
| `fontScaleSizes.L` | `S-123` | **documentSettings** | 【規】同上 | **上げない**（⚠️ §6-2） | tbl-settings.md:295 |

> ⚠️ **保存するなら 3 つ目の入れ子オブジェクトになる**（葉が 86 → 89）。E09 の B-2 と同じ所見。
> ⚠️ **`S-121`〜`S-123` は `S-3` `rulerFont` の既定の関数に現れる**（`tbl-settings.md:51`）。**documentSettings の値が別の documentSettings の値の既定を決める**ので群はまたがないが、§6-2 の `FR-039` の破れがここから出る。

---

## 6. 数の検算と、原典どうしの衝突

### 6-0. 在庫表 E09 の「7 表 17 鍵」は合わない —— **正しくは 20 行（うち可否未記載は 19 行）**

`E09-settings-blob.md:338` は「**明記しているのは表 T-209 だけである**（自分で数えた：8 表中 1 表）。**残り 7 表は保存の可否が書かれていない**」と書き、`:352` と `:526` で「**これら 17 鍵**」と数を挙げている。**自分で数え直した結果は下のとおりで、17 にはならない。**

| 数えたもの | E09 | **本書（自分で数えた）** | 数え方 |
|---|--:|--:|---|
| 表 T-208 〜 T-215 の全行 | （記載なし） | **23** | `grep -c "^| S-"` を 247 行以降に掛けた |
| うち T-209 を除く 7 表の行 | **17** | **20** | 3＋3＋5＋2＋2＋2＋3（T-208 / T-210 / T-211 / T-212 / T-213 / T-214 / T-215） |
| 保存の可否が**書かれていない**行 | 17 | **19** | 23 −（T-209 の 3 行 ＋ `S-105` の 1 行）。`S-105` は行の中に「独立した色を**保存しない**」と書いてある |
| 8 表のうち英語の鍵名を持つ行 | （記載なし） | **18** | 名前の列が `` ` `` で始まる行を数えた。持たないのは `S-106` `S-107` `S-108` `S-111` `S-118` の 5 行 |
| 前プロジェクト台帳に現れる行 | 0 | **0** | `grs-document-settings-ja.md` を 16 の鍵名（`fontScaleSizes` で 3 行ぶん）と「週の始まり」で機械検索し、**すべて 0 件**。E09 の「すべて仕様書側で新設」は正しい |

> **17 という数の出どころは特定できない。** 「7 表の**名前つき**の鍵 18 −（保存しないと書いてある）`S-105` 1 ＝ 17」という算だと一致するが、**E09 はその数え方を書いていないので、そう意図したとは断定しない【類】。**
> **本書が使う数は 23 行 / 20 行 / 19 行である。**

### 6-1. ⚠️⚠️ **`FR-063` は新しい規則と正面から衝突する（本担当で見つけた最大の破れ）**

`01-04-requirements.md:3111`（`FR-063` の STATEMENT）は、**いまこう書いてある**:

> **版数を上げるのは、文書に保存される値を変える更新すべてとすること（MUST）。保存される値の全数は `_assets/tbl-settings.md` の各表のうち ⛔ と 🅿 が付いていない行である。**
> **版数を上げないのは、文書に保存されない操作だけである** —— 選択・構えの変更・フォーカスの移動と、**表 T-206 が持つ値を変える操作**である。
> ⚠️ **ズーム・スクロール・パン・`Dual Cursor` の位置・パネル幅と、表 T-202 が持つ表示の切り替えは文書に保存される（表 T-202 / T-203）ので版数を上げる。**

**新しい規則は、この最後の 1 文が名指しした値をすべて「上げない」側へ移す。** ズーム・スクロール・パネル幅・表示切替は、利用者判断で documentSettings 群である。
**`FR-063` を書き換えずに新しい規則を採ることはできない。**（本書は `docs/spec/` を編集しない。ここに記録するだけである。）

同時に、`FR-063` の「⛔ と 🅿 が付いていない行 ＝ 保存される値」という**機械的な定義**は、本担当の範囲では次のように壊れている（§6-3）。

### 6-2. ⚠️ `FR-039` の「文書の編集」と、新しい規則の「上げない」

`01-04-requirements.md:3426` は、**読む人が文字サイズを変えたときの帰結をこう定める**:

> **文字サイズの変更は目盛にも及ぶこと（MUST）** —— 目盛の文字と目盛の帯の高さがこれに追随し、**その保存値が書き換わる**（`S-3` / `S-2`）。
> ⚠️ **保存値は初期値であり、`fontScale` を変えたときに追随して書き換わることが「文書の編集」になる**

**新しい規則では文字サイズは見せ方であり、版数を上げてはならない。** ところが `FR-039` は同じ操作を「文書の編集」と呼び、`FR-063` は「文書に保存される値を変える更新すべて」で版数を上げると定める。
**「文書の編集」と「版数を上げる」を切り離さないかぎり、読む人が文字を大きくしただけで版数が進む。** WCAG 1.4.4 が求める「強制されないこと」を満たすための操作が、Agent API の楽観的排他（`AG-9a` が拒否に現在の版数を含める。`01-04-requirements.md:3156`）を空振りさせる。

### 6-3. ⚠️ **⛔ / 🅿 の印は表 T-201 の 4 行にしか付いていない（機械で確かめた）**

`grep -n "⛔\|🅿" tbl-settings.md` の結果は **5 行**で、うち 1 行は凡例（`:37`）である。値の行は次の 4 行だけ:

| 行 | 印 | 行 ID |
|---|:--:|---|
| tbl-settings.md:100 | 🅿 | `S-52` `statusDate`（表 T-201） |
| tbl-settings.md:101 | ⛔ | `S-53` `zoomStep`（表 T-201） |
| tbl-settings.md:102 | ⛔ | `S-54` `zoomMin`（表 T-201） |
| tbl-settings.md:103 | ⛔ | `S-55` `zoomMax`（表 T-201） |

**表 T-206（17 行）・表 T-207（3 行）・表 T-208 〜 T-215（23 行）には、⛔ も 🅿 も 1 つも付いていない。**
したがって `FR-063` の機械的な定義に素直に従うと:

- **表 T-206 の 17 行が「保存される値」に落ちる** —— 表題が「**保存しないもの**」なのに。`FR-063` は同じ文の中で表 T-206 を除外に名指ししているので、**1 つの要求の中で 2 つの節が矛盾する。**
- **表 T-207 の 3 行が「保存される値」に落ちる** —— こちらは**除外の名指しすら無い**。`watermarkOpacity` を変えると版数が上がることになるが、成果物の定数なので文書からは変えようがない。
- **表 T-208 〜 T-215 の 23 行がすべて「保存される値」に落ちる** —— `S-105` は行の中で「保存しない」と書いてあるのに、印が無いので機械的には保存側になる。

**E09 の A-3（用語辞書 `K-101` に ⛔ が無い）は、用語辞書だけの付け忘れではない。`tbl-settings.md` 側にも無く、`FR-063` の定義がその印に依存している。**

---

## 7. 分類の集計（自分で数えた）

| 群 | 実体数 | 内訳 |
|---|--:|---|
| **schedule**（版数を上げる） | **2** | `S-106` 稼働する曜日 ／ `S-107` 例外日 |
| **documentSettings**（版数を上げない） | **18** | T-204 の 4（`S-81` ＋ 葉 2 ＋ `S-82`）／ T-205 の 6（`S-83`〜`S-88`）／ `S-103` `S-104` ／ `S-109` `S-110` ／ `S-116` ／ `S-121`〜`S-123` |
| **判定不能** | **13** | `S-89` `S-108` `S-111` `S-112` `S-113` `S-114` `S-115` `S-117` `S-118` `S-119` `S-120` `S-124` `S-125` |
| **どちらでもない（文書に保存しない）** | **21** | 表 T-206 の 17 ／ 表 T-207 の 3 ／ `S-105` |
| **合計** | **54** | 担当の 52 行 ＋ 入れ子の葉 2 |

> ⚠️ **4 つ目の欄が要る。** 2 群では 21 実体（全体の 39%）が置き場を持たない。「schedule / documentSettings」の 2 分では、**文書の外にあるもの**を書く場所が無い。

---

## 判定不能と、境界が破れる場所

**13 件の判定不能と、判定はできたが境界が張り詰めている 9 件を、1 件ずつ挙げる。**

### 判定不能（13 件）

**1. `S-89` `stackSafetyCap` —— 見せ方の設定が、日程データの受理可否を決める**
`tbl-settings.md:199` は表 T-205（LOD のしきい値）に置いているが、`01-04-requirements.md:1188`（`ST-7`）は「1 つの `TaskGroup` あたりの段数に安全弁を置き、**達したらエラーを出して処理を止めること（MUST）**。黙って切り捨てても、重ねて押し込んでもならない（MUST NOT）」と定める。**LOD は描く量を減らし、`S-89` は受け付けない。** documentSettings に置いて版数を上げないと、**見せ方の変更のつもりで下げた値が、既存の日程を編集不能にする。**

**2. `S-108` 週の始まり —— 暦（schedule）に属するのに、日付を 1 日も動かさない**
`tbl-settings.md:317` は「**週の目盛が書く日付の基準**」＝見せ方と書く。`01-04-requirements.md:2314`（`FR-054`）は「**週の始まりも文書の暦に従うこと（MUST）**」＝暦の一部と書く。`01-04-requirements.md:2291`（`FR-088`）は暦の編集項目として稼働曜日・例外日と並べる。**利用者規則は暦を schedule と名指しするが、この値を変えても稼働日は変わらず、`Task` の終了日も動かない。** 規則の 2 つの半分（「暦は schedule」と「日程のデータが変わったときだけ上げる」）が別の答えを出す。

**3. `S-111` 掴み点を出す条件 —— 要求が MUST で固定した値が、設定値の表にある**
`tbl-settings.md:307` は値を「選択中のタスクだけ 🔎」とし、下限・上限を `—` にする。`01-04-requirements.md:1038`（`FR-075`）は「**掴み点は選択しているタスクにだけ出すこと（MUST）**」と要求で固定している。**変えられないなら設定値ではない。** 変えられるなら `FR-075` の MUST が破れる。どちらか決まらないので群も決まらない。

**4. `S-112` `autosaveIdleMs` —— 絵が変わらないのに §9（保存する側を含む節）にある**
`tbl-settings.md:265`。自動保存の間隔は絵を 1px も変えないので、`tbl-settings.md:16` の第 1 段（その値を変えると絵が変わるか）で落ちる。にもかかわらず `tbl-settings.md:251` は「本節の表は §1 の判定では**文書に保存する側に落ちるものを含む**」と書き、この行の可否を書かない。**見せ方でも個別の対象の意味でもない「振る舞い」であり、2 群のどちらにも入らない。**

**5. `S-113` `importMaxBytes` —— 受け入れの検証方針は 2 群のどちらでもない**
`tbl-settings.md:266`。**文書がこの値を持つと循環する** —— 取り込んだ文書の設定が、次の取り込みの可否を決める。原典に保存の可否が無い。

**6. `S-114` `importMaxItems`** —— `tbl-settings.md:267`。同上。**目標規模の 20 倍（20000）で受理し、資源が尽きる手前で止める**という運用の値であり、絵にも日程にも属さない。

**7. `S-115` `importMaxDepth`** —— `tbl-settings.md:268`。同上。`01-04-requirements.md:1275` が「**WBS の深さをクランプしてはならない（MUST NOT）**」と定めるので、この上限は「受け付けない」だけに効く。

**8. `S-125` `maxGroupDepth` —— 1 つの値が両群にまたがる**
`tbl-settings.md:269`。schedule 側: `01-04-requirements.md:1232` が「**上限に達している親の下に作らせてはならない（MUST NOT）**」、`:1271` が「階層の深さに上限を課すこと（MUST）」 —— **`TaskGroup`（行の器＝schedule）の作れる形を直接に縛る。** documentSettings 側: `tbl-settings.md:155` が `S-79` `rowTitlePanelWidth` の**下限の式**にこの値を置く。**片方に寄せると、もう片方の規則が根拠を失う。**

**9. `S-117` `dependencyLagDefault` —— 「これから作る対象の初期値」という第 3 の性質**
`tbl-settings.md:277`。ラグの実値は `Dependency` の属性＝schedule（`01-04-requirements.md:1680` が「依存の種別ごとにラグを保持し、作成者が編集できること（MUST）」）。**既定値を変えても既存の依存は 1 つも動かない**ので日程データは変わらないが、見せ方でもない。**雛形は 2 群のどちらにも入らない。**

**10. `S-118` ラグの単位 —— 大域の設定が、個別の対象に付いた意味を書き換える**
`tbl-settings.md:278`（値は「稼働日」）。**単位を変えると、既存のすべての `Dependency` のラグ値が指す日数が変わる** —— 数値は変わらないのに意味が変わる。しかも `mspdi_pj12.xsd:2201` の `LagFormat` は **`PredecessorLink` ごと**の要素であり（`LinkLag` も同 `:2196`、単位は 1/10 分）、**MSPDI 側では個別の対象に付いた意味である。** 1 つの大域設定に畳むと往復で情報が落ちる。
⚠️ 一方 `01-04-requirements.md:1680` は「**ラグは値として保持するだけで、日付を自動で動かさない**」と定めるので、**単位を変えても日付は動かない。** 「日程のデータが変わったか」で判定する規則が、この行に対して答えを返さない。

**11. `S-119` `importMinDate`** —— `tbl-settings.md:286`。受け入れる日付の下限。**文書が持つとすると、いま入っているタスクの日付を弾く値を同じ文書が持ちうる。** 保存の可否が原典に無い。

**12. `S-120` `importMaxDate`** —— `tbl-settings.md:287`。同上（`2200-12-31`）。理由は計算量（稼働日の数え上げ）であり、絵とも日程データとも無関係。

**13. `S-124` `iconHintDelayMs`** —— `tbl-settings.md:258`。アイコンの説明を出すまでの 3 秒（`01-04-requirements.md:3274` の `EZ-2`）。`S-112` と同じく第 1 段（絵が変わるか）を通らない。

### 判定はできたが、境界が張り詰めている場所（9 件）

**14. `FR-063` が新しい規則と正面から衝突する** —— `01-04-requirements.md:3111`。ズーム・スクロール・パン・`Dual Cursor`・パネル幅・表 T-202 の表示切替を、**名指しで「版数を上げる」側に置いている。** 新しい規則はこれらを documentSettings（上げない）に置く。§6-1。

**15. `FR-039` が「文書の編集」と呼ぶ操作を、新しい規則は「上げない」に置く** —— `01-04-requirements.md:3426`。読む人が文字を大きくすると `S-2` / `S-3` の保存値が書き換わり、それが文書の編集になる。§6-2。

**16. ⛔ の印が表 T-206 / T-207 / T-208〜T-215 に 1 つも無い** —— 機械で確かめた（付いているのは `tbl-settings.md:101,102,103` の 3 行と 🅿 の `:100` だけ）。`FR-063` の「保存される値の全数」はこの印に依存しているので、**43 行（17＋3＋23）が機械的には「保存される」側に落ちる。** §6-3。

**17. 表 T-206 の 9 行は「文書に保存しない」とだけ書かれ、置き場が無い** —— `tbl-settings.md:221,222,223,224,225,226,235,236,237`。`01-04-requirements.md:408`（`XO-3`）は「文書はファイルと `localStorage` **だけ**で持つ」と定めるので、**「文書でない」だけでは置き場が決まらない。** `S-90`〜`S-93` は理由文が「読む人が変えられること」を前提にしているのに、変える手段も置き場も書かれていない。

**18. 表 T-207 は `FR-063` の除外に入っていない** —— `01-04-requirements.md:3111` が名指しするのは表 T-206 だけ。`tbl-settings.md:245` の `watermarkOpacity` は成果物の定数なので文書から変えようがないが、規則の上では「保存される値」に落ちる。

**19. 表 T-209 の格納先が決まらない** —— `tbl-settings.md:319` は「保存の可否: 保存する」とだけ書き、`documentSettings` なのか `Calendar` / `WeekDay` / `Exception` のエンティティなのかを書かない。`01-04-requirements.md:2314` は「**既定の暦は表 T-209 が持つ**」「取り込んだ暦があればそれを文書の暦とする」と書くので**既定値**と読めるが、明記が無い。**群（schedule）は決まるが置き場は決まらない。**

**20. `S-105` `planActualGuideColor` は schedule を読むが、文書に無い** —— `tbl-settings.md:327`。値は「実績バーの色に追随」。追随先は個別のタスクの色または `themeHue` で、**どちらも利用者規則で schedule 側**である。**schedule の値を読んで絵を決めるのに、自分はどちらの群にも入らない。**

**21. LOD を documentSettings に置くと、同じ版数の 2 文書から違う SVG が出る** —— `tbl-settings.md:187` は表 T-205 の直前で「**出力に含まれるタスクが変わるので保存する**」と書く。`01-04-requirements.md:2824`（`WY-1`）は「日程の描画領域が書き出す前と同じになること」を求め、**判定から外してよいものを表 T-206 に限っている。** LOD は表 T-206 に無いので `WY-1` の対象であり、版数だけが追随しない。

**22. `S-116` `appHeaderMaxHeight` は `WY-1` の対象外の場所を決める** —— `tbl-settings.md:259` / `01-04-requirements.md:2160`。「**上部に使った高さはそのまま日程表から引かれる**」ので絵は変わるが、`WY-1` の判定対象は「日程の描画領域」であり `App Header` はその外。**再現性の要求からは帰属が決まらない。** 本書は利用者規則の「パネル幅」からの類推で documentSettings に置いた【類】。

**23. 「フェード」の 3 つの値が 3 か所に散る** —— フェードイン / アウト日数は `Task` の列＝schedule（`01-04-requirements.md:1038,1076`）、掴み点の寸法 `S-109` / `S-110` は documentSettings（`tbl-settings.md:305,306`）、当たり判定 `S-92` はどちらでもない非保存（`tbl-settings.md:223`）。**1 つの機能の 3 つの値が 3 つの群に分かれる。**

**24. 非保存の `zoomMin` / `zoomMax` が、保存される `zoomX` / `zoomY` の有効範囲を決める** —— `tbl-settings.md:102,103,228,229` と `:151,152`。**文書に無い値が、文書にある値の妥当性を決める。** 版数を持たない定数を変えると、既存文書の保存値が範囲外になりうる。

---

## 本書が確かめなかったこと

- `docs/spec/01-04-requirements.md` は**全文を読んでいない**。読んだのは §0 に挙げた要求と表の該当行だけである。ここに挙げた「原典に記述が無い【無】」は、**読んだ範囲に無い**という意味である。
- `previous-project-result/02-data-model/grs-document-settings-ja.md` は**機械検索しかしていない**（§9 の鍵が 0 件であることの確認のみ）。
- `docs/reference/mspdi/mspdi_pj12.xsd` は `LinkLag` / `LagFormat` の 2 か所しか読んでいない。**ラグ以外の MSPDI の主張は本書にない。**
- **`docs/spec/` は 1 文字も編集していない。**

