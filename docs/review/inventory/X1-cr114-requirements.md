# X1 — `docs/spec/01-04-requirements.md` の検討経緯 全数判定

**判定基準**: CR-114（`change-request/CR-114-remove-deliberation-history-from-spec.md`）。
**判定は 1 つ** —— 「この文を消したら、仕様か、それを採る理由が失われるか。」失われないなら **除く**。

⚠️ **本書は編集していない（読むだけ）。** 以下は編集の指示書である。
⚠️ **置換前の文字列は、いずれも本ファイル内で 1 回だけ一致することを確認済み**（章ごとに機械照合。
最後に全 100 本を再照合し、**一致数が 1 でないのは「残す」の 2 件だけ**であることを確かめた —— 表 T-011 の
`V-12` / `V-12a` と、`FR-029` / `FR-036` の `ORIGIN`。どちらも置換しないので支障はない）。
⚠️ 行番号は書かない（同時編集でずれるため）。⚠️ **`⏎` は改行を表す。**

## 数（自分で数えた）

**判定した箇所 137。** 内訳は **除く 88 / 残す 43 / 要判断 6。**
`docs/spec/01-04-requirements.md` を先頭から末尾（3,774 行）まで 300 行ずつ 13 回に分けて読み、
判定した箇所を 1 件ずつ表に載せた。**「残す」も全部載せてある**（判定した証跡）。

⚠️ **パターン検索だけでは足りなかった。** 検索に掛からないのに経緯だった箇所（過去形の
「〜だった」「〜が決まらなかった」「以前は」「〜を要求から外した」「旧」を含まない言い換え）が
**20 件以上**あり、前後を読んで拾った。逆に検索に掛かるのに経緯でない箇所（「復旧」の「旧」、
`UN-15` の「改名」、`FR-008` の担当者の改名）も除外した。

**要判断 6 件の所在** —— `FR-094` の「宙に浮いていた」表（2 件）と、表 T-026 の決着済み行（4 件）。
どちらも**他所からの参照があるか、他に同じ情報が無いかの確認が要る**もので、単独では消せない。

---

## Chapter 1（Foundation。読んだ範囲: 冒頭 〜 1.9 表記規約）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1-01 | 残す | （1.4）`**性能は骨格の段階で実測してから機能を積む。** 前プロジェクトは骨格に性能ゲートを置くところまでは正しく行い、そこで 1 度測ったきり完成版を一度も測らなかった。` | （変更なし） | 前プロジェクトの事実が**理由として機能している**。「なぜ節目ごとに再計測するのか」の答えそのもの。CR-114 §5-2 の「残す」実例と同型 |
| 1-02 | 除く | `決める。** 前プロジェクトは描画方式を決めているが、その決定記録は引継ぎ資産に含まれておらず、残っているのは「次期にも同じ選択を推奨する」という推奨までである（`previous-project-result/09-architecture/architecture-entry-ja.md` §1）。**推奨を決定として書かない。** 方式の理由と代償は` | `決める。** 方式の理由と代償は` | 引継ぎ資産に決定記録が無かったという**経緯**。Chapter 5 が決めるという事実と参照先は残るので、仕様も理由も失われない。「推奨を決定として書かない」は執筆者向けの作業規則で、製品の仕様でも採用理由でもない |
| 1-03 | 除く | `（`FR-069`。利用者判断、2026-08-13）` | `（`FR-069`）` | **いつ誰が決めたか**。CR-114 §4 が除く対象として名指ししている実例 |
| 1-04 | 除く | `受け入れたうえで名乗らない（確定 2026-08-13）` | `受け入れたうえで名乗らない` | **いつ決めたか**。LM-2b の内容（キーボードだけでは完了できない／その理由）は残る |
| 1-05 | 除く | `**最優先事項を守るほうを選ぶ**（確定 2026-08-12）` | `**最優先事項を守るほうを選ぶ**` | **いつ決めたか**。LM-2a の理由（概算と両立しない／実測は `GL-003` を崩す）は残る |
| 1-06 | 残す | （LM-10）`前プロジェクトが特定製品で検証した挙動を、役割名へ置き換えたことで一般論のように読める形になっている。` | （変更なし） | 「なぜ 1 つの対向ツールでしか確かめられていないのか」「なぜ接続先ごとに実機確認が要るのか」の**理由そのもの**。消すと制限の根拠が失われる |
| 1-07 | 除く | `前プロジェクトが慎重に選んだ語を 1 つも落とさずに引き継いだもので、データの語` | `データの語` | 辞書の**出自の記述**。辞書が正であること・5 つの表からなることは残り、仕様も理由も失われない |
| 1-08 | 除く | `。** 本書はこの型の欠陥を繰り返し作っている。**`_assets/tbl-settings.md` の `S-105`` | `。** **`_assets/tbl-settings.md` の `S-105`` | **レビューで何が見つかったか**の記録。規則の理由は直前の「重複は必ず矛盾に育つ」が持っており、消しても失われない |
| 1-09 | 残す | （命名の規約）`同じものを画面側とデータ側で別の語で呼ぶことが、前プロジェクトで不具合が止まらなかった根因である。` | （変更なし） | 前プロジェクトの事実が**「なぜモデルの語彙に一本化するのか」の理由**として機能している |
| 1-10 | 残す | （冒頭）`> **Chapter 3 と 4 はまだ器である。** 未記入の節は引用で始まる行を持つ。` | （変更なし） | 検討の経緯ではなく**現在の文書の状態**の告知。CR-114 の対象外 |
| 1-11 | 残す | （1.1）`**製品名は「いま何で描かれているか」の例として挙げるだけであり、それらの製品への依存でも評価でもない。**` | （変更なし） | 記述の**読み方の指定**であり、経緯ではない |

## Chapter 2（System Overview）＋ 3.1 / 3.2 の表 T-011

**Chapter 2 全体（2.1 概要図 〜 2.4 持たないもの）に経緯の文は 1 件も無い。**

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 2-01 | 除く | `（**`V-10` から分けたもの。** 合流は形式に依らなくなり、`UC-010` にも `UC-011` にも属さなくなったため。2026-08-13）` | `（**合流は形式に依らないため、`UC-010` にも `UC-011` にも属さない**）` | 「`V-10` から分けた」は**前はこう書いてあった**、「2026-08-13」は**いつ決めたか**。⚠️ **理由（どの UC にも属さないこと）は書き直して残す** |
| 2-02 | 残す | （V-12 / V-12a）`（要望の項番に対応するものは無い。前プロジェクトの AI 共同編集トライアルの成果から起こした）` ⚠️ **同一文字列が 2 回一致する**（V-12 と V-12a。どちらも残すので置換しない） | （変更なし） | 要望の項番を持たない UC が**なぜ立っているか**の唯一の根拠。前プロジェクトの事実が理由として機能している |
| 2-03 | 残す | （V-4）`（**表 T-026 の `RC-6` により 2 種へ縮小**）` | （変更なし） | 表 T-011 は**要望の取りこぼしを防ぐ表**であり、要望をどこまで受けたかは覆いの状態そのもの。仕様側の帰結 |
| 2-04 | 残す | （V-5）`（**モード切替のみ取り下げ。重ね順と局所分離は `FR-011` と表 T-020 が受ける**）` | （変更なし） | 同上。要望のどこを受けなかったかは表 T-011 の本体 |
| 2-05 | 残す | （V-7）`（**素の左ドラッグは範囲選択へ変更。パンは `Ctrl`・中ボタン・スクロールバーに残す**）` | （変更なし） | 同上。**いまどう動くか**を述べており、仕様である |
| 2-06 | 残す | `欠番 3 件のうち [11](../../previous-project-result/user-order.md#アイテムタスク--マイルストーン) と [14](../../previous-project-result/user-order.md#アイテムタスク--マイルストーン) は取り下げられたもの` | （変更なし） | 「なぜ覆いが無いか」の説明。取りこぼしでないことの証明で、表 T-011 の目的そのもの |
| 2-07 | 残す | `⚠️ **将来の拡張（項 65）にユースケースを立てないのは、いま作らないものにユースケースを立てないという判断である**` | （変更なし） | **こう決めた理由**。いつ誰がは書かれていない |

## Chapter 3.2 の各ユースケース（UC-001 〜 UC-013）

**UC-001 〜 UC-011 の本体（STATEMENT / SCENARIO / EXTENSIONS）に経緯の文は 1 件も無い。**
経緯は `UC-014` の後の注記と `UC-013` の後の注記の 2 か所に集中している。

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 3-01 | 除く | `> **`UC-010`（外部 WBS マスタとやりとりする）から分けたものである**（利用者判断、2026-08-13）。` | `> **合流は `UC-010`（外部 WBS マスタとやりとりする）には属さない。**` | 「分けた」は**前はこう書いてあった**、「利用者判断、2026-08-13」は**いつ誰が決めたか**。⚠️ **続く 3 行が「なぜ属さないか」の理由なので、結論の文へ書き直して残す** |
| 3-02 | 除く | `**合流はもう形式に依らない**` | `**合流は形式に依らない**` | 「もう」は**前の版との対比**。仕様（形式に依らない）は残る |
| 3-03 | 除く | `` `UC-010` に置いたままだと、`` | `` `UC-010` に置くと、`` | 「置いたままだと」は**前はそこに置いてあった**という経緯を前提にする。条件文としての意味は変わらない |
| 3-04 | 残す | `` `UC-010` を形式に依らない書き方へ広げる案は採らない `` | （変更なし） | 却下案の記録ではなく、**いまの構成を採る理由**（広げると `FR-021` / `FR-057` / `FR-058` の根拠が薄くなる）を述べている |
| 3-05 | 除く | `**本ユースケースは仕様と設計まで書き、実装は後の節目へ回す**（確定 2026-08-12）。` | `**本ユースケースは仕様と設計まで書き、実装は後の節目へ回す。**` | **いつ決めたか**。範囲の決定と、次行の理由（`Agent API` の形が対話を載せられなくなる）は残る |

## Chapter 4.1（機能要求）— `FR-001` 〜 `FR-003`（表 T-014 まで）

⚠️ **`⏎` は改行を表す**（表の中に生の改行を書けないため）。`⏎` を含む行は**行ごと消す**指示である。

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 4-01 | 除く | `UC-001 手順 1 / UC-003 手順 3（形状は作るときにしか選べなかった）` | `UC-001 手順 1 / UC-003 手順 3` | `FR-083` の `ORIGIN`。**括弧の中だけを落とす**（CR-114 §5-3）。中身は「前はこうだった」 |
| 4-02 | 除く | `**RATIONALE**: **`shapeKind` は表 T-016 の `PR-11` として持っているのに、置いたあとで変える手段が要求として無かった。** 形は最初から決まっているとは限らず` | `**RATIONALE**: 形は最初から決まっているとは限らず` | **レビューで何が足りなかったか**。理由の本体（形は後から変える作業が普通に起きる／点と期間でデータが違う）は後続に残る |
| 4-03 | 除く | `を意味する**（利用者判断、2026-08-13）。` | `を意味する。**` | `FR-083` の注（描いた直後に別の形を押す操作の意味）。**いつ誰が決めたか** |
| 4-04 | 除く | `**RATIONALE**: 表 T-012 はタスク形状の 1 値として `'milestone'` を持つだけで、**その中のどの図形を使うかを人が選べることを定めていなかった。** マイルストーンは` | `**RATIONALE**: マイルストーンは` | `FR-078`。**レビューで何が足りなかったか**。理由（最も注目される印／形で意味を伝える／`FR-030` の実現手段）は残る |
| 4-05 | 除く | `ことである。角丸（`FR-019`）と依存線の太さは個別に非追随と定めていたが、**横断の要求として持っていなかった。**` | `ことである。` | `FR-079`。**レビューで何が足りなかったか**。非追随の規則そのものは `FR-019` と `FR-094` が持つので失われない。⚠️ **4-10 と連動**（表 T-201 の表がこの文を引用している） |
| 4-06 | 除く | `**RATIONALE**: **他の値の表（表 T-205 〜 T-215）はすべて要求から名指しで指されているのに、最大の表である表 T-201 だけが指されていなかった。** 行 ID でもキー名でも要求文書に一度も現れない行が 41 あり、`importSeq` で見つかったのと同じ型が、その規模で残っていた。` | `**RATIONALE**: **寸法の正は表 T-201 であり、要求がそれを名指しで指さないと、実装が独自の値を持つ余地が残る。** 同じ図が場所によって違う寸法で描かれたとき、原因を表からも要求からも辿れなくなる。` | `FR-094`。**レビューで何が足りなかったか**（41 行が指されていなかった）。⚠️ **なぜこの要求が要るかを説明していないので、理由を書き直したうえで除く** |
| 4-07 | 要判断 | `**とくに次の 3 つが宙に浮いていた。**` | `**表 T-201 を指さないと、次の 3 つが決まらない。**` | 「宙に浮いていた」は**レビューの記録**だが、続く表の右列は**導出**（段の高さ・文字サイズ）を持つ。⚠️ **表ごと除いてよいかは、`FR-003` の `ST-9` と `FR-077` に同じ導出があるかを読んでから決める必要がある** |
| 4-08 | 要判断 | `| 浮いていたもの | 無いと何が決まらないか |` | `| 決めるもの | 表 T-201 を指さないと何が決まらないか |` | 4-07 と一体。4-07 を除くなら本行も直す |
| 4-09 | 除く | `| 予定の縦幅（`basePlanHeight`） | **ラベルの文字サイズがバーの高さから導かれること。** `FR-077` は「タスクの高さに比例させた文字」と事実として述べるだけで、比例させることを求めていない |` | `| 予定の縦幅（`basePlanHeight`） | **ラベルの文字サイズがバーの高さから導かれること**（`FR-077`） |` | 後半は**レビューで何が足りなかったか**。決まらないものの中身は残る |
| 4-10 | 除く | `| 依存線の寸法（太さ・矢じり・走り） | `FR-079` の RATIONALE が「依存線の太さは個別に非追随と定めていた」と述べるのに、**その「個別に定めた」箇所が要求に無かった** |` | `| 依存線の寸法（太さ・矢じり・走り） | **太さ・矢じり・走りの値を持つのは表 T-201 だけであり、ここを指さないと非追随の対象が決まらない**（`FR-079`） |` | **レビューで何が足りなかったか**。⚠️ **4-05 を実行すると引用元の文が消えるので、本行は 4-05 と必ず同じ塊で直す** |
| 4-11 | 除く | `適用しただけである。**表の側は 1 行も変えていない。**` | `適用しただけである。` | **編集の履歴**（この改訂で表を変えていない）。⚠️ **直前の「本要求は新しい規則を作らない」は残す** —— 規則の射程を縛る文であり、消すと `FR-094` が新しい規則を作れることになる（CR-114 §6 が挙げる事故の型） |
| 4-12 | 除く | `表 T-016 の PR-14 にデータ項目としては載っており、**拡張領域を使う 2 つはこれだけ**なのに、描く要求も編集する要求も無かった。` | `表 T-016 の PR-14 がデータ項目として持ち、**拡張領域を使う 2 つはこれだけである。**` | `FR-075`。「要求が無かった」は**レビューの記録**。拡張領域を使う 2 つだけという事実は仕様側の情報なので残す |
| 4-13 | 除く | `**RATIONALE**: **`UC-002` 手順 1（行の名前）は `FR-085` が受けているのに、`UC-001` 手順 3（タスクの名称）を受ける要求が 1 件も無かった。** 入口は表の行（表 T-023 の `MK-13`、表 T-023d の `GR-10`）と表 T-016 の `PR-1` にしかなく、**細目を持つ表だけがあって、それを要求する文が無い状態だった。**` | `**RATIONALE**: **名前の無いタスクは日程表として読めない。** 入口は表の行（表 T-023 の `MK-13`、表 T-023d の `GR-10`）と表 T-016 の `PR-1` である。` | `FR-091`。CR-114 §5-2 が挙げる `FR-085` と同型で、**なぜこの要求が要るかを説明していない**。理由を書き直し、入口の参照だけ残す |
| 4-14 | 除く | `**RATIONALE**: **この方針は 3 か所から「既にあるもの」として参照されていたのに、定める要求がどこにも無かった** —— `LM-2a`・表 T-026 の `RC-3`・`_assets/tbl-settings.md` の `S-30` である。` | `**RATIONALE**: **この方針は `LM-2a`・表 T-026 の `RC-3`・`_assets/tbl-settings.md` の `S-30` の 3 か所が前提としている。**` | `FR-093`。「定める要求が無かった」は**レビューの記録**。参照元 3 か所は追跡に要るので残す。⚠️ **実測を禁じる理由は `LM-2a` が持つ**と後段が明示しているので、ここで理由を書き足さない（1.9 の重複禁止） |
| 4-15 | 除く | `挙げている。** 定める要求が無いということは、**適合範囲を狭めた根拠が仕様に存在しない**ということである。` | `挙げている。**` | `FR-093`。**レビューで何が足りなかったか**。`LM-2a` がこの方針を唯一の根拠にしているという事実は残る |
| 4-16 | 除く | `⏎| ST-11 | （廃止。人が段を指定する手段を設けないので、指定どうしの衝突が起きない） |` | （空文字。**行ごと消す**） | 表 T-014 の `ST-11`。**何をやめたか**の記録。理由（人が段を指定しない）は `ST-6` が持つので失われない。行 ID は席番号なので、詰め直さず欠番のままにする |
| 4-17 | 残す | （`FR-003`）`外した側を受ける要求が無いと、値だけがあって変える手段の無い設定になる。` | （変更なし） | 「無かった」ではなく**条件から導かれる帰結**。積む向きを選べるようにする MUST の理由そのもの |
| 4-18 | 残す | （表 T-014 `ST-6`）`⚠️ **要望 30-6「人が `stackOrder` を指定したら優先する」を取り下げた結果である**` | （変更なし） | 表 T-011 と同じ**要望の覆いの記録**。続く「日付が動くたびに破綻する」が理由として付いている |
| 4-19 | 残す | `⚠️ **矩形と矢羽根で切り詰めの規則が違うのは、参照した実装がそうなっているだけで根拠が無い。**` | （変更なし） | 前の実装の事実が、**未決事項（表 T-026 の `RC-7`）を残す理由**として機能している |
| 4-20 | 残す | （4.1 冒頭）`**本節の件数を散文に書かない** —— 数は増減するので、書くと必ず腐る。` | （変更なし） | 書き方の規則とその理由。経緯ではない |

## Chapter 4.1（続き）— 表 T-038 〜 `FR-031`

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 5-01 | 除く | `**算入するかどうかは、表示の切り替えでタスクの位置が動くかどうかで決めている**（利用者判断、2026-08-13）。` | `**算入するかどうかは、表示の切り替えでタスクの位置が動くかどうかで決めている。**` | **いつ誰が決めたか**。判定の基準そのものは残る |
| 5-02 | 除く | `**RATIONALE**: **`UC-002` の手順 1「作成者が行見出しパネルで `TaskGroup` を作り、名前を付ける」を受ける要求が無かった。** `FR-004` は畳む・展開する・表示を切り替えるだけ、`FR-005` は既にある階層を移すだけで、**行を新しく作る経路がどこにも定められていなかった。** それでいて表 T-014 の `ST-4` は「1 か所へ集めたいときは人が専用の行を作る」と、`FR-058` は「上限が掛かるのは人が階層を作るときだけである（`FR-004`）」と、**存在しない要求を前提にして書かれていた。** 消す要求（`FR-032`）だけがあって作る要求が無い状態だった。` | `**RATIONALE**: **行を作れなければタスクを置く場所が無い。** 1 行 ＝ 1 対象がマルチバーの中核であり、器を作る手段が無ければ日程表が始まらない。表 T-014 の `ST-4`（1 か所へ集めたいときは人が専用の行を作る）と `FR-058`（上限が掛かるのは人が階層を作るとき）は、本要求が与える経路を前提にしている。` | `FR-085`。**CR-114 §4 / §5-2 が名指しした実例。** 全文がレビューの欠落記録で、**なぜこの要求が要るかを説明していない。** 理由を書き直し、**`ST-4` と `FR-058` が本要求に依存していることは残す**（消すと依存が読めなくなる） |
| 5-03 | 除く | `戻せば色も戻る。** `FR-007` は「指定しなければテーマ色に追随する」とだけ述べており、その追随先を人が決められることを定めていなかった。` | `戻せば色も戻る。**` | `FR-041`。**レビューで何が足りなかったか**。テーマ色を人が決められること自体は STATEMENT が持つ |
| 5-04 | 残す | （表 T-038 の下）`| 進捗マーカーと再開アイコン（`OC-3` / `OC-4`） | **表示を切り替えてもタスクの位置が動かないようにするため。** イナズマ線を占有幅に数えないのと同じ考え方である |` | （変更なし） | **こう決めた理由**。日付も人名も入っていない |
| 5-05 | 残す | （`FR-005`）`**RATIONALE**: **この非対称が要である。**` | （変更なし） | 仕様の理由。経緯ではない |
| 5-06 | 残す | （`FR-072`）`どちらを出しているかの規則が無いと、押すたびに違うものが出る道具になる。**` | （変更なし） | 「無かった」ではなく**規則が無い場合の帰結**。要求の理由そのもの |
| 5-07 | 残す | （`FR-007`）`**戻す入口が無いと色は片道になり、一度でも触った要素はテーマを変えても永久に取り残される。**` | （変更なし） | 同上。仮定の帰結であって、レビューの記録ではない |

## Chapter 4.1（続き）— 表 T-027 〜 表 T-020（`FR-011` まで）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 6-01 | 除く | `UC-001 手順 5 / UC-003 手順 1 / UC-004 手順 2（選択を作る手段が定められていなかった）` | `UC-001 手順 5 / UC-003 手順 1 / UC-004 手順 2` | `FR-081` の `ORIGIN`。**括弧の中だけを落とす**（CR-114 §5-3） |
| 6-02 | 除く | `**RATIONALE**: **`FR-032`（削除）・`FR-033`（複製）・`FR-034`（整列）・表 T-036 の `SK-3` はいずれも「選ばれた」複数を前提にしているのに、その複数を作る要求がどこにも無かった。** 入口は `Ctrl+A` の全選択しか定義されておらず、一部を選ぶ手段が仕様に存在しなかった。` | `**RATIONALE**: **`FR-032`（削除）・`FR-033`（複製）・`FR-034`（整列）・表 T-036 の `SK-3` はいずれも「選ばれた」複数を前提とする。** 一部を選ぶ手段が無ければ、それらはどれも全選択でしか使えない。` | `FR-081`。**レビューで何が足りなかったか**。⚠️ **どの要求が選択に依存しているかは残す**（消すと `FR-081` を消したときの波及が読めなくなる） |
| 6-03 | 除く | `**ORIGIN**: UC-001 / UC-002（削除の手順が無かった）` | `**ORIGIN**: UC-001 / UC-002` | `FR-032` の `ORIGIN`。**CR-114 §5-3 が名指しした実例。フィールドは残し、括弧の中だけを落とす** |
| 6-04 | 除く | `⚠️ **「他の図形と極力かぶらない経路」という言い方を要求から外した。**` | `⚠️ **「他の図形と極力かぶらない経路」は要求として書けない。**` | 「外した」は**前はこう書いてあった**。⚠️ **書けない理由（「極力」に合否の線が引けない）と検証範囲（`RT-1` 〜 `RT-4`）は残す** |
| 6-05 | 残す | （表 T-027 `UN-15`）`**担当者（資源）と割当の追加・改名・削除**` | （変更なし） | 「改名」は**利用者が行う操作の名**。経緯ではない（パターン検索の偽陽性） |
| 6-06 | 残す | （`FR-009` の経路）`交差は 0 にできないと実測されているので、**減らす努力ではなく読ませる手当てで解く。**` | （変更なし） | 実測の事実が**こう決めた理由**として機能している。いつ誰がは書かれていない |
| 6-07 | 残す | （`FR-032`）`**RATIONALE**: **連鎖して消えるものを黙って消すと、利用者は何が失われたかを知る手段を持たない。**` | （変更なし） | 仕様の理由。CR-114 §5-2 が「残す」側に挙げた「まとめて消すほうが利用者の直感に合う」がこの直後に続く |

## Chapter 4.1（続き）— `FR-084` 〜 表 T-023a（`FR-016` まで）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 7-01 | 除く | `表 T-019 の 5 状態のうち 2 つ（PA-3 / PA-4）は再開予定日の有無で分かれるが、**その日付を人が置く手段が定められていなかった。**` | `表 T-019 の 5 状態のうち 2 つ（PA-3 / PA-4）は再開予定日の有無で分かれるので、**その日付を人が置く手段が要る。**` | `FR-044`。**レビューで何が足りなかったか**。理由（5 状態が再開日で分かれる）は現在形へ書き直して残す |
| 7-02 | 除く | `120 日かかれば 120 である（利用者判断、2026-08-13）。` | `120 日かかれば 120 である。` | `FR-012`。**いつ誰が決めたか**。丸めない規則と例は残る |
| 7-03 | 除く | `**後から式を変える判断があり得る**（利用者判断、2026-08-13）。` | `**後から式を変える判断があり得る。**` | `FR-012`。**いつ誰が決めたか**。変更耐性を高く保つ理由は残る |
| 7-04 | 除く | `二重になる。表 T-016 の PR-10 にデータ項目としては載っていたが、**描く要求が無かった。**` | `二重になる。` | `FR-045`。**レビューで何が足りなかったか**。期限が独立した約束であることが理由の本体 |
| 7-05 | 除く | `**縦線を 1 本に統合した理由**（利用者判断、2026-08-13）—— ` | `**縦線を 1 本にする理由** —— ` | `FR-046`。**いつ誰が決めたか**。統合の理由（縦線が増えると意味が読めない／MSPDI と食い違わない）は残す |
| 7-06 | 除く | `⚠️ **これにより `WY-2`（同じ JSON から同じ SVG / PNG）が成立するようになる** —— 旧「本日線」は実行時のシステム日付に位置が依存していたので、同じ JSON を翌日開くと線が動いていた。` | `⚠️ **これにより `WY-2`（同じ JSON から同じ SVG / PNG）が成立する** —— 本日を線の位置に使うと実行時のシステム日付に依存し、同じ JSON を翌日開くと線が動く。` | 「旧『本日線』」は**前はこう書いてあった**。⚠️ **`WY-2` が成立する理屈は仕様の根拠なので、現在形へ書き直して残す** |
| 7-07 | 除く | `⚠️ **起点と単位を定めていなかったので、同じタスクでも実装ごとに違う日数が出た。**` | `⚠️ **起点と単位を定めないと、同じタスクでも実装ごとに違う日数が出る。**` | `FR-047`。過去形は**前の版で何が起きたか**の記録。理由は現在形で同じだけ成り立つ |
| 7-08 | 除く | `判定順序（表 T-023a）を置く。** 以前は「行の上でドラッグ」と「背景の上でドラッグ」という**場所**で書き分けていたため、行の空き領域が両方に当たっていた。` | `判定順序（表 T-023a）を置く。**` | `FR-016`。**前はこう書いてあった**。理由（同じ操作に 2 つの答えを立てない）は残る |
| 7-09 | 残す | （表 T-022 の下）`**完了したタスクを尖らせないのは意図的な判断である。**` | （変更なし） | **こう決めた理由**（作図慣行をそのまま実装すると遅れて見える）。いつ誰がは書かれていない |
| 7-10 | 残す | （`FR-043`）`**掴むことと押すことを 1 つの入口が受けるのも、遅れをマーカーではなく入口の形で表すのも、理由は同じである**` | （変更なし） | 仕様の理由。経緯ではない |

## Chapter 4.1（続き）— 表 T-023b 〜 `FR-018`

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 8-01 | 除く | `これは意図した設計である**（利用者判断、2026-08-13）—— ` | `これは意図した設計である** —— ` | 表 T-023 の `MK-9a`。**いつ誰が決めたか**。意図した設計であることと、その理由は残る |
| 8-02 | 除く | `| SK-1 | （廃止。キーボードだけで図形を置く経路は持たない。表 T-026 の `RC-10`） | — |` | `| SK-1 | **キーボードだけで図形を置く経路は持たない**（表 T-026 の `RC-10`） | — |` | 「廃止」は**何をやめたか**。⚠️ **規則そのもの（キーボードだけで図形を置かない）は残す** —— 消すと `LM-2b` と `RC-10` が指す規則が仕様から消える（CR-114 §6 の事故の型）。行 ID は席番号なので詰め直さない |
| 8-03 | 残す | `| SK-1a | （同上） | — |` | （変更なし） | `SK-1` を指すだけの行。8-02 を実行すれば内容も揃う |
| 8-04 | 除く | `> ⚠️ **員数が引継ぎ資産の中で食い違っている。** アイコン案は「現行 7 個 ＋ 新設 6 個」と記すが、`ui-detail-spec-ja.md` §5-1 に実在する編集の割当は、アイコン案が「現行」とした数を上回る。**本表は実在が確かめられたものと、新設と宣言されたものを合わせた全数である。**行数は本表が持ち、散文に書かない。**引継ぎ資産への書き戻しは未処理である。**` | `> ⚠️ **本表がショートカットキーの割当の全数である。**行数は本表が持ち、散文に書かない。` | 引継ぎ資産の員数の食い違いは**前プロジェクトがどうだったか**、書き戻し未処理は**作業の残り**。⚠️ **どちらも CR 側へ移す**（未処理の作業は CR-114 §8 に写したうえで別 CR か課題表へ） |
| 8-05 | 除く | `**ORIGIN**: UC-007（`FR-054` は暦に従って描くことだけを定め、暦を人が直す入口が無かった）` | `**ORIGIN**: UC-007` | `FR-088` の `ORIGIN`。**括弧の中だけを落とす**（CR-114 §5-3） |
| 8-06 | 除く | `**RATIONALE**: **設定値表の `S-107` が「必要なら MSPDI を取り込むか、**非稼働日として個別に置く**」と、置く経路の存在を前提にして書かれていた。** 既定の暦は祝日を持たない` | `**RATIONALE**: **設定値表の `S-107`（必要なら MSPDI を取り込むか、非稼働日として個別に置く）は、本要求が与える経路を前提にしている。** 既定の暦は祝日を持たない` | 「前提にして書かれていた」は**レビューで何が足りなかったか**。⚠️ **`S-107` が本要求に依存する事実は残す** |
| 8-07 | 除く | `**ORIGIN**: UC-007（要望 39。表 T-011 の V-7 が吸収済みと記していたが要求が無かった） ／ UC-005` | `**ORIGIN**: UC-007（要望 39） ／ UC-005` | `FR-054` の `ORIGIN`。**要望の項番は残し、レビューの記録だけを落とす**（`FR-070` の `UC-007（要望 64）` と同じ形になる） |
| 8-08 | 除く | `**RATIONALE**: **`FR-011` と `FR-012` はどちらも「稼働日」で計算すると述べているのに、稼働日が何かを定める要求が無かった。**` | `**RATIONALE**: **`FR-011` と `FR-012` はどちらも「稼働日」で計算するので、稼働日が何かを定める要求が要る。**` | `FR-054`。**レビューで何が足りなかったか**。依存元（`FR-011` / `FR-012`）は残す |
| 8-09 | 除く | `**ORIGIN**: UC-007 手順 3（表 T-202 の `S-67` と用語辞書の `U-17` が存在を前提にしているのに、描く要求が無かった）` | `**ORIGIN**: UC-007 手順 3` | `FR-089` の `ORIGIN`。**括弧の中だけを落とす** |
| 8-10 | 除く | `**RATIONALE**: **`S-67` の既定が `false` である理由が「縦線を目盛の段階に連動させる規定がまだ無い」というものだった** —— つまり**規定が無いこと自体が既定値を決めていた。** 連動させないと` | `**RATIONALE**: 連動させないと` | `FR-089`。**前はこう書いてあった**（規定が無いことが既定値を決めていた）。⚠️ **既定値を `true` にしてよいかの判断は表 T-026 の `RC-5` が持つ**ので、消しても未決事項は失われない |
| 8-11 | 除く | `前プロジェクトの場当たりであり、本プロジェクトは起動の手順で根治する。` | `本プロジェクトは起動の手順で根治する。` | `FR-018`。**CR-114 §4 が除く対象として名指しした実例。** 前プロジェクトがどうだったかの記述。⚠️ **禁止の理由（起動の手順で根治する／設定として残すと根治しない）は残す** |
| 8-12 | 残す | （`FR-018`）`実測では 1 ノッチで行が 9 から 21 に増えている。` | （変更なし） | 実測値が**しきい値をまたぐ丸めを禁じる理由**として機能している |
| 8-13 | 残す | （`FR-089`）`⚠️ **既定値を `true` に変えてよいかは、この連動が実物で確かめられてから決める**（表 T-026 の `RC-5`）。` | （変更なし） | 未決事項の宣言であり、経緯ではない |

## Chapter 4.1（続き）— `FR-098` 〜 `FR-056`

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 9-01 | 除く | `情報量が多いのではなく 0 である。** `FR-025` は書き出しのときの下限を扱っていたが、**画面表示側の下限が要求として無かった。**` | `情報量が多いのではなく 0 である。** **`FR-025` が扱うのは書き出しのときの下限であり、本要求は画面表示側の下限を定める。**` | `FR-077`。「無かった」は**レビューの記録**。⚠️ **`FR-025` との棲み分けは残す**（消すと 2 つの下限が同じものに読める） |
| 9-02 | 除く | `**RATIONALE**: `GL-002`（全体日程を 1 画面で見られる）を受ける操作が、ユースケースにも要求にも無かった。**測る対象は描画の実寸とし、` | `**RATIONALE**: `GL-002`（全体日程を 1 画面で見られる）を 1 つの操作で果たすための要求である。**測る対象は描画の実寸とし、` | `FR-055`。**レビューで何が足りなかったか**。⚠️ **どの目標を受ける要求かは残す** |
| 9-03 | 除く | `⚠️ **そこまで落としても 1 画面に収まるとは限らない**（利用者判断、2026-08-13）—— ` | `⚠️ **そこまで落としても 1 画面に収まるとは限らない** —— ` | `FR-055`。**いつ誰が決めたか**。収まらない理由と `MC-7` での判定は残る |
| 9-04 | 除く | `**ORIGIN**: UC-007（要望 40〜43。表 T-011 の V-7 が吸収済みと記していたが要求が無かった）` | `**ORIGIN**: UC-007（要望 40〜43）` | `FR-048` の `ORIGIN`。**要望の項番は残し、レビューの記録だけを落とす** |
| 9-05 | 除く | `**ORIGIN**: UC-007（`FR-048` は 3 種を「出し分ける」ことしか定めておらず、2 本を置く手段が無かった）` | `**ORIGIN**: UC-007` | `FR-082` の `ORIGIN`。**括弧の中だけを落とす** |
| 9-06 | 除く | `**RATIONALE**: **測る道具なのに、測る位置を人が決める手段が要求として無かった。** `dualCursor` は 2 つの日付を持つと定めながら（`tbl-settings.md` の S-65）、その日付をどう入れるかがどこにも書かれていない。` | `**RATIONALE**: **測る道具なので、測る位置を人が決める手段が要る。** `dualCursor` が持つ 2 つの日付（`tbl-settings.md` の `S-65`）は、本要求の操作で入る。` | `FR-082`。**レビューで何が足りなかったか**。⚠️ **`S-65` の日付がどこから入るかは残す** |
| 9-07 | 除く | `**RATIONALE**: **`UC-009` の拡張 2a「開いた者の名前が設定されていない → システムは名前の設定を促す」を受ける要求が無く、名前を人が設定する手段そのものが要求に無かった。** `tbl-settings.md` の `S-99a` は保管先を書いているだけである。`GL-007`` | `**RATIONALE**: **`tbl-settings.md` の `S-99a` は名前の保管先を定めるだけで、名前が入る経路は本要求が与える。** `GL-007`` | `FR-086`。**レビューで何が足りなかったか**。⚠️ **`GL-007` が `FR-020` 1 本で支えられ、名前の経路が無いと未達になるという理由は残る**（この文の後ろ） |
| 9-08 | 除く | `**一括と 1 件ずつの軸を持たせた理由**（利用者判断、2026-08-13）—— ` | `**一括と 1 件ずつの軸を持たせる理由** —— ` | `FR-022`。**いつ誰が決めたか**。理由（100 件を 100 回問えない／黙って倒すと戻せないタスクが増える）は残る |
| 9-09 | 残す | （`FR-049`）`**担当ラベルと完了率ラベルの既定が非表示である**のは、低ズームでの段数膨張の主因がこの 2 つだと実測されたためで、`GL-002` を守るための判断である。` | （変更なし） | 実測の事実が**既定値をそう決めた理由**として機能している |
| 9-10 | 残す | （`FR-021`）`> ⚠️ **保証の範囲を限定しているのは、守れない約束を置かないためである。**` | （変更なし） | **こう決めた理由**。日付も人名も無い |

## Chapter 4.1（続き）— 表 T-032 〜 `FR-061`

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 10-01 | 除く | `同じ選択肢から選ばせること（MUST）**（利用者判断、2026-08-13）。` | `同じ選択肢から選ばせること（MUST）**。` | 表 T-032 の `MG-12`。**いつ誰が決めたか** |
| 10-02 | 除く | `**RATIONALE**: `FR-021` は「情報を落とさない」ことだけを述べており、**相手のツールが開けるかどうか**と**編集していない部分を触らないこと**を定めていなかった。` | `**RATIONALE**: `FR-021` が述べるのは「情報を落とさない」ことだけであり、**相手のツールが開けるかどうか**と**編集していない部分を触らないこと**は本要求が定める。` | `FR-057`。**レビューで何が足りなかったか**。⚠️ **`FR-021` との棲み分け（往復無損失は 2 つ揃って成立する）は残す** |
| 10-03 | 除く | `**ORIGIN**: UC-003 手順 4（担当ラベルには `FR-059` があるのに、完了率ラベルには描く要求が無かった）` | `**ORIGIN**: UC-003 手順 4` | `FR-090` の `ORIGIN`。**括弧の中だけを落とす** |
| 10-04 | 除く | `**RATIONALE**: **`UC-003` 手順 4 は「担当ラベルと完了率ラベルを表示の設定に従って出す」と 2 つを並べているのに、受ける要求が担当ラベルにしか無かった。** 表 T-038 の `OC-2` は 2 つを占有幅に算入しており、`S-61` は既定値を持つ。**出す場所も値の形も決まっていなかった。**` | `**RATIONALE**: **`UC-003` 手順 4 は担当ラベルと完了率ラベルを並べており、担当ラベルは `FR-059`、完了率ラベルは本要求が受ける。** 表 T-038 の `OC-2` は 2 つを占有幅に算入し、`S-61` が既定値を持つので、**出す場所と値の形をここで決める。**` | `FR-090`。**レビューで何が足りなかったか**。⚠️ **`OC-2` と `S-61` への繋がりは残す** |
| 10-05 | 除く | `` `FR-025` は出力サイズと文字サイズを文書に保存することまでを定めていたが、**見た目が一致することを求める要求が無かった。** `` | `` `FR-025` が定めるのは出力サイズと文字サイズを文書に保存することまでであり、**見た目が一致することは本要求が求める。** `` | `FR-080` の `WY-2` 注。**レビューで何が足りなかったか**。棲み分けは残す |
| 10-06 | 除く | `**達成は表 T-041 で判定すること（MUST）**（利用者判断、2026-08-13）—— ` | `**達成は表 T-041 で判定すること（MUST）** —— ` | `FR-080`。**いつ誰が決めたか** |
| 10-07 | 除く | `**ORIGIN**: UC-011 手順 1（表 T-036 の SK-10 に割当があるのに、開く操作を定める要求が無かった） / UC-010 手順 1` | `**ORIGIN**: UC-011 手順 1 / UC-010 手順 1` | `FR-087` の `ORIGIN`。**括弧の中だけを落とす** |
| 10-08 | 除く | `**RATIONALE**: **`Ctrl+O` は表 T-036 の `SK-10` に載っているのに、「開く」を定めた要求が 1 件も無かった。** 表 T-034 は` | `**RATIONALE**: 表 T-034 は` | `FR-087`。**レビューで何が足りなかったか**。⚠️ **表 T-034・`FR-022` / `FR-056` との棲み分けは残る** |
| 10-09 | 除く | `**編集途中の文書を開いたまま別のファイルを開いたとき、未保存の編集が黙って捨てられるのかどうかが決まらなかった。**` | `**編集途中の文書を開いたまま別のファイルを開いたとき、未保存の編集をどう扱うかは本要求が決める。**` | `FR-087`。過去形は**何が決まっていなかったか**の記録。決める対象は残す |
| 10-10 | 除く | `**1 つ目のファイルも 2 つ目以降も同じ経路で入る**（利用者判断、2026-08-13）。` | `**1 つ目のファイルも 2 つ目以降も同じ経路で入る。**` | 表 T-024a の `OP-2`。**いつ誰が決めたか** |
| 10-11 | 除く | `⚠️ **`FR-024`（JSON の書き出し）の対になる要求である。** 書き出しだけがあって読み込みが無い状態だった。` | `⚠️ **`FR-024`（JSON の書き出し）の対になる要求である。**` | `FR-087`。**レビューで何が足りなかったか**。対であることは残る |
| 10-12 | 除く | `**RATIONALE**: `FR-026` は保存と復旧だけを扱い、**状態が画面から読めることを定めていなかった。**` | `**RATIONALE**: `FR-026` が扱うのは保存と復旧だけであり、**状態が画面から読めることは本要求が定める。**` | `FR-061`。**レビューで何が足りなかったか**。棲み分けは残す |
| 10-13 | 残す | （`FR-058`）`**取り込んだだけでは載る器が無く、画面に何も出ない。**` | （変更なし） | 仕様の理由。経緯ではない |

## Chapter 4.1（続き）— `FR-062` 〜 `FR-069`

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 11-01 | 除く | `**RATIONALE**: `FR-027`（テンプレート）と `FR-026`（復旧）が別々に「最初に出るもの」を定めており、**どちらが勝つかが決まっていなかった。**` | `**RATIONALE**: `FR-027`（テンプレート）と `FR-026`（復旧）が別々に「最初に出るもの」を定めているので、**どちらが勝つかを本要求が決める。**` | `FR-062`。過去形の**欠落の記録**。順序が要る理由（復旧できたはずの作業が上書きされる）は残る |
| 11-02 | 除く | `**RATIONALE**: 表 T-032 の MG-4 は「プロジェクトの基本情報が衝突したら選ばせる」と前提にしているのに、**その基本情報を人が見る・直す要求が無かった。**` | `**RATIONALE**: 表 T-032 の MG-4 は「プロジェクトの基本情報が衝突したら選ばせる」と定めており、**その基本情報を人が見る・直す経路は本要求が与える。**` | `FR-074`。**レビューで何が足りなかったか**。⚠️ **`MG-4` が本要求に依存する事実は残す** |
| 11-03 | 除く | `**RATIONALE**: **コメントボックスを置く・動かす・隠す・消す要求はあったが、文字を入れる要求が 1 つも無かった。** 本文の無いコメントボックスでは` | `**RATIONALE**: 本文の無いコメントボックスでは` | `FR-097`。**レビューで何が足りなかったか**。理由（本文が無いと `UC-008` を達成できない）は残る |
| 11-04 | 除く | `**RATIONALE**: **開く側は `FR-087` が入口を 1 つと定めたのに、出す側に対になる要求が無かった。** `FR-024`（JSON）も `FR-057`（MSPDI）も「書き出したとき」から始まる規約であり、**どちらが起きるかを決める要求がどこにも無い。** そのため表 T-036 の `SK-11`（保存する）が、MSPDI から開いた文書に対して何をするのかが決まらなかった。` | `**RATIONALE**: **開く側の入口が 1 つである（`FR-087`）のと対に、出す側の入口も 1 つとする。** `FR-024`（JSON）も `FR-057`（MSPDI）も「書き出したとき」から始まる規約なので、**どちらが起きるかは本要求が決める。** 表 T-036 の `SK-11`（保存する）が MSPDI から開いた文書に対して何をするかも、ここで決まる。` | `FR-096`。**レビューで何が足りなかったか**。⚠️ **`FR-087` との対、`FR-024` / `FR-057` / `SK-11` との繋がりは残す** |
| 11-05 | 除く | `**RATIONALE**: **表 T-041 の `WY-1` が判定の手順に「初期化」を置いているのに、それを行う要求が無かった。**` | `**RATIONALE**: **表 T-041 の `WY-1` は判定の手順に「初期化」を置いており、それを行うのが本要求である。**` | `FR-095`。**レビューで何が足りなかったか**。判定条件との対応は残す |
| 11-06 | 除く | `**RATIONALE**: `FR-028` は「値で返す」ことしか定めていなかった。細目は表に置く（1.9）。` | `**RATIONALE**: `FR-028` が定めるのは「値で返す」ことだけである。細目は表に置く（1.9）。` | `FR-064`。過去形が**前はここまでしか書いていなかった**と読める。棲み分けは現在形で同じだけ成り立つ |
| 11-07 | 除く | `のに、**同格を謳う機械向けにだけ対応物が無かった。**` | `ので、**同格を謳う機械向けにも対応物が要る。**` | 表 T-035 の `AG-9a`。**レビューで何が足りなかったか**。人向け（`NT-1`）と同格にする理由は残る |
| 11-08 | 除く | `⚠️ **本要求の実装は後の節目へ回す**（確定 2026-08-12）。` | `⚠️ **本要求の実装は後の節目へ回す。**` | `FR-066`。**いつ決めたか**。回す理由（`Agent API` の形の保証）は次文に残る |
| 11-09 | 残す | （`FR-027`）`**新規に作ること（MUST）。** 前プロジェクトのテンプレートは廃止済みのモデルで書かれているので使わない。` | （変更なし） | 前プロジェクトの事実が**「なぜ流用しないか」の理由**として機能している |
| 11-10 | 残す | （`FR-027`）`前プロジェクトは散文を中立化したときに JSON 実例の識別子を見落とした。**目に入りにくいのは値のほうである。**` | （変更なし） | 前プロジェクトの事実が**「識別子の値も対象とする」理由**として機能している |
| 11-11 | 残す | （`FR-064`）`前プロジェクトの実測で、基準版の食い違いは 1 セッションに 6 回起きている。` | （変更なし） | 実測値が**`AG-2`（版の食い違いで拒否）を置く理由**として機能している |

## Chapter 4.1 の残り（`FR-092` 〜 `FR-040`）＋ 4.2 の冒頭

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 12-01 | 除く | `**ORIGIN**: `GL-006`（達成を判定する手段が無かった）` | `**ORIGIN**: `GL-006`` | `FR-092` の `ORIGIN`。**括弧の中だけを落とす** |
| 12-02 | 除く | `**RATIONALE**: **`GL-002` は「表 T-025 の `MC-7` が定める目標規模で収まること」という判定条件を持つのに、`GL-006` には対応物が無かった。** 「マニュアルを読まずに` | `**RATIONALE**: **`GL-002` は「表 T-025 の `MC-7` が定める目標規模で収まること」という判定条件を持つが、`GL-006` の判定条件は本要求が与える。** 「マニュアルを読まずに` | `FR-092`。**レビューで何が足りなかったか**。⚠️ **`GL-002` との対比は判定条件の説明なので残す** |
| 12-03 | 除く | `**手立てを全数で挙げ、1 つずつ在ることを確かめる形にする**（利用者判断、2026-08-13）。` | `**手立てを全数で挙げ、1 つずつ在ることを確かめる形にする。**` | `FR-092`。**いつ誰が決めたか** |
| 12-04 | 除く | `**図形と配置について外部の文書を正とするのは本仕様書でここだけである**（確定 2026-08-12）。` | `**図形と配置について外部の文書を正とするのは本仕様書でここだけである。**` | `FR-029`。**いつ決めたか**。外部を正とする範囲の宣言は残る |
| 12-05 | 除く | `**RATIONALE**: 本仕様書には「知らせる」と書いた要求が 10 件以上あるが、**知らせ方の規則がどこにも無かった。** `NFR-008` が適合の対象に「エラー表示」を挙げているのに、それを受ける要求も無い。` | `**RATIONALE**: 本仕様書には「知らせる」と書いた要求が 10 件以上あり、`NFR-008` は適合の対象に「エラー表示」を挙げている。` | `FR-076`。**レビューで何が足りなかったか**が 2 文。⚠️ **理由（各所で個別に決めると伝え方が食い違う）は次文に残る** |
| 12-06 | 除く | `**RATIONALE**: `GL-006` を受ける経路がアイコンとツールチップしか無く、**操作の全体像に到達する道が 1 本も無かった。**` | `**RATIONALE**: `GL-006` を受ける経路はアイコンとツールチップだけでは足りず、**操作の全体像に到達する道が要る。**` | `FR-036`。**レビューで何が足りなかったか**。理由は現在形で同じだけ成り立つ |
| 12-07 | 除く | `固定の定数へ戻す道を残すためである（利用者判断、2026-08-13）。` | `固定の定数へ戻す道を残すためである。` | `FR-039`。**いつ誰が決めたか**。独立キーにする理由は残る |
| 12-08 | 残す | `**ORIGIN**: `GL-006`（横断。特定のユースケースから来ていない）` ⚠️ **同一文字列が 2 回一致する**（`FR-029` と `FR-036`。どちらも残すので置換しない） | （変更なし） | 経緯ではなく、**`ORIGIN` が横断であることの明示**。ユースケース由来でないことは追跡に要る |
| 12-09 | 残す | （`FR-036`）`**「全機能」と「1 画面」は両立しうると実測されている**` | （変更なし） | 実測が**「諦める前に割り付けを見直せ」の理由**として機能している |
| 12-10 | 残す | （4.2 冒頭）`**性能の数値は目標値であり、実測の基準日線ではない。** 前プロジェクトが持つ実測は骨格時点の 1 組だけで、**測定条件を定める前に測られており、基準機と基準ブラウザの記録が無い。**` | （変更なし） | **CR-114 §5-2 の「残す」実例と同型。** 前プロジェクトの事実が「なぜ基準日線を本プロジェクトの骨格で取り直すのか」の答えそのもの |
| 12-11 | 残す | （4.2 冒頭）`元文書自身が「基準日線と直接比べてはならない」と警告している。` | （変更なし） | 12-10 の根拠の出所。消すと「取り直す」判断の裏づけが落ちる |

## 4.2 の各要求 ＋ 4.3（削減候補・表 T-026）— 文末まで

⚠️ **表 T-026 は「未決事項の表」である。** 打ち消し線の付いた行（`RC-4` / `RC-5` / `RC-8` / `RC-9` / `RC-10`）は
**決着済みの記録**であり、CR-114 の言う「いつ決めたか」に当たる。**ただし他所から参照されている行があるので、
参照ごと直す必要がある。** 参照の全数は次のとおり（`docs/spec` 全体を機械検索して確認）。

| 行 | 他所からの参照 |
|---|---|
| `RC-4` | **無し**（表 T-026 の中だけ） |
| `RC-5` | `FR-089`（`docs/spec/01-04-requirements.md`）。⚠️ **相互参照になっている** |
| `RC-8` | **無し** |
| `RC-9` | **無し** |
| `RC-10` | 表 T-036 の `SK-1`、`NFR-008` の 2 か所 |
| `RC-1` | `_assets/tbl-settings.md`（担当外のファイル） |

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 13-01 | 除く | `⏎| RC-4 | ~~`fontScale` の S / M / L が何 px か未定義で、表 T-205 の時間軸のしきい値がフォント 12px の解であること~~ | **決着（2026-08-13）。** px は表 T-215 が持ち、しきい値は固定のまま**判定を実効フォントサイズで割った比で行う**と定めた（表 T-205 の注） | — | **解決済み** |` | （空文字。**行ごと消す**） | 決着済みの記録。**いつ決めたか**を含む。⚠️ **他所からの参照が無いので単独で消せる。** 定めた内容は表 T-215 と表 T-205 の注が持つ |
| 13-02 | 除く | `⏎| RC-9 | ~~タイムルーラーの上でのポインタ操作に受け皿が無い~~ | **決着（2026-08-13）。ポインタ操作を持たないと明記した**（表 T-023a の適用外の面） | — | **解決済み** |` | （空文字。**行ごと消す**） | 同上。定めた内容は表 T-023a の適用外の面の表が持つ |
| 13-03 | 要判断 | `⏎| RC-8 | **行 ID の接頭辞が文書をまたいで衝突している** —— `S-` が範囲の表（表 T-002）と設定値の表（表 T-201 ほか）の両方に、`X-` が範囲外の表と用語の由来の表の両方にある | 「表の第 1 列は行 ID であり、テストが落ちたとき行 ID が仕様の 1 行を指す」という方針が、この 2 系統では成り立たない | テストが `S-1` と書いても、どちらの表の 1 行かが決まらない | **決着（2026-08-13）。範囲の表を `SO-`、範囲外の表を `XO-` へ改名した**（設定値の `S-` は行数が多く参照も多いので残した）。**行 ID の重複を機械検査に加えた** —— この誤りは執筆中に 2 度踏んだ（`GL-` と `GOAL`、`SC-` とスクロール表） |` | （空文字。**行ごと消す**） | 改名の記録と執筆中の失敗談は**経緯**。⚠️ **ただし「設定値の `S-` だけ `S-` のまま残した」という現状の説明がここにしか無い。** 消す前に 1.9 か辞書側に同じ説明があるかを確かめる必要がある |
| 13-04 | 要判断 | `⏎| RC-5 | ~~`dateGridLinesVisible` の既定 `false`~~ | **決着（2026-08-13）。** 連動の規定は `FR-089` として置いた | — | **解決済み。既定値を `true` に変えてよいかは `FR-089` が判断を持つ** |` | （空文字。**行ごと消す**） | 決着済みの記録。⚠️ **`FR-089` と相互参照になっているので、消すなら `FR-089` の `（表 T-026 の `RC-5`）。` も同時に外す**（そうしないと壊れた参照が残る） |
| 13-05 | 要判断 | `| RC-10 | ~~キーボードだけで完了できることの全数（旧 `FR-050` と、それが指していた全数表）~~` | （行ごと消すか、打ち消し線と日付を外して現在形の規則へ書き直す） | 「旧 `FR-050`」「廃止（2026-08-13）」「利用者判断、2026-08-13」は**経緯**。⚠️ **表 T-036 の `SK-1` と `NFR-008` の 2 か所が `RC-10` を指しているので、単独では消せない。** 8-02 で `SK-1` に規則を残す案を採るなら、`RC-10` は消せる |
| 13-06 | 除く | `⚠️ **2.1.1（キーボード操作可能）を適合範囲から外した経緯と、代わりに残る手段は `LM-2b` が持つ。**` | `⚠️ **2.1.1（キーボード操作可能）を適合範囲から外す理由と、代わりに残る手段は `LM-2b` が持つ。**` | `NFR-008`。**仕様書が指すのは理由であって経緯ではない**（CR-114 §5-1）。指し先は変わらない |
| 13-07 | 要判断 | `` `RC-10` が全数表を廃したのは全操作を鍵で行う要求についてであって、フォーカスの対象を空にする意味ではない。 `` | （13-05 の結論に従う。`RC-10` を消すなら `**表 T-036 が割当を持つ操作の画面上の入口と、その場の編集に入るすべての入力欄**を対象とする` だけを残す） | `NFR-008` の STATEMENT。**何を廃したか**の記録だが、**フォーカス対象を空と読ませない歯止め**でもある。13-05 と一体で判断する |
| 13-08 | 残す | （`NFR-011`）`前プロジェクトで実際に起きた 2 つの事象に対応する` | （変更なし） | 前プロジェクトの事実が**要求の理由そのもの**（0×0 の窓／位置が決まる前の初期表示）。CR-114 §5-2 の「残す」に当たる |
| 13-09 | 残す | （`RC-6`）`**性能を理由に狭めた。** 9 点から候補を探索する方式は横切り 0 / 交差 9 で最良だったが、**再レイアウトが 13.3ms** かかり` | （変更なし） | 実測値が**アンカーを 2 種に絞る理由**そのもの。未決の候補行としても生きている |
| 13-10 | 残す | （`RC-1`）`| RC-1 | `_assets/tbl-settings.md` の 🔎 付き既定値（全件） | 由来が記録されていない。測って決めた値ではない |` | （変更なし） | 打ち消し線の無い**生きた未決事項**。`RC-2` / `RC-3` / `RC-7` も同じ（決着していないので経緯ではない） |
| 13-11 | 残す | （4.3 冒頭）`**いずれも「紐づけ先が無い」ものではなく、「根拠が弱い」ものである。**` | （変更なし） | 表の読み方の指定。経緯ではない |
| 13-12 | 残す | （`NFR-005`）`Safari を入れると Mac 実機での回帰検証と SVG のテキスト幅の差分検証が恒常的に必要になり、検証費用が製品の規模に見合わない。` | （変更なし） | **こう決めた理由**。日付も人名も無い |
