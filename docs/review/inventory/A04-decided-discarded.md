# A04 — DECIDED / DISCARDED / OPEN-ITEMS

前プロジェクトが自ら「決めた」「捨てた」「未決」と宣言したものの棚卸し。
**要約ではなく、設計に効く決定の抽出である。** 各行に出典（`ファイル名:行番号`）を付けた。

---

## 0. 読んだ文書と行数

| # | 文書 | 行数 | 読んだ範囲 | 用途 |
|:--:|---|--:|---|---|
| 1 | `previous-project-result/DECIDED-ja.md` | 354 | **全文**（1–354。3 回に分けて読んだ） | 決定の索引。全 175 行の決定見出し |
| 2 | `previous-project-result/DISCARDED-ja.md` | 212 | **全文**（1–212。2 回に分けて読んだ） | 捨てた資産と理由。判断基準 9 件 |
| 3 | `previous-project-result/OPEN-ITEMS-ja.md` | 126 | **全文**（1–126） | 実機確認の残件 4 件 |
| | **合計** | **692** | | |

> **行数は `wc -l` で数えた**（`DECIDED-ja.md` 354 / `DISCARDED-ja.md` 212 / `OPEN-ITEMS-ja.md` 126 / 計 692）。指示書の数と一致する。

**照合のために開いた（全文ではない）もの** —— 在庫表（`temp/inventory/*.md`。⚠️ 指示書は「17 枚」だが、作業中に
`A01-agent-api-spec.md` / `A03-agent-open-items.md` / `A09-architecture-standard.md` ほかが増え、
**終了時点で 21 枚**（本書 `A04` を含む）になっていた。**自分で `ls` して数えた**）、および
`previous-project-result/` のファイル木（`find`。「在庫表との食い違い」X-1 の裏取り）。

> ⚠️ **`DECIDED-ja.md` は自ら「索引であって正ではない」と宣言している**（`DECIDED-ja.md:20`–`27`）。
> 「**本書を根拠に断定してはならない。実装する前に必ず所在の節そのものを開くこと**」（`DECIDED-ja.md:26`–`27`）。
> **本書 A04 も同じ格である。** 以下の各行は「決定が存在すること」と「その所在」の記録であって、
> **値・条件・分岐の中身は原典を開かないと分からない**（`DECIDED-ja.md:29`–`38` が値を書かない理由を述べている）。

---

## 1. 決定の全数 —— **自分で数えた**

`DECIDED-ja.md` の各章の表の行数を、ヘッダ行と区切り行を除いて数えた。

| 章 | 領域 | 正の文書 | 行範囲 | **自数え** | 文書の自称（`DECIDED-ja.md:333`–`340`） | 一致 |
|:--:|---|---|---|--:|--:|:--:|
| A | 入力と全体方針 | `user-order.md` | `:75`–`:79` | **5** | 5 | ○ |
| B | データ構造 | `02-data-model/grs-native-erd-ja.md` ★ | `:87`–`:117` | **31** | 31 | ○ |
| C | 設定値 | `02-data-model/grs-document-settings-ja.md` ★ | `:128`–`:143` | **16** | 16 | ○ |
| D | 命名 | `03-ui-naming/ui-parts-ja.md` ★ | `:153`–`:163` | **11** | 11 | ○ |
| E | 画面と操作 | `03-ui-naming/ui-detail-spec-ja.md` | `:175`–`:202` | **28** | 28 | ○ |
| F | 予実と進捗 | `07-plan-actual/plan-actual-decisions-ja.md` ★ | `:212`–`:255` | **44** | 44 | ○ |
| G-1 | 機械向け要求 | `10-agent-interface/agent-interface-requirements-ja.md` | `:268`–`:285` | **18** | — | — |
| G-2 | 機械向け契約と決定 | `10-agent-interface/agent-interface-spec-ja.md` ほか | `:293`–`:314` | **22** | — | — |
| G | （G-1＋G-2） | | | **40** | 40 | ○ |
| | **合計** | | | **175** | 175 | ○ |

★ = `authority` を持つ 4 文書（`DECIDED-ja.md:63`）。

> ⚠️ **175 は「決定の件数」ではない。** `DECIDED-ja.md:352`–`354` が自ら
> 「**本書は決定の『見出し』を数えたものであって、決定の件数ではない。1 行の裏に表が何十行もあることがある**」と書く。
> 例: C 章の 1 行（`DECIDED-ja.md:130`）が **57 項目**を指している。**正確な数は所在の節で数えること。**

### 1-1. 領域別の仕分け（175 行を 1 行ずつ振り分けた。**自数え**）

| 領域 | 件数 | 内訳（章ごと） |
|---|--:|---|
| **データモデルに効く** | **63** | B 31 ＋ C 7 ＋ D 8 ＋ F 14 ＋ G 3 |
| **UI に効く** | **63** | A 1 ＋ C 7 ＋ D 3 ＋ E 28 ＋ F 24 |
| **アーキテクチャに効く** | **43** | A 1 ＋ C 2 ＋ F 3 ＋ G 37 |
| **性能に効く** | **3** | A 1（`:75` 基準機・基準ブラウザ）＋ F 2（`:239` ラベル幅は概算・`:242` 干渉を計算しない） |
| **その他（全体方針・文書管理）** | **3** | A 2（`:77` 決着した仕様一覧・`:78` やらないこと）＋ F 1（`:255` 既存文書の上書き箇所） |
| | **175** | |

**仕分けの根拠（境界が微妙なもの）** ——
C 章は「設定値」だが、`:128` `:129` `:133` `:137` `:138` `:139` `:141`（保存判定・全項目書出し・画面状態の保存・保存しない 9 項目・`language` は `localStorage`・保存も `localStorage` もしない・本日線は保存しない）は
**保存する文書の形の決定**なのでデータモデルに数えた。`:142`（自動保存は暇なとき 1 回）と `:143`（JSON 往復検査を CI）はアーキテクチャに数えた。
D 章（命名）は名前がそのまま列名になるため、`:156` `:157` `:159` の 3 行（`Bar` の定義・日英対応表・UI パーツ木）だけを UI に、残り 8 行をデータモデルに数えた。
G 章は 37 行がアーキテクチャ、`:272`（A-2 文書は整数と誰が・いつを持つ）・`:297`（`revisionStamp` / `changeLog`）・`:298`（`documentId`）の 3 行をデータモデルに数えた。

---

## 2. ⚠️ 番号つきの決定 —— **決定-1 〜 決定-6**（番号と本文をそのまま写す）

**`DECIDED-ja.md` の G-2 表（`:293`–`:314`）の中に、`決定-N` を「正」として名指す行が 7 本ある。**
下表の「本文」は `DECIDED-ja.md` の当該行の左セルを**そのまま写した**もの。

| 番号 | **本文（そのまま）** | 出典 | 領域 |
|---|---|---|---|
| **決定-1** | **描画は純粋関数。成果物は当面 単一 `.html` だけ** | `DECIDED-ja.md:310` | アーキテクチャ |
| **決定-2** | **チャット欄は MVP に入れる。保存するのは変更の理由だけ**（上限は設けない） | `DECIDED-ja.md:311` | アーキテクチャ ＋ データモデル |
| **決定-3** | **`revisionStamp` は保存する。`agentApiVersion` は入れない** | `DECIDED-ja.md:312` | **データモデル** |
| **決定-4** | **API は信頼境界ではない。既定で公開せず、人間が有効にしたときだけ公開する。無効化しても渡し済みの参照は取り消せない** | `DECIDED-ja.md:309`（`spec §6` と併記） | アーキテクチャ |
| **決定-5**（a） | **起動時の投入は優先順位つき**（埋込文書 → URL パラメータ → 自動保存 → 初期テンプレート）。**負けた自動保存は捨てずに比べる**（4 分岐） | `DECIDED-ja.md:307`（`spec §5-3` と併記） | アーキテクチャ |
| **決定-5**（b） | **自動保存のキーは文書ごとに分ける** | `DECIDED-ja.md:313` | アーキテクチャ ＋ データモデル（`documentId` を前提にする） |
| **決定-6** | **同時更新まわりを 4 語に分けた**（`ConcurrentUpdate` / `BaseRevisionCheck` / `AutomaticReconciliation` / `ImportMerge`）。使わない語も決めた | `DECIDED-ja.md:295`（`spec §0-1` と併記） | アーキテクチャ ＋ 命名 |

**読み方の注意 4 点** ——

1. **本文は `DECIDED-ja.md` 側の 1 行要約である。** 決定の全文は `10-agent-interface/agent-interface-spec-ja.md` または
   `agent-interface-open-items-ja.md` にある。**どちらの文書に載っているかは `DECIDED-ja.md` からは判別できない**
   —— G-2 の見出し（`DECIDED-ja.md:289`）が 2 文書を並記しており、行ごとの割り当ては書かれていない。**未検証**。
2. **決定-5 は 2 行に分かれて現れる**（`:307` と `:313`）。番号は 6 つだが、**`DECIDED-ja.md` 上の行は 7 本**である（自数え）。
3. **決定-3 が本作業の起点である。** 「`FR-063`（最後に書いた者と時刻）に受け皿が無い」という引継ぎ事実は**誤り**で、
   受け皿は `revisionStamp`（`revision` / `lastEditedBy` / `updatedAt` の 3 キー）として確定している。
   在庫表側も既に是正済み（`A05-user-order.md:415` の X-11、`A02-agent-api-requirements.md:262` の X-1、`A06-readme-nextsteps.md:309` の C-7）。
4. **`A02` が「未検証」と保留していた 2 件が、本作業で裏付いた** —— §8 参照。

---

## 3. データモデルに効く決定（63 件のうち、次期の設計に直接効くもの）

### 3-1. 文書の形（トップレベル・入れ子）

| # | 決定 | 出典（DECIDED） | 正の所在 |
|:--:|---|---|---|
| DM-01 | **代理キーを持たない。MSPDI の UID を主キーにする** | `DECIDED-ja.md:90` | `grs-native-erd-ja.md` §5.3 |
| DM-02 | **本質は 4 エンティティ、全体は 14 エンティティ** | `DECIDED-ja.md:89` | 同 §5.0 / §5.1 / §5.2 |
| DM-03 | **軸が 2 つある** —— WBS ＝ `Task.wbs_parent`（export する）／マルチバー ＝ `TaskGroup` ＋ `TaskGroupMember`（export **しない**） | `DECIDED-ja.md:119` | 同 §5 |
| DM-04 | **`WBS` と `TaskGroup` は別物である**（混同しない） | `DECIDED-ja.md:236` | `plan-actual-decisions-ja.md` §5-3 |
| DM-05 | 文書のトップレベルに **`revisionStamp` と `changeLog`** を足す | `DECIDED-ja.md:297` | `agent-interface-spec-ja.md` §2-1 / §2-2 |
| DM-06 | **`revisionStamp` は保存する。`agentApiVersion` は入れない**（決定-3） | `DECIDED-ja.md:312` | 同上 |
| DM-07 | `documentId` の**必要性と最小の形**は書いたが、**決めるのはデータ構造の正**（＝まだ入っていない） | `DECIDED-ja.md:298` / `:120` | `spec §2-3` ／ 未決は `NEXT-STEPS-ja.md` 4-2 |
| DM-08 | **画面の状態（ズーム・スクロール・パネル幅）は文書に保存する。** 下限・上限も持つ | `DECIDED-ja.md:133` | `grs-document-settings-ja.md` §4-2 |
| DM-09 | **常に全項目を書き出す。省略しない** | `DECIDED-ja.md:129` | 同 §2 |
| DM-10 | **保存するかどうかは検査 1 つで決まる**（判定規則がある） | `DECIDED-ja.md:128` | 同 §1 |
| DM-11 | **JSON は見た目まで再現する**ことを役割とした | `DECIDED-ja.md:104` | `grs-native-erd-ja.md` §5.7 |

### 3-2. 保存しないもの（不変条件）

| # | 決定 | 出典 |
|:--:|---|---|
| DM-12 | **自動算出できるものは保存しない**を原則とし、無駄を監査した | `DECIDED-ja.md:101` |
| DM-13 | **保存しないものは全 9 項目**であり、それぞれ理由が書いてある | `DECIDED-ja.md:137` |
| DM-14 | **`OutlineLevel` は導出する。保存しない** | `DECIDED-ja.md:235` |
| DM-15 | **本日線は保存しない** | `DECIDED-ja.md:141` |
| DM-16 | **依存線の経路は保存せず毎回再計算する** | `DECIDED-ja.md:186` |
| DM-17 | **イナズマ線は依存線と同じ固定色**とし、文書に保存しない | `DECIDED-ja.md:223` |
| DM-18 | **言語（`language`）は文書に保存せず `localStorage` に置く。** 読めないときはブラウザの言語設定に従う | `DECIDED-ja.md:138` |
| DM-19 | 保存も `localStorage` もしないものを**別枠**で定めた | `DECIDED-ja.md:139` |
| DM-20 | **会話は保存せず、変更の理由（`changeLog`）だけ保存する**（決定-2） | `DECIDED-ja.md:280` / `:311` |
| DM-21 | **`revisionStamp` は MSPDI へ書き出さない**（在庫表 `A02:187` / `A06:110` が裏を取っている） | — （`DECIDED-ja.md` には無い。**在庫表側にのみある**） |

### 3-3. 列と既定値

| # | 決定 | 出典 |
|:--:|---|---|
| DM-22 | **予定日付のフィールド名は `start` / `finish`** で確定 | `DECIDED-ja.md:163` |
| DM-23 | 期間の型は **GRS が稼働日数の整数、MSPDI が `xsd:duration`**。変換規則を持つ | `DECIDED-ja.md:108` |
| DM-24 | **MSPDI の `Stop` / `Resume` / `ResumeValid` をそのまま使う。** 保存する項目を確定した | `DECIDED-ja.md:212` |
| DM-25 | **中断は 1 回しか記録できない**（XSD で確認した**事実**。回避策ではなく制約） | `DECIDED-ja.md:213` |
| DM-26 | **5 状態を、拡張領域を 1 枠も使わずに判別する** | `DECIDED-ja.md:214` |
| DM-27 | マイルストーンは**実績の端点も進捗の掴み点も持たない。完了率は 0 か 100 だけ** | `DECIDED-ja.md:216` |
| DM-28 | **色は疎な上書き。`null` は既定に解け、値があれば人の指定が勝つ。** テーマから解くのは `TaskVisual` の 2 色 ＋ `TaskGroup.color`。**注記の枠線も疎だが解決先が違う。`lineWeight` は対象外** | `DECIDED-ja.md:102` |
| DM-29 | **色の値域は 3 種**（選んでいない / 透明 / 色）。**注記の枠線だけ透明を選べない。塗りと輪郭を同時に透明にはできない。`null` の解決先は列ごとに違う** | `DECIDED-ja.md:103` |
| DM-30 | **`themeMonochrome` は描画時のフィルタである**（保存値を変えない。人の指定色も無彩色で描かれる） | `DECIDED-ja.md:134` |
| DM-31 | **描画の設定は全 57 項目**で、全部に既定値が入っている。**由来が記録されていないものには印がある**（次期が選び直してよい） | `DECIDED-ja.md:130` / `:131` |
| DM-32 | **担当者の割当を GRS で編集し MSPDI へ書き戻す。`Task` に担当者の列は足さない。そのために増える列は無い** | `DECIDED-ja.md:94` |
| DM-33 | 資源と割当は**軽量ネイティブ化**し、**Carry に参照を残さない**ことを不変条件にした | `DECIDED-ja.md:92` |
| DM-34 | 例外日（祝日）の表現を **`Exception` に一本化**した | `DECIDED-ja.md:95` |
| DM-35 | **削除時の連鎖（cascade）の範囲**を確定した | `DECIDED-ja.md:96` |
| DM-36 | **既定行（器）の生成と寿命、二軸の追随**を確定した | `DECIDED-ja.md:100` |
| DM-37 | import 異常系の**正規化規則**と、**WBS の深さ**を定めた | `DECIDED-ja.md:98` |
| DM-38 | **`progressStatus` を廃止した** | `DECIDED-ja.md:248` |
| DM-39 | **積み段（`stackOrder`）に機能上の上限を設けない。** 安全弁に到達したら**黙って捨てず人に知らせる** | `DECIDED-ja.md:240` |
| DM-40 | 1 行の中の縦積み（`stackOrder` の自動割当）は**決定的な順序**で決まる。**マイルストーンを特別扱いしない** | `DECIDED-ja.md:193` |

### 3-4. MSPDI との境界

| # | 決定 | 出典 |
|:--:|---|---|
| DM-41 | MSPDI から受け継ぐ範囲を **Own / Consume / Reconstruct / Carry / Drop** に仕分けた | `DECIDED-ja.md:87` |
| DM-42 | **Carry ストアの設計**を確定した（エンティティ別バッグ ＋ 入口/出口の検査） | `DECIDED-ja.md:97` |
| DM-43 | **拡張領域（`ExtendedAttribute`）の使い方**を確定した。**枠は先頭から使う** | `DECIDED-ja.md:99` / `:251` |
| DM-44 | export のときに算出する要素（Reconstruct）を**全数列挙**した | `DECIDED-ja.md:114` |
| DM-45 | ERD から除外したもの（Carry / Drop）を**全数列挙**した | `DECIDED-ja.md:116` |
| DM-46 | 複数 MSPDI を取り込むときの**マージ規約**を定めた（`ImportMerge`） | `DECIDED-ja.md:91` |
| DM-47 | **往復無損失の担保の仕方**を定めた | `DECIDED-ja.md:253` |
| DM-48 | **XSD と解説書で確認した事実を、推測と分けて記録した** | `DECIDED-ja.md:254` |
| DM-49 | ベースライン（変更前予定のグレー表示）の扱いを定めた | `DECIDED-ja.md:117` |
| DM-50 | **JSON 往復の検査を CI に入れる** | `DECIDED-ja.md:143` |

---

## 4. アーキテクチャに効く決定（43 件のうち、層・依存の向き・Agent API の形）

### 4-1. 層と成果物の形

| # | 決定 | 出典 |
|:--:|---|---|
| AR-01 | **描画は DOM 非依存の純粋関数に保つ。成果物は当面 単一 `.html` だけ**（要求 A-15 ＋ 決定-1） | `DECIDED-ja.md:283` / `:310` |
| AR-02 | **将来のサーバ／機械連携は、この関数群と 1 対 1 の薄い包みにする**（A-16） | `DECIDED-ja.md:284` |
| AR-03 | **設定ファイルは作らない** | `DECIDED-ja.md:252` |
| AR-04 | **Undo / Redo は不変更新のスナップショット。明示的な差分機構は作らない** | `DECIDED-ja.md:244` |
| AR-05 | **レイアウトの計算順序**を定めた（循環を切る） | `DECIDED-ja.md:185` / `:241` |
| AR-06 | 自動保存は**暇なときにまとめて 1 回**行う | `DECIDED-ja.md:142` |
| AR-07 | ⚠️ **層仕訳は `status: draft` であって決定ではない。正式版は次期が決める** | `DECIDED-ja.md:321`–`325` |

> ⚠️ **AR-07 が本節で最も重い。** `DECIDED-ja.md` は H 章に **「アーキテクチャ — 決定ではない」** という見出しを立て、
> `09-architecture/architecture-layering-draft-ja.md` は**推奨案にすぎない**と明言する（`DECIDED-ja.md:323`）。
> **層の分割と依存の向きは、前プロジェクトが決めなかった領域である。** 残作業は `NEXT-STEPS-ja.md` ステップ 3（5 件）。

### 4-2. Agent API の形（要求 17 件 ＝ G-1）

**18 件のうち 17 件を採用した**（`DECIDED-ja.md:264` が「17 件を採用」と書き、表は 18 行 ——
先頭 1 行が総論（`:268`）、残り 17 行が `A-` 番号つき。**自数え**。落とした 1 件は A-17）。

| 要求 | 決定（そのまま） | 出典 |
|---|---|---|
| （総論） | **人間が UI で行える編集・確認・出力を、AI が API で行える** | `DECIDED-ja.md:268` |
| A-1 | 人間向け UI と**同格**の機械向けインターフェースを持ち、**入口は 1 つ**。**既定では公開しない** | `DECIDED-ja.md:269` |
| A-2 | 文書は**あらゆる更新で 1 ずつ増える整数**を持ち、誰が・いつ書いたかも持つ | `DECIDED-ja.md:272` |
| A-3 | 書き込みは**基準版を照合できる**（`BaseRevisionCheck`）。食い違えば拒否し、現在の文書を返す | `DECIDED-ja.md:274` |
| A-4 | API 経由の変更も **UI と同じ Undo / Redo の履歴**を通る。**1 回の呼び出し = Undo 1 段** | `DECIDED-ja.md:275` |
| A-5 | 一括の書き込みは**原子的**である（1 つでも拒否されたら全部戻す） | `DECIDED-ja.md:276` |
| A-6 | 読み出しは**不変のコピー**を返す | `DECIDED-ja.md:273` |
| A-7 | **API は UI の防御を迂回しない**（取込検証・`innerHTML` 禁止・XXE 無効化・透かし照合・将来の編集権限） | `DECIDED-ja.md:277` |
| A-8 | インターフェース版数を持ち、破壊的変更で上げる | `DECIDED-ja.md:270` |
| A-9 | **例外を投げず、結果を値で返す** | `DECIDED-ja.md:271` |
| A-10 | 監視は **revision スコープ**である（最後に読んだ版より後の変更だけ通知する） | `DECIDED-ja.md:278` |
| A-11 | 起こす条件は「**自分以外の書き手が何かを確定した**」である | `DECIDED-ja.md:279` |
| A-12 | 画面上で AI と会話できる。**会話は保存せず、変更の理由（`changeLog`）だけ保存する** | `DECIDED-ja.md:280` |
| A-13 | 起動時に文書を埋め込める（`file://` で唯一成立する投入経路）。**入れ口が運ぶのは文書だけで、API の有効化は運ばない** | `DECIDED-ja.md:281` |
| A-14 | 出力は**ダウンロードのダイアログを出さずに値で返す** | `DECIDED-ja.md:282` |
| A-15 | **描画は DOM 非依存の純粋関数に保つ。成果物は当面 単一 `.html` だけ** | `DECIDED-ja.md:283` |
| A-16 | 将来のサーバ／機械連携は、**この関数群と 1 対 1 の薄い包み**にする | `DECIDED-ja.md:284` |
| A-18 | **呼び出しの粒度を粗くする**（1 回で読んで・書いて・次に待つべき点を返す） | `DECIDED-ja.md:285` |
| ~~A-17~~ | **不採用**（`DECIDED-ja.md:287` が `DISCARDED-ja.md` §6 を指す） | `DECIDED-ja.md:287` |

### 4-3. Agent API の契約（G-2）

| # | 決定 | 出典 |
|:--:|---|---|
| AG-01 | 公開点の名前・契約の型名・有効化フラグ・埋め込みの入れ口の名前を確定した | `DECIDED-ja.md:293` |
| AG-02 | **ユースケースは 1 つ、経路は 3 つ** | `DECIDED-ja.md:294` |
| AG-03 | **同時更新まわりを 4 語に分けた**（決定-6）。**使わない語も決めた** | `DECIDED-ja.md:295` |
| AG-04 | 関数の**全数**と、各関数の**純粋性・品詞**を定めた | `DECIDED-ja.md:296` |
| AG-05 | **`update-*` は部分更新である**（キーが無い＝触らない / `null`＝既定へ戻す / 値＝代入）。**新しいコマンドも新しい拒否理由も足さない** | `DECIDED-ja.md:300` |
| AG-06 | 監視の契約と**1 往復の全体像**を定めた | `DECIDED-ja.md:301` |
| AG-07 | 単一 `.html` への文書の埋め込みの規約（**エスケープは必須**） | `DECIDED-ja.md:302` |
| AG-08 | **API の有効化はファイルではなく起動側が持つ** | `DECIDED-ja.md:303` |
| AG-09 | 埋込文書つき GRS の名前の規則（**ASCII のみ**） | `DECIDED-ja.md:304` |
| AG-10 | URL パラメータは **`http(s)` のときだけ**使える | `DECIDED-ja.md:305` |
| AG-11 | **保存の往復は「同じファイルへ上書き保存」で閉じる**（実測） | `DECIDED-ja.md:306` |
| AG-12 | **起動時の投入は優先順位つき**（埋込文書 → URL パラメータ → 自動保存 → 初期テンプレート）。**負けた自動保存は捨てずに比べる**（4 分岐）（決定-5） | `DECIDED-ja.md:307` |
| AG-13 | 起動時に人間へ出す面を **1 枚に集約**する | `DECIDED-ja.md:308` |
| AG-14 | **API は信頼境界ではない**（決定-4）。**無効化しても渡し済みの参照は取り消せない** | `DECIDED-ja.md:309` |
| AG-15 | **自動保存のキーは文書ごとに分ける**（決定-5） | `DECIDED-ja.md:313` |
| AG-16 | `file://` について**実測で 8 件決着した**（保存の仕組み・隣のファイル・ダウンロード・外部操作・エスケープ・既定非公開・人間の画面への接続・保管庫の共有） | `DECIDED-ja.md:314` |

---

## 5. 廃棄・撤回された決定 —— **生きている決定と混ぜないこと**

### 5-1. 全数（**自分で数えた**）

`DISCARDED-ja.md` の表の行数を、ヘッダ行と区切り行を除いて数えた。

| 節 | 内容 | 行範囲 | **自数え（行）** |
|---|---|---|--:|
| §1 | 全体像（引継ぐ / 参考 / 破棄） | `:32`–`:34` | 3 |
| §2 | 引継ぐ束の内訳 | `:54`–`:64` | 11 |
| §3 | **参考**（捨てないが引継がない） | `:78`–`:90` | 13 |
| §4-1 | 破棄 —— 実装とテスト | `:100`–`:103` | **4** |
| §4-2 | 破棄 —— **旧データ契約** | `:109`–`:112` | **4** |
| §4-3 | 破棄 —— 生成物 | `:118`–`:122` | **5** |
| §4-4 | 破棄 —— フレームワーク側資産 | `:131`–`:137` | **7** |
| §4-4b | 破棄 —— PoC の案比較 5 本 | `:143` | **1** |
| §4-5 | **破棄しなかったが注意が要る** | `:149`–`:151` | 3 |
| §5 | 選別で使った判断基準 | `:161`–`:169` | 9 |
| §6 | 機械向けインターフェースで落としたもの | `:179`–`:181` | **3** |
| §7 | 経緯の文書 6 本の破棄 | `:200`–`:205` | **6** |
| | **理由つきで「破棄」と宣言した行の合計**（§4-1〜§4-4b ＋ §6 ＋ §7） | | **30** |

**ファイル数の自称** —— 引継ぐ **35 ファイル**／参考 **約 42 ファイル**／破棄 **約 320 ファイル**（`DISCARDED-ja.md:32`–`:34`）。
⚠️ **35 と 約 42 のいずれも、自分で数えると合わない。** 「在庫表との食い違い」節の X-1 / X-2 を見ること。

### 5-2. ⚠️ **設計に効く「捨てた決定」**（次期が再提案しやすいもの）

| # | 捨てたもの | **捨てた理由（そのまま）** | 出典 |
|:--:|---|---|---|
| DS-01 | **要求 A-17「非 ASCII のデータをシェル引数に載せない」** | **シェル引数を持つ部品が存在しない。** 決定-1 が「成果物は当面 単一 `.html` だけ。ライブラリ／CLI は出さない」と定めたため、**存在しない部品への要求は先回りである**（YAGNI）。⚠️ **実測は捨てていない** —— Windows のコンソールのコードページがバイト列を壊した記録は `ai-cowork-trial-findings-ja.md` §3-2 にある | `DISCARDED-ja.md:179` |
| DS-02 | **旧 flat 形状のデータ契約**（`rows` / `sections` / `items` / `dependency.id` / `lagDays` / `startDate` / `endDate`） | **ユーザー明示により判断材料から除外。** GRS ネイティブ **14 エンティティ**（ERD 本体 12 ＋ 注記 2）へ全面置換 | `DISCARDED-ja.md:109` |
| DS-03 | **旧 JSON Schema `gr-scheduler.schema.json`** | **予定日付が `startDate` / `endDate`** で、確定した `start` / `finish` と食い違う。**次期が SSOT と誤認する危険が最も高いファイル** | `DISCARDED-ja.md:110` |
| DS-04 | **予実別行モデル（旧 IM7 方式）** —— 行 id に `Milestones-Plan` / `Milestones-Actual` があり、予定と実績を**別行**に分ける | CR-001 で「同一アイテムが実績日を持つ」方式へ移行し CR-012 でサンプルも追従した**廃止済みモデル**。さらに行 id に**制御文字 `U+0001`** を含む。**持ち込むと旧モデルが復活する** | `DISCARDED-ja.md:111` |
| DS-05 | **旧 GUI 木 `gur-components.md`** | **抜け 4 件**（依存線・グリッド線・透かし・モーダル）。完全版は `ui-parts-ja.md` §3 | `DISCARDED-ja.md:112` |
| DS-06 | **PoC の案比較 5 本**（`poc-layout-A/B/C` / `poc-zoom-A/B`） | **比較は決着済み**（**9 点アンカーの候補探索** ＋ **深さしきい値のズーム** を推奨）で、計測値は `POC-RESULTS-ja.md` に全数記録。現物は**廃止語を 158 箇所（99 行）抱えており**（`lane` / `actualEnd` / `overdue` / `LANE_H` / `LANE_OFF`）、**コピペ禁止に対する最大の誘因**だった。⚠️ **本行の「9 点アンカー」は X-4 で食い違う** | `DISCARDED-ja.md:143` |
| DS-07 | **専用のチャット窓口** ／ **汎用鍵の型名** ／ **`actor` の 2 値** ／ **命令としての `undo`** ／ **手番を回す道具** ／ **`sinceRevision` 省略可** | 6 件とも `applyCommands` / `changeExplanation` / `editedBy` / 関数 `undo(steps)` / `watchChanges({ sinceRevision })` へ置換（**無意味な汎用語の禁止**・**`sinceRevision` を必須にした**）。⚠️ **この 6 件は `DISCARDED-ja.md` ではなく在庫表が持つ** | `A02-agent-api-requirements.md:219`–`:224`（K-2〜K-7） |
| DS-08 | **会話を丸ごと保存する案** | 残す価値があったのは「なぜその手か」と「前言の訂正」だけで、**丸ごと保存すると価値の薄いものが日程表に同梱され、プライバシーの懸念だけが残る** | `A02-agent-api-requirements.md:227`（K-10） |
| DS-09 | **`progressStatus` / `progressRatio` / `importance` / `iconShapeKind` / 保存する `stop`** | 廃止・改名（`progressRatio` → `percentComplete` 整数、`iconShapeKind` → `shapeKind`）。⚠️ **`grs-data-model-ja.md` §7.3 がこの廃止済みの名前で `TaskVisual` を定義していた**ことが、同書を破棄した理由の 1 つ | `DISCARDED-ja.md:205` ／ `DECIDED-ja.md:248` ／ `E07-visual-origin.md:48`, `:51` |
| DS-10 | **上下分離表示** | **廃止した**（`plan-actual-decisions-ja.md` §2-3） | `DECIDED-ja.md:219` |
| DS-11 | **縦の通り道（行を跨ぐ x の列）を予約する案** | **不採用（2026-08-02・ユーザー判断）。** タスクの x は日付で決まるので**予約する x が存在しない場面がある**。横切りは重ね順で解く | `A08-poc-results.md:415`（X-9）／`DECIDED-ja.md:186` |
| DS-12 | **旧実装 96 本・単体テスト 80 本・E2E 21 本** | **全て旧名**（`item` / `itemKind` / `laneIndex` / `section` / `ScheduleItem` / `actualEnd` / `startDate` / `endDate`）。**コピペすると前プロジェクトのバグ根因（画面とデータの語彙混在）がそのまま再生産される**。加えてテストは **domain / usecase の純関数偏重**という構造的欠陥（＝「緑なのに壊れる」の原因）。**テスト構成ごと引き継ぐべきではない** | `DISCARDED-ja.md:100`–`:102` |

> ⚠️ **却下案とその理由は「妥当性」なので残されている**（`DISCARDED-ja.md:194`–`:196`）。
> 同じ案を再提案させないための材料は `grs-native-erd-ja.md` **§8I の 18 件**と
> `plan-actual-decisions-ja.md` **§4-5「採らない方式」**にある。**落としたのは経緯であって判断の根拠ではない。**
> **ただし X-6 のとおり、その §8I の中に旧案が 1 件生き残っている。**

### 5-3. 選別の判断基準 9 件（次期が同じ選別をするときの規則）

| # | 基準（そのまま） | 出典 |
|:--:|---|---|
| 1 | **製品に依存しない事実は最優先で引継ぐ** | `DISCARDED-ja.md:161` |
| 2 | **最後に決着した判断を引継ぎ、途中経過は参考に落とす** | `DISCARDED-ja.md:162` |
| 3 | **旧語彙を含むものは引継がない**（コピペ誘因を物理的に排除する） | `DISCARDED-ja.md:163` |
| 4 | **「正」が 2 つになるものは片方を破棄する** | `DISCARDED-ja.md:164` |
| 5 | **経験知（何をしなかったか）は文書がなければ失われるので必ず作る** | `DISCARDED-ja.md:165` |
| 6 | **生成できるものは引継がない** | `DISCARDED-ja.md:166` |
| 7 | **フレームワークから供給されるものは製品資産に混ぜない** | `DISCARDED-ja.md:167` |
| 8 | **未解決・未計測は「解決済み」と偽らずに引継ぐ** | `DISCARDED-ja.md:168` |
| 9 | **特定業種・特定製品に依存する記述は引継がない。例外は `MS Project` / `MSPDI`** | `DISCARDED-ja.md:169` |

---

## 6. 未決のまま残っている件

### 6-1. 実機確認の残件 —— **4 件**（`OPEN-ITEMS-ja.md`。全件「結果」欄が空）

| # | 確かめること | 影響先 | 優先度 | **答えが出なくても困らない理由** | 出典 |
|:--:|---|---|:--:|---|---|
| 1 | **`Number` 枠が何番まで使えるか** | `property-mspdi-mapping-ja.md` §4-2 C-1 / C-2 | 中 | 拡張領域に載せるのは **`fadeInDays` / `fadeOutDays` の 2 つだけ**。どちらも整数なので `Number1` / `Number2` に載る | `OPEN-ITEMS-ja.md:33`–`:49` |
| 2 | **`PercentComplete` に 100 超を書いたらどうなるか** | `plan-actual-decisions-ja.md` §1-4 | 中 | XSD は `xsd:integer` で**制約ファセットを持たない**ので 150 は妥当な XML。丸められると分かったら「export では 100 で頭打ち」に変える。**超過は `ActualDuration` と日付から復元できる** | `OPEN-ITEMS-ja.md:53`–`:69` |
| 3 | **完了したタスクの `Stop` / `Resume` をどう書き出すか** | `plan-actual-decisions-ja.md` §10-1 | 低 | GRS は完了時に書かないと決めており、未編集タスクは受け取った値をそのまま返す（Carry）ので**相手がどう書いても往復は壊れない** | `OPEN-ITEMS-ja.md:73`–`:86` |
| 4 | **GRS が新規に書き出した `Resource` / `Assignment` を MS Project がどう扱うか**（① 担当者として表示されるか ② `Units` / `Work` を勝手に埋めるか ③ 埋めたら `Task` の `Duration` / `Work` が変わるか） | `grs-native-erd-ja.md` §5.5a-2 | 中 | ⚠️ **③ が起きると `Task` の Carry（工数・コスト）と食い違い、往復無損失が壊れる。** 埋めると分かったら「export 時に `Units` を明示して書く」を足す。GRS は割当率を持たないので**書くとしたら固定値**になる | `OPEN-ITEMS-ja.md:90`–`:106` |

**所要 20 分程度。答えが出るまで実装を止める必要はない**（`OPEN-ITEMS-ja.md:13`–`:15`）。
**4 件すべて埋まったら影響先の文書を直してこの文書を消す**（`OPEN-ITEMS-ja.md:125`）。

**同書が「解決済みとして落とした」記録 4 件**（同じことを再調査しないため。`OPEN-ITEMS-ja.md:116`–`:119`。**自数え 4**）——

| # | 内容 | 落とした理由 | 出典 |
|:--:|---|---|---|
| 1 | MS Project が期間の量と率のどちらを信じるか | 上流解説書が 2 通りの算出を明記しており、**実機でも一意の答えは出ない**。GRS 側で整合させる既存方針が正しいことが裏付けられた | `OPEN-ITEMS-ja.md:116` |
| 2 | `Stop` の意味 | 上流解説書で **"the end of the actual portion of a task"** と確定 | `OPEN-ITEMS-ja.md:117` |
| 3 | 進捗の塗りの起点が `Start` か `ActualStart` か | **`ActualStart` に確定（ユーザー判断）。** 着手が予定に遅れた / 先行した場合の実績が明確になるため | `OPEN-ITEMS-ja.md:118` |
| 4 | `Text` / `Date` / `Flag` 枠の本数 | **使わなくなった**（拡張領域は `Number` 枠 2 本だけ） | `OPEN-ITEMS-ja.md:119` |

### 6-2. `DECIDED-ja.md` が本文中で名指す未決

| # | 未決 | 出典 | 行き先 |
|:--:|---|---|---|
| U-1 | ⚠️ **`documentId` はまだ入っていない** | `DECIDED-ja.md:120` | `NEXT-STEPS-ja.md` 4-2 |
| U-2 | ⚠️ **`ImportMerge` はまだ命名の表を通っていない** | `DECIDED-ja.md:167` | `NEXT-STEPS-ja.md` 2-8 |
| U-3 | ⚠️ **絵はまだ 1 枚も無い**（E 章は**振る舞いの決定**であって図ではない） | `DECIDED-ja.md:204` | `NEXT-STEPS-ja.md` 2-1〜2-7 |
| U-4 | ⚠️ **アーキテクチャは決定ではない。層仕訳は `status: draft`。正式版は次期が決める** | `DECIDED-ja.md:321`–`325` | `NEXT-STEPS-ja.md` ステップ 3（**5 件**） |
| U-5 | ⚠️ **機械向けインターフェースの未決は 2 件で、いずれも延期**（**自動調停の要否**・**エージェントの寿命と追いつき**） | `DECIDED-ja.md:316`–`:317` | `agent-interface-open-items-ja.md` §B |
| U-6 | 決まっていないことの全数は **16 件** | `DECIDED-ja.md:13` / `:44` | `NEXT-STEPS-ja.md` |
| U-7 | `DECIDED-ja.md` 自身が「**どれにも無ければ、それは書き漏れである**」と宣言している | `DECIDED-ja.md:46` | 凍結リポジトリ |

### 6-3. `DISCARDED-ja.md` が「注意が要る」として残した 3 件

| # | 資産 | 注意点（そのまま） | 出典 |
|:--:|---|---|---|
| N-1 | `DEF-014`（ルーラー boundingBox が稀に null） | **原因未確定・Open のまま終了。緑になったから解決したとはみなしていない。** 次期で同種の症状が出たら**起動時の寸法確定順序**を疑う | `DISCARDED-ja.md:149` |
| N-2 | レビュー記録の M-6 / M-01 | 「実測してから判断」と決めて**実測されなかった**指摘。次期の計測項目 U-1 / U-2 として性能ノート §2-5 に引き継いだ | `DISCARDED-ja.md:150` |
| N-3 | `handoff-cr-013-016-followups.md` | **`CR-017`（予実の編集モデル）を起票すると書かれているが `CR-017` は存在しない。** 未決論点として `user-order.md`「次期の開始前に決めること」3 件に昇格させた | `DISCARDED-ja.md:151` |

---

## 7. 在庫表が既に是正済みの点（重複作業を避けるための記録）

| 何 | 在庫表側の記述 | 本作業での確認 |
|---|---|---|
| `FR-063` の受け皿（`revisionStamp`） | `A05-user-order.md:415`（X-11）「**`E09` が正。受け皿は既に在る**」／`A02-agent-api-requirements.md:262`（X-1）「**出典の側が正。** 原因は `E04` の『読んだ原典』表に `10-agent-interface/` のファイルが 1 つも無いこと」／`A06-readme-nextsteps.md:309`（C-7）「**『受け皿が無い』という引継ぎ事実は誤りである**」 | `DECIDED-ja.md:312` の**決定-3**（「**`revisionStamp` は保存する。`agentApiVersion` は入れない**」）が、索引の側からも同じ結論を裏付ける。**3 枚の在庫表と `DECIDED` が一致。撤回が要るのは `E04-project.md:117` / `:125` / `:169` である**（在庫表側で既に指摘済み） |
| 地図の穴の形 | `A06-readme-nextsteps.md:84`「読む順だけを追った読み手は `appheader-palette-icons.html`（アイコンの正）と `20-spec-template/` に一度も出会わない。**今回の `10-agent-interface/` の取りこぼしと同じ形**である」 | **`DISCARDED-ja.md:45`（「⚠️ `10-agent-interface/`（5 本）が丸ごと抜けていた」）と `:70` が、同じ失敗を 2026-08-04 に一度自認している。** それでも `samples/*.json` は数から漏れたまま（X-1） |
| A-17 不採用 | `A02-agent-api-requirements.md:218`（K-1） | `DECIDED-ja.md:287` ＋ `DISCARDED-ja.md:179` と一致。**3 者一致。食い違い無し** |

---

## 8. ⚠️ `DECIDED-ja.md` / `DISCARDED-ja.md` にあるのに在庫表のどこにも現れない決定

**検査方法** —— `temp/inventory/*.md`（本書 `A04` を除く **20 枚**。⚠️ 作業開始時 **17 枚** → 終了時 **21 枚**に増えたので、
**終了時点の一覧を `ls` で数え直した**）に `grep -l` を掛けた。
⚠️ **文字列一致なので「別の語で書かれている」可能性は排除できない。以下は「その語では出てこない」という事実のみ。**

| # | 決定 | 出典 | 在庫表 20 枚のヒット | 効き方 |
|:--:|---|---|:--:|---|
| G-1 | **設定ファイルは作らない** | `DECIDED-ja.md:252` | **0**（`設定ファイル` / `config` とも 0） | **アーキテクチャに効く。** 「設定はすべて文書の中に持ち、外部ファイルを作らない」という境界が、どの在庫表にも写っていない |
| G-2 | **JSON は見た目まで再現する**ことを役割とした | `DECIDED-ja.md:104` | **0** | **データモデルに効く。** 保存する JSON の**役割の定義**であり、`E09-settings-blob.md` の根の並びが「なぜ見た目まで持つのか」の根拠になる |
| G-3 | **決定-4**（API は信頼境界ではない／**無効化しても渡し済みの参照は取り消せない**） | `DECIDED-ja.md:309` | **番号としては 0**（内容は `A03-agent-open-items.md` / `A07-security-a11y.md` に「信頼境界ではない」の語で在る） | 番号で引けないだけで、**内容の欠落ではない** |
| G-4 | **決定-6**（`ConcurrentUpdate` / `BaseRevisionCheck` / `AutomaticReconciliation` / `ImportMerge` の 4 語。**使わない語も決めた**） | `DECIDED-ja.md:295` | **番号としては 0**（4 語は `A06-readme-nextsteps.md` に在る） | ⚠️ **「使わない語も決めた」の部分がどこにも無い。** 1 概念 1 語を守るには**禁止語の一覧**が要る。原典 `agent-interface-spec-ja.md` §0-1 を開くこと |
| G-5 | **import 異常系の正規化規則と WBS の深さ** | `DECIDED-ja.md:98` | 断片のみ（`正規化` は 7 枚に在るが、`grs-native-erd-ja.md` §5.5e の**規則そのもの**を写した表は見当たらない。**未検証**） | データモデルに効く |
| G-6 | **引継ぎ資産のファイル数（35 / 約 42 / 約 320）と選別の判断基準 9 件** | `DISCARDED-ja.md:32`–`:34` / `:161`–`:169` | **0**（`35 ファイル` のヒット 0） | X-1 / X-2 |
| G-7 | **`DEF-014` / `CR-017` 不在 / M-6・M-01 未実測**（§6-3 の N-1〜N-3） | `DISCARDED-ja.md:149`–`:151` | **0**（`DEF-014` のヒット 0） | ⚠️ **「未解決・未計測は解決済みと偽らずに引継ぐ」（基準 8）が在庫表で切れている** |

---

## 在庫表との食い違い

⚠️ **最重要節。両側に `ファイル名:行番号` を付けた。確かめられないものは「未検証」と明記した。**

| # | 何が食い違うか | 出典の側（`DECIDED` / `DISCARDED` / `OPEN-ITEMS`） | 在庫表・実物の側 | **どちらが正か** |
|:--:|---|---|---|---|
| **X-1** | ⚠️ **「引継ぐ 35 ファイル」が実物と 9 件ずれる** | `DISCARDED-ja.md:32`「**引継ぐ ｜ 35 ファイル** ｜ 文書 34 本（索引 3 本を含む）＋ PoC の `.html` 1。**機械カウント 2026-08-04**」／`:66`「内訳の合計: `.md` **34**（8＋5＋5＋2＋1＋2＋1＋3＋2＋5）＋ `.html` **1** ＝ **35 ファイル**」 | **`find` で数えた実物は 44 件**（`temp/` を除く。**自分で数えた**）。差の 9 件は —— `10-agent-interface/samples/` の JSON **2**（`agent-apply-request-and-outcomes.json` / **`grs-document-with-revision-stamp.json`**）／`03-ui-naming/appheader-palette-icons.html` **1**／`20-spec-template/` **5**（`.md` 4 ＋ `spec.sgra` 1）／`21-review-standard/review-standards.md` **1**。在庫表も別ルートで検出済み（`A06-readme-nextsteps.md:76`–`:78`, `:81`） | **実物が正。** ⚠️ **`samples/*.json` を「35」から落としたことが、決定-3 の機械可読な実例（`grs-document-with-revision-stamp.json`）が資産地図から消えた直接の原因である。** `DISCARDED-ja.md:45` は 2026-08-04 に「`10-agent-interface/`（5 本）が丸ごと抜けていた」と自認したが、**その是正のときに同じフォルダの `samples/` 2 本を数え忘れた。同じ穴を 2 度開けている** |
| **X-2** | ⚠️ **「参考 約 42 ファイル」が同じ文書の §3 と 1 桁違う** | `DISCARDED-ja.md:33`「**参考 ｜ 約 42 ファイル** ｜ 分析文書 5 / アーキ図 7 / `.sdoc` 19 / モック 1 / 既存ツール比較の調査記録 1 / 前プロジェクトの用語集 1 / プロセス記録の主要分」 | **同じ文書 §3 の件数列を足すと 483**（`DISCARDED-ja.md:78`–`:90` の 13 行。1＋1＋19＋**390**＋1＋1＋1＋1＋7＋1＋1＋約50＋9。**自分で足した**）。§1 の内訳は `learn-docs` **390 本**（`:81`）・`project-records` **約 50**（`:89`）・`project-management` **9**（`:90`）を数えていない | **§3 の表が正。** §1 の「約 42」は §3 の 13 行のうち 6 行ぶんしか数えていない。**「参考」の実体は 483 件規模である** —— 次期が「参考は 42 件しかない」と思って凍結リポジトリを探すと外す |
| **X-3** | **束の内訳の列が §7 の破棄に追随していない** | `DISCARDED-ja.md:62`「`08-poc/`（**3** ＋ `.html` 1）｜ 開き方・共通仕様・**計測結果**・**判断が要った 7 件** ＋ 動く PoC 1 本」＝ **4 つ列挙して 3 ファイル**／`:56`「`02-data-model/`（**5**）｜ 入口・ERD 確定版・設定値の確定版・取捨選択台帳・プロパティ×MSPDI 対応・**設計判断の変遷**」＝ **6 つ列挙して 5 ファイル** | 同じ文書の `:204` が `08-poc/OPEN-QUESTIONS-ja.md`（＝「判断が要った 7 件」）を、`:205` が `02-data-model/grs-data-model-ja.md`（＝「設計判断の変遷」）を破棄している。**実物の `ls`**: `08-poc/` ＝ `README.md` / `POC-SPEC-ja.md` / `POC-RESULTS-ja.md` ＋ `poc-integrated.html`、`02-data-model/` ＝ 5 本（`grs-data-model-ja.md` は無い） | **§7 と実物が正。** §2 は件数だけ直して**中身の列を直していない**。読む側が「判断が要った 7 件」「設計判断の変遷」を探すと**存在しないファイルを探すことになる** |
| **X-4** | ⚠️ **依存線のアンカーが「9 点」か「2 点」か** | `DISCARDED-ja.md:143`「**比較は決着済み**（**9 点アンカーの候補探索** ＋ 深さしきい値のズーム を推奨）」 | `DECIDED-ja.md:186`「アンカーは**右辺の中央と左辺の中央の 2 種類だけ**で、依存の種別が組合せを選ぶ」／`A06-readme-nextsteps.md:167`（A-23）「**依存線のアンカーは右辺の中央・左辺の中央の 2 種類のまま**」／`A08-poc-results.md:177`「作図の是正 … **出口が下端→右辺中央**」 | **`DECIDED-ja.md:186` ＋ `A06:167` が正**（後の決定）。`DISCARDED-ja.md:143` は 2026-08-02 の案比較時点の推奨をそのまま残している。⚠️ **「候補探索」が 2 アンカーの下位アルゴリズムとして生きているのか完全な廃案なのかは未検証**（`POC-RESULTS-ja.md` と `ui-detail-spec-ja.md` §4-9 を読んでいない）。⚠️ **さらに「9 点アンカー」は名称ラベル／注記のアンカー（`nameAnchor` 0–8・`anchorPoint` 0–8）と同じ語である**（`E07-visual-origin.md:34` / `E08-comment-highlight.md:44`）—— **1 概念 1 語の違反候補** |
| **X-5** | ⚠️ **`startDate` / `endDate` が「捨てた名前」なのに、現行の列名として在庫表に生きている** | `DISCARDED-ja.md:110`「旧 JSON Schema。**予定日付が `startDate` / `endDate`** で、確定した `start` / `finish` と食い違う。**次期が SSOT と誤認する危険が最も高いファイル**」／`DECIDED-ja.md:163`「**予定日付のフィールド名は `start` / `finish`**」 | `E08-comment-highlight.md:57` / `:81` / `:109` / `:156` が **`HighlightBox.startDate` / `endDate`** を現行の列として持つ（出典 `erd:1426`, `erd:1446`）／`E04-project.md:183` が `Project.start_date` → **`startDate`** への改名を求める | **矛盾ではないが衝突の火種。** 3 つの別概念（Task の予定日付＝`start` / `finish`／HighlightBox の範囲＝`startDate` / `endDate`／Project の全体開始＝`startDate`）が同じ語幹を共有する。`DECIDED-ja.md:165`–`:166`「**語彙の重複が前プロジェクトのバグ根因であった。1 概念 1 語を崩さないこと**」に照らすと**要判断。未決** |
| **X-6** | ⚠️ **捨てた案が、生きている文書の中に残っている**（`grs-native-erd-ja.md` §8I） | `DISCARDED-ja.md:205`「§8（設計判断の変遷 18 件＋教訓）→ 同 **§8I**。**内容は変えていない**」 | `E02-task-actual.md:286`「**ERD §8I #12 が `PercentComplete` の最終案を「Own（÷100 して保持）」と書く。** 確定は「**整数のまま**保持」であり `÷100` は旧版 … **設計変遷表を読んだ実装者が `progressRatio` を再導入しうる**」 | **在庫表（`E02:286`）が正。** ⚠️ **`DISCARDED-ja.md:205` の「内容は変えていない」という移設方針そのものが、廃止済みの `progressRatio`（÷100）を生きた文書の中に温存する仕組みになっている。**「却下案は妥当性なので残す」（`:194`–`:196`）方針の副作用である。**§8I を引くときは、その行が却下案か確定かを毎回確かめること** |
| **X-7** | **`DISCARDED-ja.md` を原典として読んだ在庫表が確認できない** | `DISCARDED-ja.md` 全 212 行 | `A02-agent-api-requirements.md:18`「⚠️ **読んでいない文書**（本書の主張はこれらに**基づかない**）: … `previous-project-result/DISCARDED-ja.md`（212 行）」／`:229`「**`DISCARDED-ja.md`（212 行）は本担当では読んでいない**」。在庫表 20 枚に `35 ファイル` / `DEF-014` / `9 点アンカーの候補探索` の**ヒット 0**（**自分で `grep` した**） | **`DISCARDED-ja.md` が正で、在庫表側の欠落。** ⚠️ **§4-5「破棄しなかったが注意が要る」3 件（`DEF-014` 原因未確定・M-6 / M-01 未実測・`CR-017` 不在）は、どの在庫表にも 1 行も無い。** 基準 8（`DISCARDED-ja.md:168`）が在庫表で切れている。⚠️ 他 19 枚が読んだかどうかは**未検証**（各書の「読んだ原典」表を全部は開いていない） |
| **X-8** | **在庫表が「未検証」と保留した 2 件が、`DECIDED` で裏付いた** | `DECIDED-ja.md:310`（**決定-1**「描画は純粋関数。成果物は当面 単一 `.html` だけ」）／`:312`（**決定-3**「`revisionStamp` は保存する。`agentApiVersion` は入れない」） | `A02-agent-api-requirements.md:225`（K-8）「当面 出さない（… 決定-1。**未検証** —— 同書は未読）」／`:226`（K-9）「入れない（同 決定-3。**未検証**）」 | **両立する。本書で「未検証」は解ける。** ⚠️ ただし**決定-1 / 決定-3 が `agent-interface-spec-ja.md` と `agent-interface-open-items-ja.md` のどちらに載るかは依然 未検証** —— `DECIDED-ja.md:289` が G-2 の正として 2 文書を並記し、行ごとの割り当てを書いていない。`A02:225` の「`agent-interface-open-items-ja.md` 決定-1」は A02 側の推定である |

---

### 付記 —— 本書の限界（**推測を断定にしないための宣言**）

1. **全文を読んだのは 3 文書（692 行）だけである。** `agent-interface-spec-ja.md` / `agent-interface-open-items-ja.md` /
   `grs-native-erd-ja.md` / `plan-actual-decisions-ja.md` / `ui-detail-spec-ja.md` / `POC-RESULTS-ja.md` は**開いていない**。
   §3〜§4 の各行は「**決定が存在すること**」と「**その所在**」の記録であって、**値・条件・分岐の中身は原典を開かないと分からない**
   （`DECIDED-ja.md:29`–`:38` が値を書かない理由を述べている）。
2. **§8 の「ヒット 0」は文字列一致の結果である。** 別の語で書かれている可能性は排除できない。
3. **在庫表は本作業中に 17 枚 → 21 枚に増えた。** §8 の検査は**終了時点の 20 枚**（`A04` を除く）に対して行った。
   その後に増えた分は**未検査**である。
4. **破棄された設計（`docs/spec/_assets/fig-*.md` / `tbl-datamodel.md` / 旧 Chapter 5・6）は `git` 履歴から掘り起こしていない。**
5. **`docs/` 配下は 1 バイトも編集していない。** 書いたのは本ファイルだけである。
