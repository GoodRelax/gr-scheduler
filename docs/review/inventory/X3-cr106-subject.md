# X3 — CR-106「システムは」→「`GRS` は」置換の全数同定

**根拠**: `change-request/CR-106-product-name-grs.md`（製品名 `GoodRelax Scheduler`、略称 `GRS`。綴りの正は用語辞書 表 T-101 の `U-45`）。

⚠️ **本ファイルは編集リストである。`docs/spec/` は 1 バイトも編集していない。**
⚠️ **行番号を一切書かない。** 作業中に別の作業者が `01-04-requirements.md` を編集し、**3752 行 → 3773 行**に増えた。
CR-106 が書いた `:472` は現在 `:476`、`:368` は `:372`、`RATIONALE` の引用 `:2547` は `:2571` を経て `:2592` に動いた。
**下の表の「置換前」はすべて、そのファイル内で 1 回だけ一致することを機械で確かめたリテラルである。**

### ⚠️ 表からリテラルを取り出すときの注意（**読み違えると置換が空振りする**）

1. **`\|` は Markdown の表のためのエスケープであり、リテラルの一部ではない。** 取り出したら **`\|` を `|` 1 文字に戻すこと。**
   仕様書の表の行を指しているリテラル（`` 系 \| 本ソフトウェアは `` など）でだけ起きる
2. **セルは行内コードで囲んである。** 囲みが `` `` `` （2 本）のものは、**中身の先頭と末尾の半角空白 1 つずつが囲みの一部**なので、取り出したら落とすこと。
   **リテラル自体が空白で始まったり終わったりすることは無い**（そうならないよう作った）
3. **リテラルは必ず 1 行に収まる。** §4 と §5 だけが行まるごとの置換で、それ以外は行の一部である

---

## 0. 自分で数えた全数

`grep -o` で出現数を数えた（行数ではない）。**`.bak-20260813-155301` の 3 枚は仕様書ではないので除外した。**

| ファイル | システムは | システムが | 本ソフトウェア | 「システム」総数 |
|---|---:|---:|---:|---:|
| `docs/spec/01-04-requirements.md` | **180** | **20** | **12** | **206** |
| `docs/spec/05-07-design.md` | 0 | 0 | **1** | 0 |
| `docs/spec/08-10-test.md` | 0 | 0 | 0 | 0 |
| `docs/spec/A-appendix.md` | 0 | 0 | 0 | **1** |
| `docs/spec/_assets/tbl-glossary.md` | 0 | 0 | **2** | 0 |
| `docs/spec/_assets/tbl-settings.md` | 0 | 0 | 0 | 0 |
| **計** | **180** | **20** | **15** | **207** |

**内訳が合うことを確かめた**: 206 = 180（は）＋ 20（が）＋ 6（複合語・見出し）。

**置換の対象にならない「システム」7 件**（複合語・見出し。**触ってはならない**） ——
`## Chapter 2. System Overview (システム概要)` / 図 F-001 の `ファイルシステム`（コードフェンス内） /
表 T-007 `D-2` の `利用者の PC のファイルシステム` / 表 T-010 `AC-3` の `外部システム` /
同 `AC-4` の `外部システム` / `実行時のシステム日付` / `A-appendix.md` の `Chapter 2（システム概要）`。
**いずれも直後に「は」「が」が続かないので、リテラル「システムは」「システムが」には一致しない。**

### ⚠️ CR-106 の実測と食い違う —— 「本ソフトウェア」は 10 件ではなく **15 件**

CR-106 §6-4 は「本ソフトウェア」を **10 件**（要求 8・辞書 2）と書いている。**現在は 15 件ある。**
**「システムは」180 件と「システムが」20 件は CR-106 の実測と一致した**ので、増えたのは「本ソフトウェア」だけである。
**差の 5 件は CR-106 執筆後に別の作業で入ったものである**（数えて確かめた） ——

| 増えた箇所 | 中身 |
|---|---|
| `01-04-requirements.md` 表 T-004 の `CN-7` | 交換相手のスキーマ引用の例外 |
| 同 表 T-006a の `W-10` | 真偽値の命名 |
| 同 `W-8` 下の注（**同一行に 2 件**） | 交換相手の綴りが現れてよい範囲 |
| `05-07-design.md` | MSPDI 引用の規約 |

10 + 5 = 15。**CR-106 の「置換後 0 件になる」は、この 5 件を足さないと成立しない。**
⚠️ **同じ理由で、作業直前にもう一度数え直すこと。** 本ファイルの数も、書いた時点のものである。

---

## 1. 「システムは」180 件 —— EARS の主語かどうかの内訳

**行の先頭トークンで分類し、1 件ずつ本文を読んで確かめた。**

| 文脈 | 件数 | EARS の主語か |
|---|---:|---|
| Chapter 4 の `**STATEMENT**:` 行 | **107** | **主語である**（EARS の応答節の主語） |
| Chapter 3 §3.2 の主フロー行（`N. ` で始まる） | **33** | **主語ではない**（ユースケース手順の散文） |
| Chapter 3 §3.2 の拡張行（`- ` で始まる） | **39** | **主語ではない**（同上） |
| `**RATIONALE**:` の中の鉤括弧引用 | **1** | **主語ではない**（`UC-009` 拡張 2a の引用） |
| **計** | **180** | |

107 + 33 + 39 + 1 = 180。**足して合うことを確かめた。**

**主語でないものを分けるために確かめた 4 点** ——

1. **表のセル（`|` で始まる行）の「システムは」は 0 件。** ただし**「システムが」には 1 件ある**（表 T-024a の `OP-3`。§3 に載せた）。CR-106 §6-5 は「表のセルと引用ブロックは 0 件だった」と書いているが、**それは「システムは」についてのみ成り立つ**
2. **引用ブロック（`>` で始まる行）の「システムは」「システムが」は 0 件。** 仕様書の外（前資産・要望）を引いた文の中には無い
3. **コードフェンスの中は 0 件。** 図 F-001 の mermaid は既に `` Browser["ブラウザ<br/>GRS が載る"] `` と `GRS` を使っている
4. **「システムは〜ではない」型の、語そのものを話題にした説明文は 0 件。** 180 件すべてが「このソフトウェアが何をするか」を述べている。**したがって「語の説明だから置換してはならない」箇所は無い**

⚠️ **主語でない 73 件（手順 72 ＋ 引用 1）も置換の対象とする。** 指している対象が EARS の `[System]` と同一だからである
（表 T-007 の下の注が両者を等号で結んでいる。§4 参照）。**EARS の主語かどうかは、置換の可否ではなく、直し方の慎重さを決める材料として分けた。**

---

## 2. 「システムは」180 件 —— 置換表

**置換後は一律「`` `GRS` は ``」。** ⚠️ **直前が CJK の文字である行では、`` `GRS` `` の前に半角空白を 1 つ入れた**（CJK に隣接した装飾で docutils が落ちた記録があるため）。

### 2-1. Chapter 4 の `**STATEMENT**:`（107 件・**EARS の主語**）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `たとき、システムは、構え` | `` たとき、`GRS` は、構え `` | `FR-001` の `STATEMENT`。EARS の応答節の主語 |
| 2 | 置換する | `たとき、システムは、**選んで` | `` たとき、`GRS` は、**選んで `` | `FR-083` の `STATEMENT`。EARS の応答節の主語 |
| 3 | 置換する | `くとき、システムは、表 T-0` | `` くとき、`GRS` は、表 T-0 `` | `FR-078` の `STATEMENT`。EARS の応答節の主語 |
| 4 | 置換する | `るとき、システムは、**マイル` | `` るとき、`GRS` は、**マイル `` | `FR-079` の `STATEMENT`。EARS の応答節の主語 |
| 5 | 置換する | `**: システムは、日程` | `` **: `GRS` は、日程 `` | `FR-094` の `STATEMENT`。EARS の応答節の主語 |
| 6 | 置換する | `っているとき、システムは、**表 T-01` | `` っているとき、`GRS` は、**表 T-01 `` | `FR-075` の `STATEMENT`。EARS の応答節の主語 |
| 7 | 置換する | `たとき、システムは、そのタスク` | `` たとき、`GRS` は、そのタスク `` | `FR-091` の `STATEMENT`。EARS の応答節の主語 |
| 8 | 置換する | `` **: システムは、**`na `` | `` **: `GRS` は、**`na `` | `FR-002` の `STATEMENT`。EARS の応答節の主語 |
| 9 | 置換する | `**: システムは、ラベ` | `` **: `GRS` は、ラベ `` | `FR-093` の `STATEMENT`。EARS の応答節の主語 |
| 10 | 置換する | `` sk` があるとき、システムは、**表 T-01 `` | `` sk` があるとき、`GRS` は、**表 T-01 `` | `FR-003` の `STATEMENT`。EARS の応答節の主語 |
| 11 | 置換する | `` たとき、システムは、`T `` | `` たとき、`GRS` は、`T `` | `FR-085` の `STATEMENT`。EARS の応答節の主語 |
| 12 | 置換する | `操作したとき、システムは、**表 T-01` | `` 操作したとき、`GRS` は、**表 T-01 `` | `FR-004` の `STATEMENT`。EARS の応答節の主語 |
| 13 | 置換する | `を移したとき、システムは、**表 T-01` | `` を移したとき、`GRS` は、**表 T-01 `` | `FR-005` の `STATEMENT`。EARS の応答節の主語 |
| 14 | 置換する | `だとき、システムは、**表 T-01` | `` だとき、`GRS` は、**表 T-01 `` | `FR-006` の `STATEMENT`。EARS の応答節の主語 |
| 15 | 置換する | `たとき、システムは、プロ` | `` たとき、`GRS` は、プロ `` | `FR-072` の `STATEMENT`。EARS の応答節の主語 |
| 16 | 置換する | `ぶとき、システムは` | `` ぶとき、`GRS` は `` | `FR-007` の `STATEMENT`。EARS の応答節の主語 |
| 17 | 置換する | `だとき、システムは、**色を指` | `` だとき、`GRS` は、**色を指 `` | `FR-041` の `STATEMENT`。EARS の応答節の主語 |
| 18 | 置換する | `だとき、システムは、その行の色` | `` だとき、`GRS` は、その行の色 `` | `FR-042` の `STATEMENT`。EARS の応答節の主語 |
| 19 | 置換する | `たとき、システムは、資源` | `` たとき、`GRS` は、資源 `` | `FR-008` の `STATEMENT`。EARS の応答節の主語 |
| 20 | 置換する | `たとき、システムは、直前` | `` たとき、`GRS` は、直前 `` | `FR-031` の `STATEMENT`。EARS の応答節の主語 |
| 21 | 置換する | `を行ったとき、システムは、**表 T-02` | `` を行ったとき、`GRS` は、**表 T-02 `` | `FR-081` の `STATEMENT`。EARS の応答節の主語 |
| 22 | 置換する | `たとき、システムは、選ばれたタスク・` | `` たとき、`GRS` は、選ばれたタスク・ `` | `FR-032` の `STATEMENT`。EARS の応答節の主語 |
| 23 | 置換する | `たとき、システムは、**選ばれ` | `` たとき、`GRS` は、**選ばれ `` | `FR-033` の `STATEMENT`。EARS の応答節の主語 |
| 24 | 置換する | `たとき、システムは、選ばれたタスクの` | `` たとき、`GRS` は、選ばれたタスクの `` | `FR-034` の `STATEMENT`。EARS の応答節の主語 |
| 25 | 置換する | `だとき、システムは、その場で編` | `` だとき、`GRS` は、その場で編 `` | `FR-035` の `STATEMENT`。EARS の応答節の主語 |
| 26 | 置換する | `だとき、システムは、**引き出` | `` だとき、`GRS` は、**引き出 `` | `FR-009` の `STATEMENT`。EARS の応答節の主語 |
| 27 | 置換する | `` **: システムは、`T `` | `` **: `GRS` は、`T `` | `FR-010` の `STATEMENT`。EARS の応答節の主語 |
| 28 | 置換する | `**: システムは、実績バーの左端を` | `` **: `GRS` は、実績バーの左端を `` | `FR-011` の `STATEMENT`。EARS の応答節の主語 |
| 29 | 置換する | `くなったとき、システムは、**表 T-02` | `` くなったとき、`GRS` は、**表 T-02 `` | `FR-084` の `STATEMENT`。EARS の応答節の主語 |
| 30 | 置換する | `あいだ、システムは、実績` | `` あいだ、`GRS` は、実績 `` | `FR-043` の `STATEMENT`。EARS の応答節の主語 |
| 31 | 置換する | `あいだ、システムは、作成` | `` あいだ、`GRS` は、作成 `` | `FR-044` の `STATEMENT`。EARS の応答節の主語 |
| 32 | 置換する | `**: システムは、完了` | `` **: `GRS` は、完了 `` | `FR-012` の `STATEMENT`。EARS の応答節の主語 |
| 33 | 置換する | `**: システムは、実績バーの右端の` | `` **: `GRS` は、実績バーの右端の `` | `FR-013` の `STATEMENT`。EARS の応答節の主語 |
| 34 | 置換する | `るとき、システムは、それ` | `` るとき、`GRS` は、それ `` | `FR-045` の `STATEMENT`。EARS の応答節の主語 |
| 35 | 置換する | `たとき、システムは、その日付を` | `` たとき、`GRS` は、その日付を `` | `FR-046` の `STATEMENT`。EARS の応答節の主語 |
| 36 | 置換する | `たとき、システムは、遅れ` | `` たとき、`GRS` は、遅れ `` | `FR-047` の `STATEMENT`。EARS の応答節の主語 |
| 37 | 置換する | `るとき、システムは、**表 T-02` | `` るとき、`GRS` は、**表 T-02 `` | `FR-014` の `STATEMENT`。EARS の応答節の主語 |
| 38 | 置換する | `だとき、システムは、それ` | `` だとき、`GRS` は、それ `` | `FR-015` の `STATEMENT`。EARS の応答節の主語 |
| 39 | 置換する | `**STATEMENT**: システムは、ポインタとキーボードの操作を` | `` **STATEMENT**: `GRS` は、ポインタとキーボードの操作を `` | `FR-016` の `STATEMENT`。EARS の応答節の主語 |
| 40 | 置換する | `**: システムは、画面の各部` | `` **: `GRS` は、画面の各部 `` | `FR-051` の `STATEMENT`。EARS の応答節の主語 |
| 41 | 置換する | `たとき、システムは、行見` | `` たとき、`GRS` は、行見 `` | `FR-052` の `STATEMENT`。EARS の応答節の主語 |
| 42 | 置換する | `**: システムは、コマ` | `` **: `GRS` は、コマ `` | `FR-053` の `STATEMENT`。EARS の応答節の主語 |
| 43 | 置換する | `**STATEMENT**: システムは、**表 T-036 の` | `` **STATEMENT**: `GRS` は、**表 T-036 の `` | `FR-070` の `STATEMENT`。EARS の応答節の主語 |
| 44 | 置換する | `き**、システムは、タイ` | `` き**、`GRS` は、タイ `` | `FR-017` の `STATEMENT`。EARS の応答節の主語 |
| 45 | 置換する | `たとき、システムは、**稼働す` | `` たとき、`GRS` は、**稼働す `` | `FR-088` の `STATEMENT`。EARS の応答節の主語 |
| 46 | 置換する | `**: システムは、文書` | `` **: `GRS` は、文書 `` | `FR-054` の `STATEMENT`。EARS の応答節の主語 |
| 47 | 置換する | `るとき、システムは、**タイム` | `` るとき、`GRS` は、**タイム `` | `FR-089` の `STATEMENT`。EARS の応答節の主語 |
| 48 | 置換する | `たとき、システムは、描く` | `` たとき、`GRS` は、描く `` | `FR-018` の `STATEMENT`。EARS の応答節の主語 |
| 49 | 置換する | `**: システムは、画面に描く` | `` **: `GRS` は、画面に描く `` | `FR-077` の `STATEMENT`。EARS の応答節の主語 |
| 50 | 置換する | `たとき、システムは、1 つの操` | `` たとき、`GRS` は、1 つの操 `` | `FR-055` の `STATEMENT`。EARS の応答節の主語 |
| 51 | 置換する | `たとき、システムは、全画` | `` たとき、`GRS` は、全画 `` | `FR-071` の `STATEMENT`。EARS の応答節の主語 |
| 52 | 置換する | `たとき、システムは、それを作り` | `` たとき、`GRS` は、それを作り `` | `FR-019` の `STATEMENT`。EARS の応答節の主語 |
| 53 | 置換する | `ルを有効にしたとき、システムは、**表 T-02` | `` ルを有効にしたとき、`GRS` は、**表 T-02 `` | `FR-048` の `STATEMENT`。EARS の応答節の主語 |
| 54 | 置換する | `` or` を有効にしたとき、システムは、**表 T-02 `` | `` or` を有効にしたとき、`GRS` は、**表 T-02 `` | `FR-082` の `STATEMENT`。EARS の応答節の主語 |
| 55 | 置換する | `` たとき、システムは、**`_a `` | `` たとき、`GRS` は、**`_a `` | `FR-049` の `STATEMENT`。EARS の応答節の主語 |
| 56 | 置換する | `たとき、システムは、**透かし` | `` たとき、`GRS` は、**透かし `` | `FR-086` の `STATEMENT`。EARS の応答節の主語 |
| 57 | 置換する | `**: システムは、開い` | `` **: `GRS` は、開い `` | `FR-020` の `STATEMENT`。EARS の応答節の主語 |
| 58 | 置換する | `き**、システムは、元の` | `` き**、`GRS` は、元の `` | `FR-021` の `STATEMENT`。EARS の応答節の主語 |
| 59 | 置換する | `いとき、システムは、**` | `` いとき、`GRS` は、** `` | `FR-022` の `STATEMENT`。EARS の応答節の主語 |
| 60 | 置換する | `` 3`）、システムは `` | `` 3`）、`GRS` は `` | `FR-056` の `STATEMENT`。EARS の応答節の主語 |
| 61 | 置換する | `すとき、システムは` | `` すとき、`GRS` は `` | `FR-057` の `STATEMENT`。EARS の応答節の主語 |
| 62 | 置換する | `だとき、システムは、タス` | `` だとき、`GRS` は、タス `` | `FR-058` の `STATEMENT`。EARS の応答節の主語 |
| 63 | 置換する | `くとき、システムは、**` | `` くとき、`GRS` は、** `` | `FR-059` の `STATEMENT`。EARS の応答節の主語 |
| 64 | 置換する | `` くとき、システムは、`p `` | `` くとき、`GRS` は、`p `` | `FR-090` の `STATEMENT`。EARS の応答節の主語 |
| 65 | 置換する | `むとき、システムは` | `` むとき、`GRS` は `` | `FR-023` の `STATEMENT`。EARS の応答節の主語 |
| 66 | 置換する | `たとき、システムは、その JS` | `` たとき、`GRS` は、その JS `` | `FR-024` の `STATEMENT`。EARS の応答節の主語 |
| 67 | 置換する | `たとき、システムは、**画面に` | `` たとき、`GRS` は、**画面に `` | `FR-080` の `STATEMENT`。EARS の応答節の主語 |
| 68 | 置換する | `たとき、システムは、表 T-0` | `` たとき、`GRS` は、表 T-0 `` | `FR-025` の `STATEMENT`。EARS の応答節の主語 |
| 69 | 置換する | `を開いたとき、システムは、**表 T-02` | `` を開いたとき、`GRS` は、**表 T-02 `` | `FR-087` の `STATEMENT`。EARS の応答節の主語 |
| 70 | 置換する | `**: システムは、**操作の` | `` **: `GRS` は、**操作の `` | `FR-026` の `STATEMENT`。EARS の応答節の主語 |
| 71 | 置換する | `るとき、システムは、**同じフ` | `` るとき、`GRS` は、**同じフ `` | `FR-060` の `STATEMENT`。EARS の応答節の主語 |
| 72 | 置換する | `**: システムは、自動` | `` **: `GRS` は、自動 `` | `FR-061` の `STATEMENT`。EARS の応答節の主語 |
| 73 | 置換する | `たとき、システムは、**表 T-03` | `` たとき、`GRS` は、**表 T-03 `` | `FR-062` の `STATEMENT`。EARS の応答節の主語 |
| 74 | 置換する | `だとき、システムは、**その文` | `` だとき、`GRS` は、**その文 `` | `FR-073` の `STATEMENT`。EARS の応答節の主語 |
| 75 | 置換する | `たとき、システムは、文書` | `` たとき、`GRS` は、文書 `` | `FR-074` の `STATEMENT`。EARS の応答節の主語 |
| 76 | 置換する | `いとき、システムは、初期` | `` いとき、`GRS` は、初期 `` | `FR-027` の `STATEMENT`。EARS の応答節の主語 |
| 77 | 置換する | `たとき、システムは、それを保持` | `` たとき、`GRS` は、それを保持 `` | `FR-097` の `STATEMENT`。EARS の応答節の主語 |
| 78 | 置換する | `だとき、システムは、**表 T-02` | `` だとき、`GRS` は、**表 T-02 `` | `FR-096` の `STATEMENT`。EARS の応答節の主語 |
| 79 | 置換する | `だとき、システムは、**開いて` | `` だとき、`GRS` は、**開いて `` | `FR-095` の `STATEMENT`。EARS の応答節の主語 |
| 80 | 置換する | `るとき、システムは、人が` | `` るとき、`GRS` は、人が `` | `FR-028` の `STATEMENT`。EARS の応答節の主語 |
| 81 | 置換する | `たとき、システムは、**1 ず` | `` たとき、`GRS` は、**1 ず `` | `FR-063` の `STATEMENT`。EARS の応答節の主語 |
| 82 | 置換する | `` **: システムは、`A `` | `` **: `GRS` は、`A `` | `FR-064` の `STATEMENT`。EARS の応答節の主語 |
| 83 | 置換する | `**: システムは、人が` | `` **: `GRS` は、人が `` | `FR-065` の `STATEMENT`。EARS の応答節の主語 |
| 84 | 置換する | `あいだ、システムは、画面` | `` あいだ、`GRS` は、画面 `` | `FR-066` の `STATEMENT`。EARS の応答節の主語 |
| 85 | 置換する | `**: システムは、本体` | `` **: `GRS` は、本体 `` | `FR-067` の `STATEMENT`。EARS の応答節の主語 |
| 86 | 置換する | `たとき、システムは、AI` | `` たとき、`GRS` は、AI `` | `FR-068` の `STATEMENT`。EARS の応答節の主語 |
| 87 | 置換する | `**: システムは、**本体の` | `` **: `GRS` は、**本体の `` | `FR-069` の `STATEMENT`。EARS の応答節の主語 |
| 88 | 置換する | `**: システムは、**表 T-04` | `` **: `GRS` は、**表 T-04 `` | `FR-092` の `STATEMENT`。EARS の応答節の主語 |
| 89 | 置換する | `**: システムは、各種` | `` **: `GRS` は、各種 `` | `FR-029` の `STATEMENT`。EARS の応答節の主語 |
| 90 | 置換する | `**: システムは、状態` | `` **: `GRS` は、状態 `` | `FR-030` の `STATEMENT`。EARS の応答節の主語 |
| 91 | 置換する | `たとき、システムは、キー` | `` たとき、`GRS` は、キー `` | `FR-036` の `STATEMENT`。EARS の応答節の主語 |
| 92 | 置換する | `たとき、システムは、同じ` | `` たとき、`GRS` は、同じ `` | `FR-037` の `STATEMENT`。EARS の応答節の主語 |
| 93 | 置換する | `だとき、システムは、メニ` | `` だとき、`GRS` は、メニ `` | `FR-038` の `STATEMENT`。EARS の応答節の主語 |
| 94 | 置換する | `たとき、システムは、その指定で` | `` たとき、`GRS` は、その指定で `` | `FR-039` の `STATEMENT`。EARS の応答節の主語 |
| 95 | 置換する | `**STATEMENT**: システムは、ポインタとキーボードの入力を` | `` **STATEMENT**: `GRS` は、ポインタとキーボードの入力を `` | `FR-040` の `STATEMENT`。EARS の応答節の主語 |
| 96 | 置換する | `たとき、システムは、初期` | `` たとき、`GRS` は、初期 `` | `NFR-001` の `STATEMENT`。EARS の応答節の主語 |
| 97 | 置換する | `区間で、システムは、平均` | `` 区間で、`GRS` は、平均 `` | `NFR-002` の `STATEMENT`。EARS の応答節の主語 |
| 98 | 置換する | `区間で、システムは、フレ` | `` 区間で、`GRS` は、フレ `` | `NFR-003` の `STATEMENT`。EARS の応答節の主語 |
| 99 | 置換する | `ない間、システムは` | `` ない間、`GRS` は `` | `NFR-010` の `STATEMENT`。EARS の応答節の主語 |
| 100 | 置換する | `るとき、システムは、**空白の` | `` るとき、`GRS` は、**空白の `` | `NFR-011` の `STATEMENT`。EARS の応答節の主語 |
| 101 | 置換する | `**STATEMENT**: システムは、**表 T-039 に` | `` **STATEMENT**: `GRS` は、**表 T-039 に `` | `NFR-012` の `STATEMENT`。EARS の応答節の主語 |
| 102 | 置換する | `` **: システムは、1 つの `.h `` | `` **: `GRS` は、1 つの `.h `` | `NFR-004` の `STATEMENT`。EARS の応答節の主語 |
| 103 | 置換する | `**: システムは、Ch` | `` **: `GRS` は、Ch `` | `NFR-005` の `STATEMENT`。EARS の応答節の主語 |
| 104 | 置換する | `**: システムは、WC` | `` **: `GRS` は、WC `` | `NFR-006` の `STATEMENT`。EARS の応答節の主語 |
| 105 | 置換する | `**: システムは、文字` | `` **: `GRS` は、文字 `` | `NFR-007` の `STATEMENT`。EARS の応答節の主語 |
| 106 | 置換する | `**: システムは、キー` | `` **: `GRS` は、キー `` | `NFR-008` の `STATEMENT`。EARS の応答節の主語 |
| 107 | 置換する | `たとき、システムは、**不正な` | `` たとき、`GRS` は、**不正な `` | `NFR-009` の `STATEMENT`。EARS の応答節の主語 |

### 2-2. Chapter 3 §3.2 の主フロー行（33 件・**EARS の主語ではない**）

**ユースケースの手順を述べた散文である。** 要求ではないので `**STATEMENT**` の応答節ではない。
**それでも置換する** —— 指している対象は EARS の `[System]` と同一だからである。

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `2. システムはその期` | `` 2. `GRS` はその期 `` | `UC-001` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 2 | 置換する | `4. システムは名称ラ` | `` 4. `GRS` は名称ラ `` | `UC-001` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 3 | 置換する | `5. システムは同じ行` | `` 5. `GRS` は同じ行 `` | `UC-001` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 4 | 置換する | `3. システムは階層を` | `` 3. `GRS` は階層を `` | `UC-002` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 5 | 置換する | `5. システムは畳んだ` | `` 5. `GRS` は畳んだ `` | `UC-002` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 6 | 置換する | `2. システムはプロパ` | `` 2. `GRS` はプロパ `` | `UC-003` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 7 | 置換する | `4. システムは描画を` | `` 4. `GRS` は描画を `` | `UC-003` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 8 | 置換する | `6. システムは指定に` | `` 6. `GRS` は指定に `` | `UC-003` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 9 | 置換する | `3. システムは、引き` | `` 3. `GRS` は、引き `` | `UC-004` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 10 | 置換する | `4. システムは種別か` | `` 4. `GRS` は種別か `` | `UC-004` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 11 | 置換する | `6. システムは経路を` | `` 6. `GRS` は経路を `` | `UC-004` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 12 | 置換する | `2. システムは実績開` | `` 2. `GRS` は実績開 `` | `UC-005` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 13 | 置換する | `4. システムは状態を` | `` 4. `GRS` は状態を `` | `UC-005` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 14 | 置換する | `2. システムは、動い` | `` 2. `GRS` は、動い `` | `UC-006` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 15 | 置換する | `3. システムは遅れて` | `` 3. `GRS` は遅れて `` | `UC-006` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 16 | 置換する | `2. システムはポイン` | `` 2. `GRS` はポイン `` | `UC-007` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 17 | 置換する | `3. システムはタイム` | `` 3. `GRS` はタイム `` | `UC-007` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 18 | 置換する | `4. システムは描くタ` | `` 4. `GRS` は描くタ `` | `UC-007` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 19 | 置換する | `2. システムは位置を` | `` 2. `GRS` は位置を `` | `UC-008` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 20 | 置換する | `4. システムは範囲を` | `` 4. `GRS` は範囲を `` | `UC-008` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 21 | 置換する | `2. システムは、開い` | `` 2. `GRS` は、開い `` | `UC-009` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 22 | 置換する | `4. システムは透かし` | `` 4. `GRS` は透かし `` | `UC-009` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 23 | 置換する | `2. システムは信頼で` | `` 2. `GRS` は信頼で `` | `UC-010` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 24 | 置換する | `3. システムは現在の` | `` 3. `GRS` は現在の `` | `UC-010` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 25 | 置換する | `6. システムは保持し` | `` 6. `GRS` は保持し `` | `UC-010` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 26 | 置換する | `2. システムは、取り` | `` 2. `GRS` は、取り `` | `UC-014` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 27 | 置換する | `3. システムは、対応` | `` 3. `GRS` は、対応 `` | `UC-014` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 28 | 置換する | `5. システムは現在の` | `` 5. `GRS` は現在の `` | `UC-014` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 29 | 置換する | `2. システムは、その` | `` 2. `GRS` は、その `` | `UC-011` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 30 | 置換する | `4. システムは出力サ` | `` 4. `GRS` は出力サ `` | `UC-011` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 31 | 置換する | `5. システムは操作の` | `` 5. `GRS` は操作の `` | `UC-011` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 32 | 置換する | `4. システムは受理し` | `` 4. `GRS` は受理し `` | `UC-012` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 33 | 置換する | `4. システムは変更を` | `` 4. `GRS` は変更を `` | `UC-013` の主フロー行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |

### 2-3. Chapter 3 §3.2 の拡張行（39 件・**EARS の主語ではない**）

**拡張（代替フロー）の手順を述べた散文である。** 2-2 と同じ理由で置換する。

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `るなら、システムは**開` | `` るなら、`GRS` は**開 `` | `UC-001` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 2 | 置換する | `るなら、システムはその日` | `` るなら、`GRS` はその日 `` | `UC-001` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 3 | 置換する | `- システムは複製を` | `` - `GRS` は複製を `` | `UC-001` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 4 | 置換する | `- システムは表示だ` | `` - `GRS` は表示だ `` | `UC-001` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 5 | 置換する | `- システムは指定を` | `` - `GRS` は指定を `` | `UC-001` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 6 | 置換する | `- システムは積むの` | `` - `GRS` は積むの `` | `UC-001` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 7 | 置換する | `- システムは移動を` | `` - `GRS` は移動を `` | `UC-002` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 8 | 置換する | `- システムは行の所` | `` - `GRS` は行の所 `` | `UC-002` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 9 | 置換する | `- システムは受け付` | `` - `GRS` は受け付 `` | `UC-003` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 10 | 置換する | `- システムは資源と` | `` - `GRS` は資源と `` | `UC-003` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 11 | 置換する | `- システムはプロパ` | `` - `GRS` はプロパ `` | `UC-003` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 12 | 置換する | `- システムは依存を` | `` - `GRS` は依存を `` | `UC-004` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 13 | 置換する | `- システムは引きか` | `` - `GRS` は引きか `` | `UC-004` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 14 | 置換する | `- システムは折れ点` | `` - `GRS` は折れ点 `` | `UC-004` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 15 | 置換する | `- システムは予定バ` | `` - `GRS` は予定バ `` | `UC-005` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 16 | 置換する | `- システムは実績を` | `` - `GRS` は実績を `` | `UC-005` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 17 | 置換する | `- システムは再開可` | `` - `GRS` は再開可 `` | `UC-005` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 18 | 置換する | `- システムは最も遅` | `` - `GRS` は最も遅 `` | `UC-006` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 19 | 置換する | `- システムはその段` | `` - `GRS` はその段 `` | `UC-006` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 20 | 置換する | `- システムは別ファ` | `` - `GRS` は別ファ `` | `UC-006` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 21 | 置換する | `- システムは縦にス` | `` - `GRS` は縦にス `` | `UC-007` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 22 | 置換する | `- システムは範囲選` | `` - `GRS` は範囲選 `` | `UC-007` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 23 | 置換する | `- システムは本文を` | `` - `GRS` は本文を `` | `UC-008` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 24 | 置換する | `- システムはコメン` | `` - `GRS` はコメン `` | `UC-008` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 25 | 置換する | `- システムは見えて` | `` - `GRS` は見えて `` | `UC-008` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 26 | 置換する | `- システムは名前の` | `` - `GRS` は名前の `` | `UC-009` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 27 | 置換する | `- システムは透かし` | `` - `GRS` は透かし `` | `UC-009` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 28 | 置換する | `- システムは取り込` | `` - `GRS` は取り込 `` | `UC-010` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 29 | 置換する | `- システムはタスク` | `` - `GRS` はタスク `` | `UC-014` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 30 | 置換する | `- システムは再び複` | `` - `GRS` は再び複 `` | `UC-014` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 31 | 置換する | `- システムは文書を` | `` - `GRS` は文書を `` | `UC-014` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 32 | 置換する | `- システムは一覧で` | `` - `GRS` は一覧で `` | `UC-014` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 33 | 置換する | `- システムは縮小し` | `` - `GRS` は縮小し `` | `UC-011` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 34 | 置換する | `- システムは黙って` | `` - `GRS` は黙って `` | `UC-011` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 35 | 置換する | `- システムは初期表` | `` - `GRS` は初期表 `` | `UC-011` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 36 | 置換する | `- システムは未保存` | `` - `GRS` は未保存 `` | `UC-011` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 37 | 置換する | `` - システムは `A `` | `` - `GRS` は `A `` | `UC-012` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 38 | 置換する | `- システムは構造化` | `` - `GRS` は構造化 `` | `UC-012` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |
| 39 | 置換する | `- システムは**保` | `` - `GRS` は**保 `` | `UC-013` の拡張行。EARS の主語ではない（手順の散文）。指す対象は `[System]` と同一 |

### 2-4. `**RATIONALE**` の中の鉤括弧引用（1 件・**EARS の主語ではない**）

⚠️ **これは `FR-086` の `RATIONALE` が `UC-009` の拡張 2a を鉤括弧で引いている箇所である。**
**引用元は 2-3 の表に載っている**（`` - システムは名前の設定を促す `` の行）。
**引用元と引用先を同じ作業で置換すれば鉤括弧の中は原文と一致し続ける。片方だけ置換すると食い違う。**

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `い → システムは` | `` い → `GRS` は `` | `FR-086` の `RATIONALE` 内の引用。EARS の主語ではない。**引用元（`UC-009` 拡張 2a）と必ず同じ作業で置換すること** |

---

## 3. 「システムが」20 件 —— 置換表

**置換後は一律「`` `GRS` が ``」。**

| 文脈 | 件数 | EARS の主語か |
|---|---:|---|
| Chapter 3 §3.2 の `**STATEMENT**:`（`UC-001`〜`UC-014`） | **13** | **主語である**（複文なので「が」で受ける） |
| Chapter 4 の `**STATEMENT**:` の中の禁止節 | **2** | 主語ではない |
| `FR-076` の `**STATEMENT**:`（契機の節） | **1** | **主語だが EARS の形に合っていない**（§7） |
| `**RATIONALE**:` の散文 | **2** | 主語ではない |
| 本文の注（散文） | **1** | 主語ではない |
| 表 T-024a の `OP-3` セルの中の説明 | **1** | 主語ではない |
| **計** | **20** | |

13 + 2 + 1 + 2 + 1 + 1 = 20。**足して合うことを確かめた。**

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `を示し、システムが` | `` を示し、`GRS` が `` | `UC-001` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 2 | 置換する | `編集し、システムが` | `` 編集し、`GRS` が `` | `UC-002` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 3 | 置換する | `を選び、システムが属性を` | `` を選び、`GRS` が属性を `` | `UC-003` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 4 | 置換する | `を結び、システムが` | `` を結び、`GRS` が `` | `UC-004` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 5 | 置換する | `入力し、システムが` | `` 入力し、`GRS` が `` | `UC-005` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 6 | 置換する | `指定し、システムが` | `` 指定し、`GRS` が `` | `UC-006` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 7 | 置換する | `を行い、システムが目盛の` | `` を行い、`GRS` が目盛の `` | `UC-007` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 8 | 置換する | `を置き、システムが` | `` を置き、`GRS` が `` | `UC-008` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 9 | 置換する | `すとき、システムが` | `` すとき、`GRS` が `` | `UC-009` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 10 | 置換する | `戻して、システムが` | `` 戻して、`GRS` が `` | `UC-010` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 11 | 置換する | `を選び、システムが現在の` | `` を選び、`GRS` が現在の `` | `UC-014` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 12 | 置換する | `き出し、システムが` | `` き出し、`GRS` が `` | `UC-011` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 13 | 置換する | `を行い、システムが受理し` | `` を行い、`GRS` が受理し `` | `UC-012` の `STATEMENT`。**EARS の主語**（複文なので「が」で受ける応答節の主語） |
| 14 | 置換する | `決める。システムが` | `` 決める。`GRS` が `` | `FR-001` の `RATIONALE`。**EARS の主語ではない**（理由の散文） |
| 15 | 置換する | `と。**システムが` | `` と。**`GRS` が `` | `FR-034` の `STATEMENT` の中の禁止節（MUST NOT）。**EARS の主語の位置ではない**が、指す対象は同一。⚠️ **直前が `**`（太字の開始）。行内コードが太字の直後に来るので docutils の落ちを export で確かめること** |
| 16 | 置換する | `であり、システムが` | `` であり、`GRS` が `` | `FR-034` の `RATIONALE`。**EARS の主語ではない**（理由の散文） |
| 17 | 置換する | `の両端はシステムが` | `` の両端は `GRS` が `` | `FR-013` の本文の注。**EARS の主語ではない**（散文）。⚠️ **直前が CJK なので `` `GRS` `` の前に半角空白を 1 つ入れた** |
| 18 | 置換する | `か別かをシステムが` | `` か別かを `GRS` が `` | `FR-022` の `STATEMENT` の中の禁止節（MUST NOT）。**EARS の主語の位置ではない**が、指す対象は同一。⚠️ **直前が CJK なので `` `GRS` `` の前に半角空白を 1 つ入れた** |
| 19 | 置換する | `なるかをシステムが` | `` なるかを `GRS` が `` | `FR-087` が指す表 T-024a の `OP-3` セルの中の説明。**EARS の主語ではない**（表の中の説明）。⚠️ **CR-106 §6-5 は「表のセルは 0 件」と書いているが、これが 1 件目である**。⚠️ **直前が CJK なので `` `GRS` `` の前に半角空白を 1 つ入れた** |
| 20 | 置換する | `**: システムが` | `` **: `GRS` が `` | `FR-076` の `STATEMENT`。**EARS の主語だが、契機の節にあり応答の節に主語が無い。§7 に別項目として挙げた** |

---

## 4. 表 T-007 の下の注 —— EARS の `[System]` の定義（原文と書き換え案）

**この注が EARS の `[System]` を定義している。** ⚠️ **同じ対象を「`[System]`」「本ソフトウェア」の 2 語で呼び、その 2 つを等号で結んでいるのはこの 3 行である。**
**`GRS` を足すと 3 語になるので、この注を直さない限り「同じ 1 つのものを 3 つの語で呼ぶ」状態が残る。**

**原文（現在の 3 行。そのまま引いた）** ——

```
> **EARS の `[System]` は、D-1 の上で動く単一 `.html` の本ソフトウェアを指す。**
> 以降の要求で主語となるのはこれだけである。D-2 から D-5 は本ソフトウェアの外側にあり、
> **こちらで挙動を変えられない**。それらに対する要求を書いてはならない（MUST NOT）。
```

**書き換え案（CR-106 §5-2 に沿う。1 行ずつのリテラル置換にした）** ——

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `` > **EARS の `[System]` は、D-1 の上で動く単一 `.html` の本ソフトウェアを指す。** `` | `` > **EARS の `[System]` は `GRS` である**（名称の宣言は 2.1）。**D-1 の上で動く単一 `.html` そのものを指す。** `` | **定義の正をここに置く。** `[System]` を `GRS` と等号で結び、「本ソフトウェア」を外す。名称の宣言（2.1）へは指すだけにする |
| 2 | 置換する | `> 以降の要求で主語となるのはこれだけである。D-2 から D-5 は本ソフトウェアの外側にあり、` | `` > 以降の要求で主語となるのはこれだけである。D-2 から D-5 は `GRS` の外側にあり、 `` | 「本ソフトウェア」を `GRS` に寄せる。**行の意味は変えない** |
| 3 | 置換しない | `> **こちらで挙動を変えられない**。それらに対する要求を書いてはならない（MUST NOT）。` | `> **こちらで挙動を変えられない**。それらに対する要求を書いてはならない（MUST NOT）。` | 3 行目は `GRS` も「本ソフトウェア」も含まない。**1 文字も変えない**（判定した証跡として載せた） |

⚠️ **§6 の「本ソフトウェア」表では、この 2 件を「置換しない」と判定している。** 単独でリテラル置換すると、この注の書き換えと二重に当たるからである。**直し方はこの §4 に一本化する。**

⚠️ **`[System]` は表 T-006（EARS 構文パターン）では置き換え記号であって literal である。表 T-006 は 1 文字も変えない。** 解決先を定めるのはこの注と §5 の 2 か所だけである。

---

## 5. §1.9 にもう 1 つある `[System]` の定義（**要判断**）

**`[System]` の定義は 2 か所にあり、中身が違う。** §1.9 は「Chapter 2.2 で『対象ソフトが載る』と記した**機器の上で動くソフトウェア**」という機器経由の間接指定、
表 T-007 の下の注は「D-1 の上で動く単一 `.html` の本ソフトウェア」という直接指定である。
**§1.9 自身が「同じことを 2 度書きたくなったら、置き場を 1 つ決め、他方は行 ID か `UID` で指すこと（MUST）」と定めているので、この重複は自分の規約に違反している。**

⚠️ **この行に「システムは」「システムが」「本ソフトウェア」は 1 件も含まれない。** つまり **X3（主語の置換）の対象ではない。**
それでも挙げるのは、**主語に名前を与える作業で片方だけ直すと、2 つの定義の食い違いが固定されるからである。**

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 要判断 | `` **`[System]` の定義。** EARS の `[System]` は、**Chapter 2.2 で「対象ソフトが載る」と記した機器の上で動くソフトウェアを指す。Chapter 2 を書かずに Chapter 4 を書いてはならない（MUST NOT）。** `` | `` **`[System]` の定義。** EARS の `[System]` の定義は **Chapter 2.2（表 T-007 の下の注）が持つ。Chapter 2 を書かずに Chapter 4 を書いてはならない（MUST NOT）。** `` | 語の置換ではなく**定義の一本化**である。CR-106 §5-2 が「正は Chapter 2.2 の 1 か所」と決めているが、**X3 の範囲（主語の置換）の外**なので、§4 と同じ作業で行うかどうかは判断が要る。⚠️ **直したら §1.9 から定義の文言が消えたことを確かめること** |

---

## 6. 「本ソフトウェア」15 件 —— 同じ対象を 2 つの語で呼んでいる箇所

### どちらに寄せるべきか（**断定してよい、と指示があったので断定する**）

**`GRS` に寄せる。** 理由は 3 つ ——

1. **仕様書自身が 2 語を等号で結んでいる。** 表 T-007 の下の注（§4）が「EARS の `[System]` は…**本ソフトウェア**を指す」と書いており、**「システム」と「本ソフトウェア」が同じものであることは仕様書の主張である**（推測ではない）
2. **名前の正は用語辞書である**（§1.9「名前の正は `_assets/tbl-glossary.md`」）。辞書の `U-45` が `GoodRelax Scheduler` / `GRS` を持っている
3. **図 F-001 は既に `GRS` を使っている。** 宣言が無いまま使われているので、**寄せ先は既に事実上 `GRS` である**

⚠️ **ただし各行の「置換の是非」は下の表で個別に判定した。** 3 件は **要判断**である。

**置換後は「`` `GRS` ``」。** 直後が CJK の文字になるので、**`` `GRS` `` の後ろに半角空白を 1 つ入れた**（「本ソフトウェアは」→「`GRS` は」）。

### 6-1. `docs/spec/01-04-requirements.md`（12 件）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `本ソフトウェアは、作図` | `` `GRS` は、作図 `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一 |
| 2 | 置換する | `のうち、本ソフトウェアが` | `` のうち、`GRS` が `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一 |
| 3 | 置換する | `タ \| 本ソフトウェアの` | `` タ \| `GRS` の `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一 |
| 4 | 置換する | `は、**本ソフトウェアが` | `` は、**`GRS` が `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一。⚠️ **直前が `**`（太字の開始）。行内コードが太字の直後に来るので docutils の落ちを export で確かめること** |
| 5 | 置換する | `\| **本ソフトウェア固` | `` \| **`GRS` 固 `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一。⚠️ **直前が `**`（太字の開始）。行内コードが太字の直後に来るので docutils の落ちを export で確かめること** |
| 6 | 置換する | `ST）。本ソフトウェアが` | `` ST）。`GRS` が `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一 |
| 7 | 置換する | `こまでを本ソフトウェアが` | `` こまでを `GRS` が `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一。⚠️ **直前が CJK なので `` `GRS` `` の前に半角空白を 1 つ入れた** |
| 8 | 置換しない | `` l` の本ソフトウェアを `` | `` l` の本ソフトウェアを `` | **表 T-007 の下の注の中。§4 の書き換えに含まれる。** 単独でリテラル置換すると二重に当たる |
| 9 | 置換しない | `-5 は本ソフトウェアの` | `-5 は本ソフトウェアの` | **表 T-007 の下の注の中。§4 の書き換えに含まれる。** 単独でリテラル置換すると二重に当たる |
| 10 | 置換する | `等） \| 該当なし（本ソフトウェアの外） \|` | `` 等） \| 該当なし（`GRS` の外） \| `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一 |
| 11 | 置換する | `同上 \| 該当なし（本ソフトウェアの外） \|` | `` 同上 \| 該当なし（`GRS` の外） \| `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一 |
| 12 | 置換する | `系 \| 本ソフトウェアは` | `` 系 \| `GRS` は `` | `GRS` に寄せる。指す対象は EARS の `[System]` と同一 |

### 6-2. `docs/spec/05-07-design.md`（1 件）

⚠️ **CR-106 の変更箇所の表にこのファイルは載っていない**（§6-4 で「05-07-design.md は 0 件」と数えている）。**CR-106 執筆後に増えた 1 件である。**

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `。** 本ソフトウェアが` | `` 。** `GRS` が `` | `GRS` に寄せる。指す対象は同一。⚠️ **CR-106 の変更箇所の表に無い行なので、CR 側にも足すこと** |

### 6-3. `docs/spec/_assets/tbl-glossary.md`（2 件）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 要判断 | `本ソフトウェアの名称。略称は` | `本仕様書が仕様を定めるソフトウェアの名称。略称は` | **`U-45` の説明文。機械的な置換をしてはならない** —— 「`GRS` の名称。略称は `GRS`」となり定義が自分を指す。**言い換えが要る。**上の置換後は CR-106 §5-3 の案。⚠️ **行は足さない（`U-45` は登録済み）** |
| 2 | 置換する | `` 4`）「本ソフトウェアは `` | `` 4`）「`GRS` は `` | 表 T-009 の `XO-4` を引いた注。**語だけを置換する。**⚠️ **引用が既に原文とずれている** —— `XO-4` の原文は「`GRS` は **AI を**持たない」だが、ここは「**AI 推論の実行系を**持たない」と引いている。**ずれ自体は §8 の別件とし、本作業では直さない** |

---

## 7. `FR-076` は EARS の形に合っていない（**自分で確かめた。事実である**）

**確かめ方**: `FR-076` の `STATEMENT` を開き、表 T-006 の `E-2`（Event-driven）の「日本語の形」と並べた。

**`FR-076` の `STATEMENT`（原文）** ——

```
**STATEMENT**: システムが利用者に何かを知らせるとき、**表 T-037 の作法**に従うこと。
```

**表 T-006 の `E-2` が定める形** ——

```
| E-2 | Event-driven | **When** [Trigger], the [System] shall [Response]. | [Trigger] したとき、[System] は、[Response] すること。 | イベント起点の要求 |
```

**食い違いは 1 点**: `E-2` は **`[Trigger] したとき、[System] は、[Response] すること。`** を求めている。
`FR-076` は **契機の節に「システムが」を置き、応答の節に主語が無い。**
「システムが利用者に何かを知らせるとき」までが `[Trigger]` であり、**`[System] は、` が欠けたまま「表 T-037 の作法に従うこと。」に飛んでいる。**
**したがって `FR-076` は「システムは」を 1 件も持たない。**`FUNC_REQ` 96 件のうち、主語の置換で本文が変わらない唯一の要求である（CR-106 §6-3 の「触らない 1 件」と一致した）。

**⚠️ 2 つは別の作業である。分けて扱うこと。**

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| 1 | 置換する | `**: システムが` | `` **: `GRS` が `` | **語の置換。**§3 の表にも同じ行が入っている（重複ではなく同一の 1 件）。**文の形は変えない** |
| 2 | 要判断 | `**STATEMENT**: システムが利用者に何かを知らせるとき、**表 T-037 の作法**に従うこと。` | `` **STATEMENT**: `GRS` が利用者に何かを知らせるとき、`GRS` は、**表 T-037 の作法**に従うこと。 `` | **EARS の形の是正。**応答の節に主語を補う案。⚠️ **これは要求の書き換えであって語の置換ではない。**CR-106 §6-9 も「別件。置換はするが文の形は直さない」としている。**やるなら別の CR を立てること** |

---

## 8. 判定の集計と、置換の順序

### 8-1. 集計（**自分で数えた**）

| 判定 | 件数 | 内訳 |
|---|---:|---|
| **置換する** | **212** | 「システムは」 180 ＋「システムが」 20 ＋「本ソフトウェア」 12 |
| **置換しない** | **2** | 表 T-007 の下の注の中の「本ソフトウェア」 2 件（§4 の書き換えに含まれるため、単独では置換しない） |
| **要判断** | **1** | 用語辞書 `U-45` の説明文（機械的な置換をすると定義が自分を指す） |
| **計** | **215** | §0 で数えた 3 語の出現総数と一致 |

212 + 2 + 1 = 215。**足して合うことを確かめた。**

⚠️ **「出現の数」と「表の行の数」は一致しない。** 上の表は**出現**を数えている。**表の行は全部で 221 行ある**（数えた） ——
215（出現）＋ §4 の 3 行 ＋ §5 の 1 行 ＋ §7 の 2 行 = 221。
**うち 3 行は同じ場所を別の切り口で 2 度載せている** —— §4 の 1・2 行目は §6-1 の 8・9 番と同じ 2 か所、§7 の 1 件目は §3 の `FR-076` の行と同じ 1 か所である。
**二重に置換しないこと。** §4 の 2 行を採るなら §6-1 の 8・9 番は触らない（そう判定してある）。

**上の 3 語の出現ではないが、判定を要する項目が 3 つある**（表には載せた） ——

| 場所 | 判定 | 中身 |
|---|---|---|
| §4 の注の 3 行目 | **置換しない** | 3 語のどれも含まない。**1 文字も変えない** |
| §5（§1.9 の `[System]` の定義） | **要判断** | 語の置換ではなく定義の一本化。X3 の範囲外 |
| §7 の 2 件目（`FR-076` の EARS 形） | **要判断** | 要求の書き換えであって語の置換ではない。別の CR が要る |

### 8-2. 置換の順序（**小さい塊を先に置く**）

⚠️ **`` `GRS` `` は ASCII の行内コードであり、これを CJK に隣接させる。**
**CJK に隣接した装飾で docutils（StrictDoc の export）が落ちた記録がある**ので、**最小の塊で先に壊れを検出する。**

| 順 | 塊 | 件数 | 理由 |
|---|---|---:|---|
| 1 | §4（表 T-007 の下の注）＋ §5 の判断 | 置換 2・置換しない 1 | **定義を先に確定する。**以降の置換の根拠がこれ |
| 2 | §3 のうち直前が CJK / `**` の 4 件 | **4** | ⚠️ **最も壊れやすい 4 件を最初に通す。**うち 1 件は太字の開始直後に行内コードが来る |
| 3 | §2-4（`RATIONALE` の引用）＋ その引用元（§2-3 の該当行） | **2** | **必ず同じ塊で。**片方だけ直すと鉤括弧の中が原文と食い違う |
| 4 | §6（「本ソフトウェア」） | 置換 12 ・要判断 1 | 辞書を含むので単独の塊にする |
| 5 | §3 の残り | **16** | |
| 6 | §2-1（Chapter 4 の `STATEMENT`） | **107** | |
| 7 | §2-2 ＋ §2-3（Chapter 3 の手順） | **72** | |

**各塊のあとで `bash .claude/skills/spec-graph-check/check.sh` を回すこと。** **export が通ったことだけが、docutils で壊れていない証拠である。**

### 8-3. 置換後に成り立つべき数

| 語 | 置換後 |
|---|---:|
| 「システムは」 | **0** |
| 「システムが」 | **0** |
| 「本ソフトウェア」 | **0** |
| 「システム」 | **7**（§0 に挙げた複合語・見出しのみ） |

⚠️ **行数が変わらないことも確かめること。**§2・§3・§6 はリテラル置換なので改行は生まれない。**§4 と §5 だけが行の中身を作り替える。**

### 8-4. 本作業で見つけた、直さないもの（記録）

| # | 中身 | 状態 |
|---|---|---|
| 1 | **用語辞書の `XO-4` 引用が原文とずれている** —— 原文「AI を持たない」／引用「AI 推論の実行系を持たない」 | **別件。**§1.9 の「指す先の文を書き写してはならない（MUST NOT）」が現に破れている例。**語の置換だけ行い、ずれは直さない** |
| 2 | **`FR-076` が EARS の形に合っていない**（§7） | **別件。**要求の書き換えになる |
| 3 | **`[System]` の定義が 2 か所にある**（§4 と §5） | **要判断。**§1.9 の「同じことを 2 度書かない（MUST）」に違反している |
| 4 | **CR-106 の「本ソフトウェア 10 件」が古い**（現在 15 件） | **CR-106 の §3・§5-4・§6-4 の数を直す必要がある** |
