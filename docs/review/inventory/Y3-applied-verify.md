# Y3 — 反映済み 9 件の検算（壊れているものだけ）

**対象**: `git diff docs/spec`（未コミットの作業ツリー）。CR-101 / 102 / 103 / 104 / 108 / 111 / 112 / 113 / 114（要求側）。
**私は `docs/spec` を 1 バイトも編集していない。** 読んだだけである。

## 走らせた道具

```
python .claude/skills/spec-graph-check/graph.py --cycles
python .claude/skills/spec-graph-check/impact.py AG-11 AG-6 / FR-063 / FR-032 FR-033 T-050 / FR-031 FR-085 S-125 / FR-004 FR-018 S-127
bash .claude/skills/spec-graph-check/check.sh
```

**`check.sh` は `ALL GREEN` である。** 検査 13 は 4 件のまま（CR-114 §6 の条件を満たす）、検査 14 は 20 件。
⚠️ **緑は何も保証していない。** 下の **F-1** は Markdown の表の行が壊れているのに、検査 5〜10・12・15 のどれも拾わない。

**触った対象が作る 2 閉路は 1 つだけである** —— `AG-11` ↔ `AG-6`。**それが壊れている**（F-3）。
他の 4 つの 2 閉路（`MG-10`↔`MM-2` / `MG-13`↔`S-71` / `AG-11`↔`AG-6` を除く / `EZ-2`↔`S-124`）は、
今回の差分がどちらの側も書き換えていない。残りの編集はほぼ全部が size 195 の閉路の内側にある。

---

## F-1【最重】表 T-201 を指す小表の 1 行が Markdown として壊れている

`FR-094` の下、`| 浮いていたもの | 無いと何が決まらないか |` の表の 2 行目が、**`|` で始まっていない。**

現在のその行（リテラル・全文）——

```
FR-077` は「タスクの高さに比例させた文字」と事実として述べるだけで、比例させることを求めていない |`） | **ラベルの文字サイズがバーの高さから導かれること。** `FR-077` は「タスクの高さに比例させた文字」と事実として述べるだけで、比例させることを求めていない |
```

`HEAD` の同じ行は `| 予定の縦幅（\`basePlanHeight\`） | **ラベルの文字サイズがバーの高さから導かれること。** …` である。
**置換の引数が入れ替わって、消したはずの後半が行頭に貼り付き、行の先頭 `| 予定の縦幅（\`basePlanHeight` が失われた。**
表の行として読まれないので行 ID も持たず、**機械検査が 1 つも鳴らない。**

由来: `X1-cr114-requirements.md` の `4-09`（除く）。

**直し方** —— その行の全体を、X1 `4-09` の置換後どおりに置き換える。

```
| 予定の縦幅（`basePlanHeight`） | **ラベルの文字サイズがバーの高さから導かれること**（`FR-077`） |
```

---

## F-2【重】同じ小表が、消された `FR-079` の文を引用したまま宙に浮いている

同じ表の 3 行目は、いまも次のとおりである。

```
| 依存線の寸法（太さ・矢じり・走り） | `FR-079` の RATIONALE が「依存線の太さは個別に非追随と定めていた」と述べるのに、**その「個別に定めた」箇所が要求に無かった** |
```

しかし `FR-079` の RATIONALE から
`角丸（\`FR-019\`）と依存線の太さは個別に非追随と定めていたが、**横断の要求として持っていなかった。**`
は削除済みである（X1 `4-05` を実行した）。**引用元が仕様書に無い。**

X1 の `4-10` は「⚠️ **4-05 を実行すると引用元の文が消えるので、本行は 4-05 と必ず同じ塊で直す**」と明記していた。
**4-05 だけが当たり、4-10 が当たっていない。**

**直し方** —— X1 `4-10` の置換後どおり。

```
| 依存線の寸法（太さ・矢じり・走り） | **太さ・矢じり・走りの値を持つのは表 T-201 だけであり、ここを指さないと非追随の対象が決まらない**（`FR-079`） |
```

⚠️ **F-1 と F-2 は同じ表の隣り合う 2 行なので、1 回で直すこと。**
あわせて `4-07` / `4-08`（要判断・未処理）の `**とくに次の 3 つが宙に浮いていた。**` と
`| 浮いていたもの | 無いと何が決まらないか |` も同じ塊に入る。この 4 行は互いに文脈を持ち合っている。

---

## F-3【重】`AG-11` を書き換えて `AG-6` を触っていないので、`AG-6` が自分と矛盾する

反映後の `AG-11` ——

> **対話欄で人が確定した発話を読めること（MUST）。** 発話は日程データではないので**版数を上げない**（`FR-063`）。**それでも監視は起きること**（`AG-6`）。

未変更の `AG-6` ——

> 監視は「**自分が最後に読んだ版より後で、かつ自分以外の書き手が確定した変更だけ**」を通知すること。**確定した発話（`AG-11`）もこれに含める（MUST）**

**`AG-6` の選択条件は版でしか対象を選べない。版が上がらない発話は、この条件では 1 つも選べない。**
それでいて同じ行が「確定した発話もこれに含める（MUST）」と命じている。**1 行が自分と食い違っている。**

`CR-103` は「**`AG-6` は変更しない**」と決め、代わりに **Chapter 5.5 に「発話の通番（保存しない）」という 2 系統の設計**を置くことで
`AG-6`・`FR-066`・`FR-063` を同時に立たせるとしている。
**`docs/spec/05-07-design.md` の 5.5 は `> 未記入。` のままである**（開いて確認した）。
したがって **いま仕様書だけを読むと `AG-6` は成立しない。**

**直し方（要求側だけで閉じる案）** —— `AG-6` のリテラル
`監視は「**自分が最後に読んだ版より後で、かつ自分以外の書き手が確定した変更だけ**」を通知すること。`
を、版に依らない書き方へ置き換える。たとえば ——

```
| AG-6 | 監視は「**自分がまだ受け取っていない、自分以外の書き手が確定した変更と発話だけ**」を通知すること。**日程データの変更は版数で、確定した発話（`AG-11`）は版数に依らない別の合図で数えること（MUST）** —— 発話は版数を上げない（`FR-063`）ので、版数だけでは選べない。
```

⚠️ **`AG-6` だけを直しても閉じない。** `AG-11` の「**それでも監視は起きること**（`AG-6`）」と対で読むこと。

---

## F-4【重】`FR-063` の「群の割り当ての全数」の持ち主が存在しない

反映後の `FR-063` の STATEMENT にある文 ——

> **群の割り当ての全数は Chapter 5.4 と `_assets/tbl-settings.md` が持つ。**

- **Chapter 5.4（Domain Model）は `> 未記入。` の器である**（`docs/spec/05-07-design.md` を開いて確認した）。
- **`_assets/tbl-settings.md` に「日程データの群 / 見せ方の群」を示す列は無い。**
  同書で `群` が一致するのは **1 行だけ**で、それは表 T-201 の見出し
  `| 行 ID | 群 | キー | 単位 | 既定値 | 下限 | 上限 | 範囲の理由 |` である（自分で全 5 ファイルを検索して数えた）。
  この `群` の値は `タイムルーラー` `縦の寸法` `依存線` `ラベル` `LOD` … であり、**まったくの別物である。**

`CR-103` §5 の置換文は、持ち主を
`保存する値の全数は \`_assets/tbl-settings.md\` が、**見せ方の全数は表 T-202 / T-203 が持つ**`
としていた。**反映時に、実在する持ち主が実在しない持ち主に置き換わっている。**

さらに `CR-103` が置いていた**「見せ方」の定義**——
`**「見せ方」とは、同じ版数のデータから、全体を収める表示（\`FR-0xx\`）と表示の切り替え（表 T-202）を揃えれば同じ描画に戻せるもののことである**`
——**が落ちている。** 定義も持ち主も無いので、ある鍵がどちらの群かを読む手掛かりが仕様書に 1 つも無い。

**判定不能の実例**: `themeHue`（`S-73`。表 T-203）。日程は 1 日も動かないので「日程データの群」に見えないが、
`FR-063` が散文で挙げる見せ方の列挙（倍率・表示位置・パネル幅・`Dual Cursor` の位置・表 T-202）にも入っていない。
**どちらの群かが決まらない。** これは `CR-110` §2-4 が名指しした破れそのものである。

**直し方（どれか）**

1. 持ち主を実在するものへ戻す —— `Chapter 5.4 と \`_assets/tbl-settings.md\` が持つ` を
   `\`_assets/tbl-settings.md\` の表 T-202 / T-203 が持つ` に置き換え、あわせて `CR-103` の「見せ方」の定義文を戻す。
2. `CR-110` を当てて `DR-` の表を作り、そこを持ち主にする。**ただし F-15 の席番号の衝突を先に片づけること。**

---

## F-5【重】`FR-063` が「全数を散文で持ってはならない」と書いた直後に、散文で群を列挙している

同じ STATEMENT の中で ——

> **群の割り当ての全数は Chapter 5.4 と `_assets/tbl-settings.md` が持つ。この全数を散文で持ってはならない（MUST NOT）** —— 表に行を足すたびにここが腐る。**倍率・表示位置・パネル幅・`Dual Cursor` の位置・表 T-202 が持つ表示の切り替えは見せ方の群に属する。**

F-4 のとおり持ち主が空なので、**この散文の列挙が、いま存在する唯一の群の割り当てである。**
さらに `FR-098` が別の場所で散文で 1 件足している ——
`**ピン止めの状態は見せ方の群に属する**（\`FR-063\`）。`
**割り当てが 2 か所の散文に分かれた。** 反映前の `FR-063` は ⛔ / 🅿 という表側の印を持ち主にしていたので、
散文は補足だったが、いまは散文が正になっている。

**F-4 と F-5 は同じ 1 文の中にあるので、必ず 1 回で直すこと。**

---

## F-6【重】表 T-050 の `CD-1` が CR-112 の範囲を超え、`FR-031` の「例外は行の削除だけ」と衝突する

`CR-112` §5 の `Task` の行は ——

> | （新） | `Task` | その `Task` の `TaskVisual` / `TaskOrigin` / `TaskGroupMember` / その `Task` を端点とする依存 / その `Task` を指す割当 / その `Task` に付いた注記 |

**WBS の部分木は「行（`TaskGroup`）」の行にだけ書かれている。** 利用者の回答も
「その行の member ∪ それらの WBS 部分木でよい」であり、**行の削除についての判断である。**

反映された `CD-1` は先頭に **`**その \`Task\` の WBS の子孫**`** を足している。

**帰結** —— `Task` を 1 つ消すと、その WBS の部分木が丸ごと消える。
`FR-032` が確認を求めるのは **行の削除だけ**である（STATEMENT の `行を削除するときは確認を求めること。`）。
`Delete` / `Backspace`（表 T-036 の `SK-3`）から、確認なしで一撃で起きる。

`FR-031` の RATIONALE ——

> **例外は行の削除だけである**（`FR-032`）—— 連鎖して消える範囲が大きく、取り消しで戻せても「何が消えたか」を人が把握できないため。**例外を増やさない（MUST NOT）。**

**`CD-1` は、まさにこの理由が当てはまる操作になったのに、例外に入っていない。**
`CR-112` §6 も「⚠️ **最重要。**（中略）**本 CR は連鎖の範囲を広げるので、この理由づけがより強く当てはまる。文言の調整が要るかを読んで判断する**」と警告していた。**その調整が入っていない。**

**直し方（どちらか。片方だけでは閉路が閉じない）**

- (a) `CR-112` どおりに戻す —— `CD-1` の `**その \`Task\` の WBS の子孫**、` を消し、WBS 部分木は `CD-2` だけが持つ。
- (b) 広げた範囲を認め、確認とやり直しの規則を両側で揃える ——
  `FR-032` の `行を削除するときは確認を求めること。` を
  `行を削除するとき、および WBS の子孫を持つ \`Task\` を削除するときは確認を求めること。` に置き換え、
  `FR-031` の `**例外は行の削除だけである**（\`FR-032\`）` を
  `**例外は \`FR-032\` が確認を求めると定めた削除だけである**` に置き換える。

---

## F-7 表 T-050 の「`CD-1` と `CD-2` は同じ範囲を消すこと（MUST）」は、書いたとおりには満たせない

現在のリテラル ——

```
**`CD-1` と `CD-2` は同じ範囲を消すこと（MUST）** —— 経路によって結果が変わってはならない。
```

`CD-1` の対象は `Task` 1 つ、`CD-2` の対象は行 1 つである。**消す範囲は定義上ちがう。**
この文は `CR-112` に無く、反映時に足されている。意図（経路によって結果が変わらない）は正しいが、
**書いた形では検証できない MUST になっている。**

**直し方**

```
**`CD-2` が消す範囲は、その行に載る各 `Task` に `CD-1` を適用した和と一致すること（MUST）** —— 経路によって結果が変わってはならない。
```

---

## F-8 `FR-033` の部分木複製に、`TaskGroup` の深さの上限が掛かっていない

深さの上限（`S-125`。既定 5）を掛ける規則は 2 か所にある。

- 作る側 —— `FR-085`: `**深さの上限は \`FR-004\` に従う（値は表 T-211 の \`S-125\`）。上限に達している親の下に作らせてはならない（MUST NOT）。**`
- 移す側 —— `HM-3a`: `**移動後の深さが \`FR-004\` の上限を超える移動を受け付けてはならない（MUST NOT）。** 部分木は**移動後の最深部**で測る。`

**複製（`FR-033`）だけが穴である。** `FR-033` は
`行見出しパネルでは、選ばれた \`TaskGroup\` を**部分木ごと**複製すること。` と書くだけで、深さに触れていない。
深さ 3 の部分木を深さ 4 の親の下へ貼れば `S-125` を超える。

`CR-111` §6 は確認事項として「`FR-085`｜**深さの上限（`S-125`）に達している親の下へ貼り付けられないこと**を、複製にも適用するか」を挙げていた。**未反映。**

⚠️ **WBS 側は問題ない。** `FR-004` の下に `**WBS の深さをクランプしてはならない（MUST NOT）。**` があり、WBS は上限を持たない（`G-2`）。
**掛かるのは `TaskGroup` の深さだけである**（`A-4` に従って併記した）。

**直し方** —— `FR-033` に `HM-3a` と同じ形の 1 文を足す。

```
**貼り付け後の深さが `FR-004` の上限を超える複製を受け付けてはならない（MUST NOT）。** 部分木は**貼り付け後の最深部**で測る（`HM-3a` と同じ数え方）。拒んだことを人に知らせる（`FR-076` の `NT-1`）。
```

---

## F-9 `FR-033` から CR-111 の規則が 2 つ落ち、語が 1 つ割れている

`CR-111` §5 が足すと書いた段落と、反映されたものの差 ——

| CR-111 が書いたもの | 反映後 |
|---|---|
| `**複製したタスクには新しい \`UID\` を採ること（MUST）。** 採番は \`Project.uidHighWaterMark\` に従う。` | `**複製した \`Task\` には新しい \`UID\` を採ること（MUST）。**`（採番規則が落ちた） |
| `**複製に \`TaskOrigin\` を付けてはならない（MUST NOT）**` | `複製に取り込み元の記録を付けてはならない（MUST NOT）`（語が変わった） |
| `**貼り付け先の行は、既定行の生成規則に従って決めること。** 複製した部分木の根がサマリなら、その器が新しく作られる。` | **無い** |

1. **`uidHighWaterMark` は `docs/spec` 全体で 0 回である**（自分で数えた）。**新しい `UID` の採り方を定める文がどこにも無い。**
   ⚠️ **これを Chapter 5 / 6 へ回す判断だったのかは、CR にも差分にも書かれていない。未検証。**
2. **貼り付け先の行を決める規則が無い。** `FR-033` は「複製すること」しか言わないので、複製した `Task` がどの行（`TaskGroup`）に載るかが決まらない。
   `CD-2` は「その行に載っているすべての `Task`」を消すので、**載る行が決まらないと削除の範囲も決まらない。**
3. **同じものを 2 つの語で呼んでいる**（1.9 の命名の規約）。`FR-033` は「取り込み元の記録」、
   同じ差分で新設した `CD-1` は `\`TaskOrigin\`` と、名指しで呼んでいる。どちらかに寄せること。

---

## F-10【重】`Task.milestone` は、仕様書のどこからも読み書きされない

CR-104 が触ったのは 表 T-005 の `G-1` と 辞書 `N-1` の 2 行だけである（CR-104 §3 のとおり）。**その結果 ——**

- **`Task.milestone` の実出現は `docs/spec` 全体で 2 か所**（`G-1` と `N-1`。自分で数えた）。
- **MSPDI の要素名 `Milestone` の実出現は 0 か所**（自分で数えた）。
- 「書き出す」と書いているのは `G-1` の
  `**マイルストーンかどうかは \`Task.milestone\`（真偽値）が持ち、これを交換相手へ書き出す。**` だけである。
  **ところが表 T-005 は自分でこう宣言している** ——
  `**表 T-005 は辞書からの抜粋であり、正ではない。食い違ったら辞書が勝つ。**`
  **そして辞書 `N-1` に書き出しの話は無い。書き出しの規則が、正でないと宣言された表にしか存在しない。**
- **表 T-016 に `milestone` の行が無い。** 同表は
  `**本表が正であるのは、画面に出す項目の全数と、備考・MSPDI の対応である。**` と宣言している。
  つまり **正が「持っていない」と言っている列を、抜粋の表が「書き出す」と言っている。**
- `PR-11 | \`shapeKind\` | タスク形状 | 表 T-012 | 無い（JSON のみ）` はそのままで、`shapeKind` を `Task` の項目として並べ続けている。
  `G-1` は `**どう描くかは \`TaskVisual.shapeKind\` が持ち、書き出さない。**` と言う。**持ち主が 2 つある。**
- 辞書 `P-10` は `\`shapeKind\` | タスク形状（5 値。\`'milestone'\` のときだけ \`milestoneGlyph\` を見る）` のまま、
  `FR-083` は `**表 T-012 の \`SH-1\` 〜 \`SH-4\` と \`SH-5\`（マイルストーン）の間で変えてはならない（MUST NOT）。**` のままである。
  **「形と真偽値は別」という新しい説明と、形が実質マイルストーン性を決め続けている既存の規則が同居している。**
- CR-104 §5 の
  `取り込み | \`Task/Milestone\` → \`Task.milestone\`。\`shapeKind\` の既定は、真なら \`'milestone'\``
  `書き出し | \`Task.milestone\` をそのまま書く。**視覚層を読まない**`
  は、`FR-021` / `FR-023` / `FR-057` / 表 T-033 のどこにも入っていない。

**`Task.milestone` を真にする経路も、書き出す経路も、仕様書に無い。**

**直し方** —— 表 T-016 に行を 1 つ足すのが最小である。行 ID は席番号なので空きを確かめてから採ること
（`PR-1` 〜 `PR-17` が埋まっている。自分で数えた）。

```
| （新） | `milestone` | マイルストーンかどうか | **真偽値。`TaskVisual.shapeKind` とは別の概念**（`G-1`）。**書き出しの正はこれ** | `Task/Milestone` |
```

⚠️ **表 T-016 に行を足すと、同表を数え上げる規則が全部かかる**（SKILL.md の「full-count table」の罠）。
`FR-071`（表 T-016 の項目を出す）・`UN-3`（`Task` のプロパティの変更は取り消し対象）・`FR-083`（形状変更の禁止）を
**同じ 1 回で読むこと。**

---

## F-11 `FR-098`（ピン止め）が仕様書のどこともつながっていない

- **ORIGIN が実在しない手順を指している。** `FR-098` は `**ORIGIN**: UC-007 手順 5` だが、
  `UC-007` の手順 5 は `閲覧者が \`Ctrl\` ＋ ドラッグまたは中ボタンドラッグで、見たい位置まで等倍で動かす。`（パン）である。
  **`UC-007` の SCENARIO 5 手順と EXTENSIONS 2 分岐（`1a.` `5a.`）を全部読んだが、ピン止めの手順は 1 つも無い**（自分で数えた）。
- **表 T-011 の `V-7`（UC-007 が吸収した要望の項番）に、ピン止めに当たる項番が足されていない。**
- **ピン止めする操作が無い。** 「ピン」の実出現は `docs/spec` に 6 か所
  （`FR-098` の 4 か所、`U-46`、`S-126` と `S-127` の備考。自分で数えた）。
  **表 T-023（割当）・表 T-023d（掴み領域）・表 T-036（ショートカット）・表 T-040（マニュアルを読まずに使う手立て）のどれにも行が無い。**
  `FR-029`（同じ機能の入口を 2 か所に置かない）が前提にしている「入口」が存在しない。
- **`S-126` に、存在しない id を指していたときの規則が無い。**
  `S-126 | \`pinnedGroupIds\` | \`TaskGroup.id\` の配列` は、同じ型の
  `S-78 | \`scrollGroupId\` | \`TaskGroup.id\`（UUID 文字列）/ \`null\`` と違い、
  `存在しない id を指していたときの規則は表 T-024a の \`OP-10\`` を持たない。
  `OP-10` は `**表示位置が \`null\`、または指す行が存在しないとき**` としか書いておらず、ピン止めを含まない。
  **合流（`FR-022` / `FR-056`）や置き換え（`OP-3`）の後に、消えた行を指すピンが残る。**
- **`S-126` / `S-127` が持ち主の要求を指していない。** 設定値の行は規則を持つ要求を指すのが本書の型である
  （`S-125` → `FR-004`、`S-73` → `FR-041`、`S-79` → `FR-052`、`S-77` / `S-78` → `OP-10`）。
  `S-126` は `上限は \`S-127\``、`S-127` は `同時にピン止めできる行の数の上限` としか書いておらず、**どちらも `FR-098` を指さない。**
  そのため `impact.py S-126` / `impact.py S-127` から `FR-098` へ辿れない（`impact.py S-127` を実行し、`FR-098` が「この行を指している箇所」側にしか出ないことを確認した）。
  **値の行と規則を持つ要求が作るはずの 2 閉路（規約であって欠陥ではないほうの型）が、ここだけ存在しない。**
- **`FR-098` だけが主語に `GRS` を使っている。** `GRS` の実出現は `docs/spec` に 3 か所
  （Chapter 2.1 の mermaid `ブラウザ<br/>GRS が載る`、`FR-098` の STATEMENT、辞書 `U-45`。自分で数えた）。
  他の要求は「システムは」のままで、1.9 の
  `**\`[System]\` の定義。** EARS の \`[System]\` は、**Chapter 2.2 で「対象ソフトが載る」と記した機器の上で動くソフトウェアを指す。**`
  も未変更である（CR-106 は未反映）。**97 件の `FUNC_REQ` のうち 1 件だけが先に主語を変えている。**

**直し方** —— CR-108 の起票元を読み直し、(1) `UC-007` に手順か拡張を足して ORIGIN を実在させる、
(2) 入口の行を表 T-023 か表 T-036 に足す、(3) `S-126` の備考に `OP-10` と同型の逃げ道を書く、(4) 主語は CR-106 まで「システムは」に戻す。
**(4) を先送りするなら、`FR-098` だけが先行していることを CR-106 の §3 に書き足しておくこと。**

---

## F-12 CR-114 の除去が 88 件中 82 件しか当たっておらず、1 件は壊れている

`X1-cr114-requirements.md` の判定は **除く 88 / 残す 43 / 要判断 6**。
**私は X1 の表を機械で数え直し、同じ 88 / 43 / 6 を得た**（137 行）。

**置換前リテラルがいまも `01-04-requirements.md` に一致する「除く」は 6 件である**（自分で数えた）——

| X1 # | 未反映のリテラル（先頭） |
|---|---|
| `3-03` | `` `UC-010` に置いたままだと、`` |
| `4-10` | `\| 依存線の寸法（太さ・矢じり・走り） \| \`FR-079\` の RATIONALE が…`（**F-2**） |
| `4-16` | `\| ST-11 \| （廃止。人が段を指定する手段を設けないので、指定どうしの衝突が起きない） \|` |
| `8-02` | `\| SK-1 \| （廃止。キーボードだけで図形を置く経路は持たない。表 T-026 の \`RC-10\`） \| — \|` |
| `13-01` | `\| RC-4 \| ~~\`fontScale\` の S / M / L が何 px か未定義で…`（決着済みの行） |
| `13-02` | `\| RC-9 \| ~~タイムルーラーの上でのポインタ操作に受け皿が無い~~ …`（決着済みの行） |

さらに `4-09` は当たったが壊れた（**F-1**）。**82 件が当たり、うち 1 件が壊れ、6 件が残っている。**

**CR-114 §8「取り除いた文の保管庫」は、見出しと空の表だけである。**
CR-114 は自分でこう書いている ——
`**除いた文はここにしか残らないので、消す前に必ず §8 へ写すこと（MUST）。**`
`2. **除く前に §8 へ原文を写す（MUST）**`
**この MUST が守られていない。** 実際には X1 の「置換前」列が 88 件のリテラルを持つので原文は失われていないが、
**CR が唯一の保管庫と宣言した場所は空である。**

**CR-114 §6 の順序も守られていない** ——
`**⚠️ 本 CR は最後に回すこと。** 他の CR が本文を書き換えるので、先に掃除すると二度手間になる。`
CR-105 / 106 / 107 / 109 / 110 が未反映のまま CR-114 を先に当てた。**F-2 はその直接の帰結である。**

---

## F-13 表 T-036 の注が、数えずに「全数」を主張している

置換後（X1 `8-04`）——

```
> ⚠️ **本表がショートカットキーの割当の全数である。**行数は本表が持ち、散文に書かない。
```

元の文は `**本表は実在が確かめられたものと、新設と宣言されたものを合わせた全数である。**` と、
**何を根拠に全数と言うか**を書いていた。置換後はその根拠を落として断定だけを残している。
SKILL.md が名指しする型（**A new "this is the full count" sentence must be counted, not asserted**）そのものである。

**私が数えた表 T-036 の行は 22 行**（`| SK-` で始まる行）。**うち 2 行（`SK-1` / `SK-1a`）は「廃止」で割当が `—` である**
（X1 `8-02` が未反映のため。**F-12**）。したがって「割当の全数」は 22 でも 20 でもありうる。**どちらとも書かれていない。**

あわせて消えた `**引継ぎ資産への書き戻しは未処理である。**` は**作業の残り**である。
X1 は「⚠️ **どちらも CR 側へ移す**（未処理の作業は CR-114 §8 に写したうえで別 CR か課題表へ）」と指示していたが、
**CR-114 §8 は空なので、この未処理はどこにも残っていない。**

---

## F-14 「推奨を決定として書かない。」が仕様書からも CR からも消えた

Chapter 2.1 から次の一文が丸ごと削除された（X1 `1-02`）——

```
前プロジェクトは描画方式を決めているが、その決定記録は引継ぎ資産に含まれておらず、残っているのは「次期にも同じ選択を推奨する」という推奨までである（`previous-project-result/09-architecture/architecture-entry-ja.md` §1）。**推奨を決定として書かない。**
```

**`docs/spec` 全体で「推奨」が一致するのは 5 か所で、この禁止は 1 つも残っていない**（自分で数えた）——
1.9 の RFC 2119 の語義、`FR-004` の「上限と推奨の深さ」、`S-125` の「推奨の深さは 3」、Chapter 5.1 の器の文（`推奨案は … \`status: draft\` であり決定ではない`）。

X1 の判定（「執筆者向けの作業規則で、製品の仕様でも採用理由でもない」）は筋が通っている。
**問題は行き先である。** CR-114 §8 が空なので、この作業規則は**仕様書からも CR からも消えた。**
残っているのは Chapter 5.1 の器が局所的に言う 1 文だけで、**器が書き上がれば消える。**

**直し方** —— CR-114 §8 に写すか、1.9 の書き方の規約（`W-` の表の下）へ 1 文として置き直すこと。

---

## F-15 表 T-050 の席番号が CR-110 と衝突している（CR-110 はこのままでは当てられない）

`change-request/CR-110-document-root-grouping.md` は自分の席として **T-050 / T-051 / T-052** を書いている ——
`① **文書ルートを \`schedule\` / \`documentSettings\` の 2 群に分ける** | 新しい表 **T-050**（\`FR-024\` が指す）`、
`### 5-2. 表 T-050 — 文書ルートの群（新設）`、`表 T-050 の \`DR-5\``、`新しい表 **T-051**`、`新しい表 **T-052**`。

**T-050 は今回 CR-112 の「削除の連鎖」が取った。** T-051 は CR-109 に割り当てられている。

**私が数えた現在の表番号** —— T-001〜T-029（**T-030 は欠番**）、T-031〜T-043、**T-050**、T-101〜T-105、T-201〜T-215。
T-044〜T-047 は `A-appendix.md` の 0.7 が
`破棄した設計が使った席番号 \`T-044\` 〜 \`T-047\` と \`F-002\` 〜 \`F-007\` は使用済みであり、別のものに割り当て直してはならない（MUST NOT）`
と封じた席。**T-048 / T-049 が空きである。**

番号は席番号で詰め直せない（1.9）ので、**CR-110 の §1 / §3 / §5 と `DR-` を指す全文を書き換えるまで、CR-110 は当てられない。**
`change-request/` は `docs/spec` ではないので、直してよい。

⚠️ **`FR-063` は CR-110 に「差し替え」と書かれている（`| 5 | 同 | \`:3111\` | \`FR-063\` の \`STATEMENT\`（**差し替え**。\`CR-103\` の 5 節を上書き）|`）が、
いま入っているのは CR-103 の文言である。** CR-110 §2-4 は
`**\`CR-103\` の判定基準と、③の決定は同時に立たない。**` と書き、**利用者は構造で決める方を選んだ**としている。
**つまり、いま仕様書に入っている `FR-063` は CR-110 が明示的に退けた側の文言である。** F-4 の直し方を選ぶときにここを見ること。

---

## F-16 `HM-10` と `CD-2` が互いを指していない／表 T-050 が部分の表だと書かれていない

- `HM-10`（新設）が `**タスクバーを別の行へ移したとき、WBS の子は元の行に残る。**` と定めたので、
  `CD-2`（`**その行に載っているすべての \`Task\`**（\`CD-1\` が各 \`Task\` に連鎖する）と、配下の行`）は
  **別の行に見えている `Task` を消す。** 表 T-050 の下の
  `**消える \`Task\` のうち他の行に表示されているものには、その旨を示すこと（MUST）**` はその事実を前提にしているのに、
  **`HM-10` を名指ししていない。** 2 つは同じ 1 回で読まないと成立しない。
- ⚠️ **`CR-111` は「バーの移動で子が追随しない」行を 表 T-014 に足せと書いていた**
  （`**表 T-014 に足す行**（行 ID は起票時に空きを確かめて採る）`）。**実際には 表 T-015a に `HM-10` として入っている。**
  表 T-015a の持ち主 `FR-005` の STATEMENT は
  `作成者が行見出しパネルで階層を移したとき` であり、**`HM-10` が扱うのは日程の描画領域でのバーのドラッグである。**
  同じずれは既存の `HM-3` にもあるので新しい破れではないが、**表 T-023d の `GR-12`（予定バー本体の平行移動と行の載せ替え）から `HM-3` / `HM-10` への指しが無い。**
- **表 T-050 は「削除の連鎖」と名乗るが、削除の全数ではない。**
  `FR-081` の `SL-1` は選択・削除の対象に**基準日線**を数え（消し方は `FR-046` の「消す」）、
  `SK-3` は `選択しているものを削除する（対象の全数は表 T-023c の \`SL-1\`）` と書いている。
  **表 T-050 に基準日線の行は無い。** 資源（`FR-008` が
  `**どの割当からも参照されていない担当者を作成者が明示して削除できること（MUST）**` を持つ）の行も無い
  （`CR-112` §5 は `Resource` の行を書いていたが、反映時に落ちて `CD-3` / `CD-4` に置き換わっている）。
  **表 T-050 が部分の表であることが、表にも `FR-032` にも書かれていない。**

**直し方** —— 表 T-050 の下に 1 文を足す。

```
⚠️ **本表は `FR-032` が消すものの連鎖だけを持つ。** 基準日線を消す規則は `FR-046` が、担当者（資源）を消す規則は `FR-008` が持つ。
```

---

## 確かめて、壊れていなかったもの（記録のみ）

- **綴りの置換に取り残しは無い。** `wbs_parent_uid` / `link_type` / `status_date` / `Schedule Title` / `WatermarkPassword` は
  `docs/spec` の 5 ファイルすべてで **0 か所**（自分で数えた）。`HEAD` では **20 か所**あった
  （要求 16 ＝ `wbs_parent_uid` 4 ／ `link_type` 2 ／ `status_date` 10、辞書 3、設定値 1）。
  ⚠️ **依頼文の「17 か所」とは数が合わない。私の数は 20 である**（`git show HEAD:` の各ファイルを機械で数えた）。
- ⚠️ **未反映の CR の中に古い綴りが残っている。** `CR-113` §5 の `U-11` と `X-5` の置換元／置換先に
  `Project.status_date` が入っている（`CR-000-impact-and-order.md` も
  `**\`CR-102\` を先に適用すると、この綴りは \`Project.statusDate\` に変わり、置換元が一致しなくなる。**` と警告していた）。
  **`CR-113` は反映済みなので実害は出ていないが、CR 側の本文は古いままである。**
- `FR-063` を指してくる 5 件のうち `FR-024` / `FR-073` / `FR-066` は成り立っている（版数＝更新回数と形式の版の区別、理由と版数の対）。
  **成り立っていないのは `FR-064`（`AG-11`／`AG-6`。F-3）と `FR-098`（F-5）である。**
- `WY-1` と `FR-063` は衝突していない。`WY-1` は `倍率と表示位置を含む` 再現を求め、除外は `表 T-206`。
  見せ方は保存されるので再現でき、版数を上げないことと両立する（`CR-103` §6 が「反映時に原文に当たって確認する」としていた項目。**原文に当たって確認した**）。
- `S-99a` と `FR-020` / `FR-086` の size 8 の閉路は保たれている（`S-99a` は保管先だけを書き、規則は `FR-020`）。
- 削除された `_assets/fig-*.md` と `_assets/tbl-datamodel.md` への宙吊りの参照は無い（`fig-` `tbl-datamodel` `DM-` `T-3xx` を全ファイルで検索。**0 件**）。
