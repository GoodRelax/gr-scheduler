# A07 — security + a11y

## 0. 読んだ文書と行数

| 略号 | ファイル | 総行数 | 読んだ範囲 | 位置づけ |
| --- | --- | ---: | --- | --- |
| `sec` | `previous-project-result/05-security-a11y/security-design.md` | 627 | **全文（1–627）** | 脅威モデル・入力検証・CSP・localStorage・透かしの主原典 |
| `a11y` | `previous-project-result/05-security-a11y/a11y-wcag21-aa-checklist.md` | 261 | **全文（1–261）** | WCAG 2.1 A/AA 全 50 基準の突合表。適合範囲の決定を含む |
| `E01`〜`E11` | `previous-project-result/temp/inventory/E0*.md` `E1*.md` | 3,380 | **突合に要る箇所のみ**（E11 全 522 / E09 1–70・290–430 / 他は機械検索） | 突合先の在庫表（上書きしない） |
| `tbl-settings` | `docs/spec/_assets/tbl-settings.md` | 332 | **部分**（213–252・259–289 のみ） | 在庫表 E09 が「値の正」として指す先。値を確かめる目的でだけ開いた |
| `memory` | `~/.claude/.../memory/live-verify-gotchas.md` | 25 | 全文 | CSP ハッシュ方式の実測記録（本作業の外の観測） |

`出典` 列の `sec:470` は「`security-design.md` の 470 行目」を指す。以下同様。

**自分で数えた数**

| 数えたもの | 値 | 数え方 |
| --- | ---: | --- |
| `sec` §8 のコントロール行 | **20**（C-01〜C-20） | `^\| C-[0-9]` を機械計数 |
| うち取下げで死んでいる行 | **4**（C-03 / C-04 / C-09 / C-10） | `sec:49` の一覧を §8 の表と照合 |
| `sec` §9 のテストケース行 | **15**（ST-01〜ST-15） | `^\| ST-[0-9]` を機械計数 |
| うち死んでいる行 | **7**（ST-01〜04 / ST-11〜13） | `sec:51` |
| `sec` §2.4 の攻撃シナリオ | **5**（AS-1〜AS-5・うち AS-1 は死） | 機械計数 ＋ `sec:46` |
| `a11y` 対応表の行 | **52** | 110–161 行の表本体を機械計数 |
| うち AA 判定に数える行 | **50**（AAA 参考採用 2 行を除く） | `AAA（参考採用）` を機械計数 = 2 |
| 判定の内訳 | 適合 **1** / 該当なし **14** / 範囲外 **5** / 方針あり **11** / 要対応 **19** | 太字判定を機械計数。合計 50 で一致 |
| 在庫表 11 枚に現れる「文書の識別子」相当の列 | **0**（言及は E09:318 の 1 件のみで、列ではない） | `documentid\|document_id\|文書の識別子\|文書 ID` を 11 枚に機械検索 |
| 本ファイルが記録した設計に効く決定 | **56**（§1 が 16・§2 が 18・§3 が 22） | 各表の行を数えた |

---

## 1. データモデルに効く決定（16）

| # | 決定 | 内容 | 出典 |
| --- | --- | --- | --- |
| D-1 | **秘密を localStorage に置かない** | MVP に認証トークンは無いが、将来の JWT を localStorage に置かない方針を今から明記する（HttpOnly Cookie かメモリ） | `sec:467`–`469`, `sec:522` |
| D-2 | **localStorage に置くもの** | 日程データ・UI 設定・透かしユーザー名だけ（⚠️ 在庫と食い違う → §在庫表との食い違い Z-1） | `sec:470` |
| D-3 | **キーの規約** | アプリ固有プレフィックス `grsched.`。破損時はサイレント破棄せず「破損のため復元不可」を復旧確認で通知する | `sec:475`–`476` |
| D-4 | **自動保存のキーは文書ごとに分ける** | `grsched.autosave.<documentId>`。分けないと素の GRS・埋込文書つき GRS・古い複製が同じ鍵を奪い合う。⚠️ **`documentId` の受け皿はデータモデルに無い**（在庫 11 枚を機械検索して 0 件） | `sec:486`–`488` |
| D-5 | **復元値は信頼できない入力** | localStorage から復元する内容もスキーマ検証と安全描画を通す（前回セッションの汚染・手動改ざんを想定） | `sec:156`–`158`, `sec:473`–`474` |
| D-6 | **JSON は明示スキーマ検証** | `JSON.parse` 後に内部モデルの型へ検証。必須欠落・型不一致は**拒否してエラー通知**（強制変換しない）。未知フィールドの扱いは ⚠️ Z-3 | `sec:323`–`325`, `sec:345` |
| D-7 | **プロトタイプ汚染防御** | reviver で `__proto__` / `constructor` / `prototype` を破棄。展開先は `Object.create(null)` かスキーマ既知キーへの明示コピー。**`Object.assign(target, untrusted)` を使わない** | `sec:326`–`346` |
| D-8 | **DOCTYPE / ENTITY を含む XML は拒否** | パース**前**に文字列を検査して弾く。XXE と billion-laughs の双方を封じる（`DOMParser` の既定に頼らない多層化） | `sec:353`–`357`, `sec:365`–`369` |
| D-9 | **`<parsererror>` は破損として拒否** | 取り込まない | `sec:358` |
| D-10 | **MSPDI は許可された既知要素のみ読む** | 未知要素は警告として通知。値は安全描画と型検証を経由させる（⚠️ Carry との接続が未定義 → Z-8） | `sec:359`–`360` |
| D-11 | **取込は全か無か** | 上限超過は取り込まずエラー通知し、**部分適用しない**。大きい正当データは分割 Import を案内する | `sec:401`–`403` |
| D-12 | **上限の内訳** | ファイル `JSON/XML: 20 MB`・アイテム `20,000`・ネスト深さ `64`（値は「実装時に調整」と書かれている） | `sec:393`–`399` |
| D-13 | **色値は設定前に検証する** | `#RRGGBB` / 既定パレットキー / `rgb()` の正規表現で検証してから属性へ入れる（⚠️ 受理集合が在庫の値域と合わない → Z-7） | `sec:250`–`252` |
| D-14 | **URL 系属性に信頼できない値を入れない** | `href` / `xlink:href` へユーザー値を流さない | `sec:249`–`250` |
| D-15 | **透かし文字列は安全描画でしか出さない** | ユーザー名・日時は `<text>` ＋ `textContent`。SVG Export も `innerHTML` 連結ではなく DOM 構築 ＋ `XMLSerializer` | `sec:505`–`507` |
| D-16 | **埋め込み文書は HTML のテンプレート処理である** | 埋め込む JSON 文字列中の `<` を JSON の Unicode エスケープ `\u003c` へ置き換えないと、タスク名に含まれる `</script>` が早期にタグを閉じ、残りが HTML として本文に出る（**対照実験で実測**と原典が書く） | `sec:203`–`207` |

> D-16 は**データの中身が実行経路になる**唯一の例で、タスク名という利用者データが直接効く。エスケープ規則は文書の形の一部として決めること。

---

## 2. アーキテクチャに効く決定（18）

| # | 決定 | 内容 | 出典 |
| --- | --- | --- | --- |
| A-1 | **信頼境界の位置** | 「ビルド済みの自身のコード」と「実行時に取り込む外部データ」の間に引く。**localStorage の中身も信頼しない側**に置く | `sec:153`–`158` |
| A-2 | **関所は 1 つ** | 外部データは必ず Import Validator / Sanitizer を通り、**無害化された内部モデルだけ**が描画へ流れる。向きは 外部 → 検証 → モデル → 描画 → DOM の一方向 | `sec:177`–`188` |
| A-3 | **`Agent API` は信頼境界ではない** | ページ内でスクリプトを実行できる主体は API の有無に関わらず内部状態へ到達できる。**したがって API は攻撃面を増やさない** | `sec:192`–`193` |
| A-4 | **それでも既定で公開しない** | 利用者が有効にしたときだけ公開する（断りなく口を開けないため）。防御は入力検証と CSP に置く | `sec:194`–`195` |
| A-5 | **無効化は不可逆ではない** | 無効化は以後の名前解決を止めるだけで、**渡し済みの参照は取り消せない**（実測） | `sec:196` |
| A-6 | **再評価の条件** | 同一ページに第三者のコードが載る配信形態（埋め込み・プラグイン・サーバ配信）を採るなら本項を見直す | `sec:197`–`198` |
| A-7 | **API 専用の抜け道を作らない** | API 経由の変更も UI と同じ検証を通る | `sec:200`–`201` |
| A-8 | **描画は安全 API のみ** | `innerHTML` / `outerHTML` / `insertAdjacentHTML` / `document.write` を信頼できないデータへ使うことを**全面禁止**。実装レビューで静的検査する | `sec:244`–`245`, `sec:539` |
| A-9 | **要素生成の作法** | `document.createElementNS(SVG_NS, …)` ＋ `setAttribute`。テキストは `textContent` か `<text>` ＋ `textContent` | `sec:247`–`249`, `sec:264`–`266` |
| A-10 | **CSP は meta で配る** | サーバーヘッダが使えないので `<meta http-equiv>` としてビルド時に HTML へ埋め込む | `sec:410`–`412` |
| A-11 | **CSP の内容** | `default-src 'none'` / `script-src 'self' 'sha256-…'` / `style-src 'self' 'unsafe-inline'` / `img-src 'self' data:` / `font-src 'self' data:` / `connect-src 'none'` / `object-src 'none'` / `base-uri 'none'` / `form-action 'none'` / `frame-ancestors 'none'` | `sec:421`–`433` |
| A-12 | **インライン script はハッシュ方式** | ビルド後に SHA-256 を算出して注入する。**`'unsafe-inline'` を残さない**。nonce は静的配布物では不適 | `sec:446`–`454`, `sec:551` |
| A-13 | **`style-src 'unsafe-inline'` は当面許容** | 属性・クラスベースへ寄せ、将来の除去を目標としてバックログ化する | `sec:455`–`458` |
| A-14 | **`img-src data:` は PNG 出力のために生きている** | SVG を `data:` URI として `<img>` に読ませ canvas へ描く経路が要る。**取込が死んでも出力は生きている** | `sec:59`–`62`, `sec:441`–`443` |
| A-15 | **scheme は `data:` で決着（2026-08-02・ユーザー判断）** | 理由は ①`blob:` の解放漏れという失敗の型が存在しない ②`file://` で生成元に依存しない ③CSP 変更ゼロ。落ちたら `blob:` へ移す（費用は CSP 1 行と符号化関数だけ） | `sec:64`–`82` |
| A-16 | **符号化は `encodeURIComponent`。`btoa` を使ってはならない** | `btoa` は Latin-1 しか受け付けず、**タスク名に日本語が入ると例外で落ちる** | `sec:77`–`78`, `sec:445` |
| A-17 | **`file://` では localStorage / IndexedDB が全ローカルファイルで共有される** | フォルダが違っても同じ保管庫。**任意のローカル HTML ページが GRS の自動保存を読める**。ブラウザを落としても残る。保持したファイルハンドルも共有され、別の複製を開くと無関係な文書のハンドルが復帰しうる。`http(s)` 配信なら消える。⚠️ Playwright Chromium 1228 での実測で、**他エンジンは未測定** | `sec:479`–`494` |
| A-18 | **将来フェーズの境界** | 権限境界は必ずサーバーに置き、クライアント判定は UX 用とする。MVP では権限フックの口だけ用意し、CSP をビルド変数化してフェーズで切替可能にする | `sec:520`–`528` |

---

## 3. a11y のうち描画とレイアウトを縛るもの（22）

| # | 縛り | 内容 | SC | 出典 |
| --- | --- | --- | --- | --- |
| V-1 | **「WCAG 2.1 AA 適合」とは名乗らない**（確定 2026-08-02・ユーザー判断） | 支援技術による読み上げを対象外とし、**視覚と操作**に絞る。条文 5.2.1 が「met in full」を求め部分適合が無いため、1 つでも外すなら札を返上する | — | `a11y:69`–`77` |
| V-2 | **レベルを AA→A に下げる案は採らない** | 読み上げの核は Level A に集中し、動機（コントラスト・非文字コントラスト・フォーカス表示・文字拡大・言語）は AA にある。**レベルという刃では分けられない** | — | `a11y:83`–`86` |
| V-3 | 文字と背景 **4.5:1**。UI 配色トークンを 1 か所へ集約し AA 準拠値に調整。パレット遊休時は「地色のみ」フェード（文字は不透明） | 1.4.3 | AA | `a11y:123` |
| V-4 | 重なる相手と **3:1 以上**（特に実績 ÷ 予定）。Progress Marker は形が意味を担い色に依存しない | 1.4.11 | AA | `a11y:127` |
| V-5 | **予実の区別は破線/実線だけではない** —— **予定バーの高さ > 実績バーの高さという幾何**と Progress Marker の形 `(✓)` / `( \ )` / `(!)` が担う。**マーカーは SVG で描き、フォントの字形に依存させない** | 1.4.1 | A | `a11y:49`–`52` |
| V-6 | 選択＝破線枠、キーボードフォーカス＝実線リング、予定/実績＝破線/実線。色スウォッチに色名ラベル。`[予定][実績]` トグルは**押下状態＋アイコン形状の二重符号** | 1.4.1 | A | `a11y:121` |
| V-7 | `fontScale`（`S`/`M`/`L`）の**保存値は初期値であって読む人が変更できる。文書が強制してはならない** | 1.4.4 | AA | `a11y:124` |
| V-8 | **SVG の `<text>` は実テキストであって画像ではない。** アイコンは全て線画で文字を含まない。PNG 出力は成果物であってページの内容ではない | 1.4.5 | AA | `a11y:125` |
| V-9 | **日程表そのものは Reflow の例外に当たる**（縦=対象・横=時間の 2 次元が意味）。**例外にならないのは App Header / パネル / ダイアログ**で、320px 相当で読めること | 1.4.10 | AA | `a11y:126` |
| V-10 | 行間 1.5 倍・字間 0.12 倍の上書きに耐えること。⚠️ **「ラベル幅は概算する。実測もキャッシュもしない」という確定事項と衝突する可能性がある** | 1.4.12 | AA | `a11y:128` |
| V-11 | ツールチップは **消せる / ポインタを乗せられる / 勝手に消えない** の 3 条件 | 1.4.13 | AA | `a11y:129` |
| V-12 | キャンバスをフォーカス可能化。Tab=アイテム間、矢印=1日/1行、Shift+矢印=リサイズ、Enter/Space=配置/編集、Escape=取消。**ドラッグの等価物は Properties Panel の日付入力に預ける** | 2.1.1 | A | `a11y:130` |
| V-13 | Tab のローピングを端で止めず、フォーカスがキャンバス外へ抜けること | 2.1.2 | A | `a11y:131` |
| V-14 | **単文字キーは 3 つ** —— `F`（Fit）、削除確認ダイアログ内の `D` / `C`。`D`/`C` はダイアログ内と明記され条件を満たすが、**`F` は有効範囲が書かれていないので判定できない** | 2.1.4 | A | `a11y:132` |
| V-15 | **点滅する表示を持ち込まない**ことを設計制約とする | 2.3.1 | A | `a11y:135` |
| V-16 | DOM 順を **ヘッダー → パレット → 左ペイン → キャンバス → Properties Panel** に整列。オーバレイは絶対配置なので見た目は不変 | 2.4.3 | A | `a11y:139` |
| V-17 | `:focus-visible` で全コントロール/キャンバスに高コントラスト輪郭。フォーカス中アイテムに **SVG 実線リング** | 2.4.7 | AA | `a11y:143` |
| V-18 | **押した時点で実行せず、離した時点で確定し、途中で中断できる**こと（`Escape`=取消が既に設計にある） | 2.5.2 | A | `a11y:145` |
| V-19 | `<html lang>` を起動時とロケール切替時に active locale へ設定 | 3.1.1 | A | `a11y:149` |
| V-20 | `prefers-reduced-motion: reduce` でフェード等の transition/animation を無効化（**AAA 参考採用・AA 判定には数えない**） | 2.3.3 | AAA | `a11y:136` |
| V-21 | 掴み代は予定バーの上下 **6px**、フェード掴み点 **15×15px**。**文書に保存しない**（読む人のアクセシビリティに属する）。**AA 判定には数えない**（タッチ・モバイルは対象外） | 2.5.5 | AAA | `a11y:148` |
| V-22 | **ラベルの縁取り**（フォント × 0.17）で文字が接する色を背景色にし、比を **21:1 で固定**する（人が選んだ色に依存させない）。⚠️ Z-4 | 1.4.3 | — | `a11y:245`–`250` |

### 3-1. `1.3.1` を設計の健全性の指標として残す

**適合範囲からは外したが、捨てない。** 見た目にしか情報が無ければ、見た目を変えた瞬間にその情報は消える（`a11y:176`）。

| 事実 | 内容 | 出典 |
| --- | --- | --- |
| 関係は 4 つとも既にデータ側にある | WBS の親子＝`Task.wbs_parent_uid` / 所属＝`TaskGroupMember`（`stack_order` 込み） / 依存＝`Dependency` / 予実の対応＝**1 個の `Task` が両方を持つ** | `a11y:203`–`208` |
| 足りないのはモデルではなく**出す経路**だけ | — | `a11y:210` |
| **JSON を単一 HTML に埋めても 1.3.1 は満たせない** | 条文の **text** は「支援技術が取り出せ、かつ人間の言語である文字の並び」で `<script>` の中身は該当しない。JSON Schema の `description` は型の説明であって文書の事実ではない | `a11y:212`–`216` |
| 骨格段階での使い方 | 同じストアを読んで**関係をテキストで出す 2 本目の出力**を 1 度作り、**安いか**だけ測る。描画層をほどく必要があったら設計が壊れている合図 | `a11y:218`–`227` |

> この 4 つの関係は在庫表と一致している（`E11:109` の `wbs_parent_uid` / `E11:174`–`175` の `TaskGroupMember` / `E11:117`–`119` の `Dependency` 複合 PK / `E02` の実績 6 列）。**綴りだけ古い** —— `stack_order` は `stackOrder` へ要改名（`E11:483`）。

---

## 4. ⚠️ 単一 `.html`・サーバー不要・オフライン（`CN-1`）の下で成り立たない対策

| # | 対策 | 何が成り立たないか | 判定 | 出典 |
| --- | --- | --- | --- | --- |
| X-1 | **`frame-ancestors 'none'` によるクリックジャッキング防止** | CSP は **`<meta>` 配信のとき `frame-ancestors` を無視する**（`report-uri` / `sandbox` も同じ）。`sec:459`–`460` は「meta では `X-Frame-Options` が効かないから CSP で代替する」と書くが、**代替になっていない**。C-12 の検証項目「frame 埋込が遮断されること」は meta だけでは満たせない見込み | **未検証**（CSP 仕様の規定による指摘であり、本作業では実測していない）。実機で確かめること | `sec:459`–`460`, `sec:550` |
| X-2 | **`connect-src 'none'` で「XSS が起きてもデータを外部送信できない」** | 止まるのは fetch / XHR / WebSocket / Beacon だけで、**トップレベル遷移や `window.open` による持ち出しは CSP のどの指令も止めない**（遷移先を縛る `navigate-to` は標準に残らなかった）。`sec:436`–`438` の断定は言い過ぎ | **未検証**（機構としての指摘。実測していない）。文面を「送信 API を封じる」へ弱めるか、別の対策を足すか | `sec:436`–`438`, `sec:219` |
| X-3 | **`script-src 'self' 'sha256-…'`** | `file://` で開くと `'self'` は不透明生成元になり意味を持たない。**効いているのはハッシュだけ**であり、インライン script が 1 バイト違えば全滅する。実測記録: 文字列キーに NUL / 制御文字が混ざると Chromium が書き換え、**ハッシュ不一致でアプリ全体が起動しない**（`memory:15`）。C-13 の検証項目は「CSP にハッシュが列挙されていること」だけで、**`file://` 実機での起動を見ていない** | **実測あり（過去の観測）**。C-13 の検証を「`file://` で起動すること」まで広げる | `sec:424`, `sec:551`, `memory:15` |
| X-4 | **透かし解除パスワード**（仕様側の機能。`sec` は 1 行も扱っていない） | 既定パスワードは `gr-scheduler`（仕様書に平文で載る）で、**成果物へ埋め込むのは SHA-256 だけ**。CN-1 では照合コードもハッシュも同じ `.html` の中にあるので、**アクセス制御にはならない**（DOM を編集すれば透かしは消せる）。`sec:501` の「透かしはアクセス制御ではなく証跡マーキングである」を**パスワードにも明記する**必要がある | **確定**（値と置き場所を原典で確認） | `sec:498`–`510` ↔ `tbl-settings:231`,`243`–`244`, `E09:317`,`329`–`330` |
| X-5 | **共有 PC のプライバシ注記** | `sec:470`–`472` は「共有 PC ではデータが残存する」と書くが、`file://` では**フォルダを分けても同じ保管庫**なので、同じ PC の**別のローカル HTML ページから読める**（`sec:479`–`491`）。注記の文面が弱い | **確定**（同書内で強い事実が後から追記されている） | `sec:470`–`472` ↔ `sec:479`–`491` |
| X-6 | **C-18 依存パッケージ SCA（`npm audit` Critical/High = 0）** | ビルド時の統制なので CN-1 と矛盾しない。ただし**対象だった DOMPurify は取下げで消えた**ので、SCA の対象は残りの依存だけになる | 有効（対象が縮んだだけ） | `sec:556`, `sec:52` |
| X-7 | **§7 の JWT / E2E / サーバー側権限** | CN-1 では発生しない。**設計予約であり MVP では実装しない**と原典が明記している | 有効（実装対象外） | `sec:514`–`528` |

> X-1 と X-2 は**どちらも「CSP でこれも防げる」と書いた行**である。CSP で本当に効いているのは
> `default-src 'none'` ＋ `script-src` のハッシュ ＋ `connect-src 'none'`（送信 API）＋ `object-src` / `base-uri` / `form-action` であって、
> **frame 埋込と遷移による持ち出しは別の手当てが要る。**

---

## 5. 廃棄・撤回された決定（生きている決定と混ぜない）

| # | 死んでいるもの | 何が死んだか | 出典 |
| --- | --- | --- | --- |
| K-1 | **§3.2 SVG Import サニタイズ** | 対象機能（アイコンの SVG/PNG 取込）が取下げ済み。「もし将来復活させるなら何が要るか」の記録として残すだけ | `sec:29`–`36`, `sec:277`–`279` |
| K-2 | **§3.5 PNG Import** | 同上（マジックバイト・寸法検証・不透明ラスタ扱い） | `sec:376`–`378` |
| K-3 | **AS-1（スクリプト入り SVG アイコン）** | 攻撃シナリオが丸ごと死んでいる | `sec:46`, `sec:225`–`227` |
| K-4 | **C-03 / C-04 / C-09 / C-10** | SVG サニタイズ・SVG 外部 URL 除去・PNG マジックバイト・Import 形式を SVG/PNG に限定 | `sec:49`, `sec:541`–`548` |
| K-5 | **ST-01〜04 / ST-11〜13**（7 件） | 上記に対応するテストケース | `sec:51`, `sec:569`–`581` |
| K-6 | **DOMPurify の採用可否（申し送り 1）** | §3.2 のためだけの検討だったので死んでいる。ライセンス確認も不要 | `sec:52`, `sec:603`–`605` |
| K-7 | **申し送り 4 のうち「SVG サニタイズの実機確認」** | ⚠️ **CSP の実機確認は生きている** | `sec:52`, `sec:609`–`610` |
| K-8 | **略称（`abbrev`）** | `user-order.md` 項 20 で廃止。AS-4 / §3.1 の例 / C-02 / ST-09 の本文が古い | `sec:86`–`88` |
| K-9 | **絵文字アイコン** | `user-order.md` 項 11 は欠番。§3.1 の「1 コードポイントに制限し `textContent`」は宙に浮く | `sec:88`–`89`, `sec:253` |
| K-10 | **§1.1 の Import 対象表・§2.2 の信頼境界の SVG/PNG** | 表と脅威図の `F_SVG` / `F_PNG` ノードと辺が古い。**検証すべき外部入力は JSON と MSPDI XML の 2 つだけ** | `sec:33`, `sec:44`–`45`, `sec:128`, `sec:166`–`180` |
| K-11 | **付録 OWASP 対応表の A03 / A06 / A10 行** | 参照先に死んだ節・コントロールを挙げている | `sec:50`, `sec:54`, `sec:620`, `sec:623`, `sec:627` |
| K-12 | **本文の要求 UID（`IO-L1-*` / `ITEM-L*` / `NFR-L1-*` / `TOOL-L*`）** | 前プロジェクトの仕様書の番号で、その仕様書は引継ぎ資産に入っていない。**各コントロールの内容は自己完結しているので UID は無視してよい**。§8 のトレーサビリティ表は作り替える | `sec:91`–`95` |
| K-13 | **`item59` / `item60` / `item61`（計 9 箇所）** | 旧番号。特に `item60` は本書では共同編集を指すが、**現行の項 60 は localStorage 自動保存とクラッシュ復旧**。共同編集は現行の項 65-1。**読み替えず前後の文で判断する** | `sec:97`–`100` |
| K-14 | **`a11y` の `実装` 欄・`自動検証` 欄** | 前プロジェクトの実装の記録であり、参照先（`src/` / `tests/` / `25-nfr-a11y.sdoc`）は引継ぎ資産に無い。**次期が引き継ぐのは SC の一覧・人手確認欄・残存ギャップの 3 つだけ** | `a11y:36`–`52` |
| K-15 | **「方針あり」は「適合」ではない** | 前プロジェクトの実装は引き継がないので、**50 基準のうち実装として存在するものは 1 つも無い** | `a11y:102`–`106` |
| K-16 | **残存ギャップ #2（ユーザー任意色でラベルが読めない）** | 2026-07-31 の縁取りで解決済み（V-22）。⚠️ ただし Z-4 | `a11y:245`–`250` |
| K-17 | **`sec:19`–`23` の「要レビュー事項」のうち SVG サニタイズ実装** | K-1 に伴って死ぬ。**CSP の実機検証は生きている** | `sec:19`–`23`, `sec:43` |

---

## 6. 未決のまま残っている件

| # | 未決 | 状態 | 出典 |
| --- | --- | --- | --- |
| O-1 | **CSP ハッシュ注入のビルド組込み** | vite-plugin-singlefile 出力への SHA-256 自動注入スクリプトが未実装。`'unsafe-inline'` を残さないこと | `sec:606`–`607` |
| O-2 | **`style-src 'unsafe-inline'` の除去** | 段階的除去をバックログ化（未着手） | `sec:608`, `sec:455`–`458` |
| O-3 | **人間のセキュリティ専門家による CSP の実機確認** | 推奨のまま。SVG サニタイズ分は死んだ | `sec:19`–`23`, `sec:609`–`610` |
| O-4 | **`data:` URI の大きさ** | `file://` で 50 行 / 1000 アイテムの SVG を `<img>` が読めるかを骨格段階で実測する。**SVG 文字列は約 300KB と見積もっているが未計測** | `sec:80`–`82` |
| O-5 | **`file://` の保管庫共有は他エンジン未測定** | Playwright Chromium 1228 の実測のみ。**`1228` は Playwright のビルド番号で Chrome の版ではない**（基準ブラウザは Chrome 150 系） | `sec:492`–`494` |
| O-6 | **§8 トレーサビリティ表の作り替え** | 次期は自分の要求 UID を振り直す | `sec:95` |
| O-7 | **`a11y` の要対応 19 件** | 満たし方が書かれていない残作業。**どれも見える利用者・操作する利用者に直接効く** | `a11y:96`–`100` |
| O-8 | **`fontScale` の 3 値が何 px か** | 未定義。⚠️ 仕様側には表 T-215（`fontScaleSizes.S`/`.M`/`.L`）という受け皿があるが**既定値は未記載**で、保存の可否も未記載 | `a11y:124` ↔ `E09:350` |
| O-9 | **Toast の滞留時間が時間制限（2.2.1）に当たるか** | 当たるなら 無効化 / 調整 / 延長 のいずれかを持つ | `a11y:133` |
| O-10 | **自動更新する表示の全数（2.2.2）** | 自動保存の状態表示が該当し得る | `a11y:134` |
| O-11 | **単文字キー `F` の有効範囲** | 無効化 / 再割当 / フォーカス限定 のいずれかを満たすこと | `a11y:132` |
| O-12 | **`<title>` に開いている日程表の名前を反映させるか** | 単一 HTML なので決める必要がある | `a11y:138` |
| O-13 | **依存線作成のキーボード経路（残存ギャップ 1）** | 現状ポインタのみ。2.1.1 の完全充足には未対応 | `a11y:242`–`244`, `a11y:130` |
| O-14 | **透かし / コメント等オーバレイのコントラスト（残存ギャップ 3）** | `themeHue` / `themeMonochrome` で条件が成立した。**次期で必ず解くこと**。⚠️ ただし透かし分は仕様側で決着済み → Z-6 | `a11y:251`–`256` |
| O-15 | **モノクロ時のコントラスト** | `themeMonochrome` は描画時のフィルタで、彩度を落とすと色相差が明度差に化けない。**指定色と地の明度が近いと 3:1 を割りうる。未検証**（在庫 `E07:183` U-12 も同じ判定） | `a11y:257`–`260` ↔ `E07:183` |
| O-16 | **スイッチ / 音声入力での操作性（残存ギャップ 4）** | 未検証 | `a11y:261` |
| O-17 | **Text Spacing とラベル幅の概算の衝突** | 「実測もキャッシュもしない」という確定事項と衝突する可能性がある。**在庫 `E11:376` が同じ規則を確認している** | `a11y:128` ↔ `E11:376` |
| O-18 | **`documentId`（文書の識別子）の受け皿** | 自動保存キー（`sec:487`）と `Agent API` 有効化の記録（`E09:318`）の**2 つの機能が同じ識別子を要求している**のに、在庫表 11 枚に相当する列は **0 件**（機械検索）。`Project.id` は MSPDI `Project/UID` そのもので **null を取りうる・export しない**ため代用できない | `sec:486`–`488`, `E09:318`, `E11:52`–`62`, `E11:504`（U-1） |
| O-19 | **未知フィールドの保持と汚染防御の両立** | Z-3 の帰結。`__proto__` を捨てつつ知らない鍵を保つ受け皿（Carry 型の passthrough）の設計が要る | `sec:324` ↔ `E09:64` |
| O-20 | **取込上限を「文書の設定値」にするか「製品の定数」にするか** | Z-5 の帰結。文書側に置くと検証される側が自分の上限を運んでくる | `sec:391`–`399` ↔ `E09:346` |

---

## 在庫表との食い違い

| # | 何が食い違うか | 本書側（`sec` / `a11y`） | 在庫表側 | どちらが正か |
| --- | --- | --- | --- | --- |
| **Z-1** | **透かしユーザー名の置き場所** | 「localStorage の保存対象は 日程データ・UI 設定・**透かしユーザー名**」`sec:470` | 「`watermark`（有効 / ユーザー名 / 日時）＝ **どこにも保存しない**」`E11:398` / 「**非保存・`localStorage`**」`E09:316` | **`sec` と `E09` が正。** 文書には保存せず `localStorage` には置く。`E11:398` の「どこにも保存しない」は**文書に**の意味で、`localStorage` を落としている（同じ表の `language` 行は `localStorage` と書き分けている `E11:397`） |
| **Z-2** | **透かし解除パスワードの存在** | §6 は透かしを「証跡マーキング」「表示/非表示を利用者が制御できる」としか書かず、**パスワード機構が 1 行も無い** `sec:498`–`510` | 既定パスワードと SHA-256 が**表 T-207 として確定**し、成果物へ SHA-256 を定数として埋め込む `E09:329`–`330`。利用者が設定した SHA-256 は localStorage `E09:317` | **在庫（仕様書）側が新しい。** `sec` §6 は機能が増える前の記述である。⚠️ CN-1 の下でこの門は**アクセス制御にならない**（X-4） |
| **Z-3** | **未知フィールドの扱い** | 「**未知フィールドは無視**」`sec:324` | 「読み込み時、欠けた設定値は既定値で補い、**知らない鍵は保つ**」（表 T-024a `OP-6`）`E09:64` | **在庫（仕様書）側が正。** `sec` の「無視」は「検証で見ない」の意味に読めるが、**捨てない**ことを明記しないと `OP-6` を破る。汚染防御（`sec:326`–`346`）と両立する passthrough の設計が要る（O-19） |
| **Z-4** | **ラベルの縁取りが 21:1 を固定するか** | 「文字が接する色が縁＝背景色になるので比は **21:1 で固定**され、人が選んだ色に依存しなくなる」`a11y:246`–`250` | `labelHaloOfFont` は**保存する設定値**で「**下限 0 ＝ 縁取りなし**」`E09:201`（値は `tbl-settings:82`＝既定 0.17 / 下限 0 / 上限 0.3） | **在庫側が正。** 21:1 は**既定値でだけ**成立する。文書が `0` を持てる以上、残存ギャップ #2 を「解決済み」と言い切れない（K-16 の但し書き） |
| **Z-5** | **取込サイズの上限値** | 「最大ファイルサイズ **JSON/XML: 20 MB**」`sec:395` | `importMaxBytes` = **32 MB**（`tbl-settings:266`。在庫 `E09:346` が表 T-211 として指す） | **在庫（仕様書）側が新しい。** なお**件数 20,000**（`sec:396` ↔ `tbl-settings:267`）と**深さ 64**（`sec:397` ↔ `tbl-settings:268`）は一致している |
| **Z-6** | **透かしのコントラストを解くかどうか** | 「透かし・コメントのオーバレイ文字は対象外。**次期で必ず解くこと**」`a11y:251`–`256` | `watermarkOpacity` は「**コントラストの適合範囲から外してある**（`NFR-007` / `LM-13`）」`tbl-settings:245`（在庫 `E09:331` / `E09:493` が同行を指す） | **在庫（仕様書）側が新しい。** 透かし分は「解かない」と決着済みで、残るのは **`Comment` / `HighlightBox` の文字**だけ（`E08:219` C-8 が「文字色は原典に無い・未検証」と一致）。⚠️ `NFR-007` / `LM-13` の本文は本作業で読んでいない＝**未検証** |
| **Z-7** | **色値の受理集合** | 「色値は `#RRGGBB` / **既定パレットキー** / `rgb()` の正規表現で検証してから設定する」`sec:250`–`252` | 値域は **`null`（選んでいない）/ `'transparent'` / `'#rrggbb'`** の 3 種 `E07:38`, `E03:73` | **在庫側が正。** 検証関数の受理集合を 3 値へ合わせること。`null` と `'transparent'` は「既定パレットキー」に含まれるとは書かれていない |
| **Z-8** | **未知要素をどうするか** | 「MSPDI → 内部モデルのマッピングは**許可された既知要素のみ**を読み取り、**未知要素は警告として通知**」`sec:359`–`360` | 未知・非ネイティブ要素は**要素まるごと Carry** に温存し、export で原順序どおり書き戻す `E11:441`, `E04:50` | **食い違いというより接続が無い。** 在庫側は「捨てずに往復させる」と決めており、`sec` の「警告」がどの粒度で出るか（Carry へ落ちるたびに出すのか）が未定義。**Drop=0 は Carry が支えている**ので、`sec` の書き方だけを読むと未知要素を落としかねない |
| **Z-9** | **`stack_order` の綴り** | 「所属（行・段）＝`TaskGroupMember`（**`stack_order`** 込み）」`a11y:207` | **要改名 → `stackOrder`**（用語辞書 `N-4` が正）`E11:483` | **在庫側が正**（綴りだけの問題。関係がデータ側にあるという主張自体は `E11:174`–`175` と一致する） |

### 在庫表が「原典に無い」とした点に、本書が答えを持っているもの

| # | 在庫表の未検証 | 本書の答え |
| --- | --- | --- |
| **W-1** | 「**上限を超えたファイルを拒否するのか警告するのかは、本作業で読んだ原典に無い**」`E01:129` | 「上限超過は**取り込まずエラー通知**し、**部分適用しない**（トランザクション的に全か無か）。大きい正当データは分割 Import を案内する」`sec:401`–`403` |
| **W-2** | `Agent API` 有効化の記録は「**文書の識別子と対にして環境側へ置く**」が、識別子そのものの列は在庫に無い `E09:318` | 同じ識別子を**自動保存キー** `grsched.autosave.<documentId>` も要求している `sec:486`–`488`。**2 つの機能が同じ受け皿を待っている**（O-18） |
| **W-3** | `TaskVisual.fillColor` / `strokeColor` の値域は決まっているが、**入力検証の規則**は在庫に無い `E07:38` | 「色値は正規表現で検証してから設定する」`sec:250`–`252`（受理集合は Z-7 で要修正） |
