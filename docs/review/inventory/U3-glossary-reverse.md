# U3 — 用語辞書から逆に見る

**担当**: 逆方向の突き合わせ（辞書 → `03-ui-naming`）。
**日付**: 2026-08-14

---

## 0. 読んだもの と 自分で数えた数

**全文を読んだ。** 300 行ずつ読み、節ごとに書き足した。

| 読んだ文書 | 指示にあった数 | **自分で数えた数** | 備考 |
| --- | --- | --- | --- |
| `docs/spec/_assets/tbl-glossary.md` | 185 行 | **259 行** | ⚠️ **指示の 185 行と食い違う。** `wc -l` で 259。185 は表の行数でもない（下記） |
| `docs/spec/01-04-requirements.md` 1.9 | — | **214〜323 行**（110 行） | 表 T-006 / T-006a / T-006b を含む |
| `docs/spec/01-04-requirements.md` Chapter 3 | — | **413〜894 行**（482 行） | 表 T-010 / T-011 ＋ ユースケース 14 件 |
| `previous-project-result/03-ui-naming/` | — | **3 ファイル・4,091 行** | `ui-parts-ja.md` 741 / `ui-detail-spec-ja.md` 1,556 / `appheader-palette-icons.html` 1,794 |

**辞書の表の行数（自分で数えた）**

| 表 | 何の表か | 行数 |
| --- | --- | --- |
| T-101 | データの語 | **5**（`N-1` 〜 `N-4a`） |
| T-102 | プロパティ | **22**（`P-1` 〜 `P-22`） |
| T-103 | UI パーツ | **41**（`U-1` 〜 `U-40` ＋ `U-15a`） |
| T-104 | 設定値のキー | **105**（`K-1` 〜 `K-106`。**`K-89` は欠番**＝席番号） |
| T-105 | 直訳しない語 | **2**（`X-1` / `X-2`） |
| **合計** | | **175 行** |

**確定名の文字列**は 175 行から **187 個**取れる（1 行が `start` / `finish` のように複数名を持つため）。
以下の「出てこない」「一致」の数は、この **187 個**を母数として機械で数えた。

| 指標 | 数 |
| --- | --- |
| 辞書の確定名（文字列） | **187** |
| うち `03-ui-naming` に 1 回以上現れる | **162** |
| うち `03-ui-naming` に **1 回も現れない** | **25**（**22 行**ぶん） |

**表 T-006b（単独で使ってはならない日本語）の行数**: **15**（`A-1` 〜 `A-15`）。
`03-ui-naming` §2-1-5 の同種の表も **15 行**で、**語の顔ぶれは完全に一致する**（段 / 行 / レベル / 深さ / マーカー / 進捗 / ベースライン / キャンバス / 基準線 / 太さ / 幅 / 期間 / 端点 / バー / カーソル）。**中身は 3 か所で違う**（§2-0）。

**Chapter 3 の数**: アクター **4**（`AC-1` 〜 `AC-4`）／ 表 T-011 **16 行**（`V-1` 〜 `V-13`、枝番 `V-11a` / `V-12a` / `V-13a` を含む）／ ユースケース本体 **14 件**（`UC-001` 〜 `UC-014`）。

---

## 1. 辞書にあるが `03-ui-naming` に出てこない語 —— 全数

**＝仕様書だけが持つ名前。UI 設計はここを「前プロジェクトの資産では裏が取れない」ものとして扱うこと。**

判定はすべて **「辞書に無い」の逆** —— 辞書にはあるが、前プロジェクトの確定名が無い。
表の「用語辞書にあるか」列は**逆方向なので常に ○**である。読むべきは **「出どころ」列**（＝ `03-ui-naming` に無い、の意）。

### 1-1. UI パーツ（6 件・全数）

| UI パーツ名（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| `Status Line` | 基準日線 | **辞書のみ**（03 に 0 箇所） | ○ | `U-11` | **要改名**（03 側） | ⚠️ **最重要。** 03 は `Today Line`（本日線）を持つ（`ui-parts-ja.md` 200 / 568 行ほか計 4 箇所）。辞書は「**『本日線』と呼んではならない（MUST NOT）**」と明記。03 の `Cursors` の子 3 種は（`Today Line` / `Dual Cursor` / `Guide Cursor`）だが、辞書の `Status Line` が `Cursors` の子かどうかは**辞書の並びからは決まらない（未検証。`FR-046` を読む必要がある）** |
| `Annotations` | 注記 | **辞書のみ**（0 箇所） | ○ | `U-15a` | 辞書に無い（03 側に追加が要る） | コメントボックス＋ハイライトボックスの上位語。03 の木では両者は `Canvas Overlays` の直下に並ぶだけで、**まとめる語が無い** |
| `WatermarkPassword` | 透かし解除パスワード | **辞書のみ**（0 箇所） | ○ | `U-37` | 辞書に無い（03 側に追加が要る） | 「合言葉」禁止。既定値と SHA-256 は `tbl-settings.md` 表 T-207 |
| `ArmedShape` | 構え | **辞書のみ**（0 箇所） | ○ | `U-38` | 辞書に無い（03 側に追加が要る） | ⚠️ **日本語「構え」自体は 03 に 12 箇所ある**（`ui-detail-spec-ja.md` 2 ／ `appheader-palette-icons.html` 10）が、**英語の確定名 `ArmedShape` は 1 箇所も無い**。＝概念はあるが名前が無い状態 |
| `Selection` | 選択 | **辞書のみ**（0 箇所） | ○ | `U-39` | 辞書に無い（03 側に追加が要る） | 03 の HTML には「選択」の**振る舞い**の記述はある（545 / 548 行ほか）が、`Selection` という確定名は無い |
| `Marquee` | 範囲選択 | **辞書のみ**（0 箇所） | ○ | `U-40` | 辞書に無い（03 側に追加が要る） | 「範囲選択」という日本語も 03 に **0 箇所**。`UC-007` 拡張 5a と表 T-023a `PD-5` が振る舞いを持つ |

### 1-2. 設定値のキー（16 行 / 19 名・全数）

**すべて `03-ui-naming` §2-1-6（「全数。ここが正」と書いてある表）に無い。**
＝**§2-1-6 の「全数」は、いまの仕様書に対しては全数ではない。**

| 名前（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| `planActualGuideWeight` | 補助線の太さ | 辞書のみ | ○ | `K-91` | 辞書に無い（03 側に追加が要る） | 予実の補助線。03 に群ごと無い |
| `planActualGuidePattern` | 補助線の破線の刻み | 辞書のみ | ○ | `K-92` | 同上 | 同上 |
| `planActualGuideColor` | 補助線の色 | 辞書のみ | ○ | `K-93` | 同上 | 同上 |
| `fadeHandleHalfPx` | フェード掴み点の半辺 | 辞書のみ | ○ | `K-94` | 同上 | 03 はフェードの**掴み位置**（バー左上/右下の角）は持つが、**寸法のキーは無い** |
| `fadeHandleStrokePx` | フェード掴み点の枠線 | 辞書のみ | ○ | `K-95` | 同上 | 同上 |
| `autosaveIdleMs` | 自動保存の操作の切れ目 | 辞書のみ | ○ | `K-96` | 同上 | 03 は `Autosave Status`（パーツ）は持つが、**間隔のキーは無い** |
| `importMaxBytes` | 取り込むファイルの上限 | 辞書のみ | ○ | `K-97` | 同上 | 取込の上限 4 キーはすべて仕様書側の新設 |
| `importMaxItems` | 取り込む `Task` の件数の上限 | 辞書のみ | ○ | `K-98` | 同上 | 同上 |
| `importMaxDepth` | WBS のネストの深さの上限 | 辞書のみ | ○ | `K-99` | 同上 | 03 は「import は上限なし・7 段来れば 7 段で保持」と書いている（`ui-detail-spec-ja.md` 1152 行）。**方針が食い違う可能性がある（未検証 —— `FR-004` と表 T-2xx を読むこと）** |
| `appHeaderMaxHeight` | `App Header` の高さの上限 | 辞書のみ | ○ | `K-100` | 同上 | `App Header` 自体は 03 の木にある |
| `watermarkOpacity` | 透かしの濃さ | 辞書のみ | ○ | `K-101` | 同上 | `Watermark` は 03 の木にある（‼️ 印の 4 件のひとつ） |
| `maxGroupDepth` | `TaskGroup` の深さの上限 | 辞書のみ | ○ | `K-102` | 同上 | ⚠️ 03 は定数の**書き方の例**として `MAX_GROUP_DEPTH` を 1 箇所出すが（記法表の例）、**設定値キーとしては持たない**。面が違う |
| `importMinDate` / `importMaxDate` | 受け入れる日付の範囲 | 辞書のみ | ○ | `K-103` | 同上 | 2 名 |
| `dependencyLagDefault` | 依存を作ったときに置くラグ | 辞書のみ | ○ | `K-104` | 同上 | `UC-004` 手順 3 が使う |
| `fontScaleSizes.S` / `.M` / `.L` | 文字サイズの 3 段の px | 辞書のみ | ○ | `K-105` | 同上 | 3 名。03 は `fontScale` のみで、**3 段の実寸を持たない** |
| `iconHintDelayMs` | アイコンの説明を出すまでの待ち時間 | 辞書のみ | ○ | `K-106` | 同上 | 03 の HTML には「5 秒静止」という**旧規則の記述**があり（1041 行）、**値が違う可能性がある（未検証 —— `tbl-settings.md` を読むこと）** |

### 1-3. 準ゼロ —— 03 に文字列はあるが、確定名としては無い（2 件）

| 名前（英） | 日本語名 | 出どころ | 用語辞書にあるか | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- | --- |
| `Item` | アイテム | 辞書のみ | ○ | `N-4a` | ⚠️ **衝突** | ⚠️ **正面衝突である。** 辞書は `Item` を「**当たり判定と選択の対象の総称**」として**新設**した。一方 03 は `item` / `ScheduleItem` / `itemKind` / `Canvas Items` を**すべて廃止語**と宣言し（`ui-parts-ja.md` 98 / 610 / 638 行）、「**『アイテム』という別語をやめる**」と書いている。**同じ綴りが、片方では確定名・片方では廃止語**。UI 設計に入る前に決着が要る |
| `Agent API` | `Agent API`（訳語を当てない） | 辞書は 5 箇所 | ○ | `U-36` | 辞書に無い（03 側に追加が要る） | 03 は 1 箇所のみ（`ui-parts-ja.md` 596 行）で、しかも「**木に未収録の 4 件**」の表の中。**UI パーツ木には入っていない** |

---

## 2. 表 T-006b が禁じている曖昧語 —— 全 15 語と、`03-ui-naming` の使用状況

### 2-0. まず、2 つの表の差（辞書側 T-006b と 03 側 §2-1-5）

**語の顔ぶれは 15 語で完全一致する。中身が違うのは 3 か所である。**

| 語 | `03-ui-naming` §2-1-5 | 表 T-006b | 差の意味 |
| --- | --- | --- | --- |
| 段 | **4 義**（積み順 / WBS 深さ / `TaskGroup` 深さ / ズームのノッチ） | **6 義**（＋ ⑤目盛の帯の中の段 ⑥取り消しの 1 段） | **T-006b の方が厳しい。** 03 の記述は⑤⑥を許してしまう |
| 端点 | 単独＝**掴み点** | 単独＝**バーの端の掴み点**。⚠️ **フェードの掴み点は別物**（表 T-023d `GR-1`/`GR-2`）と明記 | T-006b が 1 義追加して切り分けた |
| 基準線 | **`Baseline`（変更前の予定）の訳語に限る** | **変更前の予定の訳語に限る** | ⚠️ **`Baseline` という英語名が辞書から消えている**（§3-3 で扱う） |

**太さ**（`A-10`）にも差がある —— 03 は「**既存キーに例外がある**」と逃げ道を書いているが、T-006b は書いていない。

### 2-1. 15 語の全数と、03 での裸使用

「裸」＝ 併記・限定なしで単独に書かれているもの。**行番号は自分で数えた実測値である。**

| 行 ID | 語 | T-006b の要求 | 03 の裸使用 | 判定 | 備考（実測） |
| --- | --- | --- | --- | --- | --- |
| `A-1` | 段 | 6 義。必ず併記。「N 段」と単独で書かない | **31 箇所** | ⚠️ **違反あり** | `ui-detail-spec-ja.md` 1251 / 1258（**「3 段」＝目盛の帯の中の段。T-006b が新設した⑤義そのもの**）、1368 / 1374（「1 段につき 1 頂点」「段の数だけ折れ点」＝積み順）、1499 / 1508（「1 行 1 段」＝積み順）、1145 / 1152（「6 段目」「7 段」＝ WBS と `TaskGroup` が混在）。`ui-parts-ja.md` 351（「行名の 1 段のインデント」）は**辞書の `K-37` にそのまま入っている**（§5 参照） |
| `A-2` | 行 | `Rows` か `TaskGroup` かを明示 | 多数（03 全体で「行」180 箇所。表の「行 ID」等を含む） | **要確認（未検証）** | ⚠️ **機械では切れない。** 「行 ID」「実行」「行う」が同じ字で混ざるため、裸使用の全数は数えていない。**この 1 語だけは目視の突き合わせが要る** |
| `A-3` | レベル | `OutlineLevel` / `TaskGroup` の深さ / LOD を明示 | **1 箇所**（規則の定義そのもの） | **一致** | 03 は規則行以外で「レベル」を使っていない。**最も守られている語** |
| `A-4` | 深さ | WBS か `TaskGroup` かを必ず併記 | **19 箇所中、裸は 13 箇所** | ⚠️ **違反あり** | `ui-detail-spec-ja.md` 1153 / 1157 / 1192 / 1307（2）/ 1314 / 1317 / 1343 / 1345 / 1348 / 1450 / 1501 / 1529。§7（LOD）でまとまって出る |
| `A-5` | マーカー | 単独＝ `Progress Marker`。「マーク」と書かない | **9 箇所**（すべて `Progress Marker` の意） | **一致** | 裸使用は規則が**認めている**読み。⚠️ ただし「**マーク**」は 03 に **6 箇所**あり（`appheader-palette-icons.html` 570 / 572 / 1150 / 1251 / 1260 ほか）、**すべて「ブランドマーク」「更新マーク」で `Progress Marker` とは無関係**。禁止の趣旨（マーカーの略）には触れていないが、UI 設計で新たに使わないこと |
| `A-6` | 進捗 | 3 つが別物。ラベルを指すなら「完了率ラベル」 | **9 箇所中、裸は 5 箇所** | ⚠️ **違反あり** | `ui-detail-spec-ja.md` 270（「担当と進捗を見る」）/ 388（「**進捗の編集入口**」＝見出し）/ 400（「キーボードでの進捗増減」）/ 758（「左: 担当+進捗」）/ 765（「担当/進捗と同時に出さない」）。**5 箇所とも意味は `percentComplete`（完了率）**。UI 設計では「**完了率ラベル**」と書くこと |
| `A-7` | ベースライン | 2 義。①は「変更前の予定」と書く | **6 箇所中、裸は 2 箇所** | ⚠️ **軽微な違反** | `ui-detail-spec-ja.md` 79（`Base` ボタン説明「変更前の予定（ベースライン）」＝**併記あり・可**）、1077（「共有ベースラインへスナップ」＝**裸。①②どちらとも読める**）。`ui-parts-ja.md` 129 は `Baseline` を英語名として使う（§3-3） |
| `A-8` | キャンバス | `Schedule Canvas` / `exportCanvas` / `canvasPadding` を区別 | **19 箇所、ほぼ全部が裸** | ⚠️ **違反あり** | `ui-detail-spec-ja.md` 935 / 941 / 952 / 962 / 982 / 1032 / 1041(2) / 1433 / 1434、`appheader-palette-icons.html` 545 / 548 / 848 / 1004 / 1111 / 1147 / 1365。**意味はすべて `Schedule Canvas`** と読めるが、`ui-detail-spec-ja.md` 1433 の「キャンバスの幅」は寸法式の中なので**危険**（`exportCanvas` と取り違えうる） |
| `A-9` | 基準線 | 変更前の予定の訳語に限る | **1 箇所**（規則の定義そのもの） | **一致** | 03 は規則行以外で使っていない |
| `A-10` | 太さ | `Width` は寸法に使い、太さには使わない | **27 箇所** | ⚠️ **要注意** | 裸の「太さ」が設定値の**日本語名そのもの**になっている（`ui-parts-ja.md` 322 `dependencyWidth` ＝「太さ」）。**この 1 行は辞書 `K-18` にそのまま入り、`K-50` と衝突している**（§3-1） |
| `A-11` | 幅 | 「日付の幅」と「占有幅」を区別 | **126 箇所** | **要確認（未検証）** | ⚠️ 「幅」は「縦幅」「横幅」「画面幅」など多数の複合語に入るため、**裸使用の全数は数えていない**。`ui-detail-spec-ja.md` 286 は「**占有幅**」と正しく書けている |
| `A-12` | 期間 | 予定の期間 / 実績期間 / 表示期間 を明示 | **15 箇所中、裸は 7 箇所** | ⚠️ **違反あり** | `ui-detail-spec-ja.md` 177（「日付または期間を変える」）/ 636 / 651（「ゼロ期間」）/ 1311 / 1314（「代表期間」）/ 1493 / 1516（「期間が長すぎる」）。「代表期間」「ゼロ期間」は**新しい複合語**で、3 分類のどれでもない |
| `A-13` | 端点 | 単独＝バーの端の掴み点。形状名は「端点スパン」4 文字 | **18 箇所中、裸は 8 箇所** | ⚠️ **1 件だけ危険** | `ui-detail-spec-ja.md` 191 / 251(2) / 344 は**掴み点の意**で規則どおり。⚠️ **`ui-parts-ja.md` 366 の `spanDotOfStroke`「端点の点の半径」だけが `endpointSpan`（形状）の意**であり、`A-13` の読みと逆になる。**この 1 行も辞書 `K-47` にそのまま入っている**（§5） |
| `A-14` | バー | `Task Bars` / `Plan Bar` / `Actual Bar` を明示 | **53 箇所が裸** | ⚠️ **最多の違反** | `ui-detail-spec-ja.md` に集中（187〜198 の掴み領域表、251、266、286、293、299、314、315、324、443、444、569、581、589(2)、648 ほか）。「バー左端」「バー本体」「バーより前面」の形。⚠️ **UI 設計で最も危ない語である** —— 掴み領域と重ね順の記述がすべてこの裸の「バー」で書かれており、**予定と実績のどちらの話かが文からは読めない**箇所がある |
| `A-15` | カーソル | 単独＝ `Cursors`（日付を指す線）。マウスは「ポインタ」 | **14 箇所**（すべて `Cursors` の意） | **一致** | 裸使用は規則が**認めている**読み。パレットの群名としても「カーソル / Cursors」（`appheader-palette-icons.html` 771）と併記されている |

**まとめ（自分で数えた）**: 15 語のうち —— **明確な違反 7 語**（`A-1` `A-4` `A-6` `A-8` `A-12` `A-13` `A-14`）、**軽微 1 語**（`A-7`）、**規則が認める裸使用 3 語**（`A-5` `A-15` ＋ `A-10` は要注意）、**一致 2 語**（`A-3` `A-9`）、**未検証 2 語**（`A-2` `A-11` —— 機械で切り分けられないため数えていない）。

### 2-2. ⚠️ Chapter 3（ユースケース）自身が T-006b に反している箇所

**仕様書の中でも起きている。** Chapter 3（413〜894 行）を機械で走査した結果。

| 行 | 本文 | 反する行 ID | どの義か | 備考 |
| --- | --- | --- | --- | --- |
| 638 | 「同じ**段**に複数のタスクがあり、頂点が競合する。」 | `A-1` | ①積み順 | `UC-006` 拡張 2a |
| 640 | 「**段**に頂点が 1 つも無い。」 | `A-1` | ①積み順 | `UC-006` 拡張 2b |
| 641 | 「システムはその**段**で基準日の位置を通す。」 | `A-1` | ①積み順 | `UC-006` 拡張 2b |
| 519 / 520 | 「**深さ**の上限より深い」「**深さ**の上限に達した」 | `A-4` | `TaskGroup` | `UC-002` 拡張 2a。文脈は `TaskGroup` だが併記が無い |
| 472 / 477 | 「**期間**を示し」「その**期間**のタスクを作り」 | `A-12` | 予定の期間 | `UC-001` STATEMENT / 手順 2 |

**さらに、T-006b に無い曖昧語が Chapter 3 に 1 つある。**

| 行 | 本文 | 語 | 判定 | 備考 |
| --- | --- | --- | --- | --- |
| 570 | 「作成者が**パレット**で依存線を構える。」 | パレット | **辞書に無い（T-006b への追加が要る）** | 辞書は `Command Palette`（`U-26`）/ `Palette Groups` / `Palette Commands`（`U-34`）の **3 つ**を持ち、さらに「**`palette` を設定値の名前に使わないこと（MUST NOT）—— 同じ語が別のものを指すことになる**」と自ら警告している。**警告しておきながら、裸の「パレット」を禁止語の表に入れていない** |

---

## 3. 衝突 —— 同じ日本語が 2 つの英語名に / 同じ英語名が 2 つの日本語に

### 3-1. 同じ日本語 → 2 つ以上の英語名（辞書の内部・全数 14 組）

**辞書 175 行を機械で突き合わせた結果。**

| 日本語名 | 割り当てられている英語名（行 ID） | 判定 | 危険度と備考 |
| --- | --- | --- | --- |
| **太さ** | `dependencyWidth`（`K-18`）/ `progressLineWidth`（`K-50`） | ⚠️ **衝突（硬い）** | **最も危険。** 2 つのキーの**日本語名が「太さ」の 1 語だけで完全に同一**。しかも「太さ」は `A-10` で単独使用が禁じられている語である。**プロパティパネルや設定画面に「太さ」というラベルを 2 つ並べることになる。改名が要る** |
| 進捗マーカー | `Progress Marker`（`U-5`）/ `progressMarkerVisible`（`K-78`） | **衝突（面が違う）** | パーツ名と表示切替キーが同じ日本語。**パレットのトグルに「進捗マーカー」と出すと、部品そのものと区別が付かない。**「進捗マーカーを表示」等、動詞を足して逃げる必要がある |
| 担当ラベル | `Assignee Label`（`U-8`）/ `assigneeVisible`（`K-75`） | **衝突（面が違う）** | 同上 |
| イナズマ線 | `Progress Line`（`U-9`）/ `progressLineVisible`（`K-79`）/ `chevron` 例外表（`X-1`） | **衝突（面が違う）** | 同上 |
| デュアルカーソル | `Dual Cursor`（`U-12`）/ `dualCursor`（`K-80`） | **衝突（面が違う）** | 同上 |
| ガイドカーソル | `Guide Cursor`（`U-13`）/ `guideCursorMode`（`K-81`） | **衝突（面が違う）** | 同上 |
| 依存線 | `Dependency Lines`（`U-16`）/ `dependencyVisible`（`K-77`） | **衝突（面が違う）** | 同上 |
| 日付罫線 | `Date Grid Lines`（`U-17`）/ `dateGridLinesVisible`（`K-82`） | **衝突（面が違う）** | 同上 |
| グループ罫線 | `Group Grid Lines`（`U-18`）/ `groupGridLinesVisible`（`K-83`） | **衝突（面が違う）** | 同上 |
| 矩形 | `'rectangle'`（`P-11`）/ `shapeHeightOf.rectangle`（`K-13`） | **衝突（軽微）** | 形状の**値**と、その形状の**縦幅の設定** が同じ日本語。`K-13` は「群」列に「形状の縦幅」があるので表の中では読めるが、**ラベルだけ抜き出すと同じ** |
| 矢羽根 | `'chevron'`（`P-12`）/ `shapeHeightOf.chevron`（`K-14`）/ `chevron`（`X-2`） | **衝突（軽微）** | 同上 |
| 矢印 | `'arrow'`（`P-13`）/ `shapeHeightOf.arrow`（`K-15`） | **衝突（軽微）** | 同上 |
| 端点スパン | `'endpointSpan'`（`P-14`）/ `shapeHeightOf.endpointSpan`（`K-16`） | **衝突（軽微）** | 同上 |
| マイルストーン | `'milestone'`（`P-15`）/ `shapeHeightOf.milestone`（`K-17`） | **衝突（軽微）** | 同上 |

> **「面が違う」8 組は矛盾ではない**（記法は表 T-006a）。ただし **UI 設計は日本語ラベルを画面に出す仕事なので、面の違いが消える。** 表示切替のラベルは**部品名そのままにしない**、という方針決めが要る。

### 3-2. 同じ英語名 → 2 つの日本語（辞書の内部）

**辞書の中には 0 件である。**（175 行を機械で突き合わせて確認した。）

### 3-3. 辞書 ↔ `03-ui-naming` をまたぐ衝突（4 件）

| 名前（英） | 辞書の日本語 | 03 の日本語 | 辞書の行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- | --- |
| `Task` | タスク（**`shapeKind` が `'milestone'` のとき**マイルストーンと表示。⚠️ **真偽値の `milestone` という列は持たない**） | タスク（**`milestone = true`** は「マイルストーン」と表示） | `N-1` | ⚠️ **衝突（判別の仕組みが逆）** | **日本語は同じだが、中身が正面から食い違う。** 03 は「`itemKind` という判別値をやめ**真偽値**にする」「MSPDI と同じ表現」と 2 か所で書いている（`ui-parts-ja.md` 100 / 638 行）。辞書は真偽値列を**禁止**した。**03 の `Task.milestone`(bool) は廃止扱いにすること** |
| `Today Line` / `Status Line` | 基準日線（`Status Line`） | 本日線（`Today Line`） | `U-11` | **要改名**（03 側） | 辞書が「本日線と呼んではならない（MUST NOT）」と明記。03 は `Cursors` の子として `Today Line` を数えているので、**カーソル 3 種の顔ぶれが変わる可能性がある（未検証）** |
| `Baseline` | **（辞書に無い）** | ベースライン＝変更前の予定 | — | **辞書に無い** | 03 は `Baseline` を英語の確定名として使う（`ui-parts-ja.md` 129 / 260 行、`ui-detail-spec-ja.md` 79 行の `Base` ボタン）。辞書に `Baseline` の行は無く、あるのは `baselineVisible`（`K-84`）だけ。**T-006b `A-9` は `Baseline` への言及を落としている。**「変更前の予定」を指す英語名を持つのか持たないのか、**決着していない** |
| 基準日 | `Project.status_date`（辞書本文・`U-11` と §4 の規約） | `statusDate` 🅿（`ui-parts-ja.md` 376 行。**PoC 専用キー**と注記） | `U-11` ほか | **綴りが違う ＋ 要改名** | **同じ「基準日」に 2 つの綴り。** しかも 03 の `statusDate` は**偶然、2026-08-14 の決定（lowerCamelCase）と一致している** —— ただし 03 はそれを「PoC 専用のキー」と位置づけ、製品側は `Project.status_date` だと書いている。**§4-6 参照** |

---

## 4. 直近で決まった名前（2026-08-14）が辞書に入っているか

**6 件すべてを機械で数えた。**

| 決まった名前 | 辞書での出現 | 用語辞書にあるか | 辞書の行 ID | 判定 | 何をすべきか |
| --- | --- | --- | --- | --- | --- |
| **`GoodRelax Scheduler` / `GRS`** | **`GRS` 0 箇所 / `GoodRelax` 0 箇所** | **✕** | — | **辞書に無い（追加が要る）＋ 03 側は要改名** | ⚠️ **03 の `Branding` は `GR Scheduler`（GRS ではなく GR、空白あり）と `© 2026 GoodRelax` の 2 行**（`appheader-palette-icons.html` 422 / 579 / 586 / 594 / 620 / 1245 / 1264 行）。**画面に出る文字列なので UI 設計が直接踏む。** 03 の HTML には「`GRScheduler` ではなく `GR Scheduler` にした」という**判断の記録**まであり、それごと差し替えになる。散文の `GRS` は 03 に 2 箇所（`ui-detail-spec-ja.md` 1156 / `ui-parts-ja.md` 622）あり、**略語だけは既に使われている** |
| **`CommentBox`** | **0 箇所**（あるのは `Comment Boxes`） | **✕**（別綴りならあり） | `U-14`（`Comment Boxes`） | **綴りが違う（追加が要る）** | 辞書は **UI パーツ名**（`W-6`＝空白あり複合語・複数形）しか持たない。**型名（`W-1` PascalCase 1 語）`CommentBox` の行が無い。**「コメント」と略さない規則は `U-14` が持っている |
| **`HighlightBox`** | **0 箇所**（あるのは `Highlight Boxes`） | **✕**（別綴りならあり） | `U-15`（`Highlight Boxes`） | **綴りが違う（追加が要る）** | 同上。「囲み枠」禁止は `U-15` が持つ |
| **`commentBoxes` / `highlightBoxes`（JSON の鍵）** | **どちらも 0 箇所** | **✕** | — | **辞書に無い（追加が要る）** | 記法は `W-2`（camelCase）。**辞書の T-104 は `documentSettings` のキーしか持たず、文書本体の JSON の鍵を置く表が無い**（＝置き場所から決める必要がある。**未検証**） |
| **`wbsParentUid`** | **0 箇所**（あるのは `wbs_parent_uid`） | **✕**（旧綴りならあり） | `P-22` | ⚠️ **要改名** | 辞書 `P-22` が `wbs_parent_uid`。**表 T-006a の `W-8`（snake_case 例外 3 語）が根拠になっている**ので、`W-8` ごと廃止しないと辞書だけ直しても食い違う。03 側は `wbs_parent_uid` が **4 箇所** |
| **`linkType`** | **0 箇所**（`link_type` も辞書には 0 箇所） | **✕** | — | **辞書に無い（追加が要る）** | ⚠️ **`link_type` は辞書に 1 行も無い。** 出てくるのは要求 1.9 の表 T-006a `W-8` の**例示**としてだけである。03 側は `link_type` が **3 箇所**。**依存の種別は `UC-004` の中核なのに、確定名が辞書に無い** |
| **`statusDate`** | **0 箇所**（あるのは `Project.status_date` が 2 箇所） | **✕**（旧綴りならあり） | `U-11` ＋ §4 の規約 | ⚠️ **要改名** | 辞書は 2 か所で `Project.status_date` と書く（`U-11` の本文、および「キーに関する規約」の 4 つ目）。**`W-8` の廃止に連動する** |
| **（参考）MSPDI の綴りの封じ込め** | 辞書に `MSPDI` **1 箇所**（`U-27` の本文「MSPDI の `Project/Title` に対応する」）。`Carry` **0 箇所** / `carryElements` **0 箇所** | **✕** | — | **辞書に無い（追加が要る）** | 新しい規則は「**MSPDI の綴りが現れてよいのは `carry` / `carryElements` の内側だけ**」。⚠️ **その `carry` / `carryElements` 自身が辞書に 1 行も無い**（`Carry` は要求 1.8 の表 T-005 `G-13` にあるだけ）。**規則が指す先が辞書に存在しない状態。** `U-27` の MSPDI 言及が識別子でなく散文なので違反かどうかは **未検証** |

---

## 5. ⚠️ 副産物 —— 辞書自身が表 T-006b に反している箇所

**逆方向に見たので見つかった。** 03 の裸使用の一部は、**そのまま辞書に写っている**。

| 辞書の行 ID | 日本語名 | 反する行 ID | 判定 | 備考 |
| --- | --- | --- | --- | --- |
| `K-11` `stackGap` | 「**段**の間隔」 | `A-1` | ⚠️ **違反** | 群は「縦の寸法」。①積み順の意だが併記が無い |
| `K-37` `rowTitleIndent` | 「行名の **1 段**のインデント」 | `A-1` | ⚠️ **違反** | 「**N 段と単独で書かない**」に真っ向から反する。意味は `TaskGroup` の深さ |
| `K-105` `fontScaleSizes.*` | 「文字サイズの **3 段**の px」 | `A-1` | ⚠️ **違反 ＋ 義が足りない** | **`A-1` の 6 義（積み順 / WBS 深さ / `TaskGroup` 深さ / ズームのノッチ / 目盛の帯の段 / 取り消しの 1 段）のどれでもない。「文字サイズの段」という第 7 の義が、辞書自身の中にある。** `A-1` は「6 義ある」と全数を主張しているので、**その主張が崩れている** |
| `K-18` `dependencyWidth` | 「**太さ**」 | `A-10` ＋ 衝突 | ⚠️ **違反** | §3-1 参照。`K-50` と同名 |
| `K-50` `progressLineWidth` | 「**太さ**」 | `A-10` ＋ 衝突 | ⚠️ **違反** | 同上 |
| `K-47` `spanDotOfStroke` | 「**端点**の点の半径 ÷ 線の太さ」 | `A-13` | ⚠️ **違反（読みが逆になる）** | `A-13` は「単独＝バーの端の掴み点」と定めるが、ここは `endpointSpan`（形状）の端点。**規則どおりに読むと意味を取り違える** |
| `K-65` `canvasPadding` | 「**キャンバス**の余白」 | `A-8` | 軽微 | `A-8` が 3 義の 1 つとして「余白（`canvasPadding`）」を挙げているので、辿れば読める |
| `K-12` `rowGap` / `K-36` `rowTitleFont` / `K-70` `scrollGroupId` | 「**行**の間隔」「**行**名の文字」「表示の上端が指す**行**」 | `A-2` | **要確認（未検証）** | `Rows` か `TaskGroup` かの併記が無い。`K-70` は `scrollGroupId` という名前が `TaskGroup` を指すので、名前側では読める |
| `K-49` `minShapeWidth` | 「ゼロ**期間**でも残す最小幅」 | `A-12` | 軽微 | 3 分類（予定の期間 / 実績期間 / 表示期間）のどれでもない新しい複合語 |
| `K-55` `taskLevelOfDetailReadablePx` | 「この**幅**を割った**深さ**は描かない」 | `A-4` ＋ `A-11` | ⚠️ **違反** | 深さの WBS/`TaskGroup` 併記が無い。キー名の `task...` から WBS と読めるが、日本語だけでは読めない |

---

## 6. まとめ —— UI 設計に入る前に片付ける「要改名」

**印を付けた順に並べた。上ほど、UI 設計が最初の 1 時間で踏む。**

| # | いま | すべきこと | 根拠 | 誰の問題か |
| --- | --- | --- | --- | --- |
| 1 | `Branding` が **`GR Scheduler`** | **`GoodRelax Scheduler`** へ | 2026-08-14 の決定 | **画面文字列**。03 の HTML 7 箇所 |
| 2 | 03 の **`Today Line` / 本日線** | **`Status Line` / 基準日線** へ | `U-11`（MUST NOT） | UI パーツ木・カーソル群 |
| 3 | 裸の「**バー**」53 箇所 | `Plan Bar` / `Actual Bar` / `Task Bars` を明示 | `A-14` | 掴み領域と重ね順の記述**全体** |
| 4 | 裸の「**進捗**」5 箇所（意味は完了率） | 「**完了率ラベル**」へ | `A-6` | ラベルとトグル |
| 5 | 「**太さ**」が 2 つのキーの同名ラベル | どちらかを改名 | `K-18` / `K-50` の衝突 ＋ `A-10` | **設定画面のラベル** |
| 6 | `wbs_parent_uid` / `link_type` / `Project.status_date` | `wbsParentUid` / `linkType` / `Project.statusDate` | 2026-08-14 の決定 | 表 T-006a `W-8` の廃止に連動 |
| 7 | `Comment Boxes` / `Highlight Boxes` のみ | 型名 `CommentBox` / `HighlightBox` と JSON 鍵 `commentBoxes` / `highlightBoxes` を辞書へ | 2026-08-14 の決定 | 辞書に**行が無い** |
| 8 | `Item`（辞書は確定名／03 は廃止語） | **どちらが勝つかを決める** | `N-4a` vs `ui-parts-ja.md` 98 / 610 / 638 | 選択・当たり判定の設計に直結 |
| 9 | `Task.milestone`(bool)（03） | 廃止し `shapeKind === 'milestone'` へ | `N-1`（⚠️ 真偽値の列は持たない） | データモデルと描画の分岐 |
| 10 | 裸の「**パレット**」（`UC-004` 手順 1） | `Command Palette` を明示 ＋ **T-006b への追加を検討** | 辞書 §4 の規約が自ら警告 | 仕様書側 |
| 11 | `Baseline` の英語名 | 持つのか持たないのかを決める | `A-9` が言及を落とした | 03 の `Base` ボタン |
| 12 | `carry` / `carryElements` が辞書に無い | 追加する | 「MSPDI の綴りは carry の内側だけ」の規則が指す先 | 規則が空を指している |

---

## 7. 未検証 —— 断定していないこと

以下は**確かめられなかった**。断定していない。

1. **`A-2`（行）と `A-11`（幅）の裸使用の全数**。「行 ID」「実行」「縦幅」など同じ字を含む語が大量にあり、**機械で切り分けられなかった**。数えていないので数を書いていない。**目視の突き合わせが別途要る。**
2. **`Status Line` が `Cursors`（`U-10`）の子かどうか。** 辞書の表の並びでは `U-10` の直後に `U-11` が来るが、**辞書は親子関係を書いていない**。`FR-046` と表 T-023c を読む必要がある。
3. **`importMaxDepth`（`K-99`）と 03 の「import は上限なし」の食い違い**が、本当の方針変更なのか、対象が違うだけなのか。`FR-004` と設定値の表を読む必要がある。
4. **`iconHintDelayMs`（`K-106`）の値**が 03 の「5 秒静止」と同じかどうか。**辞書は名前しか持たない**（値は `tbl-settings.md`）ので、この文書だけでは決まらない。
5. **`U-27`（`Schedule Title`）の本文にある `MSPDI` の 1 語**が、「MSPDI の綴りは `carry` / `carryElements` の内側だけ」という規則に反するかどうか。**識別子ではなく散文の説明**なので、規則の射程が識別子だけなら反しない。**規則の射程が書かれていない。**
6. **JSON の鍵 `commentBoxes` / `highlightBoxes` の置き場所。** 辞書 T-104 は `documentSettings` のキーの表であり、**文書本体の JSON の鍵を置く表が辞書に無い**。新しい表が要るのか、T-104 を広げるのかは決まっていない。
7. **`03-ui-naming` の HTML（1,794 行）**は、名前の抽出のためにのみ走査した。**コードも HTML も写していない。**

---

## 8. 数え方（再現できるように）

- 辞書の 175 行は、`| <行 ID> |` で始まる行を `[NPUKX]-\d+[a-z]?` で数えた。
- 確定名 187 個は、各行の「確定名（英）」列（`K-` 行は「群」列があるので 3 列目）から `` `…` `` を取り出して数えた。
- 「03 に出てこない」は、`03-ui-naming/` の 3 ファイルを連結した文字列に対する**部分一致の出現回数が 0** で判定した。**部分一致なので甘い側に振れている** —— つまり **25 個という数は下限**である（`Item` のように、部分一致で拾えても確定名としては存在しないものがある）。
- 曖昧語の裸使用は、直前の限定語を否定先読みで除いた正規表現で数えた（例: 「バー」から「予定バー」「実績バー」「タスクバー」「スクロールバー」「マルチバー」「ツールバー」「サイドバー」を除く）。**`A-2`（行）と `A-11`（幅）はこの方法が効かないので数えていない。**
