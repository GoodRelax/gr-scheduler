# X2 — 辞書・設定値・設計 の検討経緯 全数判定（CR-114）

**判定基準**: `change-request/CR-114-remove-deliberation-history-from-spec.md`。
**判定は 1 つ** —— 「この文を消したら、仕様か、それを採る理由が失われるか。」失われないなら **除く**。

⚠️ **本書は編集の指示書である。`docs/spec/` は 1 バイトも編集していない（読むだけ）。**
⚠️ **行番号は書かない。** 作業中に他の作業者の編集で `tbl-settings.md` の行番号が 2 度ずれたのを実測した（S-99d が 234 → 236 行）。
⚠️ **置換前の文字列は、判定時点で各ファイル内に 1 回だけ一致することを機械照合済み**（33 件すべて `count=1`）。
**同時編集が続いているので、適用の直前にもう一度リテラル照合すること（MUST）。**

## セルの記法（表を壊さないための退避。適用時は必ず戻すこと）

| 表記 | 実際の文字 |
|---|---|
| `⏎` | 改行 1 個（LF） |
| `\|` | 縦棒 `|` |

**改行や縦棒を含む 4 件（G2 / S5 / S14 / S18）は、末尾の付録にコードブロックで原文のまま置いた。そちらから複写すること。**

## 数（自分で数えた）

| 対象ファイル | 調べた箇所 | 除く | 残す | 要判断 |
|---|--:|--:|--:|--:|
| `_assets/tbl-glossary.md` | 7 | 4 | 3 | 0 |
| `_assets/tbl-settings.md` | 33 | 10 | 22 | 1 |
| `05-07-design.md` | 7 | 0 | 7 | 0 |
| **合計** | **47** | **14** | **32** | **1** |

**⚠️ 設計（`05-07-design.md`）は除く対象が 1 件も無い。** 前プロジェクトへの言及 3 件はすべて理由として機能しており、CR-114 §5-2 が「残す実例」として名指ししているものを含む。

**⚠️ `tbl-settings.md` の 🔎（由来の記録）は 1 件も除いていない。** 1.9 が「例外は 2 つ —— 範囲の理由と、由来の記録」と明記しているためである。

---

## 1. `docs/spec/_assets/tbl-glossary.md`

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| G1 | 残す | `由来は前プロジェクトの命名確定版（`previous-project-result/03-ui-naming/ui-parts-ja.md` §2-1）である。` | （変更なし） | **この語彙を採る理由**そのもの（前プロジェクトで確定した命名を正として引く）。CR-114 の「前プロジェクトの事実が理由として機能している場合」に当たる。⚠️ 除くと 🔎 と同種の「由来」の指し先が本書から消える |
| G2 | 除く | `§2-1）である。⏎**語は 1 つも落とさずに引き継いだ。** 一方、そこに併記されていた**旧名と改名の経緯は引き継がない** —— 旧語彙を本プロジェクトへ流入させないためである。経緯が要るときは由来の文書を読む。` | `§2-1）である。` | 引き継ぎ**作業**の経緯。「1 つも落とさず」は表題「確定名の全数」と「本書が用語の正である」が既に持つ。「旧名と改名の経緯は引き継がない」は編集の方針であり、CR-114 が 1.9 に新設する規約と重なる。仕様も採用理由も失われない |
| G3 | 除く | `` `FR-029` に触れる（確定 2026-08-13） `` | `` `FR-029` に触れる `` | **いつ決めたか**。規則（入口を指すときは「行見出しパネル」と書く）と理由（入口を 2 つの語で呼ぶと `FR-029` に触れる）は残る。CR-114 §4 が除く実例として名指ししている行 |
| G4 | 除く | `> **`Agent API` は 2026-08-12 に確定した呼び名である**（ユーザー判断）。**日本語でも `Agent API` と書く。**` | `> **呼び名は `Agent API` とする。日本語でも `Agent API` と書く。**` | 「2026-08-12」「ユーザー判断」は**いつ誰が決めたか**。呼び名の規定と「日本語でも同じ語を使う」は残す。太字を 1 続きにまとめたのは、CJK に隣接する `**` の境目を増やさないため |
| G5 | 残す | `**`AI API` を採らない理由**`（同段落の全文） | （変更なし） | 却下した案の**理由**に加えて「同じ理由で再提案しないこと」という**いま効いている規則**を持つ。Chapter 2.4（表 T-009 の `XO-4`）との矛盾という理由は、消すと復元できない |
| G6 | 除く | `> **公開する識別子は `grSchedulerAgentApi` とする —— 確定 2026-08-12（ユーザー判断）。**` | `> **公開する識別子は `grSchedulerAgentApi` とする。**` | **いつ誰が決めたか**。識別子の規定と、次行以降の理由（他ツールの同種 API と衝突しない／概念と識別子は面が違うだけ）は残る |
| G7 | 残す | `要望の入力が「矢羽根」を使っており` | （変更なし） | 直訳しない例外（`X-2`）を**採る理由**そのもの。消すと「なぜ矢羽根なのか」が失われ、例外表の役目（増やすときは理由を書く）が崩れる |

**この 3 件は経緯に見えるが経緯ではないので、表に載せずに残した** —— `N-4a`（`Item` は `Task` の別名ではない）・`U-37`（「合言葉」と呼んではならない）・表 T-105 の各「例外にする理由」。いずれも規則とその理由であり、いつ誰が決めたかを書いていない。

---

## 2. `docs/spec/_assets/tbl-settings.md`（前半 —— §1 〜 §5）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| S1 | 除く | `（`previous-project-result/02-data-model/grs-document-settings-ja.md`）である。**項目は 1 つも落とさずに引き継いだ。**` | `（`previous-project-result/02-data-model/grs-document-settings-ja.md`）である。` | 引き継ぎ**作業**の経緯。全数であることは表題「既定値と範囲の全数」が持つ。⚠️ **前半の「由来は…である」は残す** —— §2 の 🔎（由来の記録）が指す先であり、消すと 🔎 の「由来」が何を指すか読めなくなる |
| S2 | 残す | `**🔎 を付けた既定値は、由来が記録されていない。** 測って決めた値ではなく、実装時に置いて動いたのでそのまま残った値である。` | （変更なし） | 🔎 の定義そのもの。1.9 が例外として認める**由来の記録**。「実装時に置いて動いたので残った」は、前プロジェクトの事実が「選び直してよい」の理由として機能している |
| S3 | 残す | `印が要る理由は、全項目を書き出す方針の帰結である。` | （変更なし） | **制約から導かれる帰結**。なぜ印が要るかの理由であり、経緯ではない |
| S4 | 除く | `**⛔ は文書に保存しない**（表 T-206）。**🅿 は前プロジェクトの PoC 専用**で、本製品の `documentSettings` には入らない。` | `**⛔ は文書に保存しない**（表 T-206）。` | 🅿 の凡例。⚠️ **S5 と対で適用すること（MUST）** —— 🅿 が付いた行は `S-52` の 1 行だけで（grep 実測）、その行を除くと凡例だけが宙に浮く |
| S5 | 除く | `\| S-51 \| イナズマ線 \| `progressLineOverhang` \| px \| 6 🔎 \| 0 \| 40 \| — \|⏎\| S-52 \| イナズマ線 \| `statusDate`（PoC 名）🅿 \| 日 \| 700 \| 0 \| 1095 \| 前プロジェクトの PoC 専用。**本製品では設定値ではない**（`FR-046`） \|` | `\| S-51 \| イナズマ線 \| `progressLineOverhang` \| px \| 6 🔎 \| 0 \| 40 \| — \|`（＝ `S-52` の行を丸ごと削除） | **前プロジェクトの PoC 専用の値**であり、行自身が「本製品では設定値ではない」と述べている。本製品の仕様を 1 つも定めていない。基準日が設定値でない規則は `FR-046` と辞書の「基準日の名前は `Project.statusDate`」が持つ。**`docs/spec/` 内から `S-52` と 🅿 への参照は無い**（grep 実測: 本行と凡例のみ）。表 T-201 の行数を数える散文も無いので検査 9 に触れない |
| S6 | 残す | `既定値 1.5 と `fontOfActual` 0.80 の組は 1.2 倍で破っていた` | （変更なし） | 「範囲の理由」列にある**上限そのものの根拠**（1.9 の例外）。上限 `1 ÷ fontOfActual` がなぜ要るかを示す実例で、消すと上限の根拠が失われる |
| S7 | 残す | `**由来は寸法統合前の 1 ページが描いていた 22 : 16 であり、設計上の導出ではない**（下記）` | （変更なし） | 🔎 付き `S-5` の**由来の記録**。1.9 の例外。表 T-026 の `RC-2` が本記述を名指しで参照している |
| S8 | 残す | `> 数値が一致する唯一の記述は「寸法を 1 か所へ統合する前に、ページごとに別々の値を持っていたときの実測 22 : 16」である。` | （変更なし） | 同上（`S-5` の由来の記録の本文）。「選び直してよい」の根拠であり、消すと 0.73 が測った値のように見える |
| S9 | 残す | `実測は、前プロジェクトの PoC で `zoomX = 0.25` のとき段数 96 → 72（24 段ぶん減る）というものである。` | （変更なし） | 既定を `false` にする**理由として機能している実測**。前プロジェクトの事実が理由になっている側 |
| S10 | **要判断** | ` 前プロジェクトの JSON 実例にある `210` に根拠は無い。`（先頭の半角空白まで含めて一致） | （除く場合）当該 1 文を削除し、直前の「**この色相だけがコントラスト検証を通っている。**」で止める | **除く側の理由**: `210` は本書のどこにも無い値で（本ファイル内の「210」は本記述と表 T-210 の表番号だけ。grep 実測 2 件）、初見の読者は**存在しない値を探しに行く** —— CR-114 §2 が名指しする失敗の型そのもの。**残す側の理由**: 「214 であって 210 ではない」と**再提案を止める**働きがあり、`G5`（`AI API` を採らない理由）と同型。⚠️ 214 が実測値であることは前段が単独で言えている |
| S11 | 残す | `前プロジェクトのコントラスト検査は色を指定していない側しか測っていない。` | （変更なし） | 「モノクロ時が未検証である」という**現在の状態の理由**。`NFR-007` に何を測らせるかの前提であり、CR-114 §5-2 の Chapter 7 と同型 |

## 2 続き（§7 〜 §9）

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| S12 | 除く | `（確定 2026-08-12。**値は 2026-08-13 に `256` から `255` へ。「256 は中途半端」という利用者判断であり、測って決めた値ではない**）` | `（**測って決めた値ではない**）` | **いつ誰が決めたか**と**前はこの値だった**。⚠️ **「測って決めた値ではない」は由来の記録なので書き残す**（1.9 の例外）。数え方と到達時の挙動は `FR-003` の `ST-7` が持ち、行の後半がそれを指している |
| S13 | 除く | `同上。⚠️ **旧称「ダミーの実績線」は用語辞書に無い語だったので改めた**（1.9 の命名の規約。2026-08-13）` | `同上。` | **旧名と改名の経緯 ＋ いつ決めたか**。改称後の名前は表 T-023d の `GR-9` が持ち、辞書に無い語を使わない規約は 1.9 が持つ。仕様も理由も失われない |
| S14 | 除く | `切り替え対象ではない** \|⏎\| S-99d \| ~~本日線の表示状態~~ \| — \| **廃止（2026-08-13）。** 基準日線は `Project.statusDate` が `null` かどうかで描画が決まり、表示状態という値を持たない（`FR-046`） \|` | `切り替え対象ではない** \|`（＝ `S-99d` の行を丸ごと削除） | 取り消し線つきの**墓標の行**。「前はこう書いてあった」の典型。表示状態という値を持たない規則は `FR-046` が、「本日線」と呼ばない規約は辞書 `U-11` が持つ。`docs/spec/` 内から `S-99d` への参照は無い（grep 実測）。⚠️ **`change-request/CR-000-impact-and-order.md` が CR-102 の影響先として `S-99d` を挙げている。CR-102 を適用した後に実施すること** |
| S15 | 除く | `⚠️ **要望 55-3 は `GoodRelax` を指定していたが、汎用の道具に固有名を既定値として置かないため製品名に変更した**（確定 2026-08-13）` | `⚠️ **汎用の道具に固有名を既定値として置かないため、既定値は製品名とする。**` | 「要望はこうだったが変更した」は**前はこう書いてあった**、「確定 2026-08-13」は**いつ決めたか**。⚠️ **理由（汎用の道具に固有名を置かない）は書き直して残す** —— これは既定値 `gr-scheduler` を採る理由そのものである |
| S16 | 除く | `（`FR-092` の `EZ-2`）。既定 3 秒は利用者の指定（2026-08-13）` | `（`FR-092` の `EZ-2`）` | **誰がいつ決めたか**。3000 に測った由来が無いことは行頭の 🔎 が既に持っているので、由来の記録は失われない |
| S17 | 残す | `**PoC の `fontMin` = 12 をそのまま S に据えた。**` | （変更なし） | 🔎 付き `S-121` の**由来の記録**（この値がどこから来たか）。1.9 の例外。⚠️ 「下限を割らない」だけを残すと、12 が測って決めた値のように読める |
| S18 | 除く | `判定の仕方は表 T-205 の注が持つ。**⏎⚠️ **本表はかつて「しきい値を実効フォントサイズの関数として持て」と指示しており、表 T-205 の「固定値であり、実行時に導出してはならない（MUST NOT）」と相互否定になっていた。表 T-205 側に寄せ、本表の指示は廃した**（2026-08-13）。` | `判定の仕方は表 T-205 の注が持つ。**` | **前はこう書いてあった ＋ いつ直したか**。いま効く規則（固定値であること・判定の仕方）は表 T-205 側にあり、直前の行がそこを指している。矛盾はもう存在しないので、矛盾していた記録は仕様でも理由でもない |
| S19 | 除く | `**単位を定めないと、同じ値で実装ごとに違う日数になる**（`FR-047` が踏んだのと同じ誤り）` | `**単位を定めないと、同じ値で実装ごとに違う日数になる**` | **過去に踏んだ誤りの記録**。単位を稼働日に揃える理由は直前の文が単独で述べており、括弧を消しても失われない。⚠️ 参照先 `FR-047` の `RATIONALE` にある「起点と単位を定めていなかったので…違う日数が出た」も同じ型の経緯であり、要求側の判定次第でこの参照は宙に浮く |
| S20 | 残す | `公開実装は `S = 10` を持つが` | （変更なし） | **却下した値とその理由**（`S-8` の下限 12 と `FR-077` に反する）。いま効いている禁止であり、消すと 10 が再提案される |
| S21 | 残す | `**由来が実測でなく、骨格で測って選び直す対象である**` | （変更なし） | §9 という括りの**定義**。表 T-026 の `RC-1` が指す先でもある |
| S22 | 残す | `**値の由来は無い** —— 動いている端末の予算から選び直す` | （変更なし） | 🔎 付き `S-95` の**由来の記録**（1.9 の例外） |
| S-GRP | 残す | 🔎 付きの「由来は無い」「記録は無い」注 —— **11 件**（`S-1` / `S-19` / `S-25` / `S-26` / `S-33` / `S-36` / `S-38` / `S-41` / `S-44` / `S-54` / `S-55`。代表: `10px の由来は無い`） | （変更なし） | **1.9 が明記する例外**「その値の由来の記録（🔎 を付けた注）」。値についての記述であって経緯ではない。**除いてはならない（MUST NOT）**。表 T-026 の `RC-1` が全件を「残す。骨格で測って選び直す」と決めている |

---

## 3. `docs/spec/05-07-design.md`

**除く対象は 1 件も無い。** 前プロジェクトへの言及は 3 件あるが、いずれも**理由として機能している**か、未記入節（器）が出どころを指しているだけである。

| # | 判定 | 置換前（リテラル・一意） | 置換後（リテラル） | 理由 |
|---|---|---|---|---|
| D1 | 残す | `前プロジェクトの記録番号は引き継がない（`previous-project-result/README.md` §0-4）。` | （変更なし） | **採番の規則**そのもの（なぜ ADR-001 から採るのか）。消すと、前の版の番号を続けてよいのかが決まらなくなる |
| D2 | 残す | `**ゲートを先に置く理由は、前プロジェクトがここで落ちたからである。**`（同段落の全文） | （変更なし） | **CR-114 §5-2 が「残す実例」として名指ししている段落。** 「なぜゲートを工程に縛るのか」の答えそのもの。⚠️ 続く「測る動機は、合否が工程を止めるときにしか続かない。」まで 1 つの理由なので分割しない |
| D3 | 残す | `⚠️ **`PG-8` 〜 `PG-12` は、前プロジェクトが「測る」と決めて測らずに終わった 5 つである**（同 §2-5）。` | （変更なし） | **なぜこの 5 つだけ担当と期限を先に決めるのか**の理由。前プロジェクトの事実が理由として機能している側で、`D2` と同じ型 |
| D4 | 残す | 未記入節（器）が出どころとして指す先 —— **4 件**（5.1 の `09-architecture/architecture-layering-draft-ja.md`、5.4 の `02-data-model/grs-native-erd-ja.md`、5.5 の `07-plan-actual/plan-actual-decisions-ja.md`、6.2 の `出どころは `previous-project-result/02-data-model/grs-native-erd-ja.md`。`） | （変更なし） | **これから書く者への執筆指示**であって、検討の経緯ではない。「`status: draft` であり決定ではない。ここで決める」も同様に、いま決まっていないという**現在の状態**の告知 |

---

## 4. 適用するときの順序と注意

| # | 注意 |
|---|---|
| 1 | **除いた文は先に CR-114 §8 の保管庫へ原文のまま写すこと（MUST）。** 除いた文はそこにしか残らない |
| 2 | **`S4` と `S5` は対で適用する（MUST）。** 片方だけだと 🅿 の凡例が宙に浮く |
| 3 | **`S14`（`S-99d` の削除）は CR-102 の後に回す。** `change-request/CR-000-impact-and-order.md` が CR-102 の影響先として `S-99d` を挙げている |
| 4 | **`S19` は要求側（`FR-047` の `RATIONALE`）の判定と突き合わせてから適用する。** 参照先が消えると宙に浮く |
| 5 | **`S10` は判断待ち。** 残す／除くのどちらでも仕様は変わらないが、`210` を再提案されたときの歯止めが変わる |
| 6 | **適用の直前にリテラルを再照合する（MUST）。** 同時編集が続いている |
| 7 | **1 塊 3〜5 件で `bash .claude/skills/spec-graph-check/check.sh`。** 行の削除が 2 件（`S5` / `S14`）あるので、検査 7（存在しない行への参照）と検査 9（行数の主張）を特に見る |

## 付録 — 改行や縦棒を含むリテラル（原文のまま。ここから複写すること）

### G2（`tbl-glossary.md`）

置換前:

```
§2-1）である。
**語は 1 つも落とさずに引き継いだ。** 一方、そこに併記されていた**旧名と改名の経緯は引き継がない** —— 旧語彙を本プロジェクトへ流入させないためである。経緯が要るときは由来の文書を読む。
```

置換後:

```
§2-1）である。
```

### S5（`tbl-settings.md`。`S-52` の行を削除）

置換前:

```
| S-51 | イナズマ線 | `progressLineOverhang` | px | 6 🔎 | 0 | 40 | — |
| S-52 | イナズマ線 | `statusDate`（PoC 名）🅿 | 日 | 700 | 0 | 1095 | 前プロジェクトの PoC 専用。**本製品では設定値ではない**（`FR-046`） |
```

置換後:

```
| S-51 | イナズマ線 | `progressLineOverhang` | px | 6 🔎 | 0 | 40 | — |
```

### S14（`tbl-settings.md`。`S-99d` の行を削除）

置換前:

```
切り替え対象ではない** |
| S-99d | ~~本日線の表示状態~~ | — | **廃止（2026-08-13）。** 基準日線は `Project.statusDate` が `null` かどうかで描画が決まり、表示状態という値を持たない（`FR-046`） |
```

置換後:

```
切り替え対象ではない** |
```

### S18（`tbl-settings.md`。表 T-215 の末尾 1 行を削除）

置換前:

```
判定の仕方は表 T-205 の注が持つ。**
⚠️ **本表はかつて「しきい値を実効フォントサイズの関数として持て」と指示しており、表 T-205 の「固定値であり、実行時に導出してはならない（MUST NOT）」と相互否定になっていた。表 T-205 側に寄せ、本表の指示は廃した**（2026-08-13）。
```

置換後:

```
判定の仕方は表 T-205 の注が持つ。**
```
