---
type: ToDo
title: 住所の無い規則を表の行にし、嘘の辺を検める
description: 長い STATEMENT に MUST を何本も抱えている要求を、既定の作法（細目は表、要求は 1 文でそれを指す）へ戻す。⛔ 字数を減らす作業ではない。引用が腐る原因を断つ作業である。
tags: [cleanup, phase-2, p2-3, todo, lying-edges]
phase: cleanup-p2
status: done   # 第 3 節（住所を作る）と第 7 節（門にするか）の両方が 2026-09-12 に完了
---

# ToDo — 住所の無い規則を表の行にし、嘘の辺を検める

⭐ **2 つの手が入っている。**第 3 節が**住所を作る**手、第 7 節が**辺を検算する**手である。⛔ **同じ病の別の治療であり、順がある**（第 7 節の末尾）。

⭐⭐⭐ **第 3 節（住所を作る）は 2026-09-12 に終わった。**6 つの段落が 6 つの表になり、**1,500 字を超える段落は 8 から 1 に減った。**⇒ ⭐ **第 7 節（辺を検める道具を門にするか）が待っていた条件が揃った** —— **件数が動く作業はもう無いので、いま測れば基準線は落ち着く。**

---

## 1. ⛔ 何が問題か —— 字数ではない

**1 つの段落に MUST が何本も入っていると、その MUST たちに住所が無い。**

| | |
|---|---|
| 表の行なら | `` 表 T-023d の `GR-5` `` と**名指せる** |
| 長い STATEMENT の中の MUST は | ⛔ **「`FR-001` の STATEMENT」としか言えない** |

⇒ ⛔⛔ **引きたい規則に住所が無いと、人は引用するしかない。**

```
健全:  `FR-011` が定める
実物:  `FR-011` は「両端を動かさないこと（MUST）」と定めている
```

**後者は参照ではない。指し先ラベルの付いた写しである。**⇒ **原本が動いても写しは動かない。**

### ⭐ これは推測ではない。2026-09-11 の巡が実測した

掃除フェーズ 2 が見つけた仕様書の嘘のうち、**18 件が「辺が嘘」**である ——
**引き先が違う席（9）／撤回済みを現在形で引く（4）／完全性の主張が偽（5）。**

⚠️ **母数は動いた**（実測 2026-09-12）—— **`DFC-445` 〜 `DFC-488` で立っている行は 43 件、取り下げ 1 件**（`DFC-473`。⛔ **前に立つ者が台帳の行を反証せずに裁定へ出し、実物は台帳の言うとおりではなかった**）。

⭐ **逆の証拠もある** —— **`GR-5` や `EP-9` のような行 ID を持つ規則には、引用の腐りがほとんど無い。指せるからである。**

⇒ ⭐⭐ **住所の粒度が粗いことが、引用の腐りの原因である。**

---

## 2. 実測（2026-09-12。⛔ 着手前に測り直すこと）

⭐⭐ **終わった。**⛔ **実測 2026-09-12（作業後）**:

| 要求 | 前 | 後 | 表 | 行 | 移した規範 |
|---|---:|---:|---|---:|---|
| `FR-001` | 3,196 | 1,766 | **T-239** `TC-` | 12 | 17 / 19 |
| `FR-043` | 2,954 | **729** | **T-240** `DM-` | 13 | **23 / 23** |
| `FR-025` | 2,212 | **610** | **T-241** `IX-` | 10 | 14 / 16 |
| `FR-020` | 3,281 | **474** | **T-242** `WM-` | 14 | **30 / 30** |
| `FR-003` | 1,680 | **430** | **T-243** `OR-` | 6 | **8 / 8** |
| `FR-016` | 1,676 | 1,191 | **T-244** `TE-` | 4 | 3 / 7 |

⭐ **1,500 字を超える段落は 8 → 1。**残る 1 つは `FR-001` の 1,766 字であり、⛔ **その中の MUST 2 本はどちらも撤回記録の中である**（裁定 E が守る）⇒ **手で刈る対象はもう無い。**

⭐ **`FR-016` がいちばん縮まなかったのは正解である** —— **7 本中 4 本が撤回記録の中に在る。**⇒ **縮み幅を成果と読むな。**残ったのは「どうしてこの規則になったか」の記録であり、生きた 3 本に席ができた今、それは記録として読める。

⛔⛔ **`FR-020` と `FR-025` は順に直せなかった** —— `induced.py` が **`FR-020` / `FR-025` / `FR-080` の大きさ 3 の閉路**を出し、道具自身が「**a cycle gets ONE plan and ONE pass**」と述べている。⇒ **1 度の走りで両方書き、門を 1 度だけ読んだ。**⭐ **壊れうる辺は 1 本だけだった** —— **`FR-025` が `FR-020` を引用で引いている**（「`FR-020` が求める「画像に載せるかどうかを選ばせる」」）。**桝は原文の切り出しなので、引用はそのまま解決した。**

### ⛔⛔ 引継書の数は間違っていた（2026-09-12 訂正）

**`cleanup-p2-next-session-prompt.md` は「`FR-001` の STATEMENT は 1 行で 6,000 字を超える」と書いていた。偽である。**
**6,056 は要求 1 件の全体**（見出し・STATEMENT・ORIGIN・RATIONALE・Relations）**であって、1 行の長さではない。**⇒ **前に立つ者が単位と行を取り違えた。**

⚠️ **手順書 `cleanup-2026-09-11-prompt.md` 618 行の「1 行が最長 3,708 字」も前の巡の数**であり、いまの最長は 3,281 字である。

⇒ ⛔ **この ToDo の数も、着手する日には腐っている。測り直せ。**

---

## 2-b. ⭐⭐ `FR-001` を割って分かったこと（2026-09-12、`eacb169`）

**赤は 2 種類出た。⛔ どちらも「条項が変わった」ためではなく「窓が割れた」ためである。**

| | 何が起きたか | 打ち直し方 |
|---|---|---|
| **試験 5 件** | 錨が**文の境を跨ぐ窓**だった —— 前の文の末尾＋条項を 1 続きで取っていた。条項は無傷だが、前の文が別の行へ移った | **窓を左から縮め**、原稿がまだ持つ最短の後方一致にした。⛔ **一意であることと MUST 記号が残ることを `assert` で縛る** |
| **検査 42 が +1** | ⛔⛔ **`somewhere_in` は「1 つのファイルが全断片を順番どおりに持つ」ことを要求する。**コメントの省略つき引用が、**指し先の 1 文を差し込んだ継ぎ目をちょうど跨いでいた** | 引用を 2 つに割り、後半を **行 ID で指す**形にした（⭐ **検査 42 自身がその助言を出力に書いている**）|

⛔ **次に割る者が必ず踏むのは 2 つ目である。**⇒ **段落の途中に 1 文でも差し込むと、そこを跨ぐ引用が全部落ちる。**

### ⛔ 手順として守ったこと

1. **1 条項も打ち直さない。**桝は原文の切り出しにする
2. **書く前に、`src/` と `tests/` が逐語で引いている文を洗い出し、バイト一致を `assert` で縛る**（実測: 48 文中 **13 文**が引かれていた。13/13 が生存）
3. ⛔ **太字は足さずに、余った 1 つを落とす** —— **`**` が CJK に隣接すると docutils の export が壊れる**
4. ⛔ **`expect(` を数える**（89 → 89）。⚠️ **`Tests` の件数は守りにならない**
5. **席番号と接頭辞は、使う前に空きを実測する**（`T-239` は `T-238` の次、`TC-` は `docs/spec` ／ `src/` ／ `tests/` ／ `tools/` ／ `.claude/` に 0 件）

---

## 2-c. ⭐⭐ 6 つ割って分かったこと（2026-09-12）

### ⛔⛔ 指し先の文を `（MUST）` で書くな —— 裸の MUST を 6 本作っていた

**「…は 表 T-xxx に従うこと（MUST）」は、それ自体が新しい MUST であり、逐語で縛る試験が無い。**⇒ **検査 39 が「A NEW MUST/MUST NOT clause may not be written bare」で止める。**

⛔⛔ **最初の 4 表では止まらなかった** —— **段落を割ると総数が減るので、指し先が足す 1 本が差し引きで隠れる。**⇒ **5 つ目で初めて表に出た。**

⭐ **正しい形**: **「…は 表 T-xxx が持つ。」** —— **義務は行が負い、指し先は在り処だけを言う。**（6 本とも直して、裸の MUST は 1,350 → **1,344**。基準線 1,349 を切り下げずに下回った。）

### ⭐⭐ 割る作業そのものが検出手段になる

**新しい行が、既に在る行の言い直しであることは、行にして初めて見える。**実測 **5 件**:

| 見つかったもの | どう出たか |
|---|---|
| `DM-10` が幅の規則を再掲 | 検査 11。⭐ **`DM-3` という住所が同じ手でできたので、指し先に変えられた** |
| `DM-13` が 表 T-023d の結びの MUST を逐語で写す | 検査 11 |
| `OR-5` が 表 T-023d の `GR-7` を逐語で引く | 検査 11 |
| `OR-5` が同じ幅の式をもう一度書く | 検査 11（同じ行に 2 件）|
| `DFC-488` 「マイルストーンの例外は 3 つ」が 4 つを挙げる | ⭐ **目で数えられた** —— 行に分けたからである |

⚠️ **`OR-5` の逐語は 5 ファイルが引いていたが、畳んでも検査 42 は動かなかった** —— **原本が `GR-7` に残っているからである。**⇒ **問いは「この写しが残るか」ではなく「`docs/spec` のどこかにその文字列が残るか」である。**

### ⛔ 赤の出方は 2 通りしかなかった

| | 何が起きたか | 打ち直し方 |
|---|---|---|
| 跨ぐ窓（**8 件**）| 錨が前の文の末尾＋条項を 1 続きで取っていた | **左から縮めて、原稿がまだ持つ最短の後方一致へ。**⛔ 一意性と MUST 記号の残存を `assert` で縛る |
| 継ぎ目を跨ぐ引用（**1 件**）| ⛔⛔ **`somewhere_in` は 1 ファイルが全断片を順番どおりに持つことを要求する** —— 段落の途中に指し先を 1 文差し込むと、そこを跨ぐ引用が落ちる | 引用を 2 つに割り、後半を**行 ID で指す** |

⭐⭐ **2 つ目は避けられる** —— **段落の頭を指し先に置き換えれば、継ぎ目が生まれない。**⇒ `FR-043` 以降の 5 つはその形にし、**継ぎ目由来の赤は 1 件も出ていない。**

---

## 3. ⭐ 正しい形 —— 文書が既に答えを持っている

`docs/development-rules/08-spec-template/spec-writing-rules.md` の「1.9 の表と図の参照の規則」:

> **細目が表または図で表せるなら、要求は 1 文でそれを指すこと（MUST）。行ごとに要求ノードを作ってはならない（MUST NOT）。**

⇒ ⭐ **`FR-001` はその作法を守っていないだけである。**

```
いま:  FR-001 の STATEMENT に MUST が 26 本（住所は 1 つ）
正しい: 表 T-0xx に 1 行 1 規則（住所が 26 個）
        ＋ STATEMENT は「表 T-0xx に従うこと」の 1 文
```

### ⛔ やってはならない 2 つ

1. ⛔ **`FR-001a` / `FR-001b` … に割るな。**同じ規則の `WR-2` が**細目を要求ノードで並べることを禁じている**（要求ノードは 1 つずつが UID・理由・関係・試験を連れてくるので、文書も追跡も重くなるだけである）。
2. ⛔ **改行を入れるだけにするな。**段落を 6 つに割っても、**住所は 1 つのままである。**⇒ **意味が無い。**

---

## 4. ✅ 着手する前に決めること（⭐ **2026-09-12 に 3 つとも決まった**）

⭐ **決まった答え** —— どの段落か: **6 つすべて**（⛔ ただし `FR-020` は RATIONALE なので、**理由は散文に残し、規則だけを行にした**）。席番号: **`T-239` 〜 `T-244`**、接頭辞 **`TC` `DM` `IX` `WM` `OR` `TE`**（⛔ **使う前に `docs/spec` / `src` / `tests` / `tools` / `.claude` で 0 件を実測した**）。指し先の付け替え: **赤くなった 9 件を打ち直した**（上の 2-c）。

- **どの段落を表にするか。**⛔ **6 つ全部とは限らない** —— 表にできるのは「**細目**」であって、論証や撤回記録は表にならない。⇒ **1 つずつ中身を見て決める。**
- **表の席番号。**⛔ **通し番号を詰め直してはならない（MUST NOT）** —— 同規則の「番号は席番号とする」。
- **指し先の付け替え。**⚠️ **いま「`FR-001` の STATEMENT」を引いている席が他に在れば、新しい行 ID へ向け直す。**⛔ **引き先を変えると、原稿を読む試験が赤くなる**（下の門）。

---

## 5. ⛔ 門 —— 仕様書を触る手ごとに 4 本

```bash
bash .claude/skills/spec-graph-check/check.sh > /dev/null 2>&1; echo "check=$?"
npx vitest run 2>&1 | grep -E "Tests "
npx tsc --noEmit; echo "tsc=$?"
npm run gen:check > /dev/null 2>&1; echo "gen=$?"
```

⛔⛔ **`vitest` を必ず走らせろ。**⚠️ **原稿を実行時に読み「その文がいまも一字一句在るか」を見る試験が在る。**⇒ **本作業は文を動かすので、確実に赤くなる。**
⭐ **赤くなったら、錨を新しい席（行 ID）へ打ち直せ。⛔ 期待値を消すな。**
⛔ **`Tests` の件数は守りにならない**（`it()` の数である）⇒ **`expect(` を数えよ。**

⚠️ **検査 42 の基準線は 469 で余裕ゼロ**である。⛔ **引用を指し先へ変える作業なので、下がることはあっても上がってはならない。**

---

## 6. ⭐ 副次的に直るもの

**長い段落は `git diff` が読めない。**⛔ 1 文字直しても「3,000 字の行が変わった」としか出ない。
⚠️ **2026-09-11 の巡は、それで差分を読むための道具を別に書く羽目になった** —— **何が消えたか確かめられなかったからである。**

⇒ ⭐ **表の行にすれば、変わった行だけが差分に出る。**

---

## 7. ✅ もう 1 つの ToDo —— 嘘の辺を検める道具を、門にするか決めた

⛔⛔ **裁定（2026-09-12）: 門にしない。実測した適合率は 3/8 = 37.5% である。**

**道具は書いた**: `.claude/skills/spec-graph-check/list-lying-edges.py`（2026-09-12）。
⛔ **門ではない。**`check.sh` は明示列挙なので拾わない（確認済み）。

### 何をする道具か

| | 問い |
|---|---|
| **検査 42** | 「この引用は **`docs/spec` のどこかに**在るか」 |
| **本道具** | 「この引用は **名指された席に**在るか」 |

⛔⛔ **検査 42 は、この巡で見つかった (a) の 9 件を 1 件も捕まえない。**引用された文は**実在する。別の席に。**

⭐ **作り直しはほとんど無い** —— 席の範囲は `specindex.py`（`row_owner` / `rows_of` / `owner_at`）、正規化と省略記号の割りと帰属の窓は検査 42 の実装。**新しいのは「席に縛る」1 段だけ。**

⭐ **動詞は現在形だけにしてある**（裁定 E のため）—— **撤回記録は撤回した規則を引用するので、その引用は席に無くて当然である。**⇒ 「と定めていた」は最初から拾わない。

### ✅ 実測（2026-09-12。⛔ 8 件すべてを手で実物に当てた）

| # | 引いている席 | 名指す席 | 判定 |
|---:|---|---|---|
| 1 | `01-04:1105` | `FR-001` | ⭐ **真** —— **短縮**（括弧と `（MUST NOT）` を落とす）。`DFC-491` |
| 2 | `01-04:1325` | `FR-029` | ⛔ 偽 —— 引用の主は「**本行**」。しかも**撤回記録** |
| 3 | `01-04:1685` | `QN-1` | ⛔ 偽 —— 引用は**書き手が書いた文**であって席の文ではない |
| 4 | `01-04:1698` | `S-78` | ⛔ 偽 —— `null` の**語釈** |
| 5 | `tbl-glossary:116` | `T-105` | ⛔ 偽 —— 語釈。`T-105` は「例外は」として名指されているだけ |
| 6 | `tbl-property-items:12` | `FR-006` | ⭐ **真** —— 生成器が `「対象」` の `「」` を落としていた。`DFC-489` |
| 7 | `tbl-property-items:14` | `FR-038` | ⭐ **真** —— 「辞書**として 1 か所に**持つこと」を「辞書 **1 つに**持つ」と刷っていた。`DFC-490` |
| 8 | `tbl-settings:277` | `T-215` | ⛔⛔ 偽 —— **最悪の形。**文は「**表 T-215 の段では*ない***」と書いており、**否定を引用と読んでいる** |

**⇒ 3 / 8 = 37.5%。**⛔ **`list-asserted-claims.py` が門に入らなかった 63% より低い。**⇒ **入れない。**

### ⭐⭐ 偽の 5 件のほうが有用だった —— 直す向きが出た

**5 件とも同じ壊れ方である** —— **近くにある席の名と、近くにある引用を、その文が実際に帰属を述べているか確かめずに対にしている。**

⇒ ⭐ **`list-inverted-authority.py` とまったく同じ壊れ方である**（あちらは識別子の近さで拾い、委ね方を見ない）。

⭐ **直す向き**: **帰属動詞（「`X` が「…」と定めている」型）がその席とその引用を結んでいることを要求し、⛔ 否定（「…では*ない*」）と過去形（「…と定めていた」）を除く。**⇒ **6 件の偽のうち 5 件がその篩で落ちる。**

### ⛔⛔ そして、門にしてはならない理由がもう 1 つ実測で出た

**住所を与える作業が、この道具の偽陽性を体系的に作る。**

**1 件目（`FR-001`）がその一歩手前だった** —— 引用が指す条項は、いま 表 T-239 の `TC-9` に在る。⇒ **道具は行を行の席（`T:T-239 TC-9`）に解決するので、`FR-001` とは別物と見る。**

⇒ ⛔ **いま門にすれば、規則に住所を与えるたびに赤が出る。**⭐ **直すなら、要求の中に在る表の行は、その要求にも属すると数えること。**

### ⭐ 真の 3 件から出た、より重い発見

**`DFC-490` を直したら検査 42 が 469 → 471 に上がった。**⇒ **言い換えが 2 段で伝播していた**:

```
生成器 property_items_json_to_md.py
  → 生成物 _assets/tbl-property-items.md
    → 2 つの試験のコメント（measured-sweep / uf-62）
```

⭐⭐ **原本を直した瞬間に、鎖の全体が一度に見えた。**⇒ **写しの連鎖は、origin を直すまで一本ずつにしか見えない。**

⛔ **`DFC-491` は直して測って戻した** —— `src/framework/single-html-shell/frame-loop.ts` がその短縮形を引いており、**短縮形は原稿のその 1 行にしか無い。**⇒ `src/` の巡待ち（`DFC-465` / `DFC-466` / `DFC-481` / `DFC-485` と同じ扱い）。

### ⚠️ 前に立つ者の読み違えを 1 件記録する

**最初「`FR-006` はその規則を持たない」と報告した。⛔ 偽である。**⭐ **持っている** —— 素の文字列一致が 0 件だったのは `「」` の差だけである。⇒ **`DFC-473` と同じ形の読み違いであり、台帳の行にも書いた。**

### ⛔⛔ 2 つの ToDo は干渉する。順を決めること

**本書の第 3 節（住所を作る）を先にやると、この道具の件数が動く。**
⇒ **住所ができれば、引用は指し先に変えられる。**⭐ **8 件のいくつかは、そもそも消える。**

| 順 | 起きること |
|---|---|
| **表にする → 測る** ⭐ | **件数が減ってから測るので、基準線が落ち着く。**⛔ ただし**表にする作業中は、この道具が当てにならない** |
| 測る → 表にする | 基準線を置いた直後に表にするので、**すぐ動いて置き直しになる** |

⇒ ⭐ **表にするのを先、門にするかの判断を後**にするのが筋である。

⭐⭐ **2026-09-12: 両方とも終わった。**前半（住所を作る）は 6 表で完了し、後半（門にするか）は**測って「しない」と裁定した。**⇒ ⭐ **この ToDo に残っている仕事は無い。**⚠️ **次に誰かがこの道具を門にしたくなったら、上の「直す向き」を先に実装し、⛔ 実装したその日にもう一度 8 件を手で当て直すこと。**

---

## 8. 関連

- 判定と裁定: `docs/review/cleanup-2026-09-11-prompt.md`（第 6 節・第 14 節）
- 引継: `docs/review/cleanup-p2-next-session-prompt.md`
- 台帳: `docs/development-records/defects.md`（未解決）と `docs/development-records/fixed-defects.md`（解決済み） —— **`DFC-445` 〜 `DFC-487`**。⛔ **2026-09-12 に 12 行が `実測済` で移ったので、片方だけを grep しては母数を取り損ねる。**
- 道具: `.claude/skills/spec-graph-check/list-lying-edges.py`（⛔ **門ではない**）
- ⭐ **`P3-6`（検査 42 を仕様書の内側へ向ける）の試作が、上の道具である。**⛔ **`P3-6` はまだ未裁定** —— **門にするかは、第 7 節の精度を測ってから決める。**
- ⚠️ **`P3-8`（完全性の主張の検算）は別物で、こちらも未裁定。**⭐ **一般には書けないので、「全数である」と書く席に比べる 2 つの集合を宣言させる形に作り直すのが筋**（`DFC-457` / `DFC-461` / `DFC-463` の 3 件がその母数）。⭐⭐ **2026-09-12 に実測で裏づいた** —— 下の第 10 節。

---

## 9. ⭐ 撤回済みを現在形で引く —— 道具を書き、精度を実測した（2026-09-12）

**`.claude/skills/spec-graph-check/list-withdrawn-cited.py`**。⛔ **門ではない。**

### ⛔⛔ なぜ検査 42 が構造的に見逃すか

**撤回記録は、撤回した規則を「」に入れて残す**（裁定 E がそれを守ると決めた）。
⇒ ⛔⛔ **撤回された文は、その日から永久に `docs/spec` の中に在る。**
⇒ **検査 42 の問い「この引用は `docs/spec` のどこかに在るか」は、必ず「在る」と答える。**

**これは検査 42 の抜かりではない。撤回記録を残すと決めたことの代価である。**

### 実測（⚠️ 裏のセッションが `docs/spec` を書きかけの木で測った。測り直すこと）

| | |
|---:|---|
| 撤回記録から回収した文 | **38** |
| よそでの再出現 | **28** |
| うち**自分で「これは昔の文だ」と印を付けている**（⭐ 健全） | **24** |
| **印の無いまま現在形で主張している** | **4** |

⭐ **4 件を全部手で当たった。4 件とも真。**（⛔ n=4 は小さい。姉妹道具は 9 件中 3 件しか当てていない）

| 場所 | 引いている撤回済みの文 | 撤回日 |
|---|---|---|
| `tests/unit/edit-task-group.test.ts:448` | 「`FR-032` は行を 1 つも無い状態にできる」 | 2026-09-02 |
| `tests/unit/uf-63.test.ts:578` | 「その行自身を畳んではならない」（`HF-11` の MUST NOT として） | 2026-08-31 |
| `src/adapter/svg-renderer/svg-renderer.ts:1205` | 「年と月の段は 1 か月」（`LF-1` gives …として） | 2026-09-04 |
| `tests/contract/display-words.contract.test.ts:1423` | 「表 T-016 の項目名は翻訳の対象ではない」（`FR-038` の文として） | 2026-09-04 |

⛔⛔ **いちばん重いのは 1 行目である** —— **試験の名が `'FR-032 can leave the document with no rows at all'` で、注が「`FR-001` states it plainly」と書き、その中身を検証している。**⭐ **双子の本番コード `edit-task-group.ts:499` は腐りを知っていて「it is reported, not edited from here」と書いた。⇒ 報告は出ていた。試験のほうを誰も動かさなかった。**

⛔ **4 行目は利用者の裁定で「逆」になった規則である**（2026-09-04「日本語モードでは日本語、英語モードは英語で表示する」）。⇒ **契約試験が、覆された規則を `FR-038` の文として引いている。**

### ⛔ 4 件はまだ台帳に立てていない —— 裁定が 2 つ要る

⚠️ **2026-09-12 時点で `defects.md` は別セッションが書きかけである**（`3b7fdf6` 直後）。⇒ **裁定 D「本は 1 つのファイルを 1 人ずつ」により手を出していない。**

1. **4 件を `DFC-477` 〜 `DFC-480` として立てるか。**⚠️ **立てる者は、そのとき測り直すこと。**
2. ⛔⛔ **`edit-task-group.test.ts` の検証そのものを反転させるか** —— ⭐ **帰属が偽なのは確実である**（`FR-001` はその文を持っていない）。⚠️ **だが「行 0 個にできる」という振る舞いの可否は 表 T-050 の不変条件の裁定であって、道具の判断ではない。**⇒ **利用者に問うこと。**

### ⚠️ 作りの要点（次に触る者へ）

1. ⛔ **「現在形の動詞を要求する」は誤りである。**28 件の再出現は**全部 `src/` と `tests/` にあり、コメントに現在形は無い。**「仕様書はこう書いている」とは書かず、**規則をそのまま書く。**⇒ **引いた時点で主張である。**
2. ⭐ **正しい問いは「これは歴史だと印が付いているか」。**
3. ⛔⛔ **印は両言語で探せ。しかも大文字を見よ。** —— このコードベースは強調を全大文字で書き、`[Ww]as` 式の文字クラスは `WAS WITHDRAWN` を 1 つも捕まえない。**これで 2 件の偽陽性が出た。**
4. ⚠️ **日付を伴わない印もある** —— 「still reads X, which the invariant has made `stale`」。**それは報告であって主張ではない。**

---

## 10. ⚠️ 完全性の主張 —— 道具は書いたが、機械にできることは少なかった

**`.claude/skills/spec-graph-check/list-prose-tallies.py`**。⛔ **門ではない。**

### ⭐ 正しい述語は「完全性を主張しているか」ではなかった

| | 件数 | |
|---|---:|---|
| 「全数」「すべて」「だけである」 素で | **316** | ⛔ 干し草 |
| 表を名指すものに絞って | **63** | ⛔ **まだ干し草。しかも大半が健全** |

```
⭐ 健全: 指令の全数は 表 T-232 が持つ        ← 表が典拠。1.9 の作法どおり。腐りようがない
⛔ 腐る: 拡張領域を使う 2 つはこれだけである   ← 散文が自分で数えている
```

⇒ ⭐⭐ **述語は「散文が数を自分で持っているか」である。**⛔ **「に限る」「のみである」は完全性の主張ではない** —— 「入力機器をマウスとキーボードに限る」は**範囲の限定**であり、表が行を得ても偽にならない。**入れると一覧が埋まる。**

### 実測

| | |
|---:|---|
| 散文が数を自分で持つ（⛔ **作業一覧**） | **27** |
| 数を表へ渡している（⭐ 健全、落とした） | **58** |
| **機械が数え直せた** | **2**（うち 1 件は正当な部分集合＝偽の警告） |

⇒ ⛔ **正直に言う: 数え直しの列はほぼ無価値だった。**⭐ **道具の値打ちは 27 件の作業一覧と、85 件中 58 件を健全として正しく捨てたことにある。**

⇒ ⭐⭐ **`P3-8` の見立てが実測で裏づいた** —— **完全性の主張は一般には機械で検められない。「全数である」と書く席に比べる 2 つの集合を宣言させるほかない。**

---

## 11. 形 (e)（仕様書が `src/` のコメントを典拠にする）—— 道具は在る。⛔ 門にはしない

**`.claude/skills/spec-graph-check/list-inverted-authority.py`**（`50255af`）。

**`DFC-470` は実在した。**`52d3fe1` の原文は「式を写して持たせてはならない（MUST NOT）—— **`groupDepthThresholdOf` の注が自ら 2 つ目の写しを禁じている**」であり、**`3b7fdf6`（2026-09-12）が理由を仕様書の側に置き直して直した**（「同じ式が 2 か所にあれば、片方だけが直されて必ず離れる」（1.9））。

### ⛔⛔ 安い述語は使えない

| | 件数 |
|---|---:|
| `docs/spec` の小文字の識別子 | 446 |
| ちょうど 1 回しか出ないもの | 236 |
| **かつ `src/` に在るもの** | ⛔ **228** |

⇒ ⭐ **「1 回しか出ない」は正常である**（表が 1 度名を立て、散文がそれを指す）。⇒ **手掛かりは委譲の言い回しのほうしか無い。**

### ⭐⭐ 篩が 1 つ、狙いを定義上除外していた（2026-09-12 に判明）

**この道具は一度「`DFC-470` を捕まえられない」として捨てられた。**⛔ **その測定は、`3b7fdf6` が直した後の木に当てていた。**直る前の木に当て直すと、原因は篩 3 だった:

```
委譲の錨「の注が」          → 見つかっている
60 字前の識別子            → `groupDepthThresholdOf` を捕まえている
⛔ 篩 3「docs/spec に 1 回だけ」→ 2 回出るので捨てていた
```

⭐ **2 回出るのは構造的に必然である。**委譲する文は**名を立ててから**（`groupDepthThresholdOf`（その段を描くのに要る倍率））**その名に委ねる**（`groupDepthThresholdOf` の注が自ら…）。⇒ ⛔⛔ **「静かにするため」の篩が、狙いそのものを定義上除いていた。**⇒ **員数は「この行の外に出ないこと」で採ること。**

| 木 | 件数 | `DFC-470` |
|---|---:|---|
| `52d3fe1`（直る前） | 3 | ⭐ **捕まえる** `05-07-design.md:330` |
| いまの木 | 2 | 無い（直っているので正しい） |

⇒ ⛔ **門にはしない。3 件中 1 件＝33% は、63% で門入りを見送った `list-asserted-claims.py` より低い。**⭐ **母数も今は空である。**⇒ **棚に置く道具として収めた。**

### ⚠️ 教訓 2 つ（同じ罠がまた来る）

1. ⛔⛔ **探知器を「捕まえない」と判ずる前に、その 1 件がまだ木に在るかを確かめよ。**⚠️ **裏で別のセッションが直していることがある。**⇒ **`git log -S` で原文を出し、直る前の木に当てること。**
2. ⛔ **未追跡のまま置くな。**この道具は未追跡だったので、消えたとき **git から戻せなかった**（読み取り記録から復元した）。
