---
type: Handover Prompt
title: 掃除フェーズ 2 の続き — 4.1 を 6 束に割って刈る
description: 2026-09-11 のセッションからの引継。フェーズ 1 は完了しタグ済み、フェーズ 2 は P2-1 完了・Chapter 1 のみ仕分け済み。次は 4.1 機能要求 267,186 字を 6 束に割り、⛔ 体を 1 つずつ走らせて刈る。束の境界は計算済みで、そのまま使える。
tags: [cleanup, phase-2, handover]
phase: cleanup-p2
status: ready
---

# 掃除フェーズ 2 の続き — 引継プロンプト

このファイルの中身を、そのまま次のセッションの最初の指示として貼ってよい。

---

> # ⛔⛔ 止まれ — 判定基準はこの文書に無い
>
> **`docs/review/cleanup-2026-09-11-prompt.md` の第 6 節（判定 — 5 つの箱、6-1 から 6-6 まで）を全文読むまで、1 文字も消すな。**
>
> **本書は手順書であって、判定基準ではない。**何を消し、何を残し、何をどこへ移すかは**すべて第 6 節が決める**。本書にはその要約すら置いていない。
>
> ⛔ **要約を置かない理由は、第 6 節自身の 6-1 ① である** —— 「原本が他所にあるか ⇒ ここに在るのは写しである」。170 行の規則を 2 か所に置けば、**この巡がまさに消している病気**を、掃除の道具そのものの中に作ることになる。⚠️ 前の巡では、規則を 2 か所に持った結果、**同じ規則が「大きな図は」と「大きい図は」に分岐していた**のが実際に見つかっている。
>
> ⛔ **体に渡すときも同じである。**第 6 節の**場所を教えて原典を読ませよ**。要約を渡すな —— 縮めると体は消す方へ倒れる。
>
> **読んだか。** 第 6 節の問いは、**問い 0 →問い 1 →問い 2 →問い 3 の順**でしか当ててはならない。⛔ 「仕様書に書けるか？」から問うと、そもそも要らないものが配られる。読んでいないなら、⛔ **ここで戻れ。**

---

## 1. ⛔ まず読む順

1. **`docs/review/cleanup-2026-09-11-prompt.md` の第 0 節（現在地）と第 14 節（裁定の記録）** —— この巡の地図と、⛔ **問い直してはならない裁定**
2. 同書の **第 6 節（判定 — 5 つの箱）を全文** —— 刈るときの判定はここがすべてである
3. 本書の第 3 節（次の手）

⛔ **第 6 節を縮めて体に渡すな。**縮めると体は消す方へ倒れる。体には節の場所を教えて**原典を読ませよ**。

---

## 2. 現在地（2026-09-11 時点の実測）

| | 値 |
|---|---:|
| タグ | `After-Cleanup-P1` = `67d46a2` |
| 最新コミット | `b5ed4fa`（push 済み、ブランチ `restart`） |
| **仕様書** | **513,758 字 / 9 ファイル**（開始 700,026 から **−26.6%**） |
| `src/` のコメント | **48,668 行** / 76,438 行（63.7%）⛔ **未着手** |
| `tests/` のコメント | 59,719 行 / 191,509 行 ⛔ 未着手 |
| 検査 | **42 本**（うち 2 本が `src/` を読む） |

門はすべて緑: `check.sh` 0 ／ `vitest` 7478 passed / 1 skipped ／ `tsc` 0 ／ `layers` 299/299 ／ `parity` OK。

⚠️ `playwright` は **2 件が赤**（`tests/nfr/` のフレーム時間と `PG-` の関門）。⛔ **この掃除より前から赤である。**直すな。

---

## 3. 次の手 — 4.1 を 6 束に割って刈る

`docs/spec/01-04-requirements.md` の **4.1 機能要求**が仕様書の **52%** を占める。ここが本命である。

**4.1 = 行 888〜4156、要求 99 件、267,186 字。**⭐ 要求 1 件が `#### 見出し` 1 本を持つので、境界は明確である。

### 束の境界（計算済み。⛔ 測り直す必要はないが、行番号は編集でずれる）

| 束 | 行 | 字数 | 件数 | 先頭の要求 |
|---|---|---:|---:|---|
| **1** | 894–1330 | 48,685 | 12 | `FR-001` 形状を選んでタスクを作る |
| **2** | 1331–1998 | 45,118 | 18 | `FR-005` 階層の移動を元データへ反映し、行の移動は反映しない |
| **3** | 1999–2455 | 45,337 | 11 | `FR-043` 実績がまだ無いタスクにも入力の入口を出す |
| **4** | 2456–3009 | 47,178 | 20 | `FR-052` パネルの幅を変える |
| **5** | 3010–3693 | 45,690 | 23 | `FR-022` 複数のファイルを合流させる |
| **6** | 3694–4156 | 35,178 | 15 | `FR-064` `Agent API` の規約 |

⚠️ **1 件だけ桁が違う**: `FR-016` が **26,401 字**（束 1 の中）。ほかの中央値は 1,807 字である。⇒ 束 1 は件数が少ないのにいちばん重い。

⛔ **行番号は、束を 1 つ刈るたびにずれる。**次の束に入る前に、`#### ` 見出しと `**UID**:` で境界を取り直せ。行番号で体に渡すな —— **要求 ID の範囲で渡せ。**

### ⛔⛔ 体は 1 つずつ。並行させるな（利用者の裁定 2026-09-11、裁定 D）

1 つのファイルを 2 体が同時に編集すると、**行範囲が交わらなくても片方の編集が消える**（読む → 書き換える → 書き戻す、の間に相手が入る）。
⚠️ **消えても赤にならない。**「消したはずのものが残っている」形でしか気づけない。

⇒ **1 セッション 1〜2 束。4.1 だけで 3〜5 セッションかかると見積もれ。**

---

## 4. 体に渡す指示に必ず入れること

- **第 6 節の場所を教えて原典を読ませる**（⛔ 要約を渡すな）
- **担当する要求 ID の範囲**（⛔ 行番号ではなく ID）
- 「⛔ **迷ったら消さずに報告せよ**」
- 「⛔ **この引継書も、指揮のブリーフも間違う。測って反証せよ**」
- ⛔ **MUST を裁定なしに消すな**（第 9-1 節）。消すべきと思ったものは**報告**
- 消すなら **6 つの観測手段のどれが捕まえるかを名指させる**。言えないなら報告
- ⛔ **`docs/spec/output/` を grep から除け**（裁定 11）。`npm run sweep` で消せる

### 前の巡で体が実際にやった仕分けの割合（Chapter 1、165 単位）

消した 4 ／ 残した 106 ／ **箱 C として報告 51** ／ 保留 4。

⇒ ⭐ **消える量は少ない。**成果は「消した行数」ではなく「**どこに在るべきかを見つけたこと**」である。⛔ 大量に消えることを期待して体を急かすな。

---

## 5. ⛔ 前の巡で踏んだ地雷 — 同じ穴に落ちるな

1. ⛔⛔ **物を動かす手は、動かした「先」でも壊す。**P2-1 で最初に赤くなった検査は、移設元のファイルを一言も書いていなかった。⇒ **移設元を grep して作った一覧では、危険の半分が原理的に見つからない。移設先を見ている検査も数えよ。**
2. ⛔ **移設先に既に同じ規則が在ると思え。**裁定 A で移した 22 の MUST のうち **6 件が既在、4 件はバイト単位で同一**だった。素直に追記していたら重複を 7 つ作っていた。
3. ⛔ **文書は互いを指している。**`spec-writing-rules.md` が「その旨を 1.9 に書く（MUST）」と定めていたため、3 行は仕様書側に残さねばならなかった。
4. ⚠️ **指し先を足す前に、既存の指し先を探せ。**「規則に従え」を書いたら原稿 8 行目に既に同じものが在り、**検査 11 が赤くなった。**
5. ⛔ **道具のコメントも腐る。**検査 39 は Chapter 1 を読まない理由を嘘で書いていた（訂正済）。
6. ⛔ **指揮のブリーフがいちばん危ない。**前の巡で、指揮は体に「`WR-1` / `WR-2` は死んだ席だ」と渡した。**両方とも生きていた。**体が測って拒否したから助かった。⇒ **毎回「反証せよ」と書け。**

---

## 6. 門の走らせ方 — ⛔ トークンを溶かすな

```bash
bash .claude/skills/spec-graph-check/check.sh > /dev/null 2>&1; echo "exit=$?"
```

| 門 | 所要 | 打ち方 |
|---|---|---|
| `check.sh`（42 本） | 約 40 秒 | 上のとおり。⛔ 出力を丸ごと受けると数千トークン溶ける |
| `npx vitest run` | 約 20 秒 | `\| grep -E "Test Files\|Tests "` だけ |
| `npx tsc --noEmit` | 数秒 | 終了コードだけ |
| `npm run stats -- --print` | 0.4 秒 | 原稿の字数を測る |
| `npx playwright test` | **8 分** | ⚠️ **フェーズの最後に 1 度だけ**。2 件の赤は元から |

⛔ **1 つの節を刈るごとに `check.sh` を通してコミットせよ**（第 9-1 節）。検査 5〜15 は散文の員数を読んでいるので、**段落を 1 つ消すと別の行が述べる数が嘘になる。**

---

## 7. コミットと push

`gr-scheduler` では Claude が `commit` / `push` / `merge` を行ってよい（利用者の裁定 2026-08-24）。⛔ `tag` は利用者が打つ。

⛔ **コミットのたびに個人情報の点検を行い、実測した数を報告してから**コミットすること。最低限、次が 0 件であること: 個人の絶対パス ／ メールアドレス ／ 秘密 ／ 利用者名。

⚠️ 誤検出が 2 つ既知である —— **URL の `file` スキームの区切り**がドライブ文字の綴りに見えるものと、**製品名 `GoodRelax Scheduler`**（利用者名ではない）。どちらも既出であり、新たな露出ではない。

⛔ **この行がまさにその罠を踏んだ。**最初はドライブ文字の綴りを**例として書いて**しまい、検査 41（precheck の罠 5、「絶対パスを書くな」）が赤にした。⇒ ⭐ **機械は正しく、書いた者が間違っていた。**禁じられた綴りは、例として書くこともできない。

---

## 8. フェーズ 2 が終わったあと

| | 残り |
|---|---|
| **P2-3** | 結合度・行長（要求 1 件が引く他 ID は中央値 19 個、1 行が最長 3,708 字） |
| **`src/` のコメント** | **48,668 行**・68 ファイル。⛔ **`src/` を開く巡は `src/` だけをやること** |
| **`tests/` のコメント** | 59,719 行。⛔ `src/` の後 |
| **フェーズ 3** | `@purity` の検査 ／ 命名規約の検査 ／ 検査 8 の拡張（⛔ 先に赤の量を数える） |

フェーズ 2 が終わったら、第 12 節の数を測り直し、利用者に次を依頼せよ:

```bash
git tag -a After-Cleanup-P2 -m "Phase 2 of 3 -- the real cleanup" && git push origin After-Cleanup-P2
```
