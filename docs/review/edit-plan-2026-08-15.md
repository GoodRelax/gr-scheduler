# 実装着手前の編集計画 — 2026-08-15

**このファイルは計画の記録である。仕様書ではない。** 当てた結果の正は `change-request/` の CR と `A-appendix.md` の変更履歴である。

## 作り方

| | |
|---|---|
| 入力 | 利用者が決めた `L-1` 〜 `L-31`（§1 に一覧）／ `docs/review/preimplementation-review-2026-08-15.md` の `F-1` 〜 `F-6` と `C-1` 〜 `C-5` |
| 起草 | 5 つの塊を並行で —— マーカー ／ 書き出し ／ 画面のモデル ／ 画面の面と AI ／ レビューの残り |
| 検算 | 塊ごとに別のエージェントが、**引用が原文と逐語一致し、そのファイルに 1 度だけ現れること**を機械で照合。席番号の空き・数え落とし・規則違反・生の `**` の型も当てた |
| 判定 | ⭐ **5 塊すべてが `needs-fix`。** 統合の段で直し、**採らなかった 18 件を §6.3 に理由つきで残した** |
| 規模 | エージェント 11 体（エラー 0）・77 分 |
| 結果 | **CR 5 本・119 編集**（＋条件付き 2）。照合した引用 106 箇所、不一致 0 |

⚠️ **当てる先は行番号ではなく引用文字列である。** 行番号は作成時点の `restart` のものであり、1 本当てるたびにずれる。

⚠️ **`U-1` 〜 `U-20`（§7）は未決である。** 利用者の判断を得るまで当ててはならない。

⚠️ **CR の順序を崩してはならない** —— `CR-139` → `CR-140` は `CP-25` と `BO-1` で、`A-appendix.md` の変更履歴は全 CR で、それぞれ前の結果を踏む。

---

# gr-scheduler 仕様書 — 統合編集計画（2026-08-15）

**この計画は読み取りだけで作った。`docs/spec/` は 1 文字も変更していない（L-30）。**
引用（**いま**）はすべて実ファイルを開いて逐語で確かめ、**各ファイル内でちょうど 1 回だけ現れること**を機械で照合した（106 箇所、不一致 0）。行番号は現 HEAD（`restart`、作業ツリーは clean）のものであり、**当てる先は行番号ではなく引用文字列である。**

---

# 1. 決定の一覧（L-1 〜 L-31）

利用者が決めたことである。**覆さない。** 各決定を受ける編集を右端に示す。

| # | 決定（要約） | 受ける編集 |
|---|---|---|
| L-1 | 進捗マーカーは固定サイズ。`FR-094` を採り `FR-013` の追随を落とす | CR-137 §1・§2 |
| L-2 | 理由 ——「見えれば十分。複雑なロジックのできるだけ外に置きたい」 | CR-137 §2・§3 |
| L-3 | 固定値は 16px（`actualMin` と同値） | CR-137 §3 |
| L-4 | 16px は設定値として持つ。マジックナンバー禁止 | CR-137 §3・§7 |
| L-5 | 文書ごとに保存する（表 T-201 の他の寸法と同じ） | CR-137 §3（`S-22` は表 T-201 のまま） |
| L-6 | やりたいことは WYSIWYG | CR-138 §9・§10 |
| L-7 | 出力は 1600 × 900 固定。A3 横 / A4 横は廃止 | CR-138 §11・§12・§14 |
| L-8 | 由来は標準画面（`MC-6`）と同じ 16 : 9 | CR-138 §13 |
| L-9 | 1600 × 900 も設定値として持つ | CR-138 §14 |
| L-10 | 横 ＝ 画面に映している期間（`scrollDate` と `zoomX`） | CR-138 §11 |
| L-11 | `Time Ruler` と `Row Title Panel` は出力に入れる | CR-138 §11（`EP-2` / `EP-4`） |
| L-12 | `Properties Panel` と `App Header` は入れない | CR-138 §11（`EP-1` / `EP-6`） |
| L-13 | 行見出しパネルの操作子は入れない | CR-138 §11（`EP-3`） |
| L-14 | 畳んだ行の印は描かない | CR-138 §11（`EP-3`） |
| L-15 | `Panel Divider` と `Scrollbars` は入れない。境界は線 1 本 | CR-138 §11（`EP-8`）・§15 |
| L-16 | `Resource Roster` は出力しない | CR-138 §11（`EP-7`） |
| L-17 | 収まらない分は `TaskGroup` 単位で落とす。規則は `FR-025` | CR-138 §12 |
| L-18 | 描かなかった件数を知らせる | CR-138 §12 |
| L-19 | 入る領域の全数を行 ID を振った表にする | CR-138 §11（表 T-076） |
| L-20 | 書き出し専用のパネル幅は新設しない（`S-79` をそのまま） | CR-138 §11（`EP-2`） |
| L-21 | 透かしはタスクエリア限定 | CR-138 §2・§3・§4 |
| L-22 | 画面全体のモデルを持ち `Entity` 層に置く | CR-139 §1・§2 |
| L-23 | 構成は案 C（2 つに割って既存の小層へ） | CR-139 §6 |
| L-24 | `ScreenRegions` → `layoutEngine` | CR-139 §6（`CP-35`） |
| L-25 | `ScreenState` → `documentModel` | CR-139 §6（`CP-36`） |
| L-26 | 根拠 —— 書き出しが文書だけの純粋関数になり、境目が 2 小層の境目と一致 | CR-139 §31（`MN-7`） |
| L-27 | GRS がやるのは ①AI が操作できる API ②GRS 内で AI とチャットする UI | CR-139 §6（`CP-37` / `CP-38`）・§10 |
| L-28 | 発話の中身を作るのは AI であって GRS ではない | CR-139 §4・§8（`CP-18` から発話を外す） |
| L-29 | 参照 —— `ai-cowork-trial` | （編集なし） |
| L-30 | 立案完了まで `docs/spec/` を変更しない | 本計画は読み取りのみ |
| L-31 | レビュー報告書は `docs/review/preimplementation-review-2026-08-15.md` | CR ごとに参照 |

---

# 2. CR の割り方と順序

## 2.1 割り方の原則

1. **同じセルを 2 つの CR が開かない。** これを満たすために、`CP-25`（`SingleHtmlShell` の責務）は **CR-140 の 1 編集にまとめ**、CR-139 では触らない。画面のモデル（旧塊 S）と面のコンポーネント（旧塊 A）は数の主張 9 箇所を共有するので **1 本に統合**した。
2. **落ちたときの戻し方が同じものを 1 本にする。** 要求だけの CR、設計だけの CR、道具を含む CR を混ぜない（CR-137 の道具 3 行だけは例外。理由は U-17）。
3. **番号は席番号。** 版は `0.30` の次から連番、CR は `CR-136` の次から連番。

## 2.2 順序と大きさ

| 順 | CR | 版 | 表題 | 触るファイル | 編集数 | 依存 |
|:-:|---|---|---|---|:-:|---|
| 1 | **CR-137** | 0.31 | 進捗マーカーを固定サイズにする | `01-04` / `tbl-settings` / `tbl-glossary` / `A-appendix` / 道具 3 本 | **12**（＋U-4 で 1） | なし |
| 2 | **CR-138** | 0.32 | 書き出しの範囲と出力サイズ | `01-04` / `tbl-settings` / `A-appendix` | **16** | なし |
| 3 | **CR-139** | 0.33 | 画面のモデルと、画面の面のコンポーネント | `05-07` / `tbl-glossary` / `model.json` / `audit-ch5.py` / `A-appendix` | **58**（§A 22・§B 25・共有 11） | CR-138（`CP-21` が表 T-076 を指す） |
| 4 | **CR-140** | 0.34 | 起動と描き直しの順序 | `05-07` / `01-04` / `A-appendix` | **14** | CR-139（`CP-25` が矩形を配る・`BO-1` が `ScreenRegions` を指す） |
| 5 | **CR-141** | 0.35 | Chapter 4 の値と経路 | `01-04` / `tbl-settings` / `erd.json` / `05-07`（1 行） / `A-appendix` | **19** | なし（`A-appendix` の追記位置のみ順に従う） |

**合計 119 編集（＋条件付き 2）。**

## 2.3 同じ行を取り合わないことの確認（セル単位で当て直した）

| ファイル | CR-137 | CR-138 | CR-139 | CR-140 | CR-141 |
|---|---|---|---|---|---|
| `01-04-requirements.md` | 1044 / 1058 / 2036 / 3919 | 757 / 858-859 / 2298 / 2527 / 2711 / 2715 / 2745 / 2746 / 2749 / 2985 / 2998 / 3000 / 3016 / 3022 | — | 3723 | 188 / 196 / 548 / 1298 / 1933 / 1943 / 2251 / 2417 / 2423 / 2436 / 2440 |
| `tbl-settings.md` | 39 / 69-70 / 125 | 190 | — | — | 319 / 325 |
| `tbl-glossary.md` | 173-174 | — | 131-132（U-50 の**次**を掴む） | — | — |
| `05-07-design.md` | — | — | 47/48/84/107/110/123/173/181/183/187/199/205/206/213/216/219/221/232/238/282/300/328/331/344/348/361/542/553/554/557/559/561/569/575/578 | 90/98/114/290/291/311/405/413/417/526/528/576 | 469 |
| `A-appendix.md` | 末尾に 1 行 | その次 | 57 ＋ その次 | その次 | その次 |

**交差は 1 セルも無い。** `A-appendix.md` の変更履歴だけは 1 行ずつ順に積むので、**必ずこの順で当てること。**

---

# 3. CR ごとの編集一覧

## CR-137（版 0.31）— 進捗マーカーを固定サイズにする

**閉じるもの**: レビュー **F-1**（同じ要素に相反する 2 つの MUST）。

### 編集 1 — `FR-094` に寸法の持ち主を名指しさせる

**場所**: `docs/spec/01-04-requirements.md:1044`
**いま**:
```
**STATEMENT**: `GRS` は、日程表を描くときの寸法 —— バーの縦幅・形状ごとの縦幅の比・段と行の間隔・依存線・進捗マーカーと再開アイコン・ラベル・形状の細部・イナズマ線・余白 —— を、**`_assets/tbl-settings.md` の表 T-201 に従って決めること（MUST）。** 同表に無い寸法を実装が独自に持ってはならない（MUST NOT）。**依存線と進捗マーカーと再開アイコンの寸法は、ズームに追随させてはならない（MUST NOT）** —— 低いズームで潰れると、線の種別も状態の記号も読めなくなる。
```
**こうする**:
```
**STATEMENT**: `GRS` は、日程表を描くときの寸法 —— バーの縦幅・形状ごとの縦幅の比・段と行の間隔・依存線・進捗マーカーと再開アイコン・ラベル・形状の細部・イナズマ線・余白 —— を、**`_assets/tbl-settings.md` の表 T-201 に従って決めること（MUST）。** 同表に無い寸法を実装が独自に持ってはならない（MUST NOT）。**依存線と進捗マーカーと再開アイコンの寸法は、ズームに追随させてはならない（MUST NOT）** —— 低いズームで潰れると、線の種別も状態の記号も読めなくなる。 進捗マーカーの径は表 T-201 の `S-22` が固定値で持ち、再開アイコンの寸法は同表の `S-25` 〜 `S-29` が持つ。
```
**理由**: L-1 / L-3 / L-4。新しい MUST を作らず、持ち主を名指しするだけにする（1.9 の :283）。

### 編集 2 — `FR-013` の寸法の段落を指しに置き換える

**場所**: `docs/spec/01-04-requirements.md:2036`
**いま**:
```
**進捗マーカーの大きさは実績バーに追随させ、実績バーより大きく描いてはならない（MUST NOT）。** ⚠️ **予定だけを表示していて実績バーを描いていないときも、径の基準は実績バーの幾何とすること（MUST）** —— 描くかどうかと寸法の基準は別である。表示の切り替えでマーカーの大きさが変わってはならない（MUST NOT）。 ⚠️ **`markerOfFont` × `fontOfActual` が 1 を超えるとこの禁止を破る** —— 径はフォント基準、フォントは実績基準なので鎖が掛かる。上限は `_assets/tbl-settings.md` の `S-21` が持つ。 低いズームでは実績バーが数 px まで縮むので、**追随させないと注釈が本体より大きくなり、何の印なのか読めなくなる。**
```
**こうする**:
```
**進捗マーカーの寸法は `FR-094` が持つ。本要求は寸法を持たない。**
```
**理由**: L-1 / L-2 / U-2。⚠️ **検算の直し**: 塊 M の案は「本要求は記号と位置と巡回だけを定める」と書いていたが、`FR-013` は `:2021` の `PM-4` 優先・`:2032` の「人が置いた」・`:2034` の色の禁止も持ったまま残るので、全数の主張になって偽になる。**否定形に替えた。**

### 編集 3 — `S-21` を欠番にし、`S-22` を `markerSize` に改める

**場所**: `docs/spec/_assets/tbl-settings.md:69-70`（2 行を 1 行に置換）
**いま**:
```
| S-21 | 進捗マーカー | `markerOfFont` | × | 1.2 🔎 | 0.5 | 1 ÷ `fontOfActual` | **`markerOfFont` × `fontOfActual` が 1 を超えると、マーカーが実績バーより大きくなり `FR-013` の MUST NOT を破る。** 既定値 1.5 と `fontOfActual` 0.80 の組は 1.2 倍で破っていた |
| S-22 | 進捗マーカー | `markerMin` | px | 16 🔎 | `fontMin` | `actualMin` | **実績バーより大きくできない**（`FR-013`）。`S-21` は掛け算の鎖を塞いでいるが、下限どうしの鎖はこの上限が塞ぐ |
```
**こうする**:
```
| S-22 | 進捗マーカー | `markerSize` | px | 16 | `fontMin` | `actualMin` | 固定値。縦の鎖に載らない（規則は `FR-094`）。上限が `actualMin` なのは、実績バーの床と同じ値までなら実績バーより大きくならないためである。下限が `fontMin` なのは、記号がこれを割ると読めないためである |
```
**理由**: L-3 / L-4 / L-5 / U-1（席を保って改名）。🔎 を外すのは 16px に由来が付くためで、代わりに編集 7 で `RC-11` が受ける。**この欄に MUST / MUST NOT は 1 つも無い**（検査 12）。

### 編集 4 — 「根は 4 つ」に鎖の外の但し書きを足す

**場所**: `docs/spec/_assets/tbl-settings.md:39`
**いま**:
```
**根は 4 つである**（`basePlanHeight` / `actualMin` / `fontMin` / `pxPerDayAt1x`）。残りはすべてそこから落ちてくる。
```
**こうする**:
```
**根は 4 つである**（`basePlanHeight` / `actualMin` / `fontMin` / `pxPerDayAt1x`）。残りはそこから落ちてくる。**進捗マーカーの径（`S-22`）だけは鎖の外の固定値である** —— 規則と理由は `FR-094` が持つ。
```
**理由**: 規則 9。「4」は動かないが「残りはすべて」が偽になる。

### 編集 5 — `S-63` に `FR-094` への指しを足す

**場所**: `docs/spec/_assets/tbl-settings.md:125`
**いま**:
```
| S-63 | `progressMarkerVisible` | 真偽 | `true` | 進捗マーカー |
```
**こうする**:
```
| S-63 | `progressMarkerVisible` | 真偽 | `true` | 進捗マーカー。寸法をズームに追随させない規則は `FR-094` が持つ |
```
**理由**: U-3。`S-62` と**同型の指し**であり、新しい主張を 1 つも作らない（F-1 が挙げた非対称の解消）。

### 編集 6 — `K-21` を欠番にし、`K-22` を改める

**場所**: `docs/spec/_assets/tbl-glossary.md:173-174`（2 行を 1 行に置換）
**いま**:
```
| K-21 | 進捗マーカー | `markerOfFont` | マーカー径 ÷ フォント |
| K-22 | 進捗マーカー | `markerMin` | マーカー径の下限 |
```
**こうする**:
```
| K-22 | 進捗マーカー | `markerSize` | マーカー径 |
```
**理由**: 名前の正は 表 T-104。値と同時に直す（tbl-settings.md:6「名前と値を 2 か所で管理しない」）。

### 編集 7 — 表 T-026 に `RC-11` を足す

**場所**: `docs/spec/01-04-requirements.md:3919`（ファイル末尾の行）
**いま**:
```
—— 表現力を捨てると、後から探索方式へ戻す道が閉じる |
```
**こうする**:
```
—— 表現力を捨てると、後から探索方式へ戻す道が閉じる |
| RC-11 | `markerSize` = 16px（`S-22`。`actualMin` と同値に置いた） | **実物で読めるかどうかを見ていない。** 由来は「実績バーの床と同じ値なら比較が要らない」ことであって、視認性の実測ではない | 径が決まらないと進捗マーカーを描けない。外すのではなく**選び直す**対象である | **残す。骨格で実物を見て選び直す** |
```
**理由**: L-4。`RC-1` は「🔎 付き既定値の全件」を受けているので、🔎 を外した行は明示的に受け直す必要がある。

### 編集 8 — 検査 12 の例外から `S-21` / `S-22` を外す

**場所**: `.claude/skills/spec-graph-check/style-checks.py:38-41`
**いま**:
```
CHECK12_ALLOWED = {
    'S-21',    # the reason a RANGE exists, not a rule about behaviour
    'S-22',    # same: the upper bound's own justification
}
```
**こうする**:
```
CHECK12_ALLOWED = set()
```
**理由**: 編集 3 で `S-21` の行が消え、`S-22` の欄から MUST の字面も消えるので、例外が 2 つとも死ぬ。**残すと、将来この行に MUST が紛れ込んでも検査が黙る。**

### 編集 9 — 廃止した席を検査 7 の既知集合に足す

**場所**: `.claude/skills/spec-graph-check/md-checks.py:157-159`
**いま**:
```
RETIRED = {'FR-050', 'T-030',
           'T-044', 'T-045', 'T-046', 'T-047',
           'F-002', 'F-003', 'F-004', 'F-005', 'F-006', 'F-007'}
```
**こうする**:
```
RETIRED = {'FR-050', 'T-030',
           'T-044', 'T-045', 'T-046', 'T-047',
           'F-002', 'F-003', 'F-004', 'F-005', 'F-006', 'F-007',
           'S-21', 'S-52', 'K-21'}
```
**理由**: ⛔ **検算で見つかった落ち。** 編集 12 の変更履歴が `S-21` / `K-21` / `S-52` をバッククォート付きで書くので、当てた直後に **検査 7 が 3 件で落ちる**（実測）。版 0.6 が `FR-050` を欠番として書いたときと同じ扱いにする（U-17）。

### 編集 10 — 同じ集合を `specindex.py` にも足す

**場所**: `.claude/skills/spec-graph-check/specindex.py:51`
**いま**:
```
RETIRED = {'FR-050', 'T-030'}
```
**こうする**:
```
RETIRED = {'FR-050', 'T-030', 'S-21', 'S-52', 'K-21'}
```
**理由**: 同上。`impact.py` / `graph.py` が同じ索引を使う。

### 編集 11 —【U-4 を採るときだけ】`FR-094` に縦の床の当て方を置く

**場所**: `docs/spec/01-04-requirements.md:1058`（`**Relations**:` は :1060。**前に入る**）
**いま**:
```
⚠️ **本要求は新しい規則を作らない。** 1.9 の「細目は表に置き、要求は 1 文でその表を指す」を、表 T-201 にも適用しただけである。
```
**こうする**:
```
⚠️ **寸法の正を表 T-201 に置くことは、1.9 の「細目は表に置き、要求は 1 文でその表を指す」を同表にも適用しただけである。**

**縦の寸法を止める床は、形状の比を掛ける前の予定の縦幅に 1 度だけ当てること（MUST）。実績の縦幅に別の床を当ててはならない（MUST NOT）** —— 2 か所で止めると、止まった側と止まらない側で実績 ÷ 予定が `S-5` から離れる。床の値は `S-4` の下限である。**文字の床（`S-8`）は別に当てる** —— 細線の倍率（`S-9`）が掛かるので、比のままでは可読の下限を割る。床に届いてなお縮めるときの扱いは `FR-077` が持つ。
```
**理由**: U-4。**これは L-3 の根拠そのものである** —— 縦の鎖に max() を当てる規則が現在の仕様書に 1 文も無い。⚠️ **検算の直し 2 件**: ①「本要求が作る規則は 1 つだけである」は数の主張になって偽なので落とした ②「床の値は `S-4` の下限（`actualMin` ÷ `actualOfPlan`）である」の括弧は 表 T-201 の写しなので落とした（1.9 の :285）。

### 編集 12 — 変更履歴に版 0.31 を足す

**場所**: `docs/spec/A-appendix.md:58`（末尾の行）
**いま**:
```
**わざと壊して落ちることを 2 件で確かめた** |
```
**こうする**:
```
**わざと壊して落ちることを 2 件で確かめた** |
| 0.31 | 2026-08-15 | ⭐ **進捗マーカーを固定サイズにした**（CR-137。利用者の裁定）—— 実装着手前レビューの **F-1**（同じ要素に相反する 2 つの MUST）を閉じた。**`FR-094`（ズームに追随させない）を採り、`FR-013` の「実績バーに追随させ」を落とした。** 径は表 T-201 の `S-22` が持つ固定値 16px とし、**鍵を `markerMin` から `markerSize` へ改めた（席は据え置き）。** 16px は `actualMin` と同値であり、**実績バーの床と一致する。** ⚠️ **`markerOfFont`（`S-21` / `K-21`）は使い手が無くなったので廃止し、番号は席番号なので欠番のまま残した**（`S-52` と同じ扱い）—— `FR-024` が全項目を書き出すと定めている以上、使い道の無い鍵を残すと全文書に永久に載る。`S-63` に `FR-094` への指しを足して `S-62` との非対称を消し、表 T-026 に `RC-11`（16px を実物で選び直す）を足した。**検査の道具を 3 行直した** —— 検査 12 の例外から `S-21` / `S-22` を外し、廃止した 3 席を検査 7 の既知集合へ入れた |
```
**理由**: CR 過程の決まり（レビュー報告書 :3「仕様書を直したときは CR と変更履歴が正である」）。

---

## CR-138（版 0.32）— 書き出しの範囲と出力サイズ

**閉じるもの**: L-6 〜 L-21。**新しい確定名 0・新しい設定値のキー 0。**

### 編集 1 — UC-011 拡張 3a を切り落としに改める

**場所**: `docs/spec/01-04-requirements.md:858-859`
⚠️ **2 行目は行頭に半角空白 2 つが要る。** 落とすと置換に失敗する。
**いま**:
```
- 3a. 内容が出力サイズに収まらない。
  - `GRS` は縮小して収める。文字が下限を割るときは警告する。
```
**こうする**:
```
- 3a. 内容が出力サイズに収まらない。
  - `GRS` は縮小せず、収まらない `TaskGroup` を描かずに、描かなかった件数を知らせる。
```
**理由**: L-7 / L-17 / L-18。`FR-025` の ORIGIN は「UC-011 手順 3 〜 4、拡張 3a」なので、出どころを直さないと要求と食い違う。

### 編集 2 — UC-009 手順 2 の透かしの範囲を限る

**場所**: `docs/spec/01-04-requirements.md:757`
**いま**:
```
2. `GRS` は、開いた者の名前と実行時の日時を薄く斜めに、画面全体へ繰り返し重ねる。
```
**こうする**:
```
2. `GRS` は、開いた者の名前と実行時の日時を薄く斜めに、`Row Area` へ繰り返し重ねる。
```
**理由**: ⛔ **検算で見つかった落ち。** 塊 X は `FR-020`（:2711）だけを直していたが、`FR-020` の ORIGIN は「UC-009 手順 1 〜 4、拡張 4a」であり、**編集 1 が自分で立てた基準（出どころを直さないと食い違う）がここにも掛かる。**

### 編集 3 — `FR-020` の透かしを `Row Area` に限る

**場所**: `docs/spec/01-04-requirements.md:2711`（STATEMENT の先頭 1 文。行の残りは触らない）
**いま**:
```
**STATEMENT**: `GRS` は、開いた者の名前と実行時の日時を、画面全体へ斜めに繰り返し薄く重ねること。
```
**こうする**:
```
**STATEMENT**: `GRS` は、開いた者の名前と実行時の日時を、`Row Area`（`_assets/tbl-glossary.md` の `U-50`）の全面へ斜めに繰り返し薄く重ねること。
```
**理由**: L-21。「タスクエリア」は確定名ではないので `Row Area`（`U-50`）で書く。L-6 により画面と書き出しで範囲を変えられないので、要求そのものを限る（U-15 も参照）。

### 編集 4 — 限定の理由を `FR-020` の RATIONALE に置く

**場所**: `docs/spec/01-04-requirements.md:2715`（RATIONALE の末尾）
**いま**:
```
**既定の透かし解除パスワードを定め、その SHA-256 だけを成果物へ定数として埋め込むこと（MUST）。** 値は `_assets/tbl-settings.md` の表 T-207 が持つ。
```
**こうする**:
```
**既定の透かし解除パスワードを定め、その SHA-256 だけを成果物へ定数として埋め込むこと（MUST）。** 値は `_assets/tbl-settings.md` の表 T-207 が持つ。 **重ねる範囲を `Row Area` に限るのは、そこが秘密の日程を映す領域だからである。** 画面の他の領域は道具の操作面であり、書き出しにも入らない（表 T-076）。**範囲を限っても `LM-1` と `LM-8` は変わらない** —— 消せることも、書き出した画像に開いた者の名前が載ることも、範囲の広さでは決まらない。
```
**理由**: 1.9 の :283（規則と理由はそれを課す要求が持つ）。⚠️ の確認事項（`WY-2` と `LM-1` に効くか）をここで閉じる。

### 編集 5 — 表 T-024 の `IO-3` から全数表へ道を通す

**場所**: `docs/spec/01-04-requirements.md:2745`
**いま**:
```
| IO-3 | SVG | 書出のみ | 画面の出力 | 出力サイズは表 T-204 の `S-81` |
```
**こうする**:
```
| IO-3 | SVG | 書出のみ | 画面の出力 | 出力サイズは表 T-204 の `S-81`。入る領域と入らない領域の全数は表 T-076 |
```
**理由**: L-19。

### 編集 6 — `IO-4` を `IO-3` へ指す形にする

**場所**: `docs/spec/01-04-requirements.md:2746`
**いま**:
```
| IO-4 | PNG | 書出のみ | 画面の出力 | 倍率は表 T-204 の `S-82` |
```
**こうする**:
```
| IO-4 | PNG | 書出のみ | 画面の出力 | 倍率は表 T-204 の `S-82`。出力サイズと入る領域は `IO-3` と同じ |
```
**理由**: L-7 / L-19 / 1.9（2 度書かない）。

### 編集 7 — `IO-6` の用途から「現在の画面」を外す

**場所**: `docs/spec/01-04-requirements.md:2749`
**いま**:
```
| IO-6 | クリップボード | 書出のみ | 現在の画面を画像として他のアプリへ渡す | ダウンロードを経由しない。資料へ貼る用途 |
```
**こうする**:
```
| IO-6 | クリップボード | 書出のみ | 日程を画像として他のアプリへ渡す | ダウンロードを経由しない。資料へ貼る用途。出る絵は `IO-3` / `IO-4` と同じ（`FR-025`） |
```
**理由**: L-7 / L-12 / L-15。出力は `exportCanvas` 固定で `App Header` を含まないので「現在の画面」は誤りになる。

### 編集 8 — `FR-077` の指す先を作り直す

**場所**: `docs/spec/01-04-requirements.md:2527`
**いま**:
```
**RATIONALE**: 縦のズームを縮めると、タスクの高さに比例させた文字はいくらでも小さくなる。**読めない文字で埋まった画面は、情報量が多いのではなく 0 である。** **`FR-025` が扱うのは書き出しのときの下限であり、本要求は画面表示側の下限を定める。**
```
**こうする**:
```
**RATIONALE**: 縦のズームを縮めると、タスクの高さに比例させた文字はいくらでも小さくなる。**読めない文字で埋まった画面は、情報量が多いのではなく 0 である。** **書き出しは画面と同じ寸法系で描く**ので（`FR-080`）、本要求の下限がそのまま書き出しにも掛かる。**書き出しのために文字を縮めてはならない（MUST NOT）** —— 縮めると、画面で読めた文字が画像で読めなくなる。
```
**理由**: `FR-025` から縮小が消えると、この文が指す先を失う。**編集 12 と必ず同時に入れること。**

### 編集 9 — `FR-080` の STATEMENT を表 T-076 の射程に限る

**場所**: `docs/spec/01-04-requirements.md:2985`
**いま**:
```
**STATEMENT**: 画像として書き出したとき、`GRS` は、**画面に見えているものと同じ絵**を出すこと。表示の切り替え・ズームの段階・LOD による増減の結果を、書き出しでも同じにすること。
```
**こうする**:
```
**STATEMENT**: 画像として書き出したとき、`GRS` は、**表 T-076 が「入る」と定めた領域について、画面に見えているものと同じ絵**を出すこと。表示の切り替え・ズームの段階・LOD による増減の結果を、書き出しでも同じにすること。**入る領域と入らない領域の全数は表 T-076 に従うこと（MUST）。縦に収まらない分の扱いは `FR-025` が持つ。**
```
**理由**: L-11 〜 L-16 / L-19。⚠️ **検算の直し**: 除外句「縦に収まらない分の扱いは `FR-025` が持つ」を足した。無いと、`EP-5`（`Row Area` は入る＝画面と同じ）と `FR-025`（収まらない `TaskGroup` を描かない）の 2 つの MUST が同時に立たない。

### 編集 10 — `WY-2` から全体表示を外す

**場所**: `docs/spec/01-04-requirements.md:2998`
**いま**:
```
| WY-2 | **同じ JSON を読み込み、全体表示（`FR-055`）を行ってから書き出したとき、透かしの層を除いた描画が同じ SVG / PNG になること** | **透かしの層**（`FR-020`）。開いた者の名前と実行時の日時を載せるので、実行のたび・機ごとに必ず変わる |
```
**こうする**:
```
| WY-2 | **同じ JSON を読み込んで書き出したとき、透かしの層を除いた描画が、機と画面の寸法によらず同じ SVG / PNG になること** | **透かしの層**（`FR-020`）。開いた者の名前と実行時の日時を載せるので、実行のたび・機ごとに必ず変わる |
```
**理由**: L-7 / L-9 / L-10。全体表示は画面の寸法から倍率を出すので、判定に噛ませると機ごとに違う絵が出て `WY-2` 自身が壊れる。**除外欄は 1 文字も変えない**（透かしを `Row Area` に限っても正しいまま）。

### 編集 11 — `FR-080` に出力の幾何を置き、表 T-076 を新設する

**場所**: `docs/spec/01-04-requirements.md:3000`（`**Relations**:` は :3002。⚠️ **必ずその前に入れる**）
**いま**:
```
⚠️ **`WY-1` は JSON が縦横の倍率と縦横の表示位置を持つことを要する** —— `zoomX` / `zoomY`（`S-75` / `S-76`）と `scrollDate` / `scrollGroupId`（`S-77` / `S-78`）がそれに当たり、`FR-024` が全項目を書き出すと定めているので満たされる。**この 4 つを保存対象から外してはならない（MUST NOT）。スクロール位置を px で持ってはならない（MUST NOT）** —— ズームや画面幅が変わると別の場所を指すので、日付と行の識別子で持つ。`FR-025` は出力サイズと文字サイズを文書に保存することまでを定めていたが、**見た目が一致することを求める要求が無かった。** 収まらないときに縮小して収める（`FR-025`）のは倍率の話であり、**描くものの取捨選択を変えてよいという意味ではない。**
```
**こうする**:
```
⚠️ **`WY-1` は JSON が縦横の倍率と縦横の表示位置を持つことを要する** —— `zoomX` / `zoomY`（`S-75` / `S-76`）と `scrollDate` / `scrollGroupId`（`S-77` / `S-78`）がそれに当たり、`FR-024` が全項目を書き出すと定めているので満たされる。**この 4 つを保存対象から外してはならない（MUST NOT）。スクロール位置を px で持ってはならない（MUST NOT）** —— ズームや画面幅が変わると別の場所を指すので、日付と行の識別子で持つ。

**出力の寸法は `exportCanvas`（`_assets/tbl-settings.md` の表 T-204 の `S-81`）に固定し、実行時の画面の寸法で変えてはならない（MUST NOT）** —— 画面の寸法で変えると、同じ JSON でも機ごとに違う絵が出て `WY-2` が成立しない。**出力の左端は `scrollDate`（`S-77`）、上端は `scrollGroupId`（`S-78`）とし、横に映る期間は出力の `Row Area` の幅と `zoomX` から求めること（MUST）。表示位置が表 T-024a の `OP-10` に当たるときは、`exportCanvas` に対して全体表示が選ぶ位置とすること（MUST）** —— 画面の寸法に対して選ぶと、機ごとに違う位置になる。⚠️ **出力の幅は画面の幅と同じではないので、横に映る期間も画面と同じにはならない。** 同じにするのは倍率・表示位置・表示の切り替え・LOD の結果である。縁の余白は `svgPadding`（表 T-201 の `S-57`）である。**これにより、書き出しは文書だけから決まる。**

**領域の名前の正は `_assets/tbl-glossary.md` の表 T-103 である。**

**表 T-076 — 書き出しに入る領域と入らない領域**

| 行 ID | 領域 | 出力 | 規則と理由 |
| --- | --- | :--: | --- |
| EP-1 | `App Header`（`U-31`）と、そこに置く `Document Title`（`U-27`）・`Autosave Status`（`U-28`）・`Header Commands` / `Branding`（`U-35`） | 入らない | 道具の操作面であって日程ではない。**出力でこの高さを取ってはならない（MUST NOT）** —— 取ると、同じ倍率でも画面より縦に狭くなる |
| EP-2 | `Row Title Panel`（`U-22`）と `Row Title Tree`（`U-23`） | 入る | **幅は表 T-203 の `S-79` をそのまま使うこと（MUST）。書き出し専用の幅を持ってはならない（MUST NOT）** —— 別の幅を持つと、同じ行見出しが画面と出力で違う幅と違う打ち切り方になる |
| EP-3 | `Row Title Panel` に置く操作子 —— `Row Expander`（`U-47`）・`Row Pin`（`U-48`）・`Hidden Group Tab`（`U-29`） | 入らない | 押す先が無い。**畳んでいることも隠していることも、別の印で描いてはならない（MUST NOT）** —— 開く手段が出力に無いので、印だけが残る。畳んだ先まで要るときは `GRS JSON` を渡す（表 T-024 の `IO-2`） |
| EP-4 | `Time Ruler`（`U-19`） | 入る | 目盛の段階は `zoomX` が決める（`FR-017`）ので、画面と同じ段階になる |
| EP-5 | `Row Area`（`U-50`）に描くもの | 入る | 何を描くかは表示の切り替え（表 T-202）と LOD（`FR-018`）が決め、画面と同じにする規則は本要求の STATEMENT が持つ。**縦に収まらない分の扱いは `FR-025` が持つ** |
| EP-6 | `Properties Panel`（`U-25`） | 入らない | 選んでいるものに依る面であり、日程ではない。**描かないので、出力の `Row Area` の右端は `svgPadding` の内側の右端とすること（MUST）** |
| EP-7 | 日程に重ねる面 —— `Command Palette`（`U-26`）・`Help Modal` / `AI Export Modal`（`U-30`）・`Resource Roster`（`U-49`）・`Dialogue Field`（`U-44`）と、知らせ（`FR-076`） | 入らない | いずれも操作の面である。名簿が要るときは `GRS JSON` か MSPDI XML を渡す（表 T-024 の `IO-1` / `IO-2`） |
| EP-8 | `Panel Divider`（`U-24`）と `Scrollbars`（`U-21`） | 入らない | 掴む先が無い。**`EP-2` と `EP-5` の境目には、`Group Grid Lines`（`U-18`）と同じ太さと色の線を 1 本引くこと（MUST）。`groupGridLinesVisible`（`S-68`）が `false` でも引くこと（MUST）** —— 引かないと行見出しと日程が地続きに見える。**帯の幅を取ってはならない（MUST NOT）** |
| EP-9 | `Watermark`（`U-20`） | 入る | 重ねる範囲と書式は `FR-020` が持つ |

**本表の 9 行は、背面をなす 5 つ（`EP-1`・`EP-2`・`EP-4`・`EP-5`・`EP-6`）と、その上に重なる 4 つ（`EP-3`・`EP-7`・`EP-8`・`EP-9`）に分かれる。**
```
**理由**: L-7 / L-9 〜 L-16 / L-19 / L-20。⭐ L-15 は `Panel Divider` に代わる名前を作らず、**領域の境目の属性**として書く（表 T-103 に 52 行目が要らない）。
⚠️ **検算の直し 4 件**: ①`OP-10`（表示位置が `null`）の扱いを書いた —— `S-77` / `S-78` の既定は `null` なので、書かないと既定の文書で `WY-2` が成立しない ②`EP-2` の理由から「横の表示期間が画面と変わる」を落とした —— `EP-6` が `Properties Panel` を落とす以上、幅も期間も画面とは変わるので、`EP-2` が自らを破っていた ③「表 T-103 を余さず割っている」の全数の主張を落とした —— `U-37` / `U-40` / `U-42` / `U-43` / `U-45` / `U-46` の 6 行がどの区分にも当たらず偽である ④ 表番号を `T-077` から **`T-076`** に改めた（CR-138 が先に当たるため）。

### 編集 12 — `FR-025` を固定・切り落とし・件数の知らせに書き替える

**場所**: `docs/spec/01-04-requirements.md:3016`
**いま**:
```
**STATEMENT**: 作成者が画像を書き出したとき、`GRS` は、表 T-024 の `IO-3`・`IO-4`・`IO-6` に従って出力し、出力サイズと文字サイズを文書に保存すること。書き出す前に出力サイズと倍率を表 T-204 の `S-81` / `S-82` から選べるようにすること（MUST）。収まらない場合は縮小して収め、**文字が下限を割るときは警告すること（MUST）。**
```
**こうする**:
```
**STATEMENT**: 作成者が画像を書き出したとき、`GRS` は、表 T-024 の `IO-3`・`IO-4`・`IO-6` に従って出力し、出力サイズと文字サイズを文書に保存すること。**出力サイズは表 T-204 の `S-81` に固定し、選ばせてはならない（MUST NOT）。** 書き出す前に PNG の倍率を表 T-204 の `S-82` から選べるようにすること（MUST）。**縦に収まらないときは、上から順に丸ごと収まる `TaskGroup` までを描き、最初に収まらなかった `TaskGroup` とそれ以降を描かないこと（MUST）。`TaskGroup` の途中で切ってはならない（MUST NOT）** —— 途中で切ると、その `TaskGroup` に積んだ `Task` の一部だけが出て、積み順（`FR-003`）が読めなくなる。**縮小して収めてはならない（MUST NOT）** —— 縮小すると画面と別の寸法系になり、`FR-080` が破れる。**描かなかった `TaskGroup` と `Task` の件数を知らせること（MUST）。作法は表 T-037 の `NT-5` に従うこと（MUST）。**
```
**理由**: L-7 / L-17 / L-18。選択肢が 1 つになるので「選べるようにする」を「固定し、選ばせない」へ反転させる。⚠️ **検算の直し**: 「出力サイズと文字サイズを文書に保存すること」を**残した** —— UC-011 手順 4 がこれを出どころとして持っており、落とすと手順 4 が宙に浮く。固定と保存が両立する理由は編集 13 が書く。文字の下限の警告は落とす（U-2 ではなく U-16 の別件。理由は編集 8 で `FR-077` が受ける）。

### 編集 13 — `FR-025` の RATIONALE に固定・保存・切り落としの理由を置く

**場所**: `docs/spec/01-04-requirements.md:3022`
**いま**:
```
**RATIONALE**: サイズを文書に保存するのは、同じ JSON から同じ出力を得るためである。
```
**こうする**:
```
**RATIONALE**: サイズを文書に保存するのは、同じ JSON から同じ出力を得るためである。**選ばせずに固定するのも同じ理由である** —— 選べると、同じ JSON から選び方の数だけ絵が出る。**固定と保存は両立する** —— 書き出すたびに選ばせないだけであって、値そのものは見せ方の群として文書が持つ（`FR-063`）。**既定の出力サイズは、表 T-025 の `MC-6` が定める基準環境の画面と同じ縦横比である。** **縮小ではなく `TaskGroup` 単位で落とすのは、縮小が画面と別の寸法系を作るからである** —— 資料に要る範囲さえ画面のまま出れば足り、残りが要るときは `GRS JSON` を渡せばよい（表 T-024 の `IO-2`）。
```
**理由**: L-8 / L-14 / L-17。⚠️ **検算の直し 2 件**: ① 塊 X は 由来を 表 T-204 の下に散文で置いていたが、1.9 の :283 が値の表に認める例外は「範囲の理由」列と 🔎 の注だけなので、**由来を要求の RATIONALE へ移した** ②「1920 × 1080 と同じ 16 : 9」の**値の写しをやめ**、`MC-6` を指すだけにした（1.9 の :285）。

### 編集 14 — `S-81` から選択肢を落とす

**場所**: `docs/spec/_assets/tbl-settings.md:190`
**いま**:
```
| S-81 | `exportCanvas` | `{ width, height }` | `1600 × 900` | SVG / PNG の出力サイズ。選べる寸法は `1600 × 900`（既定）／ A3 横 ／ A4 横 ／ 現在の画面 |
```
**こうする**:
```
| S-81 | `exportCanvas` | `{ width, height }` | `1600 × 900` | SVG / PNG の出力サイズ。固定であり、選択肢を持たない。規則と理由は `FR-025` が持つ |
```
**理由**: L-7 / L-9。**MUST を書かず `FR-025` へ指すだけにする**（1.9 の :283。検査 12）。

### 編集 15 — 表 T-031 の射程を画面に限る

**場所**: `docs/spec/01-04-requirements.md:2298`
**いま**:
```
**常時表示の規則は表 T-031 の `SC-4` が持つ** —— 消えるとキャンバスの幅が変わり、再レイアウトが走る。
```
**こうする**:
```
**常時表示の規則は表 T-031 の `SC-4` が持つ** —— 消えるとキャンバスの幅が変わり、再レイアウトが走る。 ⚠️ **本表が定めるのは画面の規則である。書き出しに何が入るかは表 T-076 が持つ。**
```
**理由**: ⛔ **検算で見つかった落ち。** `SC-4`（スクロールバーは常時表示・MUST）と `SC-3`（`App Header` は常に表示）は無条件に書かれており、表 T-076 の `EP-1` / `EP-8` が新表の欄で既存の MUST の射程を上書きする形になっていた。**射程の限定は `T-031` の側（＝ `FR-051`）に置くのが 1.9 の建て付けである。**

### 編集 16 — 変更履歴に版 0.32 を足す

**場所**: `docs/spec/A-appendix.md`（**CR-137 の編集 12 が足した行の直後**）
**いま**（CR-137 適用後の末尾）:
```
**検査の道具を 3 行直した** —— 検査 12 の例外から `S-21` / `S-22` を外し、廃止した 3 席を検査 7 の既知集合へ入れた |
```
**こうする**:
```
**検査の道具を 3 行直した** —— 検査 12 の例外から `S-21` / `S-22` を外し、廃止した 3 席を検査 7 の既知集合へ入れた |
| 0.32 | 2026-08-15 | ⭐ **書き出しを文書だけから決まるものにした**（CR-138。利用者の裁定）—— **出力の寸法を `exportCanvas` に固定し**（A3 横 / A4 横 / 現在の画面を廃止）、**収まらない分は `TaskGroup` 単位で落として件数を知らせる形に改めた。** 縮小はしない —— 縮小すると画面と別の寸法系になり `FR-080` が破れる。⭐ **入る領域と入らない領域の全数を 表 T-076（`EP-1` 〜 `EP-9`）に置いた** —— `Time Ruler` と `Row Title Panel` は入り、`App Header`・`Properties Panel`・操作子・`Panel Divider`・`Scrollbars`・重ねる面は入らない。境界は `Group Grid Lines` と同じ線を 1 本引く。**書き出し専用のパネル幅は作らない**（`S-79` をそのまま使う）。⚠️ **`WY-2` の判定から全体表示を外した** —— 全体表示は画面の寸法から倍率を出すので、判定に噛ませると機ごとに違う絵が出て判定自身が壊れる。⚠️ **透かしの範囲を `Row Area` に限った** —— 秘密の日程を映すのはそこであり、他は道具の操作面で書き出しにも入らない。`LM-1` と `LM-8` は変わらない。**新しい確定名も新しい設定値のキーも 1 つも作っていない** |
```
**理由**: CR 過程の決まり。

---

## CR-139（版 0.33）— 画面のモデルと、画面の面のコンポーネント

**閉じるもの**: L-22 〜 L-28、レビュー **F-3**。
**内訳**: §A 画面のモデル（`CP-35` / `CP-36`）／ §B 面のコンポーネント（`CP-37` / `CP-38`）／ §C 共有する数と道具。
⚠️ **§B を落とすときの数は 4 章の末尾に別掲した。**

### §A — 画面のモデル

#### 編集 1 — `LY-1` に画面の使い方の値を足す
**場所**: `docs/spec/05-07-design.md:47`
**いま**:
```
| LY-1 | `Entity` / `documentModel` | 表 T-052 が定める文書ルートの 3 群すべて（日程データの群のエンティティは 表 T-056）と、その不変条件（全数は Chapter 6.1 が持つ）。および**文書に保存しない実行時の値**（取り消しの履歴・選択・確定した発話。いずれも不変の値として持ち、丸ごと置き換える） | すべて `pure` |
```
**こうする**:
```
| LY-1 | `Entity` / `documentModel` | 表 T-052 が定める文書ルートの 3 群すべて（日程データの群のエンティティは 表 T-056）と、その不変条件（全数は Chapter 6.1 が持つ）。および**文書に保存しない実行時の値**（取り消しの履歴・選択・確定した発話・画面の使い方の値。いずれも不変の値として持ち、丸ごと置き換える） | すべて `pure` |
```
**理由**: L-22 / L-25。既存の 3 前例（`CP-4` / `CP-32` / `CP-33`）と同じ枠に入れる。**ここに「全数は 表 T-206」と書かない** —— 同表 16 行のうち画面の値は 3 行だけで、指す先が別物になる。

#### 編集 2 — `LY-2` に画面の各部の矩形を足す
**場所**: `docs/spec/05-07-design.md:48`
**いま**:
```
| LY-2 | `Entity` / `layoutEngine` | 日付と座標の対応、`Rows` の配置、描くものの頂点、表示量の増減、当たり判定 | すべて `pure` |
```
**こうする**:
```
| LY-2 | `Entity` / `layoutEngine` | 画面の各部の矩形、日付と座標の対応、`Rows` の配置、描くものの頂点、表示量の増減、当たり判定 | すべて `pure` |
```
**理由**: L-24。⛔ 5.1 の :66（`Entity` を 2 つに割った理由）は 1 文字も変えない。

#### 編集 3 — 表 T-062 に `CP-35` 〜 `CP-38` を足す
**場所**: `docs/spec/05-07-design.md:123`
**いま**:
```
| CP-34 | `documentModel` | `Document` | **文書ルートの合成と、`DR-1` の不変条件**（ルートに 3 群だけを置く／群に属する値をルート直下へ直に置かない） | 表 T-052 の `DR-1` |
```
**こうする**:
```
| CP-34 | `documentModel` | `Document` | **文書ルートの合成と、`DR-1` の不変条件**（ルートに 3 群だけを置く／群に属する値をルート直下へ直に置かない） | 表 T-052 の `DR-1` |
| CP-35 | `layoutEngine` | `ScreenRegions` | **画面の各部の矩形**（各部の名は 表 T-103 が持つ）と、ポインタがどの領域にあるかの判定 | `FR-051` |
| CP-36 | `documentModel` | `ScreenState` | **文書に保存しない画面の値** —— 構え（全数は 表 T-023b）と、表 T-206 の `S-99e` / `S-99f` / `S-99g` | `FR-053` / `FR-071` / 表 T-023b |
| CP-37 | `Adapter` | `ScreenRenderer` | 日程表の外側の UI パーツの記述を作り、対話欄で確定した発話を渡す。`ScreenSurface` を宣言する | `FR-051` / `FR-006` / `FR-036` / `FR-053` / `FR-076` / `FR-066` |
| CP-38 | `Framework` | `DomScreenSurface` | `ScreenSurface` の実装 | — |
```
**理由**: L-23 / L-24 / L-25 / L-27。⚠️ **検算の直し**: `CP-36` の「正」から**裸の 表 T-206 を外した**（編集 1 と同じ理由。責務欄で `S-99e` 〜 `S-99g` に絞る）。`CP-35` の「正」に `FR-052` を書かない —— パネル幅の検証は `PI-2` の `clampedSettings` が持ち、移すと同じ要求が 2 コンポーネントに乗る。

#### 編集 4 — 表 T-064 に `PI-35` 〜 `PI-38` を足す
**場所**: `docs/spec/05-07-design.md:344`
**いま**:
```
| PI-34 | `documentModel` | `Document` | `Document`（型。5 つの鍵は 表 T-052 の `DR-1` 〜 `DR-4`）／ `documentViolations`（`DR-1` に反する箇所） |
```
**こうする**:
```
| PI-34 | `documentModel` | `Document` | `Document`（型。5 つの鍵は 表 T-052 の `DR-1` 〜 `DR-4`）／ `documentViolations`（`DR-1` に反する箇所） |
| PI-35 | `layoutEngine` | `ScreenRegions` | `ScreenRect`（型。矩形。左上の座標と幅と高さの数値 4 つを自前で宣言し、**ブラウザの供給する型に触れない**（`LR-6`））／ `ScreenRegions`（型。各部の矩形。各部の名は 表 T-103 が持つ）／ `regionsFromScreen`（画面の寸法と `DocumentSettings` から各部の矩形を出す）／ `regionAtPointer`（ポインタがどの領域にあるか） |
| PI-36 | `documentModel` | `ScreenState` | `ScreenState`（型。構えは 表 T-023b、ほかは 表 T-206 の `S-99e` / `S-99f` / `S-99g`）／ `emptyScreenState` ／ `screenStateWithArmed` ／ `screenStateWithSurface`（開いている面）／ `screenStateWithPalette`（`S-99e`）／ `screenStateWithFullScreen`（`S-99f`）／ `escapeTarget`（`Esc` が次に消費するもの。階層は 表 T-028 の `IN-4`） |
| PI-37 | `Adapter` | `ScreenRenderer` | `ScreenSurface`（表 T-065）／ `ScreenView`（型。日程表の外側の UI パーツの記述）／ `screenViewFromRegions` ／ `dialogueMessageFromInput`（対話欄で確定した発話。順序の規則は 表 T-035 の `AG-11`） |
| PI-38 | `Framework` | `DomScreenSurface` | `ScreenSurface` の実装 1 つ |
```
**理由**: L-24 / L-25 / L-27。書き方は `PI-32`（`Selection`）と `PI-19`（`SvgSurface`）に揃えた。
⚠️ **検算の直し 2 件**: ① 塊 A の `commandFromScreenInput`（「対象の全数は `FR-016` が持つ」）を**落とした** —— `FR-016` の面の表は 5 面だけで `Properties Panel` も `App Header` も含まず、全数の主張が偽になる。**外側の面の入力も `IF-2` から入り、`CP-18` が操作へ変える。** ② `screenStateFromInput` は `PI-37` ではなく `PI-18` に置く（編集 6）。

#### 編集 5 — `PI-18` に画面の値を返すメンバを足す
**場所**: `docs/spec/05-07-design.md:328`
**いま**:
```
| PI-18 | `Adapter` | `InputCommandTranslator` | `InputSource`（表 T-065）／ `commandFromInput`（割当は 表 T-023 と 表 T-036）／ `selectionFromInput`（規則は 表 T-023c。取り消しの対象外＝`UN-9`） |
```
**こうする**:
```
| PI-18 | `Adapter` | `InputCommandTranslator` | `InputSource`（表 T-065）／ `commandFromInput`（割当は 表 T-023 と 表 T-036）／ `selectionFromInput`（規則は 表 T-023c。取り消しの対象外＝`UN-9`）／ `screenStateFromInput`（`Esc` の階層は 表 T-028 の `IN-4`。置き場は `CP-36`） |
```
**理由**: ⛔ **検算で見つかった落ち。** `IN-4` の `Esc` の階層は「開いている面 → ドラッグ → 構え → `Dual Cursor`」を 1 つの MUST で並べているので、**順序を通して見るコンポーネントが 1 つ要る。** `selectionFromInput`（`Selection` は `documentModel`）と同型にすることで、`CP-18` が持ち主になる。

### §B — 面のコンポーネント

#### 編集 6 — `CP-18` から確定発話を外す
**場所**: `docs/spec/05-07-design.md:107`
**いま**:
```
| CP-18 | `Adapter` | `InputCommandTranslator` | 画面の入力を操作へ変え、対話欄で確定した発話を渡す。`InputSource` を宣言する | `FR-016` / `FR-070` / `FR-066` |
```
**こうする**:
```
| CP-18 | `Adapter` | `InputCommandTranslator` | 画面の入力を操作へ変える。`InputSource` を宣言する | `FR-016` / `FR-070` |
```
**理由**: L-27 / L-28。レビュー **F-3(b)**。対話欄は UI パーツ（`U-44`）であって入力機器ではない。
⚠️ **検算の直し**: 塊 A は責務を「**日程の描画領域の**入力」に狭めていたが、同じ行に残る `FR-070` の 表 T-036 は `SK-13`（ヘルプ）・`SK-14`（パレット）・`SK-15`（全画面）を持ち、いずれも描画領域の外に働く。`FR-016` が限っているのは「ポインタを押したときの判定順序」だけである。**狭めず、発話の句と `FR-066` だけを外した。**

#### 編集 7 — `CP-21` に書き出しの組み立てと切り落としを持たせる
**場所**: `docs/spec/05-07-design.md:110`
**いま**:
```
| CP-21 | `Adapter` | `ImageExporter` | 画像として書き出す。`Rasterizer` を宣言する | `FR-025` |
```
**こうする**:
```
| CP-21 | `Adapter` | `ImageExporter` | 画像として書き出す。**表 T-076 が「入る」と定めた領域を組み立て、縦に収まらない `TaskGroup` を落とす。** `Rasterizer` を宣言する | `FR-025` / `FR-080` |
```
**理由**: ⛔ **検算で見つかった落ち。** CR-138 が `FR-025` に「`TaskGroup` 単位で落とす」を、`FR-080` に表 T-076 を置くのに、**どのコンポーネントがその判断を持つかが決まっていなかった。** 表 T-068（レイアウトの計算順序）には落とす段が無く、置くと循環を招くので、**組み立ての側（`ImageExporter`）に持たせる。**

#### 編集 8 — `PI-21` に SVG を組み立てるメンバを足す
**場所**: `docs/spec/05-07-design.md:331`
**いま**:
```
| PI-21 | `Adapter` | `ImageExporter` | `Rasterizer`（表 T-065）／ `exportPng`（`semi-pure-b`。失敗も値で返す。表 T-035 の `AG-8`） |
```
**こうする**:
```
| PI-21 | `Adapter` | `ImageExporter` | `Rasterizer`（表 T-065）／ `exportSvg`（`pure`。表 T-076 の入る領域を組み立てる。落とす規則は `FR-025`）／ `exportPng`（`semi-pure-b`。失敗も値で返す。表 T-035 の `AG-8`） |
```
**理由**: 表 T-024 の `IO-3`（SVG 書き出し）に対応する公開メンバが 1 つも無かった。`R2.19`（宣言された公開面だけを参照する）に照らして不足である。

#### 編集 9 — `UF-39` の純粋性を 2 つにする
**場所**: `docs/spec/05-07-design.md:282`
**いま**:
```
| UF-39 | `ImageExporter` | `image-exporter.ts` | `semi-pure-b` | `CP-21` の残り |
```
**こうする**:
```
| UF-39 | `ImageExporter` | `image-exporter.ts` | `pure` ／ `semi-pure-b` | `CP-21` の残り |
```
**理由**: 編集 8 と対。`audit-ch5.py:186` が「表 T-064 が名指しした純粋性は 表 T-075 にも現れること」を当てる。

#### 編集 10 — 表 T-065 に 9 本目 `IF-9` を足す
**場所**: `docs/spec/05-07-design.md:361`
**いま**:
```
| IF-8 | `AppShellSource` | `DocumentCodec`（`CP-20`） | `SingleHtmlShell`（`CP-25`） | アプリ自身の HTML。`IO-7` を作るのに要る |
```
**こうする**:
```
| IF-8 | `AppShellSource` | `DocumentCodec`（`CP-20`） | `SingleHtmlShell`（`CP-25`） | アプリ自身の HTML。`IO-7` を作るのに要る |
| IF-9 | `ScreenSurface` | `ScreenRenderer`（`CP-37`） | `DomScreenSurface`（`CP-38`） | 作った記述を画面に載せ、対話欄で確定した発話を返す |
```
**理由**: L-27 / `LR-5`。双方向 1 本にするのは `IF-3 FileStore` と :302 の前例に従うためで、片方向 2 本にすると 8 → 10 になる。

#### 編集 11 — 表 T-063 に `UT-7` を足す
**場所**: `docs/spec/05-07-design.md:232`
**いま**:
```
| UT-6 | `SingleHtmlShell` | `single-html-shell.ts` ／ `frame-loop.ts` | **純粋性ではない** —— 表 T-075 のとおり どちらも同じである。**起動は `FR-067` と `FR-065` が、フレームの走行は 表 T-060 の `LY-5` と 5.6 の ADR-001 が縛るので、変更の理由が別である。** ⚠️ **割らないと 1 つのユニットが 8 つの事柄を負い、`R2.2` に反する** —— 5.2 の分割基準「別々の要求が寸法と規則を持っているなら別コンポーネントとする」が、ユニットの側でも同じことを言う |
```
**こうする**:
```
| UT-6 | `SingleHtmlShell` | `single-html-shell.ts` ／ `frame-loop.ts` | **純粋性ではない** —— 表 T-075 のとおり どちらも同じである。**起動は `FR-067` と `FR-065` が、フレームの走行は 表 T-060 の `LY-5` と 5.6 の ADR-001 が縛るので、変更の理由が別である。** ⚠️ **割らないと 1 つのユニットが 8 つの事柄を負い、`R2.2` に反する** —— 5.2 の分割基準「別々の要求が寸法と規則を持っているなら別コンポーネントとする」が、ユニットの側でも同じことを言う |
| UT-7 | `ScreenRenderer` | `screen-renderer.ts` と、UI パーツごとの 9 ファイル | **純粋性ではない** —— 表 T-075 のとおり 10 とも同じである。**UI パーツごとに縛る要求が別なので割った**（`UT-2` と同じ形である）—— ヘルプの規則が変わってもプロパティパネルの規則は変わらない |
```
**理由**: U-9 の推奨（1 コンポーネント＋ユニット）。`UT-2`（`EditDocument` を集約ごとに 9 ファイル）が先例である。

#### 編集 12 — 表 T-075 に `UF-58` 〜 `UF-71` を足す
**場所**: `docs/spec/05-07-design.md:300`
**いま**:
```
| UF-57 | `Document` | `document.ts` | `pure` | `CP-34` |
```
**こうする**:
```
| UF-57 | `Document` | `document.ts` | `pure` | `CP-34` |
| UF-58 | `ScreenRegions` | `screen-regions.ts` | `pure` | `CP-35` |
| UF-59 | `ScreenState` | `screen-state.ts` | `pure` | `CP-36` |
| UF-60 | `ScreenRenderer` | `screen-renderer.ts` | `pure` | UI パーツごとの 9 ファイルを束ねて公開する |
| UF-61 | `ScreenRenderer` | `screen-frame.ts` | `pure` | `App Header`・`Panel Divider`・`Scrollbars` の割り付けと、全画面表示（`FR-051` / `FR-052` / `FR-071`） |
| UF-62 | `ScreenRenderer` | `app-header-items.ts` | `pure` | `Document Title`（`FR-035`）・`Autosave Status`（`FR-061`）・`Agent API` が有効であることの表示（`FR-065`）・表示言語の切替（`FR-038`） |
| UF-63 | `ScreenRenderer` | `row-title-panel.ts` | `pure` | `Row Title Panel` と `Row Title Tree`（`FR-085` / `FR-005` / `FR-098`） |
| UF-64 | `ScreenRenderer` | `properties-panel.ts` | `pure` | `Properties Panel`（`FR-006` / `FR-072`） |
| UF-65 | `ScreenRenderer` | `command-palette.ts` | `pure` | `Command Palette`（`FR-053` / `FR-083`） |
| UF-66 | `ScreenRenderer` | `open-modals.ts` | `pure` | 重ねて開く面（定義は 表 T-028 の `IN-4`）—— `FR-036` / `FR-074` / `FR-099` / `FR-088` / `FR-068` |
| UF-67 | `ScreenRenderer` | `notices.ts` | `pure` | 知らせ（`FR-076`。作法は 表 T-037） |
| UF-68 | `ScreenRenderer` | `dialogue-field.ts` | `pure` | `Dialogue Field`（`FR-066`。順序は 表 T-035 の `AG-11`） |
| UF-69 | `ScreenRenderer` | `tooltips.ts` | `pure` | ツールチップ（`FR-029` / `FR-037` / `FR-092`） |
| UF-70 | `ScreenRenderer` | `screen-surface.ts` | `—` | `ScreenSurface` の宣言（`IF-9`） |
| UF-71 | `DomScreenSurface` | `dom-screen-surface.ts` | `non-pure` | `CP-38` |
```
**理由**: U-9 / U-11 の推奨。⚠️ **検算の直し 2 件**: ①`UF-61` は `FR-071`（全画面）を `Panel Divider` に対応させていたので、**全画面を名指しした** ②`UF-66` の「表 T-206 の `S-99g` が数えるもの」は偽（`S-99g` は状態値 1 つで面を数えていない）なので、**`IN-4` の定義を指す形に改めた。**

#### 編集 13 〜 16 — ディレクトリ木に 4 フォルダを足す

| # | 場所 | いま | こうする |
|---|---|---|---|
| 13 | `05-07-design.md:205` | `                      edit-history/ · selection/ · dialogue-log/` | `                      edit-history/ · selection/ · dialogue-log/ · screen-state/` |
| 14 | `05-07-design.md:206` | `    layout-engine/    schedule-layout/ · schedule-geometry/ · item-hit-area/` | `    layout-engine/    schedule-layout/ · schedule-geometry/ · item-hit-area/`<br>`                      screen-regions/` |
| 15 | `05-07-design.md:213` | `                      autosave-gateway/ · clipboard-gateway/` | `                      autosave-gateway/ · clipboard-gateway/ · screen-renderer/` |
| 16 | `05-07-design.md:216` | `                      browser-clipboard/ · canvas-rasterizer/` | `                      browser-clipboard/ · canvas-rasterizer/ · dom-screen-surface/` |

**理由**: 5.3 の MUST（コンポーネントごとにフォルダを作り、語幹が同じ公開エントリを 1 つ置く）。記法は 表 T-006a の `W-11`。編集 14 で折り返すのは、1 行に足すと 94 桁になり他の行（最長 85 桁）と揃わないためである。

### §C — 数と道具（§A と §B を通した最終値を 1 度だけ当てる）

⚠️ **この 22 編集は、§A と §B の行をすべて入れた後に、最後にまとめて当てること。** 途中で当てると原文引用が外れる。

| # | 場所 | いま（抜粋） | こうする（抜粋） |
|---|---|---|---|
| 17 | `:84` | ``逆に `Framework` の 6 コンポーネントが分かれているのは`` | `… の 7 コンポーネントが …` |
| 18 | `:173` | `**層をまたぐ 8 本だけは` | `**層をまたぐ 9 本だけは` |
| 19 | `:181` | `\| **34。** 全数は 表 T-062、公開する名前は 表 T-064 \|` | `\| **38。** 全数は 表 T-062、公開する名前は 表 T-064 \|` |
| 20 | `:183` | `**57。** 全数は 表 T-075` | `**71。** 全数は 表 T-075` |
| 21 | `:187` | ``表 T-075 のとおり `EditDocument` と `DocumentCodec` が最も多くのユニットを持つが`` | ``表 T-075 のとおり `ScreenRenderer` と `EditDocument` が最も多くのユニットを持つが`` |
| 22 | `:199` | `34 のフォルダは 表 T-062 の 34 コンポーネントと 1 対 1` | `38 のフォルダは 表 T-062 の 38 コンポーネントと 1 対 1` |
| 23 | `:219` | `34 コンポーネントの公開インターフェースを … 層をまたぐ 8 本を` | `38 コンポーネントの … 層をまたぐ 9 本を` |
| 24 | `:221` | `層をまたぐインターフェースの 8 ファイルは本表に行を持たない` | `… の 9 ファイルは …` |
| 25 | `:238` | `層をまたぐインターフェースの 8 ファイルは型の宣言だけを持ち` | `… の 9 ファイルは …` |
| 26 | `:348` | ``この 8 ファイルもユニットである … `UF-44` / `UF-46` がそれである。`` | ``この 9 ファイルもユニットである … `UF-44` / `UF-46` / `UF-70` がそれである。`` |
| 27 | `:542` | `**表 T-070 の 6 つを増やした。**` | `**表 T-070 の 8 つを増やした。**` |
| 28 | `:553` | `コンポーネントを 34 に分けた（表 T-062）` | `コンポーネントを 38 に分けた（表 T-062）` |
| 29 | `:554` | `層をまたぐインターフェースを 8 本宣言した … 実装だけのコンポーネントが 6 つ増えた` | `… 9 本宣言した … 7 つ増えた` |
| 30 | `:557` | `⭐ **レイアウトと幾何をフレーム先頭で 1 回だけ計算して配る** … 辺が 2 本増え` | `⭐ **画面の矩形とレイアウトと幾何をフレーム先頭で 1 回だけ計算して配る** … 辺が 3 本増え` |
| 31 | `:559`（表 T-070 の末尾。**直前の空行を含めて引用する**） | （空行 ＋ ``**`R2.20`（MUST）が、キャッシュを用いる場合に 4 点を本節の ADR に置くことを求めている。**``） | `MN-7` と `MN-8` の 2 行を空行の前に足す（下に全文） |
| 32 | `:561` | `**ADR-001 — レイアウトと幾何をフレーム先頭で 1 回だけ計算する**` | `**ADR-001 — 画面の矩形とレイアウトと幾何をフレーム先頭で 1 回だけ計算する**` |
| 33 | `:569` | ``**Consequences** —— `Framework` から `layoutEngine` への辺が 2 本増えた。`` | `… 辺が 3 本増えた。`（以降は 1 文字も変えない） |
| 34 | `:575` | ``**そのフレームの `ScheduleLayout` と `ScheduleGeometry`**（表 T-068 の結果）`` | ``**そのフレームの `ScreenRegions` と `ScheduleLayout` と `ScheduleGeometry`**（後の 2 つは 表 T-068 の結果）`` |
| 35 | `:578` | ``` `CA-1` の 2 つは同じ契機で … **片方だけが古いという状態を作ってはならない（MUST NOT）** ``` | ``` `CA-1` の 3 つは同じ契機で … **1 つだけが古いという状態を作ってはならない（MUST NOT）** ``` |

**編集 31 の足す 2 行**:
```
| MN-7 | ⭐ **画面のモデルを `Entity` に置いた**（`CP-35` / `CP-36`） | 描く直前にその場で割り付ける | **`LR-6` により、矩形も画面の値もブラウザ無しで決まる。** 書き出しが文書だけの純粋関数になり、書き出しが通る値（`CP-35`）と画面にしか要らない値（`CP-36`）が 2 小層の境目で分かれる | 現在値は `Framework` が持つので（`LY-5`）、毎フレーム引数で内側へ渡す |
| MN-8 | ⭐ **日程表の外側の UI パーツを組み立てるコンポーネントを立てた**（`CP-37` / `CP-38`） | 描画が 1 つで日程表も外側も描く | **表 T-075 の `UF-61` 〜 `UF-69` が受ける要求を、組み立てる側で受けるコンポーネントが 1 つも無かった。** `FR-080` の `WY-2` が除外を持つのは透かしの層だけなので、外側を日程表と同じ出口に混ぜられない | コンポーネントが 2 つ、層をまたぐインターフェースが 1 本増えた |
```
⚠️ **検算の直し**: `MN-8` の「`CP-37` の正が挙げる要求を受けるコンポーネントが 1 つも無かった」は偽（`FR-066` は `CP-16` と `CP-33` が既に持つ）。**`UF-61` 〜 `UF-69` を指す形に改めた。**

#### 編集 36 — 表 T-103 に `U-51` を足す【U-5 を「名を保つ」で決めたときだけ】
**場所**: `docs/spec/_assets/tbl-glossary.md:131-132`（`U-50` の**次**を掴む。⚠️ `U-50` の行そのものは触らない）
**いま**（空行 ＋ 引用ブロックの 1 行目）:
```

> **呼び名は `Agent API` とする。日本語でも `Agent API` と書く。**
```
**こうする**:
```
| U-51 | `ScreenState` | （画面に出ない構造名。日本語を当てない）。文書に保存しない画面の値を持つ型の名である（表 T-062 の `CP-36`）。⚠️ **`tbl-settings.md` の 表 T-203「画面の状態」と同じ語で呼んではならない（MUST NOT）** —— あちらは文書に保存する値である |

> **呼び名は `Agent API` とする。日本語でも `Agent API` と書く。**
```
**理由**: U-5。名前についての禁止なので用語集に置いてよい（`style-checks.py` の `NAMING_RULE` が「呼んではならない」を用語集の主題として認める）。⛔ 表 T-203 の 10 行を `ScreenState` に吸わせない（版 0.11 の決定と `WY-1` を同時に覆す）。

#### 編集 37 — 版 0.29 の「2 つより多いユニット」を数え直す
**場所**: `docs/spec/A-appendix.md:57`
**いま**:
```
2 つより多いユニットを持つのは `EditDocument`（9）・`DocumentCodec`（5）・`AgentApiEndpoint`（3）で、いずれも平らに並べている。
```
**こうする**:
```
2 つより多いユニットを持つのは `EditDocument`（9）・`DocumentCodec`（5）・`AgentApiEndpoint`（3）・`ScreenRenderer`（11）で、いずれも平らに並べている。
```
**理由**: `audit-ch5.py:203` がこの 1 文を 表 T-075 から導いた辞書と**完全一致**で比べるので、直さないと必ず落ちる。版 0.30 が版 0.29 を直した前例がある。

#### 編集 38 〜 48 — `model.json`（原稿。図は生成物なので手で直さない）

| # | 場所 | 内容 |
|---|---|---|
| 38 | `nodes` の末尾（`209-214`） | `ScreenRegions` / `ScreenState`（`shape: component`）と `ScreenRenderer` / `DomScreenSurface` の 4 ノードを足す |
| 39 | `edges` の末尾（`715-721`） | 13 本足す（下表） |
| 40 | `604-605` | ``"source": "InputCommandTranslator"`` → ``"source": "ScreenRenderer"``（`PostDialogueMessage` への辺を付け替える） |
| 41 | `747-749` | `documentModel` クラスタに `ScreenState` |
| 42 | `759-762` | `layoutEngine` クラスタに `ScreenRegions` |
| 43 | `800-801` | `adapter` クラスタに `ScreenRenderer`（⚠️ 行頭は**半角空白 5**、閉じ括弧は 4） |
| 44 | `817-818` | `framework` クラスタに `DomScreenSurface`（同上） |
| 45 | `826-827` | `views.write` に `ScreenState`（⚠️ 行頭は**半角空白 4**） |
| 46 | `844-846` | `views.read` に `ScreenRegions` / `ScreenState` / `ScreenRenderer` / `DomScreenSurface` |
| 47 | `866-867` | `views.io` に `ScreenRenderer` |
| 48 | `880-881` | `views.startup` に `ScreenRenderer` / `DomScreenSurface` |

⚠️ **検算の直し**: 塊 A の `model.json` 5 編集は**インデントが 1 つ深く**、そのままでは 0 件で当たらない。実測した正しい深さを上表に書いた。

**編集 39 が足す 13 本**（すべて内向き。新しいクラスタ対は 0）:

| source | target | 何のため |
|---|---|---|
| `SingleHtmlShell` | `ScreenRegions` | フレーム先頭で矩形を 1 回計算する（ADR-001） |
| `ScreenRegions` | `DocumentSettings` | 保存されたパネル幅（`S-79` / `S-80`） |
| `InputCommandTranslator` | `ScreenRegions` | ポインタがどの領域にあるか |
| `InputCommandTranslator` | `ScreenState` | 構えと開いている面を読み、次の画面の値を返す |
| `SingleHtmlShell` | `ScreenRenderer` | 毎フレーム外側の UI パーツを作り直す |
| `DomScreenSurface` | `ScreenRenderer` | `ScreenSurface` の実装（`realization`） |
| `ScreenRenderer` | `ScreenRegions` | 各部がどこに座るか |
| `ScreenRenderer` | `ScreenState` | 保存しない画面の値 |
| `ScreenRenderer` | `Schedule` | 行の名前とプロパティパネルが出す属性 |
| `ScreenRenderer` | `DocumentSettings` | 見せ方の値 |
| `ScreenRenderer` | `Selection` | 何を選んでいるか |
| `ScreenRenderer` | `DialogueLog` | 対話欄が出す発話 |
| `ImageExporter` | `ScreenRenderer` | 書き出しに入る領域（表 T-076 の `EP-2`） |

⚠️ **`ScreenRenderer → ApplyDocumentChange` は引かない** —— 外側の面の入力も `IF-2` から入り、書き込みは `CP-18` の 1 本を通る（`MS-1`）。塊 A の案から落とした。

#### 編集 49 〜 57 — `audit-ch5.py`（**§A と §B を通した最終値**）

| # | 場所 | いま | こうする |
|---|---|---|---|
| 49 | `:43-45` | `34` / `34` / `8` | `38` / `38` / `9` |
| 50 | `:49` | `("34 のフォルダ", 1), ("34 コンポーネント", 2)` | `("38 のフォルダ", 1), ("38 コンポーネント", 2)`（⚠️ `("部品", 1)` は動かさない —— 規則 5） |
| 51 | `:56` | `check("T-063 rows", len(ut), 6)` | `… , 7)` |
| 52 | `:76` | `check("directory tree leaf folders", len(leaves), 34)` | `… , 38)` |
| 53 | `:88` | `check("model nodes", len(nodes), 34)` | `… , 38)` |
| 54 | `:99-101` | `FRAMEWORK = {…, "CanvasRasterizer"}` | 末尾に `"DomScreenSurface"` を足す（足さないと `unreached component` で必ず落ちる） |
| 55 | `:135` | `check("T-075 rows (units)", …, 57)` | `… , 71)` |
| 56 | `:138-139` | `57` ／ `design.count("**57。** 全数は 表 T-075")` | `71` ／ `"**71。** 全数は 表 T-075"` |
| 57 | `:168` | `check("T-065 rows that parse", len(iface_rows), 8)` | `… , 9)` |

⚠️ **`:59` のコメント（"was false for 7 of the 34"）は直さない** —— 過去のレビューが何を見つけたかの記録であり、今の全数の主張ではない。

#### 編集 58 — 変更履歴に版 0.33 を足す
**場所**: `docs/spec/A-appendix.md`（**CR-138 の編集 16 が足した行の直後**）
**こうする**（追記する 1 行）:
```
| 0.33 | 2026-08-15 | ⭐ **画面のモデルを `Entity` に置き、画面の面を組み立てるコンポーネントを立てた**（CR-139。利用者の裁定と、実装着手前レビューの **F-3**）—— 画面の各部の矩形を `ScreenRegions`（`CP-35`・`layoutEngine`）に、**文書に保存しない画面の値**を `ScreenState`（`CP-36`・`documentModel`）に置いた。**第 3 の小層は作っていない** —— 書き出しが文書だけから決まるようになった結果、**書き出しが通る値と画面にしか要らない値の境目が、2 小層の境目と一致した。** ⭐ **日程表の外側の UI パーツを組み立てる `ScreenRenderer`（`CP-37`）と、その DOM 実装 `DomScreenSurface`（`CP-38`）を立て、9 本目の層をまたぐインターフェース `ScreenSurface`（`IF-9`）を宣言した** —— ヘルプ・ツールチップ・`App Header`・コマンドパレット・プロパティパネル・知らせを担うコンポーネントが 1 つも無く、**全数を宣言して閉じた Chapter 5 の中で宙に浮いていた。** ⚠️ **確定した発話の持ち主を `InputCommandTranslator` から `ScreenRenderer` へ移した** —— `IF-2` が供給するのは「ポインタとキーの出来事」だけで、対話欄は UI パーツであって入力機器ではない。`PI-18` には `Esc` の階層（`IN-4`）を通して見る `screenStateFromInput` を足した。⚠️ **書き出しの組み立てと切り落としの持ち主を `ImageExporter`（`CP-21`）にし、`exportSvg` を宣言した** —— 表 T-024 の `IO-3` に対応する公開メンバが無かった。**コンポーネント 34 → 38・ユニット 57 → 71・層をまたぐ 8 → 9 本・辺 73 → 86・非巡回のまま・外向き 0 のまま。** 図 F-013 のクラスタ対は 9 のまま増えない |
```

---

## CR-140（版 0.34）— 起動と描き直しの順序

**閉じるもの**: レビュー **F-2** と 付録 A の **C-1** / **C-2** / **C-3**。

### 編集 1 — `CP-1` に稼働日の数え上げを持たせる
**場所**: `docs/spec/05-07-design.md:90`
**いま**:
```
| CP-1 | `documentModel` | `Schedule` | 日程データの群と、その不変条件。**予実の状態と遅れの判別** | 表 T-052 の `DR-2` / `FR-010`（表 T-019a）/ `FR-047`（表 T-021b） |
```
**こうする**:
```
| CP-1 | `documentModel` | `Schedule` | 日程データの群と、その不変条件。**予実の状態と遅れの判別。暦に従った稼働日の数え上げ** | 表 T-052 の `DR-2` / `FR-010`（表 T-019a）/ `FR-047`（表 T-021b）/ `FR-054` |
```
**理由**: C-1（`FR-054` は 05-07-design.md に 0 回。機械で再確認した）。**新コンポーネントを立てない**のは `R2.9` —— 暦は日程データの群の中にあり、`RV-2` 〜 `RV-4` と同じ理由で群の列だけから決まる。辺も 1 本も増えない（`ScheduleLayout → Schedule` と `EditDocument → Schedule` は既にある）。

### 編集 2 — `CP-9` の「正」に `FR-088` を足す
**場所**: `docs/spec/05-07-design.md:98`
**いま**:
```
| CP-9 | `UseCase` | `EditDocument` | 集約ごとの編集。検証して新しい文書を返すだけで、確定させない | 表 T-027 |
```
**こうする**:
```
| CP-9 | `UseCase` | `EditDocument` | 集約ごとの編集。検証して新しい文書を返すだけで、確定させない | 表 T-027 / `FR-088` |
```
**理由**: ⛔ **検算で見つかった落ち。** C-1 は `FR-054` と `FR-088` の 2 本を挙げていたが、塊 R の案は `FR-054` しか閉じておらず、**`FR-088` は適用後も Chapter 5 に 0 回のまま**だった。暦の編集は `PI-9` の `editCalendar` / `editProject` が実際に担っている。

### 編集 3 — `PI-1` に稼働日を数える 2 メンバを宣言する
**場所**: `docs/spec/05-07-design.md:311`
**いま**:
```
| PI-1 | `documentModel` | `Schedule` | `Schedule`（型。12 の鍵は 表 T-052 の `DR-2`）／ `scheduleViolations`（不変条件に反する箇所）／ `taskByUid`（`uid` で引く。`FR-022` の照合が使う）／ `planActualState`（表 T-019a の判別）／ `isDelayed`（表 T-021b の 3 条件）／ `delayWorkingDays`（表 T-021b の起点と終点） |
```
**こうする**:
```
| PI-1 | `documentModel` | `Schedule` | `Schedule`（型。12 の鍵は 表 T-052 の `DR-2`）／ `scheduleViolations`（不変条件に反する箇所）／ `taskByUid`（`uid` で引く。`FR-022` の照合が使う）／ `planActualState`（表 T-019a の判別）／ `isDelayed`（表 T-021b の 3 条件）／ `delayWorkingDays`（表 T-021b の起点と終点）／ `workingDaysBetween`（2 つの日付のあいだの稼働日数。暦は `FR-054`）／ `dateFromWorkingDays`（起点の日付に稼働日を加えた日） |
```
**理由**: C-1。`LR-2` により、`ScheduleLayout` と `EditDocument` が呼ぶには宣言が要る。命名は 表 T-006a の接続語の MUST（導出は `From`）に従う。

### 編集 4 — 5.5 の目次文に新しい 2 表を足す
**場所**: `docs/spec/05-07-design.md:405`
**いま**:
```
**一貫性の単位を 表 T-066 に、文書を変える手順を 表 T-067 に、予実の状態遷移を 図 F-018 に、レイアウトの計算順序を 表 T-068 に、描くときに求める値を 表 T-069 に示す。**
```
**こうする**:
```
**一貫性の単位を 表 T-066 に、文書を変える手順を 表 T-067 に、予実の状態遷移を 図 F-018 に、レイアウトの計算順序を 表 T-068 に、描くときに求める値を 表 T-069 に、起動の順序を 表 T-077 に、フレームが走る契機を 表 T-078 に示す。**
```
**理由**: F-2 / C-3。この 1 文は 5.5 の持ち物の全数を述べているので、表を足したら同時に直す（規則 9）。

### 編集 5 — `CS-1` から基準日を第 3 の源として外す
**場所**: `docs/spec/05-07-design.md:413`
**いま**:
```
| CS-1 | **1 フレーム**（描く） | 文書の凍結された複製・画面の寸法・基準日 | **フレームの先頭で 1 回** | 1 枚の絵の中で座標系が変わり、上半分と下半分が違う寸法で描かれる |
```
**こうする**:
```
| CS-1 | **1 フレーム**（描く） | 文書の凍結された複製（基準日 `Project.statusDate` を含む）・画面の寸法 | **フレームの先頭で 1 回** | 1 枚の絵の中で座標系が変わり、上半分と下半分が違う寸法で描かれる |
```
**理由**: C-2。`FR-046` が「基準日は設定値ではなく文書のデータ」と定めており、並記すると第 3 の源＝システム時計に読める。

### 編集 6 — 時計を読まないことを 表 T-066 の注に書く
**場所**: `docs/spec/05-07-design.md:417`
**いま**:
```
**3 つは入れ子である。** 身振り 1 回は多数のフレームを含み、1 回の書き込みは身振りの中でも外でも起こる。**身振りが何かは `AG-9` が定めており、対象は表 T-027 の取り消し対象行と一致する。**
```
**こうする**:
```
**3 つは入れ子である。** 身振り 1 回は多数のフレームを含み、1 回の書き込みは身振りの中でも外でも起こる。**身振りが何かは `AG-9` が定めており、対象は表 T-027 の取り消し対象行と一致する。**

⚠️ **`CS-1` はシステムの時計を集めない。** 基準日は文書の中にあり、そこへ本日を書くのは `FR-046` が定める操作の側である。**フレームの先頭で時計を読むと、日付をまたいだ瞬間に 表 T-069 の `RV-3` / `RV-4` とイナズマ線が動き、`WY-2` が崩れる。**
```
**理由**: C-2。規則そのものは `FR-046` が持つので、ここでは事実と帰結だけを書く。

### 編集 7 — `CP-25` に矩形・起動の順序・契機の観測を持たせる（**この CR の 1 編集にまとめてある**）
**場所**: `docs/spec/05-07-design.md:114`
**いま**:
```
| CP-25 | `Framework` | `SingleHtmlShell` | 起動と結線。**現在値を保持する。** **フレームの先頭でレイアウトと幾何を 1 回計算して配り**、描画を回し、入力を渡す。埋め込みの入れ物を持ち、公開点を置く。`SnapshotSource` と `AppShellSource` の実装 | `FR-067` / `FR-065` / 5.6 の ADR-001 |
```
**こうする**:
```
| CP-25 | `Framework` | `SingleHtmlShell` | 起動と結線。**起動の順序は 表 T-077、フレームを起こす契機の観測は 表 T-078 に従う。** **現在値を保持する。** **フレームの先頭で画面の各部の矩形とレイアウトと幾何を 1 回計算して配り**、描画を回し、入力を渡す。埋め込みの入れ物を持ち、公開点を置く。`SnapshotSource` と `AppShellSource` の実装 | `FR-067` / `FR-065` / `NFR-011` / 5.6 の ADR-001 |
```
**理由**: F-2（`NFR-011` を受ける設計側の担い手が無い）・C-3（寸法変化を誰が観測するか）・L-24（矩形も配り物）。⚠️ **このセルは CR-139 では触らない。** 3 件の理由で 1 セルを 3 回開かないためであり、**CR-139 と CR-140 は連続で当てること。**

### 編集 8 — `UF-47` に起動の順序を負わせる
**場所**: `docs/spec/05-07-design.md:290`
**いま**:
```
| UF-47 | `SingleHtmlShell` | `single-html-shell.ts` | `non-pure` | 起動と結線、埋め込みの入れ物、公開点を置くこと、`AppShellSource` の実装 |
```
**こうする**:
```
| UF-47 | `SingleHtmlShell` | `single-html-shell.ts` | `non-pure` | 起動と結線（順序は 表 T-077）、埋め込みの入れ物、公開点を置くこと、`AppShellSource` の実装 |
```
**理由**: F-2。`UT-6` が `single-html-shell.ts` を「起動と結線」と割っている（レビューは `UF-48` と書いたが、割り方に合わせた）。

### 編集 9 — `UF-48` に契機の観測を負わせる
**場所**: `docs/spec/05-07-design.md:291`
**いま**:
```
| UF-48 | `SingleHtmlShell` | `frame-loop.ts` | `non-pure` | 現在値の保持、フレーム先頭の収集と計算、描画と入力への配り、`SnapshotSource` の実装 |
```
**こうする**:
```
| UF-48 | `SingleHtmlShell` | `frame-loop.ts` | `non-pure` | 現在値の保持、フレームを起こす契機の観測（表 T-078）、フレーム先頭の収集と計算、描画と入力への配り、`SnapshotSource` の実装 |
```
**理由**: C-3。`UT-6` が「フレームの走行」を `frame-loop.ts` に割っている。

### 編集 10 — `CA-2` の契機の数え上げを表への指しに変える
**場所**: `docs/spec/05-07-design.md:576`
**いま**:
```
| CA-2 | 無効化の契機 | **フレームの先頭。** そのフレームのあいだは作り直さない。⚠️ **`NFR-010` により、入力も差し替えも無いフレームはそもそも走らない** |
```
**こうする**:
```
| CA-2 | 無効化の契機 | **フレームの先頭。** そのフレームのあいだは作り直さない。⚠️ **`NFR-010` により、表 T-078 の契機が 1 つも無いフレームはそもそも走らない** |
```
**理由**: C-3。現文は契機を 2 つ（入力・差し替え）と数えており、寸法の変化を足すと偽になる（規則 9）。

### 編集 11 — 稼働日の持ち主を 5.5 に 1 段落で置く
**場所**: `docs/spec/05-07-design.md:526`
**いま**:
```
**`RV-2` 〜 `RV-4` を `Schedule`（`CP-1`）が持つのは、いずれも日程データの群の列だけから決まる純粋な判定だからである。** 公開する名前は 表 T-064 の `PI-1` が持つ。
```
**こうする**:
```
**`RV-2` 〜 `RV-4` を `Schedule`（`CP-1`）が持つのは、いずれも日程データの群の列だけから決まる純粋な判定だからである。** 公開する名前は 表 T-064 の `PI-1` が持つ。

⚠️ **稼働日の数え上げも同じ理由で `Schedule`（`CP-1`）が持つ**（`FR-054`）—— 暦は日程データの群の中にある（表 T-052 の `DR-2`）。**`RV-1` を求める `ScheduleLayout` も、完了率を格納する `EditDocument`（`FR-012`）も、同じメンバを呼ぶこと（MUST）。数え方を 3 か所に書いてはならない（MUST NOT）** —— 同じタスクで違う日数が出る。
```
**理由**: C-1。1.9 の :285。

### 編集 12 — 5.5 の末尾に 表 T-077 と 表 T-078 を新設する
**場所**: `docs/spec/05-07-design.md:528`（5.6 の見出しの**前**に入れる）
**いま**:
```
### 5.6 Decisions (設計判断)
```
**こうする**:
```
**起動の順序を 表 T-077 に示す。正は `NFR-011` である。**

**表 T-077 — 起動の順序**

| 行 ID | 順 | すること | 正 |
| --- | :-: | --- | --- |
| BO-1 | 1 | 画面の寸法を確定させ、`ScreenRegions`（`CP-35`）を求める。**寸法が確定するまで 1 枚も描かない** | `NFR-011` |
| BO-2 | 2 | 表 T-034 の順で最初に開く文書を決める | `FR-062` |
| BO-3 | 3 | 見せ方の群から倍率と表示位置を取る。**表 T-024a の `OP-10` に当たるときは全体表示が選ぶ位置にする** | `FR-055` ／ `FR-087` |
| BO-4 | 4 | 表 T-068 を通し、レイアウトと幾何を作る。`OP-10` に当たるときは 2 回まで走らせる | 5.6 の ADR-001 |
| BO-5 | 5 | 最初の 1 枚を出す | `NFR-011` |

**上から順に通すこと（MUST）。前の段が済む前に次の段へ進んではならない（MUST NOT）** —— `BO-1` を飛ばすと寸法の無い 1 枚が、`BO-3` を飛ばすと位置の決まらない 1 枚が出る。**どちらも `NFR-011` が禁じている。**

⚠️ **`BO-4` の走行回数の規則は 表 T-068 の後の段が持つ。** 本表はそれを起動の側から指すだけである。

**フレームが走る契機の全数を 表 T-078 に示す。**

**表 T-078 — フレームが走る契機**

| 行 ID | 契機 | 気づくもの | 正 |
| --- | --- | --- | --- |
| FT-1 | 人の入力（ポインタとキー） | `DomInputSource`（`CP-27`）が 表 T-065 の `IF-2` で渡す | 表 T-023 ／ 表 T-036 |
| FT-2 | 現在値の差し替え（表 T-067 の `WS-6`） | `SingleHtmlShell`（`CP-25`） | 表 T-067 |
| FT-3 | **画面の寸法が変わったこと** | `SingleHtmlShell`（`CP-25`）が自分で観測する | `NFR-011` ／ 表 T-066 の `CS-1` |

**本表に無い契機でフレームを起こしてはならない（MUST NOT）** —— これが `NFR-010` の具体である。⚠️ **最初の 1 枚は 表 T-077 の `BO-5` が起こす。**

⚠️ **`IF-2` の供給物を広げない（表 T-065）** —— 寸法は入力機器の出来事ではなく、宿主が持つ値だからである。**外を読めるのは `Framework` だけなので**（表 T-060 の `LY-5`）、`FT-3` を観測するのはシェルである。**`layoutEngine` は寸法を引数で受け取る**（5.1）。

### 5.6 Decisions (設計判断)
```
**理由**: F-2 / C-3。5.5 の charter は「順序」（:403）なので置き場はここである。⚠️ **表番号を `T-076` / `T-077` から `T-077` / `T-078` に繰り下げた** —— `T-076` は CR-138 が先に取る。

### 編集 13 — `NFR-010` に寸法の変化を「操作」として数えることを書く
**場所**: `docs/spec/01-04-requirements.md:3723`（`**Relations**:` は :3725。**前に入る**）
**いま**:
```
**RATIONALE**: **素直に「ポインタが動いたら描き直す」と実装すると、ポインタを動かしているだけで毎秒 60 回描き続ける。** 電池と発熱に直に効き、他の要求をすべて満たしていても道具として使いづらくなる。ガイドカーソルが無効なときにポインタ移動で描き直さないこと（`FR-048`）は、この要求の具体である。
```
**こうする**:
```
**RATIONALE**: **素直に「ポインタが動いたら描き直す」と実装すると、ポインタを動かしているだけで毎秒 60 回描き続ける。** 電池と発熱に直に効き、他の要求をすべて満たしていても道具として使いづらくなる。ガイドカーソルが無効なときにポインタ移動で描き直さないこと（`FR-048`）は、この要求の具体である。

⚠️ **画面の寸法が変わったことは「操作」に数えること（MUST）** —— 寸法はフレームの入力であり（Chapter 5.5 の 表 T-066 の `CS-1`）、変わったのに描き直さないと前の寸法で作った絵が残る。**フレームが走る契機の全数は Chapter 5.5 の 表 T-078 が持つ。**
```
**理由**: C-3。この 1 文が無いと、寸法変化でフレームを起こす設計が `NFR-010` と `PG-5` の測定に対して黒とも白とも判定できない。

### 編集 14 — 変更履歴に版 0.34 を足す
**場所**: `docs/spec/A-appendix.md`（**CR-139 の編集 58 が足した行の直後**）
**こうする**（追記する 1 行）:
```
| 0.34 | 2026-08-15 | ⭐ **起動と描き直しの順序を Chapter 5.5 に置いた**（CR-140。実装着手前レビューの **F-2** と 付録 A の **C-1** 〜 **C-3**）—— `NFR-011` が「起動の順序を設計で決めることでしか防げない」と書いたその順序に、Chapter 5 の受け皿が無かった。**表 T-077**（起動の順序 `BO-1` 〜 `BO-5`）と **表 T-078**（フレームが走る契機 `FT-1` 〜 `FT-3`）を新設し、`CP-25` の責務と「正」に `NFR-011` を足した。⚠️ **画面の寸法が変わったときにフレームを起こす契機が、仕様書のどこにも無かった** —— `IF-2` はポインタとキーしか供給しないので、**寸法は `Framework` が自分で観測する**形に決め、`NFR-010` に「寸法の変化は操作に数える」を足した。⚠️ **表 T-066 の `CS-1` が基準日を文書と並べて集めていた** —— 基準日は `Project.statusDate` であり凍結した複製の中にあるので、由来を括弧で示して第 3 の源に見えないようにした。⭐ **稼働日の数え上げに持ち主が無かった**（`FR-054` は Chapter 5 に 0 回）—— **コンポーネントは増やさず `Schedule`（`CP-1`）に持たせ**、`PI-1` に `workingDaysBetween` と `dateFromWorkingDays` を宣言した。暦を編集する `FR-088` は `EditDocument`（`CP-9`）の「正」に足した。**コンポーネントもユニットも辺も 1 つも動かない** |
```

---

## CR-141（版 0.35）— Chapter 4 の値と経路

**閉じるもの**: レビュー **F-4** / **F-5** / **F-6** と 付録 A の **C-4** / **C-5**。

### 編集 1 — `FR-043` に実績期間を足す
**場所**: `docs/spec/01-04-requirements.md:1933`
**いま**:
```
**STATEMENT**: `Task` が未着手であるあいだ、`GRS` は、実績の入力を始める入口を**未着手と分かる見た目**でタスクの上に示し、掴まれたときに**実績開始日と `resumeValid`（`true`）を**置き、**押されたときに状態を巡らせること（MUST）。**
```
**こうする**:
```
**STATEMENT**: `Task` が未着手であるあいだ、`GRS` は、実績の入力を始める入口を**未着手と分かる見た目**でタスクの上に示し、掴まれたときに**実績開始日と実績期間（`actualDuration`）と `resumeValid`（`true`）を**置き、**押されたときに状態を巡らせること（MUST）。**
```
**理由**: F-4。表 T-019 の `PA-2` が `actualDuration` に「あり」を求めるのに、置く規則が無かった。「期間」の単独使用は 表 T-006b の `A-12` が禁じるので `actualDuration` を併記する。

### 編集 2 — 掴んで置く量を `FR-043` が定める
**場所**: `docs/spec/01-04-requirements.md:1943`（`**Relations**:` は :1945。**前に入る**）
**いま**:
```
**掴むことと押すことを 1 つの入口が受けるのも、遅れをマーカーではなく入口の形で表すのも、理由は同じである** —— **未着手のあいだ画面に出る印を 1 つに保つため**（`FR-029`）。押して状態を確定したときに置く値は表 T-021a に従うこと。
```
**こうする**:
```
**掴むことと押すことを 1 つの入口が受けるのも、遅れをマーカーではなく入口の形で表すのも、理由は同じである** —— **未着手のあいだ画面に出る印を 1 つに保つため**（`FR-029`）。押して状態を確定したときに置く値は表 T-021a に従うこと。

**掴んで置く値は、実績開始日 ＝ 掴んだ日、実績期間（`actualDuration`）＝ `0`（稼働日）、`resumeValid` ＝ `true` とすること（MUST）。掴んだまま動かしているあいだの実績期間は、表 T-023d の `GR-6` と同じく置いた日付から稼働日数を算出すること（MUST）** —— 量を定めないと、**掴んだ瞬間の実績バーの右端（`FR-011`）・完了率（`FR-012`）・イナズマ線の頂点（表 T-022 の `PL-5`）が実装ごとに変わる。** ⚠️ **表 T-019 の `PA-2` が求める「あり」を満たすのはこの値である** —— 期間は開始日と終了日の差なので、掴んだその日で離せば `0` である（`FR-012` が同じ数え方を定めている）。
```
**理由**: F-4。**表 T-021a に行を足さない**のは、同表の表題が「印を押して状態を確定したとき」であり、05-07-design.md:445 の「表 T-021a の 4 行」も動かさずに済むからである（U-18 も参照）。

### 編集 3 — 表 T-023d の `GR-9` に `actualDuration` を足す
**場所**: `docs/spec/01-04-requirements.md:2251`
**いま**:
```
| GR-9 | 実績入力の入口 | 未着手のタスクの上（当たり判定の大きさは `_assets/tbl-settings.md` の `S-93`） | 掴めば `actualStart` を置き、押せば状態を巡らせる（`FR-043`） |
```
**こうする**:
```
| GR-9 | 実績入力の入口 | 未着手のタスクの上（当たり判定の大きさは `_assets/tbl-settings.md` の `S-93`） | 掴めば `actualStart` と `actualDuration` を置き、押せば状態を巡らせる（値は `FR-043`） |
```
**理由**: F-4。`FR-011`（:1871）が「画面上で直接掴んで実績の日付を書き込む経路の全数は表 T-023d が持つ」と宣言している。

### 編集 4 — 図 F-018 の下の「置く値」の持ち主を数え直す
**場所**: `docs/spec/05-07-design.md:469`
**いま**:
```
**状態は列の組み合わせから判別する。判別の手順は 表 T-019a が、置く値は 表 T-019 と 表 T-021a が持つ。**
```
**こうする**:
```
**状態は列の組み合わせから判別する。判別の手順は 表 T-019a が、置く値は 表 T-019 と 表 T-021a、および `FR-043` / `FR-044` が持つ。**
```
**理由**: F-4。同じ節の :445 が既に「辺は 表 T-021a の 4 行と `FR-043` / `FR-044`」と書いており、食い違っていた。

### 編集 5 — `HR-6` に最上位の行の復帰口を足す
**場所**: `docs/spec/01-04-requirements.md:1298`
**いま**:
```
| HR-6 | 非表示 | 行を隠す。親の非表示グループタブから戻せること。**隠した状態を文書に保存すること（MUST）** —— 保存しないと、書き出して読み直したときに隠した行が戻り、`WY-1` が成立しない |
```
**こうする**:
```
| HR-6 | 非表示 | 行を隠す。親の非表示グループタブから戻せること。**親を持たない最上位の行は、行見出しパネルの根に置く非表示グループタブから戻せること（MUST）** —— `FR-085` が最上位の行を許しているためである。**隠した状態を文書に保存すること（MUST）** —— 保存しないと、書き出して読み直したときに隠した行が戻り、`WY-1` が成立しない |
```
**理由**: F-5。`TaskGroup.parentId` は `null` を許し、`HR-6` の保存が MUST なので、戻す口が無いとその行と `Task` が再読込を跨いで消えたままになる。**新しい UI パーツは作らない** —— `U-29`（`Hidden Group Tab`）を根にも置くという意味である。

### 編集 6 — UC-002 手順 5 から「親の」を外す
**場所**: `docs/spec/01-04-requirements.md:548`
**いま**:
```
5. `GRS` は畳んだ `TaskGroup` の配下の行を隠し、非表示にしたものは親の非表示グループタブから戻せるようにする。
```
**こうする**:
```
5. `GRS` は畳んだ `TaskGroup` の配下の行を隠し、非表示にしたものは非表示グループタブから戻せるようにする（置き場は表 T-015 の `HR-6`）。
```
**理由**: F-5。手順が「親の」と限っていることが穴の出どころである。

### 編集 7 — `LM-14` の指し先を実在する印に直す
**場所**: `docs/spec/01-04-requirements.md:188`（行の一部）
**いま**:
```
—— 全数は表 T-206 の別枠の行（⛔ の付いた `S-99` 系）である。
```
**こうする**:
```
—— 全数は表 T-206 の別枠の行、すなわち「別枠。`localStorage` に置く。」と明記した行である。
```
**理由**: F-6①。表 T-206 に ⛔ は 1 つも無く、`S-99` 系という書き方は別枠でない `S-99e` 〜 `S-99g` まで含んでしまう。

### 編集 8 — 1.8 の内訳の列挙を番号で指す形にする
**場所**: `docs/spec/01-04-requirements.md:196`（行の一部）
**いま**:
```
データの語・プロパティ・UI パーツ・設定値のキー・直訳しない語の 5 つの表からなる。
```
**こうする**:
```
表 T-101 〜 T-107 からなる。
```
**理由**: F-6②。実体は 7 表である。番号で指せば表を足すたびに散文が腐らない。**数を書かないので新しい数の主張も作らない。**

### 編集 9 — `FR-088` の反映先を書き分ける
**場所**: `docs/spec/01-04-requirements.md:2417`
**いま**:
```
**STATEMENT**: 作成者が暦を編集したとき、`GRS` は、**稼働する曜日・例外日（休業日）・週の始まり**を変えられるようにし、変更を文書の暦へ反映すること。
```
**こうする**:
```
**STATEMENT**: 作成者が暦を編集したとき、`GRS` は、**稼働する曜日・例外日（休業日）・週の始まり**を変えられるようにし、**稼働する曜日と例外日を文書の暦（`Calendar`）へ、週の始まりを `Project.weekStartDay` へ**反映すること。
```
**理由**: C-4。`erd.json` の `Calendar` は 7 列で週の始まりを持たず、`Project.weekStartDay`（交換相手の `Project/WeekStartDay`）がその置き場である。**現文は書き込み先の無い場所を指していた。**

### 編集 10 — 2 つの集約にまたがるときの確定の単位を定める
**場所**: `docs/spec/01-04-requirements.md:2423`（`**Relations**:` は :2425。**前に入る**）
**いま**:
```
**変更は取り消しの対象とすること（MUST）**（表 T-027 の `UN-13`）。**暦を変えると期間の計算がすべて動く**ので、影響する `Task` の件数を知らせること（MUST）。
```
**こうする**:
```
**変更は取り消しの対象とすること（MUST）**（表 T-027 の `UN-13`）。**暦を変えると期間の計算がすべて動く**ので、影響する `Task` の件数を知らせること（MUST）。

⚠️ **1 回の編集が `Calendar` と `Project` の両方に及ぶときも、まとめて 1 回の書き込みで確定すること（MUST）**（表 T-035 の `AG-3`）—— 別々に確定させると、稼働する曜日だけが変わって週の始まりが変わらない中間状態を人が読める。
```
**理由**: C-4。`PI-9` は `editCalendar` と `editProject` を別メンバに持つので、確定の単位を要求側で言い切らないと実装が割れる。

### 編集 11 — `FR-054` に期間の換算の拠り所を定める
**場所**: `docs/spec/01-04-requirements.md:2436`
**いま**:
```
**STATEMENT**: `GRS` は、文書が持つ暦に従って非稼働日（週末・祝日）を稼働日と区別して描くこと。**期間の計算に用いる「稼働日」も同じ暦に従うこと。1 日を何時間と決め打ちしてはならない（MUST NOT）。**
```
**こうする**:
```
**STATEMENT**: `GRS` は、文書が持つ暦に従って非稼働日（週末・祝日）を稼働日と区別して描くこと。**期間の計算に用いる「稼働日」も同じ暦に従うこと。1 日を何時間と決め打ちしてはならない（MUST NOT）。** **稼働日を交換相手の期間（時間の量）へ換算するときは `Project.minutesPerDay` に従い、それが空のときは表 T-209 の `S-128` に従うこと（MUST）。**
```
**理由**: C-5。`Task/Duration` は `xsd:duration`（時間の量）であり、`Project.minutesPerDay` は `null` を許す。決め打ち禁止の MUST NOT がある以上、既定の置き場を要求が名指ししないと `EX-1`（新規文書でも相手が開ける）の合否を誰も判定できない。

### 編集 12 — `FR-054` の週の始まりの拠り所を正す
**場所**: `docs/spec/01-04-requirements.md:2440`（行の一部）
**いま**:
```
**週の目盛は週の始まりの日付を書き、週の始まりも文書の暦に従うこと（MUST）** —— 曜日を決め打ちしない。
```
**こうする**:
```
**週の目盛は週の始まりの日付を書き、週の始まりは `Project.weekStartDay` に従うこと（MUST）** —— 曜日を決め打ちしない。**置き場の正は `FR-088` が持ち、取り込んでいない文書の既定は表 T-209 の `S-108` が持つ。**
```
**理由**: C-4。⚠️ **検算の直し**: 塊 R は `FR-088` と `FR-054` の両方に置き場を書いていたので、**正を `FR-088` に決め、こちらは指すだけにした**（1.9 の :285）。

### 編集 13 — 表 T-209 の表題に期間の換算を加える
**場所**: `docs/spec/_assets/tbl-settings.md:319`
**いま**:
```
**表 T-209 — 既定の暦（`FR-054`。MSPDI を取り込んでいない文書が使う）**
```
**こうする**:
```
**表 T-209 — 既定の暦と期間の換算（`FR-054`。MSPDI を取り込んでいない文書が使う）**
```
**理由**: C-5。`S-108` と新しい `S-128` は `Project` の列であり、表題が「暦」だけだと置き場が読めない。

### 編集 14 — `S-108` に置き場を書き、`S-128` を新設する
**場所**: `docs/spec/_assets/tbl-settings.md:325`
**いま**:
```
| S-108 | 週の始まり | 月曜 🔎 | 週の目盛が書く日付の基準（`FR-054`） |
```
**こうする**:
```
| S-108 | 週の始まり | 月曜 🔎 | 週の目盛が書く日付の基準（`FR-054`）。置き場は `Project.weekStartDay` |
| S-128 | 1 日あたりの分数 | `480` 🔎 | `Project.minutesPerDay` が空のときに使う（`FR-054`）。1 日 8 時間。選び方は `S-106` と同じである |
```
**理由**: C-4 / C-5。**MUST を書かない**（1.9 の :283。検査 12）。
⚠️ **検算の直し**: 塊 R の `S-128` は `S-106` の理由文（「特定の業種や地域を前提にしない範囲で最も広く使われる…」）を写しており、**dup-check（検査 11）が新規の重複組を 1 つ立てて赤になる**（実測。類似度 0.72）。**写しをやめ、`S-106` を指す形にした。**

### 編集 15 〜 17 — `erd.json`（原稿。生成物は手で直さない）

| # | 場所 | いま | こうする |
|---|---|---|---|
| 15 | `erd.json:406` | `     "meaning": "1 日あたりの分数。期間の換算に使う"` | `     "meaning": "1 日あたりの分数。期間の換算に使う。空のときの既定は 表 T-209 の `S-128`"` |
| 16 | `erd.json:433` | `     "meaning": "週の始まりの曜日"` | ``     "meaning": "週の始まりの曜日。暦ではなくここが置き場である（`FR-088`）"`` |
| 17 | `erd.json:818` | ``     "meaning": "隠しているか。戻す入口は親の非表示グループタブ（表 T-015 の `HR-6`）"`` | ``     "meaning": "隠しているか。戻す入口は 表 T-015 の `HR-6` が持つ"`` |

**理由**: C-5 / C-4 / F-5。行頭は**半角空白 5**。編集 17 は「親の」の 2 か所目を消す（1.9 の :285）。

### 編集 18 — 生成物を作り直す
**手順**: `python docs/spec/_assets/source/erd_json_to_md.py` を走らせ、`_assets/fig-erd-detail.md` と `fig-erd-overview.md` を**作り直す**（手で直さない。規則 8）。
**理由**: ⛔ **検算で見つかった落ち。** 走らせないと**検査 16 が FAIL する**（実測：`DRIFT fig-erd-detail.md differs from what erd.json produces`、exit=1）。塊 R の計画には工程として無かった。

### 編集 19 — 変更履歴に版 0.35 を足す
**場所**: `docs/spec/A-appendix.md`（**CR-140 の編集 14 が足した行の直後**）
**こうする**（追記する 1 行）:
```
| 0.35 | 2026-08-15 | 実装着手前レビューの Chapter 4 側 5 件を閉じた（CR-141。**F-4** / **F-5** / **F-6** と 付録 A の **C-4** / **C-5**）。⚠️ **掴んで着手したときの `actualDuration` の量がどこにも無かった** —— 表 T-019 の `PA-2` は「あり」を求めるのに置く規則が 1 つも無く、掴んだ瞬間のバーの長さ・完了率・イナズマ線の頂点が実装ごとに変わる。`FR-043` に「掴んだ日 ＝ 実績開始日、実績期間 ＝ `0`（稼働日）、動かしているあいだは `GR-6` と同じ」を書いた。**表 T-021a には行を足していない** —— 同表は「押して確定したとき」の表だからである。⚠️ **最上位の行を非表示にすると戻す入口が無かった** —— `HR-6` に行見出しパネルの根のタブを足した（`FR-085` が最上位の行を許している）。⚠️ **週の始まりを `FR-088` は「文書の暦」へ反映すると書くのに、置き場は `Project.weekStartDay` だった** —— 交換相手のスキーマがそこに持つので、データモデルを動かさず要求の文を置き場へ合わせた。⚠️ **新規文書に 1 日あたりの分数が無く、稼働日を交換相手の期間へ換算できなかった** —— 表 T-209 に `S-128` を足し、表題を「既定の暦と期間の換算」に改めた。ほかに散文の数え上げ 2 件（`LM-14` の ⛔ ／ 1.8 の「5 つの表」）を実体に合わせた |
```

---

# 4. 動く数の全数表

**数え方**: 表は `^**表 T-nnn — ` で始まる定義行、行 ID は各表の第 1 列、`34` / `57` / `8 本` / `1 対 1` は綴りを機械で全数拾った。

| 主張 / 実体 | 主張している場所 | いま | CR 後 | どの CR |
|---|---|:-:|:-:|---|
| 表 T-201 の行数 | （主張なし） | 56 | **55** | 137 |
| 表 T-104 の行数 | （主張なし） | 105 | **104** | 137 |
| 表 T-026 の行数 | （主張なし） | 8 | **9** | 137 |
| `documentSettings` の鍵 | （主張なし） | — | **1 減**（`markerOfFont`） | 137 |
| 表 T-204 の選択肢 | `S-81` の文 | 4 | **1** | 138 |
| 「A3」「A4」「現在の画面」の綴り | — | 各 1 | **0** | 138 |
| 「縮小して収める」系の綴り | — | 3 | **0** | 138 |
| 表の総数 | （主張なし） | 104 | **107**（T-076 / T-077 / T-078） | 138・140 |
| コンポーネント | `:181` `:199`×2 `:219` `:553`、`audit:43` `:44` `:76` `:88` | 34 | **38** | 139 |
| ユニット | `:183`、`audit:135` `:138` `:139` | 57 | **71** | 139 |
| 層をまたぐインターフェース | `:173` `:219` `:221` `:238` `:348` `:554`、`audit:45` `:168` | 8 | **9** | 139 |
| 表 T-063 の行数 | `audit:56` | 6 | **7** | 139 |
| 表 T-070 の行数 | `:542`（**機械検査 9 の関門**） | 6 | **8** | 139 |
| `Framework` の実装だけのコンポーネント | `:84` `:554` | 6 | **7** | 139 |
| `Framework` → `layoutEngine` の辺 | `:557` `:569` | 2 | **3** | 139 |
| `CA-1` のキャッシュ | `:578` | 2 | **3** | 139 |
| 最も多くのユニットを持つもの | `:187` | `EditDocument` / `DocumentCodec` | **`ScreenRenderer` / `EditDocument`** | 139 |
| 2 つより多いユニット（変更履歴） | `A-appendix:57`（`audit:203` が完全一致で比較） | 3 件 | **4 件**（`ScreenRenderer`（11）） | 139 |
| `model.json` のノード | `audit:88` | 34 | **38** | 139 |
| `model.json` の辺 | （主張なし） | 73 | **86** | 139 |
| 表 T-103 の行数 | （主張なし） | 51 | **52**（U-5 で `U-51` を採るとき） | 139 |
| 図 F-013 のクラスタ対 | `build.py` の 9 対 | 9 | **9**（不変） | — |
| 表 T-107 のメンバ | `audit:80` | 18 | **18**（不変） | — |
| 表 T-068 の 11 段 | `:557` `:563` `A-appendix:55` | 11 | **11**（不変） | — |
| 表 T-019 の 5 / 表 T-021a の 4 | `:445` ほか | 5 / 4 | **不変** | — |
| 表 T-065 の「8 本」以外の `表 T-nnn の N 件/行/段/つ` | 12 箇所 | — | **動くのは `:542` の 1 箇所だけ** | 139 |
| 表 T-209 の行数 | （主張なし） | 3 | **4**（`S-128`） | 141 |
| `S-` の最大 | — | S-127 | **S-128** | 141 |

**§B（面のコンポーネント）を落とすときの値**: コンポーネント **36**、ユニット **59**、層をまたぐ **8 本**（`:173` 等は動かさない）、表 T-063 **6**、表 T-070 **7**、`Framework` の実装だけ **6**（動かさない）、`model.json` ノード **36** / 辺 **77**、`A-appendix:57` は **3 件のまま**、`:187` も**動かさない**。

---

# 5. 使う空き席の一覧

| 種別 | 使う席 | 根拠（機械で数えた） |
|---|---|---|
| 表番号 | **T-076**（書き出しの領域・CR-138）／ **T-077**（起動の順序）／ **T-078**（フレームの契機・CR-140） | 章の系列の最大は `T-075`。`T-030` / `T-048` / `T-049` / `T-055` は飛び番だが**詰め直さない**（1.9 の :271）。`T-044` 〜 `T-047` は封印（`A-appendix:35`） |
| 行 ID | `EP-1` 〜 `EP-9` | 接頭辞 `EP` は `docs/spec` に 0 件。既存の `EX-` / `E-` / `EZ-` と重ならない |
| 行 ID | `BO-1` 〜 `BO-5` ／ `FT-1` 〜 `FT-3` | 接頭辞 `BO` / `FT` はいずれも 0 件 |
| 行 ID | `RC-11` | 表 T-026 の最大は `RC-10`、欠番は `RC-4` / `RC-9`（廃止跡なので使わない） |
| 行 ID | `CP-35` 〜 `CP-38` ／ `PI-35` 〜 `PI-38` | `CP` / `PI` とも最大 34・欠番なし。⚠️ `audit:46` が `CP-n` と `PI-n` の 1 対 1 を当てる |
| 行 ID | `UF-58` 〜 `UF-71` | 最大 57・欠番なし |
| 行 ID | `IF-9` ／ `UT-7` ／ `MN-7` ／ `MN-8` | 最大 `IF-8` / `UT-6` / `MN-6` |
| 行 ID | `U-51`（U-5 の判断次第） | 表 T-103 の最大は `U-50`・欠番なし |
| 設定値 | **`S-128`** | `tbl-settings.md` の最大は `S-127`、欠番は `S-52` のみ（廃止跡なので使わない） |
| **欠番にする席** | `S-21`（`markerOfFont`）／ `K-21` | どちらも指しているのは本計画で消える 2 行だけ（`impact.py` で 0 件を確認） |
| **使わない席** | `S-129` 以降（U-7 のスクロールバーの太さ、U-8 のパレットの位置）／ `K-107`（表 T-104 の最大は `K-106`）／ `T-079` 以降 ／ `F-019` 以降 ／ `FR-` `NFR-` `UC-` `GL-` は **1 つも足さない** | — |

---

# 6. 検算で残った懸念と、採らなかったもの

## 6.1 当てる前後に必ず走らせるもの

| 順 | すること | なぜ |
|:-:|---|---|
| 0 | `bash .claude/skills/spec-graph-check/check.sh` と `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/audit-ch5.py` を**当てる前に**走らせて全緑を確かめる | 基準線が無いと、落ちたのが自分のせいか元からかを切り分けられない |
| 1 | CR ごとに当てる。**CR-139 → CR-140 の順を崩さない** | `CP-25` と `BO-1` が CR-139 の結果を指す |
| 2 | CR-139 の後に `python docs/spec/_assets/source/build.py` | `model.json` を直すと `fig-components.svg` / `view-*.svg` / `docs/review/components/components.md` が古くなる。⚠️ draw.io CLI が無い機では `.svg` が更新されないので、そこだけ別の機で回す |
| 3 | CR-141 の後に `python docs/spec/_assets/source/erd_json_to_md.py` | 走らせないと**検査 16 が必ず落ちる**（実測） |
| 4 | `strictdoc export docs/spec`（`docs` ではない）して**刊行物を目で見る** | 16 検査が全緑でも表が刊行物から消える事故が版 0.25 で起きている。⚠️ 新しく足した自由文（CR-137 編集 11 / CR-140 編集 6・11 / CR-141 編集 2・10）は**いずれも `**Relations**:` の前**に入れてあるが、空行が 2 つ連続していないことも見る |
| 5 | `check-render.py`（生の `**` を数える） | 規則 6。本計画の AFTER は `。` の直後に閉じる太字の後へ必ず半角空白を入れてあるが、機械で確かめる |

## 6.2 隠さずに残す懸念

1. **⚠️ 検査 11（dup-check）が CR-139 で赤になるおそれがある。** 表 T-075 に 14 行、表 T-062 / T-064 に 4 行が増え、責務欄の言い回しが近い行が並ぶ。塊 R の重複（`S-106` / `S-128`）は編集 14 で潰したが、CR-139 は実測していない。**U-14（基準線を張り直すか）を CR-139 の実測後に決めること。**
2. **⚠️ 細線の 2 形状ではマーカーが実績バーより大きい。** `shapeHeightOf.arrow` / `endpointSpan` は比 0.5 なので実績バーは床で 8.0px しかなく、16px のマーカーはこれより大きい。**現行の鎖でも同じ（今日すでに 16px）なので新たな悪化ではない**が、L-3 の保証が効くのは比 1 以上の形状だけである。**U-2 で MUST NOT を残すと、この 2 形状が即違反になる。**
3. **⚠️ 高いズームで押し所が縮む。** `zoomY` = 4 で 78.5px → 16px、`zoomMax` = 64 では 1256px → 16px。表 T-023d の `GR-7` / `GR-8` は掴み代の行を持たない（表 T-206 の掴み代は `S-90` 〜 `S-93` の 4 行だけ）。WCAG の目標サイズは適合範囲外（`LM-2`）なので規約違反ではないが、**実機で確かめる価値がある。**
4. **⚠️ `FR-043` の入口とマーカーで同じ `(!)` が 2 つの大きさで出る余地が残る。** 入口の**描画**寸法はどこにも無く、あるのは当たり判定 `S-93` だけである。L-1 〜 L-5 の外なので本計画には入れていない。
5. **⚠️ 書き出しの窓の期間は画面と一致しない**（U-15）。L-7（出力固定）と L-10（横＝画面に映している期間）と L-20（パネル幅そのまま）を同時に満たすと、こうなるほかない。
6. **⚠️ `CP-25` の責務がまた長くなる。** 版 0.28 は `CP-25` の責務が 8 節あることを `R2.2` 違反と判定して `UT-6` で割った経緯がある。本計画は 2 件を足すので、`UF-47` / `UF-48` に割り当てて受けたが、**次のレビューで再指摘されうる。**
7. **⚠️ `01-04-requirements.md:2197` / `:2298` / `:2315` が「キャンバス」を単独で使っており、表 T-006b の `A-8` に触れている。** 本計画の対象外だが、CR-138 の編集 15 が `:2298` を触るので目に付く場所になる。別件として立てるかを決めること。
8. **⚠️ レビュー C-3 の残り**（画面の寸法が変わったときの観測）は CR-140 で閉じるが、**`ScreenRegions` の再計算とデバウンスの粒度は決めていない** —— 表 T-078 の `FT-3` は「契機がある」ことだけを定める。
9. **版と CR の番号は U-12 に依存する。** どれか 1 本を落とすなら、以降の版と CR の数字だけを繰り上げる（本文は動かない）。**当てる直前に `A-appendix.md` の最終行を読むこと。**

## 6.3 5 つの塊のうち、採らなかった部分と理由

**applicable でなかった塊は 1 つも無い。** すべての塊から編集を採ったが、次は**採らなかった**。

| 塊 | 採らなかったもの | 理由 |
|---|---|---|
| M | 「本要求は記号と位置と巡回だけを定める」 | 全数の主張になって偽（`FR-013` は `PM-4` 優先・色の禁止も持つ）。否定形に替えた |
| M | 「本要求が作る規則は 1 つだけである」／`S-4` の式の写し | 数の主張が偽／1.9 の :285 違反 |
| X | 表 T-204 の下に置く「由来」の段落 | 1.9 の :283 が値の表に認める例外の形ではない。`FR-025` の RATIONALE へ移した |
| X | 「表 T-103 を余さず割っている」の締めの一文 | 6 行（`U-37` `U-40` `U-42` `U-43` `U-45` `U-46`）が当たらず偽 |
| X | `EP-2` の理由「別の幅を持つと横の表示期間が画面と変わる」 | `EP-6` が `Properties Panel` を落とす以上、自分で破っている |
| X | 表番号 `T-077` | CR-138 が先に当たるので `T-076` に繰り上げ |
| S | `CP-25` の編集（塊 S の編集 3） | CR-140 と同じセルを取り合う。**CR-140 の 1 編集に畳んだ** |
| S | `CP-36` の「正」に裸の 表 T-206 | 塊 S 自身が編集 1 で禁じた指し方 |
| S | 表番号 `T-076` の予約（F-2 用） | CR-138 が取ったので `T-077` / `T-078` に繰り下げ |
| A | `CP-18` を「日程の描画領域の入力」に狭めること | `FR-070` の `SK-13` 〜 `SK-15` が描画領域の外に働く。**発話の句と `FR-066` だけ外した** |
| A | `PI-37` の `commandFromScreenInput`（「全数は `FR-016`」） | `FR-016` の面の表は 5 面だけで全数の主張が偽。外側の面の入力も `IF-2` から入る形にした |
| A | `PI-37` の `screenStateFromInput` | `Esc` の階層（`IN-4`）を通して見る者が要るので `PI-18` へ置いた |
| A | 辺 `ScreenRenderer → ApplyDocumentChange` | 書き込みは `CP-18` の 1 本を通る（`MS-1`）。引くと入口が 2 つに割れる |
| A | `model.json` の 5 編集のインデント | 実測と 1 空白ずれていた（0 件で当たらない）。正しい深さに直した |
| A | `MN-8` の「`CP-37` の正が挙げる要求を受けるコンポーネントが 1 つも無かった」 | `FR-066` は `CP-16` / `CP-33` が既に持つので偽 |
| A | `audit-ch5.py:59` のコメントの「34」 | 過去のレビューの記録であって今の全数の主張ではない |
| R | C-1 をレビュー案（新コンポーネント `WorkingCalendar`）で閉じること | `R2.9`。`CP-1` に持たせれば辺も数も 1 つも動かない |
| R | CR を 2 本に割る案（CR-139 / CR-140） | 塊 A を入れると `CP-25` と数の主張を取り合う。5 本の割り方に組み直した |
| R | `S-128` の備考の理由文 | `S-106` の写しで dup-check が赤になる（実測） |
| R | 版 0.33 / 0.34 と CR-139 / CR-140 の番号 | 5 本の順に合わせて 0.34 / 0.35・CR-140 / CR-141 に繰り下げ |

---

# 7. 利用者の判断が要ること

**ここを読んで一度に決めてください。** 推奨のとおりでよければ「推奨どおり」で構いません。

---

## U-1 — `S-22` を席を保って改名するか、欠番にして新設するか

**何が問題か。** `markerMin`（マーカー径の**下限**）を `markerSize`（マーカー径**そのもの**）に変える。値 16px・単位 px・群「進捗マーカー」・上限 `actualMin`・下限 `fontMin` は 1 つも動かないが、**「下限」から「値」へは概念が変わる。**

| 選択肢 | 帰結 |
|---|---|
| **(a) 席を保って改名**（`S-22` = `markerSize`、`K-22` = `markerSize`） | 波及が 2 行に閉じる。`S-22` を指している箇所は 0 件、`markerMin` の字面も 2 行だけ。差分が「名前だけ」に見える |
| (b) `S-22` / `K-22` を欠番にし、`S-128` / `K-107` を新設 | 履歴で「概念を変えた」ことが席番号に残る。ただし**欠番は「概念を捨てた」印として使われてきた**（`S-52` の `statusDate`、`FR-050`、`RC-10`）ので、値も範囲も残る行に使うと印の意味が薄れる。さらに `S-128` は CR-141 が取る予定なので `S-129` へ玉突きする |

**推奨: (a)。** 本計画の編集 3・6 はこの形で書いてある。

---

## U-2 — `FR-013` の「実績バーより大きく描いてはならない（MUST NOT）」を残すか消すか

**何が問題か。** ⚠️ **消すと、この禁止は仕様書の規範文から完全に消える。** 機械検索で、この禁止を述べているのは `01-04-requirements.md:2036` **だけ**である。編集後に残るのは `S-22` の「範囲の理由」欄と表 T-026 の `RC-11` だけで、どちらも規範ではない。しかも新しい `S-22` は上限 = `actualMin` なので、**`actualMin` を 16 から上げれば `markerSize` も 16 超へ上げられる形**になる。

| 選択肢 | 帰結 |
|---|---|
| **(a) 消す**（編集 2 のとおり） | 比 1 以上の形状（矩形・矢羽根・マイルストーン）では 16px = `actualMin` なので**恒真**であり、実装に比較を 1 つ書かせるだけになる（L-2 に反する）。⚠️ **比 0.5 の細線（`shapeHeightOf.arrow` / `endpointSpan`）では今日すでに破れている** —— 等倍で実績バーは 10.2px、床では 8.0px しかないのに `markerMin` の床が効いて 16px になっている |
| (b) 残す | **その 2 形状が固定化とは無関係に違反状態になり**、形状ごとに径を変える分岐（＝ L-2 が避けたかったもの）を招く |
| (c) 置き場を替えて残す | `FR-094` に「`S-22` の上限が `actualMin` を超えないこと」という**設定値の範囲**の形で置く。その範囲は既に `S-22` の上限欄が持っているので、実質は編集 3 で足りる |

**推奨: (a)。** ただし「禁止が 1 文も残らないこと」を承知のうえで決めてください。(c) を採るなら編集 3 に上限の縛りを 1 文足すだけで済みます。

---

## U-3 — `S-63` に `FR-094` への指しを足すか

**何が問題か。** 表 T-202 で `S-62`（依存線）だけが `FR-094` を指し、`S-63`（進捗マーカー）が無言である。**レビュー F-1 は、この非対称を「設定表は `FR-013` の側だけで作られている」ことの証拠として挙げた。**

| 選択肢 | 帰結 |
|---|---|
| **(a) 足す**（編集 5） | 文言を `S-62` と同型にすれば新しい主張は 1 つも生まれず、書き写しにも当たらない（1.9 が禁じるのは指す先の**文**の書き写しであって、ポインタの定型ではない）。コストは 1 セル |
| (b) 足さない | 「表 T-202 は表示の可否の表であって寸法の表ではない」という切り分け。F-1 の指摘が「解決したが痕跡が残る」形になる |

**推奨: (a)。**

---

## U-4 — `actualMin` が実行時の床であることを要求として書き足すか

**何が問題か。** ⭐ **これは L-3 の根拠そのものである。** 仕様書には現在、**縦の鎖に max() を当てる規則が 1 文も無い**（「下限」の全 6 箇所を読んで確認。いずれも文字か LOD の話である）。`S-4` の下限欄 `actualMin ÷ actualOfPlan` は**設定値の可動範囲**であって実行時の床ではない。床が無いと、`zoomY` を下げた実績バーは 0 へ向かい、**固定 16px のマーカーは必ずそれより大きくなる。** 現に `FR-013`:2036 は「低いズームでは実績バーが数 px まで縮む」と書いており、床がある読み方と矛盾している。前プロジェクトの実装は床を持っており、**床を 1 か所（比を掛ける前の予定の縦幅）だけに当てる**のが要点だと自らコメントに残している。

| 選択肢 | 帰結 |
|---|---|
| **(a) `FR-094` に足す**（編集 11） | 寸法の持ち主が床も持つ。`FR-094` の「本要求は新しい規則を作らない」を書き替える必要がある（編集 11 は同じ 1 行で処理してある） |
| (b) `FR-077` に足す | 表題が「画面の**文字**を可読の下限より小さくしない」なので、縦寸法まで広げると表題と合わなくなる |
| (c) 足さない | L-3 の「条件分岐なしで成立する」が仕様書の上では成立しないまま残る |

**推奨: (a)。** ⚠️ **床は形状の比を掛ける前に当たるので、比 0.5 の細線では実績が床の半分（8px）になる。** L-3 の保証が効くのは比 1 以上の形状だけである（U-2 と同じ論点）。

---

## U-5 — `ScreenState` の名が 表 T-203 の表題「画面の状態」と衝突する

**何が問題か。** 表 T-203（`tbl-settings.md:142`）の 10 行はいずれも**文書に保存する**値であり（`FR-063` / `DR-3`）、`ScreenState` が持つのは**保存しない**値である。**正面から逆になる。**

| 選択肢 | 帰結 |
|---|---|
| **(a) 名は `ScreenState` のまま、表 T-103 に `U-51` を足して呼び分けを明記**（編集 36） | 既存の `Selection`（`U-39`）と `ArmedShape`（`U-38`）に並ぶ素直な名詞のまま。⚠️ **前例と一部食い違う** —— `CP-32` / `CP-33` / `CP-34` を足したときは用語集に 1 行も足していない（表 T-103 にコンポーネント型名は現在 0 件） |
| (b) 名はそのまま、用語集に行を足さない | 前例には忠実。ただし**衝突を止めるものが何も無くなる**（F-1 と同じ型の曖昧さ） |
| (c) 名を替える（例 `ScreenSession`） | 衝突が消える。⚠️ **編集 1・3・4・5・12・36 と `model.json` 5 編集の全部が書き替わる** |

**推奨: (a)。** `U-38` `ArmedShape` と `U-39` `Selection` が既に表 T-103 にあり、`ScreenState` が持つのはまさにその 2 族なので、行を置くのは一貫している。この行は**名前についての禁止**なので用語集に置いてよい（検査 12 に掛からないことを実測済み）。

---

## U-6 — 構え（`ArmedShape`）を `ScreenState` に入れるか、別コンポーネントにするか

**何が問題か。** 表 T-028 の `IN-4`（`:3628`）が「**消費する階層は 開いている面 → 進行中のドラッグ・引きかけの矢印 → 構え → `Dual Cursor` モード の順とすること（MUST）**」と両者を 1 つの MUST で並べており、**どちらか一方のコンポーネントからは検査できない。**

| 選択肢 | 帰結 |
|---|---|
| **(a) 入れる**（本計画） | `MN-5` が `Document` を立てたのと同じ形（1 つの群からは全体の規則を検査できない）。`escapeTarget` が階層を 1 か所で見る |
| (b) 別コンポーネントにする | `CP-37` / `PI-37` / `UF-60` が要り、**全数が 39 / 72 になる。** 編集 3・4・12 と §C の数がすべて動く |

**推奨: (a)。** ⚠️ 本計画は (a) で書いてあるので、(b) を採るなら数の再計算が要ります。

---

## U-7 — スクロールバーの太さの置き場（仕様書に値が 1 件も無い）

**何が問題か。** `SC-4`（`:2294`）が「横・縦とも常時表示する。内容が収まっていても消さない」を MUST にしているので、**太さは `Row Area` の幅と高さから必ず引かれ、`regionsFromScreen` の入力になる。** ところが値が仕様書のどこにも無い（機械で確認した）。

| 選択肢 | 帰結 |
|---|---|
| **(a) 表 T-212 に `scrollbarThickness`（`S-129`）を新設** | 宿主のスクロールバーは内容が収まると消えるので `SC-4` を満たせず、GRS が自分で描くことになる。ならば太さは読む人の環境ではなく**本ソフトウェアの値**であり、表 T-206 ではなく表 T-212 に落ちる。⚠️ **表 T-212 の表題「画面の各部の寸法の上限（`FR-051`）と、待ち時間」も直す** |
| (b) 宿主から毎フレーム受ける引数にする | `regionsFromScreen` の引数が 3 つになる。値は仕様書に持たない |
| (c) 閉じない | `ScreenRegions` の入力が 1 つ決まらないまま `M1` に入る |

**推奨: (a)。** ⚠️ **本計画には入れていない**（未決だからである）。採るなら CR-139 に 2 編集（表 T-212 の表題と行）＋ `PI-35` の引数の 1 語が足される。席は **`S-129`**（`S-128` は CR-141 が取る）。

---

## U-8 — 浮遊コマンドパレットの位置の置き場

**何が問題か。** `FR-053`（`:2328`）は「作成者がドラッグで動かせるようにすること」と定めるのに、`S-99e` が持つのは**表示状態**だけで、**位置の行が仕様書のどこにも無い**（機械で確認した）。

| 選択肢 | 帰結 |
|---|---|
| **(a) 表 T-206 に位置の行（`S-130`）を足す** | 足さないと `ScreenState` が**仕様書のどこにも無い値**を持つことになる |
| (b) 閉じない | 同上のまま `M1` に入る |

**推奨: (a)。** ⚠️ **本計画は先回りしない** —— `PI-36` の `screenStateWithPalette` の説明は「（`S-99e`）」までにしてある。U-8 が閉じたら括弧の中に位置の行を足すだけで済む。

---

## U-9 — 画面の枠を組み立てるコンポーネントを 1 つに畳むか、2 つに割るか

**何が問題か。** ヘルプ・ツールチップ・`App Header`・パレット・プロパティパネル・知らせを担う者が 1 つも無い（F-3(a)）。作るとして、いくつ作るか。

| 選択肢 | 帰結 |
|---|---|
| **(a) 1 つに畳む**（`ScreenRenderer` 1 本、UI パーツごとに 11 ユニット） | ① 5.2 の分割基準（:84）を字義どおり当てると常設・重ね面の 2 つでも足りず **10 コンポーネント**になる ② 設計自身が `UT-6` で「分割基準は**ユニットの側でも同じことを言う**」と書いている ③ 先例が 2 つ —— `EditDocument` は集約ごとに要求が別なのに 1 コンポーネント 9 ユニット、`DocumentCodec` は形式ごとに正が別なのに 1 コンポーネント 5 ユニット |
| (b) 常設と重ね面で 2 つに割る | 境目としては唯一自然だが、**`ScreenState` と `ScreenRegions` を両方が引くので辺が 2 倍**になり、`MN-8` の代償欄が重くなる。全数は 39 / 71 |

**推奨: (a)。** 本計画は (a) で書いてある。

---

## U-10 — 確定発話の入口を `PI-18` に足すか、`ScreenRenderer` へ移すか

**何が問題か。** `CP-18` に課された「対話欄で確定した発話を渡す」を起動する公開メンバも供給物も無い（F-3(b)）。

| 選択肢 | 帰結 |
|---|---|
| **(a) `ScreenRenderer` へ移す**（本計画） | `IF-2 InputSource` が供給するのは「ポインタとキーの出来事」だけで、確定した発話はそこに載らない。⚠️ **`model.json` の辺 1 本を付け替える**（編集 40）。`PI-18` と `IF-2` は 1 文字も変えない |
| (b) `PI-18` に足す | `InputCommandTranslator` が「入力を操作へ変える」ものであることが崩れ、`IF-2` の供給物も広げることになる |

**推奨: (a)。** ⚠️ 塊 A の検算が「U-10 だけ問いの形になっていない」と指摘したので、ここに明示します。**発話の中身を作るのは AI である（L-28）以上、GRS 側は「対話欄で人が確定したものを渡す」だけであり、それは対話欄を持つ者の仕事です。**

---

## U-11 — `ScreenRenderer` が 11 ユニットになる。モジュール（`SU-2`）を 0 のまま保つか

| 選択肢 | 帰結 |
|---|---|
| **(a) 0 のまま保つ** | ① `R2.9`（要らないうちは作らない）を 5.3（:187）が既に採っている ② `EditDocument` が 9 ユニットを平らに並べた先例があり、11 は質の違う数ではない ③ **モジュールを 1 つでも入れると `01-04-requirements.md:351` の `A-17` まで直すことになり、Chapter 1 の規約表に手が伸びる** |
| (b) 入れる | 表 T-074 の `SU-2` と `A-17` を同時に直す |

**推奨: (a)。** 実装で束ねる必要が出てからで足ります。

---

## U-12 — CR を何本に割るか、どの順で入れるか

**何が問題か。** 119 編集を、落ちたときに戻せる単位へ割る必要がある。

| 選択肢 | 帰結 |
|---|---|
| **(a) 5 本**（本計画。137 → 141） | **同じセルを 2 つの CR が開かない**ことを実測で確かめてある。CR-139 が 58 編集と大きいが、**数の主張 9 箇所を共有する 2 つの塊（画面のモデルと面のコンポーネント）を割ると、どちらかを当てた瞬間に他方の原文引用が外れる** |
| (b) もっと細かく割る | 画面のモデルと面のコンポーネントは割れない（上記）。割れるとすれば CR-141 を F-4/F-5/F-6 と C-4/C-5 の 2 本にする程度（`erd.json` を触るのは後者だけ） |
| (c) 1 本にまとめる | 落ちたときに全部戻る。**推奨しない** |

**推奨: (a)。** ⚠️ CR-141 だけは「(b) で 2 本に割る」道も無理なくあります（`FR-043` / `HR-6` / 散文の 3 件 ＝ 要求の穴、`FR-054` / `FR-088` / `erd.json` ＝ 暦と期間）。**どちらでも同じ編集が同じ順で入ります。**

---

## U-13 — レビューの残り（F-2 / F-4 / F-5 / F-6 / C-1 〜 C-5）をどの CR に載せるか

| 件 | 本計画の載せ先 | 理由 |
|---|---|---|
| F-1 | CR-137 | 利用者の裁定そのもの |
| F-2（起動の順序） | **CR-140** | 5.5 の新表 2 枚と `CP-25` に着地する。`BO-1` が `ScreenRegions` を指すので **CR-139 の後**でなければならない |
| F-3（面のコンポーネント） | CR-139 §B | L-27 と同じ判断である |
| F-4 / F-5 / F-6 | CR-141 | Chapter 4 の値と散文だけ |
| C-1（稼働日の持ち主） | CR-140 | 設計側。`CP-1` / `PI-1` / `CP-9` |
| C-2（基準日の集め方） | CR-140 | 表 T-066 |
| C-3（寸法変化の契機） | CR-140 | 表 T-078 と `NFR-010` |
| C-4 / C-5（暦と期間） | CR-141 | `erd.json` を触るのはここだけ |

**推奨: この表のとおり。** ⚠️ **F-2 は `M1` のゲート `PG-4` と `MS-1` に直接掛かるので、`M1` 着手前が期限である。** CR-140 を後ろへ置きすぎないでください。

---

## U-14 — `docs/review/duplication-baseline.txt` を張り直すか

**何が問題か。** CR-139 は表 T-075 に 14 行、表 T-062 / T-064 に 8 行を足し、責務欄の言い回しが近い行が並ぶ。**検査 11 は新規の重複組が 1 つでもあれば赤になる**（ゲート）。

| 選択肢 | 帰結 |
|---|---|
| **(a) CR-139 を当てて 1 度測ってから決める** | 新規の組が 0 なら何もしない。出たら中身を見て、**言い回しを変えるか基準線を張り直すかを 1 件ずつ判断する** |
| (b) 先に張り直す | 本当の重複を見落とす |

**推奨: (a)。** ⚠️ CR-141 の重複は編集 14 で先に潰してある（実測で新規 1 件が出ていた）。

---

## U-15 — 書き出しの窓が画面と同じ期間にならないこと【新規】

**何が問題か。** L-7（出力 1600 × 900 固定）・L-10（横 ＝ 画面に映している期間）・L-20（パネル幅そのまま）を同時に満たすと、**出力の `Row Area` の幅は画面のそれと違う**（画面が 4K なら画面のほうが広い）。倍率 `zoomX` と左端 `scrollDate` が同じである以上、**横に映る日数は必ず変わる。**

| 選択肢 | 帰結 |
|---|---|
| **(a) 受け入れて明記する**（編集 11 の ⚠️ 文） | 「同じ絵」の意味を「倍率・表示位置・表示の切り替え・LOD の結果が同じ」と定義することになる。**L-7 を採る限り、これ以外に成り立つ読み方が無い** |
| (b) 出力の期間を画面に合わせる | 出力の中で `zoomX` を変えることになり、**L-7 と `WY-2` の両方を破る** |
| (c) 出力サイズを画面に合わせる | L-7 の破棄 |

**推奨: (a)。** ⚠️ **これは「WYSIWYG」の語の意味を 1 段狭める判断です。** 承知のうえで決めてください。

---

## U-16 — 表示位置が `null` の文書を書き出すとき【新規】

**何が問題か。** `scrollDate`（`S-77`）と `scrollGroupId`（`S-78`）の**既定はどちらも `null`** であり、表 T-024a の `OP-10` が「`FR-055` の全体表示が選ぶ位置にすること（MUST）」と定めている。全体表示は**画面の寸法**から位置を出すので、そのままでは新しい `WY-2`（機と画面の寸法によらず同じ）が**既定の文書で成立しない。**

| 選択肢 | 帰結 |
|---|---|
| **(a) 書き出しでは `exportCanvas` に対して全体表示が選ぶ位置を使う**（編集 11） | 出力は 1600 × 900 固定なので**機に依らず決まる。** `WY-2` が成立する。⚠️ 画面（機の寸法で全体表示）と出力（1600 × 900 で全体表示）で**位置がわずかに違う**ことがある |
| (b) `WY-2` の判定対象から外す | `FR-055` に前例がある（「`WY-2` の判定はその文書を対象にしない」）。⚠️ **既定の文書がほぼ全部対象外になる** |
| (c) 開いた時点で `OP-10` の結果を文書へ書き戻す | 開くだけで文書が変わる（版数が動く）。`FR-063` と `LM-15` に触れる |

**推奨: (a)。**

---

## U-17 — 廃止した席（`S-21` / `K-21` / `S-52`）を変更履歴でどう書くか【新規】

**何が問題か。** ⛔ **実測で検査 7 が 3 件で落ちる。** 変更履歴がバッククォート付きで書くと「存在しない行 ID」と見なされる。

| 選択肢 | 帰結 |
|---|---|
| **(a) 検査の既知集合（`RETIRED`）に 3 席を足す**（編集 9・10） | 版 0.6 が `FR-050` を欠番として書いたときの前例と同じ。**廃止した席の登録簿になり、将来の誤検出も止まる。** 道具を 2 行直す |
| (b) 変更履歴では素の字で書く（バッククォートを付けない） | 道具を触らない。⚠️ **同じ字が別の書き方で 2 系統に分かれる** |

**推奨: (a)。**

---

## U-18 — 掴んだ瞬間の `actualDuration` を `0` にするか `1` にするか【新規】

**何が問題か。** F-4 の穴を塞ぐには量を決めるしかない。

| 選択肢 | 帰結 |
|---|---|
| **(a) `0`（稼働日）** | `FR-012` が「期間は開始日と終了日の差とし、端を含む日数と取り違えてはならない（MUST NOT）」と定めており、`GR-6`（置いた日付から稼働日数を算出）と同じ数え方になる。⚠️ **実績バーが 0 幅になるが、`actualMin` の床（16px）で見える** |
| (b) `1`（掴んだ日を含む） | 直感には合うが、**`FR-012` の MUST NOT と数え方が食い違う** |

**推奨: (a)。**

---

## U-19 — `S-128`（1 日あたりの分数）の既定と、他の 2 列を置かないこと【新規】

**何が問題か。** 交換相手の期間は時間の量（`xsd:duration`）であり、稼働日の整数から換算するには 1 日の長さが要る。

| 論点 | 選択肢と帰結 | 推奨 |
|---|---|---|
| 既定値 | **`480`（1 日 8 時間）** ／ 他の値 | **`480`。** 🔎 を付けて「選び直してよい値」と明示する。⚠️ **交換相手のツールが `Project/MinutesPerDay` を持たない文書をどう解釈するかは実機で確かめていない** |
| `Project.minutesPerWeek` / `daysPerMonth`（`AT-15` / `AT-16`）にも既定を置くか | 置く ／ **置かない** | **置かない。** 換算に使うのは 1 日あたりの分数だけであり、どちらも `null` を許す省略可能な要素なので、書き出さなくても `EX-1` は満たせる。**置くと由来の無い数が 2 つ増える**（`R2.9`） |
| C-4 を「`Calendar` に列を足す」側で閉じるか | 足す ／ **足さない** | **足さない。** 交換相手が `Project/WeekStartDay` に持つ以上、`Calendar` へ移すと出自が `Own` でなくなり、`erd.json`・表 T-058・表 T-059・MSPDI の往復（`FR-021`）が同時に動く。**要求の文を置き場へ合わせるほうが blast radius が小さい** |

---

## U-20 — `BO-3` が「起動時に全体表示が走ること」を認めてよいか【新規】

**何が問題か。** `OP-10` は「表示位置が `null` のときは全体表示が選ぶ位置にすること（MUST）」と定め、`scrollDate` の既定は `null` である。**つまり新規文書は起動のたびに 表 T-068 を 2 回走らせる。**

| 選択肢 | 帰結 |
|---|---|
| **(a) 認める**（表 T-077 の `BO-3` / `BO-4`） | 既存の `OP-10` と `FR-055` の 2 回走行の規則にそのまま乗る。`NFR-001`（初期描画の時間）に 1 回ぶんの計算が乗る |
| (b) 起動では走らせない | `OP-10` 側の判断になるので**別件として立てる。** 起動直後にどこを映すかを別に決める必要がある |

**推奨: (a)。** ⚠️ `PG-1`（初期描画に要する時間）の測定でここが効くので、**`M1` のゲートで実測して重ければ (b) を再検討する**のが順当です。