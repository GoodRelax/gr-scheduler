# 要求レビュー 第 2 周 — 17 件の修正が作った欠陥と、元からあった欠陥（2026-08-13）

**これは作業用の記録であって仕様書ではない。** `docs/spec/` の外に置いてあるのは、StrictDoc が
`docs/spec` 配下の `.md` をすべて仕様書として解析するためである。**反映が終わったら破棄してよい。**

前提は `requirement-review-2026-08-13.md`（第 1 周。17 件）である。**本書はその続きである。**

## 経緯

第 1 周の 17 件を **5 つの塊に割り、塊ごとに機械検査を回して**直した。そのうえで
**観点を分けた 2 体のレビュー**（矛盾／重複・漏れ）を掛けたところ、**30 件**が出た。

| 由来 | 件数 |
| --- | --: |
| **17 件の修正が作った** | **15** |
| 元からあった（第 1 周が見ていなかった） | 15 |

### 作った 15 件の内訳が、この周で最も重要な事実である

| 種別 | 件数 |
| --- | --: |
| **重複** | **8** |
| 矛盾 | 5 |
| 漏れ | 2 |

> **矛盾を直すとき、片方を直す代わりに両方へ同じ説明を書き足した。**
> `Stop` の Carry は 3 か所、「未着手ではマーカーを出さず `FR-043` の入口が兼ねる」は **5 か所**に並んだ。

**第 1 周の教訓（「まとめて直すな」）は守ったが、別の型を踏んだ。**
分割は衝突を防いだが、**参照で済むところに説明を複製する**癖は分割では止まらない。

> **次の周へ持ち越す規律: 矛盾を直すときは、規則の置き場を 1 つ決めて、残りは参照にする。**
> **書き足す前に「これは既にどこかに書いてあるか」を検索する。**

## この周で直した 15 件

塔ごとに直し、塔の切れ目で機械検査を回した。

| 塔 | 指摘 | 内容 |
| --: | --- | --- |
| **A** | `C-1` `H-2` `H-3` `D-2` | 未着手のマーカー。`PM-1a` と `PM-4` の正面衝突、`FR-013` の巡回順と表 T-021a の食い違い、5 か所の重複 |
| **B** | `D-1` `D-7` `D-8` `D-11` `D-12` `M-1` | 重複の刈り込み（`Stop` の Carry・`S-89` の数え方・LOD の下限・「開くと合流は別」・構えの列挙） |
| **C** | `H-4` `D-6` | `fontScale` と目盛のフォント。「実効フォントサイズ」が未定義だった |
| **D** | `M-2` `M-3` `L-1`/`G-1` `G-6`（＋ `G-10`） | `NL-2` の位置づけ、`Ctrl` ＋ `Shift` の二重解釈、`FR-042` が失った手順、版数の列挙 |

### 塔 A で決めたこと（利用者判断 2026-08-13）

**未着手のまま `start` を過ぎたタスクには遅れの印を出す。ただし進捗マーカーを別に出すのではなく、
`FR-043` の実績入力の入口が `(!)` の形をとる。** —— 画面上の印を 1 つに保ち（`FR-029`）、
`PM-4` の 3 条件と表 T-021b の遅れの日数（`DL-1` 〜 `DL-3`）の 1 対 1 対応も保つ。

### 塔 C で決めたこと（利用者判断 2026-08-13）

**`fontScale` は目盛の文字にも掛かる。** `rulerFont`（`S-3`）の既定を `fontScaleSizes[fontScale]` に、
`rulerHeight`（`S-2`）の既定を `rulerFont × 3 + 6`（S = 42 / M = 48 / L = 54）に追随させた。
⚠️ **キーは独立に保つ（MUST）。UI を試したうえで固定の定数へ戻す可能性がある。**

### 塔 D で `G-10` を巻き込んだ理由

`G-6`（版数の非保存の列挙が全数でない）の直し方を「散文の列挙をやめて表 T-206 を指す」にしたので、
**本日線が表 T-206 に行を持っていないと、指した先から本日線が落ちる。** `G-10` を先に閉じないと `G-6` が閉じない。

---

# 第 3 周 — 第 2 周の修正が作った 11 件と、その場で決まった設計

第 2 周のあと同じ 2 系統でレビューを掛け、**新規 15 件**（うち **11 件が第 2 周の作業に由来**）を得た。
**周ごとの推移: 17 → 15 → 11。減ってはいるが 0 になっていない。**

## 塔 E — 取込の経路（利用者判断 2026-08-13）

**レビューの `G-A`**: 第 2 周が `OP-2` に書いた MUST「取込の入口（`FR-022`）とは別の面・別の操作として
区別すること」が、**存在しない入口を指していた。** 合流の中身（表 T-032 の 12 行）は完全に定義済みなのに、
そこへ人が到達する経路が仕様のどこにも無かった。

**利用者の回答**: 入口は「開く」1 つ。落とした時点で**置き換え / 合流**を選ばせ、
同じ ID が両側にあるときにどちらを優先するかを選ばせる。**初回も追加も同じ方法。**

`previous-project-result/03-ui-naming/appheader-palette-icons.html`（アイコンの正。`FR-029`）にも
`title="開く（.json / .xml）"` が 1 つあるだけで、取込／インポートのアイコンは存在しない。**裏が取れている。**

| 直した場所 | 内容 |
| --- | --- |
| `OP-2` | **入口は「開く」1 つ。取込に別の入口を設けてはならない（MUST NOT）** |
| `OP-3` | **置き換えか合流かを人に選ばせること（MUST）。システムが勝手に決めてはならない（MUST NOT）** |
| `OP-4` | 未保存の編集の確認を**置き換えを選んだときに限定**（合流では捨てない） |
| `FR-022` | 契機を「2 つ目以降の MSPDI」→「**合流が選ばれ、同じタスクが両側にあるとき**」 |
| `FR-056` | 「2 つ目以降の MSPDI を合流させるとき」→「**読んだファイルを現在の文書へ合流させるとき**」 |
| `UC-010` 手順 1 / 拡張 1a | 開く → 選ばせる の流れに揃えた |
| `FR-087` | `ORIGIN` と `Relations` に `UC-010` を追加（合流への入口も兼ねるため） |
| 3 か所 | 「**開くと合流は別の操作である**」を削除（この設計では誤り） |

⚠️ **新しい要求は立てていない。** 扉は既にある `FR-087`（開く）だった。

## 塔 F — 矛盾の後始末

| ID | 内容 |
| --- | --- |
| 矛盾 1 | **`rulerFont` を保存値のまま「既定が `fontScale` に追随」と書いていた。** 一度書き出した文書は実値を持つので追随せず、`FR-039`「保存された値が読む人の指定を強制してはならない（MUST NOT）」を破っていた。→ `S-2` / `S-3` に「**保存値は初期値であり、`fontScale` を変えたときは追随して書き換わる（文書の編集になる）**」と明記 |
| 矛盾 2 | **`NL-2` を「置き場所を確定させない前処理」と書いたことで、「打ち切ったら収まる」という最も普通の場合に成立する行が 1 つも無くなった。** → 打ち切りを表の**外**へ出し、表 T-013 を `NL-1` / `NL-3` の 2 行にした（`NL-1` の条件は「打ち切った後のラベルが収まる」）。**`D-9`（値の重複）もここで解消**（`S-35` を指す形にした） |
| 矛盾 4 | `MK-12` の「何もしない」が `PD-3`（当たったらそのものへの操作）と衝突。→ 「何もしない」を落とし、**何が起きるかは表 T-023a が決める**とした |
| 矛盾 3 | `UC-006` 手順 3 が「進捗マーカーを遅れの形にする」と書き、未着手にマーカーが無い塔 A の決定と噛み合っていなかった。**未着手で遅れているタスクの印が、ユースケースからの鎖に載っていなかった。** → 手順 3 を書き直し、`FR-043` の `ORIGIN` と `Relations` に `UC-006` を追加 |

## 塔 G — 重複の刈り残し

**塔 A の「5 か所 → `PM-1a` 1 か所へ寄せる」は寄せ切れていなかった** —— 参照に変わったのは 2 か所だけで、
規則の再掲が 3 か所残り、`(!)` の新しい規則を 2 か所に増やしていた（`D-A`）。

`D-A`（`PM-1a` / `FR-013` / `FR-043` の刈り込み。`(!)` の MUST は `FR-043` に置き、`PM-1a` は指すだけ）／
`D-B`（`FR-013` が表 T-021a を散文で言い直していた）／`D-C`（`S-89` が「挙動は `ST-7` が持つ」と書く直前に挙動を述べていた。`S-99d`）／
`D-E`（`FR-055` がスクロールを 2 か所）／`D-G`（`FR-049` が同じ禁止を段落の頭と末尾で）／
`G-D`（`S-99g` の「など」で全数にならない → 表 T-028 の `IN-4` が定める区分を指す形に）

---

# 第 4 周 — 第 3 周の修正が作った 11 件

第 3 周のあと同じ 2 系統でレビューを掛け、**新規 15 件**（うち **11 件が第 3 周の作業に由来**）を得た。
**周ごとの推移: 17 → 15 → 11 → 11。**

## Critical — `OP-6` が `OP-3` の「合流」を打ち消していた

**2 体のレビューが独立に同じ穴を指した。**

```
OP-1（元からある）  受け付ける形式 │ JSON と MSPDI XML
OP-3（塔 E で書いた）読んだ内容を「置き換え / 合流」から選ばせること（MUST）
OP-6（元からある）  JSON を読んだとき │ 文書のデータと documentSettings を復元すること
                                        ↑ 条件が付いていない
```

**JSON を開いて「合流」を選ぶと、「現在の文書へ足す」と「読んだファイルの設定を復元する」が同時に掛かる。**
第 3 周までは `OP-3` が「置き換える」だったので `OP-6` と整合していた。**塔 E が `OP-3` を書き換えたときに `OP-6` を見ていなかった。**

さらに、**合流の規則（表 T-032）は全 12 行が MSPDI 前提**で、JSON が持つ `TaskGroup`・注記・`documentSettings` の
受け皿が 1 行も無かった。

**利用者判断（2026-08-13）: 文書の設定が衝突したら選ばせる**（`MG-4` と同じ形）。
→ `OP-6` を「**置き換えを選んで** JSON を読んだとき」に限定し、**表 T-032 に `MG-12` を新設**した。

## その他に直した 11 件

| 由来 | 内容 |
| --- | --- |
| 矛盾（塔 G） | `FR-043` の「未着手の状態を変える入口は唯一」が、**プロパティパネルと `Agent API` を否定していた**（表 T-016 の `PR-4` 〜 `PR-8` は 5 状態を判別する列そのもの）。→「**画面上で直接掴む経路のうち**唯一」に限定 |
| 矛盾（塔 F） | `MK-12` を「**修飾キー**に割当を与えない」と書いたため、`MK-4`（`Alt` ＋ ホイール）/ `MK-5`（`Ctrl` ＋ `Shift` ＋ ホイール）の割当まで否定していた。→ `MK-10` と同じ**組合せ単位**に直した |
| 重複（塔 G の刈り残し） | `PM-1a` が「参照だけ」になっておらず規則本体と理由を保持。`FR-011` も 3 か所目を作っていた。→ `PM-1a` を「入口とその形は `FR-043` が定める」だけに |
| 重複（塔 E） | 「入口は『開く』1 つ」が `OP-2` / `FR-022` / `FR-087` の **3 か所**。→ `OP-2` の MUST NOT 1 か所に |
| 重複（塔 E の帰結） | `MG-9a` と `OP-8` が同じ規則と理由。入口が 1 つになった結果 `OP-8` が上位になった。→ `MG-9a` は `OP-8` を指すだけに |
| 漏れ（塔 F） | 「`fontScale` に追随して `rulerFont` / `rulerHeight` の保存値が書き換わる」が**値の表にしか無く、受ける要求が無かった**（`D-5` の裏返し）。→ `FR-039` に 1 段落 |
| 表題 | 表 T-021a の「**マーカー**を押して」が `PM-1a`（未着手はマーカーを出さない）と食い違う。`PV-1` の押す前は未着手。→「**印**を押して」 |
| `UC-006` STATEMENT | 「イナズマ線と**進捗マーカー**で示す」が、自分の手順 3（塔 F で書き直し）を覆っていた。→「タスクごとの遅れの印」 |
| `S-99g` | 塔 G が「など」を消して `IN-4` を指したが、**`IN-4` は「開いている面」を名指しするだけで全数を持っていなかった。** → 列挙ではなく**定義**を置いた |
| `S-89` | `256` → `255` の理由が記録されていなかった。→ 利用者判断であり測って決めた値ではない旨を明記 |
| `FR-043` | 「未着手と分かる見た目」と「遅れたら `(!)`」が両立するかが書かれていなかった（`(!)` は進行中・中断の遅れと同じ記号）。→ **未着手であることは位置が担う**（`GR-9` はタスクの上、`GR-7` は実績バーの右端の外側）と明記 |

---

# 次の周へ回す件

**利用者判断（2026-08-13）: 記録だけして次の周へ回す。**
いずれも**第 1 周の 42 件・17 件より前から存在していたもの**で、この一連の修正が作ったものではない。

⚠️ **このうち `D-9` は第 3 周の塔 F で、`G-A` と `G-10` はそれぞれ塔 E・塔 D で解消済みである**（他の修正がそれに依存したため）。
第 3 周のレビューが新たに見つけた既存分 **`D-D` / `G-B` / `G-C`** を末尾に足した。

## 重複

| ID | 重大度 | 場所 | 内容 |
| --- | --- | --- | --- |
| `D-3` | High | 表 T-023 の `MK-6` / `MK-7` / `MK-8` ↔ 表 T-023a の `PD-1` / `PD-5` / `PD-3` ＋ 表 T-023c の `SL-7` | 判定順序が正（`FR-016` の RATIONALE がそう述べる）なのに、割当の表が同じ答えを持つ。`FR-036`（ヘルプ）が両方を人に見せるので食い違いが露出する。**直し方**: `MK-6` / `MK-7` を落とし、表 T-023 は表 T-023a を指す。`MK-8` の MUST 句を落として `SL-7` を指す |
| `D-4` | High | `FR-085` ／ `FR-042` ／ 表 T-023 の `MK-13` | **行の名前を編集する入口が 3 つの面にある**（`FR-029` 違反）。`F-15` が「行の色と高さ」の重複を潰したのに、同じ `FR-042` の「名前」が残った。**直し方**: `FR-042` の対象から名前を外し、行名は `FR-085` 1 本にする |
| `D-5` | Medium | `tbl-settings.md` の 5 か所（`:156` `:160` `:162` `:166` `:164`）が `FR-052` / `FR-042` / `FR-007` / `FR-041` / `FR-039` / `NFR-007` と一字一句同じ規則と理由を持つ | 「名前と値を 2 か所で管理しない」の姉妹として、**規則も 2 か所で管理してはならない**。値の表が MUST を持っている箇所もある。**直し方**: 規則と理由は要求側に一本化し、値の表は「規則は `FR-xxx` が持つ」と指す（`S-105` が正しい書き方の例） |
| `D-9` | Medium | 表 T-013 の `NL-2` ↔ `S-35`（`truncateUnits`） | 要求の表が実値（全角 12 / 半角 24）を持っている。**直し方**: `NL-2` を `S-35` の参照にする |
| `D-10` | Medium | `G-2` ／ 表 T-015 直後 ／ `HM-3a` ／ `FR-058` | `TaskGroup` の最大 5 段が 4 か所に実値で書かれ、`tbl-settings.md` にキーが無い。**直し方**: `maxGroupDepth` を値の表に 1 行置き、4 か所をその参照にする |

## 漏れ

| ID | 重大度 | 場所 | 内容 |
| --- | --- | --- | --- |
| `G-2` | High | `S-67`（`dateGridLinesVisible`）／ `U-17` ／ `K-82` | **日付罫線を描く要求が 1 件も無い。** 要求文書に「日付罫線」が 0 回しか出ない。グループ罫線は `FR-042` が描く要求を持つ。**直し方**: 「日付ごとの縦線を目盛の段階に連動する間隔で描く」要求を 1 件立てる。`RC-5` の置き場にもなる |
| `G-3` | High | `UC-003` 手順 4 ／ `OC-2` ／ `S-61` | **完了率ラベルを描く要求が無い。** 担当ラベルには `FR-059` があるのに対応物が無く、`FR-012` が「100 を超えうる」と定めた値をどう描くかが決まらない。**直し方**: `FR-059` に足すか `FR-012` に描画の 1 文を加える |
| `G-4` | Medium | `S-71`（`importSeq`）／ `K-86` | **読む要求も書く要求も無い。** 「取込のたびに +1」を誰が実行するか、何に使うかが要求に無い。**直し方**: 表 T-032 に行を足して `MG-1` / `MG-3` の照合での使い方を書く。使わないなら `S-71` を落とす |
| `G-5` | Medium | 表 T-037（5 行） | **「受け付けたうえで注意を伝える」場面の行が無い。** `FR-054`（繰り返し例外日）・`EX-3`・`FR-061`（平文で残る）・`FR-065`（渡した参照）・`FR-020`（証跡であること）・`MG-10` / `MG-11` の 7 件がどの行にも当たらない。**直し方**: `NT-5` を 1 行足す |
| `G-7` | Medium | 表 T-030 ↔ 表 T-036 | **`KB-1` `KB-2` `KB-3` `KB-6` `KB-9` `KB-11` `KB-12` に鍵が 1 つも割り当てられていない。** `FR-070` は割当の正を表 T-036 としており、`FR-050`（キーボードだけで完了）の検証ができない。**直し方**: 表 T-036 に対応する行を足すか、表 T-030 に「割当は `SK-xx`」列を足して空欄を可視化する |
| `G-8` | Medium | `FR-029` | **「例外は表示言語の切替だけ」が、文書の他の決定と釣り合っていない。** 本書は `FR-011` の ⚠️・`FR-006` の RATIONALE・`FR-049` で「値の面と直接操作の面の二面編集」を明示的に許している（`shapeKind` / `start` / `finish` / `nameAnchor` が実例）。**直し方**: `FR-029` に「同じ値を、直接操作の面と値として編集する面の両方から編集できることは対象外」と足し、対象外の全数を表で持つ |
| `G-9` | Medium | `UC-001` 手順 3 | **「作成者がタスクに名称を入力する」を受ける要求が無い。** 行名には `FR-085` を新設したのに、タスク名には対応物が無い。入口は `MK-13` / `GR-10` / `PR-1` の表の行にしかない。**直し方**: `FR-002` の `ORIGIN` に手順 3 を足すか、`FR-085` と対になる要求を立てる |

⚠️ **`G-10`（本日線が表 T-206 に行を持たない）は第 2 周で閉じた** —— `G-6` の直し方がそれに依存したため。

## 第 3 周のレビューが新たに見つけた既存分

| ID | 重大度 | 場所 | 内容 |
| --- | --- | --- | --- |
| `D-D` | Medium | 表 T-021 の `PM-4` ↔ 表 T-021b の `DL-1` 〜 `DL-3` | **遅れの 3 条件が一字一句同じ文で二重に載っている。** 「1 対 1 に対応する」という注は対応を宣言しているだけで、片方だけ直す事故を防げない。**直し方**: 条件の全数は表 T-021b が持ち、`PM-4` は「**導出**。条件は表 T-021b」と指す |
| `G-B` | Medium | 表 T-024 の `IO-6`（クリップボードへの画像書き出し） | **実行する要求が無い。** `FR-025` は `IO-3` / `IO-4` だけを対象にし、`FR-033` は「読まない」文脈で触れるだけ。入口も無い。**直し方**: `FR-025` の対象に `IO-6` を足す（`LM-8` がこの経路にも掛かることを含めて） |
| `G-C` | Medium | `ST-5`（積む向き）／ `S-58` | **人が選ぶ入口が要求として無い。** `FR-049` が「積む向きはトグルではない」と明示的に対象から外しており、外された 4 つのうち文字サイズ（`FR-039`）・デュアルカーソル（`FR-082`）・取込の連番（`G-4`）に対し、**積む向きだけが受け手を持たない。直し方**: `FR-003` に「積む向きを文書全体で選べること」を 1 文加える |

## 第 4 周のレビューが新たに見つけた既存分

| ID | 重大度 | 場所 | 内容 |
| --- | --- | --- | --- |
| `D-H` | Medium | 表 T-038 の見出し ↔ `FR-055` | 「**2 か所で別々に数え上げてはならない（MUST NOT）**」が一字一句で 2 か所。**直し方**: 規則は表 T-038 の見出しに置き、`FR-055` は「算入するものは表 T-038 に従うこと（MUST）」だけにする |
| `D-I` | Medium | `S-68` ↔ `FR-042` ／ `tbl-settings.md` §4 の担当・完了率ラベルの根拠 ↔ `FR-049` ／ 表 T-211 の注 ↔ `FR-023` | **`D-5`（値の表が規則と理由を持つ）と同型が、`D-5` が挙げた 5 行の外にも 3 か所ある。** `D-5` を直すときに同時に適用すること |
| `D-J` | Low | 表 T-023a の注 ↔ `FR-049` | 「描いていないものが当たると、何も無い場所でドラッグしたのに既存の要素が動く」が 2 か所。`FR-049` は既に表 T-023a を指しているので理由の一文を落とせる |
| `G-E` | Medium | 表 T-027 ／ `FR-063` | **「文書全体の設定を変える編集」の行が表 T-027 に無い。** `FR-063` 自身が「表 T-027 の**対象行に加えて**、文書名・基本情報・テーマ・行の色と高さも含む」と述べており、`FR-031`（取り消しの対象は表 T-027 に従う）ではテーマ・文字サイズ・文書名を取り消せるかが決まらない。**直し方**: 表 T-027 に 1 行足し、`UN-3` の射程を「`Task` のプロパティ」に限定する |

## 第 4 周の未確認

| # | 場所 | 疑い |
| --: | --- | --- |
| 9 | `OP-3` の 2 択を**いつ**出すか（`OP-5` の検証の前か後か） | `MG-6`「取込をやめたら取込前と完全に同じ」との前後関係が読みに依存する |
| 10 | `S-2` と `S-3` が「キーは独立に保つこと（MUST）」を双方に持つ | 別キーなので各行に MUST が要るとも読め、重複と断定できない |
| 11 | 新規文書（`FR-027` のテンプレート）にファイルを落としたときも `OP-3` の 2 択を必ず問うのか | `OP-3` の MUST は例外を持たないが、合流相手が空の場合の扱いが書かれていない |
| 12 | 表 T-205 の注「保存値が読む人の設定で動くと、同じ文書から同じ絵が出なくなる」 ↔ `S-3`「`fontScale` を変えたら追随して書き換わる」 | しきい値の案を退けた理由の書き方が広すぎる（`S-3` は「文書の編集になる」ので §1 は破れないが、文面が自分の首を絞めている） |

## 未確認（両立しないとまでは言えないが、読みが 1 つに定まらないもの）

| # | 場所 | 疑い |
| --: | --- | --- |
| 1 | `ST-7` / `S-89` ↔ `FR-058` | 「人が置いた `Task`」が、取込で 5 段目の行へ載せた `Task` を含むか読めない。含まないと読むと、段が最も膨らむ経路で安全弁が働かない |
| 2 | `progressMarkerVisible` = `false` ↔ `GR-9` | 表示を切ったとき `FR-043` の入口も消えるのかが決まらない。`FR-049` の理由文は「**着手済み**タスク」とだけ言っている |
| 3 | `OC-3` ↔ `PM-1`「選択時のみ表示」／`PM-1a` ↔ `FR-055`「測る対象は描画の実寸」 | 描かれていないマーカーを占有幅に算入するのか、算入しないなら選択のたびに段割当が変わるのかが読めない |
| 4 | `S-93`「ダミーの実績線の当たり判定」 ↔ `GR-9`（実績入力の入口） | 同じものを別名で呼んでいる可能性。用語辞書に「ダミーの実績線」は無い（`CN-9` / 1 概念 1 語） |
| 5 | `FR-017` の契機「表示倍率が変わったとき」 | `fontScale` を変えると倍率を触らずに目盛の段階が変わる（表 T-205 の比による判定）。契機の書き足しが要るかもしれない |
| 6 | `UC-008` 手順 1「指す先を決める」 | `AR-5` から「当たった対象を指す先にする」経路を削った結果、指す先を決める操作は `GR-14` から間接的に読むしかない。落ちたのか、もともと `GR-14` で足りるのかを判定できていない |
| 7 | `U-22` `Row Title Panel` ↔ `U-23` `Row Title Tree` | 要求側で「パネル」と「ツリー」が同じ操作面を指しているように読める。別部品として設計されるか次第 |
| 8 | `NT-3` の括弧内（連鎖削除・取込による上書き・担当者名の波及） | 全数か例示かが読めない。`FR-088`（暦の変更で影響件数を知らせる）が括弧に無い |

---

# 機械検査（10 本。すべて緑）

コマンドは `handoff-2026-08-13.md` §2 のものに 4 本を足したものである。
**第 1 周で足すべきとした 3 本は実装済みで、うち 2 本がその場で `F-11` を捕まえた。**

| # | 検査 | 由来 |
| --: | --- | --- |
| 1 | ノード型ごとの件数 | 既存 |
| 2 | 親を持たないノード | 既存 |
| 3 | `ORIGIN` と `Relations` の不一致 | 既存 |
| 4 | UID の欠番 | 既存 |
| 5 | 未定義の表参照 | 既存 |
| 6 | 行 ID の重複 | 既存 |
| 7 | **行 ID 参照の実在** | 第 1 周の宿題。`X-4` / `X-5` を検出した |
| 8 | **指し先の行の実在**（「表 X の 行 ID」がその表の行か） | 第 1 周の宿題。`表 T-009 の X-5` を検出した |
| 9 | **散文の数と表の行数の突き合わせ** | 第 1 周の宿題 |
| 10 | **表ブロック内の列数** | `F-17`（`MG-9a` の列ずれ）を止めるため |

⚠️ **検査 7 は `（S-10）` 型を捕まえられない** —— `S-10` は設定値表に実在するので、
「実在するが**別の表の**行を指している」誤りは、参照が `表 T-xxx の` の形で書かれているときしか
検査 8 で拾えない。**行 ID を書くときは表の名前を添える**のが、この型を機械で止める唯一の手段である。

⚠️ **機械検査はいまも意味の矛盾を見ない。** 第 1 周・第 2 周とも、Critical はすべて機械検査が緑のまま出た。
