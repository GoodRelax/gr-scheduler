# 裁定の結果（2026-08-22）

⭐ **利用者の裁定を受けた。全 16 問。** ⛔ **本書は決定の記録である。仕様はまだ 1 文字も動いていない。**
⚠️ **当てるのは変更要求（`CR-209` 以降）である。** 手順は規則 02。

---

## 01 —— 文書まるごとを現在値にする道（`replaceDocument`）

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q0** | `UndoEdit` は呼び手か呼ばれる側か | ⭐ **呼ばれる側。** シェルは `ApplyDocumentChange` に頼み、そこが `UndoEdit` に訊く。⛔ **部品グラフ（図 F-014）が正であり、`CR-196` の記述は誤りだった** —— **過去の CR は書き換えず**、新しい CR で仕様側を確定させる |
| **Q1** | 差し替えた文書の刻印 | ⭐ **入ってきた刻印をそのまま持つ**（6 呼び手すべて）。`WS-5` に但し書きを 1 文足す |
| **Q2** | 履歴を誰が守るか | ⭐ **Q0 により自動的に決まった** —— 経路が呼び手ごとに適用する（案 (f)）。呼び手は「自分が 6 つのどれか」を言うだけ |
| **Q3** | `WS-1` は何と照合するか | ⭐ **呼び手が申告した「読んだ刻印」と、いまの文書。** 入ってくる文書の刻印とは照合しない。申告しない呼び手は拒否しない |
| **Q4** | 入ってくる文書を検証し直すか | ⭐ **し直さない。** 外から来た文書の検証は `OP-5` / `FR-023` が既に負う |
| **Q5** | `WS-4` は段を積むか | ⭐ **積まない。** 表 T-027 は「人が文書に対して行う操作」を分類しており、履歴を歩くこと自体は対象外 |
| **Q6** | 「日程が動いたか」を誰が作るか | ⭐ **出て行く文書と入ってくる文書の `scheduleUpdatedUtc` の等値で導く。** `AG-6` の括弧に 1 文足す |

---

## 02 —— 非同期の答えがフレームへどう戻るか

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 待っているあいだの一貫性の単位 | ⭐ **表 T-066 に 4 つ目を足す** ——「人の応答を待つ 1 回のファイル操作」。⚠️ **`CA-3` を同じ回で直す** |
| **Q2** | 待っているあいだ画面は何を見せるか | ⭐ **何も変えない。** 最後のフレームがそのまま残る。⛔ 表を 1 行も触らない |
| — | 着地の契機 | ⭐ **`FT-2` がそのまま覆う**（着地は `WS-6` である）。⛔ **表 T-078 には行を足さない** |

---

## 03 —— 開く道で誰も持っていない 3 つ

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 形式の判別 | ⭐ **拡張子と先頭 1 文字の両方が合致したら読む。どちらかが異なれば拒否する。** ⛔ **`FR-023` の順序を守る** —— BOM は先に捨ててから 1 文字目を見る（BOM を理由に拒んではならない、MUST NOT）|
| **Q1a** | `GRS` 文書の拡張子 | ⭐ **`.json`** |
| **Q1b** | MSPDI の拡張子 | ⭐ **`.xml` のみ** |
| **Q2** | `Ctrl+S` は MSPDI で開いた文書に何を書くか | ⭐ **常に `GRS JSON`。** MSPDI は書き出し（`FR-096`）でだけ出す |
| **Q3** | 確認の答えの返り道 | ⭐ **表 T-109 に行を足し、既存の入口の道で返す** |

### ⚠️ Q1 の帰結（**明示しておく**）

⭐ **`.json` を選んだので、「両方合致」の厳しさは MSPDI 側（`.xml` ＋ `<`）で効き、`GRS` 側では無関係な JSON も判別を通ります。**
⛔ **ただしその先で止まります** —— `documentFromJson` と `validateImportedDocument`（表 T-220）が `GRS` 文書でないものを拒みます。
⭐ **判別は「どちらのデコーダへ送るか」を決める役目に徹し、受理の可否は検証が持つ**形になります。

### ⚠️ Q2 の帰結

⛔ **MSPDI で開いた文書の初回の `Ctrl+S` は、保存先を問うことになります**（`.xml` へは書かないので上書き先が無い）。
⭐ **2 回目以降は、その `GRS` ファイルが「開いたファイル」になります。**

---

## 04 —— 単位を名前に持たない上限

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 改名するか | ⭐ **比べる両側を改名する**（`maxTotalSizeBytes` / `sizeBytes` / `stepSizeBytes`）**＋ シェルの換算を押さえる試験を足す。** ⛔ 設定値 34 個の掃除は別の回 |

⚠️ **`tsc` は `tests/` を見ないので、改名後は必ず `npm test` を走らせて確かめる。**

---

## 05 —— 「保存形」とは何か

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | 詰めた形か、字下げのある保存形か | ⭐ **仕様を実装に合わせる** —— `FR-031` の 1 文を「詰めた `GRS JSON` として符号化した長さ」と明示する。⛔ **`S-95` の 🔎 は外さない** |

⭐ **根拠**: `CR-182` のコミットがこの文と `stepSize` の 1 行を**同時に**書き、直したのは「文字→バイト」で、詰めた形は手つかずだった。

---

## 06 —— 表示語 172 語

| # | 問い | ⭐ 裁定 |
|---|---|---|
| **Q1** | まず何をするか | ⭐ **先に配る道を通す**（裁定不要。表 T-075 の `UF-60` が既に命じている）。⭐ **併せて `CR-194` の受け入れ試験を書く** ——「原稿の語を 1 つ変えると落ちる試験」。⛔ **語を問うのはその後** |

---

## ⛔ 当てる順（**規則 02 の手順で**）

```
1. CR-209  01 の 7 問 —— replaceDocument
   ⚠️ 表 T-067 は閉路（AG-11 ↔ AG-2 ↔ AG-6 ↔ FR-063 ↔ WS-5）を持つので 1 つの計画で一括に書く
   ⚠️ 部品グラフの辺は動かない（Q0 がグラフを正としたため）
2. CR-210  02 の 2 問 —— 表 T-066 に 4 つ目の単位。CA-3 を同じ回で直す
3. CR-211  03 の 5 問 —— 表 T-024a に判別の行、拡張子、SK-11 の保存形式、表 T-109 に確認の行
4. CR-212  05 —— FR-031 の 1 文
5. 04 と 06 は仕様を動かさない（コードと試験だけ）
```

⭐ **1 と 2 と 3 は同じ場所を触らないので、順に回せる。**
⛔ **どの回も、規則 02 の 8 段（調査 → 測る → 計画 → 変更要求 → 編集 → 検査 → 変更履歴 → 立ちどまる）を踏むこと。**
