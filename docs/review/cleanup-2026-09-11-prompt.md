---
type: Handover Prompt
title: 掃除の巡 — 仕様書とコードのコメントを ECRS で削る
description: 2026-09-11 に始める掃除の巡。裁定は 4 件とも下りている。ECRS（排除→結合→再配置→簡素化）の順に進め、最後に「1 行直すのに何行書いているか」を測り直して合否とする。
tags: [cleanup, ecrs, machine-check, handover]
phase: cleanup
status: ready
---

# 掃除の巡 — 引継プロンプト

⭐ **このファイルの中身を、そのまま次のセッションの最初の指示として貼ってよい。**

---

## 0. あなたは誰で、この巡は何か

あなたは `gr-scheduler` の前に立つ者である。**この巡は掃除である。⛔ 機能を 1 つも足さない。**

⚠️ **なぜ掃除をするのか** —— 利用者の逐語（2026-09-10）:

> ClaudeCode に Dynamic HTML を指示すると、かなり高精度にこちらの思うものを作る。 Python もそうだ。しかし、GRS は .md と .ts が主なのに、不具合修正や、俺の指摘対応に膨大な時間がかかる。これが非常に不満だし、改善したい。

> GRS の開発プロセスを改善したい。というか、そのために残件をゼロ（Deferred を除く）とさせた。

⇒ **この巡の合否は「行を何行消したか」ではない。「1 件直す費用が下がったか」である。**測り方は第 8 節に書いた。

---

## 1. まず読め（この順で）

1. `docs/review/why-a-fix-costs-what-it-does-2026-09-10.md` —— **なぜ遅いかの実測**
2. `docs/review/third-party-review-2026-09-10.md` —— **前提知識ゼロの体が書いた判定**
3. `docs/review/what-belongs-where.md` —— **何を仕様書へ、何をコードへ、何も書かないか**
4. `docs/development-rules/` の 03（実装）・04（確かめ方）・05（進め方）
5. `docs/review/audit-cadence-and-architecture-2026-08-23.md` —— 18 日前の同じ監査

⛔ **1〜3 を読まずに手を動かすな。**この巡の判断の根拠はすべてそこに在る。

---

## 2. ⛔ このプロンプトを信じるな

**このプロンプトの数は、書いた時点のものである。⭐ 測り直して反証せよ。**方法を添えてある。

| 主張 | 測り直す方法 |
|---|---|
| `src/`＋`tests/` のコメントは 108,648 行 | `//` `*` `/*` で始まる行を数える |
| うち印も ID も引用も持たない散文が 76,215 行（70.1%） | 同じ走査で、`@purity` / `「」` / `` `X-9` `` / `D-\d+` のどれにも当たらない行を数える |
| `A.3 Changelog` は 183,208 字 = `docs/spec` の .md の 23.8% | `docs/spec/A-appendix.md` の `## A.3` から末尾まで |
| 廃した ID がコメントに 265 箇所（うち素の文字列 129） | 廃番の一覧は `.claude/skills/spec-graph-check/md-checks.py` の `RETIRED` |
| 検査 39 の保持 718 のうち 367 がコメントだけ | `tests/` からコメントを除いた本文で `check-must-clause-coverage.py` の判定を再現する |
| 1 行直すのに 34 行書いている | 直近の修正コミットで、`src/` の実行行の増分と全追加行を比べる |

⚠️ **前の巡で、申し送りの「試験 195 ファイル / 7411 件」が実際には 197 / 7447 だった。**⛔ 申し送りは腐る。**測れ。**

---

## 3. 既に下りている裁定（2026-09-10、利用者）

⭐ **この 4 件は決まっている。問い直すな。**

| # | 裁定 | 中身 |
|---|---|---|
| **E1** | **移す** | `A.3 Changelog` を `docs/spec/A-appendix.md` から `docs/development-records/` へ移す。⛔ **削除ではない** —— 版番号（1.79 等）は仕様書独自の採番であり、Git に無い。⚠️ 検査 38（`check-changelog-versions.py`）の読む先を 1 行変えること |
| **E2** | **検査を作る** | `@purity` は消さない。**関数ごとに `@purity` が 1 つ在るかを検査する機械を 1 本作る。**⛔ 消してはならない理由: 規則 07 の 80 行目が「判定は R7 の `@purity` タグをそのまま使う」と定め、**R7.2 の命名（名詞か動詞か）の入力になっている**。⚠️ 規則 07 の 392 行目が自ら「grep で検査できる」と書いており、**約束を果たすだけ**である |
| **E4** | **判定を反転** | 検査 39 の判定から**コメントを除く**。基準線は `983` から **意図的に上げる**（コメントを除くと保持が 718 → 351 前後に落ちる）。⛔ **検査を廃すな** —— `CR-350` の事故（MUST を 7 か所動かして赤が 1 件も出なかった）が再発する |
| **C3** | **触らない** | 台帳 5 本は**この巡では触らない**。`fixed-defects.md`（725,550 字）は刈った先であり、人が通読するものではない。⇒ **1 件直す費用に効かない。次の巡へ回す** |

---

## 4. 作業は ECRS の順に行う

⛔ **順を入れ替えるな。**E が最も効き、S は最後である。⚠️ **とくに E4 → C1 の順は逆にできない**（理由は該当箇所に書いた）。

### 波 E — 排除

| | 手 | 目安 | 合否 |
|---|---|---|---|
| **E1** | `A.3 Changelog` を `docs/development-records/changelog.md` へ移す。`A-appendix.md` には移設先を指す 1 行だけ残す | `docs/spec` の .md が **23.8% 軽くなる** | 検査 38 が緑。`A-appendix.md` を引いている条項がすべて解決する |
| **E2** | `@purity` を検査する機械を 1 本作る（`.claude/skills/spec-graph-check/` へ）。⭐ 4 値のうち `semi-pure-a` は実測 **1 個**しかない —— 検査ができたら報告せよ | 新設 1 本 | 検査が緑。付け忘れが在れば数を報告 |
| **E3** | 空振り試験と着かない ID を消す。⛔ **`tests/system/nfr-004-file-scheme-sweep.sws.test.ts` の `[data-icon="IC-69"]` は `if (yes !== null)` に守られて永久に空振りしている。**着かない仕様 ID は 9 個 23 参照（`RS-45` ×12 ほか） | 小さい | ⭐ **嘘が消える。**赤を先に出して確かめよ |
| **E4** | 検査 39 の判定からコメントを除き、基準線を意図的に上げる。**理由をコミットに書く** | `tests/` の変更行 52.3% に効く | 検査 39 が緑（新しい基準線で） |

### 波 C — 結合

| | 手 | 目安 | 合否 |
|---|---|---|---|
| **C1** | `src/` の逐語引用（6,856 行）を、行 ID の指し先 1 本へ畳む。⛔ **E4 の後でなければ着手するな** —— 先にやると検査 39 の数が二重に動いて、どちらが原因か分からなくなる。⚠️ 規則 03 §3 が既にこれを命じている | 6,856 行 | 検査 42 の数が**下がる**。⛔ 上がったら引用を壊している |
| **C2** | `FR-093` の数え式（`charCodeAt(0) < 0x100 ? 1 : 2`）が **4 か所**に在る（`row-title-panel.ts:188` / `schedule-geometry.ts:1813` / `schedule-layout.ts:516` / `schedule-layout.ts:693`）。⚠️ **`row-title-panel.ts:51` のコメントは「二度ある」と書いている。**⛔ 層規則（`LR-3`）が結合を許さない対はそのまま残し、**同じファイル内の 2 本目は必ず畳め** | 本物の欠陥 | 赤を先に出して確かめよ。`layers` が緑のまま |
| **C3** | 台帳 5 本の統合 —— ⛔ **この巡では行わない**（裁定済） | — | — |

### 波 R — 再配置

| | 手 | 目安 | 合否 |
|---|---|---|---|
| **R1** ⭐⭐ | **`src/` と `tests/` を見る機械を新設する。**いま `src/` を見る検査は **0 本**であり、これが**発生源対策**である。3 段で作れ（下記） | 新設 1〜3 本 | 段 1・2 は門にする。段 3 は**まず候補出し**、精度を測ってから門へ |
| **R2** | 関門の順序を変える。⚠️ 関門一式 9.4 分のうち **88% が Playwright**（8 分 17 秒）。速い門（`tsc` / `vitest` / `check.sh`）を先に、e2e を最後に | 待ち時間 | 赤が出るまでの時間が縮む |
| **R3** | 逐語保持の場所を、コメントから `it()` の表題・期待値へ移す | E4 の従属 | 検査 39 の保持数が回復する |

#### R1 の 3 段（⛔ すべて古典的判定で作れ。外部モデルを呼ぶな）

| 段 | 判定 | 道具 | 門にするか |
|---|---|---|---|
| **1** | **廃した ID・着かない ID** —— `docs/spec` が定義する ID の集合と、`src/`＋`tests/` が参照する ID の集合の差 | `re` ＋ `set` | ⭐ **門にする** |
| **2** | **同じ式が N 箇所** —— 正規化した式の指紋で数える（`dup-check.py` の手法をコードに当てる） | `re` か tree-sitter | ⭐ **門にする** |
| **3** | **現在形で断定している文だけを抽出し、その中の ID を検査する** —— 「〜である」「〜する」は主張、「〜だった」「〜まで…と定めていた」「until 20XX」は記録 | `re` | ⚠️ **最初は候補出し** |

⛔ **LLM を呼ぶ 4 段目は作るな。**非決定的な門は、この製品がいちばん困っている「巡が赤で終わる」を増やす。⭐ どうしても要るなら、門ではなく**掃除のときだけ手で走らせる候補出しの道具**にせよ。

⭐ **R1 の段 1 は、いまこの瞬間に 265 箇所を指せる**（`` ` ` `` で囲まれた 136 ＋ 素の文字列 129）。⛔ **素の 129 箇所は、いまどの検査からも見えていない。**

### 波 S — 簡素化

| | 手 | 目安 | 裁定 |
|---|---|---|---|
| **S1** | コメントから台帳の番号（2,020 行）・撤回の記録（191 行）・日付つき実測（29 行）を台帳へ戻す | **2,240 行** | 不要 |
| **S2** | 論駁の圧縮 | 未測 | ⚠️ **上限の裁定が要る。着手前に問え** |

---

## 5. ⛔ 触ってはならないもの

- **`D-311`**（第 8・9・10 章が空の器）—— 2026-09-06 に「試験の章は一旦そのまま」と裁定されている。**1 文字も触るな**
- **`docs/development-rules/`** —— 規則の書き換えは裁定案件。⛔ 第 3 節の 4 件以外は、**問うてから**
- **基準線の無断切り下げ** —— 上げるのも下げるのも、**理由をコミットに書く**こと。⛔ 黙って動かすな
- **`docs/spec/` の MUST を、裁定なしに消すこと** —— ⚠️ 前の巡で、不具合と思われたものが**仕様どおりの挙動**だった実例が在る（表 T-021a の `PV-1`）。**消す前に問え**

---

## 6. 検査の走らせ方（⛔ トークンを溶かすな）

検査は**別プロセス**で走り、**あなたのコンテキストに入るのは標準出力だけ**である。⭐ だから既定はこう打つ:

```bash
bash .claude/skills/spec-graph-check/check.sh > /dev/null 2>&1; echo "exit=$?"
```

⚠️ **実測（2026-09-10）**: `check.sh` は **37 秒**、読む量 **354 ファイル / 18.0 MB**、出力 **240 行 / 21 KB**。⇒ **出力を丸ごと受け取ると 1 回で数千トークン溶ける。**落ちたときだけ該当節を `grep` で取り出せ。

関門一式:

| 門 | 所要 | 打ち方 |
|---|---|---|
| `npx tsc --noEmit` | 数秒 | 誤りの数だけ数える |
| `npx vitest run` | 21.7 秒 | 末尾 3 行だけ見る |
| `bash .claude/skills/spec-graph-check/check.sh` | 37 秒 | 上のとおり |
| `npm run layers` | 数秒 | 末尾 2 行 |
| `npm run parity` | 数十秒 | 末尾 3 行 |
| `npx playwright test` | **8 分 17 秒** | ⚠️ **最後に 1 度だけ。**2 件の赤は `PD-443`（フレーム時間）で、**この巡より前から赤である** |

---

## 7. 進め方の作法

- ⭐ **赤を先に出せ。**直す前に「いまの造りが本当に間違っている」ことを測る（規則 04）
- ⭐ **波ごとにコミットせよ。**⛔ 4 波を 1 コミットに混ぜるな —— どの手が何を壊したか分からなくなる
- ⭐ **機械的な作業は体に投げよ**（利用者の裁定 2026-09-08）。⛔ ただし**体は委ねて止まる** —— 報告の文ではなく**木の状態を数えて**確かめること
- ⭐ **体には必ず書け**: 「原典を読め。⛔ 申し送りを信じるな。⛔ このブリーフも間違う」
- ⛔ **コミットの前に毎回、個人情報の点検を行い、実測した数を報告してから**コミットすること（利用者名・個人の絶対パス・メールアドレス・秘密が各 0 件、`.gitignore` が私的な資料を覆っていること）
- ⛔ **コミットメッセージに attribution を書くな**
- ⛔ **一時ファイルはセッションの scratchpad へ。**⛔ **このリポジトリの外のファイルを書き換えるな**（読むのはよい）
- ⛔ **`git tag` は利用者が行う。**あなたは打てない

---

## 8. この巡の合否 —— 最後に測り直す

⛔ **消した行数は合否ではない。**次を測り、掃除の前後で比べよ。

| 測るもの | 掃除前（2026-09-10 の実測） | 方法 |
|---|---|---|
| **1 行直すのに何行書いているか** | **33.9 行**（直近 5 本の平均。今日の巡は 35.3 行） | 修正コミットの全追加行 ÷ `src/` の実行行の増分 |
| `src/` のコメント ÷ 実行行 | **1.95** | 行を数える |
| `docs/spec` の .md の字数 | **768,332 字** | 同上 |
| 廃した ID の生きた参照 | **265 箇所** | R1 の段 1 が数える |
| 関門一式の所要 | **9.4 分** | 時間を測る |

⭐ **「1 行直すのに何行書いているか」が下がっていれば成功である。**⛔ 下がっていなければ、行を消しただけで律速は動いていない。

---

## 9. ⭐⭐ 終わったら —— 利用者にタグ付けを依頼せよ

⛔ **あなたは `git tag` を打てない**（`gr-scheduler` の git 例外に `tag` は含まれない）。

⇒ **掃除が終わり、すべての門が緑になり、コミット＆Push を済ませたら、利用者に次のコマンドを渡して、タグ付けを依頼すること。**

```bash
git tag -a After-Cleanup -m "After the spec and comment cleanup of 2026-09-11" && git push origin After-Cleanup
```

⭐ 巡の始まりには `Before-Cleanup` が打たれている。⇒ **その 2 本が揃えば、掃除の成果は 1 行で出る:**

```bash
git diff Before-Cleanup..After-Cleanup --stat
```

⚠️ **依頼の際は、第 8 節の「掃除の前後の数」を添えること。**⛔ 「終わりました」だけでは、成果が測れたことにならない。

---

## 10. 報告の形（波ごとに）

1. **実測値** —— 何行消し、何行増えたか。第 8 節の数がどう動いたか
2. **門の終了コード** —— `tsc` / `vitest` / `check.sh` / `layers` / `parity`（`playwright` は最後の 1 回）
3. **意図して動かした基準線** —— どれを、いくつからいくつへ、なぜ
4. ⭐ **このプロンプトのどこが間違っていたか** —— ⛔ 見つからなかったなら「測っていない」と書け
