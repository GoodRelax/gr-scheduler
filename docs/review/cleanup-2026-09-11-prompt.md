---
type: Handover Prompt
title: 掃除の巡 — 3 つのフェーズで仕様書とコードの無駄を狩る
description: 2026-09-11 に始める掃除。フェーズ 1 は道具を作るだけ、フェーズ 2 が本命の無駄狩り、フェーズ 3 が後始末。第 6 節の 5 つの箱が判定であり、順に問うこと。合否は消した行数ではなく「1 件直す費用が下がったか」である。
tags: [cleanup, ecrs, machine-check, handover]
phase: cleanup
status: ready
---

# 掃除の巡 — 引継プロンプト

このファイルの中身を、そのまま次のセッションの最初の指示として貼ってよい。

> **語の断り —— 「押す」**
> 本書で「押す」とは、出荷ビルド（`dist/index.html`）を実際に操作して何が起きるかを見ることである。⛔ Git の `push` とは無関係。この製品の既存の語で、規則 04 が「押しに行く」節を持ち、`fixed-defects.md` は 948 回使っている。
> 押す道は 2 つ —— 人が開いて押すか、`scratch/` に Playwright の探り針を書いて押させるか（直近の実例は `scratch/d2026-09-10-eight.mjs`）。⛔ ブラウザ枠では測れない（枠は再描画を起こさない）。

> **語の断り —— 「フェーズ」と「 wave 」**（利用者の裁定 2026-09-11）
> ⛔ **「週」という語は使わない** —— 暦の週と紛れる上に、下の wave と数字が衝突した。
> **フェーズ** = この巡の大きな段。3 つある（道具 ／ 本命 ／ 後始末）。手は `P1-1` `P2-3` の形で呼ぶ。
> **wave** = **1 つの手のなかで、体を並列に配る 1 回**。1 wave は体 3～4 体、互いに交わらないファイル集合を持つ。
> ⚠️ **1 つの手が何 wave かかることがある** —— `P1-3` は 96 ファイルに及び、wave 1 で触れたのは 11 本だけであった。
> ⛔ **wave が 1 つ終わったことを、手が終わったことと報告するな。**

---

## 0. 現在地 — ⛔ ここから読め（2026-09-11 更新）

**フェーズ 1 完了（タグ `After-Cleanup-P1`）。フェーズ 2 は P2-1 完了、P2-2 は Chapter 1 と 4.1 の全 99 要求が完了。**

| 手 | 状態 |
|---|---|
| **P1-1〜P1-4** | ✅ 完了。検査 40 → **42** 本（うち 2 本が `src/` を読む。この製品で初めて） |
| **P2-1** 変更履歴の移設 | ✅ 完了。原稿 700,026 → 516,935 字 |
| **P2-2** 原稿の仕分け | ⚠️ **Chapter 1 ＋ 4.1 の 99 要求が完了**（裁定 A〜F 適用済）。原稿 **509,606 字** |
| **P2-3** 結合度・行長 | ⛔ 未着手 |
| **`src/` のコメント** | ⛔ **未着手。48,668 行・68 ファイル** |
| **`tests/` のコメント** | ⛔ 未着手（`src/` の後） |

### ⭐ 4.1 の 6 束の実測（2026-09-11。⛔ これが本節の主たる教訓である）

| 束 | 件数 | 消した箇所 | 字数 | 減 |
|---|---:|---:|---|---:|
| 1 | 12 | 9 | 48,260 → 47,073 | −2.46% |
| 2 | 18 | 8 | 44,468 → 43,871 | −1.34% |
| 3 | 11 | 12 | 44,891 → 44,290 | −1.34% |
| 4 | 20 | 7 | 46,644 → 46,072 | −1.23% |
| 5 | 23 | 12 | 45,029 → 44,199 | −1.84% |
| 6 | 15 | 8 | 34,730 → 34,279 | −1.30% |
| **計** | **99** | **56** | **264,022 → 259,784** | **−1.61%** |

⛔⛔ **「4.1 は仕様書の 52% だから本命である」という読みは、実測で外れた。**⇒ **6 束・99 要求を通して 1.61% しか減らない。**⭐ **`FR-029`（5,861 字）に至っては 1 字も出なかった** —— 中身が裁定 E の守る撤回記録と、その席が原本である MUST ばかりだったからである。

### ⭐⭐ 代わりに出たもの —— 仕様書の嘘 **23 件**（`D-445` 〜 `D-467`）

⛔ **これがこの巡の主たる成果である。消した字数ではない。**⭐ **4 つの形に分かれる:**

| 形 | 件数 | 中身 | 機械で捕まえられるか |
|---|---:|---|---|
| **(a)** | 7 | ある席が別の席を「…と定めている」と引き、**引かれた側は別のことを定めている** | ⭐ **P3-6**（検査 42 を内側へ向ける。母数 31） |
| **(b)** | 4 | **撤回済みの規則を、いま在るものとして引いている**（うち 1 件は MUST で振る舞いに効く） | ⚠️ 未案 |
| **(c)** | 2 | **散文が抱えた員数が偽**（1 件は腐った、1 件は生まれつき） | ⛔ **母数を数えたら小さかった。検査は要らない**（`D-456`） |
| **(d)** | 5 | **完全性の主張が偽**（「本表が全数である（MUST）」と書いて漏れている） | ⭐ **P3-8**（2 つの表の集合差。ただし `D-462` は捕まらない） |
| ほか | 5 | 刷り崩れ・指し先切れ・隠れた重複・検査 11 の非単調性 | `D-448` `D-455` `D-466` `D-467` |

⛔ **どれも既存の 42 本の検査は 1 件も捕まえない。**⇒ **仕様書の中の引用・員数・全数を検める機械が 1 本も無い。**

### ⭐ 次の巡が最初にやること — 4.1 を束に割る

`01-04-requirements.md` の **4.1 機能要求が 267,429 字（原稿の 52%）**であり、ここが本命である。

- ⭐ **`####` 見出しが 99 本**（要求 1 件 ＝ 1 見出し）。例「形状を選んでタスクを作る」「ズームしても形が歪まない」
- 1 件の大きさは中央値 **1,807 字**、最大 **26,406 字**
- ⇒ **字数で均等な 5〜6 束**に割る（各 45,000 字前後）

⛔⛔ **体は 1 つずつ。並行させるな（利用者の裁定 2026-09-11、裁定 D）。**
理由: 1 つのファイルを 2 体が同時に編集すると、行範囲が交わらなくても片方の編集が消える（読む→書き換える→書き戻すの間に相手が入る）。⚠️ **消えても赤にならない**のが最悪である。
⇒ **1 セッション 1〜2 束。4.1 だけで 3〜5 セッションかかると見積もれ。**

### 原稿の実測構成

| ファイル | 字数 | 全体比 |
|---|---:|---:|
| `01-04-requirements.md` | 約 317,000 | **62%** |
| `05-07-design.md` | 79,450 | 15% |
| `_assets/tbl-settings.md` | 46,545 | 9% |
| `_assets/tbl-glossary.md` | 35,773 | 7% |
| `_assets/fig-erd-detail.md` | 26,465 | 5% |
| 残り 4 本 | 8,178 | 2% |

### ⛔ フェーズ 2 で学んだこと — 次の巡が繰り返さないために

1. ⛔⛔ **物を動かす手は、動かした「先」でも壊す。** P2-1 で最初に赤くなった検査 24 は、移設元のファイルを一言も書いていない —— `docs/development-records/` に新しい `.md` が増えたことを見ていた。⇒ **移設元を grep して作った一覧では、危険の半分が原理的に見つからない。**
2. ⛔ **移設先に既に同じ規則が在ると思え。** 裁定 A で移した 22 の MUST のうち **6 件は既に移設先に在り、4 件はバイト単位で同一**だった。素直に追記していたら重複を 7 つ作っていた。
3. ⛔ **文書は互いを指している。** `spec-writing-rules.md` が「その旨を 1.9 に書く（MUST）」と定めているため、3 行は仕様書側に残さねばならなかった。**片方向しか読まない移設はもう片方を壊す。**
4. ⚠️ **指し先を足すときは、既存の指し先を先に探せ。**「規則に従え」を書いたら、原稿 8 行目に既に同じ指し先が在り、**検査 11（重複検出）が赤くなった。**
5. ⛔ **道具のコメントも腐る。** 検査 39 は Chapter 1 を読まない理由を「検査 5〜15 が既に読んでいる」と書いていたが、**読んでいなかった**（訂正済）。

### ⭐ 統計は自動で取れるようになった

`npm run stats`。セッション開始時にフックが 1 行追記する（`docs/development-records/measurements/stats.jsonl`）。
同じフックが `docs/spec/output/` を掃ける（`npm run sweep`）。⛔ **サーバが動いているときは何も消さない。**

⚠️ **費用の指標はまだ定めていない**（利用者の裁定 2026-09-11）。工程自体を試行中なので、**まず数を貯めて後から読む**。台帳はまとめて刈られるので「不具合 1 件あたりの行数」は分母にならない（40 コミットの窓で 32 件 → 240 件 → 0 件）。

---

## 14. 裁定の記録 — ⛔ 問い直すな（2026-09-11）

| # | 裁定 | 結果 |
|---|---|---|
| **A** | §1.9 の書き方の規則を `spec-writing-rules.md` へ移す | ✅ 完了。⭐ **仕様書には「規則に従え」の指し先を残した**（前文を指す形） |
| **B** | MUST 26 行の仕分け | ✅ 完了。**移す 15 行＋2 文 ／ 残す 9 行＋1 文 ／ 消す 1 文** |
| **C** | `GRS JSON` の指し先違い | ✅ 完了（表 T-005 を指す） |
| **D** | 1 ファイルを体にどう配るか | ✅ **直列。体は 1 つずつ**（第 0 節参照） |
| **E** | 仕様書の**撤回記録**（「…まで『…』と定めていた」型）を刈るか | ✅ **残す。1 文字も触るな**（利用者の裁定 2026-09-11、逐語「残すでよい」） |
| **F** | **原本が他所に在る MUST の「写し」**を指し先 1 語に畳んでよいか | ✅ **畳んでよい**（利用者の裁定 2026-09-11、逐語「写しなら畳んでよい」） |

### ⭐ 裁定 F —— 第 9-1 節と第 6-1 節の食い違いの決着

⚠️ **2 か所が正面からぶつかっていた** —— 第 9-1 節は「⛔ MUST を裁定なしに消すな」と言い、第 6-1 節の実例表は「`` `FR-011` は『両端を動かさないこと（MUST）』と定めている ``」を問い 0 ① で消して「**指し先 1 語だけ残せ**」と書いていた。

**分かれ目は 原本か 写しか である。⛔ MUST を含むかどうかではない。**

| | 扱い |
|---|---|
| **その席が原本である MUST** | ⛔ **消すな。**裁定なしに触るな（第 9-1 節はこちらを守っている） |
| **原本が他所に在る MUST の写し** | ⭐ **指し先 1 語へ畳んでよい。**⛔ **これは削除ではない** —— **規則は原本の席に残る** |

⛔ **体には必ずこう課せ**: 「**畳むなら、原本の行 ID か要求 UID を名指せ。言えないなら畳むな。**」
⚠️ **原本の側が（MUST）の形をしていないことがある** —— そのときは**畳むと規範性が落ちる**ので、⛔ **畳まずに報告せよ**（束 2 の `FR-009` 1844 行がこの形。表 T-018a の `RT-2` に（MUST）が無い）。

⭐ **束 1〜3 で「写しの MUST」として報告済みのもの**は、束 6 まで終えたあとに**まとめて 1 巡で畳む**。⇒ 束 2 が 2 件（`FR-009`）、束 3 が 4 件（`FR-016` の `FR-031` 写し 3 件ほか）。

### ⛔⛔ 裁定 E の根拠 — 問い直すな。数は測ってある

**撤回記録は箱 A ではない。**問い 0 ① は「原本が他所にあるか」を問うが、**半分で答えが「無い」である。**

| 実測 2026-09-11（仕様書 9 ファイル、117 文） | 件数 |
|---|---:|
| 原本が `docs/development-records/` に在る | 61 |
| **台帳に無い —— 仕様書が唯一の記録である** | **56（48%）** |

⛔ **この照合はわざと甘い**（引用部の 12 字窓が台帳 17 ファイルのどれかに 1 つでも当たれば「在る」と数える）。⇒ **56 は下限であって上限ではない。**

さらに 3 つ:

1. ⭐ **量が危険に見合わない。**撤回記録 12,039 字は仕様書の **2.38%**。日付つき実測 6,873 字・逐語引用 6,927 字を足しても、**消せる散文は全部で 5.1%** である。⇒ **束 1 は撤回記録に触れずに 2.46% 減らした。足しても桁は変わらない。**
2. ⛔ **仕様書自身が 2 か所で守り、片方は理由を書いている。**`FR-004` の `HF-2`「却下の履歴を消してはならない（MUST NOT）—— 同じ判断を 3 度目にやり直さないためである」と、`HF-11`「日付つきの引用は規則ではなく記録であり、この禁止に当たらない」（`FR-085` の日付つきの注を**先例として名指している**）。
3. ⚠️ **検査 42 が赤くなる。**束 1 で**実測した** —— 仕様書の 2 文を消しただけで 469 → **473**（基準線 471）。117 文のうち **70 件**を `src/` / `tests/` のコメントが逐語で引いている（上限）。

### ⭐ 裁定 E の但し書き（利用者の裁定 2026-09-11、`D-465`）

**撤回記録の中でも、機械が数え直せる員数は持たない。⛔ 範囲と日付と結論は残せ。**

⚠️ **実測がこの但し書きを生んだ。**`FR-029` の撤回記録の 1 文が員数を 2 つ抱えており、**2 つとも壊れていた** ——

| 散文が言う数 | 実測 2026-09-11 | |
|---|---|---|
| 表 T-233 の `RS-1` 〜 `RS-46` の **44 行** | **43 行**（欠番 `RS-17` `RS-18` `RS-45`）| ⛔ **書いた当日から偽** |
| 「薄く描」と書かれた **13 か所** | **16 か所** | ⚠️ **6 日で腐った** |

⭐ **根拠**: **撤回記録の仕事は「外した判断を問い直す人が再現できること」である。**⇒ 要るのは**走査の範囲と日付**であって、**員数ではない**（員数は副産物）。
⛔ **しかも偽の員数は、再現しようとした人を積極的に迷わせる。**⭐ **「薄く描」は数を落とすほうが結果が良い** —— **いま再現する人は 16 か所を掃くべきであり、13 と教えられるのは害である。**

⛔ **今日の値へ「直す」のは記録の改竄である。**2026-09-05 の走査が 16 か所を見たことにしてしまう。⇒ **落とすのであって、直すのではない。**

⇒ ⭐⭐ **フェーズ 2 の本命は 4.1 の散文ではなかった。**4.1 は仕様書の 52% だが、**そのうち消せるのは 5%** である。残る重さは **P2-3（結合度と行長。`FR-001` の STATEMENT は 1 行で 6,000 字を超える）**と、**`src/` のコメント 48,668 行（63.7%）**に在る。第 12 節の合否「1 行直すのに何行書いているか」を動かすのは後者である。

### ⛔⛔ 裁定 E と同時に判った、第 9 節の順の誤り

**仕様書の一文は、`tests/` のコメントがそれを引いている限り消せない。**

第 9 節は P2-2（仕様書）を `src/` / `tests/` のコメントより**先**に置いているが、依存は逆向きである。束 1 で実測: `FR-004` `HF-19` の実測と `FR-003` のダミー掴みシロの実測を消したところ、それを逐語で引く `tests/` の 4 つのコメントが宙に浮き、**検査 42 が 469 → 473 へ上がって赤くなった**（基準線 471）。⇒ **2 件はバイトごと戻した。**

⛔ **仕様書の一文を消す前に `check-quoted-source.py` の当たり集合の差分を取れ。**⭐ 引かれている文は、`tests/` のコメント掃除（裁定 3 待ち）が済むまで動かせない。

#### ⭐ ただし「grep に当たったら消すな」は広すぎる（束 2 の実測、2026-09-11）

⛔ **問いは「コメントが引いているか」ではない。「消した後も `docs/spec` のどこかにその文字列が残るか」である。**

検査 42 は**引用の出どころが原稿のどこかに在るか**を見る。⇒ **同じ文が仕様書の別の行にも在るなら、片方を消しても検査 42 は動かない。**

実測: 束 2 で grep に当たった 7 件のうち **3 件は原本が仕様書の別の行に在り、消しても 469 のままだった**（`ST-9` の写しの原本は表 T-014 本体、`FR-091` の写しの原本は `FR-091` 本体、`AS-9` の写しの原本は `AS-9` 本体）。⇒ ⚠️ **広すぎる規則のせいで、この 3 件は取りこぼしとして残った。**

#### ⛔⛔ そして grep で「判定」するな —— 検査 42 は 0.6 秒で直に走る（束 3 の実測、2026-09-11）

```bash
python .claude/skills/spec-graph-check/check-quoted-source.py 2>&1 | grep -E "^(OK|FAIL) " | head -1
```

⚠️ **実測 0.564 秒。**⛔ `check.sh` 一式（約 40 秒）ではない。⇒ **「消す → 測る → 動いたら戻す」が 1 候補あたり 1 秒でできる。**

⛔ **`| tail -1` を使うな**（束 5 の実測、2026-09-11）—— **赤のときは逐語の羅列が最終行に来るので、`FAIL` の行が押し出されて数が見えない。**⚠️ 出力は CRLF 混在で、端末に化けた行が出る。⭐ **上の `grep` なら、緑でも赤でも 1 行で数が読める。**

⛔ **grep による判定は、止めなくてよいものまで止める。**束 3 が grep の当たり 4 件を実際に消して測った結果:

| 候補 | grep 規則の判定 | 実測した検査 42 | 正解 |
|---|---|---|---|
| `FR-043` の歩数の実測 | ⛔ 消すな | **469（動かない）** | **消してよかった** |
| ほか 3 件 | ⛔ 消すな | 470 | 消すな ✔ |

⇒ **4 件中 1 件（25%）が誤った停止であった。**

> ### ⛔⛔ 訂正（束 4 の実測、2026-09-11）—— ここに書いてあった理由づけは嘘だった
>
> **本節はかつて「括弧無しの地の文として写されている文は、消しても検査 42 は動かない」と書いていた。⛔ 偽である。**
>
> **括弧を見る側が逆である。**`check-quoted-source.py` の `QUOTED` が `「」` / `『』` を探すのは**コメントの側**であって、**仕様書の側ではない**（`find_unsourced()` が `QUOTED.finditer(block)` を `sources()` すなわち `src/` と `tests/` の コメント塊に当て、原稿のほうは `somewhere_in(parts, hay)` で**ただの部分一致**として引く）。
>
> ⇒ **仕様書で括弧無しの地の文でも、コメントがそれを `「」` で囲って引いていれば検査 42 は動く。**束 4 が消して測って戻した 2 件は、どちらも仕様書側に括弧が 1 つも無く、それでも **469 → 471** と **469 → 470** に動いた。
>
> ⛔ **だからこの節の手順（消す → 測る → 動いたら戻す）は正しいが、その理由づけを読んで先回りしてはならない。**⭐ **直に測るしかない。**
>
> ⚠️ **この嘘を書いたのは指揮である**（体の報告を要約する際に、括弧の掛かる側を取り違えた）。⇒ ⭐ **第 5 節の「指揮のブリーフがいちばん危ない」の実例が 1 つ増えた。**

### ⛔⛔ 2026-09-11 の反省 —— 指揮が持ち込んだ誤りの実測

**体は 8 巡連続でブリーフの誤りを実測で潰した。⇒ 偶然ではない。**

| # | 指揮が書いたこと | 実測 | 体が止めなければ |
|---:|---|---|---|
| 1 | 「`vitest` は走らせるな（コードを触らない）」 | ⛔ **原稿を読む試験が在る** | **6 件の赤を 3 束・4 push ぶん見逃した**（実際に起きた） |
| 2 | 「括弧の無い文は消しても検査 42 は動かない」 | ⛔ **括弧が掛かるのはコメントの側** | 体が測らずに消す |
| 3 | `D-466`「`FR-031` の写しが 2 か所」 | ⛔ **`FR-031` に原本は無い。行も違う。3 か所** | **2026-09-07 の利用者の指示を破っていた** |
| 4 | `D-463`「落ちているのは 1 件」→「5 件」 | ⛔ **6 件**（言及を所属と数えた） | 1/6 だけ直して穴が残る |
| 5 | 「`GL-006` の子は 9 件」 | ⛔ **12 件**（非機能要求 3 件を数えていない） | **書いた日に反例 3 件を抱える MUST** |
| 6 | `D-452`「イナズマ線」 | ⛔ **日付の縦線**（別の要素） | 台帳が別物を指したまま残る |
| 7 | `D-459`「往復の対象が 1 つ増える」 | ⛔ **`documentStamp` は書き出さない** | 高く見積もって案②を退けた |
| 8 | `ET-16` は「表 T-057 の行」 | ⛔ **表 T-056** | 検査 8 が捕まえた |

⇒ ⭐⭐ **8 件中 6 件が「数え間違い」と「名前の取り違え」である。**
⛔ **素の文字列一致は所属ではない**（#4 #5 —— 別の行の本文中の言及を、表の行と数えた）。
⛔ **「この巡は X を触らない」は主張であって事実ではない**（#1）。

**⇒ 体に渡す 1 行は、毎回これでなければならない:**

> ⛔ **このブリーフも台帳も間違う。測って反証せよ。**⚠️ **指揮はこの巡だけで、要求名・行・員数を取り違えている。**

### ⚠️ 手つきの反省（規則ではなく作法）

- ⛔ **体が走っているあいだに同じ台帳を編集した** —— 裁定 D と同じ危険である。⭐ 今回は両方残ったが**運である。**
- ⛔ **体が走っているあいだに門を走らせ、偽の赤を追いかけた** —— **半分書かれたファイルを検査が読む。**⇒ **体が走っているあいだ、門を触るな。**
- ⛔ **自分で記録した罠（`D-468` の番号衝突）に、記録の 30 分後に落ちた。**⇒ **記憶に頼る規則は、書いた本人すら守らない。**
- ⛔ **コミット文に「秘密 0 件」と書いたが実測は 5 件だった**（すべて行 ID の接頭辞）。⇒ **数を書くなら、書く前に見ろ。**

**⛔ 体に渡す手順はこう書け:**

1. grep は**候補を挙げる**ためだけに使う（⛔ `docs/spec/output/` を除く。裁定 11）
2. **消す**
3. **`check-quoted-source.py` を直に 1 回走らせる**
4. **数が動いた** ⇒ ⛔ **バイトごと戻して報告せよ**（`tests/` の巡を待つ）
5. **動かない** ⇒ そのままでよい

#### ⛔⛔⛔ そして `vitest` を走らせろ —— 原稿を読む試験が在る（束 1〜6 で踏み抜いた、2026-09-11）

**⛔ 指揮は 6 束すべてのブリーフに「`vitest` / `playwright` は走らせるな（この束はコードを触らない）」と書いた。偽である。**

⚠️ **この製品には、原稿を実行時に読み、「その文がいまも一字一句在るか」を見る仕様駆動の試験が在る。**⇒ **仕様書を 1 文直すだけで落ちる。**

**実測**: 束 3 が 表 T-023a の行から「パンは等倍とすること（MUST）」を指し先へ畳んだ時点で `tests/unit/t-023a-pd-1-pan-follows-the-pointer.test.ts` が赤くなり、**束 4・5・6 と 4 回 push するまで誰も気づかなかった。**最終的に **6 件・5 ファイル**が赤。⛔ **`check.sh` は 42 本とも緑のままである。**

⇒ ⛔⛔ **仕様書を触る巡は、手ごとに `npx vitest run` を通せ。**

```bash
npx vitest run 2>&1 | grep -E "Test Files|Tests "
```

⚠️ 約 20 秒。⛔ **`check.sh` が緑であることは、試験が緑であることを何ひとつ意味しない。**

⭐ **赤くなったときの直し方**: その試験は**原稿の逐語を錨にしている**。⇒ **畳んだ先（原本の席）へ錨を打ち直す**のであって、**期待値を消して緑にするのではない。**⛔ **錨を切るのは、その試験が何を守っていたのかを消すことである。**

#### ⛔⛔ ただし検査 42 だけを見て済ませるな —— 消した場所と赤くなる場所は別でありうる（束 6 の実測、2026-09-11）

**`check.sh` 一式を、要求 3〜4 件ごとに必ず通せ。**検査 42 が 1 も動いていないのに `check.sh` が落ちる形が実在する。

⚠️ **束 6 の実測**: `FR-040` の写しを畳んだところ、**検査 42 は 469 のまま**で、**検査 11（重複検出）が赤くなった。**⛔ **赤くなった 2 行は、束 6 が 1 文字も触っていない 表 T-023a の `PD-4` と 表 T-023d であった。**

**機序**（`docs/review/dup-check.py` を読んで確かめた）: `COMMON_GRAM = 60` があり、`if len(bucket) > COMMON_GRAM` で**61 以上の単位に現れる 4-gram を「信号を持たない」として捨てる**。
⇒ どこか別の場所を消して、ある gram の出現単位が **61 → 60** に落ちると、**その gram が数えられるようになり、無関係な 2 行の一致度が上がる。**
実測: `解いてはならない` が消えて gram `いてはな` が 61 → 60 になり、`T-023a PD-4` 対 `T-023d` が **0.4286 → 0.4571**（しきい値 0.45）。

⭐ **この赤は本物の写しを指していた**（`D-466`。`FR-031` の同じ MUST NOT が 2 か所）—— **しきい値の直下に隠れていたものが、別の削除で顔を出した。**
⛔ **体には基準線の追記を禁じている**（第 4 節）ので、体は**畳んだ 1 件をバイトごと戻して緑に復し、隠れていた重複を報告する**しかない。⭐ **それでよい。**前に立つ者が `D-466` を畳めば、次から赤くならない。

#### ⚠️ 表 T-023a / T-023d などの行を触ると、検査 37 が指紋違いで赤くなる

`dictionary-table-pairing.txt` が**表の行ごとに指紋を 1 つ**持っており、12 の対照表（`T-109` / `T-016` / `T-104` / `T-037` / `T-234` / `T-024` / `T-023b` / `T-023a` / `T-023c` / `T-023d` / `T-036` / `T-023`）の行を 1 文字でも動かすと赤くなる。

⛔ **これは切り下げてはならない基準線ではない**（しきい値ではなく指紋である）が、⛔ **黙って `--write-baseline` を打つな。**検査が在る理由は「**行が動いたら、表示語がまだその行の言うことを言っているか人が読み直せ**」だからである。

⇒ **手順**: `docs/spec/_source/display-words.json` のその `rowId` の語を引き、**行と読み合わせてから** `--write-baseline`。⛔ **コミットに、読み合わせた語と行を書け。**

### ⭐ 裁定 B の根拠 — ⛔ 次の巡が覆さないように

**`src/` を縛る規則は仕様書に居てよい。** 分かれ目は「利用者に見えるか」ではなく **「機械を作れるか」**である。

根拠: 第 6-2 節の表が自ら挙げる例 —— 表 T-061 の `LR-1`「層をまたぐ依存は内向きだけ」も**利用者には見えない**が、`docs/spec` に置かれ `npm run layers` が観測手段になっている。

⇒ 表 T-006a / T-006b の命名規約（`LOD` は `LevelOfDetail` と書く、`Of` は比率に限る…）も同じ形であり、**仕様書に残る。**

### ⚠️ フェーズ 3 へ回したもの

- **命名規約の検査を 1 本作る** —— 表 T-006a の 5 件は観測手段が無い。`@purity`（裁定 4）と同じ扱いで、**作るまで消さない**。⭐ フェーズ 1 で `src/` を読む検査の作り方は確立済み
- **検査 8 をファイル名形式にも効かせる** —— `GRS JSON` の指し先違いはどの機械も見つけられなかった。⛔ **先に何件赤くなるか数えること**

---

## 1. この巡は何か

あなたは `gr-scheduler` の前に立つ者である。**この巡は掃除である。⛔ 機能を 1 つも足さず、製品コードの行も消さない。消えるのは文だけである。**

なぜ掃除をするのか —— 利用者の逐語（2026-09-10）:

> ClaudeCode に Dynamic HTML を指示すると、かなり高精度にこちらの思うものを作る。 Python もそうだ。しかし、GRS は .md と .ts が主なのに、不具合修正や、俺の指摘対応に膨大な時間がかかる。これが非常に不満だし、改善したい。

---

## 2. まず読め

1. `docs/review/why-a-fix-costs-what-it-does-2026-09-10.md` —— なぜ遅いかの実測
2. `docs/review/third-party-review-2026-09-10.md` —— 前提知識ゼロの体が書いた判定
3. `docs/review/what-belongs-where.md` —— 何を仕様書へ、何をコードへ、何も書かないか
4. `docs/development-rules/` の 01・04・07 —— 進め方・確かめ方・見直しの基準

---

## 3. ⛔ この文書の数を信じるな

**ここの数は書いた時点のものである。測り直して反証せよ。**方法を添えてある。

| 主張 | 測り直す方法 |
|---|---|
| `src/` は 76,506 行のうち 48,736 行（63.7%）がコメント | `//` `*` `/*` で始まる行を、**`src/` だけ**歩いて数える |
| `tests/` は 191,702 行のうち 59,912 行（31.3%）がコメント | 同じ走査を **`tests/` だけ**に |
| ~~`docs/spec` の .md は 768,332 字~~<br>⛔ **反証済（2026-09-11）。真の原稿は 9 ファイル / 700,026 字。**差の **68,306 字（8.9%）は `docs/spec/output/` の中の古い写し 4 本**であり、原稿ではない | 字を数える。⛔ **`output/` を除け**（裁定 11） |
| ~~廃した ID がコメントに 265 箇所（うち素の文字列 129）~~<br>⛔ **反証済（2026-09-11）。実測 328 箇所 / 300 行、素の文字列 177。** さらに**未定義の ID が 33 個 117 参照**あり、こちらは「9 個 23 参照」と書いてあった | 検査 44（`check-spec-id-references.py`）が数える |
| 検査 39 の保持 718 のうち 367 がコメントだけ | `tests/` からコメントを除いて `check-must-clause-coverage.py` を再現 |
| 決着した不具合 431 件のうち、自動試験が落ちて見つかったのは 4.9% | `fixed-defects.md` の行を、名指されている手段で数える |
| 1 行直すのに 33.9 行書いている | 修正コミットの全追加行 ÷ `src/` の実行行の増分 |

⚠️ 前の巡で、申し送りの「試験 195 ファイル / 7411 件」が実際には 197 / 7447 だった。**申し送りは腐る。測れ。**

---

## 4. 指揮と作業の分担

**あなたは指揮である。手を動かすのは体（subagent）である。**

### あなた（指揮）だけがやること

- **測る。**報告する数はすべて自分で測り直す
- **体が上げた「保留」を裁く**
- **体の成果を木の状態で検算する** —— ⛔ 報告の文を証拠にするな。体は作業を孫に委ねて未完のまま返ることがある。**行を数えよ**
- 門を走らせ、コミットし、push する
- 利用者に報告し、タグを依頼する（第 13 節）

### 体（subagent）にやらせること

- **ファイル 1 本のコメントを 1 行ずつ第 6 節に当てる** —— ⛔ 体 1 体につきファイル 1 本
- 仕様書 1 章の無駄狩り
- 検査の実装 —— 1 本につき 1 体

機械的な仕分けは速い体でよい。「行が無いから実装しない」の判断が要るものは強い体を使う（利用者の裁定 2026-08-30）。

### ⛔ 体にさせてはならないこと

- `docs/development-rules/` の書き換え —— **裁定案件**
- 基準線の切り下げ
- 複数ファイルの一括変更
- **自分の作業の合否判定**

### 体に渡す指示に必ず書くこと

- **第 6 節を全文。**⛔ 縮めるな —— 縮めると体は消す方へ倒れる
- 「原典を読め。⛔ 申し送りも、このブリーフも間違う」
- 「⛔ 迷ったら消さずに報告せよ」
- 触ってよい範囲（ファイル 1 本の名）

---

## 5. 既に下りている裁定 —— ⛔ 問い直すな

| # | 裁定 | 中身 |
|---|---|---|
| **1** | **借金返済をやめる** | 検査 39 の基準線 983 は「返すべき借金」ではなく「悪化させない閂」である。既存 983 を返すために試験を書き足すな。根拠: 983 は検査新設日にたまたま在った数。実測で `tests/` の追加 2,560 行（28.2%）が、仕様も `src/` も動かない巡で書かれ、コミットが自ら `repay the debt` と述べている |
| **2** | **5 つの箱の基準と、問う順** | **第 6 節が全文である。**1 行に縮めて引用するな |
| **3** | **検査 39 の判定を反転** | 判定からコメントを除く。⛔ **裁定 1 の後でなければ着手するな** —— 反転すると保持が 718 → 351 に落ちる |
| **4** | **`@purity` は消さない** | 検査を 1 本作る（フェーズ 3）。規則 07 の 80 行目が「判定は `@purity` タグをそのまま使う」と定め、`R7.2` の命名の入力になっている |
| **5** | **`A.3 Changelog` は移す。削除ではない** | 版番号は仕様書独自の採番で Git に無い。`docs/development-records/` へ移し、検査 38 の読む先を 1 行変える |
| **6** | **台帳 5 本は触らない** | `fixed-defects.md`（725,550 字）は刈った先であり、人が通読しない。1 件直す費用に効かない |
| **7** | **記録の扱いは 6-4 に従う**<br>（裁定 2026-09-11、フェーズ 1 で発生した衝突） | `tests/` の**撤回の記録・日付つき実測は消す** —— 6-1 の ① の実例そのものであり、6-4 の表も 消す と書いている。⛔ **ただし例外が 1 つ**: その文が「なぜその literal がその値なのか」の**唯一の説明**であるなら、それは記録ではなく **WHY（箱 D）であり残す**。日付と旧値だけを落とし、理由を現在形で書き直せ。⚠️ **フェーズ 1 では着手しない** —— 第 9 節が `tests/` を `src/` の後に置いている |
| **8** | **検査 44 の 0 は目標ではない** | 廃番を名指すのが**正しい**参照が在る —— ①撤回そのものを守る試験の名前、②原稿の撤回記録を留めるアサーション、③`P-001` のような仕掛けデータ（そもそも仕様 ID ではない）。⇒ **上がったら赤**。⛔ standing の数を借金と読むな（裁定 1 と同じ理由）。**第 8 節 P1-3 の合否「消したら 0 になること」は誤りである** |
| **9** | **廃番一覧の取りこぼしは別件** | `U-29` / `RS-45` / `IC-94` などは**撤回が記録されているのに** `md-checks.py` の `RETIRED` に入っていない。⇒ 検査 44 は「未定義」と報告する。**追加は検査の判定を変えるので裁定案件**。⛔ 掃除の中で勝手に入れるな |
| **10** | **試験名・アサーションが廃番を名指すのは台帳案件**<br>（裁定 2026-09-11。体 3 つが独立にぶつかった） | `describe('FR-061 (MUST NOT) ...')` のように、**試験の名前**が廃番を名指すことがある。試験自体は生きた性質を守っていることが多い。直すには試験名を変えるしかなく、それは掃除が禁じている。⇒ ⛔ **直さずに報告し、台帳へ回せ。**検査 44 の床の一部である |
| **11** | **⛔ `docs/spec/output/` を見るな**<br>（裁定 2026-09-11、実害が出た後） | 生成物であり、**Git が 1 ファイルも追跡しておらず、誰も作り直していないので腐っている**。実測 2026-09-11: `CM-70` は原本の `_assets/tbl-glossary.md` に **0 回**、`output/` の写しに **1 回**。⇒ `grep -rn ... docs/spec` を打った体が「その行は生きている」と結論する。⚠️ **実際に起きた** —— `t-027-outside-the-history.test.ts` の偽の出どころはこれである。⛔ **体に渡す指示には必ず「`docs/spec/output/` を除け」と書け** |

---

## 6. 判定 — 5 つの箱

**掃除の中心はここである。⛔ 問う順を変えるな。**

```
問い 0 ── そもそも要るか？ ───────────── いいえ → 消す（箱 A）
   │ はい
問い 1 ── 要求か、自称か？ ────── 自称 → 消す（箱 A）
   │ 要求
   ├── 製品が満たすこと → docs/spec（箱 B）
   └── 作り手が守ること → docs/development-rules（箱 C）
   │ どちらでもない
問い 2 ── コードを読んでも分からない「なぜ」か？ → 製品コードのコメント（箱 D）
   │ いいえ
問い 3 ── どれでもない ──────────────── 保留して報告（箱 E。⛔ 消すな）
```

⛔ **「仕様書に書けるか？」から問うな。**そこから入ると書く前提になり、そもそも要らないものが仕様書かコードへ配られる。
⛔ **箱 C へ移すべきものを見つけても、自分で移すな**（第 11 節）—— **報告せよ。**

### 6-1. 問い 0 — そもそも要るか（ここで消えるものがいちばん多い）

**1 つでも「はい」なら消す。**

| | 問い | 意味 |
|---|---|---|
| ① | **原本が他所にあるか** | Git ／ 台帳 ／ 仕様書 ／ 生成元。⇒ ここに在るのは写しである |
| ② | **機械が同じことを言えるか** | 型 ／ 検査 ／ 生成 |
| ③ | **コードが自明に語っているか** | 名前で言えることを言い直していないか。⛔ ひどい名前をコメントで補うな —— **名前を直せ** |
| ④ | **その文を消しても、次の人は同じ間違いをしないか** | 機械が止めるなら消してよい。⛔ 止めないなら残す |
| ⑤ | **それは「自称」か** | 「この実装は〜を満たしている」という申告 → 6-2 |

**実例:**

| 実物 | どれで消えるか |
|---|---|
| `2026-09-10 まで「…」と定めていた` | ① —— 仕様書の撤回記録と Git が持つ |
| `実測（2026-09-10、出荷ビルド）: 6 × 16.002px` | ① —— 台帳の `実物確認` 欄が原本 |
| `` `FR-011` は「両端を動かさないこと（MUST）」と定めている `` | ① —— **指し先 1 語だけ残せ** |
| `/** 名前を返す。 */ function nameOf()` | ③ —— 名前が言っている |
| `負の値は AT-39 が禁じている` の隣に `if (n < 0) refuse()` | ②③ |

⛔ **例外 1 つ**: `@purity pure` は ② に見えるが、判定する機械がまだ無い（裁定 4）。**作るまで消すな。**

### 6-2. 問い 1 — 要求か、自称か → 仕様書か規則へ

**観測手段が 1 つも無い文は、どこにも書かない。**観測はこの製品に 6 つある（決着した不具合 431 件が自ら名指した手段。重複あり）:

| 手段 | 実測 | 具体 |
|---|---:|---|
| **出荷ビルドを押す** | **74.9%** | `dist/index.html` ／ Playwright の探り針 |
| 利用者が使って気づく | 59.6% | 申し立ての逐語 |
| 書き出したものを開く | 16.0% | JSON ／ MSPDI ／ SVG ／ PNG |
| 機械検査が指す | 12.3% | `check.sh`（**42 本**。うち 2 本が `src/` を読む） |
| e2e が落ちる | 8.6% | `tests/system` |
| `Agent API` で読む | 8.4% | `readDocument()` |
| **自動試験が落ちる** | **4.9%** | `vitest` |

⚠️ **自動試験が見つけた欠陥は 4.9% である。**それなのに直近の巡は追加行の 76.4% を `tests/` に注いだ。**試験を厚くしても欠陥は見つからない。押すことが見つけている。**

**要求と自称の見分け:**

| | 例 | 行き先 |
|---|---|---|
| **要求**（製品が満たす） | 「層をまたぐ依存は内向きだけとすること」（表 T-061 の `LR-1`） | `docs/spec`。観測手段 ＝ `npm run layers` |
| **要求**（作り手が守る） | 「すべての関数に `@purity` タグを付す」（`R7.6`） | `docs/development-rules`。観測手段 ＝ ⚠️ まだ無い（裁定 4） |
| **要求**（性能） | フレーム時間の上限（`NFR-002`） | `docs/spec`。観測手段 ＝ Playwright の計測 |
| データ | `@purity pure` の印そのもの | **残す。**要求が要求しているデータである |
| ⛔ **自称** | 「この設計は Clean Architecture に従う」／「この関数は純粋である」／「この計算は O(n) である」 | ⛔ **どこにも書かない（箱 A）** |

**見分けの目印**: 要求は「〜すること（MUST）」の形。⛔ 自称は「〜である」「〜に従う」の形。
自称を書かない理由は 1 つ —— 観測手段が**ある**ならコメントは機械の重複であり、**無い**なら誰にも検証できない。
⚠️ **自称は必ず腐る**: `FR-093` の隣のコメントは「2 か所に書くと必ず離れる」と述べるが、実物は **4 か所**である。

### 6-3. 問い 2 — 製品コードのコメントに何を残すか

⛔ **この節が裁くのは「コメントの文」であって、コードではない。**「この行は要るか」ではなく **「この文は要るか」と問え。**

**「コード」は 2 つある。⛔ 混ぜて数えるな**（利用者の裁定 2026-09-11）:

| | 実測 2026-09-11 | 扱い |
|---|---|---|
| **製品コード** `src/` | 68 本 ／ 76,506 行、うち **コメント 48,736 行（63.7%）** | **本節。ここが的である** |
| テストコード `tests/` | 227 本 ／ 191,702 行、うち コメント 59,912 行（31.3%） | 6-4。役割が違うので規則も違う |

⚠️ **製品コードは 3 行に 2 行がコメントである。この巡でいちばん重い数はこれである。**

**判定は 2 段。漏れなく重なりなく 3 つに分かれる:**

| その文は | コードを読めば | 押せば | 行き先 |
|---|---|---|---|
| **何をしているか**（WHAT） | 分かる | — | **書かない（箱 A）**。名前で言えるなら**名前を直せ** |
| **何を満たすか**（約束） | 分からない | 分かる | **仕様書（箱 B／C）** |
| **なぜそう書いたか**（WHY） | 分からない | 分からない | **コメント（箱 D）** |

**Readable Code 第 5 章が「書け」と言うもの** —— 要点は 1 つ、**コメントの目的は書き手が何を考えていたかを読み手に伝えることである**:

| 書くもの | この製品の実例 |
|---|---|
| **採らなかった道とその理由** | 「`ScheduleGeometry` から読むと `LR-3` の閉路になる」 |
| **読み手が抱く疑問への先回り** | 「`Math.max` にしたのは、`null` が来ると `undefined` が伝播して黙って消えるから」 |
| **罠の告知**（触ると壊れる／遅い／順序に依存する） | 「ここで早期に返すと、下の分岐が到達不能になる」／「ここに `const` を置くと初期化前に読む」 |
| **なぜその値なのか**（根拠） | 値そのものは仕様書が原本。書くのは根拠だけ |
| 欠陥の自覚（`TODO` ／ `FIXME`） | ⚠️ この製品では台帳が担う。二重に書くな |
| 全体像・要約（この塊は何のためか） | ファイル頭の 2〜3 行。中身の言い直しは書くな |

**仕様書とコメントの違い:**

| | 仕様書 `docs/spec` | コメント `src/` |
|---|---|---|
| 誰に向けた文か | 製品の外 —— **利用者** | **次にこの関数を書き換える人だけ** |
| 何を言うか | **何を満たすか**（WHAT） | **なぜそう書いたか**（WHY） |
| 確かめ方 | **押せば分かる** | **押しても分からない** |
| 変わる時 | 要求が変わったとき | 実装を書き換えたとき |
| **腐ったら誰が気づくか** | 利用者が押して気づく | ⚠️ **誰も気づかない** |
| **原本か** | **原本である** | **原本ではない** |

**最後の 2 行が、この掃除の理由そのものである。**コメントは**腐っても誰も気づかない唯一の場所**であり、しかも原本ではない。⇒ **だから最小限にせよ。**

**消すもの:**

| 消す文 | なぜ | どこへ |
|---|---|---|
| `function nameOf()` の上の「名前を返す」 | コードを読めば分かる（WHAT） | どこへも。消す |
| ひどい名前を補うコメント | 松葉杖 | **名前を直せ** |
| 「この関数は純粋である」 | 自称（6-2） | どこへも。消す |
| 「`FR-011` は『両端を動かさないこと』と定めている」 | 原本は仕様書 | **指し先 1 語だけ残す** |
| 「`S-93` は 30px である」 | 原本は生成元 | `_source/settings.json` |
| 「2026-09-10 まで別の値だった」 | 原本は Git | Git ／ 仕様書の撤回記録 |
| 「D-408 で見つかった」 | 原本は台帳 | 台帳 |

**写しは、直す人が全部を見つけない限り必ず腐る。**実装の理由にはその経路が無い —— コード行が消えれば文も一緒に消える。実測: 仕様の逐語引用は `src/` に 2,118 行、`tests/` に 4,912 行。

⚠️ **ただしコメントの理由も腐る。**「2 か所に書くと必ず離れる」と書いた隣で実物は 4 か所である（`FR-093`）。**箱 D に入れる前に、その理由が今も真かを確かめよ。**

### 6-4. テストコード（`tests/`）は規則が違う

⛔ **製品コードの規則をそのまま当てるな。**試験の仕事は**どの条項を押さえているかを名指すこと**だからである。赤くなった試験を見た人が、まず知りたいのは「どの条項が破れたか」である。

| コメントの中身 | 判定 | 実測 `tests/` |
|---|---|---:|
| **指し先**（`` `FR-011` ``） | ⛔ **消すな。**消すと、赤が何を守っていたのか誰にも分からない | 4,685 行 |
| 何を押したか（手順が自明でないときの 1 行） | 残す | 散文の中 |
| **条項の逐語引用** | 消す。指し先だけ残せ | **4,912 行** |
| 台帳の番号 | 消す | 1,008 行 |
| 撤回の記録・日付つき実測 | 消す | 108 行 |
| `@purity` の印 | 消すな（裁定 4） | 440 行 |

⛔ **試験のコメントの嘘は、製品コードより悪い** —— 製品コードの嘘は押せば露見することがあるが、**試験のコメントの嘘は試験が緑のまま居座る。**引用した条項が変わっても、その試験は別のことを確かめたまま緑を出し続ける。
実在する: 検査 39 が数える保持 718 件のうち **367 件（51.1%）は試験の主張ではなくコメントだけ**である。

⛔ **順を守れ** —— **裁定 3 を済ませてから `tests/` のコメントに手を付けよ。**逆にすると、消した分だけ保持が落ちて検査 39 が赤くなる。

### 6-5. 問い 3 — どれでもない → 保留して報告

⛔ **判断がつかないものを消すな。**報告の形は「ファイル ／ 行 ／ その文 ／ 迷った理由」。**前に立つ者が裁く。**
⚠️ 体には「⛔ 迷ったら消さずに報告せよ」と必ず書け。**体は判断を求められると消す方へ倒れる。**

### 6-6. 見分け表

| 箱 | 判定 | 実測（この製品） |
|---|---|---|
| **A・消す** | 原本が他所／機械が言える／コードが自明／機械が止める／自称である | **いちばん多い**。`src/`: 逐語 2,118・台帳 881・撤回 60・実測 9 行 |
| **B・`docs/spec`** | **製品が満たすこと**で、6 つの手段で観測できる | 値・語・順序・約束・層規則・性能 |
| **C・`docs/development-rules`** | **作り手が守ること**で、観測手段が在るか作れる | 命名（`R7.2`）・純粋性（`R7.6`）。⛔ **自分で移すな。報告せよ** |
| **D・製品コードのコメント** | コードを読んでも押しても分からない ⇒ **なぜそう書いたか** | 採らなかった道・疑問への先回り・罠・値の根拠・全体像 |
| **E・保留** | どれでもない | **消さずに報告** |

⚠️ `tests/` のコメントはこの表では裁けない。**6-4 の別表による。**

---

## 7. 3 つのフェーズに分ける。⛔ 順を入れ替えるな

⚠️ **フェーズ 1 は本命ではない。フェーズ 2 を安全かつ速くするための道具作りである。**

| フェーズ | 何をするか | なぜその順か |
|---|---|---|
| **フェーズ 1** ✅ 完了<br>道具 | 借金返済を止める ／ `src/` を見る機械を作る ／ その機械の動作確認を兼ねて死んだ ID を消す | ⛔ この 3 つが無いまま `src/` の散文 34,841 行に手を入れると、「何を消したか分からず、何が壊れたか分からず、次の巡でまた生える」 |
| **フェーズ 2** ⚠️ 進行中<br>**本命** | **仕様書本体の無駄狩り**（**700,026 字**）と **`src/` のコメントの無駄狩り**（48,736 行）。⚠️ `tests/` は裁定 3 の後、`src/` を終えてから | 基準が裁定済みで機械が守るので、**体を並列で走らせられる** |
| **フェーズ 3**<br>後始末 | `@purity` の検査 ／ 関門の順序 ／ `FR-093` の結合 ／ 台帳 | 急がない。フェーズ 2 の結果を見てから |

---

## 8. フェーズ 1 — 道具を作る（✅ **完了**。⛔ やり直すな。何をどう作ったかの記録として読め）

| | 手 | 合否 |
|---|---|---|
| **P1-1** | **借金返済を止める。**`check-must-clause-coverage.py` を「基準線より悪化したら落ちる」だけに保つ。返済を促す出力を消すか、新規 MUST だけを見る門を別に立てる。どちらにするかは原典を読んで決め、**理由をコミットに書く** | 検査 39 が緑。**返済の作業が二度と発生しない形であること** |
| **P1-2** | **`src/` と `tests/` を見る機械を新設する。**いま `src/` を見る検査は **0 本**であり、これが発生源対策である | 段 1・2 が門。段 3 は候補出し |
| **P1-3** | **P1-2 段 1 の動作確認を兼ねて、死んだものを消す。**`nfr-004-file-scheme-sweep` の `[data-icon="IC-69"]` は `if (yes !== null)` に守られて永久に空振り。着かない仕様 ID は 9 個 23 参照。廃した ID の生きた参照は 265 箇所 | ⛔ **合否を書き換えた（裁定 8）。**機械が指すこと、赤を先に出せること、そして**基準線が下がること**。⛔ **0 にはならない** —— 撤回を守る試験名や原稿の撤回記録を留めるアサーションは、廃番を名指すのが正しい |
| **P1-4** | 裁定 3（検査 39 の反転）を、⛔ **P1-1 の後に**行う | 検査 39 が緑（新しい基準線で） |

**P1-2 の 3 段** —— ⛔ すべて古典的判定で作れ。外部モデルを呼ぶな。

| 段 | 判定 | 道具 | 門にするか |
|---|---|---|---|
| 1 | **廃した ID・着かない ID** —— `docs/spec` が定義する ID の集合と、`src/`＋`tests/` が参照する集合の差 | `re` ＋ `set` | **門にする** |
| 2 | **同じ式が N 箇所** —— 正規化した式の指紋で数える | `re` か tree-sitter | **門にする** |
| 3 | **現在形で断定している文だけを抽出し、その中の ID を検査** —— 「〜である」は主張、「〜だった」「until 20XX」は記録 | `re` | ⚠️ まず候補出し。精度を測ってから門へ |

⛔ **LLM を呼ぶ 4 段目は作るな。**非決定的な門は「巡が赤で終わる」を増やす。要るなら門ではなく、掃除のときだけ手で走らせる候補出しの道具にせよ。
⚠️ **段 3 が要る理由**: 廃した ID 265 箇所のうち **129 箇所はバッククォートの外**に在り、検査 42 からも `md-checks` からも見えていない。

**フェーズの終わり**: `After-Cleanup-P1` のタグ付けを利用者に依頼せよ（第 13 節）。

---

## 9. フェーズ 2 — 本命の無駄狩り

⛔ **フェーズ 1 の道具が緑になるまで、このフェーズに入るな。**判定はすべて第 6 節による。

### 9-1. 仕様書本体（**700,026 字**。⛔ 768,332 は生成物込みの誤り）

| | 手 | 目安 |
|---|---|---|
| **P2-1** | `A.3 Changelog` を `docs/development-records/changelog.md` へ移す（裁定 5）。`A-appendix.md` には移設先を指す 1 行だけ残す | **183,208 字＝23.8%** |
| **P2-2** | 残る **513,758 字**を、第 6 節の順で仕分ける。⛔⛔ **体を並列で走らせるな（裁定 D、2026-09-11）** —— 1 つのファイルを 2 体が同時に編集すると、行範囲が交わらなくても片方の編集が消え、⚠️ **消えても赤にならない。**第 0 節を読め | Chapter 1 のみ完了 |
| **P2-3** | ⚠️ 要求 1 件が引く他 ID は中央値 19 個・最大 164 個、1 行が最長 3,708 字。**この結合度と行長そのものが「読む量」を生んでいる** | 未測 |

⛔ **`docs/spec` を削るときの絶対条件:**
- ⛔ **MUST を裁定なしに消すな。**⚠️ 前の巡で、不具合と思われたものが仕様どおりの挙動だった実例が在る（表 T-021a の `PV-1`）
- **問い 1 を必ず通せ** —— その MUST は 6 つの手段のどれで観測するのか。⚠️ 答えられない MUST は、消すのではなく**報告せよ**
- ⛔ 削るたびに `check.sh` を通し、**1 つの節を削るごとにコミットせよ**

### 9-2. コードコメント —— ⛔ `src/` を先に終えてから `tests/` へ

**`src/`** —— 68 本 ／ 76,506 行、うち コメント 48,736 行（63.7%）

| 分類 | 行数 | 判定 |
|---|---:|---|
| **印も ID も引用も持たない散文** | **34,841** | **本命。第 6 節を 1 行ずつ当てる** |
| 中身の無い行（`/**` `*` `*/` `//` だけ） | 8,828 | ⚠️ 消しても読む量は減らない。優先度は低い |
| **仕様の逐語引用** | **2,118** | 問い 0 の ① ⇒ 指し先 1 語へ |
| `@purity` の印 | 1,228 | 消すな（裁定 4） |
| 台帳の番号 | 881 | 問い 0 の ① ⇒ 消す |
| 行 ID の指し先だけ | 771 | 消すな。仕様書に戻る道である |
| 撤回の記録・日付つき実測 | 69 | 問い 0 の ① ⇒ 消す |

**散文 34,841 行の進め方:**
1. P1-2 の段 3 が「現在形で断定していて、かつ廃した ID を含む」行を指す ⇒ **嘘なので消す**（優先）
2. 残りは体に読ませ、第 6 節の順で 5 つの箱へ。⛔ 体 1 体につきファイル 1 本
3. ⛔ 迷ったら消さずに報告させよ

**`tests/`** —— 227 本 ／ 191,702 行、うち コメント 59,912 行（31.3%）。⛔ **裁定 3 が済むまで着手するな**（6-4）。

| 分類 | 行数 | 判定 |
|---|---:|---|
| 散文 | 41,556 | ⚠️ `src/` を終えてから当てよ |
| 中身の無い行 | 7,203 | 優先度は低い |
| **仕様の逐語引用** | **4,912** | 指し先 1 語へ |
| 行 ID の指し先だけ | 4,685 | **消すな**（6-4） |
| 台帳の番号 | 1,008 | 消す |
| `@purity` の印 | 440 | ⛔ 消すな |
| 撤回の記録・日付つき実測 | 108 | 消す |

⚠️ **数え方**: 行頭が `//` ／ `*` ／ `/*` の行をコメントとし、**1 行は最初に当たった分類にだけ計上する**（順は表の上から）。⇒ 列の和がコメント行数に一致する。⛔ `src/` と `tests/` は別々に歩くこと。

**フェーズの終わり**: 第 12 節の数を測り直し、`After-Cleanup-P2` のタグ付けを依頼せよ。

---

## 10. フェーズ 3 — 後始末

| | 手 | 裁定 |
|---|---|---|
| **P3-1** | `@purity` を検査する機械を 1 本作る（裁定 4）。4 値のうち `semi-pure-a` は実測 **1 個**しかない —— 検査ができたら数を報告せよ | 済 |
| **P3-2** | 関門の順序を変える。⚠️ 関門一式 9.4 分のうち **88% が Playwright**（8 分 17 秒） | 不要 |
| **P3-3** | `FR-093` の数え式が 4 か所に在る。層規則が結合を許さない対は残し、**同じファイル内の 2 本目は必ず畳め** | 不要 |
| **P3-4** | 論駁の圧縮 | ⚠️ **上限の裁定が要る。着手前に問え** |
| **P3-5** | 台帳 5 本 | 裁定 6 により触らない |
| **P3-6** ⭐⭐ | **検査 42 を `docs/spec` の内側へ向ける。**⛔ **フェーズ 2 がいちばん多く見つけた欠陥はこの形である** —— **要求が別の席を「…と定めている」と引き、引かれた側は実際には別のことを定めている。**⚠️ **束 1〜3 の 3 束だけで 6 件**（`D-445` `D-446` `D-447` `D-449` `D-450` `D-451`）。⛔ **いま見ている機械は 1 本も無い** —— 検査 42 は `src/` と `tests/` のコメントしか読まず、**引用元も引用先も `docs/spec` に在ると当たらない。**⭐ **母数は小さい** —— **実測 2026-09-11: 「`X` は『…』と定めている」の最も狭い形が 31 件**（`01-04` 18・`05-07` 5・`tbl-settings` 5・`tbl-property-items` 2・`tbl-glossary` 1）。⇒ **検査 42 の正規化と照合をそのまま内側へ当てられる。**⛔ **先に何件赤くなるか数えること** | ⚠️ **未裁定。フェーズ 2 が終わってから問え** |
| **P3-7** | **`**` の対応を見る検査を 1 本作る**（`D-448`）。⚠️ **実測: 奇数の行 10・`****` が 35 か所。**⛔ **検査 16（render）は全部緑で通す。** | ⚠️ **未裁定。`D-448` と同じ** |
| **P3-8** ⭐⭐ | **2 つの表の集合差を取る検査を 1 本作る。**⛔ **仕様書は「本表が全数である（MUST）」と何度も言い、そのうちいくつかは偽である。**⚠️ **実測 2026-09-11**: 表 T-076 は 表 T-103 の面を **7 つ**取りこぼしながら「行を持たないのは 3 つだけ」と述べ（`D-457`）、`FR-074` の除外一覧は `Project` の列を **1 つ**落としながら「それぞれ別の持ち主がいる」と述べる（`D-461`）。⭐ **どちらも 2 つの表の行 ID の差を取るだけで出る。**⛔ **先に何件赤くなるか数えること** | ⚠️ **未裁定。フェーズ 2 が終わってから問え** |

---

## 11. ⛔ 触ってはならないもの

- **`D-311`**（第 8・9・10 章が空の器）—— 2026-09-06 に「一旦そのまま」と裁定。**1 文字も触るな**
- **`docs/development-rules/`** —— 規則の書き換えは裁定案件。第 5 節の 6 件以外は**問うてから**
- **基準線の無断切り下げ** —— 上げるのも下げるのも**理由をコミットに書く**こと
- **`Playwright` の 2 件の赤** —— `PD-443`（フレーム時間 20.42ms 対 上限 16.7ms）。**この掃除より前から赤である**

---

## 12. 合否 —— フェーズの終わりごとに測り直す

⛔ **消した行数は合否ではない。**

| 測るもの | 掃除前（実測） | 方法 |
|---|---|---|
| **1 行直すのに何行書いているか** | **33.9 行** | 修正コミットの全追加行 ÷ `src/` の実行行の増分 |
| **`src/` のコメント行** | 開始 48,736 → **48,668 行**（76,438 行の 63.7%）<br>⛔ **フェーズ 2 の本命。まだ手を付けていない** | `npm run stats` が数える |
| `src/` のコメント ÷ 実行行 | 1.95 | 行を数える |
| `tests/` のコメント行 | 開始 59,912 → **59,719 行**（191,509 行の 31.2%） | `npm run stats` が数える |
| `docs/spec` の .md の字数 | ~~768,332~~ → 開始 **700,026** → いま **509,606 字**（**−27.2%**）/ 9 ファイル<br>⚠️ **うち P2-1 の移設が 183,208 字。4.1 の 6 束が出したのは 4,238 字（仕様書の 0.6%）である** | `npm run stats` が数える。⛔ `output/` を除く |
| ⭐⭐ **仕様書の嘘** | 開始 **0 件**（誰も探していなかった）→ **23 件**（`D-445` 〜 `D-467`、うち 3 件が振る舞いに効く）| ⛔ **機械が無い。**体に 1 件ずつ実物と突き合わせさせ、前に立つ者が独立に測り直した |
| 廃した／着かない ID の生きた参照 | ~~265~~ → **実測 445**（廃番 328・未定義 117）<br>**P1-3 完了時 163**（廃番 107・未定義 56、wave 1〜3 で 78 ファイル） | 検査 44 が数える。⛔ **0 は目標ではない**（裁定 8）。163 は床に近い |
| `tests/` の変更行のうち、仕様も `src/` も動かない巡の分 | 28.2% | `git show --numstat` を分類 |
| 関門一式の所要 | 9.4 分 | 時間を測る |

**「1 行直すのに何行書いているか」が下がっていれば成功である。**
⚠️ フェーズ 1 はこの数がほとんど動かない。それでよい —— 効くのは フェーズ 2 である。

---

## 13. 作法・検査の走らせ方・報告

### 作法

- **赤を先に出せ**（規則 04）。**手ごとにコミットせよ** —— ⛔ フェーズを 1 コミットに混ぜるな
- ⛔ **コミットの前に毎回、個人情報の点検を行い、実測した数を報告してから**コミットすること
- ⛔ **コミットメッセージに attribution を書くな**
- ⛔ **一時ファイルはセッションの scratchpad へ。このリポジトリの外のファイルを書き換えるな**（読むのはよい）
- ⛔ **コードとスクリプトは英語・ASCII で書く**（日本語の仕様書・記録は例外。逐語の引用も例外）

### 検査の走らせ方 —— ⛔ トークンを溶かすな

検査は**別プロセス**で走り、あなたのコンテキストに入るのは標準出力だけである。既定はこう打つ:

```bash
bash .claude/skills/spec-graph-check/check.sh > /dev/null 2>&1; echo "exit=$?"
```

⚠️ 実測: `check.sh` は **37 秒**、読む量 354 ファイル / 18.0 MB、出力 240 行 / 21 KB。⇒ **出力を丸ごと受け取ると 1 回で数千トークン溶ける。**落ちたときだけ該当節を `grep` で取り出せ。

| 門 | 所要 | 打ち方 |
|---|---|---|
| `npx tsc --noEmit` | 数秒 | 誤りの数だけ数える |
| `npx vitest run` | 21.7 秒 | ⛔⛔ **仕様書を触る巡は必須。**正は「⛔⛔⛔ そして `vitest` を走らせろ —— 原稿を読む試験が在る」の節であり、**本表はその打ち方だけを持つ** —— `Tests ` の行だけを `grep` で受ける。⛔ **「この巡はコードを触らない」を理由に飛ばすな** —— **原稿を実行時に読む試験が在り、仕様書を 1 文畳むだけで落ちる。**⚠️ **前の巡は飛ばし、`check.sh` が 42 本とも緑のまま 6 件が赤になり、そのまま 4 回 push した。** |
| `check.sh` | 37 秒 | 上のとおり |
| `npm run layers` ／ `npm run parity` | 数秒〜数十秒 | 末尾 2〜3 行 |
| `npx playwright test` | **8 分 17 秒** | ⚠️ **フェーズの最後に 1 度だけ** |

### 報告の形（手ごと・フェーズごと）

1. **実測値** —— 何行消し、何行増えたか。第 12 節の数がどう動いたか
2. **箱ごとの内訳** —— 消した分を、問い 0 のどの下位問いで消したか。**保留に回した数も必ず報告せよ**
3. **門の終了コード** —— `tsc` / `vitest` / `check.sh` / `layers` / `parity`
4. **意図して動かした基準線** —— どれを、いくつからいくつへ、なぜ
5. **このプロンプトのどこが間違っていたか** —— ⛔ 見つからなかったなら「測っていない」と書け

### タグ付けの依頼

⛔ **あなたは `git tag` を打てない**（`gr-scheduler` の git 例外に `tag` は含まれない）。各フェーズの終わりに、門が緑になり、コミット＆Push を済ませたら、利用者に次を渡して依頼すること。

```bash
git tag -a After-Cleanup-P1 -m "After week 1 of the cleanup: the instruments" && git push origin After-Cleanup-P1
```

巡の始まりには `Before-Cleanup` が打たれている。⇒ 成果は 1 行で出る:

```bash
git diff Before-Cleanup..After-Cleanup-P2 --stat
```

⚠️ **依頼の際は第 12 節の「掃除の前後の数」を添えること。**「終わりました」だけでは、成果が測れたことにならない。
