# W5 `Framework` —— 開発の記録

**規則は [`docs/development-rules/05-working-method.md`](../development-rules/05-working-method.md)。書式は [README](README.md)。**

**対象**: `src/framework`   ⭐ **機械検査がこの宣言を読み、状態表と実物を突き合わせる。**

---

## 現在地

⭐ **2026-08-22: `FT-1` が結線され、押すと動くようになった。**

`boot()` が `ScreenRenderer` と `DomScreenSurface` を 表 T-077 の順で呼び、`appHeaderHeight` を
`FR-051` のとおり実測する形は前回のままである。**今回足したのは、`DomInputSource`（`PI-27`）を起こして
`IF-2` の出来事を `PI-18` の 3 メンバへ渡す道である。**

`FrameLoop` に 2 つ増えた（どちらも `non-pure`）:

```
isBrowserDefaultStopped(input)   MK-10 の答え。⛔ 監視者が聞く前に問われる。何も変えない
receiveInput(input)              FT-1。文脈を 1 つ組み、3 メンバへ渡し、書き込み、フレームを要る
```

### ⭐ 実 DOM で自分で確かめた（⛔ サブの報告ではない）

⚠️ **隠れた文書では `requestAnimationFrame` も `ResizeObserver` も配送されない**ので、
測定側で `requestAnimationFrame` を差し替えてから測った。**アプリのコードは 1 行も変えていない。**

```
MK-10 の両側     Ctrl+A / Ctrl+Shift+D / Ctrl+Z / F1 → 止める
                 素の Q / Ctrl+Shift+Q               → 止めない
IN-4 / IN-4a     F1 → Esc（止める）→ Esc（止めない）→ F1 → Esc（止める）→ Esc（止めない）
                 ⭐ 段を 1 つずつ食う。**ループが screenState を出来事の間で保っている証拠である**
FT-1 の約束      ホイール 4 件 → フレーム 4 回。押下・移動・離し 3 件 → 3 回
ホイール         Ctrl / Shift / Alt でバーの右端が 954.13 → 954.23 → 957.19 → 954.32 と動く
書き込み（T-067）Ctrl+Shift+D で line 6 → 5、もう一度で 5 → 6
                 ⭐ 戻った絵は元と同じではない（circle 50 → 49 のまま）——
                    **テンプレートの基準日ではなく本日が入ったからである。`FR-046` が効いている**
身振り           葉のバーを掴んで 80px 右へ → x 441.72 → 522.01。図形 8 個が連動
                 ⚠️ 最上位の要約バーを掴んでも動かない。**欠陥ではない**（あれは巻き上げである）
console error    0
```

### ⛔ 自動試験はまだ 1 件もこの道を守っていない（**正直に書く**）

⚠️ **`tests/unit/uf-47-48.test.ts` は `receiveInput` も `isBrowserDefaultStopped` も 1 度も呼ばない。**
⛔ **だから `UF-48` の段を `🔧 実装済` へ戻した** —— 規則 05 の 4. は「別の者が仕様だけを読んで書いた試験が緑」を
`✅ 受入済` の条件としており、増えた 2 メンバはその条件を満たしていない。
⛔ **`UF-47` は前回同様 🔧 のままである** —— `boot()` は取り込むだけで起動するので node の単体試験から届かない。
**表 T-218 の `TS-3`（Playwright）で受けるまで上がらない。**

### ✅ 仕様だけを読んだ試験が入り、本物を 3 件出した

`tests/unit/uf-48-input.test.ts`（42 件）。⭐ **原稿を読み込み時に引くので、行の文言が変わればここが落ちる。**
⛔ **赤で出た 3 件はすべて本物の欠陥であった**（`FR-048` ／ `IN-4` ／ `IN-4a`）。**直したのはコードであり、試験ではない。**

⚠️ **`UF-48` の段はまだ `🔧` である** —— **増えた 2 メンバは緑になったが、`carryOutAction` の取り消し・やり直しの枝が試験 0 件である。**

**次の一手**:

```
1. ⭐ tests/usecase（TS-1）—— 結線が通ったので書けるようになった
2. ⛔ replaceDocument（PI-8）を書く —— CR-205 が刻印を裁定したので、いま書ける
     ⭐ これが取り消し・やり直しの「第 2 の入口」（MS-1 が禁じる形）を閉じる
3. ⛔ scheduleViolations（PI-1）—— 表 T-220 の 17 個の不変条件に実装が 1 つも無い
     ⚠️ 検査 26b の baseline に負債として立っている
4. AM-13 の書き出しの絵（IF-7 を広げ、パネルを閉じた 1 枚をシェルが組む）
     ⚠️ 赤 2 件はこれである
5. ⚠️ HistoryLimits.maxTotalSize の名前に単位が無い（R2.1 は MUST）—— 公開署名なので CR
```

---

## 状態表

⭐ **段**: ⬜ 未着手 → 🔧 実装済 → 🧪 試験済 → ✅ 受入済
⛔ **✅ になるまで、他のユニットがそれに依存してはならない**（05 の 4.）。
⭐ **例外は型だけ** —— 公開エントリの型が型検査を通れば、依存側は書き始めてよい（05 の 3.）。

### `browser-clipboard` —— BrowserClipboard（PI-30）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-53 | `browser-clipboard.ts` | **公開エントリ** | non-pure | PI-30 | ✅ 受入済 |

### `canvas-rasterizer` —— CanvasRasterizer（PI-31）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-54 | `canvas-rasterizer.ts` | **公開エントリ** | semi-pure-b | PI-31 | ✅ 受入済 |

### `dom-input-source` —— DomInputSource（PI-27）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-50 | `dom-input-source.ts` | **公開エントリ** | non-pure | PI-27 | ✅ 受入済 |

### `dom-screen-surface` —— DomScreenSurface（PI-38）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-71 | `dom-screen-surface.ts` | **公開エントリ** | non-pure | PI-38 | ✅ 受入済 |

### `dom-svg-surface` —— DomSvgSurface（PI-26）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-49 | `dom-svg-surface.ts` | **公開エントリ** | non-pure | PI-26 | ✅ 受入済 |

### `file-system-access-file-store` —— FileSystemAccessFileStore（PI-28）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-51 | `file-system-access-file-store.ts` | **公開エントリ** | semi-pure-b ／ non-pure | PI-28 | ✅ 受入済 |

### `local-storage-document-store` —— LocalStorageDocumentStore（PI-29）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-52 | `local-storage-document-store.ts` | **公開エントリ** | semi-pure-b ／ non-pure | PI-29 | ✅ 受入済 |

### `single-html-shell` —— SingleHtmlShell（PI-25）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-47 | `single-html-shell.ts` | **公開エントリ** | non-pure | PI-25 | 🔧 実装済 |
| UF-48 | `frame-loop.ts` | 内部 | non-pure |  | 🔧 実装済 |

---

## 記録

⭐ **追記のみ。過去の行を書き換えない。**

| 日付 | 種別 | 内容 |
| --- | --- | --- |
| 2026-08-19 | 先行 | ⭐ **W4 の縦線のために `UF-47` / `UF-48` / `UF-49` を先に書いた**（規則 05 の 2. の「次の波へ先に手を出してよい」）。⛔ **触ったのは 8 コンポーネント中 2 つ・9 ファイル中 3 ファイルだけである** |
| 2026-08-19 | 決めた | ⭐ **`SvgSurface` は SVG を**文字列**で受ける**（`showSvg(svg: string): void`）—— `SvgRenderer` は `pure`（表 T-075 の `UF-32`）なので、継ぎ目を越えるものは値でなければならない。⚠️ **差分ではなく画面ぜんたいを渡す** —— 表 T-078 が frame の回数を既に絞っており、差分の規則はどの要求も持たない |
| 2026-08-19 | 決めた | ⭐ **`frame-loop.ts` は frame を「予約」する** —— 契機が 2 つ同じ tick に来ても 表 T-068 が 2 度走らないようにするため。⚠️ **最初の 1 枚だけは予約せず直に描く**（表 T-078 の注が「最初の 1 枚は `BO-5` が起こす」と定める）|
| 2026-08-19 | 決めた | ⚠️ **`appHeaderHeight` は 0 で走らせている** —— `FR-051` は実測を求めるが、測る側（`ScreenRenderer` と `DomScreenSurface`）はまだ無い。⭐ **0 は仮置きではなく、いま画面にヘッダーが無いという事実である。** 第 2 歩で実測へ差し替える |
| 2026-08-19 | 決めた | ⚠️ **テンプレートの `Document` への変換は型の言い切りで通している** —— ⛔ **`DocumentCodec` の `documentFromJson`（`UF-35`）がまだ stub だからである。** ⭐ **型も JSON も同じ `erd.json` から生成されているので、食い違えばそれは生成器の欠陥である。** `UF-35` ができたら解析と検証に置き換える |
| 2026-08-19 | 罠 | ⛔ **層検査が `.json` の取り込みを「ファイルではない」と拒んだ** —— 取り込み先を必ず `.ts` に直して探していた。⭐ **`.json` は表 T-075 のユニットではなく（検査 18 も `.ts` だけを数える）、コンポーネントへの手出しでもない**ので、存在だけ確かめて通す形に直した |
| 2026-08-19 | 罠 | ⛔ ⭐ **実 DOM が欠陥を 2 件出した。型検査も 684 件の試験も 1 つも捕まえなかった。** ⚠️ **①** `BO-3` の `OP-10` の分岐を実装しておらず、`scrollDate` の既定が `null` なので**時間軸の原点が 1970-01-01 になり、2026 年のタスクが `x ≈ 124139` に落ちていた**（SVG の幅は 6801）。⭐ `frame-loop.ts` に `viewSettings` を置いて閉じた —— **文書は変えない**（`OP-10` は読む側の規則）。⚠️ **②** 絵の寸法に `layout.contentWidth` を流用しており、座標は画面空間なので**図形が枠の外に出ていた**（`maxX 978 > 908`）。⭐ **`FR-080` は「`GRS` が占める画面の全体」を求める**ので、CR-189 で辺を 1 本足して画面の矩形を渡す形にした |
| 2026-08-19 | 実測 | ⭐ **縦線が通った。** SVG は viewport（1280×800）と一致し、図形は枠内。`aria-label` に文書名。⚠️ **全体表示では polygon が 7 個しか出ない** —— **欠陥ではなく `HR-1a`** （100 行を 1 画面へ収めると LOD が深さ 1 まで畳み、バーを直に持つ最上位の行が `Total System` だけ）。⭐ **テンプレートの形の問題である** —— 対象ごとの最上位の行にも要約のバーを置くと `VK-3` が動いて見える |
| 2026-08-19 | 発見 | ⛔ **仕様騼動の試験が `frame-loop.ts` の欠陥を 2 件見つけた。** ① **寸法が変わらない resize でもフレームを走らせていた** —— `FT-3` は「画面の寸法が変わったこと」であり、`NFR-010` は表 T-078 に無い契機で起こすことを禁じている。② **0×0 でも 1 枚描いていた** —— `BO-1` は「寸法が確定するまで 1 枚も描かない」を MUST で求める。⚠️ **プレビュー枠が 0×0 で冷間起動するのは実際に起きる**。⭐ 両方直し、**最初に寸法が確定した resize を `BO-5` として扱う**（`FT-3` ではない）|
| 2026-08-19 | 段 | ✅ **`UF-33` / `UF-32` / `UF-49` / `UF-48` は受入済** —— 別の者が仕様だけを読んで書いた試験が緑で、型検査と層の検査も緑である（21 ファイル / 741 件）。⚠️ **`UF-47` だけは 🔧 のまま** —— 取り込むだけで起動し、何も export しないので node の単体試験から届かない。**表 T-218 の Playwright 側で受けるのが箋である** |
| 2026-08-22 | 実測 | ⭐ **シェルが画面を組んだ。** `boot()` が `ScreenRenderer` ＋ `DomScreenSurface` を 表 T-077 の順で呼び、`appHeaderHeight` を実測するようにした（`FR-051`）。**実 DOM を自分で数えた**（1280×800）—— `data-role` 1 → 45、`data-icon` 0 → 74、`button` 0 → 78、`input` 0 → 1、日程は描けたまま、console error 0 |
| 2026-08-22 | 罠 | ⛔ **「窓を変えても SVG が動かない」を欠陥として報告しかけた。** ⚠️ **切り分けたら `document.hidden === true` で、`requestAnimationFrame` が 600ms 経っても 1 度も発火していなかった。** ⭐ `frameLoop` は `BO-5` の 1 枚目だけを同期で描き、以降は `ask()` → rAF を通る。**だから「最初の 1 枚のまま止まる」のは計測環境である。** ⛔ 切り分け方: 自分で rAF を 1 回仕掛けて発火を見る |
| 2026-08-22 | 発見 | ⛔ **実 DOM が本物の欠陥を出した —— 押しボタン 78 個のうち 21 個が 4 × 0 で押せない。** `Row Expander`（`IC-58` / `IC-59`）と `Row Pin`（`IC-60`）である。⚠️ **原因は `dom-screen-surface.ts` の `rowControlElement` が `<button>` に中身を 1 つも入れていないこと** —— 同じファイルの `commandEntry` は「語が空ならアイコン ID へ退く」道を持っており、あちらは 34 × 50 に育つ。⛔ **型検査も 2195 件の試験も 1 件も捕まえていない。** ⭐ `IC-60` は本日より前から在るので退行ではなく既存の穴である |
| 2026-08-22 | 決めた | ⛔ **`UF-47` の段は上げない。** ⚠️ `tests/unit/uf-47-48.test.ts` が駆動しているのは `frame-loop`（`UF-48`）だけで、**`boot()` は 1 度も呼ばれていない。** ⭐ **本日の結線を確かめたのは実 DOM だけである** —— 自動試験は 1 件も守っていない。表 T-218 の `TS-3` で受けるまで 🔧 のままとする |
| 2026-08-22 | 発見 | ⛔ **ズームの 3 値が `src/` のどこにも届いておらず、`FT-1` を丸ごと塞いでいた。** 表 T-206 の `S-96` / `S-97` / `S-98` は値を持たず 表 T-201 の `S-53` / `S-54` / `S-55` を**名指して**おり、⚠️ **生成器がその指し先を辿れなかった。** `InputContext.zoomStep` が作れないので `PI-18` のどの入口も呼べない。⭐ `tools/generate_entity_types.py` に `pointed_row()` を足して閉じた（`NOT_STORED_ZOOM_STEP` / `NOT_STORED_ZOOM_BOUNDS`）。**結線そのものは次回である** |
| 2026-08-22 | 直した | ⭐ **押せない操作子 21 個を直した** —— `rowControlElement` に中身（行 ID）を 1 つ入れた。⛔ **発明ではない** —— `PD-154` が既にこの穴を記録し「行 ID を `data-icon` に置き、図形は描かない」を推奨しており、同ファイルの `commandEntry` が同じ退き方をしている。`@provisional PD-154` を付けた。⭐ **実測: 4 × 0 → 41.7 × 24。`IF-9` の引きが 7 行 × 3 とも入口を答える** |
| 2026-08-22 | 発見 | ⛔ **表 T-051 の `HF-6` が満たされていない。試験を 1 件わざと赤で残した。** 「操作子は薄く描き、ポインタが乗っているあいだだけ濃くする」のに、⛔ **3 つとも `color:CanvasText`（全強度）で `opacity` も無く、様式の要素がツリーに 1 つも無いので、ポインタが乗ったことに応じる道がそもそも無い。** ⚠️ `FR-098` が「濃さは折り畳みの操作子と同じ」と引くので `Row Pin` も同じである。⭐ 同ユニットは別の場所で `GrayText` を「薄く」に使っており、語彙は既に在る |
| 2026-08-22 | 発見 | ⛔ **表 T-051 の `HF-4`（MUST）も満たされていない** —— 「行名の長さによらず `Row Title Panel` の右端に揃える」のに、⛔ **`IC-58` / `IC-59` は行名より前（x=4 / x=49.7）に描かれ、行の深さの余白の内側なので深さで横にずれる。** `IC-60` だけが x=166 で右端に揃っている。⚠️ **起動テンプレートは全行が深さ 1 なので今日は目に見えない** |
| 2026-08-22 | 罠 | ⛔ ⭐ **作り物が寸法を決めている試験は、寸法の欠陥を原理的に見つけられない。** `uf-72-screen-part.test.ts` の `laidOut` は試験が持つ表から箱を答えるので、⛔ **外接矩形の幅が正であることを試験に書いても、4 × 0 の操作子に緑を出す。** ⚠️ **2195 件が緑のまま 21 個が出荷された理由がこれである。** ⭐ 効いた手は「ユニットが書いた宣言から寸法の出どころを求める述語」を書き、中身の無い節点に当てて落ちることを見せること |
| 2026-08-22 | 発見 | ⛔ **「残るのは結線だけ」は半分しか正しくなかった。** ⭐ **`InputContext` の 12 メンバを 1 つずつ突き合わせたところ、出どころが無いものが 3 つ残っていた** —— `today`（`FR-046` は「その時点の本日」としか書いていない）・`editedBy`（`AT-128` の値域が無い）・`PointerSlop` の 2 値（`S-91` は散文、線の掴み代は行そのものが無い）。⚠️ **どれもコードで埋められる形をしていたので、測らなければ黙って選んでいた** |
| 2026-08-22 | 裁定 | ⭐ **利用者の裁定 3 件を `CR-206` / `CR-207` / `CR-208` として当てた。** ⛔ **どれも分類 `D` または表への追加であり、推奨で走ってはならないものであった**（規則 06）。**`FR-046` に「読む人の機のローカルの暦の日」（`CR-206` / `PD-166`）、表 T-229 に書き手が名乗る語（`CR-207` / `PD-165`。`ED-1` = `user`）、表 T-206 に `S-91` の数と `S-137`（`CR-208` / `PD-167` / `PD-168`）。** ⭐ **数の予測はどの回も的中した** —— `tables` 128 → 129、`rows` 1580 → 1585 |
| 2026-08-22 | 罠 | ⛔ **変更履歴に「表 T-023d の 4 行」と書いて検査 9 が落ちた** —— **「表 X の N 行」は「X は N 行を持つ」と読まれる。** ⭐ **行 ID を名指す形（`GR-5` / `GR-6` / `GR-13` / `GR-16`）に直して通した。** ⚠️ **技能書がこの罠を明記していたのに踏んだ** |
| 2026-08-22 | 決めた | ⛔ **`A` 分類の `@provisional` の印を生成ブロックに付けられない。** ⭐ **`PointerSlop` の宣言（手書きの側）に付けた** —— 値の意味を述べているのはそちらであり、生成ブロックは次の `npm run gen` で消える。⚠️ **原稿の 🔎 と `@provisional` は別の仕組みである** —— 前者は「由来が無い」、後者は「裁定を待っている」 |
| 2026-08-22 | 決めた | ⭐ **`FT-1` の結線を `frame-loop.ts` に置き、`single-html-shell.ts` は 2 文だけにした。** ⛔ **表 T-063 の `UT-6` がこの分け方の根拠である** —— 起動が縛られる理由（`FR-067` / `FR-065`）と、フレームを回す理由（`LY-5` / `ADR-001`）は別である。⭐ **`MK-10` の答えは工場の引数で往き、出来事は監視者で来る** —— `InputWatcher` が何も返さないので、答えは来た道を戻れない |
| 2026-08-22 | 決めた | ⛔ **順序が効いている。** ⭐ **押下で掴んだ press は、翻訳器が読んだ後・書き込みの前に落とす** —— 離しは press だけから決まり（`CS-2`）、かつ `WS-2` は身振りの最中の書き込みを拒む（`AG-9`）。**逆順にすると、身振りで作った書き込みが 1 件も通らない** |
| 2026-08-22 | 実測 | ⭐ **実 DOM で押して動くことを自分で確かめた**（詳細は「現在地」）。**`Ctrl+Shift+D` が基準日線を消して戻し、戻った絵が元と違う** —— テンプレートの基準日ではなく本日が入るからで、`CR-206` がそのまま効いている。**葉のバーは掴んで動く（x 441.72 → 522.01、図形 8 個が連動）** |
| 2026-08-22 | 罠 | ⛔ **隠れた文書では `ResizeObserver` も配送されない。** ⚠️ **rAF だけの話だと思っていた** —— 実測: プレビュー枠が 0×0 で冷間起動すると `BO-1` が正しく 1 枚目を止め、**復帰路である `ResizeObserver` が飢えて永久に白いままになる。** ⭐ **`data-role` が 45 ではなく 8 で止まるのがその形である。** ⛔ **アプリの欠陥ではない**。⚠️ **もう 1 つ: `ask()` は `owed` を立てたら次のフレームまで再要求しないので、rAF が 1 度も発火しない環境では最初の 1 件で詰まる** —— 差し替えは**読み込みの前**に入れないと効かない |
| 2026-08-22 | 発見 | ⚠️ **`S-95`（取り消しの合計サイズ）は原稿が MB で述べ、`historyWithStep` は UTF-8 バイトで比べている。** ⛔ **`frame-loop.ts` は換算せずに素通しし、`HISTORY_LIMITS` の注記でそう名乗った** —— **ここで係数を書けば、このファイルが他所の定める境界を決めることになる。** ⭐ **別の変更要求である** |
| 2026-08-22 | 直した | ⛔ **その読みが誤りだった。仕様が換算そのものを述べているので、欠けているものは何も無かった。** ⭐ **`S-95` の備考が「1 MB = 1024 × 1024 バイトとする」と書き、`FR-031` が「UTF-8 で符号化した長さ（バイト）で測ること（MUST）」と定める。** ⚠️ **上限が 64 バイトになり、1 段 655,969 バイトの起動テンプレートでは書き込みのたびに履歴が 1 段へ切り詰められていた**（`S-94` は 50 段を約束している）。⭐ **同じ形は `CR-173` が `S-113` で決着させており、`validate-imported-document.ts` が `BYTES_PER_MEGABYTE` を持つ。** ⛔ **`STOP` は「何が無いか」を書く道具であって、「調べていない」を書く道具ではない** |
| 2026-08-22 | 直した | ⛔ **`Esc` が身振りの段を消費していなかった**（表 T-028 の `IN-4`。MUST）。⚠️ **`hasEndedGesture` はポインタの `up` と `lost` にしか真にならず、打鍵が 1 度も届かなかった。** ⭐ **`screen-state.ts` 自身が「シェルが press を落とす」と書いていた。** ⛔ **起きていたこと 2 つ**: 掴む→動かす→`Esc`→離す で**中断したはずの移動が書き込まれる**。⛔ **1 度でもボタンが下りたら `escapeTarget` が永久に「身振り」を答え、`Esc` がブラウザへ二度と届かない**（`IN-4a` の MUST が破れ、`FR-071` が到達不能）。⭐ **`escapeTarget` は `PI-36` が公開しているので、シェルが問うのが正しい** |
| 2026-08-22 | 直した | ⛔ **素のポインタ移動でフレームを起こしていた**（`FR-048` の MUST NOT）。⭐ **同じ要求が判定の書き方まで定めている** ——「**判定はポインタではなく描く内容に置くこと**」。⚠️ **例外も定めている** —— ポインタが乗って濃さが変わる UI パーツは「描く内容が変わる」に当たり対象外。⭐ **移動のときだけ 4 条件で判定する形にした**（身振り中／追従するものが描かれている／選択・画面状態・文書が変わった／ポインタの下の `ScreenPart` が変わった）。⛔ **`ScreenPart` は `IF-9` に問う** —— 描いた側が答える（Chapter 5.3） |
| 2026-08-22 | 罠 | ⛔ ⭐ **前に立つ者の実 DOM 計測が誤り、穴を誤報しかけた**（`docs/review/ft1-wiring-findings-2026-08-22.md` の第 6 節）。⚠️ **`ask()` の詰まりを避けるためモジュールを再取り込みしたら、`boot()` が 2 つ目の実体を起こし、両方が `window` の出来事を受けていた。** ⛔ **指紋を document 全体から取っていたので、複数実体の絵を連結して読んでいた。** ⭐ **測る前に `[data-role="Schedule Canvas"]` が 1 つであることを確かめ、指紋は実体の根の中だけから取ること。** ⚠️ **ポリゴンの指紋は狭い** —— 648 タスクが配置されても SVG に出る `polygon` は 71 個で、**「絵が動かない」は「文書が動かない」の証拠にならない** |
| 2026-08-22 | 発見 | ⛔ **測っている形が「保存形」ではない。** `FR-031` は「その段の**保存形**を UTF-8 で符号化した長さ（バイト）で測ること（MUST）」と定めるのに、⚠️ **`stepSize` は `JSON.stringify(document)`（詰めた形）で測っており、保存形は `JSON.stringify(document, null, 1) + '\n'` である。** ⛔ **同じ文書で 655,969 と 895,286 —— 36% 違い、合計メモリの予算が仕様より緩い。** ⭐ **単純に直せない** —— 保存形を作るのは `DocumentCodec`（`Adapter`）で測る側は `UseCase` であり、`LR-1` が外向きの依存を禁じている。⚠️ **いまは効かない**（この文書なら 50 段で 32.8MB、`S-95` の 64MB に届かない）|
| 2026-08-22 | 発見 | ⛔ **取り消しの「段数」を止めている試験が 1 件も無かった。** ⚠️ **いちばん近い 3 本はどれも 64 バイト上限のバグを素通しする** —— `uf-20-21` は履歴を手で組んで段の大きさを `1` と決め打ち、`uf-27-28-29` は自分で `maxSteps: 1` を渡し、`document-model` は実文書のバイト数を 1 度も見ない。⛔ **`carryOutAction` の取り消し・やり直しの枝は試験 0 件である。** ⭐ **2296 件が緑のまま CRITICAL が出荷されかけた理由がこれである** |
| 2026-08-22 | 発見 | ⛔ **表 T-064 が名指すメンバの実装を見る機械検査が無く、未実装のメンバが 2 件在った。** ⭐ **検査 26b を新設した**（97 メンバを読み、9 欄は「名前が書かれていない」と申告して数を刷る）。⛔ **`PI-1` の `scheduleViolations`** —— 表 T-220 の 17 個の不変条件に実装が 1 つも無い。⛔ **`PI-8` の `replaceDocument`** —— `CR-196` が名前を開け、刻印が未決（分類 `D`）だから保留していたが、⭐ **`CR-205` がその刻印を裁定済にしたので、いまは書ける。** ⚠️ **どちらも本回の結線とは無関係の既存の穴である** |
| 2026-08-22 | 決めた | ⛔ **検査 26b を赤のまま置かず、baseline を与えた。** ⭐ **弱めたのではない** —— 規則 05 の 1. が「止まるときは緑で止まる」と定め、**永久に赤い検査は走り全体を無視させる**（README の「noisy gate は無い gate より悪い」）。⭐ **この repo は既にその作法を持っていた** —— 重複検出器が `duplication-baseline.txt` で `A=26 (new 0)` と刷る形である。⛔ **baseline に無い穴が出たら赤／baseline が実物と合わなくなったら赤**（払った負債が居座ると baseline が腐る）|
