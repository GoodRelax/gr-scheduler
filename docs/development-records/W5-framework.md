# W5 `Framework` —— 開発の記録

**規則は [`docs/development-rules/05-working-method.md`](../development-rules/05-working-method.md)。書式は [README](README.md)。**

**対象**: `src/framework`   ⭐ **機械検査がこの宣言を読み、状態表と実物を突き合わせる。**

---

## 現在地

⭐ **2026-08-23（第 4 巡）: 試験の段を合流し、裁定 9 件のうち 8 件を仕様へ当てた。⛔ 1 件は図形の裁定待ちで差し戻した。**

### ⭐ 合流点の実測（⛔ 体の報告ではなく、前に立つ者が自分で走らせた）

```
precheck   OK（生成物 2 件の告知だけ。gen:check が 12/12 で答える）
typecheck  0 エラー          layers     OK
gen:check  12 / 12 OK        check.sh   ALL GREEN
仕様        版 0.97 ／ tables 132 ／ figures 11 ／ rows 1610 ／ uids 148
test       3310 通過 / 13 赤   e2e  15 通過 / 2 赤
build      dist/index.html 1 枚 858.74 kB
```

⛔ **赤 15 件はすべて「名前と居場所のある欠陥」である。増えた 3 件も退行ではない:**

| 赤 | 何を言っているか |
|---|---|
| **7 件**（`if-3-file-store`）| ⛔ **本物の製品欠陥 2 つ。** ①`file-system-access-file-store.ts` が `askToWriteOver` を 1 度も呼ばず、`DI-4` の MUST が果たされていない ②3 つ落としても `ignoredFileCount` が 0 で、`OP-11` の「残りを無視したことを告げる（MUST）」が果たされていない |
| **3 件**（`IV-1` / `IV-2` / `IV-16`）| ⛔ **原稿に元データが無いので答えられない。** 別の巡へ回した（下記）|
| **3 件**（`IV-18`）| ⭐ **退行ではない。** 表 T-220 を駆動する契約試験に、**本巡で足した行**の実装がまだ無いことを言っている |
| **2 件**（`SWS-6`）| ⛔ 書き出しの入口が面を開くが**選ぶものを運んでいない**ので、MSPDI を 1 つも作れない |

### ⭐ 試験の段が本物を掘り当てた（**72 ケース**）

⛔ **`if-3` の体は、欠陥が製品か試験台かを自分で切り分けた** —— 製品を一時的に直すと 26/26 が通り、
**戻して `cmp` でバイト一致を確かめてから報告した**（規則 04 の 2. そのもの）。⭐ **鵜呑みにしてよい形の報告である。**

⭐ **AM-13 の赤 2 件が消えた** —— 試験体が `exportScene` を必須にし、試験台に渡す形へ直した。
⚠️ **実装した体は「約 100 件が一度に赤くなる」として避けた判断であり、体どうしで割れていた。**
⭐ **合流して走らせたら約 100 件は赤くならず、試験体の判断が正しかった** —— 3262 → 3310 通過。

### ⛔ CR-220 を差し戻した（**図形は利用者の裁定である**）

⭐ **`OP-3` の 3 択を問う面は、表 T-103 に `Open Chooser`、表 T-109 に入口 3 つで当たるはずだった。**
⛔ **`generate_icon_glyphs.py` が拒んだ:**

```
table T-109 holds 3 row(s) that fig-icons.svg draws no shape for (IC-71, IC-72, IC-73),
and the glossary says every row has one
```

⚠️ **表 T-109 は「全行が図形を持つ」ことを前提に組まれており、表 T-026 の `RC-13` が
「⛔ 本仕様書の中で発明しない —— 図形の決定は利用者の裁定である」と定める。**
⭐ **発明せずに全部差し戻した**（`U-56`・`IC-71`〜`IC-73`・`RC-13` の追記・前文の 3 か所）。

⛔ **開く経路はまだ死んでいる。** ⭐ **次に要るのは面ではなく、3 つの図形の裁定である。**

### ⚠️ 別の巡へ回したもの

⛔ **`IV-1` / `IV-2` / `IV-16` を答えさせる原稿の形の変更** —— `erd.json` の関係に外部キーの列名、
`settings.json` の ε を `exclusive` へ。⚠️ **生成器と `SETTINGS_BOUNDS` とその読み手を引き連れる**ので、
仕様だけの巡には収まらない。⭐ **`IV-18` だけを当てた。**

### ⭐ 本巡で直した道具の欠陥 2 つ

| 何が壊れていたか | どう直したか |
|---|---|
| ⛔ `tools/precheck.py` が「58 件の罠」と報告 —— 実体は**走っている最中のワークフロー自身の作業木**だった。⚠️ `.claude/worktrees/` が `.gitignore` に無く、コミット事故の危険もあった | `.gitignore` に規則を足した |
| ⛔ 同じく、**ファイル末尾の改行を「空行 2 連続」と誤検知**していた | 末尾だけ除外した。⭐ **わざと空行を 2 つ入れて赤くなることを確かめ、戻して `cmp` でバイト一致を見た** |

### ⭐ `precheck` が私自身の誤りを 6 件捕まえた

```
CR-221 / CR-222 / CR-223 / CR-225   CH- と GL- の対応違い（表 T-054 と突き合わせて捕捉）
tbl-glossary.md                     `CR-172` をバッククォート付きで書いた
A-appendix.md                       `CR-210` をバッククォート付きで書いた
```

⚠️ **`GL-007` は表 T-054 のどの課題も受けていない番号だった。** ⭐ **番号だけ差し替えず、課題そのものを選び直した。**

### ⚠️ 自分の変更が前文を 3 か所偽にした（**規則 02 の 4. そのもの**）

`IC-71`〜`IC-73` を足した時点で、表 T-109 の前文の「70 行ある」「70 個の確定名」
「**図形を持たない行は無い**」が同時に偽になった。⭐ **差し戻しで元へ戻したが、
「表 X の N 行」と書くと数が連鎖することの実例である。**

### ⛔ 次の一手

```
1. コミットする（利用者が行う）
2. 図形 3 つの裁定を受ける → CR-220 を当てる → 開く経路が生きる
3. IV-18 の実装 ／ askToWriteOver ／ ignoredFileCount ／ 書き出しの入口
   ⭐ どれも赤い試験が居場所を名指している
4. erd.json の関係と settings.json の ε（生成器を引き連れる巡）
```

---
⭐ **2026-08-23（第 3 巡）: ワークフローで 4 継ぎ目を実装し、合流点で 1 回検証した。⛔ 試験の段はまだ走らせていない。**

### ⭐ 合流点の実測（⛔ 体の報告ではなく、前に立つ者が自分で走らせた）

```
precheck   OK（13 ファイル）        typecheck  0 エラー
test       3262 通過 / 2 赤        layers     OK
gen:check  12 / 12 OK              check.sh   ALL GREEN
build      dist/index.html 1 枚 858.75 kB
e2e        5 → 13 件、全部通過（tests/system の 8 件が入った）
```

⭐ **検査 26b の既知の欠が 0 件になった** —— `PI-1` `scheduleViolations` の負債を
`published-members-baseline.txt` から落とした。**あの台帳は空になった。**

⛔ **赤 2 件は増えても減ってもいない**（`uf-27-28-29` の AM-13）。⭐ **原因は実装ではなく試験台である** ——
`snapshotOf` が `exportScene` を渡さないので AM-13 が寸法の違う枝に落ちる。⚠️ **試験台を直すのは試験の段の仕事であり、
実装した体にやらせてはならない**（規則 04 の 1.）。

### ⛔ ワークフローの台本に欠陥が 2 つあった（**前の巡は書いただけで走らせていない**）

| 何が起きたか | どう直したか |
|---|---|
| ⛔ `meta.whenToUse` が文字列連結で、読み込みが `meta must be a pure literal` で拒否した | リテラル 1 本に畳んだ |
| ⛔ `pipeline` の第 2 段（試験体）も `isolation: 'worktree'` なので、**実装が存在しない木で試験を書く**ことになっていた。台本自身が命じる「壊して赤くなることを見よ」（規則 04 の 2.）が実行できない | `args.stage` の門を足し、**実装 → 合流点で検証 → 試験**の 2 回に割った。⭐ 規則 05 の「合流点で 1 回」とも一致する |

### ⚠️ worktree は HEAD から切られる（**次も刺さる**）

⛔ **未コミットの作業は体から見えない。** 走らせる前に測ったら:

```
frame-loop.ts    HEAD 1248 行 / 作業木 1573 行 —— 325 行が見えない
tools/precheck.py 未追跡。worktree に来ない
```

⭐ **だから巡の頭でコミットしてもらってから走らせる。** ⚠️ **それでも 4 体目は `main`(30ddd78) から切られた** ——
1〜3 体目は自分で `restart` へ早送りしたが、4 体目はしなかった。⭐ **その成果は
「自分の HEAD との差分」として回収できた**（`playwright.config.ts` は `restart` の本文＋注記の差だけであることを確かめた）。

⛔ **`src/framework/single-html-shell/**` を 2 体に持たせたのは台本の誤りである**（規則 05 の 3.「同じファイルを 2 体で書かない」）。
⭐ **衝突は 1 か所だけで済んだ** —— `frame-loop.ts` の import 位置。両側とも足すだけだったので両方を残した。

### ⛔ 体が「無い」と言って止まったもの（**推測しなかった。ここが次の仕事である**）

| 継ぎ目 | 何で止まったか |
|---|---|
| **`FileGateway`** | ⭐ **保存の半分は通った**（`SK-11`/`IC-2` → `CS-4` の作法 → `FR-096`/`FR-060`）。⛔ **開く半分は `OP-3` で止まっている** —— 3 択（`replace`/`merge`/`baseline`）を問う面が 表 T-103 に無く、表 T-109 は自ら網羅と名乗るので入口を発明できない |
| **`scheduleViolations`** | ⭐ **17 個のうち 11 個が答える。** ⛔ `IV-1`/`IV-2`/`IV-16` は**生成物に元データが無い** —— `erd.json` は列の `key`(PK/FK) を持つのに生成器が出さず、`relations` は外部キーの列名を持たず、`settings.json` の式の境界は散文である |
| **`AM-13`** | ⭐ **絵は出るようになった**（`ExportScene` を `IF-7` で運ぶ）。⛔ 赤 2 件は試験台。⚠️ `exportScene` は**任意のまま**にしてある —— 必須にすると約 100 件が一度に赤くなる |
| **`tests/system`** | ⭐ **閉じた。8 件が緑。** ⛔ `SWS-6`（MSPDI 2 文書の突き合わせ）は**製品側が無い**ので ケースを書けない。表 T-231 の `NS-1`/`NS-2`/`NS-6` も同じ理由で判定できない |

### ⛔ 次の一手

```
1. コミットする（利用者が行う）        ⛔ しないと試験体が HEAD を見て、また空の木で書く
2. Workflow({ scriptPath, args: { stage: 'test' } })
3. 合流点でもう 1 回検証する
```

---
⭐ **2026-08-23（第 2 巡）: 成果物が 1 枚になり、裁定が 14 件下り、仕様へ 4 本当て終えた。**

### ⭐ ① 1 ファイル化 —— 閉じた

`vite.config.ts` に 1 枚へ畳む結線を書いた（第三者の差し込みは使わない。表 T-003 の `CN-7`）。
⛔ **`NFR-004` の「ファイルを直接開いた状態でも判定すること（MUST）」を実際に果たした** ——
⭐ **`chromium.launch({ channel: 'msedge' })` は端末に入っている Edge をそのまま使うので、browser の取り寄せが要らない。**
表 T-003 の `CN-2` が Chromium 系を基準と定めているので、これは仕様内の判定環境である。

```
dist/index.html   1 ファイルのみ ／ src= href= は 0 件 ／ fetch( は 1 → 0
CSP               default-src 'none'; img-src data:; style-src 'unsafe-inline';
                  script-src 'sha256-...'; base-uri 'none'; form-action 'none'
file:// で実測    Schedule Canvas 1 ／ data-role 45 ／ SVG 347 ／ 外へ出た要求 0 ／ console error 0
tests/nfr         5 件（TS-4。表 T-218 が Playwright と定める置き場を開けた）
```

⚠️ **記憶にあった「CSP ハッシュ NUL」の罠が本当に再来した** —— 編集の途中で `vite.config.ts` に NUL が 1 個入り、
気づいて ASCII のエスケープへ直した。⭐ **ビルド時に NUL を見つけたら落とす番人を入れてある。**

### ⭐ ② 裁定 14 件（記録は `docs/review/rulings-2026-08-23/RULINGS.md`）

⛔ **問う前に読み切ったら、8 件のうち 2 件は裁定が要らなかった。**

```
PD-171  既に 3 重に決着済み（FR-055 の STATEMENT ／ BO-3 ／ CR-160 の裁定）
        式は前プロジェクトの確定文書（03-ui-naming §6-7、確定 2026-08-02）が持っていた
PD-174  前プロジェクトが 6 案を比較して確定していた（地球儀＋小文字の言語コード）
```

⚠️ **裁定待ちの一覧に「未裁定」と書いてあることは、未決であることの証明ではない。**

### ⭐ ③ 仕様へ当てた 4 本（⛔ すべて 1 体で。規則 05 の 6.）

```
CR-215  表 T-231 ＋ SWS-7   SVG の正規化と丸めの許容差、WY-2 の相手   予測 4/4 的中
CR-216  表 T-232 ＋ SWS-8   CN-8 の内容セキュリティ方針の全数         予測 4/4 的中
CR-217  IF-7 を広げ CM-72 を足す   押下がどの行で始まったか ／ 全体表示を 2 書き込みへ
CR-218  FR-032 に「語で示す」    ＋ 古いセル 2 つを掃除
版 0.91 → 0.95 ／ tables 130 → 132 ／ rows 1596 → 1609 ／ uids 146 → 148 ／ 表示語 176 → 177
```

⚠️ **`CR-217` で閉路の予測を外した** —— 0 と見込んだが `FR-055` ↔ `HF-8` が 1 つ在った。
⭐ **編集計画がその 2 つのどちらにも触れないので 1 つずつ書けた。** ⛔ **測らずに書いていたら気づかなかった。**

### ⛔ 本巡で踏んだ罠（**次も刺さる**）

```
⛔ 変更履歴に `CR-202` とバッククォート付きで書くと検査 7 が落ちる（引継書が警告していたそのもの）
⛔ 生成物を直接編集しかけた —— tbl-settings.md は _source/settings.json が原稿である
   ⭐ npm run gen:settings:check が DRIFTED で教えてくれた。原稿へ入れ直した
⛔ StrictDoc は空行が 2 つ続くと落ちる。挿入した塊の前後を必ず見る
   ⚠️ 並行実行だと「process pool が異常終了」としか言わないので --no-parallelization で理由を出す
⛔ サブエージェントが rm -rf scratch を実行した。scratch/ は共有である。名指しで消させること
```

### ⭐ 効いたこと

⛔ **`tests` を型検査に入れたのが本巡いちばんの収穫である**（実測 60 件 / 24 ファイル）。
**ただ乗りしていた試験が本当に出た:**

```
uf-32.test.ts:645   Element を string の引数へ渡しており、"[object Object]" が何にも一致せず
                    null と色を比べて「合格」していた。FR-019 は 1 度も検査されていなかった
uf-41-42.test.ts    structuredClone が通っていたのは、試験台に関数メンバが欠けていたからである
uf-27-28-29.test.ts hasMovedSchedule を結線し直したが、⛔ **どの試験もその真偽を区別できない**
                    —— AG-6 のその半分には試験が 1 件も無い（次に書くべきはこれである）
uf-51.test.ts       askToWriteOver を実装が 1 度も呼ばないことを、どの試験も捕まえていない
tooltips.ts:229     「到達できない」と書いた注記が偽になった（RowTitle.wholeLabel が今は在る）
```


⭐ **2026-08-22: `FT-1` が結線され、押すと動くようになった。**

`boot()` が `ScreenRenderer` と `DomScreenSurface` を 表 T-077 の順で呼び、`appHeaderHeight` を
`FR-051` のとおり実測する形は前回のままである。**今回足したのは、`DomInputSource`（`PI-27`）を起こして
`IF-2` の出来事を `PI-18` の 3 メンバへ渡す道である。**

`FrameLoop` に 2 つ増えた（どちらも `non-pure`）:

```
isBrowserDefaultStopped(input)   MK-10 の答え。⛔ 監視者が聞く前に問われる。何も変えない
receiveInput(input)              FT-1。文脈を 1 つ組み、3 メンバへ渡し、書き込み、フレームを要る
```

### ⭐ 実 DOM で自分で確かめた（⛔ サブの報告ではない）

⚠️ **隠れた文書では `requestAnimationFrame` も `ResizeObserver` も配送されない**ので、
測定側で `requestAnimationFrame` を差し替えてから測った。**アプリのコードは 1 行も変えていない。**

```
MK-10 の両側     Ctrl+A / Ctrl+Shift+D / Ctrl+Z / F1 → 止める
                 素の Q / Ctrl+Shift+Q               → 止めない
IN-4 / IN-4a     F1 → Esc（止める）→ Esc（止めない）→ F1 → Esc（止める）→ Esc（止めない）
                 ⭐ 段を 1 つずつ食う。**ループが screenState を出来事の間で保っている証拠である**
FT-1 の約束      ホイール 4 件 → フレーム 4 回。押下・移動・離し 3 件 → 3 回
ホイール         Ctrl / Shift / Alt でバーの右端が 954.13 → 954.23 → 957.19 → 954.32 と動く
書き込み（T-067）Ctrl+Shift+D で line 6 → 5、もう一度で 5 → 6
                 ⭐ 戻った絵は元と同じではない（circle 50 → 49 のまま）——
                    **テンプレートの基準日ではなく本日が入ったからである。`FR-046` が効いている**
身振り           葉のバーを掴んで 80px 右へ → x 441.72 → 522.01。図形 8 個が連動
                 ⚠️ 最上位の要約バーを掴んでも動かない。**欠陥ではない**（あれは巻き上げである）
console error    0
```

### ⛔ 自動試験はまだ 1 件もこの道を守っていない（**正直に書く**）

⚠️ **`tests/unit/uf-47-48.test.ts` は `receiveInput` も `isBrowserDefaultStopped` も 1 度も呼ばない。**
⛔ **だから `UF-48` の段を `🔧 実装済` へ戻した** —— 規則 05 の 4. は「別の者が仕様だけを読んで書いた試験が緑」を
`✅ 受入済` の条件としており、増えた 2 メンバはその条件を満たしていない。
⛔ **`UF-47` は前回同様 🔧 のままである** —— `boot()` は取り込むだけで起動するので node の単体試験から届かない。
**表 T-218 の `TS-3`（Playwright）で受けるまで上がらない。**

### ✅ 仕様だけを読んだ試験が入り、本物を 3 件出した

`tests/unit/uf-48-input.test.ts`（42 件）。⭐ **原稿を読み込み時に引くので、行の文言が変わればここが落ちる。**
⛔ **赤で出た 3 件はすべて本物の欠陥であった**（`FR-048` ／ `IN-4` ／ `IN-4a`）。**直したのはコードであり、試験ではない。**

⚠️ **`UF-48` の段はまだ `🔧` である** —— **増えた 2 メンバは緑になったが、`carryOutAction` の取り消し・やり直しの枝が試験 0 件である。**

### ✅ 2026-08-23: 裁定 16 問は仕様も実装も当て終えた

```
仕様   CR-209 表 T-230 ／ CR-210 CS-4 と CA-3 ／ CR-211 OP-12・SK-11・U-55・IC-69/70 ／
       CR-212 詰めた GRS JSON ／ CR-213 formatFromFile を PI-20 へ ／ CR-214 判別の値を列へ
実装   replaceDocument（PI-8。取り消し・やり直しが第 2 の入口を通らなくなった）
       表示語を配る道（裁定 06）／ formatFromFile と交換形式の生成器（裁定 06 の値）
       Confirmation を描き IC-69 / IC-70 を押せるように
       ⭐ アイコンを図形で描く生成器（FR-029。画面は "IC-1" という文字を刷っていた）
       ⭐ 画面からの書き込みが監視者へ届くように（AG-6 / WS-7）
       ⭐ WS-2 が押下中の書き込みを拒むように（AG-9）
改名   maxTotalSizeBytes / sizeBytes / stepSizeBytes（裁定 04）
試験   2361 → 3263 件（緑 3259）。赤 4 件は既知の AM-13 2 件 ＋ PD-169 の 2 件
検査   26 本 ALL GREEN ／ audit-ch5 PASS ／ 描画 PASS ／ 26b の負債 2 → 1
```

⛔ **次に来る者へ**: 赤 4 件は**直し忘れではない**。`AM-13` は `IF-7` を広げる別件、
`PD-169` は「押下がどの行で始まったかを問う道」が公開されていないという裁定待ちである。
⭐ **どちらも仕様だけを読んだ試験が、仕様の文を引用したまま赤で立っている。**

### （前回）2026-08-23: 裁定 16 問のうち、仕様へ当てる 4 本を当て終えた

```
CR-209  01 の 7 問   表 T-230 を新設（RD-1 〜 RD-6）。閉路 5 つのうち 3 つを 1 度に書いた
CR-210  02 の 2 問   表 T-066 に CS-4。CA-3 を同じ回で直した。表 T-078 は 1 行も動かない
CR-211  03 の 5 問   OP-12（判別）・拡張子・SK-11 は常に GRS JSON・U-55・IC-69 / IC-70
CR-212  05 の 1 文   FR-031 の「保存形」を「詰めた GRS JSON」と明示。コードは動かさない
⛔ 残るのは 04（改名と試験）と 06（表示語を配る道）—— どちらも仕様を動かさない
```

⭐ **版 0.85 → 0.89。** tables 129 → **130** ／ rows 1585 → **1596** ／ figures 11 ／ uids 146。
⚠️ **重複検出は `A=26` → `A=28`** —— 足した 2 群はどちらも同じ表の兄弟行である（記録を見よ）。

**次の一手**:

```
0. ⭐ 裁定 04 —— maxTotalSize / size / stepSize を Bytes 付きへ改名し、シェルの換算を押さえる試験
     ⚠️ tsc は tests/ を見ないので、改名後は必ず npm test を走らせる
0b. ⭐ 裁定 06 —— 表示語を配る道を通す（表 T-075 の UF-60）＋ CR-194 の受け入れ試験
     ⭐「原稿の語を 1 つ変えると落ちる試験」。⛔ 語を問うのはその後
1. ⭐ tests/usecase（TS-1）—— 結線が通ったので書けるようになった
2. ⛔ replaceDocument（PI-8）を書く —— CR-209 が 表 T-230 を置いたので、いま全部書ける
     ⭐ 呼び手 6 つ（RD-1 〜 RD-6）ごとの履歴・刻印・取り消しの 1 段が仕様に在る
     ⭐ これが取り消し・やり直しの「第 2 の入口」（MS-1 が禁じる形）を閉じ、検査 26b の PI-8 の負債も閉じる
     ⚠️ シェル側の同名メンバ FrameLoop.replaceDocument は意味が違う。改名が要る
3. ⛔ scheduleViolations（PI-1）—— 表 T-220 の 17 個の不変条件に実装が 1 つも無い
     ⚠️ 検査 26b の baseline に負債として立っている
4. AM-13 の書き出しの絵（IF-7 を広げ、パネルを閉じた 1 枚をシェルが組む）
     ⚠️ 赤 2 件はこれである
5. ⚠️ HistoryLimits.maxTotalSize の名前に単位が無い（R2.1 は MUST）—— 公開署名なので CR
```

---

## 状態表

⭐ **段**: ⬜ 未着手 → 🔧 実装済 → 🧪 試験済 → ✅ 受入済
⛔ **✅ になるまで、他のユニットがそれに依存してはならない**（05 の 4.）。
⭐ **例外は型だけ** —— 公開エントリの型が型検査を通れば、依存側は書き始めてよい（05 の 3.）。

### `browser-clipboard` —— BrowserClipboard（PI-30）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-53 | `browser-clipboard.ts` | **公開エントリ** | non-pure | PI-30 | ✅ 受入済 |

### `canvas-rasterizer` —— CanvasRasterizer（PI-31）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-54 | `canvas-rasterizer.ts` | **公開エントリ** | semi-pure-b | PI-31 | ✅ 受入済 |

### `dom-input-source` —— DomInputSource（PI-27）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-50 | `dom-input-source.ts` | **公開エントリ** | non-pure | PI-27 | ✅ 受入済 |

### `dom-screen-surface` —— DomScreenSurface（PI-38）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-71 | `dom-screen-surface.ts` | **公開エントリ** | non-pure | PI-38 | ✅ 受入済 |

### `dom-svg-surface` —— DomSvgSurface（PI-26）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-49 | `dom-svg-surface.ts` | **公開エントリ** | non-pure | PI-26 | ✅ 受入済 |

### `file-system-access-file-store` —— FileSystemAccessFileStore（PI-28）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-51 | `file-system-access-file-store.ts` | **公開エントリ** | semi-pure-b ／ non-pure | PI-28 | ✅ 受入済 |

### `local-storage-document-store` —— LocalStorageDocumentStore（PI-29）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-52 | `local-storage-document-store.ts` | **公開エントリ** | semi-pure-b ／ non-pure | PI-29 | ✅ 受入済 |

### `single-html-shell` —— SingleHtmlShell（PI-25）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-47 | `single-html-shell.ts` | **公開エントリ** | non-pure | PI-25 | 🔧 実装済 |
| UF-48 | `frame-loop.ts` | 内部 | non-pure |  | 🔧 実装済 |

---

## 記録

⭐ **追記のみ。過去の行を書き換えない。**

| 日付 | 種別 | 内容 |
| --- | --- | --- |
| 2026-08-19 | 先行 | ⭐ **W4 の縦線のために `UF-47` / `UF-48` / `UF-49` を先に書いた**（規則 05 の 2. の「次の波へ先に手を出してよい」）。⛔ **触ったのは 8 コンポーネント中 2 つ・9 ファイル中 3 ファイルだけである** |
| 2026-08-19 | 決めた | ⭐ **`SvgSurface` は SVG を**文字列**で受ける**（`showSvg(svg: string): void`）—— `SvgRenderer` は `pure`（表 T-075 の `UF-32`）なので、継ぎ目を越えるものは値でなければならない。⚠️ **差分ではなく画面ぜんたいを渡す** —— 表 T-078 が frame の回数を既に絞っており、差分の規則はどの要求も持たない |
| 2026-08-19 | 決めた | ⭐ **`frame-loop.ts` は frame を「予約」する** —— 契機が 2 つ同じ tick に来ても 表 T-068 が 2 度走らないようにするため。⚠️ **最初の 1 枚だけは予約せず直に描く**（表 T-078 の注が「最初の 1 枚は `BO-5` が起こす」と定める）|
| 2026-08-19 | 決めた | ⚠️ **`appHeaderHeight` は 0 で走らせている** —— `FR-051` は実測を求めるが、測る側（`ScreenRenderer` と `DomScreenSurface`）はまだ無い。⭐ **0 は仮置きではなく、いま画面にヘッダーが無いという事実である。** 第 2 歩で実測へ差し替える |
| 2026-08-19 | 決めた | ⚠️ **テンプレートの `Document` への変換は型の言い切りで通している** —— ⛔ **`DocumentCodec` の `documentFromJson`（`UF-35`）がまだ stub だからである。** ⭐ **型も JSON も同じ `erd.json` から生成されているので、食い違えばそれは生成器の欠陥である。** `UF-35` ができたら解析と検証に置き換える |
| 2026-08-19 | 罠 | ⛔ **層検査が `.json` の取り込みを「ファイルではない」と拒んだ** —— 取り込み先を必ず `.ts` に直して探していた。⭐ **`.json` は表 T-075 のユニットではなく（検査 18 も `.ts` だけを数える）、コンポーネントへの手出しでもない**ので、存在だけ確かめて通す形に直した |
| 2026-08-19 | 罠 | ⛔ ⭐ **実 DOM が欠陥を 2 件出した。型検査も 684 件の試験も 1 つも捕まえなかった。** ⚠️ **①** `BO-3` の `OP-10` の分岐を実装しておらず、`scrollDate` の既定が `null` なので**時間軸の原点が 1970-01-01 になり、2026 年のタスクが `x ≈ 124139` に落ちていた**（SVG の幅は 6801）。⭐ `frame-loop.ts` に `viewSettings` を置いて閉じた —— **文書は変えない**（`OP-10` は読む側の規則）。⚠️ **②** 絵の寸法に `layout.contentWidth` を流用しており、座標は画面空間なので**図形が枠の外に出ていた**（`maxX 978 > 908`）。⭐ **`FR-080` は「`GRS` が占める画面の全体」を求める**ので、CR-189 で辺を 1 本足して画面の矩形を渡す形にした |
| 2026-08-19 | 実測 | ⭐ **縦線が通った。** SVG は viewport（1280×800）と一致し、図形は枠内。`aria-label` に文書名。⚠️ **全体表示では polygon が 7 個しか出ない** —— **欠陥ではなく `HR-1a`** （100 行を 1 画面へ収めると LOD が深さ 1 まで畳み、バーを直に持つ最上位の行が `Total System` だけ）。⭐ **テンプレートの形の問題である** —— 対象ごとの最上位の行にも要約のバーを置くと `VK-3` が動いて見える |
| 2026-08-19 | 発見 | ⛔ **仕様騼動の試験が `frame-loop.ts` の欠陥を 2 件見つけた。** ① **寸法が変わらない resize でもフレームを走らせていた** —— `FT-3` は「画面の寸法が変わったこと」であり、`NFR-010` は表 T-078 に無い契機で起こすことを禁じている。② **0×0 でも 1 枚描いていた** —— `BO-1` は「寸法が確定するまで 1 枚も描かない」を MUST で求める。⚠️ **プレビュー枠が 0×0 で冷間起動するのは実際に起きる**。⭐ 両方直し、**最初に寸法が確定した resize を `BO-5` として扱う**（`FT-3` ではない）|
| 2026-08-19 | 段 | ✅ **`UF-33` / `UF-32` / `UF-49` / `UF-48` は受入済** —— 別の者が仕様だけを読んで書いた試験が緑で、型検査と層の検査も緑である（21 ファイル / 741 件）。⚠️ **`UF-47` だけは 🔧 のまま** —— 取り込むだけで起動し、何も export しないので node の単体試験から届かない。**表 T-218 の Playwright 側で受けるのが箋である** |
| 2026-08-22 | 実測 | ⭐ **シェルが画面を組んだ。** `boot()` が `ScreenRenderer` ＋ `DomScreenSurface` を 表 T-077 の順で呼び、`appHeaderHeight` を実測するようにした（`FR-051`）。**実 DOM を自分で数えた**（1280×800）—— `data-role` 1 → 45、`data-icon` 0 → 74、`button` 0 → 78、`input` 0 → 1、日程は描けたまま、console error 0 |
| 2026-08-22 | 罠 | ⛔ **「窓を変えても SVG が動かない」を欠陥として報告しかけた。** ⚠️ **切り分けたら `document.hidden === true` で、`requestAnimationFrame` が 600ms 経っても 1 度も発火していなかった。** ⭐ `frameLoop` は `BO-5` の 1 枚目だけを同期で描き、以降は `ask()` → rAF を通る。**だから「最初の 1 枚のまま止まる」のは計測環境である。** ⛔ 切り分け方: 自分で rAF を 1 回仕掛けて発火を見る |
| 2026-08-22 | 発見 | ⛔ **実 DOM が本物の欠陥を出した —— 押しボタン 78 個のうち 21 個が 4 × 0 で押せない。** `Row Expander`（`IC-58` / `IC-59`）と `Row Pin`（`IC-60`）である。⚠️ **原因は `dom-screen-surface.ts` の `rowControlElement` が `<button>` に中身を 1 つも入れていないこと** —— 同じファイルの `commandEntry` は「語が空ならアイコン ID へ退く」道を持っており、あちらは 34 × 50 に育つ。⛔ **型検査も 2195 件の試験も 1 件も捕まえていない。** ⭐ `IC-60` は本日より前から在るので退行ではなく既存の穴である |
| 2026-08-22 | 決めた | ⛔ **`UF-47` の段は上げない。** ⚠️ `tests/unit/uf-47-48.test.ts` が駆動しているのは `frame-loop`（`UF-48`）だけで、**`boot()` は 1 度も呼ばれていない。** ⭐ **本日の結線を確かめたのは実 DOM だけである** —— 自動試験は 1 件も守っていない。表 T-218 の `TS-3` で受けるまで 🔧 のままとする |
| 2026-08-22 | 発見 | ⛔ **ズームの 3 値が `src/` のどこにも届いておらず、`FT-1` を丸ごと塞いでいた。** 表 T-206 の `S-96` / `S-97` / `S-98` は値を持たず 表 T-201 の `S-53` / `S-54` / `S-55` を**名指して**おり、⚠️ **生成器がその指し先を辿れなかった。** `InputContext.zoomStep` が作れないので `PI-18` のどの入口も呼べない。⭐ `tools/generate_entity_types.py` に `pointed_row()` を足して閉じた（`NOT_STORED_ZOOM_STEP` / `NOT_STORED_ZOOM_BOUNDS`）。**結線そのものは次回である** |
| 2026-08-22 | 直した | ⭐ **押せない操作子 21 個を直した** —— `rowControlElement` に中身（行 ID）を 1 つ入れた。⛔ **発明ではない** —— `PD-154` が既にこの穴を記録し「行 ID を `data-icon` に置き、図形は描かない」を推奨しており、同ファイルの `commandEntry` が同じ退き方をしている。`@provisional PD-154` を付けた。⭐ **実測: 4 × 0 → 41.7 × 24。`IF-9` の引きが 7 行 × 3 とも入口を答える** |
| 2026-08-22 | 発見 | ⛔ **表 T-051 の `HF-6` が満たされていない。試験を 1 件わざと赤で残した。** 「操作子は薄く描き、ポインタが乗っているあいだだけ濃くする」のに、⛔ **3 つとも `color:CanvasText`（全強度）で `opacity` も無く、様式の要素がツリーに 1 つも無いので、ポインタが乗ったことに応じる道がそもそも無い。** ⚠️ `FR-098` が「濃さは折り畳みの操作子と同じ」と引くので `Row Pin` も同じである。⭐ 同ユニットは別の場所で `GrayText` を「薄く」に使っており、語彙は既に在る |
| 2026-08-22 | 発見 | ⛔ **表 T-051 の `HF-4`（MUST）も満たされていない** —— 「行名の長さによらず `Row Title Panel` の右端に揃える」のに、⛔ **`IC-58` / `IC-59` は行名より前（x=4 / x=49.7）に描かれ、行の深さの余白の内側なので深さで横にずれる。** `IC-60` だけが x=166 で右端に揃っている。⚠️ **起動テンプレートは全行が深さ 1 なので今日は目に見えない** |
| 2026-08-22 | 罠 | ⛔ ⭐ **作り物が寸法を決めている試験は、寸法の欠陥を原理的に見つけられない。** `uf-72-screen-part.test.ts` の `laidOut` は試験が持つ表から箱を答えるので、⛔ **外接矩形の幅が正であることを試験に書いても、4 × 0 の操作子に緑を出す。** ⚠️ **2195 件が緑のまま 21 個が出荷された理由がこれである。** ⭐ 効いた手は「ユニットが書いた宣言から寸法の出どころを求める述語」を書き、中身の無い節点に当てて落ちることを見せること |
| 2026-08-22 | 発見 | ⛔ **「残るのは結線だけ」は半分しか正しくなかった。** ⭐ **`InputContext` の 12 メンバを 1 つずつ突き合わせたところ、出どころが無いものが 3 つ残っていた** —— `today`（`FR-046` は「その時点の本日」としか書いていない）・`editedBy`（`AT-128` の値域が無い）・`PointerSlop` の 2 値（`S-91` は散文、線の掴み代は行そのものが無い）。⚠️ **どれもコードで埋められる形をしていたので、測らなければ黙って選んでいた** |
| 2026-08-22 | 裁定 | ⭐ **利用者の裁定 3 件を `CR-206` / `CR-207` / `CR-208` として当てた。** ⛔ **どれも分類 `D` または表への追加であり、推奨で走ってはならないものであった**（規則 06）。**`FR-046` に「読む人の機のローカルの暦の日」（`CR-206` / `PD-166`）、表 T-229 に書き手が名乗る語（`CR-207` / `PD-165`。`ED-1` = `user`）、表 T-206 に `S-91` の数と `S-137`（`CR-208` / `PD-167` / `PD-168`）。** ⭐ **数の予測はどの回も的中した** —— `tables` 128 → 129、`rows` 1580 → 1585 |
| 2026-08-22 | 罠 | ⛔ **変更履歴に「表 T-023d の 4 行」と書いて検査 9 が落ちた** —— **「表 X の N 行」は「X は N 行を持つ」と読まれる。** ⭐ **行 ID を名指す形（`GR-5` / `GR-6` / `GR-13` / `GR-16`）に直して通した。** ⚠️ **技能書がこの罠を明記していたのに踏んだ** |
| 2026-08-22 | 決めた | ⛔ **`A` 分類の `@provisional` の印を生成ブロックに付けられない。** ⭐ **`PointerSlop` の宣言（手書きの側）に付けた** —— 値の意味を述べているのはそちらであり、生成ブロックは次の `npm run gen` で消える。⚠️ **原稿の 🔎 と `@provisional` は別の仕組みである** —— 前者は「由来が無い」、後者は「裁定を待っている」 |
| 2026-08-22 | 決めた | ⭐ **`FT-1` の結線を `frame-loop.ts` に置き、`single-html-shell.ts` は 2 文だけにした。** ⛔ **表 T-063 の `UT-6` がこの分け方の根拠である** —— 起動が縛られる理由（`FR-067` / `FR-065`）と、フレームを回す理由（`LY-5` / `ADR-001`）は別である。⭐ **`MK-10` の答えは工場の引数で往き、出来事は監視者で来る** —— `InputWatcher` が何も返さないので、答えは来た道を戻れない |
| 2026-08-22 | 決めた | ⛔ **順序が効いている。** ⭐ **押下で掴んだ press は、翻訳器が読んだ後・書き込みの前に落とす** —— 離しは press だけから決まり（`CS-2`）、かつ `WS-2` は身振りの最中の書き込みを拒む（`AG-9`）。**逆順にすると、身振りで作った書き込みが 1 件も通らない** |
| 2026-08-22 | 実測 | ⭐ **実 DOM で押して動くことを自分で確かめた**（詳細は「現在地」）。**`Ctrl+Shift+D` が基準日線を消して戻し、戻った絵が元と違う** —— テンプレートの基準日ではなく本日が入るからで、`CR-206` がそのまま効いている。**葉のバーは掴んで動く（x 441.72 → 522.01、図形 8 個が連動）** |
| 2026-08-22 | 罠 | ⛔ **隠れた文書では `ResizeObserver` も配送されない。** ⚠️ **rAF だけの話だと思っていた** —— 実測: プレビュー枠が 0×0 で冷間起動すると `BO-1` が正しく 1 枚目を止め、**復帰路である `ResizeObserver` が飢えて永久に白いままになる。** ⭐ **`data-role` が 45 ではなく 8 で止まるのがその形である。** ⛔ **アプリの欠陥ではない**。⚠️ **もう 1 つ: `ask()` は `owed` を立てたら次のフレームまで再要求しないので、rAF が 1 度も発火しない環境では最初の 1 件で詰まる** —— 差し替えは**読み込みの前**に入れないと効かない |
| 2026-08-22 | 発見 | ⚠️ **`S-95`（取り消しの合計サイズ）は原稿が MB で述べ、`historyWithStep` は UTF-8 バイトで比べている。** ⛔ **`frame-loop.ts` は換算せずに素通しし、`HISTORY_LIMITS` の注記でそう名乗った** —— **ここで係数を書けば、このファイルが他所の定める境界を決めることになる。** ⭐ **別の変更要求である** |
| 2026-08-22 | 直した | ⛔ **その読みが誤りだった。仕様が換算そのものを述べているので、欠けているものは何も無かった。** ⭐ **`S-95` の備考が「1 MB = 1024 × 1024 バイトとする」と書き、`FR-031` が「UTF-8 で符号化した長さ（バイト）で測ること（MUST）」と定める。** ⚠️ **上限が 64 バイトになり、1 段 655,969 バイトの起動テンプレートでは書き込みのたびに履歴が 1 段へ切り詰められていた**（`S-94` は 50 段を約束している）。⭐ **同じ形は `CR-173` が `S-113` で決着させており、`validate-imported-document.ts` が `BYTES_PER_MEGABYTE` を持つ。** ⛔ **`STOP` は「何が無いか」を書く道具であって、「調べていない」を書く道具ではない** |
| 2026-08-22 | 直した | ⛔ **`Esc` が身振りの段を消費していなかった**（表 T-028 の `IN-4`。MUST）。⚠️ **`hasEndedGesture` はポインタの `up` と `lost` にしか真にならず、打鍵が 1 度も届かなかった。** ⭐ **`screen-state.ts` 自身が「シェルが press を落とす」と書いていた。** ⛔ **起きていたこと 2 つ**: 掴む→動かす→`Esc`→離す で**中断したはずの移動が書き込まれる**。⛔ **1 度でもボタンが下りたら `escapeTarget` が永久に「身振り」を答え、`Esc` がブラウザへ二度と届かない**（`IN-4a` の MUST が破れ、`FR-071` が到達不能）。⭐ **`escapeTarget` は `PI-36` が公開しているので、シェルが問うのが正しい** |
| 2026-08-22 | 直した | ⛔ **素のポインタ移動でフレームを起こしていた**（`FR-048` の MUST NOT）。⭐ **同じ要求が判定の書き方まで定めている** ——「**判定はポインタではなく描く内容に置くこと**」。⚠️ **例外も定めている** —— ポインタが乗って濃さが変わる UI パーツは「描く内容が変わる」に当たり対象外。⭐ **移動のときだけ 4 条件で判定する形にした**（身振り中／追従するものが描かれている／選択・画面状態・文書が変わった／ポインタの下の `ScreenPart` が変わった）。⛔ **`ScreenPart` は `IF-9` に問う** —— 描いた側が答える（Chapter 5.3） |
| 2026-08-22 | 罠 | ⛔ ⭐ **前に立つ者の実 DOM 計測が誤り、穴を誤報しかけた**（`docs/review/ft1-wiring-findings-2026-08-22.md` の第 6 節）。⚠️ **`ask()` の詰まりを避けるためモジュールを再取り込みしたら、`boot()` が 2 つ目の実体を起こし、両方が `window` の出来事を受けていた。** ⛔ **指紋を document 全体から取っていたので、複数実体の絵を連結して読んでいた。** ⭐ **測る前に `[data-role="Schedule Canvas"]` が 1 つであることを確かめ、指紋は実体の根の中だけから取ること。** ⚠️ **ポリゴンの指紋は狭い** —— 648 タスクが配置されても SVG に出る `polygon` は 71 個で、**「絵が動かない」は「文書が動かない」の証拠にならない** |
| 2026-08-22 | 発見 | ⛔ **測っている形が「保存形」ではない。** `FR-031` は「その段の**保存形**を UTF-8 で符号化した長さ（バイト）で測ること（MUST）」と定めるのに、⚠️ **`stepSize` は `JSON.stringify(document)`（詰めた形）で測っており、保存形は `JSON.stringify(document, null, 1) + '\n'` である。** ⛔ **同じ文書で 655,969 と 895,286 —— 36% 違い、合計メモリの予算が仕様より緩い。** ⭐ **単純に直せない** —— 保存形を作るのは `DocumentCodec`（`Adapter`）で測る側は `UseCase` であり、`LR-1` が外向きの依存を禁じている。⚠️ **いまは効かない**（この文書なら 50 段で 32.8MB、`S-95` の 64MB に届かない）|
| 2026-08-22 | 発見 | ⛔ **取り消しの「段数」を止めている試験が 1 件も無かった。** ⚠️ **いちばん近い 3 本はどれも 64 バイト上限のバグを素通しする** —— `uf-20-21` は履歴を手で組んで段の大きさを `1` と決め打ち、`uf-27-28-29` は自分で `maxSteps: 1` を渡し、`document-model` は実文書のバイト数を 1 度も見ない。⛔ **`carryOutAction` の取り消し・やり直しの枝は試験 0 件である。** ⭐ **2296 件が緑のまま CRITICAL が出荷されかけた理由がこれである** |
| 2026-08-22 | 発見 | ⛔ **表 T-064 が名指すメンバの実装を見る機械検査が無く、未実装のメンバが 2 件在った。** ⭐ **検査 26b を新設した**（97 メンバを読み、9 欄は「名前が書かれていない」と申告して数を刷る）。⛔ **`PI-1` の `scheduleViolations`** —— 表 T-220 の 17 個の不変条件に実装が 1 つも無い。⛔ **`PI-8` の `replaceDocument`** —— `CR-196` が名前を開け、刻印が未決（分類 `D`）だから保留していたが、⭐ **`CR-205` がその刻印を裁定済にしたので、いまは書ける。** ⚠️ **どちらも本回の結線とは無関係の既存の穴である** |
| 2026-08-22 | 決めた | ⛔ **検査 26b を赤のまま置かず、baseline を与えた。** ⭐ **弱めたのではない** —— 規則 05 の 1. が「止まるときは緑で止まる」と定め、**永久に赤い検査は走り全体を無視させる**（README の「noisy gate は無い gate より悪い」）。⭐ **この repo は既にその作法を持っていた** —— 重複検出器が `duplication-baseline.txt` で `A=26 (new 0)` と刷る形である。⛔ **baseline に無い穴が出たら赤／baseline が実物と合わなくなったら赤**（払った負債が居座ると baseline が腐る）|
| 2026-08-22 | 実測 | ⭐ **CR-209 の測り。** `impact.py T-067` —— 表 T-067 を指している要求は **2 件**（`FR-088` `FR-064`）、2 次は 4 件。`impact.py T-064` —— ⚠️ **指している要求は 0 件**（表が要求から浮いている）。`induced.py T-067 WS-1 WS-4 WS-5 AG-2 AG-6 AG-11 FR-063 T-064 PI-8 T-027 UN-6 OP-4 FR-031 FR-087` —— 種 **15/15** が解決、辺 **16**、⛔ **閉路 1 件（大きさ 5。`AG-11` `AG-2` `AG-6` `FR-063` `WS-5`）。** ⭐ **したがって `WS-5` ／ `AG-6` ／ `FR-063` は 1 つの計画で 1 度に書いた** |
| 2026-08-22 | 発見 | ⛔ **裁定 01 の `Q1`（「入ってきた刻印をそのまま持つ。6 呼び手すべて」）が、6 呼び手のうち合流とだけ噛み合わなかった。** ⚠️ **合流の結果は現在の文書から組まれる**（`import-document.ts:1316` が `{...request.current, schedule, documentSettings}` を返す）ので、**入ってきた刻印はいまの刻印そのものである。** ⛔ **進めないと `FR-063` の MUST ／ `AG-2` の楽観ロック ／ 裁定 `Q6` の通知の判定の 3 つが同時に破れる。** ⭐ **利用者に問い、「取り込みだけ進める」の裁定を受けた** —— ⚠️ **裁定 01 の §3 の表は最初からそう測っていた**（合流: 刻印 進める ／ 段 積む）。**要約の 1 行だけが射程を落としていた** |
| 2026-08-22 | 変更要求 | ⭐ **CR-209 を書いた**（`change-request/CR-209-the-six-callers-and-what-each-keeps.md`）—— 裁定 01 の 7 問。⛔ **予測**: tables 129 → **130**（表 T-230）／ figures 11 ／ rows 1585 → **1591**（`RD-1` 〜 `RD-6`）／ uids 146 ／ 辺 89（不変）／ 重複 `A=26 (new 0)` |
| 2026-08-22 | 適用 | ✅ **CR-209 を当てた**（オブジェクトごとに 1 回、計 8 か所）—— 5.5 の冒頭の 1 文、**表 T-230 を新設**（6 行 ＋ MUST を 6 文）、表 T-064 の `PI-8` のセル、表 T-067 の `WS-5` の但し書き、表 T-035 の `AG-6` の括弧、`FR-063` の 2 段落目、表 T-070 の `NF-2`、変更履歴 版 0.86。⭐ **数の予測は 4 つとも的中**（tables **130** ／ figures 11 ／ rows **1591** ／ uids 146、重複 `A=26 (new 0)`）。⛔ **`_source/components.json` は 1 バイトも動かない** —— 裁定 `Q0` がその図を正としたためである |
| 2026-08-22 | 罠 | ⛔ **踏んだ罠が 2 つ、どちらも既知だった。** ① 変更履歴に `` `CR-196` `` とバッククォート付きで書いたら**検査 7 が落ちた** —— 変更要求の番号は仕様書のオブジェクトではない（`PD-153` で 1 度踏んだのと同じ形）。**印を外した。** ② 「部品グラフ」と書いたら **`audit-ch5` が落ちた** —— 表 T-006b の `A-17` は「部品」を**複合語であっても**禁じる。⭐ **「コンポーネントの図」に直した** |
| 2026-08-22 | 判断 | ⭐ **表 T-067 に列を足さず、表 T-230 を新設した。** ⛔ **列を足すと 7 つの順すべてが 6 つの呼び手の欄を持ち、命令の道にとって空の欄が 30 個できる。** ⚠️ **違うのは 3 つ（履歴・刻印・取り消しの 1 段）だけで、`WS-2` `WS-6` `WS-7` はそのまま効く** |
| 2026-08-22 | 判断 | ⭐ **裁定 01 の §3 の表が「起動時の文書 → `ChooseStartupDocument` に問う」と書いていたのを、「呼び手が持って来る」に直した。** ⛔ **`_source/components.json` の辺は `SingleHtmlShell → ChooseStartupDocument` であり、`ApplyDocumentChange → ChooseStartupDocument` は存在しない。** ⭐ **`Q0` がその図を正としたので、辺を足さずに表の側を合わせた** |
| 2026-08-23 | 実測 | ⭐ **CR-210 の測り。** `impact.py T-066` —— 表 T-066 を指している要求は **1 件**（`NFR-010`）、2 次は 2 件。`impact.py T-078` —— 同じく **1 件**。`induced.py T-066 CS-1 CS-2 CS-3 T-078 FT-2 FT-3 NFR-010 NFR-011 T-071 CA-3 T-077 BO-1 T-067 WS-6` —— 種 **15/15** が解決、辺 **10**、⭐ **閉路 0 件。** したがって 1 か所ずつ順に書いた |
| 2026-08-23 | 発見 | ⛔ **`CA-3`（許容する陳腐化）が `R2.20` の MUST に対して偽であった。** ⚠️ **「フレームの途中で文書が変わることは無い」の根拠に挙げた 2 つの拒否（`AG-9` と `WS-2`）は、どちらもファイル選択が開いているあいだ何も拒否しない。** ⭐ **`R2.20` 自身が「陳腐化が生むのは遅い答えでなく違う答えであり、正しさの観点である」と書いている** |
| 2026-08-23 | 実測 | ⭐ **`docs/spec` は非同期・`Promise`・`async`・`await`・待機のどれも 1 度も書いていない**（出現 0 回。自分で数えた）。⛔ **`Promise` はすべて `src/` 側の決定であり、「仕様が既に非同期を受け入れている」とは言えない** |
| 2026-08-23 | 変更要求 | ⭐ **CR-210 を書いた**（`change-request/CR-210-the-forty-seconds-nobody-wrote-down.md`）—— 裁定 02 の 2 問。⛔ **予測**: tables 130（不変）／ figures 11 ／ rows 1591 → **1592**（`CS-4`）／ uids 146 |
| 2026-08-23 | 適用 | ✅ **CR-210 を当てた**（オブジェクトごとに 1 回、計 6 か所）—— 表 T-066 に `CS-4`、その下の「3 つは入れ子である」を `CS-1` 〜 `CS-3` に限って `CS-4` の規則を MUST で足し、`R7.4` の適合の 1 文に但し書き、起動の注の「3 単位」→「4 単位」、表 T-071 の `CA-3`、変更履歴 版 0.87。⭐ **数の予測は 4 つとも的中**（tables 130 ／ figures 11 ／ rows **1592** ／ uids 146、重複 `A=26 (new 0)`）。⛔ **表 T-078 は 1 行も動かさない** —— 着地は `WS-6` であり `FT-2` が既に覆う |
| 2026-08-23 | 実測 | ⭐ **CR-211 の測り。** `impact.py T-024` / `T-024a` / `T-109` / `T-103` —— 表 T-024 は要求 **6 件**、表 T-024a は **5 件**、表 T-109 は **3 件**（`FR-099` `FR-029` `FR-038`）、⚠️ **表 T-103 は 0 件**（名前の表は要求から浮いている）。`induced.py`（種 19 個）—— 種 **19/19** が解決、辺 **14**、⭐ **閉路 0 件。** ⚠️ **表 T-024 と 表 T-024a は同じ塊「開くと合流」に属する**ので、同じ塊の 表 T-032 を触らないことを確かめた |
| 2026-08-23 | 変更要求 | ⭐ **CR-211 を書いた**（`change-request/CR-211-which-decoder-and-who-answers.md`）—— 裁定 03 の 5 問。⛔ **予測**: tables 130（不変）／ figures 11 ／ rows 1592 → **1596** ／ uids 146 ／ 表 T-109 は 68 → **70** ／ 図 F-019 は 68 → **70** 図形 |
| 2026-08-23 | 適用 | ✅ **CR-211 を当てた**（オブジェクトごとに 1 回、計 12 か所）—— 表 T-024 の `IO-1` / `IO-2` の備考、表 T-024a に `OP-12`、`FR-096` の散文、表 T-036 の `SK-11`、表 T-103 に `U-55`、表 T-109 に `IC-69` / `IC-70`、同表の前の散文の数 2 か所、図 F-019 に図形 2 つ ＋ `aria-label`、表 T-026 の `RC-13`、`npm run gen`、変更履歴 版 0.88。⭐ **`viewBox` は 452 × 284 のまま** —— 最終行（`y=232`）は 8 個までしか埋まっておらず、12 列目まで空いていた（自分で座標を数えた）|
| 2026-08-23 | 判断 | ⚠️ **数の予測が 1 つ外れた** —— ⛔ **重複検出が `A=26 (new 0)` の予測に対し `A=28 (new 2)` を出した。** ⭐ **拾われたのはどちらも同じ表の兄弟行である**（`IC-69` ＋ `IC-70` の 2 択、`IO-1` ＋ `IO-2` の拡張子と先頭 1 文字）。⭐ **`CR-191` が同じ形で `IC-67` ＋ `IC-68` を基準線へ足した先例に従って足した** —— **規則が 2 度書かれているのではなく、1 つの問いの 2 つの答え・1 つの規則の 2 つの値である。** ⛔ **語を捻って検出器をよけない** |
| 2026-08-23 | 実測 | ⚠️ **`npm run gen` が 1 度落ちた** —— **表示語の生成器が「原稿は 68 件、仕様は 70 件を求める」と名指して止まった。** ⭐ **これは検査が効いている形である**（`CR-194` が仕組んだとおり、表が動けば黙らずに落ちる）。`_source/display-words.json` に `IC-69` / `IC-70` の空欄を足して 172 → **176 語**。⛔ **語は 1 つも書いていない**（`PD-160` は未裁定のまま）|
| 2026-08-23 | 判断 | ⭐ **確認の面の図形を「丸で囲んだ ✓ / ✗」にした。** ⛔ **裸の ✗ は `IC-52` が、四角で囲んだ ✓ は `IC-67` が既に使っている**（図形を自分で取り出して確かめた）。⚠️ **同じ図形を 2 行に与えると `FR-029` の「用途を伝える」が果たせない。** ⭐ **新しい 2 つは 表 T-026 の `RC-13` の「実物で見比べていない」債務に足した**（12 個 → 14 個）|
| 2026-08-23 | 実測 | ⭐ **CR-212 の測り。** `impact.py FR-031` —— 指している先は**表 10 個・行 18 個**、指している箇所は**要求 4 件 ／ 参照 20 箇所**。⚠️ **塊「ポインタ操作」に触れる**が、本 CR はそのどれも書き換えない。`induced.py FR-031 T-027 S-95 S-94 T-206 FR-024 T-024 IO-5 FR-061` —— 種 **9/9** が解決、辺 **4**、⭐ **閉路 0 件** |
| 2026-08-23 | 変更要求 | ⭐ **CR-212 を書いた**（`change-request/CR-212-the-form-that-is-only-counted.md`）—— 裁定 05。⛔ **予測**: tables 130 ／ figures 11 ／ rows 1596 ／ uids 146 は**すべて不変**（`FR-031` の 1 文だけを書き換える）|
| 2026-08-23 | 適用 | ✅ **CR-212 を当てた**（2 か所）—— `FR-031` の `STATEMENT` の 1 文と、変更履歴 版 0.89。⭐ **数の予測は 4 つとも的中**（すべて不変）。⛔ **コードは 1 行も動かしていない**（`npm test` は 2359 / 2361 のまま。赤 2 件は `AM-13` の別件）。⛔ **`S-95` の 🔎 は外していない** |
| 2026-08-23 | 決めた | ⭐ **「詰めた」の意味を括弧で述べ、保存する形と別であることを書いた。** ⚠️ **書かないと、次の担当者が「保存形で測れ」と読み直して同じ穴を掘る** —— **`R1.2` は「詰めた」だけでは満たせない。** ⛔ **`git` の証拠（CR-182 の同じコミットが文と実装を書いた）は仕様書に書かず、変更要求と裁定の記録に置いた** |
| 2026-08-23 | 発見 | ⛔ **CR-211 が 1 セル開けそこねていた。** ⭐ **`OP-12` は「どちらの形式として読むか」の規則を置いたのに、それを答えるメンバが 表 T-064 のどこにも無かった。** ⚠️ **コンポーネントの図には `FileGateway → DocumentCodec` の辺が既に在り、その札は `format`（「交換の形式を得る」）である** —— **図が描いていたものを表が名乗っていなかった。** ⭐ **CR-213 で `PI-20` に `formatFromFile` を足した。辺は 1 本も動かしていない** |
| 2026-08-23 | 決めた | ⛔ **判別の値（拡張子と先頭の非空白 1 文字）を散文に置いたままにしなかった。** ⚠️ **`docs/spec` を読む生成器は 7 本あり、7 本とも行 ID と列で繋いでいる。散文を解析するものは 1 つも無い**（自分で数えた）。⭐ **CR-214 で 表 T-024 に 2 列を足し、値を備考から列へ移した** —— **規則 03 の 1「仕様が持つ値は写さずに生成する」を、前例のある形で満たすため。** ⭐ **副産物として重複検出の群が 1 つ減った**（`A=28` → `A=27`）|
| 2026-08-23 | 多体 | ⭐ **スカウト 4 体（読み取り専用）＋ 改名 1 体を先に回した。** ⛔ **スカウトが「先に壊せ」と言った逆向きの辺は本物だった** —— `tools/check_layer_rules.py --report` が `use-case/undo-edit → use-case/apply-document-change` と `redo-edit → 同` を刷っており、**`ChangeStep` と `HeldDocument` が `apply-document-change.ts` に宣言されていたため**である。⚠️ **そのまま `ApplyDocumentChange → UndoEdit` の呼び出しを足すと `LR-3` の 2 閉路になる。** ⭐ **宣言を `undo-edit.ts` へ移して先に潰した** —— 表 T-064 は動かない（引数と戻り値は `src/` が持つ）|
| 2026-08-23 | 実測 | ⭐ **裁定 04 の改名は 26 か所 11 ファイル。** ⚠️ **`tsc` は `tests/` を見ないので、改名した側が 1 件落とした試験を自分で見つけて直した** —— `use-case.test.ts` が `HeldStep` の欄を構造的に読んでおり、`undefined` から `NaN` になっていた。⛔ **これは規則 04 の 1 の例外である「呼び出し側の機械的な改名」に当たる** |
| 2026-08-23 | 実装 | ⭐ **`replaceDocument`（`PI-8`）を書いた。** ⛔ **先に層の逆向きの辺を潰した** —— `ChangeStep` と `HeldDocument` の宣言を `undo-edit.ts` へ移し、`apply-document-change.ts` が再公開する形にした（`DocumentCommand` が既に採っている形）。⭐ **`WS-1`〜`WS-5` は `document-change-plan.ts`（`UF-9`、pure）、`WS-6` と `WS-7` は `apply-document-change.ts`（`UF-8`、non-pure）** —— `UT-1` が純粋性で割った境目そのもの。⭐ **検査 26b の `PI-8` の負債 2 行を消した**（負債は 2 → 1）|
| 2026-08-23 | 実装 | ⭐ **取り消し・やり直しが「第 2 の入口」を通らなくなった**（`MS-1` が禁じる形）。⛔ **シェルの同名メンバ `FrameLoop.replaceDocument` は `holdDocument` へ改名し、さらに 表 T-230 の行を引数に取る形にした** —— **「履歴を持ち越す」は 6 つの行のどれでもなく、名乗らない差し替えを禁じる MUST NOT に触れていた** |
| 2026-08-23 | 実装 | ⭐ **表示語を配る道を通した**（裁定 06）。5 ユニットが生成された辞書を読み、⭐ **空の語のときは今日までと同じ退避値を刷る**（`word === '' ? standIn : word` と明示的に書き、`||` を使わない）。⛔ **語は 1 つも書いていない**（`PD-160`）。⚠️ **`ScreenView` に表示言語の欄が増えた** —— 表 T-075 の `UF-60` の欄がそれを命じている |
| 2026-08-23 | 実装 | ⭐ **`formatFromFile`（`OP-12`）を書き、値を運ぶ生成器を新設した**（`tools/generate_exchange_formats.py`）。⛔ **拡張子と先頭 1 文字を `src/` に打っていない** —— 表 T-024 の 2 列から生成する。⭐ **BOM を落としてから 1 文字目を見る順序を守り、`json-codec.ts` の BOM 欠落（`FR-023` の MUST NOT 違反）も同じ回で閉じた** |
| 2026-08-23 | 実装 | ⭐ **`Confirmation`（`U-55`）を描き、`IC-69` / `IC-70` を押せるようにした。** ⚠️ **3 ファイルが同じ `STOP` で「できない」と書いていたが、その 2 つの理由（語が無い・入口が無い）は `CR-211` が両方外していた** |
| 2026-08-23 | 発見 | ⛔ ⭐ **アイコンが 1 つも描かれていなかった** —— **`FR-029` は 図 F-019 を図形の正とする（MUST）のに、その図を `src/` へ運ぶ生成器が無く、画面は "IC-1" という文字を刷っていた。** ⭐ **`tools/generate_icon_glyphs.py` を新設して閉じた**（70 図形。表 T-109 と相互検査）。⭐ **実 DOM で確かめた** —— 74 入口すべてが図形を描き、行 ID を刷る入口は 0 |
| 2026-08-23 | 罠 | ⛔ **私（前に立つ者）が誤報した。** ⚠️ **ブラウザ工具のコンソール履歴はタブごとに読み込みをまたいで積み上がる。** アイコン結線を書いている最中（`dom-screen-surface.ts` が 40 回書き換わり、そのたびに自動再読み込みされた）の古い誤りを「いま出ている」と読んだ。⭐ **1 体が起動前に 10 通りの経路を張って測り直し、`d` の書き込み 145 件すべてが正しいことを示した。** ⭐ **安全な読み方**: 最新の `[vite] connected.` より後の行だけを見るか、新しいタブを開く |
| 2026-08-23 | 発見 | ⛔ **画面からの書き込みが監視者に 1 人も届いていなかった**（`AG-6` / `WS-7`。仕様だけを読んだ試験が赤で出した）。⚠️ **シェルが両方の書き込み口に空の聴き手を渡しており、`ApplyDocumentChange → NotifyChangeWatchers` の辺は `Agent API` が書き手のときしか通っていなかった。** ⭐ **`PI-15` の冒頭が渡すべき形を逐語で指示していた。** 閉じた。試験は 1 文字も触っていない |
| 2026-08-23 | 判断 | ⚠️ **赤 2 件を意図して残した** —— `AG-9` は「パンと範囲選択は拒否しない」と明文で除外するのに、`WS-2` に答えるシェルは押下中のすべてを拒否している。⛔ **どの行で始まった押下かを問う道が公開されていない**（`PD-169`）。⭐ **広いほうの誤りを採った** —— **`AG-9` の MUST を守るほうを残した。** 規則 05 の 1. のとおり、赤の理由をここまで絞って記録して止まる |
| 2026-08-23 | 実測 | ⭐ **本日の総計。** 試験 2361 → **3263 件**（緑 3259）。⛔ **赤 4 件** —— 既知の `AM-13` 2 件と、上の `AG-9` の 2 件。機械検査 26 本 ALL GREEN ／ `audit-ch5` PASS ／ 描画 PASS ／ `typecheck` ／ `layers` ／ `gen:check` 12 本 OK ／ 生成器 7 → **9 本**（交換形式・アイコン図形）|
