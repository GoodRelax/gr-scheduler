# 不具合・改善要望の管理台帳

**規則は [`docs/development-rules/05-working-method.md`](../development-rules/05-working-method.md) の 7.（5 段のパイプライン）。**

⛔ **利用者が指摘した不具合は、直るまでこの表から消さない。** ⭐ **消してよいのは `実測済` か `取下げ` になった行だけである。**

⭐⭐ **完了分は隣のファイルに在る**: [`fixed-defects.md`](fixed-defects.md)（利用者の裁定 2026-09-01）。⛔ **要約せず、1 文字も削らずに移してある。**
⭐ **移す集合は「本書の `実測済` と `取下げ` の行すべて」である。⛔ 移した行の数を本書に書くな** —— **数は書いた瞬間から腐る。**⇒ **実数は上の件数表が刷る。**
⛔⛔ **戻り道が在る。片道にするな。** **実物で落ちたら、その行を `fixed-defects.md` から本書へ戻すこと。**⚠️ **前の巡は `D-06` と `D-43` が「自動試験は緑なのに実物で落ちる」ことを示した。**
⚠️ **道具と検査は 2 ファイルの両方を読む** —— `tools/ledger_metrics.py` と 検査 28 / 29 / 31。⛔ **片方しか読まない道具を足すな** —— **刈り取ったぶんだけ空を測り、全部緑になる。**

⚠️ **「自動試験が緑」は実物で動いた証拠ではない**（規則 04）。**だから `試験待ち`（コードに在るが自動試験がまだ緑でない）・`実測待ち`（緑だが出荷ビルドをまだ押していない）・`実測済`（押して期待値を測った）を分けて持ち、`実物確認` の欄で裏を取る。**

---

<!-- ledger-metrics: begin -- generated by tools/ledger_metrics.py -->

### ⭐ 件数（**`tools/ledger_metrics.py` が刷る。手で書き換えない**）

| | 残件 | 総件数 | 未検討 | 裁定待ち | 仕様待ち | 実装待ち | 試験待ち | 実測待ち | 実測済 | 取下げ |
| --- | --: | --: | --: | --: | --: | --: | --: | --: | --: | --: |
| セッション開始時 2026-09-08 05:32 | 31 | 400 | 15 | 0 | 5 | 7 | 2 | 2 | 333 | 36 |
| 最新 2026-09-08 09:13 | 32 | 411 | 0 | 5 | 4 | 6 | 2 | 15 | 335 | 44 |
| ⭐ 差分 | +1 | +11 | -15 | +5 | -1 | -1 | ±0 | +13 | +2 | +8 |

⚠️ **巡の頭で `python tools/ledger_metrics.py --start` を 1 度走らせる。**⛔ **手で書かない** —— 測れる数を手で書けば必ず腐る。⚠️ **時刻まで持つ** —— 1 日に複数の巡が走るので、日付だけでは見分けられない。

⛔⛔ **上の数は 2 ファイルの合計である** —— 本書と [`fixed-defects.md`](fixed-defects.md)。⚠️ **合計で数えるのは、刈り取りが進歩に見えないようにするためである** —— **片方だけを数えれば、仕事を終えた行の数ぶん総件数が減り、何もしていない巡が前進に見える。**

### ⭐ 束ねた数（8 状態を 5 束へ。同じく `tools/ledger_metrics.py` が刷る）

⭐ **なぜ割るか** —— 上の表を 1 つの「残件」で読むと、`裁定待ち` と `仕様待ち`（まだ仕様に行が無い・裁定が無い）が `実装待ち` と `試験待ち`（コードの仕事）と同じ 1 つの数に混ざり、**減らない仕様の穴が、減らない不具合に見える。**

| | 未仕分け | 仕様の穴 | 製品の仕事 | 検証の借り | 決着 |
| --- | --: | --: | --: | --: | --: |
| セッション開始時 2026-09-08 05:32 | 15 | 5 | 9 | 2 | 369 |
| 最新 2026-09-08 09:13 | 0 | 9 | 8 | 15 | 379 |
| ⭐ 差分 | -15 | +4 | -1 | +13 | +10 |

⛔ **「不具合」と呼んでよいのは `製品の仕事` だけである。** `仕様の穴` は仕様を書く仕事、`検証の借り` は押しに行く仕事であって、どちらもコードの欠陥ではない。

⚠️ **`道具・試験の借り`（重なりを許す目安。上の 5 束とは重なる。分割ではない）: 4 件** —— 開いている行（`決着` でない行）のうち、行の全文（全セル）が正規表現 `\b(tools|tests|src)/[\w./-]+` で拾える語のうち `tools/` か `tests/` で始まる語を 1 つ以上持ち、`src/` で始まる語を 1 つも持たない行の数。

<!-- ledger-metrics: end -->

---

## ステータスの定義

⭐ **2026-09-01、利用者の裁定で 10 状態を 8 状態へ組み替えた。理由は 2 つある。**

⚠️ **1 つ目 —— 古い 10 状態は軸を 2 本混ぜていた。** **仕事がどこまで進んだか**と、**いま誰が持っているか**の 2 本である。2 本目（`〜待ち` と `〜中` の別）は腐った。**実測すると `テスト中` 20 行と `テスト待ち` 20 行は実務上まったく同じ意味で使われており**、しかも `テスト待ち` は 2026-09-01 まで定義の表に無かった。

⛔ **2 つ目 —— `テスト完了` は主張を 2 つ同時に担いでいた。** 「自動試験が緑である」と「誰かが出荷ビルドを開いて実測した」の 2 つである。**この 2 つは同じではない。** この巡は、**5570 件の自動試験がすべて緑のまま、実物を押して欠陥を 4 件見つけている。**

⭐ **だから新しい 8 状態は、終端の 2 つを除き、すべて「何を待っているか」で名づける** —— 名前が、次に動く者を言う。

| ステータス | 意味 | 次に動かす者 |
|---|---|---|
| `未検討` | 受け付けただけ。原因も対応案も無い | Claude（調査）|
| `裁定待ち` | 原因は分かったが、**仕様に行が無い**か、利用者にしか決められない | ⛔ **利用者** |
| `仕様待ち` | 裁定は下りた。⚠️ **まだ仕様書に書かれていない** | Claude（仕様を書く）|
| `実装待ち` | 仕様書に在る。⚠️ **まだコードに無い** | Claude（実装）|
| `試験待ち` | コードに在る。⚠️ **自動試験がまだ緑でない** | Claude（試験）|
| `実測待ち` | 自動試験が緑。⛔ **まだ出荷ビルドを押していない** | Claude（実物で押す）|
| `実測済` | 押して期待値を測った | ⭐ **刈り取り（次巡で `fixed-defects.md` へ）** |
| `取下げ` | 欠陥ではなかった／別の行に吸収された | ⭐ **刈り取り** |

⚠️ **2026-09-01 の付け替えは機械的である。** 古い名前から新しい名前へは次のとおり移した —— `取下げ`→`取下げ`、`テスト完了` で `実物確認` が ⛔ で始まらないもの→`実測済`、`テスト完了` で ⛔ で始まるもの→`実測待ち`、`テスト中`／`テスト待ち`→`試験待ち`、`実装待ち`／`実装中`／`仕様書反映済み`→`実装待ち`、`仕様確定`→`仕様待ち`、`追加裁定待ち`→`裁定待ち`、`未検討`→`未検討`。⛔ **`ステータス` の欄以外は 1 バイトも触っていない。** ⭐ **すでに実測を持つ行は、系の試験が無いことを理由に後ろへ戻さない（利用者の裁定）。**

---

## 一覧

⛔ **`対応方針・決定仕様` の欄は 2 つを持つ。**裁定が下りるまでは**対応方針**を、仕様書へ入れたら**書いた先**を書く —— **章・仕様管理番号（要求 UID か表の行 ID）・仕様の逐語**（利用者の裁定 2026-08-30）。⭐ **これで、仕様書への記載漏れが台帳の上から見える。**

| ID | 不具合内容 | 期待値 | 対応案 | 対応方針・決定仕様 | ステータス | 詳細状況 | 補足 | 実物確認 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D-151 | ⚠️ **1 日ぶんのドラッグが、描画領域の幅しだいで 1 日足りなくなる**（体の実測 2026-08-30。CR-304 の配線がむき出しにした）| ドラッグした日数だけ動く。**画面の幅で結果が変わらない** | ①`dateAtX` の丸めが境目でどちらへ落ちるかを決める | ✅ **取り下げる。既に直っていた（反証 2026-09-03）** —— ⛔ **「ポインタの下の日」という読みは `docs/spec/` に 0 件である。**⭐ **`CR-314`（版 1.68）が「ポインタの位置から求めること」へ直しており**、当該の `vitest` 2 ファイルは **106/106 緑**である | `取下げ` | ⭐ **実測**: CR-304 でパネルが押しただけでは開かなくなり、描画領域が **280px 広がった。**すると `pxPerDay` が変わり、`fr-052-t-023d-picture-while-held.test.ts` の 「1 日動かす」と「4 日ぶん戻す」が **1 日足りなく**なった（3 日の場合は通る）。⛔⛔ **原因は幅の変更ではない** —— `from.x + n × pxPerDay` が日の境目の**わずかに手前**に落ち、`dateAtX` が切り捨てるからである。⚠️ **境目そのものは前から在り、幅が変わったことで乗る側が入れ替わっただけである。**⇒ ⭐ **「試験が古い」のではなく、「日の境目でどちらへ落ちるか」を仕様が定めていない可能性がある。**⛔ **試験を緩めて閉じるな。**⚠️ **台帳 D-138（縦のパンが行の隙間を飛ばす）と同じ家族である** —— あちらも量子化であり、1 回の長いドラッグでは隠れていた | ✅⭐⭐ **前に立つ者が仕様を引いた（2026-08-30）—— 行は在る。**表 T-023d のあとの結び（`01-04-requirements.md:2356`）が逐語で「**`GR-1` / `GR-2` の日数は、ポインタの下の日から求めること（MUST）**」「**いずれも 1 日単位に四捨五入する**」「⛔ **ドラッグの開始にしきい値を設けてはならない（MUST NOT）—— 掴み点は時間軸の上の「ある日」そのものであり、画素の感度というものが存在しない**」と定めている。⇒ ⭐⭐ **これは製品の欠陥ではなく、試験の作り方の問題である見込みが強い** —— **日の左端ちょうどを狙うと、浮動小数の 1 ulp でどちらの日にも落ちうる。**⛔ **正しい狙い方は「その日の真ん中」である。**⚠️ **これは主張を緩めることではない** —— 入力の作り方を仕様どおりにすることである。✅⭐⭐ **仕様だけを読む体が最後まで詰めた（2026-08-30）—— 答えは「両方」であった。**⭐ **試験の側の誤り**: 日の**ちょうど端**を狙っていた。⛔ **そこは「ポインタの下の日」の境目であると同時に「四捨五入」の引き分けでもあり、浮動小数の 1 ulp でどちらにも落ちる。**実測: 端では 1 日の運びが 0 日、4 日の運びが 3 日になり、2 日と 3 日は正しく落ちた。⭐ **1 日の 4 分の 1 だけ内側**を狙う形に直して両方とも緑（⛔ **真ん中の 0.5 にしない** —— そこが四捨五入の引き分けである）。⛔⛔ **仕様の側の誤り —— 表 T-023d の結びが自分と矛盾している。**「**ポインタの下の日から求めること（MUST）**」は切り捨てを言い、「**いずれも 1 日単位に四捨五入する**」は丸めを言う。**境目のわずかに手前で、製品は前の日に落ちる（切り捨てている）。**⇒ ⛔ **どちらか 1 文を落とす裁定が要る。**体は 2 文目を採って**わざと赤い 1 件**を置いた（⭐ **緩めていない**）| ✅ **2026-09-03** —— 仕様全文を当たって 0 件、試験 106/106 緑 |
| D-194 | ⛔ **行を 1 つ足して名前を付けると、行見出しパネルの見え位置が中身より下へ外れ、祖先の行が描かれなくなる**（前に立つ者の実測 2026-09-02、出荷ビルド）—— **読む人が上へスクロールするまで戻らない。**⚠️ **背の高い行（タスクを載せた 148px の行）が立っているときに起きる。** | 行に名前を付けたあとも、その行の祖先が描かれたままであること | ①名前を確定したあと、見え位置を中身の中へ戻す ②新しい行を描いた時点で、その行が見えるところまで寄せる | ✅ **取り下げる。3 通りで再現しなかった（反証 2026-09-03、出荷ビルド）** —— ⭐ **台帳がいう引き金（148px の `Mobile Client` を残して行を足し、名を付ける）でも起きない** | `取下げ` | ⭐ **実測（2026-09-02、出荷ビルド）**: 1 行まで消す → `add` → 「A Child」と命名 ⇒ 描かれるのは `["1:A Child"]` だけ。**パネルを上へスクロールすると `["0:Quality And Rele…","1:A Child"]` に戻る。**⇒ **文書は 2 行を持ったままである。**⚠️ **1963px のパネルに約 204px ぶんの行しか無いのに、見え位置が中身の下へ park している。** | ⭐ **`npm run parity` はこれとは別の理由で落ちていた** —— **`emptyTheApp` が「数が 1」で止まり、残ったのが見本の 148px の行だったためである。****いまは「画面の行の集合が変わらなくなるまで」押す不動点で止める形になり、25/25 に戻った。** | ✅ **2026-09-03（出荷ビルド）** —— 命名後に描かれたのは `["Mobile Client"@85, "A Child"@241]` の 2 行、`scrollTop/scrollHeight/clientHeight = 0/1080/1080`。見え位置は外れていない |
| D-202 | ⛔ **アプリが自分自身の HTML を読み戻せなかったときの理由に、専用の行が無い**（体の指摘 2026-09-02）—— **`EmbeddedHtmlFault` の 3 つの理由が、いずれも `RS-42`（原因の分からない失敗）へ落ちている。** | 3 つそれぞれに、読んだ人が次に採れる手が告げられること | ①表 T-233 に行を足す ②`RS-42` のままでよいと明記する | ⭐⭐ **利用者の裁定 2026-09-03（逐語）**: 「**html の読み戻しは不要。ユーザーは .html をダブルクリックして開けばよい。**」⇒ ⛔⛔ **機能そのものを取り下げる。**⭐ **`RS-45`（この画面を動かす本体が、このファイルの中に見つからない）を足したばかりだが、読み戻す道が不要になったので、その理由も要らなくなる。**⛔ **退けるときは 表 T-233 から行を消さず、席を退いた記録を残すこと**（`RS-17` と同じ作法） | `取下げ` | ⭐ **`D-173` で 単一 `.html` が書けるようになったことで、初めて到達しうる道になった。**⚠️ **`RS-15` ではない** —— **`FR-076` の「行の無い理由の落ち先」ではなく、`RS-42` という当たる行が既に在る。** | ⭐ **`D-173` から出た。あちらは閉じている。** | ✅ **2026-09-03 —— 利用者の裁定により取り下げ。**⭐ 単一 `.html` は「配って開いてもらう」ためのものであり、アプリが自分自身を読み戻す道は要らない |
| D-207 | ⛔ **掴み代を横へ引いても、行の段が変わらない**（体の実測 2026-09-02、出荷ビルド）—— **深さ 3 の `Phone Home Screen` の掴み代を右へ 60px 引くと、描かれる行の集合は変わる（動いてはいる）が、その行の深さは 3 のままである。** | 表 T-051 の `HF-15` の「左右は段を変えること（MUST）」のとおり、深さが変わること | ①横の引きが段の変更へつながっていない箇所を探す | ✅ **再現しなかった（実測 2026-09-03、出荷ビルド）** —— 掴み代を **60px 右へ引くと深さは 3 → 4 に変わる**。⛔ **前の巡の観測は引きが短かったものと見られる**（1 回しか測っていないと本行自身が断っていた） | `取下げ` | ⭐ **縦の引き（行の並べ替え）は効いている** —— **描かれる集合が変わった。**⛔ **横だけが効いていない。** | ⚠️ **`CR-336` の担当外で見つかった** —— **体は掴み代が押せるようになったことを確かめる途中で引いてみた。** | ✅ **2026-09-03（出荷ビルド）** —— 60px の右引きで深さ 3 → 4 |
| D-221 | ⛔ **`permissionLost` が「開いているファイルが無い」に当たるかを、どの行も言っていない**（体の指摘 2026-09-03）—— **`OP-13` は「開いているファイルが無いときは何もしないこと（MUST）」としか言わない。** | `permissionLost` をどちらに数えるかが決まっていること | ①「無い」に含めない（ファイルはまだ名前を持ち、`FR-060` の上書き先である） ②含める | ✅ **①で決着していた（反証 2026-09-03）** —— ⛔ **本行の「どの行も言っていない」は偽であった** —— 表 T-233 は `RS-1`（権限が無い）と `RS-2`（開いているファイルが無い）を**別の行として**持ち、`FR-060` は権限を失ったファイルを「上書き先を取り戻す」相手と呼ぶ。⭐ **実装も既に `RS-1` を選んでおり、仕様と一致している。**⇒ 欠陥ではなかった | `取下げ` | ⭐ **`D-212` を直す途中で見つかった。** | ⚠️ **どちらでも実装は 1 行である。** | ✅ **2026-09-03** —— 表 T-233 の `RS-1`／`RS-2` と `FR-060` を当たり、実装が `RS-1` を返すことを確かめた |
| D-223 | ⛔⛔ **`IC-90`（1 階層だけ開く）が、開かずに行を消す**（体の実測 2026-09-03、出荷ビルド）—— **`Mobile Client` を畳んだ状態でパネルは 7 行を y 85..265 に描くが、`IC-90` を押すと 4 行になる。`Back Office` ／ `Billing` ／ `Reporting` が消え、`Mobile Client` の畳んだ件数は 4 に増える。**⇒ **1 階層も開いていない。** | 表 T-051 の `HF-13` のとおり、配下が 1 階層だけ開くこと | ①`IC-90` の命令が何を畳み直しているかを追う | ✅ **`D-225` に吸収した（2026-09-03）** —— 原因は行の帯高に格子の下限が無いことであり、`IC-90` は「開かずに行を消す」のではなく、**押しが次の行の `IC-59`（隠す）に取られている**。⭐ 逐語で観測した機構は `D-225` の欄に移してある | `取下げ` | ⭐ **`parity` の板の門を越えられるようにした結果、初めて見えた。** | ⚠️ **`D-142`（何も畳まれていないときの `IC-58` ／ `IC-77` ／ `IC-90` が死んでいる）とは別** —— **こちらは畳まれた対象が在って、押すと逆のことが起きる。** | ⛔ 未 |
| D-224 | ⛔⛔ **`parity` の 25 手のうち 14 手が食い違い、どちらの名簿にも載っていない**（体の実測 2026-09-03）—— **11 手が一致、0 手が既知の不具合で説明され、14 手が説明の無い食い違いである（exit 1）。**⚠️ **板の門が止まっていたあいだ、この 14 手は一度も走っていなかった。** | 食い違う手が、既知の不具合の名簿か台帳の行のどちらかで説明されること | ①14 手を 1 つずつ当たり、行の在るものは結び、無いものは開く | ✅ **`D-225` に吸収した（2026-09-03）** —— ⭐⭐ **14 手すべてが 1 つの原因であった。**⛔ **「14 件の不具合」と数えなかったのは正しかった。**割り方の記録は `D-225` の欄が持つ | `取下げ` | ⭐ **体が基準も測った** —— **広げる手当てを外して門を跨ぐと 5/25 一致・20 手食い違いで、5 手は切られた名前だけが理由であった。**⇒ **`CR-336` の後のほうが良く、悪化ではない。** | ⚠️ **`parity` は `npm test` に含まれない** —— **別に回すこと。** | ✅ **2026-09-03** —— 11 一致 ／ 既知 0 ／ 説明の無い食い違い 14 を再現し、原因を 1 つに絞った |
| D-250 | ⛔ **`tests/contract/display-words.contract.test.ts` の冒頭が、在りもしない 8 行を除外し続ける**（反証の体の実測 2026-09-04）—— **「表 T-104 の 8 行は設定の面のどの欄にも当たらない」と書くが、本巡に両向きを数えたところ逆向きは 0 であった。** | 試験の冒頭が、いまの員数を述べること | ①数え直して書き換える ②員数を書かず、生成側に数えさせる | ⭐ **②が本筋である** —— **規則 05 の 3.9 は「数を書くな（表から読むか生成させろ）」と定める。**⚠️ **除外そのものが要るかどうかも確かめること** —— **逆向きが 0 なら、除外の仕組みごと落ちる可能性がある。**  ⭐⭐ **利用者の裁定 2026-09-04（逐語）**: 「**提案通りに進めよ。**」⇒ **検査 38 → 35 → 37 を先に入れ、検査 34 と 36 は基準線つきで入れる。**⛔ **基準線つきとは、いまの件数を記録し、それより増えないことだけを強制することである** —— **既存のごみを一度に直さずに済む。**⭐ **本行も 検査 34 が拾う類である。**⛔ **ただし員数を書き直すのではなく、生成側に数えさせること。** ⛔⛔ **反証の結果、主張は偽であった**（体の実測 2026-09-05）—— **除外している 8 行は 8 行とも 表 T-104 に実在する（`K-60` `K-62` `K-63` `K-64` `K-86` `K-90` `K-93` `K-101`）。在る 8 件・無い 0 件。**⭐⭐ **しかも除外は数を打ち込んでいない** —— **`SETTINGS_DEFAULTS`（実装）と 表 T-104（原稿）を毎回突き合わせて動的に計算しており、案②「生成側に数えさせる」は最初から実装済みであった。**⚠️ **突き合わせは目で数えず、表の読み手と試験の落とし方を Python で写して実測した**（表 T-104 116 行、`SETTINGS_DEFAULTS` 113 鍵、設定の面に出る 108 行、落ちる 8 行、孤児 0 件）。⇒ ⛔ **本行は取り下げる。**⚠️ **体が隣で 1 つ見つけた** —— **同ファイルの別のコメントが「113 鍵のうち 6 つ」と述べるが、実測では孤児は 0 件である。`D-238` が既に決着しており、その対応欄の「逆向きは 8 ではなく 0」がこの数の出どころと見られる。向きが逆の別の軸なので、本行では触れていない。** | `取下げ` | ⭐ **`D-145` を反証しに行った体が見つけた。**⛔ **本巡に 表 T-104 へ 7 行足したので、いま数え直す好機である。** |  | ✅ **2026-09-05（反証の体、Python で突き合わせ）** —— **名指しされた 8 行が 8 行とも実在することを数で示した。主張のほうが偽であった。** |
| D-252 | ⛔ **表 T-034 の `BT-2`（起動時に渡された文書）を渡す口が、実装に 1 つも無い**（反証の体の実測 2026-09-04）—— **`single-html-shell.ts:920` が `handed: { kind: 'none' }` を打ち込んでおり、`src/framework/` のどこにも `launchQueue` も `location.search` も無い。** | 渡された文書を受け取れること | ①宿主の受け口を配線する ②`BT-2` を仕様から降ろす | ⭐ **`D-124` から割れて出た** —— **あちらの問い（読めなかったときに告げるか）は「告げる」で決着した。本行は、そもそも渡せないという別の話である。**⚠️ **`BT-1`（ファイルに埋め込まれた文書）は生きており、実測で告げることも確かめた。**⛔ **利用者の裁定「デバッグ用に破損した日程を埋め込んでテストしろ」は `BT-1` で果たせている** —— **体は写しに仕込んで通した。**⇒ ⚠️ **`BT-2` を配線する必要が本当に在るかを、先に利用者へ問うこと。**  ⭐⭐ **利用者の対案 2026-09-04（逐語）**: 「**htmlのケースだろ？ html書き出し時に GitHub の本リポジトリから GRS を読み込んでデフォルト日程部分に日程を書き出す。 書き出したhtmlをダブルクリックすると、デフォルト日程として書き出した日程が開く。 これでどうか？意見くれたし**」⇒ ⭐⭐ **前に立つ者の意見（2026-09-04）: 望んでいる体験は既に仕様に在る。`IO-7`（単一 HTML の書き出し）と 表 T-034 の `BT-1`（埋め込まれた文書を開く）がまさにそれである。**⛔ **読む側（`BT-1`）は動く**（実測済）。⛔⛔ **書く側（`IO-7`）は仕様に在るが未配線である** —— `agent-api-members.ts:907` が自ら「NOT WIRED」と書いている。⇒ **要るのは `IO-7` を配線すること 1 つだけで、`BT-2` は要らない。**⛔⛔ **GitHub から本体を読む部分は採らない** —— **①アプリは既に自分の本体を読める**（`single-html-shell.ts:630` が起動時に保持し、`readAppShell` が返す。注が自ら「単一 .html から起動したアプリは自分自身の出典を読んでいる」と書く）**②`CN-1`「サーバーを必要とせず、ネットワークから切り離した状態で全機能が動くこと」と`CN-6`「実行時に外部へ通信しない。外部から取得する資源を持たない」に正面から反する。**⚠️ **利用者の返事待ち。** ⭐⭐ **利用者の裁定 2026-09-05（逐語）**: 「**②提案通りに進めろ。 ただし、PoCを回してからだ。**」⇒ **`IO-7` を配線し、`BT-2` は凍結して残す。**⭐⭐ **PoC を回した（2026-09-05、出荷ビルド、file://、1920×1080）。結果は合格である。**⛔⛔ **そして前に立つ者の申告が 2 つとも誤りであった** —— **①「`IO-7` は未配線」は偽。`frame-loop.ts:6587` が `exportEmbeddedHtml` を呼んでおり、書き出しの面は 「単一 HTML .html」を実際に出す。未配線なのはエージェント API の口だけである。②「`file://` では保存の器が無い」も偽。`showSaveFilePicker` は関数として在り、`isSecureContext` は真であった。最初に書き出せなかったのは、宿主の保存ダイアログが開いて道具が答えられなかったからである。**⭐ **測れたこと（宿主のダイアログは差し替え。⚠️ 示せるのは取っ手を持ったあとだけである）**: 書き出し 2,010,341 バイト ／ **CSP の script ハッシュは生き残る**（`sha256-7nod35…` が一致）／ 埋め込みの器に `GRS JSON` が入る ／ **文書は完全**（`tasks` 1000・`taskGroups` 100 で起動テンプレートと同数）／ 開き直して誤り 0 件・題も一致。⚠️ **描かれる量だけは違う**（699 → 60 の text）—— **文書は欠けていないので、倍率と表示位置の違いである。**⭐ **書き出したファイルは inline script が 2 本になる**（2 本目が埋め込み文書）。**`CSP` は 1 本目しか名乗らないが、`type` が実行形でないので止められない**（実測で開けた）。⇒ ⛔ **残る仕事は 2 つだけ** —— **①`BT-2` を「実装しない」と仕様に明記する ②`IO-7` に試験を付ける。**⚠️ **PoC が答えられなかったこと**: **人が実際に保存ダイアログを操れるか。`LM-14` が既に「`file://` では上書き保存が働かないことがある」と書いている。** ⭐⭐ **CR-350 で仕様に入った（2026-09-05）。**⛔⛔ **表 T-034 の `BT-2` に「この出どころは実装しない」を書いた**（利用者の裁定 2026-09-04）—— **いまの出荷形は単一の `.html` であり、ブラウザはディスクから直接開いた1 枚の HTML にファイル関連付けを与えない。**⭐ **求める体験は `IO-7` と `BT-1` が果たす**（2026-09-05 の PoC で往復を実測）。⭐ **席は残した** —— **順序（埋め込み が 渡された に勝つ）は設計判断であり、`OP-14` も名指している。宿主が文書を渡せる出荷形を将来作るなら、そのまま生きる。**⇒ **本行は裁定により閉じる。** ⛔⛔ **「`IO-7` に試験が 1 本も無い」も偽であった**（仕様だけを読む体の実測 2026-09-05）—— **`tests/unit/uf-37-38.test.ts` に往復の試験が既に在る。`exportEmbeddedHtml` で書き、器の中身を取り出し、`documentFromJson` で読み、元の文書と深く等しいことを、空・1 件・小・全体の 4 通りで見ている。その巡より前のコミットで入っている。**⇒ ⛔ **本行に書いた「残る仕事は 2 つ」のうち、②は最初から半分済んでいた。これで PoC の巡に前に立つ者が申告した 3 つのうち 3 つとも偽であったことになる。**⭐ **本当に欠けていたのは、書き出す側と起動時に選ぶ側をつないだ試験である** —— **`uf-23.test.ts` は選ぶ側に作り物の候補しか渡さず、`uf-37-38.test.ts` は選ぶ側を 1 度も呼ばない。`BT-2` の備考が言う「求める体験は `IO-7` と `BT-1` が果たす」はまさにその継ぎ目の主張である。**⇒ ⭐ **`tests/unit/io-7-bt-1-the-exported-file-reopens-itself.test.ts` を足した（6 本、緑）。** ⚠️ **体が自分で 2 つの主張を「仕様の逐語ではなく推論である」と申告した** —— **①`BT-1` が通ったとき通知が空であること（既存試験から受け継いだ形）②一度開いて書き直すと同じバイトになること（`FR-024` の理由と 表 T-075 の `pure` を継いだもの）。⛔ どちらも残した。**⛔ **書かれたファイルに生の `NUL` バイトが 1 つ混じっていた**（敵対的な文字列の試験のため。わざとではあるが原始バイトで書かれていた）—— **`grep` がそのファイルを binary と見なすので、逃がし記法へ直した。**⚠️ **体が見つけた仕様の沈黙**: **埋め込みの器の形（`script type="application/json"`、ちょうど 1 つ、在れば置き換える）は `docs/spec` の散文が決めていない。`06-pending-decisions.md` の `PD-70` / `PD-71` に未裁定のまま在り、試験 2 本がその形に構造として依っている。`PD-70` が覆るなら 2 本とも同時に動かすことになる。** | `取下げ` | ⭐ **`OP-14` は `BT-2` を名指して規則を持つので、仕様の側は `BT-2` が在る前提で書かれている。**⛔ **降ろすなら `OP-14` と 表 T-034 の両方を直すことになる。** | ⚠️ **体は「この確認には再ビルドが要る」と申告し、再ビルドせずに測れるところまでで止めた。** | ⛔ 未 |
| D-258 | ⛔ **`FR-086`（透かしに載せる名前）を書き込む道が実装に無い**（透かしを書いた体の申告 2026-09-05）—— **読む側は 表 T-206 の `S-99a` を見るが、そこへ書く者が誰も居ないので、どの読み手も既定値 `user` のまま描かれる。** | 開いた者の名前が、その人のものであること | ①名前を入れる面を作る ②既定値のままでよいと定める | ⚠️ **`GL-007`（撮影されたときの証跡）が求めるのは「誰が」であり、全員が `user` では跡にならない。**⭐ **読む側は実装済みである** —— **`NOT_STORED_WATERMARK_NAME` の既定値へ落ちる道は在り、空文字と未設定を同じ答えにしている（`FR-086` の MUST NOT）。**⛔ **足りないのは書く側だけである。** ⛔⛔ **実装しに行った体が、門の前で止まった（2026-09-05）。正しい判断である。**⭐⭐ **入口が仕様に無い** —— **体は表を丸ごと読んで数えた: 表 T-109（入口）87 行・表 T-036（鍵）23 行・表 T-234（問い）7 行・表 T-103（面）59 行。透かしの名前を入れる入口は 0 件である。表 T-016 も 0 件、辞書の透かしの語 16 件のうち名前を入れる面の見出しや札は 0 件。**⭐ **読む側は実物で効いていることを確かめた** —— **店に `grsched.openedBy` を手で書いて開き直すと、透かしの 1 つ目が その名前 に変わる。空文字と未設定は同じ答えになる（`FR-086` の MUST NOT が生きている）。**⭐ **`IC-41` を押すと出るのは `Watermark Unlock` の面**（透かし解除パスワード）**で、名前は書かれない。**⭐⭐ **同じことが `PD-181` に未裁定として既に在った** —— **「`S-99a` は『入力させ、`localStorage` へ保存すること』と定めるのに入口が無い」。**⛔⛔ **しかも仕様の中で 2 つの文が食い違っている** —— **`FR-086` は「閲覧者が求めたとき」（押されたとき）と書き、表 T-037 の `NT-4` は「透かしに出す名前の設定」を**起動時**の保留中の用件として挙げている。どちらが正かを決めないと入口の置き場が決まらない。**⇒ ⛔ **どちらを採るかは、利用者が決めることである。**⭐ **案①名前を入れる面を作る**（表 T-109 に入口・表 T-103 に面・辞書に見出しと札・`FR-086` にそれらの名指しを足す。`CR-329` が透かし解除パスワードのときに足したのと同じ組である）。⭐ **案②既定値のままでよいと定める** —— **`FR-086` の「入力させ」を取り下げる。コードは 1 行も要らない。⛔ ただし `GL-007`（撮影の証跡）が「誰が」を求めるので、全員が同じ名前で描かれることを受け入れることになる。**⚠️ **`RS-19`（名前がまだ設定されていない、を告げる語）は辞書に在るが実装に無い** —— **体は「設定する道が無いのに告げるのは黙っているより悪い」として意図して作らなかった。案①なら一緒に入れる。** ⭐⭐ **利用者の裁定 2026-09-05（逐語）**: 「**②でよい。 透かしはあとでやる。**」⇒ ⭐ **案②を採り、`CR-352` で仕様に入った。**⭐ **`FR-086` に「名前を入力させる道は、いまは実装しない」と、その代償（`GL-007` が求める「誰が」をいまは満たさない）を書いた。**⛔ **満たしているふりをしない。**⭐ **要求の席は残した**（`BT-2` と同じ形）—— **「あとでやる」ので、入口を足すときの書き先が要る。**⭐ **表 T-037 の `NT-4` から「透かしに出す名前の設定」を外した** —— **起動時に問う道は作らないので、挙げたままだと在りもしない用件を数える。⇒ 同時に `FR-086`（押されたとき）と `NT-4`（起動時）の食い違いも消えた。**⭐ **表 T-233 の `RS-19` に「いまは告げてはならない（MUST NOT）」を置いた** —— **設定する道が無いのに告げるのは黙っているより悪い。⛔ 語は消さない。あとでやるときに使う。**⛔⛔ **書いている最中に機械が 4 回鳴き、4 回とも正しかった** —— **①変更要求が `R2`〜`R7` の条項を名乗っていない（`R1` は数に入らない）②検査 39 が「MUST NOT を 1 つ足したのに留める試験が無い」と数えた（基準線を上げず、28 文字の下限に届く長さで留める試験を書いた）③表 T-233 の共変試験が「`RS-19` の場面が動いた。語と一緒に読み直せ」と要求した④仕様書が `PD-181` を名指していた（未裁定の行 ID は仕様の行 ID ではない）。**⚠️ **`RS-19` の語は変えていない** —— **次の一手（「名前を設定すると…」）はいま実行できないが、告げない以上だれも誤らないし、道ができた日に要る語である。指紋にその判断を書き添えた。**⚠️ **`PD-181` の残り 2 つ（`S-99b` と `S-99c`）は未裁定のままである。**⛔ **`src/` は 1 行も変えていない。実物は既に裁定どおりに動いている。** | `取下げ` | ⭐ **透かしを描いた体が、papering over せずに自分から申告した。** | ⚠️ **`D-195` / `D-257` と同じ透かしの話だが、別の軸である** —— **あちらは描くか・見えるか、本行は誰の名かである。** | ✅ **2026-09-05（出荷ビルド、1920 × 1080、file://）** —— **起動時 `localStorage` は 0 鍵、面も通知も 0、描かれる入口 116 個のうち名前を入れるものは 0。透かしは既定の名前で描かれる。⇒ 裁定どおりの姿である。** |
| D-261 | ⛔⛔ **`FR-025` の「PNG の倍率は 表 T-204 の `S-82` から選べるようにすること（MUST）」に対し、人が倍率を選ぶ入口が `src/` に 1 つも無い**（コードを読んだ体の申告 2026-09-05）—— **設定欄は値を文字で出すだけで操作子を持たず（`properties-panel.ts:1543` が `controls: []`）、書き出しの面は形式だけを出す（`open-modals.ts:198-205, 630-646`）。`setExportPngScale`（`CM-70`）を発行する箇所は `src/` に 0 件で、届くのは `Agent API` の任意命令経路だけである。** | 人が PNG の倍率を `S-82` の中から選べること | ①設定欄に操作子を付ける ②書き出しの面に倍率を出す ③要求から「選べるようにすること」を取り下げる | ⭐⭐ **意図して後回しにされている** —— **`properties-panel.ts:1534-1542` の STOP 注が自分で「この巡はこちら側に操作子を作らない」と述べている。⇒ 忘れられたのではなく、繰り越されたまま要求が MUST で残っている。**⭐ **同じ条項の残り半分は守っている** —— **幅を `S-81` に固定し、高さは絵が収まるまで伸ばす（`image-exporter.ts:524, 526-528, 546, 606`）。**⚠️ **`D-258`（透かしの名前）と同じ形である** —— **読む側は在り、書く側だけが無い。⇒ 案③を採るなら `FR-086` と同じく「いまは実装しない」と代償つきで書く。** | `取下げ` | ⭐ **取下げ 2026-09-06。**入口が無いことは欠陥ではなくなった —— **`FR-025` は利用者の裁定で「倍率を持ってはならない（MUST NOT）」となり、`S-82` も `CM-70` も退役した。選ばせる値が無い。** | ⚠️ **`Agent API` からは届く** —— **命令そのものは在り、人の入口だけが無い。** | ⛔ **まだ押していない。** |
| D-266 | ⛔⛔ **`Delete` と `Backspace` が、選んでいる `Task` を消さない**（前に立つ者が人として押した 2026-09-05）—— **表 T-036 の `SK-3` は「選択しているものを削除する / `Delete` / `Backspace`」と定めている**（`docs/spec/01-04-requirements.md:2509`）。**バーを押すと選択の枠（`fill=none` の矩形＋両端の 9 × 9 の掴み点）が描かれるので、選択そのものは成立している。**その状態で `Delete` を押しても `Backspace` を押しても、**バーもラベルも残り、通知も出ない** | 選んでいる `Task` が `Delete` または `Backspace` で消えること。消せないなら理由を告げること（`FR-029`） | ①鍵の経路を `SK-3` に繋ぐ ②選択の集合から消す命令を発行する ③消せない場面なら 表 T-233 に行を立てて告げる | ⚠️ **`SK-3` の対象の全数は 表 T-023c の `SL-1` が持つ** —— **行（`TaskGroup`）はその集合に入っていないので、行が消えないのは正しい。本行が問うのは `Task` である。**⭐ **行を消す道は別に在り（`IC-82`）、そちらは働いている** ⛔⛔ **取り下げる。本行は偽であった（2026-09-05、波 1 の体 A が反証し、前に立つ者が押し直して確かめた）。**⭐⭐ **`Delete` は効いている** —— **押すと `FR-032` の確認（`U-55`）が立ち、`Yes` と `No` の語のボタンが出る。`Yes` を押すと SVG の `text` が 748 → 733 へ減る。連鎖して消える範囲が大きいので、`FR-031` が「取り消しで取り戻せないものを失う場面」に確認を許した、そのとおりの振る舞いである。**⭐ **葉の `Task` を選んで押したときは確認が立たず、748 → 747 になる**（体 A の実測）。⛔⛔ **前に立つ者の測り方が悪かった** —— **`Notification Area` と `rect` の数だけを見て、`[role="dialog"]` と `[data-confirmation-answer]` を見なかった。⇒ 問いは「通知」ではないので、通知の欄には何も出ない。**⭐ **教えは 引継書 §8 へ下ろした**: **「押しても何も起きない」と言う前に、確認の問いが立っていないかを見ろ。** | `取下げ` | ⭐ **前に立つ者が、巡の頭で人として通しで押して見つけた。**⚠️ **`npm run parity` はこれを問えない** —— 見本は行の操作子だけを持ち、タスクバーを持たない | ⚠️ **`D-263` が引く仕様の実測注記（「`Delete` を押しても何も消えなかった」）と同じ場所を指している** —— **あちらは「描いた直後に選ばれていない」が直ったと述べているが、本行は「選んでいても消えない」であり、別の半分である** | ✅ **2026-09-05（出荷ビルド、`file://`、1920 × 1080、前に立つ者が押し直した）** —— **バーを (1500, 100) で押すと選択の枠が 1 本。`Delete` を押すと 300ms で `Yes` / `No` が立ち、3 秒待っても `text` は 748 のまま（⭐ 問いが書き込みを止めている）。`Yes` を押すと 748 → 733。**⚠️ **測った道具**: `scratch/push-29-delete-refute.mjs` |
| D-273 | ⛔ **担当者の名前を変えても、影響する件数を添えた通知が立たない**（978 本を反証した体の申告 2026-09-05、前に立つ者が裏を取った）—— **表 T-037 の `NT-3` は「破壊的な結果を伴うとき、対象の件数を添えること」と定め、例として「担当者名の波及」を名指している**（`docs/spec/01-04-requirements.md:3888`）。⭐⭐ **コードが自分で手放している** —— **`src/use-case/edit-document/edit-resource.ts:134-137` に「⚠️ The MUST to say how many tasks change with the name is NOT this file's: … NT-3 of table T-037 (FR-076) owns the notice and its count」と書いてある。**⛔ **その受け手が `src/` に無い** —— **`NT-3` の仕組み自体は `src/adapter/screen-renderer/notices.ts:597-598` に在るが、担当者名を変えた経路からそれを立てる所が 1 つも無い。** | 担当者名を変えたとき、影響する `Assignment` の件数を添えた通知が立つこと | ①改名の経路から `NT-3` の通知を立てる ②表 T-233 に担当者名の波及の理由行を足す ③`NT-3` の例から「担当者名の波及」を外す | ⛔⛔ **本行の主張の 1 つが偽である（実測 2026-09-06）** —— **「表 T-233 に担当者名の波及に当たる `RS-` の行が 1 つも無い」は偽である。**`RS-49`（担当者の名前が変わり、その担当者が付いているタスクの表示が変わった）が 2026-09-06 に `CR-364` で立っており、辞書の語も対の基準線も既に在る。⛔ **したがって `FR-029` の「当たる行があるのに落ち先を運んではならない」は既に効いており、選択の余地は無い** —— **造りは `RS-49` を使う。**⭐ **仕様の場所**: 表 T-037 の `NT-3` と 表 T-233 の `RS-49`。 ⛔⛔ **本行は 2 度反証された（実測 2026-09-07）。**① 「表 T-233 に行が無い」は偽 —— `RS-49` が `01-04-requirements.md:3949` に在る。② **告げる相手の行為そのものが仕様に無い** —— 表 T-225 の 10 行を 1 行ずつ読んだが、`CM-41`（`setResourceName`）を出す行が 1 つも無い（`AS-7` は `CM-40`＋`CM-44`、`AS-3` は `CM-45`、`AS-8` は小さい uid へ割当、`AS-10` は増やさない）。`FR-099` は「本要求が足すのは名簿の表示と削除だけである」と述べ、表 T-109 の `IC-62`〜`IC-68` にも改名は無い。⇒ **`CN-41` の入口が仕様に存在しない。**⛔ **いま通知を作れば、誰も行えない行為への告知になる。**⇒ **要るのは 表 T-225（または 表 T-109）に「どの操作が `Resource` を改名するか」の行 1 つである。**  ⇒ ⭐ **`PD-431` として立てた（2026-09-07）** —— 問いは「担当者の改名は、どの入口が行うか」である。候補は ①`AS-1` の担当ラベルのその場編集を「改名」と定める ②名簿（`FR-099`）に入口を足す ③`CM-41` を退役させ、`FR-008` の通知の条項も同時に落とす。⛔ **分類 `D`（`Resource.name` は文書に保存される）なので、推奨値で進めてはならない。**  ⭐⭐ **利用者の裁定 2026-09-07（逐語）: 「裁定はすべて提案通りでよい。」**⇒ **PD-431 が `裁定済` になった。⛔ 二度と問うな。次は仕様を書く番である。** | `取下げ` | ⭐ 利用者の裁定 2026-09-08、逐語: 「差し替えでOK。担当を変える場合はすでにプロパティーパネルから切り替え可能。削除も担当者一覧から削除可能。」⇒ 担当者を改名する入口は作らないと決まった。表T-225に一度足した`AS-11`は同日に退役し、`AS-7`は「担当者が就いていても同じように別人を作って差し替える(MUST)」に戻った。`CM-41`(`setResourceName`)に人の入口が無い状態は残るが、それは裁定の結果であって欠陥ではない。`Agent API`からは呼べるので、通知を求める条項が孤立してはいない。実物確認: `md-checks.py`のRETIREDに`AS-11`の席をburn済(理由つき)。検査exit 0。 ⭐ 反証 2026-09-08: ⭐ 「仕様待ち」の中身が判明した: 通知（`NT-3`）と理由行（`RS-49`）は**両方とも既に書かれている**。 ⛔ 欠けているのは「どの操作が `Resource` を改名するか」の行である —— 表 T-225 の 10 行（`01-04-requirements.md:1569` 付近）を逐語で読むと、 `AS-1` は欄を開くだけ、`AS-7` は名簿に無い名前で新しい `Resource` を作る、`AS-8` は既存の小さい uid へ割り当てる。 ⇒ 改名に落ちる行が 1 つも無い。 造りの側も同じ穴を持つ: `commandFromFieldCommit` の腕は task / taskVisual / taskGroup / commentBox / dependency / project の 6 つで、**assignee の腕が無い**（`FieldKey` の assignee は在り、`:7016` が `editInPlace` を出すのに、 確定した文字がどの命令にもならない）。 ⇒ 表 T-225 に「確定した名前が、そのタスクに就いている担当者の改名にあたるとき」の 1 行を足し、`CM-41` と `RS-49` を負わせる。 ステータスは `仕様待ち` のまま。  ⛔ **造りだけが未着手。**件数を数えるのは `src/use-case/edit-document/edit-resource.ts:134-137`、通知を立てるのは `src/framework/single-html-shell/frame-loop.ts` の `NoticeReason` と `raiseNotice` である。⭐ **`RS-51` が同じ形（件数を運ぶ理由）で既に通っているので、それに倩え。**⚠️ **前に立つ者は 2026-09-06 に `RS-52` を新設しかけて取り消した** —— **`RS-49` を探さずに台帳の主張を信じたためである。捉まえたのは対の基準線を見る契約試験である。** ⭐ 測定は `frame-loop.ts:8632` の既存の `MK-13` 担当ラベル STOP の中に書いた。⚠️ **その場編集の器が無いことは、これとは別の第 2 の門である** —— `ScreenView`（`screen-renderer`）と欄（`dom-screen-surface`）が要る。 | ⚠️ **`FR-099`（担当者の名簿）の周りは、この巡まだ人が押していない。** | ⛔ **まだ押していない。** |
| D-274 | ⛔ **プロパティパネルを出しているあいだに選択が動いても、中身が移らない**（前に立つ者が人として押した 2026-09-05）—— **`FR-072` は「プロパティパネルに出す中身を最後に行われた操作で決めること」と定め、「出しているあいだは、選択が動けば中身がそれに移る」と続けている**（`docs/spec/01-04-requirements.md`）。**行の名前をダブルクリックしてパネルを出すと 名称 / 色 / 高さ（表 T-016 の `PR-18` `PR-19` `PR-20`）が出る。そのままタスクバーを押すと選択の枠は描かれるのに、パネルは行の 3 項目のまま動かない。** | パネルを出しているあいだ、選択が `Task` へ動いたら中身も `Task` の項目へ移ること | ①選択の変化をパネルの中身へ繋ぐ ②`FR-072` の「移る」を取り下げる | ⭐ **仕様の場所**: `FR-072`（`docs/spec/01-04-requirements.md`）と 表 T-016。⭐⭐ **`Task` のパネル自体は在る** —— **バーをダブルクリックすると 21 の欄（名称・開始日/終了日・担当者・実績・完了率・期限・備考・再開・マイルストーン形）が出る。**⇒ **欠けているのは「出しているあいだに移る」道だけである。** ⛔ **`src/adapter/screen-renderer/properties-panel.ts` は原因ではない（実測 2026-09-07 で反証）。**⭐ **候補は 2 つで、どちらも framework 側である**: ① 渡される `Selection` にそもそも `Task` が入っていない（`frame-loop.ts:8713-8718` が `selectedGroupIds` を書く唯一の場所で、絵の上を押しても消していない） ② `dom-screen-surface.ts:6891` が `isFieldHeld` のあいだ描き直しを断る。  ⛔⛔ **反証された。不具合ではない（実測 2026-09-07、出荷ビルド）。**⭐ **プロパティパネルは選択に従って中身を移している** —— **行の名前をダブルクリックして `AT-53` / `AT-58` / `AT-59` の 3 欄が立ったあと、タスクバーを押すと `PR-1` ほか 16 欄へ移る（幅 280px のまま）。**⛔ **台帳が読み違えた理由も測れた** —— **パネルをしまったあとも、表面は最後の中身を幅 0 で DOM に残す。**⇒ **`textContent` だけを読むと、立っていないパネルの古い文字が見える。**⭐ **`Enter` でパネルが閉じるのは 表 T-036 の `SK-19` の 2 段目であり、そのあと `FR-072` の MUST NOT が「選択が動いても開き直さない」を正しく守っている。**⛔ **`selectedGroupIds` も `dom-screen-surface.ts` の `isFieldHeld` も原因ではない。** | `取下げ` | ⭐ **前に立つ者が、巡の頭で人として押して見つけた。** ⭐⭐ **実測（2026-09-07、`propertiesPanelFromSelection` を直接呼んだ）**: 行だけ ＝ `AT-53` `AT-58` `AT-59` の 3 行 ／ `Task` だけ ＝ `PR-*` 16 行 ／ **`Task` と行の両方が立っている ＝ `PR-*` 16 行のあとに `AT-53` `AT-58` `AT-59` の 19 行**。⇒ **引数をどう組んでも、`selection.items` に `Task` が入っているかぎり、このユニットが行の 3 行だけを答えることはない。**⭐ 条項は `properties-panel.ts:1759-1760` と `frame-loop.ts:9548-9550` の 2 か所で守られており、後者は 2026-08-30 から在る（本行を測った 2026-09-05 より**前**である）。⭐ `tests/unit/fr-072-a-moved-selection-does-not-open-the-panel.test.ts:744` が `Task`→`Task` の場合を既に緑で持っている。 | ⚠️ **`FR-072` は同じ段で「表 T-023c の選択が動いたことだけを理由に、パネルを出し始めてはならない（MUST NOT）」とも定めている** —— **禁じているのは出し始めることであって、出ているものが追随することではない。** | ⭐ **押した（2026-09-07、出荷ビルド）。両方の順序で測り、どちらも仕様どおりであった。** |
| D-279 | ⛔ **文書を置き換えるたびに確認を求めている。`FR-031` はそれを禁じている**（978 本を反証した体の申告 2026-09-05）—— **`FR-031` は「例外は、取り消しで取り戻せないものを失う場面に限る（MUST）。それ以外の場面で確認を求めてはならない（MUST NOT）」と定めている。****`src/framework/single-html-shell/frame-loop.ts:6262-6272` は置き換えのたびに必ず問う。**⭐⭐ **その場の注釈の言い訳（「読める保存済みの値が無い」）は古い** —— **`hasUnsavedEdits` が `frame-loop.ts:3555` に在り、`:5979` と `:6471` で保守されているのに、この分岐がそれを読んでいない。** | 未保存の編集が無いときは、置き換えで確認を求めないこと | ①`hasUnsavedEdits` を読んで分岐する ②`FR-031` の禁止を緩める | ⭐ **仕様の場所**: `FR-031` の RATIONALE（`docs/spec/01-04-requirements.md`）。⚠️ **直すのに要る値は既に在る** —— **書くのは分岐 1 つである。** ⛔⛔ **取り下げる。本行は偽であった（2026-09-05、波 2 の体 C が直しにかかり、既存の試験がその直しを落とした）。**⭐⭐ **`OP-4`（`docs/spec/01-04-requirements.md:3311`）は条件を付けていない** —— **「置き換えを選んだときは、捨てる前に確認を求めること（MUST）。黙って捨ててはならない（MUST NOT）。取り消しの履歴は引き継がない」。**⇒ **置き換えは常に問う。**⛔ **体は先頭の欄（`未保存の編集`）を条件と読んだが、それは何が失われるかを述べる欄であって、条件ではない。**⭐⭐ **`FR-031` の RATIONALE がそれを裏づける** —— **「取り消しの履歴ごと捨てる場合も…この階級に入る」と、履歴を捨てることを名指しで許している。置き換えは必ず履歴を捨てるので、未保存の編集が無くても問う場面である。**⛔⛔ **直しを当てたら `tests/unit/uf-47-48-choosers.test.ts` の 7 件が落ちた** —— **そのうち 1 件は `OP-4` の MUST NOT を逐語で留めている。⇒ 戻り止めが働いた。**⭐ **合流点で `npm test` を走らせたので捕まった。**⛔ **体に走らせていたら、体は自分の直しを緑と報告していた。** | `取下げ` | ⭐ **`share 07` / `share 08` の `A` 179 本を 1 本ずつ反証した体が挙げ、前に立つ者が `grep` で裏を取った。** | ⚠️ **注釈が偽になっていた形である** —— **`D-263` と同じ「事実のほうが動いた」。** | ✅ **2026-09-05 —— 直しを当てて単体試験が 6615 → 6608 緑 / 7 赤になり、戻した。**⛔ **実物では押せない** —— **`askToDiscardCurrentDocument` の呼び手は開く道 1 つだけで、`file://` には `showOpenFilePicker` の安全な文脈が無い**（体 C の実測）。 |
| D-283 | ⛔ **コメントボックスの本文を編集する入口が繋がっていない**（978 本を反証した体の申告 2026-09-05、前に立つ者が裏を取った）—— **命令 `CM-48`（`setCommentBoxText`）は `src/use-case/edit-document/edit-annotation.ts:78` `:256` に在るが、`src/adapter/input-command-translator/input-command-translator.ts:726-730` が「CM-48 is its own piece of work; the kind is left out until that work is …」と書いて出していない。**⇒ **人がコメントボックスに文字を入れる道が無い。** | コメントボックスの本文を人が編集できること | ①`MK-13` の道から `CM-48` を出す ②`FR-097` の該当部分を代償つきで取り下げる | ⭐ **仕様の場所**: `FR-097` と 表 T-023 の `MK-13`（`docs/spec/01-04-requirements.md`）。⭐⭐ **`D-261` `D-271` `D-278` と同じ形である** —— **命令は在り、人の入口だけが無い。この巡だけで 4 件目であり、これは 1 件ずつの不具合ではなく 1 つの型である。** ⭐ **本物である。⛔ 本波の持ち分の外だったので手を付けていない（2026-09-06）。**⭐ **条項**: **表 T-023 の `MK-13`「コメントボックス ＝ 本文の編集」と、`FR-097`「入口は 表 T-023 の `MK-13` に従うこと（MUST）」。**⛔⛔ **道が 2 つとも塞がっている（体が測った）** —— **① パネルの道: `property-items.json` は 19 項目を持ち、コメントボックスの行は**無い**。足すのは原稿の変更であり、発明である。② その場の道: 殻に「日程表の上の編集できる欄」を描く仕掛けが無い（担当者ラベルが同じ穴で待っている）。**⚠️⚠️ **文書名の欄は先例にならない** —— **あれはヘッダが常に描く**唯一**の欄（`U-27`）であり、「どの項目の欄か」を名乗る行 ID を要さない。**⇒ ⭐ **殻に汎用のその場編集器ができた瞬間、翻訳側は 6 行で済む。** ⛔ **2 つの道が両方とも閉じていることを、体が別々に確かめた（2026-09-06）** —— **① プロパティパネルの道: `docs/spec/_source/property-items.json` の 19 項目にコメント欄は無く、`comment` の語が 0 件。表 T-016 はタスクの属性表なので、行を足すのは原稿の変更である。② その場編集の道: **その場で書き換える器がこの造りに 1 つも無い**（担当者ラベルの件と同じ壁）。** ⭐ **`FR-097` は入口を 表 T-023 の `MK-13` に従えと定め、`MK-13` は「コメントボックス ＝ 本文の編集」と書くだけで、**どこに書くのかを両方とも書いていない**。⇒ 場所を決めるのは発明になるので、体は決めなかった。正しい。** ⭐ **誤解を招く注記を、閉じた 2 つの道と、その場編集の器が来たとき本側が負う仕事を名指す注記に替えた。** ⭐⭐ **裁定された（2026-09-06、`CR-367`）** —— **案 ⓐ（プロパティパネルに本文欄を足す）。**⭐ **根拠は一貫性である** —— **同じ 表 T-023 の `MK-13` の中で、タスクも行見出しも既にパネルへ行っており、付箋だけ別の器官を要求する理由が仕様に無い。**⇒ ⭐ **原稿（`_source/property-items.json`）に 1 行足して生成し直す仕事である。新しい器官は作らない。**⚠️ **その場編集器の話は消えていない** —— **担当ラベルの改名（`D-273`）が同じ器官を待っている。作るなら 2 件まとめて別の行で。** ⛔⛔ **押して否と出た（2026-09-07、出荷ビルド）。本行の「造りは着地した」は偽である。**⭐ **`CM-48` 自体は生きている** —— `Agent API` の `applyCommands({kind:'setCommentBoxText'})` は `accepted: true` で本文が入る。⛔ **無いのは `MK-13`（MUST）が言う「パネルを出し、`PR-21` に焦点を置く」入口である。**⇒ **人は本文を編集できない。**⭐ **書く側も塞がっている** —— `input-command-translator.ts` の `commandFromFieldCommit` に `commentBox` の腕が無いので、欄で確定した値はどの命令にもならない。 | `取下げ` | ⚠️ 疑いを戻す(2026-09-08): `取下げ`にしたのは早すぎた疑いが濃い。反証は「入口・書き戻し・面の3つとも在る」と確かめたが、殻の`editInPlace`に`commentBoxText`の枝が無いことがのちに判明した(assigneeと同じSTOPに落ちている)。走行中の体が出荷ビルドを押して判定する。その報告が来るまでステータスを動かすな。 ⭐ 反証 2026-09-08: コメントボックス本文の編集は既に通っている。反証 2026-09-08: 引出し口 `input-command-translator.ts:7027`（`GR-14` のダブルクリック → `editInPlace/commentBoxText`）、 書き戻し 同 `:3779`（`commandFromFieldCommit` の `commentBox` の腕 → `setCommentBoxText`）、 面 `docs/spec/_source/property-items.json` の `PR-21`（`appliesTo: "CommentBox"`）。 ⇒ 行が「無い」と書いた 3 つはすべて在る。  ⭐ **造りは着地 2026-09-06。**`input-command-translator.ts` の `commentBox` の腕が `setCommentBoxText`（`CM-48`）を出す。試験は緑。⛔ **出荷ビルドではまだ押していない。** ⭐⭐ **実測（2026-09-07、出荷ビルド `file://`）**: 箱は置ける（`commentBoxes` 1 件、`anchorDate` `2026-07-26`、`text` は `null`）。本文の箱は `svg rect (896,471,20,20)` で、1 クリックすると `readSelection` ＝ `{kind:'commentBox'}`。⛔ **ダブルクリックの後、`Properties Panel` は 幅 0 ／ `[data-field-row]` 0 件 ／ 焦点 `BODY` ／ `PR-21` の値 `null`。**⭐ **対照**: 同じ手をタスクのバーに打つと 幅 **280** ／ 行 **37 件** ／ 焦点 **`INPUT:PR-1`**。⚠️ `IC-17` を押すとパネルは開くが、出るのは文書の描画設定（`K-nn` 112 行）で `PR-21` は無い。⚠️ **`D-349`（欄が操作子を持つこと）は別に閉じている** —— 欄そのものは `PR-21` として正しく描かれる。本行は**そこへ至る道**である。⭐ 証跡は `docs/development-records/evidence/measured-2026-09-07.md`。 | ⚠️ **箱そのものは置ける**（`IC-35` は構えられ、`D-06` の錨がそれを留めている） | ⛔ **押した。ダブルクリックでは開かない（2026-09-07）。** |
| D-285 | ⛔ **掴めない端点を薄く描き、理由をツールチップで示す道が無い**（978 本を反証した体の申告 2026-09-05、前に立つ者が裏を取った）—— **`FR-029` の第 1 文が「掴めない端点を薄く描いて理由をツールチップで示すこと（MUST）」と定めている。**⛔ **`grep -c "FR-029" src/adapter/svg-renderer/svg-renderer.ts` は 0 である。****`src/adapter/screen-renderer/tooltips.ts:76-78` にも端点の理由は無い。** | 掴めない端点が薄く描かれ、乗せると理由が出ること | ①端点を描く所で掴めるかを判じ、薄くする ②理由の語を辞書へ足す | ⭐ **仕様の場所**: `FR-029` の第 1 文（`docs/spec/01-04-requirements.md`）。⚠️ **`D-265` `D-281` と合わせて、`FR-029` は 1 条項で 3 か所破れている。**⇒ ⭐ **1 つずつ直すより、この条項をまとめて当てるほうが安い。** ⭐⭐ **調べた（2026-09-05、波 1 の体 B）。⛔ 直せなかった。理由は仕様の側に在る。**⭐ **対象は決まった** —— **「バーの端の掴み点」（`GR-3` / `GR-4` / `GR-5` / `GR-6`）である。根拠は `docs/spec/01-04-requirements.md:350` の `A-13`「単独で書いたら**バーの端の掴み点**（全形状が持つ）…フェードの掴み点（`GR-1` / `GR-2`）は別物」。`FR-029` は「端点」と単独で書いている。**⛔⛔ **その端点が「掴めない」場面を定める行が、仕様に 1 つも無い** —— **表 T-023d の全行、`FR-029` の全文、「薄く描」の 13 か所、表 T-233 の `RS-1` 〜 `RS-46` の 44 行を掃いて 0 件。**⛔⛔ **さらに `FR-029` が自分と食い違っている** —— **第 1 文は「理由を**ツールチップ**で示す（MUST）」と言い、後段は「ポインタが乗っただけで理由を出してはならない（MUST NOT）」（利用者の裁定 2026-08-30）と言う。**⇒ ⛔ **語を 1 つも決められない。`PD-429` を立てた。** ⭐⭐ **裁定は 2026-09-05 に下りていた（`CR-356`、利用者の逐語「裁定 7: 提案通り」）。⛔ 本行だけが `裁定待ち` のまま残っていた。**⭐ **問い A ＝ ⓐ（掴めない端点は無いと定める）／問い B ＝ ⓧ（理由は押されたときに告げる）。**⭐ **仕様にも当たっている（2026-09-06 に確かめた）** —— **`FR-029` の第 1 文は外され、外した理由 2 つが実測として本文に残っている。** ⛔⛔ **本行が名指していた条項は、裁定により**削られた**。⇒ ⭐ 直す対象が無くなったので取り下げる。**⚠️ **掴めない端点の場面が将来出てきたら、そのときに定める（`CR-356` の逐語）。** | `取下げ` | ⭐ **`share 07` / `share 08` の `A` 179 本を 1 本ずつ反証した体が挙げ、前に立つ者が `grep` で裏を取った。** | ⚠️ **端点は 表 T-023d の `A-13`（端点スパン）と `GR-1` / `GR-2`（フェードの掴み点）で別物である** —— **どちらの話かを先に決めること。** | ✅ **2026-09-06（`docs/spec/01-04-requirements.md` を読んで確かめた）** —— **「掴めない端点」は 3 か所に残るが、3 つとも「2026-09-05 まで本文はこう続けていた」という**記録**の中である。⛔ 命じている行は 1 つも無い。**⭐ **押す対象そのものが仕様から消えたので、押して確かめる物は無い。** |
| D-287 | ⛔⛔ **中身のない行が 1 つあると、取り込みがファイルごと落ちる**（978 本を反証した体の申告 2026-09-05、前に立つ者が裏を取った）—— **`EX-5` の除外の枝は `src/use-case/validate-imported-document/validate-imported-document.ts:501` `:522` に在るのに、`src/framework/single-html-shell/frame-loop.ts:6193` が `emptyRowTaskUids: []` を直値で渡すので集合が常に空になる。**⭐ **その場の STOP が自白している**（`frame-loop.ts:6188`「⛔ NO CODEC ANSWERS THIS」）。⇒ **中身のない行が 1 つあると `FR-012` の拒否が立ち、取込が落ちる。** | 中身のない行は禁止の対象外とし、ファイルごと拒まないこと | ①コーデックが中身のない行の `uid` を返す ②取込の入口でそれを数える ③`EX-5` の除外を取り下げる | ⭐ **仕様の場所**: `EX-5` と `FR-012`（`docs/spec/01-04-requirements.md`）。⚠️ **`D-275`（拒否が誰にも届かない）と同じ経路である** —— **落ちたことすら人に伝わらない。** ⛔⛔ **取り下げる。本行は偽であった（2026-09-05、波 2 の体 C の反証）。**⭐⭐ **`emptyRowTaskUids: []` は穴ではなく正しい答えである** —— **`MSPDI` のコーデックが `EX-5` を既に果たしている。`tasksFromRoot`（`src/adapter/document-codec/mspdi-codec.ts:1315`）が `Task/IsNull` を`Task` になる前に `carriedRows` へ逸らすので、中身のない行は `schedule.tasks` に届かず、`validate-imported-document.ts:522` が掃く対象がそもそも存在しない。**⇒ **除外の集合が空なのは、母数が空だからである。**⭐ **`tests/unit/uf-36.test.ts:1115` が既にこれを留めている**（`schedule.tasks` に UID 90 が無い）。⚠️ **`frame-loop.ts:6188` の STOP（「⛔ NO CODEC ANSWERS THIS」）は、その注釈のほうが古い。**⭐ **`D-263` と同じ「事実のほうが動いた」形である。** | `取下げ` | ⭐ **`share 03` / `share 04` の `A` 189 本を 1 本ずつ反証した体が挙げ、前に立つ者が `grep` で裏を取った。** | ⚠️ **他所の道具が書いた `MSPDI` には、中身のない行が普通に入る。** | ⛔ **押す必要が無い** —— **母数が空であることは、単体試験が留めている。** |
| D-296 | ⛔ **画面の上に倍率の入口が無い。表 T-109 にも行が無い**（978 本を反証した体の申告 2026-09-05）—— **`docs/spec/_source/icon-roster.json` の 88 行に `FR-016` を正とする行が 0 件である。**⇒ ⛔ **これはコードの穴であると同時に仕様の穴でもある。** | `FR-016` が求める倍率の入口が、表 T-109 に行を持ち、画面に在ること | ①表 T-109 に行を足して実装する ②既存の `IC-12` 〜 `IC-15` が `FR-016` を果たすと仕様に書く | ⭐ **案②が有力である** —— **ヘッダーには `IC-12`（横に縮小）`IC-13`（横に拡大）`IC-14`（縦に縮小）`IC-15`（縦に拡大）が実在し、前に立つ者が押して効くことを測った（`scratch/push-4-quiet.mjs`、行の高さが 148 → 138 → 148 と動く）。**⇒ **足りないのは実装ではなく、`FR-016` と 表 T-109 の対応づけである。**⭐ **仕様の場所**: `FR-016` と 表 T-109（`docs/spec/_assets/tbl-glossary.md`、原稿は `docs/spec/_source/icon-roster.json`）。 ⛔⛔ **取り下げる。前提が 2 つとも偽であった（2026-09-06 に測った）** —— **① `FR-016` は倍率の入口を求めていない。本文は「ポインタとキーボードの操作を 表 T-023 の割当で受け付けること」と「倍率を `S-75` / `S-76` の範囲へ収めること（MUST）」であり、入口の要求ではなく**割当と丸めの規則**である。② 倍率の入口は 表 T-109 に**在る** —— `IC-10`（全体表示）・`IC-12`〜`IC-15`（時間軸と行軸の拡大縮小）がすべて `App Header` の行として立っており、正は `FR-018` / `FR-055` である。⭐ しかも 5 つとも 2026-09-06 に実際に押して動くことを確かめてある。**⚠️ **本行が数えた `icon-roster.json` は、その道に存在しない。** | `取下げ` | ⭐ **`share 03` / `share 04` の `A` 189 本を 1 本ずつ反証した体が挙げ、前に立つ者が `grep` で裏を取った。** | ⚠️ **体は「入口が無い」と申告したが、前に立つ者が押したら在った** —— **無いのは名簿の行である。** | ✅ **2026-09-06（表 T-109 を読み、出荷ビルドで 5 つとも押した）** —— **入口は在り、絵も動く。** |
| D-298 | ⛔ **スクロールバーのつまみが、溝のどこから始まるかを決められない**(体が2026-09-08に題を差し替え) | スクロールバーの摘みを掴んで画面を送れること | ①摘みの大きさを見えている割合から出す ②押しと引きを受ける | ⭐ **仕様の場所**: `FR-052` と 表 T-103 の `Scrollbars`（`docs/spec/01-04-requirements.md`）。⚠️ **前に立つ者は横方向の車輪でも画面が動かないことを測った**（`scratch/push-13-create.mjs`、車輪 40 回で図が 1 節点も動かない）。⇒ **送る道が 1 つも無い可能性がある。押して確かめること。** ⭐⭐ **仕様の穴は塞いだ（2026-09-07、前に立つ者）** —— 表 T-023d に **`GR-21`（`Scrollbars` のつまみ）**を立てた。⭐ **長さは「見えている範囲 ÷ 全体」の割合とし、下限を `S-205` とする** —— **新しい設定値を 1 つも作っていない**（帯の太さの下限と同じ値を使い、最小のつまみを正方形にする）。⚠️ **つまみの外の帯を押したときの振る舞いは、まだ定めていない。**  ⭐⭐ **掴んで動かす道は着いた（2026-09-07）** —— **`ScreenPart` に軸を答える欄を足し、`input-command-translator.ts` が帯の掴みを送りに変える。**⭐ **歯車比は `GR-21` の 2 つの条項から導いた** —— **見えている範囲 ÷ 全体のつまみがポインタに追従して帯を走るなら、絵は全体を走る。**⇒ **新しい設定値を 1 つも作っていない。**⛔⛔ **つまみの長さだけが残る** —— **`UF-61` は `ScreenRegions` / `DocumentSettings` / `ScreenState` しか渡されず、「見えている範囲 ÷ 全体」に要る `ScheduleLayout` の全体の大きさが届かない。`components.json` は `ScreenRenderer` から `ScheduleLayout` への辺を持たない。**⇒ **仕様の穴は塞がったが、配線が無い。⭐ 要るのは `ScreenSession` の欄 1 つ（殻が埋める）か、新しい component の辺（仕様の変更）である。⛔ 体はどちらも発明しなかった。**⛔ **つまみの外の帯を押したときの振る舞いは、`GR-21` が今も定めていない**（STOP を残した）。⭐⭐ **横ホイールの前提は反証された** —— **修飾なしの横回しが何も動かさないのは正しい**（表 T-023 は修飾なしに縦スクロールしか与えていない）。**`Ctrl+Shift` では両軸とも動く。** | `実装待ち` | ⚠️ 旧題(2026-09-08に差し替え): ⛔ **スクロールバーのつまみの長さが、見えている範囲の割合になっていない**（反証 2026-09-08 に題を差し替え。旧題と経緯は詳細状況欄）。 ⭐ 反証 2026-09-08: ⛔ 題を書き換える: **「スクロールバーのつまみの長さが、見えている範囲の割合になっていない」**。 反証 2026-09-08: 掴んで動かす造りは在る —— `input-command-translator.ts:4462` `scrollbarFollow` / `:5163` `commandFromScrollbar`、歯車比 `:4416` `scrollGearing`。 `tests/unit/t-023d-follows-the-pointer.test.ts` は 69 件すべて緑。 ⛔ 本物は `screen-frame.ts:171` の `return { axis, track, thumb: track }` —— つまみが溝いっぱいのままである。 同 `:138` 付近の STOP が理由を持つ（`GR-21` の「見えている範囲 ÷ 全体」に要る全体量が `UF-61` の引数に届かない）。 ステータスは `実装待ち` のまま。  ⚠️ 旧題（2026-09-08 に差し替え）: ⛔ **スクロールバーを掴んで動かせない**（978 本を反証した体の申告 2026-09-05）—— **`src/adapter/screen-renderer/screen-frame.ts:123-125` で摘み（thumb）が溝（track）と同じ大きさになり、押す道が 0 件である。**  ⭐ **`share 03` / `share 04` の `A` 189 本を 1 本ずつ反証した体が挙げ、前に立つ者が `grep` で裏を取った。** ⛔ **実測 2026-09-07**: `screen-frame.ts:137` はいまも `thumb: track` を返す。⛔ **原因の本体は「つまみの大きさ」ではなく「掴み領域が仕様に無かったこと」であった** —— 表 T-023 の `MK-9a` が 表 T-023d を掴み領域の**全数**と宣言しているので、行が無いかぎり、押しをスクロールに変えるユニットは読む先を持たない。⇒ **`GR-21` が立ったので、`src/adapter/input-command-translator/` に当たり判定を書ける。**⚠️ **`S-205` は帯の太さの下限であって、つまみの大きさではない**（`single-html-shell.ts:559` は正しく守っている）。 ⛔⛔ **`src/adapter/screen-renderer/screen-frame.ts:118-133` の STOP が、書かれた当日に古くなった（実測 2026-09-07）** —— **同注は「表 T-023d に帯の行が無い」ことを理由につまみの大きさを決めるのを断っているが、`GR-21` は同じ日に立った。**⇒ **塞いでいたものは無くなっている。**⭐ **`scrollbarIn` はいまも `{ axis, track, thumb: track }` を返す** —— **どの文書の大きさでもつまみが帯そのものであり、割合も `S-205` の下限も無い。**⭐ **2 本の試験が同じことを別の側から言っている**（`t-023d-follows-the-pointer` と `uf-30-31` が写しの取りこぼしで赤くなった）。  ⭐⭐ **体が押して測った（2026-09-07）—— 本行の「まだ無い」は実測で真である。**`scrollbarThickness: 14` の環境で、縦の帯は `track` も `thumb` も `{x:1176, y:48, width:14, height:628}` —— **つまみが帯を丸ごと埋めており、掴む握りが無い。**その中央を押して 120px 動かしても、離しても、絵も文書も 1 バイト動かない。⛔ **押しを `GR-21` として報せる道がそもそも無い** —— `src/entity/layout-engine/item-hit-area/item-hit-area.ts:90` の `GrabArea` は `GR-1` 〜 `GR-18` だけであり、`ScreenRegions` も帯に矩形を与えていない。⭐ **留めは書いてある** —— `tests/unit/t-023d-follows-the-pointer.test.ts` の節 (e) の 4 件が、**`GR-21` が実装された日に赤くなる。** | ⚠️ **`Scrollbars` の `data-role` は 2 つ在る**（前に立つ者が実測） | ⭐ **押した（2026-09-07、出荷ビルド、内容が `Row Area` を超える倍率で）** —— **新しい枝を切ると 100px の引きで絵が 1 バイトも動かず、入れると両軸とも動いた。** |
| D-302 | ⛔ **コメントボックスの引出し（リーダー）を選ぶ入口が無く、引出しそのものも描かれない**（978 本を反証した体の申告 2026-09-05）—— **`docs/spec/_source/property-items.json` に `leaderShapeKind` が 0 件で、`src/adapter/svg-renderer/svg-renderer.ts:2066-2074` が「引出しは描かない」と自白している。** | コメントボックスの引出しが 2 種から選べ、描かれること | ①表 T-016 に項目を足して描く ②引出しの要求を取り下げる | ⭐ **仕様の場所**: `FR-097` と 表 T-016（`docs/spec/01-04-requirements.md`、原稿は `docs/spec/_source/property-items.json`）。⚠️ **`D-283`（本文の編集）と同じコメントボックスの話である** —— **箱は置けるが、中身も引出しも無い。**  ⇒ ⭐ **`PD-436` として立てた（2026-09-07）** —— 問いは 2 つに割れる: **① `calloutBox` と `polyline` が幾何としてどう違うか（⛔ 図が要る） ② 選ぶ面はどこか（`PD-435` と同じ形であり、表 T-016 に行を足すなら一貫する）**。⛔ **①を決めずに②だけ決めても、描くものが無い。**⚠️ **`X2` の踏査が `setCommentBoxLeaderShapeKind`（`CM-49`）を本行に集約した** —— 新しい行を立てていない。  ⭐⭐ **利用者の裁定 2026-09-07（逐語）: 「裁定はすべて提案通りでよい。」**⇒ **PD-436 が `裁定済` になった。⛔ 二度と問うな。次は仕様を書く番である。** | `実測待ち` | ⭐ 直した(2026-09-08): `svg-renderer.ts`のSTOPを閉じ、錨の点から本体の箱の左下角へ1本の線を描くようにした。本体より先に`push`するので、塗りが線の端を覆う。「選ぶ入口が無い」という指摘は、既に裁定済みの提案(1種に絞ったので選ばせない、と決まったもの)で決着している。わざと壊した実測: `push`を消すと2件、本体の上左へ向けると1件が赤。 ⭐ 反証 2026-09-08: ⛔ 2 つの主張に割る。 ① **「引出しを選ぶ入口が無い」は もはや欠陥ではない** —— `PD-436` が「引出しは折れ線 1 種に絞り、 2 種から選ばせることを取り下げる」「`leaderShapeKind` の列と enum は退役させる」と裁定済。選ばせないのだから入口は要らない。 ② **「引出しそのものが描かれない」は TRUE** —— `svg-renderer.ts:2469` の STOP 「THE LEADER IS NOT DRAWN. AT-111 gives a comment box two leader…」。 ⚠️ 描き方の条項は無い ⇒ ステータスは `仕様待ち` のまま。 ⭐ `leaderShapeKind` の広がりを測り直した（生成物 `docs/spec/output/strictdoc/**` を除く）: **30 ファイル**。 集合の名: src 3（`document-codec/json-codec.ts` / `entity/document-model/schedule/schedule.ts` / `use-case/edit-document/edit-annotation.ts`）、tests 14（contract 2 ＋ unit 12）、 docs/spec 4（`A-appendix.md` / `_assets/fig-erd-detail.md` / `_source/erd.json` / `_source/grs-document.schema.json`）、 docs/development-records 4、docs/review 3。⇒ 退役が動かすのは実質 21 ファイル（src+tests+spec）。  ⭐ **`share 05` / `share 06` の `A` 192 本を 1 本ずつ反証した体が挙げ、前に立つ者が `grep` で裏を取った。** ⭐ **形の道は開いた（2026-09-06）** —— **`COLUMN_SHAPES.CommentBox.leaderShapeKind` が `enum ['calloutBox', 'polyline']` を持つ。**⛔⛔ **止まっているのは仕様の行である** —— **`_source/property-items.json` に `leaderShapeKind` の行が無く、`FR-006` の MUST NOT（対象の違う行を出してはならない）によりパネルは出せない。**⭐ **要るのは 2 つ** —— **① 原稿に 1 行（対象 `CommentBox` ／ 列 `leaderShapeKind` ／ 入力の型 `選択`）。② `svg-renderer.ts:2066-2074` の引出しの描画。** ⭐⭐ **裁定された（2026-09-06、利用者。逐語）** —— **「付箋はアンカーとなる場所を1点決める そこの日付を基準に付箋を付ける。付箋の基準はは左下の点とする。」**⛔⛔ **これは引出し線の形の話ではない。付箋の留め方の裁定である** —— **付箋は 1 点のアンカーを持ち、その点の日付を基準に置かれる。付箋の側の基準点は左下である。**⇒ ⛔ **先に確かめること**: **いまの原稿が付箋に持たせている `anchorDate` / `anchorGroupId` / `bodyOffsetPx` が、この裁定と合っているか。合っているなら足りないのは描画だけである。**⚠️ **引出し線の 2 つの形を選ばせるかどうかは、この裁定は述べていない。** ⭐⭐ **裁定の 3 点を 1 つずつ測った（体が測った 2026-09-06）** —— **① 「アンカー 1 点」＝ **合っている**（`FR-019` 逐語「コメントボックスは 1 点に置くもので、囲う範囲を持たない」）。② 「その点の日付が基準」＝ **合っている**（`anchorDate` ＋ `anchorGroupId` の 1 組しか無い）。③ 「付箋側の基準は左下」＝ ⛔ **違う**。**⛔ **どの表にも本文の基準隅を定める行が無く、造りは左上を採っている** —— **`schedule-geometry.ts:1626-1631` が本文を `{x: anchor.x + dx, y: anchor.y + dy}` として置き、`svg-renderer.ts:2152-2154` がそれを矩形の左上として描く。**⚠️ **留める点そのものも行の中心である**（`schedule-geometry.ts:1618`、`@provisional PD-233`）。⇒ ⛔ **足す仕様は「`bodyOffsetPx` は留めた点から本文の**左下**へのずれとする」である。**⚠️⚠️ **引出し線は裁定が触れていない** —— **`leaderShapeKind` は `calloutBox` と `polyline` の 2 値を持つが、**どちらが何として描かれるかを定めた行が、どの表にも無い**（描く側が STOP でそう申告している）。** | ⭐ 反証 2026-09-08: ⛔ 2 つの主張に割る。 ① **「引出しを選ぶ入口が無い」は もはや欠陥ではない** —— `PD-436` が「引出しは折れ線 1 種に絞り、 2 種から選ばせることを取り下げる」「`leaderShapeKind` の列と enum は退役させる」と裁定済。選ばせないのだから入口は要らない。 ② **「引出しそのものが描かれない」は TRUE** —— `svg-renderer.ts:2469` の STOP 「THE LEADER IS NOT DRAWN. AT-111 gives a comment box two leader…」。 ⚠️ 描き方の条項は無い ⇒ ステータスは `仕様待ち` のまま。 ⭐ `leaderShapeKind` の広がりを測り直した（生成物 `docs/spec/output/strictdoc/**` を除く）: **30 ファイル**。 集合の名: src 3（`document-codec/json-codec.ts` / `entity/document-model/schedule/schedule.ts` / `use-case/edit-document/edit-annotation.ts`）、tests 14（contract 2 ＋ unit 12）、 docs/spec 4（`A-appendix.md` / `_assets/fig-erd-detail.md` / `_source/erd.json` / `_source/grs-document.schema.json`）、 docs/development-records 4、docs/review 3。⇒ 退役が動かすのは実質 21 ファイル（src+tests+spec）。  ⚠️ **一緒に直すのが安い。** | ⛔ **まだ押していない。** |
| D-303 | ⛔ **隠した依存線を戻す道が無い**（978 本を反証した体の申告 2026-09-05）—— **`src/entity/layout-engine/schedule-geometry/schedule-geometry.ts:1696` が `dependencyVisible` で幾何を空にし、`src/adapter/input-command-translator/input-command-translator.ts:2263-2271` の `everythingSelectable` はその幾何を歩くので選択もできない。別の面も無い。** | 隠した依存線を、人が戻せること | ①隠したものを載せる面を作る ②幾何を空にせず薄く描く ③隠す機能を取り下げる | ⭐ **仕様の場所**: `FR-013` と 表 T-206 の `dependencyVisible`（`docs/spec/01-04-requirements.md`、原稿は `docs/spec/_source/settings.json`）。⭐⭐ **`FR-029` が「入口の無い戻り道」を禁じている先例が `HF-3` に在る**（`D-259` の周り）。 | `取下げ` | ⭐ **取下げ 2026-09-06。**前提が偽だった —— **戻す入口は既に在る（表 T-109 の `IC-81`。実測で対称なトグルであることを確かめた）。**幾何は毎フレーム組み直されるので、戻した次のフレームで依存線は再び掴める。利用者の裁定 2026-09-06「コマンドパレットに依存線表示/非表示があるだろ？」。`FR-049` は依存線を別の面の規則から除外した。 | ⚠️ **`IC-81`（依存線）を押すと図が 4206 → 3945 節点へ減ることを前に立つ者が測った** —— **戻す道は測っていない。** | ⛔ **まだ押していない。** |
| D-305 | ⛔ **暦を変えたときに、影響する `Task` の件数を告げていない**（978 本を反証した体の申告 2026-09-05）—— **`src/use-case/edit-document/edit-calendar.ts:81-92` が「呼び手の仕事」と書くが、`src/use-case/notify-change-watchers/` と `src/adapter/screen-renderer/notices.ts` に `calendar` が 0 件で、辞書には拒否理由が 1 本在るだけである（`display-words.json:3027`）。** | 暦を変えたとき、影響する `Task` の件数が告げられること | ①改暦の経路から `NT-3` の通知を立てる ②表 T-233 に行を足す ③`NT-3` の例から暦を外す | ⭐ **仕様の場所**: 表 T-037 の `NT-3` と `FR-088`（`docs/spec/01-04-requirements.md`）—— **`NT-3` が例として「暦の変更（`FR-088`）」を名指している。**⭐⭐ **`D-273`（担当者名の波及）とまったく同じ形である** —— **`NT-3` が挙げる 4 つの例のうち 2 つが、届く先を持たない。** | `取下げ` | ⭐ **`share 05` / `share 06` の `A` 192 本を 1 本ずつ反証した体が挙げ、前に立つ者が `grep` で裏を取った。** ⭐⭐ **裁定された（2026-09-06、利用者。逐語）** —— **「稼働日の歴を変えても実績や予定を変更しない。」**⛔⛔ **これは 3 つの案のどれでもない。前提を覆す裁定である** —— **暦を変えても `Task` の予定と実績の日付は動かない。⇒ 動かないのだから、件数を告げる相手が無い。**⭐ **本行は「告げる」問題ではなく「そもそも動かしてはならない」という規則になる。**⇒ ⛔ **仕様を当てる前に測ること**: **いまの造りが暦の変更で日付を動かしているか。動かしているならそれ自体が欠陥であり、動かしていないなら本行は取り下げである。** ⛔⛔ **取り下げる。利用者の裁定と食い違う条項は 0 件であった（体が測り、前に立つ者が裏を取った 2026-09-06）** —— **裁定は「稼働日の歴を変えても実績や予定を変更しない」。⭐ いまの造りは既にそうなっている。**⭐ **測った内容**: **`edit-calendar.ts` の `editCalendar` が書くのは `Calendar.weekDays[].dayWorking` と `Project.weekStartDay` だけで、返す文書の `tasks` は同一参照のままである。`Task` の `start` / `finish` / `actualStart` / `actualFinish` に触れる行は 1 本も無い。**⭐ **仕様も動かすとは書いていない** —— **暦を引く条項はすべて描画と数え上げである（`FR-047` は遅れの量を稼働日で数えて**示す**、`GR-9` / `GR-18` はダミーを**描く**）。**⇒ ⛔ **告げる相手が無い。件数の通知は要らない。** | ⚠️ **2 件を 1 つの手当てで閉じられる可能性が高い。** | ⛔ **まだ押していない。** |
| D-309 | ⚠️ **速く送ったときの空白帯を塞ぐ仕掛けが無い**（978 本を反証した体の申告 2026-09-05）—— **先読みの余白を持つ所が `src/` に 0 件で、`NFR-011` の後段は実装も測定も無い。** | 速く送っても空白の帯が出ないこと。または出ることを測って許すこと | ①先読みの余白を持つ ②`NFR-011` の後段を測って、いまで足りるなら記録する | ⭐ **仕様の場所**: `NFR-011`（`docs/spec/01-04-requirements.md`）。⚠️ **`D-313`（性能を測る仕掛けが 1 つも無い）と同じ根に在る** —— **測らなければ、足りているかも分からない。** ⛔⛔ **取り下げる。本行の前提が偽であった（体が 2026-09-06 に測った）** —— **「先読みの余白を持つ所が `src/` に 0 件」は偽。`svg-renderer.ts` が上下に `Row Area` 1 つ分ずつの余白を持っている（本行が立った日の間引きで入った）。**⭐ **対照つきで測った**: **速い縦パンの 5 地点で、「画面に立っているのに間引かれたもの」は**すべて 0**。⭐ 対照（余白を内側へ 1 つ分ひっくり返した同じ問い）は各地点で 10〜75 を返すので、測定は穴を見つけられる状態にある。**⭐ **構造上も覗く余地が無い** —— **描く道の 3 ファイルに `async` も `await` も `setTimeout` も 1 つも無く、絵は 1 回の差し替えで置き換わる。⇒ フレームは自分の位置について完成しているか、描かれないかのどちらかであり、後者は落ちたフレーム（`NFR-010`）であって空白帯ではない。**⛔ **案① には条項が無い** —— **「先読み」も prefetch も overdraw も `docs/spec/` に 0 件。⇒ 足せば発明になる。**⚠️ **残るのは案②（`NFR-011` の後半を測る）だけであり、それは `D-313` の穴で、`RISK-001` の関門の向こうである。** | `取下げ` | ⭐ **`share 09` / `share 10` の `A` 131 本を 1 本ずつ反証した体が挙げた。** | ⚠️ **前に立つ者は横の車輪で画面が動かないことを測った**（`D-298`）—— **送れないものは、送りの性能も問えない。** | ✅ **2026-09-06（探針で速い縦パンを 67 段、対照つき）** —— **間引かれた立ち物 0 件／対照は毎段 10〜75。⇒ 空白帯は出ていない。** |
| D-310 | ⚠️ **`NFR-008`（コントラスト）の参照が `src/` に 1 つも無い**（978 本を反証した体の申告 2026-09-05）—— **コメントボックスをコントラストの対象に含める所が無い。** | コントラストの対象にコメントボックスが入っていること | ①対象に含める ②`NFR-008` の母数を仕様で絞る | ⭐ **仕様の場所**: `NFR-008`（`docs/spec/01-04-requirements.md`）。⚠️ **`LM-2` は「色覚とコントラスト・フォーカスの判別・文字サイズと言語・エラー表示」に絞ると述べており、コントラストは絞った先に残っている。** ⭐⭐ **裁定（2026-09-07、前に立つ者）: 取り下げる。両方の半分が偽である。** | `取下げ` | ⭐ **`share 09` / `share 10` の `A` 131 本を 1 本ずつ反証した体が挙げた。** ⛔⛔ **裁定は要らない。本行は要求を取り違えている（前に立つ者が自分で読んで反証した 2026-09-06）** —— **`NFR-008` はコントラストではなく**フォーカスの見え方**の要求であり、しかも対象を自分で絞っている: 「対象は、表 T-036 が割当を持つ操作の画面上の入口と、その場の編集に入るすべての入力欄とすること（MUST）」。**⭐ **コントラストは `NFR-007`（文字 4.5 : 1、図形と操作できる要素 3 : 1）である。**⇒ ⛔ **決まっていないことは 1 つも無い。**⭐⭐ **本行の「詳細状況」が自分でそう書いている**: 「押して測れる —— 背景と文字の相対輝度を実測すればよい」。⇒ ⭐ **これは裁定待ちではなく、押して測る仕事である。** ⛔⛔ **本行は要求を取り違えている（実測 2026-09-07）** —— `NFR-008` は**焦点の見え方**（WCAG 2.4.7）であり、しかも自分で対象を限っている（「表 T-036 が割当を持つ操作の画面上の入口と、その場の編集に入るすべての入力欄」）。⇒ **対応案②（分母を仕様で絞る）は要求自身が既に済ませている。**⛔ **コントラストは `NFR-007` であり、対応案①も既に済んでいる** —— `src/adapter/svg-renderer/svg-renderer.ts:2226-2238` が MUST を名指し、実測比を記録している（`S-147` on `S-146`: 明 17.76:1 / 暗 14.94:1。`ANNOTATION_COLOUR` は暗で 3.58:1 なので輪郭にのみ用いる）。⚠️ `RC-10` が 2026-08-13 に `NFR-008` を絞った裁定を記録している。⇒ **未決も欠落も無い。** | ⚠️ **押して測れる** —— **背景と文字の相対輝度を実測すればよい。** | ⛔ **まだ押していない。** |
| D-311 | ⛔⛔ **第 8・9・10 章（試験の仕様）が引用 1 行の器のままで、生成器も `tests/usecase/` も無い**（978 本を反証した体の申告 2026-09-05）—— **`docs/spec/08-10-test.md` は 3 章とも中身を持たず、`SW_SPEC_TEST` を起こす生成器が `tools/` にも `docs/spec/_source/` にも無い。`tests/usecase/`（表 T-218 の `TS-1`）も存在しない。**⇒ **「Ch.9 は生成物」「生成物は原稿と一致する」「手で直さない」という 3 つの MUST が、果たす対象を 1 つも持たない。** | 第 8・9・10 章が、生成される中身を持つこと。または持たないと決まっていること | ①生成器を書く ②3 章の要求を代償つきで取り下げる ③章を畳む | ⛔⛔ **これは製品ではなく工程の穴である。**⭐ **仕様の場所**: 表 T-218 と 第 8・9・10 章（`docs/spec/08-10-test.md`）。⚠️ **`playwright.config.ts` は `usecase/**/*.test.ts` を拾う設定を持ち、`--pass-with-no-tests` でごまかしている。** ⭐⭐ **裁定された（2026-09-06、`CR-364`）** —— **逐語「試験の章は一旦そのまま」。**⭐ **取り下げではない。⛔ いま何も変えない。**⚠️ **本行は開いたまま残す** —— **「一旦」は後で戻る意である。**⭐ **実際の試験は 166 ファイル・6,800 件超が動いており、章が空でも試験が無いわけではない。** | `仕様待ち` | ⭐ **`share 09` / `share 10` の `A` 131 本を 1 本ずつ反証した体が挙げた。** | ⚠️ **検査 39 の基準線 978 には、この 3 章の MUST も入っている。** | ⛔ **まだ押していない。** |
| D-316 | ⚠️ **`Task` を 1 つ消しただけで、およそ 5,300 の SVG 要素をすべて作り直している**（波 2 の体 C の実測 2026-09-05。⛔ 本行の最初の見立て「描き直しが遅れる」は誤りであった）—— **負荷の無い機械では 21〜48 ミリ秒で収まるが、CPU を 20 倍絞ると 842〜1023 ミリ秒になる。**⇒ **待ち時間が機械の負荷に比例して伸びる。** | 文書の小さな変化が、絵の小さな書き直しで済むこと | ①差分で描く ②作り直しの費用を測って、第 7 章の合否に載せる | ⭐ **仕様の場所**: `FR-048`（フレームを要求する）と 第 7 章の 表 T-043（`docs/spec/01-04-requirements.md` と `docs/spec/05-07-design.md`）。⛔⛔ **これは `D-266` を偽の不具合に見せた原因である** —— **押した直後に測ると、絵がまだ古い。⇒ 測る側は「同じ読みが 2 回続くまで」待つほかない。**⚠️ **4 回のうち 1 回だけなので、常には起きない。⛔ 何が違ったかはまだ分かっていない。** ⛔⛔ **見立てが誤っていた（2026-09-05、波 2 の体 C の実測）。⭐ 症状は本物だが、原因は違う。**⛔ **「フレームを要求していない」は偽である** —— **フレームは要求されており、次のアニメーションフレームで届く。**⭐⭐ **ページの中で `keydown` から時計を起こして測り直した（n=23、暖機 13 ＋ 冷間 10）: `21`〜`48` ミリ秒、外れ値 0。⛔ 1065 ミリ秒は 23 回中 0 回も再現しなかった。**⭐⭐ **CPU を絞ると線形に伸びる** —— **8 倍で `215` / `223` / `225` / `241` / `294` / `301` ミリ秒、20 倍で `842` / `896` / `930` / `1022` / `1023` ミリ秒。**⇒ ⛔ **前に立つ者が測った 1065 ミリ秒は、体を 5 つ走らせていた機械の負荷である。**⭐⭐ **それでも残る本物はこれである** —— **`Task` を 1 つ消しただけで、およそ 5,300 の SVG 要素をすべて作り直している。描き直しは 1 つのまとまりで届き（36〜48 ミリ秒）、行のパネルと絵に分かれてもいない。**⇒ **負荷が上がると、その作り直しの費用がそのまま人の待ち時間になる。**⭐ **直す場所は `src/adapter/screen-renderer/` か `src/framework/dom-screen-surface/`（差分で描く）であり、殻ではない。** ⛔ **直す場所は `src/framework/dom-screen-surface/` ではない**（実測 2026-09-07 で反証）—— **日程の絵を書くのは `dom-svg-surface` であり、しかも差分で描く仕事は `src/adapter/svg-renderer/` が先に identity を与えないと成り立たない。** ⭐ **identity は着地した（2026-09-07）。**⛔ **差分で描く側はまだである。**⭐ **`<g transform>` を行ごとに置く道は退けた（測って）** —— **絵は行ではなく図形の種類の順に並んでおり**（`planParts` → `guideParts` → `actualParts` → `markerParts` → ラベル）、**`LF-12` のはみ出しとラベルが最初の行を越えて走るので、その順序が効いている。行でくくると重なりの勝ち負けが変わる ＝ 画素が動く。**⇒ **図形ごとの鍵にした。**`svg-renderer.ts:332` の `figureKey` が 26 か所で ` data-figure="…"` を書く（`task-<uid>-plan` ／ `-actual` ／ `-guide` ／ `-marker` ／ `-label` ほか、`row-<groupId>-band` ／ `-rule`、`dep-<pred>-<succ>`、`box-<id>`、`ruler-<row>-tick` ／ `-label-<serial>` ほか）。⛔ **添字は使っていない** —— **添字こそが削除で動くものである。**  ⛔⛔ **反証された（実測 2026-09-07）。本行が数えたものは、意図された設計である。**⭐ **`data-figure` の「衝突」は衝突ではない** —— **`MC-7` の規模で 1,317 個の鍵つき要素・899 の相異なる鍵・263 の繰り返しがあるが、⭐⭐ 263 のすべてが文書順で連続した一続きであり、散らばっているものは 0 である。**⇒ **1 つの鍵が離れた 2 か所の別の図形を指すことは 1 度も無い。**⭐ **`figureKey` の注が「円盤と記号は 1 つの図形で 1 つの鍵を持つ」と述べているとおりである。**⛔ **本行の数（およそ 5,300 要素）も偽である** —— **実測は 1,376 要素・187,563 字であり、`svg-renderer.ts` の画面外の間引きが効いている。**⇒ ⭐ **残る本物は「1 つ消すと絵が丸ごと組み直される」ことだけであり、それは `IF-1` の継ぎ目の話である。**⛔ **`D-330` へ寄せた。** | `取下げ` | ⭐ **`D-266` を反証しに行った体が、反証の過程で見つけた。** ⭐⭐ **実測（2026-09-07、出荷ビルド、同梱の起動文書 1,375 要素）**: `Task` を 1 つ消すと `<svg>` 直下の子は 827 → 823。**どこかに同じものが在る要素は 682（82.5%）。⛔ 同じ位置に在るのは 20（2.4%）。共通の接頭 0 / 接尾 104** ⇒ 接頭接尾の差分でも 827 のうち 723 を書き直す。⛔⛔ **`data-role` は 1 個、`id` は 3 個。どの図形も identity を持っていない。**⇒ **82.5% は本当に残るが、位置でも接頭接尾でも鍵でも届かない。**⚠️ さらに `SvgSurface.showSvg(svg: string)`（表 T-065 の `IF-1`）は**文字列**を渡すので、面の側は比べる構造を持たない。⇒ **面の側だけで差分を書いても、723 を書き直したうえに解析が 1 回増えるだけである（測って確かめた）。**⭐ **面の側の唯一できることは既に在る** —— `dom-svg-surface.ts:39` が同一の描き直しを短絡している。 ⭐⭐ **実測（40 `Task`・2 行、`Task` を 1 つ削除）**: 描かれた要素 174 → 174、**鍵を持つもの 174 / 174（0 個が無鍵）**、鍵の衝突 0。**同じ添字で一致 82（47%）に対し、鍵で一致 170（97.7%）。**消えた鍵は 4 つだけ（`task-7-plan` / `-actual` / `-marker` / `-label`）。⭐ **画素は 1 つも動いていない** —— 差分の削除行はすべて閉じ `/>` か署名であり、幾何・色・順序は変わっていない。2499 件のうち 2497 件が 23 ファイルで緑。⛔ **残るのは DOM の側である** —— `dom-svg-surface.ts:41` の `host.innerHTML = svg` が作り直しの本体である（`D-330` が名指す）。⚠️⚠️ **裁定が 1 つ要る**: **鍵は書き出す絵にも入る。**表 T-076 は「どの部分を描くか」を定め、属性については何も言っていないので、**書き出しで落とすのはこのファイルが規則を発明することになる。**⛔ **`Task` の UID と `TaskGroup` の id が、渡した相手のファイルに載る。**⚠️ **1 要素あたり約 29 バイト増え、`D-330` の `PG-3` はまだ赤い。** | ⚠️ **`D-313`（性能の測定器が 1 つも無い）に当たる話でもある** —— **いま誰もこれを測っていない。** | ⛔ **押した（2026-09-07、出荷ビルド、`MC-7` の規模）。** |
| D-317 | ⛔⛔ **すべての `Task` を消すと、行見出しパネルが 100 行のうち 7 行しか描かなくなる**（仕様だけを読む体が赤い試験で挙げ、前に立つ者が文書を読んで裏を取った 2026-09-05）—— **`Ctrl+A` → `Delete` → 確認に `Yes` と答えると、文書の `taskGroups` は 100 のまま （⭐ 行は 1 つも消えていない。`SL-1` と `HM-6` は守られている）、`tasks` は 1000 → 0 になる。ところが描かれる行は 8 → 7 に減り、しかも 7 つとも深さ 1 である。**⛔ **消す前は深さ 1・2・3 の行が描かれていた（`1,2,2,1,2,3,3,3`）。**⛔ **場所が足りないのではない** —— **消したあとの行の高さは `64,48,48,48,48,48,48` で合計 352px、パネルは 1080px まで在る。⇒ 700px 空いているのに、93 行が描かれない。** | 文書が持つ行が、場所の在るかぎり描かれること | ①なぜ深さ 2 以上が落ちるのかを突き止める ②`FR-018`（詳しさの段）の規則が正しく当たっているかを見る | ⛔⛔ **原因はまだ分かっていない。**⭐ **分かっていること**: **①行は消えていない（`Agent API` の `readDocument()` で数えた）②描かれるのは深さ 1 だけになる ③場所は余っている。**⚠️ **仕様だけを読む体は「行が消えた」と読んだが、それは偽である** —— **`[data-depth]` は「描かれている窓」であって文書ではない**（引継書 §8 の罠）。⭐ **仕様の場所**: `FR-018`（詳しさの段）と 表 T-015 の `HR-1a`（`docs/spec/01-04-requirements.md`）。 ⭐ **本物である。⭐ 体が仕掛けを 2 つ潰した（2026-09-06）** —— **7 行は「根だけで深さ 1」の形であり、`schedule-layout.ts` の `depthLimit === 1` から出る。⛔ そこへ至る道は 2 つとも消しの手では通らない: ① `groupDepthLimit` は `zoomY` の純関数で、消しの経路は `zoomY` を書かない。② 消しの経路に `isCollapsed` を真にする者はいない。**⇒ ⭐ **次に測る所は `schedule-layout.ts` の `drawnGroups` と `depthLimit` である。⛔ `FR-018` から始めるな。**⚠️⚠️ **本行の期待値には条項が無い** —— **「場所の在るかぎり描かれること」を定める要求は 1 つも無い。⭐ 言い直すなら「倍率を変えない操作は群の LOD の深さを変えてはならない（`FR-018`）」である。** ⛔⛔ **取り下げる。条項は 1 つも破れていない（体が 2026-09-06 に測った）** —— **仕掛けは、前の体が潰した 2 つのどちらでもなく、**第 3 の道**であった: `viewSettings` がどの腕を通るか。**⭐ **実測**: **`Task` が在るあいだは `BT-4` の除外が働き、文書自身の `zoomY` 1 が残って深さ 3 まで描く（68 行）。全部消すと除外の条件が消え、`FR-055` の全体表示が走って `zoomY` 0.2667・深さ 1（7 行）になる。**⭐⭐ **そしてその落ち方は `OP-10` が**命じている** —— **「`Task` を 1 件も持たない文書には、`BT-4` の除外を働かせないこと（MUST）… そのような文書は本行の冒頭にもどり、`FR-055` の全体表示が選ぶ倍率と表示位置になる（MUST）」。**⭐ **深さごとに測った**: **深さ 1 が 400px、2 が 1464px、3 が 3816px、4 が 5496px、5 が 5608px。場所は 958px。⇒ 収まるのは深さ 1 だけであり、「収まる最も深い段を採る」の答えである。**⛔⛔ **本行の期待値は `FR-055` 自身の一文が否定している** —— **「画面の下に隙間が残ることは許す —— 本要求は収めることを求めており、埋めることを求めていない」。**⇒ ⭐ **700px の余白は明示的に許されている。**⚠️ **前に立つ者が渡した言い直し（「倍率を変えない操作は深さを変えてはならない」）も破れていない** —— **倍率は変わった（1 → 0.2667）。消しが書いたのではなく、書かせない条件のほうが消えたのである。** | `取下げ` | ⭐ **仕様だけを読む体が `SK-3` / `SL-1` の試験を書いて赤くした。⛔ その体の読み（行が消えた）は前に立つ者が反証したが、⭐ 赤そのものは本物である。** | ⚠️ **`tests/system/three-rows-read-from-the-spec-alone.test.ts:696` が赤いまま置いてある** —— **この行が閉じるまで鳴り続ける。**⛔ **巡の終わりに赤を 1 本残した。理由は本行である**（規則 05 の 1.）。 | ⛔ **押す対象が無い。⭐ 代わりに直す物が 1 つある** —— **赤い生の試験が**代理**を測っている: 行見出しパネルに出る**行名の数**を数えて 「行が選べなくなった」と判じているが、パネルは描かれた窓であって文書ではない。⭐ 文書の行は 100 → 100 のままである。**⇒ ⭐ **その試験は、機械の口の `readDocument()` で文書の行を数えるように書き替えること。⛔ パネルの行数を数えてはならない。** |
| D-320 | ⛔⛔ **絵の書き出しを選ぶと面は閉じるのに、1 バイトも書かれず、何も告げない**（`NFR-004` の掃引 2026-09-05）—— **表 T-024 の `IO-3` / `IO-4` / `IO-7` の 3 行が、そろってそうなる。**⛔ **`src/framework/single-html-shell/frame-loop.ts:6523-6528` が `content === null` のとき黙って戻る。****`exportedText`（同 `:2038`）は 3 つの形式に `null` を返し、代わりを担うはずの `exportPictureContent`（同 `:6591`）も `null` を返している。** | 書き出しを選んだら、書かれるか、書けない理由が告げられること | ①`exportPictureContent` を実装する ②書けない形式は `RS-40`（この形式は、このビルドではまだ書けない）を告げる | ⭐ **仕様の場所**: 表 T-024 の `IO-3` / `IO-4` / `IO-7` と 表 T-233 の `RS-40`（`docs/spec/01-04-requirements.md`）。⛔⛔ **`RS-40` は「この形式は、このビルドではまだ書けない」として既に在る。⇒ 告げる語は揃っている。**⭐ **`IO-6`（クリップボードへの画像）は動いている** —— **画面が動かないので死んで見えるが、`navigator.clipboard.writeText` を包んで数えたら届いていた。**⭐⭐ **`D-261`（PNG の倍率を選ぶ入口が無い）と同じ場所である** —— **倍率どころか、絵が 1 枚も出ていない。** ⛔⛔ **取り下げる。本行は偽であった（2026-09-05、作りに行った体の反証）。**⭐⭐ **表 T-024 の 5 行すべてが書かれる**（出荷ビルド、`file://`、1920 × 1080、宿主の保存ダイアログを `addInitScript` で差し替えて掴んだ）—— **`GRS JSON` 895,422 ／ `MSPDI` 645,459 ／ 単一 `.html` 2,034,321 ／ `SVG` 395,400 ／ `PNG` 269,097 バイト。**⭐ **`WY-2` も満たす** —— **同じ状態で 2 回書き出すと、長さもハッシュも一致する。**⭐ **`S-82` の倍率も効く** —— **1 倍で 1600 × 900 / 268,105 バイト、2 倍で 3200 × 1800 / 904,248 バイト。**⛔⛔ **本行が挙げた見当（`frame-loop.ts:6523-6528` と `exportPictureContent` が `null` を返す）も外れていた** —— **3 本とも実装済みで、`single-html-shell.ts:1033/:1037` が必要なものを渡している。⇒ 前の巡（`CR-333` / `CR-337`）で閉じていた。**⚠️⚠️ **偽であった理由は、測った時点が合流の前だったことである** —— **`NFR-004` の掃引は、その巡の直しが入る前の出荷ビルドを押していた。**⇒ ⛔ **出荷ビルドを押すときは、いつのビルドかを数と一緒に書くこと。**⭐⭐ **それでも「黙って閉じる面」は別の道で生きていた** —— **`D-334` へ分けた。** | `取下げ` | ⭐ **`NFR-004` の母数 84 行を `file://` と `http://` で押し切った掃引が挙げた**（`tests/system/nfr-004-file-scheme-sweep.sws.test.ts`）。⛔ **`file://` の話ではない —— 両方式で同じように落ちる。** | ⚠️ **`D-220` の錨（伸ばしても描けない大きさは `RS-43` で拒む）は緑のままである** —— **拒む道は在り、書く道が無い。** | ✅ **2026-09-05（出荷ビルド `6f91366`、`file://`、1920 × 1080）** —— **5 形式とも書かれ、2 度目の書き出しが一致し、`PNG` の倍率も効く。** |
| D-324 | ⛔⛔ **行見出しパネルで行をドラッグしても、兄弟の並びが動かない**（仕様だけを読む体の実測 2026-09-05、出荷ビルド）—— **表 T-015a の `HM-8` は「兄弟どうしの並べ替えができること（MUST）—— 入口は行見出しパネルでのドラッグである」と定めるが、`Phase Gates` を `Phase Bars` の上へ引いても `order` は 1 のまま 1 であった。⭐ 4 通り試した —— 名前を掴んで上へ、帯を掴んで上へ、名前を掴んで下へ、`applyCommands` 経由。** | 行見出しパネルのドラッグで、兄弟の並びが変わること | ①ドラッグの受け手を実装する ②`HM-8` を代償つきで取り下げる | ⛔⛔ **これは `HM-9`（並べ替えた順序も WBS へ伝わること）の唯一の入口である。**⇒ **`CR-354` で `HM-9` の写し方を定めたが、それを起こす操作が働かないので、定めた規則はどこからも発火しない。**⭐ **順番に意味がある** —— **`HM-8` が動くまで `HM-9` の実装を測れない。⛔ `HM-9` から先に実装させるな。** ⛔⛔ **取り下げる。本行は書いたとおりには偽であった（2026-09-05、直しに行った体の反証）。**⭐⭐ **並べ替えは壊れていない** —— **掴み代の上で掴んで引くと、`Phase Gates` の `order` が 1 → 0、`Phase Bars` が 0 → 1 に動く。`CM-73`（`moveTaskGroup`。⛔ `CM-35` ではない）が `HM-8` を担い、`input-command-translator.ts:6062` の `commandFromRowGrab` まで繋がっている。**⛔⛔ **偽であった理由は、試験が掴む場所を 8px 外していたことである** —— **深さ 2 の行の掴み代は x 32..48 に在り、行の名前は x 52 から始まる。試験は x = 60 を掴んでおり、それは名前の上である。**⭐⭐ **名前は掴む場所ではない** —— **表 T-023d の `GR-20` が「行の左端に敷く掴み代（幅は `S-138`）。⭐⭐ 行の左端とは、その行の字下げの後ろである（MUST）」と定めており、`tests/unit/t-051-hf-15-grabbing-a-row.test.ts` が「掴むのは掴み代であって、行の他の場所ではない」を逐語で留めている。**⛔⛔ **体は `HM-8` の「入口は行見出しパネルでのドラッグである」を「パネルのどこでもよい」と読み、名前からも掴めるように広げた。⇒ その試験が落ちた。**⭐ **`HM-8` の「パネル」は、日程の描画領域（表 T-023a）に対する「パネル」であって、パネルの中のどこでもよいという意味ではない。**⇒ ⭐ **広げた側は戻した。⛔ 直すのは試験の掴む座標である。** | `取下げ` | ⭐ **仕様だけを読む体が、`CR-354` の条文を留める試験を書いていて見つけた**（`tests/system/fr-019-hm-9-the-row-tree-decides-what-is-below.test.ts`）。⛔ **`src/` を 1 バイトも読んでいない体である。** | ⚠️ **`D-272` と同じ場所だが別の層である** —— **あちらは「WBS へどう伝えるか」、本行は「そもそも並べ替えが起きない」。** | ✅ **2026-09-05（開発ビルド、掴み代の上で掴んだ）** —— **`order` 1 → 0 と 0 → 1。⭐ 掴み代は深さ 2 で x 32..48、名前は x 52 から。** |
| D-327 | ⛔⛔ **同じ文書が、届き方によって違う絵になる**（性能の実測 2026-09-05）—— **同梱の起動文書として立ち上がると図形が 5,041 個描かれるのに、同じファイルを `IC-1` で読み込むと 439 個しか描かれない。**⭐ **同一性は証明済み** —— **出荷ビルドが同梱している起動文書そのものを読ませて比べた。** | 同じ文書は、届き方によらず同じ絵になること | ①読み込みの経路が落としているものを突き止める ②起動の経路と読み込みの経路を 1 本にする | ⛔⛔ **これは性能ではなく正しさの欠陥である。**⭐ **`D-328` と同じ場所を指している疑いが濃い** —— **読み込みのあとは描き直しが起きず、439 個のまま止まる。ホイールを 1 度回すと 439 → 6,714 に跳ねる。**⇒ ⚠️ **「読み込みが落とす」のではなく「読み込みのあと描き直していない」のかもしれない。⛔ どちらかは押して切り分けること。** ⚠️⚠️ **報告を読み直して、これは欠陥でないかもしれないと分かった。**⭐ **439 という数は「収める表示」の状態である**（同じ報告の `PG-6` が「5,041 at rest ／ 6,716 zoomed in ／ **439 fitted**」と記録している）。**読み込んだあとの絵は目盛りが年、画面に 3 年ぶんで、起動時は日、10 か月ぶんであった。**⇒ ⭐ **「読み込んだら全体表示にする」なら、439 は正しい振る舞いである**（`FR-055`）。⛔ **仕様がそう定めているかを、まだ引いていない。**⇒ ⛔⛔ **先に引け。定めているなら本行は取り下げ、残る欠陥は `D-328`（その絵が凍りつく）だけになる。** ⭐⭐ **引いた。取り下げる。本行は欠陥ではない（2026-09-05）。**⛔⛔ **表 T-024a の `OP-10` がそう定めている** —— **「表示位置が `null`、または指す行が存在しないとき、`FR-055` の全体表示が選ぶ倍率と表示位置にすること（MUST）。このとき `HF-8` を働かせてはならない（MUST NOT）」。**⭐ **読み込んだ文書は表示位置が `null` である**（測った者が「`scrollDate` / `scrollGroupId` はそのホイールが来るまで `null`」と記録している）。⇒ ⭐ **439 図形・目盛りは年・画面に 3 年ぶん、は全体表示そのものであり、仕様が命じている姿である。**⭐ **起動が 5,041 図形なのは、同梱の起動文書が表示位置を持っているからである。**⇒ ⛔ **「届き方で絵が違う」のではなく「表示位置を持つ文書と持たない文書で違う」だけである。**⭐⭐ **残る本物は `D-328` だけである** —— **全体表示にしたあと、その絵が凍りつくこと。** | `取下げ` | ⭐ **利用者の指示（2026-09-05）で走らせた性能の実測が挙げた** —— **`sample-schedule` の見本と、同梱の起動文書（`Task` 1000 件）で測った。生の数と表は `scratch/perf-report.md`（427 行。⚠️ gitignore 済みなので作業樹には無い）。** | ⚠️ **`D-328` と割れて出た。⭐ 2 つは同じ原因かもしれない。** | ⭐ **押す必要が無い** —— **仕様が定めている姿であることを、`OP-10` を引いて確かめた。** |
| D-330 | ⛔⛔ **第 7 章の合否のうち 3 本が外れている。⭐ 原因は 2 か所に絞れている**（性能の実測 2026-09-05）—— **`PG-2`（毎フレームの予算）: 1 フレームの仕事が 30.9 〜 33.5 ミリ秒で、毎秒およそ 30 枚に頭打ちになる。`PG-3`（p95 の天井 16.7 ミリ秒）: 巻き取り 42.0 ／ 行の拡大縮小 48.0 ／ 時間軸 51.9。`PG-8`（掴んで動かす）: p95 20.9。**⭐ **内側だったもの**: **`PG-1`（起動 164 ミリ秒 ／ 上限 1500）、`PG-5`（無操作で 4 秒間 0 枚）、`PG-14`（8 倍の `Task` で 6.9 倍。許される上限は 10.7 倍）。** | 第 7 章の合否を満たすこと | ①行の操作子の高さを覚えて測り直さない ②巻き取りを作り直しでなく変換で行う ③絵が変わらないフレームを捨てる ④動く層と静かな層を分ける | ⭐⭐ **時間の行き先は測って特定されている（推測ではない）** —— **①`src/framework/dom-screen-surface/dom-screen-surface.ts:6138` の `reportRowControlsHeight` が、毎フレーム、行の操作子ごとに `getBoundingClientRect` を呼ぶ。15.1 回で 12.3 ミリ秒 ＝ **フレームの 37%**。⭐ 対照実験: 同じフレームの道を通る横ホイールはこの呼び出しが 0 回で、12.6 ミリ秒で済む（対 33.4）。②`src/framework/dom-svg-surface/dom-svg-surface.ts:41` が `host.innerHTML = svg` で毎フレーム絵を丸ごと書き直す —— `Task` 1000 件で 527,000 文字・約 5,300 節点で 9.6 ミリ秒、8000 件で 4,183,000 文字・67.5 ミリ秒。③残り約 11.6 ミリ秒は、その 527,000 文字を組み立てる `svg-renderer.ts`。**⭐ **CPU を絞ると線形に伸びる（31.3 → 156.1 → 391.4 → 1149.6 ミリ秒）** ⇒ **待ちではなく仕事の量の問題である。**⭐⭐ **対策の見込み**: **案①は 1 ファイルで毎フレーム 12.3 ミリ秒（37%）を落とす。案②は 3 ファイルで、絵が変わらないときの 2.8 ミリ秒へ近づける。⛔ 目標尺度で合否の内側に入るのは案①と案②の両方を当てたときだけ、と測った者は述べている。** ⭐⭐ **2 つの対策を当てて測り直した（2026-09-05、前に立つ者の実測）。**⭐ **巻き取り 1 フレームの仕事**: **中央値 30.9〜33.5 → `14.0` / `16.4` / `13.7` ミリ秒、p95 42.0 → `21.5` / `20.8` / `17.9` ミリ秒**（3 回、ページ内の時計、出荷ビルド）。⭐ **図形は 5,041 → 1,418。**⇒ ⛔ **`PG-3` はまだ外である**（天井 16.7 ミリ秒）。⭐ **ただし超過は 25 ミリ秒から約 4 ミリ秒へ縮んだ。**⭐⭐ **効いたのは P-1 のほうである** —— **画素に届かない図形を書かないことで、`innerHTML` が 527,573 → 183,589 文字、要素が 5,038 中 3,557（文字数の 74%）減った。**⛔⛔ **P-2 の効き目は、名指しされたものとは違った（測った体の反証）** —— **15.1 回の `getBoundingClientRect` のうち、費用を持つのは**最初の 1 回だけ**で、11.3 ミリ秒は 15 回の読みではなく 1 回の強制レイアウトである。CDP の `Performance` で見ると Layout（5.3→5.5）・RecalcStyle（6.3→6.5）・Script（19.9→20.5）は動かず、動くのは 1 フレームあたりのレイアウト回数だけ（1.15 → 0.98）。**⇒ ⭐⭐ **P-2 が消したのは「rAF の中で払う費用」であって、ブラウザが絵を描くために払う費用ではない。****`NFR-003` の数は約 36% よくなるが、人が受け取るフレームの間隔は変わらない。**⇒ ⛔ **残りを詰めるなら、測るべきは `work` ではなくフレームの間隔である。**  ⭐⭐ **1 フレームの行き先を測り分けた（2026-09-07、出荷ビルド、Edge 152、1000 `Task`）** —— **フレーム全体 20.42ms のうち、絵ができる前の幾何と割付けの算が 7.4ms、`innerHTML` の書き込みが 3.2ms、その解析が 2.3ms、1,376 要素を差し替えたことに続く style / layout / paint が 6.1ms、画面の眺めが 1.8ms、残りが予定表と GC である。**⛔⛔ **本行の数は偽であった** —— **絵は 187,563 字・1,376 要素であって、527,000 字・5,300 要素ではない。**⛔ **「費用は文字列の組み立てにある」も偽である** —— **`svgFromSchedule` は 1.38ms、フレームの 6.5% にすぎない。**⭐⭐ **いちばん高い 1 本は `dayOf` であった** —— **保存された日付の文字を正規表現で読む関数で、自分の時間だけで 3.58ms／フレーム、1 フレームにおよそ 24,000 回呼ばれていた。**⭐ **1 回のパスのあいだだけ覚える表を 2 つ置いて、幾何の算を 4.16 → 3.00ms、`PG-14` の 1000 件での中身を 25.7 → 10.0ms へ下げた。**⭐ **絵は 1 バイトも動いていない**（透かしの秒を伏せた比較でハッシュ一致、`npm run parity` 75/75）。⛔⛔ **それでも合否には届かない。⭐ 算がそう言っている** —— **いま 20.42ms。`indexOfCalendar` の作り直し 2.66ms、`IF-1` の継ぎ目 9.2ms、行見出しの木の組み直し 1.8ms を全部取り返しても、残りは 6.8ms である。**⇒ ⭐ **`IF-1`（`SvgSurface.showSvg` が文字列を受ける）を変えるかどうかは仕様の裁定である。**⇒ ⭐⭐ **`PD-443` として立てた（2026-09-07）** —— 問いと候補はその行が持つ。⛔ **本行に写しを作らない。**⛔ **鍵つきの差分は既に反証されている** —— **15.5ms 対 5.8ms で、差分のほうが遅い。**  ⭐⭐ **描く範囲を横にも絞った（2026-09-07、体）** —— **`src/adapter/svg-renderer/svg-renderer.ts` の 1 ファイルだけ。縦の間引きの隣に横の境目を置き、タスクは予定と実績の広いほうで、依存線は経路の点で判じる。書き出しは触っていない。**⛔⛔ **上流の 2 段は絞れなかった。理由を先に測ってある** —— **`layout.placements` は `FR-016` の `zoomY` の上限（`bandGrowthOf`）が全行を読み、`geometry.tasks` は `SK-2` の全選択（`everythingSelectable`）が全件を読む。⇒ スクロール位置で `Ctrl+A` の意味が変わることになるので、条項の裏づけなしには絞れない。**⚠️ **よって `layoutFromSchedule` の 3.04 ミリ秒と `geometryFromLayout` の 3.00 ミリ秒は残っている。**⛔⛔ **予算はまだ満たしていない** —— **`NFR-003` の p95 ≤ 16.7 ミリ秒は、どの場面でも外れたままである。**⭐ **残る最大の梃子は `svg-surface.ts` の `host.innerHTML = svg`** —— **節点の同一性が毎フレーム壊れるので、解析・木の更新・レイアウト・描画を全額払っている（およそ 8 ミリ秒）。体は手を付けていない（書き直しになる）。**⚠️⚠️ **余白 1.5 の読み方が 2 通りある。裁定を仰ぐ** —— **「片側 1.5 倍」（いま出荷している側、窓の 1920 に対し左右 2880 ずつ）と「両側あわせて 1.5 倍」（片側 0.25 倍）。実測では後者が 1 フレームあたりさらに 3.1 ミリ秒速い（21.52 → 18.38 ミリ秒）。変えるのは定数 1 つである。** | `実装待ち` | ⭐ **利用者の指示（2026-09-05）で走らせた性能の実測が挙げた** —— **`sample-schedule` の見本と、同梱の起動文書（`Task` 1000 件）で測った。生の数と表は `scratch/perf-report.md`（427 行。⚠️ gitignore 済みなので作業樹には無い）。** ⭐⭐ **実測を足した（2026-09-07、本作業木、Intel UHD、1000 `Task`）**: 巻き取り **27.2fps / p95 54.6ms**、ズーム **38.6fps / 42.3ms**、パン **46.4fps / 36.4ms**、範囲選択 **44.5fps / 36.3ms**。⚠️ **`MC-4` の条項は通った**（renderer が Intel UHD であった）ので、`D-348` が数えた 14 のうち 1 本はこれで消える。⭐ **原因は `dom-screen-surface.ts:6138` の `reportRowControlsHeight` が毎フレーム `getBoundingClientRect` を呼ぶことに絞れている。**⭐⭐ **利用者の裁定 2026-09-07: 「いま測定器を書いて測る」** —— `RISK-001` の門はこの裁定で開いた。⛔ **合否を下げる案は退けられた。** | ⚠️ **`D-316`（`Task` を 1 つ消すと 5,300 要素を作り直す）の一般形である** —— **あちらは削除の 1 例、本行は毎フレームの構造。** | ⛔ **押した（2026-09-07）。`PG-2` / `PG-3` / `PG-8` / `PG-10` / `PG-12` は依然として外側である。**  ✅ **2026-09-07、出荷ビルド、1920 × 1080、1000 タスク、既定の眺め（体の実測、3 回を交互に走らせて機械の揺れを平す）** —— **要素 1,375 → 1,069、鍵つきの図 1,317 → 1,011、窓の右に居るもの 870 → 593、文字数 187,563 → 145,407（−22%）。フレームの中央値 24.17 → 21.52 ミリ秒。**⭐ **段が効いているのは `PG-14`（文書が大きくなったときの伸び）である** —— **呼び出しの中の時間 22.6 → 10.4 ミリ秒、図 858 → 244、文字 137,714 → 36,114、125 件から 1000 件への伸びが 5.38 倍 → 2.26 倍（上限 11.45 倍）。**⭐ **`PG-12`（札が混む場面）は合格に変わった** —— **56.99 → 62.23 fps、札 212 → 167。**⭐ **`PG-10`（依存線が密な場面）**: **45.98 → 53.44 fps、引いた経路 186 → 46。**⚠️ **`MK-*` の p95 は動かない** —— **垂直同期の倍数に量子化されており（33.33 は 2 フレーム）、1.7 ミリ秒の節約では跨げない。量子化されていない `PG-14` だけが半分になった。**⭐ **書き出しはバイト一致**（透かしの層を伏せたうえで 627,553 字が同一）。`npm run parity` は 75/75 で一致。`npx vitest run` は体の基準と同数。 |
| D-339 | ⛔⛔ **コードのコメントが、仕様に無い一文を「表の行」として引用しており、その引用が欠陥を 1 つ塞いでいた（実測 2026-09-06）** —— **`input-command-translator.ts:6014` が「`HF-17`（段 0 へ足す）には親が無いので当たらない」を表 T-051 の行として引き、それを根拠に段 0 の畳みを外さない（`D-318` の原因）。**⭐ **実測: この一文は `docs/spec/` に 0 件。見つかるのは `defects.md` の `D-251` 行だけであり、それは仕様ではなく**台帳が書いた言い換え**である。**⚠️ **同じ形がほかにもあるかは、まだ数えられていない** —— **素朴な掃引を書いたが偽陽性だらけで使えなかった（引用の途中の `——` や `…`、コメントの折り返し、複数の原稿を繋いだ干し草での順序探索）。⛔ 数を出すには道具が要る。** | コードが引く一文は、原稿のどこかに逐語で在ること | ①引用の出所を機械で突き合わせる検査を足す ②見つかった引用を 1 つずつ直す | ⭐ **仕様の場所**: 引用されている当の行、いまの 1 件では 表 T-051 の `HF-14` と `HF-17`。⭐⭐ **これは規則の信頼性のはしごの話である** —— **「原稿を写すな、指させ」は原則として書かれているが、機械が見ていない。⇒ 見ていない規則は、いずれ守られなくなる。**⚠️ **検査を書くなら、偽陽性を潰す正規化（`——`・`…`・折り返し・強調の記号）が本体になる。** ⭐ **本行が名指した 1 件は既に直っていた**（`input-command-translator.ts:6225`）。⭐⭐ **代わりに掃引を作り、区画 B から 3 件の本物を見つけて直した（2026-09-07）**: ① `frame-loop.ts:914` が `FR-031` として「1 段の大きさはその段の**保存形**を UTF-8 で」を引いていたが、`FR-031` は詰めた `GRS JSON` と述べ、「詰めた形は保存する形とは別である」と警告している（**コードは正しく、引用が別物であった**）。② `frame-loop.ts:8855` が `FR-072` の文として「見出しで『選択なし』と示すこと（MUST）」を引いていたが、`CR-272` 以降 `FR-072` は逆に「パネルの先頭に見出しの行を置いてはならない（MUST NOT）」と述べている。③ `input-command-translator.ts:5335` が `HR-6` を「その行の**配下**を畳んだ状態にする」と引いていたが、原文は「その行**と、**その配下を」であり、その誤引用が **12 行下のコードと矛盾する一文を正当化していた**。⇒ **3 件とも直した。**  ⭐ **もう 1 件見つかって直った（2026-09-07）** —— **`frame-loop.ts` が `HF-19` の理由として「操作子の高さは読む人の文字サイズが動かす」を引いていたが、`HF-19` は逆のことを MUST NOT で述べている。**⭐ **同じ誤りは `dom-screen-surface.ts` では直っており、こちらだけが残っていた。**⭐ **`OP-10` の言い換えに（MUST）が付いていた 1 か所も直した。**⛔ **本行が名指した 2 か所（`FR-031` と `FR-072`）は、測った時点で既に直っていた。**  ⭐⭐ **本行が「まさにこれ」と呼べる 1 件を、反証の体が挙げた（2026-09-08）。**⛔⛔ **`src/adapter/input-command-translator/input-command-translator.ts` の注が、表 T-233 の `RS-30` に「その行は畳まれておらず、隠れている子も無い」という文を負わせている。**⭐ **実物の `RS-30` は「直下に、画面へ戻せる子が 1 つも無い」であり、辞書の語も「この行の直下に、画面へ戻せる子がありません」である。**⇒ **どちらにも「畳まれておらず」は無い。**⛔⛔ **その捏造した文から、注は結論まで導いている** —— **「狭めると告げが嘘になる（畳まれている行に『畳まれておらず』と出る）」。**⇒ **実物の `RS-30` にその危険は無いので、その理由づけは丸ごと成り立たない。**⚠️⚠️ **挙動そのものが誤りかどうかは、まだ決まっていない** —— **畳まれていて子を 1 つも持たない行を「使える入口」として構えることが、表 T-051 の結び（描かれる行が増減しないなら対象が無いものとして扱う）と噛み合うか。**⇒ **別の行に立てた。**⛔ **誤った注に合わせてコードを直すな** —— **基準線のファイルが同じ罠を記録している（コードが正しく、理由だけが誤っていた例）。** | `実測済` | ⭐ 反証 2026-09-08: ⛔ 前に立つ者が自分で測った。⭐ 体 2 つの報告（5 か所・9 か所）は**どちらも誤り**である。 実数は **5 ファイル 7 か所**であり、`docs/spec` には **0 件**である: ``` src/adapter/input-command-translator/input-command-translator.ts:6666 ⭐ ここだけは訂正の注である src/adapter/screen-renderer/row-title-panel.ts:112, :675 src/adapter/screen-renderer/screen-renderer.ts:533, :554 tests/unit/fr-029-the-reason-a-press-carries.test.ts:46 tests/unit/uf-63.test.ts:664 ``` `RS-30` の実文は `01-04-requirements.md:3973` 「RS-30 の列＝直下に、画面へ戻せる子が 1 つも無い ／ 辞書＝`NT-1` ／ 表＝表 T-051 の `HF-13`」。 ⛔ 注に合わせてコードを直すな —— コードが正しく、理由だけが誤っている。 ⚠️ `screen-renderer.ts:554` はその捏造文から「畳まれた子無しの行も構えたままにする」と**推論している**。⇒ 推論ごと消すこと。 ステータスは `実装待ち`。  ⭐ **黙って戻る門を直しに行った体が、`D-318` を調べる途中で見つけた。****前に立つ者が逐語の `grep` で裏を取った。** ⭐ **掃引の作り方（再利用できる）**: コメントから `「…」` を取り、行頭を剥がし、継続行を繋ぎ、省略記号（`...` `…` `——` ` -- `）で割り、8 文字以上の断片を `docs/spec/` の全文と突き合わせる。⭐ **生では区画 B に 107 件。**規則を担う引用（`MUST` ／ `こと（` ／ `ならない`）に絞り、打ち消しとして導入されていないものだけにすると **10 件**。うち **3 件が本物、7 件は `（MUST）` の位置ずれの雑音**（引用の外に印を出す書き方）。⇒ **正規化で消せる形である。**⛔ **機械検査に載せるのは `.claude/skills/spec-graph-check/` の仕事であり、区画 B の外である。** | ⚠️ **`D-318` は本行の帰結である。⛔ 引用を直さないまま `D-318` を直すと、誤った根拠が残る。** | ✅ **2026-09-08** —— 検査42が501→473(この巡の合計28件の返済)。実数は5ファイル7か所ではなく11か所であった(素朴なgrepが4か所を落とす。文がコメントの折り返しで2行に割れているため)。「おらず」と「隠れている子」の2本で引くと全数が出る。5か所はわざと残した(いずれも「この行にはそう書いていない」と打ち消すための引用)。`screen-renderer.ts`がその捏造文から導いていた推論は丸ごと消した。 |
| D-342 | ⚠️ **単体試験が 1 本、走らせるたびに結果が変わる（実測 2026-09-06）** —— **`tests/unit/uf-47-48.test.ts` の曜日の件が、全体を走らせたとき 1 度落ち、そのあとの 3 回はすべて通った。単独で走らせると 3 回とも通る。**⇒ ⛔ **ファイルをまたぐ状態（表示言語らしい）が残っている疑いが濃い。**⚠️ **これは推測である。⭐ 裏を取ること。** | 同じ木に対して、走らせる順で結果が変わらないこと | ①ファイルをまたぐ状態を各件が自分で立て直す ②落ちる条件を先に特定する | ⭐ **先に測ること** —— **落ちるのがどの順番のときかを掴むまで、直しに入らない。**⚠️ **緑と赤が混ざる試験は、赤の意味を殺す** —— **「たまに落ちる」で片づけられるようになると、本物の退行も同じ扱いになる。** | `取下げ` | ⭐ **取下げ 2026-09-06。**`D-352` に吸収された —— **本行の推測「表示言語らしい」は反証されている（`rulerWeekdayWords` は `@purity pure` で、模作も大域も持たない）。**掉ちる本当の理由は共有される `globalThis.requestAnimationFrame` で、`D-352` がそれを持つ。 | ⚠️ **本波の変更が持ち込んだものではない。基準の実行で既に落ちている。** | ✅ **2026-09-06** —— **6 回走らせて 1 度も再現しなかった。** |
| D-348 | ⛔⛔ **`npm run e2e` が赤い（5 ファイル、実測 2026-09-06 の巡の頭）** —— **85 通過 / 5 落ち。引継書はこの状態を何も述べていなかった。**⚠️ **症状を集めただけであり、原因はまだ分けていない。**⭐ **5 つの症状**: ①`nfr-002-003` の性能の関門 14 条項（`D-330` が既に持つ）②`FR-025` `exportPng` が `notAvailable`（「`Rasterizer`（`IF-6`）を運んでいない」）③`FR-067` `exportEmbeddedHtml` が `notAvailable`（「`AppShellSource`（`IF-8`）を運んでいない」）④`FR-073` / `FR-022` の `U-61`（`Difference Review`）が描かれていない⑤`measured-sweep` の `D-210`（月の段の押下）と `three-rows` の `SK-3` / `SL-1`（`Delete`）。 | `npm run e2e` が緑であること。赤いなら、どの行がそれを持つかが台帳から読めること | ①症状を二分して原因ごとに行を立てる ②既存の行（`D-330` `D-300` など）に寄せる | ⭐⭐ **本行は 7 つの原因に割った（2026-09-07）。**⇒ **`D-330`（性能）に寄せた 1 束 ＋ 新しい `D-356`〜`D-361`。**⛔⛔ **本行の数は 3 つとも誤りであった** —— ① 実測は **84 通過 / 6 落ち / 6 ファイル**であって 85 / 5 / 5 ではない ② `nfr-002-003` の条項は **13** であって 14 ではなく、本行が ②③ と呼んだものは**別ファイルではなく同じ 1 つの `it` の中の条項**である ③ **`npm run e2e` の前に `npx vite build` が要る**（`playwright.config.ts` の `webServer` は `npm run dev` だけを立てる）。⛔⛔ **これを知らずに測ると偽の赤が 7 本出る。**⚠️ **`sample-schedule/` は `.gitignore` されているので、どの作業木にも無い** —— 親から写さないと `FR-021` の条項が 1 本余分に落ちる。 | `取下げ` | ⛔ **未検討。**⭐⭐ **④ は台帳と食い違う** —— **`D-300` は「面は建ち、押して測った」と記録されているのに、出荷する造りを押す試験は「`U-61` はページに無い」と言う。**⭐ **実測待ち 12 行を押す体が同じ日に走っており、`D-300` はその 12 行に入っている。その報告で分けること。**⚠️ **②③ はいずれも `Agent API` の継ぎ目であり、造りが自分で「まだ作っていない」と告げている。** ⭐⭐ **④ は `D-300` ではないことが同日に分かれた** —— **出荷ビルドを押す体が、`Difference Review` が候補 1000 行を並べ `IC-95` 〜 `IC-97` が立つことを実測した。**⇒ ⛔ **e2e が落ちているのは `Agent API` の `importDocument`（`AM-8`）の経路であり、人が押す経路ではない。②③ と同じ束である。** ⛔ **本行が 1 つも挙げていなかった赤が 2 つ在った**（`user-reported-fixes.test.ts:715` と `nfr-004-file-scheme-sweep.sws.test.ts:1181`）—— **どちらも本行を書いた `1024dbd` より後に入った変化である。** | ⭐ **巡の頭で `npm run e2e` を数えたときに出た（規則どおり 3 つとも数えた）。** ⚠️ **`npm test` 6896 と `npm run parity` は同じ日に緑であった** —— 赤いのは e2e だけである | ✅ **2026-09-06** —— **`npm run e2e`: 85 通過 / 5 落ち / 7.9 分。落ちたファイル名は上に挙げた。** |
| D-350 | ⛔⛔ **`src/use-case` が捌く命令 81 種のうち 15 種に、人の入口が 1 つも無い**（機械で数えた 2026-09-06）—— **翻訳器と殻からコメントを剥がして、その命令名が 1 度も現れない種を数えた。**⭐⭐ **台帳に行が在るのは 4 種だけである** —— **`setExportPngScale`（`D-261`）／`setResourceName`（`D-273`）／`setCommentBoxText`（`D-283`）／`setCommentBoxLeaderShapeKind`（`D-302`）。**⛔ **残る 11 種はどこにも記録が無い**: `deleteUnreferencedResources` / `reorderTaskGroupSiblings` / `setCalendar` / `setCommentBoxAnchor` / `setFontScale` / `setHighlightBoxRange` / `setHighlightBoxStrokeColor` / `setProjectProfile` / `setStackDirection` / `setThemeHue` / `setThemeMonochrome`。⚠️ **うち 5 種は `STOP` のコメントにすら現れない** —— **誰も気づいていない。** | 仕様が定める命令に、人が押す入口が在ること。無いなら、無いことが記録されていること | ①15 種それぞれに入口が要るかを仕様で確かめ、要る種に行を立てる ②入口を持たない種は、その旨を仕様に書く | ⭐ **第 4 章 `FR-029`（`docs/spec/01-04-requirements.md`）。**同要求は 表 T-109 を「アイコンの全数」と定め、**入口はそこにしか置けない。**⇒ ⛔ **本行は「入口が無い」を数えた記録であり、15 種それぞれの是非は仕様を引いて決める。**⚠️ **すべてに入口が要るとは限らない** —— **`FR-065` の `Agent API` から届く命令や、ほかの命令の副作用として出る命令が混ざりうる。**⛔ **確かめずに 11 行を立てるな。** ⛔⛔ **数を訂正した（実測 2026-09-07）: 81 種のうち 15 種ではなく、72 種のうち 13 種である。**⭐ 命令の全数 72 は 表 T-108 の行数（`CM-1`〜`CM-69` ＋ `CM-71` ／ `CM-72` ／ `CM-73`、`CM-70` は退役）と完全に一致する。⛔ **81 が出たのは、`src/use-case` 全体の `case` を数えて、命令でない 8 種（取り込みの様式 3 ＋ 予実の状態値 5）を混ぜたからである。**⭐ **15 → 13 の差分**: `setExportPngScale` はもはや命令ではない（`CM-70` 退役）、`setCommentBoxText` は入口が着いた。⛔ **「5 種は STOP にすら現れない」も誤り** —— **13 種のうち 12 種**が宣言と `case` 以外どこにも現れない。⚠️ `edit-document.ts` の冒頭が「73 命令（見せ方の群 16）」と書くが、実数は 72（同 15）である。  ⇒ ⭐ **種別 C の 5 件を `PD-431`〜`PD-435` として立てた（2026-09-07）** —— 担当者の改名・暦の面・文書の基本情報の面・テーマの色相の入口・ハイライトボックスの線色の面。（`setCommentBoxLeaderShapeKind` は `D-302` に集約した。）⛔ **5 件とも 規則 06 の `D` か `E` に落ちるので、推奨値で進めてはならない。**⭐ **種別 A の 6 種は `実装待ち` のまま 3 束に割れる**（設定の面が操作子を持たない 3 種 ／ `GR-14` の当たり判定が無い 2 種 ／ `IC-65` と `FR-099` の食い違い 1 種）。  ⭐⭐ **利用者の裁定 2026-09-07（逐語）: 「裁定はすべて提案通りでよい。」**⇒ **PD-431〜PD-435 が `裁定済` になった。⛔ 二度と問うな。次は仕様を書く番である。** | `仕様待ち` | ⭐ 数の訂正は済んでいる(71種のうち13種)。残るのは記録の無い6種だけで、それはD-404が持つ。数の訂正は済み、残るのはD-404の6種である。 ⭐ 反証 2026-09-08: ⛔ 数を訂正する: **「71 種のうち 13 種」**（旧「81 種のうち 15 種」）。 測り方（機械、`docs/spec` に依らない）: `edit-document.ts` の 8 本の `*_KINDS` 配列 （`ROUTE_TABLE` が全数を強制している）から文字列を抜いて重複を除く ⇒ 71 種。 入口の側は `input-command-translator` / `single-html-shell` / `screen-renderer` / `dom-screen-surface` の 全 `.ts` からコメントを剥がし、名が 1 度も現れない種を数える ⇒ 13 種: `deleteUnreferencedResources` / `reorderTaskGroupSiblings` / `setCalendar` / `setCommentBoxAnchor` / `setCommentBoxLeaderShapeKind` / `setFontScale` / `setHighlightBoxRange` / `setHighlightBoxStrokeColor` / `setProjectProfile` / `setResourceName` / `setStackDirection` / `setThemeHue` / `setThemeMonochrome`。 吸収済みは 2 種のみ: `setResourceName` → `D-273` ＋ `D-397`、`setCommentBoxLeaderShapeKind` → `D-302`。 ⚠️ 残る 11 種のうち 5 種は `PD-431`〜`PD-435` が裁定済。 ⛔ どの PD にも D にも記録の無い 6 種: `deleteUnreferencedResources` / `reorderTaskGroupSiblings` / `setCommentBoxAnchor` / `setFontScale` / `setStackDirection` / `setThemeMonochrome`。 ⭐ `setExportPngScale` は `CM-70` の退役で集合から消えている（旧 15 種の水増しの一部）。  ⛔ **未検討。**⭐⭐ **これは 1 件ずつの不具合ではなく 1 つの型である** —— **`D-283` の欄が「`D-261` `D-271` `D-278` と同じ形である。この巡だけで 4 件目であり、これは 1 件ずつの不具合ではなく 1 つの型である」と既に書いている。**⇒ ⭐ **本行はその型を機械で数え切ったものである。**⛔⛔ **台帳の残件は、この型を数え直すと増える** —— **減っていないのではなく、数えていなかった。**⭐ **数え方**: **`src/use-case` の `case '<kind>':` を集め、`src/adapter/input-command-translator/` と `src/framework/` からコメントを剥がしてその文字列を数える。0 なら入口が無い。**⚠️ **同型だが命令ではない 2 件が別に在る** —— **`restoreOpenedFilePermission`（`D-278`）と `clampedSettings`（`D-312`）は宣言と本体が在って呼び手が 0 である。**⭐ **束の全表は `docs/development-records/evidence/blockers-2026-09-06.md` に在る。** ⭐ **13 種を種別で割った（2026-09-07）**: **種別 A（仕様に条項が在るのに造りが無い）6 種** —— `setStackDirection` `setFontScale` `setThemeMonochrome`（`IC-17` の設定の面が値を刷るだけで操作子を持たない。`properties-panel.ts:1695` の `settingsFields` が `controls: []`）、`setCommentBoxAnchor` `setHighlightBoxRange`（`GR-14` のアンカーと四隅の当たり判定の大きさがどの表にも無い）、`deleteUnreferencedResources`（`IC-65` の「正」欄が `CM-43` を名指すのに、造りは同じ入口を「選ぶ」に読み替えている）。**種別 B（そもそも入口を持たない設計）1 種** —— `reorderTaskGroupSiblings`（`HM-8` の入口は `CM-73` が受けている）。**種別 C（仕様が何も言っていない）6 種** —— `setResourceName` `setCalendar` `setProjectProfile` `setThemeHue` `setHighlightBoxStrokeColor` `setCommentBoxLeaderShapeKind`（最後の 1 つは `D-302` に集約する）。⛔ **種別 C の 5 件は規則 06 の `D` か `E` に落ちるので、推奨値で進めてはならない。** | ⭐ 反証 2026-09-08: ⛔ 数を訂正する: **「71 種のうち 13 種」**（旧「81 種のうち 15 種」）。 測り方（機械、`docs/spec` に依らない）: `edit-document.ts` の 8 本の `*_KINDS` 配列 （`ROUTE_TABLE` が全数を強制している）から文字列を抜いて重複を除く ⇒ 71 種。 入口の側は `input-command-translator` / `single-html-shell` / `screen-renderer` / `dom-screen-surface` の 全 `.ts` からコメントを剥がし、名が 1 度も現れない種を数える ⇒ 13 種: `deleteUnreferencedResources` / `reorderTaskGroupSiblings` / `setCalendar` / `setCommentBoxAnchor` / `setCommentBoxLeaderShapeKind` / `setFontScale` / `setHighlightBoxRange` / `setHighlightBoxStrokeColor` / `setProjectProfile` / `setResourceName` / `setStackDirection` / `setThemeHue` / `setThemeMonochrome`。 吸収済みは 2 種のみ: `setResourceName` → `D-273` ＋ `D-397`、`setCommentBoxLeaderShapeKind` → `D-302`。 ⚠️ 残る 11 種のうち 5 種は `PD-431`〜`PD-435` が裁定済。 ⛔ どの PD にも D にも記録の無い 6 種: `deleteUnreferencedResources` / `reorderTaskGroupSiblings` / `setCommentBoxAnchor` / `setFontScale` / `setStackDirection` / `setThemeMonochrome`。 ⭐ `setExportPngScale` は `CM-70` の退役で集合から消えている（旧 15 種の水増しの一部）。  ⭐ **残件 50 行を「何が止めているか」で束ね直した体が、機械で数えて挙げた（2026-09-06）。** ⚠️ **この巡に同型が 3 件閉じた**（`D-290` / `D-321` / `D-301`）—— ⛔ **ただし `D-301` の `Esc` の道はまだ命令を出さない** | ⛔ **押していない。**⭐ **数えたのは木であって画面ではない。** |
| D-352 | ⚠️ **単体試験を並行で走らせると 9 ファイルが落ちる**（実測 2026-09-06）—— **`--sequence.concurrent` を入れると 9 ファイルが落ちる。既定の隔離では落ちない。**⭐ **痑うべきはファイル内で共有される `globalThis.requestAnimationFrame` である** —— **`tests/unit/uf-47-48.test.ts:363` が `realRaf` を**取り込み時**に捕まえており、前のファイルの偽物を掋みうる。** | 試験が、走らせ方で結果を変えないこと | ①`realRaf` を取り込み時ではなく使うときに捕まえる ②偽物を置く側に後始末を置く | ⭐ **仕様の行では決まらない。規則 04 の側である** —— **「緑と赤が混ざる試験は、赤の意味を殺す」（`D-342` の期待値の欄）。** ⭐⭐ **裁定（2026-09-07、前に立つ者）: 取り下げる。**⛔ **`--sequence.concurrent` は本試験群の走らせ方ではない。** | `取下げ` | ⛔ **未検討。**⭐⭐ **`D-342`（走らせるたびに結果が変わる）を掴まえにいった体が、6 回走らせて 1 度も再現できず、代わりにこれを掘り当てた。**⛔ **「表示言語らしい」という `D-342` の推測は反証された** —— **`rulerWeekdayWords` は `@purity pure` で、模作も大域も持たない。**⭐ **実測した 6 通り**: **`tests/unit` 全体 × 2（既定の順）／`--sequence.shuffle --seed=11`／`--no-isolate --shuffle --seed=5`／単独 `--shuffle --seed=1`。** ⛔ **数は 9 ではなく 7 である**（2 回の実測が一致）。⛔⛔ **台帳が名指した原因は 4 通りに反証された** —— ①`uf-47-48.test.ts` はこの旗の下で通る ②`:363` の `realRaf` の形は赤くなるファイルと同一で、赤緑を分けていない ③`--no-isolate`（ファイルが大域を共有する）は緑なので、偽の rAF はファイルの境を越えていない ④赤い 7 本は 1 本ずつ単独でも赤く、**原因はファイル内の同時走行である。**⭐ **本当の原因は 3 機構**（大域の `requestAnimationFrame` 差し替え・`vi.resetModules()`／`vi.doMock` のモジュール登録簿・`expect.soft` を並行の件で vitest が拒むこと）で、どれも vitest に per-test の形が無い。⭐ **そして並行は速くない** —— 既定 19.15 秒に対し 20.03 / 20.09 秒。⇒ **買えるものが遅くなることだけである。**⚠️ 測ったことは `vitest.config.ts:19-52` に書いた。⛔ **config で並行を off にはしていない** —— それは塞ぐことになる。 | ⭐ **`D-342` を掴まえにいった体が、掴まえられずに別の手掛かりを持ち帰った（2026-09-06）。** ⚠️ **`D-342` は閉じていない** —— 再現条件を掴むまで直しに入らないのが規則である | ✅ **2026-09-06** —— **`--sequence.concurrent` で 9 ファイルが落ちる。既定の隔離では 6 回中 0 回。** |
| D-357 | ⛔ **`Agent API` の `importDocument`（`AM-8`）が拒む。`FR-073` ／ `FR-022` の e2e 7 条項がすべてここから落ちている**（実測 2026-09-07。`D-348` の束 3）。 | 機械から取り込んだときも `FR-022` の 3 択が果たされること | ①`AM-8` に `MergeChoices` を渡させる ②`U-61` を立てて人に答えさせる | ⭐ **かつての問い（1 点だけ。2026-09-07 に下りた）** —— **呼ぶ側が機械であるとき、`FR-022` の「人に選ばせる」をどう満たすか。**⛔ **拒否の理由の半分は既に偽である** —— `agent-api-members.ts:837` の「`U-61` does not exist in this build」は `CR-366`（2026-09-06、`D-300`）が反証しており、`U-61` は `screen-renderer.ts:1499` ／ `:1579` ／ `:2485`、`dom-screen-surface.ts:5378`、`frame-loop.ts:1890` ／ `:2334` ／ `:6739` ／ `:7187` に在る。⇒ **残る理由は面の形（`AgentImportSource = { text }` に対し `ImportRequest` は 10 欄）だけであり、「表 T-107 の決めごと」という言い分は `D-356` と同じ理由で成り立たない。**  ⇒ ⭐ **`PD-438` として立てた（2026-09-07）** —— 問いと候補はその行が持つ。⛔ **本行に写しを作らない。**  ⭐⭐ **利用者の裁定 2026-09-07（逐語）: 「裁定はすべて提案通りでよい。」**⇒ **PD-438 が `裁定済` になった。⛔ 二度と問うな。次は仕様を書く番である。**  ⭐⭐ **仕様を書いた（2026-09-07）** —— **`FR-022` に「呼ぶ側が機械であっても、選ぶのは人であること（MUST）」「`AM-8` が合流にあたるときは `U-61` を立て、人が答えるまで待つこと（MUST）」「合流を拒んではならない（MUST NOT）」を足した。**⚠️⚠️ **代償を承知の裁定である** —— **無人運転はできない。**  ⭐⭐ **不具合は閉じた（2026-09-07）。**⭐ **`AM-8` は `Promise` になり、`U-61` を立てて人が答えるまで待つ。**⛔ **選択肢は呼び出しに渡させていない**（`FR-022` の MUST NOT）。⭐ **機械も人と同じ道を歩く** —— **`OP-5`・`FR-023` の落とし・`FR-088`・`OP-4`・`U-61`・書き込み。分かれるのは読む半分だけである。**⛔⛔ **反証**: **「`FR-073` の告げが面に無いから取り込みを拒む」は成り立たなかった** —— **`FR-073` も「拒んではならない（MUST NOT）」と定めており、1 つの MUST NOT を守るために別の MUST NOT を破っていた。**⛔⛔ **e2e の件が、いまの `FR-022` と噛み合わない** —— **同じ `page.evaluate` の中で `AM-8` を `await` しているので、答える押しに到達できず 240 秒で時間切れになる。⭐ これは裁定が名指した代償そのものである（無人運転はできない）。**⇒ **件の側を「約束を預けて → 押して → 読む」形に直すこと。⛔ 直した体には書き直させない**（規則 04 の 1 条）。⚠️ **同じ件の 2 条項は別の持ち主である** —— **`reviewText` が読めなかった列を名指し、その列が `AM-11` を越えて残ること。⇒ `DocumentCodec` の `FR-073` の運びである。** | `実測待ち` | ⭐ 直した(2026-09-08): `page.evaluate`の中で人の答えを待つ呼びを`await`していた試験を3つに割った(①約束を`window`へ預ける ②`U-61`の答えを押す ③解決した約束を読む)。実測: 直す前は1落ち・1未実行+`beforeAll`が240秒で時間切れ⇒直した後は1通過・1落ち(10.9秒)。到達できなかった4条項が通るようになった。残る赤は別の欠陥(`GR-21`の掃引が押す先を持たない)である。 ⭐ 反証 2026-09-08: ⛔ 誤診であった。製品の欠陥ではなく、**試験の欠陥**である。 `tests/system/fr-073-fr-022-the-difference-review-that-is-not-there.test.ts:334` が `page.evaluate` の中で `AM-8` を `await` している。`AM-8` は `U-61` を立てて人が答えるまで解決しないので、 この `await` は同じ評価の中では永遠に返らず、`:180` の `beforeAll` が時間切れになる。 ⇒ 7 条項すべてが `sweepFailed` 経由で同時に落ちる。 仕様どおりに動いている: `FR-022`「呼ぶ側が機械であっても、選ぶのは人であること（MUST）」。 ⇒ `:330` 付近のループを 3 つに割る（①約束を window へ預ける evaluate ②`U-61` の答えを押す ③解決した約束を読む evaluate）。 ステータスは `試験待ち`。  ⚠️ `agent-api-members.ts:837` の偽になった注は、直す体が同時に消すこと。 | ⛔ **まだ押していない。** | ✅ **2026-09-07、出荷ビルド** —— `AM-8` に 999 件の写しを渡すと `Difference Review` が立ち、UID と名前で候補を並べ、**呼び出しは立っているあいだ未決のまま**（`stillWaiting: true`）。`IC-95` を押すと `{"accepted":true,…}` で決着し、**引いた 1 件は残る（前後とも 1000 件）**。 |
| D-362 | ⛔ **`MK-13`（コメントボックスのダブルクリック）が、本文の欄を持つプロパティパネルを開かない**（実測 2026-09-07）—— **さらに、欄に打った値はどの命令にも届かない。** | コメントボックスをダブルクリックすると本文の欄が立ち、打った値が文書に入ること | ①`MK-13` の道を繋ぐ ②`commandFromFieldCommit` に `commentBox` の腕を足す | ⭐ **①と②の両方が要る。**⛔ **実測**: 単押しでは選ばれる（`readSelection` は `{kind:"commentBox"}` を返す）が、ダブルクリックの後もプロパティパネルは `display: none` のままである。⛔ **書く側も塞がっている** —— `input-command-translator.ts` の `commandFromFieldCommit` に `commentBox` の腕が無いので、確定した値はどの命令にもならない。⚠️ **`D-349`（欄が操作子を持つこと）は別に閉じている** —— 欄そのものは `PR-21` として正しく描かれる。本行は**そこへ至る道**である。 ⛔⛔ **重複である。取り下げる（2026-09-07、同じ巡のうちに）。**⭐ **本行の中身は `D-283` がそのまま持っている** —— 同じ `MK-13` の道であり、同じ `commandFromFieldCommit` の欠けである。⚠️ **前に立つ者が、掃引の報告が戻る前に行を立てた** —— 引継書 2 節の ① が名指す型そのものであり、**同じ巡で 2 度目である。** | `取下げ` | ⭐ 見つけたのは `D-349` の試験を書いた体である（`FR-072` の `IC-17` を経由してパネルへ回り込んだ）。 ⭐ **捉まえたのは、掃引の報告と自分の行を突き合わせたことである。**⛔ **次は、行を立てる前に台帳を `grep` すること。** | ⛔ **押した。ダブルクリックでは開かない。** | ⛔ **まだ押していない。** |
| D-369 | ⚠️ **`NFR-004` の掃引で `SL-7b` が押せない —— 前の探りが残した状態が原因の疑いが濃い**（掃引の幾何を直した体が 2026-09-07 に見つけた）。⭐ **直す前は緑であったが、それは押しが `Command Palette` に取られていたからである。** | 掃引の各手が、前の手の残した状態に左右されずに押せること | ①手と手のあいだで選択を戻す ②`SL-7b` の手を掃引の先頭へ移す ③`SL-2` を疑う（⛔ 反証済み） | ⛔ **未検討。**⭐ **③は反証されている** —— **開き直した出荷ビルドでは仕掛けが健全である**（1 本のバーで破線 10 → 11、`Ctrl+A` で 141、**素の押しでもう一度 11 —— `SL-2` はちゃんと置き換える** —— `Shift` で 12 になり `data-enabled=true`）。⇒ **`SL-2` は壊れていない。**⭐ **落ちたときの文言は既に精しい** —— 「2 つの押しが揃えの入口を `data-enabled=false` のままにした。破線は (1382, 479) の素の押しと (1643, 721) の `Shift` の押しを通じて 9 → 9 → 9」。⇒ **その位置では、バーを押しても何も選ばれていない。**⛔ **`IC-37` が薄いのは `SL-7b` 自身の MUST NOT が働いている姿であって、欠陥ではない。**  ⭐⭐ **反証され、直った（2026-09-07）。**⛔ **アプリの不具合ではなく、掃引の欠陥であった。**⭐ **掃引は `polygon` を無差別に拾っており、ズームのあとで最も広い候補が実績のバーになっていた。**⛔ **実績のバーの本体は、表 T-023d が意図して掴めなくしている**（`item-hit-area.ts` が逐語でそう書いている）。⇒ **2 度の押しはどちらも何も選んでおらず、`IC-37` が薄いのは `SL-7b` の MUST NOT が働いている姿であった。**⭐ **`-plan` だけを拾うようにしたところ、同じ 22 の前段のあとで破線が 11 → 11 → 12 になり `IC-37` が効いた。**  ⭐⭐ **直った（2026-09-07、`npm run e2e`）** —— **`SL-7b` はもう落ちていない。**⛔⛔ **ただし、その裏に別の行が隠れていた** —— **同じ件がいま `SK-19` で落ちる**（「`SK-19`（answers: moved=false notices=0 shapes+0 dashed+0 files+0 clipboard+0 held=none）」）。⇒ **`SL-7b` が押せないあいだ、掃引は `SK-19` まで到達していなかった。**⭐ **`D-382` として割った。**⛔ **本行は `SL-7b` の件であり、そちらは閉じてよい。** | `取下げ` | ⭐ 反証 2026-09-08: `SL-7b` は既に緑。反証 2026-09-08: `tests/system/nfr-004-file-scheme-sweep.sws.test.ts:388` が 実績のバーを除く filter を持ち、修理は `5223627` で入っている。 ⭐ 薄いのは正しい姿であった（`SL-7b` 自身の MUST NOT）。裏に残った件は `D-382` が持つ。  ⭐ **掃引 1 回が 16 分かかるので、体は三行の担当の外として追わずに戻した。正しい止まり方である。**⭐ **診断は足してある** —— `press` が「入口が無い／大きさ 0 ／覆われている／薄い」のどれかを言い、`didNotAnswer` が 5 つの読みのどれが動いたかを刷る。 |  | ⭐ **押した（2026-09-07、`npm run e2e`）。`SL-7b` は緑。** |
| D-372 | ⛔⛔ **タスクを作る流れが、利用者の求める形になっていない**（利用者の指示 2026-09-07）—— **①構えが拒まれることがある ②ドラッグ中に絵が伸びない ③置いたあと名前を入れる道が閉じない ④構えが続かない、の 4 つ。** | アイコンを押すと必ずその構えに入り、ドラッグ中は形がポインタに付いて伸び、離すとプロパティパネルが `Name` を出し、`Enter` で閉じて選択が外れ、構えは続き、`Esc` で構えも解けること（利用者の逐語） | ①4 つを 1 つの流れとして仕様に書き、まとめて直す ②1 つずつ別の行に割る | ⛔ **未検討。**⭐ **仕様の場所**: `FR-030`（作る）・`FR-078`（構え）・表 T-023d（掴み領域）・表 T-028 の `IN-4`（`Esc` の階層）・`FR-072`（プロパティパネル）・表 T-016 の `PR-1`（`Name`）。⚠️ **①は既存の規則と噛み合わせが要る** —— **「他のマイルストーンを選択していても構えに入る」は、選択と構えが別の状態であることを言っている。**⚠️ **③の「`Enter` で閉じて選択を解除」は、いまの `Esc` の階層（`textEntry` → `propertiesPanel`）と別の道を足すことになる。**  ⭐⭐ **仕様を書いた（2026-09-07）** —— **`FR-083` の `SP-2` / `SP-3` が「構えは変えない」をやめ、「形状を変え、あわせてその形状を構える」になった。**⛔ **選んでいるものが在ることを理由に構えを拒むことは MUST NOT で禁じた。**⭐ **形状の変更が拒まれたときも構えは立てる** —— **`CM-20` の拒否は選んでいるものの話であって、次に置くものの話ではない。**⭐ **表 T-023a（押したときの判定順序）の「構えて何もない場所を押す」行に「押しているあいだ、置くことになる姿を描いてポインタに追従させ、引いた向きに伸ばすこと（MUST）」を足した。**⚠️ **その行の ID は別の名簿の行と綴りが衝突するので、本行には綴らない。**⭐ **`FR-091` に「作った直後の名称を `Enter` で確定したときは、同じ 1 回の押下でパネルを閉じ、その選択を解くこと（MUST）」を足した。**⚠️⚠️ **`FR-072` の 2026-08-30 の MUST NOT（パネルをやめても選択を解くな）とは衝突していない** —— **あちらは人が選んだ選択を守り、こちらは道具が`FR-001` のために立てた選択を片づける。**⛔ **覆すなら言ってほしい。**  ⭐⭐ **①を実装した（前に立つ者、2026-09-07）** —— **`screenStateFromEntry` から `if (context.selection.items.length > 0) return state` を外した。**⭐ **選択は、構えを決める場面で 1 度も読まれなくなった** —— **押した入口が既に構えている入口なら解け、そうでなければ構える。それだけである。**⭐ **形状を変える半分（`commandFromArmingEntry`）は元から働いており、触っていない。**⚠️ **②③④はまだ残っている。**  ⭐⭐ **②を利用者が再現し、操作ログが機構まで示した（2026-09-08）** —— **押下 `seq 3218`（x=896, y=97）から離す `seq 3281`（x=964, y=127）まで、途中のフレームは 32 枚あり、`svgBytes` は 14889 で 1 バイトも動かない。離した瞬間に 15579 へ跳ぶ。**⇒ **引いているあいだ、絵に何も足していない。**⭐ **フレーム自体は回っている**（`frame=true` が並ぶ）—— **止まっているのは「何を描くか」であって、ループではない。**⚠️ **同じ形が `seq 3932`〜`4041` にもある**（14889 → 16910）。⇒ **次の巡で最初に直すべきものの 1 つである。** | `実装待ち` | ⭐⭐ **操作ログの実測（2026-09-07）**: **①** `IC-29`（五角形）を押しても `act=-` で**何も起きない**（seq 1719-1723）。`IC-28`（六角形）は `act=changeDocument` だが `doc=same` で**通知が 1 枚立つ**（seq 981-986、1393-1398）—— **構えが拒まれている。②** 空所を押して 147px 引いても、離すまで `svgBytes` が 20597 のまま動かない（seq 2409-2482）—— **ドラッグ中に何も描かれていない。③** `Enter` は `settleTextEntry` を起こすがパネルは開いたままで、そのあとの `Esc` が `propertiesPanel` を閉じている（seq 1868-1955）。**④** 置いたあとに続けて置けているので、構えの継続は満たされている可能性がある（seq 1843 と 1947）。 |  | ⛔ **押した。①②③は実測で確かめた（2026-09-07）。** |
| D-373 | ⛔ **マイルストーンを構えているときのドラフトが、タスクの形で描かれる**（利用者の指摘 2026-09-07）—— **置こうとしている物と違う形が出る。** | ドラフトが、そのマイルストーンの予定の形状と同じ種類の形状であること（利用者の逐語） | ①構えている行 ID から 表 T-012 の形状を引いて描く ②ドラフトを出さない | ⛔ **未検討。**⭐ **仕様の場所**: 表 T-012 の `SH-5`（形状の面積順の並び）と `FR-078`（構え）、表 T-023d の閉じの規則（掴んでいるあいだ置くことになる姿を描く）。⭐ **①が素直である** —— **構えは行 ID を持っており、その行 ID が形状を決める。**⛔ **形を 2 か所に持たせないこと** —— 描くほうと置くほうが同じ表を引く。  ⭐⭐ **仕様を書いた（2026-09-07）** —— **表 T-023a（押したときの判定順序）の「構えて何もない場所を押す」行に「その姿は、構えている行が 表 T-012 で持つ形状とすること（MUST）。ほかの形状で描いてはならない（MUST NOT）」を足した。**⛔ **形状を 2 か所に持たせないこと（MUST NOT）** —— **描く側と置く側が同じ表を引く。**⚠️ **踏査の実測（2026-09-07）: いまの造りには、構えているあいだのドラフトを描く仕掛けが 1 つも無い。**⇒ **利用者が見た「タスクの形」は、構えていないときに出る `PD-5` の選択の矩形である可能性が高い。** | `実装待ち` | ⚠️ **操作ログにはドラフトが 1 度も現れていない**（`follow=-` が全行）—— **本行の「タスクが表示されて変」は別の場面（構えたまま日程の上を動かしたとき）で起きている可能性が高い。**⛔ **まず再現の場面を押さえること。** |  | ⛔ **利用者が見ている。前に立つ者はまだ押していない。** |
| D-377 | ⚠️ **検査 39 の借金を 934 → 951 へ上げた（2026-09-07）** —— `FR-016` の 2 つの上限と `EP-9` の太さを書いたが、その 18 の条項を逐語で持つ試験がまだ無い。 | 上げた 17 を、同じ巡のうちに返すこと | ①実装の波で錨を書き、基準線を下げる ②条項を減らす（⛔ 裁定に反する） | ⛔ **未検討。**⭐ **上げた理由は基準線のファイル自身が持つ。**⭐ **上げた場所は仕様に在る** —— **`FR-016`（`docs/spec/01-04-requirements.md`、拡大の上限と行の軸の錨）と 表 T-076 の `EP-9`（仕切りの太さ）である。**⭐ **返す当てはある** —— **`D-374` / `D-375` / `D-366` / `D-363` を実装する波が、直った 1 行につき錨を 1 本書く。その錨が条項を逐語で持てば、そのぶん基準線は下がる。**⛔ **下げ忘れたまま巡を閉じてはならない** —— **借金は、返す当てがあるときだけ許される。** | `実装待ち` | ⭐ **上げた 2 か所**: `FR-016`（拡大の上限・行の軸の錨）と `EP-9`（仕切りの太さ）。 |  | ⛔ **押していない（仕様の借金である）。** |
| D-382 | ⛔ **`NFR-004` の掃引で `SK-19` が答えない（実測 2026-09-07、`npm run e2e`）** —— **「`SK-19`（answers: moved=false notices=0 shapes+0 dashed+0 files+0 clipboard+0 held=none）」。**⇒ **`Enter` を押しても、絵も通知も選択も 1 つも動かない。** | 掃引の各手が答えること（`SK-19` は `Enter` の階層である） | ①`SK-19` の場面を掃引が作れていない ②`Enter` の道が本当に何もしていない ③期待している答えの読みが違う | ⛔ **未検討。**⭐⭐ **これは `D-369` を直して初めて見えた** —— **`SL-7b` が押せないあいだ、掃引はここまで到達していなかった。**⚠️⚠️ **`SK-19` の規則はこの巡で動いている** —— **`FR-091` に「作った直後の名称を `Enter` で確定したときは、同じ 1 回の押下でパネルを閉じ、選択を解く（MUST）」を足したので、⚠️ 反証済み（2026-09-08）: `D-372` の実装が済むまで、この件が何を期待すべきかが定まらない。**⇒ ⛔ **`D-372` を実装してから読み直すこと。先に試験だけを直すな。**⭐ **仕様の場所**: 表 T-036 の `SK-19` と `FR-091`、表 T-028 の `IN-4`。 | `実測待ち` | ⭐ 直した(2026-09-08): 申し送り「D-372を実装してからでないと期待値が定まらない」は偽であった。さらに、反証の診断(「1度目のEnterが第1段に食われる」)も偽であった——体が出荷ビルドを押して測り直し、新しい盤ではダブルクリックがそもそもパネルを開かない(SK-18の後、いちばん広い-planの走りがTaskのバーではない)ことを突き止めた。表T-109のIC-17(文書の描画設定をパネルに出す)から開く形に替えた。倍率で動かない入口である。実測: nfr-004が1通過・2落ち⇒2通過・1落ち。 ⭐ 反証 2026-09-08: ⛔ 誤診であった。⛔ 「`D-372` を実装してからでないと期待値が定まらない」という申し送りは**偽**である —— `FR-091` の新しい MUST は「作った直後の名称を Enter で確定したとき」の話であり、この手はタスクを作っていない。 本物は掃引の側: `tests/system/nfr-004-file-scheme-sweep.sws.test.ts:1085` 付近が `barBody` をダブルクリックしてから Enter を押す。ダブルクリックは `PR-1` に焦点を置き（表 T-023 の `MK-13`）、 焦点だけで `hasUnsettledTextEntry()` は真になる（`dom-screen-surface.ts:8271` は `heldTextControl !== null` を返すだけ）。 ⇒ Enter は `input-command-translator.ts:3889` の第 1 段（その場の編集の確定）に食われ、 値が動いていないので `IN-6` / `FR-031` により何も書かれず、面も閉じない。 掃引の合否は `one.moved` の差だけなので、**適合した振る舞いが必ず赤になる**。 仕様は 表 T-036 の `SK-19` の 2 段構造で、第 2 段は `:3890` 付近に実装済み。 ⛔ アプリを直すな。⇒ 掃引側を直す。ステータスは `試験待ち`。  ⭐ **`D-369` の直しが露出させた。** |  | ⛔ **押した（掃引の中で。単独ではまだ測っていない）。** |
| D-383 | ⚠️ **構えている入口を押し直しても、何かを選んでいると解けなかった**（利用者の裁定 2026-09-07、見本 `previous-project-result/12-task-milestone-operation` の 6 件のうち②）—— **前に立つ者は「押し直しは何もしない」を提案し、利用者が却下した。** | 構えている入口を押し直したら、何を選んでいても構えが解けること | ①押し直しをトグルにし、選択を読まない ②押し直しを無効の押下のままにする | ⭐⭐ **利用者の逐語「反対。トグルにせよ。 ユーザーに選択肢がある。」** —— **案①を採る。**⭐ **仕様を書いた（2026-09-07）** —— **表 T-023b の締めの段落から「何も選んでいないときの」を外し、⛔ 選んでいるものの有無でこの再押下の意味を変えてはならない（MUST NOT）を足した。**⭐ **`FR-083` の同名の行からも同じ条件を外し、注を 1 つ足した** —— **「形状を変える」半分はこの押しでも残り、解けるのは構えの側だけである。**⭐ **「押せば必ずその構えに入る」（同日の別の指示）とは衝突しない** —— **あれは別の入口についての規則であり、既に構えている入口はその規則が語らない唯一の場合である。**⭐⭐ **実装した（前に立つ者、2026-09-07）** —— **`screenStateFromEntry` が選択を読まなくなった。1 行の削除である。** | `取下げ` | ⭐ 反証 2026-09-08: 構えの押し直しは選択を読まない。反証 2026-09-08: `input-command-translator.ts:9099` が `isSameArm` だけで判じており、選択は 1 度も読まれない。 仕様 `FR-083`（`01-04-requirements.md:996`）「`SP-4` は選択の有無で変わらないこと（MUST）」に合っている。 ⚠️ 残るのは注 1 行 —— 同関数の先頭注（`:8963` 付近）が旧文 「With something selected the arming is left exactly as it is」を残している。⇒ `D-339` の掃除に含める。  ⭐ **見本で先に確かめた（2026-09-07、Playwright、`file://`、1280 × 900）** —— **何も選んでいないとき: 押して「矩形（タスク）」、押し直して「なし」。選んでいるとき（見本の「選択を外さない」側）: 選択「あり：（無題） / 矩形」を立てたまま、押して「矩形（タスク）」、押し直して「なし」、選択は「あり：（無題） / 矩形」のまま。対照: 別の入口（矢羽根）を押すと「矢羽根（タスク）」に構え直り、解けない。ページ例外 0 件。** | ⚠️ **見本の側も直した** —— **体が「選択が無いときだけ解ける」と実装していたので、裁定の MUST NOT に合わせて選択を読まないようにした。** | ✅ **2026-09-07、出荷ビルド（`dist/index.html` を `file://` で、1920 × 1080、前に立つ者が Playwright で押した）** —— **`IC-23` を押すと `data-armed="true"`、もう一度押すと `false`。何も選んでいない側は出荷ビルドで押せている。**⚠️ **選んでいるときの半分は、出荷ビルドでは立てられなかった** —— **絵の上で選択を立てる押しが本探りでは効かず、`data-selected` を持つ節点が 0 のままだった。⇒ その半分は単体（`t-109` の新しい件）と見本の実測が押している。⛔ 出荷ビルドで押したとは言わない。** |
| D-385 | ⚠️ **畳まれていて子を 1 つも持たない行で、1 段開く入口が使えるままになっている**（反証の体が `D-339` を測る途中で見つけた 2026-09-08）—— **そう振る舞う理由として注が挙げていた `RS-30` の文が、実在しない文であった。** | 表 T-051 の結び「その操作で、描かれる行が 1 行も増減しないときは、対象が 1 つも無いものとして扱うこと」と噛み合っていること | ①結びのとおり「対象が無い」として薄く描き、押されたら `RS-30` で告げる ②いまのまま構えたままにし、その理由を条項に書く | ⛔ **未検討。**⭐ **仕様の場所**: 表 T-051 の `HF-13` と結び、表 T-233 の `RS-30`（`docs/spec/01-04-requirements.md`）。⭐ **実物の `RS-30` は「直下に、画面へ戻せる子が 1 つも無い」であり、畳まれた子無しの行にも当てはまる** —— **告げても嘘にならない。**⇒ **案①を阻む理由は、少なくとも捏造された注の中には無い。**⛔⛔ **挙動を変える前に、`HF-13` の行そのものを読むこと** —— **注に合わせて直すのは、`D-339` が記録している失敗の型そのものである。** | `実測待ち` | ⭐ 直した(2026-09-08): 押す側の読みを描く側に揃えた(「絵が持たない直下の子を持つ親」)。描く側と押す側が同じ1回の描画から出た同じ絵を読むことを確かめた。わざと壊した実測: 条件を反転すると4件が赤。古い読みへ戻すと0件(=この行を押さえる錨は元から無かった)。 ⭐ 反証 2026-09-08: ⛔ 誤診であった。描く側は既に正しい —— `row-title-panel.ts:689` の `canOpenOneLevel` は `groupIdsWithAChildOutOfThePicture`（`:796` 付近）で、子が 0 の行を入れない。 本物は押す側 —— `input-command-translator.ts:6727` の `openOneLevel` が ``pressedRow?.isCollapsed === true` または `withHiddenChild.has(...)`` で、子 0 の畳まれた行を成立させる。 ⇒ `aria-disabled` を持つ入口が、押すと畳みを書く。 仕様は在る: 表 T-051 の `HF-13`（`01-04-requirements.md:1345`） 「開ける直下の子が 1 つも無いときは、`FR-029` に従って薄く描くこと（MUST）」。 ⚠️ 同ファイル `:6684` 付近の注「the drawing side answers this same question with the same two readings」は**偽**である。 ステータスは `実装待ち`。  ⭐ **場所**: `src/adapter/input-command-translator/input-command-translator.ts` の `openOneLevel` を組み立てる箇所。 | ⚠️ **`D-339`（仕様に無い一文を引く注）の子であり、同じ 1 か所から出ている。** | ⛔ **押していない。** |
| D-386 | ⚠️ **単体試験が `globalThis.requestAnimationFrame` を共有しており、38 のファイルがこれに代入している**（反証の体が `D-376` を測る途中で見つけた 2026-09-08）—— **作業スレッドで共有される値なので、復元の仕方によってはファイルをまたいで漏れる。** | ファイルをまたいで試験の結果が変わらないこと | ①代入する側に共通の据え付け／片づけを 1 つ用意する ②各ファイルで捕まえ直す ③放置する | ⛔ **未検討。**⭐ **`isolate` は既定の `true` なのでモジュールの状態は漏れないが、`globalThis` はその外である。**⚠️ **`uf-47-48.test.ts` は自分のモジュール読み込み時に捕まえた値を `afterEach` で戻しており、読み込みの順によっては別のファイルが差した値を「元の値」として戻すことになる。**⛔ **これはまだ症状ではない** —— **22 回の走行で 1 度も揺れていない。**⇒ **本行は「起きた」ではなく「起こりうる経路が在る」である。** | `取下げ` | ⭐ 反証 2026-09-08: 反証 2026-09-08。⛔ 数ではなく集合で述べる: `globalThis.requestAnimationFrame` を**読む**のは `src/framework/single-html-shell/frame-loop.ts` の 1 か所だけ。 **代入する**のは `tests/` 配下の試験ファイル群で、そのすべてが `afterEach` を持つ（未復元は無い）。 `vitest.config.ts` が「`npx vitest run --no-isolate` も緑（2026-09-07）」を実測として記録している —— 漏れうる唯一の条件で緑である。⇒ 偽の緑を作ってはいない。  ⭐ **測り方**: 同じ木で `npx vitest run` を繰り返し、落ちたファイルを記録する（体は 22 回走らせて 0 件であった）。 | ⚠️ **`D-376` の見出し（曜日の件がまれに落ちる）は偽であった** —— **本行はその代わりに見つかった、名指しされていなかった経路である。** | ⛔ **押していない。** |
| D-387 | ⛔⛔ **`Agent API` で書いたあと、実際の入力が来るまで画面が描き直されない**（反証の体の実測 2026-09-08、出荷ビルド）—— **文書は変わっているのに、絵は古いまま止まる。** | 文書が変われば、入力を待たずに画面がそれを映すこと | ①書き込みの後にフレームを 1 枚要求する ②毎フレーム文書を見比べる ③仕様として書く | ⛔ **未検討。**⭐ **`rAF` は毎秒 62 回動いている** —— **止まっているのは描き直しの判断であって、ループそのものではない。**⚠️⚠️ **これは測り方そのものを歪める** —— **`Agent API` で書いてから DOM を読む探りは、どれも「文書は変わったが絵は変わらない」と読む。**⇒ **`D-371` の元の申告がまさにその形であった。**⭐ **仕様の場所**: 表 T-078（描き直しの引き金）と `FR-028`（`Agent API`）。  ⚠️⚠️ **1 画素動かすだけでは足りないことがある（体の実測 2026-09-08）** —— **画面の状態が既にそう述べている場所へポインタを動かしても、フレームは来ない。いったん絵の外へ出して戻すこと。**⛔⛔ **ホイールでフレームを起こしてはならない** —— **ホイールはそれ自体が書き込み（`CM-65` / `CM-66`）であり、いま測っている刻を動かしてしまう。** | `実測待ち` | ⭐ 直した(2026-09-08): 表T-078のFT-2に実装が無かった。`holder.replace`(=WS-6そのもの)の末尾にFT-5と同じ形の`if (settled(environment)) ask()`を置いた。実測: `ask()`を消すと赤0件、その行を`throw`に替えると285件が赤⇒行は生きているが錨が1つも無い。FT-4の3つ目(NT-2の通知の期限)を計る仕掛けが`src/`に1つも無い(別途新しい行を立てた)。 ⭐ 反証 2026-09-08: 仕様は `05-07-design.md` の 表 T-078 の `FT-2` 「現在値の差し替え（表 T-067 の `WS-6`）｜ `SingleHtmlShell`（`CP-25`）｜ 表 T-067」。⇒ `FT-2` に実装が無い。 本物: `frame-loop.ts:4699` 付近の `holder.replace(next)` が `held` を差し替えて選択を剪定するが `ask()` を呼ばない。 `Agent API` の書き込みはこの 1 枚の扉しか通らない。人の入力は `receiveInput` 側が自前で `ask()` する。 発話だけは `:6425` で `ask()` している（`FT-5`）。 ⇒ 最小の直しは `replace` の末尾で `FT-5` の `deliver` と同じ形の `ask()`。 ⛔ 毎フレーム文書を見比べる案は 表 T-078 の結び（`NFR-010`）に触れる。 ステータスは `実装待ち`。  ✅ **実測（2026-09-08、出荷ビルド、1400 × 900）**: **`applyCommands` で題を変えたあと 1500 ミリ秒待っても `Document Title` は古い題のまま。ポインタを 1 画素動かすと直ちに新しい題になる。** | ⚠️ **これを知らずに測ると、直っている不具合を「直っていない」と読む。** | ✅ **押した（同日、体が Playwright で）。** |
| D-388 | ⚠️ **何も変えなかった書き込みでも、取り消しの段が 1 つ積まれる**（`D-378` を直した体の実測 2026-09-08）—— **人が取り消しを押しても、目に見えることが何も起きない段が挟まる。** | 何も変えなかった書き込みが、取り消しの履歴に何を残すかが決まっていること | ①段を積まない ②積む（いまのまま）と条項に書く | ⛔ **未検討。⛔ 条項がまだ無い。**⭐ **体は正しく止まった** —— **`docs/spec` は「何も変えなかった書き込みが履歴に何を残すか」をどこにも定めていないので、規則を発明せずに申告してきた。**⭐ **近い条項はあるが、どれもこの場面ではない** —— **表 T-028 の `IN-6` は「その場の欄が、始めた値をそのまま書くこと」を禁じ、理由（1 つの編集に 2 段）まで述べているが、それは入口 1 つの話であって書き込みの道の話ではない。表 T-035 の `AG-10` は 表 T-027 の対象外だけを裁き、`FR-031` と `UN-3` は操作の種類で分ける。**⭐ **`WS-4` は命令の種類だけで段を選んでいる**（`isUndoable`）—— **参照も `hasMovedSchedule` も読まない。**⇒ **`D-378` の直しは、この行には届いていない。**⭐ **前に立つ者の推す案は①である** —— **`FR-020` が「何も変えなかった書き込みでは打ち直さない」と定めている理由と同じ理由が、履歴にも当てはまる。**⛔ **ただし裁定を仰ぐ。** | `取下げ` | ⭐ 反証 2026-09-08: `ae2f201` で鎖は 2 本とも閉じた。反証 2026-09-08: `src/use-case/apply-document-change/document-change-plan.ts:591`（`WS-4`）と `:625`（`WS-5`）。 ⭐ 前に立つ者が自分で grep して確かめた。 仕様 `FR-031` / 表 T-027 の結び「値を 1 つも変えなかった書き込みは段を残さないこと（MUST）」。  ✅ **実測（2026-09-08、出荷ビルド、人と同じ `Ctrl+Z` で。直す前も後も同じ）**: **題を `TITLE-B` に変える → もう一度 `TITLE-B` を書く（何も変わらない）→ `Ctrl+Z` 1 回で `TITLE-B` のまま（何も起きない）→ 2 回目で元の題に戻る。** | ⚠️ **`D-378` とは別の場所である** —— **あちらは刻、こちらは履歴。** | ✅ **押した（2026-09-08）。** |
| D-389 | ⛔⛔ **パレットで形を押しただけで「受け付けられない操作が含まれていたので、まとめて取りやめました」と出る**（利用者の実測 2026-09-08、画面写真と操作ログ）—— **人は 1 つ押しただけで、束など作っていない。** | 押した形が選んでいるものに当てられないときは、その場面の言葉で告げること | ①この場面の理由の行を 表 T-233 に立てる ②`CM-20` の拒否をこの道では上げない ③いまのまま `RS-10` を使う | ⛔ **未検討。**⭐ **出ている語は 表 T-233 の `RS-10`** —— **「受け付けられない操作が含まれていたので、まとめて取りやめました／受け付けられなかった操作を取り除いて、もう一度行ってください」。**⛔ **後半が成り立たない** —— **人が組んだ「操作の集まり」など無いので、取り除くものが無い。**⭐ **機構**: **`FR-083` の `SP-2` / `SP-3` が、選んでいるものの形状を変える命令を立てる。`CM-20` が バーの形（表 T-012 の `SH-1` 〜 `SH-4`）と マイルストーン（`SH-5`）の乗り換えを拒み、`AG-3` が束ごと落とす。**⚠️⚠️ **「今日悪化したのか」を最初に確かめること** —— **`commandFromArmingEntry` は 2026-09-07 より前から同じ命令を立てていたので、拒否そのものは新しくない見込みだが、確かめていない。**⇒ **`git checkout` した前の版を `npx vite build` して同じ押下を測れば決まる。**⭐ **`FR-083` は「形状の変更が拒まれたときも、構えは立てること（MUST）」と定めており、拒むこと自体は仕様どおりである** —— **問題は告げる言葉である。** | `実測待ち` | ⭐ 直した(2026-09-08): 殻の写像があらゆる書き込みの拒否を1つの理由に潰していた。`CM-20`×`FR-083`の道だけを`RS-54`へ振った。出荷の道で実測した文が、利用者の報告と1字違わず一致した(直す前「受け付けられない操作が含まれていたので、まとめて取りやめました」⇒直した後「構えている形状は、選んでいるものには当てられません」)。わざと壊した実測: 全部を元の理由へ戻すと2件が赤。逆に全部を`RS-54`にしても0件(=広い側に錨が無い。別途新しい行を立てた)。 ⭐ 反証 2026-09-08: ⭐ 拒むこと自体は仕様どおりである: `FR-083`（`01-04-requirements.md:998`） 「形状の変更が拒まれたときも、構えは立てること（MUST）…拒んだことは `FR-029` の作法で告げる」。 ⛔ **語が合っていない**: `RS-10`（`:3956`）の正は 表 T-067 の `WS-3`（人が組んだ束を落とす場面）であり、 辞書の次の一手は「拒まれた変更を取り除いて、もう一度」である。⇒ この道に「取り除ける操作」は存在しない。 この場面の正は `FR-029`（`:3878`）「運ぶ理由は、押された入口の場面に当たる 表 T-233 の行とすること（MUST）」で、 当たる行が無いときの落ち先を `RS-27` と明記している。 ⇒ 表 T-233 に「構えた形状が、選んでいるものに当てられない」の 1 行（正 ＝ `FR-083`、作法 ＝ `NT-1`）を立て、 辞書に項を足し、`frame-loop.ts:1650` 付近の写像をこの道だけ新行へ振る。 ⛔ 台帳の案③（いまのまま `RS-10`）は `FR-029` の MUST に反する。 ステータスは `仕様待ち`。  ✅ **操作ログ（`FR-102`）の実測**: **`seq 2538-2543`（`on=IC-23` を押して離す）→ `act=changeDocument` なのに `doc=same`、`notices=1`。`seq 3106-3111`（`on=IC-27`）と `seq 3777-3782`（`on=IC-23`）も同じ形。**⭐ **押下のたびに `svgBytes` が 188 増える**（13397→13585 等）—— **構えの印は立っている。** | ⚠️ **利用者は 1 回の巡で 5 回以上この通知を閉じている**（`Enter` で `dismissNotice`）。 | ✅ **押した（利用者本人、2026-09-08）。** |
| D-390 | ⛔ **マイルストーンの実績ダミーが、バーの形で描かれている**（利用者の実測 2026-09-08）—— **利用者の言葉「マイルストーンの形状と種類を合わせろ。i.e., 実績の薄い形状」。** | マイルストーンのダミーは、そのマイルストーンと同じ図形を薄く描いた 1 点であること | ①ダミーの描画を形状で分ける ②いまのまま | ⛔ **未検討。**⭐⭐ **仕様は既にそう定めている** —— **`FR-043`: 「マイルストーンは実績バーを持たないので（表 T-023d の `GR-15`）、ダミーは点として 1 つだけ出すこと（MUST）。実績期間は `S-130` とすること（MUST）」。**⇒ **仕様待ちではなく、実装が条項に追いついていない。**⚠️ **利用者の求めは条項より 1 歩進んでいる** —— **条項は「点として 1 つ」までしか言っておらず、「そのマイルストーンと同じ図形で」とは書いていない。**⇒ **図形を合わせることを条項に足すか、`FR-041`（実績の色を継ぐ）と同じ形で導けるかを先に読むこと。**⛔⛔ **どのタスクの上でこれが起きるかを最初に確かめること** —— **下の実測は、ポインタが `GR-17`（実績の終了点のダミー）に当たったことを示すが、`GR-17` はバーを持つタスクの行であり、マイルストーンなら `GR-18` しか出ないはずである。**⇒ **`GR-17` が出た時点で、その図形はマイルストーンとして扱われていない。** | `実測待ち` | ⭐ 直した(2026-09-08): 図形をそのマイルストーン自身の輪郭にし、色は既存の薄さの仕掛けのまま(新しい色の式は作っていない。FR-013/FR-041のとおり)。掴み判定はわざと変えていない——FR-043のRATIONALEが「S-93が3つのダミーを同じに覆う」と述べ、「図形の輪郭を当たり判定にし直してはならない(MUST NOT)」と定めている。造りを読んで、掴みが図形を読まないことを確かめた。わざと壊した実測: 矩形へ戻すと4件、別の非矩形にすると2件、箱を2倍にすると4件が赤。 ⭐ 反証 2026-09-08: ⭐ 半分は既に守られている: **個数（1 つ）は守られている** —— `schedule-geometry.ts:1195` 付近が `sideways` のとき `GR-18` を 1 個だけ返す（`schedule-layout.ts:633` 付近が milestone → sideways）。 ⛔ **形が矩形である**: `svg-renderer.ts:1977` 付近が `GR-18` も `barSvg({form:'outline', points: cornersAround(...)})` の矩形で描く。 同ファイル `:1961` 付近の注が自認している（「drawn as the SAME figure as GR-9 and GR-17, not as a milestone's own diamond」）。 仕様 `FR-043`（`:2043`）「マイルストーンの例外は 2 つだけである —— 実績バーを持たないので（表 T-023d の `GR-15`）、 ダミーは点として 1 つだけ出すこと（MUST）。実績期間は `S-130` とすること（MUST）」。 ⛔ 「そのマイルストーンと同じ図形で」は**条項なし** ⇒ 利用者の裁定が要る。ステータスは `裁定待ち`。 ⛔ 行の ⛔⛔（「`GR-17` が出た時点でマイルストーンとして扱われていない」）は**偽**である —— マイルストーンでは `GR-17` は構造上出せない（`schedule-geometry.ts:1195` 付近が早期 return）。  ✅ **操作ログの実測（2026-09-08）**: **`seq 4127`〜`4143` でポインタを左へ動かすと、掴みは `GR-4` → `GR-12` → **`GR-17`** → `GR-7` → **`GR-17`** → `GR-12` と移る。**⭐ **同じ巡の `seq 2346` では `GR-18` も出ている** —— **両方が存在する。** | ⚠️ **`GR-18` と `GR-17` のどちらが出るかは、そのタスクがマイルストーンかどうかで決まる。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-391 | ⛔⛔ **すべてのタスクを消したあとに引くと、日付が勝手にずれて、変な位置にタスクが置かれたように見える**（利用者の実測 2026-09-08） | 引いた場所の日付でタスクができること。タスクが 0 件でも同じであること | ①0 件のときの時間軸の起点を決めている場所を突き止めて直す ②仕様に書く | ⛔ **未検討。⛔ 操作ログの残っている窓には入っていない**（古い側 2285 行が落ちている）。⭐ **疑うべき場所は 3 つある**: **①`scrollDate`（0 件の文書で何を指すか）②`xFromDay` の起点 ③全体表示（`FR-055`）が 0 件の文書に対して何を選ぶか。**⚠️ **`OP-10`（選んだ倍率と位置が、そのまま見る場所になる）とも噛み合わせが要る。**⭐ **再現の手順は短い** —— **すべて選んで消す → 空所を引く → 置かれた日付を読む。**⇒ **最初に操作ログを取り直すこと**（`FR-102`。⛔ 4285 行のうち 2285 行が捨てられていたので、短い巡で取ること）。 | `取下げ` | ⭐ 利用者の裁定 2026-09-08、逐語: 「だったら順にタスクグループを削除し、全部削除した時点で、『描くものが1つも無い文書では、倍率を等倍に戻し、表示位置をscrollDate(nullなら実行日)に合わせること(MUST)』としろ。」⇒ `FR-055`は現状のまま。実装も現状のまま。実物確認: 反証2026-09-08で「実装は条項どおり」と実測済。 ⭐ 反証 2026-09-08: ⭐ 誤診であった。引いた日付そのものは正しい —— `input-command-translator.ts:7620` 付近は `dayAtX(context.layout, …)` を使い、 その `context.layout` は `frame-loop.ts:5119` 付近で描いたのと同じ `view.settings` から作られる。 **ずれるのは軸である**: `frame-loop.ts:2992` 付近（`viewSettings` / `OP-10`）が「表示位置を名指していない文書」を毎フレーム再フィットし、 `schedule-layout.ts:2113` 付近が `contentWidth <= 0` のとき `zoomX` を 1 に強制、 `viewSettings` が `scrollDate` を `covered[0] ?? stored.scrollDate ?? runDay`（＝**実行日**）へ落とす。 ⇒ 最後のタスクを消した瞬間に軸が「実行日・等倍」へ跳び、次に 1 件置くとその 1 件にフィットして再び跳ぶ。 仕様 `FR-055`（`01-04-requirements.md:2762`） 「描くものが 1 つも無い文書では、倍率を等倍に戻し、表示位置を `scrollDate` に合わせること（MUST）。 `scrollDate` が null のときは実行日に合わせてよい（MAY）」。 ⇒ **実装は条項どおりである。**利用者の裁定が要る。ステータスは `裁定待ち`。  ⛔ **前に立つ者は再現していない。** | ⚠️ **`D-392` と同じ巡で起きている。両方とも「引いて置く」道である。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-392 | ⚠️ **引かずに離した押下で、何かが作られている疑いがある**（前に立つ者が利用者の操作ログを読んで気づいた 2026-09-08）—— **`S-208` を超えていない押下で文書が変わっている。** | バーの形を構えているときは、引かずに離した押下で何も作られないこと（`FR-001`） | ①構えていた形を突き止め、バーなら直す ②マイルストーンだったなら本行を取り下げる | ⛔ **未検討。⛔ これは疑いであって、まだ不具合ではない。**⭐ **`seq 2287`〜`2299` の移動は (537,107) → (538,109)** —— **動いた距離はおよそ 2px であり、`_assets/tbl-settings.md` の `S-208` を超えていないはずである。**⛔ **それでも離した `seq 2299` で `doc=changed`・`sel=changed` になり、`bars` が 1 → 2、`circle` が 2 → 4、`polygon` が 3 → 5 に増えている。**⚠️⚠️ **マイルストーンを構えていたのなら、これは正しい振る舞いである**（`FR-001` の「マイルストーンは押すだけで置くこと（MUST）」）。⇒ **どちらであったかは、このログだけでは決まらない。**⭐ **決め方**: **同じ場面を出荷ビルドで作り、`IC-23`（矩形）を構えて 2px だけ動かして離す。`Task` の数が増えなければ正しく、増えていれば `FR-001` の MUST NOT が破れている。** | `取下げ` | ⭐ 反証 2026-09-08: `49aa78f` で閉じている。反証 2026-09-08: `input-command-translator.ts:7683` の `if (!isMilestone && !dragged) return nothingToDo('barShapeReleasedWithoutADrag')`。 閾値は `S-208` = 6px（`hasDraggedPastThreshold`）。⇒ 引かずに離してバーの形状が作られることはあり得ない。 ⭐ 前に立つ者が自分で grep して確かめた。 ⚠️ 台帳が読んだ操作ログは `49aa78f` より前の出荷ビルドのものである。  ⛔ **前に立つ者は押していない。ログを読んだだけである。** | ⚠️ **`polygon` が 2 つ増えているのは、マイルストーンの図形としては多い**（`circle` も 2 増）—— **これが「疑い」を捨てられない理由である。** | ⛔ **押していない。** |
| D-393 | ⛔⛔ **実績のダミーが掴めない**（利用者の実測 2026-09-08） | ダミーを掴んで実績を入れられること（`FR-043`） | ①当たり判定を直す ②描く位置と当たり判定がずれているなら合わせる | ⛔ **未検討。**⚠️⚠️ **⚠️ 反証済み（2026-09-08）: 最初に確かめること —— 同日の変更が原因である可能性がある。**⭐ **2026-09-08 に 2 つ動かした**: **①`GR-3`（予定の開始点）の当たり判定を、予定の開始日の位置で右へ食い込まないよう切った。②表 T-023d で `GR-17` を `GR-9` の上へ動かした。**⚠️ 反証済み（2026-09-08）: ⇒ **前の版を `git` で取り出して `npx vite build` し、同じ場所を押せば決まる。**⛔ **これを先に済ませないと、直す先を間違える。**⚠️ **同日の実測とは食い違っている** —— **体の before/after では、1.5px/日 で予定開始日の +2px を押すと `GR-9` / `GR-17` が答えていた。**⇒ **利用者が押した場面（倍率・タスクの状態・どのダミー）を先に固定すること。** | `実測待ち` | ⭐ 直した(2026-09-08): 退行ではなかった。予定の開始日の位置で切る門を立てた(GR-1/GR-2/GR-3/GR-4が、その右に立つダミーの箱の上では成立しない)。表の順は書き換えていない——結びが「本表の順は離れているものどうしの優先を決める」と定めている。1px刻みの掃引(低倍率)の実測: 直す前は画素0でGR-3、画素1から8までGR-4が答え続け、描かれているダミーはどの画素からも掴めなかった⇒直した後は画素0でGR-3、画素1でGR-9、画素2でGR-17と、自分の日の列で答えるようになった。6px/日のdx1から5では予定の終了点が従来どおり掴める(=取りすぎていない)。わざと壊した実測: 門を外すと6件が赤。 ⭐ 反証 2026-09-08: ⭐ 誤診であった。⛔ **退行ではない** —— `7f42de9` は当たり判定を広げた側にしか動いていない （`item-hit-area.ts:502` 付近で `GR-3` を左だけに切り、`:391` 付近で `GR-17` を `GR-9` の上へ）。 当たり判定そのものも正しい（`:544` 付近、日の列の左端から右へ `S-93` = 30×20px）。 残っている犯人は表の順で上に居る 2 行である: ① `item-hit-area.ts:357` 付近 ＋ `:502` 付近 —— **`GR-4` は切られておらず `S-90` = 6px を左右へ均等に伸ばす**。 予定が短いと（低倍率で 1〜8 日）予定バーの右端がダミーの立つ日そのものに来て、 描かれている印（`svg-renderer.ts:1977` 付近、幅 `min(pxPerDay, 12)`）を丸ごと飲む。 ② `item-hit-area.ts:334` 付近 —— `GR-1` が選択中の矩形でだけ現れ（`schedule-geometry.ts:1424` 付近）、 予定の左上角から `S-92`/2 = 7.5px を claim する。 ⇒ **場面は「作った直後で選択されたまま、低倍率、短い予定」である。** ⛔ **条項が二枚舌になっている**: 表 T-023d の結び（`:2395`）は 「予定の開始日の位置が、予定側と実績側の境目であること（MUST）…その位置より右を押したときは 実績のダミー（`GR-17` / `GR-9` / `GR-18`）を掴むこと（MUST）」と言うのに、 同表 `:2397` 付近の注は「`GR-4` は 2026-09-08 の掴み分けの裁定では触れていない」と言う。 ⇒ 利用者の裁定が要る。ステータスは `裁定待ち`。  ⛔ **前に立つ者は再現していない。** | ⚠️ **`D-396`（ダミーの印が 2 本）と同じ図形の話である。一緒に測ること。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-394 | ⛔ **ステータスマークとタスク名が重なる**（利用者の実測 2026-09-08）—— **利用者の求め「必ず 担当 → 実績（実績ダミー）→ ステータスマーク → タスク名 の順に並ぶこと」。** | バーの周りの札と印が、左から その順で並び、重ならないこと | ①横の並び順を定める行を立てる ②いまのまま | ⛔ **未検討。⛔ 条項が無い。**⭐ **表 T-013（`NL-1` / `NL-3`）は「形状の中に書くか外に書くか」を決めるが、外に出したものどうしの横の並び順を決める行が 1 つも無い。**⇒ **重なるのは当然であり、実装の誤りというより穴である。**⭐ **関わる行**: **表 T-023d の `GR-11`（担当ラベル）・`GR-7`（進捗マーカー）・`GR-10`（名称ラベル）、`FR-043` の実績ダミー、表 T-013。**⚠️ **`GR-7` の場所欄は「実績バーの右端の外側。未着手のときは終了点の掴みシロの外側」と述べており、`GR-8`（再開アイコン）は「マーカーのさらに外側」である** —— **外へ外へと積む作法は既にあるので、その列に 担当 と 名前 を position づければよい。** | `裁定待ち` | ⭐ 反証 2026-09-08: ⭐ 「表 T-013 に並び順の行が無い」は**正しい**（`NL-1` / `NL-3` の 2 行だけ）。 ⚠️ ただし空白ではない —— 表 T-038 が向きを持つ（`01-04-requirements.md:1251` 付近）: `OC-1` 名称ラベル＝左右、`OC-2` 担当＋完了率＝左、`OC-3` 進捗マーカー＝実績バーの右端の外（占有幅に**算入禁止**）、 `OC-4` 再開アイコン＝さらに外（**算入禁止**）。 非重なりの意思も逐語で在る（`:1268` 付近「担当者名・完了率・タスク名は重要な情報であり、重なると極端に読みにくくなる」）。 ⛔ 足す先は 表 T-013 ではない —— あの表は「形状の中／外」の判定表であって並び表ではない。表 T-038 か 表 T-023d の結びに 1 行。 ⚠️⚠️ 名称を `OC-3` / `OC-4` の外へ置くと、`OC-1` は占有幅に算入し `OC-3` / `OC-4` は算入してはならない（MUST NOT）ので、 **マーカーの表示を切り替えるとタスクの位置が動く** —— `:1266` 付近が明示的に避けている事故である。 ⇒ 利用者の裁定が要る。ステータスは `裁定待ち`。  ✅ **画面で重なりを確認（利用者本人、2026-09-08）。** | ⚠️ **`D-400`（再開のダミー）も同じ列に並ぶので、順序は一度に決めること。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-395 | ⛔ **表 T-023d の結びの MUST が、予定の 2 端と実績の 2 端に効いていない**（反証 2026-09-08 に題を差し替え。旧題と経緯は詳細状況欄）。 | 予定も実績も、1 日を下回らないこと | ①値の下限を条項に置く ②描く幅だけ下限を持つ（いまのまま） | ⛔ **未検討。⛔ 値の下限を定める行が無い。**⭐ **`minShapeWidth` は描く幅の下限であって、`start` / `finish` の下限ではない。**⚠️⚠️ **既存の条項と噛み合わせが要る** —— **`FR-012` は「予定の期間が 0 の `Task` では割り算を行ってはならない（MUST NOT）」と、期間 0 が存在することを前提にした行を持つ。**⇒ **1 日を強いるなら、その行は語る相手を失う。どちらを動かすかを決めること。**⭐ **`FR-001` は 2026-09-08 に「クリックではバーの形状のタスクを作らない（MUST NOT）」となり、作る道からは 0 日が消えている** —— **残るのは日付を編集する道である。**  ⭐⭐ **裁定が下り、同じ日に取り下げられた（2026-09-08）。**⛔ **逐語「実績も予定も0日は禁止。 必ず1日は確保する」→ 「失礼。 前言撤回。 タスクに開始日と終了日が同じになる場合があるね」。**⇒ **日数の下限は置かない。代わりに「2 つの端点が同じ日に立つときは終了側を掴む（MUST）」を表 T-023d の結びに書いた。**⭐ **予定の 2 端・実績の 2 端・ダミーの 2 端に同じように当てはまる。**⭐ **この形ならマイルストーン（start = finish の点）の例外を書かずに済む。**⛔ **実装はまだ無い。** | `実測待ち` | ⭐ 直した(2026-09-08): 予定の2端と実績の2端にも「終了側を掴む」を効かせた。潰れた実績(開始===終了)は、直す前は掴める画素が開始側だけで、二度と伸ばせなかった。予定の側はS-49(最小幅6px)が幾何より前に効くので同日かどうかが届かず、まだ閉じていない(体が正直に報告)。 ⭐ 反証 2026-09-08: ⛔ 題を書き換える: **「表 T-023d の結びの MUST が、予定の 2 端と実績の 2 端に効いていない」**。 反証 2026-09-08: 1 日下限の条項は `docs/spec` に **0 件**（「1 日を下回」「最低 1 日」「必ず 1 日」「1 日は確保」すべて 0）。 ⇒ 利用者の前言撤回（2026-09-08）は仕様に正しく反映されている。旧題は取り下げる。 代わりに置かれた MUST は `01-04-requirements.md:2397` 「2 つの端点が同じ日に立つときは、終了側を掴むこと（MUST）…予定の 2 端（`GR-3` と `GR-4`）にも、 実績の 2 端（`GR-5` と `GR-6`）にも、ダミーの 2 端（`GR-9` と `GR-17`）にも、同じように当てはまる（MUST）」。 ⛔ 3 組のうち実装されているのはダミーの 1 組だけである（`item-hit-area.ts:391` で `GR-17` を `GR-9` の上へ）。 `:355` は `GR-3` が `GR-4` の上、`:360` は `GR-5` が `GR-6` の上のまま。 ステータスは `実装待ち`。  ⚠️ 旧題（2026-09-08 に差し替え）: ⛔ **タスクの予定や実績が 1 日未満になる**（利用者の実測 2026-09-08）—— **利用者の求め「必ず 1 日は確保しろ」。**  ⛔ **前に立つ者は再現していない。どの道で 1 日未満になるかを先に押さえること。** | ⚠️ **利用者の裁定 2026-09-08「ダミーの実績は 1 日だ」と同じ向きである。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-396 | ⛔ **タスクを置くと、実績のダミーの縦棒が 2 本出る**（利用者の実測 2026-09-08）—— **利用者の求め「1 つだけにしろ」。** | ダミーの印が 1 つだけ出ること | ①`GR-17` の印を描くのをやめ、掴みだけ残す ②2 つのダミーを 1 つに畳む ③いまのまま | ⛔ **未検討。⚠️⚠️ **提起の時点では利用者の求めと条項が衝突していたが、同日のうちに裁定が下りて解消した（下記）。****⭐ **`FR-043` は開始点のダミー（`GR-9`）と終了点のダミー（`GR-17`）の 2 つを定めており、表 T-023d にもそれぞれ行がある。**⛔ **どちらを掴んでも実績は立つ** —— **`GR-9` は開始日と期間を、`GR-17` は期間を置く。**⇒ **印を 1 つにすると、`GR-17` を掴む道が見えなくなる。**⚠️ **2026-09-08 の裁定「実績の終了日を優先しろ」は、重なったときにどちらが勝つかを定めたものであり、印の数については何も言っていない。**⭐ **案①なら条項の衝突が小さい** —— **`GR-17` は 表 T-023d に残り、`S-129` が 1 日なので `GR-9` の印のすぐ隣を掴めば当たる。**  ⭐⭐ **裁定が下りた（2026-09-08）** —— **逐語「タスクのダミーは1日とする。 だから = は1日」「1つだけにしろ」。**⇒ **ダミーの印は 1 つだけ描く（MUST）。開始側と終了側に別々の印を描かない（MUST NOT）。**⭐ **掴む先が 2 つであることは変わらない** —— **`GR-9` と `GR-17` はどちらも表 T-023d に残り、同表の結び（境目と、重なったら終了が勝つ）がその 1 つの印のどこを押したら何を掴むかを決める。**⛔ **件名にあった「条項と衝突する」は解消した。**⛔ **実装はまだ無い。** | `実測待ち` | ⭐ 直した(2026-09-08): 描く側で1つだけ描くようにした。掴む先が2つであることは変えていない。わざと壊した実測: 2本に戻すと16件、GR-17の日に描くと17件が赤(=数・場所の両方が押さえられている)。 ⭐ 反証 2026-09-08: ⭐ 条項は既に在る。**仕様の穴ではない**。 `FR-043`（`01-04-requirements.md:2043`）「ダミーの印は 1 つだけ描くこと（MUST）。 開始の側と終了の側に別々の印を描いてはならない（MUST NOT）」（利用者の裁定 2026-09-08）。 本物: `schedule-geometry.ts:1199` 付近が `GR-9` と `GR-17` の 2 個を返し、 `svg-renderer.ts:1978` 付近が `task.dummies` を 1 対 1 で写像して 2 本の枠を描く。 ⇒ 直しは `svg-renderer` 側で「`GR-9` の日から 1 日ぶんの印を 1 つだけ」描くこと。 ⛔ 幾何の側で `dummies` を 1 件に減らすな —— `markerAnchorX`（`schedule-geometry.ts:812` 付近）が `GR-17` を見失って進捗マーカーの位置が消え、`GR-17` の掴みも死ぬ。 ステータスは `実装待ち`。  ✅ **画面で 2 本を確認（利用者本人、2026-09-08）。** | ⚠️ **`D-393`（掴めない）と同じ図形である。一緒に測ること。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-397 | ⛔ **担当者をダブルクリックしても編集できない**（利用者の実測 2026-09-08）—— **利用者の求め「プロパティーパネルを開いて担当者編集のテキストボックスを編集するモードにせよ。編集後 `Enter` でプロパティーパネルを閉じろ」。** | 担当ラベルのダブルクリックで、パネルが開き、担当の欄が編集中になり、`Enter` で閉じること | ①`GR-11` の操作欄を書き換え、パネルを開く道にする ②その場で編集する道を作る | ⛔ **未検討。**⭐ **いまの条項**: **表 T-023d の `GR-11` は「ダブルクリックで担当者名を変える（`MK-13`）」とだけ述べ、パネルを開くとは書いていない。**⭐⭐ **求められている形は、この製品に先例がある** —— **表 T-051 の `HF-14`（行を足すと、その行のパネルを出して名前の欄で名づけさせる）と、`FR-091`（作った直後に名前を打てる。`Enter` で確定してパネルを閉じ、選択を解く）。**⇒ **同じ作法を担当へ広げるだけで済む見込みで、新しい考え方は要らない。**⛔ **`FR-091` の「閉じて選択を解く」をそのまま写すかは要検討** —— **あちらは作った直後の場面に限った条項である。** | `裁定待ち` | ⭐ 反証 2026-09-08: ⭐ 誤診であった。⛔ **条項は在り、しかもパネルではない**。 表 T-225 の `AS-1`（`01-04-requirements.md:1569` 付近） 「担当ラベルをダブルクリックした｜その場で担当者名を編集させること（MUST）。 入口の割当は 表 T-023 の `MK-13`、掴み領域は 表 T-023d の `GR-11` が既に持つ」。 ⇒ 台帳の「`GR-11` が…とだけ述べ、パネルとは書いていない」は 表 T-023d しか読んでいない。 入口は既に通っている: `item-hit-area.ts:429` 付近（`GR-11`, doubleClickOnly）→ `input-command-translator.ts:7015` 付近（`editInPlace` / assignee）。 ⛔ 落ちているのは殻である: `frame-loop.ts:9149` 付近に明示の STOP 「NO IN-PLACE EDITOR EXISTS FOR MK-13's 担当ラベル」。 ⇒ 条項（その場編集）と利用者の求め（パネル）が食い違っている。利用者の裁定が要る。 ステータスは `裁定待ち`。  ✅ **押した（利用者本人、2026-09-08）。** | ⚠️ **`D-399`（実績のダブルクリック）と同じ「ダブルクリックでパネル」の話である。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-398 | ⛔ **タスクを選んだまま `Esc` を押しても、選択が解けない**（利用者の実測 2026-09-08）—— **利用者の求め「プロパティーパネルが消えているとき、`Esc` で選択を解除しろ」。** | パネルが出ていないとき、`Esc` 1 回で選択が解けること | ①`Esc` の階層に「選択」を足す ②いまのまま | ⛔ **未検討。⛔ 条項が無い。**⭐⭐ **表 T-028 の `IN-4` が階層を定めている** —— **出ている通知 → 確定していないその場の編集 → 開いている面 → 進行中のドラッグ・引きかけの矢印 → 構え → `Dual Cursor` モード → 出ている説明。**⛔ **選択がこの並びに入っていない。**⚠️⚠️ **どこに挿すかが要点である** —— **「構え」より前に置くと、構えたまま何かを選んでいるときに `Esc` が構えを解けなくなる。後ろに置くのが素直だが、利用者の言う「パネルが消えているとき」という条件をどう表すかは別に要る。**⭐ **表 T-027 の `UN-9` は「選択」を取り消しの対象外としている** —— **履歴とは関わらない。** | `実測待ち` | ⭐ 直した(2026-09-08): 表T-028のIN-4に「選択」の段を、構えの後・Dual Cursorの前に入れた。段ごとの実測: 直す前は1度目でパネル、2度目でブラウザへ落ち、選択は永久に解けなかった⇒直した後は1度目でパネル(選択は同じ物体のまま=FR-072のMUST NOTを順序で守る)、2度目で選択が空になり、3度目でブラウザに落ちる。わざと壊した実測: 段を消すと8件、構えより上へ置くと1件が赤。 ⭐ 反証 2026-09-08: ⭐ **条項なし**であることが確かめられた。 `screen-state.ts:347` 付近の `escapeTarget` の段は notice → textEntry → confirmation → surface → gesture → propertiesPanel → armed → dualCursorMode → tooltip → null で、 **「選択」の段が 1 つも無い**。`frame-loop.ts:3166` 付近が `isPropertiesPanelOpen` を渡すので、 タスクを選ぶとパネルが立ち（`FR-006`）、1 度目の Esc は propertiesPanel の段が食い、2 度目は null を返してブラウザへ落ちる。 表 T-028 の `IN-4`（`01-04-requirements.md:4147`）の並びに「選択」は無い。 ⭐ `FR-072` の MUST NOT（パネルをやめても選択を解くな）とは衝突しない —— あれは Esc の段の話ではない。 ⇒ 足す場所は 表 T-028 の `IN-4` の並び 1 か所。**「構え」の後・「Dual Cursor モード」の前**。 ⭐ 「パネルが消えているとき」の条件は要らない —— propertiesPanel の段が既に上に在る。 ステータスは `仕様待ち`。  ✅ **押した（利用者本人、2026-09-08）。** | ⚠️ **`FR-072` の 2026-08-30 の MUST NOT（パネルをやめても選択を解くな）と読み合わせること** —— **あちらはパネルを閉じる操作の話であり、こちらは `Esc` の階層の話である。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-399 | ⛔ **実績をダブルクリックしてもプロパティーパネルが開かない**（利用者の実測 2026-09-08） | 実績のダブルクリックでパネルが開くこと | ①ダブルクリックの対象に実績を足す ②`MK-13` の対象を読み直す | ⛔ **未検討。**⭐ **先に読むこと**: **表 T-023 の `MK-13`（ダブルクリックが何を開くか）と、表 T-023d の 実績まわりの行（`GR-5` / `GR-6` / `GR-15`）。**⚠️ **表 T-023d は「実績バーの本体を掴み領域にしてはならない（MUST NOT）」と定めている** —— **矩形と矢羽根では実績を予定の内側に重ねるためである。**⇒ **本体が掴めないことと、ダブルクリックで開けないことが同じ理由から来ていないかを確かめること。** | `実測待ち` | ⭐ 直した(2026-09-08): `MK-13`の宛先を掴みの優先順より先に読むようにした(実績の6領域を追加)。`GR-3`/`GR-4`(予定の端)と`GR-7`/`GR-8`は`MK-13`が挙げていないので除いた。わざと壊した実測: 錨を書く前は0件、書いた後は1件が赤。 ⭐ 反証 2026-09-08: ⭐ 誤診であった。**実績の「中間」を打てばパネルは開く**（`GR-12` が claim する）。 端の上では `GR-5` / `GR-6` が `GR-12` より優先順で上なので、 `input-command-translator.ts:7159` 付近の case が同じ日を書く空振りになり、`GR-12` の名称入口へ届かない。 表 T-023 の `MK-13`（`:2355`）が挙げる対象は コメントボックス／タスク（名称ラベルと本体）／担当ラベル／行見出し の 4 つで、 **実績は 1 つも無い** ⇒ 条項なし。 ⚠️ 足すだけでは足りない —— 端の上では `GR-5` / `GR-6` が先に claim するので、 `MK-13` の宛先を優先順より先に読む旨を 表 T-023d の結びに 1 文添える必要がある。 ステータスは `裁定待ち`。  ✅ **押した（利用者本人、2026-09-08）。** | ⚠️ **`D-397` と同じ「ダブルクリックでパネル」の話である。一緒に決めること。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-400 | ⛔ **再開日（レジューム日）を入力できない**（利用者の実測 2026-09-08）—— **利用者の求め「ステータスマークを中断にしたらレジューム日のダミーを表示しろ」。** | 中断にしたとき、再開日を置くためのダミーが出て、掴んで日を置けること | ①中断のときに再開日のダミーを出す行を立てる ②パネルから入れる道だけにする | ⛔ **未検討。⛔ 条項が無い。**⭐ **いまある入口は 表 T-023d の `GR-8`（再開アイコン。マーカーのさらに外側。`resume` を変える）だけである。**⭐⭐ **求められている形は、実績のダミー（`FR-043` の `GR-9` / `GR-17`）とそっくりである** —— **まだ値が無いところに薄い掴みシロを出し、掴んで離した日を書く。**⇒ **同じ作法を写せる見込みで、新しい考え方は要らない。**⚠️ **`FR-044` の `resumeValid` との噛み合わせを読むこと** —— **再開日が「有効か」を持つ欄が既にある。** | `実測待ち` | ⭐ 直した(2026-09-08): 「掴めない」ではなく「狙えない」であった。当たり判定が矢印の素のパスの箱(14×12px)だったのを`S-93`(30×20px)にした。実測: 届く範囲がx 612-625(14px)⇒609-633(25px)/y 107-118(12px)⇒103-122(20px)。進捗マーカーとの境も測った——マーカーは自分の円の端(x=608)まで、再開アイコンは609から。わざと壊した実測: 元の箱へ戻すと3件が赤。 ⭐ 反証 2026-09-08: ⛔ 誤診であった。造りは既に在る —— `schedule-geometry.ts:1399`（`suspended` なら `resume` 日が null でも `resumeOf` を呼ぶ）、 `:875` 付近（日が無いときは印の右隣に置き、`resumeValid===false` なら `S-25` で縮める）、 `item-hit-area.ts:372`（`task.resume` は `ResumeGeometry` なので当たり判定は成立する）、 `input-command-translator.ts:7261` 付近（離した日を `resume` に書き `PA-3` へ）。 仕様も在る: `FR-044`（`01-04-requirements.md:2065`） 「中断のあいだは再開アイコンを描くこと（MUST）。再開日が未定のときは 表 T-201 の `S-25` に従って別の見た目にすること（MUST）」。 ⛔ 新しいダミーの行を立てると `FR-029` の入口二重化になる。 **本当の原因（測れる算術）**: `GR-8` の当たり判定は矢印の素のパスの箱で `slop` を渡していない （`item-hit-area.ts:377` 付近）。既定値（`S-22` markerSize=16 / `S-25` 0.7 / `S-26` 0.62 / `S-27` 0.22）で およそ **9.4 × 4.9px**。同じ表の実績ダミー `GR-9` / `GR-17` / `GR-18` には `S-93` が **30 × 20px** を与えている。 ⇒ 掴めないのではなく、狙えない。 ステータスは `仕様待ち`（当たり判定の寸法を 表 T-023d の `GR-8` 行に与える）。  ✅ **押した（利用者本人、2026-09-08）。** | ⚠️ **ダミーが増えるので `D-394`（並び順）と `D-396`（印の数）に効く。** | ✅ **押した（利用者本人、2026-09-08）。** |
| D-401 | ⛔ **単体試験の治具が、出荷より 2 倍広い掴み代で `GR-1` / `GR-2` の当たり判定を測っている**（反証の体の実測 2026-09-08）—— `tests/unit/t-023d-the-plan-start-is-the-boundary.test.ts:181` 付近は `fadeHandle: NOT_STORED_SIZES['S-92'][0]`（＝15）を渡すが、出荷の殻は `frame-loop.ts:978` 付近で `NOT_STORED_SIZES['S-92'][0] / 2`（＝7.5）を渡す。契約は `item-hit-area.ts:158` 付近「S-92: the fade handle's square, as its half-width」。 | 単体試験の治具が、出荷の殻と同じ掴み代（半径 `S-92`/2）で当たり判定を測ること | ①治具の `fadeHandle` を `S-92`/2 に直す ②出荷側を治具に合わせて `S-92` そのものへ広げる（要裁定） | ⛔ 未検討。⭐ `D-393` の②（`GR-4` が `S-90` を左右均等に伸ばす件）と同じ座標を測る試験である。一緒に扱うこと。 | `実測待ち` | ⭐ 直した(2026-09-08): 半幅に直した。直した時点では期待値が1つも動かなかった(その治具の`fadeHandles`が空で、その行が発火しなかったため)。同じ波で、その帯を押す件を足して初めて意味を持つようになった。わざと戻すと1件が赤。 ⭐ 反証 2026-09-08: ⛔ **治具の掴み代が出荷の 2 倍である。** `tests/unit/t-023d-the-plan-start-is-the-boundary.test.ts:181` 付近は `fadeHandle: NOT_STORED_SIZES['S-92'][0]`（＝15）を渡すが、 出荷の殻は `frame-loop.ts:978` 付近で `NOT_STORED_SIZES['S-92'][0] / 2`（＝7.5）を渡す。 契約は `item-hit-area.ts:158` 付近「S-92: the fade handle's square, as its half-width」。 ⇒ この試験は `GR-1` / `GR-2` を実物の 2 倍の大きさで測っている。**緑でも出荷の当たり判定を保証していない。** ステータス `実装待ち`。⭐ `D-393` の②と同じ場所を測る試験である。 | ⭐ `D-393` の②と同じ場所を測っており、そちらの裁定と合わせて直すこと。 | ⛔ 未実施 |
| D-402 | ⛔ **`frame-loop.ts:1650` 付近の写像が、書き込みの拒否をすべて 表 T-233 の `RS-10` に潰している**（反証の体の実測 2026-09-08）—— `edit-task.ts` などの `reject(...)` が返す `CM-*` の別は捨てられるので、`D-389` と同じ「束を落とした」文が、パレット以外の拒否でも出る。 | 拒否の理由（`CM-*`）ごとに、表 T-233 の対応する場面の行が選ばれること | ①`frame-loop.ts:1650` 付近の写像を `reject` の種類で分岐させる ②`D-389` の新設行と合わせて配線し直す | ⛔ 未検討。⭐ 表 T-233 に場面ごとの行を足しても、この写像を分岐させない限り語は変わらない。⭐ `D-389` と同じ波で直すこと。 | `実測待ち` | ⭐ 直した(2026-09-08): 殻の写像があらゆる書き込みの拒否を1つの理由に潰していた。`CM-20`×`FR-083`の道だけを`RS-54`へ振った。出荷の道で実測した文が、利用者の報告と1字違わず一致した(直す前「受け付けられない操作が含まれていたので、まとめて取りやめました」⇒直した後「構えている形状は、選んでいるものには当てられません」)。わざと壊した実測: 全部を元の理由へ戻すと2件が赤。逆に全部を`RS-54`にしても0件(=広い側に錨が無い。別途新しい行を立てた)。 ⭐ 反証 2026-09-08: ⛔ `frame-loop.ts:1650` 付近の写像が、**書き込みの拒否をすべて `RS-10` に潰している**。 `edit-task.ts` などの `reject(...)` が返す `CM-*` の別は捨てられるので、 `D-389` と同じ「束を落とした」文が、パレット以外の拒否でも出る。 ⇒ 表 T-233 に場面ごとの行を足しても、この 1 行を分岐させない限り語は変わらない。 ステータス `仕様待ち`。⭐ `D-389` と同じ波で直す。 | ⭐ `D-389` の裁定と対で扱うこと。 | ⛔ 未実施 |
| D-403 | ⛔ **命令の総数が、コード・台帳・仕様の 3 か所で異なる数を名乗っている**（反証の体の実測 2026-09-08）—— `src/use-case/edit-document/edit-document.ts` の冒頭コメントは「73 命令（見せ方の群 16）」、台帳の `D-380` は「実数は 72（同 15）」と訂正済みだが、`ROUTE_TABLE` が強制する実測値は 71（設定の群 14）である。 | コード・台帳・仕様（表 T-108）が、命令の総数について同じ 1 つの数を持つこと | ①`edit-document.ts` 冒頭のコメントを実測値に直す ②`D-380` の訂正を実測値へ更に訂正する ③`docs/spec` の 表 T-108 と突き合わせて正の値を確定する | ⛔ 未検討。⭐ `D-380` に吸収してよい。表 T-108 との突き合わせを先に行うこと。 | `実測済` | ⭐ 反証 2026-09-08: ⛔ **同じ数がコード・台帳・仕様で三様である。** `src/use-case/edit-document/edit-document.ts` の冒頭が「73 命令（見せ方の群 16）」と書き、 台帳の `D-380` は「実数は 72（同 15）」と訂正し、`ROUTE_TABLE` が強制する実数は **71（設定の群 14）**である。 ⇒ `D-380` の訂正も 1 ずれている。`_source` の 表 T-108 と突き合わせること。 ステータス `実装待ち`。⭐ `D-380` に吸収してよい。 | ⭐ `D-380` の訂正も 1 ずれている。次に数えるときは 表 T-108 の行数と機械集計を両方測ること。 | ✅ **2026-09-08** —— `tsc`が押さえている(鍵を1つ削るとTS2741、足すとTS2322を印字)。数を直すのではなく、集合を名乗らせた: `DocumentSettingsCommand['kind']`という集合を`satisfies`と`ROUTE_TABLE`のRecordが両側から締める。3通り(用語表のCM-行/ROUTE_TABLEの足し算/鍵を1つ削ったときtscが印字するunion)で一致し、どれも71命令・見せ方の群14であった。台帳の訂正「72(15)」は1ずれ、コードの「73(16)」は2ずれていた。 |
| D-404 | ⛔ **入口の無い命令のうち 6 種は、どの `PD` にも どの `D` にも記録が無い**（反証の体の実測 2026-09-08）—— `deleteUnreferencedResources` / `reorderTaskGroupSiblings` / `setCommentBoxAnchor` / `setFontScale` / `setStackDirection` / `setThemeMonochrome`。 | 入口の要否が未裁定のまま残る命令が無いこと（`PD` か `D` のどちらかで追跡されていること） | ①6 種それぞれについて入口の要否を仕様で確かめ、`PD` を立てて利用者の裁定を仰ぐ ②既に裁定済の同種の問い（担当者の改名など）と同じ形で束ねる | ⛔ 未検討。⭐ 「入口が要るのか、要らないのか」自体が未裁定である。 | `裁定待ち` | ⭐ 反証 2026-09-08: ⛔ 入口の無い命令のうち **6 種**は、どの PD にも どの D にも記録が無い: `deleteUnreferencedResources` / `reorderTaskGroupSiblings` / `setCommentBoxAnchor` / `setFontScale` / `setStackDirection` / `setThemeMonochrome`。 ⇒ 「入口が要るのか、要らないのか」自体が未裁定である。ステータス `裁定待ち`。 | ⭐ `D-350` の測り直し（71 種のうち 13 種）から漏れていた集合である。 | ⛔ 未実施 |
| D-405 | ⛔ スクロールバーのつまみの割合にも下限にも試験の錨が無い(体の実測2026-09-08) | 溢れる文書でつまみ÷溝が見えている÷全体にほぼ等しいこと、行の多い文書でつまみがS-205以上であること、ピンで帯が伸びたとき縦の割合が大きくならないことの3本が、いずれも試験で押さえられていること | 上記3本を押さえる試験を書く | ⭐ 仕様は`S-205`/`GR-21`として既に決定済み。実装も在るが、押さえる自動試験が1つも無い | `試験待ち` | 実測2026-09-08: 長さを半分にしても赤0件、下限を外しても0件、溝いっぱいに戻しても0件。長さを0にしたときだけ2件が赤くなる⇒押さえられているのは「0でないこと」だけである。 | D-298の測り直しから見つかった穴 | ⛔ 未実施 |
| D-406 | ⛔ 書き込みの拒否のうち、広い側(RS-54にまとめる分岐)に試験の錨が無い(体の実測2026-09-08) | WS-3の拒否条件を広げて元の理由が殻から到達不能になったとき、試験が落ちること | 広い側を押さえる試験を追加する | ⭐ D-389/D-402で行った`CM-20`×`FR-083`から`RS-54`への切り分けに対応する試験の錨が無い状態 | `試験待ち` | 実測2026-09-08: 条件を広げてWS-3の拒否を全部新しい理由にしても赤0件⇒元の理由が殻から到達不能になっても、試験は1件も気づかない。狭い側(2件)は守られている。 | D-389/D-402の直しから見つかった穴 | ⛔ 未実施 |
| D-407 | ⛔ 予定を表示していない未着手のマイルストーンでは、ダミーに写す図形が無く、矩形に落ちる(体の実測2026-09-08) | 未着手のマイルストーンでも、そのダミーがマイルストーン自身の輪郭図形を持つこと | milestoneGlyphを、予定を表示していない経路にも運ぶ | ⛔ 覆う条項が無い。FR-043のMUST NOT(図形の輪郭を当たり判定にし直してはならない)に触れる | `仕様待ち` | 直せるのは幾何の側だけである(milestoneGlyphを運ぶ必要がある)。覆う条項が無い。 | D-390の直しの傍らで見つかった | ⛔ 未実施 |
| D-408 | ⚠️ 進捗マーカーの円が、実績のダミーの箱の先頭4pxに、どの倍率でも重なる(体の実測2026-09-08) | 進捗マーカーと実績のダミーが重ならず、両方とも掴めること | 進捗マーカーの掴み優先順を、掴み分けの結びへ加える裁定を仰ぐ | ⛔ 覆う条項が無い。マーカーは掴み分けの結びが名指す4行に入っていない | `裁定待ち` | 実測: マーカーはGR-17の位置+間隔+半径に立つので、円が+4..+20を覆う。ダミーは+0..+3でまだ掴める。マーカーは掴み分けの結びが名指す4行に入っておらず、覆う条項が無い。 | D-399の直しの傍らで見つかった | ⛔ 未実施 |
| D-409 | ⚠️ 中断中のタスクで再開日が予定の中に立つと、再開アイコンの当たり判定の箱が予定バーの中間を最大17px奪う(体の実測2026-09-08) | 再開アイコンの当たり判定が広がっても、予定バーを掴む余地が失われないこと | 再開アイコンの当たり判定に上限を設けるか、優先順を見直す裁定を仰ぐ | ⛔ 条項はS-93と表の順を定めるだけで、上限を持たない | `裁定待ち` | 当たり判定をS-93にした結果である。条項はS-93と表の順を定めるだけで、上限を持たない。 | D-400の直しの副作用として見つかった | ⛔ 未実施 |
| D-410 | ⛔ 表T-078のFT-4が数える3つの待ちのうち、通知の期限(NT-2)を計る仕掛けがsrc/に1つも無い(体の実測2026-09-08) | NT-2の通知期限を計る仕掛けが実装されていること | holder.replace側に、NT-2の期限を計るタイマーを追加する | ⭐ 仕様は表 T-078 の `FT-4` として既に決定済み | `実装待ち` | 実測: 対象ファイルのsetTimeoutは2つだけで、どちらも別の待ちである。 | D-387の直しから見つかった穴 | ⛔ 未実施 |
| D-411 | ⛔ 書き込みの拒否は53種あるが、専用の理由の行を持つのは1種だけであり、残る52種はすべて「束を落とした」という1つの語に落ちる(体の実測2026-09-08) | 少なくとも、行が要ると見込まれる拒否理由には専用の行があること | 依存の循環/依存の両端が同じ/暦が稼働曜日を1つも持たない/留めの上限/同じidが既に在る/終了が開始より前、の6種について専用の行を仕様に立てる | ⛔ 未確定(仕様待ち) | `仕様待ち` | 行が欲しそうなもの(体が名指した): 依存の循環/依存の両端が同じ/暦が稼働曜日を1つも持たない/留めの上限/同じidが既に在る/終了が開始より前。 | D-389/D-402の直しから見つかった | ⛔ 未実施 |
