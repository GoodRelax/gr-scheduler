# 不具合・改善要望の管理台帳

**規則は [`docs/development-rules/05-working-method.md`](../development-rules/05-working-method.md) の 7.（5 段のパイプライン）。**

⛔ **利用者が指摘した不具合は、直るまでこの表から消さない。** ⭐ **消してよいのは `テスト完了` になり、かつ利用者が実物で確かめたときだけである。**
⚠️ **「実装済」は試験が緑という意味であって、実物で動いた証拠ではない**（規則 04）。**だから `テスト完了` と「実物で確認」を分けて持つ。**

---

## ステータスの定義

| ステータス | 意味 | 次に動かす者 |
|---|---|---|
| `未検討` | 受け付けただけ。原因も対応案も無い | 前に立つ者（調査を出す）|
| `追加裁定待ち` | 原因は分かったが、**仕様に行が無い**か、利用者にしか決められない | ⛔ **利用者** |
| `仕様確定` | 裁定は下りた。⚠️ **まだ仕様書に書かれていないことがある** | 前に立つ者（変更要求）|
| `実装待ち` | 仕様は揃った。着手していない | 実装の体 |
| `実装中` | 体が書いている | 実装の体 |
| `テスト中` | 実装は入った。仕様だけを読む体が試験を書いている／走らせている | 試験の体 |
| `テスト完了` | 試験が緑。⚠️ **実物での確認は別欄で持つ** | 前に立つ者（実物で確かめる）|

---

## 一覧

| ID | 不具合内容 | 期待値 | 対応案 | 対応方針 | ステータス | 詳細状況 | 補足 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D-01 | **行が 7 行しか出ず、インデントも浅く、行の操作子 `>` `^` が効かない** | 入る限り深い LOD を出す。`L3` が入らなければ `L2`、`L2` も入らなければ `L1`、`L1` でも入らなければ縦スクロール | ①縦の Fit を LOD の段の離散選択にする ②しきい値 `S-87` を下げる ③起動テンプレートに `scrollDate` を持たせる | ⭐ **①**（利用者の裁定 2026-08-26）。⛔ **新しい設定値は足さない** | `仕様確定` | ⛔ **機構を測った**: `schedule-layout.ts:302` が `max(floor, basePlanHeight × zoomY)` で、`floor = actualMin ÷ actualOfPlan = 21.918px`、`basePlanHeight = 28`。⇒ `zoomY` が **0.7828** を下回ると高さは 1px も縮まず、LOD のしきい値だけを割って行を消す。`fitZoom` は 0.07 まで下げている | 実寸: `L1` 752px ／ `L2` 2774 ／ `L3` 6708 ／ `L4` 13216 ／ `L5` 27607。Row Area は 720p で 599・1080p で 959・1440p で 1319 |
| D-02 | **フェードの角の `□` がドラッグに追従しない** —— `□` は出るが、掴んで動かしても `□` はその場に留まり、フェード日数だけが変わる | ⭐ **`□` がポインタに追従する。**確定は離した時 | ①`ScreenSession` に「握っているジェスチャ」を持たせ、描く側が途中の絵を描く ②フェード専用の仕掛けを作る | ⭐ **①**（利用者の裁定 2026-08-26）。⛔ **専用の仕掛けは作らない** —— `D-03` と同じ根で、1 つ直せば両方直る | `仕様確定` | ⛔ **根**: `ScreenSession` は `pointer` と `pointerRestedMs` しか持たず、**press も drag も持たない。**描く側は握っているものを見られない。⭐ コマンドパレットだけが例外的に追従している | ⛔ **文書には離した時にしか書かない**（利用者の明示）|
| D-03 | **パネルの幅がドラッグに追従しない** —— 離すまで動かない | ⭐ 握っているあいだ追従し、離した時に確定する | ①`D-02` と同じ `ScreenSession` の仕掛け ②毎フレーム文書に書く | ⭐ **①**。⛔ **②は採らない** —— 利用者の明示「ドラッグ中は GRS JSON に保存する必要はない」 | `仕様確定` | ⚠️ **私が「文書値だから待て」と誤って分類した。**実測: パンもズームも `input-command-translator.ts:2037` により**離した時にしか書いていない** | `FR-052` はドラッグ中の絵に沈黙。⭐ 1 文足す |
| D-04 | **実績のダミーが見えないので掴めない** | 薄いダミーが見え、ポインタを乗せると濃くなり、掴むと実績が置かれる | ①表 T-210 に描く大きさの行を足す ②当たり判定（`S-93`）を流用する | ⭐ **①。幅は 12px**（利用者の裁定 2026-08-26）。⛔ **②は採らない** —— `S-93` は読む人の当たり判定であって描く寸法ではない | `仕様確定` | ⭐ **掴みと当たり判定は既に動く**（`3f8d2e8` で `CM-14` を書くようにした）。⛔ **描く行が仕様にゼロなので描けていないだけ** | ⚠️ **`EP-14`（書き出しに出さない）に入口が無い。**描き始めた瞬間に書き出しへ漏れる |
| D-05 | **2 連カーソルが 1 本も描かれない** | `DC-2` で 2 本置け、追従している側が太く描かれる | ①追従側を `ScreenSession` に持つ ②`documentSettings` に 3 つ目の欄を足す | ⭐ **①**（利用者の裁定 2026-08-26）。⛔ **②は往復に一過性の状態が乗る** | `仕様確定` | ⛔ **破断は 5 つ**: ①モードが `true` にならない（`frame-loop.ts:2234`）②`IC-45` に動作が無い ③`DC-2` のクリックが飲まれる ④~~2 つの日付が型から消えていた~~ ✅ `9decb53` で修正 ⑤`ScheduleGeometry` に行が無い | `S-65` は日付を 2 つ持つだけで「どちらが追従中か」を持たない |
| D-06 | **コメントボックスが 1 つも描かれない** | 注記が描かれ、選択すると枠が付く | ①`FR-093` の概算で AutoFit ②大きさの行を新設 | ⭐ **① ＋ 余白 3px ／ 折り返しは全角 64 文字（半角 128）**（利用者の裁定 2026-08-26）。入らなければ作成者が改行する | `仕様確定` | ⛔ `commentBoxes` は `layoutEngine` にも描画側にも 1 度も現れない。⭐ ハイライトボックスは描かれており、同じ行の片方だけが空いている | ⭐ **`FR-093` が禁じるのは実測であって、文字に合わせること自体ではない** |
| D-07 | **ルーラの背後が透ける** | 帯に地の色が敷かれる | ①`FR-041` の「地の色を自分で塗ること（MUST）」を根拠に `S-146` で 1 枚 ②帯の範囲を述べる行を新設 | ⛔ **未裁定** | `追加裁定待ち` | ⛔ **実測（実 DOM）**: 矩形は 5 枚とも行の帯（y ≥ 85）で、ルーラの帯に 1 枚も無い。⭐ `S-146` の経路は `9decb53` で通した | 帯の範囲を述べた行がどこにも無い |
| D-08 | **ルーラの枠と日付が干渉する** | 段の中で文字が枠に当たらない | ①`S-2` の式の `plusTimes` を 3 → 6 ②下側の余白の行を新設 | ⛔ **未裁定。**⚠️ **①は算術で破綻する** | `追加裁定待ち` | ⛔ `plusTimes` を 6 にすると `S-2` 自身の下限（36 ＋ 120 = 156）が上限 150 を超える。`S-136` の上限 20 は「×3 でも収まる値」として導かれている | ⇒ ②を推す |
| D-09 | **ルーラに曜日が出ない** | 4 段目で曜日が出る | ①`display-words.json` に節を足し生成器で運ぶ ②新しい表を作る | ⭐ **①**（新しい表は不要。`weekdayOf` は既に在る）。⛔ **語は利用者が書く**（`PD-160`）| `実装待ち` | ⭐ 日本語の名簿は `_source/settings.json` に既に在る。英語が空 | 置き場を作り、語は空で置く |
| D-10 | **`AR-1` が画面に literal で出る** | 構えているものが語で読める | ①辞書に表 T-023b の節を足し、語は空で置く ②空文字を返す | ⭐ **①**。⛔ **②は `FR-053` の MUST 違反** | `追加裁定待ち` | ⛔ **実測（実 DOM）**: `AR-1` が出ている。`armedRow` は 6 腕すべてに行 ID を返す | 語 6 組は利用者が書く |
| D-11 | **有効／無効のアイコンが同じ色に見える** | 有効なものが分かる | ①表 T-236 に色の行を足す（利用者は「一旦緑」と裁定）| ⭐ **①**。⚠️ **ただし `FR-072` の未達と、構え（押下）の表示は別問題** | `追加裁定待ち` | ⭐ `data-pressed` は DOM に在る。⛔ 塗る CSS が 1 行も無い（`src/` に `.css` が 0 個）| 色の行が表 T-236 に無い |
| D-12 | **パレットの構えが押下として描かれない** | 構えている入口が見て分かる | ①表 T-109 に「腕」の欄を足し生成器で運ぶ ②諦める | ⛔ **未裁定** | `追加裁定待ち` | ⛔ **そもそも「腕を押下として描け」と言う要求が 1 つも無く、`IC-54` は「ボタンではない」と明記。**15 腕のうち散文で行 ID を名指すのは 3 つだけ。唯一の対応表は別コンポーネントに在り、`LR-3` が import を禁じる | 表 T-109 に列を足すのが筋 |
| D-13 | **プロパティパネルの値が編集できない** | 参考実装と同じ寸法・入力欄 | ①表 T-203 の塊ごと測ってから寸法の行を足す | ⛔ **未裁定** | `追加裁定待ち` | ⛔ 製品に入力欄が対話欄以外 0 個。⭐ 参考実装の実測値は取得済み | `impact.py --cluster` を先に |
| D-14 | **ヘルプ画面が出ない** | 入口の一覧・ライセンス・著作権表示・帰属が出る | ①置き場を作り、語は空で置く（`PD-160`）| ⛔ **語 126 セルは利用者が書く** | `追加裁定待ち` | 表 T-023a〜d と T-036 の語が辞書に無く、ライセンス本文も `src/` に無い | 置き場作りは裁定不要 |
| D-15 | **`FD-6` が暦日、`IV-12` が稼働日で食い違う** | どちらも暦日 | ①暦日に揃える | ⭐ **①**（利用者の裁定 2026-08-26）| `仕様確定` | 非稼働日をまたぐ予定で、`FD-6` が許すフェードを `IV-12` が拒む | |
| D-16 | **長押しでズームが連続しない** | `S-172` ののち `S-173` 間隔で刻み続ける | ①既存の待ち（`beginPointerRest`）と同じ形で軽く実装 | ⭐ **①**（利用者の裁定 2026-08-26）。⛔ **仕様書にも書く** | `テスト完了` | ✅ `3f8d2e8` で実装。`S-172`/`S-173` は生成物として届く | ⚠️ **実物未確認。**⛔ 表 T-078 に契機の行を足すのが未了 |
| D-17 | **マイルストーン一覧の入口が `▼` / `▲`** | `...` のトグル | ①`IC-50` / `IC-51` の図形を三点にする | ⭐ **①**（利用者の裁定 2026-08-26）| `テスト完了` | ✅ `e59ba7c`。⭐ **実 DOM で確認済み**（`IC-50` / `IC-51` とも円 3 つ）| |
| D-18 | **ダークモードで文字が黒・縁が白** | 文字が白・縁が黒 | ①`S-168` / `S-169` を読む | ⭐ **①**（利用者の裁定）| `テスト完了` | ✅ `82996cb`。⚠️ **実物はテンプレートが `light` なので dark 側は未確認** | |
| D-19 | **フェードの掴み点が矢羽根・レクタングルで出ない** | どの形でも出る | ①日数での絞りを外し、当たり判定を選択で絞る | ⭐ **①**（利用者の裁定）| `テスト完了` | ✅ `8c3b975`。⛔ 絞りは 1000 タスク中 997 で掴み点を消していた | ⚠️ **実物未確認**（`D-02` はこの続きで見つかった）|
| D-20 | **未着手のマーカーが濃いまま** | `S-131` で薄く描く | ①`markerSvg` に不透明度を与える | ⭐ **①**（`FR-013` の MUST）| `テスト完了` | ✅ `8c3b975`。⭐ **実 DOM で確認済み**（`opacity="0.2"` が 16 個）| |
| D-21 | **`Ctrl+A` → `Del` で行が残る** | ⛔ **仕様どおり。**利用者が許容と裁定 | —— | ⛔ **対応しない** | `テスト完了` | `SL-1` のまま | |
