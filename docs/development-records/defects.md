# 不具合・改善要望の管理台帳

**規則は [`docs/development-rules/05-working-method.md`](../development-rules/05-working-method.md) の 7.（5 段のパイプライン）。**

⛔ **利用者が指摘した不具合は、直るまでこの表から消さない。** ⭐ **消してよいのは `実測済` か `取下げ` になった行だけである。**

⭐⭐ **完了分は隣のファイルに在る**: [`fixed-defects.md`](fixed-defects.md)。**2026-09-02 に 85 行を移した**（利用者の裁定 2026-09-01）。⛔ **要約せず、1 文字も削らずに移してある。**
⛔⛔ **戻り道が在る。片道にするな。** **実物で落ちたら、その行を `fixed-defects.md` から本書へ戻すこと。**⚠️ **前の巡は `D-06` と `D-43` が「自動試験は緑なのに実物で落ちる」ことを示した。**
⚠️ **道具と検査は 2 ファイルの両方を読む** —— `tools/ledger_metrics.py` と 検査 28 / 29 / 31。⛔ **片方しか読まない道具を足すな** —— **刈り取ったぶんだけ空を測り、全部緑になる。**

⚠️ **「自動試験が緑」は実物で動いた証拠ではない**（規則 04）。**だから `試験待ち`（コードに在るが自動試験がまだ緑でない）・`実測待ち`（緑だが出荷ビルドをまだ押していない）・`実測済`（押して期待値を測った）を分けて持ち、`実物確認` の欄で裏を取る。**

---

<!-- ledger-metrics: begin -- generated by tools/ledger_metrics.py -->

### ⭐ 件数（**`tools/ledger_metrics.py` が刷る。手で書き換えない**）

| | 残件 | 総件数 | 未検討 | 裁定待ち | 仕様待ち | 実装待ち | 試験待ち | 実測待ち | 実測済 | 取下げ |
| --- | --: | --: | --: | --: | --: | --: | --: | --: | --: | --: |
| セッション開始時 2026-09-02 06:24 | 58 | 183 | 5 | 1 | 5 | 7 | 40 | 0 | 121 | 4 |
| 最新 2026-09-02 11:12 | 50 | 196 | 4 | 5 | 2 | 9 | 29 | 1 | 140 | 6 |
| ⭐ 差分 | -8 | +13 | -1 | +4 | -3 | +2 | -11 | +1 | +19 | +2 |

⚠️ **巡の頭で `python tools/ledger_metrics.py --start` を 1 度走らせる。**⛔ **手で書かない** —— 測れる数を手で書けば必ず腐る。⚠️ **時刻まで持つ** —— 1 日に複数の巡が走るので、日付だけでは見分けられない。

⛔⛔ **上の数は 2 ファイルの合計である** —— 本書と [`fixed-defects.md`](fixed-defects.md)。⚠️ **合計で数えるのは、刈り取りが進歩に見えないようにするためである** —— **片方だけを数えれば、仕事を終えた行の数ぶん総件数が減り、何もしていない巡が前進に見える。**

<!-- ledger-metrics: end -->

---

## ステータスの定義

⭐ **2026-09-01、利用者の裁定で 10 状態を 8 状態へ組み替えた。理由は 2 つある。**

⚠️ **1 つ目 —— 古い 10 状態は軸を 2 本混ぜていた。** **仕事がどこまで進んだか**と、**いま誰が持っているか**の 2 本である。2 本目（`〜待ち` と `〜中` の別）は腐った。**実測すると `テスト中` 20 行と `テスト待ち` 20 行は実務上まったく同じ意味で使われており**、しかも `テスト待ち` は 2026-09-01 まで定義の表に無かった。

⛔ **2 つ目 —— `テスト完了` は主張を 2 つ同時に担いでいた。** 「自動試験が緑である」と「誰かが出荷ビルドを開いて実測した」の 2 つである。**この 2 つは同じではない。** この巡は、**5570 件の自動試験がすべて緑のまま、実物を押して欠陥を 4 件見つけている。**

⭐ **だから新しい 8 状態は、終端の 2 つを除き、すべて「何を待っているか」で名づける** —— 名前が、次に動く者を言う。

| ステータス | 意味 | 次に動かす者 |
|---|---|---|
| `未検討` | 受け付けただけ。原因も対応案も無い | Claude（調査）|
| `裁定待ち` | 原因は分かったが、**仕様に行が無い**か、利用者にしか決められない | ⛔ **利用者** |
| `仕様待ち` | 裁定は下りた。⚠️ **まだ仕様書に書かれていない** | Claude（仕様を書く）|
| `実装待ち` | 仕様書に在る。⚠️ **まだコードに無い** | Claude（実装）|
| `試験待ち` | コードに在る。⚠️ **自動試験がまだ緑でない** | Claude（試験）|
| `実測待ち` | 自動試験が緑。⛔ **まだ出荷ビルドを押していない** | Claude（実物で押す）|
| `実測済` | 押して期待値を測った | ⭐ **刈り取り（次巡で `fixed-defects.md` へ）** |
| `取下げ` | 欠陥ではなかった／別の行に吸収された | ⭐ **刈り取り** |

⚠️ **2026-09-01 の付け替えは機械的である。** 古い名前から新しい名前へは次のとおり移した —— `取下げ`→`取下げ`、`テスト完了` で `実物確認` が ⛔ で始まらないもの→`実測済`、`テスト完了` で ⛔ で始まるもの→`実測待ち`、`テスト中`／`テスト待ち`→`試験待ち`、`実装待ち`／`実装中`／`仕様書反映済み`→`実装待ち`、`仕様確定`→`仕様待ち`、`追加裁定待ち`→`裁定待ち`、`未検討`→`未検討`。⛔ **`ステータス` の欄以外は 1 バイトも触っていない。** ⭐ **すでに実測を持つ行は、系の試験が無いことを理由に後ろへ戻さない（利用者の裁定）。**

---

## 一覧

⛔ **`対応方針・決定仕様` の欄は 2 つを持つ。**裁定が下りるまでは**対応方針**を、仕様書へ入れたら**書いた先**を書く —— **章・仕様管理番号（要求 UID か表の行 ID）・仕様の逐語**（利用者の裁定 2026-08-30）。⭐ **これで、仕様書への記載漏れが台帳の上から見える。**

| ID | 不具合内容 | 期待値 | 対応案 | 対応方針・決定仕様 | ステータス | 詳細状況 | 補足 | 実物確認 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D-24 | **表 T-068 の「2 回目」が実装されていない** | 全体表示だけが層別計算を 2 回まで走らせる | ①`fitZoom` が 2 回目を持つ | ⭐ **①**（`D-01` に含める）| `試験待ち` | ⛔ `frame-loop.ts:1544` は 1 回走らせて無条件に採る。⚠️ **素朴な 2 回目は振動する** —— 実測 1.0 → 0.4291 → 0.7322 → 0.3785 → … 永久に往復。⭐ **「3 回目を走らせてはならない」はこれを防いでいた**。✅ 2 回目は `fitZoom` の (d) が持ち、着地倍率が `FR-094` の床より上のときだけ走る。振動は「3 回目を走らせない」で止まる | | ⛔ 未 |
| D-15 | **`FD-6` が暦日、`IV-12` が稼働日で食い違う** | どちらも暦日 | ①暦日に揃える | ⭐ **①**（利用者の裁定 2026-08-26）| `試験待ち` | 非稼働日をまたぐ予定で、`FD-6` が許すフェードを `IV-12` が拒む。✅ **暦日に揃えた** —— 表 T-012a の `FD-6` と 表 T-220 の `IV-12` に暦日で数える旨を書き、`Schedule` に `calendarDaysBetween` を置いて 表 T-064 の `PI-1` に宣言した。⛔ `planSpanOf`（`FR-012` の稼働日）は動かしていない。⚠️ **試験は仕様だけを読む体が書く** | | ⛔ 未 |
| D-27 | **パネル幅が取り消しで戻ってしまう** —— 表 T-027 の `UN-16` は パネル幅（`FR-052`）を取り消しの**対象外**と定めているのに、1 回の `Ctrl+Z` が幅を戻す | 幅は取り消しで動かない | ⭐ **①を推す** —— `UN-16` の逐語が「保存することと戻せることは別である」であり、**保存をやめろとは言っていない。**取り消しのときに `UN-16` / `UN-7` / `UN-8` / `UN-12` の欄だけ現在値を残せば足りる | ⛔ **未定。まず機構を測らせる** | `試験待ち` | ⛔ **前に立つ者が機構を確かめた（体の報告を引き写していない）。**⭐ **`isUndoable` は正しい** —— `document-change-plan.ts:127` が `setPanelWidths` を含む `UN-16` の 4 命令に `false` を返しており、境界の離しは段を 1 つも積まない。⛔ **壊れているのは段の中身である** —— `WS-4` が積むのは**文書まるごとの写し**なので、あとから積まれた別の段（例: フェードの編集）の写しに**その時点の幅**が入っており、取り消すと幅も一緒に戻る。⚠️ **同ファイル 137〜143 行が、倍率について同じ罠を既に論じている** —— `CM-71` と `CM-72` の順序をそこで気にしているのは、まさにこの形である。⭐ **一般の解は無く、命令ごとに手当てされてきた** ✅ **直した（2026-08-27）** —— 除外する列の点呼を `document-change-plan.ts` に置き、取り消しと やり直し の両方に当てた。18 列、それぞれ 表 T-027 の行を名乗る。⭐ **`tests/unit/fr-052-t-023d-picture-while-held.test.ts` が 41/41** —— 利用者が報告した「パネル幅が戻る」は、実際に走る経路で緑である | ⚠️ **`UN-16` は「保存することと戻せることは別である」とわざわざ書いている。**⭐ `UN-12` が `Dual Cursor` の位置で同じ形を既に持つ | ⛔ 未 |
| D-35 | **コマンドパレットに最小化ボタンが無い**（利用者の指示 2026-08-25 / 08-26）—— `[ _ ]` で最小化し、最小化中は `[ □ ]` に変わって元に戻せること。**最小化中は `[ □ ]` の左に「・・・」を縦 2 列に並べて掴む領域を表す** | 最小化と復帰ができ、最小化中も掴んで動かせる | ⭐ **案 A（4 か所に行を足す）**（利用者の裁定 2026-08-28） | ⭐ **表 T-109 には 1 行だけ。トグルは 1 つ** | `試験待ち` | ⛔ **前に立つ者が逐語の記録から抜き出し、実測で裏を取った（2026-08-26）。**⛔ **実測**: 「最小化」は `docs/spec` にも `src/` にも **0 件**。`docs/development-records/W4-adapter.md` に 1 件あるだけで、それは開発記録であって仕様ではない。⇒ **要求も入口も図形も語も、すべて無い** ⛔ **体が正しく止まった（2026-08-28）。**⭐ **実測: 「最小化」は `docs/spec` に 0 件、`src/` に 0 件。**⛔ **行が 4 か所に要る**: ①**`FR-053` に段落**（最小化が何を意味し、何が残るか。⛔ **構えの表示は残さねばならない** —— 同要求が「いま構えているものが画面上で読めること（MUST）」を持つため）②**表 T-206 に行**（最小化しているか。真偽・既定 false・保存しない。⭐ `S-142` の持ち方を写す）③**表 T-109 に 1 行**（⛔ **2 行ではない** —— `IC-11` / `IC-60` が先例で、利用者の `D-70` の言葉も「トグルは 1 つ」）④**図 F-019 に図形と辞書に語**（⚠️ **表 T-109 の刷る順どおりに置くこと** —— 順が違うと生成器が何も書かずに止まる）。⇒ **アイコン 74 → 75** ✅ **裁定が下りた（2026-08-28、案 A）。**⭐ **前に立つ者が自分で確かめた**: 「最小化」は **`docs/spec` に 0 件、`src/` に 0 件**。完全に未定義である。⛔ **書くもの 4 か所**: ①**`FR-053` に段落** ——「パレットを最小化できること（MUST）。最小化しているあいだも、掴み帯と、いま構えているものの表示は残すこと（MUST）」。⛔ **後半は省けない** —— 同要求が「いま構えているものが画面上で読めること（MUST）」を持つため。②**表 T-206 に行** ——「`Command Palette` を最小化しているか（真偽・既定 `false`・保存しない）」。⭐ **持ち手は `S-142` の形を写す**（シェルが持ち `ScreenSession` で届く）。③**表 T-109 に 1 行** ——「パレットを最小化し、同じ入口で戻す」、正 `FR-053`、構え `—`。⛔ **2 行ではない**（`IC-11` / `IC-60` が先例、利用者の `D-70` の言葉も「トグルは 1 つ」）。④**図 F-019 に図形、辞書に項**（⚠️ **表 T-109 の刷る順どおりに**）。⇒ **アイコン 73 → 74** ⭐ **仕様を書いた（CR-273、版 1.25）** —— `FR-053` に最小化の段落、表 T-206 に `S-200`、表 T-109 に `IC-75`、図 F-019 に図形、辞書に 1 項。**アイコンは 74 のままである**（`D-70` の −1 と相殺）。 ✅ **実装した（前に立つ者。2026-08-28）** —— 5 か所。`screen-renderer.ts` に `CommandPalette.minimise` ／ `.isMinimised` と `ScreenSession.isPaletteMinimised`、`command-palette.ts` が名簿から `IC-75` を組み立て**最小化中は `groups` を空にする**（帯と構えの語は残す）、`input-command-translator.ts` が `togglePaletteMinimised` を出し、`frame-loop.ts` が `S-200` を持ち、`dom-screen-surface.ts` が帯の右端に描く。⚠️ **公開型が動いたので試験の足場 21 ファイルに項の追加が要った**（機械的）。⭐ **`display-words.contract` の赤 11 件と `uf-65` の赤 6 件が閉じた。** | ⭐ **参考実装 `temp/gr-scheduler_current_io.html` に同じものが在る。**⚠️ 最小化の状態は文書に保存しない（読む人の環境） | ⛔ 未 |
| D-38 | **日付ルーラの目盛が中途半端な間隔で出る**（利用者の指摘 2026-08-26）—— ⛔ **2 日区切りなどが出る。実務では混乱を生む** | ⭐ **利用者の裁定 2026-08-26。次の 4 つだけとすること**: ①年のみ ②年と月（**1 か月単位。2 か月飛ばしを禁じる**。月は**数字のみ**とし `Jan`〜`Dec` は用いない）③年と月と週（**週は月曜日。曜日は出さない**。7 日区切り）④年と月と日と曜日（1 日区切り） | ①`tickStrideOf` の間引きを、上の 4 つに合う刻みだけに限る | ⭐ **①**（利用者の裁定 2026-08-26） | `試験待ち` | ⛔ **前に立つ者が逐語の記録から抜き出し、実測で裏を取った（2026-08-26）。**⛔ **機構**: `schedule-layout.ts:375` の `tickStrideOf` が `ceil(必要幅 ÷ pxPerDay)` を返すので、**2・3・5 …** どんな刻みでも出る。⭐ **段そのものは正しい** —— `rulerTierOf` は `rulerFont/fontMin` で割ってから閾値と比べており、出荷テンプレートの実効値 1.319 は月の閾値 1.4 を下回るので `year`。**私は一度これを欠陥と疑い、誤りだった** ✅ **実物で確認した（2026-08-27）** —— 出荷する `dist/index.html` を Playwright で開き、ホイールで段を上げながら目盛の各段の文字を読んだ。**1 倍: `2026 2027 2028 2029`（年のみ）。1 段上: 年 `2026` ／ 月 `4 5 6` ／ 週 `30 6 13 20 27 4 11 18 25 1`（7 日刻み）。2 段上: 年 `2026` ／ 月 `4` ／ 日 `1 水 2 木 3 金 4 土 5 日 6 月 7 火 …`（1 日刻み）。**⭐ **裁定の 4 つの間隔ちょうどであり、2 日おきはどの倍率でも出ない** ⛔ **`D-64` の裁定（案 E、2026-08-27）で `S-85` が 44 → 26 になる。**⚠️ **まだ実装していない** —— 段の組み方（`ROWS_OF_TIER`）と `S-85` と導出の表を同じ 1 つの変更要求で書くこと。⭐ **導出の表の第 4 行は `05 月` ／ `05 Mon` ではなく、日の行と曜日の行の広いほう（`Mon` ＝ 18px）で書き直す**| ⛔ **`L-1` の段の名前と、この 4 つの間隔は別物である。**段は「何を出すか」、本行は「どの間隔で出すか」を定める | 2026-08-27 |
| D-43 | **ドラッグしただけでプロパティパネルが開き、ラベルの編集も起きる**（利用者の指摘 2026-08-27） | ⭐ **タスク本体も名称ラベルも、ダブルクリックでプロパティパネルが出て、名称の欄（表 T-016 の `PR-1`）が編集できる状態になり、焦点が置かれ、既にある文字がすべて選ばれている**（利用者の裁定 2026-09-02。⛔ **2026-08-27 の「その場で編集する」を上書きした**）| ⭐ **裁定済 2026-08-27。利用者の提案どおり** —— **ダブルクリックはその場のラベル編集。パネルへ寄り道しない。**⛔ **前に立つ者の「パネル 1 本に」案は撤回された**（パネルは既に選択に追従しているため冗長だった）| ⭐ **①＋②**（利用者の裁定 2026-08-27） ✅⭐⭐ **裁定が下りた（利用者、2026-09-02）: 行名（`D-180`）に揃える。**⛔ **2026-08-27 の「その場でラベルの編集に入る。パネルへ寄り道しない」は、この裁定が上書きした。**⭐ **仕様は既にそう書かれている** —— **表 T-023 の `MK-13`（`docs/spec/01-04-requirements.md:2324`）が、2026-08-30 の裁定で「タスク（名称ラベルと本体のどちらでも）＝ プロパティパネルを出し、名称の欄（表 T-016 の `PR-1`）を編集できる状態にして焦点を置き、既にある文字をすべて選んだ状態にすること（MUST）」と定めており、2026-09-01 の裁定で行見出しも同じ形になった。**⇒ ⛔ **仕様も実装も動かさない。古かったのは本行の期待値の欄だけである。**⛔ **`editInPlace`（`frame-loop.ts:4375`）は建てない** —— **`SK-19` / `SK-9` / `MK-13` の 3 つの不在は、この裁定では閉じない。** | `実測待ち` | ⛔ **前に立つ者が実測した（2026-08-27）。**⭐ **いまも押下と離しは分かれており、ダブルクリックは `clickCount >= 2` で判定している**（`input-command-translator.ts:3316`）。⛔ **足りないのは「既にパネルが開いているとき、掴んだ対象へ差し替える」**経路である。⚠️ **`D-26` の裁定（ラベルを掴みから外す）と一体で考えること** —— ラベルが掴みでなくなると、ダブルクリックは `GR-12` に当たる。⭐ **実測で、この提案に費用がほぼ無いことが分かった** —— `frame-loop.ts:4871` が `if (selection !== context.selection) showPropertiesOfChoice()` を持っており、**パネルは既に選択に追従している。**⇒ **ダブルクリックでパネルを開く経路は元から冗長**であり、ダブルクリックを名称の編集に明け渡しても、パネルへ届かなくなることはない。⛔ **ただし「その場の編集器」は存在しない** —— `frame-loop.ts:4375` の `editInPlace` は STOP で、「**SK-19、SK-9、MK-13 が開こうとする欄を、このビルドは 1 つも描いていない**」と記録している。⭐ **作れば `SK-19` / `SK-9` / `MK-13` の 3 つの不在が同時に閉じる** ✅ **掴みと語彙の半分は入った（2026-08-27）** —— ダブルクリックはタスク本体からも名称の編集へ届き、`openPropertiesPanel` は語彙から消えた。⛔ **残るのはその場の文字入力欄そのものである**（`CR-266` の 5.）| ⛔ **触る行**: `MK-13` の「タスク本体 ＝ プロパティパネルを開く」を「名称の編集」に変える／`IN-5a`（編集入力の確定前）の状態を持つ／`isTextEntryUnsettled` を本物にする。⚠️ **パネルを閉じる入口は `IC-52` に在る**（`D-39`） | ⛔⛔ **2026-09-01、出荷ビルドで期待値を満たさない** —— **その場では編集できない** —— 素の押しと素のドラッグでパネルが開かないのは直っている。⛔ **しかしタスク本体をダブルクリックするとプロパティパネルが開き、右端（x ≧ 1765）の入力欄に焦点が移る。**⚠️ **その場に編集欄は出ない。**⇒ **期待値「その場でラベルの編集に入る。パネルへ寄り道しない」を満たさない。**⚠️⚠️ **利用者の 2026-09-01 の指示（タスクグループ名のダブルクリックでパネルを開く）と向きが逆である。裁定が要る。** ⚠️ **2026-09-02 に測り直しが要る** —— **2026-09-01 の実測はパネルが開いて焦点が移るところまでで、`MK-13` の MUST「既にある文字をすべて選んだ状態」は測っていない。**⇒ **選択範囲を読むまで `実測済` にしない。** |
| D-49 | **行のインデントが、画面と書き出しで違う値を使っている**（利用者の指示 2026-08-26 「1 階層下がるごとに全角で 1 文字分ずつインデントしろ」） | ⭐ **画面・書き出しとも、1 階層につき全角 1 文字ぶん** | ①`rowTitleIndent`（`S-37`）を `IF-9` に載せて画面へ渡す ②`S-37` の単位を px から em に変える ③書き出しを em に合わせる | ✅⭐⭐ **本欄の下にある「未定」は古い。読み飛ばすこと**（2026-09-01 の棚卸し）—— ⛔⛔ **本欄は書き足しで伸びるので、裁定が下りても冒頭の「未定」がそのまま残る。**⭐ **裁定は下りており、下の記述がそれを述べている。** ⛔ **止まった。`RowTitle` に項が要る**| `試験待ち` | ⭐ **画面は利用者の指示どおりである（実測）** —— `dom-screen-surface.ts:1399` の `ROW_INDENT_EM = 1` を `1828` 行が `(depth − 1) × 1em` の左余白として当てており、**全角 1 文字ぶんそのものである**。⛔ **書き出しは違う** —— `image-exporter.ts:386` は `title.depth × settings.rowTitleIndent` で、`S-37` は **px の 12** である。⇒ 読む人の文字サイズが 12px でないかぎり、画面と書き出しでインデントが食い違う。⛔ **`FR-085` の打ち切り計算も `S-37` を読む**（`row-title-panel.ts:145`）ので、**名前を切る幅と実際に押し出される幅も食い違う。**⚠️ **`dom-screen-surface.ts:1389` の STOP がこれを自ら申告している** ——「`DocumentSettings` は `IF-9` を渡らないので、この 2 つの数を合わせようがない」。⛔ **前の巡の「インデントは 4px」は誤測である** —— 4px は `rowTitle` の左右の素の余白 `0.25em`（16px 地で 4px）であって、階層ごとのインデントではない ⛔⛔ **前に立つ者の指示が逆だった。体が実測で覆した（2026-08-27）。**⛔ **「画面が正しく書き出しが誤り」は偽** —— **書き出しのほうが仕様どおりである。**`image-exporter.ts:392` と `row-title-panel.ts:191` はどちらも `depth × rowTitleIndent`（`FR-085` の算式そのもの）。⛔ **画面のほうが仮置きである** —— `dom-screen-surface.ts:1529` の `ROW_INDENT_EM = 1` は、同ファイルの注が自ら「**`rowTitleIndent`（`S-37`）ではない。本来ここに来るべき値である**」と書き、`@provisional PD-152` を付けている。理由は **`DocumentSettings` が `IF-9` を渡れないこと。**⭐ **実測した食い違い**: 画面 − 書き出し ＝ 深さ 1..5 で **−8 / −4 / 0 / +4 / +8 px**（深さ 3 だけ一致し、そこで符号が反転する）。⚠️ **`(depth − 1)` と `depth` のずれも在り、これは 2 つ目の食い違いである。**⭐ **`PD-152` 自身が答えを名指している** —— **`RowTitle` にインデントを運ばせる。**⛔ **実装は 2 ファイルぶん入ったが、`screen-renderer.ts` に `readonly indentPx: number` を足さないと型が通らず、その 1 行は別の体が持っていたので合流していない。**⚠️ **加えて 5 つの試験ファイルが `RowTitle` のリテラルを組んでおり、必須の項を足した瞬間に型検査が落ちる。**⛔ **`D-49` の逐語（全角 1 文字ぶん）はこれでも閉じない** —— **`rowTitleFont × 2 × labelCoef` ＝ 13.0px** に対し `S-37` は **12** を持ち、しかも保存する値は整数型である | ✅ **CR-287 で 3 つの食い違いを閉じた（2026-08-30）** —— `RowTitle` に `indentPx`（`depth` × `S-37`）を足し、`row-title-panel.ts` が `availableLabelWidthPx` と**同じ積**を入れ、`dom-screen-surface.ts` はそれを描く。`ROW_INDENT_EM` と `@provisional PD-152` は消えた（検査 25 が確かめた）。⭐ **実物で確かめた** —— D-123 が直って深さ 1〜3 が画面に出たので、初めて測れた。⭐ **措置前は 4 / 20 / 36 px**（段ごと 16px で、`S-37` の 12 でも `depth × S-37` でもなかった）。⚠️ **利用者の逐語（全角 1 文字ぶん＝13.0px）と `S-37`＝12 の 1px の差は残る** —— 設定値を動かすのは別の変更要求である | ✅ 2026-08-30 |
| D-52 | **選択の印が、書き出した絵に残る** —— ⛔ **表 T-076 の `EP-12` は `Selection`（`U-39`）を「描かない」と定めている** | 書き出した絵では、依存線も 2 連カーソルも、選ばれていないときと同じ太さで出る | ①書き出しの経路で選択を空にしてから描く ②線の太さを決める箇所が書き出しかどうかを見る | ⭐ **①** —— ⛔ **②は `EP-12` の 5 種すべてに同じ判定を配ることになる。**`EP-12` は 1 つの規則なので、判定も 1 か所であるべきである | `試験待ち` | ⛔ **仕様だけを読む体が書いた試験が落として発覚した（2026-08-27）。前に立つ者が走らせて確かめた。**⛔ **実測（2 件とも赤）**: 追従している 2 連カーソルは、書き出しでも太さ **2**（選ばれていないほうは 1）。選ばれた依存線は書き出しでも **3**（素の太さは 1.5）。⇒ **`SL-8` の「線は太さで示す」が、そのまま書き出しへ抜けている。**⚠️ **外接矩形の破線の枠のほうは抜けていない** —— 抜けているのは太さで示す側だけである。⭐ **`EP-12` は「指している場所と構えを表すものであり、日程ではない」と理由まで書いている** —— 渡した相手の絵に、こちらが何を選んでいたかが写る ✅ **実装した（2026-08-27）。**⛔ **記録より大きい穴であった** —— 体が測って報告した: 漏れていたのは太さの 2 形だけではなく、**選択の枠・フェードの掴み点を含む 6 形**である。今日の呼び手が空の選択を渡していたので枠が漏れなかっただけで、ユニットとしては漏れる。⭐ **`svgFromSchedule` の頭で、書き出しのときだけ選択と追従を空にする 1 文**にした —— `EP-12` は 1 つの規則なので判定も 1 か所である。⭐ **`tests/unit/t-023c-selected-line-width.test.ts` が緑** | ⭐ **`D-40`（線を太くする）を入れたときに開いた穴である。**⛔ **その巡に試験が 1 件も無かったので誰も気づけなかった** —— 引継書 §4 が言っていたとおりのことが起きていた | ⚠️ **測れなかった（2026-09-01）。ただし前提が古い** —— **測ったのは 07:54 の build で、画像の書き出しがどこにも配線されていなかった。**⭐ **同日のうちに `.png` が配線され、実測で 256,367 バイトの本物の PNG が書き出せている**（台帳 D-173）。⇒ **`.svg` と単一 `.html` が残るので、本行はそちらの完成を待つ。** |
| D-65 | **ヘッダーの更新日時が大きい／UTC とローカルの扱い** （利用者の指摘 2026-08-27） —— 「2 段小さくしろ。**更新日時は内部的に UTC で管理**しろ。だが**ヘッダーにはローカルタイムで表示**しろ。ファイルスタンプもローカル時刻だ」 | 字が 2 段小さく、保存は UTC、表示はローカル | ①字の大きさの行を新設 ②`AT-127` は UTC のまま、描く側でローカルへ変換する | ⛔ **未定。**⭐ **保存側は既に UTC である**（`AT-127` が `ISO 8601`・UTC・秒） ✅⭐ **仕様だけを読む体が全 154 行を突き合わせた（2026-08-30）。**⛔ **利用者の指示のうち 2 つに行が無い** —— `AT-127` は `scheduleUpdatedUtc` の**保管が UTC** であることしか言っていない。「ヘッダーには**ローカルタイム**で表示しろ」「**2 段小さく**しろ」に当たる行は `docs/spec` に 0 件である（`ローカル` で引いた 8 件はいずれも保管庫・`file://`・言語設定の話）。 ✅⭐⭐ **仕様書へ入れた（CR-317、版 1.71、2026-08-30）。⛔ 上の「未定」はもう真でない。**⭐ **書いた先は `FR-101` である** —— 足した逐語「**画面に出す時刻は、読む人のローカル時刻とすること（MUST）。保管は UTC のままとすること（MUST）**（利用者の指示 2026-08-27）」と「**更新日時の字の大きさは 表 T-206 の `S-210` が定める係数で決めること（MUST）。px で持ってはならない（MUST NOT）**」。⭐ **表 T-206 に `S-210`（0.75 🔎）を足した。**⛔⛔ **欄 5 が名指していた `AT-127` は、そもそも本行の相手ではなかった** —— あれは日程データの群が動いた刻（図 F-011）であり、**ヘッダーが出しているのはファイルへ最後に書いた時刻（`FR-101`）である。**⚠️ **実装は 2026-08-28 の時点で 3 つとも満たしていた** —— **足りなかったのは仕様書の側だけである。**⚠️ **時刻の綴りそのものは、まだどの行も定めていない** —— `FR-101` は宿主に渡すと明記した（表 T-028 の `IN-2` と同じ立場）。 | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⭐ **保存の半分は既に満たしている。**⛔ **足りないのは「表示はローカル」という規則と、字の大きさの行である。**⚠️ **`FR-061` が自動保存の状態を 3 つ区別すると定めるが、時刻の書式は述べていない** ⛔⛔ **既に実装されていた（実測 2026-08-28）。**⭐ **字の大きさ**: `AUTOSAVE_STATUS_TEXT_SCALE = 0.75`（`@provisional PD-326`）が `font-size:0.75em` として `STYLE.autosaveStatus` に当たっている。⭐ **表示はローカル**: `readableStamp`（`@provisional PD-325`）が `fillAppHeader` から呼ばれ、`YYYY-MM-DD HH:MM:SS`（`T` を空白へ開き `Z` を落とす）。⭐ **保存は UTC のまま** —— `AT-129` の綴りは継ぎ目で触っていない。⇒ **利用者の 3 つの求め（2 段小さく・内部は UTC・表示はローカル）はすべて満たされている。実物で確かめるだけ** | | 2026-08-28 |
| D-66 | **GRS の JSON を 2 回目に保存できない** （利用者の指摘 2026-08-27） —— 「ヘッダーのアイコンをクリックして**同じ名前でも別名でも**保存できるべきだ」 | 2 回目以降も保存でき、上書きと別名の両方ができる | ⭐ **案 B（ヘッダーにファイル名）**（利用者の裁定 2026-08-27） | ⭐ **場所は「ファイル保存時刻の上」**（利用者の裁定 2026-08-27） | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⛔ **これは実害が大きい** —— 作った日程を保存し直せない。⭐ **`IF-3`（`FileStore`）と `FR-026` を読むところから** ⭐⭐ **機構を割った（体の実測 2026-08-27）。**⛔ **「2 回目に保存できない」は偽である。2 回目は保存できている —— 見えないだけである。**⭐ **1 回目の保存が `openedHandle` を残す**（`file-system-access-file-store.ts:675`）。同変数は**書き込み 3 か所・消去 0 か所**で、`{kind:'none'}` を作る唯一の場所は `handle === null`（:337）。⇒ **ページの寿命のあいだ `openedFile.kind === 'none'` は二度と真にならず**、2 回目以降の `IC-2` は `overwriteOpenedFile`（`frame-loop.ts:4138-4142`）へ行き、**選択ダイアログを 0 回開く。**⛔ **そして答えは捨てられる**（:4144 の `if (saving.ok) return`）。⇒ **画素が 1 つも変わらない。**⭐ **台帳の案 ② 「毎回問う」は `DI-5`（表 T-227）が MUST NOT で禁じている。**⭐ **案 ① 「ハンドルを保つ」は `FR-060` が求めるものであり、既に実装されていて正しい。**⇒ ⛔ **保存の道は 1 行も直す必要が無い。**⭐⭐ **「別名で保存」も既に在る —— `IC-2` ではなく `IC-3` である**（`EXPORT_FORMAT_ROWS` の `IO-2` が `.json`、`exportedText` が本文を返す、`ENTRY.exportChooser` が `U-54` を開く。端から端まで結線済み）。⛔⛔ **足りないのは「書けた」と言う権利だけである** —— **表 T-233 は 25 行あり、そのどれも「書き込みが済んだ」ではない**（全行が拒否・失敗・注意）。`FR-076` は同表に無い理由を運ぶことを MUST NOT で禁じる。⚠️ **`FR-076` の `NT-1`〜`NT-8` にも「成功した」という種別が無い** —— `NT-5` は「受け付けたうえで**注意**を伝えるとき」であって成功ではない。⇒ ⛔ **形が 1 つに決まらないので利用者の裁定が要る**（下の「補足」） ✅ **裁定が下りた（2026-08-27）。**⛔ **利用者の逐語**: 「**案B + 場所はファイル保存時刻の上**」。⭐ **ヘッダーに開いているファイル名を出し、ファイル保存時刻の上に置く。**⚠️ **同じ裁定で自動保存そのものが廃止される**（`D-96`）ので、**ヘッダーに残る保存の手がかりは「ファイル名」と「ファイル保存時刻」の 2 つになる。**⇒ ⛔ **`D-96` と 1 つの変更要求で書くこと** —— 別々に書くと、消える `Autosave Status` の位置を前提にした配置を 1 度書いてから捨てることになる ⛔ **着手の時機: 他の不具合を直したあと**（利用者の裁定 2026-08-27） ✅ **CR-280 で `FR-101` として立てた（2026-08-29）。**⭐ **ヘッダーに `Opened File Name`（`U-58`）と `File Saved At`（`U-59`）を、名前を時刻の上にして置いた。**⛔⛔ **落ちていた 1 行を直した** —— `saveHeldDocumentToFile` の `if (saving.ok) return` が `DocumentFileSaving.openedFile` を捨てていた。**それがこの行の正体である。**⭐ いまは書けたファイル名と書いた時刻を控える。⚠️⚠️ **実物では半分しか見ていない** —— 実 DOM で **`File Status` が `flex-direction:column` で名前が第 1 子**（＝時刻の上）であること、未保存のとき「ファイル未保存」と出ることは測った。⛔ **実際にファイルへ保存して名前が出るところは見ていない** —— File System Access の選択ダイアログは自動では押せない。**利用者に見てもらうまで閉じない** | ⛔ **優先度が高い** ⛔⛔ **利用者に問う 1 件。**⭐ **機構**: 上記のとおり保存は成功しており、画面に出る変化が 0 である。⭐ **実測**: `openedHandle` の消去 0 か所 ／ 選択ダイアログ 0 回 ／ 表 T-233 の 25 行に成功が 0 行 ／ `FR-076` の種別 8 つに成功が 0 つ ／ ヘッダーの部品に `fileName` が 0 件。⭐ **選択肢**: **(a)** 表 T-233 に「文書をファイルへ書き込んだ」の行を 1 つ足し、`FR-076` にも成功の種別を 1 つ足す（**行 +1・種別 +1・辞書の語 +2**。⚠️ 8 種別すべてが「うまくいかなかったとき」で揃っている設計を崩す）／ **(b)** 通知を出さず、**ヘッダーに開いているファイル名を出す**（**行 +1 程度・語 +2**。⭐ 保存のたびに名前が変わるので上書きと別名保存が見分けられ、`FR-086` の透かしの名前とも噛み合う。⚠️ ヘッダーの部品に 1 項増える）／ **(c)** 自動保存の刻印（`Autosave Status`）を手動保存でも動かす（**行 0・語 0**。⛔ 自動保存と手動保存が同じ表示を共有し、どちらが起きたか読めなくなる）。⭐ **推奨は (b)** —— **利用者の逐語「同じ名前でも別名でも」は「いまどのファイルに向かっているか」を読めることが要点であり、名前を出せば両方が見える。**⚠️ **覆す費用**: (a) は 表 T-233 と `FR-076` の 2 表、(b) は ヘッダーの表 1 つ、(c) は 0。**どれも規則 06 の分類は `D` 以上**（画面に出るものが増えるため） | ⛔ 未 |
| D-68 | **コマンドパレットの見出し部が無駄** （利用者の指摘 2026-08-27） —— 「ヘッダー部は無用。参考実装のように**右上が掴み点 `::`、その右が最小化切替の `[_]`**」 | 見出しの帯が消え、右上に `::` と `[_]` が並ぶ | ⭐ **案 B（帯は残し、`::` と `[_]` をその帯の右端に置く）**（利用者の裁定 2026-08-28） | ⭐ **`GR-19` と `S-135a` は動かさない。帯が何を載せるかを 1 文で書く** | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⛔ **`GR-19` の行と `S-135a` を動かす。**⭐ **`D-35`（最小化ボタン）と同じ場所の話なので 1 つにまとめられる** ⛔ **体が正しく止まった（2026-08-28）。**⛔⛔ **前に立つ者の指示が誤り** —— 「`S-135a` と `GR-19` を読めば作れる」と書いたが、**その 2 行は逆を向いている。**`GR-19` の内容欄は「**パレットの上端に敷く帯**」、`S-135a` も「上端に敷く 24px」で、備考が「本行は帯の**高さ**だけを定める」と書く。⇒ **右上の掴み点は、その帯ではない。仕様変更が要る。**⭐ **`IC-53` の図形は変えなくてよい** —— 図 F-019 が既に**縦 3 点 × 2 列**（＝ 利用者の言う `::`）を描いている。⚠️ **失うもの**: `GR-19` の理由は「掴めない位置へ置けてしまうと二度と動かせなくなる」。⭐ **体の算術**: いまの帯は幅いっぱい × 24px（一覧を畳んだとき約 282 × 24 ≒ 6.8k px²）、24 × 24 の隅の点は 576px² で **約 8.5%**（一覧を開くと約 4.6%）。⛔⛔ **前に立つ者の申し送りも誤りだった** —— 「構えの語がパレットの頭に在る」は偽で、**実測では `paletteContents` の最後の子、つまり足元に在る。**⇒ **構えの語は動かさなくてよい** ✅ **裁定が下りた（2026-08-28、案 B）。**⛔⛔ **案 A（右上の掴み点へ）は採られなかった。**⭐ **これで `GR-19` も `S-135a` も 1 文字も動かさなくてよい** —— 前に立つ者の当初の指示は「その 2 行を読めば作れる」だったが、実測すると 2 行とも「**パレットの上端に敷く帯**」と定めており、右上の点はそれではなかった。⭐ **案 B なら帯はそのまま残り、`GR-19` の理由（「掴めない位置へ置けてしまうと二度と動かせなくなる」）も生きる** —— 掴み面積は幅いっぱい × 24px のままである。⛔ **書くもの**: **帯が何を載せるかを 1 文**（右端に `::` と、その右に最小化のトグル）。⭐ **`IC-53` の図形は変えなくてよい** —— 図 F-019 が既に**縦 3 点 × 2 列**（＝ `::`）を描いている。⭐ **構えの語は動かさなくてよい** —— 実測で `paletteContents` の**最後の子**（パレットの足元）に在り、頭には無い ⭐ **仕様を書いた（CR-273、版 1.25、案 B）** —— **`GR-19` も `S-135a` も 1 文字も動かしていない。**`FR-053` に「掴み帯の右端に `IC-53`、その右に `IC-75`」の 1 文を足しただけである。 ✅ **実装した（前に立つ者。2026-08-28、案 B）** —— `dom-screen-surface.ts` の `grabBandElement` が帯の中に `IC-75` を置き（`position:absolute; right:0`）、`IC-53` は帯の中央に残る。⛔ **帯そのものは取り去っていない** —— `GR-19` の理由（掴めない位置へ置けると二度と動かせない）が生きている。⭐ **`aria-pressed` で状態を述べた** —— 図形は 1 つなので、見えない人には他に見分ける手がかりが無い。 | ⭐ **`D-35` / `D-36` と 1 つの変更要求にできる** | ⛔ 未 |
| D-82 | **色の項目で、名前と選択欄のあいだの現在色が無用** （利用者の指摘 2026-08-27） | 色見本が二重に出ない | ①`S-188` の色見本を落とす | ⭐ **`FR-006` に「現在の値を操作子の手前に重ねて描いてはならない（MUST NOT）」を足す**（CR-272） | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⚠️ **`S-188` を入れたのは前の巡である。**⛔ **参考実装に在ったから写したが、利用者は要らないと述べている** ⛔ **体が正しく止まった件である。**`S-188` が色見本の一辺と隙間を持っており、**何回描いてよいかを述べる行が無かった。**✅ **仕様を動かした（CR-272）** —— ⛔ **最初 `S-188` の備考に MUST NOT を書いて検査 12 に落ちた**（「規則が値の表に居る。規則は要求のものである」）。⭐ **`FR-006` へ移し、`S-188` には指し先だけを残した。**⛔ **`S-188` の値（14 × 3px）は 1 文字も動かしていない** —— 問題は寸法ではなく回数である。⛔ **`dom-screen-surface.ts:2340-2349` がまだ `<span>` を置いている。実装が要る** ✅ **実装が入った（2026-08-28）。**⭐ **色の操作子の手前に置いていた `<span>` を消し、`propertySwatchStyle()` と `fieldSizes()` の `S-188` の 2 項も落とした。**⛔ **`S-188` の値（14 × 3px）は 1 文字も動かしていない。**⚠️ **体が 1 つ申告した** —— 消したことで `PD-270` の STOP が広がった: **`transparent` も `AT-58` の `null` も、宿主の色操作子では見ることも届くこともできない** | | ⛔ 未 |
| D-91 | **表 T-221 の `LF-1` が、新しくできた曜日の段の刻みの間隔を持たない** （試験の体の指摘 2026-08-27） | 曜日の段の刻みの間隔が `LF-1` に在る | ①`LF-1` に曜日の段の行を足す ②「日の段に従う」と 1 文を足す | ⭐ **案 ②（日の段に従う）**（前に立つ者が決めた 2026-08-27。分類 `A`〜`C`） | `試験待ち` | ⛔ **`CR-268` が作った穴である。**`LF-1` は 年の段 / 年と月の段 / 週の段 / 日の段 の 4 つに間隔を与え、**「これ以外の間隔を採ってはならない（MUST NOT）」**で閉じている。⚠️ **`CR-268` は「`LF-1` は 1 文字も動かさない —— 刻みの間隔は段の組み方と別である」と決めたが、その決定が第 5 の段を無記載のまま残した。**⇒ **間隔の全数を名乗る行が、いま在る段の 1 つを持っていない** ✅ **閉じた（2026-08-27）。**⭐ **`LF-1` を「年の段は 1 年、年と月の段は 1 か月、週の段は 7 日、**日の段と曜日の段は 1 日**」に改めた。**⭐ **理由も同じ行に書いた** —— **同じ軸を 2 段に割ったものだからで、別の間隔にすると「その日のものでない曜日」が日の下に並ぶ。**⛔ **`CR-268`（前に立つ者）が開けた穴を、前に立つ者が閉じた** | ⛔ **前に立つ者が作った穴であり、前に立つ者が閉じる** | ⛔ 未 |
| D-92 | **`FR-017` が、段が 3 つに満たない段階で帯の高さをどう割るかを述べていない** （試験の体の指摘 2026-08-27） | 余る場所がどこへ行くかが決まっている | ①上に寄せる ②下に寄せる ③段を等分に伸ばす | ⭐ **等分**（前に立つ者が決めた 2026-08-27。分類 `A`〜`C`） | `試験待ち` | ⚠️ **`S-2` の備考は段階 4 についてだけ 3 段と述べる。**⛔ **`CR-268` の畳み込みの前は段階 1 と 2 で開いていたが、畳んだ後は段階 1・2・3 の 3 つで開く**（段階 3 は 3 段ぶんの高さの帯に 2 段で立つ）。⭐ **`tests/unit/fr-017-ruler-height-constant.test.ts` が既に「これは主張していない」と記録している** ✅ **閉じた（2026-08-27）。**⭐ **`FR-017` に「段が 3 つに満たない段階では、帯の高さを段の数で等分すること（MUST）」を足した。**⛔ **余りをどこかへ寄せてはならない（MUST NOT）も併記** —— **寄せ先を決めると、その数がどこにも根拠を持たない。**⭐ **等分は数を 1 つも発明しない**（いまの実装がしていることでもある） | ⭐ **`D-91` と同じ変更要求で閉じられる** | ⛔ 未 |
| D-97 | **`Ctrl` ＋ `S` で保存できるようにする** （利用者の裁定 2026-08-27）—— ⛔ **利用者の逐語**: 「**その代わり Ctrl+S で簡単保存してもらう。**」 | `Ctrl` ＋ `S` が、開いているファイルへ上書き保存する | ⭐ **足す必要が無い。既に在る** | ⭐ **在ることを確かめて閉じる** | `試験待ち` | ⛔ **受け取ったその場で台帳へ写した。**⭐ **書き込みの道は既に在る**（`FR-060` ／ `IC-2` ／ `saveDocumentFile`）ので、足すのは**入口 1 つ**である。⚠️ **`DI-5`（MUST）により `FR-060` の経路では問わない**ので、`Ctrl` ＋ `S` も問わずに上書きする ⛔⛔ **足す必要が無かった。前に立つ者が実測して覆した（2026-08-27）。**⭐ **`Ctrl` ＋ `S` は表 T-021 の `SK-11`「保存する」として既に仕様に在る**（`01-04-requirements.md:2488`。「`FR-096` の定めにより `GRS JSON` で書く。上書き先は `FR-060`」）。⭐ **実装も在る** —— `input-command-translator.ts:861` が `s` を `'S'` へ写す。⭐ **`DI-5`（MUST）により `FR-060` の経路では問わない**ので、`Ctrl` ＋ `S` は問わずに上書きする。⇒ **足すのではなく、実物で動くことを確かめるだけである。**⚠️ **本セッションで「無い」と申し送られて実際は在ったものの 5 件目** | ⭐ **`D-96` の代わりに立つ機能である** | 2026-08-27 |
| D-98 | **同じファイルを再読み込みするショートカットを設ける** （利用者の裁定 2026-08-27）—— ⛔ **利用者の逐語**: 「**同じファイルを再読み込みするショートカットキーは Ctrl+ 何にしようか？ R かな？ Reload で。**」 | 開いているファイルを、選択ダイアログ無しで読み直せる | ⭐ **`Ctrl` ＋ `R`（`Reload`）。⛔ ただし守り 2 つを同じ変更要求に入れる**（利用者の裁定 2026-08-27） | ⭐ **①再読込の前に未保存を問う（`QN-5` が既に在る）②離脱のときも警告する（新設）** | `試験待ち` | ⛔ **受け取ったその場で台帳へ写した。**⭐ **狙いは AI 連携の往復である** —— 利用者の逐語「**AI連携するときも、人間が Ctrl+S したあと、AI が読み込む。AI が編集する、人間が再読み込み。**」⇒ **人が編集 → `Ctrl` ＋ `S` → AI が読む → AI が書く → 人が再読み込み**、という 1 本の輪である ✅ **裁定が下りた（2026-08-27）。**⭐ **キーは `Ctrl` ＋ `R`。**⭐ **技術的に可** —— `Ctrl+R` はページ側で止められる組であり、`Ctrl+T` / `Ctrl+W` のような宿主専用キーとは違う。⛔⛔ **ただし自動保存を消すと新しい危険が出るので、守りを 2 つ同じ変更要求に入れる**: **①再読み込みの前に、未保存の編集があれば問う** —— ⭐ **表 T-234 の `QN-5`「未保存の編集を捨てて置き換えるとき」が既に在り、行を足さずに使える。** **②タブを閉じる／ページを離れるときにも同じ警告を出す** —— ⛔ **実測 0 件。仕様にも `src/` にも `beforeunload` は無い。新設が要る。**⭐ **②が無いまま自動保存を消すと、タブの誤操作 1 回で作業が消える** —— **異常終了よりタブを閉じる事故のほうが多い。**⚠️ **`F5` は宿主に残るので、`Ctrl+R` ＝ ファイル再読込 と `F5` ＝ ページ再読込 が並ぶ** —— ②があれば `F5` 側も警告に掛かる。⛔ **代案は退けた**: `Ctrl+Shift+R`（ハードリロード）と `Ctrl+Shift+O`（ブックマーク管理）は宿主が奪い返す。`F9` は空いているが `Reload` の語呂を失う ✅ **CR-280 で両方入った（2026-08-29）。**⭐ **`SK-21`（`Ctrl` ＋ `R`）を 表 T-036 に、`OP-13` を 表 T-024a に置いた** —— 選択ダイアログを開かず、`OP-3` は置き換えに定まり、`OP-4` の確認は掛かる。⭐ **`OpenRoute` に `reopen` を足し、`file-system-access-file-store.ts` が保持中のハンドルから読み直す。**⭐ **守り②は `FR-100` として新設**（`beforeunload`）。⛔ **文言は宿主のものである** —— 近代のブラウザは差し出した文字列を無視するので、辞書に語を持たせず、表 T-234 にも行を作らなかった | ⛔ **`D-96` `D-97` `D-66` と 1 つの塊。⛔ 着手は他の不具合の後** | 2026-08-29 |
| D-102 | **ピン止めが「取り消しの対象」であることを測る試験がどこにも無い** （前に立つ者が実測 2026-08-28）| `CM-68` / `CM-69` の書き込みが、無関係な編集の取り消しで**巻き戻ること**を測る | ①`t-027` の対になる「対象」の側の場面を立てる ②`uf-8-9` の `UN-14` の側へ足す | ⛔ **未定。⭐ ただし仕様は揃っている**（`UN-14`。`CR-277`）| `試験待ち` | ⛔⛔ **`CR-277` が `UN-16` から `UN-14` へ動かした結果、`t-027-outside-the-history` に在った 2 件が「退いた規則」を測るものになったので落とした。**⛔ **代わりを同じ手で書いていない** —— **規則 05 の 7.（自分が動かした値の試験を自分で直さない）に従い、行を動かした当人は書かない。**⭐ **落としたことは黙っていない** —— `t-027` のその場所と `uf-8-9` の両方に、なぜ落ちたか・どこが借りかを注記で残した。⚠️ **同じ理由で前に立つ者が触った試験は本件を含めて 5 ファイルある** —— `t-027`（2 件を落とす）／ `uf-8-9`（`UN-16` の員数 3 → 2）／ `layout-engine`（ダミーの日を 1 日ずらす）／ `schedule-drawing.sws`（`LF-11` と `LF-13` の期待値、`RP-2` の足場）。⛔ **どれも仕様の文言から書き直したが、実装を見ながら書いている以上、仕様だけを読む体による書き直しが要る。** | ⭐ **`D-100` の派生。**⛔ **本行が閉じるまで、ピン止めの「対象」側は緑で守られていない** | ⛔ 未 |
| D-103 | **`IC-75` が掴み帯のどこに描かれるかを測る試験が無い** （前に立つ者が実測 2026-08-28）| `uf-65` が「`IC-75` は `groups` に居らず、帯が担う」ことを述べる | ①`uf-65` に帯の側の場面を立てる ②`uf-72` の `ScreenPart` の側で `IC-75` を押せることを測る | ⛔ **未定。⭐ 仕様は揃っている**（`FR-053`。`CR-273`）| `試験待ち` | ⛔ **`uf-65` の 表 T-109 の写しは 2 種類しか表せなかった** —— 「ボタン」と「ボタンでない」である。**`IC-75` は第 3 種、すなわち「ボタンだが群を持たない」**（`FR-053` が帯に置くため）。⇒ **入口の導出に「群を持つこと」を足した**（表自身の列から導いたので発明ではない）。⛔ **その結果、`IC-75` がどこへ行くのかを述べる場面が 1 つも無くなった。**⚠️ **`display-words.contract` は語が届くことを測るので、そちら経由では守られている** —— `paletteCommands` に帯の 1 件を足し、`tooltips.ts` も同じ 1 件を読むようにした。⛔ **しかし「帯の右端に在ること」は誰も測っていない。** | ⭐ **`D-35` の派生。**⛔ **`D-102` と同じく、行を動かした当人が書くべきでない試験である** | ⛔ 未 |
| D-105 | **ヘルプのフォントが大きすぎ、並び順も違う** （利用者の指摘 2026-08-29） | アイコンの大きさに合わせた字で、**アイコン → 説明 → マウス操作/ショートカット** の順に並ぶ | ①ヘルプの字を `S-138`（図形の一辺）に合わせる ②`FR-036` の並び順の MUST を書き換える | ⛔ **未定。**⚠️ **`FR-036` に「説明・キー・図形の順」と書いたばかりで、利用者はその逆を求めている** ✅⭐ **仕様だけを読む体が全 154 行を突き合わせた（2026-08-30）。**⛔ **仕様書は利用者が否とした順序を定めたままである** —— `FR-036` は「説明・キー・図形の順」と書いており、利用者の 2026-08-29 の指摘（フォントが大きすぎ・並び順が逆）に当たる行が無い。⇒ **`テスト待ち` に居ながら、実装すべき姿が仕様書に無い。** ✅⭐⭐ **決着済み（CR-282、2026-08-29）。⛔ 上の「利用者が否とした順序を定めたままである」はもう真でない。**⭐ **`FR-036` は既に利用者の順である** —— 逐語「**一覧の各項目は、入口の図形・その行の説明・その行の割当の 3 つを、この順に並べること（MUST）**」。⭐ **同要求の ⚠️ が経緯まで記録している** —— 「CR-279 は「説明・キー・図形」の順と定めていた。利用者の指示がそれを覆した」。⭐ 字は `S-203`（0.80 の係数）で決め、px では持たない（`NFR-007`）。⛔ **残るのは実物での 1 画面確認だけである。** | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⭐ **利用者の逐語:「ヘルプのフォントが大きすぎる。アイコンと合わせろ。ヘルプの書式は アイコン 説明 マウス操作/ショートカットキー の順に書け  例:  ⇔  時間軸を拡大  Shift ＋ ホイール」。**⛔⛔ **`CR-279` で `FR-036` に「説明・キー・図形の順、図形は説明の右」と書いた。利用者の指示はその逆である。**⇒ **要求を書き換える。**⚠️ **例の「Shift ＋ ホイール」はキーではなくマウス操作である** —— **表 T-036 の `割当` 欄はキーしか持たず、ホイール操作は 表 T-023 の割当である。**⇒ **両方を出す必要がある。** ✅ **CR-282 で閉じた（2026-08-29）。**⭐ **並びを「図形・説明・割当」へ**（利用者の指示どおり）。⛔ **CR-279 が 1 巡前に書いた「説明・キー・図形」の MUST を、利用者の指示で逆に書き直した。**⭐ **字は `S-203`（0.80）の係数**（px ではない。`NFR-007` が負う WCAG 1.4.4）。⭐⭐ **`Shift ＋ ホイール` を言えるようになった** —— 辞書の `assignments` に `press` の欄を新設し、表 T-023 の 14 行に語を書いた。⛔ **同じ節の `text` は「ホイール → 縦にスクロール」と両方を抱えていたので、`動作` だけに縮めた** —— **実物で二重に出たのを見て直した。**✅ **実物で確認した** —— 111 項目、字は **12.8px**（16 × 0.80）、並びは 図形・説明・割当、**画面に出た行 ID は 0 件** | ⭐ **`D-86` の続き。**⛔ **1 画面に収まるかを実物で測り直すこと**（字を変えると収まりが変わる） | 2026-08-29 |
| D-106 | **Tips のフォントが大きい** （利用者の指摘 2026-08-29） | 2 段階小さい | ①ツールチップの字に係数を当てる | ⛔ **未定** | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⭐ **利用者の逐語:「Tips のフォントを 2 段階落とせ」。**⚠️ **「2 段階」が何の段階かは未確定** —— 表 T-215 の `fontScaleSizes` の段か、`S-197` のような係数か。**問う前に測ること。** ✅ **裁定が下りた（利用者、2026-08-29）** —— ⛔ **利用者の逐語:「Tips の 2 段階とはフォントサイズのことだ。仮に 16pt なら 14pt ぐらいに小さくしろ」。**⇒ ⭐ **「段階」は文字の大きさそのもので、1 段階 ＝ 約 1pt ではなく 1 段階 ＝ 1pt 相当が 2 つ、つまり 16 → 14 の比（約 0.875）である。**⭐ **パネルが `S-197` を宿主の地の文字に掛けているのと同じ形で、係数 1 つを立てるのが素直である。** ✅ **CR-282 で閉じた（2026-08-29）。**⭐ **`S-204`（0.875 ＝ 14 ÷ 16）を新設し、ツールチップに掛けた。**⛔ **`STYLE` の中では書けない** —— 生成される値の塊はその時点で未定義であり、`helpStyle` と同じ形の関数（`tooltipStyle`）にした。✅ **実物で確認した** —— `IC-13` に 3 秒乗せると出るツールチップの字が **14px**（地は 16px）。⚠️ **`S-124` は 3000ms なので、2.5 秒しか待たない測り方では 1 度も出なかった。**⭐ **待ちを伸ばして初めて出た** | ⭐ **`D-105` と同じ面の話ではない**（あちらはヘルプ、こちらはツールチップ） | 2026-08-29 |
| D-115 | **スクロールバーが透明で、つまみも出ていない** （利用者の指摘 2026-08-29） | バーとつまみが見え、掴んで動かせる | ①`Scrollbars`（`U-23`）を描く側を測る | ⛔ **未定。まず機構を測る** | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⭐ **利用者の逐語:「縦スクロールバー、横スクロールバーが透明になっており、かつスクロールする奴(カーソル？)の表示がない」。**⚠️ **「スクロールする奴」＝ つまみ（thumb）である。**⛔ **`FR-051` は「太さは環境の既定の半分」と定め、`WY-3` は「バーを描かなくても場所は残る」と書いている** —— **場所だけ取って描いていない状態が、まさにいま起きている疑い。**⚠️ **`FR-037` はスクロールバーに説明を出すと定めており、入口としては存在が前提になっている。**⛔ **色の行が 表 T-236 に在るかを確かめること。** ✅ **CR-284 で閉じた（2026-08-29）。**⛔⛔ **「透明」ではなく「厚み 0」であった** —— 実測: 横 **1740 × 0px**、縦 **0 × 1009px**。**帯は `rgb(246,247,249)`、つまみは `rgb(91,96,104)` で、色は最初から付いていた。**⭐ **原因**: `FR-051` の「太さは環境の既定の半分とすること（MUST）」に下限が無く、**重ね表示のスクロールバーを持つ環境は既定 0 を返し、0 の半分は 0 になる。**⭐ **`single-html-shell.ts` の注が現象を予告していた** ——「A host with overlay scrollbars measures 0. That is its answer, not a fault」。⛔ **その「not a fault」が誤りであった** —— `FR-037` と 表 T-031 が指せて掴める帯を要求している。✅ **`S-205`（下限 8px）を新設し、`FR-051` に「半分にしてから下限を当てる（MUST）」を書いた。**✅ **実物で確認した** —— 横 **1732 × 8px**、縦 **8 × 977px**、つまみも 8px | ⭐ **`D-32` / `D-33`（ホイールのスクロール）は閉じている。本行は「バーそのものが見えない」で別** | 2026-08-29 |
| D-116 | **プロパティパネルが右へスクロールしていて、項目名が見切れる** （利用者の指摘 2026-08-29、画像付き） | 左端から出て、横スクロールしない | ①パネルの箱に横スクロールが出ている原因を測る ②`S-199` の幅指定が箱を押し広げていないかを測る | ⛔ **未定。まず機構を測る** ⛔⛔ **試験を書く体が正しく拒んだ（2026-09-02）** —— **2 つの MUST が違う幅を言っているので、どちらかを選ぶことになるからである。**⇒ **切り出し先は `D-188`。そちらの裁定が下りるまで、本行に試験は書けない。** | `試験待ち` | ⛔ **未調査。指示を受けたその場で台帳へ写した**（規則 05 の 8.）。⭐ **利用者の逐語:「プロパティーパネルが無駄に右にスクロールされてしまい、見切れてしまう」。**⛔ **画像で確認: 項目名の欄が左へ消え、入力欄だけが見えている。**⚠️⚠️ **`CR-274` で入れた `min-width:<n>em`（`widthInFontSizes`）が箱の幅を超えて押し広げている疑いが濃い** —— **同変更は「値が入る幅を必ず確保する」ものであり、パネルが狭いと横へあふれる。**⛔ **仮説であって実測ではない。**⭐ **`FR-006` は「同じ行に並ぶ操作子をすべてその幅で並べられないときは折り返すこと（MUST）」と既に定めている** —— **折り返しが効いていない可能性がある。** ⛔⛔ **利用者のキャプチャで場所が分かった（2026-08-30）。プロパティパネルではなく 行見出しパネルであった。**⭐ **利用者の逐語**:「**Whole Product って表示するスペースはあるよね？ あらかじめ操作子が出る部分を確保していて、その分が無駄になっている**」。⭐⭐ **実測して裏を取った**: 行の 3 つの操作子は `visibility:hidden` のままフレックス行に居座り、**170px のパネルのうち 48px を占めていた。**⛔⛔ **一方 `S-140`（`FR-085` が名前を切る前に引く場所）は 0px である。**⇒ **切る判断は 158px に対して行われ、実際に与えられたのは 90px で、差はブラウザ自身の省略記号が食べていた。**⛔ **黙って食べていた** —— `isLabelTruncated` は `FR-085` の切り取りを記録するので `false` のままであり、`FR-085` が MUST で求める「全文をツールチップで出す」が発動しない。**名前の残りへ到達する道が 1 本も無かった。**✅ **直した（`d0627d3`）** —— 操作子をフローの外へ出して行の右端に留め、行自身の左右の余白も外したので、名前に与えられる幅が `FR-085` の数（158px）と一致した。**`Whole Product` は 90 → 158 で全部出る。**⚠️ **読む人の字が 24px のときは、`FR-093` の概算が実グリフより短いので、まだ切れる**（別件・D-58 と同根）。⚠️ 3 つの操作子の間隔は裁定待ちの PD-348 | ⛔⛔ **前に立つ者が入れた変更の副作用の疑いがある。最優先で測ること** | ✅ 2026-08-30 |
| D-120 | **CR-280 が赤くした試験 28 件を、仕様だけを読む体が書き直す** （前に立つ者の借り 2026-08-29） | 28 件が緑になり、typecheck も緑になる | ⭐ **仕様だけを読む体に書き直させる。**⛔ **`src/` を読ませない** | ⛔ **前に立つ者が直してはならない**（規則 05 の 7.）—— 自動保存を落としたのは前に立つ者である | `試験待ち` | ⭐ **内訳を実測した**: `uf-23`（10。`BT-3` と `AutosaveCandidate` の場面）／ `uf-47-48`（5。表 T-230 の `RD-5`）／ `uf-71`（3）／ `uf-62`（3。`FR-061` の 3 状態）／ `units.contract`（2。ユニット 71 → 68・公開エントリ 38 → 36）／ `uf-8-9-replace-document`（1。`RD-5`）／ `uf-50`（1。`SK-21` が増えた）／ `uf-41-42`（1。`OpenRoute` が 3 つになった）／ `seams.contract`（1。継ぎ目 9 → 8）／ `display-words.contract`（1。節が 18 になった）。⭐ **どれも「仕様が N と言う」型の員数の主張である** —— 仕様を読めば数は出る。⚠️ **typecheck が赤いのは 3 ファイルだけ**（`uf-23` 24 ／ `uf-62` 8 ／ `uf-8-9-replace-document` 7）。⭐ **機械的な差し替えは前に立つ者が済ませた** —— `ScreenSession` と `AppHeaderItems` の雛形 18 か所と `OpenRoute` の員数 1 か所。**主張には 1 つも触れていない** ⭐ **CR-283 が 1 件足した（合計 33）**: `S-35` の既定を 24 と書いた員数の主張。⭐ **落ち方が実装の正しさを見せている** —— `expected 'xxxx…' to have a length of 24 but got 47`。 ⭐ **CR-285 が 2 件足した（合計 35）**: `Tooltip` の member が 3 つになった員数の主張ほか。⚠️ **一度 107 まで跳ねたが、設計を直して 47 へ戻した** —— 貼り合わせをやめ、`Tooltip.assignment` を `text` の隣に置いた。**62 件は正しかった。** | ⛔ **`D-96` の直後にやる** | ⛔ 未 | ⭐ **CR-281 が 4 件足した（合計 32）**: `display-words.contract`（＋2。節が 19 になり、語の重複の数え上げが 642 → 650）／`uf-47-48-choosers`（2。`IO-6` が選択面を離れ、入口が `IC-3` → `IC-2` になった）。⚠️ **うち 1 件は試験のほうが正しかった** —— 「形式の語は日英で違うはず」という主張に対し、**仕様が「固有の名は訳さない」を書いていなかった。**⭐ **試験を緩めず `FR-096` に書いた。**
| D-124 | **起動時に渡された文書が読めなかったとき、何を告げるかを定める行が無い** （仕様だけを読む体の実測 2026-08-30）| `handedUnreadable` の理由に、表 T-233 の行が 1 つ在る | ①表 T-233 に理由の行を足す（表 T-024a の `OP-11` が `RS-14` を得たのと同じ手当て）| ⛔ **未定。⭐ 小さい** | `試験待ち` | ⭐ **`uf-23` を書き直した体が見つけた。**`FR-062` は「順に負けた出どころをどう扱うかは、その出どころ自身の要求が持つ」と言い、`BT-2` は `FR-087` を指す。⛔ **ところが 表 T-024a の `OP-5` は `FR-023` へ流すだけで、「起動時に渡されたファイルが読めなかった」の行を持たない。**⛔ **表 T-233（通知が運ぶ 15 の理由）にも行が無く、最も近いのは `RS-15`「この理由にまだ行が無い」である。**⇒ 試験は理由の符号と選ばれた席しか主張できず、`BT-1` の側のように要求を逐語で引けない ✅ **仕様も `src/` も閉じた（2026-08-30）。** ⭐ **仕様（CR-299）**: 表 T-024a に **`OP-14`**（起動時に渡された文書が読めなかったとき、黙って捨てずに通知すること（MUST）。表 T-034 の次の順位へ降りること（MUST））、表 T-233 に **`RS-26`**（作法は `NT-1`）。辞書の語も入った。⭐ **`src/`**: `single-html-shell.ts:318` の `STARTUP_NOTICE_REASON.handedUnreadable` が **`RS-15`（「この理由にまだ行が無い」の受け皿）を指していた**のを `RS-26` へ。`frame-loop.ts` の `NoticeReason` と `NOTICE_MANNER_OF_REASON` にも足した。⚠️ **実物では作れない** —— `single-html-shell.ts:682` が `handed: { kind: 'none' }` を直に置いており、`BT-2` の渡し手がまだ配線されていない（同所の STOP がそう書いている）。⇒ **出荷物にこの通知が出る道はまだ無い。**⭐ **型が証明している** —— `StartupNoticeCode` → `StartupNoticeReason` は `Record` なので、`handedUnreadable` が `RS-26` 以外を指すと `tsc` が通らない | ⭐ `BT-1` 側は `FR-067` が「黙って捨てずに通知すること（MUST）」を持っており、そちらが手本になる | ⚠️ 2026-08-30（実物では作れない。BT-2 の渡し手が未配線）|
| D-126 | **`FR-101` の「名前を時刻の上に置くこと（MUST）」を、誰も主張できない** （仕様だけを読む体の実測 2026-08-30）| 並び順を運ぶ欄が在るか、その MUST を描く側の要求が持つ | ①`AppHeaderItems` に順序の欄を足す ②`UF-71`（描く側）の規則として持ち、`UF-62` は主張しない | ⛔ **未定。⭐ ②のほうが小さい** | `試験待ち`| ⭐ **`uf-62` を書き直した体が見つけた。**CR-280 が `FR-061` を退けて `FR-101` を置き、`Opened File Name`（`U-58`）と `File Saved At`（`U-59`）が 表 T-076 の `EP-1` に入った。⛔ **`FR-101` は「名前を時刻の上に（MUST）」と定めるが、`AppHeaderItems` は順序を運ぶ欄を持たない。**⇒ `uf-62` はその MUST について何も主張していない ✅⭐ **既に閉じていた（前に立つ者の実測 2026-08-29）** —— **本行は古かった。**CR-299 が案②（描く側が負う）を採り、表 T-075 の `UF-71` の 責務 に 「**`FR-101` の「名前を時刻の上に置く」を満たすのは本ユニットである**」と明記している（実測: `05-07-design.md` に `FR-101` が 2 か所、`UF-62` と `UF-71`）。⛔ **記述の側に順序の欄は作っていない**（`R2.2` で退けた）。⇒ **本行の期待「並び順を運ぶ欄が在るか、その MUST を描く側の要求が持つ」は後者で満たされている。**⚠️ **残るのは試験だけである** —— その MUST を主張する試験はまだ 0 件。| ⚠️ **描く側（`UF-71`）で測れる可能性が高い** | ⛔ 未 |
| D-130 | ⛔⛔ **プロパティパネルで確定した値が、文書に届かない** （実装の体が申告し、前に立つ者が実測で裏を取った 2026-08-30）| `FR-006` が定めるとおり、表 T-016 の読み取り専用でない項目を編集すると文書が変わる | ①`readFieldCommit` から `writeDocument` までのどこで落ちているかを測る | ⛔ **未定。まず機構を測る** | `試験待ち` | ⛔⛔ **実測（前に立つ者、`scratch/probe/panel-commit.mjs`、出荷物、実ポインタ）**: バーを選んで `P` でパネルを開き、`PR-1`（`name`。`_assets/tbl-property-items.md:20` が「バーに描くラベル」と定める欄）の値を `Survey phase` から `RenamedByProbe` に打ち替えて `Enter`。⇒ **画面の文字は `Survey phase` のまま 1 個、`RenamedByProbe` は 0 個。**⛔ **`FR-006` の「編集できるようにすること」が満たされていない。**⚠️ **`WS-2` の拒否ではない** —— 知らせは 0 件で、`Agent API` を止めた状態でも同じ。⚠️ **CR-293 より前から同じ挙動である**（本巡の後退ではない）。⭐ **CR-293 で `readFieldCommit` の経路が `WS-2` に拒まれないよう手当てした後も残っている**ので、落ちているのはそこより先である ✅ **直った（`12083a6`、2026-08-30）。落ちていたのは 1 行である。** ⭐⭐ **機構（体が測り、前に立つ者が実物で裏を取った）**: `SK-19` が入力中の素の `Enter` に `acted` を返す → `acted` が `isBrowserDefaultStopped` を立てる → `dom-input-source.ts` の keydown が `passive:false` なので `preventDefault()` を呼ぶ → ⛔⛔ **その `preventDefault` こそが、宿主に `change` を起こさせないものである。**⇒ `fieldCommit` を書く者が `onFieldChange` しか居らず、`readFieldCommit` は永久に `null`、`spendFieldCommit` の `if (commit === null) return` が毎回取られていた。⭐ **単独でも確かめた**: 素の `<input>` は `Enter` で `change` を起こすが、keydown で `preventDefault` すると 0 回になる。⛔⛔ **道具が、欄を確定させていた唯一のものをブラウザから取り上げ、代わりを置いていなかった。**✅ **パネル自身が `SK-19` の `Enter` を確定するようにした**（`DomInputSource` の window 聴き手より先に走るので、同じ押下で `spendFieldCommit` が読める）。⚠️ **宿主が離脱時に起こす重複の `change` は落とす** —— 同じ値を 2 回書くと取り消しが 2 段になり、`FR-031` と `UN-3` が禁じている（`Ctrl`＋`Z` 1 回で戻ることを確認）。⭐⭐ **実物の前 → 後（前に立つ者の実測）**: `RenamedByProbe` が画面に **0 → 1**、`Survey phase` が **1 → 0**。⭐ **表 T-016 の行ごとに測った** —— 編集できる 16 行のうち、**届いていなかったのは `PR-1` と `PR-2` の 2 行だけ**であり、どちらも「打ち込んで `Enter` で確定する」欄である。残る 14 行は、値を変えるキーそのもので 宿主が `change` を起こすので前から届いていた。⚠️ **閉じていない 3 つ**: ①宿主の `change` で確定する経路は **1 happening 遅れて**届く（`actualDuration` 138 → 139 が、次のポインタ移動まで 138 のまま）—— ⛔ **表 T-078 が契機の全数を持ち、「本表に無い契機でフレームを起こしてはならない（MUST NOT）」と定めるので、閉じてよいか自体が裁定である。**②`PR-12` の色の操作子 2 つは宿主の色選択が開くので headless では駆動できず、**壊れているとも動くとも示せていない。**③⛔ **日程を押しても欄から抜けない**（下の D-131） | ⛔⛔ **これが直るまで、プロパティパネルは事実上の読み取り専用である** —— 表 T-016 の項目すべてに掛かる疑いがあり、`PR-1` 1 つの話ではない可能性が高い。**全項目を測ること** | ✅ 2026-08-30 |
| D-133 | ⛔ **プロパティパネルが古いまま残ることがある** （実装の体の実測 2026-08-30）| 欄を確定したら、その結果がパネルにも出る | ①`dom-screen-surface.ts` の `changed('propertiesPanel')` と `lastKeys = keys` の順を測る | ✅⭐⭐ **本欄の下にある「未定」は古い。読み飛ばすこと**（2026-09-01 の棚卸し）—— ⛔⛔ **本欄は書き足しで伸びるので、裁定が下りても冒頭の「未定」がそのまま残る。**⭐ **裁定は下りており、下の記述がそれを述べている。** ⛔ **未定。まず機構を測る**| `試験待ち` | ⛔ **機構（体の申告）**: `dom-screen-surface.ts:4247` の `changed('propertiesPanel')` と `:4324` の `lastKeys = keys` が食い違う。⚠️ **`Enter` は欄を放さない**ので、確定した frame では `changed` が真でも `isFieldHeld` が真であり、**描き直さないのに `lastKeys` だけ進む。**⇒ 以後の frame は `changed` が偽になり、**欄を放してもパネルは古いまま残る。**⭐ **体は最初これを「`Enter` が届いていない」と誤読した** —— 確定した担当者が画面に出てこないためである。⇒ ⚠️ **同じ誤読が今後も起きる。**⛔ **触るべきは `:4247` と `:4324` の 2 か所。**⚠️ **前に立つ者はまだ実物で確かめていない** ✅ **直った（`7f59397`、2026-08-30）。機構は申告どおりであった。** ⭐ `lastKeys` が、**パネルが描くのを見送った記述**を記録していた（操作子を掴んでいたため）。⇒ 次の frame は `changed` を偽と答え、**確定前の読みを永久に持ち続けた。**✅ **`drawnKeys` を別に持ち、見送った部位については「最後に実際に描いた記述」を憶えるようにした。**⭐⭐ **実物の前 → 後**: 実績期間を 10 で確定して欄を放すと、パネルが **`完了率91`（古い・誤り）→ `完了率7`**。⚠️ **前に立つ者はこの 1 件を実物で測り直していない**（体の harness による測定である） | ⭐ **文書には届いている**（台帳 D-130 で確かめた）。**出ないのはパネルの側だけ** | ⚠️ 2026-08-30（体の測定）|
| D-145 | ⛔ **表示語が規則のセルと食い違っても、検査 28 本が 1 本も捕まえない** （前に立つ者の実測 2026-08-30）| 語と、その語が説明している規則が食い違ったら検査が赤くなる | ①`_source/display-words.json` の各行と、その行 ID を持つ表のセルを突き合わせる検査を足す | ⛔ **未定** | `未検討` | ⭐ **実測（2026-08-30）** —— `_source/display-words.json` の `MK-13` は 「対象で意味が決まる。…**タスク＝プロパティパネル**、行見出し＝行名」と刷っていた。⛔ **ところが同じ `MK-13` の規則は 2026-08-27 の裁定で逐語「プロパティパネルを開く経路を本行に置いてはならない（MUST NOT）」であった。**⚠️ **3 日間、語と規則が正面から矛盾したまま、検査 28 本も試験 5041 件も全部緑で通った。**⭐ **見つけたのは、CR-304 のために `MK-13` を読んだ体である。**⚠️ **本件は CR-304 の適用で「たまたま正しくなる」** —— `MK-13` がダブルクリックでパネルを開く行になるからである。⛔ **だが捕まえられなかったこと自体は直っていない。**⭐ **これは同じ形の 4 度目である** —— `ARMED_BY_ENTRY` が古い行 ID で止まっていた件、`PALETTE_ENTRY_ROWS_GROUPED` が 34 個で止まっていた件、表 T-202 の 3 つに入口が無かった件（D-128）| ⚠️ **語は生成物ではなく原稿である**（`_source/` は原稿と生成物の両方を持ち、ファイルごとに自分の素性を名乗る）。⛔ **だから生成器では直らない。突き合わせる検査が要る** | ⛔ 未 |
| D-151 | ⚠️ **1 日ぶんのドラッグが、描画領域の幅しだいで 1 日足りなくなる**（体の実測 2026-08-30。CR-304 の配線がむき出しにした）| ドラッグした日数だけ動く。**画面の幅で結果が変わらない** | ①`dateAtX` の丸めが境目でどちらへ落ちるかを決める | ⛔ **未定** | `未検討` | ⭐ **実測**: CR-304 でパネルが押しただけでは開かなくなり、描画領域が **280px 広がった。**すると `pxPerDay` が変わり、`fr-052-t-023d-picture-while-held.test.ts` の 「1 日動かす」と「4 日ぶん戻す」が **1 日足りなく**なった（3 日の場合は通る）。⛔⛔ **原因は幅の変更ではない** —— `from.x + n × pxPerDay` が日の境目の**わずかに手前**に落ち、`dateAtX` が切り捨てるからである。⚠️ **境目そのものは前から在り、幅が変わったことで乗る側が入れ替わっただけである。**⇒ ⭐ **「試験が古い」のではなく、「日の境目でどちらへ落ちるか」を仕様が定めていない可能性がある。**⛔ **試験を緩めて閉じるな。**⚠️ **台帳 D-138（縦のパンが行の隙間を飛ばす）と同じ家族である** —— あちらも量子化であり、1 回の長いドラッグでは隠れていた | ✅⭐⭐ **前に立つ者が仕様を引いた（2026-08-30）—— 行は在る。**表 T-023d のあとの結び（`01-04-requirements.md:2356`）が逐語で「**`GR-1` / `GR-2` の日数は、ポインタの下の日から求めること（MUST）**」「**いずれも 1 日単位に四捨五入する**」「⛔ **ドラッグの開始にしきい値を設けてはならない（MUST NOT）—— 掴み点は時間軸の上の「ある日」そのものであり、画素の感度というものが存在しない**」と定めている。⇒ ⭐⭐ **これは製品の欠陥ではなく、試験の作り方の問題である見込みが強い** —— **日の左端ちょうどを狙うと、浮動小数の 1 ulp でどちらの日にも落ちうる。**⛔ **正しい狙い方は「その日の真ん中」である。**⚠️ **これは主張を緩めることではない** —— 入力の作り方を仕様どおりにすることである。✅⭐⭐ **仕様だけを読む体が最後まで詰めた（2026-08-30）—— 答えは「両方」であった。**⭐ **試験の側の誤り**: 日の**ちょうど端**を狙っていた。⛔ **そこは「ポインタの下の日」の境目であると同時に「四捨五入」の引き分けでもあり、浮動小数の 1 ulp でどちらにも落ちる。**実測: 端では 1 日の運びが 0 日、4 日の運びが 3 日になり、2 日と 3 日は正しく落ちた。⭐ **1 日の 4 分の 1 だけ内側**を狙う形に直して両方とも緑（⛔ **真ん中の 0.5 にしない** —— そこが四捨五入の引き分けである）。⛔⛔ **仕様の側の誤り —— 表 T-023d の結びが自分と矛盾している。**「**ポインタの下の日から求めること（MUST）**」は切り捨てを言い、「**いずれも 1 日単位に四捨五入する**」は丸めを言う。**境目のわずかに手前で、製品は前の日に落ちる（切り捨てている）。**⇒ ⛔ **どちらか 1 文を落とす裁定が要る。**体は 2 文目を採って**わざと赤い 1 件**を置いた（⭐ **緩めていない**）| ⛔ 未 |
| D-152 | ⛔ **同じ `Enter` を繰り返すと、誰も編集していない取り消しの段が積まれる**（体の実測 2026-08-30）| 値が変わっていなければ何も書かない | ①`Enter` の聞き手が、始めた値と同じ値なら命令を組まないようにする | ✅ **閉じた（2026-08-30）** | `試験待ち` | ⭐ **実測**: 名称の欄で `Enter` を 2 度押すと `setTaskName` が **2 度**書かれる。⛔ **表 T-028 の `IN-6` が逐語で「始めた値と同じ値を書いてはならない」と定めている。**⚠️ **`FR-031` と 表 T-027 の `UN-3`（取り消しの段）にも触れる** —— **誰も編集していない段が積まれると、`Ctrl+Z` が 1 度では戻らない。**⭐ **`SK-19` の 2 段目（`Enter` 2 度でパネルが閉じる）を配線する過程で見つかった** —— ⛔ **2 段目が立たない理由そのものでもある**（`Enter` の聞き手が欄の保持を解放しない）| ✅⭐⭐ **閉じた（2026-08-30）。二重に閉じている** —— ①`Enter` の聞き手が「焦点を得たときの値と同じなら命令を組まない」ようにした ②`Enter` が欄の保持を解放するので、2 度目の `Enter` はそもそも確定するものを見つけない。⛔⛔ **前の体の見立て「`hasUnsettledTextEntry()` を偽にすればよい」は誤りであった** —— **同じ答えを 表 T-028 の `IN-5a` が単文字キーの門にも使っており、偽にすると名称の欄で `p` と打っただけでパレットが開く。**⭐ **正しい直しは「聞き手が保持を解放する」ほうで、これは仕様の逐語どおりである** | ⛔ 未 |
| D-154 | ⛔⛔ **行の操作子の並びと振る舞いが、実物で読めない**（利用者の指摘 2026-08-30、`FR-102` の記録つき）| 操作子を見ただけで、何個あって何をするかが読める | ①次の巡の冒頭で、絵と実測で一枚に整理してから問い直す | ⭐ **利用者の逐語**「そもそもタスクグループの操作子とその挙動が違和感だらけだ。次のセッションの冒頭で再整理させろ」 ✅⭐⭐ **一枚を出し、`.html` の見本を渡した（2026-08-30）。**⛔ **実測で 4 つの食い違いを名指した** —— ①同じ図形（山 2 つ）が「この行自身を畳む」と「すべての行を畳む」の 2 意 ②開く／畳むの左右が行と頭で逆 ③畳む図形が左右の山と上下の山に割れている ④隣り合う行で員数が 2 と 5 に変わる。 ⛔⛔ **実測でさらに 2 つの穴が出た** —— **行を増やす入口も、並べ替える入口も、表 T-109 の 85 個に 1 つも無い**（`UN-14` は「行の追加」を取り消せる操作として挙げているのに）。⇒ `PD-399` / `PD-400` を立てた。 ✅ **利用者の裁定（2026-08-30）**: **①スクロールして見えなくなった行は数えない（仕様のままでよい）**⚠️ **引継書の「いまは数える」は実測で再現しなかった** **②増やす入口は `[+]`、`IC-82` の `×` と対、増やす先は配下** **③「ロジックを全体的に再整理しろ」**。⇒ **2 軸（範囲 × 動作）に並べ直した。** ⛔ **残る裁定 3 つ**: 並べ替えの手（`PD-400`）／足した行の位置と名前（`PD-399`）／図形の 1 家族（`PD-401`）。 ✅⭐⭐ **第三者の体 2 つに検めさせ、欠陥 17 件を出して全部直した（2026-08-30）。**⛔ **書いた者は自分の欠陥を 1 つも見つけられなかった。**⭐ **実物を叩く体**（約 2,700 手）と**仕様だけを読む体**を、互いに独立に当てた。 ⛔⛔ **重い 2 件は、どちらも「追従が悪い」を直した修正そのものが生んだ。****①縦ドラッグが振動した** —— **親の境をまたぐ 1 歩は 102px 進むのに行の高さは 34px** しかなく、どちら側へ置いても「半分より遠い」ので進む↔戻るを繰り返した。**実測 pointermove 34 回で書き換え 783 回** ⇒ **「ポインタが今どの空き場所の上に在るか」を座標で引く形に変え、33 回**。**②ピン止めした行が、画面上まったく動かないまま木の中を移動した** —— 留めた行は先頭へ上げて描かれるのに、追従が描かれた箱を測っていた。**実測ポインタ +200px で行 +0px、木では親を 2 つまたいだ** ⇒ **掴ませない**（`PD-400` ⑤ の推しを採った）。 ⭐ **2 つの体が独立に同じ結論へ達したものが 2 件** —— `[+]` が深さ上限を見ておらず 9 段まで作れたこと、および隠すとピン止めが消えること（`FR-098` の MUST NOT）。 ⭐ **残る 13 件**: 右ドラッグが隠した行を戻す／横ドラッグ中に行を消すと例外／打ちかけの行が残る／`↑` `↓` が描かれていない兄弟に効く／トグル型で「全部畳む」が押せなくなる／頭の記録が実際と違う／図形が 2 つの入口で同一／設定を変えてもドラッグが止まらない／名前が HTML として解釈される／空の木で占有が −1%／薄い入口がタブ順に残る／既定の名が重複／`OLD` 家族が頭の図形を誤って再現。 ✅ **直したあとの実測**: 4000 手で違反 0 件（行数・重複・深さ上限・畳みの漏れ・上下で段が動くこと）、追従のずれは縦横とも **0px**、例外 0 件。 ⭐ **潰さなくてよいと分かったもの**: 薄い⇄効くの対応（1800 回押して両方向 0 件）、畳みの状態の健全さ（約 2,700 手で反例なし）、上下で段が動かないこと（親を 2 つまたぐドラッグでも成立）。 ✅⭐ **見本を `previous-project-result/11-row-controls/` へ置いた（利用者の指示 2026-08-30）** —— `row-controls-sample.html`（1 ファイルで動く）と `README.md`。⭐ **README は 2 軸の表・入口を持たない操作 3 つ・裁定済み 7 件・未決 5 件・仕様の側に要る仕事 7 件を持つ。** ✅⭐⭐ **裁定 5 件が下り、仕様書へ入れた（CR-313、版 1.67。2026-08-30）。**①畳む前の形を覚えない（`HR-1a`）②頭の「すべて畳む」は最も浅い段も畳む（`HR-2` / `HF-12`）③折り畳みの図形を 1 家族へ描き直す（裁定は `RC-13` に記した。⛔ **図形はまだ起こしていない**）④行を足す入口（`HR-8` / `HF-14`。末子・空の名で立てる）⑤行を掴んで動かす（`GR-20` / `HF-15` / `RS-36` 〜 `RS-39` / `S-208`）。⭐⭐ **段送りの刻みには設定値を新設しなかった** —— **`S-37` と同じであることを規則に書いた**（利用者の裁定「刻みも現状のままでよい」）。⛔ **数を 2 か所に置くと必ず離れていく**（実測）。⚠️ **残る仕事**: 図 F-019 に図形 4 つ、表 T-109 に入口の行、`HR-6` の入口と `U-29` の廃止。 ✅⭐ **残る仕事の記述を 2026-09-01 に測り直した** —— ⛔ **「図 F-019 に図形 4 つ」は古い。****同図は 89 図形を持ち、表 T-109 の 89 行とちょうど一致している（欠け 0）。**⇒ **残るのは `HR-6` の入口（台帳 D-155）と `U-29` の廃止だけである。** ✅⭐⭐ **裁定は下りている（2026-08-30、5 件）。⛔ 塞がっていない** —— **残るのは実装だけである。**⚠️ **2026-09-01 の棚卸しで、本行を「裁定待ち」として利用者へ出しかけた。誤りである。** | `実装待ち` | ⭐ **記録が実測で示した** —— 利用者は **40 秒のあいだ `IC-58` / `IC-59` / `IC-77` / `IC-60` / `IC-82` / `IC-74` / `IC-78` の上を延々と往復し、押したのは 3 回だけ**であった（`seq 220`〜`1253`）。⛔ **押した 3 回はどれも正しく効いている**（`IC-59` で行 68→66、もう一度で 66→55、`IC-74` で 55→68）。⇒ ⛔⛔ **効かないのではなく、読めないのである。**⚠️ **前に立つ者は、この巡で「構え」という語を二重に使って混乱を足した** —— 仕様の「構え」は**パレットが図形を構えること**（表 T-023b の `AR-2` / `AR-3`）であり、「操作子がいま効く状態」の意味で使ってはならない | ⭐ **裁定待ちは `PD-397` が持つ。**`PD-396`（ピン止めの見え方）も同じ一枚で片づける | ⛔ 未 |
| D-155 | ⛔⛔ **行（`TaskGroup`）を隠す入口が、どこにも無い**（前に立つ者の実測 2026-08-30、利用者の指摘から） | 選んだ行を隠せて、戻せる | ①表 T-109 に「隠す」の行を足す ②`∧`（1 本）に隠す役を与える | ✅⭐⭐ **裁定は下りている。以下に残る古い記述は、その裁定より前に書かれたものである** —— ⛔ **頭だけ読んで「塞がっている」と判ずるな。**⭐ **かつての検討（決着前）**（`PD-402`）。⭐ **利用者の案（2026-08-30）**: **1 本の `∧` を「この行を隠す（親へ 1 歩畳み込む）」とし、戻すのは親の `∨`。**⇒ **目のアイコンも `Hidden Group Tab` も要らなくなる** ✅⭐⭐ **裁定（2026-08-30）。1 本の `∧` に隠す役を与える。**⭐ **これは 5 日前に止まっていた作業を解く** —— 版 1.47（CR-294）は 「`HR-6` の入口は足していない —— **そのタブは実装に 1 つも無い**。**入口だけ足すと、隠したら二度と戻せない行ができる**」と書いて見送っていた。⇒ **戻す先を親の `∨` にすれば、タブが要らなくなり、入口を置ける。**⛔ **ただし `U-29` を廃止するなら、生きた参照 3 か所（Chapter 1 の散文／`HR-6` 本文／`EP-4`）を直す仕事が付く。** ✅⭐⭐ **裁定は下りている（2026-08-30）。⛔ 塞がっていない** —— **「1 本の `∧` に隠す役を与え、戻すのは親の `∨`」。**⇒ **残るのは実装と `U-29` の廃止（生きた参照 3 か所）だけである。**⚠️ **2026-09-01 の棚卸しで誤って「裁定待ち」として利用者へ出しかけた。**| `実装待ち` | ⭐ **実測**: 表 T-015 の `HR-6` は「行を隠す。親の非表示グループタブから戻せること」と定め、`AT-57` に `TaskGroup.isHidden` が在り、`U-29` に `Hidden Group Tab` が在り、`UN-14` は「畳みと**非表示**の変更」を取り消せると書く。⛔ **それなのに 表 T-109 の 85 個のアイコンに「隠す」入口が 1 つも無い。**⚠️ **`AT-57` の備考は「**戻す**入口は `HR-6` が持つ」と書いており、戻す道しか無い。**⇒ ⛔ **入口の無い操作の 2 つ目である**（1 つ目は `D-154` の「足す」） | ⭐ **実働サンプルで確かめた** —— `∧` に隠す役を与えると、目のアイコンも非表示グループタブも不要になった | ⛔ 未 |
| D-156 | ⛔⛔ **行（`TaskGroup`）を並べ替える入口も、階層を変える入口も、どこにも無い**（前に立つ者の実測 2026-08-30、利用者の問いから） | 行の順を変えられ、配下ごと段を上げ下げできる | ①ドラッグ ②`↑` `↓` の操作子を 2 つ足す ③ドラッグ＋`PR-15` | ⛔ **未定**（`PD-400`）。⭐ **利用者の裁定の骨（2026-08-30）**: **ドラッグ。上下で位置、左右で階層。軸は上下か左右のどちらか一方のみ。階層を変えるときは配下もつれて動く。**⛔ **細目は未裁定** ✅⭐⭐ **「階層を変えると GRS のデータの階層も変わるか」への答え（利用者の問い 2026-08-30。実測）。**⛔⛔ **この製品は階層を 2 つ持つ。****軸 A ＝ WBS**（`Task.wbsParentUid`。`AT-25`。由来は `Consume`）／**軸 B ＝ `TaskGroup`**（`TaskGroup.parentId`。`AT-52`。由来は `GRS`）。⭐ **`FR-004` の RATIONALE の逐語**「**この非対称が要である。** WBS は外部 WBS マスタと共有する構造であり、**`TaskGroup` は本ツール専用の器で export しない。**」⇒ ⭐ **ドラッグが書き換えるのは `TaskGroup.parentId` と `AT-55 order` だけである。****`Task.wbsParentUid` は動かないので、`DV-5` が導く `Task/OutlineLevel` も動かない。**⚠️ **プロパティパネルの `PR-15` は逆に軸 A を変える。**⛔ **同じ「階層」という語で 2 つを呼んではならない**（表 T-006b の `A-3` / `A-4`）。 ✅⭐⭐ **裁定 5 件が下り、仕様書へ入れた（CR-313、版 1.67。2026-08-30）。**①畳む前の形を覚えない（`HR-1a`）②頭の「すべて畳む」は最も浅い段も畳む（`HR-2` / `HF-12`）③折り畳みの図形を 1 家族へ描き直す（裁定は `RC-13` に記した。⛔ **図形はまだ起こしていない**）④行を足す入口（`HR-8` / `HF-14`。末子・空の名で立てる）⑤行を掴んで動かす（`GR-20` / `HF-15` / `RS-36` 〜 `RS-39` / `S-208`）。⭐⭐ **段送りの刻みには設定値を新設しなかった** —— **`S-37` と同じであることを規則に書いた**（利用者の裁定「刻みも現状のままでよい」）。⛔ **数を 2 か所に置くと必ず離れていく**（実測）。⚠️ **残る仕事**: 図 F-019 に図形 4 つ、表 T-109 に入口の行、`HR-6` の入口と `U-29` の廃止。 ✅⭐ **仕様書へ入れた（CR-313、版 1.67）。**⛔ **場所**: 表 T-023d の `GR-20`（行見出しパネルの行の掴み代）／表 T-051 の `HF-15`（掴んで動かす）／表 T-233 の `RS-36` 〜 `RS-39`（拒みの理由）／`_assets/tbl-settings.md` の 表 T-206 の `S-208`（軸が決まる距離 ＝ 6px 🔎）。⭐ **逐語**（`HF-15`）「**軸を 1 本に固定すること（MUST）。掴んでから最初に閾値を超えた向きで軸が決まり、離すまで変わらないこと（MUST）**」「**上下は位置を変え、段を変えてはならない（MUST NOT）**」「**右へ 1 歩はすぐ上の兄弟の末子になること、左へ 1 歩は親の次の兄弟になること（MUST）**」「**どちらの向きでも、その行の配下ごと動かすこと（MUST）**」「**段送りの刻みは 表 T-201 の `S-37` と同じとすること（MUST）**」。⭐ **`GR-20` の逐語**「⛔ **ピン止めしている行は掴めないこと（MUST NOT）**」。⛔⛔ **実装はまだ無い。**⚠️ **そのため試験が 7 件赤くなった** —— 表 T-023d の全数を突き合わせる契約試験 1 件と、`GR-20` の表示語を画面に出す契約試験 6 件である。⭐ **仕様が実装より先に進んだ形であり、隠していない。** | `実装待ち` | ⭐ **実測**: 表 T-109 の 85 個に並べ替えも階層変更も無い。⛔ **表 T-023d（掴みの全数表）に行見出しパネルの行を掴む行が 1 つも無い**ので、ドラッグを採るなら行を足す裁定が要る。⭐ **取り消しは片方だけ在る** —— `UN-4`「階層の変更」は在るが、**同じ親の中の並べ替えは `UN-4` にも `UN-14` にも無い。**⭐ **深さの頭打ちは在る** —— `S-125`（`maxGroupDepth`、既定 5・範囲 3〜8）。⛔ **`↑` `↓` 案は寸法で落ちる** —— 実測で操作子 8 個・174px となり、パネル 170px に**入らない**（102%） | ⭐ **実働サンプルで 3 案とも触れるようにした** | ⛔ 未 |
| D-157 | ⛔⛔ **畳んだ行の配下が「開いている」状態のまま残り、親を戻すと全階層が一度に開く**（利用者の指摘 2026-08-30、実働サンプルで再現） | 畳んだ行の配下も畳まれた状態になり、戻すときは 1 階層ずつ開く | ①畳む操作は必ず配下ごと状態を書き換える ②描画のたびに祖先を辿って導出する | ✅⭐⭐ **裁定は下りている。以下に残る古い記述は、その裁定より前に書かれたものである** —— ⛔ **頭だけ読んで「塞がっている」と判ずるな。**⭐ **かつての検討（決着前）**（`PD-403`）。⭐ **①を推す** —— 状態が 1 つの意味しか持たなくなり、**「畳まれた親の下に開いている行が在る」という状態そのものが作れなくなる** ✅⭐⭐ **裁定を受けた（利用者、2026-09-01）**「**サンプルを見たのか？ サンプルと比較しろ**」⇒ **見本（`previous-project-result/11-row-controls/row-controls-sample.html`）の型に従う。**⭐⭐ **見本の逐語**: 「`ONE state per row: folded -- this row is folded into its parent and not drawn`」「`ONE STEP DOWN: only the direct children come back; grandchildren stay folded`」⇒ **行ごとに「私は親へ畳み込まれている」という旗を 1 つだけ持つ。****開くと直下の子だけが戻り、孫は自分の旗を保つ** ⇒ **畳む前の形が自然に残る。**⛔ **前に立つ者が挙げた 2 案（配下を書き換える／祖先を辿る）は、どちらも的外れであった。**| `仕様待ち` | ⭐ **実測（実働サンプル）**: 3 つの行を隠したあと内部の状態を出すと `✕ Whole Product` の下に `● Phase Bars` `● Phase Gates` が残っており、**見えていないだけで「開いている」ままであった。**⇒ 親を戻した瞬間に全階層が展開された。✅ **直したあと 324 手を打って、畳まれた親の下に「開いている」行が残った回数は 0。**⚠️ **出荷ビルドに同じ漏れが在るかは未測定** —— あちらは `HR-5` / `HR-3` / `HR-4` の畳みだけを持ち、「隠す」を持たないので、**同型とは限らない。次の巡で測ること** | ⭐ **利用者の逐語**「データの持ち方がおかしくないか？ 過去の値を引きずるような構成にしていないか？」—— **そのとおりであった** | ⛔ 未 |
| D-166 | ⛔⛔ **`RS-30` の辞書の語が、いまの場面と正反対のことを告げていた**（仕様だけを読む体が発見 2026-08-31）—— **表 T-233 の `RS-30` は 2026-08-31 に「その行は畳まれておらず、隠れている子も無い」へ書き換わったのに、`display-words.json` は「この行は既に畳まれています」のままであった。**⇒ **効かない `IC-90` を押した人は、畳まれていないことが理由なのに「既に畳まれています」と告げられていた。** | 語が行の場面と一致する | ①辞書の 3 つの欄（ja / en / 次の手段）を書き換える | ⭐⭐ **決着（CR-322 の直し、版 1.76 の続き）。**`RS-30` は「この行は畳まれておらず、隠れている子もありません」／次の手段「配下を畳むか、行を隠してから、もう一度押してください」へ。⭐ **`RS-29` も同じ形で古かったので直した** —— `HF-11` はもう配下を畳まないのに、語は「折り畳む配下のタスクグループがありません」であった | `試験待ち` | ⛔⛔ **機械検査は緑のまま通していた** —— **検査は「その行の語が画面へ届いたか」を見るのであって、「その語が真か」を見ない。**⭐ **表の行の文言を変えたら、辞書も必ず読み直すこと。** | ⚠️ **表 T-037 の結びが既に警告していた** —— 「⛔ **行を足すときは、辞書の原稿にも項を足すこと（MUST）**」。⭐ **足すときは守られたが、書き換えるときのことは書かれていなかった** | ⛔ 未 |
| D-167 | ⛔⛔ **`HR-6` の 2 文が両立していなかった**（仕様だけを読む体が発見 2026-08-31）—— **前に立つ者が同日に書いた「その行の配下を畳んだ状態にすること（MUST）」と「戻るのはこの行だけになる」が、同時に真になりえない。**⇒ **配下だけを畳むと、戻したとき直下の子が付いてくる。** | 2 文が両立し、見本と同じ絵になる | ①見本の実測に照らす ②`HR-6` を直す ③実装と試験を追わせる | ⭐⭐ **決着。**⭐ **見本の実測が答えを持っていた** —— **`Phone App` を隠して親の `∨` を押すと、戻るのは `Phone App` だけで、その 2 つの子は畳まれたままである。**⇒ **隠す操作は「その行と、その配下」を畳む。**`HR-6` を「**その行と、その配下を畳んだ状態にすること（MUST）**」へ書き換えた | `実装待ち` | ⭐ **前に立つ者が 1 日で 2 度、同じ行を書き損じた** —— **1 度目は MUST を書き落とし（本巡の 1 つ前の直し）、2 度目は射程を 1 階層外した。**⛔ **どちらも仕様だけを読む体が捕まえた。** | ⚠️ **実装は通知の体が同じファイルを持っているあいだ待っている** | ⛔ 未 |

| D-171 | ⛔⛔ **描いたタスクが 1 つも削除できない**（利用者の指摘 2026-09-01、`FR-102` の操作記録つき）—— **どのアイテムにも当たらない場所へドラッグして作ったタスクは、名前を持たないまま作られ、同時にその行の名前の導出元にされる。**⇒ **消そうとすると「名前も導出元も無い行」ができてしまうので拒まれ、束ごと捨てられる。** | タスクを選んで削除すると消える | ①導出元が名前を持たないときの行の名前を決める ②拒まずに既定の名前で確定させる | ⭐⭐ **仕様書へ入れた（2026-09-01）。書いた先は `01-04-requirements.md` の `FR-032`** —— 逐語「**導出元の `Task` が名前を持たないときは、行の名前を既定の名前に確定させること（MUST）。名前が無いことを理由に削除を拒んではならない（MUST NOT）**」。⭐ **語は `FR-038` の辞書が持ち、要求には綴らない。**⚠️ **利用者が同じ巡で `No name` を別件（全消し後の自動生成）に採用しており、同じ語を当てる。** | `実装待ち` | ⭐⭐ **機構を突き止め、再現した（体の実測 2026-09-01）** —— `edit-task.ts` の `deleteTask` が `group.label ?? source?.name ?? null` を求め、どちらも `null` なので `CM-7` / `IV-8` で拒否。`document-change-plan.ts` が「束の 1 つが拒まれたら束ごと捨てる」ので文書は 1 バイトも動かず、`RS-10`「受け付けられない操作が含まれていたので、まとめて取りやめました」が 1 件立つ。⭐ **操作記録の `act=changeDocument doc=same notices=1` と一致する。**⛔ **同じ判断が `edit-task-group.ts` の `CM-27` にもあり、2 か所を一緒に直す。** | ⛔⛔ **`Ctrl+A` は関係ない** —— **1 つだけ選んでも消せない。**⭐ **古くからの欠陥で、この巡の変更とは無関係**（拒否の門と、それを必ず踏ませる行の作り方が 2026-08-17 の同じコミットで入っている）。⛔ **既存の試験が見逃していたのは、題材が全部「名前を持つタスク」だったからである。** | ⛔ 未 |
| D-172 | ⛔ **保存したファイルに拡張子が付かない**（利用者の指摘 2026-09-01）—— **利用者が手で `.json` を付け直した。** | 選んだ形式の拡張子で保存される | ①提案する名前を確かめる ②宿主のダイアログに形式を伝える | ⭐⭐ **仕様書へ入れた（2026-09-01）。書いた先は `01-04-requirements.md` の `FR-096`** —— 逐語「**書き換えられるのは名前であって、拡張子ではない。書き出した先の名前が、選んだ行の拡張子で終わることを保証すること（MUST）**」。⛔ **媒体型を表に持たせてはならない（MUST NOT）とし、持ち場を Framework 層と定めた** —— **外の規格が決めている値であり、宿主の作法であって製品の値ではない。** | `実装待ち` | ⭐⭐ **提案する名前は正しかった（実測 2026-09-01）** —— 宿主のダイアログを差し替えて受け取る値を読むと `{"suggestedName":"Three-Year Product Plan.json"}` であり、**拡張子は正しく渡っている。**⛔ **渡していないのは「その拡張子の形式である」という指定のほうで、これが無いとダイアログは拡張子を飾りとして扱い、守らない。**⚠️ **1 度「付いている」と読み違えた** —— 保存済みの 2 ファイルに `.json` が在ったからだが、**それは利用者が手で付けたものであった。** | ⛔⛔ **この行の下から、より重い欠陥が 1 つ出た** —— **書き出しの 5 形式のうち `.html` / `.svg` / `.png` の 3 つは、押しても何も書かずに黙って戻る**（実測: `exportedText` が 3 つとも `null` を返し、呼び手がそこで戻る）。⭐ **`.png` を描く部品自体は書かれており、どこからも呼ばれていないだけである。**⇒ **台帳 D-173 として別に立てる。** | ⛔ 未 |
| D-173 | ⛔⛔ **書き出しの 5 形式のうち 3 つは、押しても何も保存されない**（前に立つ者の実測 2026-09-01）—— **`.html` / `.svg` / `.png` を選ぶと、ファイルも通知も 0 件で戻る。** | 並んでいる形式は、押せば書き出される | ①書けない形式を面から外す ②書けるようにする | ✅⭐⭐ **裁定は下りている。以下に残る古い記述は、その裁定より前に書かれたものである** —— ⛔ **頭だけ読んで「塞がっている」と判ずるな。**⭐ **かつての検討（決着前）** —— **利用者は「順番は `.json` `.xml` `.html` `.svg` `.png`」と指示しており、並べ替えだけなら今日できるが、3 つは死んだままである。**⇒ **並べるだけか、中身も作るかを問う。** ✅⭐⭐ **裁定を受けた（利用者、2026-09-01）**「**次回ですべて作る。並行で実装できるだろ？**」⇒ **`.svg` と単一 `.html` の書き手を次の巡で作る。**⭐ **並行してよい**（利用者の言）。⭐ **`.png` は 2026-09-01 に配線済みで、実測 256,367 バイトの本物の PNG を書き出している。**⚠️ **`.svg` の下ごしらえは在る** —— **絵を組み立てる部品も、告げる理由の行（`RS-22`）も実在する。残るのは「数が 2 種類あるのに運べる数が 1 つ」の解決だけ。** ⭐ **告げる語は `D-186` が持つ（利用者の裁定 2026-09-02）** —— **書けるようになるまでのあいだ、`RS-15`（「もう一度行ってください」）に落ちるのは嘘である。** | `実装待ち` | ⭐ **実測**: `exportedText` は `grsJson` と `mspdi` だけが本文を返し、`svg` / `png` / `singleHtml` は `null` を返す。呼び手は `null` を見てそのまま戻るので、**書かないことも告げない。**⛔ **`FR-029` は「押されたときに限り、行えない理由を通知すること（MUST）」と定めるので、いまの状態はその MUST に反している。**⭐ **`.png` は描く部品（`canvas-rasterizer`）が実在し、`src/` のどこからも呼ばれていないだけである** —— **配線だけで足りる見込み。** | ⚠️ **選択面には 5 つとも並んでおり、押せる** —— **「効かない入口」ではなく「効いたように見えて何も起きない入口」である。** | ⛔ 未 |
| D-175 | ⭐ **タスクグループが 1 つも無い文書に、タスクやマイルストーンを置けてしまう**（利用者の指摘 2026-09-01）—— **どのタスクグループにも入らないタスクができる。** | タスクグループを全部消したら、`L1` の `No name` が 1 つ自動でできる | ①全消しを検出して既定の行を 1 つ作る | ⭐⭐ **裁定は下りている（利用者、2026-09-01）。**⛔ **問い直さない。**⭐ **利用者の逐語**「**すべてのタスクグループを削除した時は、自動的に L1 で No name のタスクグループを作れ**」⭐ **語は台帳 D-171 が辞書へ入れた `No name` をそのまま使う**（同じ概念であり、2 つ目を作らない）。⚠️ **まだ仕様書に 1 文字も書いていない。** ✅⭐ **仕様を書いた（2026-09-02）** —— **表 T-050（削除の連鎖）の直下に不変条件を 1 つ置いた**: 「文書は `TaskGroup` を必ず 1 つ以上持つこと（MUST）。ある操作の結果として行が 0 になるときは、その操作の一部として、深さ `L1` の行を 1 つ作ること（MUST）。名前は `FR-038` の辞書の `defaultNames` の `row` の語とすること（MUST）」（`docs/spec/01-04-requirements.md`、表 T-050 の直下）。⭐⭐ **削除だけの規則にしなかった** —— **取り込み（`FR-023`）・置き換え（表 T-024a の `OP-3`）・やり直しも行を 0 にしうる。**⛔ **取り消しの単位は分けない（MUST NOT）** —— 1 回で消した行が戻る。 | `実装待ち` | ⛔ **未調査。指示を受けたその場で写した**（規則 05 の 8.）。⭐ **`No name` は既に辞書に在る**（D-171 で `defaultNames` の枠を起こした）。⛔ **英語のまま出す** —— **利用者の裁定 2026-09-01「日本語モードでも No name でよい」。** | ⚠️ **どの時点で作るかが未調査** —— **最後の行を消した直後か、次に何かを置こうとした時か。**⛔ **取り消しとの関係も未調査** —— **自動で作った行が取り消しで消えるのかどうか。** | ⛔ 未 |
| D-180 | ⭐ **タスクグループ名を編集する経路が無い**（利用者の指示 2026-09-01） | 名前をダブルクリックするとプロパティパネルが開き、名前の編集になる。`Enter` で閉じる | ①ダブルクリックを拾う ②パネルを開いて名前の欄へ移す ③`Enter` で閉じる | ⭐⭐ **裁定は下りている（利用者、2026-09-01）。**⛔ **問い直さない。**⭐ **利用者の逐語**「**タスクグループ名をダブルクリックしたら、プロパティパネルを開き、タスクグループ名編集モードとせよ。編集後 Enter でプロパティーパネルを閉じれ**」「**(タスク名編集モードと同様の動作)**」⚠️ **まだ仕様書に 1 文字も書いていない。** | `仕様待ち` | ⛔ **未調査。指示を受けたその場で写した**（規則 05 の 8.）。⭐ **既にタスク名の編集が同じ形で在る** —— **利用者が「同様の動作」と添えているので、そちらを写す。** | ⚠️ **`Enter` でパネルを閉じるのは、いまの `Esc` で閉じる規則と別である** —— **両立するかを確かめること。** | ⛔ 未 |
| D-184 | ⛔ **書き出しの面の英語の見出しが `Export Chooser` のままである**（前に立つ者の実測 2026-09-01、出荷ビルド）—— **日本語だけ「ファイルに保存」に直り、英語は手段の名を名乗ったままになっている。** | 英語の見出しも、目的を名乗る語であること | ①`Save to File` ②`Export` ③そのまま | ✅⭐⭐ **裁定が下りた（利用者、2026-09-02）: 案① `Save to File`。**⭐ **理由は、日本語「ファイルに保存」と同じ目的を名乗るからである。**⛔ **`Chooser` は手段の名であり、面が何をする所かを言っていない。**⇒ **直す先は `docs/spec/_source/display-words.json` の `surfaces` の当該の語（`{"ja": "ファイルに保存", "en": "Export Chooser"}`）である。**⚠️ **原稿を直したら `npm run gen` を走らせること** —— `src/adapter/screen-renderer/display-words.json` は生成物である。 ✅⭐ **仕様を書いた（`CR-325`、2026-09-02）** —— **`docs/spec/_source/display-words.json` の `surfaces` を `Save to File` へ。**⭐ **`npm run gen` 済み（辞書 411 語、検査 17 本 OK）。** ⭐ **規則を持つのは `FR-038`である** —— 「画面に刷る語は言語ごとの辞書 1 つに持つ」。⚠️ **表を持たない語である** —— **面の見出しは `surfaces` の項であって、行 ID を持つ行ではない。** | `実装待ち` | ⛔ **未着手。裁定を受けたその場で台帳へ写した**（規則 05 の 8.）。 | ⭐ **`D-118`（書き出しの面が行 ID を出していた）と同じ面の話だが、あちらは日本語の語の不在で、本行は英語の語の選び方である。** | ⛔ 未 |
| D-188 | ⛔⛔ **行の名前に与える幅を、仕様の 2 つの MUST が違う値で言っている**（仕様だけを読む体の実測 2026-09-02、出荷ビルド）—— **`FR-085` は使える幅を `S-79`（170）− 深さ×`S-37`（16）− `S-140`（0）の 3 項ちょうどと定めているが、表 T-023d の `GR-20`（MUST）は 16px の掴み代を「インデントの後、名前のすぐ前」に置き、`HF-15`（MUST）がそれを常に描くと定めている。**⚠️ **`FR-085` の式にはその項が無い。** | 名前に与える幅を定める MUST が 1 つだけであること | ①`FR-085` の式に `GR-20` の項を足す ②掴み代を名前のすぐ前ではなく、名前の上へ重ねて描くと定める（操作子について `HF-6` が既に採っている形である） | ✅⭐ **裁定が下りた。⛔ 頭だけ読むな** —— **決着は本欄の末尾に在る。**⭐ **写した当時（決着前）**: 実測を受けたその場で台帳へ写した（規則 05 の 8.）。⭐ **体はどちらかを選ばずに拒んだ。その判断は正しい。** ✅⭐⭐ **裁定が下りた（利用者、2026-09-02）: 案① `FR-085` の式に `GR-20` の項を足す。**⇒ **使える幅 ＝ `S-79` − 深さ×`S-37` − `S-140` − 掴み代（`GR-20`、16px）− その隔たり（4px）。**⭐ **実物は既にそう描いており、掴み代も名前もどちらも見える**（実測 2026-09-02: 深さ 1 → 134px、2 → 118px、3 → 102px）。⇒ **直すのは仕様の式であって、コードではない。**⛔ **案②（掴み代を名前の上へ重ねる）は落ちた。** ✅⭐ **仕様を書いた（`CR-330` の内容、2026-09-02）** —— **`FR-085`（`docs/spec/01-04-requirements.md`）の式に、行の掛み代（表 T-023d の `GR-20`）に確保した場所とその隔たりを引く項を足した。**⭐ **あわせて「掛み代を名前の上へ重ねて描いてはならない（MUST NOT）」を置いた** —— **落ちた案②を二度と拾わないためである。**⛔ **`src/` は 1 行も直していない** —— **実物が既に正しく、誤っていたのは式のほうであった。** | `試験待ち` | ⭐ **実測（2026-09-02、出荷ビルド、1920×1080）**: 深さ 1 → 134px、深さ 2 → 118px、深さ 3 → 102px。**どれも `FR-085` の値よりちょうど 20px 少ない（16px の掴み代 ＋ 4px の隔たり）。**⚠️ **`S-189`（42%）も見たが決め手にならない** —— **原稿は「何の 42%」かを言っていない。** | ⭐ **`D-116` から切り出した** —— **あちらの修理自体は実測で合格している（幅 90 → 158）。****本行は、そのあとに残った「では仕様はどちらを言っているのか」である。** | ⛔ 未 |
| D-189 | ⛔ **`.png` を描く canvas が拒んだときの 3 つの理由に、表 T-233 の行が 1 つも無い**（体の実測 2026-09-02）—— **`unsupported`（この環境ではそもそも描けない）／`tooLarge`／`rasterFailed` の 3 つが、いずれも `RS-15`（行の無い理由の落ち先）に落ちている。** | 3 つそれぞれが、自分の次の一歩を持つ理由の行を持つこと | ①表 T-233 に 3 行足す ②3 つを 1 行にまとめる | ⛔ **未定。実測を受けたその場で台帳へ写した**（規則 05 の 8.）。⛔ **②は採れない見込み** —— **`rasterizer.ts` が「この 3 つは次の一歩を共有しない」と自分で述べている。**⭐ **`frame-loop.ts` の同所の注が、3 つの次の一歩を既に書き出している**: `unsupported` は SVG（`IO-3`）へ、`tooLarge` は `S-82` の小さい方へ、`rasterFailed` は再試行。 | `裁定待ち` | ⭐ **`D-186` から切り出した** —— **あちらは `.svg` と 単一 `.html` の道であり、`RS-40` で閉じた。****本行は `.png` の道であって、別である。** | ⚠️ **`RS-3` では代われない** —— **同行は「ファイルが書けなかった」であり（正は `LM-14`）、「別の保存先を選べ」と告げることになる。canvas が拒んだことの出口ではない。** | ⛔ 未 |
| D-191 | ⛔ **当たり判定の 30 × 20px が、どの画素を起点に置かれるのかを、どの行も言っていない**（仕様だけを読む体の指摘 2026-09-02）—— **表 T-206 の `S-93` は大きさ（30 × 20px）を、表 T-023d は場所（`GR-9`「翌稼働日に 1 日ぶん」、`GR-18`「図形の上」）を持つが、その場所の左端・中央・列のどれに 30px を掛けるかを書いた行が 1 つも無い。** | 当たり判定の起点を定める行が 1 つ在ること | ①表 T-023d に起点の欄を足す ②表 T-206 の `S-93` の備考に起点を書く | ⛔ **未定。指摘を受けたその場で台帳へ写した**（規則 05 の 8.）。⛔ **体は試験を書くことを拒んだ。その判断は正しい** —— **左詰め `[x, x+30]`・中央 `[x−15, x+15]`・列の中央のどれも、書かれている行を全部満たす。**⚠️ **しかも `S-93`（30）> `S-180`（12）なので、どの読みでもインクは中に入る。**⇒ **弱めずに書ける場合が 1 つも無い。**⭐ **`FR-043` は自分でこれを除いている** —— ⛔「当たり判定は本段の対象ではない（MUST NOT）」。 | `裁定待ち` | ⭐ **体は `docs/spec` の「当たり判定」を全部 grep して確かめた。**⚠️ **1 文で済む** —— **どの行に置くかだけが決まればよい。** | ⭐ **`D-187`（描く幅）から出た。あちらは閉じている。**⛔ **本行は描画ではなく当たり判定であり、別である。** | ⛔ 未 |
| D-192 | ⛔ **マイルストーンのダミー（`GR-18`）の左端を、2 つの読みが半図形ぶん離して置いている**（仕様だけを読む体の指摘 2026-09-02）—— **`FR-043` の「日の列の左端に揃えること（MUST）」は一般の規則だが、同要求のマイルストーンの段は「位置と当たり判定」を 表 T-023d の `GR-18` に渡しており、その場所は「図形の上」、さらに 表 T-221 の `LF-10` はその図形を「`start` の位置」に中央で置く。** | マイルストーンのダミーの左端を定める読みが 1 つであること | ①`GR-18` に左端の規則を書く ②`FR-043` の揃えの MUST がマイルストーンにも当たると明記する | ⛔ **未定。指摘を受けたその場で台帳へ写した**（規則 05 の 8.）。⛔ **体は `GR-18` の左端を主張する試験を書かず、幅だけを主張した。その判断は正しい。**⚠️ **実測では実装は左詰めにしている** —— **ただしそれは実装であって、行ではない。** | `裁定待ち` | ⭐ **実測（2026-09-02、3 つの倍率）**: **マイルストーンの図形の中央とダミーの左端の差は 0（±0.01 は印字の丸め）。**⇒ **`placed.x + placed.width/2 === xFromDay(その日)` が成り立っている。** | ⭐ **`D-187` から出た。⛔ あちらは幅の話で閉じており、本行は左端の話である。** | ⛔ 未 |
| D-193 | ⛔ **最後の行より下の空き地に注記を置いたとき、それが何に結び付くのかを、どの行も言っていない**（体の指摘 2026-09-02）—— **`PD-4` は「何にも当たらなかった地面では構えているものを作る」と言うが、`FR-019`（MUST）は注記の位置を「日付と行の識別子で」持たせ、`RL-18` は `anchorGroupId` を `TaskGroup` に向けている。**⇒ **行を持たない箱は、文書には在るのに画面から届く手が無い値になる。** | 行の無い地面を押したときに何が起きるかが、1 つの行で決まっていること | ①`FR-019` に「指す `TaskGroup` が無いとき」の節を足す ②表 T-233 に理由を 1 行足して拒む | ⛔ **未定。指摘を受けたその場で台帳へ写した**（規則 05 の 8.）。⛔ **体は何も置かない形にして止めた。その判断は正しい。**⛔⛔ **`FR-001` の答え（「行を 1 つ作って」）は借りられない** —— **同要求はその `Task` を新しい行の導出元にすると続けており、注記のために作った行は名前も導出元も持たないことになる。`AT-54` と `FR-058` がそれを禁じている。**⚠️ **表 T-233 にも、ここでの拒否が運べる理由が無い。** | `裁定待ち` | ⭐ **実測**: `scrollToTheGround` のあと (250, 1040) は最後の行の帯（y=635 で終わる）より下であった。⇒ **`D-06` の錨の押す場所を、行が覆う空き地へ移した。**⚠️ **空き地の見つけ方も変えた** —— **行の帯の中では最上位の要素は帯自身の矩形なので、`elementFromPoint` で「素の `<svg>` か」を問うと行の上の点が全部除かれる。**⇒ **`PD-5` が空き地に与える素の矢印を、何も構えずに読む形にした。** | ⭐ **`AR-6`（ハイライトボックス）も同じ理由で未実装のままである。****その旨は `input-command-translator.ts` に書いてある。** | ⛔ 未 |
| D-194 | ⛔ **行を 1 つ足して名前を付けると、行見出しパネルの見え位置が中身より下へ外れ、祖先の行が描かれなくなる**（前に立つ者の実測 2026-09-02、出荷ビルド）—— **読む人が上へスクロールするまで戻らない。**⚠️ **背の高い行（タスクを載せた 148px の行）が立っているときに起きる。** | 行に名前を付けたあとも、その行の祖先が描かれたままであること | ①名前を確定したあと、見え位置を中身の中へ戻す ②新しい行を描いた時点で、その行が見えるところまで寄せる | ⛔⛔ **未定。⚠️ 本行は 2026-09-02 に書き直した。最初の書き方が誤っていたからである。**⛔ **当初は「ほかの行が消える」と書いた。消えていない。**⭐ **`app.rows()` が読むのは `[data-depth]`、すなわち**描かれている窓**であって文書ではない。****パネルを上へスクロールすると祖先はそのまま戻る** —— **実装した体が測り、前に立つ者が測り直して確かめた。**⇒ ⛔ **前に立つ者が、描かれている数を文書の数と読み違えたのが原因である。** ⭐⭐ **以前から在る欠陥であることも測ってある** —— **体が `document-change-plan.ts` を `git show HEAD:` の版へ戻して同じ操作を行い、同じ結果を得た。**⇒ ⛔ **`D-175` の不変条件が持ち込んだものではない。**⭐ **行 0 から始めると起きない** —— **引き金は背の高い残り行である。** | `未検討` | ⭐ **実測（2026-09-02、出荷ビルド）**: 1 行まで消す → `add` → 「A Child」と命名 ⇒ 描かれるのは `["1:A Child"]` だけ。**パネルを上へスクロールすると `["0:Quality And Rele…","1:A Child"]` に戻る。**⇒ **文書は 2 行を持ったままである。**⚠️ **1963px のパネルに約 204px ぶんの行しか無いのに、見え位置が中身の下へ park している。** | ⭐ **`npm run parity` はこれとは別の理由で落ちていた** —— **`emptyTheApp` が「数が 1」で止まり、残ったのが見本の 148px の行だったためである。****いまは「画面の行の集合が変わらなくなるまで」押す不動点で止める形になり、25/25 に戻った。** | ⛔ 未 |
| D-195 | ⛔ **透かしのインクが 1 つも描かれていない**（体の実測 2026-09-02、出荷ビルド）—— **`[data-role="Watermark"]` は 0 個、薄い `opacity` を持つ要素も 0 個、`rotate` の変換も 0 個、ISO-8601 の UTC 刻印に合う `<text>` も 0 個。**⚠️ **`svg-renderer.ts` に透かしの実装が無く、`EP-7` への参照も無い。** | `FR-020` の定めるとおり、開いた者の名前と UTC の刻印が `Row Area` へ斜めに薄く重なること | ①`svg-renderer.ts` に描く ②`EP-7` の書き出し側と揃える | ⛔ **未定。実測を受けたその場で台帳へ写した**（規則 05 の 8.）。⭐ **`D-147`（隠す門）は閉じたが、それは設定値が動くことを示せただけである。**⛔ **インクが無いので、`watermarkVisible` が `false` になっても目に見える変化は 0 である。** | `未検討` | ⭐ **数えて確かめてある。矩形ではなく走査で数えた。**⚠️ **`FR-020` の描画の半分であり、`D-147` の対象外であった。** | ⭐ **`D-147` を実装した体が、射程外として手を触れずに報告した。その判断は正しい。** | ⛔ 未 |
| D-196 | ⛔ **消した透かしを出し直す入口が無い**（体の実測 2026-09-02）—— **表 T-109 は `IC-41` に片方向しか与えておらず、「出す」入口をどこにも置いていない。**⚠️ **`watermarkVisible` が既に `false` のとき `IC-41` を押すと、門がもう一度立つ。** | 透かしを出し直す道が 1 つ在ること。⚠️ `FR-020` は出し直す側を問うてはならないと定める | ①表 T-109 に「出す」入口を足す ②`IC-41` を、消えているときだけ問わずに戻す入口とする | ⛔ **未定。実測を受けたその場で台帳へ写した**（規則 05 の 8.）。⛔ **体は門を無条件に立てた。`FR-020` の MUST が無条件だからであり、その判断は正しい。**⚠️ **`PD-418` の隣の問いである。** | `裁定待ち` | ⭐ **`FR-020` は「透かしを出し直す側は問わないこと（MUST）」と定めており、いまの実装はそこだけ満たしていない** —— **出し直す道そのものが無いからである。** | ⭐ **`D-147` から出た。⛔ あちらは消す側の門であり、閉じている。** | ⛔ 未 |
