# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ
4. §1 を読む
```

⛔⛔ **`--start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな。**
⚠️ **前の巡はそれで基準の行を消し、取り戻すのに `git show HEAD:` で古い版を出して
道具に測り直させた**（⛔ 手で書き戻すのは禁じられている）。

**サブエージェントは許可されている**（利用者、2026-08-31／09-02／09-03／09-04 に再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**

⭐⭐ **前の巡の知見は規則へ下ろしてある**（利用者の指示 2026-09-03）。
⛔ **本書は毎巡書き換わるので、ここに置いた学びは 1 巡で消える。**

---

## 0.5 **始点（前の巡の終わりに実測）**

```
単体   153 ファイル / 6552 件 —— 全緑 / 1 skip
e2e    tests/system  72 件全部緑・1.8 分
同型   npm run parity —— 50/50 一致・説明の無い食い違い 0
検査   check.sh 33 本 ALL GREEN ／ gen:check 17 本 OK ／ typecheck 清潔
台帳   252 行。残件 18
```

**残件 18 の内訳（⭐ 5 つの束で読む。`tools/ledger_metrics.py` が刷る）:**

```
未仕分け  6   D-244（説明の借り）／ D-246 D-247 D-248 D-249 D-250
仕様の穴  2   ⛔ D-251 D-252 —— どちらも利用者の裁定を待つ。§1.1
製品の仕事 6  実装待ち 4 ／ 試験待ち 2
検証の借り 4  緑だが出荷ビルドをまだ押していない
```

⛔ **「不具合」と呼んでよいのは `製品の仕事` だけである。**

---

## 1. **前の巡が何をしたか —— ⛔ 数より、なぜ減ったかを読め**

**`実装待ち` は 15 から 4 へ減った。**⚠️ **半分は「直した」ではない。**

```
15 → 4 の内訳
  4 件  ⭐ 最初から塞がっていなかった（実測で反証）—— D-15 D-154 D-219 D-238
  1 件  ⭐ 台帳の主張そのものが偽だった（D-124。OP-14 が最初から全文を持っていた）
  6 件  ⭐ 実際に実装した —— D-228 D-232 D-237 D-240 D-241 D-243
  4 件  ⛔ 残っている —— D-145 D-195 D-239 D-242
```

⭐⭐ **いちばん効いた 1 手は「反証しに行かせた」ことである**（規則 05 の 7）。
**4 体に「確かめてこい」ではなく「反証してこい」と書いた。**
⚠️ **15 行のうち 5 行は塞がっていなかった** —— **前の巡の 11/13 と同じ比である。**
⛔ **これは 2 巡続けて起きている。台帳の「実装待ち」を信じて着手するな。**

### 1.1 ⛔⛔ **利用者の裁定を待っている 2 件（§8 に問いの全文）**

```
D-251  畳んだ親の下に足した行が見えない
       ⛔ HF-14 は詳しさの段（FR-018）だけを定め、人が畳んだ場合を言っていない
       ⭐ 実装の体は自分から STOP を出した。「何も発明していない」
D-252  表 T-034 の BT-2 を渡す口が実装に無い
       ⛔ single-html-shell.ts:920 が handed: { kind: 'none' } を打ち込んでいる
       ⚠️ 配線する必要が本当に在るかから問うこと
```

### 1.2 **残っている `実装待ち` 4 件**

| 行 | 何が要るか | ⭐ 前の巡に分かったこと |
|---|---|---|
| `D-195` | 透かしを描く | ⛔ **4 値は生成された定数として `svg-renderer.ts` に届いている**（`WATERMARK_MARKS` と `S-223`）。⛔ **読み手が 1 人も居ないので、ビルドから消えている。**⇒ **要るのは描く側だけである** |
| `D-239` | 項目名を英語で刷る | ⛔ **仕様の半分は入った**（理由が 表 T-016 の前文へ移り、`FR-038` の「同表が持つ」が真になった）。⚠️ **実装は逆を向いている** —— **パネルは 名称 / 色 / 高さ と日本語で刷る。**⛔ **画面の見た目が変わるので、先に問うこと（§8）** |
| `D-242` | 操作子の大きさを固定する | ⭐ **実測が揃っている** —— 文字 16px で 20×24、32px で 24×48。**コマンドパレットの入口は両方で 26×24。**⭐ **狙う数も仕様に在る**（`S-138` 16 ＋ `S-141` 4）。⛔ **原因は `dom-screen-surface.ts` の `ROW_CONTROL_PAD_EM` と `font: inherit`** |
| `D-145` | 共変検査を広げる | ⭐ **守られているのは 表 T-233 だけ。守られていない群は 12、324 対**（台帳に全数）。⛔ **`display-words.contract.test.ts` は片道であると自分で書いている** |

### 1.3 **`試験待ち` 2 件と `実測待ち` 4 件**

```
試験待ち  D-237 D-243   ⭐ 実装は入って開発サーバで確認済。⛔ 仕様だけを読む体に試験を書かせろ
実測待ち  D-231 D-240 D-241 D-245   ⛔ 出荷ビルドを押していないだけである
```

---

## 2. **体の使い方 —— ⛔ 前の巡の実測だけを置く**

### ⭐⭐ **「反証してこい」と書け。「確かめてこい」ではない**

⛔ **判定を 4 つに分けさせろ**: **塞がっていない ／ 半分だけ ／ 本当に塞がっている ／ 別の行と同じ原因。**
⭐ **これで当て推量が混ざる余地が無くなる。**

### ⭐ **前の巡で効いた 4 つ（数つき）**

```
反証しに行かせる            15 行のうち 5 行が塞がっていなかった
実装 ‖ 仕様だけを読む試験    体が前に立つ者の誤りを 12 件返した（下）
「落ちることを確かめろ」      反転 6/8/5/4/1 件を見せて戻させた
錨（PINNED）                ⭐ D-232 が「思いがけず通った」と自分で鳴った
```

### ⛔⛔ **前に立つ者は 12 間違え、12 とも体が捕まえた**

⚠️ **前の巡は 8 であった。増えている。**⭐ **訂正は成果である。受けたら台帳と条文の両方を直せ。**

```
数を引き算で書いた        語 537 → 536 と書いた。⭐ 1 行が 2 対を持つので 535 であった
要求の番号を間違えた       削除の確認を FR-046 と書いた。⭐ FR-032 と FR-031 であった
在り処を間違えた          理由が 表 T-016 の前文に在ると書いた。⭐ 1.9 に在った
「4 値が届いている」        ⭐ 定数は在るが読み手が 0 で、ビルドから消えていた
「使用側から返す道が要る」   ⭐ 要ったのは命令の項 1 つ。UID は FR-001 の定めそのもの
行番号・員数を 6 件       ⭐ 「29 か所」は 40 行と 2 変数、「8 行」は 101 行、ほか
```

⛔ **ブリーフに数を書いたら、この 1 文を必ず添えろ:**

> **⛔ この数と在り処は前に立つ者が書いた。原典で確かめてから使え。違っていたら訂正して報告しろ。**

### ⛔ **並走を濁らせない 3 つ（前の巡も効いた）**

```
⛔ 「いま誰がどこを触っているか」を表で書く
⛔ 「木全体の色で判断するな。自分のファイルを名前で走らせろ」と書く
⛔ 報告を 40 行以内に収めさせる
```

⛔⛔ **`npm run gen` と `npm run build` は前に立つ者だけが走らせろ。**
⚠️ **前の巡はそう決めて 1 度も衝突しなかった。**
⭐ **生成された定数が要る体には「無ければ STOP して行 ID を言え」と書け。**

### ⚠️ **`npx prettier` を体に走らせるな**

⛔ **本リポジトリに設定が無いので、既定値でファイル全体を書き換える。**
⚠️ **前の巡に 1 体が踏み、`HEAD` から復元して当て直した。**

---

## 3. **順番**

```
① §1.1 の 2 件を利用者に問う（§8）。⛔ 待つあいだ下流を止めるな（規則 06）
② §1.2 の 4 件を体に出す。⛔ D-195 と D-242 は原因も狙う数も出ている
③ §1.3 の試験と実測を片づける
④ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh
⑤ ⭐ 巡に 1 度は「人として通しで押す」（規則 04 の 3.6）
⑥ ⛔⛔ 緑で push してから §3.9 へ進む
```

---

## 3.9 ⛔ **橋頭保を取ってから —— コメントと仕様書のごみ**

⛔⛔ **順序は利用者が決めている**（2026-09-04、逐語）:

> **一旦今のやり方で不具合をゼロにしろ。それをコミット&Push して橋頭保を確保した後で、
> ごみ対策をさせるよう引き継げ。**

⇒ ⛔ **§1 が終わり、緑で push するまで、この節に手を付けるな。**

⭐⭐ **ただし、前の巡に「ごみ」の実例が 6 つ出た。⛔ どれも読んで見つけたのではない。**

```
D-245  同型の道具が、他所のファイルの状態について偽を刷っていた（直した）
D-248  注が「IV-12 は稼働日で測る」と述べる。CR-347 で暦日になっている
D-250  試験の冒頭が、在りもしない 8 行を除外し続ける
仕様   表 T-201 の結びが、FR-017 の彫った例外を知らなかった（直した）
仕様   FR-072 が入口を数えており、書かれた時点で既に偽だった（直した）
仕様   FR-091 が「その場で編集する形」と書き、FR-085 は別の道になっていた（直した）
```

⭐⭐ **共通点は 1 つである** —— **どれも「事実」を書いており、事実のほうが動いた。**
⛔ **判定基準は変わらない: その文を消したとき、失われる規則があるか。無ければごみである。**

### ⚠️ **前に立つ者が提案し、まだ裁定を受けていない検査（§8 に問い）**

```
検査 34  src/ のコメントに現れる px / 件数が、原稿の値と一致するか
検査 35  「〜が持つ」「〜に在る」と書いた在り処が、実際に在るか
検査 36  RATIONALE の中に MUST / MUST NOT / 「〜すること」が出ていないか
検査 37  ⭐ 新: 辞書の 12 群と、その行 ID を持つ表のセルの共変（台帳 D-145）
検査 38  ⭐ 新: 改訂履歴の版番号が一意であること（台帳 D-246。1 行で書ける）
```

⭐ **どれも基準線つきで入れれば、既存のごみを一度に直さずに済む。**

---

## 4. **測り方の罠（実測で踏んだものだけ）**

```
⛔ 行の名前は切られる。textContent は切られた語を返す。全文が要るならパネルを広げろ
⛔ [data-depth] は「描かれている窓」であって文書ではない。名前で引き直せ
   ⚠️ 起動文書は 101 行を持ち、絵は 7 行しか描かない。数えているのは LOD である
⛔ 行の操作子は、その行の名前にポインタが乗るまで隠れている
⛔ 外接矩形は clip-path を見ない。elementFromPoint で掃け
⛔ 二度押し（MK-13、表 T-023）は 1.5 秒空けろ
⛔ 宿主のダイアログは addInitScript で差し替えろ。⚠️ 差し替えたら
   「取っ手を持ったあとの振る舞い」しか示せない。そう報告に書かせろ
⛔ 初回描画は「同じ読みが 2 回続くまで」待て。固定の待ちは冷間で外れる
⛔ 行の削除は確認の Yes を通る
⚠️ 参照ブラウザの終了は 21〜163 秒かかる。afterAll で test.setTimeout(CLEARING_UP_MS) を呼べ
⚠️⚠️ ⭐ 新: 深い行は「無い」のではなく LOD が落としている。IC-15 を 8 回押せば深さ 5 に届く
⚠️⚠️ ⭐ 新: プレビュー枠では requestAnimationFrame が発火しない。
   1 回よけいにポインタを動かさないと合成されない。⛔ コードではなく道具の性質である
⚠️ ⭐ 新: 壊れた文書の実測に再ビルドは要らない。dist/index.html の写しに
   <div id="embedded-document"> を仕込めば BT-1 の経路を通せる
```

### ⛔ **シェルに噛まれた 4 つの形**

```
① ヒアドキュメント        バックスラッシュと引用符が壊れ、コマンドが返らなくなる
② 台帳のセルの縦棒        行が 1 セル増えて metrics が落ちる
③ python -c "..." のバッククォート   bash がコマンド置換として実行する
④ ⭐ 新: 台帳の行はセル数が揃っていない（実測 7 / 8 / 9）
   ⛔ 添字でセルを取るな。ステータスの欄を探し、その 1 つ手前を対応方針として扱え
```

⇒ ⭐ **日本語や識別子を含む Python は、スクラッチパッドへファイルを書き、パスで走らせろ。**

---

## 5. 道具

```
npm run parity             見本と出荷ビルドを 50 の操作で突き合わせる。⭐ いま 50/50
                           ⛔ 一致しきったら手を増やす番である（規則 04 の 3.6。目標 60+）
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
                           ⚠️ vitest は型を見ずに走る。⛔ 型の誤りは npm test では出ない

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated]

.claude/skills/spec-graph-check/
  check.sh    33 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED\|FAILURES ABOVE"
  induced.py  ⛔ 書く前に。閉路は 1 回の書き込みで直す
  ⚠️⚠️ ⭐ 新: induced.py は「同じ規則が 2 度述べられていないか」を見ない。
     前の巡の穴は 4 つとも辺 0・閉路 0 の中に在った
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ 表 T-016 の前文は docs/spec/_source/property_items_json_to_md.py が持つ
```

**固定の仕掛け:**
```
tests/system/measured-sweep.test.ts                    1 回の起動で回る掃引
tests/system/first-frame-is-the-settled-frame.test.ts  最初の 1 枚だけを測る
tests/system/rows-fixed-with-nothing-holding-them.test.ts  殻の規則を測る唯一の道
tests/system/user-reported-fixes.test.ts               利用者が指摘し、直したものの錨
tests/system/open-defect-pins.test.ts                  ⚠️ PINNED に D-230 だけ
tools/parity/check.mjs                                 50 手
```

⭐⭐ **錨は自分で鳴る。⛔ これが注ではなく錨を置く理由である。**
**前の巡、`D-232` は直った瞬間に「思いがけず通った」と報告した。**

---

## 6. 現在地

```
ブランチ restart（origin/restart へ push 済み）
前の巡で書いた変更要求: CR-348 / CR-349
新しく起こした: D-245 〜 D-252
作業樹は 1 つも作っていない（同じ木でファイルを分け、所有者を表で示した）
⚠️ 利用者は本リポジトリに限り、コミットと push を任せている。⛔ タグは任されていない
⛔ コミットの前に毎回、個人情報の点検を実測して数を報告すること
```

---

## 7. 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | **製品の仕事** | 6 | **0** | §1.2 と §1.3 |
| 2 | **仕様の穴** | 2 | **0** | ⛔ 利用者の裁定を待つ。§8 |
| 3 | **未仕分け** | 6 | **0** | どれも原因が書いてある。調べるだけである |
| 4 | **`npm run parity` の操作数** | 50 | **60+** | ⛔ 一致しきった検査は問うていない検査である |
| 5 | **人として通しで押す** | 0 回 | **毎巡 1 回** | ⚠️ **前の巡はこれをしていない。体の実測で代えた** |
| 6 | **赤い試験** | 0 | **0** | ⭐ 巡の終わりに全緑で置いた |

⚠️ **総件数が増えることは失敗ではない。失敗は、偽の状態を偽と知らずに置いておくことである。**
⚠️ **前の巡で台帳は 244 行から 252 行へ増え、`実装待ち` は 15 から 4 へ減った。**
⭐ **増えた 8 行のうち 7 行は、反証しに行った体がその日のうちに見つけたものである。**

---

## 8. ⛔⛔ **利用者へ問うこと（前の巡がためた 7 件）**

**利用者は 2026-09-04 に「裁定が必要なものは後でまとめて問え」と指示した。以下が全部である。**

```
① D-251  畳んだ親の下に足した行が見えない
         ⭐ 案: ①親を開く ②立てず理由を告げる ③見えないままでよいと定める
         ⚠️ ①は「人が畳んだものを製品が勝手に開く」ことになるので発明しなかった

② D-252  表 T-034 の BT-2 を渡す口が実装に無い
         ⚠️ 配線するか、BT-2 を仕様から降ろすか
         ⭐ 裁定「破損した日程を埋め込んでテストしろ」は BT-1 で果たせている

③ D-241  「1 画面幅に 2 か月を日で」が、幅 1600 では届かない
         ⭐ 実測: しきい値で 53.84 日、実際に踏める刻みで 50.13 日。61 日が要る
         ⭐ 幅 1920 なら 61.73 日で届く。文字サイズ S でも刻みでは 60.65 日で 1 日足りない
         ⛔ 束縛しているのはラベルの幅ではなく段のしきい値であった
         ⇒ S-85 をさらに下げるか、1920 以上で満たすと定めるか

④ D-239  項目名を英語にすると、画面の見た目が変わる
         ⚠️ いまパネルは 名称 / 色 / 高さ と日本語で刷る
         ⭐ 裁定は「英語のまま保つ」であったが、実装は最初から日本語であった

⑤ D-249  削除の確認を通したあと、何が消えたかを告げない
         ⭐ FR-032 は「消えたものを通知すること（MUST）」と定める
         ⚠️ 確認が既に挙げているので、要求のほうを直す道もある

⑥ 検査 34〜38 を入れるか（§3.9）
         ⚠️ 入れれば残件は増える。いま見えていないごみが数十件出るはずである

⑦ S-223（透かしのインクの色）を、読む字の色と同じにしてよいか
         ⛔ 測って決めた値ではない
```

⭐ **①〜⑤ はどれも、待つあいだ下流を止めない**（規則 06）。⛔ **止めずに進めること。**
