# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             ⭐⭐ 赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔⛔ 3 つとも数えろ
4. §1 の 4 件に着手する。⛔ 裁定は全部下りている。問い直すな
```

⭐ **サブエージェントは許可されている**（利用者、2026-08-31）。⛔ **使い方は §2 に測った答えがある。**

---

## 0.5 ⭐ **始点（この巡の終わりに実測）**

```
単体   119 ファイル / 5458 件 —— 5457 緑 ／ 1 skip ／ 赤 0
e2e    tests/system 17 件 —— 全部緑
同型   npm run parity —— 19/19 一致（見本と出荷ビルド）
検査 29 本 ALL GREEN ／ typecheck 緑 ／ layers 284/284 ／ gen:check 17 本 OK
重複 A=27（new 0）／ 設定値 217 行 ／ tables 136 ／ uids 150 ／ 語 405 対
台帳 170 行 ／ 残件 109 ／ テスト完了 57 ／ 追加裁定待ち 7 ／ 取下げ 4
```

⛔ **緑から始まる。**⭐ **だから赤が出たら、それは自分が壊したものである。**

---

## 1. ⛔⛔⛔ **この巡の仕事 —— 4 件。裁定は全部下りている**

⭐ **案・比較・根拠の全文**: `docs/review/rulings-wanted-2026-08-31.md`
⛔ **もう一度案を並べるな。**⭐ **決まったことを書いて、実装して、測れ。**

### 1.1 `D-170` ⛔⛔ **ピン止めした行の上に、スクロールする行が描画される**

⭐⭐ **利用者の裁定（逐語）**
> **タスクグループ名をピン止めしたら、タスクグループが入る行も同じところにピン止めするよ。**

⛔⛔ **前に立つ者が 1 度読み違え、利用者が正した。**
**「キャンバスがパネルより 1 段ずれている」ではない。**
**「ピン止めの行の上にスクロールする行が描画されている」である。**

⭐ **測り直した結果**（出荷ビルド 1211 × 668、実ポインタ）:

```
Whole Product を IC-60 で留め、その行が占める範囲（y=85..149）に何が描かれるかを数えた

スクロール前 : 「Deliver the whole product」1 本だけ   ← ⭐ その行自身のバー。正しい
スクロール後 : 「Deliver the whole product」(y=88)     ← ⭐ 留まったまま。正しい
             ＋「Phone App workstream」(y=124)        ← ⛔ これが入り込んでいる
```

⭐⭐ **行は 1 つである**（利用者の指摘）—— **行見出しパネルに名前が、日程の描画領域にバーが出る。**
⛔ **「帯」という語を使うな。**前に立つ者がその語で 2 つのものを発明し、読み違えの元になった。

⭐ **行見出しパネルは正しく場所を空けている** —— 次の行は y=157 から始まり、留めた行の終わり（149）と重ならない。
⇒ ⛔ **空けていないのは日程の描画領域だけである。**

**やること:**

| # | 何を |
|---|---|
| ① | `FR-098` に 1 文 —— **留めた行が占める場所を、スクロールする行に使わせないこと（MUST）** |
| ② | 日程の描画領域が、留めた行のぶんを差し引いた残りをスクロールの範囲とする |
| ③ | ⭐ **`npm run parity` にピン止めの手を足す**（いまは折り畳みだけを見ている）|

⚠️ **触る見込みのファイル**: `screen-regions.ts` ／ `layout-engine` ／ `svg-renderer.ts` ／ `row-title-panel.ts`
⭐ **絵**: `docs/development-records/evidence/d170-pinned-row-over-the-scrolled-rows.png`

---

### 1.2 `D-158` ⛔ **パレットの入口が押下状態を持てない**

⭐ **利用者の裁定**「**提案通り**」⇒ **`UF-65` に `DocumentSettings` を渡し、表 T-202 の真偽の行を読む。**

```
実測: command-palette.ts の押下判定が IC-76（操作記録）だけを見ており、
      ほかの入口は一律「押されていない」で描かれる
確実に当たるのは 4 件: IC-39 イナズマ線 / IC-40 進捗マーカー / IC-42 日付罫線 / IC-43 グループ罫線
```

⭐ **利用者の逐語がこの入口を名指している** ——「**アイコン自体を緑で塗れ。例: イナズマ線が有効ならイナズマ線アイコンを緑で塗れ。**」
⭐ **行は既に在る**（表 T-237 の `EN-2`）。⭐ **契約も公開済み**（表 T-064 の `PI-37`）。
⇒ ⛔ **欠けているのは配線だけである。仕様に足す行は無い見込み**（⚠️ 未確認。`induced.py` で測ってから書け）。

---

### 1.3 `D-149` ⛔ **`IC-18` の「しまう」が無く、しかも用語が間違っている**

⭐⭐ **利用者の裁定（逐語）**
> **提案通りだが、出す・しまう は用語が違うな。有効・無効 / enable disable, Active / Inactive /
> Visible, Invisible などじゃないか？ 適切な用語を使え。コードも仕様書もだ。
> レビュー基準を適宜読めって指示が出てるよね？ その中に、適切な命名 ってあるだろ？**

⛔⛔ **`docs/development-rules/07-review-standards.md` の `Naming` 節を読め（MUST・最重要）。**
そこは逐語でこう書いている —— 「**命名はソフトウェアの主要なインターフェースである**」
「**Boolean の変数・関数名が `is/has/can/should` から始まっているか**」「**用語一貫**」。

**何が起きているか（実測）:**

```
IC-18 と IC-20 が同じ 1 つの値（isAgentApiEnabled）を読んでいる
⇒ IC-20 を押すと、IC-18 を 1 度も押していないのに IC-18 の押下状態が立つ
⭐ 対話欄は本当に出ている（display:none → flex）。死んでいるのは「しまう」の半分
⛔ 「しまったか」を覚える行が 表 T-206 にも 表 T-203 にも 表 T-237 にも 1 行も無い
```

⭐⭐ **語が 2 つの概念を 1 つにしていたことが、この欠陥の根である:**

| 入口 | いまの語 | ⭐ 正しい概念 |
|---|---|---|
| `IC-20` | `Agent API` を**有効にする・無効にする** | ⭐ **能力**（enabled / disabled）。**そのままでよい** |
| `IC-18` | AI との対話欄を**出す・しまう** | ⛔ **見え方**（visible / invisible）。**別の概念である** |

**⭐ 測った規模（前に立つ者、2026-08-31）:**

```
表 T-109 で「出す・しまう」を使う行            12 行
  ⭐ 12 行とも中身は「見え方」であり、対応する設定の鍵は既に xxxVisible である
     （assigneeVisible / baselineVisible / dateGridLinesVisible / dependencyVisible /
      groupGridLinesVisible / percentCompleteVisible / progressLineVisible /
      progressMarkerVisible / watermarkVisible の 9 つ）
  ⇒ ⭐⭐ 語だけが鍵と食い違っている。鍵に語を合わせるのがいちばん安い

src/ の真偽の宣言（重複を除く）                97
  is / has / can / should で始まる               54
  始まらない                                     43
    うち 9 は上の xxxVisible  ⛔ 文書に保存される鍵。形式が変わるので触るな
    うち数件は交換形式の列名（resumeValid / dayWorking / milestone など）⛔ 触るな
    残りが素直に直せる分
```

**やること:**

| # | 何を |
|---|---|
| ① | ⛔ **規則 07 の `Naming` 節を読んでから始めろ** |
| ② | 表 T-109 の 12 行の「出す・しまう」を、見え方の語彙へ揃える（鍵が `xxxVisible` なのだから語もそれに従う）|
| ③ | `IC-18` 用に「対話欄が見えているか」の状態を 1 行足す（⭐ **保存しない**。`S-99e` が同じ立場の先例）|
| ④ | 表 T-237 に、その状態で塗る行を 1 つ |
| ⑤ | `src/` の名を揃える。⛔ **文書に保存される鍵と交換形式の列名は触るな** |
| ⑥ | ⚠️ **辞書（`display-words.json`）も必ず読み直せ** —— **この巡、表の文言を変えて辞書を置き去りにし、`RS-30` が正反対のことを告げた** |

---

### 1.4 `D-147` ⛔ **`IC-41`（透かしを隠す）が効かない**

⭐ **利用者の裁定**「**提案通り**」⇒ **表 T-234 に「透かし解除パスワードを問う」行を 1 つ足し、面を新設する。**

```
実測の切り分け:
 ⭐ 押下は届いている（FR-102 の記録が on=IC-41 assigned=true）
 ⛔ 命令への道が無い —— ENTRY の対応表に IC-41 の鍵が無く、黙って捨てられる
 ⛔ 書き先が 1 行足りない —— 表 T-202 の真偽は 9 行、VisibleElement は 8 行
 ⛔ 問う面が 表 T-103 にも 表 T-037 にも 表 T-234 にも 1 行も無い
 ⛔ 通知も 0 件 ⇒ いまの状態は FR-029 の MUST にも反している
```

⭐⭐ **裁定を待たずに先に直せる分が 1 つある** ——
**`VisibleElement` の 9 対 8**（`watermarkVisible` が欠けている）。**面の裁定と無関係の欠陥である。**

⚠️ **`FR-020` の MUST は「しまう側」にだけ門を立てている。**⛔ **対称な切り替えにするとその MUST が壊れる。**

---

## 2. ⭐⭐⭐ **エージェントかワークフローか —— 測って答えを出した**

### ⭐ 答え: **エージェント。⛔ ワークフローの扇形展開ではない。**

**根拠は 3 つあり、どれも実測である。**

#### ① ⛔⛔ **ファイルが衝突する。実装を並列にしてはならない**

```
D-170  row-title-panel.ts / screen-renderer.ts / dom-screen-surface.ts / svg-renderer.ts
D-158  command-palette.ts / screen-renderer.ts / dom-screen-surface.ts / app-header-items.ts
D-149  app-header-items.ts / dialogue-field.ts / screen-renderer.ts / frame-loop.ts
D-147  json-codec.ts / document-settings.ts / input-command-translator.ts / edit-document-settings.ts
                       ⛔⛔ screen-renderer.ts を 4 件中 3 件が触る
```

⚠️ **この巡、体が 2 つ同じ木を触って実害が出た** —— **1 度の `npm test` が、書きかけの
`dom-screen-surface.ts` のせいで 190 件の偽の赤を報告した。**
⇒ ⛔ **実装は 1 件ずつ順に。**⭐ **並列にしてよいのは、下の②の対だけである。**

#### ② ⭐⭐ **並列にして値打ちがあるのは「実装する体」と「仕様だけを読む体」の対である**

⭐ **この巡、その分け方が仕様の穴を 4 つ捕まえた**（§4）。
⛔ **試験を書く体に `src/` を読ませるな** —— **読ませたら、実装の振る舞いを規則として書き写す。**
⭐ **書く先が違うので衝突しない**（実装は `src/`、試験は `tests/`）。

#### ③ ⛔ **仕様は 1 つの手で書く。ここは分けるな**

⭐ **`induced.py` を走らせた**: `FR-098` `FR-066` `FR-065` `FR-020` `FR-086` `EN-2` `IC-18` `IC-20` `IC-41`
—— **閉路 0。1 件ずつ書いてよい。**
⛔ **しかし重複検出はこの巡 3 度、前に立つ者を捕まえた**（新規 6 → 3 → 1 → 0）。
**別の表の MUST を写したかどうかは、書いた本人にしか判じられない。**
⇒ ⭐ **仕様は前に立つ者が書き、書くたびに `check.sh` の重複を読む。**

### ⛔ **ワークフローが向くのはどんなときか（今回は当たらない）**

```
向く : 同じ変換を多数の対象へ当てる（移行・一斉監査・広い掃き出し）
今回 : 異なる 4 件。どれも仕様の判断が先に要り、触るファイルが重なる
⇒ 決定的な扇形展開の値打ちが出ない。⛔ 使うな
```

⚠️ **1 か所だけ迷える** —— `D-149` の⑤（`src/` の名を揃える）は同型の掃き出しである。
⭐ **それでも体 1 つで足りる**（実測 43 のうち、触ってよいのは更に少ない）。

---

## 3. ⭐⭐ **順番 —— 手戻りを出さない並べ方**

```
① 仕様を 1 パスで書く（前に立つ者。⛔ 体に渡すな）
   1.3 の語彙 ⇒ 1.4 の新しい語 ⇒ 1.1 の FR-098 の MUST
   ⭐⭐ 語彙を先に決めるのは、1.4 が新しい語を足すからである。
      ⛔ 逆順でやると、1.4 の語を古い語彙で書いて、あとで書き直すことになる
   ⛔ 書くたびに: induced.py（閉路）→ 書く → check.sh（重複 new 0 を確かめる）

② 実装と試験を 1 件ずつ、対で回す（⛔ 実装どうしを並列にしない）
   D-170 → D-158 → D-149 → D-147
   ⭐ D-170 が先なのは、利用者が画面で見つけた 1 件だからである
   各件で: 実装する体（src/ だけ）＋ 仕様だけを読む体（tests/ だけ、src/ 禁止）を同時に出す

③ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh
   ⛔ 赤を次の件へ持ち越すな

④ 最後に npm run parity へピン止めの手を足し、19 → 25 以上にする
```

⚠️ **`D-147` の `VisibleElement`（9 対 8）だけは①の前に直してよい** —— **仕様に触らない欠陥である。**

---

## 4. ⭐⭐⭐ **この巡で学んだこと（続けろ／繰り返すな）**

### ⭐ 効いた

```
⭐⭐⭐ 体を分ける（規則 05 の 7）。仕様だけを読む体が、前に立つ者の書き損じを 4 つ捕まえた:
    ① HR-6 に MUST を書き落とした（見本は隠すとき配下を畳むのに、仕様が黙っていた）
    ② 同じ行を 3 時間後にもう一度書き損じた（射程が 1 階層外れていた）
    ③ 表 T-051 の結びの MUST NOT が、逐語では 3 つの MUST と矛盾していた
    ④ RS-30 の辞書の語が、書き換えた場面と正反対のことを告げていた
    ⭐ 実装する体も 1 度、前に立つ者の指示を拒んで仕様を優先した（正しかった）

⭐⭐⭐ 見本も実物として測れ。CSS を読むだけでは形が出ない（flex:1 を見落とす）。
    npm run parity が、5457 件の単体試験が見つけられなかった欠陥を 1 つ見つけた。

⭐⭐ 表の行の文言を変えたら、辞書（display-words.json）を必ず読み直せ。
    ⛔ 機械検査は「その語が届いたか」を見るのであって、「その語が真か」を見ない。

⭐ 「効かない」と書いてある行は自分で押す。この巡も 2 件が偽であった。
```

### ⛔ 前に立つ者が犯した誤り

```
⛔⛔ 実測の見立てを 3 度誤った。⭐ どれも「窓の外」を見落としたためである:
    ・スクロールしたパネルを見て「削除が子を孤児にした」と 2 度読み違えた
    ・D-170 を「1 段ずれている」と読み違え、利用者に正された
    ⇒ ⭐⭐ 画面に描かれている分だけを見て全体を語るな。範囲を宣言してから測れ。

⛔⛔ 語を発明した。「帯」と書いて、行と別のものであるかのように扱った。
    ⭐ 利用者の指摘「行は 1 つだ」。⇒ 仕様に在る語だけを使え。

⛔ bash のヒアストリングに \uXXXX を書くと実体化し、書き込み途中で例外が出て
   settings.json が 0 バイトになった。⭐ Write ツールで .py を作り、書く前に encode() する。
⛔ 台帳のセル境界は ` | `（前後に空白）。空白を落とすと列がずれ、検査が落ちる。
⛔ 裁定待ちの状態の欄は「未裁定」か「裁定済」だけ。理由を書き足すと正規表現が外れる。
⛔ 日本語を書いていて 1 度キリル文字が混ざった。⭐ 書き終えたら走査しろ。
```

---

## 5. ⭐ 道具

```
npm run parity             ⭐⭐ 見本と出荷ビルドを 19 の操作で突き合わせる
                           tools/parity/check.mjs ／ sample-and-app.mjs
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen:check          ⭐ 巡の頭で必ず
tools/probe/harness.mjs    ⭐ export 40。出荷ビルドを実測する

⛔ SVG の x は画面の x ではない。盤面の図形は elementFromPoint で掃いて拾え
⛔ 行を y で覚えて押すな。押すたびに組み直るので別の行に当たる。名前で引き直せ
⛔ パネルは縦にスクロールする（HF-9）。窓の外の行は DOM に無い
⚠️ 行の高さは 64 / 96 / 101 / 108 / 148px とまちまち。決め打ちするな
⭐ 掴み代 [data-row-grab] ／ 件数 [data-folded-rows] ／ 通知 [data-manner]
⭐ ピン止めした行 [data-role="Pinned Row"] ／ [data-pinned]

.claude/skills/spec-graph-check/
  induced.py  ⛔ CR を書く前に。閉路は 1 回の書き込みで直す
  impact.py   波及を測る
  check.sh    29 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED"
  ⚠️ 表に行を足したら npm run words も要る
  ⚠️ 設定値を足したら tools/generate_entity_types.py にも要る
     （⛔ その一覧は 3 度ずれた。D-168 が検査を待っている）
```

---

## 6. ⭐ 現在地

```
⭐ push 済み: origin/main。⚠️ ブランチ名は restart（push 先は main）
⭐ 仕様: CR-321（1.75）〜 CR-324（1.78）
⭐ 裁定は 6 件とも下りた（2026-08-31）。⛔ 問い直すな
⭐ 作業樹は 1 つも作っていない
```

---

## 7. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔⛔ **`D-170`**（利用者が画面で見つけた）| 未 | **済** | ⭐ **画面が読めない。他より先** |
| 2 | ⛔ **`D-158` / `D-149` / `D-147`** | 未 | **済** | ⭐ 裁定は下りている |
| 3 | **赤い試験（単体・e2e・同型）** | 0 | **0** | ⭐ 3 つとも数えろ |
| 4 | **`npm run parity` の操作数** | 19 | **25+** | ⭐ ピン止め・ドラッグ・足す・消すを足す |
| 5 | ⛔ **生成された定数一覧の検査**（`D-168`）| 未 | **有** | ⛔ 手で保つ表は 3 度ずれた。4 度目が来る |
| 6 | **`tests/` の単一文字の宣言** | 72 | **0** | ⚠️ 表で機械的に直すな。1 か所ずつ読め |
| 7 | **`src/` の短い名** | 27 | **0** | 利用者の指示。⭐ `D-149` の⑤と同じ作業である |

⚠️ **総件数が増えることは失敗ではない。**⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
