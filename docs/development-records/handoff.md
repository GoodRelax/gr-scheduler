# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> ⭐⭐ **2026-09-18〜19 は、ブランチ `scale-and-fold` の上で 3 つを入れた** —— ① `CR-417`〜`CR-427`（`JDG-169`〜`JDG-179` の答え）を仕様・実装・仕様だけを読んだ試験まで（`d1d88fb6`・`1403ee62`・`ae892d1b`・`e02d87e7`）② 全体表示の答えを 1 度だけ解いて持つ直し（`f02f4050`、試験 `77422a5b`）③ 掴み代・ポインタ・札・操作を、触れる見本で利用者と決めた（`JDG-180`〜`JDG-193`、見本は `previous-project-result/16-grab-area-sizing/`）。**③ を仕様にする段取りは §1.2 が持つ。** 次の巡は下の 1 通目から始めよ。
> ⛔ **`scale-and-fold` はまだ `refactor` へ戻していない。** 利用者が `dist` を見て了承してから早送りする（`JDG-162`、未着地）。`main` では直接作業しない。
> ⛔ **本書は数を持たない。⭐ §0 の手順で測れ。** 段 0 の基準値は計画の記録 16 が、出口の実測と性能の比べ方は記録 17 が、残件は §3 の問いが、裁定は `rulings.md` が持つ。
> ⛔⛔ **異常系・準正常系のケアは、リファクタ一式が終わるまで触るな**（`JDG-78`、利用者 2026-09-14）。中身は `DFC-586` が持つ —— 起こさない・自動で直して何処をどう直したか示す・直せないものは対象の ID と行番号を示す。取り込みの拒否の語（表 T-220 の `IV-`）も、そのとき一緒に書く。
> ⚠️ **§2 は、この巡の学びを規則へ下ろす前の控えである。** ⛔ 下ろしたら、その項を本書から消せ（本書に置いた学びは 1 巡で消える）。

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 次のセッションに貼る 1 通目

利用者はこの節のブロックをそのまま新しいセッションに貼る。巡ごとに本節も書き換える。

```text
ブランチ scale-and-fold で作業せよ（refactor d70e181f から切った。根もこのブランチに置いてあり、push 済み）。
refactor へ早送りするのは、利用者が dist を見て了承してから（JDG-162、未着地）。main では直接作業しない。タグは利用者が打つ。

0. 到達点（2026-09-19 まで。scale-and-fold の先端 = 本書を書き換えた commit）
   - 2026-09-18: CR-417〜CR-427（JDG-169〜JDG-179 の答え —— 表示の倍率 100 の描く比 0.625、目盛と行見出しの字と行見出しパネルの幅を 1.5 倍、名前の太さと 1 つの書体の並び、マーカーはいつも名前の前、ダミーはマーカーの半分の幅でマーカーと接する、フェードの掴み点 5 × 5 ／ 8 × 8、行の軸のズームの端、プロパティパネルの幅の下限、ヘルプの割当の区切りとホイールの図形、名前の欄のキー、端点の矢印のポインタ）を、仕様 d1d88fb6 → 実装 1403ee62 → 仕様だけを読んだ試験 ae892d1b → 突き合わせ e02d87e7 で入れた。11 本とも tests/unit/cr-417-… 〜 cr-427-… の試験を持つ。e2e の探り針の直しは 3dd6019c。
   - 2026-09-18: 全体表示（表 T-024a の OP-10）の答えを毎フレーム解き直していたのを f02f4050 で直し、77422a5b で原稿から書いた試験を足した。
   - 2026-09-18: 性能は JDG-168「パフォーマンスは一旦良い。」で是とした ⇒ DFC-610 ／ DFC-643 は取下げ（戻す条件は各行の詳細状況）。前の 1 通目の問い ①（JDG-50 を表示の倍率のどの段で比べるか）は、これで済んだ。
   - 2026-09-18〜19: 掴み代・ポインタ・札・操作を、触れる見本で利用者と決めた。見本は previous-project-result/16-grab-area-sizing/（e1d29dfb〜3f714c6e）、裁定は rulings.md の JDG-180〜JDG-193（すべて未着地）。仕様にする段取りは本書 §1.2（Step 0 は済み。次は利用者へ問う 7 問と Step 1a。⭐ 7 問の 4 は JDG-197 で決まり、4a が増えた）。
   - 2026-09-19: 見本を書き直した —— 絵 ｜ 設定の 2 枚の窓（それぞれが自分でスクロールする）、つまみごとにその値が効く所を示す注、⑤ の行に 1 行に積んだ全部入りのタスク 3 本、依存線（押して選ぶ・消す・構えて引く）。
   - 2026-09-19: 見本の掴み代を、操作対象ごとに独立した値に書き直した —— 掴み代の値を「掴む対象 × 向き（外側／内側／上下／幅）」の表にし、表の 34 本のつまみはそれぞれ 1 つの対象の 1 つの向きだけを動かす（前に立つ者が Playwright の探り針で 34 本を 1 本ずつ動かし、動いた掴み代を数えた: 34 本すべてが自分の対象だけを動かし、ほかの対象の掴み代は 1 つも動かない）。「予定の端 横／縦」「実績の端 横／縦」「マーカー 一辺」「縦の下限」の共有つまみは外し、7 問の ① と ④ は値の組（推奨／今まで）を書き込むボタンにした。
   - 2026-09-19: 見本を触りながらの裁定 JDG-194〜JDG-200 と、JDG-183 の読みの直し —— 掴み代は対象 × 向きごとの独立した値（JDG-194）、札の余白は 4 つの独立した値（JDG-195）、依存線は予定にだけ付き、予定が無いとき・予定を表示していないときは描かない（JDG-196。FR-009 の実績への落ち先とアプリの attachedBar を覆す ⇒ DFC-658。FR-013 の文言は DFC-659）、予定の端の上下の既定は 0（JDG-197。7 問の 4 の答えで、GS-3 を予定の端について覆す）、依存線の表示の切り替え（JDG-198。仕様は S-62 ／ IC-81 で既に持つ。IC-81 の注と FR-049 の食い違いは DFC-660）、依存線の掴み代は描いた線の縁から左右 2px（JDG-199。S-137 の線の左右 6px を依存線について置き換える）、依存線の上のポインタは線の矢印で黒ぶち白抜き（JDG-200。端点の箱型の矢印と形で見分ける。見本で試す）。JDG-183 の読みは、マーカーを出さないときのマイルストーン名の書き出しを菱形の幅の 25% の位置に正した（前の読みの 75% の位置は前に立つ者の読み違い。見本は bdf49b6b で直した）。
   - 2026-09-19: 続く裁定 JDG-201〜JDG-204（見本は f15ea18e）—— 依存線の引き出し・引き入れを独立した 2 つの値に（見本 4px ／ 4px。LF-4 の「出口と入口に別々の設定値を持たない」と S-19 の走りの役を覆し、S-20 は要らなくなる。JDG-201）、矢じりの幅を長さと別の値に（見本 7 × 4.2px。S-19 の底辺の役を覆す。JDG-202）、描く順は応える順と別の表で管理し、名前と担当・進捗の札を依存線より手前に描く（仕様は MK-9a で既に分けている。表 T-020 に描くものすべての行を足す ⇒ DFC-662。マーカーを線より手前にするのは ZO-3 と逆 ⇒ 問う。JDG-203）、日程表の上の引きで字を選ばない（仕様に行が無い。JDG-204）。アプリが範囲選択の矩形を透かしと目盛より先に描き ZO-6 と食い違うのは DFC-661。
   - 2026-09-19: 続く裁定 JDG-205〜JDG-206（見本は `8f6b165c`／`f4146ca8`）—— 依存線の既定値をキャプチャの通り（引き出し 6px・引き入れ 10px、矢じり 長さ 6px × 幅 5px）にし、問い 9 の未確認 ① を既定については解消した（JDG-205）。依存線のポインタ（JDG-200）を、矢じりの幅と黒縁の太さを半分にし、縁を角張らせて作り直した（利用者「予定と見間違えそうだ」。JDG-206）。
   - 2026-09-19: 続く裁定 JDG-207〜JDG-208（見本は `46353cbe`）—— フェードのポインタ（JDG-191）の三角の縦を最初の 75% にし、依存線のポインタと同じ太さの縁（全ポインタで共有する 1 つの画面 px 値、POINTER_RIM_PX）を負わせた（JDG-207）。マイルストーンの図形の上のポインタを、手のひらの矢印から円盤（予定 〇・実績とダミー ●）に作り直し、大きさを予定 12px（最初の 75%）・実績 8px（最初の 50%）に分けた（表 T-028 の IN-2 に新しく足す。JDG-208）。あわせて設定のペインを主題ごとの節に分け、掴み代の表とポインタを節ごとに置いた（つまみの数は変えていない。サンプルの整理であり裁定ではない）。
   - 試せる dist: ブランチ scale-and-fold、dist/index.html は f02f4050 で焼いたもの（そのあと src/ は変わっていない）、sha256 007937db1e59ee8286e1531b161041a768b65e8f0c1385ce57951aa0a33729ea（§0 で測り直せ）。
   - まだ赤いもの（§0 で数え直せ）:
     単体は 77422a5b の報告で 8741 件緑。
     型検査は tests/unit/uf-45-46.test.ts の 8 件だけ（DFC-655。2026-09-19 に npx tsc --noEmit -p . で実測）。⚠️ check.sh はこの型検査を見ない。
     e2e は 3dd6019c の報告で、性能の門 2 本（nfr-001-010-011-013、nfr-002-003）、DFC-627（SWS-8）、DFC-374 の錨（tests/system/user-reported-fixes.test.ts）。⚠️ DFC-374 は fixed-defects.md で実測済であり、開いた台帳の行を持たない ⇒ §4 の「行の無い赤」である。赤のままなら、体に原因を反証させてから台帳に行を立てよ。
     check.sh は 26b と 37（下の 2 の利用者の手）。この巡の裁定で 43 も赤になる（同じく 2）。
1. 読む順: 本書 §1.2 → rulings.md の JDG-180〜JDG-227 → 見本 previous-project-result/16-grab-area-sizing/grab-area-sizing-sample.html（ダブルクリックで開く）と同じフォルダの README.md → §1.1（Step 5 で戻る所）→ §0（基準を測り直す）→ rulings.md の 2026-09-18 の節（JDG-168〜JDG-179）。
2. ⓪ 利用者が手で行うこと（⛔ 前に立つ者は基準線を触らない。2026-09-17 は分類器が消去を拒んだ）
   - 検査 26b: .claude/skills/spec-graph-check/crossing-names-baseline.txt から「ItemHitArea | NOT_STORED_SIZES」と「ItemHitArea | PointerSlop」の 2 行を消す（CR-415 で掴み代の束ねを Entity へ寄せたので、もう層をまたがない）。
   - 検査 37: 表 T-023 の MK-7、表 T-036 の SK-2・SK-3・SK-4・SK-5・SK-11・SK-17、表 T-104 の K-23 を、それぞれ辞書（display-words.json）の語と一緒に読んでから、組の検査を --write-baseline で走らせる（python .claude/skills/spec-graph-check/check-dictionary-table-covariance.py --write-baseline）。
   - 検査 43: .claude/skills/spec-graph-check/ruling-landed-baseline.txt の基準線を 18 → 65 に上げる（2026-09-19 に check-ruling-landed.py で数えた: 未着地 65 ＝ 前からの 18 ＋ この巡の JDG-180〜JDG-227 のうち JDG-198 を除く 47 行。47 行は Step 1b まで未着地のまま。JDG-198 は仕様に既にあるので数えない）。⛔ 上げるのは利用者の手である（前に立つ者は触らない）。⚠️ 上げる前に check-ruling-landed.py の数で確かめる。⭐ Step 1b が着地したら、また下げる。
   - 作業木の掃除: .claude/worktrees/ の 35（ab-*・ab2-* が 5、agent-* が 30。2026-09-19 に数えた）と、.git/worktrees/ の同名の入口。OneDrive の読み取り専用で、道具では消せない。手順は規則 04 の 6.11 —— 先に agent-a6379b60827a65173/node_modules の junction を rmdir（/s を付けない）で外し、根の node_modules が残ることを確かめる。agent-a2377c68ac2dba9ee は npm ci をした本物の node_modules を持つ（junction ではない）。ほかの agent-* の node_modules は Vite の一時フォルダだけである。そのあと attrib -R、rd /s /q、最後に git worktree prune。
   - dist を見る: 根の dist/index.html を file:// で開き、下の 4 の ⑦・⑬・⑮ を見る。
3. 最初にやること（2 つを並べる）
   ① 最初に利用者へ問う（1 通にまとめる）: 残るのは 5 つ —— 仕様案 §8 の ㉔（見た目を保った既定で S-22 の 22.4 が今の上限 16 を破る: 上限を決め直すか、マーカーを 16 へ小さくするか）・㉕（端点スパン SH-4 の点の径も、---> と同じく独立の設定値にするか。今は 線 × S-47 × 2）・㉖（再開アイコンの案 —— JDG-222 ／ JDG-223。仕様案の 表 XS 10〜13・LP・GA 20・HT・PE 11・PK 9・ZO 4 と 7 を、このまま採るか。置き場は JDG-226 で決まった —— いつも実績の右）、群 E の ㉗〜㉞（同じ CR に入れる要素のうち利用者の答えが要る 8 つ。洗い出しは previous-project-result/16-grab-area-sizing/cr-inventory-ja.md）、㉟（マーカーの押下の巡り —— JDG-227。PV-4 の MUST NOT は利用者の指示「・ に戻れるようにしろ」そのもので覆り、㉟ で決めるのは輪の形だけ。案は進み順の輪 未着手 → 進行中 → 完了 → 中断 → 未着手）。中身は仕様案 §8。⭐ ⑥（名前が形に「入る」かの判定）は JDG-224、9 の ①（引き入れと矢じり）は JDG-225 で決まった（2026-09-19）。⚠️ JDG-225 の「⑨-2」の読みと、JDG-226 の逐語 5 行目の読みは、利用者にチャットで確かめる。⭐ ㉓（---> の 3 段を低くする手）は JDG-221 で決まった —— 線の太さ・矢じりの長さ・矢じりの高さを独立した設定値にする。⭐ ほかも 2026-09-19 に答えが出た（JDG-209〜JDG-221・JDG-224〜JDG-226。7 問の 4 は JDG-197）—— 問い直さない。先に rulings.md を grep し、答えが入っていなければ、下の 4 と合わせて 1 通にまとめて問う（具体名・推奨・代償を付ける —— 下の 6 の約束）。答えは rulings.md へ新しい行として足す。⛔ Step 1b の CR はこの答えを待つ。
   ② ⭐ §1.2 の Step 1a は済んだ（2026-09-19、de5955bf）—— 旧 → 新の対応表は previous-project-result/16-grab-area-sizing/old-to-new-map-ja.md（425 件: A 189 ＋ B 236）、整えた仕様案は同じフォルダの spec-draft-ja.md（8 つの表）、同じ CR に入れる要素の洗い出しは cr-inventory-ja.md（23 件。JDG-222）。次は、① の 5 つ（㉔・㉕・㉖・群 E の ㉗〜㉞・㉟）だけを 1 通にまとめて利用者に問い、答えを rulings.md へ入れてから Step 1b の CR へ進む。群 A・B・D、群 C（⑥・9 の ① を含む。② は ㉟ で問い直した）、問い 8、---> の 3 段と線・矢じりの値（㉓）は 2026-09-19 に答えが出た（JDG-209〜JDG-221・JDG-224・JDG-225）—— 問い直さない。⛔ 対応表に行の無いものを 1 つも消すな。
4. ほかの問い（3 の ① と同じ 1 通に入れる。Step 1a を止めない。問う前に rulings.md を grep せよ）
   ⭐ 掴みの問いは問い直さない —— 前の 1 通目の ⑫（GS-2: 上下の形から等しい距離の点）と ⑬（GR-7 ／ GR-8 の当たりの箱が上下の隣に重なる）、CR-421 の返す問い 1〜3（ダミーの押せる広さ、マーカーの上を通る依存線、予定だけのときのマーカーとの隙間）、CR-427 の返す問い 1〜3（マイルストーンとフェードの角のポインタ、S-249 の一辺 —— JDG-181 が 24 → 16 と決めた）は、Step 1a の対応表と Step 1b の CR の中で決着させる。JDG-180〜JDG-193 と見本に答えが無いものだけを、CR の返す問いにして、§0.2 の 3 手を打ってから 1 度だけ問う。⚠️ ⑫ は答えが無いと分かっているので、3 の ① の 7 問の 5 に入れた。⭐ 7 問の 5 は JDG-217（2026-09-19）で決まった —— ⑫ も閉じた。
   ① 端の入口を押すと、FR-029 の理由の通知とメッセージの両方が出る（表示の倍率の IC-104 ／ IC-105 は CR-411 の返す問い 1、行の軸の IC-14 ／ IC-15 は CR-423 の返す問い 2。実装も両方）。推奨: 端の入口ではメッセージが理由を運ぶので、通知を出さない例外を FR-029 に書く。代償: 仕様の 1 段と、実装・試験の小さい波。
   ② CR-411: 端の段へ着いた押し（175 → 200）でも「（最大）」を添えるか。いまの SE-2 は変わらなかった押しでだけ添える。推奨: 添える（もう 1 度押して確かめなくて済む）。代償: SE-2 の 1 文と実装。
   ③ CR-423 の返す問い 4: 行の軸で倍率を変えた押しでもメッセージを出すか。推奨: 端のときだけ（行の軸は連続に変わるので、毎回出すと数字が流れ続ける）。
   ④ CR-423 の返す問い 3（DFC-641 の ①）: 上限より上の倍率から拡げる入力。推奨: 倍率を下げない（書かず「（最大）」を出す）。
   ⑤ CR-423 の返す問い 1（PND-204）: 全体表示が深さ 1 を採ったときの縦の倍率。JDG-172 では決まらない。推奨: PND-204 のとおり（深さ 2 のしきい値の S-53 1 刻み下）。
   ⑥ CR-418 の返す問い 1: 以前に保存した文書は、文書に保存する S-3 ／ S-2 ／ S-36 ／ S-79 の古い値のまま開く（1.5 倍にならない）。推奨: 読み替えない（JDG-152 の「読み替えは無用」と同じ）。
   ⑦ CR-425 の返す問い 1: ヘルプのホイールの図形（IC-102）を新しい形のまま残すか、語「ホイール」を刷るか。まだ実物と見比べていない。⚠️ 語を刷るのは JDG-76（語を印に置き換え、凡例を置く）を覆す。推奨: dist のヘルプで新しい形を見てから決める。
   ⑧ CR-405: ヘルプの題の行を、縦にスクロールしても箱の上に残すか。推奨: 残す（閉じる入口 IC-52 が見えたまま。代償は CSS の数行と試験 1 本）。いまは題の行ごとスクロールする（Esc でも閉じられる）。
   ⑨ CR-406: 担当者の一覧に、欄の名を刷る見出しの行を立てるか。推奨: 立てない（語を発明しない）。／ 一覧の面の題の字も 75% にするか。推奨: する（実装済み）。
   ⑩ CR-407: Import Report（取り込みが落とした Task の名前）も同じ確認の形にするか。推奨: しない（確認ではない）。／ 見出し部と一覧を隔てる線（S-242）は 1px でよいか。推奨: 1px のまま dist で見る。
   ⑪ CR-408: プロパティパネルの境界の掴み帯（S-134 8px）を広げるか。推奨: 8px で見てから決める。／ 帯が勝つ（GR-22）ことで、行見出しパネルの右端に接する入口の 4px が押しにくくなってよいか。推奨: 帯が勝つ（掴めない境界のほうが困る）。⚠️ GR-22 も Step 1a の対応表に 1 行要る（表 T-023d の GR の行のひとつ）。
   ⑫ CR-413: S-11 の鍵 stackGap の名が中身（依存線の片側の 1px）と合わない。推奨: いまは改めない。代償: 名と中身がずれたまま（改めるなら保存形の鍵が変わる。表 T-104 の K-11 と辞書も一緒に）。
   ⑬ dist を見て数を詰める試す値: S-34（縁取り）・S-174 ／ S-175（選択の枠）・S-243（行見出しの入口の余白）・S-240（担当者の一覧の字）。CR-419 の返す問い 1: Windows で Yu Gothic の 400 が細すぎないか（JDG-171）。CR-411: メッセージの置き場（Row Area の中央、上から 33%、押しを受けない）。⭐ 前の ⑲ のうち、S-109 は JDG-179 で、S-79 は JDG-169 で決まった。⭐ CR-418 の返す問い 2・3（行の名前の箱と帯の高さ、S-85 のしきい値）と CR-419 の返す問い 2（S-233 ／ S-30 を S-246 の並びで測り直す）は、先に体に出荷ビルドで測らせ、数を添えて問う。
   ⑭ DFC-642（深さ 5 の行が縦ズームで出ない。裁定待ち）: 数は CR-418 の S-36 で変わった（DFC-647 の注）。推奨: ⑬ で S-87 ／ S-88 を選ぶときに一緒に決める。
   ⑮ 手で確かめる（Playwright では見えない）: 日本語キーボードで Ctrl ＋ Shift ＋ [;]（= [+]）が表示の倍率を上げるか、Windows の入力言語の切り替えが Ctrl ＋ Shift を取らないか（届かなければヘッダーに「見た目を戻す」入口を置くか。CR-410）。名前の欄に焦点が入る前に打った字が欄に入るか（CR-426。試験はブラウザでしか見えないとして 1 件を飛ばした）。
   ⑯ 確認だけ: SK-17（Ctrl ＋ Shift ＋ [0]）で表示の倍率とズームの両方を戻したとき、取り消し 1 段で両方が戻る（実装済み）。CR-404 の 9 節の 3 問は推奨どおり当てた（見出しの [vv] も印を立てる ／ 倍率だけで落ちた配下の [vv] は濃くしない ／ 行の部分木の複製では印を写さない）。覆すなら表 T-254 の 1 行と FR-004 の 1 文。
5. 残る件（⛔ どれも反証してから動かせ。状態は 2026-09-19 に台帳で確かめた）
   - CR-417〜CR-427 のコードと試験は入った。本当に残るのは 3 つ —— ① 台帳の DFC-647〜DFC-653（2026-09-18 の報告の 7 行）がまだ実装待ち ⇒ 体に file:line で反証させ、入っていれば実測待ちへ、出荷ビルドを押して実測済へ ② rulings.md の JDG-169〜JDG-179 の状態の「コードと試験は実装の波」は古い ③ 飛ばした試験（it.skip）は、上の問いと CR-424 の 1 件（S-248 でも Row Area が 0 以下になる狭い窓 ⇒ DFC-586 と一緒、JDG-78）だけ。
   - 着地したと読める未着地の裁定: JDG-151（POINTER_SLOP は c7f87805 で frame-loop.ts から消え、item-hit-area.ts の PointerSlop ／ grabSizesOf にある）、JDG-126（DFC-610 の直しは 453f3f7b。DFC-610 は JDG-168 で取下げ）⇒ 体に反証させ、着地なら状態を直す。検査 43 の数が下がる（基準線を下げるかは利用者に問う）。
   - rulings.md の JDG-110 ／ JDG-115 の状態を「覆された（一部。JDG-148）」に直す（S-17 の 1.25 → 1.0）。rulings.md の規則 ① の形で、JDG-152 ／ JDG-156 と同じ。前の 1 通目は問いにしていたが、問う件ではない。
   - 新しい台帳の行（DFC-657〜DFC-662 のほかは未検討）: DFC-654（焦点の求めが再試行を使い切っても取り下げられない）、DFC-655（上の型エラー 8 件）、DFC-656（IC-1 に .json を渡すと何も起きない。frame-loop.ts の decodedDocument が JsonFault を捨てる見立て。⚠️ スタブの探り針からの推論 ⇒ 先に実物のピッカーかドロップで再現する）、DFC-657（表 T-261 の GS-4 の理由の文が、表 T-023d のフェードの掴み点と依存線の順と食い違う。仕様待ち —— §1.2 の 7 問の 7 は JDG-218 で決まった（形の外ではフェードが先。GS-4 に例外を置き、理由の文を直す）。2026-09-19 に台帳の行を仕様待ちへ動かした）、DFC-658（依存線の付く先。JDG-196 で決まり仕様待ち —— FR-009 ／ RT-4a とアプリの attachedBar を直す）、DFC-659（FR-013 の「FR-009 と同じ形」の文。JDG-196 で直す向きが決まり仕様待ち）、DFC-660（IC-81 の注と FR-049 が、隠した依存線を選ぶ・消す経路を逆に述べる。裁定待ち —— 7 問と同じ 1 通で問う）、DFC-661（アプリが範囲選択の矩形を透かしと目盛より先に描き、表 T-020 の ZO-6 の最前面と食い違う。コードを読んだだけで走らせていない。裁定待ち —— §1.2 の問い 8 と同じ 1 通で問う）、DFC-662（表 T-020 に行の無い描くもの —— 担当・進捗の札、ダミー、菱形、フェードの □、選択の枠、カーソル類、注記ほか。JDG-203 で向きは決まり、置き場は裁定待ち —— Step 1b の CR で行を足す）。
   - 前の巡から（状態は台帳で確かめた）: 裁定待ち DFC-550・DFC-566・DFC-569・DFC-570・DFC-642。DFC-630・DFC-632（検査 43 が .txt の着地先を数えない）・DFC-637 は実装待ち、DFC-638 は未検討。DFC-639・DFC-641 は仕様の穴（未検討）。DFC-644・DFC-645 は波 3 で実装が入ったと読めるが実装待ちのまま ⇒ 反証させてから動かせ。DFC-646（プロパティパネルの選択の欄のはみ出し、未検討）。DFC-611（依存線を引く間のカクつき、未検討）—— ⛔ 先に探り針 dependency-drag.mjs を直す（ew-resize のカーソルを目印に起点を探して 0 件になる。前の巡の記述。§0 で測り直せ。CR-427 で端点のポインタが画像の矢印になったので、目印はなお合わない見込み）。DFC-594 は保留（JDG-107）。DFC-571 は DFC-558 待ち。異常系は DFC-586（JDG-78、リファクタ一式の後）。JDG-139 の 2 段目（試験の書き直し）と 3 段目（残り 13 個の非公開化と検査 30 の締め）。
   - 検査 55 の tests の基準線（t-023d-the-plan-start-is-the-boundary.test.ts と木の密度）を下げる承認待ち（前の巡の記述。§0 で測り直せ。ae892d1b がそのファイルを書き換えた）。
   - 計画の段 3 の門（JDG-124・JDG-128）とごみ取りの歯止め 4 つ（JDG-161）は、§1.2 の Step 5 で §1.1 へ戻ってから。
6. 毎回の約束（過去の引継と今回までの学び。詳しくは handoff.md の §0.1・§0.2・§2.5〜§2.9）
   - 問う前に rulings.md を grep する。「条項が無い」は §0.2 の 3 手で反証してから。問いは 1 通にまとめ、具体名・推奨・代償を付ける。
   - ⭐ 見本や実装に規則を入れる前にも rulings.md を grep する（§2.9 の ④）。
   - ⭐ UI の大きさ・重なり・札の置き方は、散文で問わず、触れる見本を作って利用者に触らせる（§2.9 の ①）。
   - 前に立つ者は実装・試験・台帳を書かない。体に出す（判断は Opus、機械的な作業は Sonnet）。反証の体には直させない。基準線だけは、利用者の了承のあとで利用者か前に立つ者が直す。
   - 前に立つ者がブリーフに書く事実は仮説である ⇒ file:line で反証させてから使う。
   - 実装の体 ‖ 仕様だけを読む試験の体を並べるときは、新しい欄・引数・関数の名前と型と位置を、両方のブリーフに同じ字面で書く。それでも継ぎ目は割れる（§2.8 の ④）⇒ 合流のあと、赤を原稿で裁く突き合わせの体を 1 体出す。
   - 壊し試験は、合流した木で単体試験の全体を走らせて数える（実装の体だけの壊し試験は 0 件になる）。
   - 基準線を動かす直しは、先に利用者に問う（裁定を着地より先に記録して検査 43 が増えるときも）。⛔ 体に基準線を触らせない。
   - 許可の確認・見張り・分類器に止められた手順は、別の道具や別の殻で回り道しない。止まって報告する ⇒ 消す手順や検査を走らせる手順を渡すブリーフには必ず書く（§2.8 の ①）。
   - 体にリポジトリの外へ書かせない。一時ファイルは scratchpad。
   - check.sh は体が台帳を書いている間に走らせない（偽の赤）。終了コードを読んでから commit する。⚠️ check.sh は型検査を見ない ⇒ npm run typecheck も回す。
   - 個人情報・秘密の検出が 0 でなければ、中身を読んでから commit ／ push する（§2.8 の ②）。
   - 作業木を切る体のブリーフに、セッションの先端の sha と git merge --ff-only <sha> を書く（§2.8 の ③）。
   - 台帳や裁定の行を足したら、commit の前に python docs/spec/_source/row_id_prefixes_json_to_md.py と python tools/ledger_metrics.py を回す（検査 27）。閉じる行は md5 で押さえて fixed-defects.md へ移す。
   - ⭐ 本書や台帳に、行 ID だけを頭の欄に置く表の行を書かない。まだ登録していない接頭辞（表 GA など）には行番号を付けない（検査 27。§2.9 の ⑤）。
   - 性能の測定の前は利用者を呼べ。A/B は段 0 を建て直して同じ日に交互に走らせる（記録 17）。測っている間は重い体を出さない。
   - e2e は体を走らせていないときに通す。揺れる赤は単独で走らせ直してから数える（§2.8 の ⑤）。
   - F11・Esc などブラウザのアクセラレータを通る所作は Playwright では見えない ⇒ 属性ではなく効果を主張させ、残りは利用者の手で確かめる。
   - ⭐ 探り針は、locator.check() ／ uncheck() ／ click() のあとに要素の箱を読み直してからポインタを動かす（ページがスクロールする。§2.9 の ②）。
   - Bash で作業木へ cd すると主作業ディレクトリが移る ⇒ ( cd … ) の部分シェルか git -C を使う。作業木を切る体を出す前に根へ戻る。
   - Windows の python で日本語を print すると cp932 で落ちる ⇒ PYTHONIOENCODING=utf-8 を付ける。スクリプトはヒアドキュメントではなくファイルに書いて走らせる。
   - ⭐ commit の文はファイルに書いて git commit -F <file> で渡す（-m の二重引用符の中のバッククォートはコマンド置換。§2.9 の ③）。
   - tools/probe/harness.mjs の until は関数を Node の側で呼ぶ（DFC-613）⇒ 探り針を書く体のブリーフに書く。
   - git add -u や一括の add はフックが止める ⇒ パスを名指しする。並行の体がいるときは git stash をしない。
7. コミット／push は許可済み（JDG-58）。個人情報の点検の数を報告してから行え（file:// と http:// は絶対パスの検出から除く。dist の束の中の著作権表示の名前も偽陽性）。タグは利用者が打つ。説明は日本語、コードとコミット文は英語 ASCII。
```

---

## 0. **1 通目でやること（順番どおり）**

```
0. docs/development-records/rulings.md を読む   ⛔⛔ ここから始めろ。理由は 0.1
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npx vite build                                ⛔⛔ e2e の前に必ず
4. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ
5. §1 と §2 を読む
```

## 0.1 ⛔⛔ **利用者に問う前に、必ず `rulings.md` を検索しろ**

**利用者の指示（2026-09-08、逐語）:**
> **「これまでも同じ裁定を繰り返している。 何とかしてくれ。非効率すぎ」**

⛔⛔ **同じことを二度問うのは、この巡までに何度も起きた。⭐ 原因は置き場が散っていたことである。**
⇒ **`docs/development-records/rulings.md` が、いまは唯一の置き場である。**

```
問おうとしたら、まずこれを打て
  grep -n "<問おうとしている言葉>" docs/development-records/rulings.md
```

⭐ **注ではなく検査で守らせている**（⛔ 注は守られない。この木の実績値）:
```
検査 43   rulings.md の「適用済」の行の逐語が、docs/spec に実際に在るか
          ⛔ 無ければ赤 —— 「裁定は下りたが、誰も書いていない」を機械が言う
          ⛔ 「⛔ 未着地」の数が基準線を超えても赤
```
⇒ **裁定が宙に浮いたまま巡が終わることが、もう起きない。**

⚠️⚠️ **2026-09-08 に、なぜ二度問うたのかを 1 つ書き残す。⭐ 型が分かる:**
```
利用者   「タスクのダミーは1日とする。 だから = は1日」   ＝ 開始日 = 終了日 は 1 日
設計表    実績の右端 ＝ 開始日 ＋ 長さ（稼働日）          ＝ 開始日 = 終了日 は 0 日
⇒ 同じ言葉が 2 通りに読める。⛔ 体は設計表を読んで「穴は無い」と結論し、
  前に立つ者はそれを受け入れ、⇒ 利用者に同じ裁定をもう一度問うた
```
⛔⛔ **裁定が「守られていない」と見えたら、まず「言葉の意味が 2 通りある」ことを疑え。**
⭐ **利用者の読みが常に正である。⇒ 仕様の言葉のほうを直せ。**

⛔⛔ **`--start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな。**
**基準行はそのファイルの件数ブロックの中に在る。checkout は基準ごと消す。**

**サブエージェントは許可されている**（利用者、2026-08-31 以降くり返し再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
⭐⭐ **2026-09-08 に範囲が広がった**（利用者の裁定、逐語「**実測済みは、当然エージェントを使って移させろ**」）——
**台帳の刈り取りのような機械的な作業も体に出す。⛔ 見分けるのは行数ではなく、機械的かどうかである。**
⇒ **規則 05 の 6 に下ろしてある。**

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**
⭐⭐ **知見は規則へ下ろせ**（利用者の指示 2026-09-03）。⛔ **本書に置いた学びは 1 巡で消える。**
⭐ **2026-09-08 が下ろしたもの**: 規則 04 の **6.7**（作業木の 2 つの罠）、規則 05 の **6**（機械的な作業も体へ）。

---
## 0.2 ⛔⛔ **利用者に渡す前に、「条項が無い」を 自分で 反証しろ**

**利用者の指示（2026-09-09、逐語）:**
> **「なんで同じような裁定をさせるのか？ なぜ一度に裁定が終わらないのか？ 新規の不具合出たのか？ 俺が何度ども指示しているのは既存の不具合を0にしろ、新規の不具合を探すテストはするな、必要なのは修正確認テストだけだ。」**

⛔⛔ **体が「覆う条項が無い」と申告したら、それは 仮説 である。⭐ 実測ではない。**
⚠️⚠️ **2026-09-09 の実測: そうやって利用者へ渡した 4 件のうち、⛔ 3 件は裁定案件ではなかった。**

```
① 進捗マーカーを掴みシロの外へ出す
   ⇒ 表 T-023d の GR-7 が既に「終了点の掴みシロの外側」と定めていた
   ⛔ grep 1 回で分かった。⭐ 利用者の言葉:「これって裁定案件じゃないだろ？」
② 再開アイコンと予定バーのどちらを優先するか
   ⇒ 「拡大すれば予定は編集できる。しかし拡大しても再開はできない」で導けた
   ⛔ しかも前に立つ者の推し（予定バー優先）は 却下 された
   ⭐ 利用者の言葉:「これも非裁定案件。」
③ ダミーの印を押したとき、開始側と終了側のどちらを掴むか
   ⇒ 利用者は既に 2 回「終了を優先」と裁定していた。3 つ目の場面にすぎない
   ⭐ 利用者の言葉:「これも終了優先で変わらず。」
```

⇒ ⛔⛔ **`裁定待ち` に置く前に、この 3 つを打て。⭐ 打った跡を行に書け。**

```
1  grep -n "<その語>" docs/development-records/rulings.md
     ⇒ 利用者が既に同じことを裁いていないか
2  python .claude/skills/spec-graph-check/impact.py <関係する行 ID>
     ⇒ 隣の行が同じ判断を既に下していないか
     ⭐ 実例: 「実績バー本体を掴み領域にしない」は、再開アイコンと同型であった
3  その所作が 他の道 でも行えるか
     ⇒ 行えるなら、優先順は「他の道が無いほう」が勝つ
     ⭐ 利用者の②の裁定は、まさにこの形で下りている
```

⭐⭐ **3 つとも空振りしたときだけ、`裁定待ち` にしてよい。**
⛔ **そのとき行に「この 3 つを打って空振りした」と書け。⭐ 書いていない行は、利用者の時間を使う資格が無い。**

---
## 0.5 ⛔⛔ **基準値は本書に書かない**

**⇒ 第 0 節の 1〜4 を走らせれば、始点はすべて出る。**
⭐ **基準線は、それぞれのファイルの 1 行目が持っている。⛔ 理由はその下に書いてある:**

```
検査 31  .claude/skills/spec-graph-check/stale-blocked-baseline.txt
検査 37  .claude/skills/spec-graph-check/dictionary-table-pairing.txt
検査 39  .claude/skills/spec-graph-check/must-clause-coverage-baseline.txt
検査 40  .claude/skills/spec-graph-check/ruled-elsewhere-baseline.txt
検査 41  .claude/skills/spec-graph-check/decided-spec-baseline.txt
検査 42  .claude/skills/spec-graph-check/quoted-source-baseline.txt
検査 43  .claude/skills/spec-graph-check/ruling-landed-baseline.txt   ⭐ 裁定が着地するたびに下げる
検査 11  docs/review/duplication-baseline.txt
```

⛔⛔ **黙って上げるな。上げるなら、そのファイルの中に理由を書き、コミット文にも書け。**
⭐ **2026-09-08 の実例が、上げ方と返し方の両方を示している** ——
**裁定が MUST を 52 足したので検査 39 を意図的に上げ、同じ巡のうちに 21 返した。**
⭐⭐ **返せた理由は 1 行のブリーフである** —— **「錨が条項を逐語で持てば、そのぶん借金が返る」と体に書いた。**
⇒ **修理の体のブリーフに毎回この 1 行を入れろ。⛔ ただでは返らない。**

---

## 1. ⛔⛔ **利用者が決めた 4 つの段（2026-09-05、逐語）**

> **やることは不具合の全検出。 その後、修正計画/必要なら裁定。
> その後一気に既存不具合を直す。 それが終わったら、仕様やコードのごみ消し。**

⭐ **この 4 つの段は、いまはリファクタの計画の段 0〜8 が引き継いでいる。**
⛔ **段の状態は計画の状態表が持つ。本書に写すな** —— 前の版が写した表は、台帳の残件と食い違ったまま残っていた。
⚠️ **計画の「段」と、この 4 つの「段」は別の番号である。混ぜるな。**

## 1.1 ⚠️ **リファクタの段の順番と現在地（2026-09-17、`5d159a5f` で実測。2026-09-19 に位置の行だけ測り直した）**

⭐ **利用者の指示（2026-09-17、逐語「その中でリファクタリングの順番と現在地も書け。」）で本書に書く。** ⛔ 上の「本書に写すな」の理由どおり腐るので、使う前に計画の状態表と `git log` で測り直せ。
⭐ **§1.2 の段取り（掴み代の仕様）が終わったら、その Step 5 でここへ戻る。**

**2 つの番号の対応:**
- **利用者の 4 つの段**（§1、`JDG-24`）: ① 不具合の全検出 → ② 修正計画と裁定 → ③ 一気に直す → ④ 仕様とコードのごみ消し。
  - ④ のごみ消しは、清掃の巡（タグ `After-Cleanup` ／ `Before-Refactor`、`8701ccf3`）として走ったと読める（推定）。残りはリファクタの計画が引き継いだ（計画の記録 8、`JDG-53`）。
  - ⚠️ §5 の見出しの「段 4」は、この 4 つの段の 4 番目である。計画の段 4 ではない。
- **リファクタの計画の段 0〜8:** `docs/development-records/refactor-plan-report-2026-09-13.md` の状態表と流れ図が持つ。

| 計画の段 | 何をするか | 出口 | 現在地 |
|---|---|---|---|
| 0 足場 | 赤い試験を緑に、dist の焼き直し、基準の記録、割っても残るコードの不具合、CR-374 | vitest の失敗 0、基準を記録 | ✅ `cdba2292` |
| 1 棚卸し | 保存しない状態の一覧 | 状態 1 つに 1 行 | ✅ `90785873`・`698a0f14` |
| 2a 辺の規則 | JDG-47 を設計の 5.2 へ入れる CR | CR が入り、検査が緑 | ✅（推定。CR-378）⚠️ 状態表は ⬜ のまま古い |
| 2b 状態のモデル | ADR-002、状態の SSOT | 同上 | 2b-A ✅（CR-379、`3fa4fa0b`）。2b-B は段 7 へ延期 |
| 3 門 | 辺の許可表、JSON の辺、大きさの歯止め（JDG-54）、モジュールの状態 | わざと破ると赤、今日の基準で緑 | **途中**。門 5 ✅（`2fef6e19`）。門 3・4 は道具だけが在り、check.sh に未配線（56 ／ 57、基準線ファイルが無い）。門 1・2 は未着手（`tools/check_layer_rules.py` がまだ `.json` の import を飛ばす。`agent-api-members.ts` が edit-document を import したまま —— JDG-124 の ①） |
| 4 神関数 | editTask ／ editTaskGroup ／ svgFromSchedule ／ builtMerge を割る | 500 行を超えるのが frameLoop と domScreenSurface だけ | 未着手（段 3 の出口と T-075 の単位の CR を待つ） |
| 5 入力の訳出 | input-command-translator.ts を割る | 名前と署名が不変、試験が不変で緑 | 未着手 |
| 6 画面の面 | dom-screen-surface.ts を割る | 同上 ＋ DOM が同一 | 未着手（DFC-566 の裁定も待つ） |
| 7 状態機械 | frame-loop の状態を状態機械へ移す | 核の let だけが残り、モジュールの状態 0 | 未着手 |
| 8 仕上げ | 部品、図、測り直し | 計画の記録 3 の目標 | 未着手 |

**この巡の位置づけ:**
- 1 本目のセッションの段取り ①〜③（行の軸のズームの上限、縦の折り畳みと画面の部品、大きさの波）は、計画の段 4〜7 で割る関数そのもの（svgFromSchedule、input-command-translator、domScreenSurface、frameLoop）に触る。⇒ **割る前に済ませる**（`JDG-98`、`JDG-127`、計画の記録 7(a)）。段 4〜7 の出口の「試験が不変」「DOM が同一」を守るためである。
- ⭐ **2026-09-17 の 2 本目のセッションで ①〜③ を `scale-and-fold` に入れた（`cdca63ac`）。2026-09-18 に `CR-417`〜`CR-427` の波（`e02d87e7`）と `OP-10` の直し（`f02f4050`）を、2026-09-18〜19 に掴み代の見本（`e1d29dfb`〜`3f714c6e`）を、同じブランチに足した。`refactor` へはまだ戻していない**（利用者が `dist` を見てから、`JDG-162`）⇒ 段 3 の門の基準は、戻したあとの木で測る。⚠️ 段 3 の行（門 1〜4）は `d70e181f` から `scale-and-fold` の先端まで `tools/check_layer_rules.py` と `check.sh` が動いていないので変わらない（2026-09-19 に `git diff --stat` で確かめた）。⚠️ 下の関数の行数は波 3 の前の値である —— 波 3 と 2026-09-18 の波で `frame-loop.ts` などが伸びたので、門を入れる前に測り直せ。
- 段 3 の門を段取りの後ろに置いたのは選択である。段 3 の入口の条件（2a）は満たしている。⭐ 2026-09-19 からは、§1.2 の段取り（Step 1a〜4）がさらにその前に入る。
- ⚠️ 門が無いあいだに関数は育つ: frameLoop は提案のときの 2190 行 → 2352 行。ファイル `frame-loop.ts` の行数は `5d159a5f` で 3883、`cdca63ac` で 3954、`3f714c6e` で 4251（`git show <sha>:src/framework/single-html-shell/frame-loop.ts | wc -l`）。
- ⚠️ JDG-124 の基準は `178e7369` で測った値である（辺 48、関数 76 本で超過 7847 行 ／ 910 分岐、モジュールの状態 2）。`5d159a5f` では関数 78 本、8045 ／ 932 である。⇒ JDG-128 のとおり、門を入れる commit で測り直し、差を波の diff で説明できなければ入れる前に利用者に見せる。`POINTER_SLOP` を Entity へ寄せると辺の 48 も動く。
- ⚠️ 段 4 の出口には、svgFromSchedule（597 行）と editTask（510 行）を 500 行以下にすることも要る。
- `main` は `ac12202e`、`refactor` は `d70e181f`（2026-09-19 に `git rev-parse` で実測）、`scale-and-fold` は本書を書き換えた commit。`main` から `refactor` までの commit の数は `git rev-list --count main..refactor` で測れ。`JDG-61` のとおり、計画の段の出口で `main` へ早送りする。
- ⛔ 計画の状態表の 2a ／ 2b-A の行は古い ⇒ 最初の巡で体に直させる（機械的）。

---

## 1.2 ⭐⭐ 掴み代・ポインタ・札・操作を仕様にする段取り（2026-09-19、JDG-180〜JDG-193）

⭐ **利用者の理由と逐語は `rulings.md` の `JDG-180`〜`JDG-227` が持つ。⛔ 本節は写さない —— 段取りだけを持つ。**
⛔ **順番を入れ替えるな**: Step 0 → 1a（並行して 7 問を問う）→ 1b（仕様案 §8 の残りの答えのあと —— ㉔・㉕・㉖（再開アイコンの案）・群 E の ㉗〜㉞・㉟（マーカーの押下の巡り）。群 A・B・D と 8、群 C（⑥ と 9 の ① を含む）、`--->` の 3 段と ㉓ は 2026-09-19 に答えが出た —— `JDG-209`〜`JDG-221`・`JDG-224`・`JDG-225`。群 C の ② は `JDG-227` で問い直した（㉟））→ 2 → 3 ‖ 4 → 5。

### 振る舞いの正本 —— 見本

- `previous-project-result/16-grab-area-sizing/grab-area-sizing-sample.html`（1 ファイル。ダブルクリックで開く）と、同じフォルダの `README.md`。
- 下の規則は、すべて見本に実装してあり、ブラウザで測ってある。
- 見本は、仕様と実装を突き合わせる実行できる手本であり、仕様の図を起こす描き手でもある。仕様に着地した後に見本と表のどちらが勝つかは、README の「位置づけ」が持つ。
- 見本は 2 枚の窓に分かれる（絵 ｜ 設定。それぞれが自分でスクロールする）。掴み代の値は「掴む対象 × 向き」の表で持ち、表の 1 つのセルは 1 つの対象の掴み代の 1 つの向きだけを動かす。⑤ の行には全部入りのタスク 3 本を 1 行に積み（隙間は `JDG-148` の 1px ＋ 依存線 1.5px ＋ 1px ＝ 3.5px）、依存線は押して選ぶ・消す・構えて引く（引く端は 表 T-018 の左半分 ／ 右半分）ことができる。

### 表・定数・新しい問いの正本 —— 仕様案と対応表（2026-09-19、`de5955bf`）

- 整えた仕様案は `previous-project-result/16-grab-area-sizing/spec-draft-ja.md` である。上位要求（`FR-104`〜`FR-110`。`FR-043` と `FR-009` は書き直し）、8 つの表（XS・RF・LP・GA・HT・PE・PK・ZO）、定数（§5）、図（§6）、追跡の表（§7）、決めていないこと（§8。群 A〜E）を持つ。
- Step 1a の対応表は、同じフォルダの `old-to-new-map-ja.md` である（旧 → 新 425 件: A 189 ＋ B 236）。
- Step 1b の CR に同梱すべき要素の洗い出しは、同じフォルダの `cr-inventory-ja.md` である（2026-09-19、`JDG-222` の指示。23 件、重い順。仕様案の表がまだ覆っていないのに同じ CR へ入れなければならない要素）。利用者の答えが要るものは仕様案 §8 の群 E（㉗〜㉞）、語の置き換えと消す行の文を運ぶだけのものは CR を書くときに入れる（仕様案 §8 の群 E の頭）。⚠️ 表の 19（依存線を構えているあいだのポインタ）は前に立つ者が反証した —— `IN-2` は「構えているときは作図の合図」と書くので、見本の十字は正しい。
- ⛔ **これからは、表・定数と、Step 1a で見つかった新しい問い（群 A〜D）と同梱の洗い出しから出た問い（群 E）は仕様案が持つ。本節は段取りだけを持ち、仕様案の中身を写さない。** 7 問と 8・9 の事実・推奨・代償は、これまでどおり本節が持つ（仕様案 §8 の頭もそう書く）。表・定数・新しい問いについて本節と仕様案が食い違えば、仕様案を正として本節を直す。
- ⚠️ 仕様案も仕様ではない。表の名と行の番号は仮のものであり、CR で登録するまで仕様にも台帳にも書かない（下の Step 1b の 2）。

### Step 0 —— 済み（この巡）

`JDG-180`〜`JDG-193` を記録した（すべて `未着地`）。見本の README を書き直した。本書を書き換えた。同じ日に `JDG-194`〜`JDG-200` を足した（`JDG-198` のほかは `未着地`）。続けて `JDG-201`〜`JDG-204`（すべて `未着地`）と、台帳の `DFC-661` ／ `DFC-662` を足した。続けて `JDG-205`〜`JDG-206`（すべて `未着地`）を足した —— 依存線の既定値（`JDG-205`）とポインタの作り直し（`JDG-206`）。続けて `JDG-207`〜`JDG-208`（すべて `未着地`）を足した —— フェードのポインタの高さと縁（`JDG-207`）とマイルストーンのポインタの円盤化（`JDG-208`）。続けて `JDG-209`〜`JDG-211`（すべて `未着地`）と `DFC-663` を足した —— 仕様案 §8 の群 A（仕様へ戻す）・群 B（推奨どおり）・群 D（推奨どおり）の答え。続けて `JDG-212`〜`JDG-221`（すべて `未着地`）を足し、`DFC-663` を `裁定待ち` から `仕様待ち` へ動かした —— `--->` を 3 段に積む指示（`JDG-212`。マーカーは 1 段目に立つ）、群 C のうち 8 つ（① ② ③ ④a ⑤ ⑦ 9 の ② ㉒）の答え（`JDG-213`〜`JDG-220`）、`--->` の線の太さ・矢じりの長さ・矢じりの高さを独立した設定値にし、2 つの間を描いた線のあいだで測る指示（`JDG-221`。仕様案 §8 の ㉓ の答え）。続けて `JDG-222`〜`JDG-223`（すべて `未着地`）を足した —— 再開アイコンの定義が抜けていた指摘と案・同梱の洗い出し（`JDG-222`。案は仕様案 §8 の ㉖、洗い出しは `cr-inventory-ja.md` と §8 の群 E）、再開アイコンを動かすときのポインタ（`JDG-223`。折れ矢印を小さめに）。あわせて台帳の `DFC-657` を `仕様待ち` へ動かし（`JDG-218`）、`DFC-658` の補足と `PND-476` の推奨の欄に答えと案を指す行を足した。続けて `JDG-224`〜`JDG-227`（すべて `未着地`）を足した —— 7 問の 6（⑥）の答え（`JDG-224`。「入る」の判定は実績の終了の内側だけ取り置く）、問い 9 の ① の答え（`JDG-225`。引き入れのつまみの下限を矢じりの長さに結ぶ。逐語「⑨-2」を 9 の ① の 2 番目の手と読んだ —— 利用者に確かめる）、再開アイコンはいつも実績の右に立つ（`JDG-226`。`JDG-222` の案の ③ を覆す。5 行目の読みは利用者に確かめる）、マーカーを押して未着手へ戻れるようにする指示（`JDG-227`。案は仕様案 §8 の ㉟ で、利用者の答えを待つ）。あわせて `JDG-214` と `JDG-222` の状態欄に注を足した。

### Step 1b の前に利用者へ問う 7 問（1〜6 は 2026-09-19 に出した。7 は同じ日に足した。答えが来たら rulings.md へ）

⭐ `rulings.md` に `JDG-180`〜`JDG-193` を写した体の「要確認」が、この巡の裁定が古い裁定か仕様とぶつかる所を 6 つ挙げた。1 の数は前に立つ者が見本で測った。7 は、前に立つ者が見本に積んだタスクと依存線を入れて試したときに見つけた、仕様の中の食い違いである（`DFC-657`）。
⭐ **7 問は 2026-09-19 にすべて答えが出た**（4 は `JDG-197`、1・2・3・4a・5・7 は `JDG-213`〜`JDG-218`、6 は `JDG-224`）。⚠️ 2 の答え（`JDG-214`）は `JDG-227` で問い直した —— Step 1b の CR は仕様案 §8 の ㉟ の答えを待つ。Step 1a は待たずに進めてよい。
⭐ Step 1a で見つかった新しい問い（①b・①c・⑩〜㉒）と CR の組み方の決めごとは、仕様案（`spec-draft-ja.md`）§8 が群 A〜D に分けて持つ（同梱の洗い出しから出た問いは群 E）。⛔ 本書には写さない。⭐ 群 A・B・D は 2026-09-19 に答えが出た（`JDG-209`〜`JDG-211`）。群 C も同じ日に全部答えが出た（`JDG-213`〜`JDG-220`。⑥ と 9 の ① は `JDG-224`・`JDG-225`）。**Step 1b の CR は、㉔・㉕・㉖・群 E の ㉗〜㉞・㉟ の答えを待つ**（㉔ は `JDG-219` の仕様の単位から、㉕ は `JDG-212` の `--->` の 3 段から、㉖ は `JDG-222` の再開アイコンの案から（置き場は `JDG-226` で決まった —— いつも実績の右）、㉗〜㉞ は `JDG-222` の同梱の洗い出しから、㉟ は `JDG-227` のマーカーの押下の巡りから出た問い。中身は仕様案 §8）。㉓（3 段を矩形より低くする手）は `JDG-221` で決まった。
⛔ **1 通にまとめて問う**（具体名・推奨・代償を付ける。問う前に `rulings.md` を grep する —— §0.1）。
⭐ 答えが来たら、逐語を `rulings.md` に新しい行として足し、覆した古い行の状態を直す（`rulings.md` の規則 ①）。⚠️ 未着地のまま足すと検査 43 の数が増える ⇒ 基準線を上げるのは利用者の手である（1 通目の 2 と同じ）。
⭐ 4 は `JDG-197`（2026-09-19）で決まった。4a は、その答えを見本で測って出た新しい問いである（`JDG-216` で決まった —— 内側の値を持ち、既定 0。`JDG-199` で線の掴み代が細くなったので、掴みやすくする選択肢であり、欠陥ではない）。

1. **ダミーの開始の掴み代が、予定の開始の左へはみ出す**（`JDG-04` ／ `JDG-19` ／ `JDG-37` ／ `JDG-41` とぶつかる） —— ⭐ **決まった: ダミーの掴み代は実績と別の定数、既定は実績と同じ値（印の左半分が開始。予定の開始の左へは出ない）**（`JDG-213`、2026-09-19）
   - 事実（前に立つ者が見本の ② の行を既定値で測った）: ダミーの開始の掴み代は 156〜171.5、予定の開始の掴み代は 156〜168 である。当たりの順ではダミーが予定の端より上なので、行の中の帯（y 152〜166）では、156〜171 のどの点でもダミーの開始が応える。予定の開始が応えるのは、上と下の 2px の帯だけである。
   - ぶつかる所: `JDG-04` と `JDG-19`（利用者は 2 度言った）は「予定の開始の左は予定、右は実績」と定める。`JDG-186` の「実績と同じつかみシロ」も、実績の開始の掴み代は内側（右）へ伸びるので、「予定の開始から右だけ」と読める ⇒ 見本の、外へも左右対称に伸びる掴み代は、前に立つ者の読み違いである見込みが高い。
   - 推奨: 予定の開始を固い境にする。ダミーの掴み代はその右にだけ伸ばす（印の左半分が開始、右半分とその先が終了 —— `JDG-37` の割り方）。
   - 代償: 既定値では、ダミーの開始の掴み代が印の半分（約 2.5〜3.5px）に縮む。

2. **マーカーを押したときの状態の巡りが、表 T-021a と違う** —— ⭐ **決まった: 表 T-021a のまま（推奨どおり）。`PV-1` だけが実績を置き、覚えて戻す道は捨てる**（`JDG-214`、2026-09-19）
   - 事実: 見本の 未着手 → 50% → 100% → 未着手 は、前に立つ者が作った見せ方であり、利用者は選んでいない。表 T-021a は 未着手 → 完了（`PV-1`）→ 中断（`PV-3`）→ 進行中（`PV-4`）→ 完了（`PV-2`）で、`PV-4` は未着手へ戻してはならない（MUST NOT）と定める。
     ⇒ 仕様では、押して未着手に着くことが無い。`JDG-187` の「未着手ではダミーにし、次に押したら覚えた実績を戻す」は起きない。
     ⇒ さらに `PV-1` は、押したときに実績を置く。`JDG-187` の「マーカーを押下して絵も実績を変えるな」とぶつかる。
   - 推奨: 表 T-021a のままとする。`PV-1` を、描く唯一の押しとする（未着手のタスクを完了にするには実績が要る）。覚えて戻す道は、着かないので捨てる。
   - 代償: `JDG-187` の「覚えて戻す」の段が死ぬ。
   - ⚠️ **問い直した**（`JDG-227`、2026-09-19）: 利用者は「・ に戻れるようにしろ」と指示した —— 表 T-021a の `PV-4` の MUST NOT はこの指示そのもので覆る（どの輪でも）。㉟ で決めるのは輪の形だけであり、前に立つ者の案（進み順の輪）は仕様案 §8 の ㉟ が持ち、利用者の答えを待つ。

3. **実績の開始に立つマーカーの左半分が、実績の開始の掴み代に入る**（`JDG-187` と `JDG-27`） —— ⭐ **決まった: `JDG-27` を実績の開始の側について覆す（一部）。重ならないときはマーカー全体がマーカーの掴み代**（`JDG-215`、2026-09-19。見本は反映済み）
   - 事実: `JDG-187` は、実績の開始の掴み代をマーカーの中心で止めた（同行の読みの実測: 実績の開始 168〜175、マーカー 168〜182、中心 175）。⇒ マーカーの左半分は実績の開始の掴み代の中にある。`JDG-27` の残った部分（`JDG-178` の後）は「マーカーはタスクの掴み代の外側」と言う。中心で割る案は、利用者自身が出した（`JDG-187` の三通目）。
   - 推奨: `JDG-27` の状態を「覆された（一部。`JDG-187`）」にする。
   - 代償: なし。

4. **見本の縦の張り出しの既定 0 と、`GS-3` の「隣が無い向きは縮めない」**（`JDG-180` と `JDG-150` ／ `GS-3`） —— ⭐ **決まった: 予定の端の上下の既定は 0**（`JDG-197`、2026-09-19）
   - 答え: 予定の開始・終了の掴み代の上下の既定は 0px（`===` と `--->` の予定の開始・終了 計 4 セル）。表 T-261 の `GS-3`（`docs/spec/01-04-requirements.md:3485`。隣のタスクが無い向きでは掴み代を `S-90`（12px）のまま当てる（MUST））を、予定の端の縦について覆す（`JDG-150` の状態欄に記した）⇒ Step 1b の CR で `GS-3` を書き直す。フェードの掴み点（`S-92`）の `GS-3` は変わらない。
   - 事実: Step 1b の 3 の縦の張り出し（予定の端 0 ／ 実績の端 0）は、`JDG-180` のときの見本の既定値であり、予定の端は `JDG-197` の答えと同じである。表 T-261 の `GS-3` は、隣のタスクが無い向きでは掴み代を `S-90`（12px）のまま当てる（MUST）。いまの見本は既定で `===` と `--->` の予定の開始・終了 計 4 セルの上下を 0 とし（`JDG-197`）、④ の 2 つのボタン（「裁定 0」／「GS-3 の 12」）で 4 セルを書き換えて比べられる（上下は対象ごとの独立したセルである）。⑤ の行には 3 本を `JDG-148` の 3.5px の隙間で 1 行に積んであるので、隣がある向き（3 本のあいだ）と無い向き（上の 1 本の上、下の 1 本の下）を 1 か所で比べられる。
   - 測った数（2026-09-19、前に立つ者が Playwright の探り針で測った。測り方: 列ごとにポインタを 0.5px ずつ下へ動かし、予定の端が応える高さを足した）: FS の依存線が入る ② の行の予定の開始で、予定の開始が応える高さは、線の掴み代が描いた線の縁から 2px（`JDG-199` の既定）のとき、上下 0 なら 13px（バーからいちばん遠い列で 6.5px）、上下 12px なら 37px（18.5px）である。線が出る ⑤ の行の「準備」の予定の終了は、上下 0 で外側の帯の 8.5〜11.5px。⚠️ 線の掴み代が線の中心から 6px（`S-137`）だったときは、② の行で上下 12px なら 30px、0 なら 6px（外側の 4px の列は 15px ／ 3px。前に立つ者が測り、台帳の体が測り直して一致）—— 線の最後の走り（`S-20` × `S-19` ＝ 2 × 7 ＝ 14px）の掴み代が、予定の開始の掴み代の縦の中ほどを取っていた。⇒ 下の 4a はさらに掴みやすくする選択肢である。

4a. **予定の端の内側の掴み代（依存線が付く端で掴めるようにするか）** —— ⭐ **決まった: 内側の値を持ち、既定 0**（`JDG-216`、2026-09-19）。前は未決（`JDG-197` から出た新しい問い、2026-09-19。`JDG-199` のあとは、掴みやすくする選択肢であり、欠陥ではない）
   - 事実（2026-09-19、前に立つ者が Playwright の探り針で測った。測り方: 列ごとにポインタを 0.5px ずつ下へ動かし、予定の端が応える高さを足した）: 依存線は予定の端に付く（`JDG-196`）。上下 0（`JDG-197`）で、線の掴み代が描いた線の縁から 2px（`JDG-199`）のとき、線が付く予定の端は、線が出る所（⑤ の行の「準備」の予定の終了）で外側の帯の 8.5〜11.5px、線が入る所（② の行の予定の開始）で 13px（バーからいちばん遠い列で 6.5px）の高さで応える（線の中心から 6px だったときは 3px ／ 3〜6px）。予定の端に内側 6px の掴み代を持たせると、バーの上に 14〜18px の高さが足される（`GS-1`: 描いた形の上ではそのタスクだけが応え、線は描いた線の画素の上でしか応えない）。
   - 問うこと: 予定の端に内側の掴み代を持たせるか、持たせるなら何 px か（見本の表の `===` の予定の開始・終了の「内側」のセル。既定は 0。`--->` の予定の端は内側のセルを持たず、中央合わせの「幅」で持つ）。⛔ 6px は測るために置いた値であり、利用者は決めていない。
   - 推奨・代償: 問う前に前に立つ者が書く（本書を書いた体は決めていない）。

5. **同じ種別の掴み代が縦に重なるときの、等しい距離の点**（`JDG-190` は横だけ。前の 1 通目の ⑫、`GS-2`） —— ⭐ **決まった: 掴み代の中心までの直線の距離（推奨どおり）**（`JDG-217`、2026-09-19）
   - 事実: `JDG-190` の読みは、決めたのは同じ行の中で横に重なる場合だけで、上下に隣り合う 2 つの形の等しい距離の点と、`GS-2` の距離を中心までに替えるかは決めていない、と書く。
   - 推奨: 両方の軸に 1 つの規則 —— 掴み代の中心までの直線の距離がいちばん近いものが応える。これで ⑫ も閉じる。
   - 代償: Step 4 で、段を積んだ行の場合を測り直す。
   - ⚠️ ⑪（`JDG-210`）で形の外の上下の重なりは縦の距離が先になったので、⑤ の 3 つの選び方は見本の ⑤ の行で 1 点も違わない（「作業」60%、予定の端の上下 12px で測った: 前の見本 188 点 → 0 点、依存線を出すと 88 → 0）。⑤ が効くのは同じ行の横の重なり（`JDG-190`）と、縦の距離が等しいときだけ。

6. **名前が形に「入る」かの判定**（`JDG-183` と `FR-002`） —— ⭐ **決まった: 終了側だけ `S-91` を取る（推奨どおり）**（`JDG-224`、2026-09-19）
   - 事実: 見本は初め、マーカー ＋ 間隔 ＋ 名前の幅 ≦ 基準の幅 で数えた。`FR-002` は両端に `S-23` と `S-91` も取り置くので、名前が端の掴み代の上に載ることが無い。いまの見本は既定で終了側だけ `S-91` を取り、右のペインの「7 問」で「`FR-002` のまま」と「今までの形」に切り替えて比べられる（見本の README の 7 問の表。① の行の名前が実績の中に入る最小の 1 日の幅は、終了側だけ 8px、`FR-002` のまま 10px、今までの形 7px）。
   - 推奨（2026-09-19 に直した —— 見本・README・仕様案と揃えた。初めの推奨は「`FR-002` の取り置きを残す」であった）: **終了側だけ `S-91` を取る**（実績の終了の内側の幅を取り置く）。理由: `JDG-187` はマーカーを実績の開始の掴み代の上にわざと置く（開始の掴み代をマーカーの中心で止める）⇒ 開始側に `S-91` を取り置く `FR-002` の理由（端の掴み代の上に何も置かない）は、もう成り立たない。終了側の取り置きは、名前を終了の掴み代の上に載せないので残す。
   - 代償: 名前が外へ出るのは、今までの見本の形より少し早く、`FR-002` のままより少し遅い。`FR-002` の開始側の取り置きと、その理由の文を消す（対応表 B で「意図して捨てる（開始側）」）。
   - ⚠️ 答えの前（2026-09-19。答えは `JDG-224`）: 利用者はこの問いの意味が分からないとして解説を求めた（前に立つ者がチャットで解説する。`JDG-212` の注）。あわせて、進捗マーカーを出すか出さないかでも名前を形の中に入れるかが変わると指摘した —— 前に立つ者が見本で測った: ① の行の名前が実績の中に入る最小の 1 日の幅は、マーカーを出すと 終了側だけ 8 ／ `FR-002` のまま 10 ／ 今までの形 7px、隠すと 7 ／ 9 ／ 6px。
   - ⚠️ 注（問いにはしない）: 別の体は「マーカーを隠すと名前の左端が動いてよいか（`JDG-08`）」も挙げた。`JDG-08` は `JDG-92`（`CR-380`）が覆し、仕様は既に動かす —— 表 T-243 の結びの `S-63` の段「隠したマーカーのぶんだけ名称ラベルを左へ寄せる」（`docs/spec/01-04-requirements.md`、2026-09-19 に `:1573` で読んだ）⇒ 問わない。
     ⚠️ ただし同じ結びの ⛔ は、予実の表示の切り替え（`S-227` ／ `S-228`）では名前の左端を動かさない（MUST。`:1568`、`CR-380` が残した部分）。`JDG-184` は実績を隠すと札を予定に付けるので、この MUST を覆す読みになる（逐語が明示している）。⇒ Step 1a の対応表で「覆す」行にし、代償を 7 問と同じ 1 通で利用者に伝える —— 予実の表示を切り替えると `OC-1` の算入が変わり、段が組み替わりうる（同じ結びが避けていた事故。見立てであり、見本で測っていない）。
     ⚠️ `JDG-08` の状態欄の「`S-63` に依らず同じ位置に数える（残した）」は、`:1573` の `S-63` の段と食い違って読める ⇒ 状態欄の文を確かめよ。

7. **描いた形の外で、フェードの掴み点と依存線が重なるとき、どちらが応えるか**（表 T-023d と 表 T-261 の `GS-4`。`DFC-657`） —— ⭐ **決まった: フェードが先、次に線（推奨の「線が先」を退けた）**（`JDG-218`、2026-09-19）
   - 事実: 表 T-023d は、フェードの掴み点（`GR-1` ／ `GR-2`）を依存線（`GR-13`）より上に置き、結びで「素の押下の順は本表のとおりである」と書く。`GS-4` は、隙間を通る依存線を先にする理由を「表 T-023d で `GR-13` が `GR-1` 〜 `GR-4` より上に在る順と同じ」と書く —— `GR-3` ／ `GR-4` については真、`GR-1` ／ `GR-2` については偽である。
     前に立つ者が見本の ⑤ の行で測った（台帳の体が測り直して一致）: 上の 2 本のあいだの 3.5px の隙間で、下のバーの予定の開始の 4px 左（見本の座標 488, 411.75）は、フェードインの掴み点（`S-92` の 8 × 8。左上の角に中心を置くので 4px が隙間へ届く）と、依存線の掴み代（`S-137`、左右 6px）の両方に入る。表 T-023d の順ではフェードインが、`GS-4` の文のとおりなら依存線が応える。見本の ⑦ で切り替えて比べられる。
     ⭐ 誤っているのは `GS-4` の理由の文だけである —— `GS-4` の定め（MUST）は「`GS-2` と `GS-3` より先に、依存線とすること」であり、`GS-3` は形の外の掴み代として `S-90` と並べて `S-92`（フェードの掴み点）も挙げる。⇒ 描いた形の外では、仕様は既に、フェードの掴み点の形の外の部分より線を先に置いている。`JDG-150` の読み（逐語ではない）も、利用者が選んだ案 ① の中身の 1 つを「隙間に依存線があれば線が先」と述べる。理由の文は `CR-415` の決定 6 が入れた。
   - 推奨: 描いた形の外では線が先とする（`GS-4` と `JDG-150` のまま）。`GS-4` の理由の文を直し、`GR-1` ／ `GR-2` について表 T-023d の順を引かないようにする —— 表 T-023d が持つのは、描いた形の上の順と、表 T-261 が決めないものすべての順である。
     フェードの掴み点は、8 × 8 の掴み代のうち自分のバーの上に載る 4 分の 1 を残し、そこでは `GS-1` が応えをそのバーのタスクに限るので、線の隣でも掴める（台帳の体が見本の ⑤ の行で測った: ⑦ を「線が先」にすると、下のバーのフェードインの 64px² のうち、自分のバーの上の 16px² はフェードインが応え、形の外の 44px² は依存線が、上のバーに載る 4px² は上のバーが応える）。
   - 代償: 振る舞いは何も変わらない。仕様の 1 文の直し。
   - ⭐ 測り直した（2026-09-19。線の掴み代は描いた線の縁から 2px になった —— `JDG-199`）: 「作業」のフェード入の 8 × 8（64px²）の中で、線が先ならフェード入 28・線 32・上のバー 4px²、フェードが先ならフェード入 60・上のバー 4px²（上の推奨の 16 ／ 44 ／ 4 は、線の掴み代が線の中心から 6px だったころの測り）。

### 同じ 1 通で問う 8・9（2026-09-19、`JDG-201`〜`JDG-203` から足した。8 と 9 の ① ② は決まった）

8. **進捗マーカーを依存線より手前に描くか**（`JDG-203` と 表 T-020 の `ZO-3`） —— ⭐ **決まった: 手前**（`JDG-210`、2026-09-19）
   - 答え: マーカーは依存線より手前に描く（`ZO-3` の今の順を覆す）。あわせての行の無いものの置き場（㉑）と、範囲選択の矩形の前後（`DFC-661`）も、同じ `JDG-210` で仕様案 §4.8 の案のとおりと決まった。⛔ 中身は仕様案が持つ。
   - 事実: 見本（`f15ea18e`）は、マーカーを依存線より手前の層に置いた（利用者の問い「依存線の裏側に … 進捗マーカー … を描いてないか？」からの前に立つ者の読み）。`ZO-3`（`docs/spec/01-04-requirements.md:2715`）はマーカーを順 3（不透明な下地を持つ）、依存線を順 4 に置く ⇒ 仕様では線がマーカーの前である。名前（`ZO-5`）が線の前なのは仕様どおり。
   - あわせて: 表 T-020 に行の無いもの（`DFC-662`）の置き場。材料は `PND-209` ／ `PND-238` ／ `PND-312` の推奨と、見本の層の表。範囲選択の矩形と透かし・目盛の前後（`DFC-661`）も同じ問いである。
   - 推奨・代償: 問う前に前に立つ者が書く（本書を書いた体は決めていない）。

9. **依存線の引き出し・引き入れと矢じりを、別々の値にするときの 2 点**（`JDG-201` ／ `JDG-202`） —— ⭐ **決まった: ① 引き入れのつまみの下限を矢じりの長さに結ぶ（`JDG-225`）、② 割る（`JDG-219`）**
   - 事実: 見本は当初 引き出し 4px ／ 引き入れ 4px、矢じり 長さ 7px × 幅 4.2px とした。仕様の `LF-4`（入口 14px ／ 出口 7px、「出口と入口に別々の設定値を持たない」）、`S-19`（長さ ＝ 底辺 ＝ 走りの 3 役）、`S-20`（下限 1 超）には入らない。⭐ 利用者はその後、既定値をキャプチャの通り 引き出し 6px ／ 引き入れ 10px、矢じり 長さ 6px × 幅 5px と裁定した（`JDG-205`、2026-09-19。見本 `8f6b165c`）。
   - 問うこと: ① 引き入れが矢じりの長さより短いとき、矢じりの付け根が手前の曲がり角より外へ出る —— ⭐ 既定値（引き入れ 10px ＞ 矢じり 6px）については解消した（`JDG-205`）。**なお問う**: 利用者が値を既定より短く変えたときに、そのまま許すか、引き入れの下限を矢じりの長さに結ぶか ② 見本の px（描いた px）を仕様の値にするとき、描く比（`DS-1`、表示の倍率 100 で 0.625）で割るか（割れば 引き出し 9.6px・引き入れ 16px、矢じり 長さ 9.6px × 幅 8px。既定値 6／10／6×5 を 0.625 で割った計算値）。⚠️ ② は見本のほかの px（`DS-1` の群に入る行）にも掛かる —— Step 1a で確かめた: 進捗マーカーの径と札の余白も `DS-1` の群に入る（対応表 A の N13、B の `DS-1` の行。仕様案 §5 と §8 の 9）
   - ⭐ ② は決まった: 割る —— 見本を仕様の単位で書き直した（`JDG-219`、2026-09-19。既定は見た目を保つ値）。① も決まった: 引き入れのつまみの下限を矢じりの長さに結ぶ（`JDG-225`、2026-09-19。逐語「⑨-2 提案通り」を 9 の ① の 2 番目の手と読んだ —— 利用者に確かめる）。
   - あわせて伝える（要確認）: `S-19` ／ `S-20` は文書に保存する 表 T-201 の行で、鍵（`K-19` ／ `K-20`）が保存形に載る ⇒ 値を割ると保存形の鍵が変わる。線の太さ（`S-18`）の範囲を、矢じりの長さと幅のどちらに結ぶか。
   - 推奨・代償: 問う前に前に立つ者が書く。

### Step 1a —— 新しい表が置き換えるものを、先に全部数える（⛔ 新しく書く前に）—— 済み（2026-09-19）

- ⭐ **済み（2026-09-19、`de5955bf`）**: 旧 → 新の対応表は `previous-project-result/16-grab-area-sizing/old-to-new-map-ja.md` である（425 件: A 189 ＋ B 236。2 体が分野を分けて行を開いて読み、`impact.py` で漏れを拾った）。見つかった食い違いから起こした問いは、仕様案 §8 の群 A〜D にある。下は、そのときの段取りの控えである。
- `impact.py`（spec-graph-check の、影響の広がりを出す道具）に、裁定が触る行と要求を種として入れる。新しい表が運ぶ、または変えさせる既存の表の行と要求を、すべて集める。
- 最初の grep の数（⚠️ 分類ではない）: `GR` 22 行、`DM` 13、`OC` 10、`PC` 7、`OR` 6、`GS` 4、`LF-11` 1。
  ⚠️ 初めは `GR` を 23 行、`GS` を 5 行と書いていた —— 数え過ぎであり、登録簿（`docs/spec/_assets/tbl-row-id-prefixes.md` の行数）の 22 ／ 4 が正しい。23 番目の `GR` と 5 番目の `GS` はどこにも無く、過去にも無かった（23 ／ 5 は、表の見出し行を 1 つ足して数えた形と読める。対応表 A の §4.1）。
  全部が消えるのではない —— たとえば `GR-19` はパレットの掴み帯で、この件と関係しない。
- 出力は旧 → 新の対応表である。既存の行・要求 1 つに 1 行で、次の 4 つのどれかを書く（対応表は 5 つ目に「書き直す」—— 残すが中身が変わる —— を足した）。
  - 削除（新しい表のどの行が引き継ぐか）
  - ポインタ化（FR は ID を残し、本文を「表 X に従う」と RATIONALE に縮める）
  - 残す
  - 意図して捨てる（理由）
- ⛔ **対応表に行の無いものは、1 つも消さない。** 一括で消すときに、新しい表が本当は覆っていない規則を落とさないための歯止めである。
- 分類は体、数え直しは前に立つ者。

### Step 1b —— 変更要求を 1 本だけ（⛔ 仕様の体 1 体だけ）

削除と追加を同じ CR で当てる。⛔ 古い散文と新しい表が両方ある時（二重表現）も、どちらも無い時も作らない。
CR が運ぶものは次の 6 つである。
⭐ 6 つのほかに足した 2 つ（2026-09-19）は、仕様案に入った: 表 T-020 を描くものすべての行へ広げる（表 ZO。親は新しい `FR-110`）／ 日程表の上の押す・引くで字の選択を始めない（`JDG-204`。`FR-105` と表 PE の 0 行に置き、`MK-12` に例外を 1 文足す —— 仕様案 §8 の群 D で利用者が了承した。`JDG-211`）。

#### 1. Step 1a の対応表（必須の節）

`old-to-new-map-ja.md` の「削除」「書き直す」「ポインタ化」を、新しい表と同じ 1 本で当てる。

#### 2. 8 つの表 —— 仕様案 §4

⭐ 表は仕様案（`spec-draft-ja.md`）§4 が持つ —— XS（縦の断面）・RF（札の基準）・LP（札の配置）・GA（掴み代の幾何）・HT（どれが応えるか）・PE（押す・引くの効果）・PK（ポインタの形）・ZO（描く順）の 8 つ。どの表がどの表を読むかは仕様案 §3 の図 1。⛔ 本書に表の中身を写さない。

- 6 つから 8 つになった: ポインタの形を、表 GA のポインタ欄から表 PK へ出した（GA のポインタ欄はポインタの名前だけを持つ）。今の 表 T-020（重ね順）を描くものすべての行へ広げたものを表 ZO とした（`JDG-203`・`DFC-662`。応える順の表 HT とは別の表のまま —— `MK-9a`）。ZO の中の問い（マーカーと `ZO-3` の前後は問い 8、行の無いものの置き場は ㉑、範囲選択の矩形と透かしの前後は `DFC-661`）は `JDG-210`（2026-09-19）で決まった —— 中身は仕様案 §4.8。
- 表 HT は、日程の形の中の応える順だけを持つ。表 T-023d は消えず、日程の形でないもの（パレットと面の境の掴み帯など）の順を持ち、日程の形の段を「表 HT に従う」の 1 行で持つ（仕様案 §4.5）。
- ⚠️ 本書が前に書いた HT の順（フェード → マーカー → ダミー → 実績の端 → 予定の端 → …）は、依存線を落としていた。依存線は、形の上では実績の端と予定の端のあいだに、形の外（隙間・外側の帯）では先頭に入る（見本の `HIT_ORDER` と仕様案 §4.5。対応表 A の N16）。

接頭辞は、登録済みの 152 と重ならないことを確かめてある。ポインタの表は `PT` ではなく `PK` とする —— `PT` は登録済みの `PTD`（表 T-023a）と 1 字違いになる。
`GR` ／ `GS` は使い回さない —— `defects.md` と `rulings.md` が `GR-7` などを引いているため。

⛔ **行は CR に書く。本書には書かない。** 本書では「表 GA」などと呼び、接頭辞に行番号を付けた形では書かない —— 接頭辞は Step 1b で登録するので、それより前に書くと検査 27 が赤くなる（§2.9 の ⑤）。

#### 3. 定数

⭐ 定数の群・既定値・置く表は、仕様案 §5 が持つ（掴み代は表 GA の 36 の値 —— 2026-09-19 の `JDG-213` でダミーの開始・終了に内側の値が足された。前は 34。ほかに札・形・依存線・ポインタの群）。⛔ 本書に値を写さない。見本の既定値のまま設定の行にする（`JDG-180`）。

⛔ **置く表は、割る今の行の表に従う。** 文書に保存する表 T-201 の行（`S-18`〜`S-32` など。保存形の鍵 `K-19`〜`K-32` を持ち、描く比が掛かる）を割るときは T-201 に置き、製品の決まりである表 T-206 の行（掴み代・ポインタ）を割るときは T-206 に置く。T-201 の行を T-206 へ移すと、保存形の鍵が消え、描く比も外れる（対応表 B の N-14）。
⚠️ 本書は前に「すべて設定の表 T-206 の行にする」と書いていた（`JDG-180` の読みも T-206 と書く）—— T-201 の行を割るものについては誤りである。この置き場は仕様案 §8 の群 D で利用者が了承した（`JDG-211`。今の行の表のまま）。

コードは生成された定数を通して読む（検査 30）⇒ 1 つのセルを変えれば、1 つの値が変わる。

#### 4. 図

見本を固定の設定で描き、SVG を `docs/spec/_assets` へ書き出して作る。⛔ 手で描かない。どの図を起こすかは仕様案 §6。
⚠️ マーカーの状態図は新しく描かない —— 今の 図 F-018（Mermaid）が表 T-021a の巡りを既に持つので、表 PE はそれを指す（図を 2 枚にしない。対応表 A の N20）。⚠️ 仕様案 §8 の ㉟（`JDG-227`）で巡りを変えるなら、図 F-018 を描き直す（仕様案 §6）。

#### 5. 上位要求（`JDG-193`）—— 利用者が了承した案の文

⚠️ 利用者の答えの逐語は `JDG-193` が持つ。下は、前に立つ者が出して利用者が了承した案の文であり、了承したものの控えとして残す。CR は、この文を広げた仕様案 §2 から起こす。

⭐ 仕様案 §2 は、この文に 2026-09-19 の裁定を足して広げた: `FR-104` に依存線を足した ／ `FR-105` に、日程表の上の押す・引くで字の選択を始めないこと（`JDG-204`）を足した ／ `FR-106` にマイルストーンと依存線のポインタと表 PK を足した ／ `FR-108` に依存線を足した ／ `FR-109` は描く前後を持たず（札でないものを覆えない）、一括で消すと落ちる今の MUST 3 つ —— 札どうしを重ねない（`OR-1`）・再開日が未定の再開アイコンを札の並びに入れる（表 T-243 の結び）・名称をフェードの上に重ねない（`FR-002`）—— を持つ（⚠️ 2 つ目の表 T-243 の結びは `JDG-226` で消す —— 仕様案 §4.3）／ 描く前後は新しい `FR-110` が持つ ／ `FR-009` を書き直す（依存線は予定に付ける）。追跡の表も、仕様案 §7 が 8 つの表と `FR-110`・`FR-009` へ広げた。
⭐ 仕様案 §8 の群 D（`FR-110` を立てる・`FR-002` と `FR-109`・字の選択など、CR の組み方）は 2026-09-19 に了承された（`JDG-211`）。⚠️ 了承したのは群 D の表の 8 つの決めごとである —— この広げた分のうち群 D の表に行の無いもの（依存線・マイルストーンのポインタ・`FR-009` の書き直しなど）は、仕様案 §2 の頭のとおり `JDG-194`〜`JDG-208` の裁定から足したものであり、`JDG-211` は述べていない。

**FR-104 縮小しても、どれも指せる**
- STATEMENT: `GRS` は、表示をどこまで縮小しても、描いているタスク・マイルストーン・進捗マーカーのそれぞれを、ポインタで指して操作できるようにすること。そのために、各操作対象に、他の対象に取られない掴み代を持たせること。掴み代の形と大きさは表 GA、縦の段は表 XS、重なったときの応答は表 HT に従う。
- ORIGIN: GL-008 ／ GL-002
- RATIONALE: 縮小は全体を 1 枚で見るための操作であり、縮小した画面こそ日程を動かす画面になる。縮めると形は近づいて重なるので、描いた形の大きさにだけ頼ると、小さい形や後ろの形を指せなくなる。

**FR-105 重なっても、狙ったものが答える**
- STATEMENT: `GRS` は、ポインタの下で複数の掴み代が重なるとき、応答する対象を 1 つに決め、しかもその決め方を利用者が見た目から予測できるものにすること。決め方は表 HT に従う。
- ORIGIN: GL-008 ／ GL-006
- RATIONALE: 同じ場所を押して毎回違うものが動くなら、利用者は操作を信用できない。FR-104 が「指せること」を、本要求が「指したものが答えること」を受け持つ。

**FR-106 ポインタの形で、何を掴むかが分かる**
- STATEMENT: `GRS` は、ポインタが掴み代に入ったとき、ポインタを、何を掴むか（予定か、実績か、マーカーか、フェードか）と、引いたら何がどちらへ動くかが分かる形に変えること。押しているあいだは、掴んだときの形を保つこと。形は表 GA の「ポインタ」欄に従う。
- ORIGIN: GL-006 ／ GL-008
- RATIONALE: 押す前に結果が分かれば、説明を読まずに正しい所を掴める。予定と実績は同じ場所に重ねて描かれるので、見た目だけでは区別できない。ポインタの形（白抜き／塗り）がその区別を担う。

**FR-107 実績は事実なので、ついでには変わらない**
- STATEMENT: `GRS` は、実績の日付を、利用者が実績を掴んで意図して動かしたときにだけ変えること。ほかの操作のついでに変えてはならない（MUST NOT）。どの操作が何を変えるかは表 PE に従う。
- ORIGIN: GL-008
- RATIONALE: 実績は起きたことの記録であり、予定のように引き直す対象ではない。予定をずらす・状態を変える・行を移すといった軽い操作のついでに実績が動けば、利用者は気づかないまま事実を書き換えてしまう。

**FR-108 見えないものは、掴めず動かない**
- STATEMENT: `GRS` は、表示していない予定・実績を、掴めないようにし、ほかの操作で動かさないこと。表 RF と表 PE に従う。
- ORIGIN: GL-006
- RATIONALE: 画面に無いものが指の下で動けば、利用者はそれに気づけず、取り消すこともできない。

**FR-109 札は読める所に置く**
- STATEMENT: `GRS` は、1 つのタスク・マイルストーンについて、名称・担当者・進捗・進捗マーカーを互いに重ねず、どれも形に隠れて読めなくならない場所に置くこと。置き方は表 RF と表 LP、縦の段は表 XS に従う。
- ORIGIN: GL-001 ／ GL-002
- RATIONALE: 1 行に複数のタスクを置き、全体を縮めて見るほど、字と形は近づく。読めない俯瞰図は価値を失う。
- （対象は 1 つのタスクの中に限る。隣のタスクどうしの札のぶつかりは LOD（G-12）の受け持ち）

**FR-043（書き直し） 未着手でも、実績を直接描き始められる**
- STATEMENT: `GRS` は、実績をまだ持たないタスク・マイルストーンにも、実績を描き始める掴み場所（ダミー）を示すこと。ダミーは「まだ始まっていない実績」として、実績と同じ規則で置き、掴ませること。表 RF・GA・PE に従う。
- ORIGIN: GL-008
- RATIONALE: 実績の入力を、タスクを掴むのと同じ直接操作で始められるようにする。ダミーに独自の規則を持たせないことで、規則の数が減る。

どの上位要求がどの表を持つか（⭐ どの表にも親があり、どの要求も表に届く —— どちらも機械の検査にする。Step 4）:

| 上位要求 | GA | HT | RF | LP | PE | XS |
|---|:-:|:-:|:-:|:-:|:-:|:-:|
| `FR-104` どれも指せる | ● | ● | | | | ● |
| `FR-105` 狙ったものが答える | | ● | | | | |
| `FR-106` ポインタの形 | ● | | | | | |
| `FR-107` 実績はついでに変わらない | | | | | ● | |
| `FR-108` 見えないものは掴めない | ● | | ● | | ● | |
| `FR-109` 札は読める所に | | | ● | ● | | ● |
| `FR-043` 未着手でも実績を描き始める | ● | | ● | | ● | |

ORIGIN には、ユースケースの段も名指す。置き換える FR（`FR-013`、`FR-043`、`FR-075` …）の ORIGIN から、Step 1a で拾う（拾った ORIGIN は仕様案 §2 の表の ORIGIN 欄と、対応表 A の §3・B の §4）。⚠️ `FR-053`（コマンドパレット）は前にここへ数えていたが、新方式が触るものは無い（対応表 A の §1.10）。

#### 6. 理由 —— 5 つの原則

1 原則に 1 つの理由を書く（行ごとには書かない）。仕様は JDG を引き、⛔ 利用者の言葉は写さない。
⭐ 5 つの原則と、束ねる裁定・他人向けの理由は、仕様案 §1.2 が持つ（2026-09-19 に `JDG-194`・`JDG-196`・`JDG-203` を束ねに足した）。⛔ 本書に写さない。

#### CR が決めること（⚠️ 利用者はまだ裁いていない）

- 暗い実績の中に描く名前の色 —— FR-109 に要る。表 LP に列を足すか、保留するか。
- FR-107 を独自の目標に掛けるか —— 案は GL-008 の下の制約として置いている。
- 上下に隣り合う 2 つの形から等しい距離の点をどちらが取るか、`GS-2` の距離を中心までの距離に替えるか —— `JDG-190` の読みが「決めていない」と書く（`JDG-150` の状態欄に残した問いと同じ）。⇒ 上の「7 問」の 5 で問う。⭐ 決まった（`JDG-217`、2026-09-19）—— 掴み代の中心までの直線の距離。

⭐ CR の組み方の決めごと（表 HT の範囲・引いて置く値・描く順の親・`FR-002` と `FR-109`・字の選択・定数の置き場・見えないものを掴めないことの正・接頭辞）は、仕様案 §8 の群 D が案を持つ（2026-09-19 に利用者が了承した。`JDG-211`）。

### Step 2 —— 正規化

- 表を、行の数が減らなくなるまで因数分解する。原則 → 行、行 → 原則の両向きで、対応が欠けていないことを確かめる。
- 利用者が見た例: 素朴に札の置き方を決めると、形 3 × 実績を表示 × 実績がある × マーカー × 入る ＝ 48 通りになる。「ダミーはまだ始まっていない実績」で割ると、表 RF（3 行）と表 LP（8 行）の 11 行になり、ダミーは特別扱いでなくなる。

### Step 3 ‖ Step 4 —— 仕様だけを読む試験の体と並べて実装する

- 実装の体と、`docs/spec` だけを読む試験の体を同時に走らせる（確立した形。⛔ 新しい継ぎ目の名前を、両方のブリーフに同じ字面で書く）。**表の 1 行 ＝ 試験 1 件。**
- Step 4 は組み合わせの掃引である: 予定を表示 × 実績を表示 × マーカー × 入る × 形（=== ／ ---> ／ ◆ ／ ダミー）× ズームの両端 × 実績が予定より早い ／ 遅い。
- あわせて、次の 4 つの不変条件を `check.sh` に配線する（⛔ §5.1 の 3 つ —— 規則が書かれているか、数えてから基準線、わざと壊して赤 —— を守る）。
  1. どの表も、上位要求に持たれている。
  2. どの上位要求も、表に届く。
  3. 判定表は条件の空間を分割する（隙間なし・重なりなし・don't care は展開する）。
  4. どの形も、ズームの両端で空でない応答領域を持つ。⭐ 前に立つ者が手で測ったもの: 隣のマイルストーン 2 つはちょうど中点で分かれる。最小の日幅でも、予定は実績の上下に帯を残す（数は見本の README の「実測」）。

### Step 5 —— リファクタへ戻る

§1.1 の計画へ、止めた所から戻る。⚠️ 戻る前に、`scale-and-fold` を `refactor` へ早送りするか（`JDG-162`。利用者が `dist` を見て了承してから）を利用者に問う。

---

## 2. ⚠️ **この巡（2026-09-14、`c6ebfd88..` から `refactor` の先端まで）が身銭で買ったもの —— 規則へ下ろす候補**

⛔ **本節は置き場ではなく、下ろす前の控えである。** 下ろすのは前に立つ者。⛔ 下ろしたら項ごと消せ。
⭐ **前の版の ①〜⑤・⑦・⑨〜⑪ は `c6ebfd88` が規則へ下ろした**（同コミットの本文が一覧を持つ）。前の版から残したのは ⑫（いまの ⑥）と ⑥（いまの ⑦）で、⑧ は新しい規則が要らないので消した。どれも 2026-09-14 に `docs/development-rules/` を grep して、まだ無いことを確かめた。

```
① ⛔ 「終わった」と返した体が、わざと壊した版を作業木に残し、裏で Playwright を 2 本走らせ続けていた（T5b）
   前に立つ者の e2e と同時に走り、ブラウザを取り合った。写した版は壊す前のものだったので入らなかった
   ⇒ 体の最後の報告が「走行が続いている」「戻す」で終わっていたら、完了ではない。TaskStop で止め、
     node の Playwright の走行が残っていないことを数えてから e2e を回す。
     写す前に、体の作業木の現物と、写す版を diff する
   下ろす先  規則 05 の 9（体の報告を信じない）＋ 規則 04 の 6（e2e を単独で回す）   ⭐ 下ろす価値あり

② ⚠️ 仕様の表の行を 1 字直した（検査 46 の改行）波が、その表から作る生成物（icon-roster.json）を作り直さず、
   3 コミット後の全ゲートで gen:check・検査 27・検査 37 が同時に赤くなった
   ⇒ docs/spec/_assets の表を触ったコミットは、同じコミットで npm run gen を回す（仕様だけのコミットでも）
   しかも、生成物だけを別コミットに入れると、上流の無いブランチでは検査 41 の罠 3 が「手で直した」と誤って赤くなる
     （原稿が同じ差分に無いため。上流を持つ refactor へ入れると消えた）
   下ろす先  規則 02 の末尾（仕様だけのコミットの前に回すもの）   ⭐ 下ろす価値あり

③ ⚠️ 古くなった試験を直す体が、元の試験に無い主張（入口の無い項目にも図形を求める）を足し、正しい実装を赤くした（DFC-105）
   ⇒ 修理のブリーフに「元の試験が主張していなかったことは足すな。足すなら、それを名指す条項を書け」を入れる
   下ろす先  規則 05 の 9（ブリーフの型の 9「条項が無いなら発明するな」の隣）

④ ⚠️ 起草の体 2 体が、同じ新しい表番号 T-247・T-248 を取り合った（CR-378 と CR-379）
   ⇒ 新しい番号を取る起草を並行に出すなら、ブリーフで番号の帯を割り当てる
   下ろす先  規則 02（表番号の振り方）

⑤ ⚠️ 体が報告した合計（61）が、同じ報告の中の群ごとの数（56）と合わなかった
   ⇒ 数は報告の文ではなく、体が書いたファイルから数え直す
   下ろす先  規則 05 の 9（体の報告を信じない）

⑥ ⚠️ ヘッドレスの Playwright は、ファイルを開いたあとに立つ面（IC-71〜73・IC-95〜97）に届かない
   ネイティブのファイル選択は操作できず、落とす道は何も起きない（DFC-569）
   下ろす先  規則 04 の 6（測り方の罠）

⑦ ⚠️ 個人情報の数えの「秘密」の語は、この木では誤検知ばかりである
   （display 語の watermark unlock password、type="password" の入力欄、雛形の作業名 token exchange、試験の GLYPH_TOKEN）
   ⇒ 0 と言う前に当たった行を開いて読む。§9 に既に同じ注がある ⇒ 新しい規則は要らない

⑧ ⛔ 台帳の行を書いた体が、状態と中身を取り違えた（2026-09-14、DFC-588・DFC-589）
   仕様が禁じている import を「禁じていない」と書き、表の置き場を別のファイルと書き、2 行とも 裁定待ち にした
   刈り取りの体も、取下げ の行を 実測済 と報告した
   ⇒ 台帳の行を体が書いたら、前に立つ者が行を開いて読む。裁定待ち は §0.2 の 3 手の跡が無ければ差し戻す
   下ろす先  規則 05 の 9（体の報告を信じない）   ⭐ 下ろす価値あり

⑨ ⚠️ 仕様のセルとコードの公開名が同じ波に要る変更要求（表 T-064 の行と公開エントリ）は、
   仕様・実装・試験を別々の作業木で並行に出し、合流用のブランチで重ねると衝突 0 で入った
   ただし試験の体は実装の署名を知らないので、合流の後に「試験を署名に合わせる締めの体」が必ず要る（§2.6 の ⑤ と同じ代償）
   下ろす先  規則 05 の 7（波の型）

⑩ ⚠️ 型の変換（as unknown as）で手組みした試験の値は、型に必須の欄を足しても型検査で落ちず、描く所で落ちる
   ⇒ 型を広げる体のブリーフに「as unknown as で組んだ試験の値を grep で数えよ」を入れる
   下ろす先  規則 05 の 9（ブリーフの型の 4「同じ形の枝を数えよ」の例）

⑪ ⚠️ 基準線を超える変更要求を夜間に当てるときは、refactor ではなく合流用のブランチに止めて朝に問う
   コミットの門（§9）を割らずに、次の変更要求を積み上げられた
   下ろす先  規則 05 の 7（波ごとにコミットする）の隣

⑫ ⚠️ 台帳の「壊しても赤が 0」は、何ファイルを走らせて測ったかで変わる（2026-09-15、DFC-587）
   行は 5 ファイルだけで 0 と書いていたが、単体試験を全部走らせるとヘルプとパレットは既に 11〜52 件赤くなった。穴はパネルだけだった
   ⇒ 壊し試験は全体で走らせ、測った範囲を行に書く
   下ろす先  規則 04 の 6（測り方の罠）

⑬b ⛔ 体が「裏の走行（Playwright・壊し試験の台本・check.sh）を待つ」と書いて止まる（2026-09-15、4 回）
   体は自分では起きない。⇒ 前に立つ者が、その作業木の名を含むプロセスが消えるまで裏で見張り（until ループ）、
   消えたら SendMessage で「走行は終わった、報告を仕上げよ、もう走行を始めるな」と送る
   ⚠️ SendMessage で足した仕事は、最初の報告（足す前の分）が先に届き、足した分の報告は 2 通目で来る —— 1 通目で取り込むな
   下ろす先  規則 05 の 9（体の報告を信じない）   ⭐ 下ろす価値あり

⑭ ⚠️ 実装の体と「仕様だけを読む試験の体」を並べると、仕様の穴が両側から同時に出た（2026-09-15）
   CR-380 の矛盾（マーカーを数える位置）を実装の体と試験の体が別々に見つけて止まり、仕様の波 3 で閉じた。
   さらに試験と実装が食い違った赤は、反証の体に FALSE / TRUE / 誤診 で裁かせると、誤診（仕様の穴）が 3 件出た
   ⇒ 実装と試験の食い違いは、直す前に反証の体へ。穴は小さな仕様の波で閉じ、実装・試験の体へは署名と読みだけを伝える
   下ろす先  規則 05 の 7（波の型）

⑮ ⚠️ 作業ディレクトリが scratchpad に移ると、作業木を切る Agent が「リポジトリではない」と断る（2026-09-15）
   Bash で scratchpad へ cd したまま次の呼び出しをすると、主作業ディレクトリが移る ⇒ 体を出す前にリポジトリの根へ cd で戻す
   下ろす先  規則 04 の 6.7（作業木の罠）

⑯ ⚠️ 検査 11 は、仕様の文が大きく増えると、中身の変わらない既知の群を崩して赤くなる（共通語のしきい値 COMMON_GRAM）
   ⇒ 赤を新しい重複と読む前に、最後に緑だったコミットで同じ検査を回し、群の顔ぶれを比べる
   下ろす先  規則 04 の 6（測り方の罠）
```

---

## 2.5 ⛔⛔ **段取りの型（⚠️ 規則へまだ下りていない分）**

**利用者の指示（2026-09-08、逐語。`JDG-12`）**:
「**エージェントに実装と単体テストをやらせて、すべて終わったら、一気にブラウザを操作する実測テストをやればいいだろ？**」

### ⭐ 波の型（2026-09-08 に 15 体で回した形。⛔ 順序を入れ替えるな）

```
波0 反証   1 体につき 5〜8 行。⛔ 直すことを禁じる。FALSE / TRUE / 誤診 の 3 択で裁かせる
波1 裁定   波0 が挙げた問いを 1 通にまとめて利用者へ。⛔ 小出しにしない
波2 仕様   ⛔ 1 体だけ。直列（規則 05 の 6）。⛔ 体に並行で書かせると同じ文書が壊れる
波3 修理   フォルダで割って並列。⛔ 同じファイルを 2 体に渡すな
波4 試験   仕様が変わって古くなった逐語を持つ試験を直す。⭐ 波3 と同じ巡に必ず来る
波5 実測   出荷ビルドを 1 回起動して、まとめて押す
波6 台帳   機械的なので体に出す（利用者の裁定 2026-09-08）
```

⛔ **波ごとにコミットしろ**（理由は規則 05 の 7「波を出す前に commit する」）。

### ⛔ 合流の作法（⭐ 2026-09-08 は 15 回、2026-09-14 も全部 SAFE で事故 0 であった）

```
体の作業木の「変更あり」は、その体の基準に対する差である。⇒ 丸ごと写すと幹の仕事が消える。
先に数えろ:
  for f in $(git -C <wt> status --porcelain | grep -v '^?? ' | awk '{print $2}'); do
    git diff --quiet <体の基準 sha> HEAD -- "$f" && echo "SAFE $f" || echo "PATCH $f"
  done
SAFE なら cp、PATCH なら:
  git -C <wt> diff -- <file> > <scratchpad>/x.patch
  git apply -3 <scratchpad>/x.patch
```

⭐ **体の持ち場をフォルダで割っておけば、ほぼ全部 SAFE になる。**⇒ **割り方が合流の費用を決める。**

### ⭐ 体に渡すブリーフの型（⛔ この 10 行を毎回書け）

```
1  git merge --ff-only <sha>            ⛔ 1 行目に。忘れると行番号が全部ずれる
2  親から docs/spec/output/ と docs/reference/mspdi/ を写せ  ⛔ 無いと偽の赤（規則 04 の 6.7）
3  編集してよいファイルを列挙し、⛔ 同時に走っている体の持ち場を名指しで禁じる
4  「同じ形の枝が他にいくつあるか、書く前に数えろ」
5  「わざと壊して何件赤くなるか測れ。⛔ 0 なら 0 と正直に書け」
6  「対照を書け ——『逆に壊れていたら何が通ってしまうか』」
7  検査は終了コードで判じよ。⛔ FAIL を grep するな
8  基準線ファイルを触るな。⭐ ただし錨が条項を逐語で持てば借金が返る
9  ⛔ 条項が無いなら発明するな。止まって報告せよ。⭐ 止まるのは失敗ではない
10 許可の確認や分類器に止められたら、止まって報告せよ。⛔ 別の道具で回り道するな。一時ファイルは scratchpad に置け（§2.8 ④）
```

---

## 2.6 ⚠️ **前の巡の罠のうち、規則へまだ下りていないもの**

⭐ **2026-09-14 に `docs/development-rules/` を grep して、下りていないことを確かめたものだけを残した。**
⛔ **規則へ下ろしたら、ここから消せ。**

```
①  出荷ビルドを押す探り針（2026-09-10）
    フレームは rAF で描かれる。Agent API の書き込みだけでは 1 枚も来ない ⇒ 書いたあとポインタを 1px 揺らす
    focusTask はタスクの開始を画布の左端へ寄せる。そこはコマンドパレットの下である ⇒ 焦点は 1 度だけ当て、日付を右へ動かす
    雛形のタスクは年単位なので、窓に中央が入らない ⇒ 1 か月に仕立て直す

②  日本語の逐語は、折り返しで 2 行に割れると黙って効かなくなる（検査 39 / 42 は文字の並びで照合する）
    ⇒ 同じ言葉を途切れずに 1 行で書く
    ⚠️ tests/ のコメントはいま ASCII だけである（JDG-62）。効くのは試験の文字列と、門より前に書かれた試験である

③  設計の数を丸めると、不変条件を割ることがある ⇒ 数ではなく導出を書け
    ⚠️ 実例（2026-09-10）: S-5 の 0.57 は 16 ÷ 28 の丸めで、S-4 の既定が自分の下限を割った

④  「同じ形の枝が他にいくつあるか」は、読むのではなく数えさせろ
    ⚠️ 実例（2026-09-10）: ブリーフが 4 行を挙げたが、体が全行を掃くと同じ形の行はその 3 倍を超えた

⑤  複数の体を同じコミットから並行に切ると、継ぎ目に必ず借りが残る（型検査の赤など）
    ⇒ 並行の代償は「締めの体を 1 体余分に出すこと」だと見積もれ

⑥  作業木が積み上がると、幹でだけ Playwright が崩れる
    ⇒ 幹の赤を退行と読む前に、同じコミットを新しい作業木で走らせろ。古い作業木を掃くのは巡の頭の安い仕事である

⑦  言葉で立たない設計は、図で立てる。却下の理由が次の案の設計条件である
    ⇒ 図に数を書くなら、その数で描け。⛔ 比は内容である（違う比で描くと、利用者は設計ではなく図を叩く）

⑧  新しい規則を書く前に、既にある行の値を疑え（法則が仕様に在って、値だけがそれを殺していることがある）

⑨  S-93 は退役した。change-request/ と検査の基準線ファイルに残る S-93 の語は履歴である ⇒ ⛔ 消すな
    ⭐ md-checks.py の退役した行 ID の名簿が黙らせている

⑩  docs/review/duplication-baseline.txt の T-023d まわりの 3 群は、撤回の記録を逐語で残したことによる
    意図した重複である ⇒ ⛔ 畳むな

⑪  tests/unit/fr-085-double-click-a-row-name.test.ts は、全体走行でだけ落ちることがある（間欠）
    ⇒ 1 ファイルで走らせてから退行と読め。⛔ 通ったからといって直ったとも言えない
```

---

## 2.7 ⚠️ **作業の作法（2026-09-16 に分かったこと）**

⛔ **規則へ下ろしたら、項ごと消せ**（§2 と同じ）。

```
①  利用者は根の dist/index.html を file:// で開いて試す
    ⇒ 合流は別の作業木（例 .claude/worktrees/wave-cr389）で組み、根は refactor のまま置く
    ⇒ refactor を進め、根の dist を npx vite build で作り直してから「試せる」と言う
    ⭐ そのとき ブランチ・commit・dist/index.html の sha256 を添える

②  .claude/worktrees/ の下の作業木は、node の解決が根の node_modules まで上がるので、
    junction を張らなくても vitest が走る
    ⛔ junction を張るな —— 作業木を消すときに根の node_modules を消す事故のもとである（掃き方は規則 04 の 6.11）

③  性能の A/B の探り針（zoom-row-axis.mjs・dependency-drag.mjs）と回し方（run-ab.py）は
    docs/development-records/measurements/frame-time-ab-2026-09-16/extra-zoom-row-axis-and-dependency-drag/ に在る
    ⚠️ 1920×1080 と 1920×400 で二分法の道が違う —— 1080 では字の上限が先に効いて二分法は走らず、
       400 では行の領域の上限が効いて走る（記録 17 の追補）⇒ 行の軸のズームは両方の窓で測れ
    ⛔ 性能の測定の前は利用者を呼べ

④  e2e の既知の赤は NFR のフレーム時間の門 2 件である（2026-09-16 の値。いまの赤は 1 通目の 0 が持つ）
      tests/nfr/nfr-001-010-011-013-the-rest-of-chapter-7.test.ts
      tests/nfr/nfr-002-003-frame-time-is-the-interval.test.ts
    ⛔ 合流の条件は NFR 以外の赤 0（数え方は §4）

⑤  tools/probe/harness.mjs の until は、関数を渡すと Node の側で呼び、文字列を渡すとページで評価する（DFC-613）
    コメントは「関数はページの中で評価する」と読める ⇒ until(() => typeof window.x …) は例外を飲んで常に false になり、
    Agent API が起きないと誤読させる（2026-09-16、体が 2 回誤読した）
    ⛔ 探り針を書く体のブリーフに必ず書け（DFC-613 が直るまで）

⑥  実装の体と仕様だけを読む試験の体を並べるときは、新しい欄・引数・関数の名前と型と位置を
    両方のブリーフに同じ字面で書く（2026-09-16、RowTitle.fontPx と frameLoop の 12 番目の引数 FullScreenHost ／ fullScreenChanged）
    ⭐ 2 波とも合流の衝突 0。⚠️ 型の名前を export するかは検査 26b で決まる ⇒ 試験には型名を import させず、構造の型で渡させる

⑦  実装の体の壊し試験は、試験の体の試験がまだ無いので 0 件になる（2026-09-16、2 波とも全部 0 だった）
    ⇒ 壊し試験は、合流した木で単体試験の全体を走らせてから数える（同じ壊し方で 2〜9 件赤になった）

⑧  台帳や裁定の行を足すと、docs/spec/_assets/tbl-row-id-prefixes.md（records の行を数える生成物）がずれて検査 27 が赤くなる
    ⇒ 台帳・裁定を触った commit の前に python docs/spec/_source/row_id_prefixes_json_to_md.py を回す
    ⚠️ 裁定を着地より先に記録すると検査 43 の数が増える ⇒ commit の前に利用者に基準線を上げてよいか問う（2026-09-16 は 6 → 9、着地で 8）

⑨  Bash で作業木へ cd すると、セッションの主作業ディレクトリがそこへ移る
    ⇒ 作業木では ( cd <作業木> && … ) の部分シェルか git -C <作業木> を使う。作業木を切る体を出す前に根へ戻る

⑩  ブラウザのアクセラレータを通る所作（F11、Esc による全画面の解除）は Playwright では見えない
    DFC-104 の実測と nfr-004 の網羅は、DOM の属性の反転を「効いた」と数え、全画面が効かないことを見逃した
    ⇒ 属性ではなく効果（document.fullscreenElement など）を主張させ、残りは利用者に手で確かめてもらう（DFC-609 は利用者の確認で閉じた）
```

---

## 2.8 ⚠️ **2026-09-17 の 2 本目のセッションの控え —— 作法の事故（規則へまだ下りていない）**

⛔ **規則へ下ろしたら、ここから消せ。**
⭐ 1 本目のセッションの控え（利用者の指示と答え、問い、実測）は下りたので消した —— 指示と答えの逐語は `rulings.md` の `JDG-144`〜`JDG-167`、実測と決定は `change-request/` の `CR-401`〜`CR-416`、潜在の不具合は `defects.md`（`DFC-642`・`DFC-644`・`DFC-645` ほか）、性能は `docs/development-records/measurements/frame-time-ab-2026-09-17-scale-and-fold/results.md` が持つ。1 本目の作法の学びのうち、回り道・外への書き出し・ブリーフの事実は 1 通目の 6 に、検査 43 の `.txt` は `DFC-632` に移した。

```text
①  体が check.sh を Bash の見張りに止められたあと、PowerShell から bash.exe を呼んで走らせた（回り道）
    ⇒ 止められた手順は、別の道具でも別の殻でも回らない。止まって報告する。ブリーフに「どの殻でも」と書け
②  前に立つ者が 453f3f7b を commit・push したあとで、秘密の検出 3 件の中身を読んだ（3 件とも無害 —— 画面の語と設定の鍵の名）
    ⇒ 検出の数が 0 でなければ、中身を読んでから commit する。公開の push は後から消しても履歴に残る
③  体の作業木が、セッションの先端ではなく main（ac12202e）から切られた（ff の門で治った）
    ⇒ 作業木を切る体のブリーフに、先端の sha と git merge --ff-only <sha> を書け
④  実装の体 ‖ 仕様だけを読む試験の体の継ぎ目が、名前を両方のブリーフに揃えても割れた（波 3 の合流で赤が多数）
    ⇒ 突き合わせの体を 1 体出し、赤ごとに原稿（docs/spec）でどちらが正しいかを決めさせた（cdca63ac）。合流のあとの突き合わせを波の型（§2.5）に入れる候補
⑤  負荷の下の e2e は遅く（1 回 18〜20 分）、揺れた（SWS-8 の未答の行が回ごとに変わり、DFC-180 は単独では緑）
    ⇒ 赤を数える前に、揺れる件を単独で走らせ直す。通しは体を走らせていないときに 1 回だけ
⑥  （1 本目から持ち越し）「vite build が通ったから node_modules はつながっている」は偽だった（MSYS の mklink /J が壊れる）
    ⇒ junction は PowerShell の New-Item -ItemType Junction で張り、playwright の解決と msedge の起動で確かめる
```

---

## 2.9 ⚠️ **2026-09-18〜19 の控え —— 触れる見本で UI の数を決めた巡（規則へまだ下りていない）**

⛔ **規則へ下ろしたら、ここから消せ。**
⭐ 2026-09-19 に `docs/development-rules/` を grep して、まだ無いことを確かめた（③ だけは、規則 04 の 6.9 が同じ罠を別の場所で持つ）。

```text
①  触れる見本は、散文では決まらない UI の数を決める（2 日で 14 の裁定、どれも測ってある。JDG-180〜JDG-193）
    作り方: 大きさをすべてつまみ（スライダー）にした 1 ファイルの HTML ／
            掴み代の値は操作対象ごとに独立させる（対象 × 向きの表。1 つの値がほかの対象を動かさない）／
            掴み代は縁取りなし、掴む物と別の色相の薄い面を形の上に重ねる（破線は見にくい、と利用者が言った）／
            絵と設定は別々のペインにし、あいだの棒で設定の幅を変えられるように、どちらも縦横に別々にスクロールできるようにする ／
            どの掴み代が応えたかを出す表示 ／ ポインタを行に沿って掃き、どの掴み代がどこで応えるかを印字する Playwright の探り針
    ⇒ UI の大きさ・重なり・札の置き方は、散文で問わず、先に見本を作って触らせる
    下ろす先の候補  規則 05 の 7（問う前に、そもそも問ってよいかを見る）の隣

②  Playwright の探り針: locator.check() ／ uncheck() ／ click() はページをスクロールする
    ⇒ その前に測った要素の箱は古い。ポインタを動かす前に、毎回箱を読み直す
    この巡の読みのうち 2 つは、箱を読み直すまで誤っていた
    下ろす先の候補  規則 04 の 6（測り方の罠）

③  Bash: git commit -m "…" の二重引用符の中のバッククォートはコマンド置換である
    === や ---> をバッククォートで囲んだ commit の文が、黙って切れた（push の前に気づき、amend で直した）
    ⇒ commit の文はファイルに書き、git commit -F <file> で渡す
    規則 04 の 6.9（二重引用符の中のバッククォートは shell が実行する）と同じ罠である
    ⇒ 新しい規則は要らない。6.9 に commit の例を 1 行足す

④  前に立つ者が、rulings.md を grep せずに、マイルストーンのダミーの規則を見本に実装した
    JDG-05（2026-09-08）が既に同じことを持っていた
    ⇒ 台帳を先に読む規則（§0.1）は、問うときだけでなく、見本や実装に規則を入れるときにも掛かる
    下ろす先の候補  規則 05 の 10（台帳の行が「無い」と言ったら、まず探せ）

⑤  docs/spec/_source/row_id_prefixes_json_to_md.py は docs/development-records も歩き（:78）、
    表の行のうち、頭の欄が行 ID だけの行を数える（:87。コードフェンスの中は数えない）
    ⇒ 本書を含む台帳に、行 ID だけを頭の欄に置く表の行を書くと、生成（gen:check と検査 27）が落ちる ——
      未登録の接頭辞なら「どこにも登録されていない接頭辞」、仕様の接頭辞（FR など）なら「2 つの木で 2 つの意味」として（problems()、:492〜）
    ⇒ 本書の表は、頭の欄に行 ID だけを置かない。§1.2 の持ち合いの表は、ID の後ろに語を添えてある
    （2026-09-19 に本書を書き換えた体が、生成器の walk() を本書だけに掛けて 0 行を確かめた。
      写しに足した対照の 2 行 —— FR と GA の行 —— は 2 行とも数えた）
    下ろす先の候補  規則 02（表番号と行 ID の振り方）
```

---

## 3. ⛔⛔ **残件の一覧は本書に書かない**

⭐ **代わりに、一覧を作る問いを置く。毎回これを打て（実行できる）:**

```bash
PYTHONIOENCODING=utf-8 python -c "
import io
for l in io.open('docs/development-records/defects.md',encoding='utf-8'):
    if not l.startswith('| DFC-'): continue
    c=l.split('|'); st=c[6].strip().strip('\`')
    if st in ('実測済','取下げ'): continue
    print('%-7s %-6s %s' % (c[1].strip(), st, c[2].strip()[:70]))
"
```

⛔ **出てきた行をそのまま体に渡すな。**⭐ **まず反証させろ（規則 05 の 7「「塞がっている」を信じるな」）。**

### ⭐ ステータスの意味（台帳の頭にも在る）

```
未検討    受け付けただけ                    → 調べる
裁定待ち  利用者にしか決められない          → ⛔ 利用者（§6）
仕様待ち  裁定は下りた。仕様書がまだ        → 前に立つ者が書く（⛔ 1 体だけ）
実装待ち  仕様に在る。コードがまだ          → 体に出す
試験待ち  コードに在る。試験がまだ          → 仕様だけを読む体に書かせろ
実測待ち  試験が緑。出荷ビルドを押してない  → ⭐ 掃引を 1 回打て。安い
実測済    押して期待値を測った              → ⭐ 刈り取り（体に移させろ）
取下げ    欠陥ではなかった／別の行に吸収     → ⭐ 刈り取り
```

⛔⛔ **`試験待ち` と `実測待ち` を混ぜるな。⭐ 2026-09-10 はその差が 3 件の欠陥を炙り出した。**
⚠️ **`実測待ち` を `実測済` に変えてよいのは、出荷ビルドを押して 数 を書いたときだけである** —— ⭐ **押した条件（日付・ビルド・画面の大きさ・倍率）を `実物確認` の欄に必ず添えろ。**

---

## 4. ⛔⛔ **`npm run e2e` の状態**

⭐ **数は書かない**（腐るので）。⛔ **第 0 節の 4 を走らせて数えろ。**

⛔⛔ **「落ちるのは既知の N 件だけ」を信じるな。⭐ `unmet` を 1 行ずつ数えろ** ——
⚠️⚠️ **実測 2026-09-11: 前の版は「落ちる 2 つは性能の門だけ」と書いていたが、その 2 本の中に性能ではない主張が 3 つ入っていた。**
```
grep -E '^\s*\+\s*"' <出力> | grep -vcE "NFR-00[23]"   ⇒ ⛔ 0 でなければ新しい欠陥
```
⭐ **落ちる件には必ず台帳の行が在る。⛔ 行の無い赤が出たら、それは新しい欠陥である。**
⛔⛔ **先に `npx vite build` を打つこと。**
⚠️ **`sample-schedule/` は追跡されている（`git ls-files sample-schedule` で確かめよ）。** `git check-ignore` にも当たらない。⛔ 前の版の「`.gitignore` されているので作業木に無い」は誤りであった。
⛔⛔ **e2e を回しているあいだ `docs/spec` を触るな。⭐ 触ると「退行した」と読める。**
⇒ **系の試験は原稿を実行時に読む。⭐ 通しは巡の最後に、原稿が落ち着いてから 1 回だけ回せ。**

---

## 5. 🔧 **利用者の段 4 —— 仕様とコードのごみ消し**（⚠️ 計画の段 4 ではない。§1.1）

⭐ **判定基準は 1 つ**: **その文を消したとき、失われる規則があるか。無ければごみである。**
⭐⭐ **共通点はいつも同じ** —— **どれも「事実」を書いており、事実のほうが動いた。**

⭐ **2026-09-08 の実例**: **捏造引用が 11 か所、うち 4 か所は折り返しで割れていて `grep` に出ない。**
⭐ **範囲の綴り（`CM-56 to CM-71`）は数と同じに腐る。⇒ 集合を名乗らせ、コンパイラに数えさせた。**
⛔ **注に合わせてコードを直すな** —— **この巡も、コードが正しく理由だけが誤っていた例が複数あった。**

### 5.1 ⛔⛔ **検査を足す前に**

```
⛔ その検査が守らせる規則が、どこかに書かれているかを先に確かめよ
⛔ 数えてから基準線を作れ
⛔ わざと壊して赤くなることを実測してから入れよ
```

### 5.2 ⛔⛔ **ごみが増えない仕組み（2026-09-17、`5d159a5f` で実測。check.sh は 39 秒で ALL GREEN）**

利用者の問い（2026-09-17、逐語）: 「コードコメントや仕様書のごみ取りをやったが増えないような仕組みを継続してるよね？それも忘れず引継プロンプトを書け。」

⭐ **仕組みは続いている。どれも `check.sh` に配線され、緑である。** ⚠️ ただし **自動では走らない**（下の「穴」）。

| 検査 | 捕まえるもの | 基準線（1 行目） |
|---|---|---|
| 42 | 仕様のどこにも無い「…」の引用をコメントに書く | quoted-source 384 |
| 44 | 退役した ID・未定義の ID を src/ と tests/ で名指す | spec-id-references 88 |
| 45 | 20 トークン以上の同じ式の繰り返し（src/。tests/ は助言だけ） | repeated-expressions 90 |
| 55 | コメントの規則（src/ は裁定 17、tests/ は JDG-62） | comment-rules 0。tests/ はファイルごと ＋ 密度 2710 |
| 54 | 仕様に日付つきの履歴を書く | 0 でなければ赤 |
| 11 ・ 46 ・ 47 | 仕様の文の重複、文の切れ目の欠け、印の二重 | 重複 A=28（新規 0）、切れ目 1 |
| 29 | 実装待ちを過ぎたのに仕様の場所を名指さない台帳の行 | decided-spec 79 |
| 31 | 決着したのに blocked と読める台帳の行 | stale-blocked 50 |
| 39 | 逐語の試験を持たない MUST | must-clause 1401 |
| 43 | 着地していない裁定 | ruling-landed 15 |
| 30 | 生成した定数と規則 03 の一覧、JDG-139 の export の規則 | 基準線なし（完全一致） |
| 41（precheck） | 絶対パス・個人情報・秘密・生成物の手書きなど 6 つの罠 | git が変更と言うファイルだけを見る |
| 13 ・ 14 | 残骸、2 か所に書いた値 | 助言だけ |

**自動で走るもの:**
- Claude の Edit ／ Write のあと: `check-after-edit.py` が、.ts のコメントの検査と `_source/` の生成の突き合わせを走らせる。⚠️ 知らせるだけで止めない。Bash での編集には走らない。
- セッションの開始: StrictDoc の出力の掃除と stats の行だけである（precheck も check.sh も走らない）。
- `block-sweeping-git.py`: `git add -A` などを止める（ごみとは関係しない）。

**穴（塞ぐ 4 つの案 —— 規則 04 の 6.3、規則 03 のリンク、減ったら赤、pre-commit —— を利用者は提案どおりと答えた。`JDG-161`。時期は計画の段 3 の門と一緒）:**
1. git のフックも CI も無い。⇒ `npm run guard:commit`（precheck ＋ 型 ＋ gen:check ＋ 試験 ＋ check.sh）は、手で走らせたときだけ効く。守らせているのは本書 §9 の手順だけである。
2. 基準線を**下げること**は強制されていない。数が減っても exit 0 のままなので、緩みが残る（いま、検査 39 は基準より 49 少なく、55 の tests の密度は 15 少ない）。
3. 「基準線を上げるのは利用者の了承のあと」は `docs/development-rules/` に無い（本書と記憶だけにある）。規則 04 の 6.3 は「黙らせるために上げるな」までである。
4. 門 3・4（関数の大きさ・モジュールの状態、56 ／ 57）は、道具だけが在って未配線である（§1.1）。
5. 規則 03 の 3 節と 5 節は、コメントの規則（`docs/review/comment-rules-src.md`）へのリンクを持たない。3 節の日本語の例のコメントは、裁定 17 の「ASCII だけ」とぶつかる（推定）。

---

## 6. ⭐⭐ **利用者の裁定 → `rulings.md` が持つ**

⛔⛔ **裁定の逐語は `docs/development-records/rulings.md` に在る。⛔ 本書は写しを持たない。**
⛔ **行を立てる前に、`rulings.md` と `pending-decisions.md` の両方を grep しろ。**
⭐ **問う前に読むべきものは 0.1 に書いた。⛔ 飛ばすな。**

### ✅ **1 フレームの予算 —— 決着している（`JDG-11` ／ `JDG-50`）**

⇒ **ほかの不具合が無くなるまで、いまの性能を是とする**（`JDG-11`）。**リファクタの間は、段 0 で測った値より悪くしない**（`JDG-50`）。
⛔ **最適化を進めるな。**⭐ **値と、再生の手順は計画の記録 16 が持つ。⛔ 本書に写すな。**
⚠️ **`LM-19` の数は、表 T-043 が測れと定める量（届いたフレームの間隔）とは別の量である** —— 食い違いは `DFC-573` が持つ。⛔ **2 つを比べて「速くなった」「遅くなった」と読むな。**
⚠️ **入っている最適化は描く範囲を絞る 1 つだけで、絵を層に割る道は未着手である。**⛔ **「限界まで削った結果」ではない。**
⚠️ **2026-09-16 の追加の A/B（記録 17 の追補）では、2 つの操作で先端が段 0 より悪い** —— 1920×400 で行の軸の上限を越えるズーム（`DFC-610`）と、空き地の上で依存線を引くこと（`DFC-611`）。
⭐ **2026-09-18 に利用者が「パフォーマンスは一旦良い。」と答えた（`JDG-168`）** —— `cdca63ac` の A/B の性能をいまは是とし、`DFC-610` と `DFC-643` は `取下げ`（戻す条件は各行の詳細状況）。⚠️ `JDG-50` の値そのものは動かない。`DFC-611` は `未検討` のまま残る。

### ⭐ **問い方の作法**（2026-09-08 に効いた形）

```
⛔ 記号で問うな —— 具体名とユースケースで書け（利用者の指示 2026-09-08）
⭐ 案を複数挙げ、表で比べ、最適案を根拠つきで推せ
⛔ 「どちらにしますか」だけを出すな —— 前に立つ者の推しと、その代償を書け
⭐ 代償を正直に書くと、利用者は別の案を選べる（この巡は 1 件それで覆った）
```

---

## 7. **測り方の罠 → ⭐ `docs/development-rules/04-verification.md` の 6 節にある**

⛔ **本書に規則を書くな。あちらに足せ。**⭐ **検査 0 が毎回その所在を印字する。**
⭐ **2026-09-08 に足した 6.7**: **作業木では `docs/spec/output/` と `docs/reference/mspdi/` が無く、
引用元の検査が +3、単体試験が 160 件落ちる。⛔ どちらもあなたのせいではない。**

---

## 8. 道具

```
npm run parity             見本と出荷ビルドを 75 の操作で突き合わせる
npm test / npm run e2e     ⛔ 両方数えろ。⛔⛔ e2e の前に npx vite build
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
                           ⛔ >/dev/null に流すな（黙って何も書かずに止まる）
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
npm run precheck           ⛔ 編集の「前」に
tools/probe/harness.mjs    出荷ビルドを押す道具。⭐ examples/ に手本がある

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated] ／ 図形の identity [data-figure]
        ／ スクロールバーの軸 [data-axis] ／ 欄の行 [data-field-row]
⭐ 選択の枠は fill="none" の <rect> ＋ 両端の 9 × 9 の白い掴み点である
⚠️ バーは polygon である（rect でも path でもない）
⚠️⚠️ data-figure は 1 要素 1 鍵ではない —— 1 つの鍵が「文書順で連続した一続き」を指す
```

```
.claude/skills/spec-graph-check/
  check.sh    ⛔ grep だけで判じるな。⭐ 確実なのは終了コードである
  induced.py  ⛔ 仕様を書く前に。閉路は 1 回の書き込みで直す
  impact.py   影響の広がり（深さ 2）。⚠️ PYTHONIOENCODING=utf-8 を付けろ
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ プロパティ項目は docs/spec/_source/property-items.json（＋ 同 .schema.json の enum）
     ⭐ 読む側は properties-panel.ts の 1 つだけである（実測 2026-09-08）⇒ 同じ波に入れるのはそれ 1 つでよい
  ⚠️ コンポーネントの辺は docs/spec/_source/components.json
  ⚠️ 行を退役させたら md-checks.py の RETIRED に席を burn しろ（⭐ 理由も書け）
```

⛔⛔ **`tests/system` ／ `tests/usecase` ／ `tests/nfr` は Playwright であって vitest ではない**（規則 04 の 6.4）。
⛔⛔ **`dist/index.html` は追跡されている**（利用者の指示）—— **`.gitignore` は `dist/*` ＋ `!dist/index.html`。
⛔ 手で編集するな。`npx vite build` で作り直せ。**

---

## 8.5 ⭐ **裁定の着地先として残す段（2026-09-10）**

⛔ **`rulings.md` の `JDG-42` と `JDG-43` は、着地先として本節を名指す。⛔ 節ごと消すな。**
⭐ **どちらも「`docs/spec` に 1 文字も足さない」ことが着地である。だから着地先が本書になっている。**

**`JDG-42`（逐語）:**
> 「推奨通り、勝手に動かないようにしろ」

⇒ **`DFC-423` を直した代償 —— 動かさない 1 回のクリックでダミーが実績を書かなくなったこと —— を是とする。**⛔ **仕様も造りも変えない。**

**`JDG-43`（逐語）:**
> 「推奨通りとせよ。 これは俺の決めた仕様ではない」

⇒ **見本 HTML が書いた、掴みシロの入れ替わりの「2 回以下」という数を取り下げる。**⛔ **`docs/spec` に条項を立てない。**

⛔ **`DFC-311` は触るな**（`JDG-25`「一旦そのまま」）。⛔ **「既知の不具合を 0」の数に入れるな。**

---
## 9. ⛔⛔ **巡の終わりに必ず測る（⛔ 1 つでも欠けたらコミットしない）**

```
npx tsc --noEmit                                     clean
npx vitest run                                       全緑
python .claude/skills/spec-graph-check/md-checks.py . exit 0
bash .claude/skills/spec-graph-check/check.sh        exit 0（⛔ grep ではなく exit code）
npm run parity                                       75/75
npx vite build && npx playwright test                ⛔⛔ unmet を 1 行ずつ数えろ（§4）
```
⭐ **段の出口では、計画の記録 16 の再生の手順（網羅と性能）も回し、段 0 と比べる**（`JDG-50`）。
⛔ **性能は保存した 1 回の走行と比べるな。** 段 0 のコミットをビルドし直し、同じ日に A/B を交互に走らせて比べる（手順は計画の記録 17）。

⭐ **そして、巡を閉じる前に台帳を刈り取る**（利用者の裁定 `JDG-32`、2026-09-09、逐語「defects.mdで終わっている案件は(取り下げ、実測済み)はfixed-defects.mdに移せ。」）——
⛔ **`defects.md` に `実測済` と `取下げ` を 1 行も残さない。**⭐ **機械的なので体に出す**（`JDG-01`）。
```
awk -F'|' '/^\| D[A-Z]*-[0-9]+ /{ if ($7 ~ /実測済/ || $7 ~ /取下げ/) n++ } END{ print n+0 }' docs/development-records/defects.md
  ⇒ ⛔ 0 でなければ、まだ閉じていない
```
⭐ **移すときは md5 で押さえろ** —— **移す前の行と、移したあとに `fixed-defects.md` から読み直した行の md5 が一致して初めて「1 文字も削っていない」と言える。**
⭐ **そのうえで、個人情報の実測を報告してからコミットする**（利用者名・絶対パス・メール・秘密の literal）。
⚠️ **`file://` と `http://` は絶対パスではない。⭐ 検出器がそれを拾うので、数える前に中身を見ろ。**
⚠️ **`dist/index.html` は追跡されている。⭐ 縮小された束の中の `token` と著作権表示の名前は偽陽性である**（前者は識別子、後者は本リポジトリ自身の著作権表示）。

⚠️ **前の版の末尾にあった「新規のテストは要らない」（`JDG-13`）は、`JDG-49` が覆した。**⭐ **試験の計画は、リファクタの計画の記録 6 が持つ。**

---

⛔⛔ **文脈は圧縮される。本書は `defects.md` と `pending-decisions.md` と `git log` だけから書けるように保て。**
