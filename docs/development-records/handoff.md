# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             ⭐⭐ 赤い試験を疑う前にこれを走らせろ
3. ハーネスがサブエージェントを禁じているかを確かめ、禁じているなら
   「規則 05 と衝突する。体を使ってよいか」を最初に問う
4. ⭐ 利用者が眠っているときの標準指示（2026-08-30 に受けた。逐語）——
   「裁定が不要な分を進めろ。修正が容易な分（定数の変更／1 モジュール内に
     収まる変更）は裁定を待たずに進めて、後でまとめて報告しろ」
```

---

## 0.5 ⭐⭐ **赤い試験は 0 である**

```
実測（巡の終わり）: 117 ファイル / 5289 件 —— 5288 緑 ／ 1 skip ／ ⭐ 赤 0
検査 29 本 ALL GREEN ／ typecheck 緑 ／ layers 緑（284/284）／ gen:check 17 本 OK
重複検出 A=27 (new 0) ／ tables 136 ／ figures 11 ／ rows 1758 ／ uids 150
アイコン 87 ／ 図形 87 ／ 語 400 対
```

---

## 1. ⭐⭐ **この巡で何をしたか —— 行の操作子を最後まで通した**

⭐ **利用者の指示（2026-08-30、逐語）**「**裁定はあとに回す。**
`previous-project-result/11-row-controls` **それを最後まで実装しろ**」。

| 設計の項目 | 状態 |
|---|---|
| `HR-7`（配下を 1 階層開く）／ `HF-13` ／ `IC-90` | ✅ 仕様・図形・実装・試験 |
| `HR-8`（配下に足す）／ `HF-14` ／ `IC-91` | ✅ 同上。**名前は空で立て、その場で打たせる** |
| 折り畳みの図形を 1 家族へ（`RC-13`）| ✅ **5 つ描き直した。頭も一員である** |
| `HF-4`（ピン止めをいちばん外へ）| ✅ **裁定済みで未実装だった** |
| `CM-73`（行の親を変える命令）| ✅ ドラッグの書き込む先 |
| `HF-15` ／ `GR-20`（掴んで動かす）| ✅ **11 の義務すべて。試験 41 件** |
| `HR-6`（隠す）の入口 ／ `U-29` の廃止 | ⛔ **`PD-409` の裁定待ち。下の 3 節** |

⭐ **実物で確かめた 4 つ**（出荷ビルド、1920 × 1080、実ポインタ）:

```
頭        ∧∧（118–144）と ∨∨（144–170）。重なり 0
行        全 8 行が 7 つを描き、いちばん外は IC-60。葉の行は 4 つが薄い
掴んで移動 Phase Gates が親をまたいで移り、握っているあいだ文書は 1 バイトも動かない
ピン止め   留めた行は頭へ上がり、同時に掴み代を失う（GR-20 の MUST NOT）
```

---

## 2. ⛔⛔ **この巡でいちばん効いたのは「無い」を疑うことだった**

⭐⭐ **体の「仕様に無いのでできない」を 3 回とも測り直し、3 回とも偽であった。**

| 何を「無い」と言ったか | 実際は |
|---|---|
| 赤い 7 件は `GR-20` の未実装が原因 | **6 件は `help-roster.json` のずれ。`npm run helproster` 1 回で消えた** |
| 表 T-108 に整列の命令が無いので組めない | **前半は真、後半が偽。`GR-12` が同じ形を同じファイルで使っている** |
| 縦の刻みが仕様に無いので上下は実装できない | ⛔⛔ **刻みは要らない。`HF-15` は「描く順にたどること（MUST）」という順序の規則である。**⭐ **必要な箱は `frame-loop.ts:4759` が計算して捨てていた** |

⚠️ **3 つ目は特に危ない形である** —— **体は「仕様の穴」として `STOP` を書き、正しく振る舞ったように見えた。**
⭐ **差し戻したら、体自身が本物の欠陥を 1 つ見つけた**（末尾の場所が二重に立ち、`RS-39` が永久に出ないところだった）。

---

## 3. ⛔⛔ **利用者の裁定が要るもの（5 件）**

| ID | 問い | 効いてくるもの |
|---|---|---|
| **`PD-409`** | **単一の `∧` は `HR-5`（自分を畳む）か `HR-6`（隠す）か** —— ⛔ `RC-13` は「1 本が**その行自身**」、見本は「1 本の `∧` ＝ この行を**隠す**」。**同じ日の裁定 2 つが両立しない** | ⛔ **`HR-6` の入口と `U-29` の廃止が止まっている。⭐ 廃止時に直す生きた参照は 4 か所**（表 T-103 の `U-29` ／ `UC-002` 手順 5 ／ `HR-6` 本文 ／ 表 T-076 の `EP-4`）|
| **`PD-410`** | **ピン止めした行を掴めないことを、描く側と読む側のどちらが負うか** —— ⭐ 描く側は守っている（実測）。⛔ **読む側は再確認しておらず、偽の `ScreenPart` を渡すと動く** | 推奨は現状維持（`IF-9` が描いた側を持ち主と定める）|
| **`D-147`** | `IC-41` はパスワードをどこで問うのか —— 値を問う面が 表 T-103 にも T-037 にも T-234 にも 0 行 | ⛔ 単なる切り替えにすると `FR-020` の MUST を破る |
| **`D-149`** | `IC-18` の「しまう」は在るのか —— 覚える行がどの表にも無い | 直すと 表 T-064 の `PI-37` に触る |
| **`D-158`** | パレットの入口が押下状態を持てるか —— ⭐ 行は在る（表 T-237 の `EN-2`）| ⚠️ **利用者が挙げた例（イナズマ線を緑に）がこの 4 件の 1 つである** |

⭐ **`PD-408` はこの巡で決着した** —— **頭と行で同じ図形が並ぶことを許す。**見本が答えを持っていた。

---

## 4. ⭐ **仕様が決めていないと分かっているもの（実装は `STOP` を残してある）**

```
⛔ 掴み代の塗り        GR-20 は幅しか定めず、表 T-109 にも 図 F-019 にも行が無い
⛔ 握っている行の絵    隙間を開けるとも、落ち先の印を描くとも、どの行も述べていない
⛔ 配下の無い行が、1 つ下の段に場所を開くか（HF-15 の「その段に置ける場所」）
⛔ 入口の枠の太さ 1px  S-138 は図形の箱、S-141 は隙間。枠自身の太さを持つ行が無い
⛔ FR-098 の「帯が Row Area を埋め尽くしてはならない」の上限の数
```

---

## 5. ⭐ 現在地（**すべて実測**）

```
✅ push 済み: origin/main（この巡の 7 本）。未 push のコミットは無い
⚠️ ブランチ名は restart のまま。push 先は main（git push origin HEAD:main）

⭐ 仕様へ入れたもの: CR-314（1.68）／ CR-315（1.69）／ CR-316（1.70）
   ／ CR-317（1.71）／ CR-318（1.72）／ CR-319（1.73）
⭐ 残件 106 ／ 台帳 161 行 ／ テスト完了 52 ／ 追加裁定待ち 9
裁定待ち 179 行・うち未裁定 132（新: PD-406 〜 PD-410）
```

⚠️ **残件が 103 → 106 に増えたのは失敗ではない** —— **増えた 5 件は全部この巡で実測して見つけたものである**
（`D-158` / `D-159` / `D-160` / `D-161`）。⛔ **1 件（`D-150`）は測り直して欠陥ではなかったので取り下げた。**

⭐ **作業樹は 1 つも作っていない**（体はすべて本体で読み書きした）ので、消すものは無い。

---

## 6. ⭐⭐ この巡でいちばん効いた方法（**続けろ**）

```
⭐⭐⭐ 「仕様に無い」と言われたら、必ず自分で当たる。この巡 3 回とも偽であった。
    ⭐ 差し戻すときは、逐語と file:line を添えて「どこが違うか」を書く。
    ⛔ 「もう一度見ろ」だけでは、体は同じ結論に戻ってくる。

⭐⭐⭐ 実装した当人に試験を書かせない（規則 05 の 7）。この巡の収穫:
    ① 葉の行に操作子が 1 つも無い欠陥（D-161）を、仕様だけを読む体が見つけた
    ② FR-029 の穴（Task を 1 つだけ選んで押すと何もせず理由も告げない）も同じ体
    ⛔ どちらも、実装した体も前に立つ者も見つけていない。

⭐⭐ 型で MUST を守れるなら、そうする。
    `RowTitle.expander` から null を外したら、HF-1 の「各行に」が型で守られた。
    ⛔ 仕様書に「操作子を持たない行」という語彙は 1 つも無い（grep で確かめた）。

⭐⭐ 実物で測る。この巡は 6 回測り、そのたびに何かが出た。
    頭の 6px の重なりは、利用者が絵を見て気づいたものである。

⭐ 試験を書いたら、期待値を 1 つわざと壊して赤くなることを確かめる（体が実行した）。
```

---

## 7. ⛔⛔ この巡で前に立つ者が犯した誤り（**繰り返すな**）

```
⛔⛔ 仕様の散文に、別の表の MUST を逐語で引き写した。3 度捕まった。
   ⭐ 直し方: 指すだけにする。「表 T-051 の HF-15 が同じ MUST で既に求めている」。
   ⚠️ 重複検出 A=27 (new 0) は、書き足すたびに読むこと。

⛔ 検査 22 の正規表現は R2〜R7 しか見ない。R1 の条項を名指しても落ちる。
   ⭐ 逃げ道: 規則の書類の相対パスを本文に literal で書く。

⛔ A-appendix.md の版の行に `PD-394` と書いて検査 7 に捕まった。
   ⭐ 版の行に PD- / D- を書くな。CR- はバッククォート無しなら通る。

⛔ bash の中で PowerShell のヒアストリング（@'…'@）を書き、件名の頭に @ が付いた。
   ⭐ 直し方: git commit -F <ファイル>。

⛔ 実物の測定で、修飾キーを推測で決めた（Control だと思ったが Shift であった）。
   ⭐ 直し方: src/ を読んで確かめる。
```

---

## 8. ⭐ 道具（**30 行の下ごしらえを二度と書くな**）

```
tools/probe/harness.mjs   ⭐ export 40。出荷ビルドを開いて実測する
⛔⛔ SVG の x は画面の x ではない —— 1920 の窓に対し x=2093 のバーがある。
   ⭐ 押す前に document.elementFromPoint で当たるかを確かめること。
⛔ shapes() はこの巡で ReferenceError を投げた。polygon の points を自分で読め
⛔ rows() は y で並べ替える ⇒ DOM の並びとの食い違いは見えない。treeOrder() を使え
⚠️ 実測した行の高さは 64 / 96 / 101 / 108 / 148px とまちまち。決め打ちするな
⭐ 行の操作子は button[data-icon] で拾える。掴み代は [data-row-grab]

⚠️ 表 T-206 の数は 既定 の欄に在る。値 の欄は名前である
   ⛔ S-208 を素で読むと HF-15 が返り、素朴に数を取ると -15 になる

npm run gen:check          ⭐⭐ 巡の頭で必ず走らせろ
npm run precheck           ⚠️ 罠 3 は「変更された生成物」を見るだけで、
                           再生成したのか手で書いたのかを区別できない（誤検出する）
.claude/skills/spec-graph-check/
  induced.py   ⛔ CR を書く前に走らせる
  check.sh     29 本。⭐ grep -n "^FAIL\|^PROBLEM\|^DRIFTED" が 1 行で当てる
  ⚠️ 表に行を足したら npm run words も要る
  ⚠️ 重複検出の登記は docs/review/duplication-baseline.txt。
     ⛔ 行を足すと群の鍵が変わる。古い行を残さず、新しい行で置き換えること
```

---

## 9. ⛔ 名前の残り（**利用者の指示 2026-08-30**）

> **極端に短い変数は意味をあらわさない。切れの良いタイミングで、名は体を表すような変数名に改めろ。**

```
tools/probe/harness.mjs    0 件（済み）
src/                       27 件（on / is / to など属性名でもある英単語。手で直す）
tests/                     539 件（未着手）
```

---

## 10. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔ **赤い試験** | 0 | **0** | ⭐ 0 を保て |
| 2 | ⛔⛔ **利用者の裁定待ち**（3 節）| 5 | **0** | ⭐ **巡の頭に 5 つ並べて出せ。`PD-409` が `HR-6` と `U-29` を止めている** |
| 3 | **`D-159` の切り分け** | 未 | **決着** | ⛔ 実物で `Task` を 2 つ選べない。⭐ まず群の要約バーかを測る |
| 4 | **テスト完了 の行数** | 52 | **60** | ⭐ `D-146` / `D-148` / `D-160` / `D-161` / `D-65` は試験が緑。**実物で見れば閉じる** |
| 5 | **`tests/` の短い名** | 539 | **≤ 400** | 利用者の指示。⭐ 切れの良いところで |

⚠️ **総件数が増えることは失敗ではない。**⭐ **状態を正して 残件 が増えるのも失敗ではない。**
⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
