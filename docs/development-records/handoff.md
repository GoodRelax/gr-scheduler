# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             ⭐⭐ 赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔⛔ 3 つとも数えろ
4. §1 を読む。⛔⛔ この巡は 1 件目が「利用者に問う」から始まる
```

⭐ **サブエージェントは許可されている**（利用者、2026-08-31）。⛔ **使い方は §2 に測った答えがある。**

---

## 0.5 ⭐ **始点（前の巡の終わりに実測）**

```
単体   122 ファイル / 5534 件 —— 5533 緑 ／ 1 skip ／ 赤 0
e2e    tests/system 17 件 —— 全部緑
同型   npm run parity —— 25/25 一致（⭐ 19 から増やした。うち 2 件は「仕様どおり食い違う」）
検査 30 本 ALL GREEN ／ typecheck 緑 ／ gen:check 17 本 OK
重複 A=27（new 0）／ tables 136 ／ rows 1770 ／ uids 150 ／ 語 405 対 ／ アイコン 89
台帳 170 行 ／ 残件 105 ／ テスト完了 61 ／ 追加裁定待ち 6 ／ 仕様確定 1 ／ 取下げ 4
未裁定の PD 135 件（188 件中）
```

⛔ **緑から始まる。**⭐ **だから赤が出たら、それは自分が壊したものである。**

---

## 1. ⛔⛔⛔ **この巡の仕事**

### 1.1 ⛔⛔ **まず利用者に問う —— `D-147` は裁定が無いと 1 行も書けない**

⭐ **前の巡が問いを整えてある。**⛔ **問う前に実装するな。**⭐ **問いは 3 つで、どれも実測から出た。**

**問い ①**: **`D-147` の裁定は「表 T-234 に 1 行足し、面を新設する」であったが、前半が実行できない。**
表 T-234 の結びが逐語でこう定めている ——「**本表が持つのは `NT-7` の問いだけである**」
「⛔ **どちらも `Confirmation`（`U-55`）ではないので、本表に行を作ってはならない（MUST NOT）。**」
⇒ **透かし解除パスワードを問う面は `Open Chooser` と同じ「面」であって `Confirmation` ではない。**
⇒ ⭐ **面の新設（裁定の後半）はそのまま実行してよい。表 T-234 の行だけが立たない。**

**問い ②**: **その面の「送る」入口をどうするか。**

| 案 | 中身 | 図形 | 費用 |
|---|---|---|---|
| **A** | `IC-69` / `IC-70`（続ける／取りやめる）の `面` 欄に新しい面を足す | ⭐ **要らない** | 小。⭐ **`IC-52` が既に 6 つの面に同じことをしている先例がある**（CR-193 / CR-191 は「行も図形も増やさない」と明記）|
| **B** | 新しい `IC` 行を 1 つ起こす | ⛔ **要る** | 中。⛔⛔ **図形は利用者の裁定である** —— **CR-220 は発明して差し戻された** |

⭐ **前に立つ者の推奨は A** —— **図形を発明せずに済み、先例があり、`F-019` の枠も動かない。**
⚠️ **A を採ると `IC-69` / `IC-70` の `正` が 表 T-037 の `NT-7` だけでなくなる**（`IC-4` のように 2 つ書く形になる）。

**問い ③**（`PD-417`）: **排他の選択で「いま選ばれているもの」を入口の塗りで示すか。**
⛔ **いまは同じ性質のものが 2 通りに描かれている** —— **ヘッダーは `S-59` の対（`IC-8` / `IC-9`）を自分の読みで塗り、パレットの `IC-45` 〜 `IC-49` は塗らない。**
⭐ **`FR-029`（MUST）は塗りの根拠をすべて 表 T-237 に負わせているのに、同表の 5 行にその意味が無い。**
⇒ **表 T-237 に 1 行足すか、ヘッダーの塗りをやめるか。**⛔ **2 通りのままにはできない。**

⭐ **詳しくは `docs/development-records/pending-decisions.md` の `PD-416` 〜 `PD-419`。**

---

### 1.2 ⭐ **裁定が下りたら `D-147` を書いて実装する**

**測った費用（前の巡、`docs/spec` を全部読んで数えた）:**

```
表 T-103 に 1 行（U-60 相当。新しい面の確定名）
表 T-109 の IC-52 の 面 欄に新しい面を足す（＝ Esc で閉じられるようにする。行も図形も増えない）
表 T-233 に 1 行（RS-40 相当。「解除パスワードが違う」。⛔ 現状は通知 0 件で FR-029 の MUST 違反）
FR-020 に 1 文（IC-41 がその面を開くこと）
辞書 _source/display-words.json に surfaces 1 枠・reasons 2 枠（＋ 案 B なら icons 2 枠）
実装: input-command-translator の ENTRY 対応表に IC-41 の鍵、面の描画、照合（SHA-256）
```

⛔ **`VisibleElement` の「9 対 8」は既に直っている。**⭐ **前の引継書のその記述は古い** —— 実測で 9 行そろっている（`edit-document-settings.ts`）。

---

### 1.3 ⭐ **命名の掃き出し（裁定不要。いつ着手してもよい）**

⭐ **`D-149` の⑤の続きである。**⭐ **前の巡に実測した数を置く。**

```
src/ の単一文字の宣言          22 か所 / 6 ファイル
  ⚠️ 全部 x / y / r（座標）である。⛔ 表で機械的に置換するな —— 幾何の中では x が最も正確な名である
  ⭐ 直す価値があるのは、1 つの視野に座標が何組も居る所だけ

tests/ の単一文字の宣言        121 か所 / 29 ファイル（x=19 y=29 i=15 が半分）
  ⚠️ 利用者の指示は「1 か所ずつ読め」。⛔ 一括置換は禁じられている

src/ の真偽の宣言（重複を除く）  115
  is / has / can / should で始まる  57
  始まらない                        58
    うち 9 は xxxVisible          ⛔ 文書に保存される鍵。触るな
    うち themeMonochrome          ⛔ 同じく保存される鍵。触るな
    うち dayWorking / resumeValid / milestone  ⛔ 交換形式の列名。触るな
    うち altKey / ctrlKey / metaKey / shiftKey / capture / options  ⛔ ブラウザ API の形。触るな
    ⇒ ⭐ 素直に直せるのは 40 前後
```

⭐ **`ScreenState` の `paletteShown` / `fullScreen` は直してよい** —— **保存されない値であり、`is` で始めれば規則 07 に沿う。**

---

## 2. ⭐⭐⭐ **エージェントの使い方 —— 前の巡で測り直した**

### ⭐ 答え: **「実装する体」と「仕様だけを読む体」の対。⛔ ワークフローの扇形展開ではない。**

#### ① ⭐⭐⭐ **対にするのが最も効いた。前の巡は実装の欠陥を 1 件、仕様の穴を 6 件捕まえた**

```
⭐⭐ D-149 で、仕様だけを読む体が実装を赤くした ——
   IC-18 の押下を S-99i だけで描くと、Agent API が無効のあいだ
   「薄くて、かつ塗られている」入口になり、FR-029 の MUST NOT を破る。
   ⇒ 押下は「いま欄が出ているか」＝ 有効かつ表示 に直った。
   ⛔ 実装する体は自分では気づけない。仕様を読んでいないからである。

⭐⭐ D-170 で、実装する体と試験の体が「同じ穴」を別々に見つけた ——
   依存線は 2 つの行のために描かれるので「その行のために描くもの」が
   どちらを指すか決まっていない。
   ⇒ ⭐⭐ 両者が同じ読みへ行き着いたことが、その 1 文が発明ではない証しになった。

⭐ 仕様だけを読む体が挙げた穴は PD-416 〜 PD-419 として台帳に立てた。
```

#### ② ⛔ **`src/` を読ませるな。読ませたら実装の振る舞いを規則として書き写す**

⭐ **前の巡は fence をこう書いて守らせた**:「`docs/` と `tests/` だけを読め。`src/` の `.ts` は 1 行も読むな」。
⭐ **`src/**/*.json`（名簿・辞書）は許した** —— **データであって振る舞いではない。**

#### ③ ⭐⭐ **「落ちることを確かめろ」まで指示に書け**

```
⭐ D-170 の体は、自分の読み取りを 3 通りに壊して 3 通りの赤を出して見せた
   （clip を無視 → 通し過ぎで赤 ／ 全部を clip → 切り過ぎで赤 ／ 線を消す → 落とし過ぎで赤）。
⭐ D-158 の体は、入口を全部塗る偽の実装を被せて 6 件が赤くなることを見せた。
⇒ ⛔ 「緑でした」だけの報告を受け取るな。⭐ 何をすれば赤くなるかを言わせろ。
```

#### ④ ⛔ **実装どうしを並列にしてよいのは、触るファイルが重ならないときだけ**

```
前の巡の実測:
  D-170  svg-renderer.ts だけ         ⭐ 結局これ 1 ファイルで足りた
  D-158  command-palette.ts / screen-renderer.ts
  D-149  screen-state 系 / app-header-items.ts / dialogue-field.ts /
         input-command-translator.ts / frame-loop.ts / screen-renderer.ts

⇒ D-170 と D-158 は重ならないので並列にした（実害なし）。
⇒ D-158 と D-149 は screen-renderer.ts で重なるので順にした。
⛔ 並列にするなら、両方が書き終わるまで npm test を走らせるな —— 偽の赤が出る。
```

#### ⛔ **ワークフローは今回も要らなかった**

```
向く : 同じ変換を多数の対象へ当てる（移行・一斉監査・広い掃き出し）
今回 : 異なる 4 件。どれも仕様の判断が先に要る
```

⚠️ **1.3 の命名の掃き出しは同型である** —— ⭐ **それでも「1 か所ずつ読め」が利用者の指示なので、体 1 つに読ませるほうが正しい。**

---

## 3. ⭐⭐ **順番**

```
① 利用者に §1.1 の 3 つを問う（⛔ ここが先。答えが来るまで D-147 は書けない）
② 答えが来たら、仕様を 1 パスで書く（前に立つ者。⛔ 体に渡すな）
   ⛔ 書くたびに: induced.py（閉路）→ 書く → check.sh（重複 new 0 を確かめる）
③ 実装と試験を対で回す（⛔ 触るファイルが重なる件どうしは並列にしない）
④ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh
⑤ ⛔⛔ 実物で押せ。試験が緑でも実物は直っていないことがある（§4 に実例）
⑥ 手が空いたら 1.3 の命名（裁定不要）
```

---

## 4. ⭐⭐⭐ **前の巡で学んだこと（続けろ／繰り返すな）**

### ⛔⛔ **いちばん高くついた罠 —— 外接矩形は `clip-path` を見ない**

```
D-170 を実物で確かめたとき、1 度目は「まだ直っていない」と報告しかけた。
⛔ getBoundingClientRect は clip-path を無視するので、切られた図形も元の箱を返す。
⭐ elementFromPoint で掃き直したら、切られていることが分かった（正しく直っていた）。
⇒ ⛔⛔ 盤面の「見えているか」を矩形で測るな。⭐ 引継書の道具の項は前からそう書いてある。
   自分がその 1 行を読み飛ばした。
```

### ⭐ 効いた

```
⭐⭐⭐ 体を分ける（規則 05 の 7）。§2 ① のとおり、実装の欠陥 1 件と仕様の穴 6 件。
⭐⭐⭐ 「効かない」と書いてある行は自分で押す。この巡も 2 件が偽であった ——
    ・D-147 の「VisibleElement が 9 対 8」は既に直っていた
    ・D-158 の「契約は PI-37」は誤りで、正しくは PI-2（PI-37 は公開する側の行）
    ⇒ ⭐ 引継書の記述も、書いた本人の実測も、1 日で腐る。
⭐⭐ npm run parity は、5533 件の単体試験が見つけられないものを見つける。
    ⭐ 見本と製品が食い違ったとき、⛔ 見本は正ではない —— 仕様が正である。
    実例: ピン止めの順。見本は木の順、GRS は留めた順。FR-098 は「固定した順」と定める
    ⇒ GRS が正しい。⭐ だから「仕様どおり食い違う」を harness に記録し、理由を毎回刷らせた。
⭐⭐ 表の行の文言を変えたら、辞書（display-words.json）を必ず読み直せ。
    ⭐ この巡も 1 件見つかった —— SK-14 の仕様行は「表示を切り替える」なのに、
      辞書は「出す / しまう」と言っていた。
⭐ 手で保つ一覧には検査を足せ。D-168 を閉じた（検査 30）。⛔ わざと壊して落ちることまで確かめた。
```

### ⛔ 前に立つ者が犯した誤り

```
⛔⛔ 生成物を直に書き換えた。tbl-settings.md は _source/settings.json から生成される。
    ⭐ check.sh が DRIFTED で捕まえた。⇒ 原稿へ書き直した。
    ⚠️ 直す前に `npm run gen` の出力を読めば、どれが生成物かは分かる。
⛔ settings.json を json.dumps で書き戻そうとした。⛔ 同ファイルは 🔎（非 BMP）を
    サロゲートペアのまま持っており、書き戻すと化ける。⇒ テキストとして差し込んだ。
⛔ 台帳のセルは `line.split('|')` で 11 個ちょうどである。
    書き足す文の末尾に ` |` を付けたら 12 個になり、検査が落ちた。
⛔ parity の食い違い表示が「両方とも空」を刷って何も言わなかった ——
    集合が同じで順序だけ違うときである。⇒ 順序違いをそう明言するように直した。
⚠️ 検査 29（仕様書のどこに書いたか）は docs/spec 以外に落ちる行を想定していなかった。
    D-168 がその最初の例。⇒ 「docs/ のファイルを名指していれば可」に広げた。⛔ 要求は緩めていない。
```

---

## 5. ⭐ 道具

```
npm run parity             ⭐⭐ 見本と出荷ビルドを 25 の操作で突き合わせる
                           tools/parity/check.mjs ／ sample-and-app.mjs
                           ⭐ 読みは 4 つ: rows / counts / arming / pinned
                           ⭐ KNOWN_DIVERGENCES に「仕様どおり食い違う」を書く（理由を毎回刷る）
                           ⛔ 押せなかった操作は ⛔ で落ちる（黙って ✅ にしない）
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    ⭐ 原稿を直したら gen、巡の頭で gen:check
tools/probe/harness.mjs    ⭐ 出荷ビルドを実測する

⛔⛔ 外接矩形は clip-path を見ない。見えているかは elementFromPoint で掃け
⛔ SVG の x は画面の x ではない
⛔ 行を y で覚えて押すな。押すたびに組み直るので別の行に当たる。名前で引き直せ
⛔ パネルは縦にスクロールする（HF-9）。窓の外の行は DOM に無い
⚠️ 行の高さは 64 / 96 / 101 / 108 / 148px とまちまち。決め打ちするな
⭐ 掴み代 [data-row-grab] ／ 件数 [data-folded-rows] ／ 通知 [data-manner]
⭐ ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]

.claude/skills/spec-graph-check/
  induced.py  ⛔ 書く前に。閉路は 1 回の書き込みで直す
  impact.py   波及を測る
  check.sh    30 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED"
  ⚠️ 表に行を足したら npm run words も要る
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⭐ 検査 30 が「生成された定数の一覧」を見る（D-168 で足した）
```

---

## 6. ⭐ 現在地

```
⭐ ブランチ restart（push 先は main）
⭐ 仕様: CR-325 相当まで（FR-098 / FR-066 / 表 T-206 S-99i / 表 T-237 EN-5 / 表 T-109 の語）
⭐ 閉じた: D-170 / D-158 / D-149 / D-168 —— 4 件とも実物で押して確かめた
⛔ 残る: D-147（§1.1 の裁定待ち）
⭐ 作業樹は 1 つも作っていない
```

---

## 7. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔⛔ **`D-147`** | 未 | **済** | ⭐ **裁定を先に取れ**（§1.1）|
| 2 | ⛔ **`PD-417`**（排他の選択の塗り）| 未裁定 | **裁定済** | ⛔ 同じものが 2 通りに描かれている |
| 3 | **赤い試験（単体・e2e・同型）** | 0 | **0** | ⭐ 3 つとも数えろ |
| 4 | **`npm run parity` の操作数** | 25 | **30+** | ⭐ ドラッグ・段替え・削除の取り消しを足す |
| 5 | **`tests/` の単一文字の宣言** | 121 | **減らす** | ⚠️ 表で機械的に直すな。1 か所ずつ読め |
| 6 | **`src/` の真偽の名（直せる分）** | 約 40 | **0** | ⭐ 規則 07 の Naming。⛔ 保存される鍵と列名は触るな |
| 7 | **未裁定の `PD`** | 135 | **減らす** | ⭐ まとめて問える A〜C が多い |

⚠️ **総件数が増えることは失敗ではない。**⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
