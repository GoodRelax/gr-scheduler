# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npx vite build                                ⛔⛔ e2e の前に必ず
4. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ。⛔ pipefail を付けろ
5. §1 と §2 を読む
```

⛔⛔ **`--start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな。**

**サブエージェントは許可されている**（利用者、2026-08-31 以降くり返し再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**
⭐⭐ **知見は規則へ下ろせ**（利用者の指示 2026-09-03）。⛔ **本書に置いた学びは 1 巡で消える。**
⭐ **2026-09-07 が下ろしたもの**: 規則 04 の **6.4** ／ **6.5** ／ **6.6**（記録と主張の書き分け）、規則 05 の **12**。

---

## 0.5 ⛔⛔ **基準値は本書に書かない**

**⇒ 第 0 節の 1〜4 を走らせれば、始点はすべて出る。**
⭐ **基準線は、それぞれのファイルの 1 行目が持っている。⛔ 理由はその下に書いてある:**

```
検査 31  .claude/skills/spec-graph-check/stale-blocked-baseline.txt
検査 37  .claude/skills/spec-graph-check/dictionary-table-pairing.txt
検査 39  .claude/skills/spec-graph-check/must-clause-coverage-baseline.txt
検査 40  .claude/skills/spec-graph-check/ruled-elsewhere-baseline.txt
検査 41  .claude/skills/spec-graph-check/decided-spec-baseline.txt
検査 42  .claude/skills/spec-graph-check/quoted-source-baseline.txt
検査 11  docs/review/duplication-baseline.txt
```

⛔⛔ **黙って上げるな。上げるなら、そのファイルの中に理由を書き、コミット文にも書け。**
⭐ **上げてよいのは、同じ巡のうちに返す当てがあるときだけである。**
⚠️ **返し忘れを防ぐには、上げたと同時に台帳へ行を立てろ。**
⛔⛔ **2026-09-07 は検査 39 を 934 → 976 へ上げた。⭐ その借金は `D-377` が持っている。**
⇒ **`FR-016` の 2 つの上限・`EP-9` の太さ・作成の流れ・`Agent API` の 2 件を実装する波が、
錨で逐語を持てば下がる。⛔ 下げずに巡を閉じるな。**

---

## 1. ⛔⛔ **利用者が決めた 4 つの段（2026-09-05、逐語）**

> **やることは不具合の全検出。 その後、修正計画/必要なら裁定。
> その後一気に既存不具合を直す。 それが終わったら、仕様やコードのごみ消し。**

```
段 1  不具合の全検出          ✅ 済んだ
段 2  修正計画／必要なら裁定   ✅ 済んだ。⚠️ ただし新しい問いが 1 つ出た（§6）
段 3  既存不具合を一気に直す   🔧 いまここ。⭐ 2026-09-07 で 41 → 30 まで下がった
段 4  仕様とコードのごみ消し   🔧 並行して進めている（§5）
```

---

## 2. ⛔⛔ **2026-09-07 の巡が身銭で買った 6 つ（⭐ これだけは読め）**

**⓪ ⛔⛔ 反証の体を先に出すのは、今回も最も安い投資であった。**
⇒ **踏査の 4 体が、実装を 1 行も書く前に 6 行の前提を殺した。**
```
D-278  「仕様は未着手」は偽。FR-060・LM-14・設計 5.5 の 3 か所とも既に書き換わっていた
D-280  「仕様の穴がある」は偽。FR-065 は 2026-09-05 にオリジンごとへ着地済み
D-274  「パネルが選択に従わない」は偽。⭐ 台帳が幅 0 の DOM の残骸を読んでいた
D-304  「辞書に証跡の語が無い」は偽。FR-020 を直した日に語も入っていた
D-369  「アプリの不具合」は偽。掃引が実績のバーを掴もうとしていた
D-370  「アプリの不具合」は偽。刻みより札が 1 つ多いのは描き方の仕様である
```
⭐⭐ **「反証してこい」と書いた体が、いちばん多くを返す。⛔ 直す体より先に踏査の体を出せ。**

**① ⛔⛔ 体の作業木は「セッションの起点」で切られる。⭐ 対処は 1 行で済む。**
⇒ **ブリーフの冒頭に `git merge --ff-only <sha>` を書け。⭐ 2026-09-07 は 7 体すべてがこれで直った。**
⛔ **書き忘れた 2 体は「行番号がすべて違う」と報告してきた**（134 コミット遅れの体も居た）。

**② ⛔⛔ 合流は「写す」ではなく「当てる」ことがある。**
⇒ **体の作業木が自分より古い幹から切られているとき、ファイルを丸ごと写すと幹の仕事が消える。**
⭐ **2026-09-07 の実例**: **予実の分割を入れたあとの `input-command-translator.ts` に、
それより前の幹から切られた体の変更を写しかけた。**
⛔ **正しい道は差分を 3 way で当てることである:**
```
git -C <worktree> diff -- <file> > <scratchpad>/x.patch
git apply -3 <scratchpad>/x.patch
```
⭐ **どのファイルが危ないかは、先に数えれば分かる:**
```
for f in $(git -C <wt> status --porcelain | grep -v '^?? scratch/' | awk '{print $2}'); do
  git diff --quiet <体の基準 sha> HEAD -- "$f" && echo "SAFE $f" || echo "PATCH $f"
done
```

**③ ⚠️⚠️ この作業木は行末が混ざっている。⛔ Python の素朴な置換が黙って 0 件になる。**
⇒ **`fr-029-the-reason-a-press-carries.test.ts` は CRLF、隣の試験は LF であった。**
⭐ **読むときに正規化し、書くときに元の行末へ戻す小さな道具を scratchpad に置け。**
⛔ **`count(...) == 1` を主張してから置換しろ**（規則 02 の 4 条）。

**④ ⚠️ 表の行 ID は名簿をまたいで衝突する。**
⇒ **`PD-4` は 表 T-023a の押しの判定順序の行であり、同時に裁定待ちの行の ID でもある。**
⛔ **台帳の散文に `PD-4` と綴っただけで、検査 40 が「裁定済なのに未検討」と読んだ。**
⭐ **表の行を指すときは、表の名と条件で書け。**

**⑤ ⭐⭐ 数は、書いた瞬間から腐る。⛔ 注は集合を名乗れ。**
⇒ **`D-365` の注は「74 行」と書いて 89 行に、`edit-document.ts` は「73 命令」と書いて 72 になっていた。**
⭐ **直し方は「数を直す」ではない** —— **`T-109` から `ENTRY` と `ARMED_BY_ENTRY` の鍵を引いた集合、
と名乗らせた。⇒ 次に 1 行動いても腐らない。**

---

## 3. ⛔⛔ **残件の一覧は本書に書かない**

⭐ **代わりに、一覧を作る問いを置く。毎回これを打て（実行できる）:**

```bash
PYTHONIOENCODING=utf-8 python -c "
import io
L=io.open('docs/development-records/defects.md',encoding='utf-8').read().split('
')
for l in L:
    if not l.startswith('| D-'): continue
    c=l.split('|'); st=c[6].strip().strip('\`')
    if st in ('実測済','取下げ'): continue
    print('%-7s %-5s %s' % (c[1].strip(), st, c[2].strip()[:70]))
"
```

⛔ **出てきた行をそのまま体に渡すな。**⭐ **まず反証させろ（§2 の ⓪）。**

### ⭐ ステータスの意味（台帳の頭にも在る）

```
未検討    受け付けただけ                    → 調べる
裁定待ち  利用者にしか決められない      → ⛔ 利用者（§6）
仕様待ち  裁定は下りた。仕様書がまだ       → 前に立つ者が書く
実装待ち  仕様に在る。コードがまだ       → 体に出す
試験待ち  コードに在る。試験がまだ       → 仕様だけを読む体に書かせろ
実測待ち  試験が緑。出荷ビルドを押してない → ⭐ 掃引を 1 回打て。安い
```

---

## 3.5 ⭐ **波の張り方（⛔ 幅ではなく順序を設計しろ）**

⭐ **規則 05 の 11 と 12 が本体である。⛔ 本書は写しを持たない。**
⭐ **2026-09-07 に実際に効いた形だけを残す:**

```
① 踏査の体を先に出す（読むだけ。src も tests も docs も触らせない）
② 仕様は前に立つ者が書き、⛔ 書き終えてからコミットする
③ 実装の体は「編集してよいフォルダ」で割る。⛔ ブリーフに 1 行で書け
④ 体が戻ったら ②の要領で当てる（写すのではなく）→ 検査 → コミット → その sha を次の波へ
⑤ 掃引の体を 1 つ（出荷ビルドを 1 回起動して 実測待ち をまとめて閉じる）
```

⛔ **同じファイルを 2 体に渡すな。**⛔ **合流の前に必ず重なりを数えろ:**
```bash
comm -12 <(git status --porcelain | awk '{print $2}' | sort) \
         <(git -C <worktree> status --porcelain | grep -v '^?? scratch/' | awk '{print $2}' | sort)
```

⭐⭐ **2026-09-07 の実績: 11 体（踏査 4・実装 6・掃引 1）。⛔ 詰まったのは幅ではなく、
「翻訳器」と「殻」が何度も要ることであった。**⇒ **その 2 つを持つ体を、波ごとに 1 体だけにしろ。**

---

## 4. ⛔⛔ **`npm run e2e` の状態**

⛔⛔ **先に `npx vite build` を打つこと。**
⚠️ **`sample-schedule/` は `.gitignore` されているので作業木に無い。**親から写さないと `FR-021` が 1 本余分に落ちる。

⭐ **2026-09-07 の実測（出荷ビルド）: 126 通過・5 落ち。⛔ 内訳は次のとおりで、すべて台帳に行がある:**
```
nfr-001-010-011-013 / nfr-002-003   性能の合否（D-330 → 裁定は PD-443 待ち）
fr-073-fr-022                        Agent API の取り込み（D-357）
nfr-004-file-scheme-sweep ×2         SK-19 が答えない（D-382）
```
⭐⭐ **`SL-7b` と月の段（`D-369` / `D-370`）は緑になった。⛔ どちらも試験の欠陥であった。**

---

## 5. 🔧 **段 4 —— 仕様とコードのごみ消し**

⭐ **判定基準は 1 つ**: **その文を消したとき、失われる規則があるか。無ければごみである。**
⭐⭐ **共通点はいつも同じ** —— **どれも「事実」を書いており、事実のほうが動いた。**

```
✅ 2026-09-07 に片づいたもの
   D-339  「表の行」として引用されていた、仕様に無い一文（3 件 ＋ もう 1 件）
   D-365  STOP の数と行 ID の腐り ⇒ ⭐ 数をやめ、集合を名乗らせた
   D-278  消えた務め（FR-060 の申し出）を名指していた注 6 か所
   D-280  「文書ごとに覚える」と書いていた注 4 か所
   D-316  取下げ。⭐ data-figure の「衝突」は意図された設計であった

🔧 残っているもの（台帳に行がある）
   D-380  edit-document.ts の冒頭が「73 命令（見せ方の群 16）」。実数は 72（同 15）
   D-379  data-figure を「1 要素 1 鍵」と読む試験が 2 本
   D-381  GR-21 の錨が、直しても壊しても赤くならない（仕掛けが本体に届いていない）
   ⭐ 新: RS-33 と onlyOneOfPlanAndActualShown は、これを運ぶ押下が無くなった（D-371 の余り）
```

### 5.1 ⛔⛔ **検査を足す前に**

```
⛔ その検査が守らせる規則が、どこかに書かれているかを先に確かめよ
⛔ 数えてから基準線を作れ
⛔ わざと壊して赤くなることを実測してから入れよ
⛔ 検査 34（コメントの px）と 検査 36（RATIONALE の MUST）は入れるな。裁定済
⭐ 検査 35（「〜が持つ」と書いた在り処が実際に在るか）は未計測のまま
```

⭐⭐ **「記録」と「いまの主張」の書き分けは、2026-09-07 に規則 04 の 6.6 へ下りた。**
⭐ **検査 31 と 40 がそれを読む。⛔ 印は ⚠️ ＋ 全角括弧の日付だけである。**
⛔⛔ **ただし、これで直らない型が 1 つ残っている（`D-367` の報告が名指している）** ——
**対応方針が「裁定が要る」（真）で、実物確認が ✅ で始まる（これも真）とき。**
⇒ **実物確認の欄は ✅ で始まる作法なので、印を付けられない。⭐ 直すなら検査の腕そのものを退けるか、
ステータスで門を立てるかであり、それは `PD-441` の外の裁定である。**

---

## 6. ⭐⭐ **利用者の裁定**

⛔⛔ **2026-09-07 の朝に `裁定待ち` は 0 になったが、⭐ 同日の測定で 1 つ増えた。**

### ⚠️⚠️ **`PD-443` —— これだけは利用者に問え**

```
問い  1 フレームの予算に入るには、IF-1（SvgSurface.showSvg が絵を文字列で受ける）を
      変えるほかない。変えるか、合否のほうを動かすか。
数    1 フレーム 20.42ms（上限 16.7ms）。うち 9.2ms が「絵が 1 本の文字列で、
      毎フレーム丸ごと差し替わる」ことから来る（書き込み・解析 3.12ms ＋ style/layout/paint 6.11ms）
候補  ①制限事項として書く ②IF-1 が「変わった所」を受ける ③絵を層に割る ④MC-7 の規模を見直す
⛔    鍵つきの差分は反証済み（15.5ms 対 5.8ms で差分のほうが遅い）
```
⛔ **本文と根拠は `docs/development-records/pending-decisions.md` の `PD-443` が持つ。⛔ 写しを作るな。**

### ⭐ **2026-09-07 に前に立つ者が推奨で進めたもの（⛔ 覆すなら言ってほしい）**

```
D-372 の ③  「作った直後の名称を Enter で確定したとき、同じ 1 回でパネルを閉じ、選択を解く」
            ⚠️ FR-072 の 2026-08-30 の MUST NOT（パネルをやめても選択を解くな）と
            衝突しないよう、作った直後の場面に限った。⭐ 理由: その選択は FR-001 が
            FR-091 のために立てたものであり、名前が着地した時点で役目を終える
D-372 の ①  SP-2 / SP-3 を「形状を変え、あわせて構える」にした（どちらかを選ぶのではなく）
            ⭐ 「必ず構えに入る」を字義どおり満たしつつ、選んだものの形状を変える機能を残す
S-229       見えている範囲に残す日数の下限を 10 とし、表 T-206 の行にした
CM-57       退役させ、S-227 / S-228 は CM-58 が書くようにした
            ⭐ 選んだのではなく導かれた —— 真偽の行になった瞬間、UN-7 と CM-58 が既に両者を覆う
```

⛔⛔ **行を立てる前に、必ず `pending-decisions.md` を grep しろ。**

---

## 7. **測り方の罠 → ⭐ `docs/development-rules/04-verification.md` の 6 節にある**

⛔ **本書に規則を書くな。あちらに足せ。**⭐ **検査 0 が毎回その所在を印字する。**
⭐ **出荷ビルドを押すときの罠は `docs/development-records/evidence/measured-2026-09-07.md` の末尾が持つ。**

⭐⭐ **2026-09-07 に足す価値のあった罠 3 つ:**
```
⛔ IC-20 を無条件に押すな —— オリジンが「有効」を覚えるようになったので、
   押すとむしろ切れる。⇒ 状態を見てから押せ
⛔ browser.newPage() は自前の context を作る。オリジンごとの記憶は
   1 つの newContext() から newPage() を 2 回で測れ
⛔ window.showOpenFilePicker は起動時に 1 度束ねられる。差し替えは
   page.addInitScript でなければ効かない
```

---

## 8. 道具

```
npm run parity             見本と出荷ビルドを 75 の操作で突き合わせる（2026-09-07 は 75/75）
npm test / npm run e2e     ⛔ 両方数えろ。⛔⛔ e2e の前に npx vite build
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
                           ⛔ >/dev/null に流すな（黙って何も書かずに止まる）
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
npm run precheck           ⛔ 編集の「前」に
tools/probe/harness.mjs    出荷ビルドを押す道具。⭐ examples/ に手本がある
tools/probe/examples/frame-attribution.mjs   ⭐ 1 フレームの行き先を段ごとに出す

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated] ／ 図形の identity [data-figure]
        ／ ⭐ 新: スクロールバーの軸 [data-axis]（2026-09-07）
⭐ 選択の枠は fill="none" の <rect> ＋ 両端の 9 × 9 の白い掴み点である
⚠️ バーは polygon である（rect でも path でもない）
⚠️⚠️ data-figure は 1 要素 1 鍵ではない —— 1 つの鍵が「文書順で連続した一続き」を指す
      （実測 2026-09-07: 1,317 要素・899 の鍵・263 の繰り返し、散らばりは 0）

.claude/skills/spec-graph-check/
  check.sh    ⛔ grep だけで判じるな。⭐ 確実なのは終了コードである
              （bash …/check.sh > /tmp/c.txt; echo $?）
  induced.py  ⛔ 仕様を書く前に。閉路は 1 回の書き込みで直す
  impact.py   影響の広がり（深さ 2）
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ プロパティ項目は docs/spec/_source/property-items.json（＋ 同 .schema.json の enum）
  ⚠️ コンポーネントの辺は docs/spec/_source/components.json
  ⚠️ 行を退役させたら md-checks.py の RETIRED に席を burn しろ
```

⛔⛔ **`tests/system` ／ `tests/usecase` ／ `tests/nfr` は Playwright であって vitest ではない**（規則 04 の 6.4）。
⛔⛔ **`dist/index.html` は 2026-09-07 から追跡されている**（利用者の指示）——
**`.gitignore` は `dist/*` ＋ `!dist/index.html` である。⛔ 手で編集するな。`npx vite build` で作り直せ。**
⭐ **`tools/precheck.py` はこの 1 ファイルだけを「手で書く生成物」の罠から外している。理由はそこに書いてある。**

---

## 9. ⛔⛔ **次の巡の目標**

**利用者の指示（2026-09-07、逐語）**:
「**とにかく次のセッションで既知の不具合をすべて直せるようにしろ。
依存関係を明らかにして、仕様を直し、実装を直してテスト。
新規のテストは不要。 テストは不具合修正確認とデグレのチェックでよい。**」

### ⭐⭐ **この巡の試験の方針（⛔ 前の巡から変えていない）**

```
⛔ 新しい試験の様式を起こすな。⛔ 仕様だけを読む体に新規の一式を書かせるな
⭐ 書くのは 2 種だけ:
   ① 不具合修正の確認 —— 直した 1 行につき錨を 1 本
      置き場: tests/system/user-reported-fixes.test.ts
      ⚠️ 同ファイル末尾の自衛の件に、足した行 ID を必ず加えろ（忘れると赤くなる）
   ② デグレの確認    —— npm test / npm run e2e / npm run parity を数えるだけ
⭐ 錨が条項を逐語で持てば、検査 39 の借金（D-377）がそのぶん下がる
```

### ⛔⛔ **触るなと言われている 1 行**

```
D-311（第 8・9・10 章）⇒ 利用者が 2026-09-06 に「一旦そのまま」と述べた
                        ⛔ 取り下げではない。触るな。⛔ 「既知の不具合を 0」の数に入れるな
```

### ⭐⭐ **残っている仕事の依存関係**

```
波 0  反証（読むだけ）
      ⛔ 先に疑うべき 4 行:
        D-376  ばらつく試験。⚠️ 同じ木で 3 回に 1 回落ちた。単独では必ず緑
        D-378  値の変わらない書き込みでも透かしが打ち直される
        D-379  data-figure を 1 要素 1 鍵と読む試験 2 本
        D-382  掃引の SK-19。⛔ D-372 を実装してからでないと期待値が定まらない

波 1  仕様（⛔ 前に立つ者が書く。⛔ 体に渡すな）
      ⛔⛔ 残っているのは 2 束だけである:
        U2  注記      D-302  ⚠️⚠️ leaderShapeKind の退役は erd.json の列を落とすので、
                              スキーマ・型・試験 19 ファイルが同時に動く。
                              ⛔ 原稿と読む側を同じ波で着地させろ（規則 02 の 3.5）
                              ⭐ 引出しは折れ線 1 種（PD-436）。付箋の基準隅は既に FR-019 に在る
        U5  名簿と面  D-273 D-350
                              ⭐ 5 件のうち 4 件は新しいアイコンを要さない:
                                PD-431 改名 → 名簿に入口（MK-13 の作法）
                                PD-433 基本情報 → プロパティパネルの第 3 の中身
                                PD-434 テーマの色相 → 同じ面
                                PD-435 HighlightBox の線色 → 表 T-016 に 1 行
                              ⛔ 高いのは PD-432（暦の面）だけ —— 入口が要るので
                                表 T-109 の行と 図 F-019 の図形を起こすことになる
                              ⛔⛔ property-items.json に行を足すと、読む側が読み込み時に
                                投げる。⇒ properties-panel.ts を同じ波に入れろ（規則 02 の 3.5）

波 2  実装（体に出す。⛔ フォルダで割る）
        B1  ズーム      D-366 D-374 D-375   entity/layout-engine ＋ input-command-translator
        B2  仕切りと面  D-363 D-268 D-357   screen-renderer ＋ svg-renderer ＋ image-exporter
                                            ＋ agent-api-endpoint ＋ single-html-shell
        B3  作成の流れ  D-372 D-373         ⛔ 翻訳器と殻と描画の 3 つに跨がる。
                                            ⇒ B1 と B2 が戻ってから、1 体で出せ
        B4  注記        D-283 ＋ U2 の造り  ⛔ 殻に「日程表の上の編集できる欄」が無いのが壁
        B5  性能        D-330               ⛔ PD-443 の裁定待ち
        B6  ごみ消し    D-380 D-379 D-381

波 3  試験（上の方針の ①）
波 4  実測（掃引を 1 回。⭐ 安い）
```

⛔ **波ごとにコミットしろ** —— **体の worktree は起点で切られるので、
コミットしていない幹の仕事は次の波の体に見えない**（規則 05 の 12、§2 の ①②）。

⛔⛔ **文脈は圧縮される。本書は `defects.md` と `pending-decisions.md` と `git log` だけから書けるように保て。**
