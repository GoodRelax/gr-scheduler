# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。印が多いと、どれが本当に危ないか読めなくなる
> （利用者の指摘 2026-09-02）。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ
4. §1 を読む
```

**サブエージェントは許可されている**（利用者、2026-08-31／2026-09-02 に再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。本書は規則を要約しない** —— 写すと必ず離れていく。

```
01-standing-rules.md    何をおいても守る 8 条 —— 目的に沿って作る
02-changing-the-spec.md 仕様の変え方（変更要求の作法）
03-implementation.md    識別子・命名・コメント・読みやすさ
                        「いま原稿から生成されている定数」の一覧もここ（検査 30 が見ている）
04-verification.md      試験・実物・機械検査 —— 「試験が緑」と「実物で動いた」は別だと定める
                        ⛔ 1 節「期待値をコードに合わせて書き換えてはならない」が
                        前の巡で 4 件の不具合を掘り当てた出典である
05-working-method.md    記録・順番・並行・受け入れ・止まり方
                        7 節が「実装する体と、仕様だけを読む体を対で出す」の出典
                        8 節が「指示は受けたその場で台帳へ写す」の出典
06-pending-decisions.md 裁定待ちの扱い —— A〜C は推奨値で進めてよい、D〜H は止まる
07-review-standards.md  レビュー観点（R1〜R7）
                        ⛔ `Naming` 節は MUST・最重要。実装を出す前に必ず読ませること
```

---

## 0.5 **始点（前の巡の終わりに実測）**

```
単体   149 ファイル / 5939 件 —— 5938 緑 / 1 skip / 赤 0
e2e    tests/system  30 件全部緑
       ⚠️ open-defect-pins.test.ts の PINNED は空になった。門番だけが残っている
同型   npm run parity —— 11/25 一致・14 手が説明の無い食い違い・exit 1（§1.3）
検査   check.sh に FAIL / PROBLEM / DRIFTED なし ／ gen:check 17 本 OK ／ typecheck 清潔
台帳   2 ファイル合計 224 行
       defects.md 65 行（うち 11 行は 実測済 —— 刈り取りの候補）
       fixed-defects.md 159 行
```

⛔⛔ **台帳は 2 ファイルである。`tools/ledger_metrics.py` と 検査 28 / 29 / 31 は両方を読む。**
⛔ **片方しか読まない道具を足すな** —— **刈り取ったぶんだけ空を測り、全部緑になる。**
⛔ **戻り道は片道ではない。実物で落ちたら `fixed-defects.md` から `defects.md` へ戻せ。**

**開いている 65 行の内訳:**

```
実測待ち 16   試験は入った。⛔ まだ出荷ビルドで押していない
未検討   13   §1.4
実測済   11   ⛔ 刈り取れ（§1.5）
仕様待ち  9   規則が無い、または在り処が実在しない
試験待ち  9   実装は在るが、revert しても赤くならない
裁定待ち  4   §1.1
実装待ち  3
```

---

## 1. **この巡の仕事**

### 1.1 **利用者を待っているもの —— 4 件。⛔ 求める前に、これを 3 つやれ**

⛔ **台帳の「未定」を信じるな。前の巡は、塞がっていると書かれた行のうち
実際には塞がっていないものを繰り返し見つけている。**

```
① 実物で押す      「入口が無い」「図形が無い」は、たいてい在る
② 仕様書を引く    表と要求に既に書かれていないか。⛔ 全文を読め、頭だけ読むな
③ 台帳の欄を最後まで読む   ⛔ 冒頭ではなく末尾。裁定は書き足しで末尾に付く
```

**そのうえで残る 4 件。逐語と、体が並べた根拠は台帳の欄に在る:**

| 行 | 問い |
|---|---|
| `D-209` | `FR-019` の「注記」はハイライトボックスだけか、コメントボックスも含むか。⚠️ 用語集 `U-15a` は両方と定義しており、字義どおりならコメントボックスもクリックで置けなくなる。⛔ 体は①（狭める）で実装し、根拠を 3 つ並べた |
| `D-217` | `S-210`（0.75em）がファイル名にも掛かっている。⚠️ 同行の備考は「掛けない」と言うが、名前の大きさを定めた行はどこにも無い |
| `D-221` | `permissionLost` を「開いているファイルが無い」に数えるか。⛔ 体は①（数えない）で実装し、`saveHeldDocumentToFile` が既に同じ読みを採っていることを根拠にした |
| `D-115`／`D-215` | `single-html-shell.ts` に在る規則を測る道。⛔ 2 件は同じ 1 つの裁定を待っている。①`tests/system/` の Playwright ②測る点を継ぎ目として公開する（⚠️ ②は `PI-25` の変更を伴う） |

**利用者が裁定を下したら、その場で台帳へ書け**（規則 05 の 8）。
⛔ **あわせて欄の頭を書き直せ** —— **頭が「未定」のままだと、次の巡が同じ問いを立てる。**
**検査 31 がその腐りを 49 行数えている。直したぶんだけ数が下がる。⛔ 基準線を上げるな。**

### 1.2 **裁定を待たずに始められる、いちばん大きい塊 —— `D-224`**

⛔⛔ **`npm run parity` は 25 手のうち 14 手が食い違い、どちらの名簿にも説明が無い。**
**板の門が止まっていたあいだ、この 14 手は一度も走っていなかった。**

```
11 手 一致
 0 手 既知の不具合で説明される
 1 手 FR-098 の既知の相違として印つき
14 手 説明が無い（exit 1）
```

⛔ **「14 件の不具合」と数えるな** —— **1 つの原因が複数手を割っている見込みが高い。**
**走り下ろした 1 つ目が `D-223`: `IC-90`（1 階層だけ開く）が開かずに行を消す。**
**`Mobile Client` を畳んだ状態でパネルは 7 行を描くが、押すと 4 行になり、
畳んだ件数が 4 に増える。⚠️ 車輪を上へ回しても戻らないので、描く窓の外ではない。**

⚠️ **前の巡が基準も測った** —— **パネルを広げる手当てを外して門を跨ぐと 5/25 一致であった。**
⇒ **`CR-336` の後のほうが良い。悪化ではない。**

**⛔ 割り方**: **14 手を 1 つずつ実物で押し、原因ごとに束ねてから台帳の行にすること。**

### 1.3 **試験は入ったが、まだ実物で押していない 16 件**

⛔ **「単体が緑」は「実物で動いた」ではない。規則 04 がそう定めている。**
前の巡で試験を書いた行は `実測待ち` に置いてある。

```
D-15 D-52 D-65 D-66 D-82 D-92 D-98 D-103 D-105 D-106
D-124 D-126 D-130 D-152 D-166 ほか
```

⚠️ **`D-124` は実物で押しても上がらない見込みがある** —— **殻が `{kind:'none'}` を
無条件に渡しており、`FR-062`／`BT-2`／`OP-14` の MUST が満たされていない。
それ自体が行の中身である。**

### 1.4 **未検討 13 件のうち、手が動くもの**

```
D-206  行を足す入口（IC-91）を押すと打ち込み口は出るが、Enter でも焦点外しでも行が増えない
       ⛔ 副作用が大きい —— 深さ 4 の行が作れないので、FR-085 の深さ 4／5 を実物で測れない
D-207  掴み代を横へ引いても段が変わらない（縦の並べ替えは効く）
       ⚠️ 1 回しか測っていない。まず「段が変わる引きの長さ」の行が在るかを当たれ
D-208  クリップボードへ載せる絵が、書き出す絵と別物（IO-6 が生の絵を渡している）
D-214  StartupNoticeReason が、CR-280 で廃した席 'RS-17' をまだ許している
D-216  tests/contract/spec-table.ts が 表 T-012a を読めない（見出しの直後が別の表）
D-219  目盛の段の名前が仕様のどこにも無い（'yearMonthDayWeekday' など）
D-222  tools/probe/harness.mjs の openPanel() では編集が作れない
       ⚠️ 効くのは Row Title Tree の行名への二度押しである
```

### 1.5 **刈り取り —— `defects.md` の 実測済 11 行**

⛔ **移す条件は変わらない** —— **実物確認の欄が「何を測ったか」を言っていること。**
**日付だけの行は移すな。移した先では二度と読めない。**

---

## 2. **体の使い方**

### ① **実装する体と、仕様だけを読む体を対で出す**

⛔⛔ **前の巡で、この分け方が 4 件の不具合を掘り当てた。どれも試験を緩めれば消えていた。**

```
D-68   IC-53 が掴み帯の中央にいた（仕様は右端）。台帳自身の実装メモが「中央に残る」と認めていた
D-102  ピン止めが廃止された行 UN-16 の下に置かれたままで、取り消してもピンが残った
D-212  Ctrl+R が Open Chooser を立てて問いを上げず、「開く」と同じ動きだった
D-197  幅の判定が 20px 甘く、切れている名前が「切れていない」と名乗っていた
```

**4 件とも、体が規則 04 の 1 節に従って赤のまま置いたことで見つかった。**
⛔ **試験を書く体に `src/` の `.ts` を読ませるな。読ませたら実装の振る舞いを規則として書き写す。**
`src/**/*.json`（名簿・辞書）は許してよい —— データであって振る舞いではない。

⚠️ **体が型の形を知りたいときの正しい道**: **自分の試験ファイルの末尾に `keyof` の
探りを書いて `tsc --noEmit` に通し、消す。**⇒ **公開された契約だけが返り、本体は見えない。**

### ②b **実装を出す体には、規則 07 の `Naming` 節を読ませてから出せ**

⚠️ **触ってはならない名が在る** —— **文書に保存される鍵**（`xxxVisible` など）と
**交換形式の列名**（`dayWorking` / `resumeValid` / `milestone`）と
**ブラウザ API の形**（`altKey` など）。**形が変わると往復無損失が壊れる。**

### ② **「落ちることを確かめろ」まで指示に書け**

```
⛔ 「緑でした」だけの報告を受け取るな。何をすれば赤くなるかを言わせろ。
前の巡の体は、緑の 32 件を 1 つずつ反転させて 32 件とも赤くなるのを見せ、戻した。
別の体は 3 通りの反転で 3 件／13 件／5 件が赤くなることを見せた。
⛔ 落ちない試験は数に入れるな、と指示に書くこと
```

### ③ **発明を拒ませろ。拒否は成果である**

⛔⛔ **前の巡の体は 20 回以上「その規則はどこにも無い」と言って止まり、
そのすべてが本物の穴であった**（`D-133` `D-209` `D-210` `D-211` `D-213` `D-215` ほか）。

```
⛔ 指示に必ず書く一文:
   「仕様のどの行も言っていない値・語・振る舞いが要るときは、そこで止まり、
     何が無いか、どの行を当たったかを名指せ。無い規則は本物の見つけものであり、
     当て推量で書いた試験より価値がある」
⚠️ 体が「当たった先」を列挙して返してきたら、それがそのまま台帳の欄になる
```

### ④ **並列にしてよいのは、触るファイルが重ならないときだけ**

⚠️ **前の巡は 8 体を並走させた。**⛔ **報告が濁った** ——
**互いのファイルを見て「これは他の体の仕掛かりだ」と書き合うことになる。**

```
⛔ 指示に「いま誰がどこを触っているか」を書け。そして
   「木全体の色で判断するな。自分のファイルを名前で走らせろ」と書け
⛔ 仕様を先に入れたら、その場で実装の体を出せ。間を空けると全部の報告が濁る
⛔ npm run gen を 2 体に同時に走らせるな —— 生成物が壊れる
```

---

## 3. **順番**

```
① §1.1 の裁定 4 件を取りに行く（⛔ 求める前に ①②③ をやれ）
② §1.2 の 14 手を割る。⛔ 裁定を待たない。いちばん大きい塊である
③ §1.3 の 16 件を実物で押す
④ §1.4 の手が動くものを体に出す。⛔ ファイルが重ならないように射程を書き分けろ
⑤ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh
⑥ 巡の終わりに刈り取る（§1.5）。⛔ 数を書いていない行は移すな
```

---

## 3.5 **試験をどこまで書くか —— 実測で決めた規則（利用者の裁定 2026-09-01）**

```
利用者が指摘した欠陥で、画面で見えるもの
  → 実測済 にする前に、parity の手か システム試験を 1 つ足す
それ以外
  → 押して測るだけでよい。⛔ 試験の積み増しは課さない
全体
  → ⛔ 単体試験の件数を目標にしてはならない
```

**根拠は実測である。単体試験 5500 件超が生涯で見つけた欠陥は 3 件、利用者は 73 件。**
⇒ **いまの単体試験は「見つける道具」ではなく「戻らないようにする道具」である。**

⚠️ **前の巡はこれに 1 つ足せる** —— **仕様だけを読む体に書かせた試験は、
書いた時点で 4 件の不具合を掘り当てた。**⇒ **書く者を替えると、
同じ試験が「戻らないようにする道具」から「見つける道具」に変わる。**

**固定の仕掛け:**
```
tests/system/open-defect-pins.test.ts     開いた欠陥を expected-to-fail で錨にする
                                          ⚠️ PINNED は空。門番だけが残っている
tests/system/user-reported-fixes.test.ts  利用者が指摘し、直したものの錨
tools/parity/check.mjs の KNOWN_DEFECTS   見本が答えられる欠陥はこちら（いま空）
```

---

## 4. **前の巡で学んだこと（続けろ／繰り返すな）**

### **前に立つ者の誤りは、体が捕まえる。捕まえられる形で出せ**

⛔⛔ **前の巡、前に立つ者は 3 つ間違え、3 つとも体が見つけた。**

```
変更要求に「HF-6 に足す」と書いたが、原稿では HF-15 に入れていた
   → 試験を書く体が引用元を確かめて訂正した
CR-337 で要求と表は直したが、設計章の CP-21 と PI-21 が取り下げた規則を説明したままだった
   → 試験を書き直す体が見つけた
CR-338 で「注記」の語を、用語集の定義の広さを確かめずに使った
   → 実装する体が字義どおりに読み、根拠 3 つを並べて止まった（D-209）
```

⇒ ⛔ **条文に在り処を書いたら、その在り処を開いて確かめてから閉じること。**

### **道具が黙って壊れる 3 つの形**

```
⛔ 表の中に HTML コメントを書くな
   —— 生成器の行走査がそこで止まる。42 項の表が 19 項に見え、
     エラーではなく「一致しません」として出た
⛔ 台帳のセルに縦棒を書くな（`'IO-4'|'IO-7'` のような型の書き方も）
   —— 行が 1 セル増えて metrics が落ちる
⛔ 基準線ファイルを printf などで上書きするな
   —— 数字の下に、測定の由来を書いた 60 行の見出しが在る。
     Python で 1 行目だけを書き換えろ。git diff で必ず確かめろ
```

### **同じことを 2 か所に書くと、検査が数える**

```
FR-020 の理由を S-144 の備考にも写したら、重複検出が new 3 になった
   → 規則の在り処は要求。設定行は指すだけにする
値の表に MUST を丸ごと引用したら、検査 12 が鳴った
   → 引用をやめ、在り処を指す 1 文にする
```

### **試験が緑でも実物は動かない（続き）**

```
前の巡は e2e も parity も緑のまま、実物で 4 件が仕様に反していた
⚠️ parity の板の門が 14 手を隠していた —— 門で止まる不具合は、その先を全部隠す
⇒ ⛔ 門で止まったら「直った」ではなく「ここから先は一度も走っていない」と読め
```

### **測り方の罠（前の巡で踏んだもの）**

```
⛔ 行の名前は切られる。textContent は切られた語を返す
   —— 全文が要るならパネルを広げろ（FR-052）。⛔ 前方一致や記号剥がしで誤魔化すな
⛔ 帯そのものが data-icon を背負っていることがある
   —— readScreenPartAt はいちばん内側を採るので、字を子に切り出すと空白部分が無反応になる
⛔ 外接矩形は clip-path を見ない。見えているかは elementFromPoint で掃け
⛔ [data-depth] は「描かれている窓」であって文書ではない。行は組み直る。名前で引き直せ
⛔ 行の操作子は、その行の名前にポインタが乗るまで隠れている
⛔ バーの本体を押してすぐ引くと二度押し（MK-13）に取られる。1.5 秒空けろ
⛔ 宿主のダイアログは addInitScript で差し替えろ
```

---

## 5. 道具

```
npm run parity             見本と出荷ビルドを 25 の操作で突き合わせる
                           ⛔ 板の門で止まったら、その先は一度も走っていない
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
tools/probe/harness.mjs    出荷ビルドを実測する
                           ⚠️ openPanel() / shapes() / signatureAt() は壊れた実績がある。
                           使う前に確かめろ（D-222）
tools/precheck.py          ⚠️ 生成物を直したあとは「直接編集した」と誤報する。
                           gen:check が緑ならそれが答えである

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated]

.claude/skills/spec-graph-check/
  induced.py  ⛔ 書く前に。閉路は 1 回の書き込みで直す
              ⚠️ 前の巡は 3 つの閉路が、そのまま 3 つの変更要求の割り方になった
  check.sh    grep -n "^FAIL\|^PROBLEM\|^DRIFTED"
  検査 28 / 29 / 31 は台帳 2 ファイルの両方を読む
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ プロパティ項目を足すのは docs/spec/_source/property-items.json

⛔ 日本語の文書を書き換える Python を、シェルのヒアドキュメントに直接書くな
   —— バックスラッシュと引用符が壊れる。スクラッチパッドへ書いてパスで走らせろ
```

---

## 6. 現在地

```
ブランチ restart（push 先は main）
前の巡で書いた変更要求: CR-336 / CR-337 / CR-338
前の巡で閉じた: D-35 D-38 D-68 D-102 D-188 D-197 D-198 D-199 D-201 D-203 D-212
試験を入れた: D-15 D-52 D-65 D-66 D-82 D-92 D-98 D-103 D-105 D-106 D-124 D-126
              D-130 D-152 D-166
新しく起こした: D-206〜D-224
作業樹は 1 つも作っていない
⚠️ 利用者は本リポジトリに限り、コミットと push を任せている。⛔ タグは任されていない
⛔ コミットの前に毎回、個人情報の点検を実測して数を報告すること
```

---

## 7. 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | **裁定待ち** | 4 | **0** | §1.1。⛔ 求める前に ①②③ をやれ |
| 2 | **parity の説明の無い食い違い** | 14 | **0** | §1.2。⛔ 14 件の不具合と数えるな |
| 3 | **実測待ち** | 16 | **0** | §1.3。押すだけで下がる |
| 4 | **刈り取り** | 11 | **毎巡** | ⛔ 数を書かない行は移すな。§1.5 |
| 5 | **赤い試験（単体・e2e・同型）** | 単体 0 / e2e 0 / 同型 14 | **0** | 3 つとも数えろ |
| 6 | **検査 31 の腐り** | 49 | **減らす** | ⛔ 増やすな。欄の**頭**を書き直せ |
| 7 | **`npm run parity` の操作数** | 25 | **30+** | ⛔ ただし 14 手を割ってからにしろ |

⚠️ **総件数が増えることは失敗ではない。失敗は、偽の状態を偽と知らずに置いておくことである。**
⚠️ **前の巡で台帳は 188 行から 224 行へ増えた。増えたぶんの大半は、
仕様だけを読む体が「その規則はどこにも無い」と言って止まった所である。**
