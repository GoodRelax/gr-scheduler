# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **歴代 50 本は `previous-project-result/temp/` に在り、`.gitignore` の `temp/` に
当たって一度もコミットされていない。**⭐ **本書からは、この 1 ファイルを毎巡書き換える。**
⛔ **番号を増やすな。**⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
   ⭐ 走らせたあと、セッション開始時の行に数が入っていることを目で確かめる
2. ハーネスがサブエージェントを禁じているかを確かめ、禁じているなら
   「規則 05 と衝突する。体を使ってよいか」を最初に問う
   ⭐ 前の巡は問うて「使ってよい」を得た。実装と試験を別の体に分けられた
3. ⭐ 利用者が眠っているときの標準指示（2026-08-30 に受けた。逐語）——
   「裁定が不要な分を進めろ。修正が容易な分（定数の変更／1 モジュール内に
     収まる変更）は裁定を待たずに進めて、後でまとめて報告しろ」
```

---

## 0.5 ⛔⛔⛔ **この巡の最初の仕事 —— 赤い試験 8 件を閉じる**

```
実測: 5189 緑 ／ 1 skip ／ ⛔ 赤 8 件（前の巡の終わりに 1 → 8 へ増やした）
```

⛔⛔ **増やしたのは意図的である。仕様が実装より先に進んだ。**⭐ **隠していない。**

| 何件 | 何が赤いか | なぜ |
|--:|---|---|
| **7** | 表 T-023d の全数を突き合わせる契約試験 1 件と、`GR-20` の表示語を画面に出す契約試験 6 件 | **`GR-20`（行を掴んで動かす）を仕様へ入れたが、実装が 1 行も無い** |
| **1** | `tests/unit/fr-052-t-023d-picture-while-held.test.ts` の「rounds to whole days」 | `PD-394`（裁定済・未実装。下の 2 節）|

⭐ **7 件は `D-156` の実装をすれば消える。**⛔ **試験を書き換えて消してはならない** —— **仕様は正しく、実装が無いだけである。**

---

## 0.6 ⭐⭐ **進捗の測り方（利用者の裁定 2026-08-30）**

```
⭐ 残件 = テスト完了 でも 取下げ でもない案件。⇒ これを減らすことが進捗である。
⛔ tools/ledger_metrics.py が刷る。手で書くな。
実測（前の巡の終わり）: 残件 103 ／ 総件数 157
```

⚠️ **前の巡は 102 → 103 に増えた。**⛔ **失敗ではない**:

| | 数 | 何が |
|---|--:|---|
| 閉じた | **2** | `D-71` / `D-75`（実物で確かめて `テスト完了`）|
| 開いた | **3** | `D-155` / `D-156` / `D-157` —— **すべて実測で見つけた本物の穴** |
| 差し戻した | **3** | `D-74` / `D-128` / `D-129` —— **状態表示が偽であった** |

⭐ **「閉じた数」と「開いた数」を並べて報告すること。**

---

## 0.7 ⛔⛔ **台帳と仕様書の突き合わせは済んでいる（もう一度やる必要は無い）**

⭐ **別の体に全 154 行を当たらせた**（前の巡）。結果を 21 行へ書き戻した。

| 区分 | 数 | 意味 |
|---|--:|---|
| **A** | **0** | `仕様確定` なのに既に仕様書に在る行は**無かった** |
| **B1** | **94** | 欄 5 が仕様書の場所を名指さない。⭐ **検査 29 の基準ちょうど** |
| **B2** | **6** | ⛔⛔ **場所は名指すが、その場所がそう言っていない。**⇒ **黙って通る記載漏れ** |
| 腐り | **8** | 欄 5 が「未定」だが、**仕様書は既に決めている** |
| 状態が偽 | **3** | 仕様書が「利用者の裁定である」と留保しているのに `テスト中` 以降に居た |

### ⛔⛔ **B2 の 6 件 —— まだ 1 件も手をつけていない**

| ID | いま | 何が足りないか |
|---|---|---|
| **`D-135`** | `テスト中` | ⛔⛔ **仕様書が、実測で偽と分かった文を持ったままである** —— 表 T-013 直後の注が**いまも**「フェードを持つのは `SH-1` と `SH-2` だけ…ほかの 3 つの形状で本規則は何も変えない」と断定する。⭐ **これが筆頭** |
| `D-62` | `テスト中` | 仕様書は本行の期待値の**反対**を裁定として明記（表 T-028 の `IN-2`）|
| `D-65` | `テスト中` | `AT-127` は**保管が UTC** としか言わない。「ヘッダーはローカルタイム」「2 段小さく」に当たる行が 0 件 |
| `D-61` | `テスト中` | 「プロパティパネルが『面』か」をどこも述べていない |
| `D-55` | `テスト中` | `S-196` は**予定の図形の上端**からしか測らない |
| `D-105` | `テスト待ち` | `FR-036` は**利用者が否とした順序**を定めたままである |

⚠️ **どれも欄 5 が仕様書の行 ID を持つので検査 29 を素通りする。**
⇒ ⛔ **検査 29 は「名指しているか」しか見ない。「そこがそう言っているか」は人が読むしかない。**

⚠️ **判定**: 要求 UID／表の行 ID／`表 T-051` / `図 F-019` / `Chapter 6.1` のいずれか。
⛔ **`D-` / `PD-` / `CR-` は数えない。**⛔ **仕様書の散文に書くと検査 7 が落ちる**（前の巡で 3 度踏んだ）。

---

## 1. ⛔⛔⛔ **この巡の最優先 —— 図形を起こし、掴みを実装する**

### ⭐ 行の操作子の再整理は**裁定が全部そろった**（前の巡）

**`PD-396` / `PD-399` 〜 `PD-405` の 7 件すべてが 裁定済 である。**
**`CR-311` / `CR-312` / `CR-313` で仕様書へ入れた**（版 1.65 / 1.66 / 1.67）。

| 裁定 | どこに入ったか |
|---|---|
| 効いている入口は**縁ではなく塗り**で示す | `FR-029` ／ 表 T-237（`EN-1` 〜 `EN-4`）|
| 1 本の `∨` ＝ **配下を 1 階層開く** | 表 T-015 の `HR-7` ／ 表 T-051 の `HF-13` |
| 留まっている行のピンを**常時描く**／ピンを並びの**いちばん外**へ | `HF-6` の例外 ／ `HF-4` |
| **畳む前の形を覚えない** | `HR-1a` |
| 頭の「すべて畳む」は**最も浅い段も畳む** | `HR-2` ／ `HF-12` |
| 行を**足す**（末子・空の名で立てる）| `HR-8` ／ `HF-14` |
| 行を**掴んで動かす**（軸 1 本・配下ごと・段は上下で変わらない）| 表 T-023d の `GR-20` ／ `HF-15` ／ `RS-36` 〜 `RS-39` ／ `S-208` |
| 折り畳みの図形を**1 家族へ描き直す** | 表 T-026 の `RC-13`（⛔ **図形はまだ無い**）|

### ⛔⛔ **残っている仕事は 4 つ。この順である（利用者の指示 2026-08-30）**

```
① GR-20 と HF-15 を実装する  ⇒ 赤い試験 7 件が消える（0.5 節）
② ⛔⛔ 押しても効かない入口 5 件（D-146 〜 D-150。4 節）
   ⭐⭐ 利用者の指示: 「効かない入口 5 件を図形より先にやれ」（2026-08-30）
   ⛔ 2 巡続けて手つかずである。⭐ D-148 から入れ —— 原因の形が分かっている
③ 図形を 4 つ起こす           ⇒ 単一の ∧ ／ 単一の ∨ ／ 二重の ∨ ／ 枠つき ＋
   ⭐ 二重の ∧ は IC-59 の図形がそのまま当たる（実測）
   ⛔ 図形の決定は利用者の裁定である（FR-029）。⇒ 描いて見せて、問うこと
④ HR-6（行を隠す）の入口と、U-29（Hidden Group Tab）の廃止
   ⛔ U-29 には生きた参照が 3 か所ある —— Chapter 1 の散文／HR-6 の本文／EP-4
```

⭐⭐ **②を③より先にする理由**（利用者の裁定）—— ⛔ **効かない入口は「押しても何も起きない」であり、
`FR-029` の RATIONALE が第 1 文で挙げる「無反応だと故障に見える」そのものである。**
⚠️ **図形をいくら整えても、押して動かない入口が 5 つ残っていれば読めるようにはならない。**

⭐⭐ **③ は 5 日前に止まっていた作業である。**版 1.47（CR-294）の逐語:

> ⛔ **`HR-6`（行を隠す）の入口は足していない** —— 同行が「親の非表示グループタブから戻せること（MUST）」を
> 同時に要求しており、**そのタブは実装に 1 つも無い**。⭐ **入口だけ足すと、隠したら二度と戻せない行ができる。**

⇒ ⭐ **裁定「戻すのは親の `∨`」が、その障害を取り除いた。**⛔ **入口を置けるようになった。**

### ⭐ 動く見本が `previous-project-result/11-row-controls/` に在る

**`row-controls-sample.html` は 1 ファイルで動く。**⭐ **実装の前に必ず触れ** ——
**軸ロック・親をまたぐ上下・段の左右・拒みの理由・畳みの不変条件が、すべて動く形で入っている。**
`README.md` に 2 軸の表・裁定済み 7 件・仕様に要る仕事が並んでいる。

---

## 2. ⭐ 裁定済みで、まだ実装していないもの

### ⛔ `PD-394` —— **もう 1 つの赤い試験**

**裁定（2026-08-30）**: 表 T-023d の結びの自己矛盾を **案 A** で解く。

> 「**いずれも 1 日単位に四捨五入する**」を採り、
> 「**ポインタの下の日から求めること**」を「**ポインタの位置から求めること**」へ改める。

⇒ **CR を 1 本（結びの 1 文）＋ 実装。**⭐ **1 文と 1 箇所なので、巡の頭で片づく。**
⚠️ **2 巡続けて着手していない。**

### ⭐ 表 T-233 に「入口」の欄を足す（裁定済み・案 A）

⛔ **入口と理由の対がいま散文にしか無く、機械で確かめられない。**

---

## 3. ⛔ 実装できずに残っている MUST（**仕様の側に不足がある**）

```
⛔ FR-098「帯が Row Area を埋め尽くしてはならない」  上限の数が無い
⚠️ LF-14 を持つ SW_SPEC の節が Chapter 6.1 に無い
⚠️ 命令の返り値について仕様が何も言っていない（D-140 の体が報告）
```

⭐ **仕様だけを読む体が挙げた 3 つ**（`CR-311` の範囲。**まだ直していない**）:
- **`FR-029` の「上の行が勝つ」に届く帰結が無い** —— `EN-1` / `EN-2` / `EN-4` はいずれも `S-183`
- **`EN-2` を画面記述のどのメンバが運ぶかを、どの行も言っていない**
- **入口の外形 26 × 24px の「26」を試験で押さえられない** —— **枠の 1px を持つ行が無い**

---

## 4. ⛔⛔ 押しても効かない入口（**5 件。⭐ 図形より先にやる —— 利用者の指示 2026-08-30**）

```
D-146 IC-37 / IC-38  選択したものを開始日・終了日で揃える。バーが 1px も動かない
D-147 IC-41          透かしを隠す。DOM も設定値も面も動かない
D-148 IC-49          ガイドカーソルの 2 連縦線。⭐ 兄弟 3 つは動く（測り直し済み）
D-149 IC-18          AI 対話。IC-20 を押すと IC-18 の押下状態が勝手に立つ
D-150 IC-39          イナズマ線。⚠️ 欠陥と断定していない。先に IC-44 を測れ
```

⭐ **`D-148` が第一である** —— `IC-47` で `svg line` が 97 → 99、`IC-48` で 97 → 98、
⛔ **`IC-49` は 97 のまま 1 本も増えない。**兄弟のうち 1 つだけが死んでいる形である。

---

## 5. ⭐ 現在地（**2026-08-30 の巡の終わり。すべて実測**）

```
✅ push 済み: origin/main = 51e239c（この巡の 5 本）。⛔ そのあとの 1 本は未 push
⚠️ ブランチ名は restart のまま。push 先は main（git push origin HEAD:main）

⭐⭐ 検査 29 本 ALL GREEN ／ typecheck 緑 ／ layers 緑（284/284）
tables 136 ／ figures 11 ／ rows 1753 ／ uids 150 ／ アイコン 85 ／ 語 396 対
⭐ 残件 103（巡の頭は 102）／ 台帳 157 行（巡の頭は 154）／ テスト完了 52（＋2）
⚠️ 追加裁定待ち 6 ／ 仕様書反映済み 1（D-156）／ 仕様確定 0
裁定待ち 174 行・うち未裁定 128
⛔ 試験 115 ファイル / 5198 件 —— 5189 緑 ／ 1 skip ／ ⛔ 赤 8（0.5 節。7 件は仕様が先行）
```

**この巡で仕様へ入れたもの**: **CR-311**（1.65）／ **CR-312**（1.66）／ **CR-313**（1.67）

✅ **作業樹 163 個（2.6 GB）と、その枝 111 本を消した**（利用者の裁定 2026-08-30）。
**残る枝は `main` と `restart` の 2 本だけ、作業樹は本体 1 つだけである。**
⭐ **`grep -rn` が作業樹を拾って汚れる問題も消えた。**

⛔⛔ **前に立つ者は「消すとそこにしか無いコミットが辿れなくなる」と言って残そうとしたが、偽であった。**
⭐ **測ったら 111 本すべてが `origin/main` から 0 コミット先行** —— **独自のコミットを 1 つも持っていない。**
⚠️ **当然である** —— **体はコミットしない**（成果はファイルを写して取り込む）ので、
**枝は作業樹を切り出した時点のコミットを指す札のまま止まる。**

⭐⭐ **巡の終わりに落とす習慣にすること。**⛔ **体に作業樹を与えるたびに増える。**

```
git worktree list                                   # 本体 1 つだけか確かめる
git branch --list 'worktree-*' --no-merged origin/main   # ⛔ 空でなければ消すな
git branch -D $(git branch --list 'worktree-*' | tr -d ' *')
rm -rf .claude/worktrees && git worktree prune
```

⚠️ **`.git/worktrees/` の残骸は `git worktree prune` が「Permission denied」で消せないことがある。**
⭐ **`git worktree list` が本体だけになっていれば、`rm -rf .git/worktrees` で落として害は無い。**

---

## 6. ⭐⭐ この巡でいちばん効いた方法（**続けろ**）

```
⭐⭐⭐ 実装した当人に試験を書かせない（規則 05 の 7）。
    仕様だけを読む体が 57 件書き、49 件が実装を触らずに緑になった。
    ⛔ そしてその体が、前に立つ者が書いたばかりの仕様の欠陥を 3 つ見つけた。

⭐⭐⭐ 第三者の体 2 つに、互いに独立に検めさせる。
    ① 実物を叩く体（約 2,700 手）② 仕様だけを読む体
    ⛔ 欠陥 17 件が出て、書いた者は 1 つも見つけられていなかった。
    ⭐ 2 つが独立に同じ結論へ達したものが 2 件あった —— 二重に確かである。
    ⛔ 何を直したかを体に伝えないこと。伝えるとそこだけ見て通す。

⭐⭐ 体の第一報を鵜呑みにせず、前に立つ者が測り直す。この巡で 3 度効いた:
    ① 「ピンの style に ; が抜けている」は偽。right:1.5em は 24px に解決していた
    ② 引継書の「スクロールを効かないに数える」も偽。900px 動かしても変わらない
    ③ 前に立つ者自身の「HR-5 の一押しを失う」も偽。∧∧ 一押しで同じ絵になる

⭐⭐ 動く見本を渡すと、裁定が速い。利用者の指示「別の .html にサンプルを示せ」。
    絵より速い —— 触ると、言葉で説明できない違和感がその場で出る。

⭐ 数を 2 か所に置かない。刻み 26px と段送り 16px が離れていった実測が根拠。
    ⇒ 「S-37 と同じであること」と規則で書き、設定値を新設しなかった。
```

---

## 7. ⛔⛔ この巡で前に立つ者が犯した誤り（**繰り返すな**）

```
⛔⛔ A-appendix.md を 0 バイトに切り詰めた。
   補助スクリプトが 📌 のようなサロゲートペアの実行時に例外を投げ、
   すでに書き込みで開いていたファイルが空になった。git から復元した。
   ⭐ 直し方: 追跡ファイルを書き換えるスクリプトは一時ファイルへ書いて置き換える。
   ⛔ 絵文字をエスケープで書かない。書くなら literal で書く。

⛔⛔ シェルの二重引用符の中にバッククォートを置いた。
   `Math.trunc` や `S-37` がコマンド置換として食われ、空白で書き込まれた。
   ⭐ 直し方: python はヒアドキュメントかファイルにする。-c "…" に入れない。

⛔ 仕様書の散文に PD- / D- を書いて、検査 7 に 3 度捕まった。
   ⭐ 台帳・裁定待ち・変更要求は仕様書の場所ではない。指すな。

⛔ 同じ MUST を 2 か所に書いて、重複検出に 2 度捕まった。
   ⭐ 直し方は 1 つ: 持ち主を 1 つ決め、他方は指すだけにする。

⛔ 台帳の 実物確認 の欄に飾りを付けた。検査 28 は素の「✅ 2026-08-30」を読む。
⛔ 裁定待ちの 状態 の欄に辞書に無い語を書いた。使えるのは 未裁定 と 裁定済 だけ。

⛔⛔ 測らずに「危ないから残す」と言った。
   作業樹の枝 111 本を「消すとそこにしか無いコミットが辿れなくなる」と言って残そうとしたが、
   利用者に問われて測ったら 111 本とも 0 コミット先行で、独自のものは何も無かった。
   ⭐ 直し方: 「危ない」と言う前に測る。git branch --no-merged が 1 行で答える。
   ⚠️ 慎重に見える誤りは、雑な誤りより見つかりにくい。

⛔⛔ 自分の書いた仕様の欠陥を、自分では 1 つも見つけられなかった。
   表 T-237 の行 ID を code span で囲ったこと、白の禁止が自己矛盾していたこと、
   「図形を描く箱」の逐語が塗り残しを生むこと —— 3 つとも体が見つけた。
   ⭐ 仕様を書いたら、必ず別の体に「これで試験が書けるか」を問わせる。
```

---

## 8. ⭐ 道具（**30 行の下ごしらえを二度と書くな**）

```
tools/probe/harness.mjs   ⭐ export 40。出荷ビルドを開いて実測する
⛔ rows() は y で並べ替える ⇒ DOM の並びとの食い違いは見えない。treeOrder() を使え
⛔ 行の操作子は [data-role="Row Title Panel"] の子孫ではない。領域（座標）で絞れ
⛔ 行は [data-depth] で拾う。[data-role="Row Title"] という役は存在しない
⚠️ 実測した行の高さは 64 / 96 / 101 / 108 / 148px とまちまち。決め打ちするな
⛔⛔ 体が dist を建て直すので、絵を描く前に dist/index.html を scratchpad へ写せ

.claude/skills/spec-graph-check/
  induced.py   ⛔ CR を書く前に走らせる。巡回ごとに 1 計画・1 度の書き込み
  impact.py    深さ 2 の波及
  check.sh     29 本。⚠️ 落ちても「FAILURES ABOVE」としか出ない。
               ⭐ awk '/^===== /{s=$0} /^(OK|PROBLEM|DRIFTED)/{if($1!="OK")print s" -> "$0}'
  ⚠️ 表に行を足したら npm run words も要る —— 表示語の原稿が全数を持っている
```

---

## 9. ⛔ 名前の残り（**利用者の指示 2026-08-30**）

> **極端に短い変数は意味をあらわさない。切れの良いタイミングで、名は体を表すような変数名に改めろ。**

```
tools/probe/harness.mjs    0 件（済み）
src/                       27 件（on / is / to など属性名でもある英単語。手で直す）
tests/                     539 件（未着手）
⭐ 数え方: コメントと文字列を空白化してから数えること（scratch/blank_noncode.py）
```

---

## 10. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔ **赤い試験** | 8 | **0** | 7 件は `GR-20` の実装で消える。1 件は `PD-394` |
| 2 | ⛔ **B2 の 6 件**（0.7 節）| 6 | **≤ 3** | `D-135` が筆頭。**仕様書が偽の文を持っている** |
| 3 | ⛔⛔ **押しても効かない入口**（D-146〜D-150）| 5 | **0** | ⭐ **図形より先にやる**（利用者の指示）。`D-148` は原因の形が分かっている |
| 4 | **新しく起こす図形** | 0 | **4** | 描いて見せて、利用者の裁定を得る。⚠️ **③のあとである** |
| 5 | **テスト完了 の行数** | 52 | **60** | 体 6 本 × 1〜2 行 |

⚠️ **総件数が増えることは失敗ではない。**⭐ **状態を正して 残件 が増えるのも失敗ではない。**
⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
