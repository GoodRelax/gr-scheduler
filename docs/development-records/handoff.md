# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ
4. §1 を読む
```

**サブエージェントは許可されている**（利用者、2026-08-31／09-02／09-03 に再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。本書は規則を要約しない** —— 写すと必ず離れていく。
⚠️ **前の巡で 2 つ増えた**: 規則 04 に **3.5 節**（労力は歩留まりに合わせて配る）、
規則 05 に **「行は症状ではなく原因で立てる」**と**「台帳の数は 5 つの束で読む」**。

---

## 0.5 **始点（前の巡の終わりに実測）**

```
単体   153 ファイル / 6010 件 —— 6009 緑 / 1 skip / 赤 0
e2e    tests/system  59 件全部緑（うち 23 件は 1 回の起動で回る掃引。1.5 分）
同型   npm run parity —— 50/50 一致・説明の無い食い違い 0・exit 0（44 清・6 は FR-098 の説明つき）
検査   check.sh 32 本 ALL GREEN ／ gen:check 17 本 OK ／ typecheck ／ layers 清潔
       audit-ch5.py RESULT: PASS（⭐ CR-280 以来はじめて）
台帳   229 行。残件 10
```

**残件 10 の内訳 —— ⛔ コードを書く仕事は 0 である:**

```
裁定待ち 5   §1.1。⛔ 利用者を待っている。これが唯一の詰まり
実測待ち 5   §1.2。⚠️ うち 4 件は「出荷ビルドから測れない」ことを実測済み
```

⭐⭐ **台帳の数は 5 つの束で読む**（`tools/ledger_metrics.py` が刷る）。
⛔ **「不具合」と呼んでよいのは `製品の仕事` だけである。いまそれは 0 である。**

---

## 1. **この巡の仕事**

### 1.1 **利用者を待っているもの —— 5 件。⛔ 求める前に、これを 3 つやれ**

⛔ **台帳の「未定」を信じるな。**⚠️ **前の巡は、塞がっていると書かれた 13 行のうち
11 行が実際には塞がっていないことを実測した。**

```
① 実物で押す      「入口が無い」「図形が無い」は、たいてい在る
② 仕様書を引く    表と要求に既に書かれていないか。⛔ 全文を読め、頭だけ読むな
③ 台帳の欄を最後まで読む   ⛔ 冒頭ではなく末尾
```

**そのうえで残る 5 件。逐語と根拠は台帳の欄に在る:**

| 行 | 問い | 前の巡の推奨 |
|---|---|---|
| `D-195` | **透かしを描くのに要る 4 つの値が、仕様のどの行にも無い** —— 角度・文字の大きさ・繰り返しの間隔・インクの色。⛔ **5 つ目**: `S-102`（`watermarkOpacity`）は在るが、生成器が 表 T-207 に宛先を 1 つしか与えていないので**どのユニットにも届かない** | 4 値を 表 T-207 に足し、生成の宛先を増やす |
| `D-154` | **`HF-15` が「操作子は掴み代を覆ってはならない（MUST NOT）」と定めながら、同じ行で「幅が足りないときに何を縮めるかは定めない」とも言う** ⇒ ⛔ **採れる手当てはすべて禁じられているか未決である** | 縮める先を 1 つ決める |
| `D-228` | **描いた直後のタスクを選択にするか** —— `FR-083` の**散文**はそう言うが、求める MUST の行が無い | ①`FR-001` に 1 文足す |
| `D-145` | **表示語と規則のセルの食い違いを、機械で捕まえるか** —— ⛔ **32 本のどれも捕まえない。**⚠️ 語をセルへ写して比べる形は `FR-038` の MUST NOT が禁じている | 共変検査を足す |
| `D-219` | **目盛の段に名前を与えて表に載せるか** —— 名前は原稿に 0 件。⚠️ **どちらでも落ちる試験は 0 件** | 載せる |

**裁定が下りたら、その場で台帳へ書け**（規則 05 の 8）。
⛔ **あわせて欄の頭を書き直せ** —— 頭が「未定」のままだと、次の巡が同じ問いを立てる。

### 1.2 **`実測待ち` 5 件 —— ⚠️ 4 件は「押せない」ことが実測済みである**

```
D-49    押せば閉じる。試験は 19 件入っている
D-15    ⛔ FD-6 も IV-12 も画面に出ない。継ぎ目でしか見張れない
D-124   ⛔ 起動時に文書を渡す道が無く、OP-14 に一度も到達しない
        ⇒ 表 T-034 の BT-2 を配線するしかない。それ自体が行の中身である
D-202   ⛔ 綴じている本体は画面の中の要素ではないので、外から起こせない
D-211   ⛔ IV-19 は文書の不変条件で、答えを画面へ出す要求が無い
```

⛔⛔ **状態の模型そのものの問いがここに在る** —— **「緑だが押していない」と
「押しようがない」を、同じ `実測待ち` が抱えている。**⚠️ **放っておくと永遠に閉じない。**
⇒ **利用者に問うか、`実測済` の定義を「継ぎ目で見張った」まで広げるかを決めること。**

### 1.3 **⭐ 押しに行くのは 1 件ずつやるな**

`tests/system/measured-sweep.test.ts` が **1 回の起動で 23 件を 1.5 分**で判定する。
⛔ **件ごとに起動し直すな。**⭐ **閉じるだけでなく、そのまま戻り止めになる。**

---

## 2. **体の使い方**

### ① **実装する体と、仕様だけを読む体を対で出す**

⛔⛔ **前の巡、この分け方が 2 つの偽の緑を掘り当てた:**

```
表 T-034 の手写しが、CR-280 で退いた行を 5 日間持ったまま緑であった
        —— 手写しの中で辻褄が合っていたので誰も気づかなかった
前に立つ者がその日書いた条文が偽であった（「床は読む人の文字サイズが動かす」）
        —— S-138 が「⛔ 閲覧者の文字サイズに追随させない」と逐語で定めている
```

⛔ **試験を書く体に `src/` の `.ts` を読ませるな。**`src/**/*.json` は許してよい。
⚠️ **型の形が要るときは、自分の試験の末尾に `keyof` の探りを書いて `tsc` に通し、消させろ。**

### ② **「落ちることを確かめろ」まで指示に書け**

⛔ **「緑でした」だけの報告を受け取るな。**前の巡の体は、それぞれ 8 件・6 件・5 件・
2 件の反転を見せて戻した。**落ちない試験は数に入れるな、と指示に書くこと。**
⭐ **道具を直したときも同じである** —— 前の巡は 9 通りの変異で `audit-ch5.py` の
検査が 1 つずつ噛むことを見せた。

### ③ **発明を拒ませろ。拒否は成果である**

⛔⛔ **前の巡、体は 5 回止まり、5 回とも本物の穴であった**（`D-195` `D-154` `D-228` ほか）。

```
⛔ 指示に必ず書く一文:
   「仕様のどの行も言っていない値・語・振る舞いが要るときは、そこで止まり、
     何が無いか、どの行を当たったかを名指せ。無い規則は本物の見つけものであり、
     当て推量で書いた試験より価値がある」
```

### ④ **並列にしてよいのは、触るファイルが重ならないときだけ**

⭐ **前の巡は 4 体を 2 回、並走させて濁らなかった。**⛔ **効いたのは次の 3 つである:**

```
⛔ 指示に「いま誰がどこを触っているか」を表で書く
⛔ 「木全体の色で判断するな。自分のファイルを名前で走らせろ」と書く
⛔ 報告を 40 行以内に収めさせる（前に立つ者の枠が保つ）
```

⛔ **`npm run gen` を 2 体に同時に走らせるな。**⛔ **`npm run build` も 1 体だけにしろ。**

### ⑤ **前に立つ者のブリーフは間違う。体に訂正させろ**

⛔⛔ **前の巡、前に立つ者は 4 つ間違え、4 つとも体が捕まえた:**

```
`S-37` は 12 だと書いたが、いまは 16 である（利用者の裁定 2026-09-01）
`MK-13` を 表 T-028 と書いたが、表 T-023 に在る
床が「読む人の文字サイズで動く」と条文に書いたが、S-138 が逐語でそれを禁じている
`D-154` の「8 行のうち 6 行」は CR-336 前の古い数であった
```

⇒ ⛔ **ブリーフに数や在り処を書いたら、「原典で確かめてから使え」と添えること。**

---

## 3. **順番**

```
① §1.1 の裁定 5 件を取りに行く（⛔ 求める前に ①②③ をやれ）
② §1.2 の状態の模型の問いを片づける（⛔ 4 件が永遠に閉じない形になっている）
③ 裁定が下りたら、仕様 → 実装 ‖ 試験 の順で 1 巡にまとめる
④ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh
⑤ 巡の終わりに刈り取る。⛔ 数を書いていない行は移すな
```

---

## 3.5 **試験をどこまで書くか —— 実測で決めた規則**

**規則 04 の 3.5 節が持つ。本書は要約しない。**⭐ **要点だけ**: 単体試験の増産には上限が
ある。書いてよいのは①実物で測って直したものを固定するとき②仕様だけを読む体が書くとき。
⛔ **単体試験の件数を目標にしてはならない**（利用者の裁定 2026-09-01）。

**固定の仕掛け:**
```
tests/system/measured-sweep.test.ts       23 件。1 回の起動で 1.5 分
tests/system/rows-fixed-with-nothing-holding-them.test.ts  12 件。殻の規則を測る唯一の道
tests/system/user-reported-fixes.test.ts  利用者が指摘し、直したものの錨
tests/system/open-defect-pins.test.ts     ⚠️ PINNED は空。門番だけが残っている
tools/parity/check.mjs                    50 手。⭐ いま 50/50 一致（44 清・6 は説明つき）
```

---

## 4. **前の巡で学んだこと（続けろ／繰り返すな）**

### **1 つの原因が 14 の症状を割っていた**

⛔⛔ **`npm run parity` の 14 手の食い違いは、14 件の不具合ではなく 1 件であった。**
**`Task` を持たない行の帯は矩形 1 つぶん（22〜28px）で、`HF-1` の格子は 48px。
下段の 2 つが次の行の上に立ち、押しがそちらに取られていた。**
⇒ ⭐ **床を 1 つ置いたら 11/25 が 25/25 になった。**
⛔ **症状を数えるな。原因を数えろ**（規則 05 に入れた）。

### **仕様の裏付けの無い数が、1 巡ぶん defect を隠した**

⛔ **`tools/parity/*.mjs` が「行は 64〜148px（`FR-042`）」と書いていたが、
その範囲は仕様のどこにも無い。**⭐ **床は 64 だと信じた読み手は、22px の行を探さない。**
⇒ ⛔ **道具のコメントに書いた数にも、在り処を付けろ。**

### **一致しきった検査は、もう問うていない検査である**

⭐⭐ **`npm run parity` は 25/25 で頭打ちであった。**⛔ **それは「欠陥が無い」ではなかった。**
**手を 25 → 50 に増やした**（使い切った入口を押す／ピンを 3 つ立てる／ピン止めした行の祖先を
畳む・隠す／真ん中のピンから外す／子を持たない行を畳む・開く）。**50 手すべて一致した。**
⚠️ **ところが、増やす作業の副産物として新しい欠陥が 1 件出た**（`D-229`）——
**書き出した絵が、人が畳んだ状態に追随しない。**
⛔⛔ **見本は書き出しを持たないので、parity では永久に出ない種類であった。**
⇒ ⭐ **発見器は 1 つでは足りない。parity・掃引・仕様だけを読む体は、別のものを捕まえる。**

### **原因は 1 つとは限らない。`D-229` は 2 つであった**

⛔ **`exportScene()` の中に 2 つ在り、それぞれ別の状態を割っていた** ——
①`fromTemplate` を `false` 固定で渡していた（触らない状態と全開きが割れる）
②`S-211`（段 0 の畳み）が書き出しへ届いていなかった（全畳みが割れる）。
⭐ **1 か所ずつ戻して測ったので、どちらがどれを割っているかが数で出た。**
⇒ ⛔ **「症状が消えた」で止めるな。原因ごとに戻して確かめろ。**

### **道具は黙って壊れる。`check.sh` に載っていないものは特に**

```
audit-ch5.py       CR-280 以来ずっと赤。⛔ check.sh に入っていないので誰も見ていなかった
spec-table.ts      表 T-012a を読めなかった。原因は見出しの決め打ち 1 か所
harness.mjs        openPanel() が `p`（＝コマンドパレット）を押していた
禁止語「部品」      規則を書いた文書が自分で使っていた。32 本のどれも捕まえない
```
⇒ ⭐ **4 つとも直した。禁止語の検査は 検査 32 として `check.sh` に載せた（基準線なし）。**

### **道具が黙って壊れる 3 つの形（続き）**

```
⛔ 表の中に HTML コメントを書くな —— 生成器の行走査がそこで止まる
⛔⛔ 台帳のセルに縦棒を書くな —— ⚠️ 前の巡、前に立つ者が 1 日に 2 度踏んだ
     （`| 点 |` と正規表現の `\|`）。行が 1 セル増えて metrics が落ちる
⛔ 基準線ファイルを printf などで上書きするな —— 由来を書いた見出しが下に在る
⛔ 生の制御文字を書くな —— ⚠️ 試験ファイルに生の NUL が 3 つ入っていた。
     grep がファイルをバイナリと判定し、公開前の点検が中身を読めなくなる
```

### **基準線の数は、それ自体が嘘をつく**

⚠️ **検査 31 の「腐り 49」は、実際には 3 であった** —— **46 は決着済みの側に在り、
誰も読み返さない。**⇒ ⛔ **総数を目標にするな。効く数だけを数えろ**（基準線に由来を書いた）。

### **試験が緑でも実物は動かない（続き）**

⭐ **前の巡は 5 件を「実物では測れない」と実測で示した。**⛔ **これは失敗ではない** ——
**測れないこと自体が行の中身である。**⚠️ **ただし `実測待ち` に置いたままにすると
永遠に閉じない。**⇒ §1.2。

---

## 5. 道具

```
npm run parity             見本と出荷ビルドを 50 の操作で突き合わせる。⭐ いま 50/50
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
tools/probe/harness.mjs    出荷ビルドを実測する。⭐ openPanel() は直った（二度押し）
                           ⚠️ signatureAt は半径内の DOM 順で最初を返す。
                             広い帯がバーに重なる所では帯を名乗る
tools/precheck.py          ⚠️ 生成物を直したあとは「直接編集した」と誤報する。
                           gen:check が緑ならそれが答えである

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated]

.claude/skills/spec-graph-check/
  induced.py  ⛔ 書く前に。閉路は 1 回の書き込みで直す
  check.sh    32 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED\|FAILURES ABOVE"
  audit-ch5.py  ⭐ 直った。絶対数の直書きをやめ、読んだものどうしの関係にしてある
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ プロパティ項目を足すのは docs/spec/_source/property-items.json

⛔ 日本語の文書を書き換える Python を、シェルのヒアドキュメントに直接書くな
   —— バックスラッシュと引用符が壊れる。スクラッチパッドへ書いてパスで走らせろ
```

---

## 6. 現在地

```
ブランチ restart（origin/restart へ push 済み。63e030b）
前の巡で書いた変更要求: CR-339 / CR-340 / CR-341 / CR-342 / CR-343
前の巡で閉じた: D-27 D-49(試験) D-115 D-116 D-120 D-133 D-151 D-157 D-171 D-180
              D-194 D-202 D-204 D-205 D-206 D-207 D-208 D-209 D-210 D-211 D-213
              D-214 D-215 D-216 D-217 D-218 D-220 D-221 D-222 D-223 D-224 D-225
              D-226 D-227 ほか（残件 54 → 10）
新しく起こした: D-225 D-226 D-227 D-228
作業樹は 1 つも作っていない（4 体を 2 回、同じ木でファイルを分けて走らせた）
⚠️ 利用者は本リポジトリに限り、コミットと push を任せている。⛔ タグは任されていない
⛔ コミットの前に毎回、個人情報の点検を実測して数を報告すること
```

---

## 7. 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | **裁定待ち** | 5 | **0** | §1.1。⛔ 求める前に ①②③ をやれ |
| 2 | **実測待ち** | 5 | **0** | §1.2。⛔ うち 4 件は模型の問いである |
| 3 | **`製品の仕事`** | **0** | **0 のまま** | ⭐ ここが 0 でない巡は、裁定が下りた巡である |
| 4 | **parity の説明の無い食い違い** | **0** | **0 のまま** | ⛔ 下げるな |
| 5 | **赤い試験（単体・e2e・同型）** | 0 / 0 / 0 | **0** | 3 つとも数えろ |
| 6 | **`npm run parity` の操作数** | **50** | **60+** | ⭐ 25 → 50 で新しい欠陥が 1 件出た（`D-229`）。⛔ 一致しきった検査は、もう問うていない検査である |
| 7 | **検査 31 の腐り（開いている側）** | 3 | **0** | ⛔ 46 は決着済みの側。総数を目標にするな |

⚠️ **総件数が増えることは失敗ではない。失敗は、偽の状態を偽と知らずに置いておくことである。**
⚠️ **前の巡で台帳は 224 行から 228 行へ増え、残件は 54 から 10 へ減った。**
⭐ **減ったぶんの半分は、直したのではなく「塞がっていない」と実測で示したものである。**
