# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ。⛔ pipefail を付けろ
4. §1 と §2 を読む
```

⛔⛔ **`--start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな。**

**サブエージェントは許可されている**（利用者、2026-08-31 以降くり返し再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**
⭐⭐ **知見は規則へ下ろせ**（利用者の指示 2026-09-03）。⛔ **本書に置いた学びは 1 巡で消える。**

---

## 0.5 **始点（前の巡の終わりに実測。⛔ 自分でも測り直せ）**

```
単体   158 ファイル / 6616 件 —— 6615 緑 / 1 skip
e2e    82 件 —— 80 緑 / ⛔ 2 赤（下を読め）。8 分（掃引が 1 本重い）
同型   npm run parity —— 75/75 一致（50 手から増やした）
検査   check.sh ALL GREEN ／ gen:check OK ／ typecheck 清潔
台帳   322 行。残件 54

⛔⛔ 赤い試験が 2 本ある。⭐ どちらも開いている行を留めているので、消すな
   ① tests/system/three-rows-read-from-the-spec-alone.test.ts:696
        D-317（すべての Task を消すと行が 7 行しか描かれない）
   ② tests/system/nfr-004-file-scheme-sweep.sws.test.ts:1181
        D-320 / D-321 / D-322（書き出しが 0 バイト・コピー貼り付け・F2）
   ⭐ 規則 05 の「通せないなら、どこが赤いかを書いてから止まる」に従って残した。
   ⚠️ ②は 8 分かかる。⛔ 落ちうる例はファイル最後の 1 本に畳んである
      —— 落ちた例のあとに別の例があると走行が終わらない（体の実測）
```

**残件 54 の内訳（⭐ 5 つの束で読む。`tools/ledger_metrics.py` が刷る）:**

```
未仕分け   8
仕様の穴   9   D-262 D-272 D-280 D-285 D-296 D-300 D-311 D-313 D-315
               （うち 3 件は裁定待ち: D-262 / D-285 / D-315）
製品の仕事 37  ⛔ この巡に 1 → 37 になった。§2 を読め
検証の借り 0   ⭐ D-264 を返した（§3.1）
```

⛔ **「不具合」と呼んでよいのは `製品の仕事` だけである。**
⚠️⚠️ **37 に増えたことは失敗ではない。⛔ 36 件は前の巡にも在り、見えていなかっただけである。**

---

## 1. ⛔⛔ **利用者が決めた 4 つの段（2026-09-05、逐語）**

> **やることは不具合の全検出。 その後、修正計画/必要なら裁定。
> その後一気に既存不具合を直す。 それが終わったら、仕様やコードのごみ消し。**

```
段 1  不具合の全検出          ✅ ほぼ済んだ（残りは D-264 の掃引と、押さないと決まらない 15 件）
段 2  修正計画／必要なら裁定   ✅ 済んだ（§4 に計画、§7 に裁定 8 件）
段 3  既存不具合を一気に直す   🔧 いまここ。⛔ 波 1 を出したところである（§5）
段 4  仕様とコードのごみ消し   ⬜ 未着手（§6）
```

---

## 2. **この巡に何が起きたか —— ⛔ 数より、なぜ増えたかを読め**

### 2.1 ⭐⭐ **いちばん大きい成果 —— 「反証してこい」と書いたら、歩留まりが 1 桁変わった**

**前の巡**: 分類した体が「怪しい」と思った 51 本を掘らせた ⇒ **本物 1 本**。
**この巡**: 残る 822 本を 5 体に割り、**「製品はこれを守っている、という主張を反証しろ。
守っている場所を `src/` の中でファイル名と行番号で挙げろ。挙げられないものが答えである」**と書いた。

```
BROKEN  20   条文が禁じることをしている、または求めることの逆をしている
ABSENT  43   条文を果たす所が src/ に 1 つも無い
PUSH    15   読んでも決まらない。押さないと分からない
HELD   732   守っている（ファイル名と行番号つき）
```

⛔⛔ **同じ 822 本、同じ体の種類である。変えたのは問いの立て方だけである。**
⭐ **「確かめてこい」は「たぶん守っています」を返す。「反証してこい」は場所を返す。**

### 2.2 ⭐⭐ **`ABSENT` の 43 本の大半は、1 つの型である**

**命令は在る。`use-case` も書けている。人の入口だけが繋がっていない。**

```
D-261  PNG の倍率        setExportPngScale を発行する所が 0 件
D-271  段数の安全弁の通知  RS-24 の語だけが在り、立てる所が 0 件
D-278  権限の復帰         restoreOpenedFilePermission の呼び手が 0 件
D-283  コメントの本文     CM-48 を出さないと翻訳器が自分で書いている
D-288  担当者を消す       deleteResource の発行元が 0 件
D-301  Dual Cursor を消す clearDualCursor の呼び手が 0 件
D-300  合流              use-case は全部書けていて、面と入口だけが無い（5 条項が 1 か所で止まる）
```

⇒ ⭐⭐ **これは 7 件の不具合ではなく 1 つの型である。⛔ 1 件ずつ直すより、型として当たるほうが安い。**
⚠️ **どれも「実装済」に見える** —— **命令も型も試験も在るからである。**

### 2.3 ⭐⭐ **人として通しで押したら、全部緑の状態から 5 件出た**

**自動試験 6616 件・e2e 72 件・同型 75/75・機械検査 36 本・型検査、すべて緑のまま:**

```
D-265  取り消しとやり直しが、戻すものが無くても濃く描かれ、押しても理由を告げない（FR-029）
D-267  行の名前の欄で Esc を 1 回押したあと、取り消しが RS-8 で拒まれる
D-268  AI 出力の面が空である —— 題と閉じるボタンだけ（FR-068）
D-274  パネルを出しているあいだに選択が動いても、中身が移らない（FR-072）
D-266  ⛔ これは偽であった。取り下げた（§2.4 の ③）
```
⭐ **4 件のうち生き残ったのは 3 件である。⛔ 1 件は前に立つ者の測り方が悪かった。**

⭐ **`D-262` も押した** —— **利用者の「押してから問え」の裁定どおりに押したら、破れた**（§7 ①）。

### 2.4 ⛔ **前に立つ者がこの巡に間違えた 4 つ**

```
① Claude のブラウザ枠で測ろうとした
   ⛔ 枠が隠れていると requestAnimationFrame が発火せず、通知が何秒も描かれない。
   ⇒ ⛔ この製品はブラウザ枠では測れない。Playwright を scratch/ に書け
② 「何も動かない」と 4 回書いて、4 回とも測り方が悪かった
   IC-16（テーマ）／IC-4（重ね）／IC-20（Agent API）／IC-50（図形の一覧）——
   ⛔ SVG の節点数を数えていたが、動くのは色・塗り・入口の員数だった
   ⇒ ⭐ 測るものは「その要求が何を動かすと言っているか」から選べ
③ ⛔⛔ 台帳へ起こした D-266 が偽であった。体が反証し、押し直して確かめた
   ⇒ Delete は効いている。押すと FR-032 の確認（U-55）が立ち、Yes で 748 → 733 になる。
     ⛔ 前に立つ者は Notification Area と rect の数だけを見て、
       [role="dialog"] と [data-confirmation-answer] を見なかった。
   ⇒ ⭐⭐ 「押しても何も起きない」と言う前に、問いが立っていないかを見ろ（§8）
④ D-267 の見立ても半分外れていた —— 最初の Esc は正しく値を戻している。
   ⛔ 1 文字も打たずに押したので、戻した値と出ていた値が同じで見えなかった
   ⇒ ⭐ 押して測るときは、まず状態を変えてから戻せ
⑤ 体に「3 本だけ触れ」と書いたのに 4 本目を触られかけた（合流点の git status で見つけた）
⑥ 検査 31 が新しい行で落ちた。⛔ 語を書き換えて黙らせかけた
   ⇒ ⭐ 基準線を 1 上げ、理由を基準線のファイルに書いた（§8）
```

⭐⭐ **③ と ④ から取れる教えは 1 つである** —— **前に立つ者の実測も、体の実測と同じく反証にかけろ。**
⛔ **この巡、体に出した 5 件の反証のうち 2 件は、前に立つ者自身の申告を覆した。**

---

## 3. ⛔ **段 1 の残り（⭐ ここから始めろ）**

### 3.1 ✅ **`D-264` は返した —— `NFR-004` は破れていない**

⭐⭐ **母数を機械で数えて 84 行**（表 T-023a 6 ／ T-023b 6 ／ T-023c 10 ／ T-023d 20 ／
T-023 14 ／ T-036 23 ／ 表 T-024 の書出 6 ＝ 85、`LM-14` が除く `SK-11` を引いて 84）。
**84 行すべてを `file://` と `http://` の同じバイト列で押し、`file://` でだけ落ちる行は 0 件であった。**
⛔ **代わりに、両方式で落ちる行が 6 つ出た** —— `D-320`（書き出しが 0 バイト）
`D-321`（コピー貼り付け）`D-322`（`F2`）。⇒ **これは `NFR-004` の破れではない。**
⭐ **掃引は `tests/system/nfr-004-file-scheme-sweep.sws.test.ts`。母数は散文でなく機械が持つので、
押していない行があれば第 1 例が行 ID を挙げて落ちる。**
⚠️ **限界が 2 つ書いてある**（宿主の保存ダイアログは差し替えてあるので「取っ手を持ったあと」しか示せない／
表 T-023d は DOM に掴み手が無いので座標で押している）。

### 3.2 **押さないと決まらない 15 件（`PUSH`）**

**`scratch/must-clauses/refute-*.md` の判定欄が `PUSH` の行である。**
⛔ **`scratch/` は gitignore なので、作業木には無い。無ければ §3.3 の道具で作り直せ。**

⭐ **この巡に前に立つ者が 2 件を押して片づけた**（⛔ 台帳に行は起こしていない。破れていないからである）:

```
10 #22  PO-4「埋め込んだスクリプトの sha256 を 1 つだけ置く」
        ⇒ ⭐ 出荷物は sha256 を 1 つだけ持ち、本物の <script> も 1 つだけである。
          （grep が 3 つ当たるのは、埋め込み用の文字列リテラルが 2 つ在るからである）
09 #46  フォーカスの輪が、塗られた入口の上で判別できるか
        ⇒ ⭐ 宿主の既定の輪（outline auto 1px rgb(16,16,16)）が、
          塗り rgb(31,122,61) に対して 3.47 : 1。⛔ 3 : 1 を上回るので破れていない
```

### 3.3 **母数を作り直す道具**

```
検査 39      .claude/skills/spec-graph-check/check-must-clause-coverage.py
基準線 978   .claude/skills/spec-graph-check/must-clause-coverage-baseline.txt
⛔ 基準線を上げて黙らせるな。留める試験を書け
⚠️ 見出しで範囲を切るときは改行を含めて照合しろ（chr(10) + '## '）
```

---

## 4. ⛔ **段 2 —— 計画（済んでいる。⭐ そのまま使え）**

### 4.1 ⭐⭐ **直す体はファイルで割る。⛔ 同じファイルを 2 体に渡すな**

**38 件の `製品の仕事` を、触るファイルで束ねるとこうなる:**

```
入力と Esc      input-command-translator.ts / screen-state.ts / tooltips.ts
                D-283 D-286 D-292 D-295 D-307
                ⚠️ D-267 は dom-screen-surface.ts:6905-6913 に在る（この巡に特定した）
描画            svg-renderer.ts / schedule-geometry.ts
                D-293 D-303 D-306（⭐ D-299 は直した。D-285 は裁定待ち）
殻              frame-loop.ts（⛔ 大きい。1 体にしか渡すな）
                D-275 D-279 D-284 D-287 D-289 D-290 D-291
画面の入口      command-palette.ts / screen-frame.ts / properties-panel.ts / notices.ts
                D-261 D-265 D-274 D-281 D-298 D-305 D-273
実体と層        schedule-layout.ts / document-settings.ts / edit-task.ts
                D-270 D-271 D-294 D-312 D-286
書き出し        image-exporter.ts
                D-276 D-277
原稿            docs/spec/_source/
                D-302 D-304 D-308（⛔ 原稿を直すので変更要求が要る）
継ぎ目          file-gateway / codec
                D-278 D-282
面              open-modals.ts
                D-268
```

⚠️ **重なりが在る**（`D-286` は翻訳器と `edit-task.ts` の両方に出る）。⭐ **重なる件は 1 つの体に寄せろ。**

### 4.2 ⭐⭐ **体の使い方 —— この巡に測れたことだけを書く**

```
⭐⭐ 「反証してこい」と書け。「確かめてこい」ではない（§2.1。歩留まりが 1 桁違った）
⭐⭐ 「守っている場所を src/ の中でファイル名と行番号で挙げろ。挙げられないものが答えだ」と書け
⛔⛔ 作業木は古い commit で切られる。⛔ プロンプトの冒頭に必ずこれを書け:
     git status --porcelain で汚れていないことを見て、git merge --ff-only <この巡の sha>
     ⇒ ⭐ この巡、これを書いた 2 体はどちらも正しい commit に着いた。
       書かなかった 1 体は 55 commit 古いファイルを「直した」
⛔ 「体を出すな。自分で読め」と必ず書け。SendMessage はこのセッションで使えない
⛔ 報告を 40 行以内に収めさせ、表はファイルへ書かせろ
⛔ 触ってよいファイルを名指しで書け。⚠️ それでも 4 本目を触る体が居る。合流点で git status を見ろ
⛔ npm test / e2e / check.sh / gen / build を体に走らせるな。⭐ 許すのは
   npx tsc --noEmit と、scratch/ に置いた小さな Playwright だけである
⚠️ npx prettier を体に走らせるな（設定が無く、既定値で全体を書き換える）
⭐ 機械的な仕事は Sonnet、判断の要る仕事は Opus（利用者の裁定 2026-08-30）
```

---

## 5. 🔧 **段 3 —— 一気に直す（波 1 を出したところ）**

```
⛔ 直す体に試験を書かせるな。別の体に、仕様だけを読ませて書かせろ（規則 04 の 1.）
⛔ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh / typecheck
⛔ pipefail を付けろ。⛔ vitest 1 ファイルの緑は「試験が通った」ではない
⭐ 直したら錨を置け（tests/system/user-reported-fixes.test.ts）。錨は自分で鳴る
⛔ コミットの前に毎回、個人情報の点検を実測して数を報告すること
```

⭐⭐ **波 1 は終わった。⛔ 3 勝 3 敗ではなく、「直った 1 / 偽 1 / 場所が分かった 2 / 裁定待ち 2」である:**

```
D-299  ✅ 直した（合流済み）。ハイライトボックスの角に rx が届いた。⛔ ただし原稿からは届いていない ⇒ D-314
D-266  ⛔ 偽であった。取り下げた —— Delete は効いており、確認が書き込みを止めていた
D-267  ⭐ 原因が分かった。⛔ 直す場所は dom-screen-surface.ts:6905-6913 で、体に渡した 3 本の外だった
D-307  ⭐ 階層の最終段は入れた（合流済み）。⛔ 継ぎ目が 2 本足りず、振る舞いはまだ変わらない
D-285  ⛔ 直せない。仕様が「掴めない端点」を一度も定めておらず、FR-029 が自分と食い違っている ⇒ PD-429
新規   D-314（原稿の S-132 が src へ届いていない）／ D-315（FR-029 の自己矛盾）／ D-316（鍵の描き直しが遅れる）
```

⭐ **波 2（3 体、ファイルで割った）:**

```
D-267  ✅ 直した。⛔ 前に立つ者が渡した直し方（欄は焦点を保て）は誤りで、体がそれを正した
       —— 欄に焦点が在ること ＝「入力中」である（IN-6 / AG-9）。取り消したら欄を手放す
D-281  ⭐ 描く側は直した（畳んで消した対象を数えなくなった）。⛔ 押す側はまだ黙って書く
D-279  ⛔ 偽であった。OP-4 は条件を付けていない —— 置き換えは常に問う。
       ⇒ 直しを当てたら単体試験が 7 件落ちた（うち 1 件は OP-4 の MUST NOT を逐語で留めている）
D-287  ⛔ 偽であった。MSPDI のコーデックが EX-5 を既に果たしており、除外の母数が空である
D-316  ⛔ 見立てが誤っていた。フレームは要求されている。1065 ミリ秒は機械の負荷であった
       ⇒ ⭐ 残る本物は「Task を 1 つ消すと 5,300 の SVG 要素を全部作り直す」ことである
新規   D-317 D-318 D-319
```

⭐ **波 2 の追い足し（`D-265` の継ぎ目を埋める体）:**

```
D-265  ✅ 直した。読み込んだ直後 IC-5 / IC-6 が S-149 で薄くなり、押すと RS-27 を告げる
D-281  ✅ 押す側も直した。畳んで消した対象を数えなくなり、押すと RS-34 を告げる
⛔⛔ 直す途中で分かったこと —— 空の履歴でも replaceHeldDocument({row:'RD-1'}) は受理され、
   undoEdit が undone:false を返しても対がそのまま積まれていた。
   ⇒ 誰もしていない編集で hasUnsavedEdits が真になっていた
```

⚠️ **合流点で `check.sh` が 1 本落ちた** —— **`stepCount` が層を越える名前だが 表 T-064 に無い。**
⭐ **前に立つ者が 1 行で直した（`history.done.length` は同じ値で、公開されている型の項である）。**
⛔ **これは「前に立つ者は実装を書かない」の例外である。合流を緑にするための機械的な直しであり、
そう報告してある。**

⭐⭐⭐ **この巡の最大の教えはここに在る。⛔ 直しに出した 6 件のうち、3 件は偽か誤診であった。**

```
本物で直った        D-267 D-281（半分）D-299
偽であった          D-266 D-279 D-287
見立てが誤っていた   D-267（原因）D-316（原因）
仕様が足りず直せない D-285
```

⇒ ⛔⛔ **台帳の 1 行は「事実」ではなく「仮説」である。**
⇒ ⭐⭐ **だから直す体に必ず「まず反証しろ。偽なら偽と報告して直すな」と書け。**
⚠️ **偽を捕まえたのは、3 件とも「直しに行った体」と「既存の試験」であって、前に立つ者ではない。**

⚠️ **`D-299` は「原稿の値が描画に 1 度も届いていない」実例であった** ——
⛔ **直したあとも、原稿の `S-132` を変えて `rx` が変わることは、いまも起きない**（`D-314`）。

---

## 6. ⬜ **段 4 —— 仕様とコードのごみ消し（未着手）**

⭐ **判定基準は 1 つ**: **その文を消したとき、失われる規則があるか。無ければごみである。**
⭐⭐ **これまでに出た実例は 9 つあり、9 つとも共通点が 1 つである** —— **どれも「事実」を書いており、事実のほうが動いた。**

```
D-248  注が「IV-12 は稼働日で測る」と述べる。CR-347 で暦日になっている
D-250  試験の冒頭が、在りもしない 8 行を除外し続ける
D-263  FR-083 の実測注記が「選ばれない」と述べる。50b2243 で解消済み
D-276  ⭐ 新: image-exporter.ts:258-260 の「SvgRenderer が描いたら合わせよ」が期限切れ
D-279  ⭐ 新: 「読める保存済みの値が無い」と書く注釈。hasUnsavedEdits は在る
       ほかに、single-html-shell.ts:539-543 の「nothing WRITES S-99 back」は偽（frame-loop.ts:6766 が書く）、
       document-change-plan.ts:566 の「six rows」は 5 行の誤り（体の申告。⛔ 台帳の行は起こしていない）
```

### 6.1 ⛔⛔ **検査を足す前に**

```
⛔ その検査が守らせる規則が、どこかに書かれているかを先に確かめよ
   ⇒ 書かれていないなら、作るのは検査ではなく規則の捏造である
⛔ 数えてから基準線を作れ
⛔ わざと壊して赤くなることを実測してから入れよ
⛔ 検査 34（コメントの px）と 検査 36（RATIONALE の MUST）は入れるな。裁定済み
⭐ 検査 35（「〜が持つ」と書いた在り処が実際に在るか）は未計測のまま
```

---

## 7. ⛔⛔ **利用者の裁定が要る 8 件（⭐ この巡に集めた。一度に問え）**

**`docs/development-records/pending-decisions.md` の `PD-420` 〜 `PD-428` に根拠つきで書いてある。**
⛔ **本書はその写しを持たない。⭐ 一覧だけ置く:**

```
PD-420  D-262  ハイライトボックスの「下」を、画面の順で決めるか木の順で決めるか
               ⭐ 押して測った。破れる。⛔ 分類 D（文書に保存される）
PD-421  D-272  HM-9（並べ替えが WBS へ伝わる）を、どう果たすか。⛔ 分類 D
PD-422  D-280  S-99b の「文書の識別子」とは何か。⛔ 分類 D
PD-423  D-300  合流の面を作るか、合流を取り下げるか。⛔ 分類 E
PD-424  D-311  第 8・9・10 章（試験の仕様）をどうするか。⛔ 分類 G
PD-425  D-313  性能の測定器をいま作るか。⛔ 分類 G。⛔⛔ 走らせる前に声を掛ける約束がある
PD-427  D-282  知らない schemaVersion を受けるか拒むか。⛔ 分類 F（時間の非対称性）
PD-428  D-269  分類を誤ったまま実装されている 7 行をどうするか。⛔ 分類 D
               ⛔⛔ これは先送りできない —— 分類を直した瞬間に検査 25 が落ちる
PD-429  D-285  ①「掴めない端点」とはどの場面か（仕様に 0 件）
        D-315  ②理由を乗せて出すのか押されたときに出すのか
               ⛔⛔ FR-029 の第 1 文と、同じ要求の後段（利用者の裁定 2026-08-30）が正面から食い違う
               ⭐ ②には推奨が在る（第 1 文を直す）。⛔ ①には無い。分類 E
```

⚠️ **`PD-426` は書いて消した** —— **「表 T-109 のどの行が `FR-016` を果たすか」は仕様を書く仕事であって、
暫定の値ではない。印を付ける場所が `src/` に無い。**⭐ **`D-296` が持っている。**

---

## 8. **測り方の罠（実測で踏んだものだけ）**

```
⛔⛔ ⭐ 新: Claude のブラウザ枠が隠れていると requestAnimationFrame が発火しない
   ⇒ 通知が何秒も描かれず、「通知が出ない」と読み違える。
   ⛔ この製品はブラウザ枠では測れない。scratch/ に Playwright を書いて押せ
⚠️ ⭐ 新: ブラウザ枠の座標は「最後に撮った画像の枠」であって DOM の座標ではない
⛔⛔ ⭐ 新: 測るものを、その要求が動かすと言っているものから選べ
   ⇒ SVG の節点数だけを見て「何も動かない」と 4 回書き、4 回とも外れた
     （テーマは色、重ねは塗り、Agent API は別の入口の薄さ、図形の一覧は入口の員数が動く）
⛔⛔ ⭐ 新: 「押しても何も起きない」と言う前に、問いが立っていないかを見ろ
   ⇒ Delete は確認（[data-confirmation-answer] の Yes / No）を立てて書き込みを止める。
     通知の欄には何も出ないので、通知だけを見ていると「効いていない」と読める（D-266 が偽であった理由）
⛔ ⭐ 新: 鍵で書いたあとの描き直しは、ときどき 1 秒近く遅れる（D-316。4 回中 1 回）
   ⇒ ⛔ 固定の待ちで測るな。「同じ読みが 2 回続くまで」待て
⛔ ⭐ 新: IC-2 の保存は、無頭ブラウザの file:// にファイルを渡さない
   ⇒ 文書を読むなら IC-20 で Agent API を有効にして
     window.grSchedulerAgentApi.readDocument() を呼べ（exportJson なども在る）
⛔ 管は最後のコマンドの終了コードを返す。set -o pipefail を付けるか $? を単独で読め
⛔ vitest は型を見ない。⛔ 1 ファイルの緑は typecheck の緑ではない
⛔ 行の名前は切られる。textContent は切られた語を返す
⛔ [data-depth] は「描かれている窓」であって文書ではない。名前で引き直せ
⛔ 行の操作子は、その行の名前にポインタが乗るまで隠れている
⛔ 掴み代は「行の名前と同じ高さ」に在る。⚠️ 行の中央では掴めない（前に立つ者が 1 度外した）
⛔ 外接矩形は clip-path を見ない。elementFromPoint で掃け
⛔ 二度押し（MK-13、表 T-023）は 1.5 秒空けろ
⛔ 宿主のダイアログは addInitScript で差し替えろ。差し替えたら「取っ手を持ったあと」しか示せない
⛔ 初回描画は「同じ読みが 2 回続くまで」待て。固定の待ちは冷間で外れる
⚠️ 参照ブラウザの終了は 21〜163 秒かかる
⚠️ 深い行は「無い」のではなく LOD が落としている。IC-15 を 8 回押せば深さ 5 に届く
⚠️ 画面いっぱいに描く層を足すと、形で選ぶ掃引が壊れる
⚠️ 単体試験がまれに落ちるのは、読みと書きが重なるからである（D-255）
⛔⛔ ⭐ 新: 「dist/index.html の写しに入れ物を仕込めば BT-1 を通せる」は、そのままでは通らない
   ⚠️ 3 者が食い違い、実測でこうなった:
     通った    CR-353 の体 —— id を変え、⭐ CSP のハッシュを計算し直した
     通らない  CR-357 の試験を書いた体 —— 4 つの id、ハッシュに触れていない
     通らない  前に立つ者 —— 3 つの id（正しい embedded-document を含む）、ハッシュ無し
               ⇒ 埋めた文書ではなく起動の雛形が開いた（3 つとも同じ）
   ⇒ ⭐ 判別要因は CSP のハッシュらしい。⛔ 仕込んだら必ず計算し直せ
```

---

## 9. 道具

```
npm run parity             見本と出荷ビルドを 75 の操作で突き合わせる。⭐ いま 75/75
                           ⛔ また一致しきったら手を増やす番である
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated]
⭐ 選択の枠は fill="none" の <rect> ＋ 両端の 9 × 9 の白い掴み点である（この巡に実測）

.claude/skills/spec-graph-check/
  check.sh    36 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED\|FAILURES ABOVE"
  induced.py  ⛔ 仕様を書く前に。閉路は 1 回の書き込みで直す
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  must-clause-coverage-baseline.txt   検査 39 の基準線 978
  stale-blocked-baseline.txt          ⭐ この巡 46 → 47。理由はファイルの中に書いた
```

**固定の仕掛け:**
```
tests/system/measured-sweep.test.ts                    1 回の起動で回る掃引
tests/system/first-frame-is-the-settled-frame.test.ts  最初の 1 枚だけを測る
tests/system/rows-fixed-with-nothing-holding-them.test.ts  殻の規則を測る唯一の道
tests/system/user-reported-fixes.test.ts               利用者が指摘し、直したものの錨
tests/system/open-defect-pins.test.ts                  ⚠️ PINNED は空
tools/parity/check.mjs                                 75 手
```

⭐⭐ **錨は自分で鳴る。⛔ これが注ではなく錨を置く理由である。**

---

## 10. 現在地

```
ブランチ restart
この巡に起こした台帳の行: D-265 〜 D-316（52 行。うち D-266 は偽として取下げ、D-299 は実測済）
この巡に起こした裁定待ち: PD-420 〜 PD-429（9 件。PD-426 は取り消し）
同型の手: 50 → 75
巡の終わりの実測: 単体 6616 全緑 ／ e2e 72 全緑 ／ 同型 75/75 ／ check.sh ALL GREEN ／ typecheck 清潔
⚠️ 利用者は本リポジトリに限り、コミットと push を任せている。⛔ タグは任されていない
⛔ コミットの前に毎回、個人情報の点検を実測して数を報告すること
```

---

## 11. **次の巡の目標値（⛔ 始める前に自分で測り直せ）**

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | **製品の仕事** | **36** | **減らす** | ⛔ 段 3。§4.1 の束でファイルごとに割れ |
| 2 | **仕様の穴** | **9** | **0** | ⛔ 8 件は §7 の裁定待ちである。裁定が下りたら書け |
| 3 | **裁定待ちの返事** | **9 件未** | **全部** | ⛔ `PD-428` は先送りできない（検査 25 が落ちる）|
| 4 | **検証の借り** | 1 | **0** | `D-264` |
| 5 | **未仕分け** | 7 | **0** | §0.5 の 7 件 |
| 6 | **人として通しで押す** | 1 回 | **巡の頭に 1 回** | ⭐ この巡は 1 回で 5 件出た |
| 7 | **条文を守る試験の覆い** | **31.4%** | 上げる | ⛔ 基準線 978 を上げるな |
| 8 | **`npm run parity` の手** | 75 | 一致しきったら増やせ | ⛔ 一致しきった検査は問うていない検査である |
| 9 | **赤い試験** | **1** | **0** | ⛔ `D-317` を留めている 1 本である（§0.5）|

⚠️ **総件数が増えることは失敗ではない。失敗は、偽の状態を偽と知らずに置いておくことである。**
⛔⛔ **この巡、台帳は 264 行から 313 行へ増え、`製品の仕事` は 1 → 38 になった。**
**その 37 件は前の巡にも在り、`製品の仕事 1` と刷られているあいだ、ずっと在り続けていた。**
