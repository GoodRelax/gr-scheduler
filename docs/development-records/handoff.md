# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ。⛔ pipefail を付けろ
4. §1 と §2 を読む
```

⛔⛔ **`--start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな。**

**サブエージェントは許可されている**（利用者、2026-08-31 以降くり返し再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**
⭐⭐ **知見は規則へ下ろせ**（利用者の指示 2026-09-03）。⛔ **本書に置いた学びは 1 巡で消える。**

---

## 0.5 ⛔⛔ **基準値も、もう本書に書かない**

**⇒ 第 0 節の 1〜3 を走らせれば、始点はすべて出る。**

⛔⛔ **基準値（借金の数）も書かない。前の巡で 4 度動いた** —— **950 → 952 → 941 → 943 → 941。**
⇒ **本書に書いた瞬間から古くなり、次に読む者が古い数を信じる。**

⭐ **基準線は、それぞれのファイルの 1 行目が持っている。⛔ 理由はその下に書いてある:**

```
検査 31  .claude/skills/spec-graph-check/stale-blocked-baseline.txt
検査 37  .claude/skills/spec-graph-check/dictionary-table-pairing.txt
検査 39  .claude/skills/spec-graph-check/must-clause-coverage-baseline.txt
検査 40  .claude/skills/spec-graph-check/ruled-elsewhere-baseline.txt
検査 41  .claude/skills/spec-graph-check/decided-spec-baseline.txt
検査 42  .claude/skills/spec-graph-check/quoted-source-baseline.txt
```

⛔⛔ **黙って上げるな。上げるなら、そのファイルの中に理由を書き、コミット文にも書け。**
⭐ **上げてよいのは、同じ巡のうちに返す当てがあるときだけである。**
⚠️ **返し忘れを防ぐには、上げたと同時に台帳へ行を立てろ** —— **前の巡は `D-354` がそれである。**

### ⭐ 検査 37 と 41 は、答える順番が決まっている

```
検査 37  行と語を読み合わせる → 語が行を言っていなければ語を直す
         → そのあとで python .claude/skills/spec-graph-check/check-dictionary-table-covariance.py --write-baseline
         ⛔ 先に --write-baseline を打つと、読まずに刻んだことになる
検査 41  実装待ち 以上へ動かす行は、対応方針の欄に「どこに書いたか」を書く
         ⭐ docs/spec の外でもよい（docs/development-rules/… でも通る）が、
           ファイル名か 表 T-nnn か 行 ID のどれかが要る
```

---
## 1. ⛔⛔ **利用者が決めた 4 つの段（2026-09-05、逐語）**

> **やることは不具合の全検出。 その後、修正計画/必要なら裁定。
> その後一気に既存不具合を直す。 それが終わったら、仕様やコードのごみ消し。**

```
段 1  不具合の全検出          ✅ ほぼ済んだ
段 2  修正計画／必要なら裁定   ✅ 済んだ
段 3  既存不具合を一気に直す   🔧 いまここ
段 4  仕様とコードのごみ消し   ⬜ 未着手（§6）
```

---

## 2. ⛔⛔ **前の巡が身銭で買った 7 つ（⭐ これだけは読め）**

**⓪ ⛔⛔ いちばん高くついたのは、5 巡ぶん誰も問わなかった 1 つの問いである。**
⇒ **「一度に何体出せるか」。**⛔ **引継書 5 版の「学び」は全部「体がどう誤ったか」だった。**
⇒ **どの巡も 1 波 2〜4 体で回し、⭐ 試験だけの行が 16 も溜まっていた ——**
**それらは `tests/` しか触らないので、⛔ 誰とも衝突しないのに、待たされていた。**
⭐ **測り方と、いま出せる幅は §3.5 と §9 に在る。規則 05 の 11 に下ろした。**

**① ⛔⛔ 台帳の行は仮説である。「無い」と書いてあったら、まず探せ。**
⇒ **`D-273` が「表 T-233 に担当者名の波及の行が 1 つも無い」と言うのを信じて、
同じ場面の行をもう 1 つ書いた。`RS-49` が同じ日に既に立っていた。**
⛔ **前に立つ者が自分で踏んだ** —— **体には「反証してこい」と言いながら、自分はしなかった。**
⭐ **捉まえたのは契約試験である** —— 対の基準線が無い指紋を拒んだ。
**規則 05 の 10 に下ろした。**

**② ⛔⛔ 直したら、同じコミットで台帳の行を動かせ。**
⇒ **巡の頭に開いていた 53 行のうち 8 行は、既に直っていた状態を述べていた。**
⛔ **直っている行は、次の巡に実測 1 回を負わせる。**

**③ ⭐⭐ 1 回の Playwright 起動で、`実測待ち` の行がまとめて閉じる。**
⇒ **`npm run build` して `dist/index.html` を `file://` で押す小さな `.mjs` を 1 本書くだけで、
3 行が 40 秒で `実測済` になった。**
⛔ **別の巡に回すな。波の最後に必ず置け。**⭐ 雛形は `scratch/m0906b-sweep.mjs`。

**④ ⛔ 生成される定数を 1 つ足すには、3 か所に書く。**
```
tools/generate_entity_types.py  組の表（行番号ではなく `NOT_STORED_` で探せ）
tools/generate_entity_types.py  ファイルごとの出力式（`not_stored_block(...)` を並べている所）
docs/development-rules/03-implementation.md  定数の一覧
```
⛔ **2 つ目を落とすと、黙って何も出ない。**⚠️ **3 つ目を落とすと、木と一覧の数が合わないと検査が鳴く。**

**⑤ ⛔ 体に `npm run gen` を許すかどうかを、ブリーフに書け。**
⇒ **許さなかった体は、生成物を手書きして「次巡で畳んでよい」と注を残した。**
⭐ **判断は単純である** —— **原稿（`_source/`）に触れる仕事なら許せ。触れないなら禁じろ。**

**⑥ ⚠️ 体に「逆語で grep して確かめろ」と言うな。概念を言え。**
⇒ **「基準隅は左下」で確かめさせたが、仕様は「基準隅を左下」だった。**
⭐ **体は 0 件を報告し、実際の文言を探して続けた。正しい振る舞いである。**
⛔ **脆い文字列で門を作ると、正しい体を止める。**

### ⭐⭐ 体に持たせて実際に効いた作法 —— **「反証してこい」**

**条項を守っている `file:line` を名指させる。**
⭐ **この巡も 6 体すべてがブリーフの誤りを訂正して返した**（行番号、逆語、
「まだ無い」が実は在ったもの、「問うのが禁止」が実は別物だったもの）。
⭐ **訂正は成果である。**⛔ **同じ作法を前に立つ者自身にも適用せよ（①）。**

---

## 3. ⛔⛔ **残件の一覧は、もう本書に書かない**

⇒ **一覧は腐る。前の巡は、書いてあった 53 行のうち 8 行が既に直っていた。**
⭐ **代わりに、一覧を作る問いを置く。毎回これを打て（実行できる）:**

```bash
PYTHONIOENCODING=utf-8 python -c "
import io
L=io.open('docs/development-records/defects.md',encoding='utf-8').read().split('
')
for l in L:
    if not l.startswith('| D-'): continue
    c=l.split('|'); st=c[6].strip().strip('\`')
    if st in ('実測済','取下げ'): continue
    print('%-7s %-5s %s' % (c[1].strip(), st, c[2].strip()[:70]))
"
```

⛔ **出てきた行をそのまま体に渡すな。**⭐ **まず反証させろ（§2 の ①）。**

### ⭐ ステータスの意味（台帳の頭にも在る）

```
未検討    受け付けただけ                    → 調べる
裁定待ち  利用者にしか決められない      → ⛔ 利用者（§6 で一度に問え）
仕様待ち  裁定は下りた。仕様書がまだ       → 前に立つ者が書く
実装待ち  仕様に在る。コードがまだ       → 体に出す
試験待ち  コードに在る。試験がまだ       → 仕様だけを読む体に書かせろ
実測待ち  試験が緑。出荷ビルドを押してない → ⭐ §2 の ③を打て。安い
```

---

## 3.5 ⛔⛔ **まず幅を決めろ。⭐ これが 5 巡ぶん見落とされていた**

⛔⛔ **引継書 5 版を読み返して分かったこと（2026-09-07）** ——
**5 版すべての「学び」が「体がどう誤ったか」であり、**
**「一度に何体出せるか」を一度も問うていない。**
⇒ **どの巡も 1 波 2〜4 体で回していた。⛔ それがペースの正体である。**

⭐⭐ **利用者の指摘（2026-09-07）**: 「うまく整理して複数のエージェントにやらせれば 1 回で終わらないのか？」
⇒ ⭐ **終わる。幅を測れば終わる。**

### ⭐ 幅の測り方（3 分で出る）

```
① 開いている行を状態で数える（§3 の問いを打つ）
② 試験待ち の行 → ⭐ tests/ しか触らない。⛔ 誰とも衝突しない。何体でも同時に出せる
③ 実測待ち の行 → ⭐ 体は要らない。Playwright を 1 回起動して前に立つ者が閉じる
④ 実装待ち の行 → src のファイルで割る。⛔ ここだけが幅の上限を決める
⑤ 裁定待ち / 仕様待ち の行 → ⛔ 体に渡すな。前に立つ者が仕様を書く
```

⛔⛔ **④ の上限を数える問い（実行できる）:**

```bash
PYTHONIOENCODING=utf-8 python -c "
import io,re,collections
L=io.open('docs/development-records/defects.md',encoding='utf-8').read().split('\n')
files=collections.defaultdict(list)
for l in L:
    if not l.startswith('| D-'): continue
    c=l.split('|'); st=c[6].strip().strip('\`')
    if st in ('実測済','取下げ'): continue
    for m in set(re.findall(r'src/[A-Za-z0-9_./-]+\.ts', l)):
        files[m].append(c[1].strip())
for f,v in sorted(files.items(), key=lambda x:-len(x[1])):
    if len(v)>=2: print('%-60s %d  %s' % (f, len(v), ' '.join(sorted(set(v)))))
"
```

⭐ **2026-09-07 に測った熱いファイル**（⛔ 自分でも測り直せ）:

```
frame-loop.ts                 5 行     input-command-translator.ts   4 行
dom-screen-surface.ts         3 行     svg-renderer.ts               3 行
schedule-layout.ts            2 行     notices.ts                    2 行
image-exporter.ts             2 行     edit-annotation.ts            2 行
```

### 手順（毎波これを回せ。⛔ 波は 1 つでよい）

```
1. git log --oneline -1 で いまの sha を取る
2. 幅を測る（上）。⛔ 2 体で回すな。試験の体は衝突しないので惜しむ理由が無い
3. 体を 1 通のメッセージで同時に出す。ブリーフの先頭に必ず:
     git merge --ff-only <sha>  →  git log --oneline -1 を報告の 1 行目に
     触ってよいファイルを名指しで列挙し、他の体の分を名指しで禁じる
     npm run gen を許すか禁じるかを書く（原稿に触れるなら許す）
     ⛔「台帳の行は仮説である。まず反証しろ」を必ず書く
     ⛔「原稿に行が無いなら、造りを変えずにそう報告しろ」を必ず書く
     試験の体には ⛔「既存の試験ファイルを書き換えるな。新しい 1 本に書け」と
       ファイル名まで指定する（⭐ これで試験の体どうしも衝突しない）
4. 体が走っているあいだに、前に立つ者は仕様を書く（衝突しない）
5. 全部戻ったら、まとめて合流する。1 体ずつ検査を回すな:
     体ごとに重なりを数える（0 件を見てから写す）
       comm -12 <(git diff --name-only <sha> HEAD | sort) \
                <(git -C <worktree> status --porcelain | awk '{print $2}' | sort)
     その体が書いたファイルだけを cp
   ⇒ 全部写してから npm run gen → tsc → check.sh → vitest 全体 を 1 回だけ
6. 台帳の行を動かして、同じコミットで落とす
7. 最後に出荷ビルドを 1 回押す（§2 の ③）
```

⛔ **同じファイルを 2 体に渡すな。**⭐ **それ以外に幅の制約は無い。**
⚠️ **前に立つ者の合流は 1 体あたり 3〜5k である** —— **9 体でも 40k に収まる。**

---
## 4. ⛔⛔ **`npm run e2e` が赤い（`D-348`）**

**巡の頭で数えて出た。⛔ 前の引継書はこの状態を何も述べていなかった。**
**85 通過 / 5 落ち / 7.9 分。**⚠️ **症状を集めただけであり、原因はまだ分けていない。**

```
① nfr-002-003 の性能の関門 14 条項                    → D-330 が既に持つ
② FR-025 exportPng が notAvailable                    「Rasterizer（IF-6）を運んでいない」
③ FR-067 exportEmbeddedHtml が notAvailable           「AppShellSource（IF-8）を運んでいない」
④ FR-073 / FR-022 の U-61 が描かれていない
⑤ measured-sweep の D-210、three-rows の SK-3 / SL-1
```

⭐⭐ **④ は `D-300` ではない（同日に分けた）** —— **出荷ビルドを押すと `Difference Review` は候補 1000 行を並べ、
`IC-95` 〜 `IC-97` が立ち、`IC-96` でタスクが 1000 → 2000 になる。**
⇒ ⛔ **落ちているのは `Agent API` の `importDocument`（`AM-8`）の経路であり、②③ と同じ束である。**

⚠️ **`npm test` と `npm run parity` は同じ日に緑であった。赤いのは e2e だけである。**

---

## 5. ⬜ **段 4 —— 仕様とコードのごみ消し（未着手）**

⭐ **判定基準は 1 つ**: **その文を消したとき、失われる規則があるか。無ければごみである。**
⭐⭐ **これまでに出た実例は 11 で、11 とも共通点が 1 つである** —— **どれも「事実」を書いており、事実のほうが動いた。**

```
D-248  注が「IV-12 は稼働日で測る」と述べる。CR-347 で暦日になっている
D-250  試験の冒頭が、在りもしない 8 行を除外し続ける
D-263  ⭐ この巡に半分閉じた —— 実物で押して「選ばれていない」は偽と分かり、
       「Delete が効かない」は焦点の話として残ることを仕様に書いた
D-276  image-exporter.ts の「SvgRenderer が描いたら合わせよ」が期限切れ
D-279  「読める保存済みの値が無い」と書く注釈。hasUnsavedEdits は在る
D-339  コードのコメントが、仕様に無い一文を「表の行」として引用している
D-341  退役した理由の行（RS-45）を、殻がまだ名指している
⭐ 新: input-command-translator.ts の STOP が数を 3 つとも古いまま持っている
       （体が測った: 表 T-109 は 89 行、ENTRY の IC 行は 57、ARMED_BY_ENTRY は 22）
⭐ 新: FR-006 と同じ形が他にもあるはずである —— 表の欄の値を散文で数え上げている所
       ⛔ この巡は FR-006 が 表 T-016 の 対象 を数え上げて 1 日で腐った
```

### 5.1 ⛔⛔ **検査を足す前に**

```
⛔ その検査が守らせる規則が、どこかに書かれているかを先に確かめよ
⛔ 数えてから基準線を作れ
⛔ わざと壊して赤くなることを実測してから入れよ
⛔ 検査 34（コメントの px）と 検査 36（RATIONALE の MUST）は入れるな。裁定済み
⭐ 検査 35（「〜が持つ」と書いた在り処が実際に在るか）は未計測のまま
```

⚠️⚠️ **この巡、検査の側の欠陥が 2 つ出た（`D-344` と `D-347`）。**
⭐ **どちらも「散文を読む検査が、記録や引用を現在の主張と読む」形である。**
⛔ **3 つ目が出たら、検査の書き方そのものを見直せ。**

---

## 6. ⛔⛔ **利用者の裁定が要る（⭐ 一度に問え）**

**根拠つきの本文は `docs/development-records/pending-decisions.md` にある。⛔ 本書は写しを持たない:**

```
PD-420  D-262  ハイライトボックスの「下」を画面の順で決めるか木の順で決めるか
PD-421  D-272  HM-9（並べ替えが WBS へ伝わる）をどう果たすか
PD-422  D-280  S-99b の「文書の識別子」とは何か
PD-424  D-311  第 8・9・10 章（試験の仕様）をどうするか
        ⭐ 利用者は 2026-09-06 に「一旦そのまま」と述べた。取り下げではない
PD-425  D-313  性能の測定器をいま作るか
        ⛔⛔ 走らせる前に声を掛ける約束がある（RISK-001）
PD-427  D-282  知らない schemaVersion を受けるか拒むか
PD-428  D-269  分類を誤ったまま実装されている行をどうするか
        ⛔⛔ 先送りできない —— 分類を直した瞬間に検査 25 が落ちる
        ⭐ 残るのは PD-2（B）と PD-397（A）の 2 件だけになった
PD-429  D-285 / D-315  掴めない端点と、理由をいつ出すか
```

### ⭐⭐ **2026-09-07 に下りた 3 件 —— ⛔ 二度と問うな。書くのは仕様である**

⛔ **3 件とも「裁定は下りた・仕様書がまだ」の状態で置いてある。⭐ 台帳の行が逐語を持つ。**

```
上書き保存の相手を覚えるか          → ⛔ 覚えない（案 B）
  ⇒ FR-060 の第 2 の MUST（起動時に権限の復帰を申し出る）を落とす
  ⇒ LM-14 がその申し出を当てにしているので、同時に書き直す
  ⇒ 「毎回ファイルを選び直すのが仕様である」と明記する
  ⛔ IndexedDB を足してはならない（この裁定が禁じた）
  ⭐ 造りは動かさなくてよい。要求を落とすので検査 39 の借金は下がる

書き出しの Document Title の置き方  → ⭐ 1 つの規則を画面と書き出しの両方が読む（案 B）
  ⇒ 書き出し専用の 2 定数（帯の高さ × 0.4 / × 0.5）を退ける
  ⇒ 帯の余白と題の字の大きさに 表 T-206 の行を立て、両者がそれを読む
  ⭐ 実測（出荷ビルド 1920×1080）: 画面 = 字 16px・左 12px / 書き出し = 字 14.8px・左 18.5px
    ⇒ 7.5% 小さく 6.5px 右。EP-1 の「位置を動かしてはならない（MUST NOT）」に反している
  ⚠️ 行を立てるとき、画面の見た目は 1px も変えない（実測値をそのまま行にする）
  ⭐ 書き出しは ScreenRenderer への辺を既に持つ（components.json で実測）
  ⛔ DOM を直接測る形にしてはならない
  ⭐ 画面側の padding:0.375em 0.75em の直値も同時に片づく

暦を変えたときの完了率              → ⭐ 数え直す（案 A）
  ⇒ FR-012 の再計算の引き金に「暦を編集したとき」を足す
  ⇒ 件数を添えて告げる（表 T-037 の NT-3 が暦の変更を例として名指ししている）
  ⚠️ 同じ書き込みの中で行う（暦の変更は取り消せるので、再計算も一緒に取り消される）
  ⭐ 例: 予定 9/7(月)→9/14(月)・実績 3 日 は 月〜金の暦で 50%、土曜を足すと 43%
  ⛔ 告げる理由の行は、無いと決めつけずに 表 T-233 を grep しろ
    （この巡、前に立つ者が RS-49 を見落として重複を書きかけた）
```
### ⭐ 2026-09-06 に片づいた裁定 —— ⛔ **二度と問うな**

```
D-318 / D-283 / D-345 / D-275   → CR-368 で仕様に着地
カーソル 3 種と PD-343          → CR-369 で着地。'double-vertical' は退役
退役した設定値を持つ文書          → 「外すだけ。古い文書は拒む」
D-346 の左半分・右半分            → 「公開名を 1 つ足す」（署名は動かさない）
ST-7 の安全弁の境界（PD-430）      → 「S-89 は許される段数の上限」。⭐ 前に立つ者が推奨で進めた
依存線の交差（裁定②）              → 地の色の縁。選ばれた線が常に手前。バーの上では縁を敷かない
依存線を隠す（裁定③）              → 別の面を置かない。トグル IC-81 が戻り道である
PNG の大きさ（裁定⑤）              → 常に S-81。倍率を持たない。S-82 CM-70 K-88 は退役
付箋の基準隅（裁定⑥）            → 左下。留める点の縦位置は未決のままでよい
透かしの打ち直し（裁定⑦）          → 開いたときと、文書が変わったときの 2 つ。毎フレームは禁止
仕切りの線の色（PD-51）          → 直値を置かず SvgRenderer に問う（S-149）
```

⛔⛔ **行を立てる前に、必ず `pending-decisions.md` を grep しろ。**

---

## 7. **測り方の罠 → ⭐ `docs/development-rules/04-verification.md` の 6 節にある**

⛔ **本書に規則を書くな。あちらに足せ。**⭐ **検査 0 が毎回その所在を印字する。**

---

## 8. 道具

```
npm run parity             見本と出荷ビルドを 75 の操作で突き合わせる
                           ⛔ また一致しきったら手を増やす番である
npm test / npm run e2e     ⛔ 両方数えろ。⚠️ e2e はいま赤い（§4）
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
npm run precheck           ⛔ 編集の「前」に。⭐ 生成物の罠は 2026-09-06 に直した

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated]
⭐ 選択の枠は fill="none" の <rect> ＋ 両端の 9 × 9 の白い掴み点である

.claude/skills/spec-graph-check/
  check.sh    41 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED\|FAILURES ABOVE"
  ⚠️ FAILURES ABOVE が出ても FAIL 行が無いことがある —— precheck と md-checks は別の形で刷る
  induced.py  ⛔ 仕様を書く前に。閉路は 1 回の書き込みで直す
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ プロパティ項目は docs/spec/_source/property-items.json（＋ 同 .schema.json の enum）
  ⚠️ 行を退役させたら md-checks.py の RETIRED に席を burn しろ —— 変更履歴が名指している
  must-clause-coverage-baseline.txt   検査 39 の基準線
  dictionary-table-pairing.txt        検査 37 の対（--write-baseline は「一緒に読んだ」後だけ）
```

**固定の仕掛け:**
```
tests/system/measured-sweep.test.ts                    1 回の起動で回る掃引
tests/system/first-frame-is-the-settled-frame.test.ts  最初の 1 枚だけを測る
tests/system/rows-fixed-with-nothing-holding-them.test.ts  殻の規則を測る唯一の道
tests/system/user-reported-fixes.test.ts               利用者が指摘し、直したものの錨
tests/system/open-defect-pins.test.ts                  ⚠️ PINNED は空
tools/parity/check.mjs                                 75 手
docs/development-records/evidence/measured-2026-09-06.md  ⭐ 実測待ち 12 行を押した表
```

---

## 9. ⛔⛔ **次の巡の目標 —— 既知の不具合を 1 巡で 0 にする**

**利用者の指示（2026-09-07）**: 「次回で一気に既知の不具合を 0 にするよう引き継げ」

⛔ **数は書かない（§0.5）。⭐ §3 の問いを打って自分で出せ。**
⚠️ **2026-09-07 の終わりに測った内訳は、参考としてだけ読め:**
**試験待ち 16 / 実装待ち 14 / 未検討 6 / 実測待ち 5 / 仕様待ち 6。**

### ⭐⭐ 1 波でこう割れる（⛔ 自分で測り直してから出せ）

```
【試験だけの体 —— tests/ しか触らない。⛔ 誰とも衝突しない】
  T1  段数の安全弁            D-270 D-271 D-345
  T2  複製と貼り付け・カーソル  D-290 D-321 D-301
  T3  行の畳みと帯            D-306 D-318 D-319
  T4  語・理由・透かし         D-308 D-340 D-341 D-354
  T5  色・角・付箋の欄         D-277 D-314 D-349
  ⛔ 各体に「新しい試験ファイル 1 本の名前」まで指定しろ。既存を書き換えさせるな

【直す体 —— src のファイルで割る。⛔ ここだけが幅の上限】
  R1  screen-renderer（notices.ts を除く）  D-274 D-298
  R2  frame-loop ＋ 翻訳器 ＋ edit-resource ＋ notices
        D-273 D-284 D-297 D-339
  R3  dom-screen-surface                    D-338

【前に立つ者が自分でやる —— 体に渡すな】
  ・実測待ち 5 行を Playwright 1 回で閉じる  D-275 D-283 D-292 D-312 D-346
  ・裁定 3 件の仕様を書く（§6）              D-276 D-278 D-353
  ・仕様待ち の残り                          D-280 D-302 D-311
  ⭐ これは体が走っているあいだにやる。衝突しない

【踏査だけ。⛔ 直すのではなく、まず割る】
  D-348  e2e の 5 つの症状を原因ごとに割る
  D-350  入口を持たない命令 15 種を 1 種ずつ仕様に当てる
  D-269  分類を誤ったまま実装されている行
  D-351 D-352  試験の道具の欠陥
```

⇒ ⭐ **体 8 つ＋前に立つ者。1 波でおよそ 23 行に手が付く。**

### ⛔ 1 巡で 0 にできない行が 2 つある。⭐ 先に見分けろ

```
⛔ 性能の測定（D-313 / D-316 / D-330）
   ⇒ 走らせる前に利用者へ声を掛ける約束がある（RISK-001）。⛔ 黙って走らせるな
⛔ 新しい裁定が要ると分かった行
   ⇒ 体が「原稿に行が無い」と報告したら、それは失敗ではない。§6 へ積んで一度に問え
```

⛔⛔ **文脈の使い方（前の巡の実測）**

```
体 1 つを出して合流する（前に立つ者の側）                  3〜5k
仕様を 1 本書く（原稿・生成・検査・基準線）                約 15k
出荷ビルドの掃き寄せ 1 回                                約  5k
本書を書き直す                                          約 30k
```

⭐ **体は 120k〜370k を自分で使うが、親の文脈はほとんど食わない。**
⇒ ⭐⭐ **測る仕事は必ず体に出せ。自分で grep を回すな。**
⇒ ⭐⭐ **体を惜しむ理由が無い。⛔ 2 体で回すな（§3.5）。**

⛔⛔ **文脈は圧縮される。本書は `defects.md` と `git log` だけから書けるように保て** ——
**前の巡は圧縮を 1 度踏んだが、台帳とコミット文が残っていたので何も失わなかった。**
