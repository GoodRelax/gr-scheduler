# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ。⛔ pipefail を付けろ
4. §1 と §2 を読む
```

⛔⛔ **`--start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな。**

**サブエージェントは許可されている**（利用者、2026-08-31 以降くり返し再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**
⭐⭐ **知見は規則へ下ろせ**（利用者の指示 2026-09-03）。⛔ **本書に置いた学びは 1 巡で消える。**

---

## 0.5 ⛔ **始点の数は、もう書かない**

**前の巡までは実測値を並べていた。⛔ やめた。**
⭐ **同じ文書が「この数を信じるな、自分で測れ」と続けて書いていた** —— **信じるなと書くくらいなら、書かないほうがよい。**
⇒ **第 0 節の 1〜3 を走らせれば、すべて出る。**

⚠️ **例外は基準値だけである** —— **これは測って出る数ではなく、**動かしてよいかどうかの判断**が要る数だからである:

```
検査 31 = 52    検査 39 = 942    検査 37 の対 = 326    検査 40 = 7
```

⛔⛔ **検査 31 と 39 は「借金」の基準である。黙って上げるな。**
⭐⭐ **2026-09-06 に検査が 2 つ増えた** —— **検査 40（台帳の行が「未裁定」と読めるのに、その行が名指す未決事項が裁定済）と、検査 41（`tools/precheck.py` を関門に入れた。個人情報と、逆引用符付きの変更要求番号）。**
⛔ **検査 40 は、この巡でいちばん高くついた発見に対する答である** —— **「未検討」として渡した 4 件が既に決着していた件。**
⚠️ **ただし 4 件のうち捕まるのは 1 件だけである** —— **残り 3 件は裁定が変更要求にしかなく、「行が名指す変更要求」で門にすると 38 行が鳴る（体が測った）。⇒ 検査 40 はその 38 行を毎回**印字する**が、門にはしていない。**

⭐ **検査 37 の対は性質が違う** —— **「行と語を一緒に読んだ」という記録なので、一緒に直したら読み直すのが設計である。混ぜるな。**

---

## 1. ⛔⛔ **利用者が決めた 4 つの段（2026-09-05、逐語）**

> **やることは不具合の全検出。 その後、修正計画/必要なら裁定。
> その後一気に既存不具合を直す。 それが終わったら、仕様やコードのごみ消し。**

```
段 1  不具合の全検出          ✅ ほぼ済んだ（残りは D-264 の掃引と、押さないと決まらない 15 件）
段 2  修正計画／必要なら裁定   ✅ 済んだ（§4 に計画、§7 に裁定 8 件）
段 3  既存不具合を一気に直す   🔧 いまここ。⛔ 波 1 を出したところである（§5）
段 4  仕様とコードのごみ消し   ⬜ 未着手（§6）
```

---

## 2. ⛔⛔ **前の巡が身銭で買った 5 つ（⭐ これだけは読め）**

**① 行を立てる前に `pending-decisions.md` を grep しろ。**
⇒ **前の巡は「未検討」として 4 件を体に渡し、4 件とも既に決着していた。**うち 1 件は **13 日前**の裁定で、**落ちる試験の名前まで書いてあった。**
⛔ **台帳と未決事項表は別々に育つ。同じ問いが 2 つの帳簿に別々に載る。**

**② 体を同時に走らせるなら `isolation: "worktree"` を必ず書け。**
⇒ **前の巡は指定を落とし、2 体を同時に本体の作業樹へ入れた。持ち分が交わっていなかったので助かっただけである。**

**③ 表の行を足す・退役させる仕事は、生成器の入力も一緒に渡せ。**
⇒ **`_assets/fig-icons.svg` と `_source/display-words.json`。生成器は警告ではなく `sys.exit` する。**
⛔ **前の巡はこれを落とし、体が 1 字も書けずに止まった。**

**④ 変更要求の番号は裸で書け。**逆引用符で囲むと検査 7 が「存在しない UID への参照」と読む。⚠️ **前の巡は 2 度やった。**

**⑤ 済んだ印を付けた升に、開いた問いを混ぜるな。行を分けろ。**
⇒ **検査 31 が捕まえる。**

### ⭐⭐ 体に持たせて実際に効いた作法 —— **「確かめるな、覆せ」**

**条項を守っている `file:line` を名指させる。**
⇒ **前の巡はこれで 3 件の行が「主張の半分が偽」と判明した。**
⭐ **覆されるのは失敗ではない。台帳の行は仮説であって事実ではない。**

---

## 3. ⛔ **止めている残り 4 行（⭐ ここから始めろ）**

⭐⭐ **4 行のうち 3 行は 2026-09-06 に裁定が下りている。⇒ 迷わず書けるようになっている。**
**根拠と比較は `change-request/CR-367-*.md` にある。⛔ 本書は写しを持たない。**

### ⭐ 裁定済 —— そのまま書け

```
D-318  行見出しパネルの頭の [＋足す] を [すべて畳む] の直後に押す場面
       ⭐ 裁定 ⓐ: 足すときに段 0 の畳みを 1 階層だけ開く
       ⛔⛔ 「1 階層だけ」が限定である —— HF-16 と同じで、HF-10（すべて開く）ではない
         ⭐ 人が畳んだのは人の意思である。行を 1 つ足すために
           全部を開き直すのは、その意思を捨てることになる
       ⛔ 直す場所は 2 つ ——
         ① _assets/tbl-settings.md の S-211 の注記「戻す道は 2 つある」を 3 つに
         ② frame-loop.ts が、行を足した文書の変更を当てたあとに
            段 0 を 1 階層だけ開く（HF-16 と同じ書き込み）
       ⚠️ 「2 つ」は [＋足す] が実装されていなかった頃の古い列挙である

D-283  図の上の付箋（コメントボックス）に文字を書く場面
       ⭐ 裁定 ⓐ: プロパティパネルに本文欄を足す。新しい器官は作らない
       ⛔ 直す場所は _source/property-items.json に 1 行足して npm run gen
         ＋ MK-13 の「本文の編集」がその欄を指すようにする
       ⚠️ その場編集器の話は消えていない —— 担当ラベルの改名（D-273）が
         同じ器官を待っている。作るなら 2 件まとめて別の行で立てろ

D-345  図を画像に書き出したら、安全弁で行が落ちていた場面
       ⭐ 裁定 ⓐ: 書き出しの後に画面で告げる
       ⛔ 直す場所は frame-loop.ts:5484 付近の STOP（exportScene の側）
       ⭐⭐ 前の巡は ⓒ（告げない）を推奨し、実測で自分を覆した ——
         「頭を畳んだ状態で、画面は 0 行を描き、書き出した SVG は 7 行を運んだ」
         ⇒ 書き出しは画面と別に配置を組む。「画面で既に告げた」は成り立たない
```

### ⛔ 裁定は要らない。前の巡の負債である

```
D-275  取り込みが落とした行を人に告げる面
       ⛔⛔ 2026-09-06 に利用者が裁定している ——
         「その様な無効なデータは削除して残りを取り込め。
           そして『下記の無効なタスクを削除して取り込みました [OK]』としろ」
       ⛔ 前の巡はその裁定を仕様に当てなかった。FR-023 はいまも
         「(1) 落として取り込む (2) 中止する から選ばせること（MUST）」と定める
       ⚠️ 印を書き換えると検査 39 の 942 が動く。同じ回に数え直せ
       ⭐ 測る側は完成している。要るのは告げることだけ

D-284  新しく文書を始める入口が、どの表にも 0 件
       ⭐ FR-095 の本体は在るが「選ぶ」場所が無い。欠けているのは仕様の行
       ⛔ 呼び手の無い本体を先に作るな —— それが D-290 を生んだ形である
```

### ⭐⭐ D-275 と D-284 は**同じ 1 つの壁**の向こうにある

**壁の正体: 入口の表（`docs/spec/_assets/tbl-glossary.md` の T-103 と T-109）と
語の辞書（`docs/spec/_source/display-words.json`）。**
⛔ **生成器が警告ではなく `sys.exit` するので、`_assets/fig-icons.svg` も必ず一緒に渡せ。**
⇒ **1 体にこの 3 ファイルと生成器を渡せば、2 行が同時に動く。**

### ⚠️ 押して確かめてから分類を決める行が 1 つ

**`D-298`（横のホイールとスクロールバーの摘み）** —— **「傷つけている」に置いてあるが、そもそもスクロールの道が無いなら「止めている」に上がる。**

---

## 4. ⛔ **段 2 —— 計画（済んでいる。⭐ そのまま使え）**

### 4.1 ⭐⭐ **直す体はファイルで割る。⛔ 同じファイルを 2 体に渡すな**

**38 件の `製品の仕事` を、触るファイルで束ねるとこうなる:**

```
入力と Esc      input-command-translator.ts / screen-state.ts / tooltips.ts
                D-283 D-286 D-292 D-295 D-307
                ⚠️ D-267 は dom-screen-surface.ts:6905-6913 に在る（この巡に特定した）
描画            svg-renderer.ts / schedule-geometry.ts
                D-293 D-303 D-306（⭐ D-299 は直した。D-285 は裁定待ち）
殻              frame-loop.ts（⛔ 大きい。1 体にしか渡すな）
                D-275 D-279 D-284 D-287 D-289 D-290 D-291
画面の入口      command-palette.ts / screen-frame.ts / properties-panel.ts / notices.ts
                D-261 D-265 D-274 D-281 D-298 D-305 D-273
実体と層        schedule-layout.ts / document-settings.ts / edit-task.ts
                D-270 D-271 D-294 D-312 D-286
書き出し        image-exporter.ts
                D-276 D-277
原稿            docs/spec/_source/
                D-302 D-304 D-308（⛔ 原稿を直すので変更要求が要る）
継ぎ目          file-gateway / codec
                D-278 D-282
面              open-modals.ts
                D-268
```

⚠️ **重なりが在る**（`D-286` は翻訳器と `edit-task.ts` の両方に出る）。⭐ **重なる件は 1 つの体に寄せろ。**

### 4.2 ⭐ **体の使い方 → `docs/development-rules/05-working-method.md` へ移した**

⛔ **同じ理由である。6 版すべてで同一だった。**⇒ **`05-working-method.md` の第 9 節に在る。**

## 5. 🔧 **段 3 —— 一気に直す（進行中）**

### ⛔⛔ カーソル 3 種の裁定が着地していない（次の巡が引き継ぐ）

**利用者は 2026-09-06 に裁定した。体は 8 ファイルを名指して 1 字も書かずに止まった。正しい止まり方である。**

⭐ **裁定が触れていない 2 つを体が掘り当てた:**

- **設定行 `S-209`**（「縦 2 本」の 2 線の隔たり）は退役する値のためだけに在る。
  ⇒ **設定の行が 1 つ死ぬ。**変更要求は「新しい設定値 0」としか予告していなかった。
- **未決事項 `PD-343`** が、利用者が裁定したのと**同じ問い**を開いたまま持っている。

⚠️ **`FR-049` の MUST NOT が仕掛けに当たる**（「多値（`S-59` / `S-66`）は対象外」）。

⭐ **仕事は見積もりより小さい** —— **測るための 2 本の押し直しは仕様も造りも完成しており、本当に新しいのは十字と縦 1 本の 2 つだけである。**

⇒ ⭐⭐ **割り方**: **①(`S-209` と `PD-343` を含む) と ②③ に分け、②③ には `fig-icons.svg`・`display-words.json`・`command-palette.ts` と、検査 37 の対を読み直す許しを渡せ。**

---

## 6. ⬜ **段 4 —— 仕様とコードのごみ消し（未着手）**

⭐ **判定基準は 1 つ**: **その文を消したとき、失われる規則があるか。無ければごみである。**
⭐⭐ **これまでに出た実例は 9 つあり、9 つとも共通点が 1 つである** —— **どれも「事実」を書いており、事実のほうが動いた。**

```
D-248  注が「IV-12 は稼働日で測る」と述べる。CR-347 で暦日になっている
D-250  試験の冒頭が、在りもしない 8 行を除外し続ける
D-263  FR-083 の実測注記が「選ばれない」と述べる。50b2243 で解消済み
D-276  ⭐ 新: image-exporter.ts:258-260 の「SvgRenderer が描いたら合わせよ」が期限切れ
D-279  ⭐ 新: 「読める保存済みの値が無い」と書く注釈。hasUnsavedEdits は在る
       ほかに、single-html-shell.ts:539-543 の「nothing WRITES S-99 back」は偽（frame-loop.ts:6766 が書く）、
       document-change-plan.ts:566 の「six rows」は 5 行の誤り（体の申告。⛔ 台帳の行は起こしていない）
```

### 6.1 ⛔⛔ **検査を足す前に**

```
⛔ その検査が守らせる規則が、どこかに書かれているかを先に確かめよ
   ⇒ 書かれていないなら、作るのは検査ではなく規則の捏造である
⛔ 数えてから基準線を作れ
⛔ わざと壊して赤くなることを実測してから入れよ
⛔ 検査 34（コメントの px）と 検査 36（RATIONALE の MUST）は入れるな。裁定済み
⭐ 検査 35（「〜が持つ」と書いた在り処が実際に在るか）は未計測のまま
```

---

## 7. ⛔⛔ **利用者の裁定が要る（⭐ 一度に問え）**

**根拠つきの本文は `docs/development-records/pending-decisions.md` にある。
⛔ 本書は写しを持たない。一覧だけ置く:**

```
PD-420  D-262  ハイライトボックスの「下」を画面の順で決めるか木の順で決めるか
PD-421  D-272  HM-9（並べ替えが WBS へ伝わる）をどう果たすか
PD-422  D-280  S-99b の「文書の識別子」とは何か
PD-424  D-311  第 8・9・10 章（試験の仕様）をどうするか
        ⭐ 利用者は 2026-09-06 に「一旦そのまま」と述べた。取り下げではない
PD-425  D-313  性能の測定器をいま作るか
        ⛔⛔ 走らせる前に声を掛ける約束がある（RISK-001）
PD-427  D-282  知らない schemaVersion を受けるか拒むか
PD-428  D-269  分類を誤ったまま実装されている行をどうするか
        ⛔⛔ 先送りできない —— 分類を直した瞬間に検査 25 が落ちる
PD-429  D-285 / D-315  掴めない端点と、理由をいつ出すか
PD-343  カーソルの「縦 2 本」がどこに立ち、測るための 2 本とどう見分けるか
        ⛔ 利用者は 2026-09-06 に同じ問いを裁定したが、この行は開いたままである
        ⇒ カーソル 3 種を着地させる回に、必ず一緒に閉じろ（第 5 節）
```

### ⭐ 2026-09-06 に片づいた裁定 —— ⛔ **二度と問うな**

```
PD-423  合流の面を作るか取り下げるか        → 作る。面は建ち、押して測った
D-318   全畳みのあと [＋足す] を押す場面    → ⓐ 足すときに畳みも開く
D-283   付箋の本文をどこで打つか            → ⓐ プロパティパネルに欄を足す
D-345   止まった書き出しが告げるか          → ⓐ 書き出しの後に画面で告げる
```

⛔⛔ **この巡だけで「もう裁定されていた」件が 4 つ見つかっている。**
⇒ ⭐⭐ **行を立てる前に、必ず `pending-decisions.md` を grep しろ。**
**台帳と未決事項表は別々に育っており、同じ問いが 2 つの帳簿に別々に載る。**

---

## 8. **測り方の罠 → ⭐ `docs/development-rules/04-verification.md` へ移した**

⛔⛔ **本節はここに在ってはならない。**
**6 版すべてで 1 バイトも変わらず写され続けていた** —— **それは「この巡に起きたこと」ではなく**規則**だという意味である。**
⇒ **`04-verification.md` の第 6 節に在る。**⭐ **検査 0 が毎回その所在を印字するので、写す必要はもう無い。**

---

## 9. 道具

```
npm run parity             見本と出荷ビルドを 75 の操作で突き合わせる。⭐ いま 75/75
                           ⛔ また一致しきったら手を増やす番である
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated]
⭐ 選択の枠は fill="none" の <rect> ＋ 両端の 9 × 9 の白い掴み点である（この巡に実測）

.claude/skills/spec-graph-check/
  check.sh    36 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED\|FAILURES ABOVE"
  induced.py  ⛔ 仕様を書く前に。閉路は 1 回の書き込みで直す
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  must-clause-coverage-baseline.txt   検査 39 の基準線 978
  stale-blocked-baseline.txt          ⭐ この巡 46 → 47。理由はファイルの中に書いた
```

**固定の仕掛け:**
```
tests/system/measured-sweep.test.ts                    1 回の起動で回る掃引
tests/system/first-frame-is-the-settled-frame.test.ts  最初の 1 枚だけを測る
tests/system/rows-fixed-with-nothing-holding-them.test.ts  殻の規則を測る唯一の道
tests/system/user-reported-fixes.test.ts               利用者が指摘し、直したものの錨
tests/system/open-defect-pins.test.ts                  ⚠️ PINNED は空
tools/parity/check.mjs                                 75 手
```

⭐⭐ **錨は自分で鳴る。⛔ これが注ではなく錨を置く理由である。**

---

## 10. ⚠️ **試験を書いていない行が 6 つある**

⛔ **作った体に試験を書かせるな**（規則 04 §1）。**`docs/spec/` だけを読む別の体に書かせろ。**
⇒ ⭐ **この作法は前の巡で本物の欠陥を 2 件掘り当てており、親の文脈をほとんど食わない。**

---

## 11. **次の巡の目標値（⛔ 始める前に自分で測り直せ）**

```
① 止めている残り 4 行を 0 にする
   ⭐ 3 行は 1 体で片づく（入口の表 + 語の辞書 + 生成器を渡せ）
   ⛔ D-318 と D-283 は先に裁定を取れ

② 試験待ち 6 行に試験を付ける（仕様だけを読む体に書かせる）

③ 実測待ち 12 行を、出荷する造りで押して閉じる
   ⭐ D-300 でやったやり方を使え —— vite build して dist/index.html を
     file:// で開き、Playwright で押す。scratch/ に置けば追跡されない

④ カーソル 3 種の裁定を着地させる（第 5 節の割り方に従え）
```

⛔⛔ **文脈の使い方（前の巡の実測）** —— **1 波（体を出す・差分を取り込む・台帳を書く・検査・コミット）に約 20k 使う。**
⭐ **体は 160k〜340k を自分で使うが、親の文脈はほとんど食わない。**
⇒ ⭐⭐ **測る仕事は必ず体に出せ。自分で `grep` を回すな。**

⚠️ **予備が尽きそうになったら、波を始めずに本書を書け。中途半端な波は次の巡の負債になる。**
