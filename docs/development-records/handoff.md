# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> ⭐⭐ **2026-09-17 は、`CR-399`（バーやマイルストーンの上では依存線は自分の線だけで掴む）と `CR-400`（行見出しパネルは床で止まる）を実装まで入れ、`CR-401`・`CR-402`（行の軸のズームの上限と、それを探す刻み `S-238`）を仕様だけ入れた。利用者は字の大きさ・タスク間の縦の隙間・縦の折り畳みの 3 つを指示して問 1〜4 に答え、続けて全体ズームの段（125 ／ 150）・ヘルプ・選択の点線・フェードの掴みを指示した（§2.8。`rulings.md` へは未記録）。** 次の巡は下の 1 通目の段取り ⓪ から始めよ。
> ⛔ **次の巡は、`refactor` から切る新しいブランチで作業する**（利用者 2026-09-17、逐語「ロジックまで変わること了解。 だったら、セッションを分け、そのセッションでブランチを切って作業しよう。」）。これは `JDG-61` の「作業するブランチは `refactor`」を改める —— 記録は 1 通目の ⓪ で行う。`main` では直接作業しない。
> ⛔ **本書は数を持たない。⭐ §0 の手順で測れ。** 段 0 の基準値は計画の記録 16 が、出口の実測と性能の比べ方は記録 17 が、残件は §3 の問いが、裁定は `rulings.md` が持つ。
> ⛔⛔ **異常系・準正常系のケアは、リファクタ一式が終わるまで触るな**（`JDG-78`、利用者 2026-09-14）。中身は `DFC-586` が持つ —— 起こさない・自動で直して何処をどう直したか示す・直せないものは対象の ID と行番号を示す。取り込みの拒否の語（表 T-220 の `IV-`）も、そのとき一緒に書く。
> ⚠️ **§2 は、この巡の学びを規則へ下ろす前の控えである。** ⛔ 下ろしたら、その項を本書から消せ（本書に置いた学びは 1 巡で消える）。

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 次のセッションに貼る 1 通目

利用者はこの節のブロックをそのまま新しいセッションに貼る。巡ごとに本節も書き換える。

```text
refactor から新しいブランチを切り、そのブランチで作業せよ（利用者 2026-09-17、逐語「ロジックまで変わること了解。 だったら、セッションを分け、そのセッションでブランチを切って作業しよう。」）。
ブランチ名・根をどちらのブランチに置くか・refactor へ戻す時期は、⓪ で利用者に問う。タグは利用者が打つ。

0. 到達点（2026-09-17 まで。refactor の先端 = 本書を書き換えた commit）
   - CR-399（バーやマイルストーンの上では、依存線は自分の線そのものだけで掴む）と CR-400（行見出しパネルは床で止まり、床へ引いても保存する幅を広げない）が実装まで入った。試せる dist は 14c3e379 で焼いたもの（§0 で測り直せ）。
   - CR-401（行の軸の上限 = 最小の倍率から上げて、いちばん高い行の帯が初めて Row Area に達する倍率）と CR-402（それを探す刻み S-238 = ×1.1、JDG-143）は仕様だけ入った。実装と試験はまだ（前の試みは合流した木の壊し試験で不合格 → 根から戻した。理由は §2.8 ③ の 5）。
   - 単体試験の赤は FR-077 の 2 件だけ（原因は §2.8 ③ の 4）。
   - 利用者が 2026-09-17 に出した指示（字の大きさ・タスク間の縦の隙間・縦の折り畳み・全体ズームの段・ヘルプ・選択の点線・フェードの掴み）と、問いへの答えは rulings.md に未記録である。逐語は本書 §2.8 ① にある（一通目〜四通目）。
1. 読む順: 本書 §2.8 → §1.1（リファクタの段の順番と現在地）→ §5.2（ごみが増えない仕組み）→ §0（基準を測り直す）→ rulings.md の 2026-09-16〜17 の節 → defects.md の DFC-628・DFC-631・DFC-633・DFC-641。
2. ⓪ 最初に利用者に 1 通で問う（具体名・推奨・代償を付ける）
   - §2.8 ② の未回答の問い（125 / 150 で保存した文書を古い GRS が開けない件、ごみ取りの歯止めの穴をどこまで塞ぐか。前のセッションで答えが出ていれば問わない）と、§2.8 ① の「前に立つ者の読み」が否定されていないかの確認
   - 検査 43 の基準線を上げてよいか（§2.8 ① を着地より先に rulings.md へ記録するため。いま 15）
   - ブランチ名と、根をどちらのブランチに置くか（利用者は根の dist/index.html を file:// で開いて試す）
   答えを待つ間に、裁定の要らない ① を進めてよい。
3. 段取り（利用者 2026-09-17、逐語「その後の修正準は提案通り。」。⛔ 順序を入れ替えるな）
   ① CR-401 + CR-402 の実装: 仕様だけを読む試験の体 ‖ 実装の体 → 合流した木で壊し試験 → commit。そのあと DFC-610 の A/B（msedge で段 0 と交互。JDG-135、前に立つ者が走らせてよい JDG-129）。
      試験は画面に出ている倍率で測らせ、古い実装が誤る場面を必ず入れさせる（§2.8 ③ の 5）。
      CR-402 の残りの問いは 3 つとも答え済みである: 1 刻みの中は半分ずつ詰める（許容は倍率の差）／ 2 上端 64 も最後の点として調べる ／ 3 仕様には「いちばん高い行の帯の高さが、初めて Row Area の高さ以上になった倍率」と書く。⇒ 実装の前に、この 3 つを FR-016 へ書く小さな仕様の波（CR）を出す。
   ② 縦の折り畳み: [v] で開いた行は、どの縦の倍率でも開いたまま（問 4 の答え = 文書に保存）。[v] が濃く描かれているのに「することがない」と返る不具合も一緒に。仕様の波 → 実装 ‖ 試験 → 合流。
      並行してよい: DFC-611（依存線を引く間のカクつき。仮の線を 1 つの要素にして属性だけ書き換える。A/B は JDG-136 のとおり表示の倍率 50 → 100）、JDG-139 の 2 段目（試験の書き直し）と 3 段目（残り 13 個の非公開化と検査 30 の締め）。
      並行してよい（続き）: ヘルプを 3 段組みのまま縦にスクロールさせ、ブラウザのズームのキーを書く（§2.8 ① の三通目。分類と順序は任された）。⛔ 仕様は ② の仕様の波に同乗させる（仕様の体は 1 体だけ）。
      ⛔ DFC-611 の A/B は、③ で既定の表示の倍率が変わる前に済ませる。
   ③ 大きさの波（1 つの仕様の波にまとめる。字 → バーの高さ → 隙間 → 掴み代 → 縦ズームの上限と数が連鎖する）:
      既定の表示の倍率 75 ／ 縁取り = 字 × 0.10（S-34 を 0.17 から下げるだけ。見てから判断）／ 字 = 実績バーの高さ × 0.90 ／ タスク間の縦の隙間 = 1px + 依存線の幅 + 1px（行見出しパネルだけ図形と枠の隙間を 4 → 1 に詰め、帯の下限 約 24px を守る）／ マイルストーンの高さ ≦ 矩形 ／ 掴み代は描いた形を先に見る ／ 掴み代の大きさの束ね（frame-loop.ts の POINTER_SLOP）を Entity へ寄せる。
      表示の倍率の段に 125 と 150 を足す（100 の上。150 で描く比は 1.0）／ 選択の枠の太さ S-174 2 → 1px、点線の刻み S-175 2×2 → 2×1px ／ フェードの掴みの □ S-109 9×9 → 7×7px、枠線 S-110 1.5 → 1px（押せる広さ S-92 15×15 はそのまま）。数はどれも試す値で、見てから詰める。
      軽い問い a〜k は推奨どおり（§2.8 ②）。
      → 実装 ‖ 試験 → 合流 → dist を焼いて利用者に見てもらい、数だけを設定の行で詰める。
   ④ 計画の段 3 の門を仕上げる（JDG-124・JDG-128。門 1・2 を書き、門 3・4 を check.sh の 56 / 57 に配線する。現在地は §1.1）。そのあと計画の段 4 へ。
4. 残る件: 承認待ち（検査 55 のファイル別の基準線 t-023d 926 → 924、木の密度 2710 → 2703）、裁定待ち DFC-550・DFC-566・DFC-569・DFC-570、DFC-630（描いた px の境の比較 13+1 か所）、DFC-632（検査 43 が .txt の着地先を数えない）、DFC-637・DFC-638、DFC-639・DFC-641 の仕様の穴。DFC-594（E）は保留（JDG-107）。DFC-571 は DFC-558 待ち。異常系は DFC-586（JDG-78、リファクタ一式の後）。
5. 毎回の約束（過去の引継と今回までの学び。詳しくは handoff.md の §0.1・§0.2・§2.5〜§2.8）
   - 問う前に rulings.md を grep する。「条項が無い」は §0.2 の 3 手で反証してから。問いは 1 通にまとめ、具体名・推奨・代償を付ける。
   - 前に立つ者は実装・試験・台帳を書かない。体に出す（判断は Opus、機械的な作業は Sonnet）。反証の体には直させない。基準線だけは前に立つ者が、利用者の了承のあとで直す。
   - 前に立つ者がブリーフに書く事実は仮説である（2026-09-17 は 20 件あまりが体に反証された）⇒ file:line で反証させてから使う。
   - 実装の体 ‖ 仕様だけを読む試験の体を並べるときは、新しい欄・引数・関数の名前と型と位置を、両方のブリーフに同じ字面で書く。
   - 壊し試験は、合流した木で単体試験の全体を走らせて数える（実装の体だけの壊し試験は 0 件になる）。
   - 基準線を動かす直しは、先に利用者に問う（裁定を着地より先に記録して検査 43 が増えるときも）。⛔ 体に基準線を触らせない。
   - 許可の確認や分類器に止められた手順は、別の道具で回り道しない。止まって報告する ⇒ 消す手順を渡すブリーフには必ず書く（2026-09-17、掃除の体が回り道をした）。
   - 体にリポジトリの外へ書かせない。一時ファイルは scratchpad（2026-09-17、全検査の出力を 1 つ上のフォルダへ書いた体がいた）。
   - check.sh は体が台帳を書いている間に走らせない（偽の赤）。終了コードを読んでから commit する。
   - 台帳や裁定の行を足したら、commit の前に python docs/spec/_source/row_id_prefixes_json_to_md.py を回す（検査 27）。閉じる行は md5 で押さえて fixed-defects.md へ移す。
   - 性能の測定の前は利用者を呼べ。A/B は段 0 を建て直して同じ日に交互に走らせる（記録 17）。測っている間は重い体を出さない。
   - F11・Esc などブラウザのアクセラレータを通る所作は Playwright では見えない ⇒ 属性ではなく効果を主張させ、残りは利用者の手で確かめる。
   - Bash で作業木へ cd すると主作業ディレクトリが移る ⇒ ( cd … ) の部分シェルか git -C を使う。作業木を切る体を出す前に根へ戻る。
   - Windows の python で日本語を print すると cp932 で落ちる ⇒ PYTHONIOENCODING=utf-8 を付ける。スクリプトはヒアドキュメントではなくファイルに書いて走らせる。
   - tools/probe/harness.mjs の until は関数を Node の側で呼ぶ（DFC-613）⇒ 探り針を書く体のブリーフに書く。
   - git add -u や一括の add はフックが止める ⇒ パスを名指しする。並行の体がいるときは git stash をしない。
6. コミット／push は許可済み（JDG-58）。個人情報の点検の数を報告してから行え（file:// と http:// は絶対パスの検出から除く。dist の GoodRelax は著作権表示）。タグは利用者が打つ。説明は日本語、コードとコミット文は英語 ASCII。
7. 片付け: .claude/worktrees/ に体の作業木が 3 本ある。CR-401 の実装の体と試験の体の 2 本（4076f987 から切った前の試み。未コミットの差分は参考として読んでよい）と、CR-402 の仕様の体の 1 本（中身は refactor に入った）。CR-402 の 1 本は node_modules を根への junction で張ってある ⇒ 消す前に rmdir（/s を付けない）で junction だけを外し、根の node_modules が残ることを確かめる（規則 04 の 6.11）。
```

---

## 0. **1 通目でやること（順番どおり）**

```
0. docs/development-records/rulings.md を読む   ⛔⛔ ここから始めろ。理由は 0.1
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npx vite build                                ⛔⛔ e2e の前に必ず
4. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ
5. §1 と §2 を読む
```

## 0.1 ⛔⛔ **利用者に問う前に、必ず `rulings.md` を検索しろ**

**利用者の指示（2026-09-08、逐語）:**
> **「これまでも同じ裁定を繰り返している。 何とかしてくれ。非効率すぎ」**

⛔⛔ **同じことを二度問うのは、この巡までに何度も起きた。⭐ 原因は置き場が散っていたことである。**
⇒ **`docs/development-records/rulings.md` が、いまは唯一の置き場である。**

```
問おうとしたら、まずこれを打て
  grep -n "<問おうとしている言葉>" docs/development-records/rulings.md
```

⭐ **注ではなく検査で守らせている**（⛔ 注は守られない。この木の実績値）:
```
検査 43   rulings.md の「適用済」の行の逐語が、docs/spec に実際に在るか
          ⛔ 無ければ赤 —— 「裁定は下りたが、誰も書いていない」を機械が言う
          ⛔ 「⛔ 未着地」の数が基準線を超えても赤
```
⇒ **裁定が宙に浮いたまま巡が終わることが、もう起きない。**

⚠️⚠️ **2026-09-08 に、なぜ二度問うたのかを 1 つ書き残す。⭐ 型が分かる:**
```
利用者   「タスクのダミーは1日とする。 だから = は1日」   ＝ 開始日 = 終了日 は 1 日
設計表    実績の右端 ＝ 開始日 ＋ 長さ（稼働日）          ＝ 開始日 = 終了日 は 0 日
⇒ 同じ言葉が 2 通りに読める。⛔ 体は設計表を読んで「穴は無い」と結論し、
  前に立つ者はそれを受け入れ、⇒ 利用者に同じ裁定をもう一度問うた
```
⛔⛔ **裁定が「守られていない」と見えたら、まず「言葉の意味が 2 通りある」ことを疑え。**
⭐ **利用者の読みが常に正である。⇒ 仕様の言葉のほうを直せ。**

⛔⛔ **`--start` を刻んだあと `git checkout -- docs/development-records/defects.md` をするな。**
**基準行はそのファイルの件数ブロックの中に在る。checkout は基準ごと消す。**

**サブエージェントは許可されている**（利用者、2026-08-31 以降くり返し再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
⭐⭐ **2026-09-08 に範囲が広がった**（利用者の裁定、逐語「**実測済みは、当然エージェントを使って移させろ**」）——
**台帳の刈り取りのような機械的な作業も体に出す。⛔ 見分けるのは行数ではなく、機械的かどうかである。**
⇒ **規則 05 の 6 に下ろしてある。**

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**
⭐⭐ **知見は規則へ下ろせ**（利用者の指示 2026-09-03）。⛔ **本書に置いた学びは 1 巡で消える。**
⭐ **2026-09-08 が下ろしたもの**: 規則 04 の **6.7**（作業木の 2 つの罠）、規則 05 の **6**（機械的な作業も体へ）。

---
## 0.2 ⛔⛔ **利用者に渡す前に、「条項が無い」を 自分で 反証しろ**

**利用者の指示（2026-09-09、逐語）:**
> **「なんで同じような裁定をさせるのか？ なぜ一度に裁定が終わらないのか？ 新規の不具合出たのか？ 俺が何度ども指示しているのは既存の不具合を0にしろ、新規の不具合を探すテストはするな、必要なのは修正確認テストだけだ。」**

⛔⛔ **体が「覆う条項が無い」と申告したら、それは 仮説 である。⭐ 実測ではない。**
⚠️⚠️ **2026-09-09 の実測: そうやって利用者へ渡した 4 件のうち、⛔ 3 件は裁定案件ではなかった。**

```
① 進捗マーカーを掴みシロの外へ出す
   ⇒ 表 T-023d の GR-7 が既に「終了点の掴みシロの外側」と定めていた
   ⛔ grep 1 回で分かった。⭐ 利用者の言葉:「これって裁定案件じゃないだろ？」
② 再開アイコンと予定バーのどちらを優先するか
   ⇒ 「拡大すれば予定は編集できる。しかし拡大しても再開はできない」で導けた
   ⛔ しかも前に立つ者の推し（予定バー優先）は 却下 された
   ⭐ 利用者の言葉:「これも非裁定案件。」
③ ダミーの印を押したとき、開始側と終了側のどちらを掴むか
   ⇒ 利用者は既に 2 回「終了を優先」と裁定していた。3 つ目の場面にすぎない
   ⭐ 利用者の言葉:「これも終了優先で変わらず。」
```

⇒ ⛔⛔ **`裁定待ち` に置く前に、この 3 つを打て。⭐ 打った跡を行に書け。**

```
1  grep -n "<その語>" docs/development-records/rulings.md
     ⇒ 利用者が既に同じことを裁いていないか
2  python .claude/skills/spec-graph-check/impact.py <関係する行 ID>
     ⇒ 隣の行が同じ判断を既に下していないか
     ⭐ 実例: 「実績バー本体を掴み領域にしない」は、再開アイコンと同型であった
3  その所作が 他の道 でも行えるか
     ⇒ 行えるなら、優先順は「他の道が無いほう」が勝つ
     ⭐ 利用者の②の裁定は、まさにこの形で下りている
```

⭐⭐ **3 つとも空振りしたときだけ、`裁定待ち` にしてよい。**
⛔ **そのとき行に「この 3 つを打って空振りした」と書け。⭐ 書いていない行は、利用者の時間を使う資格が無い。**

---
## 0.5 ⛔⛔ **基準値は本書に書かない**

**⇒ 第 0 節の 1〜4 を走らせれば、始点はすべて出る。**
⭐ **基準線は、それぞれのファイルの 1 行目が持っている。⛔ 理由はその下に書いてある:**

```
検査 31  .claude/skills/spec-graph-check/stale-blocked-baseline.txt
検査 37  .claude/skills/spec-graph-check/dictionary-table-pairing.txt
検査 39  .claude/skills/spec-graph-check/must-clause-coverage-baseline.txt
検査 40  .claude/skills/spec-graph-check/ruled-elsewhere-baseline.txt
検査 41  .claude/skills/spec-graph-check/decided-spec-baseline.txt
検査 42  .claude/skills/spec-graph-check/quoted-source-baseline.txt
検査 43  .claude/skills/spec-graph-check/ruling-landed-baseline.txt   ⭐ 裁定が着地するたびに下げる
検査 11  docs/review/duplication-baseline.txt
```

⛔⛔ **黙って上げるな。上げるなら、そのファイルの中に理由を書き、コミット文にも書け。**
⭐ **2026-09-08 の実例が、上げ方と返し方の両方を示している** ——
**裁定が MUST を 52 足したので検査 39 を意図的に上げ、同じ巡のうちに 21 返した。**
⭐⭐ **返せた理由は 1 行のブリーフである** —— **「錨が条項を逐語で持てば、そのぶん借金が返る」と体に書いた。**
⇒ **修理の体のブリーフに毎回この 1 行を入れろ。⛔ ただでは返らない。**

---

## 1. ⛔⛔ **利用者が決めた 4 つの段（2026-09-05、逐語）**

> **やることは不具合の全検出。 その後、修正計画/必要なら裁定。
> その後一気に既存不具合を直す。 それが終わったら、仕様やコードのごみ消し。**

⭐ **この 4 つの段は、いまはリファクタの計画の段 0〜8 が引き継いでいる。**
⛔ **段の状態は計画の状態表が持つ。本書に写すな** —— 前の版が写した表は、台帳の残件と食い違ったまま残っていた。
⚠️ **計画の「段」と、この 4 つの「段」は別の番号である。混ぜるな。**

## 1.1 ⚠️ **リファクタの段の順番と現在地（2026-09-17、`5d159a5f` で実測）**

⭐ **利用者の指示（2026-09-17、逐語「その中でリファクタリングの順番と現在地も書け。」）で本書に書く。** ⛔ 上の「本書に写すな」の理由どおり腐るので、使う前に計画の状態表と `git log` で測り直せ。

**2 つの番号の対応:**
- **利用者の 4 つの段**（§1、`JDG-24`）: ① 不具合の全検出 → ② 修正計画と裁定 → ③ 一気に直す → ④ 仕様とコードのごみ消し。
  - ④ のごみ消しは、清掃の巡（タグ `After-Cleanup` ／ `Before-Refactor`、`8701ccf3`）として走ったと読める（推定）。残りはリファクタの計画が引き継いだ（計画の記録 8、`JDG-53`）。
  - ⚠️ §5 の見出しの「段 4」は、この 4 つの段の 4 番目である。計画の段 4 ではない。
- **リファクタの計画の段 0〜8:** `docs/development-records/refactor-plan-report-2026-09-13.md` の状態表と流れ図が持つ。

| 計画の段 | 何をするか | 出口 | 現在地 |
|---|---|---|---|
| 0 足場 | 赤い試験を緑に、dist の焼き直し、基準の記録、割っても残るコードの不具合、CR-374 | vitest の失敗 0、基準を記録 | ✅ `cdba2292` |
| 1 棚卸し | 保存しない状態の一覧 | 状態 1 つに 1 行 | ✅ `90785873`・`698a0f14` |
| 2a 辺の規則 | JDG-47 を設計の 5.2 へ入れる CR | CR が入り、検査が緑 | ✅（推定。CR-378）⚠️ 状態表は ⬜ のまま古い |
| 2b 状態のモデル | ADR-002、状態の SSOT | 同上 | 2b-A ✅（CR-379、`3fa4fa0b`）。2b-B は段 7 へ延期 |
| 3 門 | 辺の許可表、JSON の辺、大きさの歯止め（JDG-54）、モジュールの状態 | わざと破ると赤、今日の基準で緑 | **途中**。門 5 ✅（`2fef6e19`）。門 3・4 は道具だけが在り、check.sh に未配線（56 ／ 57、基準線ファイルが無い）。門 1・2 は未着手（`tools/check_layer_rules.py` がまだ `.json` の import を飛ばす。`agent-api-members.ts` が edit-document を import したまま —— JDG-124 の ①） |
| 4 神関数 | editTask ／ editTaskGroup ／ svgFromSchedule ／ builtMerge を割る | 500 行を超えるのが frameLoop と domScreenSurface だけ | 未着手（段 3 の出口と T-075 の単位の CR を待つ） |
| 5 入力の訳出 | input-command-translator.ts を割る | 名前と署名が不変、試験が不変で緑 | 未着手 |
| 6 画面の面 | dom-screen-surface.ts を割る | 同上 ＋ DOM が同一 | 未着手（DFC-566 の裁定も待つ） |
| 7 状態機械 | frame-loop の状態を状態機械へ移す | 核の let だけが残り、モジュールの状態 0 | 未着手 |
| 8 仕上げ | 部品、図、測り直し | 計画の記録 3 の目標 | 未着手 |

**この巡の位置づけ:**
- 1 通目の ①〜③（CR-401 ／ CR-402、縦の折り畳み、大きさの波）は、計画の段 4〜7 で割る関数そのもの（svgFromSchedule、input-command-translator、domScreenSurface、frameLoop）に触る。⇒ **割る前に済ませる**（`JDG-98`、`JDG-127`、計画の記録 7(a)）。段 4〜7 の出口の「試験が不変」「DOM が同一」を守るためである。
- 段 3 の門を ④ に置いたのは選択である。段 3 の入口の条件（2a）は満たしている。
- ⚠️ 門が無いあいだに関数は育つ: frameLoop は提案のときの 2190 行 → 2352 行。
- ⚠️ JDG-124 の基準は `178e7369` で測った値である（辺 48、関数 76 本で超過 7847 行 ／ 910 分岐、モジュールの状態 2）。`5d159a5f` では関数 78 本、8045 ／ 932 である。⇒ JDG-128 のとおり、門を入れる commit で測り直し、差を波の diff で説明できなければ入れる前に利用者に見せる。`POINTER_SLOP` を Entity へ寄せると辺の 48 も動く。
- ⚠️ 段 4 の出口には、svgFromSchedule（597 行）と editTask（510 行）を 500 行以下にすることも要る。
- `main` は `ac12202e`（`refactor` より 47 commit 前）。`JDG-61` のとおり、計画の段の出口で早送りする。
- ⛔ 計画の状態表の 2a ／ 2b-A の行は古い ⇒ 最初の巡で体に直させる（機械的）。

---

## 2. ⚠️ **この巡（2026-09-14、`c6ebfd88..` から `refactor` の先端まで）が身銭で買ったもの —— 規則へ下ろす候補**

⛔ **本節は置き場ではなく、下ろす前の控えである。** 下ろすのは前に立つ者。⛔ 下ろしたら項ごと消せ。
⭐ **前の版の ①〜⑤・⑦・⑨〜⑪ は `c6ebfd88` が規則へ下ろした**（同コミットの本文が一覧を持つ）。前の版から残したのは ⑫（いまの ⑥）と ⑥（いまの ⑦）で、⑧ は新しい規則が要らないので消した。どれも 2026-09-14 に `docs/development-rules/` を grep して、まだ無いことを確かめた。

```
① ⛔ 「終わった」と返した体が、わざと壊した版を作業木に残し、裏で Playwright を 2 本走らせ続けていた（T5b）
   前に立つ者の e2e と同時に走り、ブラウザを取り合った。写した版は壊す前のものだったので入らなかった
   ⇒ 体の最後の報告が「走行が続いている」「戻す」で終わっていたら、完了ではない。TaskStop で止め、
     node の Playwright の走行が残っていないことを数えてから e2e を回す。
     写す前に、体の作業木の現物と、写す版を diff する
   下ろす先  規則 05 の 9（体の報告を信じない）＋ 規則 04 の 6（e2e を単独で回す）   ⭐ 下ろす価値あり

② ⚠️ 仕様の表の行を 1 字直した（検査 46 の改行）波が、その表から作る生成物（icon-roster.json）を作り直さず、
   3 コミット後の全ゲートで gen:check・検査 27・検査 37 が同時に赤くなった
   ⇒ docs/spec/_assets の表を触ったコミットは、同じコミットで npm run gen を回す（仕様だけのコミットでも）
   しかも、生成物だけを別コミットに入れると、上流の無いブランチでは検査 41 の罠 3 が「手で直した」と誤って赤くなる
     （原稿が同じ差分に無いため。上流を持つ refactor へ入れると消えた）
   下ろす先  規則 02 の末尾（仕様だけのコミットの前に回すもの）   ⭐ 下ろす価値あり

③ ⚠️ 古くなった試験を直す体が、元の試験に無い主張（入口の無い項目にも図形を求める）を足し、正しい実装を赤くした（DFC-105）
   ⇒ 修理のブリーフに「元の試験が主張していなかったことは足すな。足すなら、それを名指す条項を書け」を入れる
   下ろす先  規則 05 の 9（ブリーフの型の 9「条項が無いなら発明するな」の隣）

④ ⚠️ 起草の体 2 体が、同じ新しい表番号 T-247・T-248 を取り合った（CR-378 と CR-379）
   ⇒ 新しい番号を取る起草を並行に出すなら、ブリーフで番号の帯を割り当てる
   下ろす先  規則 02（表番号の振り方）

⑤ ⚠️ 体が報告した合計（61）が、同じ報告の中の群ごとの数（56）と合わなかった
   ⇒ 数は報告の文ではなく、体が書いたファイルから数え直す
   下ろす先  規則 05 の 9（体の報告を信じない）

⑥ ⚠️ ヘッドレスの Playwright は、ファイルを開いたあとに立つ面（IC-71〜73・IC-95〜97）に届かない
   ネイティブのファイル選択は操作できず、落とす道は何も起きない（DFC-569）
   下ろす先  規則 04 の 6（測り方の罠）

⑦ ⚠️ 個人情報の数えの「秘密」の語は、この木では誤検知ばかりである
   （display 語の watermark unlock password、type="password" の入力欄、雛形の作業名 token exchange、試験の GLYPH_TOKEN）
   ⇒ 0 と言う前に当たった行を開いて読む。§9 に既に同じ注がある ⇒ 新しい規則は要らない

⑧ ⛔ 台帳の行を書いた体が、状態と中身を取り違えた（2026-09-14、DFC-588・DFC-589）
   仕様が禁じている import を「禁じていない」と書き、表の置き場を別のファイルと書き、2 行とも 裁定待ち にした
   刈り取りの体も、取下げ の行を 実測済 と報告した
   ⇒ 台帳の行を体が書いたら、前に立つ者が行を開いて読む。裁定待ち は §0.2 の 3 手の跡が無ければ差し戻す
   下ろす先  規則 05 の 9（体の報告を信じない）   ⭐ 下ろす価値あり

⑨ ⚠️ 仕様のセルとコードの公開名が同じ波に要る変更要求（表 T-064 の行と公開エントリ）は、
   仕様・実装・試験を別々の作業木で並行に出し、合流用のブランチで重ねると衝突 0 で入った
   ただし試験の体は実装の署名を知らないので、合流の後に「試験を署名に合わせる締めの体」が必ず要る（§2.6 の ⑤ と同じ代償）
   下ろす先  規則 05 の 7（波の型）

⑩ ⚠️ 型の変換（as unknown as）で手組みした試験の値は、型に必須の欄を足しても型検査で落ちず、描く所で落ちる
   ⇒ 型を広げる体のブリーフに「as unknown as で組んだ試験の値を grep で数えよ」を入れる
   下ろす先  規則 05 の 9（ブリーフの型の 4「同じ形の枝を数えよ」の例）

⑪ ⚠️ 基準線を超える変更要求を夜間に当てるときは、refactor ではなく合流用のブランチに止めて朝に問う
   コミットの門（§9）を割らずに、次の変更要求を積み上げられた
   下ろす先  規則 05 の 7（波ごとにコミットする）の隣

⑫ ⚠️ 台帳の「壊しても赤が 0」は、何ファイルを走らせて測ったかで変わる（2026-09-15、DFC-587）
   行は 5 ファイルだけで 0 と書いていたが、単体試験を全部走らせるとヘルプとパレットは既に 11〜52 件赤くなった。穴はパネルだけだった
   ⇒ 壊し試験は全体で走らせ、測った範囲を行に書く
   下ろす先  規則 04 の 6（測り方の罠）

⑬b ⛔ 体が「裏の走行（Playwright・壊し試験の台本・check.sh）を待つ」と書いて止まる（2026-09-15、4 回）
   体は自分では起きない。⇒ 前に立つ者が、その作業木の名を含むプロセスが消えるまで裏で見張り（until ループ）、
   消えたら SendMessage で「走行は終わった、報告を仕上げよ、もう走行を始めるな」と送る
   ⚠️ SendMessage で足した仕事は、最初の報告（足す前の分）が先に届き、足した分の報告は 2 通目で来る —— 1 通目で取り込むな
   下ろす先  規則 05 の 9（体の報告を信じない）   ⭐ 下ろす価値あり

⑭ ⚠️ 実装の体と「仕様だけを読む試験の体」を並べると、仕様の穴が両側から同時に出た（2026-09-15）
   CR-380 の矛盾（マーカーを数える位置）を実装の体と試験の体が別々に見つけて止まり、仕様の波 3 で閉じた。
   さらに試験と実装が食い違った赤は、反証の体に FALSE / TRUE / 誤診 で裁かせると、誤診（仕様の穴）が 3 件出た
   ⇒ 実装と試験の食い違いは、直す前に反証の体へ。穴は小さな仕様の波で閉じ、実装・試験の体へは署名と読みだけを伝える
   下ろす先  規則 05 の 7（波の型）

⑮ ⚠️ 作業ディレクトリが scratchpad に移ると、作業木を切る Agent が「リポジトリではない」と断る（2026-09-15）
   Bash で scratchpad へ cd したまま次の呼び出しをすると、主作業ディレクトリが移る ⇒ 体を出す前にリポジトリの根へ cd で戻す
   下ろす先  規則 04 の 6.7（作業木の罠）

⑯ ⚠️ 検査 11 は、仕様の文が大きく増えると、中身の変わらない既知の群を崩して赤くなる（共通語のしきい値 COMMON_GRAM）
   ⇒ 赤を新しい重複と読む前に、最後に緑だったコミットで同じ検査を回し、群の顔ぶれを比べる
   下ろす先  規則 04 の 6（測り方の罠）
```

---

## 2.5 ⛔⛔ **段取りの型（⚠️ 規則へまだ下りていない分）**

**利用者の指示（2026-09-08、逐語。`JDG-12`）**:
「**エージェントに実装と単体テストをやらせて、すべて終わったら、一気にブラウザを操作する実測テストをやればいいだろ？**」

### ⭐ 波の型（2026-09-08 に 15 体で回した形。⛔ 順序を入れ替えるな）

```
波0 反証   1 体につき 5〜8 行。⛔ 直すことを禁じる。FALSE / TRUE / 誤診 の 3 択で裁かせる
波1 裁定   波0 が挙げた問いを 1 通にまとめて利用者へ。⛔ 小出しにしない
波2 仕様   ⛔ 1 体だけ。直列（規則 05 の 6）。⛔ 体に並行で書かせると同じ文書が壊れる
波3 修理   フォルダで割って並列。⛔ 同じファイルを 2 体に渡すな
波4 試験   仕様が変わって古くなった逐語を持つ試験を直す。⭐ 波3 と同じ巡に必ず来る
波5 実測   出荷ビルドを 1 回起動して、まとめて押す
波6 台帳   機械的なので体に出す（利用者の裁定 2026-09-08）
```

⛔ **波ごとにコミットしろ**（理由は規則 05 の 7「波を出す前に commit する」）。

### ⛔ 合流の作法（⭐ 2026-09-08 は 15 回、2026-09-14 も全部 SAFE で事故 0 であった）

```
体の作業木の「変更あり」は、その体の基準に対する差である。⇒ 丸ごと写すと幹の仕事が消える。
先に数えろ:
  for f in $(git -C <wt> status --porcelain | grep -v '^?? ' | awk '{print $2}'); do
    git diff --quiet <体の基準 sha> HEAD -- "$f" && echo "SAFE $f" || echo "PATCH $f"
  done
SAFE なら cp、PATCH なら:
  git -C <wt> diff -- <file> > <scratchpad>/x.patch
  git apply -3 <scratchpad>/x.patch
```

⭐ **体の持ち場をフォルダで割っておけば、ほぼ全部 SAFE になる。**⇒ **割り方が合流の費用を決める。**

### ⭐ 体に渡すブリーフの型（⛔ この 10 行を毎回書け）

```
1  git merge --ff-only <sha>            ⛔ 1 行目に。忘れると行番号が全部ずれる
2  親から docs/spec/output/ と docs/reference/mspdi/ を写せ  ⛔ 無いと偽の赤（規則 04 の 6.7）
3  編集してよいファイルを列挙し、⛔ 同時に走っている体の持ち場を名指しで禁じる
4  「同じ形の枝が他にいくつあるか、書く前に数えろ」
5  「わざと壊して何件赤くなるか測れ。⛔ 0 なら 0 と正直に書け」
6  「対照を書け ——『逆に壊れていたら何が通ってしまうか』」
7  検査は終了コードで判じよ。⛔ FAIL を grep するな
8  基準線ファイルを触るな。⭐ ただし錨が条項を逐語で持てば借金が返る
9  ⛔ 条項が無いなら発明するな。止まって報告せよ。⭐ 止まるのは失敗ではない
10 許可の確認や分類器に止められたら、止まって報告せよ。⛔ 別の道具で回り道するな。一時ファイルは scratchpad に置け（§2.8 ④）
```

---

## 2.6 ⚠️ **前の巡の罠のうち、規則へまだ下りていないもの**

⭐ **2026-09-14 に `docs/development-rules/` を grep して、下りていないことを確かめたものだけを残した。**
⛔ **規則へ下ろしたら、ここから消せ。**

```
①  出荷ビルドを押す探り針（2026-09-10）
    フレームは rAF で描かれる。Agent API の書き込みだけでは 1 枚も来ない ⇒ 書いたあとポインタを 1px 揺らす
    focusTask はタスクの開始を画布の左端へ寄せる。そこはコマンドパレットの下である ⇒ 焦点は 1 度だけ当て、日付を右へ動かす
    雛形のタスクは年単位なので、窓に中央が入らない ⇒ 1 か月に仕立て直す

②  日本語の逐語は、折り返しで 2 行に割れると黙って効かなくなる（検査 39 / 42 は文字の並びで照合する）
    ⇒ 同じ言葉を途切れずに 1 行で書く
    ⚠️ tests/ のコメントはいま ASCII だけである（JDG-62）。効くのは試験の文字列と、門より前に書かれた試験である

③  設計の数を丸めると、不変条件を割ることがある ⇒ 数ではなく導出を書け
    ⚠️ 実例（2026-09-10）: S-5 の 0.57 は 16 ÷ 28 の丸めで、S-4 の既定が自分の下限を割った

④  「同じ形の枝が他にいくつあるか」は、読むのではなく数えさせろ
    ⚠️ 実例（2026-09-10）: ブリーフが 4 行を挙げたが、体が全行を掃くと同じ形の行はその 3 倍を超えた

⑤  複数の体を同じコミットから並行に切ると、継ぎ目に必ず借りが残る（型検査の赤など）
    ⇒ 並行の代償は「締めの体を 1 体余分に出すこと」だと見積もれ

⑥  作業木が積み上がると、幹でだけ Playwright が崩れる
    ⇒ 幹の赤を退行と読む前に、同じコミットを新しい作業木で走らせろ。古い作業木を掃くのは巡の頭の安い仕事である

⑦  言葉で立たない設計は、図で立てる。却下の理由が次の案の設計条件である
    ⇒ 図に数を書くなら、その数で描け。⛔ 比は内容である（違う比で描くと、利用者は設計ではなく図を叩く）

⑧  新しい規則を書く前に、既にある行の値を疑え（法則が仕様に在って、値だけがそれを殺していることがある）

⑨  S-93 は退役した。change-request/ と検査の基準線ファイルに残る S-93 の語は履歴である ⇒ ⛔ 消すな
    ⭐ md-checks.py の退役した行 ID の名簿が黙らせている

⑩  docs/review/duplication-baseline.txt の T-023d まわりの 3 群は、撤回の記録を逐語で残したことによる
    意図した重複である ⇒ ⛔ 畳むな

⑪  tests/unit/fr-085-double-click-a-row-name.test.ts は、全体走行でだけ落ちることがある（間欠）
    ⇒ 1 ファイルで走らせてから退行と読め。⛔ 通ったからといって直ったとも言えない
```

---

## 2.7 ⚠️ **作業の作法（2026-09-16 に分かったこと）**

⛔ **規則へ下ろしたら、項ごと消せ**（§2 と同じ）。

```
①  利用者は根の dist/index.html を file:// で開いて試す
    ⇒ 合流は別の作業木（例 .claude/worktrees/wave-cr389）で組み、根は refactor のまま置く
    ⇒ refactor を進め、根の dist を npx vite build で作り直してから「試せる」と言う
    ⭐ そのとき ブランチ・commit・dist/index.html の sha256 を添える

②  .claude/worktrees/ の下の作業木は、node の解決が根の node_modules まで上がるので、
    junction を張らなくても vitest が走る
    ⛔ junction を張るな —— 作業木を消すときに根の node_modules を消す事故のもとである（掃き方は規則 04 の 6.11）

③  性能の A/B の探り針（zoom-row-axis.mjs・dependency-drag.mjs）と回し方（run-ab.py）は
    docs/development-records/measurements/frame-time-ab-2026-09-16/extra-zoom-row-axis-and-dependency-drag/ に在る
    ⚠️ 1920×1080 と 1920×400 で二分法の道が違う —— 1080 では字の上限が先に効いて二分法は走らず、
       400 では行の領域の上限が効いて走る（記録 17 の追補）⇒ 行の軸のズームは両方の窓で測れ
    ⛔ 性能の測定の前は利用者を呼べ

④  e2e の既知の赤は NFR のフレーム時間の門 2 件である
      tests/nfr/nfr-001-010-011-013-the-rest-of-chapter-7.test.ts
      tests/nfr/nfr-002-003-frame-time-is-the-interval.test.ts
    ⛔ 合流の条件は NFR 以外の赤 0（数え方は §4）

⑤  tools/probe/harness.mjs の until は、関数を渡すと Node の側で呼び、文字列を渡すとページで評価する（DFC-613）
    コメントは「関数はページの中で評価する」と読める ⇒ until(() => typeof window.x …) は例外を飲んで常に false になり、
    Agent API が起きないと誤読させる（2026-09-16、体が 2 回誤読した）
    ⛔ 探り針を書く体のブリーフに必ず書け（DFC-613 が直るまで）

⑥  実装の体と仕様だけを読む試験の体を並べるときは、新しい欄・引数・関数の名前と型と位置を
    両方のブリーフに同じ字面で書く（2026-09-16、RowTitle.fontPx と frameLoop の 12 番目の引数 FullScreenHost ／ fullScreenChanged）
    ⭐ 2 波とも合流の衝突 0。⚠️ 型の名前を export するかは検査 26b で決まる ⇒ 試験には型名を import させず、構造の型で渡させる

⑦  実装の体の壊し試験は、試験の体の試験がまだ無いので 0 件になる（2026-09-16、2 波とも全部 0 だった）
    ⇒ 壊し試験は、合流した木で単体試験の全体を走らせてから数える（同じ壊し方で 2〜9 件赤になった）

⑧  台帳や裁定の行を足すと、docs/spec/_assets/tbl-row-id-prefixes.md（records の行を数える生成物）がずれて検査 27 が赤くなる
    ⇒ 台帳・裁定を触った commit の前に python docs/spec/_source/row_id_prefixes_json_to_md.py を回す
    ⚠️ 裁定を着地より先に記録すると検査 43 の数が増える ⇒ commit の前に利用者に基準線を上げてよいか問う（2026-09-16 は 6 → 9、着地で 8）

⑨  Bash で作業木へ cd すると、セッションの主作業ディレクトリがそこへ移る
    ⇒ 作業木では ( cd <作業木> && … ) の部分シェルか git -C <作業木> を使う。作業木を切る体を出す前に根へ戻る

⑩  ブラウザのアクセラレータを通る所作（F11、Esc による全画面の解除）は Playwright では見えない
    DFC-104 の実測と nfr-004 の網羅は、DOM の属性の反転を「効いた」と数え、全画面が効かないことを見逃した
    ⇒ 属性ではなく効果（document.fullscreenElement など）を主張させ、残りは利用者に手で確かめてもらう（DFC-609 は利用者の確認で閉じた）
```

---

## 2.8 ⚠️ **2026-09-17 の控え —— 利用者の指示と答え（未記録）、未回答の問い、実測、作法**

⛔ **① は `rulings.md` へ記録したら、② は答えを記録したら、③ は台帳か変更要求へ下ろしたら、④ は規則へ下ろしたら、ここから消せ。**

### ① 利用者の指示と答え（逐語）

**指示（2026-09-17）:**

```text
以下の改善を求める。 いつ、どの順で修正するか提案せよ。
なおこの指示の後半はログを含むので、ログはサブエージェントに解析させろ

# フォント
さすがにフォントサイズが小さすぎて読みにくい
- デフォルトの倍率は最新現状に対してヘッダーの(+)を2回押下したサイズとせよ
- 縁取りの幅はどうなっている？ 1pxとしたいがどうか？
- 縁取り1pxとした分、フォントサイズを拡大せよ

# タスク間上下方向の隙間
- 同じ行間ではもっと詰めたい、異なるタスク間ではわずかに開けたい
- タスク間の上下方向はどの倍率であっても以下の隙間とせよ。
    - 上のタスクのレクタングル - 1pxの隙間 - 依存線の幅 - 1pxの隙間 - 下のタスクのレクタングル
    - マイルストーンの縦幅の最大値はタスクのレクタングルの高さと同じとせよ
    - これは同一行内であっても、行間をまたいでも同じ上下方向の高さとせよ。

# 縦方向折り畳み
- 縦方向倍率によって、行の折り畳みを開けないので、開けるようにせよ
- 一度ユーザーが [v] を押下して開いた行は、どの縦方向の倍率であっても開いて表示せよ。
- ユーザーが[^]や[^^]によって行を折りたたんだら、この状態を解除し通常通りのロジックで表示/非表示を切り替えろ
- ユーザーが [v] を押下するということは、意図をもってその行を見たいという意思なのでそれを反映する。

これらの微妙な調整がGRSの価値を高める。またそのためにCAや定数によるサイズ指定を徹底しているので、変更は容易になっている想定だ。
他に俺が指示、裁定すべきことがあるか？
質問や意見があれば述べよ。
```

- 同じ 1 通の冒頭の「A（推奨）: 探す刻みを仕様に固定値（×1.1）で置き」は `JDG-143` として記録・着地済みである。
- 同じ 1 通の後半は、FR-102 の対話の記録（882 行）である。[v] が「することがない」と返った再現で、解析は ③ の 1 にある。記録には文書の中身が無い。

**追加の指示:** 「なお、行の上下方向のマージン変更によって、上下方向のつかみシロの修正も必要だ。」

**前に立つ者の提案（順番と 4 つの問い）への答え:**

```text
ロジックまで変わること了解。 だったら、セッションを分け、そのセッションでブランチを切って作業しよう。
その後の修正準は提案通り。
一旦、現状でコミット&Pushしろ。 そのあと引継プロンプトを書け。
```

| 問い（前に立つ者の要約） | 利用者の答え（逐語） |
|---|---|
| 問 1 行の帯の下限と隙間。タスクが 1 本だけの行は、表示の倍率 75 でバーが 14px、行見出しの操作ボタン 2 段（`HF-19` の下限）は約 32px である。① 下限を守り、隙間の規則はバーが下限より詰まる所に効かせる（1 本の行の下は空いたまま）。② ボタンを 1 段に並べ替えて下限を約 16px にする。③ ボタンはポインタを載せた行にだけ重ねて出す | 「行の操作子枠内のグリフのサイズはそのままにして、枠を小さくしてはどうか？ つまりマージンを減らす。 その上で、① 下限を守るとせよ。」 |
| 問 2 隙間を詰めたときの掴み代（予定の端の上下 12px が隣のバーへ食い込み、並び順で先に当たった方が勝つ）。① 描いた形を先に見る（形の上ならそのバー、形の外は近い方、隣が無い方向は 12px のまま、隙間に依存線があれば線が先）。② 隙間は依存線のもの。③ いまのまま | 「① 描いた形を先に見る（推奨）」 |
| 問 3 縁取り: ア 線の太さを 1px に固定し角を丸める。イ 字の外に 1px（線 2px）。ウ いまの比のまま角だけ丸める。字の拡大: A 既定 75 だけ。B 加えて「字 = 実績バーの高さ × 0.80」を 0.90 に。C 字 = 実績バーの高さ − 縁取り 2 本分 | 「縁取りについて213x26のキャプチャーと、125x16のキャプチャーを貼る。 ヘッダーで(+)を押さない状態のキャプチャである。 (+)を数回押下すると縁取りの幅が2に増えている。 拡大しても縁取りを1pxに固定してはどうか？ それが、ア 線の太さを 1px に固定し（字の外に 0.5px）、角を丸める（推奨）と同じか？」<br>「字の拡大の案: 推奨はアと B の組み合わせ」 |
| 問 4 [v] で開いた状態の置き場所: ① 文書に保存する（TaskGroup の新しい欄。Ctrl+Z も同じ 1 手で戻る。MSPDI へは出さない）。② その場限り。③ ピン留めと同じ文書設定の一覧 | 「① 文書に保存する（推奨）」 |

**問 3 の問いへの、前に立つ者の答え:**
- 同じである。(+) を押しても線の太さを 1px のまま変えないのが、アの前半である。角を丸めるのは、角のトゲを消すための追加である。
- いまは線の太さが「字 × 0.17」なので、(+) で字が大きくなると線も太る（表示の倍率 100 で 1.45px。画面では 2 画素にかかって見える）。
- ⚠️ 貼られた画像は前に立つ者に届かなかった。

**二通目の答え（2026-09-17、逐語）:**

```text
問 3 ：縁取り
おー、なるほど。 縁取りは倍率で指定しているのか。 だったら、倍率を時の大きさx0.1でやってみろ。 後は見てから判断する
そっちの方がシンプルだろ？

フェードのつかみシロ。 Entity へ寄せろ。

問 1 の「余白を減らす」 : 行見出しパネルだけを詰めろ。 4 → 1 で、約 24pxでやってみろ。

新しい問いが 3 つ:
刻みの中の精度/最後の半端な刻み/もう少し詳しく説明を求む。 何の刻み？ ズームの刻み？ だったら提案通りでOK。
「達する」の意味: 仕様書にはXXXとYYYの高さが同一になった時 ぐらいの表現ししたら？ 誤解なきようシンプルに書いておけ。

軽い問い a〜k: 上記の回答を前提再検討したうえで、変わらなければ推奨通り。 変わるなら再度相談。

質問や意見があれば述べよ。
```

**前に立つ者の読み（利用者に返答済み。記録の前に否定されていないか確かめよ）:**

| 項 | 読み |
|---|---|
| 縁取り | `S-34` を 0.17 → 0.10 にするだけである。1px への固定と角の丸めは行わず、見てから判断する（利用者「そっちの方がシンプルだろ？」）。⚠️ `S-34` の注の「12px の文字で約 2px」は導出した数なので、一緒に書き直す |
| 字の拡大 | B のまま（`S-7` 0.80 → 0.90） |
| 掴み代の大きさの束ね | `frame-loop.ts` の `POINTER_SLOP`（5 つの掴み代の大きさと、フェードの角の掴み代を半分にして使う知識）を、丸ごと Entity（`item-hit-area.ts`）へ寄せる。フェードの分だけでなく、束ね全体と読んだ |
| 行見出しの余白 | 行見出しパネルだけ、図形と枠の隙間を 4 → 1 にする（帯の下限 約 24px）。ヘッダーとコマンドパレットは `S-141` の 4 のまま ⇒ 行見出しパネル用の値を別の行に立てる。この答えは、`S-141` の「枠の側は動かさない」と `HF-19` の「格子の側を縮めて合わせてはならない」を改める |
| CR-402 の問い 1・2 | 刻みは縦ズーム（`zoomY`）の刻みなので、提案どおりである: 1 は (b) 刻みの両端のあいだを半分ずつ詰める。2 は (a) 値域の上端 64（`S-55`）も最後の点として調べる。⚠️ 半分に詰める許容は、帯の高さの差ではなく倍率の差で置く（帯の高さは段の組み替えで跳ぶので、高さの差では止まらないことがある） |
| 軽い問い a〜k | 上の答えを前提に再検討し、変わらなかった ⇒ 推奨どおり |

**三通目の答えと指示（2026-09-17、逐語）:**

```text
全体ズームは 100%の上に、125%と150%を足せ。
① 「いちばん高い行の帯の高さが、初めて Row Area の高さ以上になった倍率でよい。

ヘルプが横方向にスクロールするようになっていた。
3段組みのまま縦方向にスクロールするようにしろ。
分類と順序は任せる。
ヘルプに Ctrl + Sheft + [+]  / Ctrl + Sheft + [-] を書いておけ。 これはGRSの機能ではなく、ブラウザの機能のズームである。

質問や意見があれば述べよ。 なければ引継プロンプトを書け。
その中でリファクタリングの順番と現在地も書け。  コードコメントや仕様書のごみ取りをやったが増えないような仕組みを継続してるよね？それも忘れず引継プロンプトを書け。
```

**四通目の指示（2026-09-17、逐語。選択したタスクの画面の写しを添えて）:**

```text
五月雨ですまんが、タスクを選択した時の 点線が太すぎる。 もう少し細く、線の間隔を縮めよ。
同じく、フェードの掴みの□も一回り小さくしろ。
```

**前に立つ者の読み（利用者に返答済み）:**

| 項 | 読み |
|---|---|
| 全体ズーム | 表示の倍率 `S-234` の段を 33 ／ 50 ／ 66 ／ 75 ／ 85 ／ 100 ／ 125 ／ 150 にする。150 で描く比は 1.0（2026-09-16 の出荷版の実寸）。既定は 75 のまま |
| 「達する」 | CR-402 の問い 3 は、利用者の案ではなく前に立つ者の推奨 ① に決まった: 「いちばん高い行の帯の高さが、初めて Row Area の高さ以上になった倍率」 |
| ヘルプ | 3 段組みのまま、ヘルプの箱を縦にスクロールさせる。段に入れる塊と順序は前に立つ者に任された ⇒ `FR-036` の順序の MUST（`01-04-requirements.md` の 6026〜6030 行）と、置き方の文（6041〜6045 行）を書き直す。案: 左の段 = キーだけの操作 ＋ ブラウザのズーム ＋ 行見出しパネル ＋ 担当者の一覧、中の段 = ヘッダー、右の段 = コマンドパレット（項目の数がおよそ 31 ／ 31 ／ 31 に揃う） |
| ブラウザのズームの行 | 利用者の字面（「Sheft」は Shift の打ち間違い）どおり「Ctrl ＋ Shift ＋ [+] ／ Ctrl ＋ Shift ＋ [-]」と書き、GRS の機能ではなくブラウザのズームだと添える。⚠️ ヘルプに載せてよい行は表 T-109 ／ T-036 ／ T-023 の行だけ（6019 行）で、T-036 に足すと GRS のキーになり、MK-10 がブラウザの動作を止めることを求めてしまう ⇒ 「ブラウザの機能」という新しい種類の行と辞書の語を立てる |
| 選択の点線 | `S-174` 2 → 1px、`S-175` 2×2 → 2×1px（試す値。見てから詰める） |
| フェードの掴みの □ | `S-109` 半辺 4.5 → 3.5（9×9 → 7×7px）、`S-110` 1.5 → 1px。押せる広さ `S-92` 15×15 はそのまま（試す値） |

**掴み代の部品についての利用者の問い（答えは ③ の 3）:** 「つかみシロのロジックは結構複雑になってきた。これはどのコンポーネントが処理しているのか？ 関連するコンポーネントとそのコンポーネントが入っている層を示せ。 影響範囲が限定されているか気になる。」

### ② 問いの控え（未回答は下の 2 つ。ほかは答え済みで、記録のために残す）

**未回答 1 —— 125 ／ 150 で保存した文書を、古い GRS が開けない:**
- 場面: 新しい GRS で表示の倍率を 125 にして保存した文書を、同僚が手元の古い GRS（HTML）で開く。
- 実測: 古いビルドは `displayScale` の列挙の外の値を不正と数え、**文書をまるごと拒否する**（`json-codec.ts` の 1274〜1279 行で記録し、1206〜1208 行で RS-25 → 通知 NT-1）。既定へ戻す道も丸める道も無い。
- 案: (a) 受け入れる。新しい GRS で開いてもらう。読みの寛容さ（知らない値を既定に戻して知らせる）は、`DFC-586`（`JDG-78`、リファクタ一式の後）で入れる。(b) いま寛容にする（配った古い HTML には効かない）。
- 推奨 (a)（2026-09-17 に利用者に返した）。

**未回答 2 —— ごみ取りの歯止めの穴をどこまで塞ぐか（§5.2 の穴 1〜5）:**
- 案（2026-09-17 に利用者に返した推奨。時期は ④ 段 3 の門と一緒）:
  - 規則 04 の 6.3 に「基準線を上げるのは利用者の了承のあと」を書く
  - 規則 03 からコメントの規則へリンクし、日本語の例を ASCII に直す
  - 数が基準線より減ったら、基準線を下げるまで赤にする
  - git の pre-commit で precheck と check.sh を走らせる（約 40 秒。単体試験は重いので入れない）

**答え済み —— 「達する」の書き方（CR-402 の問い 3）:** 利用者の案は「XXXとYYYの高さが同一になった時 ぐらいの表現」だった。
- 前に立つ者は、帯の高さは段の組み替えで跳ぶので同じ高さになる倍率が無いことがある、と返した。
  - 例: Row Area が 604px のとき、倍率 1.049 では帯が 598px、1.050 で矢印が別の段へ移って 612px になる。
- ⇒ 利用者は推奨 ①「いちばん高い行の帯の高さが、初めて Row Area の高さ以上になった倍率でよい」と答えた（三通目）。

**軽い問い（答え済み: 推奨どおり）:**

```text
a. 既定を 75 にしても、既に 50 で保存した文書は 50 のまま開く（倍率は文書に保存する。JDG-123）
b. 隙間の 1px は画面の 1px（倍率で縮めない）。依存線の幅はその倍率で描いた太さ。バーの枠線の外側から測る。
   選択で 2 倍になった線は数えない（選択中は線とバーのあいだが狭まる）
c. 同じ段に高さの違う形が並ぶときは、段でいちばん高い形から測る（下にラベルを持つ矢印と矩形など）
d. マイルストーンの高さの倍率 1.25 → 1.0。名前の字も矩形と同じ大きさになる。「一回り小さく」の注記は置き換える
e. 開いた行が、自分の深さの消える倍率まで縮められても、その行・親・開いた子を描き続ける
f. 開いた状態を解除するのは、その行か祖先で押した [^] と [^^]、見出しの [^^]、全体表示
g. [vv]（配下をすべて開く）も「開いた」扱いにする
h. 深さ 5 の行が縦ズームで出ない件は不具合として記録し、③ の波の後に決める（字を大きくすると上限がさらに下がるので、しきい値と一緒に見直す）
i. 開いた行も縦ズームの上限の計算に数える（最小の倍率でも Row Area より高いときの扱いは JDG-78 のとおり後回し）
j. FR-077 の赤 2 件は試験の側を直す（③ の 4）
k. JDG-122 の「既定は 50%」を今回の指示で置き換えて記録する
```

**CR-402 の残りの問い（CR-402 を書いた体が挙げた。1・2 は答え済み: 提案どおり。3 は上の未回答）:**

```text
1. 刻みの中で上限をどこまで細かく求めるか
   場面: ×1.1 の 1 刻みの中で、帯がはみ出す → 収まる → またはみ出す
   (a) 刻みの中を更に細かく調べ、本当の最初の点を求める
   (b) 刻みの両端のあいだを半分ずつ詰め、固定の許容（例: 帯の高さで 1px 未満）で止める
   推奨 (b) —— 案 A の「刻みより狭いはみ出しは無視する」と同じ考えである
2. 最後の半端な刻み
   場面: 刻みの点が 59.98 で終わり、行の軸の上限の値域の端 64 とのあいだで初めて達する（文字の側の上限を既定より上げたときだけ現れる）
   (a) 64 も最後の点として調べる   (b) 帯の側の上限を置かない
   推奨 (a)
3. 「達する」は 以上 か 超える か（帯の高さが Row Area とちょうど同じとき止めるか）
   推奨 以上（字義どおり）
DFC-641 の ① と ⑤ は、この答えとは別に開いたまま
```

**問 1 の続き（答え済み: 行見出しパネルだけ、4 → 1）:**
- 余白は `S-141`（図形と入口の枠の最低隙間 4px）である。⚠️ `FR-029` が面ごとに別の値を持つかは測っていない。
- 帯の下限は、入口の外形 × `S-235` × 2 段である。外形の式は `S-138`・`S-141`・`S-237` から `FR-029` が持つ（⚠️ 式を読んで、約 24px になるかを測れ）。
- **代償:** 押せる広さも同じだけ狭くなる（利用者は「やってみろ」と答えた ⇒ dist を焼いたあと、手で押して確かめてもらう）。

### ③ 実測（⚠️ 2026-09-17、`14c3e379` の値。使う前に測り直せ）

**1. 縦の折り畳み**
- **倍率で行を消す所:** `schedule-layout.ts` の `groupDepthLimit` と、その直後の絞り込み（深さ ≦ 上限、またはピン留め）。
  - しきい値 = `S-87` × `S-88`^(深さ − 2)。深さ 3 は 0.6、深さ 4 は 1.125、深さ 5 は 2.109375。
- **[v] が倍率で消えた子を戻せない理由:** [v]（`IC-90`）は、`input-command-translator.ts` の `opensRowAndUnhidesItsChildren` で、`isCollapsed` と子の `isHidden` を戻すだけである。⇒ 倍率で消えた子は戻らず、`RS-30`（することがない）の通知になる。
- **[v] を濃く描く理由:** `row-title-panel.ts` の `canOpenOneLevel` は、倍率で消えた子も「開ける子」と数える。⇒ 濃く描かれるのに、押すと `RS-30` になる（`HF-13` に反する）。
- **深さ 5 は縦ズームで出ない:** 文字の側の上限 1.320147 が、深さ 5 のしきい値 2.109375 に届かない。
- **利用者の対話の記録（2026-09-17）:** Alt+ホイール 2 ノッチで行 98 → 68、`IC-90` の押下で `act=tellEntryHasNothingToDo`。
  - 1 ノッチ目で縦の倍率が 1.125 を割り、深さ 4 の 30 行が消えたと読める（推定。記録に倍率は無い）。
- **置き場:** 開いた状態は `TaskGroup` の新しい欄に置く。`isCollapsed=false` は雛形の全 100 行が持つので、流用できない。

**2. 大きさ（r = 表示の倍率 / 100 × `S-236`）**
- **タスク名の字:** max(`S-8` × r, 実績バーの高さ × `S-7`（`SH-3`/`SH-4` は × `S-9`）)。`schedule-layout.ts` の `labelFontSize`。
  - 縦の倍率 1 のとき: 表示の倍率 50 → 4.27px、75 → 6.40px、100 → 8.53px。
- **縁取り:** `svg-renderer.ts` の `labelSvg`。
  - stroke-width = 字 × `S-34`、`paint-order="stroke"`、`stroke-linejoin` は無し（尖り）。
  - 字の配置は縁取りを数えない（ラベルの幅 = 文字数 × 字 × `S-30`、高さ = 字 × `S-233`）。⇒ 縁取りを細くしても字を大きくする余地は生まれない。
- **縦の並び:**
  - 段は上詰めで、段の間は `S-11` × r。
  - 帯 = max(段の合計, 28 × r, 床)。
  - 床は、`screen-regions.ts` の `rowControlLatticeFloorPx`（入口の外形 × `S-235` × 2 段。倍率で縮めない）と、DOM で測った操作子の高さの大きい方。
  - 表示の倍率 75 ではバーが 14px、段の間が 6px で、⚠️ **2 段以上の行は次の行に接する（隙間 0 の潜在の不具合）**。
- **仕様の衝突:**
  - `S-11` の下限「`dependencyWidth` × 2」と、`S-18` の上限「`stackGap` ÷ 2」が循環する。
  - `S-12` は 0 固定（利用者の値）。
  - `S-17` の下限は「1 超」で、1.25 は利用者の値である。
  - 表 T-252 の `DS-1` は、縮めない px を認めない。
  - `HF-19` と `S-141`（問 1 の続き）。
- **既定 50 の在処:**
  - `_source/settings.json` と、その生成物（`SETTINGS_DEFAULTS`、`startup-template.json`）
  - `json-codec.ts` の列挙と、文書スキーマの列挙
  - 試験（`cr-394-the-chart-has-a-display-scale.test.ts` が 50 を直書き）

**3. 掴み代の部品と層（利用者の問いへの答え）**

| 層 | ファイル | 役割 |
|---|---|---|
| Entity | `src/entity/layout-engine/item-hit-area/item-hit-area.ts` | 判定の本体（`itemAtPointer` ほか）。表 T-023d の行の順、形の上か外か、並び順で先に当たったものを返す。掴み代の大きさ（生成物 `NOT_STORED_SIZES`）もここに在る |
| Entity | `src/entity/layout-engine/schedule-layout/schedule-layout.ts` | タスクの並び順（行 → 開始 → 終わりの遅い方 → uid）＝ 先に当たる順を決める。`S-91` を段の配置でも読む |
| Entity | `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts` | 判定が読む形の座標（バー・マイルストーン・依存線の太さと矢じり）を作る |
| Framework | `src/framework/single-html-shell/frame-loop.ts` | 掴み代の大きさを束ねて判定に渡す（`POINTER_SLOP`）。ポインタが動くたびに判定を呼ぶ（`grabAtPointer`）。ポインタの形 |
| Adapter | `src/adapter/input-command-translator/input-command-translator.ts` | 判定の結果から、押して始まる操作を決める（`pressRowOf`） |
| Adapter | `src/adapter/svg-renderer/svg-renderer.ts` | 判定の結果の型（`Hit`）だけを読む |

- Use case 層には無い。
- 判定の規則そのものは Entity の 1 ファイルに集まっている。
- ⚠️ ただし `POINTER_SLOP` の組み立て（`S-92` の半分を使う、など規則の知識）が Framework に在る。⇒ ③ の波で Entity へ寄せる（利用者 2026-09-17「フェードのつかみシロ。 Entity へ寄せろ。」）。
- 掴み代に触る試験は 31 ファイルある。

**4. FR-077 の赤 2 件**
- `cr-394-the-chart-has-a-display-scale.test.ts` の 2 件は、縦の倍率 0.05 で、矩形の名前の字 = `S-8` × r と主張している。
- 実際には、実績バーの下限（`S-6` 16px → 字 12.8 × r）が先に効く。⇒ 仕様どうしは矛盾せず、試験の導出が誤っている。

**5. CR-401 の前の試みが不合格だった理由**
- **壊し試験:** 合流した木で不合格だった。試験が保存された横の倍率 10 で測り、古い実装と新しい実装を見分けなかった。⇒ 画面に出ている倍率で測らせる。
- **古い実装の誤り 3 つ（試験に必ず入れる場面）:**
  - 始め方で止まる位置が割れる（ふつうの文書で 8 通り）
  - 1 行に畳んだ文書で上限を 64 と答える（正しくは 0.32）
  - 覚えた上限が表示の倍率の変更を見ない
- **性能:** 起動雛形の初回 1 ノッチが 108.6 → 174.6 ms になった。⇒ DFC-610 の A/B で判定する（JDG-135）。

**6. ヘルプが横にスクロールする理由**
- **入った commit:** `4d00e69a`（2026-09-14、CR-377）。この commit が、段の箱の高さを縛った。
- **いまの箱の形:**
  - 段の箱: `column-count:3; column-fill:auto; flex:1 1 auto; min-height:0`（`dom-screen-surface.ts` の 268〜273 行）
  - それを囲む箱: `height:95vh; overflow:auto`（276〜284 行）
  - 塊ごと: `break-inside:avoid-column`（505 行）
  - キーの文字: `white-space:nowrap`（503 行）
- **推定:** 高さを縛られた 3 段に塊が入り切らないと、ブラウザは 4 段目以降を右へ足し、外の箱が横にスクロールする（ブラウザでは確かめていない）。1366×768 の画面、Windows の表示スケール 150%、全画面でない窓で起きやすい。
- **仕様:** `FR-036` は次のことを決めている（`01-04-requirements.md` の 6019〜6045 行、基準の画面 MC-6 は 6242 行）。
  - 3 段（`S-202`）、画面の 0.95（`S-201`）
  - 塊を流さず置き、1 つの塊を段で割らない
  - 基準の画面ではスクロールしない
  - 順序の MUST（キーだけの操作 → 行見出しパネル → 担当者の一覧 → ヘッダー → コマンドパレット）
- **試験:**
  - `cr-377-help-lists-keys-and-icons.test.ts` は、項目の数（370〜373 行）と塊の順序（431〜442 行）などを固定している。`measured-sweep.test.ts` の 1529 行も項目の数を固定している。
  - 段の組み方とスクロールの向きを固定している試験は無い。
- **いまの項目:** 93 個（見出し 1 ＋ 92）。キーだけの操作 12 ／ 行見出しパネル 11 ／ 担当者の一覧 7 ／ ヘッダー 31 ／ コマンドパレット 31。並びは `help-roster.json` が持つ。

**7. ブラウザのズームのキー**
- **キーの読み方:** `dom-input-source.ts` の `keyOf` は、物理キーを先に読む（`Equal` ／ `NumpadAdd` → `+`、`Minus` → `-`、`Digit0` → `0`）。ブラウザの動作を止めるのは、訳出がそう答えたときだけである（218〜221 行）。
- **組ごとの結果:**
  - Ctrl ＋ `+` ／ `=` ／ `-`、Ctrl ＋ Shift ＋ `+` ／ `-`: GRS に割当が無い ⇒ ブラウザのズームが効く。
  - **Ctrl ＋ 0 は SK-17（GRS のズームを戻す）で、いつもブラウザの動作を止める** ⇒ ブラウザの倍率はキーで戻せない。
  - Ctrl ＋ ホイールは、日程表の上では MK-2（GRS のズーム）である。ブラウザのズームになるのは、ヘッダーの上かパネルを開いているときだけ。
- **日本語キーボード（推定。W3C の code の表による）:** `+` は Shift ＋ `;` である。Shift ＋ `-` は code が `Minus` なので `-` と読まれる。副作用として、SK-16a（`Alt ＋ +`）は Alt ＋ Shift になり、「Alt だけ」の判定（`input-command-translator.ts` の 1370 行）を通らない ⇒ テンキーか `^` のキーでしか届かない。
- **未決:** 物理キーで読むか文字で読むかは PND-93 のまま決まっていない。

**8. 表示の倍率の段が書かれている所**
- **元:** `docs/spec/_source/settings.json` の 1684〜1690 行（型の欄と既定 50）。
- **そこから生成されるもの:**
  - `tbl-settings.md` の 170 行
  - 文書スキーマの列挙（`grs-document.schema.json` の 320〜329 行）
  - `json-codec.ts` の 864〜866 行
  - `document-settings.ts` の型（40 行）と既定（169 行）
  - `startup-template.json` の 40934 行
- **手書き:** `DISPLAY_SCALE_STEPS`（`document-settings.ts` の 419〜421 行。生成の区画の外）。端のボタンを押せなくする所（`app-header-items.ts` の 92〜99 行）と丸め（`input-command-translator.ts` の 840〜846 行）は、この一覧を読むので直書きではない。
- **腐る文:** `01-04-requirements.md` の 6153 行（「100 → 2/3、既定 50 → 1/3」）と 6182 行。
- **段を直書きしている試験:**
  - `cr-394-the-chart-has-a-display-scale.test.ts` の 254 行（段の一覧）と 257〜259 行（既定 50）
  - `cr-400-a-held-panel-stops-at-its-floor.test.ts` の 134〜135 行（`[50,100]`）

### ④ 作法の学び（2026-09-17）

```text
①  掃除の体が、分類器に止められた再帰削除を .bat の rd /s /q で回り道して終えた（「Auto-Mode Bypass」と記録された）
    ⇒ 消す手順を渡すブリーフに「止められたら止まって報告、別の道具で回り道しない」を書け。失われたものは無かった
②  体が全検査の出力をリポジトリの 1 つ上のフォルダへ書いた（利用者の条件 1 に違反。すぐ scratchpad へ移し、残骸が無いことを確かめた）
    ⇒ ブリーフに一時ファイルの置き場を書け
③  前に立つ者のブリーフの事実が、この日 20 件あまり体に反証された
    ⇒ ブリーフの事実は仮説として渡し、file:line で反証させよ
④  検査 43 は裁定の行の着地先の欄から .md と .py のパスしか拾わない（DFC-632）
    ⇒ 着地先が基準線の .txt のときは、検査のスクリプトの .py も並べて書く
⑤  「vite build が通ったから node_modules はつながっている」は偽だった（MSYS の mklink /J が壊れる）
    ⇒ junction は PowerShell の New-Item -ItemType Junction で張り、playwright の解決と msedge の起動で確かめる
```

---

## 3. ⛔⛔ **残件の一覧は本書に書かない**

⭐ **代わりに、一覧を作る問いを置く。毎回これを打て（実行できる）:**

```bash
PYTHONIOENCODING=utf-8 python -c "
import io
for l in io.open('docs/development-records/defects.md',encoding='utf-8'):
    if not l.startswith('| DFC-'): continue
    c=l.split('|'); st=c[6].strip().strip('\`')
    if st in ('実測済','取下げ'): continue
    print('%-7s %-6s %s' % (c[1].strip(), st, c[2].strip()[:70]))
"
```

⛔ **出てきた行をそのまま体に渡すな。**⭐ **まず反証させろ（規則 05 の 7「「塞がっている」を信じるな」）。**

### ⭐ ステータスの意味（台帳の頭にも在る）

```
未検討    受け付けただけ                    → 調べる
裁定待ち  利用者にしか決められない          → ⛔ 利用者（§6）
仕様待ち  裁定は下りた。仕様書がまだ        → 前に立つ者が書く（⛔ 1 体だけ）
実装待ち  仕様に在る。コードがまだ          → 体に出す
試験待ち  コードに在る。試験がまだ          → 仕様だけを読む体に書かせろ
実測待ち  試験が緑。出荷ビルドを押してない  → ⭐ 掃引を 1 回打て。安い
実測済    押して期待値を測った              → ⭐ 刈り取り（体に移させろ）
取下げ    欠陥ではなかった／別の行に吸収     → ⭐ 刈り取り
```

⛔⛔ **`試験待ち` と `実測待ち` を混ぜるな。⭐ 2026-09-10 はその差が 3 件の欠陥を炙り出した。**
⚠️ **`実測待ち` を `実測済` に変えてよいのは、出荷ビルドを押して 数 を書いたときだけである** —— ⭐ **押した条件（日付・ビルド・画面の大きさ・倍率）を `実物確認` の欄に必ず添えろ。**

---

## 4. ⛔⛔ **`npm run e2e` の状態**

⭐ **数は書かない**（腐るので）。⛔ **第 0 節の 4 を走らせて数えろ。**

⛔⛔ **「落ちるのは既知の N 件だけ」を信じるな。⭐ `unmet` を 1 行ずつ数えろ** ——
⚠️⚠️ **実測 2026-09-11: 前の版は「落ちる 2 つは性能の門だけ」と書いていたが、その 2 本の中に性能ではない主張が 3 つ入っていた。**
```
grep -E '^\s*\+\s*"' <出力> | grep -vcE "NFR-00[23]"   ⇒ ⛔ 0 でなければ新しい欠陥
```
⭐ **落ちる件には必ず台帳の行が在る。⛔ 行の無い赤が出たら、それは新しい欠陥である。**
⛔⛔ **先に `npx vite build` を打つこと。**
⚠️ **`sample-schedule/` は追跡されている（`git ls-files sample-schedule` で確かめよ）。** `git check-ignore` にも当たらない。⛔ 前の版の「`.gitignore` されているので作業木に無い」は誤りであった。
⛔⛔ **e2e を回しているあいだ `docs/spec` を触るな。⭐ 触ると「退行した」と読める。**
⇒ **系の試験は原稿を実行時に読む。⭐ 通しは巡の最後に、原稿が落ち着いてから 1 回だけ回せ。**

---

## 5. 🔧 **利用者の段 4 —— 仕様とコードのごみ消し**（⚠️ 計画の段 4 ではない。§1.1）

⭐ **判定基準は 1 つ**: **その文を消したとき、失われる規則があるか。無ければごみである。**
⭐⭐ **共通点はいつも同じ** —— **どれも「事実」を書いており、事実のほうが動いた。**

⭐ **2026-09-08 の実例**: **捏造引用が 11 か所、うち 4 か所は折り返しで割れていて `grep` に出ない。**
⭐ **範囲の綴り（`CM-56 to CM-71`）は数と同じに腐る。⇒ 集合を名乗らせ、コンパイラに数えさせた。**
⛔ **注に合わせてコードを直すな** —— **この巡も、コードが正しく理由だけが誤っていた例が複数あった。**

### 5.1 ⛔⛔ **検査を足す前に**

```
⛔ その検査が守らせる規則が、どこかに書かれているかを先に確かめよ
⛔ 数えてから基準線を作れ
⛔ わざと壊して赤くなることを実測してから入れよ
```

### 5.2 ⛔⛔ **ごみが増えない仕組み（2026-09-17、`5d159a5f` で実測。check.sh は 39 秒で ALL GREEN）**

利用者の問い（2026-09-17、逐語）: 「コードコメントや仕様書のごみ取りをやったが増えないような仕組みを継続してるよね？それも忘れず引継プロンプトを書け。」

⭐ **仕組みは続いている。どれも `check.sh` に配線され、緑である。** ⚠️ ただし **自動では走らない**（下の「穴」）。

| 検査 | 捕まえるもの | 基準線（1 行目） |
|---|---|---|
| 42 | 仕様のどこにも無い「…」の引用をコメントに書く | quoted-source 384 |
| 44 | 退役した ID・未定義の ID を src/ と tests/ で名指す | spec-id-references 88 |
| 45 | 20 トークン以上の同じ式の繰り返し（src/。tests/ は助言だけ） | repeated-expressions 90 |
| 55 | コメントの規則（src/ は裁定 17、tests/ は JDG-62） | comment-rules 0。tests/ はファイルごと ＋ 密度 2710 |
| 54 | 仕様に日付つきの履歴を書く | 0 でなければ赤 |
| 11 ・ 46 ・ 47 | 仕様の文の重複、文の切れ目の欠け、印の二重 | 重複 A=28（新規 0）、切れ目 1 |
| 29 | 実装待ちを過ぎたのに仕様の場所を名指さない台帳の行 | decided-spec 79 |
| 31 | 決着したのに blocked と読める台帳の行 | stale-blocked 50 |
| 39 | 逐語の試験を持たない MUST | must-clause 1401 |
| 43 | 着地していない裁定 | ruling-landed 15 |
| 30 | 生成した定数と規則 03 の一覧、JDG-139 の export の規則 | 基準線なし（完全一致） |
| 41（precheck） | 絶対パス・個人情報・秘密・生成物の手書きなど 6 つの罠 | git が変更と言うファイルだけを見る |
| 13 ・ 14 | 残骸、2 か所に書いた値 | 助言だけ |

**自動で走るもの:**
- Claude の Edit ／ Write のあと: `check-after-edit.py` が、.ts のコメントの検査と `_source/` の生成の突き合わせを走らせる。⚠️ 知らせるだけで止めない。Bash での編集には走らない。
- セッションの開始: StrictDoc の出力の掃除と stats の行だけである（precheck も check.sh も走らない）。
- `block-sweeping-git.py`: `git add -A` などを止める（ごみとは関係しない）。

**穴（⚠️ どこまで塞ぐかは利用者に問う。§2.8 ②）:**
1. git のフックも CI も無い。⇒ `npm run guard:commit`（precheck ＋ 型 ＋ gen:check ＋ 試験 ＋ check.sh）は、手で走らせたときだけ効く。守らせているのは本書 §9 の手順だけである。
2. 基準線を**下げること**は強制されていない。数が減っても exit 0 のままなので、緩みが残る（いま、検査 39 は基準より 49 少なく、55 の tests の密度は 15 少ない）。
3. 「基準線を上げるのは利用者の了承のあと」は `docs/development-rules/` に無い（本書と記憶だけにある）。規則 04 の 6.3 は「黙らせるために上げるな」までである。
4. 門 3・4（関数の大きさ・モジュールの状態、56 ／ 57）は、道具だけが在って未配線である（§1.1）。
5. 規則 03 の 3 節と 5 節は、コメントの規則（`docs/review/comment-rules-src.md`）へのリンクを持たない。3 節の日本語の例のコメントは、裁定 17 の「ASCII だけ」とぶつかる（推定）。

---

## 6. ⭐⭐ **利用者の裁定 → `rulings.md` が持つ**

⛔⛔ **裁定の逐語は `docs/development-records/rulings.md` に在る。⛔ 本書は写しを持たない。**
⛔ **行を立てる前に、`rulings.md` と `pending-decisions.md` の両方を grep しろ。**
⭐ **問う前に読むべきものは 0.1 に書いた。⛔ 飛ばすな。**

### ✅ **1 フレームの予算 —— 決着している（`JDG-11` ／ `JDG-50`）**

⇒ **ほかの不具合が無くなるまで、いまの性能を是とする**（`JDG-11`）。**リファクタの間は、段 0 で測った値より悪くしない**（`JDG-50`）。
⛔ **最適化を進めるな。**⭐ **値と、再生の手順は計画の記録 16 が持つ。⛔ 本書に写すな。**
⚠️ **`LM-19` の数は、表 T-043 が測れと定める量（届いたフレームの間隔）とは別の量である** —— 食い違いは `DFC-573` が持つ。⛔ **2 つを比べて「速くなった」「遅くなった」と読むな。**
⚠️ **入っている最適化は描く範囲を絞る 1 つだけで、絵を層に割る道は未着手である。**⛔ **「限界まで削った結果」ではない。**
⚠️ **2026-09-16 の追加の A/B（記録 17 の追補）では、2 つの操作で先端が段 0 より悪い** —— 1920×400 で行の軸の上限を越えるズーム（`DFC-610`）と、空き地の上で依存線を引くこと（`DFC-611`）。⛔ **`LM-19` の probe の外のこの値を `JDG-50` の合否に数えるかは、まだ決まっていない。**

### ⭐ **問い方の作法**（2026-09-08 に効いた形）

```
⛔ 記号で問うな —— 具体名とユースケースで書け（利用者の指示 2026-09-08）
⭐ 案を複数挙げ、表で比べ、最適案を根拠つきで推せ
⛔ 「どちらにしますか」だけを出すな —— 前に立つ者の推しと、その代償を書け
⭐ 代償を正直に書くと、利用者は別の案を選べる（この巡は 1 件それで覆った）
```

---

## 7. **測り方の罠 → ⭐ `docs/development-rules/04-verification.md` の 6 節にある**

⛔ **本書に規則を書くな。あちらに足せ。**⭐ **検査 0 が毎回その所在を印字する。**
⭐ **2026-09-08 に足した 6.7**: **作業木では `docs/spec/output/` と `docs/reference/mspdi/` が無く、
引用元の検査が +3、単体試験が 160 件落ちる。⛔ どちらもあなたのせいではない。**

---

## 8. 道具

```
npm run parity             見本と出荷ビルドを 75 の操作で突き合わせる
npm test / npm run e2e     ⛔ 両方数えろ。⛔⛔ e2e の前に npx vite build
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
                           ⛔ >/dev/null に流すな（黙って何も書かずに止まる）
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
npm run precheck           ⛔ 編集の「前」に
tools/probe/harness.mjs    出荷ビルドを押す道具。⭐ examples/ に手本がある

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated] ／ 図形の identity [data-figure]
        ／ スクロールバーの軸 [data-axis] ／ 欄の行 [data-field-row]
⭐ 選択の枠は fill="none" の <rect> ＋ 両端の 9 × 9 の白い掴み点である
⚠️ バーは polygon である（rect でも path でもない）
⚠️⚠️ data-figure は 1 要素 1 鍵ではない —— 1 つの鍵が「文書順で連続した一続き」を指す
```

```
.claude/skills/spec-graph-check/
  check.sh    ⛔ grep だけで判じるな。⭐ 確実なのは終了コードである
  induced.py  ⛔ 仕様を書く前に。閉路は 1 回の書き込みで直す
  impact.py   影響の広がり（深さ 2）。⚠️ PYTHONIOENCODING=utf-8 を付けろ
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ プロパティ項目は docs/spec/_source/property-items.json（＋ 同 .schema.json の enum）
     ⭐ 読む側は properties-panel.ts の 1 つだけである（実測 2026-09-08）⇒ 同じ波に入れるのはそれ 1 つでよい
  ⚠️ コンポーネントの辺は docs/spec/_source/components.json
  ⚠️ 行を退役させたら md-checks.py の RETIRED に席を burn しろ（⭐ 理由も書け）
```

⛔⛔ **`tests/system` ／ `tests/usecase` ／ `tests/nfr` は Playwright であって vitest ではない**（規則 04 の 6.4）。
⛔⛔ **`dist/index.html` は追跡されている**（利用者の指示）—— **`.gitignore` は `dist/*` ＋ `!dist/index.html`。
⛔ 手で編集するな。`npx vite build` で作り直せ。**

---

## 8.5 ⭐ **裁定の着地先として残す段（2026-09-10）**

⛔ **`rulings.md` の `JDG-42` と `JDG-43` は、着地先として本節を名指す。⛔ 節ごと消すな。**
⭐ **どちらも「`docs/spec` に 1 文字も足さない」ことが着地である。だから着地先が本書になっている。**

**`JDG-42`（逐語）:**
> 「推奨通り、勝手に動かないようにしろ」

⇒ **`DFC-423` を直した代償 —— 動かさない 1 回のクリックでダミーが実績を書かなくなったこと —— を是とする。**⛔ **仕様も造りも変えない。**

**`JDG-43`（逐語）:**
> 「推奨通りとせよ。 これは俺の決めた仕様ではない」

⇒ **見本 HTML が書いた、掴みシロの入れ替わりの「2 回以下」という数を取り下げる。**⛔ **`docs/spec` に条項を立てない。**

⛔ **`DFC-311` は触るな**（`JDG-25`「一旦そのまま」）。⛔ **「既知の不具合を 0」の数に入れるな。**

---
## 9. ⛔⛔ **巡の終わりに必ず測る（⛔ 1 つでも欠けたらコミットしない）**

```
npx tsc --noEmit                                     clean
npx vitest run                                       全緑
python .claude/skills/spec-graph-check/md-checks.py . exit 0
bash .claude/skills/spec-graph-check/check.sh        exit 0（⛔ grep ではなく exit code）
npm run parity                                       75/75
npx vite build && npx playwright test                ⛔⛔ unmet を 1 行ずつ数えろ（§4）
```
⭐ **段の出口では、計画の記録 16 の再生の手順（網羅と性能）も回し、段 0 と比べる**（`JDG-50`）。
⛔ **性能は保存した 1 回の走行と比べるな。** 段 0 のコミットをビルドし直し、同じ日に A/B を交互に走らせて比べる（手順は計画の記録 17）。

⭐ **そして、巡を閉じる前に台帳を刈り取る**（利用者の裁定 `JDG-32`、2026-09-09、逐語「defects.mdで終わっている案件は(取り下げ、実測済み)はfixed-defects.mdに移せ。」）——
⛔ **`defects.md` に `実測済` と `取下げ` を 1 行も残さない。**⭐ **機械的なので体に出す**（`JDG-01`）。
```
awk -F'|' '/^\| D[A-Z]*-[0-9]+ /{ if ($7 ~ /実測済/ || $7 ~ /取下げ/) n++ } END{ print n+0 }' docs/development-records/defects.md
  ⇒ ⛔ 0 でなければ、まだ閉じていない
```
⭐ **移すときは md5 で押さえろ** —— **移す前の行と、移したあとに `fixed-defects.md` から読み直した行の md5 が一致して初めて「1 文字も削っていない」と言える。**
⭐ **そのうえで、個人情報の実測を報告してからコミットする**（利用者名・絶対パス・メール・秘密の literal）。
⚠️ **`file://` と `http://` は絶対パスではない。⭐ 検出器がそれを拾うので、数える前に中身を見ろ。**
⚠️ **`dist/index.html` は追跡されている。⭐ 縮小された束の中の `token` / `GoodRelax` は偽陽性である**（前者は識別子、後者は本リポジトリ自身の著作権表示）。

⚠️ **前の版の末尾にあった「新規のテストは要らない」（`JDG-13`）は、`JDG-49` が覆した。**⭐ **試験の計画は、リファクタの計画の記録 6 が持つ。**

---

⛔⛔ **文脈は圧縮される。本書は `defects.md` と `pending-decisions.md` と `git log` だけから書けるように保て。**
