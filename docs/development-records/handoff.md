# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             ⭐⭐ 赤い試験を疑う前にこれを走らせろ
3. npm test  かつ  npm run e2e                   ⛔⛔ 両方を数えろ（下の 0.5 節）
4. ハーネスがサブエージェントを禁じているかを確かめ、禁じているなら
   「規則 05 と衝突する。体を使ってよいか」を最初に問う
5. ⭐ 利用者が眠っているときの標準指示（2026-08-30 に受けた。逐語）——
   「裁定が不要な分を進めろ。修正が容易な分（定数の変更／1 モジュール内に
     収まる変更）は裁定を待たずに進めて、後でまとめて報告しろ」
```

---

## 0.5 ⭐⭐ **赤い試験は 0 である（⛔ 今度は e2e も数えた）**

```
単体   118 ファイル / 5406 件 —— 5405 緑 ／ 1 skip ／ ⭐ 赤 0
e2e    tests/system  17 件 —— ⭐ 全部緑
検査 29 本 ALL GREEN ／ typecheck 緑 ／ layers 緑（284/284）／ gen:check 17 本 OK
設定値 215 行（`S-213` を新設）／ tables 136 ／ figures 11 ／ uids 150 ／ 語 404 対
```

⛔⛔ **前巡の引継書の「赤い試験は 0」は単体しか数えていなかった** ——
**`npm run e2e` を走らせたら 2 件赤であった**（台帳 `D-165`）。
⭐ **原因は試験の側で、直した。**⛔ **次の巡も必ず両方走らせろ。**

---

## 0.6 ⭐⭐ **前巡の筆頭は片づいた —— 3 列の表が在る**

⭐ **利用者の指示（2026-08-31、逐語）**「引き継ぎ書にサンプルと仕様書と今の実装を
徹底的に比較して、サンプルに合わせて仕様書を書き、仕様書に合わせて実装するようにしろ。」

✅ **全数の突き合わせは書いた。⛔ 消すな。**

```
docs/review/sample-vs-spec-vs-implementation-2026-08-31.md
   A. パネルの頭  6 行   B. 行の描かれ方 12 行
   C. 折り畳み    7 行   D. 掴んで動かす 10 行   E. 足す／消す 5 行
```

⭐ **次に「サンプルと合っているか」を問われたら、測り直す前にここを読め。**
⛔ **1 行潰したら、その行の判定を書き換えること。**

### ⛔⛔ **この巡でいちばん効いた教訓 —— 見本も「実物」として測れ**

⭐ **前巡の台帳 `D-162` は、見本の側も実装の側も偽であった。**

| | 前巡が書いたこと | ⭐ 実測 |
|---|---|---|
| 見本の件数の場所 | 「名前の直後」 | ⛔ **行の右端**。`.nm{flex:1}` が名前を伸ばして押し出す |
| 実装の件数の場所 | 「行の右端に寄っている」 | ⛔ **右端から 124px 内側。名前の 2 文字目の上** |

⇒ ⭐⭐ **見本の CSS を読んで「順序」を語ると、`flex:1` を見落とす。**
⛔ **見本を Chromium で開いて `getBoundingClientRect` で測れ**（`scratch/probe/measure-sample.mjs` の形）。

---

## 1. ⭐⭐ **この巡で何をしたか**

### 1.1 ✅ 仕様へ入れた（CR-321、版 1.75）—— **見本が持ち、仕様が黙っていた 3 つの形**

| 何を | どこ |
|---|---|
| 件数の**置き場**（行の右端＝`HF-4` が操作子を留める端）と ⛔ 名前の途中の禁止 | `HF-18` |
| 件数の**字形**（`▾` U+25BE を数の前に）| `HF-18` |
| 掴み代の**字形**（`⋮⋮` U+22EE ×2）と ⛔ 図 F-019 の図形にしない | `HF-15` |
| 帯の**太さ** = `S-213`（3px。⭐ **2 本の帯で 1 つの値**）| `HF-15` ／ `HF-18` ／ 表 T-206 |

⛔ **実装が件数を名前の上に描いていた原因は 1 行である** ——
**`right` を `em` で書き、同じ宣言で `font-size:0.75em` を与えていた。**
⇒ **`em` はその要素自身の字で解かれるので、7 歩ぶんが 3/4 の距離になった。**

### 1.2 ✅ 反証した 2 件（**「無い」を疑って出た**）

| 台帳 | 前巡の記述 | ⭐ 実測 |
|---|---|---|
| `D-159` | 「`Task` を 2 つ選んだ状態を作れなかった」 | ⛔ **偽。`Shift` で 2 つ選べる**（枠が 2 つ描かれる）。**押下が `Task` に当たっていなかった** |
| `D-146` | 「整列の入口が効かない」 | ⛔ **偽。効く。**2 つ選んで `IC-37` を押すと 2 本とも `x=350` に揃う |

⛔⛔ **どちらも道具の使い方が原因である** —— **バーの場所を `getBoundingClientRect` から取っていた。**
⭐ **`tools/probe/harness.mjs` の覚書きが「SVG の x は画面の x ではない」と既に警告している。**
⭐ **正しい取り方**: `document.elementFromPoint` で盤面を掃き、`rect`/`polygon`/`path`/`circle` だけを拾い、
予定バーの塗り（実測 `rgb(33,80,140)`）で絞る。

### 1.3 ✅ 試験の名前（利用者の指示 `§9`）

```
tools/probe/  0 件（前巡で済み）
tests/        矢印の引数 273 → 79（194 件を改名。23 ファイル）
src/          27 件（手つかず）
```

---

## 2. ⛔⛔ **利用者の裁定が要るもの（9 件）**

⭐ **巡の頭に 9 つ並べて出せ。⛔ どれも他の仕事を止めていない。**

| ID | 問い | 推奨 |
|---|---|---|
| **`PD-413`** | ⛔⛔ **`∧∧`（配下をすべて畳む）が見本より 1 階層浅い** —— 押しても直下の子が描かれたまま残る（実測）| ⭐ **見本に合わせる。**⛔ **`HR-4` / `HF-11` / `HF-1` の MUST を書き換える。**⭐ **仕様の側に既に 2 つの矛盾がある**（下の 2.1）|
| **`PD-414`** | 留めた行の地を塗るか（見本は塗る）| 現状維持（塗らない）|
| **`PD-415`** | 頭の件数を語にするか（見本は「N 行を畳み込み中」）| 現状維持（`▾ N`）—— パネルは 170px しかない |
| **`PD-410`** | ピン止めした行を掴めないことを、描く側と読む側のどちらが負うか | 現状維持（`IF-9`）|
| **`PD-411`** | 表 T-233 の `RS-30` が到達不能になった | 場面を与え直すか、行を落とすか |
| **`PD-412`** | `HF-18` の射程が 2 通りに読める | ② その下の総数（実装済）|
| **`D-147`** | `IC-41` はパスワードをどこで問うのか | ⛔ 単なる切り替えにすると `FR-020` の MUST を破る |
| **`D-149`** | `IC-18` の「しまう」は在るのか | 直すと 表 T-064 の `PI-37` に触る |
| **`D-158`** | パレットの入口が押下状態を持てるか | ⚠️ 利用者が挙げた例（イナズマ線を緑に）がこの 1 つ |

### 2.1 ⭐⭐ **`PD-413` は仕様の自己矛盾でもある（⛔ ここを読ませてから問え）**

実測（出荷ビルド、2026-08-31）:

```
行の ∧∧（IC-77）を Mobile Client で押す
  ⇒ Phone App と Tablet App は描かれたまま。消えるのは孫から下
頭の ∧∧（IC-78）を押す
  ⇒ 描かれる行 0。段 0 ごと畳む
見本の ∧∧ を Mobile Client で押す
  ⇒ Mobile Client だけが残る（配下が残らず消える）
```

⛔ **仕様は実装のほうを書いている**（`HR-4`「配下をすべて閉じる」＋ `HF-11`「その行自身を畳んではならない」）。
⛔⛔ **ところが、それと食い違う 2 文が既に仕様に在る:**

1. `HR-2`「**本行と `HR-4` は同じ動作の範囲違いであり、頭に置いた入口が段 0 のそれである**」
   ⇒ ⛔ **頭は段 0 を畳む。行は畳まない。1 階層ずれている**
2. `HF-1`「**`HR-4` を 1 度押せば**（`HR-5` と）**同じ絵になることを実測しており**（版 1.67）」
   ⇒ ⛔ **今のビルドでは偽である**（`HR-5` は行自身を閉じるので直下の子が消える）

⭐ **見本に合わせると 2 文とも真になる。**⚠️ **`HF-11` の「その行自身を畳んではならない」は
「隠してはならない」（`HF-3` の職務）と読み直すことになる。**

---

## 3. ⭐ **仕様が決めていないと分かっているもの**（実装は暫定で埋めてある）

```
⛔ 掴んでいる行の地の色     HF-15 は「地を敷くこと（MUST）」だけ。実装は S-149、見本は強調色 12%
⛔ 抱えている帯の濃さ       太さ（S-213）は決めた。濃さの行は無い。見本は 70%、実装は 100%
⛔ 名前を打ちかけの行が 1 つだけであること   見本は守る。仕様に行が無い
⛔ 掴む足場が消えたときドラッグを終えること  同上
⛔ 入口の枠の太さ 1px       S-138 は図形の箱、S-141 は隙間。枠自身の太さを持つ行が無い
⛔ FR-098 の「帯が Row Area を埋め尽くしてはならない」の上限の数
```

---

## 4. ⭐ 現在地（**すべて実測**）

```
⚠️⚠️ push していない。⛔ この巡の 4 本が手元にある —— git push origin HEAD:main
⚠️ ブランチ名は restart のまま

⭐ 仕様へ入れたもの: CR-321（版 1.75）の 1 本
⭐ 残件 105 ／ 台帳 165 行 ／ テスト完了 56 ／ 追加裁定待ち 9 ／ 取下げ 4
   （巡の頭は 残件 107 ／ 162 行 ／ テスト完了 52 ／ 取下げ 3）
裁定待ち 184 行・うち未裁定 136（新: PD-413 / PD-414 / PD-415）

⭐ 作業樹は 1 つも作っていない。消すものは無い
```

---

## 5. ⭐⭐ この巡でいちばん効いた方法（**続けろ**）

```
⭐⭐⭐ 見本も実装も「実物」として測る。⛔ 原文（CSS / TS）を読むだけでは形が出ない。
    この巡は見本を測って前巡の記録が偽であることが分かった。2 度目である。

⭐⭐⭐ 「効かない」と書いてある行は、まず自分で押す。この巡 2 件とも偽であった。
    ⛔ 前巡が外した理由は製品ではなく道具であった（SVG の x ≠ 画面の x）。

⭐⭐ 数を 1 か所に持つ。2 本の帯は 2px と 3px に割れていた。
    ⛔ S-208 の覚書きが「同じ数が 2 か所に載ると必ず離れていく」と既に書いている。

⭐⭐ 試験を書いたら、期待値をわざと壊して赤くなることを確かめる（この巡も実行した）。
    件数を元へ戻し、▾ を外し、帯を 2px へ戻す ⇒ 11 件が赤。戻すと全部緑。

⭐ 生成ブロックの定数を、モジュール先頭の const で読むな（TDZ で落ちる）。
    関数にする。rowControlRightEm の覚書きが同じ罠を記録している。
```

---

## 6. ⛔⛔ この巡で前に立つ者が犯した誤り（**繰り返すな**）

```
⛔⛔ bash のヒアストリング（<<'PY'）に \uXXXX を書いたら実体化し、
   書き込みの途中で例外が出て settings.json が 0 バイトになった。
   ⭐ 直し方: ① Write ツールで .py を作ってから走らせる
             ② 書く前に encode() し、成功してからファイルを開く
   ⚠️ git が戻してくれた。⛔ 追跡されていないファイルなら失われていた。

⛔ 開発記録（docs/development-records/*.md）は「1 波 1 ファイル」であり、
   検査 26 が「**対象**: `src/…`」の宣言を求める。突き合わせの表はそこではなく
   docs/review/ に置く（日付つきの名前で）。

⛔ 台帳の行を書き換えるとき、セル境界の ` | ` を含む長い文字列で当てにいくと外す。
   ⭐ 直し方: 短くて一意な末尾（「… | ⛔ 未 |」）で当てる。
```

---

## 7. ⭐ 道具（**30 行の下ごしらえを二度と書くな**）

```
tools/probe/harness.mjs   ⭐ export 40。出荷ビルドを開いて実測する
⛔⛔ SVG の x は画面の x ではない —— ⭐ この巡、台帳 2 行が偽であった原因である。
   ⭐ 盤面の図形は document.elementFromPoint で掃いて拾え。
     rect / polygon / path / circle だけにする（text と BUTTON も返ってくる）。
⛔ shapes() は前巡 ReferenceError を投げた。polygon の points を自分で読め
⛔ rows() は y で並べ替える ⇒ DOM の並びとの食い違いは見えない。treeOrder() を使え
⚠️ 実測した行の高さは 64 / 96 / 101 / 108 / 148px とまちまち。決め打ちするな
⭐ 行の操作子は button[data-icon]、掴み代は [data-row-grab]、件数は [data-folded-rows]
⭐ 選択の枠は stroke が S-151 かつ stroke-dasharray が空でないもの

scratch/probe/ にこの巡の測定器が 8 本ある（⚠️ gitignore なので次の巡には無い）
   measure-sample.mjs      見本を実物として測る
   measure-row-panel.mjs   行の中身を全部出す
   measure-bands.mjs       2 本の帯を測る（ドラッグを含む）
   multiselect3.mjs        Shift で 2 つ選べるかを測る

npm run gen:check          ⭐⭐ 巡の頭で必ず走らせろ
npm run e2e                ⛔⛔ 巡の頭で必ず走らせろ（前巡は数えていなかった）
npm run precheck           ⚠️ 罠 3 は「変更された生成物」を見るだけで、
                           再生成したのか手で書いたのかを区別できない（誤検出する）
.claude/skills/spec-graph-check/
  induced.py   ⛔ CR を書く前に走らせる（この巡は GR-20 と HF-15 が閉路と出た）
  impact.py    ⭐ 波及を測る（HF-18 は要求 0 件、GR-20 は 2 件）
  check.sh     29 本。⭐ grep -n "^FAIL\|^PROBLEM\|^DRIFTED" が 1 行で当てる
  ⚠️ 表に行を足したら npm run words も要る
  ⚠️ 設定値を足したら tools/generate_entity_types.py の NOT_STORED_TARGETS にも要る
```

---

## 8. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔⛔ **利用者の裁定待ち**（2 節）| 9 | **0** | ⭐ **巡の頭に 9 つ並べて出せ。**⛔ **`PD-413` は仕様の自己矛盾を含むので最初に出せ** |
| 2 | ⛔ **赤い試験（単体 ＋ e2e）** | 0 | **0** | ⭐ **両方を数えろ** |
| 3 | **3 列の表の ⚠️ と ⛔** | 6 | **0** | ⭐ **裁定が下りた分から埋める。表は `docs/review/` に在る** |
| 4 | **`tests/` の単一文字の宣言** | 72 | **0** | ⚠️ **表で機械的に直すな** —— **`const a = Math.abs(rx)` と `const a = layoutFromSchedule(…)` は同じ名前にならない。1 か所ずつ読め** |
| 5 | **`src/` の短い名** | 27 | **0** | 利用者の指示 |
| 6 | **テスト完了 の行数** | 56 | **64** | ⭐ `D-146` は実物で確かめた。試験が付けば閉じる |

⚠️ **総件数が増えることは失敗ではない。**⭐ **状態を正して 残件 が増えるのも失敗ではない。**
⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
⭐⭐ **この巡は、偽と分かっていなかった行を 4 つ（`D-162` / `D-146` / `D-159` / `D-165`）測り直して正した。**
