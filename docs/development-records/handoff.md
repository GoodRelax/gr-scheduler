# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔ 3 つとも数えろ
4. §1 を読む
```

**サブエージェントは許可されている**（利用者、2026-08-31／09-02／09-03 に再確認）。
**利用者の指示: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

### **規則は `docs/development-rules/` にある。読まずに書くな**

**索引は同フォルダの `README.md`。⛔ 本書は規則を要約しない** —— **写すと必ず離れていく。**

⭐⭐ **前の巡の知見は、本書ではなく規則へ下ろした**（利用者の指示 2026-09-03
「ノウハウとその改善を次回や、その先に必ず残るように仕組化しろ」）。
⛔ **本書は毎巡書き換わるので、ここに置いた学びは 1 巡で消える。**⇒ **置き場は次のとおり:**

```
規則 04 の 3.5   労力は歩留まりに合わせて配る（単体試験の増産に上限）
規則 04 の 3.6   ⭐ 発見器は 1 つでは足りない ／ 一致しきった検査は問うていない検査である
規則 04 の 3.7   ⭐ 原因は 1 つとは限らない。原因ごとに戻して確かめろ
規則 04 の 3.8   ⭐ 道具は黙って壊れる。直したら check.sh に載せろ
規則 05 の 7     ⭐ 「塞がっている」を信じるな、反証しに行け
                 ⭐ 前に立つ者のブリーフは間違う。体に原典で訂正させろ
                 ⭐ 行は症状ではなく原因で立てる ／ 台帳の数は 5 つの束で読む
```

⭐ **検査に下ろしたもの**: **第 5 章の自己検算は 検査 33 として `check.sh` の中に入った** ——
**`CR-280` 以来ずっと赤だったのに、`check.sh` が走らせていなかったので誰も見ていなかった。**
⛔ **覚えていなければならない規則は、走らない規則である。**

---

## 0.5 **始点（前の巡の終わりに実測）**

```
単体   153 ファイル / 6523 件 —— 6522 緑 / 1 skip / 赤 0
e2e    tests/system  67 件全部緑・3.5 分（うち 28 件は 1 回の起動で回る掃引）
       ⚠️ PINNED に D-232 の錨が在る。⭐ 裁定が下りたので、直せば「思いがけず通った」と鳴る
同型   npm run parity —— 50/50 一致・説明の無い食い違い 0（44 清・6 は FR-098 の説明つき）
検査   check.sh 33 本 ALL GREEN ／ gen:check 17 本 OK ／ typecheck ／ layers 清潔
台帳   244 行。残件 17
```

**残件 17 の内訳:**

```
仕様待ち 14   ⛔ 裁定は全部下りている。書くだけである。§1
実装待ち  1   D-145（共変検査を他の表へ広げる）
未仕分け  2   D-237（深さ 3 で行が増えない。原因未診）／ D-244（利用者への説明の借り）
裁定待ち  0   ⭐ 2026-09-03 に 17 件すべて裁定が下りた
```

⭐⭐ **台帳の数は 5 つの束で読む**（`tools/ledger_metrics.py` が刷る）。
⛔ **「不具合」と呼んでよいのは `製品の仕事` だけである。**

---

## 1. **この巡の仕事 —— 裁定は全部下りている。書いて、実装して、測れ**

⛔⛔ **利用者の逐語は台帳の `対応方針・決定仕様` の欄に 1 件ずつ入っている。⭐ そこを読め。**
**本書は要約であり、食い違ったら台帳が正である。**

### 1.1 **⭐ まず 1 本の変更要求にまとめる（14 件が仕様待ち）**

⛔ **`induced.py` を先に 1 回走らせろ**（規則 02）。**閉路に入る対象を 2 つ以上触るなら 1 パスで書く。**

| 行 | 何を書くか |
|---|---|
| `D-231` | 確認の文から「取り消しでは戻せません」を落とす。⛔ **`FR-046` の「取り消せない削除の前に問う」も「配下ごと消える削除の前に問う」へ改める** |
| `D-195` | 透かしの 4 値（角度 **−30°** ／ 大きさ **絵の幅の 8%** ／ 間隔 **文字高の 3 倍** ／ 色）を 表 T-207・T-236 へ。⛔⛔ **値は生成した定数として持て。`src/` に数を打ち込むな**（利用者の逐語） |
| `D-154` | `S-79` を **170 → 200**（原稿は `_source/settings.json`）。⚠️ **併せて `HF-15` の「何を縮めるか」に 1 文** |
| `D-228` | `FR-001` に「**作ったタスクを選択にすること（MUST）**」。⭐ **`D-232` も同時に閉じる** |
| `D-219` `D-240` | 段の名前と**構成の表**。⛔ 構成は利用者の逐語（下の表）。**月の表示が意図と違う不具合も直す** |
| `D-241` | 日の段へ切り替わるしきい値を下げ、**1 画面幅に 2 か月を日で**。⭐ 曜日の幅は幅狭フォントか縮小で |
| `D-238` | 表 T-104 に 6 行。⛔ **先に逆向きの 8 件を見て、2 つの表の役割を確かめてから** |
| `D-239` | 項目名を英語に保つ**理由**を 表 T-016 の前文に書く |
| `D-15` | フェードの日数は**暦日**。⛔ `IV-12`（稼働日）のほうを直す |
| `D-124` | 起動時に読めなかった理由を 表 T-233 に 1 行＋辞書。⛔ **壊した日程を埋め込んだビルドで実際に通す** |
| `D-202` | ⭐ **取り下げ済**（`.html` の読み戻しは不要）。⛔ **席を退いた記録だけ残す** |
| `D-242` | **操作子の大きさは文字サイズに追随しない**（`HF-5`）。⛔ **前に立つ者の推奨は却下された** |
| `D-243` | 行を足すと **`No name` の行を作ってプロパティパネルを開く**。⛔ **前に立つ者の推奨は却下された** |

⛔⛔ **利用者が示した目盛の段の構成（逐語）:**

```
|倍率|1行目  |2行目      |3行目|
|----|-------|-----------|-----|
|年  |yyyy   | -         |     |
|月  |yyyy   |m          | -   |
|週  |yyyy-mm|d(月曜の日)| -   |
|日  |yyyy-mm|d          | 曜  |
```

### 1.2 **⛔ 前に立つ者の推奨が却下された 3 件 —— 提案と取り違えるな**

```
D-242  操作子の大きさ   推奨「格子の段で固める」→ 却下
       ⭐ 正: 大きさは押しやすさで決まり、読む字の大きさとは無関係
       ⭐ コマンドパレットのアイコンと同じ考え方に揃えること
D-243  行を足す操作感   推奨「いまのまま」→ 却下
       ⭐ 正: No name の行を作り、パネルを開いて名づけさせる（改名と同じ道）
状態の模型             推奨「継ぎ目済という状態を足す」→ 却下
       ⭐ 利用者の逐語:「決まってないのがボトルネックなら裁定待ち/仕様待ちのどちらかでしょ？」
       ⇒ ⛔ 状態を増やすな。詰まりの本体で分類しろ。既に 4 件ともそう分け直してある
```

### 1.3 **借りが 2 つ**

```
D-244  ⭐ 操作子の 2 × 2 の格子が何を意味するかを、利用者に解説する
       ⚠️ 「上下に読めば動作、左右に読めば範囲」（利用者自身の 2026-08-30 の裁定）が
         実物で成り立っているかを、画面の絵で示すこと
       ⛔ 説明して「やはり分からない」なら、それは並びのほうの欠陥である
D-237  ⛔ 深さ 3 の一部で、行を足す入口が打ち込み口を出すのに行ができない
       ⚠️ 深さ上限の件（D-206）とは別。⭐ 退行ではない（変更の前後で同じ）
       ⇒ 裁定ではなく調査が要る
```

---

## 2. **体の使い方**

⛔ **本節も規則の要約ではない。**⭐ **効き方の実測だけを置く。作法は規則 05 の 6・7 が持つ。**

### ⭐ **前の巡で効いた 4 つ（数つき）**

```
実装 ‖ 仕様だけを読む試験   偽の緑を 2 件、前に立つ者の偽の条文を 1 件掘り当てた
「落ちることを確かめろ」     反転 8 / 6 / 5 / 2 件を見せて戻させた。落ちない試験は数に入れない
4 体の並走（2 回）           報告が濁らなかった。効いたのは下の 3 つ
人として通しで押す           50/50・6009 件・32 本が全部緑の状態で 6 件出た
```

**並走が濁らなかった理由（⛔ 3 つとも指示に書け）:**

```
⛔ 「いま誰がどこを触っているか」を表で書く
⛔ 「木全体の色で判断するな。自分のファイルを名前で走らせろ」と書く
⛔ 報告を 40 行以内に収めさせる（前に立つ者の枠が保つ）
```

⛔ **`npm run gen` を 2 体に同時に走らせるな。**⛔ **`npm run build` も 1 体だけにしろ。**

### ⛔ **ブリーフに数を書いたら、この 1 文を必ず添えろ**

> **⛔ この数と在り処は前に立つ者が書いた。原典で確かめてから使え。違っていたら訂正して報告しろ。**

⚠️ **前の巡、前に立つ者は 8 つ間違え、8 つとも体が捕まえた。**⭐ **半分は数と在り処である。**
**訂正は成果である。**⛔ **受けたら台帳と条文の両方を直すこと。**

---

## 3. **順番**

```
① §1.1 の 14 件を 1 本の変更要求へまとめる。⛔ 先に induced.py
② 仕様へ当てる（⛔ 1 体で。並行に書けば壊れる）
③ 実装する体と、仕様だけを読む体を対で出す（規則 05 の 7、段 4）
④ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh
⑤ ⭐ 巡に 1 度は「人として通しで押す」（規則 04 の 3.6）
⑥ 巡の終わりに刈り取る。⛔ 数を書いていない行は移すな
```

⚠️ **`PINNED` の錨を忘れるな** —— `D-232` は裁定が下りたので、直れば
`tests/system/open-defect-pins.test.ts` が「思いがけず通った」と鳴る。**そこで錨を外す。**

---

## 4. **測り方の罠（実測で踏んだものだけ）**

```
⛔ 行の名前は切られる。textContent は切られた語を返す。全文が要るならパネルを広げろ（FR-052）
⛔ [data-depth] は「描かれている窓」であって文書ではない。名前で引き直せ。8 行で頭打ちになる
⛔ 行の操作子は、その行の名前にポインタが乗るまで隠れている
⛔ 外接矩形は clip-path を見ない。elementFromPoint で掃け
⛔ 二度押し（MK-13、表 T-023）は 1.5 秒空けろ。バーを押してすぐ引くと二度押しに取られる
⛔ 宿主のダイアログは addInitScript で差し替えろ。⚠️ 差し替えたら
   「取っ手を持ったあとの振る舞い」しか示せない。そう報告に書かせろ
⛔ 初回描画は「同じ読みが 2 回続くまで」待て。固定の待ちは冷間で外れる
⛔ 行の削除は確認の Yes を通る
⚠️ Task を持たない行の帯は 48px（2026-09-03 に床が入った。22〜28px は古い）
⚠️ 参照ブラウザの終了は 21〜163 秒かかる。afterAll で test.setTimeout(CLEARING_UP_MS) を呼べ
   ⛔ 忘れると「負荷の高いときだけ赤い」試験になる（7 ファイル中 5 つがそうなっていた）
```

### ⛔ **シェルに噛まれた 3 つの形（1 日で 3 度踏んだ）**

```
① ヒアドキュメント        バックスラッシュと引用符が壊れ、コマンドが返らなくなる
② 台帳のセルの縦棒        行が 1 セル増えて metrics が落ちる（`| 点 |` と正規表現の `\|`）
③ python -c "..." のバッククォート
   bash がコマンド置換として実行し、`FR-093` などの識別子が 4 つのセルから黙って消えた
   ⛔ 文はそのまま残るので気づきにくい
```

⇒ ⭐ **日本語や識別子を含む Python は、スクラッチパッドへファイルを書き、パスで走らせろ。**
⛔ **書いたあと必ず読み直して確かめろ。**

---

## 5. 道具

```
npm run parity             見本と出荷ビルドを 50 の操作で突き合わせる。⭐ いま 50/50
                           ⛔ 一致しきったら手を増やす番である（規則 04 の 3.6）
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    原稿を直したら gen、巡の頭で gen:check
npm run typecheck          tsc 2 本。⛔ npm test だけでは足りない
tools/probe/harness.mjs    出荷ビルドを実測する。⭐ openPanel() は二度押しに直った
                           ⚠️ signatureAt は半径内の DOM 順で最初を返す
tools/precheck.py          ⚠️ 生成物を直したあとは「直接編集した」と誤報する。
                           gen:check が緑ならそれが答えである

ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]
        ／ 切られた名前 [data-truncated]

.claude/skills/spec-graph-check/
  check.sh    33 本。⭐ 検査 33 が第 5 章の自己検算。検査 32 が禁止語
              grep -n "^FAIL\|^PROBLEM\|^DRIFTED\|FAILURES ABOVE"
  induced.py  ⛔ 書く前に。閉路は 1 回の書き込みで直す
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
  ⚠️ 設定の名を足すのは 表 T-104（生成器が辞書の settings 節を起こす）
```

**固定の仕掛け:**
```
tests/system/measured-sweep.test.ts                    28 件。1 回の起動
tests/system/first-frame-is-the-settled-frame.test.ts  最初の 1 枚だけを測る
tests/system/rows-fixed-with-nothing-holding-them.test.ts  殻の規則を測る唯一の道
tests/system/user-reported-fixes.test.ts               利用者が指摘し、直したものの錨
tests/system/open-defect-pins.test.ts                  ⚠️ PINNED に D-232
tools/parity/check.mjs                                 50 手
```

---

## 6. 現在地

```
ブランチ restart（origin/restart へ push 済み）
前の巡で書いた変更要求: CR-339 〜 CR-344
前の巡で閉じた: 40 行以上（残件 54 → 17。⚠️ うち 15 行は同じ巡で新しく起こしたもの）
新しく起こした: D-225 〜 D-244
作業樹は 1 つも作っていない（4 体を 2 回、同じ木でファイルを分けて走らせた）
⚠️ 利用者は本リポジトリに限り、コミットと push を任せている。⛔ タグは任されていない
⛔ コミットの前に毎回、個人情報の点検を実測して数を報告すること
```

---

## 7. 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | **仕様待ち** | 14 | **0** | 裁定は全部下りている。§1.1 |
| 2 | **未仕分け** | 2 | **0** | `D-237` は調査、`D-244` は説明の借り |
| 3 | **赤い試験（単体・e2e・同型）** | 0 / 0 / 0 | **0** | ⚠️ `PINNED` の錨は外すこと |
| 4 | **`npm run parity` の操作数** | 50 | **60+** | ⛔ 一致しきった検査は問うていない検査である |
| 5 | **人として通しで押す** | 1 回 | **毎巡 1 回** | 規則 04 の 3.6。⭐ 前の巡はこれで 6 件出た |
| 6 | **検査 31 の腐り（開いている側）** | 3 | **0** | ⛔ 46 は決着済みの側。総数を目標にするな |

⚠️ **総件数が増えることは失敗ではない。失敗は、偽の状態を偽と知らずに置いておくことである。**
⚠️ **前の巡で台帳は 224 行から 244 行へ増え、残件は 54 から 17 へ減った。**
⭐ **増えた 20 行のうち 15 行は、その日のうちに見つけて起こしたものである** ——
**parity を倍にし、人として通しで押したことで出た。**
