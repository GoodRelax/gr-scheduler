# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **歴代 50 本は `previous-project-result/temp/` に在り、`.gitignore` の `temp/` に
当たって一度もコミットされていない。**⭐ **本書からは、この 1 ファイルを毎巡書き換える。**
⛔ **番号を増やすな。**各巡の引継書が自ら「本書が唯一の引継書である」と名乗ってきたのだから、
ファイルも 1 つでよい。⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. ハーネスがサブエージェントを禁じているかを確かめ、禁じているなら
   「規則 05 と衝突する。体を使ってよいか」を最初に問う
3. ⭐ 利用者が眠っているときの標準指示（2026-08-30 に受けた。逐語）——
   「裁定が不要な分を進めろ。修正が容易な分（定数の変更／1 モジュール内に
     収まる変更）は裁定を待たずに進めて、後でまとめて報告しろ」
```

---

## 0.5 ⛔⛔⛔ **進捗の測り方が変わった（利用者の裁定 2026-08-30）**

```
⭐ 残件 = テスト完了 でも 取下げ でもない案件。⇒ これを減らすことが進捗である。
⛔ tools/ledger_metrics.py が刷る（総件数の左に列がある）。手で書くな。
実測（2026-08-30 夕）: 残件 102 ／ 総件数 154
```

⚠️ **1 つ罠がある。**⛔ **実物で測って穴を見つけると 残件 は増える。**
この巡は 98 → 102 に増えたが、**増えた 10 行はすべて実測で見つけた本物の穴**であり、
**閉じた 6 行と相殺されている。**⇒ ⭐ **残件だけを見ず、「閉じた数」と「開いた数」を
並べて報告すること。**差分の行がそれを示す。

---

## 0.6 ⛔⛔ **台帳の形が変わった（利用者の裁定 2026-08-30）**

```
① 対応方針 の欄が 対応方針・決定仕様 になった
② 仕様確定 と 実装待ち のあいだに 仕様書反映済み を挿した
③ 仕様書へ入れたら、その欄に 章・仕様管理番号・仕様の逐語 を書く
```

⭐ **狙いは利用者の逐語**「**これで、仕様書記載漏れが無くなるだろ？**」——
⛔ **いままでは「裁定が下りた」と「仕様書に書いた」が同じ `仕様確定` に混ざっており、
書き忘れが台帳の上から見えなかった。**

⛔⛔ **1 通目でやること（利用者の指示）**: **別の体に、全 154 行の現況を調べさせろ。**
- `仕様確定` に居るのに**もう仕様書へ入っている**行はないか
- `実装待ち` 以降に居るのに**仕様書に書かれていない**行はないか
- ⭐ 見つけたら `仕様書反映済み` へ移し、**章・仕様管理番号・逐語**を欄に書かせる

⭐ **調べ終えたら、前に立つ者が利用者へ諮ること（この巡で提案し、まだ裁定を受けていない）**:

> **`実装待ち` 以降に居るのに `対応方針・決定仕様` の欄に仕様管理番号が無い行を数え、
> 0 でなければ落とす検査を足すか。**

⛔ **同じ形が既に効いている** —— **検査 28** は「`テスト完了` なのに実物で見ていない行」を
数えて落とす。⇒ **記憶にある梯子のとおり、規則は機械が見るところまで下ろさないと守られない。**

---

## 0.7 ⛔⛔ **状態表示は 2 度嘘をついた（差し戻し済み）**

```
D-71（アイコン自体を緑で塗れ）  テスト中 → 仕様確定 へ差し戻した
D-75（グリフが枠に対して小さい） テスト中 → 仕様確定 へ差し戻した
D-142（構えていない入口）        テスト完了 だったが利用者が実物で否定した（この巡）
```

⭐ **救いが 1 つある** —— **検査 28** が「`テスト完了` なのに実物で見ていない行」を数えており、
**いま 0 件**である。⇒ ⛔ **守られていないのは `テスト中` と `テスト待ち`** で、
そこは「試験が緑」だけを根拠にしている。**実物で見るまで信用するな。**

---

## 1. ⛔⛔⛔ **この巡の最優先 —— 利用者が指示した順番**

```
① アイコンの「有効」の見せ方を、すべて一気に直す（PD-398 ／ 台帳 D-71・D-75）
② 行の操作子（タスクグループ）の一枚（PD-397 ／ 台帳 D-154）
⛔ この順である（利用者の指示）—— 「そうしないと、ピンの問題が片付かないだろ？」
⚠️ ピンは 2 つに割れる: 塗りと色は ①、常時描くこと（HF-6 の例外）は ②。
  ⇒ ピンが完全に片づくのは ② の終わりである。
```

### ⛔ ① アイコンの「有効」（`PD-398`。**裁定済み。仕様の書き換えから入れ**）

⛔⛔ **根はここである** —— 表 T-236 の **`S-183` の行の名前が逐語で「構えている入口の
*縁*の色」**であり、`FR-053` も**縁**に名指している。⇒ **実装は仕様に忠実だった。
仕様が「縁を塗れ」と言っていた。**
⚠️ **台帳 `D-71` は 2026-08-27 の利用者の逐語**「アイコン自体を緑で塗れ。
例: イナズマ線が有効ならイナズマ線アイコンを緑で塗れ」**をそのまま持ちながら `テスト中` に居る。**
⛔ **状態表示が偽である**（`D-142` と同型）。⇒ **`テスト中` を信用するな。**

**裁定（2026-08-30）:**
1. **縁ではなく塗り。**
2. **抜き色は `S-146`（地の色）。**⛔ 白は使えない —— 暗いテーマの `S-183` は明るい緑
   `#6fc98d` である。地の色なら**両テーマで自動的にコントラストが立つ。**
3. **4 つとも塗りで統一** —— 構え（`S-183`）／ON のトグル／ピン止め（`S-151`）／
   `FR-072` の押下状態。⭐ **形は 1 つ、意味は色。**
4. **`D-75`（グリフが枠に対して小さい。アイコンの大きさは変えずにグリフを拡大）を
   同じ一枚で一気に直す。**

### ⛔ ② 行の操作子の一枚（`PD-397` ／ 台帳 `D-154`）

⭐ **ピンの裁定は済んでいる（`PD-396`）:**
- **留まっているあいだ常時描く**（`HF-6` の例外）
- **色は `S-151`（選択色）**
- **`×` と `📌` の左右を入れ替える**
- **名前の末尾がピンに隠れることは受け入れる**（`FR-085` / `FR-052` が逃げ道を持つ）。
  ⛔ **場所を確保する案は採らない** —— `FR-085` の MUST NOT に触れ、書き出しと食い違う。



**一枚に整理する中身（`PD-397` が持つ）:**

| | いま |
|---|---|
| 行ごと | `IC-58` 配下をすべて開く ／ `IC-59` 行自身を畳む ／ `IC-77` 配下をすべて畳む ／ `IC-60` ピン止め ／ `IC-82` 行を削除 |
| パネル頭 | `IC-74` すべての行を開く ／ `IC-78` すべての行を畳む |

**同じ一枚で片づける残りの問い:**
1. ⛔ **操作子が「効かない」を判定するとき、画面をスクロールして行が見えなくなったことも数えるか。**
   ⚠️ **いまは数える** ⇒ **行を画面外へ送っただけで、上の行の畳む操作子が薄くなる。**
   ⭐ これは前に立つ者が仕様に「画面に描かれている側で数える」と書いたことの直接の帰結である。
2. `HF-3` を**描かれている葉の行**で押したときの理由（`RS-30` は当たらず `RS-27` に落ちる）

⛔⛔ **やり方**: **絵を描いて見せる。**言葉で問うな。この設計で 2 巡続けて実証されている。
`tools/probe/harness.mjs`（export 40）で出荷ビルドを開き、案を DOM に注入して撮る。

---

## 2. ⭐ 裁定済みで、まだ実装していないもの

### ⛔ `PD-394` —— **唯一の赤い試験がこれで緑になる**

**裁定（2026-08-30）**: 表 T-023d の結びの自己矛盾を **案 A** で解く。

> 「**いずれも 1 日単位に四捨五入する**」を採り、
> 「**ポインタの下の日から求めること**」を「**ポインタの位置から求めること**」へ改める。

⭐ **根拠 3 つ**: ①結びは既に四捨五入と明記しており、落とすほうが情報の削除である
②同じ結びが「**掴み点は時間軸の上の『ある日』そのものであり、画素の感度というものが存在しない**」と
書いており、画素の端で答えが変わる切り捨てはその一文と噛み合わない
③フェードの端を掴むのは「この辺」を指す操作なので、近いほうへ寄せるのが手の意図に近い。

⇒ **CR を 1 本（結びの 1 文）＋ 実装。**赤い試験は
`tests/unit/fr-052-t-023d-picture-while-held.test.ts` の「rounds to whole days」。

### ⭐ 表 T-233 に「入口」の欄を足す（裁定済み・案 A）

⛔ **入口と理由の対がいま散文にしか無く、機械で確かめられない。**
⚠️ **この巡だけで「手書きの対応表が原稿と食い違う」形が 4 度出ている。**⇒ **5 度目が必ず来る。**

---

## 3. ⛔ 実装できずに残っている MUST（**仕様の側に不足がある**）

```
⛔ FR-098「帯が Row Area を埋め尽くしてはならない」        上限の数が無い
⛔ FR-098「収まらないときは帯自身をスクロールできる」      スクロール位置の置き場が無い
   ⭐ 利用者の裁定（2026-08-30）: ②は「特に対応不要」。⇒ 急がない
⚠️ LF-14 を持つ SW_SPEC の節が Chapter 6.1 に無い（親の無い事例を SWS-2 に置いてある）
⚠️ 命令の返り値について仕様が何も言っていない（D-140 の体が報告）
```

---

## 4. ⛔ 押しても効かない入口（**まだ 4 件開いている**）

```
D-146 IC-37 / IC-38  選択したものを開始日・終了日で揃える。バーが 1px も動かない
D-147 IC-41          透かしを隠す。DOM も設定値も面も動かない
D-148 IC-49          ガイドカーソルの 2 連縦線。⭐ 兄弟 3 つは動く（測り直し済み）
D-149 IC-18          AI 対話。IC-20 を押すと IC-18 の押下状態が勝手に立つ
D-150 IC-39          イナズマ線。⚠️ 欠陥と断定していない。先に IC-44 を測れ
```

⭐ **D-148 が第一である** —— 兄弟のうち 1 つだけが死んでいる形は、
**この巡に 4 度出た「手書きの対応表が途中で止まる」形そのもの**である。

---

## 5. ⭐ 現在地（**2026-08-30 の夕。すべて実測**）

```
✅ push 済み: origin/main = 36f5880（この巡で 7 本）
⚠️ ブランチ名は restart のまま。push 先は main（git push origin HEAD:main）

⭐⭐ 検査 28 本 ALL GREEN ／ typecheck 緑 ／ layers 緑（284/284）
tables 135 ／ figures 11 ／ rows 1739 ／ uids 150 ／ アイコン 85 ／ 語 387 対
⭐ 残件 102（巡の頭は 98）／ 台帳 154 行（巡の頭は 144）／ テスト完了 50（巡の頭は 44）
⚠️ 仕様確定 2（D-71 / D-75。テスト中から差し戻した）／ 仕様書反映済み 0
裁定待ち 167 行・うち未裁定 131
試験 112 ファイル / 5136 件 —— 5134 緑 ／ 1 skip ／ ⛔ 1 わざと赤（PD-394。上の 2 節）
```

**この巡で仕様へ入れたもの**: CR-304（1.58）／ CR-305（1.59）／ CR-306（1.60）／
CR-307（1.61）／ CR-308（1.62）／ CR-309（1.63）／ CR-310（1.64）

---

## 6. ⭐⭐ この巡でいちばん効いた方法（**続けろ**）

```
⭐⭐⭐ FR-102 の操作記録が、この巡で初めて元を取った。
    seq 808 が act=changeDocument doc=changed と書き、seq 809 のフレームが
    直前と 1 バイトも違わない —— ⛔ 言葉では絶対に書き取れない種類の事実である。
    ⇒ 利用者が「効かない」と言ったら、まず記録を出してもらえ。

⭐⭐ 体の第一の容疑を鵜呑みにせず、前に立つ者が測り直す。この巡で 3 度効いた:
    ① 引継書 50 の「手書きの対応表が古い」は偽。配線は無事だった
    ② 前に立つ者の「ピンが二重に描かれている」も偽。踏査の体が名指しで正した
    ③ 「1 段目の 7 つ」も偽。正しくは薄くなりうる入口は 10 個

⭐⭐ 実装した当人に試験を書かせない（規則 05 の 7）。この巡の値打ちは決定的だった。
    実装の体は自分の仕事を 5 通り・4 通り・3 通りに壊して全数を走らせ、
    ⛔ そのたび「自分の仕事はどの試験からも見られていない」と自ら報告した。
    ⭐ そして仕様だけを読む体が書き直したら、実装は 3 つとも既に満たしていた。
      古かったのは試験のほうだけだった。

⭐ 仕様の側に答えが既にあることがある。⛔ 発明する前に引け。
    「ピン止めの上端はどこか」→ EP-5 が Pinned Row を Row Area の中身と数えていた。
    「FR-018 の免除で単調性が壊れないか」→ 同要求が同じ形の免除を既に持っていた。
    ⇒ SC-1 / RT-6 / EP-5 は 1 文字も書き換えずに済んだ。
```

---

## 7. ⛔⛔ この巡で前に立つ者が犯した誤り（**繰り返すな**）

```
⛔⛔ 体が走っている最中に足元を動かした。
   「走っている間は仕様書を書き換えるな」と自分で書いた直後に、
   ① CR-308 を仕様へ入れているあいだ CR-307 の配線の体が試験を走らせていた
      ⇒ その体の赤の数が走行中に動き、帰属の切り分けに手間を取らせた
   ② 試験の体が走る src/ で改名を試み、6 ファイル・941 件を一時的に壊した
      ⇒ 体が観測して報告に書いた
   ⭐ 直し方: 体を出す前に、仕様も src も「閉じてから」出す。

⛔⛔ 「構え」という確定語を二重に使った。
   仕様の「構え」は パレットが図形を構えること（表 T-023b の AR-2 / AR-3）である。
   ⛔ 「操作子がいま効く状態」の意味で使ってはならない。
   ⇒ 利用者が「そもそも構えってなんだ？」と問い返した。混乱を足したのは前に立つ者である。

⛔ 規則 03 が禁じている形をそのままやった。
   U-50 の定義と EP-9 の規則を散文へ写して、検査 11（重複検出）に 2 度捕まった。
   ⭐ 直し方は 1 つ: 写すのをやめて指すだけにする。

⛔ 台帳の列数を読み違えて、実物確認の証拠を 補足 の欄へ書いた（10 列で、実物確認は index 9）。
   ⇒ 検査 28 が捕まえた。⭐ 書く前に 1 行 split して数えろ。

⛔ 改名器を 4 度書き直した。落とし穴は 3 つとも「見えないものを見落とす」形:
   ① press.on という属性参照を局所変数として書き換えた
   ② 正規表現リテラルの } で範囲判定が狂った
   ③ テンプレート文字列の ${...} を丸ごと空白化して、使用箇所が見えなかった
   ⭐ 4 度目は字句を正しく分ける形にし（scratch/blank_noncode.py）、
     それでも落ちた 3 ファイルはコンパイラの判定で元に戻した。
```

---

## 8. ⭐ 道具（**30 行の下ごしらえを二度と書くな**）

```
tools/probe/harness.mjs   ⭐ export 40。⛔ 1〜2 文字の名前は 0 件（この巡で直した）
   足したもの: rowPanel / hoverRow / panelEntries / pressPanelEntry
              diff / styleSignature / treeOrder / ancestry / wheelOver

⛔⛔ 押した結果の「盤面」を自分で狭く書き直すな。実測で両方向に嘘をついた:
   ① census() が数えている svg line / arrows を落として、効いている入口 10 個を
      「死んでいる」と報告した
   ② IC-16（テーマ）は要素の数も data-role も body の地の色もまったく動かさない。
      動くのはインラインの様式だけ ⇒ styleSignature() を盤面に入れろ
⛔ rows() は y で並べ替える。⇒ DOM の並びと画面の並びの食い違いは絶対に見えない。
   treeOrder() を使え（ピン止めの並びの破れはこれで初めて出た）。
⛔ 「2 つ在る」が親子かどうかは ancestry() で判ずる。
   前に立つ者はこれを怠って 2 件を誤報告した。
⛔ スクロールが起きる状態を作らないと FR-098 は測れない。
   5 行の文書では「上端に上げる意味が無い」ので、正しく何もしない実装と区別できない。
⛔ 行の操作子は [data-role="Row Title Panel"] の子孫ではない。領域（座標）で絞れ。
⛔ プロパティパネルは常に DOM に在る。display で測れ。
⛔ P はコマンドパレットである。パネルの入口は MK-13（ダブルクリック）と IC-17 の 2 つだけ。
⚠️ emptyPoint() は実ポインタで掃くので遅い。空きは 1 度求めて使い回せ。
⛔ バーは polygon で、外接する箱は画面外まで伸びる。cursorAt が 'grab' を返す点を探せ。
⭐ 選択は DOM に印が無い。Delete が何かに届くかで測れ。

tools/probe/examples/    手本 6 本
```

---

## 9. ⛔ 名前の残り（**利用者の指示 2026-08-30**）

> **極端に短い変数は意味をあらわさない。切れの良いタイミングで、名は体を表すような変数名に改めろ。**

```
tools/probe/harness.mjs    41 → 0 件（済み）
src/                       84 → 27 件
tests/                     539 件（未着手）
⭐ x0 / x1 / x2 のような線分の端点は残してよい —— 2 つ揃って意味になる名前である。
⛔ 残る 27 件は on / is / to など 属性名でもある英単語で、機械には見分けられない。手で直す。
⭐ 数え方: コメントと文字列を空白化してから数えること（scratch/blank_noncode.py）。
  ⛔ 素の正規表現で数えると、コメントの英文を識別子として拾って 136 と出る（正しくは 84）。
```

---

## 10. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | 2026-08-30 夕 | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔ **`PD-397` の一枚が出来て、利用者の裁定が下りている** | 0 | **1** | 最優先。落とせない |
| 2 | **わざと赤い試験** | 1 | **0** | `PD-394` は裁定済み。CR 1 本と実装で閉じる |
| 3 | **押しても効かない入口**（D-146〜D-150）| 5 | **≤ 2** | D-148 は原因の形が分かっている |
| 4 | **テスト完了 の行数** | 50 | **60** | 体 6 本 × 1〜2 行 |
| 5 | **実物確認を持たない行** | 43 | **≤ 36** | harness で 1 件 2〜4 秒 |

⚠️ **総件数が増えることは失敗ではない。**この巡も 144 → 154 に増えたが、
**増えたのは実物で測って穴を見つけた結果である。**
