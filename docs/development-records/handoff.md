# 引継プロンプト —— **本書が唯一の引継書である**

**本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**
⛔ **本書は状態だけを持つ** —— 着地・止めたもの・次にやること・利用者の確認待ち。**学びと規則は `docs/development-rules/` へ下ろす**（規則 05 の 1）。検査 72 が、最新の日付の節の外に残った学びを赤にする。

> 🆕⭐⭐⭐⭐⭐⭐⭐⭐ **2026-09-26 の巡（調整役 —— 定数 `JDG-602`・`CR-570`・`CR-573` の着地、CR の起草の山、重複の調査、段 8 の性能は赤）—— 本節が最新。下の節より本節が正。** ⛔ 数は測り直せ（`git log -1 origin/refactor`、`git log --oneline 2f83685e..origin/refactor`）。
> - **着地（`refactor` は push 済み、先端 `82d3c7e5`）**:
>   - ⓪ 定数 `JDG-602`: 題の字 `S-225` 16 → 20（行の高さ 1.2、頭の帯は 29px のまま）／ `S-124` 1000 → 150 ms —— のち `JDG-658` で一部覆った: 300 ms へ、タスクの説明の待ちは `S-439` 500 ms（`CR-576` が当てる）。
>   - `CR-570`（`treeState` の 5 値、段 0 の畳みも保存）: 仕様 `c5a95f23`・コードと試験 `879e9ff2`・開いたときの全体表示の倍率の直し `a1678f86`（`DFC-1009`）。
>   - `CR-573`（試験の方針）: `e873bcc6` ／ `81b07680`。単体 139 ファイルを削除・66 を contract へ・ユースケース試験 14・parity 75/75・性能の関門は `GRS_PERF=1` のときだけ（`JDG-605`）。細目は下の「試験の方針」の節。
>   - `DFC-1086`（既定の起動テンプレートは行の高さを書かない）。
>   - 起草して合流したが **未適用**: `CR-571`（検索）・`574`・`575`・`576`・`577`・`578`・`579`・`580`・`582`・`572`（起草）。
>   - 重複の調査の報告 `docs/review/duplicate-survey-2026-09-26.md`: 親の群 136、うち偶然の写し 128、写しがすでに食い違うもの 49、正しい文書で表に出るもの 10。台帳 `DFC-1051`〜`1075`。
>   - この日の裁定: `JDG-600`〜`609`・`620`〜`629`・`630`〜`634`・`635`〜`645`・`646`〜`649`・`650`〜`673`・`680`〜`681`・`690`〜`691`・`695`・`700`〜`702`・`710`〜`717`（`rulings.md` を grep せよ）。
> - **止めたもの**:
>   - `CR-572` の波 1（仕様だけ。設計どおり赤）は枝 `claude/cr-572-settings-sort` の `34f4236c` にあり、**合流していない**。最後の緑は `81b07680`。続け方は調整役に任された —— 推奨: `34f4236c` から波 2（道具 → entity ／ use-case → adapter → framework と見本）、次に波 3 と perf-pending へ。
>   - `CR-578`・`579`・`580`（割る案）: ユニットの表は重複の調査の群を待つ。`CR-578` には `JDG-681`「割る」と `JDG-695`（手書き 1,000 行を超えるファイルは割る。501〜1,000 行は責務が 2 つのときだけ。ファイルの大きさのラチェットの検査を `CR-578` に入れる）がある。
>   - `CR-581`（重複の予防）: CR の本文はまだ無い。試作の測りは `previous-project-result/temp/cr-581-guard-prototypes-2026-09-26/`（gitignore、手元だけ）へ、`CR-578` の棚卸しは `previous-project-result/temp/cr-578-frame-loop-inventory-2026-09-26/` へ救った。
> - ⛔ **段 8 の性能 = 赤**（`82d3c7e5`、`docs/development-records/measurements/performance-runs.md`）: スクロール `MK-1` の中央値 18〜20 → 91〜97 ms、ズーム `MK-2` 23〜30 → 79〜91 ms。表 T-043 の関門 `PG-8`・`PG-10`・`PG-12` と `NFR-002`・`NFR-003` が赤。手がかり: ホイールの同期の配送が約 2.5 倍 ／ 描いた絵が倍（要素 493 → 989、字 66k → 185k）／ 描き直しの呼び出しの中で強制のスタイル再計算とレイアウト（1 フレーム約 5.6 ms）が走る —— 段 0 では 0 だった。`CR-570`・`CR-573` は `perf-pending.md` に残る。**`main` は早送りしていない**（`03fb4d74` のまま）。生の出力は `previous-project-result/temp/stage8-perf-run-2026-09-26/`（手元だけ）。
> - ⛔ **次にやること（利用者と合意した順、2026-09-26。memory `process-first-plan-2026-09-26`）**:
>   1. **段取りの改善 —— 提案チップ P1〜P4 を並行で**:
>      - P1 規則: §2〜§2.11 の学びと規則にすべき memory を `docs/development-rules/` へ下ろす（手順か機械の検査を優先）。`07-review-standards.md` に「1 つの仕事は 1 か所」と「補助関数を書く前に公開の入口を探せ」を足す。刈り込み、本書を状態だけへ縮め直す。
>      - P2 並行の道具: 合流の道具（合流 ＋ 再生成 ＋ 検査 ＋ 個人情報の数 ＋ 関門を 1 回で、結果は 5 行）／ 衝突しない生成物（台帳の数の表・`tbl-row-id-prefixes.md`）／ 合流での仮番号の詰め直し ／ 関門の列（関門は 1 本ずつ。XSD の関門は根で。性能の測り直しもこれを通す）／ 体への 10 行のブリーフを手順か道具に。
>      - P3 重複の予防 = `CR-581` の前半: 表 T-064 を `_source` の JSON から生成（公開に CR が要らなくなる —— 偶然の写し 128 のうち 78 は公開されていないことだけが止めていた）／ 公開の入口の索引 ／ 手書きの値の集合が生成された表を言い直さないことの検査 ／ 双子のコメントの登録簿を増やさない基準線。名前を正規化した写しの差分の関門は割った後。
>      - P4 試験の道具: 生成する試験の目録（試験ごとに置き場と、結ぶ仕様の行・UC）。残した単体 122 の振り分け直しは CR の整理を待つ。
>      - P1〜P4 と並行して: **性能の根本原因の分析**（読むだけ —— コード・トレース・計装した dist の写し）。「設計どおり中身が増えた」（`CR-567` の葉が親の行に載る・`CR-570` のすべて開く・`JDG-399` のラベル）と無駄（描き直しの中の強制レイアウト・入力の道から起きるレイアウト）を分けよ。
>   2. **性能の対策**: 設計どおりの増えなら裁定を問う → 直す → 関門の列を通して静かな機械で測り直す（`GRS_PERF=1`、`JDG-605`）→ 緑なら `main` の早送りを利用者に問う（`JDG-61`）。⛔ 性能の直しは割る作業より前（割る作業はバイト一致を保つため）。
>   3. **新しいセッションが保留の CR をすべて整理する**（`555`〜`563`・`571`・`572` の残り・`574`〜`577`・`578`〜`580`・`582`・`DFC-1022` の UC-003 の文・調査の裁定 `JDG-700`〜`702`・`DFC-1003`／`1086` の続き）: 重なる行を解き、番号を 1 回で詰め、仕様を 1 回で通す。CR ごとに毎フレームの経路に触れるかを印し、性能の測りの時期を決める。
>   4. **持ち場ごとの並行の実装**（〜5 セッション。利用者の裁定: 上限なし、詰まりは仕組みで直す）: 割る `578` → `579` → `580`（調査の群を合わせる）／ `CR-555`（依存線の「…」—— 利用者は 2026-09-24 から待っている）・`557`・`558`・`559` を先に（`frame-loop.ts` に触れない）／ `CR-572` のコード ／ 目録を使った試験の振り分け ／ そのあと `frame-loop.ts` の CR（`556`・`560`〜`563`・`571`・`574`〜`577`・`582`）と台帳の欠陥（UC の食い違い `DFC-1011`〜`1022`、調査で届くと分かった欠陥）。
> - ⏳ **利用者の確認待ち**: 新しいものは無い。下の 09-25 の節から残るもの —— `JDG-565` の下で上げた基準線（検査 55・60・45）／ `DFC-855`（まだ `defects.md` に在る）／ `CR-569` の依存の並び（`MR-2`）。`DFC-82` は `fixed-defects.md` へ移った（閉じた）。

> 🆕 **2026-09-26 —— 試験の方針（`CR-573`、`JDG-635`〜`645`・`JDG-605`〜`607`・`JDG-672`）—— 着地・push 済み（`e873bcc6` ／ `81b07680`）。** 決まったことは規則 04 の 3.5（表 `UO`）・3.9（既知の赤）・4（表 `GT`）・5（表 `PW`）と、仕様の表 T-334 が持つ。
> - ⛔ **残り**: ① 当面残した単体 122 ファイル（副作用のある関数の分岐）は、カバレッジを測ってから振り分け表をもう一度利用者に見せる（検査 65 の `UO-4` も未測）② `docs/development-records/perf-pending.md` の `CR-570`・`CR-573` を、`main` の早送りの前の段 8 の測りでまとめて測る ③ 台帳 `DFC-1008`（`builtMerge` の札の嘘、src の直し待ち）・`DFC-1011`〜`1022`（ユースケース試験が見つけた仕様との食い違い、`test.fail` で固定）・`DFC-1023`（display-words）・`DFC-1024`（負荷での時間切れ、flaky）。

---

## 前の巡 —— 履歴と、まだ開いているもの

⛔ **中身は各 CR・`rulings.md`・`defects.md` と commit が持つ。** 下は「どこを見ればよいか」と「閉じたと確かめていないもの」だけである。⚠️ **開いているものは、書いた日の時点の姿である。使う前に測り直せ。**

- **2026-09-25 夜〜26**（`CR-569` の着地 → `CR-570` の起草のやり直し）: `CR-569` は起草 `3457e412`・波 1 `9da81e29`・波 2a `dfbba1de`・台帳 `0951b3cd`・仕様だけの試験 `70a17c90`・dist `9d8ed382`。`CR-570` はその後に着地した（冒頭の節）。捨てた最初の案の波 1 `0d49e737` は作業木の枝 `cr-570-apply` にだけ在る —— ⛔ `refactor` へ載せない。
  - ⚠️ 根の作業木に、追跡されていない `previous-project-result/16-grab-area-sizing/row-height-sample.html` が残っている。利用者の指示（2026-09-26）で残す —— ⛔ 消すな。
- **2026-09-25**（段 7.5 の閉じ `566bf187` → `CR-568` W1 `0cddb2ac`・W2 `c39f4e6e`・W3 `a188fec0` → `DFC-979` 閉じ `4c7087fe` → `CR-565` 波 0 `25dbbec7`〜波 1e `aa891a59`・試験 `f1b93b9b`・dist `33bbc1ef` → `CR-567` 波 1 `74dd4398`・波 2a `1b2e01ca`・試験 `ddfd993b`・dist `19c74a20` → 段 8 の測り直しのうち性能を除くもの。計画の記録 18 節と `docs/development-records/measurements/structure-stage8-2026-09-25/`）。
  - ⏳ 開いている: `tests/nfr/nfr-004-single-file.test.ts` の `CN-6` の赤（出荷物の生の字にダウンロードのページの URL。`CR-565` が `CN-6` に「人が押すリンクは出荷物自身の通信ではない」と書き足したので、試験が古い見込み。台帳の行はまだ無い）／ 段 8 の (a) の残り —— 書き直した試験のコメント（`frame-loop.ts` を import する 94 本のうち 49 本、検査 55 の行 17,450）、現状固定の試験の後始末、関数の大きさの上限値（`JDG-54`）と越境名 130 の目標、理由の欄の無い使われていない図の辺 11 本。
- **2026-09-24〜25**（段 7.5 —— `CR-554` で 7 ファイルを入口と兄弟 46 に割った。`UF-126`〜`UF-171`、`UT-13`〜`UT-17`。コミットは件名「Split …」、`d1ffc7bc`〜`d99cb255` ほか。`frameLoop` 1,440 行は `JDG-440` で残す）。
  - ⏳ 台帳の手入れで開いている観察: `DFC-630` の `schedule-layout.ts:601`（`S-86`）はもう無い行を指す ／ `pointer-shape.ts` の `IN-2` を引く WHY が `IN-2` の字と合わない ／ `check-repeated-expressions.py:17`・`:23` が存在しない `schedule-geometry.ts` の行を引く ／ 型の上では送れない `holder: 'resource'` の確定で `spendFieldCommit` が投げる ／ `JDG-440`: 割った後で `frame-loop.ts` の入口を開く頻度と理由を測る。台帳 `DFC-969`（素の設定値の道でだけ起きる `fitZoom` の例外）・`DFC-970`〜`978`（守りの無い TRAP の転記、`試験待ち`）。
- **2026-09-24**（段 7 の後続 H2〜H10 と `DFC-806`: H2 `18db8646`・H4 `bd83a718`・H3 `0d446d12`・H7 `338f39bf`・E-24 `a42c0cb4`・`DFC-806` `032c2023`・H10 `121782d5`、dist `3011de43`。仕様は `CR-566`）。⇒ `handoff-state-machine.md` の §3 の引き受けた後続は全部閉じた —— あちらは消した（本書に従属していた。持ち主は調整役）。
  - ⏳ 開いている: `DFC-943`（断られたドロップのファイルが置き場に残る。異常系 ⇒ `DFC-586`）・`DFC-730`（`FR-033` の editGroup の例外を `it.fails` で留めた）・シェルの選択の刈り（H2）とドロップの聞き手（H3）は自動の試験が届かない（実物で確かめる）・写した元が消えてから貼るとき（仕様が語らない）・固定行の帯に入りきらない行のタスクは刈らない（仕様が語らない）・`DFC-946`〜`949`。
- **2026-09-23〜24**（`CR-551` —— 使い勝手の裁定 16 件、`JDG-367`〜`405`、続きの `JDG-406`〜`408`・`JDG-524`・`JDG-525`）。
  - ⏳ 開いている: `DFC-792`（全体表示で浅い段へ下がる分岐を探り針が通していない）・`DFC-805`（焦点の継ぎ目で「欄が無い」と答える形が仕様に無い）・`DFC-911`（UI から押せないので `実測待ち`）・期限の印（`FR-045`、MUST）は描き手に一度も配線されていない（`DFC-675`）・タスクを選んだまま行名をダブルクリックすると両方の欄が出る（`PND-142` の仮置き）。
- **遅延診断 / Delay Diagnostics（`CR-561`）—— 起草済み・裁定済み（`JDG-470`〜`490`）・未適用。** ⛔ 当てる前に: 帯の外の番号（`FR-130`〜`136` ほか、CR の 11 節）を検査 62 で測り直す ／ 新しいユニットの `UF` と編集の命令の番号は調整役から受ける ／ `S-392`〜`S-396` と面の形は触れる見本で決める（規則 05 の 7）／ 4 節の案の文を旧・新の塊へ起こしてから当てる ／ 当てたら出どころのメモ 3 本（`docs/review/progress-check-2026-09-23.md` ほか）の頭に「`CR-561` に束ねた」と書く。⛔ 用語「点検 / inspection」「進捗検査 / Progress Check」「ピンク」を使わない（`JDG-473`・`JDG-474`）。後に回したのは修正候補の仕組みだけ（`PND-581`）。
- **使い勝手の機能 6 本（`CR-555`〜`CR-560`）—— 起草済み・裁定済み・未適用。** 裁定は `rulings.md` の「2026-09-24 —— CR-555〜CR-560 …」の節、欠陥は `DFC-860`〜`873`。⛔ 当てる前に: 各 CR の `file:line` を分割した後の木で測り直す ／ 番号を検査 62 で測り直す ／ `CR-560` は `dictionary-table-pairing.txt` の 2 行を動かす（利用者は了承済み `JDG-546`、動かすのは調整役）／ `CR-560` の `MK-15` は `MK-13` の直後 ／ `CR-559` は `CR-558` の後。
- **2026-09-22**（段 7 の波 B の結線、全 9 領域: 通知 `43496359`・身振り `c8d05ab1`・ファイル操作 `4b9687e8`・未保存の編集 `f1dcda1d`・名前付けと入力欄 `aae9551d`・`CR-500` の波 B `81dca548`・選択 `888185ef`・操作の記録 `96851652`・Agent API `fcc1e046`。dist `b3f66ec7`。`S-124` は `37fc8da2`、のち `JDG-602`・`JDG-658` で動いた）。
  - ⏳ その日に開いていた（測り直せ）: 検査 61 の基準線の `HELD ...apply-document-change.ts:deliveringNotices` は指す先が無い（消してよいかを利用者に問う —— 消せば `CR-530` の決定 6 の `REGISTRATIONS` の移しも入る）／ 選択の出来事 `emptyAreaClicked`・`selectionEscapePressed`・`selectionSettleKeyPressed` は送っていない（`CR-490` の 2.2 注 2）／ `CR-460` の Promise の継続 3 つは `let` のまま ／ `CR-440` の決定 13 の仕様の文（`PI-8`）は未記入 ／ `PND-142`・`143` の STOP が元の `let` の場所に残った。
- **2026-09-20〜22**（段 3〜6 を閉じた巡: 7 つの原則 `CR-431`、段 3 の門 4 つ（検査 59・19・60・61）、`R2.2` の改定、識別子の予約（規則 02 の 2.5）と検査 62、要求の起点 `CR-435`、割る基準 `CR-432`、段 4 の分割 `911e3d31`・`182a36e1`・`776e34d0`、段 5 `CR-438`、段 6 `CR-439`（`7a9235e8`・`006aefbc`）、裁定待ちの仕分け `CR-541`〜`CR-550`（締め `33112cb2`・`c5e65515`））。
- **2026-09-14〜26 の学び**（旧 §2・§2.5〜§2.11 と各節の「学び」）は、2026-09-26 の process-first の巡の P1 が `docs/development-rules/` へ下ろした。何をどこへ下ろしたかは、その commit の本文が持つ。

⛔⛔ **異常系・準正常系のケアは、リファクタ一式が終わるまで触るな**（`JDG-78`、利用者 2026-09-14）。中身は `DFC-586` が持つ —— 起こさない・自動で直して何処をどう直したか示す・直せないものは対象の ID と行番号を示す。

⏳ **ごみ取りの歯止め（`JDG-161`）のうち、③ 数が基準線より減ったら下げるまで赤 と ④ git の pre-commit で precheck と check.sh は、まだ着地していない。** ① と ② は規則 04 の 6.3 と規則 03 の 3 節に着地した（2026-09-26）—— `rulings.md` の行の状態を直すのは調整役。

---

## リファクタの段の順番と現在地

⭐ **利用者の指示（2026-09-17、逐語「その中でリファクタリングの順番と現在地も書け。」）で本書に書く。** ⛔ 腐るので、使う前に計画の状態表と `git log` で測り直せ。

**2 つの番号の対応:**
- **利用者の 4 つの段**（`JDG-24`、2026-09-05「やることは不具合の全検出。 その後、修正計画/必要なら裁定。 その後一気に既存不具合を直す。 それが終わったら、仕様やコードのごみ消し。」）: ① 不具合の全検出 → ② 修正計画と裁定 → ③ 一気に直す → ④ 仕様とコードのごみ消し。④ の残りはリファクタの計画が引き継いだ（計画の記録 8、`JDG-53`）。
- **リファクタの計画の段 0〜8:** `docs/development-records/refactor-plan-report-2026-09-13.md` の状態表と流れ図が持つ。⚠️ ⛔ **その状態表は古い**（段 2a・段 3 を ⬜ のまま持つ）⇒ 下の表を正とし、状態表を直すのは体に出す。⚠️ 計画の「段」と、利用者の 4 つの「段」は別の番号である。混ぜるな。

| 計画の段 | 何をするか | 現在地 |
|---|---|---|
| 0 足場 | 赤い試験を緑に、基準の記録 | ✅ `cdba2292` |
| 1 棚卸し | 保存しない状態の一覧 | ✅ `90785873`・`698a0f14` |
| 2a 辺の規則 | 辺の定義を 5.2 へ | ✅ `CR-378`（表 T-247・T-248） |
| 2b 状態のモデル | 状態の持ち方と置き場 | 2b-A ✅ `CR-379`。2b-B は `CR-436` |
| 3 門 | 辺・JSON・大きさ・モジュール状態 | ✅ 2026-09-21。門 1 ＝ 検査 59、門 2 ＝ 検査 19、門 3 ＝ 検査 60、門 4 ＝ 検査 61 |
| 4 神関数 | 大きい関数を要求の境目で割る | ✅ 2026-09-21（`911e3d31`・`182a36e1`・`776e34d0`、試験 `ea039e52`）。`builtMerge` は外した（`JDG-284`） |
| 5 入力の訳出 | `input-command-translator.ts` を入力の種類で割る | ✅ 2026-09-21（`CR-438`、`UF-90`〜`UF-102`、`UT-10`） |
| 6 画面の面 | `dom-screen-surface.ts` を UI パーツで割る | ✅ 2026-09-22（`CR-439`、`UF-103`〜`UF-112`、`UT-12`） |
| 7 状態機械 | `frameLoop` の状態を状態機械へ | ✅ 波 A・波 B の結線（2026-09-22）と後続 H2〜H10（2026-09-24）が着地 |
| 7.5 大きなファイルの分割 | `frame-loop.ts` ほかを SW 工学の目で割る | ✅ 2026-09-25（`CR-554`、`UF-126`〜`UF-171`、`UT-13`〜`UT-17`） |
| 8 仕上げ | 図、測り直し、性能、`main` の早送り | 🔄 性能を除く測り直しは 2026-09-25。⛔ 性能は赤（冒頭の節） |

⭐ 割る波の手順と継ぎ目の 3 つの証拠は、規則 05 の 7「割る波の手順」が持つ。

---

## 裁定 → `rulings.md` が持つ

⛔⛔ **裁定の逐語は `docs/development-records/rulings.md` に在る。本書は写しを持たない。** 問う前に grep する手順は規則 06 の 1、問いの形は規則 01 の ⑧ が持つ。

---

## 次のセッションに貼る 1 通目

利用者はこの節のブロックをそのまま新しいセッションに貼る。巡ごとに本節も書き換える。

```text
ブランチ: refactor（2026-09-26 に 82d3c7e5 まで push 済み。⛔ 数は信じず git log -1 origin/refactor で測れ）。main は 03fb4d74 のまま —— 段 8 の性能が赤なので早送りしていない（⛔ 測れ）。⛔ main では直接作業しない。タグは利用者が打つ。
あなたは調整役である。調整だけをする（利用者の裁定）—— 仕事は提案チップ（セッション）と体（Agent）で配り、帯・持ち場・順・合流・push の口を 1 人で持て。並行は 〜5 セッションでよい。
唯一の引継書は docs/development-records/handoff.md である。冒頭の 🆕 の節（2026-09-26 の巡（調整役））から読め。memory の process-first-plan-2026-09-26 も読め。

0. 最初にやること
   - 巡の頭の手順（規則 05 の 5: ListAgents → rulings.md → ledger_metrics --start → guard:start・gen:check）で今の姿を測る。
   - 利用者に問う前に rulings.md を grep する（規則 06 の 1）。「条項が無い」は自分で反証してから（規則 05 の 10）。
   - 並行の作法（番号・push の口・載せ直し・枝の見せ方・個人情報の数）は規則 05 の 13 が持つ。
1. 提案チップ P1〜P4 と、性能の根本原因の分析（読むだけ）を並行で出す。中身は冒頭の節の「次にやること」の 1。
   P1 規則を下ろす ／ P2 並行の道具（合流の道具・衝突しない生成物・仮番号の詰め直し・関門の列）／ P3 重複の予防（CR-581 の前半）／ P4 試験の目録。
2. 性能の対策: 設計どおりの増えなら裁定を問う → 直す → 関門の列を通して静かな機械で測り直す（GRS_PERF=1、JDG-605）→ 緑なら main の早送りを利用者に問う（JDG-61）。性能の直しは割る作業より前。
3. そのあと、新しいセッションが保留の CR をすべて整理する（重なる行を解き、番号を 1 回で詰め、仕様を 1 回で通す）。
4. 持ち場ごとの並行の実装（割る 578 → 579 → 580、CR-555・557・558・559 を先に、CR-572 のコード、試験の振り分け、そのあと frame-loop.ts の CR と台帳の欠陥）。
5. 毎回の約束
   - 説明は日本語。コードとコミット文は英語 ASCII。絶対パスをどこにも書かない。
   - 体に作業木や node_modules の junction を作らせない。git stash をさせない。体の報告は仮説 —— 行を数えて確かめよ。
   - 検査は終了コードで判じる（rm -rf output を先に）。関門は 1 本ずつ回す。
   - 基準線: 下げ・改名・掃除・登録は調整役が書いてよい（JDG-520）。上げは毎回利用者に問え。
   - e2e を GRS_PERF つきで回さない —— 利用者が性能の測りを認めたときだけ。
   - 利用者の裁定が要るときは、説明して複数の案を比べ、推奨を根拠と共に示せ。記号ではなく具体名と使う場面で。
6. コミット／push は許可済み（gr-scheduler に限る）。コミットの前に毎回、個人情報の点検の数（名前・絶対パス・メール・秘密・.gitignore）を測って報告してから行え。
   ⛔ MSPDI の XSD と SDK から取り出したファイルは、絶対にコミットも push もしない。
```

---

## 8.5 裁定の着地先として残す段（2026-09-10。`JDG-42`・`JDG-43` が「8.5」の名で指すので番号を残す）

⛔ **`rulings.md` の `JDG-42` と `JDG-43` は、着地先として本節を名指す。⛔ 節ごと消すな。**
⭐ **どちらも「`docs/spec` に 1 文字も足さない」ことが着地である。だから着地先が本書になっている。**

**`JDG-42`（逐語）:**
> 「推奨通り、勝手に動かないようにしろ」

⇒ **`DFC-423` を直した代償 —— 動かさない 1 回のクリックでダミーが実績を書かなくなったこと —— を是とする。**⛔ **仕様も造りも変えない。**

**`JDG-43`（逐語）:**
> 「推奨通りとせよ。 これは俺の決めた仕様ではない」

⇒ **見本 HTML が書いた、掴みシロの入れ替わりの「2 回以下」という数を取り下げる。**⛔ **`docs/spec` に条項を立てない。**

⛔ **`DFC-311` は触るな**（`JDG-25`「一旦そのまま」）。⛔ **「既知の不具合を 0」の数に入れるな。**

---

⛔⛔ **文脈は圧縮される。本書は `defects.md` と `pending-decisions.md` と `git log` だけから書けるように保て。**
