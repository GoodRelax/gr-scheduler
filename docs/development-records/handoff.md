# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **歴代 50 本は `previous-project-result/temp/` に在り、`.gitignore` の `temp/` に
当たって一度もコミットされていない。**⭐ **本書からは、この 1 ファイルを毎巡書き換える。**
⛔ **番号を増やすな。**⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. ハーネスがサブエージェントを禁じているかを確かめ、禁じているなら
   「規則 05 と衝突する。体を使ってよいか」を最初に問う
   ⭐ この巡は問うて「使ってよい」を得た。実装と試験を別の体に分けられた
3. ⭐ 利用者が眠っているときの標準指示（2026-08-30 に受けた。逐語）——
   「裁定が不要な分を進めろ。修正が容易な分（定数の変更／1 モジュール内に
     収まる変更）は裁定を待たずに進めて、後でまとめて報告しろ」
```

⭐⭐ **`--start` の欠陥をこの巡で直した。**⛔ **残件の欄が増えたとき `--start` の側だけが
1 欄少ないままで、長さの番人が全欄を `—` に落としていた。**⇒ ⛔ **巡の頭に基準が
1 つも刻まれていなかったのに、初回の走行と見分けがつかず、黙って通っていた。**
⭐ **直したので、`--start` のあとに数が入っていることを目で確かめてから先へ進め。**

---

## 0.5 ⛔⛔ **進捗の測り方（利用者の裁定 2026-08-30）**

```
⭐ 残件 = テスト完了 でも 取下げ でもない案件。⇒ これを減らすことが進捗である。
⛔ tools/ledger_metrics.py が刷る。手で書くな。
実測（2026-08-30 の巡の終わり）: 残件 100 ／ 総件数 154
```

⭐ **この巡は 102 → 100。**⛔ **ただし「閉じた数」と「開いた数」を並べて読むこと**:

| | 数 | 何が |
|---|--:|---|
| 閉じた | **2** | `D-71`（塗り）／ `D-75`（グリフの大きさ）—— **実物で確かめて `テスト完了`** |
| 差し戻した | **3** | `D-74` / `D-128` / `D-129` —— **状態表示が偽であった**（下の 0.7）|
| 状態を正した | **21** | 仕様だけを読む体の突き合わせによる（下の 0.6）|

---

## 0.6 ⭐⭐ **台帳と仕様書の突き合わせは済んだ（この巡で実施）**

⭐ **別の体に全 154 行を当たらせた。**⛔ **読むだけで、1 バイトも書き換えさせていない。**
結果を 21 行へ書き戻した。**次の巡はこれをもう一度やる必要はない。**

| 区分 | 数 | 意味 |
|---|--:|---|
| **A** | **0** | `仕様確定` なのに既に仕様書に在る行は**無かった**。2 行とも本当に未記載だった |
| **B1** | **94** | 欄 5 が仕様書の場所を名指さない。⭐ **検査 29 の基準ちょうど** |
| **B2** | **6** | ⛔⛔ **場所は名指すが、その場所がそう言っていない。**⇒ **黙って通る記載漏れ** |
| 腐り | **8** | 欄 5 が「未定」だが、**仕様書は既に決めている**。⭐ **台帳のほうが古かった** |
| 状態が偽 | **3** | 仕様書が「利用者の裁定である」と留保しているのに `テスト中` 以降に居た |
| 名指す先が違う | **4** | 中身は仕様書に在るが、欄 5 だけ読むと辿り着けない |

### ⛔⛔ **B2 の 6 件 —— これが次の巡の実務である**

| ID | いま | 何が足りないか |
|---|---|---|
| **`D-135`** | `テスト中` | ⛔⛔ **仕様書が、実測で偽と分かった文を持ったままである** —— 表 T-013 直後の注が**いまも**「フェードを持つのは `SH-1` と `SH-2` だけ…ほかの 3 つの形状で本規則は何も変えない」と断定する。**本行自身が「変わる」と実測している。**⭐ **これが 6 件の筆頭である** |
| `D-62` | `テスト中` | 仕様書は本行の期待値の**反対**を裁定として明記（表 T-028 の `IN-2`）。指摘に当たる行が無い |
| `D-65` | `テスト中` | `AT-127` は**保管が UTC** としか言わない。「ヘッダーはローカルタイム」「2 段小さく」に当たる行が 0 件 |
| `D-61` | `テスト中` | 「プロパティパネルが『面』か」をどこも述べていない。`[x]` で閉じる入口の行も無い |
| `D-55` | `テスト中` | `S-196` は**予定の図形の上端**からしか測らない。矢印の頭・端点スパンの点に触れる行が無い |
| `D-105` | `テスト待ち` | `FR-036` は**利用者が否とした順序**を定めたままである |

⚠️ **どれも欄 5 が仕様書の行 ID を持つので検査 29 を素通りする。**
⇒ ⛔ **検査 29 は「名指しているか」しか見ない。「そこがそう言っているか」は人が読むしかない。**

✅ **検査 29 が入っている**（`実装待ち` 以降で場所を名指さない行を数え、増えたら落ちる）。
基準は `.claude/skills/spec-graph-check/decided-spec-baseline.txt` の **94**。
⚠️ **判定**: 要求 UID／表の行 ID／`表 T-051` / `図 F-019` / `Chapter 6.1` のいずれか。
⛔ **`D-` / `PD-` / `CR-` は数えない。**⛔ **仕様書の散文に `D-nn` を書くと検査 7 が落ちる**（この巡で 2 度踏んだ）。

---

## 0.7 ⛔⛔ **状態表示は、また嘘をついた（合計 5 件目まで来た）**

```
D-71 / D-75   前の巡に 仕様確定 へ差し戻され、この巡で本当に テスト完了 になった
D-142         前の巡に 利用者が実物で否定した
D-74 / D-128 / D-129   この巡、追加裁定待ち へ差し戻した
              ⛔ 仕様書の RC-13 が「図形の決定は利用者の裁定である」と留保しているのに
                 テスト中 / テスト待ち に居た。試験は緑でも、決めるべき人が決めていない
```

⭐ **検査 28 は「`テスト完了` なのに実物で見ていない行」を数えており、いま 0 件である。**
⛔ **守られていないのは `テスト中` と `テスト待ち`** —— そこは「試験が緑」だけを根拠にしている。
⭐⭐ **仕様だけを読む体に当たらせると、この嘘は機械的に出る。**この巡はそれで 3 件出た。

---

## 1. ⛔⛔⛔ **この巡の最優先 —— 利用者が指示した順番**

### ✅ ① アイコンの「有効」（`PD-398` ／ 台帳 `D-71` `D-75`）—— **終わった**

**`CR-311`。仕様・実装・試験・実物確認まで完了。**

⭐⭐ **根はこうだった** —— **仕様書が「縁を塗れ」と言っていた。**
表 T-236 の `S-183` の行の名前が逐語で「構えている入口の**縁**の色」であり、`FR-053` も縁に名指していた。
⇒ **忠実な実装はすべて縁を描いた。利用者が 3 度否と述べたのは実装ではなく、この 2 行である。**

| 何を | どうした |
|---|---|
| `FR-029` | **塗りの MUST を 1 段落**と、**表 T-237**（新設。`EN-1` 〜 `EN-4`）を置いた |
| 表 T-237 | 構え＝`S-183` ／ ON＝`S-183` ／ ピン止め＝`S-151` ／ プロパティパネル＝`S-183` |
| `FR-053` / `FR-072` / `HF-6` | それぞれ自分の行（`EN-1` / `EN-4` / `EN-3`）を指すだけにした |
| `S-183` | 名前を「構えている入口の**塗り**の色」へ |
| `S-185`（縁の太さ）| **廃止。席は欠番**（読み手が居なくなった）|
| `S-138` / `S-141` | **12 → 16 ／ 6 → 4**。⭐ **入口の外形は 26 × 24px のまま、グリフが 1.33 倍** |
| `PD-290` / `PD-340` / `PD-398` | **3 つとも閉じた**（`PD-290` は問いそのものが消えた）|

**実測（出荷ビルド。撮影あり）**: 構えた `IC-23` の地 `rgb(31,122,61)`＝`S-183`、グリフ `rgb(255,255,255)`＝`S-146`。
ON の `IC-7` も同じ。ピン止めした `IC-60` の地 `rgb(33,75,130)`＝`S-151`。**縁は 1px の `S-149` のまま動かない。**

### ⛔ ② 行の操作子（`PD-397` ／ 台帳 `D-154`）—— **一枚を出し、裁定が 3 つ残っている**

⭐⭐ **利用者は「まずはロジックを全体的に再整理してくれ」と述べ、`.html` の見本を求めた。**
⇒ **2 軸（範囲 × 動作）に並べ直し、触れる `.html` を渡した。**

**⭐ 2 軸に並べると、在るものと無いものが一目で出る:**

| 動作 ＼ 範囲 | すべて（＝ L0。パネルの頭） | この行の配下 | この行自身 |
|---|---|---|---|
| 開く | `IC-74`（`HR-1`）| `IC-58`（`HR-3`）| — |
| 畳む | `IC-78`（`HR-2`）| `IC-77`（`HR-4`）| `IC-59`（`HR-5`）|
| 増やす | ⛔ 無い | ⛔ **無い ← ここに置く** | — |
| 消す | — | — | `IC-82` |
| 留める | — | — | `IC-60` |
| 並べ替える | — | ⛔ 無い | ⛔ 無い |

⭐⭐ **利用者の読みは正しかった** —— 「パネルの頭がイメージ的に L0 となる」。
**表 T-015 が既にそう書いている**: `HR-1`/`HR-3` と `HR-2`/`HR-4` は**同じ動作の範囲違い**である。
⛔ **それなのに図形が別物である** —— 行の「配下を開く」は `>`、頭の「すべて開く」は `＋`。

**⛔ 実測で出た 4 つの食い違い:**
1. **同じ図形が 2 意** —— 山 2 つが、行では `IC-59`「この行自身を畳む」、頭では `IC-78`「すべての行を畳む」
2. **開く／畳むの左右が逆** —— 行は x 70 → 90（開く→畳む）、頭は x 120 → 140（畳む→開く）
3. **畳む図形が 2 家族に割れている** —— `IC-59` は上向きの二重山、`IC-77` は左向きの山、`IC-58` は右向きの山
4. **隣り合う行で員数が 2 と 5 に変わる** —— 畳めない行は 2 つ、葉の行は 5 つ（3 つ薄い）

**⛔⛔ さらに 2 つの穴が出た:**
> **表 T-109 の 85 個のアイコンに、行を増やす入口も、並べ替える入口も 1 つも無い。**
> ⚠️ **`UN-14`（取り消しの表）は「行（`TaskGroup`）の追加」を取り消せる操作として挙げている。**
> ⇒ **取り消し表が知っている操作を、行う手段が無い。**

**✅ この巡に下りた裁定:**
- **スクロールして見えなくなった行は「効かない」に数えない** —— **仕様のままでよい。**
  ⚠️ **引継書 51 の「いまは数える」は実測で再現しなかった** —— 900px スクロールしても `data-can-*` は 1 つも動かない。
- **行を増やす入口は `[+]`**、`IC-82`（枠の中の `×`）と**対**にし、**増やす先は配下**である。

**⛔ 残っている裁定 3 つ（次の巡の最優先）:**

| | 何を決める | 行 |
|---|---|---|
| 1 | **並べ替えの手** —— ドラッグか、`↑` `↓` の操作子 2 つか、ドラッグ＋`PR-15` か | `PD-400` |
| 2 | **足した行の位置と名前** —— 末子か長子か／空の名か既定の名か | `PD-399` |
| 3 | **図形の 1 家族** —— 「向き＝動作、本数＝範囲」を採るか | `PD-401` |

⚠️ **もう 1 つ**: **畳めない行に、効かない操作子を薄く描くか。**⛔ **実測でいま両方が起きている**
（`Phase Bars` は描かない、`Phone Sign In` は 3 つ薄く描く）。

⛔⛔ **やり方**: **絵を描いて見せる。言葉で問うな。**この巡で 3 度目の実証である。
⭐ **`.html` を渡すと利用者が触れる**（利用者の指示「別の .html にサンプルを示せ」）。

---

## 2. ⭐ 裁定済みで、まだ実装していないもの

### ⛔ `PD-394` —— **唯一の赤い試験が、まだこれである**

**裁定（2026-08-30）**: 表 T-023d の結びの自己矛盾を **案 A** で解く。

> 「**いずれも 1 日単位に四捨五入する**」を採り、
> 「**ポインタの下の日から求めること**」を「**ポインタの位置から求めること**」へ改める。

⇒ **CR を 1 本（結びの 1 文）＋ 実装。**赤い試験は
`tests/unit/fr-052-t-023d-picture-while-held.test.ts` の「rounds to whole days」。
⚠️ **この巡は着手していない。**⭐ **1 文と 1 箇所なので、次の巡の頭で片づく。**

### ⭐ 表 T-233 に「入口」の欄を足す（裁定済み・案 A）

⛔ **入口と理由の対がいま散文にしか無く、機械で確かめられない。**

---

## 3. ⛔ 実装できずに残っている MUST（**仕様の側に不足がある**）

```
⛔ FR-098「帯が Row Area を埋め尽くしてはならない」        上限の数が無い
   ⭐ 利用者の裁定（2026-08-30）: スクロールの置き場は「特に対応不要」
⚠️ LF-14 を持つ SW_SPEC の節が Chapter 6.1 に無い（親の無い事例を SWS-2 に置いてある）
⚠️ 命令の返り値について仕様が何も言っていない（D-140 の体が報告）
```

⭐ **仕様だけを読む体が、この巡さらに 3 つ挙げた**（`CR-311` の範囲。**まだ直していない**）:
- **`FR-029` の「1 つの入口に 2 行が同時に当たるときは上の行が勝つ」に、届く帰結が無い** ——
  `EN-1` / `EN-2` / `EN-4` はいずれも `S-183` であり、`EN-3` は構えも ON も持たない行の操作子である。
  ⇒ **色が違う 2 行がぶつかる組み合わせが存在しない。**⚠️ **将来 1 行の色が変われば効く。**
- **`EN-2` を画面記述のどのメンバが運ぶかを、どの行も言っていない。**
  `EN-2`（`FR-049` の切替）と `EN-4`（`FR-072`）は画面の側で見分けがつかない。
- **入口の外形 26 × 24px の「26」を試験で押さえられない** —— **枠の 1px を持つ行が表 T-206 にも T-201 にも無い。**
  ⭐ **高さ 24（＝ `S-138` ＋ `S-141` × 2）は押さえられる。**

---

## 4. ⛔ 押しても効かない入口（**5 件、まったく手つかず**）

```
D-146 IC-37 / IC-38  選択したものを開始日・終了日で揃える。バーが 1px も動かない
D-147 IC-41          透かしを隠す。DOM も設定値も面も動かない
D-148 IC-49          ガイドカーソルの 2 連縦線。⭐ 兄弟 3 つは動く（測り直し済み）
D-149 IC-18          AI 対話。IC-20 を押すと IC-18 の押下状態が勝手に立つ
D-150 IC-39          イナズマ線。⚠️ 欠陥と断定していない。先に IC-44 を測れ
```

⭐ **`D-148` が第一である** —— `IC-47` を押すと `svg line` が 97 → 99、`IC-48` で 97 → 98、
⛔ **`IC-49` は 97 のまま 1 本も増えない。**兄弟のうち 1 つだけが死んでいる形である。
⚠️ **この巡は ① と ② に費やしたので、5 件とも触っていない。**

---

## 5. ⭐ 現在地（**2026-08-30 の巡の終わり。すべて実測**）

```
✅ ローカルの HEAD = 2c4def2（この巡で 3 本）。⛔ まだ push していない
⚠️ ブランチ名は restart のまま。push 先は main（git push origin HEAD:main）

⭐⭐ 検査 29 本 ALL GREEN ／ typecheck 緑 ／ layers 緑（284/284）
tables 136（＋1。表 T-237）／ figures 11 ／ rows 1742（＋3）／ uids 150 ／ アイコン 85
⭐ 残件 100（巡の頭は 102）／ 台帳 154 行（変わらず）／ テスト完了 52（＋2）
⚠️ 追加裁定待ち 4（0 から。3 件は状態表示が偽で差し戻し、1 件は D-154）
裁定待ち 170 行・うち未裁定 130（PD-399 / PD-400 / PD-401 を立て、PD-290 / PD-398 を閉じた）
試験 115 ファイル / 5193 件 —— 5191 緑 ／ 1 skip ／ ⛔ 1 わざと赤（PD-394）
```

**この巡で仕様へ入れたもの**: **CR-311**（版 **1.65**。A.3 の変更履歴に記入済み）

⛔⛔ **`.claude/worktrees/` に 163 個の作業樹が残っており、2.6 GB ある。**
**多くが未コミットの変更を抱えている**ので、**勝手に消さなかった。**
⚠️ **`grep -rn` がここを拾って結果を汚す** —— **`docs src tests tools` を明示して引くこと。**
⇒ **利用者に「消してよいか」を問うのが筋である。**

---

## 6. ⭐⭐ この巡でいちばん効いた方法（**続けろ**）

```
⭐⭐⭐ 実装した当人に試験を書かせない（規則 05 の 7）。この巡も決定的だった。
    仕様だけを読む体が 57 件書き、49 件が実装を触らずに緑になった。
    ⛔ そして その体が、前に立つ者が書いたばかりの仕様の欠陥を 3 つ見つけた:
    ① 表 T-237 が行 ID を code span で囲っており（`| \`EN-1\` |`）、
      tests/contract/spec-table.ts が 行 0 件 と読んだ ⇒ 表で試験を駆動できない
    ② 「抜き色に白を使ってはならない」が自分の MUST と矛盾していた
      —— 明るいテーマの S-146 は #ffffff そのものである
    ③ 「図形を描く箱を塗りつぶし」は、表 T-206 が箱＝S-138（16px）と定めるので、
      逐語に読むと 26 × 24 の枠の内側に 4px の塗り残しが出る
    ⇒ ⛔ 前に立つ者が書いた仕様は、前に立つ者には見えない。体に読ませろ。

⭐⭐ 体の第一報を鵜呑みにせず、前に立つ者が測り直す。この巡で 2 度効いた:
    ① 体の「ピンの style に ; が抜けている」は偽。実測で right:1.5em は 24px に解決していた
    ② 引継書 51 の「スクロールを効かないに数える」も偽。900px 動かしても何も変わらない

⭐⭐ 仕様の側に答えが既にあることがある。⛔ 発明する前に引け。
    「パネルの頭は L0 か」→ 表 T-015 が HR-1/HR-3・HR-2/HR-4 として既に対で持っていた。
    ⇒ 利用者の読みが正しく、足りないのは図形だけだと 1 度の grep で分かった。

⭐ CR を書く前に induced.py を走らせる。CR-311 は巡回が 2 つ出て、
    {FR-053 S-183 S-185} と {FR-029 S-138 S-141} をそれぞれ 1 度の書き込みで直した。
```

---

## 7. ⛔⛔ この巡で前に立つ者が犯した誤り（**繰り返すな**）

```
⛔⛔ 自分が書いた仕様の欠陥を、自分では 1 つも見つけられなかった。
   表 T-237 の行 ID を code span で囲ったこと、白の禁止が自己矛盾していたこと、
   「図形を描く箱」の逐語が塗り残しを生むこと —— 3 つとも体が見つけた。
   ⭐ 直し方: 仕様を書いたら、必ず別の体に「これで試験が書けるか」を問わせる。

⛔ 仕様書の散文に台帳の ID（`D-75`）を書いて、検査 7 に捕まった。
   ⭐ 台帳・裁定待ち・変更要求は仕様書の場所ではない。指すな。

⛔ 同じ MUST NOT を FR-029 と FR-053 の両方に書いて、重複検出に捕まった（A18 [NEW]）。
   ⭐ 直し方は 1 つ: 持ち主を 1 つ決め、他方は指すだけにする。

⛔ 台帳の 実物確認 の欄に飾りを付けて書いた（「✅ **2026-08-30 実測**（…）」）。
   検査 28 は素の「✅ 2026-08-30」を読む。⭐ 既存の行を 1 つ見てから書け。

⛔ 裁定待ちの 状態 の欄に、辞書に無い語（「一部裁定」「消滅」）を書いて 2 度落とした。
   ⭐ 使えるのは 未裁定 と 裁定済 の 2 つだけである。
```

---

## 8. ⭐ 道具（**30 行の下ごしらえを二度と書くな**）

```
tools/probe/harness.mjs   ⭐ export 40。出荷ビルドを開いて実測する
⛔ rows() は y で並べ替える ⇒ DOM の並びとの食い違いは見えない。treeOrder() を使え
⛔ 行の操作子は [data-role="Row Title Panel"] の子孫ではない。領域（座標）で絞れ
⛔ 行は [data-depth] で拾う。[data-role="Row Title"] という役は存在しない
⛔ 「2 つ在る」が親子かどうかは ancestry() で判ずる
⚠️ 実測した行の高さは 64 / 96 / 101 / 108 / 148px とまちまちである。決め打ちするな
⭐ この巡で使った形（scratch/probe/ に残っている。gitignored）:
   measure-icon-box / row-controls-now / row-controls-questions / verify-cr311
⛔⛔ 体が dist を建て直すので、絵を描く前に dist/index.html を scratchpad へ写せ。
   写しを file:// で開けば、実装の体と競合しない

.claude/skills/spec-graph-check/
  induced.py   ⛔ CR を書く前に走らせる。巡回ごとに 1 計画・1 度の書き込み
  impact.py    深さ 2 の波及
  check.sh     29 本。⚠️ 1 本でも落ちたら「FAILURES ABOVE」としか出ない。
               ⭐ grep -E "PROBLEM|DRIFTED|check [0-9]+ *:" で場所を出せ
```

---

## 9. ⛔ 名前の残り（**利用者の指示 2026-08-30**）

> **極端に短い変数は意味をあらわさない。切れの良いタイミングで、名は体を表すような変数名に改めろ。**

```
tools/probe/harness.mjs    0 件（済み）
src/                       27 件（on / is / to など属性名でもある英単語。手で直す）
tests/                     539 件（未着手）
⭐ 数え方: コメントと文字列を空白化してから数えること（scratch/blank_noncode.py）
```

---

## 10. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔ **`PD-399` / `PD-400` / `PD-401` の裁定が下りている** | 0 | **3** | `.html` は渡した。問うだけである |
| 2 | **わざと赤い試験** | 1 | **0** | `PD-394` は裁定済み。CR 1 本と実装で閉じる |
| 3 | ⛔ **B2 の 6 件**（0.6 の表）| 6 | **≤ 3** | `D-135` が筆頭。**仕様書が偽の文を持っている** |
| 4 | **押しても効かない入口**（D-146〜D-150）| 5 | **≤ 2** | `D-148` は原因の形が分かっている |
| 5 | **テスト完了 の行数** | 52 | **60** | 体 6 本 × 1〜2 行 |

⚠️ **総件数が増えることは失敗ではない。**⭐ **状態を正して 残件 が増えるのも失敗ではない。**
⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
