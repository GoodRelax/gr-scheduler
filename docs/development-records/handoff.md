# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
2. npm run gen:check                             ⭐⭐ 赤い試験を疑う前にこれを走らせろ
3. npm test / npm run e2e / npm run parity       ⛔⛔ 3 つとも数えろ
4. §1 を読む
```

⭐ **サブエージェントは許可されている**（利用者、2026-08-31）。
⛔⛔ **利用者の指示（2026-09-01）: 前に立つ者は実装と試験を書かない。体に出すこと。**
前に立つ者がするのは、**仕様を書く・台帳を書く・体の報告を検証する・実物で確かめる**の 4 つである。

---

## 0.5 ⭐ **始点（前の巡の終わりに実測）**

```
単体   123 ファイル / 5548 件 —— 5547 緑 ／ 1 skip ／ 赤 0
e2e    tests/system 17 件 —— 全部緑
同型   npm run parity —— 25/25 一致（うち 2 件は「仕様どおり食い違う」）
検査 31 本 ALL GREEN ／ typecheck 緑 ／ gen:check 17 本 OK
重複 A=27（new 0）／ tables 136 ／ rows 1770 ／ uids 150
台帳 182 行 ／ 残件 63 ／ テスト完了 115 ／ テスト中 20 ／ テスト待ち 20 ／ 追加裁定待ち 7
```

⭐⭐ **前の巡で 残件 105 → 63、テスト完了 61 → 115。**
⛔ **その大半は「仕事が終わった」のではなく「台帳が動かされていなかった」だけである。**§4 を読め。

---

## 1. ⛔⛔⛔ **この巡の仕事**

### 1.1 ⛔ **まず利用者の裁定を取りに行く（帰宅後に答える約束になっている）**

⭐ **本当に塞がっているのは 7 行である**（台帳が `追加裁定待ち` と記す行）。
⛔⛔ **うち 4 行は同じ 1 つの規則で塞がっている** —— 表 T-026 の `RC-13`
「**新しい図形の決定は利用者の裁定である**」。⇒ **体は勝手にアイコンを描けない。**

| 行 | 何が決まっていないか（具体名で） |
|---|---|
| `D-74` | 利用者が名指しで挙げたマイルストーン形状 7 つ（書類・箱・フロッピー・サーバー・会議の人・慶事の顔・ビールジョッキ）の要否と図形 |
| `D-128` | 表示の切り替え 8 つのうち **3 つに入口が無い**。足すなら図形 3 つ |
| `D-129` | 行の階層操作 6 つのうち **3 つに入口が無い**（すべて畳む／配下を畳む／行を隠す）|
| `D-154` | **行の操作子が読めない** —— 同じ二重山印が「この行を畳む」と「全部畳む」の両方を意味する等、実測で 4 つの矛盾 |
| `D-155` | **タスクグループを隠す入口が無い**。`∧` 1 つで親へ畳み、親の `∨` で戻す案を利用者が出している |
| `D-157` | **親を畳んで開き直すと配下が全階層いっぺんに開く**（畳む前を覚えていない）|
| `D-173` | 書き出しの `.svg` と単一 `.html` が書けない。**中身を作るかどうか** |

⭐ **これに加えて、前の巡で新たに溜まった問いが 4 つある:**

```
① D-43 の向き —— タスク本体のダブルクリックは、いまプロパティパネルを開く。
   台帳の期待値は「その場で編集。パネルへ寄り道しない」。
   ⛔ ところが利用者の 2026-09-01 の指示（D-180）は
      「タスクグループ名のダブルクリックでパネルを開け」であり、向きが逆である。
② 「このビルドではまだ書けない形式です」の理由の行が無い。
   いま借りている行の文言は「もう一度行ってください」——⛔ 何度やっても書けないので嘘になる。
   ⚠️ さらに FR-029 は落ち先を RS-27 と定めており、いま使われている RS-15 と食い違う。
③ 書き出しの面の英語の見出しが `Export Chooser` のまま（日本語だけ「ファイルに保存」にした）。
   ⛔ 英語名は面の確定名なので、勝手に変えると発明になる。
④ 行の字下げが 12px。利用者の逐語「全角で 1 文字分」を計算すると 13.0px。
   ⭐ 12 は誰も測っていない値（🔎 付き）なので、13 に直すのが筋と見ている。
```

### 1.2 ⭐ **裁定は下りているが、まだ 1 文字も書いていない 7 件**

⭐ **利用者の逐語つきで台帳に書き取ってある。**⛔ **問い直すな。**

| 行 | 中身 |
|---|---|
| `D-174` | 2 択の問いを `[Yes]` / `[No]` の語のボタンにし、`y` / `n` で答えられるようにする。`Y` と `N` を太字に。⭐⭐ **これは `D-147`（透かしのパスワード面）の塞がりも解く** —— **語のボタンなら新しい図形が要らない** |
| `D-175` | タスクグループを全部消したら `L1` の `No name` を自動で 1 つ作る |
| `D-176` | マイルストーン形状は `○` 〜 `☆` を既定で出し、残りは `…` で追加表示 |
| `D-177` | パレットの並びを 依存線 → コメントボックス → ハイライトボックス → 開始日揃え → 終了日揃え に |
| `D-178` | 最小化中は「マイルストーン形状」のような文字を出さない |
| `D-179` | ヘッダーから出し直したら標準の大きさで出る |
| `D-180` | タスクグループ名のダブルクリックでパネルを開き名前編集、`Enter` で閉じる |

⭐ **`D-147`（透かし）も裁定済み**（利用者「問1 A」）—— **パスワードを問う面を建てる。**
⛔ **実物では入口がまったく効かない**ことが 2 度測られている（押しても差分 0・通知 0）。

### 1.3 ⛔⛔ **実物で落ちた 3 件（試験は全部緑のまま）**

```
D-06  コメントボックスを 1 つも置けない —— 入口は構え、ポインタも変わるのに、
      空いた 5 か所で押しても引いても図形 0 増・通知 0 件。
      ⭐ 同じ点でタスクの入口を使うと図形が増えるので、測り方の誤りではない。
      ⇒ ハイライトボックスと依存線の入口も同じ。
D-181 バーの左端を掴むと、その行が丸ごと隣の段と入れ替わる（2 度測って同じ、Ctrl+Z で戻る）
D-182 バーのダミーをどこへ落としても実績の開始が予定の開始になる（+0/+3/+8 日とも同じ）
      ⭐ マイルストーンでは落とした場所が正しく効く ⇒ 形状で振る舞いが違う
```

⚠️ **同じ族がもう 1 つ**: **タスクを引いているあいだは何も描かれず、離した瞬間に出る。**

### 1.4 ⭐ **試験を書く（31 行、12 の束にまとめてある）**

⭐ **束は要求ごとに切ってあるので、1 束を 1 つの体に渡せる。**
目盛の刻み（`D-38` `D-91` `D-92`）／案内カーソル（`D-72` `D-148`）／全体表示の押下（`D-22` `D-24` `D-16`）／
パネルの確定と再描画（`D-130` `D-82` `D-116`）／ヘッダー（`D-65` `D-87` `D-126`）／
ファイルの道（`D-66` `D-98`）／取り消しの射程（`D-27` `D-102`）／辞書の真（`D-34` `D-166`）／
通知（`D-124` `D-120`）／フェードとラベル（`D-15` `D-135`）／行見出しの寸法（`D-160` `D-103`）／
単独（`D-45` `D-89` `D-105` `D-106` `D-115` `D-152`）

---

## 2. ⭐⭐⭐ **体の使い方**

### ⭐ 答え: **「実装する体」と「仕様だけを読む体」の対。⛔ ワークフローの扇形展開ではない。**

#### ① ⭐⭐⭐ **対にすると、実装の欠陥を仕様の側から捕まえられる**

```
⭐⭐ 前の巡: 仕様だけを読む体が実装を赤くした ——
   IC-18 の押下を S-99i だけで描くと、Agent API が無効のあいだ
   「薄くて、かつ塗られている」入口になり FR-029 の MUST NOT を破る。
⭐⭐ 仕様だけを読む体が、こちらの過剰な要求を突き返しもした ——
   「4 状態を描き分けよ」は別の MUST が禁じており、行き過ぎだと自分で判じた。
⛔ 試験を書く体に src/ の .ts を読ませるな。読ませたら実装の振る舞いを規則として書き写す。
   ⭐ src/**/*.json（名簿・辞書）は許してよい —— データであって振る舞いではない。
```

#### ② ⭐⭐ **「落ちることを確かめろ」まで指示に書け**

```
⭐ 前の巡の体は、自分の読み取りを 3 通りに壊して 3 通りの赤を出して見せた。
⭐ 別の体は、偽の実装を被せて 6 件が赤くなることを見せた。
⭐⭐ さらに別の体は、赤い試験を「narrow すれば緑になるが、それでは穴が戻る」と言って
   わざと赤のまま残した。⇒ これが正しい。
⇒ ⛔ 「緑でした」だけの報告を受け取るな。⭐ 何をすれば赤くなるかを言わせろ。
```

#### ③ ⛔ **並列にしてよいのは、触るファイルが重ならないときだけ**

⚠️ **前の巡は 6 体を同時に走らせた。**衝突は出なかったが、**それは射程を書き分けたからである。**
⛔ **`npm run gen` を 2 体に同時に走らせるな** —— 生成物が壊れる。

---

## 3. ⭐⭐ **順番**

```
① 利用者の裁定を取りに行く（§1.1。11 件ある）
② 裁定済みで未着手の 7 件を書く（§1.2）—— 仕様は前に立つ者が 1 パスで
   ⛔ 書くたびに: induced.py（閉路）→ 書く → check.sh（重複 new 0）
③ 実物で落ちた 3 件を直す（§1.3）
④ 試験の 12 束を体へ配る（§1.4）
⑤ 各件のあと必ず: npm test / npm run e2e / npm run parity / check.sh
⑥ ⛔⛔ 実物で押せ。§4 のとおり、試験が緑でも実物は動いていないことがある
```

---

## 4. ⭐⭐⭐ **前の巡で学んだこと（続けろ／繰り返すな）**

### ⛔⛔⛔ **いちばん大きい発見: 台帳の「残っている仕事」は、大半が仕事ではなかった**

```
試験の状態に留まっていた 94 行を全数で仕分けた結果:
  55 行  既に試験が在った   ← 台帳が動かされていなかっただけ
  31 行  試験が本当に要る
   1 行  古い
   7 行  裁定が要る（と思われた）

⭐⭐ さらに、その「裁定が要る」7 行のうち 6 行は既に裁定が下りていた。
⛔⛔ 原因は台帳の書き方そのものである ——
   対応方針の欄は書き足しで伸びるので、裁定が下りても冒頭の「⛔ 未定」が残る。
   ⇒ 欄の頭だけを読む者は「塞がっている」と読む。
   ⛔ D-131 に至っては、欄が「仕様に行が無い。利用者の裁定が要る」だけで、
      決着（IN-6）が別の欄に書かれていた。

⭐⭐⭐ 検査 31 を足して機械に見せるようにした。実測 61 行が同じ形で腐っている。
```

### ⛔⛔ **試験が緑でも実物は動かない —— 前の巡だけで 4 回**

```
コメントボックスが 1 つも置けない（D-06）
バーの左端を掴むと行が入れ替わる（D-181）
ダミーをどこへ落としても実績開始が予定開始になる（D-182）
透かしの入口がまったく効かない（D-147）
⇒ どれも 5546 件の単体試験は全部緑のまま。
```

### ⛔⛔ **測る道具そのものが壊れていた**

```
tools/probe/harness.mjs の shapes() と signatureAt() が ReferenceError で落ちていた。
⛔ どちらも「図形を見つけたときだけ」落ちる ⇒ 答えが要る場面に限って例外になる。
⭐ 直した（直す前後の出力を両方測ってある）。
⇒ ⛔ 道具を疑え。実物確認の体が 2 つとも独立に見つけた。
```

### ⛔⛔ **外接矩形は clip-path を見ない**

```
getBoundingClientRect は切られた図形も元の箱を返す。
⇒ 「まだ直っていない」と報告しかけた。elementFromPoint で掃き直したら直っていた。
⛔ 盤面の「見えているか」を矩形で測るな。
```

### ⭐ 効いた

```
⭐⭐⭐ 「塞がっている」と書いてある行は、まず反証しろ。前の巡は 7 行中 6 行が偽であった。
⭐⭐⭐ 引継書の記述も 1 日で腐る。前の巡で 4 件が偽であった ——
    ・「VisibleElement が 9 対 8」→ 既に直っていた
    ・「契約は PI-37」→ 正しくは PI-2
    ・「png を描く実装が無い」→ 在った。どこからも呼ばれていなかっただけ
    ・「svg は理由の行が無いので塞がっている」→ RS-22 が既に在った
⭐⭐ 利用者の測定を疑うな。「拡張子が付かない」に対し、こちらは
    「保存済みファイルに .json が在るから付いている」と読み違えた。
    ⛔ それは利用者が手で付けたものであった。⭐ 宿主のダイアログを差し替えて
      受け取る値を読んだら、渡す側は正しく、宿主が守っていなかった。
```

---

## 5. ⭐ 道具

```
npm run parity             ⭐⭐ 見本と出荷ビルドを 25 の操作で突き合わせる
                           ⭐ 読みは 4 つ: rows / counts / arming / pinned
                           ⭐ KNOWN_DIVERGENCES に「仕様どおり食い違う」を理由つきで書く
                           ⛔ 押せなかった操作は落ちる（黙って ✅ にしない）
npm test / npm run e2e     ⛔ 両方数えろ
npm run gen / gen:check    ⭐ 原稿を直したら gen、巡の頭で gen:check
tools/probe/harness.mjs    ⭐ 出荷ビルドを実測する（前の巡で 2 か所直した）

⛔⛔ 外接矩形は clip-path を見ない。見えているかは elementFromPoint で掃け
⛔ 行を y で覚えて押すな。押すたびに組み直る。名前で引き直せ
⛔ 行の操作子は、その行の名前にポインタが乗るまで隠れている
⚠️ 行の高さは 64 / 96 / 101 / 108 / 148px とまちまち
⛔ 宿主のダイアログを測るときは addInitScript で差し替えろ ——
   殻は起動時に 1 度だけ掴むので、あとから差し替えても無視される
⭐ ピン止め [data-pinned] ／ 押下 [data-pressed] ／ 薄い [aria-disabled]

.claude/skills/spec-graph-check/
  induced.py  ⛔ 書く前に。閉路は 1 回の書き込みで直す
  check.sh    31 本。grep -n "^FAIL\|^PROBLEM\|^DRIFTED"
  ⭐ 検査 30: 生成された定数の一覧が木と一致するか
  ⭐ 検査 31: 「未定」と書いてある行が、同じ行で「裁定が下りた」とも言っていないか
  ⚠️ 設定値を足すのは docs/spec/_source/settings.json（⛔ tbl-settings.md は生成物）
  ⚠️ 表示語を足すのは docs/spec/_source/display-words.json
```

---

## 6. ⭐ 現在地

```
⭐ ブランチ restart（push 先は main）
⭐ 前の巡で閉じた: D-170 / D-158 / D-149 / D-168 / D-171 / D-172 と、実物確認 36 行
⭐ 部分的に閉じた: D-173（.png は書ける。.svg と単一 .html は理由を告げて書かない）
⛔ 実物で落ちている: D-06 / D-43 / D-147 / D-181 / D-182
⭐ 作業樹は 1 つも作っていない
```

---

## 7. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔⛔ **裁定待ち**（§1.1 の 11 件）| 11 | **0** | ⭐ 利用者が帰宅後に答える約束 |
| 2 | ⛔ **裁定済み未着手**（§1.2 の 7 件）| 7 | **0** | ⭐ 逐語は台帳に在る |
| 3 | ⛔⛔ **実物で落ちている件** | 5 | **0** | ⭐ 試験は全部緑のまま落ちている |
| 4 | **赤い試験（単体・e2e・同型）** | 0 | **0** | ⭐ 3 つとも数えろ |
| 5 | **試験が要る行**（§1.4）| 31 | **減らす** | ⭐ 12 束に切ってある |
| 6 | **検査 31 の腐り** | 61 | **減らす** | ⛔ 増やすな |
| 7 | **`npm run parity` の操作数** | 25 | **30+** | ⭐ ドラッグ・段替え・削除の取り消しを足す |

⚠️ **総件数が増えることは失敗ではない。**⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
