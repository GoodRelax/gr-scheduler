# 引継プロンプト —— **⛔ 本書が唯一の引継書である**

⚠️ **本書は追跡されており、公開リポジトリに載る**（利用者の指示 2026-08-30）。
⛔ **番号を増やすな。本書 1 ファイルを毎巡書き換える。**
⚠️ **公開なので、個人情報・絶対パス・秘密を書かないこと。**

---

## 0. ⛔⛔ **1 通目でやること（順番どおり）**

```
1. python tools/ledger_metrics.py --start        基準を測って刻む。⛔ 手で書くな
   ⭐ 走らせたあと、セッション開始時の行に数が入っていることを目で確かめる
2. ハーネスがサブエージェントを禁じているかを確かめ、禁じているなら
   「規則 05 と衝突する。体を使ってよいか」を最初に問う
3. ⭐ 利用者が眠っているときの標準指示（2026-08-30 に受けた。逐語）——
   「裁定が不要な分を進めろ。修正が容易な分（定数の変更／1 モジュール内に
     収まる変更）は裁定を待たずに進めて、後でまとめて報告しろ」
```

---

## 0.5 ⭐⭐ **赤い試験は 0 である。ここから始めろ**

```
実測（巡の終わり）: 116 ファイル / 5216 件 —— 5215 緑 ／ 1 skip ／ ⭐ 赤 0
検査 29 本 ALL GREEN ／ typecheck 緑 ／ layers 緑（284/284）
重複検出 A=26 (new 0) ／ tables 136 ／ figures 11 ／ rows 1755 ／ uids 150
```

⛔⛔ **前の巡の引継書は、赤い 7 件を「`GR-20` の実装が無いから」と書いていた。偽であった。**

| 何件 | 本当の原因 | 直し方 |
|--:|---|---|
| **6** | `help-roster.json` が表から**ずれていた**だけ | `npm run helproster` **1 回**。手で書いた行は 0 |
| **1** | 表 T-023d の結びが**自分の全数を数えていなかった** | 散文に 1 文 |
| **1** | `PD-394`（表 T-023d の結びの自己矛盾）| 1 語と実装 1 か所 |

⇒ ⛔⛔ **赤い試験を「未実装の合図」と読むな。⭐ まず `npm run gen:check` を走らせろ。**

---

## 0.6 ⭐⭐ **進捗の測り方（利用者の裁定 2026-08-30）**

```
⭐ 残件 = テスト完了 でも 取下げ でもない案件。⇒ これを減らすことが進捗である。
⛔ tools/ledger_metrics.py が刷る。手で書くな。
実測: 残件 104（巡の頭は 103）／ 総件数 159（巡の頭は 157）
```

| | 数 | 何が |
|---|--:|---|
| 閉じた | **1** | `D-150`（実測して**欠陥ではなかった**ので 取下げ）|
| 進めた | **6** | `D-146` / `D-148` / `D-65` / `D-135` / `D-62` / `D-61` / `D-55` / `D-105` |
| 開いた | **2** | `D-158` / `D-159` —— **どちらも実測で見つけた** |

⭐ **「閉じた数」と「開いた数」を並べて報告すること。**⛔ **増えるのは失敗ではない。**

---

## 1. ⛔⛔ **この巡でいちばん重い発見 —— 検査が 4 つの生成物を見ていなかった**

```
npm run gen:check   が走らせる --check : 16 個
check.sh            が走らせていた     : 12 個   ⛔ 4 個が野放し
```

⛔⛔ **野放しだった 1 つが `help-roster.json` であり、赤い 6 件はそれである。**
⭐ **`gen:check` は最初から `PROBLEM` と言っていた。⛔ 誰も走らせていなかった。**

✅ **直した** —— 検査 27 が 7 個から **11 個**になった（help-roster・property-items 2 種・licence）。
⭐ **門が本当に落ちることを確かめた** —— `help-roster.json` の行 ID を 1 つ壊したら `PROBLEM` になり、戻したら `OK` に戻る。

⚠️ **教訓**: ⛔ **「検査が緑だから生成物は正しい」と言う前に、その検査が何を見ているかを数えろ。**

---

## 2. ⛔ **押しても効かない入口 —— 5 件の内訳（前の巡は「5 件すべて未調査」）**

| ID | 何が | いま |
|---|---|---|
| **`D-146`** | `IC-37` / `IC-38`（揃える）| ✅ **直した。**⛔ **実物確認は未了**（下の `D-159`）|
| **`D-147`** | `IC-41`（透かしをしまう）| ⛔ **半分。⚠️ 裁定待ち** |
| **`D-148`** | `IC-49`（縦 2 本）| ✅ **直した。実物で確かめた**（97 → 99、追従も） |
| **`D-149`** | `IC-18`（AI 対話）| ⛔ **裁定待ち**（規則 06 の **E**）|
| **`D-150`** | `IC-39`（イナズマ線）| ✅ **欠陥ではなかった。取下げ** |

### ⛔ `D-146` の真因（**`src/` の STOP が半分嘘をついていた**）

⭐ **押下は届いており、選択が**使える**ときに限って捨てられていた。**
⛔ **STOP の逐語**「表 T-108 に整列の命令が無いのだから、押下を手にしても組むものが無い」——
⭐ **前半は真、後半は従わない。**`GR-12`（バー本体を引く）が**同じファイルの中で**
選択の全部を `CM-11` の束として書いている。⇒ **`alignWrites` が同じ形で組む。**

⚠️ **⛔ `STOP` を根拠に「仕様に無い」と言うな。この巡でまた 1 件出た。**

### ⛔ `D-150` は交絡であった（**測り直して分かった**）

⭐ **起動文書は基準日を最初から持っている**ので、`IC-44` の 1 回目は基準日を**消す**。
⇒ **そのあと `IC-39` を押しても線が出ないのは正しい。**
⭐ **`IC-39` を単独で押すと `polyline` が 0 → 1 → 0 と往復する。**

⚠️ **教訓**: ⛔ **入口を続けて押した測定は、前の押下が状態を変えている。1 つずつ測れ。**

---

## 3. ⛔⛔ **利用者の裁定が要るもの（4 件。⭐ ここから始めるのがいちばん速い）**

| ID | 問い | なぜ進めなかったか |
|---|---|---|
| **`D-147`** | `IC-41` はパスワードをどこで問うのか | ⛔ **値を問う面が 表 T-103 にも 表 T-037 にも 表 T-234 にも 0 行。**⚠️ 表 T-234 の前文が「同表に無い問いを立ててはならない（MUST NOT）」と定める。⛔ **単なる切り替えにすると `FR-020` の MUST そのものを破る** |
| **`D-149`** | `IC-18` の「しまう」は在るのか | ⛔ **しまったかを覚える行が 表 T-206 にも 表 T-203 にも無い。**⭐ **代案**: 表 T-109 の語を `FR-066`（有効なあいだ出す）に合わせて縮める。⚠️ 直すと 表 T-064 の `PI-37` に触る（**E**）|
| **`D-158`** | パレットの入口が押下状態を持てるか | ⭐ **行は既に在る**（表 T-237 の `EN-2`）。⛔ **UF-65 が `DocumentSettings` を受け取る必要があり、契約は `PI-37` で公開されている**（**E**）。⚠️ **利用者が挙げた例（イナズマ線を緑に）がまさにこの 4 件の 1 つである** |
| **`PD-407`** | 行の親を変える命令を 表 T-108 に足すか | ⛔⛔ **`HF-15`（行を掴んで動かす）が書き込む先が無い。**`CM-35` は兄弟の中の並べ替え専用で、実装自身が他の親からの持ち込みを拒む。⭐ **推奨は案 A**（`moveTaskGroup` を 1 行足す）|

⭐ **図形 4 つ**（単一の `∧` ／ 単一の `∨` ／ 二重の `∨` ／ 枠つき `＋`）も**まだ裁定待ちである。**
⛔ **図形の決定は利用者の裁定である**（`FR-029`）。⇒ **描いて見せて、問うこと。**
⭐ **二重の `∧` は `IC-59` の図形がそのまま当たる**（前の巡の実測）。

---

## 4. ⭐ **次の巡の第一手 —— `GR-20` は「実装が無い」ではなく「書き込む先が無い」**

⛔⛔ **前の巡の引継書は `GR-20` を「実装 0 行」とだけ書いていた。⭐ もっと悪い。**

```
① 表 T-108 に、行の親を変える命令が無い          ⇒ PD-407（裁定待ち）
② 表 T-109 に、掴み代の行が無い（図形も入口も）  ⇒ ScreenPart に真偽を 1 つ足す形が要る
③ 握っているあいだの絵を、どの行も述べていない   ⇒ 位置をずらすだけが最小である
```

⭐ **①が下りれば残りは 2 つの体に割れる**（境目は `IF-9`）:

```
2a 書き込む側  generate_entity_types.py ／ edit-task-group.ts ／ edit-document.ts
2b 手つきと絵  screen-surface.ts ／ screen-renderer.ts ／ row-title-panel.ts ／
               input-command-translator.ts ／ frame-loop.ts ／ dom-screen-surface.ts
```

⛔ **`.ts` を新しく作るな** —— `npm run tree:check` が 表 T-075 に無いファイルで落ちる。
⭐ **動く見本が `previous-project-result/11-row-controls/` に在る。実装の前に必ず触れ。**

---

## 5. ⭐ 現在地（**すべて実測**）

```
⛔ push していない。⭐ コミットは 3 本（origin/main = a13f234 のまま）
   c6e1ab8 検査 27 を 7 → 11 個へ
   277787b 赤 8 → 0、入口 2 つ
   58eb7cb ヘッダーの更新日時の裁定を仕様書へ
⚠️ ブランチ名は restart のまま。push 先は main（git push origin HEAD:main）

⭐ 仕様へ入れたもの: CR-314（1.68）／ CR-315（1.69）／ CR-316（1.70）／ CR-317（1.71）
⭐ 残件 104 ／ 台帳 159 行 ／ テスト完了 52 ／ 追加裁定待ち 9
裁定待ち 176 行・うち未裁定 130（新: PD-406 / PD-407）
```

⭐⭐ **巡の終わりに作業樹を落とす習慣を続けろ。**⛔ **この巡は作業樹を 1 つも作っていない**
（体はすべて本体で読み書きした）ので、消すものは無い。⚠️ **確かめるのは 1 行である:**

```
git worktree list                                   # 本体 1 つだけか
git branch --list 'worktree-*' --no-merged origin/main   # ⛔ 空でなければ消すな
```

---

## 6. ⭐⭐ この巡でいちばん効いた方法（**続けろ**）

```
⭐⭐⭐ 診断の体を 3 つ、読み取り専用で同時に走らせた。
    5 件の入口を 1 巡で「直った 2・欠陥でない 1・裁定待ち 2」まで分けきった。
    ⛔ 体には「STOP コメントを信じるな。自分で仕様を読め」と明示的に書くこと。
    ⭐ この巡は、体が STOP の誤りを 2 件見つけた（D-146 と D-148）。

⭐⭐⭐ 実装した当人に試験を書かせない（規則 05 の 7）。
    仕様だけを読む体が 18 件書き、⛔ 実装が見落としていた FR-029 の穴を見つけた ——
    「`Task` を 1 つだけ選んで押すと、何もせず理由も告げない」。
    ⭐ 直した（構えの読みと翻訳の読みを、両方 2 個以上に揃えた）。

⭐⭐ 体の第一報を鵜呑みにせず、前に立つ者が測り直す。この巡で 4 度効いた:
    ① 「赤 7 件は GR-20 の実装で消える」は偽。6 件は生成物のずれであった
    ② 台帳の「色は S-195 が持っている」は偽。ガイドが使うのは S-148 である
    ③ 台帳の「手書きの対応表が 3 つで止まっている」も偽。分岐が 2 つだっただけ
    ④ 台帳の欄が名指していた AT-127 は、そもそもヘッダーの時刻ではなかった

⭐⭐ 数を 2 か所に置かない。⛔ この巡は同じ罠を 2 度踏んだ ——
    仕様の散文で MUST を逐語で引き写し、重複検出に 2 度捕まった。
    ⭐ 直し方は 1 つ: 持ち主を 1 つ決め、他方は指すだけにする。
```

---

## 7. ⛔⛔ この巡で前に立つ者が犯した誤り（**繰り返すな**）

```
⛔⛔ 仕様の散文に、別の表の MUST を逐語で引き写した（2 度）。
   ⭐ 直し方: 指すだけにする。「表 T-051 の HF-15 が同じ MUST で既に求めている」。
   ⚠️ 重複検出 A=26 (new 0) は、書き足すたびに読むこと。

⛔ 検査 22 の正規表現は R2〜R7 しか見ない。R1 の条項を名指しても落ちる。
   ⭐ 逃げ道: 規則の書類の相対パスを本文に literal で書く。

⛔ A-appendix.md の版の行に `PD-394` と書いて検査 7 に捕まった。
   ⭐ 版の行に PD- / D- を書くな。CR- はバッククォート無しなら通る。

⛔ 実物の測定で、修飾キーを推測で決めた（Control だと思っていた）。
   ⭐ 直し方: src/ を読んで確かめる。⚠️ それでも 2 つ選べず、D-159 に残した。

⛔ bash の中で PowerShell のヒアストリング（@'…'@）を書き、
   コミットの件名の頭に @ が付いた。⭐ 直し方: git commit -F <ファイル>。
```

---

## 8. ⭐ 道具（**30 行の下ごしらえを二度と書くな**）

```
tools/probe/harness.mjs   ⭐ export 40。出荷ビルドを開いて実測する
⛔⛔ SVG の x は画面の x ではない —— 1920 の窓に対し x=2093 のバーがある。
   ⭐ 押す前に document.elementFromPoint で当たるかを確かめること。
⛔ shapes() はこの巡で ReferenceError を投げた。polygon の points を自分で読め
⛔ rows() は y で並べ替える ⇒ DOM の並びとの食い違いは見えない。treeOrder() を使え
⛔ 行の操作子は [data-role="Row Title Panel"] の子孫ではない。領域（座標）で絞れ
⚠️ 実測した行の高さは 64 / 96 / 101 / 108 / 148px とまちまち。決め打ちするな
⛔⛔ 体が dist を建て直すので、絵を描く前に dist/index.html を scratchpad へ写せ

npm run gen:check          ⭐⭐ 巡の頭で必ず走らせろ。check.sh より広い時期があった
.claude/skills/spec-graph-check/
  induced.py   ⛔ CR を書く前に走らせる。巡回ごとに 1 計画・1 度の書き込み
  check.sh     29 本。⚠️ 落ちても「FAILURES ABOVE」としか出ない。
               ⭐ grep -n "^FAIL\|^PROBLEM\|^DRIFTED" が 1 行で当てる
  ⚠️ 表に行を足したら npm run words も要る —— 表示語の原稿が全数を持っている
```

---

## 9. ⛔ 名前の残り（**利用者の指示 2026-08-30**）

> **極端に短い変数は意味をあらわさない。切れの良いタイミングで、名は体を表すような変数名に改めろ。**

```
tools/probe/harness.mjs    0 件（済み）
src/                       27 件（on / is / to など属性名でもある英単語。手で直す）
tests/                     539 件（未着手）
⭐ 数え方: コメントと文字列を空白化してから数えること
```

---

## 10. ⭐ 次の巡の目標値（**始める前に自分で測り直せ**）

| # | 指標 | いま | 目標 | 根拠 |
|---|---|--:|--:|---|
| 1 | ⛔ **赤い試験** | 0 | **0** | ⭐ 0 を保て。増やすなら「仕様が先行した」と本書に書け |
| 2 | ⛔⛔ **利用者の裁定待ち**（3 節）| 4 | **0** | ⭐ **利用者に問うのが最も速い。巡の頭に 4 つ並べて出せ** |
| 3 | **`D-159` の切り分け** | 未 | **決着** | ⛔ 本当に 2 つ選べないなら `FR-034` に届く道が無い。⭐ まず群の要約バーかを測る |
| 4 | **新しく起こす図形** | 0 | **4** | 描いて見せて、利用者の裁定を得る |
| 5 | **テスト完了 の行数** | 52 | **58** | ⭐ `D-146` / `D-148` / `D-65` は試験が緑。**実物で見れば閉じる** |

⚠️ **総件数が増えることは失敗ではない。**⭐ **状態を正して 残件 が増えるのも失敗ではない。**
⛔ **失敗は、偽の状態を偽と知らずに置いておくことである。**
