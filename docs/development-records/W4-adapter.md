# W4 `Adapter` —— 開発の記録

**規則は [`docs/development-rules/05-working-method.md`](../development-rules/05-working-method.md)。書式は [README](README.md)。**

**対象**: `src/adapter`   ⭐ **機械検査がこの宣言を読み、状態表と実物を突き合わせる。**

---

## 現在地

⭐ **2026-08-26（第 4 巡）: 引継書 DESIGN-34 のうち、裁定の要らない 7 件を閉じた。仕様 1.15。**

### ⭐ 実測（⛔ 体の報告ではなく、前に立つ者が合流点で自分で走らせた）

```
typecheck 0   layers 278/278   check.sh ALL GREEN   gen:check 13/13
test 3718 通過 / 1 skip / 0 赤   e2e 17 通過   build 984.32 kB
仕様 版 1.15 ／ tables 135 ／ figures 11 ／ rows 1698 ／ uids 148
```

### ⭐ 閉じた 7 件

```
ダークの文字色    labelSvg が #111111 / #ffffff を直書きしていた。S-168 / S-169 へ
S-85 30 → 8      CR-262。導出の前提（1 日に 1 ラベル）を LF-1 が取り除いていた
生成器 2 件       null 許容オブジェクトが欄を失う欠陥（dualCursor）／ S-146 の経路
実績の掴み     GR-9 / GR-17 / GR-18 が CM-14 を書くようになった
再描画         日程上のポインタ移動がフレームを要求する（owesFrame）
IC-50/51/52     CR-263。三点のトグルと、Properties Panel の [x]
PM-1a           未着手のマーカーを S-131 で薄く描く（FR-013）
PD-191          フェードの掴み点を選択で絞る。PI-6 の署名が動いた
```

### ⛔ 体が反証した、引継書の誤り

```
「AR-1 は空でよい」          ⭐ 逆。FR-053 の MUST が「構えているものが読めること」。
                              ⛔ 要るのは辞書の節であり、空は仕様違反になる
「パネル幅は CR-246 が MUST」  ⛔ CR-246 の MUST はコマンドパレットだけ。FR-052 は沈黙
「掴めばの位相が沈黙」        ⛔ 01-04-requirements.md:2334 が定めていた。STOP の根拠 3 つとも偽
「カーソルが変わらないのは
  フレームが起きないから」      ⛔ showPointerShape は owesFrame より前で無条件に呼ばれる
「仕様が dualCursor に黙っている」 ⛔ 原稿は型付けていた。生成器の分岐の順序が欠陥だった
```

### ⚠️ 自分で踏んだ罠

```
⛔ CR-263 で触るオブジェクトを 1 つ数え落とした ——
   IC-52 の面の欄が、閉じられる面ごとの見出しの語の名簿を兼ねていた。
   ⭐ generate_display_words.py が拒んで発覚した。数の予測は当たっていた
⛔ S-85 を動かした当人が、その境界値を持つ試験 2 件を自分で直した ——
   規則 05 の「書いた者に試験を書かせない」に反する。
   ⭐ 仕様だけを読む体に独立に導出させて監査させている
```

---

⭐ **2026-08-25（第 3 巡）: 利用者の裁定 4 件とその修正 1 件を、仕様から試験まで通した。仕様 1.13。**

### ⭐ 実測（⛔ 体の報告ではなく、前に立つ者が合流点で自分で走らせた）

```
gen:check 13/13   typecheck 0   layers 277/277   check.sh ALL GREEN   audit PASS
重複 A=28（new 0）   test 3708 通過 / 1 skip / 0 赤   e2e 17 通過   build 982.65 kB
仕様 版 1.13 ／ tables 135 ／ figures 11 ／ rows 1698 ／ uids 148
裁定待ち 86 行・未裁定 66
```

### ⭐ 入った裁定

```
CR-257  打ち切った行名の説明をやめ、`…` とした。全文はパネルを広げて見る
CR-258  選択の合図を `SL-1` の 5 種すべてへ
CR-259  通知を人が消せる（`NT-8`、語は両言語とも `OK`）
CR-260  スクロール位置に端数（`S-176` ／ `S-177`。錠自身の大きさに対する比）
CR-261  依存線・基準日線・2 連カーソルは枠ではなく太さ（`S-178`、`DC-8`）
```

### ⛔ 試験の体が見つけた「仕様が正・実装が誤」2 件

```
PD-193  2 連カーソルが 1 本も描かれていない。`DC-8` 以前の穴
PD-194  コメントボックスが 1 つも描かれていない。`EP-5` が未達であり、CR-258 以前の穴
```

### ⚠️ 正直に言って、まだ確かめられていない

⛔ **合成したポインタイベントは `setPointerCapture` で落ちるので、押し下げとドラッグの経路は実物で 1 つも確かめていない。**
⚠️ **選択の見せ方、通知の `[OK]`、スクロールの端数は、試験では緑だが実物では未確認である。**

---

⭐ **2026-08-25（第 2 巡）: 実物で見つかった 19 件のうち 15 件を閉じた。仕様 1.11。**

### ⭐ 実測（⛔ 体の報告ではなく、前に立つ者が合流点で自分で走らせた）

```
precheck OK   typecheck 0   gen:check 13/13   layers 277/277（100.0%）
check.sh ALL GREEN   audit-ch5 PASS   重複 A=28（new 0）
test 3684 通過 / 1 skip / 0 赤   e2e 17 通過 / 0 赤   build 979.52 kB
仕様 版 1.11 ／ tables 135 ／ figures 11 ／ rows 1690 ／ uids 148
裁定待ち 84 行・未裁定 64
```

### ⭐ 実物で確かめた（開発サーバの実 DOM）

```
グリフと枠の隙間   16px / 24px / 32px のどの文字サイズでも 6px   ⭐ 追随を止めた
開いた直後の説明   6 つ → 0 つ
ルーラ                2026 / 2027 / 2028 / 2029 が出る（1 つも出ていなかった）
依存線の矢じり     marker 定義 1 つ、使う線 3 本（0 だった）
カーソル            バーの端で ew-resize、何も無い場所で crosshair
行と帯              見出し 6 行と帯 6 本が y = 61/133/299/430/562/694 で一致（SC-1）
```

### ⚠️ ここからは確かめられなかった

⛔ **合成したポインタイベントは `setPointerCapture` で落ちる**（`NotFoundError`）。
⚠️ **押し下げとドラッグの経路（D1 / D2 / D3 / D5 / F1）は実物では未確認である。**
⭐ **試験では緑である。**⛔ **緑な試験は動くアプリを意味しない**（規則 04）ので、利用者の確認が要る。

### ⛔ 閉じていない 4 件

```
D7  長押しの連続   ⭐ 仕様は入った（`FR-018` ／ `S-172` 1000ms ／ `S-173` 120ms）。⛔ 実装は未着手
E5  ヘルプ画面     ⛔ `entries` / `licenceText` / `copyrightNotice` / `attributions` が埋まらない。
                   ⭐ 表 T-023a/b/c/d と T-036 の語が辞書に無く、ライセンス本文も `src/` に無い
F2  通知を消す入口 ⛔ 表 T-109 の行と図形と語が要る。`RC-13` により利用者の裁定
IN-3 の「消せること」 ⛔ `×` を取った。`IN-4` の最後の階層へ移したが、`Esc` の経路は未実装
```

---

⭐ **2026-08-25: 仕様 1.06 を当て、実装と試験を回し、`49cf123` を push した。全部緑。**
⛔ **そのうえで、利用者が実物を触って 19 件の不具合を見つけた。次の巡はこれを直す。**

### ⛔ 実物で見つかった 19 件（**依存の順**）

```
値だけ    グリフと枠の隙間 12px → 6px（S-141。枠の角の R が 6px だから）
          実績バーとステータス印の掴み代 6px → 12px（S-91 / S-93）
取り消し  行の操作子の「下げ」をやめ、行名と同じ高さへ戻す（HF-5 / S-139。昨日入れたばかり）
当たり    行の操作子を押しても効かない／パネルの幅を掴めない／タスクの端でカーソルが
          変わらない／実績の端を動かせない／背景を掴んでスクロールできない
描画      ルーラに年月日が出ない／依存線の矢印が無い／選択の点線が無い／
          フェードの掴み点が無い／行の帯とラベルが 1 行ずれる（SC-1 違反）
面と経路  ダブルクリックでプロパティパネルが出ない／ヘルプ画面が出ない／
          Ctrl+A → Del で全部消せない／その通知を [OK] で消せない
ツールチップ ⛔ 開いた直後に出る・名前に当てても出る・ずらしても消えない
長押し    拡大縮小アイコンの 1 秒長押しで連続しない（⚠️ 値を持つ行が無い）
```

### ⭐ 利用者の問いへの答え

⛔ **「Whole Product の謎のオレンジ」は不具合ではない。** 起動テンプレートが `Mobile Client` に
`color: "orange"` を持っており（`Quality And Release` は `lightgray`）、`FR-042` が
「指定された色で帯を描く（MUST）」と定めている。⭐ **帯は 2026-08-25 に初めて描いたので、いま初めて見えた。**
⚠️ **ただし帯とラベルが 1 行ずれて見えるのは別の不具合である**（`SC-1`）。

### ⚠️ 裁定が要る 3 件

```
長押しの待ち始めと刻み ／ 選択の点線の見せ方 ／ 通知を消す入口（[OK]）の語と図形
⛔ 図形と確定名の新設は 表 T-026 の RC-13 と 表 T-103 により利用者の裁定である
```

⭐ **詳細は `previous-project-result/temp/handoff-DESIGN-32.md`**（git 管理外の内部メモ）。

---

⭐ **2026-08-24（第 14 巡・結）: 試験 3510 → 3673。押せる入口 33 → 55。`Agent API` が初めて動いた。**

### ⛔ 行の操作子は「壊れている」と私が誤って測った

⚠️ **前に立つ者（私）が実 DOM で「IC-59 を押しても畳まれない」と測り、欠陥として体に渡した。**
⛔ **誤りは私の側であった。** ⭐ **体が反証を持って戻ってきた:**

```
まっさらな状態で IC-59 を押す        畳まれた数 0 → 2   ⭐ 書き込みは落ちている
同じ行でもう 1 度押す                2 → 2              ← 私が測ったのはこれ
触っていない別の行で押す              2 → 31
```

⭐ **私の「前」は、自分が前の探りで既に押した「後」の姿であった。** ⛔ **`foldsUnderRow` が既に畳まれた行を飛ばし、空の束を書き込みにしないのは正しい**（`WS-4` —— 何も動かさない押下で取り消しの段を積まないため）。

### ⛔ ただし、体は本物の穴を 1 つ見つけた（**仕様の穴。コードではない**）

⛔ **`IC-59` で配下を畳むと、戻す入口が無い。**
⚠️ **`IC-58` は `HF-2` であり「その行自身の畳みを開く」なので、行自身が畳まれていなければ何も書かない。**
⛔ **表 T-109 は `HR-3`（子を全展開）・`HR-5`・`HR-6` に入口を 1 つも置いていない。**
⭐ **つまり 1 度畳むと、その行は永久に死んで見える。** ⚠️ **これは裁定が要る。**

### ⚠️ もう 1 つ、見え方の問題

⭐ **行見出しは `session.rowBoxes` の数だけ描かれる**（いまは根の 7 行）ので、
**配下を畳んでも見出しの行数は変わらない。** ⚠️ **押しても何も起きていないように見える一因である。**

---

⭐ **2026-08-24（第 14 巡）: 押せる入口が 33 → 55 になった。** 仕様 1.05。

### ⭐ 表 T-109 の 73 入口の内訳（**自分で数え直した**）

```
55  押すと動く   ⭐ 朝は 33 だった（ENTRY 18 ＋ 構え 15）。いまは ENTRY 40 ＋ 構え 15
 5  シェルが答える  IC-21 ／ IC-69 ／ IC-70 ／ IC-71 〜 IC-73（frame-loop）
 4  ⛔ そもそも入口ではない  IC-54 〜 IC-57（表 T-109 自身が打鍵と自動保存の表示だと書いている）
 9  まだ動かない  下表
```

### ⛔ まだ動かない 9 つと、その本当の理由（**一覧の見立てではなく実測**）

| 入口 | 塞いでいるもの |
|---|---|
| `IC-18` | ⭐ **欠陥ではない** —— `FR-066` は「`Agent API` が有効であるあいだ欄を出す」と定め、**RATIONALE 自身が「本要求の実装は後の節目へ回す」と書いている。**⚠️ 表 T-109 は 出す・しまう と書くが、要求は切り替えを求めていない |
| `IC-37` / `IC-38` | ⛔ **表 T-108 に `FR-034` の揃えのコマンドが 1 行も無い。** 押下を握っても組み立てるものが無い |
| `IC-41` | ⛔ **`PD-186` の裁定待ち**（透かしをしまうと書き出す絵からも消えるか）|
| `IC-45` | ⛔ **`PD-2`。** `setDualCursor` は 2 日付を同時に要求する（`IV-13`）ので、1 度目の click に置き場が無い |
| `IC-50` / `IC-51` | ⛔ **図形の一覧の開閉を持つ行がどこにも無い。** ⚠️ `FR-078` は図形を**選べること**しか求めておらず、一覧の開閉は 表 T-109 だけが持つ |
| `IC-66` | ⛔ **`QN-3` の問いを立てる者がいない**（`FR-099` が MUST で確認を求める）|

### ⭐ 5 つの「開けられない継ぎ目」のうち 3 つは塞がっていなかった

⛔ **引継書は `IF-4` 〜 `IF-8` を「5 件の裁定が塞いでいる」と書いていた。** ⭐ **踏査で 3 件が消え、1 件は逆であった:**

```
PD-182  裁定不要   FR-028 の 2 経路は「か」で結ばれている。片方で足りる
PD-184  裁定不要   AG-9 が写し方を持ち、表 T-233 は 1 行しか許さない
PD-185  裁定不要   綴りは previous-project-result/10-agent-interface に在った（探索順の 3 段目）
PD-186  ⛔ 逆     表 T-037 に行を足すと 2 つの MUST を破る。要るのは 表 T-202 の真偽 1 行
PD-183  ⭐ 本物   FR-061 が逐語で「鍵はまだ決まっていない」と書いている
```

### ⛔ 利用者の裁定が要る 2 件（**これだけ**）

```
1. PD-183  自動保存の鍵を DI-1 からファイル名を除いた形にしてよいか。
           あわせて「開いたばかりでまだ 1 度も自動保存していない文書」を
           4 つ目の状態にするか、既存の「保存済み」で見せるか
2. PD-186  透かしをしまったとき、書き出す絵からも消えるか
```

### ⚠️ 実物で確かめられていない 1 件

⛔ **アイコンの説明の待ち時間（`EZ-2`）だけは実 DOM で駆動できていない** —— 枠が非表示だと rAF が止まる。
⭐ **単体試験は当てている。** ⚠️ 枠を出せる回に人の目で見ること。

---

⭐ **2026-08-24（第 14 巡）: 実 DOM で確かめた。** ⛔ **4 巡ぶり** —— これまでは枠が非表示で rAF が止まり駆動できなかった。

### ⭐ 実測（**開発サーバの実 DOM。体の報告ではない**）

```
図形の箱   75 個すべて 12 × 12 px
           Header Commands 22 ／ Palette Commands 31 ／ Row Expander 14 ／ Row Pin 7 ／ ほか 1
           ⭐ FR-029 の「載る面によって変えてはならない（MUST NOT）」が実測で成立している
行の操作子 margin-top: 4.225px  ＝ S-36(13) × S-38(1.3) × S-139(0.25)
           align-items: flex-start（⭐ 中央揃えをやめた。HF-5 の MUST NOT）
行の鍵     data-group-id を持つ要素 7 個（＝ 行の数）
```

### ⭐ `FR-085` の欠陥が、実物で直ったことを確かめた

```
行見出しの表示   "Quality And Releas"    ← 打ち切られている
ツールチップ     "Quality And Release"   ← 全文
```

⛔ **今日の朝まで、ツールチップは打ち切られた側を出していた。** ⭐ **見つけたのは仕様駆動の試験であり、3510 件の緑は見逃していた。**

### ⚠️ 確かめられなかったこと

⛔ **アイコンの説明の待ち時間（`EZ-2`）は実物で駆動できていない** —— ブラウザ枠が非表示だと rAF が止まり、
`FT-4` の起こす `ask()` が次のフレームを描けない。⭐ **単体試験は当てている**（`uf-69` の待ち時間と場所の両条件）。
⚠️ **枠を出せる回に、アイコンに 3 秒乗せて説明が出ること・外して消えることを人の目で見ること。**

---

⭐ **2026-08-24（第 14 巡・続き）: 仕様 1.05 を当て、実装と仕様駆動の試験を 2 波回した。**

```
直近のコミット  afa06fd  ScreenPart に行の鍵を足した（型検査 6 件が赤。第 2 段の成果物）
1 つ前          5d23f0a  仕様駆動の試験と、それが見つけた欠陥の修正（全部緑）
試験            3510 → 3560（＋50）   e2e 17 通過   成果物 954.92 kB
```

### ⭐ 仕様駆動の試験が本物の欠陥を 1 件見つけた（**3510 件の緑が見逃していた**）

⛔ **`FR-085`（MUST）は「全文をツールチップで出す」と定めるのに、`tooltips.ts` は打ち切った側（`label`）を入れていた。**
⚠️ **読む人は、切れた名前に触れて、同じ切れた名前を見せられていた。** ⭐ **要求が何も買っていなかった。**
⚠️ **既存の試験が全部通っていたのは、どれも「文字列が空でない」しか見ていなかったからである。**
⛔ **その上に立っていた STOP は「全文は届かない」と書いており、`RowTitle.wholeLabel` が足された時点で嘘になっていた。**

### ⛔ STOP の記述は当てにならない（**2 波で 5 つの主張が偽と判明**）

| STOP の主張 | 実測 |
|---|---|
| `IC-71` 〜 `IC-73` は「経路が無い」 | ⛔ **偽。** `frame-loop.ts` が端から端まで配線済みである |
| `IC-69` / `IC-70` は答えられていない | ⛔ **偽。** 3 つの起こし手が問いを立て、両方の押下が答えている |
| 資源名簿の 6 行すべてに鍵が要る | ⛔ **偽。** `IC-63` 〜 `IC-66` は名簿の頭に 1 度だけ描かれる。鍵が要るのは `IC-67` / `IC-68` の 2 つ |
| 25 行が書けない | ⛔ **数が合わない。** 挙げている群は 28 行ある |
| 残る 12 行は「どの値へ動くかを 表 T-109 が言っていない」 | ⛔ **12 行とも偽。** 下表 |

### ⭐ 残る 12 行は仕様が既に決めている（**変更要求は 1 本も要らない**）

| 行 | 決めている場所 |
|---|---|
| `IC-46` 〜 `IC-49` | 表 T-109 が移り先の値を逐語で綴っている（`S-66` の 4 値） |
| `IC-4` `IC-39` `IC-40` `IC-42` `IC-43` | 型が真偽の行。`FR-049` が**真偽の行だけ**をトグルの対象と定める（MUST） |
| `IC-8` / `IC-9` | `FR-049` の「**両方を非表示にできてはならない（MUST NOT）**」が、行き先の無かった 1 遷移を禁じている |
| `IC-16` | `S-72` は 2 値しか持たず、`FR-029` が入口を 2 つ置くことを禁じている |

### ⛔ 本物に残っているもの

```
1. 行の操作子が働かない  IC-58 / IC-59 / IC-60。鍵は afa06fd で足した。配線は第 2 波
2. Esc が問いを閉じない  escapeTarget は surface しか見ず、問いは ScreenSession にある
                         S-99g が「面とは Esc の第 1 階層が閉じるもの」と定義し、
                         用語集は U-55 を面と呼ぶ。⛔ 満たされていない MUST である
3. IC-67 / IC-68        名簿の 1 人ぶんの行に data-icon が無く、入口として印が付いていない
4. IC-66                QN-3 / NT-7 の起こし手待ち
```

### ⚠️ 罠（**原因まで掴んだ**）

⛔ **隔離した作業木は `main` が古いからではなく、セッション開始時の commit で切られる。**
⭐ **門を `git merge --ff-only <sha>` で自己修復させると通る。** 12 体が 12 体ともこの手順で正しい木に乗った。

---

⭐ **2026-08-24（第 14 巡）: 説明が出る条件・図形の大きさ・操作子の縦の揃えを仕様で決め、継ぎ目を 1 メンバー開けた。仕様 1.05。**

### ⛔ いまここが赤い（**型検査 6 件。原因はここまで絞れている**）

⭐ **`RowTitle` に `controlTopOffsetPx` を足したので、埋める側がまだ書かれていない。** ⛔ **これは退行ではなく、第 2 段の成果物である** ——
**体を出す前に、触るファイルの全数が名指しで出た:**

```
製品 1 件  src/adapter/screen-renderer/row-title-panel.ts:292
試験 5 件  tests/unit/uf-39-40.test.ts:320 ／ uf-45-46.test.ts:627 ／ uf-69.test.ts:195
           uf-71.test.ts:1009 ／ uf-72-screen-part.test.ts:1178
```

⚠️ **`row-title-panel.ts` には `rowTitleFontPx(depth, settings)` が既に在る** —— 深さごとの名前の文字サイズを既に解いており、⭐ **掛けるだけで済む。**

### ⭐ 先頭で取った裁定 4 件（規則 06 の 3b。**うち 2 件は推奨どおりでない**）

```
「75% 小さく」の読み   いまの 75%。⛔ ただし対象はヘッダーとコマンドパレット、
                       かつ「可変倍率は不要」—— 推奨した em 追随ではなく固定 px になった
IN-3                   WCAG の 3 条件へ書き直す（推奨どおり）
使えない入口の Tips    ポインタ条件を付ける（推奨どおり）
HF-5 の下げ幅          ⛔ 推奨した固定 px ではなく、
                       タスクグループの文字サイズに掛ける比になった
```

### ⛔ 引継書の見立てが 2 つ外れていた（**自分で測って引き直した**）

| 引継書 | 実測 |
|---|---|
| `IN-3` は 表 T-040 | ⛔ **表 T-028 である。** T-040 は `EZ-2` のほう |
| アイコンは 18 × 18 px | ⛔ **図形の箱は 16 × 16 px。** 18 は枠と余白を含む入口の外形（30 × 26）の一部を見た数である |
| 3-1 は `IN-3` の件である | ⛔ **半分。** 利用者の訴え 2 つは、どちらも 表 T-078 の `FT-4` が配線されていないことから出ている。⭐ **`IN-3` を直しても 1 つも直らない** |

### ⭐ 訴えの実体（**実測**）

```
frame-loop.ts:1035  pointerRestedMs: 0      ← 固定値。休止が常に 0
frame-loop.ts:1036  iconUnderPointer: null  ← 固定値
tooltips.ts:217     if (command.isEnabled && !isIconHintDue) continue
```

⛔ **判定が常に偽なので、使える入口は 1 つも説明を出せない**（⭐ 辞書は 73 / 73 記入済みで、語の穴ではない）。
⛔ **使えない入口だけが、ポインタ条件なしで毎フレーム出ている** —— だから × でしか消えない。
⭐ **後者に仕様の裏づけは無い** —— `FR-029` の MUST は**掴めない端点**に向いており、**実装が発明した規則である。外すだけでよい。**

### ⭐ 閉じた罠 1 つ

⛔ **4 巡続いた「隔離した作業木が 154 コミット手前から切られる」は消えた** —— `main` が `31bdda7` を指しており、`restart` と同じである。⭐ **隔離が使える。**

### ⛔ 次の一手

```
1. 実装の段  I1 FT-4 の時計（frame-loop）／ I2 継ぎ目の消費（row-title-panel ＋ 表面）
             I3 S-138 の生成と適用
2. 試験の段  ⛔ 実装した者に書かせない。docs/spec だけを読ませる
3. 実物      ⚠️ ブラウザ枠が表示できる回に。今回は rAF が止まっていて駆動できなかった
```

---

⭐ **2026-08-23（第 12 巡）: パレットが掴んで動くようになった。仕様 1.01（`CR-236`）。**

### ⭐ 求められた 3 つのうち、1 つは既に在った（**引いてから作った**）

| 求められたもの | 実測 |
|---|---|
| ヘッダーの切替 | ⭐ **仕様も実装も既に在り、動いていた** —— `IC-7` を押すと消え、`P`（`SK-14`）で戻る。**1 行も書いていない** |
| 最小化 | ⭐ **利用者の裁定で「しまう」と同じものと定まった。** ⛔ **第 3 の状態を作っていない** |
| 上部を掴んで動かす | ⛔ **仕様が半分しか無く、実装は 0 だった** |

⚠️ **引かずに作っていたら、動くものを作り直していた。** ⭐ **この数巡で 5 回目である。**

### ⛔ 掴み帯の穴は 2 段構えだった

```
仕様   FR-053 は「ドラッグで動かせるようにすること（MUST）」と定め、IC-53 は
       「掴んで動かせることを示す」と書いているのに、⛔ 表 T-023d（掴みの全数と
       優先順位）にパレットの行が無かった —— U-43 が「全数は同表が持つ」と
       宣言しているので、無い＝存在しないことになっていた
実装   ⛔ IC-53 は DOM に 1 つも無く、commandPaletteAt は Row Area の角に
       固定されて、書き換える者が 1 人もいなかった
```

⭐ **`GR-19` を 表 T-023d の先頭へ置いた**（同表の前文が「上の行ほど優先すること（MUST）」）。
高さは `S-135a` として 表 T-206 に。⛔ **場所は覚えない**（利用者の裁定）ので行を足していない。

### ⛔ 体の割り方を 2 か所で誤った（**私の落ち度である**）

```
1. 「tools/ を触るな」と書いた —— ⛔ ところが S-135a を src/ へ運ぶ道が tools/ にしか無い
   ⇒ 2 体とも同じところで止まった。原稿の移送は前に立つ者の仕事（規則 05 の 6）なので自分で当てた
2. dom-screen-surface を誰の所有にもしなかった —— ⇒ 帯を描く側が空いたまま合流した
   ⇒ 3 体目を立てた
```

⭐ **2 体とも発明せずに止まり、「どの定数に落とすべきか」まで書いて返した。** ⛔ **零を代用値に選ばなかった**
—— 高さ 0 の帯は誰も掴めず、`GR-19` が「掴めない位置へ置けると二度と動かせない」と警告している当のものである。

### ⭐ 実測（成果物を実ブラウザで開いて測った）

```
band        x=171 y=88 w=590 h=24        ⛔ 直前は DOM に存在せず
palette     (170, 87) → (410, 247)        pointer (466,100) → (706,260)
moved by    dx=240 dy=160                 ⭐ カーソルが動いた分だけ、ちょうど
faint off   0.6                           FR-053 の「薄く透明に描く」は保たれている
page errors 0
```

### ⛔ 検査が 2 つ落ちて、2 つとも私の書いた散文だった

```
audit-ch5     ⛔ 禁止語「部品」を 2 か所へ持ち込んでいた（表 T-006b の A-17）
              ⇒ 画面の要素は「UI パーツ」と書く
描画検査       ⛔ 閉じる ** の直後が CJK で、生の ** が刊行物に出た
              ⇒ 空白を 1 つ入れる
```

⚠️ **どちらも `check.sh` の ALL GREEN をすり抜ける** —— 別の走者が捕まえた。

### ⛔ 体が測って報告した、まだ開いている穴

```
帯の見た目      GR-19 は場所と働きを言うが、色も境界も言わない（塗らずに図形だけ置いた）
帯の名前        ⛔ IC-53 の語は辞書に在るのに、IF-9 を渡る道が無い
                ⇒ 目の見えない読み手に FR-053 のドラッグが届かない
角の境界        ドラッグで画面の外へ出せる。⛔ 縛る行がどこにも無い
GR-19 と開いた面 「帯の下に何が描かれていても帯が勝つ」と S-99g / IN-4 が逆を指しうる
uf-48-input     見出しが「3 件を意図的に赤のままにする」と言うが、3 件とも緑である
```

### ⭐ 実測（⛔ 体の報告ではなく、前に立つ者が自分で走らせた）

```
precheck OK ／ typecheck 0 ／ layers OK ／ gen:check 12/12
check.sh ALL GREEN ／ audit-ch5 PASS ／ 描画検査 PASS
仕様 tables 133 ／ rows 1634（予測どおり）
test 3496 通過 / 0 赤（3478 → ＋18）  e2e 17 通過 / 0 赤  build 923.23 kB
```

---

⭐ **2026-08-23（第 11 巡）: 利用者が実物で見つけた 2 つを閉じた。仕様 1.01。**

### ⛔ 報告された不具合と、実測した原因

```
コマンドパレット      期待どおりに表示できない
プロパティウインドウ  期待どおりに表示できない
```

| | 実測した原因 |
|---|---|
| **パレット** | ⛔ **大きさをどの表も持っていなかった。** `command-palette.ts` は `⛔ NOT HELD ANYWHERE` と書いて **0** を返しており、**発明を拒んだのは正しい。** DOM の `padding:0.5em` だけが残り、⛔ **18 × 18px の箱に入口が全部詰まっていた**（`Palette Groups` は幅 0 × 高さ 554）|
| **プロパティ面** | ⛔ **`S-80` の既定 `280` が予約されるのに、`propertiesShowing` が `null` なので中身が 1 つも描かれない。** ⭐ **同じセルの注が「`FR-080` の基準環境が『面は閉じている』『その場所は日程へ渡る』と定める」と既に書いており、既定だけが食い違っていた**（値には 🔎 が付いており、測って決めた値ではなかった）|

### ⛔ 実 DOM を「見た」つもりで測り違えるところだった

⚠️ **ブラウザ枠が表示されていないと `rAF` が止まり、1 枚も描かれない。**
⛔ **その状態の DOM は「パレットも面も存在しない」と答える** —— それを実測と読めば、原因を丸ごと取り違えていた。
⭐ **成果物を実ブラウザで開いて測る捨て script に切り替えた**（`dist/index.html` を 1280×800 で開き、全部品の幾何を刷る）。

### ⭐ 直した結果（**実測**）

```
Command Palette   18 × 18   →  592 × 274      Palette Groups  幅 0 → 574
日程表の幅        820       →  1100           ページの誤り     0 件
```

### ⭐ 直し方は「値」ではなく「規則」にした

⛔ **表に大きさの行を足していない。** `FR-053` に **「中身に合わせること（MUST）。大きさを設定値の表に持ってはならない（MUST NOT）」** を置いた ——
**入口の数も語の長さも表示言語で変わるので、固定値はどちらかの言語で入り切らなくなる。**
⚠️ **大きさを持たない以上、「薄く描く」の判定を矩形の内外では行えない** ので、**ポインタの下の部品**で行うと定めた。
⭐ **面は既にそれを答えている**（`data-role` の連鎖）ので、新しい当たり判定は 1 つも増えていない。

### ⛔ 公開する形が 2 つ動いた

```
CommandPalette.box            → at { x, y }     大きさを知らない側が矩形を持つのは嘘である
CommandPalette.isPointerOver  → 廃止           pure な単位は「どの部品の上か」を答えられない
```

⚠️ **試験台が 63 件落ちた**（6 件は `S-80`、57 件は形）。⭐ **どれも仕様から書き直した。**
⛔ **1 件は消した** —— `UF-65` の「薄く描く」の件は、`FR-053` が判定を `IF-9` の側へ移したので**あの単位のものではなくなった。**

### ⛔ 体が測って報告した、まだ開いている穴

```
薄さの度合い        FR-053 は状態を言うが「どれだけ薄いか」を言わない（0.6 は据え置き）
開いた面の幅        S-80 は「開いたときに取る幅」の行を持たない（開く経路が src/ に無い）
commandPaletteAt    表 T-203 にも T-206 にも行が無い。ドラッグした場所はページを離れると消える
薄く描く件の行き先   UF-65 から外れたが、まだどの試験台も持っていない
中身が窓より大きいとき  FR-053 は中身に合わせよと言い、SC-6 はスクロールを許していない
```

### ⭐ 実測（⛔ 体の報告ではなく、前に立つ者が自分で走らせた）

```
precheck 生成物 1 件の告知だけ   typecheck 0 ／ layers OK ／ gen:check 12/12
check.sh ALL GREEN ／ tables 133 ／ rows 1632（予測どおり不変）
test 3478 通過 / 0 赤   e2e 17 通過 / 0 赤   build 922.54 kB
```

---

⭐ **2026-08-23（第 9 巡）: 利用者が実物で見つけた 2 件を閉じた。`MK-1` と `MK-5` である。**

### ⛔ 報告された不具合

```
マウスのホイール回転による縦スクロール            表 T-023 の MK-1  ... 動かない
Ctrl + Shift + ホイール回転による横スクロール      表 T-023 の MK-5  ... 動かない
```

### ⭐ 何が破れていたか（**推測ではなく、走らせて出した**）

| | 実測 |
|---|---|
| **`MK-5`** | ⛔ **横へ動かす距離を `scrollPx.x` から取っていた。** マウスのホイールは `Ctrl+Shift` を押しても**回転を縦の delta で報告する**ので、横は 0 のままだった。⚠️ **同じ経路の `notches` は既に「縦が 0 なら横を見る」落ち先を持っていた** —— 距離のほうだけが軸を字義どおりに保っていた |
| **`MK-1`（下へ）** | ⛔ **算術は正しく、モデルが量子化していた。** `S-78` は表示位置を**行まるごと**で持つ。起動時の雛形の帯は **148〜188px**、1 ノッチは **100px** —— つまり **1 ノッチは自分がいる行の中に着地し、答えがいまの値と同じになる。** ⚠️ **2000px 回すと動いた**ので、書き込みも再描画も壊れていなかった |
| **`MK-1`（上へ）** | ⛔ **先頭行より上には行が無く、「軸が答えられない」を `scrolledAnchor` が「いまの値のまま」と読んでいた。** ⭐ **日程の先頭へ二度と戻れなかった** |

### ⛔ 試験台がこれを 1 度も捕まえられなかった理由

⚠️ **`tests/unit/uf-30-31.test.ts` には `MK-1` の件が既に在った。** ⛔ **雛形の行が 36px 程度で、1 ノッチ（100px）より低かった。**
⭐ **背の高い行を持つ雛形に替えたら、同じ件が破れを捕まえるようになった。**
⚠️ **しかも 2 つの件の見出しが「⛔ DELIBERATELY LEFT FAILING」と書いたまま緑だった**（片方は `rowGap` の古い診断を述べ、`S-59` を引いていたが `rowGap` は `S-12` である）。**両方とも直した。**

### ⭐ 直し方（**発明していない**）

```
MK-5   横の距離は、装置が横を報告したときはそれを、しなければ回転の距離を使う
       ⛔ MK-1 には同じ落ち先を与えない —— 素の横回転は 表 T-023 に行が無い
MK-1   距離が 0 でないかぎり、向いた向きへ最低 1 行動かす（PD-176）
       上へ行き過ぎたら先頭行で止め、下へ行き過ぎたらいまの値のままとする（PD-177）
```

⛔ **ノッチあたりの行数を発明していない** —— 距離は装置が出したままで、**1 行ぶんの床を敷いただけである。**
⭐ **`0` は `MK-1`（縦スクロール）が唯一あり得ないと定めている答えである。**
⚠️ **`scrolledAnchor` そのものは触っていない** —— `PD-1` のパンが同じ関数を使い、`MK-7` は「ポインタが動いた距離だけ動かすこと（MUST）」と定めるからである。

### ⛔ 裁定待ちを 2 行足した（分類 B。`PD-12` / `PD-13` と同じ形）

```
PD-176  1 ノッチが 1 行より短いときに何行動くか
PD-177  上端・下端を行き過ぎたときにどこへ着くか
```

⭐ **印を `rowTurnedTo` に付けた。検査 25 が 69 行 / 未裁定 57 で緑である。**

### ⭐ 実測（⛔ 体の報告ではなく、前に立つ者が自分で走らせた）

```
precheck OK ／ typecheck 0 ／ layers OK ／ check.sh ALL GREEN
test 3479 通過 / 0 赤（3476 → ＋3）   build 922.69 kB
```

⚠️ **実 DOM では確かめられなかった** —— ブラウザ枠が表示されていないと `rAF` が止まり、画面が 1 枚も描かれない。
⭐ **代わりに、走らせた証拠を 4 つ取ってある** —— 直しを 1 つずつ壊すと、**それぞれ対応する件だけが赤くなる。**
⛔ **`MK-1` の床を外したときの赤は、利用者が報告した症状そのものである**（`expected 'g1' not to be 'g1'` —— 行が動かない）。

### ⛔ この巡が示したこと

⚠️ **3479 件の試験と 26 本の機械検査が全部緑のまま、道具として使えない不具合が 2 つ在った。**
⭐ **規則 04 の 3.「緑の試験は動くアプリを意味しない」の、いちばん高くついた実例である。**
⛔ **雛形の寸法が現実と違うと、試験は現実を測っていない。**

---

⭐ **31 ユニットすべてが受入済である。** ⛔ **W4 に赤は 1 件も残っていない。**

```
✅ 受入済  31 ／ 🔧 実装済 0 ／ ⬜ 未着手 0
試験       tests/unit 全部緑（2026-08-21 時点で 2016 / 2016）
```

⭐ **最後に残っていた `UF-61` は 2026-08-21 に閉じた** —— `FR-051` が `Panel Divider` を
「掴みやすさのために広げた帯」と呼ぶのに幅を持つ行がどの表にも無く、`screen-frame.ts` は
**幅 0 を出荷していた（どの利用者にも掴めない）。**
`CR-195` が 表 T-206 に `S-134`（境界に重なる 8px 🔎）を足し、
**生成器が `NOT_STORED_PANEL_DIVIDER_SIZES` として同ユニットへ届けるようにした。**
⛔ **数はコードに打っていない**（規則 03 の 1.）。

### ⭐ 2026-08-22: その宿題は 1 件を残して片付いた

```
CR-196  exportSvg（PI-21）        ✅  ClipboardGateway も ImageExporter から取るようにした
CR-196  xFromDay（PI-5）          ✅  私有の写し 2 つを 1 つにした
CR-197  DI-1 〜 DI-5 ／ OP-11     ✅  FileGateway ＋ notices
CR-198  先頭の BOM               ✅
CR-198  身振りの 3 件            ⬜  レーン G として残る
CR-199  拡張領域の読み書き        ✅  ⚠️ Alias が空のあいだ 1 度も交換されない（設計どおり）
CR-196  AM-13 の結線             ✅  **2026-08-23 に閉じた。赤 2 件は緑になった**（下）
```

### ⭐ 2026-08-23: AM-13 が書き出しの絵で答えるようになった

⭐ **`IF-7` の `exportScene` を「省ける欄」から「必ず答える欄」へ変えた**（`ExportScene | null`）——
⛔ **省けたことが、`AM-13` にもう 1 本の道を残していた**: 絵が届かないあいだ**画面の絵**を返しており、
それは `IO-3`（表 T-024）が定める寸法ではなく、表 T-041 の `WY-2` / `WY-3` が成り立たない。
⭐ **`frame` と同じ `null` にした** —— 寸法が決まる前（`BO-1`）という不在は 1 つであり、2 つ書く理由が無い。

⚠️ **踏んだ罠**: `AM-13` は `snapshot.frame` を見て断っていたが、要るのは絵の環境の方である。
**判定を `exportScene` へ移した** —— 2 つは必ず一緒に来るが、`frame` から作った絵は画面の寸法になる。

⭐ **`svgFromSchedule` の実行時 import が `agent-api-members.ts` から消えた** ——
辺は消えていない（`snapshot-source.ts` が `PI-19` を型で引き続き引く）。消えたのは 2 本目の道である。

⭐ **継ぎ目そのものの試験を `tests/contract/if-7-snapshot-source.test.ts` に置いた**（表 T-218 の `TS-5`）——
`frameLoop`（`CP-25` 側）が組んだ環境を `installAgentApi`（`CP-17` 側）へ渡して 1 本に繋ぐ。
⚠️ **`tests/unit/uf-27-28-29.test.ts` の bench は `IF-7` の実装者なので、絵の環境を答えるようにした**
—— ケースの本文は 1 文字も動かしていない。**「閉じた」を 2 か所で決めないため、シェルに訊いている。**

⛔ **まだ無い**: `single-html-shell.ts` は `SnapshotSource` を実装していない（`installAgentApi` を `src/` の
どこも呼んでいない）。**絵は繋がったが、`Agent API` そのものがまだ製品に据えられていない。**

### ⛔ AM-13 だけが閉じられなかった（**当時の記録**）

⛔ **`exportSvg` の `ExportScene` は 4 値を要るのに、継ぎ目 `IF-7` は 2 値しか運ばない。**
⚠️ **`screenView` は Chapter 5.2 が `AgentApiEndpoint → ScreenRenderer` の辺を引いていないので届かず、
書き出し用の絵は「パネルを閉じた状態」で描き直したものでなければならない。**

⭐ **根拠は `FR-080` の `:3104`** —— `RATIONALE` の後ろ・表 T-041 の外に独立した MUST がある:
**「プロパティパネルとコマンドパレットだけは例外とし、閉じた状態として書き出すこと（MUST）。
空白として残してはならない（MUST NOT）…… 閉じたぶんの場所は日程に使う」**
⛔ **だから現フレームを縮めた絵では作れない。**

⚠️ **「基準環境は 表 T-041 の判定語にすぎない」という読みを 2 体に refute させ、覆された。**
⭐ **正しかった半分**: `MC-6` の 1920×1080 は判定の作り物であり、比は実際の画面の幅で割る。

⭐ **閉じ方**: `IF-7` を広げ、シェル（`CP-25`）がパネルを閉じた 1 枚を組んで渡す。
⛔ **毎フレームは不可**（`ADR-001` / `MN-6`）。⛔ **`R7.4` により後から読みに行く形も不可。**

### ⭐ 2026-08-21 に仕様の側から降ってきた宿題（**当時の一覧**）

⛔ **どれも「仕様に無いので書けない」と `STOP` で止めていたものであり、いま書ける。**

| CR | ユニット | 何をするか | 今まで止まっていた理由 |
| --- | --- | --- | --- |
| `CR-196` | `UF-39` `UF-28` `UF-45` | `exportSvg`（`PI-21`）と、その 3 人の呼び手 | `PI-21` が組み立てた画面を SVG で返す口を持たなかった |
| `CR-196` | `UF-30` | `xFromDay`（`PI-5`）を使う | ⭐ **私有の `xOfDay` が 2 つのユニットに写しで在る。公開すれば 1 つになる** |
| `CR-197` | `UF-41` `UF-42` `UF-67` | 表 T-227 の `DI-1` 〜 `DI-5`（上書きの確認）| 「同じ文書」の判定規則が無かった |
| `CR-198` | `UF-36` | 先頭の BOM を受け入れて捨てる | `CN-5` が読み書きを分けていなかった |
| `CR-198` | `UF-30` | 身振りの 3 件（稼働日へ寄せない・押下で見せる・フェードの日数）| 表 T-023d が境界を書いていなかった |
| `CR-198` | `UF-41` `UF-42` | `OP-11`（一度に N 件）| 同時に渡されたときの規則が無かった |
| `CR-199` | `UF-36` | 拡張領域への書き出しと取り込み | ⛔ **`mspdi-codec.ts:1247` の `STOP` がまさにこれである** |

⚠️ **`replaceDocument`（`PI-8`・`UF-8` / `UF-9`）だけは、まだ全部は書けない** ——
⛔ **差し替えた文書が持つ版数が未決である**（分類 `D`）。**入口は書けるが、版数は `STOP` を残すこと。**

### ✅ 繰り越しは閉じた（2026-08-22）

`layoutFromSchedule` が `scrollGroupId` を一度も適用していなかった件は、`S-78` の行が
`regions.rowArea.y` に来るよう帯全体をずらす形で閉じた。⭐ **`contentHeight` はずらす前に測るので
`FR-055` の全体表示は変わらない。**

### ⚠️ 次の波（W5）が塞いでいるもの

⭐ **ヘッダーもパネルも出るようになった**（`data-role` 45 / `button` 78。W5 の記録を見よ）。
⛔ **残るのは `FT-1` の結線だけである** —— `DomInputSource` が起きておらず、**押しても何も起きない。**
⚠️ **`tests/usecase` が書けないのはこれが理由である。**

---

## 状態表

⭐ **段**: ⬜ 未着手 → 🔧 実装済 → 🧪 試験済 → ✅ 受入済
⛔ **✅ になるまで、他のユニットがそれに依存してはならない**（05 の 4.）。
⭐ **例外は型だけ** —— 公開エントリの型が型検査を通れば、依存側は書き始めてよい（05 の 3.）。

### `agent-api-endpoint` —— AgentApiEndpoint（PI-17）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-27 | `agent-api-endpoint.ts` | **公開エントリ** | non-pure | PI-17 | 🧪 試験済 |
| UF-28 | `agent-api-members.ts` | 内部 | non-pure |  | 🧪 試験済 |
| UF-29 | `snapshot-source.ts` | 内部 | n/a |  | 🧪 試験済 |

### `clipboard-gateway` —— ClipboardGateway（PI-24）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-45 | `clipboard-gateway.ts` | **公開エントリ** | non-pure | PI-24 | ✅ 受入済 |
| UF-46 | `clipboard.ts` | 内部 | n/a |  | ✅ 受入済 |

### `document-codec` —— DocumentCodec（PI-20）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-34 | `document-codec.ts` | **公開エントリ** | pure | PI-20 | ✅ 受入済 |
| UF-38 | `app-shell-source.ts` | 内部 | n/a |  | ✅ 受入済 |
| UF-37 | `embedded-html-codec.ts` | 内部 | semi-pure-b |  | ✅ 受入済 |
| UF-35 | `json-codec.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-36 | `mspdi-codec.ts` | 内部 | pure |  | ✅ 受入済 |

### `file-gateway` —— FileGateway（PI-22）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-41 | `file-gateway.ts` | **公開エントリ** | semi-pure-b | PI-22 | ✅ 受入済 |
| UF-42 | `file-store.ts` | 内部 | n/a |  | ✅ 受入済 |

### `image-exporter` —— ImageExporter（PI-21）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-39 | `image-exporter.ts` | **公開エントリ** | semi-pure-b | PI-21 | 🧪 試験済 |
| UF-40 | `rasterizer.ts` | 内部 | n/a |  | 🧪 試験済 |

### `input-command-translator` —— InputCommandTranslator（PI-18）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-30 | `input-command-translator.ts` | **公開エントリ** | pure | PI-18 | 🧪 試験済 |
| UF-31 | `input-source.ts` | 内部 | n/a |  | 🧪 試験済 |

### `screen-renderer` —— ScreenRenderer（PI-37）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-60 | `screen-renderer.ts` | **公開エントリ** | pure | PI-37 | ✅ 受入済 |
| UF-62 | `app-header-items.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-65 | `command-palette.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-68 | `dialogue-field.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-67 | `notices.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-66 | `open-modals.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-64 | `properties-panel.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-63 | `row-title-panel.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-61 | `screen-frame.ts` | 内部 | pure |  | ✅ 受入済 |
| UF-70 | `screen-surface.ts` | 内部 | n/a |  | ✅ 受入済 |
| UF-69 | `tooltips.ts` | 内部 | pure |  | ✅ 受入済 |

### `svg-renderer` —— SvgRenderer（PI-19）

| ユニット | ファイル | 種別 | 純粋性 | 公開 | 段 |
| --- | --- | --- | --- | --- | --- |
| UF-32 | `svg-renderer.ts` | **公開エントリ** | pure | PI-19 | ✅ 受入済 |
| UF-33 | `svg-surface.ts` | 内部 | n/a |  | ✅ 受入済 |

---

## 記録

⭐ **追記のみ。過去の行を書き換えない。**

| 日付 | 種別 | 内容 |
| --- | --- | --- |
| 2026-08-18 | 準備 | ⭐ **W4 が読む値はすべて原稿から生成済み** —— `COLUMN_DEFAULTS` / `NOT_STORED_SIZES` / `NOT_STORED_LIMITS` / `DEFAULT_CALENDAR_VALUES` / `DATE_COLUMNS`。**数を写す作業は無い** |
| 2026-08-18 | 実測 | ⛔ **`index.html` が読む `single-html-shell.ts` は中身が無い** —— いまアプリは何も描かない。実物確認は縦線が通るまでできない |
| 2026-08-18 | 未決 | ⚠️ **裁定 J（3 件）が未回答** —— `setHighlightBoxStrokeColor`（`null` に戻す入口を置くか）／ `setTaskGroupCollapsed`（選択前提を `Agent API` がどう呼ぶか）／ `createResource` ほか 4 件（面が無い）|
| 2026-08-18 | 裁定不要 | ⭐ **J-1 は仕様が既に答えていた。** `FR-019` の RATIONALE が「**ハイライトボックスの線色を指定でき、指定が無ければ注記用の固定色で描くこと（MUST）**」と定め、`null` の意味を「既定色で描く」に固定している。表 T-108 は自らを命令の全数と宣言し（`tbl-glossary.md:341`）、**引数は `src/` の公開エントリが正**（同 `:342`。CR-146 の裁定）。⭐ **`src/use-case/edit-document/edit-annotation.ts:97` が既に `strokeColor: string \| null` を宣言し、`'transparent'` を拒む**（`FR-019` の MUST NOT）。⛔ **独立した reset 命令は要らない** —— `FR-007`（`:1461`）が「戻せること（MUST）」の射程を `Task` の線色・塗り色と行の色に明示的に限っており、注記を含まない。**だから `CM-23` / `CM-31` は在り、注記の reset は無い** |
| 2026-08-18 | 裁定不要 | ⭐ **J-2 は前提そのものが誤っていた。** `setTaskGroupCollapsed` は選択を前提にしない。`src/use-case/edit-document/edit-task-group.ts:83` が `{ kind, groupId, collapsed }` を宣言し、試験も通っている。**`FR-085`（`:1270`）が「表 T-015 の `HR-3` 〜 `HR-5` … が前提にしている『選ばれた行』は本規則が与える」と明記**し、しかもその選択は 表 T-023c の描画領域の選択とは**別の集合**である。⛔ **`UN-9`（選択は取り消しの対象外）と `UN-14`（畳みは対象）が制度的に分けており、選択が `DocumentCommand` になりえない。** ⭐ **`Agent API` は `AM-7` `applyCommands` に値を渡すだけ**（表 T-107 に選択を変えるメンバは無い）。**複数行に効く入口（`HR-1` 〜 `HR-4` / `HF-3`）は `CM-33` を N 本にして 1 回に束ねる** —— 原子性は `AG-3`、履歴 1 段は `FR-031` が既に持つ |
| 2026-08-18 | 罠 | ⛔ ⭐ **`SvgRenderer` が色に 1 つも届かない** —— `_source/components.json` の辺は `ScheduleGeometry` / `ScheduleLayout` / `DocumentSettings` / `Selection` の 4 本だけで、**`Schedule` への辺が無い。** ところが `themeHue` は `Project` にあり（`AT-19`。表 T-052 の `DR-5` が「見せ方の群が持ってはならない（MUST NOT）」と定める）、`TaskVisual` の線色・塗り色と `TaskGroup.color` も日程データの群にある。⚠️ **幾何も色を運んでいない**（`schedule-geometry.ts` / `schedule-layout.ts` に `color` の綴りが 0 件。自分で数えた）。⭐ **5.1 が「描き方は `Adapter` が持ち、`layoutEngine` は座標までしか持たない」と書いているので、幾何に色を載せる案は仕様の言葉に反する。** ⛔ **`UF-32` を書く前に CR で辺を 1 本足す必要がある**（原稿を触るので図の再生成を伴う）|
| 2026-08-18 | 食い違い | ⛔ **J-3 は「面が無い」のではなく、仕様の中で 2 つの文が食い違っている。** 件数も誤りで、一次資料（`CR-150:127`）は「`createResource` ほか **3** 件」であり引継書の「ほか 4 件」は写し違い。さらに `CM-41` `setResourceName` には面が在る（表 T-023 の `MK-13` ／ 表 T-023d の `GR-11` ／ 表 T-036 の `SK-19`）。⭐ **残るのは `CM-40` / `CM-44` / `CM-45` の 3 件である。** ⛔ **食い違いの中身**: `UC-003` 手順 3（`:573`）が「日付・形状・色・線の太さ・**担当者**のいずれかを変える」と書き、`FR-006` の ORIGIN（`:1371`）が「UC-003 手順 1 〜 3」を名乗るのに、**表 T-016 の `PR-16`（`:1398`）が「読み取り専用」と書いている。** しかも `FR-006` の STATEMENT は「**同表が読み取り専用と記した項目を除いて**編集できるようにする」なので、`PR-16` の 1 語が面を閉じている。⭐ **決め手は要望の入力である** —— `previous-project-result/user-order.md:178`（`PR-16` の直接の祖先の行）が「**割当の追加・編集・解除ができる**（項 69）」と書き、項 69（`:198`）が「担当者を `GRS` 側で追加・編集・解除でき」と書く。**したがって面はプロパティパネルであり、「発明」ではなく「採用」である** |
| 2026-08-18 | 実測 | ⚠️ **エージェントの引用は行番号がよくずれる。** 反証の 9 巡が挙げた食い違いはほぼ全部が行番号の 1〜25 行のずれで、**逐語の中身は正しかった。** ⛔ **ただし 1 件だけ本物があった** —— 「前プロジェクトに面が無いので発明になる」という判定が誤りで、`user-order.md:178` に面が在った。⭐ **観点を変えた 3 体で反証させたのが効いた**（要求本文 / 表と図 / 先例と参考）|
| 2026-08-18 | 裁定 | ⭐ **利用者の裁定を 3 件受けた。** ①色は案 (a)（`SvgRenderer → Schedule` の辺を足す）。②担当者の面は 2 つ —— 担当ラベル（表示モード時。未設定は `-`、`Del` か `-` で解除）とプロパティパネル（UID はドロップダウン＋部分一致検索）。**見かけのキーは名前、実際のキーは UID。名簿に無い名前は追記し、同名が複数 UID にあるときは小さい UID を採る。** ③初期文書はバンドル済み `GRS JSON`（一般的なソフト開発・3 年・7 フェーズを矢羽根・L1〜L5・**100 行 / 1000 タスク**・`schemaVersion` は日付）|
| 2026-08-18 | 罠 | ⛔ ⭐ **`.drawio` の後ろ指し（XML 注記）が、図を 5 枚とも空にしていた** —— 66KB → **437 バイト**（`width="37px"`）。⚠️ **draw.io は「何も描かない」を返しながら終了コード 0 を返す**ので `build.py` の `run()` は気づかない。⛔ **その状態で機械検査 25 本は ALL GREEN だった** —— 検査 16 は `.svg` を何とも突き合わせていない。⭐ **切り分け**: 注記を外すと 51974 バイト、`mxGraphModel` の属性にしても 51974 バイト（注記なしと同じ）。**属性へ移した。** ⚠️ **`build.py` の docstring は「`<root>` の中なら 1 バイト違わず出る」と実測を主張していたが、その実測はもう成り立たない**（exe は 2026-05-28 付）。⭐ **`assert_drawn` を生成器に足し、わざと壊して落ちることを確かめた** |
| 2026-08-18 | 罠 | ⚠️ **`build.py` は `overview.json` の `$comment`（生成物の帯）を書いていなかった** —— 手で足された帯が再生成で消え、**検査 21 が落ちた。** ⭐ **検査 21 が入ってから誰も再生成していなかったので、いま初めて露見した。** `build_overview` が `$comment` を先頭鍵として書く形に直した |
| 2026-08-18 | 適用 | ✅ **CR-185 適用**（`SvgRenderer → Schedule` の辺）。辺 86 → 87、Chapter 5 の散文は 1 文字も動かず、数（124/11/1534/145）も不変 |
| 2026-08-18 | 適用 | ✅ **CR-186 適用**（担当者の面）。⭐ **表 T-225 を新設**（`AS-1` 〜 `AS-10`）し、`PR-16` の「読み取り専用」を外した。**`FR-006` と `FR-029` は 1 文字も直していない** —— 前者は「同表が読み取り専用と記した項目を除いて」と書いており、後者は「掴む面と値として編集する面の併存」を既に対象外としているからである。⭐ **予測と実測が一致**: tables 124→**125**、rows 1534→**1544**（`AS-` の 10 行）、uids **145**（不変）、重複 **new 0** |
| 2026-08-18 | 実測 | ⭐ **裁定 8（同名なら小さい `uid`）は発明ではなかった** —— `FR-059` が担当ラベルの並びで既に「同名は `UID` の昇順」を採っている。⛔ **一方 `MG-5`（合流は同名を統合）とは読者が必ず混同するので、`MG-5` の側に境界を 1 文書いた** |
| 2026-08-18 | 実測 | ⛔ **`assigneeVisible` の既定が `false` なので、既定の状態では担当者を編集する面が 1 つも無く、`FR-049` の MUST に違反していた** —— 裁定を待たずとも直す理由が既に在ったことになる。⚠️ **仕様書に入力ウィジェットの形を定めた記述は 0 件で、`AS-5`（ドロップダウン＋部分一致）が最初である** |
| 2026-08-18 | 適用 | ✅ **CR-187 適用**（初期テンプレートの置き場・形・版の書式）。⭐ **表 T-226 を新設**（`TP-1` 〜 `TP-8`）し、`FR-027` に業種の例外を理由つきで置き、`FR-073` に `schemaVersion` は日付・辞書順で判別する規則を置いた。tables 125→**126**、rows 1544→**1552** |
| 2026-08-18 | 罠 | ⚠️ **表 T-226 の `TP-5` に深さの上限を値と経路ごと書いたら、重複検出が `new 1` を出した** —— 拾われたのは「`_assets/tbl-settings.md` の 表 T-nnn が持つ」という**規約そのもの**（19 か所）で、私の 1 行が 19 個目だった。⭐ **規則の持ち主（`FR-004`）を指す形に直したら基準線（`A=25 new 0`）へ戻った。** ⛔ **基準線に足して黙らせない** |
| 2026-08-18 | 罠 | ⛔ ⭐ **テンプレートに `$comment` の帯を置けない** —— `GRS JSON` スキーマはルートを `additionalProperties: false` で閉じており、**帯を置くと `FR-027` が「同じ検証器を通せる」と言っている当の検証器に落ちる。** ⭐ **後ろ指しは生成器と `gen:check` が持つ形にした** |
| 2026-08-18 | 停止 | ⛔ ⭐ **`.json` は書けずに止まっている。** 生成器は 100 行 / 1000 タスクを組み立て、スキーマに**8 個を除いて通った**（`dualCursor` / `exportCanvas` / `importMaxDate` / `importMinDate` / `pinnedGroupIds` / `planActualGuidePattern` / `rulerFont` / `rulerHeight`）。⚠️ **`generate_entity_types.py` が自ら「原稿が機械可読な値として述べていないので推測せずに生成しない」と書いている。** ⛔ **`FR-024` は 97 個すべてを MUST で求めるので、欠けた文書は不正である。** ⛔ **生成器へ数を書き写さない**（規則 03 の 1.）。⭐ **出口は裁定 ㊸（CR-175）—— 生成器の射程を広げる。** ⚠️ **2 個は値ではない**（`dualCursor` は `null`、`pinnedGroupIds` は空）。⭐ **生成器は不正な文書を書かずに落ちる。わざと走らせて確かめた** |
| 2026-08-19 | 適用 | ✅ **CR-188 適用**（原稿が言えなかった 8 個の値）。⭐ **裁定 ㊸ をそのまま当てた** —— 機械セルに `quote` / `sep` / `parts`、散文セルに `index` / `from` / `times` / `plus`。⛔ **行を 1 つも足していない**（`S-81` を 2 行に割る案は、`FR-080` ほかの指し先の意味が変わるので捨てた）。⭐ **`tbl-settings.md` の印字は 1 バイトも変わっていない**（`{"ja": …}` があれば印字器が早期に返すため。差分 0 を確認）。⚠️ **`rulerFont` / `rulerHeight` は数を書かず、導出の規則を原稿に持たせて生成器が評価する**（14 / 48 が出て、印字の「M = 14 / M = 48」と一致）|
| 2026-08-19 | 実測 | ⭐ **`startup-template.json` が出た。`GRS JSON` スキーマに valid。** 100 行 / 1000 タスク / 深さ 5 / 最上位 7 行の森 / 1 行あたり 3〜24 本の 14 通り / 節目 7・矢羽根 7 / 2026-04-01〜2029-03-30。**`IV-1` `IV-2` `IV-4` `IV-5` `IV-6` `IV-7` `IV-8` `IV-10` `IV-14` `IV-17` を自分で数えて全通過** |
| 2026-08-19 | 実測 | ⭐ **値が本当に届くことを確かめた**（規則 04 の 2.）—— 原稿の `exportCanvas` を 1600→1601 にすると成果物が 1601 になり、戻すと戻った。⭐ **歯止めも壊して確かめた** —— 成果物の `themeHue` を書き換えると `startup:check` が落ち、戻すと OK になった |
| 2026-08-18 | 未決 | ⚠️ **`FR-027` の初期テンプレート（`Document` の値）を持つユニットが 表 T-075 の 71 行に無い。** `chooseStartupDocument` は値として**受け取る**だけである。⭐ **縦線は保存経路を 1 本も結線しないので、開発用のリテラルで進めてよい**（外へ出て行かないので覆すコストは 1 か所）。⛔ **本物のテンプレートを書くと同時に保存経路も結線する、はやらない**（規則 06 の `D` と `H`）|
| 2026-08-19 | 実装 | ⭐ **繰り越し (l) と (y) を閉じた** —— `TaskPlacement` が `milestoneGlyph` を運ぶようになり、`schedule-geometry.ts` が **8 図形すべて**を描く。⭐ **8 つとも同じ円に内接させた** —— `S-48` の備考が「この値が ☆ の面積を決め、`milestoneGlyph` の面積順に影響する」と言えるのは、8 つが大きさを共有しているからである。⚠️ **☆ の腰は `S-48` から読む**（数を書き写していない）。⭐ **実 DOM で 7 個が 3・4・4・5・6・10・24 頂点になったのを確認。** 受入済の 87 件も緑のまま。⛔ **嘘になったファイル冒頭の注記を直した**（「◇ しか描かない」「glyph を読まない」）|
| 2026-08-19 | 実装 | ⭐ **`DocumentCodec` の JSON 側を書いた**（`UF-34` / `UF-35`）。`documentFromJson` は**形だけ**を見る —— ⛔ **中身の規則は `FR-023` が `ValidateImportedDocument` に置いており、3 経路が同じ検証を共有するからである。** ⭐ **シェルの型の言い切りが消えた** —— テンプレートは codec を通って `Document` になる。⚠️ **MSPDI と単一 `.html` は stub のまま**（表 T-063 の `UT-5` が 3 つを別々の正に紐づけているので、1 つだけ先に着地できる）|
| 2026-08-19 | 実測 | ⭐ **codec をわざと壊して確かめた**（規則 04 の 2.）—— JSON でない／根の鍵が欠ける（欠けた鍵を名指す）／`DR-1` に反する 6 個目の鍵、をすべて拒み、往復も通った。⛔ **その試験は残していない** —— 書いた者が試験を書いてはならない（規則 04 の 1.）ので、仕様だけを読む別の者に書かせる |
| 2026-08-19 | 発見 | ⛔ ⭐ **仕様だけを読ませた試験が、`svgFromSchedule` の欠陥を 6 件見つけた。** 規則 04 の 1. の存在理由そのものである。① **表 T-017a の `CT-3`（実績 ÷ 予定 ≧ 3:1）違反** —— 明度 46%/30% は **1.79:1** で、`FR-007` が同表を MUST にしている。② **重ね順が逆** —— 依存線を最背面に書いていたが、`ZO-4` はバーより前面である。③ **進捗マーカーを描いていなかった**（`ZO-3` / `EP-5`）。④ **名称ラベルを描いていなかった**（`ZO-5` / `EP-5`）。⑤ **`HighlightBox.strokeColor` を無視していた** —— `FR-019` の「線色を指定でき」の半分が未実装だった。⑥ **`themePreference`（明暗）に追従していなかった** —— light と dark で出力が 1 バイトも違わなかった（`FR-041`）。⭐ **6 件とも直した。** 明度は測って選び直し、**light 72%/27% で色相 214 の 4.81:1、dark 60%/23% で 3.74:1** —— ⭐ **`tbl-settings.md` §5 が記録している実測値と一致する**。どの色相でも `CT-3` を満たす |
| 2026-08-19 | 実測 | ⭐ **試験の取り出し方だけが萼ちた 2 件は、書いた側へ差し戻した** —— ⛔ **自分で直すと規則 04 の 1. の趣旨を壊すからである。** 独立の再検証も **大域の重ね順**で一致し、しかも良い論拠を出した —— **表 T-020 の下の 2 文は、別の Task の要素どうしが出会う場面でしか意味を持たない**。取り出しは位置ではなく入れ子の素性へ直され、**期待値は 1 文字も緩んでいない** |
| 2026-08-19 | 実装 | ⭐ **`ScreenRenderer` の継ぎ目と型を書いた**（`UF-70` 88 行 ／ `UF-60` 717 行）。⛔ **9 本の内部ユニットは stub のまま** —— ⭐ **`UF-61` 〜 `UF-69` それぞれの関数名・引数・戻り値・従う規則を、`screen-renderer.ts` にコメントで固定してある。** ⚠️ **`screenViewFromRegions` は何も計算しない** —— 全部空で返し、`isFullScreen` / `documentTitle` / `autosaveStatus` の 3 つだけ素通しする。⭐ **その旨をファイルが自分で述べている**（「NOT THE FINISHED VIEW … which is what is true of this build」）。⚠️ **未使用引数に `_` を付けてある** —— 結線のときに外れる印である |
| 2026-08-19 | 決めた | ⭐ **継ぎ目 `IF-9` は 2 メンバ** —— `showScreenView(view: ScreenView): void` と `readDialogueInput(): DialogueInput \| null`。⛔ **記述は値であって節点の木ではない**（`SvgSurface` が文字列を取るのと同じ理由。`UF-60` は `pure`）。⚠️ **`author` と `settledAt` は値として渡す** —— 誰が話したかと時計は `Framework` のものである（`LY-5` ／ `CS-1`）。⛔ **順序は継ぎ目に無い** —— `AG-11` の番号は `logWithMessage`（`PI-33`）が振る |
| 2026-08-19 | 決めた | ⚠️ **`ScreenView` は `Time Ruler`（`U-19`）を描かない** —— 表 T-075 の `UF-61` 〜 `UF-69` のどれにも現れないからである。⭐ ファイル冒頭に大きく書いた |
| 2026-08-19 | 未決 | ⛔ **仕様が沈黙している 9 件が出た**（分類つき）。⚠️ **`pending-decisions.md` にはまだ載せていない** —— 検査 25 は「未裁定の行なのに印が無い」を落とすので、⭐ **印を付ける回に一緒に載せる。** 中身: ①`IF-9` のメンバ（`E`。ただし CR-146 が宣言側に委ねている）②表 T-109 の 61 行が `src/` へ届いていない（`A`。生成器の射程を広げれば解ける）③翻訳語の置き場（`FR-038` が MUST だが辞書の表が無い）④浮遊パレットの位置とプロパティパネルの表示中身（`D`。⭐ **保存しない側に決めた**）⑤矩形を持つ UI パーツの線引き ⑥`OpenModal` が単数 ⑦`Notice` を上げる側と出す側で 1 型にした（`C`）⑧空の発話を拒む規則が無い ⑨`Hidden Group Tab`（`U-29`）はどの要求も定めていない |
| 2026-08-21 | 実測 | ⭐ **CR-A（アイコンの裁定）の測り。** `impact.py T-109` —— 表 T-109 は 61 行、指している要求は `FR-029` **1 件のみ**（3 箇所）、2 次は 9 件、要求以外は 4.3 節と A.3 変更履歴。`induced.py T-109 F-019 FR-029 FR-096 FR-099 T-026` —— ⚠️ **`F-019` は仕様のオブジェクトではない**（種は 6 個中 5 個が解決）。辺 3・**閉路 0**。指し先を足した先（`T-108` `T-103` `T-036` `T-028` `FR-025`）まで広げて 10 種でも辺 4・**閉路 0** —— ⭐ **1 か所ずつ順に書いてよい** |
| 2026-08-21 | 変更要求 | ⭐ **CR-191 を書いた**（`change-request/CR-191-the-icons-nobody-had-drawn.md`）—— 裁定 D2 の両半分（表 T-109 に 7 行 ／ 図 F-019 に 12 図形）と、図案セッションが見つけた食い違い 5 件をまとめて閉じる。⛔ **予測**: tables 126（不変）／ figures 11（不変）／ rows 1552 → **1559** ／ uids 145（不変）。表 T-109 は 61 → **68** 行、図 F-019 は 57 → **68** 図形、`viewBox` 240 → **284**。⚠️ **生成物 `icon-shapes.json` は本 CR に入れない** —— `src/` ・`package.json` ・`check-provenance.py` を触る作業であり、仕様書の外である |
| 2026-08-21 | 適用 | ✅ **CR-191 を当てた**（オブジェクトごとに 1 回、計 11 か所）—— 図 F-019 は 68 図形 / 6 行 / `viewBox` 284、表 T-109 は 68 行、`FR-029` と `FR-099` と `RC-13` の散文、変更履歴 版 0.68。⭐ **`npm run gen` が `icon-roster.json` を 61 → 68 で書き直し、`gen:check` は 5 本とも OK。** ⛔ **手で書いた生成物は 0 件** |
| 2026-08-21 | 罠 | ⛔ **引き渡しが書いていた「既存 57 個の外枠 x 3 〜 21.5 ／ y 3 〜 21」は左端が誤り** —— 自分で図形の座標を取り出して測ると、⚠️ **`IC-8`（x 2.90）・`IC-20`（x 2.50）・`IC-35`（x 2.40）が x 3 より左へ出ている。** ⭐ **実測の外枠は x 2.40 〜 21.50 ／ y 3.00 〜 21.00 で、新しい 12 個（x 3.00 〜 21.00 ／ y 4.00 〜 20.50）はその内側に収まる。** ⚠️ **測る道具も 1 度間違えた** —— 円弧を端点で近似すると半円 2 つの膨らみを落とし、`q`（2 次曲線）を知らない書き方だと数値が前の命令に食われて偽の逸脱が 5 件出た。**弧は中心を解いて 64 点で標本化し、`q` を教えてから測り直した** |
| 2026-08-21 | 検査 | ✅ **CR-191 の検査が全部緑になった** —— 機械検査 26 本 ALL GREEN、`audit-ch5` PASS、刊行物の描画 PASS（生の `**` 0）、`gen:check` 5 本 OK、`typecheck` 通る、`npm test` 1956 / 1957。⭐ **数の予測は 4 つとも的中**（tables 126 ／ figures 11 ／ rows **1559** ／ uids 145）。⛔ **赤いのは `tests/unit/uf-61.test.ts` の 1 件だけで、別件である** |
| 2026-08-21 | 発見 | ⛔ ⭐ **原稿を動かした瞬間、仕様駆動の試験が 10 件落ちた。** ⚠️ **製品コードは正しかった** —— `UF-65` / `UF-66` は生成された `icon-roster.json` を読むので、原稿が動けば新しい答えを返す。**落ちたのは試験が抱えている表の写しのほう**（`T_109_PALETTE` に `IC-62` が無く、`T_109_MODAL_PLACEMENTS` が `Resource Roster` を 1 行も知らない）。⭐ **原稿の移送として直した。製品コードは 1 行も触っていない。** ⚠️ **1 件だけ主張を狭めた** —— 「`U-49` には表 T-109 の行が 1 つも無い」は偽になったので落とした（配置は同じ describe の 1 つ目の case が全面で検査する）。⛔ **これは規則 04 の 1. の「期待値をコードに合わせて書き換える」ではない** —— 写しの側が原稿に追いついていなかった |
| 2026-08-21 | 判断 | ⚠️ **重複検出が `new 2` を出した**（`IC-58` ＋ `IC-59` ／ `IC-67` ＋ `IC-68`）。⭐ **基準線へ足した** —— ⛔ **「基準線に足して黙らせない」に触れないことを確かめてから足した。** 同じ表の兄弟行が既に 8 群も基準線に載っており（`IC-12` ＋ `IC-13` ほか）、**規則が 2 度書かれているのではなく 1 つの操作子の 2 状態である。** ⛔ **語を捻って検出器をよけると、兄弟だと読めなくなる。** ⭐ **副産物**: `IC-35` ＋ `IC-61` の群は消えた |
| 2026-08-21 | 実測 | ⭐ **わざと壊して落ちることを確かめた**（規則 04 の 2.）—— 散文の「68 行ある。」を 69 にすると `generate_icon_roster.py` が行数の食い違いを名指しして終了コード 1 を返し、控えから戻すと OK に戻った。⚠️ **控えは `ls -la` で大きさを見てから戻した** |
| 2026-08-21 | 実測 | ⭐ **CR-B（裁定 D1）の測り。** `impact.py T-065` —— 表 T-065 は 9 行、⚠️ **指している要求は 0 件**（表が要求から浮いており、指しているのは 5.3 / 5.5 / 5.6 / A.3 の散文だけ）。`induced.py T-065 T-064 FR-071 FR-083 FR-085 T-041` —— 種 6/6 が解決、辺 0・**閉路 0**。⭐ **1 か所ずつ順に書いてよい。** ⛔ **要求が 0 件なので、継ぎ目を広げても要求の側は 1 行も動かない** —— 動くのは 表 T-065 の 1 セルと、`src/` の宣言・実装・消費者である（CR-146 が署名を `src/` に委ねている） |
| 2026-08-21 | 変更要求 | ⭐ **CR-192 を書いた**（`change-request/CR-192-the-entries-nobody-could-press.md`）—— 裁定 D1（`IF-9` に「点 → 入口」を 1 メンバ）。⛔ **仕様書側は 表 T-065 の `IF-9` の 1 セルと直後の散文 1 文だけ** —— CR-146 が署名を `src/` に委ねているからである。⚠️ **`_source/components.json` に辺を 1 本足す**（`InputCommandTranslator → ScreenRenderer`。88 → 89）—— **型を引くことが辺であり、`InputCommandTranslator → ItemHitArea` が同じ形の前例である**（`hit` も呼び手が解いて渡す）。⛔ **予測**: tables 126 ／ figures 11 ／ rows 1559 ／ uids 145 は**すべて不変**。⭐ **8 件のうち閉じるのは 2 件**（#6 パレットの構え・#1 の半分）、**閉じないのは 5 件**（#2 #3 #4 #5 #7 —— いずれも「面 → 矩形」か `ScreenView` のメンバの話で、裁定 D1 が裁いた問いではない）、**#8 は D4 のもの** |
| 2026-08-21 | 適用 | ✅ **CR-192 の仕様書側を当てた**（オブジェクトごとに 1 回、計 4 か所）—— 表 T-065 の `IF-9` の 1 セル、同表の下の散文 1 文、`_source/components.json` の辺 1 本、変更履歴 版 0.69。⭐ **`build.py` の再生成で動いたのは 図 F-017（`view-startup`）だけ** —— **図 F-013 は 1 バイトも動かない**（Adapter → Adapter の対は既に在るので、層と層の間の矢印は増えない）。⚠️ **`docs/review/components/components.md` も 88 → 89 で書き直された** |
| 2026-08-21 | 実装 | ⭐ **CR-192 を `src/` へ当てた**（4 ユニット）—— `UF-70` が `ScreenPart` と `readScreenPartAt` を宣言、`UF-60` が再公開、`UF-71` が `elementFromPoint` から根まで登って答える（内側の `data-icon` ＋ **いちばん外側**の `data-role`）、`UF-30` が `PointerPress.on` で受ける。⭐ **`FR-083` の `SP-1` 〜 `SP-4` が通った** —— 構え（`ScreenState`）と形状変更（`setTaskVisualShapeKind` ／ `setTaskVisualMilestoneGlyph`）の両方。⛔ **混在した選択は絞り込まない** —— `SP-3`（全部変える）と「跨いではならない（MUST NOT）」のどちらが勝つかを仕様が言っていないので、束を組んで `CM-20` に拒ませる（`AG-3` で原子的）。**絞ると仕様が言っていないことを決めることになる** |
| 2026-08-21 | 発見 | ⛔ ⭐ **表 T-023a の面の MUST が実際に破れていた** —— 「判定順序を適用するのは日程の描画領域だけ（MUST）」なのに、**浮遊パレット・開いている面・知らせ・対話欄は `ScreenRegions` の矩形を 1 つも持たない**ので `regionAtPointer` はその上の点を `rowArea` と答え、**面の上を押すと下の日程表で範囲選択が始まっていた。** ⭐ **`press.on !== null` を 表 T-023a より先に読むことが、その MUST の判定そのものになる**（`commandFromInput` と `selectionFromInput` の両方） |
| 2026-08-21 | 実測 | ⭐ **わざと壊して確かめた**（規則 04 の 2.）—— 捨てる実験台で 5 件（入口の上／面の上・入口の外／行見出しツリーは `Row Title Panel` と答える／面の外／点に何も無い）。**`Row Title Panel` の期待値を `Row Title Tree` にすると落ちる**ことを見てから消した。⛔ **`readScreenPartAt` の試験は残していない** —— 書いた者が書いてはならない（規則 04 の 1.）。**仕様だけを読む別の者に書かせる** |
| 2026-08-21 | 検査 | ✅ **CR-192 の検査が全部緑になった** —— 機械検査 26 本 ALL GREEN、`audit-ch5` PASS、刊行物の描画 PASS（生の `**` 0）、`gen:check` 7 本 OK、`typecheck` 通る、`layers` OK、`npm test` **1956 / 1957**。⭐ **数の予測は 6 つとも的中**（tables 126 ／ figures 11 ／ rows 1559 ／ uids 145 ／ 辺 89 ／ 重複 `A=26 new 0`）。⛔ **赤いのは `uf-61` の 1 件だけで別件** |
| 2026-08-21 | 移送 | ⚠️ **仕様駆動の試験 2 本が持つ写しを追随させた** —— `uf-71.test.ts` の「継ぎ目はちょうど 2 メンバ」2 件（`:1035` `:1047` と冒頭の見出し）。⭐ **出どころは 表 T-065 の `IF-9` の欄であり、本 CR がその欄を変えた** —— 変わった文を試験の中に逐語で引いてある。⛔ **製品コードに合わせて期待値を曲げたのではない。** ⚠️ **`uf-30-31.test.ts` は固定データに `on: null` が 4 か所増えただけで、主張は 1 つも緩めていない** |
| 2026-08-21 | 実測 | ⭐ **CR-C（裁定 D3 ＋ §7 の `Tooltip` ＋ `Export Chooser`）の測り。** `impact.py T-037` —— 表 T-037 は **7 行**（`NT-1` `NT-2` `NT-3` `NT-3a` `NT-4` `NT-6` `NT-5`）、指している要求は `FR-025` と `FR-076` の **2 件**、2 次は 9 件、要求以外は 5.3 / 5.5（2）/ 8. の 4 箇所。`induced.py T-103 T-037 T-109 FR-032 FR-099 FR-096 T-028` —— ⭐ **種 7/7 がすべて解決**（空のグラフではない）、辺 **1**・**閉路 0**。⛔ **したがって 表 T-103 と 表 T-037 は同じ閉路に入っておらず、1 か所ずつ順に書いてよい** —— それでも 1 本の CR にまとめるのは、裁定 D3 が「`NT-7` と `ScreenView` の面を 1 本で」と定めているからであって、閉路のためではない |
| 2026-08-21 | 変更要求 | ⭐ **CR-193 を書いた**（`change-request/CR-193-the-names-that-were-missing-and-the-asking.md`）—— 裁定 D3（`NT-7` ＋ `ScreenView` の面）と、表 T-103 に `Tooltip` ／ `Export Chooser` の 2 つの確定名。⛔ **予測**: tables 126（不変）／ figures 11（不変）／ rows 1559 → **1562**（`U-53` `U-54` `NT-7`）／ uids 145（不変）。表 T-109 は **68 行のまま**（`IC-52` の面の欄が 3 → 4 になるだけ）。⚠️ **図案セッションの理由 (b) は偽だった** —— `IN-4` の第 1 階層は `S-99g` の「面」であり、`S-99g` 自身が「面とは `Esc` の第 1 階層で閉じられるもの」と定義している。**ツールチップは `S-99g` に入らないので `Esc` は届かない。** ⭐ **理由 (c) だけが残り、行を足さない結論は変わらない** |
| 2026-08-21 | 適用 | ✅ **CR-193 の仕様書側を当てた**（オブジェクトごとに 1 回、計 6 か所）—— 表 T-103 に `U-53` `Tooltip` と `U-54` `Export Chooser`、表 T-109 の `IC-52` の面の欄に `Export Chooser`、表 T-037 の末尾に `NT-7`、`FR-031` の例外を 2 つに数え直し、表 T-075 の `UF-67` を「知らせと確認」、変更履歴 版 0.70。⭐ **数の予測は 4 つとも的中**（tables 126 ／ figures 11 ／ rows 1559 → **1562** ／ uids 145）。`icon-roster.json` は 68 行のまま `IC-52` の面が 3 → 4 になり 11308 → **11330** バイト |
| 2026-08-21 | 罠 | ⛔ **踏んだ罠が 2 つ、どちらも既知だった。** ① `U-53` に「`_assets/tbl-settings.md` の `S-99g` が運ぶ対象ではない」と書いたら**重複検出が `A=27 (new 1)`** —— 拾われたのは CR-187 で 1 度踏んだ「`_assets/tbl-settings.md` の 表 T-nnn が持つ」という**規約そのもの**（18 か所目）。⭐ **表 T-028 の `IN-4` を指す形に直して基準線 `A=26 (new 0)` へ戻した。** ⛔ **基準線に足して黙らせない。** ② 変更履歴に `PD-153` と書いたら**検査 7 が落ちた** —— 暫定の印は仕様書のオブジェクトではない（正は `docs/development-records/pending-decisions.md` と `src/`）。**語から落とした** |
| 2026-08-21 | 判断 | ⚠️ **検査 13（助言）が 7 → 8 に増えた。** 増えたのは `NT-7` の行（`:3682`）で、**「規則を述べながら持ち主も指している」**という形である。⭐ **そのまま残す** —— 述べている規則（問い方）と指している規則（名前で示す義務・件数だけの禁止は `FR-032` と `FR-099`）は**別の規則**であり、指さないと `R1b` に反して同じ禁止を 3 か所目に書くことになる。**同じ形が既に 7 か所ある** |
| 2026-08-21 | 実装 | ⭐ **CR-193 を `src/` へ当てた**（4 ファイル）—— `UF-60` が `Confirmation` / `ConfirmationItem` を宣言し `ScreenView` と `ScreenSession` に 1 メンバずつ、`UF-67`（`notices.ts`）が `confirmationFromSession` を持つ。⛔ **新しいユニット（新しいファイル）は作らない** —— 検査 18 が `src/` と 表 T-075 の 71 ユニットを突き合わせるので、ファイルを足せば表に行が要る。**作法が 1 つ増えただけで担う者は増えていない**（表 T-075 の `UF-67` を「知らせと確認」にしたのがその根拠）。⭐ **`UF-30` は `SK-12` / `IC-3` を `screenStateWithSurface(state, 'Export Chooser')` へ移し、動作 `openExportChooser` を落とした** —— **`SK-13` の `F1` と同じ形になり、`IN-4` の第 1 階層で閉じられる。** ⚠️ **`UF-71` は `ROLE.tooltips` を `'Tooltip'` に直した**（規則 03。仕様書が名付けた綴りを使う）|
| 2026-08-21 | 罠 | ⛔ **`Ctrl+Shift+E` の分岐を `screenStateFromInput` の末尾へ置いたら死んだコードになっていた** —— 同関数は冒頭で `plain = isCombo(modifiers, false, false, false)` を測り、**修飾キーが 1 つでもあれば即座に返す。** ⭐ **`plain` の関門より前へ移した** —— 表 T-036 のうち、この関数へ修飾キーを持ったまま届く行は `SK-12` だけである |
| 2026-08-21 | 判断 | ⛔ **`Confirmation` に「はい / いいえ」の語も、押す入口も載せない**（`R2.9`）—— **仕様が 1 か所も定めていない。** 載せたのは `NT-7` が MUST で求める 3 つだけ（作法の行 ID・問いの言葉・**消えるものの名前の並び**）。⭐ **名前の並びは `RosterResource.unassignedTaskNames` と同じ形**（`string \| null` —— `Task.name` は無いことがある）にし、`FR-032` の「他の行に表示されているものはその旨を示す（MUST）」のために 1 つだけ真偽値を足した。⚠️ **空の並びを許す** —— 裁定 D6 の上書き確認は何も連れて行かないので、空を禁じると `NT-7` を使えない。⛔ **答えが戻る経路が無いことは `STOP` 注記に 3 か所書いた**（`notices.ts` ／ `input-command-translator.ts` ／ `dom-screen-surface.ts`）|
| 2026-08-21 | 移送 | ⚠️ **仕様駆動の試験 12 本が持つ写しを追随させた。** ⛔ **製品コードに合わせて期待値を曲げたのではない** —— 原稿が動いた。**何が偽になったか**: `uf-60`（`T_075_PARTS` が「`UF-67` は `notices` だけを埋める」と書いていた ／ 「A tenth member would be a part no unit of that table owns」）・`uf-67` と `uf-43-44`（`T_037` の写しが 7 行）・`uf-66`（`T_109_MODAL_PLACEMENTS` の `IC-52` が面を 3 つしか持たない）・`uf-30-31`（`T_036` の `SK-12` が `member: 'action'`）・`uf-71`（`ROLE` の「表 T-103 に行が無いのは 2 つ」が 1 つになった）。⭐ **`uf-62` 〜 `uf-69` と `uf-71` は引数側**（`ScreenSession` / `ScreenView` の固定データに `confirmation: null` が 1 つ増えただけで、主張は 1 つも緩めていない）。⚠️ **`uf-60` の `eightFrom` / `eightOf` / `T_075_EIGHT` は名前が嘘になるので改名した**（9 つになったため）|
| 2026-08-21 | 検査 | ✅ **CR-193 の検査が全部緑になった** —— 機械検査 26 本 ALL GREEN、`audit-ch5` PASS、刊行物の描画 PASS（生の `**` 0）、`gen:check` 7 本 OK、`typecheck` 通る、`layers` OK、`npm test` **2014 / 2016**。⭐ **数の予測は 4 つとも的中**（tables 126 ／ figures 11 ／ rows **1562** ／ uids 145、重複 `A=26 new 0`）。⛔ **赤い 2 件はどちらも別件** —— `uf-61`（表 T-206 に掴み帯の行が無い）と、別のエージェントが自ら ⛔ DELIBERATELY LEFT FAILING と書いた `uf-72-screen-part`（`IC-60` の Row Pin に `data-icon` が無い）|
| 2026-08-21 | 実測 | ⭐ **わざと壊して落ちることを確かめた**（規則 04 の 2.）—— 表 T-103 の `U-53` を `Tooltip` → `Tooltips` にすると `uf-71.test.ts` の「表 T-103 spells every settled name the cases look for」が `expected '` + "`Tooltips`" + `' to contain '` + "`Tooltip`" + `'` で落ち、控えから戻すと緑に戻る。⚠️ **控えは `ls -la` で大きさを見てから戻した**（44004 バイト）。⭐ **これが意味を持つのは、確定名が本当に `src/` の `data-role` まで届いているからである** |
| 2026-08-21 | 注意 | ⚠️ **作業木に CR-191 / CR-192 の適用が未コミットのまま在る** —— `_assets/fig-icons.svg` ／ `_source/components.json` ／ `_source/view-startup.drawio` ／ `_assets/view-startup.svg` ／ `docs/review/components/components.md` ／ `docs/review/duplication-baseline.txt` ／ `src/adapter/screen-renderer/screen-surface.ts` ／ `src/use-case/edit-document/edit-document.ts`。⭐ **本 CR は 1 つも触っていない。** コミットを分けるならここで切る |
| 2026-08-21 | 実測 | ⭐ **CR-D（裁定 D4）の測り。** `impact.py FR-038` —— **指している先は 表 T-016 ただ 1 つ**（行は 0）、**指している箇所は要求 1 件（`FR-029`）＋ 参照 7 箇所**。`induced.py FR-038 T-103 T-109 T-037 T-016` —— ⭐ **種 5/5 がすべて解決**、辺 **1**・**閉路 0**（空のグラフではない）。⛔ **`FR-038` が表を 1 つしか指していないこと自体が欠落である** —— **`T-016` は「訳さないもの」への矢印であり、訳すもの側の指し先が 1 本も無い** |
| 2026-08-21 | 調査 | ⭐ **表 T-016 の項目名は辞書に入れない。決めている文が 2 つとも逐語で在った** —— `FR-038`（`:3748`）「タスク名と行名、および表 T-016 の項目名は翻訳の対象ではない」と 表 T-016 の直後（`:1396`）「項目名は英語表記とすること（MUST）」。⚠️ **同表の「日本語」の欄は用語辞書からの抜粋であって画面の文字ではない**（同表の前書き）。⛔ **文書自身の値も対象外**（`FR-038`「日程表の出力に言語は含まれない」）|
| 2026-08-21 | 発見 | ⛔ ⭐ **`FR-038` の MUST が 2 本、実際に満たされていなかった。** ① 「押す前に現在どちらの言語かが読めること（MUST）」—— **切替の入口は 2 か所なのに、現在の言語を運ぶ値は `HelpModal.language` だけで、`App Header` 側（`IC-21`）は答えられない**（`CommandItem` の `isPressed` は「入っているトグル」であり、2 言語の選択に「切」は無い）。② 「言語の状態は 1 つ（MUST）」—— **`ScreenView` に宣言が無いので、機械が確かめる相手がいない** |
| 2026-08-21 | 変更要求 | ⭐ **CR-194 を書いた**（`change-request/CR-194-a-home-for-the-words-nobody-may-invent.md`）—— 裁定 D4。**原稿 `_source/display-words.json` ＋ 生成器 `tools/generate_display_words.py` ＋ `src/adapter/screen-renderer/display-words.json`。⛔ 語は 1 つも書かない**（172 の欄 × 2 言語 = 344 の空文字）。⭐ **名簿は原稿が持たず、生成器が毎回 `docs/spec` から起こして突き合わせる** —— 表が動けば黙らずに落ちる。⛔ **予測**: tables 126 ／ figures 11 ／ rows **1562** ／ uids 145 は**すべて不変**（散文とセルの書き換えだけ）。検査 21 の生成物 11 → **12**、検査 23 の原稿 3 → **4** |
| 2026-08-21 | 決めた | ⭐ **`S-134`（表 T-206）をコードへ届ける経路を決めた**（`CR-195` の ⑧ の 4 が未定として残していた件） —— `tools/generate_entity_types.py` に `NOT_STORED_PANEL_DIVIDER_SIZES` を追加し、`src/adapter/screen-renderer/screen-frame.ts` の末尾へ生成する。⛔ **`NOT_STORED_SIZES` へは相乗りさせない** —— あちらは `item-hit-area.ts`（`Entity`）へ生成される定数であり、掴み帯を使うのは `Adapter` の `UF-61` である。⚠️ **引数で受け取る道は無い** —— `screen-renderer.ts` の継ぎ目が `UF-61` を 3 引数に固定しており、`FR-051`（MUST NOT）が設定値も禁じている。⭐ **帯は境界に中心を合わせて重ねる**ので `Row Area` から幅を取らない。⛔ **線の太さは未決のまま**（`EP-9` が新しい設定鍵を禁じており、幅を持つ行がどこにも無い）—— `screen-frame.ts` の `STOP` 注記はその 1 件だけに狭めた |
| 2026-08-21 | 実測 | ⭐ **原稿の値を 1 つ変えて落ちることを確かめた**（規則 04 の 2.）—— `_source/settings.json` の `S-134` を 8 → 9 にすると `npm run types:check` が `DRIFTED src/adapter/screen-renderer/screen-frame.ts` で落ち、`npm run types` を走らせると生成定数も 9 になる。⚠️ **控えは `ls -la` で大きさを見てから戻した**（73440 バイト、`cmp` で一致確認）。⭐ **試験は幅の数値を見ていない**（仕様が数値を検査に固定していない）ので、値が届いていることを保証するのは `types:check` のこの落ち方である |
| 2026-08-21 | 実装 | ⭐ **`UF-61` の幅 0 の仮置きをやめた**。`uf-61.test.ts` の ⛔ LEFT FAILING の 1 件が緑になり、`tests/unit` は **43 ファイル 1747 件全緑**。検査 21 の生成物は 13 → **14**（`screen-frame.ts` を名簿へ追加）|
| 2026-08-21 | 実測 | ⭐ **`tests/integration`（`TS-2`）を開き、`SWS-1` 〜 `SWS-5` に 24 ケースを置いた** —— 駆動は `regionsFromScreen` → `layoutFromSchedule` → `geometryFromLayout`（＋ `svgFromSchedule`）。⚠️ **表の写しを打ち直さず `tests/contract/spec-table.ts` で読み時に解析している**ので、表 T-221 / T-222 を直すと落ちる。⛔ **本物の欠陥が 3 件出た**（`LF-11` 進捗マーカーが `Math.max` で違うバーに付く／`LF-12` イナズマ線が既定の `stackDirection: 'up'` で折り返す／`LF-1` 除数に `labelGap` が混ざり `FR-093` の純粋な積でない）。**3 件とも赤のまま残してある** —— 直すのは実装側であり、試験を触ってはならない（規則 04 の 1.）。⭐ **型検査も 2016 件の単体試験も 1 件も捕まえていない** |
| 2026-08-21 | 決めた | ⚠️ **`SWS-6`（Canonical XML）にケースを置かない** —— `FR-021` の MSPDI 比較がまだ実装されていない。⭐ **試験ファイルの冒頭にそう書かせた** |
