# 裁定待ちの一覧

**規則は [`docs/development-rules/06-pending-decisions.md`](../development-rules/06-pending-decisions.md)。**

⛔ **載せる前に引く** —— 仕様 → `docs/reference/` → 前プロジェクト。**未読を裁定待ちにしない。**
⭐ **`A`〜`C` は推奨値で実装し、印（`// @provisional PD-n`）を付けて先へ進む。⛔ `D`〜`H` は実装しない。**

---

## 分類（[規則 06](../development-rules/06-pending-decisions.md) の 2.）

| | 進めてよいか | 何が未決か |
|---|---|---|
| **A** | ✅ | 原稿にあり生成で届く値（線幅・色・待ち時間・寸法）|
| **B** | ✅ | 純関数の内部。観測できる振る舞いは仕様が固定している |
| **C** | ✅ | 表示だけの選択で、保存形に痕跡が残らない |
| **D** | ⛔ | 文書または `MSPDI` に保存・交換される値 |
| **E** | ⛔ | 公開署名・継ぎ目（表 T-064 / T-065）|
| **F** | ⛔ | 入力を受理するか拒むかの境界 |
| **G** | ⛔ | 他の規則の論証が乗っている値（しきい値・上限・不変条件）|
| **H** | ⛔ | 暫定の上に暫定を積むこと（**例外なく禁止**）|

---

## 一覧

⭐ **`A`〜`C` はまとめて承認できる形で並べる。`D`〜`H` は 1 件ずつ問う。**
⛔ **裁定を受けたら、行は消さずに状態を `裁定済` にする**（記録である）。

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-2 | **マイルストーンの 8 図形の頂点** —— `S-48` が ☆ の腰の位置を与えるだけで、図形の角そのものはどの表にも無い（図 F-019 が描くのはパレットの**アイコン**である）| **8 つとも同じ円に内接させる。**〇 は 24 角形、⬡ は 6、⬠ は 5、◇ と □ は同じ 4 角を 1/8 回転ずらしたもの、△ と ▽ は 3 角、☆ は 10 角で腰を `S-48` に採る。⭐ 根拠は `S-48` の備考「この値が ☆ の面積を決め、`milestoneGlyph` の面積順に影響する」—— **面積の順が言えるのは、8 つが同じ大きさを共有しているからである。** ⚠️ 〇 を多角形にするのは、`BarGeometry` の outline が経路しか持たないためで、弧の形を足すと受入済の 2 ユニットが建てている型が動く | B | W2 | `S-48` / `AT-101` / `FR-078` / `LF-10` | ⭐ **8 つが互いに違う頂点数で出る試験**（`schedule-geometry` の受け入れ試験）。⚠️ **`S-48` を変えると ☆ の腰が動く試験** | 未裁定 |
| PD-1 | **`themeHue` から実際の色を解く彩度と明度、および固定色 2 つの値** —— 依存線・イナズマ線の色と、注記の色 | **予定は彩度 62% / 明度 46%、実績は明度 30%、依存線とイナズマ線は `#6b7280`、注記は `#b45309`。** ⭐ 根拠は「`_assets/tbl-settings.md` の §5 が解き方を散文でしか持たない」こと —— 「色相ごとに、コントラスト規則が壊れる寸前まで濃い地色」とあるだけで、数が 1 つも無い。⚠️ **実測されているのは色相 214 だけ**なので、その近傍に置き、実測を名乗らない | C | W4 | `S-73` / `FR-041` / `FR-042` / `FR-019` / `NFR-007` | ⭐ **`themeHue` を変えると SVG の色が変わる試験**（`svg-renderer` の受け入れ試験）。⚠️ **コントラスト比の試験は `NFR-007` が測る対象を決めてから** | 未裁定 |
| PD-4 | **ヘッダーとパレットの入口の語** —— ⭐ **置き場は `CR-194` が作った**（`docs/spec/_source/display-words.json`）。⛔ **未決なのは語そのものに変わった** | **空のまま置く。**⭐ 根拠と扱いは [[PD-160]] と同じ —— ⛔ agent が語を書くと確定名の新設になる | C | W4 | `FR-038` / `FR-029` / 表 T-109 / `U-35` | ⭐ **`appHeaderItems.commands` の語を読む試験**（`screen-renderer` の受け入れ試験）。⭐ **引き当ては済んだ** —— `app-header-items.ts`・`command-palette.ts`・`open-modals.ts` が生成物 `display-words.json` を行 ID で引き、⛔ **欄が空のときだけ従来の表示へ退く**（空のラベル、パレットの群は 表 T-109 自身の群の欄）。⚠️ **印が外れるのは語が書かれたときであって、置き場ができたときではない** | 裁定済 |
| PD-10 | **`Cmd`（meta）を、`Ctrl` だけを名乗る行の `Ctrl` として読むか** —— ⚠️ `Cmd` の綴りは仕様全体で 1 度、`MK-2` の割当欄にしか現れない（自分で `docs/spec` 全体を grep して確認） | **meta を `Ctrl` として読む。** ⭐ 根拠は `MK-2` が `Ctrl（Cmd）` と併記していること —— 併記が 1 か所しかないのは、他の行が別扱いだからではなく、1 度書けば足りると考えたからと読む。⚠️ 逆に読むと Mac で保存も取り消しもできない | B | W4 | `MK-2` `MK-5` `MK-7` `MK-10` `MK-12` `SK-2`〜`SK-17` | ⭐ **meta だけを立てた `S` が `saveDocument` を答える試験**（`uf-30-31`） | 未裁定 |
| PD-11 | **`SK-16` / `SK-16a` の 1 打鍵あたりの倍率** —— 仕様はホイールの `zoomStep`（`S-53`）しか持たない | **ホイールと同じ段を使う。** ⭐ 根拠は `S-96` が「`zoomStep` は操作の速さであって結果ではない」と書き、結果の絵は変わらないと明言していること。⛔ **コードに数を書いていない** —— `InputContext.zoomStep` で受ける | B | W4 | `SK-16` `SK-16a` `S-53` `S-75` `S-76` `FR-016` | ⭐ **`Shift`＋`+` が `settings.zoomX * ctx.zoomStep` を答える試験**（`uf-30-31`） | 未裁定 |
| PD-12 | **ホイールを本ツールが読む面はどこまでか** —— `MK-1`〜`MK-5` は面の範囲を持たず、表 T-023a の判定順序は**押下だけ**を日程の描画領域に限っている | **アプリヘッダー以外の全体（`Schedule Canvas`）で読む。** ⭐ 根拠は 表 T-023a がタイムルーラーに禁じているのが「掴んで動かす操作」であり、同じ文が代替として `MK-2`〜`MK-4` と `MK-7` を挙げていること —— ホイールを禁じる文ではない | B | W4 | `MK-1`〜`MK-5` ／ 表 T-023a の面の一覧 ／ `U-32` `U-50` | ⚠️ **タイムルーラー上のホイールが `setScrollPosition` を答える試験**（`uf-30-31`） | 未裁定 |
| PD-13 | **ホイールがどちら向きに拡大するか** —— `MK-2`〜`MK-4` は「ズーム」としか書かず、`S-53` の行に向きの欄が無い | **手前から遠ざける向きで拡大する**（`factor = zoomStep ** -notches`）。⭐ 根拠は `S-96` が 1 ノッチの量を入力装置に委ねており、向きも装置の慣習に従うのが自然なこと。⚠️ **試験は向きに依存しない事実だけを押さえてある**（両軸が同じ倍率で動く・2 ノッチは 2 乗・戻せば戻る） | B | W4 | `MK-2` `MK-3` `MK-4` `S-53` `S-96` | ⚠️ **向きを引っくり返すと落ちる試験は今は無い** —— 裁定が下りたら 1 件足す | 未裁定 |
| PD-176 | **1 ノッチが 1 行より短いとき、縦スクロールが動かないこと** —— ⛔ `S-78` は表示位置を**行まるごと**で持ち、`S-96` は「1 ノッチで何倍動くかは入力装置に依存する」と定めるが、**距離を行数へ変える率をどの行も書いていない。** ⚠️ **実測: 起動時の雛形の帯は 148〜188px、1 ノッチは 100px なので、`MK-1` は 1 度も動かなかった** | **距離が 0 でないかぎり、向いた向きへ最低 1 行動かす。**⭐ 根拠は `MK-1` が「縦スクロール」と定めている以上、**0 は唯一あり得ない答え**であること。⚠️ **ノッチあたりの行数を発明していない** ——距離は装置が出したままで、1 行ぶんの床を敷いただけである。長い turn は今までどおり遠くまで届く | B | W4 | `MK-1` `S-78` `S-96` `S-77` | ⭐ **`tests/unit/uf-30-31.test.ts` の `MK-1` の件** ——雛形より背の高い行で 1 ノッチ回して行が変わることを見る | 未裁定 |
| PD-177 | **上端・下端を行き過ぎたときにどこへ着くか** —— ⛔ `MK-1` も `S-78` も端の扱いを書いていない。⚠️ **上へは 1 行も戻れなかった** —— 先頭行より上には行が無く、`scrolledAnchor` は null を「いまの値のまま」と読むので、**日程の先頭へ二度と戻れなかった** | **上は先頭行で止める**（その上に行が無いことは証明できる）。**下はいまの値のままとする** ——⛔ 行き先が残っているかは `Row Area` が既に何行見せているかで決まり、それを定める行が無い | B | W4 | `MK-1` `S-78` `OP-10` | ⭐ 上端で止まる件（先頭行より上へは動かない）| 未裁定 |
| PD-14 | **作成ドラッグのどちらの端が、新しい `Task` の載る行を決めるか** | ⛔ **裁定は要らなかった。`FR-001` の STATEMENT が既に定めている** ——「**作ったタスクは、ドラッグを始めた縦位置が指す `TaskGroup` に載せること（MUST）**」（`01-04-requirements.md:943`）。⭐ **推奨値はたまたま一致していたが、暫定ではない。印は外した。** ⚠️ **見つけたのは、仕様だけを読んで試験を書いた側である** —— 規則 06 の 1. が記録する型（未読を裁定待ちにする）の 3 例目 | B | W4 | `FR-001` `PD-4` `AR-2` `AR-3` `CM-6` | ⭐ **行 A で始め行 B で終えたドラッグが `groupId === A` を答える試験**（`uf-30-31`）| 裁定済 |
| PD-20 | **人が選んだ先へ書いた文書が、以後の上書き保存の宛先になるか** | **往復する 2 形式（`grsJson` / `mspdi`）では宛先になり、出て行くだけの 3 形式（`svg` / `png` / `singleHtml`）ではならない。** ⭐ 根拠は `FR-060` の上書き保存が「開いたファイル」を対象にしており、表 T-024 が形式ごとに取り込みの向きを持つかどうかを分けていること | C | W4 | `FR-060` `FR-096` ／ 表 T-024 の `IO-1`〜`IO-4` `IO-7` ／ `IC-2` `IC-3` | ⭐ **`chosenFile` へ保存した後、`openedFile` への保存が同じ先へ行く試験**（`uf-41-42`） | 未裁定 |
| PD-50 | **書き出しの帯（`EP-1`）とパネル（`EP-3`）と、その文字の色** —— 画面側の色は `CP-38` の様式であって設定値ではなく、`_assets/tbl-settings.md` はどちらの色も持たない | **地を `#f3f4f6`、文字を `#111111` とする。** ⭐ 根拠は、判定規則がどれも色を読まないこと —— `WY-3` は外接矩形を、`WY-2` は書き出し同士を比べる。⚠️ 地を不透明にするのは、`Row Area` が刈り込まなかったバーが行見出しの下から透けないようにするためである | C | W4 | `EP-1` `EP-3` `U-31` `U-22` `U-27` `U-23` | ⭐ **書き出した SVG の帯とパネルの `fill` を読む試験**（`uf-39-40`） | 未裁定 |
| PD-51 | **`Panel Divider` の境界線の色** —— `EP-9` は「`Group Grid Lines` と同じ線を 1 本引くこと（MUST）」と定め、新しい設定値のキーを作るなと言うが、その線の色をどの表も持たない | `#d1d5db` とし、⛔ **`SvgRenderer` が `Group Grid Lines` を描くときに、この定数と一致させること。** ⭐ `EP-9` を grep すれば 2 か所とも出る形にしてある | C | W4 | `EP-9` `U-24` `U-18` `FR-042` | ⭐ **境界線とグループ罫線の色が同じであることを読む試験** —— `SvgRenderer` が罫線を描いた後に書ける | 未裁定 |
| PD-52 | **`Document Title` が帯の中のどこに立つか** —— `EP-1` は「位置を動かしてはならない（MUST NOT）」と定めるのに、どの表も位置を持たず、`AppHeaderItems` は文字列だけを運ぶ（`RowTitle` は `box` を運ぶのに、である） | **帯の高さの 0.4 倍を字の大きさ、0.5 倍を左の余白とし、ベースラインは `S-33` を `svg-renderer.ts` と同じ当て方で使う。** ⭐ 根拠は、帯の高さだけが実測された値であること（`FR-051` が測り、`S-116` は上限にすぎない） | C | W4 | `EP-1` `U-27` `WY-3` `FR-051` `S-116` `S-33` | ⭐ **書き出した SVG の `<text>` の `x` / `y` / `font-size` を読む試験**（`uf-39-40`） | 未裁定 |
| PD-60 | **`agentApiVersion`（`AM-1`）の値** —— `AG-1` は版を持つことと、互換性を壊す変更で上げることを求めるが、最初の値をどの行も述べていない | **整数の 1。** `AGENT_API_VERSION` に置いた。⭐ 根拠は、`AG-1` が求めるのは単調性だけであり、起点は最小の自然数を採るのが読む者に最も説明が要らないこと | C | W4 | `AG-1` `AM-1` | ⭐ **`installAgentApi(...).agentApiVersion` を読む試験**（`uf-27-28-29`） | 未裁定 |
| PD-61 | **`focusTask`（`AM-16`）が、その `Task` を画面のどこへ置くか。予定の開始日が無いときどうするか** | **`Task` 自身の開始日を `S-77` に、その `Task` が描かれた行を `S-78` に書く** ——すなわち `Row Area` の左上へ置く。⚠️ 開始日が無いときは `actualStart` へ退き、両方無ければ拒む | C | W4 | `S-77` `S-78` `FR-055` `AM-16` `CM-66` | ⭐ **`focusTask(uid)` が `scrollDate` / `scrollGroupId` を書く試験**（`uf-27-28-29`） | 未裁定 |
| PD-62 | **新しく購読した `watchChanges`（`AM-17`）に、何が最初に届くか** —— `AG-6` は「まだ受け取っていないもの」としか言わず、まだ何も受け取っていない購読者にとっての意味が決まらない | **これから起きることだけ。** 購読した時点の版と発話の番号を起点に採る。⭐ 根拠は、履歴の再生を求める行がどこにも無く、⚠️ 逆に読むと購読のたびに全履歴が流れて `AG-6` の「受け取っていないもの」が無意味になること | C | W4 | `AG-6` `AG-11` `AM-17` `PI-15` | ⭐ **購読の前に発話し、最初の通知にそれが含まれないことを読む試験**（`uf-27-28-29`） | 未裁定 |
| PD-70 | **単一 `.html` の中で、文書をどの要素がどう運ぶか** —— `FR-067` と `IO-7` は「埋め込む」と定めるが、入れ物の記法をどの表も持たない | **`<script type="application/json" id="{id}">` に `GRS JSON` を入れ、最後の `</body>` の直前に置く。既に在れば差し替える。** ⭐ 根拠は、この型のスクリプト要素をブラウザが実行も解釈もせず、中身を書き換えないこと —— ⚠️ 規則 04 の 3. が記録する「鍵に制御文字が 1 つ混じるとアプリ全体が止まる」事故を避ける唯一の形である | C | W4 | `FR-067` `IO-7` `BT-1` `CN-8` `CN-5` `IF-8` | ⭐ **書き出した `.html` がその入れ物をちょうど 1 つ持つことを読む試験**（`uf-37-38`） | 未裁定 |
| PD-71 | **シェルが `IF-8` 越しに渡してよい要素 id の形と、外れたものの扱い** —— どの行も形を定めていない | **`/^[A-Za-z][A-Za-z0-9_-]*$/` だけを受け、外れたら拒む。** ⭐ 今の HTML が許す範囲より狭いのは、⚠️ 引用符や空白を含む id が属性を割って別の要素を生み、`FR-067` の「ちょうど 1 つ」を壊しうるためである | C | W4 | `FR-067` `IF-8` `CN-8` | ⭐ **形から外れた id を渡すと拒む試験**（`uf-37-38`） | 未裁定 |
| PD-80 | **MSPDI から起こした行（`TaskGroup.id`）が受け取る UUID** —— `AT-51` は形（UUID）を、`IV-1` は一意性を定めるが、どう作るかはどの行も述べていない | **その `Task` 自身の UID から決定的に導く**（版 4・変種 1 の並びに嵌める）。⭐ 根拠は、⚠️ 乱数で作ると同じファイルを 2 度取り込んだときに別の id が出て、`FR-021` の往復が比較できなくなること | B | W4 | `AT-51` `AT-52` `IV-1` `FR-058` `PI-20` | ⭐ **同じファイルを 2 度取り込むと同じ id が出る試験**（`uf-36`） | 未裁定 |
| PD-81 | **GRS が自分で作った文書の `Project/SaveVersion`** —— `DV-2` が出どころ（書いた側の版）を、`EX-1` が要素の必須を定めるので**形は決まっており、数だけが無い** | **この製品自身のメジャー版（`0`）を書く。** ⛔ `12` は書かない —— 公式 XSD がその値を「Project 2007」と文書化しており、名乗ると**交換相手の道具から出たファイルだと主張することになる** | C | W4 | `DV-2` `EX-1` `FR-021` | ⭐ **書き出した MSPDI が `SaveVersion` を持ち、それが 12 でないことを読む試験**（`uf-36`） | 未裁定 |
| PD-82 | **GRS が自分で作った文書の `Project/CurrencyCode`** —— `DV-3` はこの値を `carry` から採るが、取り込んだことのない文書は持たない。それでも `EX-1` は要素を要求する | `XXX` **とする** —— ISO 4217 の「通貨が関わらない」符号であり、スキーマ自身の文書がその符号集合を名指している。⛔ 実在の通貨は書かない —— **金額を持つ列が 表 T-058 に 1 つも無いので、文書が持たない事実を述べることになる** | C | W4 | `DV-3` `EX-1` `AT-125` | ⭐ **書き出した MSPDI の `CurrencyCode` を読む試験**（`uf-36`） | 未裁定 |
| PD-90 | **二度押しが「同じ押下の 2 度目」と数えられる時間と距離**（`MK-13`）—— どの表も持たない | **500 ms・4 px、押下から押下まで、押しボタンごとに測る。**⭐ 根拠は `S-124` の説明表示の待ち時間より短くないと、説明が出てから二度押しが成立してしまうこと | C | W5 | `MK-13` `GR-10` `GR-12` `IN-1` `S-124` `S-112` | ⭐ **400 ms 離れた同じ点の 2 押しが `clickCount 2` を答える試験**（`uf-50`） | 未裁定 |
| PD-91 | **ホイールが行や画素で報告されたときの 1 ノッチの値** —— `S-96` は「1 ノッチで何倍動くかは入力装置に依存する」と書くだけで、装置の単位を揃える値をどの表も持たない | **1 行 40 px・1 ノッチ 100 px・1 ノッチ 3 行。** 頁単位は宿主自身の `innerWidth` / `innerHeight` で測る | C | W5 | `MK-1` `MK-2` `MK-3` `MK-4` `MK-5` `S-53` `S-96` | ⭐ **`deltaMode=1` で `deltaY=3` が 1 ノッチを答える試験**（`uf-50`） | 未裁定 |
| PD-92 | **押下が無いときの `move` が報告する `button` と `clickCount` | `left` と `0`。⭐ `0` は「押下がまだ 1 度も無い」を表せる唯一の値である | C | W5 | `IN-1` `IN-2` `MK-8` `MK-13` | ⭐ **押下の無い `pointermove` が `clickCount 0` を答える試験**（`uf-50`） | 未裁定 |
| PD-93 | **`+` `-` `0` を、配列が出した文字で見るか物理キーで見るか | **物理キーで見る**（`Equal`／`Minus`／`Digit0` と各テンキー）。⭐ 根拠は、配列によっては `+` に修飾キーが要り、文字で見ると同じ操作が配列ごとに変わること | C | W5 | `SK-16` `SK-16a` `SK-17` `MK-10` | ⭐ **`key` が `_` でも `code` が `Minus` なら縮小になる試験**（`uf-50`） | 未裁定 |
| PD-94 | **本ツールが割り当てていない押下も、ポインタ捕捉で保持するか**（保持すれば `IN-1a` の中断が効く） | **保持しない** —— 呼び手が「割り当てた」と言った押下だけを捕捉する。⭐ 根拠は `MK-10` が「割り当てていない組合せを止めてはならない（MUST NOT）」と定めており、捕捉も止める手の 1 つであること | C | W5 | `IN-1` `IN-1a` `MK-10` `MK-13` `AG-9` | ⭐ **割当ありの左押下だけが捕捉を掛ける試験**（`uf-50`） | 未裁定 |
| PD-95 | **表 T-036 が割り当てていない打鍵を報告するか、どの綴りで報告するか** | **すべて報告する**（正規化はするが名前は変えない）。⭐ 根拠は `MK-10` / `MK-12` が「割り当てていないものを止めるな」と言う以上、⚠️ **止めないという判断を下せるのは割当を知る側**（`UF-30`）であり、ここで捨てるとその判断が届かないこと | C | W5 | `SK-1` `SK-20` `MK-10` `MK-12` `IN-5` | ⭐ **割当の無い打鍵が宿主の名前のまま届く試験**（`uf-50`） | 未裁定 |
| PD-104 | **ファイル選択の絞り込み** —— `OP-1` は `GRS JSON` と MSPDI XML を認めるが、拡張子も MIME 型もどの表も持たない | **絞り込まない。** ⭐ 根拠は `FR-023` の検証がファイルを拒む唯一の場所であること —— ⚠️ 選択側で絞ると、拒む場所が 2 か所になり、片方が緩いと `FR-023` の意味が消える | C | W5 | `OP-1` `OP-2` `IO-1` `IO-2` `CN-5` | ⭐ **選択に渡す options が空であることを読む試験**（`uf-51`） | 未裁定 |
| PD-105 | **`queryPermission` を持たない宿主に、書き込み可否を何と答えるか | **許可されていると答え、書き込みが実際に確かめる。** ⚠️ 拒まれれば `permissionLost` として値で返る。⭐ 根拠は、逆に読むと `FR-060` の上書き保存がその宿主で永久に使えなくなること | C | W5 | `FR-060` `NT-4` `LM-14` `IF-3` | ⭐ **`queryPermission` の無いハンドルが `writable` を答える試験**（`uf-51`） | 未裁定 |
| PD-110 | **`localStorage` の鍵に付ける、この道具固有の接頭辞の綴り** —— `FR-026` の RATIONALE が付けることを求めるが、綴りをどの表も持たない | `grsched.` **とし、定数として公開する** —— ⭐ 表 T-206 の 4 行が同じ置き場を共有するので、打ち直させずに読ませる | C | W5 | `FR-026` `S-99` `S-99a` `S-99b` `S-99c` `LM-6` `LM-14` | ⭐ **store が渡す全部の鍵が接頭辞で始まる試験**（`uf-52`） | 未裁定 |
| PD-111 | **自動保存の置き場が 1 枠か、文書ごとに 1 枠か** —— `AutosaveGateway` が分類 D として報告した宿題 | **1 枠**（鍵と本文を一緒に持ち、書くたびに置き換える）。⭐ 根拠は `BT-3` が起動時に差し出すのを 1 つとしており、⚠️ 複数枠にすると「どれを差し出すか」という規則が新たに要ること | C | W5 | `FR-061` `FR-062` `BT-3` `FR-026` `IO-5` | ⭐ **文書 A の後に文書 B を書くと A が消える試験**（`uf-52`） | 未裁定 |
| PD-112 | **`FR-062` の「脇へ置く」を、`IF-4` にメンバが無いままどう行うか | **`IF-4` ではなく `CP-29` が返す具象の側に置く。** ⛔ 継ぎ目は広げない —— ⚠️ 広げると内側の層が「脇へ置く」を知ることになり、`LR-5` の向きに反する | C | W5 | `FR-062` `BT-3` `FR-026` `NT-3a` `NT-6` | ⭐ **脇へ置いた後の読み出しが「無い」を答える試験**（`uf-52`） | 未裁定 |
| PD-121 | **ブラウザの拒否のどれを `notPermitted` と読むか** —— 継ぎ目は 3 値とその意味を固定するが、宿主のどの信号が当たるかをどの行も持たない | **投げられた値の名前が `NotAllowedError` のものだけを `notPermitted` とし、他は `writeFailed`。**⭐ 根拠は `NT-3a` が「次にできること」を求めており、権限の話とそれ以外では次の一手が違うこと | C | W5 | `FR-028` `NT-3a` `IF-5` | ⭐ **`NotAllowedError` だけが `notPermitted` になる試験**（`uf-53`） | 未裁定 |
| PD-130 | **ラスタライズの失敗 3 値に、宿主のどの断り方が当たるか** | `unsupported` は 2D 文脈が得られない場合と汚染された canvas、`tooLarge` は要求した大きさが得られない場合、残りは `rasterFailed`。⭐ 根拠は `NT-3a` の「次にできること」が 3 つで違うこと | C | W5 | `IF-6` `NT-3a` `AG-8` `IO-3` `IO-4` | ⭐ **宿主の失敗ごとに `reason` を読む試験**（`uf-54`） | 未裁定 |
| PD-131 | **外部を取りに行く参照を含む絵をラスタライズするか** —— `href` や `url()` が `#` でも `data:` でもない場合 | **拒む**（参照を名指して `rasterFailed`）。⭐ 根拠は `WY-2` が「同じ状態の 2 回の書き出しは一致する」ことを求めており、⚠️ **網の向こうの資源はそれを保証できない**こと | C | W5 | `WY-2` `WY-3` `FR-080` `IO-4` | ⭐ **外部 href を含む SVG を拒む試験**（`uf-54`） | 未裁定 |
| PD-132 | **根の `<svg>` が持つ幅と高さをどう扱うか** —— `IO-3` と `IO-4` は別々の大きさを定めるのに、絵は 1 本の文字列で来る | **`viewBox` があれば、根の幅と高さを求められた画素数へ書き換える**（古い対は外す）。⭐ 根拠は、書き換えないと復号器ごとに違う大きさに解かれ `WY-2` が壊れること | C | W5 | `IO-3` `IO-4` `S-82` `WY-2` | ⭐ **偽の `<img>` に渡された `src` の寸法を読む試験**（`uf-54`） | 未裁定 |
| PD-133 | **画素数が整数でない、あるいは 1 未満のとき** | **拒む**（受け取った大きさを名指して `rasterFailed`）。⛔ 丸めない —— ⚠️ 丸めると `WY-3` の「丸めの規則を当てたうえで一致する」判定が、どちらの丸めを指すか分からなくなる | C | W5 | `FR-025` `S-81` `S-82` `WY-3` | ⭐ **`1600.5` を渡すと拒む試験**（`uf-54`） | 未裁定 |
| PD-134 | **PNG が透明を保つか、不透明な地を敷くか** | **絵の下には何も敷かない。** ⭐ 根拠は `FR-080` と 表 T-076 が「何を描くか」を定めており、⚠️ **描けと言われていない地を敷くのは、書き出しにだけ在るものを足すことになる** | C | W5 | `FR-080` `IO-4` | ⭐ **`drawImage` が最初で唯一の描画である試験**（`uf-54`） | 未裁定 |
| PD-140 | **開いた面ごとに何を運ぶか** —— `FR-036` `FR-074` `FR-099` `FR-088` `FR-068` の 5 つが面を開くのに、中身の形をどの表も持たない | **面で判別する共用体にし、要求ごとに要る中身だけを持たせる。** ⛔ 名前は表 T-103 が確定させた 3 つだけを使い、⚠️ **名の無い 2 面は要求の UID で運ぶ**（`IconId` が `IC-7` で、`Notice.manner` が `NT-1` で運ぶのと同じ手）—— **新しい確定名を作らない** | C | W4 | `FR-036` `FR-074` `FR-099` `FR-088` `FR-068` `S-99g` `U-30` `U-49` | ⭐ **各面が自分の中身を運ぶことを読む試験**（`uf-66`） | 未裁定 |
| PD-141 | **ポインタがどのアイコンの上にあるか** —— `EZ-2` は時間と**場所**の 2 条件を課すのに、場所を答えられる値がこの層のどこにも無い | **シェルが答える**（`ScreenSession.iconUnderPointer`）。⭐ 根拠は、⛔ **入口を実際に描いたのは `DomScreenSurface` だけ**であり、`ScreenView` のどの部品も入口の矩形を持たないこと。⚠️ `CommandItem` に矩形を持たせる案は、測れない側に測らせることになるので却下した | C | W4 | `EZ-2` `S-124` `FR-029` `PI-35` | ⭐ **休止が閾を越えても、下にあるアイコンだけが説明を得る試験**（`uf-69`） | 未裁定 |
| PD-142 | **行見出しパネルで選ばれている行の集合** —— `FR-085` と `FR-042` が同じ集合を読むのに、⚠️ `SL-1` が行を描画領域の選択から外しているので `Selection` が持てない | `ScreenSession.selectedGroupIds` **として運ぶ。** ⭐ `FR-042` の「行の色と高さをプロパティパネルに出す」は**どの行が選ばれたか**だけが欠けていた —— 色も高さも `schedule` から既に届く | C | W4 | `FR-085` `FR-042` `SL-1` `AT-51` `AT-58` `AT-59` | ⭐ **選ばれた行の色と高さがプロパティパネルに出る試験**（`uf-64`） | 未裁定 |
| PD-143 | **担当者名簿で選ばれている担当者の集合** —— `FR-099` の「すべて選ぶ／すべて解く」と 2 つ目の削除経路が読むのに、`SL-1` は担当者を認めない | `ScreenSession.selectedResourceUids` **として `uid` で運ぶ。** ⭐ 根拠は 表 T-225 の `AS-6` 「人に見せるのは担当者名、文書に書くのは `uid` とすること（MUST）」 | C | W4 | `FR-099` `AS-6` `AT-85` `SL-1` | ⭐ **選択が `uid` で解決され、同名でも取り違えない試験**（`uf-66`） | 未裁定 |
| PD-144 | **選択が消えたときプロパティパネルが何を保つか** —— `FR-072` は「保って、その旨を見出しに言う（MUST）」と定めるが、直前の何を保つのかを述べていない | **描かれた項目ではなく「対象」を保つ**（`ScreenSession.propertiesSubject`）。⭐ 決め手は `FR-072` 自身の「**もう一度同じ入口を押したら直前の選択物へ戻すこと**」—— ⚠️ 項目を保つと、編集で既に古くなった値を見せることになる | C | W4 | `FR-072` `SL-1` `FR-085` | ⭐ **選択を消した後も見出しと項目が保たれる試験**（`uf-64`） | 未裁定 |
| PD-150 | **対話欄の入力を確定させる鍵** —— `SK-19` は `Enter` を「その場の編集を確定する」に割り当てるが、⚠️ **その一覧に `Dialogue Field` が無い** | `Enter` **を採る** —— ⭐ 仕様が持つ確定の割当のうち最も近いものであり、⛔ 新しい割当を発明していない | C | W5 | `SK-19` `AG-11` `FR-066` | ⭐ **`Enter` で `isSettled` が立つ試験**（`uf-71`） | 未裁定 |
| PD-151 | **各部品をどこにどう置き、どう塗るか** —— `ScreenView` は `Properties Panel` や `Dialogue Field` の矩形を持たない | **長さはすべて相対値（`em`・百分率・ヘアライン）にする。** ⭐ 根拠は `FR-051` がヘッダーの高さを「どこにも値として持ってはならない」と定めていること —— ⚠️ **相対値なら環境の答えがそのまま `BO-1` で測り返される** | C | W5 | `FR-051` `BO-1` `S-116` `SC-4` | ⭐ **描かれた部品が `data-role` で見つかり、寸法が環境から来る試験**（`uf-71`） | 未裁定 |
| PD-152 | **行を 1 段ごとにどれだけ字下げするか** —— ⛔ 正しい値は `S-37`（`rowTitleIndent`）だが、`RowTitle` は切った名前と `depth` しか運ばず、`DocumentSettings` はこの層に届かない | **1 段 1`em` で描く。** ⛔ **これは暫定であり、正しい直し方は `RowTitle` が字下げ幅を運ぶことである** —— ⚠️ そうしないと `FR-085` が名前を切った幅と、実際に描かれる字下げが別々に決まる | C | W5 | `S-37` `FR-085` `S-79` `EP-4` | ⭐ **深さごとの字下げを読む試験**（`uf-71`） | 未裁定 |
| PD-153 | **説明を消す操作の見た目** —— `IN-3` は「消せること」を求めるが、その入口の図形を 表 T-109 が持たない | ⛔ **2026-08-25 の裁定で推奨は覚された** —— 利用者の逐語は「x を押して消すのは間違い」である。⭐ **`x` を取り、消す手立ては 表 T-028 の `IN-4` の階層の最後（出ている説明）へ移した**（CR-252）。⚠️ **その `Esc` の経路はまだ実装されていない** —— 描く側に STOP が立っている | C | W5 | `IN-3` `IN-4` `FR-029` `IC-52` | ⭐ **説明が `×` を持たない試験**（`uf-71`） | 裁定済 |
| PD-154 | **入口の図形をどう描くか** —— `FR-029` は 図 F-019 を図形の正とし（MUST）第三者の図形集を使うことを禁じる（MUST NOT）が、⛔ **その図が `src/` へ生成されていない**（`icon-roster.json` は名簿だけを運ぶ） | **行 ID を `data-icon` に置き、図形は描かない。** ⭐ こうすると ⛔ **第三者の図形を使わずに済み**、⚠️ 図が生成されるようになった回に、描く側だけを差し替えられる。⭐ **2026-08-24 に両半分とも閉じた** —— 図は `icon-glyphs.json` として `src/` へ生成済みであり（本行の前提はその時点で偽になっていた）、残っていた「箱の大きさを誰も決めていない」は `CR-241` が 表 T-206 に `S-138` を立てて閉じた。印は消した | C | W5 | `FR-029` `F-019` `T-109` | ⭐ **各入口が自分の行 ID を持つ試験**（`uf-71`）。⭐ **足された** —— 同じ箱で描かれることを 4 つの面で当てる 10 件と、原稿の `S-138` を 12 から 21 へ変えると 8 件落ちることの実測 | 裁定済 |
| PD-155 | **行見出しパネルとプロパティパネルの幅をどこから取るか** —— `ScreenView` は両者の矩形を持たない | **`Panel Divider` の線を各パネルの端として使う。** ⭐ 根拠は `FR-052` が「その帯をドラッグすると**そのパネルの幅が変わる**」と定めていること —— ⚠️ 分割線が無い側は窓の端に置く | C | W5 | `FR-052` `FR-051` `S-79` `S-80` | ⭐ **分割線の位置がパネルの端と一致する試験**（`uf-71`） | 未裁定 |
| PD-156 | **`isSettled` を立てる側** —— `AG-11` は「確定していないものを発話として読んではならない（MUST NOT）」と定めるが、確定を誰が判断するかを述べていない | **この側（シェル）が立てる。** ⭐ 根拠は、⛔ **半端な入力そのものがこの側から出ない**のが `AG-11` の求める姿であり、`dialogueMessageFromInput` はその旗を信じて拒むだけでよいこと | C | W5 | `AG-11` `IF-9` `FR-066` | ⭐ **確定していない入力が発話にならない試験**（`uf-71`） | 未裁定 |
| PD-157 | **通知が持つ件数（`NT-3`）をどう人に見せるか** —— ⛔ 属性にだけ置くと `NT-1` の「色や枠だけで伝えてはならない（MUST NOT）」に反するが、添える語を `FR-038` の訳語表が持たない | **数そのものを字として描く。** ⭐ 語を足さないのは、⚠️ **訳語の置き場が無い**（[[PD-3]] / [[PD-4]] と同じ穴）ため —— ⛔ **読める形にすることと、語を発明しないことの両方を満たす最小の形である** | C | W5 | `NT-1` `NT-3` `NT-3a` `FR-038` | ⭐ **件数が字として描かれる試験**（`uf-71`） | 未裁定 |
| PD-160 | **表示に出す語そのもの** —— CR-194 が原稿・生成器・成果物を作った。⭐ **2026-08-24 実測: 枠は 242、記入も 242。**⛔ **「176 語の枠が空のまま在る」「語は 1 つも書かれていない」と書いてあったが、どちらも偽になっていた** —— 枠は増え、利用者が全部書いた。⚠️ **同じ嘘が原稿の $comment と生成器の刷り文にも残っており、同日に 3 か所とも直した** | **空のまま置き、印字する側は従来どおりに退く**（空のラベル・行 ID・表 T-109 自身の群の語）。⭐ 根拠は、`FR-038` の MUST NOT が**言語の選択すら文書から外す**ので、覆す実費が原稿のセルだけであること。⛔ **agent が語を書くと、表 T-109 が明文で拒んでいる「確定名の新設」をそのまま行うことになる**（利用者の裁定 2026-08-21、案 (a)）| C | W4 | `FR-038` ／ 表 T-109 ／ 表 T-103 ／ 表 T-037 ／ `EZ-2` | ⭐ **原稿の 1 語を埋めると画面に届く試験**（`gen:check` が 176 語 0 記入を数えており、⭐ **画面までの道は通した** —— 表 T-075 の `UF-60` の 1 セルを根拠に `ScreenView` が `language` を持ち、`UF-62` `UF-64` `UF-65` `UF-66` `UF-69` の 5 ユニットが自分の担当節だけを引く）| 裁定済 |
| PD-3 | **表 T-023 の割り当てを画面に出すときの語** —— ⭐ **置き場は `CR-194` が作った**（`docs/spec/_source/display-words.json`）。⛔ **未決なのは語そのものに変わった** | **空のまま置く。**⭐ 根拠と扱いは [[PD-160]] と同じ —— ⛔ agent が語を書くと確定名の新設になる | C | W4 | `FR-037` / `FR-038` / 表 T-023 の `MK-1` `MK-5` | ⭐ **スクロールバーのヒント本文を読む試験**（`tests/unit/uf-69.test.ts`）。⭐ **引き当ては済んだ** —— `tooltips.ts` が生成物 `display-words.json` の `assignments` を行 ID で引き、⛔ **欄が空のときだけ従来どおり行 ID そのものへ退く**。⚠️ **印が外れるのは語が書かれたときである** | 裁定済 |

| PD-161 | **座標の丸めの許容差と、SVG の正規化の規則** —— `FR-080`（`:3083`）が「正規化と丸めの規則そのものは Chapter 6.1 が持つ」と委任し、`WY-3` の合否がそこに乗っているのに、⛔ **Chapter 6.1 にその規則が無い**。⭐ **`CR-202` が PNG の比べ方と基準環境を閉じ、書き出す桁を [[PD-162]] へ割り出した。残るのはこの 2 つである** | **推奨を添えて問う。推奨で走らない。** ⛔ **許容差は「判定に含めない差」の境を引く値であり、`WY-3` の合否がその値に直接乗る** —— 値を後から動かすと、それまで「合格」としていた判定が全部組み直しになる。⚠️ **裁定 `D10` が裁いたのは XML の側だけである** | G | W4 | `FR-080` / `WY-3` / 表 T-041 / Chapter 6.1 | ⭐ **`WY-3` を駆動する試験**（まだ無い。表 T-218 の `TS-3`）| 裁定済 |
| PD-162 | **書き出す座標の桁** —— `WY-2` と `WY-3` が比べる SVG / PNG の座標を、何桁で書くか。⭐ **[[PD-161]] から割り出した半分である**（`CR-202`）| **0.01 px の格子に丸める**（`Math.round(value * 100) / 100`）。⭐ **いまの実装がそうしている値である。** ⛔ **整数 px にすると、`S-81` の 1600 px 幅で細い線と矢羽根の角度が崩れ、`FR-094` の寸法が壊れる。** ⭐ **丸めずに書くと綴りが膨らむが、判定する側だけを直せばそちらへ移れる** —— 移りやすいほうを既定に置いた。⚠️ **分類が `A` なのは、SVG / PNG が書出のみ（表 T-024 の `IO-3` / `IO-4`）で、文書にも MSPDI にも残らないからである** | A | W4 | `FR-080` / `WY-2` / `WY-3` / `S-81` | ⭐ **書き出した SVG の座標が 0.01 px の格子に乗る試験**（まだ無い。表 T-218 の `TS-3`）| 裁定済 |
| PD-163 | **`xsd:duration` の綴り** —— `NR-4`（表 T-228）が型付きの値を標準字句表現へ揃えると定めたが、⛔ **`XML Schema Part 2` はこの型に標準字句表現を定めていない**（3.2.6 は字句表現・順序関係・ファセット比較の 3 節だけである）。⚠️ **交換相手はこの型を 13 の要素名・35 か所で使うが、`GRS` が解釈するのは `AT-35`（`Task.actualDuration`）ただ 1 列である** —— 残りは `carryElements` へ文字列のまま入るので綴りが変わらない | ⭐ **裁定済（2026-08-22。案 `A` ＋ 案 `D`。`CR-203`）** —— **書き出す綴りを `PTnHnMnS` に固定し**（表 T-033 の `EX-9`。`EX-7` が日付で同じ穴を埋めた先例に倣う）、**比べる側は字句ではなく時間の量として比べる**（表 T-228 の `NR-5`）。⛔ **元の綴りを列として覚える案は採らなかった** —— 版 0.44 が同型の案を「解釈した値と元の文字列を両方持つ」として退けている | D | W4 | `EX-9` / 表 T-033 / `NR-5` / 表 T-228 / `AT-35` | ⭐ **`durationOfMinutes` の綴りを崩すと落ちる試験**（まだ無い。表 T-218 の `TS-3`）| 裁定済 |
| PD-164 | **稼働日の整数にならない実績期間の丸め** —— `AT-35`（`Task.actualDuration`）は稼働日の整数を持ち、交換相手は時間の量を書く。⛔ **`Project.minutesPerDay` で割り切れない長さ**（1 日 480 分に対する `PT41H` など）**の扱いを、どの行も述べていない**。⭐ **いまの実装は読まずに `null` とし、人に知らせている**（`mspdi-codec.ts` の `workingDaysOfActualDuration`。注記自身が「the rounding is undecided」と書く）—— ⚠️ **値を発明していない点は規則どおりだが、本一覧に 1 行も無いまま既定になっていた** | ⭐ **裁定済（2026-08-22。案 ④ 四捨五入。`CR-204`）** —— **1 日単位に四捨五入し、ちょうど半日は絶対値の大きいほうへ寄せる。割り切れないことを理由に値を捨てない（MUST NOT）。** ⛔ **「読まない」を退けたのは、`ActualDuration` が `TASK_CONSUMED` にあり `carryElements` に残らないため** —— **`null` は書き出しで 1 要素も書かれず、交換相手が書いた長さが往復で消える。** ⚠️ **四捨五入も無損失ではない**ので、丸めた `Task` の件数を知らせる（`FR-076` の `NT-1`）| D | W4 | `AT-35` / `FR-054` / `EX-9` / `S-128` | ⭐ **`PT41H` が 5 日になり、知らせが 1 件出る試験**（まだ無い。`uf-36` 系）| 裁定済 |

⭐ **W4 の裁定 J（3 件）はここに載せない** —— **どれも `E`（継ぎ目と公開署名）であり、
実装前に問うものだからである。** 規則 06 の 2. の表を見よ。

---

## ⛔ 2026-08-22 に足した 1 行（`AT-128`）

⚠️ **一覧の表が長いので末尾に置く。⭐ 分類と波は行が自分で名乗る（検査 25 が見る）。**

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-165 | **`lastEditedBy`（`AT-128`）の値域と、書き手を見分けられるかどうか** —— 意味欄は「最後に書いた者。人か、AI か」と書くだけで、⛔ **何を書いてよいかも、書き手どうしを見分けられることも定めていない。** ⚠️ **出荷しているテンプレート自身が `"template"` を書いており**（`startup-template.json`）、人でも AI でもない。⛔ **`AG-6` の MUST NOT（自分の書き込みは通知しない）は `lastEditedBy` の等値だけでそれを判定するので、AI が 2 体在って同じ文字列を書くと、互いの書き込みを自分のものと読んで通知が落ちる** | **推奨を添えて問う。推奨で走らない。** ⛔ **文書に保存され、`MSPDI` の往復にも乗る値であり、緩く受理した文書は後から締め直せない**（規則 06 の `D` と `F` の両方に当たる）。⭐ **問うべき形は「一意であることを求めるか、それとも `AG-6` の判定に書き手の識別を別で持たせるか」である** —— 前者は値域の裁定、後者は継ぎ目の裁定になる | D | W4 | `AT-128`（`documentStamp.lastEditedBy`。CR-205 で器が改名された）/ `AG-6` / `AG-2` / `FR-063` / `DR-4` | ⭐ **同じ `lastEditedBy` を名乗る 2 体のうち一方の書き込みが、他方へ通知される試験**（まだ無い。`uf-27-28-29` の `watchChanges` の並び） | 裁定済 |

⭐ **裁定は 2026-08-22。当てたのは `CR-207` である** —— **書く語の全数を 表 T-229 に置き、画面は `user`、起動テンプレートは `template`、`Agent API` の呼び手は申告した名とした。**
⛔ **後者の重複だけは閉じていない。** ⚠️ **`LM-16` として制限事項に書いた** —— 閉じるには監視の継ぎ目に識別を足すことになり、分類 `E` の裁定である。

⛔ **`@provisional` の印は付けない** —— 分類 `D` は実装しないものであり、印を付けると検査 25 が落とす（規則 06 の 6.）。
⚠️ **刻印の組み直し（`docs/review/stamp-rulings-kiss-2026-08-22.md`）が `revisionStamp` を `documentStamp` へ改めるので、その変更要求は本行の行 ID を差し替えること。**

---

## CR-206 が足した 1 行（`FR-046`）

⚠️ **`B-1` は `docs/review/stamp-and-utc-2026-08-22.md` の §5.2 が挙げた問いであり、本一覧に行が無かった。** ⭐ **裁定と同じ回に起こして `裁定済` にする**（規則 06 の 3. は行を消さないことを求めており、載せずに閉じると記録が残らない）。

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-166 | **`FR-046` の「その時点の本日」が誰の暦の日か**（`docs/review/stamp-and-utc-2026-08-22.md` の `B-1`）—— ⛔ **`statusDate` に何を書くかを定めた MUST は在るのに、どの暦の日かを述べる行がどこにも無かった。** ⚠️ **`InputContext.today` を繋ぐ側が黙って選べる状態であった** | **読む人の機のローカルの暦の日とする。** ⭐ **`statusDate` はゾーンを持たない暦の日を入れる列であり**（`FR-020` の RATIONALE）、**UTC を採ると東京の午前 9 時より前に押した線が前日に立つ。** ⚠️ **`R3.6` の「ローカルは表示にのみ」は瞬間を保存する値の条項であり、暦の日には掛からない** | D | W5 | `FR-046` / `AT-13` / `CU-1` | ⭐ **シェルが UTC の暦の日を渡すように壊したら落ちる試験**（まだ無い。`FT-1` の結線が先）| 裁定済 |

⭐ **裁定は 2026-08-22。当てたのは `CR-206` である。** ⛔ **`@provisional` の印は付けない** —— 分類 `D` であり、かつ既に裁定済である。

---

## CR-208 が足した 2 行（表 T-206）

⭐ **どちらも分類 `A`（原稿にあり、生成でコードへ届く値）なので、推奨値で走り、印を付ける。**
⛔ **印は生成ブロックには付けられない**（次の `npm run gen` で消える）ので、**`item-hit-area.ts` の `PointerSlop` の宣言に付けた** —— 値の意味を述べているのはそちらである。

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-167 | **実績の端点を、左右どれだけ離れても掴めるか**（`S-91`）—— ⛔ **表 T-206 の既定は「実績バーの帯」という散文だけで、**帯は縦の広がりである。** ⚠️ **端点は点なので、横の許容が無いと `GR-5` / `GR-6` は決して当たらず、実績を動かす経路が丸ごと塞がる**（表 T-023d の注は「実績を動かす経路は端点だけである」と定める）| **`S-90` と同じ 6px とする。** ⭐ **根拠は 表 T-206 自身の理由欄** ——「掴み領域は読む人のアクセシビリティに属する。手が震える人には大きな掴み代が要る」。**震える手は、掴む相手が予定か実績かで震え方を変えない。** ⚠️ **重なったときに何が勝つかは 表 T-023d の優先順が既に決めている**ので、そろえても取り合いにならない。⛔ **`S-90` を指さずに自分の数を持たせた** —— 予定と実績で別に選ぶ余地を残すためである | A | W4 | `S-91` / `GR-5` / `GR-6` / `S-90` | ⭐ **実績バーの端点から左右へ `S-91` ちょうど離れた点が当たり、1px 外れた点が当たらない試験**（まだ無い。`item-hit-area` の受け入れ試験）| 未裁定 |
| PD-168 | **線に「乗った」と数える距離**（`S-137`）—— ⛔ **表 T-023d の前文は掴み代の値を 表 T-206 へ全部委ねているのに、`GR-13`（依存線）と `GR-16`（基準日線）のための行が 1 つも無かった。** ⚠️ **「線の上」は幅を持たない集合であり、許容を与えないと決して当たらない** | **線の左右に 6px とする。** ⭐ **根拠は `PD-167` と同じ理由欄と、`GR-13` / `GR-16` が 表 T-023d の下のほうに在ること** —— **広く取っても上の行から掴みを奪わない。** ⚠️ **由来を持つ数ではないので 🔎 を付けた** | A | W4 | `S-137` / `GR-13` / `GR-16` | ⭐ **依存線から `S-137` ちょうど離れた点が当たり、1px 外れた点が当たらない試験**（まだ無い。`item-hit-area` の受け入れ試験）| 未裁定 |
| PD-169 | **押している身振りが 表 T-023a のどの行で始まったかを、書き込みの側から問う道** —— ⛔ **`AG-9` は「パンと範囲選択は文書を変えないので拒否しない」と明文で除外するのに、`WS-2` に答えるシェルは押下中のすべてを拒否している。** ⚠️ **どの行で始まったかを決めているのは `PI-18` の私有の判定であり、表 T-023a の判定順序をそこに置いたのは同表の注そのものである。⛔ 写せば `R2.7` の重複になる** | **`PI-18` が「その押下がどの行で始まったか」を答えるか、`PointerPress` がそれを運ぶ。⭐ どちらも 表 T-064 か 表 T-065 を動かすので裁定が要る。** ⚠️ **いまは広いほうの誤りを採っている** —— `AG-9` の MUST を守るほうを残し、除外の文だけが破れている。**逆にすると MUST が破れる** | E | W5 | `AG-9` / `WS-2` / `PD-1` / `PD-5` / `UN-8` / `UN-9` / 表 T-064 の `PI-18` | ⛔ **`tests/unit/uf-48-write-moment.test.ts` の 2 件が既に赤で立っている**（パンの押下中・範囲選択の押下中に書き込みが着地すること）| 未裁定 |
| PD-170 | **全体表示を取り消したとき、倍率と表示位置まで戻ってしまうこと** —— ⛔ **`UN-17` は「戻るのは畳みだけであり、倍率と表示位置は `UN-8` のまま対象外である」と定めるのに、`WS-4` が積む段は文書まるごとを持ち、取り消しはそれを逐語で戻す** | **段が「文書まるごと」であるかぎり部分的には戻せない。⭐ 選べるのは 2 つ —— ①全体表示を履歴の対象から外す（`UN-17` が対象と明記しているので仕様に反する）②段が戻す範囲を列で絞れるようにする（`FR-031` の段の定義が動く）。⛔ どちらも仕様の側の裁定である** ⭐ **2026-08-24 に調べ直した結果、CR-217（版 0.94）が既に当て終えていた** —— ⛔ **一覧だけが腐っていた。**⚠️ **引継書が足そうとした 1 文「表 T-027 が対象外と定める値を戻してはならない」は、裁定で明示的に退けられた案である。書けば退けたはずのものを入れることになる。** | G | W5 | `UN-17` / `UN-8` / `WS-4` / `FR-031` / `FR-055` | ⛔ **まだ無い。** ⭐ 全体表示 → 倍率を変える → 取り消し、で倍率が戻らないことを見る試験 | 裁定済 |
| PD-171 | **`OP-10` の表示位置を、全体表示が選ぶものにすること** —— ⛔ **`OP-10` は「`FR-055` の全体表示が選ぶ倍率と表示位置にすること（MUST）」と定めるのに、`fitZoom`（`PI-5`）は倍率しか答えず、シェルが位置を自分で決めている**（最も早い予定の開始日）。⚠️ **`FR-055` の RATIONALE は「左の張り出しが行見出しパネルに隠れる」ことをその害として名指している** | **`fitZoom` が位置も答える。⛔ `PI-5` の公開メンバの署名が動くので裁定が要る** | E | W5 | `OP-10` / `FR-055` / 表 T-064 の `PI-5` | ⛔ **まだ無い。** ⭐ 実績が予定より前に始まる文書で、全体表示の位置が張り出しを隠さないことを見る試験 | 裁定済 |
| PD-172 | **確認を立てる者** —— ⛔ **`FR-032` は行の削除と WBS の子孫を持つ `Task` の削除に確認を求める（MUST）のに、`ScreenSession.confirmation` に `null` 以外を置く者が `src/` に 1 人もいない。** ⭐ **問いの面・2 択の入口・描画・答えの返り道は本回で全部そろった** | **問いを答えが返るまで保持する者を決める。⚠️ `LY-5` は現在値を `Framework` に置くので、そこが持つのが素直だが、⛔ 削除の命令を「答えを待つあいだ」どう保つかを仕様が述べていない** | E | W5 | `FR-032` / `NT-7` / `OP-4` / `DI-4` / `IC-69` / `IC-70` | ⛔ **まだ無い。** ⭐ WBS の子孫を持つ `Task` を消そうとすると問いが立ち、取りやめると 1 つも消えない試験 | 未裁定 |
| PD-173 | **選ばれた表示言語が次の起動まで残らないこと** —— ⛔ **`FR-038` は「起動したときは前回選ばれた言語で開き、それを読み出せないときはブラウザの言語設定に従うこと（MUST）」と定めるのに、後半しか走っていない。** ⚠️ **`S-99` は 表 T-206 が `localStorage` に置くと定める 4 行の 1 つで、`src/` はその 4 行をどれも読み書きしない** | **`localStorage` に置く 4 行を運ぶ者を決める。⚠️ `PI-29`（`LocalStorageDocumentStore`）は文書の自動保存を持つが、この 4 行は文書ではない** ⭐ **2026-08-24 に調べ直した結果、仕様は動かないと判じた** —— 表 T-060 の `LY-5` と 表 T-062 の `CP-25` が既に担い手を定めており、`FR-020` に先例がある。⛔ **`S-99` の配線は `frame-loop.ts` に既に在る。残るのは試験だけである。** | E | W5 | `FR-038` / `S-99` / 表 T-206 / `PI-29` | ⛔ **まだ無い。** ⭐ 言語を選んで開き直すと同じ言語で開く試験 | 裁定済 |
| PD-174 | **押す前に現在の言語が読めること（ヘッダーの側）** —— ⛔ **`FR-038` は入口を 2 か所に置くこと（MUST）と、押す前に現在どちらの言語かが読めること（MUST）を並べて定める。** ⭐ **ヘルプの面は運んでいる。** ⛔ **ヘッダーの入口は運ぶ欄を持たない** | **`CommandItem` に運ぶ欄を足すか、既存の「入っているトグル」の欄で表すか。⚠️ 後者は 2 言語の選択に「切」が無いので嘘になる。⛔ どう見せるかを仕様が述べていないので裁定が要る** | E | W5 | `FR-038` / `IC-21` / 表 T-109 | ⛔ **まだ無い** | 裁定済 |
| PD-175 | **「他の行に表示されている」ことを人に見せる印** —— ⛔ **`FR-032` はその旨を示すこと（MUST）と定めるのに、語（`PD-160`）も図形（表 T-109 が図形の全数）も決まっていない。** ⭐ **値は属性として画面まで届いている** | **語か図形のどちらかを決める。⛔ どちらも利用者の裁定である**（表 T-109 は新しい図形の発明を禁じ、語は `PD-160` が待っている）| E | W5 | `FR-032` / `NT-7` / `PD-160` / 表 T-109 | ⛔ **まだ無い** | 未裁定 |

⚠️ **3 つの掴み代（`S-90` / `S-91` / `S-137`）が同じ 6px になったことにも由来は無い。** ⭐ **原稿の 🔎 がそれを印している** —— 選び直してよい。


---

## 2026-08-23 のアーキテクチャの巡が足した 1 行（表 T-014 の ST-7）

⭐ **監査（`docs/review/audit-cadence-and-architecture-2026-08-23.md` の 4.）が「測れなかったこと」に挙げた 1 件である** —— 同書は経路を追ったが、どちらが仕様の意図かを本文から決められなかった。
⭐ **巡の先頭で問い、その場で裁定を受けた**（規則 06 の 3b）。⛔ **仕様への適用はまだである。**

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-178 | **安全弁に達したときの返し方** —— ⛔ **表 T-014 の `ST-7` は「達したらエラーを出して処理を止めること（MUST）」と定め、`FR-028` は「例外を投げてはならない（MUST NOT）」と定めるのに、本文がこの 2 つを調停していない。** ⚠️ **`layoutFromSchedule` は `StackSafetyCapReached` を投げ、`src/` にも `index.html` にも捕まえる者が 1 人もいない** —— 監査の測定では `runFrame` が死に、画面が黙って止まる | **値で返す。表 T-233 に行を新設する**（いずれも利用者の裁定 2026-08-23）。⭐ **根拠は前プロジェクトの確定文書が 2 か所で「到達したら人に知らせる（黙って捨てない）」と書いていること**（`03-ui-naming/ui-detail-spec-ja.md` の 1105 行 ／ `07-plan-actual/plan-actual-decisions-ja.md` の 925 行）。⚠️ **`FR-033` の貼り付けの拒否は値でしか答えられず**（`edit-task.ts` の ⛔ MISSING が「段数は文書だけの関数では数えられない」と書いている）、**throw を残すと同じ安全弁が 2 つの機構を持つ。** ⭐ **呼び手は `frame-loop.ts` の 3 か所だけなので実費が小さい** | E | W5 | 表 T-014 の `ST-7` ／ `FR-028` ／ `FR-033` ／ 表 T-233 ／ 表 T-064 の `PI-5` | ⭐ **`tests/unit/layout-engine.test.ts` の 512 行** —— いま `toThrow(StackSafetyCapReached)` を見ており、値で返す形にすると必ず落ちる | 裁定済 |

---

## 2026-08-24 の踏査が開けた 11 行

⛔ **どれも `D`〜`H` である。** ⭐ **実装しない。** ⚠️ **配線の巡は、これらが閉じるまで該当の継ぎ目を開けられない。**
⭐ **全部、規則 06 の 1.（仕様 → `docs/reference/` → 前プロジェクト）を当てたうえで残ったものである。**

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-179 | **貼り付けが安全弁に達したかを、誰が数えるか** —— ⛔ **`FR-033` は「段が `ST-7` の安全弁に達したときは貼り付けを受け付けずに通知すること（MUST）」と定めるのに、段数はレイアウトの量である**（`ST-1` が占有幅で数え、表 T-038 が日付だけで数えることを MUST NOT で禁じる）。⚠️ **表 T-067 の `WS-1` 〜 `WS-5` はすべて `pure` で「外を読まない」と本文が明言しており、書き込みの経路がレイアウトを見る行が無い** | **命令にレイアウトを渡す道を決める。**⛔ **`src/use-case/edit-document/edit-task.ts` が自ら「その数はここでは取れない。署名も仕様も誰が渡すかを述べていない」と書いている** | E | W5 | `FR-033` ／ `ST-1` ／ `ST-7` ／ 表 T-038 ／ 表 T-067 | ⛔ **まだ無い。** ⭐ 安全弁に達する行へ貼り付けると受け付けられず通知される試験 | 未裁定 |
| PD-180 | **`localStorage` の鍵の綴りを仕様に載せるか** —— ⛔ **`frame-loop.ts` が自ら「接頭辞の後ろの綴りは仕様に無い。公開するには変更要求が要る」と書いている。**`grsched.language` ほか 4 つは同ファイルが自分で選んだ | **載せないほうを推す。**⭐ 自動保存の鍵（`grsched.autosave`）も仕様に無く、先例がある。⛔ **載せるなら 表 T-206 に鍵の欄が要り、図・表示語・重複基準線の 3 つに連鎖する** | D | W5 | 表 T-206 の `S-99` ／ `S-99a` ／ `S-99b` ／ `S-99c` ／ `PD-110` | ⛔ **まだ無い** | 未裁定 |
| PD-181 | **`S-99a` / `S-99b` / `S-99c` に書き手がいない** —— ⛔ **`S-99a`（透かしに出す名前）は `FR-020` が「入力させ、`localStorage` へ保存すること」と定めるのに入口が無い。⛔ `S-99b`（`Agent API` を有効にした文書）を書く者がいない。⛔ `S-99c`（解除パスワードの `SHA-256`）は 表 T-037 に問う行が無いので誰にも訊けない** | **3 つを別々に決める。**⚠️ `S-99c` は問う道そのものが無いので、表 T-037 に行が要るかから始まる | E | W5 | `FR-020` ／ `FR-086` ／ 表 T-206 の `S-99a` `S-99b` `S-99c` ／ 表 T-037 | ⛔ **まだ無い** | 未裁定 |
| PD-182 | ⭐ **2026-08-24 の踏査で解けた。裁定は要らなかった。** `FR-028` の MUST は「既定では公開しないこと」であり、起動側のフラグと画面での有効化は「か」で結ばれている —— **片方だけで足りる。** `FR-065` の画面の道は必須であり、公開する識別子は用語集 §3 が既に綴っている。⛔ **起動側のフラグは、そもそも存在を求められていない。** 旧記述: **`FR-028` の「起動側のフラグ」に名前も置き場も無い** —— ⛔ **同要求は「既定では公開しないこと（MUST）—— 起動側のフラグか、人による画面での有効化で公開する」と定めるが、フラグを持つ行がどの表にも無い。**⚠️ **`installAgentApi` の呼び出し元は `src/` に 0 件である** | **何をもって旗とするか（ビルド時の定数か、`URL` の問いかけか、宿主の値か）と、答えを置く「公開点」の名を決める** | E | W5 | `FR-028` ／ 表 T-035 ／ 表 T-065 の `IF-7` | ⛔ **まだ無い** | 裁定済 |
| PD-183 | ⚠️ **2026-08-24 の踏査で半分だけ解けた。** 置き場は決まっている（表 T-024 の `IO-5`、`FR-026` の「同じ鍵を上書きするだけ」）。⛔ **残るのは鍵の値と 4 つ目の状態だけであり、`FR-061` が逐語で「まだ決まっていない」と書いているので、これは本物の裁定待ちである。** 旧記述: **`FR-061` の鍵と、4 つ目の状態** —— ⛔ **同要求が逐語で「本要求が使う鍵はまだ決まっていない」と書き、表 T-227 の `DI-1` を使うことを MUST NOT で禁じている。**⛔ **3 状態のどれも「開いたばかりでまだ 1 度も自動保存していない文書」を述べていない** | **鍵を決め、4 つ目を立てるか `saved` に倒すかを決める。**⛔ **`IF-4` の `writeSnapshot` が鍵を要求するので、これ無しには繋げない** | E | W5 | `FR-061` ／ 表 T-227 の `DI-1` ／ 表 T-065 の `IF-4` | ⛔ **まだ無い** | 未裁定 |
| PD-184 | ⭐ **2026-08-24 の踏査で解けた。裁定は要らなかった。** 写し方は `AG-9` 自身が持つ（「対象は 表 T-027 の取り消し対象行と一致させる」）。⛔ **拒否の区分を身振りごとに割ってはならない** —— 表 T-233 は `RS-7` の 1 行しか持たず、同表の前文が持たない理由を運ぶことを MUST NOT で禁じている。**問いは自分で答えていた。** ⭐ **さらに実測で分かったこと: 逐語どおりに広げると、豊かになるどころか壊れる** —— 画面の入口に落ちた押下は 表 T-023a の行を持たない（同表の判定順序は日程の描画領域だけに掛かる）ので、⛔ 行を運ぶ形にすると「行が無い」が「文書を変えない」と読まれ、パレットの押下すべてから `AG-9` の保護が外れる。**真偽値のままが正しい。** 旧記述: **`IF-7` が運ぶ身振りの行 ID を、拒否の区分へどう写すか** —— ⛔ **表 T-065 の `IF-7` は「どの身振りの最中か（表 T-023a の行 ID を運ぶ）」と定めるのに、`snapshot-source.ts` は真偽値のままである**（`CR-217` が広げた欄に `src/` が追いついていない）。⚠️ **`AgentRefusalReason` は 2 値である** | **行ごとに分けるか 2 値のままにするかを決める。**⭐ **仕様が行 ID を運べと定めている以上、運ぶ側は動く。分けるかは拒否の区分の話である** | E | W5 | 表 T-065 の `IF-7` ／ 表 T-035 の `AG-9` `AG-9a` ／ 表 T-023a | ⭐ `tests/contract/if-7-snapshot-source.test.ts` | 裁定済 |
| PD-185 | ⭐ **2026-08-24 の踏査で解けた。裁定は要らなかった。** 綴りは規則 06 の探索順の 3 段目にあった —— `previous-project-result/10-agent-interface/` が HTML の id と「ちょうど 1 つ」を明記している。⛔ **分類 D も誤りであった** —— この id は `GRS JSON` にも `MSPDI` にも届かない（表 T-024 の `IO-7` は書出のみ）ので移行が要らず、C である。旧記述: **`IF-8` の入れ物の id の綴り** —— ⛔ **`app-shell-source.ts` は「シェルが id を持つ」と定めるだけで値を述べておらず、仕様のどの表にも無い。**⚠️ **`BT-1` が読む側なので、綴りが 1 つに決まらないと起動の 1 段目が閉じない** | **1 つに決めて記録する。**⭐ 分類は `A` に見えるが、⛔ **保存された `.html` から読み出す綴りなので、覆すと出荷済みの単一 `.html` が開けなくなる** | C | W5 | 表 T-065 の `IF-8` ／ 表 T-034 の `BT-1` ／ `FR-067` | ⛔ **まだ無い** | 裁定済 |
| PD-186 | ⛔ **2026-08-24 の踏査で、本行の主張が逆であることが分かった。** `FR-020` は「選ばせる」であって「問う」ではなく、`NT-7` は「要求が確認を求めると定めた場面だけ」に問うことを許し、`FR-031` はそれ以外で確認を求めることを MUST NOT で禁じている —— ⛔ **表 T-037 に行を足すと 2 つの MUST を破る。** ⭐ **要るのは表 T-202 の真偽の行 1 つである。**残る裁定は 1 つだけ: **透かしをしまったとき、書き出す絵からも消えるか。** 旧記述: **「画像に載せるかどうか」を問う行が 表 T-037 に無い** —— ⛔ **`FR-020`（MUST）は選ばせることを求め、`FR-025` は「`LM-8` はこの経路にも掛かるのでここでも通すこと（MUST）」と `IO-6` にも及ぼしているのに、`NT-7` は「続ける／取りやめる」の 2 択しか持たない。**⛔ **`src/` にも問う道が無い** | **2 択で足りるのか、別の作法が要るのかを決める。**⚠️ **`IF-5` と `IF-6` の両方に掛かる** | E | W5 | `FR-020` ／ `FR-025` ／ 表 T-004 の `LM-8` ／ 表 T-037 の `NT-7` ／ 表 T-024 の `IO-6` | ⛔ **まだ無い** | 未裁定 |
| PD-187 | **浮いた `Promise` 3 本の拒否をどうするか** —— ⛔ **`frame-loop.ts` の 3 本が `void ...().finally(...)` の形で、`.finally` は拒否を消費しない。**⚠️ **3 本とも「拒否は `IF-3` の契約違反である」と自ら宣言している** | **握り潰す・そのままにする・新しい行で告げる、のどれかを決める。**⛔ **仕様がどれとも書いていない。**⭐ 表 T-004 の `LM-14` とは別の話である | E | W5 | 表 T-065 の `IF-3` ／ 表 T-004 の `LM-14` ／ 表 T-233 | ⛔ **まだ無い** | 未裁定 |
| PD-188 | **失敗値の形が揃っていない** —— ⛔ **実測: 弁別子が 4 通り（`ok: false` ／ `accepted: false` ／ `kind:` ／ 旗なし）、積荷の名前が 7〜9 通り、`ImportRefusal` という同名の型が別の形で 2 つ在る** | **揃えるかどうかを決める。**⭐ **揃えるなら 表 T-064 の公開名が動くので `E` である。**⚠️ **揃えないなら、読む側は型ごとに形を覚えることになる** | E | W5 | 表 T-064 ／ `PI-8` ／ `PI-13` ／ `PI-19` | ⛔ **まだ無い** | 未裁定 |
| PD-189 | **`uuid` の 10 列を誰も見ていない** —— ⛔ **生成スキーマの `format` のうち、日付 2 件は 表 T-220 の `IV-14` が持つが、`uuid` の 10 件は他に持ち主がいない。**⚠️ **CR-239 が検証器に持たせない側で書いたので、10 列は誰にも見られないまま残る** | **検証器に持たせるか、表 T-220 に行を足すかを決める。**⛔ **`CR-239` はこれを穴として記録しただけで、埋めていない** | E | W5 | 表 T-220 の `IV-14` ／ Chapter 6.1 の 表 T-220 の前文 | ⛔ **まだ無い** | 未裁定 |
| PD-190 | **`clampedSettings` を誰も呼んでいない** —— ⛔ **表 T-064 の `PI-2` が「下限・上限に収める」として公開しているのに、`src/` の呼び手が 0 件である。**⚠️ **`scheduleViolations` とまったく同じ形の穴であり、2026-08-24 の巡はそちらを閉じてこちらを見落としていた。**⛔ **どこで呼ぶかを仕様が 1 行も述べていない** | **読む路（`CP-20` か `CP-10`）で呼ぶのを推す。**⭐ 根拠は 表 T-024a の `OP-6` が読む側に寛さを求めていることと、`ClampResult` が「動かした値」の一覧を返す形になっていること —— ⚠️ **その一覧を人へ告げるなら、表 T-233 に行が要る**（作法は `NT-5`）。⛔ **告げるかどうかも未決である** | E | W5 | 表 T-064 の `PI-2` ／ 表 T-024a の `OP-6` ／ `CP-10` ／ `CP-20` ／ 表 T-233 | ⛔ **まだ無い。** ⭐ 下限を割る設定値を持つ文書を開くと、値が収まり、そのことが告げられる試験 | 未裁定 |
| PD-191 | **フェードの掴み点を、幾何の側が選択で絞れない** —— ⛔ **`FR-075` の MUST と `S-111` は「選択中のタスクだけ」と定めるが、`geometryFromLayout` も `itemAtPointer` も `Selection` を渡されていない。**⚠️ **描く側は選択を見ており正しい。当たり判定だけが広い** ⛔ **2026-08-26 の裁定: 矢羽根もレクタングルも掘み点を出せ。**⭐ **絞りを外し、遮りは当たり判定を選択で絞って直す** | ⭐ **2026-08-26 に本筋を当てた** —— `geometryFromLayout` の署名に `Selection` を通し、フェードの日数での絞りを外した。⚠️ **その絞りが効いて見えたのは、起動テンプレートの 1000 タスクのうち 997 が `fadeInDays` を持たないからであり、実際には 99.7% の行で掴み点を消していた。**⛔ **矮すぎる切り口であった。**⭐ **矢羽根とレクタングルには新しいコードなしで掴み点が出た** —— 形の門（`actualPlacement === 'inside'`）が元から `FD-5` の `SH-1` と `SH-2` を通していた。⚠️ **当たり判定の側（`itemAtPointer`）には選択を渡していない** —— 出す側が既に絞っているので、`S-111` の条件を 2 か所に書かない | E | W4 | 表 T-064 の `PI-6` ／ `FR-075` ／ `S-111` ／ 表 T-023d の `GR-1` `GR-2` `GR-3` `GR-4` | ⭐ **選択していないタスクの角を押しても `GR-1` にならない試験** | 裁定済 |
| PD-192 | **1 行より短いパンが、行丁度飛ぶこと** —— ⛔ **`S-78` は表示位置を行まるごとで持つので、行の途中を表せない。**⚠️ **床を置かなければ短いパンは何も動かず、置けば 表 T-023 の `MK-7` の「パンは等倍とする（MUST）」を破る。**⭐ **`PD-176` と同じ穴の、ホイールではない側である** | **1 行の床を置いた（`rowTurnedTo` を `PD-1` も呼ぶ）。**⭐ 根拠は、利用者の訴えが「背景を掴んでスクロールできない」であり、**動かないことのほうが大きい不具合だからである。**⛔ **本筋は `S-78` に行内の端数を持たせることだが、それは保存形の変更である** | D | W4 | `S-78` ／ 表 T-023 の `MK-7` `MK-1` ／ 表 T-023a の `PD-1` | ⭐ **1 行未満のパンで絵が動く試験** | 裁定済 |
| PD-193 | **2 連カーソルが 1 本も描かれていない** —— ⛔ **表 T-029a の `DC-8`（MUST）は追従している側を太く描けと定めるが、描く側はカーソルを見ていない。**⚠️ **実測: `DocumentSettings.dualCursor` はむき出しの無いオブジェクトか `null` としか型付けされておらず 2 つの日付が型付けされていない、`ScheduleGeometry` に行が無い、描画側に STOP が立っている。**⭐ **`DC-8` 以前の穴である** | **`dualCursor` の 2 つの日付を `erd.json` で型付け、`ScheduleGeometry` に行を与えるのを推す。**⚠️ **値の裁定ではなく、形の欠落である** | E | W4 | 表 T-029a の `DC-1` 〜 `DC-8` ／ `S-71` ／ 表 T-064 の `PI-6` | ⭐ **追従している側が太く描かれる試験** | 未裁定 |
| PD-194 | **コメントボックスが 1 つも描かれていない** —— ⛔ **表 T-076 の `EP-5` は注記を描くと定め、表 T-023c の `SL-8` は選択時に枠で囲めと定めるが、`commentBoxes` は `layoutEngine` にも描画側にも現れない。**⚠️ **ハイライトボックスは描かれており、枠も付く** —— 同じ行の片方だけが空いている。⭐ **CR-258 以前の穴である** | **`ScheduleGeometry` にコメントボックスの矩形を与えるのを推す。**⚠️ **文字を測って大きさを決めることは `FR-093` が禁じている**ので、大きさを持つ行が先に要る | E | W4 | 表 T-076 の `EP-5` ／ 表 T-023c の `SL-8` ／ `FR-093` ／ 表 T-064 の `PI-6` | ⭐ **コメントボックスを持つ日程を描くと要素が出る試験** | 未裁定 |
| PD-195 | **描く深さの上限が 1 に張り付き、行が 68 → 7 に落ちる** —— ⛔ **実測: `fitZoom` が `zoomY` 0.0708 を答え、深さ 2 のしきい値 0.32 を割る。**⚠️ **起動テンプレートが `scrollDate` を `null` で出すので `OP-10` により毎フレーム再フィットされ、保存値を上書きし続ける。**⛔ **`setZoom` は `scrollDate` を書かないので、縦の拡大では抜け出せない。**⭐ **行の操作子が効かないのもインデントが浅いのもこの 1 つが原因** | **決めるのは 3 つ** —— ① 再フィットが保存値を上書きしてよいか、② `setZoom` も `scrollDate` を書くべきか、③ `groupLevelOfDetailBase`（0.32）は妥当か。⛔ **値の調整ではなく設計の穴である** | G | W4 | `FR-018` ／ `FR-055` ／ 表 T-024a の `OP-10` ／ `S-87` `S-88` | ⭐ **新規文書で縦の拡大を押すと深い行が現れる試験** | 未裁定 |
| PD-196 | **4 段目（年・月・日・曜日）のしきい値が遠い** —— ⛔ **`S-85` は 30 px/日で、届くには `zoomX` ≧ 5.83（ホイール約 19 ノッチ）要る。**⭐ **参考実装は 8 px/日（`zoomX` ≧ 1.33）** | ⛔ **2026-08-26 の裁定: 30 → 8 前後へ下げる。**⚠️ **`FR-017` が実装による書き換えを禁じるので、原稿の 1 セルを変える** | A | W4 | `S-85` ／ `FR-017` ／ 表 T-005a の `L-1` | ⭐ **`zoomX` を数ノッチ上げると 4 段目に入る試験** | 裁定済 |
| PD-197 | **実績のダミーを掘めない** —— ⛔ **利用者の報告（2026-08-26）: ダミーのある辺りにポインタを持っていっても現れず、掘めない。**⚠️ **未調査** | **まず測る。**⭐ 仕様は既に「薄く描き、ポインタが乗っているあいだだけ濃くすること（MUST）」と定め、`FR-048` の名簿にも入っている。⛔ **裁定ではなく不具合である可能性が高い** ⛔ **裁定すべきことは無い** —— 利用者の「掘めるようにしろ」が答えであり、仕事は実装と調査である。⭐ **行は記録として残す** | B | W4 | `FR-043` ／ `FR-013` ／ `GR-9` `GR-17` `GR-18` ／ `S-93` ／ `S-131` | ⭐ **ポインタをダミーに乗せると濃くなり、押すと実績が置く試験** | 裁定済 |
| PD-198 | **マイルストーンの一覧を開く入口の図形** —— 状態は `S-142` が持つが、図形が `▼` / `▲` である | ⛔ **2026-08-26 の裁定: `...` のトグルにする。**⚠️ **表 T-109 の行と 図 F-019 の図形を同じ編集で足すこと** —— 片方だけだと生成器が拒む | C | W4 | `S-142` ／ `FR-053` ／ 表 T-109 ／ 図 F-019 | ⭐ **一覧を開く入口が `...` で描かれる試験** | 裁定済 |
| PD-199 | **実績のダミーを描く図形と大きさが、どの表にも無い** —— ⛔ **`U-52` に仕様が与えているのは 4 行だけである**：`S-129` と `S-130` は期間、`S-131` は薄さ、`S-93` は**当たり判定**。⭐ **描く大きさの行が 1 つも無い。**⚠️ **これが「ダミーが見えない」の真因である** —— 当たり判定は生きており、目をつぶって押せば掴める | **表 T-210 に、`S-109` ・ `S-110` の隣へ 1 行置くのを推す** —— フェードの掴み点がまさにその形で描く大きさを持っている。⛔ **図形そのものは発明しない** —— 表 T-026 の `RC-13` が図形の決定を利用者の裁定と定めている。⚠️ **`GR-9` と `GR-17` が 2 つの点なのか 1 本の帯の両端なのかも同じ行が決める。⛔ **分類 `E` の根拠**: コード側に暫定値を置ける話ではなく、**行が先に要る**。`PD-194` と同じ形である | E | W4 | `U-52` ／ `S-131` ／ `S-93` ／ 表 T-210 ／ 表 T-023d の `GR-9` `GR-17` `GR-18` ／ `FR-013` | ⭐ **未着手のタスクを描くとダミーの要素が出る試験** | 未裁定 |
| PD-200 | **`FD-6` の「期間」が暦日なのか稼働日なのか** —— ⛔ **実装が 2 つに割れている。**`schedule-geometry.ts` は暦日で切り詰め、`edit-task.ts` の `IV-12` は稼働日で拒む。⚠️ **非稼働日をまたぐ予定では、`FD-6` が許すフェードを `IV-12` が拒む** | **暦日を推す** —— ⭐ 根拠は 表 T-012a が 4 点を「横（日）」と日の軸で綴っていることと、`FD-6` が既に実装されている唯一の場所が暦日であること。⛔ **ただしこれは受理の境界を動かす** | F | W4 | 表 T-012a の `FD-6` `FD-7` ／ `IV-11` `IV-12` ／ `FR-075` ／ 表 T-023d の結びの規則 | ⭐ **非稼働日をまたぐ予定で、角を端まで引いても拒まれない試験** | 裁定済 |
| PD-201 | **表 T-078 に、長押しの連続がフレームを起こす契機の行が無い** —— ⛔ **同表は「本表に無い契機でフレームを起こしてはならない（MUST NOT）」と自らを閉じている。**⚠️ `FT-1` の括弧は `CS-4`（人の答え待ち）を名指し、`FT-4` の注は「数えるのは 3 つ」と書いている | **`FT-4` の注を 3 から 4 へするのを推す** —— ⭐ 同行は「時間が来たこと」「シェルが自分で計る」であり、長押しの刻みはまさにそれである。⚠️ **`FT-1` の括弧を広げる道もあるが、そちらは「待ちをまたぐ続き」の意味を緩める | G | W4 | 表 T-078 の `FT-1` `FT-4` ／ `FR-018` ／ `S-172` `S-173` | ⭐ **長押しで倍率が刻まれ続ける試験** | 未裁定 |
| PD-202 | **タイムルーラの帯を塗ると述べた行が無い** —— ⛔ **実測（開発サーバの実 DOM）: 矩形は 5 枚とも行の帯であり、ルーラの帯に 1 枚も無い。**⚠️ **背後が透けるという利用者の報告はこれである** | **`FR-041` の「地の色を自分で塗ること（MUST）」を根拠に、`S-146` で帯に 1 枚敷くのを推す** —— ⭐ **`S-146` は既に `SvgRenderer` へ届いている**（本巡で経路を通した）。⛔ **ただし帯の範囲を述べた行が無いので、体は描かずに止まった。⛔ **分類 `E` の根拠**: コード側に暫定値を置ける話ではなく、**行が先に要る**。`PD-194` と同じ形である | E | W4 | `FR-041` ／ 表 T-236 の `S-146` ／ `S-2` ／ 表 T-005a の `L-1` | ⭐ **ルーラの帯に地の矩形が 1 枚出る試験** | 未裁定 |
| PD-203 | **全体表示の横をどの倍率で測るか** —— ⛔ **基準を名乗る行が無い。**⚠️ 縦の答えは横に乗る —— `S-86` が低い `zoomX` で絵を間引き、高い `zoomX` では幅広の帯が重ならなくなるので、**同じ深さでも描く高さが `zoomX` で動く** | **等倍で測るのを推す** —— ⛔ 自分が書く倍率で測ると、答えがもう一度漸化式になり `D-22` の冕等性が戻らない。⭐ 等倍は保守的な底でもある —— `S-86` が何も落とさないので、合わせる実寸が最も大きい | B | W5 | `FR-055` ／ 表 T-068 の `LC-1`〜`LC-9` ／ `S-86` | ⭐ **保存された `zoomX` を動かしてから押しても同じ絵になる試験** | 未裁定 |
| PD-204 | **深さ 1 に降りたときの縦の倍率** —— ⛔ **`FR-018` の定義域は深さ 2 以上であり、深さ 1 には「描ける最小の倍率」が存在しない。**`FR-055` の MUST NOT はそこで空転する | **深さ 2 のしきい値の `S-53` 1 刻み下を推す** —— ⭐ 深さ 1 だけを描く最大の倍率であり、`FR-055` の「無用に縦幅を増やすな」に合う。⚠️ 縦ズーム拡大 1 押しでちょうど深さ 2 に乗る。⛔ `FR-055` の RATIONALE はここまで述べていない | B | W5 | `FR-055` ／ `FR-018` ／ 表 T-205 の `S-87` `S-88` ／ `S-53` | ⭐ **深さ 1 に落ちた後、縦ズーム拡大 1 押しで深さ 2 が出る試験** | 未裁定 |
| PD-205 | **全体表示が横だけを `S-97` ／ `S-98` に収め、縦を収めないこと** —— ⛔ **`FR-016` は範囲をズーム操作に置き、`CM-71` が適用するが、どちらの軸に掛かるかを述べた行が無い** | **横は収め、縦は収めないのを推す** —— ⛔ 横を収めなければ、`CM-71` が切った倍率で描くことになり、**測っていない絵を出す。**⭐ 縦の着地値はすべて `S-97`〜`S-98` の内側にあり、clamp すればしきい値を外れて別の深さを答えてしまう | B | W5 | `FR-016` ／ `CM-71` ／ 表 T-206 の `S-97` `S-98` | ⭐ **横の答えが `S-97`〜`S-98` に収まり、縦がしきい値の上に乗り続ける試験** | 未裁定 |
| PD-206 | **全体表示だけが深さを直に指定できること**（`groupDepthCap`）—— ⛔ **表 T-068 の 1 回目は「その文書が持つすべての深さ」を 1 つの倍率で測れと定めるが、その倍率では `FR-018` が深さを 1 つに定めてしまうので、`zoomY` 経由では深い段を頒めない** | **`FR-055` 専用の引数として足すのを推す** —— ⭐ 表 T-064 の見出し自体が引数と戻り値を `src/` に任せており、**公開されたメンバは動かない。**⚠️ 他の呼び手が渡さないことは規則ではなく慎みである | A | W5 | `FR-055` ／ 表 T-068 ／ `FR-018` ／ 表 T-064 の `PI-5` | ⭐ **全体表示以外の呼び手が深さを渡さず、`FR-018` の導く深さを得る試験** | 未裁定 |
| PD-207 | **2 回目が一段退いたとき、表示位置をどの測定から取るか** —— ⛔ **どの行も述べていない。**退いた深さを厳密に測るには 3 回目が要るが、表 T-068 の結びの規則がそれを禁じている | **床の測定から取るのを推す** —— ⭐ `scrollGroupId` はどちらでも厳密（同じ深さは同じ行を同じ順で描く）。⛔ `scrollDate` はわずかに遅れ得るが、`LC-6` は左にラベルを置かないので、**ずれは `LF-10` の図形半分（予定高さの半分）が上限である** | B | W5 | `FR-055` ／ 表 T-068 の `LC-6` ／ `LF-10` ／ `S-77` `S-78` | ⭐ **2 回目が退いた文書で、表示位置が描く絵の左端から予定高さの半分を超えて離れない試験** | 未裁定 |
