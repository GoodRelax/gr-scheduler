# 裁定待ちの一覧

**規則は [`docs/development-rules/06-pending-decisions.md`](../development-rules/06-pending-decisions.md)。**

⛔ **載せる前に引く** —— 仕様 → `docs/reference/` → 前プロジェクト。**未読を裁定待ちにしない。**
⭐ **`A`〜`C` は推奨値で実装し、印（`// @provisional PD-n`）を付けて先へ進む。⛔ `D`〜`H` は実装しない。**

---

## 分類（[規則 06](../development-rules/06-pending-decisions.md) の 2.）

| | 進めてよいか | 何が未決か |
|---|---|---|
| **A** | ✅ | 原稿にあり生成で届く値（線幅・色・待ち時間・寸法）|
| **B** | ✅ | 純関数の内部。観測できる振る舞いは仕様が固定している |
| **C** | ✅ | 表示だけの選択で、保存形に痕跡が残らない |
| **D** | ⛔ | 文書または `MSPDI` に保存・交換される値 |
| **E** | ⛔ | 公開署名・継ぎ目（表 T-064 / T-065）|
| **F** | ⛔ | 入力を受理するか拒むかの境界 |
| **G** | ⛔ | 他の規則の論証が乗っている値（しきい値・上限・不変条件）|
| **H** | ⛔ | 暫定の上に暫定を積むこと（**例外なく禁止**）|

---

## 一覧

⭐ **`A`〜`C` はまとめて承認できる形で並べる。`D`〜`H` は 1 件ずつ問う。**
⛔ **裁定を受けたら、行は消さずに状態を `裁定済` にする**（記録である）。

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-2 | **マイルストーンの 8 図形の頂点** —— `S-48` が ☆ の腰の位置を与えるだけで、図形の角そのものはどの表にも無い（図 F-019 が描くのはパレットの**アイコン**である）| **8 つとも同じ円に内接させる。**〇 は 24 角形、⬡ は 6、⬠ は 5、◇ と □ は同じ 4 角を 1/8 回転ずらしたもの、△ と ▽ は 3 角、☆ は 10 角で腰を `S-48` に採る。⭐ 根拠は `S-48` の備考「この値が ☆ の面積を決め、`milestoneGlyph` の面積順に影響する」—— **面積の順が言えるのは、8 つが同じ大きさを共有しているからである。** ⚠️ 〇 を多角形にするのは、`BarGeometry` の outline が経路しか持たないためで、弧の形を足すと受入済の 2 ユニットが建てている型が動く | B | W2 | `S-48` / `AT-101` / `FR-078` / `LF-10` | ⭐ **8 つが互いに違う頂点数で出る試験**（`schedule-geometry` の受け入れ試験）。⚠️ **`S-48` を変えると ☆ の腰が動く試験** | 未裁定 |
| PD-1 | **`themeHue` から実際の色を解く彩度と明度、および固定色 2 つの値** —— 依存線・イナズマ線の色と、注記の色 | **予定は彩度 62% / 明度 46%、実績は明度 30%、依存線とイナズマ線は `#6b7280`、注記は `#b45309`。** ⭐ 根拠は「`_assets/tbl-settings.md` の §5 が解き方を散文でしか持たない」こと —— 「色相ごとに、コントラスト規則が壊れる寸前まで濃い地色」とあるだけで、数が 1 つも無い。⚠️ **実測されているのは色相 214 だけ**なので、その近傍に置き、実測を名乗らない | C | W4 | `S-73` / `FR-041` / `FR-042` / `FR-019` / `NFR-007` | ⭐ **`themeHue` を変えると SVG の色が変わる試験**（`svg-renderer` の受け入れ試験）。⚠️ **コントラスト比の試験は `NFR-007` が測る対象を決めてから** | 未裁定 |
| PD-4 | **ヘッダーとパレットの入口の語** —— ⭐ **置き場は `CR-194` が作った**（`docs/spec/_source/display-words.json`）。⛔ **未決なのは語そのものに変わった** | **空のまま置く。**⭐ 根拠と扱いは [[PD-160]] と同じ —— ⛔ agent が語を書くと確定名の新設になる | C | W4 | `FR-038` / `FR-029` / 表 T-109 / `U-35` | ⭐ **`appHeaderItems.commands` の語を読む試験**（`screen-renderer` の受け入れ試験）。⭐ **引き当ては済んだ** —— `app-header-items.ts`・`command-palette.ts`・`open-modals.ts` が生成物 `display-words.json` を行 ID で引き、⛔ **欄が空のときだけ従来の表示へ退く**（空のラベル、パレットの群は 表 T-109 自身の群の欄）。⚠️ **印が外れるのは語が書かれたときであって、置き場ができたときではない** | 裁定済 |
| PD-10 | **`Cmd`（meta）を、`Ctrl` だけを名乗る行の `Ctrl` として読むか** —— ⚠️ `Cmd` の綴りは仕様全体で 1 度、`MK-2` の割当欄にしか現れない（自分で `docs/spec` 全体を grep して確認） | **meta を `Ctrl` として読む。** ⭐ 根拠は `MK-2` が `Ctrl（Cmd）` と併記していること —— 併記が 1 か所しかないのは、他の行が別扱いだからではなく、1 度書けば足りると考えたからと読む。⚠️ 逆に読むと Mac で保存も取り消しもできない | B | W4 | `MK-2` `MK-5` `MK-7` `MK-10` `MK-12` `SK-2`〜`SK-17` | ⭐ **meta だけを立てた `S` が `saveDocument` を答える試験**（`uf-30-31`） | 未裁定 |
| PD-11 | **`SK-16` / `SK-16a` の 1 打鍵あたりの倍率** —— 仕様はホイールの `zoomStep`（`S-53`）しか持たない | **ホイールと同じ段を使う。** ⭐ 根拠は `S-96` が「`zoomStep` は操作の速さであって結果ではない」と書き、結果の絵は変わらないと明言していること。⛔ **コードに数を書いていない** —— `InputContext.zoomStep` で受ける | B | W4 | `SK-16` `SK-16a` `S-53` `S-75` `S-76` `FR-016` | ⭐ **`Shift`＋`+` が `settings.zoomX * ctx.zoomStep` を答える試験**（`uf-30-31`） | 未裁定 |
| PD-12 | **ホイールを本ツールが読む面はどこまでか** —— `MK-1`〜`MK-5` は面の範囲を持たず、表 T-023a の判定順序は**押下だけ**を日程の描画領域に限っている | **アプリヘッダー以外の全体（`Schedule Canvas`）で読む。** ⭐ 根拠は 表 T-023a がタイムルーラーに禁じているのが「掴んで動かす操作」であり、同じ文が代替として `MK-2`〜`MK-4` と `MK-7` を挙げていること —— ホイールを禁じる文ではない | B | W4 | `MK-1`〜`MK-5` ／ 表 T-023a の面の一覧 ／ `U-32` `U-50` | ⚠️ **タイムルーラー上のホイールが `setScrollPosition` を答える試験**（`uf-30-31`） | 未裁定 |
| PD-13 | **ホイールがどちら向きに拡大するか** —— `MK-2`〜`MK-4` は「ズーム」としか書かず、`S-53` の行に向きの欄が無い | **手前から遠ざける向きで拡大する**（`factor = zoomStep ** -notches`）。⭐ 根拠は `S-96` が 1 ノッチの量を入力装置に委ねており、向きも装置の慣習に従うのが自然なこと。⚠️ **試験は向きに依存しない事実だけを押さえてある**（両軸が同じ倍率で動く・2 ノッチは 2 乗・戻せば戻る） | B | W4 | `MK-2` `MK-3` `MK-4` `S-53` `S-96` | ⚠️ **向きを引っくり返すと落ちる試験は今は無い** —— 裁定が下りたら 1 件足す | 未裁定 |
| PD-176 | **1 ノッチが 1 行より短いとき、縦スクロールが動かないこと** —— ⛔ `S-78` は表示位置を**行まるごと**で持ち、`S-96` は「1 ノッチで何倍動くかは入力装置に依存する」と定めるが、**距離を行数へ変える率をどの行も書いていない。** ⚠️ **実測: 起動時の雛形の帯は 148〜188px、1 ノッチは 100px なので、`MK-1` は 1 度も動かなかった** | **距離が 0 でないかぎり、向いた向きへ最低 1 行動かす。**⭐ 根拠は `MK-1` が「縦スクロール」と定めている以上、**0 は唯一あり得ない答え**であること。⚠️ **ノッチあたりの行数を発明していない** ——距離は装置が出したままで、1 行ぶんの床を敷いただけである。長い turn は今までどおり遠くまで届く | B | W4 | `MK-1` `S-78` `S-96` `S-77` | ⭐ **`tests/unit/uf-30-31.test.ts` の `MK-1` の件** ——雛形より背の高い行で 1 ノッチ回して行が変わることを見る | 未裁定 |
| PD-177 | **上端・下端を行き過ぎたときにどこへ着くか** —— ⛔ `MK-1` も `S-78` も端の扱いを書いていない。⚠️ **上へは 1 行も戻れなかった** —— 先頭行より上には行が無く、`scrolledAnchor` は null を「いまの値のまま」と読むので、**日程の先頭へ二度と戻れなかった** | **上は先頭行で止める**（その上に行が無いことは証明できる）。**下はいまの値のままとする** ——⛔ 行き先が残っているかは `Row Area` が既に何行見せているかで決まり、それを定める行が無い | B | W4 | `MK-1` `S-78` `OP-10` | ⭐ 上端で止まる件（先頭行より上へは動かない）| 未裁定 |
| PD-14 | **作成ドラッグのどちらの端が、新しい `Task` の載る行を決めるか** | ⛔ **裁定は要らなかった。`FR-001` の STATEMENT が既に定めている** ——「**作ったタスクは、ドラッグを始めた縦位置が指す `TaskGroup` に載せること（MUST）**」（`01-04-requirements.md:943`）。⭐ **推奨値はたまたま一致していたが、暫定ではない。印は外した。** ⚠️ **見つけたのは、仕様だけを読んで試験を書いた側である** —— 規則 06 の 1. が記録する型（未読を裁定待ちにする）の 3 例目 | B | W4 | `FR-001` `PD-4` `AR-2` `AR-3` `CM-6` | ⭐ **行 A で始め行 B で終えたドラッグが `groupId === A` を答える試験**（`uf-30-31`）| 裁定済 |
| PD-20 | **人が選んだ先へ書いた文書が、以後の上書き保存の宛先になるか** | **往復する 2 形式（`grsJson` / `mspdi`）では宛先になり、出て行くだけの 3 形式（`svg` / `png` / `singleHtml`）ではならない。** ⭐ 根拠は `FR-060` の上書き保存が「開いたファイル」を対象にしており、表 T-024 が形式ごとに取り込みの向きを持つかどうかを分けていること | C | W4 | `FR-060` `FR-096` ／ 表 T-024 の `IO-1`〜`IO-4` `IO-7` ／ `IC-2` `IC-3` | ⭐ **`chosenFile` へ保存した後、`openedFile` への保存が同じ先へ行く試験**（`uf-41-42`） | 未裁定 |
| PD-50 | **書き出しの帯（`EP-1`）とパネル（`EP-3`）と、その文字の色** —— 画面側の色は `CP-38` の様式であって設定値ではなく、`_assets/tbl-settings.md` はどちらの色も持たない | **地を `#f3f4f6`、文字を `#111111` とする。** ⭐ 根拠は、判定規則がどれも色を読まないこと —— `WY-3` は外接矩形を、`WY-2` は書き出し同士を比べる。⚠️ 地を不透明にするのは、`Row Area` が刈り込まなかったバーが行見出しの下から透けないようにするためである | C | W4 | `EP-1` `EP-3` `U-31` `U-22` `U-27` `U-23` | ⭐ **書き出した SVG の帯とパネルの `fill` を読む試験**（`uf-39-40`） | 未裁定 |
| PD-51 | **`Panel Divider` の境界線の色** —— `EP-9` は「`Group Grid Lines` と同じ線を 1 本引くこと（MUST）」と定め、新しい設定値のキーを作るなと言うが、その線の色をどの表も持たない | `#d1d5db` とし、⛔ **`SvgRenderer` が `Group Grid Lines` を描くときに、この定数と一致させること。** ⭐ `EP-9` を grep すれば 2 か所とも出る形にしてある | C | W4 | `EP-9` `U-24` `U-18` `FR-042` | ⭐ **境界線とグループ罫線の色が同じであることを読む試験** —— `SvgRenderer` が罫線を描いた後に書ける | 未裁定 |
| PD-52 | **`Document Title` が帯の中のどこに立つか** —— `EP-1` は「位置を動かしてはならない（MUST NOT）」と定めるのに、どの表も位置を持たず、`AppHeaderItems` は文字列だけを運ぶ（`RowTitle` は `box` を運ぶのに、である） | **帯の高さの 0.4 倍を字の大きさ、0.5 倍を左の余白とし、ベースラインは `S-33` を `svg-renderer.ts` と同じ当て方で使う。** ⭐ 根拠は、帯の高さだけが実測された値であること（`FR-051` が測り、`S-116` は上限にすぎない） | C | W4 | `EP-1` `U-27` `WY-3` `FR-051` `S-116` `S-33` | ⭐ **書き出した SVG の `<text>` の `x` / `y` / `font-size` を読む試験**（`uf-39-40`） | 未裁定 |
| PD-60 | **`agentApiVersion`（`AM-1`）の値** —— `AG-1` は版を持つことと、互換性を壊す変更で上げることを求めるが、最初の値をどの行も述べていない | **整数の 1。** `AGENT_API_VERSION` に置いた。⭐ 根拠は、`AG-1` が求めるのは単調性だけであり、起点は最小の自然数を採るのが読む者に最も説明が要らないこと | C | W4 | `AG-1` `AM-1` | ⭐ **`installAgentApi(...).agentApiVersion` を読む試験**（`uf-27-28-29`） | 未裁定 |
| PD-61 | **`focusTask`（`AM-16`）が、その `Task` を画面のどこへ置くか。予定の開始日が無いときどうするか** | **`Task` 自身の開始日を `S-77` に、その `Task` が描かれた行を `S-78` に書く** ——すなわち `Row Area` の左上へ置く。⚠️ 開始日が無いときは `actualStart` へ退き、両方無ければ拒む | C | W4 | `S-77` `S-78` `FR-055` `AM-16` `CM-66` | ⭐ **`focusTask(uid)` が `scrollDate` / `scrollGroupId` を書く試験**（`uf-27-28-29`） | 未裁定 |
| PD-62 | **新しく購読した `watchChanges`（`AM-17`）に、何が最初に届くか** —— `AG-6` は「まだ受け取っていないもの」としか言わず、まだ何も受け取っていない購読者にとっての意味が決まらない | **これから起きることだけ。** 購読した時点の版と発話の番号を起点に採る。⭐ 根拠は、履歴の再生を求める行がどこにも無く、⚠️ 逆に読むと購読のたびに全履歴が流れて `AG-6` の「受け取っていないもの」が無意味になること | C | W4 | `AG-6` `AG-11` `AM-17` `PI-15` | ⭐ **購読の前に発話し、最初の通知にそれが含まれないことを読む試験**（`uf-27-28-29`） | 未裁定 |
| PD-70 | **単一 `.html` の中で、文書をどの要素がどう運ぶか** —— `FR-067` と `IO-7` は「埋め込む」と定めるが、入れ物の記法をどの表も持たない | **`<script type="application/json" id="{id}">` に `GRS JSON` を入れ、最後の `</body>` の直前に置く。既に在れば差し替える。** ⭐ 根拠は、この型のスクリプト要素をブラウザが実行も解釈もせず、中身を書き換えないこと —— ⚠️ 規則 04 の 3. が記録する「鍵に制御文字が 1 つ混じるとアプリ全体が止まる」事故を避ける唯一の形である | C | W4 | `FR-067` `IO-7` `BT-1` `CN-8` `CN-5` `IF-8` | ⭐ **書き出した `.html` がその入れ物をちょうど 1 つ持つことを読む試験**（`uf-37-38`） | 未裁定 |
| PD-71 | **シェルが `IF-8` 越しに渡してよい要素 id の形と、外れたものの扱い** —— どの行も形を定めていない | **`/^[A-Za-z][A-Za-z0-9_-]*$/` だけを受け、外れたら拒む。** ⭐ 今の HTML が許す範囲より狭いのは、⚠️ 引用符や空白を含む id が属性を割って別の要素を生み、`FR-067` の「ちょうど 1 つ」を壊しうるためである | C | W4 | `FR-067` `IF-8` `CN-8` | ⭐ **形から外れた id を渡すと拒む試験**（`uf-37-38`） | 未裁定 |
| PD-80 | **MSPDI から起こした行（`TaskGroup.id`）が受け取る UUID** —— `AT-51` は形（UUID）を、`IV-1` は一意性を定めるが、どう作るかはどの行も述べていない | **その `Task` 自身の UID から決定的に導く**（版 4・変種 1 の並びに嵌める）。⭐ 根拠は、⚠️ 乱数で作ると同じファイルを 2 度取り込んだときに別の id が出て、`FR-021` の往復が比較できなくなること | B | W4 | `AT-51` `AT-52` `IV-1` `FR-058` `PI-20` | ⭐ **同じファイルを 2 度取り込むと同じ id が出る試験**（`uf-36`） | 未裁定 |
| PD-81 | **GRS が自分で作った文書の `Project/SaveVersion`** —— `DV-2` が出どころ（書いた側の版）を、`EX-1` が要素の必須を定めるので**形は決まっており、数だけが無い** | **この製品自身のメジャー版（`0`）を書く。** ⛔ `12` は書かない —— 公式 XSD がその値を「Project 2007」と文書化しており、名乗ると**交換相手の道具から出たファイルだと主張することになる** | C | W4 | `DV-2` `EX-1` `FR-021` | ⭐ **書き出した MSPDI が `SaveVersion` を持ち、それが 12 でないことを読む試験**（`uf-36`） | 未裁定 |
| PD-82 | **GRS が自分で作った文書の `Project/CurrencyCode`** —— `DV-3` はこの値を `carry` から採るが、取り込んだことのない文書は持たない。それでも `EX-1` は要素を要求する | `XXX` **とする** —— ISO 4217 の「通貨が関わらない」符号であり、スキーマ自身の文書がその符号集合を名指している。⛔ 実在の通貨は書かない —— **金額を持つ列が 表 T-058 に 1 つも無いので、文書が持たない事実を述べることになる** | C | W4 | `DV-3` `EX-1` `AT-125` | ⭐ **書き出した MSPDI の `CurrencyCode` を読む試験**（`uf-36`） | 未裁定 |
| PD-90 | **二度押しが「同じ押下の 2 度目」と数えられる時間と距離**（`MK-13`）—— どの表も持たない | **500 ms・4 px、押下から押下まで、押しボタンごとに測る。**⭐ 根拠は `S-124` の説明表示の待ち時間より短くないと、説明が出てから二度押しが成立してしまうこと | C | W5 | `MK-13` `GR-10` `GR-12` `IN-1` `S-124` `S-112` | ⭐ **400 ms 離れた同じ点の 2 押しが `clickCount 2` を答える試験**（`uf-50`） | 未裁定 |
| PD-91 | **ホイールが行や画素で報告されたときの 1 ノッチの値** —— `S-96` は「1 ノッチで何倍動くかは入力装置に依存する」と書くだけで、装置の単位を揃える値をどの表も持たない | **1 行 40 px・1 ノッチ 100 px・1 ノッチ 3 行。** 頁単位は宿主自身の `innerWidth` / `innerHeight` で測る | C | W5 | `MK-1` `MK-2` `MK-3` `MK-4` `MK-5` `S-53` `S-96` | ⭐ **`deltaMode=1` で `deltaY=3` が 1 ノッチを答える試験**（`uf-50`） | 未裁定 |
| PD-92 | **押下が無いときの `move` が報告する `button` と `clickCount` | `left` と `0`。⭐ `0` は「押下がまだ 1 度も無い」を表せる唯一の値である | C | W5 | `IN-1` `IN-2` `MK-8` `MK-13` | ⭐ **押下の無い `pointermove` が `clickCount 0` を答える試験**（`uf-50`） | 未裁定 |
| PD-93 | **`+` `-` `0` を、配列が出した文字で見るか物理キーで見るか | **物理キーで見る**（`Equal`／`Minus`／`Digit0` と各テンキー）。⭐ 根拠は、配列によっては `+` に修飾キーが要り、文字で見ると同じ操作が配列ごとに変わること | C | W5 | `SK-16` `SK-16a` `SK-17` `MK-10` | ⭐ **`key` が `_` でも `code` が `Minus` なら縮小になる試験**（`uf-50`） | 未裁定 |
| PD-94 | **本ツールが割り当てていない押下も、ポインタ捕捉で保持するか**（保持すれば `IN-1a` の中断が効く） | **保持しない** —— 呼び手が「割り当てた」と言った押下だけを捕捉する。⭐ 根拠は `MK-10` が「割り当てていない組合せを止めてはならない（MUST NOT）」と定めており、捕捉も止める手の 1 つであること | C | W5 | `IN-1` `IN-1a` `MK-10` `MK-13` `AG-9` | ⭐ **割当ありの左押下だけが捕捉を掛ける試験**（`uf-50`） | 未裁定 |
| PD-95 | **表 T-036 が割り当てていない打鍵を報告するか、どの綴りで報告するか** | **すべて報告する**（正規化はするが名前は変えない）。⭐ 根拠は `MK-10` / `MK-12` が「割り当てていないものを止めるな」と言う以上、⚠️ **止めないという判断を下せるのは割当を知る側**（`UF-30`）であり、ここで捨てるとその判断が届かないこと | C | W5 | `SK-1` `SK-20` `MK-10` `MK-12` `IN-5` | ⭐ **割当の無い打鍵が宿主の名前のまま届く試験**（`uf-50`） | 未裁定 |
| PD-104 | **ファイル選択の絞り込み** —— `OP-1` は `GRS JSON` と MSPDI XML を認めるが、拡張子も MIME 型もどの表も持たない | **絞り込まない。** ⭐ 根拠は `FR-023` の検証がファイルを拒む唯一の場所であること —— ⚠️ 選択側で絞ると、拒む場所が 2 か所になり、片方が緩いと `FR-023` の意味が消える | C | W5 | `OP-1` `OP-2` `IO-1` `IO-2` `CN-5` | ⭐ **選択に渡す options が空であることを読む試験**（`uf-51`） | 未裁定 |
| PD-105 | **`queryPermission` を持たない宿主に、書き込み可否を何と答えるか | **許可されていると答え、書き込みが実際に確かめる。** ⚠️ 拒まれれば `permissionLost` として値で返る。⭐ 根拠は、逆に読むと `FR-060` の上書き保存がその宿主で永久に使えなくなること | C | W5 | `FR-060` `NT-4` `LM-14` `IF-3` | ⭐ **`queryPermission` の無いハンドルが `writable` を答える試験**（`uf-51`） | 未裁定 |
| PD-110 | **`localStorage` の鍵に付ける、この道具固有の接頭辞の綴り** —— `FR-026` の RATIONALE が付けることを求めるが、綴りをどの表も持たない | `grsched.` **とし、定数として公開する** —— ⭐ 表 T-206 の 4 行が同じ置き場を共有するので、打ち直させずに読ませる | C | W5 | `FR-026` `S-99` `S-99a` `S-99b` `S-99c` `LM-6` `LM-14` | ⭐ **store が渡す全部の鍵が接頭辞で始まる試験**（`uf-52`） | 未裁定 |
| PD-111 | ⭐ **CR-280 が主題ごと消した（2026-08-29、利用者の裁定）** —— 自動保存が無くなったので問い自体が立たない。**自動保存の置き場が 1 枠か、文書ごとに 1 枠か** —— `AutosaveGateway` が分類 D として報告した宿題 | **1 枠**（鍵と本文を一緒に持ち、書くたびに置き換える）。⭐ 根拠は `BT-3` が起動時に差し出すのを 1 つとしており、⚠️ 複数枠にすると「どれを差し出すか」という規則が新たに要ること | C | W5 | `FR-061` `FR-062` `BT-3` `FR-026` `IO-5` | ⭐ **文書 A の後に文書 B を書くと A が消える試験**（`uf-52`） | 裁定済 |
| PD-112 | ⭐ **CR-280 が主題ごと消した（2026-08-29、利用者の裁定）** —— 自動保存が無くなったので問い自体が立たない。**`FR-062` の「脇へ置く」を、`IF-4` にメンバが無いままどう行うか | **`IF-4` ではなく `CP-29` が返す具象の側に置く。** ⛔ 継ぎ目は広げない —— ⚠️ 広げると内側の層が「脇へ置く」を知ることになり、`LR-5` の向きに反する | C | W5 | `FR-062` `BT-3` `FR-026` `NT-3a` `NT-6` | ⭐ **脇へ置いた後の読み出しが「無い」を答える試験**（`uf-52`） | 裁定済 |
| PD-121 | **ブラウザの拒否のどれを `notPermitted` と読むか** —— 継ぎ目は 3 値とその意味を固定するが、宿主のどの信号が当たるかをどの行も持たない | **投げられた値の名前が `NotAllowedError` のものだけを `notPermitted` とし、他は `writeFailed`。**⭐ 根拠は `NT-3a` が「次にできること」を求めており、権限の話とそれ以外では次の一手が違うこと | C | W5 | `FR-028` `NT-3a` `IF-5` | ⭐ **`NotAllowedError` だけが `notPermitted` になる試験**（`uf-53`） | 未裁定 |
| PD-130 | **ラスタライズの失敗 3 値に、宿主のどの断り方が当たるか** | `unsupported` は 2D 文脈が得られない場合と汚染された canvas、`tooLarge` は要求した大きさが得られない場合、残りは `rasterFailed`。⭐ 根拠は `NT-3a` の「次にできること」が 3 つで違うこと | C | W5 | `IF-6` `NT-3a` `AG-8` `IO-3` `IO-4` | ⭐ **宿主の失敗ごとに `reason` を読む試験**（`uf-54`） | 未裁定 |
| PD-131 | **外部を取りに行く参照を含む絵をラスタライズするか** —— `href` や `url()` が `#` でも `data:` でもない場合 | **拒む**（参照を名指して `rasterFailed`）。⭐ 根拠は `WY-2` が「同じ状態の 2 回の書き出しは一致する」ことを求めており、⚠️ **網の向こうの資源はそれを保証できない**こと | C | W5 | `WY-2` `WY-3` `FR-080` `IO-4` | ⭐ **外部 href を含む SVG を拒む試験**（`uf-54`） | 未裁定 |
| PD-132 | **根の `<svg>` が持つ幅と高さをどう扱うか** —— `IO-3` と `IO-4` は別々の大きさを定めるのに、絵は 1 本の文字列で来る | **`viewBox` があれば、根の幅と高さを求められた画素数へ書き換える**（古い対は外す）。⭐ 根拠は、書き換えないと復号器ごとに違う大きさに解かれ `WY-2` が壊れること | C | W5 | `IO-3` `IO-4` `S-82` `WY-2` | ⭐ **偽の `<img>` に渡された `src` の寸法を読む試験**（`uf-54`） | 未裁定 |
| PD-133 | **画素数が整数でない、あるいは 1 未満のとき** | **拒む**（受け取った大きさを名指して `rasterFailed`）。⛔ 丸めない —— ⚠️ 丸めると `WY-3` の「丸めの規則を当てたうえで一致する」判定が、どちらの丸めを指すか分からなくなる | C | W5 | `FR-025` `S-81` `S-82` `WY-3` | ⭐ **`1600.5` を渡すと拒む試験**（`uf-54`） | 未裁定 |
| PD-134 | **PNG が透明を保つか、不透明な地を敷くか** | **絵の下には何も敷かない。** ⭐ 根拠は `FR-080` と 表 T-076 が「何を描くか」を定めており、⚠️ **描けと言われていない地を敷くのは、書き出しにだけ在るものを足すことになる** | C | W5 | `FR-080` `IO-4` | ⭐ **`drawImage` が最初で唯一の描画である試験**（`uf-54`） | 未裁定 |
| PD-140 | **開いた面ごとに何を運ぶか** —— `FR-036` `FR-074` `FR-099` `FR-088` `FR-068` の 5 つが面を開くのに、中身の形をどの表も持たない | **面で判別する共用体にし、要求ごとに要る中身だけを持たせる。** ⛔ 名前は表 T-103 が確定させた 3 つだけを使い、⚠️ **名の無い 2 面は要求の UID で運ぶ**（`IconId` が `IC-7` で、`Notice.manner` が `NT-1` で運ぶのと同じ手）—— **新しい確定名を作らない** | C | W4 | `FR-036` `FR-074` `FR-099` `FR-088` `FR-068` `S-99g` `U-30` `U-49` | ⭐ **各面が自分の中身を運ぶことを読む試験**（`uf-66`） | 未裁定 |
| PD-141 | **ポインタがどのアイコンの上にあるか** —— `EZ-2` は時間と**場所**の 2 条件を課すのに、場所を答えられる値がこの層のどこにも無い | **シェルが答える**（`ScreenSession.iconUnderPointer`）。⭐ 根拠は、⛔ **入口を実際に描いたのは `DomScreenSurface` だけ**であり、`ScreenView` のどの部品も入口の矩形を持たないこと。⚠️ `CommandItem` に矩形を持たせる案は、測れない側に測らせることになるので却下した | C | W4 | `EZ-2` `S-124` `FR-029` `PI-35` | ⭐ **休止が閾を越えても、下にあるアイコンだけが説明を得る試験**（`uf-69`） | 未裁定 |
| PD-142 | **行見出しパネルで選ばれている行の集合** —— `FR-085` と `FR-042` が同じ集合を読むのに、⚠️ `SL-1` が行を描画領域の選択から外しているので `Selection` が持てない | `ScreenSession.selectedGroupIds` **として運ぶ。** ⭐ `FR-042` の「行の色と高さをプロパティパネルに出す」は**どの行が選ばれたか**だけが欠けていた —— 色も高さも `schedule` から既に届く | C | W4 | `FR-085` `FR-042` `SL-1` `AT-51` `AT-58` `AT-59` | ⭐ **選ばれた行の色と高さがプロパティパネルに出る試験**（`uf-64`） | 未裁定 |
| PD-143 | **担当者名簿で選ばれている担当者の集合** —— `FR-099` の「すべて選ぶ／すべて解く」と 2 つ目の削除経路が読むのに、`SL-1` は担当者を認めない | `ScreenSession.selectedResourceUids` **として `uid` で運ぶ。** ⭐ 根拠は 表 T-225 の `AS-6` 「人に見せるのは担当者名、文書に書くのは `uid` とすること（MUST）」 | C | W4 | `FR-099` `AS-6` `AT-85` `SL-1` | ⭐ **選択が `uid` で解決され、同名でも取り違えない試験**（`uf-66`） | 未裁定 |
| PD-144 | **選択が消えたときプロパティパネルが何を保つか** —— `FR-072` は「保って、その旨を見出しに言う（MUST）」と定めるが、直前の何を保つのかを述べていない | **描かれた項目ではなく「対象」を保つ**（`ScreenSession.propertiesSubject`）。⭐ 決め手は `FR-072` 自身の「**もう一度同じ入口を押したら直前の選択物へ戻すこと**」—— ⚠️ 項目を保つと、編集で既に古くなった値を見せることになる | C | W4 | `FR-072` `SL-1` `FR-085` | ⭐ **選択を消した後も見出しと項目が保たれる試験**（`uf-64`） | 未裁定 |
| PD-150 | **対話欄の入力を確定させる鍵** —— `SK-19` は `Enter` を「その場の編集を確定する」に割り当てるが、⚠️ **その一覧に `Dialogue Field` が無い** | `Enter` **を採る** —— ⭐ 仕様が持つ確定の割当のうち最も近いものであり、⛔ 新しい割当を発明していない | C | W5 | `SK-19` `AG-11` `FR-066` | ⭐ **`Enter` で `isSettled` が立つ試験**（`uf-71`） | 未裁定 |
| PD-151 | **各部品をどこにどう置き、どう塗るか** —— `ScreenView` は `Properties Panel` や `Dialogue Field` の矩形を持たない | **長さはすべて相対値（`em`・百分率・ヘアライン）にする。** ⭐ 根拠は `FR-051` がヘッダーの高さを「どこにも値として持ってはならない」と定めていること —— ⚠️ **相対値なら環境の答えがそのまま `BO-1` で測り返される** | C | W5 | `FR-051` `BO-1` `S-116` `SC-4` | ⭐ **描かれた部品が `data-role` で見つかり、寸法が環境から来る試験**（`uf-71`） | 未裁定 |
| PD-152 | ⭐ **CR-287 で閉じた（2026-08-30）** —— `RowTitle` が `indentPx`（`depth` × `S-37`）を運ぶようになり、画面・書き出し・`FR-085` の打ち切りの 3 つが同じ数を使う。**行を 1 段ごとにどれだけ字下げするか** —— ⛔ 正しい値は `S-37`（`rowTitleIndent`）だが、`RowTitle` は切った名前と `depth` しか運ばず、`DocumentSettings` はこの層に届かない | **1 段 1`em` で描く。** ⛔ **これは暫定であり、正しい直し方は `RowTitle` が字下げ幅を運ぶことである** —— ⚠️ そうしないと `FR-085` が名前を切った幅と、実際に描かれる字下げが別々に決まる | C | W5 | `S-37` `FR-085` `S-79` `EP-4` | ⭐ **深さごとの字下げを読む試験**（`uf-71`） | 裁定済 |
| PD-153 | **説明を消す操作の見た目** —— `IN-3` は「消せること」を求めるが、その入口の図形を 表 T-109 が持たない | ⛔ **2026-08-25 の裁定で推奨は覚された** —— 利用者の逐語は「x を押して消すのは間違い」である。⭐ **`x` を取り、消す手立ては 表 T-028 の `IN-4` の階層の最後（出ている説明）へ移した**（CR-252）。⚠️ **その `Esc` の経路はまだ実装されていない** —— 描く側に STOP が立っている | C | W5 | `IN-3` `IN-4` `FR-029` `IC-52` | ⭐ **説明が `×` を持たない試験**（`uf-71`） | 裁定済 |
| PD-154 | **入口の図形をどう描くか** —— `FR-029` は 図 F-019 を図形の正とし（MUST）第三者の図形集を使うことを禁じる（MUST NOT）が、⛔ **その図が `src/` へ生成されていない**（`icon-roster.json` は名簿だけを運ぶ） | **行 ID を `data-icon` に置き、図形は描かない。** ⭐ こうすると ⛔ **第三者の図形を使わずに済み**、⚠️ 図が生成されるようになった回に、描く側だけを差し替えられる。⭐ **2026-08-24 に両半分とも閉じた** —— 図は `icon-glyphs.json` として `src/` へ生成済みであり（本行の前提はその時点で偽になっていた）、残っていた「箱の大きさを誰も決めていない」は `CR-241` が 表 T-206 に `S-138` を立てて閉じた。印は消した | C | W5 | `FR-029` `F-019` `T-109` | ⭐ **各入口が自分の行 ID を持つ試験**（`uf-71`）。⭐ **足された** —— 同じ箱で描かれることを 4 つの面で当てる 10 件と、原稿の `S-138` を 12 から 21 へ変えると 8 件落ちることの実測 | 裁定済 |
| PD-155 | **行見出しパネルとプロパティパネルの幅をどこから取るか** —— `ScreenView` は両者の矩形を持たない | **`Panel Divider` の線を各パネルの端として使う。** ⭐ 根拠は `FR-052` が「その帯をドラッグすると**そのパネルの幅が変わる**」と定めていること —— ⚠️ 分割線が無い側は窓の端に置く | C | W5 | `FR-052` `FR-051` `S-79` `S-80` | ⭐ **分割線の位置がパネルの端と一致する試験**（`uf-71`） | 未裁定 |
| PD-156 | **`isSettled` を立てる側** —— `AG-11` は「確定していないものを発話として読んではならない（MUST NOT）」と定めるが、確定を誰が判断するかを述べていない | **この側（シェル）が立てる。** ⭐ 根拠は、⛔ **半端な入力そのものがこの側から出ない**のが `AG-11` の求める姿であり、`dialogueMessageFromInput` はその旗を信じて拒むだけでよいこと | C | W5 | `AG-11` `IF-9` `FR-066` | ⭐ **確定していない入力が発話にならない試験**（`uf-71`） | 未裁定 |
| PD-157 | **通知が持つ件数（`NT-3`）をどう人に見せるか** —— ⛔ 属性にだけ置くと `NT-1` の「色や枠だけで伝えてはならない（MUST NOT）」に反するが、添える語を `FR-038` の訳語表が持たない | **数そのものを字として描く。** ⭐ 語を足さないのは、⚠️ **訳語の置き場が無い**（[[PD-3]] / [[PD-4]] と同じ穴）ため —— ⛔ **読める形にすることと、語を発明しないことの両方を満たす最小の形である** | C | W5 | `NT-1` `NT-3` `NT-3a` `FR-038` | ⭐ **件数が字として描かれる試験**（`uf-71`） | 未裁定 |
| PD-160 | **表示に出す語そのもの** —— CR-194 が原稿・生成器・成果物を作った。⭐ **2026-08-24 実測: 枠は 242、記入も 242。**⛔ **「176 語の枠が空のまま在る」「語は 1 つも書かれていない」と書いてあったが、どちらも偽になっていた** —— 枠は増え、利用者が全部書いた。⚠️ **同じ嘘が原稿の $comment と生成器の刷り文にも残っており、同日に 3 か所とも直した** | **空のまま置き、印字する側は従来どおりに退く**（空のラベル・行 ID・表 T-109 自身の群の語）。⭐ 根拠は、`FR-038` の MUST NOT が**言語の選択すら文書から外す**ので、覆す実費が原稿のセルだけであること。⛔ **agent が語を書くと、表 T-109 が明文で拒んでいる「確定名の新設」をそのまま行うことになる**（利用者の裁定 2026-08-21、案 (a)）| C | W4 | `FR-038` ／ 表 T-109 ／ 表 T-103 ／ 表 T-037 ／ `EZ-2` | ⭐ **原稿の 1 語を埋めると画面に届く試験**（`gen:check` が 176 語 0 記入を数えており、⭐ **画面までの道は通した** —— 表 T-075 の `UF-60` の 1 セルを根拠に `ScreenView` が `language` を持ち、`UF-62` `UF-64` `UF-65` `UF-66` `UF-69` の 5 ユニットが自分の担当節だけを引く）| 裁定済 |
| PD-3 | **表 T-023 の割り当てを画面に出すときの語** —— ⭐ **置き場は `CR-194` が作った**（`docs/spec/_source/display-words.json`）。⛔ **未決なのは語そのものに変わった** | **空のまま置く。**⭐ 根拠と扱いは [[PD-160]] と同じ —— ⛔ agent が語を書くと確定名の新設になる | C | W4 | `FR-037` / `FR-038` / 表 T-023 の `MK-1` `MK-5` | ⭐ **スクロールバーのヒント本文を読む試験**（`tests/unit/uf-69.test.ts`）。⭐ **引き当ては済んだ** —— `tooltips.ts` が生成物 `display-words.json` の `assignments` を行 ID で引き、⛔ **欄が空のときだけ従来どおり行 ID そのものへ退く**。⚠️ **印が外れるのは語が書かれたときである** | 裁定済 |

| PD-161 | **座標の丸めの許容差と、SVG の正規化の規則** —— `FR-080`（`:3083`）が「正規化と丸めの規則そのものは Chapter 6.1 が持つ」と委任し、`WY-3` の合否がそこに乗っているのに、⛔ **Chapter 6.1 にその規則が無い**。⭐ **`CR-202` が PNG の比べ方と基準環境を閉じ、書き出す桁を [[PD-162]] へ割り出した。残るのはこの 2 つである** | **推奨を添えて問う。推奨で走らない。** ⛔ **許容差は「判定に含めない差」の境を引く値であり、`WY-3` の合否がその値に直接乗る** —— 値を後から動かすと、それまで「合格」としていた判定が全部組み直しになる。⚠️ **裁定 `D10` が裁いたのは XML の側だけである** | G | W4 | `FR-080` / `WY-3` / 表 T-041 / Chapter 6.1 | ⭐ **`WY-3` を駆動する試験**（まだ無い。表 T-218 の `TS-3`）| 裁定済 |
| PD-162 | **書き出す座標の桁** —— `WY-2` と `WY-3` が比べる SVG / PNG の座標を、何桁で書くか。⭐ **[[PD-161]] から割り出した半分である**（`CR-202`）| **0.01 px の格子に丸める**（`Math.round(value * 100) / 100`）。⭐ **いまの実装がそうしている値である。** ⛔ **整数 px にすると、`S-81` の 1600 px 幅で細い線と矢羽根の角度が崩れ、`FR-094` の寸法が壊れる。** ⭐ **丸めずに書くと綴りが膨らむが、判定する側だけを直せばそちらへ移れる** —— 移りやすいほうを既定に置いた。⚠️ **分類が `A` なのは、SVG / PNG が書出のみ（表 T-024 の `IO-3` / `IO-4`）で、文書にも MSPDI にも残らないからである** | A | W4 | `FR-080` / `WY-2` / `WY-3` / `S-81` | ⭐ **書き出した SVG の座標が 0.01 px の格子に乗る試験**（まだ無い。表 T-218 の `TS-3`）| 裁定済 |
| PD-163 | **`xsd:duration` の綴り** —— `NR-4`（表 T-228）が型付きの値を標準字句表現へ揃えると定めたが、⛔ **`XML Schema Part 2` はこの型に標準字句表現を定めていない**（3.2.6 は字句表現・順序関係・ファセット比較の 3 節だけである）。⚠️ **交換相手はこの型を 13 の要素名・35 か所で使うが、`GRS` が解釈するのは `AT-35`（`Task.actualDuration`）ただ 1 列である** —— 残りは `carryElements` へ文字列のまま入るので綴りが変わらない | ⭐ **裁定済（2026-08-22。案 `A` ＋ 案 `D`。`CR-203`）** —— **書き出す綴りを `PTnHnMnS` に固定し**（表 T-033 の `EX-9`。`EX-7` が日付で同じ穴を埋めた先例に倣う）、**比べる側は字句ではなく時間の量として比べる**（表 T-228 の `NR-5`）。⛔ **元の綴りを列として覚える案は採らなかった** —— 版 0.44 が同型の案を「解釈した値と元の文字列を両方持つ」として退けている | D | W4 | `EX-9` / 表 T-033 / `NR-5` / 表 T-228 / `AT-35` | ⭐ **`durationOfMinutes` の綴りを崩すと落ちる試験**（まだ無い。表 T-218 の `TS-3`）| 裁定済 |
| PD-164 | **稼働日の整数にならない実績期間の丸め** —— `AT-35`（`Task.actualDuration`）は稼働日の整数を持ち、交換相手は時間の量を書く。⛔ **`Project.minutesPerDay` で割り切れない長さ**（1 日 480 分に対する `PT41H` など）**の扱いを、どの行も述べていない**。⭐ **いまの実装は読まずに `null` とし、人に知らせている**（`mspdi-codec.ts` の `workingDaysOfActualDuration`。注記自身が「the rounding is undecided」と書く）—— ⚠️ **値を発明していない点は規則どおりだが、本一覧に 1 行も無いまま既定になっていた** | ⭐ **裁定済（2026-08-22。案 ④ 四捨五入。`CR-204`）** —— **1 日単位に四捨五入し、ちょうど半日は絶対値の大きいほうへ寄せる。割り切れないことを理由に値を捨てない（MUST NOT）。** ⛔ **「読まない」を退けたのは、`ActualDuration` が `TASK_CONSUMED` にあり `carryElements` に残らないため** —— **`null` は書き出しで 1 要素も書かれず、交換相手が書いた長さが往復で消える。** ⚠️ **四捨五入も無損失ではない**ので、丸めた `Task` の件数を知らせる（`FR-076` の `NT-1`）| D | W4 | `AT-35` / `FR-054` / `EX-9` / `S-128` | ⭐ **`PT41H` が 5 日になり、知らせが 1 件出る試験**（まだ無い。`uf-36` 系）| 裁定済 |

⭐ **W4 の裁定 J（3 件）はここに載せない** —— **どれも `E`（継ぎ目と公開署名）であり、
実装前に問うものだからである。** 規則 06 の 2. の表を見よ。

---

## ⛔ 2026-08-22 に足した 1 行（`AT-128`）

⚠️ **一覧の表が長いので末尾に置く。⭐ 分類と波は行が自分で名乗る（検査 25 が見る）。**

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-165 | **`lastEditedBy`（`AT-128`）の値域と、書き手を見分けられるかどうか** —— 意味欄は「最後に書いた者。人か、AI か」と書くだけで、⛔ **何を書いてよいかも、書き手どうしを見分けられることも定めていない。** ⚠️ **出荷しているテンプレート自身が `"template"` を書いており**（`startup-template.json`）、人でも AI でもない。⛔ **`AG-6` の MUST NOT（自分の書き込みは通知しない）は `lastEditedBy` の等値だけでそれを判定するので、AI が 2 体在って同じ文字列を書くと、互いの書き込みを自分のものと読んで通知が落ちる** | **推奨を添えて問う。推奨で走らない。** ⛔ **文書に保存され、`MSPDI` の往復にも乗る値であり、緩く受理した文書は後から締め直せない**（規則 06 の `D` と `F` の両方に当たる）。⭐ **問うべき形は「一意であることを求めるか、それとも `AG-6` の判定に書き手の識別を別で持たせるか」である** —— 前者は値域の裁定、後者は継ぎ目の裁定になる | D | W4 | `AT-128`（`documentStamp.lastEditedBy`。CR-205 で器が改名された）/ `AG-6` / `AG-2` / `FR-063` / `DR-4` | ⭐ **同じ `lastEditedBy` を名乗る 2 体のうち一方の書き込みが、他方へ通知される試験**（まだ無い。`uf-27-28-29` の `watchChanges` の並び） | 裁定済 |

⭐ **裁定は 2026-08-22。当てたのは `CR-207` である** —— **書く語の全数を 表 T-229 に置き、画面は `user`、起動テンプレートは `template`、`Agent API` の呼び手は申告した名とした。**
⛔ **後者の重複だけは閉じていない。** ⚠️ **`LM-16` として制限事項に書いた** —— 閉じるには監視の継ぎ目に識別を足すことになり、分類 `E` の裁定である。

⛔ **`@provisional` の印は付けない** —— 分類 `D` は実装しないものであり、印を付けると検査 25 が落とす（規則 06 の 6.）。
⚠️ **刻印の組み直し（`docs/review/stamp-rulings-kiss-2026-08-22.md`）が `revisionStamp` を `documentStamp` へ改めるので、その変更要求は本行の行 ID を差し替えること。**

---

## CR-206 が足した 1 行（`FR-046`）

⚠️ **`B-1` は `docs/review/stamp-and-utc-2026-08-22.md` の §5.2 が挙げた問いであり、本一覧に行が無かった。** ⭐ **裁定と同じ回に起こして `裁定済` にする**（規則 06 の 3. は行を消さないことを求めており、載せずに閉じると記録が残らない）。

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-166 | **`FR-046` の「その時点の本日」が誰の暦の日か**（`docs/review/stamp-and-utc-2026-08-22.md` の `B-1`）—— ⛔ **`statusDate` に何を書くかを定めた MUST は在るのに、どの暦の日かを述べる行がどこにも無かった。** ⚠️ **`InputContext.today` を繋ぐ側が黙って選べる状態であった** | **読む人の機のローカルの暦の日とする。** ⭐ **`statusDate` はゾーンを持たない暦の日を入れる列であり**（`FR-020` の RATIONALE）、**UTC を採ると東京の午前 9 時より前に押した線が前日に立つ。** ⚠️ **`R3.6` の「ローカルは表示にのみ」は瞬間を保存する値の条項であり、暦の日には掛からない** | D | W5 | `FR-046` / `AT-13` / `CU-1` | ⭐ **シェルが UTC の暦の日を渡すように壊したら落ちる試験**（まだ無い。`FT-1` の結線が先）| 裁定済 |

⭐ **裁定は 2026-08-22。当てたのは `CR-206` である。** ⛔ **`@provisional` の印は付けない** —— 分類 `D` であり、かつ既に裁定済である。

---

## CR-208 が足した 2 行（表 T-206）

⭐ **どちらも分類 `A`（原稿にあり、生成でコードへ届く値）なので、推奨値で走り、印を付ける。**
⛔ **印は生成ブロックには付けられない**（次の `npm run gen` で消える）ので、**`item-hit-area.ts` の `PointerSlop` の宣言に付けた** —— 値の意味を述べているのはそちらである。

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-167 | **実績の端点を、左右どれだけ離れても掴めるか**（`S-91`）—— ⛔ **表 T-206 の既定は「実績バーの帯」という散文だけで、**帯は縦の広がりである。** ⚠️ **端点は点なので、横の許容が無いと `GR-5` / `GR-6` は決して当たらず、実績を動かす経路が丸ごと塞がる**（表 T-023d の注は「実績を動かす経路は端点だけである」と定める）| **`S-90` と同じ 6px とする。** ⭐ **根拠は 表 T-206 自身の理由欄** ——「掴み領域は読む人のアクセシビリティに属する。手が震える人には大きな掴み代が要る」。**震える手は、掴む相手が予定か実績かで震え方を変えない。** ⚠️ **重なったときに何が勝つかは 表 T-023d の優先順が既に決めている**ので、そろえても取り合いにならない。⛔ **`S-90` を指さずに自分の数を持たせた** —— 予定と実績で別に選ぶ余地を残すためである | A | W4 | `S-91` / `GR-5` / `GR-6` / `S-90` | ⭐ **実績バーの端点から左右へ `S-91` ちょうど離れた点が当たり、1px 外れた点が当たらない試験**（まだ無い。`item-hit-area` の受け入れ試験）| 未裁定 |
| PD-168 | **線に「乗った」と数える距離**（`S-137`）—— ⛔ **表 T-023d の前文は掴み代の値を 表 T-206 へ全部委ねているのに、`GR-13`（依存線）と `GR-16`（基準日線）のための行が 1 つも無かった。** ⚠️ **「線の上」は幅を持たない集合であり、許容を与えないと決して当たらない** | **線の左右に 6px とする。** ⭐ **根拠は `PD-167` と同じ理由欄と、`GR-13` / `GR-16` が 表 T-023d の下のほうに在ること** —— **広く取っても上の行から掴みを奪わない。** ⚠️ **由来を持つ数ではないので 🔎 を付けた** | A | W4 | `S-137` / `GR-13` / `GR-16` | ⭐ **依存線から `S-137` ちょうど離れた点が当たり、1px 外れた点が当たらない試験**（まだ無い。`item-hit-area` の受け入れ試験）| 未裁定 |
| PD-169 | **押している身振りが 表 T-023a のどの行で始まったかを、書き込みの側から問う道** —— ⛔ **`AG-9` は「パンと範囲選択は文書を変えないので拒否しない」と明文で除外するのに、`WS-2` に答えるシェルは押下中のすべてを拒否している。** ⚠️ **どの行で始まったかを決めているのは `PI-18` の私有の判定であり、表 T-023a の判定順序をそこに置いたのは同表の注そのものである。⛔ 写せば `R2.7` の重複になる** | **`PI-18` が「その押下がどの行で始まったか」を答えるか、`PointerPress` がそれを運ぶ。⭐ どちらも 表 T-064 か 表 T-065 を動かすので裁定が要る。** ⚠️ **いまは広いほうの誤りを採っている** —— `AG-9` の MUST を守るほうを残し、除外の文だけが破れている。**逆にすると MUST が破れる** | E | W5 | `AG-9` / `WS-2` / `PD-1` / `PD-5` / `UN-8` / `UN-9` / 表 T-064 の `PI-18` | ⛔ **`tests/unit/uf-48-write-moment.test.ts` の 2 件が既に赤で立っている**（パンの押下中・範囲選択の押下中に書き込みが着地すること）| 未裁定 |
| PD-170 | **全体表示を取り消したとき、倍率と表示位置まで戻ってしまうこと** —— ⛔ **`UN-17` は「戻るのは畳みだけであり、倍率と表示位置は `UN-8` のまま対象外である」と定めるのに、`WS-4` が積む段は文書まるごとを持ち、取り消しはそれを逐語で戻す** | **段が「文書まるごと」であるかぎり部分的には戻せない。⭐ 選べるのは 2 つ —— ①全体表示を履歴の対象から外す（`UN-17` が対象と明記しているので仕様に反する）②段が戻す範囲を列で絞れるようにする（`FR-031` の段の定義が動く）。⛔ どちらも仕様の側の裁定である** ⭐ **2026-08-24 に調べ直した結果、CR-217（版 0.94）が既に当て終えていた** —— ⛔ **一覧だけが腐っていた。**⚠️ **引継書が足そうとした 1 文「表 T-027 が対象外と定める値を戻してはならない」は、裁定で明示的に退けられた案である。書けば退けたはずのものを入れることになる。** | G | W5 | `UN-17` / `UN-8` / `WS-4` / `FR-031` / `FR-055` | ⛔ **まだ無い。** ⭐ 全体表示 → 倍率を変える → 取り消し、で倍率が戻らないことを見る試験 | 裁定済 |
| PD-171 | **`OP-10` の表示位置を、全体表示が選ぶものにすること** —— ⛔ **`OP-10` は「`FR-055` の全体表示が選ぶ倍率と表示位置にすること（MUST）」と定めるのに、`fitZoom`（`PI-5`）は倍率しか答えず、シェルが位置を自分で決めている**（最も早い予定の開始日）。⚠️ **`FR-055` の RATIONALE は「左の張り出しが行見出しパネルに隠れる」ことをその害として名指している** | **`fitZoom` が位置も答える。⛔ `PI-5` の公開メンバの署名が動くので裁定が要る** | E | W5 | `OP-10` / `FR-055` / 表 T-064 の `PI-5` | ⛔ **まだ無い。** ⭐ 実績が予定より前に始まる文書で、全体表示の位置が張り出しを隠さないことを見る試験 | 裁定済 |
| PD-172 | **確認を立てる者** —— ⛔ **`FR-032` は行の削除と WBS の子孫を持つ `Task` の削除に確認を求める（MUST）のに、`ScreenSession.confirmation` に `null` 以外を置く者が `src/` に 1 人もいない。** ⭐ **問いの面・2 択の入口・描画・答えの返り道は本回で全部そろった** | **問いを答えが返るまで保持する者を決める。⚠️ `LY-5` は現在値を `Framework` に置くので、そこが持つのが素直だが、⛔ 削除の命令を「答えを待つあいだ」どう保つかを仕様が述べていない** | E | W5 | `FR-032` / `NT-7` / `OP-4` / `DI-4` / `IC-69` / `IC-70` | ⛔ **まだ無い。** ⭐ WBS の子孫を持つ `Task` を消そうとすると問いが立ち、取りやめると 1 つも消えない試験 | 未裁定 |
| PD-173 | **選ばれた表示言語が次の起動まで残らないこと** —— ⛔ **`FR-038` は「起動したときは前回選ばれた言語で開き、それを読み出せないときはブラウザの言語設定に従うこと（MUST）」と定めるのに、後半しか走っていない。** ⚠️ **`S-99` は 表 T-206 が `localStorage` に置くと定める 4 行の 1 つで、`src/` はその 4 行をどれも読み書きしない** | **`localStorage` に置く 4 行を運ぶ者を決める。⚠️ `PI-29`（`LocalStorageDocumentStore`）は文書の自動保存を持つが、この 4 行は文書ではない** ⭐ **2026-08-24 に調べ直した結果、仕様は動かないと判じた** —— 表 T-060 の `LY-5` と 表 T-062 の `CP-25` が既に担い手を定めており、`FR-020` に先例がある。⛔ **`S-99` の配線は `frame-loop.ts` に既に在る。残るのは試験だけである。** | E | W5 | `FR-038` / `S-99` / 表 T-206 / `PI-29` | ⛔ **まだ無い。** ⭐ 言語を選んで開き直すと同じ言語で開く試験 | 裁定済 |
| PD-174 | **押す前に現在の言語が読めること（ヘッダーの側）** —— ⛔ **`FR-038` は入口を 2 か所に置くこと（MUST）と、押す前に現在どちらの言語かが読めること（MUST）を並べて定める。** ⭐ **ヘルプの面は運んでいる。** ⛔ **ヘッダーの入口は運ぶ欄を持たない** | **`CommandItem` に運ぶ欄を足すか、既存の「入っているトグル」の欄で表すか。⚠️ 後者は 2 言語の選択に「切」が無いので嘘になる。⛔ どう見せるかを仕様が述べていないので裁定が要る** | E | W5 | `FR-038` / `IC-21` / 表 T-109 | ⛔ **まだ無い** | 裁定済 |
| PD-175 | **「他の行に表示されている」ことを人に見せる印** —— ⛔ **`FR-032` はその旨を示すこと（MUST）と定めるのに、語（`PD-160`）も図形（表 T-109 が図形の全数）も決まっていない。** ⭐ **値は属性として画面まで届いている** | **語か図形のどちらかを決める。⛔ どちらも利用者の裁定である**（表 T-109 は新しい図形の発明を禁じ、語は `PD-160` が待っている）| E | W5 | `FR-032` / `NT-7` / `PD-160` / 表 T-109 | ⛔ **まだ無い** | 未裁定 |

⚠️ **3 つの掴み代（`S-90` / `S-91` / `S-137`）が同じ 6px になったことにも由来は無い。** ⭐ **原稿の 🔎 がそれを印している** —— 選び直してよい。


---

## 2026-08-23 のアーキテクチャの巡が足した 1 行（表 T-014 の ST-7）

⭐ **監査（`docs/review/audit-cadence-and-architecture-2026-08-23.md` の 4.）が「測れなかったこと」に挙げた 1 件である** —— 同書は経路を追ったが、どちらが仕様の意図かを本文から決められなかった。
⭐ **巡の先頭で問い、その場で裁定を受けた**（規則 06 の 3b）。⛔ **仕様への適用はまだである。**

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-178 | **安全弁に達したときの返し方** —— ⛔ **表 T-014 の `ST-7` は「達したらエラーを出して処理を止めること（MUST）」と定め、`FR-028` は「例外を投げてはならない（MUST NOT）」と定めるのに、本文がこの 2 つを調停していない。** ⚠️ **`layoutFromSchedule` は `StackSafetyCapReached` を投げ、`src/` にも `index.html` にも捕まえる者が 1 人もいない** —— 監査の測定では `runFrame` が死に、画面が黙って止まる | **値で返す。表 T-233 に行を新設する**（いずれも利用者の裁定 2026-08-23）。⭐ **根拠は前プロジェクトの確定文書が 2 か所で「到達したら人に知らせる（黙って捨てない）」と書いていること**（`03-ui-naming/ui-detail-spec-ja.md` の 1105 行 ／ `07-plan-actual/plan-actual-decisions-ja.md` の 925 行）。⚠️ **`FR-033` の貼り付けの拒否は値でしか答えられず**（`edit-task.ts` の ⛔ MISSING が「段数は文書だけの関数では数えられない」と書いている）、**throw を残すと同じ安全弁が 2 つの機構を持つ。** ⭐ **呼び手は `frame-loop.ts` の 3 か所だけなので実費が小さい** | E | W5 | 表 T-014 の `ST-7` ／ `FR-028` ／ `FR-033` ／ 表 T-233 ／ 表 T-064 の `PI-5` | ⭐ **`tests/unit/layout-engine.test.ts` の 512 行** —— いま `toThrow(StackSafetyCapReached)` を見ており、値で返す形にすると必ず落ちる | 裁定済 |

---

## 2026-08-24 の踏査が開けた 11 行

⛔ **どれも `D`〜`H` である。** ⭐ **実装しない。** ⚠️ **配線の巡は、これらが閉じるまで該当の継ぎ目を開けられない。**
⭐ **全部、規則 06 の 1.（仕様 → `docs/reference/` → 前プロジェクト）を当てたうえで残ったものである。**

| PD | 何が未決か | 推奨と根拠 | 分類 | 波 | 触る行 ID | 覆したとき落ちる試験 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PD-179 | **貼り付けが安全弁に達したかを、誰が数えるか** —— ⛔ **`FR-033` は「段が `ST-7` の安全弁に達したときは貼り付けを受け付けずに通知すること（MUST）」と定めるのに、段数はレイアウトの量である**（`ST-1` が占有幅で数え、表 T-038 が日付だけで数えることを MUST NOT で禁じる）。⚠️ **表 T-067 の `WS-1` 〜 `WS-5` はすべて `pure` で「外を読まない」と本文が明言しており、書き込みの経路がレイアウトを見る行が無い** | **命令にレイアウトを渡す道を決める。**⛔ **`src/use-case/edit-document/edit-task.ts` が自ら「その数はここでは取れない。署名も仕様も誰が渡すかを述べていない」と書いている** | E | W5 | `FR-033` ／ `ST-1` ／ `ST-7` ／ 表 T-038 ／ 表 T-067 | ⛔ **まだ無い。** ⭐ 安全弁に達する行へ貼り付けると受け付けられず通知される試験 | 未裁定 |
| PD-180 | **`localStorage` の鍵の綴りを仕様に載せるか** —— ⛔ **`frame-loop.ts` が自ら「接頭辞の後ろの綴りは仕様に無い。公開するには変更要求が要る」と書いている。**`grsched.language` ほか 4 つは同ファイルが自分で選んだ | **載せないほうを推す。**⭐ 自動保存の鍵（`grsched.autosave`）も仕様に無く、先例がある。⛔ **載せるなら 表 T-206 に鍵の欄が要り、図・表示語・重複基準線の 3 つに連鎖する** | D | W5 | 表 T-206 の `S-99` ／ `S-99a` ／ `S-99b` ／ `S-99c` ／ `PD-110` | ⛔ **まだ無い** | 未裁定 |
| PD-181 | **`S-99a` / `S-99b` / `S-99c` に書き手がいない** —— ⛔ **`S-99a`（透かしに出す名前）は `FR-020` が「入力させ、`localStorage` へ保存すること」と定めるのに入口が無い。⛔ `S-99b`（`Agent API` を有効にした文書）を書く者がいない。⛔ `S-99c`（解除パスワードの `SHA-256`）は 表 T-037 に問う行が無いので誰にも訊けない** | **3 つを別々に決める。**⚠️ `S-99c` は問う道そのものが無いので、表 T-037 に行が要るかから始まる | E | W5 | `FR-020` ／ `FR-086` ／ 表 T-206 の `S-99a` `S-99b` `S-99c` ／ 表 T-037 | ⛔ **まだ無い** | 未裁定 |
| PD-182 | ⭐ **2026-08-24 の踏査で解けた。裁定は要らなかった。** `FR-028` の MUST は「既定では公開しないこと」であり、起動側のフラグと画面での有効化は「か」で結ばれている —— **片方だけで足りる。** `FR-065` の画面の道は必須であり、公開する識別子は用語集 §3 が既に綴っている。⛔ **起動側のフラグは、そもそも存在を求められていない。** 旧記述: **`FR-028` の「起動側のフラグ」に名前も置き場も無い** —— ⛔ **同要求は「既定では公開しないこと（MUST）—— 起動側のフラグか、人による画面での有効化で公開する」と定めるが、フラグを持つ行がどの表にも無い。**⚠️ **`installAgentApi` の呼び出し元は `src/` に 0 件である** | **何をもって旗とするか（ビルド時の定数か、`URL` の問いかけか、宿主の値か）と、答えを置く「公開点」の名を決める** | E | W5 | `FR-028` ／ 表 T-035 ／ 表 T-065 の `IF-7` | ⛔ **まだ無い** | 裁定済 |
| PD-183 | ⚠️ **2026-08-24 の踏査で半分だけ解けた。** 置き場は決まっている（表 T-024 の `IO-5`、`FR-026` の「同じ鍵を上書きするだけ」）。⛔ **残るのは鍵の値と 4 つ目の状態だけであり、`FR-061` が逐語で「まだ決まっていない」と書いているので、これは本物の裁定待ちである。** 旧記述: **`FR-061` の鍵と、4 つ目の状態** —— ⛔ **同要求が逐語で「本要求が使う鍵はまだ決まっていない」と書き、表 T-227 の `DI-1` を使うことを MUST NOT で禁じている。**⛔ **3 状態のどれも「開いたばかりでまだ 1 度も自動保存していない文書」を述べていない** | **鍵を決め、4 つ目を立てるか `saved` に倒すかを決める。**⛔ **`IF-4` の `writeSnapshot` が鍵を要求するので、これ無しには繋げない** | E | W5 | `FR-061` ／ 表 T-227 の `DI-1` ／ 表 T-065 の `IF-4` | ⛔ **まだ無い** | 未裁定 |
| PD-184 | ⭐ **2026-08-24 の踏査で解けた。裁定は要らなかった。** 写し方は `AG-9` 自身が持つ（「対象は 表 T-027 の取り消し対象行と一致させる」）。⛔ **拒否の区分を身振りごとに割ってはならない** —— 表 T-233 は `RS-7` の 1 行しか持たず、同表の前文が持たない理由を運ぶことを MUST NOT で禁じている。**問いは自分で答えていた。** ⭐ **さらに実測で分かったこと: 逐語どおりに広げると、豊かになるどころか壊れる** —— 画面の入口に落ちた押下は 表 T-023a の行を持たない（同表の判定順序は日程の描画領域だけに掛かる）ので、⛔ 行を運ぶ形にすると「行が無い」が「文書を変えない」と読まれ、パレットの押下すべてから `AG-9` の保護が外れる。**真偽値のままが正しい。** 旧記述: **`IF-7` が運ぶ身振りの行 ID を、拒否の区分へどう写すか** —— ⛔ **表 T-065 の `IF-7` は「どの身振りの最中か（表 T-023a の行 ID を運ぶ）」と定めるのに、`snapshot-source.ts` は真偽値のままである**（`CR-217` が広げた欄に `src/` が追いついていない）。⚠️ **`AgentRefusalReason` は 2 値である** | **行ごとに分けるか 2 値のままにするかを決める。**⭐ **仕様が行 ID を運べと定めている以上、運ぶ側は動く。分けるかは拒否の区分の話である** | E | W5 | 表 T-065 の `IF-7` ／ 表 T-035 の `AG-9` `AG-9a` ／ 表 T-023a | ⭐ `tests/contract/if-7-snapshot-source.test.ts` | 裁定済 |
| PD-185 | ⭐ **2026-08-24 の踏査で解けた。裁定は要らなかった。** 綴りは規則 06 の探索順の 3 段目にあった —— `previous-project-result/10-agent-interface/` が HTML の id と「ちょうど 1 つ」を明記している。⛔ **分類 D も誤りであった** —— この id は `GRS JSON` にも `MSPDI` にも届かない（表 T-024 の `IO-7` は書出のみ）ので移行が要らず、C である。旧記述: **`IF-8` の入れ物の id の綴り** —— ⛔ **`app-shell-source.ts` は「シェルが id を持つ」と定めるだけで値を述べておらず、仕様のどの表にも無い。**⚠️ **`BT-1` が読む側なので、綴りが 1 つに決まらないと起動の 1 段目が閉じない** | **1 つに決めて記録する。**⭐ 分類は `A` に見えるが、⛔ **保存された `.html` から読み出す綴りなので、覆すと出荷済みの単一 `.html` が開けなくなる** | C | W5 | 表 T-065 の `IF-8` ／ 表 T-034 の `BT-1` ／ `FR-067` | ⛔ **まだ無い** | 裁定済 |
| PD-186 | ⛔ **2026-08-24 の踏査で、本行の主張が逆であることが分かった。** `FR-020` は「選ばせる」であって「問う」ではなく、`NT-7` は「要求が確認を求めると定めた場面だけ」に問うことを許し、`FR-031` はそれ以外で確認を求めることを MUST NOT で禁じている —— ⛔ **表 T-037 に行を足すと 2 つの MUST を破る。** ⭐ **要るのは表 T-202 の真偽の行 1 つである。**残る裁定は 1 つだけ: **透かしをしまったとき、書き出す絵からも消えるか。** 旧記述: **「画像に載せるかどうか」を問う行が 表 T-037 に無い** —— ⛔ **`FR-020`（MUST）は選ばせることを求め、`FR-025` は「`LM-8` はこの経路にも掛かるのでここでも通すこと（MUST）」と `IO-6` にも及ぼしているのに、`NT-7` は「続ける／取りやめる」の 2 択しか持たない。**⛔ **`src/` にも問う道が無い** | **2 択で足りるのか、別の作法が要るのかを決める。**⚠️ **`IF-5` と `IF-6` の両方に掛かる** | E | W5 | `FR-020` ／ `FR-025` ／ 表 T-004 の `LM-8` ／ 表 T-037 の `NT-7` ／ 表 T-024 の `IO-6` | ⛔ **まだ無い** | 未裁定 |
| PD-187 | **浮いた `Promise` 3 本の拒否をどうするか** —— ⛔ **`frame-loop.ts` の 3 本が `void ...().finally(...)` の形で、`.finally` は拒否を消費しない。**⚠️ **3 本とも「拒否は `IF-3` の契約違反である」と自ら宣言している** | **握り潰す・そのままにする・新しい行で告げる、のどれかを決める。**⛔ **仕様がどれとも書いていない。**⭐ 表 T-004 の `LM-14` とは別の話である | E | W5 | 表 T-065 の `IF-3` ／ 表 T-004 の `LM-14` ／ 表 T-233 | ⛔ **まだ無い** | 未裁定 |
| PD-188 | **失敗値の形が揃っていない** —— ⛔ **実測: 弁別子が 4 通り（`ok: false` ／ `accepted: false` ／ `kind:` ／ 旗なし）、積荷の名前が 7〜9 通り、`ImportRefusal` という同名の型が別の形で 2 つ在る** | **揃えるかどうかを決める。**⭐ **揃えるなら 表 T-064 の公開名が動くので `E` である。**⚠️ **揃えないなら、読む側は型ごとに形を覚えることになる** | E | W5 | 表 T-064 ／ `PI-8` ／ `PI-13` ／ `PI-19` | ⛔ **まだ無い** | 未裁定 |
| PD-189 | **`uuid` の 10 列を誰も見ていない** —— ⛔ **生成スキーマの `format` のうち、日付 2 件は 表 T-220 の `IV-14` が持つが、`uuid` の 10 件は他に持ち主がいない。**⚠️ **CR-239 が検証器に持たせない側で書いたので、10 列は誰にも見られないまま残る** | **検証器に持たせるか、表 T-220 に行を足すかを決める。**⛔ **`CR-239` はこれを穴として記録しただけで、埋めていない** | E | W5 | 表 T-220 の `IV-14` ／ Chapter 6.1 の 表 T-220 の前文 | ⛔ **まだ無い** | 未裁定 |
| PD-190 | **`clampedSettings` を誰も呼んでいない** —— ⛔ **表 T-064 の `PI-2` が「下限・上限に収める」として公開しているのに、`src/` の呼び手が 0 件である。**⚠️ **`scheduleViolations` とまったく同じ形の穴であり、2026-08-24 の巡はそちらを閉じてこちらを見落としていた。**⛔ **どこで呼ぶかを仕様が 1 行も述べていない** | **読む路（`CP-20` か `CP-10`）で呼ぶのを推す。**⭐ 根拠は 表 T-024a の `OP-6` が読む側に寛さを求めていることと、`ClampResult` が「動かした値」の一覧を返す形になっていること —— ⚠️ **その一覧を人へ告げるなら、表 T-233 に行が要る**（作法は `NT-5`）。⛔ **告げるかどうかも未決である** | E | W5 | 表 T-064 の `PI-2` ／ 表 T-024a の `OP-6` ／ `CP-10` ／ `CP-20` ／ 表 T-233 | ⛔ **まだ無い。** ⭐ 下限を割る設定値を持つ文書を開くと、値が収まり、そのことが告げられる試験 | 未裁定 |
| PD-191 | **フェードの掴み点を、幾何の側が選択で絞れない** —— ⛔ **`FR-075` の MUST と `S-111` は「選択中のタスクだけ」と定めるが、`geometryFromLayout` も `itemAtPointer` も `Selection` を渡されていない。**⚠️ **描く側は選択を見ており正しい。当たり判定だけが広い** ⛔ **2026-08-26 の裁定: 矢羽根もレクタングルも掘み点を出せ。**⭐ **絞りを外し、遮りは当たり判定を選択で絞って直す** | ⭐ **2026-08-26 に本筋を当てた** —— `geometryFromLayout` の署名に `Selection` を通し、フェードの日数での絞りを外した。⚠️ **その絞りが効いて見えたのは、起動テンプレートの 1000 タスクのうち 997 が `fadeInDays` を持たないからであり、実際には 99.7% の行で掴み点を消していた。**⛔ **矮すぎる切り口であった。**⭐ **矢羽根とレクタングルには新しいコードなしで掴み点が出た** —— 形の門（`actualPlacement === 'inside'`）が元から `FD-5` の `SH-1` と `SH-2` を通していた。⚠️ **当たり判定の側（`itemAtPointer`）には選択を渡していない** —— 出す側が既に絞っているので、`S-111` の条件を 2 か所に書かない | E | W4 | 表 T-064 の `PI-6` ／ `FR-075` ／ `S-111` ／ 表 T-023d の `GR-1` `GR-2` `GR-3` `GR-4` | ⭐ **選択していないタスクの角を押しても `GR-1` にならない試験** | 裁定済 |
| PD-192 | **1 行より短いパンが、行丁度飛ぶこと** —— ⛔ **`S-78` は表示位置を行まるごとで持つので、行の途中を表せない。**⚠️ **床を置かなければ短いパンは何も動かず、置けば 表 T-023 の `MK-7` の「パンは等倍とする（MUST）」を破る。**⭐ **`PD-176` と同じ穴の、ホイールではない側である** | **1 行の床を置いた（`rowTurnedTo` を `PD-1` も呼ぶ）。**⭐ 根拠は、利用者の訴えが「背景を掴んでスクロールできない」であり、**動かないことのほうが大きい不具合だからである。**⛔ **本筋は `S-78` に行内の端数を持たせることだが、それは保存形の変更である** | D | W4 | `S-78` ／ 表 T-023 の `MK-7` `MK-1` ／ 表 T-023a の `PD-1` | ⭐ **1 行未満のパンで絵が動く試験** | 裁定済 |
| PD-193 | **2 連カーソルが 1 本も描かれていない** —— ⛔ **表 T-029a の `DC-8`（MUST）は追従している側を太く描けと定めるが、描く側はカーソルを見ていない。**⚠️ **実測: `DocumentSettings.dualCursor` はむき出しの無いオブジェクトか `null` としか型付けされておらず 2 つの日付が型付けされていない、`ScheduleGeometry` に行が無い、描画側に STOP が立っている。**⭐ **`DC-8` 以前の穴である** | **`dualCursor` の 2 つの日付を `erd.json` で型付け、`ScheduleGeometry` に行を与えるのを推す。**⚠️ **値の裁定ではなく、形の欠落である** | E | W4 | 表 T-029a の `DC-1` 〜 `DC-8` ／ `S-71` ／ 表 T-064 の `PI-6` | ⭐ **追従している側が太く描かれる試験** | 未裁定 |
| PD-194 | **コメントボックスが 1 つも描かれていない** —— ⛔ **表 T-076 の `EP-5` は注記を描くと定め、表 T-023c の `SL-8` は選択時に枠で囲めと定めるが、`commentBoxes` は `layoutEngine` にも描画側にも現れない。**⚠️ **ハイライトボックスは描かれており、枠も付く** —— 同じ行の片方だけが空いている。⭐ **CR-258 以前の穴である** | **`ScheduleGeometry` にコメントボックスの矩形を与えるのを推す。**⚠️ **文字を測って大きさを決めることは `FR-093` が禁じている**ので、大きさを持つ行が先に要る | E | W4 | 表 T-076 の `EP-5` ／ 表 T-023c の `SL-8` ／ `FR-093` ／ 表 T-064 の `PI-6` | ⭐ **コメントボックスを持つ日程を描くと要素が出る試験** | 未裁定 |
| PD-195 | **描く深さの上限が 1 に張り付き、行が 68 → 7 に落ちる** —— ⛔ **実測: `fitZoom` が `zoomY` 0.0708 を答え、深さ 2 のしきい値 0.32 を割る。**⚠️ **起動テンプレートが `scrollDate` を `null` で出すので `OP-10` により毎フレーム再フィットされ、保存値を上書きし続ける。**⛔ **`setZoom` は `scrollDate` を書かないので、縦の拡大では抜け出せない。**⭐ **行の操作子が効かないのもインデントが浅いのもこの 1 つが原因** | **決めるのは 3 つ** —— ① 再フィットが保存値を上書きしてよいか、② `setZoom` も `scrollDate` を書くべきか、③ `groupLevelOfDetailBase`（0.32）は妥当か。⛔ **値の調整ではなく設計の穴である** | G | W4 | `FR-018` ／ `FR-055` ／ 表 T-024a の `OP-10` ／ `S-87` `S-88` | ⭐ **新規文書で縦の拡大を押すと深い行が現れる試験** | 未裁定 |
| PD-196 | **4 段目（年・月・日・曜日）のしきい値が遠い** —— ⛔ **`S-85` は 30 px/日で、届くには `zoomX` ≧ 5.83（ホイール約 19 ノッチ）要る。**⭐ **参考実装は 8 px/日（`zoomX` ≧ 1.33）** | ⛔ **2026-08-26 の裁定: 30 → 8 前後へ下げる。**⚠️ **`FR-017` が実装による書き換えを禁じるので、原稿の 1 セルを変える** | A | W4 | `S-85` ／ `FR-017` ／ 表 T-005a の `L-1` | ⭐ **`zoomX` を数ノッチ上げると 4 段目に入る試験** | 裁定済 |
| PD-197 | **実績のダミーを掘めない** —— ⛔ **利用者の報告（2026-08-26）: ダミーのある辺りにポインタを持っていっても現れず、掘めない。**⚠️ **未調査** | **まず測る。**⭐ 仕様は既に「薄く描き、ポインタが乗っているあいだだけ濃くすること（MUST）」と定め、`FR-048` の名簿にも入っている。⛔ **裁定ではなく不具合である可能性が高い** ⛔ **裁定すべきことは無い** —— 利用者の「掘めるようにしろ」が答えであり、仕事は実装と調査である。⭐ **行は記録として残す** | B | W4 | `FR-043` ／ `FR-013` ／ `GR-9` `GR-17` `GR-18` ／ `S-93` ／ `S-131` | ⭐ **ポインタをダミーに乗せると濃くなり、押すと実績が置く試験** | 裁定済 |
| PD-198 | **マイルストーンの一覧を開く入口の図形** —— 状態は `S-142` が持つが、図形が `▼` / `▲` である | ⛔ **2026-08-26 の裁定: `...` のトグルにする。**⚠️ **表 T-109 の行と 図 F-019 の図形を同じ編集で足すこと** —— 片方だけだと生成器が拒む | C | W4 | `S-142` ／ `FR-053` ／ 表 T-109 ／ 図 F-019 | ⭐ **一覧を開く入口が `...` で描かれる試験** | 裁定済 |
| PD-199 | **実績のダミーを描く図形と大きさが、どの表にも無い** —— ⛔ **`U-52` に仕様が与えているのは 4 行だけである**：`S-129` と `S-130` は期間、`S-131` は薄さ、`S-93` は**当たり判定**。⭐ **描く大きさの行が 1 つも無い。**⚠️ **これが「ダミーが見えない」の真因である** —— 当たり判定は生きており、目をつぶって押せば掴める | **表 T-210 に、`S-109` ・ `S-110` の隣へ 1 行置くのを推す** —— フェードの掴み点がまさにその形で描く大きさを持っている。⛔ **図形そのものは発明しない** —— 表 T-026 の `RC-13` が図形の決定を利用者の裁定と定めている。⚠️ **`GR-9` と `GR-17` が 2 つの点なのか 1 本の帯の両端なのかも同じ行が決める。⛔ **分類 `E` の根拠**: コード側に暫定値を置ける話ではなく、**行が先に要る**。`PD-194` と同じ形である | E | W4 | `U-52` ／ `S-131` ／ `S-93` ／ 表 T-210 ／ 表 T-023d の `GR-9` `GR-17` `GR-18` ／ `FR-013` | ⭐ **未着手のタスクを描くとダミーの要素が出る試験** | 未裁定 |
| PD-200 | **`FD-6` の「期間」が暦日なのか稼働日なのか** —— ⛔ **実装が 2 つに割れている。**`schedule-geometry.ts` は暦日で切り詰め、`edit-task.ts` の `IV-12` は稼働日で拒む。⚠️ **非稼働日をまたぐ予定では、`FD-6` が許すフェードを `IV-12` が拒む** | **暦日を推す** —— ⭐ 根拠は 表 T-012a が 4 点を「横（日）」と日の軸で綴っていることと、`FD-6` が既に実装されている唯一の場所が暦日であること。⛔ **ただしこれは受理の境界を動かす** | F | W4 | 表 T-012a の `FD-6` `FD-7` ／ `IV-11` `IV-12` ／ `FR-075` ／ 表 T-023d の結びの規則 | ⭐ **非稼働日をまたぐ予定で、角を端まで引いても拒まれない試験** | 裁定済 |
| PD-201 | **表 T-078 に、長押しの連続がフレームを起こす契機の行が無い** —— ⛔ **同表は「本表に無い契機でフレームを起こしてはならない（MUST NOT）」と自らを閉じている。**⚠️ `FT-1` の括弧は `CS-4`（人の答え待ち）を名指し、`FT-4` の注は「数えるのは 3 つ」と書いている | **`FT-4` の注を 3 から 4 へするのを推す** —— ⭐ 同行は「時間が来たこと」「シェルが自分で計る」であり、長押しの刻みはまさにそれである。⚠️ **`FT-1` の括弧を広げる道もあるが、そちらは「待ちをまたぐ続き」の意味を緩める | G | W4 | 表 T-078 の `FT-1` `FT-4` ／ `FR-018` ／ `S-172` `S-173` | ⭐ **長押しで倍率が刻まれ続ける試験** | 未裁定 |
| PD-202 | **タイムルーラの帯を塗ると述べた行が無い** —— ⛔ **実測（開発サーバの実 DOM）: 矩形は 5 枚とも行の帯であり、ルーラの帯に 1 枚も無い。**⚠️ **背後が透けるという利用者の報告はこれである** | **`FR-041` の「地の色を自分で塗ること（MUST）」を根拠に、`S-146` で帯に 1 枚敷くのを推す** —— ⭐ **`S-146` は既に `SvgRenderer` へ届いている**（本巡で経路を通した）。⛔ **ただし帯の範囲を述べた行が無いので、体は描かずに止まった。⛔ **分類 `E` の根拠**: コード側に暫定値を置ける話ではなく、**行が先に要る**。`PD-194` と同じ形である | E | W4 | `FR-041` ／ 表 T-236 の `S-146` ／ `S-2` ／ 表 T-005a の `L-1` | ⭐ **ルーラの帯に地の矩形が 1 枚出る試験** | 未裁定 |
| PD-203 | **全体表示の横をどの倍率で測るか** —— ⛔ **基準を名乗る行が無い。**⚠️ 縦の答えは横に乗る —— `S-86` が低い `zoomX` で絵を間引き、高い `zoomX` では幅広の帯が重ならなくなるので、**同じ深さでも描く高さが `zoomX` で動く** | **等倍で測るのを推す** —— ⛔ 自分が書く倍率で測ると、答えがもう一度漸化式になり `D-22` の冕等性が戻らない。⭐ 等倍は保守的な底でもある —— `S-86` が何も落とさないので、合わせる実寸が最も大きい | B | W5 | `FR-055` ／ 表 T-068 の `LC-1`〜`LC-9` ／ `S-86` | ⭐ **保存された `zoomX` を動かしてから押しても同じ絵になる試験** | 未裁定 |
| PD-204 | **深さ 1 に降りたときの縦の倍率** —— ⛔ **`FR-018` の定義域は深さ 2 以上であり、深さ 1 には「描ける最小の倍率」が存在しない。**`FR-055` の MUST NOT はそこで空転する | **深さ 2 のしきい値の `S-53` 1 刻み下を推す** —— ⭐ 深さ 1 だけを描く最大の倍率であり、`FR-055` の「無用に縦幅を増やすな」に合う。⚠️ 縦ズーム拡大 1 押しでちょうど深さ 2 に乗る。⛔ `FR-055` の RATIONALE はここまで述べていない | B | W5 | `FR-055` ／ `FR-018` ／ 表 T-205 の `S-87` `S-88` ／ `S-53` | ⭐ **深さ 1 に落ちた後、縦ズーム拡大 1 押しで深さ 2 が出る試験** | 未裁定 |
| PD-205 | **全体表示が横だけを `S-97` ／ `S-98` に収め、縦を収めないこと** —— ⛔ **`FR-016` は範囲をズーム操作に置き、`CM-71` が適用するが、どちらの軸に掛かるかを述べた行が無い** | **横は収め、縦は収めないのを推す** —— ⛔ 横を収めなければ、`CM-71` が切った倍率で描くことになり、**測っていない絵を出す。**⭐ 縦の着地値はすべて `S-97`〜`S-98` の内側にあり、clamp すればしきい値を外れて別の深さを答えてしまう | B | W5 | `FR-016` ／ `CM-71` ／ 表 T-206 の `S-97` `S-98` | ⭐ **横の答えが `S-97`〜`S-98` に収まり、縦がしきい値の上に乗り続ける試験** | 未裁定 |
| PD-206 | **全体表示だけが深さを直に指定できること**（`groupDepthCap`）—— ⛔ **表 T-068 の 1 回目は「その文書が持つすべての深さ」を 1 つの倍率で測れと定めるが、その倍率では `FR-018` が深さを 1 つに定めてしまうので、`zoomY` 経由では深い段を頒めない** | **`FR-055` 専用の引数として足すのを推す** —— ⭐ 表 T-064 の見出し自体が引数と戻り値を `src/` に任せており、**公開されたメンバは動かない。**⚠️ 他の呼び手が渡さないことは規則ではなく慎みである | A | W5 | `FR-055` ／ 表 T-068 ／ `FR-018` ／ 表 T-064 の `PI-5` | ⭐ **全体表示以外の呼び手が深さを渡さず、`FR-018` の導く深さを得る試験** | 未裁定 |
| PD-207 | **2 回目が一段退いたとき、表示位置をどの測定から取るか** —— ⛔ **どの行も述べていない。**退いた深さを厳密に測るには 3 回目が要るが、表 T-068 の結びの規則がそれを禁じている | **床の測定から取るのを推す** —— ⭐ `scrollGroupId` はどちらでも厳密（同じ深さは同じ行を同じ順で描く）。⛔ `scrollDate` はわずかに遅れ得るが、`LC-6` は左にラベルを置かないので、**ずれは `LF-10` の図形半分（予定高さの半分）が上限である** | B | W5 | `FR-055` ／ 表 T-068 の `LC-6` ／ `LF-10` ／ `S-77` `S-78` | ⭐ **2 回目が退いた文書で、表示位置が描く絵の左端から予定高さの半分を超えて離れない試験** | 未裁定 |
| PD-208 | **`GR-18`、マイルストーンのただ 1 つのダミーを何として描くか** —— 付録は「点のダミーを 1 つだけ」と書くが、表 T-012 の `SH-5` が定めるのはマイルストーン自身の図形であって掴みシロの図形ではない | **`GR-9` / `GR-17` と同じ薄い矩形を推す** —— `S-180` の幅、実績バーの帯の高さ、予定図形の中央に置く。⭐ 根拠は規則 03 の 4.（似たものは似た形で描く）—— `FR-043` は 3 つとも「掴みシロ」と呼び、`S-93` は 3 つに**同じ**当たり判定を与えている。⛔ `SH-5` を写した菱形にすると、2 つ目のマイルストーンに見える | C | W5 | `FR-043` ／ 表 T-023d の `GR-9` `GR-17` `GR-18` ／ 表 T-206 の `S-180` `S-93` ／ 表 T-012 の `SH-5` | ⭐ **マイルストーンのダミーが、矩形のダミーと同じ形・同じ幅で出る試験** | 未裁定 |
| PD-209 | **ダミーが重ね順のどこに入るか** —— 表 T-020 は 5 行を持ち、`U-52` の行が無い | **`ZO-2`、実績バー自身の層を推す** —— ⭐ ダミーは実績バーの端の代役であり、`Task` がダミーを持つのは実績バーを**持たないとき**に限るので、両者は決して競合しない。⚠️ 表 T-020 が実際に述べている順序はどちらにも保たれる —— `ZO-3` のマーカーと `ZO-5` のラベルは前に残る | C | W5 | 表 T-020 の `ZO-2` `ZO-3` `ZO-5` ／ `U-52` ／ 表 T-023d の `GR-7` | ⭐ **ダミーが進捗マーカーとラベルより後ろに描かれる試験** | 未裁定 |
| PD-210 | **書き出しの路が「ダミーを描くな」をどう伝えるか**（`EP-14` の入口）—— 同行は描かないと定めるのに、伝える口が 1 つも無い | **`svgFromSchedule` の 7 番目の引数（必須）を推す** —— `picture` は「画面」と「書き出し」の 2 値、末尾に足す。⭐ 引数の並びが `src/` のものであることは 3 か所が独立に述べている（表 T-064 の見出し・`image-exporter.ts`・検査 26b の注）。⛔ **既定値を持たせない** —— 持たせると、書き出しの路が 1 つ足し忘れられただけでダミーが黙って漏れる。**`EP-14` はまさにそれを防ぐために在る** | A | W5 | `EP-14` ／ `EP-5` ／ 表 T-041 の `WY-3` ／ 表 T-064 の `PI-19` | ⭐ **同じ日程表を書き出した絵にダミーが 0 個で、進捗マーカーは残る試験** | 未裁定 |
| PD-221 | **構えている行の語が辞書に無いとき、`armedText` が何を言うか** —— 仕様に答えが無い | **空文字を推す** —— ⛔ 表 T-023b の結びが行 ID を刷ることを禁じているので、表 T-023 が `assignmentText` で行 ID を代役に使う手は取れない。⚠️ 表 T-233 の `RS-15` や 表 T-234 の `QN-8` に当たる落ち先の行を 表 T-023b は持たず、**7 行目を発明するのは規則 03 の 1. が禁じる鋳造である。**⭐ 空文字はどちらの規則も禁じておらず、`entryLabel` が既に取っている代役と同じである。⚠️ `gen:check` が通るあいだこの枝には到達しない —— 守っているのは手で書き換えられた生成物である | C | W5 | `FR-053` ／ 表 T-023b の結びの規則 ／ 表 T-233 の `RS-15` ／ 表 T-234 の `QN-8` | ⭐ **`uf-65` の `armedText` の各件** | 未裁定 |
| PD-230 | **折り返した各行のベースラインが行の箱のどこに座るか** —— `FR-097` は 1 行の高さを定めたが、その中のどこに字を置くかは述べていない | **em の箱の足を推す** —— `body.y` ＋ `S-181` ＋（`i` ＋ 1）× 文字の大きさ。⚠️ `rulerSvg` が同じ足を自分の持ち上げつきで計算しているが、**`S-179` はルーラの行であり、規則 03 の 1. が借用を禁じる。**⛔ 最終行の下に伸びる字が `S-181` を食う | C | W5 | `FR-097` ／ 表 T-215 ／ `S-181` ／ `S-179` | ⭐ **2 行の本文の `text` の `y`** | 未裁定 |
| PD-231 | **本文の箱の地の色と字の色** —— 表 T-236 に注記の色が 1 行も無い | **地は `S-146`、字は `S-147`、縁は既存の `ANNOTATION_COLOUR` を推す** —— ⛔ `NFR-007` は 4.5:1 を MUST とし、コメントボックスを名指している。⭐ **実測（体が測り直した）**: `S-147` on `S-146` は明 **17.76:1** ／ 暗 **14.94:1**。⚠️ **注記の色を字に使うと暗で 3.58:1 となり MUST を破る。**`S-162` が印を浮かせるために `S-146` を取る前例である。⛔ **地を敷くので後ろのバーが隠れる。これは MUST の代償である** | C | W5 | `NFR-007` ／ 表 T-236 の `S-146` `S-147` `S-151` `S-162` ／ `FR-019` | ⭐ **どの色相でも両テーマで対比が 4.5:1 を超える試験** | 未裁定 |
| PD-232 | **`bodyOffsetPx` が `null` のとき本文の箱がどこに座るか** —— `CM-46` が作る箱はすべてこの状態である | **`{dx:0, dy:0}` を推す** —— `FR-019` は同値をアンカーから本文までの距離として持つので、「一度も引かれていない」を素直に読めば「距離が無い」である。⛔ ほかの既定は、誰も置いていない場所へ箱を置く | C | W5 | `FR-019` ／ `AT-115` ／ `UC-008` の手順 2 | ⭐ **本文の左上がアンカーに一致する試験** | 未裁定 |
| PD-233 | **アンカーの `y` が行のどこか** —— `FR-019` は日付と行で結ぶとしか述べていない | **行の帯の中央（`row.y` ＋ `row.height` ÷ 2）を推す** —— ⭐ 行は帯であり、**その中央はほかのどの規則も語っていない唯一の点である。**⛔ 帯の上端・下端は採らない —— そちらは `highlightGeometry` のものであり、ハイライトは範囲を**囲う** | C | W5 | `FR-019` ／ 表 T-221 の `LF-2` | ⭐ **アンカーの `y` が帯の中央にある試験** | 未裁定 |
| PD-234 | **指す先の行が描かれていない箱、または日付が `null` の箱をどうするか** —— `AT-113` / `AT-114` はどちらも許す | **描かない（したがって掴めない）のを推す** —— `UC-008` の拡張 2a が畳んだ行・隠した行の箱を隠すと定め、表 T-023a の注が「描かれていないものは当たり判定の対象外」と読ませる。⛔ **`highlightGeometry` の落ち先はあえて写さない** —— あちらは拡張 4a に仕える別の規則である。⚠️ **帰結**: 指す先を失った箱は届かず、消せもしない | C | W5 | `UC-008` の拡張 2a ／ 表 T-023a の注 ／ `AT-113` `AT-114` | ⭐ **畳んだ行の箱が描かれず、当たりもしない試験** | 未裁定 |
| PD-235 | **`GR-14` の中で、コメントボックスとハイライトボックスが重なったときどちらが勝つか** —— 表 T-023d は両者に 1 行しか与えず、順を述べていない | **コメントボックスを先に推す** —— ⭐ コメントボックスはハイライトの範囲の内側に置ける。**囲うほうが勝つと、内側の箱を二度と掴めない** —— `GR-19` の但し書きが同じ罠を綴っている（「掴めない位置へ置けてしまうと二度と動かせなくなる」） | C | W5 | 表 T-023d の `GR-14` `GR-19` ／ 表 T-023a | ⭐ **ハイライトの内側のコメントボックスを掴める試験** | 未裁定 |
| PD-236 | **本文が空または `null` の箱を、どの大きさで描くか** —— ⛔ **`CM-46` はすべての箱をこの状態で作る** | **全角 1 文字ぶん・1 行ぶん（文字の大きさ ＋ `S-181` × 2 の正方形。M で 20×20）を推す** —— ⛔ `S-181` × 2 だけだと 6×6 になり、狙えない。**最も普通の場合で `D-06` を開き直すことになる。**⭐ 導出は 表 T-215 ＋ `S-30` ＋ `S-181` を `FR-093` の em の箱で読むだけであり、新しい値を鋳造していない | C | W5 | `FR-093` ／ `S-30` ／ `S-181` ／ 表 T-215 ／ `CM-46` | ⭐ **`text` が `null` の箱が描かれ、掴める試験** | 未裁定 |
| PD-237 | **折り返しが語の途中に落ちてよいか** —— `FR-097` は折り返しを `S-182` に送るだけで、語の切れ目に触れていない | **どの 2 文字のあいだでも折ってよいのを推す** —— ⚠️ **`S-182` の備考は逆に読める**（「これを超える語は作成者が改行する」）が、⛔ **同行の既定が全角 64 を綴っているのは、空白で語を切らない書記体系のためである。**そこには尊重すべき境界が無い | C | W5 | `FR-097` ／ `S-182` ／ `FR-093` | ⭐ **全角 64 文字が 1 行、65 文字で 2 行になる試験** | 未裁定 |
| PD-238 | **注記が重ね順のどこに入るか** —— ⛔ **表 T-020 に注記の行が 1 つも無い**（実測: `ZO-1` `ZO-1a` `ZO-2`〜`ZO-5` だけ） | **`ZO-5` の名称ラベルより前を推す** —— ⭐ `NFR-007` の 4.5:1 は本文自身の地に対して満たしているので、**その上をラベルが横切ると、測っていない字がそこに乗る。**`handleParts` とルーラが既に取っている読みと同じである | C | W5 | 表 T-020 ／ `NFR-007` ／ `FR-019` | ⭐ **本文を横切るラベルが後ろに回る試験** | 未裁定 |
| PD-250 | **掴んでいるあいだ絵を描く行がどれか** —— 表 T-023d は結びの規則 2 つで `GR-9` / `GR-17` / `GR-18` と `GR-1` / `GR-2` に絵を求めるが、残りの行については何も述べていない | **その 5 行と `Panel Divider` だけを true にするのを推す** —— ⭐ 5 行は推測ではない。表 T-023d の結びが既に MUST で求めており、境界は `FR-052` の STATEMENT が求めている。⛔ 他の行を true にするのは、この単位が要求を発明することである（規則 03 の 1.）。⚠️ `GR-19` は `GrabArea` に無いので載せられない —— パレットは文書から描かれず、`paletteFollow` が別の路で既に追従している | C | W5 | 表 T-023d の結びの規則 2 つ ／ `FR-052` ／ `FR-031` ／ 表 T-028 の `IN-1` | ⭐ **`GR-1` を掴んで動かすと、離す前に □ が動く試験。⛔ 同じドラッグで文書の `fadeInDays` が動かない試験** | 未裁定 |
| PD-252 | **フェードの掴み点が、矢羽根の切り込みの頂点に乗るのか時間軸の日に乗るのか** —— `FD-5` はフェード長が切り込みの深さを `max(fadeIn, fadeOut)` で置き換えると定めるので、2 つのフェードが異なる矢羽根では両者が一致しない | **時間軸の `start + fadeIn` / `end − fadeOut` を推す** —— ⭐ 表 T-023d の結びが「掴み点は時間軸の上の『ある日』そのものであり、画素の感度というものが存在しない」と述べており、日はポインタの下の日から取る。⛔ 切り込みの頂点へ置くと、ドラッグが読まない日に掴み点が立つ。⚠️ 見える代償は、`fadeOut` が `fadeIn` より大きい矢羽根で `GR-1` の掴み点が切り込みの内側に入ること。`FD-5` は多角形に第 4 の点を与えないので第 3 の選択肢は無い | C | W5 | 表 T-012a の `FD-4` `FD-5` `FD-6` `FD-6b` ／ 表 T-023d の `GR-1` `GR-2` と結びの規則 ／ `FR-075` | ⭐ **`fadeIn` と `fadeOut` が異なる矢羽根で、2 つの掴み点が `start + fadeIn` と `end − fadeOut` に立つ試験** | 未裁定 |
| PD-253 | **ドラッグの途中で `editDocument` が書き込みを拒んだとき、何を描くか** —— `FR-052` の `Row Area` が 0 以下になる組、または `FD-6` / `IV-12` に触れたフェード | **そのフレームは絵を出さず、`held.document` を描くのを推す** —— ⭐ 表 T-067 の `WS-3` は 1 つでも拒まれれば束ごと捨てるので、その場で離しても何も書かれない。**離した結果と一致する絵は、変わっていない絵である。**⛔ 「最後に受理された絵」を保つと、絵がポインタの現在の純関数であるという不変条件が壊れ、離しても得られない幅を見せることになる。⚠️ 見え方の代償: 限界を越えて境界を引くと、絵が限界で止まらず保存された幅へ戻る | C | W5 | `FR-052` ／ 表 T-067 の `WS-3` ／ 表 T-012a の `FD-6` ／ `IV-12` | ⭐ **`Row Area` が 0 以下になる位置まで境界を引いたとき、絵が保存された幅に戻る試験** | 未裁定 |
| PD-254 | **フレームの領域・割り付け・図形を、絵の文書から測るのか `held.document` から測るのか** —— 後者なら 1 フレームに 2 つの文書が混じる | **絵の文書から測り、`settingsLimitsOf` を同時に直すのを推す** —— ⭐ 表 T-070 の `MN-6` は `ADR-001` の 1 パスを `NFR-002` / `NFR-003` に対して選んでおり、絵の脇に「正直な」基準をもう 1 度測る第 2 パスは、まさにその裁定が拒んだ費用である。⛔ `settingsLimitsOf` を直さないと、絵の幅で切られた `Row Area` に保存された幅を足すことになり、`FR-052` が判定する和がドラッグの移動量ぶんずれる —— これは暫定ではなく誤りである | C | W5 | `FR-052` ／ 表 T-070 の `MN-6` ／ `ADR-001` ／ 表 T-064 の `PI-25` ／ `NFR-002` `NFR-003` | ⭐ **境界を 300px 引いているあいだ、`SettingsLimits.rowAreaWidthWithoutPanels` が動かない試験** | 未裁定 |
| PD-267 | **プロパティパネルの項目名は `snake_case` か `camelCase` か** —— ⛔ **MUST を負う表が 2 つ、正面から食い違っている。**表 T-006a の `W-5` は 「i18n キー・プロパティパネルの項目名 ｜ snake_case ｜ `stack_order`」 と綴り、表 T-016 は 18 行すべてを `camelCase` で綴っている。⚠️ **製品は 表 T-016 のほうを画面に出しており**（`actualStart` / `fadeInDays`）、**参考実装は `W-5` のほう**（`actual_start` / `fade_in_days`）である | **推奨を添えて問う。推奨で走らない** —— ⛔ **どちらが負けても、負けたほうの行を書き直すことになる。**⚠️ **`FR-038` が「項目名は訳さない」根拠として 表 T-016 の綴りに寄りかかっているので、そちらを動かすと訳さない理由も動く。**⭐ **`D-13` は止めない** —— 今日の綴りのまま編集できるようにし、綴りは後から 1 欄で直せる | E | W5 | 表 T-006a の `W-5` ／ 表 T-016 の全 18 行 ／ `FR-038` ／ `FR-006` | ⭐ **画面の項目名が `W-5` の綴りに従う試験**（まだ無い）。⭐ **裁定済 2026-08-26: `W-2` と同じ lowerCamelCase。表 T-016 の綴りがそのまま画面に出る。**`W-5` は i18n キーだけを受け持つよう狭めた | 裁定済 |
| PD-290 | **構えの縁を、入口の枠へどう載せるか** —— `FR-053` は色（`S-183`）と太さ（`S-185`）を定めるだけで、**縁の付け方も、太くなったぶん箱がどうなるかも、どの行も持っていない** | **`border-width` と `border-color` の 2 つの長い綴りを `entryStyle` の宣言のあとへ足すのを推す** —— ⭐ `border` の一括綴りは `solid` を言い直すが、`solid` はどの行も定めていない `entryStyle` 自身の選びである。⚠️ **箱は 2px ぶん大きくなる** —— 打ち消す行が 表 T-206 に無く、`FR-053`（MUST）はパレットの大きさを中身へ追随させるので、パレットもそのぶん動く。⛔ **打ち消す数を発明しない**（規則 03 の 1.） ⛔⛔ **問いそのものが消えた（CR-311、2026-08-30）。****`PD-398` の裁定で縁をやめて塗りにしたので、`S-185` は読み手を失って廃止された。**⇒ **「縁をどう載せるか」も「太くなったぶん箱がどうなるか」も、載せる縁が無い以上、問いが成り立たない。**⭐ **箱は 26 × 24px のまま動かない** —— 塗りは太さを持たない。 | C | W5 | `FR-053` ／ `S-183` ／ `S-185` ／ 表 T-109 の `IC-54` | ⭐ **構えている入口の縁が `S-185` の太さと `S-183` の色で描かれる試験。⛔ その入口に `aria-pressed` が立たない試験** | 裁定済 |
| PD-291 | **構えを示す縁が、1 つの入口ではなく一群に掛かること** —— ⛔ **表 T-109 の `構え` の欄は種類を名乗る。**`AR-2` は `IC-23`〜`IC-26`、`AR-3` は `IC-27`〜`IC-34` に付くので、**矩形を構えると 4 つ、1 つの菱形を構えると 8 つの入口が同時に光る。**⚠️ **`AR-4` / `AR-5` / `AR-6` だけが 1 対 1 であり、そこでは正しく 1 つだけ光る** | **推奨を添えて問う。推奨で走らない** —— ⛔ **細かくするには 表 T-023b に形状ごとの行を足すか、`Armed.shapeKind` / `Armed.glyph` の綴りを決めるかのどちらかが要り、後者は `CR-172` が未定のまま残した値である。**⭐ **いまの絵は `FR-053` の逐語どおりである** —— 同要求は 表 T-109 の欄を権威と名指しており、欄が種類しか持たない以上、種類で光るのが仕様の答えである。⚠️ **利用者が「どの図形を構えているか」を見たいなら、行を足す変更要求になる** | F | W5 | 表 T-109 の `構え` の欄 ／ 表 T-023b の `AR-2` `AR-3` ／ `FR-053` ／ `CR-172` | ⭐ **矩形を構えたとき、縁が付く入口がちょうど 1 つである試験**（いまは 4 つで落ちる）。⛔ **裁定済 2026-08-26。推奨は却下された** —— ⭐ **「選択されているパレットのアイコンは 1 つだけ色を付けろ」。**理由: **置くのは 1 つずつであり、全部色づくとどの形状を引きたいのか分からなくなる**（利用者の逐語）。⇒ `Armed.shapeKind` / `Armed.glyph` の綴りを決めて 1 対 1 にする | 裁定済 |
| PD-270 | **プロパティパネルの欄が「いつ」確定するか** —— ⛔ 表 T-016 も `FR-006` も、打鍵ごとか欄を離れたときかを述べていない | **`change`（欄を離れる・Enter）を推す** —— ⭐ 参考実装がそうしている（前に立つ者が `temp/` の実物で確認）。⛔ 打鍵ごとだと、`FR-031` と 表 T-027 の `UN-3` が「1 つのプロパティ変更 ＝ 取り消し 1 段」と定めているのに 1 文字ごとに 1 段積み、取り消しが 1 文字だけ戻す | C | W5 | `FR-006` ／ `FR-031` ／ 表 T-027 の `UN-3` ／ 表 T-065 の `IF-9` ／ 表 T-064 の `PI-18` | ⭐ **1 文字打っただけでは命令が出ず、`change` で 1 つ出る試験** | 未裁定 |
| PD-271 | **書き直しが、いま人が触っている欄をどうするか** —— どの行も述べていない | **その欄を持っているあいだパネルの中身を描き直さないのを推す** —— ⭐ 参考実装は `document.activeElement` を避ける。⛔ 表 T-078 は契機のたびフレームを回すので、`replaceChildren` が打鍵の途中の欄を捨てる。⚠️ 代償は、そのあいだ他所の編集がパネルに追随しないこと。⭐ 実装は `focusin` / `focusout` で見ている —— `ScreenSurfaceWiring` が宿主に対して `createElement` しか呼ばないと定めるため | C | W5 | `FR-006` ／ `FR-072` ／ 表 T-078 | ⭐ **欄に焦点があるあいだ、パネルの子が入れ替わらない試験** | 未裁定 |
| PD-272 | **`PR-15`（WBS の親）の候補に何を並べるか** —— 表 T-016 は 選択 とだけ書き、原稿は `wbsParentUid` を整数としか言わない | **自分以外の全タスクの `uid` を文書が持つ順で並べ、先頭に空を置くのを推す** —— ⛔ **子孫を除かない**: `FR-005` の循環の規則は書き込み側（`CM-18`）のものであり、選ばせる側が判定すると同じ規則が 2 か所になる | C | W5 | 表 T-016 の `PR-15` ／ `FR-005` ／ `CM-18` ／ 表 T-058 の `AT-26` | ⭐ **タスクが 3 つあるとき、`PR-15` の候補が 空 ＋ 他の 2 つになる試験** | 未裁定 |
| PD-310 | **追従している線を、ポインタの生の x に描くのか、その x が落ちる日に吸い付けるのか** —— どの行も述べていない | **吸い付けるのを推す**（`dateAtX` → `xFromDay`）—— ⭐ それは `DC-2` のクリックがまさに確定する読みであり、**見えている場所とクリックが書く場所が一致する。**⛔ 吸い付けないと、1 日ぶんの幅だけ離れた場所に線が立ち得る | C | W5 | 表 T-029a の `DC-1` `DC-2` | ⭐ **追従線の x が、その日の左端に一致する試験** | 未裁定 |
| PD-311 | **ポインタが窓の外にあるあいだ、追従している線がどこに立つか** —— どの行も述べていない | **保存されている日付の位置に立てるのを推す** —— ⛔ `DC-7` の MUST NOT は、置いた 2 本を消すまで残すと定めている。**窓から手が出ただけで測定の半分が消えてよい理由が無い** | C | W5 | 表 T-029a の `DC-1` `DC-7` | ⭐ **ポインタを失っても 2 本とも残る試験** | 未裁定 |
| PD-312 | **2 連カーソルが 表 T-020 の重ね順のどこに入るか** —— ⛔ **同表はどちらのカーソルにも行を持たない** | **`CU-1` の基準日線が既に居る層を推す** —— `ZO-1`〜`ZO-3` の上、ラベル・注記・選択の印の下。⭐ 表 T-029 の 2 行を同じ層に置くのは、**読む人が基準日と突き合わせて測るからである** | C | W5 | 表 T-020 ／ 表 T-029 の `CU-1` `CU-2` | ⭐ **カーソルがラベルより後ろに描かれる試験** | 未裁定 |
| PD-313 | **`IC-45` を押したとき、軸がポインタにも `Row Area` の中点にも日を与えられない場合どうするか** | **モードに入らず、何も書かないのを推す。押下は取る**（`MK-10`）—— ⛔ `DC-1` は入る道で 2 つの日付を置くと定め、`IV-13` は半端な対を許さないので、**日付の無いモードという状態が表に存在しない。**⭐ 到達できるのは軸に原点が無いあいだだけで、`BO-1` はそこで描くことを既に禁じている | B | W5 | 表 T-029a の `DC-1` ／ `IV-13` ／ 表 T-077 の `BO-1` ／ 表 T-023 の `MK-10` | ⭐ **軸に原点が無いときに `IC-45` を押しても文書が動かない試験** | 未裁定 |
| PD-314 | **`DC-2` のクリックが、軸の日を持たない点の上で起きたらどうするか** | **何も書かず、追従する側も入れ替えないのを推す。押下は取る** —— ⭐ 3 つの条件は今日そろって到達不能である: `PD-2` は追従中にしか通らず、`DC-1` は追従があるときは必ず対を立てている | B | W5 | 表 T-029a の `DC-2` ／ 表 T-023a の `PD-2` ／ 表 T-023 の `MK-10` | ⭐ **日を持たない点でのクリックが文書も追従側も動かさない試験** | 未裁定 |
| PD-315 | **日付罫線（`FR-089`）を何色で引くか** —— ⛔ **表 T-236 にその行が無い** | **`S-149`（罫の色、「区切りの線」）を推す** —— ⭐ **目盛の帯の中の罫が既に同じ行を取っている**ので、時間軸の区切りが 1 つの色で通る。⛔ `S-165` は行の帯の**縁**であり、横の線である | C | W5 | `FR-089` ／ 表 T-236 の `S-149` | ⭐ **日付罫線が `S-149` で引かれる試験** | 未裁定 |
| PD-316 | **`GR-14` のドラッグを、どちらの命令で書くか** —— 表 T-023d の `GR-14` は「動かす」とだけ書き、列を名指していない | **`CM-51`（`setCommentBoxBodyOffsetPx`）を推す** —— ⭐ **見える振る舞いは既に定まっている**（箱がポインタに追い、離した場所に留まる。表 T-023d の結びと 表 T-028 の `IN-1`）。⛔ **開いているのは「既にある 2 つの命令のどちらが運ぶか」だけである。**⭐ **`CM-50` と `CM-51` はどちらも 表 T-108 に在り、どちらも `edit-annotation.ts` に実装済みで、どちらの読みで書かれた文書も正しく、もう一方の読みでも同じ場所に箱が描かれる** —— ⇒ **移行が要らない** | B | W5 | 表 T-023d の `GR-14` ／ 表 T-108 の `CM-50` `CM-51` ／ `FR-019` | ⭐ **コメントボックスの本体を掴んで離すと、離した先の位置が文書に残る試験** | 未裁定 |
| PD-318 | **`GR-8` のドラッグが、実績期間を持たない中断中のタスクに来たらどうするか** —— `PA-3` は実績期間を要求する | **`CONSUMED_ELSEWHERE` を返す（何も書かない）ことを推す** —— ⭐ **`actualEndPlacement` が同じ理由で既に同じ腕を取っている。**⛔ **表 T-019a のどの行も綴れない状態なので、書くとすれば行を発明することになる。**⚠️ **1 つの純粋な関数の中に閉じており、外から見える振る舞いは「その状態では何も起きない」だけである** | B | W5 | 表 T-023d の `GR-8` ／ 表 T-019 の `PA-3` ／ `FR-044` | ⭐ **実績期間を持たない中断中のタスクで `GR-8` を離しても文書が 1 バイトも変わらない試験** | 未裁定 |
| PD-319 | **行の操作子は、押しても絵が変わらないとき構えるべきか** —— ⛔ **仕様のどの行も操作子を構える条件を述べていない。**表 T-051 の `HF-1`〜`HF-10` は置き場所と各半分が何を**する**かだけを持ち、`HR-6`（隠した行）は操作子について何も言わない | **構えないことを推す。⚠️ 分類 `B` なので前に立つ者が暫定で採り、実装済である（2026-08-27）。⛔ 利用者はまだ見ていないので `未裁定` のままとする** —— ⭐ **`FR-029` の RATIONALE の第 1 文が「無反応だと故障に見える」であり、`CR-271` が同じ行を当てて、書けない操作子をパネルから外したばかりである。**⛔ **書きはするが誰にも見えない操作子は、`D-76`「行パネルの操作子がどれも動かない」を再生産する。**⚠️ **反対の読みも立つ** —— 畳みは `AT-56` に残り、隠しが解けたときに効く。⭐ **覆したときに落ちるのは 1 件**（`tests/unit/uf-63.test.ts` の「does not arm the opening control for a hidden child (HR-6)」） —— ⭐⭐ **裁定済（2026-08-30）** —— **利用者が出荷ビルドの実物を見て「構えない」を追認し、さらに広げた。**CR-307 がそれを仕様へ入れており（版 1.61）、表 T-051 の結びが 「`HF-2` / `HF-3` / `HF-10` / `HF-11` / `HF-12` が対象とするのは、いま描かれている行である（MUST）」 と定めた。⛔ **暫定の時と違い、狭めるのは `HR-6` だけではない** —— `HR-1a` も表示量（`FR-018`）も同じ 「絵に居ない行」である。⭐ **`src/adapter/screen-renderer/row-title-panel.ts` の `@provisional PD-319` の印は外した（同日）** —— `input-command-translator.ts` の `IC-59` の分岐に立っていた印も同じ。 | B | W5 | 表 T-051 の `HF-1` 〜 `HF-3` ／ `HR-6` ／ `FR-029` ／ `AT-56` ／ `AT-57` | ⭐ **子が隠れている行で、開く操作子も閉じる操作子も構えない試験** | 裁定済 |
| PD-323 | **表示言語を、面の根の要素の `lang` だけに書くか、頁自身の `<html lang>` にも書くか** —— `FR-038` は言語の状態が画面全体で 1 つであることを定めるが、その状態を環境へどう名乗るかはどの行も述べていない。⚠️ `docs/reference/wcag` は 3.1.1（頁の言語）と 3.1.2（部分の言語）を別の規則として持ち、どちらが本件かを仕様が選んでいない | **面の根の要素にだけ書き、`index.html` の `lang="ja"` は動かさない。** ⭐ 根拠は `ScreenSurfaceWiring` が宿主に対して `createElement` しか呼ばないと約束していることと、この unit が描く入力欄がすべてその根の下に在ること —— 1 か所書けば 表 T-016 の日付欄が開く暦にも届く。⚠️ 頁自身へ届けるには周回から shell への 2 本目の道（`showPointerShape` の対）が要り、それは別の巡である | C | W5 | `FR-038` ／ `S-99` ／ 表 T-016 の入力の型 ／ `NFR-002` | ⭐ **面の根の `lang` が `ScreenView.language` と一致することを読む試験**（`uf-71`）。⚠️ **宿主が描く暦の中の語は読まない** —— あれは環境の語である | 未裁定 |
| PD-325 | **自動保存の刻を画面へ刷る綴りと時間帯** —— `FR-061` は「保存済み（時刻を併記）」としか定めず、時間帯も綴りもどの行も持たない。⚠️ 文書が持つ `AT-129` は `ISO 8601`・UTC・秒までで、それは保存する形であって画面の形ではない ✅⭐ **2026-08-30、`FR-101` が向きを定めた**（CR-317、版 1.71）—— 「**画面に出す時刻は、読む人のローカル時刻とすること（MUST）**」と「**字の大きさは 表 T-206 の `S-210` が定める係数で決めること（MUST）**」。⛔ **本行は 未裁定 のままである** —— **利用者の 2026-08-27 の指示が定めたのは向き（ローカル・2 段小さく）であって、綴りそのものでも 0.75 という数でもない。**⭐ **`S-210` は 🔎 のまま置いた。** | **読む人の機のローカル時刻とし、`YYYY-MM-DD HH:MM:SS` と綴る**（`AT-129` の形から `T` を空白へ開き、`Z` を落とす）。⭐ 根拠は 3 つ —— ①語を 1 つも刷らないので `FR-038` の「辞書は 1 つ」に触れない、②両言語で同じ文字列になる、③`Z` は「これは UTC である」という主張であり、変換したあとは偽になる。⚠️ 別案は同じ綴りに `+09:00` を付けること —— ローカルだと明言できるが、利用者が大きすぎると言った行に 6 文字増える。⛔ 継ぎ目は動かない —— `AutosaveStatus.at`（`IF-9`）は `AT-129` の綴りのままである | C | W5 | `FR-061` ／ `AT-129` ／ `FR-046` ／ 表 T-065 の `IF-9` | ⭐ **`Autosave Status` の文字列が `AT-129` の綴りと異なり、`Z` を含まないことを読む試験**（`uf-71`）。⚠️ **時間帯の差や区切り文字は読まない** —— どちらも未決である | 未裁定 |
| PD-326 | **`Autosave Status` の文字の大きさの係数** —— 表 T-206 は `App Header` にもその中のどの部にも文字の大きさの行を持たない（実測: 文字の大きさの係数を持つ行は `S-197` と `S-198` の 2 行だけで、どちらも `Properties Panel` のものである）。`S-116` は帯の高さの上限であって文字ではない ✅⭐ **2026-08-30、`FR-101` が向きを定めた**（CR-317、版 1.71）—— 「**画面に出す時刻は、読む人のローカル時刻とすること（MUST）**」と「**字の大きさは 表 T-206 の `S-210` が定める係数で決めること（MUST）**」。⛔ **本行は 未裁定 のままである** —— **利用者の 2026-08-27 の指示が定めたのは向き（ローカル・2 段小さく）であって、綴りそのものでも 0.75 という数でもない。**⭐ **`S-210` は 🔎 のまま置いた。** | **`0.75` とし、`font-size:0.75em` として宿主が与える地の文字の大きさに掛ける。** ⭐ 数の根拠は原稿が持つ唯一の大きさの梯子 —— 表 T-215 の `S-123`（L = 16）と `S-121`（S = 12）で、2 段下は 12/16 である。⚠️ **借りているのは梯子の比であって値ではない** —— `FR-006` が `fontScaleSizes` を掛ける相手から外したのと同じ理由で、ヘッダーは日程表の枠であって日程表の一部ではない。⛔ **px にしてはならない**（`NFR-007` が運ぶ WCAG 2.1 の 1.4.4）。⛔ **測って決めた値ではない** | C | W5 | `FR-061` ／ 表 T-206（新行が要る）／ `S-197` ／ `S-198` ／ `S-116` ／ 表 T-215 の `S-121` `S-123` ／ `NFR-007` | ⭐ **`Autosave Status` の文字が同じ帯の `Document Title` より小さく、宿主の地の大きさを変えると両方が動くことを読む試験**（`uf-71`）。⛔ **係数そのものは、表 T-206 に行ができるまで読めない** | 未裁定 |
| PD-327 | **プロパティパネルの閉じる入口（`[x]`）が、パネルのどこに立つか** —— ⛔ **入口の矩形を持つ表が 1 つも無い**（`PD-141`）。`FR-072` が見出しの行を禁じたので、以前の「見出しの隣」という答えは消えた | **最初の項目の行の右端に置くことを推す** —— ⭐ **利用者の指示 2026-08-27（`D-57`）が「`name` と同じ高さに `[x]` を持ってこい」と逐語で述べている。**⛔ **座標は発明しない** —— `margin-left:auto` で「左に残っただけ取る」と述べるので、`S-80` をどこまで引いても右端である。⚠️ **項目が 1 つも無いときは単独で立つ** —— 閉じられない面のほうが悪い | C | W5 | `FR-006` ／ `FR-029` ／ `FR-072` ／ 表 T-109 ／ 表 T-103 ／ `PD-141` ／ `S-186` 〜 `S-198` | ⭐ **パネルの最初の項目の行の中に入口が居ることを読む試験**（`uf-71`）。⛔ **座標は読まない** | 未裁定 |
| PD-337 | **`Properties Panel` が `IN-4` の梯子のどの段に立つか** —— ⛔ **どの行も述べていない。**`S-99g` は 面 を「画面の上に重ねて開き」と定めるが、**このパネルは重ならない** —— `FR-052` がその幅を `Schedule Canvas` から**引く**からである | **「開いている面」の下、「進行中のドラッグ」の上に置くことを推す** —— ⛔ **ドラッグより上に置くと `IN-1` の `Esc` による中断へ手が届かなくなる。**⭐ **重ならない面である以上、重なる面より後に消えるのが筋である** | C | W5 | 表 T-028 の `IN-4` ／ `IN-4a` ／ `S-99g` ／ `FR-052` ／ `FR-006` | ⭐ **開いた面とパネルが同時に在るとき、`Esc` が先に面を閉じることを読む試験** | 未裁定 |
| PD-338 | **パネルを閉じたことを、どこが保つか** —— ⛔ **表 T-206 にも `S-80` にも行が無い。**⚠️ **変更履歴 1.08 が `S-171` について「文書が保つ値にはしない」と記録しており、閉じたことも文書には書かない | **周回が保つ一時の値とすることを推す** —— ⭐ **`S-80` は幅であって「閉じたこと」ではない。**⭐ **閉じているあいだパネルは幅を取らず、その場所は `Row Area` へ渡る**（`FR-052`）。⚠️ **`S-80` へ書き戻さないので、人がドラッグした幅は戻ってきたときそのまま残る** | C | W5 | `FR-052` ／ `FR-006` ／ `S-80` ／ 表 T-206 | ⭐ **閉じて開き直しても、人が引いた幅が変わらないことを読む試験** | 未裁定 |
| PD-339 | ✅⭐⭐ **CR-304 が答えた（2026-08-30、利用者の指示と裁定）—— 答えは「戻ってこない」。**出す入口は 表 T-023 の `MK-13`（ダブルクリック）と `IC-17` の 2 つであり、**選び直すことは入口ではない**（`FR-072` に「表 T-023c の選択が動いたことだけを理由に、パネルを出し始めてはならない（MUST NOT）」を足した）。⛔⛔ **これは右の「推奨」欄を覆している** —— 同欄は「`FR-006` の『選ぶ』が連れ戻すことを推す」と述べていたが、利用者の逐語は「ちょっと位置をずらしたいだけなのに、プロパティーパネルが開くのは鬱陶しい」であった。⭐ **推奨は残す** —— **覆された推奨は、次に読む者が同じ道を検討し直さないための記録である。**⚠️ **`IC-17` を立っていないパネルに押したときは触れていない** —— あちらは本行がついでに挙げていた別の問いであり、**まだ未裁定である。**もとの問い ＝ **閉じたパネルが、次の選択で戻ってくるか** —— ⛔ **`FR-006` と 表 T-023 の `MK-13` が逆を向いており、`FR-029` の MUST NOT が両方を入口にすることを禁じている。**あわせて **`IC-17` を、立っていないパネルに押したとき何が起きるかもどの行も述べていない** | **`FR-006` の「選ぶ」が連れ戻すことを推す** —— ⭐ **それが持ち上げた道であり、選び直しても戻らないなら戻る道が無くなる。**⭐ **`IC-17` は押せばパネルを連れ戻し、巡り自体は触らない** ので「直前の選択物」に着地する | C | W5 | `FR-006` ／ `FR-072` ／ 表 T-023 の `MK-13` ／ `FR-029` ／ `IC-17` | ⭐ **閉じたあと別の対象を選ぶとパネルが戻ることを読む試験** | 裁定済 |
| PD-340 | **有効になっている入口を、何の色で塗るか** —— ⛔ **`FR-029` は「操作できるものが操作できるように見えること」を求めるが、**入っている状態**を色で示せとはどの行も述べていない。**表 T-236 にも入口の押下を塗る行が無い | **`S-183`（構えの色）を借りることを推す** —— ⭐ **利用者の逐語（`D-71`）は「アイコン自体を緑で塗れ。例: イナズマ線が有効ならイナズマ線アイコン全体が緑」であり、色の名を挙げていない。**⭐ **`S-183` は既にパレットで「いま構えている」を示しており、同じ画面で 2 つ目の強調色を持ち込まない。**⛔ **塗るのは `color:` である** —— 図 F-019 の図形は `currentColor` を使うので、**グリフ全体がその色になる**（利用者が求めたのはそれである）。⚠️ **`FR-029` が薄く描く入口には当てない** —— 押されているが使えない入口が濃くなると、薄さの意味が消える | C | W5 | `FR-029` ／ 表 T-109 ／ 表 T-236 ／ `S-183` ／ `S-185` | ⭐ **入っている入口のグリフが `S-183` の色で塗られ、薄い入口は塗られないことを読む試験**（`uf-71`）| 未裁定 |
| PD-341 | **ガイドカーソル（`CU-3`）の線の色と太さ** —— ⛔ **表 T-236 にガイドカーソルの色の行が無く**（実測: カーソルに触れる行は `S-163` 基準日線・`S-195` 2 連カーソルの 2 行だけ）、**表 T-206 にも太さの行が無い**（実測: `S-194` は 2 連カーソルのものである） | **色は `S-148`（控えめな文字の色、「副次」）、太さは 1 を推す** —— ⭐ **`S-163` でも `S-195` でもないことのほうが根拠である**: `FR-048` の結びが「同じ見た目なので、どちらが出ているかを画面上で区別できるようにすること（MUST）」と定めており、日付を持たない補助線が、日付を持つ 2 本と同じ色で出てはならない。⭐ 太さの 1 は基準日線が既に立っている値である（`SL-8` の倍率を掛ける前の、行を持たない太さ）。⛔ **測って決めた値ではない** | C | W5 | 表 T-029 の `CU-3` / `S-66` / 表 T-236 の `S-148` `S-163` `S-195` / 表 T-206 の `S-194` | ⭐ **十字のモードで、描かれた 2 本の色が `S-163` とも `S-195` とも異なる試験** | 未裁定 |
| PD-342 | **ガイドカーソルを、どの領域に、どの層に描くか。ポインタがその領域の外に在るあいだどうするか** —— ⛔ **どの行も述べていない。**表 T-020 の重ね順はカーソルの行を 1 つも持たず（`PD-312`）、`CU-3` は「ポインタに追従する補助線」としか書かない | **`Row Area` の中だけに引き、ポインタがその外に在るあいだは 1 本も描かないことを推す** —— ⭐ **`CU-1` と `CU-2` が走る 2 つの端と同じである**（どちらも `Row Area` の上端と下端）。層も同じ（`PD-312` が置いた層）。⛔ **ルーラを横切らせない** —— 横切らせる行が無く、ルーラは最後に全面へ描かれる。⚠ **ポインタが日程の上に無いのに日程を貫く線を引くと、目を、目が居ない場所へ誘導する** | C | W5 | 表 T-029 の `CU-3` / 表 T-020 / 表 T-077 の `BO-1` / `PD-312` | ⭐ **ポインタを `Row Area` の外（ルーラ・パネル）へ動かすと線が 0 本になり、中へ戻すと出る試験。線の両端が `Row Area` の上端・下端に一致する試験** | 未裁定 |
| PD-343 | **`'double-vertical'`（縦 2 本）の 2 本がどこに立つか** —— ⛔ **間隔も位置もどの行も持たない**（実測: 表 T-029・表 T-202・表 T-206・表 T-236 のいずれにも行が無い）。⛔ **さらに `FR-048` が「同じ見た目なので、どちらが出ているかを画面上で区別できるようにすること（MUST）」と求めるのに、区別する手段もどの行も持たない** | **描かない（実装済 2026-08-29）** —— ⛔ **間隔と区別の印の 2 つを同時に発明することになり、規則 06 の分類 `H`（暫定の上に暫定を積む）に当たる。**⭐ **なし・十字・縦 1 本の 3 モードは動く**ので、利用者が見て決められる。⚠ **裁定が要る**: 間隔（画面上の px か、日の幅か、その倍数か）と、`CU-2` の 2 本と見分ける手立て | C | W5 | 表 T-029 の `CU-3` と `CU-2` / `FR-048` の結び / `S-66` | ⭐ **`'double-vertical'` を選んでもポインタを動かしても線が 1 本も出ない試験**（裁定が下りたら 2 本出る試験に入れ替わる） | 未裁定 |
| PD-344 | **`DC-3`（測る）の日数を、どこに、何の単位で、どの語で出すか** —— ⛔ **表 T-029a の `DC-3` は「2 本の間の日数を示す」の 1 行しか持たない。**⚠️ **実測: 置き場の行が無い**（表 T-020 の重ね順にカーソルの行は 1 つも無く、表 T-021 の占有幅にも `OC-8`（`FR-047` の遅れの日数）に当たる行が `DC-3` には無い）、**単位の行が無い**（`FR-047` は 表 T-021b で起点・終点・稼働日を定めるが `DC-3` は何も定めない。`FD-6` は暦日を MUST で選んでおり、二者は仕様の中で割れている）、**語の行が無い**（`_source/display-words.json` に日数を刷る語が無く、`FR-038` は画面に刷る語を 1 つの辞書に持つことを MUST とする）| **実装しないことを推す** —— ⛔ **置き場・単位・語の 3 つを同時に発明することになり、分類 `H` に当たる。**⭐ **`FR-047` の理由書が同じ危険を自ら書いている**: 「起点と単位を定めないと、同じタスクでも実装ごとに違う日数が出る」。⚠️ **裁定が要るのは 3 つ**: ①どこに出すか（`Row Area` の中か、ルーラか、プロパティパネルか）②暦日か稼働日か ③辞書に足す語 | H | W5 | 表 T-029a の `DC-3` ／ `CU-2` ／ `FR-047` の 表 T-021b ／ 表 T-012a の `FD-6` ／ `FR-038` ／ `_source/display-words.json` | ⭐ **2 本を置いたときに日数が 1 つ出て、その値が定めた単位で数えられていることを読む試験** | 未裁定 |
| PD-345 | **`DC-7`（消す）の入口を、表 T-109 のどの行として、どの図形で置くか** —— ⛔ **`DC-7` は「置いた 2 本を消す入口を、モードを出る入口とは別に置くこと（MUST）」と定めるが、表 T-109 に行が無い**（実測: 全 74 行のうちカーソルに触れるのは `IC-44` 基準日・`IC-45` 2 連カーソルを置く・`IC-46`〜`IC-49` ガイドカーソルの 6 行だけで、消す入口はどれでもない）。⛔ **表 T-036 の鍵盤にも無い**（実測: `SK-20` は基準日線の出す/消すを `IC-44` に載せるが、2 連カーソルの行は無い）。⚠️ **`clearDualCursor`（`CM-61`）は `edit-document-settings.ts` に書かれており、押す道だけが無い** | **実装しないことを推す** —— ⛔ **75 個目の図形を起こすことになり、表 T-026 の `RC-13` が「本仕様書の中で発明しない —— 図形の決定は利用者の裁定である」と定めている。**⚠️ **払う代償**: 表 T-076 の `EP-6` が値の在るかぎり書き出しに 2 本を描き続け、`DC-7` 自身が警告するとおり**消す経路が存在しない**。⭐ **裁定が要るのは 2 つ**: ①表 T-109 に行を足すか（足すなら 面・群・図形）②`IC-44` のように 1 つの入口へ置く/消すをまとめるか、`DC-7` の MUST どおり別に置くか | H | W5 | 表 T-029a の `DC-7` ／ 表 T-109 ／ 図 F-019 ／ 表 T-026 の `RC-13` ／ 表 T-076 の `EP-6` ／ `CM-61` | ⭐ **2 本を置いたあとその入口を押すと `dualCursor` が `null` へ戻り、モードは出ないままであることを読む試験** | 未裁定 |
| PD-346 | **`FR-059` の「先頭 1 名と残りの人数」を、1 つのラベルにどう並べるか** —— ⛔ **要求は出すもの（先頭 1 名と残りの人数）だけを定め、2 つをつなぐ書き方をどの行も持たない**（実測: `FR-059`・表 T-225・表 T-038 の `OC-2`・表 T-013 の前書き・`_source/display-words.json` のいずれにも無い）| **半角空白 1 つと `+` と数字で書く**（`Seated +2`）。⭐ 根拠は、`FR-038` が訳す対象を「語」としており、`+` と数字は語でないこと —— 表 T-013 の前書きが省略記号 `…` を「どの言語でも同じ印」として扱っているのと同じ立場である。⛔ **`ほか 2 名` のような語を書かない** —— 語を発明すると `_source/display-words.json` の外に確定名が増える | C | W6 | `FR-059` ／ 表 T-225 の `AS-2` ／ 表 T-038 の `OC-2` ／ `FR-038` | ⭐ **2 人以上が就いたタスクの担当ラベルが `先頭 1 名` と残りの人数を出す試験**（まだ無い）| 未裁定 |
| PD-347 | **`OC-2` の 2 つのラベルの、並び順・2 つのあいだの隙間・縦の位置** —— ⛔ **表 T-038 の `OC-2` は 2 つに 1 つの欄しか与えず「左（バーの外側へ張り出す）」としか書かない。**⛔ **表 T-221 にも表 T-012 の「名称ラベルの縦位置」の欄にも、この 2 つの行は無い**（実測: 表 T-221 の `LF-1`〜`LF-13`・表 T-012・表 T-013・表 T-201 のいずれにも無い）| **バーに近いほうを担当ラベル、その外側を完了率ラベルとし、隙間はどちらも `labelGap`（`S-32`）、縦はどちらも予定バーの中心に 1 行ぶん**とする。⭐ 順序の根拠は `OC-2` と `UC-003` 手順 4 がどちらも「担当ラベルと完了率ラベル」の順で並べていること。⭐ 隙間の根拠は `S-135` の備考が「`labelGap`（`S-32`）は形状の外へ出すラベル用」と明記しており、`OC-2` はまさに形状の外であること。⭐ 縦の根拠は、バーの外側に物を置く唯一の行である表 T-221 の `LF-11` が「縦は予定バーの中心」と定めており、そこが置く進捗マーカーは `OC-3`、すなわち `OC-2` の隣の行であること。⛔ **新しい設定値を 1 つも作っていない** | C | W6 | 表 T-038 の `OC-2` ／ 表 T-221 の `LF-11` ／ `S-32` ／ `FR-059` ／ `FR-090` ／ 表 T-023d の `GR-11` | ⭐ **担当ラベルの右端がバーの左端から `labelGap` だけ離れている試験**（`t-023d-double-click-only-rows` の `GR-11` の枠が既にこの位置を前提にしている）| 未裁定 |
| PD-348 | **行の 3 つの操作子が、行の右端からどれだけの間隔で並ぶか** —— ⛔ **表 T-051 の `HF-4` は「行見出しパネルの右端に揃えること（MUST）」としか定めず、3 つのあいだの間隔を持つ行が 表 T-206 にも 表 T-051 にも無い**（実測 0 件）| **右端から 0.25`em` に 1 つ目を置き、以降 1.25`em` 刻み。**⭐ 根拠は `em` であること —— `FR-039` が読む人の字の大きさをパネルへ運ぶので、px で持つと字を大きくした瞬間に操作子だけ取り残される| C | W5 | `HF-4` `HF-5` `HF-6` `S-140` `FR-085` `FR-039` | ⭐ **3 つの操作子の x が右端から等間隔に並ぶ試験**（`uf-71`）| 未裁定 |
| PD-349 | **入力中の `Ctrl`＋`A` をブラウザへ渡すか** —— ⛔ **表 T-028 の `IN-5a` は例外を `SK-4` / `SK-5`（`Ctrl+C` / `Ctrl+V`）の 2 つだけとしており、`SK-2`（`Ctrl+A`）の行がどの表にも無い**（実測: `IN-5a`・表 T-023 の `MK-10`・表 T-023c・表 T-036 の `SK-2` のいずれにも無し）。⚠️ **その結果、入力欄で `Ctrl`＋`A` は何も起きない** —— 訳者がブラウザの既定動作を止め（`MK-10`）、`selectionFromInput` は入力中を避ける | **実装しないことを推す** —— ⛔ **例外を 1 つ発明することになり、これは受理と拒否の境界（分類 `F`）である。**⭐ **裁定が要るのは 1 つ**: `IN-5a` の例外に `SK-2` を加えるか。⭐ **加えるなら、直すのは 1 行** —— `input-command-translator.ts` の `commandFromKey` が `IN-5a` を読む枝で、`SK-4` / `SK-5` と並べて `SK-2` を返さないだけである| F | W5 | 表 T-028 の `IN-5a` ／ 表 T-023 の `MK-10` ／ 表 T-036 の `SK-2` ／ 表 T-023c | ⭐ **入力欄で `Ctrl`＋`A` を送ると欄の全文が選ばれる試験**（まだ無い）| 未裁定 |
| PD-350 | **対話欄（`Dialogue Field`）の打ちかけを、`IN-5a` と `WS-2` が数えるか** —— ⛔ **表 T-035 の `AG-9` は「プロパティパネルで入力中など」と名指し、`IN-5a` が数えるのは名称・担当者名・行名・文書名・注記の本文の 5 つで、どれも発話ではない**（実測: 表 T-035・表 T-028・表 T-067 のいずれにも発話の行が無し）。⚠️ **その結果、対話欄では単文字キーが今も奉られる**（`P` を打つとプロパティパネルが開く）| **実装しないことを推す** —— ⛔ **`IN-5a` の適用範囲を広げるのは受理と拒否の境界（分類 `F`）であり、`AG-11`（発話の確定）と `SK-19`（`Enter`）の関係を同時に決めることになる。**⭐ **裁定が要るのは 2 つ**: ①対話欄の打ちかけを「確定していない文字入力」と数えるか ②数えるなら `Esc` の第 1 階層が発話をどこへ戻すか | F | W5 | 表 T-035 の `AG-9` ／ `AG-11` ／ 表 T-028 の `IN-5a` ・`IN-4` ／ 表 T-036 の `SK-19` | ⭐ **対話欄で `P` を打ってもプロパティパネルが開かない試験**（まだ無い）| 未裁定 |
| PD-351 | **`FR-013` の「濃くする」の度合いと、その単位（1 つのダミーか、そのタスクのダミー全体か）** —— ⛔ **`FR-013` の MUST は「薄く描き、ポインタが乗っているあいだだけ濃くすること」と定め、薄さには `S-131` を名指すが、濃いほうの値を持つ行がどの表にも無い**（実測: `_assets/tbl-settings.md` の 表 T-206 と 表 T-201〜T-205、表 T-051 の `HF-6`、`FR-043` のいずれにも 0 件）。⛔ **「乗っている」の単位を定める行も無い** —— 表 T-023d は `GR-9` / `GR-17` / `GR-18` を別々の行として持つが、描くほうは 1 つの群である（重なったとき 2 枚の透過が第 3 の値に合成されるので、`S-131` が何の濃さでもなくなる）| **濃さは「薄さを外した姿」とし、新しい値を置かない**（`opacity` を 1 にする）。⭐ 根拠は `S-131` の説明欄が「実績のダミーと未着手マーカーの濃さ」と述べており、要求もそれを唯一の値として名指すこと —— **足すのではなく外すなら、値は 1 つのままである。**⭐ 表 T-051 の `HF-6` の先例も同じ形（描かない → 描く の 2 値であって、2 段の濃さではない）。**単位はタスクのダミー全体**とし、`GR-9` / `GR-17` / `GR-18` のどれに乗っても同じ群が濃くなる | C | W4 | `FR-013` ／ `FR-043` ／ 表 T-023d の `GR-7` / `GR-9` / `GR-17` / `GR-18` ／ `S-131` | ⭐ **ポインタを乗せたとき `opacity` が `S-131` から変わる試験**（`svgFromSchedule` の `hovered`）| 未裁定 |
| PD-352 | **同じプロパティパネルの別の欄を押したとき、焦点を欄から外すか** —— ⛔ **表 T-028 の `IN-6` は「欄の外を押したら確定する」とだけ定め、焦点をどこへ渡すかを定めていない**（利用者の裁定 2026-08-30 の射程外）。⛔ **その押下も割当を持つので `preventDefault` が働き、宿主の焦点移動は止まっている** ⇒ ここで焦点を外すと、**どこにも焦点の無い状態**になる | **確定はするが、焦点は外さない。**⭐ 根拠は `IN-6` が閉じようとしたもの（`isTextEntryUnsettled` が真のまま残ること）が、このとき残らないこと —— 押した先の欄が焦点を得れば旗は立ったままでよく、得られなくても打った値は届いている。⛔ **「どこにも焦点が無い」を選ぶと、次の 1 文字がどこにも入らない** | C | W4 | 表 T-028 の `IN-6` ／ `IN-5a` ／ 表 T-067 の `WS-2` ／ `FR-006` | ⭐ **欄 A に打って欄 B を押したとき、A の値が文書へ届く試験** | 未裁定 |
| PD-353 | **行を削除する入口（`IC-82`）を、ポインタが乗っているあいだだけ描くか** —— ⛔ **表 T-051 の `HF-6` は「操作子は、その行の名前にポインタが乗っているあいだだけ描くこと（MUST）」と定めるが、同表は「行ごとの**折り畳み**の操作面」であり、削除は折り畳みではない**（実測: `HF-1`〜`HF-12` のどの行も削除を持たない）| **乗っているあいだだけ描く**（`HF-6` と同じ扱い）。⭐ 根拠は `S-140` = 0 であること —— **確保する場所が 0 なので、常に描くと削除の図形が行の名前の上に永久に重なる。**⭐ **`IC-60`（ピン止め、`FR-098`）も同じ扱いを受けており、あちらも 表 T-051 の行ではない** —— 先例が既にある | C | W6 | 表 T-051 の `HF-6` / `HF-4` ／ `S-140` ／ `FR-085` ／ `FR-032` ／ 表 T-109 の `IC-82` | ⭐ **行にポインタが乗っていないとき `IC-82` が見えない試験**（`uf-71`）| 未裁定 |
| PD-360 | **`FR-013` の「ポインタが乗っている」が、描いた図形の上のことか、表 T-023d が点を与えた行のことか** —— ⛔ **`FR-013` の MUST は「作法は表 T-051 の `HF-6` と同じである」と述べ、`HF-6` は「その行の名前にポインタが乗っているあいだだけ描く」** —— **条件は図形の上に在ることであって、優先順位は入っていない。**⛔ **一方いまの実装は 表 T-023d の勝った行で決めており、`GR-3` / `GR-4`（予定の端点）が `GR-9` / `GR-17` より上に在るので、1 日の幅が `S-90` の 6px を割る倍率ではダミーが決して濃くならない**（実測 2026-08-30、全体表示 `F`: 画面上のダミー 21 点すべてが `ew-resize` を返し **0/21** が濃くならず、うち **17/21** は予定バーの端から 2.3px / 5.2px。横に 8 段ズームすると **2/2** が濃くなる）| **描いた図形の上で決める**（本巡で当てた）。⭐ 根拠は `FR-013` が名指す 作法 が `HF-6` であり、`HF-6` の条件が幾何であること。⭐ **掴みの側は 1 行も動かしていない** —— `MK-9a` は 優先順位 を「掴む対象が重なった」ときのものと限っており、描く側の規則ではない。⚠️ **未着手のマーカーは 表 T-023d の勝った行のまま残した** —— `GR-7` はマーカーの正方形に届くどの行よりも上に在り、実測で **21/21** が濃くなるので、変えると測っていない変更になる | C | W4 | `FR-013` ／ 表 T-051 の `HF-6` ／ 表 T-023d（`GR-3` / `GR-4` / `GR-7` / `GR-9` / `GR-17` / `GR-18` と前文の MUST） ／ 表 T-023 の `MK-9a` ／ `S-90` ／ `S-93` ／ `S-180` ／ `FR-055` | ⭐ **全体表示の倍率でダミーにポインタを乗せると `opacity` が `S-131` から外れる試験**（`svgFromSchedule` の `pointer`）| 未裁定 |
| PD-361 | **低い倍率で `GR-3` / `GR-4` が `GR-9` / `GR-17` を飲み込むとき、どちらを掴ませるか** —— ⛔ **表 T-023d は「上の行ほど優先すること（MUST）」と定め、`GR-3` / `GR-4` を `GR-9` / `GR-17` より上に印刷している。**⛔ **`FR-043` は「1 日ずらすことで、左が `GR-3`、右が `GR-9` として別々に掴める」を理由として述べるが、1 日の幅が `S-90` の 6px を割る倍率でその理由が成り立たないことを述べた行がどの表にも無い**（実測: 表 T-023d・`FR-043`・`FR-055`・表 T-206 の `S-90` / `S-93` のいずれにも 0 件）。⚠️ **`FR-055` の全体表示は起動時の倍率であり、そこでは実績を掴んで入れる経路が丸ごと無い** | ⛔ **推さない。発明になる。**⭐ **裁定が要るのは 1 つ**: 重なりが解けない倍率で `GR-9` / `GR-17` を `GR-3` / `GR-4` より上へ置くか、それとも掴めないことを受け入れるか。⚠️ **前者を採るなら 表 T-023d の印刷順そのものを動かすことになる**（`GR-9` の「重なったら開始点が勝つ」と正面から当たる）| F | W4 | 表 T-023d（前文の MUST と `GR-3` / `GR-4` / `GR-9` / `GR-17`）／ `FR-043` ／ `FR-055` ／ `S-90` ／ `S-93` | ⭐ **全体表示の倍率でダミーを掴むと実績が置かれる試験**（まだ無い）| 未裁定 |
| PD-362 | **`GR-5`（実績の開始点）を掴んだとき、実績の終了日を据え置くか** —— ⛔ **表 T-023d の `GR-5` は「`actualStart` を変える」としか述べず、`actualDuration` をどうするかを持たない。**⛔ **`FR-011` の MUST NOT（「人が置いていない限り両端を動かさない」）は `GRS` が自ら動かすことを禁じたもので（RATIONALE が挙げる例は完了と基準日である）、掴みの片端に掛かるとは書いていない** ⇒ **`actualDuration` を据え置くと実績バー全体が平行移動する**（実測 2026-08-30: 左端を 72px 引くと左 500→572・右 2204→2282 で両端が動いた）。⚠️ **利用者の逐語は「正しくは開始日のみ移動だ」**（2026-08-29）| ⛔ **推さない。裁定が要る。**⭐ **選択肢は 2 つ**: ①`GR-5` に「終了日は据え置き、`actualDuration` を置き直す」を足す（`GR-6` が「置いた日付から稼働日数を算出する」と対になる）②いまの平行移動を正とし、`GR-5` にそう書く。⚠️ **①を採ると `GR-5` の 操作 の欄が 2 列を変えることになる** —— 表の他の行はどれも 1 列である | F | W4 | 表 T-023d の `GR-5` / `GR-6` / `GR-15` ／ `FR-011` ／ 表 T-019 の `PA-2`〜`PA-5` ／ `FR-054` | ⭐ **`GR-5` を引いても実績の右端が動かない試験**（まだ無い）| 未裁定 |
| PD-363 | **握っているあいだ、パン（`PD-1`）と範囲選択（`PD-5`）の絵を描くか** —— ⛔ **表 T-023d の結びは「操作の欄が『動かす』『変える』『ずらす』と述べる行は必ず追従する（MUST）。追従しない行を残してはならない（MUST NOT）」と定めるが、その射程は 表 T-023d の行だけであり、パンと範囲選択は 表 T-023a の `PD-1` / `PD-5` で掴み領域を持たない。**⛔ **範囲選択の矩形を描く行はどの表にも無い**（実測: 表 T-020 の重ね順に矩形の行が 0 件、表 T-236 に色が 0 件、`SL-3` は取る規則だけ）。⛔ **パンが握っているあいだ追従することを述べる行も無い**（「パンは等倍とすること（MUST）」は距離の規則である）（実測 2026-08-30: `Ctrl`＋ドラッグで 6 回動かしても図形の x は 170 のまま、離した時点で −70 へ跳ぶ。範囲選択は握っているあいだ `svg` の要素数が 1 つも増えない。⭐ **離した時点の選択そのものは効く** —— 全体表示で破線枠が 8→44）| ⛔ **推さない。発明になる**（矩形の図形・色・重ね順を 3 つとも起こすことになり、図形の決定は 表 T-026 の `RC-13` が利用者の裁定と定める）。⭐ **裁定が要るのは 2 つ**: ①`PD-1` を「握っているあいだ追従する」に足すか ②`PD-5` の矩形を描く行を起こすか | F | W5 | 表 T-023a の `PD-1` / `PD-5` ／ 表 T-023d の結び ／ 表 T-023c の `SL-3` ／ 表 T-020 ／ 表 T-028 の `IN-1` ／ 表 T-026 の `RC-13` | ⭐ **握っているあいだ絵が動く試験**（まだ無い）| 未裁定 |
| PD-364 | **削除された対象が選択に残ることを、どこで断つか** —— ⛔ **選択は文書の外の値であり（表 T-027 の `UN-9`）、`selectionFromInput` は入力からしか動かないので、`SK-3` で消した `Task` の `uid` が選択に残る。**⛔ **消えたものを選択から落とす行がどの表にも無い**（実測: 表 T-023c の `SL-1`〜`SL-8`・`FR-032`・表 T-050・表 T-036 の `SK-2` / `SK-3` のいずれにも 0 件）⇒ **`Ctrl`＋`A` で全消しした直後にパレットの図形を押すと、`SP-3` が死んだ `uid` へ `CM-20` を組み立て、`editTask` が `IV-2`（`no Task with uid`）で拒み、`AG-3` が束ごと落として 表 T-233 の `RS-10` が出る**（実測 2026-08-30、2 回中 2 回。利用者の画像の文言と一致）| ⛔ **推さない。裁定が要る。**⭐ **選択肢は 3 つ**: ①削除の直後に選択を空にする（`SK-3` に足す）②毎フレーム、実在しないものを選択から落とす ③`SP-2` / `SP-3` を「実在するものだけを変える」と読む。⚠️ **③は 表 T-023d の混在選択の扱いと同じ穴に触れる** —— `commandFromArmingEntry` は「どちらが勝つかを述べる行が無い」ことを理由に絞り込みを拒んでいる | F | W5 | 表 T-023c の `SL-1` ／ `FR-032` ／ 表 T-050 ／ 表 T-036 の `SK-2` / `SK-3` ／ `FR-083` の `SP-2` / `SP-3` ／ 表 T-035 の `AG-3` ／ 表 T-233 の `RS-10` ／ `IV-2` | ⭐ **全消しの直後にパレットの図形を押すと構えが変わる試験**（まだ無い）| 未裁定 |
| PD-380 | **確認の面の上のホイールと、その面の中のスクロールバーを、宿主に委ねるか** —— ⛔ **表 T-023 の `MK-10` は逐語で「**本ツールが割り当てた**修飾キーの付いた入力」を主語とし、MUST（画面全体で止める）も MUST NOT（割り当てていない組合せを止めてはならない）も修飾キーを伴う入力だけを述べる。素のホイールは修飾キーを伴わないので、`MK-10` はどちらの側からもこの場面に届かない。**⛔ **`MK-1`（ホイール＝縦スクロール）の射程を面に限る行も無い**（実測: 表 T-023・表 T-023a の注・`FR-016`・`FR-051`・表 T-103 のいずれにも 0 件。`isWheelHere` は `@provisional PD-12` として同じ穴を既に記録している）⇒ **`regionAtPointer` は日程の各部しか知らないので、日程の上に浮いた `Confirmation` の上のホイールが `rowArea` と判定され、`preventDefault` が掛かる。**（実測 2026-08-30、出荷ビルド: 消える `Task` の名前が 9,341 文字のとき、名前の欄は `scrollHeight` 6986・`clientHeight` 928 でスクロール可能だが、ホイール 40 回で `scrollTop` は 0 のまま。スクロールバーを 380px 引いても 0 のまま —— 押下にも割当があるため）⇒ **`FR-032` の「消える `Task` の名前を示すこと（MUST）」が、1 画面に収まらない一覧では満たされない** | ⛔ **推さない。裁定が要る。**⭐ **選択肢は 3 つ**: ①`MK-1` に「面の上では宿主に委ねる」を足す ②`InputContext` に「面が立っているか」を運び `isWheelHere` を偽にする（`frame-loop.ts` が埋める） ③面の中でスクロールが要らない出し方を定める。⛔ **DOM の側で `stopPropagation` する道は塞がっている** —— `tests/unit/uf-71.test.ts` の 2 件（「listens only where IF-9 gives it something to notice」と「asks the host for one listener and no more」）が、この単位が `Dialogue Field` と `Properties Panel` の外に聴き手を置くことと、宿主へ 2 つ目の `addEventListener` を出すことを、どちらも赤で禁じている | F | W4 | 表 T-023 の `MK-1` / `MK-10` ／ 表 T-023a の注 ／ 表 T-037 の `NT-7` ／ `FR-032` ／ 表 T-024a の `OP-4` ／ 表 T-103 の `U-55` ／ `PD-12` | ⭐ **名前が 1 画面に収まらない確認で、ホイールを回すと名前の欄が動く試験**（まだ無い）| 未裁定 |
| PD-390 | **`EZ-6` の説明で、タスク名と 2 つの日付のあいだに何を置くか** —— ⛔ **`EZ-6` が定める区切りは「開始・終了の順に `/` で並べること（MUST）」の 1 つだけであり、名前と日付の並びのあいだを何で区切るかを述べた行がどこにも無い**（実測: `FR-092` の 表 T-040・表 T-028 の `IN-3`・表 T-076 の `EP-15`・表 T-206 のいずれにも 0 件）。⚠️ **同行は「値を持たない項目は、その場所を空けること（MUST）」と定めるので、詰めて畳むことはできない** | **空白 1 つを置く。**⭐ 根拠は、この実装が既に `EZ-2` の説明と割当のあいだに空白 1 つを置いており、その理由として「意味を持つ区切りは語であり、`FR-038` の辞書に無い記号を置けない」と書いていること —— **同じ理由がここにもそのまま効く。**⛔ **`/` を 3 つ目の区切りに流用していない** —— 日付 2 つの区切りとして `EZ-6` が名指ししたものであり、名前との境にも使うと同じ記号が 2 つの意味を持つ | C | W4 | `FR-092` の 表 T-040 の `EZ-6` ／ `FR-002` ／ `FR-038` | ⭐ **タスクの上の説明が「名前 開始日 / 終了日」を返す試験**（`uf-69` の `EZ-6` の件。まだ無い）| 未裁定 |
| PD-391 | **`EZ-6` の説明が画面のどこに立つか。そして、そこに立つとポインタを奪うこと** —— ⛔ **`EZ-6` は出す中身と引き金と消える条件を定めるが、置き場を 1 文字も書いていない**（実測: 表 T-040・表 T-028 の `IN-3`・表 T-076 の `EP-15`・表 T-206 に 0 件）。⚠️ **アイコンの説明は描かれた入口の要素に添えられるが、タスクのバーは `IF-1` を通って日程の絵として上がるので、説明を描く側には添える先の要素が無い。**⛔⛔ **ポインタの点に置くと、その箱自身がポインタの下の要素になる** —— 出荷ビルドで実測: `readScreenPartAt` が `Tooltip` と答え、`grabAtPointer` は「画面の面が答えた点」をすべて退けるので、**下のバーが掴めなくなり、一度動いて消えた説明が二度と戻らなかった**（移動 20px、5 秒待って 0 件） | **ポインタの点に置き、この説明だけポインタを取らない（`pointer-events:none`）。**⭐ 置き場の根拠は 表 T-076 の `EP-15` が「ポインタが止まったことで出るものであり、書き出した時点のポインタの位置に意味が無い」と述べていること。⭐ **ポインタを取らない根拠は `EZ-6` 自身**「ポインタが動いたら消すこと（MUST）」—— **説明の上へポインタを運ぶ動きは、その説明を消す動きでもあるので、`IN-3` の「乗せられること」はこの引き金では届かない。**⚠️ **`IN-3` は全ツールチップに掛かる MUST なので、これは仕様どうしの食い違いであり、裁定が要る。**⛔ **ずらし量を発明していない**（余白の数を持つ行が無い）。⚠️ **残る 2 つの引き金は従来どおり**（描かれた要素に添えられ、点の上には立たない）| C | W4 | `FR-092` の 表 T-040 の `EZ-6` ／ 表 T-076 の `EP-15` ／ 表 T-028 の `IN-3` ／ 表 T-023a の注 | ⭐ **タスクの上の説明がポインタの点に立ち、その下のバーが掴めたままである試験**（まだ無い）| 未裁定 |
| PD-392 | ✅ **裁定（2026-08-30）—— 今のまま。**⭐ 説明は「その入口が何をするものか」を述べるものであり、行えない理由は押下時の通知が担う。**同じことを 2 か所が言わない。** ⭐ **構えていない入口に乗せたとき、説明は何と言うべきか** —— ⛔ **実測（2026-08-30、出荷ビルド）: 構えていない `IC-58` に乗せると「この行の配下をすべて開く」と出る。しないことをすると名乗っている。**⚠️ **利用者の裁定（2026-08-30）は「薄く描け。押下した場合のみ理由を説明するポップアップを出せ」であり、乗せたときの文言には触れていない** | **今のままとする** —— ⭐ `FR-092` の `EZ-2` の説明は**その入口が何をするものか**を述べるものであり、`FR-029` の新しい規則が「行えない理由は押されたときだけ」と定めた以上、乗せたときに理由を混ぜると **同じことを 2 か所が言う。**⚠️ **覆すなら `EZ-2` を書き換えることになり、誘導部分グラフの輪（`EZ-2` ↔ `IN-3`）に入るので 1 度に書く必要がある** | C | W5 | `FR-029` ／ `FR-092` の `EZ-2` ／ 表 T-028 の `IN-3` ／ 表 T-233 の `RS-27` | ⭐ **構えていない入口に乗せて、出た説明の本文を読む試験** | 裁定済 |
| PD-393 | ✅ **裁定（2026-08-30）—— 今のまま。**逐語どおり、対話の欄に焦点があっても`Enter` はパネルを閉じる。 ⭐ **`AI` 対話の欄に焦点があるときの `Enter` が、プロパティパネルを閉じてよいか** —— ⛔ **表 T-036 の `SK-19` は逐語で「焦点が名称の欄の外にあるときも同じである」と書いており（利用者の裁定 2026-08-30）、文字どおりに読むと対話の欄も含む。**⚠️ **利用者はその隅を述べていない** | **逐語どおり閉じる** —— ⭐ 表の結びが除いたのは**面（`U-55` と `S-99g`）が立っているあいだ**だけであり、⚠️ **対話の欄は面ではなく `FR-065` の入力欄である。**⛔ **除きたいなら 表 T-036 の結びに 1 文を足すことになる** | C | W5 | 表 T-036 の `SK-19` ／ 同表の結び ／ `FR-065` ／ `FR-072` ／ 表 T-028 の `IN-5a` | ⭐ **対話の欄に焦点を置いて `Enter` を押し、パネルが閉じるかを読む試験** | 裁定済 |
| PD-394 | ✅ **裁定（2026-08-30）—— 案 A。「1 日単位に四捨五入する」を採り、「ポインタの下の日から求めること」を「ポインタの位置から求めること」へ改める。**⭐ **根拠 3 つ**: ①結びは既に四捨五入と明記しており、落とすほうが情報の削除である ②同じ結びが「掴み点は時間軸の上の『ある日』そのものであり、画素の感度というものが存在しない」と書いており、画素の端で答えが変わる切り捨てはその一文と噛み合わない ③フェードの端を掴むのは「この辺」を指す操作なので、近いほうへ寄せるのが手の意図に近い。⛔ **わざと赤い 1 件はこれで緑になる。** ⛔⛔ **表 T-023d の結びが自分と矛盾している** —— 「**`GR-1` / `GR-2` の日数は、ポインタの下の日から求めること（MUST）**」は**切り捨て**を言い、「**いずれも 1 日単位に四捨五入する**」は**丸め**を言う。⭐ **実測（2026-08-30、出荷ビルド）**: 境目のわずかに手前で製品は**前の日に落ちる**（切り捨てている）。⚠️ **日のちょうど端では、浮動小数の 1 ulp でどちらにも落ちる** | **「1 日単位に四捨五入する」を採り、「ポインタの下の日から求めること」を「ポインタの位置から求めること」に改める** —— ⭐ 何も決めない節を残す書き方は本書のどこにもしていない。⚠️ **逆を採るなら「四捨五入」の 1 文を落とすことになり、端を狙った掴みが常に前の日へ落ちる** | B | W5 | 表 T-023d の結び ／ `FR-011` ／ 表 T-012a の `FD-6` ／ `FR-031` | ⭐ **境目のすぐ手前とすぐ後ろの両方から掴んで、同じ日に落ちることを読む試験**（仕様だけを読む体が既に置いた）。✅ **CR-314（版 1.68）で緑になった** —— **実装は `input-command-translator.ts` の `GR-1` / `GR-2` の枝 1 か所**。⭐ **同ファイル 43 件全緑**、検査 29 本 ALL GREEN | 裁定済 |
| PD-395 | ✅ **裁定（2026-08-30）—— 今のまま。**打たずに `Enter` を 1 度押したら閉じる。⭐ 利用者の逐語が「タスク名の編集が**終わったら** `Enter`」であり、打っていないなら編集は始まっていない。 ⭐ **ダブルクリックしたあと 1 文字も打たずに `Enter` を押したとき、パネルはその 1 度で閉じてよいか** —— ⛔ **実測（2026-08-30、出荷ビルド）: 閉じる。**⚠️ **打ってから押した場合は、利用者の裁定どおり `Enter` 2 度で閉じる。**⭐ **分かれ目は「確定していないその場の編集が 1 つも無いとき」を、焦点が載っているだけの欄が満たすかどうかである** | **今のままでよい** —— ⭐ 利用者の逐語は「タスク名の編集が**終わったら** `Enter`」であり、**打っていないなら編集は始まっていない。**⚠️ **加えて、値が変わっていないのに命令を組むことは 表 T-028 の `IN-6` が禁じている**（台帳 D-152）| C | W5 | 表 T-036 の `SK-19` ／ 表 T-023 の `MK-13` ／ 表 T-028 の `IN-6` ／ `FR-031` | ⭐ **打たずに `Enter` を 1 度押してパネルが閉じるかを読む試験** | 裁定済 |
| PD-396 | ✅⭐⭐ **裁定（2026-08-30）。****①ピンは、留まっているあいだ常時描く**（`HF-6` の例外）。**②色は `S-151`（選択色）。**⛔ 赤は採らない —— この製品で赤は「遅れ／不良」である。**③`×` と `📌` の左右を入れ替える。****④名前の末尾がピンに隠れることは受け入れる**（`FR-085` / `FR-052` が逃げ道を持つ）。⛔ **場所を確保する案は採らない** —— `FR-085` の MUST NOT に触れ、書き出しと打ち切り位置が食い違う。もとの案 ＝ ⭐ **ピン止めされていることを、行を一覧しただけで読めるようにする** —— ⛔ **いまピンの操作子はポインタが乗っているあいだしか描かれない**（表 T-051 の `HF-6` の MUST）ので、**どの行が留まっているかは総当たりでしか分からない。**⚠️ **利用者の案（2026-08-30）**「`×` と `📌` の左右を入れ替える。ピン止め中、ピンのアイコンは常時赤アイコンで表示する」 | ⭐ **入れ替えは賛成** —— ①削除がいちばん端にあり、右から流し込んだポインタが最初に触る ②押す頻度はピンのほうが高く、削除は一度きりである。⛔ **常時「赤」には反対** —— **この製品で赤は「遅れ／不良」である**（`S-154`）。健全な行に赤が点き続けると警告に見える。⭐ **`S-151`（強調の色。選択と現在位置）を推す** —— ピン止めは「この行に注目している」印である。⭐⭐ **色より先に `HF-6` の例外が要る** ——「ピン止めされている行のピンの操作子だけは、ポインタが乗っていなくても描くこと（MUST）」。**これが本体である。** | B | W5 | 表 T-051 の `HF-6` ／ `FR-098` ／ 表 T-236 の `S-151` `S-154` ／ 表 T-109 の `IC-60` `IC-82` | ⭐ **ポインタを乗せずにピン止めされた行のピンが描かれることを読む試験** | 裁定済 |
| PD-397 | ⛔⛔ **行の操作子の並びと振る舞いが、実物で読めない** —— ⭐ **`FR-102` の記録が実測で示した（2026-08-30）**: 利用者は **40 秒のあいだ `IC-58` / `IC-59` / `IC-77` / `IC-60` / `IC-82` / `IC-74` / `IC-78` の上を延々と往復し、押したのは 3 回だけ**であった。⚠️ **利用者の逐語**「そもそもタスクグループの操作子とその挙動が違和感だらけだ。次のセッションの冒頭で再整理させろ」 | ⛔ **未定。次の巡の冒頭で、絵と実測で一枚に整理してから問い直す。**⭐ **同時に片づける問い** —— ①操作子が「効かない」を判定するとき、**画面をスクロールして行が見えなくなったこと**も数えるか（いまは数える。⇒ **行を画面外へ送っただけで上の行の畳む操作子が薄くなる**）②`PD-396`（ピン止めの見え方）③`HF-3` を描かれている葉の行で押したときの理由（`RS-30` は当たらず `RS-27` に落ちる） ✅⭐⭐ **一枚を出し、実測で 4 つの食い違いを名指した（2026-08-30）。**⛔ ①**同じ図形が 2 つの意味を持つ** —— 山 2 つは、行では `IC-59`「この行自身を畳む」、パネルの頭では `IC-78`「すべての行を畳む」である。⛔ ②**開く／畳むの左右が逆** —— 行は x 70 → 90 で 開く → 畳む、頭は x 120 → 140 で 畳む → 開く。⛔ ③**畳む図形が 2 家族に割れている** —— `IC-59` は上向きの二重山、`IC-77` は左向きの山、`IC-58` は右向きの山（左右の対と上下が混ざる）。⛔ ④**隣り合う行で員数が 2 と 5 に変わる** —— 畳めない行は 2 つしか描かず、葉の行は 5 つ描いて 3 つ薄い。 ✅ **利用者の裁定（2026-08-30）**: **①スクロールして見えなくなった行は「効かない」に数えない（仕様のままでよい）。**⚠️ **引継書の「いまは数える」は実測で再現しなかった** —— 900px スクロールしても `data-can-*` は 1 つも動かない。**②「そもそもパネルの頭が不適切だ」「^^ と二重の ^ で気持ち悪い」「まずはロジックを全体的に再整理しろ」。****③行を増やすのは `[+]` と `[x]` の対で、増やす先は配下である。** ⭐ **2 軸（範囲 × 動作）に並べ直した一枚と、触れる `.html` の見本を渡した。**⛔ **残る裁定は 3 つ** —— 並べ替えの手（`PD-400`）／足した行の位置と名前（`PD-399`）／畳めない行に薄い操作子を描くか。| A | W5 | 表 T-051 の全行 ／ `FR-029` ／ 表 T-233 ／ `FR-098` | ⭐ **整理した一枚に対して、入口ごとの試験を起こす** | 未裁定 |
| PD-398 | ⛔⛔ **「有効」の見せ方が、仕様の側で「縁」と決まっている** —— ⭐ **根**: 表 T-236 の `S-183` の行の名前が逐語で「構えている入口の**縁**の色」であり、`FR-053` も**縁**に名指している。⇒ **実装は仕様に忠実で、仕様が「縁を塗れ」と言っていた。**⚠️ **台帳 D-71 は 2026-08-27 の利用者の逐語**「アイコン自体を緑で塗れ。例: イナズマ線が有効ならイナズマ線アイコンを緑で塗れ」**をそのまま持ちながら`テスト中` に居る** —— ⛔ **状態表示が偽である**（D-142 と同型）| ✅⭐⭐ **裁定（2026-08-30）。**①**縁ではなく塗り**にする。②抜き色は **`S-146`（地の色）** —— ⛔ 白は使えない（暗いテーマの `S-183` は明るい緑 `#6fc98d`）。地の色なら**両テーマで自動的にコントラストが立つ。**③**4 つとも塗りで統一する** —— 構え（`S-183`）／ON のトグル／ピン止め（`S-151`）／`FR-072` の押下状態。⭐ **形は 1 つ、意味は色。**④**`D-75`（グリフが枠に対して小さい）と同じ一枚で一気に直す**（利用者の指示） ✅ **仕様書へ入れた（CR-311、2026-08-30）** —— `FR-029` と新設の 表 T-237、`FR-053` / `FR-072` / 表 T-051 の `HF-6`。| B | W5 | 表 T-236 の `S-183` `S-146` `S-151` ／ `FR-053` ／ `FR-072` ／ 図 F-019 ／ 台帳 D-71 ／ D-75 | ⭐ **塗りと抜き色を両テーマで読む試験。**⛔ **縁のままの実装が通らないこと**を直接主張する | 裁定済 |
| PD-399 | ⛔⛔ **行（`TaskGroup`）を増やす入口が、どこにも無い** —— ⭐ **実測（2026-08-30）**: 取り消しの表 `UN-14` は「**行（`TaskGroup`）の追加**・削除・名前の変更…」を取り消せる操作として挙げているが、**表 T-109 の 85 個のアイコンに、行を足すものは 1 つも無い。**⇒ **取り消し表が知っている操作を、行う手段が無い。** | ✅ **裁定の一部は下りている（2026-08-30）**: **入口は `[+]` とし、`IC-82`（枠の中の `×`）と対にする。増やす先は配下である**（利用者の逐語「増やすのは [+]と [x] でまぁ、対応はとれるだろう」「増やすのは配下か？同列か？ 配下だな」）。⛔ **残る問い 2 つ** —— **①足した行は末子か長子か。②名前は空で立つか、既定の名で立つか**（⚠️ **空の名は `FR-085` の打ち切りも `FR-052` の幅合わせも掴むものが無い）。**⭐ **`＋` の図形は空く** —— いま `IC-74`（すべての行を開く）が使っているが、`PD-401` の 1 家族へ移せば「足す」に空く ✅⭐⭐ **裁定（2026-08-30）。足した行は末子。名前は空で立てて、その場で打たせる。**⛔ **既定の名は採らない** —— ⛔⛔ **改名の入口が 表 T-109 に 1 つも無い**（`UN-14` は「名前の変更」を取り消せると書いているのに）ので、**既定の名で立てると直せない行ができる。**⭐ **末子を採る理由**: **長子にすると既存の並びが押し下がり、位置は足したあとに動かせる**（`HF-15`）。✅ **仕様書へ入れた（CR-313）**: 表 T-015 の `HR-8` ／ 表 T-051 の `HF-14`。⚠️ **入口の図形はまだ無い** —— `PD-401` の裁定を待つ。 | B | W5 | 表 T-109 ／ `UN-14` ／ 表 T-051 の `HF-1` ／ `FR-032` の `IC-82` ／ 表 T-015 | ⭐ **入口を押すと配下に行が 1 つ増えることと、取り消しで消えることを主張する** | 裁定済 |
| PD-400 | ⛔⛔ **行（`TaskGroup`）を並べ替える入口が、どこにも無い** —— ⭐ **実測（2026-08-30）**: `PD-399` と同じ形で、表 T-109 に並べ替える入口が 1 つも無い。⚠️ **利用者の問い（2026-08-30）**「入れ替えをどうする？ ↑ ↓か？ ドラッグか？」 | ⛔ **未定。3 つ出した。**⭐ **①ドラッグ** —— 深さも同時に変えられる（配下へ入れる／出す）。⛔ **表 T-023d は掴みの全数表であり、行見出しパネルの行を掴む行が 1 つも無い**ので、行を足す裁定が要る。⭐ **②`↑` `↓` の操作子を 2 つ足す** —— 表に行を足すだけで済むが、**操作子が 5 → 7 になり、深さは変えられない。**⭐ **③ドラッグで並べ替え、深さは `PR-15`（`wbsParentUid`）で変える** —— 掴みは 1 次元で済む ✅⭐⭐ **裁定の骨が下りた（2026-08-30。利用者の逐語）**「**上下方向で位置を変えられるように、左右方向で階層を変えられるようにしよう。ただし、変えられるのは上下か左右どちらかのみ。階層を変える時は配下の階層もつれて移動する。**」⇒ **ドラッグを採る。軸は 1 本に固定する。配下は追従する。** ⛔⛔ **細目が 6 つ残る。** ①**右へ動かす意味** —— **段は親を持たないと決まらない**ので、「右へ 1 歩＝**直前の兄弟の子になる**」以外に定義しようがない。⇒ **直前の兄弟が無い行（長子）は右へ動かせない。**②**左へ出たとき、後ろに居た兄弟をどうするか** —— ⭐ **そのまま元の親に残すのを推す**（もう一方は「出た行の子になる」）。③**軸の決め方** —— 最初に閾値を超えた向きで決まり、離すまで変わらない。⛔ **閾値を持つ行が無い**ので設定値が 1 行要る（`S-93` と同じ性質）。④**引いているあいだ何を描くか** —— ⛔ **描かないと「横に引いたのに何も起きない」＝ `D-146` 〜 `D-150` と同じ苦情になる。**縦なら挿入線、横なら段の目安。⑤**ピン止めした行を掴めるか** —— ⛔ `FR-098` はピン止めした行を先頭へ上げるので、**上げられた位置で上下に引くと木の順ではなく表示の順を触ることになる。**⭐ **掴ませない（薄く描いて理由を告げる）を推す。**⑥**取り消し** —— ⭐ **`UN-4`「階層の変更」は既に在る**が、⛔ **同じ親の中の並べ替えは `UN-4` にも `UN-14` にも無い。** ⭐ **深さの頭打ちは `S-125`（`maxGroupDepth` 既定 5）が既に持つ** —— 超える右移動は拒み、理由を 表 T-233 の行で告げる。⛔ **表 T-023d に行を足すことが要る** —— 掴みの全数表に行見出しパネルの行が 1 つも無い ✅⭐ **追加の裁定と実測（2026-08-30、実働サンプル）。****①上下は親をまたぐ** —— **その段の空き場所を読み順に辿る**ので、ある群の末子の次は次の群の長子の位置になる。⇒ **親を越えても段は変わらない。**⛔ **前に立つ者が「同じ親の中だけ」と勝手に狭めており、利用者が指摘して直した。**⭐ **4000 手を打って違反 0 件**（行数・重複・深さ上限・畳みの漏れ・上下で段が動くこと）、うち親をまたいだのは 519 回。**②畳まれた群の中の場所は飛ばす** —— そこへ落とすと動かした行が消え、壊れて見える。**③追従には 3 つの欠陥が在った**（利用者の指摘）—— ⛔ **刻みが 26px で段送りが 16px** だったので 1 段ごとに 10px 離れた。⛔ **切り捨て（`Math.trunc`）でまる 1 刻み動くまで行が動かなかった**（縦は四捨五入で、横だけ違っていた）。⛔ **刻みの途中で行が動かなかった。**⇒ **刻みを段送りに揃え、四捨五入にし、途中もポインタに追従させた。**⭐ **実測でポインタと行のずれは縦横とも 0px。****④縦にも同型の欠陥が在った** —— **親の境をまたぐとき行は群の見出しの上も越えるので 1 刻みが 34px ではなく 68px 進む。**⇒ **固定 px をやめ、行が着いた場所を測り直して追いつく形にした。**⛔⛔ **設定値の行が 2 つ要る** —— **軸を決める閾値**と**段送りの刻み**。⚠️ **刻みは描かれる段送りと同じ値でなければならない**（違うと必ず離れていく）ので、**`S-37`（`rowTitleIndent`。実測 12px）を読むのが筋であり、新しい行は閾値の 1 つだけで足りる可能性が高い。**⭐ **意見が 1 つ変わった** —— **左へ出たときの兄弟の扱いは「元の親に残す」で確定してよい。****上下で親をまたげるなら、連れ出したい兄弟は個別に動かせるからである。**⚠️ **動かせないときの見せ方**: 4 割だけ動いて踏みとどまる（滑って離れるより理由を告げやすい）。**未裁定** ✅⭐⭐ **細目の裁定（2026-08-30）。**①**軸が決まる距離は設定値を 1 行新設**（`S-208` ＝ 6px 🔎）。⭐⭐ **段送りの刻みは新設しない** —— **表 T-201 の `S-37` と同じであることを規則に書いた。**⛔ **数を 2 か所に置くと必ず離れていく**（実測: 刻み 26px・段送り 16px でポインタ 64px に対し行 22px）。②**動かせないときは踏みとどまり、理由を告げる** —— 表 T-233 に `RS-36` 〜 `RS-39` を足した。③**ピン止めしている行は掴めない** —— 実測で、留めた行を引くと画面は 1px も動かないまま親を 2 つまたいだ。✅ **仕様書へ入れた（CR-313）**: 表 T-023d の `GR-20` ／ 表 T-051 の `HF-15` ／ 表 T-233 の 4 行 ／ `S-208`。 | B | W5 | ⭐ **軸が 1 本に固まること・配下が追従すること・長子が右へ動けないこと・`S-125` を超えないことを主張する。**⛔ **並べ替えたあと取り消しで元の順に戻ることも主張する** | ⭐ **並べ替えたあとの並びと、取り消しで戻ることを主張する** | 裁定済 |
| PD-401 | ⛔ **行と頭の折り畳みの図形が、3 つの比喩に割れている** —— ⭐ **実測（2026-08-30）**: `IC-58` は右向きの山、`IC-77` は左向きの山、`IC-59` は上向きの二重山（縦積み）、`IC-78` は上向きの二重山（横並び）、`IC-74` は `＋`。⛔⛔ **「山が 2 つ」が、行では「自分を畳む」・頭では「すべて畳む」の 2 つの意味を持つ。** ⚠️ **利用者の逐語（2026-08-30）**「^^ と 二重の ^ で 何か気持ち悪い」 | ⛔ **未定。1 家族を見本で出した。**⭐ **「向き＝動作、本数＝範囲」** —— 下向き＝開く／上向き＝畳む、**1 本＝この行自身／2 本＝配下／2 本＋棒＝すべて**（棒は「根で止まる」の意）。⭐ **表 T-015 が既に範囲の対を持っている** —— `HR-1`/`HR-3` と `HR-2`/`HR-4` は同じ動作の範囲違いであり、**パネルの頭は L0 である**（利用者の読みどおり）。⛔ **図形の決定は利用者の裁定である**（`FR-029` は 図 F-019 を正と定め、仕様書の中で発明することを禁じている） ✅⭐⭐ **裁定（2026-08-30）。1 家族へ描き直す。**⭐ **向きが動作、本数が範囲**（下向きが開く／上向きが畳む、1 本がその行自身／2 本がその配下）。⭐ **新しく起こすのは 4 つで足りる**（単一の上向き・単一の下向き・二重の下向き・枠つきの `＋`）—— **二重の上向きは `IC-59` の図形がそのまま当たる**（実測）。⭐ **`＋` が「足す」に空く。**✅ **裁定は 表 T-026 の `RC-13` へ記した（CR-313）。**⛔⛔ **図形そのものはまだ起こしていない** —— **次の巡の仕事である。**図 F-019 に 4 つ、表 T-109 に `HR-7` / `HR-8` / `GR-20` の入口の行が要る。 | B | W5 | 図 F-019 ／ 表 T-109 の `IC-58` `IC-59` `IC-74` `IC-77` `IC-78` ／ 表 T-015 ／ 表 T-051 ／ `FR-029` | ⭐ **同じ図形が 2 つの行 ID に立たないことを主張する** | 裁定済 |
| PD-402 | ⛔⛔ **行（`TaskGroup`）を隠す入口が、どこにも無い** —— ⭐ **実測（2026-08-30）**: `HR-6`（「行を隠す。親の非表示グループタブから戻せること」）も `AT-57`（`TaskGroup.isHidden`）も `U-29`（`Hidden Group Tab`）も `UN-14`（「畳みと**非表示**の変更」）も在るのに、**表 T-109 の 85 個のアイコンに「隠す」入口が 1 つも無い。**⚠️ **`AT-57` は「**戻す**入口は `HR-6` が持つ」と書いており、戻す道しか無い** | ✅⭐⭐ **裁定の骨が下りた（2026-08-30。利用者の逐語）**「**^ は選択した行を非表示（上の階層の折り畳みの対象）にしたら？ そしたら目のアイコンが要らんだろ？**」⇒ **1 本の `∧` を「この行を隠す（親へ 1 歩畳み込む）」とし、戻すのは親の `∨`。**⭐ **これで消えるものが 2 つある**: ①新設しかけた「目に斜線」の図形 ②`U-29` `Hidden Group Tab`（戻す先が親の `∨` になるので**読む者が居なくなる**）。⛔ **残る問い 2 つ**: ①**`HR-5`「この行自身を畳む」の一押しを失ってよいか** —— 1 本の `∧` が「隠す」になるため。②**畳みを文書に保存するか** —— `HR-6` は「**隠した状態を文書に保存すること（MUST）**」と定め、`HR-1a` の畳みは画面の使い方である。**1 つの操作にすると、どちらかに揃えることになる** ⛔⛔ **ここに書いた 2 つが、仕様だけを読む体の突き合わせで偽と分かった（2026-08-30。前に立つ者が読み直して確かめた）。****①「畳みを文書に保存するか」という問いは成り立たない** —— **`AT-56`（`TaskGroup.isCollapsed`。真偽・GRS 由来）は実在し、`_source/grs-document.schema.json` にも入っている。**⇒ **畳みは既に文書へ保存されている。**⚠️ **`UN-14` 自身が「畳みと非表示が `UN-7` でないのは、どちらも `TaskGroup` の列だからである」と書いている。**⇒ ⭐ **残る問いは「`HR-5` の一押しを失ってよいか」の 1 つだけである。****②「`U-29` は読む者が居なくなる」も偽** —— **変更履歴を除いて 3 か所が生きて指している**: Chapter 1 の散文（「非表示にしたものは非表示グループタブから戻せるようにする」）／`HR-6` の本文（2 度）／書き出しの `EP-4`。⇒ **廃止するなら、その 3 か所を直す仕事が付いてくる。** ✅⭐⭐ **そして、この設計は 5 日前に止まっていた作業を解く。**版 1.47（CR-294）の逐語: 「⛔ **`HR-6`（行を隠す）の入口は足していない** —— 同行が「親の非表示グループタブから戻せること（MUST）」を同時に要求しており、**そのタブは実装に 1 つも無い**。⭐ **入口だけ足すと、隠したら二度と戻せない行ができる。**」⇒ ⭐ **「戻すのは親の `∨`」は、その障害そのものを取り除く。**入口が無かったのはタブが無かったからであり、**タブを要らなくすれば入口を置ける。** ⛔⛔ **ここに書いた「`HR-5` の一押しを失ってよいか」は誤りであった（2026-08-30。実測で確かめた）。**⭐ **`HR-4`（配下をすべて畳む）を 1 回押せば、`HR-5`「自分は残して子だけ畳む」と同じ絵になる。****一押しは失われていない。**⛔ **前に立つ者は 2 度そう報告し、本行にもそう書いた。**⭐ **実際に変わるのは戻し方だけである** —— 旧 `HR-5` は畳む前の形を覚えて丸ごと戻したが、新しい形は 1 階層ずつ開く。⇒ **その問いは `PD-403`（畳みの状態の持ち方）へ移り、そちらで裁定が下りた。**✅ **残る問いは無い。**⭐ **`∧` に隠す役を与える裁定は 2026-08-30 に下りている。**⛔ **仕様書へはまだ入っていない** —— `HR-6` の入口と `U-29` の廃止は次の巡の仕事である。 | B | W5 | 表 T-015 の `HR-1a` `HR-3` `HR-4` `HR-5` `HR-6` ／ 表 T-109 ／ `U-29` ／ `AT-57` ／ `UN-14` ／ 表 T-051 の `HF-1` `HF-2` `HF-3` | ⭐ **隠した行が親の `∨` で戻ることと、`Hidden Group Tab` を 1 つも描かないことを主張する** | 未裁定 |
| PD-403 | ⛔⛔ **畳みの状態の持ち方** —— ⭐ **実測（2026-08-30。実働サンプル）**: 「隠す」がその行 1 つだけを畳んでいたため、**配下は「開いている」まま残り、親が見えないから見えていなかっただけ**であった。⇒ **親を戻した瞬間に全階層が一度に開いた。**⚠️ **利用者の逐語**「**データの持ち方がおかしくないか？ 過去の値を引きずるような構成にしていないか？**」 | ⭐ **不変条件を 1 つ置くことを推す** ——「**畳んだ行の配下は、それ自身も畳まれた状態とすること（MUST）**」。⇒ **畳む操作は必ず配下ごと状態を書き換える**ことになり、**「畳まれた親の下に開いている行が在る」という状態そのものが作れなくなる。**✅ **直したあと 324 手を打って、残った回数は 0。**⛔ **もう一方の案は「描くたびに祖先を辿って導出する」** —— 状態は 1 つで済むが、**「1 階層開く」が「どこまで開いていたか」を思い出せない**（導出では覚えていられない）。⚠️ **出荷ビルドに同じ漏れが在るかは未測定** —— あちらは畳みだけを持ち「隠す」を持たないので、同型とは限らない ✅⭐⭐ **裁定（2026-08-30）。覚えない。**「**畳んだ行の配下は、それ自身も畳まれた状態とすること（MUST）。畳む前の形を覚えて、開いたときに戻してはならない（MUST NOT）**」。✅ **仕様書へ入れた（CR-313）**: 表 T-015 の `HR-1a`。⚠️ **代償**: 開くときは 1 階層ずつ（`HR-7`）か一度に全部（`HR-3`）かになり、**畳む前の形には戻せない。** | B | W5 | 表 T-015 の `HR-1a` ／ `AT-57` ／ 表 T-051 の `HF-2` `HF-3` ／ `PD-402` | ⛔ **畳まれた行の配下に「開いている」行が 1 つも無いことを、全操作のあとで主張する**（324 手で 0 を確かめた形） | 裁定済 |
| PD-404 | ⛔ **パネルの頭の「すべて畳む」は、L1 も畳むか** —— ⭐ **実測（2026-08-30）**: 実働サンプルで頭だけ 1 階層ずれていた。**行の `∧∧` は直下の子ごと畳むのに、頭の `∧∧` は L1 を残して L2 から下だけを畳んでいた。**⇒ ⛔ **下が全部畳まれているのに頭の `∨`（1 階層開く）が薄いまま**になり、壊れて見えた。⚠️ **利用者の指摘**「パネル頭の動作がおかしい。L1 の動作とパネル頭の動作のロジックがずれてないか？」 | ⛔ **未定。2 つある。**⭐ **①L1 も畳む** —— **頭が本当に L0 になり、4 つの入口が各行と同じロジックになる。**⛔ **「すべて畳む」でパネルが空になる**（頭のラベルに「N 行を畳み込み中」と出て、`∨` 一押しで L1 が戻る）。⚠️ **`HR-2`「すべての `TaskGroup` を閉じる」の読みが変わる。**⭐ **②L1 は残す（いまの `HR-2`）** —— 見慣れた振る舞いだが、⛔ **頭だけ 1 階層ずれたままになる。**⇒ ⭐ **①を推す** —— **「頭は L0 である」は利用者自身の読みであり**（「つまりパネルがイメージ的に L0 となる」）、**表 T-015 が `HR-1`/`HR-3` と `HR-2`/`HR-4` を同じ動作の範囲違いとして既に持っている** ✅⭐⭐ **裁定（2026-08-30）。L1 も畳む。**⭐ **頭は段 0 として扱い、その入口は各行の入口と同じ論理で働く。**⛔ **最も浅い段を残す読みは採らない** —— ⚠️ **実測で、残す読みだと頭の「1 階層開く」入口が押したあと常に効かなくなる。**⚠️ **行が 1 つも描かれない状態になりうるので、頭にいま何行を畳み込んでいるかを示すこと（MUST）を併せて置いた。**✅ **仕様書へ入れた（CR-313）**: 表 T-015 の `HR-2` ／ 表 T-051 の `HF-12`。 | B | W5 | 表 T-015 の `HR-1` `HR-2` `HR-3` `HR-4` ／ 表 T-051 の `HF-10` `HF-12` ／ 表 T-109 の `IC-74` `IC-78` | ⭐ **頭の 4 つが、行の 4 つと同じ形で効くことを主張する** | 裁定済 |
| PD-405 | ⛔⛔ **「配下を 1 階層開く」は、2026-08-25 に利用者自身が却下した仕様である** —— ⭐ **`HF-2` の逐語**「**開く操作子は、その行の配下をすべて開くこと（MUST）** —— 表 T-015 の `HR-3` である。⚠️ **以前は「1 段だけ開く」と定めていた** —— ⛔ **閉じる操作子が畳むものと対にならず、畳んだものを開く組み合わせが存在しなかった**（利用者の裁定、2026-08-25）」。⚠️ **仕様だけを読む体が見つけた**（2026-08-30）—— **前に立つ者は却下の履歴を知らずに 1 本の `∨` を提案していた** | ✅⭐⭐ **裁定（2026-08-30）。却下を覆し、1 本の `∨`（配下を 1 階層開く）を採る。**⭐ **利用者の逐語**「**いいよ。今のサンプルの方がはるかに使いやすい。**」 ⭐ **却下の理由は既に消えている** —— 2026-08-25 に問題だったのは「**畳んだものを開く組み合わせが存在しなかった**」ことであり、**いまは 2 本の `∨∨`（配下をすべて開く）が対として在る。**⇒ **同じ理由で却下されることにはならない。** ⛔⛔ **これに伴って 表 T-015 が 7 行目を持つ** —— 同表はいま 6 操作（`HR-1` 〜 `HR-6`）であり、**「配下を 1 階層開く」はそのどれでもない。**⇒ **行を足す変更要求が要る。**⛔ **`HF-2` の逐語も書き換わる** —— 「その行の配下を**すべて**開くこと（MUST）」は 2 本の側の規則になり、1 本の側の規則が別に要る。⚠️ **却下の履歴は消さずに残すこと** —— **同じ判断を 3 度目にやり直さないためである** | B | W5 | 表 T-015 の `HR-3` ／ 表 T-051 の `HF-2` `HF-11` ／ 表 T-109 ／ 図 F-019 ／ `PD-401` ／ `PD-402` ／ `PD-404` | ⭐ **1 本の `∨` が直下の子だけを戻し、孫を戻さないことを主張する。**⛔ **2 本の `∨∨` との違いが 1 つの試験で分かれることを主張する** | 裁定済 |
| PD-406 | ⛔ **`FR-034` の整列で、揃えないほうの端がどうなるかをどの行も述べていない** —— 同要求は「選ばれたタスクの**開始日または終了日**を、最後に選んだタスクの日付へ揃えること」とだけ述べる。⚠️ **一方の端だけを書くと、`CM-11` が `IV-10`（終了日は開始日より前であってはならない）で拒む場合がある** —— **基準の日がもう一方の端を越えたときである** | ⭐ **推奨: 期間を保って両端を平行移動させる（実装済・暫定）。**⭐ **根拠 3 つ**: ① **平行移動は `IV-10` で拒まれえない**——長さが変わらないので順序が崩れない ② **同じ算術を `GR-12`（バーの本体を引く）が既に使っている** —— 2 つ目の答えを作らない ③ **片端だけを動かすのは「揃える」ではなく「伸ばす」であり、`IC-37` / `IC-38` の表示語（揃える）と合わない**。⚠️ **逆を採るなら、拒まれたときの理由を 表 T-233 に 1 行立てることになる** | C | W4 | `FR-034` ／ 表 T-108 の `CM-11` ／ `IV-10` ／ 表 T-109 の `IC-37` `IC-38` | ⭐ **2 つ選んで `IC-37` を押し、後に選んだ方は 1px も動かず、先に選んだ方は期間を保ったまま開始日が揃うことを主張する試験** | 未裁定 |
| PD-407 | ⛔⛔ **表 T-108 に、行の親を変える命令が 1 つも無い** —— `HF-15`（行を掴んで動かす）の MUST は「**左右は段を変えること（MUST）。右へ 1 歩はすぐ上の兄弟の末子になること、左へ 1 歩は親の次の兄弟になること（MUST）**」と定めるが、⭐ **同表の `TaskGroup` の命令は `CM-26` 〜 `CM-35` の 10 件で、親を動かすものが無い。**⛔ **`CM-35`（`reorderTaskGroupSiblings`）は兄弟の中の並べ替え専用であり、実装自身が「別の親から行を持ち込む一覧は、この親の子の順序を述べていない」と拒む。**⚠️ **上下の移動も同じ穴に落ちる** —— `HF-15` は「その段に置ける場所を描く順にたどること（MUST）」と定め、⇒ **ある群の末子の次は次の群の長子であり、親をまたぐ。** ✅⭐ **案 A を当てた（2026-08-30、CR-318、版 1.72）。⛔ 裁定が下りたのではない。**⭐ **利用者の指示は「裁定はあとに回す。`previous-project-result/11-row-controls` を最後まで実装しろ」であり、`HF-15` のドラッグは書き込む先が無ければ 1 行も実装できない。**⇒ **表 T-108 に `CM-73`（`moveTaskGroup`）を立てた。**⚠️ **覆すなら落ちるのは `CM-73` の行と、それを呼ぶ翻訳の枝だけである** —— **`CM-35` は 1 文字も動かしていない。** | ⭐ **推奨: 案 A —— 表 T-108 に 1 行足す**（`moveTaskGroup(groupId, parentId, order)`）。⭐ **根拠 3 つ**: ① **`UN-4`（階層の変更）が取り消しの行として既に在る** ② **`HM-5`「行の器を作り直してはならない（MUST NOT）」と `HM-2`「移しても `UID` を保つ（MUST）」を同時に満たせるのは、親を書き換える 1 つの命令だけである** ③ **`HM-4`（配下を自分の親にしてはならない）と `FR-085` の深さの上限を、1 か所で守れる。**⛔ **案 B（`CM-35` を広げる）を推さない** —— **1 行が 2 つの意味を持ち、その命令自身の根拠（`FR-005` ／ `HM-8`）と食い違う。**⛔⛔ **案 C（`CM-27` 削除 ＋ `CM-26` 生成）は不可** —— **`HM-5` と `HM-2` を正面から破る。** | E | W4 | 表 T-108 の `CM-26` 〜 `CM-35` ／ 表 T-051 の `HF-15` ／ 表 T-023d の `GR-20` ／ `HM-2` `HM-4` `HM-5` `HM-8` ／ `UN-4` ／ `FR-085` | ⭐ **親をまたいで 1 行を動かし、`UID` と名前と色と高さと畳みが保たれることを主張する試験。**⛔ **いまはその試験を書く相手の命令が無い** | 未裁定 |
| PD-408 | ⛔⛔ **`RC-13` の 1 家族を当てると、1 つの面に見分けのつかない図形が 2 組できる** —— ⭐ **`HR-2` が「パネルの頭は段 0 として扱う」と定めるので、頭の `HR-1`（全展開）は `HR-3`（配下を全展開）の段 0 であり、家族は両者に同じ図形を与える。**⇒ **`IC-58` ＝ `IC-74`（二重の下向き）、`IC-77` ＝ `IC-78`（二重の上向き）。いずれも面は `Row Title Panel` である。**⛔ **これはこの製品が 2 度「欠陥」として直してきた形である** —— 版 1.25「図 F-019 が両者に 1 バイト違わぬ同じ図形を与えており」、版 1.47「縦積みの `⌃⌃` は `IC-59` が既に描いており、行見出しパネルに見分けのつかない図形が 2 つ並ぶ」 ✅⭐⭐ **決着した（2026-08-30、CR-319、版 1.73）。見本が答えを持っていた。**⭐ **逐語**「親へ畳み込んだ。**パネルの頭（L0）の `∨∨`** で戻る」——**見本は頭にも行と同じ二重の図形を使っている。**⇒ ⭐ **段 0 と行とで同じ図形が並ぶことを許す。**⛔ **`FR-029` の例外の段落に足す必要は無かった** —— **同要求が禁じるのは「同じ機能の入口を 2 か所」であり、1 行の配下と全行は違う機能である。**⭐ **理由は `RC-13` に書いた。** | ⭐ **推奨: 段 0 と行とで同じ図形が並ぶことを許す。**⭐ **根拠**: `HR-2` 自身が「同じ動作の範囲違いであり、頭に置いた入口が段 0 のそれである」と書いており、**同じ図形であることこそが伝えたいことである。**⛔ **許すなら `FR-029` の例外の段落に 1 文が要る** —— 同要求が「例外を増やすときは、ここに理由を書いて足すこと」と定めている。⚠️ **逆を採るなら、頭の 2 つに別の図形が要り、「向きが動作・本数が範囲」の家族は頭には及ばないことになる** | E | W5 | 表 T-026 の `RC-13` ／ `FR-029` の例外の段落 ／ 表 T-015 の `HR-1` `HR-2` `HR-3` `HR-4` ／ 表 T-109 の `IC-58` `IC-59` `IC-74` `IC-77` `IC-78` ／ 図 F-019 | ⭐ **図 F-019 の 87 図形に、1 バイト違わぬ対が無いことを主張する試験**（いまは 0 対）| 裁定済 |
| PD-409 | ⛔⛔ **単一の `∧` が `HR-5` なのか `HR-6` なのかで、同じ日の裁定 2 つが食い違う** —— ⭐ **`RC-13` の逐語**「**1 本がその行自身**」⇒ `HR-5`（自分を畳む）。⭐ **見本の README の逐語**「**1 本の `∧` ＝ この行を隠す**」⇒ `HR-6`（隠す）。⛔ **両立しない。**⚠️ **実測**: `HR-5` は仕様書にそのまま在り、`HR-1a` はそれを 1 文字も変えていない。⚠️ **`HR-6` に図形を与えた行は、仕様書のどこにも無い。** ⚠️ **2026-08-30 の時点では `RC-13` の逐語を採った** —— **単一の上向きは `IC-59`（`HR-5`・自分を畳む）である。**⛔ **これは裁定ではない** —— **`HR-6` はいま入口を 1 つも持たないので、どちらを採っても衝突しないうちに、書いてあるほうを当てただけである。**⭐ **覆すなら動くのは図形 1 つの割り当てである** —— `IC-59` を `HR-6` へ回し、`HR-5` に別の図形を与えることになる。 | ⭐ **推奨: 見本を採る**（単一の `∧` ＝ 隠す）。⭐ **根拠 2 つ**: ① **`HR-5` は `HR-4`（配下をすべて畳む）を 1 回押せば同じ絵になる**（版 1.67 が実測で記録している）ので、失う一押しは実質無い ② **`HR-6` はいま入口を 1 つも持たず、`U-29` の戻す道も実装に無い** —— **入口の無い MUST を 1 本減らせる。**⛔ **`RC-13` を採るなら、`HR-6` の入口は別の図形を待つことになり、`U-29` の廃止も止まる。**⚠️ **どちらでも、隠した行を戻す道を先に決めること** —— **`U-29` は実装に 1 つも無い**（版 1.47 が記録）| E | W5 | 表 T-026 の `RC-13` ／ 表 T-015 の `HR-5` `HR-6` ／ 表 T-051 の `HF-3` ／ 表 T-103 の `U-29` ／ 表 T-076 の `EP-4` ／ `UC-002` の手順 5 | ⭐ **単一の `∧` を押したときに動く列が `isCollapsed` か `isHidden` かを主張する試験** | 裁定済 |
| PD-410 | ⚠️ **ピン止めした行を掴めないことを、描く側と読む側のどちらが負うのか** —— `GR-20` は「⛔ **ピン止めしている行は掴めないこと（MUST NOT）**」と定めるが、**同行は掴み領域の表の行であり、表 T-065 の `IF-9` は「掴み領域がどこにあるか」を描いた側の答えとしている。**⭐ **描く側は守っている**（実測: ピン止めした行に掴み代は描かれない）。⛔ **しかし読む側は再確認していない** —— **試験の体が「掴み代の上である」と偷って手渡すと、ピン止めした行が動いた**（実測 2026-08-30）。⚠️ **同行の備考が記録する実測の害そのものである** —— 「留めた行を引くと画面は 1px も動かないまま親を 2 つまたいだ」 | ⭐ **推奨: いまのままでよい**（描く側が単独の持ち主）。⭐ **根拠**: `IF-9` が描いた側を答えの持ち主と定めており、**読む側はピンの状態をそもそも見られない**（ピンは `ScreenSession` のものであり、`UN-16` が文書の外に置いている）。⚠️ **逆を採るなら、ピンの状態を翻訳側へ渡す継ぎ目が 1 つ要る** —— **`drawnRowGroupIds` と同じ形の任意メンバで済む。** | E | W5 | 表 T-023d の `GR-20` ／ 表 T-065 の `IF-9` ／ `FR-098` ／ `UN-16` | ⭐ **掴み代の上であると偷って手渡しても、ピン止めした行が動かないことを主張する試験** ✅ **裁定を受けた（利用者、2026-08-31、逐語）**「**ピン止めしたら上下左右に掴んで動かせないって意味ならそれで OK。**」⇒ **現状維持。**⭐ **規則は描く側（`GR-20` の MUST NOT）が持ち、読む側では繰り返さない**（`IF-9`）| 裁定済 |
| PD-411 | ⛔ **表 T-233 の `RS-30`「その行は既に畳まれている」が到達不能になった** —— **同行の正は 表 T-051 の `HF-3` であり、その `HF-3` は 2026-08-30 の裁定で畳むのをやめて隠すものになった。**⭐ **描かれている行を隠す操作は、少なくともその行自身を画面から消すので、表 T-051 の結びの規則がいう「描かれる行が 1 行も増減しない」にはならない。** | ⭐ **推奨: 行を落とす。**⛔ **場面の無い理由は、読む者には存在しないのと同じである。**⚠️ **逆に、畳む操作をもう一度行に与えるなら、`HR-5` の入口を復活させるということであり、それは見本の 2 × 2 の格子を崩す。** | C | W5 | 表 T-233 の `RS-30` ／ 表 T-051 の `HF-3` ／ 表 T-015 の `HR-5` `HR-6` | ⭐ **表 T-233 の全行に、その理由を出す押下が 1 つ以上存在することを主張する試験** ✅ **CR-322（版 1.76）で決着** —— ⭐ **`RS-30` の宿主を `HF-3` から `HF-13` へ移し、文言を「その行は畳まれておらず、隠れている子も無い」へ改めた。**⛔ **`HF-3`（隠す）は薄くならないので、あそこに置かれていた `RS-30` は到達不能であった。**⭐ **`HF-13` は薄くなる場面を持つので、いまは届く。** | 裁定済 |
| PD-412 | ⚠️ **`HF-18`（行が抱えている畳み込みの件数）の射程が 2 通りに読める** —— 「配下に畳み込んでいる行があるとき、その行数」は、**① その行自身の畳みが隠している数**とも、**② その下のどこかで畳まれている行の総数**とも読める。 | ⭐ **推奨: ②（実装済・暫定）。**⭐ **根拠**: **頭の件数（`HF-12`）は段 0 が抱える総数であり、行を① で読むと頭と行で数え方が違うことになる。**⚠️ **実測では、開いている親が畳まれた子を 1 つ持つと 1 を出す。** | C | W5 | 表 T-051 の `HF-18` `HF-12` | ⭐ **開いている親の下に畳まれた孫がある盤面で、親の数を読む試験** | 未裁定 |
| PD-413 | ⛔⛔ **`∧∧`（配下をすべて畳む）が、見本より 1 階層浅い** —— **実測 2026-08-31**: `Mobile Client` で `IC-77` を押すと、見本では配下が残らず消えて`Mobile Client` だけになるが、**実装では直下の子（`Phone App` / `Tablet App`）が描かれたまま残り、消えるのは孫から下である。**⭐ **仕様は実装のほうを書いている** —— `HR-4`「配下をすべて閉じる」＋ `HF-11`「その行自身を畳んではならない（MUST NOT）」。⛔⛔ **ところが仕様の側に、それと食い違う 2 文が既に在る** —— ① `HR-2`「本行と `HR-4` は**同じ動作の範囲違い**であり、頭に置いた入口が段 0 のそれである」（⚠️ **頭の `∧∧` は段 0 を畳んで行を 0 にする。実測済み**）② `HF-1`「**`HR-4` を 1 度押せば**（`HR-5` と）**同じ絵になることを実測しており**（版 1.67）」（⚠️ **今のビルドでは偽。`HR-5` は行自身を閉じるので直下の子が消える**）| ⭐ **推奨: 見本に合わせる（`∧∧` は直下の子から下を描かれない状態にする）。**⭐ **根拠**: **見本が正であるという利用者の指示**に加え、**上の 2 文がどちらもそれを前提に書かれている。**⚠️ **`HF-11` の「その行自身を畳んではならない」は「隠してはならない」（`HF-3` の職務）と読み直すことになる。**⛔ **`HR-4` / `HF-11` / `HF-1` の MUST を書き換えるので、裁定を待つ。** | A | W4 | 表 T-015 の `HR-2` `HR-4` `HR-5` `HR-7` ／ 表 T-051 の `HF-1` `HF-11` `HF-13` | ⭐ **`∧∧` を押したあと、直下の子が描かれているかを数える試験**（頭と行で同じ答えになることも） ✅ **利用者の指示（2026-08-31、逐語）**「**この作業に俺の裁定は要らない。ただ「サンプルと同じ動作にしろ」というのが俺の指示だ。**」⇒ **見本に合わせた。**⭐ **CR-322（版 1.76）で `HR-4` は「その行を畳む」、`HR-7` は「その行の畳みだけを解く」、`HR-3` は「その行と配下から畳みと隠しを取り除く」へ。**⛔ **`HF-11` の「その行自身を畳んではならない（MUST NOT）」を落とした** —— **その 1 文が、行と頭を 1 階層ずらしていた張本人である。** | 裁定済 |
| PD-414 | ⚠️ **ピン止めした行の地を塗るか** —— **見本は留めた行の地を強調色の 9% で薄く塗る**（`.row.pinnedTop`）。**実装は塗らず、常に描かれる `IC-60` の塗り（`EN-3` ＝ `S-151`）だけで示している。**⭐ `FR-098` は「どの行が留まっているかを、行を一覧しただけで読めること（MUST）」と定め、**その規則は `HF-6` が持つ**と書く。⇒ **どちらでも MUST は満たせる。** | ⭐ **推奨: 現状維持（塗らない）。**⭐ **根拠**: **地を 1 枚増やすと、`HF-6` が操作子の下に敷く地（`S-150`）と重なり、留めた行だけ操作子の見え方が変わる。**⚠️ **塗るなら、色は「強調色の n%」という新しい種類の値になり、表 T-236 に持ち方が無い。** | C | W5 | `FR-098` ／ 表 T-051 の `HF-6` ／ 表 T-237 の `EN-3` | ⭐ **留めた行の地を、留めていない行の地と比べる試験** ✅ **CR-323（版 1.77）で決着** —— **利用者の指示「状況に応じてアイコンの色を変える点も合わせろよ」**⇒ **見本に合わせて塗る。**⭐ **色は 表 T-236 の `S-151`、濃さは 表 T-206 の `S-214`（9%）。**⚠️ **前に立つ者の推奨（現状維持）は採られなかった** —— **利用者が見本を正と定めた。** | 裁定済 |
| PD-415 | ⚠️ **頭の件数を語にするか** —— **見本は「・N 行を畳み込み中」と語で出す。実装は `▾ N` だけを出す。**⭐ `HF-12` は「頭にいま何行を畳み込んでいるかを示すこと（MUST）」と定め、**形を定めていない。** | ⭐ **推奨: 現状維持（`▾ N`）。**⭐ **根拠**: **行見出しパネルの幅は `S-79` ＝ 170px であり、見本のパネルは 337px である。**⚠️ **語にすると 表 T-206 ではなく `FR-038` の辞書に2 語（ja / en）を足すことになる。**⭐ **行の側（`HF-18`）と同じ形にそろえたことが、現状維持の側の利点である。** | C | W5 | 表 T-051 の `HF-12` `HF-18` ／ `FR-038` | ⭐ **頭が出す文字列を読む試験** ✅ **裁定を受けた（利用者、2026-08-31）**「**提案通り**」⇒ **現状維持（`▾ N`）。**⭐ **実測**: パネル 170px、頭の入口 4 つが x=66 から始まるので使える幅は 66px。`▾ N` は 29px、語にすると 130px 超で入らない | 裁定済 |
