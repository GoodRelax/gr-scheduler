# 残件 50 行を「何が止めているか」で束ね直した表

基点 `0d1e4c2`。台帳 `docs/development-records/defects.md` の
`実測済` / `取下げ` でない 50 行（実装待ち 32 / 試験待ち 11 / 未検討 5 / 仕様待ち 2）。

## 束（大きい順）

### 束 1 -- 生成器 `tools/generate_entity_types.py` が 4 実体しか形にしない
- 1 つ足す: `SHAPED_ENTITIES` に `CommentBox`（と `HighlightBox`）、
  `PropertyFieldKey` に付箋を名乗る腕。
- 開く行: D-349、その後ろで D-283、D-302 の半分（`leaderShapeKind`）。同じファイルで D-314。 = 4
- 触るファイル: `tools/generate_entity_types.py:185`,
  `src/adapter/screen-renderer/screen-renderer.ts:974-1030`,
  `src/use-case/edit-document/edit-annotation.ts:106`
- 継ぎ目: はい -- `PropertyFieldKey`（公開型）、`COLUMN_SHAPES`（生成定数）
- 仕様の行: D-349 / D-283 は不要（`PR-21` は CR-368 で入っている）。
  D-302 は要（`_source/property-items.json` に `leaderShapeKind` の行が 0 件）。
  D-314 は `docs/development-rules/03-implementation.md` の名簿に 1 行（検査 30 が数える）。
- 実測: `SHAPED_ENTITIES = ['Task','TaskVisual','TaskGroup','Dependency']`（:185）。
  `erd.json` の `CommentBox` は `id / leaderShapeKind / text / anchorDate / anchorGroupId / bodyOffsetPx`、
  `HighlightBox` は `... strokeColor / cornerRadiusPx`（= D-314 の `S-132`）。
  `PropertyFieldKey` の腕は 5 つ（`task` / `taskVisual` / `taskGroup` / `dependency` / `project`）。

### 束 2 -- 検査・試験の道具（src/ を 1 バイトも触らない）
- 開く行: D-339 / D-342 / D-343 / D-344 = 4
- 触るファイル: `tests/contract/spec-table.ts`（D-343）、`tests/unit/uf-47-48.test.ts`（D-342）、
  `.claude/skills/spec-graph-check/`（D-344 の詰まり検査、D-339 の引用出所の新しい検査）
- 継ぎ目: いいえ／仕様の行: いいえ
- 実測: 1 つの編集ではない。1 体の持ち分である（4 行とも `src/` の語を 1 つも持たない）。

### 束 3 -- 段数の安全弁（造りは入った。残るは試験だけ）
- 開く行: D-270 / D-271 / D-345 = 3（3 行とも `試験待ち`）
- 触るファイル: なし（`tests/` のみ）。錨は
  `tests/unit/st-7-rs-24-the-stack-safety-valve-returns-a-value.test.ts`
- 継ぎ目: いいえ（`stackSafetyCapReached` は既に返り値に在る）／仕様の行: いいえ（CR-368 で `ST-7` 済み）
- 実測: `RS-24` は entity 4 / adapter 1 / framework 8 か所。語も辞書に在る。

### 束 4 -- 書き出しが画面の値へ届かない（`WY-3` の一致）
- 開く行: D-276 / D-277 = 2
- 触るファイル: `src/adapter/image-exporter/image-exporter.ts:263, 270-281, 379-389`,
  `src/adapter/svg-renderer/svg-renderer.ts`, `src/adapter/screen-renderer/app-header-items.ts`
- 継ぎ目: はい -- `AppHeaderItems`（題を矩形なしで運んでいる）、`themed()` の公開
- 仕様の行: いいえ（表 T-076 `EP-1` / `WY-3`、表 T-236 `S-149` / `S-165` が既に縛る）
- 実測: `themed(` は adapter に 29 件、全部 `svg-renderer.ts` の私有。
  `image-exporter.ts` は色を直値で 3 件持つ。

### 束 5 -- 毎フレーム／毎変更の全面作り直し
- 開く行: D-330 / D-316 / D-348 の症状① = 3
- 触るファイル: `src/framework/dom-svg-surface/dom-svg-surface.ts:41`（`innerHTML` 全書き換え）、
  `src/framework/dom-screen-surface/dom-screen-surface.ts:6138`、`src/adapter/svg-renderer/svg-renderer.ts`
- 継ぎ目: いいえ／仕様の行: いいえ（第 7 章 表 T-042 / T-043 が合否を持つ）
- 実測: D-330 の 補足 が逐語で「`D-316` … の一般形である」。
  D-348 の症状① が逐語で「`D-330` が既に持つ」。
  門は間隔 p95 24.2〜30.3 ms（天井 16.7）で赤。

### 束 6 -- 複製と貼り付け（造りは入った。残るは試験だけ）
- 開く行: D-290 / D-321 = 2（両方 `試験待ち`）
- 触るファイル: なし（`tests/` のみ）
- 実測: `pasteTaskSubtree` は framework に 3 件、`pasteTaskGroupSubtree` は 4 件（コメント除く）。
  D-321 が逐語で「`D-290` … と同じ 1 つの穴である -- 一緒に直すこと」。

### 束 7 -- バーの左半分・右半分を公開名から問う
- 開く行: D-292 / D-346 = 2
- 触るファイル: `src/entity/layout-engine/item-hit-area/item-hit-area.ts`,
  `src/adapter/input-command-translator/input-command-translator.ts:4048`
- 継ぎ目: はい -- 新しい公開名を 1 つ。⛔ `itemAtPointer`（`PI-7`）の署名は動かさない（利用者の裁定 2026-09-06）
- 仕様の行: はい -- 表 T-023a の `PD-3` に「どこで半分に割るか」を書く
- 実測: D-346 が逐語で「`D-292`（直したと記録されている）と食い違うかもしれない … 先に測れ」。

### 束 8 -- `NT-3` の「件数を添えた通知」を、変更の経路から立てる
- 開く行: D-273 / D-305 = 2
- 触るファイル: `src/framework/single-html-shell/frame-loop.ts`（`raiseNotice` の並び）、
  `src/use-case/edit-document/edit-calendar.ts:81-92`, `edit-resource.ts:134-137`
- 継ぎ目: いいえ／仕様の行: はい -- 暦の側に `RS-` の行が無い（担当者側は `RS-49` で入った）
- 実測: D-305 が逐語で「`D-273`（担当者名の波及）とまったく同じ形である …
  `NT-3` が挙げる 4 つの例のうち 2 つが、届く先を持たない」。

### 束 9 -- 日程表の上に立つ欄（その場編集器）
- 開く行: D-273 / D-338 = 2
- 触るファイル: `src/framework/dom-screen-surface/dom-screen-surface.ts`
- 継ぎ目: いいえ／仕様の行: いいえ（表 T-023 `MK-10` / `MK-13`、`U-60` が先例）
- 実測: `frame-loop.ts` の STOP 逐語「⛔ NO IN-PLACE EDITOR EXISTS FOR MK-13's 担当ラベル」。
  ⚠️ D-273 は束 8 と束 9 の両方で止まっている（二重に塞がっている）。

## 同じ型 -- 「命令は在り、人の入口だけが無い」

見分け方（機械で測った、`scratch/typecount.py`）: `src/use-case/` の `case '<kind>':` を集め、
`src/adapter/input-command-translator/` と `src/framework/` からコメントを剥がして
その文字列リテラルを数える。0 件なら型に当たる。

**`src/use-case` が捌く命令 81 種のうち 15 種が、人の入口を 1 つも持たない。**

| 命令 | 台帳の行 |
|---|---|
| `setExportPngScale` | D-261 |
| `setResourceName` | D-273 |
| `setCommentBoxText` | D-283 |
| `setCommentBoxLeaderShapeKind` | D-302 |
| `deleteUnreferencedResources` | ⛔ 行が無い |
| `reorderTaskGroupSiblings` | ⛔ 行が無い |
| `setCalendar` | ⛔ 行が無い |
| `setCommentBoxAnchor` | ⛔ 行が無い |
| `setFontScale` | ⛔ 行が無い |
| `setHighlightBoxRange` | ⛔ 行が無い |
| `setHighlightBoxStrokeColor` | ⛔ 行が無い |
| `setProjectProfile` | ⛔ 行が無い |
| `setStackDirection` | ⛔ 行が無い |
| `setThemeHue` | ⛔ 行が無い |
| `setThemeMonochrome` | ⛔ 行が無い |

命令ではないが同じ型（宣言と本体が在り、呼び手が 0）:
- D-278 `restoreOpenedFilePermission` -- `file-store.ts:301`（宣言）と
  `file-system-access-file-store.ts:862`（本体）の 2 か所だけ。
- D-312 `clampedSettings` -- `document-settings.ts:512`（本体）。`src/` の呼び手 0。
  当たるのは `json-codec.ts:771,775` と `properties-panel.ts:1565` のコメント 3 件だけ。

この巡に閉じた同型（記録のため）: D-290 / D-321（貼り付け）、D-301（`clearDualCursor` -- 翻訳器が
2 か所で出すようになった。⚠️ `Esc` の道はまだ命令を出さない）。

## 1 行しか開かない行 -- 24 件

D-263 / D-268 / D-269 / D-274 / D-275 / D-280 / D-282 / D-284 / D-291 / D-293 /
D-297 / D-298 / D-301 / D-303 / D-304 / D-306 / D-308 / D-310 / D-311 / D-313 /
D-318 / D-319 / D-340 / D-341

うち 1 手が小さいもの: D-308（原稿の 1 語）、D-341（3 か所の差し替え）、
D-263（注記 1 行 -- ⭐ 台帳の欄では既に「直した（CR-362）」と書かれている）。

## 前に立つ者の渡した 2 つの束の訂正

### 束A（`ScreenPart` に「語の入口を押した」腕を 1 つ足す）
⭐ 造りの側は正しい。⛔ **開く行は D-275 の 1 行だけである。**「面の語のボタンが押せない」で
止まっている行はほかに無い -- 逐語で測った:
- `U-60`（透かし解除）の語のボタンは `NT-7` の 2 答なので `ScreenPart.confirmationAnswer` が運ぶ
  （`open-modals.ts:87`）。
- `U-56`（開く選択）は 表 T-109 の `IC-71`..`IC-73` が在るので `entry` が運ぶ
  （`input-command-translator.ts:8654`）。
- `U-30`（AI 出力、D-268）だけが似ているが**塞がりが違う** -- 逐語
  「⚠️ FR-068's copy control is still owed one: table T-109 places nothing but IC-52 on the
  `AI Export Modal`, and FR-029 (MUST) forbids minting a row」（`open-modals.ts:791`）。
  ⇒ 要るのは `ScreenPart` の腕ではなく **表 T-109 の行**である。さらに面の本文も届いていない
  （同 :784「the document … is built outside this component and does not arrive here」）。

⛔ もう 1 つ訂正: `frame-loop.ts:6788` の STOP は不在を 2 つ挙げるが、**(1) は既に古い。**
`dom-screen-surface.ts:5415` の `importReportElements` が `droppedTaskNames` を描いている。
残るのは (2) だけである。

### 束B（`SHAPED_ENTITIES` に `CommentBox`、`PropertyFieldKey` に腕）
⭐ 正しい。⛔ **述べられているより大きい。**
- `HighlightBox` も同じ理由で外れており、その `cornerRadiusPx` が **D-314 の `S-132`** である。
- `CommentBox` の `leaderShapeKind` が **D-302 の半分**である。
- ⇒ D-349 → D-283 の 2 行ではなく、**4 行**（D-349 / D-283 / D-302 / D-314）に届く。
