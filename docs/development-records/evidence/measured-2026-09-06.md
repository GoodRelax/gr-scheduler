# 実測 2026-09-06 —— 実測待ち 12 行

**造り**: `npm run build` → `dist/index.html` を `file://` で開く。1920 × 1080、Chromium（Playwright）。
**道具**: `scratch/m0906-sweep.mjs`（1 回の起動・12 場面）。生の数は `scratch/m0906-sweep.json`。
道を探すために使った捨て探針: `scratch/m0906-orient.mjs` `scratch/m0906-explore.mjs` 〜 `scratch/m0906-explore6.mjs`、
取り込みの見本 `scratch/m0906-with-stop.xml`（小の見本に `Task` の `<Stop>` を 1 つ足したもの）。

⛔ **`src/` `tests/` `docs/spec/` `change-request/` `defects.md` は 1 バイトも触っていない。**
⛔ **走らせたのは `npm run build` と `scratch/` の Playwright だけである。**

---

| 行 | 判定 | 押した手順 | 出た数 | 期待 |
|---|---|---|---|---|
| `DFC-263` | **そもそも押す件ではない**（仕様書の注記の話）。⭐ **注記が述べる振る舞いは押した** | `IC-20` → `IC-23` を構え、日程面の空き（`y+700`）を横に引いてタスクを 1 つ描く → `Delete` → `Esc` → `Delete` | 描く前 **1000**、描いた後 **1001**、選択は `{kind:'task',uid:1961}` **1 件**。⛔ 描いた直後の焦点は **`INPUT:PR-1`**（名前の欄）で、そのまま `Delete` を押すと **1001 のまま**。`Esc` で欄を離してから `Delete` を押すと **1000**、選択は **0 件** | 描いたものの上に立つこと。`Delete` で消えること |
| `DFC-286` | **直っている**（両半分） | 描いたタスクを `AM-7` で `PA-3`（`actualStart` 2026-09-01 / `actualDuration` 2 / `resume` 2026-09-10）に置く → パネルの `PR-7` を押して `Delete` を 3 回（欄が空になる）→ 同じ欄に `2026`→`09`→`14` と打って `Enter` | 置いた直後 `resume` **2026-09-10** / `resumeValid` **true**。空にした後 `resume` **null** / `resumeValid` **false**。日付を置き直した後 `resume` **2026-09-14T00:00:00** / `resumeValid` **true** | 空にしたら `resumeValid` が偽。置いたら真 |
| `DFC-288` | **直っている** | `IC-62`（名簿）→ 何も選ばずに `IC-66` → `IC-63`（全選択）→ `IC-66` → `cancel` → `IC-66` → `proceed` | 名簿は **14 行**（全部 `data-referenced="true"`）。何も選ばずに押すと通知 **1 件**（`RS-27` の綴り）で文書は動かない。`IC-63` で **14 件**が選ばれる。`cancel` の後は 担当者 **14** / 割当 **946** / タスク **1000** のまま。`proceed` の後は 担当者 **0** / 割当 **0** / **タスクは 1000 のまま** | 名簿から担当者を消せること。`Task` が 1 つも消えないこと |
| `DFC-289` | **直っている** | 同上（`IC-66` の直後に立った問いを読んだ） | 問いは `proceed` / `cancel` の 2 択。本文は「この担当者を削除しますか？ / この タスクの割当てが外れます」に続けて**タスクの名前**が並ぶ（`Map the vendor workflow` `Assess the budget risks` …）。⭐ **担当者の名前は 1 つも出ない** | 解かれる割当のタスク名を挙げて確認を求めること |
| `DFC-294` | **直っている** | `Task` に `<Stop>` を持つ `MSPDI`（`scratch/m0906-with-stop.xml`）を `IC-1` → `IC-71`（置換）→ `proceed` で取り込み、`AM-7` で ① 名前を変える ② 実績を `PA-2` に置く | 取り込み後、`carry.Stop` を持つタスク **1 件**（`uid` 2 `Kickoff Meeting`、`2026-01-05T09:00:00`）。**名前を変えても `Stop` は残る**（`2026-01-05T09:00:00`）。**実績を編集すると `Stop` は `null`**、書き出した `MSPDI` に古い `Stop` は **含まれない** | 実績を人が編集したときに限り原値を手放すこと |
| `DFC-295` | **直っている** | `IC-23` を構える → `IC-45`（`Dual Cursor`）→ もう一度 `IC-23` → `IC-16`（明暗） | 構える前 `[data-armed="true"]` **0 個**、構えた後 **1 個**（`IC-23`）、`IC-45` の後 **0 個**。⭐ 対照: 構え直して `IC-16` を押すと **1 個のまま** | `Dual Cursor` に入ると構えが外れること。ほかの入口では外れないこと |
| `DFC-300` | **直っている** | `IC-20` で `exportJson` → 同じ内容を返す選択器を `addInitScript` で置き、`IC-1` → `IC-72`（合流）→ `IC-96`（両方とも残す） | 取り込み前 タスク **1000**。`Difference Review` が立ち、候補 **1000 行**、面の上に `IC-95` `IC-96` `IC-97` の 3 つ。`IC-96` の後 タスク **2000** / 割当 **1892** | 合流の面が在り、押せること |
| `DFC-307` | **直っている** | `IC-10` の上にポインタを置いて動かさずに待つ → `Esc` → `Esc` → ポインタを 400px 離してから戻す | 説明が立つ（「日程の全体が 1 画面に収まる倍率と位置にする F」）。`Esc` 1 回で **空**。2 回目の `Esc` の後も **空**。ポインタを離して戻すと **また立つ**（同じ文） | 出ている説明を `Esc` で消せること。あとでまた出せること |
| `DFC-322` | **直っている** | `F2` → 打鍵 → `Enter` → `Ctrl`＋`Z` | `F2` で `INPUT` が立ち（`data-field-row="U-27"`）、焦点が入り、**23 文字が全選択**（元の名前の長さ）。`Enter` で題は **`Pressed On 0906`**、`Ctrl`＋`Z` で **`Three-Year Product Plan`** に戻る。ヘッダの高さは **37 → 37** | `F2` で文書の名前を変えられること |
| `DFC-325` | **直っている** | `IC-20` で面を出し、**壊れた形 14 通り**を `applyCommands` に渡す（`undefined` / `null` / `{}` / `{commands:[]}` / `{readStamp}` / `readStamp` が `null`・文字列 / `commands` が `null`・文字列 / `commands:[null]` / `[undefined]` / `['setTaskName']` / `[1]` / 文字列そのもの）。最後に正しい形を 1 つ | 例外 **0 件**。拒否 **14 件**、理由はすべて **`malformedRequest`**。正しい形は **受理**され、タスクの名前が **`Renamed by AM-7`** になる | 例外を投げないこと。理由の区分を返すこと |
| `DFC-332` | **直っている** | `MSPDI` を `IC-1` → `IC-71` → `proceed` で取り込み、無編集のまま `AM-12`（`exportMspdi`）で書き出し、局所名で突き合わせ | 小の見本（`sample-small-website-renewal.en.xml`、`OutlineLevel` 0 を持つ）: **46 / 46 件が対応**、`UID` `ID` `OutlineLevel` `OutlineNumber` `Summary` の食い違い **すべて 0**。`ProjectLibre.xml`（起点 1）: **2 / 2**、食い違い **すべて 0** | 往復が無損失であること |
| `DFC-337` | **直っている** | 決着しない `showOpenFilePicker` / `showSaveFilePicker` を `addInitScript` で置き、門ごとに 2 度ずつ打鍵 | `Ctrl`＋`O`: 1 度目 通知 **0 件**、2 度目 **1 件**（`RS-27` の綴り）。`Ctrl`＋`S`: **0 → 1 件**。`Ctrl`＋`R`: 何も開いていないと **0 → 0 件**（⛔ 下記）。`Ctrl`＋`O` の待ちが立っている状態で `Ctrl`＋`R` を押すと **1 件**、もう一度で **同じ行が 2 件目**（`data-affected-count` ではなく通知が 2 本立つ） | 押して何も起きないときに理由を告げること |

---

## ⚠️ 数の脇に書いておくこと（すべて実測）

```
⛔ DFC-263 の注記は「半分」しか古くない
   「描いた直後は選ばれておらず」 → 偽である（選択は 1 件、uid 1961 が立つ）
   「Delete を押しても何も消えなかった」 → 焦点が名前の欄に在る限り再現する
   ⇒ 描いた直後の焦点は PR-1 の欄であり、Delete はその欄へ行く。Esc で欄を離すと消える

⛔ DFC-337 の「読み直す」門は、2 度押しでは押せない
   何も開いていないと 1 度目の待ちがその場で終わるので、2 度目は待ちに当たらない
   （台帳が「触っていない」と書いている、器が無いときの黙った return である）
   ⇒ 待ちを立ててから押した。門の見張りは RS-27 を上げる

⚠️ 再開日の綴りが道によって違う
   AM-7 で置くと "2026-09-10"、パネルで置くと "2026-09-14T00:00:00"
   ⛔ 本巡の 12 行のどれも、これを問うていない。判定には使っていない

⚠️ パネルの確定は「次の出来事」で使われる
   日付の最後の 1 桁を打った時点では、文書はその 1 つ前の日を持っている
   （2026-09-1 まで打った状態が書かれ、Enter で 2026-09-14 になる）

⭐ 測り方で踏んだ罠（次の巡のために）
   - Playwright の fill() は date の欄では効かない。実キーで押すこと
   - この造りの date は 年→月→日 の順で、年の升は 6 桁まで食う（ArrowRight で送る）
   - 描いた直後は WS-2 が editingInPlace で全部の書き込みを拒む。先に Esc
   - 取り込みで文書が入れ替わると Agent API の面が落ちる。IC-20 を押し直すこと
   - 確認の答は yes/no ではなく proceed / cancel である
   - 空きの無い日程面では描けない。1000 行の見本では y+480 より下が空いている
```
