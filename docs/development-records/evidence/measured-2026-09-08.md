# 出荷ビルドを押した実測 —— 2026-09-08

⛔ **本書は記録である。⭐ いまの主張ではない。**⇒ **読むときは日付とビルドを見よ。**
**押したビルド**: `dist/index.html`、コミット `5c89868`、1170435 バイト。
**やり方**: Playwright（Chromium を 30 回起動、実ポインタの所作およそ 360 回）。
⛔ **Browser ペインは使っていない**（隠れたペインは `rAF` を出さない）。⛔ **ホイールは 1 度も回していない。**

---

## ✅ 押して直っていたもの（11 行）

**`DFC-283` コメントボックスの本文編集**
```
箱 {x:561, y:200, w:20, h:20} の中央 (571,210) をダブルクリック
⇒ パネル幅 279、[data-field-row] 2 件（PR-21）、入力 1 個
  document.activeElement = TEXTAREA[data-field-row="PR-21"]
  打鍵 + Enter ⇒ commentBoxes[0].text = "sweep body text"
```

**`DFC-302` 引出しの線**
```
data-figure="comment-…-leader" が <line> 1 本
x1=566 y1=117 → x2=786 y2=257。箱は rect (786,237,20,20)
⇒ 錨の点から箱の左下角へ 1 本。折れ線でも 2 本でもない
```

**`DFC-385` 畳まれて子の無い行**
```
子を 1 つも持たない群を畳む ⇒ その行の入口は aria-disabled="true"
押した後: documentStamp 変わらず、changeLog 0、描かれた行 8 → 8、差分 0 件
対照: 子を持つ畳んだ群で同じ押し方 ⇒ scheduleUpdatedUtc が 00:30:43 → 00:30:44
     （押し自体は生きている）
```

**`DFC-387` `Agent API` で書いたあと、入力なしで絵が来る**
```
ポインタを (4,1070) へ退避して以後 1 度も触らずに applyCommands([setProjectTitle])
⇒ 30ms 後には題が "Three-Year Product Plan" → "SweepTitle-…"
  同時に svg の outerHTML 長も 66265 → 66252（絵も描き直っている）
⚠️ 直す前の実測は「1500 ミリ秒待っても古い題のまま」であった
```

**`DFC-389` / `DFC-402` 拒みの語**
```
タスクを選んだ状態でマイルストーンの形を押す:
  「構えている形状は、選んでいるものには当てられません」
  「構えはそのまま使えます。選択を解いて、空いている所に引いてください」＋OK
⇒ 旧文「受け付けられない操作が含まれていたので、まとめて取りやめました」は出ない
⭐ 利用者の申告と 1 字も違わない
⚠️ 同じ選択で当てられる形は黙って通る（拒まないので告げない）
```

**`DFC-390` マイルストーンのダミーの図形**
```
本体   polygon 4 点 "566,85 587,106 566,127 545,106"   42x42   fill hsl(214 46% 80%)
ダミー polygon 4 点 "575,90.67 578,106 575,121.33 572,106" 6x30.66 fill hsl(214 62% 34%)
⇒ 同じ菱形。矩形ではない。1 個。色は実績の色を <g opacity="0.2"> で薄めたもの
```

**`DFC-393` 実績のダミーが掴める**
```
6px/日、未着手、予定開始 x=698。描かれたダミーの印は 1 本、polygon x 704-710（6px = 1 日）
  x 692-698 GR-3 / x 699-703 GR-12 / x 704-713 GR-9 / x 714-724 GR-7
⇒ 印の全画素が GR-9 を返す
1.5px/日 でも同じ（印 x 701-702.5）
⚠️ 直す前は 画素 0 が GR-3、画素 1-8 が GR-4 で、印はどの画素からも掴めなかった
```

**`DFC-396` ダミーの印は 1 本**
```
未着手のタスク、6px/日: <g opacity="0.2"> 1 個 ＋ polygon 1 個 = 印 1 本
1.5px/日 でも 1 本（w=1.5）。マイルストーンでも 1 本
```

**`DFC-397` 担当ラベルのダブルクリック**
```
担当ラベル {x:249.42, right:326.39, w:76.97, y:539.72, h:24} の中央を押す
⇒ パネル幅 279、[data-field-row] 37 件、入力 21 個
  document.activeElement = INPUT[data-field-row="PR-16"]
```

**`DFC-398` `Esc` で選択が解ける**
```
パネルを開いた場面（バーをダブルクリック）:
  直後   選択あり、パネル 279、焦点 INPUT/PR-1
  Esc①  選択あり、パネル 279、焦点 BODY      （欄から焦点が抜ける）
  Esc②  選択あり、パネル 0                    （パネルが閉じる）
  Esc③  選択なし                              （選択が解ける）
パネルを開かない場面（1 回押しただけ）: Esc① で選択が解ける
⚠️ 直す前は「選択は永久に解けなかった」
```

**`DFC-399` 実績のダブルクリック**
```
実績バー {x:860, w:168, right:1028} の 5 点すべて
（左端 861 / 左+3 863 / 中間 944 / 右-3 1025 / 右端 1027）
⇒ どれもパネル幅 279、[data-field-row] 37 件、焦点 INPUT/PR-1
```

---

## ⛔ 押して直っていなかったもの（4 行）

**`DFC-395` 実績の 2 端で「終了側を掴む」が 1 画素も効いていない**
```
6px/日、実績の開始日 = 終了日。描かれた実績バー {x:860, w:6, right:866}
その 1 日の全画素（x=861 / 863 / 865）で右へ 36px 引く:
  actualStart 2026-05-20 → 2026-05-26   ⛔ 動いたのは開始側
  終了日 2026-05-20 は据わったまま
⭐⭐ 予定の側は逆に正しい —— 同じ形の 1 日の予定バーで x=861..865 の 5 画素すべてで
   finish が動き start は据わった
⚠️ 台帳の申し送りは「予定側が未着、実績側は効かせた」であった。実測は逆である
```

**`DFC-400` 再開アイコンが 1 つも描かれていない**
```
当たり判定は正しい: y 方向 584-603 = ちょうど 20px（S-93 と一致）
                    x 方向 605-627 = 23px が再開日を書く
⛔ 絵が無い: マーカーの右 40px の帯に描かれた節点が 0 個
造りの裏取り: ResumeGeometry は幾何に在るが、grep -rn "resume" src/adapter/svg-renderer/ は 0 件
⇒ 掴めるが、狙う先が見えない
```

**`DFC-394` 札が 42 か所で重なっている**
```
⭐ 要点は満たされている —— マーカーの表示切替でタスク名は 1px も動かない
   （描かれた 40 タスク中 0 件、最大移動量 0.000px）
⛔ 重なりは残っている:
   完了率 と 担当者名   40 件（＝全タスク）3.09〜10.78px
                        例: "70%" [217.89,255.08] と "Analyst A" [249.42,326.39] が 5.66px
                        ⇒ 絵では "70%Analyst A" と繋がって読める
   タスク名 と マーカー  1 件 6.92px  ⇐ 利用者が申告したのはこれ
   実績 と マーカー      1 件 8px
   並び「担当 ≦ タスク名 ≦ マーカー」を満たすのは 40 件中 18 件
```

**`DFC-401` 別の治具 2 本が数を手書きしていた**
```
出荷が渡す値: 実績の端 12 / 線 6
手書きの治具: 実績の端 6 / 線 4（2 本とも同じずれ）
⇒ この 2 本は同じ日のうちに直した（式に揃えた）
⭐ 直したことで期待値が 1 件動いた —— バーの中間のつもりで押していた点が、
  正しい掴み代では終了側に飲まれていた
```

---

## ⛔ 名指しされていなかった欠陥（4 件）

**① ⛔⛔ 負の値が文書に入る（最も重い）**
```
1 日ぶんの実績（幅 6px）の上を右へ 36px 引く:
  actualDuration   1 → -3
  percentComplete  2 → -5
actualDuration = 2（12px）でも同じ -3 / -5
⇒ 開始側が終了日を追い越しても、何も止めていない
```

**② ⛔ コメントボックスが行の上に重なると、箱を押してもタスクが選ばれる**
```
箱を (786,237,20,20) に置き中央 (796,247) を押す
  elementFromPoint  ⇒ コメントボックスの要素
  選択              ⇒ {kind:'task', uid:3}
  ダブルクリック    ⇒ タスクのパネル（37 行）
空き地へ移すと正しく箱が選ばれる
```
⛔ 反証された（2026-09-08）: それが表の印字された並び順である —— 表はコメントボックスをタスク本体より下に置いており、
「下にあるものより手前に浮く」と書かれているのはパレットの行だけである。

**③ ⛔ 図形を変える命令が enum を検めず、しかも図形が変わらない**
```
'NOT-A-GLYPH' を渡しても accepted: true が返る
⚠️ 正しい 15 個を渡しても、描かれる図形は 4 点の菱形のまま 1 度も変わらなかった
⛔ 書けていないのか描けていないのかは詰めていない。⭐ 要確認
```

**④ ⚠️ 再開アイコンの当たり判定に、日を書かない帯が 17px ある**
```
x 588-604（進捗マーカーの円が描かれている範囲）を引くと
  resumeValid  false → true
  resume       null のまま
⇒ 「妥当だが日が無い」という、どの状態の行にも当たらない状態になる
```
⛔ 反証された（2026-09-08）: その 17px の帯は再開アイコンではなく進捗マーカーの円である
（役目は状態を巡らせること）。「妥当・日が無い」は進行中という行そのものである（状態の表が明記している）。

---

## ⚠️ 測れなかったもの、と理由

```
DFC-397 の「増えるか入れ替わるか」  割当が 946 件あり、どれがそのタスクのものかを取り違えると
                                 数を誤る。⭐ 当て推量で書くより、測らなかったと書くほうがよい
DFC-398 の「3 回目でブラウザへ落ちる」 選択が空になることまでは測れたが、その次の押しが
                                 ブラウザへ渡ることは Playwright から観測できない
DFC-393 の「利用者が押した場面」    固定できたのは掃引が選んだ場面だけである。
                                 「1〜8 日の短い予定で、作った直後の選択されたまま」は再現していない
```

---

## ⭐ この掃引が示したこと

**単体試験は 7223 件すべて緑であった。⛔ そのうえで 4 行が直っておらず、4 件の欠陥が新たに出た。**
⇒ **「試験が全緑」は「人に届く」の証拠にならない。**
⭐ **押す費用は Chromium 30 回・所作 360 回で、1 巡のうちに収まる。⛔ 省くな。**

---

## ⭐ 通しの e2e が 5 件、正しい製品に対して赤くなっていた（2026-09-08）

```
DFC-182 ×2   FR-043 の裁定前の読み（印を 2 本描く）を逐語で持っていた
           ⭐ その件が数えていたのは「描かれた図形」であって「掴む先」ではなかった ——
             名前ではなく造りを読んで初めて分かった
留めの門    今朝の刈り取りで行が隣のファイルへ移ったのに、門が片方しか読んでいなかった
           ⛔ 台帳の頭が「片方しか読まない道具を足すな」と警告しているその罠である
           ⭐ 留めを外さず、門のほうを 2 ファイル読むように直した
DFC-210      ⛔ ドラフト描画のせいではなかった（1 つ前のビルドで読みが完全に同一）
           本当の原因: ドラッグが行を作るので下のバーが 40px ずり下がり、
           文字列が変わったので「新しく作られた」と誤認されていた
DFC-374      ⛔ 製品の退行ではなかった。⭐ 製品は条項を守っている ——
           いちばん高い帯 ÷ Row Area が 1080px の窓で 0.6963、700px の窓で 0.7030。
           高さの違う 2 つの窓で同じ割合 ＝「画面の高さから導く」は生きている
           壊れていたのは読み方: 行があふれると先頭の帯は上端で切り詰められて publish されるので、
           帯どうしの差は「行の間隔」ではなくスクロールの残りであった（11 倍、倍率と逆向き）
GR-21      掃引が押していなかったのは 3 行ではなく 1 行（diff の +3 は行数であった）
           ⭐ そもそも押せるようになったのはこの巡の仕事のおかげである ——
             つまみは今朝まで長さを持っていなかった

⛔⛔ 5 件のうち 4 件で、前に立つ者の診断が誤っていた。
⭐ 4 件とも、ブリーフに「先に数えろ」「原典を読め」「反証せよ」と書いた体が実測で否定した。
```
