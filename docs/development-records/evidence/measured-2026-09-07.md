# 実測 2026-09-07 —— `実測待ち` 5 行を出荷ビルドで押した

**造り**: `npm run build` → `dist/index.html`（1,157,159 バイト）を `file://` で開く。1920 × 1080、Chromium（Playwright）。
**基準**: `1a857f9`。**道具**: 使い捨ての `.mjs` 1 本・1 回の起動・4 場面。
⛔ **`src/` `tests/` `docs/` `change-request/` は 1 バイトも触っていない。**

| 行 | 判定 | 押した手順 | 出た数 | 期待 |
|---|---|---|---|---|
| `DFC-275` | **合** | `IC-20` → 選択器を差し替えて日付を壊した MSPDI を渡す → `IC-1` → `IC-71` → `proceed` → 立った面の `OK` を実マウスで押す | 取り込み前 タスク **1000** → 後 **44**（見本 46）。落ちたのは `<Start>` を 表 T-214 の床の外（`1899-12-30`）にした 1 件と空文字にした 1 件で、どちらも文書に **無い**。隣の 2 件は **在る**。`[data-role="Import Report"]` が立ち、落とした名前を 2 つとも並べる。**通知の欄は 0 件**（`RS-50` の MUST NOT どおり）。`OK`（`BUTTON`）を押すと 面は **null**、名前も消える | 落として残りを取り込み、落とした名前を `U-62` で告げ、`OK` が閉じること |
| `DFC-283` | ⛔ **否** | `IC-20` → `IC-35` を構える → 空所を 1 クリック → `Esc` → 本文の箱を 1 クリック → 同じ点をダブルクリック | 箱は置ける（`commentBoxes` **1 件**、本文の箱 ＝ `rect (896,471,20,20)`）。1 クリックで `readSelection` ＝ `{kind:'commentBox'}`。⛔ **ダブルクリックの後、`Properties Panel` は 幅 0 ／ `[data-field-row]` 0 件 ／ 焦点 `BODY` ／ `PR-21` **null**。**⭐ 対照: 同じ手をタスクに打つと 幅 **280** ／ 行 **37** ／ 焦点 **`INPUT:PR-1`**。⚠️ `IC-17` は文書の描画設定（`K-nn` **112 行**）を出すだけ。⭐ `CM-48` 自体は生きている —— `applyCommands({kind:'setCommentBoxText'})` は **accepted** で本文が入る | `MK-13`: パネルを出し、`PR-21` に焦点を置くこと |
| `DFC-292` | **合** | `IC-20` → `IC-61` を構え、バー A（`uid 16`、幅 504px）の左 1/4・右 1/4・中点から バー B（`uid 37`）の左 1/4・右 1/4 へ引く。各回 `Ctrl`＋`Z` | 基準 **509 本**。右→左 **`linkType 1`**（`DP-1` FS）／ 左→右 **2**（`DP-2` SF）／ 右→右 **0**（`DP-3` FF）／ 左→左 **3**（`DP-4` SS）。どれも `{on:37, predecessorUid:16}`。`Ctrl`＋`Z` で毎回 **509** に戻り日付も戻る。構えたまま空所へ落とすと **0 本増**・日付不動・構えは **残る**。構えを外すと **0 本増**・**日付が動く**（表 T-023d 無傷） | 構えているあいだ、左半分と右半分が別の意味を持つこと |
| `DFC-346` | **合（振舞い）／ 押せなかった（名前）** | 同上。幅 **504px** のバーの左 1/4（x=458）と右 1/4（x=710）を別々に押す。中点（x=584）も押す | 左 1/4 と右 1/4 は **別の答え**（3 と 1、2 と 0）。⇒ 台帳が引く「左 1/4 と右 1/4 が同じ答えを返す」は **偽**。**中点ちょうどは `linkType 1`** ＝ 右半分に落ちる（`FR-009` の MUST）。⛔ **`dependencyEndAtPointer` の綴りは `dist/index.html` に 0 件**（`itemAtPointer` も 0 件。縮小で消える）⇒ 公開名の登録は出荷ビルドからは問えない | 半分が公開された名前から別の答えとして読めること |
| `DFC-312` | **合** | `IC-20` → `exportJson` → `documentSettings` の 3 鍵を範囲の外へ（`minShapeWidth` 999 ／ `commentBoxWrapUnits` 1 ／ `rowTitleFont` 400）→ 選択器に返させて `IC-1` → `IC-71` → `proceed` | 拒まれず入り、タスク **1000** のまま。`minShapeWidth` **20**（`S-49` の上限）／ `commentBoxWrapUnits` **4**（`S-182` の下限）／ `rowTitleFont` **40**（`S-36` の上限）。通知 **1 件**「読んだ設定値が範囲の外だったので、範囲に収めました**3**…」 | 範囲の外の値が丸められ、件数が運ばれること |

---

## ⚠️ 測り方で踏んだ罠（**⭐ 次に押す者はここから読め**）

```
⛔ 構えた入口をもう一度押しても構えは外れない。Esc でも外れない。
   外れるのは IC-45（DFC-295）だけである。
   ⇒ 盲に押すと 2 回目以降が「構えを外す」になり、引いたつもりの操作が日付を動かす
     （5 手のうち 3 手がこれで空振りした）
⛔ バーを 1 度でも選んでから引くと、そのあとの引きが 1 本も書かれない。
   ⇒ 名指し（どの Task か読むこと）は掃引のいちばん最後に置くこと
⛔ data-armed は 1 フレーム遅れて立つ。押した直後に読むと空が返る
⛔ 引きを move → down → move → up と 1 続きで打つと落ちる。
   ポインタを置いて 300ms 休み、押して 250ms、途中の点を経由して離すこと
⛔ コメントボックスは「クリック」で置く。ドラッグでは置けない（FR-019）。
   ⚠️ ハイライトボックスとは逆である
⛔ Command Palette は絵の上に浮く（1280×800 で 200,85 → 544,286 に実測）。
   ⇒ 押す点は必ず elementFromPoint(...).closest('[data-role="Schedule Canvas"]') で確かめる。
     確かめずに歩くと、途中で図形を構え（次の空振りが Task を作る）、IC-75 を押してしまう
⛔ 描かれている行しか押せない。行を足す掃引は、深さの試しより後に置くこと
⚠️ バーは polygon（fill="hsl(214 46% 80%)"、高さ 28）である。rect にも path にも無い。
   高さ 20.44 の rect は依存線のハローの mask であって、描かれていない
⚠️ npm run e2e はビルドを走らせない（playwright.config.ts の webServer は npm run dev だけ）。
   ⛔ 先に npx vite build を打たないと、偽の赤が 7 本出る
⚠️ sample-schedule/ は .gitignore されているので、どの作業木にも無い。
   親から写さないと FR-021 の条項が 1 本余分に落ちる
```
