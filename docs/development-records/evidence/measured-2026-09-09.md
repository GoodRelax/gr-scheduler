# 出荷ビルドを押した実測 —— 2026-09-09

⛔ **本書は記録である。⭐ いまの主張ではない。**⇒ **読むときは日付とビルドを見よ。**
**当てたコミット**: `4281612`（`git merge --ff-only` で本ワークツリーへ当てた。`git log --oneline -1` で確認済み）。
**本書を書いた体の役目**: ⛔ **台帳の体である。造りは 1 行も触っていない。**⭐ **台帳（`defects.md` / `fixed-defects.md`）と本証跡だけを書いた。**
⛔ **単体 (`vitest`) と e2e は、台帳を書き換えている最中の禁止（規則 04 の 6.8）に従い、本セッションでは 1 度も走らせていない。**
下の「通しの実測」節の数は、この巡ですでに出荷ビルドへ通しをかけた体からの申告として記録するものであり、**本書の体が自分で再現したものではない**。

---

## ⭐ 台帳から見つけて `fixed-defects.md` へ移した 3 行

**両方のファイルを `担当` / `完了率` / `再開` / `+1` で grep し、`rulings.md` の `R-07`〜`R-10` の着地先と突き合わせて特定した。**
⛔ **番号は与えられていなかった。逐語が症状と一致することを確かめてから動かした。**

### `D-414` —— 担当ラベルと完了率ラベルが重なる（`R-09` の着地）
```
直す前  defects.md の逐語（2026-09-08 時点）: 「完了率のラベルと担当者名のラベルが、
        描かれた全タスクで重なる」（実測 3.089〜10.781px）。絵では「70%Analyst A」と
        繋がって読めた。
裁定    rulings.md R-09（利用者の逐語）: 「担当 完了率の順にどちらも右寄せで並べる。
        下記を参照とせよ。previous-project-result/08-poc/poc-integrated.html」
着地    FR-090（繋ぎ方・区切り・右寄せ）＋ 表 T-038 の OC-2 ＋ FR-059 から指すだけ。
        rulings.md は 適用済。
反証・実測 2026-09-09（出荷ビルド、R-09 を当てたあと）:
        直す前 描かれた 40 タスク全部で 3.089〜13.971px 重なり、繋がって読めた
        直した後 重なり 0 件。40 件すべてが 1 つの text 要素で text-anchor="end"、
                札の右端から予定バーまで 7.082〜8.000px（S-32 labelGap = 8）
                バーに乗り上げたもの 0 件
```
⇒ **`実測済` へ変更し、`fixed-defects.md` へ移した。**

### `D-413` —— 担当が就いたタスクに未知の名前を打つと、割当が増える（`R-07` の着地）
```
直す前  defects.md の逐語（2026-09-08 時点）: 「担当者が既に就いているタスクに名簿に無い
        名前を確定すると、割当が『増える』。」実測 2026-09-08: 表示が Analyst A → Analyst A +1。
裁定    rulings.md R-07（利用者の逐語）: 「差し替えでOK。担当を変える場合はすでに
        プロパティーパネルから切り替え可能。削除も担当者一覧から削除可能。」
着地    FR-008 の表 T-225 の AS-7（解除の条項）＋ 同表の下の段（1 回の呼び出しで走らせる
        命令が 3 つになる）。rulings.md は 適用済。commandsFromAssignee が
        commandsFromUnassign を呼んで CM-45 を足す（1 人だけ就いていたとき）。
反証・実測 2026-09-09（出荷ビルド、R-07 を当てたあと）:
        「Zeta One +1」（元の担当が外れない）
        ↓
        「Zeta Two」。2 人が就いたタスクでは「Zeta One +2」のまま（狭まっていない）
        取り消し 1 回で「Zeta One」に戻る（1 段である）
⚠️ 2 人以上のときどれを外すかは未裁定。⛔ そこは直っていない。行にそう明記した。
```
⇒ **`実測済` へ変更し、`fixed-defects.md` へ移した。**

### `D-412` —— 再開アイコンの置き場所が 2 つの MUST で正面衝突（`R-10` の着地）
```
直す前  defects.md の逐語（2026-09-08 時点）: 「再開アイコンの置き場所が、2 つの MUST で
        正面衝突している。」実測 2026-09-08（アイコンを掴んで左へ引いた）: アイコンが
        [926, 939.44] に立ち、マーカー [1506, 1522] の左、実績バー [338, 1502] の
        真上に 13.44px 重なった。
裁定    rulings.md R-10（利用者の逐語）: 「レジューム矢印は常に再開日の位置を正とする。」
着地    表 T-221 の LF-11（正）＋ 表 T-038 の並びの記述からは外し、場所の予約だけ残す
        ＋ LF-13（形と縦だけ）。rulings.md は 適用済。
反証・実測 2026-09-09（出荷ビルド、R-10 を当てたあと）:
        ⛔⛔ これは 造りの欠陥では なかった。⭐ 実測で否定された
        再開アイコンは再開日から 0.000px、マーカーから −38.000px
        ⇒ 幾何は最初から正しかった。壊れていたのは 注記 だけである
        （表 T-038 の並びが再開アイコンを名指していると書いてあった。もう名指していない）
⚠️ マーカー表示を切り替えても名称ラベルの左端は 0.000px 動かない（R-08 が生きている）。
```
⇒ **`実測済` へ変更し、`fixed-defects.md` へ移した。⛔ ただし「造りは正しかった。直したのは
注記」と行に明記した** —— 次の読み手が「幾何を直した」と誤解して正しい造りを触りに来ないため。

---

## ⭐ `tools/ledger_metrics.py` が刷った 8 状態（移す前 → 後）

```
                残件  総件数  未検討  裁定待ち  仕様待ち  実装待ち  試験待ち  実測待ち  実測済  取下げ
移す前 (2026-09-09 02:17)  25   421     3       7        5        7        2        1      353    43
移した後 (2026-09-09 02:20) 22   421     3       4        5        7        2        1      356    43
```
⭐ **裁定待ち -3、実測済 +3、総件数 ±0、残件 -3** —— 移した 3 行と過不足なく一致する。
`python tools/ledger_metrics.py --check` → `OK docs/development-records/defects.md: 421 row(s), the metrics block matches`。

---

## ⭐ 通しの実測（`4281612`、`npx vite build` のあと 1 回。前に立つ者からの申告として記録）

```
単体      186 ファイル・7274 通過・1 skip・落ち 0    （巡の始め 7269）
precheck  0        parity  0（75 段）        gen:check  0
e2e       133 通過・4 落ち・8.3 分            （巡の始め 133 / 4 / 8.4 分）
```
**落ちている 4 件は、名前ではなく中身で既知と確かめた：**
```
tests/nfr/nfr-001-010-011-013…     性能の門         ⇒ D-330（PD-443 の裁定待ち）
tests/nfr/nfr-002-003…             フレームの間隔    ⇒ D-330（同上）
tests/system/fr-073-fr-022…        ⛔ AM-8 を 13 回持つ ⇒ D-357 の残り 3 条項
tests/system/nfr-004…（掃引）       ⛔ GR-13 =「依存線を線の上で選ぶ」
                                    ⇒ 昨日立てた行（D-421）。file:// と http:// の両方で落ちる
```
⇒ **退行 0 件。** ⛔⛔ **この「中身で確かめた」という事実を明記する** —— 前の巡で、名前だけ見て
4 件を取り違えた事故がある。本書の体はテストを自分では走らせていないため、この節は
申告の転記であり、本書独自の再実行による確認ではないと重ねて記す。

---

## ⭐ もう 1 つの実測 —— 体が自分で壊して測った結果（申告として記録）

**機械が捕まえたもの:**
```
札を 1 枚にする      区切りを空白に→2 赤 ／ 右寄せを落とす→1 赤
                    labelGap を 2 つ数える→1 赤 ／ 左端で揃える→2 赤
                    完了率を札から外す→4 赤
担当者の差し替え     解除を外す→3 赤
死んだ枝を外す       読む行を 1 つ足す→tsc が 1 赤（TS2339）
```

**機械が捕まえ *られなかった* もの（こちらのほうが大事である）:**
```
① 札の縦位置はどこも主張していない。行の上端に描いても 27 件全部通る
   ⇒ PD-347 に残した唯一の問い
② 「担当者を全員外す」と広く書いても、検査は 1 つも赤くならない
   （検査は scope を判じない。区別できるのは裁定の逐語だけ）
   ⭐ ただし 錨 は捕まえた —— 2 人が就いた錨が 1 件赤くなった
   ⛔ その錨が無ければ 7274 件が緑のまま複数担当の道が閉じていた
③ 条項を 01-04-requirements.md だけで曲げると 検査 42 は通ってしまう
   （変更履歴が同じ逐語の写しを持ち、錨がそちらで解決される）
   ⇒ ⛔ 行の言い回しは半分しか守られていない
```

---

## ⭐ 本書の体が自分で実測したこと —— md-checks / check.sh とわざと壊した実測

**移した直後の状態（`git status --porcelain` が `defects.md` と `fixed-defects.md` のみを示す状態）で:**
```
python .claude/skills/spec-graph-check/md-checks.py .   → exit=0
bash   .claude/skills/spec-graph-check/check.sh          → exit=0（RESULT: PASS、ALL GREEN）
```
⛔ **基準線ファイルは 1 つも触っていない。** ⚠️ `check.sh` の検査 39 が
「1634 MUST/MUST-NOT clause(s), 644 held, 990 not (was 991) -- ⭐ lower the baseline
to hold the ground」と表示したが、これは `OK` 行であり `docs/spec` 側の既存の改善
（本セッションの編集より前、コミット `4281612` に既に含まれるもの）であって、
本書の体の作業とは無関係。基準線ファイルには触れていない。

**わざと壊した実測（この体自身が実行）:**
```
fixed-defects.md から D-412 の行だけを削って再実行
  → check.sh: exit 0 → 1
  → 検査「a row that still reads blocked...」の直前、
    「DRIFTED docs/development-records/defects.md: the metrics block no longer
    matches the table -- rerun tools/ledger_metrics.py」が新たに赤くなった
    （defects.md と fixed-defects.md の合計行数で数える仕掛けなので、
    fixed-defects.md 側を 1 行削るだけで defects.md の頭の集計行が古くなる）
  → RESULT 行も「ALL GREEN」から「FAILURES ABOVE」に変わった
  直後に削った行を復元し、md-checks / check.sh とも exit=0 に戻ったことを再確認した
```
⇒ **0 件ではなく 1 件、確かに赤くなった。** ⛔ 元に戻す前に出力を退避先（scratchpad）に
保存し、削った行をそのまま貼り戻したことも確認済み。

⚠️ **副次的な観測**: この実験の最中、`.` を対象にした precheck が、実験用に一時的に
リポジトリ直下へ書いた出力ファイル（`out1.txt` 等）の中の個人アカウント名を捕まえて
`1 trap(s)` と報告した。それらのファイルは実験終了後に削除済みであり、`git status`
にも残っていない。台帳（`defects.md` / `fixed-defects.md`）以外は書き換えていない。

---

## ⭐ git の最終状態

```
git status --porcelain
 M docs/development-records/defects.md
 M docs/development-records/fixed-defects.md

git diff --stat
 docs/development-records/defects.md       | 11 ++++-------
 docs/development-records/fixed-defects.md |  3 +++
 2 files changed, 7 insertions(+), 7 deletions(-)
```
