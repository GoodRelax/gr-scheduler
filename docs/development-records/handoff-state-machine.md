# 引継プロンプト（状態機械）—— ⛔ **本書は `handoff.md` に従属する**

**本書は追跡されており、公開リポジトリに載る。**
⛔ **公開なので、個人情報・絶対パス・秘密を書かないこと。**
**番号を増やすな。本書 1 ファイルを毎巡書き換える。**

> ⛔⛔ **唯一の引継書は `handoff.md` である。本書はその例外ではない。**
> `handoff.md` は「本書が唯一の引継書である」と宣言しており、本書はそれを覆さない。
> ⭐ **本書が在る理由は 1 つだけ** —— **状態機械の仕事が、別のセッションで並行して進むからである**（`JDG-60` の逐語「計画したらブランチを分けよう」）。
> ⇒ **`handoff.md` を先に読め。本書はその上に載る。** 食い違ったら `handoff.md` が正である。
> ⛔ **並行が終わったら本書を消せ。** 残す価値のある学びは `handoff.md` か `docs/development-rules/` へ下ろす。

> **印は 2 つだけ使う。**
> **⛔ 破ると壊れる** —— データが失われる、検査が偽の緑になる、巡を 1 つ無駄にする。
> **⚠️ 測ってから決めろ** —— 前の巡が実測で驚いた所。
> 印の無い文も規則である。

---

## 0. 現在地

⛔ **数を信じるな。すべて自分で測り直せ。**

```
ブランチ    refactor        ⛔ 先端は git log --oneline -1 で測れ（HEAD は動く）
main        ⛔ 直接作業しない。タグは利用者が打つ
```

⚠️ **別のセッションが同じ作業木で並行して動く。** `git status` に自分のものでない変更が見えるのが正常である。⛔ **触るな。** 約束は §6 が持つ。

---

## 1. ⭐ 決まっていること —— 設計は着地済み

⛔ **問い直すな。** 下はすべて `rulings.md` と仕様に在る。

| 出どころ | 中身 |
|---|---|
| `JDG-45` | 逐語「**だったら、自前でステートマシンを実装しよう。**」⇒ **XState は採らない** |
| `JDG-55` | 同日の裁定を `rulings.md` に載せてよい |
| `JDG-60` | 逐語「**OK。 ただし、計画したらブランチを分けよう。**」⇒ 置き場（`UseCase` に専用コンポーネント 1 つ、`ScreenState` を広げる、現在値と副作用の実行はシェル）と性能の見立てを了承。**ブランチを分けるのは、この裁定である** |
| **設計の決定（5.6 の 2 つ目）** | 「保存しない状態を、自前の状態機械 1 つで持つ」Status = **`Accepted`**。`docs/spec/05-07-design.md` の 5.6。`CR-379`（段 2b-A）で着地 |
| **表 T-249** | 状態機械の形を `SF-1` 〜 `SF-10` の 10 条で定める |

⛔ **表 T-249 の逐語を読んでから設計しろ。** 骨子だけ写す（⚠️ 写しは腐る。正は仕様である）:

```
SF-2   遷移は領域ごとの step(state, event)。次の状態と副作用の列を返す純粋関数
SF-3   何も変わらない出来事では、受け取った状態と同じ参照を返す（NFR-010）
SF-4   扱わない出来事の組は never による網羅でコンパイルエラーにする
SF-5   連続して変わる値（座標・寸法・時刻）は状態機械に入れず、フレームの値とする
SF-6   副作用は値として返し、SingleHtmlShell が実行する
SF-7   現在値はシェルが 1 つだけ持つ。内側 3 層にモジュールスコープの可変状態を置かない
SF-8   根の状態は領域の合成。1 つの switch に全領域を集めない
SF-9   クラスを作らない。公開するのは型と関数だけ
SF-10  文書は集約に入れない。セッションは読むだけ
```

---

## 2. 在るもの・まだ無いもの

**2026-09-21 に実測した。⛔ 使う前に測り直せ。**

⭐ **`CR-436` の波 A が着地した**（2026-09-21）—— 原稿・生成器・`AdvanceScreenSession`・契約試験・共通の状態機械の仕様。⛔ **ただしシェルはまだ呼ばない**（結線は波 B2）。

| | 実測 |
|---|---|
| 原稿 `docs/spec/_source/state-machines.json` | ✅ 第 1 領域「画面の値」だけ（状態 34 ・出来事 30 ・遷移 53）。表 T-280〜T-282 と図 F-026 は `_assets/tbl-state-machines.md` へ生成（`npm run gen`） |
| `src/use-case/advance-screen-session/` | ✅ 3 ファイル（公開エントリ ・ `session-step.ts` ・ `screen-values.ts`）。⛔ 呼ぶのは試験だけ。検査 33 は `audit-ch5.py` の `NOT_YET_CALLED` で名指しで待たせている（`JDG-285`）—— 呼ぶ辺が入ったら、その項を消せと赤になる |
| 共通の状態機械の仕様 | ✅ `05-07-design.md` の 5.6 —— 図 F-027（1 つの出来事の 1 巡）・図 F-028（根の状態の組み立て）・表 T-284（共有の 1 段の形、`SS`）・表 T-285（領域を足す手順、`RA`） |
| 契約試験 | ✅ `tests/contract/state-machine-screen-values.contract.test.ts`（状態 × 出来事の全組）と `t-284-the-shared-step.contract.test.ts`（表 T-284 ・ T-285 の錨）。⭐ `unchanged` を壊すと 886 件赤（前に立つ者が合流した木で数えた） |
| `frameLoop` が直に持つ `let` | **66 個**（`sed -n '1799,4414p' src/framework/single-html-shell/frame-loop.ts \| grep -c '^  let '`。⛔ 行の範囲は `frameLoop` の頭と尾を測ってから入れろ。ファイル全体で数えると `frameLoop` の外の 2 個 ——`:1610` の `owed` と `:1655` の `isFreeingAny` —— を拾って 68 になる。旧版はこの誤りで 68 と書いた） |
| `UseCase` のモジュールスコープの可変状態 | **2 個**（`apply-document-change.ts` の配布中の旗、`notify-change-watchers.ts` の登録簿） |

⚠️⚠️ **5.6 の 2 つ目の設計の決定 を書いた時点の `let` は 60 だった。8 日で 6 個増えている（消えたものは 0）。**
⇒ **着手が遅れるほど移す仕事は増える。**⭐ 出血は検査 60 が止めた（§4）が、**既に溜まった 66 個は減らない。**

⚠️ `frameLoop` は **2,616 行 / 分岐 4**、`domScreenSurface` は **871 行 / 分岐 1** である。
⭐ **分岐がほぼ無い** ⇒ これは処理ではなく**入れ物**である（内側に関数 83 個・`let` 66 個）。
⛔ **だから要求では割れない。**`R2.2b`（`07-review-standards.md`）が「下位の列挙でしか語れない水準は、固有の責務を持たない単なる入れ物である」と判定する。

---

## 3. やること

```
✅ 波 A   原稿・生成器・AdvanceScreenSession・契約試験・共通の状態機械の仕様（CR-436）
✅ 波 B1  JDG-283（隠したパネルの境界に掴み帯を置かない）—— FR-052 と GR-22 を 1 度に、
          screen-frame.ts の dividersOf、frame-loop.ts の @provisional PND-451 を消した（2026-09-22）
✅ 波 B2  シェルへ結線した（2026-09-22）。frameLoop の let 66 → 57、送り口は sendToSession 1 本、副作用は
          session-effects.ts（UF-123）の runSessionEffects、Esc は表 T-283（RG-1〜RG-13）。Adapter の袋は ScreenViewReadings に。
          照合器（5,844 の台本）で B2 の前後の差 0。写した所は DFC-703〜708、仕様とコードの食い違いは DFC-709〜711（it.fails で留めた）。
          ⭐ 次の領域の波（通知・身振り・ファイル操作・名前付け・選択）は、effectRunnersOf の自分の行と unwiredEffect の置き場を
             差し替えるだけで入る —— 別々の作業木で並行し、frame-loop.ts へは 1 本ずつ併合する
          ⭐ 入口条件は緩めた（JDG-291、2026-09-21）: 段 5・6 を待たずに結線してよい。B2 を先に 1 本で入れ、
             残りの領域は別々の作業木で並行し、frame-loop.ts へは 1 本ずつ併合する。性能は全部の結線の後に 1 回だけ測る
             （波ごとに 1 コミット ⇒ git bisect で絞れる）。持ち場: こちら frame-loop.ts ・ screen-renderer ・
             apply-document-change.ts ほか結線に要るシェルと adapter、段 6 は src/framework/dom-screen-surface/ だけ。
             ⛔ 例外: CR-500 の波 B（面の focusin ・ focusout の知らせ）は段 6（CR-439）の着地を待つ
✅ 通知    CR-440 の波 A（原稿 SM-34〜38 ・ EV-31〜35 ・ TN-54〜63、notice-values.ts、表 T-286〜T-288、図 F-029）
          ⛔ 結線（let 4 つ）と配布中の旗の移し（apply-document-change.ts、検査 61 の基準線が動く）は
             CR-440 の波 B1 ・ B2 —— 段 5・6 と CR-436 の波 B2 の後
✅ 身振り  CR-450 の波 A（pointerPressStateMachine ・ rowGrabStateMachine、gesture-values.ts、表 T-289、図 F-035）
          ⚠️ 押下の 5 種類を文書を変える押下と数える今の振る舞い（DFC-687）を、状態機械も写している（JDG-57: 移してから直す）
             ⇒ 契約試験が 23 件を it.fails で留めている。直したら留めを外す
✅ ファイル操作  CR-460 の波 A（領域 fileFlow: fileOperationStateMachine ・ confirmationStateMachine、file-flow-values.ts、表 T-290、図 F-036）
          画面の値に出来事 screen/flowSurfaceAnswered を 1 つ足した。台帳 DFC-689〜691（DFC-459 ・ 574 ・ 614 ・ 569 とも重なる）
          ⚠️ 「後で実行する書き込み」の型は緩い（unknown など）—— 厳密にすると構成図に無い辺ができ検査 59 が赤。波 B で呼び手と一緒に辺を足す
✅ ポインタ  CR-470 —— 状態機械は立てない（値はフレームの値。SF-5 に 1 文）。配られた 表 T-291・図 F-037・ユニット UF-120 は使わず返した。台帳 DFC-692〜693
✅ 名前付けと入力欄  CR-480 の波 A（領域 fieldEntry: createdTaskNamingStateMachine ・ fieldFocusWantStateMachine、field-entry-values.ts、表 T-292、図 F-038）
          ⚠️ この領域だけは状態機械が今の振る舞いを写していない —— 今のコードは選択が動いたときなど 3 つの場合にも焦点の求めを消すが、仕様（IN-5a）にその事由は無く、状態機械は消さない（DFC-694）。波 B で結線するとき、どちらに揃えるかを先に決めよ
          根の升の副作用 bringCreatedRowIntoSight を 1 つ。画面の値の原稿は変えない。台帳 DFC-694（焦点の求めを IN-5a の事由に無い場合にもコードが消す疑い。波 B は写す）
✅ 選択  CR-490 の波 A（領域 selection: selectionStateMachine（nothingSelected ／ objectsSelected）、selection-values.ts、表 T-293、図 F-039）
          構えの armModeStateMachine と直交する別の機械（JDG-289）。行・担当者・写したものは根の運ぶ値（chosenRows ・ chosenResources ・ copiedForPaste）。副作用 0。刈るのは書き込みの着地だけ（今の振る舞いを写す —— JDG-57）
          画面の値の原稿は変えない。台帳 DFC-695（Esc ・ Enter が行の選択を数えない）・DFC-696（消えた行が行の選択に残る）・DFC-700（IN-4 の注と FR-072 の食い違い）
          ⭐ CR-490 の第 11 節の分析が、別の変更要求にする新しい機械を挙げた —— 未保存の編集（FR-100）・操作の記録（FR-102）・Agent API の有効（FR-065）、それに欄の編集の機械（いま編集している欄。IF-9 の改訂が先で、利用者の判断が要る）。どれもそれぞれの CR で扱う
✅ 欄の編集  CR-500 の波 A（領域 fieldEntry の fieldFocusWantStateMachine を fieldEditStateMachine（idle ／ fieldFocusWanted ／ editingField、運ぶ値 fieldRow）へ広げた。表 T-292・図 F-038 を刷り直し）
          欄ごとに状態を立てない —— 欄は運ぶ値の行 ID（JDG-290）。出来事 fieldFocusLanded を退け fieldEditBegan ・ fieldEditEnded を足した。ガード isEditedField。IF-9 は真偽を答えるのをやめ、編集の始まりと終わりを知らせる
          ⛔ 波 B: 面（dom-screen-surface.ts の文字入力の欄の focusin ・ focusout）が知らせを溜めて返し、シェルは状態を読む前に溜まった知らせをすべて機械へ渡すこと。
             それまで仕様（IF-9 の知らせ）とコード（hasUnsettledTextEntry の真偽）は食い違う。台帳 DFC-701（作った直後の Enter がどの欄を確定したかを見ない）・DFC-702（確定も取り消しも通らずに欄が消える道）、DFC-694 に追記
✅ 未保存の変更  CR-510 の波 A（領域 fileFlow に unsavedEditsStateMachine（nothingUnsaved ／ editsUnsaved）を足した。表 T-290・図 F-036 を刷り直し）
          CR-460 の決定 9 の hasUnsavedEdits の半分を覆した。fileSavedAt は状態でも運ぶ値でもない（FR-101 ／ AT-140、DFC-459）。台帳 DFC-780〜784
✅ 操作の記録  CR-520 の波 A（第 7 領域 interactionRecord: interactionRecordingStateMachine（notRecording ／ recordingInteractions）、
          interaction-record-values.ts（UF-124）、表 T-295・図 F-041）。記録の中身はシェルに残す（CR-490 の 11.1 の 4 行目を覆した）。台帳 DFC-785・786
          ⚠️ 波 B: シェルの溜め const interactionRecord は領域のキーと同じ名 —— 結線のときに改名せよ
✅ Agent API  CR-530 の波 A（第 8 領域 agentApi: agentApiEnablingStateMachine（disabled ／ enabled）、agent-api-values.ts（UF-125）、
          表 T-296・図 F-042）。起動で読んだ記憶は出来事 rememberedEnablingLoaded、記憶へ書くのは根の副作用 storeAgentApiEnabling。
          dialogueLog と REGISTRATIONS は外（SF-10 に 1 文）。台帳 DFC-787・788
          ⚠️ 仕様の空白: 同じ出来事で根の升と機械の升の両方が副作用を出すとき、どちらが先かを表 T-284 ・ T-285 も SS-5 も書いていない
             （コードは根が先）。契約試験は順を問わない。直すなら T-284 に 1 行を足す変更要求を先に
⬜ 引き受けた後続（2026-09-22、裁定の仕分けのセッションから。JDG-300〜302、CR-541 の本文が正 —— 着地を確かめてから 1 本ずつ別のコミットで）
          H2 表示しないタスクを選べない（Q7、DFC-683）—— selection-values.ts ／ H3 長押しの繰り返しとファイルのドロップでフレームを起こす
          （Q17、表 T-078 の FT-4 は 4 つ、DFC-569、PND-446 の STOP）／ H4 複数のタスクを写して元の行へ貼る（Q16、CM-8 の sourceUids、
          PND-449 の STOP 2 つ）／ H7 PND-181 の STOP を消す（Q22）／ H9 表示倍率・行ズームの端で FR-029 の通知を出さない（Q9、
          シェル側で出しているときだけ）／ H10 PR-16 を担当者ごとに 1 行（CR-547 の後）
          H1（SK-17 を原稿から外す）・ H6（表 T-283 に RG-14）は裁定の仕分けのセッションが wave 1（a131ca8d まで）で入れた ⇒ 着地を確かめよ。
          H8（frame-loop.ts の @provisional 5 つ）は同じセッションの CR-549 が入れる予定（コメントだけ）。H11 は知らせのみ
⭐ CR-490 の 11.7 が挙げた 3 つの機械はすべて波 A まで当てた。残る「いま編集している欄」は CR-500 が当てた。⚠️ T-294 ・ F-040 は空いたまま
          ⚠️ 番号の帯（2026-09-21 に本線と決めた）: こちら 図 F-035〜F-039（F-039 まで使用 —— 使い切り。F-037 は CR-470 が返した）・UF-88〜UF-89（使い切り）。
             それより先の番号と CR の番号は、本線のセッションに宣言してから取れ（本線が終了していたら本書に書いて残せ）
          ⚠️ 図の帯: こちら F-035〜F-039（F-035 は身振り）、本線 F-030〜F-034、F-040 以降は取る側が宣言してから
⬜ 移すたびに LM-19 の手順で性能を測る（SF-3 が NFR-010 を負う）
```

⚠️⚠️ **性能を測る前に利用者を呼べ** —— 性能の測定は `RISK-001` の門である。測っているあいだ重い体を出すな。

⭐ **次の領域の原稿と遷移のユニットは、段 5・6 と並行してよい** —— 触るのが `docs/spec/_source/` と `src/use-case/advance-screen-session/` だけだからである。⛔ シェルへの結線は並行できない。
⚠️ 台帳に 6 行を足した: `DFC-679`（通知が描かれる前の約 1 フレーム、Esc 1 回が 2 段を使う疑い）・`DFC-680`（`IR-3` と `FR-076` の語の食い違い）・`DFC-681`（`NT-3` は件数の無い通知の束ね方を語らない）、それに `DFC-676`（2 つのキャッシュがフレームをまたぐ。表 T-071 と食い違う）・`DFC-677`（`FR-072` は戻り先が無いときを語らない）・`DFC-678`（`t-233` の契約試験の RS-61 の赤 2 件。`CR-433` の裁定待ち）。

⭐ **2026-09-21 に反証した —— 旧版がここで「利用者の裁定を呼ぶ」とした 3 つは、どれも導けた**（`handoff.md` §0.2 の 3 手）:
- **領域の切り方** —— 計画の記録 4 が 10 領域と移す順（最初は「画面の値」）を置き、`CR-379` の決定 3 が最初の領域を「画面の値」と定めている。領域の中の独立な軸は、軸ごとに判別共用体を 1 つ立て、`SF-8` の合成で並べる。
- **どれが状態でどれがフレームの値か** —— `SF-5`（座標・寸法・時刻はフレームの値）と、表 T-250 の前置き（原稿に載せるのは要求が名指すものだけ）を 1 つずつ当てれば決まる。
- **出来事の粒度** —— `SF-5` の後段（押す・掴む・答える、および指すものが変わったときだけ）が決める。
⇒ **問うのは `PND-451` の 1 件だけになった**（出していないプロパティパネルの境界を押したら何が起きるか。`SD-3` の契約試験は、表に無い組では同じ参照を返すことを求めるので、仕様の行が無いかぎり今の振舞いをコードにも原稿にも置けない）。
⚠️ **名の衝突**: `CR-379` の決定 4 が集約の型に選んだ `ScreenSession` は、`src/adapter/screen-renderer/screen-renderer.ts:402` に描き手の入力の型として既に在る（決定 4 は `docs/spec` しか測っていなかった）。決定 4 は「覆してよい」ので裁定は要らない ⇒ `CR-436` が片づける。
⭐ **この続きは `change-request/CR-436-*.md` が持つ** —— 2b-B と第 1 領域「画面の値」の原稿の行・波の分け方（A は当てた、B1 ・ B2 は段 5・6 待ち）。`ScreenSession` の衝突は「移行」で片づける（第 4 節。波 B2）。

---

## 4. ⛔⛔ 門を先に読め —— 破ると即赤になる

**2026-09-21 に入った。⛔ どれも自分の仕事を止める。**

| 検査 | 何で落ちるか |
|---|---|
| **60** 大きさのラチェット | 帯 = 50 行 / 15 分岐。総量が上がれば赤。名簿の関数が太れば赤。帯に入った新しい関数が名簿に無ければ赤。⭐ **縮んで帯を出た行が名簿に残っても赤** ⇒ **割る commit の中で名簿を書き直す** |
| **61** モジュール状態 | 内側 3 層に 3 個目を足すと赤。⭐ **`SF-7` を機械が見張っている** |
| **59** コード ⊆ 図 | `components.json` に無いコンポーネント間の辺を作ると赤 |
| **62** 識別子の予約 | 変更要求が「表の最大は `T-nnn`」「接頭辞は `N` 件」と書いたら、**当てる直前に測り直して日付を添える**（規則 `02-changing-the-spec.md` §2.5）。⚠️ **賞味期限は 1 コミットである** —— 2026-09-21 に 3 回発動した |
| **19** 層の規則 | `.json` を読む辺にも掛かる（`EG-8` / `JF-1`〜`JF-4`） |

⛔⛔ **基準線ファイルを体に触らせるな。** 自動判定が「CI の回避」として止める。
⇒ **数を利用者に見せてから、前に立つ者が書く。**

---

## 5. ⚠️ いま赤いもの —— あなたのせいではない。直すな

```
check.sh   red: 23 39      ⛔ どちらも別セッションの CR-433 由来。利用者の裁定待ち
型検査      8 件            tests/unit/uf-45-46.test.ts（DFC-655、既知）
```

⭐ **commit の前後で「赤の集合が同じ」ことを示せ。** ⇒ **増やしていないことの証明になる。**
⛔ 赤を跨いで commit を積むと、次の赤が新しいのか前からなのか読めなくなる。

---

## 6. ⛔⛔ 衝突しない約束

⚠️⚠️ **2026-09-21 に 2 つのセッションが実際に衝突した。**
どちらも木を読んで「木が使う表の最大はこれである」と結論し、**同じ表番号を提案した**。
片方が着地した瞬間、もう片方の根拠は偽になっていた。
⛔ **番号の帯を依頼文で配る手は、1 つのセッションの中の体までしか届かない** —— 別のセッションの草案は互いに見えない。

```
番号の帯   あなた: 表 T-280 以降 ／ FR-120 以降
           相手  : T-276 〜 T-279（CR-432 が T-276、CR-435 が T-277 を使用済み）

持ち場     あなた: docs/spec/_source/state-machines.json（新規）
                   src/use-case/advance-screen-session/（新規）
                   あなたの変更要求
           相手  : src/adapter/ ・ src/framework/ ・ 段 4 の 4 ファイル
                   （edit-task.ts / edit-task-group.ts / svg-renderer.ts / import-document.ts）
```

- ⛔ **`git add` はパスを名指しのみ。** `git add -u` / `-A` はフックが止める
- ⛔ **`git stash` をしない** —— 相手の変更を巻き込む
- **commit の前に相手のセッションへ 1 通送れ** —— HEAD が動くと、相手が測った行番号がずれる
- ⛔ **push の前に「そちらのローカルに、まだ push していないコミットがあるか」を尋ねろ** —— 2026-09-21、こちらが `origin/refactor` へ早送りで push した同じ時に、本線は根の `refactor` に 3 コミットを積んでいた。どちらも同じ親から伸び、本線が載せ直すことになった（強制 push はしない —— 公開の履歴を書き換える）
- ⚠️ **`check.sh` を根で走らせると相手の途中の変更を読む** ⇒ **自分の作業木で確かめろ**
- ⚠️ 相手のセッションが終了していることがある。⇒ **裁定待ちの引き継ぎは本書か `handoff.md` に書け**（メッセージは届かない）

---

## 7. 読む順

```
1  docs/development-records/handoff.md の §0        ⛔⛔ ここから始めろ
2  rulings.md の JDG-45 ・ JDG-55 ・ JDG-60
3  docs/spec/05-07-design.md の 5.6（5.6 の 2 つ目の設計の決定）と 表 T-249
4  refactor-plan-report-2026-09-13.md の 記録 12・13・14 と 状態表
5  refactor-stage1-state-inventory-2026-09-13.md（保存しない状態の棚卸し）
6  docs/development-rules/README.md                 ⛔ 規則を読まずに書くな
```

---

## 8. 毎回の約束

- ⛔ **利用者に問う前に `rulings.md` を grep しろ。**「条項が無い」は `handoff.md` §0.2 の 3 手で反証してから
- **実装・試験・台帳は体に出せ。前に立つ者は書かない**（判断は重い模型、機械的な作業は軽い模型）
- ⛔ **体の報告は仮説である** ⇒ `file:line` で反証させてから使え
- ⛔ **検査は終了コードで判じろ。`FAIL` を grep するな** —— `VIOLATION` と出す検査もあり、語を外すと赤い走行で 0 件と読める
- **壊し試験は合流した木で自分で数え直せ。**⛔ 渡された見立てを写すな
- ⛔ **生成物を手で編集するな** —— `npm run gen` が作るものは生成器が正である
- commit ／ push は許可済み（`JDG-58`）。⛔ **個人情報の点検の数を報告してから**行え
- 説明は日本語。**コードとコミット文は英語 ASCII**（公開リポジトリ）
- Windows: python が日本語を出すなら `PYTHONIOENCODING=utf-8`。⛔ スクリプトはヒアドキュメントではなくファイルに書いて走らせる。⚠️ 改行は CRLF と LF が混ざる ⇒ 読みで正規化し、書き戻しは多数派に揃える
