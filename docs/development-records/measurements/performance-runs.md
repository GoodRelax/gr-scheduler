# 性能の走行の記録 —— `GRS_PERF=1` で回した 1 走行につき 1 行

**規則は [`docs/development-rules/04-verification.md`](../../development-rules/04-verification.md) の 5 節（表 `PW`、`JDG-605`）。本書は置き場と書式だけを定める。**

⭐ **1 走行 1 行、追記のみ。** 過去の行を書き換えない。
⭐ 測る時期は `PW-1`、測り方は `PW-3`（他のセッションが止まっているとき、同じ dist の sha で 1 回）、利用者を呼ぶ場面は `PW-4` が持つ。
⭐ **緑の走行は、ここに 1 行残して事後に報告する。** まとめて測った変更要求の行は `../perf-pending.md` から消す。
⛔ 数の原本は走行の出力である。本書は合否と段 0 との差だけを持つ（`JDG-50`）。

## 一覧

| # | 日付 | dist の sha | 合否 | 段 0 との差（`JDG-50`） | まとめて測った変更要求 |
|---|---|---|---|---|---|
| 1 | 2026-09-26 | `97ee9405…`（`89bceda4` の木で `npx vite build`。コミット済みの `dist/index.html` `8e6b6105…` は `startup-template.json` の変更より前のビルドで古い） | ⛔ **赤** | 段 0 `5c1b915`（`8460eaa9…`）と交互に 3 回ずつ（記録 17 の手順、3 回の最小〜最大、ms）。`lm-19` 全体のフレーム時間 中央値 16.67〜21.27 → 24.25〜30.2、p95 30.2〜46.6 → 102.9〜116.66。`MK-1` スクロール 中央値 18.2〜19.68 → 90.65〜97、p95 30.5〜33.33 → 109〜133.33。`MK-2` ズーム 中央値 23.2〜30.3 → 78.65〜90.8、p95 30.4〜48.5 → 103〜121.2。`MK-7` パン・`MK-6` 範囲選択は重なる。Ctrl＋ホイール 1 ノッチの同期ディスパッチ 中央値 7.9〜13.85 → 27.8〜30.1。trace 19.16〜19.48 → 28.42〜37.82 ms/フレーム（描いた要素 493 → 989）。`tests/nfr` の性能の門 2 本も赤（`PG-8`・`PG-10`・`PG-12`、`MK-1`・`MK-2`・`MK-7`・`MK-6`）。機械: i7-12650H、Windows_NT 10.0.26200、node v24.12.0、msedge 154.0.4258.37 headless、1920×1080、`Task` 1000 | `CR-570`・`CR-573` —— 赤なので `../perf-pending.md` に残す（`PW-4` の ①） |
| 2 | 2026-09-26 | `bf503c5b…`（`refactor` の `7b775e8a` にコミット済みの `dist/index.html`。`DFC-1131`・`DFC-1132` の合流 `4894dd61` の後） | ✅ **緑**（`JDG-726` の同じ量での比べ） | 段 0 `5c1b915`（`8460eaa9…`）はその既定のまま、比べるビルドは `--display-scale 175` で、`tools/probe/examples/lm-19-frame-time-baseline.mjs` を同じ日に交互に 3 回ずつ（3 回の最小〜最大、中央値 / p95、ms。生の行は `stage8-root-cause-2026-09-26/slot6-equal-volume-run-log.txt`）。全体 12.35〜16.67 / 24.4〜30.1 → 16.67 / 24.2〜24.3。`MK-1` 18.2 / 30.2〜30.3 → 16.67〜17.35 / 24.3〜24.4。`MK-2` 18.3〜18.35 / 24.4〜30.3 → 18.1〜18.2 / 24.3〜24.4。`MK-6` 12〜12.1 / 12.3〜16.67 → 7.53〜7.63 / 16.67。⚠️ `MK-7` パンは中央値が 12.1 → 17.23〜18.1 と 1 vsync 分高い（p95 は 24.3〜24.4 → 19.63〜24.3 で悪くない）。記録 17 の読み（全体の中央値と p95、各区間の p95）では段 0 より悪くない。記録: 同じ dist で調整役が回した `GRS_PERF=1` の `tests/nfr`（既定の倍率 100）は赤 —— `MK-1` p95 42.4、`MK-2` p95 84.6、`PG-8` p95 96.9、`PG-10` p95 84.7 ms。絶対値の門は段 0 でも赤であり、合否に数えない（04 の `PW-3` の ②、`PND-443`） | `CR-570`・`CR-573`・`DFC-1131`・`DFC-1132` —— `../perf-pending.md` から消した |

⚠️ 走行 1 の赤の原因の調べ（探り針。`GRS_PERF=1` の走行ではないので上の表に行を足さない）は [`stage8-root-cause-2026-09-26/README.md`](stage8-root-cause-2026-09-26/README.md) が持つ —— 数ごとに測り方を添えてある（`DFC-1131`・`DFC-1132`）。
