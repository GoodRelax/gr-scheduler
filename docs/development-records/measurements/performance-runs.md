# 性能の走行の記録 —— `GRS_PERF=1` で回した 1 走行につき 1 行

**規則は [`docs/development-rules/04-verification.md`](../../development-rules/04-verification.md) の 5 節（表 `PW`、`JDG-605`）。本書は置き場と書式だけを定める。**

⭐ **1 走行 1 行、追記のみ。** 過去の行を書き換えない。
⭐ 測る時期は `PW-1`、測り方は `PW-3`（他のセッションが止まっているとき、同じ dist の sha で 1 回）、利用者を呼ぶ場面は `PW-4` が持つ。
⭐ **緑の走行は、ここに 1 行残して事後に報告する。** まとめて測った変更要求の行は `../perf-pending.md` から消す。
⛔ 数の原本は走行の出力である。本書は合否と段 0 との差だけを持つ（`JDG-50`）。

## 一覧

| # | 日付 | dist の sha | 合否 | 段 0 との差（`JDG-50`） | まとめて測った変更要求 |
|---|---|---|---|---|---|
