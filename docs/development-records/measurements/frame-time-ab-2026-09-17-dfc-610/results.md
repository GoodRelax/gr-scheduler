# DFC-610 の A/B —— 段 0 `5c1b915` 対 `d13dd257` 対 `5c723c6d`（2026-09-17）

⭐ 裁定: `JDG-135`（段 0 と交互に A/B し、悪くなければ閉じる）、`JDG-50`（段 0 より悪くしない）、`JDG-166`（留守中に走らせてよい。数は測り方つきで）。
測り方は `docs/development-records/refactor-plan-report-2026-09-13.md` の記録 17 とその追補に従う。

## 1. 比べたビルド

| ラベル | コミット | 何か | `dist/index.html` の sha256（ビルドし直した値） |
|---|---|---|---|
| `stage0-5c1b915` | `5c1b915` | 段 0（記録 17 の手順 1 が指すコミット。状態表の段 0 の閉じは `cdba2292`） | `8460eaa9…` —— 記録 17 の値と一致 |
| `pre-d13dd257` | `d13dd257` | 単一の解き手に変える直前 | `7de9ba22…` —— コミット済みの dist と一致 |
| `cand-5c723c6d` | `5c723c6d` | 候補（`rowBandCeilingOf` を唯一の解き手にした） | `8e75f056…` —— コミット済みの dist と一致 |

- 木: 各コミットを `git worktree add --detach .claude/worktrees/ab-<sha>` で出し、その中で `npx vite build`。node_modules はつながず、解決はリポジトリの根の node_modules へ上る（記録 17 の「git archive の写し＋node_modules をつなぐ」からの変更点。段 0 の sha256 が一致したので同じビルドである）。
- 全 36 本の JSON の `conditions.buildSha256` が上の値であることを `run-log.txt` で確かめた。

## 2. 機械と条件

| 項目 | 値 |
|---|---|
| 日時（UTC） | `2026-09-16T23:15:33Z`〜`2026-09-17T00:33Z`（1 台、同じ日、約 78 分） |
| 機械 | CPU `12th Gen Intel Core i7-12650H`（論理 16）、`Windows_NT 10.0.26200`、node `v24.12.0` |
| ブラウザ | msedge `153.0.4234.32` headless、ANGLE Intel UHD D3D11 |
| 機械の静けさ | 走行前の CPU 負荷 4%。ほかの体は走らせていない。⚠️ OneDrive の同期と利用者の常駐（タブレット・マウスのドライバ等）は動いていた。生の JSON は走行中は OS の一時フォルダに書き、終わってから本フォルダへ写した（同期の揺れを走行中に起こさないため） |
| 文書 | 起動テンプレート（`Task` 1000 件）。`folded` は起動後に `IC-78`（行を全部畳む、`setLevelZeroFolded`）を押した文書 —— 押した後 `Row Expander` の数が 0 になることを全ページで確かめた |

## 3. 探り針と交互の順

`run-ab.py`（本フォルダ）が 3 回まわした。1 回の中で探り針ごとに 3 本の木を順に走らせ、順は回ごとに 1 つずらした。

| 回 | 順 |
|---|---|
| 1 | stage0 → pre → cand |
| 2 | pre → cand → stage0 |
| 3 | cand → stage0 → pre |

| 探り針 | 置き場 | 何を測るか |
|---|---|---|
| `zoom-row-axis` | `../frame-time-ab-2026-09-16/extra-zoom-row-axis-and-dependency-drag/zoom-row-axis.mjs`（変えずに使った） | 1920×1080 と 1920×400 で 50 ノッチ上げて上限に着け、上限での区間・6 ノッチ下の区間（24 個ずつ束ねた Alt＋ホイール、240 間隔）と単発（種類ごとに 10 回） |
| `first-notch` | 本フォルダの `first-notch.mjs`（新規） | 開いたばかりのページへの **初回の 1 ノッチ** と、続く 5 ノッチ（Alt＋ホイール 1 ノッチ上げ、1 入力ずつ 700 ms 空ける）。条件ごとに新しいページ 6 枚 → 初回 6 標本・繰り返し 30 標本。1920×1080 / 1920×400 × template / folded |
| `dependency-drag` | `../frame-time-ab-2026-09-16/…/dependency-drag.mjs`（変えずに使った） | 対照。⛔ 下の 5 節 |
| `lm-19` | 各木の `tools/probe/examples/lm-19-frame-time-baseline.mjs`（3 コミットで同一） | 対照。MC-8 の 4 区間と、Ctrl＋ホイール 1 ノッチ（MK-2）の書き込み費用 |

時間の取り方（どの探り針も同じ）: 同期ディスパッチ ＝ 同じ入力イベントの window capture から window bubble まで（`zoomTimes` はこの中で走る）。入力→描き直しの呼び出しの終わり ＝ 入力の後に最初に入った rAF の呼び出しが終わるまで（計器は各木の `tests/nfr/nfr-002-003-frame-time-is-the-interval.test.ts` の `FRAME_PROBE`。3 コミットで同一）。中央値と p95（最近順位法）は探り針が数え、表は 3 回の最小〜最大を書く。

## 4. 結果（3 回の最小〜最大、ms。`summary.txt` から写した）

### 4.1 初回の 1 ノッチと繰り返し（`first-notch`）

| 窓 | 文書 | 入力 | 指標 | 段 0 中央値 | 段 0 p95 | pre 中央値 | pre p95 | 候補 中央値 | 候補 p95 |
|---|---|---|---|---:|---:|---:|---:|---:|---:|
| 1920×1080 | template | 初回 | 同期ディスパッチ | 13.45〜15.85 | 18.3〜20 | 11.55〜15.25 | 15.4〜18.2 | **202〜207.75** | **205〜217.4** |
| 1920×1080 | template | 初回 | 入力→呼び出しの終わり | 29.55〜31.65 | 34〜36 | 46.15〜49.3 | 48.9〜55.3 | **235.2〜241.1** | **237.7〜255.5** |
| 1920×1080 | template | 繰り返し | 同期ディスパッチ | 15〜15.8 | 17.7〜20.4 | 2.9〜3.25 | 16.9〜19.8 | **196.1〜196.7** | **211.3〜214.1** |
| 1920×1080 | template | 繰り返し | 入力→呼び出しの終わり | 26.35〜26.8 | 29.9〜31.3 | 14.7〜15.95 | 38.3〜41.7 | **204.15〜213.75** | **231.3〜235.1** |
| 1920×400 | template | 初回 | 同期ディスパッチ | 12.55〜14.7 | 16.7〜21 | 12.1〜15.85 | 16.7〜19.6 | **164.8〜171.45** | **170.8〜180.7** |
| 1920×400 | template | 初回 | 入力→呼び出しの終わり | 26.85〜28.3 | 31.2〜35.8 | 30.05〜33.85 | 33.9〜37.4 | **181.9〜188.9** | **188.3〜198.9** |
| 1920×400 | template | 繰り返し | 同期ディスパッチ | 1.75〜2.5 | 18.1〜19.8 | 2.75〜3.6 | 16.5〜18.2 | **165.95〜166.2** | **174.1〜174.6** |
| 1920×400 | template | 繰り返し | 入力→呼び出しの終わり | 13.7〜17.4 | 28.3〜30.4 | 11.8〜16.25 | 27.8〜30.9 | **172.85〜176.05** | **182.8〜185** |
| 1920×1080 | folded | 初回 | 同期ディスパッチ | 2.9〜3.6 | 3.4〜4 | 3.3〜4.7 | 4.2〜5.6 | **28.05〜29.8** | **28.7〜33.1** |
| 1920×1080 | folded | 繰り返し | 同期ディスパッチ | 1.3〜1.7 | 1.6〜2.1 | 0.6〜0.7 | 2.7〜3.1 | **25.55〜26.45** | **30.4〜34.5** |
| 1920×400 | folded | 初回 | 同期ディスパッチ | 2.3〜3.7 | 3.7〜4.6 | 3.65〜4.8 | 4.3〜5.2 | **25.05〜29.2** | **29.3〜32.3** |
| 1920×400 | folded | 繰り返し | 同期ディスパッチ | 1.2〜1.7 | 1.9〜2.1 | 0.55〜0.75 | 2.1〜3.2 | **23.85〜26.85** | **29.4〜33.6** |

### 4.2 上限での連続と 6 ノッチ下の連続（`zoom-row-axis`、区間、240 間隔）

| 窓 | 区間 | 指標 | 段 0 中央値 | 段 0 p95 | pre 中央値 | pre p95 | 候補 中央値 | 候補 p95 |
|---|---|---|---:|---:|---:|---:|---:|---:|
| 1920×1080 | 上限（1 下げ / 2 上げ） | フレーム時間 | 7.5〜7.7 | 14.3〜16.3 | 7.4〜8.1 | 15.2〜16.67 | **171.7〜173.05** | **183.33〜216.66** |
| 1920×1080 | 上限（1 下げ / 2 上げ） | 同期ディスパッチ | 0.5〜0.6 | 0.7〜0.9 | 0.8〜0.9 | 0.9〜1.2 | **164.3〜166.4** | **170.7〜178.2** |
| 1920×1080 | 6 ノッチ下（1 上げ / 1 下げ） | フレーム時間 | 7.4〜7.7 | 8.3〜15.23 | 7.4〜7.7 | 8.3〜14.4 | **171.8〜175.02** | **185.3〜333.32** |
| 1920×1080 | 6 ノッチ下（1 上げ / 1 下げ） | 同期ディスパッチ | 0.5〜0.6 | 0.7〜0.8 | 0.6〜0.7 | 0.9 | **164.6〜167.1** | **170.6〜180** |
| 1920×400 | 上限（1 下げ / 2 上げ） | フレーム時間 | 7.4〜7.7 | 8.3〜14.5 | 7.3〜7.4 | 8.3〜8.4 | **141.45〜141.6** | **152.3〜166.66** |
| 1920×400 | 上限（1 下げ / 2 上げ） | 同期ディスパッチ | 0.5 | 0.6〜0.8 | 0.7〜0.8 | 0.9〜1.1 | **135.4〜136.9** | **138.7〜153.2** |
| 1920×400 | 6 ノッチ下（1 上げ / 1 下げ） | フレーム時間 | 7.2〜7.3 | 8.1〜8.2 | 7.3〜7.4 | 8.1〜8.3 | **140.85〜141.7** | **150〜153.9** |
| 1920×400 | 6 ノッチ下（1 上げ / 1 下げ） | 同期ディスパッチ | 0.4 | 0.5〜0.7 | 0.6〜0.7 | 0.7〜1.2 | **135〜137** | **139.3〜143.5** |

単発（10 回ずつ）も同じ形である —— 候補は 1920×1080 のどの単発入力も同期ディスパッチ中央値 162.55〜175.35 ms、1920×400 は 132.85〜145.6 ms。段 0 は 0.5〜18.05 ms、pre は 0.8〜14.95 ms（全 28 行は `summary.txt`）。

上限の zoomY（`readDocument()`、3 回とも同じ）: 段 0 は 1920×1080 で 3.1036、1920×400 で 1.148。pre と候補はどちらの窓でも 1.3201（字の上限）。
⚠️ pre と候補の `zoom-row-axis` は 6 本とも `ok false` —— 理由はどれも「1920×400 で行の領域の上限が効かない」という探り針自身の前提の外れであり、区間と単発はすべて測れている（表に含めた）。

### 4.3 対照 —— LM-19（`lm-19`）

| 指標 | 段 0 | pre | 候補 |
|---|---:|---:|---:|
| MK-2（Ctrl＋ホイール 1 ノッチ）同期ディスパッチ 中央値 / p95 | 10.4〜14.2 / 17.1〜18.7 | 9.1〜9.3 / 14.2〜15.3 | **171.35〜176.9 / 177.3〜192.7** |
| MK-2 入力→呼び出しの終わり 中央値 / p95 | 18.8〜23.8 / 27.3〜30.1 | 16.1〜27.25 / 25.1〜34.3 | **179.75〜185.25 / 185.9〜201.3** |
| MK-2 zoom 区間 フレーム時間 中央値 / p95 | 21.6〜22.1 / 30.03〜33.33 | 170.06〜196.83 / 299.99 | 209〜210.55 / 266.66〜270.71 |
| MK-1 scroll 区間 フレーム時間 中央値 / p95 | 16.67〜18.72 / 30.8〜31 | **183.33〜196.87 / 299.99〜302.34** | 183.33〜193.25 / 311.1〜316.65 |
| MK-7 pan 区間 フレーム時間 中央値 / p95 | 7.75〜8.2 / 23.5〜29.5 | 16.78〜22.7 / 100〜225.71 | 15.95〜22.75 / 44.5〜260.8 |
| MK-6 range select 区間 フレーム時間 中央値 / p95 | 7.9〜8 / 15.6〜16.67 | 7.4〜8.3 / 8.2〜16.67 | 7.6〜7.8 / 8.2〜8.3 |
| 描き直しの呼び出しの中（全区間）中央値 | 8.8〜9.2 | 16.05〜17 | 14.6〜17.3 |

⚠️ MK-1 scroll と MK-2 zoom の区間のフレーム時間は、**pre の時点で既に** 段 0 の約 10 倍である（3 回とも）。呼び出しの中は 16〜17 ms なので、増分は呼び出しの外（style / layout / paint か、フレームの間引き）にある —— 本測定では割っていない。候補の変更が加えたのは MK-2 の書き込み費用（同期ディスパッチ 9 → 175 ms）である。

## 5. 測れなかったもの

- **`dependency-drag`（対照）**: 段 0 は 3 回とも測れた（空き地の上 8.1 ms / p95 16.67）が、pre と候補は 6 本とも `the run broke: no drag start drew an extra arrow over a partner (16 origins, 0 starts)`。setup は全起点で `no ew-resize beside the finish edge`、`finishX` がどれも 1001、`pxPerDay` 6 —— 表示の倍率の波の後の描き方に探り針の起点探しが合っていない（pre でも同じなので候補の変更のせいではない）。探り針は直していない。
- **2 行に畳んだ文書**: 3 コミットのどれにもある入力の道を見つけられず、測っていない。
- DFC-628 の数（初回 108.6/127.9 → 174.6/219.1 ms）は Node の同一プロセスで殻の道を測ったものであり、本表（実機のブラウザ、同期ディスパッチ）とは測り方が違う。直接比べない。

## 6. 本フォルダのファイル

- `run-ab.py`・`first-notch.mjs` —— 回し方と新しい探り針
- `run<i>-<probe>-<label>.json` —— 36 本の生の JSON（3 回 × 4 探り針 × 3 木）
- `run-log.txt`（走行ごとの ok・sha・notMeasured・上限）、`summary.txt`（全区間・全指標の表）
