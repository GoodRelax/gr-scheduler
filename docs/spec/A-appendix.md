# gr-scheduler 仕様書 — 付録

**Grammar**: spec.sgra
**UID**: DOC-APPENDIX
**Version**: 0.1

付録はどの章範囲にも属さないので、席番号を `A` とする独立したファイルにしてある。

> **この文書はまだ器である。** 未記入の節は引用で始まる行を持つ。

## A.1 References (参考文献)

**Type**: SECTION

> 未記入。標準規格と外部資料へのリンクを書く。MSPDI の正は公式 URL `https://schemas.microsoft.com/project/2007/mspdi_pj12.xsd` を引き、ローカル複製のパスを正として書かない（複製は cache であって正ではない。`docs/reference/README.md`）。

## A.2 Licenses (ライセンス)

**Type**: SECTION

> 未記入。依存ライブラリのライセンスを書く。単一 HTML に同梱される実行時依存は、出力にコードが載るので全数を挙げる。

## A.3 Changelog (変更履歴)

**Type**: SECTION

| 版 | 日付 | 変更内容 |
| --- | --- | --- |
| 0.1 | 2026-08-12 | 器を作成。章節の見出しと `**Type**: SECTION` のみ。要求ノードは未記入 |
| 0.2 | 2026-08-12 | 第 1 部の Chapter 1（基本事項）と Chapter 2（システム概要）を記入。`GOAL` 6 件、表 T-001 〜 T-009、図 F-001。表と図の参照規約を 1.9 に追加 |
| 0.3 | 2026-08-12 | 用語の正を本仕様書へ移した（`_assets/tbl-glossary.md`、確定名 約 165 語）。命名・記法・曖昧語の規約を 1.9 に追加。Chapter 3（ユースケース）を記入 —— アクター 4 件・`USE_CASE` 12 件・要望 66 項目との対応表 T-011。透かしを受ける `GL-007` を追加 |
| 0.4 | 2026-08-12 | 設定値の正を本仕様書へ移した（`_assets/tbl-settings.md`、表 T-201 〜 T-206 の 99 行）。既定値・下限・上限・範囲の理由と、由来の無い既定値の印を引き継いだ。Chapter 6.2 は値を再掲せずここを指す。MSPDI の列挙値は写さず公式 XSD を出典として引く |
| 0.5 | 2026-08-12 | Chapter 4（要求）を記入。`FUNC_REQ` 30 件・`NON_FUNC_REQ` 9 件・表 T-012 〜 T-026。性能は目標値と測定条件のみを定め、基準線は骨格で取り直す。削減候補は「紐づけ先が無いもの」ではなく「根拠が弱いもの」5 件 |
| 0.6 | 2026-08-13 | レビューを 10 周かけて第 1 部へ反映。`FUNC_REQ` 96 件（`FR-050` は廃止したので欠番）・`NON_FUNC_REQ` 12 件・`USE_CASE` 14 件・`GOAL` 7 件。本日線と基準線を基準日線 1 本へ統合し、合流の対応付けを 4 択（表 T-032a）、開くときの扱いを 3 択（`FR-087`）に決めた |
| 0.7 | 2026-08-13 | Chapter 7 に性能ゲートを置いた（表 T-042 節目・表 T-043 節目ごとに測るもの）。**Chapter 5 と Chapter 6 に一度記入した設計は、利用者の判断で破棄し器へ戻した。** 破棄した設計が使った席番号 `T-044` 〜 `T-047` と `F-002` 〜 `F-007` は使用済みであり、別のものに割り当て直してはならない（MUST NOT） |
| 0.8 | 2026-08-14 | 変更要求 CR-101 〜 CR-114 を第 1 部へ反映。製品名を `GoodRelax Scheduler`（`GRS`）とし、EARS の主語を置き換えた。表記の規約を 3 語の一覧から境界の規則へ改め（表 T-006a の `W-8` 〜 `W-10`）、版数の判定を日程データの群と見せ方の群で行う形にした（`FR-063`）。`FR-098`（行のピン止め）と `NFR-013`（計算量）を新設。表 T-050（削除の連鎖）・T-051（行の折り畳み）・T-054（課題）・T-106（価値のことば）・T-216（日程データに属する値）を追加 |
| 0.9 | 2026-08-14 | データモデルを記入。文書ルートの群を 表 T-052 に、交換相手の木をそのまま持つ規則を 表 T-053 に置き（`FR-024` / `FR-021`）、Chapter 5.4 と 6.2 に概要 ERD（図 F-010）と詳細 ERD（図 F-011）と説明表（表 T-056 〜 T-059）を記入した。`_assets/fig-erd-overview.md` と `_assets/fig-erd-detail.md` を新設 |
| 0.10 | 2026-08-14 | データモデルが解いた要求側の宿題を閉じた —— `null` の列を鍵ごと書き出す規則（`FR-024`）、エンティティ名の英日対応（表 T-101 の `N-8` 〜 `N-14`）、複製の採番（`FR-033`）。変更要求 CR-114 の未反映 5 件を手で当て、ピン止めに操作面と削除の連鎖を与えた（CR-115。`FR-098` / 表 T-018a の `RT-6` / 表 T-050 の `CD-2` / `U-48`） |
| 0.11 | 2026-08-14 | 独立レビュー 3 体の指摘を反映。**保存する設定値 85 行の置き場が無かった**ので、見せ方の群を `_assets/tbl-settings.md` が持つ保存する値のすべて（表 T-216 を除く）とした（`FR-063`）。楽観ロックの照合を刻印の 3 つすべてに広げ（`AG-2`）、表示量のしきい値が版数を上げずに絵を変えることを `LM-15` として受け入れた。**担当者の名簿を新設**（CR-116。`FR-099` / 表 T-050 の `CD-5` / `U-49`）—— 割当を解いても担当者が文書に残り、消す入口が無かった |
| 0.12 | 2026-08-14 | 独立レビューが挙げた**設計を止める 4 件**を閉じた —— 貼り付けた `Task` は複製元と同じ行へ（`FR-033`）、行の非表示を文書に保存（`HR-6` と `TaskGroup.isHidden`）、**変更前の予定を文書に持つ**（`FR-015` / `OP-9` / `DR-2` が 12 鍵へ / `ET-18`）、**作成時の形状が `Task.milestone` を決める**（`FR-001`）。⚠️ 変更前の予定は**交換相手へ書き出さない** |
| 0.13 | 2026-08-14 | 判断を要しない 7 件を片づけた —— 採番の規則の持ち主を `FR-001` にし `FR-033` が指す形へ ／ 注記と表示位置を表 T-050 の `CD-2` の連鎖へ ／ `OP-3` の選択肢の数を 3 に直す ／ `Project.title` の日本語を `U-27` の確定名へ ／ `schemaVersion` がどのエンティティの列でもないことを明記 ／ `importSeq`（`S-71`）を表 T-202 から表 T-216 へ。**`docs/review/datamodel-findings` の 8 件はこれで全件対応済** |
| 0.14 | 2026-08-14 | Chapter 5.1 を記入。**Clean Architecture を採り**、`Entity` の内側を `documentModel` と `layoutEngine` に割った（表 T-060・表 T-061・図 F-012 を新設）。独立レビューの指摘を反映 —— **外側の道具は内側が宣言したインターフェースを介して使う**形にして依存の向きの自己矛盾を消し（`LR-5`）、`NFR-013` の測り方を表 T-025 の `MC-9` へ直し、純粋性に `semi-pure-b` を実在させた。`documentModel` の在庫を表 T-052 の 3 群すべてとし、**見せ方の群に層が無かった穴**を閉じた |
| 0.15 | 2026-08-15 | Chapter 5.2 を記入。**コンポーネント 31 件**（表 T-062）と、全体 1 枚・経路 4 枚の図（図 F-013 〜 図 F-017。`_assets/fig-components.md`）。⚠️ **図は `docs/review/components/model.json` を唯一の原稿とし、`build.py` が SVG とコンポーネント表を書き出す。手で直さない。** 全体図のクラスタ間の矢印はコンポーネントどうしの辺から導出し、**裏づけの無い矢印は描けない**。意図して満たさない条項 2 件（`R2.13` CQS ／ `R2.5` ISP）を Remark に残した |
| 0.16 | 2026-08-15 | 変更要求 CR-117 〜 CR-124 を反映。**自動保存は表 T-027 の `UN-8` に当たる操作を「編集」に数えず、待ち時間だけ張り直す**（`FR-026` / `S-112` = 3000 ms）。`MS-1` の 3 語を確定名へ揃え、`Row Area`（`U-50`）とファイル名の記法（`W-11`）を新設。**`Agent API` のメンバ 17 を 表 T-107 に置いた** —— 前プロジェクトの一覧は `AG-11`（発話を読む）を満たしておらず、改名 5・削除 2・新設 2 で組み直した。**現在値を保持するのは `Framework` だけと定め**（`LY-5`）、`documentModel` に保存しない実行時の値の型を置いて `Selection`（`CP-32`）と `DialogueLog`（`CP-33`）を足し、7 本目 `SnapshotSource` と 8 本目 `AppShellSource` を宣言して 17 メンバすべてに経路を通した（コンポーネント 33・辺 62・非巡回）。⚠️ **`_assets/fig-components.md` を廃止し、図 F-013 〜 F-017 を 5.2 の中へ畳んだ。** 図の原稿・生成物・生成器は `_assets/source/` が持つ。他文書への案内は `[LINK:]` で書く |
| 0.17 | 2026-08-15 | Chapter 5.3 を記入（CR-125）。**33 コンポーネントの公開インターフェースを宣言し**（表 T-064）、`src/` のディレクトリ構成と、1 つより多いユニットを持つ 5 コンポーネント（表 T-063）、層をまたぐインターフェース 8 本（表 T-065）を置いた。⭐ **`_assets/source/model.json` の 62 辺すべてが宣言メンバに着地することを機械で検算した** —— これで `LR-2` が初めて判定できる。**「公開面」は「公開インターフェース」と呼ぶ**（`R2.19` が定義の正とする用語集がリポジトリに無いので本節が定義を持つ。「面」は既に 3 義ある）。**どのコンポーネントもインスタンスを作らない**（`LY-5` の帰結）。**ユニットを割る基準は純粋性だけとし、それ以外で割った 2 件は行に理由を書いた。**⚠️ **図は描かない。**`AgentApiEndpoint` の 17 メンバは 表 T-107 を指すだけにした |
| 0.18 | 2026-08-15 | Chapter 5.3 が見つけた欠陥 6 件を一括で直した（CR-126）。⭐ **対話の輪が閉じた** —— `Agent API` に 18 番目 `postDialogueMessage`（`AM-18`・群「話す」）を置き、人の発話が `PostDialogueMessage` へ届く辺と、**発話が版数を上げずに監視を起こす辺**（`AG-11` の MUST）を足した。**`SingleHtmlShell` の走行を宣言し**、出次数を 3 → 9 にした —— 呼び手のいない公開メンバが 0 になり、**着地の検査が両方向になった**（辺 → メンバ 71／メンバ → 辺 未到達 0）。`InputCommandTranslator` に時間軸への辺と `dateAtX` を与え、**ファイルのハンドルの持ち主を `CP-28` に正した**（`LY-5` と `model.json` が既にそう書いていた）。コンポーネント 33・辺 62 → 71・**非巡回のまま**。⚠️ **要求の本文は 1 文字も変えていない** —— `AG-6` が既に「自分以外の書き手」と複数の話者を前提に書いており、足りなかったのは面と辺だけだった |
| 0.19 | 2026-08-15 | **データモデルの正を `_assets/source/erd.json` にした。**`erd_json_to_md.py` が `fig-erd-detail.md` を書き出し、`docs/review/erd/` の生成器一式（`.py` 3 本・散文の型 7 本）を廃止した。移行は**旧形式の 2 文書を JSON から組み直してバイト一致を確かめてから**行った。⭐ **図 F-011 を `flowchart` ＋ HTML 表から `erDiagram` に変えた** —— 鍵を持つ列は名前を太字にし、赤/緑の色分けは廃止した（`erDiagram` は属性ごとに着色できない）。描画後の図が 42,590 字から大きく縮み、**mermaid が黙って図を捨てる 50,000 字の上限から離れた。** **表 T-056 を `fig-erd-detail.md` へ移し**、`fig-erd-overview.md` は 図 F-010 だけを持つ手書きの文書にした。⚠️ **図 F-010 が `ET-` 番号でエンティティを指していたのをやめ、名前で指すようにした** —— 番号は表の中の位置なので、エンティティを足すと黙ってずれる。**説明の散文は Chapter 6.2 が持ち、生成される .md は 1 行も持たない** |
| 0.20 | 2026-08-15 | 図 F-010 も `erDiagram` にし、`fig-erd-overview.md` を生成物にした。⭐ **箱の枠が二重（mermaid のノード枠 ＋ HTML 表）でなくなり、`autogen.css` が表のセルを後から広げてはみ出す罠が 2 枚とも成立しなくなった。** 配列であることを表す `[]` の接尾は**型の欄**に、多重度は**鳥の足の記法**に移した。⚠️ **`importSeq` に置き場を与えた** —— 表 T-216 と `FR-063` により日程データの群であるのに、図 F-010 は見せ方の群に描き、どのエンティティの列でもなかった。`DR-2` が 12 鍵を固定し、そのうち単数は `project` だけで、同じ表 T-216 の `themeHue` も文書ぜんたいの通し番号 `uidHighWaterMark` も既にそこにあるので、**`Project.importSeq`** とした。これで `documentSettings` から出る弱い参照は 2 本になった。**`使える高さ` を `Row Area` の高さ と名前で呼ぶようにした**（`U-50` が既に持つ名前である） |
| 0.21 | 2026-08-15 | ⭐ **自分の文書形式に名前を与えた** —— `G-14` に `GRS JSON` を置き、**単独の `JSON` はこれを指す**と定めた。交換相手と並ぶ 27 箇所を確定名へ書き替え、表 T-056 の列名を **「`MSPDI` へ書き出すか」**に改めた（`GRS JSON` へは `FR-024` によりどれも書き出すので、この列が数えていたのは交換相手だけだった）。⚠️ **`JSON` の一般名詞（RFC 8259・`JSON Schema`・「JSON のオブジェクト」）と、1 つの文書を指す用法は書き替えていない。** ほかに 3 件 —— 表 T-025 の `MC-8`（測る区間）に**スクロール**を足し（表 T-027 の `UN-8` が既に別物として数えていた）、表 T-043 に `PG-13`（自動保存 1 回の書き込み時間・**記録のみ**）を足し、表 T-006b に `A-16`（頂点。**単独ならイナズマ線の頂点**）を足した。**Chapter 5.3 の 10 メンバを `XOfY` から `XFromY` / `XByY` に改名した** —— `xOfY` はこの仕様書では既に「X ÷ Y」（表 T-104 に 14 名）であり、変換に使うと 1 つの形が 2 つの意味を持つ |
| 0.22 | 2026-08-15 | ⭐ **文書ルートをコンポーネントとして立てた**（CR-127。`CP-34` / `PI-34` `Document`）—— **`DR-1` は持ち主の無い唯一の `DR-n` だった。**`DR-2` は `CP-1`、`DR-3` は `CP-2`、`DR-4` は `CP-3` が「正」に持つのに、**3 群すべてに同時に掛かる `DR-1` だけはどのコンポーネントも持てなかった** —— 1 つの群からはルートの全数を見られないためである。公開するのは型 `Document`（5 鍵）と `documentViolations` の 2 つ。**文書ぜんたいを扱う者は `Document` に依存し、1 群だけを扱う者はその群に依存する**（LoD / ISP）に従い、`DocumentCodec` と `AgentApiEndpoint` の 3 群への各 3 本を 1 本へ畳み、`ApplyDocumentChange` に 1 本を足した（`AG-3`「全か無か」）。**コンポーネント 33 → 34・辺 71 のまま・非巡回のまま。**⚠️ **`GRS JSON` の形は 1 文字も変わっていない** —— 既にある 5 鍵に名前を与えただけで、器を 1 段も増やしていない。図 F-010 のルートの箱を `documentRoot` から `Document` へ改め、**「文書ルート」/ `Document` / `documentRoot` の 3 綴りを 2 つにした。** ルートの箱だけは鍵を持たないので、5.4 に但し書きを置いた |
| 0.23 | 2026-08-15 | 1.9 の記法規約の穴を 2 つ閉じた（CR-128。**散文が 2 か所増えるだけで、表の行は 1 つも動かない**）。⭐ **表 T-006a の射程を明記した** —— 縛るのは**仕様対象のソフトの中の名前**であり、**本書を組み立てる道具は対象外**である。`W-2`「JSON プロパティ」が 2 通りに読めており、「一般の JSON すべて」と読むと**この仕様書の道具立て自身が 8 鍵ぶん違反していた**（`entity_rows` / `key_note` / `row_prefix` / `container_edge` / `column_width` / `node_separation` / `rank_separation`、ほかに改名不能な `$schema` 系）。`W-11` も同じで、道具の `.py` の綴りは 3 通りに割れている。⭐ **接続語の規則を足した** —— **`Of` は比率（X ÷ Y）に限り、変換と導出は `From`、引き当ては `By`**。版 0.21 で 10 メンバを `XOfY` から改名した事故を規則として固定したものである。⚠️ **書き換えは 0 箇所** —— 大文字を仮定せずに数え直した `Of` の 20 綴り・49 出現は、比率 14 名・`shapeHeightOf.<形状>` 5 名・`LevelOfDetail` 系 3 名・例示 2・偽陽性 2 に分かれ、違反は 1 つも無かった。**`shapeHeightOf.*` は値が比率で文法が引き当てという境界例なので、`Of` の直後にドットが来る形として但し書きで除外した**（改名すると値の正である `tbl-settings.md` に手が入るのに対し、形では既に見分けられている） |
| 0.24 | 2026-08-15 | ⭐ **予実の状態の判別を全域にした**（CR-129。**表 T-019a を新設**）—— Chapter 5.5 の状態遷移図を書く準備で欠陥が 2 件出た。**表 T-019 は「`GRS` が置く値」の表なのに「読むときの判別表」も兼ねており、後者には全域関数が要る。**⚠️ **`resumeValid` を `true` にする規則が 1 つも無く**（置く規則は `PV-1` / `PV-2` の 2 つで、どちらも `false`）、着手したタスクも再開日を置いたタスクも表 T-019 のどの行にも当たらなかった。⚠️ さらに **`ActualFinish` と `Stop` / `Resume` / `ResumeValid` を同時に持つ「途中で中断して完了したタスク」が実在し**（公式 XSD は `ResumeValid` を `Whether the task can be resumed` と定義し、`Resume` は過去の日付になる）、`FR-010` の「そこから外れる入力を受け付けない」がそれを丸ごと弾いていた —— **`FR-012` が「中身のない行」で踏んだのと同型の罠である。**判別を優先順 5 段（`PS-1` 〜 `PS-5`）にし、**`actualFinish` を `resume` / `resumeValid` より先に見る**ことで両方を閉じた。`FR-043` は着手時に、`FR-044` は再開予定日を置くときに `resumeValid` を `true` にする。`PV-3` は `resume` も空にする。**表 T-021 の記号の対応は 1 つも変わらない。** |
| 0.25 | 2026-08-15 | ⭐ **刊行物に仕様書を戻した**（CR-130。**文言は 1 文字も変えていない**）—— CR-129 の実物確認で、書き出した HTML から**表 40 枚・550 行が黙って消えている**ことが分かった。**StrictDoc は要求ノードの `**Relations**:` ブロックより後ろに置いた自由文を全ビューで捨てる** —— 相関は例外 0 件（Relations より前 6/6 描画、後ろ 0/40）。消えていたのは `T-052`（文書ルートの群）・`T-035`（`Agent API`）・`T-023`（操作の割当）・`T-027`（取り消しの単位）・`T-019`（予実の 5 状態）など **Chapter 4 の中核**で、要求が「表 T-023 に従うこと」と書いていても読み手は辿り着けなかった。33 ノードの自由文を Relations の前へ動かし、**非空行の多重集合が編集前後で完全一致すること**（2582 / 2582）を表明してから適用した。あわせて **`。` の直後で閉じ、次に文字が続く太字 21 箇所**が生の `**` のまま出ていたので半角空白を入れた。**描画された表 23 → 63 / 63、`<table>` 25 → 69、生の `**` 21 → 0。**⚠️ **16 検査は始めから終わりまで全緑だった** —— 検査が見ているのは原稿の中で参照が解決することであって、**参照先が刊行物に出ているかどうかではない** |
| 0.26 | 2026-08-15 | ⭐ **Chapter 5.5（振る舞い）を記入した**（CR-131）—— **`R7.4` の MUST 違反がこれで残っていない。** 表 T-066（一貫性の単位。**1 フレーム ／ 身振り 1 回 ／ 1 回の書き込み**の 3 つが入れ子）・表 T-067（文書を変える手順 7 段。**通知は差し替えの後**）・図 F-018（予実の状態遷移）・表 T-068（レイアウトの計算順序 11 段）・表 T-069（描くときに求める値 5 つ）を新設。⚠️ **未決だった 2 件を決めた** —— **全体を収める表示は表 T-068 を 2 回まで走らせる**（倍率 → 表示量 → 実寸 → 倍率 の循環があるので、3 回目を禁じて振動を止める）／ **通知を配っているあいだの書き込みは拒否する**（再入禁止。どの版に対する通知かが決まらなくなるため）。⚠️ **図を描いて要求の欠陥が 3 件出た** —— **表 T-021a の `PV-4` が「中断 → 未着手」で実績を全消去しており、表 T-021 と矛盾していた**（未着手はマーカーを持たない状態なので、押した瞬間に押した対象が消える）。**行き先を「進行中」に正した。** `FR-044` に「**再開日を消したとき `resumeValid` を `false` に戻す**」を足した（無いと日付を消しただけで中断が黙って解ける）。**予実の状態と遅れの判別に持ち主のコンポーネントが無かった**ので、`CP-1` `Schedule` の責務と `PI-1` のメンバ（`planActualState` / `isDelayed` / `delayWorkingDays`）にした —— `DR-1`（CR-127）と同じ形である |
| 0.27 | 2026-08-15 | ⭐ **Chapter 5.6（設計判断）を記入した**（CR-132）—— **`R2.18` と `R2.20` の MUST をどちらも閉じた。** ADR-000（最小構成との比較。増やしたもの 6 件は 表 T-070）と ADR-001（キャッシュ。4 点は 表 T-071）を置き、描かなかった図（表 T-072）と**意図して払う代償の全数**（表 T-073。**索引であり理由は書き写さない**）を並べた。⚠️ **ADR の 4 項目は散文で書き、表にしていない** —— 1.9 が表の第 1 列を行 ID と定めており、`項目` / `内容` の 2 列表は行 ID を持てないためである。⭐ **CR-126 §7 がここへ送った件を決着させた** —— **表 T-068 の 11 段を必要とする経路が 1 フレームに 4 本あり**（`SvgRenderer` / `ScheduleGeometry` / `InputCommandTranslator` / `ItemHitArea`）、どのコンポーネントもインスタンスを持たないので各々が計算すると**ポインタが動くたびに 11 段が 4 回走る**。`NFR-002` / `NFR-003` の予算に収まらないので、**`SingleHtmlShell` がフレーム先頭で 1 回計算して配る**形にした（`LY-5` により現在値の持ち主は `Framework` だけである）。⚠️ **計算する場所が `Framework` になるが、算法は `layoutEngine` の純粋関数のままである。** 辺 71 → 73、**非巡回・外向き 0 のまま**。図 F-013 にクラスタ対 `framework → layoutEngine` が、図 F-015 にシェルが現れる |
| 0.28 | 2026-08-15 | **Chapter 5 を通しで規約に当て直した**（CR-134。**仕様書に足したのは `UT-6` の 1 行だけである**）。⭐ **`R2.2`（SRP・MUST）違反が 1 件見つかった** —— `CP-25 SingleHtmlShell` の責務が 8 節あり、`FR-067`・`FR-065`・`LY-5`・ADR-001 と**別々の要求が縛る事柄を 1 ユニットに抱えていた**（5.2 自身の分割基準に当たる）。`UT-4` が `AgentApiEndpoint` を同じ理由で割っている前例に倣い、`UT-6` として `single-html-shell.ts`（起動と結線）と `frame-loop.ts`（フレームの走行）に割った。**コンポーネントも辺も図も動かない** —— ユニットはファイルの割り方であってコンポーネントの境界ではない。⚠️ **`R2.17`（プロンプト工学）は仕様書が既に答えていた** —— 表 T-009 の `XO-4`「本ソフトウェアは AI を持たない。`Agent API` を持つだけである」。**足すと二重管理になるので何も書かない。** 機械で当てた 12 種（数の主張・辺の着地・層の一致・純粋性・8 インターフェース・図 F-018 と表 T-021a）はすべて一致し、**助言 13 の 4 件と 14 の 18 件は読んだ結果いずれも欠陥では無かった**（要望項番・測定環境・節番号・WCAG 比の偶然一致）。点検の道具 2 本を技能へ入れた |
| 0.29 | 2026-08-15 | ⭐ **「部品」を使用禁止語にした**（CR-135。表 T-006b の `A-17`）—— **1 語が 2 つの意味で使われており、名指しできる語は 4 つあった。** 全 72 箇所を数え、**構造の単位**（設計 55 ／ 変更履歴 14）は「**コンポーネント**」へ、**画面の要素**（`NFR-007` の WCAG 1.4.11 の引用 1 ／ 用語集の `N-5`・`N-6` の 2）は「**UI パーツ**」（表 T-006a の `W-6` が既に持つ語）へ置き替えた。⭐ **Chapter 5.3 に 表 T-074 を新設し、構造の単位を 3 語で定義した** —— **入れ子は コンポーネント ＞ モジュール ＞ ユニット であり、3 語の定義は同表の `SU-1` 〜 `SU-3` が持つ。**⚠️ **本設計にモジュールは 0 である** —— 2 つより多いユニットを持つのは `EditDocument`（9）・`DocumentCodec`（5）・`AgentApiEndpoint`（3）・`ScreenRenderer`（11）で、いずれも平らに並べている。束ねる必要が出るまで作らない（`R2.9`）。⚠️ **「モジュール」の誤用を 2 件直した** —— ADR-000 と `MN-1` が最小構成を「1 モジュール」と書いていたが、**モジュールはコンポーネントの一部なので、プログラム全体を指せない。「1 コンポーネント」が正しい**（`R2.19` の「コンポーネントが 1 つの設計」がこの形である） |
| 0.30 | 2026-08-15 | ⭐ **Chapter 5.3 に 表 T-075「ユニット」を新設し、57 ユニットを 1 行ずつ並べた**（CR-136。利用者の指示）—— コンポーネント・ファイル名・純粋性・責務の 4 欄である。⭐ **数えたところ全数は 49 ではなく 57 だった** —— 5.3 は「層をまたぐインターフェースは、宣言するコンポーネントのフォルダに、その名前の語幹で置く」と定めており、表 T-074 の `SU-3` が「ユニット＝1 ファイル」と定めている以上、**表 T-065 の 8 本はそれぞれユニットである。** ⚠️ **この取りこぼしは 表 T-063 の表題も壊していた** —— 表題「1 つより多いユニットを持つコンポーネント」は全数の主張だが、`SvgRenderer` など 6 コンポーネントが 2 ユニットを持ちながら行を持っていなかった。**表 T-063 を「ユニットを割った理由」の表に改め、全数の主張をやめた**（行は 6 のまま）。**全数は 表 T-075 が、割った理由は 表 T-063 が持つ。** ⚠️ **`LR-2` の MUST NOT には反していない** —— 表 T-064 の `PI-19` などが、そのインターフェースを公開エントリのメンバとして既に宣言している。**この再公開が暗黙だったので MUST として明文化した。** ⭐ **1 ユニットのコンポーネント 22 件の責務欄は `CP-n` を指すだけとした** —— 1.9 の「指す先の文を書き写してはならない（MUST NOT）」に従う。⚠️ **版 0.29 は、この取りこぼしのため 2 つの誤りを持っていた。両方とも 0.29 を直した** —— **どちらも「何をしたか」ではなく「何を書いたか」の誤記だからである。** ① **ユニット数** ——「`EditDocument`（9）と `DocumentCodec`（4）だけ」と書いていたが、正しくは `DocumentCodec` が 5、`AgentApiEndpoint` が 3 である。② **コンポーネントの定義** —— 同じ版で捨てたはずの「外へインターフェースを公開する」を書いていた。**レビューが「34 中 7 に当たらない」と示して `SU-1` を公開エントリで定義し直した、まさにその定義である。** 書き写す形をやめ、表 T-074 の `SU-1` 〜 `SU-3` を指す形にした。 `audit-ch5.py` に 表 T-075 を当てる検査を 12 本と、**捨てた定義が湧き直さないことを見る検査を 2 本**足し、**わざと壊して落ちることを 2 件で確かめた** |
| 0.31 | 2026-08-15 | ⭐ **進捗マーカーを固定サイズにした**（CR-137。利用者の裁定）—— 実装着手前レビューの **F-1**（同じ要素に相反する 2 つの MUST）を閉じた。**`FR-094`（ズームに追随させない）を採り、`FR-013` の「実績バーに追随させ」を落とした。** 径は表 T-201 の `S-22` が持つ固定値 16px とし、**鍵を `markerMin` から `markerSize` へ改めた（席は据え置き）。** 16px は `actualMin` と同値であり、**実績バーの床と一致する。** ⚠️ **`markerOfFont`（`S-21` / `K-21`）は使い手が無くなったので廃止し、番号は席番号なので欠番のまま残した**（`S-52` と同じ扱い）—— `FR-024` が全項目を書き出すと定めている以上、使い道の無い鍵を残すと全文書に永久に載る。⚠️ **細線の 2 形状では固定径が実績バーより大きくなるが、これは本変更の前から起きている** —— 利用者は「サイズが揃っていないと逆に変だ」として揃えるほうを採った。`S-63` に `FR-094` への指しを足して `S-62` との非対称を消し、表 T-026 に `RC-11`（16px を実物で選び直す）を足し、⭐ **縦の床を予定の縦幅に 1 度だけ当てる規則を `FR-094` に置いた**（これまで仕様書に 1 文も無かった）。**検査の道具を 3 本直した** —— 検査 12 の例外を空にし、廃止した 3 席を検査 7 の既知集合へ入れた |
| 0.32 | 2026-08-16 | ⭐ **書き出しを式で定義した**（CR-138。利用者の裁定）—— **切り出す範囲は `GRS` が占める画面の全体、縮める比は `exportCanvas` の幅 ÷ 画面の幅**とし（`FR-080`）、**出力サイズを `S-81` に固定して選択肢（A3 横 / A4 横 / 現在の画面）を廃止した**（`FR-025`）。⭐ **WYSIWYG に判定できる式を与えた** —— 表 T-041 に `WY-3` を新設し、**画面上の外接矩形に比を掛けた値と、書き出した SVG / PNG の外接矩形とが一致すること**を判定にした。「画面に見えているものと同じ絵」では機械で確かめられなかった。`WY-2` と `WY-3` が使う**書き出しの基準環境**（表 T-025 の `MC-6` ＋ プロパティパネルとコマンドパレットを閉じた状態）を定義した。⭐ **描く UI パーツと描かない UI パーツの全数を 表 T-076（`EP-1` 〜 `EP-13`）に置いた** —— 帯は高さを保って文書名だけを描き、スクロールバーと操作子は**場所を空白として残す。詰めない。** ⚠️ **プロパティパネルとコマンドパレットだけは閉じた状態として書き出す** —— 編集のあいだ一時的に出す面であり、閉じ忘れを絵に残さない。⚠️ **縦に収まらない分は縮めずに下端側から `TaskGroup` 単位で落とし、件数を知らせる**（`FR-025`。作法は `NT-5`）—— 縮めて収めると比が文書ごとに変わり、画面と別の絵になる。**書き出しの文字が可読の下限を割ることを受け入れた**ので、`FR-025` の警告を落とし `FR-077` の RATIONALE を書き直した。⭐ **透かしの範囲を `Row Area` に限った**（`FR-020` / `UC-009` 手順 2）—— 証跡が要るのは日程の絵であり、その絵を写した画像には必ず `Row Area` が入る。`LM-1` / `LM-8` / `LM-13` / `NFR-007` / `FR-076` の `NT-5` は変わらない。⭐ **行名の打ち切りの規則を `FR-058` から `FR-085` へ移した** —— 取込の要求が持っていたので、人が付けた名前に掛かるかが読めなかった。**「名称ラベルと同じ扱い」という誤り**（`truncateUnits` は名称ラベルの前処理であり、行名はパネルの幅で決まる）も一緒に消えた。⚠️ **`svgPadding`（`S-57` / `K-66`）は、縁に余白を足さないと定めたことで使い手が無くなったので廃止し、番号は席番号なので欠番のまま残した** —— 余白は `canvasPadding`（`S-56`）に一本化した。**検査の道具 2 本を直した** —— 廃止した 2 席を検査 7 の既知集合と `specindex` の `RETIRED` へ入れた |
| 0.33 | 2026-08-16 | ⭐ **図の生成器を最後まで通した**（CR-139）—— `build.py` が図 F-015 で「札を置ける位置が無い」と止まり、**その先の 図 F-016・図 F-017 と `docs/review/components/components.md` に到達していなかった。** ⚠️ **`model.json` も `build.py` も CR-133 から 1 文字も変わっていないので、同じ入力・同じコードで CR-133 の時点から落ちていたことになる** —— 引継ぎの積み残し「図 F-015 の 3 本のラベルが重なる」がこれである。**辺の両端に近い札の候補位置を 4 つ足しただけで通った**（判定の仕組みも、掛かったときに止まる厳しさも変えていない）。⭐ **draw.io CLI で PNG に落として目で確かめた** —— `presentation values` は `presentatio` で切れて `what is selected` と重なり、`coordinates` は他の札に隠れて消えていた。**3 図とも全部の札が読めるようになった。** ⚠️ **`build.py` は折れ線の長さ比で札の位置を測るが、draw.io は直交経路の中間の線分から決めるので、両者はまだ一致していない** —— `presentation values` が箱の縁に 35px ほど掛かる。読めるので実害は無いが積み残しとして残す。**仕様書の本文は 1 文字も触っていない**（`tables` / `rows` / `uids` は不変） |
| 0.34 | 2026-08-16 | ⭐ **画面のモデルを `Entity` に置き、画面の面を組み立てるコンポーネントを立てた**（CR-140。利用者の裁定と、実装着手前レビューの **F-3**）—— 画面の各部の矩形を `ScreenRegions`（`CP-35`・`layoutEngine`）に、**文書に保存しない画面の値**を `ScreenState`（`CP-36`・`documentModel`）に置いた。**第 3 の小層は作っていない** —— 書き出しが文書だけから決まるようになった結果、**書き出しが通る値と画面にしか要らない値の境目が、2 小層の境目と一致した。** ⭐ **日程表の外側の UI パーツを組み立てる `ScreenRenderer`（`CP-37`）と、その DOM 実装 `DomScreenSurface`（`CP-38`）を立て、9 本目の層をまたぐインターフェース `ScreenSurface`（`IF-9`）を宣言した** —— ヘルプ・ツールチップ・`App Header`・コマンドパレット・プロパティパネル・知らせを担うコンポーネントが 1 つも無く、**全数を宣言して閉じた Chapter 5 の中で宙に浮いていた。** ⚠️ **確定した発話の持ち主を `InputCommandTranslator` から `ScreenRenderer` へ移した** —— `IF-2` が供給するのは「ポインタとキーの出来事」だけで、対話欄は UI パーツであって入力機器ではない。`PI-18` には `Esc` の階層（表 T-028 の `IN-4`）を通して見る `screenStateFromInput` を足した。⚠️ **書き出しの組み立てと切り落としの持ち主を `ImageExporter`（`CP-21`）にした** —— 表 T-076 が「描く」と定めた UI パーツを誰が組み立てるかが決まっていなかった。⚠️ **用語集の 表 T-103 に `U-51` を足した** —— `ScreenState` を 表 T-203 と同じ「画面の状態」と呼ばないためである。**コンポーネント 34 → 38・ユニット 57 → 71・層をまたぐ 8 → 9 本・辺 73 → 86・非巡回のまま・外向き 0 のまま。** 図 F-013 のクラスタ対は 9 のまま増えない |
| 0.35 | 2026-08-16 | ⭐ **コンポーネント図の原稿を整えた**（CR-141）—— ⚠️ **図を描く `draw.py` の語彙に無い `shape` が 2 つ手で書かれていた。** 同スクリプトは `shape` を辞書引きし、引けなかった値を黙って既定値へ落とす。`component` は 180×70 で字を上寄せ、既定値は 170×60 で字を中央なので、**箱の大きさが違っていたのは選ばれた記法ではなく、語彙外の値が既定値に落ちていたからである。** 警告は出ない。⭐ **表 T-060 の `LY-1` が「文書に保存しない実行時の値」と呼ぶ 4 つは、`EditHistory`（`CP-4`）・`Selection`（`CP-32`）・`DialogueLog`（`CP-33`）・`ScreenState`（`CP-36`）であり、形が 2 対 2 に割れていた** —— `EditHistory` が同じ分類で `component` である以上、「その形が保存しないことを表す」という読みは成り立たない。**理由はどの記録にも 1 文字も無く**、2 つを入れた commit の説明文はむしろ「`EditHistory` の前例に倣う」と書いていた。⚠️ **どの機械検査も `shape` を読まないので、捕まえる仕組みがそもそも無かった。** 2 語を直して **38 箱すべてを `component` に揃え**、5.2 の「箱はコンポーネントである」を書き換えずに真にした。⭐ **原稿を `model.json` から `components.json` へ改名した** —— 同じフォルダの `erd.json` が既に内容で名乗り、生成物も揃って `components` と名乗るのに、原稿だけが「何のモデルか分からない」名だった。⚠️ **変更履歴と過去の CR、`docs/review/` の日付つき記録は書き換えない**（CR-123 と同じ作法。改名した事実は本行が引き受ける）。**仕様書の本文で変えたのは、原稿の名を指す 5.2 の 1 文だけである** —— `tables` / `figures` / `rows` / `uids` も、ノード 38・辺 86 も不変。図の座標が動いたのは `documentModel` の 1 行の 8 ノードだけで、他のクラスタは 1px も動いていない |
| 0.36 | 2026-08-16 | ⭐ **起動と描き直しの順序を Chapter 5.5 に置いた**（CR-142。実装着手前レビューの **F-2** と 付録 A の **C-1** 〜 **C-3**）—— `NFR-011` が「起動の順序を設計で決めることでしか防げない」と書いたその順序に、Chapter 5 の受け皿が無かった。**表 T-077**（起動の順序 `BO-1` 〜 `BO-5`）と **表 T-078**（フレームが走る契機 `FT-1` 〜 `FT-5`）を新設し、`CP-25` の責務と「正」に `NFR-011` を足した。⚠️ **画面の寸法が変わったときにフレームを起こす契機が、仕様書のどこにも無かった** —— `IF-2` はポインタとキーしか供給しないので、**寸法は `Framework` が自分で観測する**形に決め、`NFR-010` に「寸法の変化は操作に数える」を足した。⭐ **契機の全数を書くにあたって数え直したところ、入力・差し替え・寸法の 3 つでは既存の MUST を 4 本殺すことが分かった** —— `EZ-2`（ポインタを止めたまま `S-124` が過ぎたら説明を出す）・`NT-2`（時間で消える通知）・`FR-061`（自動保存の状態が `S-112` の切れ目のあとで変わる）・`AG-11`（発話は版数を上げないので `WS-6` を通らない）である。**`FT-4`（時間が来たこと）と `FT-5`（版数を上げずに届いた発話）を足して全数を本当にした。** ⚠️ **`BO-3` は `OP-10` のとき `BO-4` の結果を要る形になっていたので、決める側を `BO-4` に寄せた** —— そのままでは「前の段が済む前に次の段へ進んではならない」を初回起動そのもので破っていた。**表 T-077 の射程は最初の 1 枚に至る描画の順序に限り**、`FR-038` / `FR-060` / `NT-4` は外だと明記した。`R7.4` の留保（起動時の収集順が 表 T-066 の 3 単位に入らない）も 1 文で閉じた。⚠️ **表 T-066 の `CS-1` が基準日を文書と並べて集めていた** —— 基準日は `Project.statusDate` であり凍結した複製の中にあるので、由来を括弧で示して第 3 の源に見えないようにした。⭐ **稼働日の数え上げに持ち主が無かった**（`FR-054` は Chapter 5 に 0 回）—— **コンポーネントは増やさず `Schedule`（`CP-1`）に持たせ**、`PI-1` に `workingDaysBetween` と `dateFromWorkingDays` を宣言した。暦を編集する `FR-088` は `EditDocument`（`CP-9`）の「正」に足した。**コンポーネントもユニットも辺も 1 つも動かない** —— 図は再生成しない |
| 0.37 | 2026-08-16 | 実装着手前レビューの Chapter 4 側 4 件を閉じた（CR-144。**F-5** / **F-6** と 付録 A の **C-4** / **C-5**）。⚠️ **最上位の行を非表示にすると戻す入口が無かった** —— `HR-6` に行見出しパネルの根のタブを足した（`FR-085` が最上位の行を許し、`TaskGroup.parentId` は `null` を許す）。**新しい UI パーツは作っていない** —— `U-29` を根にも置くという意味である。⚠️ **週の始まりを `FR-088` は「文書の暦」へ反映すると書くのに、置き場は `Project.weekStartDay` だった** —— `Calendar` にその列は無い。データモデルを動かさず、要求の文を置き場へ合わせた。`Calendar` と `Project` の両方に及ぶ編集を 1 回の書き込みで確定することも、表 T-067 の `WS-3` を指して明記した。⭐ **新規文書に 1 日あたりの分数が無く、稼働日を交換相手の期間へ換算できなかった** —— 表 T-209 に `S-128`（`480`）を足し、表題を「既定の暦と期間の換算」に改め、**射程を「取り込んでいない文書、および、取り込んだが値が空の文書」に広げた**（`Project/MinutesPerDay` は交換相手のスキーマで任意なので、取り込んでも空になりうる）。⭐ **換算の受け皿はデータモデルに既にあった。分類が誤っていた** —— `AT-35` `Task.actualDuration` の出自を `Own`（そのまま書き戻す）から **`Consume`（取り込んで解釈し、書き出すときに作り直す）**へ改めた。**保存する値は稼働日の整数、交換相手は時間の量であり、そのまま書き戻すことは原理的にできない。** 表 T-059 には置けない —— 同表は保存しない値のためであり、生成器がそれを拒む。⚠️ **単位が違うのに `Own` を名乗る列はほかに無いことを全 140 列で確かめた**（`minutesPerDay` 等は整数どうしなので `Own` のままで正しい）。 `minutesPerWeek` と `daysPerMonth` はどの導出にも使わないので**既定を持たない**ことを明記した。⚠️ **表 T-209 の値は日程データの群に入ることを注記した** —— `FR-063` の「見せ方の群の全数」の例外である。ほかに散文の数え上げ 2 件（`LM-14` の指し先 ／ 1.8 の「5 つの表」）を実体に合わせた。**要求は 1 本も足さず、コンポーネントも辺も動かない** |
| 0.38 | 2026-08-16 | ⭐ **未着手のタスクに実績を入れる経路を作り、掴む的と押す的を分けた**（CR-143。利用者の裁定。実装着手前レビューの **F-4**）—— ⚠️ **掴んで着手したときの実績期間の量がどこにも無かった** —— 表 T-019 の `PA-2` は「あり」を求めるのに置く規則が 1 つも無く、掴んだ瞬間のバーの長さ・完了率・イナズマ線の頂点が実装ごとに変わる。**未着手のタスクに実績の開始点と終了点の掴みシロを 2 つ薄く出し**（`Actual Operation Dummy`。`U-52`）、**どちらを掴んでも実績開始日と実績期間の両方を置く**形にした。既定は `S-129` ＝ **1 稼働日** —— 実運用では 1 日で終わる業務も 1 日ぶん入力する。⭐ **未着手にも進捗マーカーの記号 `( · )` を与えた**（`PM-1a`）—— **これまで未着手だけ記号が無く、掴みシロに押す役を兼ねさせていた。** 記号を与えたことで、**どの状態でもマーカーを押せば巡り、どの掴みシロも掴むだけ**になり、`FR-043` から「入口が唯一の経路」という但し書きが消えた。**表 T-021a の 4 行も 図 F-018 の辺も 1 つも動かない** —— `FR-011` の「**未着手のタスクを 1 押しで完了にできること（MUST）**」も満たされたままである。⚠️ **遅れている未着手タスクの `(!)` をマーカー位置へ移した** —— これまで入口の形で表し「未着手であることは位置が担う」としていたが、**未着手であることは実績バーの有無が担う**に改めた。⚠️ **マイルストーンは実績バーを持たないので点のダミーを 1 つだけ出し**、実績期間は `S-130` ＝ **0** に固定した（点なので長さを持たない）。⭐ **ダミーと未着手マーカーの濃さは `S-131` に置き、🔎 を付けて暫定とした** —— 表 T-026 に `RC-12` を足し、**実物を触って選び直す**対象にした。**色は実績バーの色を継ぎ、独立した色を保存しない**（`FR-041`）。**薄さで進行中と区別してはならない —— 形が意味を担う**（`FR-030`）。**ダミーは書き出しに描かない**（表 T-076 の `EP-14`）が、タスクバーに重なるので**場所は空けない**。**要求は 1 本も足さず、コンポーネントも辺も図も動かない** |
| 0.39 | 2026-08-16 | **描き直しの判定を「描く内容」の軸に統一した**（CR-145）—— ⚠️ **`FR-048` の MUST NOT が、仕様書自身の 4 箇所に破られていた** —— 「ポインタに追従する線が 1 本も無いとき、ポインタの移動で描き直してはならない」と書きながら、表 T-051 の `HF-6`（行の操作子）・`FR-053`（コマンドパレット）・`FR-092` の `EZ-2`（待ち時間で出るアイコンの説明）は、いずれも追従する線が無いままポインタの移動で描き直す。**版 0.38 の掴みシロと未着手マーカーが 5 つ目・6 つ目になった。** ⭐ **矛盾は同じ要求の中で既に解かれていた** —— `FR-048` の次段が「**判定はポインタではなく描く内容に置くこと**」と軸を明示している。MUST NOT の側がその軸に揃っていなかっただけなので、**「ほかに描く内容も変わらないとき」を加えて揃え**、対象外の 4 つを名指しした。**規則は増えていない。** ⚠️ **表 T-025 の `MC-8` の但し書きは前提が偽だった** —— 「追従する線が 1 本も無いときのポインタの移動は`NFR-010` により**描き直しが起きず**」と書いていたが、上の 4 つで描き直しは起きる。**除外そのものは妥当なので残し、理由を「日程を作り直さないので測ろうとしている負荷が現れない」に改めた。** ⚠️ **`NFR-002` の RATIONALE は触っていない** —— 「描き直しが起きなければ測る母数が無い」は真であり、区間の定義は `MC-8` を指しているだけだからである。**要求は 1 本も足さず、値も表も図も動かない** |
| 0.40 | 2026-08-16 | ⭐ **版 0.39 の「詳細仕様に入れるか」の判定で見つかった食い違い 10 件を閉じた**（BLOCKER は 0 件で、判定そのものは「入れる」であった）—— **どれも過去の裁定が理由文まで波及していなかった取り残しである**。⭐ **利用者の裁定 1: 進捗マーカーの規則を `FR-013` に一本化した** —— `FR-011` の「未着手の印は `FR-043` が定める」が、`FR-043` 自身の「状態を巡らせるのは進捗マーカーである（`FR-013`）」と押し戻し合っていた。遅れている未着手タスクの `(!)` の規則を `FR-043` から `FR-013` へ移し、`FR-043` は掴みシロだけを持つようにした。`FR-048` が挙げる「濃さが変わる UI パーツ」の指しも、未着手のマーカーの側を `FR-013` に合わせた。⚠️ **版 0.38 が `PM-1a` に記号 `( · )` を与えたのに、`PV-4` と Chapter 5.5 が「未着手はマーカーを持たない」と、自ら指す `PM-1a` の逆を書いたままだった** —— しかも Chapter 5.5 の散文は、13 行上の 図 F-018 が描く未着手から完了への辺と割れていた。`PV-4` の MUST NOT は残し、理由を「未着手は実績を 1 つも持たない（表 T-019 の `PA-1`）」に改めた。`FR-030` の「4 記号」も 表 T-021 の 5 行に合わせ、`UC-006` 手順 3 の「実績入力の入口がその形をとる」も版 0.38 の裁定（印はマーカーの位置に出る）に合わせた。⭐ **マイルストーンの例外が `FR-043` に 1 文字も無かった** —— `S-130` が規則の正として `FR-043` を名指ししているのに、`FR-043` は実績期間を `S-129` とする例外なしの MUST を持っていた。**規則を移した先に規則が現れていない**型である。⭐ **利用者の裁定 2: 再開アイコンは `S-63` に従う** —— `OC-4` が「`OC-3` と同じ表示の切り替えに従う」と書きながら、表 T-202 に再開アイコンの行が無かった。新しい鍵は立てず `S-63` の意味欄に登記した。⚠️ **`FR-063` の「表 T-216 だけは日程データの群である」が偽になっていた**（版 0.37 が 表 T-209 を同じ扱いにした）—— **同じ STATEMENT が「この全数を散文で持ってはならない（MUST NOT）」と書いており、その通りに腐った**。散文の列挙をやめ、表 T-052 の構造判定を指す形にした。⚠️ **Chapter 5.5 と 6.2 の「実績バーの右端は交換相手にも書かない」が偽だった** —— 表 T-059 の `DV-9` が `Task/Stop` として書き出す。文書の列に持たない規則は `FR-006` が持ったままである。⚠️ **予約語「基準日線」（`Status Line`）が性能の実測基準の意で使われていた** —— 版 0.6 の 基準線 → 基準日線 の統合が性能節まで巻き込んだ跡である。`A-9` の定めどおり「基準値」に改めた。**あわせて、元文書に無い直接引用を消した** —— `previous-project-result/04-performance/performance-notes-ja.md` は一貫して「基準線」と書いており、「基準日線と直接比べてはならない」という引用は実在しなかった。⚠️ **名前の正である 表 T-104 に `pinnedGroupIds` / `pinnedRowMax` が無かった** —— `K-110` / `K-111` として登記し、`U-51` の列挙も合わせた。**要求は 1 本も足さず、コンポーネントも辺も図も動かない** —— `tables` 114・`figures` 10・`uids` 140 は不変、`rows` は 1336 → 1338（表 T-104 へ足した 2 鍵ぶん）。**重複の群は 49 → 48 に減った**。⚠️ **過去の変更履歴の行は書き換えていない**（版 0.5 の「基準線」も含む。CR-123 と同じ作法）。⚠️ **冒頭の但し書き「Chapter 3 と 4 はまだ器である」も偽になっていた** —— 同ファイルの `^> 未記入` は 0 件である。**数え上げを書き直すと再び腐るので、列挙を捨てて `grep` を指す 1 文にした** |
| 0.41 | 2026-08-16 | ⭐ **並列実装（71 ユニット）に入るための 5 件を閉じた**（CR-146 〜 CR-150。**要求は 1 本も足さず、コンポーネントも辺も図も動かない**）—— ⭐ **継ぎ目の署名の持ち主を `src/` の公開エントリにした**（CR-146。利用者の裁定）—— 表 T-064 は名前と担うことだけを持ち、**引数・戻り値は `src/`、境界値は Chapter 6.1** が持つ。委ね先だった Chapter 6.1 の器は `SW_SPEC` を EARS 1 文で並べる形であり、**引数の並びを置けない**。`src/` を正にできる根拠は 3 つある —— 実装言語と `tsc --noEmit` を Chapter 1.4 が定め、公開エントリを 1 ファイルに限る MUST を 5.3 が持ち、**正が仕様書の外にある前例を Chapter 6.2 が既に作っている**（公式 XSD）。表 T-107 の前書きも同じ形に揃えた。⭐ **`GRS JSON` のスキーマを 2 原稿から起こす生成物と定めた**（CR-147。利用者の裁定）—— Chapter 6.2 の未記入行は「上の図と表から起こす」と書いていたが、**その図と表は `erd.json` の生成物である**。日程データの群は `_assets/source/erd.json`、見せ方の群は `_assets/tbl-settings.md` を原稿とし、**値を写した 3 つ目の原稿を作ってはならない（MUST NOT）**。原稿の側には**型・値の範囲・空を許すかどうかを機械が読める形で持つこと（MUST）**を課した —— いまはそれらを日本語の散文で書いており、**列挙の中には値そのものを持たない行がある**。⚠️ **鍵の名前は書かない** —— 原稿の作りであって要求ではない。⭐ **「同じものを出す」の同値判定を正規化後の一致にした**（CR-148。利用者の裁定）—— **仕様書に `正規化` の語が 1 件も無かった**。`FR-021` は「元のファイルと同じもの」、`WY-2` は「同じ SVG / PNG」としか書いていないのに、`FR-080` が「3 行とも機械で確かめられる」と主張していた。**バイト列をそのまま突き合わせて合否としてはならない（MUST NOT）** —— バイト一致を求めると、取り込んだ文字列を丸ごと抱えて書き戻すほかに手が無くなり、合流も編集も成り立たない。`WY-3` は**両側の座標に同じ丸めを当ててから比べる**。⚠️ **どう正規化しどう丸めるかは Chapter 6.1 が持つ** —— 要求が課すのは、同じ規則を両側へ当ててから比べることだけである。⭐ **命令の名簿を 表 T-108 として置いた**（CR-149）—— `DocumentCommand` は仕様書に 1 行しか現れず（`PI-8`）、その全数の持ち主を**未記入の Chapter 6.1** に委ねていた。用語集に節 7 を起こし、`CM-1` 〜 `CM-71` を 群・確定名・組・何を担うか・正 の 6 欄で並べた。**切り方は「複数の列にまたがる MUST があるものだけ畳み、無いものは分ける」**であり、畳む理由は**その MUST の持ち主を作ること**である —— 原子性は 表 T-035 の `AG-3` が束の単位で既に保証している。⚠️ **名は仕様自身の語に合わせた** —— 状態を「巡らせる」（`cycleTaskPlanActualState`）、割当の「解除」と担当者の「削除」の書き分け（`unassignResource`）、兄弟の「並べ替え」（`reorderTaskGroupSiblings`）。接頭辞は `DC-` が 表 T-029a に埋まっているので `CM-` にした。⭐ **命令が従う行を要求側に立てた**（CR-150）—— 名簿を 1 件ずつ「この命令が書く列に従う行が在るか」で掃いた。`FR-008` に**担当者と割当を作るときの種類・採番・重複の禁止**を、`FR-001` に**作ったタスクが WBS の親を持たないことと、その場で作る行の名前の導出元**を、`FR-098` に**同じ操作子でピン止めを外すこと**を、`FR-051` に**表示位置（`S-77` / `S-78`）の書き手**を置いた。表 T-016 の `PR-14` は**フェードの適用形状**を 表 T-012a の `FD-5` へ、表 T-027 の `UN-14` は**行の色と高さの変更**を対象に加え、`FR-088` は**2 つの曜日の符号が別であること**と正の在り処を指すようにした。⚠️ **値・丸め・符号そのものは書かない** —— 境界値は Chapter 6.1 が、交換相手の符号は Chapter 6.2 が指す公式 XSD が持つ。⚠️ **掃きが見つけた穴のうち、答えが仕様の他所に無いものは 1 つも閉じていない** —— 決め打つと決めた側の根拠が仕様に残らないので、利用者の裁定にかける。**`tables` 114 → 115・`rows` 1338 → 1409。`figures` 10 と `uids` 140 は不変**。重複の群は 48 → 49（`CM-16` と `CM-17` が兄弟行として立つ） |
| 0.42 | 2026-08-17 | ⭐ **`GRS JSON` のスキーマを 2 原稿から起こす生成物にし、その生成器が見つけた原稿の不揃いを直した**（CR-147 の受け皿づくりと CR-151。**規則は 1 つも変えていない**）—— ⭐ **`_assets/source/erd.json` の 140 列すべてに機械可読な形を足した** —— `type` と `nullable` の散文の機械形であり、**散文が言っていること以外を入れない**（型の括弧に書かれた範囲は下限・上限に、字数は長さの上限になる。仕様の他所の規則は引き込まない）。`erd.schema.json` はこの鍵を必須にし、**`null` を許すかどうかが `nullable` の散文と食い違うと落ちる**。⭐ **生成器 `erd_json_to_schema.py` を置いた** —— 日程データの群は `erd.json`、見せ方の群は `_assets/tbl-settings.md` を読み、`_assets/grs-document.schema.json` を書く。⚠️ **値を作らない** —— 綴りの決まっていない列挙は素の文字列に広げ、どれを広げたかをスキーマの `$comment` に残す。**検査 17** を `check.sh` に足し、**生成物を手で直したときと、原稿を直して再生成し忘れたときの両方で赤になる**ことを実際に壊して確かめた。⭐ **CR-151: 入れ子になるエンティティの形を 4 つとも揃えた** —— `GRS JSON` は交換相手の木をそのまま持つ（表 T-053 の `DF-1`）ので、`WeekDay` と `Exception` は `Calendar` の下に入れ子で載る。ところが原稿では、`Dependency` と `CarryElement` が**親の包む列**で繋がっているのに対し、**この 2 つだけが子の側に `calendarUid` を持つ関係の形**で載っていた。`FR-024` は列を鍵ごと書き出すので、そのままでは**構造から決まる値が鍵として出て**、表 T-053 直後の「構造から決まるものを列にしないこと（MUST）」に触れる。`Calendar` に `weekDays` と `exceptions` を足し、子から `calendarUid` を落とし、識別を `CarryElement` と同じ `key_note`（親 ＋ `ordinal`）に寄せた。⚠️ **`ordinal` は残す** —— 落としているのは `Dependency` だけで、それは `DF-4` が依存について名指しで禁じているからである。⭐ **出どころは生成器である** —— 生成器が「どの鍵からも辿り着けない」と報告したので追った。**生成器の側は 1 行も直していない**（包む列を足せば既にある仕組みで木が繋がる）。⚠️ **原稿が開けたままにしているものは、推測せずに報告する形にした** —— 綴りの無い列挙 4 列（`milestoneGlyph` / `nameAlign` / `lineWeight` / `leaderShapeKind`。決まっているのは `shapeKind` だけ）、型の読めない設定値 3 件、`planActualGuideColor` に保存しない印が無いこと。**`tables` 115・`figures` 10・`rows` 1409・`uids` 140 はいずれも不変**（表 T-058 は 2 行落ちて 2 行増える）。仕様本文で変えたのは `AT-72` を `AT-73` に直した 1 語だけである |
| 0.43 | 2026-08-17 | ⭐ **並列実装の波 W1 を止めていた 9 件をすべて閉じた**（CR-152 / CR-153 / CR-154。すべて利用者の裁定）—— ⭐ **注記をタスクに留める経路は持たないことにした** —— 表 T-023b の `AR-5` が「当たった対象を指す先にしてコメントボックスを置く経路は持たない」と**理由つきで否定している**のに、表 T-050 の `CD-1` は「その `Task` に付いた注記」を連鎖削除しており、**留める経路が無いのに後始末だけが在った。** `CommentBox` から `anchorTaskUid` と `anchorPoint` を、関係から `CommentBox` → `Task` を落とし、`CD-1` から注記の句を外した。⚠️ **行を指す注記の後始末は `CD-2` が別に持つので残る。** ⭐ **`TaskGroup.order` は `null` を許さないことにした** —— 同じエンティティの他の可列がすべて `null` の意味を書いているのに `order` だけ書いておらず、「`null` = 末尾」と決めても、**その `null` どうしの並びを決める規則がもう 1 つ要る**ためである。⭐ **導出元のタスクを消すときは、行の名前を確定させてから消す**ことにした（`FR-032`）—— `AT-54` が名前も導出元も無い行を禁じている。**行そのものは消さない** —— 器は人が作ったものであり、表 T-015a の `HM-6` が保つと定めている。⭐ **行の高さの指定は下限として扱う**ことにした（`FR-042`）—— 上限や固定値として受け取ると、**開いた時点では正しかった高さが、タスクを足しただけで拒まれる値に変わる。** 表 T-014 の `ST-9` とこれで両立する。⭐ **依存の重複禁止の「組」は先行と後続の対を指す**と明記した（`FR-009`）—— 同じ 2 つの間に引ける依存は 1 本である。⚠️ **取り込みには掛からない** —— 本 MUST NOT は画面の経路を縛るもので、`FR-023` の取込の検証に依存の重複を拒む規則は 1 つも無いので `FR-021` の往復は破れない。⭐ **パネル幅の上限の判定を式にした**（`FR-052`）—— `Row Area` の幅が 0 より大きいこと、である。⚠️ **3 義ある「キャンバス」を単独で使わない**（表 T-006b の `A-8`）—— `S-79` の上限欄が既に `Row Area` と書いており、それに揃えた。⭐ **ハイライトボックスの角の丸みに値を与えた**（表 T-217 を新設し `S-132`）—— 描く規則は `FR-019` が既に持ちながら、値がどこにも無かった。⚠️ **`documentSettings` の鍵ではない**（`HighlightBox` の列である）ので用語集の 表 T-104 には登記せず、日程データの群として注記した。⭐ **文書名が無いときのタブの見出しを `Untitled` とした**（CR-154）—— `title` は `null` 可なのに `null` のとき何を出すかがどこにも無く、`無題` に類する語は `docs/spec` にも `previous-project-result/` にも 0 件であった。**空文字は受け付けない** —— 空と `null` の 2 つの「無い」を持つと、往復と合流のたびに寄せ方の規則が要る。⚠️ **表示言語で切り替えない。名前の無い文書が 2 つあるときは見分けられない** —— 手がかりが文書の側に無いためである。 **`figures` 10 と `uids` 140 は不変**、`tables` は 115 → 116、`rows` は 1409 → 1407（列が 2 行と関係が 1 行減り、設定値が 1 行増えた）。⭐ **表 T-217 を足したとき、スキーマの生成器が「分類されていない設定値表がある」と言って止まった** —— 設計どおりの挙動であり、分類を登録して通した |
| 0.44 | 2026-08-17 | ⭐ **日付は日であり、`GRS` は時刻を扱わないことに決めた**（CR-155。利用者の裁定）—— 波 W1 の実装で `Schedule` の 4 メンバ（`isDelayed` / `delayWorkingDays` / `workingDaysBetween` / `dateFromWorkingDays`）が書けずに止まったのが出どころである。⚠️ **3 つの要求が同時に掛かり、素直に実装するとどれかが破れていた** —— 交換相手に面した**日付列 12 本はすべて `Own`**（取り込んだ値をそのまま書き戻す出自）であり、`EX-4` は「日付の形式を勝手に変えない」、`EX-2` は「編集していないタスクの値を書き換えてはならない（MUST NOT）」と課す一方で、遅れも期間もフェードも**稼働日と日数**で数えるので解釈が要る。⭐ **交換相手の型を実測したところ、日付の要素はすべて `xsd:dateTime` であり、日付だけの型（`xsd:date`）は 0 件であった** —— 時刻部分は必須である。そこで、**`GRS` が書く日付の時刻は `00:00:00` とし、綴りは交換相手の型に合わせる**ことにした（表 T-033 に `EX-7` を新設）。⚠️ **取り込んだまま編集していない値には掛からない** —— そちらは元の文字列のままを書き戻す。**列は交換相手の文字列を持ち続け、日はそこから導出する**ので、8 時開始で入ってきたファイルは 8 時のまま往復する。**データモデルは 1 列も動かない。**⭐ **`FR-054` に「日付の最小単位は日とすること（MUST）。時刻を解釈してはならない（MUST NOT）」を置いた** —— あわせて**交換相手の値から日を取るときは字面の日付の部分を取り、時差を換算してはならない（MUST NOT）**とした。換算すると、書き出した日が取り込んだ日と 1 日ずれる機で入れ替わる。⚠️ **採らなかった案** —— 日付列を `Consume` にして解釈した値だけを持つ案は `EX-4` と `EX-2` を正面から破る。版 0.37 が `actualDuration` を `Own` から `Consume` にした前例は移らない —— あちらは**そのまま書き戻すことが原理的にできない**（稼働日の整数と時間の量）のに対し、日付は書き戻せる。解釈した値と元の文字列を両方 文書に持つ案は、表 T-053 直後の「構造から決まるものを列にしないこと（MUST）」と同型である。**`tables` 116・`figures` 10・`uids` 140 は不変**、`rows` は 1407 → 1408（`EX-7` の 1 行）|
| 0.45 | 2026-08-17 | ⭐ **6.2 を閉じ、Chapter 7 に系統・レベル・方針・ツール・合格基準を置いた**（CR-156 / CR-157。うち 2 件は利用者の裁定）—— ⭐ **CR-156: 6.2 の最後の 1 行は、待っていた前提を既に満たしていた** —— 版 0.42 で日程データの群の原稿が機械可読な形を得ており、生成器も生成物も検査 17 も揃っている。ところが**生成物の名は、8 つの仕様ファイルのどこにも書かれていなかった**（変更履歴の 1 行を除く）。置き場を `_assets/grs-document.schema.json` と定め、あわせて ⚠️ **スキーマは、原稿が綴っていない値を作らないこと（MUST）** と **スキーマ検証を通ったことをもって本仕様書に適合したとみなしてはならない（MUST NOT）** を置いた —— 実測で、列挙の値を原稿が綴っていない列が 4 本、型の読めない設定鍵が 3 件あり、どちらも素の文字列に広がる。⭐ **CR-157: いま在る 300 件のテストに、仕様書上の居場所が無かった** —— 仕様書が挙げていた置き場 4 つと、実在する `tests/contract/` `tests/unit/` との重なりは 0 であった。**表 T-218 を新設し、6 つの置き場に系統・受け皿・親・レベル・ツールを対応させた** —— 契約テストと単体テストは、文法が `TEST_LEVEL` に `Unit` を許さないので仕様書に受け皿を持たないが、置き場は Chapter 7 が持つ（5.3 がそう委ねている）。⭐ **Chapter 9 のケースはテストコードから起こす生成物とした**（利用者の裁定）—— 手で書くと同じ主張が 2 か所に載り、仕様書の側は落ちない（1.9 が禁じる形）。6.2 のスキーマと同じ 2 つの縛りを掛け、**表 T-219 に章ごとの起こし方と合格基準**を置いた。⭐ **`NFR-013` にゲートが無かった**（利用者の裁定）—— 計算量の伸び方は測り方（表 T-025 の `MC-9`）まで定まっているのに 表 T-043 に行が無く、**Chapter 7 自身の「測る動機は、合否が工程を止めるときにしか続かない」に反していた**。`PG-14` を足し、当たり判定と依存線の経路が揃う `M3` から掛けた。⚠️ **実装側も合わせた** —— `vitest.config.ts` と `playwright.config.ts` と `tests/README.md` の 3 か所が、実在しない `tests/e2e/` を指していた。**`figures` 10・`uids` 140 は不変**、`tables` は 116 → 118、`rows` は 1408 → 1418 |
| 0.46 | 2026-08-17 | ⭐ **Chapter 6.1 に文書の不変条件の表を置いた**（CR-158 / CR-159。うち 2 件は利用者の裁定）—— ⭐ **6 分割の全数掃きを 13 体で走らせ、候補 132 件のうち 69 件だけが敵対的検証を通った**（潰れた内訳は、逐語が主張を支えない 34、不変条件でない 21、生成スキーマが既に強制 5、重複 3）。⚠️ **半数近くが潰れている。掃きだけを信じて書いていたら、支えの無い条件が仕様に載っていた。** ⭐ **CR-158: 掃きの副産物として、原稿どうしの食い違いが 3 つ出た** —— ① `Assignment` の多重度が `0..n ─ 1`（必須）でありながら、列は `null` 可であった。⭐ **交換相手の XSD が審判になった** —— `Assignment/TaskUID` と `ResourceUID` はどちらも `minOccurs="0"` であり、出自が `Consume` である以上、相手が省けるものを必須にはできない。**誤っていたのは多重度の側である**ので、原稿を `0..n ─ 0..1` に直した。② `S-132` が `AT-124`（`CarryElement.name`）を引いていたが、正しくは `AT-122`（`HighlightBox.cornerRadiusPx`）である。⚠️ **`AT-` の番号は表の中の位置であり、列を足し引きすると動く。** ③ 5.4 の「弱い参照」の 1 文が表示位置とピン止めを一括りにしており、宙吊りのピン止めを持つ文書が正しいとも読めた。**利用者の裁定により宙吊りのピン止めは違反とし、1 文を 2 本に割った。** ⭐ **CR-159: 表 T-220 を新設した** —— `LY-1` が「不変条件の全数は Chapter 6.1 が持つ」と定めたまま 6.1 が空だったので、`scheduleViolations`（表 T-064 の `PI-1`）が書けずにいた。⭐ **鍵の欄は機械で突き合わせた** —— 原稿から数えた `PK` 系 15 列と `FK` 系 17 列に対し、草案の配り方は漏れも余りも 0 であった。⭐ **判定に要る列を名前で並べない** —— 鍵の欄と型の欄を指す形にしたので、列を足しても本表は動かない。参照整合の 4 行がこれで 1 行に潰れた。⚠️ **単一列で決まる条件を本表に入れない** —— 生成スキーマが既に強制しており、二重になる。⭐ **`FR-058` に「行に載らない `Task` を文書に残してはならない（MUST NOT）」を足した**（利用者の裁定。`IV-6` を支える要求である）。⚠️ **`FD-7` の「フェードは 0 以上」は、原稿の型に範囲が無く生成スキーマが強制していない** —— 単一列なので本表ではなく原稿を直す仕事であり、別の CR に回した。⚠️ **6.1 の未記入行は残す** —— そこは `SWS-xxx` の席であり、境界値は波ごとに足す。**`figures` 10・`uids` 140 は不変**、`tables` は 118 → 119、`rows` は 1418 → 1434 |
| 0.47 | 2026-08-17 | ⭐ **波 W2 の `layoutEngine` を止めていた 4 件を閉じた**（CR-160。すべて利用者の裁定）—— **4 ユニットを書く準備で 5 件を調べたところ、5 件とも仕様書に本当に穴があった。** ⭐ **① `TaskVisual.shapeKind` が `null` のとき何を描くかが、どこにも無かった** —— 同じ表の `fillColor` は `null` の意味を綴っているのに `AT-100` は「可」だけで、生成スキーマだけが `null` を許していた。⚠️ **当初は「期間の情報があれば矩形」を検討したが、実測により採らなかった** —— `Task` に期間の列は無く（`start` と `finish` から出す）、`percentComplete` はマイルストーンでも意味を持ち、`FR-054` により日付は日なので長さ 0 を表せない。**分けられる列は `Task.milestone` ただ 1 つであり、`AT-30` がそれを「正」と定めている。** 描く形をそれ以外から導くと同じ事実に正が 2 つ立つので、**`null` は `Task.milestone` から解く**とした。⭐ **② 隠した行の配下をどうするかが、どこにも無かった** —— 畳みの `HR-1a` は 3 つの禁止を持つのに、`HR-6` は「行を隠す」の 4 文字だけで配下にも `Task` にも触れず、`LC-1` は「非表示の行を落とす」と書きながら正の欄が畳みの `HF-7` を指していた。**畳みと同じ 2 つの禁止を掛けた** —— 載せ替えを禁じた理由（全畳みや縦ズームの縮小で 1 行に文書中のほぼ全 `Task` が集まり `ST-7` の安全弁に達する）は、隠しにもそのまま当てはまる。⚠️ **ただしグループ LOD と同じ絵になることは求めない** —— 隠しは人が駆動するので、倍率で戻してはならない。⭐ **③ 起動時に全体表示をどこまで働かせるかが、原稿どうしで食い違っていた** —— `BO-3` は「この 2 つ（倍率と表示位置）を決める」と書くのに、`OP-10` は条件も結果も表示位置だけだった。**`OP-10` を倍率と表示位置の両方へ広げ、起動時は `HF-8` を働かせないことにした** —— 起動のたびに畳みを捨てると、`HR-6` が `WY-1` のために保存させた状態が消える。⚠️ **人が全体表示を求めたときの `HF-8` は既存のまま**（畳みを捨てる）。**隠しはどちらの場合も残る。** ⭐ **④ ポインタを伴わない経路のズームの中心が、どこにも無かった** —— 中心を述べているのは `MK-2` と「ズームはポインタ位置を中心とし（MUST）」の 2 か所だけで、どちらもポインタ前提であり、画面上のボタン・ショートカット・`Agent API` では**満たしようがなかった**。**`Row Area` の中心を代理とした** —— その MUST が守っているのは「見ているものが動かない」ことであり、ポインタはその代理にすぎない。⚠️ **5 件目（名称ラベルの 9 点の対応）は W2 を止めないので含めない** —— 既定の経路は表 T-013 が完全に決めており、9 点は人が明示的に動かしたときだけ効く。**`tables` 119・`figures` 10・`rows` 1434・`uids` 140 はいずれも不変** —— 既存の行の中身を厚くしただけで、行を 1 つも足していない |
| 0.48 | 2026-08-17 | ⭐ **画面の各部のうち、何が場所を取るかを決めた**（CR-161。利用者の裁定）—— `ScreenRegions`（`UF-58`）を書こうとして **`Row Area` の矩形が出せない**ことに当たったのが出どころである。⛔ **決まっていないものが 3 つあった** —— ① `App Header` の高さ（`appHeaderMaxHeight` は在るが、⚠️ **属する 表 T-212 の題は「画面の各部の寸法の上限」であり、高さそのものを持つ行が無い**）、② `Scrollbars` の太さ、③ `Panel Divider` の幅。**画面の構成を描いた図も無い。** ⭐ **前プロジェクトの PoC が 3 つとも実測で持っていた** —— スクロールバーは「常時表示、環境の既定の半分。Windows は 17px なので 8px」、パネル境界は境界に重なる 8px の掴み帯でレイアウトの幅を取らず、ヘッダは測って引いている。⭐ **決め手は仕様書自身の 2 文であった** —— `FR-051` が 表 T-031 の直後に書く「**消えるとキャンバスの幅が変わり、再レイアウトが走る**」と、`WY-3` の注記「**バーを描かなくても場所は残る**」である。⛔ **どちらも、スクロールバーが幅を取っていなければ成り立たない。** **裁定は 3 つ** —— `App Header` の高さと `Scrollbars` の太さは**起動時に環境から確定させ、設定値として持たない**（環境ごとに違い、持つと環境をまたいだときに絵が崩れる。`appHeaderMaxHeight` はその上限であって高さそのものではない）。`Scrollbars` は `Row Area` から場所を取り、太さは環境の既定の半分とする。`Panel Divider` は場所を取らない。⭐ **`FR-052` の判定式にスクロールバーを足した** —— 同式は「`Schedule Canvas` の幅から `canvasPadding` と 2 つの幅を引いた残りが `Row Area` の幅」と綴っており、**足さないと `Row Area` の幅の定義が 2 つになる**。⚠️ **高さの式は足さない** —— `U-50` が既に持っており、同じ算術を 2 か所に書かない。⭐ **値を 1 つも発明していない。** ⚠️ **起草の途中で自分の推奨を 2 度取り下げた** —— 1 度目は「スクロールバーも境界も場所を取らない」（`FR-051` の 1 文を読み落としていた）、2 度目は「ヘッダは上限をそのまま高さに使う」（測る案の代償を過大に見積もっていた。測るのはヘッダ自身の箱であり、日程を 1 枚も描かずに済むので `NFR-011` に触れない）。**`tables` 119・`figures` 10・`rows` 1434・`uids` 140 はいずれも不変** —— 行も値も 1 つも足していない |
| 0.49 | 2026-08-17 | ⭐ **Chapter 6.1 に W2 のレイアウトの算式を置き、タスク LOD の判定を深さから幅へ移した**（CR-162 / CR-163。うち 1 件は利用者の裁定）—— ⭐ **CR-162: 表 T-068 の 11 段のうち 8 段は仕様書が既に材料も規則も持っていた。** 足りなかったのは 3 つで、**前プロジェクトの PoC が 3 つとも実測で持っていた** —— 目盛の細かい段の間引き、行の帯高、行の縦位置である。**表 T-221 に置き、仕様書で最初の `SW_SPEC` ノード 2 本（`SWS-1` / `SWS-2`）で受けた。** ⭐ **導出できるものは穴に数えなかった** —— 日付から x は `S-77` が「表示の左端が指す日付」と定めているので足し算で出る。全体表示の倍率は `FR-055` と 表 T-068 の 2 回走行が決めている。**1 つの `Task` が縦に取る高さは 表 T-012 の「実績の置き方」の欄がそのまま答えであり**、床の当て方は `FR-094` が既に持っていた。⭐ **CR-163: タスク LOD の判定が実装できなかった** —— `FR-018` は「判定は WBS の階層の深さで行い、しきい値は `S-86` に従うこと（MUST）」と定めるが、`S-86` は px の幅で深さは数字であり、**深さから幅を出す規則がどこにも無かった**。⚠️ **PoC も解いていなかった** —— 深さごとの期間を定数で持ち、そこからデータを生成していたので、答えを最初から知っていた。⭐ **`S-86` の欄そのものが答えを持っていた** ——「文字が 1 つも入らない幅にしない」。見ているのは形状が文字を抱えられるかであって、深さの代理値ではない。**判定を `Task` ごとに、形状の幅（期間 × 1 日あたりの表示幅）で行う形に改めた。** ⚠️ **占有幅（表 T-038）では判定できない** —— 外へ出したラベルを含むので、しきい値を割ることが原理的に起きない。⭐ **代理をやめると、縮めるほど描く集合が縮むことが構造で決まり、`FR-018` の逆転の禁止が論証を要さずに成り立つ。** ⚠️ **グループ LOD は 1 字も変えていない** —— 「入るだけ出す」は `FR-055`（全体表示）が既に担っており、表 T-068 の 2 回走行がその往復である。LOD 自身を高さ依存にすると同じ機構が 2 つになり、`LC-2` が `LC-9` を待って**最も縮めたときに最も働く**形になる。**`figures` 10 は不変**、`tables` は 119 → 120、`rows` は 1434 → 1437、`uids` は 140 → 142 |
| 0.50 | 2026-08-17 | ⭐ **稼働日を数える暦を文書に 1 つと定め、Chapter 6.1 に W2 の幾何の算式を置いた**（CR-164 / CR-165。うち 1 件は利用者の裁定）—— ⭐ **CR-164: 仕様書が 2 つのことを言っていた。** `FR-054` と `FR-088` は「文書が持つ暦」「同じ暦」「文書の暦」と 3 か所とも単数で書く一方、原稿の `RL-9` は `Task.calendarUid` を「このタスクが使う暦」と書いていた。**`RV-1`（実績バーの右端）を実装しようとして当たった穴である。** 利用者の裁定は**文書に 1 つ**で、`Project.calendarUid` → `isBaseCalendar` が真のうち `ordinal` の最小 → 表 T-209 の既定、の順に解く規則を `FR-054` へ置いた。⚠️ **タスクごとにすると、1 行に載る 2 本が違う日数で数えられ、イナズマ線の頂点と遅れの日数を行の中で比べられなくなる** —— マルチバーが本ツールの中核である以上、これは通せない。 **`Task.calendarUid` と `Resource.calendarUid` は往復のためだけに保持する。** そう原稿の意味欄へ明記し、`PI-1` に `workingCalendarOf` を 1 つ足した（数え方を 1 か所に閉じよという 表 T-069 の注と同じ理由である）。⛔ **出自の欄（`Consume`）は変えていない** —— 往復の扱いは変わっておらず、決めたのは導出に使うかどうかだけである。⭐ **CR-165: 「無い」と思ったものの半分以上がまた在った。** 依存の種別とアンカーを、**折れ点の本数まで**含めて 表 T-018 が持ち、経路の規則を 表 T-018a が、フェードの多角形を 表 T-012a が、記号を 表 T-021 が、頂点の横位置を 表 T-022 が既に持っていた。⛔ **本当に無かったのは算式 10 件と経路の分岐 8 通りで、前プロジェクトの確定文書と PoC が全部持っていた**（`03-ui-naming` の §4-9 は「この節が依存線の経路の正である」と自ら宣言している）。表 T-221 を 3 行から 13 行へ広げ、**表 T-222（依存線の経路の分岐）を新設**し、`SWS-3` 〜 `SWS-5` で受けた。⚠️ **PoC が仕様と食い違う箇所を踏んだ。** PoC は `markerSize` を文字サイズから導くが、`FR-094` と `S-22` は固定 16px でズームに追随させないと定めている。**仕様が勝つ** —— 前プロジェクト自身が、そう決めた根拠を後の追試で反証している。⭐ **`S-20` の「範囲の理由」が走りの算式を前提にしていたが、それを規則の置き場に数えなかった** —— `_assets/tbl-settings.md` の §3 が「範囲の理由の欄は範囲の根拠であって既定値の根拠ではない」と自ら定めており、**規則を持つ場所ではない。** `LF-4` に置いた。**`figures` 10 は不変**、`tables` は 120 → 121、`rows` は 1437 → 1455、`uids` は 142 → 145 |
| 0.51 | 2026-08-17 | ⭐ **波 W3 を止めていた裁定 8 件を閉じた**（CR-166 / CR-167 / CR-168。すべて利用者の裁定）—— ⭐ **CR-166: 仕様書が既に「多値は対象・真偽は対象外」という割り方を 2 か所で作っていた。** `UN-13` が文字サイズ（表 T-202 の `S-70`、3 値）を取り消しの対象に入れており、`FR-049` が「対象は**型が真偽である行だけ**とすること（MUST）」と定めている。**新しい原理を持ち込まずに、積む向き・予実の表示・ガイドカーソルの 3 件を `UN-13` へ寄せた。** 畳みと非表示は `UN-14` へ —— **どちらも `TaskGroup` の列**であり、`UN-7` が対象外とするのは 表 T-202 の行だからである。対象外には `UN-16`（見る場所の割り付けと出力の設定）を立て、**`UN-12` が既に作っていた「保存するが戻さない」の形に揃えた。** 全体表示が人の畳んだ状態を捨てる部分は `UN-17` で対象とし、**1 回を 1 段にまとめる MUST を `FR-031` へ置いた** —— 戻るのは畳みだけで、倍率と表示位置は `UN-8` のまま対象外である。⭐ **CR-167: 表 T-050 に対になる 表 T-223（複製の連鎖）を新設した。** ⚠️ **調べたら穴でなかったものが 1 つあった** —— 「その `Task` に付いた注記を写すか」は問いとして成り立たない。`CommentBox` も `HighlightBox` も `Task` を指さず、日付と行の識別子で位置を持つ（`FR-019`）。**表 T-050 の `CD-1` が注記を挙げていないのも同じ理由である。** `DU-1` が複製するものは **`CD-1` が消すものと `TaskOrigin` を除いて一致すること**を MUST にした —— 複製してすぐ消したときに元と違う文書が残ってはならない。⭐ **貼り付け先を「選んでいる行の子」としたのは、`FR-033` 自身が「貼り付け後の深さが `FR-004` の上限を超える複製を受け付けてはならない」と書いているからである** —— 兄弟として着けるなら深さは変わらず、その MUST NOT は一度も働かない。⭐ **CR-168: `clearDualCursor` は半分が既に答えられていた** —— `guideCursorMode` を触るかは `FR-048` と `DC-4` が MUST NOT で閉じている。残る「置いた 2 本を消す経路」を `DC-7` に置いた（表 T-076 の `EP-6` が値の在る限り書き出しに 2 本を描くので、消す入口が無いと消せる経路が存在しない）。⚠️ **`pinTaskGroup` の上限では 表 T-014 の `ST-7` の形を採らなかった** —— あちらは「届いたときは入力か操作の異常である」と自ら書く安全弁であり、5 件のピン止めは**人が普通に届く上限**である。`FR-085` が深さの上限で採っている「受け付けずに知らせる」に揃えた。`setProjectProfile` は 表 T-224 で 10 項目・編集可 8 列に絞り、**除外した 12 列すべてに持ち主を示した。** **`figures` 10 と `uids` 145 は不変**、`tables` は 121 → 123、`rows` は 1455 → 1470 |
| 0.52 | 2026-08-17 | ⭐ **アイコンの名簿と図形を仕様書の中へ入れ、公開する名前の落ちを埋め、稼働する曜日を 1 つも持たない暦を禁じた**（CR-169 / CR-170 / CR-171。CR-172 は未適用で利用者の裁定を待つ）—— ⭐ **CR-169: またしても「無い」と思ったものの大半が在った。** 入口を 2 か所に置かない規則・説明を出す作法（`EZ-2` と `S-124`）・絵文字と外部素材の除外（`SO-13` / `CN-7`）・行の操作子の置き方（表 T-051）・置ける図形の全数（表 T-012 の `SH-1` 〜 `SH-5`）・アイコンが載る面の名前（表 T-103）・アイコンが動かす値（表 T-202 / T-203 / T-206）は、**すべて既に在った。** ⛔ **本当に無かったのは、`FR-029` の MUST が寄りかかっている先である** —— 参照先は冒頭で「この文書は決定ではない（status: draft）」、末尾で「正へ書き足す権限を持たない」と自ら書き、`grep -rl "^authority:"` の 4 件にも入っていない。⛔ **その正は、仕様書が MUST で図形を求める `Row Pin`（`FR-098`）と `Row Expander`（`HF-1`）を 1 つも持っていない。** ⛔ **員数の文も違っていた** —— 「22 個の図形と 5 群」はアプリヘッダーだけの数で、実数は 57 である（参照先自身がそう書いており、書き戻し未払いの §6 では「21 個」と書いて自分と食い違っている）。⭐ **57 個の図形を 図 F-019 として起こし、名簿を 表 T-109（61 行）として `_assets/tbl-glossary.md` の §8 に置いた。** **図は図形だけの正、表は「どれが在るか・どの面のどの群か・何の入口か・持ち主は誰か」の正であり、繋ぎ目は行 ID `IC-nn` 1 つである** —— 鍵は写した事実ではないので 2 か所管理にならない。これで `EZ-1` を指すテストが仕様書の 1 行を指せる（1.9 の「第 1 列は行 ID とする（MUST）」）。⭐ **`OP-2` の断定が仕様書の中だけで確かめられるようになった** —— 「変更前の予定」は `baselineVisible`（`S-69`）の表示の切替であってファイルを読む入口ではない。**仕様書が自分で決着させている**（`OP-9` の「選ぶ入口は `OP-3` の第 3 の選択肢である」）。⛔ **員数を数えて 4 つ目の穴が出た** —— 表 T-023b の構え 6 つのうち `AR-4`（依存線）だけ入口の図形がどこにも無い。`IC-58` 〜 `IC-61` を「未描画 🔎」とし、表 T-026 に `RC-13` を立てた（`S-131` / `RC-12` と同じ形）。⛔ **図形を発明していない** —— 図形の決定は利用者の裁定である。⚠️ **英名の欄を作らなかった** —— 作ると 57 個の確定名を新設することになり、`milestoneGlyph` の 8 つの綴りは CR-172 が裁定に回した未決事項なので、先に綴ると裁定を奪う。⭐ **CR-170: 表 T-064 が「外から呼んでよい名前の全数」と書きながら 5 つ落としていた。** `PI-1` に `dayOf` / `textOfDay` / `compareDays`（6 ファイルが越境して呼ぶ。**他の越境名は全部宣言済みで、正確な穴だった**）、`PI-9` は 8 つの `editX` を落として `editDocument` / `Refusal` / `SettingsLimits` にした —— **`src/` の中で `EditDocument` が公開する名前のうち実際に呼ばれているのは `editDocument` だけであり、8 つ宣言することは `LR-2` の柵を 8 つぶん広げることである。** 集約ごとの割りは 表 T-063 の `UT-2` が既に持つ（1.9）。⚠️ **単体テストは 8 つを呼ぶが、9.1 が単体テストを仕様書の外に置いている。** ⛔ **`DocumentCommand` は `PI-8` のまま動かさない** —— `edit-document.ts` が理由を書いており、動かすと `LR-3` の層内循環になる。⭐ **CR-171: 仕様書は 7 つの曜日をすべて外した暦を止めていなかった。** `edit-calendar.ts` は「拒むのはこのファイルが発明した規則になる」と書いて受け付けており、`FR-054` が解いた暦がそれになると稼働日の数え上げが答えを持てない。⚠️ **投げる先が無い** —— `src/` に `catch` は 1 つも無く、`EditResult` も `ChangePlan` も値の直和で、`FR-028` は「例外を投げてはならない（MUST NOT）」と定める。しかもレイアウトの計算は `Task` 1 つにつき 1 フレームに 1 回そこへ届き、`ACCEPTED_DAY_SPAN` は 84,364 日である。⭐ **表 T-014 の `ST-7` の形は採らなかった** —— あちらは `S-89` の備考が自ら「測って決めた値ではない…届いたときは入力か操作の異常である」と書く安全弁であり、こちらは**人が普通に届く**。版 0.51 が `pinTaskGroup` で下したのと同じ裁定であり、`FR-085` と `FR-098` の「受け付けずに知らせる」に揃えた。⭐ **条件は 表 T-220 の `IV-17` に 1 か所だけ置いた** —— 交換相手の暦は曜日の行を 1 つも持たないことがある（`WeekDays` は必須ではない。正は Chapter 6.2 が指す公式 XSD）ので、拒む場所を `CM-39` だけにすると取り込みの経路が開いたままになる。⚠️ **解かれなかった暦は違反にしない** —— 往復のためだけに保持する暦（`FR-054`）まで縛ると書き戻せなくなる。⛔ **`FR-023`（取り込みの検証）と `DaySpanTooWide` は触っていない** —— どちらも `ValidateImportedDocument`（`PI-13`）を書くときの仕事である。⛔ **CR-172 は未適用** —— 綴られていない列挙 4 件は**メンバがすべて決まっており、無いのは格納する英語の綴りだけである**（13 のうち 11 が仕様書のどこにも無く、`triangleUp` / `triangleDown` / `calloutBox` は発明になる）。**`erd.json` は lowerCamelCase、`tbl-settings.md` の 表 T-202 は kebab-case で流儀が割れており、どちらに従うかを書いた文が無い。** `AT-99` / `AT-104` の「`'left'` ほか 3 値」は 3 とも 4 とも読めるが、⭐ **4 つ目の揃えは `docs/spec` のどこにも無いので員数の文の曖昧さである。** `S-65` と `S-104` は `erd.json` ではなく `tbl-settings.md` の行であり、生成器の変更を伴う。**`uids` 145 は不変**、`tables` は 123 → 124、`figures` は 10 → 11、`rows` は 1470 → 1533 |
| 0.53 | 2026-08-17 | ⭐ **設定値の原稿を機械可読にして値がコードへ届くようにし、0 期間のタスクが LOD で永久に消えるのを止め、綴られていない列挙 4 件を綴り、取り込みの上限に単位と起点を与えた**（CR-172 / CR-173 / CR-174 / CR-175。すべて利用者の裁定）—— ⭐ **CR-175: `S-49` を 2 から 6 へ変えたのに、生成物も型も試験も 1 つとして知らせなかった。** 追うと `SETTINGS_BOUNDS` が生成するのは範囲だけで既定値ではなく、既定の 2 を持っていたのは試験が組み立てる設定値ただ 1 か所だった。⚠️ **規模の見立ても誤っていた** —— 打ち直しは「4 件」と見積もっていたが、1 つの試験ファイルだけで 53 件あった。見せ方の群の原稿を `_source/settings.json` へ移し、`_assets/tbl-settings.md` をそこから起こす生成物にした。⛔ **本節は以前これを「得るものに見合わない」として退けており、覆したのは当時は無かった事実が 2 つ出たためである** —— 日英で刊行すると表も行も 2 倍になり値が 2 か所に在ることになる件と、打ち直しが実際に起きた件。⭐ **移送は往復のバイト一致で証明した** —— 原稿から起こした文書が元と 1 バイトも違わないこと。⚠️ **その往復が、空のセルを区切る空白を 1 つではなく 2 つ書いてしまう差を 2 行で捕まえた** —— 人の目視でも検査でも出ない差である。⭐ **受け入れ試験は「原稿の値を 1 つ変えると試験が落ちるか」とした** —— `pxPerDayAt1x` を 6 から 12 にすると 12 件以上が落ちる。⭐ **型を読めない 3 件と日付列の名簿も閉じた** —— 生成器の正規表現を賢くするのではなく、原稿に機械可読な型を書く形で。⛔ **`format` には相乗りしなかった** —— 原稿のスキーマが「日付は交換相手から来た形式を保つ（`EX-4`）ので format を持たない」と明記しており、分類と直列化形式は別だからである。⭐ **生成物が自分の出どころと作り直し方を述べることを MUST とし、検査 21 で機械が見るようにした** —— ⚠️ **`_assets/tbl-settings.md` は「本書が設定値の正である」と書いたまま生成物になり、その 1 文が偽になっていた。道標が無いことより、古い道標が残っていることのほうが危ない。** ⭐ **`GRS JSON` のスキーマを言語から切り離した** —— **`FR-024` の文書形式の契約であり、機械と交換相手が読むものであって読み物ではないので、言語ごとに 2 つ在ってはならない。** ⛔ **しかし日本語を 327 字持ち、日英で割れる場所に置かれていた。** ⚠️ **英語で置き換えるのでは解けない** —— 18 の説明は 表 T-056 にも刷られており、置き換えると日本語の仕様書が英語になる。**`erd.json` のエンティティ説明を `{ja, en}` の辞書にし、表 T-056 は `ja` を、スキーマは `en` を刷る形にした。** ⭐ **`fig-erd-detail.md` がバイト一致であることが、日本語側を 1 文字も変えていない証明である。** ⭐ **その上でスキーマを `_source/` へ移した** —— ⚠️ **「`_source/` は原稿だけの場所だから生成物を置くな」という反対は、測ったら成り立っていなかった** —— `overview.json` と `.drawio` は既に `components.json` から起こした生成物であり、しかも本節が `_source/` を正当化する理由は編集可否ではなく言語である。⭐ **よってフォルダ名で素性を伝えるのをやめ、各ファイルが自分で名乗る形に一本化した** —— 原稿は「編集せよ」、生成物は「手で直すな」と、どこから起こしたかと作り直し方を添えて。⛔ **README では代えない** —— 検索結果からファイルを直接開いた者には届かないからである。⚠️ **`.drawio` の名乗りの置き場は実測で決めた** —— XML の前書きに置くと draw.io CLI が export に失敗し、`<root>` の直後なら通って出力はバイト一致する。**検査 21 が `_source/` の全ファイルについてこれを見る。** ⛔ **表 T-206 と 表 T-209 は手を付けていない** —— 名前は実装が既に持っているが、「保存しない値は呼び手が引数で渡す」という継ぎ目を覆すかどうかが未裁定である。**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 はすべて不変** —— 変えたのは値の在り処であって、値でも表でもない。⭐ **CR-174: ⭐ **CR-174: `FR-001` は 0 期間のタスクを `minShapeWidth` の幅で描くと定め、CR-163 はその描いた幅を `S-86`（24px）に当てていた。床は倍率に依らないので、クリック 1 回で作ったタスクがどの倍率でも画面に出なかった**（`UC-001` 拡張 2a の MUST が作らせるものである）。⚠️ **絵を描いて数えたら、前回並べた 4 案のうち 2 案が消えた** —— 案 (3)（`S-86` を下げる）は ⛔ **LOD そのものを殺す**（床は全 `Task` に掛かるので、床がしきい値以上になるとどの `Task` も落ちなくなる。実測で 48 / 48 を描いた）。案 (2)（図形の幅で測る）は ⛔ **4 形状のうち 3 つで効かない** —— マイルストーンが助かるのは `shapeHeightOf` が 1.5 で床でも 32.9px あるからであって、矩形は `zoomY` < 0.86 で、矢印と端点スパンは常に 24 を割る。⭐ **免除は特例ではなく規則の言い直しとして書いた** ——「タスク LOD が測るのは期間から出た幅である。幅が期間から出ていない形状を落としてはならない（MUST NOT）」。**この 1 文はマイルストーンがなぜ助かるのかも同時に説明する**（それまで `schedule-layout.ts` のコメントにしか無かった）。**免除される集合は倍率に依らず一定なので、`FR-018` の単調性は論証を要さずに保たれる。** `S-49` を 2 → 6 にした（由来は端点の掴み代 `S-90`。2px は事実上見えず、⚠️ **PoC も同じ 2 を持っていたので参考からは新しい値が出なかった**）。⛔ **値を変えても検査も試験も 1 つも知らせなかった** —— `SETTINGS_BOUNDS` が生成するのは範囲だけで既定値ではなく、既定の 2 を持っていたのは試験が組み立てる設定値ただ 1 か所だった。**繰り越し (k) の実例である。** ⭐ **CR-172: 図形も意味も仕様書が既に決めており、決まっていなかったのは保存する英語の綴りだけだった** —— ⭐ **CR-172: 図形も意味も仕様書が既に決めており、決まっていなかったのは保存する英語の綴りだけだった** —— `milestoneGlyph` の 8 値は 表 T-012 の `SH-5` が図形と順序を（`S-48` が面積順と定める）、`nameAlign` の 3 値は `FR-002` が、`lineWeight` の 3 値は 表 T-017 の `CL-2` が、`leaderShapeKind` の 2 値は `FR-019` が既に持っていた。`shapeKind` の流儀（lowerCamelCase）に倣い、⛔ **表を新設せず `erd.json` だけに置いた**ので `tables` も `rows` も動かない。⚠️ **`AT-99` / `AT-104` の「`'left'` ほか 3 値」は 4 とも 3 とも読めていた** —— 4 つ目のメンバは `docs/spec` のどこにも無く、**メンバが足りないのではなく員数の文が曖昧だった**ので「3 値」に直した。⭐ **綴りが出た瞬間に、コードの注記 4 か所が嘘になった** —— `CM-49`（`setCommentBoxLeaderShapeKind`）は「2 種の綴りが無いので `FR-019` に守らせるものが無い」として**入口が丸ごと拒否**であり、その検査もわざと外されていた。どちらも動くようにした。⛔ **20 検査は 4 件とも知らせない。** 生成物は 表 T-058 の `AT-99` と `AT-104`、図 F-011 の同じ 2 つの列、スキーマの 4 件、`schedule.ts` の 4 型が動き、**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 は不変。** ⭐ **CR-173: どちらも値ではなく、値の読み方が無かった** —— 32 も 64 も既に在り、⛔ **`1024` は `docs/spec` に 1 度も現れなかった。** `S-113` は「`32` MB」と書きながら 1 MB が何バイトかを持たず、`S-115` は WBS の深さの上限 64 を書きながら根の行が深さ 0 か 1 かを言っていなかった。⚠️ **どちらも境界が 1 つ静かにずれるだけで、例外にも警告にもならない** —— 32,000,001 バイトのファイルが通るか弾かれるかが黙って入れ替わる。⭐ **見つけたのは `UF-22` を書いた体であり、決めずに ⛔ を付けて報告した** —— 実装が勝手に決めていれば、仕様書は無傷に見えたまま 2 通りに読めていた。`S-113` の備考に「1 MB = 1024 × 1024 バイトとする」を、`S-115` の備考に「根の行を深さ 1 と数える」を足した（後者は `FR-004` の「大 / 中 / 小」と一致する）。**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 はすべて不変** —— 既存の 2 行の備考欄への追記であり、生成器は備考欄を読まない |
| 0.54 | 2026-08-17 | ⭐ **原稿が刊行する散文を言語ごとの辞書にし、その規則を機械検査へ下ろした**（CR-176。射程は利用者の裁定）—— ⛔ **本節は版 0.53 で「散文のうち刊行される欄は、言語ごとの辞書として持つこと（MUST）」と定めたのに、同じ回に触った原稿 2 本がその MUST に従っていなかった。** `_source/erd.json` の 213 本と `_source/settings.json` の 62 本が素の文字列だった。⭐ **規則が仕様書に在っても、測る検査が無ければ同じ回のうちに破られる** —— よって、原稿が刊行する散文を言語ごとに持っていることを見る機械検査を 1 本足した。⚠️ **日本語であることと散文であることは別である** —— 型の欄と `null` の欄は分類であり、辞書にすると表示の語を原稿へ焼き付けることになる。⛔ **そこで Chapter 6.2 に「分類を述べる欄を辞書にしてはならない（MUST NOT）」を足し、検査の側では分類の 282 本を行き先と員数で免除した** —— 員数を書かない免除は儀式になるからである。⭐ **移送は往復のバイト一致で証明した** —— 書き出し器が変換前の原稿を 1 バイトも変えずに再現することを先に測ったので、そのあと動いた分は変換だけだと言える。⚠️ **その照合が 1 度落ち、落ちたのは変換ではなく照合器だった** —— 剥がす範囲を絞らずに書いたので、元から辞書だったセルまで剥がしていた。`erd.json` では単鍵の辞書が 1 つも無かったので通っており、2 本目の原稿が「たまたま通る形」を暴いた。⭐ **前セッションの実測も誤っていた** —— 散文 201・非散文 263 と数えていたが、`container` の走査が抜けており、実際は 213 と 282 である。図 F-010 の箱に刷られる語 7 本がそこに在った。⛔ **契約と検査はわざと壊して確かめた** —— 10 通り壊し、10 件とも落ちた。⚠️ **生成器が自ら持つ日本語には手を付けていない** —— 表の見出しと定型の文が `erd_json_to_md.py` の中に在り、刊行される散文なのに原稿の側に無い。移すと構造の変更になるので繰り越した。**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 はすべて不変で、生成物 5 本はバイト一致である** —— 変えたのは散文の持ち方であって、値でも表でも刊行物でもない |
| 0.55 | 2026-08-17 | ⭐ **既定のマイルストーン図形を ◇ と定め、列が既定を持てる場所を原稿に作った**（CR-177。利用者の裁定）—— ⛔ **既定を書く場所そのものが無かった** —— `erd.json` の 138 列に `default` の鍵は 1 つも無く、仕様書の側にも既定を述べる行が無かった（`SH-5` は面積順に 8 図形を並べるだけ、`P-16` は用語として挙げるだけ、表 T-202 に鍵が無い）。⚠️ **`schedule-geometry.ts` は全マイルストーンを ◇ で描き、「これは仕様の言明ではなくこのファイルの選択である」と自ら書いていた。** ⭐ **機械可読な既定を原稿に置き、表 T-058・`GRS JSON` のスキーマ・型の 3 つをそこから起こした** —— 値の家は 1 つで、`FR-078` は値を書き写さず `AT-101` を指す（書き写しを MUST NOT で禁じた）。⭐ **網を 3 段とも個別に確かめた** —— 語彙の外の既定は生成が止め、生成器の意味検査を外した場合は `tsc` が止まり、刊行された表と型が食い違えば契約テストが落ちる。**片側だけ動かす壊し方を 2 通り試して 2 件とも落ちた。** ⛔ **前回の報告に誤りがあり、本 CR で訂正した** —— 表 T-109 の `IC-27` を「既定は 〇 と既に書いてある」と読んだが、`IC-27` 〜 `IC-34` は 8 図形ぶんのパレット入口であって既定の主張ではない。**1 行だけ見て読み、表の前後を見なかった。** グラフで指し先を測ったから出た。⚠️ **`IC-27` は動かしていない** —— 動かすと 〇 の入口が消える。⚠️ **既定を文書へ保存する形にはしなかった** —— `null` は「選ばれていない」であり、既定は読むときに当てる。人が選んでいない値を焼き付けない（`FR-085` 系の裁定と同じ形）。⛔ **選んだ図形が無視される穴は残っている** —— `TaskPlacement` が図形を運んでいないので、☆ を選んでも黙って ◇ が出る。**既定より大きい穴であり、繰り越し (l) として残した。** ⚠️ **既定を刷る日本語の断片は生成器の中にある** —— 生成器の定型日本語をまとめて原稿へ移す作業（繰り越し (u)）に載せた。**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 はすべて不変** —— 既定は新しい行ではなく `AT-101` のセルに刷った。試験は 671 から 672 件 |
| 0.56 | 2026-08-17 | ⭐ **表 T-206 の値を機械可読にし、型を持つユニットへ届けた**（CR-178。利用者の裁定）—— ⛔ **この表の 16 行は生成された既定値に 1 つも入っていなかった** —— 生成器は行の鍵で引くのに、**表 T-206 の行には鍵の欄が無い**（値の欄が「予定の端点の掴み代」という散文である）。⛔ **`S-90` の数は散文の中にあり、`S-92` と `S-93` は 2 つの数が 1 つの文字列に入っていた。** ⭐ **原稿に機械可読な形（数と、その前に付く語、2 つ組）を与え、鍵は行 ID そのものにした** —— 行 ID は仕様書自身の識別子なので、名前を 1 つも発明していない。⭐ **継ぎ目は覆していない** —— 保存しない値は今までどおり呼び手が引数で渡し、生成したのは「渡すべき正しい値」である。⚠️ **単位を注記に刷るようにした** —— でなければ `64` が MB かバイトか読めない。⛔ **その過程で `S-95` に 1 MB の定義が無いことが出た** —— CR-173 が `S-113` で閉じた欠陥がこちらで開いていたので、同じ裁定の言い直しを備考に足した。⭐ **「原稿の値を変えると両側が一緒に動く」ことを実測した** —— そしてその測定が本物の欠陥を暴いた: **掴み代の注記が値を写しており、原稿を変えても古い数を言い続けた。** 注記は行 ID だけを名乗る形に改めた。⛔ **罠を 2 つ踏んだ。** ⚠️ **「バイト一致」が空証明になった** —— 契約が原稿を拒否して生成物が 1 バイトも書かれていないのに、比較は「一致」と答えた。**書かれていないファイルとの比較は何も証明しない。** ⚠️ **区間の印を逐語で引用したら、生成器がそこを区間と見なして冒頭のコメントの中へブロックを差し込んだ** —— CR-175 が踏んだ「印にパスを埋める」と同じ族であり、**印はファイルに 1 度だけ現れなければならない。** ⚠️ **表 T-209 は含めていない** —— 保存する側の値で収まる先が暦と `Project` の列であり、稼働する曜日と週の始まりに機械可読な表現が無い。**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 はすべて不変**で、刊行物で動いたのは `S-95` の備考 1 セルだけである。試験は 672 から 678 件 |
| 0.57 | 2026-08-18 | ⭐ **文書が使えない日付を黙って取り込まないようにした**（CR-179。利用者の裁定）—— ⛔ **日付として読めない値は素通りしていた** —— `ValidateImportedDocument` が「仕様の決めることだ」と自ら書いて残していた穴であり、⚠️ **振る舞いを変えても 678 件の試験は 1 件も落ちなかった。誰も見ていなかった。** ⛔ **調査で本節の側の食い違いが 2 つ出て、裁定を 2 度に分けて受けた。** ⚠️ **1 つ目: 範囲の外にある日付は既にファイルごと拒否と定めてあった** —— 読めない値にだけ「その行を落とす」を認めると、隣り合う 2 つの欠陥で扱いが割れる。⭐ **そこで「文書が使えない日付」という 1 つの類にまとめ、読めない値も範囲の外の値も同じ 2 択（その行を落として取り込む／読み込みを中止する）にした** —— 20,000 件の取り込みが 1 行の日付で丸ごと落ちる振る舞いも同時に直っている。⚠️ **「部分的に適用してはならない」は残した** —— 射程は資源の上限である。**分ける根拠を書いた: 上限は落とすべき行を名指しできないが、日付は行と列を名指せる。** ⚠️ **2 つ目: タスクを取り除く連鎖は 表 T-050 の `CD-1` が既に持っていた**（WBS の子孫も消える）。⛔ **利用者は最初「子は祖父母へ付け替える」を選んだが、`CD-1` の存在を示すと `CD-1` への一本化に改めた** —— 取り除く連鎖を 2 通り持たないためである。⭐ **その結果、本 CR は連鎖の規則を 1 行も書いていない。** ⚠️ **代償は本物である** —— 見出し行の日付が 1 つ壊れているとその下の部分木がまるごと落ちる。**2 択の 2 つ目がその代償を選べる形にしている。** ⭐ **空文字を特例にしなかった** —— 空を許す列の空は `null` であり、空文字は既に契約の外にある。⛔ **2 択を出す面と、落として取り込む処理は未実装である**（`Adapter` と `Framework`）。**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 はすべて不変**で、生成物は 1 つも動いていない —— 散文と不変条件のセルを書き直しただけである。試験は 678 から 681 件 |
| 0.58 | 2026-08-18 | ⭐ **曜日がどの数かを仕様書に書き、既定の暦を原稿から届けた**（CR-180。利用者の裁定）—— ⛔ **無かったのは値ではなく値の読み方である。** 本仕様書は曜日の数がどの曜日を指すかを 1 度も書いていなかった —— `AT-73` の意味欄は「曜日」の一語、`AT-17` は「週の始まりの曜日」だけである。⛔ **ところが交換相手は曜日を 3 通りに数えており、そのうち 2 つを我々の列が使っている** —— `WeekDay/DayType` は `1` が日曜（`0` は例外日）、`Project/WeekStartDay` は `0` が日曜。**つまり同じ月曜が、一方では 2、他方では 1 と書かれる。** ⚠️ **取り違えても例外にも警告にも出ず、稼働日が 1 日ずれた日程を黙って書き戻すだけである。** ⭐ **両方の意味欄に番号の起点を書き、互いに 1 ずれることを双方から名指した。** ⭐ **実装だけがこの対応を知っていた** —— 稼働日を組み立てる行が「`dayType >= 2` かつ `dayType <= 6`」と書いており、その行の注記がこの対応の書かれた唯一の場所だった。**その打ち直しは消え、値は原稿から届く。** ⭐ **曜日名は原稿に名簿を 1 つ置き、表示は生成器が刷る形にした** —— 表の側は数だけを持つので、数と語が食い違う余地が無い。**どの体系で数えたかをセルが必ず名乗る**ことも契約で強制した。⭐ **端から端まで連動することを実測した** —— 稼働日を月〜木に変えると、刊行された表・生成された定数・組み立てられる暦・試験がすべて動いた。⛔ **契約と生成器はわざと 7 通り壊し、7 件とも落ちた。** ⭐ **検査 23 が私自身の誤りを捕まえた** —— 名簿の区切り「・」を「言語に属さない」と書いたが、これは日本語の約物であって英語版では読点になる。**前の版で足した検査が、次の版の中で働いた。** ⛔ **報告の欠陥も 1 つ直した** —— 原稿が不正なとき、それを知らせる行そのものが文字コードで落ち、行を名指す代わりにスタックトレースで終わっていた。**`tables` 124・`figures` 11・`rows` 1533・`uids` 145 はすべて不変**で、刊行物で動いたのは意味欄 2 セルだけである。試験は 681 から 683 件 |
| 0.59 | 2026-08-18 | ⭐ **数がどの意味かを書いていない列を類として潰し、「どれだけか」を測る規則を 3 つ与えた**（CR-181 / CR-182。すべて利用者の裁定）—— ⭐ **CR-181: 1 件ずつ直さず、原稿の全 138 列から「値そのものが符号である列」を機械的に抽出した。** 出たのは 7 列で、**穴は 2 つだけだった** —— 依存の種別は既に書いてあり（正典と突き合わせて一致を確認した）、色相と完了率は符号ではない。⛔ **残った 2 つは、繰り返しの種別（1〜9）と名前ラベルの 9 点アンカー（0〜8）である。** ⚠️ **後者は指し先が答えを持っていなかった** —— 意味欄が指す表は自動配置の決定順であって、アンカーの格子ではない。⭐ **実装は正しく振る舞っていた** —— 範囲だけを見て「どの表も言っていない」と ⛔ 付きで書き、推測していなかった。⭐ **どちらも発明していない** —— 繰り返しの種別は交換相手の正典に、9 点は前プロジェクトの確定文書（外接矩形の 9 点、左上から右下）にあった。⚠️ **ただし後者は依存アンカーについての記述であり、同じ 9 点を 2 通りに数えるほうが危ないと判断して当てた。** ⭐ **CR-182: 3 つとも「値はあるのに測り方が無い」型である。** ⛔ **フェードは「0 以上」と定めてあるのに原稿が下限を持たず、生成されたスキーマが負の値を通していた。** ⛔ **取り消しの 1 段は 64 MB を上限としながら、実装は保存形の文字数で測っていた** —— **和文は 1 文字 3 バイトなので上限が約 3 倍に緩んでいた。** 測り方を要求の側に置き（値の表に規則を書くことは検査が禁じている）、実装をバイトへ改めた。⭐ **試験は「同じ文字数の ASCII と和文で、記録された大きさの差がちょうど 2 バイト × 文字数」とした** —— 文字数で測れば差は 0 になる。実装を戻して落ちることを確かめた。⛔ **`TextEncoder` を使おうとして型検査に止められた** —— 層の規則は DOM ライブラリ抜きのコンパイルで強制されており、「これは DOM ではない」という理屈は通らなかった。**規則が、読み手なら見逃した場所で効いた。** ⭐ **解釈しない要素の入れ子に上限を与えた** —— 意味欄が「深さの上限は定めない」と明記しており、**信頼できない入力が運べる無制限の入れ子だった。** **`rows` は 1533 から 1534**（`carryMaxDepth` の 1 行）、`tables` 124・`figures` 11・`uids` 145 は不変。試験は 683 から 684 件 |
| 0.60 | 2026-08-18 | ⭐ **生成器が抱えていた刊行文を原稿へ出した**（CR-183）—— ⛔ **版 0.54 が「原稿の散文は言語ごとに持つ」と定めたのに、生成器が持つ刊行文はその網の外に居た。** バナー 3 行・4 つの表の見出し 25 セル・派生する関係行のラベルと意味・真偽の欄が刷る語・既定を刷る文・型の詳細を繋ぐ記号・鍵を繋ぐ記号が Python の中にあり、**日英で出すにはコードを編集する必要があった。** ⭐ **すべて原稿の `printed` へ移し、生成物は 2 本ともバイト一致である** —— 刷る場所を変えただけで、刷られる文字は 1 つも動いていない。⭐ **重複を 1 つ消した** —— 表の見出しと `---` の罫線は別々に書かれており、生成器は「2 つのパイプの数が合っているか」を検査していた。**2 か所に書いたから要る検査だった。** 罫線を見出しの数から導いたので、その検査ごと消えた。⚠️ **短い記号は言語に属さない、という思い込みをまた踏みかけた** —— 型の詳細を繋ぐ `・` と鍵を繋ぐ `＋` は刷られる区切りであり、英語版では別の記号になる。**前の版で検査が同じ思い込みを捕まえたばかりだった。** ⭐ **契約はわざと 6 通り壊し、6 件とも落ちた。** ⛔ **残したものがある** —— 型のトークンを読む 2 か所は分類の解析であり、検査が員数付きで免除している類そのものである。⛔ **閉じたのは「刷る側」だけである** —— スキーマの生成器は刊行物を解析して日本語の見出しを鍵に使っており、**日英で刊行した瞬間に英語版の表で動かなくなる。** 本当の直し方は生成物ではなく原稿から起こすことであり、繰り越しに立てた。**`tables` 124・`figures` 11・`rows` 1534・`uids` 145 も試験 684 件もすべて不変** |
| 0.61 | 2026-08-18 | ⭐ **「浮いている行」を測ったら、浮いていたのは表が 1 つだった**（CR-184）—— **設定値の 136 行のうち 57 行はどの要求からも指されていない**が、⭐ **これは欠陥ではない** —— 要求は表を指し、行は表を通って届く。**57 行に参照を足すことは、表の中身を要求の側へ書き写すことである。** ⚠️ **前の版で `carryMaxDepth` を足したとき、要求を 1 文字も直さずに済んだのがその証拠である。** ⛔ **本当に浮いていたのは 表 T-217 だけだった** —— そして ⚠️ **その表の行は「ズームによらず一定に描く規則は `FR-019` が持つ」と自ら書いていたのに、`FR-019` にその規則が無かった。** **行が持ち主を名乗り、持ち主は知らなかった。** 規則を `FR-019` に書き、表を指させた。**値は書き写していない** —— 丸みも上限も表が持つ。**数も生成物も試験もすべて不変** |
| 0.62 | 2026-08-18 | ⭐ **色を塗る者が、色に届いていなかった**（CR-185。利用者の裁定）—— ⛔ **`SvgRenderer` から出る辺は 4 本しか無く、`Schedule` への辺が無かった。** ところが `themeHue` は `Project` にあり（表 T-052 の `DR-5` が「見せ方の群が持ってはならない（MUST NOT）」と定める）、`TaskVisual` の線色・塗り色も `TaskGroup.color` も同じ群にある。⚠️ **幾何も色を 1 つ運んでいない**（`color` の綴りが 0 件）。⭐ **5.1 が「描き方は `Adapter` が持ち、`layoutEngine` は座標までしか持たない」と既に分担を宣言しており、公開メンバ名も既に `svgFromSchedule` である**。原稿に辺を 1 本足した。Chapter 5 の散文は 1 文字も直していない —— `adapter → documentModel` の層の矢印は `ScreenRenderer → Schedule` ほかで既に裏づけられていた。⚠️ **再生成の途中で生成器の欠陥を 2 件見つけて直した** —— `.drawio` の後ろ指し（XML 注記）が図を 5 枚とも 437 バイトにしていたことと、`overview.json` の生成物の帯を生成器が書いていなかったこと |
| 0.63 | 2026-08-18 | ⭐ **担当者に面を与えた**（CR-186。利用者の裁定）—— ⛔ **`createResource` / `createAssignment` / `unassignResource` の 3 命令に入口が無かったが、「面が無い」のではなく仕様の中で 2 つの文が食い違っていた** —— `UC-003` 手順 3 は「担当者を変える」と書き、`FR-006` の ORIGIN はそれを名乗るのに、表 T-016 の `PR-16` が「読み取り専用」と書いていた。⭐ **決め手は要望の入力であり、`PR-16` の直接の祖先の行が「割当の追加・編集・解除ができる」と書いていた** —— 面は最初から在り、表へ移すときに落ちていた。面を 2 つとし（担当ラベルとプロパティパネル）、**見かけのキーを担当者名、実際のキーを `uid`** とする規則を 表 T-225（10 行）に置いた。⚠️ **`assigneeVisible` の既定が `false` であるため、既定の状態では担当者を編集する面が 1 つも無く、`FR-049` の MUST に違反していた。** `FR-006` と `FR-029` は 1 文字も直していない |
| 0.64 | 2026-08-19 | ⭐ **初期テンプレートに、置き場と形と版の書式を与えた**（CR-187。利用者の裁定）—— ⛔ **`FR-027` はテンプレートを求めていたが、表 T-075 の 71 ユニットのどれもそれを持たず、中身も仕様に 1 行も書かれていなかった。** バンドル済みの `GRS JSON` とし、**コードで組み立てることを禁じた** —— 取り込みの経路と同じ検証器を向けられるためである。形は 表 T-226（8 行）が持つ —— ソフトウェア開発・3 年・7 工程を矢羽根と節目・最上位の行を複数持つ森・100 行 / 1000 タスク・均一にしないこと。`FR-027` には業種を絞ってよい例外を理由つきで置き（⛔ 個別の業種名と製品名は禁止のまま）、`FR-073` には **形式の版を日付とし、辞書順で判別する** 規則を置いた |
| 0.65 | 2026-08-19 | ⭐ **原稿が言えなかった 8 個の値を、原稿に言わせた**（CR-188。裁定 ㎇ の適用）—— ⛔ **`FR-024` は 97 個すべてを書き出すことを MUST で求めるのに、`exportCanvas` ・`importMinDate` ・`importMaxDate` ・`planActualGuidePattern` ・`rulerFont` ・`rulerHeight` ・`pinnedGroupIds` ・`dualCursor` は機械可読な形で述べられていなかった。** 機械セルに `quote` / `sep` / `parts` を、散文セルに `index` / `from` / `times` / `plus` を足した —— ⭐ **導出は規則であって値ではないので、原稿が規則を持ち生成器が評価する。** ⛔ **行を 1 つも足していない**（`S-81` を 2 行に割る案は、指し先の意味が変わるので捨てた）。⭐ **印字は 1 バイトも変わっていない。** 実測: 原稿の値を 1 つ変えると成果物に届き、戻すと戻った |
| 0.66 | 2026-08-19 | ⭐ **絵は画面であって、中身ではない**（CR-189）—— ⛔ **縦線を通したとき、実物が 2 つの欠陥を出した。** ① `SvgRenderer` が画面の矩形に届かず、絵の寸法に `contentWidth` を流用して図形を枠の外に描いていた —— `FR-080` は「`GRS` が占める画面の全体」を求めるので、原稿に辺を 1 本足した（`SvgRenderer → ScreenRegions`。87 → 88）。② `BO-3` の `OP-10` の分岐を実装していなかった —— `scrollDate` の既定は `null` なので、飛ばすと時間軸の原点が 1970-01-01 になり 2026 年のタスクが `x ≈ 124000` に落ちる。⚠️ **型検査も 684 件の試験も 1 つも捕まえなかった** —— ⭐ **捕まえたのは実 DOM である**（規則 04 の 3.）|
| 0.67 | 2026-08-19 | ⭐ **作り方の規則を 1 か所に集めた**（CR-190。利用者の裁定）—— レビュー観点の規約と仕様書の書き方を `previous-project-result/` から `docs/development-rules/` へ移し、`07-review-standards.md` と `08-spec-template/` とした。本仕様書は 4 か所でそのパスを引いていたので直した。⚠️ **規則 README 自身が「移動もしない」と定めていたものを覚した** —— 規則が 2 か所に散っているほうが高くつくという判断である。⛔ **禁止の理由自体は生きているので、過去の変更要求とレビュー在庫は 1 文字も書き換えていない** |
| 0.68 | 2026-08-21 | ⭐ **描かれていなかったアイコンを描き、名簿を閉じた**（CR-191。利用者の裁定）—— ⛔ **`IC-58` 〜 `IC-61` は図形を持たず、名簿（`Resource Roster`）の入口は 1 行も無かった。** 図形 12 個（`IC-3` の差し替えと `IC-58` 〜 `IC-68`）を 図 F-019 へ置き、`IC-62` 〜 `IC-68` を 表 T-109 へ足した。 ⭐ **`IC-3` は `FR-096` の MUST NOT に触れていた** —— 「画像として書き出す」は形式名を持つ入口であり、同要求は「入口を 1 つとすること（MUST）。形式ごとに別の入口を設けてはならない（MUST NOT）」と定める。**その RATIONALE 自身が「開く側の入口が 1 つである（`FR-087`）のと対に、出す側の入口も 1 つとする」と書いている**ので、行を「書き出す形式を選ぶ」へ読み替え、正を `FR-096`（表 T-036 の `SK-12`）へ移し、図形を形式に依らないものへ差し替えた。 ⭐ **`FR-099` に 1 文だけ足した** —— 未参照の担当者を「まとめて選ぶ」操作が「まとめて消すこと」の実現手段であると書き、**消す入口をちょうど 1 つにした** —— 同要求が既に「すべて選んでから消せば同じことができ、入口を 2 つ置くと `FR-029` に触れる」と自ら書いている。 ⭐ **名簿を閉じる入口は `IC-52` の面の欄に `Resource Roster` を足して足りる** —— 新しい行も図形も要らない（表 T-028 の `IN-4`）。 ⚠️ **ツールチップを消す行は足さない** —— 表 T-103 に確定名が無く、表 T-109 の §8 が新しい面の名を作ることを禁じている。**`EZ-2` のツールチップ表示は変わらない。** ⭐ **表 T-026 の `RC-13` は残して狙いを付け替えた** —— 「未描画」ではなく「**新しく起こした 12 個を実物と見比べていない**」と読む（`RC-12` の `S-131` と同じ形）。 **`tables` 126・`figures` 11・`uids` 145 は不変**、`rows` は 1552 → 1559。図 F-019 は図形 57 → 68・5 行 → 6 行・`viewBox` の高さ 240 → 284 で、**新しい 12 個はいずれも既存の図形の外枠（実測 x 2.40 〜 21.50 ／ y 3.00 〜 21.00）の内側に収まる** —— 手書きの図なので `assert_drawn` が効かず、自分で数えた |
| 0.69 | 2026-08-21 | ⭐ **押した点がどの入口の上かを、描いた側が答えるようにした**（CR-192。利用者の裁定）—— ⛔ **画面に描いた入口が 1 つも押せなかった。** `DomScreenSurface`（`CP-38`）は 表 T-109 の入口を描いているのに、`InputCommandTranslator`（`CP-18`）が読む値は文書・レイアウト・幾何・`ScreenRegions`・`ScreenState`・選択だけで、**「押した点は入口の上か」に答えられる値が 1 つも無かった。** そのため 表 T-023b の構えは `Esc` で解くことしかできず、`FR-083` の `SP-1` 〜 `SP-4` へ入る道が無かった。⭐ **表 T-065 の `IF-9` の供給物に「画面上の点がどの UI パーツのどの入口の上かを答える」を足した** —— **メンバの名前と署名は書いていない**（CR-146 が `src/` の公開エントリを正としている）。⭐ **同表の下に、描いた側が答える理由を 1 文置いた** —— ⛔ **他所で同じ矩形を計算し直してはならない（MUST NOT）**。`ScreenView` の各部に矩形を持たせる案を退けたのは、**記述を組み立てる 9 ユニットがどれも寸法を測る手段を持たない**（`LR-6`）からであり、**同じ判断を 表 T-070 の `MN-6` が計算の側で既に下している。** ⚠️ **`ScreenRegions`（`PI-35`）に持たせる案も退けた** —— 同コンポーネントが持つのは画面の**区分**であって入口ではない。⭐ **`_source/components.json` に辺を 1 本足した**（`InputCommandTranslator → ScreenRenderer`。88 → 89）—— **型を引くことが辺であり、`InputCommandTranslator → ItemHitArea` が同じ形の前例である**（`hit` も呼び手が解いて渡す）。**図 F-017 に矢印が 1 本増え、図 F-013 は 1 バイトも動かない**（層と層の間の対は増えないため）。⭐ **要求・表・図・行・UID は 1 つも動かない** —— `tables` 126・`figures` 11・`rows` 1559・`uids` 145 のまま。⚠️ **表 T-023a の面の表が課す MUST（判定順序は日程の描画領域だけ）は、これで初めて判定できる** —— 浮遊するパレット・重ねて開く面・知らせ・対話欄は `ScreenRegions` の矩形を 1 つも持たないので、それまで `regionAtPointer` はその上の点を `rowArea` と答えていた |
| 0.70 | 2026-08-21 | ⭐ **語で呼ばれていた 2 つの面に確定名を与え、「問う」作法を足した**（CR-193。利用者の裁定）—— ⛔ **表 T-037 の `NT-1` 〜 `NT-6` は 6 行とも「告げる」作法であり、「問う」作法が 1 行も無かったのに、`FR-032`（行と WBS の子孫を消す前）と `FR-099`（割当が解かれるとき）の 2 本が確認を MUST で求めていた。** `NT-7` を足し、**何が起きるかを示して続けるか取りやめるかを選ばせること（MUST）と、消えるもの・解かれるものがあるときはその名前を挙げること（MUST）**を置いた。⚠️ **「件数だけを示してはならない」の逐語は書かない** —— `FR-032` と `FR-099` が 1 つずつ持っており、3 度目を書くと同じ事実が 3 か所になる。 ⭐ **`FR-031` の例外を 2 つに数え直した** —— 同要求は「例外は `FR-032` だけである。例外を増やさない（MUST NOT）」と書いていたが、**`FR-099` の確認は既に MUST で在り、数え落としであった** —— 例外を増やす裁定ではない。 ⭐ **表 T-103 に `Tooltip`（`U-53`）と `Export Chooser`（`U-54`）を足した** —— 前者は `FR-092` の `EZ-2` と表 T-028 の `IN-3` が、後者は `FR-096` と表 T-036 の `SK-12` が語で呼んでいたのに、確定名が無かった。⛔ **`Export Chooser` に名が無いあいだ、その面は `S-99g` に入れず、表 T-028 の `IN-4` が定める `Esc` の第 1 階層で閉じられなかった**（満たされていない MUST である）。表 T-109 の `IC-52` の面の欄に `Export Chooser` を足し、**行も図形も増やさない**（CR-191 が `Resource Roster` にしたのと同じ形）。 ⚠️ **ツールチップを消す行はやはり足さないが、理由が 1 つ入れ替わった** —— 図案セッションが挙げた「`IN-4` の `Esc` が既に閉じる手を持つ」は偽である（`IN-4` の第 1 階層は `S-99g` の「面」であり、`S-99g` 自身が「面とは `Esc` の第 1 階層で閉じられるもの」と定義しているので、**ツールチップに `Esc` は届かない**）。⭐ **残るのは「`IN-3` が求めるのは消せることであって、消す図形を置くことではない」だけであり**、さらに表 T-109 §8 が「図形を持たない行は無い」と宣言済みなので、行を足すには図 F-019 の図形と、その操作子の語の 2 つの裁定が要る。 ⭐ **表 T-075 の `UF-67` の欄を「知らせと確認」にした** —— `ScreenView` に確認を運ぶメンバが 1 つも無かったためであり、**描く側が置き場を発明せざるを得なかった**。⚠️ **メンバの名前と型は書かない**（CR-146 により署名の正は `src/` の公開エントリである）。 **`tables` 126・`figures` 11・`uids` 145 は不変**、`rows` は 1559 → 1562（`U-53` `U-54` `NT-7`）。表 T-109 は **68 行のまま** |
| 0.71 | 2026-08-21 | ⭐ **画面に刷る語に置き場を作った（語は 1 つも書いていない）**（CR-194。利用者の裁定）—— ⛔ **`FR-038` が「メニューとパネルの文字をその言語で示すこと（MUST）」と定めるのに、翻訳された文字列を持つ表が 1 つも無く、`ScreenRenderer` の 5 ユニットが語の在るべき場所に空文字を置いていた。** `FR-038` に **「画面に刷る語は言語ごとの辞書として 1 か所に持つこと（MUST）。要求にも表にも語そのものを書いてはならない（MUST NOT）」** を足し、Chapter 6.2 に置き場（`_source/display-words.json`）を定めた。⭐ **原稿は語だけを持ち、どの語が要るかの名簿を持たない** —— 名簿は 表 T-109・表 T-037・表 T-023 と `FR-072` が既に持っており、生成器が毎回そこから起こして原稿と突き合わせるので、**表が動けば黙らずに落ちる。** ⛔ **語を仕様書の表へ刷らない** —— 空の欄が並ぶだけで規則を 1 つも述べない表になり、表 T-109 が英名の欄を拒んだのと同じことが起きる。⚠️ **`FR-038` の MUST が 2 本、実際に破れていた** —— 切替の入口は画面上部とヘルプの 2 か所なのに **現在の言語を運ぶ値がヘルプ側にしか無く**、「押す前にどちらの言語かが読めること（MUST）」をヘッダーが満たせなかった。また **「言語の状態は 1 つ（MUST）」を宣言する値がどこにも無かった。** 表 T-075 の `UF-60` を **「…束ねて公開し、画面全体に効く表示言語を運ぶ」** に直し、その 1 セルを根拠に `ScreenView` がメンバを 1 つ持つようにした（CR-193 が `UF-67` にした形と同じ）。**`tables` 126・`figures` 11・`rows` 1562・`uids` 145 はすべて不変** —— 変えたのは語の在り処であって、表でも行でもない |
| 0.72 | 2026-08-21 | ⭐ **仕様書そのものの穴を 3 つ閉じ、残る 1 つを名指しした**（CR-195）—— ⛔ **どれも検査 26 本が全部緑のまま見つかったものである。** ⭐ **`K1`**: Chapter 6.1（`:685`）の但し書きが `_assets/grs-document.schema.json` を指すのに、**そのファイルは存在しない** —— Chapter 6.2（`:897`）は `_source/` を MUST で定めており、実物も生成器（`erd_json_to_schema.py:38`）の出力先も `_source/` である。**版 0.53 で移したときに 6.1 の 1 語だけが追随しなかった**。⭐ **`K2`**: `S-125`（`maxGroupDepth`）に数え始めの基準が無かった —— 同じ表 T-211 の `S-115` と `S-133` はどちらも「根を深さ 1 と数える」と書くのに、**間の 1 行だけが黙っていた。** ⚠️ **その 1 行に 3 つの MUST NOT（`:1268` / `HM-3a` / `:1728`）と文書不変条件 `IV-5` が乗っており、1 つずれると 1 段まるごとが静かに通るか静かに拒まれる**（CR-173 が `S-113` / `S-115` で同じ形を記録している）。`S-133` の言い回しをそのまま借りた。⭐ **`K3`**: 表 T-206 に `Panel Divider` の掴み帯の行が無かった —— `FR-051`（`:2368`）が「**掴みやすさのために広げた帯**」と呼ぶのに幅をどの表も持たず、`screen-frame.ts:77` は **幅 0 を出荷していた（どの利用者にも掴めない）**。`S-134` を 1 行足した —— **値 8px は発明ではなく、本変更履歴の版 0.48 が前プロジェクトの PoC の実測として既に記録していたものを採り直した**。⚠️ **`K4` は確認だけを行って閉じていない** —— Chapter 6.1 は正規化を持たないが、**そこを指しているのは `FR-021` だけではなく `FR-080`（`:3083`）もであり、消費者が 2 人いる**。片方だけ埋めない。⛔ **裁定 D8（`ClipboardGateway` の辺）も本 CR では書いていない** —— 書こうとして図の生成器に止められ、読み直した結果 **`ClipboardGateway → SvgRenderer` それ自体が欠陥である**（組み立てを通らない絵を取っている）ことが分かった。⭐ **足す先の `ImageExporter` は、組み立てた画面を SVG で返すメンバをまだ公開していない** —— そのメンバを足すのが CR-196 なので、**同じ 3 本の辺を 2 回触らないよう CR-196 へ回した**。**`tables` 126・`figures` 11・`uids` 145 は不変、`rows` は 1562 → 1563**（`S-134` の 1 行）。`_source/components.json` は 1 バイトも動いていない |
| 0.73 | 2026-08-21 | ⭐ **呼びようの無かった公開エントリを 3 つ開け、絵の出どころを 1 か所にした**（CR-196。利用者の裁定 D5a / D9 / D5b / D8）—— ⭐ **`PI-8` に `replaceDocument` を置いた** —— ⛔ **取り消し・やり直し・取り込み・自動保存からの復帰・`OP-3` の置き換え・起動時の文書の 6 つが、計算した文書を現在値にする手段を持っていなかった**（`src/` が 4 か所で STOP として記録していた）。語は発明せず、表 T-067 の `WS-6`（差し替える）と `OP-3`（置き換える）から採った。⭐ **`PI-5` に `xFromDay` を置いた** —— 「時間軸の対応」と書きながら `x → 日` の片道しか無く、逆向きは 2 つのユニットに私有の写しとして分かれていた。⚠️ **裁定の字面は `xOfDate` であったが採らなかった** —— `1.9`（`:320`）が「`Of` は比率に限る、変換と導出は `From`」を MUST としており、**表 T-064 は導出に `From` を 14 回使っている**（数えた）。⭐ **`PI-21` に `exportSvg` を置いた** —— ⛔ **`WY-2` が「同じ SVG / PNG になること」を判定にしているのに、SVG の経路だけが 表 T-076 の組み立てと `TaskGroup` の切り落としを通っていなかった**。`AM-13` の行は 1 文字も変えていない —— **壊れていたのは行ではなく、それを作れる口が無かったことである**。⭐ **`_source/components.json` は辺を 1 本も増やさず、`ClipboardGateway` の絵の行き先を `SvgRenderer` から `ImageExporter` へ替えた**（89 のまま）—— ⛔ **その辺自体が欠陥であり、足すべき辺ではなかった**。`AgentApiEndpoint → SvgRenderer` は型だけの依存になったので札を `picture arguments` へ直した。⚠️ **図 F-016 に `SvgRenderer` を 1 節点足した**（16 → 17）—— 同図の題は「ファイル・保管庫・クリップボード・**画像**」であるのに、**絵の中身がどこから来るのかを描いていなかった**（`ImageExporter → ScreenRenderer` は既に描いていた）。**`tables` 126・`figures` 11・`rows` 1563・`uids` 145 は 1 つも動かない** —— 3 つとも既存の行のセルの編集である。⛔ **差し替えた文書が持つ版数は未決のままである**（分類 `D`。推奨で走らない）|
| 0.74 | 2026-08-21 | ⭐ **何をもって同じ文書とするかを定め、「確認を増やすな」の数え違いを直した**（CR-197。利用者の裁定 D6）—— ⛔ **仕様書は「同じ文書」という語を 3 か所で判定に使い、1 度も定義していなかった**（`FR-061` の取り違えの MUST NOT・表 T-034 の「別の文書なら触らない」・`S-99b` の「文書の識別子」）。⭐ **表 T-227 を新設し、ファイル名 ∧ `Project.name` ∧ `Project.id` の 3 つ組と定めた** —— **新しい識別子の列を作らない**（作れば既存の文書にそれが無い）。⚠️ **どちらかの側が `null` のときと、相手を `GRS JSON` として読めないときは「同じとみなさない」に倒した** —— **黙って上書きした結果は取り戻せず、余分に問うた結果は 1 回の手間である**。⭐ **`FR-060`（開いたファイルへの上書き保存）では問わない** —— 開いたファイルは定義によりこの文書のファイルである。⛔ **あわせて `FR-031` の例外の文を、列挙から階級へ替えた** —— 同文は「例外は `FR-032` と `FR-099` の削除だけ」と数えていたが、**表 T-024a の `OP-4` と `FR-095` は削除ではない確認を今日すでに行っている。** ⚠️ **同じ文は版 0.70 で 1 度数え直されており、これで 2 度目である** —— ⭐ **3 度目を数えるのではなく、「取り消しで取り戻せないものを失う場面」という階級にし、場面の列挙を MUST NOT で禁じた**。⛔ **自動保存の鍵は閉じていない** —— `DI-1` はファイル名を含むが、1 度もファイルへ保存していない文書にも自動保存は掛かるので、`FR-061` に MUST NOT を置いて**鍵自体は未決のまま残した**。**`tables` 126 → 127・`rows` 1563 → 1568、`figures` 11・`uids` 145 は不変** |
| 0.75 | 2026-08-21 | ⭐ **手がぶつかる境界 6 つを閉じ、正規化に置き場を作った**（CR-198。利用者の裁定 D10）—— ⭐ **表 T-023d の締めに 3 つの規則を置いた** —— ① **掴んだ端点を置いた日を稼働日へ寄せてはならない（MUST NOT）**（休日に働くことがある。⚠️ `GR-6` は出力の単位しか言っておらず、`FR-043` の `:1993` が寄せる先例を作っていた）、② **`GR-9` / `GR-17` / `GR-18` を掴んでいるあいだ置く実績を描いて示すこと（MUST）**（確定は `IN-1` が既に持つので書かない）、③ **`GR-1` / `GR-2` の日数はポインタの下の日から 1 日単位に四捨五入し、`FD-6` で切り詰める（MUST）**。⛔ **ドラッグのしきい値は足さない** —— 掴み点は時間軸の上の「ある日」そのものであり、画素の感度が存在しない。⭐ **`GR-9` の「押しは受けない」を「押して状態を巡らせない」へ狭めた** —— ⛔ **消すと `FR-043` の `:1999` が守っているマーカーとの区別が再び開く**。⭐ **`CN-5` を読み書きで分け、`FR-023` に「先頭の BOM は受け入れて捨てる（MUST）」を置いた** —— `BOM` は仕様書に 2 行しか出ず、もう 1 行は書き出し側である。⭐ **表 T-024a に `OP-11` を足した**（一度に 2 つ以上渡されたときは先頭だけ受け入れ、残りを告げる。作法は `NT-5`）—— ⚠️ **`OP-8` と `MG-9a` が覆うのは「取込の最中に次が来た」という時間差の場合だけであった**。⭐ **`FR-026` に「書き込む回数に上限を設けない」を記録した** —— 今日それは沈黙であって決定ではなく、表 T-211 の題が「保存と受け入れの上限」なので探した者が曖昧に行き当たる。⭐ **`SWS-6` を Chapter 6.1 に立てた**（親は `FR-021`）—— ⛔ **同章は 表 T-220 / T-221 / T-222 と `SWS-1` 〜 `SWS-5` を持ち、5 本とも描画の幾何であった** —— `FR-021` の前方参照が空き地を指していた。本文に原文を写さず、公式へのリンクと `docs/reference/w3c/` を指すだけとした（表 T-003 の `CN-7`）。⛔ **半分しか閉じていない** —— **`FR-080`（`:3083`）も同じ Chapter 6.1 へ SVG / PNG の正規化と座標の丸めを委任しており、裁定 D10 はそちらを裁いていない**。⭐ **そちらは未裁定のまま残してある。**⚠️ **Canonical XML が `FR-021` の挙げる 4 つを全部覆うかの突き合わせは済んでいない** —— 原文を未取得であることと、取得後に最初にやることを `docs/reference/w3c/README.md` に書いた。**`tables` 127・`figures` 11 は不変、`rows` 1568 → 1569（`OP-11`）、`uids` 145 → 146（`SWS-6`）** |
| 0.76 | 2026-08-21 | ⭐ **フェード日数を MSPDI へ渡す方式を確定した**（CR-199。利用者の裁定 D7）—— ⛔ **`AT-40` / `AT-41` は `Task/ExtendedAttribute` を交換先と名指すのに、「どの拡張領域か」を言う行がどこにも無く、`mspdi-codec.ts:1247` がその穴を STOP として記録していた**。⭐ **原稿 `_source/mspdi-custom-fields.json` を新設し、手書きの契約と生成器を添えて `src/adapter/document-codec/` へ運んだ** —— **`Number` 型（`CFType` 5）の枠を 2 本、`ElemType` 20（Task）。** ⛔ **番号は仕様書にもコードにも書かない** —— 規則 03 の 1.「名前を写すのではなく、値を生成する」に従う。⚠️ **番号は `GRS` の決定ではなく `PjCustomField` 列挙からの引用であることと、手元の正典にはこの 2 つが無いことを、原稿自身に名乗らせた**（検査 21）—— ⭐ **検算が効くのは `pjCustomTaskText1 = 188743731` の 1 件であり、同じ外部の表が手元の `fieldid-element.md:54` と 1 桁も違わず一致する。**⭐ **`EX-6` を書き替え、`EX-8` を足した** —— 空き枠は名簿の順に探し、**取込元が使っているかどうかは定義の `Alias` で見分ける**（値の側は `FieldID` しか持たない。`mspdi_pj12.xsd:2254`）。定義は `Project/ExtendedAttributes` へ 1 度だけ書き、**フェードを持つタスクが無ければまとまりごと書かない**（公式スキーマが空を禁じている。同 `:988`）。⭐ **`PR-14` の MSPDI の欄を `AT-40` / `AT-41` と揃えた** —— ⛔ **「無い」と書かれており、隣の `PR-3` が `Task/Start` と書く欄なので「書き出さない」とも読めた**。⚠️ **`Alias` の語は空のままである** —— 相手のツールの列見出しに出、**`GRS` が自分の枠を見分ける鍵でもある**ので、埋まるまでは枠を主張しない。⛔ **画面に刷る語ではないので訳さない。****`tables` 127・`figures` 11・`uids` 146 は不変、`rows` 1569 → 1570（`EX-8`）。生成物を名乗るファイルは 12 → 13** |
| 0.77 | 2026-08-22 | ⭐ **目盛が借りていた 2 つの隙間を、独立した設定値として立てた**（CR-200。利用者の裁定）—— ⛔ **目盛の間引き（表 T-221 の `LF-1`）が、形状のラベル用の `labelGap`（`S-32`）を借りていた** —— 表 T-104 の `K-32` はそれを「形状の外へ出すときの隙間」と定めており、**バーのラベル間隙を触ると目盛の密度が黙って変わっていた**。⚠️ **`LF-1` の算式はその隙間を 1 文字も持たず**、算式と実装が食い違っていた（試験 `SWS-1` が赤かった）。⭐ **`S-135`（`rulerLabelGap`。目盛ラベルどうしの隙間・横・8px 🔎）と `S-136`（`rulerLabelPad`。罫線と目盛ラベルの余白・縦・2px 🔎）をタイムルーラー群へ立て、`K-112` / `K-113` で名を与えた。** ⚠️ **既定値はどちらもいまの描画を 1px も変えない値であり、由来が無いので 🔎 を付けた。** ⭐ **同じ「6」が 3 か所にあった** —— `S-2` の既定値と下限、そして **`S-3` の上限 `(rulerHeight − 6) ÷ 3`** であり、**3 か所とも `rulerLabelPad` × 3 へ書き直した**（片方だけ直すと下限と上限が食い違う）。⛔ **4 か所目はコードの打ち直しであった** —— `edit-document-settings.ts` が `rulerFont * 3 + 6` と書き、**そのコメント自身が「生成される担い手に式の行を与えよ」と変更要求を求めていた**。⭐ **原稿のセルに `plusFrom` / `plusTimes` を許し、生成器に `SETTINGS_DERIVED`（答えではなく式そのもの）を刷らせた** —— `SETTINGS_DEFAULTS` は指し先も既定値のまま解いた答えしか持たず、`FR-039` が `S-2` に `S-3` への追随を求める以上、解き直せる形が要った。⚠️ **表 T-064 の `PI-2` が、既に別コンポーネントが取り込んでいる定数を 1 つも名乗っていなかった**ので、3 つとも名乗らせた（`PI-2` を指す要求は 0 件・参照は 0 箇所であることを `impact.py` で測った）。**`tables` 127・`figures` 11 は不変、`rows` 1570 → 1574、`uids` 146 は不変** |
| 0.78 | 2026-08-22 | ⭐ **規格だけでは揃わない 2 つを、規格の上に足した**（CR-201）—— ⛔ **`SWS-6` は「W3C の Canonical XML に従う」と書いてあったが、それが `FR-021` を満たすかを誰も確かめていなかった**（CR-198 の第 7 節が開けたまま閉じた件）。⭐ **原文を取得して 4 つと突き合わせた結果、覆うのは 2 つだけであった** —— **①属性の並びと③自己終了タグは覆い、②要素の間の空白は「保存する」（2.1 節・3.2 節）、④名前空間の接頭辞は「入力に現れたものと同じでなければならない」（2.3 節。4.4 節が丸ごとその理由）**。⭐ **表 T-228 を Chapter 6.1 に新設し、規格に足す分だけを `NR-1` 〜 `NR-4` に置いた** —— ⛔ **規格が既に揃える①③は書いていない**（写すと W3C の改訂で仕様書だけが古くなる）。⭐ **`NR-2` が空白を落としてよい根拠は交換相手の型定義にある** —— **混在内容を持つ型が 0 件である**。⭐ **`NR-3` が接頭辞を合否から外してよい根拠も同じところにある** —— **規格が接頭辞を守るのは内容の中の `XPath` が参照しうるためであり、交換相手は `xsd:QName` / `ID` / `IDREF` / `NOTATION` / `ENTITY` を 1 つも持たない**。⭐ **版を 1.1 と明記した** —— ⚠️ **短縮名 `xml-c14n` はいま 1.0 ではなく 1.1 を返すので、`SWS-6` は書いた者の意図と関係なく既に 1.1 を指していた**。⛔ **揺れは 4 つではなく 5 つあった** —— **型付きの値の綴り**（`1` と `true`、`PT40H0M0S` と `PT40H`）を `FR-021` の列挙に足した。**規格は 1.3 節でこれを範囲外と明言しており、文書は交換相手の値を型付きで持っている**（`Own` 43 列 ＋ `Consume` 19 列）。⚠️ **`xsd:duration` だけは閉じられなかった** —— `XML Schema Part 2` がこの型に標準字句表現を定めておらず、交換相手は 35 か所で使う（PD-163。分類 `D`）。**突き合わせの記録は `docs/review/c14n-vs-fr021-2026-08-22.md` が持つ。** **`tables` 127 → 128（`T-228`）、`rows` 1570 → 1574（`NR-1` 〜 `NR-4`）、`figures` 11・`uids` 146 は不変** |
| 0.79 | 2026-08-22 | ⭐ **`WY-2` が PNG について何と何を比べるのかを書き、基準環境に描画エンジンを入れた**（CR-202。利用者の裁定）—— ⛔ **`WY-2` は「正規化後に同じ SVG / PNG になること」としか書いておらず、PNG について「同じ」の中身が無かった** —— **PNG は XML ではないので Chapter 6.1 の正規化が当たらず、バイト列は同じ画素からでも符号器が違えば違う**（圧縮の段・フィルタの選び方・付随する記録の有無）。⭐ **表 T-041 の締めに「復号したあとの画素の並びで判定すること（MUST）。ファイルのバイト列で判定してはならない（MUST NOT）」を置いた。** ⭐ **`FR-080` の「書き出しの基準環境」の定義に `MC-5`（基準ブラウザ）を足した** —— ⚠️ **画素で比べる判定は描画エンジンに依るのに、定義は `MC-6`（画面環境）しか拾っておらず、`MC-5` はどの要求からも名指されていなかった**（`impact.py` で 0 件）。⭐ **表 T-025 の締めに「基準ブラウザの版を、測った値と対にして記録すること（MUST）」を置いた** —— **版が変わって落ちたときに、退行なのか更新なのかを切り分ける手掛かりが要る**。⛔ **特定の版を仕様に書かない** —— 書けば更新のたびに仕様書を直すことになる。⭐ **裁定待ちの PD-161 を 2 つに割った** —— **書き出す座標の桁を PD-162（分類 `A`。推奨 0.01 px）へ割り出し、PD-161 には座標の丸めの許容差と SVG の正規化だけを残した**（分類 `G`）。⚠️ **1 行が `G` であるあいだ波 W4 は閉じられない**ので、安いほうを割り出した。⛔ **値は 1 つも動かしていない** —— `image-exporter.ts` と `svg-renderer.ts` の丸めに `@provisional PD-162` の印を置いただけである（**2 か所に置くのは、片方だけだともう片方が決まった値に見えるからである**）。**`tables` 128・`figures` 11・`rows` 1578・`uids` 146 はいずれも不変** |
| 0.80 | 2026-08-22 | ⭐ **誰も綴りを決めていなかった「時間の量」に、書き方と比べ方を与えた**（CR-203。利用者の裁定。PD-163 を案 A ＋ 案 D で閉じた）—— ⛔ **`EX-4` は「取り込んだ値の書式を踏襲すること」と定めるが、`AT-35`（`Task.actualDuration`）は出自が `Consume` であり、元の文字列を持たない** —— **踏襲する相手が存在しない列が 1 つあった。** ⭐ **日付ではまったく同じことが起き、版 0.44 が `EX-7` を新設して閉じている** —— 時間の量にはその対が無かった。⭐ **表 T-033 に `EX-9` を足した** ——「`GRS` が書く時間の量は `PTnHnMnS` の形とし、時・分・秒の 3 つを必ず置くこと（MUST）。年と月を書いてはならない（MUST NOT）」。⚠️ **取り込んだまま編集していない値には掛からない**（`EX-2` と `EX-4` が元の文字列のままを求める）。⭐ **綴りが要るのは `Consume` の列だけである** —— **交換相手は `xsd:duration` を 13 の要素名・35 か所で使うが、本ソフトウェアが解釈するのは `AT-35` ただ 1 列であり、残りは `carryElements` へ文字列のまま入る**（表 T-053 の `DF-2`）。⭐ **表 T-228 に `NR-5` を足した** —— **`xsd:duration` は字句ではなく時間の量として比べる。** ⛔ **年と月を持つ値は量として読まない**（長さが数え始めに依る。取込側は既にそう振る舞っている）。⚠️ **`NR-4` に相乗りさせず別の段取りにしたのは、`NR-4` が「字句を揃える」でありこちらは「字句を揃えずに量で比べる」だからである** —— **1 行に 2 つの規則を持たせない**。⛔ **この型に標準字句表現が無いことは `XML Schema Part 2` の 3.2.6 をローカル複製で確かめた**（字句表現・順序関係・ファセット比較の 3 節しか無い）。⛔ **元の綴りを列として覚える案は採らなかった** —— 版 0.44 が同型の案を「解釈した値と元の文字列を両方 文書に持つ」として退けている。⛔ **`src/` は 1 行も書いていない** —— `durationOfMinutes` が既に `PTnHnMnS` を書いており、本 CR は実装だけが持っていた規則を仕様へ移した。⚠️ **隣にあった未申告の決定を PD-164 として起こした** —— **`Project.minutesPerDay` で割り切れない長さの丸め**は、実装が「読まずに知らせる」を選んだまま裁定待ちの一覧に 1 行も無かった（分類 `D`）。**`tables` 128・`figures` 11・`uids` 146 は不変、`rows` 1578 → 1580（`EX-9` と `NR-5`）** |
| 0.81 | 2026-08-22 | ⭐ **交換相手の期間を稼働日へ換算する向きの規則を `FR-054` に置いた**（CR-204。利用者の裁定。PD-164 を案 ④ 四捨五入で閉じた）—— ⛔ **`FR-054` は「稼働日 → 期間」の向きだけを持ち、取り込みが毎回走る「期間 → 稼働日」の向きを 1 文も持っていなかった**（`R1.4` の違反）。⛔ **実装は割り切れない長さを読み捨てて `actualDuration` を `null` にしており、書き出し側は `null` の `ActualDuration` を 1 要素も書かない** —— **`ActualDuration` は `TASK_CONSUMED` にあり `carryElements` にも残らないので、交換相手が書いた `PT41H` が往復で跡形もなく消えていた。** ⭐ **「1 日単位に四捨五入すること（MUST）。ちょうど半日のときは絶対値の大きいほうへ寄せること（MUST）。割り切れないことを理由に値を捨ててはならない（MUST NOT）」を置いた。** ⚠️ **寄せ先を「大きいほう」ではなく「絶対値の大きいほう」と書いたのは、負の長さで語と規則が食い違わないようにするためである** —— `-5.5` は「大きいほう」なら `-5`、四捨五入なら `-6` であり、**符号に依らない書き方なら 1 つの規則で済む**（負の長さが実際に来るかは未確認であり、CR-203 が開いたまま残している）。⭐ **年と月を持つ長さを換算しないことも同じ文に置いた** —— ⛔ **これは `minutesOfDuration` だけが持っていた規則であり、比べる側の `NR-5`（表 T-228）は既に同じことを言っていた。片側だけ書かれている状態のほうが危ない。** ⭐ **四捨五入は無損失ではないので、丸めた `Task` の件数を知らせることを MUST とした**（知らせ方は `FR-076` の `NT-1`。件数で知らせる形は `FR-011` と `FR-088` が既に採っている。⛔ `Task` ごとに 1 件ずつ知らせる形は、1000 `Task` の文書で知らせが 1000 件出るので採らない）。⚠️ **振る舞いを変えても、いまの試験は 1 件も落ちない** —— `tests/` を全数検索したところ `actualDuration` は固定データの組み立てにしか現れず、**割り切れない長さを取り込ませる試験が 1 件も無い**（`R6.2` の違反）。⛔ **本 CR は試験を書いていない** —— 書いた者にその試験を書かせない（規則 04 の 1.）。要るのは 3 件である（`PT41H` が 5 日になること、半日の寄せ先、知らせが出ること）。⛔ **丸めた値は往復で元に戻らない** —— 四捨五入は「消える」より良いが `FR-021` の無損失を満たすわけではなく、そこは開いたままである。**`tables` 128・`figures` 11・`rows` 1580・`uids` 146 はいずれも不変** —— 行を足さず、`STATEMENT` に文を足した |
| 0.82 | 2026-08-22 | ⭐ **刻印から「順序」という判定を全部取り除いた**（CR-205。利用者の裁定 1 〜 11）—— ⛔ **`revision`（1 ずつ増える整数）1 本が 4 つの判定に刺さっていた** —— `AG-2` の照合（等値）・`AG-6` の選別（順序）・起動時の比較（順序）・`changeLog` の主キー（一意性）。⛔ **そのうち順序の 2 つが本物の欠陥だった** —— **取り消しは以前の文書を刻印ごと復元するので、戻った版数は現在より小さく、`t2` を受け取り済みの監視者に通知が出ない。`AG-6` の「まだ受け取っていない変更**だけ**を通知する」の違反である。** ⚠️ **起動時の比較は壁時計で順序を決めており、`NTP` 補正や夏時間で逆行しうる。** ⭐ **`documentStamp` を「日程データの群が動いた刻」「どちらの群であれ動いた刻」「最後に書いた者」の 3 値とし、4 つの判定をすべて等値へ移した** —— `AG-2` は 3 値の等値、`AG-6` は `WS-5` が既に下している「日程データの群が動いたか」の判定と、購読し直しのときの刻の等値、起動時も 3 値の等値、`changeLog` の鍵は `ordinal`。⭐ **`ordinal` は新しい概念ではない** —— `erd.json` を全数で測ったところ `WeekDay` / `Exception` / `CarryElement` が既に同じ作法の主キーを持っており、**`changeLog` だけが自前の鍵を持たず刻印の値を借りていた。** ⛔ **同刻衝突は後勝ちとした** —— 先勝ちは「同じ刻の 2 回目」を見分ける弁別子を要し、その弁別子こそ本 CR が取り除いた順序である。根拠は `FR-063` の「**最後に**書いた者」と `WS-6` の 1 参照置換。⚠️ **`erd.json` の列は 1 つも増減させず、その場で書き換えた** —— ⛔ **`erd_json_to_md.py` が `AT-` を位置で連番しているので、列を 1 つ足すと以降が全部ずれ、規則 02 の「番号を振り直さない」と正面衝突する。** ⭐ **文書の外に持つ値は 0 で、表 T-064 / T-107 は 1 行も動かない**（`ConfirmedChange` は `src/` の型であり、CR-146 が署名の持ち主を `src/` と定める）。⚠️ **「版数」の語が仕様の他所に 18 か所残っていた** —— `LM-15`・`DR-5`・`FR-024`・`FR-063` の見出しと RATIONALE・`AG-9a`・`CP-8`・`CP-33`・`PI-33`・`WS-5`・`FT-5`・`PI-3` の `isNewerStamp` など。**全部を刻へ書き直した。** ⭐ **`AG-1` と `FR-064` の「版数」だけは残した** —— あちらは `Agent API` 自身の版であって刻印ではない。**`tables` 128・`figures` 11・`rows` 1580・`uids` 146・`columns` 138 はいずれも不変** |
| 0.83 | 2026-08-22 | ⭐ **基準日線が立つ「本日」を、読む人の機のローカルの暦の日と定めた**（CR-206。利用者の裁定。PD-166）—— ⛔ **`FR-046` は「その時点の本日」としか書いておらず、どの暦の日かを述べていなかった。** ⚠️ **`InputContext.today` を繋ぐ側が黙って選べる状態であり、UTC を採ると東京の午前 9 時より前に押した線が前日に立つ。** ⭐ **足したのは `FR-046` の `STATEMENT` に 1 文だけである** —— **`statusDate` の列（`AT-13`）も `CU-1` も `WY-2` も 1 文字も動かない。** `tables` 128・`figures` 11・`rows` 1580・`uids` 146 はいずれも不変 |
| 0.84 | 2026-08-22 | ⭐ **書き手が名乗る語の全数を 表 T-229 に置いた**（CR-207。利用者の裁定。PD-165）—— ⛔ **`FR-063` は「最後に書いた者」を持たせよと言うだけで、何を書くかを述べておらず、`AT-128` の意味欄の「人か、AI か」は出荷している起動テンプレートの `"template"` をその場で説明できていなかった。** ⭐ **`ED-1` = `user`（画面）／ `ED-2` = 呼び手が申告した名 ／ `ED-3` = `template`。** ⛔ **画面と起動テンプレートの 2 語を予約し、呼び手が名乗ることを禁じた** —— `AG-6` は等値だけで「自分以外か」を判定するためである。⚠️ **呼び手どうしの重複は閉じていない。`LM-16` として制限事項に書いた。** ⭐ **`AG-2` / `AG-6` / `AG-11` / `WS-5` は 1 文字も動かない** —— 判定はいまも等値であり、変わるのは比べる文字列の中身だけである。`tables` 128 → **129**・`rows` 1580 → **1584**・`figures` 11・`uids` 146・`columns` 138 |
| 0.85 | 2026-08-22 | ⭐ **`GR-5` / `GR-6` / `GR-13` / `GR-16` が持っていなかった掴み代を 表 T-206 に置いた**（CR-208。利用者の裁定。PD-167 / PD-168）—— ⛔ **`S-91`（実績の端点）の既定は「実績バーの帯」という散文だけで、帯は縦の広がりである。端点は点なので、横の許容が無いと `GR-5` / `GR-6` は決して当たらず、実績を動かす経路が丸ごと塞がっていた。** ⛔ **`GR-13` / `GR-16` の「線の上」は幅を持たない集合であり、行そのものが無かった。** ⭐ **`S-91` に数を与え、`S-137`（線の掴み代）を新設し、`S-90` が横にも掛かることを同じ 1 つの数のまま述べた** —— `S-49` の下限は既にそう読んでいた。⚠️ **3 つとも 6px だが由来は無く、🔎 を付けた。** ⭐ **表 T-023d は 1 行も動かない** —— あの表の前文は既に値を 表 T-206 へ委ねており、足りなかったのは委ねられた側である。`tables` 129・`rows` 1584 → **1585**・`figures` 11・`uids` 146 |
| 0.86 | 2026-08-22 | ⭐ **まるごと差し替える道の呼び手 6 つを 表 T-230 に置いた**（CR-209。利用者の裁定）—— ⛔ **`replaceDocument`（`PI-8`）は CR-196 で名前だけが開き、呼び手ごとに履歴・刻印・取り消しの 1 段をどう扱うかが 1 つも書かれていなかった。** ⭐ **`UndoEdit` は呼ばれる側である** —— コンポーネントの図（図 F-014）を正とし、`ApplyDocumentChange` が `ImportDocument` / `UndoEdit` / `RedoEdit` へ問う形に確定させた。⛔ **`_source/components.json` の辺は 1 本も動かない。** ⭐ **`WS-1` は呼び手が申告した刻印と現在の文書を照合し、入ってくる文書の刻印とは照合しない。** ⭐ **`WS-3` の位置に立つのは 表 T-230 が名指すもので、この道では入ってくる文書を検証し直さない**（`OP-5` と `FR-023` が既に負う）。⭐ **刻印は `RD-3`（取り込み）だけが進み、残る 5 つは入ってきたまま**（`FR-063` の「取り消しは刻印ごと復元する」と `FR-062` の照合が根拠）。⚠️ **`RD-3` を例外としたのは利用者の裁定である** —— 合流の結果は現在の文書から組まれるので、進めないと `FR-063` と `AG-2` と `AG-6` の 3 つが同時に破れる。⭐ **`WS-7` へ渡す「日程が動いたか」は前後の `scheduleUpdatedUtc` の等値で導く**（`AG-6` の括弧に足した）。⭐ **`A-1` の「段」の裸書きを 2 か所直した**（`PI-8` と `NF-2`）。⛔ **表 T-027 と 表 T-078 は 1 行も動かない。** `tables` 129 → **130**・`rows` 1585 → **1591**・`figures` 11・`uids` 146 |
| 0.87 | 2026-08-23 | ⭐ **人の応答を待つファイル操作に一貫性の単位を与えた**（CR-210。利用者の裁定）—— ⛔ **`docs/spec` は非同期・`Promise`・`await`・待機のどれも 1 度も書いておらず、待っているあいだの規律がどこにも無かった**（自分で数えた。出現 0 回）。⭐ **表 T-066 に `CS-4`（人の応答を待つ 1 回のファイル操作）を足した** —— 始める前に要るものを全部集め、待つあいだ現在値を読み直さず、着地は `replaceDocument` で行う。⚠️ **`CS-4` だけは入れ子ではない** —— 待っているあいだに `CS-1` が何度も走るので、「3 つは入れ子である」を `CS-1` 〜 `CS-3` に限った。⭐ **待つことそのものは画面を何も変えない** —— 表 T-078 に契機が無い。**待っていることを示す表示は出さない**（出すなら 表 T-037 に行が要る）。⛔ **凍らせるのではない** —— 寸法が変われば `FT-3` が走る（`NFR-011`）。⭐ **`CA-3`（許容する陳腐化）を同じ回で直した** —— **挙げていた 2 つの拒否（`AG-9` と `WS-2`）はどちらもファイル選択が開いているあいだ何も拒否しない**ので、`R2.20` の MUST に対して偽であった。⛔ **表 T-078 には 1 行も足さない** —— 着地は `WS-6` であり `FT-2` が既に覆う。**足すと同じ出来事に 2 つの名前が付く。** ⚠️ **起動の注の「3 単位」を「4 単位」に直した。** `tables` 130・`rows` 1591 → **1592**・`figures` 11・`uids` 146 |
| 0.88 | 2026-08-23 | ⭐ **開く道の 3 つの穴を閉じた**（CR-211。利用者の裁定）—— ⛔ **`OP-1` は 2 つの形式を受け付けると書きながら、どちらかを決める者がどの表にも無かった。** ⭐ **表 T-024a に `OP-12` を置き、拡張子と先頭の非空白 1 文字の両方が 表 T-024 の同じ行に合致したときだけ読む形にした**（どちらか一方でも違えば読まない、MUST NOT）。⛔ **先頭の 1 文字を見る前に BOM を捨てる（MUST）** —— 順序を違えると `FR-023` の「BOM を理由に拒んではならない」がそのまま破れる。⭐ **値は 表 T-024 が持つ** —— `IO-1` は `.xml` と `<`、`IO-2` は `.json` と `{`。⛔ **書出だけの形式には拡張子を与えていない**（`suggestedFileName` の持ち主がまだ無い）。⭐ **`SK-11`（`Ctrl+S`）はどの形式から開いた文書でも `GRS JSON` で書くと `FR-096` に定めた** —— 同要求の RATIONALE が「ここで決まる」と自ら名乗っており、`FR-021` の往復無損失は「編集せずに書き出したとき」に限られている。⭐ **確認の 2 択に入口を与えた** —— 表 T-103 に `U-55`（`Confirmation`）、表 T-109 に `IC-69` / `IC-70`、図 F-019 に図形 2 つ。**答えは既存の入口の道（`ScreenPart.entry`）で返る** —— 表 T-065 の MUST が道を 1 本に定めているためである。⚠️ **表 T-109 は 68 → 70 行、図 F-019 は 68 → 70 図形、`viewBox` は 452 × 284 のまま**（最終行に空きが 4 つあった）。`tables` 130・`rows` 1592 → **1596**・`figures` 11・`uids` 146 |
| 0.89 | 2026-08-23 | ⭐ **「その段の保存形」が何かを明示した**（CR-212。利用者の裁定）—— ⛔ **`FR-031` の文言と実装が 36% 違う形を測っていた**（起動テンプレートで実測 —— 詰めた形 655,969 バイト、字下げのある形 895,286 バイト）。⭐ **仕様の側を実装に合わせ、「詰めた `GRS JSON`（字下げも改行も持たない形）」と書いた** —— ⛔ **コードは 1 行も動かしていない。** ⚠️ **「保存形」は仕様のどこにも定義が無く、同じ語を持つ行は 表 T-024 の `IO-5` だけで、そこが指すのは字下げのある形である。** ⭐ **根拠は CR-182 のコミットで、この文と実装の 1 行を同時に書き、直したのは「文字数 → バイト数」であって直列化には触れていない。** ⛔ **`S-95` の 🔎 は外さない** —— 測る形を緩いほうに固定したので、上限そのものは選び直せる状態に保つ。⛔ **層の壁により「保存する形で測る」は採れない** —— `stepSize` は `UseCase`、`jsonFromDocument` は `Adapter` であり、`LR-1` が内向きの依存だけを許す。`tables` 130・`rows` 1596・`figures` 11・`uids` 146 は 1 つも動かない |
| 0.90 | 2026-08-23 | ⭐ **形式を名指すメンバを開けた**（CR-213）—— ⛔ **CR-211 が `OP-12` で「どちらの形式として読むか」の規則を置いたのに、それを答えるメンバが 表 T-064 のどこにも無かった。** ⭐ **`PI-20`（`DocumentCodec`）に `formatFromFile` を足した。** ⛔ **コンポーネントの図の辺は 1 本も動かない** —— `FileGateway → DocumentCodec` の辺は既に在り、その札は `format`（「交換の形式を得る」）である。**図が描いていたものを表が名乗っていなかった。** ⚠️ **`Of` ではなく `From` を採った**（1.9。導出は `From`）。`tables` 130・`rows` 1596・`figures` 11・`uids` 146 は 1 つも動かない |
| 0.91 | 2026-08-23 | ⭐ **判別が要る 2 つの値を、散文から列へ移した**（CR-214）—— ⛔ **`OP-12` は書けたのに実装できなかった。** 規則 03 が「仕様が持つ値は写さずに生成する」と定めるのに、**拡張子と先頭の非空白 1 文字が備考の文の中に在り、機械が行 ID と列で繋げなかった。** ⚠️ **`docs/spec` を読む生成器は 7 本あり、7 本とも行 ID と列で繋いでいる。散文を解析するものは 1 つも無い。** ⭐ **表 T-024 に「拡張子」と「先頭の非空白 1 文字」の 2 列を足し、`IO-1` は `.xml` / `<`、`IO-2` は `.json` / `{` とした。** ⛔ **書出だけの 5 行は欄を持たない** —— 判別されるのは `OP-1` が取込で受け付ける 2 行だけであり、`IO-7` の埋め込み文書を読むのは 表 T-034 の `BT-1` である。⭐ **重複検出の群が 1 つ減った**（`A=28` → **`A=27`**）—— CR-211 が備考へ同じ形の文を 2 行並べたのが原因であり、列にすれば表の形であって重複ではない。`tables` 130・`rows` 1596・`figures` 11・`uids` 146 は 1 つも動かない |
| 0.92 | 2026-08-23 | ⭐ **2 枚の絵が「同じ」だと言える規則を置いた**（CR-215。利用者の裁定）—— ⛔ **`FR-080` は「正規化と丸めの規則そのものは Chapter 6.1 が持つ」と委任したのに、委任先にその規則が無かった。** ⚠️ **`WY-3` の合否がそこに乗っており、この 1 件が在るかぎり波 W4 を閉じられなかった**（規則 06 の 5.）。⭐ **表 T-231 を新設し、`NS-1` 〜 `NS-6` を置いた** —— 規格（W3C Canonical XML 1.1）を当て、要素の間の空白を落とし、数の綴りを 0.01 px の格子へ揃え、`id` を定数に限る。⭐ **`WY-2` の「同じ」の相手を「同じ走りの中で 2 回書き出したもの同士」と定めた** —— ⛔ **CR-202 が自分で「開いたまま」と挙げていた 3 件目である。** ⚠️ **版元に置いた見本と比べる案は退けた** —— `WY-2` が測るのは書き出しの経路が決定的かどうかであり、機と版をまたぐ浮動小数の再現性はその射程に無い。⭐ **`WY-3` の許容差を「双方を同じ格子へ丸めたうえで、外接矩形の各辺の差の絶対値が 1.0 px 未満」と定めた** —— **CR-148 が「6.1 が丸め式を書いた時点で、この除外が許容差として残るのか不要になるのかを判定し直すこと」と残した指示に答え、残すこととした。** ⛔ **表 T-228 は広げない** —— あちらの親は `FR-021` であり、`NR-4` と `NR-5` は交換相手のスキーマに固有である。⭐ **`SWS-7` を置き、Chapter 6.1 の末尾に残っていた未記入の断り書きを落とした** —— `SWS-1` から `SWS-6` までが入った時点で偽になっていた。⛔ **`FR-080` と 表 T-041 は 1 行も動かさない。** ⚠️ **重複検出は `A=27`（new 0）のまま** —— 兄弟表が群を増やすと見込んだが、増えなかった。`tables` 130 → **131**・`rows` 1596 → **1602**・`uids` 146 → **147**・`figures` 11 は不変 |
| 0.93 | 2026-08-23 | ⭐ **単一ファイルが 1 度も持っていなかった方針を置いた**（CR-216。利用者の裁定）—— ⛔ **表 T-003 の `CN-8` は「単一 HTML に対する内容セキュリティ方針を持つ」と定めるのに、成果物に 1 つも無かった。** ⚠️ **`NFR-009` はその方針を担保として `CN-8` を名指しているので、担保が実質どこにも無かったことになる。** ⭐ **実証した** —— 外部の画像への参照を仕込んだ成果物をファイルとして開くと、要求が本当にネットワークへ出た。方針を入れた版は要求の前に拒む。⭐ **表 T-232 を新設し、`PO-1` 〜 `PO-6` を置いた** —— 土台を閉じ、絵は `data:` だけ、様式は要素に直に載る形だけ、スクリプトは埋め込んだ本文の `sha256` を 1 つだけ。⛔ **`unsafe-inline` をスクリプトに書いてはならない** —— それでは注入されたものも走る。⛔ **`nonce` は採れない** —— サーバーが要り、`NFR-004` はファイルを直接開いた状態での判定を MUST としている。⚠️ **`base-uri` と `form-action` を別に置いた** —— **`default-src` はこの 2 つに及ばない。** 足さないと、注入された基底 URL が相対参照の行き先を変えられる。⭐ **`SWS-8` を置いた**（親は `NFR-009`）。⛔ **表 T-003 は行を足さず、`CN-8` の欄に指し先を 1 つ足すだけにした** —— 指令を書き写すと正が 2 つになる。⚠️ **ハッシュの値そのものは仕様に書かない** —— ビルドのたびに変わる。仕様が持つのは規則である。`tables` 131 → **132**・`rows` 1602 → **1608**・`uids` 147 → **148**・`figures` 11 は不変 |
| 0.94 | 2026-08-23 | ⭐ **1 回の押下が言わなければならない 2 つのことを書いた**（CR-217。利用者の裁定）—— ⛔ **`AG-9` は「パンと範囲選択は文書を変えないので拒否しない」と明文で除外するのに、`IF-7` は「身振りの最中かどうか」という真偽しか運んでいなかった。** ⚠️ **`AG-9a` が拒否の値に「理由の区分」を求める以上、真偽 1 つでは「パンだった」と「本物のドラッグだった」を区別できない。** ⭐ **`IF-7` の欄を「どの身振りの最中か」へ広げ、表 T-023a の行 ID を運ぶことにした** —— **判定順序の正は 表 T-023a のままであり、シェル側で 2 度読ませない。** ⛔ **表 T-064 は動かない** —— `PointerPress` の欄は `src/` の公開エントリが持つ。⭐ **全体表示の押下を 2 つの書き込みに割った** —— `CM-71` は倍率と表示位置だけを置き、新設の `CM-72`（`expandAllTaskGroups`）が畳んだ行をすべて開く。**順序を入れ替えてはならない（MUST NOT）。** ⭐ **`WS-4` が積むのはその書き込みの前の文書なので、2 つ目が積む段は既に新しい倍率を持つ** —— 取り消すと倍率は新しいまま畳みだけが戻り、`UN-17` の ⚠️ 節と「1 段にまとめる」が同時に成り立つ。⚠️ **群を `TaskGroup` にした** —— `isCollapsed` は同エンティティの列であり、見せ方の群へ置くと `FR-063` の刻印の判定が枝で狂う。⛔ **`UN-17` も `UN-8` も 1 文字も動かさない** —— どちらも既に正しく、破れていたのは実装の側である。⚠️ **閉路の予測を外した** —— `FR-055` と `HF-8` の 2 者の閉路が 1 つ在ったが、編集計画がそのどちらにも触れないので 1 つずつ書いた。`tables` 132・`uids` 148・`figures` 11 は不変、`rows` 1608 → **1609** |
| 0.95 | 2026-08-23 | ⭐ **「他の行に表示されている」印の示し方を語と定めた**（CR-218。利用者の裁定）—— ⛔ **`FR-032` は「その旨を示すこと（MUST）」と定めるのに、語なのか図形なのかを言っていなかった。** ⚠️ **値は画面まで届いているのに、示す手段が無いので描かれていなかった。** ⭐ **語とし、図形を起こす道を閉じた** —— 表 T-109 がアイコンの全数であり、**新しい図形の決定は 表 T-026 の `RC-13` が利用者の裁定と定めている。** ⭐ **語の枠は原稿に立てた** —— `confirmationMarks` の群を足し、表示語は 176 → **177** 枠になった。⛔ **仕様の表には 1 行も立てない** —— `FR-038` が「要求にも表にも語そのものを書いてはならない（MUST NOT）」と定めている。⭐ **あわせて古いセルを 2 つ掃いた** —— 表 T-109 の注が「68 個の確定名」のままだった（同じ表の 2 か所は 70 と書いていた。CR-211 の取りこぼし）。**表 T-206 の「注」を引いていた注記**は、その注が存在しないので `S-90` の理由の欄を名指す形へ直した。⚠️ **後者は生成物の側を直しかけて気づき、原稿（`_source/settings.json`）へ入れ直した。** `tables` 132・`rows` 1609・`uids` 148・`figures` 11 はいずれも不変 |
| 0.96 | 2026-08-23 | ⭐ **CR-217 が「動かない」と予測した 1 メンバを当てた**（CR-219）—— ⛔ **予測が外れた。** CR-217 は「表 T-064 は動かない」と書いたが、裁定待ちの PD-169 を実装すると `pressRowOf` を公開せざるを得なかった。⚠️ **裁定が退けたのは「シェルが押下値の隣に第 2 の値を持ち、書き込みの時に訊く」形であって、押下の時にメンバを 1 つ埋めるために呼ぶ形ではない** —— ⭐ **呼び手が値を解決して運ぶ形は `hit` と同じである**（あちらは `PI-7` の `itemAtPointer` を呼ぶ）。⭐ **表 T-064 の `PI-18` に `PressRow`（型）と `pressRowOf` を足した** —— `R2.19` が公開面の宣言を MUST で問うており、**シェルが呼ぶ関数が表に無い状態は宣言が 1 つ欠けている状態である。** ⛔ **過去の CR は書き換えない** —— 2026-08-22 の裁定に従い、新しい CR で確定させた。`tables` 132・`rows` 1609・`uids` 148・`figures` 11 はいずれも不変 |
| 0.97 | 2026-08-23 | ⭐ **実装した体が「仕様に無い」と言って止まった 9 件のうち 8 件に答えた**（CR-221 / CR-222 / CR-223 / CR-224 / CR-225。すべて利用者の裁定）—— ⭐ **`data-role` の記法は、無いのではなく二重に決まっていた** —— 表 T-006a の `W-4` は kebab-case、`W-6` は PascalCase の複合語であり、実装も新しい `tests/system` も `W-6` の形を書いていた。**確定名を kebab へ訳すと同じものに 2 つ目の綴りができる**ので、`W-4` に但し書きを足した。⭐ **待ちをまたいで立った問いを描く契機が無かった** —— 表 T-078 は 5 つに閉じて「本表に無い契機でフレームを起こしてはならない（MUST NOT）」と定め、仕様自身が「待つことそのものは画面を何も変えない —— 表 T-078 に契機が無いからである」と書きながら、**待ちの終わりを詰め忘れていた。** `FT-1` を「その入力の、待ちをまたいだ続きを含む」へ広げた（CR-210 は着地だけを論じていた）。⭐ **`TaskGroup.parentId` の輪をどの網も捕まえていなかった** —— `IV-4` は `wbsParentUid` だけ、`HM-4` は画面の移動だけ、`FR-023` は取り込みの WBS だけである。`IV-18` を立てた。⚠️ **分類 F（受理の境界）なので、利用者の文書が 1 つも存在しないいまが最も安い。** ⭐ **`IC-54` は存在しない打鍵を指していた** —— 正として挙げる `SK-19` は `Enter`（その場の編集の確定）であり、`SK-1` は「キーボードだけで図形を置く経路は持たない」と明文で定める。**差し替える候補が存在しない**ので、行を廃さずに正を 表 T-023b（構え）へ付け替えた。⭐ **`Cmd` は仕様書全体で `MK-2` の 1 か所だけだった** —— `macOS` も `⌘` も 0 件、表 T-036 は 9 か所すべて素の `Ctrl` で綴り、`MK-5` 自身も書いていない。落とした。⭐ **閉じたプロパティ面の幅（`0`）と、選択面が提案するファイル名（文書名 ＋ 表 T-024 の拡張子）を定めた。** ⭐ **表 T-006b が縛るのは本仕様書の散文であり、画面に刷る語は対象外**と明記した。⛔ **CR-220（`OP-3` の 3 択を問う面）は当てられなかった** —— 表 T-103 に `Open Chooser` を立てて 表 T-109 に入口 3 つを置いたところ、**`generate_icon_glyphs.py` が「図 F-019 が図形を描かない行が 3 つある」と拒んだ。** 表 T-109 は全行が図形を持つことを前提に組まれており、**図形の決定は 表 T-026 の `RC-13` が利用者の裁定と定める。** ⛔ **発明せずに差し戻した** —— 3 つの図形の裁定を受けてから当てる。⚠️ **`IV-1` / `IV-2` / `IV-16` を答えさせる原稿の形の変更（`erd.json` の関係に列名、`settings.json` の ε を `exclusive` へ）も別の巡へ回した** —— 生成器と `SETTINGS_BOUNDS` とその読み手を引き連れるからである。⭐ **数の予測は、当てた分について的中した** —— `tables` 132 不変・`figures` 11 不変・`rows` 1609 → 1610（＋1、`IV-18`）・`uids` 148 不変 |
| 0.98 | 2026-08-23 | ⭐ **開き方を問う面を立て、開く経路の閉塞を解いた**（CR-220。利用者の裁定）—— ⛔ **前の巡はここで差し戻していた** —— 表 T-103 に面を、表 T-109 に入口 3 つを足したところ、`generate_icon_glyphs.py` が「図 F-019 が図形を描かない行が 3 つある」と拒み、**表 T-026 の `RC-13` が「本仕様書の中で発明しない —— 図形の決定は利用者の裁定である」と定める**ため、**発明せずに全部戻した。** ⭐ **本巡は図形の裁定を受けてから当てた。** ⭐ **`IC-71`（置き換える）は端を折ったページの対** —— 点線のページから実線のページへ矢印が渡る。⭐ **`IC-72`（合流させる）は上下 2 つの枠から右の枠へ集まる 2 本の矢印。** ⭐ **`IC-73`（重ねる）は `IC-4`（変更前の予定）の対に入りの矢印を 1 本足したもの** —— **意味の差は「入ってくること」だけなので、絵の差もそれだけにした。** ⚠️ **図 F-019 の枠が 1 段伸びた** —— 12 列 × 6 段の 72 席のうち空きは 2 席しかなく、`IC-71` / `IC-72` がそれを埋め、`IC-73` が 7 段目を開いた。`viewBox` の高さは 284 → 328。⚠️ **3 つとも `RC-13` の「実物で見比べて選び直す」一覧に載せた** —— `IC-58` 〜 `IC-70` の 14 個と同じ扱いである。⚠️ **`IC-71` と `IC-72` が重複検出に新しい組を作った** —— 同じ面の姉妹行が同じ言い回しを持つためで、**既に `IC-69` / `IC-70` が同じ形で基準線に載っている。** 並べて載せた（`new 0`）。⭐ **表示語の枠が 177 → 183 になった**（アイコン 3 行 × `label` / `hint`）。⛔ **語は空のままである**（PD-160）。⭐ **数の予測は的中した** —— `tables` 132 不変・`figures` 11 不変・`rows` 1610 → 1614（＋4、`U-56` と `IC-71` 〜 `IC-73`）・`uids` 148 不変。⭐ **下流は既に用意できていた** —— `importDocument`（`PI-10`）が `OP-3` の答えを引数で取り、表 T-230 の `RD-3` / `RD-4` が着地を持つ。**残るのはシェルが面を出す結線だけである** |
| 0.99 | 2026-08-23 | ⭐ **知らせの理由に引ける行を与え、重ねの枠・書出の拡張子・0 バイトの書き出し先を決めた**（CR-228 / CR-229 / CR-230 / CR-231。すべて利用者の裁定）—— ⛔ **表 T-037 の `NT-1`（どの項目がなぜ誤りかを文字で示す MUST）と `NT-3a`（次に取れる手段を添える MUST）が、実装のどこでも果たせていなかった** —— 知らせは理由の鍵を運ぶのに、`FR-038` の辞書に理由で引ける節が 1 つも無かった。⭐ **表 T-233 を新設し、人へ届く理由の全数を行 ID で持った** —— ファイル操作の失敗 5・書き込みが通らなかった 5・形式の食い違い 3・受け付けたうえでの注意 1、それに行の無い理由の落ち先 1 である。⚠️ **引継書は「11 鍵」と書いていたが、公開されている型から数え直すと 14 であった。**⛔ **`cancelled` は入れない** —— 人が選択をやめただけで失敗ではなく、取り込みの検証の拒否も入れない（Chapter 6.1 の 表 T-220 の行が既に行 ID を持ち、同じものに 2 つ目の鍵を作ることになる）。⭐ **重ねの枠に何を入れるかを 2 本の MUST が別々に答えていた** —— 表 T-024a の `OP-9` は「読んだものを入れろ」と言い、`FR-015` は「持つのは重ねて描くのに要るものだけ」と言う。`UID` が 1 つも一致しないときに逆を指していたので、**入れるのは一致するものだけと `OP-9` の側へ書いた** —— `FR-015` は 1 文字も動かしていない。⭐ **`FR-096` の「提案する名は文書名 ＋ 表 T-024 が定める拡張子（MUST）」が、書出 6 行のうち 4 行で解けなかった** —— 表 T-024 の拡張子の欄が `IO-1` と `IO-2` にしか無く、前文は「書出のときに添える名前は本表が持たない」と逐語で食い違っていた。`IO-3` を `.svg`、`IO-4` を `.png`、`IO-7` を `.html` とし、`IO-6` は「ファイルとして出ないので持たない」と書いた。⭐ **0 バイトの書き出し先を `DI-6` として立てた** —— 選択面が作ったばかりのファイルと元から空のファイルは見分けられず、**失うものが無い以上、問うことのほうが `FR-031` の MUST NOT に当たる。**⚠️ **数の予測**: `tables` 132 → **133**・`rows` 1614 → **1630**・`figures` 11 不変・`uids` 148 不変 |
| 1.00 | 2026-08-23 | ⭐ **名の無い UI パーツに確定名を与え、2 通りに書かれていた 1 つの語を統一した**（CR-233 / CR-234。いずれも利用者の裁定）—— ⚠️ **本行の番号は文書の版であって、製品の版ではない** —— 裁定待ちはまだ 57 件開いている。⭐ **実 DOM を見て見つけた** —— `data-role` の 18 種のうち 1 つだけが小文字の作り名で、表 T-103 が通知の立つ場所に名を与えていなかった。`W-4`（MUST）は「表 T-103 の名をそのまま書く」と定めるのに、**書く名が存在しなかった。**`U-57` `Notification Area` を立てた。⛔ **「面」ではないと明記している** —— `S-99g` の「面」は `Esc` の第 1 階層で閉じるものであり、通知は答えを求めない。⭐ **値の型 `Notice` と公開メンバ名は動かしていない** —— `W-4` が縛るのは `data-role`、`W-6` が縛るのは散文であり、**公開メンバ名を縛る行はどこにも無い。**⭐ **「知らせ」を「通知」へ統一した** —— 生きた散文 20 ファイルの 83 か所。表 T-037 の見出しも、`FR-076` の題も、表 T-233 の見出しも通知で揃えた。⛔ **過去の記録は掃いていない** —— 本表の過去の行も、変更要求も、開発の記録も、**当時そう書いたという記録である。**⭐ **CR-233: 重ねて描かれなかったタスクを誰も告げていなかった** —— `FR-015`（MUST）は「描かずに通知すること」と定め、`PI-10` は一致しなかった `UID` を報告まで運んでいたが、**表 T-233 にその場面の行が無く、`FR-076`（MUST NOT）が同表に無い理由を運ぶことを禁じていた。**`RS-16` を立てた（作法は `NT-5`）。⭐ **`RS-13` の場面を広げた** —— 公開されている `FormatMismatch` の `both` が 2 つの場面を1 つの値にしており、**片方で文が嘘になっていた。** 行を足さず語を真にした。⚠️ **数の予測は的中** —— `tables` 133 不変・`figures` 11 不変・`uids` 148 不変・`rows` 1630 → **1632**（`RS-16` と `U-57` で ＋2）|
| 1.01 | 2026-08-23 | ⭐ **利用者が実物で見つけた 2 つを閉じた**（CR-235。利用者の裁定）—— ⛔ **コマンドパレットの大きさをどの表も持っていなかった** —— `command-palette.ts` は発明を拒んで幅も高さも 0 を返しており、**実測 18 × 18px の箱に入口が全部詰まっていた。**⭐ **大きさは値ではなく規則だと判じ、`FR-053` に「中身に合わせること（MUST）。大きさを設定値の表に持ってはならない（MUST NOT）」を置いた** —— 入口の数も語の長さも表示言語で変わるので、固定値はどちらかの言語で入り切らなくなる。⚠️ **大きさを持たない以上、「薄く描く」の判定を矩形の内外では行えない**ので、ポインタの下の UI パーツで行うと定めた。⭐ **`S-80` の既定を `280` から `0` へ** —— ⛔ **同じセルの注が「`FR-080` の基準環境が『面は閉じている』『その場所は日程へ渡る』と定める」と書いているのに、既定だけがそれと食い違っていた**（値には 🔎 が付いており、測って決めた値ではなかった）。実測で 280px が予約され、中身は 1 つも描かれていなかった。⛔ **開いたときに取る幅の行は足していない** —— 開く経路そのものが `src/` に無く、**無い経路のために数を発明しない。**⭐ **実測で確かめた** —— パレット 18×18 → **592×274**、日程表の幅 820 → **1100**。⚠️ **数の予測は的中** —— `tables` 133・`figures` 11・`rows` 1632・`uids` 148 はすべて不変 |
| 1.02 | 2026-08-23 | ⭐ **掴めると書いてあるのに、掴む場所が表にも画面にも無かった**（CR-236。利用者の裁定）—— ⛔ **本行は後から足した** —— 仕様への編集は 2026-08-23 に当たっていたのに、**本表への 1 行だけが抜けていた**（規則 02 の手順 7）。⚠️ **直前の 1.01 の行は書き換えていない** —— 同行の `rows` 1632 はその時点の実測であり、**過去の行を真のまま残す**。⭐ **`FR-053`（MUST）は作成者が `Command Palette` をドラッグして動かせることを求め、`IC-53` は掴む領域を示すと定めていたのに、**掴み帯を持つ行がどの表にも無かった**。⭐ **表 T-023d の先頭へ `GR-19` を置いた** —— 優先順の頭であることが、中身を掴む前に帯が勝つことを意味する。⭐ **高さは `_assets/tbl-settings.md` の `S-135a` が持つ** —— `Panel Divider` の掴み帯（`S-134`）と同じ形である。⚠️ **数の実測**: `tables` 133 不変・`figures` 11 不変・`rows` 1632 → **1634**・`uids` 148 不変 |
| 1.03 | 2026-08-24 | ⭐ **告げる道と問う道を、仕様の側で開いた**（CR-237 / CR-238 / CR-239。いずれも利用者の裁定）—— ⛔ **`FR-076` は「同表に無い理由を運んではならない（MUST NOT）」と定めるのに、別の要求が MUST で通知を求める場面が 9 つ、運べる行を持っていなかった。** ⭐ **問いの側も同じ形の穴を持っていた** —— `NT-7` が「何が起きるかを示すこと（MUST）」と定めるのに、`FR-038` の辞書に問いの文で引ける節が 1 つも無く、画面は 3 か所とも空文字で問うていた。⭐ **表 T-234 を新設して問える場面の全数を持った**（`QN-1` 〜 `QN-8`）—— ⚠️ **問う場所は 5 つではなく 6 つであった** —— `FR-062` の「自動保存の刻印が 1 つでも違えば確認を求めること（MUST）」と、`FR-026` の復旧の申し出が数えられていなかった。⭐ **後者を問い側に置いたのは、表 T-037 の `NT-4` が逐語で「復帰・復旧の確認」と書いているからである。**⛔ **引継書の「表 T-233 に `NT-4` の 4 行」は 4 つとも真ではなかった** —— `FR-060` は `RS-1` が既に持ち、`FR-065` には起動時に申し出ると書いた本文が無く、`FR-026` は 1 場面ではなく 3 場面であった。⭐ **表 T-233 は `RS-17` 〜 `RS-25` の 9 行を得た。**⭐ **`ST-7` の安全弁を、例外ではなく値で告げる形に改めた** —— `FR-028` の「例外を投げてはならない（MUST NOT）」と本文が調停されていなかった。**「黙って切り捨てても、重ねて押し込んでもならない（MUST NOT）」は 1 文字も動かしていない。変えたのはどう告げるかだけである。**⛔ **表 T-220 の免除の根拠が、製品では 1 度も走っていなかった** —— 「スキーマが既に強制する」という現在形が偽であり、`src/` からの参照は 3 件ともコメントであった。⭐ **走らせること（MUST）と、走らせる場所（`CP-20`）と、`documentSettings` を厳格にしないこと（MUST NOT）を書いた** —— 表 T-024a の `OP-6` が「知らないキーは捨てずに保つ」と読む側に命じているからである。⚠️ **`uuid` の 10 列は、まだ誰も見ない列として残っている。埋めていない。**⚠️ **数の予測は的中** —— `tables` 133 → **134**・`figures` 11 不変・`rows` 1634 → **1651**・`uids` 148 不変。⛔ **辞書の枠だけ 1 度外した** —— 244 と書いたが実測 242 で、1 枠を言語ごとに 2 つ数えていた |
| 1.04 | 2026-08-24 | ⛔ **前の行が置いた MUST が強すぎたので、同じ日に狭めた**（CR-239 の第 4 節）—— ⛔ **1.03 は「`documentSettings` の群に当てるのは鍵ごとの型・範囲・列挙だけとする（MUST）」と書いた。その「範囲」が誤りであった。**⚠️ **配線の体はその文に忠実に検証器を書き、下限を割る設定値 41 件で文書ごと拒否した** —— ⭐ **落としたのは仕様どおりの実装であって、体の誤りではない。**⭐ **表 T-064 の `PI-2` が `clampedSettings`（下限・上限に収める）を公開しており、この製品の答えは「範囲外の値を拒まずに動かして収める」である。**⛔ **拒めば、見せ方の 1 鍵が範囲を外れただけで文書全体が開けなくなり、表 T-024a の `OP-6` が読む側に求めている寛さと逆になる。**⭐ **「鍵ごとの型と、原稿が値を綴った列挙だけとする（MUST）。下限・上限に反することを拒む条件を当ててはならない（MUST NOT）」に改めた。**⚠️ **列挙は残した** —— `S-82` のように原稿が値を綴った鍵は、それ自体が取りうる値の全数だからである。⛔ **あわせて `clampedSettings` に呼び手が 0 件であることが分かった** —— `scheduleViolations` とまったく同じ形の穴であり、PD-190 として記録した。**実装していない。**⚠️ **数の実測**: `tables` 134 不変・`figures` 11 不変・`rows` 1651 不変・`uids` 148 不変 |
| 1.05 | 2026-08-24 | ⭐ **説明が出る条件と消える条件、入口の図形の大きさ、操作子の縦の揃えを決めた**（CR-240 / CR-241 / CR-242。いずれも利用者の裁定）—— ⭐ **CR-240: 表 T-028 の `IN-3` は「勝手に消えないこと」とだけ書いており、`FR-092` の `EZ-2` が出す引き金を定めるのに、その引き金が外れたときを何も言っていなかった。** ⛔ **`docs/reference/wcag/` の SC 1.4.13 は 3 つ目の条件を「引き金が外れるまで」と限っており、本書はその限りを落として絶対化していた。**⚠️ **原典が緩いことは直す理由にならない** —— 規則 02 が仕様の側の強さを許している。⭐ **直す理由は、利用者が実物で「× を押すまで消えない」と言っていることである。**⭐ **`Dismissible` と `Hoverable` は原典より強いまま残した** —— 原典は消せることに除外を持つが、本書は持たない。緩める理由が本件に無い。⛔ **`EZ-2` と `S-124` は 1 文字も触っていない** —— 誘導部分グラフの唯一の閉路がその 2 つであり、片方だけ書くと収束しない。⚠️ **本行は利用者の訴えを 1 つも直さない** —— 原因は Chapter 5.5 の 表 T-078 の `FT-4` が配線されていないことであり、**仕様の側に足すものは無い**。⛔ **使えない入口の説明を条件なしで出し続けていたのは実装が発明した規則であって、`FR-029` はどこにもそう書いていない**（同要求の MUST は掴めない端点に向いている）。⭐ **CR-241: 表 T-051 の `HF-5` が「操作子を同じ大きさで描くこと（MUST）」と求めるのに、その大きさを持つ行がどの表にも無かった。** ⛔ **描く側は自分で選んだ値に暫定の印を付けて報告しており、規則 02 の作法どおりに振る舞っていた。**⭐ **実測: いまの箱は 16 × 16px である**（環境の文字サイズの 1 倍）。⚠️ **引継書の「18 × 18px」は誤りで、あれは枠と余白を含む入口の外形 30 × 26 のほうである。**⭐ **その 75% の 12px を `S-138` として 表 T-206 に置き、`FR-029` から名指した。**⛔ **面ごとに分けていない** —— 表 T-109 が持たない分類を 表 T-206 が作ることになるうえ、1 つにすると `HF-5` の「同じ大きさ」も同時に満たされる。⛔ **下限と上限は持たせていない** —— 同表にその列が無く、1 行のために散文へ数を写すのは規則 03 が禁じている。⚠️ **入口の外形には触っていない** —— 実測で入口の高さは行送りが決めており、図形を縮めても帯は縮まない。⭐ **CR-242: `HF-5` は「上端で揃える」と定めるのに、描く側は中央で揃えていた。** ⛔ **どちらが正かは仕様が決めるので、仕様の側を利用者の裁定へ寄せた** —— 名前の上端から下げて揃えること、中央で揃えてはならないこと、下げる量を名前の文字サイズに比例させることの 3 つ。⭐ **量は `S-139` として 表 T-206 に置いた**（名前の文字サイズに掛ける 0.25。⛔ 由来は無い）。⭐ **`FR-098` のピン止めは置き方を `HF-4` 〜 `HF-6` と `HF-9` に自ら委ねているので、1 セルで追随する** —— 書き足すと同じ規則が 2 か所になる。⛔ **操作子を名前の下の行へ移す案は採らなかった** —— 表 T-031 の `SC-1` が行見出しを日程側の行に縦で従わせており、見出し側だけが高くなると噛み合わない。⛔ **`HF-4` `HF-6` `HF-9` は 1 文字も触っていない。** `tables` 134・`figures` 11・`uids` 148 は不変、`rows` は 1651 → **1653**（`S-138` と `S-139`）|
| 1.06 | 2026-08-25 | ⭐ **暗いテーマを実際に暗くし、畳んだ行を開けるようにし、操作子と図形の大きさを決め直した**（CR-243 〜 CR-248。いずれも利用者の裁定）—— ⭐ **CR-243: 「ダークモードで色が変わるが、ダークになっていない」の原因は色ではなかった。** ⛔ **`color-scheme` は `src/` に 1 度も現れず、地の色を塗っている場所が 1 つも無かった。**⚠️ **画面の道具はすべて閲覧環境のシステム色で描かれており、あれは OS の明暗に追随して読む人の選んだ `themePreference` には追随しない。**⛔ **`S-72` を読む場所は 3 か所あるが、そのどれも DOM へ届いていなかった。** ⭐ **表 T-236 を新設し、前プロジェクトの試作が持つ 25 の色を明暗 2 欄で置いた** —— **1 つの色は 1 つの決めごとであり、明暗で 2 つの表に割ると対応する行が黙ってずれる。**⭐ **色相に追随するかどうかは行ごとに違う**ので、行が自分の欄で名乗る。⛔ **`FR-041` の「依存線とイナズマ線は同じ固定色とし」を書き換えた** —— **同じ 1 文が両者を「テーマに追随させる対象」にも挙げており、自己矛盾であった。**⭐ **`FR-019` は既に両者をテーマの色相と別のものとして扱っており、本文がそちらへ揃った。**⚠️ **明暗で依存線の色相が 26 → 30 へ動くのは試作がそうだからであり、理由は記録されていない。写した。** ⭐ **CR-244: 1 度畳むと開く手立てが無かった。** ⛔ **`HF-2` は「1 段だけ開く」、`HF-3` は「配下をすべて閉じる」であり、閉じたものを開く組み合わせが存在しなかった。** ⭐ **`HF-2` を `HR-3`（子を全展開）、`HF-3` を `HR-5`（自分を畳む）にして対にし、最上位が自分を畳んだときのために `HF-10` を足した。**⛔ **入口は増やしていない** —— `IC-58` と `IC-59` は面も数も変えず、意味だけを揃えた。⚠️ **`HF-8` の「唯一の操作」は倍率と表示位置ごと戻すことに限った** —— `HF-10` は畳みだけを戻すので同じ操作ではない。⭐ **図形は `＋` とした** —— ⛔ **発明していない。利用者の裁定が `[+] Expand All` と綴っており、指された実物も同じ隅に `+` を描いている。** ⭐ **CR-245: 名前と操作子の干渉の原因は、ホバーの不在ではなかった。** ⛔ **`FR-085` が MUST で求める「操作子に確保する場所」を、実装が 0 で出荷していた。**⭐ **`S-140` として値を与え、`HF-6` を「行の名前にポインタが乗っているあいだだけ描く」に書き換えた。**⛔ **確保する場所は描くかどうかで変えない** —— 書き出しが操作子を描かずに場所を確保しているのと同じ形である。⭐ **グリフが小さい原因も枠ではなかった** —— **図形は 24 × 24 の座標系の約 75% しか使っておらず、12px の箱に入れると見える印は約 9px になる。**⭐ **`S-141` で最低隙間を 2px と定め、座標系を図形の外接枠へ詰めることを MUST にした。**⛔ **図形ごとに別々に詰めてはならない** —— 相対的な大きさが変わり、同じ箱でも揃って見えなくなる。 ⭐ **CR-246: 掴んでも付いてこない原因は実測した** —— ⛔ **`ScreenSession` が「掴み始めた場所」を持たず、描く側は離した後の角しか知らされていなかった。** ⭐ **握っているあいだ追従することを MUST にし、角が決まるのは離した時点のままとした**（`IN-1`）。⭐ **群の見出しは刷らないことにし、境目は線とした** —— ⛔ **文字にすると `FR-038` の表示語になり言語ごとに持つことになる。**⚠️ **群の欄も辞書の語も消していない** —— 群は並べる順のために要り、ヘルプは語で並べる。**消えるのはパレットに刷ることだけである。** ⭐ **CR-247: 裁定待ちの一覧が持っていた見立ては逆であった** —— ⛔ **同行は 表 T-037 に「問う」行を足せと言うが、`FR-020` は「選ばせる」であり、`NT-7` は要求が確認を求めると定めた場面だけに問うことを許し、`FR-031` はそれ以外を禁じている。行を足すと 2 つの MUST を破る。**⭐ **要ったのは 表 T-202 の真偽 1 行だけである。** ⭐ **CR-248: `FR-061` が `DI-1` を禁じていた理由はただ 1 つ、「ファイル名を含むが未保存の文書にも自動保存は掛かる」であった。** ⭐ **既定のファイル名を定めてその理由を消し、禁止を外した。**⚠️ **未保存の文書どうしが枠を共有することは受け入れた** —— ⛔ **起動テンプレートは `Project.name` が固定で `Project.id` を持たないので、2 つ目が 1 つ目を上書きする。表 T-004 の `LM-17` として名乗る。** `tables` 134 → **135**（`T-236`）、`rows` 1653 → **1687**、`figures` 11・`uids` 148 は不変。表 T-109 は 73 → **74** 行 |
