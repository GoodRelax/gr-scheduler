# 設定値 — 既定値と範囲の全数

**UID**: DOC-TBL-SETTINGS
**Version**: 0.1

> ⛔ 本書は生成物である。  
> 手で直さない —— 直しても次の `npm run gen` で消える。
> **設定値の唯一の正は `_source/settings.json` である。**  
> 本書はそれを `_source/settings_json_to_md.py` が印字したものである。
> **作り直す**: `npm run gen` ／ **ズレを検出する**: `npm run gen:check`（検査 16 が呼ぶ）。

**設定値の正は `_source/settings.json` が持つ。**  
名前の正は `tbl-glossary.md` の表 T-104 が持ち、**本書は値** （既定値・下限・上限・範囲の理由）を印字する。  
**名前と値を 2 か所で管理しない。**

由来は前プロジェクトの設定値確定版（`previous-project-result/02-data-model/grs-document-settings-ja.md`）である。

## 1. 保存するかどうかの判定

**Type**: SECTION

**判定は 2 段である。**

> **第 1 段 —— その値を変えると、画面または出力の絵が変わるか。**
> **第 2 段 —— その値は文書の内容か、読む人の環境か。**
>
> ⚠️ **第 3 の枠がある** —— 保存値を**初期値** として持ち、読む人の指定が上書きするもの（`fontScale` / `themePreference`。  
> 規則は `FR-039`）。

第 1 段で絵が変わり、**かつ** 第 2 段で文書の内容であるものだけを文書が持つ（`documentSettings`）。  
第 1 段で変わらないものは持たない。  
絵が変わっても読む人の環境に属するものは持たない。  
その全数と、環境に属すると判断した理由は表 T-206 が持つ。

**同じ JSON からは同じ絵が出る** ことが第 1 段の目的である。  
⚠️ **第 2 段で外したものは、この保証の対象に入らない。**

**書き出しの規則と理由は `FR-024` が持つ** （常に全項目を書き出す）。

## 2. 由来の無い既定値

**Type**: SECTION

**🔎 を付けた既定値は、由来が記録されていない。**  
測って決めた値ではなく、実装時に置いて動いたのでそのまま残った値である。  
本プロジェクトで選び直してよい。  
むしろ選び直すべきである。

印が要る理由は、全項目を書き出す方針の帰結である。  
凍結する以上、由来の無い値も永久に残る。  
どれが測った値でどれが置いただけの値かを区別できないと、次の担当者は全部を等しく尊重してしまい、直す機会を永久に失う。

「範囲の理由」列は範囲（下限・上限）の根拠であって、既定値の根拠ではない。  
両者は別である。

**⛔ は文書に保存しない** （表 T-206）。

**根は 4 つである** （`basePlanHeight` / `actualMin` / `fontMin` / `pxPerDayAt1x`）。  
残りはそこから落ちてくる。  
**進捗マーカーの径（`S-22`）だけは鎖の外の固定値である** —— 規則と理由は `FR-094` が持つ。

## 3. 描画の設定

**Type**: SECTION

**表 T-201 — 描画の設定**

| 行 ID | 群 | キー | 単位 | 既定値 | 下限 | 上限 | 範囲の理由 |
| --- | --- | --- | :--: | --: | --: | --: | --- |
| S-1 | タイムルーラー | `pxPerDayAt1x` | px | 6 🔎 | 0.5 | 60 | 基準の環境で 1 年がおよそ 2200px になる値だが、そう選んだ記録は無い |
| S-2 | タイムルーラー | `rulerHeight` | px | `rulerFont` × 3 + `rulerLabelPad` × 3（S = 42 / M = 48 / L = 54）🔎 | `rulerFont` × 3 + `rulerLabelPad` × 3 | 150 | 段階 4 は 3 段（年 ＋ 月 / 日 / 曜日）。<br>3 段ぶんの文字と余白が入る高さで、余白は `S-136` が持つ。<br>追随の規則と、キーを独立に保つことは `FR-039` が持つ |
| S-3 | タイムルーラー | `rulerFont` | px | `fontScaleSizes[fontScale]`（S = 12 / M = 14 / L = 16）🔎 | `fontMin` | (`rulerHeight` − `rulerLabelPad` × 3) ÷ 3 | 和文の可読下限を割らない。<br>帯の 3 段に収まる。<br>`fontScale`（表 T-215）に追随する。<br>追随の規則と、キーを独立に保つことは `FR-039` が持つ |
| S-135 | タイムルーラー | `rulerLabelGap` | px | 2 🔎 | 0 | 30 | 目盛ラベルどうしの隙間（横）。<br>この隙間を足して導くのは 表 T-205 の `S-85` だけである —— 刻みが 1 日の段でしか隣り合うラベルが接しない。<br>⭐ **2 とするのは、実測の隙間が 3.4px であり、8 では実物と合わないからである（出荷ビルド、表示言語 en で隣り合う曜日ラベルの隙間を測った）。<br>** ⛔ `labelGap`（`S-32`）は形状の外へ出すラベル用であり、目盛には当てない。<br> 上限は `S-32` と同じとし、由来は無い |
| S-136 | タイムルーラー | `rulerLabelPad` | px | 2 🔎 | 0 | 20 | 罫線と目盛ラベルの余白（縦）。<br>帯の高さ（`S-2`）はこれを目盛の帯の中の段（表 T-006b の `A-1` の ⑤）3 つぶん含む。<br> 上限は、`rulerFont` が下限（`S-8`）のときでも 3 段ぶんが `S-2` の上限に収まる値 |
| S-179 | タイムルーラー | `rulerLabelBottomPad` | px | 3 | 0 | `rulerFont` | 目盛ラベルの下側の余白（縦）。<br>⛔ **帯の高さ（`S-2`）はこれを含まない** —— 段の高さは `rulerFont` と `rulerLabelPad` のままで、この余白は文字の箱の中から取る。<br>上限が `rulerFont` なのは、超えるとベースラインが段の上の罫線より上へ出るためである |
| S-4 | 縦の寸法 | `basePlanHeight` | px | 28 | `actualMin` ÷ `actualOfPlan` | 200 | 等倍がこの値だと縦に縮める余地が無くなる |
| S-5 | 縦の寸法 | `actualOfPlan` | — | 0.5715 | 0.05 | 1 未満 | 実績 < 予定。<br>既定は `S-6` の下限 16px ÷ `S-4` の既定 28px ＝ 0.571428… を、小数第 4 位へ切り上げた値である （下記）。<br>⛔ **切り下げてはならない** —— `S-4` の下限は `actualMin` ÷ `actualOfPlan` であり （Chapter 6.1 の 表 T-220 の `IV-16`）、切り下げると `S-4` の既定 28 がみずからの下限を割る。<br>`S-4` の下限（16 ÷ 本値）は、0.5714 では 28.0014、0.5715 では 27.9965 である。<br> ⭐ 既定で立つ姿は 予定 28px ⊃ 実績 16.002px ⊃ 依存線 12px、予定だけの縁は上下 5.999px である —— 表 T-023d の結びが定める縦の入れ子と、線の掴み代が実績の縦幅より狭いことを、既定値のまま満たしている。 |
| S-6 | 縦の寸法 | `actualMin` | px | 16 | `fontMin` ÷ `fontOfActual` | 80 | これを割ると文字が下限に張り付く |
| S-7 | 縦の寸法 | `fontOfActual` | — | 0.80 | 0.05 | 1 未満 | フォント < 実績 |
| S-8 | 縦の寸法 | `fontMin` | px | 12 | 12 | 40 | 和文がこれを割ると読めない |
| S-9 | 縦の寸法 | `thinFontScale` | — | 0.85 🔎 | 0.3 | 1 | **この倍率を掛けた後も可読の下限（`S-8`）を割らないこと。<br>** 割るときの扱いは `FR-077` が持つ（倍率ではなく行の量を減らす） |
| S-10 | 縦の寸法 | `actualGap` | px | 2 🔎 | 0 | 20 | — |
| S-11 | 縦の寸法 | `stackGap` | px | 12 | `dependencyWidth` × 2 | 60 | 依存線が 1 本通る幅 |
| S-12 | 縦の寸法 | `rowGap` | px | 8 🔎 | 0 | 60 | — |
| S-13 | 形状の縦幅 | `shapeHeightOf.rectangle` | × | 1.0 | 1 | 1 | 基準なので 1 固定 |
| S-14 | 形状の縦幅 | `shapeHeightOf.chevron` | × | 1.0 🔎 | 0.2 | 3 | 矩形と同じ |
| S-15 | 形状の縦幅 | `shapeHeightOf.arrow` | × | 0.5 | 0.1 | 1 未満 | 細線は矩形より薄い（それが取り柄） |
| S-16 | 形状の縦幅 | `shapeHeightOf.endpointSpan` | × | 0.5 | 0.1 | 1 未満 | 同上 |
| S-17 | 形状の縦幅 | `shapeHeightOf.milestone` | × | 1.5 | 1 超 | 4 | 目立たせるので矩形より大きい |
| S-18 | 依存線 | `dependencyWidth` | px | 1.5 | 0.5 | `stackGap` ÷ 2 | 段の間隔の半分を超えない |
| S-19 | 依存線 | `dependencyArrowLength` | px | 7 🔎 | `dependencyWidth` × 2 | 40 | 線より太くならない長さ。<br>⭐ **同じ行が 3 つを兼ねる** —— 三角の長さ・三角の底辺（二等辺で描くため）・依存線の走り（表 T-221 の `LF-4`。<br>入口 ＝ 本値 × `S-20`、出口 ＝ そこから本値を引いたもの）。<br>⭐ **7 は、一回り小さくするために 10 を 30% 減じた値である** —— ⛔ **10px の由来は元から無い。<br>** ⚠️ **走りが一緒に縮むのは害ではない** —— `S-20` の備考が走りと三角の釣り合いを前提に書いており、比が保たれるので釣り合いは崩れない |
| S-20 | 依存線 | `dependencyRunOfArrow` | × | 2 | 1 超 | 6 | 1 以下だと三角形が入口の走りを食い尽くし、同時に出口の走りが 0 以下になる |
| S-22 | 進捗マーカー | `markerSize` | px | 16 | `fontMin` | `actualMin` | 固定値。<br>縦の鎖に載らない（規則は `FR-094`）。<br>上限が `actualMin` なのは、実績バーの床と同じ値までなら実績バーより大きくならないためである。<br>下限が `fontMin` なのは、記号がこれを割ると読めないためである |
| S-23 | 進捗マーカー | `markerGap` | px | 4 | 4 | 4 | 固定。<br>端点の掴み代と重ならない最小距離であり、それ以上離さない |
| S-24 | 進捗マーカー | `markerStroke` | px | 1.3 🔎 | 0.5 | 4 | — |
| S-25 | 進捗マーカー | `resumeScaleInvalid` | × | 0.7 🔎 | 0.3 | 1 | 再開日未定は小さく薄く。<br>0.7 の由来は無い |
| S-26 | 進捗マーカー | `resumeArmOfMarker` | — | 0.62 🔎 | 0.2 | 1 − `resumeHeadOfMarker` | 矢じりぶんを残す。<br>0.62 の由来は無い |
| S-27 | 進捗マーカー | `resumeHeadOfMarker` | — | 0.22 🔎 | 0.05 | 0.5 | — |
| S-28 | 進捗マーカー | `resumeDashOn` | px | 3 🔎 | 1 | 12 | — |
| S-29 | 進捗マーカー | `resumeDashOff` | px | 2 🔎 | 1 | 12 | — |
| S-30 | ラベル | `labelCoef` | — | 0.5 🔎 | 0.3 | 1 | 全角 1 文字 = フォント × 2 × 係数。<br>**実測しない方針なので、この 1 値がレイアウト精度を単独で決める** |
| S-31 | ラベル | `labelPad` | px | 6 🔎 | 0 | 30 | — |
| S-32 | ラベル | `labelGap` | px | 8 🔎 | 0 | 30 | — |
| S-33 | ラベル | `labelBaseline` | — | 0.35 🔎 | 0 | 0.8 | フォントのベースライン補正。<br>実測の記録が無い |
| S-34 | ラベル | `labelHaloOfFont` | — | 0.17 | 0 | 0.3 | 0 = 縁取りなし。<br>12px の文字で約 2px。<br>バーの上の文字が色に依存せず読める |
| S-35 | ラベル | `truncateUnits` | — | 48 | 4 | 120 | 全角 24 文字 = 半角 48。<br>⭐ **タスク名を半角 48 文字まで表示し、残りを ... で省略するための値である**。<br>⚠️ **省略記号を含めた長さがこの値に収まる** —— 規則は 表 T-013 の前書きが持つ |
| S-36 | ラベル | `rowTitleFont` | px | 13 🔎 | `fontMin` | 40 | 和文の可読下限を割らない。<br>13 という値自体の由来は無い |
| S-37 | ラベル | `rowTitleIndent` | px | 16 | 0 | 60 | 行見出しの字下げ。<br>1 段深くなるごとにこの幅だけ字下げする。<br>⭐ **16 は測って決めた値ではなく、利用者が数で指定した値である** —— ⛔ **以前の 12 は誰も測っておらず（🔎 付き）、根拠が無かった** |
| S-38 | ラベル | `rowTitleTopScale` | × | 1.3 🔎 | 1 | 2 | 深さ 1 は他より小さくしない。<br>1.3 の由来は無い |
| S-39 | 形状の細部 | `planStroke` | px | 1 🔎 | 0 | 4 | — |
| S-40 | 形状の細部 | `thinStrokeOfPlan` | — | 0.20 🔎 | 0.05 | 0.6 | — |
| S-41 | 形状の細部 | `thinStrokeMin` | px | 1.2 🔎 | 0.5 | `thinStrokeMax` | 上限を超えない。<br>1.2 の由来は無い |
| S-42 | 形状の細部 | `thinStrokeMax` | px | 4 | `thinStrokeMin` | 20 | 下限を下回らない |
| S-43 | 形状の細部 | `chevronNotchOfHeight` | — | 0.45 🔎 | 0.05 | 1 | — |
| S-44 | 形状の細部 | `chevronNotchOfWidth` | — | 0.35 🔎 | 0.05 | 0.5 | 0.5 を超えると先端が反転する。<br>0.35 の由来は無い |
| S-45 | 形状の細部 | `arrowHeadOfStroke` | × | 3.2 🔎 | 1.5 | 8 | — |
| S-46 | 形状の細部 | `arrowHeadOfSpan` | — | 0.4 🔎 | 0.1 | 1 | — |
| S-47 | 形状の細部 | `spanDotOfStroke` | × | 1.15 🔎 | 0.5 | 4 | — |
| S-48 | 形状の細部 | `starInnerOfOuter` | — | 0.45 🔎 | 0.2 | 0.8 | この値が ☆ の面積を決め、`milestoneGlyph` の面積順に影響する |
| S-49 | 形状の細部 | `minShapeWidth` | px | 6 🔎 | 1 | 20 | 期間がゼロでも残す最小の幅（`FR-001`）。<br>⭐ **本値は `S-90` に縛られない** —— 予定の端点の掴み代（`S-90`）は端の外側だけに届くので、胴は幅によらず必ず残る。<br> ⭐ **由来を持たないので選び直してよい** （🔎）。<br>⚠️ 床が `S-86` 以上になると、どの `Task` も LOD で落ちなくなる —— 床は全 `Task` に掛かるためである |
| S-50 | イナズマ線 | `progressLineWidth` | px | 2 🔎 | 0.5 | 8 | — |
| S-51 | イナズマ線 | `progressLineOverhang` | px | 6 🔎 | 0 | 40 | — |
| S-53 | ズーム | `zoomStep` ⛔ | × | 1.1 🔎 | 1.01 | 2 | — |
| S-54 | ズーム | `zoomMin` ⛔ | × | 0.02 🔎 | 0.001 | `zoomMax` | 上限を超えない。<br>0.02 の由来は無い |
| S-55 | ズーム | `zoomMax` ⛔ | × | 64 🔎 | `zoomMin` | 512 | 下限を下回らない。<br>64 の由来は無い |
| S-56 | ズーム | `canvasPadding` | px | 10 🔎 | 0 | 60 | — |
| S-129 | 予実 | `actualInitialDuration` | 稼働日 | 1 | 0 | 1 | 掴みシロを掴んだときに置く実績期間（`FR-043`）。<br>**1 日で終わる業務も 1 日ぶん入力する** ので 1。<br>0 にすると幅が 0 になる |
| S-130 | 予実 | `milestoneActualDuration` | 稼働日 | 0 | 0 | 0 | マイルストーンの実績期間（`FR-043`）。<br>**点なので長さを持たない** （表 T-012 の `SH-5`）。<br>表 T-019 の `PA-2` 〜 `PA-5` を満たすための固定値である |
| S-131 | 予実 | `dummyOpacity` | — | 0.20 🔎 | 0.05 | 0.5 | 実績のダミーと未着手マーカーの濃さ（`FR-043` / `FR-013`）。<br>**狙いも適合範囲の外し方も `S-102` と同じである。<br>** 掴める大きさは濃さと別に `S-180` が持つので、薄くしても掴みにくくならない |
| S-181 | 注記 | `commentBoxPad` | px | 3 | 0 | 30 | コメントボックスの本文の箱の内側の余白（上下左右とも）。<br>上限は `labelPad`（`S-31`）に揃える —— 同じ「文字の周りの余白」であり、別の上限を持つ理由が無い |
| S-182 | 注記 | `commentBoxWrapUnits` | — | 128 | 4 | 240 | コメントボックスの本文を折り返す単位数。<br>**全角 64 文字 ＝ 半角 128**。<br>数え方は `FR-093`（全角 2・半角 1）。<br>⚠️ **これを超える語は作成者が改行する。<br>** 下限は `truncateUnits`（`S-35`）と同じで、全角 2 文字を割ると語が残らない。<br>上限は、`fontScaleSizes.L` と `labelCoef` の既定で 1 行が 表 T-025 の `MC-6` の画面幅にちょうど収まる値 |

**帯の高さを目盛の段階で動かさない規則は `FR-017` が持つ。**  
文字の大きさは段階によらず `rulerFont` とし、段階が変えるのは帯の中の段の組み方だけである —— 段階で文字が変わると、段階 → 実効フォントサイズ → 判定 → 段階 の循環になる。  
⚠️ 例外が 1 つある。  
規則は `FR-017` が持つ （曜日の行だけを`S-219` の比で小さく刷ってよい）—— ⛔ **ここに書き写してはならない。**  
⭐ **例外が上の理由を壊さないのは、比が段階ではなく行に掛かるからである** —— **どの段階でも同じ比であり、段階 → 文字 → 判定 の循環に入らない。**

> ⭐ `actualOfPlan` は縦寸法の鎖の最上流にある。  
> 既定 0.5715 は測って決めた値ではなく、導いた値である —— 導き方と、切り上げなければならない理由は、本表の `S-5` の備考が持つ。  
> ⭐ ここはそれを指すだけである —— 同じ導出が 2 か所に載ると、必ず離れていく。
> ⚠️ 「0.57」と書かれることがあるが、この数を小数 2 桁へ丸めた書き方であって、別の値ではない。  
> ⛔ 丸めたまま採ると `tools/generate_startup_template.py` が実際に拒む —— **理由は `S-5` の備考が持つ。**
> ⭐ **狙いは値ではなく姿である** —— **重なるものが入れ子になり、行の高さは 1px も動かない。**  
> ⭐ **立つ姿の寸法も `S-5` の備考が持つ。**

## 4. 表示の切り替えと文書全体の書式

**Type**: SECTION

**表 T-202 — 表示の切り替えと書式**

| 行 ID | キー | 型 | 既定 | 意味 |
| --- | --- | --- | --- | --- |
| S-58 | `stackDirection` | `'up'` / `'down'` | `'up'` | 積む向き |
| S-227 | `planVisible` | 真偽 | `true` | 予定の表示。<br>実績（`S-228`）とは独立に切り替える。<br>規則と理由は `FR-049` |
| S-228 | `actualVisible` | 真偽 | `true` | 実績の表示。<br>予定（`S-227`）とは独立に切り替える。<br>規則と理由は `FR-049` |
| S-60 | `assigneeVisible` | 真偽 | `false` | 担当ラベル。<br>既定は隠す（下記） |
| S-61 | `percentCompleteVisible` | 真偽 | `false` | 完了率ラベル。<br>既定は隠す（下記） |
| S-62 | `dependencyVisible` | 真偽 | `true` | 依存線。<br>核機能であり、段数に影響しない。<br>寸法をズームに追随させない規則は `FR-094` が持つ |
| S-63 | `progressMarkerVisible` | 真偽 | `true` | 進捗マーカー（`FR-013`）と再開アイコン（`FR-044`）。<br>寸法をズームに追随させない規則は `FR-094` が持つ |
| S-64 | `progressLineVisible` | 真偽 | `false` | イナズマ線。<br>基準日と実績から引くので、**新規文書には出しても意味のある形にならない** |
| S-65 | `dualCursor` | `{ date1, date2 }` / `null` | `null` | デュアルカーソル。<br>2 本の日付を持つ理由は表 T-029 の `CU-2` |
| S-66 | `guideCursorMode` | `'none'` / `'crosshair'` / `'single-vertical'` | `'none'` | ガイドカーソル。<br>⛔ **`'double-vertical'` は値に持たない** —— **縦 2 本のガイドカーソルは、測るための 2 本（表 T-029 の `CU-2`）と見分けが付かない。<br>** ⛔ **この値を持つ文書は取り込めない** —— 見せ方の群に当てるのは原稿が綴った列挙だけであり（第 6 章）、拒む条件を足してはならない。<br> ⚠️ **初回リリース前であり、この値を持つ文書はまだ 1 つも存在しない。<br>** ⭐ **`'none'` は値として残るが、押す入口を持たない** —— **同じ入口の押し直しで到達する（`FR-048`）。<br>** |
| S-67 | `dateGridLinesVisible` | 真偽 | `false` | 日付罫線。<br>既定が `false` である理由と、目盛の段階に連動させる規則は `FR-089` が持つ。 |
| S-68 | `groupGridLinesVisible` | 真偽 | `true` | グループ罫線。<br>本数は行数ぶんで軽い。<br>描く規則と理由は `FR-042` |
| S-69 | `baselineVisible` | 真偽 | `false` | 変更前の予定を重ねるか。<br>既定が `false` である理由は `FR-015` |
| S-70 | `fontScale` | `'S'` / `'M'` / `'L'` | `'M'` | 文字サイズ。<br>**各値が何 px かは表 T-215 が持つ** |

`assigneeVisible` と `percentCompleteVisible` の既定が `false` である根拠は `FR-049` が持つ。  
実測は、前プロジェクトの PoC で `zoomX = 0.25` のとき段数 96 → 72（24 段ぶん減る）というものである。

**依存線とイナズマ線の色の規則と理由は `FR-041` が持つ。**

## 5. 画面の状態

**Type**: SECTION

**表 T-203 — 画面の状態**

| 行 ID | キー | 型 | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --- | --- | --- | --- | --- |
| S-72 | `themePreference` | `'light'` / `'dark'` | `'light'` 🔎 | — | — | 明暗テーマ。<br>列挙なので範囲は無い |
| S-74 | `themeMonochrome` | 真偽 | `false` | — | — | モノクロにするか |
| S-75 | `zoomX` | 数値 | `1` | `zoomMin` | `zoomMax` | 横のズーム倍率 |
| S-76 | `zoomY` | 数値 | `1` | `zoomMin` | `zoomMax` | 縦のズーム倍率。<br>`basePlanHeight` が「予定の縦幅（`zoomY` = 1）」と定義されているので等倍がその基準 |
| S-77 | `scrollDate` | 日付 / `null` | `null` | — | — | 表示の左端が指す日付。<br>`null` は「人がまだ場所を決めていない」を表す。<br>読み込む側の規則は表 T-024a の `OP-10` |
| S-78 | `scrollGroupId` | `TaskGroup.id`（UUID 文字列）/ `null` | `null` | — | — | 表示の上端が指す行。<br>⚠️ **整数ではない。<br>** 存在しない id を指していたときの規則は表 T-024a の `OP-10` |
| S-176 | `scrollGroupOffset` | 数値 | `0` | 0 | 1 | 表示の上端が、`scrollGroupId` が指す行のどこにあるか。<br>その行が占める送り（その行の帯の高さと、その下の隙間を合わせた長さ。<br>次の行の上端までの距離であり、最後の行は自身の帯）に対する比であり、px ではない （`FR-080`）。<br>⚠️ **帯の高さではなく送りである理由は 表 T-023d が持つ** —— 同表のパンの規則が、何に対する比かまで定めている。<br>⚠️ 上限の 1 は含まない。<br>1 以上になったら錠を 1 つ隣へ送る —— 同じ位置を 2 通りに書けると往復の比較（`NS-4`）が偽になる |
| S-177 | `scrollDayOffset` | 数値 | `0` | 0 | 1 | 表示の左端が、`scrollDate` が指す日のどこにあるか。<br>⭐ **横の軸の `S-176` である** —— 持ち方も上限の扱いも同行が持つ |
| S-79 | `rowTitlePanelWidth` | px | `200` 🔎 | `rowTitleIndent` × `maxGroupDepth` | `Row Area` の幅 > 0 に従う | 行見出しパネルの幅。<br>下限が 0 にならない理由は `FR-052`。<br>範囲としては `S-125` の深さぶんのインデントが入る幅を要する⭐ **深さ 5 の掴み代が操作子に覆われないことから決めた。<br>** 深さ `d` の掴み代の右端は `16d+16`、操作子の左端は 幅 − 104 なので、深さ 5 では 幅 ≧ 200 が要る。<br>⚠️ 実測（幅 170px）: 深さ 4 と 5 で掴み代が操作子の下に隠れ、掴もうとした押しが別の操作子に取られた。 |
| S-80 | `propertyPanelWidth` | px | `0` | 0 | 同上 | プロパティパネルの幅。<br>⛔ **本行は幅だけを持ち、面を出しているかどうかは持たない**—— **出しているかは 表 T-206 の `S-99h` が持つ。<br>** ⚠️ **`0` は「まだ広げていない」であって「閉じている」ではない** —— `FR-052` は境界のドラッグで幅を 0 まで畳めると定めており、0 は正当な幅である。<br>⭐ **面を出したときに置く幅は 表 T-206 の `S-171` が持つ** —— 人が境界をドラッグした後は本行が勝つ（`FR-052`） |
| S-126 | `pinnedGroupIds` | `TaskGroup.id` の配列 | `[]` | 0 件 | `pinnedRowMax` | ピン止めしている行。<br>上限は `S-127`。<br>指す行を消したときの規則は表 T-050 の `CD-2` |
| S-127 | `pinnedRowMax` | 整数 | `5` 🔎 | 1 | 8 | 同時にピン止めできる行の数の上限 |

**`themeHue = 214` は実測値である。**  
予実の色表は色相 214 で測られており（実績 ÷ 予定 = ライト 4.81 : 1 / ダーク 3.74 : 1）、**この色相だけがコントラスト検証を通っている。**  
前プロジェクトの JSON 実例にある `210` に根拠は無い。

2 つのパネル幅の上限は互いに依存する。  
式と検証の規則と理由は `FR-052`。

**派生する色を保存しない規則と理由は `FR-041` が持つ。**  
解き方は「色相ごとに、コントラスト規則が壊れる寸前まで濃い地色」である。

**行の帯の色も `themeHue` から解く。**  
解き方の規則と理由は `FR-042`。

**`themeMonochrome` は保存値を変えない。**  
描画時にどう効くかの規則と理由は `FR-041` が持つ。

> ⚠️ **モノクロ時のコントラストは未検証である。**  
> 前プロジェクトのコントラスト検査は色を指定していない側しか測っていない。  
> **扱いと測る対象は `NFR-007` が持つ。**

> ⚠️ `themePreference` と `fontScale` の保存値は初期値である。  
> 読む人が変更できること、および変更が文書の編集になることは `FR-039` が定める。

## 5a. 日程データに属する値

**Type**: SECTION

**表 T-216 — 日程データに属する値**

| 行 ID | キー | 型 | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --- | --- | --- | --- | --- |
| S-73 | `themeHue` | 0〜359 | `214` | 0 | 359 | テーマの色相。<br>置き場は表 T-052 の `DR-5`。<br>追随の対象と規則は `FR-041` |
| S-71 | `importSeq` | 整数 | `0` | — | — | 取込のたびに +1。<br>既定は取込を 1 度もしていない状態。<br>置き場は `Project.importSeq`（表 T-058）。<br>**使い道は表 T-032 の `MG-13`** |

## 6. 出力

**Type**: SECTION

**表 T-204 — 出力**

| 行 ID | キー | 型 | 既定 | 意味 |
| --- | --- | --- | --- | --- |
| S-81 | `exportCanvas` | `{ width, height }` | `1600 × 900` | SVG / PNG の出力サイズ。<br>規則と理由は `FR-025`、縮める比は `FR-080` が持つ |
| S-217 | `exportCanvasHeightCap` | px | `4096` | 書き出す絵の高さの上限。<br>規則と理由は `FR-025` が持つ。<br>⭐ **`S-81` の高さは伸ばす前の下限であって、上限ではない** 。<br>⚠️ `4096` は 2^12 であり、canvas とテクスチャの上限として最も広く使われる値である —— 4000 前後の、よく使われる値を選んだ |

## 7. LOD のしきい値

**Type**: SECTION

出力に含まれるタスクが変わるので保存する。

**表 T-205 — LOD のしきい値**

| 行 ID | キー | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --: | --- | --- | --- |
| S-83 | `rulerTierPxPerDayMonth` | `1.4` | 0.1 | `rulerTierPxPerDayWeek` | 目盛が「年」から「年 ＋ 月」に変わる px/day。<br>次のしきい値を超えない |
| S-84 | `rulerTierPxPerDayWeek` | `4.3` | `rulerTierPxPerDayMonth` | `rulerTierPxPerDayDay` | 目盛が「年 ＋ 月」から「年 ＋ 月 ＋ 週」に変わる px/day。<br>前後のしきい値の間 |
| S-85 | `rulerTierPxPerDayDay` | `17.5` | `rulerTierPxPerDayWeek` | 60 | 目盛が「年 ＋ 月 ＋ 週」から「年 ＋ 月 ＋ 日 ＋ 曜日」に変わる px/day。<br>前のしきい値を下回らない。<br>⭐ **導き方は本表の下の注が持つ。<br>** ⚠️ **`LF-1` が間引かないので、日ごとのラベルがそのまま並ぶ幅が要る** —— ⭐ 日と曜日が別の段を持つので、束縛するラベルは `Mon`（3 単位）である（`FR-017`）⭐ **1 画面幅に 2 か月を日の段で出すことを狙う。<br>** ⛔ この導き方は、書き出す絵の寸法（`S-81` ＝ 1600px）を基準環境の幅に使わない。<br>`S-81` は書き出す絵の寸法であって画面幅ではない。<br> ⭐ 基準環境は 表 T-025 の `MC-6` が持ち、1920 × 1080 である。<br>⇒ 1920px から行見出しパネル（`S-79` ＝ 200px）とスクロールバー（18px）を引いた 1702px に 61 日 ⇒ 27.9px/日。<br> ⚠️ **測り方**: 起動直後から Ctrl ＋ ホイールで 1 刻みずつ拡げ、曜日の段が現れた最初の刻みで、隣り合う曜日ラベルの間隔を px/日 とし、（画布 1920 − 行見出しパネル 200 − スクロールバー 18）÷ px/日 を数えた。<br> ⚠️ **保証するのは基準環境である** —— 「1 画面幅」は幅を言わなければ検められない約束だからである。<br> ⭐ **17.5 とするのは、書き出す絵に 2 か月を日の段で入れるためである。<br>** ⛔ 段を決める式が本行の値をそのまま使っていない —— `rulerTierOf` が比べるのは `pxPerDay ÷ (rulerFont ÷ fontMin)` であり、`rulerFont` 14 ／ `fontMin` 12 なので割る数は 1.1667 である。<br>⇒ しきい値 `T` は実際には `T` × 1.1667 px/日 を意味する（22 なら 25.67px/日）。<br> ⭐ **導き方**: 書き出す絵の描画幅は `S-81` 1600 − 行見出しパネル 200 ＝ 1400px。<br>そこに 61 日を入れるには 22.95px/日 が要り、しきい値に直すと 19.67。<br>⛔ さらに倍率の刻みが実測 1.121 倍なので、どの初期倍率からでも届くには1 刻みぶんの余裕が要る ⇒ 22.95 ÷ 1.1667 ÷ 1.121 ＝ 17.55 以下。<br> ⇒ ⭐ **17.5 とした。<br>** ⭐ 実測（出荷ビルド、1920 × 1080）: 日の段が出る最初の刻みは 21.11px/日 になり、書き出した `.svg` に 66.3 日入る。<br>画面には 80.6 日入る。 |
| S-86 | `taskLevelOfDetailReadablePx` | `24` | `fontMin` | 200 | 形状の幅がこれを割る `Task` を描かない。<br>文字が 1 つも入らない幅にしない |
| S-87 | `groupLevelOfDetailBase` | `0.32` | 0.01 | 2 | グループ LOD の初項。<br>`threshold(d) = base × ratio^(d − 2)` |
| S-88 | `groupLevelOfDetailRatio` | `1.875` | 1 超 | 4 | 同上の公比。<br>**1 以下だと深いほど出やすくなり単調性が壊れる** |
| S-89 | `stackSafetyCap` | `255` | — | — | **1 つの `TaskGroup` あたりの段数の安全弁** （**測って決めた値ではない** ）。<br>**数え方と、到達したときの挙動は `FR-003` の `ST-7` が持つ。<br>** これだけの本数が 1 つの行で同時に重なるのは実務では起きないので、届いたときは入力か操作の異常である |

**3 つのしきい値は、目盛のフォントが 12px のときの解である。**  
ラベル幅の概算は `FR-093` に従う（導出時点の `labelCoef` は 0.5）。  
和文の可読下限 12px を当てて導いた。  
⚠️ **毎日刻む段だけは `rulerLabelGap`（`S-135`）を足す** —— 刻みが 1 日で、隣り合うラベルが接するのは日の段と曜日の段だけだからである。  
⚠️ **その 2 段は、両方のうち広いほうの行を、さらに両言語のうち幅の広いほうで導く** —— 表示言語は文書に保存しないので（`FR-038`）、**1 つの値が両方を満たさねばならない。**

| 段階 | ラベル | 単位数 | 必要幅 | 条件 | 解 | 採用 |
| --- | --- | :--: | --: | --- | --: | --: |
| 2 年 ＋ 月 | `2026-01` | 7 | 42px | `30.4 × ppd ≧ 42` | 1.38 | **1.4** |
| 3 ＋ 週 | `01-05` | 5 | 30px | `7 × ppd ≧ 30` | 4.29 | **4.3** |
| 4 ＋ 日曜 | 日 `15` ／ 曜日 `水` ・ `Mon` | 3 | 18px ＋ `S-135` | `1 × ppd ≧ 26` | 26 | **26** |

上の式は既定値を選ぶときの考え方であって判定式ではない。  
固定値であること・判定の仕方・実効フォントサイズの定義・基準とする文字の大きさは `FR-017` が持つ。

「一定数以下なら全部描く」というしきい値は本表に無い。  
禁止と理由は `FR-018` が持つ。

## 8. 保存しないもの

**Type**: SECTION

**表 T-206 — 保存しないもの（本体と、`localStorage` に置く別枠）**

| 行 ID | 値 | 既定 | 保存しない理由 |
| --- | --- | --- | --- |
| S-90 | 予定の端点の掴み代 | バーの上下と、端点の外側に 12px | **掴み領域は読む人のアクセシビリティに属する。<br>** 手が震える人には大きな掴み代が要る。<br>文書が強制してよい値ではない。<br>⚠️ **端点は点なので、上下だけでは面積を持たない** —— 横にも届く。<br>⭐ **届くのはバーの端の外側だけである** （規則は表 T-023d の結びが持つ）—— 端の内側は実績の端点（`S-91`）が持つ。<br>⭐ 外側だけに届くので、`S-49` の下限は横の広がりに縛られない（`S-49` の注） |
| S-91 | 実績の端点の掴み代 | 実績バーの帯と、端点の内側に 12px 🔎 | 同上。<br>⚠️ **「帯」は縦の広がりであり** （縦の鎖は `S-5` が持つ）、**横は別に要る。<br>** ⛔ `S-90` を指さずに自分の数を持つのは、予定と実績で別の掴み代を選べるようにするためである。<br>⭐ **横が届くのは端の内側だけであり、実績バーの半分を超えない** （規則は表 T-023d の結びが持つ）—— ⭐ 掴み代がバーの外へ 1px も出ないので、担当ラベル（表 T-023d の `GR-11`）の端に乗ることがない。 |
| S-92 | フェード掴み点の当たり判定 | 15 × 15px | 同上 |
| S-180 | 実績のダミーを描く幅（表 T-023d の `GR-9` / `GR-17` / `GR-18`） | 30px | `S-138` と同じ理由で保存しない。<br>出ないのは入口ではなくダミーであり、根拠の行は 表 T-076 の `EP-14` である。<br>⭐ **本行が描く幅であり、掴みシロでもある** —— 表 T-023d の結びが当たり判定を `FR-043` の描いた印そのものと定めたので、見える幅と掴める幅は 1 つになった。<br>規則は同表の結びが持つ。<br> ⭐ **本行が定めるのは横だけである** —— 縦の広がりは実績バーの帯に従う（`S-91` が予実の端点について置いた分け方と同じである）。<br>⛔ **掴みシロの大きさを別の値として持たない** —— **掴みシロは印そのものであり、描いた印がそのまま掴める。<br>** ⛔ **測って決めた値ではない** |
| S-196 | 名称ラベルを形状の上へずらす隙間（表 T-012 の `SH-3` / `SH-4`） | 2px 🔎 | ⭐ **予定の図形の上端と、ラベルの下端とのあいだの隙間である** —— ラベルの高さそのものはフォントが決めるので、ここが持つのは隙間だけでよい。<br>⛔ どの形状がこの隙間を使うかは 表 T-012 の「名称ラベルの縦位置」の欄が持ち、本行は持たない。<br> ⚠️ **保存しない理由は `S-138` のそれとは違う** —— `S-138` は「書き出しに入口を描かない」ことを根拠にするが、**名称ラベルは 表 T-076 の `EP-5` により書き出しに入る。<br>** ⭐ 本行を保存しないのは、文書がラベルのアンカー（`PR-13`）を持ち、形状の種類から決まる隙間のほうは持たないからである。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-194 | 2 連カーソルの線の太さ（表 T-029 の `CU-2`） | 1px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **`S-178` を掛ける前の、その線自身の太さである** —— 掛ける規則は 表 T-029a の `DC-8` が 表 T-023c の `SL-8` から引く。<br>⚠️ `S-178` の備考が「2 連カーソルは太さの行そのものを持たない」と書いているのは本行の不在を指しており、本行が埋める。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-186 | プロパティパネルの入力欄の高さの下限（`FR-006`） | 17px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **「高さ」ではなく「下限」である** —— 読む人がブラウザで文字を大きくしたとき、固定の高さは文字を切り落とす（`NFR-007`） |
| S-187 | 色の入力欄の高さの下限（`FR-006`） | 16px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **`S-186` とは別の値である** —— 参考実装が別に持っており、色見本の並びを挟むぶん 1px 低い |
| S-188 | 色見本の一辺と隙間（`FR-006`） | 14 × 3px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⭐ **一辺と隙間の組であって、幅と高さではない** （`S-175` と同じ持ち方）—— **色見本は 14px の正方形** であり、3px はその隣との隙間である⭐ **本行が持つのは見本の寸法だけである** —— **何回描いてよいかは `FR-006` が持つ。<br>** |
| S-189 | 項目名の欄が取る割合（`FR-006`） | 42% 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⭐ **px ではなく割合である** —— パネルの幅は `S-80` で人が動かすので、絶対値で持つと広げたときに入力欄だけが伸びる |
| S-190 | 項目名と入力欄のあいだ（`FR-006`） | 6px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **`S-190` が横で `S-191` が縦である** —— 欄は横に名前と入力が並び、縦に積むので、2 つの隔たりは別の軸にある |
| S-191 | 欄どうしの縦の隔たり（`FR-006`） | 2px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。 |
| S-192 | プロパティパネルの内側の余白（`FR-006`） | 6 × 8px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⭐ **縦・横の組である** |
| S-193 | 複数行の入力欄が見せる行数（`FR-006`） | 2 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **表 T-016 の「入力の型」が `複数行` と記す行だけが使う** （いまは `PR-2` の 1 行） |
| S-197 | プロパティパネルの文字の大きさの係数（`FR-006`） | 0.70 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**—— パネルの文字を `calc(0.7 × ui-font)` で描いており、`ui-font` が 17px のとき **11.9px** であることを実測した。<br>⭐ **px ではなく係数である。<br>** ⛔ 掛ける相手は宿主が与える地の文字の大きさであって、`fontScaleSizes[fontScale]` ではない —— 理由は `FR-006` が持つ。<br>⛔ **参考実装の 11.9px をそのまま書き取ってはならない** —— あれは本仕様が持たない 17px から出た数である |
| S-198 | 項目名だけをさらに小さくする係数（`FR-006`） | 0.90 🔎 | ⭐ 掛ける相手は `S-197` を適用したあとの大きさであって、`fontScaleSizes` ではない —— パネル全体が縮んだうえで、その中でさらに項目名だけが小さい。<br>参考実装では値の欄が 11.9px のとき項目名の欄が **10.71px** で、比はちょうど 0.9 であった（実測）。<br>⚠️ **`S-189`（項目名の欄が取る割合）とは別物である** —— あちらは幅、本行は文字の大きさ。<br>保存しない理由は `S-138` と同じである |
| S-199 | 入力欄が、値のほかに要る幅（`FR-006`。<br>字の大きさに対する倍率） | 2.19× 🔎 | 同上。<br>⛔ **値そのものの幅ではない** —— そちらは `FR-093` の概算である。<br>枠と内側の余白と、宿主が入力の型に応じて付ける入力補助（`date` の暦を開く釦、`number` の上下の釦）が取るぶん である。<br>⛔ **px ではなく倍率で持つ** —— `FR-006` が「px の定数として持ってはならない（MUST NOT）」と定めており （WCAG 2.1 の 1.4.4）、px で足すと、読む人が文字を 2 倍にしたときに要る幅が 2 倍にならず、欄だけが取り残される。<br> ⭐ **参考実装の実測から起こした**—— 値の欄が 11.9px のとき足りないぶんが約 26px で、比は **2.19** 。<br>⛔ **1 つの操作子に何を割るかという規則は本行が持たない。<br>** 規則は `FR-006` にある |
| S-137 | 線の掴み代（表 T-023d の `GR-13` / `GR-16`） | 線の左右に 6px 🔎 | 同上。<br>⛔ **「線の上」は幅を持たない集合であり、許容を与えないと決して当たらない。<br>** ⭐ **依存線（表 T-023d の `GR-13`）は予定バー本体より上に在る** —— その条件（掴み代の縦幅が実績の縦幅の下限より狭いこと）は 表 T-023d の結びが持つ。<br> ⭐ **既定はその条件を満たしている。<br>**⚠️ **基準日線（`GR-16`）は上げない** —— 行の全高を占めるので、同じ条件を満たせない。 |
| S-134 | `Panel Divider` の掴み帯（`FR-051`） | 境界に重なる 8px 🔎 | 同上。<br>**`Row Area` から場所を取らない** （`FR-051` の MUST NOT）ので、境界の線に重ねて敷く。<br>由来は前プロジェクトの PoC の実測（変更履歴の版 0.48） |
| S-135a | `Command Palette` の掴み帯（`FR-053`） | 上端に敷く 24px 🔎 | 同上。<br>パレットの上部に敷く帯であり、表 T-023d の `GR-19` が最優先と定める 。<br>⚠️ **パレットの大きさは中身が決める** （`FR-053`）ので、本値が定めるのは帯の高さだけである。<br>⛔ **測って決めた値ではない** （🔎） |
| S-225 | `Document Title`（`U-27`）の字の大きさ | 16px | **画面と書き出しの両方が本行を読むこと**。<br>規則は 表 T-076 の `EP-1` が持つ（画面と書き出しが本行を読むこと、書き出し専用の定数を持たないこと）。<br>⭐ **値は画面の実測である** （出荷ビルド 1920×1080）—— 画面の見た目を 1px も変えないために、測った値をそのまま行にした。<br>⛔ 書き出しは帯の高さに比例させて字を描いてはならない（MUST NOT）—— 帯の高さ × 0.4 では 14.8px となり、画面より 7.5% 小さい |
| S-226 | `Document Title` の、帯の左端からの余白 | 12px | 同上。<br>⭐ **`S-225` と対である** —— 字の大きさだけを揃えても、左の余白が違えば位置は揃わない。<br>⚠️ 帯の高さ × 0.5 を左の余白にすると 18.5px となり、画面より 6.5px 右へずれる （出荷ビルド 1920×1080 の実測）。<br>⭐ 帯の高さに比例しないのは、帯の高さが `FR-051` の測る値であり、題の置き方を決める値ではないからである。<br>規則は 表 T-076 の `EP-1` が持つ |
| S-138 | 入口の図形を描く箱の一辺（`FR-029`） | 16px 🔎 | **画面の道具の寸法であり、文書の内容ではない** —— 書き出す絵に入口は出ない（表 T-076 の `EP-1` と `EP-4`）ので、渡した相手の絵はこの値で変わらない。<br>⛔ **閲覧者の文字サイズに追随させない** —— 大きくしたい人はブラウザの表示倍率で変える。<br>⚠️ **本値は `S-141` と対である** —— ⭐ 本値 16 と `S-141` の 4 で入口の外形は 26 × 24px となり、図形を大きくしても入口の大きさは変わらない。<br>⛔ **測って決めた値ではない** （🔎） |
| S-218 | 行の掴み代と行の名前のあいだ（表 T-023d の `GR-20`） | 4px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **`FR-085` の式が引く項である**。<br>⛔ **`S-141`（図形と入口の枠の最低隙間）ではない** —— 数が同じだけで、あちらは入口の枠の話であり、掴み代は 表 T-051 の `HF-15` が「押す入口ではなく、掴める場所を指す印」と定めている。 |
| S-140 | 行の操作子に確保する場所（`FR-085`） | 0px 🔎 | 同上。<br>⛔ **描くかどうかで変えてはならない** （`FR-085` の MUST NOT）—— 書き出しは操作子を描かないのに場所を確保しており（表 T-076 の `EP-4`）、ポインタが乗ったときだけ描く形も同じである。<br>⚠️ **打ち切りの位置が動かないことが、この規則の目的である** 。<br>⭐ **0 でもこの MUST NOT は真である** —— 常に 0 なら、描くかどうかで変わっていない。<br>⭐ **目的も保たれる** —— 書き出しも 0 なので、画面と書き出しで打ち切りの位置が一致する。<br>⛔ **本行を消してはならない** —— 消すと `FR-085` の算式が項を 1 つ失う。<br>⚠️ **0 とするのは、`HF-6` により操作子がほとんどの時間どの行にも描かれないからである** —— 0 でなければ、描かれない操作子の幅が行の名前から引かれ続ける。<br> ⭐ **代わりに操作子は名前の上へ重ねる** （表 T-051 の `HF-6`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-141 | 図形と入口の枠の最低隙間（`FR-029`） | 4px | 同上。<br>⛔ **枠の側は動かさない** —— 定めるのは隙間だけである。<br>⚠️ **図形が座標系をどれだけ使うかで見える大きさが変わる** ので、図 F-019 の座標系は図形の外接枠へ詰める（`FR-029`）⭐ **角の R と同じ値である** —— **アイコンの角の R が曲がり始めるところと同じぐらいの余白があれば足りるからである**。<br>⭐ 角の R は **4px** である —— 出荷ビルドの入口は `border-radius:0.25em` を持ち、`font-size` は 16px である（出荷ビルドの入口の CSS を読んで求めた）。 |
| S-171 | プロパティパネルが開いたときに取る幅（`FR-072`） | 280px 🔎 | 同上。<br>⭐ `S-80` は文書が保つ幅であり、面を出しているかどうかは 表 T-206 の `S-99h` が持つ—— 本値は**面を出すときに置く幅** である。<br>⚠️ **人が境界をドラッグした後は `S-80` が勝つ** （`FR-052`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-142 | マイルストーンの図形の一覧を開いているか（`FR-053`） | `false` | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-99e` と同じ性質である。<br>⛔ **`Esc` では閉じない** —— 重ねて開く「面」ではなく、パレットの中の並びが伸びるだけなので、`S-99g` に当たらない |
| S-216 | コマンドパレットに常に出すマイルストーンの図形の数（`FR-053`） | 6 | 同上。<br>⭐ 表 T-012 の `SH-5` が面積順に並べるはじめの 6 つ、すなわち 〇 から ☆ までである。<br>⛔ **残りは `S-142` の一覧を開くまで出さない** —— 規則は `FR-053` が持つ。<br>⚠️ **面積順を持つのは `SH-5` のはじめの 8 つだけである** （`FR-078`）—— あとの 7 つは面積の順を持たないので、常に残りの側に入る |
| S-200 | `Command Palette` を最小化しているか（`FR-053`） | `false` | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-99e` と同じ性質である。<br>⛔ **`S-99e`（表示しているか）とは別である** —— 最小化は出ている状態の一種であり、非表示にすることではない。<br>⛔ **`Esc` では戻らない** —— 重ねて開く「面」ではないので `S-99g` に当たらない |
| S-201 | ヘルプが画面に占める割合（`FR-036`） | 0.95 🔎 | 同上。<br>⭐ **幅と高さの両方に同じ割合を当てる**。<br>⛔ **px で持たない理由は `FR-036` が持つ。<br>** ⚠️ **1 を超えてはならない** —— 画面より大きいヘルプは、閉じる入口ごと外へ出る |
| S-202 | ヘルプの段の数（`FR-036`） | 3 | 同上。<br>⭐ **ヘルプを 3 段組みにして全項目を表示するための値である**。<br>⛔ `FR-036` は「基準環境では 1 画面に収め、スクロールを要さないこと」と定めており、段の数はそれを満たすための割り付けである —— 110 項目を 1 段に積むと基準環境の高さに入らない。<br>⚠️ **これより低い画面ではスクロールを許す** （同要求） |
| S-203 | ヘルプの文字の大きさの係数（`FR-036`） | 0.80 🔎 | ⭐ **ヘルプの字をアイコンの大きさに合わせるための係数である**。<br>⛔ **px で持たない理由は `FR-036` が持つ。<br>** ⚠️ 掛ける相手は宿主の地の文字であり、`fontScaleSizes`（表 T-215）ではない —— ヘルプは日程の絵ではなく、その周りの枠である（`S-197` と同じ立場） |
| S-204 | ツールチップの文字の大きさの係数（`FR-092` の `EZ-2`） | 0.875 🔎 | ⭐ **16pt を 14pt ほどに小さくする比である** （14 ÷ 16）。<br>⛔ **px で持たない理由は `S-203` と同じである。<br>** ⚠️ **「2 段階」は 表 T-215 の段ではない** —— 文字サイズの段のことである |
| S-205 | `Scrollbars` の太さの下限（`FR-051`） | 8px 🔎 | ⛔ **太さそのものではない** —— 太さは環境から確定させる値であり、`FR-051` が設定値として持つことを禁じている。<br>**本行が持つのは「これ以下では指せない・掴めない」という、この道具が決める下限** であって、環境ごとに違う値ではない。<br>⭐ **要る理由** —— 重ね表示のスクロールバーを持つ環境は既定が **0** を返し、その半分は 0 になる。<br>⚠️ **`S-138`（入口の図形の一辺 12px）より細い** —— 帯は目印であって指の的ではなく、掴む的は 表 T-023d の当たり判定が別に持つ |
| S-143 | パレットの群を隔てる線の太さとまわりの空き（`FR-053`） | 1 × 6px | 同上。<br>⭐ **2 つ目の数は上下左右のすべてに当たる** —— ⛔ **上下だけの数を別に持たない。<br>** 罫のまわりを等方にするので、**新しい数を 1 つも起こさない。<br>** ⛔ 左右だけに当ててはならない（MUST NOT）—— 上下が 0px となり、群のボタンが罫に接する。<br>⛔ 線であって文字ではない。<br>理由は `FR-053` が持つ。<br> ⚠️ **図形でもない** ので 表 T-109 の行も 図 F-019 の図形も要らない |
| S-172 | 入口を押し続けたとき、連続に変わるまでの待ち時間（`FR-018`） | 1000ms | 同上。<br>⛔ **1 秒以上の長押しで連続とする。<br>** ⚠️ **次の行の刻みより長いことを `FR-018` が求める** |
| S-173 | 押し続けているあいだ、倍率を刻む間隔（`FR-018`） | 120ms 🔎 | 同上。<br>⛔ **繰り返しの速さは読む人のアクセシビリティに属する** —— 掴み代の行と同じ論であり、文書が強制してよい値ではない。<br>⛔ **測って決めた値ではない** （🔎） |
| S-174 | 選択の枠の太さ（表 T-023c の `SL-8`） | 2px 🔎 | 同上。<br>⛔ **倍率に追随させない** —— 規則と理由は `SL-8` が持つ。<br>⛔ **測って決めた値ではない** （🔎） |
| S-175 | 選択の枠の破線の刻み（表 T-023c の `SL-8`） | 2 × 2px 🔎 | 同上。<br>⭐ **描く長さと空ける長さの組である。<br>** ⛔ **測って決めた値ではない** （🔎） |
| S-178 | 選択された線の太さの倍率（表 T-023c の `SL-8`、表 T-029a の `DC-8`） | 2× 🔎 | 同上。<br>⭐ **絶対値ではなく、その線自身の太さに掛ける倍率である** —— 依存線は `S-18` を持つが、基準日線と 2 連カーソルは太さの行そのものを持たない。<br>⛔ **測って決めた値ではない** （🔎） |
| S-224 | 交差を読ませる縁の太さの倍率（`FR-009`） | 3× 🔎 | 同上。<br>⭐ `S-178` と同じく、その線自身の太さ（依存線なら `S-18`）に掛ける倍率である —— 絶対値ではない。<br>⛔ **1 以下にしてはならない** —— 本線より細い縁は奥の線を途切らず、`FR-009` の「どちらが手前かが読める」を果たせない。<br> ⚠️ **大きくしすぎると、密な図で隣の線を食う。<br>** ⭐ 3 を薦めるのは、依存線の太さの両側に本線 1 本分ずつの地色が出る幅であるからである。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-94 | 取り消しの段数 | 50 | 動いている端末のメモリ予算。<br>**履歴自体を保存しないのに回数だけ文書が指定するのは筋が通らない** |
| S-95 | 取り消しの合計メモリ上限 | `64` MB 🔎 | 同上。<br>**値の由来は無い** —— 動いている端末の予算から選び直す。<br>**1 MB = 1024 × 1024 バイトとする** （`S-113` と同じ） |
| S-96 | `zoomStep` | `S-53` | **操作の速さであって結果ではない。<br>** 1 ノッチで何倍動くかは入力装置に依存する。<br>結果の絵は変わらない |
| S-97 | `zoomMin` | `S-54` | 同上。<br>倍率そのもの（`zoomX` / `zoomY`）は保存するので再現性は損なわれない |
| S-98 | `zoomMax` | `S-55` | 同上 |
| S-99a | 透かしに出す**開いた者の名前** | `user` | 別枠。<br>`localStorage` に置く。<br> 文書に保存しない規則と理由は `FR-020` |
| S-99c | 利用者が設定した**透かし解除パスワードの SHA-256** | — | 別枠。<br>`localStorage` に置く。<br> 保存の規則は `FR-020` と `FR-086` が持つ。<br>設定されていなければ表 T-207 の既定値を使う |
| S-99b | `Agent API` を有効にした文書の記録 | — | 別枠。<br>`localStorage` に置く。<br> `FR-065` が「ブラウザ（オリジン）ごとに記憶する」と定めるので、**オリジンごとに 1 つ** 置く。<br>**有効化は読む人の判断であって文書の内容ではない。<br>** ⚠️ 文書の識別子と対にしては置けない —— その識別子は仕様のどこにも無い |
| S-99 | `language`（`ja` / `en`） | — | 別枠。<br>`localStorage` に置く。<br> 保存しない規則と理由は `FR-038`。<br>⚠️ **表 T-202 に行が無いので `FR-049` の切り替え対象ではない** |
| S-99e | コマンドパレットの表示状態 | 表示 | 画面の使い方であって文書の内容ではない（`FR-053`。<br>切替は表 T-036 の `SK-14`） |
| S-99f | 全画面表示かどうか | 通常 | 同上（`FR-071`。<br>切替は表 T-036 の `SK-15`） |
| S-99g | 開いている面 | 開いていない | 同上。<br>「面」とは、画面の上に重ねて開き、`Esc` の第 1 階層で閉じられるものをいう （表 T-028 の `IN-4`） |
| S-99h | プロパティパネルを出しているか（`選択` ／ `文書全体の設定` ／ 出していない） | 出していない | 同上。<br>⛔ **幅（`S-80`）とは別に持つこと**—— ⚠️ **幅 `0` は「まだ広げていない」であって「出していない」ではない。<br>**`FR-052` は境界のドラッグで幅を 0 まで畳めると定めており、幅で有無を決めると、畳んだ人が次に開くときの幅を失う。<br>⭐ **既定が「出していない」なのは `FR-080` の基準環境である。<br>** ⚠️ **`S-99g` の「面」ではない** —— パネルは画面の上に重ねず、`Esc` の第 1 階層でも閉じない（表 T-028 の `IN-4` が別の段を与えている） |
| S-99i | 対話欄（`_assets/tbl-glossary.md` の `U-44`）を表示しているか（`FR-066`） | 表示 | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-99e` と同じ性質である。<br>⛔ `Agent API` が有効かどうか（`S-99b`）とは別の値として持つこと。<br>1 つの値で兼ねてはならない —— **あちらは能力であり、本行は見え方である。<br>** ⚠️ 兼ねると、`Agent API` を有効にしただけで `IC-18` の押下状態が立つ。<br>⭐ 既定が「表示」なのは、`FR-066` が有効なあいだ欄を表示すると定めているからである —— **有効にした人が、もう一度別の入口を押すまで何も出ない形にしない。<br>** ⚠️ **`S-99g` の「面」ではない** —— **理由は `S-99h` と同じであり、ここでは繰り返さない。<br>** |
| S-144 | 透かしを出すか（`watermarkVisible`） | 出す | ⛔ **文書に保存しない** —— 規則は `FR-020` が持つ。<br>⚠️ **表 T-202（文書の値）ではなく本表に置く** —— **理由は `FR-020` が持つ。<br>** ⭐ 入口は 表 T-109 の `IC-41`。<br>出ているときだけ解除パスワードを求め、戻す側は問わない （`FR-020`）。<br>⭐ **画面から消したときは書き出す絵からも消える。<br>** ⚠️ **`FR-049` の切り替えの対象ではない** —— **同要求が見るのは 表 T-202 の真偽の行である。<br>** |
| S-206 | 操作と描画を記録しているか（`FR-102`） | `false` | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-200` と同じ性質である。<br>⛔ **既定が `false` なのは、記録が読む人の判断で始まるものだからである。<br>** ⚠️ **`Esc` では止まらない** —— 重ねて開く「面」ではないので `S-99g` に当たらない |
| S-207 | 記録に残せる件数の上限（`FR-102`） | 2000 🔎 | 同上。<br>⛔ **上限が無いと、長く回した人の記録が貼れない大きさになる** （`FR-102`）。<br>⚠️ **上限に達したあとの捨て方は `FR-102` が持つ。<br>** ⛔ **測って決めた値ではない** （🔎） |
| S-208 | 掴んだ行の軸が決まる距離（表 T-051 の `HF-15`）と、図形を置くときに押しと引きを分ける距離（`FR-001` / `FR-019`） | 6px 🔎 | `S-138` と同じ理由で保存しない。<br>**この距離を超えて最初に動いた向きで軸が決まり、離すまで変わらない** （表 T-051 の `HF-15`）。<br>⛔ **0 にしてはならない** —— **1 画素の震えで軸が決まってしまう。<br>** ⚠️ **段送りの刻みは本行ではない** —— **刻みは 表 T-201 の `S-37` と同じであること** を `HF-15` が定める。<br>⛔ **刻みを本表に持たせてはならない** —— **同じ数が 2 か所に載り、必ず離れていく** 。<br>⛔ **測って決めた値ではない** （🔎）⭐ **図形を置くときの境目にも使う** —— **同じ手の動きに同じ値を使う。<br>** ⛔ **別に持ってはならない** —— **片方だけ動いたとき、同じ手つきが場所によって違う意味になる。<br>** |
| S-210 | ヘッダーの更新日時の文字の大きさの係数（`FR-101`） | 0.75 🔎 | ⭐ **文字を 2 段小さくする比である**。<br>⛔ **px で持たない理由も、掛ける相手も `S-203` と同じである。<br>** ⭐ **ファイルの名前にも掛かる** —— **係数は名前と日時の両方を抱える箱に宣言され、名前が継ぐ。<br>** ⚠️ 実測（出荷ビルド）: 名前も日時も 12px、`Document Title` は 16px。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-212 | 拒まれた向きへ掴んだ行が追従する割合（表 T-051 の `HF-15`） | 0.4 🔎 | 同上。<br>⭐ **掛ける相手は、その軸の 1 歩ぶんである** —— 左右なら 表 T-201 の `S-37`、上下ならその行が占める送りである。<br>⛔ **0 にしてはならない** —— **まったく動かないと、掴めていないのか拒まれているのかが読めない。<br>** ⛔ **1 にしてはならない** —— **拒んだのに付いて行くので、手応えが返らない。<br>** ⭐ **0.4 を薦めるのは、1 歩の半分に届かないからである** —— **半分を超えると、次の歩へ入ったように見える。<br>** ⛔ **測って決めた値ではない** （🔎） |
| S-213 | 行の辺に引く帯の太さ（表 T-051 の `HF-15` と `HF-18`） | 3px 🔎 | 同上。<br>⭐ **2 つの帯で 1 つの数である** —— `HF-15` が掴んでいる行に描く軸の帯と、`HF-18` が抱えている行の左の辺に引く帯は、**どちらも「行の辺に引く 1 本の帯」であり、太さを別々に持つ理由がない。<br>** ⛔ **2 行にしてはならない** —— **同じ数が 2 か所に載ると、必ず離れていく** （`S-208` が同じ理由で刻みを拒んでいる）。<br>⚠️ **見本は両方とも 3px で描いている** （`previous-project-result/11-row-controls/row-controls-sample.html` の `.row.holds` と `.row.axisV` / `.row.axisH` を実測）。<br>⛔ **罫の色（`S-149`）の 1px では「帯」にならない。<br>** ⛔ **測って決めた値ではない** （🔎） |
| S-214 | 状態を地で薄く示すときの濃さ | 9% 🔎 | 同上。<br>⭐ `FR-029` がポインタの乗った入口に敷く地と、`FR-098` が留めた行に敷く地の、両方が読む —— **1 行である理由は `S-213` が述べたものと同じである。<br>** ⚠️ **掛ける相手までは同じではない** —— 入口の側は 表 T-236 の `S-147`、行の側は同表の `S-151` であり、それぞれの要求が名指す。<br> ⚠️ **見本の実測値である** （`row-controls-sample.html` の `.e:hover` と `.row.pinnedTop`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-215 | 掴んでいる行の地の濃さ（表 T-051 の `HF-15`） | 12% 🔎 | 同上。<br>⭐ **`S-214` より濃い値を別に持つ** —— 留まっているだけの行といま手が持っている行が同じ画面に並ぶので、同じ濃さでは見分けられない。<br> ⚠️ **見本は 9% と 12% で描き分けている** （同書の `.row.dragging`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-211 | 段 0（行見出しパネルの頭）が畳まれているか（表 T-015 の `HR-2`） | 畳まれていない | 同上。<br>⭐ `HR-2` の「押すと行が 1 つも描かれない状態になりうる」を成り立たせるのは本行であり、その理由も、列を増やさないという規則も同行が持つ。<br> ⛔ **保存しない** —— `S-99g` と同じ立場であり、画面の状態であって日程の内容ではない。<br>⭐ **戻す道は 3 つある** —— 表 T-051 の `HF-16`（段 0 を 1 階層開く）と `HF-10`（すべて開く）、そして `HF-17`（最も浅い段へ行を足す）である。<br>⚠️ **`HF-17` が戻す道になるのは、足すときに段 0 を 1 階層だけ開くからである** —— 足した行が見えないまま名づけさせないためである。<br> ⭐ **何階層開くかは `HF-17` が持つ** |
| S-219 | 目盛の曜日の段の文字の大きさの係数（`FR-017`） | 0.6 🔎 | ⭐ **英語の曜日（`Mon` など）が日の段の幅を決めてしまうので、そこだけ縮める**。<br>⛔ **family の差し替えの禁止は 表 T-238 の直下が持つ** —— `FR-039` が読む人の字を尊重すると定めているためでもある。<br>⛔ **画面の道具の寸法であり、文書の内容ではない。<br>** ⭐ **0.6 とするのは、日の段が 21.11px/日 で出るからである** —— `S-85` が 17.5 なので、そこに英語の曜日が入らねばならない。<br> ⭐ 実測（出荷ビルド、表示言語 en）: `Wed` の幅 17.7px（字 8.4px）、隙間 3.4px、重ならない。<br> ⛔ **0.8 では 22.6px で入らない。<br>** ⭐ **暫定の値である** —— 日本語（水 木 金…）と英語（Wed Thu Fri…）の両方を出荷ビルドで見て、読めることを確かめた。<br> ⚠️ **暫定なので、狭い画面や高い画素密度で読みにくいと分かれば動かしてよい。<br>** |
| S-229 | 見えている範囲に残す日数の下限（`FR-016` の `zoomX` の上限） | 10 | ⭐ **最大で 10 日間まで拡大できる値である。<br>** ⛔ **`src/` に 10 を打ち込んではならない。<br>** ⭐ **上限そのものは倍率ではなく式である** —— `Row Area` の幅 ÷（本行 × 等倍のときの 1 日の幅）。<br>⇒ **画面の広さが変わっても「10 日は見えている」という約束が保たれる。<br>** ⚠️ **画面の道具の寸法であり、文書の内容ではない。<br>** ⛔ **測って決めた値ではない** |

**表 T-207 — 透かし（成果物に埋め込む定数、文書には保存しない）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-100 | 既定の透かし解除パスワード | `goodrelax-scheduler` | — | — | 保存の規則は `FR-020` が持つ。<br>ここに書くのは仕様として値を定めるためであり、成果物へ入るのは下の SHA-256 だけである。<br>⚠️ **汎用の道具に固有名を既定値として置かないため、既定値は製品名とする。<br>** |
| S-101 | 既定の透かし解除パスワードの SHA-256 | `e2b7f98dfe8145444b33263989fe5e47f9150fe1ef6460713268af974e6df134` | — | — | UTF-8・末尾の改行を含めない。<br>**成果物へ定数として埋め込むのはこの値である** |
| S-102 | `watermarkOpacity` | `0.06` 🔎 | 0.02 | 0.30 | 透かしの濃さ。<br>**コントラストの適合範囲から外してある** （`NFR-007` / `LM-13`）ので、可読性ではなく「よく見れば分かるが通常は気にならない」を狙う値である。<br>⭐ **薄いほうがよい、が狙いである**。<br>⛔ その規則は `FR-020` が持つ。<br>ここに書き写さない。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-220 | 透かしを斜めに刷る角度 | `-30` | `-90` | `90` | 単位は度（`FR-020`）。<br>⭐ 右上がり。<br>日程のバーは横に伸びるので、水平から充分離れ、かつ読める。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-221 | 透かしの文字の大きさの係数 | `0.01125` | `0.01` | `1` | 掛ける相手は書き出す絵の幅（`S-81`）である（`FR-020`）。<br>⭐ **画面の大きさが変わっても割合が保たれる。<br>** ⭐ **参考の実装** （`temp/gr-scheduler_current_io.html`。<br>リポジトリの追跡外）は、字 18px・濃さ 0.06・色 `#888888`・間隔 260px であった —— 札の幅がおよそ 243px なので、間隔のほうが広い。<br>だから重ならない。<br> ⇒ **参考の 18px を書き出す絵の幅 1600 で割って割合にした。<br>** ⛔ **絶対の px にはしていない** —— 本行の「画面の大きさが変わっても割合が保たれる」という理由が失われるからである。<br> ⭐ 実測（出荷ビルド、1920 × 1080）: 画面で 21.6px、書き出す絵で 18px。 |
| S-222 | 透かしの繰り返しの間隔の係数 | `14.44` | `1` | `20` | 掛ける相手は透かしの文字の高さである（`FR-020`）。<br>縦横とも同じ間隔とする。<br>⭐ **14.44 は、参考の 260px ÷ 18px から出した。<br>** ⛔ **3 では重なる** —— 実測（`S-221` が 0.08、本値が 3 のとき）: 字 153.6px に対し札の幅がおよそ 2000px、間隔 460.8px で、横に 4〜5 重に重なり「誰が・いつ」が読めなかった。<br> ⭐ 実測（本値 14.44 のとき）: 札の幅 282px、間隔 312px。<br>重ならない。 |

**表 T-236 — 画面の色（成果物に埋め込む定数、文書には保存しない）**

| 行 ID | 色 | 明るいテーマ | 暗いテーマ | 色相追随 | 備考 |
| --- | --- | --- | --- | :--: | --- |
| S-146 | 地の色 | `#ffffff` | `hsl(H 12% 9%)` | ○ | 地。<br>⛔ **塗らないと OS の既定が出る** —— 暗いテーマを選んでも白いままになる |
| S-147 | 文字の色 | `#16181d` | `#e8eaee` | — | 主たる文字 |
| S-148 | 控えめな文字の色 | `#5b6068` | `#9aa1ab` | — | 副次の文字 |
| S-149 | 罫の色 | `hsl(H 14% 87%)` | `hsl(H 12% 23%)` | ○ | 区切りの線 |
| S-150 | パネルの地の色 | `hsl(H 20% 97%)` | `hsl(H 14% 13%)` | ○ | 行見出しパネル・プロパティパネル・パレットの地 |
| S-151 | 強調の色 | `hsl(H 59% 32%)` | `hsl(H 62% 68%)` | ○ | 選択と現在位置。<br>⭐ **明るいテーマの明度が 32% なのは、42% では `NFR-007` の 3 : 1 を割る色相が在るからである。<br>**⭐ **42% のとき、全 360 色相を 3 つの地に当てて実測した**: 地（`S-146`）に対し **107/360** 、パネルの地（`S-150`）に対し **139/360** 、**予定バーの塗り（`S-155`）に対し 179/360（49.7%）** が割れ、最悪は色相 60 の **1.88** 。<br>⭐ **32% では 3 つの地すべて・全 360 色相で最悪 3.11** となる。<br>⛔ **彩度は動かしていない** —— 明度だけで足りるので、動かす値を 1 つに留めた。<br>⚠️ **暗いテーマは 1 文字も変えていない** —— 同じ測り方で最悪 3.53 であり、元から満たしている |
| S-152 | 良の色 | `#1f7a3d` | `#6fc98d` | — | 順調 |
| S-183 | 構えている入口の塗りの色（`FR-053`） | `#1f7a3d` | `#6fc98d` | — | 構えている入口の図形の箱に敷く（`FR-029` の 表 T-237 の `EN-1`）。<br>⛔ **縁ではなく塗りである** —— ⚠️ **図形の箱を塗る色であって、縁を描く色ではない** —— 「縁の色」と読むと、忠実な実装は縁を描く。<br> ⭐ **`S-152` と同じ対を使う** —— 緑は「いま効いている」を表す色として本表が既に持っている。<br>⛔ **一旦の色である**。<br>⚠️ **色相に追随させない** —— 追随させると、`FR-041` が主題の色を動かしたときに構えの印が地に紛れる |
| S-195 | 2 連カーソルの色 | `hsl(H 59% 32%)` | `hsl(H 62% 68%)` | ○ | 2 本とも同じ色である —— どちらが追従中かは色で示さない（表 T-029a の `DC-8` が 表 T-023c の `SL-8` を引き、同行は線について太さだけを変えると定める）。<br>⭐ **`S-151` を継ぐ** —— 本表が `S-151` を選択と現在位置の色としており、`CU-2` は読む人が置いた現在位置である。<br>⛔ **明度は `S-151` と同じ 32% である**。<br> 42% では `NFR-007` の 3 : 1 を割る色相が在る。<br>⛔ **`S-163` を継がない** —— 基準日線と 2 連カーソルは同時に出るので、同じ色だとどちらの線か読めない |
| S-153 | 注意の色 | `#a8600f` | `#e0a353` | — | 注意 |
| S-154 | 不良の色 | `#a02b2b` | `#e07a7a` | — | 遅れ |
| S-155 | 予定バーの塗り | `hsl(H 46% 80%)` | `hsl(H 32% 26%)` | ○ | 予定 |
| S-156 | 予定バーの線 | `hsl(H 44% 46%)` | `hsl(H 46% 66%)` | ○ | 予定の縁 |
| S-157 | 実績バーの塗り | `hsl(H 62% 34%)` | `hsl(H 62% 64%)` | ○ | 実績 |
| S-158 | 実績バーの線 | `hsl(H 66% 22%)` | `hsl(H 70% 80%)` | ○ | 実績の縁 |
| S-159 | 依存線の色 | `hsl(26 88% 44%)` | `hsl(30 92% 60%)` | — | ⛔ **テーマの色相に追随させない** （`FR-041`）。<br>⚠️ 明暗で色相が 26 → 30 へ動くのは実物がそうだからであり、理由は記録されていない |
| S-160 | イナズマ線の色 | `hsl(354 62% 42%)` | `hsl(354 70% 64%)` | — | ⛔ **テーマの色相に追随させない** （`FR-041`）。<br>⭐ **依存線とは別の色である** |
| S-161 | 進捗マーカーの文字色 | `#16181d` | `#e8eaee` | — | マーカーの記号 |
| S-162 | 進捗マーカーの地の色 | `S-146` に同じ | `S-146` に同じ | — | 同上。<br>⭐ **`S-146` を継ぐ** —— マーカーは地の上に浮くので、地と同じでないと記号の抜きが濁る |
| S-163 | 基準日線の色 | `#8b9099` | `#767c86` | — | 基準日 |
| S-164 | 行の帯の色 | `hsl(H 42% 96%)` | `hsl(H 18% 20%)` | ○ | ⛔ **いまは 1 本も描かれていない** （`FR-042`） |
| S-165 | 行の帯の縁の色 | `hsl(H 34% 88%)` | `hsl(H 16% 28%)` | ○ | 帯の縁 |
| S-166 | 最上位の行の地の色 | `hsl(H 40% 97%)` | `hsl(H 20% 17%)` | ○ | 深さ 1 の行 |
| S-167 | 交互の行の地の色 | `hsl(H 20% 99%)` | `hsl(H 14% 11%)` | ○ | 縞 |
| S-168 | ラベルの文字色 | `#000000` | `#ffffff` | — | バーの上の文字 |
| S-169 | ラベルの縁取りの色 | `S-146` に同じ | `S-146` に同じ | — | 同上。<br>⭐ **`S-146` を継ぐ** —— 縁取りはバーの色から文字を切り離すためのものなので、地と同じ色でなければ役に立たない。<br>太さは 表 T-201 の `S-34` |
| S-170 | 浮いた層の影 | `rgba(0,0,0,0.28)` | `rgba(0,0,0,0.6)` | — | パレットと面。<br>⚠️ **暗い側で濃くする** —— 明るい側の影は暗い地に溶けて層が浮かない |
| S-223 | 透かしのインクの色 | `S-148` に同じ | `S-148` に同じ | — | 透かしの文字を塗る色（`FR-020`）。<br>⭐ **読む字と同じ色を `S-102` の濃さで薄く重ねる** —— 別の色を与えると、薄いところで色味だけが残り、地と喧嘩する。<br>⭐ `S-148`（控えめな文字）を使い、`S-147`（主たる文字）は使わない —— 透かしのコントラストを小さくするためである。<br>`S-148` は明暗の両方を持ち、`S-147` と同じ色系統なので、上の「別の色を与えると」に当たらない。<br> ⛔ **参考の実装のように固定の灰（`#888888`）にはしていない** —— **暗い主題で地と近づきすぎるからである。<br>** ⭐ 実測（明るい主題）: `rgb(91, 96, 104)`。<br> ⛔ **濃さをここに書いてはならない** —— 表 T-207 の `S-102` が持つ |

## 9. 後から選び直す値

**Type**: SECTION

**本節の表は、§1 の判定では文書に保存する側に落ちるものを含む。**  
§8 と分けてあるのは保存の可否ではなく、**由来が実測でなく、骨格で測って選び直す対象である** という括りだからである（表 T-026 の `RC-1`）。
各表の中で保存の可否を明記する。

**表 T-212 — 画面の各部の寸法の上限（`FR-051`）と、待ち時間**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-124 | `iconHintDelayMs` | `2000` ms 🔎 | 500 | 10000 | **説明を出すまでの待ち時間** （`FR-092` の `EZ-2`（アイコン）と `EZ-6`（タスク））。<br>⚠️ **2000 は、ポインタを当ててから 2 秒で説明を出すための値である**。<br>⚠️ **値は 1 つである** —— 場所ごとに違う待ち時間を持たない（`EZ-6` の MUST NOT）。<br>⚠️ **鍵の綴りは据え置く** —— 鍵は文書に載るので、綴りを変えると既存の文書の往復が壊れる |
| S-116 | `appHeaderMaxHeight` | `56` px 🔎 | 32 | 96 | **上部に使った高さはそのまま日程表から引かれる。<br>** 「視認できる範囲で最小」では合否を判定できないので、上限を数値で置く |

**表 T-211 — 受け入れの上限（`FR-023`）と構造の上限（`FR-004`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-113 | `importMaxBytes` | `32` MB 🔎 | 1 | 256 | 取り込むファイルの上限（`FR-023`）。<br>**1 MB = 1024 × 1024 バイトとする。<br>** |
| S-114 | `importMaxItems` | `20000` 🔎 | 1000 | 200000 | 取り込む `Task` の件数の上限。<br>**目標規模（表 T-025 の MC-7 は 1000）の 20 倍** 。<br>性能の目標より広く受理し、資源が尽きる手前で止める |
| S-115 | `importMaxDepth` | `64` 🔎 | 8 | 256 | WBS のネストの深さの上限。<br>**`maxGroupDepth`（`S-125`）とは別物** （WBS はクランプしない）。<br>**根の行を深さ 1 と数える** —— `wbsParentUid` が `null` の `Task` が深さ 1 である |
| S-125 | `maxGroupDepth` | `5` 🔎 | 3 | 8 | `TaskGroup` の深さの上限。<br>**推奨の深さは 3。<br>** 規則（上限を課すこと・LOD の判定でのみ頭打ちにしてよいこと）と理由は `FR-004`。<br>**根の行を深さ 1 と数える** （`S-115` と同じ）—— `parentId` が `null` の `TaskGroup` が深さ 1 である |
| S-133 | `carryMaxDepth` | `16` 🔎 | 4 | 64 | 解釈しない要素（`AT-126` の `children`）の入れ子の深さの上限。<br>⚠️ **`importMaxDepth`（`S-115`）とは別物** —— あちらは WBS であり、こちらは交換相手が運んできた解釈しない木である。<br>**根の要素を深さ 1 と数える** （`S-115` と同じ）。<br>由来の無い値であり、実測で選び直す |
| S-145 | 既定のファイル名 | `'grs-default.json'` | — | — | まだ 1 度もファイルへ保存していない文書が名乗る名。<br>**表 T-227 の `DI-1` が使えるようにするために要る** —— ⛔ **`DI-1` はファイル名を含み、名の無い文書では 3 つ組が揃わない。<br>** ⚠️ **未保存の文書どうしは同じ名になるので、`DI-1` は互いを同じ文書と答える** |

⚠️ 上限を超えた入力の扱いと、循環をこの上限では検出できないことは `FR-023` が定める。

**表 T-213 — 依存のラグ（`FR-009`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-117 | `dependencyLagDefault` | `0` | — | — | 依存を作ったときに置く値。<br>**0 は「間を空けない」** |
| S-118 | ラグの単位 | **稼働日** 🔎 | — | — | `FR-011` / `FR-012` / `FR-054` が使う単位に揃える。<br>**単位を定めないと、同じ値で実装ごとに違う日数になる** |

⚠️ **ラグが日付を動かさないことは表 T-018 が定める。**

**表 T-214 — 受け入れる日付の範囲（`FR-023`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-119 | `importMinDate` | `1970-01-01` 🔎 | — | — | これより前の日付を持つ入力を受け付けない |
| S-120 | `importMaxDate` | `2200-12-31` 🔎 | — | — | これより後を受け付けない。<br>**231 年ぶんの期間でも稼働日の数え上げが計算量に効く** （件数の上限とは別の経路） |

**表 T-215 — 文字サイズ（`FR-039` の `fontScale`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-121 | `fontScaleSizes.S` | `12` px 🔎 | `fontMin` | `fontScaleSizes.M` | **PoC の `fontMin` = 12 をそのまま S に据えた。<br>** 下限を割らない |
| S-122 | `fontScaleSizes.M` | `14` px 🔎 | `fontScaleSizes.S` | `fontScaleSizes.L` | |
| S-123 | `fontScaleSizes.L` | `16` px 🔎 | `fontScaleSizes.M` | 40 | |

⚠️ `fontScale` は読む人が文字を大きくするための設定であり（WCAG 1.4.4）、下限を割るためのものではない。  
公開実装は `S = 10` を持つが、これは `S-8`（`fontMin` の下限 12）と `FR-077` に反するので採らない。
⚠️ `fontScale` は目盛の文字（`S-3`）と目盛の帯の高さ（`S-2`）にも掛かるので、時間軸のしきい値の判定にも効く。  
判定の仕方は表 T-205 の注が持つ。

**表 T-210 — フェードの掴み点（`FR-075`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-109 | `fadeHandleHalfPx` | `4.5` px 🔎 | 3 | 8 | 掴み点の半辺。<br>**正方形 9 × 9px を点の中心に置く** |
| S-110 | `fadeHandleStrokePx` | `1.5` px 🔎 | 1 | 3 | 掴み点の枠線 |
| S-111 | 掴み点を出す条件 | 選択中のタスクだけ 🔎 | — | — | 出す条件の規則と理由は `FR-075` |

⚠️ **当たり判定の大きさは `S-92`（15 × 15px）が別に持つ。**  
見た目の点（9 × 9px）より広く取る —— 掴み代は読む人のアクセシビリティに属する（理由の欄は 表 T-206 の `S-90`）。

**表 T-209 — 既定の暦と期間の換算（`FR-054`、取り込んでいない文書、および、取り込んだが値が空の文書が使う）**

| 行 ID | 名前 | 値 | 備考 |
| --- | --- | --- | --- |
| S-106 | 稼働する曜日 | 月・火・水・木・金 🔎 | **土日を非稼働とする。<br>** 特定の業種や地域を前提にしない範囲で最も広く使われる形である |
| S-107 | 例外日（休業日） | **無し** 🔎 | **祝日を持たない** （理由は `FR-088`）。<br>必要なら MSPDI を取り込むか、非稼働日として個別に置く |
| S-108 | 週の始まり | 月曜 🔎 | 週の目盛が書く日付の基準（`FR-054`）。<br>置き場は `Project.weekStartDay` |
| S-128 | 1 日あたりの分数 | `480` 🔎 | `Project.minutesPerDay` が空のときに使う（`FR-054`）。<br>1 日 8 時間。<br>選び方は `S-106` と同じである |

⚠️ 保存の可否: 保存する。  
規則と理由は `FR-054` が持つ。

⚠️ **本表の値は日程データの群に入る** —— 収まる先が暦（`Calendar`）と `Project` の列だからである。  
**どちらの群に属するかの判定は表 T-052 が持つ** （表 T-216 と同じ扱いである）。

**表 T-208 — 予実の補助線（`FR-084`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-103 | `planActualGuideWeight` | `1` px 🔎 | 0.5 | 2 | バーより目立つと、事実（バー）より補いの線が前に出るので細くする |
| S-104 | `planActualGuidePattern` | `2,2` 🔎 | — | — | 破線の刻み（描く長さ, 空ける長さ）。<br>刻みの規則は表 T-020a の `GD-6` |
| S-105 | `planActualGuideColor` | 実績バーの色に追随 🔎 | — | — | **実績から伸びる線なので実績の色を継ぐ。<br>** 独立した色を保存しない —— 派生する色を保存しない規則は `FR-041` が持つ |

⚠️ **ズームに追随させない規則は表 T-020a の `GD-6` が持つ。**  
依存線と同じ扱いである（`FR-079`）。

**既定値を本書に置くのは、仕様として値を定めるためである。**  
秘密として機能しないことの議論は表 T-004 の `LM-12` が持つ。
**この機能は将来サーバーと接続したときに意味を持つ** ものであり、現段階では動作を示せればよい（`LM-12`）。

**表 T-217 — 注記の既定値**

| 行 ID | キー | 型 | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --- | --- | --- | --- | --- |
| S-132 | `cornerRadiusPx` | 数値 | `4` 🔎 | 0 | 24 | ハイライトボックスの角の丸み（`AT-122`）。<br>ズームによらず一定に描く規則は `FR-019` が持つ。<br>上限は、枠が角丸に食われて囲んだ範囲が読めなくなる手前 |

⚠️ **本表の値は日程データの群に入る** —— 収まる先が `HighlightBox` の列だからである。  
**どちらの群に属するかの判定は表 T-052 が持つ** （表 T-209 と同じ扱いである）。
