# 設定値 — 既定値と範囲の全数

**UID**: DOC-TBL-SETTINGS
**Version**: 0.1

> ⛔ 本書は生成物である。  
> 手で直さない —— 直しても次の `npm run gen` で消える。
> **設定値の唯一の正は `_source/settings.json` である。**  
> 本書はそれを `_source/settings_json_to_md.py` が印字したものである。
> **作り直す**: `npm run gen` ／ **ズレを検出する**: `npm run gen:check`（検査 16 が呼ぶ）。

**設定値の正は `_source/settings.json` が持つ。**  
名前の正は `tbl-glossary.md` の表 T-104 が持ち、**本書は値** （既定値・下限・上限・範囲の理由）を印字する。  
**名前と値を 2 か所で管理しない。**

由来は前プロジェクトの設定値確定版（`previous-project-result/02-data-model/grs-document-settings-ja.md`）である。

## 1. 保存するかどうかの判定

**Type**: SECTION

**判定は 2 段である。**

> **第 1 段 —— その値を変えると、画面または出力の絵が変わるか。**
> **第 2 段 —— その値は文書の内容か、読む人の環境か。**
>
> ⚠️ **第 3 の枠がある** —— 保存値を**初期値** として持ち、読む人の指定が上書きするもの（`fontScale` / `themePreference`。  
> 規則は `FR-039`）。

第 1 段で絵が変わり、**かつ** 第 2 段で文書の内容であるものだけを文書が持つ（`documentSettings`）。  
第 1 段で変わらないものは持たない。  
絵が変わっても読む人の環境に属するものは持たない。  
その全数と、環境に属すると判断した理由は表 T-206 が持つ。

**同じ JSON からは同じ絵が出る** ことが第 1 段の目的である。  
⚠️ **第 2 段で外したものは、この保証の対象に入らない。**

**書き出しの規則と理由は `FR-024` が持つ** （常に全項目を書き出す）。

## 2. 由来の無い既定値

**Type**: SECTION

**🔎 を付けた既定値は、由来が記録されていない。**  
測って決めた値ではなく、実装時に置いて動いたのでそのまま残った値である。  
本プロジェクトで選び直してよい。  
むしろ選び直すべきである。

印が要る理由は、全項目を書き出す方針の帰結である。  
凍結する以上、由来の無い値も永久に残る。  
どれが測った値でどれが置いただけの値かを区別できないと、次の担当者は全部を等しく尊重してしまい、直す機会を永久に失う。

「範囲の理由」列は範囲（下限・上限）の根拠であって、既定値の根拠ではない。  
両者は別である。

**⛔ は文書に保存しない** （表 T-206）。

**根は 4 つである** （`basePlanHeight` / `actualMin` / `fontMin` / `pxPerDayAt1x`）。  
残りはそこから落ちてくる。  
**進捗マーカーの径（`S-22`）だけは鎖の外の固定値である** —— 規則と理由は `FR-094` が持つ。

## 3. 描画の設定

**Type**: SECTION

**表 T-201 — 描画の設定**

| 行 ID | 群 | キー | 単位 | 既定値 | 下限 | 上限 | 範囲の理由 |
| --- | --- | --- | :--: | --: | --: | --: | --- |
| S-1 | タイムルーラー | `pxPerDayAt1x` | px | 6 🔎 | 0.5 | 60 | 基準の環境で 1 年がおよそ 2200px になる値だが、そう選んだ記録は無い |
| S-2 | タイムルーラー | `rulerHeight` | px | `rulerFont` × 3 + `rulerLabelPad` × 3（S = 60 / M = 69 / L = 78）🔎 | `rulerFont` × 3 + `rulerLabelPad` × 3 | 150 | 段階 4 は 3 段（年 ＋ 月 / 日 / 曜日）。<br>3 段ぶんの文字と余白が入る高さで、余白は `S-136` が持つ。<br>追随の規則と、キーを独立に保つことは `FR-039` が持つ |
| S-3 | タイムルーラー | `rulerFont` | px | `fontScaleSizes[fontScale]` × 1.5（S = 18 / M = 21 / L = 24）🔎 | `fontMin` | (`rulerHeight` − `rulerLabelPad` × 3) ÷ 3 | 和文の可読下限を割らない。<br>帯の 3 段に収まる。<br>`fontScale`（表 T-215）に追随する。<br>追随の規則と、キーを独立に保つことは `FR-039` が持つ。<br>⭐ 文字サイズの段に掛ける 1.5 は、利用者が日付の字を 1.5 倍に大きくするよう指定した試す値である —— 描いた絵を見て選び直す。<br>⚠️ 目盛の帯の高さ（`S-2`）は本行から導くので、1.5 倍の字の 3 段ぶんの高さに立つ |
| S-135 | タイムルーラー | `rulerLabelGap` | px | 2 🔎 | 0 | 30 | 目盛ラベルどうしの隙間（横）。<br>この隙間を足して導くのは 表 T-205 の `S-85` だけである —— 刻みが 1 日の段でしか隣り合うラベルが接しない。<br>⭐ **2 とするのは、実測の隙間が 3.4px であり、8 では実物と合わないからである（出荷ビルド、表示言語 en で隣り合う曜日ラベルの隙間を測った）。<br>** ⛔ `labelGap`（`S-32`）はタスク名を進捗マーカーの右端から離す量であり、目盛には当てない。<br> 上限は `S-32` と同じとし、由来は無い |
| S-136 | タイムルーラー | `rulerLabelPad` | px | 2 🔎 | 0 | 20 | 罫線と目盛ラベルの余白（縦）。<br>帯の高さ（`S-2`）はこれを目盛の帯の中の段（表 T-006b の `A-1` の ⑤）3 つぶん含む。<br> 上限は、`rulerFont` が下限（`S-8`）のときでも 3 段ぶんが `S-2` の上限に収まる値 |
| S-179 | タイムルーラー | `rulerLabelBottomPad` | px | 3 | 0 | `rulerFont` | 目盛ラベルの下側の余白（縦）。<br>⛔ **帯の高さ（`S-2`）はこれを含まない** —— 段の高さは `rulerFont` と `rulerLabelPad` のままで、この余白は文字の箱の中から取る。<br>上限が `rulerFont` なのは、超えるとベースラインが段の上の罫線より上へ出るためである |
| S-4 | 縦の寸法 | `basePlanHeight` | px | 28 | `actualMin` ÷ `actualOfPlan` | 200 | 等倍がこの値だと縦に縮める余地が無くなる |
| S-5 | 縦の寸法 | `actualOfPlan` | — | 0.5715 | 0.05 | 1 未満 | 実績 < 予定。<br>既定は `S-6` の下限 16px ÷ `S-4` の既定 28px ＝ 0.571428… を、小数第 4 位へ切り上げた値である （下記）。<br>⛔ **切り下げてはならない** —— `S-4` の下限は `actualMin` ÷ `actualOfPlan` であり （Chapter 6.1 の 表 T-220 の `IV-16`）、切り下げると `S-4` の既定 28 がみずからの下限を割る。<br>`S-4` の下限（16 ÷ 本値）は、0.5714 では 28.0014、0.5715 では 27.9965 である。<br> ⭐ 既定で立つ姿は 予定 28px ⊃ 実績 16.002px、予定だけの縁は上下 5.999px である —— 表 T-023d の結びが定める縦の入れ子を、既定値のまま満たしている。<br>⭐ **この寸法は表示の倍率の描く比を掛ける前の値である** —— 描く比は予定と実績の縦幅の両方に掛かる（`FR-039` の 表 T-252 の `DS-1`）ので、入れ子は倍率に依らない。<br>⛔ **線の掴み代（表 T-206 の `S-137`）はこの入れ子に入らない** —— 表示の倍率を掛けない（`DS-7`）ので、描いたバーの帯より広くなりうる。<br>描いた予定と実績の形状の上で依存線が何を取るかは 表 T-023d の結びが持つ。 |
| S-6 | 縦の寸法 | `actualMin` | px | 16 | `fontMin` ÷ `fontOfActual` | 80 | これを割ると文字が下限に張り付く |
| S-7 | 縦の寸法 | `fontOfActual` | — | 0.90 | 0.05 | 1 未満 | フォント < 実績。<br>⭐ 0.90 は、利用者が字を大きくするよう求めて置いた試す値である —— 表示の倍率の既定（`S-234`）を上げるのに加えて、実績の縦幅に対する字の比を上げる。<br>描いた絵を見て選び直す。<br>⚠️ `S-6` の下限（`fontMin` ÷ 本値）は本値を読む —— 12 ÷ 0.90 ＝ 13.33…px であり、`S-6` の既定 16px はそれを割らない。<br>⛔ 測って決めた値ではない |
| S-8 | 縦の寸法 | `fontMin` | px | 12 | 12 | 40 | 和文がこれを割ると読めない |
| S-9 | 縦の寸法 | `thinFontScale` | — | 0.85 🔎 | 0.3 | 1 | **この倍率を掛けた後も可読の下限（`S-8`）を割らないこと。<br>** 割るときの扱いは `FR-077` が持つ（倍率ではなく行の量を減らす） |
| S-10 | 縦の寸法 | `actualGap` | px | 2 🔎 | 0 | 20 | `--->` と端点スパンの、予定の線の下の縁から実績の線の上の縁までの隔たりである（表 T-271 の `XS-6`）。<br>⭐ 描いた線どうしで測る —— 段の中央からではない。<br>0 にすれば 2 本の線は接する。<br>⚠️ 矩形と菱形は帯が入れ子なので、本行を読まない |
| S-11 | 縦の寸法 | `stackGap` | px | 1 | 1 | 1 | 固定。<br>形と形の縦の隙間（`FR-094` の 表 T-259）のうち、依存線の上と下に 1 つずつ空ける画面の px である —— 隙間は 本値 ＋ 描いた依存線の太さ ＋ 本値 である（同表の `VG-2`）。<br>⭐ 利用者が「1pxの隙間」と指定した値である。<br>⚠️ 表示の倍率を掛けない（`FR-039` の 表 T-252 の `DS-10`） —— 同じ群のほかの px の行と違う |
| S-12 | 縦の寸法 | `rowGap` | px | 0 | 0 | 0 | 固定。<br>行の帯と帯のあいだに隙間を置かない —— 帯は中身と操作子の格子の床（表 T-051 の `HF-19`）で既に高さを持ち、隙間はその下に何も描かれない帯を 1 本足すだけである。<br>⭐ 利用者が指定した値である。<br>⚠️ 読み込んだ文書が 0 以外を持つときは、黙って 0 として読む —— 表 T-233 の `RS-51` で告げない（同行が本行を対象から外す） |
| S-13 | 形状の縦幅 | `shapeHeightOf.rectangle` | × | 1.0 | 1 | 1 | 基準なので 1 固定 |
| S-14 | 形状の縦幅 | `shapeHeightOf.chevron` | × | 1.0 🔎 | 0.2 | 3 | 矩形と同じ |
| S-15 | 形状の縦幅 | `shapeHeightOf.arrow` | × | 0.5 | 0.1 | 1 未満 | 細線は矩形より薄い（それが取り柄） |
| S-16 | 形状の縦幅 | `shapeHeightOf.endpointSpan` | × | 0.5 | 0.1 | 1 未満 | 同上 |
| S-17 | 形状の縦幅 | `shapeHeightOf.milestone` | × | 1.0 | 0.1 | 1 | 矩形の縦幅を超えない。<br>⭐ 上限 1 と既定 1.0 は、利用者が「マイルストーンの縦幅の最大値はタスクのレクタングルの高さと同じ」と指定した値である。<br>⭐ 名前の字も矩形の名前の字と同じ大きさになる —— 名前の字の求め方は、予定の縦幅 28 × 本値 × `S-5` 0.5715 × `S-7` 0.90 ＝ 14.40…px（`zoomY` が 1 で、表示の倍率の描く比を掛ける前）であり、矩形の 28 × 1.0 × 0.5715 × 0.90 と等しい。<br>⚠️ 下限 0.1 は、矩形より薄い形状の `S-15` / `S-16` と揃えた値であり、利用者が選んだ値ではない |
| S-18 | 依存線 | `dependencyWidth` | px | 2.4 | 0.5 | `dependencyArrowWidth` ÷ 2 | 矢じりの幅の半分を超えない —— 矢じりの付け根が線の 2 倍より細くならないためである。<br>⭐ 既定の 2.4 は、表示の倍率 100 で描くと 1.5px になる（`S-236` の描く比 0.625 を掛ける）。<br>⚠️ 形と形の縦の隙間は描いた本値の太さのぶん広がる（`FR-094` の 表 T-259 の `VG-2`）ので、隙間の側から上限を置かない |
| S-19 | 依存線 | `dependencyArrowLength` | px | 9.6 | `dependencyWidth` × 2 | 40 | 依存線の矢じりの長さ（三角の高さ）だけを定める。<br>⭐ 既定の 9.6 は、表示の倍率 100 で描くと 6px になる。<br>⛔ かつて本行が兼ねていた 2 つの役は、それぞれ自分の行を持つ —— 三角の底辺は `dependencyArrowWidth`、依存線の走りは `dependencyLeadOut` と `dependencyLeadIn` である。<br>下限が線の太さの 2 倍なのは、線より太く見える三角にしないためである |
| S-298 | 依存線 | `dependencyLeadOut` | px | 9.6 | 0 | 40 | 出口の辺から、経路が最初に折れるまでの走りである（表 T-222 の前書き）。<br>⛔ 入口の走りと 1 つの値で持たない —— 引き入れは矢じりを含むので、同じ長さにすると矢じりのぶんだけ走りが短く見える（1 値 1 役） |
| S-299 | 依存線 | `dependencyLeadIn` | px | 16 | `dependencyArrowLength` | 40 | 経路が最後に折れてから入口の辺までの走りであり、矢じりを含む（表 T-222 の前書き）。<br>⭐ 下限を矢じりの長さに結ぶのは、矢じりが走りを食い尽くさないためである（矢じりを伸ばせば引き入れも伸びる） |
| S-300 | 依存線 | `dependencyArrowWidth` | px | 8 | `dependencyWidth` × 2 | 40 | 依存線の矢じりの底辺の長さである。<br>⛔ かつては `dependencyArrowLength` が長さと底辺を兼ねていたので、二等辺の三角しか描けなかった。<br>下限は `dependencyWidth` の上限の裏返しである |
| S-22 | 進捗マーカー | `markerSize` | px | 22.4 | `fontMin` | `actualMin` ÷ `actualOfPlan` | 固定値。<br>縦の鎖に載らない（規則は `FR-094`）。<br>⭐ 既定の 22.4 は、表示の倍率 100 で描くと 14px になる。<br>⚠️ 表 T-012 の `SH-3` / `SH-4` の径は本行ではなく、`FR-094` がそのタスクの名称ラベルの字から導く。<br>⭐ 上限は、予定バーが行の軸の倍率でどこまで縮んでも取る高さ（`S-4` の下限と同じ式。<br>既定で 28）である —— そこまでなら予定バーより大きくならないので、行の外へ出ない。<br>⛔ 上限を `basePlanHeight` そのものにしない —— それを上げてマーカーも上げたあと行の軸の倍率を下げると、予定は床で止まりマーカーだけがはみ出す（マーカーは倍率に追随しない）。<br>下限が `fontMin` なのは、記号がこれを割ると読めないためである |
| S-24 | 進捗マーカー | `markerStroke` | px | 1.3 🔎 | 0.5 | 4 | — |
| S-25 | 進捗マーカー | `resumeScaleInvalid` | × | 0.7 🔎 | 0.3 | 1 | 再開日未定は小さく薄く。<br>0.7 の由来は無い |
| S-26 | 進捗マーカー | `resumeArmOfMarker` | — | 0.62 🔎 | 0.2 | 1 − `resumeHeadOfMarker` | 矢じりぶんを残す。<br>0.62 の由来は無い |
| S-27 | 進捗マーカー | `resumeHeadOfMarker` | — | 0.22 🔎 | 0.05 | 0.5 | — |
| S-28 | 進捗マーカー | `resumeDashOn` | px | 3 🔎 | 1 | 12 | — |
| S-29 | 進捗マーカー | `resumeDashOff` | px | 2 🔎 | 1 | 12 | — |
| S-308 | 進捗マーカー | `resumeOpacityInvalid` | — | 0.55 🔎 | 0.05 | 1 | 再開日が未定のときの再開アイコンの濃さである。<br>⭐ 小さく描くこと（`resumeScaleInvalid`）とは別の値で持つ —— 大きさと濃さは別の向きである（1 値 1 役）。<br>⛔ 測って決めた値ではない |
| S-309 | 進捗マーカー | `resumeDashWidth` | px | 1.92 🔎 | 0.5 | 20 | 停止日から再開アイコンまで引く破線の太さである。<br>刻みは `resumeDashOn` / `resumeDashOff` が持つ。<br>⛔ 測って決めた値ではない |
| S-30 | ラベル | `labelCoef` | — | 0.5 🔎 | 0.3 | 1 | 全角 1 文字 = フォント × 2 × 係数。<br>**実測しない方針なので、この 1 値がレイアウト精度を単独で決める** |
| S-31 | ラベル | `labelPad` | px | 9.6 | 0 | 30 | タスク名を基準の開始から右へ離す量である（表 T-273 の `LP-3` / `LP-4` / `LP-6`）。<br>⛔ 進捗マーカーを出すときは本行を足さない —— そのときは `labelGap` が効く |
| S-32 | ラベル | `labelGap` | px | 9.6 | 0 | 30 | タスク名を進捗マーカーの右端から右へ離す量である（表 T-273 の `LP-1` / `LP-2` / `LP-5`）。<br>⛔ マイルストーン名は本行ではなく `milestoneNameMarkerGap` を読む（1 値 1 役） |
| S-301 | ラベル | `milestoneNameMarkerGap` | px | 9.6 | 0 | 30 | マイルストーン名を進捗マーカーの右端から右へ離す量である（表 T-273 の `LP-7`）。<br>上限は 表 T-201 が札に置くほかの px の値と同じ 30 とし、別の上限を立てる理由が無い |
| S-302 | ラベル | `assigneeLabelGap` | px | 6.4 | 0 | 30 | 担当と進捗の札の右端を、描いている予定と実績のうち早い方の開始から左へ離す量である（表 T-273 の担当と進捗の段）。<br>上限の理由は `milestoneNameMarkerGap` と同じである |
| S-303 | ラベル | `milestoneNameStartOfWidth` | — | 0.25 | 0 | 1 | 進捗マーカーを出さないとき、マイルストーン名を基準の菱形の幅のどこから左詰めで書き出すか（表 T-273 の `LP-8`）。<br>菱形の左端を 0、右端を 1 とする比である。<br>⛔ px で持たない —— 菱形の幅は行の軸の倍率で変わる |
| S-33 | ラベル | `labelBaseline` | — | 0.35 🔎 | 0 | 0.8 | フォントのベースライン補正。<br>実測の記録が無い |
| S-34 | ラベル | `labelHaloOfFont` | — | 0.10 | 0 | 0.3 | 0 = 縁取りなし。<br>縁取りの太さは字の大きさ × 本値である —— 12px の字で 1.2px（12 × 0.10）、表示の倍率が既定の 100 のときの字の床 7.5px（`S-8` の 12 × 描く比 0.625）で 0.75px。<br>バーの上の文字が色に依存せず読める。<br>⭐ 0.10 は、利用者が「字の大きさ × 0.1」で試すよう指定した値である —— 描いた絵を見て選び直す |
| S-35 | ラベル | `truncateUnits` | — | 48 | 4 | 120 | 全角 24 文字 = 半角 48。<br>⭐ **タスク名を半角 48 文字まで表示し、残りを ... で省略するための値である**。<br>⚠️ **省略記号を含めた長さがこの値に収まる** —— 規則は `FR-002` が持つ |
| S-36 | ラベル | `rowTitleFont` | px | 19.5 🔎 | `fontMin` | 40 | 和文の可読下限を割らない。<br>⭐ 19.5 は、利用者がタスクグループ名の字を 1.5 倍に大きくするよう指定した試す値である（13 × 1.5）—— 描いた絵を見て選び直す。<br>⚠️ 元の 13 という値自体の由来は無い。<br>⚠️ `FR-016` の行の軸の上限の字の側は本行と `S-38` を読むので、本値で上限も動く |
| S-37 | ラベル | `rowTitleIndent` | px | 16 | 0 | 60 | 行見出しの字下げ。<br>1 段深くなるごとにこの幅だけ字下げする。<br>⭐ **16 は測って決めた値ではなく、利用者が数で指定した値である** —— ⛔ **以前の 12 は誰も測っておらず（🔎 付き）、根拠が無かった** |
| S-38 | ラベル | `rowTitleTopScale` | × | 1.3 🔎 | 1 | 2 | 深さ 1 は他より小さくしない。<br>1.3 の由来は無い |
| S-39 | 形状の細部 | `planStroke` | px | 1 🔎 | 0 | 4 | — |
| S-304 | 形状の細部 | `thinStrokeWidth` | px | 2.8 | 0.5 | 20 | 線だけの形（表 T-012 の `SH-3` / `SH-4`）の、予定の矢と実績の矢の線の太さである。<br>⛔ 予定の縦幅から導かない —— 太さ・矢じりの横幅・矢じりの縦幅は、それぞれ 1 つの値が1 つの向きだけを決める |
| S-305 | 形状の細部 | `thinArrowHeadLength` | px | 5.6 | 0.5 | 40 | 線だけの形の矢じりの横幅（矢の向きに取る長さ）である。<br>⚠️ 短い矢では `arrowHeadOfSpan` で頭打ちになるが、そのとき段の位置は動かない |
| S-306 | 形状の細部 | `thinArrowHeadHeight` | px | 5.6 | 0.5 | 40 | 線だけの形の矢じりの縦幅（高さ）である。<br>⚠️ 線より高いぶんの半分ずつが上下の間へはみ出す —— はみ出すのは矢の終わりの 1 か所だけである |
| S-43 | 形状の細部 | `chevronNotchOfHeight` | — | 0.45 🔎 | 0.05 | 1 | — |
| S-44 | 形状の細部 | `chevronNotchOfWidth` | — | 0.35 🔎 | 0.05 | 0.5 | 0.5 を超えると先端が反転する。<br>0.35 の由来は無い |
| S-46 | 形状の細部 | `arrowHeadOfSpan` | — | 0.4 🔎 | 0.1 | 1 | — |
| S-307 | 形状の細部 | `spanDotSize` | px | 6.4 | 0.5 | 40 | 端点スパン（表 T-012 の `SH-4`）の両端に置く点の径である。<br>⛔ 線の太さから導かない。<br>範囲は `thinArrowHeadHeight` と揃える —— どちらも矢の終わりを示す印であり、別の範囲を持つ理由が無い |
| S-48 | 形状の細部 | `starInnerOfOuter` | — | 0.45 🔎 | 0.2 | 0.8 | この値が ☆ の面積を決め、`milestoneGlyph` の面積順に影響する |
| S-49 | 形状の細部 | `minShapeWidth` | px | 6.4 | 1 | 20 | `Task` の形状を描く最小の幅（`FR-001` ・ `FR-018`）。<br>期間がゼロの `Task` も、期間の短い `Task` も、本値に描く比（`FR-039` の 表 T-252 の `DS-1`）を掛けた幅より細く描かない。<br>⭐ 既定の 6.4 は、表示の倍率 100（描く比 0.625、`S-236`）で 4px（6.4 × 0.625）に描く値である —— 利用者が、縮めても描くタスクの最小の幅を 4px と定めた。<br>見本（`previous-project-result/16-grab-area-sizing/min-width-lod-sample.html`）は 8px を推したが、利用者は 4px を採った。<br> ⭐ 本値は予定の端の掴み代に縛られない —— 予定の端の掴み代（表 T-266 の `GA-1` / `GA-2`）は端の外側だけに届き、内側の既定は 0 なので、胴は幅によらず必ず残る。 |
| S-50 | イナズマ線 | `progressLineWidth` | px | 2 🔎 | 0.5 | 8 | — |
| S-51 | イナズマ線 | `progressLineOverhang` | px | 6 🔎 | 0 | 40 | — |
| S-53 | ズーム | `zoomStep` ⛔ | × | 1.1 🔎 | 1.01 | 2 | — |
| S-54 | ズーム | `zoomMin` ⛔ | × | 0.02 🔎 | 0.001 | `zoomMax` | 上限を超えない。<br>0.02 の由来は無い |
| S-55 | ズーム | `zoomMax` ⛔ | × | 64 🔎 | `zoomMin` | 512 | 下限を下回らない。<br>64 の由来は無い |
| S-56 | ズーム | `canvasPadding` | px | 10 🔎 | 0 | 60 | — |
| S-129 | 予実 | `actualInitialDuration` | 稼働日 | 1 | 0 | 1 | 掴みシロを掴んだときに置く実績期間（`FR-043`）。<br>**1 日で終わる業務も 1 日ぶん入力する** ので 1。<br>0 にすると幅が 0 になる |
| S-130 | 予実 | `milestoneActualDuration` | 稼働日 | 0 | 0 | 0 | マイルストーンの実績期間（`FR-043`）。<br>**点なので長さを持たない** （表 T-012 の `SH-5`）。<br>表 T-019 の `PA-2` 〜 `PA-5` を満たすための固定値である |
| S-131 | 予実 | `dummyOpacity` | — | 0.20 🔎 | 0.05 | 0.5 | 実績のダミーと未着手マーカーの濃さ（`FR-043` / `FR-013`）。<br>**狙いも適合範囲の外し方も `S-102` と同じである。<br>** 掴める大きさは濃さと別に 表 T-266 が持つので、薄くしても掴みにくくならない |
| S-181 | 注記 | `commentBoxPad` | px | 3 | 0 | 30 | コメントボックスの本文の箱の内側の余白（上下左右とも）。<br>上限は 表 T-201 が札に置く px の値（`S-31` ほか）と同じ 30px とする —— どれも札の字の周りに置く px の値であり、別の上限を持つ理由が無い |
| S-182 | 注記 | `commentBoxWrapUnits` | — | 128 | 4 | 240 | コメントボックスの本文を折り返す単位数。<br>**全角 64 文字 ＝ 半角 128**。<br>数え方は `FR-093`（全角 2・半角 1）。<br>⚠️ **これを超える語は作成者が改行する。<br>** 下限は `truncateUnits`（`S-35`）と同じで、全角 2 文字を割ると語が残らない。<br>上限は、`fontScaleSizes.L` と `labelCoef` の既定で 1 行が 表 T-025 の `MC-6` の画面幅にちょうど収まる値 |

**帯の高さを目盛の段階で動かさない規則は `FR-017` が持つ。**  
文字の大きさは段階によらず `rulerFont` とし、段階が変えるのは帯の中の段の組み方だけである —— 段階で文字が変わると、段階 → 実効フォントサイズ → 判定 → 段階 の循環になる。  
⚠️ 例外が 1 つある。  
規則は `FR-017` が持つ （曜日の行だけを`S-219` の比で小さく刷ってよい）—— ⛔ **ここに書き写してはならない。**  
⭐ **例外が上の理由を壊さないのは、比が段階ではなく行に掛かるからである** —— **どの段階でも同じ比であり、段階 → 文字 → 判定 の循環に入らない。**

> ⭐ `actualOfPlan` は縦寸法の鎖の最上流にある。  
> 既定 0.5715 は測って決めた値ではなく、導いた値である —— 導き方と、切り上げなければならない理由は、本表の `S-5` の備考が持つ。  
> ⭐ ここはそれを指すだけである —— 同じ導出が 2 か所に載ると、必ず離れていく。
> ⚠️ 「0.57」と書かれることがあるが、この数を小数 2 桁へ丸めた書き方であって、別の値ではない。  
> ⛔ 丸めたまま採ると `tools/generate_startup_template.py` が実際に拒む —— **理由は `S-5` の備考が持つ。**
> ⭐ **狙いは値ではなく姿である** —— **重なるものが入れ子になり、行の高さは 1px も動かない。**  
> ⭐ **立つ姿の寸法も `S-5` の備考が持つ。**

## 4. 表示の切り替えと文書全体の書式

**Type**: SECTION

**表 T-202 — 表示の切り替えと書式**

| 行 ID | キー | 型 | 既定 | 意味 |
| --- | --- | --- | --- | --- |
| S-58 | `stackDirection` | `'up'` / `'down'` | `'up'` | 積む向き |
| S-227 | `planVisible` | 真偽 | `true` | 予定の表示。<br>実績（`S-228`）とは独立に切り替える。<br>規則と理由は `FR-049` |
| S-228 | `actualVisible` | 真偽 | `true` | 実績の表示。<br>予定（`S-227`）とは独立に切り替える。<br>規則と理由は `FR-049` |
| S-60 | `assigneeVisible` | 真偽 | `false` | 担当ラベル。<br>既定は隠す（下記） |
| S-61 | `percentCompleteVisible` | 真偽 | `false` | 完了率ラベル。<br>既定は隠す（下記） |
| S-232 | `planDatesVisible` | 真偽 | `false` | 名称ラベルの右に添える予定日。<br>規則と書き方は `FR-002` の 表 T-251。<br>既定は隠す —— 担当ラベル（`S-60`）と同じく、外へ出したラベルが長くなって段数が増えるため（`FR-049`） |
| S-62 | `dependencyVisible` | 真偽 | `true` | 依存線。<br>核機能であり、段数に影響しない。<br>寸法をズームに追随させない規則は `FR-094` が持つ |
| S-63 | `progressMarkerVisible` | 真偽 | `false` | 進捗マーカー（`FR-013`）と再開アイコン（`FR-044`）。<br>寸法をズームに追随させない規則は `FR-094` が持つ |
| S-64 | `progressLineVisible` | 真偽 | `false` | イナズマ線。<br>基準日と実績から引くので、**新規文書には出しても意味のある形にならない** |
| S-65 | `dualCursor` | `{ date1, date2 }` / `null` | `null` | デュアルカーソル。<br>2 本の日付を持つ理由は表 T-029 の `CU-2` |
| S-66 | `guideCursorMode` | `'none'` / `'crosshair'` / `'single-vertical'` | `'none'` | ガイドカーソル。<br>⛔ **`'double-vertical'` は値に持たない** —— **縦 2 本のガイドカーソルは、測るための 2 本（表 T-029 の `CU-2`）と見分けが付かない。<br>** ⛔ **この値を持つ文書は取り込めない** —— 見せ方の群に当てるのは原稿が綴った列挙だけであり（第 6 章）、拒む条件を足してはならない。<br> ⚠️ **初回リリース前であり、この値を持つ文書はまだ 1 つも存在しない。<br>** ⭐ **`'none'` は値として残るが、押す入口を持たない** —— **同じ入口の押し直しで到達する（`FR-048`）。<br>** |
| S-67 | `dateGridLinesVisible` | 真偽 | `false` | 日付罫線。<br>既定が `false` である理由と、目盛の段階に連動させる規則は `FR-089` が持つ。 |
| S-68 | `groupGridLinesVisible` | 真偽 | `true` | グループ罫線。<br>本数は行数ぶんで軽い。<br>描く規則と理由は `FR-042` |
| S-69 | `baselineVisible` | 真偽 | `false` | 変更前の予定を重ねるか。<br>既定が `false` である理由は `FR-015` |
| S-70 | `fontScale` | `'S'` / `'M'` / `'L'` | `'M'` | 文字サイズ。<br>**各値が何 px かは表 T-215 が持つ** |
| S-234 | `displayScale` | `50` / `67` / `75` / `90` / `100` / `110` / `125` / `150` / `175` / `200` | `100` | 表示の倍率（百分率）。<br>日程表の図形と字をまとめて縮める・拡げる。<br>**描く比の求め方と、何に掛けるかは `FR-039` の 表 T-252 が持つ**。<br>⭐ 段の並びは型の欄の順である。<br>⭐ 既定の 100 は 2026-09-16 の出荷ビルドの 5/8 の大きさ、200 は出荷ビルドの 5/4 の大きさである（描く比は `S-236` の注）。<br>⭐ 文書に保存する —— 適切な出力の倍率はプロジェクトの規模で変わる。<br>⚠️ 文書を開いたときに保存した値で描くことと、型の欄に無い値を持つ文書の扱いは `FR-039` が持つ |

`assigneeVisible` と `percentCompleteVisible` の既定が `false` である根拠は `FR-049` が持つ。  
実測は、前プロジェクトの PoC で `zoomX = 0.25` のとき段数 96 → 72（24 段ぶん減る）というものである。

**依存線とイナズマ線の色の規則と理由は `FR-041` が持つ。**

## 5. 画面の状態

**Type**: SECTION

**表 T-203 — 画面の状態**

| 行 ID | キー | 型 | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --- | --- | --- | --- | --- |
| S-72 | `themePreference` | `'light'` / `'dark'` | `'light'` 🔎 | — | — | 明暗テーマ。<br>列挙なので範囲は無い |
| S-74 | `themeMonochrome` | 真偽 | `false` | — | — | モノクロにするか |
| S-75 | `zoomX` | 数値 | `1` | `zoomMin` | `zoomMax` | 横のズーム倍率 |
| S-76 | `zoomY` | 数値 | `1` | `zoomMin` | `zoomMax` | 縦のズーム倍率。<br>`basePlanHeight` が「予定の縦幅（`zoomY` = 1）」と定義されているので等倍がその基準 |
| S-77 | `scrollDate` | 日付 / `null` | `null` | — | — | 表示の左端が指す日付。<br>`null` は「人がまだ場所を決めていない」を表す。<br>読み込む側の規則は表 T-024a の `OP-10` |
| S-78 | `scrollGroupId` | `TaskGroup.id`（UUID 文字列）/ `null` | `null` | — | — | 表示の上端が指す行。<br>⚠️ **整数ではない。<br>** 存在しない id を指していたときの規則は表 T-024a の `OP-10` |
| S-176 | `scrollGroupOffset` | 数値 | `0` | 0 | 1 | 表示の上端が、`scrollGroupId` が指す行のどこにあるか。<br>その行が占める送り（その行の帯の高さと、その下の隙間を合わせた長さ。<br>次の行の上端までの距離であり、最後の行は自身の帯）に対する比であり、px ではない （`FR-080`）。<br>⚠️ **帯の高さではなく送りである理由は `FR-016` が持つ** —— 同要求のパンの規則が、何に対する比かまで定めている。<br>⚠️ 上限の 1 は含まない。<br>1 以上になったら錠を 1 つ隣へ送る —— 同じ位置を 2 通りに書けると往復の比較（`NS-4`）が偽になる |
| S-177 | `scrollDayOffset` | 数値 | `0` | 0 | 1 | 表示の左端が、`scrollDate` が指す日のどこにあるか。<br>⭐ **横の軸の `S-176` である** —— 持ち方も上限の扱いも同行が持つ |
| S-79 | `rowTitlePanelWidth` | px | `300` 🔎 | `rowTitleIndent` × `maxGroupDepth` | `Row Area` の幅 > 0 に従う | 行見出しパネルの幅。<br>下限が 0 にならない理由は `FR-052`。<br>範囲としては `S-125` の深さぶんのインデントが入る幅を要する⭐ **深さ 5 の掴み代が操作子に覆われないことから決めた。<br>** 深さ `d` の掴み代の右端は `16d+16`、操作子の左端は 幅 −（入口の外形 × 4）なので、深さ 5 では 幅 ≧ 96 ＋ 外形 × 4 が要る —— 外形 26px（`S-138` 16 ＋ `S-141` 4 × 2 ＋ `S-237` 1 × 2）では 96 ＋ 104 ＝ 200 である。<br>⭐ 本値 300 は、利用者が行見出しの名前の字（`S-36`）とともにパネルの幅も 1.5 倍にするよう指定した試す値である（200 × 1.5）—— 描いた絵を見て選び直す。<br>⚠️ 行見出しパネルの入口の隙間を `S-243` の 1 とした外形 20px（16 ＋ 1 × 2 ＋ 1 × 2）では、要る幅は 96 ＋ 80 ＝ 176 である。<br>⚠️ 実測（幅 170px）: 深さ 4 と 5 で掴み代が操作子の下に隠れ、掴もうとした押しが別の操作子に取られた。<br>⚠️ 上の数は表示の倍率を掛ける前の数である —— 描く幅に掛ける比と、描く幅の床は `FR-039` の 表 T-252 の `DS-9` とその後の段が持つ。 |
| S-80 | `propertyPanelWidth` | px | `0` | 0 | 同上 | プロパティパネルの幅。<br>⛔ **本行は幅だけを持ち、面を出しているかどうかは持たない**—— **出しているかは 表 T-206 の `S-99h` が持つ。<br>** ⚠️ `0` は「まだ広げていない」であって「閉じている」ではない。<br>⭐ 面を出したときに置く幅は 表 T-206 の `S-171` が持つ —— 人が境界をドラッグした後は本行が勝つ（`FR-052`）。<br>⚠️ ただし本行が 表 T-206 の `S-248` を下回る文書（0 を含む）では、面を出すときに `S-171` の幅で描き、本行は書き換えない（`FR-052`）。<br>⚠️ 境界のドラッグは `S-248` より細い幅を作らない（`FR-052`） |
| S-126 | `pinnedGroupIds` | `TaskGroup.id` の配列 | `[]` | 0 件 | `pinnedRowMax` | ピン止めしている行。<br>上限は `S-127`。<br>指す行を消したときの規則は表 T-050 の `CD-2` |
| S-127 | `pinnedRowMax` | 整数 | `5` 🔎 | 1 | 8 | 同時にピン止めできる行の数の上限 |

**`themeHue = 214` は実測値である。**  
予実の色表は色相 214 で測られており（実績 ÷ 予定 = ライト 4.81 : 1 / ダーク 3.74 : 1）、**この色相だけがコントラスト検証を通っている。**  
前プロジェクトの JSON 実例にある `210` に根拠は無い。

2 つのパネル幅の上限は互いに依存する。  
式と検証の規則と理由は `FR-052`。

**派生する色を保存しない規則と理由は `FR-041` が持つ。**  
解き方は「色相ごとに、コントラスト規則が壊れる寸前まで濃い地色」である。

**行の帯の色も `themeHue` から解く。**  
解き方の規則と理由は `FR-042`。

**`themeMonochrome` は保存値を変えない。**  
描画時にどう効くかの規則と理由は `FR-041` が持つ。

> ⚠️ **モノクロ時のコントラストは未検証である。**  
> 前プロジェクトのコントラスト検査は色を指定していない側しか測っていない。  
> **扱いと測る対象は `NFR-007` が持つ。**

> ⚠️ `themePreference` と `fontScale` の保存値は初期値である。  
> 読む人が変更できること、および変更が文書の編集になることは `FR-039` が定める。

## 5a. 日程データに属する値

**Type**: SECTION

**表 T-216 — 日程データに属する値**

| 行 ID | キー | 型 | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --- | --- | --- | --- | --- |
| S-73 | `themeHue` | 0〜359 | `214` | 0 | 359 | テーマの色相。<br>置き場は表 T-052 の `DR-5`。<br>追随の対象と規則は `FR-041` |
| S-71 | `importSeq` | 整数 | `0` | — | — | 取込のたびに +1。<br>既定は取込を 1 度もしていない状態。<br>置き場は `Project.importSeq`（表 T-058）。<br>**使い道は表 T-032 の `MG-13`** |

## 6. 出力

**Type**: SECTION

**表 T-204 — 出力**

| 行 ID | キー | 型 | 既定 | 意味 |
| --- | --- | --- | --- | --- |
| S-81 | `exportCanvas` | `{ width, height }` | `1600 × 900` | SVG / PNG の出力サイズ。<br>規則と理由は `FR-025`、縮める比は `FR-080` が持つ |
| S-217 | `exportCanvasHeightCap` | px | `4096` | 書き出す絵の高さの上限。<br>規則と理由は `FR-025` が持つ。<br>⭐ **`S-81` の高さは伸ばす前の下限であって、上限ではない** 。<br>⚠️ `4096` は 2^12 であり、canvas とテクスチャの上限として最も広く使われる値である —— 4000 前後の、よく使われる値を選んだ |

## 7. LOD のしきい値

**Type**: SECTION

出力に含まれるタスクが変わるので保存する。

**表 T-205 — LOD のしきい値**

| 行 ID | キー | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --: | --- | --- | --- |
| S-83 | `rulerTierPxPerDayMonth` | `1.4` | 0.1 | `rulerTierPxPerDayWeek` | 目盛が「年」から「年 ＋ 月」に変わる px/day。<br>次のしきい値を超えない |
| S-84 | `rulerTierPxPerDayWeek` | `4.3` | `rulerTierPxPerDayMonth` | `rulerTierPxPerDayDay` | 目盛が「年 ＋ 月」から「年 ＋ 月 ＋ 週」に変わる px/day。<br>前後のしきい値の間 |
| S-85 | `rulerTierPxPerDayDay` | `17.5` | `rulerTierPxPerDayWeek` | 60 | 目盛が「年 ＋ 月 ＋ 週」から「年 ＋ 月 ＋ 日 ＋ 曜日」に変わる px/day。<br>前のしきい値を下回らない。<br>⭐ **導き方は本表の下の注が持つ。<br>** ⚠️ **`LF-1` が間引かないので、日ごとのラベルがそのまま並ぶ幅が要る** —— ⭐ 日と曜日が別の段を持つので、束縛するラベルは `Mon`（3 単位）である（`FR-017`）⭐ **1 画面幅に 2 か月を日の段で出すことを狙う。<br>** ⛔ この導き方は、書き出す絵の寸法（`S-81` ＝ 1600px）を基準環境の幅に使わない。<br>`S-81` は書き出す絵の寸法であって画面幅ではない。<br> ⭐ 基準環境は 表 T-025 の `MC-6` が持ち、1920 × 1080 である。<br>⇒ 1920px から行見出しパネル（`S-79` ＝ 200px）とスクロールバー（18px）を引いた 1702px に 61 日 ⇒ 27.9px/日。<br> ⚠️ **測り方**: 起動直後から Ctrl ＋ ホイールで 1 刻みずつ拡げ、曜日の段が現れた最初の刻みで、隣り合う曜日ラベルの間隔を px/日 とし、（画布 1920 − 行見出しパネル 200 − スクロールバー 18）÷ px/日 を数えた。<br> ⚠️ **保証するのは基準環境である** —— 「1 画面幅」は幅を言わなければ検められない約束だからである。<br> ⭐ **17.5 とするのは、書き出す絵に 2 か月を日の段で入れるためである。<br>** ⛔ 段を決める式が本行の値をそのまま使っていない —— `rulerTierOf` が比べるのは `pxPerDay ÷ (rulerFont ÷ fontMin)` であり、`rulerFont` 14 ／ `fontMin` 12 なので割る数は 1.1667 である。<br>⇒ しきい値 `T` は実際には `T` × 1.1667 px/日 を意味する（22 なら 25.67px/日）。<br> ⭐ **導き方**: 書き出す絵の描画幅は `S-81` 1600 − 行見出しパネル 200 ＝ 1400px。<br>そこに 61 日を入れるには 22.95px/日 が要り、しきい値に直すと 19.67。<br>⛔ さらに倍率の刻みが実測 1.121 倍なので、どの初期倍率からでも届くには1 刻みぶんの余裕が要る ⇒ 22.95 ÷ 1.1667 ÷ 1.121 ＝ 17.55 以下。<br> ⇒ ⭐ **17.5 とした。<br>** ⭐ 実測（出荷ビルド、1920 × 1080）: 日の段が出る最初の刻みは 21.11px/日 になり、書き出した `.svg` に 66.3 日入る。<br>画面には 80.6 日入る。<br>⚠️ 上の導き方と実測は `rulerFont` 14 と行見出しパネル 200px の組で数えた —— `S-3` が文字サイズの段に 1.5 を掛け、`S-79` を 300 とした組では、割る数は `rulerFont` 21 ÷ `fontMin` 12 ＝ 1.75 になり、書き出す絵の描画幅も行見出しパネルのぶん狭くなる。<br>⇒ 本値 17.5 で書き出す絵に 2 か月を日の段で入れる狙いが保たれるかは、数え直していない |
| S-87 | `groupLevelOfDetailBase` | `0.32` | 0.01 | 2 | グループ LOD の初項。<br>`threshold(d) = base × ratio^(d − 2)` |
| S-88 | `groupLevelOfDetailRatio` | `1.5` | 1 超 | 4 | 同上の公比。<br>**1 以下だと深いほど出やすくなり単調性が壊れる** |
| S-89 | `stackSafetyCap` | `255` | — | — | **1 つの `TaskGroup` あたりの段数の安全弁** （**測って決めた値ではない** ）。<br>**数え方と、到達したときの挙動は `FR-003` の `ST-7` が持つ。<br>** これだけの本数が 1 つの行で同時に重なるのは実務では起きないので、届いたときは入力か操作の異常である |

**3 つのしきい値は、目盛のフォントが 12px のときの解である。**  
ラベル幅の概算は `FR-093` に従う（導出時点の `labelCoef` は 0.5）。  
和文の可読下限 12px を当てて導いた。  
⚠️ **毎日刻む段だけは `rulerLabelGap`（`S-135`）を足す** —— 刻みが 1 日で、隣り合うラベルが接するのは日の段と曜日の段だけだからである。  
⚠️ **その 2 段は、両方のうち広いほうの行を、さらに両言語のうち幅の広いほうで導く** —— 表示言語は文書に保存しないので（`FR-038`）、**1 つの値が両方を満たさねばならない。**

| 段階 | ラベル | 単位数 | 必要幅 | 条件 | 解 | 採用 |
| --- | --- | :--: | --: | --- | --: | --: |
| 2 年 ＋ 月 | `2026-01` | 7 | 42px | `30.4 × ppd ≧ 42` | 1.38 | **1.4** |
| 3 ＋ 週 | `01-05` | 5 | 30px | `7 × ppd ≧ 30` | 4.29 | **4.3** |
| 4 ＋ 日曜 | 日 `15` ／ 曜日 `水` ・ `Mon` | 3 | 18px ＋ `S-135` | `1 × ppd ≧ 26` | 26 | **26** |

上の式は既定値を選ぶときの考え方であって判定式ではない。  
固定値であること・判定の仕方・実効フォントサイズの定義・基準とする文字の大きさは `FR-017` が持つ。

「一定数以下なら全部描く」というしきい値は本表に無い。  
禁止と理由は `FR-018` が持つ。

## 8. 保存しないもの

**Type**: SECTION

**表 T-206 — 保存しないもの（本体と、`localStorage` に置く別枠）**

| 行 ID | 値 | 既定 | 保存しない理由 |
| --- | --- | --- | --- |
| S-250 | 表 T-266 の `GA-1`（矩形の予定の開始）の掴み代 —— 端の外側 | 12px | **掴み領域は読む人のアクセシビリティに属する。<br>** 手が震える人には大きな掴み代が要る。<br>文書が強制してよい値ではない。<br>⭐ 本行から続く 表 T-266 の値は、どれも 1 つの対象の 1 つの向きだけを決める —— ⛔ ある対象の値が、ほかの対象の掴み代の大きさを決めない。<br>⚠️ 表示の倍率の描く比を掛けない（`FR-039` の 表 T-252 の `DS-7`）ので、画面の px のまま届く。<br>重なったときにどれが応えるかは 表 T-267 と 表 T-268 が持ち、本行は形と大きさだけを持つ |
| S-251 | 表 T-266 の `GA-1` の掴み代 —— 端の内側 | 0px | 同上。<br>⭐ 値は持ち、既定を 0 とする—— 上げると短い予定では本体が消える。<br>上げる値の上限の規則は、上げると決めたときに足す |
| S-252 | 表 T-266 の `GA-1` の掴み代 —— 予定の帯から上下へ | 0px | 同上。<br>⭐ 既定を 0 とするのは、上下へ広げると上か下に積んだタスクの形へ届くからである |
| S-253 | 表 T-266 の `GA-2`（矩形の予定の終了）の掴み代 —— 端の外側 | 12px | 同上。 |
| S-254 | 表 T-266 の `GA-2` の掴み代 —— 端の内側 | 0px | 同上。 |
| S-255 | 表 T-266 の `GA-2` の掴み代 —— 予定の帯から上下へ | 0px | 同上。 |
| S-256 | 表 T-266 の `GA-3`（矩形の実績の開始）の掴み代 —— 端の外側 | 0px | 同上。<br>⭐ 外側に届かないのは、予定の開始の掴み代（`S-250`）と取り合わないためである |
| S-257 | 表 T-266 の `GA-3` の掴み代 —— 端の内側 | 12px | 同上。<br>⭐ 実績の幅の半分を超えない —— 超えると短い実績で開始と終了が入れ替わる。<br>進捗マーカーが立つときの頭打ちは 表 T-266 の `GA-3` が持つ |
| S-258 | 表 T-266 の `GA-3` の掴み代 —— 実績の帯から上下へ | 0px | 同上。 |
| S-259 | 表 T-266 の `GA-4`（矩形の実績の終了）の掴み代 —— 端の外側 | 0px | 同上。 |
| S-260 | 表 T-266 の `GA-4` の掴み代 —— 端の内側 | 12px | 同上。<br>⭐ 実績の幅の半分を超えない（`S-257` と同じ理由） |
| S-261 | 表 T-266 の `GA-4` の掴み代 —— 実績の帯から上下へ | 0px | 同上。 |
| S-262 | 表 T-266 の `GA-5`（矩形のダミーの開始）の掴み代 —— 端の外側 | 0px | 同上。<br>⭐ ダミーは実績と別の値で持ち、既定は実績と同じにする |
| S-263 | 表 T-266 の `GA-5` の掴み代 —— 端の内側 | 12px | 同上。<br>⭐ 印の幅の半分を超えない —— 印を中央で割り、左半分が開始、右半分が終了になる |
| S-264 | 表 T-266 の `GA-5` の掴み代 —— 実績の帯から上下へ | 0px | 同上。 |
| S-265 | 表 T-266 の `GA-6`（矩形のダミーの終了）の掴み代 —— 端の外側 | 0px | 同上。<br>⭐ 印の外へ出ない |
| S-266 | 表 T-266 の `GA-6` の掴み代 —— 端の内側 | 12px | 同上。<br>⭐ 印の幅の半分を超えない（`S-263` と同じ理由） |
| S-267 | 表 T-266 の `GA-6` の掴み代 —— 実績の帯から上下へ | 0px | 同上。 |
| S-268 | 表 T-266 の `GA-7`（フェード入の掴み点）の掴み代 —— 点に中央を合わせた正方形の一辺 | 8px | 同上。<br>⭐ 8 は利用者が指定した値である。<br>⚠️ 見た目の点（表 T-210 の `S-109`、5 × 5px）より広い |
| S-269 | 表 T-266 の `GA-8`（フェード出の掴み点）の掴み代 —— 点に中央を合わせた正方形の一辺 | 8px | 同上。 |
| S-270 | 表 T-266 の `GA-10`（線だけの形の予定の開始）の掴み代 —— 基準点に中央を合わせた幅 | 12px | 同上。<br>⭐ 線だけの形は予実を中線で分けるので、外側と内側に割らず 1 つの幅で持つ |
| S-271 | 表 T-266 の `GA-10` の掴み代 —— 予定の矢の帯から上下へ | 0px | 同上。 |
| S-272 | 表 T-266 の `GA-11`（線だけの形の予定の終了）の掴み代 —— 基準点に中央を合わせた幅 | 12px | 同上。 |
| S-273 | 表 T-266 の `GA-11` の掴み代 —— 予定の矢の帯から上下へ | 0px | 同上。 |
| S-274 | 表 T-266 の `GA-12`（線だけの形の実績の開始）の掴み代 —— 基準点に中央を合わせた幅 | 12px | 同上。 |
| S-275 | 表 T-266 の `GA-12` の掴み代 —— 実績の矢の帯から上下へ | 0px | 同上。 |
| S-276 | 表 T-266 の `GA-13`（線だけの形の実績の終了）の掴み代 —— 基準点に中央を合わせた幅 | 12px | 同上。 |
| S-277 | 表 T-266 の `GA-13` の掴み代 —— 実績の矢の帯から上下へ | 0px | 同上。 |
| S-278 | 表 T-266 の `GA-15`（マイルストーンの予定）の掴み代 —— 菱形の幅に対する比 | 1.15× | 同上。<br>⭐ 中心を合わせた正方形の一辺を、菱形の幅の何倍に取るかである。<br>⛔ px で持たない —— 菱形の幅は行の軸の倍率で変わる |
| S-279 | 表 T-266 の `GA-15` の掴み代 —— 菱形の帯から上下へ | 0px | 同上。 |
| S-280 | 表 T-266 の `GA-16`（マイルストーンの実績）の掴み代 —— 外接正方形の外側 | 0px | 同上。<br>⭐ 掴み代は描いた図形の外接正方形そのものである |
| S-281 | 表 T-266 の `GA-16` の掴み代 —— 菱形の帯から上下へ | 0px | 同上。 |
| S-282 | 表 T-266 の `GA-17`（マイルストーンのダミー）の掴み代 —— 外接正方形の外側 | 0px | 同上。 |
| S-283 | 表 T-266 の `GA-17` の掴み代 —— 菱形の帯から上下へ | 0px | 同上。 |
| S-284 | 表 T-266 の `GA-18`（進捗マーカー）の掴み代 —— 描いたマーカーの外側 | 0px | 同上。<br>⭐ 掴み代は描いたマーカーそのものである。<br>実績の開始の掴み代と重なるときだけ、マーカーの中心で割る |
| S-285 | 表 T-266 の `GA-19`（依存線）の掴み代 —— 描いた線の縁から左右へ | 2px | 同上。<br>⭐ 線の中心からではなく、描いた線の縁から数える—— 線を太くしても的の広がり方が変わらない。<br>⚠️ 基準日線は本行を読まない —— そちらは `S-137` の 6px のままである |
| S-286 | 表 T-266 の `GA-20`（再開アイコン）の掴み代 —— 掴む箱の外側 | 0px | 同上。<br>⭐ 掴む箱はそのタスクの進捗マーカーの径（線だけの形は名称ラベルの字の大きさ）であり、描いた大きさによらない —— 再開日が未定のアイコンは小さく描くが、箱は縮めない |
| S-287 | 表 T-266 の `GA-21`（線だけの形のダミーの開始）の掴み代 —— 基準点に中央を合わせた幅 | 12px | 同上。<br>⭐ 印の横幅が字の大きさの半分しかないので、2 つの掴み代は重なる。<br>重なりは印の横幅の中央で割る—— そのぶん印の外へ出る |
| S-288 | 表 T-266 の `GA-21` の掴み代 —— 実績の矢の帯から上下へ | 0px | 同上。 |
| S-289 | 表 T-266 の `GA-22`（線だけの形のダミーの終了）の掴み代 —— 基準点に中央を合わせた幅 | 12px | 同上。 |
| S-290 | 表 T-266 の `GA-22` の掴み代 —— 実績の矢の帯から上下へ | 0px | 同上。 |
| S-180 | 実績のダミーを描く幅の上限（表 T-240 の `DM-3`） | 30px | `S-138` と同じ理由で保存しない。<br>出ないのは入口ではなくダミーであり、根拠の行は 表 T-076 の `EP-14` である。<br>⭐ 描く幅そのものは `DM-3` が進捗マーカーの径と `S-247` から導き、本行はその上限だけを持つ —— マーカーの大きさ（`S-22`）と比を選び直しても、印が 30px より太らない。<br>⭐ **本行が定めるのは横だけである** —— 縦の広がりは実績の帯に従う。<br>⛔ **掴みシロの大きさは本行が持たない** —— 掴み代は 表 T-266 が対象ごとに持つ。<br>⚠️ 矩形のダミーの掴み代は印の外へ出ないが、線だけの形（表 T-012 の `SH-3` / `SH-4`）のダミーは印の外へ出る。<br>⛔ **測って決めた値ではない** |
| S-196 | 線だけの形の 1 段目と予定の線のあいだ（表 T-271 の `XS-5`） | 2px 🔎 | ⭐ **1 段目の下端（字の大きさの箱の下端）と、予定の線の上の縁とのあいだの隙間である** —— 描いた字の箱ではなく、字の大きさで数える。<br>⛔ どの形がこの隙間を使うかは 表 T-271 が持ち、本行は持たない。<br> ⚠️ **保存しない理由は `S-138` のそれとは違う** —— `S-138` は「書き出しに入口を描かない」ことを根拠にするが、**名称ラベルは 表 T-076 の `EP-5` により書き出しに入る。<br>** ⭐ 本行を保存しないのは、隙間が形状の種類から決まり、文書が名称ラベルの位置を持たないからである。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-233 | 名称ラベルが縦に取る高さの、字の大きさに対する比（段の割り当てに使う） | 1.5 🔎 | ⭐ 保存しないのは、描いた字の箱の高さを決める書体を文書が持たないからである —— 名称ラベルの `<text>` の書体は、製品が持つ 表 T-206 の `S-246` の並びである。<br>⛔ **線だけの形の 1 段目の高さには本行を当てない** —— そちらは字の大きさ f そのもので数える（表 T-271 の `XS-4`）。<br>⚠️ 実測（2026-09-16、ブラウザ、書体の指定なし、表示倍率 1）: 字の大きさ 12px の `<text>` の箱は基線の上 13px・下 5px の計 18px で、比は 1.5 である。<br>同じ測りで 12.8px は 20px、16px は 24px、16.9px は 25px、21.12px は 31px —— 上下の張り出しを整数の px に丸めるので、比は 1.46 〜 1.57 に揺れる。<br>⛔ 測って決めた値ではない（🔎） —— 1 つの宿主で 1 度測っただけであり、書体の違う宿主では揺れる |
| S-194 | カーソルの線の太さ（表 T-029 の `CU-2` ・ `CU-3`） | 1px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **`S-178` を掛ける前の、その線自身の太さである** —— 掛ける規則は 表 T-029a の `DC-8` が 表 T-023c の `SL-8` から引く。<br>⛔ **測って決めた値ではない** （🔎） |
| S-186 | プロパティパネルの入力欄の高さの下限（`FR-006`） | 17px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **「高さ」ではなく「下限」である** —— 読む人がブラウザで文字を大きくしたとき、固定の高さは文字を切り落とす（`NFR-007`） |
| S-187 | 色の入力欄の高さの下限（`FR-006`） | 16px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **`S-186` とは別の値である** —— 参考実装が別に持っており、色見本の並びを挟むぶん 1px 低い |
| S-188 | 色見本の一辺と隙間（`FR-006`） | 14 × 3px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⭐ **一辺と隙間の組であって、幅と高さではない** （`S-175` と同じ持ち方）—— **色見本は 14px の正方形** であり、3px はその隣との隙間である⭐ **本行が持つのは見本の寸法だけである** —— **何回描いてよいかは `FR-006` が持つ。<br>** |
| S-189 | 項目名の欄が取る割合（`FR-006`） | 42% 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⭐ **px ではなく割合である** —— パネルの幅は `S-80` で人が動かすので、絶対値で持つと広げたときに入力欄だけが伸びる |
| S-190 | 項目名と入力欄のあいだ（`FR-006`） | 6px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **`S-190` が横で `S-191` が縦である** —— 欄は横に名前と入力が並び、縦に積むので、2 つの隔たりは別の軸にある |
| S-191 | 欄どうしの縦の隔たり（`FR-006`） | 2px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。 |
| S-192 | プロパティパネルの内側の余白（`FR-006`） | 6 × 8px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⭐ **縦・横の組である** |
| S-193 | 複数行の入力欄が見せる行数の下限（`FR-006`） | 2 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**。<br>⛔ **表 T-016 の「入力の型」が `複数行` と記す行だけが使う** （いまは `PR-2` の 1 行）。<br>⛔ **行数そのものではなく下限である** —— 幅に入り切らない値は折り返して欄を縦に伸ばす（規則は `FR-006`） |
| S-197 | プロパティパネルの文字の大きさの係数（`FR-006`） | 0.70 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **参考実装の実測値である**—— パネルの文字を `calc(0.7 × ui-font)` で描いており、`ui-font` が 17px のとき **11.9px** であることを実測した。<br>⭐ **px ではなく係数である。<br>** ⛔ 掛ける相手は宿主が与える地の文字の大きさであって、`fontScaleSizes[fontScale]` ではない —— 理由は `FR-006` が持つ。<br>⛔ **参考実装の 11.9px をそのまま書き取ってはならない** —— あれは本仕様が持たない 17px から出た数である |
| S-198 | 項目名だけをさらに小さくする係数（`FR-006`） | 0.90 🔎 | ⭐ 掛ける相手は `S-197` を適用したあとの大きさであって、`fontScaleSizes` ではない —— パネル全体が縮んだうえで、その中でさらに項目名だけが小さい。<br>参考実装では値の欄が 11.9px のとき項目名の欄が **10.71px** で、比はちょうど 0.9 であった（実測）。<br>⚠️ **`S-189`（項目名の欄が取る割合）とは別物である** —— あちらは幅、本行は文字の大きさ。<br>保存しない理由は `S-138` と同じである |
| S-199 | 入力欄が、値のほかに要る幅（`FR-006`。<br>字の大きさに対する倍率） | 2.19× 🔎 | 同上。<br>⛔ **値そのものの幅ではない** —— そちらは `FR-093` の概算である。<br>枠と内側の余白と、宿主が入力の型に応じて付ける入力補助（`date` の暦を開く釦、`number` の上下の釦）が取るぶん である。<br>⛔ **px ではなく倍率で持つ** —— `FR-006` が「px の定数として持ってはならない（MUST NOT）」と定めており （WCAG 2.1 の 1.4.4）、px で足すと、読む人が文字を 2 倍にしたときに要る幅が 2 倍にならず、欄だけが取り残される。<br> ⭐ **参考実装の実測から起こした**—— 値の欄が 11.9px のとき足りないぶんが約 26px で、比は **2.19** 。<br>⛔ **1 つの操作子に何を割るかという規則は本行が持たない。<br>** 規則は `FR-006` にある |
| S-137 | 基準日線の掴み代（表 T-023d の `GR-16`） | 線の左右に 6px 🔎 | 同上。<br>⛔ **「線の上」は幅を持たない集合であり、許容を与えないと決して当たらない。<br>** ⭐ **本行は基準日線だけを持つ** —— 依存線の掴み代は 表 T-266 の `GA-19` が描いた線の縁から左右へ 2px で持つ。<br>⚠️ **基準日線は上げない** —— 理由は 表 T-023d の結びが持つ。 |
| S-230 | ハイライトボックスの四隅の掴み代（表 T-023d の `GR-14`） | 6px 🔎 | 同上。<br>⭐ **本行は四隅だけを持つ** —— コメントボックスのアンカー（線先）は`commentAnchorSlop` が自分の数を持つ（1 値 1 役）。<br>⭐ 6px は利用者が指定した値である |
| S-293 | ハイライトボックスの枠の掴み代（表 T-023d の `GR-14`） | 枠線から内と外へ 3px | 同上。<br>⭐ 枠の近くだけを掴ませ、囲んだ内側は下のタスクへ素通しにする—— 先に応えても、中のタスクから押しを取らない。<br>⭐ 3px は利用者が指定した値である |
| S-291 | コメントボックスの引出し線の掴み代（表 T-023d の `GR-14`） | 線から左右へ 3px | 同上。<br>⭐ 引けば本文の箱が動く（`CM-51`）。<br>⭐ 3px は利用者が指定した値である |
| S-292 | コメントボックスの線先の掴み代（表 T-023d の `GR-14`） | 線先から 6px | 同上。<br>⭐ 線先はタスクやマイルストーンの上に置く取っ手なので、描いた形の上でも本行の掴み代で応える。<br>⭐ 6px は利用者が指定した値である |
| S-134 | `Panel Divider` の掴み帯（`FR-051`） | 境界に重なる 8px 🔎 | 同上。<br>**`Row Area` から場所を取らない** （`FR-051` の MUST NOT）ので、境界の線に重ねて敷く。<br>由来は前プロジェクトの PoC の実測。<br>⭐ 帯と、帯に重なる縦の `Scrollbars` の帯やパネルのどちらを押したことにするかは 表 T-023d の `GR-22` が持ち、本行は帯の幅だけを持つ |
| S-135a | `Command Palette` の掴み帯（`FR-053`） | 上端に敷く 24px 🔎 | 同上。<br>パレットの上部に敷く帯であり、表 T-023d の `GR-19` が最優先と定める 。<br>⚠️ **パレットの大きさは中身が決める** （`FR-053`）ので、本値が定めるのは帯の高さだけである。<br>⛔ **測って決めた値ではない** （🔎） |
| S-225 | `Document Title`（`U-27`）の字の大きさ | 16px | **画面と書き出しの両方が本行を読むこと**。<br>規則は 表 T-076 の `EP-1` が持つ（画面と書き出しが本行を読むこと、書き出し専用の定数を持たないこと）。<br>⭐ **値は画面の実測である** （出荷ビルド 1920×1080）—— 画面の見た目を 1px も変えないために、測った値をそのまま行にした。<br>⛔ 書き出しは帯の高さに比例させて字を描いてはならない（MUST NOT）—— 帯の高さ × 0.4 では 14.8px となり、画面より 7.5% 小さい |
| S-226 | `Document Title` の、帯の左端からの余白 | 12px | 同上。<br>⭐ **`S-225` と対である** —— 字の大きさだけを揃えても、左の余白が違えば位置は揃わない。<br>⚠️ 帯の高さ × 0.5 を左の余白にすると 18.5px となり、画面より 6.5px 右へずれる （出荷ビルド 1920×1080 の実測）。<br>⭐ 帯の高さに比例しないのは、帯の高さが `FR-051` の測る値であり、題の置き方を決める値ではないからである。<br>規則は 表 T-076 の `EP-1` が持つ |
| S-138 | 入口の図形を描く箱の一辺（`FR-029`） | 16px 🔎 | **画面の道具の寸法であり、文書の内容ではない** —— 書き出す絵に入口は出ない（表 T-076 の `EP-1` と `EP-4`）ので、渡した相手の絵はこの値で変わらない。<br>⛔ **閲覧者の文字サイズに追随させない** —— 大きくしたい人はブラウザの表示倍率で変える。<br>⚠️ **本値は `S-141` と `S-237` と対である** —— ⛔ **本行は入口の外形を持たない** —— 外形の幅はこの 3 行から求まり、その式は `FR-029` が持つ。<br>⚠️ 描くときは、どの面でも `S-235` を掛ける（規則は `FR-029`）。<br>⛔ 表示の倍率（表 T-202 の `S-234`）は掛けない（`FR-039` の 表 T-252 の `DS-7`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-218 | 行の掴み代と行の名前のあいだ（表 T-023d の `GR-20`） | 4px 🔎 | `S-138` と同じ理由で保存しない。<br>⭐ **`FR-085` の式が引く項である**。<br>⛔ **`S-141`（図形と入口の枠の最低隙間）ではない** —— 数が同じだけで、あちらは入口の枠の話であり、掴み代は 表 T-051 の `HF-15` が「押す入口ではなく、掴める場所を指す印」と定めている。 |
| S-140 | 行の操作子に確保する場所（`FR-085`） | 0px 🔎 | 同上。<br>⛔ **描くかどうかで変えてはならない** （`FR-085` の MUST NOT）—— 書き出しは操作子を描かないのに場所を確保しており（表 T-076 の `EP-4`）、ポインタが乗ったときだけ描く形も同じである。<br>⚠️ **打ち切りの位置が動かないことが、この規則の目的である** 。<br>⭐ **0 でもこの MUST NOT は真である** —— 常に 0 なら、描くかどうかで変わっていない。<br>⭐ **目的も保たれる** —— 書き出しも 0 なので、画面と書き出しで打ち切りの位置が一致する。<br>⛔ **本行を消してはならない** —— 消すと `FR-085` の算式が項を 1 つ失う。<br>⚠️ **0 とするのは、`HF-6` により操作子がほとんどの時間どの行にも描かれないからである** —— 0 でなければ、描かれない操作子の幅が行の名前から引かれ続ける。<br> ⭐ **代わりに操作子は名前の上へ重ねる** （表 T-051 の `HF-6`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-141 | 図形と入口の枠の最低隙間（`FR-029`） | 4px | 同上。<br>⭐ **定めるのは隙間だけであり、図形の箱（`S-138`）は動かさない** —— 隙間を詰めれば入口の枠（外形）が小さくなる（外形の式は `FR-029`）。<br>⚠️ 行見出しパネルに載る入口の隙間は本行ではなく `S-243` が持つ —— 本行は `App Header`・`Command Palette`・ヘルプ・プロパティパネルほか、ほかの面の入口の隙間である。<br>⚠️ **図形が座標系をどれだけ使うかで見える大きさが変わる** ので、図 F-019 の座標系は図形の外接枠へ詰める（`FR-029`）⭐ **角の R と同じ値である** —— **アイコンの角の R が曲がり始めるところと同じぐらいの余白があれば足りるからである**。<br>⭐ 角の R は **4px** である —— 出荷ビルドの入口は `border-radius:0.25em` を持ち、`font-size` は 16px である（出荷ビルドの入口の CSS を読んで求めた）。 |
| S-237 | 入口の枠の線の太さ（`FR-029`） | 1px | `S-138` と同じ理由で保存しない。<br>⭐ **`S-138` の箱と `S-141`（行見出しパネルでは `S-243`）の隙間の外側に引く、入口 1 つぶんの枠の線である** —— 外形の幅の求め方は `FR-029` が持ち、本行は太さだけを持つ。<br>⭐ 出荷ビルドの入口が `border:1px` を持つことを読んで求めた（`S-141` の角の R と同じ求め方である）。<br>⛔ **`S-143`（パレットの群を隔てる線の太さ）とは別物である** —— 数が同じだけで、あちらは群を隔てる罫であり、本行は入口の枠である。<br>⚠️ 描くときに掛ける比と掛けない比は `S-138` と同じである（同行の注）。 |
| S-171 | プロパティパネルが開いたときに取る幅（`FR-072`） | 280px 🔎 | 同上。<br>⭐ `S-80` は文書が保つ幅であり、面を出しているかどうかは 表 T-206 の `S-99h` が持つ—— 本値は**面を出すときに置く幅** である。<br>⚠️ **人が境界をドラッグした後は `S-80` が勝つ** （`FR-052`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-142 | マイルストーンの図形の一覧を開いているか（`FR-053`） | `false` | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-99e` と同じ性質である。<br>⛔ **`Esc` では閉じない** —— 重ねて開く「面」ではなく、パレットの中の並びが伸びるだけなので、`S-99g` に当たらない |
| S-216 | コマンドパレットに常に出すマイルストーンの図形の数（`FR-053`） | 6 | 同上。<br>⭐ 表 T-012 の `SH-5` が面積順に並べるはじめの 6 つ、すなわち 〇 から ☆ までである。<br>⛔ **残りは `S-142` の一覧を開くまで出さない** —— 規則は `FR-053` が持つ。<br>⚠️ **面積順を持つのは `SH-5` のはじめの 8 つだけである** （`FR-078`）—— あとの 7 つは面積の順を持たないので、常に残りの側に入る |
| S-200 | `Command Palette` を最小化しているか（`FR-053`） | `false` | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-99e` と同じ性質である。<br>⛔ **`S-99e`（表示しているか）とは別である** —— 最小化は出ている状態の一種であり、非表示にすることではない。<br>⛔ **`Esc` では戻らない** —— 重ねて開く「面」ではないので `S-99g` に当たらない |
| S-201 | ヘルプが画面に占める割合（`FR-036`） | 0.95 🔎 | 同上。<br>⭐ **幅と高さの両方に同じ割合を当てる**。<br>⛔ **px で持たない理由は `FR-036` が持つ。<br>** ⚠️ **1 を超えてはならない** —— 画面より大きいヘルプは、閉じる入口ごと外へ出る |
| S-202 | ヘルプの段の数（`FR-036`） | 3 | 同上。<br>⭐ **`FR-036` が名指す項目を 3 段に割り付けるための値である**。<br>⛔ `FR-036` は「基準環境では 1 画面に収め、スクロールを要さないこと」と定めており、段の数はそれを満たすための割り付けである —— 89 項目を 1 段に積むと約 1538px（出荷ビルドの行送り 17.28px × 89）になり、基準環境の段の高さ 808px（出荷ビルド、1920 × 1080）に入らない。<br>⚠️ **これより低い画面ではスクロールを許す** （同要求）。<br>⭐ どの塊をどの段に置くかは `FR-036` の 表 T-256 が持ち、本行は段の数だけを持つ —— 同表の行の数は本行と一致する（同要求） |
| S-203 | ヘルプの文字の大きさの係数（`FR-036`） | 0.80 🔎 | ⭐ **ヘルプの字をアイコンの大きさに合わせるための係数である**。<br>⛔ **px で持たない理由は `FR-036` が持つ。<br>** ⚠️ 掛ける相手は宿主の地の文字であり、`fontScaleSizes`（表 T-215）ではない —— ヘルプは日程の絵ではなく、その周りの枠である（`S-197` と同じ立場） |
| S-204 | ツールチップの文字の大きさの係数（`FR-092` の `EZ-2`） | 0.875 🔎 | ⭐ **16pt を 14pt ほどに小さくする比である** （14 ÷ 16）。<br>⛔ **px で持たない理由は `S-203` と同じである。<br>** ⚠️ **「2 段階」は 表 T-215 の段ではない** —— 文字サイズの段のことである |
| S-205 | `Scrollbars` の太さの下限（`FR-051`） | 8px 🔎 | ⛔ **太さそのものではない** —— 太さは環境から確定させる値であり、`FR-051` が設定値として持つことを禁じている。<br>**本行が持つのは「これ以下では指せない・掴めない」という、この道具が決める下限** であって、環境ごとに違う値ではない。<br>⭐ **要る理由** —— 重ね表示のスクロールバーを持つ環境は既定が **0** を返し、その半分は 0 になる。<br>⚠️ **`S-138`（入口の図形の一辺 12px）より細い** —— 帯は目印であって指の的ではなく、掴む的は 表 T-023d の当たり判定が別に持つ |
| S-143 | パレットの群を隔てる線の太さとまわりの空き（`FR-053`） | 1 × 6px | 同上。<br>⭐ **2 つ目の数は上下左右のすべてに当たる** —— ⛔ **上下だけの数を別に持たない。<br>** 罫のまわりを等方にするので、**新しい数を 1 つも起こさない。<br>** ⛔ 左右だけに当ててはならない（MUST NOT）—— 上下が 0px となり、群のボタンが罫に接する。<br>⛔ 線であって文字ではない。<br>理由は `FR-053` が持つ。<br> ⚠️ **図形でもない** ので 表 T-109 の行も 図 F-019 の図形も要らない |
| S-172 | 入口を押し続けたとき、連続に変わるまでの待ち時間（`FR-018`） | 1000ms | 同上。<br>⛔ **1 秒以上の長押しで連続とする。<br>** ⚠️ **次の行の刻みより長いことを `FR-018` が求める** |
| S-173 | 押し続けているあいだ、倍率を刻む間隔（`FR-018`） | 120ms 🔎 | 同上。<br>⛔ **繰り返しの速さは読む人のアクセシビリティに属する** —— 掴み代の行と同じ論であり、文書が強制してよい値ではない。<br>⛔ **測って決めた値ではない** （🔎） |
| S-174 | 選択の枠の太さ（表 T-023c の `SL-8`） | 1px 🔎 | 同上。<br>⛔ **倍率に追随させない** —— 規則と理由は `SL-8` が持つ。<br>⭐ 1 は、利用者が「もう少し細く」と求めて置いた試す値である —— 描いた絵を見て選び直す。<br>⛔ **測って決めた値ではない** （🔎） |
| S-175 | 選択の枠の破線の刻み（表 T-023c の `SL-8`） | 2 × 1px 🔎 | 同上。<br>⭐ **描く長さと空ける長さの組である。<br>** ⭐ 2 × 1 は、利用者が「線の間隔を縮めよ」と求めて置いた試す値である —— 描いた絵を見て選び直す。<br>⛔ **測って決めた値ではない** （🔎） |
| S-178 | 選択された線の太さの倍率（表 T-023c の `SL-8`、表 T-029a の `DC-8`） | 2× 🔎 | 同上。<br>⭐ **絶対値ではなく、その線自身の太さに掛ける倍率である** —— 依存線は `S-18`、基準日線は `S-333`、カーソルは `S-194` を、線自身の太さとして持つ。<br>⛔ **測って決めた値ではない** （🔎） |
| S-224 | 交差を読ませる縁の太さの倍率（`FR-009`） | 3× 🔎 | 同上。<br>⭐ `S-178` と同じく、その線自身の太さ（依存線なら `S-18`）に掛ける倍率である —— 絶対値ではない。<br>⛔ **1 以下にしてはならない** —— 本線より細い縁は奥の線を途切らず、`FR-009` の「どちらが手前かが読める」を果たせない。<br> ⚠️ **大きくしすぎると、密な図で隣の線を食う。<br>** ⭐ 3 を薦めるのは、依存線の太さの両側に本線 1 本分ずつの地色が出る幅であるからである。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-94 | 取り消しの段数 | 50 | 動いている端末のメモリ予算。<br>**履歴自体を保存しないのに回数だけ文書が指定するのは筋が通らない** |
| S-95 | 取り消しの合計メモリ上限 | `64` MB 🔎 | 同上。<br>**値の由来は無い** —— 動いている端末の予算から選び直す。<br>**1 MB = 1024 × 1024 バイトとする** （`S-113` と同じ） |
| S-96 | `zoomStep` | `S-53` | **操作の速さであって結果ではない。<br>** 1 ノッチで何倍動くかは入力装置に依存する。<br>結果の絵は変わらない |
| S-97 | `zoomMin` | `S-54` | 同上。<br>倍率そのもの（`zoomX` / `zoomY`）は保存するので再現性は損なわれない |
| S-98 | `zoomMax` | `S-55` | 同上 |
| S-99a | 透かしに出す**開いた者の名前** | `user` | 別枠。<br>`localStorage` に置く。<br> 文書に保存しない規則と理由は `FR-020` |
| S-99c | 利用者が設定した**透かし解除パスワードの SHA-256** | — | 別枠。<br>`localStorage` に置く。<br> 保存の規則は `FR-020` と `FR-086` が持つ。<br>設定されていなければ表 T-207 の既定値を使う |
| S-99b | `Agent API` を有効にした文書の記録 | — | 別枠。<br>`localStorage` に置く。<br> `FR-065` が「ブラウザ（オリジン）ごとに記憶する」と定めるので、**オリジンごとに 1 つ** 置く。<br>**有効化は読む人の判断であって文書の内容ではない。<br>** ⚠️ 文書の識別子と対にしては置けない —— その識別子は仕様のどこにも無い |
| S-99 | `language`（`ja` / `en`） | — | 別枠。<br>`localStorage` に置く。<br> 保存しない規則と理由は `FR-038`。<br>⚠️ **表 T-202 に行が無いので `FR-049` の切り替え対象ではない** |
| S-99e | コマンドパレットの表示状態 | 表示 | 画面の使い方であって文書の内容ではない（`FR-053`。<br>切替は表 T-036 の `SK-14`） |
| S-99f | 全画面表示かどうか | 通常 | 同上（`FR-071`。<br>ブラウザの全画面表示の状態を写す値である。<br>入口は表 T-036 の `SK-15` と `_assets/tbl-glossary.md` の 表 T-109 の `IC-11`） |
| S-99g | 開いている面 | 開いていない | 同上。<br>「面」とは、画面の上に重ねて開き、`Esc` の段（表 T-028 の `IN-4`）の「開いている面」で閉じられるものをいう。<br>⚠️ プロパティパネルは面ではない —— 同行は、進行中のドラッグの次にパネルの段を別に置いている（`S-99h`） |
| S-99h | プロパティパネルを出しているか（`選択` ／ `文書全体の設定` ／ 出していない） | 出していない | 同上。<br>⛔ **幅（`S-80`）とは別に持つこと**—— ⚠️ **幅 `0` は「まだ広げていない」であって「出していない」ではない。<br>**`FR-052` は境界のドラッグで幅を 0 まで畳めると定めており、幅で有無を決めると、畳んだ人が次に開くときの幅を失う。<br>⭐ **既定が「出していない」なのは `FR-080` の基準環境である。<br>** ⚠️ **`S-99g` の「面」ではない** —— パネルは画面の上に重ねず、`Esc` の段も面とは別である —— 表 T-028 の `IN-4` は、面より下、進行中のドラッグの次にパネルの段を与えている |
| S-99i | 対話欄（`_assets/tbl-glossary.md` の `U-44`）を表示しているか（`FR-066`） | 表示 | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-99e` と同じ性質である。<br>⛔ `Agent API` が有効かどうか（`S-99b`）とは別の値として持つこと。<br>1 つの値で兼ねてはならない —— **あちらは能力であり、本行は見え方である。<br>** ⚠️ 兼ねると、`Agent API` を有効にしただけで `IC-18` の押下状態が立つ。<br>⭐ 既定が「表示」なのは、`FR-066` が有効なあいだ欄を表示すると定めているからである —— **有効にした人が、もう一度別の入口を押すまで何も出ない形にしない。<br>** ⚠️ **`S-99g` の「面」ではない** —— **理由は `S-99h` と同じであり、ここでは繰り返さない。<br>** |
| S-144 | 透かしを出すか（`watermarkVisible`） | 出す | ⛔ **文書に保存しない** —— 規則は `FR-020` が持つ。<br>⚠️ **表 T-202（文書の値）ではなく本表に置く** —— **理由は `FR-020` が持つ。<br>** ⭐ 入口は 表 T-109 の `IC-41`。<br>出ているときだけ解除パスワードを求め、戻す側は問わない （`FR-020`）。<br>⭐ **画面から消したときは書き出す絵からも消える。<br>** ⚠️ **`FR-049` の切り替えの対象ではない** —— **同要求が見るのは 表 T-202 の真偽の行である。<br>** |
| S-206 | 操作と描画を記録しているか（`FR-102`） | `false` | 同上。<br>⭐ **画面の使い方であって文書の内容ではない** —— `S-200` と同じ性質である。<br>⛔ **既定が `false` なのは、記録が読む人の判断で始まるものだからである。<br>** ⚠️ **`Esc` では止まらない** —— 重ねて開く「面」ではないので `S-99g` に当たらない |
| S-207 | 記録に残せる件数の上限（`FR-102`） | 2000 🔎 | 同上。<br>⛔ **上限が無いと、長く回した人の記録が貼れない大きさになる** （`FR-102`）。<br>⚠️ **上限に達したあとの捨て方は `FR-102` が持つ。<br>** ⛔ **測って決めた値ではない** （🔎） |
| S-208 | 掴んだ行の軸が決まる距離（表 T-051 の `HF-15`）と、図形を置くときに押しと引きを分ける距離（`FR-001` / `FR-019`） | 6px 🔎 | `S-138` と同じ理由で保存しない。<br>**この距離を超えて最初に動いた向きで軸が決まり、離すまで変わらない** （表 T-051 の `HF-15`）。<br>⛔ **0 にしてはならない** —— **1 画素の震えで軸が決まってしまう。<br>** ⚠️ **段送りの刻みは本行ではない** —— **刻みは 表 T-201 の `S-37` と同じであること** を `HF-15` が定める。<br>⛔ **刻みを本表に持たせてはならない** —— **同じ数が 2 か所に載り、必ず離れていく** 。<br>⛔ **測って決めた値ではない** （🔎）⭐ **図形を置くときの境目にも使う** —— **同じ手の動きに同じ値を使う。<br>** ⛔ **別に持ってはならない** —— **片方だけ動いたとき、同じ手つきが場所によって違う意味になる。<br>** |
| S-210 | ヘッダーの更新日時の文字の大きさの係数（`FR-101`） | 0.75 🔎 | ⭐ **文字を 2 段小さくする比である**。<br>⛔ **px で持たない理由も、掛ける相手も `S-203` と同じである。<br>** ⭐ **ファイルの名前にも掛かる** —— **係数は名前と日時の両方を抱える箱に宣言され、名前が継ぐ。<br>** ⚠️ 実測（出荷ビルド）: 名前も日時も 12px、`Document Title` は 16px。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-212 | 拒まれた向きへ掴んだ行が追従する割合（表 T-051 の `HF-15`） | 0.4 🔎 | 同上。<br>⭐ **掛ける相手は、その軸の 1 歩ぶんである** —— 左右なら 表 T-201 の `S-37`、上下ならその行が占める送りである。<br>⛔ **0 にしてはならない** —— **まったく動かないと、掴めていないのか拒まれているのかが読めない。<br>** ⛔ **1 にしてはならない** —— **拒んだのに付いて行くので、手応えが返らない。<br>** ⭐ **0.4 を薦めるのは、1 歩の半分に届かないからである** —— **半分を超えると、次の歩へ入ったように見える。<br>** ⛔ **測って決めた値ではない** （🔎） |
| S-213 | 行の辺に引く帯の太さ（表 T-051 の `HF-15` と `HF-18`） | 3px 🔎 | 同上。<br>⭐ **2 つの帯で 1 つの数である** —— `HF-15` が掴んでいる行に描く軸の帯と、`HF-18` が抱えている行の左の辺に引く帯は、**どちらも「行の辺に引く 1 本の帯」であり、太さを別々に持つ理由がない。<br>** ⛔ **2 行にしてはならない** —— **同じ数が 2 か所に載ると、必ず離れていく** （`S-208` が同じ理由で刻みを拒んでいる）。<br>⚠️ **見本は両方とも 3px で描いている** （`previous-project-result/11-row-controls/row-controls-sample.html` の `.row.holds` と `.row.axisV` / `.row.axisH` を実測）。<br>⛔ **罫の色（`S-149`）の 1px では「帯」にならない。<br>** ⛔ **測って決めた値ではない** （🔎） |
| S-214 | 状態を地で薄く示すときの濃さ | 9% 🔎 | 同上。<br>⭐ `FR-029` がポインタの乗った入口に敷く地と、`FR-098` が留めた行に敷く地の、両方が読む —— **1 行である理由は `S-213` が述べたものと同じである。<br>** ⚠️ **掛ける相手までは同じではない** —— 入口の側は 表 T-236 の `S-147`、行の側は同表の `S-151` であり、それぞれの要求が名指す。<br> ⚠️ **見本の実測値である** （`row-controls-sample.html` の `.e:hover` と `.row.pinnedTop`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-215 | 掴んでいる行の地の濃さ（表 T-051 の `HF-15`） | 12% 🔎 | 同上。<br>⭐ **`S-214` より濃い値を別に持つ** —— 留まっているだけの行といま手が持っている行が同じ画面に並ぶので、同じ濃さでは見分けられない。<br> ⚠️ **見本は 9% と 12% で描き分けている** （同書の `.row.dragging`）。<br>⛔ **測って決めた値ではない** （🔎） |
| S-211 | 段 0（行見出しパネルの頭）が畳まれているか（表 T-015 の `HR-2`） | 畳まれていない | 同上。<br>⭐ `HR-2` の「押すと行が 1 つも描かれない状態になりうる」を成り立たせるのは本行であり、その理由も、列を増やさないという規則も同行が持つ。<br> ⛔ **保存しない** —— `S-99g` と同じ立場であり、画面の状態であって日程の内容ではない。<br>⭐ **戻す道は 3 つある** —— 表 T-051 の `HF-16`（段 0 を 1 階層開く）と `HF-10`（すべて開く）、そして `HF-17`（最も浅い段へ行を足す）である。<br>⚠️ **`HF-17` が戻す道になるのは、足すときに段 0 を 1 階層だけ開くからである** —— 足した行が見えないまま名づけさせないためである。<br> ⭐ **何階層開くかは `HF-17` が持つ** |
| S-219 | 目盛の曜日の段の文字の大きさの係数（`FR-017`） | 0.6 🔎 | ⭐ **英語の曜日（`Mon` など）が日の段の幅を決めてしまうので、そこだけ縮める**。<br>⛔ **family の差し替えの禁止は 表 T-238 の直下が持つ** —— `FR-039` が読む人の字を尊重すると定めているためでもある。<br>⛔ **画面の道具の寸法であり、文書の内容ではない。<br>** ⭐ **0.6 とするのは、日の段が 21.11px/日 で出るからである** —— `S-85` が 17.5 なので、そこに英語の曜日が入らねばならない。<br> ⭐ 実測（出荷ビルド、表示言語 en）: `Wed` の幅 17.7px（字 8.4px）、隙間 3.4px、重ならない。<br> ⛔ **0.8 では 22.6px で入らない。<br>** ⭐ **暫定の値である** —— 日本語（水 木 金…）と英語（Wed Thu Fri…）の両方を出荷ビルドで見て、読めることを確かめた。<br> ⚠️ **暫定なので、狭い画面や高い画素密度で読みにくいと分かれば動かしてよい。<br>** |
| S-235 | `App Header` と `Command Palette` の縮尺（`FR-051` / `FR-053`）、および入口の図形の箱と隙間の縮尺（`FR-029`） | 0.6667 | ⭐ **利用者が「現行の 2/3」と指定した値である**。<br>⭐ 2/3 を小数第 4 位へ丸めた値である。<br>⛔ **表示の倍率（表 T-202 の `S-234`）を `App Header` と `Command Palette` に掛けてはならない** —— 規則は `FR-051` と `FR-053` が持つ。<br>⛔ **`S-236` と兼ねてはならない** —— あちらは日程表の絵の基準であり、片方だけを選び直すことがある（本値 0.6667 と `S-236` の 0.625 は既に違う）。<br>`S-138` と同じ理由で保存しない |
| S-236 | 表示の倍率が 100 のときの描く比（`FR-039` の 表 T-252） | 0.625 | ⭐ **利用者が、2026-09-16 の出荷ビルドの 2/3 × 3/4 の見た目をさらに 1.25 倍した見た目を表示の倍率 100 と定めた値である** —— 2/3 × 3/4 × 5/4 ＝ 5/8 なので、0.625 ちょうどであり、丸めていない。<br>⭐ 描く比の求め方は `FR-039` が持つ —— `S-234` の 100 で 0.625、200 で 1.25（出荷ビルドの 5/4）、50 で 0.3125 である。<br>⚠️ `App Header` と `Command Palette` の大きさは本行を読まない —— `S-235` だけを読む（`DS-6`）。<br>⭐ 保存しないのは、文書ごとの大きさを `S-234` が持つからである —— 本行は製品の目盛りの基準である。<br>⛔ **`S-235` と兼ねてはならない** —— 理由は同行が持つ |
| S-229 | 見えている範囲に残す日数の下限（`FR-016` の `zoomX` の上限） | 10 | ⭐ **最大で 10 日間まで拡大できる値である。<br>** ⛔ **`src/` に 10 を打ち込んではならない。<br>** ⭐ **上限そのものは倍率ではなく式である** —— `Row Area` の幅 ÷（本行 × 等倍のときの 1 日の幅）。<br>⇒ **画面の広さが変わっても「10 日は見えている」という約束が保たれる。<br>** ⚠️ **画面の道具の寸法であり、文書の内容ではない。<br>** ⛔ **測って決めた値ではない** |
| S-238 | 行の軸の上限（`FR-016` の帯の側）を探す倍率の刻み | 1.1× | ⭐ 利用者が選んだ値である —— 刻みの使い方は `FR-016` の 表 T-253 の `BC-1` が持ち、本行は刻みの大きさだけを持つ。<br>⭐ 保存しないのは、本行が決めるのは拡大の入力がどこで止まるかだけであり、止まった倍率そのもの（`S-76` の `zoomY`）は保存するからである —— 本行を保存しなくても、同じ文書から同じ絵が出る（`S-97` と同じ理由である）。<br>⛔ **`S-53`（1 ノッチで動く倍率）と兼ねてはならない** —— `S-53` は操作の速さであって結果ではない（`S-96` の注）ので、兼ねると読む人の操作の設定で上限が動く。<br>⚠️ 刻みを細かくするほど `FR-016` の代償（調べるどの倍率も含まない狭い区間を見落とす）は小さくなるが、上限を求めるために帯の高さを問う回数が、刻みの数に比例して増える。<br>⛔ 1 以下にしてはならない —— 倍率が上がらず、上限を探す道が終わらない。<br>⛔ 測って決めた値ではない —— 本行のほかの刻みと、費用も見落としも比べていない |
| S-239 | 行の軸の上限（`FR-016` の帯の側）を探すとき、刻みの中を詰めるのをやめる倍率の差 | 0.000001 | ⭐ 差の使い方と、帯の高さの差ではなく倍率の差で置く理由は `FR-016` の 表 T-253 の `BC-5` が持ち、本行は差の大きさだけを持つ。<br>⭐ 保存しない理由は `S-238` と同じである。<br>⚠️ 小さくするほど止まる位置は境に近づくが、半分に詰めるたびに帯の高さを 1 回問うので、問う回数が増える。<br>⛔ 0 以下にしてはならない —— 上端と下端の差がそれ以下にならず、詰めるのが終わらない。<br>⛔ 測って決めた値ではなく、利用者が選んだ値でもない |
| S-240 | 担当者の一覧（`Resource Roster`）の文字の大きさの係数（`FR-099` の 表 T-257 の `RR-1`） | 0.75 | ⭐ 利用者が「今の 75%」と指定した値である —— 係数を持たない一覧の字は宿主が与える地の文字の大きさであり、本行はその 0.75 倍を与える。<br>⭐ **px ではなく係数である** —— px で持たない理由は `S-197` と同じである。<br>⚠️ 掛ける相手と、その理由は `S-203` と同じである —— 一覧も日程の絵ではなく、その周りの面である。<br>⛔ 表示の倍率（表 T-202 の `S-234`）も `S-235` も掛けない —— 一覧は `App Header` でも `Command Palette` でもなく、日程表の絵でもない。<br>`S-138` と同じ理由で保存しない |
| S-241 | 担当者の一覧の欄を隔てる罫線の太さ（`FR-099` の 表 T-257 の `RR-5`） | 1px | ⭐ 利用者が「縦横1pxの罫線」と指定した値である。<br>⭐ **画面の px である** —— 表示の倍率（`S-234`）も `S-235` も `S-240` も掛けない（規則は 表 T-257 の `RR-5`）。<br>⛔ **`S-237`（入口の枠の線）とも `S-143`（パレットの群を隔てる線）とも兼ねてはならない** —— 数が同じだけで、あちらは入口の枠と群の罫であり、本行は一覧の欄の罫である。<br>`S-138` と同じ理由で保存しない |
| S-242 | 一覧を持つ確認の、見出し部と一覧を隔てる線の太さ（`FR-076` の 表 T-258 の `CQ-2`） | 1px | ⭐ 線を引くことは利用者が指定した（「ヘッダー部とタスク部の間に水平線を置き」）。<br>⛔ 太さは利用者が選んだ値ではなく、測って決めた値でもない —— 入口の枠（`S-237`）と、担当者の一覧の罫（`S-241`）と同じ 1px に揃えた。<br>⛔ **`S-241` と兼ねてはならない** —— 数が同じだけで、あちらは一覧の欄の罫であり、本行は問いと一覧の境目である。<br>⭐ 画面の px であり、表示の倍率（`S-234`）も `S-235` も掛けない —— 確認は日程表の絵ではない。<br>`S-138` と同じ理由で保存しない |
| S-243 | 行見出しパネルに載る入口の、図形と入口の枠の最低隙間（`FR-029`） | 1px | ⭐ 利用者が「行見出しパネルだけを詰めろ。 4 → 1」と指定した試す値である —— 描いた絵を見て選び直す。<br>⭐ 行の帯の床（`HF-19`）は、行の操作子の格子が縦に取る高さであり、`05-07-design.md` の 表 T-221 の `LF-3` は入口 1 つの縦を `S-138` と隙間で数え、格子をその 2 段ぶんとし、描くときは `FR-029` のとおり `S-235` を掛ける —— 床は 2 ×（16 ＋ 1 × 2）× 0.6667 ＝ 24.0012px である（隙間が `S-141` の 4 なら 2 ×（16 ＋ 4 × 2）× 0.6667 ＝ 32.0016px）。<br>⚠️ 縦を数える `LF-3` は枠の線の太さ（`S-237`）を数えない —— `FR-029` は `S-237` を外形の幅に加え、外形の縦は定めない。<br>⚠️ 詰めた代償は `FR-029` が持つ。<br>⛔ **`S-141` と兼ねてはならない** —— 面ごとに選び直す値であり、いまは数も違う。<br>⭐ `S-235` は掛けるが表示の倍率は掛けず（`FR-029`、`FR-039` の 表 T-252 の `DS-7`）、保存しない理由も `S-138` と同じである |
| S-313 | 行見出しパネルの右端と、並びのいちばん右の行の操作子の外形とのあいだ（表 T-051 の `HF-4`） | 4px | 出荷ビルドが使っていた値をそのまま行にしたものである。<br>⚠️ `S-235` も表示の倍率も掛けない —— 出荷ビルドは掛けずに描いている。<br>保存しない理由は `S-138` と同じである |
| S-244 | 表示の倍率を示すメッセージを出しておく時間（`FR-039` の 表 T-260 の `SE-3`） | 1500ms | ⭐ 利用者が「1.5秒で自動で消してよい」と指定した値である。<br>⚠️ 続けて押したときは、最後の押しから数え直す（`SE-4`）。<br>⛔ 表 T-037 の通知の時間ではない —— メッセージは通知ではない（`SE-5`）。<br>保存しないのは、画面の道具の時間であって文書の内容ではないからである |
| S-245 | タスクとマイルストーンの名称ラベルの字の太さ（`FR-039`） | 600 🔎 | ⭐ 利用者がタスクとマイルストーンの字をもう少し太くするよう求めて置いた試す値である —— 描いた絵を見て選び直す。<br>⭐ CSS の `font-weight` の数である —— 400 が標準、700 が太字である。<br>⚠️ 掛けるのは 表 T-012 の形状の名称ラベル（表 T-038 の `OC-1`）だけであり、行見出しの名前・担当と完了率の札・目盛の字は標準の太さのままである（規則は `FR-039`）。<br>⚠️ 字の幅の概算（`FR-093`、表 T-201 の `S-30`）は太さを数えない —— 太い字は横に広がるので、概算より実寸が広くなりうる（表 T-026 の `RC-3`）。<br>保存しないのは、字の描き方を製品が持ち、文書が持たないからである（`S-233` と同じ） |
| S-246 | 画面と書き出す絵の字の書体の並び（`FR-039`） | `"Yu Gothic UI", "Yu Gothic", YuGothic, "BIZ UDPGothic", sans-serif` | ⭐ 利用者が、Yu Gothic を第 1 とし、無ければ BIZ UDPGothic とするよう指定した並びである。<br>⭐ CSS の `font-family` の並びである —— 左から順に、閲覧環境に在る最初の書体で描く。<br>⭐ 最後の `sans-serif` は総称であり、並びのどの書体も無い閲覧環境でも字の書体が必ず 1 つに決まる。<br>⚠️ `Yu Gothic UI` と `Yu Gothic` は Windows の、`YuGothic` は macOS の名である。<br>⛔ 書体の素材を成果物に同梱しない —— 並べるのは名だけであり、表 T-003 の `CN-7` を守る。<br>⚠️ Windows の Yu Gothic は太さ 400 で細く描かれる —— 読めるかは、利用者が描いた絵を見て確かめる項目である（名称ラベルは `S-245` の太さで描く）。<br>保存しない理由は `S-245` と同じである |
| S-247 | 実績のダミーを描く幅の、そのタスクの進捗マーカーの径に対する比（表 T-240 の `DM-3`） | 0.5 | ⭐ 利用者が「今マーカーの50%の幅にしろ」と指定した値である。<br>⭐ 径の求め方は 表 T-240 の `DM-3` が持つ —— 既定の表示の倍率 100 の矩形では、径 22.4 × 0.625 ＝ 14px に対して幅 7px である。<br>⚠️ 横の倍率（`zoomX`）に依らない —— 1 日の幅が数 px を切る倍率でも、印は同じ幅で立つ。<br>⛔ 描いた幅の上限は `S-180` が持つ。<br>保存しない理由は `S-180` と同じである |
| S-248 | プロパティパネルの幅の下限（`FR-052`） | 160px 🔎 | ⭐ 前に立つ者が提案し、利用者が試すよう答えた値である —— 描いた絵を見て選び直す。<br>⭐ 境界のドラッグはこの幅で止まり、保存された 表 T-203 の `S-80` が本値を下回る文書では、面を出すときに `S-171` の幅で描く（規則と理由は `FR-052`）。<br>⚠️ `S-80` の下限 0 は変えない —— 0 は「まだ広げていない」である（`S-80` の注）。<br>⚠️ パネルの幅なので、日程表の絵に掛ける比はどれも掛からない。<br>`S-138` と同じ理由で保存しない。<br>⛔ 測って決めた値ではない |
| S-249 | 箱の矢印と線の矢印のポインタの画像の一辺（表 T-269 の `PK-1` / `PK-4`） | 16px 🔎 | ⭐ 矢印を描く正方形の一辺であり、押さえる点はその中心に置く。<br>⚠️ 試す値である —— 指定も測りも持たないので、描いた絵を見て選び直す。<br>⚠️ ポインタの画像に表示の倍率を掛けない規則は 表 T-269 の後の段が持つ。<br>`S-138` と同じ理由で保存しない |
| S-294 | フェードの三角のポインタの画像の大きさ（表 T-269 の `PK-3`） | 10 × 7.5px | 同上。<br>⭐ 縦は横の 0.75 である。<br>直角が押さえる点であり、入は左上へ、出は右下へ広がる |
| S-295 | 円のポインタの径（表 T-269 の `PK-5` の ○ と ●） | 12 / 8px | 同上。<br>⭐ 白抜きの ○ が予定、塗りの ● が実績とダミーである —— 実績の方を小さくするのは、菱形の実績が予定より小さく描かれることに揃えたものである |
| S-296 | 再開の折れ矢印のポインタの画像の一辺（表 T-269 の `PK-9`） | 12px | 同上。<br>⭐ 押さえる点は折れ目（縦棒と腕の交わる点）であり、アイコンが表す再開日の左の境目に当たる。<br>⭐ 12px は利用者が指定した値である |
| S-297 | ポインタの画像に共通の縁の太さ（表 T-269） | 0.47px 🔎 | 同上。<br>⭐ どの形も同じ太さの縁を持つ —— 理由は `FR-106` の結びが持つ。<br>⛔ 測って決めた値ではない |
| S-325 | 名称ラベルに添える予定日の文字の大きさの、名前の字の大きさに対する係数（`FR-002`、表 T-251） | 0.85 | 見せ方の比であり、日程の内容ではないので保存しない。<br>⭐ **px ではなく係数である** —— 名前の字は表示の倍率と縦の倍率に従うので、日付の字もそれに付いて行く。<br>⭐ 掛ける範囲は予定日の文字と、その前に置く半角空白である（`FR-002`） |
| S-328 | 遅れの記号 `(!)` の縦棒の太さの、`S-24` に対する係数（`FR-013`） | 1.7 🔎 | 見せ方の比であり、日程の内容ではないので保存しない。<br>⭐ 記号の形は `FR-094` の宣言した寸法だけで描く —— 本行と `S-329` 〜 `S-331` がその宣言である。<br>⛔ **測って決めた値ではない** （🔎） |
| S-329 | 遅れの記号 `(!)` の縦棒の下端の位置 —— 記号の半分の高さ（`S-341`）に対する比、中心から下へ（`FR-013`） | 0.15 🔎 | 見せ方の比であり、日程の内容ではないので保存しない。<br>⭐ 縦棒と点のあいだの隙間は本行で開く —— 小さくすると隙間が広がる。<br>⛔ **測って決めた値ではない** （🔎） |
| S-330 | 遅れの記号 `(!)` の点の中心 —— 記号の中心から下への距離を、記号の半分の高さ（`S-341`）に対する比で（`FR-013`） | 0.8 🔎 | 点は縦棒の下端（`S-329`）より下に置き、あいだを空ける。<br>日程の内容ではないので保存しない。<br>⛔ **測って決めた値ではない** （🔎） |
| S-331 | 遅れの記号 `(!)` の点の半径 —— マーカーの円の半径に対する比（`FR-013`） | 0.14 🔎 | 見せ方の比であり、日程の内容ではないので保存しない。<br>⛔ **測って決めた値ではない** （🔎） |
| S-332 | 全体表示で横に残す余白の、`Row Area` の幅に対する比（片側。<br>`FR-055`） | 0.025 | 全体表示の見せ方であり、日程の内容ではないので保存しない。<br>⭐ **左右それぞれに取る** —— 両側で `Row Area` の幅の 2 倍の比になる |
| S-333 | 基準日線の太さ（表 T-029 の `CU-1`、`FR-046`） | 3px | 選ばれた基準日線は、本行の太さに `S-178` を掛けて描く（表 T-023c の `SL-8`）。<br>線の太さは見せ方であり保存しない |
| S-334 | `Dual Cursor` の読み出しの文字の大きさの係数（表 T-029a の `DC-3`） | 0.4375 | ⛔ **px で持たない理由は `S-204` と同じである。<br>** ⭐ `S-204`（0.875）の半分である —— 読み出しはポインタのそばに出て日程を覆うので、ツールチップより小さくする。<br>⚠️ `S-204` から導かず、本行が値を持つ —— ツールチップの字を変えても読み出しの字は動かない。<br>⭐ 下限は `S-340` |
| S-339 | ツールチップと窓の縁のあいだに残す余白（表 T-028 の `IN-7`） | 8px 🔎 | 説明の見せ方であり、日程の内容ではないので保存しない。<br>⭐ 説明の幅の上限は、窓の幅から両側に本行を引いた幅である —— 描き手はこれを窓の幅に従う書き方（例: `calc(100vw - 2 × 本行)`）で持ち、窓の大きさを読みに行かない。<br>⛔ **測って決めた値ではない** （🔎） |
| S-340 | `Dual Cursor` の読み出しの文字の大きさの下限（表 T-029a の `DC-3`） | 10px | ⭐ `S-334` を掛けた大きさが本行を下回るときは、本行の大きさで描く —— 宿主の地の文字が 16px のとき `S-334` だけでは 7px になり読めない。<br>⚠️ px で持つのは、下限は読める字の大きさの絶対の床だからである |
| S-335 | 透明の見本の市松の、1 辺のますの数（表 T-017b の `CV-9`） | 2 | 欄の見せ方であり、日程の内容ではないので保存しない。<br>⭐ 見本は `S-335` × `S-335` のますに割る |
| S-338 | プロパティパネルの色の欄の、1 段に並べる見本の数（表 T-017b の `CV-9`） | 5 | 欄の見せ方であり、日程の内容ではないので保存しない。<br>⭐ パレットの 10 色（表 T-294 の、透明を除く名）が 2 段に並ぶ |
| S-341 | 進捗マーカーの記号の半分の高さの、マーカーの円の半径に対する比（表 T-021 の `PM-2` ・ `PM-3` ・ `PM-4`、`FR-013`） | 0.5 | 見せ方の比であり、日程の内容ではないので保存しない。<br>⭐ `S-329` と `S-330` は、この長さに対する比である |

**表 T-207 — 透かし（成果物に埋め込む定数、文書には保存しない）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-100 | 既定の透かし解除パスワード | `goodrelax-scheduler` | — | — | 保存の規則は `FR-020` が持つ。<br>ここに書くのは仕様として値を定めるためであり、成果物へ入るのは下の SHA-256 だけである。<br>⚠️ **汎用の道具に固有名を既定値として置かないため、既定値は製品名とする。<br>** |
| S-101 | 既定の透かし解除パスワードの SHA-256 | `e2b7f98dfe8145444b33263989fe5e47f9150fe1ef6460713268af974e6df134` | — | — | UTF-8・末尾の改行を含めない。<br>**成果物へ定数として埋め込むのはこの値である** |
| S-102 | `watermarkOpacity` | `0.06` 🔎 | 0.02 | 0.30 | 透かしの濃さ。<br>**コントラストの適合範囲から外してある** （`NFR-007` / `LM-13`）ので、可読性ではなく「よく見れば分かるが通常は気にならない」を狙う値である。<br>⭐ **薄いほうがよい、が狙いである**。<br>⛔ その規則は `FR-020` が持つ。<br>ここに書き写さない。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-220 | 透かしを斜めに刷る角度 | `-30` | `-90` | `90` | 単位は度（`FR-020`）。<br>⭐ 右上がり。<br>日程のバーは横に伸びるので、水平から充分離れ、かつ読める。<br> ⛔ **測って決めた値ではない** （🔎） |
| S-221 | 透かしの文字の大きさの係数 | `0.01125` | `0.01` | `1` | 掛ける相手は書き出す絵の幅（`S-81`）である（`FR-020`）。<br>⭐ **画面の大きさが変わっても割合が保たれる。<br>** ⭐ **参考の実装** （`temp/gr-scheduler_current_io.html`。<br>リポジトリの追跡外）は、字 18px・濃さ 0.06・色 `#888888`・間隔 260px であった —— 札の幅がおよそ 243px なので、間隔のほうが広い。<br>だから重ならない。<br> ⇒ **参考の 18px を書き出す絵の幅 1600 で割って割合にした。<br>** ⛔ **絶対の px にはしていない** —— 本行の「画面の大きさが変わっても割合が保たれる」という理由が失われるからである。<br> ⭐ 実測（出荷ビルド、1920 × 1080）: 画面で 21.6px、書き出す絵で 18px。 |
| S-222 | 透かしの繰り返しの間隔の係数 | `14.44` | `1` | `20` | 掛ける相手は透かしの文字の高さである（`FR-020`）。<br>縦横とも同じ間隔とする。<br>⭐ **14.44 は、参考の 260px ÷ 18px から出した。<br>** ⛔ **3 では重なる** —— 実測（`S-221` が 0.08、本値が 3 のとき）: 字 153.6px に対し札の幅がおよそ 2000px、間隔 460.8px で、横に 4〜5 重に重なり「誰が・いつ」が読めなかった。<br> ⭐ 実測（本値 14.44 のとき）: 札の幅 282px、間隔 312px。<br>重ならない。 |

**表 T-236 — 画面の色（成果物に埋め込む定数、文書には保存しない）**

| 行 ID | 色 | 明るいテーマ | 暗いテーマ | 色相追随 | 備考 |
| --- | --- | --- | --- | :--: | --- |
| S-146 | 地の色 | `#ffffff` | `hsl(H 12% 9%)` | ○ | 地。<br>⛔ **塗らないと OS の既定が出る** —— 暗いテーマを選んでも白いままになる |
| S-147 | 文字の色 | `#16181d` | `#e8eaee` | — | 主たる文字 |
| S-148 | 控えめな文字の色 | `#5b6068` | `#9aa1ab` | — | 副次の文字 |
| S-149 | 罫の色 | `hsl(H 14% 87%)` | `hsl(H 12% 23%)` | ○ | 区切りの線 |
| S-150 | パネルの地の色 | `hsl(H 20% 97%)` | `hsl(H 14% 13%)` | ○ | 行見出しパネル・プロパティパネル・パレットの地 |
| S-231 | 掴み代の印の色 | `hsl(H 14% 82%)` | `hsl(H 12% 28%)` | ○ | 行見出しパネルの行の掴み代の印（表 T-051 の `HF-15`）。<br>⭐ `S-149` の明度を、パネルの地（`S-150`）から 5 ポイント遠ざけた色である —— 色相と彩度は `S-149` のままとし、明度だけを動かす。<br>⭐ 実測（色相 214、パネルの地に対する WCAG のコントラスト比）: 明るいテーマ 1.271 → 1.445、暗いテーマ 1.411 → 1.711。<br>測り方は HSL を RGB へ換算し、相対輝度 L から (L1 + 0.05) ÷ (L2 + 0.05) を求めた。<br>⛔ `S-149` をそのまま使わない —— 罫と同じ色では掴めることが読みにくい |
| S-151 | 強調の色 | `hsl(H 59% 32%)` | `hsl(H 62% 68%)` | ○ | 選択と現在位置。<br>⭐ **明るいテーマの明度が 32% なのは、42% では `NFR-007` の 3 : 1 を割る色相が在るからである。<br>**⭐ **42% のとき、全 360 色相を 3 つの地に当てて実測した**: 地（`S-146`）に対し **107/360** 、パネルの地（`S-150`）に対し **139/360** 、**予定バーの塗り（`S-155`）に対し 179/360（49.7%）** が割れ、最悪は色相 60 の **1.88** 。<br>⭐ **32% では 3 つの地すべて・全 360 色相で最悪 3.11** となる。<br>⛔ **彩度は動かしていない** —— 明度だけで足りるので、動かす値を 1 つに留めた。<br>⚠️ **暗いテーマは 1 文字も変えていない** —— 同じ測り方で最悪 3.53 であり、元から満たしている |
| S-152 | 良の色 | `#1f7a3d` | `#6fc98d` | — | 順調 |
| S-183 | 構えている入口の塗りの色（`FR-053`） | `#1f7a3d` | `#6fc98d` | — | 構えている入口の図形の箱に敷く（`FR-029` の 表 T-237 の `EN-1`）。<br>⛔ **縁ではなく塗りである** —— ⚠️ **図形の箱を塗る色であって、縁を描く色ではない** —— 「縁の色」と読むと、忠実な実装は縁を描く。<br> ⭐ **`S-152` と同じ対を使う** —— 緑は「いま効いている」を表す色として本表が既に持っている。<br>⛔ **一旦の色である**。<br>⚠️ **色相に追随させない** —— 追随させると、`FR-041` が主題の色を動かしたときに構えの印が地に紛れる |
| S-195 | カーソルの色（表 T-029 の `CU-2` ・ `CU-3`） | `#c2188f` | `#f07ad0` | — | ⛔ **テーマの色相に追随させない** —— マゼンタ寄りの色であり、選択と現在位置の色（`S-151`）と見分ける。<br>2 本とも同じ色である —— どちらが追従中かは色で示さない（表 T-029a の `DC-8` が 表 T-023c の `SL-8` を引き、同行は線について太さだけを変えると定める）。<br>⭐ ガイドカーソル（`CU-3`）も本行で描く —— 2 つは排他であり（表 T-029a の `DC-9`）、同時には出ない。<br>⛔ **測って決めた値ではない** |
| S-153 | 注意の色 | `#a8600f` | `#e0a353` | — | 注意 |
| S-154 | 不良の色 | `#a02b2b` | `#e07a7a` | — | 遅れ |
| S-155 | 予定バーの塗り | `hsl(H 46% 80%)` | `hsl(H 32% 26%)` | ○ | 予定 |
| S-156 | 予定バーの線 | `hsl(H 44% 46%)` | `hsl(H 46% 66%)` | ○ | 予定の縁 |
| S-157 | 実績バーの塗り | `hsl(H 62% 34%)` | `hsl(H 62% 64%)` | ○ | 実績 |
| S-158 | 実績バーの線 | `hsl(H 66% 22%)` | `hsl(H 70% 80%)` | ○ | 実績の縁 |
| S-159 | 依存線の色 | `hsl(26 88% 44%)` | `hsl(30 92% 60%)` | — | ⛔ **テーマの色相に追随させない** （`FR-041`）。<br>⚠️ 明暗で色相が 26 → 30 へ動くのは実物がそうだからであり、理由は記録されていない |
| S-160 | イナズマ線の色 | `hsl(354 62% 42%)` | `hsl(354 70% 64%)` | — | ⛔ **テーマの色相に追随させない** （`FR-041`）。<br>⭐ **依存線とは別の色である** |
| S-312 | 注記の色 | `#b45309` | `#b45309` | — | 線の色を指定しないハイライトボックスの枠と、コメントボックスの枠と引出しの線（`FR-019`）。<br>テーマの色相に追随させない（`FR-041`）。<br>⚠️ 色相 26 は依存線（`S-159`）と同じであり、離すことは `FR-019` が SHOULD に留めている。<br>⚠️ 暗いテーマも明るいテーマと同じ値である —— 暗いテーマで選び直していない |
| S-161 | 進捗マーカーの文字色 | `#16181d` | `#e8eaee` | — | マーカーの記号 |
| S-162 | 進捗マーカーの地の色 | `S-146` に同じ | `S-146` に同じ | — | 同上。<br>⭐ **`S-146` を継ぐ** —— マーカーは地の上に浮くので、地と同じでないと記号の抜きが濁る |
| S-310 | 再開アイコンの色 | `S-161` に同じ | `S-161` に同じ | — | 同上。<br>⭐ **進捗マーカーの記号と同じ色を継ぐ** —— 再開アイコンは進捗マーカーと同じ切り替え（`S-63`）で出入りし、同じ族の印だからである |
| S-311 | 停止日から再開アイコンへ引く破線の色 | `S-158` に同じ | `S-158` に同じ | — | 同上。<br>⭐ **実績の縁を継ぐ** —— 破線は実績の終わりからアイコンまでを結ぶ線であり、実績の一部として読ませる |
| S-163 | 基準日線の色 | `#eb6101` | `#ff8c42` | — | 基準日。<br>⭐ **朱色である** —— 本表のほかの線の色（`S-160` のイナズマ線、`S-195` のカーソル）と見分ける。<br>⛔ **測って決めた値ではない** |
| S-164 | 行の帯の色 | `hsl(H 42% 96%)` | `hsl(H 18% 20%)` | ○ | ⛔ **いまは 1 本も描かれていない** （`FR-042`） |
| S-165 | 行の帯の縁の色 | `hsl(H 34% 88%)` | `hsl(H 16% 28%)` | ○ | 帯の縁 |
| S-166 | 最上位の行の地の色 | `hsl(H 40% 97%)` | `hsl(H 20% 17%)` | ○ | 深さ 1 の行 |
| S-167 | 交互の行の地の色 | `hsl(H 20% 99%)` | `hsl(H 14% 11%)` | ○ | 縞 |
| S-168 | ラベルの文字色 | `#000000` | `#ffffff` | — | バーの上の文字 |
| S-169 | ラベルの縁取りの色 | `S-146` に同じ | `S-146` に同じ | — | 同上。<br>⭐ **`S-146` を継ぐ** —— 縁取りはバーの色から文字を切り離すためのものなので、地と同じ色でなければ役に立たない。<br>太さは 表 T-201 の `S-34` |
| S-170 | 浮いた層の影 | `rgba(0,0,0,0.28)` | `rgba(0,0,0,0.6)` | — | パレットと面。<br>⚠️ **暗い側で濃くする** —— 明るい側の影は暗い地に溶けて層が浮かない |
| S-223 | 透かしのインクの色 | `S-148` に同じ | `S-148` に同じ | — | 透かしの文字を塗る色（`FR-020`）。<br>⭐ **読む字と同じ色を `S-102` の濃さで薄く重ねる** —— 別の色を与えると、薄いところで色味だけが残り、地と喧嘩する。<br>⭐ `S-148`（控えめな文字）を使い、`S-147`（主たる文字）は使わない —— 透かしのコントラストを小さくするためである。<br>`S-148` は明暗の両方を持ち、`S-147` と同じ色系統なので、上の「別の色を与えると」に当たらない。<br> ⛔ **参考の実装のように固定の灰（`#888888`）にはしていない** —— **暗い主題で地と近づきすぎるからである。<br>** ⭐ 実測（明るい主題）: `rgb(91, 96, 104)`。<br> ⛔ **濃さをここに書いてはならない** —— 表 T-207 の `S-102` が持つ |
| S-326 | 遅れのマーカーの地の色（`FR-013`） | `#ffd400` | `#e6c229` | — | 表 T-021 の `PM-4`。<br>⭐ **黄である** —— 遅れを最も見つけたい（`UC-006`）。<br>⛔ **テーマの色相に追随させない。<br>** ⛔ **測って決めた値ではない** |
| S-327 | 遅れのマーカーの記号の色（`FR-013`） | `#16181d` | `#16181d` | — | ⭐ **両テーマで同じ暗い色である** —— 地（`S-326`）が両テーマで明るい黄なので、`S-161` の暗いテーマの明るい記号色では `!` が地に沈む |
| S-336 | 透明の見本の市松の明るいます（表 T-017b の `CV-9`） | `#ffffff` | `#ffffff` | — | ⭐ 両テーマで同じ色である —— 市松は「透けている」ことを示す約束の絵であり、地の色に合わせると透明に見えない |
| S-337 | 透明の見本の市松の暗います（表 T-017b の `CV-9`） | `#c0c0c0` | `#c0c0c0` | — | `S-336` に同じ |

表 T-294 はパレット色（`01-04-requirements.md` の 表 T-017 の `CL-1`）が描く値である。  
保存するのは「保存する綴り」の欄の名だけであり、値は保存しない（同書の 表 T-017b の `CV-1`）。
値は `previous-project-result/08-poc/poc-integrated.html` の「色と縁取り」タブの確定した組（予定の塗り・予定の縁・実績の塗り）と行の帯の規則を、各色の色相に当てて求めた。  
テーマの色相には追随しない。
測り方（2026-09-22）: 比は WCAG 2.1 のコントラスト比で、丸めた `#rrggbb` の値から PoC 自身の輝度の式（同ファイル `:610-615`）で求めた。  
暗いテーマの地は `S-146` の 360 の色相の最悪と `#14161a` の悪いほうとした。  
モノクロは、その値を HSL の明度を保ったまま彩度 0 にして測った（`01-04-requirements.md` の `FR-041`）。

**表 T-294 — パレット色の値（成果物に埋め込む定数、文書には保存しない）**

| 行 ID | 色 | 保存する綴り | 明るいテーマの塗り | 明るいテーマの縁 | 明るいテーマの実績の塗り | 明るいテーマの行の帯 | 暗いテーマの塗り | 暗いテーマの縁 | 暗いテーマの実績の塗り | 暗いテーマの行の帯 | 備考 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S-314 | 白 | `white` | `#ffffff` | `#ffffff` | `S-157` に同じ | `S-146` に同じ | `#14161a` | `#14161a` | `S-157` に同じ | `S-146` に同じ | 地と同じ色である（PoC の地 `:41` / `:663`）。<br>暗いテーマでは黒と入れ替わる（`CV-6`）。<br>⚠️ 地の上では塗りも縁も見えない —— 表 T-017a の `CT-4` と `CT-5` を問わない（`CV-8`）。<br>実績の塗りはテーマの実績（`S-157`）を継ぐ —— PoC に白の実績の形が無い |
| S-315 | 黒 | `black` | `#000000` | `#000000` | `S-157` に同じ | —（行の色には選ばせない） | `#ffffff` | `#ffffff` | `S-157` に同じ | —（行の色には選ばせない） | ラベルの文字色（`S-168`、PoC `:72` / `:106`）と同じ値である。<br>暗いテーマでは白と入れ替わる（`CV-6`）。<br>実績の塗りはテーマの実績（`S-157`）を継ぐ —— 黒より濃い実績は無い。<br>行の帯を持たない —— 明るいテーマで黒の帯はラベルの黒い字を消す（`01-04-requirements.md` の 表 T-017b の `CV-9`） |
| S-316 | 濃い灰色 | `dimgray` | `#575757` | `#383838` | `S-157` に同じ | `#e0e0e0` | `#a3a3a3` | `#cccccc` | `S-157` に同じ | `#474747` | PoC の実績の組（`:55-56` / `:95-96`）を彩度 0（`:2733`）で描いた値である。<br>実績の塗りはテーマの実績（`S-157`）を継ぐ —— これより濃い灰で 3 : 1 に届く値が無い（明 2.95、暗 2.52）。<br>行の帯は PoC の帯の縁の灰であり、帯としての根拠は PoC に無い |
| S-317 | 薄い灰色 | `lightgray` | `#cccccc` | `#757575` | `#575757` | `#f5f5f5` | `#424242` | `#a8a8a8` | `#a3a3a3` | `#333333` | PoC の予定の組（`:53-55` / `:93-95`）を彩度 0 で描いた値である |
| S-318 | 赤 | `red` | `#e3b9b5` | `#a94c42` | `#8c2c21` | `#f9f1f1` | `#58312d` | `#d08880` | `#dc766a` | `#3c2c2a` | PoC の色相 6（`:707`）に、確定した組（`:53-56` / `:93-96`）を当てた値である |
| S-319 | 青 | `blue` | `#b5c9e3` | `#426ea9` | `#21508c` | `#f1f4f9` | `#2d3f58` | `#80a3d0` | `#6a9cdc` | `#2a323c` | PoC の色相 214（`:701`。<br>`themeHue` の既定と同じ）に、確定した組を当てた値である |
| S-320 | 黄 | `yellow` | `#dfd6a9` | `#8c7d36` | `#79691c` | `#f9f8f1` | `#58502d` | `#d0c380` | `#dcc96a` | `#3c392a` | PoC の色相 50（`:705`）。<br>明るいテーマは PoC の寄せの規則（`:2750-2760`）で k = 8 寄せた —— 寄せないと縁 ÷ 地が 2.89 : 1 で `CT-4` を割る。<br>k は、地と行の地（`S-164` / `S-166` / `S-167`、360 の色相の最悪）の両方で `CT-3` ・ `CT-4` ・ `CT-5` を満たす最小の値である |
| S-321 | 緑 | `green` | `#afe1bf` | `#3c9a5b` | `#1f8340` | `#f1f9f3` | `#2d583b` | `#80d09b` | `#6adc90` | `#2a3c30` | PoC の色相 140（`:703`）。<br>明るいテーマは `S-320` と同じ規則で k = 4 寄せた —— 寄せないと縁 ÷ 地が 2.96 : 1 で `CT-4` を割る |
| S-322 | オレンジ | `orange` | `#e3cab5` | `#a97242` | `#8c5321` | `#f9f5f1` | `#58412d` | `#d0a680` | `#dc9f6a` | `#3c322a` | PoC の色相 28（`:706`）に、確定した組を当てた値である |
| S-323 | 紫 | `purple` | `#d8b5e3` | `#8f42a9` | `#72218c` | `#f7f1f9` | `#4d2d58` | `#bc80d0` | `#c06adc` | `#382a3c` | PoC の色相 285（`:709`）に、確定した組を当てた値である |
| S-324 | 透明 | `transparent` | 描かない | 描かない | 描かない | 描かない | 描かない | 描かない | 描かない | 描かない | 塗りも線も描かない。<br>`null`（選んでいない）とは別物である（`_assets/tbl-glossary.md` の `P-19`） |

## 9. 後から選び直す値

**Type**: SECTION

**本節の表は、§1 の判定では文書に保存する側に落ちるものを含む。**  
§8 と分けてあるのは保存の可否ではなく、**由来が実測でなく、骨格で測って選び直す対象である** という括りだからである（表 T-026 の `RC-1`）。
各表の中で保存の可否を明記する。

**表 T-212 — 画面の各部の寸法の上限（`FR-051`）と、待ち時間**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-124 | `iconHintDelayMs` | `1000` ms 🔎 | 500 | 10000 | **説明を出すまでの待ち時間** （`FR-092` の `EZ-2`（アイコン）と `EZ-6`（タスク））。<br>⚠️ **1000 は、ポインタを当ててから 1 秒で説明を出すための値である**。<br>⚠️ **値は 1 つである** —— 場所ごとに違う待ち時間を持たない（`EZ-6` の MUST NOT）。<br>⚠️ **鍵の綴りは据え置く** —— 鍵は文書に載るので、綴りを変えると既存の文書の往復が壊れる |
| S-116 | `appHeaderMaxHeight` | `56` px 🔎 | 32 | 96 | **上部に使った高さはそのまま日程表から引かれる。<br>** 「視認できる範囲で最小」では合否を判定できないので、上限を数値で置く |

**表 T-211 — 受け入れの上限（`FR-023`）と構造の上限（`FR-004`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-113 | `importMaxBytes` | `32` MB 🔎 | 1 | 256 | 取り込むファイルの上限（`FR-023`）。<br>**1 MB = 1024 × 1024 バイトとする。<br>** |
| S-114 | `importMaxItems` | `20000` 🔎 | 1000 | 200000 | 取り込む `Task` の件数の上限。<br>**目標規模（表 T-025 の MC-7 は 1000）の 20 倍** 。<br>性能の目標より広く受理し、資源が尽きる手前で止める |
| S-115 | `importMaxDepth` | `64` 🔎 | 8 | 256 | WBS のネストの深さの上限。<br>**`maxGroupDepth`（`S-125`）とは別物** （WBS はクランプしない）。<br>**根の行を深さ 1 と数える** —— `wbsParentUid` が `null` の `Task` が深さ 1 である |
| S-125 | `maxGroupDepth` | `5` 🔎 | 3 | 8 | `TaskGroup` の深さの上限。<br>**推奨の深さは 3。<br>** 規則（上限を課すこと・LOD の判定でのみ頭打ちにしてよいこと）と理由は `FR-004`。<br>**根の行を深さ 1 と数える** （`S-115` と同じ）—— `parentId` が `null` の `TaskGroup` が深さ 1 である |
| S-133 | `carryMaxDepth` | `16` 🔎 | 4 | 64 | 解釈しない要素（`AT-126` の `children`）の入れ子の深さの上限。<br>⚠️ **`importMaxDepth`（`S-115`）とは別物** —— あちらは WBS であり、こちらは交換相手が運んできた解釈しない木である。<br>**根の要素を深さ 1 と数える** （`S-115` と同じ）。<br>由来の無い値であり、実測で選び直す |
| S-145 | 既定のファイル名 | `'grs-default.json'` | — | — | まだ 1 度もファイルへ保存していない文書が名乗る名。<br>**表 T-227 の `DI-1` が使えるようにするために要る** —— ⛔ **`DI-1` はファイル名を含み、名の無い文書では 3 つ組が揃わない。<br>** ⚠️ **未保存の文書どうしは同じ名になるので、`DI-1` は互いを同じ文書と答える** |

⚠️ 上限を超えた入力の扱いと、循環をこの上限では検出できないことは `FR-023` が定める。

**表 T-213 — 依存のラグ（`FR-009`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-117 | `dependencyLagDefault` | `0` | — | — | 依存を作ったときに置く値。<br>**0 は「間を空けない」** |
| S-118 | ラグの単位 | **稼働日** 🔎 | — | — | `FR-011` / `FR-012` / `FR-054` が使う単位に揃える。<br>**単位を定めないと、同じ値で実装ごとに違う日数になる** |

⚠️ **ラグが日付を動かさないことは表 T-018 が定める。**

**表 T-214 — 受け入れる日付の範囲（`FR-023`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-119 | `importMinDate` | `1970-01-01` 🔎 | — | — | これより前の日付を持つ入力を受け付けない |
| S-120 | `importMaxDate` | `2200-12-31` 🔎 | — | — | これより後を受け付けない。<br>**231 年ぶんの期間でも稼働日の数え上げが計算量に効く** （件数の上限とは別の経路） |

**表 T-215 — 文字サイズ（`FR-039` の `fontScale`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-121 | `fontScaleSizes.S` | `12` px 🔎 | `fontMin` | `fontScaleSizes.M` | **PoC の `fontMin` = 12 をそのまま S に据えた。<br>** 下限を割らない |
| S-122 | `fontScaleSizes.M` | `14` px 🔎 | `fontScaleSizes.S` | `fontScaleSizes.L` | |
| S-123 | `fontScaleSizes.L` | `16` px 🔎 | `fontScaleSizes.M` | 40 | |

⚠️ `fontScale` は読む人が文字を大きくするための設定であり（WCAG 1.4.4）、下限を割るためのものではない。  
公開実装は `S = 10` を持つが、これは `S-8`（`fontMin` の下限 12）と `FR-077` に反するので採らない。
⚠️ `fontScale` は目盛の文字（`S-3`）と目盛の帯の高さ（`S-2`）にも掛かるので、時間軸のしきい値の判定にも効く。  
判定の仕方は表 T-205 の注が持つ。

**表 T-210 — フェードの掴み点（`FR-075`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-109 | `fadeHandleHalfPx` | `2.5` px 🔎 | 2.5 | 8 | 掴み点の半辺。<br>**正方形 5 × 5px を点の中心に置く**。<br>⭐ 2.5 は、利用者が描く四角を 5 × 5px にするよう指定した試す値である —— 描いた絵を見て選び直す。<br>⚠️ 下限は本値と同じ 2.5 である —— 本値が自身の下限を割らないためであり、元の下限 3 は導きを持たない。<br>⚠️ 下限をさらに下げるときは、半辺を枠線（`S-110`）の太さの半分より大きく保つ —— 枠線の中心は正方形の辺に乗るので、半辺が太さの半分以下では線が正方形の内側を覆う |
| S-110 | `fadeHandleStrokePx` | `1.0` px 🔎 | 1 | 3 | 掴み点の枠線。<br>⭐ 1.0 は、`S-109` と同じ求めで置いた試す値である（小数を持てる値なので 1.0 と書く） —— 描いた絵を見て選び直す |
| S-111 | 掴み点を出す条件 | 選択中のタスクだけ 🔎 | — | — | 出す条件の規則と理由は `FR-075` |

⚠️ **当たり判定の大きさは 表 T-206 の `S-268` / `S-269`（8 × 8px）が別に持つ。**  
見た目の点（5 × 5px）より広く取る —— 掴み代は読む人のアクセシビリティに属する（理由の欄は 表 T-206 の `S-250`）。

**表 T-209 — 既定の暦と期間の換算（`FR-054`、取り込んでいない文書、および、取り込んだが値が空の文書が使う）**

| 行 ID | 名前 | 値 | 備考 |
| --- | --- | --- | --- |
| S-106 | 稼働する曜日 | 月・火・水・木・金 🔎 | **土日を非稼働とする。<br>** 特定の業種や地域を前提にしない範囲で最も広く使われる形である |
| S-107 | 例外日（休業日） | **無し** 🔎 | **祝日を持たない** （理由は `FR-088`）。<br>必要なら MSPDI を取り込むか、非稼働日として個別に置く |
| S-108 | 週の始まり | 月曜 🔎 | 週の目盛が書く日付の基準（`FR-054`）。<br>置き場は `Project.weekStartDay` |
| S-128 | 1 日あたりの分数 | `480` 🔎 | `Project.minutesPerDay` が空のときに使う（`FR-054`）。<br>1 日 8 時間。<br>選び方は `S-106` と同じである |

⚠️ 保存の可否: 保存する。  
規則と理由は `FR-054` が持つ。

⚠️ **本表の値は日程データの群に入る** —— 収まる先が暦（`Calendar`）と `Project` の列だからである。  
**どちらの群に属するかの判定は表 T-052 が持つ** （表 T-216 と同じ扱いである）。

**表 T-208 — 予実の補助線（`FR-084`）**

| 行 ID | 名前 | 値 | 下限 | 上限 | 備考 |
| --- | --- | --- | --: | --: | --- |
| S-103 | `planActualGuideWeight` | `1` px 🔎 | 0.5 | 2 | バーより目立つと、事実（バー）より補いの線が前に出るので細くする |
| S-104 | `planActualGuidePattern` | `2,2` 🔎 | — | — | 破線の刻み（描く長さ, 空ける長さ）。<br>刻みの規則は表 T-020a の `GD-6` |
| S-105 | `planActualGuideColor` | 実績バーの色に追随 🔎 | — | — | **実績から伸びる線なので実績の色を継ぐ。<br>** 独立した色を保存しない —— 派生する色を保存しない規則は `FR-041` が持つ |

⚠️ **ズームに追随させない規則は表 T-020a の `GD-6` が持つ。**  
依存線と同じ扱いである（`FR-079`）。

**既定値を本書に置くのは、仕様として値を定めるためである。**  
秘密として機能しないことの議論は表 T-004 の `LM-12` が持つ。
**この機能は将来サーバーと接続したときに意味を持つ** ものであり、現段階では動作を示せればよい（`LM-12`）。

**表 T-217 — 注記の既定値**

| 行 ID | キー | 型 | 既定 | 下限 | 上限 | 意味・範囲の理由 |
| --- | --- | --- | --- | --- | --- | --- |
| S-132 | `cornerRadiusPx` | 数値 | `4` 🔎 | 0 | 24 | ハイライトボックスの角の丸み（`AT-122`）。<br>ズームによらず一定に描く規則は `FR-019` が持つ。<br>上限は、枠が角丸に食われて囲んだ範囲が読めなくなる手前 |

⚠️ **本表の値は日程データの群に入る** —— 収まる先が `HighlightBox` の列だからである。  
**どちらの群に属するかの判定は表 T-052 が持つ** （表 T-209 と同じ扱いである）。
