# 設計 — コンポーネント

**UID**: DOC-FIG-COMPONENTS
**Version**: 0.1

> **本書が指す `.svg` は `docs/review/components/build.py` が書き出す。手で直さない。**
> 直すのは `docs/review/components/model.json`（部品・辺・層の木）である。
> **図と部品表を同じ原稿から起こすことで、両者の食い違いが起きないようにしている。**

**層の定義と依存の規則は `05-07-design.md` の 5.1（表 T-060・表 T-061・図 F-012）が持つ。本書はそこを指し、規則を再掲しない。**

## 1. 全体

**Type**: SECTION

**箱は部品である。** 層ごとに枠で囲み、**内側の層ほど上に置いた。矢印はすべて上を向く。**

**本図の矢印は層と層の間だけを結ぶ。** 部品どうしの辺は経路ごとの図（図 F-014 〜 図 F-017）が持ち、部品の全数は 表 T-062 が持つ。
**層をまたぐ矢印は、それを裏づける部品どうしの辺が 1 本以上あるときにだけ描かれる**（`build.py` が検算する）。

**図 F-013 — コンポーネントと層**

[![図 F-013 — コンポーネントと層](fig-components.svg)](fig-components.svg)

**図をクリックすると原寸で開く。**

## 2. 書き込みの経路

**Type**: SECTION

**人向けの画面と `Agent API` が同じ入口へ入り、確定して配るまでを示す。** 入口が 1 つであることは `FR-028` と 表 T-042 の `MS-1` が定める。

**図 F-014 — 書き込みの経路**

[![図 F-014 — 書き込みの経路](view-write.svg)](view-write.svg)

## 3. 読み取りの経路

**Type**: SECTION

**描画が書き込みの経路を通らないことを示す。** SVG を作る部品は `Adapter` にあるが、`UseCase` を通らずに `layoutEngine` を直接読む。

**図 F-015 — 読み取りの経路**

[![図 F-015 — 読み取りの経路](view-read.svg)](view-read.svg)

## 4. 出し入れの経路

**Type**: SECTION

**ファイル・保管庫・クリップボード・画像と、取り込みの検証を示す。** 取り込みの 3 経路が同じ検証を通ることは `FR-023` が要求する。

**図 F-016 — 出し入れの経路**

[![図 F-016 — 出し入れの経路](view-io.svg)](view-io.svg)

## 5. 起動の経路

**Type**: SECTION

**シェルが結線し、開く文書を決め、`Agent API` を公開し、発話を配るまでを示す。** 開く文書の順は 表 T-034 が持つ。

**図 F-017 — 起動の経路**

[![図 F-017 — 起動の経路](view-startup.svg)](view-startup.svg)
