# 公開インターフェース — 表 T-064

**UID**: DOC-TBL-PUBLISHED-ENTRIES
**Version**: 0.1

> ⛔ 本書は生成物である。  
> 手で直さない —— 直しても次の `npm run gen` で消える。
> **公開インターフェースの唯一の正は `_source/published-entries.json` である。**  
> 本書はそれを `_source/published_entries_json_to_md.py` が印字したものである。
> **作り直す**: `npm run gen` ／ **ズレを検出する**: `npm run gen:check`。

規則は `05-07-design.md` の 5.3 が持つ。  
本書は 表 T-064 の全行を印字する。

**表 T-064 — 公開インターフェース**

| 行 ID | 層 | コンポーネント | 公開するメンバ |
| --- | --- | --- | --- |
| PI-1 | `documentModel` | `Schedule` | `Schedule`<br>　　型。<br>　　12 の鍵は 表 T-052 の `DR-2`<br>／ `DATE_COLUMNS`<br>　　表 T-058 の型の欄が日付とする列の全数。<br>　　`IV-14` が名指さずに指すので、写しではなく生成する<br>／ `scheduleViolations`（不変条件に反する箇所）<br>／ `taskByUid`<br>　　`uid` で引く。<br>　　`FR-022` の照合が使う<br>／ `planActualState`（表 T-019a の判別）<br>／ `isDelayed`（表 T-021b の 3 条件）<br>／ `delayWorkingDays`（表 T-021b の起点と終点）<br>／ `workingDaysBetween`<br>　　2 つの日付のあいだの稼働日数。<br>　　暦は `FR-054`<br>／ `calendarDaysBetween`<br>　　2 つの日付のあいだの**暦日**数。<br>　　表 T-012a の `FD-6` がフェードの単位として定め、`IV-12` が同じ数え方に従う<br>／ `dateFromWorkingDays`（起点の日付に稼働日を加えた日）<br>／ `nextWorkingDay`<br>　　起点の**翌稼働日**。<br>　　⛔ **`dateFromWorkingDays` の半開区間の終端とは別物である**<br>／ `workingCalendarOf`<br>　　文書の暦を解く。<br>　　順は `FR-054`<br>／ `dayOf`<br>　　日付の字面を日にする。<br>　　規則は `FR-054`<br>／ `textOfDay`（日を日付の字面に戻す）<br>／ `compareDays`（2 つの日の前後）<br>／ `calendarSpanOf`<br>　　2 つの日のあいだの年・月・日と暦日数。<br>　　向きを持たない。<br>　　規則は表 T-029a の `DC-3`<br>／ `actualLastDay`<br>　　実績の最後の日。<br>　　完了したタスクでは `actualFinish`、それ以外では `stop` であり、未着手なら無い。<br>　　`FR-011`<br>／ `actualLengthOf`<br>　　実績の長さ。<br>　　数え方は `FR-011` が持ち、`_assets/fig-erd-detail.md` の `DV-11` がこの長さを読む<br>／ `lastDayForLength`<br>　　`actualStart` と長さから実績の最後の日を置く。<br>　　`FR-011` の、`stop` を持たない取り込みと以前の版の読み<br>／ `isStoredColour`<br>　　色の列が取る綴りか。<br>　　表 T-017b の `CV-1` / `CV-2`<br>／ `customColourOf`<br>　　カスタムカラーを明暗の 2 つの側に分ける。<br>　　`CV-2`<br>／ `customSideOf`<br>　　未定義の側を他方で埋めて、描く側の値を返す。<br>　　`CV-3`<br>／ `customColourChosen`<br>　　カスタムカラーを選んだときの新しい綴り。<br>　　`CV-4` |
| PI-2 | `documentModel` | `DocumentSettings` | `DocumentSettings`<br>　　型。<br>　　鍵は 表 T-104、値は `_assets/tbl-settings.md`<br>／ `clampedSettings`（下限・上限に収める）<br>／ 原稿を刷った 3 つの定数（既定値・単独の下限と上限・他の鍵の式で書かれた既定値）<br>／ `DISPLAY_SCALE_STEPS`<br>　　表示の倍率の段の並び。<br>　　並びの正は `_assets/tbl-settings.md` の 表 T-202 の `S-234` の型の欄である。<br>　　⭐ 公開したのは、`FR-039` が両端で巡らせることを禁じており、端を余りで数える側と段を進める側が同じ 1 本を読まねばならないからである |
| PI-3 | `documentModel` | `DocumentStamp` | `DocumentStamp`<br>　　型。<br>　　3 つは `DR-4`<br>／ `advancedStamp`（版を進める）<br>／ `isStampMatched`<br>　　照合。<br>　　表 T-035 の `AG-2` |
| PI-4 | `documentModel` | `EditHistory` | `EditHistory`（型）<br>／ `historyWithStep`（1 段積む）<br>／ `previousStep`<br>／ `nextStep` |
| PI-5 | `layoutEngine` | `ScheduleLayout` | `ScheduleLayout`（型）<br>／ `layoutFromSchedule`<br>／ `dateAtX`<br>　　時間軸の対応。<br>　　`FR-017`<br>／ `timeAxisOf`<br>　　行を割り付けずに、時間軸の対応だけを求める。<br>　　`dateAtX` はこれを読める。<br>　　表 T-029a の `DC-3` の読み出しが、ポインタの下の日を求めるために読む<br>／ `xFromDay`<br>　　その逆向き。<br>　　日から横の位置を出す<br>／ `tickStrideOf`<br>　　目盛の間引き。<br>　　`LF-1`<br>／ `fitZoom`（`FR-055`）<br>／ `taskPlacement`（どこに載るか）<br>／ `labelUnits`<br>　　`FR-093` の「全角 2・半角 1 で数えた単位数」。<br>　　⭐ `FR-006` が入力欄の要る幅を同じ数え方で求めるので、両側が同じ 1 本を使う<br>／ `groupDepthLimit`<br>　　いまの詳しさの段が描く最も深い段。<br>　　`FR-018`<br>／ `keptInViewByTreeState`<br>　　`expanded` と `temporarilyExpanded` が倍率によらず描かせる行。<br>　　`FR-018` の 表 T-329 の `TD-6`・`TD-7`。<br>　　入口の構えと行見出しパネルが、描かれていない子を数えるために読む<br>／ `groupDepthThresholdOf`<br>　　その段を描くのに要る倍率。<br>　　`FR-018`<br>　　—— ⭐ **2 つとも 表 T-051 の `HF-14` のために公開した**。<br>　　同行の定めにより、行を立てる側が、立てる前に落ちるかどうかを問えなければならない。<br>　　⛔ **式を写して持たせてはならない（MUST NOT）** —— **同じ式が 2 か所にあれば、片方だけが直されて必ず離れる**（1.9）。<br>／ `rowPlacesAtZoomY`<br>　　その倍率での行の位置。<br>　　`FR-016` の行の軸の錨<br>　　—— ⭐ **`FR-016` が本行に置くことを MUST で定めるために公開した**。<br>　　**行の軸は `zoomY` に対して線形ではないので**（理由は同要求）、**呼ぶ側は倍率から位置を算で求められない**。<br>　　⛔ Adapter に自前の割付けをさせないことは、表 T-070 の `MN-6` が既に定めている<br>／ `zoomYAtRectangleLabelFont`<br>　　与えた字の大きさに、矩形（表 T-201 の `S-13`）の名称ラベルの字が等しくなる `zoomY`。<br>　　床（`S-8` と `FR-094` の予定の縦幅の床）だけで字が既に与えた大きさ以上になるときは、床が外れて字が大きくなり始める倍率。<br>　　`FR-016` の行の軸の上限が深さ 1 の行の名前の字を与えて問う<br>／ `NOT_STORED_LABEL_SIZES`<br>　　表 T-206 の `S-196` と `S-233` を刷った定数。<br>　　⭐ 本行が持つ —— 表 T-038 の `OC-10` で名称ラベルのぶんを帯高に数えるのが本コンポーネントであり、`ScheduleGeometry` は本コンポーネントを読むので逆向きには読めない（`LR-3`）。<br>　　`ScheduleGeometry` は名称ラベルの縦の置き場を求めるために本メンバを読む<br>／ `markerDiameterOf`<br>　　進捗マーカーの径。<br>　　表 T-012 の `SH-3` / `SH-4` はそのタスクの名称ラベルの字の大きさ、ほかは 表 T-201 の `S-22`。<br>　　規則は `FR-094`<br>　　—— ⭐ **公開したのは、`ScheduleGeometry` が同じ径でマーカーを描くためである** —— ⛔ **式を `src/` の 2 か所に置いてはならない（MUST NOT）** —— 片方だけが直されて必ず離れる（1.9）<br>／ `LabelReference`<br>　　型。<br>　　札が付く基準 —— その横の位置と幅、「入る」を判ずる幅、名前の字の床。<br>　　基準をどれに取るかは `01-04-requirements.md` の `FR-109` の 表 T-272 が持つ<br>／ `labelReferenceOf`（その基準を求める）<br>／ `LabelLayout`<br>　　型。<br>　　札の配置 —— 「入る」かどうか、進捗マーカーの左端、名前の書き出し。<br>　　規則は同要求の 表 T-273 が持つ<br>／ `labelLayoutOf`（その配置を求める）<br>／ `dummyBandOf`<br>　　ダミーを描く帯。<br>　　幅は 表 T-240 の `DM-3`、マイルストーンは点である<br>／ `outwardStartOf`<br>　　基準の外へ札を並べ始める位置。<br>　　再開日が未定のアイコンに札が届くときは、その掴む箱の右端から並べる —— 表 T-273 の結び<br>／ `assigneeAnchorOf`<br>　　担当と進捗の札の右端を合わせる位置。<br>　　表 T-273 の担当と進捗の段<br>　　—— ⭐ **この 7 つを公開したのは、`ScheduleGeometry` が同じ札を描くためである** —— ⛔ 置き場の式を `src/` の 2 か所に置いてはならない（MUST NOT）<br>／ `labelledAssigneeUidOf`<br>　　担当ラベルが名を出す担当者、すなわち `FR-059` の絞りと並びで先頭に来る 1 名の資源の `uid`。<br>　　いなければ `null`<br>　　—— ⭐ **`ScreenRenderer` が、プロパティパネルの担当者の欄のどれに焦点を置くか（表 T-225 の `AS-1`）を決めるために公開した** —— 絞りを写して持たせると、`FR-059` の規則が 2 か所になる（1.9） |
| PI-6 | `layoutEngine` | `ScheduleGeometry` | `ScheduleGeometry`（型）<br>／ `geometryFromLayout`<br>／ `DependencyGeometry`<br>　　型。<br>　　描いた依存線 1 本 —— 結ぶ 2 つの `Task` と種別、経路（`LC-10`、表 T-222）、描いた太さと矢じり。<br>　　⭐ 太さと矢じりは任せてよい —— 当たりだけを問う呼び手は描く値を持たない<br>／ `HighlightGeometry`<br>　　型。<br>　　描いたハイライトボックス 1 つ —— 囲む矩形と角の丸め（`FR-019`）<br>／ `CommentGeometry`<br>　　型。<br>　　描いたコメントボックス 1 つ —— 線先の点、本文の箱、本文の行と字の大きさ（`FR-019` ／ `FR-097`）<br>／ `leaderOf`<br>　　そのコメントボックスの引出し線。<br>　　⭐ 公開したのは、線先から本文の箱までの経路を `src/` の 2 か所に置かないためである —— 描く側と、`01-04-requirements.md` の 表 T-023d の `GR-14` の掴み代を問う側が同じ 1 本を読む |
| PI-7 | `layoutEngine` | `ItemHitArea` | `itemAtPointer`<br>　　対象は 表 T-023c の `SL-1`。<br>　　掴み代は `FR-104` の 表 T-266、応える順は `FR-105` の 表 T-267 と 表 T-268<br>／ `itemsInMarquee`<br>　　`SL-3`。<br>　　完全に囲まれたものだけ<br>／ `dependencyEndAtPointer`<br>　　`FR-009` の「左半分 / 右半分」を答える。<br>　　⛔ **構えが依存線のときだけ呼ぶ** —— 同要求が `itemAtPointer` に構えを渡すことを禁じている<br>／ `dependencyStartOfHit`<br>　　押したときの当たり（`itemAtPointer` の答え）から、引き出す依存線の起点を答える。<br>　　当たりがタスクのときだけ、そのタスクに限って `dependencyEndAtPointer` を問うた答えを返し、そうでなければ `null` を返す。<br>　　⭐ `InputCommandTranslator`（離したときに依存を作る）と `SingleHtmlShell`（引いているあいだ仮の線を描く、`FR-009`）が同じ 1 本を読むために公開した —— 呼ぶ側が同じ断片を写して持つと、片方だけが直されて離れる（1.9）。<br>　　構えが依存線のときだけ呼ぶことは `dependencyEndAtPointer` と同じである<br>／ `grabSizesOf`<br>　　`itemAtPointer` に渡す掴み代の大きさを、`_assets/tbl-settings.md` の 表 T-206 の掴み代の行（`01-04-requirements.md` の `FR-104` の 表 T-266 が読む `S-250` 〜 `S-290` と、`S-137` / `S-230` / `S-291` / `S-292` / `S-293`）から 1 組にして答える。<br>　　⭐ `SingleHtmlShell` が押したところの当たりを問うときに同じ値を読むために公開した —— 呼ぶ側が値を写して持つと、片方だけが直されて離れる（1.9）。<br>　　表示の倍率は掛けない（`01-04-requirements.md` の 表 T-252 の `DS-7`）<br>／ `GrabArea`<br>　　掴んだ所の型 —— `FR-104` の 表 T-266 と 表 T-023d の行 ID の集合。<br>　　⭐ `InputCommandTranslator` が離したときの置き方を掴んだ所で選ぶために公開した —— 呼ぶ側が同じ集合を写して持つと、片方だけに行が足されて離れる（1.9）<br>／ `isTaskDrawn`<br>　　1 つのタスクの幾何が、表 T-023c の結びの「描かれている」に当たるかを答える —— 予定・実績・実績のダミー（表 T-240）のどれかが在れば真とする。<br>　　⭐ `InputCommandTranslator` が、描かれていないタスクをクリック（`SL-2`）でも全選択（`SL-5`）でも取らないために公開した<br>／ `selectionWithinDrawn`<br>　　選択から、幾何に描かれていないタスクを外した選択を答える。<br>　　選んだ順序（`SL-7b`）は保ち、外すものが無ければ同じ値を返す。<br>　　⭐ `SingleHtmlShell` が、表示の切り替え・行の畳みと隠し・行の軸の倍率で描かれなくなったタスクを、その時点で選択から外すために公開した<br>／ `selectionWithinDrawnRows`<br>　　`selectionWithinDrawn` の答えに、表 T-023c が名指さない行 —— ピン止めの帯に入りきらない行（`FR-098`）と、段数の安全弁（表 T-014 の `ST-7`）が置かなかった行 —— のタスクを戻した選択を答える。<br>　　掴んで動かす間の下書きの絵では絞らない。<br>　　⭐ `SingleHtmlShell` が、フレームごとに組んだ配置と幾何から 1 度だけ刈るために公開した |
| PI-8 | `UseCase` | `ApplyDocumentChange` | `DocumentCommand`<br>　　型。<br>　　**全数は 表 T-108 が持つ**<br>／ `applyDocumentChange`<br>　　`non-pure`。<br>　　命令の列で書き込む<br>／ `replaceDocument`<br>　　`non-pure`。<br>　　`ApplyDocumentChange` の外で組み立てた文書を現在値にする。<br>　　手順は 表 T-067、呼び手ごとの扱いは 表 T-230 |
| PI-9 | `UseCase` | `EditDocument` | `editDocument`<br>　　表 T-108 の命令を集約へ振り分ける。<br>　　表 T-067 の `WS-3` が呼ぶ。<br>　　集約ごとの割りは 表 T-063 の `UT-2`<br>／ `Refusal`<br>　　型。<br>　　拒んだ理由。<br>　　表 T-035 の `AG-9a`<br>／ `InvariantRefusal`<br>　　型。<br>　　取り込みの検証（`FR-023`）が拒んだときの `Refusal`。<br>　　理由の区分を拒んだ行の行 ID とするのは `AG-9a` と、`FR-076` の 表 T-233 の結びである<br>／ `InvariantRow`<br>　　型。<br>　　その理由の区分であり、Chapter 6.1 の 表 T-220 の行 ID<br>／ `SettingsLimits`<br>　　型。<br>　　`editDocument` に渡す下限・上限<br>／ `EditReport`<br>　　型。<br>　　受け入れた編集が人に告げるべきこと。<br>　　`FR-012` が数え直した `Task` の uid を運ぶ<br>／ `treeStateWritesFor`<br>　　表 T-328 の出来事 1 つが書き換える行の `treeState` を求める。<br>　　入口・全体表示・縦を縮める入力が、1 つの束の書き込みとして読む<br>／ `levelZeroWritesFor`（同じ出来事が書き換える段 0 の値 `levelZeroTreeState` を求める）<br>／ `TreeStateEvent`<br>　　型。<br>　　表 T-328 の出来事<br>／ `cycleTaskPlanActualState`<br>　　表 T-108 の `CM-15` の効果を求める。<br>　　押す前の `Task` と、`ScreenState` が覚えている実績を受け取り、置き直した予実と、覚え直す実績を返す。<br>　　巡りの輪と置く値は `01-04-requirements.md` の `FR-107` の 表 T-270 の結びと 表 T-021a が持つ。<br>　　⭐ 公開したのは、覚えた実績が `ScreenState` に在り、命令の側だけでは巡りを閉じられないからである<br>／ `confirmationOwedBy`（削除の書き込みの束が 表 T-234 の問い（`QN-1`・`QN-2`・`QN-10`）を負うかを決め、負うなら消える `Task` の名を挙げた問いを返す —— 連鎖は 表 T-050 の `CD-1`・`CD-2`・`CD-6`）<br>／ `confirmationOwedByResourceDeletion`（担当者の削除が解く割当があるときの `QN-3` の問い —— `CD-5`）<br>／ `DeletionQuestion`<br>　　型。<br>　　その問いの行 ID と挙げる名。<br>　　告げ方（表 T-037 の `NT-7`）は持たず、問いを立てる `SingleHtmlShell` が添える<br>／ `wbsSubtreesOf`<br>　　`Task` の集合に、`WBS` の子孫をすべて足した集合 —— `CD-1`。<br>　　⭐ `SingleHtmlShell` が取り込みで一緒に落ちるタスクの名を挙げるために公開した |
| PI-10 | `UseCase` | `ImportDocument` | `importDocument`（合流の選択肢は 表 T-032a） |
| PI-11 | `UseCase` | `UndoEdit` | `undoEdit` |
| PI-12 | `UseCase` | `RedoEdit` | `redoEdit` |
| PI-13 | `UseCase` | `ValidateImportedDocument` | `validateImportedDocument`（`FR-023` / `NFR-009`） |
| PI-14 | `UseCase` | `ChooseStartupDocument` | `chooseStartupDocument`（順は 表 T-034） |
| PI-15 | `UseCase` | `NotifyChangeWatchers` | `watchChanges`（`non-pure`）<br>／ `unwatchChanges`（`non-pure`）<br>／ `notifyChangeWatchers`（`non-pure`）<br>／ `emptyChangeWatchers`<br>　　空の監視の登録。<br>　　シェルが持って 3 つの口へ引数で渡す —— `SF-10` |
| PI-16 | `UseCase` | `PostDialogueMessage` | `postDialogueMessage`（`non-pure`） |
| PI-17 | `Adapter` | `AgentApiEndpoint` | `installAgentApi`<br>　　`non-pure`。<br>　　既定で公開しない。<br>　　`FR-065`<br>／ `SnapshotSource`（表 T-065）。<br>　　⚠️ 外へ公開する 18 メンバの名前は `_assets/tbl-glossary.md` の 表 T-107 が持つ。<br>　　本表に書き写さない（MUST NOT） |
| PI-18 | `Adapter` | `InputCommandTranslator` | `InputSource`（表 T-065）<br>／ `PressRow`<br>　　型。<br>　　表 T-023a の行 ID<br>／ `pressRowOf`<br>　　押下がどの行で始まったかを答える。<br>　　呼び手が押下の時に解決して `PointerPress` へ載せる<br>／ `commandFromInput`（割当は 表 T-023 と 表 T-036）<br>／ `commandFromFieldCommit`<br>　　プロパティパネルで確定した値を 表 T-108 の命令にする。<br>　　割当は 表 T-016 の「入力の型」の欄と 表 T-104<br>／ `selectionFromInput`<br>　　規則は 表 T-023c。<br>　　取り消しの対象外＝`UN-9`<br>／ `screenEventFromInput`<br>　　入力から画面の値の出来事を 1 つ作る。<br>　　作るものが無ければ `null`。<br>　　出来事の全数は 表 T-280。<br>　　`Esc` には作らない —— 段はシェルが決める（表 T-283）<br>／ `SpentEntranceSituation`<br>　　型。<br>　　押された入口が何を持たないのかを名乗る。<br>　　⭐ **場面から 表 T-233 の行への写しは殻が持つ** —— 訳出の側は通知の語彙を知らない<br>／ `rowBandCeilingOf`<br>　　`FR-016` の行の軸の上限のうち、いちばん高い行の帯の高さが初めて `Row Area` の高さ以上になった倍率を、表 T-253 の手順で探した答え。<br>　　⭐ 殻が続けざまの刻みのあいだ同じ答えを読むために公開した —— 帯は `zoomY` に対して線形でないので、刻みごとに解くと日程の割付けを繰り返す。<br>　　⛔ 帯の側の上限を解くのは本メンバ 1 本とし、呼ぶ側が別の区間や止め方で解き直さない —— 同じ上限を 2 か所で解くと、片方だけが直されて答えが離れ、止まる位置が拡大を始めた倍率で変わる（1.9、`FR-016` はその上限が拡大を始めた倍率に依らないことを求める）<br>／ `isCombo`<br>　　修飾キーの組が求める組と一致するかを答える。<br>　　`Meta` は `Ctrl` と同じに扱う —— 表 T-023。<br>　　⭐ 殻が確定の `Enter` を同じ 1 本で判じるために公開した |
| PI-19 | `Adapter` | `SvgRenderer` | `SvgSurface`（表 T-065）<br>／ `svgFromSchedule`（`FR-080`）<br>／ `colourOf`<br>　　表 T-236 の行 ID と、いまの色の好みから 1 色を返す。<br>　　`EP-9` が書き出しに画面と同じ線を引けと定めるので、`ImageExporter` が同じ色を問えなければならない。<br>／ `swatchOf`<br>　　色の欄の見本の色。<br>　　表 T-017b の `CV-9` が、見本を描く値と同じ値で塗ることを定めるので、`ScreenRenderer` が描く側と同じ 1 本を問う<br>／ `GROUP_GRID_LINE_WIDTH_PX`<br>　　`Group Grid Lines` の罫の太さ。<br>　　`EP-9` が、仕切りの線の太さを 1 か所から読むことと、画面と書き出しがその同じ 1 か所を読むことを定めているので、**色と同じ理由でこの数も問えなければならない**。<br>　　⭐ **描く当の本人が持つ** —— `U-18` を引くのはこのユニットである |
| PI-20 | `Adapter` | `DocumentCodec` | `AppShellSource`（表 T-065）<br>／ `documentFromJson`<br>／ `jsonFromDocument`<br>／ `documentFromMspdi`<br>／ `mspdiFromDocument`<br>／ `exportEmbeddedHtml`<br>　　`semi-pure-b`。<br>　　表 T-024 の `IO-7`<br>／ `formatFromFile`<br>　　どちらの形式として読むかを答える。<br>　　規則は 表 T-024a の `OP-12`<br>／ `extensionOfFormat`<br>　　表 T-024 の行 ID から、その形式の拡張子を答える。<br>　　⭐ 殻が形式の名簿を直に読まないために公開した —— 表 T-248 の `JF-3` |
| PI-21 | `Adapter` | `ImageExporter` | `Rasterizer`（表 T-065）<br>／ `exportSvg`<br>　　表 T-076 が「描く」とした UI パーツを組み立てて返す。<br>　　⛔ 高さの天井に収まらないときは絵を返さず、拒みを返す —— 規則は `FR-025`<br>／ `exportPng`<br>　　`semi-pure-b`。<br>　　失敗も値で返す。<br>　　表 T-035 の `AG-8` |
| PI-22 | `Adapter` | `FileGateway` | `FileStore`（表 T-065）<br>／ `openDocumentFile`（`semi-pure-b`）<br>／ `saveDocumentFile`（`non-pure`） |
| PI-24 | `Adapter` | `ClipboardGateway` | `Clipboard`（表 T-065）<br>／ `writeClipboard`<br>　　`non-pure`。<br>　　表 T-024 の `IO-6` と `FR-033` |
| PI-25 | `Framework` | `SingleHtmlShell` | **他のコンポーネントから呼ばれるメンバを持たない**。<br>Vite の入口である。<br>`SnapshotSource` と `AppShellSource` の実装を、宣言したコンポーネントへ渡す |
| PI-26 | `Framework` | `DomSvgSurface` | `domSvgSurface`（`SvgSurface` の実装 1 つを返す） |
| PI-27 | `Framework` | `DomInputSource` | `domInputSource`（`InputSource` の実装 1 つを返す） |
| PI-28 | `Framework` | `FileSystemAccessFileStore` | `fileSystemAccessFileStore`（`FileStore` の実装 1 つを返す） |
| PI-30 | `Framework` | `BrowserClipboard` | `browserClipboard`（`Clipboard` の実装 1 つを返す） |
| PI-31 | `Framework` | `CanvasRasterizer` | `canvasRasterizer`（`Rasterizer` の実装 1 つを返す） |
| PI-32 | `documentModel` | `Selection` | `Selection`<br>　　型。<br>　　順序は 表 T-023c の `SL-7b`<br>／ `selectionWith`<br>／ `selectionWithout`<br>／ `emptySelection`<br>／ `isSelected`<br>／ `selectionWithinSchedule`<br>　　文書に無くなった対象を外した選択を答える —— `FR-081` の結びの「文書に実在する対象だけを指すこと」。<br>　　選んだ順序（`SL-7b`）は保ち、外すものが無ければ同じ値を返す。<br>　　⭐ `SingleHtmlShell` が、書き込みが着地するたびに 1 か所で刈るために公開した —— 消える入口ごとに落とさないことは同じ結びが定める |
| PI-33 | `documentModel` | `DialogueLog` | `DialogueLog`<br>　　型。<br>　　刻印とは別の順序は 表 T-035 の `AG-11`<br>／ `logWithMessage`（1 件積む）<br>／ `messagesSince`（`AG-6` の選び方） |
| PI-34 | `documentModel` | `Document` | `Document`<br>　　型。<br>　　5 つの鍵は 表 T-052 の `DR-1` 〜 `DR-4`<br>／ `documentViolations`（`DR-1` に反する箇所） |
| PI-35 | `layoutEngine` | `ScreenRegions` | `ScreenRect`<br>　　型。<br>　　矩形。<br>　　左上の座標と幅と高さの数値 4 つを自前で宣言し、**ブラウザの供給する型に触れない**（`LR-6`）<br>／ `ScreenRegions`<br>　　型。<br>　　各部の矩形。<br>　　各部の名は 表 T-103 が持つ<br>／ `regionsFromScreen`（画面の寸法と `DocumentSettings` から各部の矩形を出す）<br>／ `regionsAtDisplayScale`<br>　　与えた表示の倍率で描いたときの各部の矩形。<br>　　`Row Area` の縦の置き場と高さは描いた目盛の帯の高さ（`FR-039` の 表 T-252 の `DS-1`）で動くので、倍率を変える側は、変えたあとの `Row Area` を変える前に問える<br>　　—— ⭐ **公開したのは、`FR-039` が `Row Area` の縦の中点の指す行を動かさないと定めており、倍率を変える側が変更前の矩形しか持たないからである** —— ⛔ **倍率から矩形を算で求めさせない** —— 組み立ては `regionsFromScreen` が持ち、写すと片方だけが直されて必ず離れる（1.9）<br>／ `regionAtPointer`（ポインタがどの領域にあるか）<br>／ `displayRatioOf`<br>　　表示の倍率から描く比を出す。<br>　　求め方は `FR-039`<br>／ `drawnSettingsOf`<br>　　保存値に描く比を 1 度だけ掛けた設定値を返す。<br>　　掛ける行は `FR-039` の 表 T-252 が持つ。<br>　　⛔ **保存値そのものを書き換えない（MUST NOT）** —— 同要求が定めており、描く側はこの 1 本を通って掛ける<br>／ `rowControlLatticeHeightPx`<br>　　行の操作子の格子（表 T-051 の `HF-1`）を描いたときに縦に取る高さ。<br>　　算式は 表 T-221 の `LF-16` が持ち、それを行の帯高の下限にしないことは 表 T-051 の `HF-19` が持つ<br>　　—— ⭐ **公開したのは、最後の行の下の余白（`LF-16`）を求める `ScheduleLayout` が同じ 1 本を問うためである** —— ⛔ **算式を `src/` の 2 か所に置かない** —— 片方だけが直されて必ず離れる（1.9）。<br>　　⚠️ 外形に `S-235` を掛けるのは `drawnSettingsOf` と同じ本コンポーネントであり、表示の倍率は掛けない（`FR-039` の 表 T-252 の `DS-7`） |
| PI-36 | `documentModel` | `ScreenState` | `DualCursorSide`<br>　　型。<br>　　`date1` と `date2` のどちらが追従しているか。<br>　　表 T-029a の `DC-2`。<br>　　⚠️ **文書には持たない** —— 一過性の状態であり、`DC-8` の印は書き出しに出さない<br>／ `EscapeTarget`<br>　　型。<br>　　`Esc` が消費する段の語。<br>　　段は 表 T-028 の `IN-4`、並びは 表 T-283<br>／ `EscapeContext`<br>　　型。<br>　　段の判定に要る真偽。<br>　　呼び手が画面の値とほかの領域から集める<br>／ `escapeTarget`<br>　　`EscapeContext` だけから、`Esc` が次に消費する段を答える。<br>　　消費するものが無ければ `null`<br>／ `RememberedActual`<br>　　型。<br>　　中断から未着手へ戻したときに外した実績（`01-04-requirements.md` の `FR-107` の 表 T-270 の「押下の巡り」）。<br>　　実績の開始・実績の最後の日・停止日と、取り込んだ原値の実績期間を運ぶ。<br>　　⛔ **文書には持たない** —— 文書を開いているあいだだけの値であり、閉じれば消える。<br>　　覚えるのは画面の値の根（表 T-280）である |
| PI-37 | `Adapter` | `ScreenRenderer` | `ScreenSurface`（表 T-065）<br>／ `ScreenView`<br>　　型。<br>　　日程表の外側の UI パーツの記述<br>／ `ScreenViewReadings`<br>　　型。<br>　　描き手が根の状態のほかに読む値 —— フレームで取った値（`SF-5`）、文書から導く値（`SF-10`）、まだ根へ移していない領域の値<br>／ `screenViewFromRegions`<br>　　根の状態（`PI-39` の `ScreenSession`）と `ScreenViewReadings` から `ScreenView` を作る。<br>　　根の状態は読むだけである<br>／ `dialogueMessageFromInput`<br>　　対話欄で確定した発話。<br>　　順序の規則は 表 T-035 の `AG-11`<br>／ `dismissKeyOf`（表 T-037 の `NT-8` で人が消した告げを名指す鍵）<br>／ `rulerWeekdayWords`<br>　　目盛の第 4 段が刷る曜日 7 語。<br>　　表示言語ごと。<br>　　`FR-017` ／ `FR-038`<br>／ `DEFAULT_ROW_NAME`<br>　　行を既定の名前で立てるときの語。<br>　　表 T-051 の `HF-14`<br>　　—— ⭐ **公開したのは、綴りを `src/` の 2 か所に置かないためである** —— 同行は「既定の名前は表示語として持つこと（MUST）。仕様書が規則として綴りを刷ってはならない（MUST NOT）」と定める。<br>　　⭐ 内側の層は読まず、値で受け取る（表 T-248 の `JF-4`）<br>／ `rowTitleFontPxOf`<br>　　行の名前の字の大きさ。<br>　　深さ 1 は 表 T-201 の `S-36` × `S-38`、ほかは `S-36`。<br>　　`FR-016` の行の軸の上限が深さ 1 を問う。<br>　　⭐ 公開したのは、字の式を `src/` の 2 か所に置かないためである —— 行見出しを描く側と、上限を求める側・書き出す側が同じ 1 本を読む<br>／ `FieldEditNotice`<br>　　型。<br>　　入力欄の編集が始まった・終わったことと、その欄の行 —— `IF-9` ・ 表 T-292<br>／ `horizontalWholeOf`<br>／ `verticalWholeOf`<br>　　つまみが表す全体を配置と各部の矩形から測る —— `GR-21` の「内容の範囲といま見えている範囲の和」。<br>　　⭐ `SingleHtmlShell` が押した時点の全体を覚えるために公開した。<br>　　覚えた値を読むのは `InputCommandTranslator` である（`FR-051`）<br>／ `HorizontalWhole`<br>／ `VerticalWhole`<br>　　型。<br>　　その全体<br>／ `scrollExtentOf`<br>　　配置と各部の矩形と全体から `ScreenViewReadings` のスクロールの範囲を答える。<br>　　縦の見えている高さはピン止めの帯の下の残り（`FR-098`） |
| PI-38 | `Framework` | `DomScreenSurface` | `ScreenSurface` の実装 1 つ<br>／ `pageGroundStyle`（地の色の宣言）<br>／ `ScreenTheme`（型）<br>　　—— ⭐ **地を塗るのはシェルである** —— 本コンポーネントの根は日程の上に重なって敷かれており、そこに地を塗ると日程が隠れる。<br>　　⛔ `FR-041` は地を塗ることを MUST で求めるので、塗る側が宣言を受け取れなければ満たせない |
| PI-39 | `UseCase` | `AdvanceScreenSession` | `ScreenSession`<br>　　型。<br>　　根の状態。<br>　　領域の合成（`SF-8`）で、いまは画面の値・通知・身振り・ファイル操作と問い・名前付けと入力欄・選択・操作の記録・`Agent API` の 8 領域<br>／ `SessionEvent`<br>　　型。<br>　　全数は 表 T-280 ・ 表 T-286 ・ 表 T-289 ・ 表 T-290 ・ 表 T-292 ・ 表 T-293 ・ 表 T-295 ・ 表 T-296 の出来事の定義<br>／ `SessionEffect`<br>　　型。<br>　　副作用の名の全数は 表 T-280 ・ 表 T-286 ・ 表 T-289 ・ 表 T-290 ・ 表 T-292 ・ 表 T-293 ・ 表 T-295 ・ 表 T-296 の状態遷移表の升<br>／ `emptyScreenSession`<br>　　初期の状態。<br>　　表 T-280 ・ 表 T-286 ・ 表 T-289 ・ 表 T-290 ・ 表 T-292 ・ 表 T-293 ・ 表 T-295 ・ 表 T-296 の状態の一覧の初期<br>／ `advanceScreenSession`<br>　　1 段進める。<br>　　何も変わらないときは受け取った参照を返す —— `SF-3`<br>／ `ScreenValues`<br>　　型。<br>　　画面の値の領域の状態 —— 表 T-280<br>／ `ScreenValuesEvent`<br>　　型。<br>　　画面の値の領域の出来事 —— 表 T-280 の出来事の定義<br>／ `PropertiesSubject`<br>　　型。<br>　　プロパティパネルが出す対象<br>／ `ArmKind`<br>　　型。<br>　　構えの種類 —— 表 T-023b<br>／ `FileOperationState`<br>　　型。<br>　　ファイル操作の状態 —— 表 T-290<br>／ `FileFlowOpenRoute`<br>　　型。<br>　　文書へ至った道 —— 表 T-290 の状態が運ぶ値<br>／ `FileFlowQuestion`<br>　　型。<br>　　立てた問いの中身 —— `NT-7` ・ 表 T-290<br>／ `FileFlowOwedAction`<br>　　型。<br>　　問いの答えを待っている操作 —— 表 T-290<br>／ `FileFlowWriteForm`<br>　　型。<br>　　書く形が保存か書き出しか —— 表 T-290<br>／ `FileFlowSurfaceName`<br>　　型。<br>　　ファイル操作が出す UI パーツの名 —— 表 T-290<br>／ `FileFlowImportAnswer`<br>　　型。<br>　　取り込みが続きに使う答え —— 表 T-290<br>／ `PressedOn`<br>　　型。<br>　　押されたもの。<br>　　位置は持たない —— 表 T-289 ・ `SF-5`<br>／ `GrabbedRowAxis`<br>　　型。<br>　　掴んだ行を動かす軸 —— 表 T-289<br>／ `StandingNotice`<br>　　型。<br>　　立っている告げ —— 表 T-286 |
