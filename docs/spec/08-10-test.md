# gr-scheduler 仕様書 — テスト

**Grammar**: spec.sgra
**UID**: DOC-TEST
**Version**: 0.1

本書は第 3 部（Chapter 8 〜 10）である。

> **この文書はまだ器である。** 未記入の節は引用で始まる行を持つ。

## Chapter 8. Use Case Tests (ユースケーステスト)

### 8.1 Test Cases (テストケース)

**Type**: SECTION

> ⛔⛔ **いまは作らない（利用者の裁定 2026-09-05）。**⭐ **代償は、ユースケースから試験仕様が自動で落ちないことである。**⚠️ **取り下げた根拠は歩留まりの実測である** —— **単体試験 12.6 万行が生涯で見つけた欠陥は 3 件、「押す」ことと「仕様だけを読む体」は 1 巡で 63 件であった。生成器を書く 1 巡は、押すことに使うほうが桁で多く出る。**⭐ **品質が安定したときに書く**（同裁定「もう少し品質が安定したら、仕様書に最低限のテストを書く」）。⛔ **章は畳まない** —— **消すと「なぜ無いのか」が失われる。**
>
> ⭐ **書くときの形**: `USE_CASE_TEST` ノード（`TC-xxx`）を並べる。`GIVEN` / `WHEN` / `THEN` を完全な文で書き、`UC-xxx` を親に取り、`File` 関係で `tests/usecase/` のテストコードを指す。

### 8.2 Test Results (テスト結果)

**Type**: SECTION

> 未記入。`TEST_RESULT` ノード（`TR-xxx`）を並べる。`RESULT` / `EXECUTED_ON` / `TESTED_VERSION` / `ENVIRONMENT` / `EVIDENCE` はすべて必須。`EVIDENCE` には後から取り出せる位置を書く（Playwright の trace / report のパスなど）。

## Chapter 9. Software Specification Tests (ソフトウェア仕様テスト)

### 9.1 Test Cases (テストケース)

**Type**: SECTION

> ⛔⛔ **いまは作らない（利用者の裁定 2026-09-05）。第八章と同じ理由と同じ代償である。**
>
> ⭐ **書くときの形**: `SW_SPEC_TEST` ノード（`TC-xxx`）を並べる。`TEST_LEVEL` は `Integration` か `System` のいずれか（`Unit` は選べない。単体テストは仕様書の外）。`SWS-xxx` を親に取り、`File` 関係で `tests/integration/` または `tests/system/` を指す。

### 9.2 Test Results (テスト結果)

**Type**: SECTION

> 未記入。`TEST_RESULT` ノードを並べる。欄は 8.2 と同じ。

## Chapter 10. Non-Functional Tests (非機能テスト)

### 10.1 Test Cases (テストケース)

**Type**: SECTION

> 未記入。`NON_FUNC_TEST` ノード（`TC-xxx`）を並べる。`GIVEN` に測り方と負荷の条件、`THEN` に親の `NON_FUNC_REQ` と一致する数値を書く。`NFR-xxx` を親に取り、`File` 関係で `tests/nfr/` を指す。**ここが性能ゲートの実体**であり、`MILESTONES-ja.md` の各節目はこれが通るまで閉じない。

### 10.2 Test Results (テスト結果)

**Type**: SECTION

> ⭐⭐ **本章は残す（利用者の裁定 2026-09-05）** —— **第八・九章と違い、ここには入れる中身がある。**
>
> ⭐ **2026-09-05、はじめて速さを測った。****結果の原本は `docs/development-records/measurements/2026-09-05-performance.md` が持つ。**⛔ **数を本章へ写さない** —— **2 か所に置くと必ずずれる。**
>
> **測って分かったこと**: **`PG-1`（初期描画）・`PG-5`（無操作時の描き直し）・`PG-14`（規模との伸び）は合否の内側。`PG-2`（平均フレームレート）・`PG-3`（95 パーセンタイル）・`PG-8`（掴んで動かす）は外側。`PG-10` / `PG-11` / `PG-12` は測っていない。**
>
> ⛔⛔ **この測定は節目の合否として使えない** —— **ソフトウェア描画で取っており、表 T-025 が定める統合 GPU の条件ではない。⭐ 節目を閉じる直前に、その条件で取り直すこと（利用者の裁定 2026-09-05）。**
>
> ⭐ **書くときの形**: `TEST_RESULT` ノードを並べる。**節目ごとに再計測して足す。** 1 回測って終わりにしたのが前プロジェクトの失敗である（`previous-project-result/README.md` §0-2）。
