# gr-scheduler 仕様書 — テスト

**Grammar**: spec.sgra
**UID**: DOC-TEST
**Version**: 0.1

本書は第 3 部（Chapter 8 〜 10）である。

> ⭐ **本書は試験のノードを持たない**（`05-07-design.md` の 表 T-219）。  
> 3 章とも、確かめるものは Chapter 7 の 表 T-334、置き場は 表 T-218 が持つ。

## Chapter 8. Use Case Tests (ユースケーステスト)

### 8.1 Test Cases (テストケース)

**Type**: SECTION

> ⭐ **本章はケースのノードを持たない。**  
> 確かめるものは 表 T-334 の `VT-1`（Chapter 7）、置き場は 表 T-218 の `TS-1`、合格基準は 表 T-219 の `TW-1` が持つ。  
> ⛔ **章は畳まない** —— **消すと「なぜ無いのか」が失われる。**

### 8.2 Test Results (テスト結果)

**Type**: SECTION

> 結果は関門の走行の出力が持つ（表 T-219 の `TW-1`）。

## Chapter 9. Software Specification Tests (ソフトウェア仕様テスト)

### 9.1 Test Cases (テストケース)

**Type**: SECTION

> ⭐ **本章はケースのノードを持たない。**  
> 置き場は 表 T-218 の `TS-2` と `TS-3`、合格基準は 表 T-219 の `TW-2` が持つ。

### 9.2 Test Results (テスト結果)

**Type**: SECTION

> 結果は関門の走行の出力が持つ（表 T-219 の `TW-2`）。

## Chapter 10. Non-Functional Tests (非機能テスト)

### 10.1 Test Cases (テストケース)

**Type**: SECTION

> ⭐ **本章はケースのノードを持たない。**  
> 確かめるものは 表 T-334 の `VT-3`（Chapter 7）、合否は 表 T-043 が持つ。

### 10.2 Test Results (テスト結果)

**Type**: SECTION

> ⭐ **本章は残す** —— **第八・九章と違い、ここには入れる中身がある。**
>
> ⭐ 速さを測った結果の原本は `docs/development-records/measurements/2026-09-05-performance.md` が持つ。  
> ⛔ **数を本章へ写さない** —— **2 か所に置くと必ずずれる。**
>
> **測って分かったこと**: `PG-1`（初期描画）・`PG-5`（無操作時の描き直し）・`PG-14`（規模との伸び）は合否の内側。  
> `PG-2`（平均フレームレート）・`PG-3`（95 パーセンタイル）・`PG-8`（掴んで動かす）は外側。  
> `PG-10` / `PG-11` / `PG-12` は測っていない。
>
> ⛔ **この測定は節目の合否として使えない** —— ソフトウェア描画で取っており、表 T-025 が定める統合 GPU の条件ではない。  
> ⭐ 節目を閉じる直前に、その条件で取り直すこと。
>
> ⭐ **節目ごとに再計測し、結果は `docs/development-records/measurements/` に足す**（表 T-219 の後の段）。  
> ⛔ 1 回測って終わりにしない —— 骨格の性能は完成版の性能を示さない（`previous-project-result/README.md` §0-2）。
