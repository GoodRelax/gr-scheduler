# gr-scheduler 仕様書 — テスト

**Grammar**: spec.sgra
**UID**: DOC-TEST
**Version**: 0.1

本書は第 3 部（Chapter 8 〜 10）である。

> **この文書はまだ器である。** 未記入の節は引用で始まる行を持つ。

## Chapter 8. Use Case Tests (ユースケーステスト)

### 8.1 Test Cases (テストケース)

**Type**: SECTION

> 未記入。`USE_CASE_TEST` ノード（`TC-xxx`）を並べる。`GIVEN` / `WHEN` / `THEN` を完全な文で書き、`UC-xxx` を親に取り、`File` 関係で `tests/usecase/` のテストコードを指す。

### 8.2 Test Results (テスト結果)

**Type**: SECTION

> 未記入。`TEST_RESULT` ノード（`TR-xxx`）を並べる。`RESULT` / `EXECUTED_ON` / `TESTED_VERSION` / `ENVIRONMENT` / `EVIDENCE` はすべて必須。`EVIDENCE` には後から取り出せる位置を書く（Playwright の trace / report のパスなど）。

## Chapter 9. Software Specification Tests (ソフトウェア仕様テスト)

### 9.1 Test Cases (テストケース)

**Type**: SECTION

> 未記入。`SW_SPEC_TEST` ノード（`TC-xxx`）を並べる。`TEST_LEVEL` は `Integration` か `System` のいずれか（`Unit` は選べない。単体テストは仕様書の外）。`SWS-xxx` を親に取り、`File` 関係で `tests/integration/` または `tests/system/` を指す。

### 9.2 Test Results (テスト結果)

**Type**: SECTION

> 未記入。`TEST_RESULT` ノードを並べる。欄は 8.2 と同じ。

## Chapter 10. Non-Functional Tests (非機能テスト)

### 10.1 Test Cases (テストケース)

**Type**: SECTION

> 未記入。`NON_FUNC_TEST` ノード（`TC-xxx`）を並べる。`GIVEN` に測り方と負荷の条件、`THEN` に親の `NON_FUNC_REQ` と一致する数値を書く。`NFR-xxx` を親に取り、`File` 関係で `tests/nfr/` を指す。**ここが性能ゲートの実体**であり、`MILESTONES-ja.md` の各節目はこれが通るまで閉じない。

### 10.2 Test Results (テスト結果)

**Type**: SECTION

> 未記入。`TEST_RESULT` ノードを並べる。**節目ごとに再計測して足す。** 1 回測って終わりにしたのが前プロジェクトの失敗である（`previous-project-result/README.md` §0-2）。
