# CR-001: 予実データモデルの案H移行 ＋ MSPDI codec 実装漏れ・追加推奨

- CR番号: CR-001
- 日付: 2026-07-19
- 起票: ユーザー起点（本会話での方針決定）
- 変更原因: requirement-change（予実モデル）＋ scope-change（MSPDI 相互運用の拡充）
- 影響度: **High**（複数モジュール横断・データ形式変更・既存JSON移行を伴う）
- 状態: **提案（未着手 / 実装は別担当へ引継ぎ）**
- 承認: 未（High のためユーザー承認が必要）
- 関連要求: PLAN-L1-001..004, ITEM-L1-001/006, IO-L1-001/002, DEP-L1-001/002
- 関連設計: DATA-JSON-005/006, DATA-MSPDI-002..006, ARCH-C-014（進捗線）, ARCH-C-018（MSPDI codec）
- 根拠資料: [docs/analysis/mspdi-json-fidelity.md](../../docs/analysis/mspdi-json-fidelity.md)（JSON⇔MSPDI フィデリティ分析）

---

## 1. 背景・目的

GR JSON と MSPDI XML の相互変換を分析（フィデリティ・レポート）した結果、次が判明した。

1. **形式差（本質的損失）**: GR→MSPDI は視覚層、MSPDI→GR は工数・資源・制約層が相手側で消える（双方向・非対称）。これは根治にモデル拡張が要る。
2. **実装漏れ（修正可能）**: 両形式とも表現できるのに現 codec が写していない項目が複数ある（レポート §5）。
3. **予実モデルの方針転換**: 進捗入力／イナズマ線の検討から、予実の持ち方を**案H**へ移行する判断に至った（本会話で決定）。

本 CR は上記を1つの変更パッケージとして**別担当の実装者へ引き継ぐ**ためにまとめる。以下、Part A（予実モデル案H）・Part B（MSPDI 実装漏れ）・Part C（GRモデル追加推奨）で構成する。

---

## 2. 変更内容

### Part A: 予実データモデルを「案H」へ移行（最重要・requirement-change）

**方針**: 実績を「別アイテム」ではなく「**同一アイテムの実績日フィールド**」で持つ。進捗率のみ（案P）では実績の時間ズレ（着手遅れ・期間変動）を表現できず、日本の予実文化に不足するため採らない。案H は MSPDI（Baseline/Actual/PercentComplete）にも素直。

**item スキーマ変更:**
```diff
  startDate, endDate            // 予定(plan)スパンの意味に確定
+ actualStart?: IsoDate         // 実績開始（任意）
+ actualEnd?:   IsoDate | null  // 実績終了（任意, milestone は null）
  progressRatio?: number        // 進行中バーの到達割合（据え置き）
  previousPlan?                 // 変更前予定＝ベースライン履歴（据え置き）
- planActualKind                // 廃止（'plan'|'actual'）
- planGroupId                   // 廃止
```

**viewState 追加（表示スタイルは両対応・ビュー切替）:**
```diff
+ planActualStyle: 'separate' | 'overlay'   // Opt1 分離バー / Opt2 重ね塗り
  planActualDisplay: 'plan-only'|'actual-only'|'both'|'none'  // 据え置き（フィルタ）
```
- **Opt1（分離）**: 予定バーと実績バー（`actualStart..actualEnd`）を上下に分けて描く。
- **Opt2（重ね塗り）**: 予定バー上に実績/進捗を塗り重ねる。
- どちらも**同一データからの描画切替**であり、データ設計とは独立。

**イナズマ線 front の統一規則（ARCH-C-014 / progress-today-layer）:**
```
実績日あり → front = actualStart + ratio × (actualEnd − actualStart)
実績日なし → front = startDate  + ratio × (endDate − startDate)   // MSP % 相当
どちらも無 → 未着手（頂点を出さない）
```

**MSPDI マッピング（往復が素直に）:**
`startDate/endDate ↔ Task Start/Finish` ／ `actualStart/actualEnd ↔ ActualStart/ActualFinish` ／ `progressRatio ↔ PercentComplete` ／ `previousPlan ↔ BaselineStart/Finish`。

### Part B: MSPDI codec の実装漏れ（フィデリティ §5・両形式が表現可能なのに未対応）

| # | 実装漏れ | 対応方針 | 現状 |
|---|---------|---------|------|
| B-1 | 行/セクション階層 | `OutlineLevel`+`Summary` で双方向変換（`DATA-MSPDI-005`） | Export は `OutlineLevel` を 1 固定・サマリ未生成／Import は階層を読まず全タスクを `row-0` に集約 |
| B-2 | 担当 `assignee` ⇔ Resource/Assignment | Resource として出力・読取 | 双方向とも未対応 |
| B-3 | 進捗 `progressRatio` ⇔ `PercentComplete` | 数値を往復 | 双方向とも未対応 |
| B-4 | 予実/変更前予定 ⇔ `Baseline*`/`ActualStart/Finish` | Part A の actual*/previousPlan を Baseline・実績日で表現 | 未対応（Part A と一体で実装） |
| B-5 | 分割タスク `SplitParts` → マルチバー | 分割区間を GR の 1 行複数アイテムへ写す | Import が `SplitParts` を無視し連続バーに潰す |
| B-6 | 説明 `description` ⇔ `Task/Notes` | タスク単位 Notes に格納 | 未対応（Project 単位 Notes をサイドカーが占有中のため配置設計が要る） |

> B-4 は Part A のモデル変更と不可分。B-1/B-2/B-3/B-5/B-6 は Part A と独立に着手可能。

### Part C: GR モデルへの追加推奨（フィデリティ §6）

MSPDI→GR で形式差（✕）となる項目のうち、GR に取り込む価値があるもの。採否は「俯瞰日程表」という製品スコープで判断した。

| 項目 | 現状の受け皿 | 推奨 | 備考 |
|------|-------------|:---:|------|
| 担当者 `assignee` | モデルに有（配線のみ） | **採用（最優先）** | 実体は B-2 の実装作業 |
| 進捗率 `progressRatio` | モデルに有（配線のみ） | **採用** | 実体は B-3 |
| 予実・変更前予定 | モデルに有（配線のみ） | **採用** | 実体は Part A / B-4 |
| 依存の種別（FS/SS/FF/SF） | モデルに**無** | **採用推奨（モデル拡張）** | `dependencies[]` に `linkType` 追加。MSPDI `PredecessorLink/Type`(FF=0/FS=1/SF=2/SS=3) と往復。低コスト高価値 |
| 依存のラグ | モデルに**無** | 条件付き採用 | 種別追加時に `lagDays` を併せて検討 |
| 期限（目標終了日） `Deadline` | モデルに**無** | **採用検討** | バー端と独立の「◆ここまで」目標マーカー。`item.targetDate` 追加で表現。俯瞰日程表と相性良 |
| 制約（ルール）`Constraint` | モデルに**無** | 見送り | GR は手動配置でスケジューラが無く適用先が無い |
| 工数/コスト/カレンダー/資源配分/タスク種別 | モデルに**無** | 見送り | 製品スコープ外（資源計画は非目的）。将来 CR で再評価 |

---

## 3. 影響範囲（別担当への実装対象）

| 層 | ファイル | 変更内容 |
|----|---------|---------|
| 仕様 | `docs/spec/18-plan-actual.sdoc` | PLAN 要求を案H へ改訂（予実の持ち方・表示切替） |
| 仕様 | `docs/spec/40-data-format.sdoc` | §1 item フィールド（actual*/planActualStyle）、§2 MSPDI マッピング（B-1..B-6） |
| 仕様 | `docs/spec/16-dependencies.sdoc` | 依存 `linkType`/`lagDays`（Part C 採用時） |
| スキーマ | `docs/api/gr-scheduler.schema.json` | item に actualStart/actualEnd 追加、planActualKind/planGroupId 削除、viewState.planActualStyle、dependency.linkType |
| モデル | `src/domain/model/schedule-model.ts` | 上記型定義 |
| codec | `src/domain/usecase/json-codec.ts` | 直列化＋**旧JSON移行**（§4） |
| codec | `src/domain/usecase/mspdi-codec.ts` | B-1..B-6 の標準要素 Export/Import |
| 描画 | `src/adapters/render/layers/progress-today-layer.ts` | front 規則の統一 |
| 描画 | `src/adapters/render/layers/item-layer.ts`, `src/domain/usecase/plan-actual-colors.ts` | Opt1/Opt2 の2表示モード |
| データ | `src/app/sample-data.ts` | サンプルを案H へ |
| テスト | `tests/**` | 予実・進捗線・往復整合の単体/E2E 更新 |
| 追従 | `docs/analysis/mspdi-json-fidelity.md` | 実装完了後に §5/§6 の状態を更新 |

---

## 4. 既存データの移行（後方互換）

`schemaVersion` を上げ、Import 時に旧→新へ決定的変換する（既存の `src/domain/usecase/id-migration.ts` と同方式）。

- 旧 `planActualKind:'actual'`（`planGroupId=G`）アイテムを、同グループの `plan` アイテムへ `actualStart`/`actualEnd`（＝旧 actual の start/end）・`progressRatio` として畳み込み、**実績アイテムを削除**。
- `planGroupId` を持たない単独アイテムは plan 扱い（`planActualKind` 除去）。
- 移行は往復整合テストで検証（旧JSON → 新モデル → 再Export → 再Import の同値性）。

---

## 5. 工数・スケジュール影響（概算・要精査）

| 区分 | 概算 | 備考 |
|------|------|------|
| Part A（モデル案H＋描画2モード＋移行） | 中〜大 | データ形式変更＋描画分岐＋マイグレーション＋テスト |
| Part B（MSPDI 実装漏れ B-1..B-6） | 中 | B-4 は Part A 連動。他は独立 |
| Part C（linkType / targetDate 追加） | 小〜中 | スキーマ＋描画＋MSPDI 往復 |

**High 判定理由**: 複数モジュール横断＋承認済みデータ形式の変更＋移行を伴う。スケジュールへの影響は1日以上と見込む。

---

## 6. 受入基準（Acceptance Criteria）

- [ ] item に actualStart/actualEnd が入り、planActualKind/planGroupId が廃止されている。
- [ ] Opt1/Opt2 が viewState.planActualStyle で切替でき、同一データから両描画できる。
- [ ] イナズマ線が §2 の統一 front 規則で描かれる（実績日あり/なし両ケース）。
- [ ] 旧JSON（planGroupId 方式）が Import で新モデルへ決定的に移行され、往復整合が保たれる。
- [ ] MSPDI 往復で B-1..B-6 の各項目が標準要素として Export/Import される（他ツール可視）。
- [ ] Part C 採用分（assignee/progress/previousPlan、linkType、必要なら targetDate）が MSPDI と往復する。
- [ ] 単体95%以上／結合100%／往復整合テスト green（品質目標準拠）。

---

## 7. 未決事項（ユーザー承認が必要）

1. **本 CR（High）の承認可否**。
2. **Part C の範囲**: 依存 `linkType`（＋`lagDays`）と 期限 `targetDate` を本 CR に含めるか、別 CR に切り出すか。
3. **表示スタイルの既定値**: 既定を Opt1（分離）か Opt2（重ね塗り）か。
4. **ブランチ運用**: この規模は `feature/` ブランチ推奨（develop 経由）。別担当の作業ブランチ方針の確認。

---

## Footer: 変更履歴

| 日付 | 版 | 変更 | 記録者 |
|------|----|----|--------|
| 2026-07-19 | 1.0 | 初版作成（別担当引継ぎ用にPart A/B/Cを統合） | （本会話） |
