# 予定と実績の設計 — 決定事項（handover 反映前のレビュー用）

- 日付: 2026-07-28
- 目的: 本セッションで決まったことを全数記録し、**`handover/` へ反映する前にユーザーが点検できる**ようにする。
- 位置づけ: **レビュー用の一時文書**。承認後に `handover/` の各ファイルへ溶かし込み、本書は破棄する。
- 出所の区別: 「**XSD**」= `handover/01-mspdi/mspdi/mspdi_pj12.xsd` で確認。「**解説書**」= 上流の要素別リファレンスで確認。
  「**調査**」= 他ツールの横断調査。**製品名は中立化してある**（例外は MS Project / MSPDI）。

---

## 0. この設計が解いた問題

前プロジェクトは「予定 = `start`/`finish`、実績 = `actualStart`/`actualFinish`、進捗 = `progressRatio`」という
3 項目のモデルだったが、**進行中のタスクで実績バーの右端をどこに描くかが未定義**だった。

本セッションはそこから出発し、以下を経て**MSPDI の要素をそのまま使うモデル**に着地した。

```
案1  右端 = actualStart + 率 x 予定期間      率から位置を導出する
案2  右端 = 基準日                          経過をそのまま描く
案3  右端 = 案1 の分母を基準日まで伸ばす
到達点モデル  右端は人が置く日付。率は導出値
MSPDI 準拠モデル（確定）  右端 = MSPDI の Stop。新しい語を作らない
```

**却下理由**（同じ検討を繰り返さないため）:

| 案 | 却下理由 |
|---|---|
| 案 2 | 進行中のバーが全て基準日で終わるため、**遅れが描けない**。「予定 7/1–7/20 を 7/10 時点で 25%」のとき実績線は 7/5 で止まっていなければならない |
| 案 3 | 率を上げていないのに基準日が進むだけで前線が伸びる。**放置したタスクほど進んで見える**（予定 7/1–7/10・90% を放置すると前線が 7/23 → 8/19 と 27 日進む） |
| 案 1 | 率が 1 以下である以上、**進行中に予定終了日を越えられない**。「見積もりが甘くて予定より長くかかっている」を描けない |
| 到達点モデル | 方向は正しいが、`actualThrough` という**新語を作る必要があった**。MSPDI の `Stop` が同じ意味なので不要 |

---

## 1. 実績と進捗のデータモデル（確定）

### 1-1. 保存する項目 — すべて MSPDI と同名・同義

| GRS | MSPDI | 型 | 意味 |
|---|---|---|---|
| `start` / `finish` | `Start` / `Finish` | 日付 | 予定 |
| `actualStart` | `ActualStart` | 日付 | 実績開始 |
| **`stop`** | **`Stop`** | 日付 | **実績部分の終わり＝実績バーの右端** |
| **`resume`** | **`Resume`** | 日付 | **残りの部分が再開する予定日** |
| **`resumeValid`** | **`ResumeValid`** | 真偽 | **再開できるか** |
| `actualFinish` | `ActualFinish` | 日付 | 実際に終わった日 |
| **`percentComplete`** | **`PercentComplete`** | **整数 0〜100** | 進捗率 |
| `deadline` | `Deadline` | 日付 | 期限 |
| `milestone` | `Milestone` | 真偽 | マイルストーンか |

**解説書の原文**（この設計の根拠）:

```
Stop         For a Task, Stop is the date that represents the end of the actual portion of a task.
Resume       For a Task, Resume is the date the remaining portion of a task is scheduled to resume.
ResumeValid  Indicates whether the task can be resumed.
```

**重要**: `Resume` は「最後に再開した日」ではなく「**残りがいつから始まる予定か**」。過去の記録ではなく未来の予定である。

### 1-2. 中断は 1 回しか記録できない — XSD で確認

`Stop` / `Resume` / `ResumeValid` はいずれも **Min 0 / Max 1**。MSPDI は**中断の履歴を持たない**。
常に「今の境界」1 つだけを持つ。したがって:

- 2 回目に中断すると、記録は新しい境界に置き換わる（1 回目は残らない）
- `ActualFinish` が入ると残りの部分が無くなるので `Resume` は意味を失い、`Stop` = `ActualFinish` になる
- **完了したらバーは 1 本になる**

`TimephasedData` は `maxOccurs="unbounded"` だが、中身は `Type` の列挙が示すとおり
**工数・原価・進捗率の時間配分**であって、バーの分割区間を表す構造ではない。

> **GRS も同じ制約に倣う。** 分割区間のリストを持たない。持てば往復で必ず落ちる。

### 1-3. 4 状態の判別（拡張領域を 1 枠も使わない）

| 状態 | `actualStart` | `stop` | `actualFinish` | `resume` | `resumeValid` |
|---|---|---|---|---|---|
| 未着手 | 空 | 空 | 空 | 空 | — |
| 進行中 | あり | あり | 空 | 空 | true |
| **中断・再開予定あり** | あり | あり | 空 | **日付** | true |
| **中断・再開日未定** | あり | あり | 空 | 空 | **false** |
| 完了 | あり | あり | **あり** | 空 | false |

**すべて MSPDI ネイティブの要素で一意に判別できる。** 中止（＝再開日未定の中断）のために
拡張領域を消費する必要はない。

### 1-4. 進捗率の計算

```
ActualDuration  = stop − actualStart      稼働日
Duration        = finish − start          稼働日
PercentComplete = round(ActualDuration / Duration x 100)
```

- **格納する**（導出結果をデータとして保持する）。日付を編集したら再計算する。
- 分母は**予定線の長さ**。`actualStart` が `start` より後なら 100% には届かない。
  着手が遅れた分は取り返せていないので、これは正しい挙動である。
- **完了フラグが立ったら 100% とする**（`ActualFinish` があって 100 未満は MS Project では矛盾するため）。
- 単位は `DurationFormat` に従う。XSD の列挙に **`7=d`（稼働日）/ `8=ed`（暦日）** があり、
  既存の確定規則「元の値の書式を踏襲する」がそのまま効く。

> **未編集タスクは再計算しない。** 受け取った `PercentComplete` を Carry からそのまま書き戻す
> （既存の確定規則 §3-4 #3）。丸め誤差による往復差分を防ぐ唯一の方法。

### 1-5. マイルストーンの扱い

| | `Duration` | `stop` | `PercentComplete` |
|---|---|---|---|
| タスク | 予定日数 | `actualStart` + `ActualDuration` | 0〜100 の連続値 |
| **マイルストーン** | **0** | **`actualStart` と同じ**（幅がない） | **0 か 100 のみ** |

マイルストーンには実績の端点も進捗の掴み点も無い。**点そのものを掴んで移動する**。

---

## 2. 描画

### 2-1. 実績バー

```
左端 = actualStart      人が置く日付
右端 = stop             人が置く日付。勝手には動かない
```

- **完了を押しても右端は 1 日も動かない**（`actualFinish` に `stop` の値がそのまま入る）。
- **基準日を動かしても、バー・日付・進捗率のどれも動かない。** 基準日で動くのは
  イナズマ線と遅れ判定だけ。
- 完了したときだけ、右端に**実線の縦キャップ**を描く（`ActualFinish` という確定日であることを示す）。
  キャップが無ければ `Stop`（実績部分の終わり）である。

### 2-2. 字形 — 5 種

| 表記 | 名前 | 上下の幅 | 実績の置き方 | フェード掴み点 |
|---|---|:--:|---|:--:|
| `===` | `bar` | **あり** | 予定の内側に重ねる | あり |
| `>===>` | `chevron` | **あり** | 予定の内側に重ねる | あり |
| `--->` | `arrow` | なし | **予定の下にずらす** | なし |
| `*----*` | `endpointSpan` | なし | **予定の下にずらす** | なし |
| ◇ | `milestone`（`Task.milestone = true`） | なし | **予定の下にずらす** | なし |

**規則は 1 本**:

```
上下に幅がある字形（bar / chevron）  → 実績を内側に重ねる ＋ フェードあり
幅がない字形（arrow / endpointSpan / milestone）  → 実績を下にずらす ＋ フェードなし
```

- **`|-----|`（縦棒で端を示す細線）は廃止**。`*----*`（端に点を打つ）に置き換える。
- 予定バーの高さ > 実績バーの高さ とし、**上下に露出した帯で予定を掴む**。

### 2-3. 上下分離表示は廃止

`user-order.md` 項 52 の「重ね表示 / 上下分離表示の切替」と `[分離]` トグルを**廃止する**。

**廃止できる理由**: 分離が必要だったのは「予実を両方表示すると実績の端点を掴めない」ためだった。
**予定の高さを実績より大きくすれば幾何的に解決する**ので、モードで解く必要がなくなった。

**例外**: 幅のない 3 字形（`arrow` / `endpointSpan` / `milestone`）は内側に収められないため、
**その字形だけ実績を下にずらす**（局所的な分離）。文書全体のモードではなく、字形ごとの描き方である。

`[予定]` `[実績]` の 2 トグルは残す（表示の絞り込みとして有効）。

### 2-4. Progress Marker（状態の印）

実績バーの右端の**外側**に、**固定ピクセルサイズ**で描く。

| 状態 | 記号 | 表示条件 |
|---|---|---|
| 完了 | **`(✓)`** | 常時 |
| 中断 | **`( \ )`** | 常時。斜線は**左上から右下**（`( / )` ではない） |
| 期限超過 | **`(!)`** | 常時。未完了かつ 基準日 > `finish` |
| 未完了 | **`( )`** | **選択したときだけ薄く**表示 |

- **クリックで 3 状態を巡る**: 未完了 → 完了 → 中断 → 未完了。
- **記号は SVG で描く**（フォントの字形に依存しない）。
- **全体を非表示にするトグル**を用意する（記号が増えて見にくいときのため）。
- **不透明な下地を持つ**。予定バーに重なっても読める。色に依存せず形が意味を担う（WCAG 1.4.11 / 項 25）。
- 実績バーの右端から**固定 4px** 離す。端点の掴み代と重ならない最小距離であり、それ以上離さない。
- **占有幅に含める。** 含めないとマルチバー行で右隣のタスクと重なる
  （担当・進捗ラベルと同じ扱い。`handover-ui-detail-spec-ja.md` §4-2 の注意と同型）。

**なぜ固定サイズか**: 低ズームでバーが潰れても、記号は同じ大きさで残る。
`98%` と `100%` が同一ピクセルに落ちても、記号の有無で必ず分かれる。

**却下した符号化**:

| 案 | 却下理由 |
|---|---|
| ハッチング（斜線塗り） | 調査した描画ライブラリの公式アクセシビリティ文書が「パターン塗りと破線は視覚的に混乱させ、一部の利用者にはかえってアクセシブルでなくなり得る」と警告。低ズームで潰れる |
| 中抜き（hollow）で完了を表す | 作図慣行で**中抜き＝未着手**の意味が定着している |
| 取り消し線 | 作図慣行で**取り消し線＝キャンセル**の意味が定着している |
| 三角＋`!` | **システムエラーの通知に見える**。遅れを伝えるには強すぎる |
| 色だけ | 項 25（色だけで状態を伝えない）／ WCAG 1.4.1 違反 |

### 2-5. Resume アイコン

**中断のときだけ**、Progress Marker の右に出る。

- 形は **L 字の折れ矢印（角の丸い）**。下へ降りて右へ曲がり、矢じりが付く。
- `resume` が空のときは**薄い〇**として出る。クリックすると日付が入る。
- 日付が入ったら `X(resume)` の位置へ移り、**実績バーの右端から破線で繋がる**。以後はドラッグで動かせる。
- `resume` が空のまま中断している状態が **`resumeValid = false`（再開日未定）**。

---

## 3. 掴み点

### 3-1. 対象と位置

| 対象 | 掴み点 | 操作 |
|---|---|---|
| 予定 | 左端 | `start` |
| 予定 | 右端 | `finish` |
| 予定 | **左上の角** | フェードイン（`bar` / `chevron` のみ） |
| 予定 | **右下の角** | フェードアウト（`bar` / `chevron` のみ） |
| 予定 | 本体 | 平行移動（長さを保つ） |
| 実績 | 左端 | `actualStart` |
| 実績 | 右端 | **`stop`** |
| マイルストーン | 点そのもの | 平行移動のみ（端点は持たない） |
| Resume | アイコン | `resume` |

### 3-2. 掴み代は見た目より大きくとる

```
予定の端点   予定バーの上下に 6px ずつはみ出させる
実績の端点   実績バーの帯だけ（予定より狭い）
フェード     15 x 15px の角
ダミーの実績線  見た目 11px に対し 30 x 20px の当たり判定
```

**予定の高さ > 実績の高さ**なので、**上下の縁を狙えば予定、真ん中を狙えば実績**が掴める。
モードで切り替える必要がない。

> **ダミーの実績線の掴み代が小さすぎると着手できない。** モックで実際に起きた不具合である
> （11 x 10px では的が小さく、クリックできているのに気づけない）。

### 3-3. 優先順位

```
端点 > 名称ラベル > バー本体
```

理由: 端点は範囲が小さいので、大きい領域に負けると永久に掴めない。**小さいものを優先する。**

### 3-4. ダミーの実績線（初回入力）

実績が無いタスクには、**中抜き＋破線**の欠片を予定の開始位置に描く。掴むと `actualStart` と `stop` が入り、
ベタ塗りに変わる。

- **Start ボタンは作らない。** 最頻の操作（初回入力）の前に 1 クリック増え、モードも 1 つ増える。
- **中抜き・破線を選ぶ理由**: 作図慣行で「中抜き・破線枠＝未着手」の意味が定着しているため、
  学習が要らない。§2-4 で完了に中抜きを使わないと決めたのと矛盾しない（本来の意味どおりの用法）。

---

## 4. イナズマ線（Progress Line）

### 4-1. 頂点の規則

**規則は 1 文**: **動いているべき日を過ぎているものだけ、その日に頂点を打つ。**

| 状態 | 頂点 |
|---|---|
| 完了 | **打たない** |
| 中断・再開日未定 | **打たない**（止めると決めたものを遅れとして数えない） |
| 中断・再開予定あり | `resume` < 基準日 なら **`resume`** に打つ。まだ来ていなければ打たない |
| 未着手 | `start` < 基準日 なら **`start`** に打つ。まだ来ていなければ打たない |
| 進行中 | **`stop`** に打つ |

未着手の `start` と中断の `resume` は、どちらも「ここから動くはずだった日」なので同じ扱いになる。

### 4-2. マルチバー行 — 最も遅れた頂点が勝つ

**`user-order.md` 項 53 の「その行で最も進んだ頂点が勝つ」を「最も遅れた頂点が勝つ」に変更する。**

**変更理由**: 項 53 自身がイナズマ線の目的を「**どこがボトルネックか可視化**」と書いている。
最良値を採ると、行内に致命的に遅れたタスクがあっても隠れてしまい、目的と矛盾する。
マルチバーは本ツール最大の差別化（最優先事項 2）なので、1 行に複数タスクが載るのが標準である。

**実証**（モックで確認）:

```
1 行に 3 本  基準日 7/17
  前工程  7/1–7/7    完了            → 打たない
  本工程  7/9–7/15   stop 7/11       → 頂点 7/11   遅れ 6 日
  後工程  7/17–7/25  stop 7/19       → 頂点 7/19   遅れなし

最も遅れた（新）  行の頂点 = 7/11   6 日の遅れが見える
最も進んだ（旧）  行の頂点 = 7/19   遅れが完全に消える
```

### 4-3. 完了タスクを尖らせない根拠（調査）

| 裏付け | 強さ |
|---|---|
| 調査した描画ライブラリが実装コードで明示 — 頂点を打つのは「基準日より前に開始 かつ 進捗 < 1」または「基準日以降の開始 かつ 進捗 > 0」だけ。それ以外は直線 | **強** |
| MS Project の実挙動 — 「100% 完了で進捗点より前のタスクに左への尖りは意味を成さない」 | 中（**公式ドキュメントに記述なし**） |
| MS Project は未来開始のタスクを進捗線から除外する | 中〜強 |

> ⚠️ **日本のイナズマ線の作図慣行どおりに作ると壊れる。** 複数の日本語資料が一致して
> 「完了した場合は右端に点を打つ」と書いており、そのまま実装すると**完了タスクが左へ尖って
> 「遅れている」ように見える**。意図的に外す判断である。

> ⚠️ **MS Project の進捗線を全面的に真似ない。** 調査で、サマリタスクが常に含まれて除外できず、
> その頂点位置は「意味がない」と専門家が述べていることが確認できた。参考にはするが基準にはしない。

### 4-4. 基準日の縦線は常時表示にしない

**却下した案**: 「`[実績]` が ON のときは基準日の縦線を常に描く（表示トグルで消させない）」。

**却下理由**: イナズマ線だけを見たいとき、カーソルだけを見たいときに邪魔になる。
**表示内容はユーザーが選べるべき**である。`user-order.md` 項 43（Today Line・各カーソルは表示/非表示を
切り替えできる）をそのまま維持する。

縦線を消しても遅れは読める。**Progress Marker の `(!)`** が期限超過を、
**「遅れ」の日数**が量を示すため、縦線に依存しない。

### 4-5. 採らない方式

**MS Project 式のデータ正規化は採らない。** MS Project は描画で例外処理する代わりに、
実績データをステータス日へ寄せるオプション（基準日より後の完了部分を基準日まで戻す等）を持つ。
これは**ユーザーの入力データを書き換える**ため、Undo・自動保存・MSPDI 往復整合のすべてに波及する。
本ツールの価値は構造化データを保持することなので採用しない。

---

## 5. 進捗の編集入口

| 入口 | 採否 |
|---|---|
| **実績バーの右端をドラッグ**（`stop` を動かす） | **採用** |
| **Progress Marker のクリック**（3 状態を巡る） | **採用** |
| **Resume アイコンのクリック / ドラッグ** | **採用** |
| Properties Panel での日付入力 | 採用（従来どおり） |
| 右クリックのクイック設定（0 / 25 / 50 / 75 / 100%） | **不採用** |
| キーボードでの進捗増減 | **不採用** |

> **アクセシビリティの確認**: WCAG 2.1 の 2.1.1（キーボード操作可能）はドラッグ操作に
> キーボード等価物を要求する。**Properties Panel で日付を入力できればこれを満たす**
> （ドラッグは代替手段であって唯一の手段ではない）。Properties Panel が
> キーボードで到達・編集できることを実装時に確認すること。

**中断は専用操作を持たない。** 実績バーの右端をドラッグすることが「ここまで進んだ」であり
「ここで止まっている」でもある。同じ 1 つの操作なので、ボタンもモードも増えない。

**誤操作対策は Undo のみ。** 調査した範囲で、進捗のドラッグに確認ダイアログを出す製品も、
修飾キーを必須にする製品も 1 つも無かった。業界の答えは一貫して「確認するのではなく Undo で戻す」。

---

## 6. Undo / Redo

| 項目 | 決定 |
|---|---|
| 回数 | **50 回**。加えて**合計メモリの上限**でも打ち切る（1 操作で大量のタスクを変更した場合に備える） |
| 方式 | **不変更新のスナップショット**。明示的な差分機構（JSON Patch 等）は作らない |
| ドラッグ | **押した瞬間と離した瞬間で 1 エントリ**にまとめる |
| localStorage | **履歴を入れない**。保存するのは現在の文書だけ。クラッシュ復旧で履歴は失われるが許容する |
| Carry ストア | **履歴の対象に含める**。含めないと Undo で往復無損失が壊れる |

**スナップショットを選ぶ理由**: 状態管理が不変更新なので、**変更されていない部分は同じオブジェクトを共有する**。
50 スナップショット持っても実メモリは差分ぶんしか増えない。明示的な差分機構は
「差分の適用が壊れると文書が壊れる」という破壊的な失敗モードを持つので採らない。

**履歴に入れない操作**（入れると「Undo したのに何も戻らない」に見える）:

```
入れる    タスクの追加・削除・移動 / 日付・進捗・状態の変更 / プロパティの変更
          行階層の移動・畳み / 注記・強調枠の編集
入れない  [予定] [実績] のトグル / ズーム / スクロール / 選択 / パレットの移動 / 言語の切替
```

---

## 7. 命名

### 7-1. 「アイコン」という語の使い分け

「アイコン」は既に 2 つの意味で使われている（`iconShapeKind` と、メニュー・パレットの絵）。
**3 つ目の意味を与えない**（項 66）。

| 対象 | 旧 | **確定** |
|---|---|---|
| タスクの線の形 | `iconShapeKind` | **`shapeKind`** |
| ボタンやメニューの絵 | アイコン | **アイコン**（この用法だけ残す） |
| タスクの状態を示す印 | — | **`Progress Marker`** |

**`taskShapeKind` ではなく `shapeKind`**。`Task.taskShapeKind` は所属と重複する。
確定原則「UI とモデルの区別は名前空間で行う」（`handover-ui-parts-ja.md` §5-1 の理由 3）に従えば、
`Task.shapeKind` で文脈は決まる。マイルストーンも `Task` なので 1 列で両方を持つ既存の決定と矛盾しない。

### 7-2. 改名・廃止

| 旧 | 新 | 理由 |
|---|---|---|
| `iconShapeKind` | **`shapeKind`** | 「アイコン」の 3 つ目の用法を作らない |
| `progressRatio`（0〜1） | **`percentComplete`**（整数 0〜100） | MSPDI 同名・同型（`xsd:integer`） |
| `span`（字形名） | **`endpointSpan`** | `measuredSpanDays`（デュアルカーソルの計測スパン）と語義が衝突する（§4-2(c) で既に警告済み） |
| `actualThrough`（本セッションの検討中の語） | **不要**。`stop` を使う | MSPDI に同じ意味の要素がある |
| `progressStatus` | **廃止** | 下記 |

**`progressStatus` を廃止する理由**:

1. **GRS 独自の語**であり、MSPDI に受け皿が無い（項 20 の表で「拡張領域」行き）。
   `PercentComplete` とは別物（自由文字列 対 整数%）。
2. **状態が構造化されたので役目を失った。** 未完了 / 完了 / 中断（再開日あり）/ 中断（再開日未定）は
   `actualFinish` `resume` `resumeValid` で一意に表せる。期限超過は基準日と `finish` の比較で導出できる。
3. 廃止すると **`Progress` が 3 つから 2 つに減る**（`Progress Line` と `Progress Marker` だけ）。
4. **拡張領域の枠を 1 本節約**できる。
5. 自由文の説明は `notes` で書ける（テキスト列を増やさない方針とも合う）。

### 7-3. パネル表示の文字は割り当てない

`handover-ui-detail-spec-ja.md` §2 の確定（2026-07-26）に
「**全ボタンをアイコン化する（テキストラベルをやめる）。`SS` のような略語は項 6-6 に反する**」とある。

したがって `Progress Marker` にも `M` や `S` のような表示文字を割り当てない。
`Milestone` の `M` との衝突を考える必要がない。

短い符号が必要なのは実装内部（`data-role` / CSS クラス）だけで、そこは kebab-case のフル語
（`progress-marker`）を使う（§1-2 の記法規則）。

### 7-4. UI パーツ木への追加

```
Schedule Canvas
├─ Rows
│   └─ Task Bars
│       └─ Progress Marker      (✓) 完了 / ( \ ) 中断 / (!) 期限超過 / ( ) 未完了
└─ Canvas Overlays
    └─ Progress Line            イナズマ線（既存）
```

`Progress Marker` と `Progress Line` は**別のパーツ**。語幹が揃うのは「どちらも進み具合を示す」ため。
片方は行を横断する線、片方はタスクに付く印、と役割で読み分ける。

---

## 8. MSPDI との対応

### 8-1. Own（読んで保持し、書き戻す）

```
Start / Finish / ActualStart / ActualFinish / Deadline / Milestone
Stop / Resume / ResumeValid          ← 本セッションで拡張領域から Own へ変更
PercentComplete
```

**変更点**: 前プロジェクトは §3-4 #8 で「`Stop` / `Resume` は MSPDI へ写さず拡張領域に置く」と決めていた。
理由は「MSPDI の `Stop` の意味は『実績が入っている境界』で、GRS の中断/再開とずれる」だった。

**この理由は消えた。** 解説書で `Stop` = "the end of the actual portion of a task" と確定し、
GRS の実績バーの右端と**同じ意味**であることが分かったため。ずれていたのは旧モデルの
「中断/再開」であって、新しいモデルとは一致する。

### 8-2. 拡張領域の消費（更新）

| 属性 | 旧 | **新** |
|---|---|---|
| `progressStatus` | 1 枠 | **0**（廃止） |
| `stop` / `resume` | 2 枠 | **0**（MSPDI ネイティブへ） |
| 中止フラグ | （未検討） | **0**（`resumeValid` で表現） |
| `importance` | 1 枠 | 1 枠 |
| `fadeInDays` / `fadeOutDays` | 2 枠 | 2 枠 |
| **合計** | **6 枠** | **3 枠** |

**往復させる属性が 6 → 3 に減った。** 実機確認（枠が何番まで使えるか）の結果が
「少ない」だった場合でも、ハイブリッド案に落とす必要がなくなる可能性が高い。

### 8-3. XSD で確認した事実（推測ではない）

| 事実 | 内容 |
|---|---|
| `CompleteThrough` | **存在しない**。MS Project が進捗の塗りに使う日付は MSPDI に serialize されない |
| `Active` | **存在しない**。MS Project の「非アクティブ タスク」は本スキーマに無い |
| `IsNull` | 存在するが「**空行かどうか**」の意味。中止の表現に流用してはならない |
| `PercentComplete` | `xsd:integer`。"The percentage of the task duration completed." |
| `ActualDuration` | "The span of actual working time for a task so far, based on the scheduled duration and current remaining work or percent complete. **Actual duration can be calculated in two ways, either based on percent complete or remaining duration.**" |
| `Stop` / `Resume` / `ResumeValid` | Min 0 / **Max 1**。履歴を持たない |
| `DurationFormat` | `7=d`（日）/ `8=ed`（経過日）を含む。**稼働日と暦日の区別がここにある** |
| `TimephasedData` | `maxOccurs="unbounded"` だが中身は工数・原価・進捗率の時間配分。**バーの分割区間ではない** |

---

## 9. 既存 handover との差分（反映先）

| ファイル | 箇所 | 変更 |
|---|---|---|
| `user-order.md` | 項 50 | 実績の入力規則を全面差し替え（3 状態 → 4 状態。`actualFinish`/`progressRatio` → `stop`/`resume`/`resumeValid`/`percentComplete`） |
| `user-order.md` | **項 52** | **削除**（上下分離表示の廃止） |
| `user-order.md` | 項 53 | イナズマ線の頂点規則を差し替え。**「最も進んだ頂点が勝つ」→「最も遅れた頂点が勝つ」** |
| `user-order.md` | 項 20 | プロパティ表を更新（`progressStatus` 削除 / `progressRatio` → `percentComplete` / `iconShapeKind` → `shapeKind` / `stop`・`resume` の MSPDI 列を「無い」から実要素名へ） |
| `user-order.md` | 項 10 | 字形の一覧を差し替え（`|---|` を `*---*` へ） |
| `handover-ui-detail-spec-ja.md` | §4-0 | 入力規則を差し替え |
| 同 | §4-1 | 掴み点に Resume を追加。掴み代の大きさの規定を追加 |
| 同 | §4-3 | 重ね順に Progress Marker を追加 |
| 同 | §4-4 | **`[分離]` トグルを削除**。「モードで解く」を「高さの差で解く」に差し替え |
| 同 | §3 | パレットの図形一覧を字形 5 種に更新 |
| 同 | **§4-6（新設）** | **Progress Marker と Resume アイコンの仕様** |
| `handover-ui-parts-ja.md` | §3 | UI パーツ木に `Progress Marker` を追加 |
| 同 | §4-2(a)(b) | `iconShapeKind` → `shapeKind` / `progressStatus` 廃止 / `progressRatio` → `percentComplete` |
| `handover-property-mspdi-mapping-ja.md` | §3 全体 | 到達点モデルへ差し替え。`Stop`/`Resume`/`ResumeValid` を Own へ |
| 同 | §3-4 #8 | **判断を撤回**（拡張領域 → Own）。撤回理由を記録 |
| 同 | §4-1 / §4-2 | 往復対象を 6 属性 → **3 属性**に更新。C-1 の条件分岐を見直す |
| `grs-native-erd-ja.md` | Task の属性 | 実績まわりを差し替え |
| **`OPEN-ITEMS-ja.md`（新規）** | — | 実機確認 3 件 |

---

## 10. 実機確認の残件（3 件）

| # | 確認すること | 影響 |
|:--:|---|---|
| 1 | MS Project で `Text` / `Number` / `Date` / `Flag` の枠が**何番まで使えるか** | 拡張領域の割当表。ただし消費が 6 → 3 に減ったので**条件は緩んだ** |
| 2 | 進捗の塗りの**起点**が `Start` か `ActualStart` か | `ActualStart ≠ Start` のとき両ツールの絵がずれる |
| 3 | 完了時に `Stop` / `Resume` を**どう書き出すか** | 優先度低。GRS 側は `stop = actualFinish` で整合するので結果は同じ |

**解決済みとして落とした項目**:

| 旧 # | 内容 | 落とした理由 |
|---|---|---|
| — | MS Project が期間の量と率のどちらを信じるか | 解説書に「**2 通りの計算方法がある**」と明記。実機でも一意の答えは出ない。GRS 側で整合させる既存方針が正しいことが裏付けられた |
| — | `Stop` の意味 | 解説書で "the end of the actual portion of a task" と確定 |

---

## 11. 未決事項

| # | 内容 | 状況 |
|:--:|---|---|
| 1 | `Progress Marker` の記号を実際に描いたときの見やすさ | **PoC で確認する**。特に低ズームでの判別 |
| 2 | 幅のない 3 字形で実績を下にずらしたときの行高 | 行が高くなる。マルチバーの段数と干渉しないか未検証 |
| 3 | Resume アイコンがマルチバー行で隣のタスクと重なる条件 | 占有幅に含める規則は決めたが、実測していない |

---

## 12. この設計で消えた問題

記録として残す。同じ議論を繰り返さないため。

| 消えた問題 | 理由 |
|---|---|
| 進行中の実績バーの右端が未定義 | `stop` が定義そのもの |
| 「完了させるとバーが跳ねる」 | `actualFinish` に `stop` の値が入るので動かない |
| 「未完了に戻したとき進捗率をどうするか」 | 進捗率が導出値になったので自動で戻る。完了直前の値を覚える仕組みが要らない |
| 「進捗率の分母をどちらにするか」 | MSPDI の `PercentComplete = ActualDuration / Duration` に一致させて決着 |
| 「到達点という新語をどう名づけるか」 | MSPDI の `Stop` が同じ意味。新語が要らない |
| 「中止を拡張領域のどの枠に置くか」 | `resumeValid = false` で表現でき、枠を使わない |
| 「予実が重なって編集できない」 | 予定の高さ > 実績の高さ で幾何的に解決。モードが要らない |
| 「イナズマ線の頂点の 5 ケース」 | `stop` を参照するだけになり、式が消えた |
