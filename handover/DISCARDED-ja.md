---
type: Reference
title: 資産の選別台帳
description: 引継がなかった資産とその理由。同じものを作り直さないために読む。
tags: [index]
phase: packaging
status: stable
---
# 資産の選別台帳（引継ぎ Step 5 成果物）

- 日付: 2026-07-26
- 目的: 前プロジェクトの全資産を **引継ぐ / 参考 / 破棄** の 3 分類に仕分け、
  **破棄したものはその理由を残す**。同じものを作り直さないため、
  および「なぜ渡されていないのか」を次期が問い直せるようにするため。
- 分類の定義:

| 分類 | 意味 | 置き場所 |
|---|---|---|
| **引継ぐ** | 次期の入力として使う。`handover/` にコピー済み | `handover/` |
| **参考** | 捨てないが `handover/` には入れない。必要になったら凍結リポジトリを見る | 凍結リポジトリのまま |
| **破棄** | 次期に持ち込まない。**理由を本書に記録** | 凍結リポジトリのまま（削除はしない） |

> **「破棄」は削除ではない。** 凍結リポジトリは git 履歴とともに残る。
> 破棄とは「**次期の入力に含めない**」という判断である。

---

## 1. 全体像

| 分類 | 件数 | 内訳 |
|---|:--:|---|
| **引継ぐ** | **30 ファイル** | 文書 29 本 ＋ PoC の `.html` **1**（`handover/` 全体は索引 3 本を含め **33 ファイル**） |
| **参考** | 約 42 ファイル | 分析文書 5 / アーキ図 7 / `.sdoc` 19 / モック 1 / **既存ツール比較の調査記録 1** / **前プロジェクトの用語集 1** / プロセス記録の主要分 |
| **破棄** | 約 320 ファイル | 実装 96 ＋ テスト 101 ＋ 生成物 ＋ フレームワーク側資産 ＋ 旧データ契約 |

> **2026-07-26 追記（ドメイン中立化）**: 「特定領域に特化していると誤解されたくない」という方針により、
> `handover/` から**特定業種の例示と特定製品名を排除**した。語の対応は `NEUTRALIZED-TERMS-ja.md`。
> この方針で**既存ツール比較の調査記録を「引継ぐ」から「参考」へ移した**（下記 §3）。

---

## 2. 引継ぐ（30 ファイル）

> **MSPDI の正本 XSD は同梱していない**（ライセンスの都合。`README.md` §0-3 / `01-mspdi/mspdi/README.md`）。
> 上流の MS Learn 文書（CC-BY-4.0）も同じ理由で入れていない。**第三者の著作物は 1 つも再配布しない**という 1 本の規則である。

`handover/README.md` §1「読む順」および §2「フォルダの構成」を参照。要約:

| 束 | 中身 | 引継ぐ理由 |
|---|---|---|
| ルート直下（6） | **`user-order.md`**（次期開発の入力。項 1〜68。**11 / 14 / 52 が欠番**＝実質 65 項目。**これが次期の起点**で Step 1 でゼロベース書き直し済み）／`README.md`（索引）／**`NEXT-STEPS-ja.md`**（実開発ステップ別の欠落 14 件）／`OPEN-ITEMS-ja.md`（実機確認の残件 3 件）／`DISCARDED-ja.md`（本書）／`NEUTRALIZED-TERMS-ja.md` | 入口・入力・残件・選別の記録 |
| `01-mspdi/`（5） | 落とし穴・enum 全数・構造・テーブル責務 ＋ **`mspdi/README.md`**（正本 XSD の入手方法・照合ハッシュ・ライセンス） | **製品に依存しない事実**。MS Project 側は変わらないので次期でもそのまま有効。落とし穴は「素直に実装すると必ず踏む」ものだけを選抜済み。⚠️ **XSD 本体は同梱していない**（上の注記） |
| `02-data-model/`（6） | 入口・**ERD 確定版**・**設定値の確定版**・取捨選択台帳・**プロパティ×MSPDI 対応**・設計判断の変遷 | データ構造は前プロジェクトで**最後に決着した最新の判断**。ここだけは実装より先に進んでいる |
| `03-ui-naming/`（2） | UI パーツ確定名（**命名の正**）・**UI 詳細仕様**（掴み領域・操作割当・予実の編集モデル） | **語彙の重複がバグ根因**だった。1 概念 1 語を最初から守るための唯一の材料。**用語の正は 4 つだけ**にした（命名＝ここ／データ＝`grs-native-erd-ja.md`／予実＝`handover-plan-actual-decisions-ja.md`／設定値＝`grs-document-settings-ja.md`） |
| `04-performance/`（1） | 効いた手 / 効かなかった手 / 罠 / 実測基準線 / 参考実装の設計要点 | **ぬるサクは設計書では引き継げない**。最優先事項 1 の実体 |
| `05-security-a11y/`（2） | 脅威モデル・サニタイズ設計 / WCAG 2.1 AA | どちらも**生きている要求**（`user-order.md` 62 / 条件付きプロセス有効） |
| `06-background/`（4） | 陳腐化監査・改訂差分・バグ根因分析・予実未決モデル | ⚠️ **2026-08-04 に全 4 本を破棄した**（§7）。結論は引く側の本文へ移してある |
| `07-plan-actual/`（1） | **予実・進捗の確定設計**（MSPDI 準拠モデル・5 状態・形状・Progress Marker・イナズマ線・Undo・命名） | **この領域の正**。`08-poc` 以外の複数文書の予実記述を上書きする |
| `08-poc/`（4 ＋ `.html` **1**） | 開き方・共通仕様・**計測結果**・判断が要った 7 件 ＋ **動く PoC 1 本**（6 タブ） | **読む前に開いて触れる**唯一の資産。サーバー不要。案比較の 5 本は 2026-08-02 に外した（§4-4） |
| `09-architecture/`（1） | アーキ領域の**所在**（描画方式の結論はどこか / 前提だけ参照されている未設計 / 意図的な空白） | 決定はしない。**散らばった既決を 1 か所に集める**ためだけにある |

> **内訳の合計**: `.md` 32（6 ＋ 5 ＋ 6 ＋ 2 ＋ 1 ＋ 2 ＋ 4 ＋ 1 ＋ 4 ＋ 1）＋ `.html` **1** ＝ **33 ファイル**。
> このうち索引 3 本（`README.md` × 3）を除いた **30** が §1 の「引継ぐ」である。**機械カウント（2026-08-02）。**

---

## 3. 参考（`handover/` に入れないが捨てない）

| 資産 | 件数 | 参考に留めた理由 |
|---|:--:|---|
| **`old/日程管理ツール.md`**（既存ツール比較の調査記録・513 行） | 1 | **当初は引継ぐ判断だったが 2026-07-26 に「参考」へ変更**（ドメイン中立化）。本書は「**特定製品の比較評価 × 特定業種の開発**」を前提に書かれており、実名比較 46 箇所・業種前提の記述多数。**中立化すると比較表の情報価値そのものが消える**（実名なしの「ツール A/B/C」は検証不能）。一方、必要な結論＝「調査した既存ツールはいずれも 1 行マルチバーを持たない → 自社構築」は `user-order.md`「それはどうして？」に吸収済みなので、**引継ぎに実物は要らない**。選定の妥当性を後から問い直すときだけ凍結リポジトリで読む |
| **前プロジェクトの用語集**（`docs/spec/glossary.md`・387 行 / 199 表行） | 1 | **当初は引継ぐ判断だったが 2026-07-26 に「参考」へ変更**。理由: ①**表行の大半が旧名**（「改名予定」列を 18 箇所持つ）②記法規則は `handover-ui-parts-ja.md` §1-2 に、揺れの決着は同 §2/§4/§4-2/§5 に**吸収済み**（重複）③UI 表示文字列の ja/en 対訳は**パーツ名が確定名に変わったので使えない** ④§10 の JSON 構造は旧 flat 形状で `40-data-format.sdoc` と同じ理由で破棄すべきもの。**残すと「どちらが勝つか」を読む側が毎回判断させられる**。未吸収だった §6（掴み領域）は `handover-ui-detail-spec-ja.md` §4-1 へ**確定名で移してから**外した。→ **用語の正は 4 つだけ**（命名＝ui-parts / データ＝grs-native-erd / 予実＝handover-plan-actual-decisions / 設定値＝grs-document-settings） |
| **`docs/spec/*.sdoc`**（19 本 / 293 要求） | 19 | **陳腐化を含む**（旧仕様の陳腐化監査が §2 で 6 件、§3 で 3 件を特定した。**監査文書は 2026-08-04 に破棄**）。機能要求 約 145 件は `user-order.md` に抽出・統合済み。**293 要求をそのまま持ち込むと、古い記述がノイズになる**（Step 0 の発見そのもの）。ただし**網羅性の照合用**に価値があるため捨てない。凍結リポジトリで参照する |
| `docs/spec/vendor/mspdi/learn-docs/`（上流 MS Learn 文書 390 本 / 2.7MB） | 390 | 全要素の英語リファレンス。**正本は XSD** なのでこちらは補助。2.7MB の重複を避けて `handover/` から外した。**必要になったら上流から取得すればよい**（手順は `01-mspdi/mspdi/README.md`。CC-BY-4.0 なので帰属表示を付ければ再配布もできるが、**本リポジトリでは第三者の著作物を一切再配布しない**方針により入れていない） |
| `docs/spec/vendor/mspdi-declutter-erd-ja.md`（725 行） | 1 | MSPDI スキーマの選別を導く途中分析。結論は `grs-mspdi-field-ledger-ja.md`（引継ぐ）に落ちている。**過程は参考** |
| `docs/analysis/glossary-open-questions-ja.md`（213 行） | 1 | 用語の未決 22 件の作業メモ。確定分は `handover-ui-parts-ja.md` に反映済み |
| `docs/analysis/grs-table-erd-comparison-ja.md`（252 行） | 1 | テーブル表現 vs ERD の比較検討。`grs-native-erd-ja.md` で決着済み。**却下側の根拠として参考** |
| `docs/analysis/mspdi-json-fidelity.md`（164 行） | 1 | 往復忠実度の初期分析。結論は ledger の Carry / Drop=0 規約に吸収済み |
| `docs/spec/_assets/` のアーキ図・シーケンス図（`arch-*.md` / `sequence-*.md` / `component-architecture-full.md` / `domain-model-class.md`） | 7 | **現行実装の構造を描いたもの**。実装は作り直すので設計の出発点にはしない。ただし「往復の手順」「自動保存の状態遷移」は考え方として参考になる |
| `docs/mock/gr-scheduler-mock.html`（21KB） | 1 | 初期の WYSIWYG 視覚モック。**視覚的な目標としては有効**だが、旧語彙の DOM を含み **コピペ誘因**になるため `handover/` から外した。見るだけならよい |
| `docs/dev/perf-benchmark.md`（85 行） | 1 | 計測ハーネスの手順書。実装が変わるので手順自体は使えないが「何を測るか」の定義は有効。**要点は `handover-performance-notes-ja.md` §2-4 / §2-5 に吸収済み** |
| `project-records/` の主要分（DEC-001/002・CR-001〜016・DEF-001〜014・リスク台帳・レビュー記録） | 約 50 | **性能ノートと監査文書の出典**。引継ぎ文書から引用済みなので原典を持ち込む必要はないが、根拠を辿るために残す |
| `project-management/`（最終報告・受入手順・引継ぎメモ・WBS・インタビュー記録） | 9 | プロセスの記録。次期は別プロセスで回る。**未回答論点の出典**（`handoff-cr-013-016-followups.md`）として参考 |

---

## 4. 破棄（次期の入力に含めない）— **理由の記録**

### 4-1. 実装とテスト

| 資産 | 件数 | **破棄理由** |
|---|:--:|---|
| `src/**/*.ts` | 96 | **全て旧名**（`item` / `itemKind`(`'task'`/`'milestone'`) / `laneIndex` / `section` / `ScheduleItem` / `actualEnd` / `startDate`/`endDate` …）。確定名は `Task` / `Task.milestone`(bool) / `TaskGroupMember.stackOrder` / `TaskGroup` / `actualFinish` / `start`/`finish`。**コピペすると前プロジェクトのバグ根因（画面とデータの語彙混在）がそのまま再生産される**。ユーザー指示: 既存はあくまで参考案であり、命名規則を中心に**完全新規に設計・実装する。コピペは禁止**。設計の要点だけ `handover/04-performance/` §4 に言語化した（依存線の自動配線 / 多段レイアウト / ズーム・パンの描画経路の 3 領域） |
| `tests/*.ts`（単体 80） | 80 | 同上（旧名の API を検証している）。加えて **domain/usecase の純関数偏重**という構造的欠陥がある（レンダラは E2E だけが実機検証していた＝「緑なのに壊れる」の原因）。**テスト構成ごと引き継ぐべきではない**。教訓は `handover-performance-notes-ja.md` N-5 / T-8 に記録 |
| `tests/e2e/*`（21） | 21 | 同上。旧 `data-role` / 旧アクセシブル名に依存 |
| `index.html` / `vite.config.ts` / `vitest.config.ts` / `playwright.config.ts` / `eslint.config.js` / `tsconfig.json` / `package.json` / `package-lock.json` | 8 | ビルド構成。単一 HTML 化（`vite-plugin-singlefile`）と CSP ハッシュの組合せは有効な知見だが、**構成ファイルは次期の技術選定後に書く**もの。知見（**文字列キーは ASCII のみ**でないと CSP ハッシュが壊れる）は性能ノート T-9 に記録済み |

### 4-2. 旧データ契約（ユーザー明示で判断材料から除外）

| 資産 | **破棄理由** |
|---|---|
| `docs/spec/40-data-format.sdoc`（27 要求） | **ユーザー明示により判断材料から除外**。旧 flat 形状（`rows` / `sections` / `items` / `dependency.id` / `lagDays` / `startDate`/`endDate`）で、GRS ネイティブ **14 エンティティ**（ERD 本体 12 ＋ 注記 2）へ全面置換された。`grs-native-erd-ja.md` が正 |
| `docs/api/gr-scheduler.schema.json`（10.9KB） | 同上。旧 JSON Schema。**予定日付が `startDate`/`endDate`** で、確定した `start`/`finish` と食い違う。次期が SSOT と誤認する危険が最も高いファイル |
| `old/gr-scheduler-template.json` | **廃止済みの予実別行モデル（旧 IM7 方式）で書かれている** — 行 id に `Milestones-Plan` / `Milestones-Actual` があり、予定と実績を**別行**に分けている。これは CR-001 で「同一アイテムが実績日を持つ」方式へ移行し、CR-012 でサンプルも追従した**廃止済みモデル**。さらに行 id に**制御文字 `U+0001`** を含む（性能ノート T-9 の CSP ハッシュ破壊と同種の危険）。**持ち込むと旧モデルが復活する**。初期テンプレートは `user-order.md` 63 の要求として**新規に作る** |
| `docs/spec/gur-components.md`（44 行） | 旧 GUI 木。**抜け 4 件**（依存線・グリッド線・透かし・モーダル）があり、それを統合した完全版が `handover-ui-parts-ja.md` §3 にある。旧名（`Activity Title Panel` / `Activity Spans` / `ribbon`）で書かれている |

### 4-3. 生成物

| 資産 | **破棄理由** |
|---|---|
| `dist/index.html`（309,503 バイト） | ビルド成果物。サイズは基準線として性能ノート §2-4 に記録済み |
| `output/`（StrictDoc HTML 出力・`_cache`） | 生成物。`.sdoc` から再生成できる |
| `docs/spec/__pycache__/` | Python バイトコード。生成物 |
| `test-results/` | Playwright の実行結果。生成物 |
| `node_modules/` | 依存。生成物 |

### 4-4. gr-sw-maker フレームワーク側の資産（**製品資産ではない**）

**次期でもフレームワークから供給される**ため、製品の引継ぎ資産に含めない。
gr-scheduler 固有の内容は持っていない。

| 資産 | 件数 | **破棄理由** |
|---|:--:|---|
| `process-rules/`（プロセス規則・文書管理規則・エージェント一覧・用語集・defect 分類・レビュー観点・移植ガイド・プロンプト構造・仕様テンプレート、各 ja/en/無印） | 33 | **開発フレームワーク（gr-sw-maker）の規則**。製品固有ではない。次期は最新版がフレームワークから来る |
| `essays/`（ANMS / ANGS エッセイ、グラフDB比較、SDD フレームワーク比較 等） | 13 | **フレームワークの調査・論考**。gr-scheduler の設計判断には使われていない |
| `CLAUDE.md` / `CLAUDE-ja.md` / `CLAUDE-en.md` | 3 | プロジェクト指示書。**次期は次期の CLAUDE.md を書く**（技術スタック・品質目標は `user-order.md` から再導出する） |
| `user-order-ja.md` / `user-order-en.md` | 2 | **中身が空のフレームワークテンプレート**（13 行のコメントのみ）。正本は `user-order.md`。**紛らわしいので次期に持ち込まない** |
| `setup.js` / `tools/jsonl2md.mjs` | 2 | フレームワークのセットアップ / ログ変換ユーティリティ |
| `.claude/`（エージェント定義・スラッシュコマンド） | — | フレームワーク側。次期でも供給される |
| `README.md` / `README-ja.md` / `LICENSE` / `NOTICE` | 4 | リポジトリのメタ情報。次期リポジトリで新規に書く |

### 4-4b. PoC の案比較 5 本（2026-08-02）

| 資産 | 件数 | **破棄理由** |
|---|:--:|---|
| `poc-layout-A-orthogonal-channel.html` / `-B-anchor-search` / `-C-reserved-lane` / `poc-zoom-A-depth-threshold` / `-B-fit-first` | 5 | **比較は決着済み**（**9 点アンカーの候補探索** ＋ **深さしきい値のズーム** を推奨）で、**計測値は `08-poc/POC-RESULTS-ja.md` に全数記録されている**。現物は**廃止語を 158 箇所（99 行）抱えており**（`lane` / `actualEnd` / `overdue`／定数 `LANE_H` `LANE_OFF`）、`README.md` §0-1「コピペ禁止」に対する最大の誘因だった。次期は「自分の数字を取り直す」方針（同 §0-2）なので、前回の比較実装を参照する場面が無い。**PoC は `poc-integrated.html` 1 本に絞った** |

### 4-5. 破棄しなかったが**注意が要る**もの

| 資産 | 注意点 |
|---|---|
| `project-records/defects/DEF-014`（ルーラー boundingBox が稀に null） | **原因未確定・Open のまま終了**。緑になったから解決したとはみなしていない。次期で同種の症状が出たら**起動時の寸法確定順序**を疑う（性能ノート T-10） |
| `project-records/reviews/` の M-6 / M-01 | 「実測してから判断」と決めて**実測されなかった**指摘。次期の計測項目 U-1 / U-2 として性能ノート §2-5 に引き継いだ |
| `handoff-cr-013-016-followups.md` | **CR-017（予実の編集モデル）を起票する**と書かれているが CR-017 は存在しない。未決論点として `user-order.md`「次期の開始前に決めること」3 件に昇格させた |

---

## 5. 選別で使った判断基準

次期が同種の選別をするときのため、基準を残す。

| # | 基準 | 適用例 |
|---|---|---|
| 1 | **製品に依存しない事実は最優先で引継ぐ** | MSPDI の落とし穴・enum・正本 XSD（MS Project 側は変わらない） |
| 2 | **最後に決着した判断を引継ぎ、途中経過は参考に落とす** | `grs-native-erd-ja.md` は引継ぐ / `mspdi-declutter-erd-ja.md`・`grs-table-erd-comparison-ja.md` は参考 |
| 3 | **旧語彙を含むものは引継がない**（コピペ誘因を物理的に排除する） | `src/` / `tests/` / `gur-components.md` / モック HTML |
| 4 | **「正」が 2 つになるものは片方を破棄する** | `schema.json` と `40-data-format.sdoc` は `grs-native-erd-ja.md` と競合するので破棄 |
| 5 | **経験知（何をしなかったか）は文書がなければ失われるので必ず作る** | 性能ノートを新規作成（前プロジェクトには存在しなかった） |
| 6 | **生成できるものは引継がない** | `dist/` / `output/` / `__pycache__` / `test-results/` |
| 7 | **フレームワークから供給されるものは製品資産に混ぜない** | `process-rules/` / `essays/` / `CLAUDE.md` |
| 8 | **未解決・未計測は「解決済み」と偽らずに引継ぐ** | DEF-014（原因未確定）/ U-1〜U-5（未計測）を明示 |
| 9 | **特定業種・特定製品に依存する記述は引継がない**（汎用ツールと誤解されなくなるため） | 例示を中立語へ置換／対向ツールは役割名で呼ぶ（`NEUTRALIZED-TERMS-ja.md`）／既存ツール比較の調査記録は参考へ。**例外は `MS Project`/`MSPDI`**（交換フォーマット＝機能要求） |

---

## 6. 機械向けインターフェースの取り込みで落としたもの（2026-08-04）

`10-agent-interface/` を作るにあたり、AI 共同編集トライアルの成果から**次を落とした**。

| 落としたもの | 理由 |
|---|---|
| **要求 A-17「非 ASCII のデータをシェル引数に載せない」** | **シェル引数を持つ部品が存在しない。** 決定-1 が「成果物は当面 単一 `.html` だけ。ライブラリ／CLI は出さない」と定めたため、**存在しない部品への要求は先回りである**（レビュー観点規約 R2.9 YAGNI）。⚠️ **実測は捨てていない** — Windows のコンソールのコードページがプロセスに届く前にバイト列を壊した記録は `10-agent-interface/ai-cowork-trial-findings-ja.md` §3-2 にある。**CLI を出す日が来たらそこを読む** |
| **トライアルの試作コード**（`.mjs` / `.html` 一式・題材の実装） | **基準 3**（旧語彙を含むものは引継がない）。トライアルのコードは**題材（盤ゲーム）の語彙**で書かれており、GRS の確定名ではない。⚠️ **ただし実測の再現手順としては生きている** — 8 件の決着それぞれに、どのファイルで再現できるかが `10-agent-interface/agent-interface-open-items-ja.md` §0 の表に書いてある。**実物は凍結リポジトリの `ai-cowork-trial/` にある** |
| **ドラフト一式の入口**（`README.md`）と**組込み手順**（`MERGE-PLAN-ja.md`） | **基準 4**（正が 2 つになるものは片方を破棄する）。入口は `handover/README.md` が持ち、組込みは終わった。**役目が終わった作業文書である** |

> **要求は 18 件のうち 17 件を採用した。** 全数と根拠は
> `10-agent-interface/agent-interface-requirements-ja.md`。

---

## 7. 経緯の文書 6 本を破棄した（2026-08-04）

**基準は 1 つ** — 次期は **AI に製品版を実装させる**。したがって残すのは
**最新の要求と、その理由／妥当性**だけである。**旧実装の構造記述と旧語彙の図表は、
読ませると誤って実装される**ので置かない。

⚠️ **却下案とその理由は「妥当性」なので残している。** 同じ案を再提案させないために要る
（`02-data-model/grs-native-erd-ja.md` **§8I** の 18 件、`07-plan-actual/handover-plan-actual-decisions-ja.md`
§4-5「採らない方式」など）。**落としたのは経緯であって、判断の根拠ではない。**

| 破棄したもの | 量 | 理由 | 結論はどこへ |
|---|--:|---|---|
| `06-background/refactor-gui-data-separation-ja.md` | 14.6KB | **旧名で書かれた UI 木**（`Activity Title Panel` → `Activity List` → `Activity Rows` → `Canvas Items` → `Activity Spans`）と、**却下された提案が提案の形のまま**残っていた（「`taskShape` は `activitySpanShape`、`itemKind` の値 `'task'` は `'activity-span'` になる」）。**旧語彙を教える文書**である | 結論「語彙の重複が前プロジェクトのバグ根因」は **`README.md` §0-1** が本文で持つ |
| `06-background/plan-actual-visibility-operability-model-ja.md` | 17.3KB | **旧案**。`07-plan-actual/handover-plan-actual-decisions-ja.md` が全面的に上書き済み。旧名の表を含む | 07 が正。採らなかった方式は同 §4-5 |
| `06-background/handover-stale-spec-audit-ja.md` | 12.4KB | **対象の `.sdoc` 19 本が `handover/` に無い**（外部参照 18 箇所）。旧仕様を監査する文書は、旧仕様を持たない次期には使えない | 結論（§2 に 6 件・§3 に 3 件）は本書 §3 が本文で持つ |
| `06-background/handover-user-order-diff-ja.md` | 8.4KB | `user-order.md` **改訂の経緯**。現行の `user-order.md` そのものが結論である | `user-order.md` |
| `08-poc/OPEN-QUESTIONS-ja.md` | 4.9KB | **全 7 件が決着済み**の記録＝経緯。決定内容は各正の文書へ反映済み | 各正の文書 |
| `02-data-model/grs-data-model-ja.md` | 45.2KB | `deprecated` なのに §2 / §4 / §7 が「**確定**」と書いており、**正が 2 つある状態**だった。§7.3 は `progressRatio` / `iconShapeKind` / `importance` という**廃止済みの名前で `TaskVisual` を定義**していた | ⚠️ **生きていた 2 節を正へ移設した** — §6（往復規約・実機検証の出所）→ `02-data-model/grs-native-erd-ja.md` **§8H**、§8（設計判断の変遷 18 件＋教訓）→ 同 **§8I**。**内容は変えていない** |

**合計 102.8KB。`06-background/` フォルダは消滅した。**

> **被参照 44 箇所はすべて追随させた**（**32 行**を書き換えまたは削除。1 行に複数の参照がある箇所があるため行数は箇所数より少ない）。
> 引く側はいずれも**結論を自分の本文に持っていた**ので、括弧の中の出典を差し替えるだけで済んだ。
> これは OKF の `Background` が定める条件（「引く側が結論を自分の本文に書いていること」）
> がもともと満たされていたということである。
