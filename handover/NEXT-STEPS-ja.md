---
type: Open Items
title: 実開発ステップ別の欠落一覧
description: 次期の実開発で埋めるべき 14 件を、どのステップで埋めるかまで割り当てた一覧。
tags: [open-items]
phase: packaging
status: stable
---
# 実開発ステップ別の欠落一覧

- 日付: 2026-08-02
- 目的: **次期の実開発で「まだ決まっていないこと」の全数と、それを埋めるステップ。**
- 対象の順序は次期の実開発の順である:

```
1 要望ヒアリング → 2 UI モック作成 → 3 アーキ設計 → 4 データモデル設計
                 → 5 JSON/MSPDI 入出力設計 → 6 UI×データ複合動作
```

## この文書の読み方

**各行の「決まっていること」列は handover の一次資料に実際に当たって確かめた実測である。**
ここを読まずに着手すると、**既に決まっているものを決め直す**ことになる。それが前プロジェクトで
最も多く時間を溶かした失敗であった。

- **決まっていること** … 所在を書く。**本文はここに複製しない**
  （`DISCARDED-ja.md` の原則 4「**『正』が 2 つになるものは片方を破棄する**」）。
- **決めること** … これが残作業である。

`OPEN-ITEMS-ja.md` とは別物である。あちらは **MS Project の実機を触らないと分からない 3 件**であり、
本書は**次期が自分で決めれば済む 14 件**である。

---

## ステップ 1 — 要望ヒアリング（0 件）

### 1-1. ~~対象環境の定義~~ → **決着（2026-08-02）**

**5 件すべて決まった。性能ゲートは機能する状態になった。**

| 決めたこと | 正はどこか |
|---|---|
| ① 対応ブラウザ ＝ **Chromium 系（Chrome / Edge）基準・Firefox は動作確認・Safari 対象外** | `user-order.md`「対象環境」 |
| ② 対応 OS ＝ **問わない**（単一 HTML なので OS 差は実質フォントだけ） | 同上 |
| ③ タッチ・モバイル ＝ **対象外**。**WCAG 2.5.5 は AA 判定に数えない**（既存の扱いの追認）。掴み代 6px / フェード掴み点 15×15px はそのまま | 同上 ／ `05-security-a11y/a11y-wcag21-aa-checklist.md` |
| ④ 性能測定の基準マシンと基準ブラウザ ＝ **機種・CPU・GPU・メモリ・ブラウザ版を実測して記録済み。基準 GPU は内蔵（Intel UHD）** | `04-performance/handover-performance-notes-ja.md` **§2-0** |
| ⑤ CSP の `img-src` ＝ **`data:`**（現行の推奨 CSP から変更なし） | `05-security-a11y/security-design.md` §4 |

> ⚠️ **基準機の GPU は 2 つある。測る前にブラウザを内蔵 GPU へ固定すること**（手順は §2-0）。
> 固定しないと OS が場面で切り替えて数字が揺れる。

### 1-2. ~~WCAG 2.1 AA の適用範囲~~ → **決着（2026-08-02）**

**A / AA 全 50 基準の突合が済んだ。** 正は `05-security-a11y/a11y-wcag21-aa-checklist.md`。

一次資料は W3C 勧告の原文（**W3C Recommendation 2025-05-06**）。
**基準の番号・名称・レベルは原文から機械抽出**しており、記憶や要約で書いていない。
入手手順とハッシュは `docs/spec/vendor/wcag/README.md`（**原文は git 管理外**。MSPDI の XSD と同じ扱い）。

**適用範囲も決まった** — ⚠️ **「WCAG 2.1 AA 適合」とは名乗らない。**
**AA を指針とし、視覚と操作に絞る。読み上げは対象外。**
要求としての記述は `user-order.md`「アクセシビリティの適合範囲」。

```text
WCAG 2.1 の達成基準  全 78（A 30 / AA 20 / AAA 28）
AA 適合に要るのは A + AA = 50

  適合      1   原文が「常に満たされる」と定めている（4.1.1）
  該当なし  14  この製品には当たらない（理由つき）
  範囲外    5   支援技術に構造を渡すためのもの（1.1.1 / 1.3.1 / 1.3.2 / 4.1.2 / 4.1.3）
  方針あり  11  満たし方が handover にある。実装はまだ無い
  要対応    19  満たし方が書かれていない ← 残作業
```

- **なぜ名乗らないか**: 条文に**部分適合が無い**（5.2.1「met in full」）。1 つ外せば適合とは言えない。
- ⚠️ **レベルを A に下げる案は採らなかった。逆効果である** — 読み上げの核は **Level A** に集中し
  （1.1.1 / 1.3.1 / 1.3.2 / 2.4.3 / 4.1.2）、動機そのものは **AA** にある
  （1.4.3 / 1.4.11 / 2.4.7 / 1.4.4 / 3.1.2）。**レベルという刃では分けられない。**
- **2.5.5 Target Size は AAA なので数えない**（1-1 ③ でタッチを対象外にしたので確定）。
- **`1.3.1` だけは別枠で残した** — 適合範囲の外だが、**「画面とデータを分離した」が本物かを外から測る指標**として。
  骨格段階で試す（同書「1.3.1 だけは別枠で残す」）。

> ⚠️ **範囲外にしても件数はあまり減らない（21 → 19）。減ったのは重量である。**
> 5 件が「SVG とは別に支援技術向けの出力経路を作る」という最大の設計負担を丸ごと持っていた。
>
> ⚠️ **要対応 19 件は次期の実装作業であって、handover の欠落ではない。**
> **何を満たす必要があるかは全数そろった。**

---

## ステップ 2 — UI モック作成（7 件）

### 2-1. 視覚モック

| | |
|---|---|
| **決まっていること** | 画面の 4 部構成・上部ボタンの並び・パレットの内訳・掴み領域・操作割当 ＝ `03-ui-naming/handover-ui-detail-spec-ja.md` §1〜§5。UI パーツ名と責務 ＝ `03-ui-naming/handover-ui-parts-ja.md`（**命名の正**） |
| **決めること** | **絵が 1 枚も無い。** PoC（`08-poc/poc-integrated.html`）にあるのはキャンバス描画・形状の基準タブ・読取専用の Properties Panel だけで、**`Command Palette` / `Watermark` / `Comment Boxes` / `Highlight Boxes` は 6 本すべてに 0 件**である |

> 前プロジェクトの視覚モックは「**視覚的な目標としては有効**だが、コピペ誘因になる」として
> 意図的に外した（`DISCARDED-ja.md`）。**次期は確定名で描き直す。**

### 2-2. アイコンの図形定義

| | |
|---|---|
| **決まっていること** | 作画規則 ＝「**全て線画**。20×20 のデザイングリッドで作画し、太さを揃える」（`03-ui-naming/handover-ui-detail-spec-ja.md` §3）。アイコンが要る箇所の列挙も同 §2・§3 にある — **App Header（現行 11）**／パレット: タスク形状 4・マイルストーン 8・機能コマンド 7・**カーソル 6**（本日線 ＋ デュアル ＋ ガイド 4 モード）・予実トグル 2 |
| **決めること** | ① **図形そのものが 0 件。** `03-ui-naming/handover-ui-detail-spec-ja.md` §3 は「前プロジェクトのアイコン定義が参考になる」と書くが、**それは handover に入っていない**（形状の意図だけを引き継ぐ、という但し書きの側だけが残った） ② **必要数が確定しない。** 同 §2 の確定（2026-07-26）は App Header を**文書に対する操作 8 個**（`Fit` / `Load` / `Save` / `Base` / `Undo` / `Redo` / `AI` / `?`）と定義しており、**現行 11 個のうち `Cmd` / `SS` / `Theme` の行き先が書かれていない**。モックで確定させる |

> タスク形状（4 種）は**アイコンではなく描画対象**であり、作図の幾何まで確定している
> （`chevronNotchOfHeight` / `arrowHeadOfStroke` / `spanDotOfStroke` ほか ＝ `02-data-model/grs-document-settings-ja.md` §3）。
> **マイルストーン（8 種）は形状の集合と並び順だけが確定**（面積の大きい順 ＝
> `07-plan-actual/handover-plan-actual-decisions-ja.md` §2-2-2）。
> **六角形の向き・五角形の回転といった作図の幾何は未定**なので、ここはモックで決める。

### 2-3. i18n の表示文字列カタログ

| | |
|---|---|
| **決まっていること** | 対象言語は **en / ja の 2 つだけ**（それ以上に広げない）、項目名は `language`、値は `ja` / `en`（`user-order.md` 項 21）。**i18n キーの記法は snake_case**（`03-ui-naming/handover-ui-parts-ja.md` §1-2 ＝ **記法の正**）。**翻訳の対象はメニューとパネルの文字だけ**で、タスク名・行名は**データなので対象外**（`02-data-model/grs-document-settings-ja.md` §5-2）。UI パーツ名・プロパティ名の**英日対訳は 57 行ある**（`03-ui-naming/handover-ui-parts-ja.md` §2-1-1〜§2-1-4。機械カウント） |
| **決めること** | **キー付きの表示文字列カタログ（ボタン文言・ツールチップ・通知メッセージ）が 0 件。** 前プロジェクトの ja/en 対訳は「**パーツ名が確定名に変わったので使えない**」として破棄済み（`DISCARDED-ja.md`） |

> ⚠️ **`language` を `documentSettings` に入れてはならない。** 「どの言語で開くかは**読む人の環境**」であり、
> **文書には保存しない**（`user-order.md` 項 21）。置き場所は **`localStorage`**
> （`02-data-model/grs-document-settings-ja.md` §5-2 ＝ **設定値の正**）。
> 保存すると「日本語で作った文書を英語の利用者が開いたときにメニューまで日本語になる」。
> `localStorage` が読めないときは**ブラウザの言語設定に従う**（同 §5-2）。

### 2-4. Properties Panel の UI 設計

| | |
|---|---|
| **決まっていること** | **表示項目は全数確定している** — `user-order.md` 項 20 の表。項目名は**英語固定**（同 項 22）。MSPDI 対応は `02-data-model/handover-property-mspdi-mapping-ja.md`。パネル幅は `propertyPanelWidth` として文書に保存する（`02-data-model/grs-document-settings-ja.md` §4-2）。**PoC に読取専用の実装がある**（`08-poc/poc-integrated.html`） |
| **決めること** | 項目のグループ分け／入力ウィジェットの種別／編集可否／バリデーションの見せ方／**フォーカス順**。`03-ui-naming/handover-ui-parts-ja.md` の UI パーツ木で、**`Properties Panel` だけ子ノードが 1 行も無い** |

> ⚠️ **ここは a11y の適合根拠を預かっている。** `07-plan-actual/handover-plan-actual-decisions-ja.md` §7 は、
> ドラッグ操作の **WCAG 2.1.1（キーボード操作可能）適合を「Properties Panel で日付を入力できること」に預けて**おり、
> その到達性は「実装時に確認すること」のまま検証されていない。**このパネルの設計は a11y 要求である。**

### 2-5. 通知（Toast）の設計

| | |
|---|---|
| **決まっていること** | 支援技術への伝え方だけ ＝ `aria-live="polite"` / `role="status"`（`05-security-a11y/a11y-wcag21-aa-checklist.md`） |
| **決めること** | **`Toast` が UI パーツ木に無い**（`03-ui-naming/handover-ui-parts-ja.md` にヒット 0）。表示位置・滞留時間・多重時の積み方・i18n が未定 |

**通知の発生源は既に 8 系統ある。** 設計はこの 8 つを収容できること:

| # | 何を知らせるか | 所在 |
|:--:|---|---|
| 1 | 取込で「届かなかった」N 件 | `02-data-model/grs-native-erd-ja.md` |
| 2 | 連鎖削除の件数 | 同上 |
| 3 | 工数（`Work` 系）を温存したこと | `02-data-model/handover-property-mspdi-mapping-ja.md` / `02-data-model/handover-data-model-entry-ja.md` |
| 4 | Carry の予約枠が満杯でフォールバックしたこと | `02-data-model/handover-property-mspdi-mapping-ja.md` |
| 5 | `stackSafetyCap` に到達したこと（`user-order.md` 項 30-7） | `02-data-model/grs-document-settings-ja.md` ほか 2 か所 |
| 6 | 自動保存の成否 | `05-security-a11y/a11y-wcag21-aa-checklist.md` / `03-ui-naming/handover-ui-parts-ja.md` |
| 7 | 取込バリデーションの拒否・警告 | `05-security-a11y/security-design.md` §3 |
| 8 | localStorage 破損で復元できないこと | 同 §5 |

### 2-6. 初期テンプレートの中身

| | |
|---|---|
| **決まっていること** | 「初期表示用のテンプレートを 1 つ提供すること。**新規に作る**こと」＝ `user-order.md` 項 63。旧テンプレートを使わない理由（廃止済みの予実別行モデル・行 id に制御文字）＝ `DISCARDED-ja.md` |
| **決めること** | **中身が 0 件。** 何行・何タスク・どの設定値を持つかが未指定 |

> ✅ **前提だった 4-1（既定値未定）は 2026-08-02 に決着した。** `documentSettings` は
> **常に全項目を書き出す**規約なので（`02-data-model/grs-document-settings-ja.md` §2）、
> 既定値が決まるまでテンプレートの JSON は完成しなかった。**この依存は外れている。**

### 2-7. `fontScale` の S / M / L が何 px か

| | |
|---|---|
| **決まっていること** | `fontScale` は `'S'` / `'M'` / `'L'` の 3 値で**文書に保存する**、既定は `'M'`（`02-data-model/grs-document-settings-ja.md` §4-1）。**読む人は変更できること**、変更したら文書の編集になる（同 §4-2）。和文の可読下限は **12px**（`07-plan-actual/handover-plan-actual-decisions-ja.md` §2-4-1） |
| **決めること** | **3 値がそれぞれ何 px かが、どこにも書かれていない。** 保存する設定なのに効果が定義されていない |

> ⚠️ **目盛のしきい値がこれに依存している。** 2026-08-02 に決めた `1.4 / 4.3 / 30` は
> **「目盛のフォントが 12px のとき」の解**である（`02-data-model/grs-document-settings-ja.md` §4-4）。
> `L` がこれより大きいと**段階 2（年＋月）と段階 3（＋週）のラベルが入らなくなる** —
> **間引けるのは日の段だけ**で、年月・週の段は「間引かない」と決まっている
> （`03-ui-naming/handover-ui-detail-spec-ja.md` §6-3）。
> **px を決めたら、しきい値 3 本が最大のフォントでも成り立つかを検算すること。**

---

## ステップ 3 — アーキ設計（5 件）

**状況と所在は `09-architecture/handover-architecture-entry-ja.md` にまとめてある。ここには複製しない。**
本表は「**何を決めるか**」だけを持つ。

| # | 決めること | 状況 | 詳細 |
|:--:|---|---|---|
| **3-1** | **描画方式の追認** | handover 内の裏付けは**推奨 1 件と PoC の前提 1 行だけ** | `09-architecture/handover-architecture-entry-ja.md` §1 |
| **3-2** | **状態管理／不変更新ストアの設計** | **既存の決定 2 件がこれを前提にしている**のに設計が無い | 同 §2 |
| **3-3** | **モジュール／ディレクトリ構成** | **意図的な空白** | 同 §4 |
| **3-4** | **技術スタック** | **意図的な空白** | 同 §4 |
| **3-5** | **localStorage のキー設計とスキーマ移行手順** | プレフィックスのみ確定 | 同 §3 |

---

## ステップ 4 — データモデル設計（0 件）

### 4-1. ~~既定値が決まっていない 16 項目~~ → **決着（2026-08-02）**

**16 キーすべてに既定値が入り、`documentSettings` に空欄は残っていない。**
正は `02-data-model/grs-document-settings-ja.md` §4-1 / §4-2 / §4-4。**ここには複製しない。**

決着のしかたは 2 通りに分かれた。

| | |
|---|---|
| **値を入れた（15 キー）** | 根拠を各表の直後に併記した。うち **🔎 3 つ**（`themePreference` / `rowTitlePanelWidth` / `propertyPanelWidth`）は**由来が無い**ので次期が選び直してよい |
| **キーを廃止した（1 キー）** | **`progressLineColor`**。イナズマ線の色を文書に保存すると `themeHue` を変えたときにこの線だけ取り残される。**依存線と同じ固定色**にした（規則は `07-plan-actual/handover-plan-actual-decisions-ja.md` §2-6） |

**あわせて目盛のしきい値 3 本を決め直した** — `1 / 4 / 14` → **`1.4 / 4.3 / 30`**。
旧値は §6-3 の目安を**3 つとも割っていた**（`02-data-model/grs-document-settings-ja.md` §4-4 に導出と検算）。

> ⚠️ **§3 の 🔎 印 41 個は別物である。** あちらは「**既定値の由来が記録されていない**」＝ 値はある。
> 本項は「**値そのものが無かった**」であり、こちらは埋まった。

> ⚠️ **`02-data-model/handover-data-model-entry-ja.md` の JSON 実例は、いまも既定値ではない。**
> `"importSeq": 1`（1 回取り込んだ後）や `"scrollDate"` / `"scrollGroupId"` の実値は
> **インスタンスの値**である。**既定は `0` / `null` / `null`。**
> 下の 5-1 が同じ JSON を「完全な実例」として読ませているので、**取り違えに注意。**

---

## ステップ 5 — JSON / MSPDI 入出力設計（2 件）

### 5-1. JSON Schema

| | |
|---|---|
| **決まっていること** | **完全な文書インスタンスの実例がある**（`02-data-model/handover-data-model-entry-ja.md` の JSON。`schemaVersion` / `project` / `documentSettings` / `tasks` を含む）。データ構造は `02-data-model/grs-native-erd-ja.md`（**データの正**） |
| **決めること** | **機械可読なスキーマ（型・必須・値域）が 0 件。** `user-order.md` 項 57 が JSON を主データ形式とし、項 62 が**取込を信頼できない入力として厳格に検証すること**を要求しているので、**スキーマが無いと項 62 が実装できない** |

> 旧 `gr-scheduler.schema.json` は**意図的に捨てた** — 予定日付が `startDate` / `endDate` で、
> 確定名 `start` / `finish` と食い違うため（`DISCARDED-ja.md`）。**流用しないこと。**

### 5-2. 完全な MSPDI XML 実例

| | |
|---|---|
| **決まっていること** | **スキーマ側の事実は全数そろった（2026-08-02 に XSD から機械抽出）** ＝ `01-mspdi/mspdi-core-tree.md`「XSD の要素順と最小妥当文書」節。名前空間 URI ／ **`elementFormDefault="qualified"`** ／ トップレベル要素は `Project` 1 つ ／ **4 つの容れ物の宣言順の全数**（`Project` 70 / `Task` 96 / `Resource` 71 / `Assignment` 265）／ 必須は全体で 3 つだけ（`SaveVersion`・`CurrencyCode`・各 `UID`）／ **最小妥当文書の実例**。順序と空容れ物の罠は `01-mspdi/mspdi-pitfalls-ja.md` **B-5 / B-6**。文字コードは UTF-8・BOM なし（`user-order.md`） |
| **決めること** | **往復に使う代表文書 1 本だけが残る。** GRS の取捨選択（Own / Consume / Reconstruct / Carry / Drop ＝ `02-data-model/grs-mspdi-field-ledger-ja.md`）を一通り踏む MSPDI 文書を作り、import → export で失われないことを検査する |

> **これはテストの実データである。** 実装の隣に置いて往復検査から参照するものなので、
> **次期のリポジトリで作る**（handover に置くとコードと乖離する）。
>
> ⚠️ **正本 XSD はリポジトリに入っていない**（ライセンスのため）。入手方法とハッシュは
> `01-mspdi/mspdi/README.md`。**上の事実は入手済みの複製から抽出したもの**なので、
> 次期は XSD を取得しなくてもこの順序表を使える。**新たに事実を足すときだけ取得が要る。**

---

## ステップ 6 — UI×データ複合動作（0 件）

### 6-1. ~~LOD のレベル減の量の式~~ → **決着（2026-08-02）**

**式は立てない。深さ 5〜1 を順に試し、実際の行数からフォントが下限を上回る最初の深さを採る**
（`07-plan-actual/handover-plan-actual-decisions-ja.md` §2-4-1）。
軸B の深さは高々 5 段なので試す範囲が 5 通りしかなく、**推定が 1 つも入らない。**

### 6-2. ~~依存線の経路~~ → **決着（2026-08-02）**

**未決は残っていない。全 6 件が `03-ui-naming/handover-ui-detail-spec-ja.md` §4-9 で決着した。**

- **横切りは許容し、重ね順で解く** — 依存線は**バーより前面・名称ラベルより背面**（同 §4-3）。
  行をまたいでも段が離れても、避けようとしない（タスクの x は日付で決まるので避け先が無い場面がある）。
- **アンカーは右辺の中央・左辺の中央の 2 種類のまま。** 依存の種別（`link_type`）が組合せを選ぶだけで、
  SF / SS は x を反転して同じ規則に載る。**核機能**（`user-order.md` 項 49）はこれで満たされる。
- **経路は 8 パターン・形は 3 種類**（水平 1 本 / 2 折れ / 通路経由の 4 折れ）。折れ点の上限は 0〜4。
- **新しい設定値キーは 1 つも増えていない。**

> ⚠️ **絵で確かめられていないものが残る。** PoC は FS の 5 例しか描いておらず、`Frozen Record` なので
> 追随させない。**SF（①〜⑤ の鏡像）と FF / SS（⑥ ⑦ ⑧）はステップ 2 の 2-1（視覚モック）で最初に描くこと。**

### 6-3. ~~Fit の仕様~~ → **決着（2026-08-02）**

**算出規則は `03-ui-naming/handover-ui-detail-spec-ja.md` §6-7 が正。ここには複製しない。**

- **`Fit` は「全体が収まる」ことを保証しない。** 保証するのは「軸A と軸B の深さを試したうえで、
  **収まる最大のズームに着地する**」ことだけである。収まらない軸はスクロールを残す
  （縦は上から下、横は左から右。**中央寄せはデータ形式が表現できない**）。
- **LOD で消えたタスクのために場所を空けない。** 画面は資源であり、`Fit` はその資源に
  優先度の高い情報を詰めるための機能である。
- **縦と横は性質が違う** — 縦は畳めるが、**横は畳めない**（日付の幅は文書の事実）。
- **収まらないケースの全数**（縦 4 件 / 横 3 件）も同節にある。
  ⚠️ **目標規模の 50 行は、全部 1 段でも縦に入らない**（1 行 1 段の最小占有が約 30px、入るのは約 32 行）。
  最優先事項 3 は**軸B で畳んで初めて成り立つ**。

**あわせて 2 件直した。**

| | |
|---|---|
| `rulerHeight` | 既定 **34 → 48px**、下限 `rulerFont + 6` → **`rulerFont x 3 + 6`**。旧値は**段階 4 の 3 段を収容できなかった**。**帯の高さは目盛の段階によらず固定**（ユーザー判断）とし、`zoomX` → 段階 → 高さ → `zoomY` の循環を消した |
| §4-2 の 9 キー | **下限・上限の列そのものが無かった**ので新設。パネル幅が画面を食い尽くすと `Fit` が定義できない。取込の検証（項 62）も範囲が無ければできない |

> **PoC が挙げた「Fit で最下行が半分切れる」（`08-poc/POC-RESULTS-ja.md` §7）は不具合ではなかった。**
> 案 C が**軸B の深さの試行をしていなかった**ためで、`Fit` 自体の欠陥ではない。
> 深さを試しても入らない文書は実在するので、そのときは**縦スクロールが残る**のが正しい振る舞いである。

---

## 数

| ステップ | 件数 |
|---|:--:|
| 1 要望ヒアリング | **0** |
| 2 UI モック作成 | **7** |
| 3 アーキ設計 | 5 |
| 4 データモデル設計 | **0** |
| 5 JSON/MSPDI 入出力設計 | 2 |
| 6 UI×データ複合動作 | **0** |
| **合計** | **14** |

> **2026-08-02 に 18 → 17 になった。** ステップ 6 の 6-2（依存線の経路）が決着したため。
