---
type: Working Note
title: 改訂 → 反映先 台帳
description: "改訂の正と反映先の全数。マージが終わったら to-del/ へ移す。"
tags: [working]
phase: packaging
status: draft
---
# 改訂 → 反映先 台帳（作業用）

- 日付: 2026-08-01
- **この文書は作業用である。反映が終わったら各文書へマージし、本書は `to-del/` へ移す。**
- 目的: 2026-07-30 / 07-31 / 08-01 の確定について、**正がどこにあり、どの文書が追随すべきで、
  追随できているか**を全数で押さえる。
- 検証: 状態列はすべて `handover/` 全 `.md` に対する実測（grep / 本文照合）である。推測は入れていない。

## 状態の印

| 印 | 意味 |
|:--:|---|
| ✅ | 反映済。実測で確認した |
| ❌ | **未反映。修正が要る** |
| ➖ | 反映先なし。正の文書だけで完結する |
| ☑ | 本セッションの A 群で反映済 |

---

## 0. 既にある表と、無い穴

**「改訂 → 反映先」表は 2 本存在する。**

| 表 | 覆う範囲 |
|---|---|
| `07-plan-actual/handover-plan-actual-decisions-ja.md` §11 | 2026-07-29 / 07-30 の**予実まわり**の確定 |
| `02-data-model/grs-document-settings-ja.md` §9 | 2026-07-31 の**設定値まわり**の確定 |

**穴は 3 つある。**

1. 2026-07-30 のうち、**予実以外**の確定（`ui-detail §4-9` 依存線の経路）に表が無い
2. 2026-07-31 のうち、**設定値以外**の確定（`ui-parts` の改名・`ui-detail` の下限と走り・`07 §2-7`）に表が無い
3. **2026-08-01 は全件、表が無い**

> CLEANUP-PLAN §0 は「07-31 に同等の表が無い」と書いているが、
> `settings §9` が存在するため**部分的に誤り**である。実際の穴は上の 3 つ。

---

## 1. 2026-07-30 の補遺 — `07 §11` が拾っていない確定

| # | 確定内容 | 正 | 反映先 | 状態 |
|:--:|---|---|---|:--:|
| 30-a | **依存線の経路は 2 点（右辺中央 → 左辺中央）と 5 パターン 1 規則。候補列挙も採点も探索も要らない** | `ui-detail §4-9` | `04-performance/handover-performance-notes-ja.md` §4-1 が**候補列挙＋辞書式採点のまま**。撤回告知が §4-1 末尾 1 行に埋没し、`user-order` 項 48 が**この旧方式を名指しで読めと指している** | ❌ |
| 30-b | **WBS の深さの上限は「人がインデントで作るとき」だけ。import は上限なし、export はクランプしない** | `ui-detail §6-1-1` | `grs-native-erd-ja.md` §5.5e ／ `grs-data-model-ja.md` §4.6・§8 #18 ／ `grs-mspdi-field-ledger-ja.md` §B ／ `stale-spec-audit` ／ `NEUTRALIZED-TERMS` の計 **17 箇所**が「5 段にクランプ」のまま残っていた。`ledger` は冒頭表・§7.4 と §B で自ファイル内が割れていた | ☑ |

---

## 2. 2026-07-31 — 11 件

| # | 確定内容 | 正 | 反映先 | 状態 |
|:--:|---|---|---|:--:|
| 31-1 | `Bar` は総称。形状を問わず、マイルストーンも `Bar` | `ui-parts §2-0` | 語の使い方。機械的な反映先を持たない | ➖ |
| 31-2 | `shapeKind` の値 `'bar'` → **`'rectangle'`** | `ui-parts §2-1-2` | 全文書 | ✅ 旧値の残存 **0**（改名表を除く） |
| 31-3 | 略語キーを語幹一致へ展開（`depWidth`→`dependencyWidth` ほか **20 キー超**） | `ui-parts` 改名表 | `settings §3` / `ui-detail §4-10` / PoC | ✅ 旧名の残存 **0**（改名表を除く） |
| 31-4 | 文字色はテーマの前景色 1 つ。バーに載る文字に背景色の縁取り（フォント × 0.17） | `07 §2-7` | `ui-detail §6-1-3` ✅ ／ `settings` の `labelHaloOfFont` ✅ ／ `POC-RESULTS §D` ✅ | ✅ |
| 31-5 | `documentSettings` は**常に全項目を書き出す** | `settings §2` | `erd §5.7-1` ✅ ／ **`handover-data-model-entry-ja.md` §2 の JSON 実例が 16 キーのみ** | ❌ |
| 31-6 | **本日線は保存しない**（`todayLineVisible` を廃止） | `settings §7` | `erd:1021` ✅ ／ **`entry:90` の JSON 実例に `"todayLineVisible": true` が残存** | ❌ |
| 31-7 | テーマ / ペイン幅 / ズーム / スクロールを「保存しない」から `documentSettings` へ移した | `settings §4-2` | `erd §5.7-1` ✅ ／ `grs-data-model:161` ✅ ／ **`entry` の注記が「テーマ・ズーム・スクロールは保存しない」と真逆** | ❌ |
| 31-8 | 縦の下限は**予定側 1 か所だけ**。実績は比率で追随しクランプを掛けない | `ui-detail §4-10-3` | `settings §6` ✅ | ✅ |
| 31-9 | 依存線の走り = 出口は入口より三角形ぶん短い。**倍率 1 つから両方を導く** | `ui-detail §4-9` | `settings` の `dependencyRunOfArrow`（下限 1 + ε の理由まで一致）✅ | ✅ |
| 31-10 | 自動保存は `requestIdleCallback` ＋ `timeout` **5000ms** | `settings §8-1` | — | ➖ |
| 31-11 | JSON 往復の検査 **6 件** | `settings §8-2` | **`handover-property-mspdi-mapping-ja.md` §4-2 C-8 に全文が二重に存在し、どちらが正か書かれていない** | ❌ |

---

## 3. 2026-08-01 — 18 件

| # | 確定内容 | 正 | 反映先 | 状態 |
|:--:|---|---|---|:--:|
| 01-1 | **マイルストーンの実績は実績日の位置へ横にずらす**（縦は ◇ 1 つぶん） | `07 §2-2` | 16 箇所 | ☑ |
| 01-2 | 縮める比率は **`actualOfPlan` 1 つだけ**。全タスク形状に同じ比率 | `07 §2-2-1` | `settings §3` ✅ ／ `ui-detail §4-10-3` ✅ | ✅ |
| 01-3 | 矢羽根の切り欠きの角度も予実で同じ（横長に `actualOfPlan` を掛ける） | `07 §2-2-1` | 設定値は増えない。描画規則のみ | ➖ |
| 01-4 | **`(!)` は遅れ**。判定は 3 条件 | `07 §2-4` | 2 箇所 | ☑ |
| 01-5 | **イナズマ線は途切れさせない**。頂点のない段は基準日の位置を通る | `07 §4-2` | `ui-detail:1149` ✅ ／ **`user-order` 項 53 が「1 行に複数の Task があるときは最も遅れた頂点が勝つ」のまま。「段ごと」「途切れ」の語が 0 件** | ❌ |
| 01-6 | **`themeHue` / `themeMonochrome` を文書に保存する**（色のテーマは作者の決めごと） | `settings §4-2` | `erd:1009-1010` ✅ ／ `ui-parts:342-343` ✅ ／ **`entry` の JSON 実例に両キーが無い** | ❌ |
| 01-7 | 時間軸のしきい値は**境目の数だけキーを置く**（`rulerTierPxPerDayMonth` / `Week` / `Day` の 3 本） | `settings §4-4` | `ui-parts:322-324` ✅ ／ `ui-detail §6-3` ✅ ／ **`user-order` 項 33 が `1` と `8` の 2 しきい値のまま** | ❌ |
| 01-8 | 目盛は **4 段階**（年 / 年＋月 / 年＋月＋**週** / 年＋月＋日＋曜日）。週は月曜日の日付 | `ui-detail §6-3` | `settings §4-4` ✅ ／ `ui-parts §2-1-6` ✅ ／ **`user-order` 項 33 が 3 段のまま**（01-7 と同じ 1 箇所） | ❌ |
| 01-9 | 何が横／縦にスクロールするか（行見出しは横に流れない・目盛は縦に流れない） | `ui-detail §1` | `ui-parts` の UI パーツ木 ✅ | ✅ |
| 01-10 | `Grid Lines` は**独立したトグル 2 つ**（4 択の循環にしない） | `ui-detail §1` | `settings §4-1` の `gridDateLinesVisible` / `gridGroupLinesVisible` ✅ ／ `ui-detail §3` のパレット ✅ | ✅ |
| 01-11 | **`Esc` は閉じるものがあるときだけ受け取る**。無ければ `preventDefault()` を呼ばない | `ui-detail §5-1` | `user-order` に `Esc` の項が無い | ➖ |
| 01-12 | **修飾キーが付いたら必ずブラウザの既定動作を止める** | `ui-detail §5-2` | `perf` E-12（`{ passive: false }` ＋常に `preventDefault()`）✅ | ✅ |
| 01-13 | **パンは等倍**。倍率を掛けない | `ui-detail §5-2` | — | ➖ |
| 01-14 | **背景の上の素の左ドラッグはパン**（アイテムの上では違う） | `ui-detail §5-2` | **`user-order` 項 37 が「素の左ドラッグでパンしてはならない」のまま** | ❌ |
| 01-15 | **スクロールバーは横・縦とも常時表示**。幅は環境の既定の半分 | `ui-detail §5-2` | `ui-parts:414` ✅ | ✅ |
| 01-16 | ショートカットのヒントは **`Scrollbars` の帯で 0.5 秒静止**（旧「キャンバスで 5 秒」から改訂） | `ui-detail §5-5` | — | ➖ |
| 01-17 | `ui-detail §4-5` の全文複製を **07 への参照に置き換えた** | `ui-detail §4-5` | **同じ処置が §4-0 / §4-1 / §4-2 / §4-7 / §6-1-2 / §6-6 に未適用**（予実の表が 4 系統に複製されたまま） | ❌ |
| 01-18 | PoC を **6 本へ再統合**。値は `poc-integrated.html` へ焼き込み、共有 CSS/JS を廃止 | `08-poc/README` | `handover/README` §2 の木 ✅ ／ **`POC-RESULTS-ja.md` が既に無いページ名で呼び続け、自ら冒頭で警告している** | ❌ |

---

## 4. 未反映の全数 — マージ作業リスト

**11 行。ただし修正箇所は 8 か所**（`entry` の 3 行は同じ JSON 実例と注記で閉じ、`user-order` 項 33 の 2 行は 1 か所）。

| 修正先 | 該当 # | 直すこと |
|---|---|---|
| `02-data-model/handover-data-model-entry-ja.md` | 31-5 / 31-6 / 01-6 | JSON 実例の `documentSettings` を**全項目**にする。`todayLineVisible` を除く。`themeHue` / `themeMonochrome` ほか §4-2 の 8 キーを足す。「保存しない」の注記を現行に合わせる |
| `user-order.md` 項 33 | 01-7 / 01-8 | 目盛を 4 段階・しきい値 3 本（`1` / `4` / `14`）へ |
| `user-order.md` 項 37 | 01-14 | 「素の左ドラッグでパンしてはならない」→ **背景の上ではパンする** |
| `user-order.md` 項 53 | 01-5 | 頂点は**行ごとではなく段ごと**。**途切れさせない**（頂点のない段は基準日を通る） |
| `04-performance/handover-performance-notes-ja.md` §4-1 | 30-a | 依存線を `ui-detail §4-9` の 2 点・5 パターンへ差し替える。`user-order` 項 48 の参照先も直す |
| `02-data-model/handover-property-mspdi-mapping-ja.md` §4-2 C-8 | 31-11 | JSON 往復検査の全文複製を `settings §8-2` への参照に置き換える |
| `03-ui-naming/handover-ui-detail-spec-ja.md` §4-0 / §4-1 / §4-2 / §4-7 / §6-1-2 / §6-6 | 01-17 | §4-5 と同じ処置（全文複製 → 07 への参照）を適用する |
| `08-poc/POC-RESULTS-ja.md` | 01-18 | 現存しないページ名を現在の 6 本へ。`POC-SPEC` と同じ「現在の正」対照表を持たせる |

---

## 5. 次にこれを繰り返さないために

**改訂した日に、その改訂の「正」と「反映先」を書く。** 表が 2 本しか無かったのは、
表を書く運用が**予実と設定値の担当のときだけ**動いたためである。

**マージ後、本書の内容は各正の文書へ入る。** 状態列（✅ / ❌）は作業の痕跡なので**持ち込まない**。
次の担当者が要るのは「今どうなっているか」だけで、「いつ何から変わったか」は不要である。
