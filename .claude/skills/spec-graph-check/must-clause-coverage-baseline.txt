952
#
# RAISED 950 -> 952 ON 2026-09-06, the same debt as the line below, grown.
# The body that implemented CR-363 found two more sentences of FR-098 that
# the ruling overturned and the CR had not edited: the list of reasons a
# pinned row may go undrawn (it said those reasons were the ONLY ones), and
# the MUST NOT against the band filling the Row Area -- which the ruling's
# own wording presupposes breaking. Both are now written as the ruling has
# them, and nothing holds the new wording yet.
#
# LOWER THIS when the spec-only body writing FR-098's cases returns. It was
# already running when this was raised.
#
#
# RAISED 949 -> 950 ON 2026-09-06, and it is a debt, not a result.
# CR-363 took the user's ruling on the pinned band: it REMOVED one MUST (the
# band scrolls when it does not fit) and put two in its place -- what does not
# fit is not drawn (MUST), and the band may not be made scrollable (MUST NOT).
# Net one more clause, and nothing holds either yet.
#
# LOWER THIS BACK TO 949 when a body that reads only docs/spec has written
# them. A raise that outlives one round is a clause nobody ever tests.
#
#
# LOWERED 960 -> 949 ON 2026-09-06. A spec-only body wrote four files for
# rows that were built and measured but held by nothing: entering the Dual
# Cursor drops the arm, a standing explanation is the last rung of the Esc
# ladder, F2 opens the document name in place, and AM-7 refuses a malformed
# request instead of throwing. Eleven more clauses are held.
#
# Six of its cases were RED on purpose and are green now: they showed that
# the AM-7 guard read the request's two members and nothing inside them, so
# `commands: [null]` still threw. That is the shape of test this project
# wants -- the body was told not to soften an assertion and did not.
#
#
# LOWERED 965 -> 960 ON 2026-09-06, WHICH PAYS OFF THE RAISE BELOW AND ONE
# CLAUSE MORE. A spec-only body wrote the cases that press the four clauses the
# two rulings of 2026-09-06 added, and quoted each of them verbatim:
#
#   OP-10   tests/unit/t-024a-op-10-a-chosen-zoom-is-the-place.test.ts
#           (both clauses: the choice becomes the place, and the row is not
#            redone every frame)
#   FR-021  tests/unit/fr-021-the-outline-base-of-the-file-comes-back.test.ts
#   Ch 5.4  tests/contract/erd-seats-are-never-reissued.contract.test.ts
#
# ⭐ THE TARGET WAS 961 AND THE MEASUREMENT SAYS 960, so 960 is what is written
# here -- this file may fall freely and 「lower the baseline to hold the
# ground」 is what the check itself printed. The fifth clause is `NF-4` of
# table T-224 (「使われないまま残った席番号であり、別のものに割り当て直しては
# ならない（MUST NOT）」), quoted in the contract file's head comment because it
# is the reason that file refuses to assert the seats are contiguous. ⚠️ It is
# held by a QUOTATION GOVERNING A DELIBERATE NON-ASSERTION, not by an
# assertion -- which is all this check ever claims of any of its 494.
#
# ⛔ THE RUN IS RED ON PURPOSE IN TWO PLACES, and neither assertion was
# weakened to make it green:
#   - seven cases of the OP-10 file. Measured at commit c782071: one press of
#     SK-16 writes `zoomX: 1 -> 1.1` into the document and the frame draws the
#     same `pxPerDay` as before it, with `scrollDate` / `scrollGroupId` still
#     null. CR-359 wrote the sentences and left the implementation to a later
#     body ("裁定 1 の実装場所を仕様では指さない"), so the work is owed.
#   - one case of the ERD seats file. 表 T-057 prints thirty `RL-` rows while
#     the manuscript seats twenty-two; `RL-23` to `RL-30` (the carryElements
#     rows) are numbered from the end of the list, so one more relation
#     renumbers all eight. A-appendix 1.84 counts 22 relations seated.
#
#
# RAISED 961 -> 965 ON 2026-09-06, AND THE RAISE IS A DEBT, NOT A RESULT.
# Two rulings added four clauses and no test holds any of them yet:
#
#   OP-10   a person's zoom or view choice BECOMES the view position (MUST)
#   OP-10   the row may not be redone every frame (MUST NOT)
#   FR-021  the outline base of the file that came in is kept (MUST)
#   Ch 5.4  an ET-/AT-/RL-/DV- number, once given, is never reissued
#           (MUST NOT)
#
# The tests are owed to a body that reads only docs/spec/, which cannot be
# spawned until this work is a commit for it to branch from. LOWER THIS BACK
# TO 961 when those four are held. A raise that outlives one round is a
# clause nobody ever writes a test for.
#
#
# LOWERED 972 -> 961 ON 2026-09-05. A spec-only body wrote the cases that
# press the frame-time clauses this round added, and quoted them. Eleven more
# clauses are now held. The run is RED on purpose: one operation gets two
# opposite verdicts depending on which number is read, which is exactly what
# the new clauses forbid confusing.
#
# LOWERED 978 -> 972 ON 2026-09-05, after two spec-only bodies wrote the
# tests that hold this round rulings clauses. The check itself said to:
#   "976 not (was 978) -- lower the baseline to hold the ground".
# The rulings ADDED clauses (978 -> 1000 at the worst point); the ground was
# recovered by writing tests that press them and quote them, not by raising
# the number. Two of the new tests are RED on purpose -- a red test holds a
# clause just as well as a green one, and says the work is owed.
#
# ⭐⭐ LOWERED 1164 -> 978 ON 2026-09-05, THE SAME DAY IT WAS FIRST SET,
# because the first measurement counted two kinds of clause it should never
# have read. Nothing was written to earn this; the scope was wrong.
#
#   165  A-appendix.md, every one of them under A.3 Changelog. Those are
#        RECORDS -- past-tense sentences saying a MUST was placed somewhere.
#        A test cannot hold one, and asking for one asks a test to pin
#        history. Same distinction HF-14 was corrected for the same day.
#    45  Chapter 1 of 01-04-requirements.md. Those rule how the
#        SPECIFICATION is written, not what the product does, and checks 5
#        to 15 already read the manuscript for exactly them.
#
# ⛔ AND THE SCOPE CHANGE ITSELF SHIPPED A BUG THAT WAS CAUGHT BY READING THE
# NUMBER. The first attempt matched the chapter heading with a bare "# ",
# which also matches inside "## ", so the skipped span ran to the end of a
# file that carries one top-level heading -- 1465 clauses vanished and the
# check happily reported 146. A count that moves that far is not a result.
#
# ⭐ Coverage as it now stands: 447 held of 1425.
#
# ⛔ MUST / MUST NOT clauses in docs/spec/ (the nine manuscript files
# check-must-clause-coverage.py reads -- see its own docstring) with no
# verbatim tie to any file under tests/.
#
# ⭐ MEASURED 2026-09-05, THE DAY D-254 WAS FILED: 1,634 marked clauses total,
# 470 held (some part of the clause, at least 28 characters ending at the
# marker, quoted verbatim in some file under tests/), 1,164 not. This is the
# FIRST measurement -- nobody had counted this before, so 1,164 is simply
# today's true state, not a chosen number.
#
# ⭐ The number coming DOWN is the work (a round that writes a verbatim-
# holding test for a previously bare clause lowers it, and should lower this
# file to match). It going UP is a MUST or MUST NOT clause added, or a clause
# whose only tie broke, without a test taking its place.
#
# ⛔ RAISE THIS ONLY TO TAKE THE DEBT ON DELIBERATELY, and say why in the commit.
