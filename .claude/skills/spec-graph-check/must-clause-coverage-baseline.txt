1347
#
# LOWERED 1349 -> 1347 ON 2026-09-12, applying the user's rulings of that day,
# and neither clause got covered: both stopped existing. 05-07-design.md said
# 「`GRS JSON` には、どのエンティティも書き出す（`FR-024`）」 and 「辺のラベル
# には、その線が何を表すかだけを書く」; the first named the wrong seat (table
# T-052's DR-2 is what rules it) and became a pointer, and the second was
# measured false -- 3 of the 30 labels carry a rule's pointer -- and was
# loosened to match what the figure actually does.
# ⚠️ THE HEADROOM IS WHY THIS IS WORTH WRITING DOWN. The round measured 1346
# against a baseline of 1349 before it started: three. A body adding four MUST
# clauses to the specification would have gone red without touching a test,
# and the brief it was given warned only about check 42. ⇒ Read BOTH numbers
# on entry when a round writes new rules.
# ⛔ WHEN A ROUND LEGITIMATELY ADDS A MUST, take the debt deliberately: raise
# this number in the same commit and say in the message which clause and why
# no test holds it yet. Silently raising it is what turns a baseline into
# permission.
#
# LOWERED 1350 -> 1349 ON 2026-09-11, and the one clause is not a clause that
# got covered -- it is a clause that stopped existing. 05-07-design.md quoted
# the rule 「表の第 1 列は行 ID とする（MUST）」 verbatim while attributing it to
# section 1.9, and commit ee9f5c4 moved that rule out of the specification
# entirely. The quotation outlived the rule it quoted, so it went; the sentence
# now points at spec-writing-rules.md and carries no marker of its own.
#
# ⭐ THE RATCHET, NOT REPAYMENT. Ground that falls out of real work on the
# specification is booked so it cannot be given back. ⛔ This is not an
# invitation to close the standing 1349 -- see line 1's head.
#
# RAISED 983 -> 1350 ON 2026-09-11 BY INVERTING WHAT COUNTS AS A HOLD -- the
# user's ruling. ⛔ NOT ONE CLAUSE CHANGED AND NOT ONE TEST WAS TOUCHED. The
# corpus this check searches now has its COMMENT LINES REMOVED, so a clause is
# held only if its words appear in the test's own code.
#
# ⭐ THE 367 THE RAISE IS MADE OF WERE NEVER HELD. Measured 2026-09-11, both
# ways, in one pass over the same 1701 markers: 718 held against the corpus
# as it stood, 351 against the same corpus with comment lines struck out.
# ⇒ 367 clauses (51.1% of everything this check called held) were held by
# NOTHING BUT A COMMENT. The raise books what was already true; it does not
# take on anything new.
#
# ⚠️ WHY A COMMENT-ONLY HOLD IS WORSE THAN NO HOLD AT ALL. An unheld clause
# is honestly unheld and this check says so. A comment-only hold reads as
# covered, and when the clause is later rewritten the test beside it goes on
# asserting whatever it always asserted and goes on printing green -- so the
# one thing the count exists for, knowing which clauses a change would break,
# is exactly what it destroys. Nothing presses the words; nothing ever did.
#
# ⛔ THE RAISE IS NOT A DEBT. See line 1's head and the docstring: no round
# may write a test whose purpose is to lower this number.
#
# ⛔⛔ THIS NUMBER IS A BOLT, NOT A DEBT -- the user's ruling, 2026-09-11.
# It fails the round that writes a NEW MUST / MUST NOT clause with nothing
# under tests/ quoting it. ⛔ THE STANDING COUNT IS NOT OWED: no round may
# write a test whose purpose is to lower it. See the docstring of
# check-must-clause-coverage.py for what was measured and why.
#
# ⚠️ EVERY ENTRY BELOW IS A RECORD OF A DAY, NOT AN ORDER FOR TODAY. They were
# written while this number was read as a debt, and several of them tell a
# later round to pay something back. ⛔ Do not obey them. Only line 1 is read
# by the machine.
# RAISED 975 -> 983 ON 2026-09-10, AFTER PAYING BACK SIXTEEN OF THE TWENTY-FOUR
# THE GRAB-AREA RULINGS BORROWED. ⚠️ MEASURED, NOT SUBTRACTED: this check
# printed 999 before the round and 983 after, and the two hit sets were diffed
# against the run at commit c2e58be (the commit that wrote 975) to separate the
# clauses this wave ADDED from the ones other rounds happened to close. The
# wave added 34 unheld markers and other work closed 10 elsewhere, so 975 + 34
# - 10 = 999; sixteen of the 34 are now held, leaving eighteen and 983.
#
# ⭐⭐ FIFTEEN OF THE SIXTEEN WERE ALREADY PRESSED, AND ONLY THE QUOTATION WAS
# BROKEN. ⛔ THIS IS WORTH MORE THAN THE NUMBER, because it is a trap the next
# round will fall into as well: this check matches a TAIL of the manuscript's
# own characters, and a quotation wrapped across two comment lines carries a
# `// ` inside the run, so it latches NOTHING. Every test named below already
# quoted its clause -- wrapped. The repair was to write the same words a second
# time, whole, on ONE line, beside the case that presses them. ⛔ No new test
# was written and no assertion was touched (R-13: 新規のテストは要らない);
# `git diff` shows no changed line that is not a comment, and vitest ran 7434
# passed / 1 skipped either side of the round.
#
# ⭐ WHAT WAS PAID BACK, and the case that presses each (all in tests/unit/):
#   layout-engine.test.ts, R-35 / R-36 / R-38 -- `GR-5` / `GR-6` の掴み代は端の
#     内側だけ（MUST）; 実績の端点を端の外側へ伸ばしてはならない（MUST NOT）;
#     未着手のダミーと着手済の実績は同じ形の掴みシロ（MUST）; 線の掴み代の縦幅
#     が `S-6` より狭いこと（MUST）
#   t-023d-the-plan-start-is-the-boundary.test.ts -- 端の外は予定、端の内は実績
#     （MUST）; `GR-13` を `GR-12` より上に置くこと（MUST）; `GR-8` をマイルス
#     トーンに当ててはならない（MUST NOT）; `GR-18` は 1 か所（MUST）と 開始側
#     と終了側に分けてはならない（MUST NOT）（本行 / GR-18 の 2 か所）; 印を
#     左右に割る規則はマイルストーンに当てない（MUST NOT）; 印の左半分は `GR-9`
#     右半分は `GR-17`（MUST）; 印より右に残る当たり判定は終了側（MUST）;
#     再開アイコンの当たり判定は `S-22`（MUST）
#   t-023d-gr-5-relays-the-actual-duration.test.ts -- `GR-5` の 実績バーを平行
#     移動させてはならない（MUST NOT）
#   fr-009-pd-3-the-two-halves-of-a-bar.test.ts -- 引き出した辺と引き入れた辺の
#     組合せは 4 つの種別と 1 対 1（MUST）
# ⚠️ THE LAST TWO WERE HELD BEFORE THIS WAVE AND LOST IT TO A RE-WRAP, not to a
# ruling -- more evidence that the wrap, not the wording, is what breaks a tie.
#
# ⛔ THE EIGHTEEN THIS ENTRY BUYS, NAMED BY THE ROW THAT OWNS THEM, and why
# each is left rather than latched:
#   FR-043 x2 -- 「印は、長さ 1 稼働日の実績と同じ形で描くこと（MUST）」 and
#     「未着手と着手済で形を変えてはならない（MUST NOT）」. ⛔ NOT LATCHED ON
#     PURPOSE: fr-043-dummy-drawn.test.ts presses the dummy's WIDTH, its band,
#     its colours and (for a milestone) its figure, but no case compares the
#     bar dummy's FORM against a one-working-day actual's. Quoting the clause
#     there would be a copy made to quiet the machine.
#   表 T-023d の `GR-9` x1 -- 「押して状態を巡らせない（MUST NOT）」. The only
#     cases about 巡らせる are t-023d-follows-the-pointer.test.ts's, and they
#     are about `GR-7`, which is the row that DOES cycle.
#   表 T-023d の結び x4, HISTORICAL -- the four clauses the closing paragraph
#     records as withdrawn on 2026-09-08 / 09 (「描かれたダミーの印の画素も、
#     同じく終了側（`GR-17`）を掴むこと（MUST）」, 「その 1 つの印のどの画素を
#     押しても `GR-17` を掴むこと（MUST）」, 「印の一部を `GR-9` に割り当てて
#     掴み分けてはならない（MUST NOT）」, 「人が印を押したときに掴むのは終了側
#     とすること（MUST）」). ⛔ A test may not hold these: the tree is required
#     to do the OPPOSITE of all four. This is the same shape A-appendix.md is
#     excluded for -- a dated quotation is a record, not a rule.
#   表 T-023d の結び x4, AUTHORING -- 「見本そのものを仕様として読んではならない
#     （MUST NOT）」, 「ここに書き写してはならない（MUST NOT）」, 「当たり判定は
#     本段の対象ではない（MUST NOT）」, 「別の規則として書き起こしてはならない
#     （MUST NOT）」. These rule how the MANUSCRIPT is written, which is the
#     distinction Chapter 1 is skipped for; a test quoting them would be a
#     second place holding one rule.
#   表 T-023d の結び x2, `S-90` / `S-91` -- 「予定の端点と実績の端点の掴み代の
#     横幅を揃えること（MUST）」 and 「横幅で予定と実績を見分けさせてはならない
#     （MUST NOT）」. ⚠️ NOTHING ASSERTS THE TWO NUMBERS ARE EQUAL. Both rows
#     are read at run time by in-2-pointer-shape.test.ts and layout-engine
#     .test.ts, and both happen to be 12px today, but no case would go red if
#     one moved. ⭐ This is the one of the eighteen that a case could honestly
#     close, and it is a REAL gap, not a wrapping accident.
#   表 T-023d の結び x1, `S-137` -- 「読む人が `S-137` を広げたときに、道具の側
#     で切り詰めてはならない（MUST NOT）」. No case hands the hit test an
#     enlarged `S-137`.
#   表 T-023d の結び x1, `GR-16` -- 「基準日線（`GR-16`）を同じように上げては
#     ならない（MUST NOT）」. The R-38 cases press `GR-13`; none presses the
#     status line's own place in the order.
#   表 T-023d の結び x1, 「境目はバーの端であること（MUST）」. ⛔⛔ THIS ONE
#     CANNOT BE HELD AT ALL, and it is a fact about the check rather than the
#     tree: even the 28-character tail reaches back across a BLANK LINE in the
#     manuscript, so the run a test would have to carry contains two newlines
#     and no comment can hold it. ⭐ The clause's own words ARE latched one
#     sentence later (「端の外は予定、端の内は実績とすること（MUST）」), and the
#     cases in section 12 of the boundary file press exactly this rule. The
#     file already carries two constants marked 「HELD AT 28」 for the same
#     reason, so the shape was known before this round found a third.
#   05-07-design.md 表 T-221 の `LF-11` x2 -- 「マイルストーンには再開アイコンを
#     置かないこと（MUST NOT）」 and 「`resume` を持つマイルストーンでも描かない
#     （MUST NOT）」. ⛔ THE DRAWING SIDE HAS NO CASE AT ALL: the boundary file
#     presses that a milestone answers no `GR-8` when a resume figure IS handed
#     in, which is the hit side; r-28-the-resume-icon-beats-the-plan-bar.test.ts
#     draws no milestone. ⭐ These two and the FR-043 pair are the honest debt
#     of this round -- four clauses that want a case, not a quotation.
#
# LOWERED 977 -> 975 ON 2026-09-09. Two more clauses gained an anchor while
# RS-56 was given its road: table T-037's closing MUST (a row added to T-233
# needs a road that reaches it) and SK-19's own wording about what an Enter
# does when a telling is standing.
#
# LOWERED 991 -> 977 ON 2026-09-09, PAYING BACK THE WHOLE OF THE ENTRY BELOW.
# tests/unit/r-28-the-resume-icon-beats-the-plan-bar.test.ts holds every clause
# R-27 / R-28 / R-29 / R-30 wrote, verbatim, and the run before it printed 991
# not held against the run after's 977.
# ⛔⛔ THE DEBT WAS FOURTEEN, NOT ELEVEN, AND THE ENTRY BELOW IS WRONG ABOUT IT.
# Measured this round by running this check against the manuscript as commit
# 52b605d left it, with today's tests/ as the corpus: 1634 markers, 977 unheld.
# The commit added FOURTEEN markers and every one of them was bare, so the true
# count went 977 -> 991. The "eleven" is the difference against the 980 written
# in this file, and that 980 was already three too high -- the entry that wrote
# it says as much in its own last paragraph ("the run immediately before this
# file existed printed 990 not held ... some other round's work had already
# lowered the true count"). ⭐ 977 here is the count this check printed, not a
# subtraction.
# ⚠️ AND THE FOUR REFUSAL-REASON ROWS OWED NOTHING. RS-55..RS-58 are rows of
# 表 T-233, and a table row carries no （MUST）marker; the two MUST clauses in
# that table's closing paragraph pre-date the commit and are still unheld,
# among the 977. The brief handed down named those rows as one of the four
# places the debt sat, and they were not one of them.
# ⭐ WHAT IS AND IS NOT PRESSED, stated so the number is not read as coverage:
# four of the fourteen are pressed against the build (R-28, `itemAtPointer`
# answering GR-8 over GR-12 and the span it takes growing 1:1 with S-93). The
# other ten are QUOTED ONLY, and the file says why for each: R-27 and R-29 are
# measured NOT to hold in the tree today (the marker covers all but 4px of the
# GR-17 dummy's box; the drawn mark's pixels answer GR-9, not GR-17), so a case
# would be red, and R-30's Command Palette entrance has no row in 表 T-109 to
# ask for.
#
# RAISED 980 -> 991 ON 2026-09-09, and it is meant to come back down in this
# same round. Landing R-27 / R-28 / R-29 / R-30 and the four refusal-reason
# rows RS-55..RS-58 wrote eleven new MUST / MUST NOT clauses, and the body
# that wrote them was forbidden tests/ on purpose -- a specification body that
# writes its own anchor writes the anchor its own reading needs.
# ⛔ MEASURED, not predicted: the specification body expected thirteen; the
# trunk measured eleven after the merge.
# ⚠️ THE HONEST FINDING that came with them, and it is worth more than the
# number: the SUBSTANCE of R-27 and R-28 is held by nothing mechanical. The
# body reversed each rule in place, keeping the verbatim, and measured ZERO
# reds either way -- a specification saying the opposite of what the user
# ruled passes every check and every test. Only the verbatim is latched, by
# check 43, and only because rulings.md names it.
#
# LOWERED 991 -> 980 ON 2026-09-09, PAYING BACK PART OF D-377's DEBT.
# tests/unit/d-377-fr-016-zoom-ceilings-and-ep-9-thickness.test.ts holds nine
# of the eighteen clauses commit 381c2ef raised 934 -> 951 on 2026-09-07 (the
# two zoom ceilings of FR-016, T-076 EP-9's Panel Divider thickness): four of
# FR-016's and all five of EP-9's. The other nine of the eighteen were ALREADY
# held, verbatim, by tests/system/user-reported-fixes.test.ts -- a fact this
# round measured rather than trusted, because the brief handed down said none
# of the eighteen had a tie. ⭐ Two of the nine held here are quoted only
# (FR-016's "no font comparison" and "no dead entrance at the ceiling" MUST
# NOTs): the first names a private function no published entry surfaces, the
# second's own text points the press at FR-029's screen-renderer convention,
# neither of which a black-box unit test can honestly assert without inventing
# a contract the specification does not state. ⭐ The other seven are PRESSED
# against the real build, not merely quoted: EP-9's five against
# `GROUP_GRID_LINE_WIDTH_PX` / `screenFrameFromRegions` / `exportSvg` (a
# hand-typed duplicate width was measured to turn two cases red), and FR-016's
# remaining two against `rowPlacesAtZoomY` (an arithmetic-from-zoom shortcut,
# the literal violation the MUST NOT names, was measured to turn one case red).
# ⚠️ MEASURED, NOT ASSUMED: the run immediately before this file existed
# printed 990 not held (the baseline above still read 991 -- some other
# round's work had already lowered the true count without updating this file);
# the run immediately after printed 980. ⛔ D-377's debt is not closed: this
# entry pays back only the nine named above. FR-016's STATEMENT block carries
# well over a hundred MUST/MUST NOT markers in all (table T-023a/T-023b/T-023d
# among them), most of them unrelated to the 2026-09-07 raise and untouched by
# this entry -- D-377 itself is the eighteen this round was scoped to, not the
# whole requirement.
#
# LOWERED 995 -> 991 ON 2026-09-09 by three rulings landing at once.
# Four MUST clauses gained an anchor that quotes them word for word:
# the one-card outside label (FR-090) took three, and AS-7's release
# took the fourth. ⭐ Measured, not estimated -- the runs before and
# after the merge printed 995 and 991 held against the same manuscript.
#
# LOWERED 1016 -> 995 ON 2026-09-08, IN THE SAME ROUND THAT RAISED IT.
# The entry below took on fifty-two clauses of debt and said to expect it to
# fall again from the fix-confirmation anchors alone, without a fresh suite.
# It fell twenty-one. ⭐ THE PREDICTION HELD, and it held because the repair
# bodies were told to make their anchors quote the clause verbatim rather
# than paraphrase it -- one line in each brief, and it paid for itself.
# ⚠️ It is still 31 above where the round opened (964). D-377 owns the rest.
# ⛔ Do not close that row while this number stands above 964.
#
# RAISED 964 -> 1016 ON 2026-09-08, DELIBERATELY, AND THE USER SET THE PRICE.
# Four rulings and six clause-gaps landed in one wave and brought about
# fifty-two MUST / MUST NOT clauses with them -- the boundary binding GR-4,
# the milestone dummy's figure and colour, the panel that AS-1 now opens,
# AS-11's rename, RS-54, IN-4's 選択 rung, MK-13 read before the priority
# order, GR-8's hit box, T-038's order and its reserved space, and FR-019's
# one polyline.
# ⛔ NO TEST WAS WRITTEN FOR THEM, and that is not an oversight: the user's
# instruction that morning was 「新規のテストは要らない。 不具合修正確認の
# テストや実測だけでよい」. ⇒ The tie comes from the fix-confirmation anchors
# the repair bodies write, not from a fresh suite.
# ⭐ EXPECT THIS TO FALL AGAIN IN THE SAME ROUND. Every anchor that quotes its
# clause verbatim pays some of it back -- 966 -> 965 -> 964 all happened that
# way, in hours, without anyone setting out to do it.
# ⚠️ The debt is D-377's. ⛔ Do not close that row while this number stands
# above 964.
#
# LOWERED 965 -> 964 ON 2026-09-08. The nineteen tests that held FR-043's
# PRE-ruling reading were rewritten to the ruling itself, and two new cases
# were added that press WHERE the one mark stands, not only how many there
# are. The clause they now carry verbatim is the 2026-09-08 MUST NOT against
# a second mark -- one of the six this file's next entry took on debt for.
#
# LOWERED 966 -> 965 ON 2026-09-08. Not a test written for the purpose -- an
# existing test grew the clause verbatim while a body was mending a fabricated
# citation beside it, so the ground came for free.
# ⭐ This is the direction D-377 owes: the debt went 934 -> 976, was walked
# back to 972, then to 966, now 965. Every clause an anchor takes verbatim
# pays a little of it back. ⛔ Never raise this to make a wave pass.
#
# RAISED 960 -> 966 ON 2026-09-08, DELIBERATELY AND WITH THE USER'S LEAVE.
# Six clauses arrived from three rulings given that evening and none has a
# verbatim tie yet:
#   表 T-023d  the dummy is drawn as ONE mark, not one per end (MUST), and the
#             two ends may not be drawn separately (MUST NOT)
#   表 T-023d  where two endpoints land on ONE day the finish is grabbed (MUST),
#             and the table's printed order may not be used to hand the press to
#             the start instead (MUST NOT) -- this applies to the plan's two ends,
#             the actual's two ends and the dummy's two ends alike
#   FR-031    a write that moved nothing leaves no 段 -- the three clauses of
#             that ruling ARE held (tests/unit/fr-031-...), and are not part of
#             this raise
# ⛔ THE USER SAID THIS DEBT NEED NOT BE PAID THIS ROUND: 「追加の検査で見つかった
# 不具合は対応不要。とにかく既知の不具合をすべて直せ」 (2026-09-08).
# ⭐ THE DEBT IS STILL BOOKED, and the handoff names it: a spec-only body pays
# it back in the round that implements those clauses, which is the same round
# that closes ledger rows D-393 and D-396.
# ⚠️ THE OTHER HALF OF THAT INSTRUCTION WAS NOT TAKEN THE SAME WAY. The
# quoted-source check (comments citing sentences docs/spec does not contain) is
# held at its baseline, because on this one day it caught two fabricated
# citations and one of them was load-bearing -- the code reasoned from words no
# table holds. That one costs no new test, only a corrected comment. The
# handoff records this as an opinion awaiting the user's word, not a decision.
#
# LOWERED 967 -> 960 ON 2026-09-08 by the two rulings of 2026-09-07
# (「トグルにせよ。 ユーザーに選択肢がある。」 and 「タスクはドラッグ必須。」).
# A spec-only body holds THIRTEEN clauses verbatim in
# tests/unit/fr-001-fr-083-the-toggle-and-the-drag.test.ts (36 cases), and
# every one of them is PRESSED as well as held -- including the two controls
# that stop the easy wrong builds passing: a different entrance still arms
# while a selection stands, and a milestone pressed without travel is still
# placed and still tells nobody.
# ⛔ ONE CLAUSE OF THAT NEIGHBOURHOOD CANNOT EVER BE HELD, and the body
# measured why rather than skipping it: 表 T-023b's 「構えは持続すること
# （MUST）」 starts a paragraph with only 17 characters before its marker, and
# this check's shortest window is 28 -- so every window reaches back across a
# blank line, which no single string literal can carry. The file says so in
# its header and presses the rule anyway.
# ⚠️ ALSO MEASURED: FR-083's milestone exemption is written
# （MUST NOT 適用しない）, which this check's marker regex does not match. That
# exemption is invisible to the count -- a bare-clause list is not complete.
#
# LOWERED 972 -> 967 ON 2026-09-07 by the row axis: the ceiling and the row
# anchor landed, and their anchors in tests/system/user-reported-fixes.test.ts
# carry five clauses verbatim. ⛔ Only clauses the anchor actually exercises
# were quoted -- a clause held by a case that does not drive it pays this
# check in appearance only, and the body that wrote them refused to do that.
#
# LOWERED 976 -> 972 ON 2026-09-07, part of the debt D-377 booked. The wave
# that built FR-016's ceiling for the DATE axis (ledger row D-375) wrote its
# anchor in tests/system/user-reported-fixes.test.ts, and that anchor carries
# four of the clauses raised that day in the manuscript's own characters: the
# range S-75 / S-76 holds, 「拡大の側には、軸ごとに導かれる上限を置くこと」,
# 「固定の倍率で止めてはならない」 and 「日数を `src/` に打ち込んではならない」.
# ⛔ THE REST OF D-377'S DEBT IS STILL OWED, and deliberately: FR-016's ceiling
# for the ROW axis (D-374) and its row anchor (D-366) are not built in this
# tree, so their clauses are not quoted -- a clause held by a test that does
# not exercise it pays this check in appearance only.
#
# RAISED AGAIN 951 -> 976 THE SAME DAY, by the creation flow (FR-083's SP-2 /
# SP-3, the press order of table T-023a, FR-091) and by the two Agent API
# rulings (FR-068, FR-022). ⛔ Same debt, same holder: the ledger row D-377.
#
# RAISED 934 -> 951 ON 2026-09-07 by the two zoom ceilings (FR-016) and the
# Panel Divider's thickness (EP-9), both written from the user's rulings of
# that day. ⛔ THE DEBT IS OWED BACK THE SAME ROUND: the ledger row D-377
# holds it, and the anchors land with the code, in the wave that implements
# them. A clause written today and tested tomorrow is the exact shape D-254
# measured, so it is recorded rather than left to be noticed.
#
# LOWERED 935 -> 934 ON 2026-09-07 by the plan/actual split (FR-049, the
# user's ruling). Three new clauses were written and all three are held
# verbatim by tests/unit/uf-62.test.ts; the two clauses the ruling struck
# went out with the enumeration they described.
#
# LOWERED 959 -> 935 ON 2026-09-07, THE SAME DAY IT WAS RAISED. The debt
# booked as D-355 is paid: tests/unit/d-355-the-four-rulings-of-2026-09-07
# holds all eighteen clauses of that day's four rulings verbatim, plus four
# neighbours caught in the same windows -- 22 markers, 20 constants,
# 31 cases. ⭐ Nine of the eighteen are also PRESSED against the build
# (FR-060 four ways, EP-1/S-225/S-226 three, FR-020 two); the other nine
# are held verbatim only, because the build does not exist yet, and the
# file says so instead of writing a case that passes on nothing.
# ⛔ THE CONSTANTS ARE CUT FROM THE MANUSCRIPT BY SCRIPT, never typed, and
# the file re-reads docs/spec at run time and goes red if a clause is
# reworded -- check 39 counts raw bytes under tests/, so a test that only
# reads the specification holds nothing.
#
# RAISED 941 -> 959 ON 2026-09-07. Eighteen clauses were written into the
# specification in one round, from four rulings the user gave that day, and
# none of them has a verbatim tie under tests/ yet:
#   FR-060  (3) the overwrite target is NOT remembered, no start-up offer to
#           restore the permission, and no file handle in localStorage/IndexedDB
#   FR-012  (3) a calendar edit re-counts the stored percent complete, tells
#           with a count, and does it inside the SAME write as the calendar
#   RS-52   (1) may not re-count silently
#   EP-1    (3) screen and export read ONE row for the title's size and inset,
#           no export-only constant, no measuring the DOM
#   S-225 / S-226 (2) no export-only value; do not scale by the band's height
#   FR-020  (2) the place that shows "evidence, not access control" is QN-9,
#           and no second display word may be minted for it
#   GR-21   (4) the grip's length is the visible fraction, its floor is S-205,
#           plus FR-051's own MUST that the grip must be draggable
# ⛔ THE DEBT IS BOOKED. Ledger row D-355 carries it and names the four tests
# that pay it back; lower this number in the commit that lands them.
#
# LOWERED 952 -> 941 ON 2026-09-06. Eleven clauses of this round are now held
# verbatim by a test that also presses what they say: FR-025 (a picture may
# not carry a scale; a document holding the retired key keeps it) through the
# export's own size and a JSON round trip; FR-023's five (drop the row, do not
# ask, tell the names afterwards) through an import of a task with an unusable
# date; and HF-17 (unfold segment zero by ONE level, not all of it) through a
# fully folded document. ⭐ Two more were already held, by the ST-7 file.
# ⛔ None of the eleven went red -- the code already matched, and the sites
# that carry them quote the clauses in their own comments.
# RAISED 950 -> 952 ON 2026-09-06. FR-020 gained the two clauses the user
# settled about WHEN the watermark's trail is stamped: at the moment the
# document is opened and again when the document CHANGES (a watermark
# answers "who had this content on screen, and when" -- change the content
# and the answer changes), and never from a clock read inside the frame
# loop (two frames drawing the same schedule would otherwise be two
# different pictures, and FR-102's record a different string each second).
# ⛔ Debt: the write path lives in frame-loop.ts, which another body holds
# this round. Held verbatim when that file is free.
# RAISED 948 -> 950 ON 2026-09-06. Two more of the user's rulings reach the
# specification: a comment box's body is placed by an offset from its anchor
# and that offset lands on the box's BOTTOM-LEFT (a second reference corner
# would put the same number in two places); and hiding the dependency lines
# may NOT be given a separate face, because the palette toggle is symmetric
# and the geometry is rebuilt every frame, so showing them again is the way
# back. ⛔ Debt: held verbatim when the drawing follows.
# RAISED 946 -> 948 ON 2026-09-06. FR-025 lost the MUST that made the PNG
# scale selectable and gained two in its place, both the user's: a picture
# MAY NOT carry a scale at all, and a document that still holds the retired
# key keeps it (OP-6 covers keys this build no longer knows).
# ⭐ S-82 retires with the idea -- anyone needing more than 1600x900 is
# handed JSON, single html, MSPDI XML or SVG instead of a bigger raster.
# ⛔ Debt: held verbatim when the code follows.
# RAISED 943 -> 946 ON 2026-09-06. FR-009's crossing rule got the two halves
# it never had, both from the user: the halo is drawn on the line in front
# (never by detecting crossings, which would put that search on the every-
# frame road NFR-013 governs); the line in front is the SELECTED one, and
# otherwise the later-made one; and the halo may not be laid where the line
# runs over a bar, because a bar is the schedule itself and may not be eaten
# for the sake of a dependency line.
# ⛔ Debt: a spec-only body holds these three verbatim when the drawing lands.
# LOWERED 952 -> 943 ON 2026-09-06. A spec-only body measured which of five
# rows were ALREADY held and refused to write those, then held only what was
# bare: that the depth valve actually raises RS-24 when it is reached (the
# existing file only asked the negative), and that the EXPORT road tells --
# ST-7's clause of that morning. 35 new cases, all green.
# ⛔ It also left four clauses unwritten and said why: the specification does
# not say how often RS-24 may be raised, does not name a reason row for a
# refused paste or an empty store, and does not ask the non-picture exports
# to stay silent. ⭐ Those are holes, not coverage.
# RAISED 948 -> 952 ON 2026-09-06, DELIBERATELY. FR-009 gained four marked
# clauses when the user's ruling on D-346 was written down: where the half
# splits (the bar's own midpoint, not table T-038's occupied width), which
# side the exact midpoint falls on, that the answer must be reachable from a
# published name, and that the existing hit test may not take the arm.
# ⛔ Debt, not a settlement: the body that implements it runs beside a
# spec-only body whose job is to hold these four verbatim.
# LOWERED 952 -> 948 ON 2026-09-06, the same day it was raised. Retiring
# the guide cursor's 'double-vertical' (CR-369) took marked clauses out with
# it, and the spec-only body that followed the moved tables held four more
# verbatim. ⛔ The nine clauses the raise was taken on are still bare; this
# is not them being covered.
# RAISED 942 -> 952 ON 2026-09-06, DELIBERATELY. Four rulings of that day
# reached the specification and each added marked clauses that nothing under
# tests/ holds yet:
#   FR-023  the import drops the invalid rows and tells afterwards -- the two
#           choices are gone, the telling and the "not a count alone" are new
#   T-023 MK-13 / T-016 PR-21  the comment box body is edited in the panel
#   T-014 ST-7  the telling may not be limited to the drawing road
#   T-051 HF-17  adding a row at level 0 opens the fold by one level, and
#           may not open it all the way
#   T-109 IC-98 / T-103 U-62  the entrance FR-095 never had, and the face
#           that lists what the import dropped
# â This is debt, not a settlement: a spec-only body is to hold these
# verbatim and bring the number back down.
#
# LOWERED 951 -> 942 ON 2026-09-06. A spec-only body wrote four files for rows
# that were built and measured and held by nothing: the resume date, the
# carried original, deleting an assignee with its question, and the three file
# gates. Nine clauses newly held, 63 cases.
#
# ⭐ One of its cases was RED and found a defect nobody had filed: FR-044's
# FIRST must -- putting a resume date makes it valid -- was still broken while
# its second was being repaired. The requirement names the failure beside the
# clause. Fixed, so the case is green.
#
# ⚠️ It also measured what the corpus actually is: the RAW FILE BYTES. A
# trailing window containing a paragraph break cannot be held by a TS string
# literal, so two clauses had to be held at 60 and 40 characters rather than
# 120. The fall-through is what saved them.
#
#
# RAISED 950 -> 951 ON 2026-09-06, a debt. DC-7 was reversed by the user's
# ruling: it used to require a SEPARATE entrance to clear the two measuring
# lines and forbid the mode's exit from clearing them; it now requires the
# exit to clear them. Two clauses left, one arrived, and nothing holds it.
#
# LOWER THIS when a body that reads only docs/spec has written it. The
# spec-only body running this round was given five other rows, not this one.
#
#
# LOWERED 952 -> 950 ON 2026-09-06. The spec-only body's FR-098 file landed:
# 17 cases, five of which were RED when it wrote them and are green now that
# the band is cut. It holds both clauses the pinned-band ruling created.
#
# ⛔ Two of its cases had to be retired on arrival, and by me rather than by
# it: they latched 「帯が Row Area を埋め尽くし…（MUST NOT）」, which the same
# ruling withdrew AFTER the body branched. A test for a clause that no longer
# exists is worse than no test. The latches now hold the sentence that
# replaced it, so the files still go red if the manuscript drifts back.
#
# ⚠️ THE DEBT IS NOT FULLY PAID. It stood at 949 before the ruling; the two
# corrections to FR-098 put it at 952 and this pays back two.
#
#
# RAISED 950 -> 952 ON 2026-09-06, the same debt as the line below, grown.
# The body that implemented CR-363 found two more sentences of FR-098 that
# the ruling overturned and the CR had not edited: the list of reasons a
# pinned row may go undrawn (it said those reasons were the ONLY ones), and
# the MUST NOT against the band filling the Row Area -- which the ruling's
# own wording presupposes breaking. Both are now written as the ruling has
# them, and nothing holds the new wording yet.
#
# LOWER THIS when the spec-only body writing FR-098's cases returns. It was
# already running when this was raised.
#
#
# RAISED 949 -> 950 ON 2026-09-06, and it is a debt, not a result.
# CR-363 took the user's ruling on the pinned band: it REMOVED one MUST (the
# band scrolls when it does not fit) and put two in its place -- what does not
# fit is not drawn (MUST), and the band may not be made scrollable (MUST NOT).
# Net one more clause, and nothing holds either yet.
#
# LOWER THIS BACK TO 949 when a body that reads only docs/spec has written
# them. A raise that outlives one round is a clause nobody ever tests.
#
#
# LOWERED 960 -> 949 ON 2026-09-06. A spec-only body wrote four files for
# rows that were built and measured but held by nothing: entering the Dual
# Cursor drops the arm, a standing explanation is the last rung of the Esc
# ladder, F2 opens the document name in place, and AM-7 refuses a malformed
# request instead of throwing. Eleven more clauses are held.
#
# Six of its cases were RED on purpose and are green now: they showed that
# the AM-7 guard read the request's two members and nothing inside them, so
# `commands: [null]` still threw. That is the shape of test this project
# wants -- the body was told not to soften an assertion and did not.
#
#
# LOWERED 965 -> 960 ON 2026-09-06, WHICH PAYS OFF THE RAISE BELOW AND ONE
# CLAUSE MORE. A spec-only body wrote the cases that press the four clauses the
# two rulings of 2026-09-06 added, and quoted each of them verbatim:
#
#   OP-10   tests/unit/t-024a-op-10-a-chosen-zoom-is-the-place.test.ts
#           (both clauses: the choice becomes the place, and the row is not
#            redone every frame)
#   FR-021  tests/unit/fr-021-the-outline-base-of-the-file-comes-back.test.ts
#   Ch 5.4  tests/contract/erd-seats-are-never-reissued.contract.test.ts
#
# ⭐ THE TARGET WAS 961 AND THE MEASUREMENT SAYS 960, so 960 is what is written
# here -- this file may fall freely and 「lower the baseline to hold the
# ground」 is what the check itself printed. The fifth clause is `NF-4` of
# table T-224 (「使われないまま残った席番号であり、別のものに割り当て直しては
# ならない（MUST NOT）」), quoted in the contract file's head comment because it
# is the reason that file refuses to assert the seats are contiguous. ⚠️ It is
# held by a QUOTATION GOVERNING A DELIBERATE NON-ASSERTION, not by an
# assertion -- which is all this check ever claims of any of its 494.
#
# ⛔ THE RUN IS RED ON PURPOSE IN TWO PLACES, and neither assertion was
# weakened to make it green:
#   - seven cases of the OP-10 file. Measured at commit c782071: one press of
#     SK-16 writes `zoomX: 1 -> 1.1` into the document and the frame draws the
#     same `pxPerDay` as before it, with `scrollDate` / `scrollGroupId` still
#     null. CR-359 wrote the sentences and left the implementation to a later
#     body ("裁定 1 の実装場所を仕様では指さない"), so the work is owed.
#   - one case of the ERD seats file. 表 T-057 prints thirty `RL-` rows while
#     the manuscript seats twenty-two; `RL-23` to `RL-30` (the carryElements
#     rows) are numbered from the end of the list, so one more relation
#     renumbers all eight. A-appendix 1.84 counts 22 relations seated.
#
#
# RAISED 961 -> 965 ON 2026-09-06, AND THE RAISE IS A DEBT, NOT A RESULT.
# Two rulings added four clauses and no test holds any of them yet:
#
#   OP-10   a person's zoom or view choice BECOMES the view position (MUST)
#   OP-10   the row may not be redone every frame (MUST NOT)
#   FR-021  the outline base of the file that came in is kept (MUST)
#   Ch 5.4  an ET-/AT-/RL-/DV- number, once given, is never reissued
#           (MUST NOT)
#
# The tests are owed to a body that reads only docs/spec/, which cannot be
# spawned until this work is a commit for it to branch from. LOWER THIS BACK
# TO 961 when those four are held. A raise that outlives one round is a
# clause nobody ever writes a test for.
#
#
# LOWERED 972 -> 961 ON 2026-09-05. A spec-only body wrote the cases that
# press the frame-time clauses this round added, and quoted them. Eleven more
# clauses are now held. The run is RED on purpose: one operation gets two
# opposite verdicts depending on which number is read, which is exactly what
# the new clauses forbid confusing.
#
# LOWERED 978 -> 972 ON 2026-09-05, after two spec-only bodies wrote the
# tests that hold this round rulings clauses. The check itself said to:
#   "976 not (was 978) -- lower the baseline to hold the ground".
# The rulings ADDED clauses (978 -> 1000 at the worst point); the ground was
# recovered by writing tests that press them and quote them, not by raising
# the number. Two of the new tests are RED on purpose -- a red test holds a
# clause just as well as a green one, and says the work is owed.
#
# ⭐⭐ LOWERED 1164 -> 978 ON 2026-09-05, THE SAME DAY IT WAS FIRST SET,
# because the first measurement counted two kinds of clause it should never
# have read. Nothing was written to earn this; the scope was wrong.
#
#   165  A-appendix.md, every one of them under A.3 Changelog. Those are
#        RECORDS -- past-tense sentences saying a MUST was placed somewhere.
#        A test cannot hold one, and asking for one asks a test to pin
#        history. Same distinction HF-14 was corrected for the same day.
#    45  Chapter 1 of 01-04-requirements.md. Those rule how the
#        SPECIFICATION is written, not what the product does, and checks 5
#        to 15 already read the manuscript for exactly them.
#
# ⛔ AND THE SCOPE CHANGE ITSELF SHIPPED A BUG THAT WAS CAUGHT BY READING THE
# NUMBER. The first attempt matched the chapter heading with a bare "# ",
# which also matches inside "## ", so the skipped span ran to the end of a
# file that carries one top-level heading -- 1465 clauses vanished and the
# check happily reported 146. A count that moves that far is not a result.
#
# ⭐ Coverage as it now stands: 447 held of 1425.
#
# ⛔ MUST / MUST NOT clauses in docs/spec/ (the nine manuscript files
# check-must-clause-coverage.py reads -- see its own docstring) with no
# verbatim tie to any file under tests/.
#
# ⭐ MEASURED 2026-09-05, THE DAY D-254 WAS FILED: 1,634 marked clauses total,
# 470 held (some part of the clause, at least 28 characters ending at the
# marker, quoted verbatim in some file under tests/), 1,164 not. This is the
# FIRST measurement -- nobody had counted this before, so 1,164 is simply
# today's true state, not a chosen number.
#
# ⭐ The number coming DOWN is the work (a round that writes a verbatim-
# holding test for a previously bare clause lowers it, and should lower this
# file to match). It going UP is a MUST or MUST NOT clause added, or a clause
# whose only tie broke, without a test taking its place.
#
# ⛔ RAISE THIS ONLY TO TAKE THE DEBT ON DELIBERATELY, and say why in the commit.
