96
# RAISED 93 -> 96 on 2026-09-14: CR-376 retired AT-35 (the stored working-day
# actualDuration). Three references stay on purpose: cr-376-exchange-carries-the-last-day.test.ts
# pins that table T-058 no longer holds AT-35, and two WHY comments (properties-panel.ts,
# fr-006-panel-typography.test.ts) record that PR-5 has no column since AT-35 retired.
# LOWERED 108 -> 93 on 2026-09-14: the tests/ comment wave (JDG-77) removed comments
# naming retired specification ids.
#
# Spec ids named in src/ and tests/ that docs/spec does not define: retired ids
# plus undefined ones. Held by check-spec-id-references.py; line 1 is the
# number.
#
# ⛔ This is a ratchet, not a debt schedule: its one job is to fail the round
# that adds a dead reference. Lower it in the commit that removes references.
# ⛔ 0 is not the target. Naming a retired id is correct where a comment
# records what was retired, where a test pins that an id stays retired, and
# where a retirement list itself names it.
