161
#
# LOWERED 163 -> 161 ON 2026-09-12, stage 1 of the src/ comment cleanup.
# Both were IC-69 / IC-70 in screen-renderer.ts -- UNDEFINED, not retired --
# and both sat inside a retraction record that 6-1 ① removes anyway. The
# retired count is unchanged at 143; 18 undefined stands.
# ⛔ 0 IS STILL NOT THE TARGET (裁定 8). Naming a burnt ID is correct in
# three shapes, and this floor is close.
#
#
# LOWERED 248 -> 163 ON 2026-09-11 by wave 3 of P1-3: three bodies, 45 files.
# Across all three waves 445 -> 163 over 78 files, and ⛔ NOT ONE LINE OF
# EXECUTABLE CODE CHANGED in any of them. tsc 0, vitest 7478 passed /
# 1 skipped, layers 299/299, parity OK -- identical either side, every wave.
#
# ⛔ 163 IS NEAR THE FLOOR, NOT NEAR ZERO. Wave 3's bodies classified 39 of
# their own findings as CORRECT references that must never be removed:
# a case named 'T-034 ... never answers with the seat CR-280 retired'; an
# assertion reading expect(rows.find(one => one.id === 'S-139')).toBeUndefined();
# a fixture literally named documentWithRetiredKey whose whole purpose is to
# prove a retired key stays inert; and prose anti-examples (FR-04, HF-120,
# UF-6n) that exist to show what a regex must NOT match. ⇒ A RISE is the
# fault. ⛔ The standing number is not work owed.
#
# LOWERED 370 -> 248 ON 2026-09-11 by wave 2 of P1-3: five bodies over
# disjoint file sets, plus one repair to this check itself.
#
# ⛔ NOT ONE LINE OF EXECUTABLE CODE CHANGED, over any of the 33 files the
# two waves have touched. tsc 0, vitest 7478 passed / 1 skipped, layers
# 299/299, parity OK -- all identical either side.
#
# 21 OF THE 122 WERE NEVER REFERENCES AT ALL. This check faulted HM-4s,
# IV-10s, PI-17s, FR-085s, NT-1s and six more -- the project writing
# 「HM-4s own row」 for 「HM-4's own row」, with the trailing [a-z] that lets
# T-005a and SK-11a match swallowing the possessive. ⛔ Two sat in a TEST
# NAME, which a cleanup may not edit, so they would have sat here forever
# being counted as debt that was never owed.
#
# ⭐ FOUR IDS THIS CHECK CALLS "not defined" ARE DELIBERATE RETIREMENTS THAT
# NEVER REACHED md-checks.py's RETIRED SET: U-29 (CR-320), IC-51 (CR-273),
# IC-94 (CR-366) and S-185 (CR-311), plus IC-69 / IC-70 (CR-327) and RS-45
# (CR-347). Booking them changes what a check decides, so ruling 9 holds them
# for a ruling rather than letting a cleanup make it in passing.
#
# ⚠️ PD-100 .. PD-103 WERE NOT WITHDRAWN -- THEY WERE NEVER WRITTEN.
# pending-decisions.md runs 95, 104, 105; git log -S over the whole history
# returns 0 commits for each of the four. Six comments in
# file-system-access-file-store.ts pointed at them, and one claimed to have
# MEASURED that a row 「is gone from pending-decisions.md (measured
# 2026-09-07)」. It was never in that file. A fabricated measurement is worse
# than no comment: it tells the next reader the checking has been done.
#
# LOWERED 445 -> 370 ON 2026-09-11, the day this check was written, by
# repairing 75 references across eleven files in src/ and tests/. ⛔ NOT ONE
# LINE OF EXECUTABLE CODE CHANGED: the diff over all eleven files carries no
# changed line that is not a comment, vitest ran 7478 passed / 1 skipped on
# both sides, and tsc reported 0 either way.
#
# ⚠️ 370 IS NOT A DEBT AND 0 IS NOT THE TARGET. A floor exists and it is not
# zero, because some references to a burnt seat are CORRECT:
#   - a test named for the withdrawal itself -- the case called 「table T-109
#     holds neither IC-46 nor IC-49, and the seats are left empty」 guards the
#     retirement, and its name has to say which seats
#   - an assertion that PINS the manuscript's own withdrawal record, so that a
#     manuscript quietly restoring the old rule turns the test red
#     (t-023d-the-hit-box-starts-at-the-day.test.ts:497)
#   - fixture data that merely looks like an id -- the projectId values P-001
#     and P-002, ten occurrences in uf-41-42.test.ts, which are not
#     specification ids at all
# ⇒ Read a RISE as "new code was written against a burnt seat". ⛔ Do not read
# the standing number as work owed, for the same reason check 39 stopped being
# read that way on this day.
#
# SET AT 445 ON 2026-09-11, THE DAY check-spec-id-references.py WAS WRITTEN AND
# THE FIRST DAY ANY CHECK IN THIS DIRECTORY OPENED A FILE UNDER src/ OR tests/.
#
# WHAT THE NUMBER COUNTS. Occurrences, not lines: every place in the 295 files
# under src/ and tests/ (.ts, .tsx, .js, .mjs; node_modules skipped -- all 295
# are .ts today) where an ID-shaped token names a row the specification retired
# on purpose, or names nothing at all. Backticked and bare alike; inside string
# literals and comments alike. The split on the day it was set:
#
#     328  retired   -- 24 distinct IDs out of the 54 then recorded
#                       (the set holds 61 since 2026-09-11; see retired.py)
#     117  undefined -- 33 distinct IDs that resolve nowhere
#     445  total, over 405 distinct lines
#
# 263 of the 445 sit OUTSIDE any code span, which is why this check does not
# require backticks the way specindex.py's REF_TOKEN does; a backtick-only rule
# would have seen 182 and called the other 263 clean.
#
# ⭐ THE TWENTY-FOUR RETIRED IDS AND WHAT THEY COST, measured 2026-09-11
# BEFORE the round. ⚠️ Today it is 23 distinct over 145 references -- run
# the check rather than reading this list as current:
#     S-93 175, IC-46 22, S-59 17, IC-49 17, S-82 15, FR-061 11, BT-3 11,
#     S-139 10, CM-70 10, RD-5 7, CM-57 5, RS-17 4, IF-4 3, IC-57 3,
#     S-209 3, U-28 2, IC-55 2, FR-026 2, CP-29 2, CP-23 2, FR-050 2,
#     S-112 1, S-52 1, K-88 1
# S-93 alone is 39.3% of the whole number. It left table T-023d on 2026-09-10,
# one day before this baseline was set, and 175 places under src/ and tests/
# still name it.
#
# ⚠️ THE UNDEFINED HALF IS NOT ALL DEFECT, and the module docstring names the
# three false-positive classes kept on purpose rather than narrowed away: an
# English possessive written without its apostrophe (FR-085s, HF-17s, NT-1s and
# six more, 17 occurrences), fixture data shaped like an ID (projectId 'P-001' /
# 'P-002' in tests/unit/uf-41-42.test.ts, 10 occurrences), and a prose example
# of a prefix that must NOT match (FR-04, HF-120, UF-6n, 3 occurrences). ⛔ DO
# NOT ADD AN EXEMPTION FOR THEM. The narrowing that would remove the first class
# -- dropping the optional trailing letter from the token pattern -- also stops
# matching the manuscript's own suffixed rows (T-005a, PD-4a, SK-11a) and takes
# the count to 424, hiding real hits to remove 17 harmless ones.
#
# ⭐ THE REST OF THE UNDEFINED HALF IS THE THING THIS CHECK WAS BUILT FOR:
# withdrawals that were never recorded as retirements. IC-69 (13) and IC-70 (9)
# were taken out of table T-109 by CR-327 on 2026-09-02 and are named in six
# source files and four test files, one of them as a live Playwright selector
# ([data-icon="IC-69"], tests/system/nfr-004-file-scheme-sweep.sws.test.ts).
# RS-45 (18), U-29 (9), S-185 (8), UF-72 (8), IC-94 (3), SK-11a (3), RS-47 (2),
# IC-51, T-055, T-100, MK-14 and F-3 are the same shape. ⛔ NONE OF THEM IS IN
# md-checks.py's RETIRED SET, so no check in this directory has ever seen them.
# ⚠️ Some of those may deserve a seat in RETIRED rather than a repair in the
# code -- that is a judgement for whoever repairs them, not for this file.
#
# ⛔ THIS IS A RATCHET, NOT A DEBT SCHEDULE. Its one job is to fail the round
# that writes NEW code against a burnt seat. It does not order anyone to pay the
# 445 down, and no round should write a repair whose only purpose is to lower
# the number -- the same ruling that governs must-clause-coverage-baseline.txt.
# ⭐ When a round moves docs/spec or src/ and the count falls out of that work,
# lower this number so the ground cannot be given back.
