82
# LOWERED 86 -> 85 on 2026-09-22, with the user's approval (asked in chat, answered "4 つとも
# 下げる（推奨）"): measured 85 by check 45 after the CR-441 code wave (frame-loop.ts, item-grab.ts,
# schedule-geometry.ts, schedule-layout.ts); which group went was not attributed.
#
# LOWERED 90 -> 86 on 2026-09-21, with the user's approval (the same answer as the raise below):
# the eight copies of reject() in src/use-case/edit-document/ are one, in edit-document.ts.
# The copy edit-dependency.ts held took an optional reasonCategory; that one is kept, so the
# three-argument calls return the same shape as before (golden harness, 128 cases).
#
# RAISED 89 -> 90 on 2026-09-21, with the user's approval (asked in chat, answered
# "分割で 90、次のコミットで 8 つを畳む（推奨）"): stage 4 wave 2 split edit-task.ts and
# edit-task-group.ts, and both now export their reject() so the siblings can use it. The two
# copies were there before the split; the export made them read as one new group. The very
# next commit folds all 8 copies of reject() in src/use-case/edit-document/ into one and
# lowers this line again.
#
# RAISED 88 -> 89 on 2026-09-20, with the user's approval (asked in chat: "進めてよい"): CR-430 left one
# copy that cannot be folded without publishing a name across the component boundary
# (isThinShape in schedule-geometry.ts and laidBelow in schedule-layout.ts), which needs a row
# of table T-064. Fold it in the next round.
#
# LOWERED 90 -> 88 on 2026-09-20, with the user's approval ("③ 数え直してから下げてよい", JDG-271):
# CR-429 replaced mspdi-codec.ts's hand-written child order with the generated table and
# merged the two codecs' isObject; recounted on the merged tree (88). Hold the ground.
#
# LOWERED 91 -> 90 on 2026-09-15, with the user's approval ("すべて推奨通りにやれ。", JDG-121):
# 0b5aa83b folded the dependency-start fragment the translator and the shell both held into
# ItemHitArea's dependencyStartOfHit (CR-388, DFC-607). Hold the ground.
#
# Runs of at least FLOOR tokens written out in two or more places under src/.
# Held by check-repeated-expressions.py; line 1 is the number and every other
# line is ignored.
#
# THE NUMBER IS A COUNT AT THE GATED FLOOR (20), and a count at one floor says
# nothing about another -- the same tree gives several times as many groups at
# floor 10. The check pins its gate to FLOOR in its own source and does not read
# this file at any other floor.
#
# WHY 20: ten groups drawn at random were read by hand at each floor; 6 of 10
# named one expression genuinely written twice at floor 10, and 9 of 10 at
# floor 20. A gate that is wrong four times in ten teaches people to ignore red.
# WHAT FLOOR 20 DOES NOT SEE: a short expression repeated in a few places, such
# as the FR-093 width estimate (a 10-token expression). See that band by hand
# -- it prints and does not gate:
#     python .claude/skills/spec-graph-check/check-repeated-expressions.py --floor 10
#
# tests/ is ADVISORY and not in this number: a test case states its own
# arrangement so it reads alone. Both counts are printed on every run.
#
# ⛔ A debt, not a permission. Folding a copy LOWERS this file in the same
# commit. Raising it is a decision: say in the commit which copy was added and
# why it could not be folded.
