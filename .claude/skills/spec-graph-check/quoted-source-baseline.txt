505
#
# LOWERED 508 -> 505 ON 2026-09-06, the day this check was written. Five
# quotations in input-command-translator.ts were held against their rows: two
# were verbatim once the （MUST） markers were put back, two were paraphrases
# and now name the row instead of quoting it, and ONE WAS FALSE.
# ⛔⛔ The false one attributed to HF-19 the claim 「読む人の文字サイズが動かす」.
# HF-19 says the opposite in a MUST NOT, and so do LF-3 and S-138. The same
# sentence stood in dom-screen-surface.ts as the stated reason for measuring
# the lattice off the DOM -- the code was right and only the reason was wrong,
# so anyone who "fixed" the code to agree with it would have broken S-138.
# ⭐ That is the second time a paraphrase quoted as a citation hid something.
# The first kept D-318 open for days.
# ⛔ Comments in src/ and tests/ that put a sentence in the specification's
# mouth: a 「…」 quotation, inside a comment, that no manuscript under
# docs/spec contains. `D-339` is why this is counted -- one such sentence
# (「`HF-17`（段 0 へ足す）には親が無いので当たらない」) was quoted as a row of
# 表 T-051 and kept `D-318` open for a round, because the code trusted it.
# ⭐ That one is already corrected in `input-command-translator.ts`; the
# number below is everything the same sweep finds beside it.
#
# ⭐ MEASURED 2026-09-06, FIRST RUN, AND THE FILTERS WERE MEASURED TOO.
#   896  every 「…」 quotation of 10+ characters, wraps joined, elisions split,
#        emphasis and spacing normalized on both sides, each manuscript
#        searched on its own
#   508  after dropping the two shapes that are not citations of these
#        manuscripts at all: a quotation with no CJK in it (the code quoting
#        ITSELF, or an English rule), and a quotation attributed WITHIN 120
#        characters to another book -- the ledger, a PD, a CR, the rule files
#        or the user's own words. Quoting those is correct.
# `--list` prints all 508.
#
# ⛔⛔ THE WINDOW IS 120 CHARACTERS AND NOT THE WHOLE COMMENT, and that was
# found by rehearsal, not by taste. A fabricated citation was planted at the
# top of a test file and the check MISSED it: a block-wide test for the other
# books let one `D-` row named anywhere in a long file header exempt every
# quotation under it. With the window, the plant is caught (508 -> 509) and
# the real count rose 283 -> 508, which is how much the coarse test was hiding.
#
# ⚠️ THIS FLOOR IS NOT ZERO AND NOT EVERY HIT IS ROT. A comment may quote a
# notice the code itself prints, or an older wording of a row. ⭐ But the ones
# read on the first run were real, and two change the meaning:
#   - tests/unit/uf-8-9-history-depth.test.ts quotes 「1 段の大きさは、その段
#     の保存形を UTF-8 で符号化した長さ（バイト）で測ること（MUST）」. The
#     requirement says 「その段の文書を詰めた `GRS JSON`（字下げも改行も持たない
#     形）へ直列化し」 and then 「詰めた形は、保存する形とは別である」. The
#     quotation names exactly the form the requirement excludes.
#   - tests/unit/uf-72-screen-part.test.ts quotes 「掴んでいる行には地を敷く
#     こと（MUST）—— どれを持っているかが読めなくなる。」. The first half is
#     verbatim; the manuscript does not write what follows 「——」 there.
#
# ⭐ LOWER THIS by fixing a quotation -- point at the row (表 T-051 の `HF-17`)
# instead of retyping it, or copy the manuscript's own words. ⛔ Do not lower
# it by narrowing the filters, and do not raise it to absorb a new one.
