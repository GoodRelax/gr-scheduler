605
#
# RAISED 411 -> 605 ON 2026-09-13, deliberately, by CR-375 -- the user's
# ruling, and the user chose to raise this line and commit rather than hold the
# specification back. ⛔ NOT ONE COMMENT CHANGED. The specification changed:
# CR-375 removed from docs/spec every attribution, dated quote and overturned
# paragraph (1,261 occurrences), and 194 comments in src/ and tests/ were
# quoting exactly those words. ⭐ The rules the comments meant are still in the
# specification; what is gone is the history they quoted with it.
# ⇒ PAID BACK BY THE CODE-COMMENT AND TEST CLEANING that follows the
# specification's -- the user ordered src/ and tests/ fixed together, after.
# ⛔ Do not raise this again for anything else before that round lowers it.
#
# LOWERED 412 -> 411 ON 2026-09-13, by the CR that abolished `D-` and `R-`.
# ⛔ NOT A TIDY-UP, AND NOT A CONSEQUENCE OF THE RENAME EITHER. The rename
# exposed an over-broad exemption: `OTHER_BOOKS` listed `D-\d+` with no
# lookbehind, so it matched the TAIL of every id ending in `D-<digits>` --
# `RD-1` and `RD-2` of 表 T-230 among them. Six quotations beside those two
# rows had been excused by that accident, never by a citation, and under them
# were two rows' CELLS welded into one quotation with the column heading
# inside the 「」. A seventh, in uf-47-48-choosers, quoted the display
# dictionary -- which is not in the haystack -- and was green only because
# `D-186` sat at EXACTLY the 120-character attribution boundary; `DFC-186`
# needs 122.
# ⭐ All seven were CORRECTED, not excused: every 「」 still holds a verbatim
# cell, the headings moved outside the quotation, and the dictionary
# quotation got its attribution beside it. 419 -> 411.
# ⚠️ The 412 it replaces had been carrying an unknown number of accidentally
# excused quotations for as long as `D-\d+` was in that regex. These six are
# the ones that surfaced; a sweep for other `...D-<digits>`-tailed row ids in
# src/ and tests/ would say whether more are hiding.
#
# LOWERED 435 -> 412 ON 2026-09-12, stage 2 section 1: three bodies, one
# section each of the three largest files (frame-loop, input-command-
# translator, dom-screen-surface).
# ⭐ THE SHARES RECONCILE EXACTLY: -9, -6, -8 = -23, measured by each body
# against its own file with the rest of the tree held constant. ⛔ The gate's
# own number attributes to nobody while three bodies write.
# ⛔⛔ EVERY ONE OF THE 23 WAS A REAL FABRICATION, not a normalization miss:
# a quotation spliced or trimmed INSIDE its own 「」. Specimens --
#   「タスクはドラッグ必須。」   TC-7 has no 。; a character was added inside the marks.
#   「運ぶ理由は…表 T-233 の行とすること（MUST）」   drops 同要求の, making it a paraphrase.
#   「足す押し」 appears NOWHERE in docs/spec, yet was cited as a 2026-09-03 ruling.
# ⚠️ Two more are absent from the manuscripts entirely and are unfiled:
#   「図形を描く箱を… 表 T-236 の `S-146`（地の色）で抜くこと」 attributed to FR-029
#   「行見出しパネルの最上部の右端」 attributed to HF-10
# ⭐ AND ONE IS THE MANUSCRIPT'S OWN TYPO: a comment wrote 閲覧環境 where
# 01-04-requirements.md:6010 writes 閉覧環境. 閲覧 occurs 29 times there, 閉覧 once.
# The comment had silently corrected the manuscript.
#
#
# LOWERED 467 -> 435 ON 2026-09-12, by stage 0 and stage 1 of the src/ comment
# cleanup: four bodies over six files (svg-renderer, screen-renderer,
# schedule-layout, row-title-panel, properties-panel, schedule-geometry).
# ⭐ NOT ONE QUOTATION WAS TRIMMED. Every one was folded to a pointer or
# deleted whole, which is why the drop is real rather than a re-cut.
# ⛔ THE SHARES WERE MEASURED IN ISOLATION, NOT READ OFF THIS GATE. Three
# bodies wrote in the same window, so the tree-wide number attributes to
# nobody. Each ran the checker's own extractor against its own files:
# 1A -10, 1B -19, 1C -3 = -32, which reconciles exactly with 467 -> 435.
# ⚠️ Bodies that skip that step will each claim the whole drop.
# ⭐⭐ FIVE OF THE FOLDED QUOTATIONS WERE FABRICATIONS, not normalization
# misses -- a paraphrase hardened into a citation, the DFC-339 pattern still
# running. Three sat in properties-panel.ts: FR-006's 「対象は 表 T-016 の
# `対象` の欄が持つ。」 had 「`Task` か `TaskGroup` のいずれかである」 appended
# INSIDE the quote marks -- and the very next line of the manuscript reads
# ⛔ 本文がその欄の値を数え上げてはならない（MUST NOT）, with the same drift
# recorded as measured on 2026-09-06. An eight-line argument stood on it.
# One sat in svg-renderer.ts: 「間隔は 表 T-221の `LF-1` が段階ごとに持つ
# 1 つの値とすること」 attributed to FR-089, which never says it --
# it is LF-1's own wording WITHDRAWN on 2026-09-04.
#
#
# LOWERED 469 -> 467 ON 2026-09-12, mending DFC-483 / DFC-484 / DFC-486. All three
# were comments arguing from a sentence the manuscript had WITHDRAWN -- FR-001
# on 2026-09-02, HF-11 on 2026-08-31, FR-038 on 2026-09-04 -- and this check
# saw none of them. ⛔ IT CANNOT: a withdrawal record keeps the retired words
# in 「」 for ever (裁定 E of the cleanup), so the quotation always resolves.
# list-withdrawn-cited.py is what found them, and it is deliberately not a
# gate. ⇒ The two this check DID hold were the ones that resolved nowhere:
# HF-11's retired wording, quoted as if current, and 「タスク名と行名、および
# 表 T-016 の項目名は…」, which was never the manuscript's sentence in any
# generation -- the withdrawal record holds only the second half of it.
# ⭐ NOT ONE VERBATIM WAS DELETED. Every quotation was re-framed as 「…まで…と
# 定めていた」 and its anchor re-driven at the live seat, which is why check
# 39's hold count did not move (345 either side).
# ⭐ THE SAME ROUND FOLDED A COPY AND THE COUNT DID NOT MOVE: HR-6's copy of
# HR-1a's MUST NOT became a pointer (裁定 F), and the one comment quoting the
# folded sentence was re-cut to HR-1a's wording in the same commit. ⛔ THAT IS
# THE ORDER -- fold and re-anchor together, never fold and measure later.
#
# LOWERED 471 -> 469 ON 2026-09-11, to make the file say what the practice
# already was. Through the whole of phase 2's section 4.1 -- 99 requirements,
# 56 deletions across six bundles -- and the repair round after it, every body
# was handed "the count on entry is 469; if it moves, revert byte for byte".
# They obeyed that number, not this file: bundles 1, 3, 4 and 5 each reverted
# deletions the moment the count reached 470 or 471, never letting it stand.
# ⛔ SO THE TWO OF SLACK GUARDED NOTHING. It was the remainder of the
# 2026-09-10 lowering, and in one day it would have let two false citations
# through: the count rose four times on 2026-09-11 (repairing DFC-451, folding
# DFC-466, and twice inside bodies), and one of those was 471 exactly -- green
# under the old baseline, caught only because the entry measurement was taken.
# ⚠️ THE POPULATION IS WHY THIS MATTERS. Measured the same day: comments in
# src/ and tests/ carry 6,716 quotations of ten characters or more containing
# CJK, of which 469 have no source -- so roughly 6,247 DO resolve to docs/spec
# and any one of them can be orphaned by editing the manuscript. The rest of
# phase 2 (05-07-design.md, 78,428 characters) and the tests/ pass will move
# this check dozens of times.
# ⛔ WHEN A LEGITIMATE DELETION CANNOT AVOID RAISING IT, send the sentence to
# the tests/ pass instead of raising the baseline -- the same handling DFC-456
# was ruled into on the same day.
# ⭐ THE FLOOR IS STILL NOT ZERO, for the reason the 2026-09-08 entries give:
# a comment may honestly quote something that is not the specification.
#
# LOWERED 486 -> 471 ON 2026-09-10, and the shape is the one the entry below
# predicted word for word: the grab-area rulings of 2026-09-09/10 landed, and
# fifteen comments that had quoted the sentences those rulings replaced became
# false citations in one day. ⭐ NOT ONE WAS A FABRICATION WHEN IT WAS WRITTEN.
# ⚠️ Measured, not counted by hand: 486 with docs/spec/output/ copied in (rule
# 04 §6.7 -- a worktree without it reads 3 too high), the fifteen found by
# running this check's own detector against the manuscripts as commit 6442b29
# left them and diffing the two hit sets, and 471 after. The 473 the baseline
# held is 471 plus two the repair wave of 2026-09-08 had already mended
# without lowering the file.
# ⭐ THE FOUR SHAPES THIS ROUND MET, worth naming because none is a typo:
#   1. A TABLE ROW GREW A MIDDLE COLUMN. 表 T-023d's GR-3 / GR-4 / GR-6 each
#      gained 「掴み代は端の外側だけである」 (or 内側), so every comment quoting
#      the row as 「予定の開始点 | 予定バーの左端 | `start` を変える」 stopped
#      being a quotation. Six comments, all re-cut to the new row.
#   2. A SENTENCE WAS GIVEN ROW IDS. FR-009's three forbidden dependencies were
#      one sentence until 2026-09-10; they are now 表 T-018b's DN-1 / DN-2 /
#      DN-3, and the manuscript says so in its own words. Three comments.
#   3. A DATED MEASUREMENT WAS RESTATED IN TODAY'S NUMBERS. A comment had
#      "helpfully" updated 「`GR-4` の `S-90`（端点の左右へ 6px）…」 to today's
#      12px. ⛔ THAT IS A FABRICATION, not an update: the manuscript's sentence
#      is a measurement of 2026-09-08 and keeps the value S-90 carried that
#      day. The same paragraph says the swallowing 「これで構造から消える」.
#   4. TWO ROWS WELDED INTO ONE QUOTATION. `svg-renderer.ts` glossed K-28 and
#      K-29 together as one 実部 / 空部 sentence; a sentence made out of two
#      rows is in neither of them.
# ⚠️ AND ONE THAT IS NOT A SPEC QUOTATION AT ALL. `nfr-002-003` quotes ledger
# row DFC-356, which itself quotes 「`FR-029` を押せなかった」 -- the nested
# quotation had drifted more than the 120 characters this check looks back for
# an attribution, so it read as a citation of docs/spec. The words are the
# ledger's; the fix was to put `DFC-356` next to them again.
# ⛔ THE BEHAVIOUR WAS NOT CHANGED, AND NEITHER WAS docs/. Comment lines only:
# `git diff` shows no changed line that is not a comment, and vitest ran 7434
# passed / 1 skipped either side.
#
# LOWERED 486 -> 473 ON 2026-09-08 across the repair wave. Thirteen more, and
# not one of them was written as a fabrication -- every one was TRUE when it
# was written and the specification moved underneath it. That is the shape to
# expect: the day four rulings land, the comments that quoted the old wording
# become the largest single source of false citations in the tree.
# ⭐ THE CHECK IS WHAT MAKES A RULING SAFE TO APPLY. Without it, a wave that
# rewrites AS-1, T-023d's closing rule, FR-043's exceptions and MK-13 leaves
# behind a dozen comments still arguing from the sentences it replaced, and
# the next reader believes them.
# ⚠️ Two kinds slipped past a naive grep this round and are worth naming:
# a sentence broken across two comment lines, and an ABBREVIATED quotation --
# 「1 日ぶんと `S-180` の小さい方」 for a clause that also says where S-180
# lives. Dropping the half that says where to look is a fabrication too.
#
# LOWERED 493 -> 486 ON 2026-09-08 by a body sent to close nineteen red tests.
# It was not asked to mend citations; it found seven of its own on the way.
# All seven abbreviated the same clause the same way, writing 「1 日ぶんと
# `S-180` の小さい方」 where FR-043 says 「1 日ぶんと `_assets/tbl-settings.md`
# の 表 T-206 の `S-180` の小さい方」. ⚠️ AN ABBREVIATION IS A FABRICATION to
# this check, and rightly: the words it drops are the ones that say where the
# number lives, which is the half a reader needs.
# ⭐ The body measured 496 in its worktree and 489 after; the trunk read 493
# and 486. That constant three is rule 04's 6.7 -- docs/spec/output/ is
# gitignored, so no worktree can resolve the three quotations living there.
#
# LOWERED 501 -> 493 ON 2026-09-08 by finishing the repair the entry below
# began. That entry mended ONE of the places carrying the fabricated 場面
# 「その行は畳まれておらず、隠れている子も無い」. A body sent to REFUTE the
# ledger row found ten more, and four of them a naive grep never sees --
# the sentence is broken across two comment lines, so it must be hunted
# with 「おらず」 and 「隠れている子」 separately. ⛔ Front session measured
# seven and was wrong; the body measured eleven and was right.
# ⭐ Five copies deliberately REMAIN, every one of them quoted in order to
# deny it -- and those five is why this lands at 493 and not lower.
# ⭐ THE BEHAVIOUR WAS STILL NOT CHANGED TO MATCH ANY COMMENT. What did
# change is DFC-385, and it changed the other way: the pressing side now
# reads what the drawing side reads, which is HF-13's own MUST.
#
# LOWERED 502 -> 501 ON 2026-09-08 by repairing a FABRICATED CITATION that was
# load-bearing -- the third time this check has caught one that the code
# reasoned from. A comment in input-command-translator.ts attributed to `RS-30`
# of table T-233 the words 「その行は畳まれておらず、隠れている子も無い」 and then
# argued from them that narrowing `openOneLevel`'s arm would make the telling
# untrue. ⛔ RS-30 says 「直下に、画面へ戻せる子が 1 つも無い」 and the dictionary
# entry says the same; neither mentions being unfolded, so the danger the
# comment described does not exist and the reasoning was void.
# ⭐ THE BEHAVIOUR WAS NOT CHANGED TO MATCH THE COMMENT. Whether a folded
# childless row should still arm that entrance is now ledger row DFC-385, to be
# settled from HF-13's own row -- this file already records a case where the
# code was right and only its stated reason was wrong.
