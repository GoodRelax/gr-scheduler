358
# LOWERED 384 -> 358 on 2026-09-20, with the user's approval (asked in chat: "進めてよい"): comments that
# quoted a sentence docs/spec no longer holds now name the source they really quote (a defect
# row, a change request or a development rule), so 145 quotations left the count.
# LOWERED 425 -> 384 on 2026-09-16, as the raise below planned, under the user's ruling JDG-137 (asked
# in chat: "検査 42 の基準線 ① 384 に下げる"): the display-size wave's implementation rewrote the tests
# whose comments quoted CR-380 .. CR-385's old wording, and more -- 384 is 11 below the 395 before
# the raise.
# RAISED 395 -> 425 on 2026-09-15, with the user's approval (asked in chat: raise 42 for now):
# CR-380 .. CR-385 rewrote 30 sentences that test comments still quote verbatim (the day after
# a start date, the row gap, the reserved marker room). The implementation wave rewrites those
# tests; lower it there.
# LOWERED 479 -> 395 on 2026-09-14: the tests/ comment wave (JDG-77) brought 64 test
# files to JDG-62 and removed quotations with no source in docs/spec.
#
# Check 42 -- 「…」 quotations inside comments of src/ and tests/ that no
# manuscript under docs/spec contains. Held by check-quoted-source.py; line 1
# is the number, and `--list` prints the quotations.
#
# ⛔ A comment may point at a row (表 T-051 の `HF-17`) but may not put words in
# the specification's mouth: a paraphrase quoted as a citation reads as a rule
# the specification does not have.
# ⭐ Correct a quotation -- keep it verbatim, or drop the marks and point -- and
# lower this in the same commit. Never excuse one.
# ⚠️ The standing count includes about 200 comments that quote attributions,
# dated quotes and overturned wording removed from the specification (CR-375).
# The code-comment cleaning pays them back. ⛔ Raise this for nothing else
# before that round lowers it.
