30
#
# LOWERED 35 -> 30 on 2026-09-17 (second session): CR-404 landed JDG-145 and CR-405 .. CR-408 landed
# JDG-155, JDG-157, JDG-158 and JDG-159 (help, resource roster, list confirmations, property panel).
#
# RAISED 15 -> 35 on 2026-09-17 by the user's own edit, after approving it in chat ("上げて先に記録する
# (推奨)"): JDG-145 .. JDG-162, JDG-166 and JDG-167 were recorded before the waves land them.
#
# LOWERED 18 -> 15 on 2026-09-17, as the raise below planned: the spec wave CR-399 .. CR-401 landed
# JDG-132, JDG-133 and JDG-134, whose rows now read 適用済 (code and tests follow in the implementation
# wave). JDG-140 and JDG-141 (the user's answers on the milestone figure and the end grab) landed in
# CR-399 and JDG-142 (check 44 lowered to 88) in the same commit, so they are recorded as 適用済 and
# never counted. Remaining 15: JDG-78, JDG-85, JDG-87, JDG-102, JDG-107, JDG-124, JDG-126 .. JDG-130,
# JDG-135, JDG-136, JDG-138, JDG-139.
#
# RAISED 11 -> 18 on 2026-09-16 (evening), with the user's approval (asked in chat: "検査 43 は 18 に
# 上げてよい"). Seven rulings were recorded before anything lands them: JDG-132 (over a drawn bar a
# dependency line takes only its own drawn stroke -- CR-399), JDG-133 (the row title panel stops at its
# floor while held, and a drag into the floor never raises the stored width -- CR-400), JDG-134 (the
# row axis ceiling is the first zoom, rising from the minimum, at which the tallest row reaches the Row
# Area -- CR-401), JDG-135 (DFC-610 is folded into DFC-628's fix and A/B-measured after it), JDG-136
# (DFC-611's cause is split on f45e9cd7; its fix is judged on the current tip at display scale 50, then
# 100), JDG-138 (drawn-px boundary comparisons go through one float-noise tolerance -- rule 03) and
# JDG-139 (a generated constant is exported only when another file reads it, in three steps). JDG-137
# (check 42 lowered to 384) lands in the same commit and is not counted. Lower it in each landing commit.
#
# LOWERED 13 -> 11 on 2026-09-16 in the landing commit, as the raises below planned: the display-size
# spec wave (CR-390 .. CR-395) landed JDG-122, JDG-123 and JDG-131, whose rows now read 適用済.
# Remaining 11: JDG-78, JDG-85, JDG-87, JDG-102, JDG-107, JDG-124, JDG-126 .. JDG-130. JDG-126
# (DFC-610) and JDG-122/123/131's code and tests follow in the implementation wave; JDG-124 and
# JDG-128 land with the stage 3 gates.
#
# RAISED 12 -> 13 on 2026-09-16, with the user's approval (asked in chat: "基準線を 13 に上げてよい").
# JDG-130 (DFC-614: the Agent API's importDocument asks the human the same replace / merge /
# overlay question as opening a file) is recorded before the spec wave after the display-size wave
# lands it. Lower it in that landing commit.
#
# RAISED 8 -> 12 on 2026-09-16 (second session), with the user's approval (asked in chat: "今記録して
# 12 に上げる"). Four rulings were recorded before anything lands them: JDG-126 (DFC-610 is fixed in
# the display-size change request's implementation wave), JDG-127 (the extra A/B counts under
# JDG-50; DFC-611 is traced before it is fixed), JDG-128 (stage 3 baselines re-measured on the tree
# with the display wave in are installed if every difference is explained by that wave's diff) and
# JDG-129 (the front session may run the DFC-610 A/B and the DFC-611 trace while the user is away).
# Lower it in each landing commit: JDG-122/123/126 with the display-size wave, JDG-124/128 with the
# stage 3 gates, JDG-127/129 when DFC-611's measurement and fix are recorded.
#
# LOWERED 9 -> 8 on 2026-09-16, as planned in the raise below: JDG-125 landed with CR-389
# (FR-071, table T-233's RS-59, UF-48, table T-078's FT-6) and its implementation wave, and its
# row now reads 適用済. Remaining 8: JDG-78, JDG-85, JDG-87, JDG-102, JDG-107, JDG-122 .. JDG-124.
#
# RAISED 6 -> 9 on 2026-09-16, with the user's approval (asked in chat: "検査 43 の基準線を 6 → 9 に
# 上げてよい"). Four rulings were recorded before anything lands them: JDG-122 and JDG-123 (the
# display-size change request and the answers to its nine questions -- next session's spec wave),
# JDG-124 (stage 3's three baselines and four recommendations -- the commit that installs the
# gates) and JDG-125 (the full-screen icon and F11 do nothing -- CR-389 and its fix). The count
# before them was 5 (JDG-78, JDG-85, JDG-87, JDG-102, JDG-107), so the line also absorbs the
# one-row slack the last landing left. Lower it in each landing commit: to 8 when JDG-125 lands.
#
# LOWERED 19 -> 6 on 2026-09-15, with the user's approval (JDG-111): the spec wave CR-380 ..
# CR-385 landed JDG-83 .. JDG-101 and the day's answers; JDG-114 (the pointer-over-target form
# of the provisional dependency line), JDG-78 and JDG-107 stay open.
#
# RAISED 17 -> 19 on 2026-09-15, under the same day's approval: JDG-107 (the user parks E,
# DFC-594, until it can be reproduced) and JDG-108 (the gap is the inter-row rowGap S-12, not a
# Lv gap, DFC-600) were recorded before the spec wave lands them. Lower it in each landing.
#
# RAISED 4 -> 17 on 2026-09-15, with the user's approval (asked in chat: raise and commit).
# The user gave 12 new directives (JDG-83 .. JDG-94: dummies, the dependency line following the
# pointer, vertical zoom, the top row, the status mark, the row grab contrast) and answered five
# questions (JDG-99 .. JDG-102); they are recorded before any change request applies them, per
# JDG-98 (measure, one spec wave, implement -- before stages 4 to 6). JDG-78 stays for DFC-586.
# Lower this in each commit that lands one of them.
#
# RAISED 2 -> 4 on 2026-09-14, deliberately and with the user's approval of
# the baselines, for JDG-80 (IC-41's label, written into display-words.json
# next session with JDG-79's words, DFC-585) and JDG-81 (the help title row
# reads legend, language toggle, close at the far right -- lands in FR-036,
# dom-screen-surface.ts and the order tests next round). Lower in each landing.
#
# RAISED 0 -> 2 on 2026-09-14, deliberately, for JDG-78 and JDG-79. JDG-78 is
# the user's policy on abnormal and semi-normal paths (prevent them, name the
# object by ID and line, auto-fix and say where): the user ruled its care comes
# after the whole refactor, so it lands with DFC-586, not now. JDG-79 is the
# user's choice of the dictionary words outside table T-220: they are written
# into display-words.json next session (DFC-585), except IC-41, which got two
# different choices and goes back to the user. Lower this in each landing commit.
#
# LOWERED 6 -> 0 on 2026-09-14, for JDG-67, JDG-69 and JDG-73 .. JDG-76: the
# spec session applied CR-376 (the actual held as dates -- FR-011, table
# T-245's GO-3 / GO-4, table T-021a's PV-1 .. PV-3, AT-141 and DV-11 in
# erd.json) and CR-377 (the help as a list of keys and icons -- FR-036, table
# T-036's SK-16 / SK-16b / SK-16a / SK-16c, table T-109's IC-102 and figure
# F-019) to docs/spec, and rulings.md's six rows now read 適用済 with their
# landings named. Code and generated files follow in the implementation wave.
#
# RAISED 2 -> 6 on 2026-09-14, deliberately, for JDG-73 .. JDG-76: the user's
# 2026-09-11 adoption of the help rebuild (previous-project-result/
# 15-help-reference-card/README.md) and the three directives given with it
# were recorded in no ruling row, so they were copied into rulings.md this
# day. None has landed: the change request that carries them into FR-036,
# table T-036, table T-109 / figure F-019 and display-words.json is not
# drafted yet. Lower this number in the commit where that CR is applied.
# Remaining besides them: JDG-67 and JDG-69 (see the note below).
#
# LOWERED 3 -> 2 on 2026-09-14, for JDG-71: the ruling landed in 01fc9b0 at
# table T-246's HB-4 (horizontal, the nearest day boundary), with HB-5 and
# HB-6 derived from its reason (docs/spec/01-04-requirements.md), and
# rulings.md's JDG-71 row already reads 適用済 -- but this number was left at
# 3 in that commit, and check 43 counted 2 against it. Remaining at 2: JDG-67
# and JDG-69, held below for their own reasons. JDG-72 (the comment-box
# anchor is read by where it is released, and drawn at the centre of its day
# column and row band) was appended in this same change already landed
# (GR-14's paragraph and FR-019 in 01-04-requirements.md, table T-221's LF-15
# in 05-07-design.md), so it does not count.
#
# RAISED 2 -> 3 on 2026-09-14, deliberately, for JDG-71 (DFC-568's corner
# grab: which day boundary a released corner snaps to horizontally). The
# ruling was answered (nearest boundary) but the DFC-568 row it lands under
# (table T-246, still to be added) is not written yet -- the spec-only body's
# draft sits in a worktree, not in docs/spec. R2's refutation also surfaced
# two design holes this ruling does not settle: (1) reading the vertical
# release point can fall in the row gap outside S-230 and be refused by
# rowAt, and (2) a naive "crossed the opposite edge -> 1-day box" rule makes
# the width jump 1 -> 3 days with no 2-day step. Those two holes are left to
# the next round (defects.md's DFC-568 row). Lower this back to 2 in the
# commit that lands JDG-71 in docs/spec.
#
# LOWERED 3 -> 2 on 2026-09-14, for JDG-70: the user's ruling landed at table
# T-246's HB-3 (docs/spec/01-04-requirements.md) -- the top and bottom of a
# highlight box body drag now shift by the same count of on-screen rows,
# rejecting the drag when there is no row to land on. rulings.md's JDG-70 row
# was updated to 適用済 in the same commit. Remaining at 2: JDG-67 and JDG-69,
# both still held below for their own reasons -- the storage-shape decision
# they need goes to a change request after stage 0, not into this commit.
#
# RAISED 2 -> 3 on 2026-09-14, deliberately, for JDG-70 (DFC-568's highlight
# box body drag: the top and bottom shift by the same count of on-screen
# rows). The ruling was answered but the DFC-568 row it lands under (table
# T-023d's closing GR-14 paragraph) is being written by another body, in
# .claude/worktrees/, at the same time as this commit. Lower this back to 2
# in the commit that lands JDG-70 in docs/spec.
#
# RAISED 0 -> 1 on 2026-09-14, deliberately, for JDG-67 (a task's actual end
# released on a non-working day becomes that non-working day). The ruling
# cannot land yet: the actual is held as actualStart plus a count of working
# days, and that shape has no way to end on a non-working day. The storage
# options went to the front session, not into the repository. Lower this back
# to 0 in the commit that lands JDG-67.
# The other way out -- a 状態 word check 43 does not count -- was refused: it
# would hide exactly the ruling this file exists to count.
#
# RAISED 1 -> 2 on 2026-09-14, deliberately, for JDG-69 (how JDG-67 is held:
# the actual's start is ActualStart, its in-progress edge is Stop, and its
# finish is ActualFinish -- dates are authoritative, ActualDuration is counted
# from them, and a non-working day is a legal value for all three). This is a
# storage-shape decision, not yet a specification change: it needs its own
# change request and a rewrite of the actual-reading clauses, rendering, and
# tests before it lands. Lower this back to 1 in the commit that lands JDG-69.
#
# Check 43 -- rulings in docs/development-records/rulings.md whose 状態 still
# holds 未着地: answered by the user and written nowhere the product is built
# from. Held by check-ruling-landed.py; line 1 is that count.
#
# ⭐ Lowering this is the point of the file: when a ruling lands, change the
# row's 状態 and lower the number in the same commit.
# ⛔ Raising it takes on the most expensive debt this repository has measured
# -- a ruling written nowhere gets asked again. Raise it only deliberately, and
# name the rows in the commit.
#
# HELD LINES -- `HELD JDG-nn <reason>` exempts one 適用済 row whose landing no
# document can show. ⛔ A held line is a debt, not a permission, and it holds in
# BOTH directions: a row that stops being a miss turns this file red, so an
# exemption cannot rot. No row is held.
