50
#
# Check 31 -- rows of defects.md whose cells still read as blocked while the
# row says it is settled. Held by check-stale-blocked.py; line 1 is the number.
#
# A sentence opening 「⚠️ 実測（YYYY-MM-DD）」 is a RECORD, not a present claim;
# the check strikes such sentences (docs/development-rules/04-verification.md
# section 6.6, ledger_quotes.py) before reading. Most standing rows were written
# before that notation, so they still count.
# ⛔ Lower this when a row is corrected; raise it only deliberately, with the
# row and the reason in the commit.
