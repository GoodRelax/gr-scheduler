49
# ⛔ Rows whose 対応方針・決定仕様 cell still contains a still-blocked phrase
# (未定 / 利用者の裁定が要る / 裁定を待つ / 仕様に行が無い) while the SAME row
# also shows the block is over -- a settled phrase in the same cell, a
# ステータス of 試験待ち/実測待ち/実測済, or a 実物確認 that starts ✅.
#
# ⚠️ MEASURED 2026-09-01, FIRST RUN OF THIS CHECK. 180 rows have the full
# 11-cell shape; 68 of them carry a still-blocked phrase somewhere in cell 5,
# and 61 of those 68 also carry an unblocking signal in the same row. Only 7
# rows are genuinely still open. This is the append-only failure the check
# exists to name: an opening 未定/裁定 clause is not deleted when the ruling
# lands, so most rows that were ever blocked still read that way today.
#
# ⭐ The number coming down is the work -- and per the check's own docstring,
# coming down here means REWORDING a row once its status is confirmed decided
# (so the stale opening no longer sits beside the settled text), not deleting
# evidence. A row going up is a NEW row written with a stale opening that
# outlives its own ruling.
#
# ⚠️ SOME OF THESE 61 ARE THE HONEST EXCEPTION THE CHECK CANNOT TELL APART:
# "the first question was ruled, a second one is now open" in the same cell
# reads identically to append-only staleness under a plain substring match.
# D-04 is one of these -- the width question closed at 決着 → S-180, and a
# separate, later opacity question opened its own 仕様に行が無い further down
# the same cell. It stays in this baseline rather than being special-cased.
#
# D-131 is a confirmed true past-positive kept honest by the ledger itself:
# its cell now says outright it was one of "5 行" the 2026-09-01 review found
# reading as blocked, right next to the still-present 仕様に行が無い /
# 利用者の裁定が要る text that makes it match here too.

# ⭐ 2026-09-01 の状態の付け替え —— この検査の数は付け替えでは 1 も動いていない。
# 利用者の裁定で 10 状態が 8 状態になり、`テスト中` と `テスト待ち` は `試験待ち`
# へ、`テスト完了` は `実測待ち` と `実測済` へ移った。この検査が「決定が要る
# はずがない」と見る状態は、その 3 つ（`試験待ち` / `実測待ち` / `実測済`）に
# 名前を替えただけで、**拾う行の集合は同じ 161 行のままである。**
#
# ⛔ 61 -> 62。⚠️ **この +1 は付け替えではない。** 付け替えの前後で測って
# 確かめた —— 直前のコミットの台帳では 61、付け替え前の作業ツリーの台帳では
# すでに 62 だった。増えたのは **D-129** の 1 行で、この巡の先の作業で
# ステータスが試験の段まで進んだ結果、欄の頭に残ったままの
# 「⛔ 未定。⚠️ 図形は 表 T-026 の `RC-13` により利用者の裁定が要る」が
# 追記された決着の文と並んだためである —— この検査がまさに名指すために在る
# 追記型の腐りそのものである。
# ⭐ **借りを取り込んだのであって、直したのではない。** D-129 の欄を書き直せば
# 62 は 61 へ戻る。
#
# 2026-09-01: 62 -> 60. D-128 and D-129 both opened with 「未定。図形は RC-13 に
# より利用者の裁定が要る」 while figure F-019 already held all 89 shapes and the
# table already held all 89 rows -- the ruling was never outstanding. The front
# session had asked the user to rule on shapes that were already drawn, on the
# strength of those two cells. This is exactly the rot this check exists to name,
# and it caught its own author.
#
# 2026-09-02: 60 -> 59. Three cells were rewritten at the HEAD so that a reader
# of the opening is not told the row is blocked when its own end records the
# ruling: D-72, D-135 and D-148. All three had been settled on 2026-08-30 and
# still opened with 未定. ⚠️ The count is now taken across BOTH files of the
# ledger -- defects.md and fixed-defects.md -- because the harvest of this day
# moved 85 finished rows out of the first, and most rotten openings travel with
# a finished row. A check reading one file would have printed a large fall as
# ground won.
