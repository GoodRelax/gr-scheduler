9
# RAISED 6 -> 9 on 2026-09-25 under JDG-565 (the user away: raises allowed with the reason; to confirm on return): CR-568 wave W2 moved the GR-21 scroll wholes into screen-frame.ts (UF-61) with their comments from frame-loop.ts; screen-frame.ts now holds 20 comment lines against 154 code lines (limit 17), so 3 over. All 3 are WHYs moved as-is.
# RAISED 0 -> 6 on 2026-09-25 under JDG-565 (the user away: raises allowed with the reason; to confirm on return): CR-554 wave F2 moved pointer-shape.ts (UF-157) out of frame-loop.ts, where its comments were diluted by the closure. After its restatements of tables T-269/T-266 were cut and its two unguarded TRAPs moved to DFC-973/974, 35 comment lines remain against 264 code lines (limit 29): WHYs the specification does not state, see anchors, the header and the DFC-556 DEVIATION. All 6 are in that one file.
#
# Check 55 -- the comments of src/ against ruling 17
# (docs/review/comment-rules-src.md). Held by check-comment-rules.py; line 1 is
# the number, `--list` prints it file by file and `--file <path>` line by line.
#
# The number is A + B + C + D:
#   A  comment lines holding a character outside printable ASCII
#   B  comment lines in no form section 3 of the ruling allows, or past the
#      line budget of the form they open
#   C  over each file with 100 or more code lines, the comment lines past its
#      10% allowance, floor(code lines / 9)
#   D  the same allowance taken over the whole tree
# A line that breaks several rules is counted once under each.
#
# This is a ratchet. The count rising above line 1 FAILS the run. A commit that
# lowers the count lowers line 1 in that same commit, so the ground cannot be
# given back. Raise it only on purpose, and say why in the commit.
