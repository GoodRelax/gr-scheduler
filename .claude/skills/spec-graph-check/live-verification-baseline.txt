0
#
# Rows of defects.md standing at `実測済` with nothing in 実物確認 -- a row
# claiming it was measured in the shipped build without a measurement. Held by
# check-live-verification.py; the first line that is not a comment is the
# number.
#
# ⛔ A debt, not a target. It may fall freely: lower it in the commit that
# measures a row. It may rise only deliberately, and that commit says which row
# took the debt on and why.
# ⚠️ Count a panel's controls one at a time, not substrings of its whole text:
# a word the panel keeps on purpose elsewhere makes a fixed row read as broken.
