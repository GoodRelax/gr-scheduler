79
#
# LOWERED 80 -> 79 on 2026-09-16: the 80th row was DFC-604 itself. Check 29 also reads
# fixed-defects.md, so reaping a row does not drop it; its decided-spec cell now names
# docs/development-rules/04-verification.md 6.11, where the fix and the measuring trap were
# written down. Measured: 79 rows, DFC-604 no longer listed.
#
# RAISED 79 -> 80 on 2026-09-15, with the user's approval ("すべて推奨通りにやれ。"): DFC-604 is a
# defect of the e2e harness (a cold Vite dev server outlives the 30 s test timeout). No clause of
# docs/spec governs the harness, so the row cannot name one. Lower it when DFC-604 is reaped.
#
# Check 29 -- rows of defects.md at or past `実装待ち` whose 対応方針・決定仕様
# cell names no place in the specification (a chapter, a requirement UID or a
# table row id, or a file). Held by check-decided-spec.py; line 1 is the
# number and every other line is ignored.
#
# These rows were written before the cell was asked to carry the place, so the
# number is old debt. It comes down when a row names where the specification
# says it; lower it in that commit.
# ⛔ Raise it only to take the debt on deliberately, and say which row and why
# in the commit.
