# CR-395 — ヘッダーとパレットは 2/3 の大きさで立つ

> **閉じるもの**: 利用者の変更要求 2026-09-16 `JDG-122`「ヘッダー部分やパレットは現行の2/3にしろ」と、問いへの答え `JDG-123` ①「[-] と [+] の倍率は、ヘッダーとパレットにも掛けるか 掛けない。ヘッダーとパレットは固定で 2/3」—— 逐語は `docs/development-records/rulings.md` の同行。
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `ccdcfabc`。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（全体日程を 1 画面で見られる）—— `FR-051` の RATIONALE が「上部に使った高さはそのまま日程表から引かれる」と述べるとおり、ヘッダーとパレットを小さくしたぶんが日程に返る。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（既存の MUST との衝突）** —— `FR-029`「図形を描く箱の一辺は `S-138` に従うこと（MUST）。載る面によって変えてはならない（MUST NOT）」。ヘッダーとパレットの入口だけを 2/3 にすると、行の操作子・ヘルプ・プロパティパネルの入口と大きさが違い、この MUST NOT を破る ⇒ 決定 2。
- **`R1.3`（書き出しとの一致）** —— 表 T-076 の `EP-1` は `Document Title` の字と左の余白を画面と書き出しが同じ行（`S-225` / `S-226`）から読むと定める。画面だけ 2/3 にすると書き出しと別の絵になる ⇒ 決定 3。
- **`R1.1`（曖昧さ）** —— 「現行」は数で定まっていない値（帯の高さは `FR-051` が環境から確定させる、パレットの大きさは `FR-053` が中身に合わせる）を含む ⇒ 決定 1 で、数で持つ中身の値に掛け、外形は中身に従わせた。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 表 T-206 に `S-235`（0.6667、保存しない）を立て、`App Header` の中身（入口の箱と隙間、`Document Title` の字と左の余白、ファイルの名前と保存した日時の字）と `Command Palette` の中身（入口の箱と隙間、掴み帯の高さ `S-135a`、群を隔てる線のまわりの空き `S-143`）に掛ける（MUST）。表示の倍率（`S-234`）は掛けない（MUST NOT） | `JDG-123` ①。帯の高さは環境から確定させる値のまま（`FR-051`）、パレットの大きさは中身に合わせる（`FR-053`）ので、中身に掛ければ外形も 2/3 に従う。値を書き換える道（`S-138` を 16 → 10.67 など）は、同じ 2/3 が 6 行に散り、片方だけ直される | `S-116`（ヘッダーの高さの上限 56px）はそのまま |
| 決定 2 | 入口の図形の箱（`S-138`）と隙間（`S-141`）には、どの面でも `S-235` を掛ける（`FR-029`） | `FR-029` の「面によって変えない」を保つ唯一の形 | ⚠️ 利用者が名指していない行の操作子・ヘルプの凡例・プロパティパネルの入口も 2/3 になる（下の問い 3）。掴み代の `GR-20` の幅（`S-138`）も 2/3 になる |
| 決定 3 | `EP-1` の `Document Title` の字と左の余白は、画面と書き出しの両方で `S-235` を掛ける | `EP-1` の「同じ行を読む」を保つ | 書き出す絵の題も 2/3 になる |
| 決定 4 | `S-235` と、`CR-394` の `S-236`（表示の倍率 100 のときの比）は、同じ 0.6667 でも別の行とし、兼ねてはならないと両行の注に書いた | 片方だけを選び直すことがある（ヘッダーの大きさと日程の絵の基準は別の要求） | 同じ数が 2 行に載る —— 注が名指し合う |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| ヘッダーの高さは環境から確定させ、設定値として持たない | `FR-051`（`S-116` は上限） |
| パレットの大きさは中身に合わせ、設定値の表に持たない | `FR-053` |
| 入口の図形の箱は面によって変えない | `FR-029`、`S-138` |
| ヘッダーの字は宿主の地の文字に係数を掛ける | `S-210`（0.75）、`S-225`（`Document Title` 16px） |
| 掴み帯の高さ 24px、群の隔ての空き 6px | `S-135a`、`S-143` |

## 2. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| 足した `（MUST）` / `（MUST NOT）` | `FR-051` +2、`FR-053` +1、`FR-029` +1 | 足した文を数えた |
| 大きさを数で持つ `App Header` / `Command Palette` の行（T-206） | 7（`S-138`、`S-141`、`S-135a`、`S-143`、`S-210`、`S-225`、`S-226`） | 表 T-206 を「`App Header`」「パレット」「入口」「ヘッダー」で引いて読んだ |

## 3. グラフ

**`impact.py FR-029 S-138 S-141 FR-053 S-135a S-143 S-116 S-225 S-226 S-210 EP-1`**: 247 行。`S-138` は `FR-029`・`FR-085`（行の名前の幅の式）・表 T-023d の `GR-20`・`JDG-76` の凡例から指される。

**`induced.py`**（`CR-394` と同じ束）: 閉路 `EP-1 ↔ S-225`、`FR-029 ↔ S-138` ⇒ `EP-1` と `S-225` の注、`FR-029` と `S-138` の注を同じ巡で書いた。

**届いた行を `rulings.md` で引いた**: `JDG-76`（凡例の印の大きさは `S-138`）—— 決定 2 で凡例も 2/3 になる。衝突ではないが代償に挙げた。

## 4. 当てた中身

| ID | 置き場 | 中身 |
|---|---|---|
| `FR-051` | `01-04-requirements.md` | 決定 1（`App Header`） |
| `FR-053` | 同 | 決定 1（`Command Palette`） |
| `FR-029` | 同 | 決定 2 |
| 表 T-076 の `EP-1` | 同 | 決定 3 |
| `S-235` | `_source/settings.json`（表 T-206） | 決定 1・4 |
| `S-138` / `S-225` の注 | 同 | 決定 2・3 |

## 5. 数の予測

rows +1（`S-235`）。ほかは `CR-394` の第 5 節。

## 6. 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `App Header` の割り付けと中身（表 T-075 の `UF-61` / `UF-62`、`domScreenSurface` の `appHeader` の CSS） | 入口の箱と隙間、題と名前と日時の字に 0.6667 を掛ける |
| `Command Palette`（`UF-65`） | 入口の箱と隙間、掴み帯、群の隔てに 0.6667 |
| 行の操作子・ヘルプ・プロパティパネルの入口 | 箱と隙間に 0.6667（`FR-029`） |
| 書き出しの題（`ImageExporter` / `svgFromSchedule`） | `S-225` / `S-226` に 0.6667 |

## 7. 試験が主張すべきこと

1. `App Header` の入口の図形の箱が 16 × 0.6667 ＝ 10.67px、`Document Title` の字が 10.67px（`FR-051`）。
2. `Command Palette` の掴み帯の高さが 24 × 0.6667 ＝ 16px（`FR-053`）。
3. `displayScale` を 33 と 100 に変えても、上の 2 つが変わらない（`DS-6`）。
4. 書き出した SVG の `Document Title` の字が、画面の字 × `FR-080` の比（`EP-1`）。

## 9. ⛔ この変更でやらないこと ／ 返した問い

- ⛔ `S-116` を変えない
- ✅ 答えを得た（問い 4、2026-09-16、台帳 `JDG-131`「案 A」）: 決定 2 のまま —— どの面の入口も `S-235` の 2/3 で描く。`FR-029` に例外を足さない
