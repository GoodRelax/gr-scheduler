# CR-295 — 編集を終わらせる押下

> **閉じるもの**: 台帳 `D-131`（日程を押しても、プロパティパネルの入力欄から焦点が抜けない）。
> ⭐ **利用者の裁定（2026-08-30）: 案①「押下で確定する」。**取り消さない。
> ⛔ **台帳が「仕様に行が無い。利用者の裁定が要る」と書いていた** —— 本 CR はその裁定を仕様の行にする。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`（すぐわか / `GL-006`）である。**⛔⛔ **欄の外を押しても何も起きない画面は、「押しても効かない」と読まれる。**
⚠️ **帰結はそれだけではない** —— ⛔ **`isTextEntryUnsettled` が真のままになり、`WS-2` が `Agent API` の書き込みを拒み続け、`IN-5a` が `Delete` を飲み続ける**（台帳 `D-131` の実測）。⇒ **入力欄 1 つが、道具の 2 つの機構を止めている。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 確定・取消の道 | ⛔ **仕様が名指すのは `Esc`（表 T-028 の `IN-4`）と `Enter`（表 T-036 の `SK-19`）の 2 つだけで、「欄の外を押したとき」を定める行は 1 つも無い**（実測）|
| **`R3.4`**（同じものを 2 か所に置かない）| 置き場 | ⭐ **表 T-023a には置けない** —— 同表は自分で「**判定順序を適用するのは日程の描画領域だけとすること（MUST）**」と書いており、押下はパネルの上にも空白にも落ちる。⭐ **表 T-036 にも置けない** —— あれは鍵の割当表であり、ポインタの押下は行を持てない。⇒ **表 T-028（入力の作法）が唯一の置き場である**（`IN-4` と `IN-5a` の隣）|
| **`R2.7`**（同じ規則を 2 か所で判定しない）| 確定の作り手 | ⭐ **`fieldCommitOf` が既に「確定した値」を 1 か所で作っている**（`Enter` と宿主の `change` が同じものを通る）。**押下も同じものを通す** |
| **`R2.19`**（驚かせない）| 打った文字 | ⭐ **取り消さない**（裁定）—— ⛔ **取り消すと、打った内容が押下 1 つで消える** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **押下そのものは、いつもどおり働く** | ⛔ **押下を飲み込む規則を書くと、バーを選ぶつもりの押下が選ばなくなる。**⭐ **表 T-023a も 表 T-028 の `IN-1` も、押下を飲む行を 1 つも持たない** —— 確定は押下に**乗る**のであって、押下を**置き換えない** |
| 2 | ⭐ **打った値が始めた値と同じときは、何も書かない** | ⛔ **同じ値を書くと取り消しが 1 段積む**（`FR-031` / `UN-3`）。⚠️ **`Enter` の側が既に同じ守りを持っている** |
| 3 | ⚠️ **同じパネルの別の欄を押したときは、焦点をこちらから外さない** | ⛔⛔ **その押下も割当を持つので、宿主の焦点移動は止められている** —— ここで焦点を外すと、**どこにも焦点の無い状態**になる。⭐ **確定だけは行う。**⚠️ **裁定はここまで定めていない**ので `@provisional PD-352` を付け、`pending-decisions.md` に行を足した |

---

## 1. ⛔ 測った

| 測ったこと | 実測 |
|---|---|
| ⛔ 欄に焦点があるとき日程を押したときの `document.activeElement` | ⛔⛔ **`INPUT/text` のまま**（台帳 `D-131` の実測）|
| 理由 | ⭐ **その押下は割当を持つので `isBrowserDefaultStopped` が立ち、`preventDefault` が焦点を欄に留める** |
| 表 T-028 の行 | **8**（`IN-1` `IN-1a` `IN-2` `IN-3` `IN-4` `IN-4a` `IN-5` `IN-5a`。⇒ 新設は **`IN-6`**）|
| 確定を書く者 | ⭐ **`fieldCommitOf`（`dom-screen-surface.ts`）。`Enter` と宿主の `change` の両方が通る 1 か所** |
| 押下の届き方 | ⭐ **`DomInputSource` は窓の泡立ちで聴く** ⇒ **窓より内側に掛けた聴き手のほうが先に走る**（`Enter` の聴き手が既に同じ順に乗っている）。⚠️ **どこへ掛けられるかは 5. の実測が決めた** |
| `induced.py T-028 IN-4 SK-19 T-023a FR-006 T-016` | ⭐ **閉路 0**（辺は 1）|

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `01-04-requirements.md` の 表 T-028 | ⭐ **`IN-6` を 1 行** —— **確定していないその場の編集があるとき、その欄の外でポインタを押したら、その編集を確定すること（MUST）。取り消してはならない（MUST NOT）。押下そのものの働きを止めてはならない（MUST NOT）** |
| 2 | `A-appendix.md` | 変更履歴に 1 行（版 1.48）|

⛔ **`IN-4` も `SK-19` も動かさない** —— どちらも自分の引き金（`Esc` / `Enter`）の行であり、押下は third の引き金である。

## 3. 数の予測

```
tables   135 → 135
figures   11 →  11
rows    1715 → 1716   （表 T-028 に 1 行。CR-294 のあとの数から）
uids     150 →  150
アイコン  77 →   77
語       351 →  351
```

## 4. 落ちる／緑になる試験

| 試験 | 予測 |
|---|---|
| 既存の 4670 件 | ⭐ **1 件も赤くしない**（押下の働きを止めないため）|
| `IN-6` そのものの試験 | ⭐ **本体は書かない。仕様だけを読む体に書かせること**（規則 05 の 7.）|
| 実物 | ⭐ **欄に打つ → 日程を押す → 画面の文字が変わり、焦点が欄から抜ける**ことを数で示す |

---

## 5. ⛔ 実装で 1 つ増えた裁定（**覆してよい**）

⛔⛔ **聴き手を置ける場所は 1 つしか無かった** —— 実物で測って分かった。
⭐ **実測（出荷ビルド）**: `body` の子は **2 つ**（`div[Schedule Canvas]` と、この体が描く mount）である。
日程のバーを押したときの経路は **`polygon < svg < div[Schedule Canvas] < body`** であり、
⛔ **`DomScreenSurface` が作った根はその経路に 1 度も現れない**（`IF-1` が絵を丸ごと別の面として上げるため）。
⇒ **根に掛けた聴き手は、裁定が名指したまさにその押下だけを聞き逃す。**

| 決めたこと | 何に載せたか |
|---|---|
| ⭐ **聴き手を宿主（`Document`）に 1 つだけ掛ける** | ⛔ **`ScreenSurfaceWiring` は「宿主に呼ぶのは要素を作る 2 つと `elementFromPoint` だけ」と書いていた** —— 4 つ目として `addEventListener` を足し、**理由（`IN-6` は他に果たす道が無い）を同じ場所に書いた。**⭐ **`FT-1` は動かない** —— この聴き手は殻に何も報せず、フレームも起こさない。確定した値は `readFieldCommit` で出ていき、殻はもともとそれを読む。⚠️ **`readScreenPartAt` に載せる道は退けた** —— あれは**ホバーでも呼ばれる**ので、ポインタが画面を横切るたびに編集が終わる |

⚠️ **この 1 点は `uf-71` の 3 つの断定を動かした**（宿主の員数 2 つ → 3 つ、聴き手の置き場）。
⭐ **断定は緩めていない** —— 員数は閉じたままで、**`addEventListener` に触れた回数を 1 に固定する行を足した**（2 つ目の聴き手は 2 回になって落ちる）。

## 6. ⭐ 実物の実測（出荷ビルド、実ポインタ）

| | 前 | 後 |
|---|---|---|
| `document.activeElement` | `INPUT/text` | ⭐ **`BODY`**（焦点が欄から抜けた）|
| 画面の `PressSettled` | **0** | ⭐ **1** |
| 画面の `Survey phase` | **1** | ⭐ **0** |
| `Delete` を押したときの図形 | ⛔ **1753 → 1753**（`IN-5a` が飲んでいた）| ⭐ **1753 → 1748**（選んだバーが消えた）|

⭐ **手順**: バーを押す → `P` でパネル → `PR-1` の欄を選んで `PressSettled` と打つ → ⭐ **`Enter` を押さずに日程を押す。**
⛔ **数は 4 つとも同じ 1 回の走行のものである。**
