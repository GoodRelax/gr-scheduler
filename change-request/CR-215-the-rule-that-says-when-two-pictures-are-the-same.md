# CR-215 — 2 枚の絵が「同じ」だと言える規則

> **閉じるもの**: ⛔ **`FR-080` は「正規化と丸めの規則そのものは Chapter 6.1 が持つ」と委任したのに、Chapter 6.1 にその規則が無い。**
> ⭐ **利用者の裁定（2026-08-23）で 3 つとも決まった** —— 丸めの格子と許容差（`PD-161a` ＋ `PD-162`）、SVG の正規化（`PD-161b`）、`WY-2` の「同じ」の相手。
> ⭐ **表 T-231 を新設し、`SWS-7` を置く。** ⛔ **`FR-080` は 1 文字も動かない** —— 同要求は既に 6.1 へ委任している。

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`。受ける目標は `GL-001` / `GL-002`。**

⚠️ **初稿は `GL-004` と書いていた。誤りである** —— 表 T-054 で `CH-2` が受けるのは `GL-001` と `GL-002` である。⛔ **`tools/precheck.py` が最初の実走で掘り当てた**（人も検査 22 も見落としていた）。⭐ **決定は 1 つも変わらない** —— 直したのは指し先の誤記である。

⛔ **`WY-3` の合否が、存在しない規則に乗っていた。** ⚠️ **`PD-161` が在るかぎり波 W4 は閉じられない**（規則 06 の 5.：`D`〜`H` の未裁定が 1 件でもあれば閉じられない）。
⭐ **本 CR はその最後の 1 件を閉じる。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.2`**（曖昧・非検証な表現の排除）| `WY-3` の「丸めによる 1 px 未満の差」 | ⛔ **境が書かれていなかった。** 「1 px 未満」は大きさを言うが、**どこで丸めてから引き算するのかを言っていない。** ⭐ 両側を同じ格子へ丸めてから各辺で比べる、と手続きを書いて検証可能にした |
| **`R1.3`**（唯一の正がある）| `WY-2` の相手 | ⛔ **`CR-202` §7 が自分で「開いたまま」と挙げていた。** ⚠️ **「2 回の書き出し同士」と「版元の見本」では、浮動小数の再現性が判定に入るかどうかが変わる。** ⭐ 前者に定めた |
| **`R2.7`**（DRY）| 表 T-228 との関係 | ⛔ **`SWS-6` を広げてはならない** —— あちらの親は `FR-021`（MSPDI の往復）であり、`NR-4` / `NR-5` は交換相手のスキーマに固有である。⭐ **別表にした** |
| **`R2.9`**（YAGNI）| 設定値の行を立てるか | ⭐ **立てない。** ⚠️ **`CR-202` の `R4` が同じ判断をしている。** 格子も許容差も**人が選ぶ値ではなく、比べ方の規則**である |
| **`R5.1`**（計算量）| 正規化の段取り | ⭐ **書き出しの経路には入らない。** 比べるときだけ走る（`WY-2` / `WY-3` の判定側） |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **表を 1 つにまとめた**（正規化 4 行 ＋ 判定 2 行）| ⚠️ **表 T-228 は正規化だけを持ち、判定は `SWS-6` の STATEMENT が持つ。** ⛔ **こちらは判定に値（格子と許容差）が要る** —— 規則 02 の 4「値は表、規則は要求」に従い、値を表へ置いた |
| 2 | ⭐ **行 ID の接頭辞を `NS-` にした** | `docs/spec` に既存の `NS-` は 0 件（自分で数えた）。⚠️ `NR-` は 表 T-228 が使っている |
| 3 | ⭐ **`id` の規則を「定数に限る」とした** | ⛔ **いまの実装は `svg-renderer.ts` が `id` を 1 つも出さず、`image-exporter.ts` が定数 1 つだけを出す**（自分で確かめた）。⭐ **規則を書かないと、後から生成 `id` を入れた者が黙って `WY-2` を落とせる** |
| 4 | ⭐ **`WY-3` の除外句を残した** | ⚠️ **`CR-148:135` が「6.1 が丸め式を書いた時点で、この除外が許容差として残るのか不要になるのかを判定し直すこと」と指示していた。** ⭐ **残す** —— 許容差そのものだからである |
| 5 | ⭐ **`05-07-design.md:892` の `> 未記入。` を掃いた** | ⛔ **`SWS-1`〜`SWS-6` が 6 本入った時点で偽になっている。** 規則 03 の 3「偽になった注記は欠陥」 |

---

## 1. 測った（⛔ 2 本とも走らせた）

```
impact.py  FR-080
induced.py IF-7 CM-71 FR-032 FR-080 WY-2 WY-3 T-041 T-228 CN-8 NFR-009 T-109
```

| 測ったこと | 実測 |
|---|---|
| 種が仕様に解決したか | **11 / 11** |
| 種の間の辺 | **4** |
| **閉路** | ⭐ **0** —— 1 つずつ書いてよい（規則 02 の 3.）|
| `FR-080` を指している要求 | **2 件**（`FR-077` / `FR-025`）／ 参照 19 箇所 |
| `FR-080` が指している表 | 8 表（`T-204` `T-076` `T-041` `T-025` `T-206` `T-201` `T-202` `T-029`）|

⭐ **`FR-080` を 1 文字も動かさないので、上の 2 件と 19 箇所はどれも動かない。**

---

## 2. ⛔ 何が無かったか（**逐語で示す**）

| どこ | 逐語 | 何が無いか |
|---|---|---|
| `FR-080`（`01-04-requirements.md:3096`）| 「⚠️ **正規化と丸めの規則そのものは Chapter 6.1 が持つ** —— 同じ規則を両側へ当てることまでが本要求の射程である」 | ⛔ **委任先に規則が無い** |
| `WY-3`（`:3106`）| 判定に含めないもの：「**丸めによる 1 px 未満の差** —— 写し取った側は像を縮め、書き出した側は縮めた寸法で描き直すので、端数の処理が一致しない」 | ⛔ **大きさは在るが、どこで丸めてから引き算するのかが無い** |
| `WY-2`（`:3105`）| 「同じ JSON を書き出しの基準環境で読み込み、全体表示（`FR-055`）を行ってから書き出したとき、透かしの層を除いた描画が正規化後に同じ SVG / PNG になること」 | ⛔ **「同じ」の相手が誰かを言っていない** |
| Chapter 6.1 | 表 T-220（不変条件）／ T-221（算式）／ T-222（依存線）／ **T-228（MSPDI の正規化）**／ `SWS-1`〜`SWS-6` | ⛔ **SVG の正規化だけが無い。** ⚠️ **表 T-228 は自分で「本表が持つのは規格に足す分だけである」と XML に限定している** |

---

## 3. 編集計画（**オブジェクトごとに 1 回**）

| # | 対象 | すること |
|---|:-:|---|
| 1 | **表 T-231**（新設）| `NS-1`〜`NS-6` を置く。前書きに、表 T-228 と同じ「規格に足す分だけを持つ」の縛りを書く |
| 2 | **`SWS-7`**（新設）| 親は `FR-080`。STATEMENT は 表 T-231 を指す。⛔ **1 行規則を並べない**（表を指す）|
| 3 | `05-07-design.md:892` | `> 未記入。` を落とす（`SWS-7` が入るので位置も動く）|

⛔ **表 T-041 は 1 行も動かさない。** ⭐ **`WY-2` と `WY-3` の欄が言い足りない分は、`NS-5` / `NS-6` が持つ** —— 同じことを 2 か所に書かない（1.9）。

---

## 4. 数の予測

| | 前 | 後 | 差 |
|---|--:|--:|--:|
| `tables` | 130 | **131** | +1 |
| `rows` | 1596 | **1602** | +6 |
| `uids` | 146 | **147** | +1 |
| `figures` | 11 | **11** | 0 |

⚠️ **重複検出**: `NS-1`〜`NS-4` は 表 T-228 の `NR-1`〜`NR-3` と**似た形の文**になる。⭐ **群が 1 つ増える見込み**（`A=27` → `A=28`）。⛔ **増えたら、それが兄弟表であることを記録に書く。**

---

## 5. 置く中身

### 表 T-231 — 書き出した絵を比べる前の正規化と、一致の判定

| 行 ID | 段取り | 規則 |
| --- | --- | --- |
| NS-1 | 規格を当てる | **W3C Canonical XML 1.1**（`https://www.w3.org/TR/2008/REC-xml-c14n11-20080502/`）を当て、**注釈を含めない形**を用いる。⚠️ **短縮名の URL は版の指定にならない** |
| NS-2 | 要素の間の空白を落とす | `NS-1` の**前に**、要素の子を持つ要素の直下にある、空白だけの文字ノードを落とす。⛔ **葉の要素の中の空白は落とさない** —— そちらは値である |
| NS-3 | 数の綴りを揃える | `NS-1` の**前に**、座標と寸法を **0.01 px の格子**へ丸めた綴りにする。**双方に当てること（MUST）。片側だけに当ててはならない（MUST NOT）** |
| NS-4 | `id` を定数に限る | 書き出す `SVG` の `id` は、**成果物の中で不変の定数だけとすること（MUST）。走るたびに変わる値から作ってはならない（MUST NOT）** —— 変えると `NS-1` の出力が毎回変わり、`WY-2` が決して通らない |
| NS-5 | `WY-2` の相手 | **同じ走りの中で 2 回書き出したもの同士**とする。⛔ **版元に置いた見本と比べてはならない（MUST NOT）** —— `WY-2` が測るのは書き出しの経路が決定的かどうかであり、機と版をまたぐ浮動小数の再現性は含めない |
| NS-6 | `WY-3` の許容差 | **双方を `NS-3` の格子へ丸めたうえで、外接矩形の各辺の差の絶対値が `1.0` px 未満なら一致とする。** ⚠️ **端数が必ず出る理由は `WY-3` の欄が持つ** |

### `SWS-7` — 書き出した絵を比べる前の正規化

**Type**: SW_SPEC
**UID**: SWS-7

**STATEMENT**: 2 つの `SVG` を比べるとき、`GRS` は、表 T-231 に従って双方を正規化してから比べること。

**RATIONALE**: `FR-080` が「正規化と丸めの規則そのものは Chapter 6.1 が持つ」と委任した先である。⚠️ **規格だけでは足りない** —— **W3C の Canonical XML は要素の間の空白を保存し、小数の桁を揃えない。** 表 T-231 が持つのは、その足りない分と、`WY-2` / `WY-3` が「同じ」と言える境である。**正は W3C の公式文書であり、事実はローカル複製**（`docs/reference/w3c/`）**で確かめる。** ⚠️ **本仕様書に原文を写さない**（表 T-003 の `CN-7`）。**突き合わせの記録は `docs/review/c14n-vs-fr021-2026-08-22.md`、裁定の記録は `docs/review/rulings-2026-08-23/RULINGS.md` が持つ。**

**Relations**:

- **Type**: `Parent`
  **ID**: `FR-080`
  **Role**: `Satisfies`

---

## 6. ⛔ 本 CR が閉じないもの（**黙って閉じたと読まれないために**）

| 件 | 状態 |
|---|---|
| **PNG の側の許容差** | ⭐ **本 CR の射程外。** `FR-080` の `:3098` が「復号したあとの画素の並びで判定する（MUST）」と定めるところまでは在り、画素の許容差は `WY-2` の相手が定まったこと（`NS-5`）で**同じ走りの 2 回になるため、いま要らない** |
| **`PD-162`** | ⭐ **本 CR が `NS-3` として吸収した。** 一覧の行は `裁定済` にする |
| **`FR-021`（XML）の側** | ⛔ **射程外。** 表 T-228 と `SWS-6` が既に持つ |
