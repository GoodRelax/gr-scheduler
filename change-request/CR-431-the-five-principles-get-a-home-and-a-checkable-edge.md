# CR-431 — 7 つの原則に住所を与え、96 行との対応を両向きで機械に読ませる

> ⭐ **状態: 当てた（2026-09-21）。**  
>  起草は 2026-09-20 で、そのときは原則が 5 つであった。  
> 利用者の裁定「**7 つにする**」（2026-09-21、逐語。  
> `JDG-279`）で **7 つ**に確定し、その形で仕様へ当てた。  
> ⭐ 当てた体が本書の文から変えた所は **13 節**にすべて並べてある。
> 読んだ木: `0044b08f`（ブランチ `refactor`）。  
> **行番号はすべてこの木のものである。**
>
> **閉じるもの**: `CR-430` の 4.7 節に置いた原則が、`change-request/` の中にしか住んでいないこと。
> **⛔ 行は 1 つも退役させず、併合せず、振り直さない** —— 利用者の裁定（2026-09-20、逐語）
> 「**=== / ---> / ◆ はすべて規則が異なる。 疎結合にするべき。**」に従い、9 つの表は **96 行のまま**である。
> 原則の置き場についての同じ日の裁定は「**仕様に書き、行と結ぶ**」であり、
> 原則の**数**についての裁定は 2026-09-21 の「**7 つにする**」（`JDG-279`）である。
>
> **正本**: `change-request/CR-430-grab-margins-hit-order-pointers-and-labels.md` の 4.7 節（5 つの原則の文）と 4.5 節（表と上位要求の追跡）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`**（描いた形を直接掴んで動かす）と **`GL-006`**（マニュアルを読まずに主要な操作へ到達する）である。  
9 つの表の 96 行は、その 2 つの目標を縮小と重なりの中で成り立たせるための値であり、**なぜその値なのかを言う文が仕様の外にある**あいだは、行を 1 つ直すたびに理由を読み直す先が無い。

⚠️ **ただし正直に言うと、本書の大半は仕様書そのものの手入れである** ——利用者が画面で気づく変化は 0 である。  
`CH-` を 1 つも前へ進めない部分があることを、隠さずに書く。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。  
唯一の正がある）** —— ① 5 つの原則の正が `change-request/` にあり、
  `docs/spec/` にも `docs/development-records/rulings.md` にも無い（測った。  
  1 節の (a)(b)）。
  ② 語「原則」は既に仕様の中で**別の意味**を持つ —— `01-04-requirements.md:545`・`:546`・`:550` の
  「作り方の原則」（`previous-project-result/user-order.md` の項 65 〜 68。  
  **製品ではなく進め方**の原則）である。
  ⇒ 本書は新しい語を「**直接操作の原則**」とし、⛔ ただの「原則」とは呼ばない。
- **`R2`（命名。  
名前を足すときは必ず引く）** —— 接頭辞 `PP` を 1 つ足す。
  ⭐ 「1 つの接頭辞に 1 つの意味」に従い、既存 157 件と衝突しないことを測った（1 節の (g)）。
- **`R1.6`（網羅）** —— 5 つの原則が 96 行のうち何行を覆うかは、いま**誰にも数えられない** ——
  覆うことの定義が書かれていないからである。  
  本書はその定義を表の形で置き、数えられるようにする。

### ③ 利用者に問わずに決めたこと

⭐ 下の 4 つは、根拠を添えて**利用者に問わずに決めた**（あとから覆せる）。

1. **置き場を新しい節 1.10 にした**（3 節）—— 7 つのうち 6 つが別々の要求を名指すので、
   どれか 1 つの要求の下に置くと、その要求が持たない規則の正になる（`R1.3`）。
2. **行 ID の接頭辞を `PP` にした** —— `P`・`PS`・`PL`・`PR`・`PK`・`PE` は登録済みで、`PP` は空いている。
3. **裁定の番号（`JDG-nnn`）を仕様の表に書かない** —— 検査 7 が「定義の無い行 ID」として赤にする
   （`CR-430` の 12 節の 6 が実際に踏んだ）。  
   束ねる裁定は `rulings.md` の着地の欄が持つ。
4. **9 つの表に列を足さない**（4 節）—— 代償と数を添えて比べた。

---

## 1. 読んで確かめた事実（`0044b08f` で、引き継いだ主張を 1 つずつ測り直した）

⛔ **下の表は、前に立つ者から渡された主張と、本書が自分で測った数の突き合わせである。**  
⚠️ **3 つは成り立たず、1 つは合計だけが合って中身が違った。**

| # | 渡された主張 | 測った結果 | 判定 |
|---|---|--:|---|
| (a) | `grep -c 原則 docs/development-records/rulings.md` は 0 | **0**（終了コード 1） | ⭐ **成り立つ** |
| (b) | 語「被らせない」はリポジトリに 1 か所しか無い | `change-request/CR-430-…md:296` の **1 件のみ**。<br>`docs/` 全体で **0 件** | ⭐ **成り立つ** |
| (c) | 5 つの原則は仕様に**要求の題として 4 つ**残る | **題は 3 つ**である ——「どれも指せる」（`FR-104` の題、逐語）・「実績は事実」（`FR-107` の題、逐語）・「見えないものは掴めない」（`FR-108` の題「見えないものは、掴めず動かない」、言い換え）。<br>⭐ 4 つめの「ダミー」は**題ではなく `FR-043` の RATIONALE** に逐語で在る（`01-04-requirements.md:2707`「ダミーに独自の規則を持たせないことで、規則の数が減る」）。<br>5 つめ「被らせない」は名としてはどこにも無い | ⛔ **成り立たない**（4 → 3 ＋ 1） |
| (d) | 9 つの表は 96 行 | **96**（GA 22 ＋ HT 4 ＋ TY 9 ＋ PK 9 ＋ PE 14 ＋ XS 14 ＋ RF 3 ＋ LP 8 ＋ ZO 13） | ⭐ **成り立つ** |
| (e) | 5 つのどれも覆わない行が **22**（PK 9 ＋ ZO 8 ＋ PE 4 ＋ `PE-0`） | **合計 22 は出たが、内訳が違う。<br>** `CR-430` の 4.5 節の追跡表を使って**表ごと**に読むと、原則を持つ要求から届かないのは **PK 9 ＋ ZO 13 ＝ 22** である。<br>**行ごと**に読むと、「被らせない」が `ZO-5`（名称ラベル・札は線より手前）を覆うので ZO は 12 になり、代わりに `PE-0`・`PE-11`・`PE-12`・`PE-13` の 4 行が加わって **25** になる。<br>⛔ 「ZO 13 行のうち 8 行だけが外れる」という割り方は、どう読んでも再現できなかった | ⚠️ **合計は合い、中身は合わない** |
| (f) | その 22 行にも、宿主の要求の RATIONALE に書かれた理由が在る | **PK と ZO については成り立つ** —— `FR-106` の RATIONALE（`:3572` 〜 `:3575`）と `FR-110` の RATIONALE（`:3850` 〜 `:3852`）。<br>⚠️ ただし行ごとに読んだときの `PE-11`・`PE-12`・`PE-13` の理由は、宿主 `FR-107` の RATIONALE には無く、`FR-075`・`FR-049`・`FR-044` にある | ⚠️ **半分成り立つ** |
| (g) | 接頭辞は 157 件 | **157**（`docs/spec/_source/row-id-prefixes.json`）。<br>`PP` は空き | ⭐ **成り立つ** |
| (h) | `CR-430` の 5 節の `rows` 予測 2161 に対し、実測は 2162 で、差の 1 行は `JDG-277` の ① が足した行 | **実測 `tables=168 figures=17 rows=2162 uids=160`**。<br>差の 1 行は `FR-103` の 表 T-245 の **`GO-11`**（`01-04-requirements.md:3392`。<br>マイルストーンの予定を横に引いて離したときに置く値）。<br>`rulings.md:391` の `JDG-277` が「①②③④とも推奨どおりで進めろ」と裁き、着地の欄が 表 T-245 の `GO-11`（新）を名指す。<br>`CR-430` の 5 節は 表 T-245 を 1 度も数えていない | ⭐ **成り立つ。<br>⛔ 数え落としではない** |

### ⛔ 「覆う条項が無い」と書く前に打った 3 つ（規則どおり）

- `grep -n "被らせない" docs/development-records/rulings.md` ⇒ **0 件**。
- `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py LP-1` ⇒
  属する表 T-273。  
  **指している要求 0 件**、参照は `_assets/tbl-settings.md:106` の 1 件のみ。
- **ほかの道で同じものに届くか** ⇒ ⭐ **届く。  
⛔ だから「条項が無い」とは書かない。**
  条項は在る —— `01-04-requirements.md:3711`（`FR-109` の STATEMENT、逐語）:
  「`GRS` は、1 つのタスク・マイルストーンについて、名称・担当者・進捗・進捗マーカー・再開アイコンを互いに重ねず、どれも形に隠れて読めなくならない場所に置くこと（MUST）。」
  ⇒ **「被らせない」に欠けているのは条項ではなく、名と、行との結び目である。**

同じ 3 手を `FR-108` にも打った ——`impact.py FR-108` は「指している先: 表 `T-005a` 表 `T-272` 表 `T-270` 表 `T-023a`、**行: なし**」と答える。  
⇒ **`FR-108` は自分の表を 1 つも持たず、4 つの他所の表を指すだけの要求である**（6 節）。

---

## 2. グラフ（測った先）

- `impact.py PK-1` ⇒ 属する表 T-269。  
指しているのは `FR-104` の 4 か所（表 T-266 のポインタ欄）と `tbl-settings.md:417`。
  ⇒ **PK の行は `FR-106` の下に立ちながら、名指してくるのは `FR-104` である。**
- `impact.py ZO-5` ⇒ 属する表 T-020。  
**指している箇所が 0 件**（「行が浮いている可能性がある」と道具が言う）。
  ⇒ ZO の 13 行は、宿主 `FR-110` 以外のどこからも名指されていない。
- ⭐ **本書が足すのは表 1 つと行 7 つだけで、閉路には入らない** —— 新しい表は 96 行を**指す**側であり、
  96 行の側は本書では 1 文字も変えない（4 節の推奨を採る場合）。

---

## 3. 決定 1 —— 原則をどこに置くか

### 比べた 4 つ

| 案 | 置き場 | 機械に読ませられるか | 代償 |
|---|---|---|---|
| A | `FR-104` 〜 `FR-110` の RATIONALE に文で書く | ⛔ **無理** —— 散文には行 ID が無く、検査 7・8 が見る形にならない | 5 つが 7 か所に散り、`R1.3` の「唯一の正」を自分で破る |
| B | 1.9（表記規約）の中の 1 節 | ⚠️ できるが**主題が違う** —— 1.9 は語と記号の書き方の規約であり、製品の振る舞いの理由ではない | 1.9 を読む者が探しに来ない |
| C | `FR-104` の下に表として置く | ⭐ できる | ⛔ 7 つのうち 6 つは `FR-106`・`FR-107`・`FR-108`・`FR-109`・`FR-110`・`FR-043` の規則の理由である ⇒ `FR-104` がそれらの正になってしまう（`R1.3`） |
| **D**（⭐ **推奨**） | **新しい節 `### 1.10 Direct Manipulation Principles (直接操作の原則)` に 表 T-274 を置く** | ⭐ できる（下の「検査が読むもの」） | 章 1 に節が 1 つ増える |

### ⭐ なぜ D か

- **Chapter 1 には既に同じ形のものが 3 つある** —— 表 T-002（`SO-*` 範囲）・表 T-003（`CN-*` 制約）・
  表 T-004（`LM-*` 制限）。  
  どれも**どの要求にも属さず、多くの要求が答える**横断の行である。
  直接操作の原則はまったく同じ形をしている。
- **節は SECTION 型であり UID を持たない** —— 測った: 1.4・1.5・1.6 のどれにも UID の行が無い。
  ⇒ **`uids` は動かない**（8 節）。
- 差し込む所は `01-04-requirements.md:392` の次、`:394` の `## Chapter 2.` の前である。  
**1 か所の挿入で済む。**
- ⛔ **節の題を「原則」だけにしない** —— `:545`・`:546`・`:550` の「作り方の原則」と同じ語になり、
  索引と `grep` が 2 つの意味を混ぜる（`R1.3`）。  
  **「直接操作の原則」と 6 文字で書く。**

### 置く表（中身は `CR-430` の 4.7 節の「仕様に書く理由」の欄をそのまま取る）

**表 T-274 — 直接操作の原則**（`PP-1` 〜 `PP-7`、接頭辞 `PP`）

| 行 ID | 原則 | 仕様が言う理由 | 覆う行 |
|---|---|---|---|
| `PP-1` | どれも指せる | どの形も、ズームのどの段でも、それだけが応える領域を持つ。<br>縮めた画面こそ日程を動かす画面だからである | `GA-1` 〜 `GA-22` ／ `XS-1` 〜 `XS-14` ／ `HT-1` 〜 `HT-4` ／ `TY-1` 〜 `TY-9` |
| `PP-2` | 実績は事実 | 実績は起きたことの記録である。<br>軽い操作のついでに変わってはならず、変えるのは実績の端を掴む意図した操作に限る | `PE-1` 〜 `PE-3` ／ `PE-6` 〜 `PE-10` |
| `PP-3` | 見えないものは掴めない | 画面に無いものが指の下で動けば、利用者は原因を知りようがない | `RF-1` 〜 `RF-3` |
| `PP-4` | 被らせない | 中心線を共有する形は札を横へ逃がし、段で分かれた形は札を動かさない。<br>札は線より手前に描く | `LP-1` 〜 `LP-8` ／ `ZO-5` |
| `PP-5` | ダミーはまだ始まっていない実績 | ダミーに独自の規則を持たせない。<br>規則の数が減る | `PE-4` ／ `PE-5` |
| `PP-6` | ポインタの形で、何を掴むかが分かる | 押す前に結果が分かるようにするためである。<br>理由の全文は `FR-106` の RATIONALE が持つ | `PK-1` 〜 `PK-9` |
| `PP-7` | 描く前後は 1 つの表で決める | 前後の決まりを 1 か所に集めるためである。<br>理由の全文は `FR-110` の RATIONALE が持つ | 表 T-020 の `ZO-5` を除く行（`ZO-1` ／ `ZO-1a` ／ `ZO-2` ／ `ZO-3` ／ `ZO-4` ／ `ZO-6` ／ `ZO-7` ／ `ZO-8` ／ `ZO-9` ／ `ZO-10` ／ `ZO-11` ／ `ZO-12`） |

⚠️ **列に `JDG-nnn` を置かない**（0 節の ③ の 3）。  
束ねる裁定は `rulings.md` の着地の欄が持つ。  
⚠️ **表の中に `（MUST）` を書かない**（値の表・横断の表の作法）。  
規則は要求が持つ。

**表の結び（⭐ 覆わない行を名指す段。  
これが 行 → 原則 の向きを閉じる）**

> ⭐ 本表の 7 つが覆うのは、9 つの表の 96 行のうち **92 行**である。
> 残る **4 行**は本表に属さず、それぞれの宿主の要求の RATIONALE が理由を持つ ——
> `PE-0` は `FR-105`、`PE-11` は `FR-075`、`PE-12` は `FR-049`、`PE-13` は `FR-044` である。
> ⛔ この段に名の無い行を、9 つの表へ足してはならない（MUST NOT）。

⚠️ **上の 92 と 4 は、本書が 4 節の推奨をそのまま当てたときの数である。**  
⛔ **当てる体は、当てた直後に下の検査を走らせて数え直すこと** —— 手で数えた数は claim であって measurement ではない。

### ⭐ 検査が読むもの（**ここが利用者の裁定「行と結ぶ」の実体である**）

⛔ **新しい Python の検査も、新しい基準線ファイルも要らない。**  
 既に在る道具で両向きが閉じる。

```
読むもの  tests/contract/spec-table.ts の specTable(id) と bareAll(cell)
          （表を見出しの字で引くので、列を足しても壊れない）
置く所    tests/contract/cr-431-principles-cover-every-row.contract.test.ts

  1. specTable('T-274') の各行の「覆う行」の欄を bareAll で開き、集合 C を作る
  2. T-266 T-267 T-268 T-269 T-270 T-271 T-272 T-273 T-020 の行 ID を集め、集合 R を作る
  3. |R| == 96                     ← 表に行が増減したらここで止まる
  4. C ⊆ R                         ← 原則 → 行。原則が無い行を名指していない
  5. C の中に重複が無い             ← 1 行の理由は 1 つ
  6. R \ C == 結びの段が名指す 4 行  ← 行 → 原則。どの行も、原則か、名指された例外か
```

⭐ **4 と 6 が両向きである。**  
 5 が「1 行 2 原則」を禁じ、3 が「黙って行が増える」を禁じる。  
⚠️ 既存の検査もただで効く —— **検査 7** は「覆う行」の欄の `GA-23` のような存在しない ID を赤にし、**検査 9** は結びの段に「表 T-274 の 7 行」と書いたときの数を突き合わせる。  
⛔ **基準線ファイルは 1 つも動かない**（検査 37・39・42・43・44 に触れない）。

---

## 4. 決定 2 —— 行の側が原則を名乗るか、原則の側が行を名乗るか

| 案 | 形 | 触る文書 | 作り直す生成物 | 数への影響 | 代償 |
|---|---|--:|---|---|---|
| **甲** | **9 つの表に「原則」の列を 1 本足す** | 1（`01-04-requirements.md`） | 無し（9 つとも手書きの表であり、生成物ではない） | `rows` は動かない。<br>⚠️ **刊行物の表の形が動く**（T-266 は 7 → 8 列、T-269 も 7 → 8 列） | ⛔ **編集は 114 行** —— 見出し 9 ＋ 区切り 9 ＋ 本体 96。<br>**同じ 1 ファイルの中で 96 回の行編集**は、`CR-430` の 9 節が名指した取り違えの罠（1 度しか現れない字を数えてから置き換える）を 96 回踏むことになる。<br>⭐ 試験は赤くならない —— `spec-table.ts` は見出しの字で列を引く（測った） |
| **乙**（⭐ **推奨**） | **表 T-274 の「覆う行」の欄が行を名乗る** | 1（`01-04-requirements.md`） | `_assets/tbl-row-id-prefixes.md` の 1 つだけ（`npm run gen`。<br>`PP` の登録） | `tables` ＋1、`rows` ＋7、`uids` ±0 | ⚠️ 結び目が 1 ホップ遠い（行を見ても原則が書いていない）。<br>⭐ **ただし 3 節の検査 6 がその向きを機械で閉じるので、「遠い」のは人の目にだけである** |

⭐ **乙を推す理由は 3 つ。**

1. **代償の大きさが 1 桁違う** —— 7 行の追加 対 114 行の編集。
2. **両向きの機械検査は、どちらの案でも同じ 1 本の試験で閉じる** ——
   甲は「列 → 表 T-274 の行 ID」を検査し、乙は「表 T-274 の欄 → 9 つの表の行 ID」を検査する。
   **分割（partition）の主張は定義から両向きである。**
3. **⛔ 96 行を触らない案のほうが、利用者の裁定に忠実である** ——
   「疎結合にするべき」は 9 つの表を独立に保てという裁定であり、
   9 つ全部に同じ列を足すのは、**9 つの表を 1 つの列で結び直すこと**である。

---

## 5. 決定 3 —— どの原則にも属さない行をどうするか

⭐ **三つの道を比べ、起草（2026-09-20）は道 C を推し、6 つめ・7 つめの原則は一存で立てずに 12 節で問うた。**  
⭐ **利用者は 2026-09-21 に「7 つにする」と裁いた（`JDG-279`、逐語）**  
⇒ ⛔ **採ったのは道 A である。**  
⭐ 残る 4 行については道 C の形がそのまま残る。

| 道 | 何をするか | 代償 | 判定 |
|---|---|---|---|
| **A** | **6 つめ・7 つめの原則を立てて PK と ZO を覆う** | ⚠️ 「原則」の語が 7 つに増える。<br>⛔ **ただし発明ではない** —— `FR-106` と `FR-110` の題は既に原則の形で書かれており、1 文字も変えずに昇格させる | ⭐ **採った**（`JDG-279`。<br>問いと答えは 12 節） |
| B | 5 つのどれかを広げて PK と ZO を呑ませる | ⛔ 広げた原則の文がぼやけ、覆う範囲が読めなくなる。<br>⚠️ `PP-1`「どれも指せる」を「掴むことのすべて」に広げると、5 つが 1 つに潰れる | **やらない** |
| C | その行は宿主の要求の RATIONALE が理由を持つと定め、検査は結びの段の名指しで通す | ⚠️ 起草では 25 行ぶんの名前が結びの段に並んだ。<br>⭐ 道 A を採った後も形は残り、並ぶ名は `PE-0`・`PE-11`・`PE-12`・`PE-13` の 4 つになる | ⭐ **その 4 行について採った** |

⛔ **道 C は「理由が無い」ことを認める道ではない。**  
 測った（1 節の (f)）——PK の 9 行には `FR-106` の RATIONALE が、ZO の 12 行には `FR-110` の RATIONALE が、それぞれ理由を書いている。  
⇒ ⭐ **道 A はその 2 つの RATIONALE を原則の欄へ昇格させただけであり、新しい理由を 1 つも書いていない。**  
⇒ **正しい言い方は「理由が無い」ではなく「7 つの原則は 96 行のうち 92 行を覆う」である。**  
⛔ **`ZO-5`（名称ラベル・担当と進捗の札）は `PP-7` ではなく `PP-4`（被らせない）のままとする**（`JDG-279`）。

---

## 6. 決定 4 —— 2 つの名前の穴

### 6.1 「被らせない」に要求の題が無い

⭐ **穴ではない。  
直さない。**  
条項は `01-04-requirements.md:3711` に逐語で在る（1 節の「3 つ」に全文を引いた）。  
欠けていたのは**名**だけであり、本書の `PP-4` がその名を与え、`LP-1` 〜 `LP-8` と `ZO-5` に結ぶ。

⛔ **`FR-109` の題「札は読める所に置く」を書き換えない（MUST NOT）** ——題は目標を述べ、原則は理由を述べる。  
2 つを 1 つの字にすると、どちらかが必ず後で嘘になる。

### 6.2 `FR-108` が自分の表を 1 つも持たない

⚠️ **これは本物の穴である。  
⛔ ただし本書では直さない。**

測った —— `FR-108` の STATEMENT（`:3692`）は、掴めないものを **9 種**、1 つの文の中に読点で並べている（隠した予定・実績・依存線・進捗マーカー・再開アイコン、LOD で描かないタスク、畳んだ注記、隠した行、変更前の予定の重ね、選んでいないタスクのフェードの掴み点）。  
⭐ 仕様の作法は「要求は表を指し、規則を 1 行ずつ数え上げない」である ⇒ **表 1 つぶんの中身が散文に入っている。**

⛔ **本書で直さない理由**: 10 個めの表を足すと、`tables`・`rows`・接頭辞が本書の予測を越えて動き、**「96 行を 1 つも動かさない」という本書の約束と、同じ巡で混ざる。**

⭐ **代わりに、台帳へ 1 行開くことを提案する**（⛔ 本書は `defects.md` を編集しない。  
下は当てる体が書く文である）:

> `FR-108`（見えないものは、掴めず動かない）の STATEMENT が、掴めない対象 9 種を散文で数え上げている
> （`docs/spec/01-04-requirements.md` の `FR-108` の STATEMENT）。  
> 表を持たない唯一の
> `FR-104` 〜 `FR-110` であり、表にすれば行 ID が付いて試験が 1 行ずつ主張できる。
> ⚠️ `CR-431` の巡では直さない（同じ巡で表の数を動かさないため）。

---

## 7. `CR-430` の 5 節への訂正（⛔ `CR-430` は 1 文字も編集していない。下が訂正の文であり、その正は本節である）

> ⚠️ **訂正（`CR-431`、2026-09-20）**: 本節は `rows` の後を **2161** と予測したが、
> `0044b08f` の実測は **2162** である。  
> 差の 1 行は `FR-103` の 表 T-245 の `GO-11`
> （マイルストーンの予定を横に引いて離したときに置く値）であり、
> `JDG-277` の ① が**本書を当てている最中に**裁いて足した行である —— 本節を書いた時点には存在しなかった。
> ⛔ 数え落としではない。  
> ⭐ 本節は 表 T-245 を 1 度も数えていないので、予測の式のほうが正しく、
> 予測の**前提となった木が動いた**だけである。

⛔ **当てる体は `CR-430` へこの 1 行を書き足していない** —— 当てる巡の指示が `CR-430` を 触ってよいファイルから明示的に外したからである（13 節の 5）。  
⇒ **この訂正の正は本節である。**  
`CR-430` の 5 節を読む者は、本節を併せて読むこと。

---

## 8. 数の予測

`0044b08f` の実測は **tables=168 ／ figures=17 ／ rows=2162 ／ uids=160 ／ 接頭辞 157**（`md-checks.py` と `row-id-prefixes.json` で数えた）。

| 量 | 式 | 差 | 後 |
|---|---|--:|--:|
| tables | 新しい表 1（T-274） | **+1** | **169** |
| rows | `PP-1` 〜 `PP-7` | **+7** | **2169** |
| uids | 節は `SECTION` であり UID を持たない（1.4・1.5・1.6 で測った） | **±0** | **160** |
| figures | 足さない | **±0** | **17** |
| 接頭辞 | `PP` を 1 つ | **+1** | **158** |

- ⛔ **9 つの表の 96 行は 1 行も動かない。**  
 GA 22 ／ PE 14 ／ LP 8 ／ XS 14 ／ PK 9 ／ HT 4 ／ TY 9 ／ RF 3 ／ ZO 13。
- 突き合わせは `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py` の
  `tables= figures= rows= uids=` と `python docs/spec/_source/row_id_prefixes_json_to_md.py`。
- ⭐ **外れたら数え落としが出ている**（規則 02 の 2 節）。
- ⭐ **当てた後に測った（2026-09-21、この木で）**: `tables=169  figures=17  rows=2169  uids=160`、
  接頭辞 **158**。  
  **5 つとも上の表の「後」と一致した。**  
  ⛔ 起草時の表は `rows` ＋5 ⇒ 2167 と書いていた（原則 5 つのときの式である）。  
  7 つの表で数え直して ＋7 ⇒ 2169 に直してある（13 節の 3）。

---

## 9. 影響

### 9.1 仕様

- `docs/spec/01-04-requirements.md` —— 1 か所の挿入（`:392` の次、`:394` の前）。
  節 `### 1.10 Direct Manipulation Principles (直接操作の原則)` ＋ 表 T-274 ＋ 結びの段。
- `docs/spec/_source/row-id-prefixes.json` —— `PP` を 1 件登録し、`npm run gen` で
  `_assets/tbl-row-id-prefixes.md` を作り直す（⛔ 生成物を手で直さない。  
  検査 21・27）。
- ⛔ **ほかの仕様ファイルは触らない。**  
 `05-07-design.md`・`08-10-test.md`・`A-appendix.md` は 0 行。

### 9.2 記録（当てるときに書く）

- `docs/development-records/rulings.md` —— ⭐ **当てた形は `JDG-279` の 1 行である**
  （逐語「7 つにする」、着地の欄が 1.10 節の 表 T-274 と `PP-6` ／ `PP-7` を名指す）。  
  ⛔ 2026-09-20 の 2 つの裁定（「=== / ---> / ◆ はすべて規則が異なる。 疎結合にするべき。」と
  「仕様に書き、行と結ぶ」）は **書いていない** —— 当てる巡の指示が `JDG-279` の 1 行だけを求めたからである（13 節の 5）。
  ⛔ **`PP-1` のような接頭辞＋番号を `docs/development-records/` に書いてよいのは、
  `PP` を登録簿に登録した後である**（検査 27）。  
  ⭐ 本巡では登録を先に済ませ、`npm run gen` が緑になってから書いた。
- `docs/development-records/defects.md` —— 6.2 節の 1 行。  
  ⛔ **当てる体は書いていない**（触ってよいファイルから外されている。  
  13 節の 5）。
- `docs/development-records/changelog.md` —— 1 行（`A-appendix.md` の A.3 は 2026-09-11 に
  中身をここへ移しており、足せる行が無い。  
  `CR-430` の 12 節の 4 で測ってある）。  
  ⛔ **当てる体は書いていない**（同上）。

### 9.3 実装と試験（⛔ 本書は直さない。所だけ書く）

- `tests/contract/cr-431-principles-cover-every-row.contract.test.ts` を 1 本足す（3 節）。
- ⛔ **`src/` は 0 行。**  
 表 T-274 は理由の表であり、値も振る舞いも持たない。
- ⚠️ だから**仕様だけの commit になりうる** —— ⛔ それでも `npx vitest run` を回すこと
  （規則 02 の 1 節の 6。  
  仕様の試験は原稿を読む）。

---

## 10. 試験が主張すべきこと（Step 4。⛔ 実装した者に書かせない）

1. 表 T-274 は 7 行であり、行 ID は `PP-1` 〜 `PP-7` である。
2. 「覆う行」の欄が名指す行 ID は、すべて 9 つの表（T-266・T-267・T-268・T-269・T-270・T-271・T-272・T-273・T-020）のいずれかに属する。
3. 同じ行 ID が 2 つの原則に現れない。
4. 9 つの表の行の総数は 96 である。
5. 9 つの表の行のうち「覆う行」に現れないものは、結びの段が名指す集合と**過不足なく一致する**
   （`PE-0`・`PE-11`・`PE-12`・`PE-13` の 4 つである）。
6. ⭐ **壊しの試験**: 表 T-274 の「覆う行」から `GA-1` を 1 つ落とすと 5 が赤くなること。
   9 つの表のどれかに行を 1 つ足すと 4 が赤くなること。
   ⛔ **この 2 つが赤くならないなら、結び目は機械に読まれていない。**

---

## 11. 書くときの作法（⛔ 当てる体はここを読んでから書く）

- ⛔ **置き換える文字列が 1 度しか現れないことを、書き換える前に数える**（`count(...) == 1`）。
  ⭐ 本書の当て方は**挿入 1 か所**なので、置換は 0 回である —— 置換が要ると思ったら、設計を読み違えている。
- ⚠️ **CJK の直後に強調を開き、その直後にコードスパンを置かない**。  
句読点や ⚠️ の直後は安全。
- ⚠️ `` を作らない（片方が生の強調記号になる。  
刊行物の描画検査だけが見る）。
- ⚠️ 変更履歴の行に生の縦棒を書かない（列が割れる）。
- ⚠️ 「表 X の N 行」と書くと検査 9 が「X は N 行を持つ」と読む。
- ⚠️ 値の表・用語集に `（MUST）` を書かない。
- ⚠️ `A-17` が単独でも複合語でも禁じる語を書かない（測った: 本書は 0 件）。
- ⚠️ 表を差し込んだあと、空行が 2 つ続いていないか見る。  
検査 48（表の直下の散文が行に吸われる）に注意し、
  結びの段の前に**必ず空行を 1 つ**置く。
- ⚠️ 書いたあと `python tools/fix_line_breaks.py <file>`（検査 46）。
- ⛔ **生成物を手で直さない** —— `tbl-row-id-prefixes.md` は `npm run gen` が作る。
- **道具**: `PYTHONIOENCODING=utf-8`。  
スクリプトはヒアドキュメントではなくファイルに書いて走らせる。
  改行は CRLF と LF が混ざる ⇒ 読みで正規化し、書き戻しは多数派に揃える。

---

## 12. ⛔ この変更でやらないこと ／ 返した問いと、その答え

**やらないこと**:

- ⛔ **9 つの表の 96 行を、1 行も退役・併合・振り直ししない**（利用者の裁定「疎結合にするべき」）。
- ⛔ **9 つの表に列を足さない**（4 節の甲を採らない）。
- ⛔ **`FR-109` の題を書き換えない**（6.1 節）。
- ⛔ **`FR-108` に表を与えない**（6.2 節。  
台帳に 1 行開くだけ）。
- ⛔ **`CR-430` も `defects.md` も編集しない** —— 当てる体も触っていない（7 節・9.2 節・13 節の 5）。  
  ⭐ `rulings.md` にだけ `JDG-279` の 1 行が入った。
- ⛔ **基準線ファイルを 1 つも動かさない** —— 3 節の検査は新しい契約試験であり、基準線を持たない。
  ⭐ **動かす基準線は 0 件である**（検査 37・39・42・43・44 のどれにも触れない）。

**返した問い**: **1 件。  
⭐ 2026-09-21 に答えが出た。**

> **問い**（前に立つ者が送った形）: **原則は 5 つのままか、7 つにするか。**
>
> **答え**（利用者、2026-09-21、逐語）: **「7 つにする」**（`JDG-279`）。

⭐ 答えに従って立てた 2 つは、⛔ **発明ではなく**、既に在る要求の題を **1 文字も変えずに**昇格させたものである。

- **`PP-6` ポインタの形で、何を掴むかが分かる** —— 出所は `FR-106` の題
  （`01-04-requirements.md:3560`）。
  理由は同じ要求の RATIONALE（`:3572`）。
  覆う行: `PK-1` 〜 `PK-9`。
- **`PP-7` 描く前後は 1 つの表で決める** —— 出所は `FR-110` の題（`:3837`）。
  理由は同じ要求の RATIONALE（`:3850`）。
  覆う行: 表 T-020 の `ZO-5` を除く行。

⭐ これで覆う行は **92** になり、結びの段が名指す例外は `PE-0`・`PE-11`・`PE-12`・`PE-13` だけになった。  
⛔ **`ZO-5` は `PP-4`（被らせない）のままである**（`JDG-279`）。  
⛔ **`FR-106` と `FR-110` の題は 1 文字も書き換えていない** —— 題は目標を述べ、原則は理由を述べる（6.1 節と同じ読み）。

⭐ **いま返す問いは 0 件である。**

⚠️ **許しを求めるもの（問いではない）**: **0 件。**  
 基準線は 1 つも動きません。

---

## 13. ⭐ 当てる体が CR の文から変えた所（2026-09-21）

⛔ 中身の判断で自分から変えたものは 1 つも無い。  
変えたのは、⭐ **利用者の裁定 `JDG-279` が本書の起草（原則 5 つ）を覆した所**と、⚠️ その裁定から機械に導かれる数、そして当てる巡の指示が触ってよいファイルを絞った所である。

| # | CR の文 | 当てた形 | なぜ |
|---|---|---|---|
| 1 | 原則は 5 つ（3 節の表、5 節の道 C、12 節の返す問い） | **7 つ**（`PP-1` 〜 `PP-7`） | ⭐ 利用者の裁定「7 つにする」（`JDG-279`、2026-09-21）。<br>⛔ 本書が一存で立てないと書いた 2 つに、答えが出た |
| 2 | `PP-6` の名「形で分かる」／ `PP-7` の名「前後は 1 つの表で決める」（12 節の下書き） | **`PP-6` ポインタの形で、何を掴むかが分かる**／**`PP-7` 描く前後は 1 つの表で決める** | ⛔ 裁定は「要求の題をそのまま昇格させる」である。<br>`FR-106` の題（`:3560`）と `FR-110` の題（`:3837`）を**逐語で**取った。<br>12 節の下書きは題を縮めていた |
| 3 | 8 節の `rows` ＋5 ⇒ **2167** | **＋7 ⇒ 2169** | ⭐ 5 行ぶんの算である。<br>7 行で数え直し、当てた後に実測して一致を確かめた（`tables=169  figures=17  rows=2169  uids=160`、接頭辞 158） |
| 4 | 3 節の「覆う行」の欄の `GA-1` 〜 `GA-22` のような**範囲の書き方** | 仕様の原稿では**行 ID を 1 つずつ並べた**（`GA-1` ／ `GA-2` ／ … ） | ⛔ 3 節が定めた検査は `bareAll(cell)` でコードスパンを開いて集合 C を作る。<br>範囲のままでは C が両端の 2 つしか拾わず、**4 と 6 の両向きが閉じない**。<br>⭐ 指す集合は 1 つも変えていない（92 行。<br>本書の表は読みやすさのため範囲のまま残した） |
| 5 | 7 節（`CR-430` へ 1 行）・9.2 節（`defects.md` と `changelog.md` へ 1 行ずつ、`rulings.md` へ 2026-09-20 の 2 行） | **どれも書いていない。<br>**<br>`rulings.md` の `JDG-279` の 1 行だけを書いた | ⛔ 当てる巡の指示が、触ってよいファイルを `docs/spec/**`・本書・`rulings.md` に限り、`CR-430` と `defects.md` を名指しで外した。<br>⭐ 7 節の訂正の文はそのまま本書に残るので、正は失われていない |
| 6 | 登録簿の `PP` の `words`（英語の綴り） | **空にした** | ⛔ 登録簿自身の規則である ——「letters の由来が文書から読み取れないときは `words` を空にし、`means` だけを書く。推測で広げるとこの登録簿が在る理由そのものを壊す」。<br>⭐ `PS`・`PLM`・`A` も同じく空である |
| 7 | `PP-6` ／ `PP-7` の「仕様が言う理由」の欄に、`FR-106` ／ `FR-110` の RATIONALE の文を**写す**（12 節の下書き） | 欄は**理由の在り処を指すだけ**にした（「押す前に結果が分かるようにするためである。理由の全文は `FR-106` の RATIONALE が持つ」） | ⛔ 写すと検査 11（重複の検出）が新しい組を 2 つ出し、`docs/review/duplication-baseline.txt` を動かすことになる。<br>⛔ 当てる巡の指示は基準線を 1 つも動かすなと定めている。<br>⭐ **測った**: 写した形で `A=33（new 2）`、指すだけの形で `A=31（new 0）` —— 基準線と一致する。<br>⭐ これは `R1.3`（唯一の正）にも忠実である —— 理由の正は RATIONALE 1 か所に残る。<br>⚠️ `PP-1` 〜 `PP-5` は写しても組が出ない（出所が `CR-430` であって仕様の中に無い）ので、そのままである |
| 8 | 節の本文（3 節は表と結びの段だけを書いていた） | 表の前に 3 行、後に 2 行の散文を置いた | ⭐ 仕様の作法は「要求・節は表を指し、規則を数え上げない」である（表 T-003・表 T-004 と同じ形）。<br>指す文（「…を表 T-274 に示す」）と、`V-13a` の「作り方の原則」と混ぜて読ませない 1 行を足した |
