# CR-134 — Chapter 5 を通しで点検し、シェルのユニットを割る

## 1. 変更概要

**Chapter 6 / 7 へ進む前に、Chapter 5 までを規約と自分自身に当て直した。**

| # | 直すもの | 置き場 |
|---|---|---|
| ① | ⭐ **`SingleHtmlShell` を 2 ユニットに割る**（表 T-063 に `UT-6`）—— `R2.2`（SRP・MUST）違反を閉じる | 5.3 |
| ② | **点検の道具を 2 本、技能へ入れる**（`audit-ch5.py` / `check-render.py`） | `.claude/skills/spec-graph-check/` |

⭐ **仕様書の変更は表の 1 行だけである。** 要求の本文にも、表 T-062・T-064・T-065 にも触れない。

## 2. 点検の結果

### 2-1. 機械で当てて問題が無かったもの

**`audit-ch5.py` を書いて当てた。** 章が主張する数と対応を、原稿と `model.json` の両方に突き合わせる。

| 検算 | 結果 |
|---|---|
| 表 T-062 の行数 / 表 T-064 の行数 / 表 T-065 の行数 | **34 / 34 / 8** —— 章の主張どおり |
| `CP-n` ↔ `PI-n` の 1 対 1 | **34 件一致** |
| 5.3 のディレクトリ木の葉フォルダ | **34。**「34 のフォルダは 34 部品と 1 対 1」に一致 |
| 表 T-107 の行数 | **18。**設計が 3 箇所で言う「18 メンバ」に一致 |
| `model.json` の節点 ↔ 表 T-062 の部品名 | **34 件すべて一致** |
| 表 T-062 の層 ↔ `model.json` のクラスタ | **34 件すべて一致** |
| 辺の着地 —— メンバを宣言していない相手 | **0** |
| ⭐ シェルを呼ぶ辺 | **0**（`PI-25`「他の部品から呼ばれるメンバを持たない」に一致） |
| メンバを持つのに呼び手が無い部品 | **0**（`Framework` の 7 部品を除く） |
| `LR-1` 外向きの辺 | **0** |
| **純粋性** —— 表 T-060 の層の規則 ↔ 表 T-064 のメンバ注記 | ⭐ **矛盾 0**（`documentModel` / `layoutEngine` に非 `pure` の注記は無く、`Framework` に `pure` の注記も無い） |
| **層をまたぐ 8 本** —— 宣言する部品が 表 T-064 でそれを宣言しているか | **8 本とも一致** |
| **図 F-018 ↔ 表 T-021a** | **`PV-1` 〜 `PV-4` の 4 遷移とも一致**（＋ `FR-043` / `FR-044` の 4 辺） |

### 2-2. ⚠️ 見つかった問題 —— `R2.2`（SRP・MUST）

**`CP-25 SingleHtmlShell` の責務が 8 節あった。**

```
起動と結線 ／ 現在値を保持する ／ フレームの先頭でレイアウトと幾何を 1 回計算して配り ／
描画を回し ／ 入力を渡す ／ 埋め込みの入れ物を持ち ／ 公開点を置く ／
SnapshotSource と AppShellSource の実装
```

**他の部品も 3 〜 5 節あるが、いずれも同じ表・同じ要求が縛る 1 つの事柄である**（例 —— `ScheduleLayout` の 5 節はすべて表 T-201 と `FR-017` 系）。**シェルだけは `FR-067`・`FR-065`・`LY-5`・5.6 の ADR-001 と、別々の要求が縛る事柄を抱えていた。**

⚠️ **これは 5.2 自身の分割基準に当たる** —— 「**別々の要求が寸法と規則を持っているなら別部品とする**」。

**そして `SingleHtmlShell` は 表 T-063 に無かった** —— つまり **1 ユニット（1 ファイル）と宣言されていた。**

⭐ **前例がある。**`UT-4` は `AgentApiEndpoint` を「**設置は `FR-065` が、18 メンバは 表 T-107 が縛るので、変更の理由が別である**」として 2 ユニットに割っている。**シェルは同じ形でありながら割られていなかった。**

⚠️ **CR-132 が 1 節（フレーム先頭の計算）を足して悪化させた。** そして**意図して満たさないと宣言されている条項は `R2.13` と `R2.5` の 2 つだけで、`R2.2` は入っていない。**

### 2-3. `R2.17`（プロンプト工学）—— **仕様書は既に答えていた**

**「プロンプト」は仕様書全体で 0 件だったので当否を調べたところ、表 T-009（Chapter 2.4 持たないもの）が答えていた。**

```
| XO-4 | AI 推論の実行系 | 本ソフトウェアは AI を持たない。`Agent API` を持つだけである |
```

**向きも定まっている** —— `D-5`（AI エージェントの実行環境）は「載らない」機器であり、`AC-3` は AI エージェントを**外部システム**として立てている。**AI が `Agent API` を呼ぶのであって、本ソフトウェアが AI を呼ぶのではない。**

⭐ **したがって足すものは無い。** レビュアーは `XO-4` を見て `Verdict` に `NA` と記入できる。**5.6 に書き足すと 1.9 が禁じる二重管理になる。**

### 2-4. 助言 13（4 件）を読んだ —— **欠陥は 0**

**セッションを通じて 4 のまま動いていなかったので、4 件とも原文を読んだ。**

| 位置 | 中身 | 判定 |
|---|---|---|
| `FR-006` 付近 | `stop` を列に持たない理由と、取り込んだ `Stop` の扱い。規則の本体は 表 T-019 の注記を指す | ✅ 「規則はあちら、向き付けはここ」の正しい形 |
| 表 T-023c の `SL-1` | 選択の対象を定め、基準日線が `SL-3` / `SL-7` の対象外であることを述べる | ✅ **`SL-1` は「対象」の行であり、対象の範囲を定めるのは正しい置き場。**`SL-7b` も同じ形で `SL-3` / `SL-5` を指している |
| 表 T-024a の `OP-3` | 3 択を定め、「重ねる」の理由を `FR-015` へ | ✅ 正しい形 |
| `FR-070` 付近 | 入口を 2 か所に置かない規則と、例外の理由を `FR-038` へ | ✅ 正しい形 |

### 2-5. 助言 14（18 件）を読んだ —— **欠陥は 0**

**要望の対応表（要望項番）に当たる 5 件を除いた 13 件を当てた。すべて偶然の数字一致だった。**

| 例 | 実体 |
|---|---|
| `30` | **要望 30-6** の項番（`ST-6` の取り下げ記録） |
| `64` / `12` / `16` | 表 T-025 の**測定環境**（`MC-2` CPU・`MC-3` メモリ 64GB） |
| `50` / `1000` | 表 T-025 の `MC-7`**目標規模**「50 行 / 1000 `Task`」 |
| `1.4` | **節番号** 1.4 |
| `4.5` | **WCAG のコントラスト比** 4.5 : 1 |

⭐ **値を要求の散文へ書き写した箇所は 1 件も無かった。**

## 3. 変更箇所

| # | ファイル | 何を |
|---|---|---|
| 1 | `docs/spec/05-07-design.md` | 表 T-063 に `UT-6` を 1 行 |
| 2 | `.claude/skills/spec-graph-check/audit-ch5.py` | 新規。章の主張と `model.json` を突き合わせる |
| 3 | `.claude/skills/spec-graph-check/check-render.py` | 新規。刊行物に表・図が出ているか、生の `**` が残っていないかを数える |

⛔ **触らないもの** —— 要求の本文、表 T-062・T-064・T-065、`model.json`、`erd.json`、生成物一式。

## 5. 変更後の仕様

**表 T-063 に足す 1 行**

| 行 ID | 部品 | ユニット | 割った理由 |
| --- | --- | --- | --- |
| UT-6 | `SingleHtmlShell` | `single-html-shell.ts`（公開エントリ。起動と結線、埋め込みの入れ物、公開点を置くこと、`AppShellSource` の実装）／ `frame-loop.ts`（現在値の保持、フレーム先頭の収集と計算、描画と入力への配り、`SnapshotSource` の実装） | **純粋性ではない。どちらも `non-pure` である。** 起動は `FR-067` と `FR-065` が、フレームの走行は 表 T-060 の `LY-5` と 5.6 の ADR-001 が縛るので、**変更の理由が別である** |

⚠️ **部品は増やさない。** `model.json` の節点も辺も図も動かない —— **ユニットはファイルの割り方であり、部品の境界ではない**（5.3）。

## 6. 影響反映と影響分析結果

### 6-1. 機械検査（**予測と実測**）

| 検査 | 予測 | 実測 | |
|---|---|---|:-:|
| `tables` | 109 のまま | **109** | ✅ |
| `figures` | 10 のまま | **10** | ✅ |
| `rows` | 1214 → **1215** | **1215** | ✅ |
| `uids` | 140 のまま | **140** | ✅ |
| 検査 11 | `new 0` | **`A=16 (new 0)`** | ✅ |
| ゲート 14 本 | すべて緑 | **すべて緑** | ✅ |
| 刊行物 | 設計 16/16 表・7/7 図・生の `**` 0 | **一致** | ✅ |
| `audit-ch5.py` | PASS | **PASS** | ✅ |

### 6-2. `R2.2` が閉じたことの確認

**割ったあと、1 ユニットあたりの変更の理由は 1 つになる。**

| ユニット | 縛る要求 |
|---|---|
| `single-html-shell.ts` | `FR-067`（単一 `.html`）／ `FR-065`（既定で公開しない） |
| `frame-loop.ts` | 表 T-060 の `LY-5`（現在値）／ 5.6 の ADR-001（フレーム先頭の 1 回）／ `NFR-010`（操作していない間は描き直さない） |

## 7. 最終判断日時

**2026-08-15。** 利用者が「表 T-063 でユニットに割る」を選んだ。

| 決定 | 結論 | 却下した案 |
|---|---|---|
| `R2.2` の閉じ方 | ⭐ **表 T-063 でユニットに割る** | **意図して満たさないに加える**（合成の根は本質的に全体を結線する、という理由は成り立つが、`UT-4` の前例がある以上、割れるものを割らない理由にならない）／ **部品ごと割る**（部品 34 → 35、辺の付け替え、図 5 枚の再生成まで動く）／ **何もしない**（後から `R2` で審査する者が同じ問いを再び立てる） |

## 8. 反映記録（2026-08-15）

**§6-1 のとおり。予測を外した項目は 0 件。**

### 8-1. 学び

⭐ **「機械検査が緑」「刊行物に出ている」の次に、「規約に当てる」がある。** 本 CR が見つけた `R2.2` 違反は、16 検査でも刊行物の数え上げでも見つからない。**`FR-092` の `EZ-5` が設計の合否を `R2` で判定すると定めている以上、条文を 1 つずつ当てるまで終わらない。**

⭐ **助言 13 / 14 は 22 件とも欠陥では無かった。** ただし**読むまでそれは分からない。** 数が動かないことは「見た」ことの証明ではない。

### 8-2. 残った作業

| # | 件 | どこで |
|---|---|---|
| 1 | `SW_SPEC` ノード（`SWS-xxx`）と JSON Schema | **Chapter 6.1 / 6.2**（未記入） |
| 2 | 系統・テストレベル・方針・ツール・合格基準 | **Chapter 7 の残り**（未記入） |
| 3 | 図 F-015 の 3 本の重なり ／ 概要図の矢印が 1 通路に重なること | 別 CR 2 件（CR-133 §8-2） |
| 4 | `FR-063` / `FR-066` の `MSPDI` 書き出し時の扱い | 別 CR |
