# CR-226 — 2 つの欠陥を同時に閉じる 1 セル

> **閉じるもの**: ⛔ **`Open Chooser`（`U-56`）に閉じる入口が無い** —— 表 T-109 の `IC-52`（開いている面を閉じる）の「面」の欄は
> `Help Modal` / `AI Export Modal` / `Resource Roster` / `Export Chooser` の 4 つで、本巡に立てた `U-56` が載っていない。
> ⛔ **同じセルが、辞書の見出しの枠も決めている** —— `tools/generate_display_words.py` は `surfaces` の名簿を**その欄から**起こす。
> ⭐ **1 セルで 2 つの欠陥が閉じる。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`。受ける目標は `GL-006`。** ⛔ **開いた面から出られないのは、説明を読まずに操作できることの正反対である。**

⚠️ **`CR-220` が `U-56` を立てたときの取りこぼしである** —— 面を足したら、その面を閉じる入口も要る。
⭐ **`IC-52` の欄が名簿だと分かっていれば同じ回で当たっていた。** `CR-191` と `CR-193` も同じ 1 セルへ名を足している（前例が 2 つある）。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（網羅性）| `IC-52` の面の欄 | ⛔ **`U-56` が抜けている。** 表 T-103 は 5 面を持つのに、閉じられるのは 4 面 |
| **`R1.3`**（唯一の正がある）| 辞書の `surfaces` の名簿 | ⭐ **生成器が `IC-52` の欄を名簿として読む**ので、正は 1 か所である。⛔ **だから欄の漏れが辞書の枠の漏れになる** |
| **`R4`**（POLA）| `IN-4` の `Esc` で代用する案 | ⛔ **退けた。** `Esc` は「読むのをやめる」と読める道であり、**選ばずに閉じることと取りやめることが同じ打鍵になる** |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **新しい行を立てず、`IC-52` の欄に足した** | ⚠️ **その欄は名簿である** —— `CR-191` / `CR-193` が同じことをしている。行を立てると閉じる入口が 2 つになる |
| 2 | ⛔ **図形を 1 つも起こしていない** | `IC-52` は既に図形を持つ。**足すのは面の名だけ**であり、表 T-026 の `RC-13` に触れない |

---

## 1. 測った

```
induced.py  IC-52 T-109 T-103 U-56 IN-4 T-028 FR-038 T-037 NT-1 NT-3a FR-028 FR-029
impact.py   IC-52
```

| 測ったこと | 実測 |
|---|---|
| 閉路 | ⭐ **0** |
| `IC-52` を指している箇所 | 要求 **0 件** ／ 参照 **2 箇所** |
| 生成器が読む場所 | `tools/generate_display_words.py` の `CLOSE_SURFACE_ROW = 'IC-52'` |

⭐ **実測で確かめた** —— 辞書に `Open Chooser` の行を足したら生成器が
「`surfaces`: 原稿は 5、仕様は 4 を求める。仕様に無い: Open Chooser」で拒否した。**セルが名簿である証拠である。**

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-109 の `IC-52` | 面の欄に ` / \`Open Chooser\`` を足す |
| 2 | `_source/display-words.json` | `surfaces` に `Open Chooser` の行（語は空。`PD-160`）|
| 3 | A-appendix | 変更履歴に 1 行 |

⛔ **`IN-4` も 表 T-028 も 1 文字も動かさない。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **132** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1614 | **1614** | ⭐ **0** —— 既存の 1 セルへの追記 |
| `uids` | 148 | **148** | 0 |
| 辞書の枠 | 183 | **184** | ＋1（`surfaces` の見出し 1 枠）|
