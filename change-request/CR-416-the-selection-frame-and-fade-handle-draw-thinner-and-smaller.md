# CR-416 — 選択の枠を細かく、フェードの掴み点を一回り小さく描く

> **閉じるもの**: 利用者の裁定 `JDG-156`（四通目）を仕様へ着地させる。逐語は `docs/development-records/rulings.md` の `JDG-156` が持つ。
> - 四通目: 「五月雨ですまんが、タスクを選択した時の 点線が太すぎる。 もう少し細く、線の間隔を縮めよ。」「同じく、フェードの掴みの□も一回り小さくしろ。」
>
> ⭐ **本書は仕様だけを当てる。** 表 T-206 の `S-174`（選択の枠の太さ）2 → 1px、`S-175`（破線の刻み）2 × 2 → 2 × 1px、表 T-210 の `S-109`（フェードの掴み点の半辺）4.5 → 3.5px（9 × 9 → 7 × 7px）、`S-110`（その枠線）1.5 → 1.0px にし、表 T-210 の後の段の「見た目の点（9 × 9px）」を 7 × 7px に直した。`S-92`（当たり判定 15 × 15px）は変えない。どれも利用者が見てから詰める試す値である。読んだ木は `1fe521ca`。
> ⚠️ 8 本（`CR-409`〜`CR-416`）は 1 つの計画で 1 度に当てた（`CR-409` の第 3 節）。本書が 8 本の最後なので、8 本の合計（第 5 節）を持つ。
> ⚠️ `src/`・`tests/`・`tools/` は触っていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（`CH-2` の受ける目標は `GL-002`）—— 縮めた絵の上で、選択の枠とフェードの掴み点がタスクの形と名前を覆う量を減らす。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— 表 T-210 の後の段「当たり判定の大きさは `S-92`（15 × 15px）が別に持つ。見た目の点（9 × 9px）より広く取る」の 9 × 9 が、`S-109` の 3.5 で偽になる ⇒ 7 × 7 に直した。15 × 15 は 7 × 7 より広いまま。
- **`R1.3`** —— `S-109` の範囲は 3 〜 8、`S-110` は 1 〜 3 —— 3.5 と 1.0 は範囲の中にある。`S-174` / `S-175` は範囲を持たない。
- **`R1.3`** —— `SL-8` の「どちらも倍率に追随させてはならない（MUST NOT）」は変わらない（値だけを変えた）。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 値を `JDG-156` の読みのとおり（`S-174` 1、`S-175` 2 × 1、`S-109` 3.5、`S-110` 1.0）にし、4 行とも注に「描いた絵を見て選び直す試す値」と書く | ⭐ `JDG-156` の読み「数はどれも試す値で、見てから詰める」 | 無し |
| 決定 2 | `S-110` を `1` ではなく `1.0` と書く | ⭐ 文書スキーマ（`erd_json_to_schema.py`）は、値の綴りが小数点を持たないと鍵の型を `integer` に狭めた（`fadeHandleStrokePx` が `number` → `integer`、実測）。型は文書を読む路で強制される（`05-07-design.md` の 表 T-220 の前文）ので、1.5 を持つ文書が開けなくなる ⇒ `1.0` で `number` を保った | 無し |
| 決定 3 | `S-92`（15 × 15px）を変えない | ⭐ `JDG-156` の読み「押せる広さ `S-92`（15×15px）はそのまま」 | 無し |
| 決定 4 | 🔎（由来の記録が無い）の印を 4 行とも残す | ⭐ 試す値であり、測って決めた値ではない | 無し |

---

## 1. 読んで確かめた事実

| 事実 | 所（`1fe521ca`） |
|---|---|
| `S-174` ＝ 2px 🔎、`S-175` ＝ 2 × 2px 🔎（表 T-023c の `SL-8`）| `docs/spec/_source/settings.json:2950`〜`:2976`（当てる前）|
| `S-109` ＝ 4.5px 🔎（正方形 9 × 9px）、範囲 3 〜 8。`S-110` ＝ 1.5px 🔎、範囲 1 〜 3 | 同 `:4673`〜`:4710`（当てる前）|
| 表 T-210 の後の段「見た目の点（9 × 9px）より広く取る」| 同 `:4731`（当てる前）|
| `fadeHandleHalfPx` / `fadeHandleStrokePx` は文書スキーマに鍵として在る（`number`）| `docs/spec/_source/grs-document.schema.json`（当てる前）|

## 2. 測った

- 掴み点の見た目の一辺 ＝ 半辺 × 2: 4.5 × 2 ＝ 9px → 3.5 × 2 ＝ 7px。当たり判定 15px との差 6px → 8px。
- 選択の枠の破線の 1 周期 ＝ 描く長さ ＋ 空ける長さ: 2 ＋ 2 ＝ 4px → 2 ＋ 1 ＝ 3px。
- ⚠️ 描いた絵は測っていない。

## 3. グラフ

**当てる前**: `impact.py S-174` / `S-175` —— 指す要求 1（`FR-081`）。`S-109` / `S-110` —— 指す箇所 0（浮いている）。`S-92` —— 指す要求 2。
**`induced.py`**: `CR-409` の第 3 節と同じ実行。4 行とも閉路に入らない。
**届いた行を `rulings.md` で引いた**（`S-174` / `S-175` / `S-109` / `S-110`）: `JDG-156` だけ。衝突 0。

## 4. 当てた中身

| 対象 | 置き場（当てたあと） | 中身 |
|---|---|---|
| `S-174` | `docs/spec/_source/settings.json`（→ `_assets/tbl-settings.md:328`）| 1px 🔎。注に試す値 |
| `S-175` | 同（→ `:329`）| 2 × 1px 🔎。注に試す値 |
| `S-109` | 同（→ `:474`）| 3.5px 🔎。注「正方形 7 × 7px」、試す値 |
| `S-110` | 同（→ `:475`）| 1.0px 🔎。注に試す値、1.0 と書く理由 |
| 表 T-210 の後の段 | 同（→ `:479`）| 見た目の点（7 × 7px）|

## 5. 数の予測

本書の差: tables 0 ／ figures 0 ／ rows 0 ／ uids 0。⭐ 足した `（MUST）` / `（MUST NOT）` は 0。

**8 本の合計**（`CR-409`〜`CR-416`）:

| | 当てる前 | 予測 | 当てたあと（`check.sh`）|
|---|--:|--:|--:|
| tables | 157 | 160（T-259、T-260、T-261）| 160 |
| figures | 11 | 11 | 11 |
| rows | 1997 | 2018（`VG-1`〜`VG-7`、`SE-1`〜`SE-5`、`GS-1`〜`GS-4`、`DS-10`、`SK-22`、`SK-23`、`S-243`、`S-244`）| 2018 |
| uids | 153 | 153 | 153 |

⭐ 足した `（MUST）` は 23（`CR-409` 1、`CR-410` 1、`CR-411` 7、`CR-413` 8、`CR-414` 1、`CR-415` 5）、`（MUST NOT）` は 7（`CR-409` 1、`CR-411` 2、`CR-413` 2、`CR-415` 2）、合わせて 30。数え方: 当てた文を 1 文ずつ読み、足した印を数えた。
⚠️ 検査 39（テストが逐語で押さえていない条項）は 1400 → 1430（+30）で、基準線 1401 を 29 超える（実測）。`git diff -U0` の足した行と消した行ごとに `（MUST）` / `（MUST NOT）` を数えると、足した行 46・消した行 16 で差 +30 —— 8 本が足した 30 と一致し、既存の条項で押さえが外れたものは数の上では無い。基準線は実装の波の試験が逐語で押さえれば下がる。⛔ 基準線の編集は本書の範囲の外。

## 6. 影響

### 6.1 仕様

| 所 | 本書で |
|---|---|
| 表 T-023c の `SL-8`、`FR-081` | そのまま |
| `FR-075`（フェード）、表 T-023d の `GR-1` / `GR-2` | そのまま —— 当たり判定は `S-92` |

### 6.2 記録

| 所 | 書いたこと |
|---|---|
| `docs/development-records/rulings.md` の `JDG-156` | 状態を 適用済 へ、着地先に本書と行 ID を足した |
| `docs/development-records/changelog.md` | 本書の 1 行 |

### 6.3 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `npm run gen` | `NOT_STORED_SELECTION_SIZES` の `S-174` / `S-175`、`SETTINGS_DEFAULTS` の `fadeHandleHalfPx` / `fadeHandleStrokePx`、`startup-template.json` |
| `tests/unit/uf-32.test.ts` | 注が 表 T-210 の旧い文（「正方形 9 × 9px を点の中心に置く」）を引く ⇒ 検査 42 が数える。新しい文へ |
| `tests/unit/t-023c-sl-3-marquee-drawn-while-held.test.ts`、`tests/unit/t-023c-selected-line-width.test.ts` ほか | 2px・2 × 2・4.5・1.5 を直書きしていれば表から読む形へ |

## 7. 試験が主張すべきこと

⭐ 名前は本書のとおりに写すこと: `S-174`（1px）、`S-175`（2 × 1px）、`S-109`（3.5px）、`S-110`（1.0px）、`S-92`（15 × 15px、変えない）。

1. `S-174` / `S-175`: 選んだタスクの破線の枠が `stroke-width` 1、`stroke-dasharray` 2 1。表示の倍率 50 と 200 でも同じ（`SL-8` の MUST NOT）。範囲選択の矩形（`SL-3`）も同じ。
2. `S-109` / `S-110`: 選んだタスクのフェードの掴み点が一辺 7px の正方形で、枠線 1px。
3. `S-92`: 掴み点の中心から 7px 離れた点（見た目の点の外、当たり判定の中）を押すと `GR-1` / `GR-2` を掴む。

## 8. 関係 —— `JDG-156`

- ⭐ **`JDG-156` を着地させる** —— 「点線が太すぎる。 もう少し細く、線の間隔を縮めよ。」は `S-174` と `S-175`、「フェードの掴みの□も一回り小さくしろ。」は `S-109` と `S-110`。「押せる広さ」は `S-92` のまま。

## 9. ⛔ この変更でやらないこと ／ 返す問い

- ⛔ `S-92` を変えない。
- ⛔ `src/`・`tests/`・`tools/` を編集しない（第 6.3 節）。

**返す問い**: 無し（値は dist を見てから利用者が詰める）。
