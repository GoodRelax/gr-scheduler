# CR-562 — 日程の画像をデータ化するプロンプトを、アプリからクリップボードへ写す

> 起草の状態: 書き直し（2026-09-24）。⛔ **`docs/spec` と `src/` は 1 字も触っていない。** 起草（同じ日、`45ba1707`）の後に利用者が入口の形を覆した（`JDG-504` 〜 `JDG-509`）ので、0 節 〜 4 節を今の木に対して書き直した。起草の決定のうち成り立たなくなったものは、消さずに「取り下げ」と書いて残した（0 節 ③）。
> 読んだ木: `47be689f`（`claude/goofy-borg-332fd7`、`refactor` の上）。行番号・数・参照は、すべてこの木で測り直した（測り方は 13 節）。書いている間に木は `88e853b4` へ進んだが、進んだ 3 つのファイル（`rulings.md` ・ `tbl-row-id-prefixes.md` ・ `CR-563`）は 4 節の編集が当てるファイルではない。
> ⛔ **当てる順**: **`CR-565` を本書より先に当て、両方ともリファクタの段 7.5 の分け（split）の後に当てる。** ⚠️ **当てる前に数え直すこと** —— 当てるのはリファクタ（`CR-551`〜`CR-554`、H2〜H10、段 7.5 ・ 段 8）と `CR-565` の後である。
> ⛔ **前提 `CR-565`**（書き出しはいつもアプリのスキーマの版に揃え、取り込みはそのスキーマが読むものだけを読む。表 T-024a の `OP-6` と 表 T-024 の `IX-3` を覆す）: 本書はプロンプトに公開のスキーマを丸ごと埋める（決定 4）ので、埋めるのは `CR-565` が残した形のスキーマである。⚠️ `CR-565` は `47be689f` の木に無いので、本書は中身を読んでいない —— 依存だけを書く。
> 4 節の編集（E- ・ J- ・ N-）は `47be689f` の木で書き、旧がそれぞれのファイルに 1 回だけ現れることを数えた（13 節の 5）。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示 2 件（`JDG-501` ・ `JDG-502`。逐語は 0.1 節）と、2026-09-24 の裁定 6 件（`JDG-504` 〜 `JDG-509`）。欠陥 `DFC-920`（プロンプトの「自由」の色が今のスキーマで断られる）と `DFC-921`（土台の文書がスキーマから外れている）。
> ⛔ **覆すもの**: `FR-068` の「AI へ渡す文書（`GRS JSON`）を面で確かめて写す」—— 開いている文書を写す道をやめ（`JDG-505`）、ヘッダーの `IC-115` を押すとすぐプロンプトを写す（`JDG-509`）。**AI 出力の面（表 T-103 の `U-30` の 2 つ目の名 `AI Export Modal`）を退ける**（`JDG-509`）。表 T-109 の `IC-19` を廃番にする（`JDG-506`）。`JDG-503` の問 4（AI 出力の面に中身の切替 `IC-115` を足す）を覆す（`JDG-506`）。`FR-066` の「`Agent API` が無効のあいだ `IC-18` は薄く描かれる」と、表 T-233 の `RS-35` を退ける（`JDG-507`）。`docs/guides/schedule-to-grs-json/` の使い方 1〜2（ファイルを 2 つ落として添える）。
> ⭐ **形の方針**: 新しい交換形式を作らない。スキーマは 1 つ（`docs/spec/_source/grs-document.schema.json`）のまま、読む側の検証（`GRS_DOCUMENT_SCHEMA`）とプロンプトが同じ原稿から生まれる。返ってきた JSON は今ある「開く」（`OP-2`）の 1 つの入口から入る。

### 0.1 利用者の逐語（2026-09-23 〜 2026-09-24）

⚠️ 2026-09-23 の 2 つは、前に立つ者が本書の起草者へ渡した写しのまま写した。2026-09-24 の 6 つは `rulings.md` の逐語の欄から写した（`<br>` は改行に直し、本書に関わる段だけを取った）。

| # | 逐語 |
|---|---|
| Q1（`JDG-501`） | 「画像認識してGRSのJSONを生成する機能も実装しろ。」 |
| Q2（`JDG-502`） | 「画像から JSONはAI向けのプロンプトをクリップボードにコピーさせればよい。GRS JSONのスキーマは他と共用できるだろ？」 |
| Q3（`JDG-504` の CR-562 の段） | 「アイコン名:  日程の画像をデータ化するプロンプト  とせよ。 依頼分は逆に意味不明。 プロンプトが良い。」 |
| Q4（`JDG-505`） | 「入り口は日程の画像をデータ化するプロンプト だけでよいはず。 JSONは別途ファイルに保存するので、そっちを使えばいいのでは？」 |
| Q5（`JDG-506`） | 「IC-18 AI との対話欄を出す・隠す / IC-20 Agent API を有効・無効にする / IC-x 日程の画像をデータ化するプロンプト の3つではだめか？」 |
| Q6（`JDG-507`） | 「Agent API が無効な間に AIとの対話欄を出すアイコンを有効にされた場合は、Agent APIを自動で有効にしてよい。 こっちの方が自然な導線だ。」 |
| Q7（`JDG-508` の CR-562 の段） | 「IC-115 の説明:  AIにPPTやExcelなどで作った日程をGoodRelax Schedulerで処理できるJSONデータに変換させるプロンプトをクリップボードに出力する としろ。」 |
| Q8（`JDG-509`） | 「A. すぐ写す（推奨）でよい。 ただし、コピー後 クリップボードに貼り付けたプロンプトをAIに渡してください。冒頭部分は適宜編集してください。 [OK] と表示しろ。 そうしないと、ユーザーは何が起こったか分からない。」 |

⭐ **Q2 が Q1 の形を決めている** —— `GRS` 自身は画像を読まない。画像を読むのは利用者が選んだ AI であり、`GRS` はその AI へ渡すプロンプトを写すだけである。⇒ `FR-023` の RATIONALE「画像の取り込みは範囲外なので、攻撃面はこれだけである」は偽にならない（返ってくるのは今までどおりの `GRS JSON` のファイルである）。
⭐ **Q2 の問い「スキーマは他と共用できるだろ？」への答えは「できる。既に 1 つである」** —— 読む側の検証 `GRS_DOCUMENT_SCHEMA`（`src/adapter/document-codec/json-codec.ts:714`）は `tools/generate_json_schema_validator.py` がこの原稿から刷ったものである。本書は 3 つ目の読み手（プロンプト）を同じ原稿に繋ぐ。
⭐ **Q4 〜 Q5 ・ Q8 が入口の形を決めている** —— ヘッダーの AI の群は 3 つのアイコンを常に出す。開いている文書を写す道（`IC-19` と AI 出力の面）は無くなり、日程を AI へ渡すときは保存した `.json` を添える。`IC-115` を押すと面を開かずにすぐ写し、写したことを [OK] 付きで告げる。
⚠️ 「依頼文」の語は Q3 でやめた。本書の語はすべて「プロンプト」である（13 節の 2 —— 本書の中の「依頼文」は、この注と測り方の 1 行だけに残る）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`**（作図ソフトと同じ操作感で描きながら、構造化した日程データを出す）である。受ける目標は `GL-004`（成果物が構造化データである）。
作図ソフトや表計算ソフトで描かれた日程は構造を持たない。その絵を構造化データへ起こす道を、アプリの中から始められるようにする。
⚠️ **`GL-005`（1 つのファイルだけで動く）は動かさない** —— 写すのは文字列であり、送り先も通信も持たない。AI へ貼るのは利用者である。

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた結果 |
|---|---|
| `R2.17`（プロンプト工学: `src/` に置く・入出力スキーマを明示・曖昧指示なし・プロンプトテスト・版の方針・ハルシネーション対策） | ⛔ **今の案内は 4 つを欠く** —— 置き場が `docs/guides/`（製品の外）、入出力スキーマは「添付があれば」の任意、試験が無い、版はプロンプトに無い。⭐ 決定 3〜6 で 4 つとも塞ぐ。ハルシネーション対策は既に在る（出力 1 の「推定したこと」の表、`prompt-ja.md:112`） |
| `R1.3`（唯一の正・矛盾が無い） | ⛔ **スキーマの写しが 2 つある** —— 案内が添えさせる `grs-document.schema.json` と、案内の中の列の一覧（`prompt-ja.md` の 作り方 2〜9）。⭐ 決定 4 で写しをやめ、スキーマを丸ごと埋める。⛔ **`JDG-507` は `FR-029` の「同じ機能の入口を画面上の 2 か所に置いてはならない（MUST NOT）」と食い違って見える** —— `IC-18` を押しても `Agent API` が有効になるからである。⭐ 決定 21 で解く: `IC-18` の職務は欄を出すことであり、有効にするのは欄を出す前提を満たすためである。`IC-18` は `Agent API` を無効にしない（有効・無効を切り替える入口は `IC-20` 1 つのまま） |
| `R1.4`（異常系） | ⛔ **AI が誤った JSON を返したとき、開く路が黙る** —— `DFC-656`（`documentFromJson` が断ると通知が 0 件）と `DFC-922`（表 T-220 のうち開く路が見るのは `IV-17` だけ）。⭐ 決定 8 でこの 2 行を本書の前提に置く。⚠️ **写せなかったときの理由の行が無い** —— 今の造りは落ち先の `RS-15`（「この理由にまだ行が無い」）を告げる（`frame-loop.ts:4016`）。`JDG-509` は「写せなかったときは今ある `RS-15`」と決めたので、本書は行を足さない |
| `R1.6`（否定の要求は代わりの行動を示す） | ⭐ 「開いている文書を写す道は持たない」（E-20）に代わりの行動を添えた —— 保存した `GRS JSON` のファイルを AI に添える |
| `R4.4`（状態機械の定義・命名・状態遷移表） | `JDG-507` は 2 つの領域（`screen` の対話欄と `agentApi` の有効化）に同じ押しを届ける。⭐ 決定 21: 領域ごとに出来事を 1 つずつ持ち（原稿は同じ鍵を 2 度使うことを断る —— `state-machines.json` の `$comment`）、`agentApi` の側は「無効 → 有効」の 1 つの升だけを足す。有効のときの升は置かない（置かない升は「変わらない」 —— 同じ `$comment`）。⭐ `IC-115` の押しは状態を持たないので、状態機械の原稿に出来事を置かない（`IC-3` の画像のコピーと同じ —— 13 節の 2） |
| `R2.7`（DRY） | 土台の文書（`grs-skeleton.json`）が手書きで、起動時の文書を刷る道具と別に保たれ、既に離れている（`DFC-921`）。⭐ 決定 5 で同じ道具から刷る |
| `R2.9`（YAGNI） | ⭐ 貼り付けで開く入口・AI への送信・画像の読み取りを持たない（10 節）。⭐ 面の中身の切替（起草の決定 12）も、写す前に見せる面（`JDG-509`）も要らなくなった |

### ③ 利用者に問わずに決めたこと

⚠️ 起草の日の決定 1 〜 16 の番号はそのまま残す。成り立たなくなった決定は「取り下げ」と書き、行を消さない（`JDG-503` ・ `JDG-504` がこの節の番号を名指している）。利用者が答えて決まった行は、その裁定を名指す。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | ⭐ **プロンプトの言語は、写した瞬間の表示言語（`FR-038` の `ja` / `en`）に従う** | 表示言語は利用者が読める言語であり、「推定したこと」の表を読むのも同じ利用者である。言語を別に選ばせる入口は `FR-029` の「入口を増やさない」に反する | 英語の AI に日本語の画面から頼みたい人は、`IC-21` で表示言語を切り替えてから写す |
| 決定 2 | ✅ **利用者の裁定（`JDG-509`）**: `IC-115` を押すと、面を開かずにすぐプロンプトをクリップボードへ写し、写せたら 表 T-233 の `RS-65` を告げる（[OK] は 表 T-037 の `NT-8` がどの通知にも置く入口である）。写せなかったときは今の落ち先 `RS-15`。⚠️ 起草の日は「面は写す前に中身を読める形で見せる」と決めた —— その形は取り下げた | プロンプトは日程を 1 字も持たないので、今の `FR-068` の「何を渡しているか分からないまま渡させない」は当たらない。押しても画面が変わらないので、告げなければ何が起きたか分からない（Q8） | 写す前にプロンプトを読む道は無い —— 読みたい人は AI へ貼る前に貼った先で読む |
| 決定 3 | ⭐ **プロンプトの原稿は `docs/spec/_source/` に言語ごとに 1 つ置き、生成で `src/` へ届ける**（`display-words.json` と同じ道） | `R2.17` は製品のプロンプトを `src/` に置けと言い、この木の約束（`_source` が正、`npm run gen` が刷る。検査 21）は原稿を `_source` に置けと言う。生成物が `src/` に在れば両方を満たす | 生成器が 1 つ増える。検査 16 と同じく、生成物を手で直してはならない |
| 決定 4 | ⭐ **スキーマは URL で指さず、プロンプトに丸ごと埋める**（最小化した `grs-document.schema.json`、22,917 バイト）。⭐ **埋めるのは `CR-565` を当てた後のスキーマである** —— `CR-565` が書き出しをアプリのスキーマの版に揃えるので、AI が書く版と、返った文書を開く側が読む版が同じ 1 つになる | 利用者は git を使わずファイルを落とすだけである（利用者の裁定 2026-09-16）。URL は版がずれ、`$id` は生のファイルを指さない。添付させると手順が 2 つ増える | 写す文字が増える（ja 42,240 バイト、en 40,639 バイト。13 節の測り方 1）。貼り付けの長さに上限を持つ AI では入りきらないことがある。⚠️ 埋めるスキーマは `documentSettings` を閉じている（`DFC-841`）ので、AI は同群の鍵を 1 つも欠かさず書こうとする。土台が同じ版で刷られていれば（決定 5）害は無い。⚠️ スキーマの大きさは `CR-565` の後に数え直す |
| 決定 5 | ⭐ **土台の文書（空の `GRS JSON`）は、起動時の文書を刷る道具（`tools/generate_startup_template.py`）から同じ版で刷る** | 起動時の文書の `schemaVersion` がこの造りの知る最大の版（`GREATEST_KNOWN_SCHEMA_VERSION`、`frame-loop.ts:177`）であり、土台がそれと同じなら版の違いの扱いが起きない。今の土台は `2026-09-14`、起動時の文書は `2026-09-17` で離れている（`DFC-921`）。⚠️ 起草では「`OP-6` の補いも起きない」と書いたが、`OP-6` は `CR-565` が覆すので、E-20 は `OP-6` を名指さない | 案内の `grs-skeleton.json` も生成物になり、手で直せなくなる |
| 決定 6 | ⭐ **プロンプトは自分の版として、写した造りの `schemaVersion` を 1 行持つ** | `R2.17` の版の方針。返ってきた JSON がどの造りのプロンプトから作られたかを、利用者が AI の答えと並べて読める | — |
| 決定 7 | ⭐ **返ってきた JSON は、今ある「開く」（`IC-1` とドラッグ＆ドロップ、`OP-2`）から入れる。貼り付けで開く入口は作らない** | `OP-2` が「入口は本要求の『開く』1 つとし…別の入口を設けてはならない（MUST NOT）」と定めている。開いた後は `OP-3` が「置き換える／合流する」を問うので、今の日程に足すことも選べる | 利用者は AI の答えを `〜.json` として保存する手間を 1 つ持つ |
| 決定 8 | ⭐ **本書の実装の波は、`DFC-656` と `DFC-922` が閉じてから始める** | AI の答えは誤りを含みうるのが前提である。誤った JSON を開いて何も起きない（`DFC-656`）、UID が重なった JSON が黙って開く（`DFC-922`）のままでは、利用者は何が悪いのか分からない | 本書の着地が 2 行ぶん後ろへずれる |
| 決定 9 | ⭐ **プロンプトの色の決まりを、今の保存の形に合わせる**（`CR-548` のカスタムカラー `"#rrggbb/"`） | 「自由」の `"#RRGGBB"` は `fillColor` の `pattern` に断られ（`json-codec.ts:313`）、開く路が黙る（`DFC-920`） | 案内の `:35` を書き直す |
| 決定 10 | ⭐ **案内（`docs/guides/schedule-to-grs-json/`）は開発者向けの写しとして残し、利用者向けの使い方は「アプリのヘッダーの `IC-115` を押す」に書き直す** | 利用者はアプリだけを持つ（利用者の裁定 2026-09-16）。案内の `:9`〜`:12`（ファイルを 2 つ落とす）と `:223`〜`:231`（Python での確かめ）は利用者には打てない | 案内のプロンプトの塊は生成物になる |
| 決定 11 | ⛔ **`GRS` は「推定したこと」の表を読まない** | 表は AI の答えの本文に在り、JSON の外である。`GRS` が読めば新しい交換形式になる（`FR-068` の MUST NOT） | 利用者は AI の画面で表を読み、`GRS` の画面と見比べる |
| 決定 12 | ⛔ **取り下げ**（2026-09-24、`JDG-506`）。起草では「AI 出力の面に中身の切替を 1 つ足す（`IC-115`）。閉じる入口 `IC-52` はいま見せている中身を写す」と決め、利用者も一度は答えた（`JDG-503` の問 4）。`JDG-505` で面の中身が 1 つになり、`JDG-506` で `IC-115` はヘッダーのアイコンになり、`JDG-509` で面そのものが無くなった | — | — |
| 決定 13 | ⛔ **取り下げ**（2026-09-24、`JDG-505`）。起草では「面を開くたびに中身は渡す文書から始める」と決めた。渡す文書も面も無くなった | — | — |
| 決定 14 | ⭐ **`IC-115` の図形は、絵の枠から曲がった矢印が `{ }` へ向かう形とする**（4 節の E-14。起草で起こした図形をそのまま使う）。⚠️ 2026-09-24 改: 置く升は `IC-106` の次ではなく、廃番にする `IC-19` の升（決定 18） | 表 T-109 の前文「図形を持たない行は無い」（MUST）が、行を足す日に図形を求める。`FR-029` は図形を 図 F-019 の正とし、語で書き取らせない。既存の図形を借りると、借りた先の意味と混ざる | 実物と見比べて選び直す義務は 表 T-026 の `RC-13` が持つ —— 当てる者が実物で確かめ、合わなければ図形だけを替える |
| 決定 15 | ⛔ **取り下げ**（2026-09-24、`JDG-505` ・ `JDG-506`）。起草では中身の見出し 2 つを辞書の新しい節 `aiExportContents` に置くと決めた。面が無くなったので見出しも要らない | — | — |
| 決定 16 | ⭐ **生成物は `src/adapter/screen-renderer/image-to-grs-json-prompt.json` の 1 本とする** | 同じ置き場の生成物 `icon-roster.json` ・ `display-words.json` に倣う。表 T-075 はユニットの表であり、生成した JSON に行を持たない | 名を変えるときは原稿の 1 行目の道標（N-01 ・ N-02）も替える |
| 決定 17 | ⭐ **ヘッダーの AI の群の並びは、左から `IC-115` → `IC-20` → `IC-18` とする**（表 T-109 の行の順。E-13）。⭐ 覆してよい | `IC-115` だけが `Agent API` に依らない（日程を 1 字も渡さない）ので、左の文書の群の隣に置く。`IC-18` は `IC-20` の能力の上に立つ見え方なので、「有効にする → 欄を出す」と左から読める並びにした | 利用者の逐語（Q5）の並び（`IC-18` → `IC-20` → `IC-x`）とも、差分が最小の並び（`IC-18` → `IC-115` → `IC-20`、`IC-19` の席に置く）とも違う —— 11 節の問 3 で問う |
| 決定 18 | ⭐ **`IC-19` は廃番にし、行 ID を二度と使わない**（`JDG-506`）。退け方は `CR-369` が `IC-46` ・ `IC-49` を退けた形に倣う —— 表 T-109 の行を消し（番号は振り直さない。規則 02 の「欠番はそのまま残す」）、図 F-019 の図形を消し、辞書の項を消し、`retired.py` に理由つきで入れる（E-24）。⭐ **`IC-115` の図形は `IC-19` の升（`translate(232 56)`）に置く** —— 升の座標は動かさない（`CR-369` の決定 3 と同じ）。⚠️ 図の並びは `IC-18` → `IC-115` → `IC-20` となり、表の並び（決定 17）と違う —— 図と表の並びを照合する検査は無い（`CR-369` の体が測った） | `IC-115` を `IC-106` の次の升に置くと、他の変更要求（`CR-561` の `IC-107` ・ `IC-108` ほか）と同じ升を取り合う。空いた升を埋めれば、図の大きさも最後の升も動かない | 図の名（1 行目の `aria-label`、E-09）には他の変更要求も書く —— 当てる順で数え直す（8 節） |
| 決定 19 | ⛔ **取り下げ**（2026-09-24、`JDG-509`）。この書き直しの途中では「面の名は `AI Export Modal` のまま」と決めた。面そのものが無くなった —— 退け方は決定 23 | — | — |
| 決定 20 | ⭐ **`FR-068` の親を `UC-012` ・ `UC-013` から `UC-011` の新しい拡張 1c へ移す**（E-19 ・ E-22 ・ E-23） | `UC-012` ・ `UC-013` は `Agent API` の使い方であり、プロンプトは `Agent API` を使わない。`FR-027`（初期テンプレート）が `UC-011` の拡張 1a（まだ何も持っていない）から来ているのと同じく、「日程が画像としてしか無い」を `UC-011` の拡張に置く | `UC-011` の名（文書を保存して配る）には、画像から起こす話は少し遠い —— 新しいユースケースを立てるほどの手順を持たない |
| 決定 21 | ⭐ **`JDG-507` を最小の形で書く**: ① `FR-066` に「無効のあいだに `IC-18` が押されたら欄を表示にし、`Agent API` も有効にする（MUST）」を足し、「薄く描かれる」を消す（E-17）。② 状態機械は、領域 `agentApi` に出来事 `enablingAskedByDialogueField` を 1 つ足し（J-10）、`agentApiEnablingStateMachine` に `disabled` → `enabled` の升を 1 つ足す（効果は今ある `storeAgentApiEnabling`。J-11）。領域 `screen` の `dialogueFieldDisplayStateMachine` は、無効のあいだの押しで `RS-35` を告げるのをやめ、`hidden` は無条件に `shown` へ、`shown` は無効のあいだ自己に留まる（J-09）。③ `RS-35` は告げる場面が無くなるので退ける（E-18 ・ J-07 ・ J-12 ・ E-24）。④ `FR-065` と `S-99i` は書き換えない —— `FR-065` の「人が有効にする」は `IC-18` の押しにもそのまま当たり、`S-99i` の「`S-99b` とは別の値」は守られる | 値は 2 つのまま（`FR-066` の MUST NOT）。欄を隠しても `Agent API` は無効にならない —— 無効にする升を置かないので、状態機械がそのまま示す | `IC-18` が押されると 2 つの領域が同じ押しを受ける —— 入力の翻訳が 1 つの押しを 2 つの出来事にする（9 節の X-10） |
| 決定 22 | ⭐ **`IC-115` の行の「何の入口か」は、辞書の説明（Q7）と同じことを短く言う**（「日程の画像をデータ化するプロンプトをクリップボードへ写す」。E-13） | 表 T-109 の行と辞書の語は検査 37 が組で読む —— 同じことを言っていれば、組を 1 度読めば済む | — |
| 決定 23 | ⭐ **AI 出力の面は、表 T-103 の `U-30` から名 `AI Export Modal` と語「AI 出力」を外して退ける。行 ID `U-30` は `Help Modal` の行として残る**（E-27）。面を名指す所 —— 表 T-109 の `IC-52` の `面` の欄（E-28）、表 T-076 の `EP-11`（E-26）、辞書の節 `surfaces` の項（J-14）、状態機械の `surfaceEntryPressed` の入力 `IC-19`（J-08）、表 T-075 の `UF-66`（E-30）—— をすべて同じ回に外す | `U-30` は 2 つの面を 1 行で持つ行であり、`Help Modal` は残る。1 つの ID は 1 つの意味のまま —— 意味が狭まるだけで、別の意味に使い回さない。面の名は行 ID ではないので、`retired.py` に焼く席は無い。辞書の節 `surfaces` の名簿は `IC-52` の `面` の欄から刷られる（`tools/generate_display_words.py`）ので、欄と項は同じ回に外さないと生成器が止まる | `U-30` を引く既存の文（`FR-080` の `EP-11`、状態機械の `surfaceName`）は `Help Modal` だけを指すようになる —— どちらも `Help Modal` を今も指しているので偽にならない |
| 決定 24 | ⭐ **写せたときの告げは 表 T-233 の新しい行 `RS-65`、作法は 表 T-037 の `NT-5`（受け付けたうえで注意を伝えるとき）とする**（E-25 ・ J-13）。語は利用者の文（Q8）を 2 つに分け、辞書の `text` に「クリップボードに貼り付けたプロンプトをAIに渡してください。」、`nextStep` に「冒頭部分は適宜編集してください。」を置く。[OK] は `NT-8` がどの通知にも置く消す入口（辞書の `noticeDismiss`、どの言語でも `OK`）である | 通知の作法は `FR-076` の 1 つであり、「[OK] 付きの告げ」は既に `NT-8` が持つ。新しい作法を作らない。`NT-5` は受け付けた事実と注意を 1 つの面で示し、読み終える前に消えない（`NT-2`）。辞書の理由の項は `text` と `nextStep` の 2 つを持つ決まりであり、`NT-9` が 1 行に並べるので、利用者の 2 文がそのまま続けて読める | 利用者の文を 2 つの升に分けた —— 画面で 2 文が続けて読めることを当てる者が確かめる。英語の 2 つは案（11 節の問 2） |
| 決定 25 | ⭐ **`FR-068` の起点のユニットを、表 T-075 の `UF-66`（`open-modals.ts`）から `UF-48`（`frame-loop.ts`）へ移す**（E-29 ・ E-30。形は `OW-2`） | 面が無くなるので `open-modals.ts` は `FR-068` を負わない。押しを受けてクリップボードへ写し、告げを上げるのは殻であり、操作の記録をクリップボードへ渡す `FR-102` と同じ持ち方である。移すだけなので、`05-07-design.md` の「起点のユニットを名指している要求 99 件」「起点をまだ書いていない要求 17 件」は動かない —— `CR-563` の E-11 ・ E-12 が書き換える同じ文に触れずに済む | — |

---

## 1. 範囲 —— 項目ごとの行き先

| 項目 | 行き先 | 覆すときの戻し方 |
|---|---|---|
| 入口 | 表 T-109 の `App Header` の群 `AI` を `IC-115` ・ `IC-20` ・ `IC-18` の 3 行にする（E-13）。`IC-19` の行を消す（廃番。決定 18）。図 F-019 は `IC-19` の升を `IC-115` の図形に替える（E-14 ・ E-09） | 3 行を元の `IC-18` ・ `IC-19` ・ `IC-20` に戻す。⛔ `IC-19` の番号は戻しても使えない —— 戻すなら新しい番号で行を立てる |
| 写す中身 | `FR-068` の本文: 「プロンプト（表示言語の原稿）＋ 版の 1 行 ＋ スキーマ（最小化）＋ 土台の文書（最小化）」の順に連ねた 1 つの文字列（順は 5 節）。開いている文書を写す道は持たない（E-20） | 段を消す |
| 写し方と告げ | `FR-068` の段: `IC-115` を押すとすぐ写し、写せたら 表 T-233 の `RS-65` を告げる（E-01 ・ E-21 ・ E-02。決定 2 ・ 24）。表 T-233 に `RS-65`（E-25）と辞書の項（J-13） | 段と行と項を消す |
| AI 出力の面 | 退ける（決定 23）: 表 T-103 の `U-30`（E-27）、表 T-109 の `IC-52` の `面` の欄（E-28）、表 T-076 の `EP-11`（E-26）、辞書の節 `surfaces` の項（J-14）、状態機械の `surfaceEntryPressed` の入力（J-08）、表 T-075 の `UF-66` ・ `UF-48`（E-30 ・ E-29） | 名と語を戻す |
| 原稿 | `docs/spec/_source/image-to-grs-json-prompt.ja.md` ・ `.en.md`（新規。役割を 1 行目に宣言する —— 検査 21）。置き場の規則を `05-07-design.md` の 6.2 に 1 段（E-10） | ファイルを消し、6.2 の段を消し、生成器を外す |
| 対話欄と `Agent API`（`JDG-507`） | `FR-066` の段（E-17）、状態機械の 2 領域（J-09 〜 J-12）、`RS-35` を退ける（E-18 ・ J-07 ・ E-24） | 段を消し、`RS-35` は新しい番号で立て直す |
| 経路と親 | 表 T-008 の `CHN-9`（E-15）と `FR-033` の注（E-16）の「`Agent API` へ渡す文書」を「日程の画像をデータ化するプロンプト」に。`UC-011` に拡張 1c（E-19）、`FR-068` の ORIGIN と親（E-22 ・ E-23） | 語を戻し、拡張を消す |
| 生成 | `npm run gen` に 1 つ: 原稿 2 つ ＋ `grs-document.schema.json` ＋ 刷った土台 → `src/adapter/screen-renderer/` の下の生成物 1 つ ＋ 案内の `prompt-*.md` の塊と `grs-skeleton.json` | 生成器を外す |
| 写せなかったとき | 今ある落ち先 `RS-15` をそのまま使う（`frame-loop.ts:4016`。`JDG-509`）。行を足さない | — |
| 開いたときの確かめ | 何も足さない。4.6 節の一覧が今の確かめである。欠けは `DFC-656` ・ `DFC-922` が持つ | — |

## 2. 識別子

⚠️ 規則 02 の 2.5 節: **当てる直前に測り直すこと。**

| 識別子 | 何か | 出どころ |
|---|---|---|
| `IC-115` | 表 T-109 の行: `App Header` の群 `AI`。日程の画像をデータ化するプロンプトを写す入口（E-13） | 取りまとめ役の予約（2026-09-24）。⚠️ `IC-107` ・ `IC-108` は `CR-561`、`IC-109`〜`IC-114` は使い勝手の変更要求のセッションが押さえている —— 「いまの最大の次」で採った番号は、次のコミットで失効する |
| `RS-65` | 表 T-233 の行: 日程の画像をデータ化するプロンプトをクリップボードへ写した（`NT-5`、E-25） | 取りまとめ役の予約（2026-09-24）。⚠️ `RS-62` は `CR-563`、`RS-63` ・ `RS-64` は `CR-565` のものである |
| `IC-19` を廃番にする | 表 T-109 の行（ヘッダーの「AI 出力」）。番号は振り直さず、`retired.py` に理由つきで焼く（決定 18、E-24） | `JDG-506` |
| `RS-35` を廃番にする | 表 T-233 の行（`Agent API` が入っていないので対話欄を出せない）。告げる場面が無くなる（決定 21、E-24） | `JDG-507` |
| 面の名 `AI Export Modal` を外す | 表 T-103 の `U-30` の 2 つ目の名。行 ID ではないので焼く席は無い。`U-30` は `Help Modal` の行として残る（決定 23） | `JDG-509` |
| 辞書の項 | 節 `icons` の `IC-115` を足し、`IC-19` を消す（J-06）。節 `reasons` の `RS-35` を消し（J-07）、`RS-65` を足す（J-13）。節 `surfaces` の `AI Export Modal` を消す（J-14）。語は `display-words.json` だけが持つ（`FR-038`） | 日本語は `JDG-504`（名）・ `JDG-508`（説明）・ `JDG-509`（告げ）の利用者の語。英語の 4 つは 11 節の問 2 |
| 出来事 `enablingAskedByDialogueField` | 領域 `agentApi` の出来事（J-10）。識別子ではなく名なので予約は要らない（`git grep` で 0 件 —— 13 節の 2） | 決定 21 |
| 原稿 2 つ ＋ 生成器 1 つ | 決定 3 ・ 決定 16 | — |
| ⛔ 使わなくなった名 | 起草の出来事 `aiExportContentToggled` ・ 状態機械 `aiExportContentStateMachine` ・ 辞書の節 `aiExportContents` は、木に一度も入っていない（13 節の 2）ので、焼く席は無い | 決定 12 ・ 13 ・ 15 の取り下げ |

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 旧 | 新 | 編集 |
|---|---|---|
| `FR-068`: 開いている文書（`GRS JSON`）を面で確かめて写す | `IC-115` を押すと、日程の画像をデータ化するプロンプトをすぐ写し、`RS-65` を告げる。開いている文書を写す道は持たない —— 保存した `.json` を AI に添える | E-01 ・ E-20 ・ E-21 ・ E-02 |
| `FR-068` の「MSPDI を渡してはならない」 | 消す —— 渡す文書が無くなった。AI に返させる形は `GRS JSON` だけと E-20 が定める | E-20 |
| `FR-068` の親 `UC-012` ・ `UC-013` | `UC-011` の拡張 1c | E-19 ・ E-22 ・ E-23 |
| `FR-068` の起点 `UF-66`（`open-modals.ts`） | `UF-48`（`frame-loop.ts`） | E-29 ・ E-30 |
| 表 T-103 の `U-30`: `Help Modal` / `AI Export Modal`（ヘルプ / AI 出力） | `Help Modal`（ヘルプ） | E-27 |
| 表 T-109 の `IC-52` の `面` の欄の `AI Export Modal` | 外す | E-28 |
| 表 T-076 の `EP-11` の「`Help Modal` ・ `AI Export Modal`（`U-30`）」 | 「`Help Modal`（`U-30`）」 | E-26 |
| 辞書の節 `surfaces` の `AI Export Modal`（「AI 出力」） | 消す | J-14 |
| 表 T-109 の `IC-18` ・ `IC-19` ・ `IC-20` の 3 行 | `IC-115` ・ `IC-20` ・ `IC-18`（`IC-19` は廃番） | E-13 |
| 図 F-019 の `IC-19` の図形（顔のある書類） | 同じ升に `IC-115` の図形 | E-14 ・ E-09 |
| 辞書の `IC-19` の項（「AI 出力」） | `IC-115` の項。`IC-20` は並びだけ替える。`IC-18` は並びを替え、説明の語に「Agent API が無効なら有効にする」を足す（`JDG-510`） | J-06 |
| 状態機械の `surfaceEntryPressed` の入力 `IC-19` | 外す（`IC-115` は面を立てない） | J-08 |
| 表 T-008 の `CHN-9` と `FR-033` の注の「`Agent API` へ渡す文書（`FR-068`）」 | 「日程の画像をデータ化するプロンプト（`FR-068`）」 | E-15 ・ E-16 |
| `FR-066` の「`Agent API` が無効のあいだ `IC-18` は薄く描かれる」 | 無効のあいだに押されたら欄を表示にし、`Agent API` も有効にする。薄く描かない | E-17 |
| 対話欄の状態機械の、無効のあいだの押し（`RS-35` を告げて留まる） | `hidden` → `shown`、`shown` は自己。`agentApi` の側で `disabled` → `enabled` | J-09 〜 J-11 |
| 表 T-233 の `RS-35` と辞書の項、`agentApiEnablingStateMachine.disabled` の根拠の `RS-35` | 消す（廃番） | E-18 ・ J-07 ・ J-12 ・ E-24 |
| `JDG-503` の問 4 の形（面の中身の切替、起草の E-04 〜 E-08 ・ E-11 ・ E-12 ・ J-01 〜 J-05） | 取り下げ（4 節の前文の表） | — |
| 案内の使い方 1〜2（`prompt-ja.md:9`〜`:12`、`prompt-en.md` の同じ所）: 2 つのファイルを落として添える | 「`GRS` のヘッダーの `IC-115` を押す（写る）」 | X-5 |
| プロンプトの 添付（`prompt-ja.md:24`〜`:27`）: 土台とスキーマは添付 | 土台とスキーマはプロンプトの末尾に埋まっている | N-01 ・ N-02 |
| プロンプトの「自由」の色 `"#RRGGBB"`（`prompt-ja.md:35`） | `"#rrggbb/"`（`CR-548` のカスタムカラー。明るいテーマの値だけ） | N-01 ・ N-02 |
| 手で保つ `grs-skeleton.json` | 刷った土台（決定 5） | X-2 |
| 案内の「できた JSON を確かめる」の Python（`prompt-ja.md:217`〜`:231`） | 開発者向けの注へ移す。利用者向けには「`GRS` で開き、告げが出たら AI にその語を渡して直させる」 | X-5 |

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-553` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（13 節の 5 の道具が E- ・ J- の 31 件すべてで 1 回を確かめ、写しに順に当てた）。旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である。
⚠️ `47be689f` では、当てる 7 つのファイル（`01-04-requirements.md` ・ `05-07-design.md` ・ `_assets/tbl-glossary.md` ・ `_assets/fig-icons.svg` ・ `_source/display-words.json` ・ `_source/state-machines.json` ・ `retired.py`）はどれも LF である。当てる木が CRLF なら、`\r\n` を `\n` にしてから数え、書くときに多いほうへ戻すこと。
⚠️ 行末の半角空白 2 つ（`  `）は旧・新の一部である（E-17 ・ E-19 ・ E-20 ・ E-21 ・ E-02 ・ E-10）。
⚠️ J-06 〜 J-14 を当てたら `json.loads` で読めることを確かめ、`npm run gen` で生成物を刷る（生成物を手で直さない）。⛔ **J-06 と J-13 の英語の 4 つ（`IC-115` の `label.en` ・ `hint.en`、`RS-65` の `text.en` ・ `nextStep.en`）は案である** —— 辞書の `$comment` が体の起こした語を禁じているので、11 節の問 2 の答えが出るまで当てない（日本語は `JDG-504` ・ `JDG-508` ・ `JDG-509` の利用者の語である）。
⚠️ N-01 ・ N-02 はファイルを新しく作る編集であり、旧を持たない。

⚠️ **取り下げた編集**（番号は再び使わない）:

| 編集 | 起草の日の中身 | 取り下げた理由 |
|---|---|---|
| E-03 | `IC-19` の行にプロンプトを書き足す | 起草の日に取り下げた（辞書との組が検査 37 で赤になる） |
| E-04 | 表 T-109 の `IC-52` の次に `IC-115`（面の中身の切替）を足す | `JDG-506`: `IC-115` はヘッダーの行になった —— E-13 が替わる |
| E-05 〜 E-07 ・ E-11 | 表 T-109 の行数 97 → 98（前文 3 箇所と `FR-086`） | `IC-19` を消して `IC-115` を足すので、行数は 97 のまま |
| E-08 | `IC-115` の図形を `IC-106` の次の升に足す | 決定 18: `IC-19` の升に置く —— E-14 が替わる |
| E-12 | `FR-036` のヘルプで `IC-19` の下に `IC-115` を字下げして並べる | `IC-115` は `App Header` の行なので、`App Header` の塊にそのまま載る（13 節の 5 で `generate_help_roster.py` が書けた） |
| J-01 | 辞書の `icons` に `IC-115`（切替の語）を足す | 語が `JDG-504` ・ `JDG-508` で替わり、置く所も替わった —— J-06 が替わる |
| J-02 ・ J-03 | 辞書の `$comment` と新しい節 `aiExportContents`（中身の見出し 2 つ） | 決定 15 の取り下げ |
| J-04 ・ J-05 | 領域 `screen` の出来事 `aiExportContentToggled` と状態機械 `aiExportContentStateMachine` | 決定 12 ・ 13 の取り下げ（切り替える値が無い） |

⚠️ 当てる順: E- と J- は互いに行を分け合わない（13 節の 5）。`CR-563` の旧とも行を分け合わないが、**隣り合う所が 2 つある**（E-25 と J-13 —— 8 節）。

### 4.1 要求（`01-04-requirements.md`）

<!-- EDIT id=E-15 file=docs/spec/01-04-requirements.md -->
表 T-008 の `CHN-9` の行（クリップボードへ出るもの）。旧
```text
画像、`Agent API` へ渡す文書（`FR-068`）、および操作と描画の記録（`FR-102`）
```
新
```text
画像、日程の画像をデータ化するプロンプト（`FR-068`）、および操作と描画の記録（`FR-102`）
```

<!-- EDIT id=E-19 file=docs/spec/01-04-requirements.md -->
`UC-011` の拡張に 1c を足す（決定 20）。手順の番号は変えない。旧
```text
- 1b. 作成者が、開いている文書を捨てて新しく始める。
  - `GRS` は未保存の編集を確認したうえで文書を捨て、初期表示用のテンプレートを出す。
```
新
```text
- 1b. 作成者が、開いている文書を捨てて新しく始める。
  - `GRS` は未保存の編集を確認したうえで文書を捨て、初期表示用のテンプレートを出す。
- 1c. 作成者の日程が、画像としてしか無い（作図ソフトや表計算ソフトで描いた絵など）。
  - `GRS` は、その画像から `GRS JSON` を AI に作らせるプロンプトを写し、写したことを告げる。  
    作成者は画像と一緒にプロンプトを AI へ渡し、返った JSON を保存して「開く」で開く。
```

<!-- EDIT id=E-16 file=docs/spec/01-04-requirements.md -->
`FR-033` の注（クリップボードを読まないことの段）。旧
```text
`Agent API` へ渡す文書は `FR-068`
```
新
```text
日程の画像をデータ化するプロンプトは `FR-068`
```

<!-- EDIT id=E-26 file=docs/spec/01-04-requirements.md -->
`FR-080` の 表 T-076（書き出しで描く UI パーツと描かない UI パーツ）の `EP-11`（重ねて出す面）から、退ける面の名を外す（決定 23）。旧
```text
`Help Modal` ・ `AI Export Modal`（`U-30`）
```
新
```text
`Help Modal`（`U-30`）
```

<!-- EDIT id=E-17 file=docs/spec/01-04-requirements.md -->
`FR-066`（`JDG-507`、決定 21）。「薄く描かれる」の段を、無効のあいだの押しの規則に替える。1 つ目の行（1 つの値で兼ねない）は変えない。⚠️ `CR-563` の E-08 の旧（`FR-066` の見出しから UID まで）の 3 行下である —— 行は分け合わない（8 節）。旧
```text
⛔ `Agent API` の有効・無効（`FR-065`）と 1 つの値で兼ねてはならない（MUST NOT） —— 能力と見え方は別の概念であり、兼ねると `Agent API` を有効にしただけで `IC-18` が押された状態になる。  
⚠️ **`Agent API` が無効のあいだ `IC-18` は薄く描かれる** —— 規則は `FR-029` が、告げる理由は同要求の 表 T-233 の `RS-35` が持つ。  
```
新
```text
⛔ `Agent API` の有効・無効（`FR-065`）と 1 つの値で兼ねてはならない（MUST NOT） —— 能力と見え方は別の概念であり、兼ねると `Agent API` を有効にしただけで `IC-18` が押された状態になる。  
⭐ `Agent API` が無効のあいだに `IC-18` が押されたときは、欄を表示にし、`Agent API` も有効にすること（MUST） —— 人が押す操作なので、`FR-065` の「人が有効にする」に当たる。  
値は 2 つのままである —— 欄の見え方は `S-99i` に、有効化は `FR-065` の記憶に、別々に書く。  
⚠️ 逆向きは無い —— 欄を非表示にしても `Agent API` は有効のままである。  
有効・無効を切り替える入口は 表 T-109 の `IC-20` 1 つであり、`IC-18` は `Agent API` を無効にしない。  
⚠️ これは `FR-029` の「同じ機能の入口を 2 か所に置かない」に当たらない —— `IC-18` の職務は欄を出すことであり、有効にするのはその前提を満たすためである。  
⚠️ そのため `IC-18` は、`Agent API` が無効のあいだも薄く描かない —— 押せば欄が出るので、`FR-029` の「何も変えられない」に当たらない。  
```

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
`FR-068` の見出しと STATEMENT（決定 2）。旧
```text
#### AI へ渡す文書を画面で確かめる

**Type**: FUNC_REQ
**UID**: FR-068

**STATEMENT**: 作成者が求めたとき、`GRS` は、AI へ渡す文書を画面上で確認できるようにし、それをクリップボードへ複写できるようにすること。
```
新
```text
#### 日程の画像をデータ化するプロンプトを写す

**Type**: FUNC_REQ
**UID**: FR-068

**STATEMENT**: 作成者が求めたとき、`GRS` は、日程の画像から `GRS JSON` を AI に作らせるプロンプトをクリップボードへ複写し、複写したことを告げること。
```

<!-- EDIT id=E-20 file=docs/spec/01-04-requirements.md -->
`FR-068` の最初の段。AI に返させる形・文書を写さないこと・プロンプトの組み立て・読まないもの。旧
```text
⭐ 渡す文書は、表 T-024 の `GRS JSON` そのものとすること（MUST） —— ⛔ **この面のために新しい交換形式を鋳てはならない（MUST NOT）。**  
⭐ `FR-024` が「その JSON から同じ見た目が再現できるだけの情報」と定めているので、渡す情報に欠けがない。  
⛔ **MSPDI を渡してはならない（MUST NOT）** —— **注記を運ばないので、付箋について問われても答えられない。**
```
新
```text
⭐ AI に返させる形は、表 T-024 の `GRS JSON` そのものとすること（MUST） —— ⛔ **このプロンプトのために新しい交換形式を鋳てはならない（MUST NOT）。**  
返った文書は 表 T-024a の `OP-2` の「開く」1 つから入る。  
⚠️ 開いている文書を AI のために写す道は持たない —— 日程を AI へ渡すときは、保存した `GRS JSON` のファイルを AI に添える。  
⚠️ 代償: まだ保存していない編集は、そのファイルに入らない。

⭐ プロンプトは、写した瞬間の表示言語（`FR-038`）の原稿から作ること（MUST）。  
原稿は言語ごとに 1 つとし、置き場は `05-07-design.md` の 6.2 が定める。  
⚠️ プロンプトは画面の語ではなく、渡す中身である —— `FR-038` の辞書に置かない。  
⭐ 写すプロンプトは、(1) 表示言語の原稿、(2) 版の 1 行（`schemaVersion:` と 1 字の空白に、初期テンプレート（`FR-027`）の `schemaVersion` の値を続けたもの）、(3) `GRS JSON` のスキーマ（`05-07-design.md` の 6.2 が起こす `_source/grs-document.schema.json`）、(4) 土台の文書、の 4 つをこの順に空行で区切って連ねた 1 つの文字列とすること（MUST）。  
(3) と (4) は空白を除いた 1 行の JSON とし、それぞれ `json` の囲みに入れる。  
⭐ 土台の文書は、初期テンプレートと同じ版と同じ `documentSettings` を持ち、タスクを 1 つも持たない `GRS JSON` とし、初期テンプレートと同じ生成器が同じ回に起こすこと（MUST） —— 版が揃うので、返った文書を開いても版の違いによる告げも読み替えも起きない。  
⛔ `GRS` は、AI が答えに添える「推定したこと」の表を読んではならない（MUST NOT） —— 表は JSON の外に在り、読めばそれが新しい交換形式になる。
```

<!-- EDIT id=E-21 file=docs/spec/01-04-requirements.md -->
`FR-068` の面と複写の段を、入口とすぐ写すことと告げに替える（決定 2 ・ 24）。旧
```text
⭐ 面にはその文書を読める形で出すこと（MUST）。  
題と閉じる入口だけにしてはならない（MUST NOT）

⭐ 複写の入口は 表 T-109 の `IC-52` とすること（MUST）。  
新しい行を足してはならない（MUST NOT） —— **同じ機能の入口を増やさない**（`FR-029`）。  
その行は既にこの面に在る。
```
新
```text
⭐ 入口は 表 T-109 の `IC-115` とし、押されたときに面を開かずにプロンプトを複写すること（MUST）。  
⭐ 複写できたときは、複写したことと次に行うことを告げること（MUST） —— 運ぶ理由は 表 T-233 の `RS-65` とする。  
⚠️ 押しても画面の絵は何も変わらないので、告げなければ押しが効いたかどうかが読めない。
```

<!-- EDIT id=E-22 file=docs/spec/01-04-requirements.md -->
`FR-068` の ORIGIN（決定 20）。旧
```text
**ORIGIN**: UC-012 / UC-013
```
新
```text
**ORIGIN**: UC-011 拡張 1c
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
`FR-068` の RATIONALE（決定 2）。旧
```text
**RATIONALE**: 何を渡しているか分からないまま渡すのは、`Agent API` を既定で公開しないという判断と釣り合わない。
```
新
```text
**RATIONALE**: 面を開かずにすぐ写すのは、プロンプトが日程を 1 字も持たないからである —— 写す前に見せる理由（何を渡しているか分からないまま渡させない）が当たらない。  
⭐ プロンプトにスキーマと土台の文書を埋めるのは、利用者がアプリの外のファイルを落として添えずに済むようにするためである —— 添えさせると手順が 2 つ増え、URL で指すと造りと版がずれる。  
⭐ 開いている文書を写す道を持たないのは、日程を AI へ渡す道を、保存したファイルを添える 1 つにするためである。
```

<!-- EDIT id=E-23 file=docs/spec/01-04-requirements.md -->
`FR-068` の親（決定 20）。次の要求の見出しまでを錨にする。旧
```text
- **Type**: `Parent`
  **ID**: `UC-012`
  **Role**: `Satisfies`
- **Type**: `Parent`
  **ID**: `UC-013`
  **Role**: `Satisfies`

#### ライセンスと帰属表示を成果物に埋め込む
```
新
```text
- **Type**: `Parent`
  **ID**: `UC-011`
  **Role**: `Satisfies`

#### ライセンスと帰属表示を成果物に埋め込む
```

<!-- EDIT id=E-18 file=docs/spec/01-04-requirements.md -->
表 T-233 の `RS-35` を退ける（決定 21）。前の行 `RS-34` を錨にし、`RS-34` は変えない。次の行 `RS-36` は `RS-35` を継いでいない（「同・」は無い —— 規則 02 の罠）。旧
```text
| RS-34 | 揃える相手の `Task` が選ばれていない | `NT-1` | `FR-034` |
| RS-35 | `Agent API` が入っていないので、対話欄を出せない | `NT-1` | `FR-066` |
```
新
```text
| RS-34 | 揃える相手の `Task` が選ばれていない | `NT-1` | `FR-034` |
```

<!-- EDIT id=E-25 file=docs/spec/01-04-requirements.md -->
表 T-233 に `RS-65` を足す（決定 24）。落ち先の結びの行 `RS-15` の前に置き、`RS-15` は変えない。⚠️ `CR-563` の E-10 の旧（`RS-61` の行）のすぐ次の行である —— 8 節。旧
```text
| RS-15 | この理由にまだ行が無い | `NT-3a` | 本表 |
```
新
```text
| RS-65 | 日程の画像をデータ化するプロンプトをクリップボードへ写した | `NT-5` | `FR-068` |
| RS-15 | この理由にまだ行が無い | `NT-3a` | 本表 |
```

### 4.2 用語集（`_assets/tbl-glossary.md`）と 図 F-019（`_assets/fig-icons.svg`）

⚠️ 表 T-109 は手書きの原稿である（`_assets/tbl-glossary.md`。検査 21 の生成物の一覧に無い）。`src/adapter/screen-renderer/icon-roster.json` はそこから刷る生成物なので、旧は表に取る。図 F-019 も原稿である。
⭐ 表 T-109 の前文の「97 行」（3 箇所）と `FR-086` の「表 T-109 の 97 行」は変えない —— 1 行消して 1 行足す。図形も 97 のまま。

<!-- EDIT id=E-27 file=docs/spec/_assets/tbl-glossary.md -->
表 T-103 の `U-30` から、退ける面の名と語を外す（決定 23）。⚠️ `impact.py` は 表 T-103 を塊「命名と置き場の規約」に数える —— 塊の他の行は名を引かないことを確かめた（13 節の 2）。旧
```text
| U-30 | `Help Modal` / `AI Export Modal` | ヘルプ / AI 出力 |
```
新
```text
| U-30 | `Help Modal` | ヘルプ |
```

<!-- EDIT id=E-13 file=docs/spec/_assets/tbl-glossary.md -->
群 `AI` の 3 行（決定 17 ・ 18 ・ 22）。`IC-19` を消し、`IC-115` を先頭に足し、`IC-20` と `IC-18` の順を入れ替える。`IC-18` ・ `IC-20` の行の文は 1 字も変えない（辞書との組が動かない）。旧
```text
| IC-18 | `App Header` | AI | AI との対話欄を表示する・非表示にする | `FR-066` | — |
| IC-19 | `App Header` | AI | AI へ渡す文書を画面で確かめて写す | `FR-068` | — |
| IC-20 | `App Header` | AI | `Agent API` を有効にする・無効にする | `FR-065` | — |
```
新
```text
| IC-115 | `App Header` | AI | 日程の画像をデータ化するプロンプトをクリップボードへ写す | `FR-068` | — |
| IC-20 | `App Header` | AI | `Agent API` を有効にする・無効にする | `FR-065` | — |
| IC-18 | `App Header` | AI | AI との対話欄を表示する・非表示にする | `FR-066` | — |
```

<!-- EDIT id=E-28 file=docs/spec/_assets/tbl-glossary.md -->
`IC-52` の `面` の欄から、退ける面を外す（決定 23）。辞書の節 `surfaces` の名簿はこの欄から刷られる —— J-14 と同じ回に当てる。旧
```text
| IC-52 | `Help Modal` / `AI Export Modal` / `Resource Roster` /
```
新
```text
| IC-52 | `Help Modal` / `Resource Roster` /
```

<!-- EDIT id=E-14 file=docs/spec/_assets/fig-icons.svg -->
`IC-19` の図形を消し、同じ升に `IC-115` の図形を置く（決定 14 ・ 18）。24 × 24 の格子の上に、他の図形と同じ `class="s"`（線の太さ 2）で起こした。旧
```text
  <g transform="translate(232 56)">
    <path class="s" d="M6 3.5 h7.5 L19 9 v9.5 a1.5 1.5 0 0 1 -1.5 1.5 H6 a1.5 1.5 0 0 1 -1.5 -1.5 V5 A1.5 1.5 0 0 1 6 3.5 Z"/>
    <path class="s" d="M13.5 3.5 V9 H19"/>
    <circle class="f" cx="9.6" cy="13.6" r="0.95"/>
    <circle class="f" cx="14" cy="13.6" r="0.95"/>
    <path class="s" d="M9.4 16.1 q2.2 2 4.4 0"/>
  </g>
  <text class="lbl" x="244" y="90">IC-19</text>
```
新
```text
  <g transform="translate(232 56)">
    <rect class="s" x="2.5" y="3.5" width="10" height="8.5" rx="1.5"/>
    <path class="s" d="M4.5 10 L7 7.5 L8.8 9.3 L10.2 8 L11 8.8"/>
    <path class="s" d="M7.5 14.5 V17.5 A1.5 1.5 0 0 0 9 19 H13"/>
    <path class="s" d="M11 17 L13 19 L11 21"/>
    <path class="s" d="M17.5 12.5 Q16 12.5 16 14 V15.8 Q16 17 15 17 Q16 17 16 18.2 V20 Q16 21.5 17.5 21.5"/>
    <path class="s" d="M20 12.5 Q21.5 12.5 21.5 14 V15.8 Q21.5 17 22.5 17 Q21.5 17 21.5 18.2 V20 Q21.5 21.5 20 21.5"/>
  </g>
  <text class="lbl" x="244" y="90">IC-115</text>
```

<!-- EDIT id=E-09 file=docs/spec/_assets/fig-icons.svg -->
図の名（1 行目）。⚠️ 他の変更要求（`CR-561` ほか）も同じ語を書き換える —— 当てる時点の範囲に書き直す（例: `IC-107` 〜 `IC-114` が先に着いたなら `IC-1 to IC-115 (IC-19, IC-46 and IC-49 retired)`）。旧
```text
aria-label="GRS icon glyphs IC-1 to IC-106 (IC-46 and IC-49 retired)"
```
新
```text
aria-label="GRS icon glyphs IC-1 to IC-106 and IC-115 (IC-19, IC-46 and IC-49 retired)"
```

### 4.3 設計（`05-07-design.md`）

<!-- EDIT id=E-29 file=docs/spec/05-07-design.md -->
表 T-075 の `UF-48`（`frame-loop.ts`）の「負う要求」の欄に `FR-068` を足す（決定 25）。欄の頭の並びは変えない。旧
```text
`FR-100`（`OW-2`）・`FR-102`（`OW-2`）
```
新
```text
`FR-068`（`OW-2`）・`FR-100`（`OW-2`）・`FR-102`（`OW-2`）
```

<!-- EDIT id=E-30 file=docs/spec/05-07-design.md -->
表 T-075 の `UF-66`（`open-modals.ts`）から `FR-068` を外す（決定 23 ・ 25）。旧
```text
| UF-66 | `ScreenRenderer` | `open-modals.ts` | `pure` | 重ねて開く面（定義は 表 T-028 の `IN-4`）—— `FR-036` / `FR-074` / `FR-099` / `FR-088` / `FR-068` | `FR-036`（`OW-2`）・`FR-068`（`OW-2`）・`FR-074`（`OW-2`）・`FR-099`（`OW-2`） |
```
新
```text
| UF-66 | `ScreenRenderer` | `open-modals.ts` | `pure` | 重ねて開く面（定義は 表 T-028 の `IN-4`）—— `FR-036` / `FR-074` / `FR-099` / `FR-088` | `FR-036`（`OW-2`）・`FR-074`（`OW-2`）・`FR-099`（`OW-2`） |
```

<!-- EDIT id=E-10 file=docs/spec/05-07-design.md -->
6.2 の画面に刷る語の原稿の段の後に、プロンプトの原稿の段を足す（決定 3 ・ 決定 16）。旧
```text
**語が届く先は `src/` の生成物 1 本とし（MUST）**、その素性は下の道標の規則に従う。
```
新
```text
**語が届く先は `src/` の生成物 1 本とし（MUST）**、その素性は下の道標の規則に従う。

⭐ 日程の画像をデータ化するプロンプト（`01-04-requirements.md` の `FR-068`）の原稿は、`_source/image-to-grs-json-prompt.ja.md` と `_source/image-to-grs-json-prompt.en.md` の 2 つとする（MUST） —— 言語ごとに 1 つであり、言語を足す作業は原稿を 1 つ足す記入で済む。  
⛔ 原稿に `GRS JSON` のスキーマと土台の文書を写してはならない（MUST NOT） —— どちらも生成器が原稿の後ろへ連ねる（`FR-068`）。  
写せば、スキーマの原稿（上の「起こす原稿は 2 つ」）と初期テンプレート（`FR-027`）から離れる。  
⚠️ 原稿は 1 行目で、唯一の正であることと、起こす生成物と、作り直し方を名乗る。  
その 1 行はプロンプトに入れない。  
**プロンプトが届く先は `src/` の生成物 1 本とし（MUST）**、その素性は下の道標の規則に従う。
```

### 4.4 原稿 JSON（`_source/display-words.json` ・ `_source/state-machines.json`）

<!-- EDIT id=J-06 file=docs/spec/_source/display-words.json -->
節 `icons` の `IC-18` ・ `IC-19` ・ `IC-20` の 3 項を、`IC-115` ・ `IC-20` ・ `IC-18` にする（節の並びは 表 T-109 の印字順 —— E-13）。`IC-19` の項を消す。`IC-20` の語は 1 字も変えない。`IC-18` は `hint` だけを `JDG-510` の語にする（`label` は変えない）。⭐ `IC-115` の `label.ja` は `JDG-504`、`hint.ja` は `JDG-508` の利用者の語のまま（製品の名は仕様の背景 `01-04-requirements.md:22` が既に例として書いている）。⛔ `label.en` ・ `hint.en` は案（11 節の問 2。`hint.en` は日本語の文を語順のまま写した）。旧
```text
   "rowId": "IC-18",
   "label": {
    "ja": "対話欄",
    "en": "Dialogue Field"
   },
   "hint": {
    "ja": "AI との対話欄を表示する。もう一度押すと非表示にする",
    "en": "Show the Dialogue Field for talking with the AI; press again to hide it"
   }
  },
  {
   "rowId": "IC-19",
   "label": {
    "ja": "AI 出力",
    "en": "AI Export"
   },
   "hint": {
    "ja": "AI へ渡す文書を画面で確かめて写す",
    "en": "Check the document to hand to the AI on screen, then copy it"
   }
  },
  {
   "rowId": "IC-20",
   "label": {
    "ja": "Agent API",
    "en": "Agent API"
   },
   "hint": {
    "ja": "Agent API を有効にする。もう一度押すと無効にする",
    "en": "Enable the Agent API; press again to disable it"
   }
```
新
```text
   "rowId": "IC-115",
   "label": {
    "ja": "日程の画像をデータ化するプロンプト",
    "en": "Prompt to Turn a Schedule Image into Data"
   },
   "hint": {
    "ja": "AIにPPTやExcelなどで作った日程をGoodRelax Schedulerで処理できるJSONデータに変換させるプロンプトをクリップボードに出力する",
    "en": "Copy to the clipboard a prompt that has an AI convert a schedule made in PowerPoint, Excel, etc. into JSON data GoodRelax Scheduler can process"
   }
  },
  {
   "rowId": "IC-20",
   "label": {
    "ja": "Agent API",
    "en": "Agent API"
   },
   "hint": {
    "ja": "Agent API を有効にする。もう一度押すと無効にする",
    "en": "Enable the Agent API; press again to disable it"
   }
  },
  {
   "rowId": "IC-18",
   "label": {
    "ja": "対話欄",
    "en": "Dialogue Field"
   },
   "hint": {
    "ja": "AI との対話欄を表示する（Agent API が無効なら有効にする）。もう一度押すと非表示にする",
    "en": "Show the Dialogue Field for talking with the AI (turns the Agent API on if it is off); press again to hide it"
   }
```

<!-- EDIT id=J-14 file=docs/spec/_source/display-words.json -->
節 `surfaces` の `AI Export Modal` の項を消す（決定 23。E-28 と同じ回に当てる）。次の項 `Resource Roster` を錨にし、変えない。旧
```text
  {
   "name": "AI Export Modal",
   "heading": {
    "ja": "AI 出力",
    "en": "AI Export Modal"
   }
  },
  {
   "name": "Resource Roster",
```
新
```text
  {
   "name": "Resource Roster",
```

<!-- EDIT id=J-07 file=docs/spec/_source/display-words.json -->
節 `reasons` の `RS-35` の項を消す（E-18）。次の項 `RS-36` を錨にし、`RS-36` は変えない。旧
```text
  {
   "rowId": "RS-35",
   "text": {
    "ja": "対話欄は Agent API が入っているあいだだけ出せます",
    "en": "The dialogue field is only available while the Agent API is on"
   },
   "nextStep": {
    "ja": "Agent API を入れてから、もう一度押してください",
    "en": "Turn the Agent API on first, then press again"
   }
  },
  {
   "rowId": "RS-36",
```
新
```text
  {
   "rowId": "RS-36",
```

<!-- EDIT id=J-13 file=docs/spec/_source/display-words.json -->
節 `reasons` に `RS-65` の項を足す（E-25、決定 24）。並びは 表 T-233 と同じく結びの `RS-15` の前。⭐ `text.ja` と `nextStep.ja` は `JDG-509` の利用者の文を 2 つに分けたもの（1 字も変えていない）。⛔ `text.en` ・ `nextStep.en` は案（11 節の問 2）。⚠️ `CR-563` の J-03 の旧（`RS-61` の項の末尾）のすぐ次の行である —— 8 節。旧
```text
  {
   "rowId": "RS-15",
```
新
```text
  {
   "rowId": "RS-65",
   "text": {
    "ja": "クリップボードに貼り付けたプロンプトをAIに渡してください。",
    "en": "Paste the prompt from the clipboard into an AI."
   },
   "nextStep": {
    "ja": "冒頭部分は適宜編集してください。",
    "en": "Edit its opening part as needed."
   }
  },
  {
   "rowId": "RS-15",
```

<!-- EDIT id=J-08 file=docs/spec/_source/state-machines.json -->
領域 `screen` の出来事 `surfaceEntryPressed` の入力から `IC-19` を外す（決定 23）。`IC-115` は面を立てないので足さない —— 状態を持たない押しは原稿に置かない（`IC-3` と同じ）。旧
```text
       "SK-13",
       "IC-19",
```
新
```text
       "SK-13",
```

<!-- EDIT id=J-09 file=docs/spec/_source/state-machines.json -->
`dialogueFieldDisplayStateMachine` の `dialogueFieldEntryPressed` の升（決定 21）。無効のあいだの押しで `RS-35` を告げるのをやめる。`hidden` は無条件に `shown` へ、`shown` は無効のあいだ自己に留まる（欄は表示のまま。同じ押しで `Agent API` が有効になるので欄が出る）。旧
```text
      "dialogueFieldEntryPressed": {
       "shown": [
        {
         "to": "hidden",
         "guard": [
          {
           "name": "isAgentApiEnabled"
          }
         ],
         "evidence": [
          "S-99i",
          "IC-18"
         ]
        },
        {
         "to": "shown",
         "guard": [
          {
           "name": "isAgentApiEnabled",
           "not": true
          }
         ],
         "effect": "raiseNotice",
         "effectArgument": "RS-35",
         "evidence": [
          "FR-066",
          "RS-35"
         ]
        }
       ],
       "hidden": [
        {
         "to": "shown",
         "guard": [
          {
           "name": "isAgentApiEnabled"
          }
         ],
         "evidence": [
          "S-99i",
          "IC-18"
         ]
        },
        {
         "to": "hidden",
         "guard": [
          {
           "name": "isAgentApiEnabled",
           "not": true
          }
         ],
         "effect": "raiseNotice",
         "effectArgument": "RS-35",
         "evidence": [
          "FR-066",
          "RS-35"
         ]
        }
       ]
      }
```
新
```text
      "dialogueFieldEntryPressed": {
       "shown": [
        {
         "to": "hidden",
         "guard": [
          {
           "name": "isAgentApiEnabled"
          }
         ],
         "evidence": [
          "S-99i",
          "IC-18"
         ]
        },
        {
         "to": "shown",
         "guard": [
          {
           "name": "isAgentApiEnabled",
           "not": true
          }
         ],
         "evidence": [
          "FR-066",
          "IC-18"
         ],
         "note": {
          "ja": "欄は表示のまま。同じ押しで `Agent API` が有効になるので欄が出る（`agentApi/enablingAskedByDialogueField`）"
         }
        }
       ],
       "hidden": {
        "to": "shown",
        "evidence": [
         "S-99i",
         "IC-18",
         "FR-066"
        ],
        "note": {
         "ja": "`Agent API` が無効なら、同じ押しで有効にもなる（`agentApi/enablingAskedByDialogueField`）"
        }
       }
      }
```

<!-- EDIT id=J-10 file=docs/spec/_source/state-machines.json -->
領域 `agentApi` の出来事に、`IC-18` の押しを足す（決定 21。`rememberedEnablingLoaded` の前）。原稿は同じ鍵を 2 つの領域で使うことを断るので、`screen` の `dialogueFieldEntryPressed` とは別の鍵にする。旧
```text
    {
     "key": "rememberedEnablingLoaded",
```
新
```text
    {
     "key": "enablingAskedByDialogueField",
     "source": {
      "kind": "input",
      "rows": [
       "IC-18",
       "FR-066"
      ],
      "note": {
       "ja": "`IC-18` を押した。`Agent API` が無効なら有効にする —— 対話欄の入口は `Agent API` を無効にしない"
      }
     },
     "carries": []
    },
    {
     "key": "rememberedEnablingLoaded",
```

<!-- EDIT id=J-11 file=docs/spec/_source/state-machines.json -->
`agentApiEnablingStateMachine` の升を 1 つ足す（決定 21。`rememberedEnablingLoaded` の升の前）。`enabled` の升は置かない —— 置かない升は「変わらない」である。旧
```text
      "rememberedEnablingLoaded": {
```
新
```text
      "enablingAskedByDialogueField": {
       "disabled": {
        "to": "enabled",
        "effect": "storeAgentApiEnabling",
        "evidence": [
         "FR-065",
         "FR-066",
         "IC-18"
        ],
        "note": {
         "ja": "有効をブラウザ（オリジン）の記憶に書く"
        }
       }
      },
      "rememberedEnablingLoaded": {
```

<!-- EDIT id=J-12 file=docs/spec/_source/state-machines.json -->
`agentApiEnablingStateMachine.disabled` の根拠から、退ける `RS-35` を外す（決定 21）。旧
```text
        "CP-17",
        "RS-35"
```
新
```text
        "CP-17"
```

⚠️ J-08 〜 J-12 を当てたら `npm run gen` で `_assets/tbl-state-machines.md`（表 T-280 ・ 表 T-296 ・ 図 F-026 ・ 図 F-042）と `src/use-case/advance-screen-session/` の 2 つのユニットの生成の領域を刷る。遷移の関数（手書き）は実装の波が書く。

### 4.5 新しい原稿（`_source/`）

<!-- EDIT id=N-01 file=docs/spec/_source/image-to-grs-json-prompt.ja.md -->
新規。2 行目から後ろは、`docs/guides/schedule-to-grs-json/prompt-ja.md` の「プロンプト」の塊（`47be689f` の `:22`〜`:124`、4 つの逆引用符の囲みの中身）を写し、次の 4 つだけを書き換える（3 節の表）。1 行目（役割の宣言。検査 21 が先頭 1400 字に `SINGLE SOURCE OF TRUTH` を探す）は次のとおり。
```text
<!-- SINGLE SOURCE OF TRUTH -- EDIT THIS FILE. The Japanese prompt of FR-068 (turn a schedule image into GRS JSON); this first line is not part of the prompt. Generated from it: src/adapter/screen-renderer/image-to-grs-json-prompt.json and the prompt block of docs/guides/schedule-to-grs-json/prompt-ja.md. Rebuild: npm run gen -->
```

| # | 塊の中の旧（`prompt-ja.md` の行） | 新 |
|---|---|---|
| 1 | `# 添付` の 3 項のうち、`grs-skeleton.json` と `grs-document.schema.json` の 2 項（`:26`〜`:27`） | 消す。代わりに 1 項「形の正（JSON Schema 2020-12）と土台の文書: 本文の後ろの 2 つの `json` の囲み（前がスキーマ、後が土台）。スキーマに厳密に従う」。`日程の原本` の項は残す（画像は利用者が添える） |
| 2 | `休日` の項の「grs-skeleton.json の暦」（`:31`）と、作り方 1 の「grs-skeleton.json を丸ごと写し」（`:38`） | 「土台の文書の暦」 ／ 「土台の文書を丸ごと写し」 |
| 3 | 色の `自由` の項の `"#RRGGBB"`（`:35`） | `"#rrggbb/"`（`CR-548` のカスタムカラー。明るいテーマの値だけを書き、`/` の後ろは空ける） |
| 4 | （無い） | スキーマと土台は原稿に書かない —— 生成器が版の 1 行と共に後ろへ連ねる（E-20 ・ E-10） |

<!-- EDIT id=N-02 file=docs/spec/_source/image-to-grs-json-prompt.en.md -->
新規。N-01 と同じ形で、`prompt-en.md` の同じ塊（`:22`〜`:124`）を写し、同じ 4 つを英語の側で書き換える（`attached` の 2 項、`grs-skeleton.json's calendar` と `Copy grs-skeleton.json whole`、`free` の `"#RRGGBB"`）。1 行目は次のとおり。
```text
<!-- SINGLE SOURCE OF TRUTH -- EDIT THIS FILE. The English prompt of FR-068 (turn a schedule image into GRS JSON); this first line is not part of the prompt. Generated from it: src/adapter/screen-renderer/image-to-grs-json-prompt.json and the prompt block of docs/guides/schedule-to-grs-json/prompt-en.md. Rebuild: npm run gen -->
```

⚠️ 原稿の中身はプロンプトであり、仕様の文ではない —— 特定の製品の名を書かないこと（今の塊には無い。塊の外の案内の前書きにだけ在る）。`.md` なので検査 23（`.json` だけを読む）には掛からない。⚠️ `JDG-509` の「冒頭部分は適宜編集してください」は、原稿の頭（`# 日程の原本` と添える画像の説明）を利用者が自分の画像に合わせて直すことを指す —— 原稿の頭は、直す所が一目で分かる形のまま保つ。⚠️ `CR-565` が 表 T-024 の `IX-3` を覆すので、プロンプトの本文が「古い版でもよい」と読める所を持つなら、当てる時点で `CR-565` の形に揃える（`47be689f` の塊には版を選ばせる文は無い —— 13 節の 4）。

### 4.6 `GRS` が開くときに確かめること（`47be689f` で読み直した）

⭐ 利用者の流れは **ヘッダーの `IC-115` を押す（写り、`RS-65` が告げる）→ 画像と一緒に AI へ貼り、冒頭を自分の画像に合わせて直す → 返った JSON を `〜.json` で保存 → `GRS` の「開く」→ `OP-3` で置き換えるか合流するかを選ぶ → AI の「推定したこと」の表と画面を見比べる** である。開く路は次の順で確かめる。本節は編集ではない —— 何も足さない（1 節）。⚠️ 3 の「古い版の補い」は `CR-565` が `OP-6` を覆すので、`CR-565` の後は変わる。

| 順 | 確かめること | 場所 | 告げ |
|---|---|---|---|
| 1 | 拡張子と先頭の 1 字が合う（`IO-2`: `.json` と `{`） | `document-codec.ts:96`〜`:114` | `RS-11`〜`RS-13` |
| 2 | JSON として読める | `json-codec.ts:1490`〜`:1497` | `RS-25` の理由は作るが、⛔ **告げない**（`DFC-656`） |
| 3 | 退いた列を外す（`withoutRetiredColumns`、`:1425`）と、古い版の補い（`isKeptOpen` ・ `editGroup` ・ `sourceFormat` ・ 旧い実績の長さ） | `json-codec.ts:1352`〜`:1482` | — |
| 4 | スキーマの歩き（型・列挙・範囲・長さ・`pattern`・`required`・`additionalProperties`。`documentSettings` は型と列挙だけ） | `json-codec.ts:1280`〜`:1334`、`:1509` | 断れば 2 と同じく告げない（`DFC-656`） |
| 5 | 版の新旧（`FR-073`） | `json-codec.ts:1510`〜`:1515` | `RS-48` |
| 6 | 設定値を範囲へ収める（`clampedSettings`） | `json-codec.ts:1528`〜`:1541` | 告げる（`frame-loop.ts:3698`） |
| 7 | 大きさ・タスク数・WBS の輪と深さ・日付・開始と終了・実績の順（`FR-023`、`S-113`〜`S-115` ・ `S-119` ・ `S-120` ・ `IV-14` ・ `IV-21`） | `validate-imported-document.ts:160`〜`:310` | 読めない日付のタスクは消して名前を告げる（`frame-loop.ts:3716`〜`:3734`） |
| 8 | 稼働する曜日が 1 つ以上（`IV-17`） | `frame-loop.ts:1042` | 告げる |
| ⛔ | **表 T-220 の残りの行（`IV-1` 主キーの重なり・`IV-2` 指す先・`IV-6` どのタスクもちょうど 1 つの行に載る ほか）** | `scheduleViolations` は数えるが、開く路は `IV-17` しか読まない | ⛔ **黙って開く**（`DFC-922`）。⚠️ AI は UID を重ねやすい |

### 4.7 検査の道具（`.claude/skills/spec-graph-check/retired.py`）

<!-- EDIT id=E-24 file=.claude/skills/spec-graph-check/retired.py -->
廃番の席を焼く（決定 18 ・ 21）。`CR-541` が `SK-17` を焼いた形に倣い、理由を英語で添える。旧
```text
           'L-2', 'S-86', 'DS-2', 'K-55',
           'T-006'}
```
新
```text
           'L-2', 'S-86', 'DS-2', 'K-55',
           # CR-562 (2026-09-24, rulings JDG-505 .. JDG-507): IC-19 (the
           # header entrance that showed and copied the open document for an
           # AI) left table T-109 when the user chose to hand a schedule to an
           # AI as the saved .json file; the header's AI group became IC-115,
           # IC-20 and IC-18. RS-35 (the dialogue field cannot be shown while
           # the Agent API is off) left table T-233 when pressing IC-18 while
           # the Agent API is off began to turn it on, which leaves the reason
           # no scene. CR-562, CR-563 and the ledgers name both, so the seats
           # stay burnt.
           'IC-19', 'RS-35',
           'T-006'}
```

## 5. 継ぎ目 —— 両側の依頼にこのまま写すこと

```text
写す文字列の順: <プロンプトの原稿（表示言語）> "\n\n" <版の 1 行: "schemaVersion: " + 起動時の文書の schemaVersion>
  "\n\n```json\n" <最小化したスキーマ> "\n```\n\n```json\n" <最小化した土台> "\n```\n"
生成物の置き場: src/adapter/screen-renderer/image-to-grs-json-prompt.json（決定 16。表 T-075 に行は要らない）
生成物の中身: { ja: <原稿 ja の 2 行目から>, en: <原稿 en の 2 行目から>, schemaVersion, schema: <最小化>, emptyDocument: <最小化> }
  鍵の名は当てる者が決めてよい。⛔ 連ねた文字列を言語ごとに 2 つ持たない（スキーマと土台が 2 回入る）
IC-115 の押し（決定 2 ・ 25）: 殻（frame-loop.ts、UF-48）が押しを受け、面を立てずにその場で組んで写す。
  ⚠️ 写しは押しを受けたその呼び出しの中で始める —— ブラウザはクリップボードへの書き込みを利用者の操作による活性の中でしか
  受け付けないことがある（全画面表示の求めについて UF-48 の行が同じことを書いている）
クリップボードへの書き込み: 今ある writeClipboard（clipboard-gateway.ts:17）を使う
  写せた → raiseNotice('RS-65')（NT-5。[OK] は NT-8 の消す入口）／ 写せない → raiseNotice('RS-15')（今の frame-loop.ts:4016 と同じ）
IC-18 の押し（決定 21）: 入力の翻訳が 1 つの押しを screen/dialogueFieldEntryPressed（isAgentApiEnabled を運ぶ）と
  agentApi/enablingAskedByDialogueField の 2 つにする。無効のとき: 欄は shown、Agent API は enabled、記憶に書く（storeAgentApiEnabling）
```

## 6. グラフ（`47be689f` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 行 | 前（`47be689f`） | 後（4 節を当てた写しで測った。13 節の 5） |
|---|---|---|
| `FR-068` | 1 / 5（`FR-033`、2.3 ・ 5.2 ・ 5.3 の節、表 T-109 の節） | 2 / 8（`FR-033`、`FR-076`（表 T-233 の `RS-65` の行）、2.3 の節、5.2 の節、5.3 の節は `UF-66` から `UF-48` へ、6.2 の節 2 箇所、表 T-109 の `IC-115` の行） |
| `IC-19` | 0 / 1（`tbl-state-machines.md:61` の出来事） | 廃番（`retired.py`）。3 つとも E-13 ・ J-08 で消える |
| `IC-18` | 1 / 5（`FR-066` の 3 行、表 T-206 の節、`tbl-state-machines.md:76`） | 1 / 9（`FR-066` の 6 行、表 T-206 の節、`screen` と `agentApi` の出来事） |
| `FR-066` | 3 / 16（`FR-070` ・ `FR-076` ・ `FR-040` ほか） | 2 / 16（`FR-076` が抜ける —— `RS-35` の行が消える。`agentApi` の出来事が 1 つ入る） |
| `RS-35` | 1 / 3（`FR-066`、2 つの状態機械） | 廃番 —— 3 つとも E-17 ・ J-09 ・ J-12 で消える |
| `IC-115` ・ `RS-65` | —（まだ無い） | どちらも 1 / 1（`FR-068` の E-21 の 1 行） |
| `U-30` | 1 / 2（`FR-080` の `EP-11`、`openSurfaceStateMachine`） | 同じ 2 つ —— どちらも `Help Modal` を指したまま（決定 23） |

⭐ **要求の閉路は 1 つ増えるが、形は既に在るものと同じである** —— E-21 で `FR-068` が 表 T-233 の `RS-65` を理由として名指し、`RS-65` の行は「正」の欄で `FR-068` を名指す。表 T-233 は `FR-076` の中に在るので、`impact.py` はこれを `FR-068` ↔ `FR-076` と数える。理由の行を持つ要求はどれもこの形である（前の `FR-066` ↔ `FR-076` は `RS-35` の行が作っていて、E-18 で消える）。表 T-109 の `IC-115` の行と `FR-068` の間も同じ形（行が正を名指し、要求が入口を名指す）。それ以外に `FR-068` が新しく指す先（`FR-027` ・ `FR-038` ・ 表 T-024a の `OP-2` ・ 6.2 の節）と E-22 ・ E-23 の `UC-011` は、どれも `FR-068` を指していない。E-10 で 6.2 が `FR-068` を指し返すが、6.2 は節である。E-17 で `FR-066` が新しく指す `IC-20` ・ `FR-029` は `FR-066` を指さない（`IC-20` の行は `FR-065` を指す）。
⭐ `FR-066` と `FR-065` の間は、`FR-066` → `FR-065` の一方向のまま（`FR-065` は書き換えない —— 決定 21）。

## 7. 数の予測（`47be689f` で測った。当てた後に同じ数え方で突き合わせる）

| 数 | 前 | 後 |
|---|---|---|
| 表 T-109 の行 | 97（13 節の 2） | 97（`IC-19` を消し `IC-115` を足す）。⚠️ 他の変更要求の `IC-` が先に着けば、そのぶん前も後も増える |
| 表 T-109 の前文の「97」・`FR-086` の「97 行」 | 4 箇所 | 変えない |
| 図 F-019 の図形 | 97 | 97（E-14） |
| 表 T-233 の行 | — | ±0（`RS-35` を消し `RS-65` を足す） |
| 表 T-103 の行 | — | ±0（`U-30` の名が 1 つ減る） |
| 辞書の項 | — | `icons` ±0、`reasons` ±0、`surfaces` −1 |
| 領域 `screen` の状態機械 ／ 出来事 | 12 ／ 32 | 12 ／ 32（J-08 は入力の行を外すだけ） |
| 領域 `agentApi` の状態機械 ／ 出来事 | 1 ／ 2 | 1 ／ 3（J-10） |
| `md-checks.py` の数 | `tables=187 figures=27 rows=2303 uids=162` | 同じ（写しで測った。検査 5 〜 10 ・ 15 ・ 48 は 0 のまま） |
| 辞書の語（`generate_display_words.py`） | 611 語（569 を書いた） | 610 語（568）—— 節 `surfaces` の 1 つ |
| ヘルプの項目（`generate_help_roster.py`） | 94 | 94（`IC-19` が抜け `IC-115` が入る） |
| 検査 39 の「逐語の試験を持たない MUST」 | 1326（基準線 1329） | 1331（＋5。基準線を 1331 へ上げる —— X-8） |
| 要求の数と、起点のユニットの数（`05-07-design.md` の 5.3 の結び） | 121 件、起点を名指す 99 件、まだ書いていない 17 件 | 変えない（要求を足さず消さず、`FR-068` の起点を移すだけ —— 決定 25） |
| `dist/index.html` | 1,311,004 バイト（この worktree の `dist`。刷った木は確かめていない） | ＋約 53.1 KB（原稿 ja 13,751 ＋ en 12,150 ＋ スキーマ 22,917 ＋ 土台 5,516 ＝ 54,334 バイト）＝ 約 ＋4.1 %。⚠️ 土台とスキーマは `CR-565` の後の実物で数え直す |
| 写す文字列 | — | ja 42,240 バイト ／ en 40,639 バイト（5 節の順で組んで数えた。`45ba1707` の見積もり —— 原稿・スキーマ・案内は `47be689f` まで変わっていない） |

## 8. 波と当てる順

⭐ **当てる順: リファクタの段 7.5 の分け → `CR-565` → 本書。** 本書の決定 4 ・ 5 はスキーマと版を `CR-565` の後の形で受ける。
⭐ **`CR-563` とは、どちらを先に当ててもよい** —— 旧を分け合う行は 0 である（13 節の 5 で、`CR-563` を当てた写しに本書の旧を、本書を当てた写しに `CR-563` の旧を数え、どちらも 1 回だった）。ただし近い所が 4 つあるので、後に当てる側は数え直すこと:

| 本書 | `CR-563` | 間 |
|---|---|---|
| E-17（`FR-066` の STATEMENT の後の 2 行、`01-04-requirements.md:6191`〜`:6192`） | E-08（`FR-066` の見出しから UID まで、`:6184`〜`:6187`。その前に `FR-150` を差し込む） | 3 行（`:6188` 〜 `:6190`） |
| E-25（表 T-233 の `RS-15` の行、`:6549`） | E-10（同表の `RS-61` の行、`:6548`。その後ろに `RS-62`） | ⛔ **隣り合う（0 行）**。どちらを先に当てても、`RS-62` は `RS-61` の後ろに、`RS-65` は `RS-15` の前に入り、並びは `RS-61` → `RS-62` → `RS-65` → `RS-15` になる。`CR-565` の `RS-63` ・ `RS-64` も同じ所に入るなら、`RS-65` の前に来る |
| J-13（辞書の `reasons` の `RS-15` の項の頭、`display-words.json:2849`〜`:2850`） | J-03（`RS-61` の項の末尾、`:2847`〜`:2848` 付近） | ⛔ **隣り合う（0 行）**。並びは上と同じ |
| E-18（表 T-233 の `RS-34` ・ `RS-35`、`:6523`〜`:6524`） | E-10（`:6548`） | 23 行 |

⚠️ 他に近い所は無い: `CR-563` の `UC-012` の拡張（`:985` 付近）と本書の E-19（`UC-011`、`:965`〜`:966`）は 19 行離れる。`CR-563` の `05-07-design.md` の要求の数（`:499` ・ `:513`）に本書は触らない（決定 25）。本書の E-29（`:448`）・ E-30（`:466`）は同じ表 T-075 だが 30 行以上離れる。表 T-108 の `CM-76`（`tbl-glossary.md:476`）と本書の E-13（`:529`〜`:531`）は 53 行離れる。
⚠️ `CR-563` の 4 節の前文は、本書の書く所を「表 T-109 の `IC-115`・辞書の AI 出力の中身の切替の項」と書いている —— 起草の日の形である。今は 表 T-109 の群 `AI` の 3 行と `IC-52`・表 T-103 の `U-30`・辞書の `icons` ・ `surfaces` ・ `reasons`・`FR-066`・表 T-233 の `RS-35` と `RS-65` である（本書は `CR-563` を書き換えない）。

リファクタの後に 1 波。原稿と生成器（持ち主 A）と、ヘッダー・写しと告げ・面の撤去・対話欄の押し（持ち主 B）の 2 体。試験は仕様だけを読む別の体が書く（写した文字列にスキーマが 1 字違わず入っていること、刷った土台が `GRS_DOCUMENT_SCHEMA` を通ること、表示言語で原稿が変わること、`IC-115` を押すと面が立たずに写り `RS-65` が告げること、写せないときに `RS-15` が告げること、ヘッダーの群 `AI` が 3 つで `IC-19` と AI 出力の面が無いこと、`Agent API` が無効のあいだに `IC-18` を押すと欄が出て `Agent API` が有効になり記憶に書かれること、有効のあいだに `IC-18` で欄を隠しても `Agent API` が有効のままであること、`IC-18` が無効のあいだも薄くないこと）。
⚠️ 4 節の編集を当てる体（仕様だけ）は、実装の波より先に 1 つ。当てたら `npm run gen` と検査を回し、J-06 ・ J-13 の英語の 4 つは 11 節の問 2 の答えが出てから当てる。

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

`DFC-656` ・ `DFC-922`（決定 8 の前提）。

| # | 置き場 | 何を |
|---|---|---|
| X-1 | `tools/`（新しい生成器 1 つ）と `package.json` の `gen` ・ `gen:check` | 原稿 2 つ（1 行目を除く）＋ 最小化したスキーマ ＋ 刷った土台 ＋ 版を `src/adapter/screen-renderer/image-to-grs-json-prompt.json` へ（決定 16。中身の形は 5 節）。`--check` を持たせ、検査 27 に加える |
| X-2 | `tools/generate_startup_template.py` | 同じ回に、タスクを持たない土台の文書を 1 つ刷る（`FR-068`、決定 5）。⚠️ **今の道具は土台を刷らない** —— 書き出すのは 1000 タスクの起動時の文書（`startup-template.json`）1 つだけである。土台もスキーマに照らしてから書く（`check_schema` と同じ道） |
| X-3 | ⛔ 取り下げ（起草の日は `generate_display_words.py` に節 `aiExportContents` を足す仕事だった —— 決定 15 の取り下げ） | — |
| X-4 | `.claude/skills/spec-graph-check/check-provenance.py` の `ARTIFACTS` | 新しい生成物 1 本（原稿の名 `image-to-grs-json-prompt`）。道標は `$comment` の鍵で持つ（`icon-roster.json` と同じ） |
| X-5 | `docs/guides/schedule-to-grs-json/prompt-ja.md` ・ `prompt-en.md` ・ `grs-skeleton.json` | 3 節の表のとおり。プロンプトの塊と `grs-skeleton.json` は X-1 ・ X-2 の生成物になり、案内に生成物の道標を置く。使い方 1〜2 を「アプリのヘッダーの `IC-115` を押す（写る）」に、Python の確かめ（`:217`〜`:231`）を今ある「開発者向けの注」（`:241`）へ移す。英語版も同じ所 |
| X-6 | `docs/development-rules/09-tools.md` の生成器の表 | X-1 の生成器の行を足す |
| X-7 | ⛔ 取り下げ（起草の日は `generate_help_roster.py` に `IC-19` の下の字下げを足す仕事だった —— E-12 の取り下げ） | — |
| X-8 | 検査の基準 2 つ | 検査 37 の `dictionary-table-pairing.txt`: `T-109 IC-19` の行を消し（刈り込み）、`T-109 IC-52`（`面` の欄が変わる）を読み直し、`T-109 IC-115` の組を語と表を読み合わせてから足す。⚠️ `IC-115` の英語の語が決まるまで組は確定しない。検査 39 の `must-clause-coverage-baseline.txt` は、試験が新しい MUST を逐語で持つまで上がる（数は 13 節の 5）。⛔ 基準を上げるのは利用者の了承が要る |
| X-9 | `src/`（AI 出力の面の撤去と写し） | `input-command-translator.ts:689` の `aiExportModal: 'IC-19'` を `IC-115` の押しに。`open-modals.ts` ・ `screen-renderer.ts` ・ `screen-state-input.ts` ・ `frame-loop.ts` から `AI Export Modal` の面を外す。`frame-loop.ts:4011`〜`:4021` の「閉じると写す」の枝を、`IC-115` の押しで組んで写し `RS-65` ／ `RS-15` を上げる形に（5 節）。`RS-35`（`frame-loop.ts:828` ・ `:883`）を外し、`RS-65` を足す。⚠️ 表 T-233 の前文「本表に行を足す者は、その行へ振り分ける道が在ることまで確かめること（MUST）」—— `RS-65` の道はこの X-9 が作る |
| X-10 | `src/use-case/advance-screen-session/screen-values.ts:1219` と `agent-api-values.ts`、入力の翻訳 | 対話欄の遷移の関数から `RS-35` の告げを外し、`hidden` → `shown` を無条件に。`IC-18` の押しを `agentApi/enablingAskedByDialogueField` にも届け、`disabled` → `enabled` で記憶に書く。`IC-18` を無効のあいだ薄く描く枝を外す |

⚠️ **生成したスキーマは `documentSettings` を閉じている**（`additionalProperties: false` と `required`）が、`05-07-design.md` は知らない鍵と欠けた鍵を拒むことを禁じ、アプリも受け入れる —— この食い違いは `DFC-841` として台帳に在る。⭐ 本書はそのスキーマを AI へ丸ごと渡す（決定 4）ので、AI は `documentSettings` の鍵を 1 つも欠かさず書こうとする。土台が同じ版で刷られていれば（決定 5）害は無い。`CR-565` の後に同じことを確かめ直す。

## 10. ⛔ この変更でやらないこと

- `GRS` の中で画像を読むこと（Q2）。
- AI へ送ること。送り先を持てば `GL-005` とぶつかり、利用者の日程が外へ出る。
- 貼り付けで開く入口（決定 7）。
- 「推定したこと」の表を読むこと（決定 11）。
- 開いている文書を AI のために写すこと（`JDG-505`）。日程は保存した `.json` を添えて渡す。
- 写す前にプロンプトを見せる面（`JDG-509`）。
- 写せなかったときの理由の行を新しく足すこと（`JDG-509` —— 今の落ち先 `RS-15` のまま）。
- `IC-18` で `Agent API` を無効にすること（`JDG-507` の逆向きは今のまま）。

## 11. 前に立つ者へ返す問い

| 問い | 案 | 推奨 |
|---|---|---|
| 問 1: 入口をどこに置くか | ✅ 答えがあった —— `JDG-503` で A（面の中身の切替）、それを `JDG-506` が覆してヘッダーの 3 つ目のアイコン | —（決定 12 の取り下げ、決定 17 ・ 18） |
| 問 2: 辞書の英語の語（✅ 2026-09-24、`JDG-510`: 説明と告げは B を当てた。⚠️ 英語の名だけ未決 —— 利用者の案 "Prompt for Schedule Image to JSON" を検討中）（J-06 ・ J-13。⛔ 辞書の `$comment` は体が起こした語を禁じている） | ✅ 日本語は決まった（`IC-115` の名 `JDG-504`、説明 `JDG-508`、告げ `JDG-509` —— どれも利用者の文のまま）。⚠️ 残りは英語の 4 つ: `IC-115` の名 "Prompt to Turn a Schedule Image into Data"、説明 "Output to the clipboard a prompt that has an AI convert a schedule made in PPT, Excel or the like into JSON data GoodRelax Scheduler can process"、`RS-65` の text "The prompt is on the clipboard. Hand it to an AI."、nextStep "Edit its opening part as needed." | ⭐ 案のまま —— どれも日本語の文を語順のまま写した。⚠️ `JDG-504` の「なぜ 2 パターンになるのか説明せよ」は、中身が 2 つ（文書とプロンプト）だった形への問いであり、`JDG-505` で中身が 1 つになったので問いの前提が無くなった |
| 問 3: ヘッダーの AI の群の並び（決定 17）（✅ 2026-09-24、`JDG-510`: A） | **A. `IC-115` → `IC-20` → `IC-18`**（本書） ／ B. `IC-18` → `IC-20` → `IC-115`（利用者の逐語 Q5 の順） ／ C. `IC-18` → `IC-115` → `IC-20`（今の並びで `IC-19` の席に置く。差分が最小） | ⭐ **A** —— `Agent API` に依らない `IC-115` を文書の群の隣に置き、「有効にする → 欄を出す」と左から読める。⚠️ 代償: 今の `IC-18` と `IC-20` の位置が入れ替わるので、慣れた人の手が迷う |
| 問 4: 押してすぐ写すか、面で見せてから写すか（決定 2） | ✅ 答えがあった —— `JDG-509` で A（すぐ写し、[OK] 付きで告げる） | — |
| 問 5: `IC-18` の説明の語（✅ 2026-09-24、`JDG-510`: 足す。J-06 に当てた） | 「AI との対話欄を表示する。もう一度押すと非表示にする」は、無効のあいだに押すと `Agent API` も有効になることを言わない | ⭐ 今のまま —— 本書は語を 1 字も起こさない。変えるなら利用者の語で辞書の 1 項 |

## 12. 台帳

- 裁定: `rulings.md` の「2026-09-23 —— MCP 連携・行の削除・画像から GRS JSON（CR-562 / CR-563）」の節の `JDG-501` 〜 `JDG-509`。
- 欠陥: `defects.md` の `DFC-920` ・ `DFC-921` ・ `DFC-922`。
- 裁定待ち: 台帳に行を置かない。⚠️ 問 2 の英語の語・問 3 の並び・問 5 の語は表示だけの選択（`pending-decisions.md` の分類 `C`）であり、`C` の行はコードに暫定の印を求める（検査 25）。本書は実装をリファクタの後に置くので、本節と前に立つ者の問いだけが持つ —— J-06 ・ J-13 の英語の 4 つは答えが出るまで当てない。

## 13. 測り方の再現

```bash
# 1. sizes: prompt block, minified schema, minified skeleton (re-run on 47be689f, 2026-09-24)
python - <<'EOF'
import json, re
G = 'docs/guides/schedule-to-grs-json/'
for lang in ('ja', 'en'):
    t = open(G + 'prompt-%s.md' % lang, encoding='utf-8').read()
    print(lang, len(re.search(r'````text\n(.*?)\n````', t, re.S).group(1).encode()))
for p in ('docs/spec/_source/grs-document.schema.json', G + 'grs-skeleton.json'):
    print(p, len(json.dumps(json.load(open(p, encoding='utf-8')), separators=(',', ':'), ensure_ascii=False).encode()))
EOF
#   -> ja 13751, en 12150, schema 22917, skeleton 5362 (same as 45ba1707: git diff --stat shows
#      no change under docs/guides, the schema, document-codec, json-codec or validate-imported-document)
#   -> empty document 5516 bytes and the copied text ja 42240 / en 40639 are the 45ba1707 estimate
wc -c dist/index.html          # -> 1311004

# 2. the rows, the figure, the surface, and the names this change request introduces or drops
grep -oE '^\| IC-[0-9]+' docs/spec/_assets/tbl-glossary.md | wc -l      # -> 97
grep -c '<text class="lbl"' docs/spec/_assets/fig-icons.svg             # -> 97
git grep -n 'IC-19\b' -- docs/spec .claude/skills src                   # -> T-109 row, glyph, dictionary,
#      state-machines.json:437 and its generated row, icon/help rosters, input-command-translator.ts:689,
#      dictionary-table-pairing.txt:212
git grep -n 'AI Export Modal' -- docs/spec tools .claude/skills          # -> T-103 U-30, T-109 IC-52,
#      T-076 EP-11 (01-04-requirements.md:5543), display-words.json surfaces -- all four edited
git grep -n '"IC-3"' -- docs/spec/_source/state-machines.json           # -> 0: a stateless press has no event
git grep -n 'enablingAskedByDialogueField\|aiExportContent\|image-to-grs-json-prompt' -- docs/spec src tools .claude/skills
#   -> 0 (the names are free; the withdrawn names never entered the tree. Outside these folders only
#      the ledgers match, and only through this change request's own file name)
grep -c '依頼文' change-request/CR-562-*.md   # -> 2: the 0.1 note and this line

# 3. the open path reads only IV-17 of table T-220; the lines of section 4.6
grep -n 'scheduleViolations(document.schedule' src/framework/single-html-shell/frame-loop.ts   # -> 1042
git diff 45ba1707 47be689f -- src/framework/single-html-shell/frame-loop.ts
#   -> +1 line at :236, net 0 at :2741 and :3363, -1 at :4346: only 1041 -> 1042 moves among the
#      lines this change request cites (3698, 3716, 3734, 4011, 4016, 4021 unchanged)
grep -n "raiseNotice('RS-15'" src/framework/single-html-shell/frame-loop.ts   # -> 4016 (the copy), 4258

# 4. the free colour the prompt allows, and whether the prompt asks for a version
grep -n 'RRGGBB' docs/guides/schedule-to-grs-json/prompt-ja.md       # -> :35
grep -n 'schemaVersion' docs/guides/schedule-to-grs-json/prompt-ja.md

# 5. graph, and every old block of section 4 once in its file, applied in order to a COPY
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py FR-068   # and IC-19 IC-18 FR-066 RS-35 U-30
git archive 47be689f docs tools .claude/skills src tests package.json | tar -x -C <scratchpad>/copy
PYTHONIOENCODING=utf-8 python <scratchpad>/cr562-edits.py . <scratchpad>/copy
#   -> edits 31 problems 0 (E-01 E-02 E-09 E-10 E-13..E-30, J-06..J-14; E-03..E-08 E-11 E-12 J-01..J-05
#      withdrawn); sharing a line 0; both JSON manuscripts parse with json.loads
#   -> the same count with CR-563 applied first, and CR-563's 15 old blocks with this one applied first:
#      every old block still occurs once (0 problems both ways)
#   on the copy (N-01 / N-02 written as line 1 + today's prompt block):
#   -> md-checks.py: tables=187 figures=27 rows=2303 uids=162, checks 5-10 / 15 / 48 all 0
#   -> check-provenance.py OK (30 artifacts, every _source file says which it is);
#      check-language-dictionary.py OK (8 manuscripts)
#   -> state_machines_json_to_md.py, generate_state_machine_types.py (agent-api-values.ts rewritten),
#      generate_icon_roster.py (97 icons), generate_icon_glyphs.py (97 shapes),
#      generate_help_roster.py (94 items; 94 before), generate_display_words.py (610 words; 611 before)
#   -> check 37 red, 3 lines, all X-8: T-109 IC-115 new pairing, T-109 IC-52 fingerprint moved,
#      T-109 IC-19 stale
#   -> check 39 red: 1331 bare clauses against the baseline 1329 (the tree itself stands at 1326)
#   -> impact.py FR-068 2 / 8, IC-115 1 / 1, IC-18 1 / 9, FR-066 2 / 16, RS-65 1 / 1, U-30 1 / 2

# change-request discipline (check 22) and identifier reservation (check 62), judged by exit code
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/check-cr-discipline.py ; echo "exit=$?"
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/check-identifier-reservation.py ; echo "exit=$?"
```
