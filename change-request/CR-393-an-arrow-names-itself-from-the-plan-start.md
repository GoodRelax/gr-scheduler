# CR-393 — 矢印の形状は予定の開始点から名乗り、隠したマーカーのぶん名前を寄せる

> **閉じるもの**: 利用者の変更要求 2026-09-16 `JDG-122` のうち次の 3 文と、問いへの答え `JDG-123` ⑦（「---> 系」は矢印 `SH-3` と両端の点の線 `SH-4`）⑧（「予定の開始点の上に名前」は ---> 系）—— 逐語は `docs/development-records/rulings.md` の同行。
> - 「---> 系の進捗マーカーはフォントサイズに合わせて一回り小さくしろ」
> - 「---> 系のタスク名は予定の幅、実績の幅にかかわらず、常に予定の上の開始点部分から表示しろ 現行は予定幅や実績幅が変わるとタスク名の位置が変わって見にくい。」
> - 「進捗マーカーを非表示にするときはその分タスク/マイルストーン名を左にずらせ」
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `ccdcfabc`。
> ⛔ 同じ要求の「マーカーと実績タスク名が実績の範囲に入るなら、マーカーと実績タスク名を実績の中に表示せよ」（矩形系）は **当てていない** —— 実績の中のどこに立てるかと、実績の端点の掴み代との重なりが条項に無い。前に立つ者への問いとして返した（本書の第 9 節）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（全体日程を 1 画面で見られる）—— 名前が幅につれて動くと、どのタスクの名前かを目で追えず、一覧が読めない。マーカーを隠したときに空いたままの場所は、名前をタスクから遠ざける（`JDG-92` と同じ害）。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（唯一の正）** —— 矢印の名前の横の位置の正は、いまは 表 T-013 の `NL-1` / `NL-3` と、表 T-012 の注「`NL-1` の形状の中に書くは横方向の収まりだけ」の 2 つに分かれている ⇒ 決定 1 で `FR-002` 1 か所に寄せ、表 T-012 の注は指すだけにした。
- **`R1.3`（裁かれた行との衝突）** —— `JDG-08` が残した「名称ラベルの左端は `S-63` に依らず同じ位置に数える」を、`JDG-122` の 3 文目が覆す。`JDG-112`（予実の表示の切り替えでは名前も帯高も動かさない）は `S-227` / `S-228` の話なので残る ⇒ 決定 3。
- **`R2`（命名）** —— 新しい語を立てていない。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | `SH-3` / `SH-4` の名称ラベルに 表 T-013 を当てず、名称ラベルの箱の左端を予定の開始点とする（MUST）。予定や実績の幅で横の位置を変えない（MUST NOT）。形状の右端より右へ出たぶんは `OC-1` に数える（MUST） | `JDG-122` の 2 文目。`OC-1` に数えるのは、表 T-038 の `OC-10` が「横は算入しない（形状の外へ出したぶんは `OC-1` が数える）」と既に述べているのに揃えた | 形状より長い名前は、これまで形状の右（`NL-3`）へ出て高さを取らなかったぶんが、形状の上で右へ伸びる |
| 決定 2 | `SH-3` / `SH-4` の進捗マーカーの径は `S-22` に代えて、そのタスクの名称ラベルの字の大きさとする（MUST）。再開アイコンの広がり（`S-26` / `S-27`）もその径から導く（MUST） | `JDG-122` の 1 文目「フォントサイズに合わせて一回り小さく」。既定では矢印の名前の字は床の 12px、マーカーは 16px なので、字に合わせると 12px で一回り小さくなる | その形状のマーカーは字と一緒に `zoomY` に従う（`FR-094` の STATEMENT の「ズームに追随させない」の例外として書いた）。字の床で止まるので潰れない |
| 決定 4 | `SH-1` / `SH-2` で、名称ラベルの箱の幅（字の幅 ＋ `S-31`）＋ `S-32` ＋ マーカーの径 ＋ `S-23` ＋ `S-91` × 2 ≦ 実績の幅 のとき、表 T-013 を当てず、実績の右端から `S-91` ＋ `S-23` 内側にマーカーの右端、その左 `S-32` に名前の箱の右端を置く（MUST）。`S-63` が偽なら判定からマーカーの径と `S-23` を除き、名前の右端を実績の右端から `S-91` 内側に置く（MUST）。`FR-013` に「このときマーカーは実績の中に立つ」と注した | 利用者の答え「案 A」（2026-09-16、台帳 `JDG-131`）。`S-91` を両端に数えるのは、実績の端点の掴み代（`GR-5` / `GR-6`）の上に置かないため | 実績の幅が境目のタスクでは、実績を 1 日伸ばしただけで名前とマーカーが外から中へ跳ぶ |
| 決定 3 | `S-63` が偽のときは、`FR-002` の `NL-1` の幅の段でも 表 T-243 の結びでもマーカーを数えず、名称ラベルの左端を形状の右端 ＋ `S-32` とする（MUST）。予実の表示（`S-227` / `S-228`）の切り替えでは、これまでどおり動かさない | `JDG-122` の 3 文目。「タスク/マイルストーン名」なので、形状の中に書く名前にも外に出す名前にも当てた | `S-63` を切り替えると `OC-1` の算入が変わり、段が組み替わりうる —— `JDG-08` が避けていた事故が `S-63` の切り替えに限って戻る |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| 矢印の名前は 表 T-013 の `NL-1` / `NL-3` で横の位置が決まり、上へずらす | `01-04-requirements.md` の 表 T-012 の注（当てる前の `:1092-1093`） |
| `NL-1` の幅はマーカーの右端 ＋ `S-32` から数える | `FR-002` の 表 T-013 の後の段（当てる前の `:1373-1385`） |
| 名称ラベルの左端は `S-63` に依らず同じ位置 | 表 T-243 の結び（当てる前の `:1531-1544`）、`JDG-08` の「残した」部分 |
| マーカーの径は `S-22` の固定値 16px | `FR-094` の STATEMENT、表 T-201 の `S-22` |
| 矢印の名前の字は 28 × 0.5（`S-15`）× 0.5715（`S-5`）× 0.80（`S-7`）× 0.85（`S-9`）＝ 5.44px で、床の 12px（`S-8`）に止まる | 表 T-201 の値を掛けた |

## 2. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| `S-63` を名指す要求の文（当てる前） | 4（`:1376`、`:1535`、`:1539`、`:1543`） | `grep -n S-63` |
| 足した `（MUST）` / `（MUST NOT）` | `FR-002` +5、表 T-243 の結び +1、`FR-094` +2 | 足した文を数えた |
| 字面を変えた既存の MUST の文 | 表 T-243 の結び 3（`:1531`、`:1532`、`:1539`）、`FR-002` 1（`:1376`） | 同上。⚠️ 検査 39 の保持が減りうる |

## 3. グラフ

**`impact.py FR-002 T-013 T-243 OC-10 FR-094 S-22 FR-013 SH-3 SH-4 S-63`**: 284 行。`S-22` は `FR-094`・`FR-013`・表 T-243 の結び・表 T-023d の `GR-8` から指される。`S-63` は表 T-109 の `IC-40` から指される。

**`induced.py FR-002 T-013 T-243 OC-10 FR-094 S-22 FR-013 S-63 FR-085 FR-093 EP-3 FR-025 IO-6 S-56 FR-052`**: 閉路 2 —— `FR-094 ↔ S-22`、`FR-002 ↔ FR-085`。⇒ `FR-094` と `S-22` の注を同じ巡で書いた。`FR-085` は `CR-392` が同じ巡で書いた。

**届いた行を `rulings.md` で引いた**: `JDG-08`（覆す —— 決定 3）、`JDG-27` / `JDG-91` / `JDG-92`（並びとマーカーの左の名前 —— 決定 3 の後も成り立つ）、`JDG-112`（予実の表示 —— 残した）、`JDG-86`（矢印の名前が最上段で見切れる —— `OC-10` の縦の算入のまま）。

## 4. 当てた中身

| ID | 置き場 | 中身 |
|---|---|---|
| 表 T-012 の注 | `01-04-requirements.md` | 「`NL-1` の形状の中に書くは横方向の収まりだけ」を「`SH-3` / `SH-4` には 表 T-013 を当てず、規則は `FR-002`」へ |
| `FR-002` | 同（表 T-013 の後、フェードの段の前） | 決定 1 |
| `FR-002` | 同（マーカーの位置の段） | 数えるのを予実の表示の組に絞り、`S-63` が偽なら数えない（決定 3） |
| 表 T-243 の結び | 同 | 描かないときの同じ位置を予実の表示の切り替えに絞り、`S-63` が偽の段を足した（決定 3） |
| `FR-002` | 同（`SH-3` / `SH-4` の段の後、フェードの段の前） | 決定 4（矩形系は実績の中） |
| `FR-013` | 同（STATEMENT の読み替えの文の後） | 決定 4 の注（マーカーは実績の中に立つ） |
| `FR-094` | 同（床の段の後） | 決定 2 |
| `S-22` | `_source/settings.json` → 生成 `_assets/tbl-settings.md` | 注に「`SH-3` / `SH-4` の径は `FR-094` が名前の字から導く」 |

## 5. 数の予測

tables 0 / figures 0 / rows 0 / uids 0。

## 6. 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| 名称ラベルの置き場所を決めるユニット（表 T-064 の `PI-5` の `taskPlacement` の側） | `SH-3` / `SH-4` は `start` の x を箱の左端に。`NL-1` / `NL-3` を通さない。右へ出たぶんを占有幅へ |
| 同（表 T-243 の結びと `FR-002` の幅の段） | `progressMarkerVisible`（`S-63`）が偽のときマーカーの ① ② を数えない |
| 進捗マーカーと再開アイコンを描く所（`svgFromSchedule` と画面の描き手） | `SH-3` / `SH-4` の径を名前の字の大きさに。`GR-8` の当たり判定も同じ径で |

## 7. 試験が主張すべきこと

1. `SH-3` のタスクで予定の幅と実績の幅を変えても、名称ラベルの箱の左端の x は `start` の x のまま（`FR-002`）。
2. `SH-3` / `SH-4` のマーカーの径が、そのタスクの名称ラベルの字の大きさに等しい（`FR-094`）。
3. `S-63` を偽にすると、形状の外へ出した名前の左端が形状の右端 ＋ `S-32` になる。`S-227` / `S-228` を切り替えても名前の左端は動かない（表 T-243 の結び）。

## 9. ⛔ この変更でやらないこと ／ 返した問い

- ✅ 答えを得て当てた（問い 2、2026-09-16、台帳 `JDG-131`「案 A」）: 矩形系（`SH-1` / `SH-2`）は、名称ラベルの箱の幅 ＋ `S-32` ＋ マーカーの径 ＋ `S-23` ＋ `S-91` × 2 ≦ 実績の幅 のとき、実績の右端の内側に「名前 → マーカー」の順で置く（決定 4）。
- ⛔ `S-22` の値そのものを変えない
