# CR-317 — 誰も書き留めなかった裁定（ヘッダーの更新日時）

⭐ **2026-08-27 に下りた裁定を、3 日遅れで仕様書へ入れる。**
⛔⛔ **製品は最初から正しく動いていた。仕様書だけが、その裁定を 1 文字も持っていなかった。**

| | 何を | 行 |
|---|---|---|
| 1 | **画面に出す時刻は読む人のローカル時刻とする（保管は UTC のまま）** | `FR-101` |
| 2 | **更新日時の字の大きさは係数で決める（px で持たない）** | `FR-101` ／ 表 T-206 の `S-210`（新設）|

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

`GL-006`（アフォーダンスとシグニファイア）。
⭐ **ヘッダーの時刻は「最後に書けたのはいつか」を読むためのものである**（`FR-101` の RATIONALE）。
⛔ **読む人の時計と違う時刻は、その問いに答えていない。**

### ② レビュー観点のどの条項を当て、何が出たか

`docs/development-rules/07-review-standards.md` の **`R1.1`**（全数がそろっていること）と
**`R6.1`**（試験が仕様を根拠にできること）。
⇒ ⛔⛔ **利用者の指示 3 つのうち 2 つに、仕様書の行が 1 つも無かった。**
⇒ ⭐ **`AT-127` は「保管が UTC」しか言っておらず、「ローカルタイム」は `docs/spec` 全体で 0 件であった。**
⚠️ **`R6.1` に当たる** —— **仕様だけを読む体には、この振る舞いの試験が書けなかった。**

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

1. ⛔⛔ **時刻の綴りそのものを、あえて定めなかった。**
   ⭐ **宿主に渡すと明記した** —— 表 T-028 の `IN-2` がカーソルの綴りを宿主に渡しているのと同じ立場である。
   ⚠️ **これは決めない節ではない** —— **「ローカルにする」は値を変える決定である。**
   ⛔ **`PD-325` は 未裁定 のまま残した** —— **同行は `+09:00` を付ける別案を数つきで持っている。**
2. ⭐ **`S-210` の 0.75 を 🔎（測って決めた値ではない）で置いた。**
   ⛔ **利用者が言ったのは「2 段小さくしろ」であって 0.75 ではない。**
   ⚠️ **`PD-326` も 未裁定 のまま残した。**
3. ⭐ **`FR-061` ではなく `FR-101` に書いた。**
   ⛔ **`PD-325` / `PD-326` の 触る行 の欄は `FR-061` を名指しているが、実測で違っていた** ——
   **画面のその部は `fileStatus` であり、実装自身の注が `FR-101` を挙げている。**

---

## 1. ⛔ 測った

### 仕様書に無かったもの（実測 2026-08-30）

```
「ローカルタイム」「ローカル時刻」「現地時刻」 を docs/spec で引く : 0 件
ヘッダーの字の大きさの係数を持つ行                                 : 0 件
（係数を持つ行は S-197 / S-198 / S-203 / S-204 の 4 行で、どれも別の部のもの）
```

### 製品が既にしていたこと（実測 2026-08-28、出荷ビルド）

```
字の大きさ : font-size:0.75em      ⇒ 「2 段小さくしろ」を満たす
表示       : YYYY-MM-DD HH:MM:SS   ⇒ 「ローカルタイムで表示しろ」を満たす
保管       : ISO 8601・UTC・秒     ⇒ 「内部的に UTC で管理しろ」を満たす
```

⭐ **3 つとも満たされていた。**⛔ **満たしていない根拠が、仕様書の側に無かっただけである。**

### 欄が名指していた先が違っていた

⛔⛔ **台帳の欄 5 は `AT-127`（`scheduleUpdatedUtc`）を名指していた。**
⭐ **あれは日程データの群が動いた刻**（図 F-011。監視 `AG-6` が見るもの）であり、
**ヘッダーが出しているのはファイルへ最後に書いた時刻である。**
⇒ ⚠️ **検査 29 は「名指しているか」しか見ないので、この取り違えを素通りしていた。**

---

## 2. ⭐ 波及（**測った**）

| 触るもの | 何が変わるか |
|---|---|
| `FR-101` | ⭐ **2 文**。ローカル時刻の MUST と、係数の MUST |
| 表 T-206 | ⭐ **`S-210` を 1 行**（0.75 🔎）|
| `AT-127`（図 F-011）| ⛔ **変わらない**。保管の綴りであり、本要求は動かさない |
| `PD-325` ／ `PD-326` | ⚠️ **未裁定 のまま**。向きは決まったが、綴りと数はまだ利用者のものである |

```
rows 1754 → 1755（S-210 の 1 行）
tables 136 ／ figures 11 ／ uids 150 は不変
検査 29 本 ALL GREEN ／ 重複検出 A=26 (new 0)
```

⛔⛔ **1 度目の `S-210` の備考は `S-203` の文を写しすぎて、重複検出が新しい組を 2 つ立てた。**
⭐ **`S-203` を指すだけに書き直したら `A=26 (new 0)` に戻った。**
⚠️ **同じ罠をこの巡で 2 度踏んだ**（CR-316 でも 1 度）。

---

## 3. ⭐ 残ること

⛔ **`D-65` は実物で 1 度確かめれば閉じる。**
⚠️ **時刻の綴りと 0.75 は、まだ利用者に問うていない。**
