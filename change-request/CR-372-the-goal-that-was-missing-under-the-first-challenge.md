# CR-372 — 最初の課題の下に、目標が 1 つ欠けていた

> **閉じるもの**: ⛔ **利用者の裁定（2026-09-13）** ——
> 「『作図ソフトと同じ操作感で描きながら』を受ける `GL` がないな。このための `GL` を新設して、表 T-054 の受ける目標列に追記する」
> ⭐ **同（文言）**「利用者が、画面に描かれたタスクとマイルストーンを直接掴んで動かすことで日程を変えられ、握っているあいだその結果が絵に出る状態になること。はいいな」
> ⭐ **同（価値のことば）**「`WYSIWYG` を価値のことば ってのは撤回する。じかづかみとかも要らない。価値の言葉は既存の 3 つでよい」
> ⭐ **同（`GL-007` の穴）**「案 甲でよい」
> ⭐ **着地済み（2026-09-13）。**第 6 節に実測を置いた。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **触った行から、宣言された親を辿って書く。**

| 触る行 | 所有する要求 | 親 | 目標 |
|---|---|---|---|
| 表 T-001 の `PLM-8`（新設）| —— | —— | 背景そのもの |
| 表 T-054 の `CH-1` / `CH-6`（新設）| —— | —— | 課題そのもの |
| `GL-008`（新設）| —— | —— | **本書が立てる目標** |
| `FR-016` の `Relations` | `FR-016` | `UC-007` ＋ **`GL-008`** | `GL-002` ＋ **`GL-008`** |

⛔ **本書は仕様書の骨格（1.1 背景・1.2 課題・1.3 目標）を触るので、「どの目標を進めるか」ではなく「目標の集合そのものを直す」変更である。**

### ② レビュー観点のどの条項を当て、何が出たか

`R1`（要求品質）の**追跡性**。⭐ **`docs/review/dr-collision-plan.md` を書くあいだの実測で出た** ——
`CR-265` と `CR-266` が `CH-1` に `GL-001` を対にしており、表 T-054 は `GL-004` を渡していた。
その食い違いを追ったところ、**食い違いは CR の書き損じではなく、目標の集合の穴**であった。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**（すべて 2026-09-13、逐語は冒頭）:

1. 目標を新設するか ⇒ **する**
2. その文言 ⇒ **利用者が示した文をそのまま使う**
3. 価値のことばを増やすか ⇒ ⛔ **増やさない。既存の 3 つでよい**（`CH-1` のセルは `——` のまま）
4. `GL-007` の穴 ⇒ **案 甲**（背景と課題の両方を足す）

**問わずに決めた（覆してよい）**:

- ⭐ **`FR-016` を分割せず、第 2 の親を足すだけにする** —— 実測: `Relations` は複数の親を既に許しており、**26 節点が 2 つ以上の親を持つ**（`UC-007` 自身が `GL-002` と `GL-003`、`FR-031` は 6 つ）。⛔ **分割すると `FR-016` を引く `src/` と `tests/` の 125 箇所が動く。第 2 の親なら 0 箇所である。**
- `GL-008` の番号は `GL-007` の次の空き番号とする。
- `PLM-8` の番号は `PLM-7` の次とする（`PLM-2` は欠番のまま触らない）。

---

## 1. 測った（2026-09-13）

### 1.1 穴は `PLM-1` の片側である

```
表 T-001「現状」 PLM-1
  現状      日程表を作図ソフト（PowerPoint など）や表計算ソフト（Excel など）で描いている
  何が起きるか
    ① 日付が 1 つ動くたびに、図形と線を人が引き直す。更新が滞り、図と実態が離れる
    ② 他の日程管理ソフトへ日程をデータとして渡せず、機械が読めない

表 T-054「課題」 CH-1
  作図ソフトと同じ操作感で描きながら、構造化した日程データを出す
     └─ 前半が ① に答え、後半が ② に答える連言である

受ける目標
  ② → GL-004「成果物が構造化データである」
  ① → ⛔ 無い
```

⇒ **`CH-1` は害を 2 つ持つ背景に答える連言なのに、受ける目標が 1 つしかない。**
既存の 7 つで代用できないことも 1 つずつ当てて確かめた:

| 代用候補 | なぜ代用できないか |
|---|---|
| `GL-003`「操作が引っかからない」 | バーがまったく掴めなくても「引っかかって」はいない。無いだけ |
| `GL-006`「マニュアルを読まずに使える」 | 掴めることが**分かる**かの話。**掴める**かの話ではない |
| `GL-001`「1 行で全工程を見渡せる」 | 見る話であって、動かす話ではない |
| `GL-004`「成果物が構造化データ」 | 出し入れの話。掴めなくても満たせる |

### 1.2 `GL-007` には課題が無く、それは書き落としではない

- 表 T-054 の 5 行が渡す目標は `GL-001` `GL-002` `GL-003` `GL-004` `GL-005` `GL-006`。⛔ **`GL-007` はどこからも受けられていない。**
- 背景 表 T-001 は `PLM-1`〜`PLM-7`（`PLM-2` 欠番）で、**秘密・機密に触れる行が 1 つも無い。**
- 1.2 の導入文は「**背景の現状を解くために**何を成すかを表 T-054 に示す」と定める。
- ⇒ **背景の行が無いから、課題を書きようがなかった。**
- ⭐ **ただし木は切れていない** —— `GL-007` → `UC-009`（透かしを重ね、消すには解除パスワードを求める）→ 要求群。達成の鎖は通っている。欠けているのは 1.2 の枠組みだけである。
- `GL-007` は 2026-08-12 の `e12fc15`（最初の執筆期）で目標の初期集合と一緒に入っており、裁定の記録は無い。

### 1.3 木は既に完全で、複数の親を許している

```
GOAL           7        （本書で 8 になる）
  ↑ Relations: Parent / Satisfies
USE_CASE      14        14 すべてが GOAL を親に宣言
  ↑
FUNC_REQ      99        88 が USE_CASE を、11 が GOAL を親に宣言
NON_FUNC_REQ  13        13 すべてが GOAL を親に宣言
  ↑
表の行（GR- / IN- / MK- / S- …）はその表を持つ要求に属する

親を持たない節点 0。⭐ 検査 2（parentless nodes）が毎回これを守っている。
Parent 関係 159 本。⭐ 2 つ以上の親を持つ節点 26。
```

### 1.4 掴みの規則は `FR-016` にほぼすべて集まっている

`表 T-023d` / `表 T-244` / `GR-n` を名指す回数で数えた:

```
178  FR-016  マウスとキーボードの割当           親: UC-007 → GL-002
 31  FR-043  実績がまだ無いタスクにも入力の入口を出す   親: UC-005 / UC-006
  9  FR-003  重なるタスクを縦に積む              親: UC-001
  8  FR-040  入力の作法                     親: GL-006
```

⇒ **`FR-016` が 178 で突出**。⛔ そして `FR-016` の親は `UC-007`「見たい範囲を出す」→ `GL-002`「全体日程を 1 画面で見られる」であり、**掴みの規則が「1 画面で見られる」の下にぶら下がっている。**

⚠️ ただし `FR-016` は表を 5 つ持つ相互作用の中枢（`T-023` `T-023a` `T-023b` `T-023d` `T-244`）で、**拡大縮小とパンとホイール**も持つ。⇒ 親を**付け替える**のではなく**足す**のが正しい。

---

## 2. 編集計画（オブジェクトごとに 1 回）

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `docs/spec/01-04-requirements.md` 1.3 | **`GL-008` を新設**（下記の逐語）|
| 2 | 表 T-001 | **`PLM-8` を 1 行新設**（`PLM-7` の次）|
| 3 | 表 T-054 | `CH-1` の**受ける目標を `GL-008` / `GL-004` に**。⛔ 価値のことばの列は `——` のまま触らない |
| 4 | 表 T-054 | **`CH-6` を 1 行新設**（`GL-007` を受ける）|
| 5 | `FR-016` の `Relations` | **`GL-008` を第 2 の親として足す**（`UC-007` は残す）|
| 6 | `.claude/skills/spec-graph-check/check-cr-discipline.py:47` | `(CH-[1-5]\|GL-00[1-7])` を `(CH-[1-6]\|GL-00[1-8])` に |
| 7 | `A-appendix` | 変更履歴に 1 行 |

### 1 の逐語（利用者の文をそのまま）

```
#### 描いたものを直接掴んで日程を変えられる

**Type**: GOAL
**UID**: GL-008

**STATEMENT**: 利用者が、画面に描かれたタスクとマイルストーンを直接掴んで動かすことで
日程を変えられ、握っているあいだその結果が絵に出る状態になること。
```

### 2 の逐語（⛔ 背景は利用者の主張なので、文言の裁定を仰ぐ）

```
| PLM-8 | 日程表を画像や資料として配ると、誰がいつ持ち出したか追えない | 秘密を含む日程が外へ出たとき、出どころが辿れない |
```

### 3 と 4 の逐語

```
| CH-1 | 作図ソフトと同じ操作感で描きながら、構造化した日程データを出す | —— | `GL-008` / `GL-004` |
| CH-6 | 秘密の日程を画面に出したとき、誰がいつ出したかを絵の中に残す | —— | `GL-007` |
```

---

## 3. 数の予測（⛔ 編集後に実測し、外れたら外れたと書く）

| 測るもの | いま | 予測 |
|---|---:|---|
| GOAL の数 | 7 | **8** |
| 表 T-001 の行 | 7（`PLM-2` 欠番）| **8** |
| 表 T-054 の行 | 5 | **6** |
| 表 T-054 が渡す目標 | 6 種（`GL-007` が漏れ）| **8 種。漏れ 0** |
| `Parent` 関係 | 159 | **160** |
| 親を持たない節点 | 0 | **0** |
| 価値のことば（表 T-106）| 3 | **3（変えない）** |
| `src/` と `tests/` の変更 | —— | **0 箇所**（`FR-016` の ID は変わらない）|
| `vitest` | 202 / 7,479 | ⛔ **同じであること。1 件も減らすな** |

⚠️ **赤くなる見込みのもの**（前例あり）:

- StrictDoc の節点数（検査 1〜4）—— GOAL が 1 増える
- 検査 22 —— `GL-00[1-7]` の literal。⛔ **編集 6 を同じ手でやらないと、`GL-008` を名指した CR が「名指していない」と読まれる**
- 検査 37（辞書と表の指紋）—— 表 T-054 の `CH-1` の行が動く
- ⭐ **仕様に行を足すと contract 試験が意図どおり赤くなる** —— `IV-20` を足したときの実例がある。表を実行時に読む試験があれば同じことが起きる

---

## 4. ⛔ この変更でやらないこと

- ⛔ **価値のことばを増やさない**（利用者の裁定）。`WYSIWYG` は 表 T-041 と `FR-080` が既に別の意味で使っており、`GL-008` に当てると 1 つの綴りが 2 つを指す —— `PD-` / `D-` / `R-` で消したばかりの欠陥と同型である
- ⛔ **`FR-016` を分割しない**（`src/` と `tests/` の 125 箇所が動く）
- ⛔ **`CH-1` の連言を割らない**（列は複数の目標を持てる。`CH-2` が既にそうしている）
- ⛔ **`CR-265` / `CR-266` の訂正は本書では行わない** —— `GL-008` が landed してからでないと正しく書けない。別書で閉じる
- ⛔ **`FR-043` ほかへ `GL-008` を足すかは本書では決めない** —— 測った候補は 1.4 に置いた。1 件ずつ裁く

---

## 5. 裁定を仰いだ 1 点 —— 裁定済み

**`PLM-8` の文言。**背景は観測ではなく**この製品が世界をどう見ているかの主張**であり、前に立つ者が勝手に書くものではないので問うた。
⭐ **利用者の裁定（2026-09-13、逐語）「提案通りでよい。先に進め」** ⇒ 案のまま入れた。

---

## 6. 実測（編集後 2026-09-13）

### 6.1 予測した 7 つは全部当たった

| 測るもの | 予測 | 実測 |
|---|---|---|
| GOAL の数 | 8 | **8** ✅ |
| 表 T-001 の行 | 8 | **8** ✅ |
| 表 T-054 の行 | 6 | **6** ✅ |
| 表 T-054 が渡す目標 | 8 種・漏れ 0 | **8 種・漏れ 0** ✅ |
| `Parent` 関係 | 160 | **160** ✅ |
| 親を持たない節点 | 0 | **0** ✅ |
| 価値のことば | 3 | **3** ✅ |
| `src/` と `tests/` の変更 | 0 箇所 | **0 箇所** ✅ |
| `vitest` | 202 / 7,479 | **202 / 7,479 ＋ skipped 1** ✅ |

### 6.2 ⛔ 外した予測が 1 つある

**「仕様に行を足すと contract 試験が赤くなる」と書いたが、赤くならなかった。**
`IV-20` のときは表 T-220 を実行時に読む試験が在ったが、**GOAL と 課題 の表を実行時に読む試験は無い**。
⇒ ⭐ **前例を当てはめる前に、その表を読む試験が在るかを測るべきだった。**

### 6.3 門が 2 つ捕まえた —— どちらも正しい

| 何を | なぜ |
|---|---|
| **検査 3（ORIGIN vs Relations）** | `FR-016` の `Relations` に `GL-008` を足して `ORIGIN` を揃え忘れた。⭐ 2 つを別々に持つ以上、必ず両方動かす |
| **precheck の罠 3（生成物の手編集）** | `tbl-row-id-prefixes.md` が原稿なしで差分に出た。⛔ **罠が正しかった** —— この生成物は**3 つの木を歩いて行数を数える**のに、来歴が `row-id-prefixes.json` しか名乗っていなかった。生成器の銘板に 3 つの木を書き足して直した（検査 21 の規則そのもの）|

### 6.4 最終の門

```
precheck   OK 6 file(s), 6 challenge pairing(s) known   ← CH-6 を拾っている
gen:check  緑
tsc        緑
vitest     202 ファイル / 7,479 緑 ＋ skipped 1（不変）
check.sh   46 検査すべて緑、終了コード 0
```
