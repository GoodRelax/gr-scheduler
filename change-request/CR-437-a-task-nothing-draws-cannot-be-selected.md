# CR-437 — 描かれていないタスクは選ばせない

> ⭐ **状態: 当てた（2026-09-21、ブランチ `rebase-tests` の `ea039e52` の上）。**
> 当てた先は `docs/spec/01-04-requirements.md` の 2 か所（表 T-023c の結び、`FR-049` の隠した図形の段）と、
> `docs/development-records/changelog.md` の版 2.64 の 1 行である。
>
> **裁定**: `docs/development-records/rulings.md` の `JDG-288`。
> **閉じる欠陥**: `docs/development-records/defects.md` の `DFC-683`（`仕様待ち` から `実装待ち` へ。⛔ 本書は台帳を書かない。前に立つ者が書く）。
>
> **⛔ 本書がしないこと**: `src/` と `tests/` を 1 行も直さない（7 節）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

`CH-4`（すぐわか）と `GL-006` を前へ進める。  
選んだのに何も見えないタスクが選択に残ると、利用者は「何が選ばれているのか」を画面から読めず、削除（`FR-032`）や整列（`FR-034`）が見えない相手に効く。  
描かれている物だけが選べれば、選択は画面に見えているものと一致する。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`**（矛盾が無い・唯一の正がある）—— `docs/development-rules/07-review-standards.md:569`。
  ⛔ **違反していた。** `SL-8` の「選択を色以外の手掛かりでも示すこと（MUST）」と、`FR-049` の「隠した予定・実績・ダミーは描かない（MUST）」が、両方を隠したタスクで同時に立たない（`DFC-683`）。
  本書はその組そのものを選択の対象から外すので、2 つの MUST がぶつかる場面が消える。
- **`R1.4`**（異常系・準正常系の網羅）—— 同書 `:570`。
  選んだあとで隠す、という順の操作が仕様に無かった。本書はその時点で選択から外すと定める。
- **`R4`**（状態遷移）—— 選択は文書の外の状態であり（表 T-027 の `UN-9`）、表示の切り替えも取り消しの外（`UN-7`）である。
  2 つの外の状態の間の遷移を、切り替えの時点に 1 つだけ置いた。
- **`R2`**（名前を足すとき）—— 新しい名前を 1 つも足さない。行 ID も表番号も作らない（1 節 (e)）。

### ③ 利用者に問わずに決めたこと

1. **行を足さず、表 T-023c の結びに段を足した** —— `CR-300` が選択の不変条件を `SL-9` として書き、ヘルプの生成器に止められて結びへ移した前例に従った（`change-request/CR-300-the-eight-rulings-and-the-pictures-they-came-from.md:37`）。
2. **MUST は 1 つだけにした** —— 「選択は、描かれている対象だけを指すこと（MUST）」。3 点の中身はその 1 文を説明する文として書き、それぞれに印を付けない。
3. **「描かれていない」を表示の可否（表 T-202、`FR-049`）によるものに限った** —— 縮めたときにタスクを落とす段（LOD）で描かれないタスクまで選択から外すことは、裁定が扱っていない（7 節）。
4. **`FR-049` の側には MUST の無い 1 文だけを置き、結びを指させた** —— 規則の正を 2 か所に書かない（`R1.3`）。

---

## 1. 読んで確かめた事実（`ea039e52` で測った。2026-09-21）

- (a) 選択の行 `SL-1` 〜 `SL-8` は **表 T-023c**（`FR-081`）に在る —— `docs/spec/01-04-requirements.md:2193-2210`。
  ⚠️ `JDG-288` の文面と依頼文は「表 T-022」と書くが、**誤記である**。表 T-022 はイナズマ線の頂点の表である（`:3088` 付近）。本書は T-023c と書く。rulings.md の直しは前に立つ者が行う。
- (b) `FR-049`（`:4855`）は隠した予定・実績・実績のダミーについて「占有を残し、描かず、当たり判定から外すこと（MUST）」と定め、両方を隠すことを許す（「両方を非表示にできてよい」）。
- (c) ⚠️ **ダミーは `S-227` では隠れない。** `FR-043`（`:2763`）は未着手のあいだダミーを描くと定め（MUST）、表 T-240 の `DM-14`（`:2803`）はダミーを `S-228` が偽のときだけ描かないとし、「予定の表示（`S-227`）で、ダミーを描くかどうかを変えてはならない（MUST NOT）」と書く。表 T-270 の `PE-4` / `PE-5` はダミーを押すとタスクを「選ぶ」と定める。
- (d) `SK-2`（表 T-036、`:4159`）は「選択できるものをすべて選択する（表 T-023c の SL-1）」と書き、表 T-023c を指すだけである —— 本書の結びがそのまま効くので、動かさない。`MK-11`（`:3301`）は解除だけを定め、影響を受けない。
- (e) 識別子: `change-request/` に `CR-437` の名を持つファイルは 0 件だった（2026-09-21 に測った）。
  `SL-9` は `CR-300` が一度名乗った名であり（上の ③ の 1）、本書は使わない。本書が仕様に足す新しい識別子は 0 である。

## 2. 前に立つ者の例の誤りを、利用者の原則に合わせて正した

`JDG-288` の ① は利用者が自分で打った言葉であり、原則は「**非表示にしたものは選択できない**」である。  
② の 3 点は前に立つ者が選択肢に書いた文であり、そこに「予定を隠して実績が無い」タスクを描かれていない例として挙げていた。  
1 節 (c) のとおり、そのタスクにはダミーが描かれ、押せば選ばれる —— **ダミーは非表示ではない。**  
⇒ 例のほうが誤っていた。本書は利用者の原則を正とし、**「描かれていない」＝ 予定・実績・実績のダミーのどれも描かれていない**と定義した（前に立つ者が 2026-09-21 にこの読みを採った）。

## 3. グラフ（測った先）

- `impact.py FR-081`: 指す先は 表 T-023c・T-036・T-206・T-236・T-020・T-023・T-027・T-050・T-024a。指されるのは `FR-083` と `05-07-design.md:437` の 2 か所。
- `impact.py FR-049`: 指す先は 表 T-202・T-023a・T-016・T-018b・T-109・T-020a・T-038・T-023c・T-272・T-243。指す側は要求 13 件・参照 39 か所。
- `induced.py FR-081 FR-049 FR-034 SK-2 PE-4 PE-5 DM-14 FR-043`: 種 8 件がすべて解決した。
  当てる前は閉路 0 件。当てた後は **`FR-049` ⇄ `FR-081` の大きさ 2 の閉路が 1 つ**できる —— 規則を持つ結びと、それを指す 1 文の組であり、作法どおりである。2 つは本書の 1 回で書いた。

## 4. 当てた文（旧 → 新）

### 4.1 表 T-023c の結び（`01-04-requirements.md:2216-2222`。段を 1 つ足した）

旧: （無し。結びは「⚠️ いつ落とすかは本規則が定めない」で終わっていた）

新:

```
選択は、描かれている対象だけを指すこと（MUST）。
描かれていないタスクとは、表 T-202 の表示の可否（`FR-049`）によって、予定・実績・実績のダミー（表 T-240）のどれも描かれていないタスクである。
そのタスクは、`SL-2` 〜 `SL-5` のどの選び方でも取らない —— 全選択（`SL-5`）も範囲選択（`SL-3`）も取らない。
表示の切り替えでそうなったタスクは、その時点で選択から外し、`SL-7b` の順序からも除く —— 除かないと、`FR-034` の「最後に選んだタスク」が見えないタスクになり、見えない日付へ揃う。
⚠️ 消えた対象を落とす時点を定めない上の段とは違い、こちらは時点を定める —— 入口は表示の切り替えの 1 つだけであり、選んだまま隠れた対象は、隠れているあいだ選択の枠（`SL-8`）が描けない。
予定と実績の片方だけを隠したタスクは、見えている図形で選べる。
`SL-8` の外接矩形は、見えている図形だけから組む —— 隠した図形の占有（`FR-049`）を数えると、何も描かれていない所を枠が囲む。
```

裁定の 3 点との対応: (1) は 3 行目、(2) は 4 行目、(3) は 6・7 行目。

### 4.2 `FR-049` の隠した図形の段（`:4896`。1 文を足した。MUST なし）

旧: 「……見た目は透明に描いたときと同じであり、表示を切り替えても行の帯高も段も動かない。」の次は「⚠️ **描く札は動く**」だった。

新: その間に次の 1 文を置いた。

```
⭐ 選択は描いた図形だけを見る —— 何も描かれていないタスクを選ばせないことと、選択の枠が囲む図形は、`FR-081` の 表 T-023c の結びが持つ。
```

### 4.3 変更履歴

`docs/development-records/changelog.md` に版 2.64 の 1 行を足した。

## 5. 数の予測

- tables = 0（新しい表を作らない）
- rows = 0（新しい行を作らない。表 T-023c は 10 行のまま）
- uids = 0
- figures = 0
- 検査 39 の数: 1362 → 1363（新しい MUST が 1 つ）

## 6. 影響

- **仕様**: 上の 2 か所だけ。`SK-2`・`MK-11`・`FR-034`・`SL-8` の文は動かない（1 節 (d)）。
- **記録（⛔ 本書は書かない。前に立つ者が書く）**: `JDG-288` を適用済にし、着地先の「表 T-022」を「表 T-023c」に直す。`DFC-683` を `実装待ち` にする。検査 39 の基準線を 1363 へ上げるかどうかは利用者の OK が要る。
- **実装（別の波）**: 全選択と範囲選択の対象の絞り込み、表示の切り替え（`S-227` / `S-228`）で選択と選んだ順序を刈る経路、選択の枠を見えている図形だけから組むこと。`DFC-683` が挙げた `src/adapter/svg-renderer/schedule-task-figures.ts:426-432` の `if (box !== null)` は、本書のあとでは「描かれていないタスクは選択に無い」ことで守られる。

## 7. ⛔ この変更でやらないこと

- `src/` と `tests/` を直さない —— 実装は別の波で行う。
- 縮めたときにタスクを落とす段（LOD）で描かれないタスクを、選択から外すかどうかは定めない —— `JDG-288` は表示の切り替えを裁いたのであり、倍率を裁いていない。
- `PE-4` / `PE-5`（ダミーを押すと選ぶ）と `DM-14` は変えない —— 2 節の定義により、ダミーが描かれているタスクは描かれているタスクである。
- rulings.md・defects.md・handoff.md・検査の基準線を書かない。
