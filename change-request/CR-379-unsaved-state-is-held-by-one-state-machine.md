# CR-379 — 保存しない状態を、自前の状態機械 1 つで持つ（リファクタ段 2b）

> **閉じるもの**: ⛔ **利用者の裁定 `JDG-45`（2026-09-13）・`JDG-59`・`JDG-60`** —— 逐語は `docs/development-records/rulings.md` の各行。
> - `JDG-45`: 「だったら、自前でステートマシンを実装しよう。」
> - `JDG-59`: 「これまでのところの提案はすべてOK。」（状態機械は集約型、クラスを作らない、遷移は領域ごと、原稿 1 つから図・表・型を生成し契約試験で結ぶ）
> - `JDG-60`: 「OK。 ただし、計画したらブランチを分けよう。」（置き場 —— UseCase に専用コンポーネント 1 つ、`ScreenState` を広げる、現在値と副作用の実行はシェル）
>
> ⛔ **起草だけである。仕様書・コード・試験・台帳は 1 文字も動かしていない。** 読んだ木は `3cf64a4`。
> ⭐ 置く位置はリファクタ計画（`docs/development-records/refactor-plan-report-2026-09-13.md`）の状態表の **段 2b**。入口の段 1（棚卸し）は済んでいる。
> ⛔ **段 2a（`CR-378`。辺の定義を 5.2 へ、JSON の置き場）は本書の外である。** 境目は第 8 節。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **直接に前へ進める `CH-` / `GL-` は無い —— 基盤の作業である。**
利用者に見える振る舞いを 1 つも足さず、`frame-loop.ts` の `let` 60 に散った保存しない状態の持ち方を、1 つの形に揃える。

守るもの（壊さないことを本書の規則で縛るもの）:
- **`GL-003`（ぬるサク）** —— 何も変わらない出来事では同じ参照を返す（`SF-3`）ので、`NFR-010`「利用者が操作していない間、画面を描き直さないこと」（`01-04-requirements.md:6086`）を割らない。性能の合否は `JDG-50`（段 0 の値より悪くしない）のまま。
- **`GL-008`（握っているあいだ結果が絵に出る）** —— 身振りの状態を機械へ移しても、連続して変わる座標は機械の外に置く（`SF-5`）ので、握っているあいだの追従はフレームの値のまま。

### ② レビュー観点のどの条項を当て、何が出たか

（`docs/development-rules/07-review-standards.md`）

- **`R2` 命名（品詞の規約）** —— UseCase のコンポーネントは動詞句（`AdvanceScreenSession` は `ApplyDocumentChange` / `NotifyChangeWatchers` と同じ形で合格）。⚠️ **出来事は過去形**と同じ表が定めている —— 計画の記録 4・12 はこれに触れていない ⇒ `SF-1` に入れた。
- **`R2.2`（SRP）** —— `step` を 1 つの `switch` に集めると新しい神になる ⇒ 領域ごとのユニットに割る（`SF-8`、`UT-2` と同じ理由の形）。
- **`R2.18`（MUST。ADR-000 で最小構成と比べる）** —— 状態機械は最小構成に対して増やすものなので、**表 T-070 に行が要る。⚠️ 計画の記録 12 の「仕様への影響」はこれを落としていた** ⇒ 第 5 節の `MN-9`。
- **`R2.19`（コンポーネント境界）** —— 新しいコンポーネントも公開エントリ 1 つだけを外へ見せる（5.3 の規則のまま）。
- **`R4` グリッジ** —— 「Ch5.5 の状態遷移図が実装可能な形で定義されているか」「複数フィールドの同時更新の原子性」⇒ `step` は次の状態を丸ごと返し、シェルは参照 1 つを差し替える（`SF-2`・`SF-7`）。遷移は原稿から図を生成し、契約試験で関数と結ぶ（`SD-3`）。
- **`R4` レースコンディション**（`await` をまたぐ共有状態）—— 副作用の結果は出来事として戻す（`SF-6`）ので、`await` の後に現在値を直に読み書きする形（棚卸しの A〜E 節で「非同期: はい（直）」の行 12。`awk` で節を切り `grep -c "はい（直）"`）が構造上なくなる。
- **`R7.1` / `R7.2` / `R7.9`** —— 判断（`step`、`pure`）と実行（副作用の実行、`non-pure`）を別の層に置く。
- **`R2.20`（キャッシュ）** —— 該当しない。同じ参照を返すことはキャッシュではない（無効化の契機を持たない）。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**（本書を起こす前）: `JDG-45`（自前）、`JDG-59`（集約型・クラス無し・領域ごと・原稿 1 つから生成・契約試験）、`JDG-60`（置き場）。

**問うこと**: ⭐ **なし（0 件）。** 問いの候補 7 つに `handoff.md` §0.2 の 3 手を打ち、すべて導けた（第 4 節）。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| D-1 | **遷移の持ち主は「新しい原稿」**（計画の記録 4 の 2 択の後者） | `JDG-59` の読み「状態と遷移は原稿 1 つ（仮称 `state-machines.json`）から図・表・型を生成し、契約試験で遷移の関数と結ぶ」がそのまま後者である。記録 4 は「段 1 の結果を見て選ぶ」と書いたが、**`JDG-59`（2026-09-13）が既に選んでいる**。段 1 の棚卸しの記録 5 の推し（名指す状態が `frameLoop` の 38/60）も同じ向き |
| D-2 | ⭐ **本書は原稿のファイルを作らない。原稿の規則だけを作る**（第 3 節の 2b-A）。原稿と最初の行は、段 7 の最初の領域（画面の値）の変更要求が、その行を読むコードと同じ波で置く | 規則 02 の 3.5「原稿の行と、それを読む側の直しを 1 つの波にする」。いまその原稿を読むコードは 0 行である（`grep -rn "state-machines" src tools` 0 件）。記録 12「原稿に載せるのは、要求が名指す状態・出来事・遷移だけ」⇒ 行は領域ごとの変更要求が名指す要求を読んで書くもので、行の無い原稿を先に置くと、生成する表は空、生成する型は読み手 0 になる。⭐ 行 ID の接頭辞を行より先に置く形は、登録簿の原稿が `pending: true` として既に用意している（`_source/row-id-prefixes.json` の `$comment`「registered ahead of the wave that writes its rows」） |
| D-3 | ⭐ **表 T-062 / T-064 / T-075 / T-074 と `components.json` の変更も、段 7 の最初の領域のコードと同じ波で着地させる**（2b-B）。本書はその中身を決めるだけ | 検査 18（`tools/generate_unit_tree.py --check`）は「`src/` のパスの集合が表 T-075 と同じ」を見る（同ファイルの docstring「compares the SET of paths」）。検査 26b（`check-published-members.py`）は表 T-064 のメンバが公開エントリから出ていることを見る。⇒ ファイルの無いユニットの行、実装の無いメンバの行を先に置くと赤になる。規則 02 の 3.5 と同じ理由の形。`JDG-48`「ファイルを割る・足すたびに変更要求を起こす」も、領域ごとのユニットの行を領域の変更要求に置くことを求める |
| D-4 | 名前: コンポーネント `AdvanceScreenSession`（公開の操作 `advanceScreenSession`）、集約の型 `ScreenSession`、原稿 `docs/spec/_source/state-machines.json`、行 ID の接頭辞 `SM`（状態）・`EV`（出来事）・`TN`（遷移）。仮称を正式の名にする | `JDG-59` / `JDG-60` が仮称のまま了承した。`R2` 命名（UseCase は動詞句、公開の操作はその camelCase —— 5.3 の規則）に合う。`ScreenSession` は `docs/spec` に 0 件（第 1 節）、「セッション」は `01-04-requirements.md:5360` の一般語と `importSessionId` だけで衝突しない。⚠️ `U-51` が「画面の状態」という日本語を禁じているので、`ScreenSession` にも日本語の呼び名を当てない。接頭辞の空きは第 1 節で測った |
| D-5 | ADR-002 の数え上げられる中身を表 T-247（仮番）に、原稿が持つもの・持たないものを表 T-248（仮番）に置く。行 ID の接頭辞は `SF`（State-machine Form）と `SD`（State-machine Definition） | 5.6 の冒頭「4 項目は散文で書き、表にしない…数え上げられる中身だけを番号付きの表にする」（`05-07-design.md` 5.6、ADR-001 が表 T-071 を持つ前例）。表 T-248 を 5.5 に置くのは、`R4` が状態遷移図の置き場を Ch5.5 と定め、図 F-018 も 5.5 にあるから。原稿の説明を図の節に置く前例は 5.2 の「図の原稿は `_source/components.json` である」。⚠️ 表の番号は、`CR-376`〜`CR-378` と同じ仕様のセッションで当てるときの順で振り直す（いまの最大は T-246） |
| D-6 | **領域をまたぐ優先順の「順」の正は `IN-4` に残す。** 原稿の優先順の項目は、奪い合う出来事・順を決めた要求の行（`IN-4` など）・段ごとの状態のキーを持ち、順を写して正にはしない | 記録 12 の表が状態について既に同じ形を取っている —— 既定値と上下限は「表 T-206 が持ち、`S-` 行を指すだけ」。1.9 の「同じ式が 2 か所にあれば、片方だけが直されて必ず離れる」（`PI-5` が引く）。`IN-4` は要求 8 件・参照 22 箇所から指されている（第 2 節）ので、要求の側を原稿へ移すと影響が大きすぎる。⛔ 原稿の段の並びと `IN-4` の段を機械で突き合わせる方法は、本書では決めない（原稿を作る波で生成器の `--check` に持たせるかを決める。第 8 節） |
| D-7 | UseCase のモジュールスコープの可変状態 2 つ（`deliveringNotices`・`REGISTRATIONS`）は、セッションの領域（通知・Agent API）へ移し、シェルが持つ 1 つの現在値の一部として引数で渡す。表 T-065 は増やさない | 5.3「どのコンポーネントもインスタンスを作らない…**内側の 3 層には漏らせる可変状態がそもそも無い**」（`05-07-design.md:243`）と `LY-5`「現在値を保持するのはこの層だけである」（`:57`）が、いまのコードの 2 つを既に禁じている。⇒ 仕様が正、直すのはコード。記録 12「表 T-065 は増やさない」⇒ 新しいインターフェースではなく引数で運ぶ。棚卸しの E 節は 2 つを「通知」「Agent API」の領域に置いた。⛔ 公開の署名（`PI-8` / `PI-15` のどのメンバが `pure` になるか）は、その領域を移す変更要求が決める |
| D-8 | `ScreenState`（`CP-36`）を広げる先は、記録 12 の 7 つ —— 開いている面・構え・パレット・全画面・透かし・`Dual Cursor` の追従側・出ている説明。流れの状態（問いの待ち・合流の候補など、UseCase の型を運ぶもの）は `ScreenSession`（UseCase）が持つ | `JDG-60`「`ScreenState` を広げる」＋ 記録 12「流れの状態は UseCase の型（合流の候補、文書の命令）を運ぶので、Entity に置くと外向きの依存になる」（表 T-061 の依存の向き） |

---

## 1. 測った —— 計画の数を測り直した（`3cf64a4`）

| 数 | 計画の値 | 実測 | 測り方 |
|---|--:|--:|---|
| `frameLoop` が直に持つ `let` | 60 | **60** | `grep -nE "^  let " src/framework/single-html-shell/frame-loop.ts` ⇒ 62 行。`frameLoop` の開始 1463（`grep -nE "^(export )?(async )?function "`）と、その後の最初のトップレベルの閉じ 3678（`grep -n "^}"`）の範囲で切ると 60。外の 2 つは 1274・1319 |
| `frameLoop` の範囲 | 1458〜3630（棚卸しの記録 1） | **1463〜3678** | 同上。⚠️ **棚卸しの行番号は古い**（例: `held` は棚卸し 1471、いま 1476）。数は変わらない |
| UseCase のモジュールスコープの可変状態 | 2 | **2** | `grep -rnE "^(export )?(let\|var\|const) " src/use-case` から、`let` と `new Map/Set/WeakMap/WeakSet`・`[]`・`{}` の `const` を拾い、`名前\.(set\|add\|delete\|clear)\(` で書き換えを grep した。`deliveringNotices`（`apply-document-change.ts:69`、`let`）と `REGISTRATIONS`（`notify-change-watchers.ts:37`、書き換え 3 か所）。`ROUTES`（`edit-document.ts:234`）は `ReadonlyMap` で書き換え 0、`NO_MESSAGES`・`NO_REFUSALS` は読むだけ |
| 棚卸しの一覧の行数 | 101 | **101** | 棚卸しの A〜E 節で、第 1 セルがバッククォートで始まる表の行を節ごとに数えた ⇒ A 60・B 1・C 10・D 25・E 5 |
| `ScreenState` の項目 | 5 | **5** | `src/entity/document-model/screen-state/screen-state.ts:12-17` —— `armed`・`paletteShown`・`fullScreen`・`surface`・`watermarkVisible` |
| `ScreenState` の項目を仕様が名指す数 | —— | `PI-36` は 5、**`CP-36` は 4** | `05-07-design.md:141` の `CP-36` は「構えと `S-99e` / `S-99f` / `S-99g`」で、`S-144`（透かし）を落としている。`:427` の `PI-36` は `screenStateWithWatermark`（`S-144`）を持つ ⇒ 2b-B で `CP-36` を書き直すときに揃える |
| `ScreenSession` が `docs/spec` に現れる数 | 0 | **0** | `grep -rn "ScreenSession" docs/spec`（全ファイル種、`output/` を含む）⇒ 0 件。台帳の `PND-141`〜`PND-144` だけが使う |
| `SM` / `EV` / `TN` の空き | 無い | **使用 0・登録 0** | `grep -rhoE "\b(SM\|EV\|TN)-[0-9]+[a-z]?\b" docs/spec docs/development-records docs/development-rules change-request src tests` ⇒ 0。`grep -c '"prefix": "SM"'` など `_source/row-id-prefixes.json` ⇒ 0 |
| `SF` / `SD` の空き | —— | **使用 0・登録 0** | 同じ 2 つの grep（`docs change-request src tests`）。⚠️ 候補の `SH`（192）・`SC`（56）・`SO`（36）は使用中なので退けた |
| 表の番号の最大 | —— | **T-246** | `grep -rhoE "表 T-[0-9]+" docs/spec/*.md docs/spec/_assets/*.md` の数の最大 |
| `frame-loop.ts` を import する試験 | 記録 5: 51 ／ 並行作業の表: 56 | **56** | `grep -rlE "from ['\"][./]*.*single-html-shell/frame-loop" tests \| wc -l`。⚠️ **記録 5 の 51 は古い**（名を含むだけのファイルは 71） |
| 表 T-062 の行（コンポーネント） | 36 | **36** | `components.json` の `nodes` 36、`edges` 85（`python -c` で数えた）。5.3 の「36」を刷る散文は `:259`・`:280` と `MN-2`（`:809`）、表 T-074 の `SU-1` |
| 表 T-075 の行（ユニット） | —— | **68** | `impact.py T-075` の「行: 68 行」、表 T-074 の `SU-3` の「68」 |

---

## 2. グラフ

**`impact.py LY-3 LY-5 CP-18 CP-25 CP-36 T-062 T-063 T-064 T-065 T-075 UT-6 PI-18 PI-25 PI-36 UF-47 UF-48 UF-59 T-060 T-074 SU-1 SU-3 MN-2 MN-7 WS-2 IN-4 S-99e S-99f S-99g S-144 UT-1 UT-3 PI-8 PI-15 CP-8 CP-15`**（出力 550 行）:

| 種 | 指している要求 / 参照 |
|---|---|
| `LY-3` | 0 / 2 |
| `LY-5` | 1 / 9 |
| `CP-18` | 0 / 2 |
| `CP-25` | 0 / 7 |
| `CP-36` | 0 / 3 |
| 表 T-062 / T-063 / T-075 / T-060 / T-074 | 要求 0（5.2・5.3・5.5・5.6 の節からだけ指される） |
| 表 T-064 | 要求 2（2 次 5） |
| 表 T-065 | 要求 1（`FR-040`。2 次 `FR-092`） |
| `WS-2` | 2 / 8 |
| `IN-4` | 8 / 22 |
| `S-99e` | 1 / 9 |
| `S-99f` | 0 / 3 |
| `S-99g` | 3 / 14 |
| `S-144` | 0 / 1 |
| `UT-1` / `UT-3` / `PI-8` / `CP-8` | 0 / 2 ・ 0 / 1 ・ 0 / 2 ・ 0 / 1 |
| `UT-6` `PI-18` `PI-36` `UF-47` `UF-48` `UF-59` `SU-1` `MN-2` `MN-7` `PI-15` `CP-15` | 0 / 0（`PI-25` は 0 / 1、`SU-3` は 0 / 1） |

届いた要求（出現の多い順）: `FR-053` `FR-076` `FR-040` `FR-009` `FR-070` `FR-016` `FR-020` `FR-102` `FR-092` `FR-075` `FR-071` `FR-049` `FR-013`。

**`induced.py`**（41 種: `T-060 LY-1 LY-3 LY-5 T-062 CP-8 CP-15 CP-18 CP-25 CP-36 T-063 UT-1 UT-3 UT-6 T-064 PI-8 PI-15 PI-18 PI-25 PI-36 T-065 T-075 UF-47 UF-48 UF-59 T-070 MN-2 MN-7 T-074 SU-1 SU-3 T-067 WS-2 IN-4 AG-6 S-99e S-99f S-99g S-144 T-071 CA-1`）:
**41 種すべて解決、種の間の辺 34、閉路 0。** ⇒ 1 対象ずつ書いてよい。
⚠️ `induced.py` が読む StrictDoc の書き出しは、この作業木に無かったので `strictdoc export docs/spec --formats=json --output-dir scratch/spec-check/sd-out --no-parallelization`（`check.sh` の 1〜4 と同じ命令だけ）で `3cf64a4` の木から作った。`scratch/` は gitignore の対象。
⚠️ 新設の表 T-247 / T-248、`MN-9`、`CP-39` はまだ無いので種にできない。適用の後にもう一度走らせる。

**届いた行を `rulings.md` で引いた**（`` `ID` `` と語の出現）:

| 行 / 語 | 裁定 | 着地の条項を読んだ結果 |
|---|---|---|
| 表 T-065 | `JDG-68` | `IF-9` を正とし仕様は変えない。本書も表 T-065 を増やさず、`IF-9` に触れない ⇒ 衝突なし |
| 表 T-075 | `JDG-48` | 「ファイルを割る・足すたびに変更要求を起こす」⇒ D-3 の、領域ごとのユニットの行を領域の変更要求に置く形を支える |
| 表 T-206 | `JDG-65` | ハイライトボックスの四隅の掴み代の行（`HB-4`）。本書が触れる `S-99e`〜`S-144` とは別の行 ⇒ 衝突なし |
| `FR-013` | `JDG-44` | `PV-1` の 1 押しの実績。本書は文書の値を集約に入れない（`SF-10`）ので触れない ⇒ 衝突なし |
| 「状態機械」 | `JDG-45` `JDG-57` `JDG-59` `JDG-60` | `JDG-57`: 書き直される場所の欠陥は、その領域を移して一致を確かめた直後に直す ⇒ 本書は欠陥を直さない（第 8 節）。ほか 3 つは本書の出どころ |
| `state-machines` | `JDG-59` | D-1 |
| ほかの届いた行（`LY-*` `CP-*` `PI-*` `UT-*` `UF-*` `SU-*` `MN-*` `WS-2` `IN-4` `AG-6` `S-99e`〜`S-144` と上の `FR-` 12 本、`NFR-010` `NFR-011`） | 0 件 | —— |

⇒ **裁定のある行と導いた条項の衝突は 0。**

---

## 3. 案 —— 2 つの波に分けて着地させる

⭐ **段 2b の出口「変更要求が当たり、検査が緑」を満たすのは 2b-A である。** 2b-B は中身を本書で決め、段 7 の最初の領域（画面の値）の変更要求が、そのコードと同じ波で当てる（D-2・D-3）。

### 2b-A —— 次の仕様のセッションで当てる（コードを待たない）

| 対象 | 置き場 | 中身 |
|---|---|---|
| **ADR-002 — 保存しない状態を、自前の状態機械 1 つで持つ** | `05-07-design.md` 5.6、ADR-001 の後 | **Context** —— 保存しない状態が `frameLoop` の `let` 60 と UseCase のモジュール状態 2 に散り、同じガードが写しで並ぶ（棚卸しの記録 6: `asking`・`openChoosing`・`isFileOperationWaiting` の 3 項が 5 か所、`escapeLevelOf` の組み立てが 2 か所）。`LY-5` は現在値を `Framework` に限るが、形を定めていないので、クロージャの `let` しか置き場が無かった。**Decision** —— 表 T-247 の形で持つ。ライブラリは使わない。**Status** —— `Accepted`。**Consequences** —— UseCase にコンポーネントが 1 つ増える（2b-B）。遷移は原稿（表 T-248）から図・表・型を生成し、契約試験で関数と結ぶ。性能は段 7 で領域を移すたびに `LM-19` の手順で測る |
| **表 T-247（仮番）— 状態機械の形** | ADR-002 の中 | 下の `SF-1`〜`SF-10` |
| **表 T-070 に `MN-9`** | 5.6 | 増やしたもの「保存しない状態を状態機械で持つ（ADR-002）」／最小構成では「シェルのクロージャが値を直に持つ」／増やした理由「同じガードが写しで並び、`await` をまたいで現在値を読み書きする形が残る（`R4`）。遷移を図と試験で確かめられない」／代償「出来事と副作用の型を書く手間。原稿と遷移の関数を契約試験で結ぶ手間」 |
| **表 T-248（仮番）— 状態機械の原稿が持つもの** | `05-07-design.md` 5.5、図 F-018 の前 | 下の `SD-1`〜`SD-4`、および散文 3 文: 「原稿は `_source/state-machines.json` である」「領域ごとの状態遷移図・状態の表・出来事の表・遷移表・優先順の表と、TypeScript の判別共用体・遷移表の定数を生成する。手で書くのは各領域の遷移の関数だけである」「原稿に載せるのは、要求が名指す状態・出来事・遷移だけとする（MUST）。実装の都合の細部は状態の種類を増やさず、状態が運ぶ値としてコードが持つ」。⚠️ 図 F-018 は文書のデータの状態なので原稿に入れない、と 1 文添える |
| **登録簿に `SF` `SD`（行あり）と `SM` `EV` `TN`（`pending: true`）** | `_source/row-id-prefixes.json` | `SF`: 状態機械の形の条（表 T-247）。`SD`: 状態機械の原稿が持つものの条（表 T-248）。`SM`: 状態機械の状態 1 つ。`EV`: 状態機械が受ける出来事 1 つ。`TN`: 状態機械の遷移 1 つ。⭐ 3 つとも行が書かれるのは 2b-B なので `pending: true`。⛔ 行が入る波で外す（行を持つ pending は生成器が落とす） |

**表 T-247 の行**:

| 行 ID | 条 | 正 |
|---|---|---|
| SF-1 | 状態は領域ごとの判別共用体（`kind` で分かれる型）で持つ。出来事も共用体とし、名は過去形とする | `R2` 命名の品詞の規約（イベントは過去形） |
| SF-2 | 遷移は領域ごとの `step(state, event)` とし、次の状態と副作用の列を 1 つの値で返す純粋関数とする | `R7.2`、`R4` グリッジ（次の状態は丸ごと返す） |
| SF-3 | 何も変わらない出来事では、受け取った状態と同じ参照を返す。空の副作用の列は共有の定数とする | `NFR-010` |
| SF-4 | 扱わない出来事の組は `never` による網羅でコンパイルエラーにする | `R4` グリッジ（無効な遷移が存在しないか） |
| SF-5 | 連続して変わる値（ポインタの座標・寸法・時刻）は状態機械に入れず、フレームの値として持つ。出来事は、押す・掴む・問いに答える、および指しているパーツや当たりが変わったときだけ作る | 計画の記録 4・13（`JDG-60` が了承した性能の見立て） |
| SF-6 | 副作用（書き込み・ファイル・クリップボード・問い）は値として返し、`SingleHtmlShell` が実行する。結果は出来事として `step` へ戻す | `LY-5`、`R7.9`、`R4` レースコンディション |
| SF-7 | 現在値はシェルが 1 つだけ持ち、参照を差し替えて進める。内側の 3 層のモジュールスコープに可変状態を置かない | `LY-5`、5.3 の「内側の 3 層には漏らせる可変状態がそもそも無い」 |
| SF-8 | 根の状態は領域の合成とし、`step` は領域ごとのユニットに分ける。1 つの `switch` に全領域を集めない | `R2.2`、表 T-063 の `UT-2` と同じ理由の形 |
| SF-9 | クラスを作らない。公開するのは型と関数だけ | 5.3、`JDG-59` |
| SF-10 | 文書は集約に入れない。文書を変えるのは書き込みの唯一の経路で、セッションは読むだけ | 表 T-042 の `MS-1` |

**表 T-248 の行**（記録 12 の表をそのまま条にした）:

| 行 ID | 項目 | 持つもの | 持たないもの |
|---|---|---|---|
| SD-1 | 状態（`SM`） | 行 ID、キー、親（入れ子）、初期状態か、運ぶ値の名前、根拠の行 | 既定値と上下限（表 T-206 が持ち、`S-` 行を指すだけ） |
| SD-2 | 出来事（`EV`） | 行 ID、キー、どこから来るか（入力の行・副作用の結果・時間）、運ぶ値の名前 | 画面の語（辞書が持つ） |
| SD-3 | 遷移（`TN`） | 元の状態、出来事、ガードの名前、先の状態、副作用の名前、根拠の行 | ガードの条件式と副作用の中身（コードが持つ）。⭐ 表と関数のずれは、領域ごとの契約試験（すべての状態と出来事の組で先の状態の種類が表と一致し、表に無い組では同じ参照を返す）が止める |
| SD-4 | 領域をまたぐ優先順 | 奪い合う出来事、順を決めた要求の行、段ごとの状態のキー | **順そのものの正**（`IN-4` などの要求の行が持つ。D-6） |

### 2b-B —— 段 7 の最初の領域の変更要求が、コードと同じ波で当てる

| 対象 | いま | 案 |
|---|---|---|
| 表 T-060 `LY-3` | 文書を変える操作と確定までの手順、取り込みの検証、変更の通知 | ＋「保存しない画面とセッションの流れの遷移（出来事から次の状態と副作用を決める）」。純粋性の欄に「遷移は `pure`」 |
| 表 T-062 に `CP-39` | —— | `UseCase` / `AdvanceScreenSession` / 「保存しない画面とセッションの流れを、出来事を受けて 1 段進める。領域をまたぐ優先順を当てる。副作用を値で返す」/ 正「5.6 の ADR-002 ／ 表 T-028 の `IN-4` ／ 表 T-067 の `WS-2`」 |
| `CP-18` `InputCommandTranslator` | 画面の入力を操作へ変える | 画面の入力を、どこで何が起きたかを運ぶ出来事へ変える（`InputSource` の宣言は残す） |
| `CP-25` `SingleHtmlShell` | 現在値を保持する … | セッションの現在値を 1 つ保持し、出来事 → 遷移 → 差し替え → 副作用の実行を回す。副作用の結果を出来事で戻す（残りの責務は据え置き） |
| `CP-36` `ScreenState` | 構えと `S-99e` / `S-99f` / `S-99g` | 構え（表 T-023b）、`S-99e` / `S-99f` / `S-99g` / `S-144`、`Dual Cursor` の追従側（`DC-2`）、出ている説明（`IN-3`）（D-8。⚠️ いま落ちている `S-144` を足す） |
| 表 T-063 に `UT-8` | —— | `AdvanceScreenSession` / 公開エントリ・共通の型のユニット・領域ごとの遷移のユニット / 「純粋性ではない。領域ごとに縛る要求が別なので割った（`UT-2` と同じ形）」 |
| 表 T-075 | 68 行 | 公開エントリ `advance-screen-session.ts` と共通の型のユニット（`Step`・`unchanged`・`assertNever`）の 2 行はこの波。⛔ 領域ごとのユニットの行は、その領域を移す変更要求が足す（`JDG-48`） |
| 表 T-064 に `PI-39`、`PI-18` / `PI-36` / `PI-8` / `PI-15` | —— | `PI-39`: `ScreenSession`（型）／ `advanceScreenSession`。`PI-18`: `commandFromInput` / `selectionFromInput` / `screenStateFromInput` を出来事を作るメンバへ（名はその波で決める）。`PI-36`: 広げた項目の書き手。`PI-8` / `PI-15`: モジュール状態を引数で受けるメンバへ（D-7。純粋性はその領域の変更要求が決める） |
| `UF-47` / `UF-48` | 現在値の保持 … | `UF-48` の「現在値の保持」を「セッションの現在値 1 つの保持と、副作用の実行」へ |
| 表 T-074 `SU-1` / `SU-3`、5.3 の散文（`:259` `:280`）とディレクトリ木、`MN-2` | 36 / 68 | 37 / 68 ＋ その波で足すユニットの数。木の `use-case/` に `advance-screen-session/` |
| `components.json` | nodes 36 / edges 85 | node `AdvanceScreenSession` を `UseCase` の枠に足す。辺は `CR-378` が定める辺の定義で数える。図 F-013〜F-017 は `build.py` が作り直す |
| 5.5 の散文（`:563` の通知の配布中の窓）と `WS-2` | 窓の旗の持ち主を書いていない | 窓はセッションの通知の領域の状態である、と 1 文（通知の領域を移す波。`DFC-562` と重なる） |
| 原稿 `state-machines.json` と生成器 | —— | 画面の値の領域の `SM` / `EV` / `TN` 行、生成器とその `--check`（`gen` / `gen:check` への登録）、生成する表と図を 5.5 へ。登録簿の `SM` `EV` `TN` から `pending` を外す |
| 表 T-065 | 8 行 | **増やさない**（記録 12） |

---

## 4. 問いの候補に打った 3 手（`handoff.md` §0.2）

| 候補 | (1) `rulings.md` を引いた | (2) `impact.py` の隣の行 | (3) 他の道 | 結果 |
|---|---|---|---|---|
| 遷移の持ち主（記録 4 の 2 択） | 「state-machines」⇒ `JDG-59` が原稿を了承済み | 5.5 に手書きの状態遷移図 F-018 の前例があるが、文書のデータなので競合しない | 棚卸しの記録 5 も原稿を推す | 導けた（D-1） |
| 原稿を今作るか | 「原稿」「state-machines」⇒ `JDG-59` は時期を言わない | 規則 02 の 3.5、検査 18・26b、登録簿の `pending` | 空の原稿を先に置く道 ⇒ 生成物が空・読み手 0 | 導けた（D-2） |
| 表 T-062 / T-075 / T-064 / `components.json` をいつ当てるか | `T-075` ⇒ `JDG-48`（割るたびに変更要求） | 検査 18・26b の docstring | 空のスタブで先に置く道 ⇒ 26b が赤 | 導けた（D-3） |
| 名前（コンポーネント・型・原稿・接頭辞） | 「AdvanceScreenSession」「ScreenSession」0 件、`JDG-59` / `JDG-60` が仮称で了承 | `R2` 命名、`U-51` の禁止語、使用 0 の実測 | 名は着地前なら変更要求 1 本で替えられる | 導けた（D-4、覆してよい） |
| 優先順の正（`IN-4` と原稿の二重） | `IN-4` 0 件 | `IN-4` は要求 8 件・参照 22 箇所。記録 12 の「`S-` 行を指すだけ」と同じ形 | 要求を原稿へ移す道 ⇒ 影響が大きい | 導けた（D-6）。突き合わせの方法は決めない |
| UseCase のモジュール状態の行き先 | `deliveringNotices` `REGISTRATIONS` 0 件（台帳の `DFC-562` だけ） | `LY-5`、5.3:243、`WS-2`、表 T-065 を増やさない | 新しいインターフェースを立てる道 ⇒ 記録 12 が退けた | 導けた（D-7） |
| ADR-002 と原稿の規則の置き場 | 0 件 | 5.6 の冒頭（表は数え上げだけ）、`R4`（状態遷移図は Ch5.5）、5.2 の原稿の前例 | —— | 導けた（D-5） |

⇒ ⭐ **残る問い 0 件。**

---

## 5. 数の予測

方法: `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py .` の最終行。起草時（`3cf64a4`）は `tables=145  figures=11  rows=1901  uids=153`。

**2b-A**（本書が当てる分。`CR-376`・`CR-377`・`CR-378` と同じセッションで当てるときは、差だけを足す）:

| | 前 | 後（予測） | 差 | 内訳 |
|---|--:|--:|--:|---|
| tables | 145 | 147 | +2 | 表 T-247、表 T-248 |
| figures | 11 | 11 | 0 | |
| rows | 1901 | 1916 | +15 | `SF-1`〜`SF-10`（10）、`SD-1`〜`SD-4`（4）、`MN-9`（1） |
| uids | 153 | 153 | 0 | ADR は `UID` を持たない（ADR-001 と同じ） |

⚠️ 登録簿の `_assets/tbl-row-id-prefixes.md` は 5 行増える（`SF` `SD` `SM` `EV` `TN`）が、表の番号も行 ID 形の第 1 列も持たないので、md-checks の rows に入らない見込み。適用時に実測で直す。

**2b-B**（段 7 の最初の領域の変更要求が予測を書き直す。いまの見込み）: tables +（生成する表の数）、figures +（領域ごとの図 1）、rows +1（`CP-39`）+1（`UT-8`）+2（`UF-` 2 行）+1（`PI-39`）+（原稿の行）、uids 0。

---

## 6. 影響 —— 本書の外で動くもの

| 所 | 何が動くか |
|---|---|
| `docs/development-records/rulings.md` | `JDG-45` / `JDG-59` / `JDG-60` の着地先に「`CR-379`（ADR-002、表 T-247 / T-248）」を適用した日に足す |
| `docs/development-records/refactor-plan-report-2026-09-13.md` | 状態表の段 2b の出口を「2b-A が当たり検査が緑。2b-B は段 7 の最初の領域が当てる」と書き直す。段 7 の入口にも 2b-B を足す。記録 5 の「51 本」を 56 に、記録 12 の「8 領域をまたぐ」を直す（第 7 節） |
| `docs/development-records/defects.md` | ⚠️ 5.3 の「内側の 3 層には漏らせる可変状態がそもそも無い」（`05-07-design.md:243`）にいまのコードの 2 つが反している。台帳に行が無い（`grep "REGISTRATIONS" defects.md` 0 件、`deliveringNotices` は `DFC-562` の症状としてだけ）⇒ 裁定 2026-09-13（仕様とコードの食い違いは台帳へ）に従って行を立てるのは台帳の体。直すのは段 7 |
| `docs/development-records/pending-decisions.md` の `PND-141`〜`PND-144` | 語 `ScreenSession` が本書で正式の名になる（D-4）。置き場の裁定は未決のまま（それぞれの領域の変更要求で閉じる） |
| 登録簿の生成（`npm run gen:prefixes`） | `SF` `SD` `SM` `EV` `TN` の 5 行。`pending` の 3 行は「登録のみ」と印字される |
| 検査 22（`check-cr-discipline.py`） | 本書の第 0 節（`GL-003` / `GL-008`、`R2` など、「問わずに決めた」）で満たす |
| 規則 02 の 3.5 | 2b-B で、原稿の行と生成器と、それを読むコードを同じ波にする |
| `docs/development-records/changelog.md` | A-appendix の 1 行（2b-A の適用時） |

---

## 7. 計画の主張を反証した結果

| 計画の主張 | 実測 | 判定 |
|---|---|---|
| 記録 1: `frameLoop` の `let` 60 | 60 | 正 |
| 棚卸しの記録 1: `frameLoop` は 1458〜3630 行 | 1463〜3678 行 | ⚠️ 行番号が古い（数は正） |
| 記録 1: UseCase のモジュール状態 2 | 2 | 正 |
| 記録 4: `ScreenState`（5 項目） | 5 | 正。⚠️ ただし仕様の `CP-36` は 4 つしか名指さない |
| 記録 4: `ScreenSession` は `docs/spec` に 0 件 | 0 | 正 |
| 記録 4: 遷移の持ち主は段 1 の後に 2 択から選ぶ | `JDG-59` が既に原稿を選んでいる | ⚠️ 2 択は開いていない |
| 記録 12: 行 ID の接頭辞 `SM` `EV` `TN` は登録簿に無い | 使用 0・登録 0 | 正 |
| 記録 12:「IN-4 の優先順は 8 領域をまたぐ」 | `IN-4`（`01-04-requirements.md:5972`）の段は 8 つ。棚卸しの領域で割ると **6 領域**（通知・名前付けと入力欄・画面の値〔面・構え・`Dual Cursor`〕・身振り・選択・ポインタとツールチップ） | ⛔ **誤り**（段の数と領域の数を取り違えている） |
| 記録 12:「書き込みを拒むガードは 3 領域を同時に読む」 | 棚卸しの記録 4（`collectWriteMoment`: 身振り・名前付け・通知） | 正 |
| 記録 12: 仕様への影響は T-060 LY-3、T-062（+1、CP-18/25/36）、T-063/T-075/T-064、`components.json`。T-065 は増えない | 加えて 表 T-070 の新しい行（`R2.18`）、表 T-074 の `SU-1` / `SU-3`、5.3 の散文の「36」2 か所とディレクトリ木、`MN-2`、`UF-48`、`PI-8` / `PI-15`（モジュール状態）、5.5 の通知の窓の散文、登録簿、5.6 の ADR-002 と 5.5 の原稿の規則 | ⛔ **欠けがある**（第 3 節で足した） |
| 状態表: 段 2b の出口「変更要求が当たり、検査が緑」 | 表 T-075 / T-064 の行をコードより先に当てると検査 18・26b が赤 | ⛔ **このままでは満たせない**（2b-A / 2b-B に割った） |
| 記録 5:「`frame-loop.ts` を import する試験 51 本」 | 56 本 | ⛔ **古い**（並行作業の表の 56 は正） |

---

## 8. ⛔ この変更でやらないこと

- ⛔ **原稿 `state-machines.json` と生成器を作らない**（D-2。2b-B）
- ⛔ **表 T-062 / T-063 / T-064 / T-075 / T-074、`components.json`、`LY-3`、`CP-18` / `CP-25` / `CP-36` を 2b-A で動かさない**（D-3。中身は第 3 節で決めた）
- ⛔ **領域ごとのユニットと遷移を決めない** —— 段 7 の領域ごとの変更要求が、名指す要求を読んで書く（`JDG-48`、記録 12）
- ⛔ **文書（`Document`）を集約に入れない**（`SF-10`）。倍率と表示位置（`S-75`〜`S-78`・`S-176`・`S-177`）は文書の設定値なので、状態機械の外（記録 13）
- ⛔ **図 F-018（予実の状態遷移）を原稿に入れない** —— 文書のデータの状態である
- ⛔ **原稿の優先順と `IN-4` の段を機械で突き合わせる方法を決めない**（D-6。原稿を作る波で決める）
- ⛔ **辺の定義（`JDG-47`）を 5.2 に置くことと、JSON の置き場を決めない** —— `CR-378`（段 2a）が持つ。本書は 2b-B の `components.json` の辺をその定義で数える、とだけ書く。生成する TypeScript の型と遷移表の定数を `src/` のどこへ置くかは、`CR-378` の JSON の置き場の規則に従う
- ⛔ **欠陥を直さない** —— `DFC-538` `DFC-541` `DFC-562` などは、その領域を移して一致を確かめた直後に直す（`JDG-57`）
- ⛔ **`IN-4` の文言、表 T-065、表 T-206 の値を変えない**
- ⛔ **`src/` と `tests/` に触らない**（起草の段）
