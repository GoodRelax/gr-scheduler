# CR-401 — 行の軸の上限を、最小の倍率から上げて帯が初めて `Row Area` に達する所に置く

> **閉じるもの**: 利用者の答え（2026-09-16）「問 3　行の縦ズーム ①」「問 4　行の縦ズーム  ②」。
> - `JDG-134`（問 3 の ①）: `FR-016` の行の軸の上限（帯の側）を、`S-76` の下限から倍率を上げていき、いちばん高い行の帯が `Row Area` の高さに**初めて**達する倍率とする。どこから拡大を始めても同じ上限とする。上限を求める計算は 1 つにする。
> - `JDG-135`（問 4 の ②）: `DFC-610`（行の縦ズームが遅い）は `DFC-628` の直しに畳み、直したあと実機で段 0 と A/B する ⇒ **仕様の変更は無い**（第 8 節）。
> - 台帳の `DFC-628`（`裁定待ち`）: 帯の上限を解く実装が `src/` に 2 つあり、区間も止め方も違う。
>
> ⭐ **本書は仕様の 3 か所（`FR-016` の帯の側の文、同要求の字の側の結びと新しい段、表 T-064 の `PI-18` のセル）を当てる。** 実装は別に続く。読んだ木は `3844d1b2`。
> ⚠️ 本書を読んだ木の `docs/development-records/rulings.md` には `JDG-134` / `JDG-135` の行がまだ無い —— 記すのは前に立つ者である。本書は利用者の逐語と、作業指示が伝えた中身に拠った。
> ⭐ **初稿の問い 1〜4 に前に立つ者が答えた（2026-09-17）** —— 問い 1 は `JDG-78` により記録だけ、問い 2 は `:3137` を事実どおりに直す、問い 3 は今回決めない、問い 4 は `DFC-628` の条件の答え。第 9 節に答えを添え、問い 2 の直しを同じ作業木で当てた。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`**（マニュアルを読まずに使える。表 T-054 の `CH-4` が受ける目標は `GL-006`）—— 拡大がどこで止まるかが、拡大を始めた倍率で変わるなら、利用者は止まる位置を覚えられず、説明を読まないと「なぜ今回はここで止まったのか」が分からない。同じ文書なら同じ所で止まることが、読まずに使える前提である。
⭐ 上限そのものが仕える目標は、`CR-381` と同じく **`GL-002`**（全体日程を 1 画面で見られる）である —— 1 つの行が画面を埋め尽くす拡大を止める。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.2`（曖昧・非検証な表現を排除）** —— 「いちばん高い行の帯が `Row Area` の高さに**達する**倍率」は、達する倍率が 1 つに限られないとき、どれを指すかを決めていなかった。⭐ 「`S-76` の下限から上げていって**初めて**達する倍率」で 1 つに定まる。⭐ 検証できる形も立つ —— 拡大を始める倍率を変えて、同じ所で止まることを確かめればよい（第 7 節）。
- **`R1.3`（矛盾・重複がない。唯一の正がある）** —— 同じ上限を解く実装が `src/` に 2 つあり（第 1 節）、区間と止め方が違う。⭐ 要求の側は「拡大を始めた倍率に依らない」を MUST で持ち、1 本に置くことは表 T-064 の `PI-18` が持つ（決定 3）。
- **`R1.3`（同）** —— `:3137` の「上限の倍率は表示の倍率に依らない」は字の側についてだけ成り立ち、帯の側は表示の倍率で動く（第 2 節）。⇒ 字の側に限ると明示し、帯の側は動くと書き添えた（決定 5）。
- **`R2.7`（DRY）** —— 表 T-064 は既に `rowBandCeilingOf` を帯の側の上限として公開している（`CR-396` 決定 6）。⛔ 呼ぶ側が別の区間で解き直すと、同じ上限が 2 か所に立ち、片方だけが直されて離れる（1.9）。
- **`R1.4`（境界値）** —— **下限の倍率で既に帯が `Row Area` の高さを超える文書**では、字義どおりに読むと上限は `S-76` の下限そのものになり、拡大の入力で倍率が下がる。⭐ そういう文書は実在する（第 2 節）。⇒ 利用者の裁定 `JDG-78`（異常系・準正常系は台帳に残し、リファクタ一式が終わったあとに回す）により、条項を足さず記録だけ残す（決定 4）。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | `FR-016` の帯の側の文（`01-04-requirements.md:3128`）は頭だけを書き換え、末尾「ちょうど埋めた先には、見せられるものが残っていない。」と、続く `:3129`〜`:3136` の並びを 1 文字も動かさない。⭐ 新しい段は `:3137` の後に置く | ⚠️ `tests/unit/cr-380-381-384-name-beside-the-shape-and-row-zoom.test.ts` は改行を繋いだ本文に、`:3128` の末尾から `:3136` までを 7 つの断片で逐語に探す（`FR_016_ROW_CEILING_IS_THE_SMALLER` ほか）。⛔ あいだに行を挟むと、条項を変えていない断片まで赤になる | 「初めて」の理由の注（`:3142`）が、定義の文から 14 行離れる |
| 決定 2 | 不変の MUST を「いまの `zoomY` だけが違う 2 つの状態は、同じ上限を持つ」と書き、「同じ文書・同じ窓・同じ日付の軸の倍率なら」と**列挙しない** | ⭐ 帯の高さは、文書・窓・日付の軸の倍率のほかに、表示の倍率（第 2 節で上限が動いた）、段 0 の畳み、行の操作子の高さでも動く —— `rowPlacesAtZoomY` の引数と、`src/framework/single-html-shell/frame-loop.ts:2449` の `bandCeilingFor` が覚える鍵がそれを示す。⛔ 列挙すると 1 つ漏れたとき、仕様が偽の約束をする | 何に依って上限が動くかを本条は名指さない |
| 決定 3 | 「上限を求める計算は 1 つにする」を `FR-016` に書かず、表 T-064 の `PI-18` の `rowBandCeilingOf` のセルに ⛔ の平叙文で書く。⭐ `（MUST）` / `（MUST NOT）` の印は足さない | ⭐ 要求が持つのは振る舞い（どこから始めても同じ所で止まる）であり、1 本に置く DRY は表 T-064 の持ち場である（1.9。同表 `PI-5` の「式を写して持たせてはならない」と同じ形）。⭐ `CR-397` / `CR-398` の決定 4 が、同表のセルに印を足さない先例である | 検査 39 はこの禁止を数えない |
| 決定 4 | 下限の倍率で既に帯が達している文書の扱いを、条項として足さない。本書に記録だけ残す | ⭐ 前に立つ者の答え（2026-09-17）—— 利用者の裁定 `JDG-78` により、異常系・準正常系は台帳に残してリファクタ一式のあとに回す。⭐ 字義どおりに読めば「初めて達する倍率」は下限そのものであり、足さなくても 1 つに定まる | 帰結（行の軸が拡大できず、拡大の入力で倍率が下がる）が仕様の文に見えない ⇒ 第 9 節の問い 1 に記録 |
| 決定 5 | `:3137` の結びを「**字の側の**上限の倍率は表示の倍率に依らない」と限り、続けて `:3138` に「帯の側の上限の倍率は、表示の倍率で動く」、`:3139` に「帯の側が字の側より小さい画面では、行の軸の上限は表示の倍率を変えると動く」を足す。⭐ 前に立つ者は「限ると明示する、または書き添える」と答えた —— **両方を採った**のは本書の判断である | 限るだけでは、読み手が帯の側も表示の倍率に依らないと推して読む余地が残る。書き添えるだけでは、`:3137` の文そのものが偽のまま残る。⚠️ `:3137` の `（MUST）` より前（`tests/unit/cr-394-the-chart-has-a-display-scale.test.ts:82` が逐語で持つ）は動かしていない | 仕様が 2 行増える。⚠️ 帯の側が動く理由を「帯の高さを作る縦の寸法には描く比が掛かる（表 T-252 の `DS-1` / `DS-8`）が、`Row Area` の高さは画面の高さから導く」と書いた —— 表の定めからの読みであり、仕組みを 1 つずつ切り分けてはいない |
| 決定 6 | 帯が倍率に対して単調でないことを、**存在も原因も**仕様に書かない。⭐ `:3142` の注は「再び収まる倍率が**あっても**」と仮定で書いた | ⭐ 「初めて達する倍率」は、単調であってもなくても 1 つの倍率を指す。⚠️ 非単調は、起動テンプレートの 1000 件を少数の行に畳んだ文書で出る（第 2 節。本書は追試していない）。⛔ 原因は切り分けられていない | 単調でない文書で何が起きるかを、仕様は例で示さない |
| 決定 7 | 「初めて」を選んだ理由を `:3142` の ⚠️ の注に残す | `JDG-134` が案 ②（最大の側から下げて初めて収まる倍率）を退けた理由 —— ① との間に 1 つの行が画面からはみ出す倍率を含み、`FR-016` の理由に反する。⭐ 理由を持たない MUST は、次の読み手が「最後に収まる倍率」へ書き換える | 仕様が 1 行増える |
| 決定 8 | 表 T-064 の `PI-5` を変えない | ⭐ 同行が持つのは `rowPlacesAtZoomY`（その倍率での行の位置）と `zoomYAtRectangleLabelFont`（字の側の倍率）であり、帯の側の上限の定義を持たない。帯の側を解く 1 本は `PI-18` の `rowBandCeilingOf` である | 無し |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| 書き換える前の文は「行の軸（`zoomY`）の上限は、いちばん高い行の帯が `Row Area` の高さに達する倍率とすること（MUST）」であり、達する倍率が複数あるときにどれを採るかを述べていなかった | `01-04-requirements.md:3128`（`3844d1b2`） |
| ⭐ `CR-381` は上限の**読み**を定めていない —— 決定 1 は「いまの上限（いちばん高い行の帯が `Row Area` に達する倍率）は残し、新しい上限との小さい方をとる」、決定 7 は「いちばん高い行の帯は `PI-5` の `rowPlacesAtZoomY` に問うて求める（MUST）」とだけ書き、最初に達する倍率か最後に収まる倍率かを選んでいない | `change-request/CR-381-the-row-zoom-stops-where-a-name-outgrows-its-row.md` の第 0 節と第 7.2 節 |
| `JDG-126` の注は「上限そのもの（`FR-016`、`CR-381`）は変えない」であり、性能の直し（`DFC-610`）に掛けた縛りである | `docs/development-records/rulings.md` の `JDG-126` |
| 書き換える前の `:3137` は「両方の字とその床に同じ比が掛かる（表 T-252 の `DS-1`）ので、上限の倍率は表示の倍率に依らない」であり、「比べる 2 つの字」の段の結び、つまり字の側についての文である | `01-04-requirements.md:3137`（`3844d1b2`） |
| 帯の側の上限を解く実装が 2 つある。`zoomYWithinBand` は 40 回・区間「いま収まっている倍率 または `zoomMin`」〜「欲しい倍率」・止め方 `over × 1e-6`。`rowBandCeilingOf` は 80 回・区間 `zoomMin`〜`zoomMax`・止め方 `fits × 1e-7`。後者の呼び手は殻の `bandCeilingFor` 1 つである | `src/adapter/input-command-translator/input-command-translator.ts:3292` / `:3326`、`src/framework/single-html-shell/frame-loop.ts:2466` |
| どちらも、下限で帯が収まらないときは下限を答える（`zoomYWithinBand` は、いまの倍率でも欲しい倍率でも収まらないとき） | 同 `:3307`（`zoomYWithinBand`）、`:3332`（`rowBandCeilingOf`） |
| 表 T-064 の `PI-18` は既に `rowBandCeilingOf` を「`FR-016` の行の軸の上限のうち、いちばん高い行の帯が `Row Area` に達する倍率」として公開している。⭐ 解く式の持ち主が `PI-5` だというのは字の側（`zoomYAtRectangleLabelFont`）と帯の源（`rowPlacesAtZoomY`）についてであり、帯の側の上限を解く 1 本は `PI-18` にある | `05-07-design.md:425` / `:438`、`01-04-requirements.md:3135` |
| 表 T-252 は、縦の寸法とラベルの字（`DS-1`）と行の軸のズーム（`DS-8`）に描く比を掛けると定める | `01-04-requirements.md:6150` / `:6157` |
| `FR-016` は「上限の式は、できた結果で判じる —— だから式を変えるときは、実測を添えて出すこと」と定める | `01-04-requirements.md:3149` ⇒ 第 2 節に実測を添えた |
| `FR-055` は「1 つの行に人が多くの `Task` を積んだ文書では、どの倍率でも収まりようがない」と認める | `01-04-requirements.md:4106` ⇒ 第 9 節の問い 1 |
| 既存の自動試験に、帯の側の上限（`rowBandCeilingOf` / `rowBandCeiling`）を名指すものは 0 件である | `tests/` を `rowBandCeiling` / `bandCeiling` で引いて 0 件 |

## 2. 測った

**測り方**: scratchpad の vitest 探り針（リポジトリの外に置き、`src/` の公開名だけを呼ぶ）。起動の文書 `src/framework/single-html-shell/startup-template.json`（`Task` 1000 件、畳まない）を `documentFromJson` で読み、`regionsFromScreen` で矩形を出し、`rowPlacesAtZoomY` が返す行の `height` の最大を「いちばん高い行の帯」とした。倍率は `S-97` 0.02 〜 `S-98` 64 の対数等間隔 400 点で読み、最初に `Row Area` の高さを超えた点とその 1 つ前の点のあいだを 60 回二分して「初めて達する倍率」とした。字の側は `zoomYAtRectangleLabelFont(rowTitleFontPxOf(1))`。段 0 は畳まない。`App Header` は `regionsFromScreen` が丸める上限の高さまで取り、スクロールバーは 17px とした（表の 3 行目だけ 0 と 0）。

| 窓 | 表示の倍率 | `zoomX` | `Row Area` の高さ | 下限での帯 | 初めて達する倍率 | 全区間の二分 | 再び収まる点 | 行の軸の上限 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|
| 1920 × 1080 | 50 | 0.78897（全体を収めた倍率） | 981px | 73.33px | 7.378365 | 7.378364 | 0 | **1.320147**（字の側） |
| 1920 × 400 | 50 | 0.78897（同） | 301px | 73.33px | 3.202192 | 3.202192 | 0 | **1.320147** |
| 1920 × 400（`App Header` 0、スクロールバー 0） | 50 | 0.79648（同） | 374px | 73.33px | 3.693421 | 3.693420 | 0 | **1.320147** |
| 1920 × 400 | 100 | 0.38860（同） | 285px | 146.66px | 1.533376 | 1.533376 | 0 | **1.320147** |
| 1920 × 400 | 25 | 1.58973（同） | 309px | 64.00px | 6.235375 | 6.235374 | 0 | **1.320147** |

- ⭐ 3 行目は、台帳の `DFC-610` が別の体の探り針で書いた「1920×400・倍率 50 で 3.693420」と一致する ⇒ 同じ所を測れている。
- ⭐ 窓 2 種 × 表示の倍率 3 段（25 / 50 / 100）× 行の操作子の高さ 3 通り（未設定 / 32.0016 / 48）× `zoomX` 3 通り（全体を収めた倍率 / 0.05 / 1）の **54 条件**を 200 点ずつ走査して、**再び収まる点は 54 条件すべてで 0**、下限での帯は 64.00〜146.66px、`Row Area` は 285px 以上、行の軸の上限は **54 条件すべてで字の側の 1.320147** だった。
- ⇒ **この起動文書（畳まない 100 行）と既定の設定では、`JDG-134` の定義で上限の値は 1 つも動かない。**
- ⭐ **帯の側は表示の倍率で動く**（表の 2・4・5 行目: 25 で 6.235、50 で 3.202、100 で 1.533）。字の側は 1.320147 のまま ⇒ `:3137` を直した根拠（決定 5）。
- ⭐ **`DFC-628` の非単調は、畳んだ文書で出る（前に立つ者の答え、2026-09-17）** —— 配線の体の探り針（`monotone.probe.test.ts` と `monotone.json`。本書の木には無い）を前に立つ者が読んだ。起動テンプレートの 1000 件を少数の行に畳んだ文書で、窓 1920×400 で「2 行に畳む」: 下限での帯 1022.96px・再び収まる点 69、「1 行に畳む」: 64px・122 点、窓 1920×1080 で「1 行に畳む」: 146 点。畳まない起動テンプレートでは 0 点で、本書の 54 条件と一致する。⇒ **`DFC-628` は覆っていない。** ⛔ 本書はこの数を追試していない。
- ⚠️ 段 0 を畳むと描く行が 0 になり、帯はどの倍率でも達しない —— そのとき帯の側は上限を置かず、字の側が上限になる。

## 3. グラフ

**`impact.py FR-016 PI-5 PI-18 S-76 S-54`**: `FR-016` を指すのは要求 2 件 / 参照 13 箇所、`PI-5` は要求 3 件 / 参照 6 箇所、⭐ **`PI-18` を指す箇所は 0 件**、`S-76` は要求 3 件 / 参照 6 箇所、`S-54` は参照 1 箇所（表 T-206 の `S-97`）。

**`induced.py FR-016 PI-18 PI-5 PI-37 S-76 T-064 T-203 FR-039 DS-1 FR-055`**: 10 種すべて解決、辺 18、閉路 1 —— 大きさ 4（`FR-016` `FR-039` `PI-37` `PI-5`）。⭐ **本書がその閉路で書くのは `FR-016` の 1 つだけである**（`PI-18` は閉路の外。`:3138` は `FR-039` の表 T-252 を指すだけで、同表を書き換えない）⇒ 1 巡で書ける。

**届いた行を `rulings.md` で引いた**（`FR-016` / `zoomY` / 行の軸 / 縦方向 / `rowBandCeiling` / `PI-18` / `DFC-628` / `DFC-610`）: `JDG-88` / `JDG-99` / `JDG-110` / `JDG-115`（`CR-381` で着地、字の側と「小さい方」を裁いた）、`JDG-123`（表示の倍率を字の側に掛ける）、`JDG-126`（第 8 節）、`JDG-127` / `JDG-129`（測定）⇒ **「達する倍率」の最初か最後かを裁いた行は 0 件、衝突 0。** ⭐ 決定 4 は前に立つ者の答えにより `JDG-78` に従う。

## 4. 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| `FR-016` の帯の側の文 | `01-04-requirements.md:3128` | 「いちばん高い行の帯が `Row Area` の高さに達する倍率」を「表 T-203 の `S-76` の下限から倍率を上げていったとき、いちばん高い行の帯が `Row Area` の高さに初めて達する倍率」へ（決定 1） |
| `FR-016` の字の側の結び | `01-04-requirements.md:3137`〜`:3139` | 結びを「字の側の上限の倍率は表示の倍率に依らない」へ限り、帯の側は表示の倍率で動くことと、その帰結を足した（決定 5） |
| `FR-016` の新しい段 | `01-04-requirements.md:3140`〜`:3142` | 行の軸の上限は拡大を始めた倍率に依らない（MUST）／ ⇒ 止まる位置を前もって知れる ／ ⚠️「初めて」の理由（決定 2・6・7） |
| 表 T-064 の `PI-18` の `rowBandCeilingOf` | `05-07-design.md:438` | 定義の語を「初めて達する」に揃え、帯の側の上限を解くのは本メンバ 1 本で、呼ぶ側が別の区間や止め方で解き直さないと ⛔ の平叙文で足した（決定 3） |

## 5. 数の予測

当てる前 `tables=151 figures=11 rows=1962 uids=153`。本書の差: tables 0 ／ figures 0 ／ rows 0 ／ uids 0 ⇒ 予測 `tables=151 figures=11 rows=1962 uids=153`。
⚠️ 足した `（MUST）` は 1 つ（`:3140`）。試験がまだ逐語で押さえていないので、検査 39 の未押さえは 1355 → 1356 になる見込み（基準線 1401 の内側）。`:3137`〜`:3139` の直しは印を足さない。

## 6. 影響 —— 行の軸の上限を述べる所の全数（本書が動かすのは ★ の 4 つ）

### 6.1 仕様

| 所 | 述べていること | 本書で |
|---|---|---|
| `01-04-requirements.md:3117` | 倍率を 表 T-203 の `S-75` / `S-76` の範囲へ収める | そのまま（「下限」の出どころ） |
| `:3120` / `:3121` | 拡大の側に軸ごとに導かれる上限を置く（MUST）／ 固定の倍率で止めない（MUST NOT） | そのまま |
| ★ `:3128` | 帯の側の上限 | 書き換えた |
| `:3129` | そのために新しい設定値の行を立てない（MUST NOT） | そのまま |
| `:3130`〜`:3134` | 字の側の上限と「小さい方」、矩形で測る、字の式を倍率について解く、比から求めない、床だけで超える組 | そのまま |
| `:3135` | 式の持ち主は 表 T-064 の `PI-5`、深さ 1 の字は `PI-37` | そのまま |
| `:3136` | いちばん高い行の帯は `PI-5` の `rowPlacesAtZoomY` に問う（MUST） | そのまま |
| ★ `:3137`〜`:3139` | 比べる 2 つの字は描く比を掛けたあと、字の側の上限の倍率は表示の倍率に依らない、帯の側は動く | 結びを限り、2 行を足した |
| ★ `:3140`〜`:3142` | 拡大を始めた倍率に依らない（MUST）と「初めて」の理由 | 足した |
| `:3143` / `:3144` | 代償（`S-36` / `S-38` で上限が動く、マイルストーンの名前が深さ 1 を超える） | そのまま |
| `:3145`〜`:3148` | 切られた名前の印を上限の信号にしない（MUST NOT）と実測 | そのまま |
| `:3149` | 上限の式は結果で判じ、変えるときは実測を添える | そのまま ⇒ 第 2 節 |
| `:3151` | 上限に達したことを押しても何も起きない入口で示さない（`FR-029`） | そのまま |
| `:3153` / `:3154` | 倍率を変えたとき行の軸でも掴んだ行を留める、その位置を答えるメンバを `PI-5` に置く | そのまま |
| `:4106`（`FR-055`） | どの倍率でも 1 画面に収まりようがない文書がある | そのまま ⇒ 第 9 節の問い 1 |
| `:6150` / `:6157`（表 T-252 の `DS-1` / `DS-8`） | 縦の寸法とラベルに描く比を掛ける ／ 行の軸のズームと表示の倍率を掛け算で重ねる | そのまま（`:3137` / `:3138` が指す） |
| `:6165`（`FR-039`） | その倍率での行の位置は `PI-5` の `rowPlacesAtZoomY` に問う（MUST NOT で算を禁じる） | そのまま |
| `05-07-design.md:125`（表 T-062 の `CP-18`） | `InputCommandTranslator` が `FR-016` を持つ | そのまま |
| `05-07-design.md:425`（表 T-064 の `PI-5`） | `rowPlacesAtZoomY` と `zoomYAtRectangleLabelFont` | そのまま（決定 8） |
| ★ `05-07-design.md:438`（表 T-064 の `PI-18`） | `rowBandCeilingOf` | 書き換えた |
| `05-07-design.md:455`（表 T-064 の `PI-37`） | `rowTitleFontPxOf`（字の側が深さ 1 を問う） | そのまま |
| `05-07-design.md:725`（5.5） | 全体を収める表示と `FR-016` の行の軸の錨だけが割付けの表を 2 回まで走らせる | そのまま ⇒ 第 9 節の問い 3 |
| `_assets/tbl-settings.md:89`（`S-17`） | 範囲の理由が `FR-016` の行の軸の上限の段を指す | そのまま |
| `_assets/tbl-settings.md:124` / `:125` / `:188` / `:335` / `:336` | `S-54` / `S-55`（`zoomMin` / `zoomMax`）、`S-76` の範囲、`S-97` / `S-98` | そのまま（走査の端） |
| `_assets/tbl-glossary.md:518` / `:519`（`IC-14` / `IC-15`） | 行軸を縮小する / 拡大する入口 | そのまま |

⚠️ **同じ「帯」の字を使うが別物**: `01-04-requirements.md:3994` / `:4019`（`FR-098`）の帯は**ピン止めの帯**であり、行の軸の上限ではない ⇒ 本書は届かない。

### 6.2 変更要求と記録

| 所 | 本書との関係 |
|---|---|
| `CR-381` 決定 1・5・7、第 7.5 節 | 上限の読みを定めていない（第 1 節）⇒ 衝突しない |
| `CR-394` 決定 6、第 7 節の 6（行の軸の上限の `zoomY` が表示の倍率 33 と 100 で同じ） | 字の側についての主張として成り立つ。⭐ 同書の試験 `tests/unit/cr-394-the-chart-has-a-display-scale.test.ts:515`〜`:528` は `zoomYAtRectangleLabelFont` だけを測っており、直した `:3137`（字の側に限った）と食い違わない |
| `CR-396` 決定 6 | `PI-18` に `rowBandCeilingOf` を載せた。本書はそのセルを直した |
| `rulings.md` の `JDG-78` | 決定 4 と第 9 節の問い 1 |
| `rulings.md` の `JDG-126` | 第 8 節 |
| `defects.md` の `DFC-374` / `DFC-595`（済）、`DFC-610` / `DFC-628`（未） | 第 8 節 |

### 6.3 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `src/adapter/input-command-translator/input-command-translator.ts:3292`（`zoomYWithinBand`） | 区間「いま収まっている倍率 または `zoomMin`」から解き直す道を捨て、帯の側は `rowBandCeilingOf` の 1 本に問う（`DFC-628` の対応案 ①） |
| 同 `:3326`（`rowBandCeilingOf`） | ⚠️ 区間 `zoomMin`〜`zoomMax` の二分は、帯が単調でない文書（第 2 節の畳んだ文書）では「初めて達する倍率」を返す保証が無い —— 二分する区間が初めて達する倍率を挟んでいることを確かめる形にする。⛔ 解き方は本書が決めない |
| `src/framework/single-html-shell/frame-loop.ts:2449` / `:2480`（`bandCeilingFor` と `wantsBandCeiling`） | 覚えた上限を行の軸のズームの道で使う配線（`DFC-610`、`JDG-135`） |
| `tests/system/user-reported-fixes.test.ts:1347` / `:1376` | 失敗の文言が書き換える前の文「高さに達する倍率とすること」を引いている（照合ではなく文言だけ）⇒ 語を揃える |

⭐ **本書の直しで赤になる逐語の試験は 0 件** —— `tests/` を「表示の倍率に依らない」「同じ比が掛かる」「掛けたあとの字とすること」で引いて、当たるのは `cr-394-the-chart-has-a-display-scale.test.ts:82`（`:3137` の `（MUST）` までの頭。動かしていない）と `:91`（`FR-017` の文。別の要求）の 2 か所だけである。

## 7. 試験が主張すべきこと

1. 同じ文書・同じ画面で、`zoomY` を下限・等倍・字の側の上限の直下など違う倍率に置いてから拡大の入力を続けたとき、止まる `zoomY` が同じ（`FR-016` `:3140`）。
2. 帯の側の上限より下のすべての倍率で、いちばん高い行の帯が `Row Area` の高さ以下である（走査で確かめる。`:3128`）。
3. 殻が覚えた上限を使う道と使わない道が、同じ倍率で止まる（表 T-064 の `PI-18`）。
4. 字の側の上限は表示の倍率の段を変えても動かず（既存の `cr-394-the-chart-has-a-display-scale.test.ts:515`）、帯の側の上限は動きうる（`:3137`〜`:3139`）。
5. 下限の倍率で既に帯が達している文書の振る舞いは、`JDG-78` によりリファクタの後に書く。

## 8. 関係 —— `JDG-135`・`DFC-610`・`DFC-628`・`JDG-126`

- ⭐ **`JDG-135`（問 4 の ②）は仕様を変えない。** `DFC-610`（行の縦ズームの入力 1 回が遅い）は `DFC-628` の直しに畳み、直したあと実機で段 0 と A/B する。⚠️ 本書の `PI-18` の 1 本（決定 3）は、殻が刻みのあいだ同じ答えを読む道（同行の「続けざまの刻みのあいだ同じ答えを読むために公開した」）と同じ向きであり、`DFC-610` の直しを縛らない。数え方は `JDG-127`（`JDG-50`、段 0 より悪くしない）のまま。
- ⭐ **`DFC-628` は本書で仕様の側が決まる** —— 「どちらの答えを正とするか」は、`S-76` の下限から上げて初めて達する倍率である。⚠️ 台帳の状態と、測った条件（畳んだ文書）の書き漏れを直すのは前に立つ者である。
- ⭐ **`JDG-126` の注「上限そのもの（`FR-016`、`CR-381`）は変えない」と両立する** —— 同注は性能の直しに掛けた縛りであり、本書は利用者がその後に `JDG-134` で上限の**読み**そのものを決めたものである。⭐ しかも第 2 節の 54 条件（畳まない起動文書）では上限の値は 1 つも動かない。

## 9. ⛔ この変更でやらないこと ／ 返した問いと答え

- ⛔ 字の側の上限の求め方（`:3130`〜`:3136` と、`:3137` の `（MUST）` まで）を変えない。
- ⛔ `S-54` / `S-55` / `S-76` の値を変えない。
- ⛔ 帯の高さの数え方（表 T-038 の `OC-10`、表 T-014）と表 T-252 を変えない —— 前者は `JDG-134` が退けた案 ④ である。
- ⛔ 帯が単調でない原因を書かない（決定 6）。
- ⛔ `src/`・`tests/`・`docs/development-records/rulings.md`・`docs/development-records/defects.md`・変更履歴（`docs/development-records/changelog.md`）を編集しない —— **本体の持ち場の外である。**<br>⚠️ 手順 02 の 7 は履歴を 1 行足せと定めるが、**前に立つ者は本書について足さないと指示した**（2026-09-17）。
- ⭐ **問い 1（`R1.4`）—— 答えを得た。記録だけ残す**: 下限の倍率で既に、いちばん高い行の帯が `Row Area` の高さを超える文書では、**字義どおりに読むと上限は最小の倍率（`S-76` の下限）そのものとなり、拡大の入力で倍率が下がる。**<br>⭐ そういう文書は実在する —— 第 2 節の「2 行に畳む」文書で、窓 1920×400 の下限での帯 1022.96px が `Row Area` 374px を超える（前に立つ者が配線の体の探り針を読んだ数。本書は追試していない）。<br>⭐ **利用者の裁定 `JDG-78`（異常系・準正常系は台帳に残し、リファクタ一式が終わったあとに回す）により、リファクタの後に回す。** 仕様の条項は足さない（決定 4）。いまのコードの 2 つの実装も下限を答え（第 1 節）、`FR-055` はそういう文書が在ると認めている。
- ⭐ **問い 2 —— 答えを得て当てた**: `:3137` の「上限の倍率は表示の倍率に依らない」は字の側についてだけ成り立ち、帯の側は表示の倍率で動く（第 2 節）。⇒ 字の側に限ると明示し、帯の側は動くと書き添えた（決定 5、`:3137`〜`:3139`）。赤になる逐語の試験は 0 件（第 6.3 節）。
- ⚠️ **問い 3（前に立つ者へ。今回は決めない）**: `05-07-design.md:725` は「`FR-016` の行の軸の錨だけが本表を 2 回まで走らせる」と定めるが、帯の側の上限を解くには `rowPlacesAtZoomY` を倍率を変えて何度も問う（`CR-381` 決定 7 の代償）。「2 回まで」が上限を解く道にも掛かるのかは、本書を読んだ限り決まっていない。本書が書く前から在る食い違いであり、本書は動かしていない。⭐ 前に立つ者が台帳に記録する（2026-09-17 の答え）。
- ⭐ **問い 4 —— 答えを得た**: 台帳の `DFC-628` の非単調は、起動テンプレートの 1000 件を少数の行に畳んだ文書で出る（第 2 節）。畳まない起動テンプレートでは 0 点で、本書の 54 条件と一致する ⇒ `DFC-628` は覆っていない。台帳の条件の書き漏れは前に立つ者が直す。⭐ 本書の条項は単調かどうかに依らず 1 つの倍率を指す。
