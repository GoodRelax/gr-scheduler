# CR-232 — 問う面が 3 つ立ったのに、問いの文をどこからも読めない

> **閉じるもの**: ⛔ **表 T-037 の `NT-7` は「何が起きるかを示したうえで、続けるか取りやめるかを選ばせること（MUST）」と定める。**
> ⛔ **問う場所は 5 つとも `src/` に結線され、2 択の入口も語も図形も揃ったのに、`RaisedConfirmation.text` は 3 か所とも空文字で上がっている。**
> ⚠️ **`FR-038` の辞書には問いの文で引ける節が無く、書く側は「ここに書けば 2 つ目の翻訳置き場になる」と自ら注記して空で上げている。**
> ⭐ **利用者の裁定（2026-08-23）: 表 T-234 を立てる。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`。受ける目標は `GL-006`。** ⛔ **文の無い確認は、何を消すのかを言わずに「続ける／取りやめる」を選ばせる面である。**
⚠️ **`FR-031` が確認の階級を「取り消しで取り戻せないものを失う場面」に絞っている以上、その面は最も説明を要する面である。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `NT-7` の「何が起きるか」| ⛔ **辞書の 9 節はどれも問いの文で引けない。** ⭐ **本巡の 表 T-233 とまったく同じ形の穴であり、あちらは理由、こちらは問いである** |
| **`R1.1`**（矛盾が無い）| `confirmationOwedBy` の注記 | ⛔ **「問う場所は 2 か所であり、ほかに無い」と書いてあるが、`FR-099` が 3 つ目を MUST で持つ** ——「消すことで解かれる割当があるときは、そのタスクの名前を示して確認を求めること（MUST）」。⚠️ **版 0.70 の変更履歴が「`FR-031` の例外を 2 つに数え直した」と既に書いており、コードだけが古い** |
| **`R2.18`**（公開する契約が自分で意味を成す）| `RaisedConfirmation.text` | ⚠️ **型は `string` で、何を入れてよいかを言う先が無い。** 本 CR で 表 T-234 の行になる |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **鍵は 行 ID（`QN-n`）とする** | **表 T-233 と同じ形にする。** ⛔ 同じ仕組みに 2 つの流儀を作らない |
| 2 | ⭐ **消えるものの名前は辞書に置かない** | **`NT-7`（MUST）が挙げよと定めるのは対象の名前であり、それは文書の値である。** ⛔ 辞書に置けば文書の中身を翻訳置き場に持つことになる。`RaisedConfirmation.items` が既に運ぶ |
| 3 | ⭐ **`FR-099` の行を立てる** | ⛔ **コードの「2 か所」は誤りである。** 仕様が 3 つ目を持つ以上、名簿は 3 つ目を持つ |
| 4 | ⭐ **行の無い問いの落ち先 `QN-6` を置く** | **表 T-233 の `RS-15` と同じ理由** —— 落ち先が無いと、文の無い問いが画面へ出る |

---

## 1. 測った

```
induced.py  T-233 T-037 FR-076 FR-038 FR-032 FR-099 T-227 T-024a T-103
            FR-031 UN-8 UN-17 T-067 T-027 FR-055 T-206 FR-020 FR-065
            FR-086 T-230 T-033 FR-025
```

| 測ったこと | 実測 |
|---|---|
| 種のうち仕様書に在るもの | 22 / 22 |
| 種の内側の辺 | 16 |
| 閉路 | ⭐ **0** —— 1 つずつ書ける |
| ⛔ 問いの文で引ける辞書の節 | **0**（辞書は 9 節）|
| 空文字で上がっている問い | **3 か所**（`confirmationOwedBy` ／ `askToWriteOverDestination` ／ `askToDiscardCurrentDocument`）|
| `NT-7` を MUST で求める要求 | **5 場面**（`FR-032` が 2 ／ `FR-099` ／ `DI-4` ／ `OP-4`）|

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-076` の本文 | 1 文足す ——「問いが示す文は 表 T-234 の行とすること（MUST）。同表に無い問いを立ててはならない（MUST NOT）」 |
| 2 | 表 T-234（新設）| 6 行。欄は 行 ID / 場面 / 名前を挙げるか / 正 |
| 3 | Chapter 6.2 の原稿の節 | 名簿の出どころに 表 T-234 を足す |
| 4 | `_source/display-words.json` | `questions` 節（6 項目 × `text`）|
| 5 | `tools/generate_display_words.py` | 表 T-234 から名簿を起こす |
| 6 | A-appendix | 変更履歴に 1 行 |

⛔ **`FR-032` と `FR-099` と `DI-4` と `OP-4` は 1 文字も動かさない** —— どれも既に正しい。欠けていたのは文の置き場である。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 133 | **134** | **＋1**（表 T-234）|
| `figures` | 11 | **11** | 0 |
| `rows` | 1630 | **1636** | **＋6**（`QN-1` 〜 `QN-6`）|
| `uids` | 148 | **148** | 0 |

⚠️ **辞書の枠は 214 → 220 になる**（6 行 × `text` の 1 欄）。
