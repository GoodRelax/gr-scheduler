# CR-222 — 待ちをまたいで立った問いが乗るフレーム

> **閉じるもの**: ⛔ **表 T-078 は契機を 5 つに閉じ「本表に無い契機でフレームを起こしてはならない（MUST NOT）」と定めるが、`CS-4` の待ちが終わって問いが立つ瞬間の契機が無い。**
> ⭐ **利用者の裁定（2026-08-23、裁定 21）**: **`FT-1` の文言を広げる。**
> ⚠️ **仕様は自分で「待つことそのものは画面を何も変えない ——「表 T-078 に契機が無いからである」」と書いており、待ちの終わりを詰め忘れていた。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`。受ける目標は `GL-006`。** ⛔ **上書きの確認（表 T-227 の `DI-4`、MUST）と行の削除の確認（`FR-032`、MUST）が、規則の上では描けない。**

⚠️ **どちらも「問うこと」を MUST で求める要求である。** 問いを立てても、それを画面に出す契機が表に無ければ `NFR-010` の MUST NOT に触れる。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（網羅性）| 表 T-078 の 5 契機 | ⛔ **`CS-4` の待ちの後が抜けている。** `CR-210` は**着地**（`FT-2`）だけを論じた |
| **`R4.4`**（状態遷移の抜け）| 押下 → 待ち → 問い | ⛔ **3 番目の遷移に契機が無い。** 1 番目は `FT-1`、着地は `FT-2` |
| **`R1.2`**（曖昧さの排除）| `FT-1`「人の入力（ポインタとキー）」 | ⚠️ **いまの文言は「入力そのもの」としか読めない。** 待ちをまたいだ続きが含まれるかどうかが読めない |
| **`R2.9`**（YAGNI）| `FT-6` を新設する案 | ⛔ **退けた。** 契機の粒度が「入力」から「入力の一部」へ落ちる。⭐ **そのフレームは押下の続きであって、別の契機ではない** |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **「気づくもの」の欄は動かさない** | ⛔ **`DomInputSource` が渡すのは最初の押下だけであり、続きを渡すのはシェル自身である。** ⚠️ **ここを広げると `IF-2` の供給物を広げたことになり、表 T-078 の末尾の注が明文で禁じている** |
| 2 | ⭐ **`CS-4` の注（`05-07-design.md`）に 1 文足す** | ⛔ **あの注は「待つことそのものは画面を変えない」で終わっており、読んだ者が「では問いは？」で止まる。** ⭐ **`FT-1` を指させて閉じる** |

---

## 1. 測った

```
induced.py  T-078 FT-1 FT-2 CS-4 T-066 （ほか 34 種と同時に）
impact.py   T-078
```

| 測ったこと | 実測 |
|---|---|
| 表 T-078 の行数 | **5 行**（`FT-1` 〜 `FT-5`）|
| 表 T-078 を指している要求 | **1 件** |
| 閉路 | ⭐ **0** |

⭐ **逐語で確かめた:**

```
表 T-078 の末尾   「本表に無い契機でフレームを起こしてはならない（MUST NOT）」
                  ——「これが NFR-010 の具体である」
05-07-design:449  「⛔ 待つことそのものは画面を何も変えない —— 表 T-078 に契機が無いからである。
                    待っていることを示す表示を出してはならない（MUST NOT）」
FT-2 の括弧       「現在値の差し替え（表 T-067 の WS-6）」⇒ 文書の差し替えに固定。問いは文書ではない
```

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-078 の `FT-1` | 契機の欄を広げる ——「人の入力（ポインタとキー）。⭐ **その入力の、待ち（表 T-066 の `CS-4`）をまたいだ続きを含む**」|
| 2 | `05-07-design.md` の `CS-4` の注 | 1 文足す ——「⭐ **待ちが終わって問いが立つときのフレームは `FT-1` である**（その押下の、遅れて来た残りだからである）」|
| 3 | A-appendix | 変更履歴に 1 行 |

⛔ **`FT-2` 〜 `FT-5` は 1 文字も動かさない。** ⛔ **`CS-4` の MUST NOT（待っているあいだ現在値を読み直さない／待っている表示を出さない）も動かさない。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **132** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1613 | **1613** | ⭐ **0** —— 既存の 1 セルへの追記 |
| `uids` | 148 | **148** | 0 |
