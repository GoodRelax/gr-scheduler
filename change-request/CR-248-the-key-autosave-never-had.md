# CR-248 — 自動保存が持てなかった鍵

> **閉じるもの**: ⛔ **`FR-061` は逐語で「本要求が使う鍵はまだ決まっていない」と書き、表 T-227 の `DI-1` を使うことを MUST NOT で禁じている。**
> ⭐ **禁じている理由は 1 つだけである** —— 「`DI-1` はファイル名を含むが、**1 度もファイルへ保存していない文書にも自動保存は掛かる**」。
> ⭐ **利用者の裁定（2026-08-25）: `grs-default.json` を既定のファイル名とする。未保存の文書は 1 枠を共有してよい。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-5`（単一の `.html` で完結させる）。受ける目標は `GL-005`。**
⚠️ **自動保存とクラッシュ復旧はサーバー無しで完結させる約束の一部である**（表 T-024 の `IO-5`）。

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `FR-061` の鍵 | ⛔ **要求が自分で「決まっていない」と書いている。**⭐ **決めれば閉じる** |
| **`R3`**（境界）| 未保存の文書どうし | ⛔ **既定のファイル名を与えると、未保存の文書は 3 つ組がすべて一致して同じ鍵になる** —— 起動テンプレートは `Project.name` が固定で `Project.id` を持たない |
| **`R2.19`**（驚かせない）| 上書き | ⚠️ **2 つ目の新規文書が 1 つ目の自動保存を上書きする。**⭐ **利用者はこれを承知で「1 枠を共有してよい」と裁定した** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`FR-061` の MUST NOT を外し、鍵を `DI-1` とする** | **禁止の理由がファイル名の不在ただ 1 つであり、裁定がそれを埋めた。**⛔ **理由が消えた禁止は残さない** |
| 2 | ⭐ **既定のファイル名は 表 T-206 ではなく 表 T-211 に置く** | ⚠️ **文書に保存する値ではないが、`localStorage` に置く別枠でもない** —— **成果物が持つ既定である。**⛔ **`S-99a`（透かしに出す名前）と同じ形にはしない** —— あちらは読む人が決める値である |
| 3 | ⛔ **4 つ目の状態は作らない** | ⭐ **裁定は 1 枠の共有を許した。**⚠️ **「まだ 1 度も自動保存していない」を別の状態にすると 表 T-109 の行・図形・語が要る**（`RC-13` の裁定を含む）。**利用者はそれを求めていない** |
| 4 | ⚠️ **上書きの危険を要求に書き残す** | ⛔ **裁定で受け入れた危険であって、消えた危険ではない。**⭐ **`LM-` の限界として書くのが本書の作法である** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| `FR-061` の禁止の理由 | 「`DI-1` はファイル名を含むが、1 度もファイルへ保存していない文書にも自動保存は掛かる」—— ⭐ **理由はこれ 1 つ** |
| `DI-1` の中身 | **ファイル名 ＋ `Project.name` ＋ `Project.id` の 3 つがすべて一致するときに限り同じ文書**（MUST）|
| 起動テンプレート | ⛔ **`Project.id` を持たない。`Project.name` は `Product Development Programme` の固定値** |
| ⇒ 帰結 | ⛔ **既定ファイル名を与えると、テンプレートから始めた未保存の文書はすべて同じ鍵になる** |
| 置き場 | ⭐ **既に決まっている** —— 表 T-024 の `IO-5`、および `FR-026` の「同じ鍵を上書きするだけ」 |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-061` | 鍵を `DI-1` とすること（MUST）。⛔ **「まだ決まっていない」の文と、`DI-1` を禁じる MUST NOT を削る** |
| 2 | `_source/settings.json` の 表 T-211 | 1 行（既定のファイル名 `grs-default.json`）|
| 3 | 表 T-004 の `LM-` | 1 行 —— **未保存の文書は自動保存の枠を共有し、後のものが前のものを上書きする**（利用者の裁定で受け入れた限界）|
| 4 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1685 | **1687** | ＋2（既定ファイル名の行と `LM-` の行）|
| `uids` | 148 | **148** | 0 |
