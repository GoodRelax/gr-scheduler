# CR-441 — 掴みの 9 つの表を、ほかの規則が既に決めている違いで割り切り、96 行を 86 行にする（Step 2）

> ⭐ **状態: 起草（2026-09-22）。⛔ 仕様・試験・コード・設定・基準線・台帳には 1 文字も当てていない。**
> 読んだ木: `9b8a22b7`（ブランチ `claude/intelligent-blackwell-b1b4b3`、`origin/refactor` と同じ commit）。
> **行番号・行数・参照の数は、すべてこの木で自分で測ったものである。** 測り方は 14 節にある。
>
> **閉じるもの**: `CR-430` が開けたままにした **Step 2（表の正規化）** —— 利用者の `JDG-192` の一通目「Step2. 仕様の整理 (ロジックの整理) 幾何学、トポロジー、集合論、... 的に正規化、シンプル化する。」。
> `CR-430` の 5 節「⚠️ Step 2（正規化）で行が減ったら、数え直してから当てる」と 11 節「表 GA の値の正規化（Step 2）を先取りしない」が、この変更を別の 1 枚として予告している。
> あわせて `CR-430` の 8 節の `check.sh` の不変条件 1〜3 に要る規則の文を置く（6 節）。
>
> **型**: 利用者が示した例 —— 札の置き方を素朴に数えると 48 通り（実績を表示 2 × 実績がある 2 × 形 3 × マーカー 2 × 入る 2）になるが、「ダミーはまだ始まっていない実績」という原則で割ると 表 T-272 と 表 T-273 の 11 行になる。
> **本書は同じ手を 9 つの表の全部に、行が減らなくなるまで当てた。**
>
> ⛔ **守った枠（利用者の逐語）**:
> - 「**=== / ---> / ◆ はすべて規則が異なる。 疎結合にするべき。**」（2026-09-20。`CR-431` の冒頭が逐語で持つ）⇒ **形をまたいで行を併せたものは 0 である。**
> - `JDG-194`「あるオブジェクトに対する設定値(定数)が他のオブジェクトに影響を与えるのは蜜結合となって悪手だろ？」⇒ **表 T-206 の `S-250` 〜 `S-290`（41 の値、1 値 1 行）は 1 行も併せない。** 定数を持つ掴み代の行も併せない（2 節の「退けたもの」）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`**（`CH-1` の受ける目標。描いた形を直接掴んで動かす）—— 9 つの表は、その目標を縮小と重なりの中で成り立たせる規則そのものである。
同じ違い（ダミーか実績か、再開日が未定か、ポインタの白か黒か）を 2 行に書いていると、片方だけを直したときに 2 行が食い違い、利用者が掴んだものの振る舞いが行によって変わる。
行を減らすのは、その食い違いの起きる所を減らすためである。

同じく **`GL-006`**（マニュアルを読まずに主要な操作へ到達する）—— ポインタの形の表（表 T-269）の白黒の約束を 1 か所に寄せる。

⚠️ **正直に言うと、利用者が画面で気づく変化は 0 である**（どの併合も結論を変えないものだけを採った。2 節）。
本書の大半は仕様の手入れであり、`CH-` を直に前へ進めるのは上の 2 点に限る。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正がある）／ `R1b` の「同じことを2度書かない」** —— 退ける 10 行はどれも、ほかの行か、既に在る MUST が決めている違いだけを持つ（3 節）。
  ⭐ 機械も同じものを指していた —— 検査 11 の重複の基準線（`docs/review/duplication-baseline.txt:49`）が `PK-1` と `PK-2` を「同じ中身の組」として持っている。
- **`R1.5`（MECE）** —— 表 T-272 の条件の列「実績がある」は、`FR-043` の MUST（ダミーは「まだ始まっていない実績」として実績と同じ規則で置く）のもとでは結論を分けない列である。
  列を落としても、決定表の検査（1.9 の MUST）は 2 組を 2 行で覆う（6 節）。
- **`R1.4`（異常系・境界値）** —— 表 T-268 の `TY-3`（ダミー）と `TY-4`（実績の端）の併合を検討し、**結論が変わる組を 1 つ見つけたので退けた**（2 節の「退けたもの」の ⑤）。
- **`R2`（命名。名前を足すときは必ず引く）** —— 新しい接頭辞・表・図・要求 ID は **0** である。退けるのは行 ID 10 個だけで、どれも振り直さない。

### ③ 利用者に問わずに決めたこと

⭐ 下の 6 つは、根拠を添えて利用者に問わずに決めた（あとから覆せる）。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | **`JDG-279` の読みの欄にある「⛔ 9 つの表の行は 1 行も退役・併合・振り直しをしない」を、`CR-431` の巡の約束と読む** | ⭐ `JDG-279` の逐語は「7 つにする（推奨）（体が書いたもの）」だけであり、行の凍結は逐語に無い。⭐ `CR-431` の冒頭はその約束を「利用者の裁定（疎結合）に従い、9 つの表は 96 行のまま」と**その巡の範囲**として書く。⭐ 一方 `JDG-192` の逐語は Step 2 を「正規化、シンプル化する」と名指しで予定し、`CR-430` の 5 節・11 節がそれを別の 1 枚として予告している。⭐ 逐語が 2 つとも生きる読みは「疎結合 ＝ 形をまたいで併せない」「凍結 ＝ `CR-431` の巡だけ」である | ⚠️ `JDG-279` の読みの欄は、当てるときに前に立つ者が範囲の注を足す必要がある（本書は `rulings.md` を触らない） |
| 決定 2 | **形をまたぐ併合は 1 つも採らない** | 利用者の逐語「=== / ---> / ◆ はすべて規則が異なる。 疎結合にするべき。」 | 結論が同じでも形が違えば残る（`GA-9` と `GA-14`、`LP-1` と `LP-5` ほか。2 節） |
| 決定 3 | **残る行は小さい方の ID を引き継ぎ、意味を広げる**（例: `PK-1` が白と黒の両方を持つ）。退く ID は二度と使わない | ⭐ `CR-430` の決定 9 と同じく、ほかから名指されている ID を変えない。⭐ 広げた後も、今それを名指している文はどれも偽にならない（4 節で 1 つずつ確かめた）。例外は `PE-3`（`PE-2` より `PE-3` が名指されている —— `FR-107` の `:3730`「停止日を動かすのは実績の終了の端（`PE-3`）」）| 行 ID の番号と「どの行が残ったか」の対応は規則的でない |
| 決定 4 | **表 T-274 で、`RF-1` を `PP-3` から `PP-5` へ移す** | ⭐ `PP-5`（ダミーはまだ始まっていない実績）が覆っていた `PE-4`・`PE-5` は `PE-3`・`PE-7` に吸われる。そのまま当てると `PP-5` が覆う行は **0** になり、原則 → 行 の向きが欠ける（5 節）。⭐ 移す先に `RF-1` を選ぶのは、`PP-5` の出所である `JDG-186` の着地の欄が「`FR-043` と 表 T-272 の `RF-2`」であり、`RF-2` を吸うのが `RF-1` だからである。⭐ 利用者の示した例（48 → 11）も、この原則を表 T-272 に当てた形である | `PP-3`（見えないものは掴めない）の覆う行は `RF-3` の 1 行になる |
| 決定 5 | **`PE-2`（予定の端）を `PE-3`（実績の端）に併せる** | ⭐ 2 行は、3 つの結論の列が今も**字面まで同じ**である（「選ぶ」「その端だけ（置く値は 表 T-245）」「—」）。決定表で、結論の同じ 2 行が 1 つの条件の値でだけ違うなら、その条件は結論を分けていない。⭐ 予定と実績で置く値が違うことは 表 T-245 の `GO-1`〜`GO-7` が持ち、本表は持たない | ⚠️ 表 T-270 を「予定の行」「実績の行」と目で探す読み手には 1 行が 2 役に見える。`PP-2`（実績は事実）の覆う行が 1 つ減る |
| 決定 6 | **`FR-044` に、表 T-270 を指す 1 文を足す**（不変条件 2 を当てた日に緑にするため） | ⭐ 表 T-274 の結びが既に「`PE-13` の理由は `FR-044` が持つ」と書き、`PE-13` の行も「（`FR-044`。置く値は 表 T-245）」と書く —— 結び目は表の側にだけあり、`FR-044` の側に無い。⭐ 足すのは指し先であって規則ではない | `FR-044` の STATEMENT が 1 行長くなる |

---

## 1. 読んで確かめた事実（`9b8a22b7` で測った）

| # | 事実 | 所 |
|---|---|---|
| (a) | 9 つの表は **96 行**: GA 22 ／ HT 4 ／ TY 9 ／ PK 9 ／ PE 14 ／ XS 14 ／ RF 3 ／ LP 8 ／ ZO 13 | `01-04-requirements.md:3497` ／ `:3582` ／ `:3595` ／ `:3649` ／ `:3701` ／ `:3797` ／ `:3839` ／ `:3849` ／ `:3926` |
| (b) | 仕様全体は **tables=181 ／ figures=25 ／ rows=2241 ／ uids=162**、接頭辞 **160** | `md-checks.py` と `row-id-prefixes.json`（14 節） |
| (c) | ⭐ **不変条件 1 の規則は既に在る** —— `FR-104` の RATIONALE（`:3495`）「本要求が持つ表は、どれも `FR-104` 〜 `FR-110` ／ `FR-043` ／ `FR-009` ／ `FR-044` のいずれかから名指される —— 親を持たない表を置かないこと（MUST NOT）」。検査 56（`check-grab-table-parents.py`）が読み、いま緑（13 の表、名指しの無い表 0） | `:3495` |
| (d) | ⛔ **不変条件 2（どの要求も表を名指す）の規則はどこにも無い。** 検査 56 の docstring は「逆向きは書かれていないので足すな —— `FR-044` は表を名指していない」と明記する | `check-grab-table-parents.py` の docstring、`check.sh:188` 〜 |
| (e) | ⛔ **`FR-044` は 9 つの表を 1 つも名指さない**（名指すのは 表 T-019・T-019a・T-021・T-201 だけ）。`CR-430` の 4.5 節の追跡表は `FR-044` の行に ● を 9 つ置いたが、当てた形には 1 つも無い | `:2852` 〜 |
| (f) | ⭐ **不変条件 3 の規則は既に在る** —— 1.9 の末尾（`:393` 〜 `:396`）の決定表の MUST。検査 57（`check-decision-tables.py`）が 表 T-272（4/4）・表 T-273（12/12）・表 T-270（14 行 × 3 列 ＝ 42/42）で読み、いま緑 | `:393` |
| (g) | ⛔ **`CR-431` の 3 節・10 節が定めた両向きの契約の試験（表 T-274 の「覆う行」と 9 つの表の行の突き合わせ）は、木に無い**。`grep -rn "T-274" tests/` は 0 件 | `tests/` |
| (h) | 検査 11 の重複の基準線は、9 つの表の中に 4 つの組を持つ: (`GA-4`,`GA-5`,`GA-6`) ／ (`TY-2`,`TY-3`) ／ (`GA-9`,`GA-14`) ／ (`PK-1`,`PK-2`) | `docs/review/duplication-baseline.txt:43`・`:44`・`:47`・`:49` |
| (i) | 利用者の逐語「疎結合にするべき」は `rulings.md` に **0 件**（`grep -c 疎結合` ＝ 0）。逐語は `CR-431` の冒頭にだけある | `rulings.md` |
| (j) | `PK-1` 〜 `PK-9` と `PE-8`〜`PE-10` はコードの**値**である —— `src/framework/single-html-shell/frame-loop.ts:274` 〜 `:282` の型の和と `:301` 〜 の掴み代 → ポインタの写像、`src/adapter/input-command-translator/item-grab.ts:231` の戻り値の型 | 同左 |

### ⛔ 「覆う条項が無い」と書く前に打った 3 つ（規則どおり）

本書が「無い」と書くのは (d) と (g) の 2 つだけである。どちらも次の 3 つを打った。

- (d) —— ① `grep -n "名指す" docs/spec/01-04-requirements.md` で `FR-104`〜`FR-110` の近くを読んだ: 片向きの 1 文（`:3495`）だけ。② `impact.py FR-044` ⇒ 指す先に 9 つの表が無い。③ ほかの道で同じものに届くか ⇒ 表 T-274 の結び（`:418`）が `PE-13` の理由を `FR-044` に置く —— **表 → 要求 の向きはあるが、要求 → 表 の向きが無い**。⇒ 無いのは規則の文と `FR-044` の 1 文である（6 節・決定 6）。
- (g) —— ① `grep -rln "T-274\|PP-1" tests src tools .claude/skills` ⇒ `check-identifier-reservation.py` と `check.sh` の注だけ（どちらも表の番号の衝突の話）。② `CR-431` の 13 節（当てた体の差分）に試験の着地の記載が無い。③ 検査 56・57 は表 T-274 を読まない。⇒ **原則 ↔ 行 の両向きは、いま機械のどこにも無い**（10 節の所見 2）。

---

## 2. 正規化 —— 手と、繰り返した回

### 2.1 手（3 つ。どれも「結論を変えない」ことを確かめてから採った）

| 手 | 中身 | 使った規則（既に在るもの） |
|---|---|---|
| **A** ダミーは実績に数える | 2 行が「ダミーか実績か」だけで違い、結論が同じなら、実績の行に「ダミーを含む」と書いて 1 行にする | `PP-5`。`FR-043`（`:2772`）「ダミーは『まだ始まっていない実績』として、実績と同じ規則で置き、掴ませること（MUST）」。`FR-107` の STATEMENT「実績（ダミーを含む）」。⭐ 表の中に前例が 3 つある —— `XS-2`「実績・ダミー」、`XS-9`「実績・ダミーの菱形」、`ZO-2`「実績バー・実績のダミー…」 |
| **B** ほかの MUST が決める変種は行にしない | 2 行が、別の所の MUST が既に決めている 1 つの違いだけを持つなら、1 行にして、その違いは MUST を指す | 表 T-269 の結び「白 ＝ 予定、黒 ＝ 実績とダミー、の約束を、箱の矢印と円で揃えること（MUST）」／ `FR-044`「再開日が未定のときは表 T-201 の `S-25` に従って別の見た目にすること（MUST）」と 表 T-273 の結び「再開日があれば再開日に、未定なら停止日の翌日」。⭐ 前例: `PK-3`（入 ／ 出を 1 行）、`PK-1`（← ／ → を 1 行）、`GA-20`（未定のアイコンも 1 行） |
| **C** 結論の同じ行を 1 つにする | 決定表で、結論の列がすべて同じ 2 行は、違う条件の値が結論を分けていない | 1.9 の決定表の MUST（覆い、重ならない）は、併せた後も満たす |

### 2.2 回ごとの結果

| 回 | 手 | 退く行（→ 吸う行） | 減 |
|---|---|---|--:|
| 1 | A | `RF-2` → `RF-1` ／ `PE-4` → `PE-3` ／ `PE-5` → `PE-7` ／ `PE-9` → `PE-8` ／ `XS-14` → `XS-6` | −5 |
| 2 | B | `PK-2` → `PK-1` ／ `PK-6` → `PK-5` ／ `XS-11` → `XS-10` ／ `XS-13` → `XS-12` | −4 |
| 3 | C | `PE-2` → `PE-3` | −1 |
| 4 | A・B・C を全部の表にもう一度 | **0**（下の「退けたもの」がすべてである） | 0 |

⭐ **回 4 で何も減らなかったので止めた。**

### 2.3 表ごとの行数（前 → 後）

| 表 | 接頭辞 | 前 | 後 | 退く行 |
|---|---|--:|--:|---|
| 表 T-266 | GA | 22 | **22** | — |
| 表 T-267 | HT | 4 | **4** | — |
| 表 T-268 | TY | 9 | **9** | — |
| 表 T-269 | PK | 9 | **7** | `PK-2`・`PK-6` |
| 表 T-270 | PE | 14 | **10** | `PE-2`・`PE-4`・`PE-5`・`PE-9` |
| 表 T-271 | XS | 14 | **11** | `XS-11`・`XS-13`・`XS-14` |
| 表 T-272 | RF | 3 | **2** | `RF-2` |
| 表 T-273 | LP | 8 | **8** | — |
| 表 T-020 | ZO | 13 | **13** | — |
| **計** | | **96** | **86** | **10** |

測り方: 「前」は 14 節の数え方で `9b8a22b7` を数えた。「後」は「前」から 3 節の 10 行を引いた予測である（当てた後に同じ数え方で突き合わせる）。

### 2.4 ⛔ 退けたもの（検討して、併せなかった行）

| # | 候補 | 退けた理由 |
|---|---|---|
| ① | `GA-5`・`GA-6` → `GA-3`・`GA-4`、`GA-17` → `GA-16`、`GA-21`・`GA-22` → `GA-12`・`GA-13`（手 A） | ⛔ 定数が別である —— `JDG-213`「ダミーの掴み代の値は、実績の掴み代とは別の定数として持つ」、`FR-104` の結び（`:3544`）「ダミーの掴み代は実績と別の値で持ち…（MUST）」、`JDG-194`。行の鍵は「掴む対象」であり、対象ごとに独立した定数がある限り行は対象の数だけ要る。⭐ 検査 11 の組 (`GA-4`,`GA-5`,`GA-6`) もこれで残る |
| ② | `GA-7` ↔ `GA-8`（フェード入 ／ 出） | ⛔ 定数が別（`S-268` ／ `S-269`、`JDG-194`） |
| ③ | 形をまたぐ組: `GA-9` ↔ `GA-14`、`GA-1`〜`GA-4` ↔ `GA-10`〜`GA-13`、`LP-1`・`LP-3` ↔ `LP-5`・`LP-6`、`XS-10` ↔ `XS-12`、`PE-6`・`PE-7` ↔ `PE-1`・`PE-3` | ⛔ 決定 2（疎結合）。⭐ 検査 11 の組 (`GA-9`,`GA-14`) もこれで残る |
| ④ | `LP-1`〜`LP-4`（`===` の 4 行）を「入る」と「マーカー」の 2 つの小表に割る | 2 × 2 の積であり、割っても 2 ＋ 2 ＝ 4 行で減らない。形ごとに割ると 4 ＋ 6 ＝ 10 行になり増える |
| ⑤ | `TY-3`（ダミー）→ `TY-4`（実績の端）（手 A） | ⛔ **結論が変わる組がある。** 手順 2（`HT-2`）は「上下に近いタスク」を残すので、同じ段に左右に並んだ 2 つのタスクは縦の距離が同じ（0）で 2 つとも残る。例: `--->` のタスク A の実績の終了（`GA-13`、中央合わせ `S-276` ＝ 12px）と、すぐ右のタスク B のダミーの開始（`GA-21`、`S-287` ＝ 12px）が形の外で重なる所では、今は形の外の列でダミー（4 位）が実績の端（5 位）より先なので B が応える。併せると同じ種別になり、手順 4（`HT-4`）の中心までの距離で決まる —— A の終了に近い点では A が応える。⭐ 検査 11 の組 (`TY-2`,`TY-3`) もこれで残る |
| ⑥ | `TY-2` ↔ `TY-7`（実績の外 ／ 中のマーカー） | 順位が違う（2 位 ／ 7 位）。併せると順が消える |
| ⑦ | 表 T-020 のどの 2 行 | 各行が 1 つの順位である。併せると 2 つの前後が消え、前後を無関係にする規則（重ならないことの保証）は仕様に無い |
| ⑧ | 表 T-267 | 4 つの手順は「前で決まれば後を読まない」の段であり、どれも別の問いに答える |
| ⑨ | `PK-7` ↔ `PK-8`（環境の指 ／ てのひら） | どちらの形かを決める規則が行のほかに無い |
| ⑩ | `XS-7`（中線） | 位置は `XS-5`・`XS-6` から導けるが、`GA-10`〜`GA-13`・`GA-21`・`GA-22` と `FR-104` の結び（`:3549`）が名指す定義である |
| ⑪ | `RF-1`・`RF-3`（後の 2 行） | 条件 1 列 × 2 値であり、それより減らない |
| ⑫ | `PE-7`（◆ の実績）↔ `PE-3`（端）、`PE-12` ↔ `PE-11` ほか | 前者は ③（形をまたぐ）。後者は結論が違う |

### 2.5 行ではなく列の重複（⛔ 本書は当てない。行数は動かない）

- **表 T-268 の 2 つの列** —— 表の前書き（`:3598`）が自ら「2 つの列の違いは依存線の位置だけである」と書く。9 × 2 ＝ 18 のセルは、1 列 9 セル ＋「形の外では依存線をフェードの掴み点の次へ上げる」の 1 文で持てる。
- **表 T-266 のポインタの欄** —— 22 のセルは「掴む所」の種別（予定の端・実績とダミーの端・フェード・本体・◆ の予定・◆ の実績とダミー・マーカー・依存線・再開アイコン）で決まる推移的な従属である。
- ⚠️ どちらも試験（`cr-430-ty-1-to-9-type-order`・`cr-430-ga-pointer-column`）が列を読むので、当てるなら別の 1 枚にする（10 節の所見 5）。

---

## 3. ⛔ 退く行（削除を先に列挙する）と、旧 → 新の対応

⛔ **ID は振り直さない。退く ID は二度と使わない。** 新しい行 ID は 0 である。

| 退く行 | → | 吸う行 | 使った手と規則 |
|---|---|---|---|
| `PK-2`（箱の矢印 黒） | → | `PK-1` | 手 B —— 表 T-269 の結びの白黒の約束（MUST） |
| `PK-6`（円 ●） | → | `PK-5` | 手 B —— 同上。径の 2 つの値は `S-295` の対のまま（値は併せない） |
| `PE-2`（予定の端） | → | `PE-3` | 手 C —— 結論の列が字面まで同じ（決定 5） |
| `PE-4`（ダミー。タスク） | → | `PE-3` | 手 A —— `PP-5`・`FR-043`・`FR-107` の「実績（ダミーを含む）」 |
| `PE-5`（ダミー。◆） | → | `PE-7` | 手 A —— 同上 |
| `PE-9`（ダミーの右のマーカー） | → | `PE-8` | 手 A —— 同上。⭐ `JDG-234` の理由「ダミーは実績の代わりである（`JDG-213`）」がこの行を立てた理由そのものである |
| `XS-11`（`===` の未定の再開アイコン） | → | `XS-10` | 手 B —— `FR-044` の `S-25` の MUST（大きさ）と 表 T-273 の結び（位置）。⭐ 縦の中心は今も「`XS-10` と同じ中心線」（`JDG-226`） |
| `XS-13`（`--->` の未定の再開アイコン） | → | `XS-12` | 手 B —— 同上 |
| `XS-14`（`--->` の未着手の印） | → | `XS-6` | 手 A —— 縦は実績の線と同じ中心・同じ太さ（`XS-6` と同じ）。横幅と形は 表 T-240 の `DM-3`・`DM-4`、置く日は `DM-1` が既に持つ |
| `RF-2`（実績がない → ダミー） | → | `RF-1` | 手 A —— `FR-043` の MUST。列「実績がある」ごと落ちる |

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **書き換える文字列が 1 度しか現れないことを、書き換える前に数えること**（`CR-430` の 9 節）。

### 4.1 9 つの表の行

**表 T-269**（`PK-2`・`PK-6` の行を消し、次の 2 行に書き直す）

| 行 ID | 名前 | 形 | 中 ／ 縁 | 縁の角 | 大きさ | 押さえる点 |
| --- | --- | --- | --- | --- | --- | --- |
| PK-1 | 箱の矢印 | 幅の広い箱型の矢印（← ／ →） | 予定は 白 ／ 黒、実績とダミーは 黒 ／ 白（本表の結びの約束） | 丸め | 一辺 `S-249` | 中心 |
| PK-5 | 円 | 円 | 予定は ○（白 ／ 黒）、実績とダミーは ●（黒 ／ 白）（同上） | — | 径は `S-295`（○ と ● で別の値） | 中心 |

**表 T-270**（`PE-2`・`PE-4`・`PE-5`・`PE-9` の行を消し、次の 3 行に書き直す）

| 行 ID | 掴んだもの | 押して離す（動かさない） | 横に引く | 縦に引く（行を移る） |
| --- | --- | --- | --- | --- |
| PE-3 | 予定の端・実績の端（実績の端はダミーの端を含む。<br>`===` と `--->`） | 選ぶ | その端を離した日にする（置く値と据え置く値は 表 T-245） | — |
| PE-7 | ◆ の実績（ダミーを含む） | 選ぶ | その日付だけ（置く値は 表 T-245） | — |
| PE-8 | 進捗マーカー（`===` で実績の右に立つ。<br>実績はダミーを含む。<br>中断のあいだを除く） | 状態を 1 つ進める（下の「押下の巡り」） | 実績の終了を離した日にする（置く値は 表 T-245） | — |

⚠️ 表の前書き `:3703` の「タスクに属する行（`PE-1` 〜 `PE-11`）」は、退いた ID を範囲に含むので、`PE-1` ／ `PE-3` ／ `PE-6` ／ `PE-7` ／ `PE-8` ／ `PE-10` ／ `PE-11` と並べて書き直す。

**表 T-271**（`XS-11`・`XS-13`・`XS-14` の行を消し、次の 3 行に書き直す）

| 行 ID | 形 | 帯 | 縦の位置 | 高さ |
| --- | --- | --- | --- | --- |
| XS-6 | `--->` | 3 段目: 実績の矢（未着手の印 ＝ ダミーを含む） | 線の上の縁を、予定の線の下の縁から `S-10` 下に置く | 予定と同じ線の太さと矢じり。<br>未着手の印の横幅と形は 表 T-240 の `DM-3` / `DM-4` |
| XS-10 | `===` | 再開アイコン | 腕を実績の帯の中心線に置く（箱の中心 ＝ 中心線）。<br>縦棒は再開の位置（表 T-273 の結び —— 再開日、未定なら停止日の翌日）の左の境目 | 箱 ＝ マーカーの径（`S-22`）。<br>再開日が未定のときは `FR-044` の `S-25` を掛ける |
| XS-12 | `--->` | 再開アイコン | 3 段目。<br>腕を実績の線に置き、縦棒は線から下へ伸ばす。<br>縦棒は再開の位置（同上）の左の境目 | 箱 ＝ 名前の字の大きさ（`FR-094`）。<br>再開日が未定のときは `FR-044` の `S-25` を掛ける |

⚠️ 今の `XS-10` の「箱の下端はマーカーの下端に揃う」は、再開日のあるアイコンについての帰結であり、規則は `05-07-design.md` の `LF-13`（「下端をマーカーの下端に」）が持つ ⇒ 書き直した行からは落とす。

**表 T-272**（列「実績がある」と `RF-2` の行を消す）

| 行 ID | 実績を表示 | 札の基準 |
| --- | --- | --- |
| RF-1 | する | 実績（ダミーを含む —— まだ始まっていない実績はダミーであり、幅は 表 T-240 の `DM-3`、マイルストーンはダミーの菱形） |
| RF-3 | しない | 予定 |

**表 T-274**（1.10 節。「覆う行」の欄を 5 つ書き直す。⛔ 仕様の原稿では範囲ではなく 1 つずつ並べる —— `CR-431` の 13 節の 4）

| 行 | 今 | 後 |
|---|---|---|
| `PP-1` | GA 22 ／ HT 4 ／ TY 9 ／ `XS-1`〜`XS-14` | GA 22 ／ HT 4 ／ TY 9 ／ `XS-1`〜`XS-10`・`XS-12`（計 46） |
| `PP-2` | `PE-1`・`PE-2`・`PE-3`・`PE-6`〜`PE-10` | `PE-1`・`PE-3`・`PE-6`・`PE-7`・`PE-8`・`PE-10`（6） |
| `PP-3` | `RF-1`〜`RF-3` | `RF-3`（1） |
| `PP-5` | `PE-4`・`PE-5` | `RF-1`（1。決定 4） |
| `PP-6` | `PK-1`〜`PK-9` | `PK-1`・`PK-3`・`PK-4`・`PK-5`・`PK-7`・`PK-8`・`PK-9`（7） |

`PP-4`・`PP-7` と結びの段（`:418`・`:419`）は変えない。

### 4.2 指し先（仕様の中。消えた ID か、消えた ID を含む範囲を名指す所）

| 所 | 今 | 後 |
|---|---|---|
| `01-04:3511`〜`:3514`・`:3520`・`:3521`・`:3529`・`:3530`（`GA-3`〜`GA-6`・`GA-12`・`GA-13`・`GA-21`・`GA-22` のポインタの欄） | 「箱の矢印 ← 黒（`PK-2`）」ほか 8 つ | 「箱の矢印 ← 黒（`PK-1`）」ほか。⭐ 「黒」の字は残す |
| `01-04:3524`・`:3525`（`GA-16`・`GA-17`） | 「円 ●（`PK-6`）」 | 「円 ●（`PK-5`）」 |
| `01-04:3674`（`FR-106`） | 「`PK-5` と `PK-6` に置き換わる」 | 「`PK-5` に置き換わる」 |
| `01-04:3640`（`FR-106`） | 「（表 T-270 の `PE-8` / `PE-9`）」 | 「（表 T-270 の `PE-8`）」 |
| `01-04:2931`（`FR-013`）・`:3690`（`FR-107` の STATEMENT） | 「`PE-8` 〜 `PE-10`」 | 「`PE-8` と `PE-10`」 |
| `01-04:3392`（`FR-016`） | 「（`FR-107` の 表 T-270 の `PE-8` / `PE-9`）」 | 「（`FR-107` の 表 T-270 の `PE-8`）」 |
| `01-04:3400`（`FR-016`） | 「引く 2 つの場面（`PE-8` / `PE-9`）では追従する」 | 「引く場面（`PE-8`）では追従する」 |
| `01-04:3462`（`FR-103` の表 T-245 の `GO-9`） | 「（… `PE-8` / `PE-9`）」「実績の右に立つマーカー（`PE-8`）では」「未着手のダミーの右に立つマーカー（`PE-9`）では」「`PE-8` では `actualStart`」 | 「（… `PE-8`）」「実績の右に立つときは」「未着手のダミーの右に立つときは」「実績の右に立つときは `actualStart`」 —— ⭐ 値の 2 つの場合分けは 表 T-245 が持ち続ける（本書の外） |
| `01-04:3819`（`FR-109` の 表 T-271 の結び） | 「（`XS-4` 〜 `XS-7` と `XS-12` 〜 `XS-14`）」 | 「（`XS-4` 〜 `XS-7` と `XS-12`）」 |
| `01-04:1536`（`FR-003`） | 「`XS-10` 〜 `XS-13` が持つ」 | 「`XS-10` ／ `XS-12` が持つ」 |
| `05-07:1250`（表 T-221 の `LF-11`） | 「再開アイコンは `XS-10` 〜 `XS-13`」 | 「再開アイコンは `XS-10` ／ `XS-12`」 |
| `05-07:1252`（同 `LF-13`） | 「同表の `XS-11` 〜 `XS-13`（未定のアイコンと、線だけの形の 3 段目のアイコン）の縦は」 | 「同表の `XS-10` の未定のアイコンと `XS-12`（線だけの形の 3 段目のアイコン）の縦は」 |
| `_source/settings.json` の `S-249` の `value.ja`（`:4130`） | 「（表 T-269 の `PK-1` / `PK-2` / `PK-4`）」 | 「（表 T-269 の `PK-1` / `PK-4`）」 |
| 同 `S-295` の `value.ja`（`:4160`） | 「（表 T-269 の `PK-5` の ○ と `PK-6` の ●）」 | 「（表 T-269 の `PK-5` の ○ と ●）」 |

⛔ `_assets/tbl-settings.md` の 2 か所（`:417`・`:419`）は生成物である —— `npm run gen` で作り直す（手で直さない。検査 21）。

### 4.3 足す 2 文（6 節と決定 6）

- `FR-104` の RATIONALE、`:3495` の直後に 1 文（6.1）。
- `FR-044` の STATEMENT の「中断のあいだは再開アイコンを描くこと（MUST）。」の直後に 1 文: 「再開予定日を画面上で動かすときの効果は 表 T-270 の `PE-13` に従うこと。」

---

## 5. 追跡 —— 両向き（当てた後の形）

### 5.1 原則 → 行（どの原則も 1 行以上を覆う）

| 原則（表 T-274） | 覆う行（後） | 数 | 吸われた行と、吸うのに使った規則 |
|---|---|--:|---|
| `PP-1` どれも指せる | GA 22 ／ HT 4 ／ TY 9 ／ `XS-1`〜`XS-10`・`XS-12` | 46 | `XS-11`・`XS-13`（`FR-044` の `S-25`）、`XS-14`（`PP-5`） |
| `PP-2` 実績は事実 | `PE-1`・`PE-3`・`PE-6`・`PE-7`・`PE-8`・`PE-10` | 6 | `PE-2`（手 C）。`PE-4`・`PE-5`・`PE-9` は `PP-5` で `PP-2` の行へ入った |
| `PP-3` 見えないものは掴めない | `RF-3` | 1 | — |
| `PP-4` 被らせない | `LP-1`〜`LP-8`・`ZO-5` | 9 | — |
| `PP-5` ダミーはまだ始まっていない実績 | `RF-1` | 1 | ⭐ **5 行を吸った** —— `RF-2`・`PE-4`・`PE-5`・`PE-9`・`XS-14`（3 つの表にまたがる）。⭐ 「規則の数が減る」という理由の文が、行の数で裏づけられた |
| `PP-6` ポインタの形で、何を掴むかが分かる | `PK-1`・`PK-3`・`PK-4`・`PK-5`・`PK-7`・`PK-8`・`PK-9` | 7 | `PK-2`・`PK-6`（表 T-269 の結びの白黒の約束） |
| `PP-7` 描く前後は 1 つの表で決める | 表 T-020 の `ZO-5` を除く 12 行 | 12 | — |
| 結びの段が名指す例外 | `PE-0`・`PE-11`・`PE-12`・`PE-13` | 4 | — |
| **計** | | **86** | **10** |

### 5.2 行 → 原則（どの行も、原則 1 つか、名指された例外 1 つに属する）

⭐ 86 行 ＝ 46 ＋ 6 ＋ 1 ＋ 9 ＋ 1 ＋ 7 ＋ 12 ＋ 4。重なり 0（決定 4 で `RF-1` を `PP-3` から外したので、`RF-1` が 2 つの原則に現れることは無い）。

### 5.3 ⚠️ 追跡で出た穴（所見。発明で埋めていない）

| # | 穴 | 扱い |
|---|---|---|
| 穴 1 | 手 A をそのまま当てると `PP-5` の覆う行が 0 になる | 決定 4（`RF-1` を移す）で閉じた |
| 穴 2 | `PP-3` の覆う行が 3 → 1 に痩せる | 穴ではない（1 行以上ある）。記録だけ |
| 穴 3 | 手 B が使った 2 つの規則（白黒の約束、未定の `S-25`）は表 T-274 の原則ではない | 穴ではない —— それぞれ `PP-6`・`PP-1` の領分の中の MUST であり、行は 1 つの原則に属したままである |
| 穴 4 | 両向きを機械で読む試験が無い（1 節 (g)） | 10 節の所見 2 |
| 穴 5 | `FR-044` は 9 つの表を 1 つも名指さない（1 節 (e)） | 決定 6 で閉じる |

---

## 6. 不変条件 1〜3 の規則の文（規則が先。`handoff.md` の §5.1 の作法）

### 6.1 不変条件 1・2（追跡） —— `FR-104` の RATIONALE

- **1（表 → 要求）**: ⭐ **既に在る。新しい文は要らない。** `01-04-requirements.md:3495` の逐語（1 節 (c)）。⛔ 検査 56 の錨（「親を持たない表を置かないこと（MUST NOT）」）を含むので、この行の字を変えない。
- **2（要求 → 表）**: `:3495` の直後に、次の 1 行を足す（逐語）:

  > ⚠️ **逆に、`FR-104` 〜 `FR-110` ／ `FR-043` ／ `FR-009` ／ `FR-044` のどれも、表 T-266 ／ 表 T-267 ／ 表 T-268 ／ 表 T-269 ／ 表 T-270 ／ 表 T-271 ／ 表 T-272 ／ 表 T-273 ／ 表 T-020 のうち少なくとも 1 つを名指すこと（MUST）** —— 表を指さない要求は、規則を散文で持つことになる。

- ⭐ 当てた日に緑である条件は 決定 6 の 1 文である。測った: 今 `FR-044` 以外の 9 つは 1 つ以上を名指す（`FR-104` 5 表・`FR-105` 5・`FR-106` 3・`FR-107` 2・`FR-108` 2・`FR-109` 5・`FR-110` 2・`FR-043` 5・`FR-009` 1）。
- ⛔ 置き場を 1.9 にしない —— 1.9 は語と記号の書き方の規約であり、この 10 の要求と 9 つの表に限った規則ではない（`CR-431` の 3 節の案 B と同じ読み）。

**検査の側が持つもの**（⛔ 仕様に書かない）:
- 許される親: 1 の行（錨で探す）に書かれた `FR-nnn`（`〜` を開く）—— 今の検査 56 のまま。
- 名指すべき表の集合: 2 の行に書かれた `表 T-nnn` の 9 つ（2 の行を錨「少なくとも 1 つを名指すこと（MUST）」で探す）。
- 名指しの数え方: 要求のブロックの中の `表 T-nnn`。⛔ 表自身の見出し（`**表 T-nnn —`）は数えない（検査 56 と同じ）。
- ⚠️ 検査 56 の docstring と `check.sh:188` の注の「⛔ ONE-DIRECTIONAL … Do not add the other direction」は、2 の文が入った日に偽になる ⇒ 同じ波で書き直す。

### 6.2 不変条件 3（決定表） —— 1.9 の表記規約

⭐ **既に在る。新しい文は要らない。** `01-04-requirements.md:393` 〜 `:396`（逐語は 1 節 (f)）。

**検査の側が持つ条件の列と値の域**（`check-decision-tables.py` の `TABLES`。当てた後の形）:

| 表 | 読み方 | 条件の列と値の域 | 組 ／ 行 |
|---|---|---|---|
| 表 T-272 | 全組 | ⚠️ **変わる**: `実績を表示` ∈ {する, しない} の 1 列だけ。⛔ `実績がある` の列を検査から消す（表から列が消えるので、残すと「列が無い」で赤になる） | 2 組 ／ 2 行 |
| 表 T-273 | 全組 | 変わらない: `形` ∈ {`===`, `--->`, ◆}、`マーカー` ∈ {出す, 出さない}、`入る` ∈ {入る, 入らない} | 12 組 ／ 8 行 |
| 表 T-270 | 行列 | 変わらない: 条件は `掴んだもの`（行が域）。結論 3 列 | 10 行 × 3 ＝ 30 セル |

⚠️ 同じファイルの注（`:96` の「表 T-270's `PE-8` 〜 `PE-10`」、docstring の「表 T-270 13 rows」）は古い —— 後者は今でも 14 行である。同じ波で直す。

---

## 7. グラフ（測った先）

**`induced.py`（種 31）** —— 退く 10 行・吸う 9 行・表 T-274 の 5 行・書き直す要求と設定（`FR-044`・`FR-104`・`FR-106`・`FR-107`・`FR-109`・`FR-103`・`FR-016`・`FR-013`・`S-249`・`S-295`・`GO-9`）:

```
seeds in the specification : 31 of 31
edges inside the seed set  : 48
cycles in the induced graph: 3
   size 3 : PK-1 PK-2 S-249
   size 3 : PK-5 PK-6 S-295
   size 2 : FR-016 FR-104
```

- 3 つとも「値の行と、その規則を持つ行の間の小さな閉路」（規則 02 の 1 節）であり、直さない。⭐ **それぞれ 1 回で書く** —— 表 T-269 と `settings.json` の `S-249`・`S-295` を同じ回に、`FR-016` の 2 か所と `FR-104` の RATIONALE を同じ回に。

**`impact.py`（退く 10 行、1 つずつ）** —— 仕様の中で名指す箇所（行そのものの定義を除く）:

| 退く行 | 名指す箇所 |
|---|---|
| `PK-2` | 表 T-274（`:415`）、`FR-104` の GA の 8 行、`tbl-settings.md:417` |
| `PK-6` | 表 T-274、`FR-104` の 2 行（`:3524`・`:3525`）、`FR-106`（`:3674`）、`tbl-settings.md:419` |
| `PE-2` | 表 T-274（`:411`）だけ |
| `PE-4`・`PE-5` | 表 T-274（`:414`）だけ |
| `PE-9` | 表 T-274、`FR-016`（`:3392`・`:3400`）、`FR-103` の `GO-9`（`:3462`）、`FR-106`（`:3640`） |
| `XS-11` | 表 T-274、`05-07:1252` |
| `XS-13` | 表 T-274、`FR-003`（`:1536`、範囲）、`05-07:1250`・`:1252` |
| `XS-14` | 表 T-274、`FR-109`（`:3819`、範囲） |
| `RF-2` | 表 T-274（`:412`）だけ |

⚠️ `impact.py` は範囲の書き方（「`PE-8` 〜 `PE-10`」）の中の ID を拾わない —— `grep` で足した（`FR-013` の `:2931`、`FR-107` の `:3690`、表 T-270 の前書き `:3703`）。4.2 節の表は両方を合わせたものである。
⭐ **表 T-023d は 1 か所も名指さない**（`GR-23` の行は表を名指すだけ）⇒ 別のセッションの持ち場に触れない。

---

## 8. 仕様の外で退く ID を名指す所（数えた。`scratch/` と `change-request/` を除く）

| 区分 | ファイル | 数 |
|---|---|--:|
| 仕様の原稿 | `docs/spec/01-04-requirements.md` 38（うち行の定義 10・表 T-274 の欄 10）／ `docs/spec/05-07-design.md` 3 | 41 |
| 設定の原稿 | `docs/spec/_source/settings.json`（`S-249`・`S-295`） | 2 |
| 生成物 | `docs/spec/_assets/tbl-settings.md`（作り直す） | 2 |
| 図 | `docs/spec/_assets/fig-pointer-shapes.svg`（図 F-025。`tools/probe/grab-figures.mjs` が作る） | 2 |
| 道具 | `tools/probe/grab-figures.mjs` 2（`'PK-2'`・`'PK-6'` の鍵）／ `tools/generate_entity_types.py` 2（注） | 4 |
| 検査 | `.claude/skills/spec-graph-check/check-decision-tables.py`（表 T-272 の域、`PE-8` 〜 `PE-10` の注）／ `check-grab-table-parents.py` と `check.sh` の注（6.1） | 3 か所 |
| 基準線 | `docs/review/duplication-baseline.txt:49`（`PK-1` ＋ `PK-2` の組が消える） | 1 |
| `src/` | `framework/single-html-shell/frame-loop.ts` 16（`PK-2`・`PK-6` —— 型の和と写像の**値**）／ `entity/layout-engine/schedule-geometry/schedule-geometry.ts` 7（`RF-2`・`XS-11`・`XS-13`）／ `entity/layout-engine/schedule-layout/schedule-layout.ts` 4 ／ `adapter/input-command-translator/item-grab.ts` 3（`PE-9` —— 戻り値の型の**値**） | 30（4 ファイル） |
| `tests/` | `unit/cr-430-table-pe-press-and-drag-effects` 25 ／ `unit/cr-430-t-271-the-cross-section-of-each-shape` 24 ／ `unit/cr-430-t-272-which-basis-the-labels-follow` 8 ／ `unit/in-2-a-the-dummies-say-slide` 7 ／ `unit/cr-430-table-pk-the-pointer-shapes` 6 ／ `unit/cr-427-endpoints-show-arrow-pointers-and-a-press-keeps-its-shape` 5 ／ `unit/in-2-b-armed-dependency-shows-the-arrow` 5 ／ `unit/in-2-pointer-shape` 5 ／ `unit/cr-430-ga-pointer-column` 4 ／ `unit/fr-043-a-the-dummy-sits-in-the-actual-band` 3 ／ `unit/t-038-oc-2-labels-in-the-occupied-width` 2 ／ `unit/cr-430-bench` 1 ／ `unit/cr-430-cross-section-scene` 1 ／ `unit/cr-430-table-go-the-values-a-release-puts` 1 ／ `unit/dm-14-a-hidden-actual-hides-its-dummy` 1 ／ `unit/t-023d-double-click-only-rows` 1 ／ `unit/t-023d-follows-the-pointer` 1 | 100（17 ファイル） |
| `tests/system/cr-430-*` | `cr-430-labels-and-hidden-things-sweep.test.ts` | **0** |
| 台帳（⛔ 本書は触らない） | `rulings.md` の着地の欄 10 行（`JDG-181`・`JDG-186`・`JDG-200`・`JDG-206`・`JDG-208`・`JDG-222`・`JDG-226`・`JDG-230`・`JDG-234`・`JDG-288`）／ `defects.md` 1（`DFC-652`） | 11 |

- 図: **F-025 だけが描き直しになる**（`grab-figures.mjs` の `PK-2`・`PK-6` の小窓は、`PK-3a`・`PK-3b` の前例のとおり `PK-1`・`PK-5` の 2 つの塗りとして残す）。F-020〜F-024 の原稿は退く ID を 0 回しか名指さない。
- ⚠️ `src/` の 2 ファイル（`schedule-geometry.ts`・`item-grab.ts`）は**別のセッションが編集している**（`item-hit-area` ／ `schedule-geometry` ／ `item-grab`）⇒ 10 節の所見 6。
- 範囲の書き方の中の退く ID（`PK-1 through PK-9` などのコードの注）は上の数に入れていない —— 範囲としては偽にならない。

---

## 9. 数の予測

`9b8a22b7` の実測（2026-09-22）は **tables=181 ／ figures=25 ／ rows=2241 ／ uids=162 ／ 接頭辞 160**。

| 量 | 式 | 差 | 後 |
|---|---|--:|--:|
| tables | 表は足さず退けない（表 T-272 は列が減るだけ） | ±0 | **181** |
| rows | 9 つの表の行 −10（3 節）。設定・鍵の行は字を直すだけ | **−10** | **2231** |
| uids | 要求は足さず退けない（`FR-044`・`FR-104` は文が増えるだけ） | ±0 | **162** |
| figures | 図 F-025 は描き直し（数は変わらない） | ±0 | **25** |
| 接頭辞 | `PK`・`PE`・`XS`・`RF` はどれも行が残る | ±0 | **160** |

- 突き合わせは `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py` の `tables= figures= rows= uids=` と `docs/spec/_source/row-id-prefixes.json` の `prefixes` の数。⭐ 外れたら数え落としが出ている（規則 02 の 2 節）。
- ⚠️ ほかのセッションの着地で「前」が動く —— 当てる直前に「前」を測り直し、差（−10 ／ ±0）で突き合わせる（規則 02 の 2.5 節）。

---

## 10. 所見（発明せずに記すもの）

| # | 所見 | 数 | 提案 |
|---|---|--:|---|
| 1 | `JDG-279` の読みの欄の「9 つの表の行は 1 行も退役・併合・振り直しをしない」は、本書を当てた日に字面では偽になる | 1 行 | 決定 1。前に立つ者が当てるときに範囲の注（「`CR-431` の巡の約束。Step 2 は `CR-441`」）を足す |
| 2 | `CR-431` の両向きの契約の試験が木に無い（1 節 (g)） | 0 本 | 本書の波で、仕様だけを読む体に書かせる —— 数は 86 ／ 覆う 82 ／ 例外 4（5 節） |
| 3 | `GA-20`（再開アイコン）は、9 つの表で唯一、1 行が 2 つの形（`===` ／ `--->`）を持ち、形ごとの条件をセルの中に書く（「`--->` の 3 段目では中線より下だけ」）。定数も 1 つ（`S-286`）を共有する | 1 行 | ⛔ 本書は割らない —— 割ると行と定数が 1 つずつ増え、新しい定数の値を発明することになる。疎結合の裁定に照らして割るかは、別に測る |
| 4 | 利用者の逐語「疎結合にするべき」が `rulings.md` に無い（`CR-431` の 13 節の 5 が書かなかった） | 0 件 | 前に立つ者が裁定の行にするかを決める（本書は台帳を触らない） |
| 5 | 列の重複 2 つ（2.5 節）—— 表 T-268 の 2 列（18 セル → 9 ＋ 1 文）、表 T-266 のポインタの欄（22 セルが 9 つの種別で決まる） | 2 | 別の 1 枚で。試験 2 本が列を読む |
| 6 | 本書のコードの波は、別のセッションが編集中の `schedule-geometry.ts`（7 か所）と `item-grab.ts`（3 か所）に触れる | 10 か所 | その 2 ファイルは、別のセッションの着地の後に本書の波を切る（注の書き換えだけだが、同じ行の取り合いになる） |
| 7 | 同じ手 A は範囲の外の 表 T-245 にも候補がある（`GO-6`・`GO-7`・`GO-8` のダミーの行）。⚠️ ただし反対の端を床の日に作るので値が違い、手 A の条件（結論が同じ）を満たさない | 3 行 | 当てない。記録だけ |
| 8 | `check-decision-tables.py` の docstring の「表 T-270 13 rows」は今でも古い（今 14、後 10） | 1 | 6.2 節の波で直す |

---

## 11. 利用者に問う問い —— **0 件**

⭐ 問いの候補は 5 つあった。どれも 3 つの手を打ってから、問いにせず決めた。

| 候補 | (a) `rulings.md` を grep | (b) 隣の行で `impact.py` | (c) ほかの道はあるか | 結果 |
|---|---|---|---|---|
| 行を減らしてよいか（`JDG-279` の読み） | `正規化` ⇒ `JDG-192`（Step 2 を逐語で予定）。`疎結合` ⇒ 0 件。`JDG-279` の逐語は「7 つにする」だけ | `PP-1`〜`PP-7`（表 T-274 の欄だけが名指す） | 行を退かずに減らす道は無い | 決定 1・2 |
| `PP-5` の行をどこに置くか | `ダミーはまだ始まっていない` ⇒ 0 件。`JDG-186` の着地 ⇒ `RF-2` | `RF-1`・`RF-2`・`RF-3`（表 T-274 だけ） | `PE-4` を残す道 ⇒ 手 A を捨てることになる | 決定 4 |
| 予定の端と実績の端を 1 行にするか | `PE-2`・`PE-3` ⇒ 0 件（着地の欄にも無い） | `PE-2` ⇒ 表 T-274 だけ。`PE-3` ⇒ 表 T-274 と `FR-107` の 1 か所 | 残す道はある（代償は 1 行） | 決定 5（覆せる） |
| `FR-044` に 1 文足すか | `FR-044` の行の読み ⇒ 表を名指せと言う裁定は 0 件 | `PE-13` ⇒ 表 T-274 の結びが `FR-044` を名指す | 検査 56 の逆向きを入れない道 ⇒ 不変条件 2 を捨てることになる | 決定 6 |
| `GA-20` を形ごとに割るか | `JDG-222` ⇒ 「`===` と `--->` だけ」（要確認のまま） | `GA-20` ⇒ `S-286` を共有 | 割らない道（今のまま） | 所見 3（問いにしない） |

⚠️ **許しを求めるもの（問いではない）**: 基準線の動き —— `docs/review/duplication-baseline.txt` の `:49` の 1 行が消える（検査 11）。検査 44（`src/`・`tests/` が消える ID を名指す回数）と検査 43（裁定の着地の欄が名指す ID）も、同じ波で動く見込み。⛔ 本書も体も触らない —— 前に立つ者が、数を添えて利用者に 1 通で求める。

---

## 12. ⛔ この変更でやらないこと

- 仕様・試験・コード・設定・基準線・台帳を、本書では 1 文字も変えない（起草だけ）。
- 表 T-206 の `S-250` 〜 `S-290`（41 の値）を 1 つも併せない（`JDG-194`）。
- 形をまたいで行を併せない（決定 2）。
- 列の正規化（2.5 節）、`GA-20` の分割（所見 3）、表 T-245 の手 A（所見 7）を当てない。
- 表 T-023d に触れない（7 節の最後の行）。
- 新しい接頭辞・表・図・要求・裁定・欠陥の番号を 1 つも取らない。

---

## 13. 当てる順と commit の割り方

⭐ `CR-430` の 7.2 節と同じ理由で、**仕様だけでは commit できない** —— 退く ID は `src/` の型の値（`frame-loop.ts` の `PK-2`・`PK-6`、`item-grab.ts` の `PE-9`）と、17 の試験ファイル（100 か所）が名指す。

1. **`01-04-requirements.md`** を 1 回、文書の順に: 表 T-274 の 5 つの欄 → `FR-003`（`:1536`）→ `FR-013`（`:2931`）→ `FR-044`（決定 6）→ `FR-016`（`:3392`・`:3400`）と `FR-104`（GA の 10 セル・RATIONALE の 1 文）を**同じ回に**（閉路）→ `FR-103` の `GO-9` → 表 T-269（`PK-1`・`PK-5`）→ `FR-106`（`:3640`・`:3674`）→ `FR-107`（`:3690`・表 T-270・前書き `:3703`）→ 表 T-271 と結び（`:3819`）→ 表 T-272。
2. **`_source/settings.json`** の `S-249`・`S-295`（1 と同じ波。閉路）→ `npm run gen`。
3. **`05-07-design.md`** の `LF-11`・`LF-13`。
4. **図 F-025** を `tools/probe/grab-figures.mjs` で描き直す。
5. **検査**: `check-decision-tables.py`（表 T-272 の域）、検査 56 の逆向き（6.1）、`retired.py` に 10 の ID を理由付きで。
6. **`src/`**: `frame-loop.ts`（`PK-1`・`PK-5` に塗りを渡す形へ）、`item-grab.ts`（`PE-9` を返さない）、`schedule-geometry.ts`・`schedule-layout.ts` の注 —— ⚠️ 所見 6 の順で。
7. **試験**: ⛔ 実装した者に書かせない —— 仕様だけを読む体が、17 ファイルの退く ID を付け替え、`CR-431` の両向きの契約（所見 2）を 86 ／ 82 ／ 4 で書く。
8. 規則 02 の 3.5 節: **当てる前に「読む側は import の時点で投げるか」を測る**（`rowOf` を字下げ 0 で評価する試験。起草の時点の `grep` では、退く ID をモジュールの頭で引く行は 0 だった）。
9. `npm run gen` → `gen:check` → `check.sh` → `npm run typecheck` → vitest（統合の作業木で）→ 9 節の数を突き合わせる。
10. 台帳（前に立つ者）: `rulings.md` の着地の欄 10 行と `JDG-279` の範囲の注、`changelog.md` に 1 行。

---

## 14. 測り方の再現

```
# totals (before / after)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
python -c "import json;print(len(json.load(open('docs/spec/_source/row-id-prefixes.json',encoding='utf-8'))['prefixes']))"

# rows per table: under each '**表 T-nnn —' heading, count the '|' lines of the
# first table, minus the header and the '| --- |' rule (the same reading as
# find_table() in .claude/skills/spec-graph-check/check-decision-tables.py)

# graph
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py PK-2   # and each of the 10
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py PK-1 PK-2 PK-5 PK-6 PE-2 PE-3 PE-4 PE-5 PE-7 PE-8 PE-9 XS-6 XS-14 RF-1 RF-2 PP-1 PP-2 PP-3 PP-5 PP-6 FR-044 FR-104 FR-106 FR-107 FR-109 FR-103 FR-016 FR-013 S-249 S-295 GO-9

# references outside the specification: every file under the tree except
# node_modules/ .git/ dist/ scratch/ change-request/, the ID followed by
# neither a digit, a letter nor '-<digit>' (so PE-1 does not match PE-10)

# the checks that already hold invariants 1 and 3
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/check-grab-table-parents.py
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/check-decision-tables.py
```
