# CR-150 — 命令が従う行を要求側に立てる

> ⛔ **未適用である。** 7 編集は逐語検証済み（`old` はすべて `count=1`。行は 943 / 1395 / 1520 / 1548 / 2292 / 2438 / 2523）。
> ⚠️ **起草をやり直した。** 前の草稿（10 編集）は受け持ちが CR-149 と混ざり、**発明を 1 件・偽の根拠を 2 件**持っていた。落としたもの・差し替えたものは §4 にある。
> ⚠️ **`CR-145` は版 0.39 の変更履歴が席を使っている。ファイルは無いが再利用しない。**

**閉じるもの**: 命令の名簿（71 件）を 1 件ずつ掃いて出た**仕様の穴 39 件**のうち、**仕様の他所が既に答えを含意している 12 件から 10 件**。
⛔ **本 CR は要求本文だけを触る。** 名簿そのもの（表 T-108 と「正」の列）は **CR-149** が持つ。

## 1. 出どころ

名簿の実体は `previous-project-result/temp/document-command-roster.md` の 71 件である。それを 1 件ずつ「**この命令が書く列に、従う行が仕様に在るか**」で掃いた。**穴は、命令が要らないという意味ではなく、命令が従う行が無いという意味である。**

| | 数え直した実数 |
|---|---:|
| 掃いて出た穴（新規） | **39 件** |
| ├ **A. 仕様の他所が答えを含意している** | **12 件** |
| └ **B. 利用者の裁定が要る** | **27 件** |
| 既知の穴（`PRE-1` 〜 `PRE-10`） | 10 件 |
| **合計** | **49 件** |

⭐ **本 CR が閉じるのは A の 12 件のうち 10 件である。** 残る 2 件と、閉じきらずに残る部分は §3 へ降りる。

| A のうち | 行き先 |
|---|---|
| **`setCommentBoxAnchor` / `setCommentBoxBodyOffsetPx` / `setHighlightBoxRange` の「正」が誤り** | ⛔ **CR-149 が直す** —— 名簿の「正」の列は CR-149 の受け持ちである（§5）|
| **`setTaskVisualMilestoneGlyph` の 8 値の綴り** | ⚠️ **裁定へ**（§3 行 6）—— 置き場は用語集の `P-11` と決まっているが、**綴りが仕様のどこにも無い。**含意ではなく発明になる |
| 残る 10 件 | **§2 の 7 編集で閉じる** |

⚠️ **3 件は一部だけが閉じる。** `createResource`（`calendarUid`）・フェード日数（対象外の形状へ変えたときの値の存廃）・`createTaskGroup`（人が作る経路の `derivedFromTaskUid`）は、**編集①③④が閉じるのは含意のある部分だけ**であり、残りは §3 の行 1 / 9 / 13 になる。

⚠️ **本 CR は穴を「埋める」のではなく「どの行に従うか」を書く。** 値・寸法・丸めは書かない —— **境界値の持ち主は Chapter 6.1 である**と CR-146 が既に決めている。

## 2. 何をするか —— 7 編集。すべて `01-04-requirements.md`

**表も行も図も `UID` も 1 つも増やさない。** 触るのは既存の段落・STATEMENT・表のセルだけである。

| # | 場所 | 何を | 含意の在り処 |
|---|---|---|---|
| **①** | `FR-008`（`:1520` の段落末） | `createResource` が作る資源の種類・`createResource` / `createAssignment` の採番・`createAssignment` の重複禁止 | `FR-059`（`:2912`）／ `FR-001`（`:943`）と `AT-20` ／ 表 T-032 の `MG-5`（`:2837`）|
| **②** | `FR-088`（`:2438` の段落末） | `setCalendar` が触る 2 つの曜日の符号が別物であることと、その符号の持ち主 | `AT-17` / `AT-72`（表 T-058）と Chapter 6.2（`05-07-design.md:687`。正は公式 XSD）|
| **③** | `FR-001`（`:943` の STATEMENT 末） | `createTask` が WBS の親を与えないことと、その場で作る行の名前の導出元 | 表 T-015a の `HM-3`（`:1344`）と `AT-25` ／ `FR-058`（`:2896`）|
| **④** | 表 T-016 の `PR-14`（`:1395`） | フェード日数の適用形状を `PR-17` と同じ形の但し書きで限る | 表 T-012a の `FD-5`（`:1098`）。掴み領域の `GR-1` / `GR-2` も同じ絞りを持つ |
| **⑤** | 表 T-027 の `UN-14`（`:1548`） | 行の色と高さの変更に取り消しの区分を与える | `FR-042`（`:1493`）が編集させる 2 つ。同表末尾の MUST NOT が、文書を変える操作を対象外に置けなくしている |
| **⑥** | `FR-098`（`:2523` の段落末） | `unpinTaskGroup` の規則を `Row Pin` に持たせる | `FR-029`（同じ機能の入口を 2 か所に置かない）。同型の前例は `FR-071`（`:2582`）|
| **⑦** | `FR-051`（`:2292` の STATEMENT 末） | `setScrollPosition` の書き手を定める（`S-77` / `S-78` を誰が持つか）| 表 T-024a の `OP-10`（読む側）と `FR-026`（書き出しの間隔）|

**足す規範語は 7 つである**（①が 3 つ・③が 2 つ・⑥が 1 つ・⑦が 1 つ。②④⑤は 0）。

### ① `FR-008` —— 担当者と割当を作るときの 3 つ

```text
BEFORE …打ち間違えた名前や合流で流れ込んだ不要な担当者を消す手段が無いと、候補の一覧が永久に汚れたまま残る。**
AFTER  …（同上）。**
       ␣
       **新しく作る担当者は作業資源として作ること（MUST）** —— 担当ラベルに出す対象を `FR-059` が作業資源に絞っており、外れた種類で作ると、作った担当者の名前が日程表に出ない。**新しい `Resource` と `Assignment` の `uid` も `Project.uidHighWaterMark` に従って採ること（MUST）** —— 理由は `FR-001` が持つ。`AT-20` は発番済みの `uid` の最大値であって `Task` に限らない。**同じ `Task` と同じ `Resource` の組の割当を 2 つ作ってはならない（MUST NOT）** —— 合流の側は表 T-032 の `MG-5` が既に 1 つへ統合すると定めており、画面で作る側にだけ禁止が無いと、合流を通すかどうかで同じ操作の結果が変わる。
```

⚠️ **「担当ラベルに出る資源」とは書かない。** `:292` が 1 概念 1 語を MUST で課しているので、`FR-059` が使う語（**作業資源**）をそのまま用いる。

### ② `FR-088` —— 2 つの曜日の符号

```text
BEFORE …別々に確定させると、稼働する曜日だけが変わって週の始まりが変わらない中間状態を人が読める。
AFTER  …（同上）。
       ␣
       ⚠️ **2 つの曜日の符号は別である** —— `Project.weekStartDay` と `WeekDay.dayType` は範囲が違い（表 T-058 の `AT-17` / `AT-72`）、**どちらも交換相手へ書き出す列なので、符号の正は Chapter 6.2 が指す公式 XSD が持つ**。
```

⚠️ **数値そのもの（0〜6 と 1〜7 の対応）は書かない。** 交換相手の符号であり、正は XSD 側にある。

### ③ `FR-001` —— 作ったタスクの WBS と、その場で作る行の名前

```text
BEFORE …**開始日と終了日が同じタスクを作ること（MUST）。**
AFTER  …**開始日と終了日が同じタスクを作ること（MUST）。** **作ったタスクに WBS の親を与えないこと（MUST）** —— 行と WBS は別の軸であり（表 T-015a の `HM-3`）、階層を与える入口は `FR-005` が持つ。`wbsParentUid` の `null` は根を表す（`AT-25`）。**その場で作った行は名前の指定を持たないので、載せたタスクをその行の名前の導出元とすること（MUST）** —— 名前も導出元も無い行の禁止は `FR-058` が持つ。
```

⚠️ **根拠は `HM-3` と `AT-25` である。** `FR-005` は**階層を移す入口**であって、作ったときの既定を含意しない（前の草稿はここを取り違えていた。§4）。

### ④ 表 T-016 の `PR-14` —— フェード日数の適用形状

```text
BEFORE | 日付の曖昧さを端のぼかしで表す | 無い（**拡張領域はこの 2 つだけ**） |
AFTER  | 日付の曖昧さを端のぼかしで表す。**適用する形状は表 T-012a の `FD-5` が限る** | 無い（**拡張領域はこの 2 つだけ**） |
```

⚠️ **`PR-17` の文言を写さない。** `PR-17` は「`shapeKind` が `'milestone'` のときだけ有効」と書くが、同じ言い回しを並べると検査 11 の新しい重複群になる。**指す形にした。**

### ⑤ 表 T-027 の `UN-14` —— 行の色と高さ

```text
BEFORE | UN-14 | 対象 | **行（`TaskGroup`）の追加・削除・名前の変更**（`FR-085` / `FR-032`）。⚠️ …
AFTER  | UN-14 | 対象 | **行（`TaskGroup`）の追加・削除・名前の変更と、行の色と高さの変更**（`FR-085` / `FR-032` / `FR-042`）。⚠️ …
```

⚠️ **行を増やさない。** 同表末尾の**「対象外の操作で文書が戻ってはならない（MUST NOT）」**により、文書を変える操作を対象外へ置くことはできない。`UN-3` は `Task` のプロパティ（表 T-016）に限られるので、行の色と高さはどこにも当たっていなかった。

### ⑥ `FR-098` —— ピン止めを外す

```text
BEFORE …**図形で描くこと（MUST）** —— 絵文字への対応は範囲外である（表 T-002 の `SO-13`）。
AFTER  …（同上）。 **その操作子で同じようにピン止めを外せること（MUST）** —— 各行に 1 つと決めた以上、外す入口を別に置くと `FR-029` に触れる。**同じ入口で解除する形は `FR-071` が既に採っている。**
```

⚠️ **「外す」「解除」の語が仕様に無い、とは書かない。** 数え直したところ `docs/spec` に**外す 13 件・解除 28 件**あった（`:1514` / `:1557` / `:2582`）。穴の実体は「`FR-098` に外す規則が 1 行も無い」ことであり、根拠は語の不在ではなく `FR-029` と `FR-071` の前例である。

### ⑦ `FR-051` —— 表示位置を書くのは誰か

```text
BEFORE **スクロールバーの操作でも表示位置を変えられるようにすること（MUST）。**
AFTER  **スクロールバーの操作でも表示位置を変えられるようにすること（MUST）。** **表示位置が変わったときは、`_assets/tbl-settings.md` の表 T-203 の `S-77` と `S-78` が新しい表示位置を指すようにすること（MUST）** —— 読む側の規則は表 T-024a の `OP-10` が持つ。⚠️ **その値をいつ書き出すかは `FR-026` が持つ。**
```

⛔ **「そのつど書くこと（MUST）」とは書かない。** `FR-026`（`:3113`）が**「編集のたびに書いてはならない（MUST NOT）」**と定め、**表 T-027 の `UN-8`（ズーム・スクロール・パン）を編集に数えてはならない**とまで書いている。前の草稿はここで正面衝突していた（§4）。**本 CR が課すのは「値が何を指すか」だけであり、書き出しの間隔には触れない。**

⚠️ **丸め（連続した縦位置を行の id へ落とす規則）は書かない。** 境界値なので Chapter 6.1 の領分である（CR-146 の線引き）。

## 3. ⚠️ 当てる前後を問わず利用者の裁定が要る —— 31 件（24 行）

**どれも「仕様の他所が答えを含意していない」ものである。** 決め打つと、決めた側の根拠が仕様に残らない。
**内訳は B の 27 件と、A から降りた 4 件**（行 1・6・9・13）である。

| # | 命令 | 決まらないこと |
|---|---|---|
| 1 | `createResource` | `calendarUid` を与えないのか、文書の暦（`AT-18`）を写すのか。`FR-008` は編集の禁止だけを持つ |
| 2 | `createResource` ほか 3 件 | 入口の面が無い。`PR-16` は `FR-008` へ送り返し、`FR-099` は「表示と削除だけ」と自ら限り（`:1672`）、`UF-66` に `FR-008` が挙がっていない |
| 3 | `setProjectProfile` | `Project` の 15 のスカラーのどれを編集させるか。`FR-074` は Chapter 5 へ送るが、Chapter 5 に該当の表が無い |
| 4 | `setProjectProfile` | 「基本情報」が用語集に無く、命令名の英語の正が無い（`:292` は 1 概念 1 語を MUST で課す）|
| 5 | `setProjectTitle` | `title` が空・`null` のときタブに何を出すか。空を受け付けるか（`AT-3` は `null` 可、`FR-035` は文書名を出す MUST）|
| 6 | `setTaskVisualMilestoneGlyph` | 8 値の ASCII 表記そのもの。⭐ **置き場は決まっている**（用語集の `P-11`）が、8 つの綴りがどこにも無い |
| 7 | `setTaskVisualNamePlacement` | `nameAnchor` の整数と 9 点の対応・原点・向き。`FR-002`（`:1162`）は「9 点アンカー」としか言わない |
| 8 | `setTaskVisualShapeKind` | `shapeKind` が `null` で `Task.milestone` が偽のとき、`SH-1` 〜 `SH-4` のどれで描くか |
| 9 | `setTaskFadeInDays` / `setTaskFadeOutDays` | 値を持つ `Task` を対象外の形状へ変えたとき、値を捨てるか残すか（編集④で閉じるのは適用範囲だけ）|
| 10 | `deleteTask` / `deleteTaskGroup` | 消した `Task` を `derivedFromTaskUid` で指す行の扱い。`CD-1` に `TaskGroup` が無く、`label` が空の行は `AT-54` に反する状態で残る |
| 11 | `pasteTaskSubtree` / `pasteTaskGroupSubtree` | 複製が `TaskVisual`・割当・注記・載っている `Task` を連れて行くか。削除には表 T-050 があるのに、複製には対になる表が無い |
| 12 | `pasteTaskGroupSubtree` | 複製した部分木をどの親のどの位置に着けるか（`:1697` は深さの上限しか言わない）|
| 13 | `createTaskGroup` | 人が作る経路で `derivedFromTaskUid` を置くことがあるか（編集③で閉じるのは `FR-001` の自動生成の経路だけ）|
| 14 | `setTaskGroupHeight` | 指定が段数の要る高さに満たないとき、切るのか広げるのか拒むのか。`ST-9`（`:1221`）と `FR-042`（`:1493`）が衝突する |
| 15 | `setTaskGroupCollapsed` / `setTaskGroupHidden` | 畳み・非表示の変更の取り消しの区分。`isHidden` は保存するのに表 T-027 のどの行にも当たらない |
| 16 | `setTaskGroupHidden` | 非表示にした行の配下の行を描くか。`HR-1a` は畳みについてだけ定める |
| 17 | `setTaskGroupCollapsed` | 「選ばれた行」を前提にする `HR-3` 〜 `HR-5` を `Agent API` がどう呼ぶか。選択は `UN-9` により文書ではない |
| 18 | `reorderTaskGroupSiblings` | `TaskGroup.order` の `null` の意味と、行を作ったときに置く値。同じ表の他の可の列はすべて `null` の意味を書いている |
| 19 | `createDependency` | `:1770` の「組」が先行と後続の対だけか、種別（`:1772`）まで含むか。同じ 2 つの間に種別違いの 2 本を引けるかが決まる |
| 20 | `createCommentBox` / `setCommentBoxAnchor` | `anchorTaskUid` / `anchorPoint` の書き手。`AR-5`（`:2222`）は留める経路を持たないと明記するのに、`CD-1` は留まった注記を連鎖削除する |
| 21 | 注記の 3 つの入口 | 引出し線の形を選ぶ入口（`setCommentBoxLeaderShapeKind`）、枠色を固定色へ戻す入口（`setHighlightBoxStrokeColor`。`FR-007` の射程が注記を含まない）、`cornerRadiusPx` の入口と既定（設定値表に行が 0 件）|
| 22 | `setZoom` / `setPanelWidths` / `pinTaskGroup` | ボタン経由のズームの中心（`:2275` はポインタ位置を前提にする）／ `FR-052` に上限の式が無く「キャンバス」が 3 義（`A-8`）のまま ／ `S-127` に達した状態でさらに止めたとき |
| 23 | `clearDualCursor` / `fitScheduleToScreen` | `dualCursor` を `null` へ戻す規則が無い（`DC-4` はモードを出るだけ）／ 畳みを捨てる操作を取り消せるか、起動時の `OP-10` の経路でも `HF-8` を働かせるか |
| 24 | 多値の設定 6 件 | `setStackDirection` / `setPlanActualDisplay` / `setGuideCursorMode` / `setPanelWidths` / `pinTaskGroup` / `setExportPngScale` の取り消しの区分。`UN-7` はトグルに限り、`UN-8` はズーム・スクロール・パンのみ、`UN-13` は列挙である |

⛔ **31 件を一度に利用者へ出さないこと。** 波の順に、その波のぶんだけ出す。**切り分けは `previous-project-result/temp/wave-slice.md` が持つ**（W1 は 9 件。うち 7 件が `Schedule`（`CP-1`）1 つに集まる）。
⭐ **既知の 10 件（`PRE-1` 〜 `PRE-10`）も本 CR では閉じない。** 同じ裁定にかける。⚠️ うち `PRE-9`（`ラグ` の用語登記）は用語集に行を足すので、**CR-149 と席を取り合う。**

## 4. ⛔ 前の草稿から落としたもの・差し替えたもの

**受け持ちが CR-149 と混ざったのが、作り直した理由である。**

| | 何が起きていたか | 本 CR |
|---|---|---|
| **発明 1 件** | 編集 7 の「**そのつど**書くこと（MUST）」は、`FR-026`（`:3113`）の「**編集のたびに書いてはならない（MUST NOT）**」と正面衝突していた | ⭐ **書き出しの間隔を `FR-026` へ委ね、値が何を指すかだけに絞った**（編集⑦）|
| **偽の根拠 1** | 編集 3 が `FR-005` を「作成時の既定を含意する」根拠として引いていた。`FR-005` は階層を移す入口である | ⭐ **`HM-3`（`:1344`）と `AT-25` に差し替えた**（編集③）|
| **偽の根拠 2** | 編集 6 の「『外す』『解除』は仕様全体で 0 件」。実際は**外す 13 件・解除 28 件** | ⭐ **語の不在を根拠から外し、`FR-071`（`:2582`）の前例を挙げた**（編集⑥）|
| ⛔ **受け持ちの越境** | 編集 8 〜 10 が名簿の「正」（`FR-019`→`FR-016`）を書き替えようとしていた。**その 3 行は CR-149 の 表 T-108 に既に入っている** | ⛔ **3 編集とも落とした。CR-149 が直す**（§5）|
| **数が 3 か所で合わない** | 「31 件」が §2 の**箇所数**と §3 の**行数**の混成、新しい MUST も「8 つ」→ 実数 7 | ⭐ **A 12 / B 27 / 既知 10 と、規範語 7 に数え直した**（§1 / §2）|
| **行番号の誤り** | 編集 2 の対象を `:2436`、`**Relations**` を `:2438` としていた | ⭐ **実物は対象が `:2438`、`**Relations**` は `:2440`** |

⚠️ **落とした 3 編集の判定そのものは正しい。** `FR-019`（`:2599`）の STATEMENT は「構えて置いたとき…作り…持つこと」＝**置くときだけ**を縛り、置いた後に留め先・ずれ・範囲を変える規則は 表 T-023d の `GR-14`（`:2268`。`FR-016` の表）にしか無い。**変えたのは受け持ちであって、判定ではない。**

## 5. CR-149 側で直すこと（本 CR には含めない）

| 行 | 確定名 | 「正」 |
|---|---|---|
| `CM-50` | `setCommentBoxAnchor` | `FR-019` → **`FR-016`** |
| `CM-51` | `setCommentBoxBodyOffsetPx` | `FR-019` → **`FR-016`** |
| `CM-54` | `setHighlightBoxRange` | `FR-019` → **`FR-016`** |

⭐ **CR-149 は未適用なので、これは CR-149 の本文を直すだけで済む。**編集の数も表の行数も動かない（`FR-019` が 7 → 4、`FR-016` が 1 → 4。**種類の数は 39 のまま**）。
⚠️ **名簿の材料（`previous-project-result/temp/document-command-roster.md`）も同じ 3 行を持つ。** git 管理外の作業メモなので当てる対象ではないが、**直さないと次に名簿から起こしたときに誤りが戻る。**

## 6. 席と順序

| CR | 触るファイル |
|---|---|
| CR-146 | `05-07-design.md`（表 T-064）／ `_assets/tbl-glossary.md`（表 T-107）|
| CR-147 | `05-07-design.md`（Chapter 6.2）|
| CR-148 | `01-04-requirements.md`（`FR-021` `:2745` `:2756` ／ `FR-080` `:3006` 〜 `:3014`）|
| CR-149 | `_assets/tbl-glossary.md:335` ／ `05-07-design.md:338` ／ `01-04-requirements.md:195` ／ `docs/review/duplication-baseline.txt` |
| **CR-150** | **`01-04-requirements.md` のみ**（`:943` `:1395` `:1520` `:1548` `:2292` `:2438` `:2523`）|

**本文では 1 行も競合しない。順序は問わない。**
⛔ **5 本とも `A-appendix.md` の変更履歴の末尾という同じ席を取り合う。当て終えてから 1 回で書くこと。** 本 CR のぶんとして書く内容は次のとおりである。

> ⭐ **命令が従う行を要求側に立てた**（CR-150）—— 名簿（表 T-108）の 71 件を 1 件ずつ「この命令が書く列に従う行が在るか」で掃いたところ、**含意はあるのに行が無い箇所**が出た。`FR-008` に**担当者と割当を作るときの種類・採番・重複禁止**を、`FR-001` に**作ったタスクが WBS の親を持たないことと、その場で作る行の名前の導出元**を、`FR-098` に**同じ操作子でピン止めを外すこと**を、`FR-051` に**表示位置（`S-77` / `S-78`）の書き手**を置いた。表 T-016 の `PR-14` は**フェードの適用形状**を表 T-012a の `FD-5` へ、表 T-027 の `UN-14` は**行の色と高さ**を対象に加え、`FR-088` は**2 つの曜日の符号が別であること**と正の在り処を指すようにした。⚠️ **値・丸め・符号そのものは書かない** —— 境界値は Chapter 6.1 が、交換相手の符号は Chapter 6.2 が指す公式 XSD が持つ。表・行・図・`UID` は 1 つも動かない

⚠️ **版数は当てる順が決まってから振ること。本 CR は版数を主張しない。**

### ⚠️ 重複検出は CR-149 と同時に測り直す

本 CR は**新しい規範語を 7 つ足す**ので、検査 11 の `A` / `groups` / `pairs` が改定前の `A=16 (new 0)` / `groups=48` / `pairs=119` から動きうる。**CR-149 の編集④（`duplication-baseline.txt` への 1 行）と同じ回で測ること。**

## 7. 数の予測

| | 改定前 | 改定後 |
|---|---|---|
| `tables` | 114 | **114**（新しい表を作らない）|
| `figures` | 10 | **10** |
| `rows` | 1338 | **1338**（④⑤は既存のセルの中だけを直す）|
| `uids` | 140 | **140**（要求を 1 本も足さない）|
| 検査 11 | `A=16 (new 0)` `groups=48` `pairs=119` | ⚠️ **要実測**（規範語を 7 つ足す）|
| 検査 12 | 0 件 | **0 件**（`_assets` を 1 文字も触らない）|
| `audit-ch5.py` | PASS | **要実測**（Chapter 5 を触らないので素通りするはず）|
| `check-render.py` | PASS | ⚠️ **要実測** —— **`。` の直後に閉じる `**` を新設していない**ことは確かめたが、`strictdoc export` で実物を見るまで確定しない |
| 生成物 | — | **再生成は要らない** —— `erd.json`・`components.json`・`_assets/source/` を 1 つも触らない |

⚠️ **本 CR は複製での実測を済ませていない。** CR-146 / CR-148 / CR-149 は写しで実測済みだが、**本 CR は当てたあとに 16 検査・`audit-ch5.py`・`strictdoc export` → `check-render.py` を必ず走らせること。**
