# CR-439 — 画面の面を、描く UI パーツごとに割る

> ⭐ **状態: 当てた（2026-09-22、`a01ef3fb` の上）。**  
> 起草も同じ日である。  
> 当てた体が測った、本書と実物の違いは 15 節にある。
> 読んだ木: `9b8a22b7`（ブランチ `claude/brave-sinoussi-1d3590`。  
> `CR-438` の着地 `223a5a5b` と `CR-500` の波 A を含む）。
> **行番号・行数・分岐数・共変の数・読み手の数は、すべてこの木で自分で測ったものである。**
> `CR-432`・`CR-438`・計画の記録の数は 1 つも写していない（写した数は、出どころを添えて「引いた」と書く）。
>
> **開くもの**: 計画の段 6（`src/framework/dom-screen-surface/dom-screen-surface.ts` を UI パーツごとに割る）の入口。
> `JDG-48` により、`src/` に新しいファイルを置くには先に 表 T-075 がその行を持たねばならない。
> **本書は割らない。  
> 割ることを合法にし、どの形で割るかを定める。**
>
> **⛔ 本書が絶対にしないこと**: `.claude/skills/spec-graph-check/function-size-baseline.txt` の編集と、`docs/spec/` の編集。
> 基準線と表の行は、分割そのもののコミットが書き換える（7 節・8 節）。
>
> **正本**: `docs/development-records/refactor-plan-report-2026-09-13.md` の状態表の段 6 の行（`:97`）、同書の「段 6 画面に描く係」（`:299`）、試験の計画の段 6 の行（`:326`）、欠陥の置き場の表（`:344`）。
> **型**: `CR-438`（節の立て方と 15 節の教訓）と `CR-432`（とくに 4.1 の S1〜S5 と共変の数え方）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⚠️ **1 つも前へ進めない。  
利用者が画面で気づく変化は 0 である。**  
表 T-054 の `CH-1` 〜 `CH-6` のどれにも、`GL-001` 〜 `GL-008` のどれにも、本書は直に結ばない。  
本書は作り方の側の手入れである（`CR-432` の 0 節 ① と同じ答え）。

⭐ 間接の結びは 2 つある。
- このファイルは誕生でなくコードを変えたコミットを **98 件**受けている（1 節 (f)）。  
その 1 件ごとに 3,331 行の 1 ファイルを開かせてきたことが、直しの値段である。
- 並行セッションの `CR-500` の波 B（欄の編集の始まりと終わりを知らせる）は、このファイルの閉包の中に書く予定である。  
本書のあとでは、それが 416 行の 1 つの兄弟（`field-editing.ts`）に収まる（4.6）。

### ② レビュー観点のどの条項を当て、何が出たか

- `R2.2`（設計 ・ **MUST** ・ 1 ユニットが単一の変更理由）—— `docs/development-rules/07-review-standards.md:574`。
  ⛔ **違反している。**
  関数の上の `// see` の注記は 116 行あり、名指す要求は 19、行 ID と表は合わせて 93 である（1 節 (h)）。
  表 T-075 の `UF-71` の欄が名指すのは `FR-101` の 1 件だけである。
- `R2.2b`（設計 ・ **MUST** ・ 上位の責務は要約であり総和ではない）—— 同書 `:576`。
  ⛔ `domScreenSurface` は 871 行・分岐 1 の閉包であり、内側に 56 の関数と 23 の `let` を抱える（1 節 (i)）。  
  処理ではなく入れ物である（`docs/development-records/handoff.md` の 1.1 と同じ読み）。
  ⇒ 閉包の中の塊を、描く UI パーツと欄の編集の兄弟へ出す（4.3）。  
  残る閉包は 339 行・`let` 4 である —— まだ入れ物であり、段 7 が状態機械の軸で縮める。
- `R2.2a`（設計 ・ **MUST** ・ 責務文の 5 段の試験）—— 同書 `:104` 〜 `:122` の手順、`:575` の条。
  4.2 で、提案する 11 ユニットすべてに当てた。  
  畳んだ句と (a)(b)(c) を各行に書いた。
- 表 T-276 の `UD-1`（変更の理由）—— ⭐ **本書の芯である。**
  描く UI パーツごとに、縛る要求と表が互いに素である（4.1）。
- 表 T-276 の `UD-2`（純粋性）—— 当てて、割る理由にならないと判じた。
  表 T-060 の `LY-5` は `Framework` の層に `semi-pure-b` と `non-pure` しか与えない。  
  このファイルの `@purity pure` の関数（65）は、どれも同じファイルの描き手だけが呼ぶ補助であり、層の上で「純粋な側」を成さない。  
  ⇒ 11 ユニットとも `non-pure` とする（表 T-075 の `UF-71` のいまの値と同じ）。
- 表 T-276 の `UD-4`（割り方はコードの位置で裏づける）—— 4.2 のすべての行に `file:line` を書いた。
  3,331 行のすべてが、ちょうど 1 つのユニットに落ちることをスクリプトで確かめた（重なり 0、漏れ 0）。
- 表 T-276 の `UD-5`（迷いの試験）—— 4.2 の責務文で当てた。  
⭐ 欄の編集を別の兄弟にしたのは、この試験による（4.1 の判断の 3）。
- `R2.9`（要らないうちは作らない）—— 6 節。  
最小は `screen-frame-drawing.ts` の 50 行である。
- `R2`（名前を足すときは必ず引く）—— ⭐ **新しい接頭辞・新しい表・新しい図は 0 である。**
  足すのは 表 T-075 の行（`UF-103` 〜 `UF-112`）と 表 T-063 の行（`UT-12`）だけである。  
  与えられた `T-278`・`T-279` と `F-030` 〜 `F-034` は使わない。

### ③ 利用者に問わずに決めたこと

⭐ 下の 10 個は根拠を添えて利用者に問わずに決めた（あとから覆せる）。

1. ⚠️ **`DFC-566`（色の欄が 表 T-017 のパレットでなく宿主の色選びを出す）を段 6 の入口の条件にしない** —— 前に立つ者の決定を、本書が書き留める。
   計画の状態表（`:97`）と欠陥の置き場の表（`:344`）は「裁定待ちの 2 行は、裁定を受けて段 6 の前に直す」と書き、利用者はその案 d を `JDG-57`（逐語「d で進めろ。」）で選んだ。  
   ⇒ **本書の決定はその字面から外れる。**
   外れてよいとした根拠: ① `DFC-566` は `PND-494`（分類 D、未裁定、「推奨を出さない」）を待っており、待つ終わりが無い ② 純粋な分割は振る舞いを変えないので、欠陥はコードと一緒に兄弟へ動くだけである（色の操作子は `properties-panel-drawing.ts` へ行く）③ `JDG-57` が先に直す理由に挙げた「直しの確認の試験が安全網を兼ねる」は、本書では照合器（8.3 の MUST (c)）が負う ④ もう 1 つの理由「台帳の行は今のファイルと行で書かれている」は、`DFC-566` ではもう崩れている —— 台帳の `:1226-1234` はいま `:1359-1368` にある（8.4）。
   ⛔ 前に立つ者は、分割の波のあとで `DFC-566` の所在を `properties-panel-drawing.ts` へ書き直すこと（MUST —— 8.4）。
   ⚠️ 覆すなら利用者である。  
   覆れば段 6 は `PND-494` の裁定まで止まる。
2. **兄弟が共有する語彙 —— 色（表 T-236）・入口の寸法と見た目・`STYLE` の表・UI パーツの名 —— は入口に残した**（4.1）。
   別の兄弟（語彙の 1 ファイルと色の 1 ファイル）として出す案は、S2 が 7 つの組で拒んだ（0.60 〜 1.00）。  
   `CR-432` の S4 と、`CR-438` の入口の前例に当たる。
   ⇒ ⚠️ 入口は **1,107 行**になる（4.4 に内訳）。
3. **欄の編集（焦点・`Esc`・`Enter`・欄の外の押し・確定した値）を、UI パーツではない 1 つの兄弟 `field-editing.ts` にした**（4.1）—— 計画の「`ScreenRenderer` の 10 ユニットと 1 対 1」から外れる唯一の所である（4.5）。
   縛るのは 表 T-028 の `IN-4`・`IN-5a`・`IN-6` と `SK-19` と 表 T-065 の `IF-9` であり、どの UI パーツの記述とも変更の理由が違う。
4. **パネルの置き場（`placePanels`）は入口に残した** —— 画面の枠の兄弟へ出す案は、欄の編集と **0.60（5 件中 3 件）** で拒まれた（4.1）。
5. **`FR-101` の負う要求を `UF-71` から `UF-104`（`app-header-drawing.ts`）へ移し、`UF-71` の責務の欄には `FR-101` を満たす兄弟を名指す文を残す**（4.7）。
   試験 1 本が 表 T-075 の `UF-71` の行に `FR-101` の字があることを読む（5.3）。  
   その試験の意図（描く側が負い、記述の側 `UF-62` が負わない）は割ったあとも真であり、試験は無変更で緑のままである。
6. **兄弟のファイル名に `-drawing` を付けた** —— `audit-ch5.py:196` が「ユニットのファイル名は重ならない」を検査しており、`ScreenRenderer` の兄弟と同じ名（`row-title-panel.ts` ほか）は置けない。  
`tools/generate_unit_tree.py:136` もファイル名で引く。
7. **閉包の中の塊は、引数で受ける関数か、自分の `let` を持つ小さな工場として出す**（4.3）—— 閉包の外へ出すには、閉包の値を引数で渡すしかない。
   前例 —— `CR-438` の 15 節の 3（`commandFromEntry` の腕の本体を兄弟の関数へ出した）。
8. **割る体は同時に 1 体だけとした**（8 節）—— 10 の兄弟はすべて 1 本のファイルから出るので、取り込みの段が必ず重なる。
9. **行番号を引く記録のうち、書き直すのは生きている台帳と、着地していない変更要求だけとした**（8.4）—— `CR-438` の 0 節 ③ の 9 と同じ理由。
10. **表 T-063 の新しい行を `UT-12` にした** —— 2 つのセッションの取り決め（`docs/development-records/handoff.md:46`「UT は UT-12 以降を先に宣言してから」）。  
`UT-12` は `docs/spec` と `change-request/` に 0 件である（2026-09-22、`9b8a22b7` で `grep`）。  
⚠️ 前に立つ者は、書く前に相手へ宣言すること。

---

## 1. 読んで確かめた事実（`9b8a22b7` で測った）

| # | 事実 | 測り方 |
| --- | --- | --- |
| (a) | ファイルは **3,331 行**である。<br>関数は 186（名のある関数 149、無名 37）。<br>帯（50 行 ／ 15 分岐）を超える関数は **6** | `.claude/skills/spec-graph-check/function-size.mjs` に 1 ファイルを渡した出力 |
| (b) | 基準線の `HELD` 行はこのファイルについて **6 行**あり（`function-size-baseline.txt` の 69〜74 行目）、6 つの値はすべて (a) の実測と一致した | 基準線と (a) の突き合わせ |
| (c) | 6 関数の超過の和は **1,144 行 ／ 62 分岐**。<br>基準線の総量（`excess-lines=7868` ／ `excess-branches=777`）の **14.5% ／ 8.0%** である | (b) の値から `max(0, 行-50)` と `max(0, 分岐-15)` を足した |
| (d) | 公開エントリが `export` する名前は **5**（`ScreenTheme`・`pageGroundStyle`・`ScreenSurfaceWiring`・`domScreenSurface`・`NOT_STORED_ICON_SIZES`）。<br>表 T-064 の `PI-38` が名指すのは「`ScreenSurface` の実装 1 つ」・`pageGroundStyle`・`ScreenTheme` | `grep '^export'` と `PI-38` の行（`05-07-design.md:557`） |
| (e) | 表 T-075 の `UF-71` の「負う要求」は `FR-101`（`OW-2`）の 1 件だけである | `05-07-design.md:467` |
| (f) | ファイルの履歴は **114 コミット**。<br>誕生は 2（`1d0412b1` 足場、`4715c316` 中身を書いた手番 —— コードの行 643 のうち 643 を足した）。<br>誕生でなくコードを変えたものは **98** | 4.0 の数え方で、1 行目から末尾までを 1 つの範囲にして数えた |
| (g) | 公開エントリを取り込むファイルは **44**（`src/` 1 ・ `tests/` 43）。<br>名前空間の取り込みと動的な取り込みは 0 件 | 5.2 |
| (h) | 関数の上の `// see` の注記は **116 行**。<br>名指す要求は **19**（`FR-006`・`FR-020`・`FR-023`・`FR-029`・`FR-031`・`FR-032`・`FR-035`・`FR-036`・`FR-038`・`FR-039`・`FR-041`・`FR-051`・`FR-053`・`FR-066`・`FR-072`・`FR-076`・`FR-098`・`FR-099`・`FR-101`）、要求と行 ID と表を合わせて **93** | `// see` の行から ID を抜き出して重ねを除いた |
| (i) | `domScreenSurface` は `:2298-3168` の **871 行・分岐 1**。<br>内側の関数 **56**（名のある関数 35）、`let` **23** | (a) の出力のうち開始行が閉包の中のものを数えた。<br>`let` は 2 字下げの行頭で数えた |
| (j) | ファイル全体のコメント密度は **9.9%**（コード 2,461 行、コメント 269 行） | `check-comment-rules.py` の `measure_text` をこのファイルに掛けた |
| (k) | 計画の記録は「`dom-screen-surface.ts` を import する試験は 28 本」と書いている（`:292`）。<br>今日は **43 本**である | (g) |

**関数の大きさ（帯を超える 6 と、帯の近くの関数）**

| 関数 | `file:line` | 行 | 分岐 |
| --- | --- | --- | --- |
| `domScreenSurface`（閉包） | `:2298-3168` | 871 | 1 |
| `modalElement` | `:1949-2120` | 172 | 33 |
| `rowTitleElement` | `:1092-1239` | 148 | 16 |
| `showScreenView` | `:2539-2671` | 133 | 37 |
| `controlElement` | `:1484-1545` | 62 | 20 |
| `readScreenPartAt` | `:3073-3130` | 58 | 31 |
| `paletteElement` | `:1679-1725` | 47 | 7 |
| `confirmationElement` | `:2203-2247` | 45 | 2 |
| `watchDocumentTitleField` ／ `watchWatermarkUnlock` | `:2947-2989` ／ `:3003-3045` | 43 ／ 43 | 0 ／ 0 |

### ⛔ 渡された主張のうち、この木で成り立たなかったもの

| 渡された主張 | 測った結果 |
| --- | --- |
| 「ファイルは約 3,331 行」 | 3,331 行 —— 成り立った |
| 「`CR-500` の波 A が 表 T-065 の `IF-9` を、真偽 `hasUnsettledTextEntry` から、欄の行 ID つきの始まりと終わりの知らせへ書き換えた」 | 成り立った（`05-07-design.md:579`、コミット `9b8a22b7`）。<br>コードはまだ真偽である（`src/adapter/screen-renderer/screen-surface.ts:58-59`、このファイルの `:3060-3062`） |
| 「波 B では、面が `focusin` ・ `focusout` で知らせを送る」 | ⚠️ 半分だけ成り立った。<br>`CR-500:231` の波 B は、知らせを面の中に**溜め**、読む側が「1 度読むと消える形で」取る（`readFieldCommit` と同じ形）。<br>面から押し出すのではない。<br>置き場の結論（4.6）は変わらない |
| 「約 28 本の試験がこのファイルを取り込む」 | 43 本（1 節 (k)） |
| 「`DFC-566` の色の操作子は `type="color"`」 | 成り立った（`:1359-1368` の `CONTROL_INPUT_TYPE` の `color: 'color'`、`:1509-1510` で `type` になる）。<br>⚠️ 台帳の所在 `:1226-1234` と `:1293-1294` は、既にこの木の行ではない（8.4） |
| 「並行セッションの持ち場は `CR-450..530`」 | ⚠️ 狭い。<br>同じセッションの `CR-436`（画面の値）と `CR-440`（通知。<br>波 A を当て、波 B を残す —— `change-request/CR-440-notices-move-into-the-state-machine.md:3`）もある。<br>8.5 に足した |

### ⛔ 「覆う条項が無い」と書く前に打った 3 つ

| 打った手 | 結果 |
| --- | --- |
| `rulings.md` を `段 6`・`DFC-566`・`PND-494`・`UF-71`・`UD-2`・`dom-screen-surface`・`JDG-118`・`JDG-54` で `grep` | `段 6` は 2 件 —— `:480`（`JDG-283`。<br>パネルを出していないあいだ境界の帯を描かない直しが「段 6 を待つ波」）と `:485`（`CR-436` の波 B2 は段 5・段 6 の後）。<br>`DFC-566`・`PND-494`・`UF-71`・`UD-2` は 0 件。<br>`dom-screen-surface` は 3 件（`JDG-81`・`JDG-94`・`JDG-204`。<br>どれも描き方の直しの裁定で、割り方には及ばない） |
| `impact.py UF-71` ／ `FR-101` ／ `UD-2` ／ `IF-9` ／ `UF-62` ／ `PI-38` | `UF-71`: 参照 1 箇所（`05-07-design.md:458`、`UF-62` の責務の文）。<br>`FR-101`: 要求 1 件 ／ 参照 10 箇所。<br>`UD-2`・`PI-38`: 参照 0 箇所。<br>`IF-9`: 要求 1 件 ／ 参照 5 箇所。<br>`UF-62`: 参照 1 箇所（`:467`、`UF-71` の責務の文） |
| 別の道で届くか | ⭐ 届いた —— 割る基準は 表 T-276 が、要求の区分は 表 T-277 が、割り方の手順と共変の数え方は `CR-432` の 4.1 が、層の純粋性は 表 T-060 の `LY-5` が既に持つ。<br>本書が足す規則は 0 である |

---

## 2. グラフ（測った先）

⭐ **コンポーネントも辺も図も動かない。**  
割るのは `DomScreenSurface`（`CP-38`）の内側であり、10 の兄弟はすべて同じフォルダに置く。  
`docs/spec/_source/components.json` と 図 F-013 は無変更である。

| 検査 | 何が動くか |
| --- | --- |
| 18（`generate_unit_tree.py --check`） | `src/` のファイルの集合が 表 T-075 と一致しなくなるので、表の行と `src/` を同じコミットで動かす |
| 19・59 | 兄弟が外から取り込むのは、入口がいま取り込む 2 つの公開エントリ（`screen-renderer`・`screen-regions`）だけである。<br>辺はコンポーネントで数えるので動かない |
| 26b（`check-published-members.py`） | 表 T-064 は 1 行も動かない。<br>⚠️ 外へ渡る名の組（コンポーネント ｜ 名）は、兄弟がいま入口の取り込む名だけを取り込む限り増えない（5.4 の MUST） |
| 30（`check-generated-constants.py`） | 兄弟が読む生成定数が `PUBLISHED_READ_BY_SRC` に無いと赤（5.5） |
| 45（同じ式の繰り返し） | 入口が新たに `export` を付けると、別のコンポーネントの同じ字面に届くことがある（`handoff.md` の 2.11 ⑱）。<br>各波のあとに回す |
| 55（コメント） | 入口と `field-editing.ts` が 10% を超える見込み（6 節）。<br>各波のあとに回す |
| 60（`check-function-size.py`） | 7 節 |
| `audit-ch5.py` | 表 T-063 の行の数を `11` と持つ（`:83`）。<br>`UT-12` を足すと `12` に直す。<br>`SU-3` の文（`:192`）とファイル名の重なり（`:196`）も読む |

---

## 3. 基準 —— ⭐ 仕様は既に持っている

割る基準は 表 T-276（`UD-1` 〜 `UD-5`）が持つ。  
`CR-432` が置き、`05-07-design.md:273` の文が指している。  
⇒ **本書は基準を 1 行も足さない。  
表も新設しない。**

⭐ 本書が S1 で使う出どころは 2 つである。
- 計画の記録の「`ScreenRenderer` の 10 ユニットと同じ切り方で割る。記述を作る側と描く側が 1 対 1 になる」（`:299`）—— 表 T-075 の `UF-60` 〜 `UF-69` が、UI パーツごとの記述の側の継ぎ目をすでに持つ。
- 各 UI パーツを描く関数の上の `// see` が名指す要求と表（1 節 (h)）—— 表 T-276 の `UD-1` は「変更の理由」を「要求 ID・表・裁定」と定義している（`CR-438` の 0 節 ③ の 1 と同じ読み）。

---

## 4. 割り方

⛔ **`UD-4` に従い、提案する全ユニットに `file:line` を添える。  
元のファイルのすべての行が、どこかのユニットに 1 度だけ落ちる**（スクリプトで確かめた。  
重なり 0、漏れ 0、合計 3,331）。

### 4.0 手順と数え方 —— `CR-432` の 4.1 と `CR-438` の 4.0 のとおり

S1（立てる）・S2（拒む）・S3（絡み）・S4（残り）・S5（確かめる）の段、共変の数え方（`git log -L`、コメントだけの変更と誕生を除く、閾値は **0.5 を超える**、小さい側の下限 **5 件**、残る元のファイルに対する共変は拒む理由にしない）は `CR-432` の 4.1 のとおりである。

- **範囲は 1 つずつ `git log -L` に掛けた**（`CR-438` の 4.0 の癖 —— 1 つの `git log` に `-L` を並べると表明で落ちる）。  
落ちた範囲は 0 である（終了コードと stderr を読んで確かめた）。
- **コメントだけの変更**: 範囲の差分で、コメント行・空行・行末のコメントを除いたコードの行の多重集合が、消えた側と足された側で等しいコミットは、その範囲について数えない。
- **誕生の判じ方**: そのコミットが足したコードの行が、その時点のファイルのコードの行の半分以上であるもの。  
`1d0412b1`（1 行のうち 1）と `4715c316`（643 行のうち 643）の 2 件が当たった。
- ⚠️ `CR-432` の 4.1 が書いた癖（範囲の隣にあってのちに消えた関数の差分を拾う）は、本書でも差分を 1 件ずつ読んでは確かめていない。

### 4.1 最初の案と、S2 が拒んだ継ぎ目

S1 で、`ScreenRenderer` の 9 つの UI パーツの記述（`UF-61` 〜 `UF-69`）に 1 対 1 の描き手 9 つ、欄の編集 1 つ、兄弟が共有する語彙 1 つ（UI パーツの名・入口の寸法と見た目・`STYLE` の表）、色 1 つ（表 T-236 の `PAINT_ROW` と主題）、残る入口の **13 ユニット**の案を立てた。

S2 が拒んだのは次の 7 つである（どれも語彙か色を含む）。

| 組 | 数 | 読み |
| --- | --- | --- |
| 語彙 と 通知 | **1.00（7 件中 7 件）** | ⛔ 拒む |
| 語彙 と パレット | **0.89（9 件中 8 件）** | ⛔ 拒む |
| 語彙 と ツールチップ | **0.75（8 件中 6 件）** | ⛔ 拒む |
| 語彙 と ヘッダ | **0.71（7 件中 5 件）** | ⛔ 拒む |
| 語彙 と 色 | **0.70（10 件中 7 件）** | ⛔ 拒む |
| 語彙 と 重ねて開く面 | **0.62（21 件中 13 件）** | ⛔ 拒む |
| 色 と 行見出し | **0.60（10 件中 6 件）** | ⛔ 拒む |

⭐ **語彙の中で共変を運んでいるのは `STYLE` の表（`:437-567`）である** —— `STYLE` だけを 1 つの範囲にして数えると、通知と 1.00（7 件中 7 件）、パレットと 0.78（9 件中 7 件）、ヘッダと 0.71（7 件中 5 件）、重ねて開く面と 0.52（21 件中 11 件）であった。  
UI パーツを 1 つ足すと、その見た目の項が `STYLE` に 1 つ増える —— `CR-432` の 4.1 が「兄弟はどれも元のファイルに自分を登録する」と書いた台帳と同じ形である。  
色の `PAINT_ROW` も、行の状態の地の色を足すたびに項が増える。  
⇒ ⭐ **語彙と色を入口に残した。**  
 残す形では、兄弟どうしで拒まれる組は 0 になった（4.3）。

⛔ **語彙を兄弟に残す道は、もう 1 つの理由でも採れない** —— モジュールの評価の順である。  
入口は兄弟を取り込み、兄弟は入口から補助を取り込むので、同じフォルダの中で取り込みが輪になる（前例 `CR-438`・`CR-432` の 4.1 の「輸入の輪」）。  
輪では、入口に入ったとき兄弟が先に評価される。  
⇒ 兄弟がモジュールの頂上（関数の外）で入口の名を読むと、初期化前の参照になる。  
いまの頂上の定数のうち、ほかの塊の名を読むのは `STYLE`・`STOPPING_BOX`・`ENTRANCE_STATE_FILL`（`PAINT` を読む）、`ROW_CONTROL_SHOWN_CSS`・`PALETTE_FAINT_CSS`（`UNIT_ROW`・`ROLE` を読む）、`SCALE_MESSAGE_STYLE`（`PAINT`）である。  
⭐ 本書の割り方では、この 6 つはすべて入口に居る。  
兄弟の頂上の定数は、どれも自分の中の字と数だけを読む（測った）。  
入口の頂上が兄弟の名を読む向き（`ROW_CONTROL_SHOWN_CSS` が `ROW_CONTROL_GROUND_MARK`・`ADD_CHILD_ROW_ENTRY`・`DELETE_ROW_ENTRY` を読む）は、兄弟が先に評価されるので安全である。

**そのほかの判断**

- **パネルの置き場（`placePanels`、`:2492-2518`）は入口に残した** —— 画面の枠の兄弟へ出した案では、枠と欄の編集が **0.60（5 件中 3 件）** で拒まれた。  
共に変えた 3 件は `273564c5`（プロパティパネルを編集できるようにした）・`1ef6fab3`・`50b22431` である。  
パネルを置く手と欄の折り返しの伸び（`growWrappingFields`）が同じ変更で動いてきた。  
  入口に残すと、枠の兄弟は 2 件しかなく、どの組も証拠なしになる。
- **欄の編集を 1 つの兄弟にした** —— 縛るのは `IN-4`（`Esc` の段）・`IN-5a`（入力中か）・`IN-6`（欄の外の押しで確定）・`SK-19`（`Enter`）・`IF-9`（確定した値を行 ID とともに返す）であり、プロパティパネルの描き方（表 T-016・`FR-006`）とは別の出どころである。  
  UD-5 —— 「文書名の欄で `Esc` を 2 度押すと…」の欠陥は 1 つのファイル（`field-editing.ts`）で答えられ、「欄の枠が太い」の欠陥は別の 1 つ（`properties-panel-drawing.ts`）で答えられる。  
  共変はプロパティパネルと **0.25（12 件中 3 件）**、行見出しと **0.43（14 件中 6 件）** で、どちらも拒まない。
- **欄の編集の中の 3 つの欄（パネル・文書名 `U-27`・透かし解除の答え `U-60`）は割らない** —— S3 による。  
欄の外の押し（`settleOnPressOutside`、`:2852-2875`）の本体が 3 つの欄を順に確定させ、立っている通知が `Enter` と `Esc` を先に取る判定（`isPressTakenByStandingNotice`、`:2752-2755`）を 3 つとも読む。  
共変は、文書名 1 件・透かし解除 2 件で証拠なしである。
- **読み返しの印（`readScreenPartAt` が読む属性の名 5 つ）は入口に置いた** —— `NOTICE_DISMISS_KEY_ATTRIBUTE`・`CONFIRMATION_ANSWER_ATTRIBUTE`・`IMPORT_REPORT_DISMISS_ATTRIBUTE`・`SCROLLBAR_AXIS_ATTRIBUTE`・`ROW_GRAB_STRIP_MARK`。  
読む側（入口の `readScreenPartAt`、`:3073-3130`）と書く側（兄弟）が同じ 1 つの綴りを読む。  
`CR-438` が振り分けの台帳を入口に残したのと同じ形である。
- **2 つの兄弟から呼ばれる補助は、規則の持ち主の兄弟に置いた** —— 確認の答えの入口（`confirmationAnswerElement`、`NT-7`）は通知へ、群の区切り線（`paletteGroupRuleStyle`、`S-143`）はパレットへ、欄の描き方（`fieldElement`、表 T-016）はプロパティパネルへ、欄の名乗り（`CONTROL_KEYS`・`TYPED_CONTROLS`・`TextEntryControl`）は欄の編集へ、パネルの境の線（`panelEdge`）は画面の枠へ置いた。  
持ち主の無い補助（語彙）は入口に置いた（S4）。

### 4.2 最終の割り方 —— 入口 1 ＋ 兄弟 10

⭐ 表 T-075 の行は `UF-71` の直後、`UF-84`（`advance-screen-session.ts`）の前に、下の順で置く。  
⭐ 純粋性はすべて `non-pure` である（0 節 ② の `UD-2`）。

| 行 ID | ユニット | 責務文（S5。<br>畳んだ句と、その根拠） | もとの `file:line` | 行数 |
| --- | --- | --- | --- | --- |
| `UF-71`（残る。<br>公開エントリ） | `dom-screen-surface.ts` | 「UI パーツの層を重ね、記述の変わったパーツだけを兄弟に描かせ、画面の点がどのパーツのどの入口の上かを答える（`IF-9`）」。<br>「層を重ねる順（通知が確認を隠さない）」は (c)。<br>「ヘッダの高さを測って知らせ、パネルと通知の置き場を決める（`FR-051`・`PND-155`）」は層を重ねる 1 回の段なので (a)。<br>「表示の倍率の告げ（`SE-4`・`SE-5`）を描く」は描く物の 1 つなので (b) —— 記述の側でも `screen-renderer.ts`（`UF-60`）が画面全体の値として運ぶ。<br>「兄弟が共有する語彙 —— 色（表 T-236・`FR-041`）・入口の寸法と見た目（`FR-029`・表 T-237）・`STYLE` の表・UI パーツの名 —— を持つ」は継ぎ目の宣言なので (c) | `:1-64` `:86-89` `:96-199` `:294-295` `:308-335` `:363-400` `:425-623` `:632-686` `:702-714` `:748-831` `:2279-2290` `:2296-2317` `:2319-2365` `:2395-2405` `:2492-2518` `:2537-2672` `:3071-3331` | 1,107 |
| `UF-103` | `screen-frame-drawing.ts` | 「`Panel Divider` の掴み帯と線、`Scrollbars` の溝とつまみを、記述の矩形のとおりに描く（`U-21`・`U-24`・`SC-4`・`GR-22`）」。<br>「パネルの境の線を記述から引く（`panelEdge`）」は (c) —— 描いた線の在りかを問う面 | `:692-701` `:721-729` `:878-908` | 50 |
| `UF-104` | `app-header-drawing.ts` | 「`App Header` の中身 —— 文書名・ファイルの名と保存の時刻・ヘッダの入口 —— を描く（`U-31`）」。<br>列挙は (b)。<br>「名前を時刻の上に置く（`FR-101`）」「時刻を土地の時刻で綴る（`PND-325`）」「字の大きさと左の余白を `EP-1` の行から読む」は (c)。<br>「表示言語の入口に言語の略号を添える（`IC-21`）」は入口の 1 つなので (b) | `:65-66` `:411-424` `:730-747` `:832-877` | 80 |
| `UF-105` | `row-title-panel-drawing.ts` | 「`Row Title Tree` の行を、表 T-051 の操作子・掴み代・畳んだ数とともに描く（`U-22`・`FR-098`）」。<br>「パネルの角の 4 つの入口（`HF-10`・`HF-12`・`HF-16`・`HF-17`）」は (b)。<br>「描いた操作子の高さを測って知らせる（`LF-3`・`HF-19`）」は描いた物を測る段なので (a) | `:72-85` `:624-631` `:909-1344` `:2366-2368` `:2406-2447` | 503 |
| `UF-106` | `properties-panel-drawing.ts` | 「`Properties Panel` の欄を、表 T-016 の入力の型ごとの操作子として描く（`U-25`・`FR-006`・`FR-072`）」。<br>「長い値を折り返して高さを伸ばす（`FR-006`）」「出口の入口を最初の欄の行に置く（`FR-072`）」は (c)。<br>「描いた操作子に行 ID を名乗らせる」は継ぎ目の宣言なので (c)（名乗りの表は `UF-107` が持つ） | `:200-264` `:336-362` `:1348-1372` `:1377-1462` `:1480-1655` | 379 |
| `UF-107` | `field-editing.ts` | 「文字入力の欄の編集 —— 焦点を持つ・`Esc` で打つ前へ戻す・`Enter` と欄の外の押しで確定する —— を `IN-4`・`IN-6`・`SK-19` に従って扱い、確定した値を欄の行 ID とともに返す（`IF-9`）」。<br>欄の列挙（パネルの欄・文書名 `U-27`・透かし解除の答え `U-60`）は (b)。<br>「入力中かを答える（`IN-5a`）」「立っている通知が `Enter` と `Esc` を先に取る（`NT-8`）」は (c)。<br>「焦点を求められた欄に置く（`MK-13`）」は編集を始める段なので (a) | `:1345-1347` `:1373-1376` `:1463-1479` `:2318` `:2371-2372` `:2682-3070` | 416 |
| `UF-108` | `command-palette-drawing.ts` | 「`Command Palette` —— 掴み帯・最小化・群と区切り線 —— を描く（`U-26`・`FR-053`・`GR-19`）」。<br>列挙は (b)。<br>「区切り線の太さと間を `S-143` から読む」は (c) | `:67-68` `:401-410` `:687-691` `:1656-1726` | 88 |
| `UF-109` | `open-modals-drawing.ts` | 「重ねて開く面（表 T-028 の `IN-4` の定義）を、面の種類ごとに描く」。<br>面の列挙 —— ヘルプ（`FR-036`・表 T-256）・資源の名簿（`FR-099`・表 T-257）・書き出しの形式・取り込みの報告（`FR-023`・`U-62`）・設定の欄・曜日・透かし解除の問い（`FR-020`）ほか —— は (b)。<br>「名簿の横の送りを `Ctrl` ＋ `Shift` ＋ ホイールで受ける（`RR-3`・`MK-5`）」は名簿の 1 項なので (b) | `:69-71` `:92-95` `:271-293` `:296-307` `:1727-2152` | 468 |
| `UF-110` | `notices-drawing.ts` | 「通知と確認を、表 T-037 の作法で描く（`FR-076`・`NT-1`・`NT-7`・`NT-8`）」。<br>「確認の一覧と答えを区切り線の上に置く（`CQ-2`・`CQ-4`・表 T-258）」は (c) | `:90-91` `:2153-2259` | 109 |
| `UF-111` | `dialogue-field-drawing.ts` | 「`Dialogue Field` の発話を描き、`Enter` で確定した発話を 1 度だけ返す（`FR-066`・`AG-11`）」。<br>「置き場（`PND-151`）」と「確定の時刻を `AT-129` で綴る」は (c) | `:715-720` `:2260-2278` `:2291-2295` `:2369-2370` `:2448-2459` `:2519-2536` `:2673-2681` | 71 |
| `UF-112` | `tooltips-drawing.ts` | 「ツールチップを、指す物の下に置いて描く（`IN-3`・`EZ-2`）」。<br>「指す物を UI パーツごとの錨の表から引く」は描く 1 回の段なので (a)。<br>「`EZ-6` の置き場（`PND-391`）」は (c) | `:265-270` `:2373-2394` `:2460-2491` | 60 |

⇒ **新しいファイル 10。**  
合計 1,107 ＋ 50 ＋ 80 ＋ 503 ＋ 379 ＋ 416 ＋ 88 ＋ 468 ＋ 109 ＋ 71 ＋ 60 ＝ 3,331 行。

⚠️ **行数は元のファイルの範囲の数である** —— 兄弟はそれぞれ見出しのコメント（5 行）と取り込みの段を持つので、どれも長くなる（`CR-438` の 15 節の 2 の教訓）。  
入口は取り込みの段が兄弟 10 の分だけ伸び、使わなくなる外の型の取り込みが縮む。

⚠️ **閉包の中の行を動かすユニットは 4 つある**（`UF-105`・`UF-107`・`UF-111`・`UF-112` の閉包の範囲）—— 4.3 の形で出す。  
S3 には当たらない —— 動かす塊はどれも閉包の中で閉じた関数か `let` であり、ほかの塊のループや分岐の本体を横切らない。

⚠️ **兄弟どうしの取り込み**（入口を経ない）: `open-modals-drawing.ts` が `properties-panel-drawing.ts`（`fieldElement`）・`notices-drawing.ts`（`confirmationAnswerElement`）・`command-palette-drawing.ts`（`paletteGroupRuleStyle`）・`field-editing.ts`（`TextEntryControl`）を、`properties-panel-drawing.ts` が `field-editing.ts`（`CONTROL_KEYS`・`TYPED_CONTROLS`・`TextEntryControl`）を、`row-title-panel-drawing.ts` と `dialogue-field-drawing.ts` が `screen-frame-drawing.ts`（`panelEdge`）を取り込む。  
兄弟どうしの取り込みに輪は無い（測った）。  
前例 —— `CR-438` の 4.2（`shortcut-keys.ts` が `zoom-and-fit.ts` を取り込む）。

### 4.3 閉包の塊の出し方 —— 形を定める

⭐ 閉包 `domScreenSurface`（`:2298-3168`）の中の塊は、閉包の値（要素・`let`・配線の関数）を読むので、そのままでは外へ出せない。  
下の形で出す。  
⛔ **この形から外れるなら、割る体は止まって報告すること**（黙って形を変えない）。

| 塊 | 出す先 | 形 | 入口の閉包に残る呼び出し |
| --- | --- | --- | --- |
| 行の操作子の高さの報せ（`:2366-2368` の 3 つの `let`、`:2406-2447` の `rowControlsMeasureKey`・`reportRowControlsHeight`） | `UF-105` | `rowControlsMeasureKey` は、閉包から読んでいた値（主題とヘッダの高さ）を引数で受ける関数にする。<br>`reportRowControlsHeight` は、3 つの `let` を自分で持つ小さな工場（木の要素・時計・高さの受け手を受ける）が返す関数にする | `:2586` の 1 行 |
| 対話欄（`:2369-2370` の 2 つの `let`、`:2448-2459` の `onEntryKeyDown` とその登録、`:2519-2536` の `placeDialogueField`、`:2673-2681` の `readDialogueInput`） | `UF-111` | `settled`・`isFieldUp` を持つ小さな工場（入力の要素・書き手・時計を受ける）。<br>発話を描いたか（`:2645-2649`）と、描いたあとに確定を捨てる（`:2670`）ことも、この工場の関数を呼ぶ形にする。<br>`placeDialogueField` は、欄の要素と記述を引数で受ける関数にする | `:2458` の登録の位置で工場を作る。<br>`:2645-2649`・`:2652`・`:2670` の呼び出し |
| ツールチップ（`:2373-2394` の錨の表、`:2460-2491` の `tooltipElement`） | `UF-112` | 錨の表（`anchorsByPart`・`anchorsOf`・`anchorFor`）は根の要素を受ける小さな工場にする。<br>`tooltipElement` は、宿主と錨を引く関数を引数で受ける関数にする | 6 つの `anchorsOf(…)` と `:2659` |
| 欄の編集（`:2318` と `:2371-2372` の `let`、`:2682-3070` のすべて） | `UF-107` | 14 の `let` を持つ 1 つの工場（宿主とプロパティパネルの要素を受ける）。<br>⭐ 工場が返すのは、入口の閉包が読む 10 の面である —— 描いた欄の表（`typedControlsByRow`、`:2613`）、欄を持っているか（`:2605`）、文書名の欄が開いているか（`:2567`）、描いたヘッダの文書名の箱と名を預かる（`:2570-2576`）、描いた面の透かし解除の欄を預かる（`:2631-2634`）、通知が立っているか（`:2540`）、`focusPropertyField`・`readWatermarkUnlockAnswer`（配線、`:3156-3158`）、`readFieldCommit`・`hasUnsettledTextEntry`（`IF-9` の答え、`:3164`・`:3166`） | `:2702` の登録の位置で工場を作る |

⛔ **宿主への呼び出しの順を変えないこと（MUST）** —— いまの閉包は、層を作ったあと、対話欄の `keydown`（`:2458`）→ パネルの `change`（`:2702`）→ パネルの `focusin` ほか 6 つ（`:2771-2831`）→ 宿主の `pointerdown`（`:2836-2838`）の順に受け手を登録する。  
対話欄の工場は `:2458` の位置で、欄の編集の工場は `:2702` の位置で作ること。  
照合器（8.3 の MUST (c)）は、宿主への呼び出しの列を比べる。

⭐ 工場の中の無名の受け手は、名が `<工場>#<n>` になる（`function-size.mjs` の名の規則）。  
どれも帯の中である（7 節）。

### 4.4 ⚠️ 残る入口の中身 —— 1,107 行

| 何 | 行 |
| --- | --- |
| 取り込み（`:1-34`） | 34 |
| 語彙 —— UI パーツの名（`ROLE`）・`UNIT_ROW`・`HOST_ENTER`・読み返しの印 5 つ・`GLYPH_TOKEN` | 42 |
| 色と書体 —— `PAINT_ROW`・`PAINT`・`stateGround`・`ScreenTheme`・`themeStyle`・`typefaceStyle`・`pageGroundStyle`（表 T-236・`FR-041`） | 95 |
| 入口の寸法と見た目、要素を作る補助 —— `chromeScaledPx` から `entryGlyphRoom` まで、`entryStyle`・`entranceStateFill`・`glyphStyle`・`made`・`part`・`fillEntry`・`commandEntry`・`anchoredEntry`・`boxStyle`・`anchorKey`・`appendAssignment` | 232 |
| `STYLE` の表（`FILE_STATUS_TEXT_SCALE`・`STOPPING_BOX` を含む、`:425-568`） | 144 |
| 根の重ねる規則 —— `ROW_CONTROL_SHOWN_CSS`・`PALETTE_FAINT_CSS`・`entranceHoverGroundCss`・`hoverCss`（`:636-674`） | 39 |
| `described` ・ `ScreenSurfaceWiring` | 17 |
| 閉包の残り —— 層を作って重ねる・`reportHeaderHeight`・`placePanels`・`showScreenView`・`readScreenPartAt`・`showScaleMessage`・返す面 | 342 |
| 表示の倍率の告げの見た目（`:3170-3185`） | 16 |
| 生成された定数の塊（`:3186-3331`） | 146 |

⚠️ **入口は割ったあとも、割る 11 のファイルの最大である。**  
それでも分けないのは、S2 が語彙と色を兄弟にすることを拒み（4.1）、S4 が共有の補助を入口に置くからである。  
⭐ `STYLE` の項を UI パーツごとの兄弟へ配り直す道はある。  
それは台帳を消す書き換えであり、割ることとは別の手番である（12 節）。  
⇒ ⚠️ **段 6 の後で入口を開く頻度を測り、多ければ別の変更要求で割る。  
本書は入口の大きさに上限を置かない**（`JDG-54`）。

### 4.5 計画の仮説との違い

計画の記録（`:299`）は「`ScreenRenderer` の 10 ユニットと同じ切り方で割る。記述を作る側と描く側が 1 対 1 になる」と書いた。

| 記述の側（表 T-075） | 描く側（本書） | 違い |
| --- | --- | --- |
| `UF-60` `screen-renderer.ts`（束ねて公開し、表示言語を運ぶ） | `UF-71` `dom-screen-surface.ts` | 1 対 1。<br>入口は語彙と層と読み返しも持つ（4.4） |
| `UF-61` `screen-frame.ts` | `UF-103` `screen-frame-drawing.ts` | 1 対 1。<br>ただしパネルの置き場とヘッダの高さは入口に残る（4.1） |
| `UF-62` 〜 `UF-69` | `UF-104`・`UF-105`・`UF-106`・`UF-108`・`UF-109`・`UF-110`・`UF-111`・`UF-112` | 1 対 1 |
| `UF-70` `screen-surface.ts`（`IF-9` の宣言） | —— | 宣言には描き手が無い |
| （仮説に無い） | `UF-107` `field-editing.ts` | ⭐ 描くのではなく、欄の編集の振る舞いである。<br>`IF-9` の後半（確定した値・編集の始まりと終わり）と 表 T-028 の `IN-4`・`IN-5a`・`IN-6` が持ち主である（0 節 ③ の 3） |

### 4.6 ⭐ `CR-500` の波 B が書く場所

`CR-500` の波 B（`CR-500:231`）が書く所は、本書のあとでは次にある。

| 波 B の文が名指す所（いまの木） | 割ったあと |
| --- | --- |
| パネルの欄の `focusin` ・ `focusout`（`:2771-2782`） | `field-editing.ts`（`UF-107`） |
| 文書名の欄（`:2888-2906`・`:2958`） | 同上 |
| 透かし解除の欄（`:3012-3020`） | 同上 |
| 面が欄を落とす所（`:2631`） | 入口の `showScreenView` に 1 行の呼び出しが残り、預かる本体は `field-editing.ts` の工場の面にある（4.3）。<br>⇒ 落ちたことの知らせは `field-editing.ts` に書く |
| `hasUnsettledTextEntry` の実装（`:3060-3062`） | `field-editing.ts`。<br>入口の返す面（`:3166`）に 1 行 |
| 知らせを 1 度読むと消える形で返す新しい面 | `field-editing.ts` の工場が持ち、入口の返す面に 1 行足す。<br>宣言は `src/adapter/screen-renderer/screen-surface.ts`（`UF-70`）—— ⛔ 本書の持ち場の外である |

⇒ ⭐ **状態機械のセッションは、波 B の面の側を `field-editing.ts` に書き、入口には返す面の行だけを書く。**  
⚠️ 波 B は段 6 の後に来る（`CR-500:231` の入口の条件「段 5 ・ 段 6 が済み」）。  
本書の分割のあいだ、`hasUnsettledTextEntry` の名と真偽の答えは変えない（振る舞いを変えない）。

### 4.7 負う要求の区分 —— 割る前と割ったあと

| | 割る前 | 割ったあと |
| --- | --- | --- |
| `UF-71` の欄 | `FR-101`（`OW-2`） | `—` |
| `UF-104` の欄 | —— | `FR-101`（`OW-2`） |
| ほかの兄弟 9 行の欄 | —— | すべて `—` |

⭐ **`FR-101` を移す理由（測った）**: 名前を時刻の上に置くのは `fillAppHeader`（`:843-876`）の `fileStatus.append(fileName)` → `fileStatus.append(savedAt)` の順であり、割ったあとは `UF-104` にある。  
`OW-2` の「規則の値を持つ最も内側のユニット」はそこである。  
⭐ **`UF-71` の責務の欄には、`FR-101` を満たす描き手が `UF-104` であることと、「記述の側に順序の欄を作って満たしてはならない」の ⛔ を残す** —— コンポーネントの入口から 1 ホップで正しい兄弟に着くためである（表 T-277 の `OW-3` の「入口は兄弟の全数をそこで読める」と同じ理由）。  
⚠️ `UF-62` の責務の文「上下の関係を負うのは `UF-71` である」（`05-07-design.md:458`）は `UF-104` に改める。

⭐ **兄弟が `—` である理由**: 兄弟が持つ規則の持ち主の要求は、どれも 表 T-075 で記述の側のユニットが起点を持っている（`FR-006` は `UF-64`、`FR-098` は `UF-63`、`FR-036` と `FR-099` は `UF-66`、`FR-076` は `UF-67`、`FR-066` は `UF-68`、`FR-053` は `UF-59` ほか）。  
`OW-2` の鎖の外側の端であり、欄は「最初に開くユニット」だけを書く（`CR-438` の 4.6 と同じ読み）。

---

## 5. 公開エントリはどうなるか

### 5.1 表 T-064 は 1 行も動かない

`PI-38` の名前（`ScreenSurface` の実装 `domScreenSurface`・`pageGroundStyle`・`ScreenTheme`）は、名前も署名もそのまま `dom-screen-surface.ts` に残る。  
⇒ 検査 26b は赤にならない。

### 5.2 ⛔ フォルダの外から読まれている名前 —— 1 つも動かない

測った内訳（取り込む 44 ファイル。  
`import { … } from '…/dom-screen-surface/dom-screen-surface'` を読んだ。  
名前空間の取り込みと動的な取り込みは 0 件）:

| 名前 | 読むファイル（うち `src/`） | 割ったあとの置き場 |
| --- | --- | --- |
| `ScreenTheme` | 43（1） | 入口 |
| `domScreenSurface` | 22（1） | 入口 |
| `ScreenSurfaceWiring` | 12（0） | 入口 |
| `pageGroundStyle` | 1（1） | 入口 |
| `NOT_STORED_ICON_SIZES` | 1（0） | 入口（生成の塊） |

⭐ `src/` の読み手は `src/framework/single-html-shell/single-html-shell.ts` の 1 ファイルだけである。  
⛔ **取り込みの文を 1 行も書き換えない。**  
⭐ 5 つとも入口に残るので、⭐ **再び公開する名前は 0 である**（`CR-438` の 5.2 の 4 つと違う）。

⇒ **試験は、次の 1 行を除いて 1 本も書き換えない**: `tests/contract/units.contract.test.ts:50-51` のユニットの数（`101`）。  
⚠️ これは段 6 の出口「試験が無変更で緑」の例外である。  
前例 —— `911e3d31` と `CR-438` の 5.2。

### 5.3 ⛔ ファイルの文字や仕様の行を読む試験と道具

| 読み手 | 何を読むか | 割ったあとの要り用 |
| --- | --- | --- |
| `tests/unit/fr-101-the-name-stands-above-the-time.test.ts:267-277` | 表 T-075 の `UF-71` の行に `FR-101` の字があること | ⛔ **`UF-71` の責務の欄に `FR-101` を満たす兄弟を名指す文を残すこと（MUST）**（4.7）。<br>試験の意図（`GOES RED IF: the responsibility moves back to the describing side.`）は割ったあとも真である |
| `tests/contract/units.contract.test.ts:64-76` | 表 T-075 の行ごとに、ファイルの見出しに `@unit      UF-n ` と `@purity    <純粋性>` があること | ⛔ 新しいファイルは入口と同じ見出しの形で始めること（MUST。<br>8.3） |
| `tests/unit/uf-71.test.ts:1358` ・ `tests/unit/uf-72-screen-part.test.ts:4139` | 根の要素の `data-unit` が `UF-71` であること、重ねる規則の字が `[data-unit="UF-71"]` で始まること | ⛔ **`UNIT_ROW` の値 `'UF-71'` を変えないこと（MUST）** —— 根の印はコンポーネントの印であり、兄弟の行 ID ではない |
| `tests/unit/cr-377-help-lists-keys-and-icons.test.ts:151` ・ `tests/unit/cr-425-help-separates-assignments-and-the-wheel-is-not-a-zero.test.ts:106` ・ `tests/unit/fr-029-palette-grab-marker.test.ts:177` ・ `tests/contract/fr-029-glyph-coordinate-system.contract.test.ts:105` | フォルダの `icon-glyphs.json` を道で読む | ⛔ **`icon-glyphs.json` を動かさないこと（MUST）**。<br>取り込むのは入口の `GLYPH_BY_ROW` だけである |
| `tools/generate_entity_types.py:2216` ・ `:2437` | 生成の塊を入口のファイルへ書き込む ／ 公開する定数の表 | ⛔ **塊を入口から動かさないこと（MUST）**（5.5） |
| `tools/generate_icon_glyphs.py:61` ・ `:65` ・ `.claude/skills/spec-graph-check/check-provenance.py:93` | `icon-glyphs.json` を書く ／ 出どころを読む | 動かない |
| `.claude/skills/spec-graph-check/crossing-names-baseline.txt:58` | `DomScreenSurface | domScreenSurface`（借り） | 動かない。<br>⚠️ 8.3 の MUST (b) |
| `.claude/skills/spec-graph-check/audit-ch5.py:83` ・ `:192` ・ `:196` | 表 T-063 の行の数、`SU-3` の文、ユニットのファイル名の重なり | `:83` を `12` に直す（前に立つ者） |

### 5.4 ⛔ 外から取り込む名前 —— いまの入口が取り込む名前だけ

兄弟が外から取り込んでよいのは、いまの入口が `../../adapter/screen-renderer/screen-renderer` と `../../entity/layout-engine/screen-regions/screen-regions` から取り込む 24 の名前（`:7-32`）だけである（MUST）。  
⭐ 測った内訳 —— 枠 `ScreenFrame`・`ScreenRect`、ヘッダ `AppHeaderItems`・`DisplayLanguage`、行見出し `RowTitle`・`RowTitlePanel`・`ScreenView`、プロパティパネル `PropertiesPanel`・`PropertyControl`・`PropertyControlKind`・`PropertyField`、欄の編集 `FieldCommit`・`PropertyFieldKey`、パレット `CommandItem`・`CommandPalette`、重ねて開く面 `CommandItem`・`OpenModal`、通知 `Confirmation`・`Notice`、対話欄 `DialogueField`・`DialogueInput`・`ScreenView`、ツールチップ `Tooltip`・`TooltipAnchor`。  
⇒ 検査 26b が数える組（コンポーネント ｜ 名）は 1 つも増えない（`check-published-members.py:218` は組を名で束ねる）。

### 5.5 生成された定数と、入口が新たに `export` する名前

⭐ `CR-432` の 4.4 と `CR-438` の 5.4 の決定（塊は動かさない。  
兄弟は入口から取り込む）に倣う。

| 定数 | 読むユニット | 生成の道具の要り用（`tools/generate_entity_types.py`） |
| --- | --- | --- |
| `NOT_STORED_DOCUMENT_TITLE_SIZES` | `UF-104` | `PUBLISHED_READ_BY_SRC` に入口の行を足し、そこへ入れる |
| `NOT_STORED_ROW_BAND_SIZES` ・ `NOT_STORED_ROW_GRAB_STRIP_SIZES` | `UF-105` | 同上 |
| `NOT_STORED_PROPERTY_FIELD_SIZES` | `UF-106` | 同上 |
| `NOT_STORED_PALETTE_GROUP_RULE_SIZES` | `UF-108` | 同上 |
| `NOT_STORED_HELP_SIZES` | `UF-109` ・ `UF-112` | 同上 |
| `NOT_STORED_RESOURCE_ROSTER_SIZES` | `UF-109` | 同上 |
| `NOT_STORED_CONFIRMATION_RULE_SIZES` | `UF-110` | 同上 |
| `NOT_STORED_ICON_SIZES` | 入口 ・ `UF-105` ・ `UF-109` | 既に `export` されている。<br>`PUBLISHED_READ_BY_TESTS_ONLY` から `PUBLISHED_READ_BY_SRC` へ移す（検査 30 が群の置き違いを赤にする） |
| `NOT_STORED_CHROME_SCALE` ・ `NOT_STORED_STATE_GROUND_PERCENTS` ・ `NOT_STORED_TYPEFACES` ・ `SCREEN_COLOURS` | 入口だけ | 動かない（`export` しない） |

⚠️ **兄弟が入口から取り込む名は、生成の定数のほかに 27 ある**（`STYLE`・`PAINT`・`ROLE`・`HOST_ENTER`・`made`・`part`・`fillEntry`・`commandEntry`・`anchoredEntry`・`entryStyle`・`entryFaintStyle`・`entranceStateFill`・入口の寸法 4 つ・`chromeScaledPx`・`stateGround`・`themeStyle`・`boxStyle`・`anchorKey`・`appendAssignment`・読み返しの印 5 つ。  
測った）。  
どれもいまは `export` されていない。  
⇒ 入口は 27 の名前と 8 つの定数を新たに `export` する。  
⛔ **フォルダの外からこれらを読んではならない（MUST NOT）** —— 5.3 節（`05-07-design.md`）の「フォルダの外から公開エントリ以外を読まない」と 表 T-248 の `JF-3` は、表 T-064 の名前だけを読むことを求める。  
検査 26b は表から輸出への一方向しか見ないので、これを見張らない（`CR-438` の 5.4 と同じ）。

---

## 6. ユニットが 101 から 111 に増えること

表 T-074 の `SU-3` は **101** である（`05-07-design.md:245`。  
`find src -name '*.ts'` も 101、`grep -c "^| UF-"` も 101）。  
10 足すと **111** になる。  
`DomScreenSurface` は 1 ユニットから **11** になる。  
⭐ `JDG-284` の ①（平らに並べ、モジュールを作らず、小さい兄弟もまとめない）は段 4 への裁定だが、同じ形に倣う —— `SU-2`（モジュール）は 0 のまま、フォルダは 1 つのままである。

⚠️ **`05-07-design.md:251` の文が不足になる**: 「表 T-075 のとおり `EditDocument` が最も多くのユニットを持ち、`InputCommandTranslator`、`ScreenRenderer` がそれに次ぐ」。  
割ったあとは `EditDocument` 18、`InputCommandTranslator` 15、`ScreenRenderer` 11、`DomScreenSurface` 11 である（コンポーネントのフォルダごとに `.ts` を数えた）。  
最後の波のコミットが書き直す。

⚠️ **50 行に届かないユニットは 0 である**。  
最小は `screen-frame-drawing.ts` の 50 行（`R2.9` の縁。  
共変は 2 件で証拠なし —— S1 と S5 で立てた）。

⚠️ **コメント密度（検査 55）の見込み** —— 各ユニットの範囲に 5 行の見出しを付け、取り込みの行を数えずに `check-comment-rules.py` の `measure_text` を掛けた（取り込みを数えないので上限の見積もりである）。

| ユニット | コード ／ コメント | 密度 | 10% の上限（コード 100 行以上） |
| --- | --- | --- | --- |
| 入口 | 752 ／ 94 | 11.1% | ⛔ 超える（超過 11 行。<br>取り込み 40 行を足しても 6 行ほど超える） |
| `field-editing.ts` | 312 ／ 42 | 11.9% | ⛔ 超える（超過 8 行。<br>工場の殻と取り込みを足しても 6 行ほど超える） |
| `properties-panel-drawing.ts` | 298 ／ 33 | 10.0% | 縁（取り込みを足せば収まる） |
| `open-modals-drawing.ts` ／ `row-title-panel-drawing.ts` | 372 ／ 40 ・ 378 ／ 35 | 9.7% ・ 8.5% | 収まる |
| ほかの 6 つ | どれもコード 100 行未満 | —— | 掛からない |

⇒ 8.3 の MUST (a) のとおり、超えた波で `comment-rules-src.md` の 5 節の手当て（`TRAP` を消してコードで守るか台帳へ起こす）が要る。  
`src/` の基準線の 1 行目は `0` である（`comment-rules-baseline.txt`）ので、1 行の超過でも赤になる。

---

## 7. ⛔ ラチェットが要求すること（分割ごとに、そのコミットで）

`HELD` 行の鍵は「ファイル::関数名」なので、関数を丸ごと別のファイルへ移すだけでも古い行は古び、新しい行が要る（`function-size-baseline.txt` の 9〜20 行目の注）。  
⛔ **本書はそのファイルを 1 文字も編集していない。**  
⭐ 書き換えは `JDG-118` が許している。  
⚠️ それでも、基準線を動かす前に前に立つ者がチャットで利用者の許しを得ること（`911e3d31` と `CR-438` の前例）。  
分類器や許可の確認が止めたら、迂回せずに止まって報告し、書き換える行を利用者に渡すこと（`handoff.md` の 2.11 ⑮）。

下の見込みは**行数だけ**である。  
分岐数は、割ったあとの形が決まるまで数えない（丸ごと移る関数は同じ値）。  
⛔ 各波は、割ったあとに `function-size.mjs` を掛けて実測した値だけを書くこと。

| 関数 | いまの `HELD` 行 | 割ったあとの見込み |
| --- | --- | --- |
| `controlElement` | `dom-screen-surface.ts::controlElement` 62 ／ 20 | `properties-panel-drawing.ts::controlElement`（丸ごと移るので同じ値） |
| `rowTitleElement` | 148 ／ 16 | `row-title-panel-drawing.ts::rowTitleElement`（同じ値） |
| `modalElement` | 172 ／ 33 | `open-modals-drawing.ts::modalElement`（同じ値） |
| `readScreenPartAt` | 58 ／ 31 | 鍵も値も変わらない（入口に残る） |
| `showScreenView` | 133 ／ 37 | 鍵は変わらない。<br>呼び出しに縮む 3 つの所（`:2575-2576`・`:2631-2634`・`:2646-2648`）で、およそ 127 行、分岐はおよそ 4 下がる。<br>⚠️ 下がっても赤にはならないが、実測の値に書き直す |
| `domScreenSurface` | 871 ／ 1 | 鍵は変わらない。<br>およそ 345 行（閉包に残る 339 行 ＋ 工場を作る数行）。<br>分岐 1 は `:2836` の `if` であり、欄の編集の工場へ移るので 0 になる見込み |
| （新しい関数）欄の編集の工場（`field-editing.ts`） | —— | およそ 405 行（閉包から移る 392 行 ＋ 殻と返す面）・分岐 1 ⇒ ⛔ **新しい `HELD` 行が 1 つ増える**。<br>閉包の入れ物の半分がここへ移る形であり、段 7（`CR-500` の波 B と状態機械）が縮める |
| （新しい関数）行の操作子の高さの工場 ・ 対話欄の工場 ・ 錨の表の工場 | —— | それぞれおよそ 30 ・ 40 ・ 25 行 —— 帯の縁ではない |

⇒ **落とす行 3、足す行 4、値を書き直す行 2。**  
⚠️ `excess-lines` ／ `excess-branches` の見込みは書かない（`CR-432` の 7 節と同じ理由）。  
向きだけを書く —— `domScreenSurface` の超過がおよそ 525 行減り、欄の編集の工場がおよそ 355 行を持ち込む。

---

## 8. 波の割り方と、並行してよいもの

### 8.1 ⛔ 割る体は同時に 1 体だけ

10 の兄弟はすべて `dom-screen-surface.ts` 1 本から出る。  
どの兄弟を出しても、そのファイルの取り込みの段（`:1-34`）、兄弟が読む語彙の `export`、閉包の呼び出しを書き換える。  
⇒ ⛔ **2 体が同時にこのファイルを割ってはならない（MUST NOT）** —— 取り込みの段が必ず重なり、併合がぶつかる（`CR-438` の 8.1 と同じ）。

⭐ **並行してよいのは 1 体だけ** —— 照合器を作る体である（8.3 の MUST (c)、計画の記録の試験の計画 `:326` の「固定した場面で描いた DOM を直列化し、現状固定の試験として記録する」に当たる）。  
条件（MUST）: 分割前の木（割る体が切った木と同じコミット）だけを読み、照合器をスクラッチか `tests/` の**新しいファイルだけ**に書く。  
`src/`・既存の試験・本書の持ち場のどれも書き換えない。  
割る体の最初の波より先に併合する。  
⭐ 照合器の体のブリーフには、`handoff.md` の 2.11 ⑭ のとおり「感度を 10 件で測れ（わざと壊して赤になる数）、照合器自身の分岐網羅を報告せよ」を最初から書くこと。

### 8.2 波の順

兄弟どうしの取り込み（4.2）があるので、呼ばれる側を先に出す（MUST (d)）。  
逆にすると、呼ぶ側を一度入口から取り込ませ、あとで取り込み元を書き換える二度手間になる。

| 波 | 出すユニット | 理由 |
| --- | --- | --- |
| 波 1 | `UF-103` `screen-frame-drawing.ts` ・ `UF-104` `app-header-drawing.ts` ・ `UF-110` `notices-drawing.ts` ・ `UF-108` `command-palette-drawing.ts` | どれも入口からだけ呼ばれる葉であり、閉包の中の行を持たない（頂上の宣言だけを動かす）。<br>型を先に確かめる波である |
| 波 2 | `UF-107` `field-editing.ts` ・ `UF-112` `tooltips-drawing.ts` | 閉包の塊を工場として出す（4.3）。<br>どちらも兄弟を取り込まない。<br>`UF-107` は波 3 の `UF-106` と波 4 の `UF-109` に呼ばれる |
| 波 3 | `UF-106` `properties-panel-drawing.ts` ・ `UF-105` `row-title-panel-drawing.ts` ・ `UF-111` `dialogue-field-drawing.ts` | `UF-106` は `UF-107` を、`UF-105` と `UF-111` は `UF-103` を取り込む |
| 波 4 | `UF-109` `open-modals-drawing.ts` | `UF-106`・`UF-110`・`UF-108`・`UF-107` を取り込む |

⭐ 1 つの波を 1 コミットにしても、1 ユニットを 1 コミットにしてもよい。  
⭐ 各コミットで確かめること: `vitest` が試験を 1 本も書き換えずに緑（5.2 の 1 行を除く）、`npm run gen:check`、`check.sh` の赤の集合が割る前と同じ、照合器が一致、段 6 の終わりに e2e（計画の記録の「全段で」）。

### 8.3 ⛔ 割る体の依頼文に逐語で書くこと

**共有のファイル** —— ⛔ 割る体は次を書かない。  
前に立つ者が 1 つずつ併合し、併合した木で測り直して書く。
- `.claude/skills/spec-graph-check/function-size-baseline.txt`（7 節）
- `docs/spec/05-07-design.md`（表 T-075 の行、`UF-71` と `UF-62` の責務の文、表 T-063 の `UT-12`、表 T-074 の `SU-3`、`:251` の文）
- `tests/contract/units.contract.test.ts:50-51` のユニットの数
- `.claude/skills/spec-graph-check/audit-ch5.py:83` の 表 T-063 の行の数
- `docs/development-records/` のすべて（`changelog.md`・`defects.md`・`pending-decisions.md` ほか。  
8.4 の行番号の書き直しを含む）

⭐ 割る体が書いてよい共有のファイルは `tools/generate_entity_types.py` の `PUBLISHED_READ_BY_SRC` ／ `PUBLISHED_READ_BY_TESTS_ONLY` の入口の行だけである（5.5。  
その定数を初めて読む兄弟を出す波で）。

**MUST**
1. 公開エントリの 5 つの名前を、いまと同じ名前と署名で入口に残すこと（5.2）。
2. 新しいファイルは、入口と同じ見出しのコメント（`@unit      UF-n `・`@component DomScreenSurface, layer Framework (table T-062)`・`@purity    non-pure`）で始めること。  
`@publishes` は入口だけが持つ。
3. `UNIT_ROW` の値 `'UF-71'`、生成された定数の塊、`icon-glyphs.json` を入口とフォルダから動かさないこと（5.3）。
4. 兄弟はモジュールの頂上（関数の外）で入口の名を読まないこと。  
頂上の定数は自分の中の字と数だけを読むこと（4.1 の評価の順）。
5. 閉包の塊は 4.3 の形で出し、宿主への呼び出しの順を変えないこと（4.3）。
6. 外から取り込むのは、いまの入口が取り込む 24 の名前だけとすること（5.4）。
7. 振る舞いを変えないこと。  
`hasUnsettledTextEntry` の名と真偽の答えも変えない（4.6）。  
割り方が 4.2 の範囲と食い違ったら、止まって報告すること（黙って範囲を変えない）。
8. ⭐ **(a)（逐語）**: 「各波のあとに検査 45・55・26b を回し、終了コードと赤の集合を報告せよ。検査 55 でコメント密度 10% を超えたら comment-rules-src.md の 5 節どおり TRAP を消し、消した文を報告せよ。」
9. ⭐ **(b)（逐語）**: 「『前例がある』と言う前に、その前例が crossing-names-baseline.txt（借り）に載っていないかを grep せよ。」
10. ⭐ **(c)（逐語）**: 「DOM が割る前と同一であることを、照合器（別の体が分割前の木から作る）で確かめる。照合器が不一致を出したら止まって報告せよ。」
    —— 照合器が比べるのは、固定した記述の列を `showScreenView` に通したときの宿主への呼び出しの列（何をどの順で作り、どの属性を置き、どの受け手を何に登録したか）、描いた木の直列化、`readScreenPartAt`・`readFieldCommit`・`readDialogueInput`・`hasUnsettledTextEntry` の答えである。  
    試験の偽物の宿主（`tests/unit/uf-71.test.ts:20-33` が書く形）と同じ作り方でよい。
11. ⭐ **(d)**: このファイルを割る体は同時に 1 体だけとし、波は呼ばれる側を先に出す（8.1・8.2）。
12. ⭐ **並行セッションからの MUST（`CR-438` の 8.3 の 6 から引いた）**: 「状態機械の出来事の名前（`docs/spec/_source/state-machines.json` の EV キー。例 armEntryPressed・pointerPressed・escapePressed、screen/…・gesture/…・fileFlow/…）と同じ語を新しい関数名・ファイル名に使うなら、意味を揃えること」

**MUST NOT**
1. フォルダの外のファイル —— とくに `src/adapter/screen-renderer/`（`screen-surface.ts` の宣言と、`ScreenSession` を読む形。  
`handoff.md` の 1.1 の段 6 の行）、`single-html-shell.ts` の取り込みの文、`frame-loop.ts` —— を書き換えないこと。
2. 試験を書き換えないこと（5.2 の 1 行は前に立つ者が書く）。
3. 兄弟の名を、フォルダの外から読める形で案内しないこと（5.5）。
4. `git stash` を打たないこと。  
`commit` と `push` は前に立つ者が行う。

**⭐ 12 を当てた結果（本書の名前）**: 出来事の語と重なるのは次の 4 つで、どれも意味が揃っている。  
`field-editing.ts`（領域「名前付けと入力欄」の `fieldEditBegan` ・ `fieldEditEnded` の「欄の編集」—— その知らせの出どころである）・`notices-drawing.ts`（`noticeRaised` ・ `noticeDismissPressed` の「通知」を描く）・`dialogue-field-drawing.ts`（`dialogueFieldEntryPressed` の `Dialogue Field` を描く）・`field-editing.ts` に移る `readWatermarkUnlockAnswer`（`watermarkUnlockAnswered` の `U-60` の答え）。

### 8.4 行番号を引いている記録（測った。`9b8a22b7`）

`dom-screen-surface.ts:<行>` の形を `docs/`・`change-request/`・`src/`・`tests/`・`tools/`・`.claude/skills/` で `grep` した。  
**120 件**（`docs/reference/` を除く）。

| 置き場 | 件数 | 分割のコミットで |
| --- | --- | --- |
| `docs/development-records/defects.md`（12 行） | 15 | ⛔ **直す（MUST）** —— 開いている欠陥の所在である。<br>直す体が内容で 1 件ずつ確かめること。<br>⭐ いまの木で内容が当たったもの: `DFC-574`（`:730`、`UF-104`）・`DFC-612`（`:426`、入口）・`DFC-644`（`:861`、`UF-104`）・`DFC-651`（`:2693`・`:2728`、`UF-107`）・`DFC-669`（`:1349`、`UF-106`）・`DFC-679`（`:2749`、`UF-107`）・`DFC-694`（`:2707`、`UF-107`）・`DFC-702`（`:2602`、入口の `showScreenView`）。<br>⚠️ 既に当たらないもの（書かれた日の木の行）: `DFC-566`（`:1226-1234` → いま `:1359-1368`、`UF-106`）・`DFC-571`（`:2250-2260` の `readDialogueInput` → いま `:2673-2680`、`UF-111`）・`DFC-623`（`:154-161` の `entryGlyphRoom` → いま `:189-198`、入口）・`DFC-648`（`:1772` → `[line.keys, line.press]` はいま `:1785`、`UF-109`） |
| `change-request/CR-500`・`CR-480`・`CR-440`・`CR-490`・`CR-470`（着地していない波を持つ、状態機械のセッションの変更要求） | 26 | ⛔ **直す（MUST）** —— ただし持ち主は状態機械のセッションなので、前に立つ者が相手に渡す。<br>`CR-500` は `:2771` の形（ファイル名を前に置かない行番号）も使うので、字面の `grep` より多い |
| `src/framework/single-html-shell/single-html-shell.ts:74`（行番号ではなくファイル名で面を名指すコメント。<br>120 件の外） | 1 | ⚠️ 「`dom-screen-surface.ts` が描く 2 つの属性」の `data-field-row` は `UF-106` と `UF-107` が描くようになり、字面が不足になる。<br>⛔ 本書の波では直さない —— 前に立つ者が決める |
| `src/framework/dom-input-source/dom-input-source.ts:123` ・ `src/framework/single-html-shell/frame-loop.ts:984`（ファイル名で `ROLE.dialogueField` を名指すコメント） | 2 | 直さなくてよい —— `ROLE` は入口に残る |
| `tests/unit/uf-72-screen-part.test.ts:2326`（コメント） | 1 | 直さない —— 試験は無変更（5.2） |
| `docs/development-records/fixed-defects.md` | 33 | 直さない —— 閉じた欠陥の、書かれた木の記録（0 節 ③ の 9） |
| 着地した変更要求 14 件（`CR-221`・`CR-270`・`CR-272`・`CR-378`・`CR-380`・`CR-381`・`CR-389`・`CR-392`・`CR-405`・`CR-406`・`CR-407`・`CR-408`・`CR-425`・`CR-426`） | 35 | 直さない（同上）。<br>⚠️ 14 件の状態の行を 1 件ずつは確かめていない |
| `docs/development-records/rulings.md`（`JDG-94`・`JDG-204`）・`measurements/` の 1 ファイル・`evidence/` の 1 ファイル・`docs/review/` の 2 ファイル | 10 | 直さない —— 裁定と測定と証拠と審査は、その日の木の記録である |

### 8.5 ⛔ 並行セッションの持ち場 —— 触らない

`src/use-case/advance-screen-session/`、`docs/spec/_source/state-machines.json`、`src/framework/single-html-shell/frame-loop.ts`、`change-request/CR-436`・`CR-440`・`CR-450` 〜 `CR-530`。  
⭐ `rulings.md:485` によれば、`CR-436` の波 B2（`SingleHtmlShell` から `advanceScreenSession` を呼ぶ辺）は段 5・段 6 の後に来る。  
`CR-500` の波 B も段 6 の後に来る（4.6）。  
⭐ `rulings.md:480`（`JDG-283`）の「描き手は出していないあいだ境界の帯を出さない —— 段 6 を待つ波」は、本書のあとでは帯を描く `screen-frame-drawing.ts`（`UF-103`）か、帯を記述する `screen-frame.ts`（`UF-61`）に書くことになる。  
どちらに書くかは本書は決めない。

---

## 9. 利用者に問う問い

⭐ **問いは 0 件である。**

問いの候補を 6 つ挙げ、それぞれに `handoff.md` の 0.2 の 3 つの手を打った。  
どれも問わずに決められた。

| 候補 | `rulings.md` の `grep` | `impact.py` | 他の道 |
| --- | --- | --- | --- |
| `DFC-566` を段 6 の入口の条件から外してよいか | `DFC-566`・`PND-494` とも 0 件。<br>`JDG-57` は「d で進めろ。」（計画の記録 7 の案 d） | —— | ⭐ 前に立つ者が決めた（0 節 ③ の 1）。<br>`JDG-57` が挙げた 2 つの理由は、照合器と所在の書き直しで満たせる。<br>⚠️ 案 d の字面からは外れるので、0 節 ③ の 1 にそう書いた |
| 入口が 1,107 行のまま残ってよいか | `共有の補助` は 0 件。<br>`JDG-54`（上限はラチェットだけ） | `UF-71`: 参照 1 箇所（`:458`） | ⭐ 届いた —— `CR-432` の S4 と `CR-438` の入口 1,383 行の前例（引いた。<br>`CR-438` の 4.4）。<br>S2 も語彙を兄弟にすることを拒んだ（4.1） |
| 欄の編集を UI パーツでない兄弟にしてよいか（計画の 1 対 1 から外れる） | `段 6` は 2 件、どちらも割り方に及ばない | `IF-9`: 要求 1 件 ／ 参照 5 箇所 | ⭐ 届いた —— 表 T-276 の `UD-1` と `UD-5` がそのまま当たる。<br>計画の文は仮説であり、`CR-438` も「掴みとドラッグ」を 5 つに割った（`CR-438` の 4.5） |
| `FR-101` の負う要求を `UF-104` へ移してよいか | `UF-71`・`CR-299` とも 0 件 | `FR-101`: 参照 10 箇所。<br>`UF-62`: 参照 1 箇所 | ⭐ 届いた —— 表 T-277 の `OW-2` の「規則の値を持つ最も内側のユニット」。<br>`UF-71` の文に名指しを残せば、試験も入口からの 1 ホップも保てる（4.7） |
| 11 ユニットを純粋性で割らなくてよいか | `UD-2` は 0 件 | `UD-2`: 参照 0 箇所 | ⭐ 届いた —— 表 T-060 の `LY-5` が `Framework` に `semi-pure-b` と `non-pure` だけを与える（0 節 ②） |
| 基準線を書き換えてよいか | `JDG-118`（逐語「(a) Claude Code の許可設定で、この書き換えを許可する。」） | —— | ⭐ 届いた —— 許しは既にある。<br>書き換えの手番で、前に立つ者がチャットで確かめる（7 節） |

---

## 10. 数の予測（当てる手番はこれと自分の実測を突き合わせる）

| 何 | いま（`9b8a22b7`） | 当てたあと |
| --- | --- | --- |
| 表 T-075 の行 | `UF-71` の欄は `FR-101`（`OW-2`） | `UF-103` 〜 `UF-112` の 10 行を `UF-71` と `UF-84` の間に足す。<br>`UF-71` の責務の欄を 4.2 と 4.7 の文に、負う要求の欄を `—` に改める。<br>`UF-104` の負う要求は `FR-101`（`OW-2`）。<br>`UF-62` の責務の文の `UF-71` を `UF-104` に改める |
| 表 T-063 の行 | 11 行（`UT-1` 〜 `UT-11`） | 12 行。<br>`UT-12`（`DomScreenSurface`、入口と UI パーツごとの 9 ファイルと欄の編集の 1 ファイル。<br>純粋性ではない —— `UD-1`、`LY-5`） |
| 表 T-074 の `SU-3` | 101 | 111（最後の波のあとで。<br>各波はその時点のファイルの数を書く） |
| 表 T-064 ・ `components.json` ・ 表 T-062 | —— | ⭐ 無変更 |
| 新しい表 ・ 図 ・ 接頭辞 | —— | ⭐ 0 |
| `05-07-design.md:251` | `EditDocument` ＞ `InputCommandTranslator` ＞ `ScreenRenderer` | `EditDocument` 18 ＞ `InputCommandTranslator` 15 ＞ `ScreenRenderer` 11 ＝ `DomScreenSurface` 11 |
| 検査 60 の `HELD` | このファイル 6 行 | 落とす 3 ・ 足す 4 ・ 書き直す 2。<br>⛔ 実測だけを書く |
| 書き換える試験 | —— | 1 行（`units.contract.test.ts` の数 101 → 111） |
| `tools/generate_entity_types.py` | 入口は `PUBLISHED_READ_BY_TESTS_ONLY` に 1 つ | `PUBLISHED_READ_BY_SRC` に入口の行を足して 8 つ入れ、1 つを移す（5.5） |
| 入口が `export` する名前 | 5 | 5 ＋ 27 ＋ 8（5.5） |
| 閉包 `domScreenSurface` | 871 行 ・ `let` 23 | およそ 345 行 ・ `let` 4（欄の編集の工場 14、行の操作子 3、対話欄 2 へ） |

---

## 11. 影響

### 11.1 仕様（`docs/spec/05-07-design.md`）—— ⛔ 各波の分割のコミットで、前に立つ者が書く

| 場所 | すること |
| --- | --- |
| 表 T-075 | `UF-103` 〜 `UF-112` を 4.2 の順で足す。<br>`UF-71` の責務と負う要求を改め、`UF-62` の責務の文を改める（4.7。<br>検査 18 が表と `src/` を同じコミットで一致させる） |
| 表 T-063 | `UT-12` を足す（`UT-7` と同じ書き方。<br>割った理由は 表 T-276 の `UD-1` —— UI パーツごとに縛る要求が別であり、欄の編集は 表 T-028 の `IN-4`・`IN-5a`・`IN-6` が縛る。<br>純粋性ではないことは 表 T-060 の `LY-5` を指す） |
| 表 T-074 の `SU-3` | 101 → 111 |
| `:251` | 6 節 |

### 11.2 記録（⛔ 本書は書かない）

- `docs/development-records/changelog.md` に 1 行。
- `docs/development-records/refactor-plan-report-2026-09-13.md` の段 6 の行 —— 入口の条件が満たされたこと、`DFC-566` を条件から外したこと（0 節 ③ の 1）。
- 8.4 の「直す」行。  
とくに `DFC-566` の所在（0 節 ③ の 1 の MUST）。
- ⚠️ 前に立つ者が相手のセッションへ渡すもの: `CR-500`・`CR-480`・`CR-440`・`CR-490`・`CR-470` の行番号、4.6 の波 B の置き場、`JDG-283` の帯の直しの置き場（8.5）。
- ⚠️ 前に立つ者が決めるもの: `single-html-shell.ts:74` のコメント（8.4）、照合器を `tests/` に残すか消すか（計画の記録 `:326` の「後」の欄）。

### 11.3 実装と試験（⛔ 本書は 1 行も直さない）

- `src/framework/dom-screen-surface/` に 10 ファイル。  
`icon-glyphs.json` は動かない。
- 試験は 5.2 の 1 行のほか書き換えない。  
書き換えが要ったら、それは割り方が公開の面を動かした徴である。

---

## 12. ⛔ この変更でやらないこと

| やらないこと | なぜ |
| --- | --- |
| 割ること | 本書は入口である。<br>割るのは 8 節の 4 つの波 |
| 振る舞いを変えること | 割るコミットは「試験が無変更で緑」と照合器の一致で検証される。<br>直しを混ぜると、ずれの出所が割り違いか直しかを切り分けられない |
| `DFC-566` を直すこと | `PND-494` の裁定を待つ。<br>直す場所は割ったあとの `properties-panel-drawing.ts` である |
| `CR-500` の波 B（欄の編集の知らせ）を書くこと | 状態機械のセッションの手番である。<br>本書は置き場を定めるだけ（4.6） |
| `docs/spec/` を編集すること | 本書は変更要求であり、当てるのは別の手番である |
| `function-size-baseline.txt` を編集すること | 分割のコミットが書き換える（7 節） |
| `STYLE` の項を UI パーツごとの兄弟へ配り直すこと | 台帳を消す書き換えであり、割ることとは別の手番である（4.4）。<br>共変はその道を示している（4.1 の `STYLE` の数） |
| 生成された定数の塊を兄弟へ動かすこと | 生成の道具を同じコミットで直すことになる（`CR-432` の 4.4 の決定） |
| 欄の編集の工場をさらに割ること | 段 7 の状態機械が 14 の `let` を引き取る（`CR-500`・`CR-480`）。<br>先に割ると、相手の波 B の書き場所が動く |
| `src/adapter/screen-renderer/` に触ること | `CR-436` の移行が段 6 の後の波に入る（`handoff.md` の 1.1）。<br>`IF-9` の宣言の書き換えは `CR-500` の波 B である |
| `frame-loop.ts` と状態機械に触ること | 段 7、そして並行セッションの持ち場である（8.5） |

---

## 13. 書くときの作法（⛔ 当てる体はここを読んでから書く）

- **CJK の直後に `**` を開いて、その中を code span で始めない** —— docutils が壊れる。
- 「表 X の N 行」と書かない —— 検査が X の行数についての主張として読む。**  
行 ID を名指す。**
- `python tools/fix_line_breaks.py` を、触ったファイルすべてに掛ける。
- 検査は**終了コード**で判ずる。  
⛔ `FAIL` を `grep` しない —— `check-function-size.py` は `FAIL` と刷るが、`check_layer_rules.py` は `VIOLATION` と刷る。  
検査 11・12・33 は `FAIL` の行を刷らずに赤になる。
- 行番号は最初の編集で動く。  
⚠️ 巨大ファイルの節は、行番号ではなく関数名で切ること（計画の記録の「段 4〜6 の共通の型」）。  
4.2 の行番号は `9b8a22b7` のものであり、割る体は着手の時点で関数名から引き直すこと。
- `git log -L` は範囲を 1 つずつ掛ける（4.0）。

---

## 14. 測り方の再現

本書の数は、次の 7 つの手で `9b8a22b7` から作った（スクリプトは OS の一時フォルダに置いたので木には無い。  
手順だけを書く）。

1. **関数の大きさ** —— `.claude/skills/spec-graph-check/function-size.mjs` の標準入力に `{"files":[{"file":"src/framework/dom-screen-surface/dom-screen-surface.ts","text":<本文>}]}` を渡す。  
閉包の中の関数は、開始行が `:2298` から `:3168` の間のものを数えた。
2. **行の割り当て** —— 見出しと取り込み（`:1-34`）、生成の塊（`:3186-3331`）、閉包の中の 17 の範囲（4.2 と 4.3 のとおり）を先に割り当て、残りは行頭が `function`・`const`・`let`・`type`・`interface`（`export` 付きを含む）の宣言ごとに、直前に続く列 1 のコメントの行を含めて次の宣言の手前までを 1 つの塊とし、宣言の名で 4.2 のユニットへ割り当てる。  
すべての行がちょうど 1 つのユニットに落ちることを表明で確かめる。
3. **共変** —— ユニットの範囲を 1 つずつ `git log --format=@@@%H -L <始>,<終>:<ファイル>` に掛け、コミットごとに差分の `-` と `+` の行からコメント・空行・行末のコメントを除いた多重集合を比べ、等しければその範囲について数えない。  
誕生の 2 件を除く。  
ユニットの集合は範囲ごとの集合の和。  
4.1 の拒まれた案（語彙と色を兄弟にした形、パネルの置き場を枠へ出した形）と `STYLE` だけの範囲も同じ手で数えた。
4. **読み手** —— `src/` と `tests/` で `dom-screen-surface/dom-screen-surface` を含むファイルを集め、`import … from` の波括弧の中の名前を数える。
5. **兄弟の間の名** —— 各ユニットの行から、コメントと文字列の中身を除いたコードの語を拾い、別のユニットが頂上で宣言する名と突き合わせた（5.4・5.5 の数）。  
閉包の中の名も同じ手で、閉包の範囲の間で突き合わせた（4.3 の面）。
6. **コメント密度** —— `.claude/skills/spec-graph-check/check-comment-rules.py` の `measure_text` を、各ユニットの範囲の行に 5 行の見出しを付けた字に掛けた（取り込みの行は足していない）。
7. **行番号を引く記録** —— `dom-screen-surface\.ts:[0-9]+` を 8.4 の置き場で `grep` し、`defects.md` の 12 行は引いた行の中身をいまのファイルと読み比べた。

---

## 15. ⭐ 当てる体が記した、本書と実物の違い

⭐ `CR-438` の 15 節の教訓は、本書の数に先に織り込んだ。
- 兄弟の行数は見出しと取り込みの分だけ増える（4.2 の注）。
- 台帳の行番号は、書かれた日の木の行であることが多い —— 本書は `defects.md` の 12 行を内容で読み比べ、4 行が既に当たらないことを確かめた（8.4）。
- 割る前のユニットの数は、別のセッションの着地で動く —— `SU-3` の 101 は `9b8a22b7` の値である。  
当てる手番で測り直すこと。

### 15.1 割る体が測った違い（2026-09-22、`a01ef3fb` の上の分割）

| # | 本書が書いたこと | 実物 |
| --- | --- | --- |
| (1) | 照合器が DOM の一致を確かめる（8.3 の MUST (c)） | ⚠️ 照合器の場合 `X01`（入口が `export` する名前の一覧）は必ず食い違う —— 5.5 のとおり入口が名前を足すからである。<br>前に立つ者がこの食い違いを受け入れ、一致は残る 104 の場合と、元からある 3 つの名前と値で判じた |
| (2) | 検査 18 が表と `src/` を同じコミットで一致させる（2 節） | ⚠️ 検査 26b ・ 18 ・ 44 は、表 T-075 の行が入るまで赤である。<br>同じフォルダの中の取り込みでも赤になる |
| (3) | 入口は 27 の名前と 8 つの定数を新たに `export` する（5.5） | 新たに `export` した名前は **32**（実行時の値 30 ・ 型 2）である |
| (4) | 検査 55 は入口と `field-editing.ts` が超える見込み、入口の超過は 11 行（6 節） | 検査 55 は入口でも赤になった（波 3 ・ 波 4）。<br>入口の超過は **9 行**であった。<br>消した `TRAP` は 10 個 ・ **13 行**である（`field-editing.ts` 2 個、入口 8 個）。<br>台帳での行き先は 15.2 |
| (5) | 閉包の塊は 4.3 の形で出し、宿主への呼び出しの順を変えない | `rowControlsMeasureKey` は主題を値で受け、`rowControlsHeightReporter` は配線を呼ばれたときに読む。<br>⭐ 宿主への呼び出しの順は変わっていない |
| (6) | 入口 1,107 行ほか（4.2 の範囲の行数）、ユニット 111（6 節） | 行数: 入口 1,117 ・ `field-editing.ts` 450 ・ `row-title-panel-drawing.ts` 552 ・ `open-modals-drawing.ts` 492 ・ `properties-panel-drawing.ts` 400 ・ `notices-drawing.ts` 126 ・ `command-palette-drawing.ts` 106 ・ `dialogue-field-drawing.ts` 99 ・ `app-header-drawing.ts` 95 ・ `tooltips-drawing.ts` 82 ・ `screen-frame-drawing.ts` 66。<br>`src/` の `.ts` は 111（`find src -name '*.ts'`）。<br>関数の大きさ（行 ／ 分岐）: `domScreenSurface` 337 ／ 0、`showScreenView` 124 ／ 34、欄の編集の工場 `fieldEditingOf` 417 ／ 1 |
| (7) | 仕様だけを読む試験の体を割る体の横に置く | その体の `it.fails` のうち 3 つは試験の誤りであった —— 反証して直した。<br>残る 1 つ（`NT-5` の通知が `NT-1` と見分けられない）は `DFC-750` になった |

### 15.2 当てた体が測った違い（仕様の行と台帳）

| # | 本書が書いたこと | 実物 |
| --- | --- | --- |
| (8) | 表 T-063 の新しい行を `UT-12` にする（0 節 ③ の 10。<br>「前に立つ者は、書く前に相手へ宣言すること」） | `UT-12` を 表 T-063 に足した。<br>⚠️ 相手のセッションへの宣言は、この体はしていない —— 前に立つ者がする |
| (9) | 8.4 —— `DFC-574`・`DFC-612`・`DFC-644`・`DFC-651` は `9b8a22b7` で内容が当たった | ⚠️ 当たらなかった。<br>`DFC-574` の `:730` は `readableStamp` の上の注であり、開いたファイル名を描く所ではない。<br>`DFC-612` の `:426` は `@provisional PND-326` の行であり、`text-overflow` は `:455`・`:463` にある。<br>`DFC-644` の `:861-876` はヘッダの入口であり、`Scrollbars` ではない。<br>`DFC-651` の `:2693` と `:2700-2702` は `focusPropertyField` の注と `change` の登録である。<br>⇒ この 4 行と、本書が既に古いとした `DFC-571`・`DFC-623`・`DFC-648` の所在は書き写さなかった |
| (10) | 8.4 —— 開いている欠陥の所在を直す | 内容が当たった `DFC-669`・`DFC-679`・`DFC-694`・`DFC-702` の所在を新しいファイルへ書き直した。<br>`DFC-566` の所在は `properties-panel-drawing.ts` へ書き直した（0 節 ③ の 1 の MUST）。<br>⚠️ 状態機械のセッションの変更要求（`CR-500`・`CR-480`・`CR-440`・`CR-490`・`CR-470`）の行番号は、持ち主が違うので書き直していない |
| (11) | 0 節 ③ の 1 —— `DFC-566` を段 6 の入口の条件にしない（前に立つ者の決定。<br>覆すなら利用者） | 利用者が「推奨通りにやれ」で認めた（`JDG-350`）。<br>`JDG-57` の状態は「覆された（一部）」にした |
| (12) | 6 節 —— `TRAP` を消したら `comment-rules-src.md` の 5 節どおり台帳へ起こす | 10 個のうち 2 個は既存の行が持っていた —— 対話欄を `hasUnsettledTextEntry` に数えない罠は `DFC-571` の追記と試験が、入口の器の `min-width` の罠は `DFC-623` が持つ。<br>残る 8 個は `DFC-751` 〜 `DFC-756` の 6 行に起こした |
