# CR-567 — 取り込んだ葉のタスクは親の行に載る

> 起草の状態: 起草（2026-09-24）。未着地。本書を書いた体は `docs/spec/` と `src/` に 1 字も書いていない。
> 読んだ木: `1ba35016`（`origin/refactor`）。行番号・数・参照は、すべてこの木で測った。
> ID の帯: 調整役（セッション「handoff.md リファクタと不具合修正」）から CR-567 を受けた。台帳の行は `DFC-980` ・ `JDG-560`（帯 `DFC-980` 〜 `DFC-989` ・ `JDG-560` 〜 `JDG-564`）。
> 当てる順: ⛔ `CR-554`（大きいファイルの分割）と `CR-565` の後。`src/adapter/document-codec/mspdi-codec.ts` は `CR-554` の 4.8 節で割るファイルであり、本書が直す `rowsFromTasks` は兄弟 `mspdi-imported-rows.ts`（`UF-156`）へ移る。⇒ 本書の継ぎ目（5 節）と E-02 は分割後の木に錨を置く。当てる直前に `file:line` を測り直すこと。
>
> 閉じるもの: `DFC-980`（`MSPDI` を取り込むと、深さ `S-125` 以内のすべての `Task` が、葉も含めて自分の行を 1 つずつ持つ）。利用者の裁定 `JDG-560`（逐語は 0.1 節）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### 0.1 利用者の逐語（2026-09-24）

- 報告: 「MSPDIの読み込みが変。sample-schedule/sample-large-erp-program.ja.xml を読んだら、1タスクごとに1行読むようになってしまった.... ちょっと前はそんなことなかったのに。 仕様通り1行に複数タスクが入っていた。」
- 裁定 `JDG-560`: 「A1（推奨）とせよ。  つまり最下層のタスク群が1つの行に入る。」

⭐ 調べた結果（`DFC-980` の詳細）: 退行ではない。2026-09-07 以降の `dist/index.html` 12 本のどれでも、行あたりのタスクは 0 か 1 だった。利用者が覚えていた「1 行に複数タスク」は、公開中の旧アプリ（Pages、2026-08-12）の振る舞いである。旧アプリは `Summary` が 1 のタスクで行を開き、続く葉をその行に載せていた。

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

`GL-002`（`CH-2`、縮めても全体を 1 枚で俯瞰させる）—— 取り込んだ文書の行が、葉の数だけ縦に伸びない。
`sample-large-erp-program.ja.xml` では 257 行が 38 行になる（6 節）。
⭐ 行は複数の `Task` を載せる器である（`05-07-design.md` の 5.4 節の 2 軸）。取込でその器が 1 タスクずつにしか使われないと、器の意味が取込では失われる。

### ② レビュー観点のどの条項を当て、何が出たか

- `R1.1`（抜けが無い）—— `FR-058` は「上限を超える階層には器を作らず」とだけ書き、子を持たない `Task` が器を持つかどうかを書いていない。E-01 でこの空白を埋める。
- `R1.3`（矛盾が無い、唯一の正）—— 葉の置き場を決める文は `FR-058` の 1 か所に置く。`UF-156` の責務の欄（E-02）は、その規則を名指して写さない。
- `R4.x`（状態機械）—— 升も出来事も変えない。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 親を持たない `Task` は、子を持たなくても自分の行を持つ | `FR-058` の「行に載らない `Task` を文書に残してはならない（MUST NOT）」。親の行が無いので、載せる器がほかに無い | 根に葉が並ぶファイル（WBS を使わないファイル）は、今と同じく 1 タスク 1 行のままである |
| 決定 2 | 親に器が無いとき（親が `S-125` を超える段に在るとき）は、今と同じく最も深い祖先の行に載せる | `FR-058` の既存の MUST「その配下のタスクを最も深い段の行に載せる」をそのまま使う | — |
| 決定 3 | 子を持つ `Task` は、自分から作った行に自分自身も載る（要約のバーはその行に描く） | 裁定の案 A1 の定義（「要約バーは自分の行」）。行の名前（`AT-54` の導出元）とその行のバーが同じ `Task` になる | 要約のバーは子の葉の範囲を覆うので、その行では段が 1 つ増える |
| 決定 4 | 行の `order`（`AT-55`）は今と同じく `wbsOrder` から取り、歯抜けを詰めない | `AT-55` は「同じ親の下での並び」とだけ定め、連番を求めない。`LC-9` は昇順だけを見る（`05-07-design.md:856`） | 取り込んだ直後の兄弟の行の `order` は 0, 2, 5 のように飛ぶことがある |

---

## 1. 範囲 —— 要求ごとの行き先

| 要求 | 仕様で変える所 | 編集 |
|---|---|---|
| R1 子を持たない `Task` には器を作らず親の行に載せ、親を持たない `Task` は自分の行を持つ | `01-04-requirements.md` の `FR-058`（`:5315` の段落の後） | E-01 |
| R2 分割後のユニットの責務の文が R1 を名指す | `05-07-design.md` の 表 T-075 の `UF-156` の行（`CR-554` が足す行） | E-02 |

数: 仕様の文の編集 2（E-01 ・ E-02）。原稿 JSON の編集 0。

---

## 2. 新しい識別子

新しい行 ID・表番号・要求・設定値は 0。
台帳の行 `DFC-980` と `JDG-560` は、本書の起草より前に `1ba35016` で書いた（12 節）。

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 旧（`1ba35016`） | 新 | 理由 |
|---|---|---|
| （無い）—— `FR-058` は葉の置き場を書いていない | E-01 の段落を足す | 空白を埋めるだけで、既存の文は 1 字も消さない |
| `CR-554` が書く `UF-156` の責務の欄の「「`maxGroupDepth` で止め、深いタスクは最も深い祖先の行へ載せる」」 | 「「子を持たないタスクには行を作らず親の行へ載せ、`maxGroupDepth` で止め、深いタスクは最も深い祖先の行へ載せる」」 | ユニットが守る規則の列挙に R1 が無いと、責務の文が実装より狭くなる |
| `tests/unit/uf-36.test.ts:38-40` の注「one TaskGroup per Task down to S-125 and none below it」 | 「one TaskGroup per Task that has children or no parent, down to S-125; a Task with no children sits on its parent's row」 | 注が E-01 の前の実装を述べている。⚠️ 試験の注は仕様だけを読む試験の体が書き直す（9 節） |
| `tests/unit/uf-36.test.ts:1347` の見出し「makes one row per Task down to S-125, and none below it」 | 「makes one row per Task that has children or no parent, down to S-125, and none below it」 | 同上 |

---

## 4. 書き直す所（当てる体がそのまま使う文）

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
旧（`:5312-5316`、`FR-058` の RATIONALE の第 2 段落とその後の空行）
```text
取り込みでは `TaskGroup` の深さ上限で受け付けを拒んではならない（MUST NOT）。  
上限を超える階層には**器を作らず、その配下のタスクを最も深い段の行に載せること（MUST）**。  
上限が掛かるのは人が階層を作るときだけである（`FR-004`）。  
**WBS の深さはそのまま保たれる**ので、書き出せば元の階層が戻る。

```
新
```text
取り込みでは `TaskGroup` の深さ上限で受け付けを拒んではならない（MUST NOT）。  
上限を超える階層には**器を作らず、その配下のタスクを最も深い段の行に載せること（MUST）**。  
上限が掛かるのは人が階層を作るときだけである（`FR-004`）。  
**WBS の深さはそのまま保たれる**ので、書き出せば元の階層が戻る。

**子を持たない `Task` には器を作らず、親の `Task` の行に載せること（MUST）** —— 同じ親を持つ `Task` が 1 つの行に集まり、段割当（`FR-003`）で段に分かれる。  
親に器が無いとき（親が上限を超える段に在るとき）は、上の段落のとおり最も深い段の行に載せる。  
**親を持たない `Task` は、子を持たなくても自分の行を持つこと（MUST）** —— 載せる親の行が無い。  
子を持つ `Task` は、自分から作った行に自分自身も載る。  
⭐ 行は複数の `Task` を載せる器である（Chapter 5.4）—— 子を持たない `Task` にまで器を作ると、取り込んだ文書のどの行にも `Task` が 1 つしか載らない。  
⚠️ 1 つの行に載る `Task` が増えるので、名前の幅を占有幅に数える規則（表 T-038 の `OC-1`）により、行の段は日付の重なりだけで数えるより多くなる。

```

⚠️ 段落の末尾に置く —— 検査 42 は、試験が隣り合う文を続けて引いているかを読む。`:5312-5315` の 4 文の間には入れない。
⚠️ 文の中に日付・利用者の名・裁定の番号を書かない（検査 `check-spec-holds-no-history.py`）。裁定の逐語は `rulings.md` だけが持つ。

<!-- EDIT id=E-02 file=docs/spec/05-07-design.md -->
⛔ `CR-554` が着地してから当てる。`CR-554` の 4.8 節は、表 T-075 に `UF-156` の行を足す。その責務の欄に次の字がある（`CR-554` の `:536`）。
旧（`CR-554` が書いた `UF-156` の行の責務の欄の一部）
```text
「`maxGroupDepth` で止め、深いタスクは最も深い祖先の行へ載せる」
```
新
```text
「子を持たないタスクには行を作らず親の行へ載せ、`maxGroupDepth` で止め、深いタスクは最も深い祖先の行へ載せる」
```

⚠️ 当てる直前に、着地した `UF-156` の行の字を `grep -n "UF-156" docs/spec/05-07-design.md` で読み直すこと。`CR-554` が 4.8 節の表と違う字で当てていたら、同じ意味の所に同じ句を足す。
⭐ 負う要求の欄は変えない（`CR-554` は 4 行とも `—` と決め、`FR-058` の起点を `UF-77` に置いた）。

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| 継ぎ目 | 形 |
|---|---|
| 直す所 | `CR-554` の後の `src/adapter/document-codec/mspdi-imported-rows.ts` の `rowsFromTasks(tasks: readonly Task[], maxGroupDepth: number): ImportedRows`。⛔ 署名は変えない。`1ba35016` では `src/adapter/document-codec/mspdi-codec.ts:1358` に在る |
| 行を作る条件 | 今の条件「深さ ≦ `maxGroupDepth`」に、「その `Task` が子を持つ（ある `Task` の `wbsParentUid` がその `uid` である）か、`wbsParentUid` が `null` である」を AND で足す。子を持つかは `tasks` から 1 度だけ集合に集めて引く（`O(n)`） |
| 載せる行 | 変えない。自分の行があればそこ、無ければ `deepestAncestorRow`（行を持つ一番近い祖先へ遡る、`mspdi-codec.ts:1397`）。子を持たない `Task` の親は、深さが上限以内なら必ず行を持つので、そのまま親の行になる |
| 変えない値 | 行の `id`（`rowIdOfTask`、`AT-51`・`PND-80`）、`parentId`、`derivedFromTaskUid`、`order`（`wbsOrder` から、0 節の決定 4）、そのほかの列 |
| 試験 | `tests/unit/uf-36.test.ts` の `FR-058 -- the imported tasks land on rows`（`:1334`）。今の試験の WBS は 1 本の鎖（各段 1 タスク、`nestedTasksText`）なので、E-01 の後も期待は 1 つも崩れない —— 鎖の末端の葉は、親が上限以内なら親の行に、上限を超えるなら最も深い祖先の行に載り、行の数は上限と同じ 5 のまま |

---

## 6. グラフ（`1ba35016` で測った）

- `impact.py FR-058`: 要求 5 件・参照 8 箇所 —— `UC-014`（`:936`）、`FR-001`（`:1053` ・ `:1082`）、`FR-003`（`:1500`、表 T-014 の `ST-7`）、`FR-085`（`:1573`）、`FR-019`（`:4813`）、`05-07-design.md:338` ・ `:395`。どれも「取込が行を作る」ことと、名前の導出元の規則を指すだけで、「どの `Task` が行を持つか」を写していない。⇒ 偽になる文は 0。
- `ST-7`（`:1500`）は「取込で載せたもの（`FR-058`）を含む」と数える —— 1 つの行に載る `Task` が増えるので、安全弁 `S-89`（255）へ近づく。試算の最大は 1 行 19 タスク（`sample-large-erp-program.ja.xml`）で、255 から遠い。
- `HR-1a`（表 T-015）: 畳んだ行の配下を描かない規則は変えない。葉は畳んだ行の「配下の行」ではなく「その行に載る `Task`」になるので、行を畳んでもその行の葉は描かれたまま残る —— 載せ替えではない（取込のときから載っている）。
- `HM-9`（`:1745`）: ⚠️ **本書で最も見落としやすい帰結** —— 行を並べ替えたり親を変えたりすると、`tasksRankedByTheRowTree`（`src/use-case/edit-document/task-group-order.ts:54`）が全 `Task` の `wbsOrder` を行の木の順で付け直し、同じ行の兄弟は 表 T-014 の `ST-2` の順（予定の開始が第 1 キー）にする。E-01 の後は同じ親の葉が 1 つの行に集まるので、行を 1 つでも動かすと、どの親の葉も WBS の順が開始日の順になり、兄弟の要約タスクより前へ出る。⇒ 書き出した `MSPDI` の `ID` ・ `OutlineNumber` の順が、元のファイルと変わる。取り込んで何も動かさずに書き出したときは変わらない（`FR-021`）。仕様どおりの帰結であり（`HM-9` が同じ行の兄弟を名指している）、11 節の問い 1 で利用者に示す。
- `induced.py FR-058 FR-003 FR-004 HR-1a T-038 ST-7 S-125`: 7 の種、種どうしの辺 9、閉路 1（大きさ 5: `FR-004` `FR-058` `HR-1a` `S-125` `ST-7`）。本書が字を変えるのは `FR-058` の 1 つだけなので、閉路は編集の順を縛らない。

試算（`sample-schedule/` の 3 つ、日付の重なりだけで段を数えた。名前の幅（`OC-1`）は数えていない）:

| ファイル | 今（行 ／ 1 行のタスク） | E-01 の後（行 ／ 1 行のタスク ／ 段） |
|---|---|---|
| `sample-large-erp-program.ja.xml`（257 タスク、要約 38、深さ 4） | 257 ／ すべて 1 | 38 ／ 1 〜 19 ／ 1 〜 5 |
| `sample-medium-sfa-webapp.ja.xml`（135 タスク、要約 17、深さ 4） | 135 ／ すべて 1 | 17 ／ 1 〜 11 ／ 1 〜 6 |
| `sample-small-website-renewal.ja.xml`（46 タスク、要約 7、深さ 3） | 46 ／ すべて 1 | 7 ／ 1 〜 8 ／ 1 〜 4 |

---

## 7. 数の予測（`1ba35016` で測った。当てた後に同じ数え方で突き合わせる）

| 数 | 前 | 後 | 内訳 |
|---|--:|--:|---|
| tables | 187 | 187 | — |
| figures | 27 | 27 | — |
| rows | 2307 | 2307 | 行を足さない（E-02 は `CR-554` の行の字を変えるだけ） |
| uids | 162 | 162 | — |

⚠️ `CR-554` と `CR-565` が先に着地するので、前はその数だけずれる。**予測するのは本書の差分 ±0 である。**
⚠️ 写しで赤くなる門が 1 つある（試験の側で閉じる。基準値は動かさない）:
- `check-must-clause-coverage.py` —— 試験が逐語で持たない MUST ／ MUST NOT が 1311 → 1313（＋2。E-01 の 2 つの MUST）。試験の体が、新しい 2 つの条項を逐語で持つ。

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

| 波 | 持ち場 | 中身 |
|---|---|---|
| 1 | 仕様（前に立つ者） | E-01 ・ E-02。`npm run gen` は要らない（原稿 JSON を触らない） |
| 2a | 実装の体 | 5 節の継ぎ目。`mspdi-imported-rows.ts` の 1 か所 |
| 2b | 試験の体（⛔ 実装を読まない。`docs/spec` だけを読む） | `uf-36.test.ts` の注と見出し（3 節）、新しい場合 4 つ: ① 同じ親の葉が 1 つの行に載る ② 親を持たない葉は自分の行を持つ ③ 子を持つ `Task` は自分の行に載る ④ 親が上限を超える段に在る葉は最も深い祖先の行に載る。E-01 の 2 つの MUST を逐語で引く |
| 3 | 前に立つ者 | `dist/index.html` を刷り直し、9 節の実物確認 |

⭐ 2a と 2b は触るファイルが重ならない（`src/` と `tests/`）ので並行してよい。破る試験（break test）は合わせた木で走らせる。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

- コード: 5 節の継ぎ目。
- 試験: 8 節の 2b。
- 当てた後に、出荷ビルドで押して確かめる（Playwright、msedge、file://、`IC-1` → `IC-71`）: `sample-large-erp-program.ja.xml` を開き、全展開・縦 9000px で行が 38、1 行のタスクが 1 〜 19 であること。段の実数（名前の幅を数えたもの）を測って `DFC-980` の実物確認の欄に書くこと —— 6 節の試算は名前の幅を数えていない。
- 台帳: `DFC-980` の状態を `仕様待ち` → 当てたら `実測待ち` → 押して確かめたら `実測済`。`JDG-560` の状態を「指示」→「適用済み」（着地先は `FR-058`）。

---

## 10. ⛔ この変更でやらないこと

- 既に保存した文書（`.json`）の行は変えない —— 行は文書に保存され、本書が変えるのは取り込みで行を作るときだけである。
- 合流（`IC-72`）の突き合わせ（行の `id` を `Task` の UID から作る `PND-80`）は変えない。
- 畳み（`HR-1a`）、グループ LOD（表 T-005a の `L-3`）、段割当（`FR-003`）、占有幅（表 T-038）は変えない。
- `S-125` の既定値（5）は変えない。
- 旧アプリのように要約タスクを描かない形にはしない —— `FR-058` の「行に載らない `Task` を文書に残してはならない」に反する。

---

## 11. 前に立つ者へ返す問い

1. **`HM-9` の帰結を受け入れるか**（6 節）—— 取り込んだ文書で行を 1 つでも並べ替えると、同じ親の葉の WBS の順が開始日の順に変わり、葉が兄弟の要約より前へ出る。書き出した `MSPDI` の順が元と変わる。
   - 案 ① 受け入れる（推奨）—— `HM-9` は利用者が手で 1 行に複数タスクを載せたときに既にこう振る舞う。取込だけ別の順を守る理由が無い。
   - 案 ② 同じ行の兄弟は元の `wbsOrder` の相対順を保つよう `HM-9` を変える —— 別の変更要求になる（`HM-9` と `ST-2` の規則を変える）。
   ⭐ 利用者に問うかは調整役が決める。問わずに ① で進めるなら、0 節 ③ に決定 5 として移す。

---

## 12. 台帳

| 台帳 | 行 |
|---|---|
| `docs/development-records/defects.md` | `DFC-980`（状態 `仕様待ち`、`1ba35016` で記入済み） |
| `docs/development-records/rulings.md` | `JDG-560`（状態「指示 —— CR は未起票」、`1ba35016` で記入済み）。本書の起草で「指示 —— `change-request/CR-567-an-imported-leaf-task-sits-on-its-parents-row.md` が当てる」に変える |

---

## 13. 測り方の再現

- 出荷ビルドの行の数: 探索子は Playwright（msedge）で `dist/index.html` を file:// で開き、`showOpenFilePicker` を差し替えて `IC-1` → `IC-71` で読み、`[data-role="Row Title Tree"] [data-group-id]` の数と、各行の縦の範囲に中心が入る `[data-figure^="task-"][data-figure$="-plan"]` の数を数えた。縦 1000px では行ズームの LOD で深さ 2 までの 13 行しか描かれないので、全行は縦 9000px で数えた。
- 6 節の試算: `MSPDI` の `Tasks/Task` の `UID` ・ `OutlineLevel` ・ `Start` ・ `Finish` から WBS の木を作り、各案で行と所属を作り、行ごとに日付の区間を貪欲に段へ詰めた（名前の幅は数えない）。
- 探索子と試算の台本は起草した体の作業場（scratchpad）にあり、木には入れていない。
