# CR-287 — 木は木の順に読む

> **閉じるもの**: ⛔ **台帳 D-123（前に立つ者の実測 2026-08-29。D-77 の実物確認で出た）。**
> ⭐ **`fr-027-startup-shows-its-depth` の残る赤も、これで初めて判定できる絵になる。**
> ⚠️ **台帳 D-01 の「インデントも浅く」も同じ根である疑いが濃い。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`（マニュアルを読まずに使える）。**
⛔⛔ **利用者の逐語**:「**次こそ今ある不具合を全部直せ**」——その筆頭が「行が 7 行しか出ない」である。
⭐ **CR-286 で起動が全体表示から外れ、レイアウトは深さ 1〜3 の 68 行を並べるようになった。**
⛔⛔ **それでも画面には深さ 1 の 7 行しか出ない。並べる順が木ではなく深さだからである。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 行の縦の並び順 | ⛔⛔ **仕様書のどこにも無い。**`AT-55` は「同じ親の下での並び」としか言わず、**親どうし・親と子の前後を決める行が 1 つも無い** |
| **`R3.4`**（同じものを 2 か所に置かない）| `FR-042` の「行の位置」 | ⛔ **`FR-042` は縞を「行の位置で数えること（MUST）」と定めながら、その位置を決める規則を持っていない。**⇒ 正が無いまま参照している |
| **`R2.19`**（驚かせない）| 深さ順の並び | ⛔ **親と子が画面の別々の場所へ離れる。**木を持つ道具の絵として読めない |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`order` の意味は動かさない**（親の中での順のまま）| ⛔⛔ **台帳 D-123 の案②（`order` を全体の順に変える）を退けた** —— `AT-55` が「同じ親の下での並び」と明記しており、**全体の順にすると行を 1 つ挿すたびに以降の全行の `order` を書き直すことになる。**⇒ 往復（`FR-021`）に不要な差分が乗る |
| 2 | ⭐ **並べる規則は `LC-9` が持つ** | ⭐ **縦位置を決めているのが `LC-9` である。**新しい行を 表 T-068 に足すと 順 の 1〜11 が全部ずれる（`impact.py` で 4 箇所が 表 T-068 を指す）ので、**既にある行の中身を厚くした** |
| 3 | ⭐ **`FR-042` の「行の位置」もこの順での位置とする** | ⭐ **正を 1 つにした。**別々に決めると、同じ文書で縞と並びが食い違う |

---

## 1. ⛔ 測った

| 測ったこと | 実測 |
|---|---|
| 並べている場所 | ⛔ **`schedule-layout.ts:513`** の `drawnRows.sort((a, b) => a.depth - b.depth \|\| a.order - b.order)` **ただ 1 か所** |
| その並びが決めるもの | ⭐ `:733` の `rows` がそのまま `:955` の `y += height + rowGap` の順になる。⇒ **並び ＝ 画面の上下** |
| レイアウトが並べる行 | ⭐ **深さ 1〜3 の 68 行**（根 7 ／ 深さ 2 が 19 ／ 深さ 3 が 42）|
| 先頭 8 行の帯高 | 64 / 148 / 188 / 108 / 188 / 148 / 148 / 148 px |
| `Row Area` | y=85、高さ **977px** ⇒ **収まるのは 7 行** |
| **その 7 行の深さ** | ⛔⛔ **全部 1** |
| `order` の振り方 | ⭐ **親ごとに 0 から**（根が 0〜6、深さ 2 の最初の子も 0）。⇒ `order` だけで並べても塊になる |
| 仕様書に「並び順」を定める行 | ⛔⛔ **0 件。**`並び順` `木の順` `前順` `親の直下` はいずれも 0 ヒット |
| `AT-55` の逐語 | 「同じ親の下での並び」 |
| `impact.py T-068 LC-9 AT-55` | 表 T-068 を指すのは 4 箇所（5.5 / 5.6 / 6.1 / A.3）、`LC-9` は 2 箇所、`AT-55` は **0 箇所** |
| `induced.py T-068 LC-9 AT-55 T-058 FR-018 FR-055` | ⭐ **閉路 1 つ（`FR-018` ↔ `FR-055`）。どちらも本変更では触らない** |

### ⚠️ 台帳 D-01 との関係

⭐ **D-01 は「深さ 2 はどの画面にも収まらない」と実測して閉じており、それは全体表示（`FR-055`）の話である。**
⛔ **本件は全体表示ではない。**CR-286 以降、**同梱テンプレートは全体表示に落ちない**ので倍率は 1 のままであり、
**深さ 3 まで並んでいるのに順序のせいで見えない。**⇒ **D-01 の結論は覆らず、本件は別の破断である。**

---

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-068 の `LC-9` | ⭐ 計算 を「**行を木の順に並べ、帯高と縦位置を決める**」、主な入力 に **行の親子**、正 に **`AT-55`** を足す |
| 2 | 表 T-068 直後の散文 | ⭐ **木の順とすること（MUST）／深さの順に並べてはならない（MUST NOT）／`FR-042` の「行の位置」も この順での位置とすること（MUST）** の 1 段落 |
| 3 | `schedule-layout.ts` | ⭐ `drawnGroups` の並べ方を**深さの昇順から木の前順走査へ**替える |
| 4 | `A-appendix.md` | 変更履歴に 1 行 |

⛔ **新しい表も、新しい行 ID も、新しい設定値も足さない。**
⛔ **`AT-55` と `order` の意味は 1 文字も触らない。**

## 3. 数の予測

```
tables   135 → 135   （表を足さない）
figures   11 →  11
rows    1704 → 1704  （行を足さない。既にある LC-9 の中身を厚くするだけ）
uids     149 →  149
語       339 →  339  （表示する語を足さない）
```

⚠️ **`rows` が動いたら、それは表の行を足した証拠であり、本変更の計画外である。**

## 4. 落ちる／緑になる試験

| 試験 | 予測 |
|---|---|
| `tests/unit/layout-engine.test.ts` | ⚠️ **並び順を前提にしている件があれば動く。**深さの塊を期待している件は書き直しが要る |
| `fr-027-startup-shows-its-depth` | ⭐ **「最初の画面に深さ 2 以降が来る」を測る件が、初めて緑になりうる** |
| 検査 28 本 | ⭐ 全部緑のまま（表の員数を動かさない）|
