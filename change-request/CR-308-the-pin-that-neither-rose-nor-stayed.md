# CR-308 — 上端がどこにも無かった —— ピン止めの帯を、行見出しと日程の両方で立てる

> **ファイル名の案**: `change-request/CR-308-the-top-edge-that-was-nowhere.md`
>
> **閉じるもの**: 台帳 D-153（ピン止めした行が上端へ上がらない）と、その欄が「裁定が 3 つ要る」と書いた
> 3 つのうち **2 つ**（「上端」がどの部分の上端か ／ `FR-018` で消えた場合とスクロールで枠外へ出た場合の区別）。
> **開けるもの**: 帯が `Row Area` をどこまで取ってよいか（利用者の裁定が要る。§6 (a)）。
>
> **本 CR は仕様だけを書く。** ⛔ `src/` と `tests/` は 1 バイトも触らない（配線は次の巡）。
>
> **利用者の裁定（2026-08-30、逐語。これが本 CR の芯である）**:
> **「上端は行見出しと日程の両方である。行の名前も、その行のバーも一緒に上端へ上げる。」**
> **「拡大、縮小しても表示を続けるのがピン止めだ。」**

---

## ⛔ 先に 1 つ断る —— **本 CR は既存の 1 段落を反転させる**

`FR-098` はいま逐語でこう書いている:

> **表示量の増減（`FR-018`）でピン止めした行が描かれなくなることを許すこと。** ピン止めは表示量の増減より
> 優先しない —— 優先させると、縮小しても消えない行が残り、`FR-018` が保証する量の上限を破る。

⛔ **利用者の第 2 の裁定はこれを正面から覆す。** 本 CR はこの段落を**残さず差し替える**。
⭐ **その根拠は新しく発明しない** —— **`FR-018` が既に同じ形の免除を 1 つ持っており**、その正当化の 1 文を
そのまま当てる（§1 (c)）。⚠️ **反転の代償**は、この段落が寄りかかっていた「量の上限」の主張が失われることだが、
**その主張は `S-127` を数えていない**（同じく §1 (c)）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

**`CH-2`（ペライチ）／ `GL-002`。**
⭐ **`FR-098` の RATIONALE が自分で書いている** —— 逐語で「**拡大してスクロールすると、詳細を見たいタスクと、
それが繋がる先のマイルストーンや主要な行が同時に見えなくなる**」「**繋がる先を画面に残せないと拡大が使えなくなる**」。
⛔ **実測では、拡大してスクロールした瞬間にピン止めした行が「残らない」どころか「描かれなくなる」**（踏査の報告）。
⇒ **本要求が存在する目的そのものが 1 つも果たされていない。**
⚠️ **`CH-4`（すぐわか）にも触れる** —— 押せて、外せて、上限まで効くのに、**押した結果が画面に出ない**。

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
| --- | --- | --- |
| **`R1.3`**（矛盾・唯一の正）| `FR-098` の 2 つの段落 | ⛔⛔ **同じ要求の中で矛盾していた** —— 第 1 段落が「スクロールする領域から抜いて画面の上端へ固定すること（MUST）」と書き、第 4 段落が「表示量の増減で描かれなくなることを許す」と書く。**実装はこの 2 つ目に寄りかかって、スクロールで落ちたものまで許してしまった**（`row-title-panel.ts` の註が逐語で `FR-018` と `CD-2` を挙げている。§5）|
| **`R1.3`**（唯一の正）| 「上端」の正 | ⛔ **どの表にも無い。**⭐ **ただし仕様は既に答えを持っていた** —— 表 T-076 の `EP-5` が `Pinned Row`（`U-46`）を **`Row Area`（`U-50`）の中身**として数えている。⇒ **帯は `Row Area` の中である**（§1 (d)）|
| **`R1.5`**（MECE・境界条件）| ピン止めした行が描かれない場合 | ⛔ **数え上げが無かった。**実測される経路は 4 つ（LOD ／ 畳み・隠し ／ 行の消滅 ／ **スクロール**）で、**4 つ目を述べた行がどこにも無い。**⇒ **閉じた列挙を書く**（§2 の 2）|
| **`R1.4`**（異常系）| 帯が `Row Area` を食い尽くす場合 | ⛔⛔ **穴である。**`S-127` ＝ 5 と 12 段の拡大が重なると、帯の高さが `Row Area` を超えうる。**残りが負になる。**⇒ **§6 (a) で利用者に問う。決め打たない** |
| **`R2.7`**（DRY）| 帯の幾何 | ⭐ **新しい設定値を 1 つも起こさない** —— **帯高は 表 T-221 の `LF-2`、行の隙間は `rowGap`（`S-12`）が既に持つ。**⭐ **CR-305 が「いちばん左の操作子の左端から行の右端まで」と書いて数を持たずに済ませた手が、そのまま使える**（§1 (e)）|
| **`R1.3`**（唯一の正）| `SC-1` / `EP-5` / `RT-6` との整合 | ⭐⭐ **3 つとも「両方上げる」を支持する。**⛔ **むしろ「行見出しだけ上げる」といまの実装の読みが、この 3 つと衝突していた**（§1 (d)）|

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

| | 決めたこと | 何に載せたか |
| --- | --- | --- |
| 1 | ⭐ **帯を `Row Area` の**中**に置いた**（`Row Area` の矩形そのものは動かさない）| ⛔ **`U-50` を書き換える案を退けた** —— **表 T-076 の `EP-5` がピン止めした行を `Row Area` の中身と数えており**、外へ出すとその行が偽になる。⭐ **`Scrollbars`（`FR-051`）とは逆の扱いである** —— あちらは `Row Area` から場所を取り、こちらは中で取る |
| 2 | ⭐ **畳み（`HR-1a`）と隠し（`HR-6`）はピン止めより強いままにした** | ⛔ **利用者の裁定は「拡大、縮小しても」であって、畳みと隠しを名指していない。**⭐ **`HR-1a` / `HR-6` の MUST NOT には条件が付いていない**ので、広げるほうが発明になる。⚠️ **これを覆すなら利用者の裁定が要る**（§6 (c)）|
| 3 | ⭐ **帯の幾何を 表 T-221 に `LF-14` として 1 行だけ起こした** | ⛔ **`FR-098` の散文に算式を書き並べる案を退けた** —— **要求は表を指し、算式は 表 T-221 が持つ**のが本書の作法である（`FR-018` が `LF-10` を、`FR-017` が `LF-1` を指しているのと同じ形）。⛔ **表 T-206 にも 表 T-203 にも行を起こさない** —— **起こす数が 1 つも無い**（§1 (e)）|
| 4 | ⭐ **表示位置（`S-78` / `S-176`）が指す先を「残りの領域の上端」と定めた** | ⛔ **帯の上端を指すと、帯は流れないので表示位置が二度と動かなくなる。**⚠️ **`S-78` も `S-176` も 1 文字も書き換えない** —— **指す側（`FR-098`）に書く** |
| 5 | ⭐ **書き出し（表 T-076）に行を 1 つも足さず、結びに 1 文だけ足した** | ⛔ **`EP-5` も `EP-3` も `EP-4` も 1 文字も触らない** —— **3 行が既に揃っており、位置は `WY-3` が画面との一致で縛る**（§1 (f)）|
| 6 | ⭐ **表 T-051 の `HF-10` を 1 文字も触らない** | ⚠️ **同行の理由は「`FR-098` は画面の上端に置くので置き場が衝突する」と書いている。**⭐ **上端を `Row Area` の上端と定めると、`HF-10` の操作子（パネルの最上部＝目盛と同じ高さの隅）とは別の高さになり、その MUST NOT はかえって満たしやすくなる。**⛔ **理由の文が少し古びるだけで偽にはならない**ので、触らない |
| 7 | ⭐ **`FR-055` を `FR-018` と同じ 1 回の編集で書く** | ⛔ **`FR-018` ↔ `FR-055` ↔ `HF-7` ↔ `HR-1a` が輪をなす**（`induced.py` の実測。§1 (b)）—— **順に書くと収束しない** |

---

## 1. ⛔ 測った

### (a) `impact.py`（**実際に走らせた出力**）

```
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py \
    FR-098 FR-018 T-076 EP-5 T-031 SC-1 T-018a RT-6 S-126 S-127

FR-098
  指している先 = 表 T-203 T-051 T-002 T-014 T-050 /
                 行 S-127 HF-4 HF-6 HF-9 SO-13 NT-1 ST-7 S-126 CD-2
                 （塊「設定値の相互依存」と塊「占有幅と段」に触れる）
  指している側 = 要求 5 件 / 参照 16 箇所
                 FR-004 FR-031 FR-033 FR-009 FR-088
                 （4.3 削減候補 1・5.3 ファイル構成 1・変更履歴 5・
                   3. UI パーツ 1・7. DocumentCommand 2・8. アイコンの名簿 1）

FR-018
  指している先 = 表 T-005a T-205 T-038 T-201 T-221 T-206 T-109 T-051 T-015 /
                 行 S-86 S-49 LF-10 S-125 S-172 S-173 IC-12 IC-15 IC-10 IC-11
                     S-53 HF-7 HR-1a S-87 S-88
  指している側 = 要求 5 件 / 参照 20 箇所
                 FR-001 FR-004 FR-016 FR-098 FR-055
                 （5.2 コンポーネント 1・5.5 振る舞い 1・Ch7 テスト戦略 1・
                   変更履歴 3・8. アイコンの名簿 4・7. LOD のしきい値 1・8. 保存しないもの 2）

表 T-076
  定義 = 01-04-requirements.md:3188 / 15 行（EP-1 .. EP-15）
  指している要求 5 件 = FR-085 FR-004 FR-051 FR-082 FR-080
  2 次 = 18 件
  要求以外 = 4 件（5.2 コンポーネント / 5.3 ファイル構成 / 変更履歴 7 / 8. 保存しないもの 4）

行 EP-5
  属する表 = T-076
  指している箇所 = 要求 1 件（FR-080）/ 参照 3 箇所

表 T-031
  定義 = 01-04-requirements.md:2385 / 6 行（SC-1 .. SC-6）
  指している要求 2 件 = FR-051 FR-052
  2 次 = 5 件（FR-036 FR-085 FR-004 FR-031 FR-016）

行 SC-1
  指している箇所 = 要求 0 件 / 参照 1 箇所（変更履歴 1）

表 T-018a  ⛔ RT-6 が属するのは T-018a である（T-029 ではない）
  定義 = 01-04-requirements.md:1846 / 7 行（RT-1 RT-2 RT-3 RT-4 RT-4a RT-5 RT-6）
  指している要求 2 件 = UC-004 FR-009
  2 次 = 4 件（FR-081 FR-013 FR-016 FR-049）

行 RT-6
  指している箇所 = 要求 0 件 / 参照 1 箇所（変更履歴 1）

行 S-126 / S-127
  属する表 = T-203（塊「設定値の相互依存」。表 T-203 を変えると塊全体を見ること）
  S-126 を指している = 要求 2 件（FR-032 FR-098）/ 参照 4 箇所
  S-127 を指している = 要求 2 件（FR-033 FR-098）/ 参照 5 箇所
```

⭐ **2 本目**（触る側を全部入れた）:

```
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py FR-055 T-051 HF-9 HR-1a

FR-055
  指している先 = 表 T-051 T-038 T-025 / 行 WY-2 HF-8 MC-7
  指している側 = 要求 9 件 / 参照 26 箇所
                 FR-003 FR-004 FR-031 FR-070 FR-018 FR-098 FR-080 FR-087 FR-063

表 T-051
  定義 = 01-04-requirements.md:1312 / 12 行（HF-1 .. HF-12）
  指している要求 10 件 = FR-085 FR-004 FR-031 FR-013 FR-018 FR-098 FR-055 FR-048 FR-029 FR-076
  2 次 = 41 件
```

⚠️ **`FR-055` は 9 件から指されている。**⛔ **散文を 1 文字でも足すなら、`FR-018` と同じ巡で書くこと**（下の輪）。

### (b) `induced.py`（**実際に走らせた出力**）

```
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py \
    FR-098 FR-018 FR-055 FR-051 FR-080 T-076 EP-3 EP-4 EP-5 T-031 SC-1 SC-2 \
    T-018a RT-6 RT-4a T-203 S-126 S-127 T-051 HF-4 HF-6 HF-7 HF-9 \
    T-015 HR-1a HR-6 T-050 CD-2 T-206 U-46 U-50

seeds in the specification : 31 of 31
edges inside the seed set  : 39
cycles in the induced graph: 2
   size 4 : FR-018 FR-055 HF-7 HR-1a
   size 2 : CD-2 S-126
```

| 輪 | どうするか |
| --- | --- |
| `FR-018` ↔ `FR-055` ↔ `HF-7` ↔ `HR-1a` | ⛔⛔ **本 CR は `FR-018` と `FR-055` の 2 つを触る。**⇒ ⭐ **1 つの計画で 1 度に書く**（規則 02 の 3）。⛔ **`HF-7` と `HR-1a` は 1 文字も触らない** —— 畳みがピン止めより強いままであることは §0③ の 2 で決めた |
| `CD-2` ↔ `S-126` | ⭐ **どちらも 1 文字も触らない。**`FR-098` は `CD-2` を**指すだけ**である |

⭐ **`FR-098` は本 CR のすべての措置が触る。**⇒ ⛔⛔ **`FR-098` の編集も 1 回にまとめること**（規則 02 の 5）。

### (c) ⭐⭐ `FR-018` は既に同じ形の免除を持っている（**逐語**）

`FR-018` の RATIONALE、タスク LOD の段落:

> **幅が期間から出ていない形状を落としてはならない（MUST NOT）** —— 期間がゼロの `Task` は 表 T-201 の `S-49`
> の床で、マイルストーンは 図形の一辺（表 T-221 の `LF-10`）で幅が決まり、**どちらも倍率を下げても縮まない**ので、
> 期間の短さを測っていることにならない。⭐ **落とす向きの単調性はこの免除で壊れない** —— 免除される集合は
> 倍率に依らず一定であり、描く集合は「一定の集合と、縮む集合の和」だからである。

⭐⭐ **ピン止めした集合も倍率に依らない。**⇒ **その 1 文がそのまま通る。**⛔ **新しい理屈を書かない。**

⛔ **いまの `FR-098` の理由（「量の上限を破る」）は成り立たない。** 実測:

| | |
| --- | --- |
| 免除される集合の大きさ | **`S-127` が頭打ちにする（既定 5、上限 8）。**⭐ **踏査で実測済** —— 9 行に順に押して `1,2,3,4,5,5,5`、6 回目から通知が立つ |
| 倍率との関係 | ⭐ **無い** —— **ピン止めは人が押して増え、人が押して減る**（`FR-098` の「同じ操作子で外せる」）。倍率はこの集合を 1 つも動かさない |
| ⇒ 単調性 | ⭐ **壊れない** —— 描く集合は「**高々 5 の一定の集合**と、縮む集合の和」である |
| ⇒ 「量の上限」 | ⚠️ **`FR-055` の下限（深さ 1 と WBS の深さ 1）は、この 5 行ぶんだけ広がる。**⛔ **だから `FR-055` にも同じ免除を書く**（§2 の 4）—— **黙って割らない** |

### (d) ⛔⛔ `SC-1` / `EP-5` / `RT-6` は「両方上げる」と**整合する。むしろ要求している**

| 行 | 逐語（要点） | 両方上げる裁定と整合するか |
| --- | --- | --- |
| **`SC-1`**（表 T-031）| 行見出しパネル: **縦は本体と連動する。横には流れない** | ⭐⭐ **整合する。しかも「両方」でなければ `SC-1` が破れる。**「縦は本体と連動する」は **行見出しと日程の行が同じ高さに在り続けること**を要求している。⛔ **行見出しだけを帯へ上げると、その行の見出しと日程の行が別の高さに立ち、連動が切れる。**⇒ ⛔⛔ **いまの実装（見出しの側の並びだけを留めた順に組み替える）は、`FR-098` を措く前に `SC-1` を割っている**（踏査の実測: DOM の並びは留めた順、画面の並びは自然順）|
| **`EP-5`**（表 T-076）| **`Row Area`（`U-50`）の中身** —— … ／ **`Pinned Row`（`U-46`）** ｜ 描く | ⭐⭐ **整合する。しかも「上端」がどこかを既に答えている。**`Row Area` は `U-50` により **`Schedule Canvas` から `Time Ruler` の帯と余白を除いた、`Rows` が並ぶ領域**である。⇒ **ピン止めした行は日程の側に在り、帯は `Row Area` の中に在る。**⛔ **行見出しの側にしか描かないいまの実装は、この行を果たしていない** |
| **`RT-6`**（表 T-018a）| **端点の一方がピン止めした行にあるときも、依存線を描くこと（MUST）** —— ピン止めした行は描かれているので `RT-4a` の対象外である。⚠️ **縦にスクロールしているあいだは経路を算出し直さなくてよい。止まってから引き直す** —— **ピン止めした行は画面に固定されるので、スクロール中は端点の一方だけが動く** | ⭐⭐ **整合する。これも「両方」でなければ成り立たない。**依存線は `Row Area` の中身（`EP-5` の `U-16`）であり、**端点は日程の側のバーに付く**（`FR-009`「依存線は予定の幾何に付くこと（MUST）」）。⛔ **日程の側を上げないと、スクロールした瞬間に端点が枠外へ出て `RT-4a` に落ち、`RT-6` の MUST が果たせない。**⭐ **註の「端点の一方だけが動く」は、日程の側が固定されていることを前提にした文である** |

⇒ ⭐⭐ **3 行とも、利用者の裁定を追認する。**⛔ **どれも書き換えない。**
⚠️ **ただし `RT-6` は、いまの実装では成立しようがない** —— **日程の画布はピンに対して 1 バイトも描いていない**
（踏査の実測: `svgBytes` と `census()` の 6 つの数が前後で不変）。**これは仕様の穴ではなく配線の穴である。**

### (e) ⭐ 帯の幾何 —— **起こす数は 1 つも無い**

⚠️ **持つ行がどこにも無い**（踏査の報告のとおり。`S-126` は状態、`S-127` は上限だけ）。
⭐ **当たるものを先に測った**（規則 02 の 1 —— **半分以上は「実は在る」**）:

| 要るもの | 当たるもの | 数を起こすか |
| --- | --- | --- |
| **帯の高さ** | ⭐ **表 T-221 の `LF-2`（行の帯高）と `rowGap`（表 T-201 の `S-12`）で書ける** —— 「帯に置く行の帯高を合計し、あいだに `rowGap` を数から 1 を引いた数だけ加える」。⭐ **`LF-2` 自身が同じ形で書かれている**（「段と段のあいだに `stackGap` を段数から 1 を引いた数だけ加える」）| ⛔ **起こさない** |
| **`Time Ruler` との上下** | ⭐ **`U-50` が決めている** —— `Row Area` は `Schedule Canvas` から `Time Ruler` の帯と余白を除いた領域である。**帯はその中の上端**なので、**目盛の下**である。⭐ **`SC-2`（目盛は縦に流れない）とも噛み合う** | ⛔ **起こさない** |
| **`Row Area` を何 px 押し下げるか** | ⭐⭐ **押し下げない。**`Row Area` の矩形は動かず、**押し下げられるのはスクロールする行が並ぶ残りである**（`EP-5` の読み。§0③ の 1）。**残りの高さ ＝ `Row Area` の高さ － 帯の高さ － `rowGap` 1 つぶん** | ⛔ **起こさない** |
| **収まらないときのスクロールの対象** | ⭐ **帯そのものである** —— `FR-098` の第 1 段落が既に「ピン止めした行の並びを縦にスクロールできるようにすること（MUST）」と書いており、**その「並び」がこの帯である。**⚠️ **`HF-9` は行見出しパネル全体の話であって、これとは別である** | ⛔ **起こさない** |
| **帯と残りの境目の線** | ⚠️ **無い。**⭐ **`EP-9` に先例がある**（「`Group Grid Lines`（`U-18`）と同じ線を 1 本引くこと（MUST）—— 新しい確定名も新しい設定値のキーも作らない」）| ⛔ **起こさない**（§2 の 2 で同じ形を採る）|
| **帯が `Row Area` を食い尽くす場合の上限** | ⛔⛔ **無い。**⭐ **数を持たずに述べる案は在る**（「スクロールする行が 1 行も描けなくなってはならない」）が、**割合で切る案は新しい値を 1 つ要する** | ⛔ **§6 (a) で利用者に問う。決め打たない** |

⭐⭐ **CR-305 の手がそのまま効いた** —— **あちらが「いちばん左の操作子の左端から行の右端まで」と書いて `PD-348` の裁定に依らない形にしたのと同じく、本 CR は帯を「既に在る行の帯高と隙間の和」として述べ、数を 1 つも起こさない。**

⚠️ **仮に値が要ることになっても、表 T-203 / T-206 を手で書いてはならない** —— **`_assets/tbl-settings.md` は生成物であり**（冒頭に「⛔ 本書は生成物である。手で直さない」と自ら書いている）、**正は `_source/settings.json` である**。

### (f) ⭐ 書き出し（`EP-*`）—— **述べている行は既に在る。足さない**

| 問い | 実測 |
| --- | --- |
| ピン帯は書き出しに出るか | ⭐ **出る。**`EP-5` が `Pinned Row`（`U-46`）を「描く」としており、`EP-3` が `Row Title Panel` と `Row Title Tree` を「描く」としている。⇒ **行見出しの側も日程の側も出る** |
| どこに出るか | ⭐ **画面と同じ位置である。**`FR-080` の `WY-3` が、**表 T-076 が「描く」とした UI パーツについて、画面上の外接矩形に比を掛けた値と書き出した SVG の中の同じ矩形とが一致すること**を求めている。⇒ **帯の位置は `FR-098` が画面で定めれば、書き出しは自動で従う** |
| ピン止めの操作子は出るか | ⭐ **出ない** —— `EP-4`（行の操作子 `Row Pin`（`U-48`）は描かない）。⚠️ **`HF-6` の地も出ない**（同行が自ら「書き出しには出ない」と書いている）|
| 下端で切る規則（`FR-025`）に食われるか | ⭐ **食われない** —— 帯は上端に在り、`FR-025` は**下端側から** `TaskGroup` 単位で落とす |
| ⇒ 足すか | ⛔ **行は 1 つも足さない。**⭐ **表 T-076 の結びに 1 文だけ足す** —— **ピン止めした行が 2 つの行にまたがる**ことは、いまどこにも書かれていない（§2 の 5）|

### (g) ⚠️ 台帳 D-153 の「裁定が 3 つ要る」の残高

| 台帳が挙げた 3 つ | 本 CR での扱い |
| --- | --- |
| ピン帯の幾何 | ⭐ **閉じる** —— 数を 1 つも起こさずに 表 T-221 の `LF-14` で述べる（§1 (e)）。⚠️ **上限だけが残る**（§6 (a)）|
| 「上端」がどの部分の上端か | ⭐ **閉じる** —— **利用者の裁定（両方）＋ `EP-5` の読み**（§1 (d)）|
| `FR-018` で消えた場合とスクロールで枠外へ出た場合の区別 | ⭐⭐ **閉じる。しかも区別が要らなくなる** —— **`FR-018` では消えなくなる**ので、**残る経路は畳み・隠し・行の消滅の 3 つだけ**であり、`FR-098` にその閉じた列挙を書く（§2 の 2）|

---

## 2. 編集計画（**オブジェクトごとに 1 回**）

| # | 対象 | 何を書くか | 台帳 |
| --- | --- | --- | --- |
| 1 | `01-04-requirements.md` の `FR-018`（**1 回で書く**）| **グループ LOD の段落に、ピン止めした行の免除を足す。**⛔ **単調性の 1 文は §1 (c) の逐語の形に倣う** | D-153 |
| 2 | 同 `FR-098`（**1 回で書く**）| ⑴ **「上端」の定義**（両方・`Row Area` の上端・横は流れる）⑵ **帯と残りの関係**（`LF-14` を指す）⑶ **表示位置が指す先** ⑷ **`FR-018` の免除への差し替え**（既存段落の反転）⑸ **描かれない場合の閉じた列挙** ⑹ **帯の下端の線**。⛔ **第 1 段落（抜いて上端へ固定・二重に描かない・優劣を設けない・収まらないときのスクロール）は 1 文字も書き換えない** —— **既に正しい** | D-153 |
| 3 | `05-07-design.md` の 表 T-221 に **`LF-14` を 1 行足す** | 帯の高さと、スクロールする行が並ぶ残りの高さ。⚠️ **`LF-13` の次に置く。番号は振り直さない** | D-153 |
| 4 | `01-04-requirements.md` の `FR-055`（**`FR-018` と同じ 1 回の巡で書く**）| ⑴ **縦の Fit が収める先を「`Row Area` から帯を除いた残り」にする** ⑵ **最も縮めたときの下限から、ピン止めした行を外す** | D-153 |
| 5 | 同 表 T-076 の**結び** | **ピン止めした行が `EP-3` と `EP-5` の 2 行にまたがること**と、**位置は `WY-3` が縛ること**を 1 文で。⛔ **`EP-3` / `EP-4` / `EP-5` のセルは 1 文字も触らない** | D-153 |
| 6 | `A-appendix.md` 変更履歴 | 版 1 行。⛔ **台帳・裁定待ちの行 ID を backtick で書かない**（検査 7）。⚠️ **版番号は「既に使われている最大値 ＋ 1」。書く直前に max を数え直すこと**（**本稿の時点の実測: 1.61 ⇒ 1.62**）| — |

⛔ **触らないもの（名指しで確認した）**: 表 T-031 の `SC-1` / `SC-2`、表 T-018a の `RT-6` / `RT-4a`、
表 T-051 の `HF-4` / `HF-6` / `HF-7` / `HF-9` / `HF-10`、表 T-015 の `HR-1a` / `HR-6`、表 T-050 の `CD-2`、
表 T-203 の `S-126` / `S-127`、`U-46` / `U-48` / `U-50`、`FR-051`、`FR-009`。

### `FR-018` に足す文（案）—— **グループ LOD の段落の末尾へ**

> **ピン止めした行（`FR-098`）を本要求の対象から外すこと（MUST）。倍率を下げたことを理由に、その行を描くのを
> やめてはならない（MUST NOT）**（利用者の裁定 2026-08-30「拡大、縮小しても表示を続けるのがピン止めだ」）。
> ⭐ **落とす向きの単調性はこの免除で壊れない** —— 免除される集合は倍率に依らず一定であり、描く集合は
> 「一定の集合と、縮む集合の和」だからである。⚠️ **その集合の大きさは `_assets/tbl-settings.md` の 表 T-203 の
> `S-127` が頭打ちにする。**⛔ **人が畳んだ行と隠した行の配下は、この免除の外である**（表 T-051 の `HF-7` と
> 表 T-015 の `HR-1a` / `HR-6`）—— **倍率ではなく人が決めたことなので、本要求の話ではない。**

### `FR-098` に書く 6 つ（案）

**⑴ 「上端」がどこか —— 第 1 段落のすぐ後ろに置く**

> **本要求でいう「画面の上端」とは、`_assets/tbl-glossary.md` の `U-50`（`Row Area`）の上端をいう（MUST）。
> `Time Ruler` の帯より上へ出してはならない（MUST NOT）** —— 目盛の上に日程を置くと、そのバーがどの日付に
> 在るのかを読めなくなる（表 T-031 の `SC-2` は目盛を横だけ連動させている）。
> **行見出しの側と日程の側の両方を、同時に同じ高さへ上げること（MUST）。片方だけを上げてはならない（MUST NOT）**
> （利用者の裁定 2026-08-30）—— 表 T-031 の `SC-1` が行見出しを日程側の行に**縦で**従わせており、
> **片方だけ上げるとその対応が切れる。**⚠️ **横には固定しない** —— 日程の側は本体と一緒に横へ流れ、
> 行見出しの側は流れない（同表の `SC-1` / `SC-2` のまま）。

**⑵ 帯と、スクロールする残り**

> **帯は `Row Area` の中に置き、スクロールする行が並ぶのはその残りとすること（MUST）。スクロールする行を
> 帯の下へ潜らせてはならない（MUST NOT）** —— 潜らせると、同じ場所に 2 つの行が重なる。
> **帯の高さと残りの高さは `05-07-design.md` の 表 T-221 の `LF-14` が持つ。**
> ⚠️ **`Row Area` の矩形そのものは動かない** —— `U-50` は `Schedule Canvas` から `Time Ruler` の帯と余白を除いた
> 領域と定めており、帯はその中に在る。**表 T-076 の `EP-5` がピン止めした行を `Row Area` の中身として数えて
> いるのと同じ読みである。**⛔ **`Scrollbars` と同じ扱いにしてはならない（MUST NOT）** —— あちらは `Row Area`
> から場所を取ると `FR-051` が定めるが、**帯は `Row Area` の中で場所を取る。**
> **帯の下端に、`Group Grid Lines`（`U-18`）と同じ線を 1 本引くこと（MUST）** —— 引かないと、留めた行と
> スクロールする行の境目が読めない。⛔ **新しい確定名も新しい設定値のキーも作らない** —— 表 T-076 の `EP-9` が
> 同じ形を既に採っている。

**⑶ 表示位置が指す先**

> **`_assets/tbl-settings.md` の 表 T-203 の `S-78` と `S-176` が指すのは、スクロールする残りの領域の上端と
> すること（MUST）。帯の上端としてはならない（MUST NOT）** —— 帯は流れないので、そこを指すと表示位置が
> 二度と動かない。⚠️ **帯へ上げた行を `S-78` が指しているときの扱いは 表 T-024a の `OP-10` に従う。**

**⑷ `FR-018` との関係 —— ⛔ 既存の段落を丸ごと差し替える**

いまの段落（**削る**）:

> **表示量の増減（`FR-018`）でピン止めした行が描かれなくなることを許すこと。** ピン止めは表示量の増減より
> 優先しない —— 優先させると、縮小しても消えない行が残り、`FR-018` が保証する量の上限を破る。

置く段落（**案**）:

> **ピン止めした行を、表示量の増減（`FR-018`）で描かなくしてはならない（MUST NOT）**（利用者の裁定
> 2026-08-30「拡大、縮小しても表示を続けるのがピン止めだ」）—— **拡大しても縮小しても表示を続けることが
> ピン止めである。**免除の規則と、落とす向きの単調性が壊れない論証は `FR-018` が持つ。
> ⚠️ **以前は「表示量の増減で描かれなくなることを許す」と定めていた** —— ⛔ **その理由（量の上限を破る）は
> 成り立たない。**免除される集合は `S-127` で頭打ちであり、かつ倍率に依らず一定なので、描く集合は
> 「一定の集合と、縮む集合の和」のままである。

**⑸ 描かれない場合の閉じた列挙**

> **ピン止めした行が描かれないのは、人が畳んだ行の配下にあるとき（表 T-015 の `HR-1a`）と、隠した行の配下に
> あるとき（同表の `HR-6`）に限ること（MUST）。それ以外の理由で描くのをやめてはならない（MUST NOT）。**
> ⚠️ **その行が消えたときは、ピン止め自体が消える**ので、ここには入らない（表 T-050 の `CD-2`）。
> ⛔ **縦にスクロールしたことは理由にならない（MUST NOT）** —— 本要求の第 1 段落が正面から禁じている。
> ⚠️ **畳みと隠しが勝つのは、どちらも人が決めたことだからである** —— **倍率が決めることではない。**

### 表 T-221 に足す 1 行（案）

| 行 ID | 求めるもの | 算式 |
| --- | --- | --- |
| LF-14 | ピン止めした行の帯（`FR-098`）| 帯の高さは、帯に置く行の帯高（`LF-2`）を合計し、行と行のあいだに `rowGap` をその数から 1 を引いた数だけ加えたものとする。**帯へ上げた行は `LF-3` の連なりから除き、抜けた場所は詰める。** スクロールする行が並ぶのは、`Row Area` の高さから帯の高さと `rowGap` 1 つぶんを引いた残りとする |

⛔ **`MUST` / `MUST NOT` を書かない**（規則は `FR-098` が持つ。検査 12 と、表の作法）。
⚠️ **列は 3 つである**（検査 10）。⚠️ **`LF-13` の次に置き、番号を振り直さない。**

### `FR-055` に足す 2 つ（案）

**(i) 縦の Fit の収め先** —— 「**描くものが Row Area に収まる最も深い段を採る**」を次へ:

> **描くものが `Row Area` から ピン止めした行の帯（`FR-098`）を除いた残りに収まる最も深い段を採る。**

**(ii) 最も縮めたときの下限** —— 同段落の末尾へ 1 文:

> ⚠️ **ピン止めした行（`FR-098`）はこの下限の対象外である** —— **免除の規則と単調性の論証は `FR-018` が持ち、
> 数は `_assets/tbl-settings.md` の 表 T-203 の `S-127` が頭打ちにする。**⛔ **黙って割らない。**

⚠️ **`HF-8` は 1 文字も触らない** —— **全体表示が捨てるのは畳みだけであり**（同行の逐語「捨てるのは畳みだけであり、
隠した状態は残す」）、**ピン止めは表 T-027 の `UN-14` により取り消しの履歴に入る人の編集である。**⇒ **捨てない。**

### 表 T-076 の結びに足す 1 文（案）

> ⚠️ **ピン止めした行（`U-46`）は本表の 2 つの行にまたがる** —— **行見出しの側は `EP-3` が、日程の側は `EP-5` が
> 描く。**⛔ **書き出し専用の位置を設けてはならない（MUST NOT）** —— 帯の位置は `FR-098` が画面で定め、
> **書き出しは `WY-3` の一致でそれに従う。**⚠️ **`Row Pin`（`U-48`）は `EP-4` により描かない。**

---

## 3. 数の予測

| | 措置前（実測 2026-08-30。`md-checks.py`）| 予測 | 由来 |
| --- | --: | --: | --- |
| `tables` | 135 | **135** | ⛔ **表を 1 つも増やさない** |
| `figures` | 11 | **11** | ⛔ **図を 1 つも触らない** |
| `rows` | 1738 | **1739** | ⭐ **表 T-221 の `LF-14` の 1 行だけである。**ほかはすべて散文と、既存セルの差し替え |
| `uids` | 150 | **150** | ⛔ **要求も目標も増やさない** |
| アイコン | 85 | **85** | ⛔ **新しい入口も図形も起こさない** —— ピン止めの入口は `IC-60` が既に在る |
| 画面に出す語（ja / en の対）| 387 | **387** | ⛔ **通知を 1 つも増やさない** —— 上限に達したときの通知は `FR-098` が既に持ち、帯が収まらないときは**通知ではなくスクロール**である |
| **設定値の新しい行** | — | **0** | ⭐ **帯高は `LF-2`、隙間は `S-12`、上限は `S-127` が既に持つ**（§1 (e)）|
| 表 T-076 の行 | 15 | **15** | ⭐ **結びだけを書く** |
| 表 T-051 の行 | 12 | **12** | ⛔ **1 文字も触らない** |
| 表 T-203 の行 | — | **不変** | ⛔ **`_assets/tbl-settings.md` は生成物である。触らない** |

### ⭐ 生成物に届くか —— **1 つも届かない**

| 生成器 | 届くか |
| --- | --- |
| `_source/settings_json_to_md.py` | ⛔ **届かない** —— **設定値を 1 つも起こさない**ので `_source/settings.json` は動かない |
| `_source/erd_json_to_md.py` / `erd_json_to_schema.py` | ⛔ **届かない** —— `erd.json` も日程データの群も動かない |
| `tools/generate_display_words.py` | ⛔ **届かない** —— 表 T-233 に行を足さない |
| `tools/generate_icon_glyphs.py` / `generate_icon_roster.py` | ⛔ **届かない** —— 表 T-109 も 図 F-019 も触らない |
| `tools/generate_unit_tree.py` | ⛔ **届かない** —— 表 T-075 を触らない（**新しいユニットを 1 つも起こさない**）|

⇒ ⭐ **`npm run gen` の出力は 1 バイトも動かない見込みである。**⚠️ **規則 02 の作法どおり、生成物を先に消してから
生成し、書かれたことを確かめてから比べること。**

---

## 4. 落ちる／緑になる試験（**実測で名指しした**）

| | 予測 |
| --- | --- |
| ⛔ **`tests/unit/uf-63.test.ts` の「leaves out a pinned row the display amount stopped drawing」** | ⭐⭐ **赤くなる。それが正しい。**同件は逐語で「`FR-098` admits that `FR-018` may stop drawing a pinned row」と書いており、**本 CR がその前提を消す。**⇒ **書き換える対象である**（⛔ **本 CR は書き換えない。仕様が先**）|
| ⛔ **同ファイルの「describes a pinned row with the box the shell measured for it」** | ⭐ **赤くなる。**同件は「`FR-098` fixes the pinned row to the top of the SCREEN, which is a place; **the box is still the one measurement `SC-1` admits**」と書いて、**自然位置の箱をそのまま是としている。**⇒ **帯の箱を要求する件へ書き換わる** |
| ⭐ **同ファイルの 2 件は緑のまま** | 「二重に描かない」「留めた順に並ぶ（`pinnedTitles` の並び）」—— **どちらも本 CR で意味が変わらない** |
| **新しく要る試験** | ⭐ **① 縦にスクロールしても `Pinned Row` が消えないこと**（踏査の `pin-02` がそのまま件になる）／ **② 倍率を最小まで落としてもピン止めした行が描かれること**（`FR-018` の免除）／ **③ 行見出しの側と日程の側が同じ高さに立つこと**（`SC-1`）／ **④ 帯の高さが `LF-14` の算式に一致すること**／ **⑤ 畳んだ行の配下のピン止めは描かれないこと**（`HR-1a` が勝つ）|
| **`tests/unit/fr-055-*.test.ts` の 3 本** | ⚠️ **帯が 0 のときは絵が変わらないので、いまのまま緑の見込みである。**⛔ **ピン止めが在る文書で Fit を測る件が 1 つも無い**（実測）—— **新しく要る** |
| **`tests/unit/uf-72-screen-part.test.ts`（`FR-098` を 26 回名指す）** | ⚠️ **入口と箱の話であり、帯の位置を見ていない**（実測）。⇒ **本 CR では落ちない見込み** |
| **書き出しの試験** | ⚠️ **`image-exporter.ts` は既にピン止めした行を描いており**、その註が逐語で「FR-098 lifts the pinned rows out of the scrolling list and holds them at the top」と書いている。⛔ **ところが渡される箱は自然位置のものである。**⇒ **帯が立てば書き出しは自動で追随する。試験は `WY-3` の側で測ること** |
| **検査 5 〜 10 / 15** | ⚠️ **`rows` が 1738 から 1739 へ動く。** 予測と実測を突き合わせること |
| **検査 11（重複）** | ⛔⛔ **`LF-14` は `LF-2` / `LF-3` と語の並びが近い**（どれも「帯高」「`rowGap`」「合計」を含む）。⇒ **`dup-check.py` の 0.45 でひと組み立つおそれがある。**⭐ **立ったら `docs/review/duplication-baseline.txt`（いま 38 行）へ 1 行足す。**⚠️ **足す前に、本当に別のことを述べているかを読むこと** |
| **検査 12（値の表に MUST を書かない）** | ⚠️ **`LF-14` に `MUST` / `MUST NOT` を書かないこと。**⭐ **`LF-1` が MUST NOT を持って通っているので 表 T-221 は対象外の見込みだが、賭けない** |
| **検査 7（改訂履歴）** | ⚠️ **台帳 D-153 と、開くことになる裁定待ちの行 ID を backtick で書かない** |
| **検査 22（変更要求の作法）** | ⭐ **本稿は `CH-2` / `GL-002` と `R1.3` ほかを名指し、「問わずに決めた」節を持つ**（§0）|
| **検査 25（裁定待ちの印）** | ⛔⛔ **§6 (a) を決めずに進めるなら裁定待ちが 1 つ開く。**⚠️ **その `@provisional PD-nnn` の印は `src/` に要り、本 CR を書いた体は `src/` を書けない** —— ⭐ **前に立つ者が印を置くこと。**（**本稿の時点で使われている最大は PD-395。⇒ PD-396**）|

---

## 5. ⛔ 実装の場所（**本 CR は実装しない。場所を名指すだけ**）

⚠️ **踏査の報告が挙げた 5 か所を自分で開いて確かめた。5 つとも正しかった。**
⛔ **別の体が同時に `src/` を書いているので、行番号は動きうる。名前で探すこと。**

| # | 何 | 場所（**実測で確認**）|
| --- | --- | --- |
| 1 | **配置にピンの概念が 1 語も無い** | `src/entity/layout-engine/schedule-layout/schedule-layout.ts` の `RowPlacement`（:184 付近）と `Schedule*` の `rows`（:229 付近）。⭐ **確認した** —— **`layout-engine` の配下に `pinned` は 1 件も無い**（当たるのは `pinned to` という散文だけ）。**行はすべてスクロールする流れに置かれる** |
| 2 | **帯の矩形が無い** | `src/entity/layout-engine/screen-regions/screen-regions.ts` の `ScreenRegions`（:52 付近）。⭐ **確認した** —— 持つ矩形は `appHeader` / `scheduleCanvas` / `rowTitlePanel` / `timeRuler` / `propertiesPanel` / `rowArea` の **6 つだけ**であり、**帯も、スクロールする残りも無い。**⇒ ⭐ **`LF-14` が入ると、ここに 2 つ目の矩形が要る** |
| 3 | ⛔⛔ **スクロールで消える直接の場所** | `src/framework/single-html-shell/frame-loop.ts` の `rowBoxes` の組み立て（:1895 付近）。⭐ **確認した** —— `top = max(row.y, rowArea.y)` / `bottom = min(row.y + row.height, rowArea.y + rowArea.height)` で切り、**`if (bottom <= top) return []` で切って空になった行を落とす。**⛔ **ピン止めした行を除外する枝が無い。**⚠️ **直上の註は `SC-1` を引いて「縦の対だけを切る」と書いており、切ること自体は正しい** —— **欠けているのはピンの枝である** |
| 4 | **箱を自然位置から取り、黙って落とす所** | `src/adapter/screen-renderer/row-title-panel.ts` の `boxByGroupId`（:571 付近。`session.rowBoxes` から組む）と、ピンの列を作る所（:635 付近の `for (const groupId of pinnedGroupIds)` と `if (group === undefined \|\| box === undefined) continue`）。⭐ **確認した** |
| 4a | ⛔ **その `continue` の註が、本 CR で偽になる** | 同ファイルの :617 付近。逐語で「**`FR-098` admits that the display amount (`FR-018`) may stop drawing a pinned row, and `CD-2` of table `T-050` takes the pin away with the row it points at. Neither is a fault to report from here.**」—— ⛔⛔ **本 CR は前半（`FR-018`）を消す。**⇒ **註ごと書き直す対象である。**⚠️ **踏査が言うとおり、実測された第 3 の経路（スクロール）はこの註のどちらでもなかった** |
| 5 | **自然位置の箱のまま置く所** | `src/framework/dom-screen-surface/dom-screen-surface.ts` の `boxStyle(title.box)`（:2541 付近）。⭐ **確認した。**⚠️ **`fillRowTitleTree`（:2886 付近）はピンを木の先頭へ並べるだけで、位置に触れない** —— ⛔⛔ **だから DOM の並びだけが留めた順になり、画面は自然順のままになる**（踏査の `pin-04`）|
| ⭐ 6（**踏査の 5 か所に無い。本 CR で見つけた**）| **日程の画布がピンを 1 語も知らない** | `src/adapter/screen-renderer/screen-renderer.ts` は `pinnedTitles`（:330）を**行見出しパネルの型としてしか持たない**。**`svg-renderer.ts` に `pinned` は 1 件も無い**（当たるのは目盛の散文 1 件だけ）。⇒ ⛔⛔ **利用者の裁定の「日程の側」は、いま置き場そのものが無い** |
| ⭐ 7（**同上**）| **書き出しは既に帯を前提に書かれている** | `src/adapter/image-exporter/image-exporter.ts`（:474 付近）が逐語で「**`FR-098` lifts the pinned rows out of the scrolling list and holds them at the top, so the bottom edge does not reach them and the cut above does not count them.**」と書き、`pinnedTitles` を先に描いている。⛔ **ところが渡す箱は自然位置のものである。**⇒ ⭐ **帯が立てば、ここは 1 行も直さずに正しくなる見込みである** |

---

## 6. ⛔ 決めずに残したもの

### (a) ⛔⛔ **帯が `Row Area` をどこまで取ってよいか** —— **利用者の裁定が要る**

⭐ **在るのは算術だけである。**`S-127` は 5（上限 8）であり、**行軸を 12 段拡大した状態**（踏査が実際に作った状態）
では、**5 行ぶんの帯が `Row Area` の高さを超えうる。**⇒ **`LF-14` の「残り」が負になる。**

| 案 | 形 | |
| --- | --- | --- |
| ⭐ **推奨** | **スクロールする行が 1 行も描けなくなってはならない（MUST NOT）。超えるぶんは帯の中を縦にスクロールさせる**（`FR-098` が既に持つ MUST がこれを担う）| ⭐⭐ **新しい数を 1 つも起こさない。**⭐ **CR-305 の手である。**⚠️ **「1 行」がどの行かは、残りの領域の上端に来る行である** |
| 割合で切る | **帯は `Row Area` の高さの半分（など）を超えない** | ⛔ **新しい値を 1 つ起こす**（表 T-206 か 表 T-203）。⛔⛔ **`_assets/tbl-settings.md` は生成物なので、`_source/settings.json` を書くことになる** |
| 何も書かない | 上限を置かない | ⛔ **`LF-14` の残りが負になる場合を仕様が持たないまま残る。**⛔ **`R1.4`（境界条件）が落ちる** |

⛔ **前に立つ者が利用者に問うこと。**⚠️ **決めずに進むなら裁定待ちを 1 つ開き**（本稿の時点の max は **PD-395** ⇒ **PD-396**）、
**その印は前に立つ者が `src/` に置く**（検査 25）—— **本 CR を書いた体は `src/` を書けない。**

### (b) ⚠️ **帯の中の並びが、留めた順のままでよいか**

⭐ **`FR-098` は既に「固定した順に上から並べる」と定めており、本 CR は 1 文字も触らない。**
⚠️ **ただし、両方を上げると新しい見え方が 1 つ生じる** —— **帯の中では、自然順で下に在った行が上に来ることがある。**
⛔ **依存線（`RT-6`）はその向きで引かれることになる**（**上から下への依存が、帯の中では下から上に見える**）。
⭐ **`RT-6` は経路を `T-018` / `LF-5` に委ねており、向きの禁止を持たない**ので、**仕様は破れない。**
⚠️ **実物で見るまで、読みにくいかどうかは分からない。**⛔ **本 CR では決めない。**

### (c) ⚠️ **畳んだ行の配下をピン止めしたとき、どちらが勝つか**

⭐ **本 CR は「畳み・隠しが勝つ」と決めた**（§0③ の 2。⛔ **覆してよい**）。
⚠️ **利用者の裁定は「拡大、縮小しても」であって、畳みを名指していない。**
⛔ **逆の読み（ピン止めが畳みにも勝つ）を採ると、`HR-1a` の MUST NOT（畳んだ行の配下を描いてはならない）と
正面から衝突し、`HF-7` ↔ `HR-1a` の輪へ編集が広がる。**⇒ ⭐ **狭いほうを採った。**
⚠️ **覆すなら、`FR-018` / `FR-055` / `HF-7` / `HR-1a` の輪を 1 つの計画で 1 度に書くことになる。**

### (d) ⚠️ **`HF-10` の理由の文が少し古びる**

⭐ **同行の MUST NOT（ピン止めの操作子と重ねてはならない）は真のまま残る。**
⚠️ **その理由の文（「`FR-098` はピン止めした行を画面の上端に置き…置き場が衝突する」）は、上端を `Row Area` の
上端と定めると、衝突の見立てが弱くなる** —— **`HF-10` の操作子はパネルの最上部（目盛と同じ高さの隅）に在り、
帯はその下に立つからである。**
⛔ **本 CR では触らない** —— **禁止そのものは依然として意味を持ち（帯の右端に `Row Pin` が並ぶ）、
触ると `FR-004` から 表 T-051 へ伸びる 10 本の参照の巡に入る。**

### (e) ⚠️ **ピン止めした行が、全体表示（`FR-055`）を跨いで残ることの是非**

⭐ **`HF-8` は畳みだけを捨てると自ら書いており、ピン止めは 表 T-027 の `UN-14` により人の編集である。**
⇒ **残す。**⛔ **本 CR は `HF-8` を 1 文字も触らない。**
⚠️ **代償**: **ピン止めが 5 行在ると、Fit が収める先がその 5 行ぶん狭くなる。**
⭐ **`FR-055` は「必ず収まることを保証しない」と自ら書いている**ので、**仕様は破れない。**

---

## ⚠️ 編集のときの罠（規則 02 の 4。**本 CR に固有のもの**）

- ⚠️ **`****` を作らない**（空白を 1 つ入れる）／ **CJK の直後に `**` を開いてその直後へコードスパンを置かない**
- ⚠️ **「表 X の N 行」と書かない** —— **行 ID を名指す**（`LF-14` と書く）
- ⚠️ **変更履歴の行にリテラルの縦棒を書かない**
- ⚠️ **番号を振り直さない** —— **`LF-14` は `LF-13` の次であり、既存の `LF-*` は 1 つも動かさない**
- ⛔ **`LF-14` に MUST / MUST NOT を書かない** —— **規則は `FR-098` が持つ**
- ⛔ **改訂履歴に台帳・裁定待ちの行 ID を backtick で書かない**（検査 7）
- ⚠️ **版番号は書く直前に max を数え直す**（**本稿の時点で 1.61 ⇒ 1.62**）
- ⛔ **`_assets/tbl-settings.md` を手で直さない** —— **生成物である。正は `_source/settings.json`**
- ⛔ **`FR-098` の第 1 段落を書き換えない** —— **既に正しい。壊れているのは実装である**
- ⛔ **`FR-018` と `FR-055` は 1 つの巡で書く**（輪。§1 (b)）
