# CR-276 — マーカーに釘付けだった再開アイコンと、一回り大きい三角

> **閉じるもの**: ⛔ **利用者の指摘 1 件**（`D-88`）と、**赤い試験 4 件の裁定済みの原因**（`D-99`）。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`（画面から直に触れる）と `CH-1`（意図どおりに読める）。**
⛔ **掴んでも 1px も動かないアイコンは、掴めると言っていながら掴めていない。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `LF-11` | ⛔⛔ **再開アイコンの位置が `actualStart` ＋ `actualDuration` の関数であり、`resume` を 1 度も読んでいない。**⇒ **表 T-023d の `GR-8`（掴めば `resume` を変える）が原理的に成り立たない** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| `PL-3` との関係 | ⭐ **先例が在った** —— 表 T-022 の `PL-3`（イナズマ線の頂点）は既に `resume` の日に描かれている（`schedule-geometry.ts:1106`）。**新しい原理を持ち込まずに揃えられる** |
| **`R1.4`** | 矢じりの底辺 | ⛔ **底辺の幅を持つ行がどこにも無い。**`S-19` 1 つが長さ・底辺・走りの 3 つを兼ねている |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`resume` を持たないときの落ち先は「マーカーの外側」（＝ いまの位置）** | ⭐ **掴めなくなる行が 1 つも出ない。**表 T-019 の `PA-4`（再開日が未定の中断）だけがこの枝に入る |
| 2 | ⭐ **行を足さず `S-19` を 10 → 7 にした（案 ①）** | ⛔ **底辺の行を新設する案 ② は、表 T-201 に行が増えて**保存する文書のスキーマが動く**（検査 17）。**⭐ **`S-19` は仕様が自ら「10px の由来は無い」と書いており、覆すべき論証が無い** |
| 3 | ⚠️ **7 は「一回り」を 30% 減として置いた暫定値** | ⭐ **`🔎` を付けたまま**。⛔ **下限は `dependencyWidth` × 2 なので 7 は範囲内である** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 再開アイコンの x（前）| `schedule-geometry.ts:666` の `marker.centre.x + marker.radius + markerGap` ⇒ ⛔ **`resume` を読まない** |
| ⇒ 帰結 | ⛔⛔ **`resume` をどう書いてもアイコンは 1px も動かない。**絶対読みは「右へ引くと日付が早くなる」ことがあり、相対読みは「離すとマーカーの隣へ戻る」。**どちらも成立しない** |
| ⇒ 塞いでいるもの | ⭐ **ドラッグの読み方ではなく `LF-11` である** |
| `TaskGeometry.resume` | ⭐ **既に公開メンバーである**（`schedule-geometry.ts:161`）⇒ ⛔ **体の見積り「新しいメンバーが要る（分類 `E`）」は覆った。分類は `C`** |
| `S-19` が兼ねるもの | **3 つ** —— 三角の長さ・三角の底辺（`M0,0 L10,5 L0,10 Z` の二等辺）・依存線の走り（`LF-4`）|
| ⇒ 7 にしたときの実測 | 三角 **7×7** ／ 入口の走り **14px**（`S-19` × `S-20`）／ 出口の走り **7px** |
| `induced.py`（`LF-11` `T-221` `S-19` `S-20` `GR-8` `FR-044` `T-022` `PL-3`）| ⭐ **閉路 0。**⇒ **1 つずつ書いてよい** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-221 の `LF-11` | ⭐ **再開アイコンは `resume` の日に置く**（縦は予定バーの中心）＋ **`resume` が無いときだけマーカーの外側** ＋ **釘付けにできない理由** ＋ **代償** |
| 2 | `_source/settings.json` の `S-19` | ⭐ **既定 10 → 7**。備考に **3 つを兼ねること**と、**走りが一緒に縮むのが害でない理由** |
| 3 | `A-appendix` の変更履歴 | 1 行（`1.28`）|

⛔ **`S-20` は 1 文字も動かさない** —— **比が保たれるので釣り合いは崩れない。**
⛔ **表 T-201 に行を足さない** —— 足すと保存する文書のスキーマが動く。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1718 / 148 | **同じ** | **0** —— **行を足さず、既定値 1 つとセル 1 つを直す** |

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1718 / 148 | **135 / 11 / 1718 / 148** | ✅ |
| 検査 22 本（**17 を含む**）| 全緑 | **ALL GREEN** | ✅ |
| 予測から外れたもの | — | **無し** | ✅ |

## 5. 標準規則への答え

- **①（目的に沿う）**: ⭐ `GL-006`。**掴めると描いておいて動かないのは、いちばん質の悪い嘘である。**
- **②（影響をグラフで測ってから書く）**: ⭐ `induced.py` は閉路 0 と答えた。順に書いた。
- **⑧（実測を書く）**: ⭐ **§1 が実測である。**⛔ **体の見積り（分類 `E`）を実測で覆したことも残した。**

## 6. 残るもの

⛔ **本変更要求は仕様だけである。**⭐ **実装が要るもの**: `schedule-geometry.ts` の再開アイコンの x を
`resume` の日から求めること（`PL-3` と同じ算術）と、`svg-renderer.ts` が新しい `S-19` を読むこと。
⭐ **`t-023d-follows-the-pointer` の赤 5 件のうち 4 件がこれで閉じる見込みである。**
