# CR-408 — プロパティパネルの名称と備考は折り返して縦に伸び、境界は掴んで動く

> **閉じるもの**: 利用者の裁定 `JDG-159`（2026-09-17 の七通目）を仕様へ着地させ、幅が動かない不具合を `DFC-644` として台帳に記録する。逐語は `docs/development-records/rulings.md` の `JDG-159` が持つ。
> - 七通目: 「プロパティーパネルの名称と備考が右にはみ出している。」「はみ出さないようにしろ。」「名称が長い場合は縦に伸ばせ」「また、プロパティーパネルの幅の調整ができない。」「そもそもプロパティーパネルの掴み幅が狭いし、つかんでも動かない。」「これもどこかのタイミングで直せ。 みっともない。」
>
> ⭐ **本書は仕様だけを当てる。** `FR-006` に `文字` と `複数行` の欄をパネルの幅に収めて折り返す段と、閉じる入口を欄に重ねない MUST NOT を足し、`FR-052` にプロパティパネルの幅を「押した時点に描かれていた幅」から数える段を足し、表 T-023d に `GR-22`（`Panel Divider` の掴み帯）を立てた。`S-193` を「行数の下限」に、`S-134` の注に `GR-22` を足した。読んだ木は `a422e16f`。
> ⚠️ `src/`・`tests/`・`tools/` は触っていない。⭐ 幅が動かない原因は `src/` を読んで反証した（第 1.2 節）。⛔ 実物のブラウザでは押していない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`**（マニュアルを読まずに使える、`CH-4`）—— 右へはみ出した欄は値の終わりが読めず、× が重なった欄は押した先が読めない。掴めない境界は、`FR-052` が約束する「幅を変える」を人に見せない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（異なる節の暗黙の矛盾）** —— `FR-006` の「1 つの操作子に、その値を出すのに要る幅より狭い幅を割ってはならない（MUST NOT）」は、長さの決まらない名称と備考にも当たり、値が長いほど欄がパネルの外へ伸びる（第 1.1 節）。⭐ `文字` と `複数行` の型にはこの MUST NOT を当てず、パネルの幅に収めて折り返す段を足した（決定 1）。
- **`R1.3`（網羅）** —— 表 T-023d（`MK-9a` が「掴み領域の全数と優先順位は表 T-023d が持つ」と定める）に `Panel Divider` の掴み帯の行が無く、`FR-051` が同じ場所に縦の `Scrollbars` を接して置くので、どちらが勝つかがどこにも無かった。⭐ `GR-22` を立てた（決定 4）。
- **`R1.1`（曖昧さ）** —— `FR-052` の「その時点のポインタ位置が決める 2 つの幅」は、`S-80` が 0 のまま `S-171` の幅で描かれているパネルで、幅をどこから数えるかを言っていなかった。⭐ 押した時点に描かれていた幅から数える（MUST）、保存された `S-80` から数えない（MUST NOT）と書いた（決定 5）。
- **`R1.4`（境界値）** —— `S-80` が 0 のとき（`S-171` の幅から数える）、値に改行が無い長い名称（折り返しは見せ方）を書いた。
- **`R3.x` は当てていない** —— 本書は実装を変えない。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | `FR-006` の「要る幅より狭い幅を割ってはならない」を、表 T-016 の `入力の型` が `文字` と `複数行` の操作子には当てない。その操作子はパネルの幅の中に収め（MUST）、右へはみ出させず（MUST NOT）、入り切らない値は折り返して欄を縦に伸ばす（MUST）| ⭐ 七通目「はみ出さないようにしろ」「名称が長い場合は縦に伸ばせ」と `JDG-159` の読み「備考も同じにする」。⭐ 型で決め、行（`PR-1` / `PR-2` / `PR-18` / `PR-21`）を数え上げない（`FR-006` の「本文がその欄の値を数え上げてはならない」と同じ流儀）| `PR-18`（行の名前）と `PR-21`（コメントボックスの本文）も同じ扱いになる |
| 決定 2 | 折り返しは見せ方であり、値に改行を足さない（MUST NOT）| ⭐ `文字` の値は 1 行の文である（`Task/Name`）。⚠️ 導いた条項である | 無し |
| 決定 3 | `S-193`（複数行の欄が見せる行数、2）を「行数の下限」と読み替え、値の名と注を直す | ⭐ 「縦に伸ばせ」と、`S-193` が固定の行数のままでは伸びないこと | 無し |
| 決定 4 | 表 T-023d に `GR-22`（`Panel Divider` の掴み帯、幅 `S-134`）を `GR-19` の直後に立て、帯の下に何が描かれていても帯が勝つとする。帯の幅のうち押して届かない所を残さない（MUST NOT）| ⭐ 第 1.2 節の反証 —— 帯 8px の両半分が、縦の `Scrollbars` の帯とプロパティパネルに覆われている。⭐ `GR-19`（パレットの掴み帯）が同じ書き方で帯を勝たせている。⛔ `S-134` の 8px は変えない —— 利用者は値を指定しておらず、覆いを除けば掴める幅はいまの 2 倍（4px → 8px）になる | 境界の近くに置いた入口（行見出しパネルの右端の操作子など）が、帯の半分の幅だけ押せなくなることがある（第 9 節の問い 2）|
| 決定 5 | `FR-052` に、プロパティパネルの幅は押した時点に描かれていた幅（`S-80` が 0 なら `S-171`）から、ポインタが動いたぶんだけ変える（MUST）、保存された `S-80` から数えない（MUST NOT）を足す | ⭐ 第 1.2 節の反証 —— 実装が保存された 0 から数えるので、右へ引くと負で拒まれ、左へ引くと動いた距離の幅へ跳ぶ。⭐ `FR-052` の STATEMENT「その時点のポインタ位置が決める 2 つの幅で画面を描いて示す」を満たすには、描かれた境界がポインタに付いて行く必要がある。⭐ 行見出しパネルの側は実装が既に「描かれた境界から数える」（`input-command-translator.ts:1938`〜`:1955` の TRAP 注と `rowTitlePanelWidthAfterDrag`）| 無し |
| 決定 6 | 閉じる入口（`IC-52`）をどの欄にも重ねてはならない（MUST NOT）とだけ書き、置き場は決めない | ⭐ `JDG-159` の読み「× が名称の欄に重なっている ⇒ 一緒に直す」。⚠️ 置き場は `PND-327`（最初の項目の行の右端、暫定）のまま —— 仕様に置き場の表が無い | 無し |
| 決定 7 | 幅の不具合を 1 行（`DFC-644`）で台帳に記録し、状態を `実装待ち` とする | ⭐ `JDG-159` の読み「不具合として台帳に記録し、②b で直す」。⭐ 仕様（本書）が当たったので、残るのはコードの仕事 | 無し |

---

## 1. 読んで確かめた事実

### 1.1 名称と備考が右へはみ出す理由

| 事実 | 所（`a422e16f`） |
|---|---|
| `文字` と `複数行` の操作子は `min-width: <要る幅> em` を持つ。要る幅は値の長さ × `labelCoef` ＋ `S-199` | `src/framework/dom-screen-surface/dom-screen-surface.ts:329`〜`:335`（`propertyControlStyle`）、`src/adapter/screen-renderer/properties-panel.ts:393`、`:397`〜`:402`（`widthOf`）（読むだけ）|
| ⇒ 値が長いと `min-width` がパネルの幅を超え、欄が右へはみ出す。仕様の根は `FR-006` の「要る幅より狭い幅を割ってはならない（MUST NOT）」 | `docs/spec/01-04-requirements.md` の `FR-006`（当てる前 `:1817`）|
| 閉じる入口（`wayOut`、`margin-left:auto`）は最初の欄の行（名称の行）の中に足される ⇒ 名称の欄が伸びると × と重なる | `dom-screen-surface.ts:1533`〜`:1538`、`:234`〜`:240` |
| `複数行` は `rows = S-193`（2）で描く | `dom-screen-surface.ts:1415`〜`:1417` |

### 1.2 ⭐ 幅が動かない原因の反証（`JDG-159` の読み「掴み代の幅か、押しが幅の変更に届いていないか」）

`src/` を読んで、仮説を 3 つ当てた。⛔ 実物のブラウザでは押していない。

| 仮説 | 判定 | 根拠（`a422e16f`）|
|---|---|---|
| ① 掴み代が狭い | ⭐ **真**（覆いのため） | 帯は境界を中心に幅 `S-134` = 8px（`src/adapter/screen-renderer/screen-frame.ts:21`〜`:43`、`x = 境界 − 4`）。帯は `frameLayer` に置かれ、同じ層で帯の後に縦の `Scrollbars` の帯を足す（`src/framework/dom-screen-surface/dom-screen-surface.ts:861`〜`:876`、後に足したものが上）。縦の帯は `x = パネルの左端 − 太さ`、幅 = 太さ（`screen-frame.ts:90`〜`:95`）で、太さは `S-205` = 8px 以上（`src/framework/single-html-shell/single-html-shell.ts:210`〜`:221`）⇒ 帯の左半分 4px を覆う（`pointer-events:auto`、`dom-screen-surface.ts:456`）。プロパティパネルの要素は `frameLayer` より後に足され（`:2095`〜`:2108`）、`pointer-events:auto`（`:472`〜`:475`）で帯の右半分 4px を覆う。押された場所は `elementFromPoint` で読む（`:2791`〜`:2793`）⇒ **帯に届くのは、縦の帯が無い所（`Time Ruler` の横の高さ `rulerHeight` と、`Row Area` の下の帯）の左半分 4px だけ**である（縦の帯は `rowArea.y` から `rowArea.height`、パネルと帯は `canvas.y` から `canvas.height`。`src/entity/layout-engine/screen-regions/screen-regions.ts:151`〜`:168`）|
| ② 押しが幅の変更に届いていない | **偽** | 帯を押すと `dividerPanel` が読まれ（`dom-screen-surface.ts` の `data-panel`、`src/framework/single-html-shell/frame-loop.ts:3628`）、`commandFromPanelDivider` が `setPanelWidths` を出す（`src/adapter/input-command-translator/input-command-translator.ts:1652`、`:1914`〜`:1936`）。掴んでいるあいだも絵を出す道に入る（`frame-loop.ts:353`、`:2212`〜`:2236`）|
| ③ 幅は書かれるが、数える起点が違う | ⭐ **真** | 翻訳は `settings.propertyPanelWidth − travelled` で幅を出す（`input-command-translator.ts:1931`〜`:1934`）。`settings` は保存された文書（`frame-loop.ts:2505` の `held.document`）。一度も引いていない文書の `propertyPanelWidth` は 0（`src/framework/single-html-shell/startup-template.json:41088`、`S-80` の既定）で、パネルは `S-171` = 280px で描かれる（`frame-loop.ts:1639`〜`:1651`）⇒ **右へ引くと負の幅**になり書き込みが拒み（`src/use-case/edit-document/edit-document-settings.ts:175`〜`:177`）、拒まれた押しは絵を出さない（`frame-loop.ts:2232`）ので**動かない**。**左へ引くと、動いた距離そのもの（例: 10px 引けば 10px）の幅**へ跳ぶ |

⇒ **原因は ① と ③ の 2 つ**。どちらか 1 つを直しても治らない —— ① だけを直せば掴めるが右へは動かず、③ だけを直せば 4px の細い所でしか掴めない。

| 事実（そのほか）| 所 |
|---|---|
| 表 T-023d に `Panel Divider` の行は無い（`GR-19` 〜 `GR-21`）| 当てる前の `docs/spec/01-04-requirements.md:3278`〜`:3298` |
| `S-134` を指す要求は 0（`impact.py S-134`「行が浮いている可能性がある」）| 第 3 節 |
| `T-023d` の `GR-22`、`DFC-644` は `docs/` `src/` `tests/` `tools/` `.claude/` `change-request/` を引いて 0 件 | `grep -rn`（当てる前）|

## 2. 測った

⛔ 本書は描画も時間も測っていない。第 1.2 節は読んで反証した。
⭐ **仕様だけの試験**（`npx vitest run`、本書の 4 本を当てた木）: 238 ファイル中 3 ファイルの 3 件が赤 —— すべて 表 T-023d の行の写しを主張する試験である（第 6.3 節）。

## 3. グラフ

**当てる前**: `impact.py FR-006 FR-052 S-134 T-023d` —— `FR-006` を指す要求 5、`FR-052` を指す要求 6（`FR-085` / `FR-004` / `FR-031` / `FR-016` / `FR-051` / `FR-039`）、`S-134` を指す要求 0、表 T-023d を指す要求 21。
**`induced.py`**（24 種、`CR-405` の第 3 節と同じ実行）: 本書の員のうち `S-80` と `S-171` が大きさ 2 の閉路を作る。⭐ 本書は 2 つとも書き換えていない（`FR-052` の新しい段が指すだけ）。
**届いた行を `rulings.md` で引いた**（`FR-052` / `S-134` / `S-80` / `S-171` / `FR-006` / `パネルの境界`）: `JDG-133`（行見出しパネルは掴んでいるあいだも床で止める）と `JDG-159`。本書は行見出しパネルの床を変えない ⇒ 衝突 0。

## 4. 当てた中身

| 対象 | 置き場（当てたあと） | 中身 |
|---|---|---|
| `FR-006` の段 | `docs/spec/01-04-requirements.md:1824`〜`:1828` | `文字` / `複数行` はパネルの幅に収める（MUST）、はみ出さない（MUST NOT）、折り返して縦に伸ばす（MUST、`S-193` が下限）、改行を足さない（MUST NOT）、閉じる入口を欄に重ねない（MUST NOT）|
| 表 T-023d の `GR-22` | `:3298` | `Panel Divider` の掴み帯。帯が勝つ。押して届かない所を残さない（MUST NOT）|
| `FR-052` の段 | `:3631`〜`:3633` | 押した時点に描かれていた幅から数える（MUST）、`S-80` から数えない（MUST NOT）、優先は `GR-22` |
| `S-193` | `docs/spec/_source/settings.json`（→ `_assets/tbl-settings.md:301`）| 値の名を「行数の下限」へ、注に下限であることを足した |
| `S-134` | 同（→ `:307`）| 注に「優先は `GR-22`、本行は幅だけ」を足した |
| `docs/development-records/defects.md` の `DFC-644` | 末尾 | 幅が動かない不具合と、反証した原因 |

## 5. 数の予測

本書の差: tables 0 ／ figures 0 ／ **rows +1**（`GR-22`）／ uids 0。4 本の合計の予測と実測は `CR-405` の第 5 節。
⭐ 本書が足した `（MUST）` は 3、`（MUST NOT）` は 5（`FR-006` の段 MUST 2 と MUST NOT 3、`GR-22` の MUST NOT 1、`FR-052` の段 MUST 1 と MUST NOT 1）。

## 6. 影響

### 6.1 仕様

| 所 | 本書で |
|---|---|
| `FR-006` の「同じ行に並ぶ操作子をすべてその幅で並べられないときは、その行を折り返す」| そのまま —— 日付・数値・選択の欄に当たる |
| `FR-051` の縦の `Scrollbars` の置き場 | そのまま —— 帯との優先は `GR-22` |
| `FR-039` の行見出しパネルの床（`JDG-133`）| そのまま |
| `PND-327`（閉じる入口の置き場）| そのまま（決定 6）|

### 6.2 記録

| 所 | 書いたこと |
|---|---|
| `docs/development-records/rulings.md` の `JDG-159` | 状態を 適用済 へ、着地先に本書と行 ID を足した |
| `docs/development-records/defects.md` | `DFC-644` |
| `docs/development-records/changelog.md` | 本書の 1 行 |

### 6.3 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `tests/unit/t-023d-double-click-only-rows.test.ts:640`、`tests/unit/uf-30-31.test.ts:693` | ⚠️ **本書の木で赤**（実測）—— 表 T-023d の行の写し `T_023D` に `GR-22` を `GR-19` の直後へ足す |
| `tests/unit/t-023d-follows-the-pointer.test.ts:952` | ⚠️ **本書の木で赤**（実測）—— `GR-22` を追従する行（`FOLLOWING_ROWS`。`FR-052` が掴んでいるあいだ幅を描くと定める）へ数える |
| `src/framework/dom-screen-surface/dom-screen-surface.ts:329`〜`:335` と `src/adapter/screen-renderer/properties-panel.ts:393` | `文字` / `複数行` の操作子に `min-width: <要る幅> em` を当てない。`文字` は折り返して高さが伸びる操作子（`textarea` で改行を打たせない等）にする。`複数行` は `S-193` を最低の行数にして中身に合わせて伸ばす |
| `dom-screen-surface.ts:1533`〜`:1538` | 閉じる入口を欄に重ねない（例: 名称の行で欄と入口を別の列にする）|
| `dom-screen-surface.ts:861`〜`:876`、`:2095`〜`:2108` | `Panel Divider` の帯を、縦の `Scrollbars` の帯とパネルより上に置く（`GR-22`）|
| `src/adapter/input-command-translator/input-command-translator.ts:1931`〜`:1934` | プロパティパネルの幅を、押した時点に描かれていた幅（`S-80` が 0 なら `S-171`）から数える。行見出しパネルの `rowTitlePanelWidthAfterDrag`（`:1943`〜`:1955`）と同じ形 |

## 7. 試験が主張すべきこと

⭐ 名前は本書のとおりに写すこと: 表 T-023d の `GR-22`（`Panel Divider` の掴み帯）、`S-134`（8px）、`S-80`、`S-171`（280px）、`S-193`（行数の下限 2）、`IC-52`。

1. `FR-006`: 名称（`PR-1`）に、パネルの幅の 3 倍の長さの値を持つ `Task` を選ぶと、名称の操作子の右端がパネルの右端（内側の余白 `S-192` を除く）を超えず、操作子の高さが 1 行ぶんより高い。⭐ 古い実装（`min-width` で右へ伸びる）を赤にする。
2. `FR-006`: 備考（`PR-2`）に長い値を入れると、同じく右へはみ出さず、見せる行数が `S-193` 以上に増える。値に改行は足されない（保存した `notes` が入れた文字列と同じ）。
3. `FR-006`: 閉じる入口（`IC-52`）の矩形が、どの欄の操作子の矩形とも交わらない（長い名称のときも）。
4. `GR-22` / `DFC-644` の ①: プロパティパネルを出し、境界の x を中心に −4px 〜 +3px の 8 つの x（`Row Area` の高さの中ほどの y）を押すと、どれも `dividerPanel = 'propertiesPanel'` を読む。⭐ 古い実装（帯の左半分は縦のスクロールバー、右半分はパネルを読む）を赤にする。
5. `FR-052` / `DFC-644` の ③: `S-80` が 0 の文書でパネルを出し（280px で描かれる）、境界を右へ 40px 引いて離すと、幅が 240px になる。左へ 40px 引いて離すと 320px になる。掴んでいるあいだの絵も同じ幅で描かれる。⭐ 古い実装（右は動かない、左は 40px へ跳ぶ）を赤にする。
6. `FR-052`: `S-80` が 300 の文書で、境界を押して動かさずに離すと、書き込みは何も動かさない（幅 300 のまま）。
7. 表 T-023d の行の順: `GR-19` の次が `GR-22` である。

## 8. 関係 —— `JDG-159`・`DFC-644`

- ⭐ **`JDG-159` を着地させる** —— ①「はみ出さないようにしろ」「名称が長い場合は縦に伸ばせ」は `FR-006` の段と `S-193`、× の重なりは `FR-006` の MUST NOT、②「掴み幅が狭いし、つかんでも動かない」は `GR-22`（掴み幅）と `FR-052` の段（動かない）で、コードの仕事は `DFC-644` が持つ。

## 9. ⛔ この変更でやらないこと ／ 返す問い

- ⛔ `S-134` の値（8px）と `S-171` の値（280px）を変えない。
- ⛔ 閉じる入口の置き場（`PND-327`）を決めない。
- ⛔ `src/`・`tests/`・`tools/` を編集しない（第 6.3 節）。

**返す問い**（どれも推奨で当ててある）:

1. **掴み帯を 8px より広げるか** —— 覆いを除けば掴める幅は 4px → 8px になる。推奨: まず 8px で直して見てから決める。
2. **帯が勝つことで、境界の近くの入口が押せなくなってよいか**（決定 4）—— 行見出しパネルの右端に接する操作子があれば、帯の半分（4px）だけ押す場所が減る。推奨: 帯が勝つ（掴めない境界のほうが困る）。案 (b): 入口を帯より上に置き、帯の優先を `Scrollbars` とパネルに対してだけにする。
