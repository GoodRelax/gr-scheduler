# CR-374 — 日付を変える 4 つの掴みに、結果を定める要求を与える

> **閉じるもの**: ⛔ **利用者の裁定（2026-09-13）** —— `CR-373` 第 2 節が残した「掴めるのに結果を定める要求が無い行」を、
> **案 C**（日付を変える 4 つを 1 本で先に閉じる。依存線の選択と注記の箱は後で別に扱う）で閉じる。
> ⭐ **置き場は案 甲に決まった**（利用者の裁定 2026-09-13、逐語「OK。提案通りでよい。」）。
> ⛔ **仕様書はまだ 1 文字も動かしていない。コードも動かさない** —— 利用者の指示（2026-09-13）「実装は待て。現在、仕様書とコードコメントのクリーニング中だ。コードを直すのはクリーニング後にする。」
> ⇒ 本書の残りの仕事は台帳の `DFC-508` が持つ。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`「描いたものを直接掴んで日程を変えられる」**。
`CR-373` がその下に掴みの要求を 6 本置いたが、**予定バーの両端を掴んで開始日・終了日を変える**という、この道具でもっとも普通の操作の結果を定める要求が無いまま残った。

### ② レビュー観点のどの条項を当て、何が出たか

`R1`（要求品質）の**追跡性**。表 T-023d の行そのものに加え、**表の下の結びの散文と、仕様書の全要求の本文**を読み、各行を名指す文がどの要求の中に在るかを機械で数えた（第 1 節）。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**:

1. 閉じ方 ⇒ **案 C**。⛔ 案 A（6 つを 1 本）・案 B（予定の両端だけ先）・案 D（CR を立てない）は退けた
2. 予定の右端を離した日 ⇒ ⭐ **その日がそのまま終了日（`finish`）になる**（逐語「「離した日」が終了日そのものになる。 その方が直感的だろ？」）
3. 完了済みのタスクで実績の右端を延ばしても `actualFinish` が古いまま残る ⇒ ⭐ **不具合**。`DFC-507` に載せ、本書では閉じない
4. 結果をどこに書くか ⇒ ⭐ **案 甲**（`GL-008` の下に要求を 1 本新設し、その要求が 4 行の表を指す。第 3 節）
5. いつ書くか ⇒ ⛔ **仕様書とコードコメントのクリーニングが終わってから**。コードを直すのもその後

**問わずに決めた（覆してよい）**:

- ⭐ **いまのコードの動きを、要求の案の出発点にする** —— 第 2 節で実測したもの。裁定 2 と食い違う点は無かった
- ⭐ **マイルストーンの実績をずらしても、実績の日数（`S-130`）は変えない** —— 表 T-023d の `GR-15` の行が日数に触れておらず、いまのコードもそう動く

---

## 1. 測った —— 8 行ではなく 6 行であった

**方法**: `docs/spec/*.md` の全行から `GR-1` `GR-2` `GR-3` `GR-4` `GR-6` `GR-13` `GR-14` `GR-15` を名指す行を拾い（表 T-023d の行そのものは除く）、その行を囲む `**UID**:` を記録した。

| 行 | 名指している要求 | 結果（離したとき値がどう変わるか）を定めているか |
|---|---|---|
| フェードの掴み点 2 つ（`GR-1` / `GR-2`） | `FR-075` の STATEMENT「作成者がその日数を、表 T-023d の `GR-1` / `GR-2` の掴み点で編集できるようにすること」、`FR-016` の結び（日数はポインタの位置から 1 日単位に四捨五入し `FD-6` で切り詰める） | ⭐ **定めている ⇒ 本件から落ちる** |
| 予定の開始点・終了点（`GR-3` / `GR-4`） | `FR-016` の結び（掴み代の場所・重なったときの優先・追従・掴んでいるあいだ書かない・稼働日へ寄せない）、`FR-043` の `DM-1`（場所だけ） | ⛔ **定めていない** |
| 実績の終了点（`GR-6`） | `FR-016` の結び、`FR-013` の `PV-1`（伸ばす手段として名指すだけ）、`05-07-design.md` の稼働日の数え上げ | ⛔ **定めていない** |
| 実績のマイルストーン（`GR-15`） | `FR-016` の結び（追従）、`FR-043` の `DM-7`（実績バーを持たないこと） | ⛔ **定めていない** |
| 依存線（`GR-13`） | `FR-016` の結び（優先と縦幅） | ⛔ 定めていない（本書の対象外） |
| コメント / ハイライトボックス（`GR-14`） | `FR-016` の結び（追従と書かないこと） | ⛔ 定めていない（本書の対象外） |

⚠️ 表 T-023d は `FR-016` の中に在るが、`FR-016` の STATEMENT は操作の割当とズームの倍率を定めるものであり、掴んだ結果を定めていない。

## 2. 測った —— いまのコードの動き（2026-09-13）

`src/adapter/input-command-translator/input-command-translator.ts` の、離した時点で命令を作る枝を読んだ。

| 操作 | いまの動き | 拒まれるとき |
|---|---|---|
| 予定バーの左端を掴んで離す | `start` ＝ 離した日、`finish` は据え置き（命令 `CM-11`） | `finish` が `start` より前になる（`FR-012` の MUST NOT）。フェードの日数の和が期間を超える（`IV-12`） |
| 予定バーの右端を掴んで離す | `finish` ＝ 離した日、`start` は据え置き（`CM-11`） | 同上 |
| 実績バーの右端を掴んで離す | `actualStart` は据え置き、`actualDuration` ＝ `actualStart` から離した日までの稼働日（命令 `CM-13`） | 実績を持たない |
| マイルストーンの実績を掴んで離す | `actualStart` ＝ 離した日、`actualDuration` は据え置き（`CM-13`） | 実績を持たない |

⚠️ **完了済みのタスクの実績の右端では `actualFinish` を運ぶだけで置き直さない** ⇒ `DFC-507`。
⚠️ 予定の両端の枝の注は「端を越えた場合は `IV-2`」と書くが、`IV-2` は外部キーの規則である。実際に効いているのは `FR-012` と `IV-10` である（注の誤りであり、本書では直さない）。

## 3. 案 —— 結果をどこに書くか

| 案 | 中身 | 評価 |
|---|---|---|
| **甲（推奨）** | **`GL-008` の下に要求を 1 本新設し、その STATEMENT は新しい表（4 行: 予定の開始点・予定の終了点・実績の終了点・実績のマイルストーン）を指す。表 T-023d の 4 行の「操作」の欄はその要求を引く** | 表で書く流儀に沿う。4 行が同じ問い（何を据え置くか・離した日をどう読むか・拒むとき）を 1 か所で持つ |
| 乙 | 予定の両端を `FR-012` に、実績の 2 つを `FR-011` に書き足す | `FR-012` の題は完了率であり、結果の置き場として読めない |
| 丙 | 表 T-023d の「操作」の欄に文を足すだけ | 要求を引く行が増えないので、本件が閉じない |

**新しい表に入れる予定の中身**（第 2 節の実測と裁定 2 から）:

| 掴む場所 | 置く値 | 据え置く値 |
|---|---|---|
| 予定の開始点 | `start` ＝ 離した日 | `finish` |
| 予定の終了点 | ⭐ `finish` ＝ **離した日そのもの**（利用者の裁定 2026-09-13） | `start` |
| 実績の終了点 | `actualDuration` ＝ `actualStart` から離した日までの稼働日 | `actualStart`（`actualFinish` は `DFC-507`） |
| 実績のマイルストーン | `actualStart` ＝ 離した日 | `actualDuration`（`S-130`） |

⭐ 拒むときは既存の規則を引くだけにする（`FR-012` の MUST NOT、`IV-12`）。⛔ **新しい拒み方を立てない。**
⭐ 稼働日へ寄せない規則は 表 T-023d の結びが既に持つ。⛔ **書き写さない。**

## 4. 実測（編集後に書く）

⭐ クリーニングが終わったので当てた（リファクタ計画の記録 7 節、`rulings.md` の `JDG-57`）。

### 書いたもの

| 対象 | 中身 |
|---|---|
| `FR-103`（新設） | `GL-008` と `UC-005` の下。STATEMENT は 表 T-245 を指す |
| 表 T-245（新設） | `GO-1` 予定の開始点 ／ `GO-2` 予定の終了点 ／ `GO-3` 実績の終了点 ／ `GO-4` 実績のマイルストーン。欄は 掴む場所 ／ 置く値 ／ 据え置く値 |
| 接頭辞 `GO` | 行 ID の登録簿（`_source/row-id-prefixes.json`）に登録し、`npm run gen` で刷り直した |
| 表 T-023d | `GR-3` ／ `GR-4` ／ `GR-6` ／ `GR-15` の 操作 の欄に `FR-103` への指しを足した |
| 拒むとき | 既存の条項を引くだけ: `FR-012` の MUST NOT、表 T-220 の `IV-10` ／ `IV-12` ／ `IV-14`。新しい拒み方は立てていない |

### `DFC-507`（`actualFinish`）

- `GO-3` には書いた: `actualFinish` を持つときは、置き直した実績の終了日とする。根拠は 表 T-021a の `PV-2`（実績の終了日であって右端ではない）、`FR-011`（数え方）、`AT-36`（完了したときだけ入る）。持たないときは空のまま
- `GO-4`（実績のマイルストーン）: 最初は止めた。前に立つ者が、利用者の裁定は要らないと判じた（2026-09-14）。根拠は `S-130` の備考（点なので長さを持たない）と、表 T-012 の `SH-5`（実績日の 1 点に置く）である
  - `GO-4` に書いた: `actualFinish` を持つときは、置き直した `actualStart` と同じ日とする
  - `FR-011` に 1 文を足した: マイルストーンには「右端の 1 稼働日前」の読みを当てず、実績の終了日は `actualStart` と同じ日とする。持ち主は数え方を持つ `FR-011` の 1 か所だけで、`GO-4` はそこを指す
- 表 T-021a の `PV-1` を直した: `actualFinish` の読みを「`FR-011` の右端」から「`FR-011` の実績の終了日（右端そのものではない）」に替え、`PV-2` との食い違いを消した

### 数の予測と実測

| | 前 | 後 | 差 |
|---|--:|--:|--:|
| tables | 143 | 144 | +1（表 T-245） |
| figures | 11 | 11 | 0 |
| rows | 1888 | 1892 | +4（`GO-1` 〜 `GO-4`） |
| uids | 152 | 153 | +1（`FR-103`） |

方法: `python .claude/skills/spec-graph-check/md-checks.py .` の最終行を、編集の前と後で読んだ。

### 影響の測り方

- `induced.py`（T-023d、GR-3、GR-4、GR-6、GR-15、FR-016、GL-008、FR-012、IV-12、PV-2、PA-5、FR-011、S-130、T-021a、T-019 の 15 種）: 15 種すべて解決、閉路 1（`FR-011` `FR-016` `GL-008`）。作法どおりの閉路で、`FR-011` は書き換えていない
- `impact.py T-023d`: 1 次の要求 18 件、2 次 40 件
- 同じ形の文: STATEMENT が表を指す要求は 134 本中 44 本。表 T-023d で 操作 の欄が要求を引く行は 21 行中 11 行から 15 行になった

### わざと壊した

`GO-4` の行 ID を `—` に替えた ⇒ `check.sh` は検査 27（行 ID の接頭辞の登録簿が数える行とずれた）の 1 件だけが赤、`npm run gen:check` は exit 1、`md-checks.py` は exit 0（赤 0）。
コピーから戻し、`cmp` で編集後の状態とバイト一致を確かめた。

2 回目（`GO-4` ／ `FR-011` ／ `PV-1` を書いた後）: `FR-011` に足したマイルストーンの 1 文を消した ⇒ `check.sh` ／ `npm run gen:check` ／ `md-checks.py` はどれも exit 0（赤 0）。
⚠️ この 1 文を見張る検査は無い。見張れるのは、この文を逐語で持つ試験だけである。
コピーから戻し、`cmp` でバイト一致を確かめた。

### 検査（終了コードで判じた）

- `bash .claude/skills/spec-graph-check/check.sh`: exit 0
- `npm run gen:check`: exit 0
- `python .claude/skills/spec-graph-check/md-checks.py .`: exit 0
- `tests/unit/t-023d-follows-the-pointer.test.ts` と `tests/unit/t-023d-gr-5-relays-the-actual-duration.test.ts`: 89 件すべて通過

### 基準線

- 検査 37 の `dictionary-table-pairing.txt`: `GR-3` ／ `GR-4` ／ `GR-6` ／ `GR-15` の 4 行の指紋を置き換えた。表示語は行と並べて読み、変えていない。理由はそのファイルの頭に書いた
- 検査 42 の `quoted-source-baseline.txt`: 上げていない（479 のまま）。最初は `GR-15` の欄の途中に指しを入れ、試験のコメントの引用を割って 480 になった。指しを欄の末尾へ移して 479 に戻した

### 対照 —— `FR-103` が無かったら何が通るか

予定の右端を離して `start` まで動かす実装、実績の右端で `actualStart` を動かす実装、完了済みのタスクで `actualFinish` を古いまま残す実装が、どれも仕様に反しないまま通る。

### 残した仕事

| 行 | 何が残っているか |
|---|---|
| `DFC-508` | 仕様側は着地した。台帳の更新は別の波 |
| `DFC-507` | 仕様側は `GO-3` と `GO-4` の両方に着地した。台帳の更新は別の波 |
| `DFC-509` | 予定の両端の枝の注が `IV-2` と書いている誤り（コード側。本書では直さない） |
| 変更履歴 | `docs/development-records/changelog.md` は持ち場の外なので書いていない |

⭐ `GO-3` の読みを 1 つにした（2026-09-14）: 離した日は実績バーの右端の位置ではなく実績の終了日として数える（`GO-2` と同じ所作。数え方は `FR-011` のまま）。
裁定の逐語 2 つは `docs/development-records/rulings.md` の `JDG-63` / `JDG-64` へ写した。
⛔ 離した日が非稼働日のときの実績の終了日は、既存の条項から一意に決まらないので書いていない。

## 5. ⛔ この変更でやらないこと

- ⛔ **依存線の選択（`GR-13`）と、コメント / ハイライトボックス（`GR-14`）は閉じない** —— 利用者の裁定（案 C）
- ⛔ **`DFC-507`（`actualFinish` が古いまま残る）は直さない** —— 利用者の裁定「後で直す」
- ⛔ **`src/` と `tests/` には触らない**
