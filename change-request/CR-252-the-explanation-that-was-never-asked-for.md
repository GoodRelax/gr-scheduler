# CR-252 — 誰も求めていないのに立っていた説明

> **閉じるもの**: ⛔ **利用者が実物で逐語に述べた** —— 「開いた直後は、マウスをどこにも当てていないので説明は出ない」「マウスをずらすと説明は自動で消える」「× を押して消すのは間違い」。
> ⭐ **実測で原因は確定した** —— **開いた直後の画面に、誰も指していない説明が 6 つ立っている。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`（1 画面で全体を読める）。受ける目標は `GL-002` と `GL-006`。**
⛔ **指してもいない説明が 6 つ日程の上に立てば、1 画面で読める面積がそのぶん減る。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `FR-085` の説明 | ⛔ **出す条件が「名前が打ち切られたこと」だけで、ポインタの条件が 1 文も無い。**⭐ **描く側はそのとおりに実装しており、実装の発明ではない** |
| **`R2.19`**（驚かせない）| 開いた直後 | ⛔ **何もしていないのに説明が 6 つ立つ** |
| **`R1.4`**（欠落が無い）| 表 T-028 の `IN-3` の「消せること」| ⛔ **消す手立てを誰も持っていない。**⚠️ **画面が出している `×` は、押すためにポインタを動かすので同行の「ポインタもフォーカスも動かさずに」を満たさない** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`Esc` を説明の消し方とし、`IN-4` の階層の最後に置く** | ⛔ **`IN-3` は消す手立てを MUST で求めるのに、どの入口も持っていなかった。**⚠️ **最後に置くので、面もドラッグも構えも無いときにだけ説明を消す** |
| 2 | ⚠️ **説明が出ているあいだ、`IN-4a` の「ブラウザへ渡す」は 1 段遅れる** | ⛔ **全画面から `Esc` で戻る経路（`FR-071`）は、説明が出ていると 2 回押しになる。**⭐ **構えているあいだの扱いと同じ形であり、同行が既にその形を書いている** |
| 3 | ⛔ **`FR-092` の `EZ-2` と `FR-037` は 1 文字も触らない** | ⭐ **どちらも既にポインタの条件を自分で書いている。**⚠️ **条件を欠いていたのは `FR-085` だけである** |
| 4 | ⛔ **打ち切りそのものは触らない** | ⚠️ **利用者は「タスクグループ名にマウスを当てても説明は出ない」とも述べたが、いまの起動テンプレートでは 7 行のうち 6 行が実際に打ち切られており、`FR-085` は打ち切ったら全文を出すことを MUST で求めている。**⛔ **打ち切る幅そのものを変えるかは別の裁定であり、本書では問わない** |

---

## 1. 測った

⭐ **開発サーバの実 DOM で測った**（体の報告ではない）。

| 測ったこと | 実測 |
|---|---|
| 開いた直後に立っている説明 | ⛔ **6 つ** —— いずれも `rowTitle` を指し、`×` を 1 つずつ持つ |
| 行見出しの打ち切り | **7 行中 6 行が `true`** —— ⭐ **打ち切りの判定そのものは働いている** |
| `FR-085` のポインタの条件 | ⛔ **無い**（`01-04-requirements.md:1275`）|
| 描く側の自己申告 | ⭐ **一致する** —— `tooltips.ts` の冒頭が逐語で「`FR-085` はポインタの条件を 1 つも述べていない。引き金は名前が切られたことである」と書いている |
| `IN-3` の 3 条件 | 消せること／乗せられること／引き金が外れるまで出ていること（`01-04-requirements.md:3930`）|
| `IN-4` の階層 | 開いている面 → ドラッグ → 構え → `Dual Cursor`（`01-04-requirements.md:3931`）|
| 誘導部分グラフの閉路 | ⛔ **無し** —— `FR-085` と `IN-3` と `IN-4` のあいだに閉路は出ない |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-085` | **説明は、その行の名前にポインタが乗っているか、その行にフォーカスがあるあいだだけ出すこと（MUST）。常に出してはならない（MUST NOT）** |
| 2 | 表 T-028 の `IN-4` | 階層の最後に **出ている説明** を足す |
| 3 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1688 | **1688** | 0（`IN-4` はセルの編集）|
| `uids` | 148 | **148** | 0 |
