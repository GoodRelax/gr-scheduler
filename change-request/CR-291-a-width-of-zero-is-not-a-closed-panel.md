# CR-291 — 幅 0 は「閉じている」ではない

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-30）:「面の有無は別に持つ」。**
> ⭐ **最後に残った赤い試験 1 件**（`fr-006-panel-close-entrance`）**がこれで判定できるようになる。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`（マニュアルを読まずに使える）。**
⛔⛔ **同じ問いに仕様書が 2 つの答えを持っており、2 つの仕様準拠試験が正反対に読んでいた。**
⇒ **どちらが正しいかを、実装ではなく仕様書が決める状態にする。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R3.4`**（同じものを 2 か所に置かない）| `S-80` と `S-171` | ⛔⛔ **2 行がそろって「`0` であることが「閉じている」」と書いており、しかも同じ注の後半が「開いたときの幅は `S-171` が持つ」と続けて自分を打ち消している** |
| **`R1.4`**（欠落が無い）| 面を出しているかの状態 | ⛔ **`propertiesShowing` は `ScreenSession` の欄として実装に在るのに、表 T-206 に行が無い** —— 同表は「文書が持たない値」の全数を名乗る表である |
| **`R2.19`**（驚かせない）| 幅 0 のまま開く | ⛔ **人が境界を左端まで畳んだだけで面が「閉じた」ことになると、次に開いたとき幅が復元できない** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **表 T-206 に `S-99h` を新設する**（行を 1 つ足す）| ⛔⛔ **本 CR は行を足す。**⚠️ **裁定は「別に持つ」であり、持ち場を決めていない。**⭐ **表 T-206 が「文書が持たない画面の状態」の全数を名乗る表であり、`S-99e`（パレットの表示状態）・`S-99f`（全画面）・`S-99g`（開いている面）が既にそこに並んでいる。**⇒ **同じ棚である** |
| 2 | ⭐ **`S-80` の下限 `0` は残す** | ⭐ **`FR-052` が境界のドラッグで幅を 0 まで畳めると定めており、幅としての 0 は正当である。**⛔ **変えるのは「0 の意味」だけであって、値域ではない** |
| 3 | ⭐ **既定は「出していない」** | ⭐ **`FR-080` の基準環境が「面は閉じている」と定めており、そこは動かない** |

---

## 1. ⛔ 測った

| 測ったこと | 実測 |
|---|---|
| 「`0` ＝ 閉じている」と書いている行 | ⛔ **2 行**（`S-80` と `S-171`。どちらも同じ文を持つ）|
| 「開いたときの幅は `S-171`」と書いている行 | ⛔ **同じ 2 行**（自分を打ち消している）|
| `FR-052` ／ 表 T-024 に「幅で面の有無を決める」行 | ⛔ **0 件** |
| 実装がどう判じているか | ⭐ **`properties-panel.ts:1297` は `ScreenSession.propertiesShowing` で判じている**（幅では判じていない）|
| 正反対に読んでいる試験 | ⛔ **`uf-64.test.ts:258`（緑・5 件）は幅 0 でも面が出ることを期待。`fr-006-panel-close-entrance`（赤・1 件）は幅 0 なら出してはならないと期待** |
| `propertiesShowing` に対応する仕様の行 | ⛔ **0 件**（表 T-206 を全 51 行数えた）|

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `_source/settings.json` の `S-80` | ⛔ 「`0` であることが「閉じている」」を落とし、**幅だけを持つ行**にする |
| 2 | 同 `S-171` | 同じ文を落とし、**面を出したときに置く幅**と言い直す |
| 3 | 同 **`S-99h` を新設** | ⭐ **プロパティパネルを出しているか**（`選択` ／ `文書全体の設定` ／ 出していない）。既定は出していない |
| 4 | `npm run gen` | 生成物（`tbl-settings.md`）を通す |
| 5 | `A-appendix.md` | 変更履歴に 1 行 |

⛔ **`src/` は 1 文字も触らない** —— **実装は既に裁定どおりである。**足りなかったのは仕様書のほうである。
⛔ **赤い試験は、仕様だけを読む体に書き直させる**（規則 05 の 7.）。

## 3. 数の予測

```
tables   135 → 135
figures   11 →  11
rows    1704 → 1705   ⚠️ 行を 1 つ足す（S-99h）。⛔ ここだけが動く
uids     149 →  149
```

⚠️ **`rows` が 1705 以外になったら、計画外の行に触った証拠である。**

## 4. 落ちる／緑になる試験

| 試験 | 予測 |
|---|---|
| `uf-64`（緑・5 件）| ⭐ **緑のまま** —— 裁定はこちらを正としている |
| `fr-006-panel-close-entrance`（赤・1 件）| ⭐ **書き直しが要る** —— 期待が裁定と逆である |
| 検査 28 本 | ⚠️ **`rows` が 1704 → 1705 へ動く。それが予測どおりであることを確かめる** |
