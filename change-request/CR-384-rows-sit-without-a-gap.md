# CR-384 — 行と行のあいだに隙間を置かない（`rowGap` を 0 に固定）

> **閉じるもの**: 利用者の申し立て 2026-09-15 の K と、その補足 —— 逐語は `docs/development-records/rulings.md` の各行。
> - `JDG-93`: Lv2 の行の下に Lv1 の行を置くと無用な隙間が出る。隙間を無くせ。台帳 `DFC-600`
> - `JDG-108`: 隙間は Lv1 / Lv2 によらない。キャプチャの矢印（行と行のあいだの空白）を見ろ
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `87861cf3`。
> ⚠️ `docs/spec/_source/settings.json` を変えて `npm run gen` を回したので、生成物として `src/entity/document-model/document-settings/document-settings.ts`（`SETTINGS_DEFAULTS` / `SETTINGS_BOUNDS` の生成された範囲）と `src/framework/single-html-shell/startup-template.json` が動いた。手では書いていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`（全体日程を 1 画面で見られる）**。行ごとに何も描かれない 8px を積まない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— 表 T-246 の `HB-5` と 表 T-023d の結びのパンの段が「帯と帯は接していない」を理由に使っていた。`rowGap` を 0 にするとこの文が偽になる ⇒ 規則は変えず、理由の文を「隙間（`S-12`）が入るなら」に直し、いまは 0 であることを ⚠️ で書いた。
- **`R1.4`（境界値）** —— 隙間が 0 のとき、行の境目は幅を持つ範囲ではなく線になる。`HB-5` の「隙間の中では 0」はそのまま成り立つ（範囲の幅が 0 になるだけ）。
- **`R3.4`（境界とインデックス）** —— 帯が接すると、境目の 1 画素がどちらの行に属するかが問われる。⚠️ 本書は当たり判定の半開区間を新しく定めない —— 行を答える側の既存の実装が決める（第 5 節）。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**: `JDG-93`・`JDG-108`（狙いは `rowGap` そのもの）。

**問うこと**: なし（0 件）。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| 決定 1 | **行を退役させず、既定・下限・上限をすべて 0 に固定する**（`S-23` `markerGap` の 4 / 4 / 4 と同じ形） | 退役させると、`rowGap` は文書が常に全項目を書き出す設定値（`FR-024`）なので、`GRS JSON` の形（`grs-document.schema.json`）、取り込みで知らない鍵の扱い、`LF-3` / `LF-14` の算式、表 T-104 の `K-12` がすべて動く。固定なら式も形も残り、値だけが 0 になる |
| 決定 2 | 既定だけを 0 にして上限 60 を残す案を退けた | 利用者の手元の文書は `rowGap` 8 を持って保存されている（`FR-024`）。既定だけ変えても、開き直した文書は 8 のままで、申し立ての隙間が消えない |
| 決定 3 | 0 以外を持つ文書を読んだときは、**黙って 0 として読む**。表 T-233 の `RS-51` の対象から `S-12` を外し、件数にも入れない | 利用者の答え `JDG-113`（逐語「このCRより前に保存されたファイルであれば無視してよい。」）。値は `clampedSettings`（表 T-064 の `PI-2`）が 0 に収める既存の道のまま、告げることだけを外す。⚠️ `JDG-78`（自動で直したら示す）と `RS-51` の MUST NOT「黙って収めてもならない」とぶつかるが、`JDG-113` が後の裁定であり `S-12` について優先する（`JDG-113` の読みの欄）。⭐ 初稿の代償（開くたびに `RS-51` が出る）は消えた |
| 決定 4 | 格子の床（表 T-051 の `HF-19`）は対象外 | `DFC-600` の判定と `JDG-108` の読み —— 利用者が矢印で指したのは `rowGap` の 8px だけ |

---

## 1. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| 行の送り | 56px（帯 48 ＋ `rowGap` 8）、深さによらない | `DFC-600` の実測を原典で読んだ |
| `docs/spec` で `rowGap` を式に持つ所 | 2（`LF-3`、`LF-14`） | `grep -n rowGap docs/spec/*.md` |
| 「帯と帯は接していない」を理由に使う所 | 2（`HB-5`、表 T-023d の結びのパンの段） | `grep -n 接していない docs/spec` |
| `src/` で `settings.rowGap` を項に持つ行 | `schedule-layout.ts` の 775 / 843 / 845 / 847 / 854 / 856（`DFC-600` の列挙） | 本書は読むだけ。0 で式が壊れないかは実装の波が確かめる |

## 2. グラフ

**`impact.py S-12 LF-3 LF-14 HF-19`**（49 行）: `S-12` は参照 0（`LF-3` / `LF-14` はキーの名で持つ）、`LF-3` は要求 3（`FR-004` `FR-016` `FR-087`）/ 参照 7、`LF-14` は `FR-098`、`HF-19` は参照 0。
**`induced.py`**（`CR-380` と同じ 50 種）: 本書の対象に掛かる閉路は 0。
**`rulings.md`**: `S-12` / `rowGap` → `JDG-108`、`LF-3` / `HF-19` → `JDG-93`・`JDG-108`。衝突 0。

## 3. 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| 表 T-201 `S-12` | `docs/spec/_source/settings.json`（`npm run gen` で `_assets/tbl-settings.md` へ） | 既定 8 🔎 → 0、上限 60 → 0。範囲の理由「固定。行の帯と帯のあいだに隙間を置かない —— 帯は中身と格子の床で既に高さを持ち、隙間は何も描かれない帯を 1 本足すだけ。利用者が指定した値。0 以外は収めて告げる（`RS-51`）」 |
| 表 T-233 `RS-51` | `01-04-requirements.md` | `S-12` を本行の対象に数えない（MUST）、件数にも入れない（決定 3） |
| 表 T-246 `HB-5` の根拠 | `01-04-requirements.md` | 行の境目は帯と帯のあいだの隙間（`S-12`）という範囲。いまは 0 なので境目は線になる、規則は幅によらず 1 つ |
| 表 T-023d の結びのパンの段 | 同 | 比を送りに取る MUST は据置。理由を「隙間が入るなら」に直し、いまは 0 で送り ＝ 帯と ⚠️ |

## 4. 数の予測

tables 0 / figures 0 / rows 0 / uids 0（値とセルの文だけ）。

## 5. 影響 —— 本書の外で動くもの

| 所 | 何が動くか |
|---|---|
| 生成物 | `_assets/tbl-settings.md`、`grs-document.schema.json`（`rowGap` の上限）、`document-settings.ts` の生成範囲、`startup-template.json`（`npm run gen` が書いた） |
| 実装の波で確かめること | 帯が接したときの行の当たり判定（境目の 1 画素）、行の掴み（`HF-15` / `GR-20`）と落とし場所、`schedule-layout.ts` の `rowArea` と `scrollAreaY` の式（0 を入れて崩れないか）、`LF-14` のピン止めの帯 |
| 試験 | `rowGap` 8 や送り 56px を前提にした試験は赤くなりうる（実装の波で直す） |
| `docs/development-records/rulings.md` | `JDG-93` / `JDG-108` を `適用済` |
| `docs/development-records/defects.md` | `DFC-600` を `実装待ち` |

## 6. ⛔ この変更でやらないこと

- ⛔ `HF-19`（格子の床）と `LF-3` の床の式を変えない
- ⛔ `rowGap` の鍵を退役させない（決定 1）
- ⛔ 当たり判定の境目の新しい規則を発明しない
- ⛔ `src/` と `tests/` を手で書かない（生成物だけが動いた）
