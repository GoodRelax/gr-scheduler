# CR-339 — 行は、自分の操作子より低くならない

⭐⭐ **利用者の裁定（2026-09-03）**

> **行の帯高に下限を置く。**（格子が収まる高さを、行の帯高の下限とする）

**台帳の行**: `D-225`（`D-223` と `D-224` を吸収した原因の行）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

`CH-2`（操作の確実さ）と `GL-006`（アフォーダンスとシグニファイア）。

⛔ **いま、行の操作子は押せない** —— **押しは次の行の操作子に取られる。**
⭐ **見えている場所を押せば、それが起きる**という最低限の約束が破れている。

### ② レビュー観点のどの条項を当て、何が出たか

`docs/development-rules/07-review-standards.md` の **`R2`**（規則の在り処が 1 つ）。

⛔ **当てて、仕様が 2 つのことを別々に述べ、両者が衝突する場合を 1 行も述べていないと分かった。**

```
行の帯高      ST-9「行の帯高は段数で決まる。行高固定を前提にしてはならない（MUST NOT）」
              LF-2「Task を 1 つも持たない段は、矩形が縦に取る高さとする」
              LF-3「帯高は矩形が縦に取る高さを下回らない」          ← 床は在る。矩形の床である
操作子の並び  HF-1「並びは 2 × 2 の格子とすること（MUST）」（利用者の裁定 2026-08-30）
              HF-5「名前の文字サイズにかかわらず、操作子を同じ大きさで描くこと（MUST）」
```

⛔⛔ **格子が矩形より高いときにどちらが譲るかを、どの行も言っていない。**

⚠️ **当たった先で「無い」ことを確かめた**: 表 T-051 の全 18 行 ／ 表 T-201 の `S-35` 〜 `S-42` ／
表 T-206 ／ `LF-2` ／ `LF-3` ／ `ST-9` ／ `FR-042`。

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

1. ⭐ **床を数で書かない。**⛔ **格子の高さを直に持つ設定値は無い** ——
   **操作子 1 つの外形は 表 T-206 の `S-138`（16）と `S-141`（4）が決めており、
   格子はその 2 段ぶんである。**⇒ **床は「その行の操作子が縦に取る高さ」と、
   関係で述べる。**⚠️ **数を写せば、`S-138` を動かした瞬間に嘘になる。**

   ⛔⛔ **本要求は 2026-09-03、ここを 1 度書き誤った。**
   **「読む人の文字サイズが動かす（`FR-039`）」と書いたが、それは偽である** ——
   **`S-138` の備考が「⛔ 閲覧者の文字サイズに追随させない」と逐語で定め、
   入口の外形を 26 × 24px に固定している。**
   ⭐⭐ **見つけたのは、仕様だけを読む体である。**⛔ **前に立つ者が書いた条文を、
   実装を読まない体が原典と突き合わせて捕まえた** —— 規則 04 の 1 節が働いた形である。
   ⇒ **`LF-3` と `HF-19` の両方を、追随しないと明記する形に直した。**
2. ⭐ **`LF-3` の既存の床に足す形にした。**⛔ **新しい行を立てなかった** ——
   **`LF-3` は既に「帯高は矩形が縦に取る高さを下回らない」を持っており、
   床が 2 か所に散ると `R2` に反する。**
3. ⭐ **`HF-1` は動かさない。**⚠️ **利用者の 2026-08-30 の裁定（2 × 2）と、その理由
   （上下に読めば動作、左右に読めば範囲）はそのまま生きる。**

---

## 1. ⭐ 実測（この要求の根拠）

**出荷ビルド、1400×2000、`file://`、Playwright（`scratch/measure-grid2.mjs`）:**

```
起動直後の既定文書で IC-91 を 1 回押し、New Kid と名付けるだけ

  New Kid   行の箱 417..445（28px）
  その IC-90   441..465    中心 453
  中心の持ち主  次の行（148px の行）        ← 自分の行の外
  16 行中 1 行がこの状態
```

**parity の板（`Task` を 1 つも持たない行だけで組んだ木）では 11 行すべてが 22px で、
`IC-58` と `IC-90` は 1 行も押せない**（10 行は別の行へ、1 行は日程キャンバスへ届く）。

⭐ **決定的な確認**: 出荷ビルドを複写し、格子の CSS だけを 4 列 1 段に変えて
（CSP hash も計算し直して）同じ 25 手を回すと
**23 一致 ／ `FR-098` のピン順だけ 2 ／ 説明の無い食い違い 0**。
⇒ ⛔ **`npm run parity` の 14 手の食い違いは、14 件の不具合ではなく、この 1 つである。**

⚠️ **`tools/parity/*.mjs` は「行は 64〜148px（`FR-042`）」と書いていたが、
その範囲は仕様のどこにも無い。**`FR-042` の逐語は
「**指定した高さは下限として扱うこと（MUST）**」だけである。⛔ **この作り話が、
22px の行を探させなかった。**⇒ **両ファイルの注を直した（本要求の一部）。**

---

## 2. ⭐ 書くもの

### 2.1 表 T-221 の `LF-3` — 床をもう 1 つ持たせる

| | いま | あと |
|---|---|---|
| `LF-3` | 前の行の縦位置に、前の行の帯高と `rowGap` を加える。**帯高は矩形が縦に取る高さを下回らない** | 前の行の縦位置に、前の行の帯高と `rowGap` を加える。**帯高は矩形が縦に取る高さを下回らず、かつ、その行の操作子（表 T-051 の `HF-1`）が縦に取る高さも下回らない**（利用者の裁定 2026-09-03）—— ⛔ **下回ると、`HF-1` の格子の下段が次の行の帯へ落ち、押しがその行の操作子に取られる。**⚠️ **数では書かない** —— 操作子 1 つの高さは字形（`S-138`）に余白を足したもので、読む人の文字サイズが動かす（`FR-039`）|

### 2.2 表 T-051 に `HF-19` — 格子が要求する高さを、行の側の言葉で述べる

⚠️ **`HF-16` 〜 `HF-18` は既に在る。**⛔ **行 ID は席の番号であり、詰めない。**

| 行 ID | 規則 |
|---|---|
| `HF-19` | **`HF-1` の格子が縦に取る高さは、行の帯高の下限である（MUST）。行の帯がそれを下回ってはならない（MUST NOT）**（利用者の裁定 2026-09-03）—— **下回ると、下段の 2 つが次の行の帯の上に立ち、`HF-6` が「行の名前の上へ重ねて描く」と定めた重なりが、隣の行に対して起きる。**⛔ **格子の側を縮めて合わせてはならない（MUST NOT）** —— **`HF-5` が「名前の文字サイズにかかわらず、操作子を同じ大きさで描くこと（MUST）」と定めている。**⚠️ **算式は 表 T-221 の `LF-3` が持つ** |

### 2.3 表 T-051 の `HF-15` — 古い実測値を直す

⛔ **仕様が持つ実測が偽になっている。**同行は
「⚠️ **実測（2026-09-02、出荷ビルド 1920×1080）: 8 行のうち 6 行で、掴み代の中央を押すと
行が消えるか（`IC-59`）1 階層開いていた（`IC-90`）**」と書くが、
**`CR-336` の後に測り直すと、掴み代を操作子が覆うのは 5 段のうち深さ 4 と 5 の 2 段だけである**
（既定幅 170px、2026-09-03）。⇒ **実測の文を差し替える。台帳の行は `D-154`。**

---

## 3. ⭐ 影響

```
induced.py  seeds 14 / edges 7 / cycles 1（size 2: FR-001 FR-019）
            ⇒ 本要求が触る LF-3 / HF-1 / HF-5 / T-051 は、その閉路に入らない。
              単独で 1 パスで書ける
```

⚠️ **実装で動くもの:**

```
schedule-layout.ts   height = Math.max(packed, emptyLane, row.height ?? 0) に
                     操作子の高さの項が要る。⛔ いまは矩形の床しか見ていない
```

⚠️ **画面が動く** —— **`Task` を 1 つも持たない行が 22〜28px から 48px 前後になり、
その下の行がすべて下がる。**⛔ **退行ではない。**

⭐ **`npm run parity` が答えを持つ** —— **入ったあと 25 手のうち説明の無い食い違いが
0 になること**（残るのは `FR-098` のピン順の 2 手）。⛔ **これを受け入れ条件とする。**
