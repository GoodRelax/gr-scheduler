# CR-218 — 語で示す印と、古くなった 3 つのセル

> **閉じるもの**: ⛔ **`PD-175`** —— `FR-032` は「消える `Task` のうち他の行に表示されているものには、その旨を示すこと（MUST）」と定めるが、**示し方が語なのか図形なのかを言っていない。**
> ⭐ **利用者の裁定（2026-08-23）**: **語で示す。**
> ⭐ **あわせて、自分で確かめた古いセルを 2 つ掃く。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`。受ける目標は `GL-006`。** 説明を読まずに何が消えるか分かること、が本件の値打ちである。

⛔ **満たされていない MUST である** —— 値は画面まで届いているのに、**示す手段が決まっていないので描かれていない。** `dom-screen-surface.ts` が `⛔ NO MARK IS SETTLED` と書いたまま属性だけを載せている。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.2`**（曖昧さの排除）| 「その旨を示すこと」 | ⛔ **手段が読めない。** ⭐ **語と定めれば、`display-words.json` に枠が立ち、機械が「空である」ことを数えられる** |
| **`R2.9`**（YAGNI）| 図形を起こす案 | ⛔ **退けた。** 表 T-026 の `RC-13` が「**本仕様書の中で発明しない —— 図形の決定は利用者の裁定である**」と定める。⭐ **語なら図形を 1 つも起こさない** |
| **`R1.3`**（唯一の正がある）| 表 T-109 の注の「68 個」 | ⛔ **同じ表の 2 か所が 70 と書いているのに、1 か所だけ 68 のままだった**（CR-211 の取りこぼし。自分で数えた）|
| **`R2.15`**（コメントは「なぜ」を表す）| `tbl-settings.md` の注 | ⛔ **存在しない注を引いていた。** 引くべきは 表 T-206 の行 ID である |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **語の枠を `confirmationMarks` という新しい群に置く** | ⛔ **既存の `confirmation` 群の鍵は「答え」である**（`proceed` / `cancel`）。印は答えではない。⚠️ **生成器には既に「表が行を持たない鍵を、理由を書いて直に置く」前例が 2 つある**（`CONFIRMATION_ANSWERS` / `PANEL_HEADINGS`）|
| 2 | ⭐ **鍵の名を `shownOnAnotherRow` にした** | `src/` の値が既に `isShownOnAnotherRow` と綴っている。⚠️ **鍵は真偽ではないので `is` を落とした**（`R2.1` は `is` を真偽に限る）|
| 3 | ⭐ **`FR-032` に「図形を起こしてはならない」を明記した** | ⛔ **書かないと、次に読む者が 表 T-109 に行を足しにいく。** `RC-13` を名指して道を閉じる |

---

## 1. 測った

```
induced.py  FR-032 NT-7 HM-10 T-109 RC-13 FR-038 T-026
```

| 測ったこと | 実測 |
|---|---|
| 種が仕様に解決したか | **7 / 7** |
| **閉路** | ⭐ **0** —— 1 つずつ書いてよい |
| 行を足すか | ⛔ **足さない。** 語の枠は原稿（`_source/display-words.json`）に立ち、仕様の表には立たない —— **`FR-038` が「要求にも表にも語そのものを書いてはならない（MUST NOT）」と定めている** |

---

## 2. 編集計画

| # | 対象 | すること |
|---|:-:|---|
| 1 | `FR-032` の `:1678` | 示し方を語と定め、図形を起こす道を閉じる |
| 2 | `_assets/tbl-glossary.md` の 表 T-109 の注 | 「68 個の確定名」→ **70** |
| 3 | `_assets/tbl-settings.md` の `S-109` 群の後の注 | 「表 T-206 の注」→ **表 T-206 の `S-90`** |
| 4 | `tools/generate_display_words.py` ＋ 原稿 | `confirmationMarks` の群を足す（176 → **177** 語）|

---

## 3. 数の予測

| | 前 | 後 | 差 |
|---|--:|--:|--:|
| `tables` | 132 | **132** | 0 |
| `rows` | 1609 | **1609** | 0 |
| `uids` | 148 | **148** | 0 |
| `figures` | 11 | **11** | 0 |
| 表示語の枠 | 176 | **177** | +1 |

⭐ **どれも散文とセルの直しなので、オブジェクトは 1 つも増減しない。**
