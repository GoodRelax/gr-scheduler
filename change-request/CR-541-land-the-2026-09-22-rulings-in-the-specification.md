# CR-541 — 2026-09-22 の裁定（JDG-300 〜 JDG-302）を仕様書へ着地させる

> ⭐ **状態: 当てた（2026-09-22）。** 起草（`783f276a`）のあと、前に立つ者が 11 節の 14 の問いに答え、利用者が S5・S6・Q33 の位置に答えた（4.12 節）。その答えで起草を直し、4 節の編集を仕様・原稿へ当て、`npm run gen` で刷り直した。⛔ 試験・コード・台帳・検査の基準線には当てていない。
> 読んだ木: `50f925db`（ブランチ `claude/optimistic-driscoll-0e6118`。`origin/refactor` `3ec30eaf` の上の仕分けの 1 commit）。
> 行番号・行数・参照の数は、すべてこの木で測った（測り方は 12 節）。
>
> **閉じるもの**: 2026-09-22 の利用者の 2 通（scratchpad `user-words.md`）が推奨どおりとした問いのうち、仕様の文を変えるもの（前に立つ者の着地計画 `landing-plan.md` 第 3 節の CR-541 の表）。
> 裁定の行は `JDG-300`（問いを推奨どおり）・`JDG-301`（Ctrl ＋ Shift ＋ 0 を捨てる）・`JDG-302`（`[vv]` の 3 つの状態と、見本の問い S2〜S4）であり、`rulings.md` への記入は `CR-549` が行う。
>
> ⛔ **本書が外したもの**（前に立つ者の指示。10 節）: S1 の 10 色の値・Q3 の綴りの規則（E-67、`CR-548` まで待つ）・S3 の英語の語（候補のまま）・`FR-033` の `TaskOrigin` の食い違い・表 T-020 の下の注と `EX-12`。
> ⭐ **Q5（CR-410/Q2、Ctrl ＋ Shift ＋ 0 の直後の Ctrl ＋ Z）は取り下げる** —— `JDG-301` で `SK-17` そのものが無くなるので、問いの前提が消えた。仕様の文は 1 つも動かない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`**（`CH-4`、説明を読まずに操作できる）—— 端の入口で通知とメッセージを二重に出さない（Q9）、端に着いた押しにも「（最大）」を添える（Q10）、入力中の Ctrl ＋ A（Q15）、ドラッグ中の `Esc`（Q20）、ガイドカーソルの押された見た目（Q26）、依存の種別を `FS` で読ませる（Q27）、`[vv]` が縮めていても直下の子を必ず見せる（`JDG-302`）。
⭐ **`GL-002`**（`CH-2`、縮めても全体を 1 枚で俯瞰させる）—— `S-88` を 1.5 にして深さ 5 まで倍率で出す（S2）、倍率で描かれないタスクを選ばせない（Q7）。
⚠️ 残り（Q13・Q14・Q18・Q19・Q21・Q30・Q31・Q32 ほか）は、仕様どうしの食い違いや書かれていない穴を閉じる手入れであり、`CH-` を直に前へ進めない。正直にそう書く。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正がある）** —— Q20（`IN-4` と 表 T-206 の `S-99g` / `S-99h` の食い違い、`DFC-570`）、Q21（表 T-071 とコード、`DFC-676`）、Q31（`IR-3` と `FR-076` / `AG-9a`、`DFC-680`）、Q25（`IN-3` と `EZ-6` の MUST どうし）、Q32（表 T-062 の「正」と 表 T-075 の「負う要求」）。どれも片方を直して 1 つの正にした。
- **`R1.4`（異常系・境界値）** —— Q9 / Q10（端の段）、Q14/488（全消し）、Q16（複数選択と、祖先と子孫を同時に選んだ場合。③ の決定 7）、Q18（配りの最中の発話）、Q33（錨の日付が `null`）、`PND-232`（ずれが `null`）。
- **`R1.5`（MECE）** —— `JDG-302` で `HF-2` の構えの条件と `HF-18` の数が別物になる。`HF-2` に「同じ 1 つ」と書いてあった文を消し、どちらが何を数えるかを書き分けた（4 節 E-06 / E-08）。
- **`R2`（命名。名前を足すときは必ず引く）** —— 新しい行 ID は 4（2 節）、新しい接頭辞・表・図・要求 ID は 0、退ける行 ID は `SK-17` の 1 つ。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる）。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | Q20 の「B（いまのコード）」を、**プロパティパネルに `Esc` の段を 1 つ与え、進行中のドラッグの次に置く**と読む（開いている面（`S-99g`）はドラッグより上のまま） | `DFC-570` の案 ② の字のとおり（「`IN-4` でパネルを『進行中のドラッグ』の下の段に置き直し、`S-99g` / `S-99h` をそれに合わせる」）。コードの並び（`screen-state.ts:128` の `escapeTarget`: 面 → ドラッグ → パネル）とも一致する | 段が 8 から 9 になる。⚠️ 状態機械のセッションの 表 T-283 の扱いは 11 節の問い 1 |
| 決定 2 | `KO-2` を行を増やさずに書き換える（印の欄に「押した行には立て、配下からは外す」） | 表 T-254 の行 ID の並びを試験が主張している（`cr-404` の `:89`） | 1 行が 2 つの向きを持つ |
| 決定 3 | Q9 の例外を 表 T-260 の `SE-5` に置き、`FR-029` には指し先の 1 文だけを足す | 表で書く（要求は表の行を指す）。`ZE-5` が `SE-3`〜`SE-5` を行の軸へ既に当てているので、1 か所で両方に効く | — |
| 決定 4 | Q10 を「押したあとの倍率がいちばん大きい段なら（最大）」と書く | 上げる押しで着いたときも、端で変わらなかったときも同じ 1 文で言える。`SK-17` が無くなったので、押したあとが端になるのは向きの合う押しだけである | — |
| 決定 5 | Q27 の略号を、新しい生成器ではなく `tools/generate_display_words.py` に読ませ、生成される `display-words.json` へ運ぶ | その生成器は既に 01-04 を読み、プロパティパネルは生成された同じファイルを読む。生成器の数（検査 27）が動かない | 辞書の原稿（`_source/display-words.json`）には載らない値が、生成物にだけ載る（表 T-104 のキーを運ぶ前例と同じ形） |
| 決定 6 | S4 の 4px（`S-313`）には `S-235` も表示の倍率も掛けない。`S-312` の暗いテーマは明るいテーマと同じ `#b45309` とする | どちらも「いまの出荷ビルドの値を行にする」（S4 の A、`PND-1`）の字のとおり（`dom-screen-surface.ts:918` は掛けていない。`schedule-overlays.ts:60` は明暗で同じ色） | 暗いテーマでの注記の色は選び直していない（11 節の問い 4） |
| 決定 7 | Q16 で、選ばれた `Task` がほかの選ばれた `Task` の WBS の子孫なら、祖先の部分木として 1 度だけ写す | 2 度写すと同じ工程が 2 つでき、`FR-033` の「部分木ごと複製」と数が合わない | 規則が 1 文増える |
| 決定 8 | S3 の「ルーラーの帯の中央」を「タイムルーラーの帯の中の、2 本の線の中点」と読む | 見本の問い S3 が見せようとしたのは線の間隔と札の重なりであり、札は 2 本のあいだに立つ | 2 本が画面の外に出たときの置き場は書いていない |
| 決定 9 | Q14/484 を新しい `OP-` 行にせず `OP-6` の後段に足す。Q21 の 2 つの持ち越しを新しい `CA-` 行にせず `CA-1` の後段に足す。Q23 を `FR-072` に足す（`IC-17` の行ではない）。`PND-176` を `FR-016` のパンの段に足す（表 T-023 の `MK-1` は触らない） | 新しい行 ID を増やさない。`MK-1` は辞書と組にした行（検査 37）なので、触ると指紋が動く | `OP-6` の場面の欄は `GRS JSON` のままで、後段が MSPDI を述べる |
| 決定 10 | Q30 の戻り値の意味を `AG-6` に書き、05-07 の 表 T-064 の `PI-15` は触らない | `PI-15` の行は関数の名と純粋性だけを持ち、戻り値の欄が無い | 計画の「`PI-15` の戻り値の意味（05-07）」は当てない |

---

## 1. 範囲 —— 項目ごとの行き先

| 項目 | 裁定 | 仕様で変える所 | 4 節の編集 |
|---|---|---|---|
| `JDG-301`（Q8） | Ctrl ＋ Shift ＋ 0 を機能ごと捨て、ヘルプからも消す | 表 T-036 の `SK-17` を退ける ／ `FR-039` の `SK-17` の段 ／ 表 T-260 の `SE-1` ／ `FR-086` の「表 T-036 の 27 行」 ／ `display-words.json` の `SK-17` ／ `state-machines.json` の `displayScaleStepped`（H1） ／ `retired.py` ／ 組の指紋 | E-01 〜 E-04、E-53、E-60、E-61、E-62 |
| `JDG-302`（Q6） | `[vv]` は押した行を明示的に開き、配下をニュートラルへ。見出しの `[vv]` はすべてをニュートラルへ | 表 T-051 の `HF-2` ・ `HF-10` ・ `HF-18` と結びの `⇒` ／ 表 T-254 の `KO-2` ・ `KO-3` と結び | E-06 〜 E-12 |
| Q2 | `RS-61` の語を直す | `display-words.json` の `RS-61` | E-54 |
| Q7 | 倍率で描かれないタスクも選ばせない | 表 T-023c の結び（`FR-081`） | E-13 |
| Q9 | 端の入口では `FR-029` の通知を出さない | 表 T-260 の `SE-5` ／ `FR-029` | E-14、E-15 |
| Q10 | 端に着いた押しにも（最大）を添える | 表 T-260 の `SE-2` | E-03 |
| Q12 | 整列は期間を保って移す | `FR-034` | E-16 |
| Q13 | 合流で同じ id の行・注記は今の文書を保つ | 表 T-032 の `MG-12` | E-17 |
| Q14/484 | MSPDI の置き換えは設定を全部既定値に | 表 T-024a の `OP-6` | E-18 |
| Q14/485 | 足す暦・曜日の `ordinal` は最大の次 | `erd.json` の `AT-67` ・ `AT-72` | E-57、E-58 |
| Q14/488 | 全消しで作る行の id は新しく採る | 表 T-050 の結び（`FR-032`） | E-19 |
| Q14/491 | `TC-3` が作る行は最も浅い段の末子 | 表 T-239 の `TC-3` | E-20 |
| Q14/492 | 複製の根は元の WBS の親の下の兄弟 | `FR-033` | E-21 |
| Q15 | 入力中の Ctrl ＋ A は欄の文字を選ぶ | 表 T-028 の `IN-5a` | E-22、E-23 |
| Q16 | 選んだ `Task` を全部写す | `FR-033` ／ 表 T-108 の `CM-8` | E-21、E-24、E-47 |
| Q17 | 押し続けとドロップを契機に数える | 05-07 の 表 T-078 の `FT-1` ・ `FT-4` と結び | E-40 〜 E-42 |
| Q18 | 配りの最中の発話を拒む | 表 T-035 の `AG-11` ／ 05-07 の 5.5 | E-25、E-43 |
| Q19 | 重ねの取り込みを 1 段にする | 表 T-027 に `UN-18` | E-26 |
| Q20 | ドラッグ中の `Esc` はドラッグを先に取り消す | 表 T-028 の `IN-4` ／ 表 T-036 の結び ／ `S-99g` ・ `S-99h` の注 ／ 表 T-109 の `IC-52` | E-27、E-28、E-29、E-48、E-51、E-52 |
| Q21 | 表 T-071 に持ち越し 2 つを書く | 05-07 の `CA-1` ・ `CA-2` ・ `CA-4` | E-44 〜 E-46 |
| Q22 | 解除パスワードの入口もあとでやる | `FR-086` | E-30 |
| Q23 | 設定の欄は読むだけ | `FR-072` | E-31 |
| Q25 | 説明はポインタの点に出す | 表 T-028 の `IN-3` | E-32 |
| Q26 | 排他の選択で選ばれている入口を塗る | 表 T-237 に `EN-6`、結びの数 | E-33、E-34 |
| Q27 | 依存の種別を略号で出す | 表 T-018 の結び ／ 生成器 | E-35、E-63 |
| Q30 | 同じ名の購読は置き換え、戻り値で答える | 表 T-035 の `AG-6` ／ 表 T-004 の `LM-16` | E-36、E-37 |
| Q31 | 記録の理由に 表 T-220 の行も入れる | 表 T-263 の `IR-3` | E-38 |
| Q32 | 表 T-062 は根拠、表 T-075 は持ち主 | 05-07 の 5.2 | E-39 |
| Q33 | 錨の日付が `null` の箱を、その行の文書の開始日に描き、掴ませる | `UC-008` ／ `FR-019` | E-64、E-65 |
| Q34 | `IC-102` を `RC-13` から外す | 表 T-026 の `RC-13` | E-66 |
| Q3（綴りだけ） | `#rrggbb` と `"transparent"` | 表 T-017 の結び（⛔ `CR-548` まで当てない） | E-67 |
| S2 | `S-88` を 1.5 に | `settings.json` の `S-88` | E-49 |
| S3 | 暦日の日数をルーラーの帯に | 表 T-029a の `DC-3` ／ 辞書 ／ 生成器 | E-68、E-55、E-63 |
| S4 | 行の操作子と右端のあいだ 4px を行に | 表 T-206 に `S-313` ／ 表 T-051 の `HF-4` | E-59、E-69、E-63 |
| S5 | ヘルプの題の行を上に留める | ヘルプの段（`FR-036`） | E-74 |
| S6 | 帯を名前の箱より低くしない | 05-07 の 表 T-221 の `LF-3` | E-75 |
| `PND-1` | 注記の色を行に | 表 T-236 に `S-312` ／ `FR-019` の RATIONALE | E-56、E-70、E-63 |
| `PND-150` | 対話欄でも `Enter` で発話を確定する | 表 T-036 の `SK-19` | E-71 |
| `PND-176` | ホイールは装置の距離だけ動かし、端数は `S-176` に | `FR-016` のパンの段 | E-72 |
| `PND-232` | ずれが `null` の箱は `{0,0}` | `FR-019` | E-65 |
| Q5 | 取り下げ（`SK-17` が無くなった） | — | — |

**数**: 項目 38（上の表の 39 行から Q5 を除いた数。H1 は `JDG-301` の行に入る）。編集の単位は 70（E-01 〜 E-72。E-05 と E-50 は欠番）。

---

## 2. 新しい識別子（2026-09-22 に `50f925db` と、手元の 59 のブランチの先端で測った）

⛔ 規則 02 の 2.5 節: **当てる直前に、同じ測り方で空いていることを測り直すこと。**

| 識別子 | 置く所 | 項目 |
|---|---|---|
| `UN-18` | 01-04 の 表 T-027 | Q19 |
| `EN-6` | 01-04 の 表 T-237 | Q26 |
| `S-312` | `settings.json` → 表 T-236 | `PND-1` |
| `S-313` | `settings.json` → 表 T-206 | S4 |

- 4 つの識別子は、起草の日（2026-09-22、`50f925db` と 59 のブランチの先端を `git grep` で引いて 0 件。12 節）にも、当てる直前（同じ日、`783f276a` の木で同じ測り方）にも、どこにも使われていなかった。いまは本書が使っている。
- 測った最大: `UN-` は `UN-17`、`EN-` は `EN-5`、`S-` は `S-311`（`S-999` は試験の番兵なので除いた）。
- 接頭辞の登録簿は 160 件のまま（`UN` ・ `EN` ・ `S` は登録済み）。新しい表の番号は 0 である。
- `DC-` は新しい行を立てない（`DC-3` を書き直す）。
- 退ける識別子: `SK-17`（`retired.py` に理由つきで入れる。E-61）。

---

## 3. ⛔ 消すものを先に列挙する（`JDG-301`）

| 消すもの | 所（`50f925db`） | 編集 |
|---|---|---|
| 表 T-036 の `SK-17` の行 | `01-04-requirements.md:4182` | E-01 |
| `FR-039` の `SK-17` の段（1 行） | `01-04-requirements.md:6740` | E-02 |
| 表 T-260 の `SE-1` の `SK-17` の 2 か所 | `01-04-requirements.md:6747` | E-03 前半 |
| 辞書の `shortcuts` の `SK-17` | `_source/display-words.json:3324-3330` | E-53 |
| 状態機械の原稿の `displayScaleStepped.source.rows` の `"SK-17"` | `_source/state-machines.json:491` | E-60（H1） |
| 組の指紋 `T-036 SK-17 4ddb1ba2b876a116` | `.claude/skills/spec-graph-check/dictionary-table-pairing.txt:56` | E-62 |

⚠️ 「同・」の罠（規則 02 の 4 節）: 表 T-036 で `SK-17` の次の行 `SK-18` は「全体を収める（`FR-055`）」と自分の中身を持ち、`SK-17` を継いでいない。表 T-036 に「同・」「同上」は 1 つも無い（数えた）。
⚠️ `SK-17` を指す仕様の中の箇所は、`impact.py SK-17` で `FR-039`（2 か所）と、生成される `tbl-state-machines.md:55` の 3 つだけである。3 つとも上で消える。

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**:
- 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（規則 02 の 4 節）。旧を足すだけの編集（挿入）は、旧に錨の行を置き、新は錨の行に足す行を続けたものである。
- 行末の空白 2 つ（強制改行）は旧にも新にもそのまま書いてある。消さないこと。
- 旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である（12 節の照合の台本がこの形を読む）。
- 新しく書いた文には `**` を 1 つも使っていない（CJK の直後の `**` の罠を避けるため）。

### 4.1 `JDG-301` —— Ctrl ＋ Shift ＋ 0 を捨てる

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
旧（`:4181-4183`。`SK-17` の行を、前後の行を錨にして消す）
```text
| SK-23 | 日程表の図形と字を 1 段小さく描く（表示の倍率の段は `S-234`） | `Ctrl` ＋ `Shift` ＋ `-` | IC-104 |
| SK-17 | `GRS` の見た目を戻す —— 表示の倍率と日程表のズームを戻す（戻す先は `FR-039`） | `Ctrl` ＋ `Shift` ＋ `0` | — |
| SK-18 | 全体を収める（`FR-055`） | `F` | IC-10 |
```
新
```text
| SK-23 | 日程表の図形と字を 1 段小さく描く（表示の倍率の段は `S-234`） | `Ctrl` ＋ `Shift` ＋ `-` | IC-104 |
| SK-18 | 全体を収める（`FR-055`） | `F` | IC-10 |
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
旧（`:6739-6741`。`FR-039` の `SK-17` の段を消す）
```text
⭐ 表示の倍率を 1 段ずつ上げ下げする割当は、表 T-036 の `SK-22`（上げる）と `SK-23`（下げる）である。  
⭐ 表 T-036 の `SK-17` が押されたときは、表示の倍率を `S-234` の既定の段へ、日程表のズーム（同書の 表 T-203 の `S-75` / `S-76`）を等倍へ戻すこと（MUST） —— 倍率を 2 つ持つ `GRS` の見た目を、1 つのキーで元に戻せる。  
⭐ 表示の倍率を変える入口と割当が押されるたびに、いまの倍率を示すメッセージを 表 T-260 に従って出すこと（MUST） —— 段が 10 あり、絵の大きさだけではいまどの段にいるかが読めない。
```
新
```text
⭐ 表示の倍率を 1 段ずつ上げ下げする割当は、表 T-036 の `SK-22`（上げる）と `SK-23`（下げる）である。  
⭐ 表示の倍率を変える入口と割当が押されるたびに、いまの倍率を示すメッセージを 表 T-260 に従って出すこと（MUST） —— 段が 10 あり、絵の大きさだけではいまどの段にいるかが読めない。
```

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md -->
旧（`:6747-6748`。`SE-1` から `SK-17` を消し、`SE-2` を Q10 で広げる）
```text
| SE-1 | 出すとき | `IC-104` / `IC-105` と 表 T-036 の `SK-22` / `SK-23` / `SK-17` が押されるたびに出すこと（MUST）。<br>押しても倍率が変わらなかったとき（いちばん大きい段で上げる側、いちばん小さい段で下げる側、既定の段で `SK-17`）も出すこと（MUST） |
| SE-2 | 中身 | 押したあとの `S-234` の値に `%` を付けて示すこと（MUST）（例: `125%`）。<br>⭐ いちばん大きい段で上げる側を押して倍率が変わらなかったときは最大であることを示す語を、いちばん小さい段で下げる側を押して変わらなかったときは最小であることを示す語を、数の後に添えること（MUST）。<br>語は `FR-038` の辞書が持つ |
```
新
```text
| SE-1 | 出すとき | `IC-104` / `IC-105` と 表 T-036 の `SK-22` / `SK-23` が押されるたびに出すこと（MUST）。<br>押しても倍率が変わらなかったとき（いちばん大きい段で上げる側、いちばん小さい段で下げる側）も出すこと（MUST） |
| SE-2 | 中身 | 押したあとの `S-234` の値に `%` を付けて示すこと（MUST）（例: `125%`）。<br>⭐ 押したあとの倍率がいちばん大きい段のときは最大であることを示す語を、いちばん小さい段のときは最小であることを示す語を、数の後に添えること（MUST） —— 端の段に着いた押しでも、端の段で押して変わらなかったときでも同じである。<br>着いた押しに添えないと、端にいることを知るためにもう 1 度押すことになる。<br>語は `FR-038` の辞書が持つ |
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
旧（`:4924` の一部。`FR-086` が数えた 表 T-036 の行数）
```text
表 T-036 の 27 行・
```
新
```text
表 T-036 の 26 行・
```

⚠️ E-04 は `SK-17` を退けた後の数である（表 T-036 は 27 行 → 26 行）。当てた後に 表 T-036 の `| SK-` の行を数え直して 26 であることを確かめる。

### 4.2 `JDG-302` —— `[vv]` の 3 つの状態

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md -->
旧（`:1660`、`HF-2` の中）
```text
⭐ その行が抱えている畳み込みが 0 のときは、`FR-029` に従って薄く描くこと（MUST） —— ⭐ その数を示すのが `HF-18` であり、示す数と構えの条件は同じ 1 つである。<br>
```
新
```text
⭐ 押したときは、押した行に開いたままの印を立て、その配下の行はすべて人の指定の無い状態へ戻すこと（MUST） —— 配下の畳み・隠し・開いたままの印を外す（書き換える行は 表 T-254 の `KO-2`）。<br>押した人がその行の中を見たいことは明らかだが、どの深さまで開きたいかは明らかでないので、直下の子より深い行は `FR-018` の倍率に従わせる。<br>⭐ 押しても何も変わらないときだけ、`FR-029` に従って薄く描くこと（MUST） —— 押しが何かを変えるのは、押した行かその配下に畳みか隠しがあるとき、配下に開いたままの印があるとき、押した行に印が無く直下の子が `FR-018` の倍率で描かれていないときである。<br>⭐ 倍率で描かれていない直下の子は、開ける子として数える（数え方は `HF-13` と同じである）。<br>⚠️ そのため、構えの条件は `HF-18` の数と同じではない —— `HF-18` は人が畳んだ分だけを数え、倍率が落とした行も開いたままの印も数えない。<br>
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md -->
旧（`:1668`、`HF-10` の末尾）
```text
⚠️ **倍率も表示位置も動かさない** —— 動かすのは `FR-055` であり、あちらとは別の操作である |
```
新
```text
⚠️ **倍率も表示位置も動かさない** —— 動かすのは `FR-055` であり、あちらとは別の操作である<br>⭐ 押したときは、すべての行を人の指定の無い状態へ戻すこと（MUST） —— 畳みと隠し（`HR-1`）に加えて、開いたままの印もすべて外す（表 T-254 の `KO-3`）。<br>⭐ 畳み・隠し・開いたままの印のどれも 1 つも無いときだけ、`FR-029` に従って薄く描くこと（MUST） —— 印も本行が戻すものなので、印が 1 つでも立っていれば押しは何かを変える。<br>⚠️ 薄いときの理由は 表 T-233 の `RS-31` である |
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md -->
旧（`:1677`、`HF-18` の中）
```text
⛔ 数に入れないと、`HF-2` が構えているのに数が 0 になる場面ができる。<br>
```
新
```text
⛔ 数に入れないと、隠した行を抱えて `HF-2` が構えているのに数が 0 になる場面ができる。<br>
```

<!-- EDIT id=E-73 file=docs/spec/01-04-requirements.md -->
旧（`:1681`、表 T-051 の結び。前に立つ者の判断 2 から導いた）
```text
⛔ その操作で、描かれる行が 1 行も増減しないときは、対象が 1 つも無いものとして扱うこと（MUST） —— 畳む相手が描かれていても、その相手が配下を持たなければ、畳んで隠れる行は 1 つも無い。
```
新
```text
⛔ その操作で、描かれる行が 1 行も増減しないときは、対象が 1 つも無いものとして扱うこと（MUST） —— 畳む相手が描かれていても、その相手が配下を持たなければ、畳んで隠れる行は 1 つも無い。
⚠️ ただし `HF-2` と `HF-10` は、開いたままの印（表 T-254）を外すことも、行うことに数える —— 印も両者が人の指定の無い状態へ戻すものだからである（`HF-2`、`HF-10`）。
```

⚠️ E-73 は前に立つ者の判断 2（「押しても何も変わらないときだけ薄い」、印も `[vv]` と見出しの `[vv]` が戻すもの）を当てると、上の「描かれる行の差で数える」とぶつかるので足した。

<!-- EDIT id=E-09 file=docs/spec/01-04-requirements.md -->
旧（`:1698`）
```text
⇒ `HF-10` は `HF-2` を段 0 に対して行うので、印を立てる相手はすべての行になる（同表の `KO-3`）。
```
新
```text
⇒ `HF-10` は `HF-2` を段 0 に対して行うので、段 0 には印を立てず（上の段）、その配下にあたるすべての行から印を外す（同表の `KO-3`）。
```

<!-- EDIT id=E-10 file=docs/spec/01-04-requirements.md -->
旧（`:4439-4440`）
```text
| KO-2 | `HF-2`（`HR-3`） | 立てる | 押した行と、その配下のすべての行 |
| KO-3 | `HF-10`（`HR-1`） | 立てる | すべての行 |
```
新
```text
| KO-2 | `HF-2`（`HR-3`） | 押した行には立て、配下の行からは外す | 押した行と、その配下のすべての行 |
| KO-3 | `HF-10`（`HR-1`） | 外す | すべての行 |
```

<!-- EDIT id=E-11 file=docs/spec/01-04-requirements.md -->
旧（`:4448`、表 T-254 の結び）
```text
⚠️ `KO-1` が何かを行うかどうかは 表 T-051 の `HF-13` が持ち、倍率で描かれていない直下の子もそこで数える。  
```
新
```text
⚠️ `KO-1` が何かを行うかどうかは 表 T-051 の `HF-13` が持ち、倍率で描かれていない直下の子もそこで数える。  
⚠️ `KO-2` が何かを行うかどうかは同表の `HF-2`、`KO-3` は同表の `HF-10` が持つ。  
```

<!-- EDIT id=E-12 file=docs/spec/01-04-requirements.md -->
旧（`:4450`）
```text
⭐ 外すのは、押した行か、その祖先で押した入口だけである —— 子孫で `KO-4` や `KO-5` を押しても、祖先の印は残る。  
```
新
```text
⭐ 印を外すのは、押した行かその祖先で押した入口（`KO-2` の配下、`KO-4`、`KO-5`）と、すべての行を相手にする入口（`KO-3`、`KO-6`、`KO-7`）だけである —— 子孫で `KO-4` や `KO-5` を押しても、祖先の印は残る。  
```

### 4.3 Q7・Q9・Q12・Q13・Q14・Q15・Q16

<!-- EDIT id=E-13 file=docs/spec/01-04-requirements.md -->
旧（`:2217-2220`、表 T-023c の結び）
```text
描かれていないタスクとは、表 T-202 の表示の可否（`FR-049`）によって、予定・実績・実績のダミー（表 T-240）のどれも描かれていないタスクである。  
そのタスクは、`SL-2` 〜 `SL-5` のどの選び方でも取らない —— 全選択（`SL-5`）も範囲選択（`SL-3`）も取らない。  
表示の切り替えでそうなったタスクは、その時点で選択から外し、`SL-7b` の順序からも除く —— 除かないと、`FR-034` の「最後に選んだタスク」が見えないタスクになり、見えない日付へ揃う。  
⚠️ 消えた対象を落とす時点を定めない上の段とは違い、こちらは時点を定める —— 入口は表示の切り替えの 1 つだけであり、選んだまま隠れた対象は、隠れているあいだ選択の枠（`SL-8`）が描けない。  
```
新
```text
描かれていないタスクとは、表 T-202 の表示の可否（`FR-049`）によって、予定・実績・実績のダミー（表 T-240）のどれも描かれていないタスクと、描かれていない行に載るタスクである —— 描かれていない行とは、人が畳んだ行・隠した行の配下（表 T-015 の `HR-1a` / `HR-6`）と、行の軸の倍率（`FR-018`）で描かれていない行である。
そのタスクは、`SL-2` 〜 `SL-5` のどの選び方でも取らない —— 全選択（`SL-5`）も範囲選択（`SL-3`）も取らない。
表示の切り替え、行の畳みと隠し、行の軸の倍率の変更でそうなったタスクは、その時点で選択から外し、`SL-7b` の順序からも除く —— 除かないと、`FR-034` の「最後に選んだタスク」が見えないタスクになり、見えない日付へ揃う。
⚠️ 消えた対象を落とす時点を定めない上の段とは違い、こちらは時点を定める —— 入口は表示の切り替え・行の畳みと隠し・行の軸の倍率であり、選んだまま隠れた対象は、隠れているあいだ選択の枠（`SL-8`）が描けない。
⚠️ 代償: 縮めたり畳んだりしたまま全体を選んで消すことはできない —— 見えない所で変更が起きないことを先に取る。  
```

<!-- EDIT id=E-14 file=docs/spec/01-04-requirements.md -->
旧（`:6751`、`SE-5` の末尾）
```text
端の段にいることは `FR-029` の薄い入口が示し続けるので、読み落としても失うものが無い |
```
新
```text
端の段にいることは `FR-029` の薄い入口が示し続けるので、読み落としても失うものが無い<br>⭐ 端の段で押して倍率が変わらなかった入口（`SE-1` の後段と、行の軸のズームの 表 T-262 の `ZE-2` / `ZE-3`）には、`FR-029` の行えない理由の通知を出してはならない（MUST NOT） —— 端にいることは本表のメッセージが告げており、通知を重ねると同じことを 2 か所で言い、通知は人が消すまで残る。<br>⚠️ その入口を `FR-029` のとおり薄く描くことは変わらない |
```

<!-- EDIT id=E-15 file=docs/spec/01-04-requirements.md -->
旧（`:6297`、`FR-029` の中）
```text
⚠️ **どの入口にも当たる行が無いときの落ち先が `RS-27` である。**  
```
新
```text
⚠️ **どの入口にも当たる行が無いときの落ち先が `RS-27` である。**  
⚠️ 表示の倍率と行の軸のズームの入口を端の段で押したときは、この通知を出さない —— その例外は `FR-039` の 表 T-260 の `SE-5` が持つ。  
```

<!-- EDIT id=E-16 file=docs/spec/01-04-requirements.md -->
旧（`:2433`、`FR-034` の STATEMENT の末尾）
```text
**`GRS` が自動で横へ動かしてはならない（MUST NOT）。**
```
新
```text
**`GRS` が自動で横へ動かしてはならない（MUST NOT）。**  
揃えるほうの端を動かすときは、もう一方の端も同じ暦日の数だけ動かし、期間を保つこと（MUST） —— 長さが変わらないので、揃えた結果が Chapter 6.1 の 表 T-220 の `IV-10` に拒まれることは無い。  
⚠️ 片方の端だけを動かすのは揃えることではなく伸ばすことであり、期間を変えるときは端を掴んで引く。
```

<!-- EDIT id=E-17 file=docs/spec/01-04-requirements.md -->
旧（`:5171`、`MG-12` の末尾）
```text
⚠️ **設定の項目ごとに問うてはならない（MUST NOT）** —— 問う粒度は `MG-9` に従い、`documentSettings` 全体へ一括して適用する |
```
新
```text
⚠️ **設定の項目ごとに問うてはならない（MUST NOT）** —— 問う粒度は `MG-9` に従い、`documentSettings` 全体へ一括して適用する<br>⭐ 同じ id の行（`TaskGroup`）または注記が両側にあり、中身が違うときは、現在の文書の中身を保ち、取込元の違いを捨てること（MUST） —— `MG-1` の既定と同じ安全側である。<br>⚠️ `Task` の見た目と行の所属は本段ではなく `MG-8a` に従う |
```

<!-- EDIT id=E-18 file=docs/spec/01-04-requirements.md -->
旧（`:5569`、`OP-6` の末尾）
```text
⚠️ **合流を選んだときは復元しない** —— 現在の文書へ足す動作と両立しないので、表 T-032 の `MG-12` に従う |
```
新
```text
⚠️ **合流を選んだときは復元しない** —— 現在の文書へ足す動作と両立しないので、表 T-032 の `MG-12` に従う<br>⭐ 置き換えを選んで MSPDI を読んだときは、`documentSettings` のすべての項目を既定値とすること（MUST） —— MSPDI は設定を持たず、置き換えは現在の文書を捨てる（`OP-3`）ので、捨てる文書の見た目を持ち越す道が無い |
```

<!-- EDIT id=E-19 file=docs/spec/01-04-requirements.md -->
旧（`:2285`、表 T-050 の結び）
```text
⭐ **深さが `L1` になるのは導出の結果である** —— `FR-004` は深さを親から導くので、親を持たない行は `L1` のほかに立ちようがない。  
```
新
```text
⭐ **深さが `L1` になるのは導出の結果である** —— `FR-004` は深さを親から導くので、親を持たない行は `L1` のほかに立ちようがない。  
⭐ その行の id は、表 T-108 の `CM-26` が行を作るときと同じく、新しく採ること（MUST）。  
⛔ 決まった値を使ってはならない（MUST NOT） —— 行を全部消した 2 つの文書を合流させると（表 T-032 の `MG-12`）、別々に作った行が同じ行と見なされる。  
```

<!-- EDIT id=E-20 file=docs/spec/01-04-requirements.md -->
旧（`:1073`、`TC-3` の末尾）
```text
指す `TaskGroup` が無いときは行を 1 つ作ってそこへ載せること（MUST） —— 下へドラッグすれば、行の無い縦位置を指せる。<br> |
```
新
```text
指す `TaskGroup` が無いときは行を 1 つ作ってそこへ載せること（MUST） —— 下へドラッグすれば、行の無い縦位置を指せる。<br>⭐ 作る行は、最も浅い段の末子とすること（MUST） —— 行の無い縦位置は描かれた最下段の下であり、そこに描かれるのは最も浅い段の末子である（表 T-051 の `HF-17` が段 0 へ足す行も同じ所に置く）。<br> |
```

<!-- EDIT id=E-21 file=docs/spec/01-04-requirements.md -->
旧（`:2384-2386`、`FR-033` の依存の段のあと。⛔ `:2374`〜`:2376` と `:2388` には触れない）
```text
部分木の外へ出る依存を複製してはならない（MUST NOT） —— 外へ出る依存を写すと、貼り付けるたびに同じ先行へ依存線が増える。

複製した `Task` に、複製元と同じ `UID` を使ってはならない（MUST NOT）。  
```
新
```text
部分木の外へ出る依存を複製してはならない（MUST NOT） —— 外へ出る依存を写すと、貼り付けるたびに同じ先行へ依存線が増える。

⭐ `Task` が 2 つ以上選ばれているときは、選ばれた `Task` をすべて複製し、それぞれを上の段のとおり複製元と同じ行に載せること（MUST） —— 同じ形の工程をまとめて横へ並べられる。  
⭐ 選ばれた `Task` のうち、ほかの選ばれた `Task` の WBS の子孫であるものは、その祖先の部分木として 1 度だけ複製すること（MUST） —— 2 度写すと同じ工程が 2 つできる。  
⭐ 複製の根（選ばれた `Task` の複製）は、複製元と同じ WBS の親の下に兄弟として置き、部分木の内側の親子は複製どうしへ付け替えること（MUST） —— 根にすると元の親の集計から外れ、同じ形の工程を並べるという目的を満たさない。  
⚠️ 複製元を運ぶ命令は 表 T-108 の `CM-8` である。

複製した `Task` に、複製元と同じ `UID` を使ってはならない（MUST NOT）。  
```

<!-- EDIT id=E-24 file=docs/spec/01-04-requirements.md -->
旧（`:2402-2403`、`FR-033` の貼り付け先）
```text
貼り付け先は、選んでいる行の子とすること（MUST）。  
何も選んでいないときは最上位に置くこと（MUST）。  
```
新
```text
貼り付け先は、選んでいる行の子とすること（MUST）。  
何も選んでいないときは最上位に置くこと（MUST）。  
⛔ 貼り付け先として行が 2 つ以上選ばれているときは、貼り付けを受け付けずに通知すること（MUST） —— どの行の子とするかが決まらない。  
```

<!-- EDIT id=E-22 file=docs/spec/01-04-requirements.md -->
旧（`:6806`、`IN-5a` の中。対話欄の段の括弧）
```text
本行のキー（単文字キー・`Delete` / `Backspace`・下の `SK-4` / `SK-5`）は欄への打鍵としてブラウザへ渡し
```
新
```text
本行のキー（単文字キー・`Delete` / `Backspace`・下の `SK-4` / `SK-5` / `SK-2`）は欄への打鍵としてブラウザへ渡し
```

<!-- EDIT id=E-23 file=docs/spec/01-04-requirements.md -->
旧（`:6806`、`IN-5a` の中）
```text
渡さないと、その場で入力している文字をコピーも貼り付けもできない。<br>
```
新
```text
渡さないと、その場で入力している文字をコピーも貼り付けもできない。<br>⭐ `SK-2`（`Ctrl+A`）も同じく、文字入力を確定していない間はショートカットとして効かせず、欄の文字をすべて選ぶ操作としてブラウザへ渡すこと（MUST） —— 渡さないと、打ち直すために欄の文字をまとめて選ぶ道がポインタしか無い。<br>⚠️ 代償: 入力しているあいだに図のすべてを選ぶことはできない。<br>
```

<!-- EDIT id=E-47 file=docs/spec/_assets/tbl-glossary.md -->
旧（`:414`）
```text
| CM-8 | `Task` | `pasteTaskSubtree` | ⭐ | 部分木を複製する | `FR-033` |
```
新
```text
| CM-8 | `Task` | `pasteTaskSubtree` | ⭐ | 部分木を複製する（複製元の `Task` を 1 つ以上運ぶ） | `FR-033` |
```

### 4.4 Q18・Q19・Q20・Q22・Q23・Q25・Q26・Q27・Q30・Q31

<!-- EDIT id=E-25 file=docs/spec/01-04-requirements.md -->
旧（`:6065`、`AG-11` の末尾）
```text
**確定していない入力途中の文字を読めてはならない（MUST NOT）** |
```
新
```text
**確定していない入力途中の文字を読めてはならない（MUST NOT）**<br>⛔ 監視が通知を配っているあいだの発話は、書き込みと同じく受け付けてはならない（MUST NOT） —— 配りを受けた購読者どうしが互いに答えると、配りが終わらない。<br>拒否は Chapter 5.5 が持ち、理由は 表 T-233 の `RS-9` である |
```

<!-- EDIT id=E-26 file=docs/spec/01-04-requirements.md -->
旧（`:2124`）
```text
| UN-6a | 対象 | **依存線の追加・削除・ラグの変更** —— `FR-032` と `SK-3` が依存線を消せると定めている以上、戻せなければならない |
```
新
```text
| UN-6a | 対象 | **依存線の追加・削除・ラグの変更** —— `FR-032` と `SK-3` が依存線を消せると定めている以上、戻せなければならない |
| UN-18 | 対象 | 重ねの取り込み（表 T-024a の `OP-9`、`FR-015`）。<br>取り込み 1 回を 1 段とする —— 重ねた変更前の予定を外す入口はほかに無く、間違えて重ねたときに戻す道が取り消しだけである |
```

<!-- EDIT id=E-27 file=docs/spec/01-04-requirements.md -->
旧（`:6803`、`IN-4` の段の並び）
```text
消費する階層は 出ている通知 → 確定していないその場の編集 → 開いている面 → 進行中のドラッグ・引きかけの矢印 → 構え → 選択 → `Dual Cursor` モード → 出ている説明 の順とすること（MUST） —— 説明を最後に置くのは、`IN-3` が求める「消せること」を果たす手立てがほかに 1 つも無いからである。<br>
```
新
```text
消費する階層は 出ている通知 → 確定していないその場の編集 → 開いている面 → 進行中のドラッグ・引きかけの矢印 → プロパティパネル → 構え → 選択 → `Dual Cursor` モード → 出ている説明 の順とすること（MUST） —— 説明を最後に置くのは、`IN-3` が求める「消せること」を果たす手立てがほかに 1 つも無いからである。<br>⭐ プロパティパネルを進行中のドラッグの次に置くのは、ドラッグ中の `Esc` がほぼ「このドラッグをやめたい」という意味だからである。<br>⛔ ドラッグ中にパネルを先に閉じてはならない（MUST NOT） —— 閉じると日程が横に広がり（`FR-052`）、掴んでいるバーの下で絵が動き、やめるにはもう 1 度押すことになる。<br>⚠️ プロパティパネルは「開いている面」（`_assets/tbl-settings.md` の 表 T-206 の `S-99g`）ではない —— 画面の上に重ねず、段も面とは別である（`S-99h`）。<br>
```

<!-- EDIT id=E-28 file=docs/spec/01-04-requirements.md -->
旧（`:6803`、`IN-4` の中）
```text
プロパティパネルは「開いている面」に当たり、本行の並びで選択より上に在るので、出ているあいだの `Esc` はそちらが先に消費する。<br>
```
新
```text
プロパティパネルは本行の並びで選択より上の段に在るので、出ているあいだの `Esc` はそちらが先に消費する。<br>
```

<!-- EDIT id=E-29 file=docs/spec/01-04-requirements.md -->
旧（`:4197`、表 T-036 の結び）
```text
表 T-028 の `IN-4` が `Esc` の第 1 階層を「開いている面」へ既に与えており
```
新
```text
表 T-028 の `IN-4` が `Esc` の段で「開いている面」をプロパティパネルより上に置いており
```

⚠️ **表 T-283 に段が 1 つ加わる（`RG` の行が 1 つ増える）。**（前に立つ者の判断 1。状態機械のセッションが `IN-4` の段を写す表 T-283 を当てている最中である。本書を当てた時点の木に 表 T-283 は無い（`grep` で 0 件、2026-09-22）ので、状態機械のセッションが表を直す。）

<!-- EDIT id=E-30 file=docs/spec/01-04-requirements.md -->
旧（`:4931`、`FR-086` の STATEMENT の末尾）
```text
作成者が**透かし解除パスワード**を設定したときは、その SHA-256 だけを保存すること（生の値の扱いは `FR-020`）。
```
新
```text
作成者が**透かし解除パスワード**を設定したときは、その SHA-256 だけを保存すること（生の値の扱いは `FR-020`）。  
⛔ ただし、解除パスワードを設定させる道も、いまは実装しない —— 読み手はすべて 表 T-207 の既定の解除パスワードで透かしを消せる。  
⚠️ 代償を隠さない —— 全員が既定のパスワードのままであり、`LM-1` のとおりもともと弱い抑止である。  
⭐ 表 T-206 の `S-99c` の席は残す —— 入口は名前の入口と一緒に後で足す。
```

<!-- EDIT id=E-31 file=docs/spec/01-04-requirements.md -->
旧（`:1889`、`FR-072` の STATEMENT の末尾）
```text
⛔ **パネルを出すのをやめても、選択を解いてはならない（MUST NOT）**。
```
新
```text
⛔ **パネルを出すのをやめても、選択を解いてはならない（MUST NOT）**。  
⭐ パネルが文書の設定を出しているあいだ、その欄は読むだけとすること（MUST）。  
⛔ 編集できると示してはならない（MUST NOT） —— 設定を変える道は、それぞれの要求が置く入口（`Command Palette` ほか）だけであり、欄に書き換える手段が無い。  
⚠️ 代償: 入口を持たない設定の値は、画面からは変えられない。
```

<!-- EDIT id=E-32 file=docs/spec/01-04-requirements.md -->
旧（`:6802`、`IN-3` の末尾）
```text
⚠️ **`FR-002` も自分では引き金を持たない** —— 打ち切った名称ラベルの全文は `EZ-6` の説明が出す |
```
新
```text
⚠️ **`FR-002` も自分では引き金を持たない** —— 打ち切った名称ラベルの全文は `EZ-6` の説明が出す<br>⭐ 例外は `FR-092` の `EZ-6`（タスクの説明）である —— 説明をポインタの点に出し、ポインタを受け取らせないこと（MUST）。<br>そのため「ポインタを乗せられること」は当たらず、ポインタが動いたら消す（`EZ-6`）。<br>離して出すと、説明が隣のバーを覆い、そのバーを押せなくする。<br>⚠️ 代償: 説明の文字を選んで写すことはできない |
```

<!-- EDIT id=E-33 file=docs/spec/01-04-requirements.md -->
旧（`:6327`）
```text
| EN-5 | その入口が表示・非表示を切り替えるものを、いま表示している（`_assets/tbl-settings.md` の 表 T-206） | `S-183` | 本要求 |
```
新
```text
| EN-5 | その入口が表示・非表示を切り替えるものを、いま表示している（`_assets/tbl-settings.md` の 表 T-206） | `S-183` | 本要求 |
| EN-6 | その入口が、排他の選択のうちいま選ばれているものである（`_assets/tbl-settings.md` の `S-66` のガイドカーソル） | `S-183` | `FR-048` |
```

<!-- EDIT id=E-34 file=docs/spec/01-04-requirements.md -->
旧（`:6329`）
```text
⚠️ `S-183` が 4 行に立つのは、いずれも「いま効いている」を意味するからである
```
新
```text
⚠️ `S-183` が 5 行に立つのは、いずれも「いま効いている」を意味するからである
```

<!-- EDIT id=E-35 file=docs/spec/01-04-requirements.md -->
旧（`:2492-2494`、表 T-018 の下）
```text
| DP-4 | 3 | SS（開始 → 開始） | 左辺の中央 | 左辺の中央 | 同側 | 2 / 4 |

依存線を構えているときの当たり判定は、タスクの左半分と右半分のどちらに当たったかを返すこと（MUST）。  
```
新
```text
| DP-4 | 3 | SS（開始 → 開始） | 左辺の中央 | 左辺の中央 | 同側 | 2 / 4 |

⭐ 画面に依存の種別を出すときは、本表の `名` の欄の略号（括弧の前の `FS` / `SF` / `FF` / `SS`）で出すこと（MUST）。  
⛔ 保存した数（`linkType`）をそのまま出してはならない（MUST NOT） —— 人には `1` が FS だと読めない。  
⚠️ 略号は言語で変わらない記号なので、`FR-038` の辞書には載せない —— 本表から生成して使う。

依存線を構えているときの当たり判定は、タスクの左半分と右半分のどちらに当たったかを返すこと（MUST）。  
```

<!-- EDIT id=E-36 file=docs/spec/01-04-requirements.md -->
旧（`:6064`、`AG-6` の末尾）
```text
確定していない下書きで起きてはならない（MUST NOT） |
```
新
```text
確定していない下書きで起きてはならない（MUST NOT）<br>⭐ 同じ名前（表 T-229 の `ED-2`）で 2 度購読したときは、新しい購読が古い購読を置き換えること（MUST）。<br>置き換えたことを、新しい購読の戻り値で答えること（MUST） —— 本行の「自分」は名前 1 つであり、1 つの名前に「まだ受け取っていない」は 1 つしか持てない。<br>⚠️ 先に購読していた側は、知らされないまま購読を失う（`LM-16`） |
```

<!-- EDIT id=E-37 file=docs/spec/01-04-requirements.md -->
旧（`:218`、`LM-16` の末尾）
```text
⚠️ 閉じるには監視の継ぎ目に書き手の識別を別で持たせることになり、それは別の判断である |
```
新
```text
⚠️ 閉じるには監視の継ぎ目に書き手の識別を別で持たせることになり、それは別の判断である<br>⚠️ 同じ語で監視を購読できるのも 1 体だけである —— 後から購読した呼び手が先の購読を置き換え（表 T-035 の `AG-6`）、先の呼び手は知らされないまま監視を失う |
```

<!-- EDIT id=E-38 file=docs/spec/01-04-requirements.md -->
旧（`:4693`）
```text
| IR-3 | 出ている通知の理由 | 出ている通知ごとに、その理由の行 ID（表 T-233 の `RS-` の行）を書くこと（MUST）。<br>⭐ 数だけでは、どの通知が出ていたかが読めない |
```
新
```text
| IR-3 | 出ている通知の理由 | 出ている通知ごとに、その理由の行 ID（表 T-233 の `RS-` の行、または Chapter 6.1 の 表 T-220 の行）を書くこと（MUST）。<br>⭐ 数だけでは、どの通知が出ていたかが読めない。<br>⚠️ 取り込みを拒んだ通知は 表 T-220 の行を運ぶ（`FR-076`、表 T-035 の `AG-9a`）—— 記録からも、検証のどの行で拒んだかが読める |
```

### 4.5 05-07 —— Q17・Q18・Q21・Q32

<!-- EDIT id=E-39 file=docs/spec/05-07-design.md -->
旧（`:147`）
```text
本表が定めるのはコンポーネントの境界だけである。
```
新
```text
本表が定めるのはコンポーネントの境界だけである。  
⭐ 本表の `正` の欄は、そのコンポーネントの寸法と規則の根拠がどの表と要求に在るかを示す。  
要求を負うユニット（持ち主）は 表 T-075 の `負う要求` の欄が示す —— 2 つの欄は粒度が違うので、別のユニットを指して見えても食い違いではない。
```

<!-- EDIT id=E-40 file=docs/spec/05-07-design.md -->
旧（`:912`、`FT-1` の頭）
```text
| FT-1 | 人の入力（ポインタとキー）。<br>
```
新
```text
| FT-1 | 人の入力（ポインタとキー）と、ファイルのドロップ（01-04 の 表 T-024a の `OP-2`）。<br>
```

<!-- EDIT id=E-41 file=docs/spec/05-07-design.md -->
旧（`:915`）
```text
| FT-4 | **時間が来たこと** | `SingleHtmlShell`（`CP-25`）が自分で計る | `FR-092` の `EZ-2` と `EZ-6` ／ 表 T-037 の `NT-2` |
```
新
```text
| FT-4 | **時間が来たこと** | `SingleHtmlShell`（`CP-25`）が自分で計る | `FR-092` の `EZ-2` と `EZ-6` ／ 表 T-037 の `NT-2` ／ `FR-039` の 表 T-260 の `SE-3` ／ `FR-018` の押し続け（`S-172` / `S-173`） |
```

<!-- EDIT id=E-42 file=docs/spec/05-07-design.md -->
旧（`:922-924`）
```text
⚠️ **`FT-4` が数えるのは 2 つである** —— アイコンの説明を出すまでの待ち時間（`S-124`）と、時間で消える通知の期限（`NT-2`）。  
**時計を読むのはシェルであり、`CS-1` の収集には入らない** —— 基準日と混同しないこと。  
⛔ **後者は、時間で消える通知が在るときにだけ数える** —— 表 T-037 の `NT-2` は選言であり
```
新
```text
⚠️ `FT-4` が数えるのは 4 つである —— アイコンの説明を出すまでの待ち時間（`S-124`）と、時間で消える通知の期限（`NT-2`）と、表示の倍率を示すメッセージを消すまでの時間（`S-244`、表 T-260 の `SE-3`）と、入口を押し続けたときの刻み（`FR-018` の `S-172` の待ちと `S-173` の間隔）。
⚠️ 数え方: 本表の `正` の欄が名指す、時間で起きることを定める行を数えた。  
コードの `frame-loop.ts` の `setTimeout` は 3 つ（ポインタの静止、倍率のメッセージ、押し続け）であり、通知の期限を持たないのは下の選言の前の枝を選んだからである。
**時計を読むのはシェルであり、`CS-1` の収集には入らない** —— 基準日と混同しないこと。
⛔ 通知の期限は、時間で消える通知が在るときにだけ数える —— 表 T-037 の `NT-2` は選言であり
```

⚠️ E-42 の数「4」は前に立つ者の判断 9 (a) により測った数である（2026-09-22、`50f925db`）: 仕様で時間が来て起きることを定める行は `S-124` ・ `NT-2` ・ `SE-3`（`S-244`）・ `S-172` / `S-173` の 4 つ、コードの `setTimeout` は `frame-loop.ts:2346`（ポインタの静止）・`:2375`（倍率のメッセージ）・`:2415`（押し続け）の 3 つ。Q17 の裁定の字は「2 → 3」だが、`SE-3` は Q17 より前から数え落とされていた。

<!-- EDIT id=E-43 file=docs/spec/05-07-design.md -->
旧（`:694`、5.5）
```text
待ち行列を作らないのは、`FR-028` が受理したか否かをその場で値で返すと定めているためである。
```
新
```text
待ち行列を作らないのは、`FR-028` が受理したか否かをその場で値で返すと定めているためである。  
⭐ 通知を配っているあいだの発話（表 T-035 の `AG-11`）も、書き込みと同じく拒否すること（MUST） —— 発話は日程データではないが監視を起こす（`AG-6`）ので、受けた購読者が答えると次の配りが起き、2 つの購読者が互いに答え続けると配りが終わらない。  
拒否の理由は、書き込みと同じ 表 T-233 の `RS-9` とする。
```

<!-- EDIT id=E-44 file=docs/spec/05-07-design.md -->
旧（`:999`、`CA-1` の末尾）
```text
⚠️ **文書そのものはキャッシュではない** —— 現在値であり、持ち主は `LY-5` が定めている |
```
新
```text
⚠️ **文書そのものはキャッシュではない** —— 現在値であり、持ち主は `LY-5` が定めている<br>⭐ ほかに、自分の鍵で失効する持ち越しを 2 つ持つ。<br>① 全体表示が選んだ表示位置（表 T-024a の `OP-10`）—— 鍵は画面の環境であり、文書が表示位置を持ったとき、別の文書になったとき、環境が変わったときに捨てる。<br>② 行の軸のズームの上限（`FR-016`）—— 鍵は帯を組む材料（日程データの群、ズームとスクロール位置を除く文書の設定、描く横の倍率、`Row Area` の矩形、段 0 の畳み、行の操作子の高さ、ズームの下限と上限）であり、どれかが変わったときに作り直す |
```

<!-- EDIT id=E-45 file=docs/spec/05-07-design.md -->
旧（`:1000`、`CA-2` の末尾）
```text
⚠️ `NFR-010` により、表 T-078 の契機が 1 つも無いフレームはそもそも走らない |
```
新
```text
⚠️ `NFR-010` により、表 T-078 の契機が 1 つも無いフレームはそもそも走らない<br>⚠️ `CA-1` の後段の 2 つはフレームの先頭で作り直さない —— 自分の鍵が変わったときにだけ作り直す |
```

<!-- EDIT id=E-46 file=docs/spec/05-07-design.md -->
旧（`:1002`、`CA-4` の末尾）
```text
**1 つだけが古いという状態を作ってはならない（MUST NOT）** |
```
新
```text
**1 つだけが古いという状態を作ってはならない（MUST NOT）**<br>⚠️ 自分の鍵で失効する `CA-1` の後段の 2 つは、本行の外である —— 鍵が同じあいだは古くならない |
```

### 4.6 用語集 —— Q20

<!-- EDIT id=E-48 file=docs/spec/_assets/tbl-glossary.md -->
旧（`:577`、`IC-52` の「何を担うか」）
```text
| — | 開いている面を閉じる | 表 T-028 の `IN-4` | — |
```
新
```text
| — | 開いている面とプロパティパネルを閉じる | 表 T-028 の `IN-4` | — |
```

### 4.7 原稿 `settings.json` —— S2・Q20・`PND-1`・S4

<!-- EDIT id=E-49 file=docs/spec/_source/settings.json -->
旧（`:2385-2388`）
```text
     "id": "S-88",
     "key": "`groupLevelOfDetailRatio`",
     "default": {
      "num": "1.875",
```
新
```text
     "id": "S-88",
     "key": "`groupLevelOfDetailRatio`",
     "default": {
      "num": "1.5",
```

⚠️ E-49 で深さ 3 / 4 / 5 が倍率 0.48 / 0.72 / 1.08 で描かれ始める（`S-87` 0.32 × 1.5 の（深さ − 2）乗。深さ 2 は 0.32）。この数は本書にだけ書き、原稿の注には写さない（値の行に導いた数を置くと、値を変えたとき腐る）。

<!-- EDIT id=E-51 file=docs/spec/_source/settings.json -->
旧（`S-99g` の注）
```text
      "ja": "同上。「面」とは、画面の上に重ねて開き、`Esc` の第 1 階層で閉じられるものをいう （表 T-028 の `IN-4`）"
```
新
```text
      "ja": "同上。「面」とは、画面の上に重ねて開き、`Esc` の段（表 T-028 の `IN-4`）の「開いている面」で閉じられるものをいう。⚠️ プロパティパネルは面ではない —— 同行は、進行中のドラッグの次にパネルの段を別に置いている（`S-99h`）"
```

<!-- EDIT id=E-52 file=docs/spec/_source/settings.json -->
旧（`S-99h` の注の一部）
```text
パネルは画面の上に重ねず、`Esc` の第 1 階層でも閉じない（表 T-028 の `IN-4` が別の段を与えている）
```
新
```text
パネルは画面の上に重ねず、`Esc` の段も面とは別である —— 表 T-028 の `IN-4` は、面より下、進行中のドラッグの次にパネルの段を与えている
```

<!-- EDIT id=E-56 file=docs/spec/_source/settings.json -->
旧（`S-160` の末尾と `S-161` の頭。`S-312` を間に入れる）
```text
      "ja": "⛔ **テーマの色相に追随させない** （`FR-041`）。⭐ **依存線とは別の色である**"
     }
    },
    {
     "id": "S-161",
```
新
```text
      "ja": "⛔ **テーマの色相に追随させない** （`FR-041`）。⭐ **依存線とは別の色である**"
     }
    },
    {
     "id": "S-312",
     "name": {
      "ja": "注記の色"
     },
     "light": {
      "colour": "#b45309",
      "code": true
     },
     "dark": {
      "colour": "#b45309",
      "code": true
     },
     "hue": {
      "ja": "—"
     },
     "note": {
      "ja": "線の色を指定しないハイライトボックスの枠と、コメントボックスの枠と引出しの線（`FR-019`）。テーマの色相に追随させない（`FR-041`）。⚠️ 色相 26 は依存線（`S-159`）と同じであり、離すことは `FR-019` が SHOULD に留めている。⚠️ 暗いテーマも明るいテーマと同じ値である —— 暗いテーマで選び直していない"
     }
    },
    {
     "id": "S-161",
```

<!-- EDIT id=E-59 file=docs/spec/_source/settings.json -->
旧（`S-244` の頭。`S-313` をその前に入れる）
```text
    {
     "id": "S-244",
```
新
```text
    {
     "id": "S-313",
     "value": {
      "ja": "行見出しパネルの右端と、並びのいちばん右の行の操作子の外形とのあいだ（表 T-051 の `HF-4`）"
     },
     "default": {
      "num": "4",
      "suffix": "px"
     },
     "note": {
      "ja": "出荷ビルドが使っていた値をそのまま行にしたものである。⚠️ `S-235` も表示の倍率も掛けない —— 出荷ビルドは掛けずに描いている。保存しない理由は `S-138` と同じである"
     }
    },
    {
     "id": "S-244",
```

### 4.8 原稿 `display-words.json` —— `JDG-301`・Q2・S3

<!-- EDIT id=E-53 file=docs/spec/_source/display-words.json -->
旧（`:3323-3331`。`SK-17` の項を消す）
```text
  {
   "rowId": "SK-17",
   "text": {
    "ja": "表示の倍率とズームを戻す",
    "en": "Reset display scale and zoom"
   }
  },
  {
   "rowId": "SK-21",
```
新
```text
  {
   "rowId": "SK-21",
```

<!-- EDIT id=E-54 file=docs/spec/_source/display-words.json -->
旧（`:2837-2842`、`RS-61`）
```text
    "ja": "この行は別の編集グループのものです",
    "en": "This row belongs to another edit group"
   },
   "nextStep": {
    "ja": "自分の行へコピーしてから編集してください",
    "en": "Copy it into one of your own rows, then edit it"
```
新
```text
    "ja": "この行は読み取り専用です（編集グループが設定されています）",
    "en": "This row is read-only (an edit group is set on it)"
   },
   "nextStep": {
    "ja": "編集グループの無い行へコピーしてから編集してください",
    "en": "Copy it into a row with no edit group, then edit it"
```

⚠️ E-54 の en は「en も同じ意味にそろえる」（Q2 の B）に従って本書が書いた。⛔ `RS-61` の指紋（`t-233` の契約試験 `:493` の `PAIRED_ON_2026_09_03`）は前に立つ者が場面と語を読み合わせてから書く。本書も当てる体も書かない（`CR-433` ⑫）。

<!-- EDIT id=E-55 file=docs/spec/_source/display-words.json -->
旧（`:3596-3603`、`scaleEcho` の末尾とファイルの終わり）
```text
   "end": "min",
   "text": {
    "ja": "（最小）",
    "en": " (min)"
   }
  }
 ]
}
```
新
```text
   "end": "min",
   "text": {
    "ja": "（最小）",
    "en": " (min)"
   }
  }
 ],
 "dualCursorSpan": [
  {
   "rowId": "DC-3",
   "text": {
    "ja": "{n} 日",
    "en": "{n} days"
   }
  }
 ]
}
```

⚠️ E-55 の ja「{n} 日」は S3 の ① の字（「14 日」）である。⛔ **en の「{n} days」は穴である** —— 候補として当ててあるが、利用者の語を待っている（前に立つ者の判断 3）。原稿の `$comment` は体が語を発明することを禁じているので、利用者の語が来たらこの 1 語だけを差し替える。

### 4.9 原稿 `erd.json` —— Q14/485

<!-- EDIT id=E-57 file=docs/spec/_source/erd.json -->
旧（`:1707`、`AT-67` の `meaning`）
```text
      "ja": "出現順。書き出しで元の並びに戻す"
```
新
```text
      "ja": "出現順。書き出しで元の並びに戻す。`GRS` が足す暦（合流の `MG-5` ほか）は、既にある暦の最大の次を取る"
```

<!-- EDIT id=E-58 file=docs/spec/_source/erd.json -->
旧（`:1806-1819`、`AT-72` の `meaning`。「親の中での出現順」はファイルに 2 回ある（`:1818` と `:1923`）ので、`"seat": 72` から錨にして 1 か所に絞る）
```text
     "seat": 72,
     "name": "ordinal",
     "type": "整数",
     "nullable": "否",
     "json": {
      "kind": "integer",
      "null": false
     },
     "key": "PK",
     "origin": "GRS",
     "exchange": "",
     "meaning": {
      "ja": "親の中での出現順"
     }
```
新
```text
     "seat": 72,
     "name": "ordinal",
     "type": "整数",
     "nullable": "否",
     "json": {
      "kind": "integer",
      "null": false
     },
     "key": "PK",
     "origin": "GRS",
     "exchange": "",
     "meaning": {
      "ja": "親の中での出現順。`GRS` が足す曜日の行（表 T-108 の `CM-39` ほか）は、同じ親の中の最大の次を取る"
     }
```

⚠️ E-57 / E-58 は既にある行の番号を 1 つも動かさない —— `FR-054` の「`ordinal` が最小の暦」の解決が変わらない。

### 4.10 状態機械の原稿・検査の付属ファイル・生成器

<!-- EDIT id=E-60 file=docs/spec/_source/state-machines.json -->
旧（`:487-493`、`displayScaleStepped.source.rows`。H1）
```text
       "IC-104",
       "IC-105",
       "SK-22",
       "SK-23",
       "SK-17",
       "SE-1"
```
新
```text
       "IC-104",
       "IC-105",
       "SK-22",
       "SK-23",
       "SE-1"
```

⭐ 前に立つ者の指示（2026-09-22）: H1 は本書の当てる commit に入れる。この 1 行だけを消し、`npm run gen` で `tbl-state-machines.md:55` と生成される型を刷り直す。原稿のほかの所には触れない。

<!-- EDIT id=E-61 file=.claude/skills/spec-graph-check/retired.py -->
旧（`RETIRED` の中の `'IC-46', 'IC-49',` の行）
```text
           'IC-46', 'IC-49',
```
新
```text
           'IC-46', 'IC-49',
           # SK-17 (Ctrl + Shift + 0, reset the display scale and the chart
           # zoom) left table T-036 with CR-541 on the user's ruling JDG-301
           # of 2026-09-22 ("discard the function, remove it from the help
           # too"). CR-410, CR-411 and the ledgers name it, so the seat
           # stays burnt.
           'SK-17',
```

<!-- EDIT id=E-62 file=(deferred:.claude/skills/spec-graph-check/dictionary-table-pairing.txt) -->
旧（`:56`）
```text
T-036 SK-17 4ddb1ba2b876a116
```
新
```text
```

⛔ **E-62 は当てていない** —— 前に立つ者の指示（検査の基準線を変えない）により、組の指紋のファイルは触らず、動く行を報告する。⚠️ E-62 のほかに、行の字が動くので指紋を読み直す組が 2 つある: `T-036 SK-19`（E-71）と `T-109 IC-52`（E-48）。⛔ 手で指紋を貼らない —— 当てる体が行と語を読み合わせ、`check-dictionary-table-covariance.py` の見出しの手順どおりにその 2 行だけを刷り直す。

<!-- EDIT id=E-63 file=(tools。形で示す) -->
旧
```text
(tools/generate_display_words.py and tools/generate_entity_types.py as on 50f925db)
```
新
```text
tools/generate_display_words.py
  1. A held key tuple next to SCALE_ECHO_ENDS:  DUAL_CURSOR_SPAN_ROWS = ('DC-3',)
     roster entry  'dualCursorSpan': list(DUAL_CURSOR_SPAN_ROWS)
     key/fields    'dualCursorSpan': ('rowId', ('text',))
     and add 'dualCursorSpan' to the two tuples that list the held sections
     (the ones that already name 'scaleEcho', :519-521 and :561).
  2. dependencyKinds, read from table T-018 of docs/spec/01-04-requirements.md
     (NOT from the dictionary source): one entry per DP- row in table order,
       { "rowId": "DP-1", "linkType": 1, "abbreviation": "FS" }
     linkType = the integer in column `linkType`; abbreviation = the text of
     column `名` before the first "（". Written into the generated
     src/adapter/screen-renderer/display-words.json only.
tools/generate_entity_types.py
  3. 'SCHEDULE_COLOURS' (:1573) gains 'S-312' (after 'S-160').
  4. 'NOT_STORED_ROW_CONTROL_SIZES' (:1167) gains 'S-313' after 'S-140'.
No new generator: check 27's count of generated artifacts does not move.
```

### 4.11 Q33・`PND-232`・Q34・Q3・S3・S4・`PND-1`・`PND-150`・`PND-176`

<!-- EDIT id=E-64 file=docs/spec/01-04-requirements.md -->
旧（`:836-838`、`UC-008` の拡張）
```text
- 2a. 指している行が畳まれた、または非表示になった。
  - `GRS` はコメントボックスも一緒に隠す。
- 4a. 囲んだ範囲の一部の行が非表示になった。
```
新
```text
- 2a. 指している行が畳まれた、または非表示になった。
  - `GRS` はコメントボックスも一緒に隠す。
- 2b. 指す日付が無い（留める日が `null`）。
  - `GRS` はコメントボックスを決まった 1 か所に描き、掴めるようにする（位置は `FR-019`）。
- 4a. 囲んだ範囲の一部の行が非表示になった。
```

<!-- EDIT id=E-65 file=docs/spec/01-04-requirements.md -->
旧（`:4712`、`FR-019`）
```text
下辺を揃えれば、箱の高さが変わっても留めた点との関係が動かない。  
```
新
```text
下辺を揃えれば、箱の高さが変わっても留めた点との関係が動かない。
⭐ ずれが `null` の箱（まだ一度も引かれていない箱）は、ずれを `{ dx: 0, dy: 0 }` として置くこと（MUST） —— 本文の箱の左下隅が留めた点に一致する。
⭐ 留める日（`_assets/fig-erd-detail.md` の `AT-113`）が `null` の箱も描き、掴めるようにすること（MUST） —— 描かなければ、その箱は選べず、消すこともできない。
⭐ そのときは、留める行の、文書の開始日（同書の `AT-12`、`Project.startDate`）の位置を留めた点とすること（MUST）。  
```

⭐ **E-65 の 3 行目は、利用者の答えで埋めた**: 「F4 指す先の日付が空のコメント箱 → A その行の、文書の開始日の位置」（2026-09-22）。⚠️ 文書の開始日（`AT-12`）も `null` を取りうる。その縁は異常系として `DFC-586` へ回す（`JDG-78`）ので、本書は書かない。
⚠️ 「留める行」（`AT-114`）が `null` の箱は書かない —— 前に立つ者の判断 11（`DFC-586` へ回す）。

<!-- EDIT id=E-66 file=docs/spec/01-04-requirements.md -->
旧（`:7155`、`RC-13` の中）
```text
`IC-71` 〜 `IC-73` の 3 個、`IC-102`（`FR-036` の凡例に置くホイールの印）、および `IC-103`
```
新
```text
`IC-71` 〜 `IC-73` の 3 個、および `IC-103`
```

⚠️ E-66 は Q34 の A（「このままでよい」）を、利用者が dist を見て答えたのではなく「推奨通り」で受けたものである。前に立つ者は、図形を見たものとして扱ったことを利用者に告げる（`landing-plan.md` の flag 9）。

<!-- EDIT id=E-67 file=(deferred:docs/spec/01-04-requirements.md) -->
旧（`:1925`、表 T-017 の結び）
```text
**原色を使ってはならない（MUST NOT）。**  
```
新
```text
**原色を使ってはならない（MUST NOT）。**  
⭐ パレット色を文書に保存するときは、`#rrggbb`（16 進 6 桁）で綴り、透明だけを `"transparent"` と綴ること（MUST） —— 保存した文字と描く色が一致するので、描画にもモノクロにも他の道具にも読み替えが要らない。  
⛔ 色の名（`"red"` など）で保存してはならない（MUST NOT）。  
⚠️ 10 色それぞれの値は、まだ本表に無い —— 決めたときに本表の値の欄へ書く。  
```

⛔ **E-67 は当てていない** —— 前に立つ者の判断 14 により `CR-548` まで待つ（見本文書が色の名を持ったまま MUST NOT に触れる期間を作らない）。文は `CR-548` がこのまま使える。

<!-- EDIT id=E-68 file=docs/spec/01-04-requirements.md -->
旧（`:4839`）
```text
| DC-3 | 測る | 2 本の間の日数を示す |
```
新
```text
| DC-3 | 測る | 2 本の間の日数を示すこと（MUST）。<br>⭐ 日数は暦日で数え、2 本の日付の差とすること（MUST） —— `FR-047` の稼働日の数え方とは別である。<br>⭐ 置く所は、タイムルーラーの帯の中の、2 本の線の中点とすること（MUST）。<br>語は `FR-038` の辞書が持つ（例: `14 日`） |
```

⚠️ E-68 の札の重ね順は 表 T-020 の行が要るかもしれない。⛔ 表 T-020 の下の注は別のセッションが触っているので本書は表 T-020 に触れない —— 後の 1 枚に回す（11 節の問い 12）。

<!-- EDIT id=E-69 file=docs/spec/01-04-requirements.md -->
旧（`:1662`、`HF-4` の末尾）
```text
⭐ **縦に隣り合うので、この禁止は縦に読む。<br>**|
```
新
```text
⭐ **縦に隣り合うので、この禁止は縦に読む。<br>**⭐ 並びのいちばん右の操作子の外形と、行見出しパネルの右端とのあいだを、`_assets/tbl-settings.md` の 表 T-206 の `S-313` とすること（MUST）。<br>|
```

<!-- EDIT id=E-70 file=docs/spec/01-04-requirements.md -->
旧（`FR-019` の RATIONALE。`:4743`）
```text
**RATIONALE**: ハイライトボックスの線色を指定でき、指定が無ければ注記用の固定色で描くこと（MUST）。  
```
新
```text
**RATIONALE**: ハイライトボックスの線色を指定でき、指定が無ければ注記用の固定色で描くこと（MUST）。  
⭐ その固定色は `_assets/tbl-settings.md` の 表 T-236 の `S-312` である。  
```

<!-- EDIT id=E-71 file=docs/spec/01-04-requirements.md -->
旧（`:4161`、`SK-19` の中）
```text
ほかに何も出ていないときは、**その場の編集を確定する**（名称・担当者名・行名・文書名・プロパティの入力）。<br>
```
新
```text
ほかに何も出ていないときは、**その場の編集を確定する**（名称・担当者名・行名・文書名・プロパティの入力）。<br>⭐ 焦点が対話欄（`FR-066`）にあるときは、打った発話を確定して送ること（MUST）（`AG-11`） —— ⚠️ 対話欄をその場の編集に数えるのではない（表 T-028 の `IN-5a`）。<br>
```

<!-- EDIT id=E-72 file=docs/spec/01-04-requirements.md -->
旧（`:3420`、`FR-016` のパンの段）
```text
**端数は同表の `S-176` と `S-177` が持つ（MUST）。**  
```
新
```text
**端数は同表の `S-176` と `S-177` が持つ（MUST）。**  
⭐ 修飾なしのホイールの縦スクロール（表 T-023 の `MK-1`）も、入力装置が出した距離だけ日程表を動かすこと（MUST） —— 1 行に満たない端数は `S-176` に持つ。  
⛔ 距離を行の数へ丸めてはならない（MUST NOT） —— 行の帯が 1 ノッチの距離より高いと、丸めた縦スクロールは 1 度も動かない。  
```

### 4.12 利用者の答え（2026-09-22）—— S5・S6（Q33 の位置は E-65）

利用者の逐語（前に立つ者が写したまま）:
- S5: 「F2 ヘルプを下へ送ったとき、題の行 → A 残す（固定）」
- S6: 「F3 表示倍率を上げたまま縦に縮める → A 帯を名前の箱より低くしない。 理由: 行名が見えないと何の行か分からなくなる。」
- Q33 の位置: 「F4 指す先の日付が空のコメント箱 → A その行の、文書の開始日の位置」

<!-- EDIT id=E-74 file=docs/spec/01-04-requirements.md -->
旧（`:6594`、ヘルプの題の行）
```text
⭐ ヘルプの題の行は、左から、題 … 凡例（`IC-102`）・表示言語の切替（`FR-038` の `IC-21`）・閉じる入口（`IC-52`）の順に並べ、閉じる入口を右端に置くこと（MUST）。
```
新
```text
⭐ ヘルプの題の行は、左から、題 … 凡例（`IC-102`）・表示言語の切替（`FR-038` の `IC-21`）・閉じる入口（`IC-52`）の順に並べ、閉じる入口を右端に置くこと（MUST）。
⭐ 本文を下へ送っても、題の行をヘルプの上端に留めて描くこと（MUST） —— 閉じる入口と表示言語の切替を、送った先からも押せる。
```

<!-- EDIT id=E-75 file=docs/spec/05-07-design.md -->
旧（`:1243`、表 T-221 の `LF-3` の中）
```text
帯高は矩形が縦に取る高さを下回らず、かつ、その行の操作子（表 T-051 の `HF-1` の格子）が縦に取る高さも下回らない—— ⛔ **下回ると、格子の下段が次の行の帯へ落ち、押しがその行の操作子に取られる。<br>**
```
新
```text
帯高は矩形が縦に取る高さを下回らず、かつ、その行の操作子（表 T-051 の `HF-1` の格子）が縦に取る高さも下回らない—— ⛔ **下回ると、格子の下段が次の行の帯へ落ち、押しがその行の操作子に取られる。<br>**⭐ 帯高は、その行の名前（行見出しパネルに刷る行名、`FR-085`）の字の箱が縦に取る高さも下回らない —— 行名が見えないと何の行か分からなくなる。<br>⚠️ 表示の倍率（`FR-039`）を上げたまま縦に縮めたときに効く床である。<br>
```

⚠️ `impact.py LF-3` の届く先: `FR-094` ・ `FR-004` ・ `FR-016`（2 か所）・ `FR-109` ・ `FR-087` ・ 5.3 ・ 6.1（2 か所）。どれも「帯高は矩形と操作子の格子を下回らない」を引く所であり、床が 1 つ増えても偽にならない。
⚠️ E-74 の理由の文は本書が書いた（利用者は理由を述べていない）。

---

## 5. 継ぎ目（S-1 〜 S-9）—— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | CR-541 ↔ CR-542 | 表 T-260 の `SE-1` と `SE-2` の新しい行は E-03 の「新」の 2 行そのものである。`cr-411` が引く文は、`SE-1` の「`IC-104` / `IC-105` と 表 T-036 の `SK-22` / `SK-23` が押されるたびに出すこと（MUST）」と、`SE-2` の「押したあとの倍率がいちばん大きい段のときは最大であることを示す語を、いちばん小さい段のときは最小であることを示す語を、数の後に添えること（MUST）」である |
| S-2 | CR-541 ↔ CR-542 | `HF-2` の「何かを行う（濃く描く）」条件: **「押した行かその配下に畳みか隠しがある、配下に開いたままの印がある、または押した行に印が無く直下の子が `FR-018` の倍率で描かれていない」**。どれでもなければ薄く描き、押しは何もしない。見出しの `HF-10` は **「どれかの行に畳み・隠し・開いたままの印がある」** とき濃い（前に立つ者の判断 2） |
| S-3 | CR-543 ↔ H4 | 命令 `CM-8` の形: `{ kind: 'pasteTaskSubtree', sourceUids: readonly number[] }`（いまの `sourceUid: number` を置き換える）。並びは選んだ順。1 つのときも配列で運ぶ。祖先と子孫が両方入っているときに 1 度だけ写すのは use case の側（E-21 の 2 行目） |
| S-4 | CR-542 ↔ CR-543 | 行を消す命令 `CM-27` の形: `{ kind: 'deleteTaskGroup', groupId: string, newGroupId: string }`。`newGroupId` は訳者が `context.newGroupId` から入れる。use case は、消した結果で行が 0 になるときに限り、その id で `L1` の行を作る（E-19）。`EMPTY_DOCUMENT_TASK_GROUP_ID`（`document-change-plan.ts:159`）は使わない |
| S-5 | CR-541 ↔ CR-543 / CR-544 | `S-88`（キー `groupLevelOfDetailRatio`）の既定 = `1.5`（生成される `document-settings.ts` と `startup-template.json` に載る） |
| S-6 | CR-541 ↔ CR-544 | 生成される `src/adapter/screen-renderer/display-words.json` の節 `dualCursorSpan`、項 `{ "rowId": "DC-3", "text": { "ja": "{n} 日", "en": "{n} days" } }`。`{n}` を暦日の整数（2 本の日付の差）で置き換えて刷る |
| S-7 | CR-541 ↔ CR-544 | 表 T-236 の行 `S-312`（注記の色、明暗とも `#b45309`、色相に追随しない）。生成器が `svg-renderer.ts` の `SCHEDULE_COLOURS` に `'S-312'` を載せる。描く側は `themed('S-312')` で読み、`schedule-overlays.ts:60` の `ANNOTATION_COLOUR` を消す。⚠️ `schedule-task-figures.ts:103` のフェードの掴み点の 2 色は `S-312` ではない（11 節の問い 5） |
| S-8 | CR-541 ↔ CR-545 | 生成される `src/adapter/screen-renderer/display-words.json` の節 `dependencyKinds`、表 T-018 の行の順に `{ "rowId": "DP-1", "linkType": 1, "abbreviation": "FS" }` ・ `{ "rowId": "DP-2", "linkType": 2, "abbreviation": "SF" }` ・ `{ "rowId": "DP-3", "linkType": 0, "abbreviation": "FF" }` ・ `{ "rowId": "DP-4", "linkType": 3, "abbreviation": "SS" }`。パネルは保存した `linkType` で引いて `abbreviation` を出す |
| S-9 | CR-541 ↔ CR-545 | 表 T-206 の行 `S-313`（既定 `4`、単位 px、`S-235` も表示の倍率も掛けない）。生成器が `NOT_STORED_ROW_CONTROL_SIZES`（生成先は `src/adapter/screen-renderer/row-title-panel.ts:356`）に `'S-313': 4` を載せる（当てて確かめた）。描く側は `ROW_CONTROL_EDGE_PX`（`dom-screen-surface.ts:918`）をこれに置き換える |

---

## 6. グラフ（`50f925db` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（指している要求と節）

| 対象 | 指している箇所 |
|---|---|
| `SK-17` | `FR-039` ×2、`tbl-state-machines.md:55`（画面の値の出来事） |
| `FR-039` | 要求 19 件・参照 55（`FR-094` ほか）。⭐ 本書が変えるのは `SK-17` の段 1 行だけで、ほかの要求が引く段ではない |
| `SE-1` / `SE-2` / `SE-5` | `SE-1`: 画面の値の出来事 ／ `SE-2`: `FR-016` ／ `SE-5`: `FR-016`、表 T-206 の節 |
| `KO-2` / `KO-3` | `KO-2`: 0 ／ `KO-3`: `FR-004` |
| `HF-2` / `HF-10` | `HF-2`: `FR-004` ・ `FR-018` ・ `FR-076`（`RS-28`）・アイコンの名簿 ／ `HF-10`: `FR-004` ・ `FR-018` ・ `FR-076`（`RS-31`）・`FR-036` ・表 T-206 の節・画面の値の出来事（`levelZeroOpened`） |
| `RS-61` | `FR-111` |
| `FR-029` | 要求 25 件・参照 63。⭐ 本書が足すのは例外の指し先 1 文で、ほかの段は動かない |
| `FR-081`（表 T-023c） | `FR-083` ・ `FR-049` ・選択の出来事 |
| `FR-034` | `FR-081` ・ `FR-076` ・アイコンの名簿 |
| `MG-12` / `OP-6` | `MG-12`: `FR-056` ・ `FR-087` ／ `OP-6`: `FR-025` ・ `FR-087` ・ `FR-076` ・ 6.1 |
| `AT-67` / `AT-72` / `TC-3` / `CM-8` / `DC-3` / `CL-1` / `CA-2` / `CA-4` / `KO-2` | どれも 0 |
| `FR-033` | `FR-085` ・ `FR-005` ・ `FR-081` ・ `FR-057` ・ 2.3 ・ 5.2 ・ 列の節 ・ 選択の出来事 |
| `FR-032`（表 T-050） | 要求 8 件（`FR-085` ・ `FR-004` ・ `FR-111` ・ `FR-031` ・ `FR-081` ・ `FR-016` ・ `FR-076`）・ファイル操作と問いの出来事 |
| `IN-3` / `IN-4` / `IN-5a` | `IN-3`: `FR-002` ・ `FR-092` ・ `FR-029` ・ `FR-040` ／ `IN-4`: 要求 9 件（`UC-004` ・ `FR-016` ・ `FR-053` ・ `FR-070` ・ `FR-071` ・ `FR-020` ・ `FR-076` ・ `FR-039` ・ `FR-040`）と状態機械の出来事の節 6 つ ／ `IN-5a`: `FR-016` ・ `FR-040` ・名前付けと入力欄の出来事 |
| `S-99g` / `S-99h` / `IC-52` / `SK-19` | `S-99g`: `FR-053` ・ `FR-070` ・ `FR-020` ／ `S-99h`: `FR-051` ・ `FR-052` ・ `FR-102` ／ `IC-52`: `FR-004` ・ `FR-006` ・ `FR-070` ・ `FR-068` ・ `FR-036` ／ `SK-19`: `FR-085` ・ `FR-070` ・ `FR-076` ・ `FR-039` ・ `FR-040` |
| `FT-1` / `FT-4` | 節だけ（5.3、5.5） |
| `AG-6` / `AG-11` / `LM-16` | `AG-6`: `FR-063` ・ `FR-064` ・ `FR-076` ・ 1.7 ・ 5.2 ・ 5.5 ／ `AG-11`: `FR-064` ・ `FR-040` ・ 5.2 ・ 5.5 ／ `LM-16`: `FR-063` |
| `IR-3` / `CA-1` | `IR-3`: 0 ／ `CA-1`: 5.6 |
| 表 T-062 | 要求 0（節から 4） |
| `FR-086` / `FR-072` | `FR-086`: `FR-076` ・表 T-206 の節 ／ `FR-072`: `FR-091` ・ `FR-009` ・ `FR-016` ・ `FR-052` ・ `FR-029` ・ `FR-040` |
| 表 T-237 / 表 T-018 | 表 T-237: 要求 5・2 次 36 ／ 表 T-018: 要求 1・2 次 6（`UC-004` ・ `FR-016` ・ `FR-104` ・ `FR-110` ・ `FR-049` ・ `FR-076`） |
| `RC-13` / `S-88` / `HF-4` | `RC-13`: `FR-004` ・ `FR-032` ・ `FR-029` ／ `S-88`: `FR-018` ／ `HF-4`: `FR-085` ・ `FR-004` ・ `FR-098` ・ `FR-036` ・ `FR-039` |
| 表 T-206 / 表 T-236 / 表 T-027 | 行を足すだけ。行を名指す要求は動かない（表 T-206 は要求 36、表 T-236 は 7、表 T-027 は 7） |
| `FR-019` / `UC-008` / `FR-016` | `FR-019`: `FR-001` ・ `FR-041` ・ `FR-033` ・ `FR-016` ・ `FR-076` ／ `UC-008`: `FR-009` ・ `FR-019` ・ `FR-049` ・ `FR-097` ／ `FR-016`: `FR-075` ・ `FR-104` ・ `FR-039` |

⭐ 導いた条項（`HF-2` の条件、決定 1 の段、決定 7）について、届いた行ごとに `rulings.md` を引いた: `HF-2` / `HF-18` / `KO-2` / `HF-10` に当たる裁定は `JDG-145`（「開けるようにせよ」—— 本書の向きと同じ）と `JDG-160`（g を `JDG-302` が覆す。記入は `CR-549` の E1）だけである。`IN-4` に当たる裁定は `JDG-289` / `JDG-290`（選択と欄の段）で、パネルとドラッグの前後を決めていない。

### 6.2 `induced.py`（25 の種）

種: `HF-2 HF-10 HF-18 KO-2 KO-3 SE-1 SE-2 SE-5 FR-029 FR-039 SK-17 IN-4 S-99g S-99h IC-52 SK-19 FR-033 T-050 AG-6 AG-11 LM-16 FR-004 FR-018 FR-072 FR-019` —— 解決 25/25、種の中の辺 35、閉路 3:

| 閉路 | 本書の扱い |
|---|---|
| `FR-004` `FR-018` `HF-18` `HF-2` | `HF-2`（E-06）と `HF-18`（E-08）を 1 つの計画で書いた。`FR-004` と `FR-018` は触らない |
| `FR-039` `SK-17` | E-01 と E-02 を同じ commit で当てる |
| `AG-11` `AG-6` | E-25 と E-36 を同じ commit で当てる |

---

## 7. 数の予測（`50f925db` で測った。当てた後に同じ数え方で突き合わせる）

| 数 | 前 | 後 | 内訳 |
|---|--:|--:|---|
| tables | 181 | 181 | 新しい表 0 |
| figures | 25 | 25 | — |
| rows | 2231 | 2234 | −1（`SK-17`）＋ 4（`UN-18` ・ `EN-6` ・ `S-312` ・ `S-313`） |
| uids | 162 | 162 | 新しい要求 0 |
| 接頭辞 | 160 | 160 | — |
| 表 T-036 の行 | 27 | 26 | E-01 |
| 表 T-237 の行 | 5 | 6 | E-33 |
| 表 T-027 の行 | 18 | 19 | E-26 |

---

## 8. 当てる順と commit の割り方（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ 当てる前に、2 節の 4 つの識別子が空いていることを同じ測り方で測り直し、日付を書く。

```
pass (a) keys / entrances / display   E-01..E-04, E-14, E-15, E-22, E-23, E-27..E-29,
                                      E-32, E-33, E-34, E-48, E-51, E-52, E-53, E-60,
                                      E-61, E-62, E-66, E-68, E-69, E-71, E-72
pass (b) data / merge / agent         E-06..E-13, E-16..E-21, E-24..E-26, E-30, E-31,
                                      E-35..E-38, E-43, E-47, E-54, E-57, E-58, E-64,
                                      E-65 (lines 1-2 only)
pass (c) values / tables / 05-07      E-39..E-42, E-44..E-46, E-49, E-55, E-56, E-59,
                                      E-63, E-70, E-73, E-74, E-75 (E-62 and E-67 not applied)
then:    npm run gen          (writes every generated file; H1 = E-60 is in the same commit)
         strictdoc export docs/spec into a TEMP dir (never into docs/) -- render check
         check-dictionary-table-covariance.py: re-read and re-pair T-036 SK-19 and
         T-109 IC-52 only (E-62 has removed SK-17's line)
         npm run gen:check ; bash .claude/skills/spec-graph-check/check.sh (diff the
         whole output against the before-run; 11/12/33 go red without "FAIL") ;
         npx vitest run
```

- ⚠️ **1 つの worktree の中では赤が出るのが正しい** —— コードの CR（542〜546）が読む生成物の定数を本書が足し、本書が消す `SK-17` をコードと試験がまだ読む。**門は wave 1 を合わせた木で打つ**（`landing-plan.md` 第 1 節）。
- ⚠️ 表 T-283: 当てた時点の木に無い。状態機械のセッションが、パネルの段を 1 つ加える形で直す（4.4 節の注）。
- ⚠️ 変更履歴: `docs/spec/A-appendix.md` の A.3 は「変更履歴は `docs/development-records/changelog.md` が持つ」とだけ書くので、本書は A-appendix に行を足さない。変更履歴の行は `CR-549` が書く（計画の「CR-541 が A-appendix に 1 行」は、いまの木では当たらない）。
- ⚠️ 基準線: 検査 39（MUST を逐語で持つ試験）は、本書が足す MUST の数だけ動きうる。wave 1b の仕様だけを読む試験者が逐語の試験を足して止める。止まらなければ利用者に問う（本書に許しは無い）。検査 42（出所の無い引用）は `SK-17` の引用が消えて下がる。検査 23 は列を足さないので動かない。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

| 所 | 何が偽になるか | 直す CR |
|---|---|---|
| `shortcut-keys.ts:108-115`、`display-scale-steps.ts:47` ・ `:60`、`input-command-translator.ts:277`、試験 `uf-30-31` ・ `uf-48-input` ・ `uf-50` ・ `cr-405` ・ `cr-410` ・ `cr-411` ・ `cr-438-key-shortcuts` ・ `dfc-271` ・ `nfr-004` | `SK-17` を読む／引く | CR-542 |
| `row-tree-entrances.ts`（S-2）、`task-group-folding.ts`、試験 `cr-404`（`:89` の ID の並びは変わらない。`:539` 〜 の主張が変わる）・`cr-438-pointer-panels`（`:240`） | `KO-2` / `KO-3` / `HF-2` の新しい意味 | CR-542 ・ CR-543 |
| `screen-state.ts:133` の `DEVIATION` | `IN-4` がコードの並びになる | CR-542 |
| `task-paste.ts`、`edit-task.ts:82`（S-3）、`document-change-plan.ts:159`（S-4） | 命令の形 | CR-543 |
| `schedule-overlays.ts:60`（S-7）、日数の札（S-6）、試験の `S-88` の期待値（S-5） | 行と語が増え、値が変わる | CR-544 |
| `properties-panel.ts:500`（S-8）、`dom-screen-surface.ts:918`（S-9）、`command-palette.ts:116`（`EN-6`） | 生成物と行を読む | CR-545 |
| `post-dialogue-message.ts:39`（E-43） | 配りの最中の発話を拒む | CR-546 |
| `selection-values.ts`（Q7）、`frame-loop.ts`（Q17 の契機、Q16 の複数の写し、`PND-181` の STOP） | 状態機械のセッションの持ち場 | H2 ・ H3 ・ H4 ・ H7 |
| 起動時の見本文書・変換の案内・skeleton の色の名（E-67 の MUST NOT に触れる） | Q3 の綴りの規則だけを先に当てるので、`CR-548` までのあいだ食い違う | `CR-549` が `defects.md` に記す → `CR-548` |

---

## 10. ⛔ この変更でやらないこと

- **S1**（10 色の値）—— E-67 は綴りの規則だけである。値は `CR-548`。
- **Q3 の綴りの規則（E-67）** —— `CR-548` まで待つ（前に立つ者の判断 14）。
- ⭐ S5・S6・Q33 の位置は、利用者の答えで本書に入れた（4.12 節、E-65）。
- **`FR-033` の `TaskOrigin` の食い違い**（`:2376` の「複製元を `TaskOrigin` に残す（MUST）」と `:2388` の「複製に `TaskOrigin` を付けてはならない（MUST NOT）」）—— `CR-433` の再開へ回す。E-21 はその 2 行のあいだの段に入れるが、どちらの行にも触れない。
- **表 T-020 の下の注と `EX-12`** —— 別のセッションが直した所である。E-68 の札の重ね順もここでは足さない。
- **`rulings.md` ・ `pending-decisions.md` ・ `defects.md` ・ `changelog.md` ・ `@provisional` の印** —— `CR-549`。
- **`RS-61` の指紋** —— 前に立つ者。
- **`PI-15`**（決定 10）、**`MK-1`**（決定 9）、**`ZE-5`**（Q10 は `SE-2` だけ。11 節の問い 9）。

---

## 11. 前に立つ者へ返した問いと、前に立つ者の判断（2026-09-22）

| # | 問い（要約） | 前に立つ者の判断（2026-09-22）と理由 | 本書の扱い |
|---|---|---|---|
| 1 | 表 T-283 は `RG-3` / `RG-4` の入れ替えか、段の追加か | 承認された文（`DFC-570` の案 ②: 面 → ドラッグ → パネル）に従う。段が 1 つ加わり、入れ替えではない。理由: Q20 は案 ② を承認した | 4.4 節の注を「表 T-283 に段が 1 つ加わる（`RG` の行が 1 つ増える）」に直した |
| 2 | `HF-2` / `HF-10` は印だけのとき薄いか | 否。`[vv]` は「ニュートラルに戻す」（`JDG-302`）ので、配下の印も戻す状態である。配下に畳み・隠し・印があるか、押した行に印が無く直下の子が倍率で描かれていないとき濃い。`HF-10` も、どれかの行に畳み・隠し・印があれば濃い。理由: 「押しても何も変わらないときだけ薄い」（`FR-029`） | E-06・E-07・S-2 を直し、表 T-051 の結びに E-73 を足した |
| 3 | S3 の英語の語 | 未決。利用者へ回す | E-55 の en「{n} days」は候補のまま当て、穴として印した |
| 4 | `S-312` の暗いテーマ | 同じ値でよい | そのまま |
| 5 | `schedule-task-figures.ts:103` | 読んだとおり。色の行は立てず、印だけを移す | そのまま |
| 6 | Q7 に畳んだ行・隠した行のタスクを入れるか | 入れる。理由: `JDG-288` ①（`rulings.md:505`）の逐語「非表示にしたものは選択できない」は、倍率の場面に限らず描かれていないタスクのすべてを覆う | E-13 を直した |
| 7 | Q13 の報告 | 書かない | そのまま |
| 8 | Q16 で行と `Task` が両方選ばれたとき | いまの振る舞い（行を写す）のまま。何も書かない | そのまま |
| 9 | `FT-4` の数と `ZE-5` | (a) シェルが実際に計るものを数え、真の数を方法とともに書く (b) 行の軸は `ZE-5` のまま | (a) 4 と測り、E-41・E-42 に方法を書いた (b) そのまま |
| 10 | A-appendix の行 | よい | 足さない |
| 11 | 留める行が `null` の箱 | 異常系として `DFC-586` へ回す（`JDG-78`）。何も書かない | そのまま |
| 12 | `DC-3` の札の重ね順（表 T-020） | 後の 1 枚への宿題として記す。表 T-020 はいま誰も触っていないが、後の CR に回す | ⚠️ 宿題: 表 T-020 に `DC-3` の札の行を足すか |
| 13 | 隣り合う操作子どうしの間 | いまのまま（0）。何も書かない | そのまま |
| 14 | Q3 の綴りの規則を先に当てるか | E-67 は `CR-548` まで待つ。本書の当てる組から外す。理由: 分かっている違反の期間を作らない | E-67 を当てていない |

⚠️ 残る宿題: (i) S3 の英語の語（利用者）。(ii) 表 T-233 の `RS-31` の語「畳まれた行が 1 つも無い」は、E-07 のあと印も数えるので字が狭くなった —— 語は辞書の原稿の語であり利用者の語なので、本書は変えていない。(iii) 組の指紋 3 行（8 節）。

## 12. 測り方の再現

```
# totals (before)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   -> tables=181  figures=25  rows=2231  uids=162   (50f925db, 2026-09-22)
python -c "import json;print(len(json.load(open('docs/spec/_source/row-id-prefixes.json',encoding='utf-8'))['prefixes']))"
#   -> 160

# free identifiers: every local and remote branch tip (59 refs), docs/ change-request/ src/
#   git grep -ohE "\bS-31[2-9]\b|\bUN-1[89]\b|\bEN-[67]\b" <ref> -- docs change-request src
#   -> 0 hits on every ref (2026-09-22)
# largest in use on 50f925db: git grep -ohE "\bUN-[0-9]+\b" | sort -u  (UN-17), EN-5, S-311

# graph
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>     # each ID of 6.1
strictdoc export docs/spec --formats=json --output-dir scratch/spec-check/sd-out --no-parallelization
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py <the 25 seeds of 6.2>

# every old block of section 4 occurs exactly once in its file (the applying body re-runs this):
#   read each "<!-- EDIT id=... file=... -->", take the next two ```text blocks as old/new,
#   assert text.count(old) == 1 on the file (LF; these files hold no CRLF on 50f925db)
#   -> 69 of 69 file edits occur exactly once (E-63 is described, not a text edit)

# dry run (2026-09-22): all 69 applied to a COPY of docs/spec outside the tree, then
#   settings_json_to_md.py / erd_json_to_md.py / state_machines_json_to_md.py on the copy
#   md-checks.py on the copy                     -> tables=181 figures=25 rows=2234 uids=162
#   strictdoc export (html) of the copy + check-render.py  -> RESULT: PASS (0 literal **)
#   check-spec-holds-no-history.py on the copy   -> 0 sites
```

---

## 13. 当てた結果（2026-09-22、当てる commit `98968cd7` の木で測った）

- 当てた編集: 72 のうち 70（E-62 と E-67 は当てていない）。旧はどれも 1 回だけ現れた。生成器 2 つ（E-63）も当てた。
- `npm run gen` → `npm run gen:check` は差分 0。`strictdoc export` を木の外の一時の場所へ出し、`check-render.py` は PASS（`**` の生の残り 0）。
- 数: `tables=181 figures=25 rows=2234 uids=162`（7 節の予測どおり）。

### 13.1 `check.sh`（前: 仕分けの走り `check-2df504f2` の赤は 23 と 40）

| 検査 | 前 → 後 | 何が動いたか | 直す所 |
|---|---|---|---|
| 11 重複 | 緑 → 赤 | `docs/review/duplication-baseline.txt:3` の 6 行の組（`IC-58` `IC-77` `IC-90` `IC-91` `IC-92` `IC-93`）が、4 行（`IC-58` `IC-77` `IC-90` `IC-92`）と 2 行（`IC-91` `IC-93`）に割れて「NEW」と読まれる。どの行の字も本書は変えていない（似ている度合いの重みが、ほかの所の字で動いた） | 基準線（利用者の許しが要る） |
| 37 辞書と表の組 | 緑 → 赤 | `T-036 SK-19` 0fea97f9222ba0d6 → c136341b74186e3c、`T-109 IC-52` cdab431953c2c166 → ff5c0864227f3824、`T-036 SK-17` の行が残っている（E-62 を当てていないため） | 組の指紋のファイル（読み合わせてから） |
| 39 MUST を逐語で持つ試験 | 1360 → 1400 | 本書が足した MUST と、行の字が動いて逐語の結び付きが切れた MUST | 仕様だけを読む試験者の逐語の試験（wave 1b）、足りなければ基準線 |
| 42 出所の無い引用 | 358 → 366 | `src/` ・ `tests/` のコメントと試験が、変わった文を引いている | CR-542 〜 546 |
| 44 退いた ID を引く所 | 83 → 111 | `SK-17` を引くコードと試験（`shortcut-keys.ts:108`、`display-scale-steps.ts:47` ・ `:60`、`input-command-translator.ts:277` ほか） | CR-542 |
| 23 ・ 40 | 赤のまま | 前からの赤（利用者の手の編集を待つ） | — |

⚠️ 検査 46（文の終わりの改行）と 62（識別子の予約）は、途中で一度赤くなり、本書の文を直して緑に戻した。

### 13.2 `npx vitest run`（前: `783f276a` を別の worktree に出して同じ `node_modules` で走らせた）

前の赤 14（うち 10 は、木に無い MSPDI の XSD を読めずに 0 件で落ちるファイル。いまも同じ）、後の赤 44。**新しい赤は 31 件で、どれも試験とコードの側にある**（本書は `src/` の手書きのファイルと `tests/` に触れていない）:
- 仕様の逐語を引く試験: `cr-410`（3）・`cr-411`（3）・`cr-426`（`IR-3`）・`cr-380-381-384`（パンの段）・`dfc-307` ・ `dfc-322` ・ `in-4-escape-closes-the-panel` ・ `uf-48-input`（2）・`t-015-t-051-the-four-folding-controls`（2）・`fr-029-en-2` ・ `fr-029-in-effect`
- `S-88` = 1.5 の前提: `cr-404`（5）・`cr-423` ・ `fr-055-fit-reaches-deep-tiers`（2）・`fr-055-vertical-lod-fit` ・ `layout-engine`
- 辞書と生成物: `display-words.contract`（2。CR-541 の持ち分とされた試験だが、前に立つ者の指示で触れていない）
- そのほか: `dfc-271`（`ST-7` の前提）
- 消えた赤 1: `cr-430-table-t-021a-the-press-cycle`（本書と無関係。揺れと見られる）
