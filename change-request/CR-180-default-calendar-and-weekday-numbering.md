# CR-180 — 曜日の番号体系を仕様に書き、既定の暦を原稿から届ける

> **閉じるもの**: 繰り越し (k) の最後（表 T-209）／ 裁定 K の残り半分。
> **利用者の裁定**（2026-08-17）: **「生成系の SSOT は `docs/spec/_source`、生成先は `_assets` と `src`。型を持つユニットの中に置け」**
> ⛔ **本 CR の本体は値ではなく、値の読み方である** —— 曜日がどの数かを仕様書がどこにも書いていなかった。

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-3` と `GL-004` を進める。** 表 T-054 の `CH-1` `CH-2` `CH-3` `CH-4` `CH-5` と `GL-001` 〜 `GL-007` を当てた:

| 本 CR の部分 | 答え |
|---|---|
| 曜日の番号体系を `AT-73` / `AT-17` に書いたこと | ⭐ **`CH-3` と `GL-004`** —— 外部 WBS マスタとの往復そのものである。⛔ **番号を 1 つ取り違えると、稼働日が 1 日ずれた日程を書き戻す。例外にも警告にもならない** |
| 既定の暦が原稿から届くこと | ⭐ **`CH-3`** —— 取り込んでいない文書も相手のツールで開ける形で書き出す（表 T-033 の `EX-1`）ための暦である |
| 曜日名の名簿 | ⛔ **`CH-1` 〜 `CH-5` のどれも進めない。** 刊行の基盤である |

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R6`**（試験品質）| 契約 ＋ 生成器 ＋ 新設した試験 2 件 | ⭐ **わざと 7 通り壊して 7 件とも落ちた**（6 件は契約、1 件は生成器）。⭐ **さらに「原稿を変えると刊行物・生成定数・コード・試験が連動する」ことを端から端まで実測した** —— `S-106` を月〜木にすると、表が「月・火・水・木 🔎」になり、生成定数が変わり、`workingCalendarOf` の答えが変わり、試験が落ちた |
| **`R4`**（重複）| 曜日の表示語 | ⭐ **原稿に名簿を 1 つ置き、セルは数だけを持つ。** 「月・火・水・木・金」は生成器が刷る。⛔ **数と語を並べて持たない** |
| **`R3`**（エラー処理）| `settings_json_to_md.py` の `say` | ⛔ **本物の欠陥。** 問題を報告する行が cp932 へ生書きしており、**原稿に 🔎 が入っていると報告そのものが `UnicodeEncodeError` で落ちた** —— 不正な原稿が、行を名指す代わりにスタックトレースで run を殺していた。`erd_json_to_md.py` と同じ守りに揃えた |
| **`R2`**（命名）| `DEFAULT_CALENDAR_VALUES` | ⚠️ **`DEFAULT_CALENDAR` と衝突した** —— 手書きの `Calendar` が既に居た。**値の側を `_VALUES` にして、暦の側はそれを組み立てる形にした** |
| **`R7`**（純粋性）| `DEFAULT_WEEK_DAYS` | ✅ 変わらず純粋。読む先が定数になっただけである |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | 曜日名の名簿（`weekdays`）を原稿に新設し、表示を生成器が刷る形にした | ⭐ **裁定 ㊹ の「刊行される散文は言語ごとの辞書」と `R4`。** ⚠️ **セルに数と語を並べて持つ案もあった** —— そちらは食い違っても誰も気づかない |
| 2 | 名簿の索引を **0 = 日曜**（`weekStartDay` 側）に揃えた | 2 体系のどちらかを選ぶ必要があり、⭐ **0 起点のほうが配列の索引と一致する。** `dayType` は生成器が 1 ずらす |
| 3 | セルに `encoding` を必須にした | ⛔ **これが本 CR の要点。** 書かせないと「月曜」が 1 とも 2 とも読める |
| 4 | `S-128` の注記を「the number the row states」にした | ⚠️ **単位の欄が無い** —— 「1 日あたりの分数」は名前の欄にある。**`suffix` を足すと刊行物が変わるので採らなかった** |

## 1. ⛔ 無かったのは値ではなく、値の読み方だった

| | 測った事実 |
|---|---|
| 1 | ⛔ **`docs/spec` は曜日の数がどの曜日かを 1 度も書いていない** —— `AT-73` の意味欄は「曜日」の一語、`AT-17` は「週の始まりの曜日」だけ |
| 2 | ⛔ **交換相手は曜日を 3 通りに数える。** `WeekDay/DayType` は **1 = 日曜**（0 は例外日）、`Project/WeekStartDay` は **0 = 日曜**、`Exception/DaysOfWeek` はビット（1, 2, 4, 8, …）。⭐ **我々の列は前の 2 つを使う** —— `daysOfWeek` の列は持っていない |
| 3 | ⛔ **つまり「月曜」は `dayType` で 2、`weekStartDay` で 1 である。** ⚠️ **取り違えても例外にも警告にもならず、稼働日が 1 日ずれるだけである** |
| 4 | ⭐ **実装だけが知っていた** —— `schedule.ts` に `dayWorking: dayType >= 2 && dayType <= 6` と書かれ、その行のコメント「AT-73 codes the days 1..7 from Sunday」が**この対応が書かれていた唯一の場所だった** |
| 5 | ⭐ **正典は `docs/reference/mspdi/` に在った**（利用者の指摘）。`xml-schema-for-the-calendars-element.md` が `0=Exception, 1=Sunday, … 7=Saturday`、`mspdi_pj12.xsd` が `WeekStartDay` を `0=Sunday, … 6=Saturday` と書いている |

⚠️ **私は直前に「XSD はリポジトリに無い」と報告した。誤りだった** —— 見ていたのは `previous-project-result/01-mspdi/`（README だけ）で、正典は `docs/reference/` に在る。**記憶を訂正した。**

## 2. やったこと

| 段 | 内容 | ⭐ 期待値と実測 |
|---|---|---|
| **1** | `AT-73` と `AT-17` の意味欄に番号体系を書き、**互いに 1 ずれることを両方から名指した** | ⭐ **動くのは 2 セルだけ**。実測: そのとおり |
| **2** | 原稿に曜日名の名簿（`weekdays`）を置いた | 0 = 日曜で索引、短形と長形、区切り、空のときの語 |
| **3** | `S-106` / `S-107` / `S-108` を「数 ＋ どの体系か ＋ どの形か」に | ⭐ **刊行物はバイト一致**。実測: 一致（先に消してから生成し、書かれたことを確かめた）|
| **4** | `settings.schema.json` に `weekdays` と `days` / `encoding` / `form` を足した | ⭐ **わざと 7 通り壊して 7 件とも落ちた** |
| **5** | `schedule.ts` に `DEFAULT_CALENDAR_VALUES` を生成し、手書きの `DEFAULT_WEEK_DAYS` をそこから組み立てた | ⛔ **`dayType >= 2 && dayType <= 6` という打ち直しが消えた** |
| **6** | 試験 2 件（2 体系が 1 ずれること／既定の暦が `S-106` どおり働くこと）| ⭐ **原稿を月〜木にすると落ちる**。実測: 落ちた |
| **7** | `settings_json_to_md.py` の `say` を壊れない形に | ⛔ **`R3` の実物** |

### ⭐ 検査 23 が本物を捕まえた

⚠️ **名簿の区切り「・」を、私は「言語に属さない」としてスキーマにそう書いた。** ⛔ **検査 23 が落とした** —— 「・」は日本語の約物であり、英語版なら読点になる。**辞書に改めた。**
⭐ **CR-176 で足した検査が、次の CR の中で私自身の誤りを捕まえた。** ⚠️ **原則のままなら気づかなかった。**

## 3. 数の予測と実測

| | 予測 | 実測 |
|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 124 / 11 / 1533 / 145（すべて不変）| **同じ** |
| 重複 | `A=25 (new 0)` `groups=58` `pairs=139` | **同じ** |
| 刊行物で動くセル | **2**（`AT-73` と `AT-17` の意味欄）| **2。ほかはバイト一致** |
| 試験 | 681 → **683** 件 | **683 件緑** |
| 機械検査 | 23 本すべて緑 | **ALL GREEN** |

## 4. ⛔ 残っていること

| | |
|---|---|
| `Project.weekStartDay` を**使う**側 | ⛔ **未実装。** 週の目盛が `S-108` を読む経路はまだ無い（`FR-054` / `FR-088`）。⭐ **値は届いているので、書く回に読むだけである** |
| `S-128`（1 日あたりの分数）を使う側 | ⛔ **未実装。** 稼働日を交換相手の期間へ換算する経路（`FR-054`）は W4 以降 |
| `Exception/DaysOfWeek` のビット体系 | ⚠️ **我々の列に無い**ので触っていない。⭐ **繰り返しの例外日を実日付へ展開する回に当たる**（`FR-088` が未対応と知らせると定めている）|
| `AT-82` `recurrenceKind` の 1〜9 | ⚠️ **同じ形の穴が残っている** —— 正典は `1=Daily … 9=No exception type` と書くが、仕様書は数の範囲しか書いていない。**独立した CR** |
