# CR-314 — 何も決めていなかった 1 文（表 T-023d の結び）

⭐ **`PD-394` の裁定（2026-08-30、案 A）を仕様書へ入れる。**⛔ **触るのは 1 文だけである。**

| | 裁定 | 行 |
|---|---|---|
| 1 | **「1 日単位に四捨五入する」を採り、「ポインタの**下の日**から求めること」を「ポインタの**位置**から求めること」へ改める** | `PD-394` |

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

`GL-006`（アフォーダンスとシグニファイア）。
⭐ **フェードの端を掴むのは「この辺」を指す操作である。**⛔ **境目の 1 画素手前で答えが 1 日ずれると、
同じ場所を指したつもりの 2 度が違う日に落ちる。**

### ② レビュー観点のどの条項を当て、何が出たか

`docs/development-rules/07-review-standards.md` の **`R1.2`**（曖昧・非検証な表現を排除し、
各要求の主語と動作が明確であること）。
⇒ ⛔⛔ **表 T-023d の結びは、同じ 1 文の中で 2 つの相容れない算術を述べていた。**
⇒ ⚠️ **どちらを採るかを読み手が選べる文であり、`R1.2` の「非検証な表現」そのものである。**

⚠️ **検査 22 の正規表現は `R[2-7]` しか見ておらず、`R1` の条項を名指しても通らない。**
⭐ **本節が規則の書類を絶対に名指しているのはそのためである** —— **条項は `R1.2` である。**

```
「ポインタの下の日から求めること（MUST）」   ⇒ 切り捨て
「いずれも 1 日単位に四捨五入する」          ⇒ 丸め
```

⭐ **前者を採ると、日と日のあいだの数はもともと整数なので、後者は丸めるものを何も持たない。**
⛔ **何も決めない節を残す書き方は、本書のどこにもしていない。**

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

⭐ **無い。**⛔ **本件は `PD-394` として問い、裁定を受けている。**

---

## 1. ⛔ 測った

### 変更前（出荷ビルド、2026-08-30）

境目のちょうど手前と、ちょうど後ろから、同じ端を掴んで離した。

```
境目の 0.02 日 手前で離す : fadeInDays = 4
境目の 0.02 日 後ろで離す : fadeInDays = 5
```

⛔ **1 つの境目に 2 つの答えがある。**⭐ **切り捨てているのであって、四捨五入していない。**

### 変更後（同じ 2 手）

```
境目の 0.02 日 手前で離す : fadeInDays = 5
境目の 0.02 日 後ろで離す : fadeInDays = 5
```

⭐ **`tests/unit/fr-052-t-023d-picture-while-held.test.ts` の 43 件が緑。**
⛔ **同ファイルの 1 件は、仕様だけを読む体がわざと赤く置いたものである。**⭐ **それが緑になった。**

### ⚠️ 日の内側を狙う既存の 42 件は、どちらの算術でも同じ答えを出す

```
掴み点は日の 0.25 日 目に置かれる（A_QUARTER_INTO_THE_DAY）
⇒ 切り捨てても四捨五入しても同じ日に落ちる
```

⭐ **だから境目そのものを watch する行が 1 件も無く、量子が半日どちらへでも動けた。**
⚠️ **台帳 `D-138`（行と行の隙間を跳ぶ縦のパン）と同じ族である** —— **量子は長い操作の内側に隠れ、端でしか出ない。**

---

## 2. ⭐ 波及（**測った**）

```
induced.py T-023d FR-011 FD-6 FR-031
  seeds in the specification : 4 of 4
  edges inside the seed set  : 1
  cycles in the induced graph: 0
```

⭐ **巡回が無いので 1 度の書き込みで足りる。**

| 触るもの | 何が変わるか |
|---|---|
| 表 T-023d の結び | ⭐ **1 語**。「下の日」→「位置」 |
| `FR-011` | ⛔ **変わらない**。フェードの絵の定義であって、日数の求め方ではない |
| 表 T-012a の `FD-6` | ⛔ **変わらない**。求めた日数を切り詰める規則で、求め方の上流に居る |
| `FR-031` | ⛔ **変わらない**。1 回のドラッグが 1 回の取り消しである、という粒度の規則 |

⚠️ **しきい値の禁止（MUST NOT）は残る** —— ⭐ **本件はしきい値を入れていない。**
**離した瞬間の位置を読むだけであり、動いた距離は問うていない。**

---

## 3. ⛔ 仕様書の差分（**1 文**）

`docs/spec/01-04-requirements.md`、表 T-023d の 3 段落下の結び:

```diff
-**`GR-1` / `GR-2` の日数は、ポインタの下の日から求めること（MUST）。**
+**`GR-1` / `GR-2` の日数は、ポインタの位置から求めること（MUST）。**
```

⭐ **残りの 3 文はそのままである** —— 「`GR-1` は `start` からの日数、`GR-2` は `end` までの日数とし、
**いずれも 1 日単位に四捨五入する**」「得た日数は 表 T-012a の `FD-6` で切り詰めること（MUST）」
「ドラッグの開始にしきい値を設けてはならない（MUST NOT）」。

⚠️ **行の数は動かない** —— **散文の 1 文の中の 1 語である。**

---

## 4. ⭐ 実装の差分

`src/adapter/input-command-translator/input-command-translator.ts`、`GR-1` / `GR-2` の枝:

```
変更前  pulled = serialOfDay(dayAtX(x)) - serialOfDay(start)          ⇒ 切り捨て
変更後  atPointer = serialOfDay(day) + unitFraction((x - xFromDay(day)) / pxPerDay)
        pulled    = Math.round(atPointer - serialOfDay(start))         ⇒ 四捨五入
```

⭐ **端数の読み方は、同じファイルが既に持っているものをそのまま使った** ——
`dayAnchorAt` が `S-177` の「日のどこまで入っているか」を出すのに使う式である。
⛔ **公開の 2 つの変換器（`dateAtX` / `xFromDay`）の上に立つので、`x → 日` と `日 → x` が離れない。**

⚠️ **`FR-052` の「掴んでいるあいだの絵」も同じ経路で直る** ——
`frame-loop.ts` の `previewOfHeldPress` は `commandFromInput` を呼び直しているので、
⭐ **絵と、離したときに書かれる値が、1 つの算術から出る。**

⛔ **部品の境を 1 つも越えていない。**⭐ **表 T-064 / 表 T-065 は動かない。**

---

## 5. ⭐ 残ること

⛔ **無い。**⭐ **`PD-394` はこれで閉じる。**
