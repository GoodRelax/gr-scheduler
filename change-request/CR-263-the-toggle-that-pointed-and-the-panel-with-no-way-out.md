# CR-263 — 向きを指していたトグルと、閉じ方の無いパネル

> **閉じるもの**: ⛔ **利用者の裁定 2 件（2026-08-26）** ——
> ①「マイルストーンの一覧を開く入口の図形を `▼` / `▲` ではなく `...` のトグルにしろ。」
> ②「プロパティパネルは参考実装と同じ寸法・入力欄で、**右上に `[x]`**。」
> ⭐ **裁定待ちの行は `PD-198`**（分類 `C`、`裁定済`）。②のうち本変更が閉じるのは **`[x]` の分だけ**である。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`（1 画面で全体を読める）。受ける目標は `GL-002`。**
⛔ **開いた面を閉じられなければ、その面が覆っている分だけ画面が狭いままになる。**
⚠️ **`propertyPanelShowing` を `null` へ戻す文が `src/` に 1 本も無い** —— 入口が図形の名簿に無いからである。

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `IC-52` の面の欄 | ⛔ **面が 5 つ挙がっているのに `Properties Panel` が無い。**⭐ `Resource Roster` が既に前例である（面であって、いわゆるモーダルではない）|
| **`R2.19`**（驚かせない）| `▼` / `▲` | ⛔ **三角は「向き」を意味する図形であり、隣の `IC-48` / `IC-49` のカーソルと同じ語彙に見える。**⭐ `...` は「続きがある」を意味する |
| **`R3.4`**（同じ算術を 2 か所に書かない）| 開く／畳むの 2 行 | ⭐ **行は 2 つのままでよい** —— 入口としては別だが、**画面に出る図形は同じ 1 つである。**参考実装も 1 つのボタンで、`aria-expanded` だけが入れ替わる |
| **`R2.20`**（届く）| 新しい図形 | ⭐ **`circle` は 図 F-019 で既に 31 か所使われており、生成器の `DRAWABLE` に入っている** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`IC-50`（開く）と `IC-51`（畳む）の両方を、同じ三点の図形にする** | ⛔ **`IC-51` を廃していない。**⭐ **利用者の「トグル」は画面の見えの話であり、見えは 2 状態とも `...` で同じになる。**⚠️ **参考実装がまさにその形である** —— 図形の文字列は状態で変わらず、`aria-expanded` だけが入れ替わる。⛔ **行を廃すのは戻すのが高い** |
| 2 | ⭐ **図形は塗りの円 3 つ**（`cx` 7 / 12 / 17、`cy` 12、`r` 1.6）| ⛔ **参考実装の逐語は `[...]`（角括弧つきの半角ピリオド 3 つ）だが、角括弧はボタンの縁の代わりであり、⭐ 本製品の入口は既に枠を持っている。**⚠️ **持ち込むと縁が二重になる** |
| 3 | ⭐ **`IC-52` の面の欄に `Properties Panel` を足すだけとし、新しい行を作らない** | ⛔ **同じ「閉じる」に 2 つ目の行を作れば、`IN-4` の正が 2 か所に散る。**⭐ **`Resource Roster` の前例がそのまま効く** |
| 4 | ⛔ **寸法と入力欄は本変更に含めない** | ⚠️ **`S-80` は 表 T-203 の行であり、文書に保存される値の塊に属する。**⛔ **`impact.py` が「表 T-203 を変えると塊全体を見ること」と警告する。**⭐ **別の変更要求で、塊ごと測ってから触る** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| `IC-50` を指している箇所 | ⛔ **要求 0 件 / 参照 0 箇所**（`impact.py`）。⚠️ 行が浮いている |
| `IC-52` を指している箇所 | ⭐ **要求 0 件 / 参照 2 箇所**（どちらも変更履歴）|
| 誘導部分グラフの閉路 | ⛔ **無し**（種 3 / 内部の辺 0 / 閉路 0）|
| `IC-50` のいまの図形 | `<path class="f" d="M6 9 H18 L12 16 Z"/>` —— 下向きの塗り三角 |
| `IC-51` のいまの図形 | `<path class="f" d="M6 15 H18 L12 8 Z"/>` —— 上向きの塗り三角 |
| `IC-52` のいまの面 | `Help Modal` / `AI Export Modal` / `Resource Roster` / `Export Chooser` / `Open Chooser` |
| `Properties Panel` は確定名か | ⭐ **`U-25`。** 画面の状態の鍵 `K-72` も `U-50` の定義文も同じ綴りを使う |
| 図 F-019 が使う要素 | `path` 149 ／ `circle` 31 ／ `rect` 28 ／ `ellipse` 1。⭐ **`circle` は既に通っている** |
| 参考実装の同じ入口 | ⭐ **`[...]`（半角ピリオド 3 つ）。⛔ `…`（U+2026）ではない。**⚠️ **開閉で図形は変わらず、`aria-expanded` だけが入れ替わる** |
| 生成器の受け入れ | ⭐ **両方とも通った** —— `generate_icon_roster.py` が 74 個、`generate_icon_glyphs.py` が 74 形。**数はどちらも不変** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 図 F-019 の `IC-50` の群 | 下向き三角の `path` を、塗りの `circle` 3 つに差し替える |
| 2 | 図 F-019 の `IC-51` の群 | 上向き三角の `path` を、同じ 3 つに差し替える |
| 3 | 表 T-109 の `IC-52` の面の欄 | 末尾に `Properties Panel` を足す |
| 4 | ⛔ **`_source/display-words.json` の `surfaces`** | 見出しの語を 1 組（`Properties Panel`）|
| 5 | `A-appendix` | 変更履歴に 1 行（`1.15`）|

⛔ **編集 4 は最初の計画に無かった。** ⚠️ **`generate_display_words.py` が拒んで初めて分かった** ——
「原稿は 5 つ持ち、仕様は 6 つを求める。欠けているのは `Properties Panel`」。
⭐ **`IC-52` の面の欄が、閉じられる面ごとの見出しの語の名簿を兼ねている。**
⭐ **語は発明していない** —— `ja` は `U-25` の確定名そのまま、`en` は残り 5 つと同じく `name` と同一である。
⚠️ **ただし残り 5 つの `ja` は短縮形である**（`Help Modal` → 「ヘルプ」）。
⛔ **短縮するなら利用者の語であり、こちらでは縮めていない。**

⛔ **表 T-109 の `IC-50` / `IC-51` の「何の入口か」の欄は 1 文字も動かさない** ——
⭐ **入口としては開くと畳むで別のままであり、変えたのは描く図形だけである。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1698 | **1698** | 0（セルと図形の編集のみ）|
| `uids` | 148 | **148** | 0 |

⭐ **生成で動くのは 2 つ** —— `icon-roster.json`（`IC-52` の面）と `icon-glyphs.json`（`IC-50` / `IC-51` の形）。
⚠️ **どちらも個数は 74 のまま**である。
