# CR-394 — 日程表の絵は表示の倍率を持ち、文書に保存する

> **閉じるもの**: 利用者の変更要求 2026-09-16 `JDG-122` のうち次の文と、問いへの答え `JDG-123` ①〜⑥ —— 逐語は `docs/development-records/rulings.md` の同行。
> - 「ヘッダーのダークモード切替の左に [-]と[+]ボタンを追加しろ 意味は全体的に図形やフォントサイズを合わせての拡大縮小だ 倍率は現行の 33%, 50%, 66%, 75%, 85%, 100%だ。 デフォルトは現行の50%にしろ。」
> - 「レクタングル系のタスクバーの縦幅を現行の 2/3 \* 50% にしろ マイルストーンのサイズを現行の2/3 \* 50% にしろ フォントサイズは2/3 \* 50% にしろ」
> - `JDG-123`: 倍率はヘッダーとパレットに掛けない ／ 行見出しの名前に掛ける ／ 文書ファイルに保存する ／ PNG・SVG の書き出しは倍率に従う ／ 100% で現行の 2/3、既定の 50% で現行の 1/3 ／ 行の軸のズームとは掛け算で重ね、字の上限（1.3201）は掛けたあとの字に効く
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `ccdcfabc`。⚠️ `npm run gen` が生成物 `src/**` を刷り直した（設定値の型・起動テンプレート・アイコンの名簿と図形・辞書・ヘルプの名簿）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（全体日程を 1 画面で見られる）—— 利用者は既定の出力の字と図形が大きすぎて一覧性が悪いと述べた（`temp/100percent.png` と `temp/50percent.png` を見比べた。50% で 2026-04〜2027-02 が 1 画面に入り、100% では 2026-04〜2026-08）。あわせて **`GL-004`** —— 倍率を文書に保存し、書き出しも従うので、同じ文書から同じ絵が出る。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— 表 T-201 の範囲は px の行どうしの一次の関係（例: `S-22` の上限は `actualMin`、`S-4` の下限は `actualMin ÷ actualOfPlan`、`S-86` の下限は `fontMin`）である。一部の行にだけ倍率を掛けると範囲が崩れる ⇒ 決定 3。
- **`R1.3`（既存の MUST との衝突）** —— `FR-077`「画面に描く文字を可読の下限より小さくしない」は、既定の倍率で字を現行の 1/3（14 ÷ 3 ＝ 4.67px）へ縮める `JDG-123` と正面から衝突する。利用者の読みが正 ⇒ 決定 5 で下限そのものに倍率を掛けた。
- **`R1.3`（`FR-017` の判定式）** —— 判定式の「12」は `S-8` の保存値と一致すると定める。`S-8` に掛けるのは描くときであり保存値は 12 のまま ⇒ 決定 4 の MUST NOT で両立。
- **`R1.4`（境界）** —— 両端の段で入口を押したとき ⇒ `FR-029` の薄く描く規則に当たると明記（決定 7）。
- **`R2`（命名）** —— 表 T-006b を引いた。`倍率` は `zoomX` / `zoomY`（`S-75` / `S-76`「横のズーム倍率」）にも使われる語なので、新しい値は必ず「表示の倍率」と 4 字で呼び、確定名は `displayScale` とした。表 T-104 の `K-85`（`fontScale`「文字サイズ」）とは語が重ならない。
- **`R4.3`（中間の不正状態）** —— 保存値を倍率で書き換える形は、倍率を戻したときに元に戻らない ⇒ 決定 4。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 値は 表 T-202 の `S-234`（`displayScale`、型 `33` / `50` / `66` / `75` / `85` / `100`、既定 `50`）。見せ方の群（`documentSettings`）に置く | `JDG-123` ③（保存する）。表 T-202 は `fontScale`（`S-70`）の「保存値は初期値、変えれば文書の編集」の先例を持つ（`FR-039`） | MSPDI には載らない —— `documentSettings` は `_source/mspdi-custom-fields.json` に 1 つも無い（`grep` 0 件） |
| 決定 2 | 描く比 ＝ `S-234` ÷ 100 × `S-236`。`S-236` は 表 T-206 の新しい行（0.6667、保存しない） | `JDG-123` ⑤「100% で現行の 2/3、50% で 1/3」。2/3 を文書ごとの値にすると同じ大きさの意味が 2 つの鍵に分かれるので、製品の基準として 1 行に置いた | 2/3 を選び直すと、保存済みの文書の絵が変わる（`S-138` など T-206 の行と同じ立場）。いまは利用者の文書が 1 つも無い |
| 決定 3 | 描く比を掛けるのは 表 T-252 の `DS-1`〜`DS-4` と `DS-9`（表 T-201 の px の行（`S-37` を含む）、`S-86`、`S-196`、`S-1`、`S-79`）。掛けないのは `DS-5`〜`DS-7`（`S-56`、`App Header` と `Command Palette`、掴み代と入口の箱）。`S-76` と `zoomX` とは掛け算（`DS-8`、`DS-4`） | `JDG-122`「全体的に図形やフォントサイズを合わせて」と、上の `R1.3`（範囲が一次の関係なので全部に同じ比を掛けるほか崩さない道が無い）。⭐ `S-1` / `S-37` / `S-79` に掛けるのは利用者の答え（2026-09-16、台帳 `JDG-131`「両方に掛ける。プロジェクトによって最適なサイズが異なる」）。掴み代を外すのは `S-90` の保存しない理由（読む人のアクセシビリティ） | 依存線（`S-18` 1.5px）も既定で 0.5px になる |
| 決定 9 | 人がドラッグで決めた行見出しパネルの幅は、描いた幅 ÷ 描く比 を `S-79` に保存する（MUST） | 決定 4（保存値は掛ける前の値、描くときに掛ける）の逆向き | なし |
| 決定 10 | 描く行見出しパネルの幅に床を置く: max（`S-79` × 描く比、`S-37` × 描く比 × `S-125` ＋ `S-138` × `S-235` ＋ 26 × `S-235` × 4）（MUST） | ⭐ **推奨で決めた（利用者へ後で問う）**。深さ 5 の掴み代が操作子に覆われない条件（`S-79` の注）は、操作子と掴み代に表示の倍率が掛からない（`DS-7`）ので 16·5·r ＋ 10.67 ≦ 200·r − 69.33 ⇒ r ≧ 0.6667 となり、倍率 100 でだけ等号で成り立つ。85 では 56px 対 44px、既定 50 では 37.3px 対 −2.7px、33 では 28.3px 対 −25.3px で覆われる | 小さい倍率ではパネルが比より広く描かれる（33 で 97.6px。比のままなら 44px） |
| 決定 11 | 表示の倍率を変えたとき、`Row Area` の横の中点が指す日付と縦の中点が指す行を動かさない（MUST）。行の位置は `rowPlacesAtZoomY` に問う | ⭐ **推奨で決めた（利用者へ後で問う）**。`S-1` に掛けるので、左端で留めると見ていた日付が横に動く。既存の錨は UC-007 手順 2 のポインタの下の錨だけで、入口から倍率を変えるときの錨はどの条項にも無い（`grep 中央` / `錨`） | 入口の位置にポインタがあるので、ポインタの下では留められない |
| 決定 4 | 保存値を描く比で書き換えない（MUST NOT）。導く元と導いた値の両方に掛けない（MUST NOT） | `R4.3`。目盛の字（`S-3`）は `fontScaleSizes` から、矢印のマーカーは名前の字（`CR-393`）から導くので、2 回掛かる | なし |
| 決定 5 | `FR-077` の可読の下限を `S-8` × 描く比とする（MUST） | `JDG-123` ⑤ の数が下限を割ることを求めている。WCAG 1.4.4 は、読む人が拡げる入口（`IC-105`）で満たす（`FR-039` の MUST NOT は変わらない） | `FR-077` の RATIONALE の「読めない文字で埋まった画面は…0 である」は、既定の倍率では弱まる |
| 決定 6 | `FR-016` の行の軸の上限で比べる 2 つの字は、どちらも描く比を掛けたあとの字とする（MUST）。`FR-017` の 1 日あたりの表示幅と実効フォントサイズも掛けたあと、割る 12 には掛けない。`FR-016` の `zoomX` の上限の「等倍の 1 日の幅」も掛けたあと | `JDG-123` ⑥。両方の字と床に同じ比が掛かるので、上限（1.3201 ＝ 16.9 ÷ 12.8016）は倍率に依らない。`FR-017` の判定式も左辺の分子と分母に同じ比が掛かるので、目盛の段階は倍率で動かない | なし |
| 決定 7 | 入口は `App Header` の `IC-16` の左に `IC-104`（1 段縮める）、`IC-105`（1 段拡げる）。両端で巡らせない（MUST NOT）。命令は 表 T-108 の `CM-74`（`setDisplayScale`、組 `—`）。図形は拡大鏡に − と ＋（図 F-019） | `JDG-122`「[-] と [+]」。巡らせると、縮めるつもりで最大の倍率へ跳ぶ。拡大鏡は既存の図形に無く（`fig-icons.svg` を引いた）、行見出しの頭の「⊞（足す）」と紛れない | 図形は起こしたばかりであり、実物で選び直す義務は 表 T-026 の `RC-13` が持つ |
| 決定 8 | 書き出しは画面と同じ表示の倍率で描く（`FR-080` の STATEMENT）。表 T-241 の `IX-1` の「倍率を持ってはならない」は出力の倍率の話であると注した | `JDG-123` ④ | なし |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| 文書の見せ方の群は `documentSettings` | 表 T-052 の `DR-3` |
| 保存値が初期値で、読む人が変えれば文書の編集になる先例 | `FR-039`、表 T-203 の後の注（`fontScale` / `themePreference`） |
| 字の上限 1.3201 は矩形の名前の字 12.8016（28 × 0.5715 × 0.80）と深さ 1 の行の名前の字 16.9（13 × 1.3）の比 | `FR-016`、表 T-201 の `S-4` `S-5` `S-7` `S-36` `S-38` |
| 判定式の 12 は `S-8` の保存値 | `FR-017` |
| MSPDI の独自フィールドに `documentSettings` の鍵は無い | `_source/mspdi-custom-fields.json` を `fontScale` / `zoomY` / `documentSettings` で引いて 0 件 |

## 2. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| 足した `（MUST）` / `（MUST NOT）` | `FR-039` +6、`FR-016` +1、`FR-077` +1 | 足した文を数えた |
| 表 T-109 の行 | 94 → 96 | `IC-` の行を数えた。前書きの「94 行」3 か所を 96 へ |
| 生成物のアイコンの名簿 / 図形 | 96 / 96 | `npm run gen` の出力 |
| 辞書の語 | 573 語（531 記入） | 同（当てる前 568 語 / 526 記入。`IC-104` / `IC-105` の 2 語 × 2 と `K-125` の 1） |

## 3. グラフ

**`impact.py FR-039 S-70 T-202 FR-016 FR-077 FR-080 FR-017 IX-1 IC-16 T-109 CM-62`**: 377 行。`FR-039` は `FR-017`（目盛）・表 T-108 の `CM-62`・表 T-109 から、`FR-080` は `WY-1`〜`WY-3` と 表 T-076 から指される。

**`induced.py FR-039 S-70 FR-016 FR-077 FR-080 FR-017 FR-094 S-22 IX-1 IC-16 CM-62 FR-029 S-138 FR-053 FR-051 EP-1 S-225`**: 17 種すべて解決、辺 21、閉路 4 —— `EP-1 ↔ S-225`、`FR-029 ↔ S-138`、`CM-62 ↔ FR-039`、`FR-094 ↔ S-22`。⇒ 各閉路の員を同じ巡で書いた（`CM-62` は変えず、`FR-039` の側から `CM-74` を足した）。

**届いた行を `rulings.md` で引いた**: `JDG-122` / `JDG-123` のほか、`fontScale` と行の軸の上限を裁いた行は無い ⇒ 衝突 0。

## 4. 当てた中身

| ID | 置き場 | 中身 |
|---|---|---|
| `FR-039` | `01-04-requirements.md` | STATEMENT に表示の倍率を足し、決定 2・4・7 と保存の理由の段、表 T-252 を足した |
| 表 T-252 `DS-1`〜`DS-8` | 同（`FR-039` の中） | 決定 3 |
| `FR-016` / `FR-017` / `FR-077` / `FR-080` / 表 T-241 の `IX-1` | 同 | 決定 5・6・8 |
| `FR-094` | 同 | 表 T-201 の寸法に倍率を掛けることは 表 T-252 が持つ、倍率はズームではない |
| `S-234` | `_source/settings.json`（表 T-202） | 決定 1 |
| `S-236` | 同（表 T-206） | 決定 2 |
| `S-138` の注 | 同 | 表示の倍率は掛けない（`DS-7`） |
| `K-125` | `_assets/tbl-glossary.md`（表 T-104） | `displayScale` ／ 表示の倍率 |
| `CM-74` | 同（表 T-108） | `setDisplayScale` |
| `IC-104` / `IC-105` | 同（表 T-109、`IC-16` の前） | 「表示の倍率を下げる（`S-234`）」「表示の倍率を上げる（`S-234`）」、正 `FR-039`（決定 7）。前書きの「94 行」3 か所と `01-04-requirements.md` の透かしの名前の段の「表 T-109 の 94 行」を 96 へ |
| 図形 | `_assets/fig-icons.svg` | `IC-104` / `IC-105` の 2 図形、`aria-label` を `IC-105` まで |
| 語 | `_source/display-words.json` | `IC-104` / `IC-105` の名と説明（日英）、`K-125` |
| 接頭辞 | `_source/row-id-prefixes.json` | `DS`（Display Scale） |
| 生成物 | `_assets/tbl-settings.md`、`_assets/tbl-row-id-prefixes.md`、`_source/grs-document.schema.json`、`src/**` の生成物 | `npm run gen` |

## 5. 数の予測

当てる前 `tables=150 figures=11 rows=1945 uids=153`。本書の差: tables +1（表 T-252）/ figures 0（図 F-019 の中身だけ）/ rows +15（`DS-1`〜`DS-9`、`IC-104`、`IC-105`、`K-125`、`CM-74`、`S-234`、`S-236`）/ uids 0。`CR-395` の `S-235` と合わせて rows +16 ⇒ 予測 `rows=1961`。

## 6. 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `DocumentSettings`（`src/entity/document-model/document-settings/document-settings.ts`、生成） | `displayScale` が型と既定値と範囲に入った（生成物）。⚠️ 規則 02 の 3.5: 読む側が「知らない鍵」で投げないかを先に 1 ファイル走らせて測る |
| 縦の寸法の鎖（表 T-064 の `PI-5`：`layoutFromSchedule` / `rowPlacesAtZoomY`、表 T-075 の `ScheduleGeometry`） | 表 T-252 の行に描く比を掛ける。`zoomY` と掛け算 |
| 行見出しの名前の字（表 T-064 の `PI-37` の `rowTitleFontPxOf`） | 描く比を掛ける |
| 行の軸の上限（`input-command-translator` の `zoomYWithinBand`） | 比べる 2 つの字に同じ比を掛ける（結果は変わらない） |
| 書き出し（表 T-064 の `PI-19` の `svgFromSchedule`、`ImageExporter`） | 画面と同じ描く比で描く |
| 画面の描き手（`domScreenSurface`）とフレーム（`frameLoop`） | `IC-104` / `IC-105` の押下で `CM-74` を出す、両端で薄く描く |

## 7. 試験が主張すべきこと

1. 既定の文書で `displayScale` が 50、矩形の予定の縦幅が 28 × 0.5 × 0.6667 ＝ 9.33px、名前の字の床が 12 × 0.3333 ＝ 4px（`FR-039`、表 T-252、`FR-077`）。
2. `IC-105` を押すと 50 → 66 → 75 → 85 → 100 と進み、100 で `IC-105` が薄く描かれ、押すと告げて値は変わらない（`FR-029`）。`IC-104` も逆向きに同じ。
3. 倍率を変えて戻したとき、表 T-201 の保存値が 1 つも変わっていない（`FR-039` の MUST NOT）。
4. `displayScale` を変えた文書を書き出して読み込むと、同じ倍率で描かれる（`WY-1`）。書き出した SVG の予定の縦幅が画面と同じ比（`WY-3`）。
5. `App Header` と `Command Palette` の入口の大きさが倍率によって変わらない（`DS-6`）。
6. 行の軸の上限の `zoomY` が、倍率 33 と 100 で同じ（`FR-016`）。

## 9. ⛔ この変更でやらないこと ／ 返した問い

- ✅ **答えを得た（問い 1）**: `S-79` と `S-37` の両方に掛ける ⇒ 決定 3・9・10、表 T-252 の `DS-1` / `DS-9`。
- ✅ **答えを得た（問い 3）**: `S-1` にも掛ける ⇒ 決定 3・6・11、表 T-252 の `DS-4`。
- ⚠️ **利用者へ後で問う（推奨で決めた）**: 決定 10（描く幅の床）と決定 11（倍率を変えるときの中点の錨）。
- ⛔ `fontScale`（`S-70`）を退役させない —— 表示の倍率と掛け算で残る
- ⛔ キーボードの割当（表 T-036）を足さない —— 要求に無い
