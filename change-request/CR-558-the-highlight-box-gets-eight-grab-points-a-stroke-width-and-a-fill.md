# CR-558 — ハイライトボックスに 8 つの掴み点と、線の太さと、透過率つきの塗りを持たせる

> 起草の状態: 起草（2026-09-24 に 11 節の問い 3 つへ利用者が答えた。問い 4 は前に立つ者が決めた（D1）。4 節は PR-22 が着地した木で当てる）
> 読んだ木: `61bbd572`（`refactor`。起草の途中で本 worktree が `ebc71984` から早送りされた。`CR-551` の仕様とコードは `6f674198` 〜 `7dbd292d` で着地済み。`7dbd292d` → `61bbd572` は変更要求の草案と台帳だけで、`docs/spec/` の原稿と `src/` は同じ）。⭐ 本書は `CR-551` 〜 `CR-554` の後に当たるので、**4 節の旧はすべて `61bbd572` の文で数えた**（`CR-551` が書いた文を旧に含む編集は、その旨を書いた）。9 節の `file:line` も `61bbd572` のものである。測り方は 13 節。
> ⭐ `PR-22`（`HighlightBox.strokeColor`、`DFC-800` の直し）は `c32b13d7` で着地した（`property-items.json` の末尾、`appliesTo: "HighlightBox"`。`property-items.schema.json` の `appliesTo` も `HighlightBox` を持つ ⇒ J-08 は当てることが無い）。J-05 ・ J-06 はそれを錨にする。⛔ 当てる直前に、本書の旧と行番号と数を測り直すこと。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示 2 通（`JDG-455` ・ `JDG-456`。逐語は 0.1 節）と、2026-09-24 の 11 節の問い 3 つへの答え（`JDG-457` 〜 `JDG-459`）。欠陥 `DFC-866` 〜 `DFC-869`（12 節）。
> ⛔ **覆すもの**: `FR-019` の RATIONALE の「枠線が唯一の表現なので、透明にすると何も残らない」（理由だけ。枠に透明を選ばせない MUST NOT は残す）。⭐ `JDG-231`（本体は枠線の近くだけ、内側は素通し）・`JDG-248`（枠の掴み代 3px・隅 6px）・`JDG-249` は**覆さない** —— 本書は `JDG-248` の「角が6」を辺の中点へ広げ、枠の掴み代を描いた太さの外側から測るだけである（決定 2 ・ 決定 6）。
> ⭐ **形の方針**: 利用者は使い勝手の試しのあとで裁定がまた動くと述べている。⇒ 掴み点の場所と応え方は 表 T-246 の 1 行ずつ（`HB-7` 〜 `HB-12`）に、値はすべて設定値の行（`S-369` 〜 `S-373`）に置き、要求はその行を指す。裁定を戻すときは、多くが 1 行の書き換えで済む（1 節の最右列）。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が体へ渡した写しのまま写した。

| 項目 | 逐語 |
|---|---|
| 1 | 「ハイライトボックスは周囲8点に選択点を設けてサイズ変更できるようにしろ。」 |
| 2 | 「選択点以外のハイライトボックスの線部分をドラッグするとハイライトボックスを移動しろ。」 |
| 3 | 「ハイライトボックス: 線色 / 線太さ(数値) / 塗りつぶし (透過率設定可能)」 |

前に立つ者が渡した事実（利用者の言葉ではない）: 線色の欄は、`CR-551` を閉じるセッションが `DFC-800` の直しとして 表 T-016 の `PR-22`（`HighlightBox.strokeColor`）に先に立てる。⇒ 本書は線色の欄を**作らない**。線の太さ・塗り・透過率だけを、その上に足す。

⭐ 読んだ事実（2026-09-24、`61bbd572`）: 項目 2 は**仕様にもコードにも既に在る** —— 表 T-023d の結びが「枠（枠線から内と外へ `S-293`）を掴めば囲む範囲を大きさを変えずに動かし」と定め（`CR-430`、`JDG-231` ・ `JDG-248`）、`item-hit-area.ts:917-920`（`isOnTheFrame` → `{ kind: 'body' }`）と `item-grab.ts:425-431`（`dayShift` ・ `drawnRowsCrossed`）がそれを行う。⚠️ 欠けているのは次の 2 つである: ① 枠の上でポインタの形が変わらない（`frame-loop.ts:339` の `'GR-14': null`。`DFC-866`）② 描いた形の上では枠の掴み代が 0 になり、タスクの上を通る枠を事実上掴めない（`item-hit-area.ts:918` の `onShape ? 0 : sizes['S-293']`。`DFC-868`）。利用者が項目 2 を述べたのは、この 2 つのどちらかに当たったからと読む（⚠️ 画面では確かめていない —— 本書は e2e を打たない）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4` ／ `GL-006`**（説明を読まずに操作できる）—— 見える掴み点、隅と辺と枠で変わるポインタの形は、押す前に何が起きるかを示す（`FR-106` の RATIONALE と同じ理由）。
⭐ **`CH-2` ／ `GL-002`**（全体を 1 枚で俯瞰する）—— 塗りと線の太さは、囲んだ範囲を俯瞰の中で目立たせる注記の本来の役目を強める（`UC-008`）。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正）** —— ① `FR-019` の「枠線が唯一の表現なので、透明にすると何も残らない」は、塗りを足すと偽になる → 理由を「枠は箱を掴む唯一の所」に書き替えた（E-16）。② `FR-006` の「数値の下限と上限は `grs-document.schema.json` が既に持つ」は、`S-132`（`cornerRadiusPx`）の下限・上限が 表 T-217 に在るのと既に食い違っている。本書の 2 つの数値の列も同じ所に置くので、例外を 1 文で書いた（E-03、決定 9）。③ 図 F-025（ポインタの形の一覧）は `CR-551` が足した `PK-10` を描いていない（`DFC-869`）—— 本書の 5 行も同じ図に足す（4.3 節）。④ 表 T-274 の結び（`01-04-requirements.md:419`）は、`PP-` のどれも覆わない行を 表 T-269 ・ 表 T-020 に足すことを禁じる → `PP-6` ・ `PP-7` に新しい行を書き足した（E-01 ・ E-02）。
- **`R1.4`（異常系・境界値）** —— ① 小さい箱では、隅と中点の掴み代が辺を覆い尽くし、枠を掴む所が残らない → 辺の長さの下限 `S-373` を立て、それより短い辺には中点を持たせない（決定 4）。② 保存済みの文書は新しい 3 列を持たない（生成したスキーマは列をすべて `required` にする。`erd_json_to_schema.py:225-227`）→ 読むときに `null` として足し、`null` は 表 T-217 の既定で描く（E-15 ・ E-18、決定 14 —— `CR-551` の決定 7 の「読むときに形を合わせる」と同じ型。兄弟の `CR-559` の E-09 とも同じ形）。③ 範囲の外の値（手で書いた文書、`Agent API`）→ 命令は拒み、読んだ値は範囲へ寄せて描き、文書は書き換えない（E-15）。④ 8 点が重なる 1 日 1 行の箱 → いちばん近い点を採る（今の四隅の読みと同じ。`item-hit-area.ts:844-866`）。
- **`R1.2`（検証できる表現）** —— 「8点」「線太さ(数値)」「透過率」を、表 T-246 の 6 行（`HB-7` 〜 `HB-12`）と設定値の 5 行（`S-369` 〜 `S-373`）にした。透過率は 0 ＝ 不透明、100 ＝ 透明と列の名（`fillTransparencyPercent`）で言い切った（決定 11）。
- **`R2.1`（命名）** —— 利用者の「選択点」は、用語集の `U-43`（`Grab Point` ／ 掴み点）に合わせて「掴み点」と書いた。⭐ 新しい表も接頭辞も立てない —— 掴み点の場所・掴み代・ポインタは、隅と枠の規則を既に持つ 表 T-246 の `HB-` の行と同じ意味なので、同表に行を足した（調整役の裁定、2026-09-24。1 つの意味を 2 つの表に割らない）。
- **`R4.x`（状態機械）** —— 当たらない。掴んで離す流れは今の `GR-14` の流れのままで、状態も出来事も足さない。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる。戻し方は 1 節）。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 8 点は四隅と 4 辺の中点。辺の中点は、その辺 1 本だけを動かす（新しい `HB-7`） | 逐語「周囲8点」。隅は 2 本の辺を動かす（`HB-4` ・ `HB-5`）ので、中点はそれを 1 本に限る | — |
| 決定 2 | 8 点の掴み代は、今の隅と同じ `S-230`（点から縦横へ 6px の正方形）。`S-230` の名を「8 つの掴み点」に広げる | `JDG-248`「角が6」。`JDG-399`「特別ルールを設けるな。既存のルールに従え」（別の変更要求への裁定だが、同じ日の同じ構えと読んだ） | 中点の掴み代を別の値にしたくなったら、行を 1 つ足す |
| 決定 3 | 掴み点は、箱を選んでいるあいだだけ描く（1 辺 `S-372` の正方形、地 `S-146`・縁 `S-151`・縁の太さ `S-174`、重ね順 `ZO-10`）。描いていない掴み点も応える（今の四隅と同じ）—— 11 節の問い 1（答え: `JDG-457`、推奨どおり） | 逐語「選択点を設けて」。描く色と太さは、選択の枠（`SL-8`）とフェードの掴み点（`ZO-10`）が既に使う値 | 選んでいない箱でも、隅を引けば大きさが変わる（ポインタの形がそれを示す） |
| 決定 4 | 辺の長さが `S-373`（36px 🔎）に満たない辺は中点を持たない（描かず、応えない） | R1.4 ①。36 は、隅の掴み代 2 つと中点の掴み代 1 つ（`S-230` の 2 倍）を置いたあと、隅と中点のあいだに `S-230` ずつ枠を残す長さ（6 ＋ 6 ＋ 12 ＋ 6 ＋ 6） | 低い倍率の短い箱は、今と同じ四隅だけになる |
| 決定 5 | 8 点は 1 つの段とし、掴み代に入る点のうち押した点にいちばん近い点を採る。等しいときは 表 T-246 の上の行 | 今の `nearestCornerOf` と同じ読み（`item-hit-area.ts:844`「the nearest corner, not the first」） | — |
| 決定 6 | 枠の掴み代は、描いた線の縁から内と外へ `S-293`（線の中心から、太さの半分 ＋ `S-293`）。描いた形の上では、描いた線そのもの（中心から太さの半分） | `JDG-248`「線が3」は描いた線から測る。形の上の読みは依存線と同じ（`item-hit-area.ts:700` の `onShape ? half : half + S-285`）。⇒ 今のコードの 0（`DFC-868`）を直す | 太い線ほど掴める幅が広がる |
| 決定 7 | ポインタは、枠 ＝ 環境の `move`、左上・右下の隅 ＝ `nwse-resize`、右上・左下 ＝ `nesw-resize`、左右の辺 ＝ `ew-resize`、上下の辺 ＝ `ns-resize`（表 T-269 の `PK-11` 〜 `PK-15`） | `FR-106`（何を掴むかと、引けばどちらへ動くかが分かる形）。環境の綴りで足りる形であり、画像を描く理由が無い（`PK-7` ・ `PK-8` ・ `PK-10` と同じ） | ⚠️ `PK-10`（`col-resize`、境目）と `PK-12`（`ew-resize`、形の端）は別の行である —— 境目は線を動かし、辺は箱の端を引き伸ばす |
| 決定 8 | 囲んだ内側は、塗っても掴み代にしない（素通しのまま） | `JDG-231`。逐語「線部分をドラッグすると…移動」 | 大きな箱を動かすには、今までどおり枠を狙う |
| 決定 9 | 線の太さは整数の px、1 〜 8、既定 1（`S-369`）。倍率に追随させない。下限・上限は 表 T-217 に置き、原稿の型（`erd.json` の `min` / `max`）には書かない | 既定 1 は今のコードの値（`schedule-overlays.ts:206` の `stroke-width="1"`）。表 T-217 に置くのは `S-132` と同じ型（見せ方の範囲で、試して動かす値）。下限 1 は、枠が掴む所の目印だから（決定 13） | 生成したスキーマは範囲の外を拒まない（`S-132` も同じ。読む側が寄せる —— E-15） |
| 決定 10 | 塗りの色は `CR-551` の色の欄（10 色を 5 つずつ 2 段、その下に [任意] [透明] [テーマ]）で選ぶ。`null` は `S-370` の色で描き、その既定は透明（塗らない）。[テーマ] は `null` へ戻す入口 | `JDG-383` ・ `JDG-405`（2 段目を [任意] [透明] [テーマ]）。`CV-9` は欄ごとの除外を枠の色と行の色にしか持たないので、塗りの欄は透明も並べる | `null` と透明は、`S-370` が既定のうちは同じに描かれる —— `FR-007` が「透明を選ぶことは戻すことにならない」とするとおり、値としては別であり、`S-370` を替えれば `null` の箱だけが追随する |
| 決定 11 | 透過率は `fillTransparencyPercent`、整数 0 〜 100、0 ＝ 不透明、100 ＝ 透明、既定 50（`S-371` 🔎）。描く不透明度は 1 − 値 ÷ 100 | 逐語「透過率」。列の名に向きを持たせ、0 がどちらかを読み違えない。百分率の整数は `percentComplete` と同じ持ち方 | 既定の 50 は測っていない（🔎）。⚠️ 兄弟の `CR-559` はコメントボックスに `fillOpacity`（0 〜 1、不透明度）を立てる —— 揃え方は 11 節の問い 4 を前に立つ者が D1 で決めた（決定 20） |
| 決定 11a | 3 列はどれも `null` を取り、`null` は 表 T-217 の既定で描く。新しく置く箱も 3 列を `null` で持つ（`CM-52`） | 既定の 1 行を替えれば、値を置いていない箱がすべて追随する（形の方針。裁定を戻すのが 1 行で済む）。`CR-559` の決定 15（コメントボックスの 5 列）と同じ持ち方 | 値を置いた箱と置いていない箱が、見た目では見分けられない |
| 決定 12 | 塗りはバーと字の後ろに描く（表 T-020 に `ZO-14` を順 2 で足す）—— 11 節の問い 2（答え: `JDG-458`、推奨どおり） | 前に描くと、塗りがバー・字・マーカーの色を変え、表 T-017a（`CT-1` 〜 `CT-5`）で測った組が崩れる | 塗りでタスクを「上から染める」見せ方にはならない |
| 決定 13 | 枠の線に透明を選ばせない MUST NOT は残し、理由を書き替える | 枠は箱を掴む唯一の所であり（`JDG-231`）、塗りは透明でありうるので、線を透明にすると掴む所が見えなくなる | — |
| 決定 14 | 新しい 3 列を欠く `HighlightBox` は、読むときにその列を `null` として足す。形式の版を問わない。通知しない。書き出す文書の形式の版（`SCHEMA_VERSION`）は進める | `CR-551` の決定 7（表 T-297、読むときに黙って形を合わせる）と同じ型。`null` は 表 T-217 の既定で描くので今の見え方そのもの（線 1px、塗り無し）であり、読む人に直すことが無い。版を問わないのは `editGroup` の補い（`json-codec.ts:1378-1396`）と `CR-559` の E-09 と同じ | 新しい文書を古い造りで開くと、`FR-073` の「読めない版」の問いが出る（拒まれない） |
| 決定 15 | プロパティパネルの行は `PR-23`（線の太さ）・ `PR-24`（塗りの透過率）・ `PR-25`（塗りの色）。並びは `PR-23` → `PR-24` → `PR-22`（線の色、`CR-551`）→ `PR-25` | 色の行は同じ対象の並びの末尾（`JDG-382`）。線の色（`PR-22`）は `CR-551` の側が持つ | 透過率の行が塗りの色の行と離れる |
| 決定 16 | 命令は 1 列に 1 つ: `CM-77` `setHighlightBoxStrokeWidth` ・ `CM-78` `setHighlightBoxFillColor` ・ `CM-79` `setHighlightBoxFillTransparency`。どれも `null` を置ける（既定へ戻す）。どれも 1 つで取り消しの 1 単位（表 T-027 の `UN-5`）。`Agent API` は `CM-55` と同じ道で届く（`FR-028`） | `CM-55`（`setHighlightBoxStrokeColor`）の型。形と大きさを書く命令は既に `CM-54` 1 つ（表 T-023d の結び）で、足さない | — |
| 決定 16a | 表 T-217 の 3 行の鍵は `HighlightBox.strokeWidthPx` ・ `HighlightBox.fillColor` ・ `HighlightBox.fillTransparencyPercent` と実体名を頭に付ける | `CR-559` の決定 21（`CommentBox.strokeWidthPx` ほか）と揃え、同じ表の中で鍵が重ならない | 既存の `S-132` の鍵（`cornerRadiusPx`）とだけ綴りの流儀が揃わない（`CR-559` と同じ代償） |
| 決定 17 | 表 T-246 の題を「本体と掴み点を離したときに置く値」に替える。`HB-1` 〜 `HB-6` の中身は変えない | 辺の中点は `HB-7` が持ち、隅の行はそのまま当たる | — |
| 決定 18 | 掴み点は描いた形の上でも同じ掴み代で応える（今の四隅と同じ。`nearestCornerOf` は `onShape` を見ない） | 枠が形の上で線そのものに縮むのは、囲んだタスクを掴めるようにするためであり、点は小さいのでその妨げにならない | 隅がタスクの上に来たとき、そこではタスクより隅が応える |
| 決定 19 | 角の丸み（`cornerRadiusPx`、`S-132`）のパネルの行は足さない | 逐語に無い | — |
| 決定 20 | 11 節の問い 4（`CR-559` との揃え）: 塗りの透過率は 2 つの箱とも 0 〜 100 の整数の百分率（0 ＝ 不透明）とし、描くときに範囲へ寄せる。既定はハイライトボックス 50、コメントボックス 0 | ⭐ 前に立つ者の決定 D1（2026-09-24。利用者に示し、異議は無かった）。⚠️ 利用者の裁定ではない —— 裁定の行（`JDG-`）を持たず、逐語も無い | `CR-559` の `fillOpacity`（0 〜 1）を百分率の透過率へ直すのは `CR-559` の側の仕事 |

---

## 1. 範囲 —— 項目ごとの行き先

| 項目 | 仕様で変える所 | 編集 | `frame-loop.ts` | 裁定を戻すとき |
|---|---|---|---|---|
| 1 8 つの掴み点 | 表 T-023d の `GR-14` と結び、表 T-246 の題と `HB-7` 〜 `HB-11`（新）、`S-230` の名、`S-372` ・ `S-373`（新）、表 T-020 の `ZO-10`、表 T-274 の `PP-6` | E-01 ・ E-06 〜 E-11 ・ E-14 ・ J-01 ・ J-02 ・ J-07 | ⚠️ 触る（`POINTER_BY_GRAB` `:313-340`、`pointerRowOf` `:541`。`CR-554` の後は `pointer-shape.ts`） | 中点を消す: `HB-10` ・ `HB-11` ・ `HB-7` を消す ／ 描き方: 表 T-023d の結びの 1 文 ／ 大きさ: `S-372` ・ `S-373` の値 |
| 2 枠を引いて動かす | 表 T-246 の `HB-12`（新）、表 T-269 の `PK-11`（新）、`FR-106` の 1 文 | E-08 ・ E-11 ・ E-12 ・ E-13 | ⚠️ 同上 | `HB-12` の 1 行 ／ `PK-11` の 1 行 |
| 2' ポインタの形 | 表 T-269 の `PK-11` 〜 `PK-15`（新）、表 T-246 の `HB-8` 〜 `HB-12` の「ポインタは」の句 | E-11 ・ E-12 ・ E-13 | ⚠️ 同上 | その行の句 1 つ |
| 3a 線の太さ | `FR-019` の RATIONALE、`AT-145`（新）、`S-369`（新、表 T-217）、`PR-23`（新）、`CM-77`（新） | E-15 ・ E-17 ・ E-19 ・ J-03 ・ J-04 ・ J-05 ・ J-06 | 触らない | `S-369` の既定・下限・上限 |
| 3b 塗りと透過率 | `FR-019` の RATIONALE、`CV-1`（`CR-564` の後の `CV-6` ・ `CV-10` は参照だけ）、表 T-020 の `ZO-14`（新）、`AT-146` ・ `AT-147`（新）、`S-370` ・ `S-371`（新）、`PR-24` ・ `PR-25`（新）、`CM-78` ・ `CM-79`（新）、表 T-274 の `PP-7` | E-02 ・ E-04 ・ E-14 〜 E-16 ・ E-19 ・ J-03 〜 J-06 | 触らない | 塗りの位置: `ZO-14` の順の 1 セル ／ 既定: `S-370` ・ `S-371` の値 |
| 3c 古い文書 | `05-07-design.md` の読み込みの段（`T-297` の後）、`FR-006` の 1 文 | E-03 ・ E-18 | 触らない | 1 段 |
| 3d 線色 | —（`CR-551` の `PR-22`） | なし | — | — |

**数**: 仕様の文の編集 18（E-01 〜 E-19。E-05 は欠番 —— `CV-6` を書かずに E-15 の 1 文が指す形にした）、原稿 JSON の編集 8（J-01 〜 J-08。J-08 は条件つき）。

---

## 2. 新しい識別子

⭐ 番号は 2026-09-24 に前に立つ者が `b7a3b76f` で詰めた（`CR-555` 〜 `CR-560` を CR の順に、配られた帯の先頭から隙間なく。帯の外の接頭辞は木の最大 M の次から）。下の「空いている」はその木で測った主張である。⚠️ 規則 02 の 2.5 節: **当てる直前に測り直すこと。** 測り方は 13 節。

| 種類 | 採ったもの | 測った最大（`b7a3b76f`、`CR-555` 〜 `CR-560` の草案を除く） |
|---|---|---|
| 設定値 | `S-369` ・ `S-370` ・ `S-371`（表 T-217）、`S-372` ・ `S-373`（表 T-206） | `S-368`（`CR-557`）の次（`S-999` は試験の番兵） |
| 台帳 | `JDG-455` 〜 `JDG-459`、`DFC-866` 〜 `DFC-869` | 帯の中で `CR-557` の次。⭐ 0.1 節の項目 1 と 2 は 1 通の 2 文なので 1 行（`JDG-455`）に畳み、項目 3 を `JDG-456`、11 節の答え 3 つを `JDG-457` 〜 `JDG-459` とした。問い 4 は前に立つ者の決定 D1 であり、行を持たない |
| 行 | `PK-11` 〜 `PK-15`（表 T-269）、`ZO-14`（表 T-020）、`CM-77` 〜 `CM-79`、`AT-145` 〜 `AT-147`（`erd.json` の `seat` 145 〜 147） | 最大 `PK-10` ・ `ZO-12`（`CR-556` の `ZO-13` の次）・ `CM-75`（`CM-76` は `CR-563` が取る）・ `AT-144` |
| 行（`HB` の追加） | `HB-7` 〜 `HB-12`（表 T-246。`HB-7` が辺の中点、`HB-8` 〜 `HB-12` が掴み点と枠） | 最大 `HB-6`。⭐ 調整役の裁定（2026-09-24）: 掴み点と枠の行は、新しい表と接頭辞ではなく 表 T-246 に足す。⛔ 新しい表・新しい接頭辞は 0 |
| 行（調整役の帯） | `PR-23` ・ `PR-24` ・ `PR-25` | 表 T-016 の帯 `PR-23` 〜 `PR-32` の先頭。`PR-22` は `CR-551` のセッションが `HighlightBox.strokeColor` に採った（`DFC-800` の直し）—— 線の色の欄は本書より前に在る。⛔ `PR-13` は退役した番号であり、使い直さない |
| 行（使わない） | —— | 要求・入口・対応表の行（`FR-` ・ `IC-` ・ `AM-`）を足さない。`UC-` ・ `V-` ・ `GL-` ・ `CH-` も採らない。⚠️ `FR-130` 〜 `FR-136` は遅延診断の変更要求（`CR-561`）が取り置いている |
| 名 | 列 `strokeWidthPx` ・ `fillColor` ・ `fillTransparencyPercent`（`HighlightBox`）、表 T-217 の鍵 `HighlightBox.strokeWidthPx` ・ `HighlightBox.fillColor` ・ `HighlightBox.fillTransparencyPercent`、命令 `setHighlightBoxStrokeWidth` ・ `setHighlightBoxFillColor` ・ `setHighlightBoxFillTransparency`、関数 `withHighlightBoxLookColumns` ・ `nearestGrabPointOf`、`BoxPart` の種 `'edge'` | `HighlightBox` の列の名は木に 0 件。`fillColor` は `TaskVisual`（`AT-102`）にも在る —— `strokeColor` が 2 つの実体に在るのと同じ |

- 本書が取らないもの: 保留の行（12 節）。
- 6 本を詰めた後に返った帯: `S-377` 〜 `S-384`、`T-309`（`T-306` ・ `T-307` は古い文書が引くので飛ばした）、`DFC-874` 〜 `DFC-889`、`PND-561` 〜 `PND-580`（6 本とも保留の行を起こさない）、`PR-29` 〜 `PR-32`、`FR-137` 〜 `FR-148`、`IC-109` 〜 `IC-114`、`AM-20` 〜 `AM-24`（6 本とも使わない）。

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 消すもの | 所（`61bbd572`） | 置き換わる先 | 編集 |
|---|---|---|---|
| `GR-14` の場所の欄の「ハイライトボックスは枠と四隅」 | `01-04-requirements.md:3396` | 「8 つの掴み点と枠（表 T-246）」 | E-06 |
| 「ハイライトボックスは枠と四隅を持つ。」と、枠と四隅の掴み代・効き目を述べる 1 文 | `:3409-3410` | 表 T-246 への指し ＋ `CM-54` で書く MUST | E-07 |
| 「描いた形の上では、枠は描いた線そのものでだけ応えること」の「線そのもの」の読みの幅（今のコードは 0） | `:3413` | 中心から描いた太さの半分 | E-08 |
| 「本体と四隅を離したときに置く値」（2 か所） | `:3427` ・ `:3433` | 「本体と掴み点を」 | E-09 ・ E-10 |
| `FR-019` の「枠線が唯一の表現なので、透明にすると何も残らない」 | `:4819` | 「枠は箱を掴む唯一の所であり…」 | E-16 |
| 表 T-020 の順の欄の 2 〜 13（`ZO-1` 〜 `ZO-6`） | `:3983-3994` | 1 つずつ下がる（3 〜 14） | E-14 |
| `S-230` の「四隅」（名と備考の「本行は四隅だけを持つ」） | `_source/settings.json` の `S-230`（生成物 `tbl-settings.md:349`） | 「8 つの掴み点」 | J-01 |
| `HB` の接頭辞の意味の「本体と四隅」 | `_source/row-id-prefixes.json:458` | 「本体と掴み点」 | J-07 |
| コードの定数と分岐（⛔ 本書は直さない。9 節） | `schedule-overlays.ts:206` の `stroke-width="1"`、`frame-loop.ts:339` の `'GR-14': null`、`item-hit-area.ts:918` の `onShape ? 0`、`properties-panel.ts:577-579` の `case 'highlightBox': return []`、`generate_entity_types.py:1835` の「表 T-217 は `S-132` 1 行」 | 設定値の行と 表 T-246 の `HB-8` 〜 `HB-12` | 実装する者 |

⭐ 消す行は 0（表の行を 1 つも退役させない）。

⭐ **参照だけ**: 表 T-017b の `CV-3` ・ `CV-6` ・ `CV-10`（`CR-564` の後の文）—— 本書は文を書かない。E-15 の 1 文が行 ID で指す。

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-551` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（13 節の道具が 18 件すべてで 1 回を HEAD `b7a3b76f` の写しで確かめた）。旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である。
⚠️ 同じファイルの編集は上から順に当てること。
⚠️ 生成物（`_assets/tbl-settings.md` ・ `tbl-property-items.md` ・ `fig-erd-*.md` ・ `tbl-row-id-prefixes.md` ・ `_source/grs-document.schema.json` と `src/` の生成物）は手で直さない。原稿を直して `npm run gen` を打つ。
⚠️ **`CR-551` の文を旧に含む編集**: E-01（`PK-10`）・ E-12（`GR-16` と `GR-22` の 1 文）・ E-13（`PK-10` の行）・ E-18（`RK-2` の行）。`CR-551` は `7dbd292d` で当たっているので、そのまま数えてよい。

### 4.1 文の編集（`01-04-requirements.md` ・ `05-07-design.md` ・ `_assets/tbl-glossary.md`）

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
項目 1・2'。表 T-274 の結びが、覆われない行を 表 T-269 へ足すことを禁じる（`:419`）。旧
```text
| PP-6 | ポインタの形で、何を掴むかが分かる | 押す前に結果が分かるようにするためである。<br>理由の全文は `FR-106` の RATIONALE が持つ | `PK-1` ／ `PK-3` ／ `PK-4` ／ `PK-5` ／ `PK-7` ／ `PK-8` ／ `PK-9` ／ `PK-10` |
```
新
```text
| PP-6 | ポインタの形で、何を掴むかが分かる | 押す前に結果が分かるようにするためである。<br>理由の全文は `FR-106` の RATIONALE が持つ | `PK-1` ／ `PK-3` ／ `PK-4` ／ `PK-5` ／ `PK-7` ／ `PK-8` ／ `PK-9` ／ `PK-10` ／ `PK-11` ／ `PK-12` ／ `PK-13` ／ `PK-14` ／ `PK-15` |
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
項目 3b。同じ結びが 表 T-020 にも掛かる。旧
```text
| PP-7 | 描く前後は 1 つの表で決める | 前後の決まりを 1 か所に集めるためである。<br>理由の全文は `FR-110` の RATIONALE が持つ | `ZO-1` ／ `ZO-1a` ／ `ZO-2` ／ `ZO-3` ／ `ZO-4` ／ `ZO-6` ／ `ZO-7` ／ `ZO-8` ／ `ZO-9` ／ `ZO-10` ／ `ZO-11` ／ `ZO-12` |
```
新
```text
| PP-7 | 描く前後は 1 つの表で決める | 前後の決まりを 1 か所に集めるためである。<br>理由の全文は `FR-110` の RATIONALE が持つ | `ZO-1` ／ `ZO-1a` ／ `ZO-2` ／ `ZO-3` ／ `ZO-4` ／ `ZO-6` ／ `ZO-7` ／ `ZO-8` ／ `ZO-9` ／ `ZO-10` ／ `ZO-11` ／ `ZO-12` ／ `ZO-14` |
```

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md -->
項目 3c（決定 9、R1.3 ②）。旧
```text
本表へ写してはならない（MUST NOT） —— 写した瞬間に正が 2 か所になる。  
```
新
```text
本表へ写してはならない（MUST NOT） —— 写した瞬間に正が 2 か所になる。  
⚠️ ただし、`_assets/tbl-settings.md` の 表 T-217 がキーを持つ `HighlightBox` の数値の列（`_assets/fig-erd-detail.md` の `AT-122` ・ `AT-145` ・ `AT-147`）の下限と上限は、同表が持つ —— 利用者が試して動かす見せ方の範囲であり、同表の 1 行を直せば済むようにする。  
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
項目 3b。旧（`CV-1` の行の末尾の句。1 回だけ現れる）
```text
列の形は `_assets/fig-erd-detail.md` の `AT-58` ・ `AT-102` ・ `AT-103` ・ `AT-121` が持つ |
```
新
```text
列の形は `_assets/fig-erd-detail.md` の `AT-58` ・ `AT-102` ・ `AT-103` ・ `AT-121` ・ `AT-146` が持つ |
```

⭐ E-05 は欠番 —— 表 T-017b の `CV-6` の文は書き替えず、塗りの描き方は E-15 の 1 文が `CV-6` ・ `CV-10` を行 ID で指すだけにした（`CR-564` が `CV-6` を書き直し、`CV-10` を立てた。番号は前に立つ者が後で詰める）。

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md -->
項目 1。旧
```text
| GR-14 | コメントボックス / ハイライトボックス | ハイライトボックスは枠と四隅、コメントボックスは本体・引出し線・線先（規則は本表の結びが持つ） | 動かす / 大きさを変える |
```
新
```text
| GR-14 | コメントボックス / ハイライトボックス | ハイライトボックスは 8 つの掴み点と枠（表 T-246）、コメントボックスは本体・引出し線・線先（規則は本表の結びが持つ） | 動かす / 大きさを変える |
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md -->
項目 1・2。旧
```text
ハイライトボックスは枠と四隅を持つ。  
枠（枠線から内と外へ `_assets/tbl-settings.md` の 表 T-206 の `S-293`）を掴めば囲む範囲を大きさを変えずに動かし、四隅（隅から同表の `S-230`）を掴めば囲む範囲の大きさを変える —— どちらも `_assets/tbl-glossary.md` の 表 T-108 の `CM-54` で書くこと（MUST）。  
```
新
```text
ハイライトボックスは、8 つの掴み点（四隅と 4 辺の中点）と枠を持つ —— 場所・掴み代・引いたときに変わるもの・ポインタの形は 表 T-246 の `HB-8` 〜 `HB-12` に、応える順は本結びの後の段に従うこと（MUST）。  
どれを掴んで離しても、`_assets/tbl-glossary.md` の 表 T-108 の `CM-54` で書くこと（MUST） —— 掴み点を掴めば囲む範囲の大きさを変え、枠を掴めば大きさを変えずに動かす。  
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md -->
項目 2・3b（決定 6 ・ 決定 8）。旧
```text
⛔ 囲んだ内側を掴み代にしてはならない（MUST NOT） —— 内側の押下は下のタスクへ素通しにすること（MUST）。  
⭐ 素通しにするので、本表で日程の形より先に在っても、中のタスクから押しを取らない。  
⚠️ 描いた形の上では、枠は描いた線そのものでだけ応えること（MUST） —— 依存線と同じ読みである（`FR-105` の 表 T-267 の `HT-1`）。  
```
新
```text
⛔ 囲んだ内側を掴み代にしてはならない（MUST NOT） —— 内側の押下は下のタスクへ素通しにすること（MUST）。  
⭐ 塗り（`_assets/fig-erd-detail.md` の `AT-146`）を持つ箱でも同じである —— 塗りは見せ方であり、掴む所を広げない。  
⭐ 素通しにするので、本表で日程の形より先に在っても、中のタスクから押しを取らない。  
⚠️ 描いた形の上では、枠は描いた線そのもの（線の中心から、描いた太さ `AT-145` の半分）でだけ応えること（MUST） —— 依存線と同じ読みである（`FR-105` の 表 T-267 の `HT-1`）。  
```

<!-- EDIT id=E-09 file=docs/spec/01-04-requirements.md -->
項目 1・2・2'。応える順と描き方を `GR-14` の結びの終わりに置く（行そのものは 表 T-246 の `HB-8` 〜 `HB-12`。E-11）。旧
```text
**ハイライトボックスの本体と四隅を離したときに置く値は 表 T-246 が持つ。**  
⚠️ 同表は新しい拒み方を立てない —— 拒むときの理由は 表 T-233 の `RS-44` であり、告げる作法は `FR-029` に従う。
```
新
```text
**ハイライトボックスの掴み点と枠の場所・掴み代・ポインタの形と、離したときに置く値は 表 T-246 が持つ。**  
⚠️ 同表は新しい拒み方を立てない —— 拒むときの理由は 表 T-233 の `RS-44` であり、告げる作法は `FR-029` に従う。  
⭐ 同表の `HB-8` 〜 `HB-11` の 8 つの掴み点を先に、`HB-12` の枠をその後に応えさせること（MUST）。  
⭐ 8 つの掴み点は 1 つの段とし、押した点を掴み代に含む点のうち、押した点にいちばん近い点を採ること（MUST） —— 1 日 1 行の小さな箱では 8 つの掴み代が重なり、先に数えた点を採ると、押した所から遠い点が応える。  
距離が等しいときは同表の上の行を採り、同じ行の 2 点では上の点（左右の辺の中点は左）を採る。  
掴み代と描く大きさの値は `_assets/tbl-settings.md` の 表 T-206 が持ち、倍率に追随させない。  
⭐ 掴み点は、その箱を選んでいるあいだだけ描くこと（MUST） —— 1 辺 `S-372` の正方形を点に中心を合わせて置き、地を `_assets/tbl-settings.md` の 表 T-236 の `S-146`、縁を同表の `S-151`、縁の太さを 表 T-206 の `S-174` で描く。  
重ね順は `FR-110` の 表 T-020 の `ZO-10` とすること（MUST）。  
⭐ 描いていない掴み点も応えること（MUST） —— 選んでいない箱の隅も、今までどおり引けば大きさを変える。  
押す前に何が起きるかは、表 T-246 の各行が名指すポインタの形が示す（`FR-106`）。  
⚠️ 掴み点は選択の手掛かり（表 T-023c の `SL-8`）ではない —— `SL-8` の枠は変えず、その上に重ねて描く。  
⭐ 掴み点は、描いた形の上でも同じ掴み代で応えること（MUST） —— 枠が形の上で描いた線そのものに縮むのは囲んだタスクを掴めるようにするためであり、小さな点はその妨げにならない。  
⭐ 選んでいる箱が 2 つ以上あっても、掴み点を引いて変わるのは掴んだ箱だけである —— まとめて動かすのは枠を引いたとき（表 T-023c の `SL-7`）に限る。  
⭐ 取り消しは、離した 1 回（`CM-54` 1 つ）を 1 単位とする（表 T-027 の `UN-5`）。
```

<!-- EDIT id=E-10 file=docs/spec/01-04-requirements.md -->
決定 17（表 T-246 が掴む所の行も持つので、題を広げる）。旧
```text
**表 T-246 — ハイライトボックスの本体と四隅を離したときに置く値**
```
新
```text
**表 T-246 — ハイライトボックスの掴み点と枠（場所・掴み代・ポインタ）と、離したときに置く値**
```

<!-- EDIT id=E-11 file=docs/spec/01-04-requirements.md -->
項目 1・2・2'（決定 1 ・ 決定 2 ・ 決定 4 ・ 決定 6 ・ 決定 7）。旧（`HB-6` の行の末尾の句。1 回だけ現れる）
```text
別扱いで 1 日の箱にすると、境目を 1 つ越えた所で 3 日へ跳び、2 日の位置が無くなる |
```
新
```text
別扱いで 1 日の箱にすると、境目を 1 つ越えた所で 3 日へ跳び、2 日の位置が無くなる |
| HB-7 | 辺の中点を引く（`HB-10` ・ `HB-11`） | 左と右の辺の中点は `HB-4` を横についてだけ、上と下の辺の中点は `HB-5` を縦についてだけ当てる —— 掴んだ辺だけを合わせ、向かいの辺と、もう一方の向きの日と行は据え置く。<br>境目が向かいの辺に届いたとき、または越えたときは、`HB-6` の同じ向きの段に従う | 辺の中点は辺を 1 本だけ動かす点である —— 隅が 2 本の辺を動かす読み（`HB-4` ・ `HB-5`）を、1 本に限ったものである。<br>縮められる下限は `HB-1` と同じである |
| HB-8 | 左上と右下の隅の掴み点（場所・掴み代・ポインタ） | 描いた箱の隅の点。<br>掴み代は、点から縦と横へそれぞれ `_assets/tbl-settings.md` の 表 T-206 の `S-230` の正方形。<br>引けば掴んだ隅の横と縦を変え（向かいの隅は据え置く）、離したときは `HB-4` ・ `HB-5` ・ `HB-6` を置く。<br>ポインタは 表 T-269 の `PK-14` | 隅は 2 本の辺を同時に動かす（`JDG-248` の「角が6」）。<br>ポインタの向きは、その隅が箱を引き伸ばす向きである |
| HB-9 | 右上と左下の隅の掴み点 | `HB-8` と同じ。<br>ポインタは `PK-15` | 同上 |
| HB-10 | 左と右の辺の中点の掴み点 | 辺の縦の中点。<br>掴み代は `HB-8` と同じ。<br>辺の長さ（箱の高さ）が 表 T-206 の `S-373` に満たないときは持たない。<br>引けば掴んだ辺の横だけを変え（行は据え置く）、離したときは `HB-7` を置く。<br>ポインタは `PK-12` | 中点は辺 1 本を動かす（`JDG-455`）。<br>`S-373` を置くのは、短い辺では隅と中点の掴み代が辺を覆い尽くし、枠（`HB-12`）を掴む所が残らず、箱を動かせなくなるからである |
| HB-11 | 上と下の辺の中点の掴み点 | 辺の横の中点。<br>掴み代は `HB-8` と同じ。<br>辺の長さ（箱の幅）が `S-373` に満たないときは持たない。<br>引けば掴んだ辺の縦だけを変え（日は据え置く）、離したときは `HB-7` を置く。<br>ポインタは `PK-13` | 同上 |
| HB-12 | 枠（掴み点を除く枠線） | 描いた枠線。<br>掴み代は、描いた線の縁から内と外へ 表 T-206 の `S-293`（線の中心から、描いた太さ `_assets/fig-erd-detail.md` の `AT-145` の半分に `S-293` を足した距離）。<br>描いた形の上では、描いた線そのもの（線の中心から太さの半分）だけ。<br>引けば大きさを変えずに箱を動かし、離したときは、横は引いた量で日の列ごとに平行に移し（予定の平行移動と同じ読み）、縦は `HB-3` を置く。<br>ポインタは `PK-11` | 本体は枠線の近くだけであり、内側は素通しにする（`JDG-231` ・ `JDG-455`）。<br>「線が3」（`JDG-248`）は描いた線から測る。<br>形の上の読みは依存線と同じである（表 T-267 の `HT-1`） |
```

<!-- EDIT id=E-12 file=docs/spec/01-04-requirements.md -->
項目 2'。旧（`CR-551` の E-17 が足した文）
```text
⭐ 基準日線の掴み代（表 T-023d の `GR-16`）と、パネルの境界の掴み帯（同表の `GR-22`）では、表 T-269 の `PK-10` とすること（MUST） —— どちらも縦の線を左右へ動かす操作であり、同じ形が同じ動きを示す。  
```
新
```text
⭐ 基準日線の掴み代（表 T-023d の `GR-16`）と、パネルの境界の掴み帯（同表の `GR-22`）では、表 T-269 の `PK-10` とすること（MUST） —— どちらも縦の線を左右へ動かす操作であり、同じ形が同じ動きを示す。  
⭐ ハイライトボックスの掴み代（表 T-023d の `GR-14`）では、`FR-016` の 表 T-246 の `HB-8` 〜 `HB-12` が名指す形とすること（MUST） —— 隅・辺・枠のどれを掴むかと、引けばどちらへ動くかを、押す前に示す。  
```

<!-- EDIT id=E-13 file=docs/spec/01-04-requirements.md -->
項目 2'（決定 7）。旧（`CR-551` の E-18 が足した行）
```text
| PK-10 | 左右の境目 | 閲覧環境の、縦の境目を左右へ動かす形（`col-resize`） | — | — | 環境のまま | 環境のまま |
```
新
```text
| PK-10 | 左右の境目 | 閲覧環境の、縦の境目を左右へ動かす形（`col-resize`） | — | — | 環境のまま | 環境のまま |
| PK-11 | 上下左右 | 閲覧環境の、上下左右へ動かす形（`move`） | — | — | 環境のまま | 環境のまま |
| PK-12 | 左右の辺 | 閲覧環境の、左右へ引き伸ばす形（`ew-resize`） | — | — | 環境のまま | 環境のまま |
| PK-13 | 上下の辺 | 閲覧環境の、上下へ引き伸ばす形（`ns-resize`） | — | — | 環境のまま | 環境のまま |
| PK-14 | 左上と右下の隅 | 閲覧環境の、左上と右下へ引き伸ばす形（`nwse-resize`） | — | — | 環境のまま | 環境のまま |
| PK-15 | 右上と左下の隅 | 閲覧環境の、右上と左下へ引き伸ばす形（`nesw-resize`） | — | — | 環境のまま | 環境のまま |
```

<!-- EDIT id=E-14 file=docs/spec/01-04-requirements.md -->
項目 1・3b（決定 3 ・ 決定 12）。旧
```text
| ZO-7 | 1（最背面） | 行の帯・グループ罫線・日付罫線 |
| ZO-1 | 2 | 予定バー（フェードの台形・予定の菱形・予定の矢を含む） |
| ZO-1a | 3 | **予実の補助線**（`FR-084`）。<br>バーの付属物なので、マーカーや依存線より手前に出さない |
| ZO-2 | 4 | 実績バー・実績のダミー・実績の菱形・実績の矢・停止日から再開アイコンへの破線 |
| ZO-4 | 5 | 依存線（地の色の縁を含む）。<br>⚠️ 線どうしの前後は `FR-009` に従い、本表に写さない |
| ZO-8 | 6 | イナズマ線・基準日線・2 連カーソル・ガイドカーソル・ハイライトボックスの枠 |
| ZO-3 | 7 | 進捗マーカー（**不透明な下地を持つ**）・再開アイコン |
| ZO-5 | 8 | 名称ラベル・担当と進捗の札 |
| ZO-9 | 9 | コメントボックス |
| ZO-10 | 10 | 選択の枠・フェードの掴み点（選んでいるタスクにだけ出す。<br>`FR-075`） |
| ZO-11 | 11 | 構えて引いている仮の依存線 |
| ZO-12 | 12 | 透かし（`FR-020`） |
| ZO-6 | 13（最前面） | **範囲選択の矩形**（表 T-023c の `SL-3`）。<br>⛔ **握っているあいだだけ描き、離したら消すこと（MUST）** —— **離した時点で残るのは選択そのものであり、矩形ではない** |
```
新
```text
| ZO-7 | 1（最背面） | 行の帯・グループ罫線・日付罫線 |
| ZO-14 | 2 | ハイライトボックスの塗り（`FR-019`）。<br>バーと字より後ろに置き、塗りがそれらの色を変えないようにする |
| ZO-1 | 3 | 予定バー（フェードの台形・予定の菱形・予定の矢を含む） |
| ZO-1a | 4 | **予実の補助線**（`FR-084`）。<br>バーの付属物なので、マーカーや依存線より手前に出さない |
| ZO-2 | 5 | 実績バー・実績のダミー・実績の菱形・実績の矢・停止日から再開アイコンへの破線 |
| ZO-4 | 6 | 依存線（地の色の縁を含む）。<br>⚠️ 線どうしの前後は `FR-009` に従い、本表に写さない |
| ZO-8 | 7 | イナズマ線・基準日線・2 連カーソル・ガイドカーソル・ハイライトボックスの枠 |
| ZO-3 | 8 | 進捗マーカー（**不透明な下地を持つ**）・再開アイコン |
| ZO-5 | 9 | 名称ラベル・担当と進捗の札 |
| ZO-9 | 10 | コメントボックス |
| ZO-10 | 11 | 選択の枠・フェードの掴み点（選んでいるタスクにだけ出す。<br>`FR-075`）・ハイライトボックスの掴み点（選んでいる箱にだけ出す。<br>`FR-016` の 表 T-246） |
| ZO-11 | 12 | 構えて引いている仮の依存線 |
| ZO-12 | 13 | 透かし（`FR-020`） |
| ZO-6 | 14（最前面） | **範囲選択の矩形**（表 T-023c の `SL-3`）。<br>⛔ **握っているあいだだけ描き、離したら消すこと（MUST）** —— **離した時点で残るのは選択そのものであり、矩形ではない** |
```

<!-- EDIT id=E-15 file=docs/spec/01-04-requirements.md -->
項目 3a・3b（決定 9 〜 決定 12）。旧
```text
**RATIONALE**: ハイライトボックスの線色を指定でき、指定が無ければ注記用の固定色で描くこと（MUST）。  
⭐ その固定色は `_assets/tbl-settings.md` の 表 T-236 の `S-312` である。  
```
新
```text
**RATIONALE**: ハイライトボックスの線色を指定でき、指定が無ければ注記用の固定色で描くこと（MUST）。  
⭐ その固定色は `_assets/tbl-settings.md` の 表 T-236 の `S-312` である。  
⭐ ハイライトボックスの線の太さと塗り（色と透過率）も指定できること（MUST） —— 列は `_assets/fig-erd-detail.md` の `AT-145` 〜 `AT-147`、既定と範囲は `_assets/tbl-settings.md` の 表 T-217 の `S-369` 〜 `S-371`、入口は `_assets/tbl-property-items.md` の 表 T-016 の `PR-23` 〜 `PR-25` が持つ。  
⭐ 3 つの列が `null` のときは、表 T-217 の同じ列の既定で描くこと（MUST） —— 既定の 1 行を替えれば、値を置いていない箱がすべて追随する。  
⭐ `HighlightBox.fillColor`（`AT-146`）は、描いた箱の内側を、パレット色なら `_assets/tbl-settings.md` の 表 T-294 がその名に持つ塗りの値で、カスタムカラーなら 表 T-017b の `CV-3` で決まった値そのもので、不透明度を 1 − `AT-147` ÷ 100 として塗ること（MUST） —— 描く値の選び方は同表の `CV-6` に従い、`CV-10` の導出は実績バーの塗りだけに当たるのでここには当てない。`AT-147` は透過率（0 は不透明、100 は透明）である。  
塗りの色が透明の箱は塗らない（`S-370` の既定は透明である）。  
⭐ 塗りは `FR-110` の 表 T-020 の `ZO-14` に描くこと（MUST） —— バーと字より手前に色を重ねると、表 T-017a の `CT-1` 〜 `CT-5` を測った組が崩れる。  
⭐ 表 T-217 の数値の列（`AT-122` ・ `AT-145` ・ `AT-147`）が同表の下限と上限の外の値を持つ文書を読んだときは、範囲へ寄せて描き、文書を書き換えないこと（MUST） —— 1 つの見せ方の値のために文書を拒めば、ほかの値まで読めなくなる（`05-07-design.md` が `documentSettings` について述べる寛さと同じである）。  
⭐ 同じ範囲の外の値を置く命令（表 T-108 の `CM-77` ・ `CM-79`）は拒むこと（MUST） —— 範囲へ寄せて書くと、置いた値と保存された値が食い違う。  
```

<!-- EDIT id=E-16 file=docs/spec/01-04-requirements.md -->
決定 10 ・ 決定 13（R1.3 ①）。旧
```text
ハイライトボックスに透明を選ばせてはならない（MUST NOT） —— 枠線が唯一の表現なので、透明にすると何も残らない。  
```
新
```text
ハイライトボックスの枠の線に透明を選ばせてはならない（MUST NOT） —— 枠は箱を掴む唯一の所であり（表 T-246 の `HB-12`）、塗りは透明でありうるので、線を透明にすると掴む所が見えなくなる。  
⭐ 塗りには透明を選ばせてよい —— 塗りの欄は 表 T-017b の `CV-9` のとおり透明を並べる。  
色の欄のテーマに戻す入口は、塗りを `null`（`_assets/tbl-settings.md` の `S-370` の色で描く。既定は透明）へ戻す。  
```

<!-- EDIT id=E-17 file=docs/spec/01-04-requirements.md -->
項目 3a。旧
```text
角丸の半径もズームによらず一定に描く。  
```
新
```text
角丸の半径と枠の線の太さも、ズームによらず一定に描く。  
```

<!-- EDIT id=E-18 file=docs/spec/05-07-design.md -->
項目 3c（決定 14）。旧（`CR-551` の E-30 が足した文。兄弟の `CR-559` の E-09 も同じ文を旧にし、本書の後に当たる —— 5.1 節）
```text
捨てた列を通知してはならず、書き戻してもならない（MUST NOT） —— 読む人の側に直せることが何も無い。  
```
新
```text
捨てた列を通知してはならず、書き戻してもならない（MUST NOT） —— 読む人の側に直せることが何も無い。  
⭐ 同じくスキーマを走らせる前に、`_assets/fig-erd-detail.md` の `AT-145` ・ `AT-146` ・ `AT-147` の列を持たない `HighlightBox` の項には、その列を `null` として足して読むこと（MUST） —— 3 列を足す前に保存した文書は、その鍵を持たない。  
⭐ 形式の版（`FR-073`）によらずに足す —— `null` は `_assets/tbl-settings.md` の 表 T-217 の既定で描く値なので（`01-04-requirements.md` の `FR-019`）、どの版の文書でも絵が変わらない。  
足したことを通知してはならない（MUST NOT） —— 読む人の側に直すことが何も無い。  
```

<!-- EDIT id=E-19 file=docs/spec/_assets/tbl-glossary.md -->
項目 3a・3b（決定 16）。旧
```text
| CM-55 | `HighlightBox` | `setHighlightBoxStrokeColor` | — | 枠の色を置く | `FR-019` |
```
新
```text
| CM-55 | `HighlightBox` | `setHighlightBoxStrokeColor` | — | 枠の色を置く | `FR-019` |
| CM-77 | `HighlightBox` | `setHighlightBoxStrokeWidth` | — | 枠の線の太さを置く | `FR-019` |
| CM-78 | `HighlightBox` | `setHighlightBoxFillColor` | — | 塗りの色を置く | `FR-019` |
| CM-79 | `HighlightBox` | `setHighlightBoxFillTransparency` | — | 塗りの透過率を置く | `FR-019` |
```

### 4.2 原稿 JSON の編集（`_source/`）

⛔ 各塊は、当てる前に原稿を `json.loads` で読み、名指した項が 1 つだけ在ることを確かめてから当てること。当てた後は `npm run gen` の前に、手で書いた検証の形（`*.schema.json`）で確かめること。

J-01（項目 1、決定 2）`settings.json` の 表 T-206 の `S-230` —— `value` と `note` を替える。`default` は変えない。
```json
{
 "id": "S-230",
 "value": {
  "ja": "ハイライトボックスの 8 つの掴み点の掴み代（表 T-023d の `GR-14`、`FR-016` の 表 T-246 の `HB-8` 〜 `HB-11`）"
 },
 "default": {
  "num": "6",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "同上。⭐ **本行は 8 つの掴み点（四隅と 4 辺の中点）だけを持つ** —— コメントボックスのアンカー（線先）は`commentAnchorSlop` が自分の数を持つ（1 値 1 役）。⭐ 6px は利用者が四隅に指定した値であり（`JDG-248`）、辺の中点は同じ値を使う（`JDG-455`）"
 }
}
```

J-02（項目 1、決定 3 ・ 決定 4）`settings.json` の 表 T-206 —— 次の 2 項を、`S-293` の項の直後に足す。
```json
{
 "id": "S-372",
 "value": {
  "ja": "ハイライトボックスの掴み点を描く正方形の 1 辺（`FR-016` の 表 T-023d の結び）"
 },
 "default": {
  "num": "6",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "同上。⭐ 掴み代（`S-230`）の 2 倍を超えないこと —— 描いた点の端が掴めなくなる。⛔ **倍率に追随させない**。⛔ **測って決めた値ではない** （🔎）"
 }
}
```
```json
{
 "id": "S-373",
 "value": {
  "ja": "ハイライトボックスの辺が中点の掴み点を持つ、辺の長さの下限（`FR-016` の 表 T-246 の `HB-10` ・ `HB-11`）"
 },
 "default": {
  "num": "36",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "同上。⭐ 隅の掴み代 2 つと中点の掴み代（どちらも `S-230`）を置いたあと、隅と中点のあいだに枠（`HB-12`）を掴める長さを `S-230` ずつ残す長さである（6 ＋ 6 ＋ 12 ＋ 6 ＋ 6）。⚠️ `S-230` を変えたら本行も見直す。⛔ **測って決めた値ではない** （🔎）"
 }
}
```

J-03（項目 3a・3b、決定 9 〜 決定 11a ・ 決定 16a）`settings.json` の 表 T-217 —— 次の 3 項を、`S-132` の項の直後に足す（`CR-559` の J-04 が、その後ろにコメントボックスの 2 項を足す）。
```json
{
 "id": "S-369",
 "key": "`HighlightBox.strokeWidthPx`",
 "type": {
  "ja": "整数"
 },
 "default": {
  "num": "1",
  "code": true,
  "mark": "🔎"
 },
 "min": {
  "num": "1"
 },
 "max": {
  "num": "8"
 },
 "note": {
  "ja": "ハイライトボックスの枠の線の太さ（px、`AT-145`）。列が `null` のときに描く値である。ズームによらず一定に描く規則は `FR-019` が持つ。既定は、本行より前のコードが描いていた太さ。下限は、枠が箱を掴む所の目印だからである（`FR-019`）。上限は測って決めた値ではない"
 }
}
```
```json
{
 "id": "S-370",
 "key": "`HighlightBox.fillColor`",
 "type": {
  "ja": "色"
 },
 "default": {
  "lit": "'transparent'",
  "code": true
 },
 "min": "—",
 "max": "—",
 "note": {
  "ja": "ハイライトボックスの塗りの色（`AT-146`。綴りは表 T-294 の保存する綴り）。列が `null` のときに描く値である。⭐ 既定の透明は、本行より前の箱の見え方（塗り無し）である。色の欄のテーマに戻す入口は列を `null` へ戻す（`FR-019`、表 T-017b の `CV-9`）"
 }
}
```
```json
{
 "id": "S-371",
 "key": "`HighlightBox.fillTransparencyPercent`",
 "type": {
  "ja": "整数"
 },
 "default": {
  "num": "50",
  "code": true,
  "mark": "🔎"
 },
 "min": {
  "num": "0"
 },
 "max": {
  "num": "100"
 },
 "note": {
  "ja": "ハイライトボックスの塗りの透過率（`AT-147`）。列が `null` のときに描く値である。0 は不透明、100 は透明（`FR-019`）。塗りが透明の箱では効かない。⛔ **測って決めた値ではない** （🔎）"
 }
}
```

J-04（項目 3a・3b）`erd.json` の `HighlightBox` —— 次の 3 列を、`cornerRadiusPx`（座 122）の後に足す。`min` / `max` / `default` は書かない（決定 9 ・ 決定 11a —— 範囲と `null` のときの値は 表 T-217 が持つ）。
```json
{
 "seat": 145,
 "name": "strokeWidthPx",
 "type": "整数（px）",
 "nullable": "可（`null` = 表 T-217 の既定）",
 "json": {
  "kind": "integer",
  "null": true
 },
 "key": "",
 "origin": "GRS",
 "exchange": "",
 "meaning": {
  "ja": "枠の線の太さ。`null` と範囲は `_assets/tbl-settings.md` の表 T-217 の `S-369`。ズームによらず一定に描く（`FR-019`）"
 }
}
```
```json
{
 "seat": 146,
 "name": "fillColor",
 "type": "文字列",
 "nullable": "可（`null` = 表 T-217 の既定）",
 "json": {
  "kind": "color",
  "null": true
 },
 "key": "",
 "origin": "GRS",
 "exchange": "",
 "meaning": {
  "ja": "塗りの色。形は `AT-102` と同じ。透明も取る。`null` は `_assets/tbl-settings.md` の表 T-217 の `S-370` の色で描く（既定は透明 ＝ 塗らない。`FR-019`）"
 }
}
```
```json
{
 "seat": 147,
 "name": "fillTransparencyPercent",
 "type": "整数（0〜100）",
 "nullable": "可（`null` = 表 T-217 の既定）",
 "json": {
  "kind": "integer",
  "null": true
 },
 "key": "",
 "origin": "GRS",
 "exchange": "",
 "meaning": {
  "ja": "塗りの透過率。**0 は不透明、100 は透明**。`null` と範囲は `_assets/tbl-settings.md` の表 T-217 の `S-371`（`FR-019`）"
 }
}
```

J-05（項目 3a・3b、決定 15）`property-items.json` —— `PR-22`（`CR-551` のセッションが足す `HighlightBox.strokeColor` の項）の直前に `PR-23` ・ `PR-24` を、直後に `PR-25` を置く。⚠️ `PR-22` がまだ無い木には当てない（5 節の S-10）。
```json
{
 "id": "PR-23",
 "columns": ["strokeWidthPx"],
 "inputKinds": ["数値"],
 "appliesTo": "HighlightBox",
 "note": {"ja": "枠の線の太さ（px）。既定と範囲は `_assets/tbl-settings.md` の表 T-217 の `S-369`"},
 "mspdi": {"ja": "無い（`GRS JSON` のみ）"}
}
```
```json
{
 "id": "PR-24",
 "columns": ["fillTransparencyPercent"],
 "inputKinds": ["数値"],
 "appliesTo": "HighlightBox",
 "note": {"ja": "塗りの透過率。0 は不透明、100 は透明。既定と範囲は同表の `S-371`"},
 "mspdi": {"ja": "無い（`GRS JSON` のみ）"}
}
```
```json
{
 "id": "PR-25",
 "columns": ["fillColor"],
 "inputKinds": ["色"],
 "appliesTo": "HighlightBox",
 "note": {"ja": "塗りの色。`null` ＝ 同表の `S-370` の色（既定は透明 ＝ 塗らない）。規則は `FR-019`"},
 "mspdi": {"ja": "無い（`GRS JSON` のみ）"}
}
```

J-06（決定 15）`display-words.json` の `properties` —— `PR-22` の項の後に次の 3 項を足す（英語の語は 11 節の問い 3）。
```json
{"rowId": "PR-23", "label": {"ja": "線の太さ", "en": "stroke width"}}
```
```json
{"rowId": "PR-24", "label": {"ja": "塗りの透過率（%）", "en": "fill transparency (%)"}}
```
```json
{"rowId": "PR-25", "label": {"ja": "塗り", "en": "fill"}}
```

J-07（決定 17）`row-id-prefixes.json` —— `HB` の `means` を替える（新しい接頭辞は足さない）。
```json
{"prefix": "HB", "words": "Highlight box", "owner": "spec", "means": {"ja": "ハイライトボックスの掴み点と枠の場所・掴み代・ポインタと、離したときに置く値の行"}}
```

J-08（条件つき）`property-items.schema.json` の `appliesTo` の `enum` —— `PR-22` を足した手番が `"HighlightBox"` を足していなければ足す（`61bbd572` では `["Task", "TaskGroup", "CommentBox"]`）。`description` の最後の文「A CommentBox row is one whose entrance is the panel (FR-097).」の後に「A HighlightBox row is one whose entrance is the panel (FR-019).」を足す。

### 4.3 生成器と生成物（形で示す）

| 何 | 何をする |
|---|---|
| `npm run gen` | 上の原稿から `_assets/tbl-settings.md` ・ `tbl-property-items.md` ・ `fig-erd-overview.md` ・ `fig-erd-detail.md` ・ `tbl-row-id-prefixes.md` ・ `_source/grs-document.schema.json` と、`src/` の生成物（`HighlightBox` の型、`SETTINGS_DEFAULTS`、`property-items.json`、`display-words.json`、スキーマの検証器の節の表）を刷る |
| `tools/generate_entity_types.py:1819-1848`（`annotation_defaults_block`） | 表 T-217 が `S-132` 1 行だけだと主張して止まる（`:1835`）。3 行を足すと止まるので、表の全行を `NOT_STORED_ANNOTATION_SIZES` へ刷る形に直す（`S-370` は `lit` の `'transparent'`、鍵の `HighlightBox.` は刷る名に含めない）。⭐ 本書が先に当たるので、この一般化は本書の手番が行い、`CR-559` の手番は自分の 2 行を足すだけで済む（5.1 節） |
| `tools/generate_entity_types.py:1123-1131` | `NOT_STORED_SIZES` の名簿に `S-373` を足す（掴みの側が読む）。`S-372` は描く側が読む —— 渡し方は 5 節の S-4 |
| `tools/generate_startup_template.py:112` | `SCHEMA_VERSION` を当てる日の日付へ進める（決定 14。`FR-073`）。同じ日に `CR-551` 〜 `CR-554` の手番が進めていれば、そのままでよい |
| 図 F-025（`_assets/fig-pointer-shapes.svg`） | `PK-11` 〜 `PK-15` の 5 つを足す。⚠️ 図は今 `PK-10` も描いていない（`DFC-869`）—— 同じ手番で 6 つを足す。環境の形は `PK-7` ・ `PK-8` と同じく、形の名（`move` ほか）を字で示す |
| `sample-schedule/*.json` ・ `src/framework/single-html-shell/startup-template.json` ・ `docs/guides/schedule-to-grs-json/grs-skeleton.json` | どれも `highlightBoxes: []` なので、列の追加では変わらない（13 節）。版だけが上の行で動く |

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | 仕様 ↔ 当たり（`item-hit-area.ts`） | `BoxPart` に `{ kind: 'edge', side: 'left' \| 'right' \| 'top' \| 'bottom' }` を足す。`noteHitOf` は、ハイライトボックスの 8 点（四隅 ＋ `S-373` 以上の辺の中点）を 1 つの段として、`isNearPoint(x, y, point, S-230, S-230)` に入る点のうちいちばん近い点を返す（等しいときは 表 T-246 の `HB-8` 〜 `HB-11` の行の順、同じ行では上・左）。それが無いときだけ枠: `isOnTheFrame` の掴み代は `onShape ? width / 2 : width / 2 + S-293`（`width` は寄せた `strokeWidthPx`） |
| S-2 | 当たり ↔ 書き込み（`item-grab.ts` の `highlightBoxRangeWrite`） | `part.kind === 'edge'` のとき、`side` が `left` / `right` なら今の隅の横の式だけを、`top` / `bottom` なら縦の式だけを当て、もう一方の向きは据え置く（`HB-7`）。書く命令は `setHighlightBoxRange`（`CM-54`）1 つ |
| S-3 | 形 ↔ 描き手（`schedule-geometry.ts` の `HighlightGeometry`。`CR-554` の後は `task-figures.ts`） | `HighlightGeometry` に `strokeWidthPx: number`（`box.strokeWidthPx ?? S-369 の既定` を `S-369` の範囲へ寄せた値）・ `fillColor: string`（`box.fillColor ?? S-370 の既定`）・ `fillOpacity: number`（1 − 寄せた `box.fillTransparencyPercent ?? S-371 の既定` ÷ 100）と、描く中点の有無（`hasSideHandles: { leftRight: boolean; topBottom: boolean }`）を足す。塗りの色が透明なら描き手は塗りの `<rect>` を出さない |
| S-4 | 描き手（`schedule-overlays.ts`）↔ 並べ手（`svg-renderer.ts`） | `OverlayParts` に `fillParts: readonly string[]` を足し、`svg-renderer.ts` は `zoLayer('ZO-14', overlays.fillParts)` を `ZO-7` の直後に置く。枠の `<rect>` は `stroke-width` に `strokeWidthPx`、塗りの `<rect>` は `fill` と `fill-opacity`、`stroke="none"`。選んでいる箱の掴み点は `selectionParts`（`ZO-10`）に、1 辺 `S-372` の正方形で |
| S-5 | 仕様 ↔ ポインタ（`frame-loop.ts` の `POINTER_BY_GRAB` ・ `pointerRowOf` ・ `pointerImageOf`。`CR-554` の後は `pointer-shape.ts`） | `PointerRow` に `PK-11` 〜 `PK-15` を足す。`GR-14` の行は掴み代の名だけでは決まらないので、`pointerRowOf` は `hit.item.kind === 'highlightBox'` のとき `hit.boxPart` から読む: `body` → `PK-11`、隅の左上・右下 → `PK-14`、右上・左下 → `PK-15`、辺の左右 → `PK-12`、上下 → `PK-13`。`pointerImageOf` は順に `'move'` ・ `'ew-resize'` ・ `'ns-resize'` ・ `'nwse-resize'` ・ `'nesw-resize'` を返す（`PointerShape` の型に足す）。コメントボックスの `GR-14` は `null` のまま |
| S-6 | 仕様 ↔ 命令（`edit-annotation.ts`） | `{ kind: 'setHighlightBoxStrokeWidth', id, strokeWidthPx: number \| null }` ・ `{ kind: 'setHighlightBoxFillColor', id, fillColor: string \| null }` ・ `{ kind: 'setHighlightBoxFillTransparency', id, fillTransparencyPercent: number \| null }`。拒み: 無い箱 `reject('CM-77' / 'CM-78' / 'CM-79', 'AT-116', …)`、整数でないか 表 T-217 の範囲の外 `reject('CM-77', 'S-369', …)` ／ `reject('CM-79', 'S-371', …)`、名でもカスタムカラーでもない色 `reject('CM-78', 'CV-1', …)`（透明は許す）。同じ値なら `edited(document)`。`createHighlightBox` は 3 列を `null` で置く |
| S-7 | 仕様 ↔ 読み込み（`json-codec.ts`） | `withHighlightBoxLookColumns(parsed)`: スキーマの前に、`schedule.highlightBoxes` の各項で 3 つの鍵（`strokeWidthPx` ・ `fillColor` ・ `fillTransparencyPercent`）のうち無いものを `null` で足す。版を見ない。通知しない（`CR-559` の S-6 の `withCommentBoxLookColumns` と同じ形。2 つを 1 つの関数にまとめてもよい） |
| S-8 | 仕様 ↔ パネル（`properties-panel.ts`） | `case 'highlightBox'` は 表 T-016 の `appliesTo: 'HighlightBox'` の行を刷る順に出す（`PR-23` → `PR-24` → `PR-22` → `PR-25`）。数値の欄の `min` / `max` は `S-369` ・ `S-371` の下限・上限。色の欄は `CV-9` の欄（塗りは透明を並べ、枠は並べない） |
| S-9 | 並べ手（`svg-renderer.ts:352`）↔ 描き手 | 今の `strokeOfBox`（`id` → `strokeColor`）は、塗りの色も要るので、箱の色の組（`strokeColor` と `fillColor`）の写しに広げる |
| S-10 | 本書 ↔ `CR-551` のセッション（`PR-22`） | 本書の J-05 ・ J-06 ・ J-08 と S-8 は、`PR-22`（`columns: ["strokeColor"]`、`appliesTo: "HighlightBox"`）が在る木に当てる。`PR-22` の語と注は本書が書かない |

### 5.1 ⭐ 兄弟の変更要求 `CR-559`（コメントボックス）と共にする行 —— `CR-558` を先に当て、`CR-559` はその木で旧を数え直す

| 共にするもの | 本書が触る所 | `CR-559` への申し送り |
|---|---|---|
| 表 T-023d の `GR-14` の行（`:3396`） | 場所の欄の前半（ハイライトボックス）を E-06 で書き替える | コメントボックスの後半を書き替えるなら、本書の新の行を旧にする |
| 表 T-023d の結び（`:3407-3428`） | `:3409-3410` ・ `:3411-3413` ・ `:3427-3428` と、その後に応える順と描き方の段を置く（E-07 〜 E-09） | `:3414-3426`（コメントボックスの段）は本書が触らない。旧をそこから採れば重ならない |
| 表 T-206 の `GR-14` の値（`S-230` ・ `S-293` ・ `S-291` ・ `S-292`） | `S-230` の名と注（J-01）、`S-293` の直後に `S-372` ・ `S-373`（J-02） | `S-291` ・ `S-292` を書き替えても重ならない。`S-293` の直後に行を足すなら、`S-373` の後に置く |
| 表 T-269（`PK-`）と `PP-6` | `PK-11` 〜 `PK-15`（E-13）、`PP-6` に 5 つ（E-01） | ⭐ 箱を動かす形が要るなら `PK-11`（`move`）を使い、同じ形の行を足さない。足すなら `PP-6` の末尾（`PK-15` の後）に書き足す |
| 表 T-020（`ZO-`）と `PP-7` | `ZO-14` を順 2 に入れ、順の欄を 1 つずつ下げる（E-14）。`PP-7` に `ZO-14`（E-01） | 行を足すなら、本書の後の順（1 〜 14）から数える |
| 表 T-017b の `CV-1`（と、参照だけの `CV-6` ・ `CV-10`） | `CV-1` に `AT-146`（E-04）。`CR-564` の後の `CV-6` は書かない —— 塗りの描き方は `FR-019` の RATIONALE の 1 文（E-15。下の 5.2 節）が `CV-6` ・ `CV-10` を行 ID で指す | ⚠️ `CR-559` の E-02 は本書の旧 E-05 の句を旧にしていた —— その錨は無い。`CV-6` を書かずに、自分の `FR-019` の段で同じ形の 1 文を立てる（5.2 節の逐語） |
| `FR-019` の RATIONALE | `:4815-4816`（E-15）・ `:4819`（E-16）・ `:4838`（E-17） | コメントボックスの引出し線の色の段（`:4826-4827`「線は注記の色で描くこと」「新しい設定値を立ててはならない」）は本書が触らない |
| 表 T-016 ・ `property-items.json` ・ `display-words.json` の `properties` | `PR-23` 〜 `PR-25`（`HighlightBox`） | `CommentBox` の行は `PR-21` の後、`PR-23` より前に置くと対象ごとに固まる |
| `erd.json` | `HighlightBox` の座 145 〜 147 | `CommentBox` の列は `CR-559` の `AT-148` 〜 `AT-152`。本書と重ならない |
| 表 T-217 の行と `generate_entity_types.py` の `annotation_defaults_block` | `S-132` の直後に `S-369` 〜 `S-371`（鍵は `HighlightBox.` を頭に。決定 16a）、生成器を全行の形に直す（4.3 節） | `CR-559` の J-04 は `S-371` の後ろに 2 行を足す。生成器は本書の手番が一般化した後なので、`CR-559` の手番は止まり方を直さずに済む。表の後ろの段（「収まる先が `HighlightBox` の列だからである」）は本書は書かず、`CR-559` の J-04 が注記の 2 実体に広げる |
| `05-07-design.md` の読み込みの段（E-18） | 旧「捨てた列を通知してはならず…」の後に `HighlightBox` の 3 行 | `CR-559` の E-09 は同じ 1 行を旧にする。本書の後もその行は 1 回だけ現れるので数え直しで通り、`CommentBox` の 3 行は本書の 3 行の前に入る（「同じくスキーマを走らせる前に」で始まる段が 2 つ並ぶ）。まとめるなら、当てる者が 1 段に畳んでよい |
| ポインタ（`POINTER_BY_GRAB` ・ `pointerRowOf`） | `GR-14` のハイライトボックスに `PK-11` 〜 `PK-15`（S-5） | ⚠️ `CR-559` の S-8「ほかの `GR-14`（本体・引出し線・ハイライトボックス）は今のまま `null`」のうち「ハイライトボックス」は、本書の後は偽になる —— 「本書の S-5 のとおり」と読み替えて当てる |
| `schedule-overlays.ts` の 2 つの `for`（`:196-240`） | ハイライトボックスの `for`（`:196-211`）と、塗りの `fillParts` | `CR-559` の 1d はコメントボックスの `for`（`:213-240`）。隣り合うので、本書の着地の上で数え直す（`CR-559` の 8 節が既にそう書いている） |
| `tests/contract/cr-441-t-274-principles-and-rows-both-ways.contract.test.ts:28-30` | `CLAIMED_ROWS` 87 → 93、`CLAIMED_COVERED` 83 → 89 | 本書の後の数から足す |

### 5.2 ⭐ `CR-559` へ渡す逐語（塗りの描き方の 1 文）

本書の E-15 は `01-04-requirements.md` の `FR-019` の RATIONALE の 2 行（旧）

```text
**RATIONALE**: ハイライトボックスの線色を指定でき、指定が無ければ注記用の固定色で描くこと（MUST）。  
⭐ その固定色は `_assets/tbl-settings.md` の 表 T-236 の `S-312` である。  
```

の後に文を足す。塗りの描き方の文はその 3 文目で、次の行のすぐ後、「塗りの色が透明の箱は塗らない」の行のすぐ前に在る:

```text
⭐ 3 つの列が `null` のときは、表 T-217 の同じ列の既定で描くこと（MUST） —— 既定の 1 行を替えれば、値を置いていない箱がすべて追随する。  
⭐ `HighlightBox.fillColor`（`AT-146`）は、描いた箱の内側を、パレット色なら `_assets/tbl-settings.md` の 表 T-294 がその名に持つ塗りの値で、カスタムカラーなら 表 T-017b の `CV-3` で決まった値そのもので、不透明度を 1 − `AT-147` ÷ 100 として塗ること（MUST） —— 描く値の選び方は同表の `CV-6` に従い、`CV-10` の導出は実績バーの塗りだけに当たるのでここには当てない。`AT-147` は透過率（0 は不透明、100 は透明）である。  
```

⛔ `CR-559` は、この文を旧にしない（本書の着地の前後で字句が変わりうる）。コメントボックスの塗りは、`CR-559` 自身の `FR-019` の段（表 T-308 の近く）に、同じ形の 1 文（主語を `CommentBox.fillColor` にし、「描く値の選び方は同表の `CV-6` に従い、`CV-10` の導出は実績バーの塗りだけに当たるのでここには当てない」を含める）を立てる。

---

## 6. グラフ（`61bbd572` で測った。`CR-551` の後の木）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 対象 | 要求 / 参照 | 指している要求 |
|---|---|---|
| `GR-14` | 2 / 10 | `FR-016` ・ `FR-019` —— `FR-019` の `:4779`「幅 0 で描くと、本体（`GR-14`）を掴む所が消える」は、本書の後も真 |
| `S-230` ・ `S-293` | 2 / 5 ・ 2 / 3 | `FR-016` ・ `FR-039`（`FR-039` は表示の倍率の除外の名簿で引く。値の意味は変えない） |
| `HB-4` ・ `HB-5` ・ `HB-6` | 2 / 4 ・ 1 / 2 ・ 1 / 2 | `FR-016` ・ `FR-103`。`HB-7` は 3 つを引くだけで書き直さない |
| `HB-1` ・ `CM-55` ・ `S-132` ・ `PP-6` ・ `PP-7` ・ `UN-5` ・ `ZO-8` ・ `ZO-10` ・ `CV-1` ・ `CV-9` | 0 / 0 〜 2 | 浮いている。⭐ 書き直しで偽になる所が無い |
| `CM-54` | 1 / 2 | `FR-016` |
| `FR-106` | 2 / 6 | `FR-040` ・ `FR-046`（`CR-551` が足した基準日線のポインタの文） |
| `IN-2` | 2 / 3 | `FR-101` ・ `FR-106` —— `IN-2` の「掴み代の上は 表 T-269 の形」は、本書で `GR-14` にも当たるようになる（`DFC-866` の直し）。文は変えない |
| `FR-019` | 7 / 30 | `FR-001` ・ `FR-007` ・ `FR-016` ・ `FR-033` ・ `FR-041` ・ `FR-076` ・ `UC-008`。`FR-007` の「塗りと輪郭を同時に透明にすることを許してはならない」は、枠が透明を取らないので満たされる |
| `CV-6` ・ `CV-10`（`CR-564` の後。参照だけ） | 1 / 5 ・ — | `FR-007`。本書は 2 つの文を書かない —— E-15 の 1 文が行 ID で指すだけ。⭐ 新しい `CV-6` は、カスタムカラーを実績バーの塗りのほかはすべて `CV-3` の値のまま使い、実績バーの塗りだけを `CV-10` で導くと定める ⇒ E-15 の 1 文と食い違わない |
| `AT-121` | 1 / 1 | `FR-007` |
| `FR-006` | 5 / 25 | `FR-011` ・ `FR-029` ・ `FR-040` ・ `FR-043` ・ `FR-099` —— E-03 の例外は `HighlightBox` の 3 列だけに掛かる |
| `SL-8` | 3 / 12 | `FR-081` ・ `FR-082` ・ `FR-083`。`SL-8` は書き直さない（掴み点は選択の手掛かりではない —— 表 T-023d の結び、E-09） |
| `FR-110` | 3 / 8 | `FR-011` ・ `FR-104` ・ `FR-105`。順の欄が動くだけで、どの要求も順の数を引かない |
| `HT-1` | 1 / 2 | `FR-016` |
| `S-151` ・ `S-174` ・ `S-312` | 4 / 7 ・ 1 / 2 ・ 1 / 1 | 引くだけで書き直さない |
| 表 T-020 ・ T-016 ・ T-023d ・ T-217 ・ T-246 ・ T-269 | —（表を指す要求が多い） | 行を足しても、表を指すだけの要求は偽にならない。T-020 は順の数を引く要求が 0（`grep "ZO-[0-9]* の順"` 0 件） |

⭐ 導いた条項ごとに、届いた行を `rulings.md` で引いた（規則 02 の 1、handoff.md 0.2 の 3 段）: 当たるのは `JDG-231`（本体は枠の近くだけ —— 覆さない。決定 8）、`JDG-248`（線が 3、角が 6 —— 覆さない。辺の中点へ広げる。決定 2）、`JDG-249`（コメントボックス —— 当たらない）、`JDG-382`（色の行は末尾 —— 決定 15）、`JDG-383` ・ `JDG-405`（色の欄の形 —— 決定 10）、`JDG-397`（宿主の色の入力は [任意] のときだけ —— 変えない）、`JDG-399`（掴み代に特別な規則を足さない —— 決定 2 の導き）。「掴み」「ハイライトボックス」「透過」「塗り」の語で `rulings.md` を引き、ほかに当たる行は無かった（13 節）。

### 6.2 `induced.py`（4 群、`cbf5ba1a` で測った。check.sh の書き出し）

| 種 | 解決 | 種の中の辺 | 閉路 | 扱い |
|---|---|---|---|---|
| `FR-016 GR-14 S-230 S-293 T-246 HB-4 HB-5 HB-6 CM-54 HT-1` | 10/10 | 18 | 1: `CM-54` `FR-016` `HB-4` `HB-5` `HB-6` | ⭐ `FR-016` の結び（E-07 〜 E-09）と 表 T-246（E-10 ・ E-11）を 1 つの計画で書いた。`HB-4` 〜 `HB-6` ・ `CM-54` の中身は書き直さない |
| `FR-106 T-269 IN-2 PP-6 T-274 FR-104` | 6/6 | 6 | 1: `FR-106` `IN-2` | `IN-2` は書き直さない（E-12 は `FR-106` の中の 1 文） |
| `FR-019 FR-007 CV-6 CV-9 CV-10 CV-3 AT-121 S-312 T-217 S-132 FR-006 T-016` | 12/12 | 15 | 1: `FR-019` `S-312` | `S-312` は書き直さない。`CV-3` ・ `CV-6` ・ `CV-10` は参照だけ（E-15 の 1 文が指す）。⚠️ 本行は `c32b13d7`（`CR-564` の後）で測り直した。`CV-10` ・ `CV-3` を足す前の 10 種では 10/10 ・ 辺 10 ・ 閉路 1（同じ） |
| `FR-110 T-020 ZO-8 ZO-10 PP-7 SL-8 FR-081` | 7/7 | 5 | 0 | — |

---

## 7. 数の予測（当てた後に同じ数え方で突き合わせる）

| 数 | 前（`CR-551` 〜 `CR-554` の後） | 後 | 内訳 |
|---|--:|--:|---|
| tables | N | N | 新しい表は無い |
| figures | 27 | 27 | 図 F-025 は中身が増えるだけ |
| rows | M | M ＋ 25 | 設定値 5（`S-369` 〜 `S-373`）、`PK-11` 〜 `PK-15` 5、`HB-7` 〜 `HB-12` 6、`ZO-14`、`CM-77` 〜 `CM-79` 3、`AT-145` 〜 `AT-147` 3、`PR-23` 〜 `PR-25` 3。消す行 0 |
| 接頭辞 | P | P | 足さない |
| 辞書の項 | — | ＋3 | `properties` の `PR-23` ・ `PR-24` ・ `PR-25` |
| 表 T-274 の試験の主張 | 87 / 83 / 4 | 93 / 89 / 4 | `PK-11` 〜 `PK-15` と `ZO-14`（`cr-441-t-274-…` の `CLAIMED_*`） |

⚠️ N ・ M ・ P は、`CR-552` ・ `CR-553` の後に測り直す（`61bbd572`、`CR-551` の後では tables=187 figures=27 rows=2305 uids=162）。

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ 仕様の編集（4 節）は 1 つの体が 1 つの worktree で当てる。⭐ 原稿を足すと読む側が投げる名簿がある（表 T-016 は `properties-panel.ts` の `heldByOf` が知らない行で投げる、`generate_entity_types.py:1835` は 表 T-217 の 2 行目で止まる）ので、**仕様の波と生成器の直しを同じ波にし、仕様の波とコードの波を合わせた木で門を打つ。**

```
wave 0  spec + generators   section 4 (E-01..E-19, J-01..J-08) on the tree after CR-551..CR-554 and PR-22,
                            tools/generate_entity_types.py (T-217 rows; S-373 into NOT_STORED_SIZES),
                            tools/generate_startup_template.py SCHEMA_VERSION, fig-pointer-shapes.svg
                            + npm run gen + gen:check
                            -> tests that read the spec verbatim go red here, by design (section 9)
wave 1  (parallel, disjoint files; cut from wave 0's commit)
  1a entity    item-hit-area.ts (S-1), schedule-geometry.ts highlightGeometry (S-3;
               task-figures.ts after CR-554)
  1b adapter   item-grab.ts (S-2), schedule-overlays.ts + svg-renderer.ts (S-4, S-9),
               properties-panel.ts (S-8)
  1c use-case  edit-annotation.ts + edit-document.ts:201 (S-6), json-codec.ts (S-7)
  1d types     the ~20 test files that build a HighlightBox literal gain the three columns
               (a type error otherwise; section 9) -- the implementer may do this, it is not a test of the spec
wave 2  shell (ONE body)   frame-loop.ts POINTER_BY_GRAB / pointerRowOf / pointerImageOf / PointerShape (S-5;
                            pointer-shape.ts after CR-554). No function grows; check 60 holds the sizes.
wave 3  tests by a spec-only tester (never the implementer), then check.sh + vitest on the merged tree;
        a Playwright probe in scratch/ for the drawn handles and the pointer (the Browser pane fires no rAF)
```

- ⚠️ 1a と 1b は `HighlightGeometry` の形を分けて持つ（1a が作り、1b が描く）。継ぎ目は 5 節の S-3 ・ S-4 を両方の依頼文へ逐語で写す。
- ⚠️ 1a と 2 は `BoxPart` の形を分けて持つ（1a が返し、2 がポインタに読む）。S-1 ・ S-5 を両方へ写す。
- ⚠️ **`CR-551` 〜 `CR-554` と共にするファイル**: `01-04-requirements.md`（`CR-551` 〜 `CR-553`）、`05-07-design.md`（`CR-551`）、`settings.json`（`CR-551` 〜 `CR-553`）、`display-words.json` ・ `property-items.json`（`CR-551`。`PR-22`）、`erd.json`（`CR-551`）、`row-id-prefixes.json`（`CR-551` の `RK`）、`tbl-glossary.md`（`CR-551`）。コードでは `frame-loop.ts`（`CR-551` 波 2、`CR-554` が割る）・ `schedule-geometry.ts`（`CR-551` 1a、`CR-554` が割る）・ `item-hit-area.ts`（`CR-554` が兄弟 2 つを出すが、`noteHitOf` は `UF-7` の範囲 `:1-957` に残る）・ `json-codec.ts`（`CR-551` 1d、`CR-554` が検証器を出す）・ `schedule-overlays.ts` ・ `svg-renderer.ts`（`CR-551` 1b。`CR-554` は割らない）・ `properties-panel.ts`（`CR-551`。`CR-554` は割らない）。⇒ 本書は 4 本の後に当たる。
- ⚠️ `CR-553` は 表 T-246 の `HB-5` の理由を書き替える（`CR-553` の決定 14、E-16）。本書は `HB-5` を書かず、`HB-6` の後に `HB-7` を足すだけなので、塊は重ならない。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

所は `61bbd572`（`src/` は `7dbd292d` と同じ）。「（`CR-554` の後）」は、段 7.5 の分割のあとに同じ関数が住むファイル。

| 項目 | 所 | 何をする | 試験（いま引いているもの） |
|---|---|---|---|
| 1 | `src/entity/layout-engine/item-hit-area/item-hit-area.ts` の `BoxPart`（`:40`）・ `nearestCornerOf`（`:846`）・ `isOnTheFrame`（`:870`）・ `noteHitOf`（`:893-922`、`:918` の `onShape ? 0`）（`CR-554` の後も同じファイル） | S-1。`nearestCornerOf` を 8 点の `nearestGrabPointOf` に広げ、枠の掴み代を描いた太さから測る（`DFC-868`） | `tests/unit/dfc-568-gr-14-corners-anchor-and-body.test.ts`（`S-230` ・ `S-293` と結びの逐語）・ `pnd-235-gr-14-comment-over-highlight.test.ts`（`boxPart` と `S-230`）・ `cr-438-pointer-chart-presses-grabs-and-arms.test.ts`（`kind: 'corner'`、`HB-` の行） |
| 1 | `src/adapter/input-command-translator/item-grab.ts` の `highlightBoxRangeWrite`（`:393-474`） | S-2（`HB-7`） | 同上の `dfc-568` ・ `cr-438-pointer-chart-…` |
| 1 | `src/adapter/svg-renderer/schedule-overlays.ts` の `overlayParts`（`:196-211`） | 選んでいる箱の掴み点を `selectionParts` へ（S-4） | `tests/unit/t-023c-selected-line-width.test.ts` ・ `t-023c-selection-points-at-what-exists.test.ts`（`'highlightBox'` の選択） |
| 2 | `src/framework/single-html-shell/frame-loop.ts` の `PointerFallback`（`:264`）・ `PointerShape`（`:268`）・ `PointerRow`（`:284`）・ `POINTER_BY_GRAB`（`:313-340`、`:339` の `'GR-14': null`）・ `pointerImageOf`（`:512`）・ `pointerRowOf`（`:541`）（`CR-554` の後は `pointer-shape.ts`、`UF-157`） | S-5（`DFC-866`） | `tests/unit/cr-430-table-pk-the-pointer-shapes.test.ts` ・ `cr-430-ga-pointer-column.test.ts`（`pointerRowOf` と 表 T-269 の行の全数）・ `in-2-pointer-shape.test.ts` ・ `tests/contract/cr-441-t-274-principles-and-rows-both-ways.contract.test.ts:28-30`（数の主張） |
| 3a・3b | `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts` の `HighlightGeometry`（`:135`）・ `highlightGeometry`（`:1084-1115`）（`CR-554` の後は `task-figures.ts`、`UF-144`） | S-3 | `tests/unit/layout-engine.test.ts`（`cornerRadiusPx`）・ `cr-432-svg-renderer-branches-the-spec-decides.test.ts`（`T-217` ・ `S-132`） |
| 3a・3b | `schedule-overlays.ts:206` の `stroke-width="1"`（`DFC-867`）・ `svg-renderer.ts:352`（`strokeOfBox`）・ `:500`（`ZO-7`） | S-4 ・ S-9。塗りを `ZO-14` に | `tests/unit/cr-430-t-020-the-order-things-are-drawn.test.ts` ・ `t-020-zo-layer-membership.test.ts`（`ZO-` の順と層）・ `pnd-235-…`（`ZO-8`） |
| 3a・3b | `src/use-case/edit-document/edit-annotation.ts` の命令の型（`:40-50`）・ `createHighlightBox`（`:213-230`）・ `setHighlightBoxStrokeColor` の型（`:263-272`）・生成の `NOT_STORED_ANNOTATION_SIZES`（`:276-284`）、`edit-document.ts:201` の命令の名簿 | S-6（`CM-77` 〜 `CM-79`） | `tests/unit/edit-annotation.test.ts`（`CM-55` ・ `S-132` ・ 表 T-217） |
| 3a・3b | `src/adapter/screen-renderer/properties-panel.ts:577-579`（`case 'highlightBox': return []`） | S-8 | `tests/unit/uf-64.test.ts`（`'highlightBox'`）・ `cr-551-delete-all-rows-date-fields-and-colour-field.test.ts` ・ `cv-9-transparent-and-unset-swatches.test.ts`（色の欄）・ `tests/contract/display-words.contract.test.ts`（辞書の項） |
| 3c | `src/adapter/document-codec/json-codec.ts` の `withSourceFormatOfAnOlderDocument`（`:1398`）の隣に 1 関数、生成の節の表（`:599-630` の `HighlightBox`） | S-7 | `tests/contract/document-invariants.contract.test.ts`（`cornerRadiusPx` の文書）・ `tests/system/three-rows-read-from-the-spec-alone.test.ts` |
| 型 | `HighlightBox` の文字どおりの値を組む試験 20 本（`cornerRadiusPx` を持つもの。13 節） | 3 列を足す（型が `required` にするので、足さないと型の誤り） | `tests/unit/uf-32.test.ts`（3 か所）・ `uf-22.test.ts` ・ `uf-30-31.test.ts` ・ `edit-task-group.test.ts` ・ `pi-18-screen-event-from-input.test.ts` ・ `pi-37-screen-view-readings.test.ts` ・ `t-024a-op-10-first-day-covered.test.ts` ・ `cr-438-key-shortcuts-display-scale-and-escape.test.ts` ほか |
| 逐語 | 仕様の文を引く試験 | 本書の新の文に | `dfc-568-…`（`:61` の「ハイライトボックスは枠と四隅を持つ。」、`:63` の枠と四隅の文、`:73` の「本体と四隅を離したときに置く値は」、`:108` ・ `:114` の `HB-4` ・ `HB-5` の頭）・ `tests/unit/settings-defaults-follow-the-manuscript.test.ts`（表 T-217）・ `tests/contract/seams.contract.test.ts`（表 T-217）・ `tests/system/divider-colour-corner-and-sticky-field.test.ts`（`S-132`） |
| 新しく要るもの | — | — | 8 点のどれを押しても、`HB-4` / `HB-5` / `HB-7` のとおりに範囲が変わる。36px 未満の辺に中点が無い。描いた形の上の枠が太さの半分で応える。選んだ箱にだけ掴み点が描かれる（e2e）。ポインタが 表 T-246 の `HB-8` 〜 `HB-12` の形になる（e2e）。塗りがバーの後ろに `fill-opacity` で描かれる。古い文書が 3 列なしで開き、黙って既定になる。範囲の外の命令が拒まれる |

---

## 10. ⛔ この変更でやらないこと

- **線色の欄** —— `CR-551` のセッションの `PR-22`（`DFC-800`）。
- **表 T-017b の `CV-6` ・ `CV-10`**（`CR-564` の後の文）—— 書かない。塗りの描き方は E-15 の 1 文が行 ID で指すだけである。
- **コメントボックスの見た目と掴み方** —— 兄弟の `CR-559`。`schedule-overlays.ts:217` ・ `:223` のコメントボックスの `stroke-width="1"` も同じく `CR-559` の持ち場である（`DFC-867` の後半）。
- **角の丸みのパネルの行**（決定 19）、**線の種類**（破線など。逐語に無い）。
- **ダブルクリックでハイライトボックスのパネルを出すこと**（表 T-023 の `MK-13` は今コメントボックスとタスクと行見出しだけを持つ）。
- **キーボードで大きさを変えること**。
- `docs/development-records/` の `pending-decisions.md` ・ `changelog.md`、`A-appendix.md` の変更履歴（当てる者が書く）。

---

## 11. 前に立つ者へ返す問い

| # | 問い | 推奨と理由 | 答え |
|---|---|---|---|
| 1 | 選んでいない箱の掴み点も応えるか —— いまは、選んでいない箱でも四隅を引けば大きさが変わる（`JDG-248` で着地した振る舞い）。例: 3 つの箱が並んでいて、2 つ目の右下の隅にポインタを置くと、選ぶ前から `nwse-resize` の形になり、引けば大きさが変わる。A: 応える（掴み点は選んだ箱にだけ描き、ポインタの形で予告する） ／ B: 選んでいる箱だけ応える（選んでいない箱は隅も枠として動く。大きさを変えるには、1 度押して選んでから隅を引く） | **A**（決定 3）。今の振る舞いを変えず、1 手少ない。押す前の形が変わる（`FR-106`）ので、見えない掴み点で驚かせることは少ない。B は当たりの側が選択を知る形に変わる（`item-hit-area.ts` の入口の引数が増える）。戻すときは 表 T-023d の結びの 1 文（「描いていない掴み点も応えること」） | **「選んでいない箱の掴み点 → 推奨のA」**（`JDG-457`） |
| 2 | 塗りはタスクの後ろか前か —— 例: 3 つのタスクを囲んで赤く塗ったとき、A: 行の帯の上・バーの後ろに塗る（バーと名前の色はそのまま、箱の中の地だけが赤みを帯びる） ／ B: バーと名前の上から半透明に重ねる（タスクごと赤く染まる。透過率が低いと名前が読みにくくなる） | **A**（決定 12）。B は、表 T-017a の字とバーの色の比（`CT-1` 〜 `CT-5`）を測った組を上から崩し、透過率によっては名前が読めなくなる。A でも透過率は、行の帯や罫線を透かすのに効く。戻すときは表 T-020 の `ZO-14` の順の 1 セル | **「塗りを、タスクのバーの後ろと前のどちらに描くか →推奨のA」**（`JDG-458`） |
| 3 | 値と英語の語 —— 線の太さ 1 〜 8px（既定 1）、透過率の既定 50%、掴み点の大きさ 6px、中点を持つ辺の長さ 36px。英語の語「stroke width」「fill transparency (%)」「fill」 | 提案どおり当て、使い勝手の試しで 1 行ずつ直す（`S-369` 〜 `S-373`、辞書の 3 項）。英語の語は辞書の `$comment` が体に決めさせない（`JDG-324` と同じ扱い） | **「値と英語の語 → 提案どおり 」**（`JDG-459`） |
| 4 | ⭐ 兄弟の `CR-559` との揃え —— 利用者は 2 つの箱に同じ言葉「塗りつぶし (透過率設定可能)」を使ったが、`CR-559` はコメントボックスに `fillOpacity`（0 〜 1 の小数、**不透明度**、既定 1）を、本書はハイライトボックスに `fillTransparencyPercent`（0 〜 100 の整数、**透過率**、既定 50）を立てた。例: 両方の箱を選んで同じ「半分透ける」にしたいとき、片方の欄には 0.5、もう片方には 50 と打つことになり、しかも数の向きが逆である。A: 両方を透過率の百分率にそろえる（本書の形。`CR-559` の `AT-151` の名・型・範囲と `S-375` ・ `CM-83` ・ 辞書の語を替える）／ B: 両方を不透明度の小数にそろえる（`CR-559` の形。本書の `AT-147` ・ `S-371` ・ `CM-79` ・ `PR-24` の語を替える）／ C: 別々のまま。⚠️ もう 1 つの食い違い: 範囲の外の値を読んだとき、`CR-559` はそのまま描き（決定 17）、本書は範囲へ寄せて描く（E-15） | **A**。利用者の言葉は「透過率」であり、パネルの語が利用者の言葉と同じになる。百分率の整数は `percentComplete` と同じ持ち方で、打ち込みも読みも易しい。範囲の外は、寄せて描く（本書）に揃える —— 太さ 1000px の枠がそのまま描かれると日程が読めなくなる。どちらを採っても、直すのは片方の変更要求の数行である | 前に立つ者の決定 D1（利用者の裁定ではない。決定 20） |

---

## 12. 台帳

### 12.1 裁定（`<scratchpad>/CR-558-ledger.md` に、`rulings.md` へ貼る形で書いた）

`JDG-455`（項目 1 ・ 2）・ `JDG-456`（項目 3）・ `JDG-457` 〜 `JDG-459`（11 節の問い 1 〜 3 への答え）。問い 4 は前に立つ者の決定 D1 であり、裁定の行を持たない（0 節 ③ の決定 20）。状態はすべて「指示」（本書を当てたら「適用済」へ変える。当てる者の仕事）。
⛔ 当てる者が状態を変える古い行: `JDG-248`（「角が6」を辺の中点へ広げた、と状態欄に記す。覆してはいない）。

### 12.2 欠陥（同じ scratch に、`defects.md` へ貼る形で書いた）

| ID | 何 | 状態 |
|---|---|---|
| `DFC-866` | ハイライトボックスの掴み代（`GR-14`）の上でポインタの形が変わらない（`frame-loop.ts:339` の `'GR-14': null`、表 T-269 に行が無い）—— `IN-2` は掴み代の上で形を変えることを求める | `仕様待ち`（本書の E-11 〜 E-13） |
| `DFC-867` | ハイライトボックスの枠の太さが定数 `1`（`schedule-overlays.ts:206`）で、設定値の行が無い | `仕様待ち`（本書の `S-369`） |
| `DFC-868` | 描いた形の上で、ハイライトボックスの枠の掴み代が 0（`item-hit-area.ts:918`）—— 「描いた線そのもの」を依存線は太さの半分と読む（`:700`）。0 では、タスクの上を通る枠を事実上掴めない | `実装待ち`（仕様は既に「描いた線そのもの」。本書の E-08 が幅を書く） |
| `DFC-869` | 図 F-025（ポインタの形の一覧）が、`CR-551` が 表 T-269 に足した `PK-10` を描いていない（`fig-pointer-shapes.svg` の行は `PK-1` 〜 `PK-9`） | `仕様待ち`（本書の 4.3 節で `PK-11` 〜 `PK-15` と一緒に足す） |

---

## 13. 測り方の再現

```
# totals (before, 61bbd572 = after CR-551)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   -> tables=187  figures=27  rows=2305  uids=162   (ebc71984, before CR-551: 186 / 27 / 2282 / 162)

# free identifiers (2026-09-24): every prefix's maximum over docs/spec src tests tools change-request,
# on ebc71984 and again on 61bbd572 after the fast-forward
for p in S PK HB CM ZO AT PR; do
  git grep -ohE "\b$p-[0-9]+[a-z]?\b" -- docs/spec src tests tools change-request | sed "s/$p-//;s/[a-z]$//" | sort -n | tail -1
done
#   -> S 359 (999 is a test sentinel), PK 10, HB 6, CM 75, ZO 12, AT 144, PR 21   (both trees)
#   CR-559's draft (same worktree) takes S-374..376, AT-148..152, CM-80..84, PR-26..28 (compacted on b7a3b76f, 2026-09-24) -- no overlap
git grep -nE "\bS-3(69|7[0-3])\b|\bPK-1[1-5]\b|\bHB-([7-9]|1[0-2])\b|\bCM-7[7-9]\b|\bZO-14\b|\bAT-14[5-7]\b|\bPR-2[3-5]\b|\bJDG-45[5-9]\b|\bDFC-86[6-9]\b" \
  -- docs/spec src tests tools change-request
#   -> nothing but this file

# the code facts of 0.1 and section 9 (61bbd572; src is the same as 7dbd292d)
grep -n "'GR-14': null" src/framework/single-html-shell/frame-loop.ts                 # -> :339
grep -n "onShape ? 0" src/entity/layout-engine/item-hit-area/item-hit-area.ts        # -> :918
grep -n 'stroke-width="1"' src/adapter/svg-renderer/schedule-overlays.ts             # -> :206 (box), :217 :223 (comment)
grep -n "case 'highlightBox'" src/adapter/screen-renderer/properties-panel.ts        # -> :577 (returns [])
grep -o "PK-[0-9]*" docs/spec/_assets/fig-pointer-shapes.svg | sort -u               # -> PK-1 3 4 5 7 8 9
git grep -l "cornerRadiusPx" -- tests | wc -l                                         # -> 20
grep -c '"highlightBoxes": \[\]' sample-schedule/*.json src/framework/single-html-shell/startup-template.json \
  docs/guides/schedule-to-grs-json/grs-skeleton.json                                 # -> every one is empty

# graph
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>          # each ID of 6.1
# induced.py, cbf5ba1a (check.sh's export, scratch/spec-check/sd-out/json/index.json):
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-016 GR-14 S-230 S-293 T-246 HB-4 HB-5 HB-6 CM-54 HT-1
#   -> 10/10, 18 edges, 1 cycle: CM-54/FR-016/HB-4/HB-5/HB-6
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-106 T-269 IN-2 PP-6 T-274 FR-104
#   -> 6/6, 6 edges, 1 cycle: FR-106/IN-2
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-019 FR-007 CV-6 CV-9 AT-121 S-312 T-217 S-132 FR-006 T-016
#   -> 10/10, 10 edges, 1 cycle: FR-019/S-312
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-110 T-020 ZO-8 ZO-10 PP-7 SL-8 FR-081
#   -> 7/7, 5 edges, 0 cycles

# rulings searched before calling anything "needs a ruling"
grep -n "ハイライトボックス\|掴み代\|掴み点\|透過\|塗り" docs/development-records/rulings.md
#   -> JDG-231, JDG-248, JDG-249, JDG-382, JDG-383, JDG-397, JDG-399, JDG-405 (read in 6.1); nothing else decides these

# every old block of section 4.1 occurs exactly once in its file, and all 19 apply in order to a COPY:
#   <scratchpad>/check558.py (parses the EDIT blocks of this file)
#   -> on a copy of HEAD c32b13d7 (git archive c32b13d7 docs/spec): problems 0 edits 18 (E-05 is a gap)
# every JSON edit of section 4.2 applied to COPIES of the six manuscripts (a stand-in PR-22 for J-05),
#   loaded with json.loads and validated with jsonschema against the hand-written schemas:
#   <scratchpad>/check558json.py
#   -> settings.json 0 errors, erd.json 0 (seats unique), property-items.json 0 (with J-08's enum),
#      display-words properties unique, row-id-prefixes 163 -> 163 (HB means replaced, no prefix added)
```
