# CR-382 — ダミーは、実績が始まる所に立つ（予定の開始日・同じ日のマイルストーン・実績を隠したとき）

> **閉じるもの**: 利用者の申し立て 2026-09-15 の A・G・H と、その答え —— 逐語は `docs/development-records/rulings.md` の各行。
> - `JDG-83`（A）: 実績を非表示にしたとき、実績のダミーも非表示とせよ。台帳 `DFC-590`
> - `JDG-89`（G）＋ `JDG-100`: タスクのダミーの開始位置を実績の開始位置に合わせろ。これまでの裁定を覆す。台帳 `DFC-596`
> - `JDG-90`（H）＋ `JDG-101`: マイルストーンのダミーも予定と同じ日に。予定と実績のマイルストーンが同じ日にあるときの表示を肯定し、実績がダミーになっただけとする。台帳 `DFC-597`
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `87861cf3`。`src/` と `tests/` には触れていない。
> ⭐ 3 件を 1 本にしたのは、3 件とも 表 T-240 を書き換え、G と H は 表 T-023d の `GR-9` / `GR-18` と結びの境目の段、`FR-043` の段を同時に書くからである —— `induced.py` が `FR-043 ↔ GR-17 ↔ GR-9` の閉路を返したので、1 度で書いた。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`（描いたものを直接掴んで日程を変えられる）**。未着手のタスクで実績を入れる印を、実績が始まる所に立て（G）、マイルストーンでは同じ日の実績と同じ姿にし（H）、実績を隠したときは印も隠す（A）。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— G で予定の開始日に立てると、表 T-240 の `DM-1` の MUST NOT（同じ点に 2 つの掴みシロ）と衝突するように見える。表 T-023d の結び「境目はバーの端。予定の端点は端の外側だけ、実績側は内側」（`JDG-35`）で `GR-3` と `GR-9` は構造から分かれる ⇒ **矛盾は無い**（決定 2）。
- **`R1.3`** —— H で予定の図形の日に立てると、結びの境目の段（左は `GR-3`、右はダミー）がマイルストーンのダミーの左半分を掴めなくする ⇒ `GR-18` を境目の段から外し、`GR-15` と同じ扱いにした（決定 4）。
- **`R1.6`（網羅）** —— `FR-049` の一般則は `S-228` の要素に実績のダミーを数えるかを述べていない ⇒ `DM-14`。
- **`R4`（状態）** —— `FR-049` の「独立とは片方を読む所がもう片方を読まないこと」⇒ ダミーは `S-228` だけを読み、`S-227` を読まない（`DM-14` の MUST NOT）。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**: `JDG-83`・`JDG-89`・`JDG-90`・`JDG-100`・`JDG-101`。

**問うこと**: なし（0 件）。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| 決定 1 | 未着手のタスクのダミーは **予定の開始日**に立てる | `JDG-100` の読みの欄が前に立つ者の推奨として示し、「変更要求が矛盾を見つけない限り推奨どおり」とした。決定 2 と 3 で矛盾が無いことを確かめた |
| 決定 2 | **`GR-3` と `GR-9` が同じ日に立つとき、優先の順は動かさない**。境目の規則（結び）で分かれる | 結び「境目はバーの端であること（MUST）。端の外は予定、端の内は実績」「予定の端点の掴み代は端の外側だけ」（`JDG-35`）。ダミーの印は日の列の左端に揃い（`DM-3`）予定の開始日の列の中に立つ ⇒ 端の内。`GR-3` の掴み代は端の外 ⇒ 同じ画素を争わない。本表の順も既に `GR-17` / `GR-9` / `GR-18` が `GR-3` より上。⭐ `JDG-04` / `JDG-19`（予定の開始日の位置が境目、左は予定・右は実績）はそのまま成り立つ ⇒ **`JDG-04` は覆さない**。覆れたのは「翌日」（`DFC-56`）と、それを持っていた `GR-9` / `DM-1` / `DM-12` の条項だけである |
| 決定 3 | 「終了優先」（`JDG-17` / `JDG-20`、表 T-244 の `TE-1`〜`TE-4`、結びの「2 つの端点が同じ日に立つとき」）は **1 文字も変えない** | ダミーの 2 端（`GR-9` / `GR-17`）は印を中央で割る（`TE-3`）。立つ日が 1 日動いても割り方も順も同じ |
| 決定 4 | **`GR-18` を境目の段から外し、`GR-15`（同じ日の実績のマイルストーン）と同じものを掴む** | `JDG-101`「実績が実績のダミーになっただけ」。ダミーは予定の図形の日に中央で立つので、境目で割ると左半分が `GR-3` 側になり、同じ日の実績のマイルストーンの掴み方（図形そのもの、`GR-12` より上）と違ってしまう。⭐ `JDG-05`「つかみ判定も実績と合わせろ」と整合する（本表の順でも `GR-18` は `GR-12` より上に既に在る） |
| 決定 5 | ダミーは **`S-228` を切ったら描かず、占有はそのまま残し、`S-227` は読まない**。描かないダミーは掴めない（隠した実績と同じ扱い。`JDG-112`、規則は `FR-049`、`CR-380` の決定 6）。実績の入力はプロパティパネルか、`S-228` を戻してから | `FR-049` の一般則（表示可否は描く要求の状態条件に加わる／隠した要素の操作は別の面から）、同要求の独立の MUST NOT、`FR-011` の「プロパティパネル（`FR-006`）の実績の欄から直接入れられる」、`JDG-34`（逃げ道は予定のみ／実績のみ表示） |
| 決定 6 | 表 T-064 の `PI-1` の `nextWorkingDay` の注から「`FR-043` の掴みシロが立つ日」を外した（メンバは残す） | 注が偽になる。メンバを退けるかは、呼び手を数える実装の波が決める（`src/` を読むだけで決めない） |
| 決定 7 | 行 ID は `DM-14`（`DM-13` の次。`DM-11` は `CR-375` が退けた空き番号なので使わない） | 規則 02 の「番号を振り直さない」 |

---

## 1. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| `docs/spec` で「翌」を持つ行 | 14 か所（うちダミーの位置は 9 か所） | `grep -n 翌 docs/spec/*.md`。残る 5 か所は `FR-011` の翌暦日（2）、`FR-046` の翌日（2）、`RV-1` の翌暦日（1）で無関係 |
| 実績を隠してもダミーが残る | 4 件のうち 4 件 | `DFC-590` の実測を原典で読んだ |
| 表 T-240 の行 | 12 → 13 | `impact.py T-240` の「行: 12 行」 |

## 2. グラフ

**`impact.py T-240 DM-1 DM-12 GR-9 GR-17 GR-18 GR-15 GR-3 FR-043 FR-049 S-228 PI-1`**（300 行）: 表 T-240 は要求 1（`FR-043`）/ 2 次 8。`DM-1` / `DM-12` は参照 0。`GR-9` は要求 4 / 参照 27（`FR-003` `FR-043` `FR-016`、表 T-244）。`GR-18` は要求 3 / 参照 10。`FR-049` と `S-228` は互いに指す 2 つ組。
**`induced.py`**（`CR-380` と同じ 50 種）: 閉路 `FR-043 GR-17 GR-9`（大きさ 3）と `FR-049 S-228`（2、値の行と要求の作法）。⇒ `FR-043`・`GR-9`・`GR-17`（行は変えず、指す日だけ）を 1 本の書き換えスクリプトで 1 度に書いた。

**届いた行を `rulings.md` で引いた**:

| 行 | 裁定 | 読んだ結果 |
|---|---|---|
| `GR-9` / `GR-17` / `FR-043` | `JDG-04` `JDG-16` `JDG-29` `JDG-33` `JDG-36` `JDG-37` `JDG-41` `JDG-67` `JDG-69` | 境目（`JDG-04`）・1 つの印（`JDG-16`）・左右に割る（`JDG-37`）・印そのものが当たり判定（`JDG-41`）・掴み代の縦横（`JDG-36`）・離した日（`JDG-67` / `JDG-69`）はどれも位置の日に依らない ⇒ 衝突 0。`JDG-29` は既に覆された |
| `GR-18` / `GR-15` | `JDG-39` `JDG-05` | 1 か所（`JDG-39`）は据置。掴みも実績と合わせる（`JDG-05`）は決定 4 が満たす |
| `DM-4` | `JDG-85` `JDG-102` | 形だけを予定に合わせる。本書は `DM-4` に触れない |
| `S-228` | `JDG-34` | 逃げ道。決定 5 と整合 |

## 3. 当てた中身

| 対象 | 置き場（`01-04-requirements.md`） | 中身 |
|---|---|---|
| `FR-043` の値の段 | 「予定の開始日の翌稼働日」→「予定の開始日」（描く位置の規則であり、離したときに書く日ではない、は据置） |
| `FR-043` の前置きの MUST NOT | マイルストーンのダミーの位置を例外にしない（据置）＋ 予定の図形と同じ日に、同じ日の実績のマイルストーンと同じ大きさ・縦位置・重ね順で描く（MUST）。「図形の上に置くと掴む所が図形の一部に見える」の理由を外した |
| 表 T-240 `DM-1` | 予定の開始日（MUST）。翌稼働日へずらさない（MUST NOT）。`GR-3` とは端の内外で分かれる |
| 表 T-240 `DM-12` | ダミーは形状を問わず予定の開始日。マイルストーンは同じ日の実績と同じ姿 |
| 表 T-240 に `DM-14` | `S-228` を切っているあいだ描かない（MUST）、`S-227` で変えない（MUST NOT）、描かないダミーは掴めない、入力はプロパティパネルか戻してから |
| 表 T-240 の後の段 2 か所 | 開始点を確定させる日と、`GR-9` の行が残る理由の日を「予定の開始日」に |
| 表 T-023d `GR-9` | 予定の開始日に 1 日ぶん。`GR-3` とは端の内外で分かれる |
| 表 T-023d `GR-18` | 予定の開始日。押したら同じ日の `GR-15` と同じものを掴む（MUST）。「図形の上に置いてはならない」を外した |
| 表 T-023d の結び | 境目の段の対象から `GR-18` を外し、当ててはならない（MUST NOT）を 1 文。「翌稼働日は予定の開始日と同じ画素に寄る」を印の幅の話に。「ダミーを稼働日に置く」を「予定の開始日に立てる」に |
| 表 T-064 `PI-1`（`05-07-design.md`） | `nextWorkingDay` の注から `FR-043` を外した |

## 4. 数の予測

当てる前 `tables=149  figures=11  rows=1931  uids=153`（`md-checks.py` の最終行）。本書の差: tables 0 / figures 0 / **rows +1（`DM-14`）** / uids 0。同じ波の合計の予測は `CR-380` の第 4 節。

## 5. 影響 —— 本書の外で動くもの

| 所 | 何が動くか |
|---|---|
| `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts` の `dummiesOf` | 立つ日（`nextWorkingDay` をやめる）、`showActual` を読む、マイルストーンは図形の日に同じ日の実績の姿で |
| 当たり判定（`ItemHitArea`） | `GR-18` を境目の規則から外す |
| 試験 | `tests/unit/t-023d-dummy-stands-clear-of-the-plan-start.test.ts`、`tests/unit/fr-043-dummy-drawn.test.ts` の類が「翌稼働日」「図形の上に置いてはならない」を逐語で引いていれば赤くなる（実装の波で直す） |
| `docs/development-records/rulings.md` | `JDG-83` / `JDG-89` / `JDG-90` / `JDG-100` / `JDG-101` を `適用済`。`JDG-04` は覆さない（決定 2） |
| `docs/development-records/defects.md` | `DFC-590` / `DFC-596` / `DFC-597` を `実装待ち` |

## 6. ⛔ この変更でやらないこと

- ⛔ 表 T-023d の順を動かさない（決定 2・決定 4 とも既にある順で足りる）
- ⛔ 表 T-244（終了優先・印を中央で割る）を変えない
- ⛔ `DM-4`（ダミーの形）と `JDG-36` の縦幅を変えない
- ⛔ `src/` と `tests/` に触らない
