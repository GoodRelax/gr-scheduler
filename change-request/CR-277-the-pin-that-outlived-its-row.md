# CR-277 — 消えた行を指し続けたピン止め

> **閉じるもの**: ⛔ **赤い試験 3 件の裁定済みの原因**（`D-100`。`t-027-outside-the-history`）。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作った人の意図どおりに読める）。**⛔ **消えた行を指すピンは、画面のどこにも描きようが無い。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `UN-16` と `IV-3` | ⛔⛔ **2 つの MUST が正面からぶつかっていた。**`UN-16`（ピン止めは取り消しの対象外 ⇒ 持ち越せ）と `IV-3`（`05-07-design.md:724`。ピンした行は実在すること）。**どちらに従うかを述べる行がどこにも無い** |
| **`R2.19`**（驚かせない）| 再現手順 | ⛔ **行をピン止め → その行を作った編集を取り消す → 持ち越した識別子が消えた行を指す** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| 掃除の規則 | ⭐ **書かずに済んだ** —— **履歴の中に入れば `IV-3` は自動的に保たれる**（スナップショットのピンは、その時点で実在した行としか対応しない）|

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **ピン止めの行き先は `UN-14`（`UN-13` ではない）** | ⭐ **`UN-14` は `TaskGroup` の列を持つ行であり、`pinnedGroupIds` はまさにそれである** —— **畳みと非表示が同じ理由で既に `UN-14` に居る** |
| 2 | ⭐ **`watermarkVisible` には仕様を書かない** | ⛔ **`UN-7`（表示の切り替え）が既に対象外と定めている。**⚠️ **外れていた理由は「`VisibleElement` がこの名を持たない」という**実装側の読み**であって、仕様の言明ではない。**⇒ **直すのは `src/` である** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 赤い試験 | **3 件** —— `pinnedGroupIds`（`S-126`）× 2、`watermarkVisible`（`S-144`）× 1 |
| 機構 | `document-change-plan.ts:214-224` の `columnsOutsideHistory()` が持ち越す鍵を並べており、**その 2 つが入っていない** |
| ⭐ `src/` の `STOP` | ⭐ **正確に書いてあった。今回は疑う必要が無かった**（⛔ 前巡の教訓の逆の例として記録する）|
| ぶつかっていた 2 行 | `UN-16`（`01-04-requirements.md:1606`）と `IV-3`（`05-07-design.md:724`）|
| ⇒ 案 ③ の利点（前に立つ者が過小評価していた）| ⭐ **掃除の規則を 1 つも書かずに不変条件が立つ** |
| 代償 | ⚠️ **ピン止めとピン外しが取り消しの 1 段を積む**（`S-94` / `S-95` の上限に少し効く）|
| `induced.py`（`UN-14` `UN-16` `T-027` `FR-098` `IV-3` `S-126` `S-144` `UN-7`）| **閉路 1 つ: `UN-14` ＋ `UN-16`（大きさ 2）。**⇒ ⭐ **2 行を 1 度に書いた** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `UN-16` | ⭐ **ピン止めを落とし、行き先（`UN-14`）を名指しする** |
| 2 | `UN-14` | ⭐ **ピン止めを対象に加え、`IV-3` とぶつかっていたこと・履歴に入れば自動的に保たれること・代償を書く** |
| 3 | `A-appendix` の変更履歴 | 1 行（`1.29`）|

⛔ **`UN-7` も `IV-3` も 1 文字も動かさない。**
⛔ **`watermarkVisible` の仕様は書かない** —— **既に `UN-7` が持っている。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1718 / 148 | **同じ** | **0** —— **2 つのセルの中身を移すだけ** |

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1718 / 148 | **135 / 11 / 1718 / 148** | ✅ |
| 検査 22 本（**25 を含む**）| 全緑 | **ALL GREEN** | ✅ |
| 予測から外れたもの | — | **無し** | ✅ |

## 5. 標準規則への答え

- **①（目的に沿う）**: ⭐ `CH-1`。**消えた行を指すピンは描きようが無い。**
- **②（影響をグラフで測ってから書く）**: ⭐ `induced.py` の 2 者閉路どおり、`UN-14` と `UN-16` を 1 度に書いた。
- **⑧（実測を書く）**: ⭐ **§1 が実測である。**⭐ **`STOP` コメントが正確であった例として記録した** —— **規則 05 の「`STOP` を根拠にしない」は「必ず自分で当たる」であって「`STOP` は常に誤り」ではない。**

## 6. 残るもの

⛔ **本変更要求は仕様だけである。**⭐ **実装が要るもの**: `document-change-plan.ts` の
`columnsOutsideHistory()` から `pinnedGroupIds` を外し（履歴の中へ入る）、`watermarkVisible` を**加える**こと。
⭐ **`t-027-outside-the-history` の赤 3 件がこれで閉じる見込みである。**
