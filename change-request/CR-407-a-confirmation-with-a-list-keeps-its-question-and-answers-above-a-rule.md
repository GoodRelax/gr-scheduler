# CR-407 — 一覧を持つ確認は、問いと答えを線の上に置き、一覧だけを送る

> **閉じるもの**: 利用者の裁定 `JDG-158`（2026-09-17 の六通目と七通目）を仕様へ着地させる。逐語は `docs/development-records/rulings.md` の `JDG-158` が持つ。
> - 六通目: 「その考えていくと、タスク削除の一覧も ヘッダー部とタスク部の間に水平線を置き、」「ヘッダー部分に   ...削除しますか？ [Yes] [No] とせよ。」
> - 七通目: 「 行の削除の確認の新しい形を、一覧を持つ確認すべてにそろえる (推奨通り)」
>
> ⭐ **本書は仕様だけを当てる。** `FR-076` の 表 T-234 の後に新しい 表 T-258（`CQ-1`〜`CQ-4`）を立て、対象を「表 T-234 の `名前を挙げるか` が「挙げる」で始まる行の問い」と定め、表 T-206 に `S-242`（線の太さ 1px）を足した。読んだ木は `a422e16f`。
> ⚠️ `src/`・`tests/`・`tools/` は触っていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`**（マニュアルを読まずに使える、`CH-4`）—— いまは問いの文が名前の一覧と一緒に送られ、答えの入口が一覧の下にある。消える名前が多いと、何を問われているかと、どこで答えるかが画面から消える。表 T-037 の `NT-7`（何が起きるかを示したうえで、続けるか取りやめるかを選ばせる）が、一覧の長さに関わらず成り立つ。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（網羅）** —— 「一覧を持つ確認すべて」を、表 T-234 の `名前を挙げるか` の欄で数えた。挙げる行は `QN-1` / `QN-2` / `QN-3` / `QN-5` の 4 つ（第 2 節）。⭐ 行 ID を数え上げず、欄の値で対象を定めた —— 表 T-234 に行が増えても本表が追う（決定 1）。
- **`R1.3`（異なる節の暗黙の矛盾）** —— `Import Report`（`U-62`）も名前の一覧を持つが、`Confirmation` ではない（`U-62` の行が自ら述べる）。⭐ 対象から外すことを 1 文で書いた（決定 2）。
- **`R1.4`（境界値）** —— 挙げる名前が 1 つも無いとき（線も一覧も描かない、`CQ-4`）を書いた。
- **`R1.6`（否定要求は代替を示す）** —— 「見出し部をスクロールさせてはならない」に「スクロールするのは一覧だけ」を置いた。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 対象を「表 T-234 の `名前を挙げるか` の欄が「挙げる」で始まる行の問い」とする。いまは `QN-1` / `QN-2` / `QN-3` / `QN-5` の 4 つ | ⭐ 七通目「一覧を持つ確認すべてにそろえる」。⭐ 名前を挙げる問いが一覧を持つ問いである（`NT-7`「その名前を挙げること（MUST）」）。⭐ 規則 02 の流儀（数え上げると表に行を足すたびに 2 か所を直す —— `FR-006` の同じ理由）| `QN-5` は捨てる文書の名前 1 つだけでも、同じ形（線の下に 1 行）になる |
| 決定 2 | `Import Report`（`U-62`）は本表に従わない | ⭐ `U-62` の行「`Confirmation`（`U-55`）ではない —— 答えを求めない」。⭐ 七通目は「確認」と言っている | 名前を並べる 2 つの面の見た目が揃わない（第 9 節の問い 1）|
| 決定 3 | 見出し部に、問いの文と答えの入口（`Yes` / `No`）をこの順に並べる | ⭐ 六通目「ヘッダー部分に   ...削除しますか？ [Yes] [No] とせよ」 | 無し |
| 決定 4 | 線の太さを 表 T-206 の新しい行 `S-242`（1px、保存しない、画面の px）とする | ⭐ 六通目「水平線を置き」。⛔ 太さは利用者が選んでいない —— 入口の枠（`S-237`）と、担当者の一覧の罫（`S-241`、`CR-406`）と同じ 1px に揃えた。⭐ 値を散文に書かず行に置く（規則 02）| 利用者が選んでいない 1px が行に入る（行の注に書いた）|
| 決定 5 | 挙げる名前が 1 つも無いときは、線も一覧も描かない（`CQ-4`）| ⭐ 隔てるものが無い線は、隠れた一覧があるように読める。⚠️ 導いた条項である | 無し |
| 決定 6 | 別の行に表示されている旨の添え書き（`QN-1`）は、その名前と同じ行に置く（`CQ-3`）| ⭐ いまの実装と同じ（第 1 節）。一覧を送ると名前と添え書きが離れないため | 無し |

---

## 1. 読んで確かめた事実

| 事実 | 所（`a422e16f`） |
|---|---|
| 確認の面は `div`（`STOPPING_BOX` ＋ `display:flex;flex-direction:column`）に、`names`（問いの文 ＋ 名前の行、`flex:1 1 auto;min-height:0;overflow:auto`）と `answers`（`Yes` / `No`）をこの順で置く ⇒ 問いの文は名前と一緒に送られ、答えは一番下 | `src/framework/dom-screen-surface/dom-screen-surface.ts:1987`〜`:2018`、`:522`〜`:531`（読むだけ）|
| 名前の行は、別の行に表示されている旨の印（`confirmationMark`）を名前と同じ行に持つ | 同 `:1996`〜`:2008` |
| 問いの行は `QN-1`〜`QN-5` を使う（`QN-4` は名前を挙げない）| `src/framework/single-html-shell/frame-loop.ts:436`〜`:442`、`:1325` |
| `Import Report` は名前を並べ、入口は `OK` だけ | `dom-screen-surface.ts:1909`〜`:1934`、`docs/spec/_assets/tbl-glossary.md` の `U-62` |
| `T-258` / `CQ-` / `S-242` は `docs/` `src/` `tests/` `tools/` `.claude/` `change-request/` を引いて 0 件 | `grep -rn`（当てる前）|

## 2. 測った

**表 T-234 の `名前を挙げるか` の欄**（当てる前の `docs/spec/01-04-requirements.md:6014`〜`:6020` を読んだ）:

| 行 | 欄の頭 | 本表の対象 |
|---|---|---|
| `QN-1` 行を削除する前 | 挙げる | ○ |
| `QN-2` WBS の子孫を持つ `Task` を削除する前 | 挙げる | ○ |
| `QN-3` 担当者を消すことで解かれる割当があるとき | 挙げる | ○ |
| `QN-4` 同じとみなせない相手へ書き出そうとするとき | 挙げない | — |
| `QN-5` 未保存の編集を捨てて置き換えるとき | 挙げる | ○ |
| `QN-9` 透かしを消す前 | 挙げない | —（`U-60` は `Confirmation` でもない）|
| `QN-8` この問いにまだ行が無い | 挙げない | — |

⇒ 対象は **4 つ**。

## 3. グラフ

**当てる前**: `impact.py T-234 NT-7` —— 表 T-234 を指す要求 4（`FR-086` / `FR-020` / `FR-100` / `FR-076`）、`NT-7` を指す要求 6（`FR-031` / `FR-016` / `FR-020` / `FR-080` / `FR-096` / `FR-076`）。
**`induced.py`**（24 種、`CR-405` の第 3 節と同じ実行）: 本書の員（`FR-076` `T-234` `NT-7` `QN-1`）は閉路に入らない。
**届いた行を `rulings.md` で引いた**（`NT-7` / `QN-1` / `T-234` / `確認`）: 当たったのは `JDG-158` だけ ⇒ 衝突 0。

## 4. 当てた中身

| 対象 | 置き場（当てたあと） | 中身 |
|---|---|---|
| `FR-076` の段 | `docs/spec/01-04-requirements.md:6049`〜`:6050` | 対象（表 T-234 の「挙げる」の行の問い）は 表 T-258 のとおりに組む（MUST）。`U-62` は従わない |
| 表 T-258 | `:6052`〜`:6059` | `CQ-1` 見出し部 ／ `CQ-2` 水平線 ／ `CQ-3` 一覧 ／ `CQ-4` 挙げる名前が無いとき |
| `S-242` | `docs/spec/_source/settings.json`（→ `_assets/tbl-settings.md:364`）| 見出し部と一覧を隔てる線の太さ、1px |
| 接頭辞 `CQ` | `docs/spec/_source/row-id-prefixes.json`（→ `_assets/tbl-row-id-prefixes.md`）| Confirmation with a list |

## 5. 数の予測

本書の差: **tables +1**（T-258）／ figures 0 ／ **rows +5**（`CQ-1`〜`CQ-4`、`S-242`）／ uids 0。4 本の合計の予測と実測は `CR-405` の第 5 節。
⭐ 本書が足した `（MUST）` は 5、`（MUST NOT）` は 1（前の段 1、`CQ-1` 1 と MUST NOT 1、`CQ-2` 1、`CQ-3` 1、`CQ-4` 1）。

## 6. 影響

### 6.1 仕様

| 所 | 本書で |
|---|---|
| 表 T-037 の `NT-7`（答えの語・`y` / `n` の打鍵）| そのまま —— 置き場だけを 表 T-258 が持つ |
| 表 T-023 の後の段（面が立っているあいだのホイールはブラウザへ）| そのまま —— 一覧の中は素のホイールで送れる |
| `FR-032` / `FR-099`（名前で示す義務）| そのまま |

### 6.2 記録

| 所 | 書いたこと |
|---|---|
| `docs/development-records/rulings.md` の `JDG-158` | 状態を 適用済 へ、着地先に本書と行 ID を足した |
| `docs/development-records/changelog.md` | 本書の 1 行 |

### 6.3 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `npm run gen` | `S-242` を読む側へ刷る |
| `src/framework/dom-screen-surface/dom-screen-surface.ts:1987`〜`:2018` | 面を「見出し部（問いの文 ＋ `answers`）→ 線（`S-242` px）→ 一覧（`overflow:auto`）」の順にする。名前が 0 のときは線と一覧を置かない |

## 7. 試験が主張すべきこと

⭐ 名前は本書のとおりに写すこと: 表 T-258（`CQ-1`〜`CQ-4`）、`S-242`（1px）。

1. `CQ-1`: `QN-1` の確認（消える `Task` を 1 画面を超える数だけ持つ行を削除する）で、問いの文と `Yes` / `No` の入口が、名前の一覧より上にあり、一覧を最後まで送っても画面上の位置が変わらない。⭐ 古い実装（問いが一覧と一緒に送られ、答えが一番下）を赤にする。
2. `CQ-2`: 見出し部と一覧のあいだに横いっぱいの線があり、太さが `S-242`（1px）。
3. `CQ-3`: 面の中でスクロールできる要素は一覧だけ。別の行に表示されている名前の添え書きは、その名前と同じ行にある。
4. `FR-076` の段: `QN-2`・`QN-3`・`QN-5` の確認も同じ形である。`QN-4` の確認（挙げない）には線が無い。
5. `CQ-4`: 挙げる名前が 0 の問いが立ったとき、線も一覧も描かれない。
6. `FR-076` の段: `Import Report`（`U-62`）の形は本書で変わらない。
7. 表 T-037 の `NT-7`: `y` / `n` の打鍵で答えられることは変わらない。

## 8. 関係 —— `JDG-158`

- ⭐ **`JDG-158` を着地させる** —— 「ヘッダー部とタスク部の間に水平線」は `CQ-2`、「ヘッダー部分に ...削除しますか？ [Yes] [No]」は `CQ-1`、スクロールするのはタスクの一覧だけ（読みの欄）は `CQ-3`、七通目の「一覧を持つ確認すべて」は `FR-076` の段（表 T-234 の「挙げる」の行）。

## 9. ⛔ この変更でやらないこと ／ 返す問い

- ⛔ 問いの文（辞書の `questions`）と答えの語（`confirmation`）を変えない。
- ⛔ `src/`・`tests/`・`tools/` を編集しない（第 6.3 節）。

**返す問い**（どれも推奨で当ててある）:

1. **`Import Report`（取り込みが落とした `Task` の名前、入口は `OK`）も同じ形にするか**（決定 2）—— 推奨: いまはしない（確認ではない）。そろえるなら `FR-023` に 表 T-258 を指す 1 文を足すだけで済む。
2. **線の太さ 1px でよいか**（決定 4）—— 利用者は太さを指定していない。
