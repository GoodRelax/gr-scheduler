# CR-398 — ある表示の倍率での各部の矩形を、`ScreenRegions` が答える

> **閉じるもの**: 表 T-064 が、`ScreenRegions` の公開する名前を 1 つ載せていない。
> - `FR-039` は、表示の倍率を変えたとき `Row Area` の横の中点が指す日付と、縦の中点が指す行を動かさないと定める。横は `a2402a5c` で着地した。⛔ **縦は着地していない** —— 台帳の `DFC-622` がその行である。
> - 縦を留めるには「ある表示の倍率での各部の矩形」が要る。それを答える名前はコンポーネントの境を越えるので、表 T-064 が名乗らねばならない（検査 26b）。
>
> ⭐ **本書は 表 T-064 のセルだけを当てる。** 実装は別に続く。読んだ木は `a2402a5c`。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`**（読む人が自分の見え方を選べる）—— `FR-039` の ORIGIN がこれである。表示の倍率は読む人が選ぶ大きさであり、⛔ **選ぶたびに見ていた場所が画面の外へ流れるなら、選べるとは言えない。** 同要求が中点を留めると定めるのはそのためであり、縦がまだ留まっていない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R2`（公開インターフェース）** —— 表 T-064 は 36 コンポーネントの公開する名前の**全数**を名乗る（5.3）。ある倍率での矩形を `ScreenRegions` が答えるなら、その名前は `Adapter` の `InputCommandTranslator` から読まれて境を越えるので、載せねばならない。⚠️ 検査 26b が両方向で見ている。
- **`R4`（DRY）** —— 各部の矩形の組み立ては `regionsFromScreen` が 1 つだけ持つ。⛔ **呼ぶ側が倍率から矩形を算で求めると、同じ組み立てが `src/` の 2 か所に立ち、片方だけが直されて必ず離れる**（1.9）。`Row Area` の縦の置き場は描いた目盛の帯の高さ（表 T-252 の `DS-1`）に乗っており、算では追えない。
- **`R1.2`（実装可能性）** —— `DFC-622` は道を 2 つ挙げる。① `ScreenRegions` に公開名を足す ② `InputContext` に `ScreenEnvironment` を足す。⭐ **①を採った** —— 下の決定 1。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | ある倍率での矩形を `ScreenRegions`（`PI-35`）が公開し、名前を `regionsAtDisplayScale` とする（`DFC-622` の対応案①） | ⭐ 組み立ては既に本コンポーネントの `regionsFromScreen` が持ち、倍率から描く比を出す `displayRatioOf` と、比を掛けた設定値を返す `drawnSettingsOf` も本コンポーネントに在る。⇒ 足す 1 本は既に在るものの上に立つ。⛔ 対応案②（`InputContext` に `ScreenEnvironment` を足す）は、`Adapter` の持つ入力の文脈に `Framework` が測った画面の寸法を通す道を新たに開き、境を越える名前が 1 つではなく型ごと増える | 呼ぶ側が矩形を 2 度得る（変える前と変えたあと）。⚠️ どちらも `layoutEngine` の同じ 1 本から出るので `LR-2` に反しない |
| 決定 2 | 表 T-064 のセルに**引数と戻り値を書かない**。⭐ 何を与えるかは「与えた表示の倍率で」とだけ述べ、署名は `src/` の公開エントリが持つ | 同表の結び「本表は名前と、それが何を担うかだけを持つ。引数・戻り値は `src/` の公開エントリが持つ」（5.3）。⭐ 先例は同行の `regionsFromScreen` であり、「画面の寸法と `DocumentSettings` から各部の矩形を出す」とだけ書いて署名を書いていない | 署名を知りたい読み手が `src/` を 1 度見る |
| 決定 3 | 中点が何を指すかの規則を**本書は 1 文字も足さない**。⭐ それは `FR-039` が既に持つ | ⛔ 同要求は「`Row Area` の横の中点が指す日付と、縦の中点が指す行を動かさないこと（MUST）」と既に定め、続けて「その倍率での行の位置は 表 T-064 の `PI-5` の `rowPlacesAtZoomY` に問い、倍率から算で求めてはならない（MUST NOT）」と定める。⇒ **規則は足りており、足りないのは矩形を問う道だけである** | 無し |
| 決定 4 | セルに `（MUST）` / `（MUST NOT）` の印を足さない | ⚠️ 検査 39 は、印の直前 28 字が `tests/` に逐語で無い条項を数え、`must-clause-coverage-baseline.txt` を超えると赤にする。⛔ **新しい印は必ず未押さえとして数えられ、基準線を 1 つ超える。** ⭐ 禁止は ⛔ の印と平叙文で書く —— `_assets/tbl-settings.md` の `S-138` の注が同じ形を既に採っている | 機械が押さえる条項が 1 つ増えない |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| `FR-039` は「表示の倍率を変えたとき、`Row Area` の横の中点が指す日付と、縦の中点が指す行を動かさないこと（MUST）」と定め、続けて行の位置を `PI-5` の `rowPlacesAtZoomY` に問えと定める | `01-04-requirements.md` の `FR-039`（`:6153` の後段） |
| `Row Area` の縦の置き場は描いた目盛の帯の高さのぶん下がり、高さもそのぶん縮む。<br>帯の高さは 表 T-252 の `DS-1` が掛ける行である | `src/entity/layout-engine/screen-regions/screen-regions.ts` の `regionsFromScreen`（`rowAreaY = canvas.y + bandHeight`、`bandHeight = drawn.rulerHeight`） |
| 各部の矩形を組み立てるのは `regionsFromScreen` の 1 本だけであり、それは画面の寸法と `DocumentSettings` を受け取って `drawnSettingsOf` を通す | 同上 |
| ⛔ 倍率を変える側は変更前の矩形しか持たない —— `displayScaleWrites` が `context.regions`（変更前）の中点を採り、同じ `context.regions` を `rowPlacesAtZoomY` へ渡す | `src/adapter/input-command-translator/input-command-translator.ts` の `displayScaleWrites` |
| 縦が未着手であることと、道が 2 つ（公開名を足す／`InputContext` に `ScreenEnvironment` を足す）あることは台帳の `DFC-622` が持ち、状態は `仕様待ち` である | `docs/development-records/defects.md` の `DFC-622` |
| 表 T-064 が「名前と、それが何を担うかだけを持つ」ことと、引数・戻り値を `src/` が持つことは同表の直前が定める | `05-07-design.md:413-414` |
| ⭐ `PI-35` を裁いた行は 0 件 —— `rulings.md` を `ScreenRegions` / `PI-35` / `regionsFromScreen` / `ScreenEnvironment` / `InputContext` / `中点` で引いて 0 件だった。<br>表示の倍率を裁いた `JDG-131` は `S-79` / `S-37` / `S-1` に倍率を掛けるかどうかの答えであり、既に `CR-390`〜`CR-395` で着地している | `docs/development-records/rulings.md` |

## 2. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| 当てる前の検査 | **ALL GREEN** | `npm run gen`（exit 0）のあと `.claude/skills/spec-graph-check/check.sh`（exit 0）を通した |
| 表 T-064 が名乗り、`src/` に届いているメンバ | 138 | 検査 26b の実測（`0 known gap(s)`、`new 0`、`8 piece(s) not covered`） |
| 足したメンバ | 1（1 行のセル） | `PI-35` に 1 つ。<br>行は増えない |
| 足した `（MUST）` / `（MUST NOT）` | 0 | 決定 4 |

## 3. グラフ

**`impact.py PI-35 FR-039 DS-1`**: 76 行。⭐ **`PI-35` を指す箇所は 0 件**（その行が指す先だけがある）⇒ セルを直しても、そこを参照している条項は 1 つも無い。`DS-1` を指すのは `FR-085` / `FR-016` / `FR-017` / `FR-039` の 4 件で、⛔ **本書はその 4 件のいずれも編集しない**（指すだけである）。`FR-039` は 16 種 38 箇所から指されており、⛔ **本書は `FR-039` を 1 文字も動かさない。**

**`induced.py T-064 PI-35 PI-5 FR-039 DS-1 DS-4 T-252`**: 7 種すべて解決、辺 7、**閉路 0** ⇒ 種のあいだに循環が無く、1 つずつ書ける。⭐ **本書が書くのは 7 種のうち 1 つ（`PI-35` のセル）だけである。**

**届いた行を `rulings.md` で引いた**: 第 1 節のとおり 0 件（`JDG-131` は別の軸で着地済み）⇒ 衝突 0。

## 4. 当てた中身

| ID | 置き場 | 中身 |
|---|---|---|
| 表 T-064 の `PI-35` | `05-07-design.md` | 決定 1・2・3・4 —— `regionsFromScreen` の直後に `regionsAtDisplayScale` を 1 つ足した |

⭐ **置いた順の理由**: `regionsFromScreen` と同じものを答える 1 本なので隣に置いた。⚠️ 末尾の `rowControlLatticeFloorPx` に付く ⭐ / ⛔ / ⚠️ の注は同メンバのものなので、そのあいだに割り込ませていない。

## 5. 数の予測

当てる前 `tables=151 figures=11 rows=1962 uids=153`。本書の差: tables 0 ／ figures 0 ／ **rows 0**（表 T-064 は行を増やさず、セルを 1 つ直しただけである）／ uids 0 ⇒ 予測 `tables=151 figures=11 rows=1962 uids=153`。

## 6. 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `src/entity/layout-engine/screen-regions/screen-regions.ts` | `regionsAtDisplayScale` を公開エントリから export する。<br>⛔ **これが着地するまで検査 26b は赤い** —— 表 T-064 が名乗る名前が `src/` に無いからである |
| `src/adapter/input-command-translator/input-command-translator.ts` | `displayScaleWrites` が、変更前の `context.regions` ではなく、変えたあとの倍率での矩形を使って縦の中点を留める（台帳の `DFC-622`）。<br>⚠️ 行の位置は引き続き `PI-5` の `rowPlacesAtZoomY` に問う —— `FR-039` が算で求めることを禁じている |
| 試験 | `DFC-622` は「試験はまだ無い」と記す。<br>⛔ 本書は試験を書かない |

## 7. 試験が主張すべきこと

1. 表 T-064 の `PI-35` が名乗る名前が、`ScreenRegions` の公開エントリから読める（検査 26b が回す）。
2. 与えた表示の倍率での `Row Area` の縦の置き場と高さが、その倍率で描いた目盛の帯の高さ（表 T-252 の `DS-1`）に従う。<br>⭐ 倍率の段を変えると矩形が動く。
3. 表示の倍率の段を 6 つとも変えたとき、`Row Area` の縦の中点が指す行が変わらない（`FR-039`）。<br>⚠️ 横の中点が指す日付が変わらないことは `a2402a5c` で既に主張されている。

## 9. ⛔ この変更でやらないこと ／ 返した問い

- ⛔ **縦の中点を留める算そのものを書かない** —— それは実装であり、⚠️ **規則は `FR-039` が既に持つ**（決定 3）。本書が足すのは矩形を問う道の名前だけである。
- ⛔ `FR-039` と 表 T-252 を動かさない —— 第 3 節のとおり 38 箇所から指されており、本書は指すだけである。
- ⛔ 表 T-064 の `PI-5` を動かさない —— `rowPlacesAtZoomY` は既に在り、`FR-039` がそれに問えと定めている。足りないのは矩形の側だけである。
- ⛔ 台帳（`docs/development-records/defects.md`）の `DFC-622` を編集しない —— **本体の持ち場の外である。** ⚠️ 同行の状態を `仕様待ち` から動かすのは、前に立つ者か実装の体である。
- ⛔ `InputContext` に `ScreenEnvironment` を足す道（`DFC-622` の対応案②）を採らない —— 退けた理由は決定 1 に書いた。
- ⛔ 変更履歴（`docs/development-records/changelog.md`）に行を足さない —— **本体の持ち場の外である。**<br>⚠️ 手順 02 の 7 は履歴を 1 行足せと定めるので、**前に立つ者が足すこと。**
- ⚠️ **問い 1（前に立つ者へ）**: **本書を読んだ木（`a2402a5c`）に `DFC-622` の行が無い。** ⭐ 同行は `refactor` の先端（`16c92b42`）で開かれており、⛔ **本書の作業指示はその 1 つ前を先端と呼んでいた。** 本書は `16c92b42` の同行を読んで書いたので中身は正しいが、⚠️ **本書を当てた木に台帳の行が無い状態が残る** —— 統合の際に `16c92b42` を含めること。
