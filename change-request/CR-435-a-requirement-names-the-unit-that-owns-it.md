# CR-435 — 要求が、開くべきユニットを名指す

> ⭐ **状態: 適用済（2026-09-21）。**
>  ⚠️ **段階的に埋めている** —— 121 件のうち **99 件**の起点を書き、1 件（`FR-092`）は起点のユニットを
> 持たないので 5.3 の結びが名指し、**21 件は起点をまだ決めていない**（6.2、7.1）。
>
> **当てる直前に測り直した識別子**（規則 02 の 2.5。
> ⭐ **どれも 2026-09-21 の実測であり、当てたので、いまは履歴である**）:
> - 表 **`T-277`** —— 当てる直前、`docs/spec` が見出しで定義していた表は 167 個で、
>  `T-276` 以降の番号はどれも使われていなかった。
>  `T-277` は `docs/spec` のどこにも 1 度も現れなかった（0 件）。
>  ⭐ **いまは本書が定義している。**
> - 行 ID の接頭辞 **`OW`**（OWner）—— 当てる直前、`docs/spec/_source/row-id-prefixes.json` に
>  並ぶ登録は 159 行であった。
>  `OW` はそこに無く、`OW` を頭に持つ登録も、`OW` が頭に持つ登録も 0 行であり、
>  `docs/spec` の `OW-n` も 0 件であった。
>  ⚠️ 1 字違いの登録済みは `OC` ・ `OP` ・ `OR` の 3 つある
>  （`CR-430` の決定 2 が 1 字違い 14 を承知で `HT` を採った前例の範囲内である）。
>  ⭐ **いまは本書が登録している。**
> - ⛔ 表 `T-276` ・ 接頭辞 `UD` ・ 要求 ID `FR-112` は取らなかった ——
>  別セッションの `CR-432` が予約している。
> - ⭐ **新しい要求 ID は 1 つも立てていない。**
>  本書が足したのは設計の表と欄だけである。
>
>  読んだ木: `a35cf547`（1 節から 9 節の行番号と数はこの木のものであり、測った日は 2026-09-21 である）。
>
> **本書が変えたもの**: `docs/spec/05-07-design.md` の 5.3（表 T-075 に欄を 1 つ、表 T-277 を新設、
> 結びの段を 1 つ）と `docs/spec/_source/row-id-prefixes.json`（接頭辞 `OW`）。
> ⛔ **`src/` と `tests/` は 1 文字も変えていない**（8 節に所だけ書く）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⛔ **どの `CH-` も直接には進めない。  
本書は造りの土台であり、画面に出る振る舞いを 1 つも変えない。**  
黙っているより、そう書くほうが正直である。

⭐ **受けているのは利用者の目的そのものである**（2026-09-21 の一通、逐語）——「目的はとにかく変更に強くすること。 変更要求に対してAIによるSW修正が容易になること。 AIによるSW修正の時間を削減できること。 このためSW工学的に正しい設計/実装が必要だろ？」

その目的に最も近い既存の目標は **`GL-006`**（`CH-4` の受ける目標）だが、あれは**人**がマニュアルを読まずに到達することを言っており、**AI が変更要求からファイルへ到達すること**は言っていない。  
⇒ 本書は `GL-006` を名乗らず、**上の逐語を宿主とする。**

### ② レビュー観点のどの条項を当て、何が出たか

- **`R2.2`（SRP・MUST。  
`docs/development-rules/07-review-standards.md:508`）** ——
  逐語「SRP: 1ユニット・モジュール・コンポーネント・層が単一の変更理由を持つ」。
  ⭐ **「単一の変更理由」は、要求の側から引けて初めて検査できる。**
  いまの木は逆向きの欄しか持たない（1 節）ので、`R2.2` はユニットの水準では**人が読んで判定するしかない**。
- **`R2.2b`（上位水準の責務・MUST。  
`:510`）** —— 逐語「一文の要約で言えず、下位の列挙（総和）でしか語れない水準は、固有の責務を持たない単なる入れ物である」。
  ⇒ 表 T-075 の `UF-10`（`05-07-design.md:347`）の責務は逐語「集約ごとの 8 ファイルを束ねて公開する」であり、**これは総和である**。
  ⭐ **本書の `OW-3` は、その総和を「入れ物」ではなく「入口」として正当化する形を与える**（4 節）。
- **`R2.2c`（適用時期・MUST。  
`:511`）** —— 既存の違反は段階的に是正してよく、未是正を理由に合否を変えない。
  ⇒ ⭐ **本書は既存のユニットを 1 つも割らない。**  
  割る話は別の変更要求である（9 節）。
- **`R1.3`（矛盾・重複がない。  
唯一の正がある）** —— 表 T-062 の「正」の欄と 表 T-075 の「責務」の欄が、
  **同じ関係を 2 つの粒度で、どちらも穴だらけに持っている**（1 節）。  
  本書はこれを 1 か所へ寄せる。
- **`R1.5`（MECE）** —— 「要求がユニットを持つ」を 1 対 1 で書くと、層を跨ぐ要求が全部こぼれる。
  ⇒ 区分を 4 つ立て、どれか 1 つに必ず入ることを検査で閉じる（4 節・6 節）。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 関係を **表 T-075 の新しい欄**に置き、要求の側（Chapter 1〜4）には 1 文字も書かない | ⭐ 表 T-075 だけがユニットとファイルの道を持ち、**検査 18 がその道を `src/` と突き合わせている**（3 節）。<br>要求の側に書くと、道を引くためにどのみち 表 T-075 へ join することになる | 向きが「ユニット → 要求」になるので、要求から引くには反転が要る。<br>⇒ 6 節の検査が全域性を保証して、反転を安全にする |
| 決定 2 | 欄に書くのは **起点だけ**とし、「この要求に触るファイル全部」は書かない | ⭐ 全部を書くと 87 要求 × 平均 3.52 ファイルで約 300 の結び目になり、**腐る**。<br>⭐ 触るファイルの集合は `src/` の注釈が既に持っている（1 節の実測 —— 87 要求が `src/` に名を持つ） | 「ほかにどこが動くか」は本欄からは読めない。<br>⇒ そこは `impact.py` と grep の仕事のままである |
| 決定 3 | 区分を **4 つ**（`OW-1` 単独 ／ `OW-2` 連鎖 ／ `OW-3` 群 ／ `OW-4` 横断）とし、`OW-2` `OW-3` `OW-4` は複数のユニットが関わることを認める | ⭐ 実測（5 節）—— 87 件のうち単独で足りるのは 40 件しかない。<br>⛔ **1 対 1 を強いると、残り 47 件について嘘を書くことになる** | 区分の判定に人の読みが要る。<br>⇒ だから区分は**表の行 ID で名乗らせ**、後から行ごとに覆せるようにする |
| 決定 4 | 検査は **`tests/contract/` の契約試験**とする（⚠️ **基準線については 6.2 が利用者の裁定で書き換わり、未記入の件数のラチェットを 1 つだけ置く**） | ⭐ `CR-431` が同じ形で先例を作っている（`change-request/CR-431-…md:163` の「検査が読むもの」）。<br>⭐ 全域性の主張は基準線と相性が悪い —— 基準線は「いまの穴の数」を許す道具であり、**全域でないなら関係として使えない** | 当てる巡で 121 件を一度に埋めることになる（7 節に数、11 節に返す問い） |
| 決定 5 | 接頭辞の字は **`OW`**（OWner） | 登録簿のどの行とも一致せず、どれも `OW` で始まらず、`OW` もどれかで始まらない（1 字の接頭辞として `O` は登録されていない）。<br>⚠️ 1 字違いの登録済みは `OC` ・ `OP` ・ `OR` の 3 つある | 1 字違いが 3 つある。<br>⚠️ `CR-430` の決定 2 は 1 字違い 14 を承知で `HT` を採っており、前例の範囲内である |

---

## 1. 読んで確かめた事実（`a35cf547` で、自分で数えた）

### 1.1 ⛔ 渡された主張の突き合わせ

| # | 渡された主張 | 測った結果 | 判定 |
|---|---|---|---|
| (a) | `FR` は 87 件、1 ファイルだけが名指すのは 19 件（22%）、1 件あたり平均 3.52 ファイル | **87 ／ 19 ／ 21.8% ／ 3.52** | ⭐ **再現した** |
| (b) | `GA` は 22 件、1 ファイルだけが名指すのは 0 件、平均 3.82 | **22 ／ 0 ／ 0.0% ／ 3.82** | ⭐ **再現した** |
| (c) | `UF` は 68 件、1 ファイルだけが名指すのは 66 件（97%）、平均 1.03 | **68 ／ 66 ／ 97.1% ／ 1.03** | ⭐ **再現した** |
| (d) | 第三者レビューの逐語「1 つの要求を読み切るには、中央値で 19 か所へ跳ぶ」 | `docs/review/third-party-review-2026-09-10.md:227` に逐語で在る。<br>⚠️ **同じ表は要求を 99 件と数えている**（`:220`）—— 本日の木は `FR` 108 件 ＋ `NFR` 13 件 ＝ **121 件**である。<br>レビューは 2026-09-10 の木を読んでいる | ⚠️ **逐語は成り立つ。<br>数は当時のものである** |
| (e) | 同レビューの結論「`E1`〜`E4` が示したのは、ファイル数が原因ではないということである」「高くつくのは… 判断あたりの読む量である」 | `:215`・`:217` に逐語で在る | ⭐ **成り立つ。<br>⛔ 本書はファイルを増やさない** |

⭐ **(a)〜(c) は測り方まで同じである** —— `src/**/*.ts` の全文（注釈を含む）を読み、`\b(FR|NFR|GA|UF)-\d+[a-z]?\b` に当たったファイルを ID ごとに集めて数えた。  
読み込みは CRLF を LF に正規化している。

### 1.2 ⭐ 本書が新しく測った 5 つ

| # | 測ったもの | 実測（2026-09-21） |
|---|---|---|
| (f) | `docs/spec` の要求の全数（`**UID**:` の行） | **121**（`FR` 108 ＋ `NFR` 13） |
| (g) | 表 T-062 の「正」の欄が名指す要求 | **42 件**（121 件の **34.7%**）。<br>うちちょうど 1 コンポーネントが名乗るのは **30 件**、2 つ以上が名乗るのが **12 件**、**79 件はどのコンポーネントからも名指されていない** |
| (h) | 表 T-075 の「責務」の欄が要求を名指している行 | **68 行のうち 12 行**。<br>名指された要求は **24 件**で、うち 1 行だけが名指すのは 21 件 |
| (i) | 表 T-075 の行が、`docs/spec` のどこかから名指されている数 | ⛔ **68 行のうち 12 行だけ。<br>56 行はどこからも名指されていない** |
| (j) | 上の (g)(h) のどちらかが既に半分決めている要求 | **121 件のうち 48 件**。<br>残り **73 件**は手がかりが 1 つも無く、うち 11 件は `NFR` である |

⇒ ⭐ **(i) が本書の核心である。**  
 表 T-075 はグラフの葉であり、**要求から入ってくる辺が無い。**  
`impact.py` に `UF-63` を渡すと、道具そのものがこう言う —— 逐語「**指している箇所が無い。** 行が浮いている可能性がある。」

### 1.3 ⛔ 「覆う条項が無い」と書く前に打った 3 つ（規則どおり）

1. **`grep` `docs/development-records/rulings.md`** ⇒ 表 T-075 を名指す裁定は **1 件**、`JDG-48`（`:125`）。
   逐語「**「ユニット（ファイル）の一覧は必要になり次第毎回CRを作成しろ。 CR作成段階でそのファイルの妥当性を評価したほうが良いだろう？」**」。
   ⭐ **これは本書を支持する** —— 一覧は仕様に残り、足すたびに変更要求で妥当性を評価する。  
   ⛔ 要求との結びについては何も言っていない。
2. **`python .claude/skills/spec-graph-check/impact.py UF-63 SU-3`** ⇒ `UF-63` は指されている箇所 0 件。
   `SU-3`（表 T-074 のユニットの定義）も参照 1 件のみ（`05-07-design.md:461`）。
3. **ほかの道で同じものに届くか** ⇒ ⭐ **半分だけ届く。  
⛔ だから「条項が無い」とは書かない。**
   条項は在る —— `docs/spec/05-07-design.md:101`（逐語）:
   「**コンポーネントを分ける基準は 1 つである** —— 同じ表・同じ要求が寸法と規則を持っているなら 1 コンポーネント、別々の要求が持っているなら別コンポーネントとする。」
   そして 表 T-062 は「正」の欄（`:106` の見出し行）で実際に要求を名乗っている。

   ⇒ **欠けているのは条項ではなく、粒度と覆いと向きである。**
   - **粒度** —— 名乗るのはコンポーネント（36）であってユニット（68）ではない。  
   ⛔ **AI が開くのはファイルである。**
   - **覆い** —— 121 件中 42 件（34.7%）しか覆っていない（実測 (g)）。
   - **向き** —— 「コンポーネント → 要求」なので、要求から引くには 36 行を読み切るしかない。

---

## 2. グラフ（測った先）

- `impact.py FR-098` ⇒ 指している先は表 14 個・行 24 個。
  指している箇所の一覧に `(section) 5.3 File Structure (ファイル構成)` `05-07-design.md:397` が 1 件出る。
  ⭐ **その行は 表 T-075 の `UF-63` である** —— つまり**結びは既に 1 本だけ存在し、道具はそれを「節」としか呼べない。**
  ⇒ 本書が足すのは新しい種類の辺ではなく、**既に 12 本ある辺を 121 本に揃え、行 ID で呼べるようにすること**である。
- `impact.py UF-63` ⇒ 属する表 T-075、指されている箇所 0 件（1.3 の 2）。
- **閉路に入らない** —— 新しい 表 T-277 は区分を定義するだけで、どの表も指さない。
  表 T-075 の新しい欄は要求を**指す**側であり、要求の側は本書では 1 文字も変えない。
  ⇒ 誘導部分グラフは「表 T-075 → 121 の要求」の一方向であり、非巡回である。

---

## 3. 決定 1 —— 関係をどこに置くか

### 比べた 4 つ

| 案 | 形 | 検査が読むもの | 判定 |
|---|---|---|---|
| **甲** | **表 T-075 に欄を 1 つ足す**（⭐ **推奨**） | `specTable('T-075')` の `row.by['負う要求']`。<br>⭐ **道は既に在る** —— 表 T-075 は `src/<層>/<コンポーネント>/<ファイル>` を決めており、**検査 18 がその集合を `src/` と突き合わせている**。<br>⇒ 要求 ID を 1 つ書くだけで、検査は**実在するファイルの絶対の道**を手に入れる | ⭐ **採る** |
| 乙 | 新しい表を立て、要求 1 件 1 行で持つ | 同じ `specTable` だが、**行が 121 になり、表 T-075 の責務の欄と同じことを 2 か所で言う**（`R1.3` に反する） | ⛔ 退ける |
| 丙 | 表 T-063（割った理由）に持たせる | 表 T-063 は 7 行しかなく、**割った理由だけを持つ**と本文が明言している（`05-07-design.md:309`、逐語「**表 T-063 が持つのは、割った理由だけである。**」）。<br>121 件は入らない | ⛔ 退ける。<br>⭐ ただし 4 節が 表 T-063 を**補強**する |
| 丁 | 要求の側（Chapter 1〜4）に欄を足す | 要求は StrictDoc の節であり、欄を足すには `spec.sgra` の文法を変え、121 の節を触ることになる。<br>⛔ **しかも道を引くには結局 表 T-075 へ join する** | ⛔ 退ける |

### ⭐ 甲が既存の道具を壊さないことを、当てる前に測った

⛔ **3 つの読み手が 表 T-075 を読む。  
欄を末尾に足したとき、3 つとも壊れない。**

1. **`tools/generate_unit_tree.py`（検査 18）** —— `row.cells` の **0〜3 番目**しか見ない（`:95`）。
   5 番目の欄は読まれない。
2. **`tests/contract/units.contract.test.ts`** —— `row.by['コンポーネント']` のように**列の見出しで引く**（`:34`）。
   欄が増えても引く先は変わらない。
3. **`.claude/skills/spec-graph-check/audit-ch5.py`（検査 33）** —— 正規表現で 4 セルに割る（`:176`）。
   ⭐ **実際に 6 列の行を食わせて確かめた** —— 一致し、純粋性は 3 番目のまま正しく取れ、増えた欄は**使われていない 4 番目**に吸われる。
   ⇒ 検査 33 は緑のままである。  
   ⚠️ **当てる者は、当てたあとに検査 33 の出力を当てる前と diff して、これを再確認すること。**

---

## 4. 決定 2 —— 「持つ」とは何か

⛔ **1 対 1 を強いてはならない。**  
 実測（5 節）で、単独の持ち主を正直に立てられるのは 87 件のうち 40 件だけである。  
⭐ **そのかわり、どの区分でも「どこから読み始めるか」は 1 つに決まる。**

**表 T-277 — 要求の持ち主の区分**（行 ID の接頭辞 `OW`）

| 行 ID | 区分 | 定義 | 「負う要求」の欄に立つユニット | 例 |
| --- | --- | --- | --- | --- |
| OW-1 | **単独** | **規則を決めるユニットが 1 つだけあり**、ほかのファイルはその結果を使うだけである。<br>要求が動いてもほかのファイルの規則は動かない | **そのユニット 1 つ** | `FR-014`（イナズマ線の頂点）は `schedule-geometry.ts` |
| OW-2 | **連鎖** | **層を跨ぐことが要求そのものである。<br>** 入力 → ユースケース → 幾何 → 描画 → DOM と伝わる。<br>⭐ **跨ぐのは表 T-061 の依存規則がそう強いているからであり、設計の失敗ではない** | **規則の値を持つ最も内側のユニット。<br>** 外側はそれに従うだけなので、変更はそこから始まる | `FR-085`（行を作る）は `edit-task-group.ts` |
| OW-3 | **群** | **同じコンポーネントの兄弟ユニットが並列に分担する。<br>** 表 T-063 の `UT-2`・`UT-7` が「集約ごと」「UI パーツごと」と言っているのがこの形である | **そのコンポーネントの公開エントリ。<br>** ⭐ 入口は規則を持たないが、**兄弟の全数をそこで読めるので 1 ホップで正しい兄弟に着く** | `FR-032`（削除）は `edit-document.ts` |
| OW-4 | **横断** | **ユニットが何をするかではなく、どう書かれるかを縛る。<br>** 検証の作法・表示言語・可読の下限・寸法の出どころなど | **起点のユニット（在るとき）。<br>** ⛔ 無いときは、正を持つ規則または表を名指すこと（MUST） | `FR-023`（外部入力の検証）は `validate-imported-document.ts`。<br>`FR-094`（寸法は 1 つの表から）は 表 T-201 |
| OW-5 | **全体の性質** | **どのユニットの性質でもなく、木の全体の性質である。<br>** ⭐ `OW-4` との境はここである —— `OW-4` はユニットがどう書かれるかを縛り、`OW-5` はどのユニットにも属さない | **ユニットではなく、確かめる手立て（試験・関門・検査）。<br>** ⛔ 表 T-075 の欄には立たず、5.3 の結びの段が名指す | `NFR-004`（単一ファイル・オフライン）は `tests/system/nfr-004-file-scheme-sweep.sws.test.ts` |

⭐ **`OW-3` は 表 T-063 の `UT-2` / `UT-7` と同じことを要求の側から言っている。**  
⇒ **表 T-063 の「割った理由」が「集約ごと」「UI パーツごと」と書いているとき、その「ごと」が何なのかを、本欄が要求 ID で名指す。**  
⛔ 表 T-063 の 7 行は 1 行も動かさない。

### ⭐ 欄に書く形

表 T-075 に欄「**負う要求**」を末尾に足し、`` `FR-013`（`OW-1`）・`FR-089`（`OW-1`） `` の形で、**要求 ID と区分の行 ID の組**を並べる。  
何も負わないユニットは `—` とする（⚠️ 層をまたぐ宣言だけの 8 ファイルは規則を持たないので、必ず `—` になる）。

⛔ **`OW-4` で起点のユニットが無い要求と、`OW-5` の要求は、表 T-075 のどの行にも現れない。**  
その全数は 5.3 の結びの段が名指すこと（MUST）—— `CR-431` が 表 T-274 の結びで 4 行を名指したのと同じ形である。

---

## 5. 決定 3 —— 「60% へ上げる」は正しい目標か

⛔ **正しくない。  
測った結果、2 つの意味で外れている。**

### 5.1 実測（2026-09-21、`src/` が名を持つ `FR` 87 件を 1 件ずつ読んだ）

| 区分 | 件数 | 割合 |
|---|--:|--:|
| `OW-1` 単独 | **40** | **46.0%** |
| `OW-2` 連鎖 | 37 | 42.5% |
| `OW-3` 群 | 3 | 3.4% |
| `OW-4` 横断 | 7 | 8.0% |

⭐ **測り方**: 87 件の `**STATEMENT**` を読み、「この要求が動いたとき、規則を書き換えるファイルは 1 つか」を判定した。  
⛔ **これは判定であって観測ではない。**  
 だから区分は行 ID で名乗らせ、当てるときに 1 件ずつ覆せるようにしてある（4 節）。

### 5.2 ⛔ 60% が外れている 2 つの理由

1. **上限が 46% である。**  
 単独の持ち主を正直に立てられるのは 40 件で、60% が要る 52 件には **12 件足りない。**
   ⛔ **60% に届かせようとすると、`OW-2` の 37 件のうち 12 件について「1 ファイルで済む」と嘘を書くことになる。**
   ⭐ 嘘をつかずに 46% を超える道は 1 つしか無い —— **ユニットを割ること。**  
   それは本書の外である（9 節）。
2. ⭐ **そもそも 22% と 60% は、目的の役に立たない量を測っている。**
   22% は「`src/` の注釈で 1 ファイルだけが名を書いている」割合である。
   ⛔ **Clean Architecture は、画面に出る振る舞いの要求が複数のファイルに名を残すことを保証する**（表 T-061 の依存規則）。
   ⇒ **この数は上げようとすべきものではない。**

### 5.3 ⭐ 目的に効く数は「全域性」である

**本書が提案する数はこれである** ——

| 測るもの | いま（2026-09-21 実測） | 当てたあと |
|---|--:|--:|
| 起点のユニットが 1 つに決まる要求（121 件中） | **24**（19.8%） | **121**（100%） |
| そのうち起点が**ユニット**である（残りは規則・表を名指す） | 24 | **120**（99.2%。<br>⛔ 残り 1 件は 5.4） |
| 単独の持ち主（`OW-1`）である割合（87 件中） | 19（21.8%）※測り方が違う | **40**（46.0%） |

⇒ ⭐ **「1 件の変更要求に対して、開くべき最初のファイルが 100% 決まる」が目標であり、「1 ファイルで済む割合」ではない。**

### 5.4 ⛔ 起点のユニットが無い 1 件

**`FR-092`（マニュアルを読まずに使えるようにする手立てを持つ）** ——その `**STATEMENT**` は逐語「`GRS` は、**表 T-040 の手立て**をすべて備えること。」であり、**どのユニットも単独では備えられない。**  
⭐ **正は既に仕様が名乗っている** —— `docs/spec/05-07-design.md:90`（逐語）:「設計の合否は `docs/development-rules/07-review-standards.md` の `R2` で判定する（`FR-092` の `EZ-5`）。」⇒ **`FR-092` の持ち主は `R2` である。**  
 5.3 の結びがそう名指す。

### 5.5 ⭐ 判定の裏取り —— 起点は、たいてい既にそのファイルに名がある

本書の 86 件の起点のうち、**62 件（72.1%）は、その起点のファイルが既にその要求 ID を注釈に書いている。**  
⇒ 判定はコードの現状とおおむね一致する。

⚠️ **一致しない 24 件は、判定違いか、規則が別のファイルに載っているかのどちらかである。**  
⛔ **後者なら欠陥である。**  
 目に付いた 3 つを 8.2 節の台帳候補に挙げる。

---

## 6. 腐らせない仕組み

⛔ **腐る対応表は、無いより悪い。**  
 だから紙の約束ではなく機械で閉じる。

### 6.1 ⭐ 検査が読むもの

⛔ **新しい `check.sh` の番号も、新しい基準線ファイルも要らない。**  
`CR-431` と同じ形で、契約試験 1 本に入れる。

```
置く所    tests/contract/cr-435-every-requirement-names-a-unit.contract.test.ts
読むもの  tests/contract/spec-table.ts の specTable(id) と bareAll(cell)
          docs/spec/01-04-requirements.md の "**UID**: " の行

  1. 要求の集合 Q = 01-04-requirements.md の **UID**: FR-nnn / NFR-nnn      （121）
  2. specTable('T-075') の各行の「負う要求」を bareAll で開き、
     (要求 ID, 区分の行 ID) の組の列 P を作る
  3. specTable('T-277') の行 ID の集合 C                                     （OW-1..OW-5）
  4. 5.3 の結びの段が名指す「起点のユニットを持たない要求」の集合 X         （5 件。
     FR-092 / NFR-001 / NFR-004 / FR-030 / FR-069）
  5. P の区分は全部 C に入る                  ← 区分を勝手に増やせない
  6. P の要求 ID は全部 Q に入る              ← 宙に浮いた要求 ID を書けない
  7. P の要求 ID に重複が無い                 ← 1 要求 1 起点（一価）
  8. P の要求 ID ∪ X == Q                     ← 覆い残しが無い（全域）
  9. |Q| を結びの段が数で述べ、その数と一致する ← 黙って要求が増えたらここで止まる
```

⭐ **7 と 8 が関係を「全域かつ一価な関数」にする。**  
 ⇒ **要求 ID を 1 つ渡せば、開くファイルがちょうど 1 つ返る。**  
⭐ **道は検査 18 がただで保証する** —— 表 T-075 の行はすべて `src/` に実在するファイルである（そうでなければ検査 18 が赤になる）。  
⚠️ **既存の検査もただで効く** —— 検査 7 は「負う要求」の欄に書いた実在しない ID を赤にし、検査 9 は結びの段の数を突き合わせる。

### 6.2 ⭐ 基準線は 1 つだけ置く —— 未記入の件数のラチェット

⚠️ **本節は利用者の裁定（2026-09-21）で書き換わった。**  
⛔ **草案は「基準線を 1 つも作らない」と書いていたが、それは取り下げる。**  
逐語 ——「**基準線は一度変更していいよ**」。

⭐ **置くのは 1 つ、形も 1 つに決まっている** ——**表 T-075 の「負う要求」の欄が起点を書いていない要求の件数**を数える基準線である。

```
測るもの  121 件の要求のうち、起点がまだ書かれていないものの件数
          （⛔ 起点のユニットを持たないと 5.3 の結びが名指す要求は、この数に入れない）
減る      埋めれば下がる。下がったら基準線を下げる
増える    ⛔ 上がったら赤。埋めた起点を消すことも、
          起点の無い要求を黙って足すこともできない
いまの数  17（2026-09-21 実測。10.7 の裁定を当てたあと。
          121 − 99（欄が名指す）− 5（`OW-5`）= 17）
```

⛔ **これは「いまの穴の数」を許す基準線ではない。  
穴の数が増えないことを縛るラチェットである。**  
⭐ **だから 2 つとも成り立つ** —— **途中でも緑**であり、**後戻りは止まる。**  
⇒ 巡をまたいで赤を持ち越さずに、段階的に埋められる。

⛔ **本書を当てる体は、基準線のファイルを書かない。**
 未記入の件数を測って報告するところで止め、**前に立つ者が利用者に見せてから書く**
 —— 体が検査の基準線を動かすのは CI の迂回として扱われる。

⚠️ **全域性の主張は、基準線が 0 になるまで成り立たない。**  
6.1 の検査 8（`P` の要求 ID ∪ `X` == `Q`）は、そのときに初めて緑にできる。  
⛔ **それまでは「要求 ID を渡せば必ずファイルが 1 つ返る」と書いてはならない。**

---

## 7. 数の予測

| 何 | いま（2026-09-21 実測） | 差 | 当てたあと |
|---|--:|--:|--:|
| tables | 170 | **+1**（表 T-277） | **171** |
| figures | 17 | ±0 | **17** |
| rows | 2178 | **+4**（`OW-1` 〜 `OW-4`） | **2182** |
| uids | 161 | ±0 | **161** |

⚠️ **表 T-075 の欄は 1 つ増えるが、行は 1 行も増えない。**  
⚠️ `docs/spec/_assets/tbl-row-id-prefixes.md` は生成物であり、`OW` の登録は `npm run gen` が書く（検査 21・27）。  
⛔ 手で直さない。

**埋める量**（実測）—— 要求 121 件。  
うち **48 件**は 表 T-062 の「正」または 表 T-075 の「責務」が既に半分決めている。  
**73 件**は手がかりが無く、うち 11 件は `NFR` である。  
⭐ **これは体（subagent）の仕事である。**  
 前に立つ者は測り直すだけでよい。

---

### 7.1 ⭐ 当てたあとに測った数（2026-09-21）

| 何 | 予測 | 実測 | 判定 |
|---|--:|--:|---|
| tables | 171 | **171** | ⭐ 一致 |
| figures | 17 | **17** | ⭐ 一致 |
| rows | 2182 | **2182** | ⭐ 一致 |
| uids | 161 | **161** | ⭐ 一致 |

**埋めた量** —— 121 件のうち **99 件**に起点を書いた。  
`FR-092` は起点のユニットを持たないので 5.3 の結びが `R2` を名指す。  
⚠️ **残り 21 件は起点を決めていない。**  
⛔ **当て推量で埋めなかった。**  
理由は 3 つに分かれ、5.3 の結びが 1 件ずつ名指している ——**仕様が起点を決めていない 12 件**（`DFC-673`）・**木の全体の性質である 8 件**・**入口をいまは作らないと決めている 1 件**（`FR-086`）。

⚠️ **この 21 件と 8 件は、当てた時点の数である。**  
⛔ **10.7 の裁定を当てたあとの数は 7.2 が持つ。**

**区分の内訳**（99 件）—— `OW-1` が 22、`OW-2` が 63、`OW-3` が 5、`OW-4` が 9。  
⚠️ 5.1 の予測（`src/` が名を持つ 87 件の `FR` について `OW-1` が 40）とは母数も測り方も違う。  
⛔ **5.1 の母数は、`src/` のどのファイルも名を書いていない 28 件を含んでいない**（`DFC-675`。  
当てる巡で独立に測り直し、同じ 28 件を得た）。  
⇒ **2 つの数を比べてはならない。**

### 7.2 ⭐ `OW-5` を当てたあとに測った数（2026-09-21）

| 何 | 7.1 の時点 | 差 | いま |
|---|--:|--:|--:|
| tables | 171 | ±0 | **171** |
| figures | 17 | ±0 | **17** |
| rows | 2182 | **+1**（`OW-5`） | **2183** |
| uids | 161 | ±0 | **161** |

| 何 | 7.1 の時点 | いま |
|---|--:|--:|
| 表 T-075 の欄が起点を名指す要求 | 99 | **99** |
| 起点のユニットを持たない要求（`OW-5`。<br>5.3 の結びが手立てを名指す） | 1 | **5** |
| ⛔ **起点をまだ書いていない要求** | 21 | **17** |

⛔ **17 が、6.2 の基準線が持つ数である。**  
⚠️ **⛔ 本書を当てる体は基準線のファイルを書かない** —— 6.2 のとおり、測って報告するところで止める。

---

## 8. 影響

### 8.1 仕様

- `docs/spec/05-07-design.md` の 5.3 —— 表 T-075 に欄「負う要求」（68 行すべてのセル）、表 T-277 を新設、結びの段を 1 つ足す。  
  ⭐ **当てた（2026-09-21）。**  
  欄に MUST は 1 つだけ書いた —— 逐語「同じ要求を 2 つ以上の行に書いてはならない（MUST NOT）」。  
  ⚠️ **検査 39 は MUST の全数を数えるので、足すほど悪くなる。**  
  ⛔ だから規則を運ぶ最小の 1 つに絞り、残りは記述の文にした。
- `docs/spec/_source/row-id-prefixes.json` に接頭辞 `OW` を登録した（生成物 `_assets/tbl-row-id-prefixes.md` は `npm run gen` が書いた）。
- ⛔ **`docs/spec/01-04-requirements.md` は 1 文字も変えない。**
- ⛔ **表 T-062 の「正」の欄は 1 文字も変えない** —— コンポーネントの正はコンポーネントの話であり、ユニットの起点とは粒度が違う。
  ⚠️ **ただし 2 つが食い違ったら欠陥である。**  
  6.1 の検査はそこを見ない（⛔ 見えないことを書いておく）。

### 8.2 記録（当てるときに書く）

- `docs/development-records/changelog.md` に 1 行。
- ⚠️ **台帳の候補 3 件**（5.5 の 24 件から。  
⛔ **本書は台帳を書かない。  
当てる者が `defects.md` に起こすか、退けるかを決める**）:
  - `FR-105`（重なっても狙ったものが答える）と `FR-106`（ポインタの形）は `item-hit-area.ts` が持つはずだが、
    いま名を書いているのは `svg-renderer.ts` と `single-html-shell.ts` だけである。
  - `FR-051`・`FR-052`（スクロールとパネル幅）は `screen-regions.ts` が持つはずだが、そのファイルが名を書いていない。
    ⛔ **`FR-052` の側は当てる巡で反証された（2026-09-21）** —— 表 T-075 の `UF-61` が
    `screen-frame.ts` に `FR-052` を書いており、そのファイル自身も名を書いている（`screen-frame.ts:68`）。
    表 T-062 は `FR-052` に「正」を持たない。
    ⇒ 食い違いではないので、欄は `UF-61` に置いた。
    ⚠️ **`FR-051` の側は残る** —— 表 T-062 の `CP-35` と 表 T-075 の `UF-61` が別のファイルを指す。
    ⛔ どちらが正かを決めないので、欄は空のままにした（`DFC-673`）。
  - `FR-063`（文書の 2 つの刻）は `document-stamp.ts` が持つはずだが、そのファイルが名を書いていない。

### 8.3 実装と試験（⛔ 本書は直さない。所だけ書く）

- 新しい契約試験 1 本（6.1）。
- ⛔ `src/` は 1 ファイルも触らない。  
⇒ **検査 60（大きさのラチェット）は動かない。**

---

## 9. 段階 4 の継ぎ目に何を意味するか

⛔ **本書は割り方を設計しない。**  
 別の変更要求の仕事である。  
⭐ **本書が与えるのは、継ぎ目の候補を機械で出す道である。**

⭐ **表 T-063 は既に「変更の理由が別なので割った」と書いている**（`UT-2`・`UT-7`）。  
⇒ **1 ユニットが `OW-1` の要求を n 件持つなら、そのユニットには変更の理由が n 個あり、`R2.2` から見れば継ぎ目の候補が n−1 本ある。**

本書の判定を数えるとこうなる（2026-09-21）——

| ユニット | 起点として負う要求 | 大きさ（検査 60 の基準線） |
|---|--:|---|
| `schedule-layout.ts` | **9** | 基準線に行を持たない |
| `schedule-geometry.ts` | 6 | 同上 |
| `edit-task.ts` | 6 | `editTask` 486 行 / 分岐 101 |
| `svg-renderer.ts` | 5 | `svgFromSchedule` 615 行 / 分岐 126 |
| `edit-task-group.ts` | 4 | `editTaskGroup` 433 行 / 分岐 103 |
| `frame-loop.ts` | **1** | `frameLoop` **2616 行** / 分岐 4 |
| `dom-screen-surface.ts` | **1** | `domScreenSurface` **871 行** / 分岐 1 |

⇒ ⭐ **予測が 2 つ出る。  
どちらも反証できる形で書く。**

1. **`svgFromSchedule`・`editTask`・`editTaskGroup` の継ぎ目は、要求の境目に落ちる。**
   それぞれ 5・6・4 の独立した変更理由を持つので、割った先は表 T-063 の `UT-2` / `UT-7` と同じ形（「要求ごと」）になる。
2. ⛔ **`frameLoop`（2616 行）と `domScreenSurface`（871 行）には、要求の継ぎ目が無い。**
   どちらも起点として負う要求は 1 件だけである。
   ⇒ **この 2 つの大きさは「複数の要求を抱えている」せいではなく、結線と DOM の量である。**
   ⭐ **割るなら別の基準が要る**（起動の順序は 表 T-077、フレームの契機は 表 T-078 が持つ）。
   ⚠️ **本書の関係は、この 2 つについて「継ぎ目はここだ」と言えない。  
   ⛔ 言えないことを書いておく。**

---

## 10. 書くときの作法（⛔ 当てる体はここを読んでから書く）

- ⛔⛔ **置き換える文字列が 1 度しか現れないことを、書き換える前に数える。**  
 `count(...) == 1` を主張してから書く。
- **StrictDoc の罠**（`CR-430` の 9 節）:
  - ⚠️ **CJK の直後に `**` を開き、その直後にコードスパンを置かない。**
  - ⚠️ `*` を 4 つ続けない。
  - ⚠️ **変更履歴の行に生の縦棒を書かない**（列が割れる）。
  - ⚠️ **「表 X の N 行」と書くと検査が「X は N 行を持つ」と読む** ⇒ **行 ID を名指す。**
  - ⚠️ **値の表・用語集に `（MUST）` を書かない。**
  - ⚠️ **`A-17` が単独で使うことを禁じる語を書かない**（測った: 本書は 0 件）。
  - ⚠️ **検査 46** —— 書いたあと `python tools/fix_line_breaks.py <file>`。  
  表の中は `<br>`。
- ⛔ **生成物を手で直さない** —— `tbl-row-id-prefixes.md` は `npm run gen`（検査 21・27）。
- **道具**: `PYTHONIOENCODING=utf-8`。  
スクリプトはファイルに書いて道で走らせる。  
改行は CRLF と LF が混ざるので、読みで正規化し、書き戻しは多数派に揃える。
- ⛔ **当てる直前に識別子を測り直す**（規則 02 の 2.5。  
検査 62）—— `CR-435` ・ 表 `T-277` ・ 接頭辞 `OW` の 3 つである。

---

## 10.5 ⭐ 利用者の裁定（2026-09-21）—— 4 つの返す問いは全部答えが出ている

⛔ **11 節の返す問いは、もう問いではない。  
逐語と、そこから出た形を残す。**

| 問い | 裁定 | 当てるときに何が変わったか |
|---|---|---|
| 問い 1（一度に埋めるか） | ⭐ **段階的に埋めてよい。<br>**  <br>逐語「**基準線は一度変更していいよ**」 | 6.2 を書き換えた。<br>基準線は 1 つだけ置き、**その形は「未記入の件数のラチェット」**である。<br>⛔ 単なる「いまの穴の数」ではない |
| 問い 2（`OW-1` の上限 46% を受け入れるか） | ⭐ **受け入れる。<br>** | 目標は **全域性**（どの要求にも起点が 1 つ決まる）であって、1 ファイルで済む割合ではない。<br>⛔ **前に立つ者が最初に出した「60% へ上げる」は誤りであった** —— 5.2 のとおり、正直に立てられる上限が 46% なので、60% に届かせるには `OW-2` の 12 件について「1 ファイルで済む」と嘘を書くしかない。<br>⚠️ そのうえ 22% と 60% は、層の依存規則が複数ファイルに名を残させる方向の数であり、上げようとすべきものではない |
| 問い 3（`NFR` を含めるか） | ⭐ **含める。<br>**  <br>**121 件（`FR` 108 ＋ `NFR` 13）が対象である** | 2026-09-21 に測り直して 121 ／ 108 ／ 13 を再現した。<br>`NFR` の起点は `OW-4`（横断）になりやすく、実際 13 件のうち 5 件が `OW-4`、1 件が `OW-1`、1 件が `OW-2`、6 件が未記入である |
| 問い 4（食い違い 24 件を台帳に起こすか） | ⭐ **起こす。<br>**  <br>⛔ **どちらが正かは決めない** | 仕様とコードの食い違いは `defects.md` へ、という 2026-09-13 の裁定に従う。<br>⛔ **本書を当てる体は台帳を書かない** —— 行は別の体が同じ巡で起こしている |

### 10.6 ⚠️ 当てる巡で受けた 3 つの測り直しと、その扱い

| 受けた測定 | 本書の扱い |
|---|---|
| ⛔ **`src/` のどのファイルも名を書いていない要求が 28 件ある**（`DFC-675`）。<br>  <br>5.1 の母数 87 件はこの 28 件を含まない | ⭐ **独立に測り直して同じ 28 件を得た**（2026-09-21）。<br>  <br>⇒ 5.1 の `OW-1` 40 ／ `OW-2` 37 ／ `OW-3` 3 ／ `OW-4` 7 は **121 件へ持ち越せない。<br>**  <br>7.1 の内訳は 121 件で測り直したものである |
| ⛔ **仕様が起点を決めていない 12 件**（`DFC-673`） | ⭐ **12 件とも空のままにした。<br>**  <br>⛔ 埋めると、仕様の 2 行のどちらが正かを本書が黙って選ぶことになる |
| ⛔ **木の全体の性質であって 1 ユニットの性質ではない要求がある** | ⭐ **8 件を空にした** —— `FR-030` ・ `FR-069` ・ `NFR-001` ・ `NFR-004` ・ `NFR-005` ・ `NFR-006` ・ `NFR-008` ・ `NFR-012`。<br>  <br>⚠️ `OW-4` の逃げ道（規則か表を名指す）は使えない —— これらは名指せる規則も表も持たない。<br>  <br>⛔ **5 つめの区分を立てるかどうかは利用者が裁くことであり、本書では決めない**（⭐ **10.7 で裁定が出た** —— `OW-5` を立て、8 件のうち 4 件がそこへ入った） |

⚠️ **1 つだけ受けなかった** —— `NFR-003`（フレーム時間の 95 パーセンタイル）を「木の全体の性質」に 入れる読みは採らず、`frame-loop.ts` に `OW-4` で置いた。  
⭐ **理由** —— `NFR-002` は逐語で同じ「表 T-025 の条件と `MC-8` の区間」を言い、`src/` で名を書いているのは `frame-loop.ts` ただ 1 ファイルである。  
表 T-075 の `UF-48` はそのユニットの責務を「フレームを起こす契機の観測」と「フレーム先頭の収集と計算」と 書いており、**どちらの予算が動いても最初に開くのはそこである。**  
⛔ **双子の片方だけを空にする理由が見つからなかった。**  
⚠️ **反証できる形で書いておく** —— `NFR-002` を空にすべきなら、`NFR-003` も空にすべきである。

### 10.7 ⭐ 利用者の裁定（2026-09-21）—— `OW-5`「全体の性質」を立てる

⛔ **10.6 の 3 つめが保留にしていた「5 つめの区分を立てるか」に答えが出た。**  
⭐ **立てる** —— 表 T-277 に `OW-5`「全体の性質」を足す。  
⛔ **`OW-5` の起点はユニットではなく、確かめる手立て（試験・関門・検査）である。**

⭐ **`OW-4` との境は 1 文で引ける** —— `OW-4` は**ユニットがどう書かれるか**を縛り、`OW-5` は**どのユニットにも属さない**性質である。

#### ⚠️ 8 件のうち手立てを持つのは 4 件だけであった（2026-09-21、独立に測り直した）

⛔ **当てる前の見立て「8 件のうち 6 件は既に対応物を持つ」は誤りであった。**  
⭐ `tests/` の全 321 ファイルと `.claude/skills/spec-graph-check/` の全 48 `.py` を、要求 ID の逐語で引いて数えた。

| 要求 | `tests/` | 検査 | `OW-5` の起点として名指したもの |
|---|--:|--:|---|
| `NFR-001` | 1 | 0 | `tests/nfr/nfr-001-010-011-013-the-rest-of-chapter-7.test.ts` |
| `NFR-004` | 11 | 0 | `tests/system/nfr-004-file-scheme-sweep.sws.test.ts` |
| `FR-030` | 3 | 0 | `tests/unit/uf-32.test.ts` |
| `FR-069` | 1 | 0 | 検査 27 が走らせる `tools/generate_licence.py --check` |
| `NFR-005` | **0** | **0** | ⛔ **無い** |
| `NFR-006` | **0** | **0** | ⛔ **無い** |
| `NFR-008` | **0** | **0** | ⛔ **無い** |
| `NFR-012` | **0** | **0** | ⛔ **無い** |

⚠️ **検査の欄が `FR-069` で 0 なのは、検査のスクリプトのどれも `FR-069` を書いていないからである。**  
⭐ それでも手立ては在る —— 検査 27 が `tools/generate_licence.py --check` を走らせ、その手前のスクリプトが `FR-069` を逐語で名乗っている。  
⚠️ 同じく `NFR-004` の検査 1 件は `check-must-clause-coverage.py` の注釈に在る**コミットの表題の引用**であり、確かめる手立てではない。  
⇒ ⛔ **どちらも「ファイルが名を書いているか」と「確かめているか」が別だと示している。**

#### ⭐ 手立ての無い 4 件をどう扱ったか —— 空のままにした

⛔ **`NFR-005` ・ `NFR-006` ・ `NFR-008` ・ `NFR-012` は `OW-5` として書かなかった。**

⭐ **理由** —— `OW-5` の起点は確かめる手立てである。  
手立てが無ければ**名指すものが無く**、`OW-5` と書いた時点で空欄が埋まったように見える。  
⛔ **在りもしない試験の名を書くことも、「手立ては無い」と書いて埋まった扱いにすることも、しない。**  
⇒ **4 件は「起点をまだ書いていない」ほうに残し、6.2 の基準線が数え続ける。**  
⭐ **だから手立てが 1 つ立てば基準線が 1 つ下がる** —— 穴が見え、減ったことが機械で分かる。

⚠️ **`FR-030` と `NFR-006` は主題が近いが、同じ扱いにしなかった。**  
⛔ **`FR-030` は `tests/unit/uf-32.test.ts` が逐語で確かめており、`NFR-006` はどこも確かめていない。**  
⇒ 主題ではなく**手立ての有無**で分かれる。

---

## 11. ⛔ この変更でやらないこと ／ 返す問い

**やらないこと**:

- 利用者が当ててよいと言う前に仕様を変えない。
- **ユニットを 1 つも割らない**（`R2.2c` のとおり、既存の違反は段階的に是正する）。
- **表 T-062 の「正」の欄と 表 T-063 の 7 行を 1 文字も変えない。**
- ⚠️ ~~基準線ファイルを 1 つも作らず、1 つも動かさない~~ —— **10.5 の問い 1 で覆った。**  
  基準線を 1 つ置く（6.2）。  
  ⛔ ただし**当てる体は書かない** —— 測って報告するところで止める。
- 「この要求に触るファイル全部」を書かない（決定 2）。
- 実装と試験を本書では直さない（8.3 に所だけ書く）。

### ⛔ 返す問い（⭐ **4 つとも 10.5 で決着している。
下の表は問うた当時のまま残す**）

| # | 問い | 本書の推奨 | 測った代償 |
|---|---|---|---|
| 問い 1 | **121 件を当てる巡で一度に埋めてよいか。<br>** ⛔ 分けて埋めるなら基準線ファイルが 1 つ要り、それは利用者の裁定に反する（6.2） | ⭐ **一度に埋める。<br>** 体 1 つの仕事にする | 埋めるのは 121 件。<br>うち 48 件は既に半分決まっている（7 節）。<br>⚠️ 表 T-075 の 68 セルに触る 1 回の大きな編集になる |
| 問い 2 | **`OW-1` の上限が 46% であることを受け入れるか。<br>** 60% へ上げるにはユニットを割るしかない（5.2） | ⭐ **受け入れる。<br>** 割る話は段階 4 の変更要求へ回す | いま割ると、本欄を書く前に表 T-075 の行が動き、同じ所を 2 度書くことになる |
| 問い 3 | **`NFR` 13 件も対象に含めるか。<br>** 11 件は手がかりが 1 つも無く、多くは `OW-4`（横断）になる見込みである | ⭐ **含める。<br>** 含めないと検査 8 の全域性が言えず、関係として使えなくなる | `NFR` の起点は規則や表になりやすく、「ユニットを名指す」率が下がる（5.3 の 99.2% はこれを織り込んだ数である） |
| 問い 4 | **5.5 の食い違い 24 件を、当てる巡で台帳に起こすか。<br>** 3 件は規則が別のファイルに載っている疑いがある（8.2） | ⭐ **起こす。<br>** ⛔ ただし**どちらが正かを本書では決めない**（仕様とコードの食い違いは `defects.md` へ、という裁定に従う） | 台帳の行が最大 24 増える。<br>⚠️ 多くは本書の判定違いのはずである |
