# CR-208 — 誰も測らなかった 2 つの掴み代

> **閉じるもの**: ⭐ **利用者の裁定（2026-08-22）** —— **`PointerSlop` が要る 2 値を 表 T-206 に足す。**
> ⭐ **`S-91`（実績の端点）に機械が読める数を与え、`S-137`（線の掴み代）を新設する。**
> ⭐ **`S-90` が横方向にも掛かることを、同じ 1 つの数のまま述べる** —— ⛔ **数は増やさない。**
> ⛔ **表 T-023d は 1 行も動かない** —— あの表は既に「掴み代と当たり判定の値は 表 T-206 が持つ」と書いており、**足りなかったのは持たれていた側である。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作図ソフトと同じ操作感で描きながら、構造化した日程データを出す）。受ける目標は `GL-004`。**

⛔ **表 T-023d の 18 行のうち 4 行が、値を持たないまま在る** —— `GR-5` / `GR-6`（実績の端点）と `GR-13` / `GR-16`（線）である。
⚠️ **`itemAtPointer` は 6 つの掴み代を引数で受け取るが、そのうち 2 つに出どころが無く、シェルが呼べない。** ⭐ **実績バーを掴む経路は `GR-5` / `GR-6` だけである**（表 T-023d の注が MUST でそう定めている）ので、**この 2 値が無いあいだ、実績は 1 度も掴めない。**

⭐ **`CH-4`（説明を読まずに操作できる）にも掛かる。** ⚠️ **押しても掴めないことは、説明では埋められない。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.1`**（計測可能な数値基準）| `S-91` の既定「実績バーの帯」 | ⛔ **本物の違反。** ⭐ **`S-90` `S-92` `S-93` `S-134` はいずれも数を持つのに、`S-91` だけが散文である。** ⚠️ **版 0.56（`CR-178`）が 表 T-206 を機械可読にしたとき、`S-91` は「取り出す数が無い」ので取り残された** —— 変更履歴が `S-90` `S-92` `S-93` だけを名指している |
| **`R1.3`**（唯一の正がある）| 「帯」が縦か横か | ⛔ **本物の違反。** ⭐ **「実績バーの帯」は縦の広がりである** —— 縦の鎖（`S-4` / `S-5` / `S-6` / `S-13` 〜 `S-16` / `S-76`）と `LF-9` はすべて縦の話であり、**横の数はどこにも無い。** ⚠️ **端点は点なので、縦だけでは面積を持たない** |
| **`R1.3`**（同上）| `S-90` の既定「バーの上下に 6px」 | ⛔ **同じ穴が `S-90` にも在る。** ⭐ **`S-49` の備考が「由来は予定の端点の掴み代（`S-90`）—— 掴み代より狭い形状は自分の胴で掴めない」と書いており、そこでは横幅として読んでいる。** ⚠️ **同じ行を、片方は縦、片方は横として読んでいた** |
| **`R1.4`**（境界値の明示）| `GR-13` / `GR-16` の「線の上」 | ⛔ **本物の違反。** ⭐ **表 T-023d の前文が「掴み代と当たり判定の値は 表 T-206 が持つ」と全部を委ねているのに、線の行が 1 つも無い。** ⚠️ **「線の上」は幅 0 の集合であり、そのままでは決して当たらない** |
| **`R2.7`**（DRY。マジックナンバーを定数化）| 数の置き場 | ⭐ **満たす。** **数は原稿の 1 か所に置き、生成器が `NOT_STORED_SIZES` として届ける** —— ⛔ **`src/` に打たない**（規則 03 の 1.）|
| **`R2.9`**（YAGNI）| 足す行の数 | ⭐ **1 行だけ足した。** ⛔ **`S-90` の横は新しい行にしない** —— **同じ端点の同じ掴み代であり、行を割ると 2 つの数が別々に動きうる**（`R1.3` の穴がもう 1 つ増える）|
| **`R1.2`**（曖昧表現の排除）| 足す文言 | ⭐ **「線の左右に 6px」のように、どの方向へどれだけかを書く。** ⛔ **「十分に」「掴みやすく」は書かない** |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **3 つとも同じ 6px を推奨した** | ⛔ **偶然そろえたのではない。** ⭐ **表 T-206 の `S-90` の理由欄が「掴み領域は読む人のアクセシビリティに属する。手が震える人には大きな掴み代が要る」と書いている** —— **震える手は、掴む相手が予定か実績か線かで震え方を変えない。** ⚠️ **重なったときに何が勝つかは 表 T-023d の優先順が既に決めている**ので、そろえても取り合いにならない |
| 2 | ⭐ **`S-91` と `S-137` に 🔎 を付けた** | ⛔ **由来が記録されていない値だからである**（原稿の §2 が定める印）。⚠️ **`S-90` の 6px 自体にも由来が無い** —— **その値をそろえたことにも由来が無い、と正直に印す** |
| 3 | ⛔ **`S-90` の数は変えず、掛かる方向だけを述べた** | ⭐ **横に掛かることは `S-49` が既に前提にしている。** ⚠️ **数を変えると `S-49` の下限（`minShapeWidth` の由来）が動く** —— **値の裁定が設計の裁定になる**（規則 06 の `G`）ので、数には触れない |
| 4 | ⭐ **`S-91` を `S-90` へ指させなかった**（`S-96` → `S-53` の形を採らなかった）| ⛔ **指すと 2 つが永久に同じ値になる。** ⚠️ **表 T-206 の理由欄は「文書が強制してよい値ではない」と書いており、予定と実績で別の掴み代を選ぶ余地を残すべきである** |
| 5 | **`S-137` の値欄に `GR-13` / `GR-16` を名指した** | ⭐ **`S-93` が同じ形を採っている**（「表 T-023d の `GR-9` / `GR-17` / `GR-18`」）。⚠️ **どの行のための数かが読めないと、次の担当者は流用先を発明する** |
| 6 | ⭐ **`@provisional PD-167` / `PD-168` の印は `item-hit-area.ts` の `PointerSlop` の宣言に付ける** | ⛔ **生成されたブロックには付けられない**（次の `npm run gen` で消える）。⭐ **意味を述べているのは宣言のほうであり、印はそこに在るのが正しい。** ⚠️ **検査 25 は分類 `A` の未裁定行が木のどこかに印を持つことを求める** |
| 7 | ⛔ **表 T-023d には 1 文字も足さなかった** | ⭐ **前文が既に 表 T-206 へ全部を委ねている。** ⚠️ **両方に書けば、行が増えるたびに片方が腐る**（規則 02 の 4.）|

---

## 1. 測った（⛔ 2 本とも走らせた）

```
impact.py  T-206
impact.py  T-023d
induced.py T-206 T-023d S-90 S-91 S-92 S-93 S-49 GR-3 GR-4 GR-5 GR-6 GR-13 GR-16 FR-094 LF-9
```

| 測ったこと | 実測 |
|---|---|
| 種が仕様に解決したか | **15 / 15** |
| 種の集合の内側の辺 | **2** |
| 閉路 | ⭐ **0 件。** したがって 1 つずつ書いてよい —— **ただしオブジェクトごとに 1 回である** |
| 表 T-206 を指している要求 | **4 件**（`FR-016` `FR-031` `FR-080` `FR-086`）。2 次で 6 件 |
| 表 T-206 の行数 | **17 行** → ⭐ **18 行**（`S-137` を足す）|
| 表 T-023d を指している要求 | **10 件。** ⛔ **どれも掴み代の数を書いていない** —— 全部が表を指している |
| 表 T-023d の行数 | **18 行。** ⛔ **1 行も動かさない** |
| ⭐ **塊** | 表 T-023d は「ポインタ操作」の塊に属し、同じ塊に 表 T-023 / T-023a / T-023b / T-023c / T-036 が在る。⛔ **本 CR はそのどれも書き換えない** |

⛔ **`S-137` は新設なので、既存のどの行からも指されていない。** ⭐ **指す側は 表 T-023d の前文（既にある「値は 表 T-206 が持つ」）と、`S-137` 自身の値欄が名指す `GR-13` / `GR-16` である。**

---

## 2. 何が欠けていたか（**逐語**）

表 T-206（`_assets/tbl-settings.md`）:

```
| S-90 | 予定の端点の掴み代 | バーの上下に 6px | 掴み領域は読む人のアクセシビリティに属する。… |
| S-91 | 実績の端点の掴み代 | 実績バーの帯 | 同上 |
| S-92 | フェード掴み点の当たり判定 | 15 × 15px | 同上 |
| S-93 | 実績のダミーの当たり判定（表 T-023d の GR-9 / GR-17 / GR-18） | 30 × 20px | 同上。 |
```

⛔ **`S-91` だけが数を持たない。** ⚠️ **そして「帯」は縦である。**

表 T-023d の前文（`01-04-requirements.md:2298`）:

```
上の行ほど優先すること（MUST）。掴み代と当たり判定の値は `_assets/tbl-settings.md` の表 T-206 が持つ。
```

表 T-023d の行（同）:

```
| GR-13 | 依存線 | 線の上 | 選ぶ |
| GR-16 | 基準日線 | 線の上 | 左右に動かして statusDate を変える（FR-046） |
```

⛔ **委ねた先に行が無い。** ⭐ **`GR-13` と `GR-16` は 仕様書全体で 2 回ずつしか現れず、どちらも上の 2 行そのものである** —— **許容の距離を述べた文は 1 つも無い。**

`item-hit-area.ts` が自分でそう書いている:

```
⛔ Table T-206 has NO row for this, and no other table has one either.
   … so how near "on it" is has never been settled.
```

---

## 3. 編集計画（**オブジェクトごとに 1 回**）

| # | オブジェクト | 何をするか |
|---|---|---|
| 1 | `S-90`（`_source/settings.json`）| 既定の前置きを「バーの上下と端点の左右に 」に広げる。⛔ **数は 6 のまま** |
| 2 | `S-91`（同）| 既定を「実績バーの帯と、端点の左右に 6px 🔎」にする |
| 3 | `S-137`（同。**新設**）| 「依存線と基準日線の掴み代（表 T-023d の `GR-13` / `GR-16`）」／「線の左右に 6px 🔎」 |
| 4 | `tools/generate_entity_types.py` | `NOT_STORED_SIZES` の行 ID に `S-91` と `S-137` を足す |
| 5 | `item-hit-area.ts` の `PointerSlop` | 宣言の注記を書き直し、`@provisional PD-167` / `PD-168` を付ける。⛔ **生成ブロックは触らない**（`npm run gen` が書く）|
| 6 | `pending-decisions.md` | `PD-167` / `PD-168` を足す（分類 `A`・未裁定）|
| 7 | `A-appendix.md` の変更履歴 | 1 行足す |

⛔ **同じ行を 2 度書かない。** ⭐ **1 〜 3 は同じ表の別々の行であり、閉路を共有しない。**

---

## 4. 数の予測

| | 前 | 予測 |
|---|--:|--:|
| `tables` | 129 | **129** |
| `figures` | 11 | **11** |
| `rows` | 1584 | ⭐ **1585**（`S-137` の 1 行）|
| `uids` | 146 | **146** |
| 裁定待ちの行 | 58（53 未裁定）| ⭐ **60（55 未裁定）** |
| 生成物 | 16 | **16** |
| `NOT_STORED_SIZES` の鍵 | 3 | ⭐ **5** |

---

## 5. 試験（**期待値を曲げない**）

⛔ **通る試験を足しても何も証明しない**（規則 04 の 2.）。
⭐ **足すべきは「原稿の値を 1 つ変えると落ちる試験」である**（規則 04 の 2. の受け入れ形）:

```
1. settings.json の S-137 を 6 から別の数に変えると落ちる試験
   ⭐ 依存線から その距離ちょうど 離れた点が当たり、1px 外れた点が当たらないこと
2. 同じく S-91 を変えると落ちる試験
   ⭐ 実績バーの端点から左右へ その距離 だけ離れた点が GR-5 / GR-6 を答えること
3. ⛔ 表 T-023d の優先順が保たれる試験
   ⚠️ 3 つを同じ値にしたので、重なった点で 上の行 が勝つことを見せる
```

⚠️ **書くのは実装した者ではない**（規則 04 の 1.）。

---

## 6. ⛔ 本 CR が閉じないもの（**黙って閉じたと読まれないために**）

| | 何 | どこへ |
|---|---|---|
| 1 | ⛔ **3 つの 6px に由来を与えること** | ⭐ **🔎 を付けて「由来が無い」と印した。** ⚠️ **`S-90` の 6px 自体が版 0.4 から由来を持たない** |
| 2 | ⛔ **`S-49`（`minShapeWidth`）と `S-90` の関係** | ⚠️ **`S-49` の下限は `S-90` を横幅として読んで決まっている。** ⭐ **本 CR はその読みを正しいと明文化しただけで、下限そのものは動かしていない** |
| 3 | ⛔ **`tbl-settings.md:326` が「表 T-206 の注」を引いているのに、その注が存在しないこと** | ⚠️ **踏査で見つけた別の欠陥である。** ⭐ **指しているのは `S-90` の理由欄と読める。** ⛔ **別の CR で閉じる** |
| 4 | ⛔ **`GR-14`（コメント / ハイライトボックス）の四隅の掴み代** | ⚠️ **同じ形の穴が残っている可能性がある。** ⭐ **本 CR は `PointerSlop` が要る 2 値だけを閉じる** |
