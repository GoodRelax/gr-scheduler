# CR-271 — キャンバスが既に答えている 2 項目

> **閉じるもの**: ⛔ **利用者の指摘（2026-08-27）2 件** ——
> 「**マイルストーンかどうかはキャンバスの形状を見れば分かるし、形状の切り替えは不可**」（`D-80`）／
> 「**パネルに `shapeKind` と形状変更は無用**」（`D-85`）。
> ⭐ **どちらも 期待値 は「その項目がパネルに出ない」であり、対応案は「表 T-016 からその行を落とす」である。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作った人の意図どおりに読める）。受ける目標は `GL-001`。**
⛔ **`milestone` のチェックは押しても何も起きない** —— 表 T-108 は 72 行あり、**`Task.milestone` を書く行は 0 である**（体が実測）。
⚠️ **`FR-029` は「無反応だと故障に見える」を RATIONALE の第 1 文に置いている。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 台帳 `D-80` の対応方針 | ⛔⛔ **根拠の行が誤っていた。前に立つ者の誤りである。**⚠️ **`FR-083` の MUST NOT は `shapeKind` が `SH-1`〜`SH-4` と `SH-5` を跨ぐことについての規則であって、`Task.milestone` という真偽値の話ではない。**⭐ **チェックを禁じているのは `FR-029`** —— **押しても何も起きない入口を置いてはならない** |
| **`R1.4`** | 表 T-016 の `PR-18` | ⛔ **`入力の型` が `真偽` で、`読み取り専用` の印が無い。**⭐ **`FR-006` は印の無い行を編集できるものとする**ので、**仕様だけを読む試験の体はチェックを要求してよい。**⇒ **実際に 2 件の試験がそう要求している。**⇒ **仕様を直さないかぎり、実装を直すと試験が赤くなる** |
| **`R2.19`**（驚かせない）| `PR-11` を落とす影響 | ⛔ **`FR-029` の注の例が 1 つ減る。**⭐ **`GR-10` を落としたときと同じ手当てで足りる**（同注は 2026-08-27 に一度それを済ませている）|
| **`R3.4`**（同じ算術を 2 か所に書かない）| `PR-18` を指す 2 か所 | ⚠️ **`fig-erd-detail.md` の `AT-30` と `tbl-glossary.md` の `N-1` が `PR-18` を指している。**⭐ **どちらも「描画の形とは別である」ことを言うために指しているだけで、`AT-30` 自身が同じことを既に書いている** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⛔ **`PR-17`（`milestoneGlyph`）は落とさない** | ⚠️ **利用者は名指していない。**⭐ **`FR-029` の注も例に挙げていない。**⛔ **名指されていない行まで落とすのは、指摘の範囲を勝手に広げることである** |
| 2 | ⭐ **`Task.milestone` はデータの列として残す** | ⛔ **落とすのはパネルの項目だけである。**⭐ **`AT-30` が交換相手の `Task/Milestone` を持ち、往復に要る。**⚠️ **表 T-016 が定めるのは「画面に出す項目の全数」であって、モデルの列ではない** |
| 3 | ⭐ **`PR-18` を指す 2 か所は、`AT-30` を指すように付け替える** | ⛔ **消すと「描画の形とは別である」という注意そのものが失われる。**⭐ **`AT-30` は同じことを自分の備考に書いているので、指し先を移せば足りる** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| `Task.milestone` を書く命令 | ⛔ **0 件**（表 T-108 の 72 行を数えた。体の実測）|
| その帰結 | `commandFromTaskColumn`（`input-command-translator.ts:2063`）は `Task` の 13 列に分岐を持ち、`milestone` の分岐が無い。⇒ **`default: return []`（:2108）に落ち、命令も通知も出ない** |
| 既に記録されている | ⭐ **同ファイル :2052 の `STOP`**: 「表 T-016 の 1 項目が、なる先の 表 T-108 の行を持たない」 |
| パネルが作る操作子 | **22 個。うち動かないのは `milestone` の 1 個だけ**（体の実測）|
| `PR-11` を指す箇所 | **要求 1 件 / 参照 1 箇所**（`FR-029` の注、`01-04-requirements.md:3752`）|
| `PR-18` を指す箇所 | **要求 0 件 / 参照 2 箇所**（`fig-erd-detail.md:332` の `AT-30` ／ `tbl-glossary.md:24` の `N-1`）|
| `shapeKind` の候補 | `COLUMN_SHAPES.TaskVisual.shapeKind.choices` の **5 値を絞らずに出す**。⛔ **マイルストーンのタスクでは 5 のうち 4 が `CM-20`（`edit-task.ts:988`）に拒まれ、残る 1 つは無変化である** |
| `induced.py`（`T-016` `PR-11` `PR-18` `FR-029` `FR-006` `FR-083` `FR-001`）| ⛔ **閉路 1 つ、大きさ 2: `FR-001` ／ `FR-083`。**⇒ **同じ 1 度で読んだ。どちらも編集しない** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-016 | ⛔ **`PR-18` の行を落とす** |
| 2 | 表 T-016 | ⛔ **`PR-11` の行を落とす** |
| 3 | `FR-029` の注 | ⭐ **`shapeKind` の例を外す** —— **値として編集する面が残っていない。**⚠️ **`GR-10` を外したときと同じ書き方にする** |
| 4 | `_source/erd.json` の `AT-30` | ⭐ **`PR-18` への指しを外す**（同行自身が「描画の形とは別」を書いている）。⛔ **`npm run gen` で `fig-erd-detail.md` を焼き直す** |
| 5 | `tbl-glossary.md` の `N-1` | ⭐ **指し先を `AT-30` へ付け替える** |
| 6 | `A-appendix` の変更履歴 | 1 行（`1.23`）|

⛔ **`FR-001` / `FR-083` / `FR-006` は 1 文字も動かさない**（閉路の相手として読んだうえで）。
⛔ **`PR-17` は落とさない。**⛔ **`Task.milestone` の列（`AT-30`）は落とさない。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1718 | **1716** | **−2** |
| `uids` | 148 | **148** | 0 |
| 語の数（検査 27）| 260 | **260** | 0 |

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1716 / 148 | **135 / 11 / 1716 / 148** | ✅ |
| 語の数 | 260 | **260（260 written）** | ✅ |
| 検査 22 本 | 全緑 | **ALL GREEN** | ✅ |

⚠️ **1 度だけ検査 7 が落ちた** —— `FR-029` の注に書いた「表 T-016 から `PR-11` を落とした」という説明そのものが、
**落とした行 ID を指す参照**と読まれた。⭐ **「形状の項目」と言い換えて解いた。**
⛔ **落とした行の ID を、落としたと述べる文の中で書いてはならない。**

⚠️ **本変更は仕様だけである。**`properties-panel.ts:341-359` は 表 T-016 の 18 行を手で写しており、
まだ 18 行のままである。⛔ **実装と試験は別の体が当てる**（規則 05 の 7.）。
