# CR-304 — 誰も頼んでいないのに開いていたパネルと、閉じ方の無かった名前

> **ファイル名の案**: `change-request/CR-304-the-press-that-opened-a-panel-nobody-asked-for.md`

> **閉じるもの**: 台帳 D-143（タスクを 1 回押しただけでプロパティパネルが開く）／ 台帳 D-144（ダブルクリックから名前を直して抜けるまでの流れが無い）。
> **決着する未裁定**: `PD-339`（閉じたパネルが、次の選択で戻ってくるか）—— ⭐ **`FR-006` と 表 T-023 の `MK-13` が逆を向いている、と保留の行が既に書いていた。**本 CR はその向きを 1 つに揃える。
> ⛔⛔ **本 CR は `PD-339` の「推奨」欄を覆す。**同欄は「**`FR-006` の『選ぶ』が連れ戻すことを推す**」と述べているが、**利用者の裁定（2026-08-30）はその逆である** —— 選ぶことは入口ではなくなる。⚠️ **`docs/development-records/pending-decisions.md` は本 CR では書き換えない** —— 前に立つ者が本 CR の適用と同時に閉じる。
> **見込みで閉じるもの**: 台帳 D-43（ドラッグしただけで開く）—— ⚠️ **本 CR では台帳を閉じない。**押しただけで出さなくなれば同じ原因が消えるが、**実物で測ってから閉じる。**
> **本 CR に含めないもの**: 行見出しパネルで行（`TaskGroup`）を選んだときの挙動（`FR-042`）—— ⭐ **今のままとする（利用者の裁定 2026-08-30）。**第 6 節の (a)。
> ⛔ **2 件を 1 本にまとめたのは利用者の裁定である**（2026-08-30）—— **どちらも 表 T-023 の `MK-13` の同じセルを書き換えるので、2 本に分けると 2 本目が 1 本目を知らないまま書くことになる。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

| 裁定 | 進めるもの | なぜ |
|---|---|---|
| 押しただけでは出さない（D-143）| **`CH-1` / `GL-004`**（作図ソフトと同じ操作感）| **作図ソフトで 1 回の押下が意味するのは「選ぶ」だけである。**利用者の逐語が理由をそのまま述べている ——「**ちょっと位置をずらしたいだけなのに、プロパティーパネルが開くのは鬱陶しい**」。⛔ **掴んで動かす前に必ず面が 1 枚開く道具は、作図ソフトの操作感ではない** |
| ダブルクリック → 名前 → `Enter` → `Enter`（D-144）| **`CH-4` / `GL-006`**（すぐわか）| **名前を変える道が、説明を読まずにたどれる 1 本の流れになる。**⭐ **どの道具でも「ダブルクリックで名前を直す」は同じ形である。**⚠️ **開いた面を閉じる手が `Esc` しか無い状態も同時に解ける** —— 開けた手（`Enter`）がそのまま閉じる手になる |

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.3`**（唯一の正）| パネルを出す入口 | ⛔⛔ **正が 2 つ立っていた。**`FR-006` は「作成者がタスクを**選んだ**とき（略）プロパティパネルに出し」と述べ、`MK-13` は「**プロパティパネルを開く経路を本行に置いてはならない（MUST NOT）**」と述べる。⭐ **どちらが入口かをどの行も述べていない** —— 保留の行 `PD-339` がまさにそれを保留にしており、`src/` の `showPropertiesOfChoice` も `@provisional PD-339` を付けて「これは読みであって裁定ではない」と書いている |
| **`R1.3`**（唯一の正）| `FR-029` の禁止 | ⛔ **入口が 2 か所になっていた。**同要求は「**同じ機能の入口を画面上の 2 か所に置いてはならない（MUST NOT）**」と定める。⭐ **本 CR は入口を 1 つに減らす** —— 選ぶことは入口ではなくなり、`MK-13` と `IC-17` だけが残る |
| **`R1.2`**（曖昧・非検証な表現を排除）| 利用者の逐語の「編集モードに進む」 | ⭐ **そのままでは検証できない。**⇒ 「**名称の欄（表 T-016 の `PR-1`）を編集できる状態にし、焦点を置き、既にある文字をすべて選んだ状態にすること（MUST）**」と書いた。**焦点がどこに在るかも、何が選ばれているかも機械で読める。**⭐ **全選択は利用者の裁定である**（2026-08-30。§0③ の ⅱ）|
| **`R1.4`**（異常系・準正常系）| 2 度目の `Enter` | ⛔ **3 つの場合が仕様に無かった。**①**確定していない編集が無いとき**（＝ 利用者の言う 2 度目）②**面が立っているとき**（確認の面の上の `Enter`）③**パネルを出していないとき**。⇒ ①は `SK-19` に、②は 表 T-036 の結びに書き（**利用者の裁定 2026-08-30**）、③は「出していれば」の条件で無害にした |
| **`R2.7`**（DRY）| 「閉じても選択は残る」の置き場 | ⭐ **入口ごとに書く案を退けた。**閉じる入口は `Enter`・`IC-52`・`Esc` と複数あり、入口ごとに書けば入口が増えるたびに同じ 1 文が増える。**不変条件 1 つを `FR-072` に置けば、入口を 1 つも数えない** |
| **`R2.7`**（DRY）| `SK-19` に 2 段目を足す形 | ⭐ **新しい行を立てなかった** —— **立てると `Enter` の正が 表 T-036 の 2 か所に分かれる。**⛔ **1 つの割当が 2 段を持つことは、行 1 つで書ける** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

⭐ **草稿の段階で 3 つを問い、3 つとも裁定が下りた（2026-08-30）。**⛔ **したがって本節から外してある** —— 下の表に残るのは、**問わずに決めたままのもの**だけである。

| | 問うたこと | 裁定 |
|---|---|---|
| ⅰ | 行見出しパネルの行（`TaskGroup`）も揃えるか | ⭐ **今のままとする。**押しただけでパネルが出る振る舞いを変えない ⇒ **射程は 表 T-023c の選択に限る。**⛔ **`FR-042` の行の色と高さの編集の道は 1 文字も触らない** |
| ⅱ | 名称の欄の既存の文字をあらかじめ選ぶか | ⭐ **全選択する。**理由は利用者の逐語「**容易に**タスク名を変更できるように」であり、**打ち直しが 1 手で済む。**⇒ `MK-13` の該当セルへ書き足す |
| ⅲ | 面が立っているあいだの 2 度目の `Enter` | ⭐ **当てない。**⇒ 表 T-036 の結びに書く。**根拠は 表 T-028 の `IN-4` が `Esc` で既に同じ階層を採っていることである** —— 面が立っているあいだ、閉じる手が向かう先は面であって、面の後ろのパネルではない |

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **不変条件（閉じても選ばれたまま）を `FR-072` の散文に置いた。行にしていない** | ⛔⛔ **行にすると `generate_help_roster.py` が `FR-036` のヘルプへ「利用者ができる操作」として刷る**（CR-300 が同じ穴で 1 度赤くなっている）。⭐ **`FR-072` は既に「選択を解除しても設定へは移らないこと（MUST NOT）」を持っており、対の 1 文がその隣に立つ** |
| 2 | ⭐ **マイルストーンの行を足さなかった** | ⚠️ **利用者の逐語は「タスクやマイルストーンをダブルクリック」である。**⛔ **`MK-13` の「タスク」は既にマイルストーンを含む** —— `AT-30`（`Task.milestone`）が「マイルストーンかどうかの正」であり、マイルストーンは `Task` の一形態である。**行を足すと同じ操作が表の 2 か所に並ぶ**（`MK-13` 自身が名称ラベルと本体を畳んだのと同じ理由）|
| 3 | ⭐ **`FR-006` の主語を「選んだとき」から「パネルが選択を出しているとき」へ変えた** | ⛔ **ここを変えないと D-143 は閉じない。**⚠️ **`src/` はこの 1 文を字句どおり読んで実装している**（第 5 節）—— **`MK-13` だけを書き換えても、`FR-006` の側が押下で出す道を要求し続ける。**✅ **前に立つ者が主語を実測して確かめた（2026-08-30）** |
| 4 | ⭐ **全選択を `MK-13` の既存のセルへの書き足しとし、新しい行にしなかった** | ⛔ **行にすると `rows` が動き、同じ操作が表の 2 か所に並ぶ。**⭐ **全選択は「ダブルクリックが何をするか」の一部であって、別の操作ではない** |

---

## 1. ⛔ 測った

### (a) 書き換える 4 か所の逐語（措置前）

| 対象 | 位置 | いまの逐語（抜粋）|
|---|---|---|
| 表 T-023 の `MK-13` の 動作 | `01-04-requirements.md:2308` | 「**対象ごとに定めること（MUST）** —— コメントボックス ＝ 本文の編集（`FR-097`）／タスク（名称ラベルと本体のどちらでも） ＝ 名称の編集／担当ラベル ＝ 担当者名の変更／行見出し ＝ 行名の編集。⛔ **プロパティパネルを開く経路を本行に置いてはならない（MUST NOT）**（利用者の裁定 2026-08-27）—— パネルの中身は `FR-072` が「最後に行われた操作」で決めており、選ぶ操作がすでにその操作である」 |
| `FR-072` の `STATEMENT` | `01-04-requirements.md:1417` | 「利用者が**選択**または設定の入口を操作したとき、`GRS` は、プロパティパネルに出す中身を**最後に行われた操作**で決めること。」 |
| `FR-006` の `STATEMENT` | `01-04-requirements.md:1381` | 「**作成者がタスクを選んだとき**、`GRS` は、**表 T-016 の項目**をプロパティパネルに出し、**同表が読み取り専用と記した項目を除いて**編集できるようにすること。」 |
| 表 T-036 の `SK-19` の 操作 | `01-04-requirements.md:2481` | 「**その場の編集を確定する**（名称・担当者名・行名・文書名・プロパティの入力）」／ 割当 `Enter` |

### (b) ⛔ 実装が先に穴を記録していた

⭐ `src/framework/single-html-shell/frame-loop.ts` の `showPropertiesOfChoice` が、逐語でこう書いている（`@provisional PD-339`）:

```
⛔ WHETHER IT SHOULD COME BACK IS NOT SETTLED, and this is the reading
rather than a ruling: FR-006 names the choosing and MK-13 of table T-023
makes a double click 「プロパティパネルを開く」, and no row says which of
the two is the entrance.
```

⇒ ⭐ **本 CR が答えるのは、まさにこの「どちらが入口か」である。**

### (c) ⛔ 画面に刷る語が、`MK-13` の MUST NOT と正面から食い違っていた

`_source/display-words.json` の `assignments` の `MK-13` は、いまこう刷っている:

```
ja  対象で意味が決まる。コメントボックス＝本文、名称ラベル＝名称、
    担当ラベル＝担当者名、タスク＝プロパティパネル、行見出し＝行名
en  ... Name Label: its name; Assignee Label: the resource name;
    task: the Properties Panel; row title: the row name
```

⛔⛔ **「タスク ＝ プロパティパネル」と刷っている。**⚠️ **`MK-13` は 2026-08-27 から「その経路を置いてはならない（MUST NOT）」と定めており、ヘルプは 3 日ぶん古い規則を画面に出し続けていた。**⭐ **検査は 1 本も赤くならない** —— 生成器は行 ID の集合だけを突き合わせ、語の中身を仕様の文と照らさない。

⇒ ⭐ **本 CR で語は正しくなる**が、**それは偶然である。**⚠️ **語と行の食い違いを見る目は、いまどこにも無い**（第 6 節の (c)）。

### (d) ⛔ 触る前に測った 2 つ（規則 02 の要求どおり）

```
impact.py MK-13 SK-19 FR-072 IN-4

  行 MK-13 : 表 T-023。塊「ポインタ操作」
      指している箇所 = 要求 4 件 / 参照 7 箇所
        FR-091:1133   FR-008:1538   FR-016:2331   FR-016:2332
        FR-097:3480   FR-097:3484   A-appendix.md:146

  行 SK-19 : 表 T-036。塊「ポインタ操作」
      指している箇所 = 要求 1 件 / 参照 2 箇所
        FR-040:4003（IN-6）        A-appendix.md:125

  FR-072   : 指す先 = 表 T-016 ／ 指す行 = なし
      指している箇所 = 要求 2 件 / 参照 10 箇所
        FR-009:1838   FR-016:2306   FR-016:2308
        05-07-design.md:308        05-07-design.md:997
        A-appendix.md:99 / 146 / 152
        tbl-glossary.md:465（IC-17）  tbl-settings.md:269（S-171）

  行 IN-4  : 表 T-028
      指している箇所 = 要求 6 件 / 参照 23 箇所
```

```
impact.py FR-006 MK-11

  FR-006   : 指す先 = 表 T-016 T-206 T-061 T-215 T-201 T-019
             指す行 = PR-3 PR-14 S-186 S-193 S-197 S-198 S-189
                      S-199 PR-12 S-30 S-188 S-3 S-8
             塊「設定値の相互依存」に触れる
             塊「予実と遅れ」に触れる
      指している箇所 = 要求 2 件 / 参照 28 箇所（FR-011:1935  FR-029:3780 ほか）

  行 MK-11 : 表 T-023
      指している箇所 = 要求 2 件 / 参照 2 箇所（FR-083:994  FR-081:1640）
```

```
induced.py MK-13 T-023 SK-19 T-036 FR-072 FR-006 IN-4 IN-6 T-028 MK-11
           FR-091 T-016 PR-1 S-99h S-171 S-80 FR-052 IC-52 IC-17 T-109 FR-036

  seeds in the specification : 21 of 21
  edges inside the seed set  : 28
  cycles in the induced graph: 1
     size 3 : S-171 S-80 S-99h
```

⭐ **輪は 1 つだけで、その 3 つはどれも値の行である** —— `S-80`（文書が保つ幅）・`S-171`（出すときに置く幅）・`S-99h`（出しているか）。
⛔ **本 CR はこの 3 つを 1 つも書き換えない。**⇒ **輪は触らないことで避ける。**
⚠️ **3 つとも 2026-08-30 の裁定で書かれたばかりで、本 CR が要る前提を既に持っている** —— **`S-99h` が「出しているか」を幅と別に持っているからこそ、「押しただけでは出さない」が書ける。**

⇒ **触る objects は 2 つの束に分かれ、objects を 1 つも共有しない。**

| 束 | objects | 何を書くか |
|---|---|---|
| 1 | 表 T-023 の `MK-13`・`FR-072`・`FR-006` | パネルを出す入口（D-143 と D-144 の前半）|
| 2 | 表 T-036 の `SK-19` と同表の結び | `Enter` の 2 段目（D-144 の後半）|

⚠️ **束 1 の 3 つは 1 つの計画で 1 度に書く** —— **`MK-13` が入口を持ち、`FR-072` が出す規則を持ち、`FR-006` が中身を持つ。**⛔ **順に書くと、2 つ目が 1 つ目の書いた入口を知らないまま「選んだとき」を残す。**

---

## 2. 編集計画

⛔ **オブジェクトごとに 1 回。同じセルを 2 度書かない。**

| # | 対象 | 何を書くか | 台帳 |
|---|---|---|---|
| 1 | `01-04-requirements.md` 表 T-023 の `MK-13` | タスクの行き先を「名称の編集」から「**プロパティパネルを出し、名称の欄（表 T-016 の `PR-1`）を編集できる状態にして焦点を置き、既にある文字をすべて選んだ状態にすること（MUST）**（利用者の指示と裁定 2026-08-30）」へ。⛔ **2026-08-27 の MUST NOT を落とし、それが覆されたことを日付つきで書く。**⚠️ **全選択は同じセルへの書き足しであって、新しい行ではない。**⚠️ **コメントボックス・担当ラベル・行見出しの 3 つは 1 文字も動かさない** | D-143 / D-144 |
| 2 | 同 `FR-072` の `STATEMENT` | 主語を「利用者が**選択または**設定の入口を操作したとき」から「**パネルを出す入口を操作したとき**」へ。⛔ 「**表 T-023c の選択が動いたことだけを理由に、パネルを出し始めてはならない（MUST NOT）**」を足し、出す入口が `MK-13` と `IC-17` であることを名指す。⛔ 「**出すのをやめても、選択を解いてはならない（MUST NOT）**」を足す（裁定 ①）。⚠️ **出しているあいだ中身が最後の操作で決まることは 1 文字も変えない** | D-143 / D-144 |
| 3 | 同 `FR-006` の `STATEMENT` | 「**作成者がタスクを選んだとき**」を「**プロパティパネルが選択を出しているとき**」へ。⚠️ **表 T-016 の項目を出すことと、読み取り専用を除いて編集できることは 1 文字も変えない** | D-143 |
| 4 | 同 表 T-036 の `SK-19` | 操作 の欄に 2 段目を足す ——「**確定していないその場の編集が 1 つも無いときは、プロパティパネルを出しているならば出すのをやめること（MUST）**」（裁定 ② を含む: 焦点が名前の欄の外でも同じ）。⛔ **新しい行を立てない** | D-144 |
| 5 | 同 表 T-036 の**結び** | 射程を 1 文 ——「**面（`S-99g`）が立っているあいだ、`SK-19` の 2 段目を当てないこと（MUST）**」（利用者の裁定 2026-08-30）。**根拠として 表 T-028 の `IN-4` を添える** —— 同行は `Esc` の第 1 階層を「開いている面」へ既に与えており、**面が立っているあいだ、閉じる手が向かう先は面である。**⛔ **行にしない**（§0③ の 1 と同じ理由）| D-144 |
| 6 | `_source/display-words.json` | `assignments` の `MK-13` と `shortcuts` の `SK-19` の `ja` / `en` を書き直す。⛔ **組を 1 つも足さず、1 つも消さない**（第 3 節）。⚠️ **生成物 `src/adapter/screen-renderer/display-words.json` は手で書かない** —— `npm run gen` で刷り直す | D-143 / D-144 |
| 7 | `A-appendix.md` 変更履歴 | **版 1.58** を 1 行。⛔ **既に使われている最大値を数え直した（2026-08-30）—— 履歴は 157 行、最大は 1.57 である。**⚠️ **表は昇順でも降順でもない**（1.57 の下に 1.56 と 1.55 が並ぶ）ので、**末尾を見るだけでは max にならない。**⛔ **過去の行を書き換えない。台帳の行 ID を backtick で書かない**（検査 7）。⚠️ **行の中にリテラルの縦棒を書かない** | — |
| 8 | ⛔ **`docs/development-records/pending-decisions.md` は本 CR では触らない** | `PD-339` は**前に立つ者が本 CR の適用と同時に閉じる。**⭐ **答えは「戻ってこない」** —— 出す入口は `MK-13` と `IC-17` の 2 つで、選び直すことは入口ではない。⛔⛔ **これは同行の「推奨」欄を覆す**（同欄は「`FR-006` の『選ぶ』が連れ戻すことを推す」と述べている）| — |
| 9 | 仕様の外 —— `docs/development-records/defects.md` | D-143 / D-144 の状態を進め、**覆した裁定の日付（2026-08-27 → 2026-08-30）を残す。**⛔ **過去の逐語を消さない** | — |

⚠️ **`_assets/tbl-settings.md` は触らない** —— `S-80` / `S-171` / `S-99h` を 1 つも書き換えないので、刷り直しも起きない。

### 書く逐語（案）

⛔ **貼る前に、置き先の行の前後を読むこと。**⚠️ **CJK の直後に強調を開いてその直後にコードスパンを置かない**（規則 02 の罠）。

```
[1] 表 T-023 の MK-13 の 動作 の欄（置き換え）

対象ごとに定めること（MUST） —— コメントボックス ＝ 本文の編集（FR-097）／
タスク（名称ラベルと本体のどちらでも） ＝ プロパティパネルを出し、名称の欄
（表 T-016 の PR-1）を編集できる状態にして焦点を置き、既にある文字をすべて
選んだ状態にすること（MUST）（利用者の指示と裁定 2026-08-30）—— 全選択とするのは、
利用者の逐語が「容易にタスク名を変更できるように」であり、打ち直しが 1 手で
済むからである／担当ラベル ＝ 担当者名の変更／行見出し ＝ 行名の編集。
⛔ 本行にパネルを開く経路を置いてはならないとした 2026-08-27 の裁定は、
2026-08-30 の指示が覆した。⚠️ 名称ラベルと本体を 1 つに畳んだのは、NL-1 が
ラベルを形状の中に描くからである —— 2 行に分けると同じ操作が表の 2 か所に並ぶ

[2] FR-072 の STATEMENT（主語の置き換えと、2 文の追加）

利用者がプロパティパネルを出す入口を操作したとき、GRS は、パネルに出す中身を
最後に行われた操作で決めること。
（既存の文はそのまま）
⛔ 表 T-023c の選択が動いたことだけを理由に、パネルを出し始めてはならない
（MUST NOT）（利用者の指示 2026-08-30）—— 出す入口は 表 T-023 の MK-13 と
IC-17 の 2 つである。⚠️ 出しているあいだは、選択が動けば中身がそれに移る ——
上の「最後に行われた操作」がそのまま当たる。
⛔ パネルを出すのをやめても、選択を解いてはならない（MUST NOT）
（利用者の裁定 2026-08-30）。

[3] FR-006 の STATEMENT（主語の置き換え）

プロパティパネルが選択を出しているとき、GRS は、表 T-016 の項目を出し、
同表が読み取り専用と記した項目を除いて編集できるようにすること。

[4] 表 T-036 の SK-19 の 操作 の欄（書き足し）

その場の編集を確定する（名称・担当者名・行名・文書名・プロパティの入力）。
確定していないその場の編集が 1 つも無いときは、プロパティパネルを出している
ならば出すのをやめること（MUST）（利用者の指示 2026-08-30）—— 焦点が名称の欄
の外にあるときも同じである。

[5] 表 T-036 の結び（1 文の追加）

Confirmation（U-55）または ScreenState が持つ面（S-99g）が立っているあいだ、
SK-19 の 2 段目を当てないこと（MUST）（利用者の裁定 2026-08-30）—— 表 T-028 の
IN-4 が Esc の第 1 階層を「開いている面」へ既に与えており、面が立っているあいだ、
閉じる手が向かう先は面であって、面の後ろのパネルではない。
⚠️ 1 段目（その場の編集の確定）は当てたままである —— 面の中にも入力欄が在る。
```

---

## 3. 数の予測

| | 措置前（実測 2026-08-30）| 予測 | 由来 |
|---|--:|--:|---|
| `tables` | 135 | **135** | 表を 1 つも増やさない |
| `figures` | 11 | **11** | 図に触らない |
| `rows` | 1729 | **1729** | ⛔ **行を 1 つも足さない。**⭐ **`MK-13` と `SK-19` はセルの書き換えであり、行の追加ではない。**⚠️ **表 T-036 の結びは行ではない。**✅ **全選択の 1 文を足したあとで数え直しても同じである** —— **同じセルの中に書くからである** |
| `uids` | 150 | **150** | 要求も目標も増やさない |
| 語（`display-words.json`）| 369 | **369** | ⭐ **2 つの `text` を書き直すだけで、組は 1 つも増減しない** |
| アイコン | 85 | **85** | ⛔ **新しい入口を 1 つも起こさない** —— 出すのは `MK-13` と `IC-17`、閉じるのは `IC-52` と `Enter` であり、**画面上の入口は既に全部在る** |
| 設定値の行 | — | ⭐ **増減なし** | `S-80` / `S-171` / `S-99h` は 2026-08-30 に書かれたばかりで、**本 CR が要る 3 つを既に持っている** |

⭐ **`figures` と `uids` が動かないことは、本 CR が「新しいものを作る」変更ではなく「入口を 1 つ減らし、`Enter` に 1 段足す」変更であることの記録である。**

✅ **措置前の 4 つは 2 度測った（草稿の時点と、3 件の裁定を織り込んだあと）。**⭐ **どちらも `tables=135  figures=11  rows=1729  uids=150` で一致した。**

---

## 4. 落ちる／緑になる試験

| | 予測 |
|---|---|
| `tests/unit/t-023d-double-click-only-rows.test.ts` | ⛔⛔ **確実に落ちる。**同ファイルは `MK_13_FORBIDDEN_ACTION` という定数に `openPropertiesPanel` を置き、「`MK-13` がパネルを開く道であってはならない」を直接主張している。⭐ **落ちるのが正しい** —— **仕様が変わったことの証拠である。**⛔ **緩めずに、新しい MUST を主張する形へ書き直す** |
| `tests/unit/t-225-choosing-who-is-on-a-task.test.ts` の 951 行目 | ⛔ **落ちる見込み。**`openPropertiesPanel` でないことを主張している。⚠️ **担当ラベルのダブルクリックは担当者名の変更のままなので、この 1 件は担当ラベルについては今も真である可能性がある** —— **どの対象についての主張かを読んでから直す** |
| `tests/unit/fr-006-panel-close-entrance.test.ts` | ⚠️ **落ちる見込み。**同ファイルは `S-99h` と `FR-072` の両方を読んでおり、**「どの操作が出すか」を主張していれば落ちる** |
| `SK-19` の 2 段目 | ⭐ **新しい試験が要る。**⛔ **`Enter` を 2 度打つ流れを 1 本で測ること** —— 1 打目で確定し、2 打目で出すのをやめ、**選択が残っている**ことまで見る（裁定 ①）|
| `MK-13` の全選択 | ⭐ **新しい試験が要る。**⛔ **焦点が名称の欄に在ることだけでは足りない** —— **既にある文字がすべて選ばれていることまで主張すること。**⚠️ **選ばれているかは宿主の入力欄が持つので、e2e 側に置く** |
| 面の上の `Enter` | ⭐ **新しい試験が要る**（結びの射程）。⚠️ **面を立てたうえで押す形なので、単位試験より e2e が向く** |
| 検査 28 本 | ⚠️ **`generate_display_words.py` と `generate_help_roster.py` を必ず走らせること** —— **どちらも 表 T-023 と 表 T-036 の行 ID を読む。**⭐ **行 ID は 1 つも動かさないので通る見込みだが、`MK-13` のセルが新たに `PR-1` を名指すので検査 8 が見る** |
| 検査 12（値の表に規則を書いていないか）| ⭐ **当たらない。**⛔ **`S-99h` の備考に MUST を 1 文字も足さない** —— 規則は `FR-072` と `SK-19` が持つ |

---

## 5. 実装への影響（⛔ 本 CR は直さない。場所を名指すだけである）

⭐ **台帳 D-143 の「実装の場所は `frame-loop.ts` の 1 行」は正しかった。**読んで確かめた（2026-08-30）。

| # | 場所 | 何が在るか |
|---|---|---|
| 1 | `src/framework/single-html-shell/frame-loop.ts` の 6087 行目 | ⛔⛔ **これが台帳の言う 1 行である** —— 選択が動いたかどうかを同一性で比べ、動いていれば `showPropertiesOfChoice()` を呼ぶ。**選択が動いたことだけを理由にパネルを出している。**⚠️ 直上のコメントが `FR-072` の逐語を引いて理由を書いている |
| 2 | 同 5652 〜 5668 行目 | `showPropertiesOfChoice()` の本体。**しまわれていた印を降ろし、出すものを「選択」にし、覚える対象を置く** の 3 行。⭐ **`@provisional PD-339` が付いており、「これは読みであって裁定ではない」と本文が書いている。**✅ **本 CR が `PD-339` に答えるので、直すときにこの印は剥がれる** —— ⛔ **印を剥がすのは仕様が確定してからであり、本 CR は `src/` を触らない** |
| 3 | 同 5546 行目 | ⚠️ **行見出しパネルで行を選んだときにも同じ関数を呼んでいる**（`@provisional PD-142`）。⛔ **本 CR の射程外である**（§0③ の 1）—— **触ると `FR-042` の編集の道が消える** |
| 4 | 同 5466 〜 5474 行目 | ⛔ **`openPropertiesPanel` の分岐が「削除された」ことを、コメントが理由つきで残している。**⇒ **本 CR が通れば、ここへ戻すのが直しである** |
| 5 | `src/adapter/input-command-translator/input-command-translator.ts` の 544 〜 550 行目 | 同じく **`openPropertiesPanel` という入力動作の種が削除された記録。**⭐ **その場の編集を求める種は 543 行目に在り、その行き先の型（432 行目）が `taskName` を持つ** —— **名前を直す配管は在る。行き先をパネルの欄へ替えるだけである** |
| 6 | 同 `frame-loop.ts` の 5460 〜 5464 行目 | ⛔ **`STOP` —— 「`SK-9` / `MK-13` のためのその場の編集器が存在しない」。**⭐ **本 CR が通れば `MK-13` はその場の編集器を要さなくなる**（行き先がパネルの欄になる）。⚠️ **`SK-9`（文書名）と `FR-091` の「作った直後」は要したままである** |
| 7 | `src/entity/layout-engine/item-hit-area/item-hit-area.ts` の 112 / 297 / 516 行目 | ⭐ **ダブルクリックの配管は既に在る** —— 押下の解像度が押しと二度押しの 2 値を持ち、行の到達性が「二度押しだけ」を持ち、押しのときに「二度押しだけ」の行を飛ばす 1 行が在る |

⛔ **本 CR は `src/` を 1 バイトも変えていない。**⭐ **直しは仕様が確定してからである。**

---

## 6. ⛔ 決めずに残したもの

### (a) ✅ 行見出しパネルの選択は、いまもパネルを出す（`FR-042`）—— **裁定済み**

⭐ **利用者の裁定（2026-08-30）: 今のままとする。**⛔ **本 CR は 1 文字も触らない。**
⚠️ **結果として、タスクは押しても出ず、行は押すと出る。**⛔ **不揃いに見えるが、根拠は在る** —— **`SL-1` は行を描画領域の選択から外しており**（`FR-085` が「表 T-023c が定める描画領域の選択とは別の集合である」と明記）、**行の色と高さを編集する道は `FR-042` の選択しか無い。**射程を広げると編集の道が消える。
⇒ ⭐ **本 CR の MUST NOT が 表 T-023c の選択だけを縛るのは、この裁定を字句で担うためである。**

### (b) ⚠️ `FR-091` の「作った直後」は、いまもその場の編集を要求している

同要求は「**作った直後に入力できること（MUST）** —— （略）**その場で編集する形**とし」と定める。
⭐ **本 CR はここを 1 文字も変えていない。**⇒ **`MK-13` はパネルへ、作った直後はその場へ、という 2 つの形が残る。**
⚠️ **`FR-029` の禁止には当たらない** —— 同要求は「同じ値を、画面上で直接掴む面と、値として編集する面の両方から編集できること」を明示的に対象外としている。
⛔ **ただし `src/` にはその場の編集器が 1 つも無い**（第 5 節の 6）ので、**今日の実物はどちらの形も満たしていない。**

### (c) ⛔ 繰り越し —— 画面に刷る語が 3 日間まちがっていて、検査 28 本が 1 本も捕まえなかった

⭐ **記録**（前に立つ者が確かめた。2026-08-30）: `_source/display-words.json` の `MK-13` は **2026-08-27 から 2026-08-30 までの 3 日間、「タスク ＝ プロパティパネル」と刷り続けていた。**同じ期間、`MK-13` 自身は「**その経路を本行に置いてはならない（MUST NOT）**」と定めていた。
⛔⛔ **検査 28 本は 1 本も赤くならなかった。**⭐ **生成器が突き合わせるのは行 ID の集合だけで、語の中身を仕様の文と照らさないからである。**
⭐ **本 CR で、この文は結果として正になる。**⛔ **それは本 CR が語を直したからであって、食い違いを見る目ができたからではない。**
⚠️ **同じ穴がほかの行にも在る見込みである。**⛔ **本 CR では数えない** —— **数えるなら全数であり、それは別の巡である。**

### (d) ✅ `Esc` は今のまま（利用者の裁定 2026-08-30）—— **裁定済み**

⛔ **表 T-028 の `IN-4` を 1 文字も触らない。**⭐ **本 CR が `IN-4` を引くのは 表 T-036 の結びの根拠としてだけである**（面が立っているあいだ、閉じる手が向かう先は面である）。
⚠️ **ただし、測って見つけた食い違いを 1 つ記録しておく** —— `S-99h` の備考は「パネルは（略）**`Esc` の第 1 階層でも閉じない**（表 T-028 の `IN-4` が別の段を与えている）」と述べるが、**`IN-4` の階層の並びにパネルの段が 1 つも見当たらない。**`IC-52` の面の欄には `Properties Panel` が在り、`src/` にもパネルを閉じる `Esc` の段が在る。
⇒ ⭐ **本 CR では触らない**（裁定 ③）。**別の巡で測ること。**
