# CR-409 — 表示の倍率は、いまの 75% の見た目を 100 と数え直す

> **閉じるもの**: 利用者の裁定 `JDG-152`（2026-09-17 の一通目・三通目・六通目・七通目・八通目）と、`JDG-160` の軽い問い a・k を仕様へ着地させる。逐語は `docs/development-records/rulings.md` の `JDG-152` / `JDG-160` が持つ。
> - 七通目: 「ところで現在の75％をデフォルトの倍率ってことにするだろ？  それって仕様上は100%にするよね？」「つまり、描画しているサイズを小さくした後の結果を100%と定義する。  ...意味わかる？」
> - 八通目: 「新しい 100% を中心にした、倍率の段の一覧: 提案通りでOK.」「100% のときの描く比を 0.5 ちょうどにしてよい」「3. 古い数え方で保存した文書の倍率: 読み替えは無用。」
> - 六通目: 「125 / 150 で保存した文書は、古い GRS では開けなくてOK。 まだ事実上、非公開だし。」
>
> ⭐ **本書は仕様だけを当てる。** 表 T-202 の `S-234` の段を 50 / 67 / 75 / 90 / 100 / 110 / 125 / 150 / 175 / 200、既定を 100 にし、表 T-206 の `S-236` を 0.5 にし、描く比から導いた散文（`FR-039`・`FR-077`・表 T-252 の `DS-1`）を数え直した。読んだ木は `1fe521ca`。
> ⚠️ `CR-409`〜`CR-416` の 8 本は、`induced.py` が 19 員の閉路（第 3 節）を返したので、1 つの計画で 1 度に当てた。各オブジェクトは 1 度だけ書いてある。
> ⚠️ `src/`・`tests/`・`tools/` は触っていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（全体日程を 1 画面で見られる。`CH-2` の受ける目標は `GL-002`）—— 既定の見た目を、利用者が一覧性を確かめた大きさ（いまの 75%）に置き直す。⭐ **`GL-006`**（`CH-4` の受ける目標は `GL-006`）—— 段の数字がブラウザのズームの段（100 を中心に 10 段）と同じ形で読める。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— `FR-039` の「倍率 100 で出荷ビルドの 2/3、既定の 50 でその 1/3」、`FR-077` の「出荷ビルドの 1/3」、表 T-252 の `DS-1` の「現行の 1/3」、表 T-206 の `S-235` の「数が同じだけで」（`S-236` と同じ 0.6667）が、新しい定義で偽になる。4 か所とも数え直した。
- **`R1.4`（異常系・準正常系）** —— 段の一覧の外の値を持つ文書（旧い 33 / 66 / 85 など）を、近い段へ読み替えない（MUST NOT）と書いた。拒む道は既にある（第 1 節）。
- **`R1.3`（唯一の正がある）** —— 段は `S-234` の型の欄だけが持つ。`FR-039` は段を数え上げない。
- **`R1.2`（非検証な表現を排す）** —— 描く比の例（100 → 0.5、200 → 1）に式を添え、読む人が数え直せるようにした。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 軽い問い a を「文書を開いたときは、文書が保存している `S-234` の値で描き、既定の 100 に置き換えない（MUST）」と書く | ⭐ `JDG-160` の a「既に保存した倍率はそのまま開く」と、`JDG-152` の「読み替えは無用」。⭐ 保存値は初期値（`FR-039` の RATIONALE）| 無し |
| 決定 2 | 段の一覧に無い値を近い段へ読み替えることを禁じ（MUST NOT）、その文書は列挙の外として文書ごと拒まれる、と既にある道を指す | ⭐ 八通目「読み替えは無用」。⭐ Chapter 6.1 の 表 T-220 の前文が列挙をスキーマに強制させ、`GRS JSON` を読む路で走らせる（MUST）ので、新しい規則を足さない | 旧い 33 / 66 / 85 で保存した文書は開けない（事実上非公開なので、手元の文書は開き直して保存し直す —— `JDG-152` の読み）|
| 決定 3 | `S-236` を 0.5 と書き、注に「2/3 × 3/4 ＝ 1/2、丸めていない」と式を添える | ⭐ 八通目「0.5 ちょうど」 | 無し |
| 決定 4 | `S-235`（0.6667）は変えない。注の「数が同じだけで」を「既に違う」に直す | ⭐ `JDG-152` は `S-236` だけを名指す。`S-235` はヘッダーとパレットの 2/3（`JDG-122`）| 無し |
| 決定 5 | `FR-039` の行見出しパネルの床の段の数（深さ 5 の条件）は、`CR-414`（入口の隙間）と同じ 1 回で数え直す | ⭐ 式が描く比と入口の外形の両方を読むので、片方ずつ書くと 2 度書くことになる（規則 02 の 5）| 本書だけを読むと、その段の数え直しが見えない（`CR-414` の第 4 節）|

---

## 1. 読んで確かめた事実

| 事実 | 所（`1fe521ca`） |
|---|---|
| 段と既定の元は `S-234` の型の欄と既定の欄（`33` / `50` / `66` / `75` / `85` / `100`、既定 `50`）| `docs/spec/_source/settings.json:1684`〜`:1690`（当てる前）|
| 生成物: 刊行物の表、文書スキーマの列挙 | `docs/spec/_assets/tbl-settings.md:170`、`docs/spec/_source/grs-document.schema.json:320`〜`:329`（当てる前）|
| `S-236` ＝ 0.6667（「2/3 を小数第 4 位へ丸めた値」）| `settings.json:3320`〜`:3329`（当てる前）|
| 腐る文: 「倍率 100 で 2026-09-16 の出荷ビルドの 2/3、既定の 50 でその 1/3」 | `docs/spec/01-04-requirements.md:6265`（当てる前。`handoff.md` §2.8 ③ の 8 は 6153 行と書くが、`CR-405`〜`CR-408` のあとで 6265 行へ移っていた）|
| 腐る文: 深さ 5 の条件「描く比 0.6667（表示の倍率 100）でだけ等号で成り立ち、既定の 50 では …」 | 同 `:6294`（当てる前。`handoff.md` は 6182 行）|
| 腐る文: `FR-077`「既定の倍率で字を … 出荷ビルドの 1/3 まで」、表 T-252 の `DS-1`「現行の 1/3」| 同 `:4150`、`:6281`（当てる前）|
| 列挙の外の値は文書ごと拒む（`RS-25` → `NT-1`）| `docs/spec/05-07-design.md:964`〜`:971`（表 T-220 の前文）。`src/adapter/document-codec/json-codec.ts:867` が列挙を持つ（読むだけ）|
| 段の並びの写しが生成の区画の外に手書きで在る | `src/entity/document-model/document-settings/document-settings.ts:419`〜`:421`（`DISPLAY_SCALE_STEPS`、読むだけ）|
| 試験が段と既定を直書きしている | `tests/unit/cr-394-the-chart-has-a-display-scale.test.ts:254`〜`:259`、`tests/unit/cr-400-a-held-panel-stops-at-its-floor.test.ts:130`〜`:135`（読むだけ）|

## 2. 測った

描く比 ＝ `S-234` ÷ 100 × `S-236`（`FR-039`）。

| `S-234` | 50 | 67 | 75 | 90 | 100 | 110 | 125 | 150 | 175 | 200 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 描く比 | 0.25 | 0.335 | 0.375 | 0.45 | 0.5 | 0.55 | 0.625 | 0.75 | 0.875 | 1 |

- 旧い 75 の描く比は 0.75 × 0.6667 ＝ 0.500025 —— 新しい 100 の 0.5 との差 0.000025 は丸めの名残である（`JDG-152`）。
- 旧い 50（0.33335）は新しい 67（0.335）に最も近い。

## 3. グラフ

**当てる前**: `impact.py S-234` —— 指す要求 6（`FR-099` / `FR-051` / `FR-053` / `FR-080` / `FR-025` / `FR-039`）。`S-236` —— `FR-039` だけ。表 T-252 —— 指す要求 6、2 次 27。
**`induced.py`**（8 本の CR が触る 53 種。8 本とも同じ実行）: 53 種すべて解決、辺 114、閉路 2 —— 大きさ 2（`S-90` `S-91`）と、大きさ 19（`FR-016` `FR-018` `FR-029` `FR-039` `FR-077` `FR-094` `HF-19` `HF-5` `LF-3` `RS-51` `S-12` `S-138` `S-141` `S-17` `S-234` `S-235` `S-236` `S-237` `S-79`）。⭐ 19 員の閉路が本書と `CR-410`〜`CR-414` にまたがるので、8 本を 1 つの計画で 1 度に当てた。
**届いた行を `rulings.md` で引いた**（`S-234` / `S-235` / `S-236` / `DS-1`）: `JDG-122`（既に `JDG-152` で覆された）、`JDG-123`（「倍率 100% で現行の 2/3、既定の 50% で現行の 1/3」の読み —— 数え方が `JDG-152` で変わった）、`JDG-131`（行見出しパネルの幅にも倍率を掛ける。本書は変えない）。衝突 0。
**当てたあと**（8 本を当てた木で）: `induced.py` に新しい員（`DS-10` `T-259` `T-260` `T-261` `S-243` `S-244` `SK-22` `SK-23`）を足した 53 種 —— 53 種すべて解決、辺 150、閉路 1: 大きさ 28（`DS-1` `DS-10` `FR-016` `FR-029` `FR-039` `FR-077` `FR-094` `HF-19` `HF-5` `LF-3` `S-11` `S-12` `S-138` `S-141` `S-17` `S-18` `S-234` `S-235` `S-236` `S-237` `S-243` `S-7` `S-79` `S-90` `S-91` `SK-17` `SK-22` `SK-23`）。⭐ 員はすべて 8 本の 1 つの計画の 1 巡で書いた。`impact.py`: 表 T-259 —— 指す要求 3、2 次 19。表 T-260 —— 指す要求 1、2 次 18。表 T-261 —— 指す要求 1、2 次 2。`S-243` —— 指す要求 3、参照 11。`S-244` —— 指す要求 1。`SK-22` / `SK-23` —— 指す要求 1。`DS-10` —— 指す要求 2。数の突き合わせは第 5 節。

## 4. 当てた中身

| 対象 | 置き場（当てたあと） | 中身 |
|---|---|---|
| `S-234` | `docs/spec/_source/settings.json`（→ `_assets/tbl-settings.md:170`）| 型の欄 `50` / `67` / `75` / `90` / `100` / `110` / `125` / `150` / `175` / `200`、既定 `100`、注に「100 は出荷ビルドの 1/2、200 は同じ大きさ」と「開いたときと段の外の値は `FR-039`」|
| `S-236` | 同（→ `:358`）| 0.5。注に 2/3 × 3/4 ＝ 1/2 と、100 → 0.5・200 → 1・50 → 0.25 |
| `S-235` の注 | 同（→ `:357`）| 「数が同じだけで」を「本値 0.6667 と `S-236` の 0.5 は既に違う」に |
| `FR-039` の描く比の文 | `docs/spec/01-04-requirements.md:6297` | 例を「既定の 100 で 1/2（100 ÷ 100 × 0.5）、200 で出荷ビルドと同じ大きさ（200 ÷ 100 × 0.5 ＝ 1）」に |
| `FR-039` の開くときの 2 文 | `:6303`〜`:6304` | 保存した `S-234` で描く（MUST）、段に無い値を読み替えない（MUST NOT）|
| 表 T-252 の `DS-1` の理由 | `:6329` | 「現行の 1/3」を「出荷ビルドの 1/2」に |
| `FR-077` | `:4179` | 「出荷ビルドの 1/3」を「1/2 … `S-236` の 0.5」に |
| 文書スキーマの列挙 | `docs/spec/_source/grs-document.schema.json` の `displayScale`（`erd_json_to_schema.py` で刷り直した）| 10 段 |

## 5. 数の予測

当てる前 `tables=157 figures=11 rows=1997 uids=153`（`check.sh` の実測）。本書の差: tables 0 ／ figures 0 ／ rows 0 ／ uids 0。
⭐ 8 本（`CR-409`〜`CR-416`）を合わせた予測は `tables=160 figures=11 rows=2018 uids=153` —— 当てたあとの `check.sh` の実測と一致した。
⭐ 本書が足した `（MUST）` は 1、`（MUST NOT）` は 1（`FR-039` の開くときの 2 文）。数え方: 当てた文を 1 文ずつ読み、足した印を数えた（8 本の合計は `CR-416` の第 5 節）。

## 6. 影響

### 6.1 仕様

| 所 | 本書で |
|---|---|
| `FR-051` / `FR-053`（ヘッダーとパレットに表示の倍率を掛けない）| そのまま |
| `FR-080`（書き出しは表示の倍率に従う）| そのまま —— 既定の書き出しは描く比 0.5 で出る |
| `FR-016` の字の側の上限 | そのまま —— 両方の字に同じ比が掛かる（`FR-016` の段）|
| `FR-039` の行見出しパネルの床の段（深さ 5 の条件）| `CR-414` が数え直した |

### 6.2 記録

| 所 | 書いたこと |
|---|---|
| `docs/development-records/rulings.md` の `JDG-152` | 状態を 適用済 へ、着地先に本書と行 ID を足した |
| `docs/development-records/rulings.md` の `JDG-160` | a と k の着地を足した（第 8 節）|
| `docs/development-records/changelog.md` | 本書の 1 行 |

### 6.3 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `npm run gen` | `SETTINGS_DEFAULTS['displayScale']` 50 → 100、`NOT_STORED_DISPLAY_SCALE_BASE['S-236']` 0.6667 → 0.5、`json-codec.ts` の列挙、`startup-template.json` の `displayScale`、`display-words.json` の写し |
| `src/entity/document-model/document-settings/document-settings.ts:419`〜`:421` | 手書きの `DISPLAY_SCALE_STEPS` を 10 段にする（⚠️ 生成の区画の外。生成器が刷らない）|
| `tests/unit/cr-394-the-chart-has-a-display-scale.test.ts:254`〜`:259` | 段の一覧 `[33, 50, 66, 75, 85, 100]` と既定 `50` を直書きしている ⇒ 10 段と `100` を `S-234` の型の欄と既定から読む形へ |
| `tests/unit/cr-400-a-held-panel-stops-at-its-floor.test.ts:130`〜`:135` | `[50, 100]`（既定と最上段）を直書きしている ⇒ 既定 `100` と最上段 `200` を表から読む形へ |

## 7. 試験が主張すべきこと

⭐ 名前は本書のとおりに写すこと: `S-234`（段 `50` / `67` / `75` / `90` / `100` / `110` / `125` / `150` / `175` / `200`、既定 `100`）、`S-236`（`0.5`）。

1. `S-234`: 生成された既定値が 100 で、段の並びが型の欄の 10 段と同じ順である。
2. `FR-039`: 描く比が `S-234` ÷ 100 × `S-236` —— 100 で 0.5、200 で 1、50 で 0.25。⭐ 古い実装（0.6667 × 0.5 ＝ 0.33335 を既定で出す）を赤にする。
3. `FR-039`: `displayScale` = 150 を保存した文書を開くと、描く比 0.75 で描かれ、保存値は 150 のまま（既定の 100 に戻らない）。
4. `FR-039`: `displayScale` = 33（旧い段）を持つ文書は開かれず、`RS-25` の通知が出る。66 / 85 も同じ。
5. `FR-039` の入口: いちばん小さい段 50 で縮める入口が薄く描かれ、いちばん大きい段 200 で拡げる入口が薄く描かれる（`FR-029`）。

## 8. 関係 —— `JDG-152`・`JDG-160`

- ⭐ **`JDG-152` を着地させる** —— 「現在の75％を … 100%と定義する」「0.5 ちょうど」は `S-236` と `FR-039` の描く比の文、「倍率の段の一覧: 提案通り」は `S-234` の型の欄、既定は `S-234` の既定、「読み替えは無用」「古い GRS では開けなくてOK」は `FR-039` の開くときの 2 文。
- ⭐ **`JDG-160` の a** —— `FR-039` の「文書が保存している `S-234` の値で描く（MUST）」。**k** —— `JDG-122` の「既定は 50%」を `S-234` の既定 100 で置き換えた（`JDG-122` の状態は `JDG-152` が既に覆された に変えてある）。

## 9. ⛔ この変更でやらないこと ／ 返す問い

- ⛔ `S-235`（ヘッダーとパレットの 2/3）を変えない。
- ⛔ 段の外の値を読み込むときの寛さ（既定へ戻して知らせる）を入れない —— `DFC-586`（`JDG-78`、リファクタ一式の後）。
- ⛔ `src/`・`tests/`・`tools/` を編集しない（第 6.3 節）。

**返す問い**: 無し。
