# CR-272 — 利用者の指示に立ちはだかっていた 3 行

> **閉じるもの**: ⛔ **利用者の指摘（2026-08-27）3 件**（`D-57` / `D-82` / `D-87`）。
> ⛔⛔ **3 件とも、体が「仕様の行が逆のことを言っている」として正しく止まった。**
> ⭐ **止めた判断は正しい。誤っていたのは、行を動かさずに実装だけ頼んだ前に立つ者である。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作った人の意図どおりに読める）と `CH-2`（1 画面で全体を読める）。**
⛔ **3 件とも「画面の場所を無駄に使っている」という同じ苦情である** —— パネル先頭の見出しの帯、色の二重表示、
そして**一番使う入口が右端に居ること**。

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R2.19`**（驚かせない）| `FR-072` の「選択なし」| ⛔ **利用者は「無用」と言った。**⭐ **同要求の目的（いま何を出しているかが読める）は、`FR-072` 自身が挙げるもう 1 つの担い手＝**押下状態**が単独で果たせる** |
| **`R1.4`**（欠落が無い）| `S-188` | ⛔ **同行は「色見本の一辺と隙間」を持つが、**現在色を何回描いてよいか**をどの行も述べていない。**⇒ 描く側が宿主の `input[type=color]` の**手前にもう 1 つ**置いていた |
| **`R1.4`** | ヘッダーの並び順 | ⛔⛔ **並び順を述べる行が 1 つも無い。**⭐ **表 T-109 の `群` の欄が「入口を並べる順を決めるためだけに在る」と自ら書いており、それが唯一の正である。**⇒ **`IC-7` を左端へ動かすとは、同表の行を動かすことである** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| `FR-072` の担い手 | ⭐ **見出しと押下状態は同じことを 2 か所で言っていた。**片方を落としても情報は失われない |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⛔ **`FR-072` の「見出しで示す」を落とし、担い手を押下状態 1 つにする** | ⭐ **利用者の指示のほうが新しく、逐語で「無用」と述べている。**⚠️ **代償**: 選択が解除されたとき、パネルは直前の中身を出したまま**そうと示さなくなる**。⭐ **これは利用者が明示的に選んだ代償である** |
| 2 | ⭐ **`S-188` は消さない。「現在色を 2 回描いてはならない」を足す** | ⛔ **消すと色見本の寸法がどこにも無くなる。**⭐ **問題は寸法ではなく回数である** |
| 3 | ⭐ **`IC-7` に群を 1 つ立て、表の先頭へ置く** | ⛔ **`群` の欄は画面に刷らない（表 T-109 の前文の MUST NOT）ので、辞書の語は 1 つも増えない。**⚠️ **`IC-7` を既存の「表示」の群に置いたまま先頭へ動かすと、群がとびとびになり「並べる順を決める」という欄の役目が壊れる** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| `FR-072` が見出しを求める箇所 | **2 か所**（「見出しで「選択なし」と示すこと（MUST）」と「押下状態とパネル先頭の見出しで示すこと」）|
| 見出しの帯が食う高さ | **31.6px**（`S-138` 12 ＋ `S-141` 6 × 2 ＋ 枠 2 ＝ 26px、＋ 余白 5.6px。体の実測）|
| `FR-072` を指す箇所 | **要求 2 件 / 参照 9 箇所**（`impact.py`）|
| 現在色を描く場所 | `dom-screen-surface.ts:2340-2349` が `<span>` を `input[type=color]` の**手前**に置く ⇒ **同じ色が 2 つ** |
| ヘッダーの並びの正 | ⭐ **表 T-109 の `群` の欄**（同表の前文が逐語で「入口を並べる順を決めるためだけに在る」）|
| いまの群 | `IC-1`〜`IC-4` ＝ **文書** ／ `IC-5` `IC-6` ＝ **履歴** ／ `IC-7`〜`IC-10` ＝ **表示** |
| ⇒ `IC-7` の位置 | **7 番目。**利用者は「**一番左、ファイル読み書きの左**」と述べている |
| `induced.py`（`FR-072` `FR-006` `S-188` `T-109` `FR-053` `IC-7` `T-016`）| **閉路 0。**⇒ 1 つずつ書いてよい |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-072` の STATEMENT | ⛔ **「見出しで「選択なし」と示すこと（MUST）」を落とす。**⭐ **「押下状態で示すこと（MUST）」へ一本化** |
| 2 | `FR-072` の RATIONALE | ⚠️ **落とした代償**（解除しても直前の中身が残り、そうと示さない）を書き残す |
| 3 | `S-188` の備考 | ⭐ **現在色は操作子自身が示すので、その手前に重ねて描いてはならない（MUST NOT）** |
| 4 | 表 T-109 の `IC-7` | ⭐ **群を `パレット` とし、行を `App Header` の塊の先頭へ移す** |
| 5 | `A-appendix` の変更履歴 | 1 行（`1.24`）|

⛔ **`S-188` の値（14 × 3px）は 1 文字も動かさない。**
⛔ **`FR-053` は 1 文字も動かさない**（群を刷らない MUST NOT は、新しい群にもそのまま当たる）。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1716 | **1716** | 0 |
| `uids` | 148 | **148** | 0 |
| 語の数（検査 27）| 260 | **257** | **−3**（「選択なし」の 3 語が要らなくなる）|

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1716 / 148 | **135 / 11 / 1716 / 148** | ✅ |
| 語の数 | 257 | ⛔ **260（外した）** | ❌ |
| 検査 22 本 | 全緑 | **ALL GREEN** | ✅ |

⛔ **語の数の予測を外した。** 「選択なし」を含む `panelHeadings` の 3 語（＝ 6 文字列）は**まだ辞書に要る**。
⭐ **理由**: `properties-panel.ts` が `PANEL_HEADINGS` をまだ持っており、**実装が見出しを落とすまで語は生きている。**
⇒ ⭐ **順序は「仕様 → 実装 → 辞書」であり、辞書を先に削ると `src/` が壊れる。**
⚠️ **実装が入ったあと、`panelHeadings` の節を落として 260 → 254 になる。**

⛔ **検査 12 に 1 度叱られた** —— `S-188` の備考に MUST NOT を書いたところ、
「**規則（MUST / MUST NOT）が値の表に居る。規則は要求のものである**」と落ちた。
⭐ **規則は `FR-006` へ移し、`S-188` には「本行が持つのは見本の寸法だけである」という指し先だけを残した。**
⛔ **値の表に規則を書かない** —— 表 T-016 の書き方の規律と同じである。

⚠️ **辞書の原稿も並べ替えが要った** —— `IC-7` の行を動かすと、生成器が
「原稿は 74 件を持ち仕様も 74 件を求めるが、**並びが違う**」と言って**何も書かずに止まった**。
⭐ **原稿の `icons` の並びは 表 T-109 の行の並びそのものである。**
