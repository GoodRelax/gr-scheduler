# CR-383 — 引いている依存線はポインタの左から当たり、依存線の構えではポインタは矢印のまま

> **閉じるもの**: 利用者の申し立て 2026-09-15 の B と、その答え —— 逐語は `docs/development-records/rulings.md` の各行。
> - `JDG-84`: 依存線を引く時、ドラッグしてもマウスに追従して依存線が出てこない。マウスの位置に到着位置がある想定で描画すればよい。台帳 `DFC-591`
> - `JDG-114`: 開始側・終了側のどちらから引き出してもマウスカーソルの左から矢印が当たるように動かせ。依存線描画モードで開始側・終了側にカーソルを当てると ⇔ になり、日付をずらせると誤解する。依存線モードではデフォルトの矢印のままにしろ。台帳 `DFC-591` / `DFC-603`
> - `JDG-115` の ③: その範囲をタスクの本体（移動の合図）まで広げる
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `87861cf3`。`src/` と `tests/` には触れていない。
> ⚠️ ブリーフは「表 T-023a の `PTD-4` の段」と名指したが、`PTD-4` は図形を構えたときの行である。依存線を引くドラッグは `PTD-3` から始まる ⇒ 条項は `FR-009` に置き、`PTD-3` から指した。
> ⛔ **1 つだけ未決を残した**（第 4 節）: ポインタが結べる相手の上に来たときの仮の線の形。
> ⭐ **2026-09-15 に閉じた** —— 利用者の答え `JDG-116`「依存線: 乙とせよ。」。`FR-009` の未決の 1 文を乙に差し替えた（`CR-386` と同じ波。第 4 節の末尾）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`（握っているあいだ結果が絵に出る）**。表 T-023d の結びが掴むもの全部に求める追従を依存線にも当て、依存線の構えのあいだは「動かせる」と読ませる合図を出さない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.6`（網羅）** —— 依存線のドラッグ（`PTD-3` の例外）だけが追従の条項を持たなかった。`JDG-84` の対話の記録（約 100 フレーム `line=48 polyline=1` のまま）がその穴の実物である。
- **`R1.3`（矛盾がない）** —— 表 T-028 の `IN-2` は「構えているときは作図の合図」と「端点の上は伸縮の合図」を並べ、依存線の構えで端点の上に来たときどちらが勝つかを述べていなかった。依存線の構えでは 表 T-023d を当てない（`PTD-3`）ので、伸縮や移動の合図は偽の合図になる ⇒ 既定の矢印と定めた。
- **`R4` グリッジ** —— 引いているあいだ文書へ書かない（`FR-031`）。書くのは離した時点の 1 回（表 T-028 の `IN-1`）。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**: `JDG-84`・`JDG-114`・`JDG-115`。

**問うこと**: ⛔ **1 件**（第 4 節）。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| 決定 1 | 仮の線は、開始側・終了側のどちらから引き出しても、**線の終わりがポインタの左から水平に入り、矢じりの先がポインタに当たる**。出口と走りと折れは 表 T-018 の出口のアンカーと 表 T-018a の経路に従い、入口だけをポインタの左に置く | `JDG-114` の逐語。初稿の「出口の反対側の辺を入口とする種別（`DP-1` / `DP-2`）」は捨てた（開始側から引くと入口が右になり、逐語に反する） |
| 決定 2 | 依存線の構え（表 T-023b の `AR-4`）のあいだは、端点・ダミー・タスクの本体・マイルストーンの図形の上でも、伸縮・掴める・作図のどの合図にもせず、既定の矢印とする | `JDG-114` の ② と `JDG-115` の ③。置き場は、ポインタの形を場所ごとに定める 表 T-028 の `IN-2`（行を立てず、条件を足した） |
| 決定 3 | 離したときに作る・拒む・捨てるは変えない。仮の線は離した時点で描くのをやめる | `FR-009` の上の段、表 T-023a の `PTD-3` / `PTD-4a` |
| 決定 4 | 依存線の表示の切り替え（表 T-109 の `IC-81`）を仮の線に効かせるかは本書で決めない | 条項も裁定も無い。発明しない |

---

## 1. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| `docs/spec` で依存線のドラッグ中の描き方を述べる条項（当てる前） | 0 | `FR-009`、表 T-023a の `PTD-3` / `PTD-4` / `PTD-4a`、表 T-023b の `AR-4` を全文で読んだ |
| ポインタの形を場所ごとに定める行 | 1（表 T-028 の `IN-2`） | `grep -n "ポインタの形" docs/spec/01-04-requirements.md` |
| `IN-2` を指す裁定 | 0 | `rulings.md` を `` `IN-2` `` で引いた |

## 2. グラフ

**`impact.py FR-009 PTD-3 PTD-4 PTD-4a`**（71 行）: `FR-009` は要求 5（`UC-004` `FR-013` `FR-016` `FR-049` `FR-076`）/ 参照 17。`PTD-3` は `FR-016` の 3 か所、`PTD-4a` は 0。
**`induced.py`**: 閉路 `FR-009 ↔ PTD-3`（本書が `PTD-3` から `FR-009` を指したことで生じた、行と要求の作法）⇒ 2 つを 1 度で書いた。
**`rulings.md`**: `FR-009` / `PTD-3` / `AR-4` → `JDG-84`・`JDG-114`。衝突 0。

## 3. 当てた中身（`01-04-requirements.md`）

| 対象 | 中身 |
|---|---|
| `FR-009` の半分の段の後 | 押してから離すまで、ポインタの位置を入口とみなした仮の線を描き追従させる（MUST）。開始側・終了側のどちらから引き出しても、ポインタの左から水平に入り矢じりがポインタに当たる（MUST）。出口と経路は表 T-018 / T-018a、入口だけポインタの左（MUST）。相手の上に来たときの形は未決と ⚠️ で明記。引いているあいだ文書へ書かない（MUST NOT）。作る・拒む・捨てるは据置 |
| 表 T-023a `PTD-3` | 「引いているあいだポインタに追従する仮の線も `FR-009` が持つ」を 1 文 |
| 表 T-028 `IN-2` | 依存線の構えのあいだは、端点・ダミー・タスクの本体・マイルストーンの図形の上でも、伸縮・掴める・作図のどの合図にもせず既定の矢印（MUST） |

## 4. ⛔ 利用者への問い（前に立つ者が問う）

```
機構    仮の線は「入口 ＝ ポインタの左」で描く（JDG-114）。一方、離して結ぶと FR-009 の左半分 / 右半分で
        種別が決まり、確定した線は表 T-018 の入口のアンカー（左辺か右辺の中央）に着く
        ⇒ ポインタが相手のタスクの右半分の上に在るとき、仮の線はポインタの左から入り、離した線は右辺から入る
実測    相手の上での形を定める条項・裁定は 0（FR-009 と rulings.md を引いた）
選択肢  甲 相手の上でも、同じくポインタの左から当てる（離したときに線の形が変わる）
        乙 相手の上では、離したら作る種別のアンカーと経路で描く（相手の上に入った瞬間に線の形が変わる）
推奨    乙 —— 離す前に、作ることになる種別（FS / SF / FF / SS）が絵で読める。JDG-114 の逐語は
        「どちらから引き出しても」と引き出す側を述べており、相手の上の形は述べていない
覆す費用 どちらも FR-009 の未決の 1 文を差し替えるだけ（規則 06 の分類で C）。落ちる試験は無い（未実装）
```

⭐ **答え（2026-09-15、`JDG-116`）: 乙。** `FR-009` に次を当てた —— 何にも当たっていないあいだは従来どおりポインタの左から入る。ポインタが結べる相手の上に在るあいだは、離したら作ることになる種別（左半分 / 右半分）の 表 T-018 の入口のアンカーと 表 T-018a の経路で描く（MUST）。
⭐ **問わずに決めた 1 点**: 引き出したもの自身の上は相手に数えない（MUST） —— 両端が同じ依存は 表 T-018b の `DN-1` が作らせないので、作ることになる種別が無い。相手の上で離すと 表 T-018b の他の行に拒まれる場合も乙の形で描く（拒むのは離した時点。`PTD-4a` のまま）。

## 5. 数の予測

tables 0 / figures 0 / rows 0 / uids 0（段と表のセルの文だけ）。

## 6. 影響 —— 本書の外で動くもの

| 所 | 何が動くか |
|---|---|
| `src/`（入力の翻訳とシェルのドラッグの状態、`SvgRenderer`） | 引いている途中の出口とポインタ位置を画面の状態に持ち、描画が経路を引く。⚠️ ドラッグの状態は状態機械へ移す領域（`JDG-57` / `JDG-97`）に当たる見込み |
| ポインタの形を決めるコード | 依存線の構えを読み、既定の矢印にする。`tests/unit/in-2-pointer-shape.test.ts` が赤くなりうる（実装の波で直す） |
| `docs/development-records/rulings.md` | `JDG-84` を `適用済`、`JDG-114` を `一部未着地`、`JDG-115` を `適用済` |
| `docs/development-records/defects.md` | `DFC-591` を `実装待ち`、`DFC-603` を立てた |

## 7. ⛔ この変更でやらないこと

- ⛔ 表 T-018a の経路の規則を変えない
- ⛔ 表 T-023d の順を動かさない（依存線の構えのときは同表を当てない —— `PTD-3`）
- ⛔ 相手の上の形を決めない（第 4 節）—— ⭐ 2026-09-15 に `JDG-116` で決まり、当てた
- ⛔ `src/` と `tests/` に触らない
