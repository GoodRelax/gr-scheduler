# CR-275 — 予定の開始点と同じ日に立っていた実績のダミー

> **閉じるもの**: ⛔ **利用者の指摘 1 件**（`D-56`）。
> ⛔⛔ **体が着手を拒み、その判断は正しかった。**⭐ **仕様の 2 行が、利用者の求めと正面からぶつかっていた。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`（画面から直に触れる）。**⛔ **2 つの掴みシロが同じ点に重なっていると、どちらを掴んでいるのか選べない。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `GR-3` と `GR-9` の重なり | ⛔⛔ **同じ日に 2 つの掴みシロが立つ。**⭐ **どちらが勝つかは 表 T-023d の順が決めており、`GR-3` が勝つ。**⇒ **`GR-9` は掴めない** |
| **`R2.19`**（驚かせない）| 利用者の求め | ⛔ **仕様の側が逆を向いていた** —— `GR-9` の場所欄（`:2340`）と `FR-043` の MUST（`:2017`）が、どちらも掴み点を予定の開始日そのものに置いている |
| **`R3.4`** | `src/` との一致 | ⭐ **実測で一致していた** —— `schedule-geometry.ts:917` が `GR-9` を `xFromDay(layout, start)` に置いている。⇒ **実装の誤りではなく、仕様の誤りである** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`GR-17`（終了点のダミー）も一緒に 1 日ずらした** | ⛔ **開始点だけ動かすと、終了点が開始点より手前に来ることがある。**⭐ **`GR-17` を「`GR-9` の日から `S-129` ぶん」と書き直すと、2 行が同じ 1 つの日を起点に読める** |
| 2 | ⭐ **表 T-023d の `GR-3` と `GR-9` の優先順は動かさない** | ⭐ **場所が離れたので重ならない。**順の欄は触る必要が無い |
| 3 | ⭐ **マイルストーン（`GR-18`）は動かさない** | ⛔ **実績バーを持たないので、そもそも `GR-3` と重ならない**（`GR-15` が別に持つ）|
| 4 | ⭐ **`S-129` と `S-130` は 1 文字も動かさない** | ⭐ **変えたのは起点であって、長さではない** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 利用者の逐語 | 「**9/2〜9/2 で実績のダミーが表示され、これをつかめるはず**」「**予定を左に広げる（着手を早める）のと実績の開始を入力するので掴み点を組み分けるため**」 |
| `GR-9` の場所欄（前）| 「未着手のタスクの上、**予定の開始日**」（`01-04-requirements.md:2340`）|
| `FR-043` の MUST（前）| 「実績開始日 ＝ **予定の開始日**」（`:2017`）|
| `GR-3` の場所 | 「予定バーの左端」＝ **同じ日** ⇒ ⛔ **重なる** |
| `src/` の実装 | `schedule-geometry.ts:917` が `xFromDay(layout, start)` ⇒ **仕様どおり。実装は誤っていない** |
| `induced.py`（`GR-9` `FR-043` `T-023d` `GR-3` `S-129` `S-130` `FR-006` `T-206` `T-016` `FR-093` `S-30`）| **閉路 1 つ: `FR-043` ＋ `S-129` ＋ `S-130`（大きさ 3）。**⇒ ⭐ **`FR-043` は 1 度に書き、`S-129` / `S-130` は動かさないと先に決めた** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-023d の `GR-9` | ⭐ **「予定の開始日の翌稼働日に 1 日ぶん」**（暦に従う）＋ **「予定の開始日そのものには置かない —— そこは `GR-3` が持つ」** |
| 2 | 表 T-023d の `GR-17` | ⭐ **起点を「`GR-9` の日」へ**（`actualStart` の確定先も同じ）|
| 3 | `FR-043` の MUST | ⭐ **実績開始日 ＝ 予定の開始日の翌稼働日** ＋ **MUST NOT（予定の開始日そのものに置かない）と、その理由（利用者の逐語）** |
| 4 | `FR-043` の終了点の段 | ⭐ **「予定の開始日から」を「実績開始日から」へ**。確定先も同じ |
| 5 | `A-appendix` の変更履歴 | 1 行（`1.27`）|

⛔ **`S-129` / `S-130` / `GR-18` / 表 T-023d の順は 1 文字も動かさない。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1718 / 148 | **同じ** | **0** —— **行を足さず、セルの中身だけを直す** |

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1718 / 148 | **135 / 11 / 1718 / 148** | ✅ |
| 検査 22 本 | 全緑 | **ALL GREEN** | ✅ |
| 予測から外れたもの | — | **無し** | ✅ |

## 5. 標準規則への答え

- **①（目的に沿う）**: ⭐ `GL-006`。**重なった 2 点は掴み分けられない。**
- **②（影響をグラフで測ってから書く）**: ⭐ `induced.py` を先に走らせ、`FR-043` の 3 者閉路を 1 度に書いた。
- **⑧（実測を書く）**: ⭐ **§1 が実測である。**⛔ **`src/` が仕様どおりであったことも測って残した** —— **直すのは仕様の側である。**

## 6. 残るもの

⛔ **本変更要求は仕様だけである。**⭐ **実装が要るもの**: `schedule-geometry.ts` が `GR-9` / `GR-17` を
翌稼働日を起点に置き直すこと（暦を通す）。⭐ **`t-023d-dummy-stands-clear` の赤 4 件がこれで閉じる見込みである。**
