# CR-561 — 遅延診断: 実績の記載漏れと矛盾を数え、ボトルネックとその経路を進捗マーカーで示す

> 起草の状態: **起草（2026-09-24）。11 節の問いに利用者が答えるまで、4 節は案の文である。** 答えを受けたら、当てる体が 4 節を `CR-551` の形（旧・新の塊と、1 回だけ現れることの数え）へ起こす。
> 読んだ木: 起草は `ebc71984`、載せ直して `61bbd572`（`refactor`）で測り直した。⭐ `61bbd572` では `CR-551` が仕様とコードに当たっている（`S-63` の既定は `false`、`S-326` ・ `S-327` と `!` の寸法 `S-328` 〜 `S-331` ・ `S-341` が在る —— 13 節）。`CR-552` 〜 `CR-554` は起草のまま。⇒ **本書は `CR-551` 〜 `CR-554` が当たった後の木を前提に書き、当てる順もその後とする**（8 節）。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示（`JDG-470` 〜 `JDG-474`。逐語は下の 0.1 節と `docs/development-records/rulings.md`）、`JDG-371`（`CR-551` の 10 節が本書へ申し送った自動表示）、`CR-551` の 10 節の 2 つ目の申し送り（桃色と赤の行）。
> **束ねるもの**: 3 本のメモ —— `docs/review/progress-check-2026-09-23.md`（最新。ファイル名は旧名）、`docs/review/inspection-viewpoints-2026-09-20.md`（観点 20 行）、`docs/review/bottleneck-detection-2026-09-20.md`（4 パス）。メモに「利用者の裁定」と書かれた事柄は、逐語が無いまま `JDG-475` 〜 `JDG-485` に写した（12.1 節）。
> ⭐ **形の方針**: `CR-551` と同じく、色・寸法・しきい値はすべて設定値の行に置き、要求はその行を指す（`JDG-474`「マジックナンバーとせず、定数を定義しろ」）。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が本セッションへ渡した写しのまま写した。`…` は写しの側の区切りであり、利用者の字ではない。

| # | 逐語 |
|---|---|
| `JDG-470` | 「進捗検査機能を実装しろ - 実績の記載漏れ、矛盾の確認、ボトルネックの特定までを一発でできるようにしたい - 特定したボトルネックの進捗マーカーを赤に塗れ、最上位階層からボトルネックまでの経路にある進捗マーカーをピンクに塗れ。」 |
| `JDG-471` | 「1. 黄色: 遅れてはいるが、ボトルネックとはなっていない。 → ボチボチがんばれ 2. ピンク: ボトルネックに影響を受けて大日程を遅らせている →ボトルネック解消次第リカバリがんばれ 3. 赤: ボトルネック本体 → 最優先でやれ。」 |
| `JDG-472` | 「ユースケースとしては下記。エグゼクティブやAIが進捗を知りたい 1. Fit → 2. Progress Check →ピンクのマーカーの階層を掘る → 赤マーカーが付いたボトルネックを見つける →最優先で支援」 |
| `JDG-473` | 「遅延診断 / Delay Diagnostics が正しい用語だ」 |
| `JDG-474` | 「ボトルネック経路の進捗マーカーはショッキングピンクではなく、桃色とせよ。いずれにせよ、マジックナンバーとせず、定数を定義しろ。」 |
| `JDG-371`（既存） | 「遅延診断を実行したときは、マーカーを自動で表示しろ。ただし、ユーザーがマーカーを非表示にできるようにしろ。」 |

⭐ **読み替え**: 逐語の「進捗検査」「Progress Check」は `JDG-473` により **遅延診断 / Delay Diagnostics** と読む。「ピンク」は `JDG-474` により **桃色**と読む。⛔ 本書と仕様は「点検 / inspection」「進捗検査 / Progress Check」「ピンク」の語を使わない。

### 0.2 渡された主張のうち、この木で成り立たなかったもの

⚠️ 前に立つ者の依頼文とメモの主張を、この木で反証した（handoff の §0.2 の型）。

| # | 主張 | 所 | 実測 | 本書の扱い |
|---|---|---|---|---|
| F-1 | 「黄の地に暗い `!`（`JDG-400`）と色の値（`JDG-401`）は `CR-551` が決めた」 | 依頼文 | ⭐ **成り立った。** `ebc71984` では 2 行とも無かった（前に立つ者の依頼文が、まだ push されていない行を指していた）が、`61bbd572` では `rulings.md` に在り、どちらも適用済（`CR-551` の問い 1 ・問い 4 への答え） | `JDG-400` ・ `JDG-401` を指す。⚠️ 反証は測った木の名とともに書くこと —— 同じ主張が 1 回の載せ直しで偽から真に変わった |
| F-2 | 「桃色は黄の部分集合である —— 子が遅れれば祖先は必ず遅れる」 | 最新メモ 6 節 | 黄（`PM-4`）は表 T-021b の `DL-1` 〜 `DL-3` で決まり、3 行とも「基準日が予定の端を**過ぎている**」ことを要る。祖先の `finish` が基準日より先にあれば、見込みが後ろへずれても祖先は `PM-4` ではない | 部分集合を前提にせず、優先順（表 T-315）で上書きを定める。桃色は黄が無い行にも出る |
| F-3 | 「マイルストーンの達成を先行から導出すると、`SC-21` は構造的に起きなくなる」 | 最新メモ 5.2 節 | 同じ節が「手で入れた `actualFinish` があればそちらが優先（明記 > 導出）」とする。手で達成を入れたマイルストーンの先行に未完了があれば、矛盾は今までどおり起きる | 矛盾の行（`VC-14`）を残し、条件を「手で入れた `actualFinish`」に絞る |
| F-4 | 「記載漏れは指摘の種別の 1 つである」と「記載漏れ 3 行の段は疑義」 | 最新メモ 1 節と 4.1 節 | 1 節の木は記載漏れ・矛盾・疑義を並べ、4.1 節の表は記載漏れ 3 行の「段」を疑義とする —— 種別と段が 2 通りに読める | 種別（記載漏れ・矛盾・疑義）を 1 本の軸とし、表を 3 つに分ける（表 T-310 〜 T-312）。記載漏れはマーカーを塗らない |
| F-5 | 「エグゼクティブの言葉を言葉ごと仕様に引くこと」 | 最新メモ 2.3 節 | 仕様は理由を持ち、利用者の言葉と帰属を持たない（検査 54、`CR-375`） | 言葉の意味を `GL-009` の文と `FR-133` の RATIONALE に規則の形で書く。逐語は `rulings.md` に置く |
| F-6 | 「`S-63` を真にすると、文書の値を書くので取り消しの単位と未保存の印を決めねばならない」 | `CR-551` の 10 節 | `S-63` は保存される文書の値（`tbl-settings.md` の 表 T-202）だが、表示の切り替えは取り消しの対象外である（表 T-027 の `UN-7`）。一方、仕様は表示の操作で文書を未保存にしないことを繰り返し求めている（`FR-016` の表 T-262 の `ZE-4`、`01-04-requirements.md:4087`） | 決定 3: 診断は `S-63` を書かず、画面の値で出す |
| F-7 | 「予定を動かしてよいのは公式に計画を修正した時だけ、という方針（利用者の裁定）」 | 観点メモ 2 節・ボトルネックメモ 4.1 節 | 仕様にその方針の文は無い（「公式」は公式の XSD の意味でしか現れない） | 11 節の問い 4 |
| F-8 | 表の帯 `T-310` 〜 `T-319` | 調整役の帯 | `CR-110` の 397 行目は、消した `tbl-datamodel.md` が昔 `T-301` 〜 `T-319` を使ったと記す。封印の規則は図の席番号（表 T-072 の `NF-4`）にしか無い | 使ってよい。13 節に記す |

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **新しい課題 `CH-7` ／ 新しい目標 `GL-009`**（遅れの発生源を、畳んだ画面から掘り下げるだけで特定できる）—— 本書の主目的。今の課題 `CH-1` 〜 `CH-6` のどれも「どこを直せば遅れが戻るか」を受けていない（表 T-054）。
⭐ **`CH-2` ／ `GL-002`**（全体を 1 枚で俯瞰する）—— 桃色の経路は、畳んだ祖先の行に出るから掘り下げの入口になる。`Fit`（`FR-055`）の後に 1 画面で「どこを掘るか」が分かる。
⭐ **`GL-004`**（成果物が構造化データである）—— AI は画面の色を読めない。レポートを `Agent API` の値で返す（`AM-19`）。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正）** —— ① 3 本のメモの食い違い 4 つ（0.2 節の F-2 〜 F-5）。② 完了の判定を 2 か所に置かない: 観点メモ 6.1 の「完了は `actualFinish` の有無」は、表 T-019a の `PS-2` と同じ 1 本である ⇒ 本書は `PS-2` を指し、自分では定義しない。③ 遅れの判定を 2 本にしない: 黄は今までどおり表 T-021b が決め、本書の量（表 T-314）は桃色と赤だけに使う。④ `S-154`（不良＝遅れ、`#a02b2b`）と赤の地が同じ意味の 2 つの定数にならないか —— `S-154` は状態の語の色、本書の `S-387` はマーカーの地であり、明暗の 2 値を持つ形（`S-326` と同じ）が要るので別の行にした（決定 11）。⑤ `Agent API` のメンバの数「18」が 3 か所に書かれている（`05-07-design.md:157` ・ `:414` と表 T-072 の `NF-3`）⇒ `AM-19` を足すと 3 か所とも偽になる。3 節で 19 に直す。
- **`R1.4`（異常系・境界値）** —— 基準日が無い（診断しない。`FR-047` と同じ読み）、依存の輪（横の壁）、先行を持たないマイルストーン（紫）、親が決まらない子（縦の壁）、タスクが 0 の文書（指摘 0、赤 0 と報告する）、同着の駆動先行（決定 8）。⚠️ **異常系のケアの一般の作法（`JDG-78`、`DFC-586`）はリファクタ一式の後である** ⇒ 本書は診断が「起こさない・示す」ものだけを書き、文書を直す道（修正候補）は問い 3 に回した。
- **`R1.2`（検証できる表現）** —— 「桃色」「赤」「紫」「炎」「`!!`」「`?`」の色と寸法、ボトルネックのしきい値を、すべて設定値の行にした（`S-385` 〜 `S-399`）。
- **`R2.1`（命名）** —— 用語の木（1.1 節）。⛔ `Error` ／ `Warning` を使わない（観点メモ 3 節: 取り込みの拒否 `Refusal` と混ざる）。新しい接頭辞 10（2 節）。
- **`R4.4`（状態機械）** —— 診断の表示の入り切りは画面の値 1 つで足り、状態機械を立てない（決定 4）。⚠️ 当てる前に `state-machines.json` に同じ値を持つ機械が無いことを測ること。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる）。**問うものは 11 節**。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 3 本のメモを 1 本にし、用語は 1.1 節の木に揃える（旧名のファイル名は読み替える） | `JDG-473`、最新メモ 8 節の 1 | — |
| 決定 2 | 観点の仮 ID（`SC-*` ・ `OM-*`）を捨て、`VC`（矛盾）・`VS`（疑義）・`VO`（記載漏れ）で 1 から振り直す | `SC` は既に「スクロール」の接頭辞（登録簿に在り、125 箇所）。観点メモ 7 節が「CR のときに 1 から振り直す」と言う | 旧 ID から新 ID への対応を 4.3 節の各表の最右列に残す |
| 決定 3 | 診断は `S-63` を書かない。診断を出しているあいだは、画面の値 `delayDiagnosticsShown`（保存しない）が真ならマーカーを描く。人がマーカーを消すのは今の入口 `IC-40` で行い、そのとき画面の値も偽にする | `JDG-371` の逐語は「自動で表示しろ」「非表示にできるように」であり、`S-63` を名指さない（`S-63` は読みの欄の字）。`S-63` を書くと、読むだけの閲覧者が開いて診断しただけで文書が未保存になる（0.2 節 F-6）。最新メモ 3 節の事後条件「文書は変わらない」とも合う | 診断を終えるとマーカーは `S-63` の値に戻る（既定 `false` なら消える） |
| 決定 4 | 診断の表示の入り切りは `ScreenSession` の真偽 1 つとし、状態機械を立てない | 入り（入口 `IC-107`）と切り（同じ入口、文書の置き換え）だけで、途中の状態が無い | 問い 2 の答えが B なら「古い」の状態が増え、機械が要る |
| 決定 5 | 入口は `App Header` の `IC-10`（全体表示）の右隣に `IC-107` を置く（群は 表示） | ユースケースの順が「Fit → 遅延診断」（`JDG-472`）。並べると 2 手が隣り合う。`CH-4` ／ `GL-006` | ヘッダーの入口が 1 つ増える |
| 決定 6 | 最早開始は予定の開始（`start`）を下回らない | メモの式 `inheritedDelayDays = max(0, 最早開始 − start)` は、依存だけから決めた最早開始が `start` より早いとき（先行に余裕があるとき）、予定どおりに始めて予定どおりに終えたタスクの `selfDelayDays` を正にしてしまう | — |
| 決定 7 | WBS の親（子を持つ `Task`）は自分で遅れを生まない（`selfDelayDays` = 0）。見込み終了は子の最も遅い見込み終了 | 観点メモ 6.3 の集約。親を発生源に数えると、子の遅れを二重に数える | 親の依存（`VS-1` が疑う形）は親の見込み終了で流す |
| 決定 8 | 押し出しを配るとき、駆動する先行が同着なら両方を辿り、それぞれに同じ残りを配る | どちらか 1 つを直しても終端は戻らない —— 両方がボトルネックである | 押し出し日数の合計が終端の遅れを超えうる。並べ替えとしきい値にだけ使う |
| 決定 9 | 赤のしきい値は押し出し日数 `S-397`（既定 1 稼働日）以上 | ボトルネックメモ 6 節「ボトルネックは複数出る」。数を絞る根拠は無い。`JDG-474` によりしきい値も定数にする | 並列の系列が多い文書では赤が多く出る |
| 決定 10 | 縦の壁（親が 0 または 2 以上の候補）のとき、紫を付けるのは子自身と、候補の親すべて | 候補のどれが本当の親でも、その親は子の遅れを受け取っていない。最新メモ 5.3 の「縦の壁を配下に持つ祖先」を、祖先が決まらない場合へ延ばした読み | 候補が多いと紫が広がる ⇒ 読み手の行動（紫を無くせ）は同じ |
| 決定 11 | 色は `S-326` と同じ形（1 行に明と暗の 2 値）で、地と記号を 2 行ずつ持つ（表 T-236）。寸法としきい値は表 T-206 | `CR-551` の 10 節の申し送り「`S-326` ・ `S-327` と同じ形」 | — |
| 決定 12 | 診断の色（紫・赤・桃色）と黄は、未着手のタスクでも薄く描かない | `FR-013` は未着手のマーカーを薄く描く。診断は見せるための機能で、未着手の赤（着手すべきなのに着手していない）こそ見せたい | `FR-013` に例外を 1 文足す（`CR-551` の E-03 の黄の例外と並べる） |
| 決定 13 | 色だけで区別させない: 紫 `?`、赤 炎、桃色 `!!`、黄 `!` | 最新メモ 1 節・6 節。モノクロ（`FR-041`）では色が消えるので、形が唯一の手がかりになる | 記号の寸法の行が要る（`S-391` 〜 `S-396`） |
| 決定 14 | 一覧の面（人向け）と `AM-19`（AI 向け）は同じ 1 つのレポート（表 T-317）を読む。人向けの書き出しはレポートを写す入口 `IC-108`（クリップボードへ、`AM-19` と同じ JSON） | 最新メモ 7 節「一覧から順に該当箇所へ移動する仕組み 1 本」。2 つの形を持つと離れる。利用者はファイルを落とすだけで使う（配布の前提）ので、写しで足りる | ファイルとしての保存は無い |
| 決定 15 | `AM-19` は読み出しであり、画面の値も `S-63` も書かない | AI は色を読めない。表 T-035 の `AG-4`（凍結した複製を返す）に乗る | AI が走らせても人の画面にマーカーは出ない |
| 決定 16 | `VK-4` の価値のことばは **遅延診断** とする | 最新メモ 2 節の表（価値 ＝ 遅延診断） | 機能名と価値のことばが同じ語になる |

---

## 1. 範囲 —— 何を仕様のどこへ

### 1.1 用語の木（`R2.1`。`_assets/tbl-glossary.md` へ）

```
遅延診断 / Delay Diagnostics（機能。入口 IC-107、AM-19）
  +-- 進捗妥当性検査 / ProgressValidation（文書の辻褄を見る段）
  |     指摘 / finding（一覧の 1 行、移動の 1 停留所）
  |       +-- 記載漏れ / omission        表 T-312  VO-*  マーカーを塗らない
  |       +-- 矛盾 / contradiction       表 T-310  VC-*  紫 ?
  |       +-- 疑義 / suspicion           表 T-311  VS-*  マーカーを塗らない
  +-- ボトルネック検出 / BottleneckDetection（遅れの発生源を名指す段。表 T-313）
        +-- 遅延 / delay                 黄 !   （表 T-021 の PM-4。診断の前から付く）
        +-- ボトルネック経路 / bottleneckPath   桃色 !!
        +-- ボトルネック / bottleneck    赤 炎
  （横断）信用できない / unreliable      紫 ?   （矛盾・壁の下流・壁を配下に持つ祖先・先行の無いマイルストーン。表 T-316）
```

⭐ 紫は「矛盾の色」ではなく「**この行の数字は信用できない**」の色である（`JDG-484`）。種別の違い（矛盾か、解析できなかったか）はレポートが持つ。

### 1.2 行き先

| メモの項 | 仕様で足す・変える所 | 編集（4 節） |
|---|---|---|
| 背景・課題・価値・目標（最新メモ 2 節） | 表 T-001 の `PLM-9`、表 T-054 の `CH-7`、`GL-009`、表 T-106 の `VK-4` | N-01 〜 N-04 |
| ユースケース（最新メモ 3 節） | `UC-015`、表 T-011 の `V-14` | N-05 ・ N-06 |
| 実行と入口 | `FR-130`、表 T-109 の `IC-107` ・ `IC-108` | N-07 ・ N-20 |
| 進捗妥当性検査（観点メモ 1 〜 10 節、最新メモ 4 節） | `FR-131`、表 T-310 ・ T-311 ・ T-312 | N-08 ・ N-11 〜 N-13 |
| ボトルネック検出（ボトルネックメモ 3 〜 7 節、最新メモ 5 節） | `FR-132`、表 T-313 ・ T-314 | N-09 ・ N-14 ・ N-15 |
| マーカーの塗り分け（最新メモ 6 節、`JDG-471` ・ `JDG-474` ・ `JDG-371`） | `FR-133`、表 T-315、`S-385` 〜 `S-396`、`FR-013` と `FR-049` の 1 文ずつ | N-10 ・ N-16 ・ E-01 ・ E-02 ・ J-01 |
| 壁と紫（最新メモ 5.3） | 表 T-316（`FR-132` が指す） | N-17 |
| レポートと一覧（最新メモ 7 節、観点メモ 9 節の 5） | `FR-134`、表 T-317、表 T-107 の `AM-19`、表 T-035 は書き直さない | N-18 ・ N-21 ・ N-22 |
| 暗黙の親子（最新メモ 5.1） | `FR-135`、表 T-318、`S-398` ・ `S-399`（親確認） | N-19 ・ N-23 |
| マイルストーンの先行と達成（最新メモ 5.2） | `FR-136`、表 T-319 | N-24 ・ N-25 |
| 修正（観点メモ 9 節、最新メモ 4.2 節）と親入力機能（最新メモ 5.1） | ⛔ **本書に入れるかは問い 3**。推奨は後の変更要求 | — |
| 計画の見直しが遅れを消す性質（最新メモ 8 節の 5） | `FR-132` の RATIONALE。守りの形は問い 4 | N-09 |

**数（案）**: 新しい要求 7（`FR-130` 〜 `FR-136`）、ユースケース 1、目標 1、表 10、設定値 15、既存の文の編集 5（E-01 〜 E-05）、原稿 JSON の編集 8（J-01 〜 J-08）。

---

## 2. 新しい識別子

⚠️ 規則 02 の 2.5 節: 下の「空いている」は 2026-09-24 に `ebc71984` で測り、載せ直した `61bbd572` で測り直した主張である。**当てる直前に測り直すこと**（検査 62）。測り方は 13 節。帯（`CR-561`・`JDG-470` 〜 `JDG-499`・`DFC-890` 〜 `DFC-909`・`PND-581` 〜 `PND-600`・`S-385` 〜 `S-399`・`T-310` 〜 `T-319`）は調整役が配った。**帯の外の番号（`FR`・`UC`・`GL`・`CH`・`PLM`・`VK`・`V`・`AM`・`IC`・`UF`）は調整役に宣言してから取る**（12.3 節）。

| 種類 | 採ったもの | 測った最大（2026-09-24、`61bbd572`） |
|---|---|---|
| 設定値 | `S-385` 〜 `S-399` は空いている | 仕様 `S-341`（`CR-551` が当たった）。`CR-552` ・ `CR-553` は `S-359` までを名に出すが取らない |
| 表 | `T-310` 〜 `T-319` は空いている | 仕様が使う最大の表は `T-297`（`CR-551` が当てた） |
| 接頭辞 | `VC` `VS` `VO` `BD` `DQ` `DG` `DW` `DX` `IP` `MP` は空いている（登録簿は 163 件） | `SC` ・ `PV` ・ `DP` ・ `RF` ・ `DM` ・ `MK` ・ `RP` は使用中なので採らない |
| 要求 | `FR-130` 〜 `FR-136` は空いている | 仕様の最大は `FR-111`。`FR-112` は `CR-432` の名残、`FR-120` 以降は状態機械のセッションの帯だった（`handoff-state-machine.md:198`）ので避けた |
| ユースケース ・ 目標 ・ 課題 ・ 背景 | `UC-015` `GL-009` `CH-7` `PLM-9` `VK-4` は空いている | `UC-014` ・ `GL-008` ・ `CH-6` ・ `PLM-8` ・ `VK-3` |
| 表の行（既存の表） | `V-14` ・ `AM-19` ・ `IC-107` ・ `IC-108` は空いている（置く表は順に T-011 ・ T-107 ・ T-109） | `V-13a` ・ `AM-18` ・ `IC-106`（`CR-551` が当てた。`CR-562` が一度 `IC-107` を名に出したが、調整役が `IC-115` へ移した）。⚠️ `V-14` は `docs/review/inventory/` の 2 本が別の意味の局所番号として使っているが、仕様の中では使われていない |
| ユニット | 遅延診断の実体のユニット 1 と、ユースケースのユニット 1（番号は当てる時に調整役から受ける） | `CR-554` が `UF-126` 〜 `UF-168` を予約 |
| 台帳 | `JDG-470` 〜 `JDG-485` | 帯は調整役が配った |
| 名 | 画面の値 `delayDiagnosticsShown`、`Agent API` のメンバ `readDelayDiagnostics`、型 `DelayDiagnosticsReport` | `git grep` で 0 件 |

**新しい接頭辞の意味**（1 つの接頭辞に 1 つの意味。`row-id-prefixes.json` へ J-08 で足す）:

| 接頭辞 | 意味（ja） | 表 |
|---|---|---|
| `VC` | 進捗妥当性検査の矛盾の観点 | T-310 |
| `VS` | 進捗妥当性検査の疑義の観点 | T-311 |
| `VO` | 進捗妥当性検査の記載漏れの観点 | T-312 |
| `BD` | ボトルネック検出の手順の段（パス） | T-313 |
| `DQ` | 遅延診断が数える遅れの量 | T-314 |
| `DG` | 遅延診断が進捗マーカーに与える状態 | T-315 |
| `DW` | 遅延診断の解析を止める壁の種類 | T-316 |
| `DX` | 遅延診断のレポートの欄 | T-317 |
| `IP` | 明記されていない WBS の親の導き方 | T-318 |
| `MP` | マイルストーンの先行と達成の導き方 | T-319 |

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

⭐ 本書は機能を**足す**変更であり、消す規則はほとんど無い。消える文と偽になる文の全数:

| 消す・偽になるもの | 所（`ebc71984`） | 置き換わる先 | 編集 |
|---|---|---|---|
| `FR-013` の「**色は実績バーの色を継ぎ、独立した色を保存しない**」の、診断の状態についての効き | `01-04-requirements.md:2979` | 表 T-315 の色（`S-385` 〜 `S-390`）。`CR-551` の E-03 が黄について足す例外に、診断の 3 状態を並べる | E-01（文は残し、例外を足す） |
| `FR-013` の「未着手のマーカー … は薄く描き」の、診断の状態と黄についての効き | `:2977` | 決定 12 | E-01 |
| `FR-049` の「マーカーを出すかは `S-63` が決める」読み（`S-63` だけが決める） | `FR-049`（表示の切替） | `S-63` か `delayDiagnosticsShown` のどちらかが真なら描く。`IC-40` で消すと両方が偽になる | E-02 |
| `Agent API` の「18 のメンバ」 | `05-07-design.md:157`（`R2.5`）・ `:414`（`UF-28`）・ 表 T-072 の `NF-3` | 19 のメンバ | E-03 〜 E-05 |
| `S-63` の備考（切り替える機能の列挙） | `_source/settings.json` の `S-63` | 診断中は `delayDiagnosticsShown` も描く理由になる、を足す | J-01 |
| 3 本のメモ（`docs/review/`） | 仕様の外 | 本書が当たったら、各メモの頭に「`CR-561` に束ねた。以後の正は仕様」と 1 行を足す（当てる者の仕事。消さない —— 裁定の出どころだから） | — |

⛔ **本書は `CR-551` が当たった後の文を旧とする**（`61bbd572` で当たっている）（`FR-013` の黄の例外、`S-63` の既定 `false`、`S-326` ・ `S-327`）。当てる前に、`CR-551` の E-03 と J-01 が当たっていることを数えること。

---

## 4. 書き足す所（案の文）

⚠️ **ここは案である。** 11 節の問いに答えが出たら、当てる体が各塊の置き場（直前の行の字）と、1 回だけ現れることを数えて、`CR-551` の形に起こす。⛔ 生成物（`_assets/tbl-settings.md` ・ `tbl-row-id-prefixes.md` ・ `src/` の生成物）は手で直さない。原稿を直して `npm run gen` を打つ。

### 4.1 課題・目標・ユースケース（`01-04-requirements.md` の Chapter 1 と 3、`_assets/tbl-glossary.md`）

**N-01 表 T-001 に `PLM-9`**（`PLM-8` の下）

| 行 ID | 現状 | 何が起きるか |
| --- | --- | --- |
| PLM-9 | 日程表は遅れている行を示すが、その遅れがどこで生まれたかは示さない | 遅れている行の多くは先行の遅れに巻き込まれた行であり、読み手は依存と WBS を自分で辿って原因を探すことになる。<br>実績が入力されていない行があると、遅れの像そのものが歪む。<br>⇒ 支援の差し向け先を誤る |

**N-02 表 T-054 に `CH-7`**（`CH-6` の下）

| 行 ID | 課題 | 価値のことば | 受ける目標 |
| --- | --- | --- | --- |
| CH-7 | 遅れの発生源と、数字が信用できない行を、読み手が判断せずに受け取れるようにする | **遅延診断** | `GL-009` |

**N-03 目標 `GL-009`**（`GL-008` の後。見出し「遅れの発生源を、掘り下げるだけで特定できる」）

> **STATEMENT**: 基準日の時点で、遅れのうち最優先で対処すべき行（ボトルネック）を、畳んだ画面から掘り下げる操作だけで特定できること。数字が信用できない行は、それと分かる形で示されること。
> **RATIONALE**: 読み手には、日程を描いた人でない者（全体を見て支援を差し向ける立場の人）と、画面を見ない AI が含まれる。どちらも依存と WBS を自分で辿らない。⭐ 読み手の行動は「信用できない行を無くさせ、そのうえでボトルネックから支援する」の 2 手で足りること —— 読み手が数手の掘り下げで届くことが本目標の要件であり、詳しさを足す要求はこの要件で測る。

**N-04 表 T-106 に `VK-4`**

| 行 ID | ことば | 意味 |
| --- | --- | --- |
| VK-4 | **遅延診断** | 目標 `GL-009` が述べる状態を指すことば |

**N-05 ユースケース `UC-015`「遅れの発生源を見つける」**（`UC-014` の後。親は `GL-009`）

> **STATEMENT**: 閲覧者または AI が遅延診断を実行し、`GRS` が信用できない数字、遅れ、ボトルネックとその経路を進捗マーカーとレポートで示すこと。
> **SCENARIO**:
> 1. 閲覧者が遅延診断を実行する（`FR-130`）。前に置く操作を要さない。
> 2. `GRS` は進捗妥当性検査を行い、矛盾を持つ `Task` のマーカーを紫（`?`）にする（`FR-131`）。
> 3. `GRS` はボトルネック検出を行い、ボトルネックを赤（炎）、その WBS の祖先を桃色（`!!`）、それ以外の遅れを黄（`!`）で示す（`FR-132` ・ `FR-133`）。
> 4. 閲覧者は、必要なら全体を 1 画面に収める（`FR-055`）。
> 5. 閲覧者は桃色の行を掘り下げる。
> 6. 閲覧者は赤の行に着き、そこへ支援を差し向ける。
>
> **EXTENSIONS**:
> - 1a. 基準日が置かれていない。 —— `GRS` は診断を行わず、基準日が要ることを告げる。
> - 2a. 紫の行がある。 —— `GRS` はその行から先の解析を行わず、レポートに解析できなかった件数を出す（表 T-316）。読み手は、紫の行を無くすよう求める。
> - 3a. 赤が 1 つも無い。 —— 遅れはすべて余裕に吸収されている。`GRS` はそう告げる。
> - 4a. 閲覧者が全体表示を行わない。 —— 4 を飛ばし、5 はそのまま行える。
> - 5a. 読み手が AI である。 —— 4 〜 6 を踏まず、レポートを読む（`FR-134`、表 T-107 の `AM-19`）。
>
> ⛔ 全体表示を診断の前提にしてはならない（MUST NOT）—— 4 は読み手の都合で行う操作であり、診断はその前でも後でも走る。

**N-06 表 T-011 に `V-14`**

| 行 ID | ユースケース | 吸収した要望の項番 | 主なアクター |
| --- | --- | --- | --- |
| V-14 | UC-015 遅れの発生源を見つける | （要望の項番に対応するものは無い。<br>利用者の指示から起こした） | AC-2 / AC-3 |

### 4.2 要求（`01-04-requirements.md` の 4.1。⭐ 置き場は `FR-047` の後の「予実と遅れ」の塊）

**N-07 `FR-130` 遅延診断を実行する**（ORIGIN: UC-015 手順 1 ／ 拡張 1a）

> **STATEMENT**: 閲覧者が遅延診断の入口（表 T-109 の `IC-107`）を押したとき、`GRS` は、進捗妥当性検査（`FR-131`）とボトルネック検出（`FR-132`）をこの順に 1 度に行い、結果を進捗マーカー（`FR-133`）と一覧（`FR-134`）に出すこと。
> 診断はいつでも行えること（MUST）—— 前に置く操作を要さない。
> 基準日（`Project.statusDate`）が `null` のときは診断を行わず、そのことを告げること（MUST）。
> ⛔ 診断は文書を変えてはならない（MUST NOT）—— タスク・依存・実績・`S-63` を書かない。導いた親子（`FR-135`）とマイルストーンの先行・達成（`FR-136`）も保存しない。
> 診断を出しているあいだに同じ入口を押したら、診断の表示を終えること（MUST）。
> ⚠️ 診断の結果を、文書が変わるたびに算出し直すかは問い 2 の答えで決める（推奨: 算出し直す）。
> **RATIONALE**: 実行の順を固定するのは、ボトルネック検出が進捗妥当性検査の結果を前提とするからである（表 T-316）。文書を変えないので、読むだけの閲覧者が診断しても未保存の編集は立たない（`FR-100`）。

**N-08 `FR-131` 進捗妥当性検査**（ORIGIN: UC-015 手順 2 ／ 拡張 2a）

> **STATEMENT**: `GRS` は、開いている文書モデルを、矛盾（表 T-310）・疑義（表 T-311）・記載漏れ（表 T-312）の観点ですべて調べ、当たった 1 件ごとに指摘を 1 つ作ること。
> 検査は文書モデルの列だけを読むこと（MUST）。⛔ `carry` ／ `carryElements` の中の値を解釈して判定してはならない（MUST NOT）。
> 完了の判定は表 T-019a の `PS-2`（`actualFinish` がある）とすること（MUST）。⛔ `percentComplete` から完了を判じてはならない（MUST NOT）。
> 指摘の語に `Error` ／ `Warning` を用いてはならない（MUST NOT）。
> ⛔ 疑義と記載漏れを断定の語で告げてはならない（MUST NOT）—— 記載漏れは「実績が入力されていない」とだけ告げる。
> 表 T-220 の `IV-*` が取り込みで拒む事柄を、指摘として持ってはならない（MUST NOT）—— 同じ事柄を 2 か所で判じることになる。
> **RATIONALE**: 取り込みの検証は「開けない」を言い、本検査は「開けるが辻褄が合わない」を言う。語を分けないと 2 つが混ざる。`percentComplete` は消化率であって完了度ではない —— 100 なのに `actualFinish` が無いのは「予定した分を使い切ったがまだ終わらない」、`actualFinish` があるのに 100 未満なのは「予定より早く終わった」であり、どちらも正常である。記載漏れと本当の遅れは機械では区別できないので断定しない。

**N-09 `FR-132` ボトルネック検出**（ORIGIN: UC-015 手順 3 ／ 拡張 3a）

> **STATEMENT**: `GRS` は、表 T-313 の 4 つの段をこの順に行い、表 T-314 の量をタスクごとに求め、押し出し日数が `S-397` 以上の `Task` をボトルネックとすること。
> 日数はすべて稼働日で数えること（MUST。暦は `FR-054`）。
> 見込み終了は表 T-313 の `BD-1` だけから決めること（MUST）。⛔ `percentComplete` から外挿してはならない（MUST NOT）。
> 計画期間はいまの計画（`start` 〜 `finish`）から採ること（MUST）。⛔ `BaselineTask` を判定に使ってはならない（MUST NOT）。
> 余裕は `GRS` が依存と暦から求め直すこと（MUST）。⛔ 取り込んだ余裕・クリティカルの値を使ってはならない（MUST NOT）。
> 表 T-316 の壁に当たったら、その表の「止まる範囲」の解析を行わないこと（MUST）。
> ⚠️ 完了したタスクをボトルネックに数えるかは問い 1 の答えで決める（推奨: 数えない。レポートには出す）。
> **RATIONALE**: ボトルネックは遅れている当人ではない —— 先行の遅れで遅れた行は巻き添えであり、名指すべきは大もとである。⭐ 遅れた量ではなく押した量で並べる —— 自分で 10 日遅れても後続に 10 日の余裕があれば下流は動かず、2 日でも余裕の無い連鎖の上にあれば終端を 2 日押す。表 T-021b は画面の遅れの印（黄）のために残す —— あの 3 行は終点が基準日であり、遅れて終わったタスクを 1 日も数えないので、押し出しには使えない。
> ⭐ **意図した性質を 3 つ書き残す**（⛔ 消すと、次の読み手が欠陥と読む）:
> ① 着手済みで終わっていない行の見込み終了は `max(finish, 基準日)` なので、**実績を入力していない行は自動的に遅れて見える。これは設計した効果である** —— 進捗を入力させる圧として働く。
> ② 同じ理由で、着手済みで終わっていない行の遅れは基準日とともに毎日伸びる。
> ③ いまの計画に対して測るので、**計画を見直すと遅れが消える**。これは、予定を動かしてよいのは計画を公式に見直したときだけである、という運用と**組で**守る（⛔ 片方だけを崩すと、予定を黙って動かして遅れを消せる）。守りの形は問い 4 の答えで書く。

**N-10 `FR-133` 進捗マーカーの塗り分け**（ORIGIN: UC-015 手順 2 〜 3 ／ `GL-009`）

> **STATEMENT**: 診断を出しているあいだ、`GRS` は、各 `Task` の進捗マーカーを表 T-315 の状態のうち優先順の最も高いもので描くこと。どの状態にも当たらない `Task` は表 T-021 の記号のままとする。
> 状態の区別を色だけに頼ってはならない（MUST NOT）—— 表 T-315 の記号の形でも分けること。
> 桃色（`DG-3`）は、ボトルネックの WBS の祖先（`wbsParentUid` を遡る縦の道、`FR-135` で導いた親を含む）にだけ付けること（MUST）。⛔ 依存の下流に付けてはならない（MUST NOT）。
> 畳んだ行に祖先が描かれているときも、その祖先に桃色を付けること（MUST）—— 付かないと掘り下げが始まらない。
> 診断を始めたら、`S-63` が偽でもマーカーを描くこと（MUST）。⛔ そのために `S-63` を書いてはならない（MUST NOT）—— 画面の値 `delayDiagnosticsShown` を真にする。
> 人がマーカーを消す入口は `IC-40`（`FR-049`）とすること（MUST）。
> 色と記号の寸法は `S-385` 〜 `S-396`（表 T-236 ・ 表 T-206）に従うこと（MUST）。
> **RATIONALE**: 読み手の行動は色の意味と 1 対 1 である —— 紫は「数字を直させる」、赤は「最優先で支援する」、桃色は「ボトルネックが解けしだい取り戻させる」、黄は「自力で取り戻させる」。紫を最上位に置くのは、信用できない数字の上に出た赤や桃色は誤りでありうるからである。形で分けるのはモノクロ（`FR-041`）と、行の色（`TaskGroup.color`、表 T-294 に赤・黄・紫が在る）と地が重なったときのためである。

**N-18 `FR-134` 診断の一覧とレポート**（ORIGIN: UC-015 手順 5 〜 6 ／ 拡張 5a）

> **STATEMENT**: `GRS` は、診断の結果を表 T-317 の欄を持つ 1 つのレポートにまとめ、人には一覧として、AI には `Agent API` の `AM-19` の値として渡すこと。
> 一覧の 1 行を選んだら、その行の `Task` が見える位置へ表示を寄せ、畳んだ行に隠れていれば開くこと（MUST）。
> 記載漏れ・矛盾・疑義・ボトルネックのすべての種別を、同じ 1 つの一覧と移動の仕組みに載せること（MUST）。⛔ 種別ごとに別の移動の道を作ってはならない（MUST NOT）。
> レポートを写す入口（表 T-109 の `IC-108`）は、`AM-19` と同じ形の値をクリップボードへ置くこと（MUST）。
> **RATIONALE**: AI は画面の色を読めない。レポートを後に回すとユースケースの半分（拡張 5a）が落ちる。疑義と記載漏れはマーカーを塗らないので、一覧が唯一の到達の道であり、その道を 1 本にしておけば、どの種別も同じ手で辿れる。
> ⚠️ 一覧の面の形と寸法は、当てる前に触れる見本で決める（10 節）。

**N-19 `FR-135` 明記されていない WBS の親**（ORIGIN: UC-015 手順 5）

> **STATEMENT**: `Task.wbsParentUid` が `null` の `Task` について、`GRS` は、表 T-318 の規則で親を導き、診断の中でだけ使うこと。
> ⛔ 導いた親を文書へ書いてはならない（MUST NOT）。
> ⛔ 候補が 2 つ以上のとき、狭いほうを親と決めてはならない（MUST NOT）。⛔ マイルストーンを親にしてはならない（MUST NOT）。
> 閲覧者が `Task` にポインタを当てているあいだ、`GRS` は、その `Task` の親（明記または導いたもの）の名称ラベルを `S-398` の色と `S-399` の太さで描くこと（MUST）。
> 導けなかった `Task` を、進捗妥当性検査の指摘（表 T-312 の `VO-4`）として出すこと（MUST）。
> **RATIONALE**: 行（`TaskGroup`）に詰めて描くので、`wbsParentUid` が書かれないまま残る。導いた親を書き込むと、行を動かしたときに古い親が残って腐る。⭐ 同じ行のバーは時間的に重なりうるので、区間の列として扱えない —— 両端で包含するものだけが候補である。親どうしが交差しているとき、どちらが親かは作成者の意図でしか決まらず、幾何に根拠が無い。点は中身を持てないのでマイルストーンは親になれない（表 T-311 の `VS-2` と同じ原則）。親を確かめる描き方は、導いた親が正しいかを人が検算できる唯一の手段である。

**N-24 `FR-136` マイルストーンの先行と達成**（ORIGIN: UC-015 手順 3）

> **STATEMENT**: `GRS` は、マイルストーンの先行を表 T-319 の `MP-1` 〜 `MP-3` で導き、達成を `MP-4` で導き、診断の中でだけ使うこと。
> ⛔ 導いた先行と達成を文書へ書いてはならない（MUST NOT）。
> 手で入れた依存と `actualFinish` は、導いたものに優先すること（MUST）—— 明記 ＞ 導出。
> 先行を 1 つも持たないマイルストーンは、表 T-316 の `DW-3` とすること（MUST）。
> ⚠️ 導いた達成を診断の外（画面のマーカーの状態）にも効かせるかは問い 5 の答えで決める（推奨: 診断の中だけ）。
> **RATIONALE**: マイルストーンの先行を人に選ばせると数が多く非現実的であり、`MP-1` 〜 `MP-3` は一意に決まる。「完了日が前」を予定で判ずるのは、実績で判ずると遅れるたびに先行の顔ぶれが変わり、診断が揺れるからである。何も出ないマイルストーンは「順調」と読まれるので、先行が無ければ紫にする。

### 4.3 表（⭐ 置き場は各要求の STATEMENT の直後）

**N-11 表 T-310 — 矛盾の観点**（両立しない。必ずどちらかが誤りである。**層** ＝ 直す順の目安、1 が先 —— 葉から幹へ、事実から派生値へ）

| 行 ID | 層 | 観点 | 使う列 | 旧 |
| --- | --- | --- | --- | --- |
| VC-1 | 1 | 依存に輪がある | `Dependency.predecessorUid` | SC-1 |
| VC-2 | 1 | 同じ `Task` を先行と後続にする依存（自己依存） | 同上 | SC-2 |
| VC-3 | 1 | 同じ 2 つの `Task` の間に依存が 2 本以上ある | 同上 | SC-3 |
| VC-4 | 2 | `milestone` が真なのに `start` ≠ `finish` | `milestone` ・ `start` ・ `finish` | SC-5 |
| VC-5 | 3 | `percentComplete` が 0 より大きいのに `actualStart` が無い | その 2 列 | SC-9 |
| VC-6 | 3 | `actualFinish` があるのに `actualStart` が無い | その 2 列 | SC-10 |
| VC-7 | 3 | 未着手（`percentComplete` が 0 かつ `actualStart` が無い）なのに `stop` または `resume` を持つ | その 4 列 | SC-11 |
| VC-8 | 3 | `actualFinish` があるのに `resumeValid` が真 | その 2 列 | SC-12 |
| VC-9 | 4 | 親が完了（`PS-2`）なのに、子に未完了がある | `actualFinish` ・ `wbsParentUid` | SC-13 |
| VC-10 | 4 | 子が全部完了なのに、親が未完了 | 同上 | SC-14 |
| VC-11 | 4 | 親の予定の両端が、子の両端（最も早い `start` と最も遅い `finish`）と違う | `start` ・ `finish` ・ `wbsParentUid` | SC-15 |
| VC-12 | 4 | 親の実績の両端が、子の両端と違う（`actualStart` は着手した子の最も早いもの、`actualFinish` は子が全部完了したときだけ最も遅いもの） | 実績の 2 列 ・ `wbsParentUid` | SC-17 |
| VC-13 | 5 | 先行が未完了なのに、後続が完了している | 依存 ・ `actualFinish` | SC-19 |
| VC-14 | 5 | 手で `actualFinish` を入れたマイルストーンの先行（明記または `FR-136` で導いたもの）に、未完了がある | 依存 ・ `milestone` ・ `actualFinish` | SC-21（0.2 節 F-3） |
| VC-15 | 5 | 予定の日付が依存の向きに反する（FS: 後続の `start` ≥ 先行の `finish` ＋ `lag`、SS: 後続の `start` ≥ 先行の `start` ＋ `lag`、FF: 後続の `finish` ≥ 先行の `finish` ＋ `lag`、SF: 後続の `finish` ≥ 先行の `start` ＋ `lag`） | 依存 ・ `start` ・ `finish` | SC-22 |

**N-12 表 T-311 — 疑義の観点**（両立はするが、ありそうにない。⛔ `GRS` は断定しない）

| 行 ID | 層 | 観点 | 告げる語 | 旧 |
| --- | --- | --- | --- | --- |
| VS-1 | 1 | 親子の関係にある 2 つの `Task` の間に依存がある | 同じ | SC-4 |
| VS-2 | 2 | マイルストーンとして描かれる `Task` が子を持つ | 「この行を束ねる上位タスクが無い」 —— ⛔ 検出の条件は「子を持つマイルストーン」のまま | SC-6 |
| VS-3 | 5 | 先行が未着手なのに、後続が着手済み | 同じ | SC-20 |
| VS-4 | 5 | 実績の日付が依存の向きに反する（式は `VC-15` を実績の列に当てたもの） | 同じ | SC-23 |
| VS-5 | 6 | 実績の日付が `Project.statusDate` より後 | 同じ | SC-24 |

**N-13 表 T-312 — 記載漏れの観点**（告げる語は「実績が入力されていない」だけ。マーカーを塗らない）

| 行 ID | 観点 | 旧 |
| --- | --- | --- |
| VO-1 | `start` が基準日より前で、先行が全部完了しているのに `actualStart` が無い | OM-1 |
| VO-2 | `finish` が基準日より前で、着手済みなのに `actualFinish` も `stop` も無い | OM-2 |
| VO-3 | FS の後続が着手済みなのに、先行に `actualFinish` が無い —— ⭐ 他の行の実績が、その行の記載漏れを示している | OM-3 |
| VO-4 | `wbsParentUid` が `null` で、表 T-318 の規則で親が 1 つに決まらない（候補 0、または 2 以上） | （最新メモ 5.1 の「親が明記されていない n 件」） |

⛔ **本書が入れない観点**（観点メモ 8 節、`JDG-478`）: `percentComplete` が 100 なのに `actualFinish` が無い ／ その逆（どちらも正常）、親の `start` の単独の比べ（`VC-11` に畳んだ）、親の `percentComplete` と子の平均の比べ（親はボトルネックの子で決まる）、`BaselineTask` が一部にしか無い（正常）、予定・実績が非稼働日（休日に稼働することがある）。

**N-14 表 T-313 — ボトルネック検出の段**

| 行 ID | 段 | 何をするか |
| --- | --- | --- |
| BD-1 | 見込み終了を置く | 完了（`PS-2`）は `actualFinish`。着手済みで未完了は `max(finish, 基準日)`。未着手は 最早開始 ＋ 計画期間（稼働日） |
| BD-2 | 前向きに流す | 依存の `linkType` ・ `lag` と暦をそのままに、日付だけを実績と見込みへ差し替えて前向きに流し、各 `Task` の最早開始（`DQ-1` の基）を求める。⭐ 最早開始は `start` を下回らない（決定 6）。未着手の `Task` の最早開始は、さらに基準日を下回らない。WBS の親は子の最も遅い見込み終了を自分の見込み終了とする（決定 7） |
| BD-3 | 帰属させる | 表 T-314 の `DQ-2` ・ `DQ-3` を求める。`DQ-3` が 0 より大きい `Task` だけが発生源の候補である |
| BD-4 | 終端まで押し出す | 終端 ＝ 後続を持たない `Task` とマイルストーン。終端ごとに、終端の遅れ（見込み終了 − `finish`、0 以上）を残りとし、終端から駆動する先行（最早開始を決めた先行）を後ろ向きに辿って、辿った各 `Task` に「`DQ-3` と残りの小さいほう」を配り、残りから引く。残りが 0 になるか、駆動する先行が無くなったら止める。同着の駆動先行は両方を辿る（決定 8） |

**N-15 表 T-314 — 遅れの量**（すべて稼働日）

| 行 ID | 量 | 英（コード） | 定義 |
| --- | --- | --- | --- |
| DQ-1 | 見込み終了 | `projectedFinish` | 表 T-313 の `BD-1` |
| DQ-2 | 持ち込まれた遅れ | `inheritedDelayDays` | max(0, 最早開始 − `start`) —— 巻き添え |
| DQ-3 | 自分で生んだ遅れ | `selfDelayDays` | max(0, `DQ-1` − (最早開始 ＋ 計画期間)) —— 発生源 |
| DQ-4 | 押し出し日数 | `pushOutDays` | `BD-4` で配られた日数の、すべての終端についての和。届いた終端の数を添える |

**N-16 表 T-315 — 遅延診断が進捗マーカーに与える状態**（優先順の小さいほうが勝つ）

| 行 ID | 優先順 | 状態 | 条件 | 地 | 記号 | 記号の形 |
| --- | --: | --- | --- | --- | --- | --- |
| DG-1 | 1 | 信用できない / unreliable | 表 T-310 の指摘を持つ、または表 T-316 が紫とする | `S-389` | `S-390` | `?`（`S-392` 〜 `S-394`。点は `S-330` ・ `S-331`、線の太さは `S-328`、長さの単位は `S-341`） |
| DG-2 | 2 | ボトルネック / bottleneck | `DQ-4` が `S-397` 以上（⚠️ 完了したものを含むかは問い 1） | `S-387` | `S-388` | 炎（`S-395` ・ `S-396`） |
| DG-3 | 3 | ボトルネック経路 / bottleneckPath | `DG-2` の `Task` の WBS の祖先（`FR-135` で導いた親を含む） | `S-385` | `S-386` | `!!`（`!` を 2 つ、中心の間隔 `S-391`。各 `!` は `S-328` 〜 `S-331` ・ `S-341`） |
| DG-4 | 4 | 遅延 / delay | 表 T-021 の `PM-4`（表 T-021b）—— ⭐ 診断の前から付く | `S-326` | `S-327` | `!`（`S-328` 〜 `S-331` ・ `S-341`） |

⭐ `DG-4` は `CR-551` が立てる黄そのものであり、本書は優先順の中に置くだけで、条件も色も変えない。`DG-1` 〜 `DG-3` は診断を出しているあいだだけ描く。

**N-17 表 T-316 — 解析を止める壁**（止まる軸が 2 本あり、効き方が逆である）

| 行 ID | 軸 | 原因 | 止まる範囲 | 紫（`DG-1`）を付ける行 |
| --- | --- | --- | --- | --- |
| DW-1 | 横（依存） | 表 T-310 の指摘を持つ `Task` | その `Task` から依存の下流へ前向きの計算が止まる（下流は解析できない） | 原因の `Task` と、その依存の下流すべて |
| DW-2 | 縦（親子） | 親が 1 つに決まらない `Task`（表 T-312 の `VO-4`） | 押し出しと桃色が、その `Task` から祖先へ上がらない。⭐ その `Task` 自身の遅れと依存の押し出しは計算できる | 原因の `Task` と、候補の親すべて（決定 10）。候補が 0 なら原因だけ |
| DW-3 | 縦（親子） | 先行を 1 つも持たないマイルストーン（`FR-136`） | そのマイルストーンの遅れと達成を診断できない | そのマイルストーン |

⭐ 縦の壁の祖先に何も出ないと、読み手は「配下は健全」と読み、掘り下げが成り立たない —— だから縦の壁でも紫を付ける。

**N-21 表 T-317 — 遅延診断のレポートの欄**（一覧と `AM-19` が読む 1 つの形）

| 行 ID | 欄 | 中身 |
| --- | --- | --- |
| DX-1 | 実行の結果 | 診断した ／ 基準日が無いので診断しなかった |
| DX-2 | 基準日 | `Project.statusDate` |
| DX-3 | 指摘 | 1 件ごとに: 観点の行 ID（`VC-*` ・ `VS-*` ・ `VO-*`）、種別、層、`Task` の `uid` と名前、判定に使った列のいまの値 |
| DX-4 | ボトルネック | 1 件ごとに: `uid` と名前、`DQ-2` 〜 `DQ-4`、届いた終端の数、経路（根からその `Task` までの祖先の `uid` の列） |
| DX-5 | 終端ごとの押し出し | 終端ごとに: `uid` と名前、終端の遅れ、配った先（`uid` と日数） |
| DX-6 | 壁 | 壁ごとに: `DW-*`、原因の `Task`、止まった範囲の件数 |
| DX-7 | 解析できなかった件数 | 紫（`DG-1`）の `Task` の数 |
| DX-8 | マーカーの状態 | `Task` の `uid` ごとの `DG-*`（どれにも当たらなければ空） |

⚠️ 問い 4 の答えが B なら、`DX-9`（計画の移動で見えなくなった遅れ —— 参考。判定とマーカーには使わない）を足す。

**N-23 表 T-318 — 明記されていない WBS の親の導き方**

| 行 ID | 規則 |
| --- | --- |
| IP-1 | 候補 ＝ 1 つ上の深さの行に描かれたバーのうち、期間を持ち（マイルストーンでない）、子を両端で包含するもの（親の `start` ≤ 子の `start` かつ 子の `finish` ≤ 親の `finish`）。⛔ `start` だけで判じてはならない（MUST NOT） |
| IP-2 | 候補がちょうど 1 つ ⇒ それを親とする |
| IP-3 | 候補が 0、または 2 以上 ⇒ 親を決めない（表 T-312 の `VO-4`、表 T-316 の `DW-2`）。⛔ 狭いほうを選んではならない（MUST NOT） |

⚠️ 候補を並べて人に選ばせる面（最新メモ 5.1 の「親入力機能」。並べ方は ① 包含 → ② 一部でも重なる → ③ 端が近い順、自動では選ばない）は問い 3 の答えで本書に入れるか決める。

**N-25 表 T-319 — マイルストーンの先行と達成の導き方**

| 行 ID | 規則 |
| --- | --- |
| MP-1 | 先行 (a): 当該マイルストーンより `finish` が前の、同じ深さの `Task` |
| MP-2 | 先行 (b): 1 つ下の深さの `Task` のうち、同じ深さの `Task` との親子の関係（明記、または表 T-318 で導いたもの）を持たず、`finish` が当該マイルストーンより前のもの。⭐ 1 つ下で止める（2 つ下は 1 つ下に親を持つので自然に落ちる） |
| MP-3 | 直前のマイルストーンより後に `finish` が来るものに限る。直前のマイルストーンは当該マイルストーンの先行とする（マイルストーンどうしを繋ぐ） |
| MP-4 | 達成: 先行が全部完了（`PS-2`）なら、達成日 ＝ 先行の最も遅い `actualFinish`。1 つでも未完了なら未達成。⭐ 手で入れた `actualFinish` が優先する |

⭐ 「前」は予定（`finish`）で判ずる（`JDG-483`）。`MP-3` が無いと直前のマイルストーンの先行が後のマイルストーンにも重なり、経路が太って読めなくなる。繋げば押し出しは直前のマイルストーンを経て届き、結果は同じまま経路が細くなる。

### 4.3b 既存の表に足す行

**N-20 表 T-109 に `IC-107` ・ `IC-108`**（`IC-107` は `IC-10` の直後。`IC-108` は一覧の面の行）

| 行 ID | 面 | 群 | 中身 | 要求 | 備考 |
| --- | --- | --- | --- | --- | --- |
| IC-107 | `App Header` | 表示 | 遅延診断を行う・診断の表示を終える | `FR-130` | — |
| IC-108 | 一覧の面（名は当てる前に見本で決める） | 表示 | 診断のレポートを写す | `FR-134` | — |

⚠️ 表 T-109 の列の並びと「備考」の欄の名は当てる者が今の表に合わせる。

**N-22 表 T-107 に `AM-19`**（`AM-18` の下）

| 行 ID | 用途 | 名 | 名の形・純粋性 | 中身 | 根拠 |
| --- | --- | --- | --- | --- | --- |
| AM-19 | 読む | `readDelayDiagnostics` | 動詞＋目的語・`pure` | 遅延診断のレポート（表 T-317）を凍結した値で返す。画面の値も文書も書かない | `FR-134` ／ 表 T-035 の `AG-4` |

⚠️ 純粋性の語（`pure` ／ `semi-pure-b` ほか）は表 T-107 の既存の行の使い方に合わせて当てる者が確かめる。

### 4.4 既存の文の編集（`CR-551` が当たった後の文を旧とする）

- **E-01 `FR-013`**: 色の文（`:2979`、`CR-551` の E-03 で黄の例外が付いた文）に「遅延診断の状態（表 T-315 の `DG-1` 〜 `DG-3`）は表 T-315 の色で描く」を足し、薄く描く文（`:2977`）に「黄（`PM-4`）と表 T-315 の状態は薄くしない」を足す。
- **E-02 `FR-049`**: マーカーの表示の切替の文に「診断を出しているあいだは `delayDiagnosticsShown`（`FR-133`）でも描く。`IC-40` で消すと、`S-63` と `delayDiagnosticsShown` をともに偽にする」を足す。⚠️ `FR-003` ・ `FR-013` ・ `FR-049` ・ `FR-094` ・ `S-63` は 1 つの閉路である（6.2 節）⇒ E-01 ・ E-02 ・ J-01 を 1 つの計画で書く。
- **E-03 〜 E-05**: `05-07-design.md:157`（`R2.5`）・ `:414`（`UF-28`）・ 表 T-072 の `NF-3` の「18」を「19」に。

### 4.5 原稿 JSON の編集（`_source/`）

| # | ファイル | 何を |
|---|---|---|
| J-01 | `settings.json` の `S-63` | 備考に「診断を出しているあいだは `delayDiagnosticsShown` でも描く（`FR-133`）」 |
| J-02 | `settings.json` | 表 T-236 に `S-385` 〜 `S-390`、`S-398`（下の表） |
| J-03 | `settings.json` | 表 T-206 に `S-391` 〜 `S-397`、`S-399`（下の表） |
| J-04 | `display-words.json` | `IC-107` ・ `IC-108` の語と説明、一覧の見出し、種別の語（記載漏れ ／ 矛盾 ／ 疑義 ／ ボトルネック）、拡張 1a ・ 3a の告げる文。⛔ 英語の語は利用者が決める（問い 6） |
| J-05 | 画面の値の置き場（表 T-206 の画面の部か `ScreenSession` の定義 —— 当てる者が測る） | `delayDiagnosticsShown`（真偽、既定 `false`、保存しない） |
| J-06 | `components.json` ・ 表 T-075 の原稿 | 新しいユニット 2（8 節） |
| J-07 | 用語（`tbl-glossary.md` は手書き —— 当てる者が確かめる） | 1.1 節の語 |
| J-08 | `row-id-prefixes.json` | 接頭辞 10（2 節の表） |

**設定値（案）**

| ID | 鍵 | 表 | 値（明 ／ 暗） | 意味 |
|---|---|---|---|---|
| S-385 | `bottleneckPathMarkerGround` | T-236 | `#f09199` ／ `#f4a7b9` | 桃色の地（`JDG-474`: ショッキングピンクではない） |
| S-386 | `bottleneckPathMarkerInk` | T-236 | `#16181d` ／ `#16181d` | 桃色の上の `!!`（`S-327` と同じ暗い色） |
| S-387 | `bottleneckMarkerGround` | T-236 | `#d32f2f` ／ `#ff5c5c` | 赤の地 |
| S-388 | `bottleneckMarkerInk` | T-236 | `#ffffff` ／ `#16181d` | 赤の上の炎 |
| S-389 | `unreliableMarkerGround` | T-236 | `#7e57c2` ／ `#b39ddb` | 紫の地 |
| S-390 | `unreliableMarkerInk` | T-236 | `#ffffff` ／ `#16181d` | 紫の上の `?` |
| S-391 | `bottleneckPathBangSpacing` | T-206 | 0.30 | `!!` の 2 つの `!` の中心の間隔（マーカーの径に対する比） |
| S-392 | `unreliableHookCentreY` | T-206 | 0.36 | `?` の鉤の中心の高さ（径に対する比、上端から） |
| S-393 | `unreliableHookRadius` | T-206 | 0.18 | `?` の鉤の半径（径に対する比） |
| S-394 | `unreliableStemBottomY` | T-206 | 0.62 | `?` の縦の線の下端（径に対する比） |
| S-395 | `bottleneckFlamePath` | T-206 | 単位の正方形の中の SVG の道（当てる者が見本で決める） | 炎の形 |
| S-396 | `bottleneckFlameScale` | T-206 | 0.70 | 炎の外接の辺（径に対する比） |
| S-397 | `bottleneckMinPushOutDays` | T-206 | 1 | ボトルネックとする押し出し日数の下限（稼働日） |
| S-398 | `parentConfirmInk` | T-236 | `#d32f2f` ／ `#ff5c5c` | 親を確かめるときの名称ラベルの色 |
| S-399 | `parentConfirmWeight` | T-206 | 700 | 同じく字の太さ |

⚠️ 値は 🔎（測って決めていない）。`S-392` 〜 `S-396` は触れる見本で決めること（`JDG-180` 〜 `JDG-193` の型）。色の比は、地と記号・地と行の帯の両方を当てる前に測ること（`DFC-728` が暗いテーマの比を持つ）。

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | 実体（診断）↔ 呼ぶ側 | `diagnoseDelay(document: Document, calendar: WorkingCalendar): DelayDiagnosticsReport` —— 純粋。`document.project.statusDate` が `null` なら `DX-1` を「診断しなかった」にして返す。文書を書かない |
| S-2 | 実体 ↔ 遅れの判定 | 黄の条件は `isDelayed` ／ `delayWorkingDays`（`CR-554` の後は `task-delay.ts`、`UF-129`）をそのまま読む。完了は `planActualState`（`plan-actual-state.ts`、`UF-127`）の完了。稼働日は `working-calendar.ts`（`UF-128`）。⛔ 同じ判定を書き写さない |
| S-3 | 実体 ↔ 配置と形 | 形の選択は `ProgressSymbol` に `'DG-1' \| 'DG-2' \| 'DG-3'` を足し、`DG-4` は今の `'PM-4'` と読む。選ぶ関数は `DelayDiagnosticsReport` の `DX-8` と `delayDiagnosticsShown` を入力に取る |
| S-4 | 形 ↔ 描き手（`schedule-task-figures.ts`） | `markerSvg` は `DG-1` → 地 `S-389`・記号 `?`、`DG-2` → `S-387`・炎、`DG-3` → `S-385`・`!!`、`PM-4` → `S-326`・`!`。寸法は 4.5 節の行と `S-328` 〜 `S-331` ・ `S-341` だけを読む |
| S-5 | 画面の値 ↔ 殻 | `delayDiagnosticsShown` は `ScreenSession` の真偽。`IC-107` で反転、文書の置き換えで偽、`IC-40` でマーカーを消すと偽。レポートは値が真のあいだ持つ（問い 2 の答えで「文書が変わるたびに S-1 を呼び直す」か決まる） |
| S-6 | 実体 ↔ `Agent API` | `readDelayDiagnostics(): DelayDiagnosticsReport` —— 凍結した複製（`AG-4`）。`delayDiagnosticsShown` も `S-63` も書かない |
| S-7 | レポート ↔ 一覧の面 | 一覧は `DX-3` ・ `DX-4` ・ `DX-6` の行を並べ、行を選ぶと `AM-16` と同じ寄せ方（`FR-055` ／ `S-77` ・ `S-78`）で寄せ、畳んだ行を開く。`IC-108` は `JSON.stringify(report)` をクリップボードへ |

---

## 6. グラフ（`ebc71984` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 対象 | 要求 / 参照 | 指している要求 | 扱い |
|---|---|---|---|
| `FR-013` | 6 / 17 | `FR-010` ・ `FR-011` ・ `FR-016` ・ `FR-043` ・ `FR-048` ・ `FR-049` | 例外の文を足すだけ。色の例外を引く者は無い |
| `S-63` | 1 / 3 | `FR-003` | 備考だけ。`FR-003` は名を引くだけ |
| `FR-049` | 13 / 39 | `FR-002` ・ `FR-003` ・ `FR-020` ・ `FR-029` ・ `FR-031` ・ `FR-043` ・ `FR-048` ・ `FR-076` ・ `FR-081` ・ `FR-090` ・ `FR-107` ・ `FR-108` ・ `FR-109` | 描く条件に 1 つ足す。⚠️ `FR-109` の表 T-273（`S-63` の出す／出さないで名称ラベルの書き出しを変える `LP-*`）は「マーカーを描くか」で読み直すこと —— `S-63` の値でなく描くかどうかで分岐しているか、当てる者が測る |
| `FR-047` | 3 / 6 | `FR-003` ・ `FR-054` ・ `FR-082` | 書き直さない（黄は今のまま） |
| `FR-094` | 8 / 31 | `FR-003` ・ `FR-013` ・ `FR-016` ・ `FR-039` ・ `FR-043` ・ `FR-055` ・ `FR-104` ・ `FR-109` | 書き直さない。新しい記号の寸法が `FR-094` の規則（ズームに追随させない）に乗る |
| 表 T-021b | 1 次 5（`FR-012` ・ `FR-013` ・ `FR-045` ・ `FR-047` ・ `FR-054`） | — | 書き直さない（棲み分けは `FR-132` の RATIONALE） |
| 表 T-236 | 1 次 9 | `FR-004` ・ `FR-007` ・ `FR-016` ・ `FR-019` ・ `FR-020` ・ `FR-029` ・ `FR-041` ・ `FR-081` ・ `FR-098` | 行を足すだけ。⚠️ 行の数を数える文があれば当てる者が測る |
| 表 T-107 | 1 次（`FR-022` ほか 6） | — | 行を足す。「18」の 3 か所は E-03 〜 E-05 |
| 表 T-109 | 1 次・2 次で 80 の要求 | — | 行を足すだけ。⚠️ `App Header` の並びを数える文（`FR-004` ・ `FR-029`）は当てる者が読む |
| 表 T-027 | 1 次・2 次で 26 | — | 書き直さない（診断は文書を書かないので取り消しの段を作らない） |
| `FR-055` | 10 / 27 | — | 書き直さない（全体表示は診断の前提でない） |
| `FR-005` | 4 / 10 | `FR-001` ・ `FR-016` ・ `FR-023` ・ `FR-081` | 書き直さない。導いた親は保存しないので、階層の移動の規則に触れない |
| `FR-015` | 5 / 12 | — | 問い 4 が B なら RATIONALE から指す |
| `FR-100` | 1 / 7 | `FR-016` | 書き直さない（未保存を立てないことの根拠として `FR-130` が指す） |
| `FR-064` | 0 / 1 | — | 書き直さない |

⭐ 導いた条項ごとに、届いた行を `rulings.md` で引いた（規則 02 の 1）: 当たるのは `JDG-368` ・ `JDG-370` ・ `JDG-371`（`CR-551`）、`JDG-08` ／ `JDG-92` ／ `JDG-112`（マーカーを描かないときの名称ラベルの位置。表 T-273 の読み —— 6.1 の `FR-049` の ⚠️）、`JDG-27`（マーカーは掴みシロの外側 —— 記号を変えても置き場は変えない）、`JDG-214` ・ `JDG-239`（マーカーを押したときの巡り —— 診断中も変えない）。

### 6.2 `induced.py`（5 群）

| 種 | 解決 | 種の中の辺 | 閉路 | 扱い |
|---|---|---|---|---|
| `FR-013 T-021 T-021b S-63 FR-049 FR-047 FR-094 FR-003` | 8/8 | 10 | 1: `FR-003` `FR-013` `FR-049` `FR-094` `S-63` | ⭐ E-01 ・ E-02 ・ J-01 を 1 つの計画で書く。`FR-003` と `FR-094` は書き直さない |
| `T-107 FR-028 FR-064 T-035` | 4/4 | 2 | 0 | — |
| `T-109 FR-055 FR-029` | 3/3 | 1 | 0 | — |
| `FR-005 T-015a FR-010 T-019a` | 4/4 | 2 | 0 | — |
| `T-027 FR-031 FR-100` | 3/3 | 1 | 0 | — |

---

## 7. 数の予測（`61bbd572` で測った。⚠️ `CR-552` 〜 `CR-554` が当たった後の数を起点に数え直すこと）

| 数 | 前 | 後（本書の分） | 内訳 |
|---|--:|--:|---|
| tables | 187 | ＋10 | 表 T-310 〜 T-319 |
| figures | 27 | ＋0 | — |
| rows | 2305 | ＋71 | 設定値 15、`VC` 15、`VS` 5、`VO` 4、`BD` 4、`DQ` 4、`DG` 4、`DW` 3、`DX` 8、`IP` 3、`MP` 4、`PLM-9` ・ `CH-7` ・ `VK-4` ・ `V-14` ・ `AM-19` ・ `IC-107` ・ `IC-108`（7）。⚠️ 問い 4 が B なら ＋1（`DX-9`） |
| uids | 162 | ＋9 | `FR-130` 〜 `FR-136`、`UC-015`、`GL-009` |
| 接頭辞 | 163 | ＋10 | 2 節の表 |

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ **当てる順**: `CR-551` 〜 `CR-554` と段 8 の後（依頼文の指示。利用者が別に言えば変える）。`CR-554` がファイルを割った後の名で書いた（`task-delay.ts` ほか）。⚠️ 当てる前に、下のファイル名が割った後の木に在ることを測り直すこと。

```
wave 0  spec     4 節の全部（N-01..N-25、E-01..E-05、J-01..J-08）+ npm run gen + gen:check
                 -> 仕様を逐語で読む試験がここで赤くなる（設計どおり）
wave 1  (並行、触るファイルが別。wave 0 の commit から切る)
  1a entity      新しいユニット src/entity/delay-diagnostics/（diagnose-delay.ts: FR-131 の 3 表、
                 FR-132 の 4 段、FR-135 の IP、FR-136 の MP、レポート T-317）—— 純粋。
                 読むだけ: task-delay.ts / plan-actual-state.ts / working-calendar.ts
  1b layout+svg  schedule-geometry の兄弟（ProgressSymbol の選択、S-3）、
                 schedule-task-figures.ts（markerSvg の 3 形と色、S-4）、親確認の名称ラベル（S-398・S-399）
  1c agent api   agent-api-members.ts（AM-19、S-6）
wave 2  shell+surface (直列、1 体)
                 screen の値 delayDiagnosticsShown（S-5）、IC-107・IC-108 の割り振り
                 （input-command-translator.ts、app-header-items.ts）、一覧の面（open-modals 系か新しい兄弟）、
                 frame-loop の兄弟（CR-554 の割った後の名で測る）
wave 3  仕様だけを読む試験の体（実装した体ではない）→ 併合した木で check.sh + vitest
```

- ⚠️ 1a を先に併合すると、1b ・ 1c は型 `DelayDiagnosticsReport` を 1a から取り込める。取り込めない間は、継ぎ目 S-1 の型を両方の依頼文に逐語で写す。
- ⚠️ `schedule-task-figures.ts` は `CR-551` の 1b も触る（`DELAY_*` の 4 つ）⇒ 本書は `CR-551` の後。
- ⚠️ 新しいユニットは表 T-075 の行と `components.json` が要る（検査 26b ・ 18 ・ 44 ・ 59）。番号は調整役から受ける。
- ⚠️ 性能: 前向きの計算は依存の数に比例する 1 回の走査である。問い 2 が A（文書が変わるたびに算出し直す）なら、段 8 の性能の測り（RISK-001、⛔ 測る前に利用者を呼べ）に「診断を出したまま編集する」場面を足すこと。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

| 所 | 何をする | 試験（いま引いているもの） |
|---|---|---|
| `src/entity/`（新しいユニット） | 5 節の S-1 | 無い（新しい） |
| `schedule-geometry.ts:73`（`ProgressSymbol`）・ `:459`（`PM-4` の選択）—— `CR-554` の後は兄弟 | S-3 | `markerStroke` を引く試験群（`CR-551` の 9 節の項目 2） |
| `src/adapter/svg-renderer/schedule-task-figures.ts:150`（`markerSvg`） | S-4 | 同上 |
| `src/adapter/agent-api-endpoint/agent-api-members.ts:380` | S-6 | `Agent API` のメンバを数える試験（「18」を名指すもの）を 19 に |
| `src/adapter/screen-renderer/app-header-items.ts` ・ `icon-roster.json`（生成）・ `input-command-translator.ts:678` 付近 | `IC-107` ・ `IC-108` | 入口の名簿の試験 |
| 画面の値 | `delayDiagnosticsShown`（S-5） | 無い（新しい） |

---

## 10. ⛔ この変更でやらないこと

- **修正**（観点メモ 9 節、最新メモ 4.2 節: 修正候補を出し、OK したら 1 つずつ直す）と **親入力機能**（最新メモ 5.1）—— ⚠️ 問い 3。推奨どおりなら `PND` に 2 行を起こして後の変更要求へ回す。
- **資源のボトルネック**と**構造のボトルネック**（合流点・余裕ゼロの連鎖長）—— ボトルネックメモ 4 節・8 節、`JDG-481`。
- **工数・コスト・出来高の整合**、`OutlineLevel` ／ `OutlineNumber` ／ `WBS` の食い違い、対になるサンプルの食い違い —— 観点メモ 10 節（モデルに無い、または取り込みの路の検査）。
- **一覧の面の形と寸法** —— 本書は面が持つ中身（表 T-317）と移動の規則（`FR-134`）だけを定める。⭐ 当てる前に触れる見本（1 ファイルの HTML ＋ Playwright）で形を決め、寸法の行は帯の外で調整役から受ける。
- **異常系のケアの一般**（`JDG-78`、`DFC-586`）。
- `docs/development-records/` の `pending-decisions.md` の答えの後の行、`changelog.md`、`A-appendix.md` の変更履歴（当てる者が書く）。

---

## 11. 利用者への問い（⭐ チャットで問う。答えを受けたら 4 節を確定する）

| # | 問い | 選択肢 | 推奨と理由 |
|---|---|---|---|
| 1 | **遅れて完了したタスクも赤（ボトルネック）にするか** —— 押し出しの大もとの多くは「もう終わったが、遅れて終わった」タスクである（ボトルネックメモ 2 節） | A: 赤は**終わっていない**タスクだけ。完了したものはレポートに「確定した押し出し」として出す ／ B: 完了したものも赤 | **A**。`JDG-471` の赤は「最優先でやれ」、桃色は「ボトルネック解消次第リカバリ」—— どちらも大もとがまだ動いていることを前提にしている。終わったタスクには支援を差し向けられない |
| 2 | **診断の結果の寿命** —— 診断を出したまま実績を直したとき | A: 診断を出しているあいだ、文書が変わるたびに算出し直す（色も一覧も常にいまの文書） ／ B: 押した時点の写しのまま。古くなったら「古い」と示し、押し直させる ／ C: 文書が変わったら診断を消す | **A**。黄（`PM-4`）も今すでに毎回導いている。観点メモ 9 節の「1 件直すと件数が 1 より多く減る。一覧は毎回作り直す」と同じ形で、紫を直した結果がその場で見える。計算は依存の数に比例する 1 回の走査で軽い（段 8 で測る） |
| 3 | **本書の範囲** —— 「見つける・示す・報告する」だけか、「直す」までか | A: 見つける・示す・報告する ＋ 親の自動導出（候補 1 つ）＋ 親確認の描き方まで。修正候補と親入力機能は次の変更要求 ／ B: 修正候補（自動・選択・人手）と親入力機能まで本書に入れる | **A**。指示は「特定までを一発で」であり、直す道は文書を書くので取り消しの単位・実績を書き換える印・`EditHistory` を決める必要がある（異常系のケアは `JDG-78` でリファクタの後）。A でも紫は `FR-005` の階層の移動や手の入力で消せる |
| 4 | **「計画を見直すと遅れが消える」の守り方** —— 仕様には「予定を動かしてよいのは公式に計画を見直したときだけ」という文が無い（0.2 節 F-7） | A: その方針を `FR-132` の RATIONALE に運用として書くだけ（`GRS` は強制しない） ／ B: A に加え、変更前の予定（`BaselineTask`）が読み込まれていればレポートに「計画の移動で見えなくなった遅れ」を**参考**として出す（判定とマーカーには使わない） ／ C: 実績のあるタスクの予定を動かすときに確かめを出す | **B**。2026-09-20 の裁定「`BaselineTask` は参考にとどめ、判定には使わない」（`JDG-481`）と両立し、黙って予定を動かしたことが読み手に見える。C は作成者の日常の編集を重くする |
| 5 | **マイルストーンの達成の導出を、診断の外にも効かせるか** | A: 診断の中だけ（画面のマーカーの状態・保存・書き出しは今のまま） ／ B: 画面のマーカーの状態にも効かせる（表 T-019a にマイルストーンの行を足す。保存はしない） | **A**。B は表 T-019a ・表 T-021 ・書き出しに届く別の変更であり、診断が要るのは A だけ。B が欲しければ別の変更要求にする |
| 6 | **語と色の値** —— `IC-107`「遅延診断」／「Delay Diagnostics」、`IC-108`「診断の結果を写す」／「Copy Diagnostics」、種別「記載漏れ ／ 矛盾 ／ 疑義」＝「Omission ／ Contradiction ／ Suspicion」、状態「信用できない」＝「Unreliable」。色は 4.5 節（桃色 `#f09199`、赤 `#d32f2f`、紫 `#7e57c2`） | 提案どおり ／ 直す | **提案どおり当て、使い勝手の試しで 1 行ずつ直す**（`CR-551` の問い 4 と同じ形） |

---

## 12. 台帳

### 12.1 裁定（`docs/development-records/rulings.md` に本書と同じ日に書いた）

- `JDG-470` 〜 `JDG-474` —— 0.1 節の逐語。状態は「指示」。
- `JDG-475` 〜 `JDG-485` —— 3 本のメモが「利用者の裁定」と記す事柄。⛔ **逐語の記録は無い**（`JDG-315` と同じ扱い）。状態は「指示」。
- ⛔ 当てる者が変える古い行: `JDG-371`（着地先を「本書の `FR-133`」に、状態を適用済へ）、`JDG-368`（読みの欄の「後の遅延診断の変更要求が … 足す」を本書へ）。

### 12.2 欠陥 ・ 裁定待ち

- 欠陥は起こさない（本書は新しい機能であり、仕様とコードの食い違いを見つけていない）。
- 裁定待ちは、11 節の答えを受けてから起こす（問い 3 が A なら「修正候補」と「親入力機能」の 2 行を `PND-581` から）。

### 12.3 調整役に宣言する番号（帯の外）

`FR-130` 〜 `FR-136`、`UC-015`、`GL-009`、`CH-7`、`PLM-9`、`VK-4`、`V-14`、`AM-19`、`IC-107` ・ `IC-108`、新しいユニット 2 つの `UF`、接頭辞 10。

---

## 13. 測り方の再現

```
# the tree
git log -1 --format=%h origin/refactor                      # -> ebc71984 when drafted; 61bbd572 after the rebase (2026-09-24)
grep -c '"S-326"' docs/spec/_source/settings.json           # -> 0 at ebc71984; 1 at 61bbd572 (CR-551 landed)
grep -n '"id": "S-63"' -A8 docs/spec/_source/settings.json  # -> default "true" at ebc71984; "false" at 61bbd572

# totals (before)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   -> tables=186  figures=27  rows=2282  uids=162   (ebc71984, 2026-09-24)
#   -> tables=187  figures=27  rows=2305  uids=162   (61bbd572, 2026-09-24, after CR-551)

# the bands and the new identifiers are unused (2026-09-24, ebc71984; re-measured at 61bbd572: only IC-107 in CR-562,
#   which the coordinator moved to IC-115, and the history hits below)
git grep -nE "\bS-3(8[5-9]|9[0-9])\b|\bT-31[0-9]\b|\bJDG-(4[7-9][0-9])\b|\bDFC-(89[0-9]|90[0-9])\b|\bPND-(58[1-9]|59[0-9]|600)\b|\bCR-561\b" origin/refactor -- . ':!previous-project-result'
#   -> only CR-110:397 (T-301..T-319 of the deleted tbl-datamodel.md) and CR-548:100 (text "T-293")
git grep -ohE "\bDP-[0-9]+|\bV-14\b|\bVK-4\b|\bAM-19\b|\bIC-10[78]\b|\bUC-015\b|\bGL-009\b|\bCH-7\b|\bPLM-9\b|\bFR-13[0-7]\b|\bVC-[0-9]+|\bVS-[0-9]+|\bVO-[0-9]+|\bBD-[0-9]+|\bDQ-[0-9]+|\bDG-[0-9]+|\bDW-[0-9]+|\bIP-[0-9]+|\bMP-[0-9]+" -- . ':!previous-project-result' | sort | uniq -c
#   -> DP-1..4 (used -> DX taken instead), V-14 x2 (docs/review/inventory local labels only), nothing else
for p in VC VS VO BD DQ DG DW DX IP MP; do grep -c "\"prefix\": \"$p\"" docs/spec/_source/row-id-prefixes.json; done   # -> all 0
python -c "import json;print(len(json.load(open('docs/spec/_source/row-id-prefixes.json',encoding='utf-8'))['prefixes']))"   # -> 162 at ebc71984; 163 at 61bbd572
git grep -nE "JDG-40[01]\b" origin/refactor -- .             # -> nothing at ebc71984; rulings.md:649-650 at 61bbd572 (0.2 F-1)

# maxima
grep -ohE "^\*\*UID\*\*: FR-[0-9]+" docs/spec/01-04-requirements.md | sort -t- -k2 -n | tail -1   # -> FR-111
for f in change-request/CR-55[1-4]-*.md; do grep -ohE '\bS-3[0-9]{2}\b' $f | sort -u | tail -1; done   # -> S-338 S-354 S-359 (CR-552/553 name but do not take)
grep -ohE '\bUF-[0-9]+\b' change-request/CR-554-*.md | sort -t- -k2 -n | uniq | tail -1          # -> UF-168

# graph (strictdoc export first: induced.py reads scratch/spec-check/sd-out)
strictdoc export docs/spec --formats=json --output-dir scratch/spec-check/sd-out
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>      # each ID of 6.1
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py <seeds>  # each row of 6.2

# the "18 members" sentences (section 3)
grep -n "18 のメンバ\|18 メンバ" docs/spec/*.md    # -> 05-07-design.md:157, :414, NF-3
```
