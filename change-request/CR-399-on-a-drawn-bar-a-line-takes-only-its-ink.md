# CR-399 — 描いた形状の上では、依存線は描いた線だけを取る

> **閉じるもの**: 利用者の裁定 `JDG-132`・`JDG-140`・`JDG-141`（2026-09-16）と、前に立つ者が決めた 3 つの読み。逐語と、前に立つ者が示した案は `docs/development-records/rulings.md` の各行（別の体が根で書いている）。
> - **崩れていたもの**: 表 T-023d の結びの「予定の帯 ⊃ 実績の帯 ⊃ 線の掴み代」と「線の掴み代の縦幅が実績の縦幅の下限（`S-6`）より狭いこと（MUST）」は、`CR-394` が表示の倍率を足してから、**描いた寸法では 6 段のどれでも成り立たない**（第 2 節）。同書の「既定値はこの条件を満たしている」は偽になっていた。
> - **`JDG-132`「問 1　依存線 B」**: 線の掴み代 `S-137`（線の左右）は縮めない。**描いた予定バー・実績バーの上では、`GR-13` は描いた線そのものしか取らない。** バーの外では `S-137` の掴み代のまま `GR-12` より上に在る。退けた案: A 線の掴み代にも表示の倍率を掛ける ／ C 文だけ直して崩れを受け入れる ／ D 判定の順を入れ替えて形状を線より先にする。
> - **`JDG-140`「A1. 含める」**: マイルストーン（表 T-012 の `SH-5`）の描いた図形の上でも、依存線は描いた線そのものだけを取る。
> - **`JDG-141`「B1. このまま受け入れ」**: 依存線が付く端では、予定の端の掴み代（`S-90`、描いたバーの外）のうち線の上下 `S-137` を線が取る。受け入れる。退けた案 B2（予定の端を依存線より先に判定する）。⇒ 仕様に明記する。
> - **前に立つ者の読み**: ① 描いた線そのものは、選択時の倍率（`S-178`）で太くした線と矢じり（`S-19`）を含む ② 表 T-252 の `DS-7` に、描いた形状の上で依存線を掴める幅は描いた線の太さであり表示の倍率で縮む、と注す ③ 矢羽根（`SH-2`）の欠けた角と線だけの形状（`SH-3` / `SH-4`）は描いた範囲の矩形とし、マイルストーンは描いた図形とする。
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `3844d1b2`。⚠️ `npm run gen` が刷り直したのは `_assets/tbl-settings.md` だけである（`src/**` の生成物は 1 バイトも動かなかった）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`**（画面に描かれたタスクとマイルストーンを直接掴んで動かすことで日程を変えられる）—— 表 T-054 の **`CH-1`** が受ける目標である。
既定の表示の倍率（50）では予定バーが 9.33px、実績バーが 5.33px に描かれ、線の掴み代は 12px のままなので、**依存線が横切る形状は、線の上下で丸ごと掴めなくなっていた**（第 2 節）。本書は描いた形状の上を常に形状に返し、線には形状の外で掴み代を残す。⭐ `GL-008` がタスクとマイルストーンを並べて書くので、`JDG-140` がマイルストーンを含めたことはこの目標の字面にも沿う。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1a`（矛盾する要求が存在しないか）** —— 表 T-023d の結びは入れ子を MUST とし、`FR-039` の 表 T-252 は `DS-1` で形状の大きさに描く比を掛け、`DS-7` で `S-137` に掛けない。⇒ **3 つを同時に満たす描いた寸法が、6 段のどこにも無い。** `JDG-132` が、入れ子から線を外し、形状の上で線が取る範囲を狭めることで解いた。
- **`R1a`（同一概念に複数の名前がない）** —— 「バー」はマイルストーンを含まない語として仕様に在る（表 T-023d の `GR-15` の行「マイルストーンは実績バーを持たない」）。`JDG-140` でマイルストーンを含めたので、「バー」のまま書くと語の範囲が 2 つになる。⇒ 表 T-012 の語「形状」を使い、「**描いた形状**」と呼んだ（決定 11）。
- **`R1b`（境界条件）** —— いちばん小さい段 33 では予定 6.16px・実績 3.52px に対し、線の掴み代は 12px。⭐ **「ただし」は段にも `S-137` の値にも依らない**ので、境界で崩れる条件が残らない。
- **`R1b`（DRY for Specs）** —— `S-137` の注が条件を括弧で写していた（「掴み代の縦幅が実績の縦幅の下限より狭いこと」）。⇒ 新しい注は規則を写さず、表 T-023d の結びを指すだけにした。`DS-7` の注も規則は写さず、結びを指す。
- **`R1b`（否定形には代替動作を）** —— `GR-16` の MUST NOT は、`GR-12` より下に留まるという代替が表の順そのものである。⇒ 規則は据え置き、理由の論証だけを直した（決定 4）。
- **`R1b`（暗黙の矛盾）** —— `DS-7` の理由「図形を縮めても、掴める大きさは縮めない」は、描いた形状の上の依存線（描いた線の太さで掴む、描く比で縮む）と字面で食い違って見える。⇒ `DS-7` に「掛けないのは掴み代の値である」の注を足した（決定 9）。

### ③ 利用者に問わずに決めたこと

⭐ `JDG-140` / `JDG-141` と前に立つ者の 3 つの読みで決まったことは下の「導き」に名指しし、**本書の体がみずから決めたことだけを「決定」として数える**ために、各行にどちらかを書いた。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 描いた形状は、`SH-1` 〜 `SH-4` では予定と実績を描いた範囲の矩形、`SH-5` では描いた予定と実績の図形そのものとする。矢羽根の欠けた角と線だけの形状の細い範囲も、その矩形で読む | **`JDG-140`**（マイルストーンを含める）と **前に立つ者の読み ③**。⭐ マイルストーンを「図形そのもの」と書いたのは、表 T-023d が `GR-18` を「`FR-043` が描いた正方形そのもの」、`GR-15` を「実績の図形の上」と書いている形に揃え、矩形へ広げない最も狭い形を採ったため | 欠けた角の上でも線は描いた線だけを取る —— 読み ③ の理由（欠けは小さく操作に効かない）を仕様に 1 文で置いた |
| 決定 2 | 描いた線そのものは、描いた本線と矢じり（`S-19`）とし、選ばれて太く描いた本線はその太さ（`S-178` を掛けた太さ）で取る | **前に立つ者の読み ①** | 無し |
| 決定 2a | 線の縁（`S-224`）は描いた線に含めない | ⚠️ **本書の体の読みである。** `FR-009` が「本線は敷かない区間でも描く —— 消えるのは縁だけである」と本線と縁を分けており、読み ① の「描いたインクそのもの」は線の色の本線と矢じりを指すと読んだ。縁は地の色（`S-146`）で描く | ⚠️ 縁はバーに重なる区間では描かない（`FR-009`）が、マイルストーンの図形に重なる区間で描くかは `FR-009` が述べていない ⇒ 問い 1 |
| 決定 3 | 「入れ子（MUST）」は **予定 ⊃ 実績** だけに残し、線の掴み代を入れ子から外した | `JDG-132` の中身。予定と実績には同じ描く比が掛かる（`DS-1`）ので、この 2 つの入れ子は倍率に依らない | 無し |
| 決定 4 | `GR-16` の MUST NOT は据え置き、理由を「**線の広がり**」で言い直した。「上の『ただし』を添えて上げても、描いた形状の外で `GR-13` より下の行の掴み代を行の全高にわたって奪う」を足した | 旧い論証「同じ 1 つの条件が、上げてよい線と上げてはならない線を分けている」の条件（`S-137` < `S-6`）が消えた。⭐ 依存線が掴み代を持つのは結んだ 2 つのタスクのあいだの経路の上だけであり、基準日線は行の全高を貫く —— **表 T-023d の順と 2 つの線の形から導ける事実であり、新しい規則ではない** | 無し |
| 決定 5 | 「広げた読む人が失うのは実績の縁」を「**描いた形状の外で線の近くに在る、`GR-13` より下の行の掴み代**」に言い直した | 描いた形状の上は広げても線に取られない。形状の外で線が勝つのは表の順（`GR-13` が `GR-14` / `GR-3` / `GR-4` / `GR-7` / `GR-12` より上）による | 無し |
| 決定 6 | 旧い「ただし線の掴み代の縦幅が…狭いこと（MUST）」を、保存値どうしの比較として残さず、新しい「ただし」で置き換えた | 保存値のまま読めば 12 < 16 は今も真だが、⛔ **その比較は描いた絵を 1 画素も縛らない**。条件の目的（「線が実績の帯を丸ごと奪う」を防ぐ）は描いた寸法の話である | `tests/` の 2 か所がこの比較を値で押さえており、緑のまま退役した規則を主張し続ける（第 6 節） |
| 決定 7 | 端の掴み代について、**守られること**（線から `S-137` より離れた所は端に残る。`S-90` はバーの上下にも届くので、既定の値では線の上にも下にも残る）と **守られないこと**（依存線が付く端では、端の掴み代のうち線から `S-137` 以内を線が取る）を書き、受け入れの理由（B2 を採ると、付いた端から `S-90` 以内の線が掴めず、走りには描く比が掛かり `S-90` には掛からないので、近い 2 つを結ぶ線は丸ごと掴めなくなる）を書いた | **`JDG-141`**。⭐ 前に立つ者の指示どおり数は設定値の名で書き、px を置かなかった。走りの長さは `05-07-design.md` の 表 T-221 の `LF-4` が `S-19` と `S-20` から導く —— 式は写さず行を指した | 無し |
| 決定 8 | 先例として、同じ結びの `GR-9` / `GR-17` / `GR-18`（描いた印そのものを当たり判定とする）と、`FR-009` の「バーに重なる区間では縁を敷かない」を指した | 裁定の理由に挙がった `S-180` の注は、前者の規則を値の側から述べたものである。後者は「バーは日程そのものであり、依存線の可読性のために削ってよいものではない」と、同じ向きを既に持つ | 無し |
| 決定 9 | 表 T-252 の `DS-7` の理由の欄に「⚠️ 掛けないのは掴み代の値である —— 描いた予定と実績の形状の上で依存線を掴める幅は、`S-137` ではなく描いた線の太さ（`S-18` に描く比を掛け、選ばれていれば `S-178` を掛けた値）であり、表示の倍率で縮む。規則は 表 T-023d の結びが持つ」を足した。`S-137` の注には足さなかった | **前に立つ者の読み ②**（`DS-7` か `S-137` の注の適切な方）。⭐ 食い違って見える文（「掴める大きさは縮めない」）が `DS-7` に在るので、読み違えが起きる所に置いた。`S-137` の注は既に結びを指しており、2 か所に書くと離れる。⭐ px は書かず、導出の設定値の名だけを書いた | 無し |
| 決定 10 | 新しい `（MUST）` を 1 つ書いた（「ただし」）。旧い 1 つを消したので、印の数は変わらない。`JDG-141` の受け入れと `DS-7` の注には印を足していない | 「ただし」は規則そのものの差し替えであり、⭐ 印の無い平叙文にすると、旧い MUST が持っていた強さを落とす。⭐ 判定の順は表 T-023d の頭の「上の行ほど優先すること（MUST）」が既に縛っており、受け入れは新しい規則ではない。⚠️ 検査 39 は注釈行を数えない —— 旧い条項は `tests/` の注釈だけが引いており、もともと「押さえていない」側に数えられていた | 検査 39 の数は 1355 のまま（基準線 1401 に触れない） |
| 決定 11 | 結びと注で「バー」を「**描いた形状**」と書き、`DS-7` の注と `S-5` / `S-137` の注も同じ語にした。⚠️ `FR-009` を指す文では同要求の語「バー」を残した | 第 0 節 ② の `R1a`。表 T-012 の表題が「タスク形状」である | 無し |
| 決定 12 | 表 T-023d の `GR-13` の行と、表 T-201 の後の注「重なるものが入れ子になり」を**動かさない** | `GR-13` の行は「条件は本表の結びが持つ」と指すだけで真。表 T-201 の後の注は線を名指ししておらず、予定 ⊃ 実績では真（第 3a 節） | 無し |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| 表 T-023d の結びが入れ子（予定 ⊃ 実績 ⊃ 線の掴み代）を MUST とし、`GR-13` を `GR-12` より上に置き、「線の掴み代の縦幅が `S-6` より狭いこと（MUST）」「既定値はこの条件を満たしている」と書いていた | `01-04-requirements.md:3317`〜`:3326`（当てる前） |
| 描く比 ＝ `S-234` ÷ 100 × `S-236`（0.6667） | `FR-039`（当てたあとの `01-04-requirements.md:6142`） |
| `DS-1` は 表 T-201 の px の行のうち群「縦の寸法」「依存線」などに描く比を掛け、`S-6` にも掛けると明言する。`DS-7` は `S-90` / `S-137` を含む掴み代に掛けない | 表 T-252（当てたあとの `01-04-requirements.md:6158` / `:6164`） |
| マイルストーンの縦幅は予定の縦幅に `S-17`（1.25）を掛けた値であり、予定の縦幅には描く比が掛かる | 表 T-201 の `S-17` と `DS-1` |
| 依存線の太さ `S-18` は 1.5px（群「依存線」⇒ `DS-1` で掛かる）。選ばれた線は `S-178` の 2 倍、縁は `S-224` の 3 倍。矢じりの長さと底辺は `S-19` | 表 T-201 と 表 T-206 |
| ⭐ 線の縁はバーに重なる区間では敷かない（MUST NOT）。「本線は敷かない区間でも描く —— 消えるのは縁だけである」 | `FR-009`（`01-04-requirements.md:2533`〜`:2535`） |
| 依存線は辺の中央に付き、走りは常にバーの外へ向く。入口の走りは `S-19` × `S-20`、出口はそこから `S-19` を引く | 表 T-018、表 T-018a の `RT-2`、`05-07-design.md` の 表 T-221 の `LF-4` |
| 予定の端の掴み代 `S-90` は「バーの上下と、端点の外側に」届く | 表 T-206 の `S-90` |
| マイルストーンの予定の図形は `GR-12` が掴み、実績の図形は `GR-15`、ダミーは `GR-18`（`FR-043` が描いた正方形そのもの）が掴む | 表 T-023d の `GR-12` / `GR-15` / `GR-18` |
| 表 T-023d の順は `GR-19` / `GR-1` / `GR-2` / `GR-5` / `GR-6` / `GR-17` / `GR-9` / `GR-10` / `GR-11` / `GR-8` / `GR-15` / `GR-18` / **`GR-13`** / `GR-14` / `GR-3` / `GR-4` / `GR-7` / `GR-12` / `GR-20` / `GR-16` / `GR-21` | 表 T-023d |
| 実装は同じ順で判定し、`GR-13` は折れ線までの距離 ≦ `S-137`（端の外へ丸く広がる）、`GR-3` / `GR-4` は予定の矩形を `S-90` だけ広げた箱の中で端の外側、`GR-12` は同じ箱で取る。マイルストーンには `GR-3` / `GR-4` を当てない | `src/entity/layout-engine/item-hit-area/item-hit-area.ts`（`isOnPolyline`、`isOnPlanEnd`、`grown(plan, slop.planEndpoint)`） |
| `JDG-38` の逐語は「依存線の優先度を上げてよい。ただし、依存線の縦幅は実績の縦幅より狭くしろ。」。条件を「掴み代の縦幅が実績の縦幅より狭いこと」と**読んで**着地していた | `docs/development-records/rulings.md` の `JDG-38` |
| ⭐ **本書は `JDG-38` の逐語を守る** —— 描いた形状の上で線が取る縦幅は描いた線の太さ（`S-18` × 描く比、選ばれて × `S-178`）であり、実績の縦幅（`S-6` × 描く比 以上）より狭い。両方に同じ描く比が掛かるので 6 段すべてで成り立つ。⚠️ 替わるのは `JDG-38` の**読み**である | 同上と 表 T-252 の `DS-1` |

## 2. 測った

**描いた寸法（`S-4` 28 × 描く比、実績 ＝ 28 × `S-5` 0.5715 × 描く比、線の掴み代 ＝ `S-137` 6 × 2、描いた線 ＝ `S-18` 1.5 × 描く比）**

| 表示の倍率 | 描く比 | 予定 | 実績 | 線の掴み代 | 予定 ⊃ 線 | 実績 ⊃ 線 | 描いた線（選ばれて） |
|--:|--:|--:|--:|--:|:--:|:--:|--:|
| 33 | 0.2200 | 6.16 | 3.52 | 12 | ✕ | ✕ | 0.33（0.66） |
| 50（既定） | 0.3333 | 9.33 | 5.33 | 12 | ✕ | ✕ | 0.50（1.00） |
| 66 | 0.4400 | 12.32 | 7.04 | 12 | ○ | ✕ | 0.66（1.32） |
| 75 | 0.5000 | 14.00 | 8.00 | 12 | ○ | ✕ | 0.75（1.50） |
| 85 | 0.5667 | 15.87 | 9.07 | 12 | ○ | ✕ | 0.85（1.70） |
| 100 | 0.6667 | 18.67 | 10.67 | 12 | ○ | ✕ | 1.00（2.00） |

測り方: `python -c` で `s / 100 × 0.6667` を掛けた（小数第 2 位へ丸め）。実績の床 `S-6` 16 × 描く比 は各段で実績と同じ値になる。
⇒ **「実績 ⊃ 線の掴み代」は 6 段すべてで偽**、「予定 ⊃ 線の掴み代」は 33 と 50 で偽。⚠️ 保存値のまま比べれば 12 < 16 は真であり、`tests/` の 2 か所はその比較だけを押さえているので緑のままだった。

⭐ **`JDG-141` の数（前に立つ者が利用者に示した値。本書は仕様に px を置かず、ここで確かめただけである）**: 既定の倍率で端の掴み代の縦 ＝ 予定 9.33 ＋ `S-90` 12 × 2 ＝ 33.33、線はその中段 12 を取り、上下に 10.67 ずつ残る。入口の走り ＝ `S-19` 7 × `S-20` 2 × 0.3333 ＝ 4.67、出口 ＝ 4.67 − 7 × 0.3333 ＝ 2.33。⇒ 6 段すべてで走りは `S-90`（12、描く比を掛けない）より短い（100 でも 9.33 / 4.67）。

| 数 | 値 | 測り方 |
|---|--:|---|
| 当てる前の検査 | **ALL GREEN**（exit 0） | `bash .claude/skills/spec-graph-check/check.sh` |
| 当てたあとの検査（`JDG-140` / `JDG-141` と 3 つの読みを当てた 2 巡目） | **ALL GREEN**（exit 0）、`tables=151 figures=11 rows=1962 uids=153` | 同。当てる前の出力と `diff` —— 動いたのは検査 13（参考）の行番号のずれ、PRECHECK の変更ファイル数、検査 22 の数（224 → 225、本書）、検査 42 の数（下の行）、`PD-` / `D-` / `R-` の走査ファイル数（本書のぶん 1 つ）だけ |
| 検査 39 の `（MUST）` / `（MUST NOT）` | 1840 印、押さえ 485、押さえ無し 1355 —— **当てる前と同じ** | 同 |
| 検査 42（`tests/` の注釈が引く、原稿に無い引用） | 384 → 387 | 同。`check-quoted-source.py --list` で、増えた 3 つが `tests/unit/layout-engine.test.ts:2003` / `:2005` と `tests/unit/t-023d-the-plan-start-is-the-boundary.test.ts:1083` であることを確かめた（第 6 節） |
| 足した `（MUST）` / 消した `（MUST）` | +1 / −1 | 決定 10 |
| 単体試験（1 巡目を当てたあと、2 巡目を当てたあと） | どちらも 7771 通過、**9 失敗（5 ファイル）** —— 同じ 9 件 | `npx vitest run` |
| 同じ 5 ファイルを、原稿 3 つを `HEAD` のバイトへ戻して走らせた | **同じ 9 失敗** ⇒ ⭐ **本書が起こした失敗は 0** | `git show HEAD:<原稿>` で戻し、5 ファイルだけを走らせ、本書の版へ戻した。失敗は `uf-36`（MSPDI の参照資料が作業木に無い）、`SWS-3`（`RP-1` / `RP-4` の境目）、`cr-394` の `FR-077` の床 2 件、`fr-052` の境界のドラッグ 5 件、`uf-39-40` の `PI-21` 1 件 |

## 3. グラフ

**`impact.py GR-13 S-137 S-5 T-023d`**:
- `GR-13` を指すのは `FR-016`（`01-04-requirements.md:3319`、`:3417` 付近）と 表 T-206 の節。
- `S-137` を指すのは `FR-016`（`:3266`、`:3317`、`:3325`）、`FR-039`（`DS-7`）、表 T-206 の `S-230`。
- `S-5` を指すのは `FR-094`（`:1230`）、`FR-016`（`:3314`）、表 T-201 の節（5 箇所）。
- 表 T-023d を指す要求は 20 件、2 次は 41 件 —— ⭐ **本書はその表の行を 1 つも動かさず、結びの 1 段と `DS-7` のセルと値の注 2 つだけを書く。**

**`induced.py GR-13 GR-12 GR-16 S-137 S-5 S-4 S-6 S-18 DS-1 DS-7 FR-009 FR-039`**: 12 種すべて解決、辺 9、**閉路 1 —— `GR-13` ↔ `S-137`**（値の行と、その規則を持つ要求のあいだの作法の閉路）。⇒ `S-137` の注と表 T-023d の結びを**同じ 1 巡で**書いた。`GR-13` の行は動かしていない。`DS-7` は閉路の外に在り、結びを指すだけである。

**届いた行を `rulings.md` で引いた**（`S-137` / `入れ子` / `GR-13` / `依存線の優先` / `依存線の縦幅` / `基準日線`）: `JDG-38` の 1 件 —— 第 1 節のとおり、本書は同行の逐語を守り、読みを替える。`JDG-34`（上下のレーンに割らない）と `JDG-35`（予定の縦幅は実績より大きい）は据え置きの入れ子（予定 ⊃ 実績）と両立する ⇒ 衝突 0。⚠️ `JDG-140` / `JDG-141` はこの作業木の台帳にまだ無い（根で書かれている）。

## 3a. 影響 —— 入れ子を述べる・前提にしている場所の一覧

`docs/spec` 全体を `入れ子` / `S-137` / `線の掴み代` / `GR-13` / `GR-16` / `実績の縦幅` / `実績の縁` で引き、`docs/spec` の外は `previous-project-result/13-grab-areas` と `tests/` と `src/` を同じ語で引いた。行番号は当てる前の木のもの。

| # | 所 | 述べること | 本書の前提で | 本書 |
|--:|---|---|---|---|
| 1 | `01-04-requirements.md:3317` | 入れ子（MUST）—— 予定 ⊃ 実績 ⊃ 線の掴み代（`S-4` / `S-6` と `S-137`） | 線の部分が偽 | **書き換えた** —— 予定 ⊃ 実績 だけを残した（頭の 28 字は据え置き） |
| 2 | `:3318` | 内側ほど狭いので外側の縁が必ず残る | 予定 ⊃ 実績 で真 | 据え置き |
| 3 | `:3319` | `GR-13` を `GR-12` より上に置く（MUST） | 真 | 据え置き |
| 4 | `:3320` | ただし線の掴み代の縦幅が `S-6` より狭いこと（MUST） | 描いた寸法で 6 段すべて偽 | **「ただし」に置き換えた**。描いた形状の範囲、描いた線の範囲、形状の外の掴み代、理由、先例 2 つ、端の掴み代の受け入れを足した |
| 5 | `:3321` | 既定値はこの条件を満たしている | 偽 | **消した** |
| 6 | `:3322` | `GR-16` を上げてはならない（MUST NOT）—— 行の全高を占める | 規則は真、論証は条件に依っていた | 規則は据え置き、**「ただし」を添えても変わらない理由を足した** |
| 7 | `:3323` | 同じ 1 つの条件が、上げてよい線と上げてはならない線を分ける | 条件が消えて偽 | **「線の広がり」に書き換えた** |
| 8 | `:3324` | この条件が縛るのは既定値である | 偽 | **「『ただし』は `S-137` の値に依らない」に書き換えた** |
| 9 | `:3325` | 読む人が `S-137` を広げたとき切り詰めない（MUST NOT） | 真 | 据え置き |
| 10 | `:3326` | 広げた読む人が失うのは実績の縁 | 偽（描いた形状の上は取られない） | **書き換えた** |
| 11 | `:3266` 表 T-023d の `GR-13` の行 | 掴み代は `S-137`、`GR-12` より上 —— 条件は本表の結びが持つ | 指すだけなので真 | 据え置き |
| 12 | `:3314` | 予定の縦幅を実績より広く取ってよい（`S-4` / `S-5`） | 真 | 据え置き |
| 13 | `:3349` | ダミーの当たり判定の縦幅は実績の帯に従う | 入れ子の員（実績の帯）であり触れない | 据え置き |
| 14 | `:2533` `FR-009` | バーに重なる区間では線の縁を敷かない。本線と縁を分ける | 同じ向き。決定 2a の根拠 | 据え置き（結びから指した） |
| 15 | `:1230` `FR-094` | 縦の床は予定に 1 度だけ当てる | 予定 ⊃ 実績 を倍率に依らず保つ根拠 | 据え置き |
| 16 | `:6145` 表 T-252 の `DS-1` | 形状の大きさと `S-6`、`S-18`、`S-19` に描く比を掛ける | 崩れの原因の片方 | 据え置き |
| 17 | `:6151` 表 T-252 の `DS-7` | `S-137` / `S-90` に描く比を掛けない —— 「掴める大きさは縮めない」 | 値については真。字面が描いた形状の上の依存線と食い違って見える | **理由の欄に注を足した**（決定 9） |
| 18 | `_assets/tbl-settings.md` の `S-5` の注（原稿 `_source/settings.json:343`） | 既定の姿 予定 28 ⊃ 実績 16.002 ⊃ 依存線 12、線の掴み代が実績より狭い | 線の部分が偽 | **書き換えた** —— 線を外し、描く比を掛ける前の値であることと、`S-137` が入れ子に入らないことを足した |
| 19 | 同 `S-137` の注（原稿 `:2648`） | 条件を括弧で写し、既定がそれを満たすと書く | 偽 | **書き換えた** —— 規則は写さず結びを指す |
| 20 | 同 表 T-201 の後の注（原稿 `:1427`、刊行物 `:144`） | 「重なるものが入れ子になり、行の高さは 1px も動かない」 | 線を名指ししておらず、予定 ⊃ 実績 で真 | 据え置き |
| 21 | 同 `S-230` の注 | 細い印を掴ませる値は `S-137` が持つ | 無関係 | 据え置き |
| 22 | 同 `S-90` の注 | 端点の掴み代は「バーの上下と、端点の外側に」届く | `JDG-141` の受け入れの根拠 | 据え置き（結びから指した） |
| 23 | `docs/development-records/rulings.md` の `JDG-38` | 読み「掴み代の縦幅が実績の縦幅より狭いこと」 | 読みが替わる（逐語は守られる） | ⛔ **持ち場の外** —— 前に立つ者が合流のときに直す |
| 24 | `previous-project-result/13-grab-areas/README.md:68`、`:131`、`grab-areas-sample.html:186`、`:208`、`:212`、`:331`、`:346`、`:349`、`:437`、`:457` | 「予定 28 ⊃ 実績 16 ⊃ 依存線 12」、`S-137` に「`S-6` より狭いこと（MUST）」を足す | 偽 | ⛔ **動かさない** —— 前の成果の記録であり、表 T-023d の結びが「見本そのものを仕様として読んではならない（MUST NOT）」と定めている |
| 25 | `tests/unit/layout-engine.test.ts:1997`〜`:2021`、`tests/unit/t-023d-the-plan-start-is-the-boundary.test.ts:1076`〜`:1140` 付近 | `S-137` × 2 < `actualMin` を値で主張し、注釈が旧い条項を引く | 退役した規則を緑のまま主張する | ⛔ 持ち場の外 —— 第 6 節 |
| 26 | `tests/unit/t-023d-the-plan-start-is-the-boundary.test.ts:199` | 定数「⭐ **重なる掴み代の縦幅は入れ子とすること（MUST）」を原稿に探す | 原稿に残した | 据え置き（緑を確かめた） |
| 27 | `src/entity/layout-engine/item-hit-area/item-hit-area.ts` の `GR-13` | 折れ線から `S-137` 以内を無条件に取る | 本書に反する | ⛔ 持ち場の外 —— 第 6 節 |

## 4. 当てた中身

| ID | 置き場（当てたあと） | 中身 |
|---|---|---|
| 表 T-023d の結び（`FR-016`） | `01-04-requirements.md:3317`〜`:3339` | 決定 1〜8・10・11 —— 入れ子を 予定 ⊃ 実績 に絞った（`:3317`）。「ただし」（MUST、`:3320`）、描いた形状の範囲（`:3321`〜`:3322`）、描いた線の範囲（`:3323`〜`:3324`）、形状の外の掴み代（`:3325`）、理由と先例 2 つ（`:3326`〜`:3328`）、端の掴み代で守られることと守られないことと受け入れの理由（`:3329`〜`:3332`）、`GR-16` の論証（`:3333`〜`:3335`）、`S-137` を広げたときの帰結（`:3336`〜`:3339`） |
| 表 T-252 の `DS-7`（`FR-039`） | `01-04-requirements.md:6164` | 決定 9 —— 理由の欄に注 |
| `S-5` の注 | `_source/settings.json:343`（表 T-201、刊行物 `_assets/tbl-settings.md:77`） | 決定 3・11 —— 既定の姿から線を外し、描く比を掛ける前の値であること、`S-137` が入れ子に入らないこと、描いた形状の上で線が何を取るかは結びが持つこと |
| `S-137` の注 | `_source/settings.json:2648`（表 T-206、刊行物 `_assets/tbl-settings.md:305`） | 決定 11・12 —— 規則を写さず結びを指す（マイルストーンの図形を含む）、値はその規則に縛られない、`GR-16` の理由は結びが持つ |
| 生成物 | `_assets/tbl-settings.md` | `npm run gen`（exit 0） |

## 5. 数の予測

当てる前 `tables=151 figures=11 rows=1962 uids=153`。本書の差: tables 0 ／ figures 0 ／ **rows 0**（結びの段と 1 つのセルと注を書き換えただけで、行を足していない）／ uids 0 ⇒ 予測 `tables=151 figures=11 rows=1962 uids=153`。
**実測: `tables=151 figures=11 rows=1962 uids=153` —— 予測どおり。**

## 6. 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `src/entity/layout-engine/item-hit-area/item-hit-area.ts` の `GR-13` | 押した点が描いた形状（`SH-1` 〜 `SH-4` は描いた範囲の矩形、`SH-5` は描いた図形）の中なら、`S-137` ではなく描いた線（本線の太さの半分、選ばれていれば `S-178` を掛けた太さの半分、および矢じり）で判定する。形状の外では今のまま `S-137`。⚠️ 描いた線の太さは描く比を掛けたあとの `S-18` であり（`DS-1`）、`PointerSlop` はいまそれを運んでいない |
| 同 | ⚠️ `isOnPolyline` は端の外へ丸く広がるので、辺の中央に付く線の付け根はいま `S-137` ぶん形状の中へ食い込んでいる —— 本書では形状の中の食い込みが消える。⭐ 端の外側（`GR-3` / `GR-4`）は `JDG-141` のとおり線が取るままである |
| `tests/unit/layout-engine.test.ts:2010` | `S-137` × 2 < `actualMin` は退役した条件である。⛔ 緑のまま残すと、仕様に無い規則を主張し続ける |
| `tests/unit/t-023d-the-plan-start-is-the-boundary.test.ts:1093`〜`:1140` 付近 | 「線の帯は実績の床より狭い」の値の主張（`:1130`）を退け、第 7 節の押下に替える |
| ⭐ **検査 42 を 384 → 387 に押し上げた、旧い条項を引く注釈 3 つ**（合流のときに別の体が直す） | `tests/unit/layout-engine.test.ts:2003`「狭くなければ、線が実績の帯を丸ごと奪う」 ／ `tests/unit/layout-engine.test.ts:2005`「ただし線の掴み代の縦幅が、実績の縦幅の下限（`S-6`）より狭いこと（MUST）」 ／ `tests/unit/t-023d-the-plan-start-is-the-boundary.test.ts:1083`「狭くなければ、線が実績の帯を丸ごと奪う」。⚠️ 同じ注釈の 3 行にまたがる引用（`layout-engine.test.ts:2001`〜`:2003`、`t-023d-the-plan-start-is-the-boundary.test.ts:1081`〜`:1083`）も旧い「ただし」を含むが、検査 42 の数には入っていない |

## 7. 試験が主張すべきこと

1. 依存線が描いた予定バーを横切るとき、**バーの矩形の中で、描いた線の外かつ線から `S-137` 以内** を押すと、線が無いときと同じもの（`GR-12`）を掴む —— 表示の倍率の 6 段すべてで。
2. 同じ所で、**描いた線の上** を押すと `GR-13` を掴む。選ばれた線では `S-178` を掛けた太さの内側で、矢じりの上でも `GR-13` を掴む。
3. 実績バーの矩形の中でも 1 と 2 が成り立つ（`GR-5` / `GR-6` の掴み代の外で押す）。
4. マイルストーンの予定の図形の上でも 1 と 2 が成り立つ（`JDG-140`）。
5. 矢羽根（`SH-2`）の欠けた角と、線だけの形状（`SH-3` / `SH-4`）の描いた範囲の矩形の中でも 1 が成り立つ。
6. **描いた形状の外** で線から `S-137` 以内を押すと `GR-13` を掴み、表で下に在る行に勝つ —— ⭐ 依存線が付く端の外側で、線から `S-137` 以内は `GR-13`、線から `S-137` より離れた `S-90` の中は `GR-3` / `GR-4`（`JDG-141`）。
7. `S-137` を広げても 1 は変わらない。
8. `GR-16` は引き続き `GR-12` より下に在る。

## 9. ⛔ この変更でやらないこと ／ 返した問い

- ⛔ `S-137` / `S-90` の値を動かさない —— `JDG-132` の中身である。
- ⛔ 表 T-023d の順を動かさない —— 案 D と B2 を退けた裁定である。
- ⛔ `previous-project-result/13-grab-areas` を動かさない（第 3a 節の 24）。
- ⛔ `src/`、`tests/`、`docs/development-records/rulings.md`、`docs/development-records/defects.md` に触れない —— **持ち場の外である。**
- ⛔ 変更履歴（`docs/development-records/changelog.md`）に行を足さない —— ⚠️ 前に立つ者が合流のときに足す。
- ⚠️ **問い 1（前に立つ者へ）**: **描いた線から縁（`S-224`）を外した読み（決定 2a）でよいか。** `FR-009` は本線と縁を分け、バーに重なる区間では縁を敷かないと定めるが、⚠️ **マイルストーンの図形に重なる区間で縁を敷くかは述べていない** —— 敷くなら、その区間の「描いたインク」は縁の幅（`S-224` 倍）まで広がる。本書は字面の「線」を本線と矢じりに限った。
- ⚠️ **問い 2（前に立つ者へ）**: `FR-009` の「バーに重なる区間」に、`JDG-140` を受けてマイルストーンの図形を含めるかは本書の範囲の外に置いた（同要求の語「バー」を残した）。当たり判定と縁の描き方が「描いた形状」と「バー」で分かれたままになる。
