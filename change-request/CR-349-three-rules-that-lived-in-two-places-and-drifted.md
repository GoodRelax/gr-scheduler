# CR-349 — 2 か所に置かれ、片方だけが動いた規則を 3 つ畳む

⭐⭐ **本変更要求も新しい裁定を 1 つも持たない。**
**`CR-348` を書いた直後に、4 体を「塞がっている」を**反証**しに出した。
その報告が、台帳の ✅ を 2 つ覆し、`CR-348` 自身の誤りを 1 つ見つけた。**

**台帳の行**: `D-124`（起動時に読めなかったとき）・`D-231`（削除の確認の文）・
`D-239`（項目名を英語に保つ理由）・`D-238`（表 T-104 の境目）。

⛔⛔ **3 つとも同じ形である** ——
**同じ規則が 2 か所に在り、片方が動いたことに他方が気づかなかった。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

`CH-3`（仕様が自分と整合していること）。⭐ **本書は機能を 1 つも足さない。**
**足したものを取り消し、指し先を直しただけである。**

### ② レビュー観点のどの条項を当て、何が出たか

**`R2`**（規則の在り処が 1 つ）。⛔ **当てて出たのは、`CR-348` を含む 3 本の変更要求が、
いずれも「もう 1 か所に既に在る」ことを確かめずに書いていたことである。**

```
CR-347  表 T-233 に理由の行を足した   ⇒ 表 T-024a の OP-14 が全文を持っていた
CR-348  FR-087 にその規則を書いた      ⇒ 同上。しかも新しいほうの行を指した
CR-347  1.9 に理由を書いた             ⇒ FR-038 は「同表が持つ」と言っていた
CR-348  表 T-104 に 7 行足した          ⇒ 1 行は設定の鍵ですらなかった
```

⭐⭐ **`induced.py` は本件を捕まえられない** —— **種はどれも仕様書に在り、
辺も閉路も 0 であった。**⛔ **グラフが見るのは「参照が在るか」であって、
「同じ規則が 2 度述べられていないか」ではない。**

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

1. ⭐⭐ **`CR-347` が足した理由の行を、席ごと取り下げた** ——
   **`RS-26` が同じ場面を、逆の作法（`NT-1` 対 `NT-3a`）で既に持っていたからである。**
   ⛔ **逆の選び方もありうる**（`RS-26` を退け、新しいほうを残す）——
   ⚠️ **`RS-26` を採ったのは、`OP-14` と実装（`single-html-shell.ts:421`）が
   どちらも既にそちらを向いていたからである。**
2. ⭐ **`FR-091` の「その場で編集する形」を落とした** ——
   **`FR-085` が 2026-09-01 の裁定でプロパティパネルの道に変わっており、
   「揃えろ」と言いながら違う形を名指していた。**
   ⚠️ **`FR-091` の STATEMENT はいまも「その場で入力・変更できるようにすること」と書く** ——
   **落としたのは道の描写だけであり、この句は残してある。**
3. ⭐ **項目名を英語に保つ理由を 1.9 から 表 T-016 の前文へ「移した」** ——
   ⛔ **写していない。**⚠️ **利用者の裁定は「前文に書く」であり、
   1.9 に残せば 2 か所になる。**
4. ⭐ **`K-117`（`cornerRadiusPx`）を 表 T-104 から退けた** ——
   **`documentSettings` の鍵ではなく `HighlightBox` の列である。**

---

## 1. ⭐ 反証しに行かせた結果（**本書はここから出た**）

⛔ **4 体に「確かめてこい」ではなく「反証してこい」と書いた**（規則 05 の 7）。
**15 行のうち 5 行は、塞がっていなかった。**

| 行 | 台帳が言っていたこと | **実測** |
|---|---|---|
| `D-15` | `IV-12` が稼働日で数える | ⭐ **3 経路とも暦日。継ぎ目の試験 8/8 が緑** |
| `D-154` | 深さ 5 の掴み代が覆われる | ⭐ **幅 200 では覆われない**（1px 掃きで確認）|
| `D-219` | 段の名前が仕様に無い | ⭐ **表 T-238 に 4 行在る。実装の仕事は無い** |
| `D-238` | 6 つの設定に名が無い | ⭐ **足した 6 つは画面に語で出る**（7 つ目は退席）|
| `D-124` | 読めなかったときに何も告げない | ⭐ **`BT-1` は告げる。壊した文書を仕込んで実測** |

⭐⭐ **`D-124` の実測がいちばん効いた** ——
**体は出荷ビルドの写しに壊れた文書を仕込んで開き、アプリが `RS-25` の語で告げ、
初期表示用のテンプレートへ降りることを示した。**⛔ **黙って空にはしていない。**
⇒ **台帳の「告げる行が無い」は最初から偽であり、`CR-347` はそこに行を足していた。**

---

## 2. ⭐ 書くもの

### 2.1 表 T-233 の行を 1 つ返し、`FR-087` は `OP-14` を指すだけにする（`D-124`）

⛔ **`OP-14` の逐語（`CR-347` より前から在る）:**

> **黙って捨てずに通知すること（MUST）。** 作法は 表 T-037 の `NT-1` とし、
> 運ぶ理由は 表 T-233 の `RS-26` とする。
> **そのうえで 表 T-034 の次の順位へ降りること（MUST）。空で起動してはならない（MUST NOT）**

⇒ **`CR-347` が足した行は退席。`FR-087` は `OP-14` を指し、規則を書き写さない。**
理由 44 → **43**、語 537 → **535**。
⚠️ **1 行が 2 対を持つ**（本文と次の一手）——
⛔ **前に立つ者は 1 を引いて 536 と書き、仕様だけを読む体が生成器の形を読んで訂正した。**

### 2.2 削除の確認から、偽の約束を落とす（`D-231`）

⛔⛔ **`CR-346` は「落とした」と記したが、原稿には残っていた** ——
`_source/display-words.json` の `QN-1` / `QN-2`。
⭐ **体の実測: `Ctrl` ＋ `Z` は 4/4 回すべてを戻す**（svg の text 699 → 692 → 699、
行の並びは 1 バイトも違わず戻る）。⇒ **句を落とした。**

⛔⛔ **台帳が名指していた `FR-046` は基準日の要求であり、削除とは無関係であった。**
⭐ **確認を課すのは `FR-032`、条件を持つのは `FR-031`。**
⚠️ **`FR-031` は取り消せる削除でも長い連鎖なら確認を許しており、確認そのものは正しい。**
⇒ **偽であったのは文だけなので、要求は 1 文字も直していない。**

### 2.3 項目名を英語に保つ理由を、指されている表へ移す（`D-239`）

⛔ **`CR-347` が書いた先は 1.9 表記規約であり、表 T-016 の前文ではなかった** ——
**`_assets/tbl-property-items.md` にも `_source/property-items.json` にも
「交換形式の列名」は 0 回。**⇒ **`FR-038` の「理由は同表が持つ」は偽のままだった。**
⇒ **前文（生成器 `property_items_json_to_md.py`）へ移し、1.9 は同表を指すだけにした。**

### 2.4 `FR-091` は道を描かず、`FR-085` を指す（`D-232` の前提）

⛔ **「`FR-085` と揃えて**その場で編集する形**とし」** ——
**`FR-085` は 2026-09-01 の裁定でプロパティパネルの道になっている。**
⇒ **道の描写をやめ、指すだけにした。**⭐ **これで `FR-085` が動けば黙って追随する。**

### 2.5 表 T-201 の結びに、例外が在ることを書く（`D-241` の副産物）

⛔⛔ **`FR-017` が 2026-09-03 に彫った例外を、表 T-201 の結びが知らなかった** ——
同結びは「**文字の大きさは段階によらず `rulerFont` とし**」と例外なしに書き、
`FR-017` は「曜日の行だけ `S-219` の比で小さく刷ってよい（MAY）」と書いていた。
⇒ **2 つの文書が違うことを言っていた。**

⭐ **見つけたのは、どちらを信じるか決めねばならなかった 仕様だけを読む体である。**
⛔ **例外を書き写してはいない** —— **規則は `FR-017` が持つ。**
⭐ **併せて、例外が結びの理由（段階 → 文字 → 判定 の循環）を壊さないことを書いた** ——
**比は段階ではなく行に掛かるので、循環に入らない。**

### 2.6 表 T-221 の `LF-1` —— 刻みは段ではなく行が決める（`D-240` の副産物）

⛔⛔ **同表は刻みの間隔を段ごとに 1 つ与え「年と月の段は 1 か月」と書いていた。**
⭐ **CR-345 が 表 T-238 でその段を 2 行に割った瞬間に、1 つの段が 2 通りに刻むようになり、
上の行について述べる文がどこにも無くなった。**
⚠️ **実測（出荷ビルド）: 月の段の年の行は 1 回、月の行は 12 回刻む。**

⇒ ⭐ **列挙をやめた** —— 「**刻むのは段ではなく行であり、その行が刷る単位に 1 つ刻むこと（MUST）。
どの行が何を刷るかは 表 T-238 が持つ**」。
⛔ **次に段が割れても、文を書き足さずに済む。**

### 2.7 表 T-104 の境目を狭め、`K-117` を返す（`D-238`）

⛔ **`CR-348` が書いた境目「`src/` がその英語の名前で引くもの」は広すぎた** ——
**文書の列にも当てはまる。**
⇒ **「`documentSettings` の鍵であるもの」に改め、`K-117`（`cornerRadiusPx`）を退けた。**
⭐ **同行は `HighlightBox` の列であり（`schedule.ts:332`、`AT-122`）、
`document-settings.ts` は持っていない。**
`rows` は 1771 → **1770**、設定の語 117 → **116**。

---

## 3. ⭐ 影響

```
docs/spec/01-04-requirements.md            表 T-233 / FR-087 / FR-091 / 1.9
docs/spec/_source/display-words.json       理由 1 行・設定 1 行を落とし、QN-1 / QN-2 を改めた
docs/spec/_assets/tbl-glossary.md          表 T-104 の境目と K-117
docs/spec/_source/property_items_json_to_md.py  表 T-016 の前文
docs/spec/A-appendix.md                    版 1.80 の数を訂正し、版 1.81 を足した
tools/parity/check.mjs                     他所の状態についての偽の 1 文（台帳 D-245）
```

**機械検査**: `check.sh` 33 本 ／ `gen:check` 17 本 ／ `npm run typecheck`。

**予告した赤**: 表 T-233 の指紋の掛け金 1 件。
⛔ **仕様を動かした当人が直してはならない** —— **仕様だけを読む体が書き直し、48/48 緑。**
⭐ **その体が、前に立つ者の数え違い（536 対 535）を生成器の形から訂正した。**

---

## 4. ⚠️ 利用者に問うこと（**本変更要求では決めていない**）

1. **退けた行の選び方** —— `RS-26` を残し、`CR-347` の行を返した。逆でもよい。
2. **`D-239` の実装** —— **項目名はいま日本語で刷られている**（名称 / 色 / 高さ）。
   裁定どおり英語にすると、**画面の見た目が変わる。**
3. **`FR-032` の通知**（台帳 `D-249`）—— **削除のあと何も告げない。**
   確認が消えるものを既に挙げているので、要求のほうを直す道もある。
