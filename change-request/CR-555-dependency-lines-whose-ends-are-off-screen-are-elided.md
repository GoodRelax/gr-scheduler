# CR-555 — 端点が見えていない依存線を、短い線と「…」で省く

> 起草の状態: 起草（2026-09-24 に 11 節の問い 2 つへ利用者が答えた。4 節はすべて当てられる）。問い 1 は B（`JDG-444`）、問い 2 は A（`JDG-445`）—— どちらも推奨どおりで、4 節の `EL-15` ・ `EL-2` ・ `EL-10` と `RT-4a` の 1 文はその答えの文である。
> 読んだ木: `61bbd572`（`origin/refactor`。worktree の枝 `claude/fervent-keller-e96fcc` を早送りした）。`CR-551` は着地済み（`6f674198` 〜 `7dbd292d`）、`CR-552` 〜 `CR-554` は未着地。行番号・数・参照は、すべてこの木で測った（測り方は 13 節）。
> ⚠️ 起草は `ebc71984`（`CR-551` の前）で始め、途中で `61bbd572` へ早送りされた木で測り直した。4 節の旧 21 塊は、`ebc71984` と `61bbd572` のどちらでも 1 回ずつ現れる（13 節）。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示（`JDG-441` 〜 `JDG-443`。逐語は下の 0.1 節）。
> ⛔ **覆すもの**: 表 T-018a の `RT-4a` の「LOD で間引いた `Task` が端点になる場合」（グループ LOD の端点を「描かない」から「省く」へ）と、`RT-6` の「縦にスクロールしているあいだは経路を算出し直さなくてよい」の範囲（省き方も含める）、`PND-416` の裁定「切られただけの端点は `RT-4a` の「描かれていない」に含めない（線は残し、残りの領域で切る）」（帯の下へ潜った端点は、これからは見えていない端点として省く）。
> ⭐ **形の方針**: 利用者は、使い勝手の試しのあとで裁定がまた動くと述べている。⇒ 場合ごとの描き方・短い線の形・印の形・ダブルクリックの送り方を、新しい表 T-303 の 1 行ずつに分け、寸法は設定値の行（`S-360` 〜 `S-363`）に置いた。裁定を戻すときは、多くが 1 行の書き換えで済む（戻し方は 1 節の最右列）。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が体へ渡した写しのまま写した。`…` は写しの側の省略であり、利用者の字ではない。

| 項目 | 逐語 |
|---|---|
| 1 | 「画面を上下に走る大量の依存線があるとき、見にくい。そもそも依存元、依存先のタスクが画面外にある依存線を表示する意味がない。…依存元と依存先が両方表示されている: 依存線を表示する／依存元のみが表示されている: 依存線の続きを...で省略する。／依存先のみが表示されている: 依存線の続きを...で省略する。／依存元と依存先が両方表示されていない: 依存線を描画しない。」 |
| 2 | 「…線が上に続く場合: 上に折れてすぐ ...／線が下に続く場合: 下に折れてすぐ ...／線が左右に続く場合: 左右に伸びてすぐ ...」 |
| 3 | 「／...をダブルクリックしたらその先のタスク/マイルストーンがあるところまでスクロールしろ。縮小して表示されていなければ拡大して表示しろ」 |

前に立つ者が添えた見立て（利用者の言葉ではない）: `PND-416` と `RT-4a` / `RT-6` を覆す（利用者は 2026-09-23 の仕分けで覆すものをすべて受けた）。人が畳んだ行・隠した行は `RT-4a` のまま。「見えている」は形が `Row Area` に重なること。書き出す絵が省くかは利用者に問う。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2` ／ `GL-002`**（全体を 1 枚で俯瞰する）—— 端が画面の外にある線は、画面を上下に横切るだけで何も読ませない（逐語 1）。省けば、見えているタスクの間の線だけが残る。
⭐ **`CH-4` ／ `GL-006`**（説明を読まずに操作できる）—— 「…」が線の続く向きを示し、ダブルクリックで先の端へ行ける。
⚠️ **`CH-3`（ぬるサク）には負に働きうる** —— 毎フレーム、描く線ごとに両端が見えているかを判じる（8 節の注）。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正）** —— ① `FR-098` の「両端ともピン止めした行にあるときに限り帯の中に描いてよく、片方でもスクロールする行にあるときは残りの領域で切ること（MUST）」（`01-04-requirements.md:4557`）は、ピン止めした端から出る短い線を帯の外で切ってしまう → 短い線は見えている端の行のために描くものと 1 文足した（E-13）。② `FR-009` の「同じ文書・同じ日付なら、パンやズームをしても同じ経路になること（MUST）」（`:2638`）は、省き方がパンで変わることと両立させる必要がある → 変わるのは描き方であり経路ではない、と書いた（E-04）。③ 表 T-023d は日程の形の掴みの順を持たず、表 T-267 ／ 表 T-268 へ預けている（`:3388` の前文、`GR-23`）→ 「…」の掴み代は依頼文の見立ての 表 T-023d ではなく 表 T-266 に置いた（`GA-24`）。③' 表 T-274 は、覆う行の名の無い行を 表 T-266 へ足すことを禁じる（`:419`）→ `PP-1` に `GA-24` を足した（E-12）。④ `FR-081` の「選択は、描かれている対象だけを指すこと（MUST）」（`:2245`）と、両端が見えないので描かない線 —— 描かない理由が見ている範囲だけのとき、スクロールで選択が変わってはならない → `EL-14` で線を描かれている対象に数えた。
- **`R1.4`（異常系・境界値）** —— ① 同じ端から同じ向きへ出る線は、同じ短い線と同じ印になる（大量の後続を持つタスク）→ 応える 1 本を `EL-13` で決めた。② 見えている端の形が `Row Area` に掛かっていても、線の付く辺が外に在れば短い線も印も外に描かれる → 押せない（決定 6 の代償）。③ グループ LOD が描かない行の端は座標を持たない → 描かれている最も近い祖先の行の下端に立てた（`EL-2`）。④ ダブルクリックの送りは取り消しの対象ではない（表 T-027 の `UN-8`）。⑤ 先の端がピン止めした行にあるときは、縦は常に見えている（`EL-12`）。
- **`R1.2`（検証できる表現）** —— 「すぐ」を、折れたあとの長さ `S-360` と横へ伸ばす長さ `S-361` にした。「...」を、径 `S-362` の点 3 つにした。
- **`R2.1`（命名）** —— 新しい接頭辞 `EL`（Elision）。「続きの印」を 表 T-303 の中の語とし、用語集の行は立てない（10 節）。
- **`R5`（パフォーマンス）** —— 見えているかの判定は線ごとに矩形 2 つの重なりであり、`routeOf` の外で済む。⛔ 最適化は進めない（`JDG-11`）。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる。戻し方は 1 節）。`rulings.md` を「依存線」「RT-4a」「RT-6」「PND-416」「ダブルクリック」「省略」で引いた —— 当たるのは `JDG-196`（予定が無ければ依存線を描かない）・ `JDG-03`（担当ラベルのダブルクリックはパネル）だけで、どちらも本書と食い違わない。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 「表示されている」を、端の `Task` の描いた予定の形（マイルストーンは描いた図形）の外接矩形が、その行を描く場所（ピン止めした行は帯、ほかは帯の下の残り）と `Row Area` の横の範囲に、幅も高さも 0 を超えて重なること、と読む（`EL-1`） | 依存線は予定の幾何にだけ付く（`FR-009`、`:2593`）。帯の中にスクロールする行を描いてはならない（`FR-098`、`:4554`）。前に立つ者の見立て「形が `Row Area` に重なること」。`CR-552` の後は幅で落ちる `Task` が無い | 名前の札だけが見えて形が画面の外にある端は、見えていない端になる |
| 決定 2 | 帯の下へ潜った行の端は、見えていない端とする（⛔ `PND-416` を覆す） | 逐語 1「画面外にある依存線を表示する意味がない」。帯の中には描けないので、潜った端は画面の上に無い | ピン止めした行から、帯の下へ潜った行へ向かう線は、短い線と印になる（いまは残りの領域で切って長く描く） |
| 決定 3 | 人が畳んだ行・隠した行の端、予定を持たない `Task` の端、予定を表示していないときは、`RT-4a` のまま描かない（本表に入れない） | 前に立つ者の見立て。`FR-018` は人の畳みと倍率を分けている（`:4484`「倍率ではなく人が決めたことなので、本要求の話ではない」）。畳んだ行を印のダブルクリックで開くと文書の畳みを書き換えることになり、送りと拡大（`UN-8` の対象外）より重い。注記も畳みと隠しで隠れる（`UC-008` 拡張 2a） | 畳んだ先へ向かう線は、いまと同じく消える（11 節の問い 2 の C は採らなかった。`JDG-445`） |
| 決定 4 | グループ LOD（表 T-005a の `L-3`）が描かない行の端は、見えていない端として省く。先の端は、描かれている最も近い祖先の行の帯の下端に立てて経路を引く（`EL-2`）。ダブルクリックは、その行の深さを描く倍率へ縦に拡大する（`EL-10`） | 逐語 3「縮小して表示されていなければ拡大して表示しろ」は、縮めたために描かれていない先の端が在ることを前提にする。`CR-552` の後、縮めて描かれなくなるのはグループ LOD の行だけである。下端は、その行が開いたときに現れる所である | `RT-4a` の「LOD で間引いた `Task`」を外す（E-02）。利用者がこの読み（11 節の問い 2 の A）を採った（`JDG-445`）。戻すときは `EL-2` ・ `EL-10` を消して `RT-4a` に戻す |
| 決定 5 | 線の続く向きは経路から導く —— 見えている端から経路をたどって最初に縦へ進む向き（上 ／ 下）。縦の走りを持たない経路（`RP-1`）だけが横（`EL-7` ／ `EL-8`） | `routeOf`（`schedule-geometry.ts:624-691`）の 8 つの形のうち、縦の走りを持たないのは `RP-1`（同じ段の前進の FS / SF、`:664-666`）だけで、ほかはどれも出口の走りのすぐ後か中点で縦に折れる。後続の側も、入口の走りの前は必ず縦である（`:646` ・ `:657` ・ `:674` ・ `:687`）。⇒ 右上の画面外の後続へ向かう FS は `RP-3` なので上 | 後続が横にだけ画面の外（縦は見えている）でも、段が違えば短い線は上か下へ折れる。ダブルクリックは横に送る（`EL-11`） |
| 決定 6 | 折れる所は、経路の最初の折れ点ではなく、走りの長さ（出口 `S-298` ／ 入口 `S-299`）の所 | 逐語 2「折れてすぐ」。`RP-2` ／ `RP-3` の最初の折れ点は 2 つの辺の中点（`:668`）、`RP-6` ／ `RP-7` は `x1` と `x2` の大きいほう（`:651`）で、先の端が遠いと画面の外へ出て、印が見えなくなる。`RP-4` ／ `RP-5` ／ `RP-8` は走りの長さで折れるので、経路の頭と一致する | `RP-2` ／ `RP-3` ／ `RP-6` ／ `RP-7` では、短い線が経路の頭と違う所で折れる。見えている端の辺そのものが画面の外なら、短い線も印も描かれない（押せない） |
| 決定 7 | 「...」は、線の向きに並べた 3 つの点（描いた図形。字ではない）。色は依存線の色 `S-159`。縦の短い線の先では縦に並ぶ（`EL-9`） | 逐語の `...`。図形にすれば、字の書体に依らずに寸法と掴み代を決められる（`GA-24`）。色の行を足さない —— 線の続きなので線の色である（⇒ `CR-551` の色の欄の作法は当たらない。プロパティパネルに行を出さない） | 縦の短い線の先は `⋮` の形になり、逐語の `...` の字面と違う。いつも横に並べたいなら `EL-9` の 1 文 |
| 決定 8 | 寸法の 4 行は 表 T-206（保存しない）に置く。`S-360` 〜 `S-362` には表示の倍率の描く比を掛け（`DS-3` へ足す）、掴み代 `S-363` には掛けない（`DS-7` へ足す） | 省き方は見ている範囲に依る見せ方であり、日程の内容ではない（`tbl-settings.md` の 1 節の 2 段の判定の第 2 段）。先例は `CR-551` が 表 T-206 に置いた見せ方の行（`S-325` ・ `S-332` ほか）。描く比は依存線の走り（`DS-1` の群「依存線」）と揃え、新しい `DS-` 行を立てずに `DS-3` の名簿へ入れる | 文書ごとに短い線の長さを持てない |
| 決定 9 | 既定値 —— `S-360` 9.6（倍率 100 で 6px）、`S-361` 19.2（12px）、`S-362` 3.2（2px）、`S-363` 4px。どれも 🔎 | `S-360` は出口の走り `S-298` の描いた長さ（6px）と同じ —— 折れる前と後が同じ長さ。`S-361` は `S-298` と `S-360` の和 —— 折れない短い線も同じ道のり。`S-362` は線の太さ（1.5px）より太い 2px —— 点が線の一部に見えない。`S-363` は 2 × 10px の印を 10 × 18px の的にする | 測って決めた値ではない |
| 決定 10 | ダブルクリックの送り方 —— 縦の倍率はグループ LOD の端のときだけ上げる（`EL-10`）。横は、先の端の形が横に外れているときだけ、倍率を変えずに形の中点を `Row Area` の中点へ送る（`EL-11`）。縦は、外れているときか `EL-10` で倍率を変えたときだけ、その行を帯の下の残りの上端へ送る（`EL-12`）。入っている軸は動かさない | 逐語 3「あるところまでスクロールしろ」を、届くだけ送ると読んだ。先例を 3 つ借り、新しい送り方を作らない: 横は基準日線を出す操作（`FR-046`、`CR-551` の E-16、`01-04-requirements.md:3077`）、縦は `Agent API` の `focusTask`（`AM-16`。`agent-api-members.ts:247-259` の `viewThatShowsTask` が `scrollGroupId` ＝ その行、`:687-694` が端数 0 を書く）、倍率は行を足したとき（`row-tree-entrances.ts:330-338` の `setZoom`、`zoomY: groupDepthThresholdOf(depth, settings)`） | 縦に送ると先の端の行が上端に来るので、元の端は画面の外へ出ることがある。先の端の上に在る入口の走りも、縦には気にしない |
| 決定 11 | 重なった印は、先の端が近い依存線が応える（`EL-13`） | 大量の後続を持つタスクから同じ向きへ出る線は、同じ短い線と同じ印になる（`EL-7` は先の端の位置を読まない）。`HT-4` の中心の距離では同点になり、行く先が押すたびに変わりうる（`item-hit-area.ts:780-789` の `beats` は同点を最初に見つけたものに残す） | 2 番目に近い先へは、1 番目へ送ったあとで辿り直す |
| 決定 12 | 本表で描かない線（`EL-6`）も、`FR-081` の選択の規則では描かれている対象に数える（`EL-14`） | `FR-081` が挙げる「描かれていない」は、表示の切り替え・人の畳みと隠し・行の軸の倍率だけで（`:2246`）、画面の外のタスクは選ばれたまま残る。線だけをスクロールで選択から外すと、同じ規則が 2 つに割れる。全選択（`selection-input.ts:74`）は `geometry.dependencies` を読むので、`EL-6` の線もそこに残す（5 節の S-1） | 選んだ線がスクロールで見えなくなっても選択に残り、`Delete` で消える（画面の外のタスクと同じ） |
| 決定 13 | 続きの印は依存線の一部 —— 押して離せば `PE-12` で線を選び、ポインタは指（`PK-7`）、応える順は同じ依存線の線より先（表 T-268 の `TY-2` ／ `TY-5`） | 利用者が印に求めたのはダブルクリックだけ。1 回目の押下は線を押したのと同じにする。小さな的を線より先にするのは、フェードの掴み点を線より先にした理由と同じ（`:3675`） | — |
| 決定 14 | 縦にスクロールしているあいだは、見えているかの判定も引き直さなくてよい（`RT-6` に足す） | `RT-6` が経路について置いた許しと同じ理由（`NFR-002`） | スクロールの途中は、省き方が止まるまで古いまま |
| 決定 15 | `MK-13` には行き先を書き写さず、表 T-303 を指す | `MK-13` の担当ラベルの項が同じ作法（「その宛先をここに書き写してはならない」）。辞書の `MK-13` はヘルプに刷られる（`FR-036`）ので語を足す（J-02） | — |

---

## 1. 範囲 —— 行き先

| 何 | 仕様で変える所 | 編集 | 裁定を戻すとき |
|---|---|---|---|
| 場合ごとの描き方（逐語 1） | `FR-009` の STATEMENT と末の段、表 T-303（新）の `EL-3` 〜 `EL-6` | E-01 ・ E-04 | 各 1 行（例: 片方だけのときも描かない ⇒ `EL-4` ／ `EL-5` を「描かない」に） |
| 見えているとは | 表 T-303 の `EL-1` | E-04 | `EL-1` の 1 行（例: 名前の札も数える） |
| 帯の下へ潜った端（`PND-416`） | 表 T-303 の `EL-1` の注、`FR-098` の 1 文 | E-04 ・ E-13 | `EL-1` の注を消し、`FR-098` の 1 文を消す |
| グループ LOD の端 | `RT-4a`、表 T-303 の `EL-2` ・ `EL-10`、`LC-10` の注 | E-02 ・ E-04 ・ E-17 | `EL-2` ・ `EL-10` を消し、`RT-4a` に「グループ LOD が描かない行」を戻す（2 か所） |
| 人の畳み・隠し | `RT-4a`（変えない側） | E-02 | `RT-4a` の 1 文と `EL-2` の 1 行（そのときは畳みを開く送りが要る） |
| 短い線の形（逐語 2） | 表 T-303 の `EL-7` ・ `EL-8`、`S-360` ・ `S-361`（新） | E-04 ・ J-01 | `EL-7` ／ `EL-8` の 1 行 ／ 各行の値 |
| 続きの印の形 | 表 T-303 の `EL-9`、`S-362`（新） | E-04 ・ J-01 | `EL-9` の 1 行 ／ `S-362` の値 |
| 印の掴み代と順 | 表 T-266 の `GA-24`（新）・ `S-363`（新）・ 表 T-267 の `HT-1` ・ 表 T-268 の `TY-2` ／ `TY-5` と結び ・ 表 T-270 の `PE-12` ・ 表 T-274 の `PP-1` | E-05 〜 E-10 ・ E-12 ・ J-01 | `GA-24` の値 ／ `TY-2` ／ `TY-5` の並び |
| ダブルクリック（逐語 3） | 表 T-023 の `MK-13`、表 T-303 の `EL-10` 〜 `EL-12`、辞書の `MK-13` | E-04 ・ E-11 ・ J-02 | `EL-10` ／ `EL-11` ／ `EL-12` の各 1 行（例: 縦も中央へ ⇒ `EL-12`） |
| 重なった印 | 表 T-303 の `EL-13` | E-04 | `EL-13` の 1 行 |
| 選択 | 表 T-303 の `EL-14` | E-04 | `EL-14` の 1 行 |
| 書き出す絵 | 表 T-303 の `EL-15` | E-04 | `EL-15` の 1 行（11 節の問い 1 の答え B、`JDG-444`。A へ戻すときは 11 節の A の文） |
| スクロール中 | `RT-6` | E-03 | `RT-6` の 1 文 |
| 描く比と掴み代 | 表 T-252 の `DS-3` ・ `DS-7` | E-14 ・ E-15 | 名簿から外す |
| 設計の側 | `05-07-design.md` の 表 T-068 の `LC-10` と注、表 T-064 の `PI-6` | E-16 〜 E-18 | 1 行ずつ |
| 接頭辞 | `_source/row-id-prefixes.json` の `EL` | J-03 | — |

**数**: 仕様の文の編集 18（E-01 〜 E-18。`01-04-requirements.md` 15、`05-07-design.md` 3）、原稿 JSON の編集 3（J-01 〜 J-03）。

⚠️ **`CR-551` は着地した**（`6f674198` 〜 `7dbd292d`）: 本書の旧 21 塊は `CR-551` の旧にも新にも 0 回で、着地した木（`61bbd572`）で 1 回ずつ現れる（13 節）。本書が名指す所のうち `CR-551` が入れたものは、`FR-046` の基準日線を中央へ送る段（`:3077`。`EL-11` が先例として名指す）、表 T-206 の末の `S-325` 〜 `S-341`（J-01 はその後ろに足す）、`row-id-prefixes.json` の `RK`（`EL` は別の位置）である。`display-words.json` の `MK-13` と、`FR-098` の帯の段（E-13）は `CR-551` が触っていない。
⚠️ **同じ日に起草中の兄弟**（`CR-556` 〜 `CR-560`、未コミット。13 節の `overlap.py`）: 本書の旧 21 塊を含むものは 0。`CR-560` の E-02 は 表 T-023 の最後の行 `MK-13` の後ろに `MK-15` を足す（旧は `MK-13` の末の `| — |` と次の段の頭）—— 本書の E-11 の旧（`MK-13` の末の句と `| — |`）と同じ `| — |` を読むが、どちらを先に当てても、もう一方の旧は 1 回だけ残る。`CR-560` の J-04 は辞書の `MK-13` の `press` の後ろに足し、本書の J-02 は `MK-13` の `text` を書く。`CR-556` ・ `CR-558` ・ `CR-559` は 表 T-274 の `PP-6` ／ `PP-7` と 表 T-266 の別の行を書き、`PP-1` と `GA-22` の行を書かない。
⚠️ **`CR-552` と重なる所**: `CR-552` は `RT-4a` を書かない（同書の決定 7 が「LOD で間引いた `Task`」を真のまま残した）。本書の E-02 がその句を書き換える —— `CR-552` の後はグループ LOD だけを指すので、`CR-552` の読みと食い違わない。同じファイル: `01-04-requirements.md`（`CR-552` の E-01 〜 E-09 は表 T-005a ・ `UC-007` ・ `FR-001` ・ `FR-018` ・ `DS-2`）、`settings.json`（`S-49` ・ `S-86`）、`display-words.json`（`K-55`）。⚠️ `CR-552` の E-09 は表 T-252 の `DS-2` の行を消す —— 本書の E-14（`DS-3`）・ E-15（`DS-7`）はその隣の行である。
⚠️ **`CR-553` と重なる所**: 同じファイル `01-04-requirements.md`（`HF-19` ・ `HF-6` ・ `FR-055` ほか）と `05-07-design.md`（表 T-221 の `LF-16`）。本書の塊とは重ならない。`CR-553` の `LF-16`（最後の行の下の余白）は帯の下の残りの縦幅を変えないので、`EL-1` の「帯の下の残り」は同じ場所を指す。
⚠️ **`CR-554` と重なる所**: 仕様は触らない（`05-07-design.md` の 表 T-075 に `UF-144` 〜 `UF-168` を足す）。コードの置き場は 9 節。
⇒ **当てる者は、`CR-552` 〜 `CR-554` が着地した木で、4 節の旧をもう 1 度数えてから当てること。**

---

## 2. 新しい識別子

⭐ 番号は 2026-09-24 に前に立つ者が `b7a3b76f` で詰めた（`CR-555` 〜 `CR-560` を CR の順に、配られた帯の先頭から隙間なく。帯の外の接頭辞は木の最大 M の次から）。下の「空いている」はその木で測った主張である。⚠️ 規則 02 の 2.5 節: **当てる直前に測り直すこと。** 測り方は 13 節。

| 種類 | 採ったもの | 測った最大（`b7a3b76f`、`docs src tests tools change-request`、`CR-555` 〜 `CR-560` の草案を除く） |
|---|---|---|
| 表 | `T-303` | 仕様が使う最大は `T-297`（`CR-551`）。`T-301` 〜 `T-319` の字は退けた `tbl-datamodel.md` と `CR-110` の中にだけ在り、`T-303` は木のどこにも無い |
| 接頭辞 | `EL`（Elision） | `EL-` の行は木のどこにも無い（`docs/development-records` と `docs/development-rules` も） |
| 行 | `EL-1` 〜 `EL-15`（表 T-303） | — |
| 行 | `GA-24`（表 T-266） | `GA` の最大は 23（`CR-431` の文の中の「存在しない行の例」。仕様の行は `GA-22`） |
| 設定値 | `S-360` ・ `S-361` ・ `S-362` ・ `S-363` | 帯 `S-360` 〜 `S-384` の先頭（`S-999` は試験の番兵） |
| 台帳 | `JDG-441` 〜 `JDG-445`、`DFC-860` | 帯 `JDG-441` 〜 `JDG-469` ・ `DFC-860` 〜 `DFC-889` の先頭 |

- 本書が取らないもの: 保留の行（11 節の問いは 2026-09-24 に答えが出た）、表 T-303 のほかの表、`RT-` ・ `TY-` ・ `PE-` ・ `DS-` ・ `MK-` ・ `PR-` ・ `FR-` ・ `IC-` ・ `AM-` ・ `UC-` ・ `V-` ・ `GL-` ・ `CH-` の新しい行（どれも既存の行の中身を書く）。
- 6 本を詰めた後に返った帯: `S-377` 〜 `S-384`、`T-309`（`T-306` ・ `T-307` は古い文書が引くので飛ばした）、`DFC-874` 〜 `DFC-889`、`PND-561` 〜 `PND-580`（6 本とも保留の行を起こさない）、`PR-29` 〜 `PR-32`、`FR-137` 〜 `FR-148`、`IC-109` 〜 `IC-114`、`AM-20` 〜 `AM-24`（6 本とも使わない）。

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 消すもの | 所（`61bbd572`） | 置き換わる先 | 編集 |
|---|---|---|---|
| `RT-4a` の「LOD で間引いた `Task` が端点になる場合」（描かない） | `01-04-requirements.md:2609` | グループ LOD の端は 表 T-303 の `EL-2`（省く） | E-02 |
| `RT-6` の「縦にスクロールしているあいだは経路を算出し直さなくてよい」の範囲（経路だけ） | `:2611` | 経路と、表 T-303 の見えているかの判定 | E-03 |
| `PND-416` の裁定「切られただけの端点は `RT-4a` の「描かれていない」に含めない（線は残し、残りの領域で切る）」 | `docs/development-records/pending-decisions.md:280` | 帯の下へ潜った端は見えていない端（`EL-1`）。`RT-4a` の読みは変わらない（潜った行は描かれている）が、線は 表 T-303 で省く | E-04（⛔ 台帳は当てる者が書く。12 節） |
| `FR-098` の「片方でもスクロールする行にあるときは残りの領域で切ること」の、ピン止めした端から出る短い線への効き | `:4557` | 短い線と印は見えている端の行のもの（1 文を足す。旧の文は残す） | E-13 |
| `LC-10` の「依存線の経路を引く」と注「端点のどちらかが描かれていない依存線は引かない」 | `05-07-design.md:843` ・ `:853` | 経路を引き、見えない端の線を省く ／ グループ LOD の端は祖先の行に立てる | E-16 ・ E-17 |
| 辞書の `MK-13` の語（行き先の列挙） | `_source/display-words.json:3529-3538` | 続きの印の項を足す | J-02 |
| コードの「両端が描かれていれば経路をすべて描く」（⛔ 本書は直さない。9 節） | `schedule-geometry.ts:1250-1262` ・ `schedule-task-figures.ts:541-594` | 表 T-303 | 波 1 の実装する者 |

⭐ **消さないもの**（読み直して真のまま）: `RT-4a` の「予定を持たない `Task`」「予定を表示していないとき」「実績の幾何へ落としてはならない」と `UC-008` 拡張 2a への揃え（畳んだ行・隠した行 —— 決定 3）。`RT-6` の第 1 文（ピン止めした端でも描く）。`FR-098` の `:4557` ・ `:4558` の文（両端が見えている線の切り方）。`FR-108` の「LOD で描かないタスク」（描かれていない `Task` は掴めない —— 掴めるのは描いた印である）。`FR-009` の「同じ経路」の MUST（`:2638`。経路は変えない）。

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-541` ・ `CR-550` ・ `CR-551` ・ `CR-552` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（13 節の道具が 18 件すべてで 1 回を確かめた）。旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である。
⚠️ 行末の 2 つの半角空白（改行の印）も旧と新の一部である。写すときに落とさないこと。
⚠️ E-06 ・ E-11 ・ E-12 ・ E-15 ・ E-18 は行の一部の置き換えであり、塊の末の改行を旧にも新にも含めない。ほかは行の全体（末の改行まで）である。
⚠️ 生成物（`_assets/tbl-settings.md` ・ `_assets/tbl-row-id-prefixes.md` ・ `_source/grs-document.schema.json` と `src/` の生成物）は手で直さない。原稿を直して `npm run gen` を打つ。
⭐ E-04 の表の `EL-15` は 11 節の問い 1 の答え B（`JDG-444`）、`EL-2` ・ `EL-10` と E-02 の `RT-4a` の 1 文と E-17 は問い 2 の答え A（`JDG-445`）の文である。

### 4.1 文の編集（`01-04-requirements.md` ・ `05-07-design.md`）

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
`FR-009` の STATEMENT の 2 行目。旧
```text
経路は表 T-018a に従うこと。
```
新
```text
経路は表 T-018a に従い、端点が見えていない依存線の描き方は表 T-303 に従うこと。
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
表 T-018a の `RT-4a`。旧
```text
| RT-4a | **端点のいずれかが描かれていないときは、その依存線を描かない（MUST NOT）** —— 畳んだ行・非表示の行・LOD で間引いた `Task` が端点になる場合、**予定を持たない `Task`** が端点になる場合、および**予定を表示していないとき**（`_assets/tbl-settings.md` の 表 T-202 の `S-227` が偽）。<br>⛔ 実績の幾何へ落としてはならない（MUST NOT） —— 規則と理由は `FR-009` が持つ。<br>注記が同じ状況で隠れるのと揃える（`UC-008` 拡張 2a） |
```
新
```text
| RT-4a | **端点のいずれかが描かれていないときは、その依存線を描かない（MUST NOT）** —— 人が畳んだ行・非表示の行（表 T-015 の `HR-1a` / `HR-6`）が端点になる場合、**予定を持たない `Task`** が端点になる場合、および**予定を表示していないとき**（`_assets/tbl-settings.md` の 表 T-202 の `S-227` が偽）。<br>⭐ グループ LOD（表 T-005a の `L-3`）が描かない行の端点と、描かれているが見えていない端点は、本行ではなく 表 T-303 に従う —— 本行が落とすのは、人がそう決めた端点と、線を付ける予定が無い端点である。<br>⛔ 実績の幾何へ落としてはならない（MUST NOT） —— 規則と理由は `FR-009` が持つ。<br>注記が同じ状況で隠れるのと揃える（`UC-008` 拡張 2a） |
```

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md -->
表 T-018a の `RT-6`。旧
```text
| RT-6 | 端点の一方がピン止めした行（`FR-098`）にあるときも、依存線を描くこと（MUST） —— ピン止めした行は描かれているので `RT-4a` の対象外である。<br>⚠️ 縦にスクロールしているあいだは経路を算出し直さなくてよい。<br>止まってから引き直す —— ピン止めした行は画面に固定されるので、スクロール中は端点の一方だけが動く。<br>毎フレーム引き直すと `NFR-002` を割る |
```
新
```text
| RT-6 | 端点の一方がピン止めした行（`FR-098`）にあるときも、依存線を描くこと（MUST） —— ピン止めした行は描かれているので `RT-4a` の対象外であり、帯の中で見えているので 表 T-303 の見えている端である。<br>⚠️ 他方の端が見えていないときの描き方は 表 T-303 が持つ。<br>⚠️ 縦にスクロールしているあいだは、経路と、表 T-303 の見えているかどうかの判定を算出し直さなくてよい。<br>止まってから引き直す —— ピン止めした行は画面に固定されるので、スクロール中は端点の一方だけが動く。<br>毎フレーム引き直すと `NFR-002` を割る |
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
`FR-009` の末の段の後ろに、省く規則と 表 T-303 を足す（`**Relations**:` の前）。旧
```text
**同じ文書・同じ日付なら、パンやズームをしても同じ経路になること（MUST）。**  
経路を可視の障害物だけから決めると視界に依存し、**日付を触っていないのに絵が変わる。**
```
新
```text
**同じ文書・同じ日付なら、パンやズームをしても同じ経路になること（MUST）。**  
経路を可視の障害物だけから決めると視界に依存し、**日付を触っていないのに絵が変わる。**  
⚠️ 見えている範囲で変わってよいのは、経路のうちどれを描くかだけである（表 T-303） —— 経路そのものを、端点が見えているかどうかで変えてはならない（MUST NOT）。

⭐ 端点の一方か両方が見えていない依存線は、表 T-303 に従って省いて描くこと（MUST） —— 端が画面の外にある線は画面を横切るだけで、どこからどこへの依存かを読む手がかりにならない（利用者の指示。`docs/development-records/rulings.md` の `JDG-441`）。  
⭐ 省いた線が見せるのは、線がどちらへ続くかと、その先へ行けることだけである —— 先へ行く入口は続きの印のダブルクリック（表 T-023 の `MK-13`）であり、送り方は 表 T-303 の `EL-10` 〜 `EL-12` が持つ。  
⚠️ 省いた短い線は、経路の頭（または尻）と同じ所で折れるとは限らない —— 折れる所を走りの長さに置くので（`EL-7`）、経路が中点で折れる形では経路と違う所で折れる。

**表 T-303 — 端点が見えていない依存線の描き方**

⭐ 本表は、`RT-4a` が描くとした依存線にだけ当てる —— 人が畳んだ行・隠した行の端点、予定を持たない `Task` の端点、予定を表示していないときの線は、本表の前に `RT-4a` が落とす。  
⭐ 続きの印はその依存線の一部である —— 押して離せばその依存線を選び（表 T-270 の `PE-12`）、掴み代は `FR-104` の 表 T-266 の `GA-24`、応える順は `FR-105` の 表 T-268 が持つ。  
値は `_assets/tbl-settings.md` の 表 T-206 の `S-360` 〜 `S-363` が持つ。

| 行 ID | 何について | 規則 |
| --- | --- | --- |
| EL-1 | 見えている端 | 端の `Task` の描いた予定の形（マイルストーンは描いた図形）の外接矩形が、その端の行を描く場所 —— ピン止めした行は帯、ほかの行は帯の下の残り（`FR-098`、`05-07-design.md` の 表 T-221 の `LF-14`）—— と、`Row Area` の横の範囲とに、幅も高さも 0 を超えて重なるとき、その端は見えている。<br>⚠️ 名称ラベル・担当と完了率の札・進捗マーカーは数えない —— 線が付くのは予定の形である（`FR-009`）。<br>⚠️ 帯の下へ潜った行の端は見えていない —— その行を帯の中に描いてはならないからである（`FR-098`） |
| EL-2 | グループ LOD が描かない行の端 | 見えていない端とすること（MUST）。<br>経路を引くために、その端は、描かれている最も近い祖先の行の帯の下端に立つものとし、横の位置は予定の日付と 表 T-018 のアンカーから決めること（MUST）（`FR-017`） —— 行が開けばその端が現れる所である。<br>⚠️ 最も浅い段はグループ LOD の対象ではないので（`FR-018`）、描かれている祖先は必ず在る |
| EL-3 | 先行も後続も見えている | 経路（表 T-018 ／ 表 T-018a）をそのまま描く |
| EL-4 | 先行だけが見えている | 先行の出口から短い線（`EL-7` ／ `EL-8`）を描き、その終わりに続きの印（`EL-9`）を置く。<br>矢じりは描かない —— 線は後続に届いていない |
| EL-5 | 後続だけが見えている | 続きの印（`EL-9`）を置き、そこから短い線（`EL-7` ／ `EL-8`）で後続の入口へ入れ、入口に矢じりを描く（寸法は `_assets/tbl-settings.md` の 表 T-201 の `S-19` ／ `S-300` のまま） |
| EL-6 | どちらも見えていない | 描かない（MUST NOT）。<br>⛔ 経路が `Row Area` を横切っていても描いてはならない（MUST NOT） —— 利用者が「依存元と依存先が両方表示されていない: 依存線を描画しない」と定めた |
| EL-7 | 短い線 —— 経路が縦の走りを持つとき（`05-07-design.md` の 表 T-222 の `RP-2` 〜 `RP-8`） | 見えている端の辺から、経路と同じ向きへ横に走りの長さ（出口は 表 T-201 の `S-298`、入口は同表の `S-299`）だけ進み、その端から経路をたどって最初に縦へ進む向き（上 ／ 下）へ折れ、表 T-206 の `S-360` だけ進んで終えること（MUST） —— 利用者の「上に折れてすぐ」「下に折れてすぐ」である。<br>⭐ 向きは経路が決め、先の端が横にずれていても縦を採る —— 例: 先行だけが見えていて後続が右上の画面外に在る FS は、経路が `RP-3` なので上へ折れる。<br>⚠️ 折れる所は走りの長さの所であり、経路の最初の折れ点ではない —— 経路の折れ点（`RP-2` ／ `RP-3` の中点、`RP-6` ／ `RP-7` の `x1` と `x2` の大きいほう）は、先の端が遠いと画面の外へ出て、続きの印が見えなくなる |
| EL-8 | 短い線 —— 経路が縦の走りを持たないとき（同表の `RP-1`） | 見えている端の辺から、その端から経路をたどる向き（左 ／ 右）へ、横に 表 T-206 の `S-361` だけ伸ばして終えること（MUST） —— 利用者の「左右に伸びてすぐ」である |
| EL-9 | 続きの印 | 3 つの点とすること（MUST）。<br>短い線の終わりから 表 T-206 の `S-362` だけ空け、線の進む向きに、径 `S-362` の点を `S-362` ずつ空けて 3 つ並べる —— 縦の短い線の先では縦に、横の短い線の先では横に並ぶ。<br>色は依存線の色（表 T-236 の `S-159`）とすること（MUST）。<br>⚠️ 選んだ線（表 T-023c の `SL-8`）でも点の径は変えない。<br>⚠️ 地の色の縁（`FR-009`、表 T-206 の `S-224`）は短い線に敷き、点には敷かない |
| EL-10 | 続きの印をダブルクリックしたとき —— 倍率 | 印の先の端が `EL-2` の端のときは、縦の倍率を、その端の行の深さを描く最小の倍率（`FR-018` のしきい値。表 T-205 の `S-87` ／ `S-88`）にすること（MUST）。<br>横の倍率は変えない。<br>⚠️ 取り消しの対象ではない（表 T-027 の `UN-8`） |
| EL-11 | 続きの印をダブルクリックしたとき —— 横 | 印の先の端の予定の形が `Row Area` の横の範囲に入っていないときは、倍率を変えずに、その形の横の中点が `Row Area` の横の中点に来るよう、表示位置を横に送ること（MUST） —— 基準日線を出す操作（`FR-046`）と同じ送り方である。<br>入っているときは横に送らない |
| EL-12 | 続きの印をダブルクリックしたとき —— 縦 | 印の先の端が `EL-1` の縦の範囲（その行を描く場所）に入っていないとき、または `EL-10` で縦の倍率を変えたときは、その端の行が帯の下の残りの上端に来るよう、表示位置を縦に送ること（MUST） —— `_assets/tbl-settings.md` の 表 T-203 の `S-78` をその行に、`S-176` を 0 にする（`Agent API` の `focusTask`、`_assets/tbl-glossary.md` の 表 T-107 の `AM-16` と同じ置き方）。<br>入っているときは縦に送らない。<br>⚠️ ピン止めした行の端は、いつも縦の範囲に入っている |
| EL-13 | 重なった続きの印 | 同じ所に重なった続きの印のうち、印の先の端が近い依存線が応えること（MUST） —— 近さは、見えている端のアンカーと先の端のアンカーの直線の距離とする。<br>距離が同じときは先の端の予定の開始が遅いほう、それも同じときは後に作られた依存線とする（`FR-009` の手前の順の ② と同じ）。<br>⭐ 1 つの端から同じ向きへ出る線は、同じ短い線と同じ印になる（`EL-7` ／ `EL-8` は先の端の位置を読まない） —— 決めておかないと、ダブルクリックで行く先が押すたびに変わる |
| EL-14 | 選択 | 本表で描かない線（`EL-6`）と短く描いた線（`EL-4` ／ `EL-5`）も、`FR-081` の選択の規則では描かれている対象に数える —— 描かないのは見ている範囲の都合であり、スクロールやパンで選択を変えてはならない（MUST NOT）。<br>⚠️ `EL-6` の線は描いたものを持たないので、掴めず（`FR-108`）、範囲選択（表 T-023c の `SL-3`）でも取れない —— 全選択（同表の `SL-5`）は取る |
| EL-15 | 書き出す絵 | 画像の書き出し（`FR-080`）でも本表で描くこと（MUST）。<br>見えているかどうかは、書き出す絵の `Row Area` と帯で判じる —— 書き出す絵は画面の全体を縮めた絵なので、画面と同じ線が同じ形で省かれる。<br>⭐ 続きの印も描く —— 押す相手のいない絵の中でも、線が絵の外へ続くことを示す |
```

<!-- EDIT id=E-05 file=docs/spec/01-04-requirements.md -->
表 T-266 に `GA-24` を足す（`GA-22` の後ろ）。旧
```text
| GA-22 | `--->`（未着手） | ダミーの終了 | 印の右端 | 中央合わせ `S-289` | 実績の矢の帯（中線より下）＋ `S-290` | 箱の矢印 → 黒（`PK-1`） |
```
新
```text
| GA-22 | `--->`（未着手） | ダミーの終了 | 印の右端 | 中央合わせ `S-289` | 実績の矢の帯（中線より下）＋ `S-290` | 箱の矢印 → 黒（`PK-1`） |
| GA-24 | 共通 | 依存線の続きの印 | 描いた印（`FR-009` の 表 T-303 の `EL-9`） | 3 つの点の外接矩形 ＋ 外側 `S-363` | 同左 | 指（`PK-7`） |
```

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md -->
表 T-267 の `HT-1` の末に 1 文を足す。旧
```text
地の色の縁（同表の `S-224`）は含めない |
```
新
```text
地の色の縁（同表の `S-224`）は含めない。<br>⭐ 依存線の続きの印（`FR-009` の 表 T-303 の `EL-9`）も、形の上では描いた点そのものでだけ応えること（MUST） —— 外側の `S-363` を形の上へ届かせない |
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md -->
表 T-268 の `TY-2`（形の外）。旧
```text
| TY-2 | 2 | 進捗マーカー（実績の外に立つ）・再開アイコン（この順） | 依存線 |
```
新
```text
| TY-2 | 2 | 進捗マーカー（実績の外に立つ）・再開アイコン（この順） | 依存線の続きの印・依存線（この順） |
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md -->
表 T-268 の `TY-5`（形の上）。旧
```text
| TY-5 | 5 | 依存線 | 実績の端 |
```
新
```text
| TY-5 | 5 | 依存線の続きの印・依存線（この順） | 実績の端 |
```

<!-- EDIT id=E-09 file=docs/spec/01-04-requirements.md -->
表 T-268 の結びに理由を 1 文足す。旧
```text
⭐ 形の外でもフェードの掴み点を依存線より先にするのは、掴み点が選んだタスクにだけ出る（`FR-075`）小さな 1 か所の的であり、線は長くてほかのどこでも選べるからである。  
```
新
```text
⭐ 形の外でもフェードの掴み点を依存線より先にするのは、掴み点が選んだタスクにだけ出る（`FR-075`）小さな 1 か所の的であり、線は長くてほかのどこでも選べるからである。  
⭐ 依存線の続きの印を同じ依存線の線より先にするのも同じ理由である —— 印は短い線の先の 1 か所であり、線を選ぶだけならほかの所でもできる。  
```

<!-- EDIT id=E-10 file=docs/spec/01-04-requirements.md -->
表 T-270 の `PE-12`。旧
```text
| PE-12 | 依存線 | 選ぶ | 何もしない | — |
```
新
```text
| PE-12 | 依存線（続きの印を含む。<br>印のダブルクリックは 表 T-023 の `MK-13`） | 選ぶ | 何もしない | — |
```

<!-- EDIT id=E-11 file=docs/spec/01-04-requirements.md -->
表 T-023 の `MK-13` の末に、続きの印の項を足す。旧
```text
2 行に分けると同じ操作が表の 2 か所に並ぶ | — |
```
新
```text
2 行に分けると同じ操作が表の 2 か所に並ぶ<br>／依存線の続きの印 ＝ `FR-009` の 表 T-303 の `EL-10` 〜 `EL-12` に従い、印の先の端が見える所まで表示を送ること（MUST） —— ⛔ **その送り方をここに書き写してはならない（MUST NOT）** —— 担当ラベルの項と同じ理由である。<br>⚠️ 1 回目の押下は、依存線を押して離したときと同じく、その依存線を選ぶ（表 T-270 の `PE-12`） | — |
```

<!-- EDIT id=E-12 file=docs/spec/01-04-requirements.md -->
表 T-274 の `PP-1` の「覆う行」に `GA-24` を足す（表 T-274 の結び「覆う行の欄にも上の段にも名の無い行を … 足してはならない」のため）。旧
```text
`GA-22` ／ `HT-1`
```
新
```text
`GA-22` ／ `GA-24` ／ `HT-1`
```

<!-- EDIT id=E-13 file=docs/spec/01-04-requirements.md -->
`FR-098` の帯の切り方の段に 1 文を足す。旧
```text
⛔ **片端が帯に届くことを理由に線を丸ごと通してはならない（MUST NOT）** —— 通すと、スクロールする側の端が帯の中を横切る。  
```
新
```text
⛔ **片端が帯に届くことを理由に線を丸ごと通してはならない（MUST NOT）** —— 通すと、スクロールする側の端が帯の中を横切る。  
⭐ 端点の一方だけが見えているときに描く短い線と続きの印（`FR-009` の 表 T-303 の `EL-4` ／ `EL-5`）は、見えている端の行のために描くものとすること（MUST） —— その端がピン止めした行にあれば帯の中に描き、スクロールする行にあれば残りの領域で切る。  
```

<!-- EDIT id=E-14 file=docs/spec/01-04-requirements.md -->
表 T-252 の `DS-3` に 3 行を足す。旧
```text
| DS-3 | 同書の 表 T-206 の `S-196` | 掛ける | 形状と名称ラベルのあいだの隙間であり、日程表の絵の寸法である |
```
新
```text
| DS-3 | 同書の 表 T-206 の `S-196` ・ `S-360` ・ `S-361` ・ `S-362` | 掛ける | 形状と名称ラベルのあいだの隙間（`S-196`）と、端点が見えていない依存線の短い線と続きの印（`S-360` 〜 `S-362`、`FR-009` の 表 T-303）であり、日程表の絵の寸法である。<br>⭐ 後の 3 つは、依存線の太さと走り（`DS-1` の群「依存線」）と一緒に縮める |
```

<!-- EDIT id=E-15 file=docs/spec/01-04-requirements.md -->
表 T-252 の `DS-7` の掴み代の名簿に `S-363` を足す。旧
```text
`S-137` / `S-180` / `S-230` / `S-291` / `S-292` / `S-293`）
```
新
```text
`S-137` / `S-180` / `S-230` / `S-291` / `S-292` / `S-293` / `S-363`）
```

<!-- EDIT id=E-16 file=docs/spec/05-07-design.md -->
表 T-068 の `LC-10`。旧
```text
| LC-10 | 10 | 依存線の経路を引く | 端点の座標 | 表 T-018a |
```
新
```text
| LC-10 | 10 | 依存線の経路を引き、端点が見えていない線を省く | 端点の座標 ／ `Row Area` と帯（表 T-221 の `LF-14`） | 表 T-018a ／ `01-04-requirements.md` の 表 T-303 |
```

<!-- EDIT id=E-17 file=docs/spec/05-07-design.md -->
表 T-068 の後の注。旧
```text
**`LC-10` が `LC-2` より後なのは `RT-4a` が定めている** —— 端点のどちらかが描かれていない依存線は引かない。
```
新
```text
**`LC-10` が `LC-2` より後なのは `RT-4a` と `01-04-requirements.md` の 表 T-303 が定めている** —— 端点のどちらかが描かれていない依存線は引かず、グループ LOD が描かない行の端点は、描かれている最も近い祖先の行に立てる（同表の `EL-2`）。
```

<!-- EDIT id=E-18 file=docs/spec/05-07-design.md -->
表 T-064 の `PI-6` の `DependencyGeometry` の説明。旧
```text
描いた依存線 1 本 —— 結ぶ 2 つの `Task` と種別、経路（`LC-10`、表 T-222）、描いた太さと矢じり。
```
新
```text
描いた依存線 1 本 —— 結ぶ 2 つの `Task` と種別、経路（`LC-10`、表 T-222）、描いた太さと矢じり、端点が見えていないときに描く短い線と続きの印（`01-04-requirements.md` の 表 T-303）。
```

### 4.2 原稿 JSON の編集（`_source/`）

⛔ 各塊は、当てる前に原稿を `json.loads` で読み、名指した項が 1 つだけ在ることを確かめてから当てること。当てた後は `npm run gen` の前に、手で書いた検証の形（`*.schema.json`）で確かめること。

J-01 `settings.json` —— 表 T-206 の末の行（`61bbd572` では `S-341`。`CR-551` の前の `ebc71984` では `S-297`）の後ろに、次の 4 行をこの順に足す
```json
[
 {
  "id": "S-360",
  "value": {
   "ja": "端点が見えていない依存線の短い線の、折れたあとに縦へ進む長さ（`FR-009` の 表 T-303 の `EL-7`）"
  },
  "default": {
   "num": "9.6",
   "suffix": "px",
   "mark": "🔎"
  },
  "note": {
   "ja": "見せ方の寸法であり、日程の内容ではないので保存しない。⭐ 表示の倍率の描く比を掛ける（表 T-252 の `DS-3`）—— 既定の 9.6 は、表示の倍率 100 で 6px になり、出口の走り（表 T-201 の `S-298`）を描いた長さと同じである。利用者の「上に折れてすぐ」の「すぐ」を、折れる前の走りと同じ長さと読んだ。⛔ 測って決めた値ではない（🔎）"
  }
 },
 {
  "id": "S-361",
  "value": {
   "ja": "端点が見えていない依存線の短い線を、経路が縦の走りを持たないとき（表 T-222 の `RP-1`）に横へ伸ばす長さ（`FR-009` の 表 T-303 の `EL-8`）"
  },
  "default": {
   "num": "19.2",
   "suffix": "px",
   "mark": "🔎"
  },
  "note": {
   "ja": "同上（保存しない。表 T-252 の `DS-3` で描く比を掛ける）。⭐ 既定の 19.2 は、表示の倍率 100 で 12px —— 出口の走り（`S-298`）と `S-360` の和であり、折れる短い線と同じ道のりにした。⚠️ 後続の側では矢じりを含む —— 表 T-201 の `S-19`（矢じりの長さ）を下回らないこと。⛔ 測って決めた値ではない（🔎）"
  }
 },
 {
  "id": "S-362",
  "value": {
   "ja": "依存線の続きの印の、点 1 つの径。点と点のあいだと、短い線の終わりから最初の点までも同じ長さを空ける（`FR-009` の 表 T-303 の `EL-9`）"
  },
  "default": {
   "num": "3.2",
   "suffix": "px",
   "mark": "🔎"
  },
  "note": {
   "ja": "同上（保存しない。表 T-252 の `DS-3` で描く比を掛ける）。⭐ 既定の 3.2 は、表示の倍率 100 で 2px —— 依存線の太さ（表 T-201 の `S-18`、描いて 1.5px）より太くし、点が線の一部に見えないようにした。印は線の向きに本値の 6 倍（手前の隙間 1 ・ 点 3 ・ 間 2）を取る。⛔ 測って決めた値ではない（🔎）"
  }
 },
 {
  "id": "S-363",
  "value": {
   "ja": "表 T-266 の `GA-24`（依存線の続きの印）の掴み代 —— 3 つの点の外接矩形から外へ"
  },
  "default": {
   "num": "4",
   "suffix": "px",
   "mark": "🔎"
  },
  "note": {
   "ja": "掴み代は読む人のアクセシビリティに属するので保存せず、表示の倍率の描く比を掛けない（表 T-252 の `DS-7`）。⭐ 既定の 4 は、表示の倍率 100 の印（2 × 10px）を 10 × 18px の的にする。⚠️ 形の上では外側を届かせない（表 T-267 の `HT-1`）。⛔ 測って決めた値ではない（🔎）"
  }
 }
]
```

J-02 `display-words.json` の `MK-13` —— `text` の 2 つの語だけを変える。旧
```json
   "text": {
    "ja": "対象で意味が決まる。コメントボックス＝プロパティパネルを開いて本文を編集、名称ラベル＝名称、担当ラベル＝プロパティパネルを開いて担当者を編集、タスクと実績＝プロパティパネルを開いて名前を編集、行見出し＝行名",
    "en": "What it does depends on what it lands on -- Comment Box: opens the Properties Panel and edits its body; Name Label: its name; Assignee Label: opens the Properties Panel and edits the assignee; task and actual: opens the Properties Panel and edits its name; row title: the row name"
   },
```
新
```json
   "text": {
    "ja": "対象で意味が決まる。コメントボックス＝プロパティパネルを開いて本文を編集、名称ラベル＝名称、担当ラベル＝プロパティパネルを開いて担当者を編集、タスクと実績＝プロパティパネルを開いて名前を編集、行見出し＝行名、依存線の続きの印（…）＝その先のタスクが見える所まで表示を送る",
    "en": "What it does depends on what it lands on -- Comment Box: opens the Properties Panel and edits its body; Name Label: its name; Assignee Label: opens the Properties Panel and edits the assignee; task and actual: opens the Properties Panel and edits its name; row title: the row name; the dots that end a dependency line: brings the task at its far end into view"
   },
```

J-03 `row-id-prefixes.json` —— 次の項を、字の順の位置（`EG` の後、`EN` の前）に足す
```json
{
 "prefix": "EL",
 "words": "Elision",
 "owner": "spec",
 "means": {
  "ja": "端点が見えていない依存線の描き方"
 }
}
```

### 4.3 生成器と生成物（形で示す）

| 何 | 何をする |
|---|---|
| `npm run gen` | `_assets/tbl-settings.md`（表 T-206 に `S-360` 〜 `S-363`）・ `_assets/tbl-row-id-prefixes.md`（`EL` 15、`GA` 22 → 23）と、`src/` の生成物（`display-words.json` の `MK-13`）を刷る。`S-360` 〜 `S-363` は保存しないので `grs-document.schema.json` と `SETTINGS_DEFAULTS` は変わらない |
| 生成の定数の名簿（`tools/generate_entity_types.py`、検査 30 の `check-generated-constants.py`） | `S-360` 〜 `S-362` を描く側（`schedule-geometry.ts`、`CR-554` の後は `dependency-route.ts`）の `NOT_STORED_*` の表へ、`S-363` を `item-hit-area.ts` の掴み代の表（いま `S-285` が居る表）へ足す —— 波 1a の実装する者（9 節） |

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | 仕様 ↔ 幾何（`schedule-geometry.ts` の `geometryFromLayout`、`DependencyGeometry`） | `DependencyGeometry` は `points`（経路の全体。いまのまま）に加えて `elision: 'EL-3' \| 'EL-4' \| 'EL-5' \| 'EL-6'`、`drawnPoints: Path`（描く折れ線。`EL-3` は `points` と同じ、`EL-6` は空）、`continuation: { dots: readonly Point[]; radius: number; farUid: number } \| null`（`EL-4` ／ `EL-5` のときだけ）を持つ。`head` は `EL-3` ／ `EL-5` のときだけ。⛔ `EL-6` の線も `geometry.dependencies` から落とさない（全選択 `selection-input.ts:74` が読む） |
| S-2 | 幾何 ↔ 見えているか | 見えているかは `EL-1` の矩形の重なり: 端の `TaskPlacement` の `x` 〜 `x + width` と、予定の帯（`planHeight`、線だけの形は `height`）。縦の範囲は、ピン止めした行（`layout.rows` の `isPinned`）なら `regions.rowArea.y` 〜 `layout.scrollAreaY`、ほかは `layout.scrollAreaY` 〜 `rowArea` の下端。横は `rowArea.x` 〜 `rowArea.x + rowArea.width`。幅も高さも 0 を超えて重なること |
| S-3 | 幾何 ↔ グループ LOD の端 | `EL-2`: `layout.placements` に居ない予定を持つ `Task` のうち、その行が人の畳み・隠しの配下に無いもの。立つ所は、`schedule.taskGroups` の親をたどって最初に `layout.rows` に居る行の `y + height`。横は `xFromDay` と `S-49` の床の幅（`shapeWidthOf` と同じ）。⚠️ 人の畳み・隠しの配下かは `drawnGroups`（`schedule-layout.ts:493-515`）と同じ読み |
| S-4 | 幾何 ↔ 描き手（`schedule-task-figures.ts` の `dependencyLinkParts`） | 描き手は `drawnPoints` と `continuation` だけを描き、`points` を描かない。点は `S-159` の円（半径 ＝ `continuation.radius`）、縁（`S-224`）は `drawnPoints` だけに敷く。帯に入れるかは、`EL-3` は今のまま（両端ピン止め）、`EL-4` ／ `EL-5` は見えている端の行がピン止めなら帯の側（`depLinkPartsPinned`） |
| S-5 | 幾何 ↔ 当たり（`item-hit-area.ts`） | `dependencyRegionOf` は `drawnPoints` と `head` を読む（空なら領域なし）。新しい種別 `'dependencyContinuation'`（掴み代 `GA-24` ＝ 点の外接矩形 ＋ `S-363`、形の上では点そのもの）を、3 つの順の名簿（`ON_SHAPE_ORDER` ・ `OFF_SHAPE_ORDER` ・ `ON_MARKER_BOX_ORDER`）で `'dependency'` の直前に置く。当たりの `item` は依存線と同じ `{ kind: 'dependency', predecessorUid, successorUid }`、`grab: 'GA-24'`。同じ所の印どうしは `EL-13` |
| S-6 | 当たり ↔ 入力（`item-grab.ts` の `commandFromGrab`） | `release.clickCount >= 2 && hit.grab === 'GA-24'` のとき、`EL-10`（`setZoom`、`zoomX` はそのまま、`zoomY: groupDepthThresholdOf(depth, settings)`）と `EL-11` ／ `EL-12`（`setScrollPosition`）の命令を返す。1 回目の押下は `PE-12`（線を選ぶ）。`GA-24` を `MK_13_GRAB_ROWS` に入れない（入れると 1 回目の押下が消える、`:92-94`） |
| S-7 | 書き出し（`frame-loop.ts` の `exportScene`） | 変えない —— 書き出しも同じ `geometryFromLayout` を同じ `regions` で呼ぶ（`:2999-3018`）ので、`EL-15` は幾何の側だけで満たされる。⭐ 11 節の問い 1 の答えは B（`JDG-444`）なので、「省かない」の引数は足さない |

---

## 6. グラフ（`61bbd572` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 対象 | 要求 / 参照 | 指している要求 |
|---|---|---|
| `FR-009` | 7 / 21 | `UC-004` ・ `FR-016` ・ `FR-049` ・ `FR-076` ・ `FR-104` ・ `FR-110` ・ `SWS-3` —— どれも経路・種別・当たりの半分・予定に付くことを引き、端点が見えているかを引かない。`SWS-3` の「可視の障害物を見ないので、`FR-009` も同時に満たす」は経路の話で、真のまま |
| 表 T-018a | 2 要求（2 次 6） | `UC-004`（`:694`）・ `FR-009`（`:2520` ・ `:2557` ・ `:2558`）。2 次: `FR-016` ・ `FR-049` ・ `FR-076` ・ `FR-081` ・ `FR-104` ・ `FR-110` |
| `RT-4a` | 1 / 3 | `FR-009`（`:2595` ・ `:2611`）・ `05-07-design.md:853`（E-17） |
| `RT-6` | 0 / 0 | 浮いている |
| `FR-098` | 11 / 24 | `FR-004` ・ `FR-005` ・ `FR-009` ・ `FR-016` ・ `FR-018` ・ `FR-019` ・ `FR-031` ・ `FR-033` ・ `FR-055` ・ `FR-080` ・ `FR-088` —— E-13 は帯の切り方の段にだけ 1 文を足し、ほかの段を書かない |
| 表 T-266 | 9 要求（2 次 31） | `FR-075` ・ `FR-043` ・ `FR-016` ・ `FR-103` ほか —— `GA-24` を足すだけで既存の行を動かさない（表 T-266 の前文「どの値も、ほかの行の掴み代を動かさない」） |
| `GA-19` | 0 / 3 | 表 T-274 の `PP-1`、`tbl-settings.md:327`（`S-285`）・ `:348` |
| 表 T-268 | 4 要求（2 次 9） | `FR-043` ・ `FR-016` ・ `FR-104` ・ `FR-105` —— `TY-2` ／ `TY-5` の中に印を線の前へ入れるだけで、行の順を変えない |
| `TY-2` ・ `TY-5` ・ `PP-1` | 0 / 1 ・ 0 / 1 ・ 0 / 0 | 表 T-274 だけ |
| `HT-1` | 1 / 2 | `FR-016`（`:3413`）・ 表 T-274 |
| `PE-12` | 1 / 2 | `FR-016`（`:3455`）・ 表 T-274 の結び（`:418`、例外の名簿 —— `PE-12` は `FR-049` が理由を持つ。本書は変えない） |
| `MK-13` | 8 / 16 | `FR-008` ・ `FR-016` ・ `FR-036` ・ `FR-040` ・ `FR-042` ・ `FR-085` ・ `FR-091` ・ `FR-097` —— どれも自分の対象の項を引く。`FR-036` はヘルプに辞書の `MK-13` を刷る（J-02） |
| `DS-3` | 0 / 0 | 浮いている |
| `DS-7` | 2 / 8 | `FR-104`（`:3563`）・ `FR-039`（`:6855`）・ `05-07-design.md:544` ・ `:570` |
| 表 T-252 | 7 要求（2 次 32） | `FR-094` ・ `FR-085` ・ `FR-016` ・ `FR-104` ほか —— 名簿に 4 つ足すだけ |
| `L-3` | 1 / 1 | `FR-004`（`:1651`、`HR-1a` の「`L-3` と同じ絵」—— 変えない） |
| `S-285` ・ `S-298` ・ `S-299` ・ `S-159` | 1 / 1 ・ 0 / 0 ・ 0 / 0 ・ 0 / 1 | 読むだけで書かない |

⭐ 導いた条項ごとに、届いた行を `rulings.md` で引いた（規則 02 の 1）: `RT-4a` は `JDG-196`（予定が無ければ線を描かない —— 残す）。`MK-13` は `JDG-03`（担当ラベルはパネル —— 触らない）。`FR-098` ・ `RT-6` ・ `PND-416` ・ `GA-19` ・ `TY-2` ・ `DS-3` は 0 件。`T-252` は `JDG-122` ほか（図形に表示の倍率を掛け、掴み代には掛けない —— 決定 8 はこの向きに従う）。`HT-1` は `JDG-140`（描いた形の上では線そのもの —— E-06 はその読みを印に広げる）。

### 6.2 `induced.py`（⚠️ 測れなかった）

⚠️ `induced.py` は `strictdoc export` の出力（`scratch/spec-check/sd-out/json/index.json`）を要し、依頼文はそれを打つことを禁じている。⇒ **当てる者が、`check.sh` の後に次の 3 群で打つこと**（13 節）。見込み: 閉路は `FR-009` ↔ `RT-4a`（`RT-4a` が `FR-009` を名指し、`FR-009` が表 T-018a を名指す —— 1 つの計画で E-01 〜 E-04 を書いた）と、`FR-104` ↔ `DS-7`（`CR-552` の 6.2 節が測った閉路。本書は `DS-7` の名簿に 1 つ足すだけ）。

| 群 | 種 |
|---|---|
| 1 | `FR-009 T-018a RT-4a RT-6 FR-098 FR-018 L-3 LC-10 FR-081 FR-108` |
| 2 | `FR-104 FR-105 FR-107 T-266 GA-19 T-267 HT-1 T-268 TY-2 TY-5 T-270 PE-12 T-274 PP-1 MK-13` |
| 3 | `FR-039 T-252 DS-3 DS-7 S-285 S-298 S-299 FR-080 T-076 EP-5` |

---

## 7. 数の予測（`61bbd572` で測った。当てた後に同じ数え方で突き合わせる）

| 数 | 前（`61bbd572`） | 後（本書だけ） | 内訳 |
|---|--:|--:|---|
| tables | 187 | 188 | 表 T-303 |
| figures | 27 | 27 | — |
| rows | 2305 | 2321（写しで測った。生成物を刷る前） | `EL-1` 〜 `EL-15` と `GA-24` の 16。`npm run gen` の後は `tbl-settings.md` の 4 行（`S-360` 〜 `S-363`）がさらに足される見込み |
| uids | 162 | 162 | — |
| 接頭辞 | — | ＋1 | `EL` |
| 設定値の行 | — | ＋4 | `S-360` 〜 `S-363`（表 T-206） |
| 辞書の項 | — | 0 | `MK-13` の語だけを変える |

⚠️ `CR-552` ・ `CR-553` の後に当てるときは、その 2 つの予測に本書の差（tables ＋1、rows ＋16）を足す。`ebc71984`（`CR-551` の前）では 186 → 187 ／ 2282 → 2298 だった。

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ **当てる順**: `CR-551`（着地済み、`7dbd292d`）→ `CR-552` → `CR-553` → `CR-554`（段 7.5 の分割）→ 本書。本書のコードは `CR-554` が割ったあとの置き場に書く（9 節の「`CR-554` の後」の欄）。

```
wave 0  spec (ONE body, one worktree cut after CR-554 landed)
          recount section 4's 21 old blocks (count == 1 each), then E-01..E-18, J-01..J-03,
          then npm run gen && npm run gen:check
          -> tests that quote the spec verbatim go red here, by design (section 9)
wave 1  (parallel, disjoint files; cut from wave 0's commit; the seams of section 5 go verbatim
         into every brief)
  1a entity/layout-engine   schedule-geometry.ts (geometryFromLayout: EL-1, EL-2, S-1..S-3),
                            dependency-route.ts (after CR-554: the short line of EL-7 / EL-8,
                            the dots of EL-9), item-hit-area.ts (S-5, GA-24, EL-13),
                            tools/generate_entity_types.py (S-360..S-363 into the NOT_STORED tables)
  1b adapter/svg-renderer   schedule-task-figures.ts (dependencyLinkParts: S-4)
  1c adapter/input-command-translator
                            item-grab.ts (commandFromGrab: S-6, EL-10..EL-12)
wave 2  tests by a spec-only tester (never a wave-1 body), reading docs/spec only, on the MERGED tree
wave 3  check.sh + vitest + e2e on the merged tree; npm run build (dist/) for the user to try by file://
```

- ⚠️ 1a ・ 1b ・ 1c は `DependencyGeometry` の形（S-1）を共有する —— 1a が型を先に置き、1b ・ 1c はその commit から切ること。
- ⚠️ 検査 39（MUST の条文の網羅）の数が動く見込み —— 新しい MUST ／ MUST NOT が E-02 ・ E-04 ・ E-06 ・ E-11 ・ E-13 に立つ。⛔ **基準線を動かすときは、利用者に先に問うこと**（記録 `baseline-moves-need-user-ok`）。
- ⚠️ 検査 57（決定表）: 表 T-303 は条件の列を持たない（`何について` の 1 列）ので、検査の名簿には入れない。`EL-3` 〜 `EL-6` は 2 つの端の見え方の 4 組をちょうど 1 行ずつ覆う。
- ⚠️ 検査 60（関数の大きさ）: `geometryFromLayout`（`HELD` 62/9）と `dependencyLinkParts` が伸びる —— 判定と短い線は別の関数に置くこと。
- ⚠️ 性能: 線ごとに毎フレーム矩形 2 つの重なりを判じる。`RT-6` の許しで縦のスクロール中は引き直さなくてよい。⛔ 性能の試し（`RISK-001` の門）を走らせる前に、前に立つ者が利用者に声をかける（記録 `perf-test-notify`）。⛔ 最適化は進めない（`JDG-11`）。
- ⚠️ `dist/` は利用者が `file://` で試す。着地の報告には枝・commit・sha を添えること。見せ所は「縦に長い文書を縦に送り、画面の外へ出たタスクへの線が短い線と `…` になり、`…` のダブルクリックで先のタスクへ飛ぶ」こと。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

| 所（`61bbd572`） | `CR-554` の後 | 何をする |
|---|---|---|
| `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts:122-132`（`DependencyGeometry`） | 同じファイル（`UF-6`、入口） | S-1 の 3 つの欄を足す |
| 同 `:1250-1262`（`geometryFromLayout` の依存線の段） | 同じファイル（入口） | `EL-1` ・ `EL-2` の判定（S-2 ・ S-3）。`EL-6` の線も残す |
| 同 `:739-775`（`routedDependency`）・ `:624-691`（`routeOf`、変えない）・ `:941-946`（`plannedPlacementsOf`） | `dependency-route.ts`（`UF-145`） | 経路から「最初に縦へ進む向き」を返し、`EL-7` ／ `EL-8` の短い線と `EL-9` の点を作る。`S-360` 〜 `S-362` に `displayRatioOf(settings)` を掛ける（`S-196` と同じ掛け方、`schedule-geometry.ts:891`） |
| `src/adapter/svg-renderer/schedule-task-figures.ts:510-595`（`dependencyLinkParts`） | 割らない | S-4。矢じり（`marker-end`）は `head` があるときだけ。画面の外の線の間引き（`:559-572`）は `drawnPoints` で測る |
| `src/entity/layout-engine/item-hit-area/item-hit-area.ts:321-351`（種別と 3 つの順）・ `:692-716`（`dependencyRegionOf`）・ `:733-736`（`claimingRegions`）・ `:1024-1041`（`itemsInMarquee`） | 同じファイル（`UF-7`）／ `marquee.ts`（`UF-151`） | S-5。範囲選択は `drawnPoints` の外接矩形で囲まれるかを見る（`EL-6` は取らない、`EL-14`） |
| `src/adapter/input-command-translator/item-grab.ts:59-96`（`commandFromGrab`） | 割らない | S-6。`EL-11` の中点へ送る計算は、`CR-551` の基準日線を出す送り（`e122fd55`）と同じ関数を使えるか実装する者が測る |
| `tools/generate_entity_types.py` の生成の定数の名簿 | — | 4.3 節 |
| `src/framework/single-html-shell/frame-loop.ts:2986-3035`（`exportScene`） | 同じファイル（入口） | 変えない（S-7。問い 1 の答え B、`JDG-444`） |
| `src/adapter/agent-api-endpoint/agent-api-members.ts:247-259` ・ `:661-694` ・ `src/adapter/input-command-translator/row-tree-entrances.ts:330-338` | 割らない | 変えない（先例として読むだけ） |
| `05-07-design.md` の 表 T-075 の `UF-145`（`CR-554` が足す `dependency-route.ts` の責務の文） | — | `CR-554` の着地後、責務の文に「端点が見えていない線を省く（表 T-303）」を足すかを、表 T-075 の検査で決める（本書の 4 節には入れない —— 旧の文がまだ木に無い） |

**試験**（いま引いているもの。仕様だけを読む試験の体が書き直す）:

| 試験 | いま主張していること | 本書の後 |
|---|---|---|
| `tests/unit/cr-430-ga-1-to-22-grab-areas.test.ts` | 表 T-266 が 22 行と 41 の値を持つ（`:102`） | ⛔ 赤。23 行（`GA-24`）と値 `S-363` を足す |
| `tests/unit/cr-430-ty-1-to-9-type-order.test.ts` | `TY-2` の形の外 ・ `TY-5` の形の上が「依存線」（`:36` の `DEPENDENCY`） | ⛔ 赤。「依存線の続きの印・依存線（この順）」に |
| `tests/unit/cr-430-table-pe-press-and-drag-effects.test.ts:395-` | `PE-12` の各欄 | 掴んだものの欄を読んでいれば赤。E-10 の語に |
| `tests/unit/cr-394-the-chart-has-a-display-scale.test.ts:322-345` | 表 T-252 の行の並びと「掛ける」の欄 | 緑のまま（`DS-3` の行は残り、欄は「掛ける」）。`DS-3` の中身を読む所があれば E-14 に |
| `tests/unit/cr-399-on-a-drawn-bar-a-line-takes-only-its-ink.test.ts:56` | `HT-1` の「依存線の描いた線は本線と矢じりであり …」の逐語 | 緑のまま（E-06 は末に足すだけ） |
| `tests/contract/cr-441-t-274-principles-and-rows-both-ways.contract.test.ts` | 表 T-274 と 9 つの表の行が両向きに覆い合う | E-05 と E-12 を同じ波で当てれば緑。片方だけなら赤 |
| `tests/unit/fr-098-nothing-scrolling-under-the-band.test.ts:79-83` ・ `:968-1010` | 帯の下で切れた行の端を「穴」として使わない（`PND-416` の場合）。片端がピン止めのとき線を残りの領域で切る | 両端が見えている場合は緑のまま。⭐ 新しく要る: 帯の下へ潜った端の線が、ピン止めした端から短い線と印になり帯の中に描かれる（E-13、`EL-1`） |
| `tests/unit/gd-6-the-arrowhead-does-not-depend-on-pinning.test.ts:113` ・ `:384` | 片端がピン止めでも線と矢じりを描く（`RT-6`） | 他方の端が見えていれば緑。見えていない配置を使っていれば `EL-4`（矢じりなし）に |
| `tests/unit/layout-engine.test.ts:1453-1459` | `zoomX` 0.05 で `dependencies` が 0 本（名は「either end is not on screen」、中身はタスク LOD の落とし） | `CR-552` で既に赤。本書の後は `EL-6` の線が `dependencies` に残る（S-1）—— 期待を `elision` で書き直す |
| `tests/unit/cr-430-fr-009-dependency-lines-hang-on-the-plan.test.ts:53-60` | `RT-4a` が「予定を持たない `Task`」「予定を表示していないとき」「実績の幾何へ落としてはならない（MUST NOT）」を含む | 緑のまま（E-02 は 3 つとも残す） |
| `tests/system/nfr-004-file-scheme-sweep.sws.test.ts:270-290` | 描いた依存線の本数と押せる所 | 画面の外へ出る線が短くなるので、数えた本数が動きうる。走らせて測る |
| `tests/unit/t-023d-double-click-only-rows.test.ts` | `commandFromGrab` の `MK-13` の 2 つの枝と `GA-9` | 緑のまま。⭐ 新しく要る: `GA-24` の 2 回目の押下が送りの命令を返し、1 回目が線を選ぶ |
| `tests/unit/uf-27-28-29.test.ts`（`focusTask`） | `AM-16` の送り | 変えない |
| 注だけが引く試験 | — | `tests/unit/fr-097-comment-box-drawn.test.ts:63` ・ `:925`（旧 `RT-4a` の「LOD で間引いた」の逐語の注）・ `tests/unit/fr-009-ptd-3-the-two-halves-of-a-bar.test.ts:377` |
| 新しく要るもの | — | `EL-3` 〜 `EL-6` の 4 組、`EL-7` の 8 つの経路の形ごとの折れる向き（右上の画面外の FS は上）、`EL-8`（`RP-1` は横）、`EL-9` の点の数・径・間（表示の倍率 100 で 2px）、`EL-2`（グループ LOD の行の端は祖先の行の下端から）、`EL-10` 〜 `EL-12`（ダブルクリックの倍率と送り、入っている軸は動かない）、`EL-13`（重なった印は近い先）、`EL-14`（スクロールで選択が変わらない）、`EL-15`（書き出す絵も省く）。⚠️ 描く絵の試験は Playwright の見本で（記録 `browser-pane-no-raf`） |

---

## 10. ⛔ この変更でやらないこと

- **人が畳んだ行・隠した行へ向かう線**（決定 3）—— `RT-4a` のまま描かない。畳みを印から開く送りも作らない（11 節の問い 2 の C）。
- **「続きの印」を用語集（`_assets/tbl-glossary.md`）の行にすること** —— 表 T-303 の中の語に留める。UI パーツ（`U-`）も立てない —— 依存線（`U-16`）の一部である。
- **印の色の行、プロパティパネルの行** —— 色は依存線の `S-159` を使う（決定 7）。`PR-` の行は取らない。
- **ヘルプの図（`FR-036`）の描き直し** —— 辞書の `MK-13` の語（J-02）だけを変える。
- **注記（コメントボックスの線先）の省き方** —— 本書は依存線だけ。
- **表 T-241 の `IX-1` ・ `IX-4` の語**（`DFC-860` の候補。12 節）—— 書き出す絵の縦の範囲の食い違いは本書の前から在る。
- **性能の最適化**（`JDG-11`）、**遅延診断**。
- `docs/development-records/` の `pending-decisions.md` ・ `changelog.md`、`A-appendix.md` の変更履歴（当てる者が書く）。

---

## 11. 前に立つ者へ返す問い

⭐ 次の 2 つを利用者に問い、2026-09-24 にどちらも推奨どおりの答えを得た（逐語は答えの欄。前に立つ者が写した `<scratchpad>/answers-2026-09-24.md` から 1 文字も変えずに写した）。4 節はその答えの文で書いてある。

| # | 問い | 案と比べ | 推奨と理由 | 答え |
|---|---|---|---|---|
| 1 | **画像の書き出し（SVG ／ PNG ／ クリップボード。`FR-080`、コードは `frame-loop.ts:2986-3035` の `exportScene` → `image-exporter.ts:167-205` の `exportSvg`）でも、依存線を「…」で省くか**。場面: 縦に長い文書の途中を画面に出して PNG に書き出し、会議の資料に貼る。画面の外のタスクへ向かう線をどう描くか | **A 書き出しは線をすべて描く**（書き出しは文書であって窓ではない、と読む）: 画面の外へ向かう線は、絵の縁まで伸びて切れる。いまの絵と同じ。`geometryFromLayout` に「省かない」の引数が 1 つ要り（5 節の S-7）、`EL-15` を「書き出しでは本表を当てず、`EL-3` で描く」に替える。 **B 書き出しも画面と同じく省く**（本書の形）: 画面の外のタスクへ向かう線は、短い線と `…` になる。`…` は押せないが、絵の外へ続くことを示す。コードは何も足さない | **B**。① 書き出す絵は「`GRS` が占める画面の全体を … 縮めた絵」であり、「LOD による増減の結果を、書き出しでも同じにすること」（`FR-080`、`01-04-requirements.md:5477-5480`）—— 見えているものが成果物になる（WYSIWYG、最優先事項 5）。コードも画面と同じ `regions` と表示位置で描き（`frame-loop.ts:2999-3018`）、絵の高さは画面の高さ × 比である（`image-exporter.ts:169-176`）。⇒ **書き出す絵は窓であり、依頼文の「書き出しは文書」は仕様と食い違う**。② A を採ると、絵の縁で切れた長い線が残る —— 利用者が「表示する意味がない」と言った絵そのものである。③ 名前の打ち切り（`FR-085`）も画面と同じ形で書き出す（表 T-076 の `EP-3`「書き出し専用の幅を設けてはならない」）—— `…` は同じ働きをする。⚠️ 表 T-241 の `IX-4` は「行が 1000 あれば 28,000px に達し」と画面より下の行まで書き出す読みを示す（12 節の `DFC-860`）。その読みを採る日が来ても、`EL-15` は「書き出す絵の `Row Area`」で判じるので、1 行も書き換えずに済む | **「Q1. 画像の書き出しでも「…」で省くか → 推奨どおり B」**（2026-09-24。`JDG-444`）—— B |
| 2 | **逐語 3「縮小して表示されていなければ拡大して表示しろ」の「縮小して表示されていない」タスクとは何か**。場面: 縦に縮めて、深い段の行がグループ LOD（表 T-005a の `L-3`）で描かれなくなった画面。浅い段のタスクから、描かれていない深い段のタスクへの依存線がある | **A 縮めたせいで描かれていない行（グループ LOD）のタスクも「…」で省き、ダブルクリックでその段が描かれる縦の倍率まで拡大する**（本書の形。`EL-2` ・ `EL-10`）。`RT-4a` の「LOD で間引いた」を外す。 **B グループ LOD の行のタスクへの線は、いまと同じく描かない**（`RT-4a` のまま）: 「拡大して表示」は起きなくなる（`CR-552` の後、幅で落ちるタスクは無い）。`EL-2` ・ `EL-10` を消し、`RT-4a` に「グループ LOD が描かない行」を戻し、E-17 の後半を消す。 **C A に加えて、人が畳んだ行・隠した行のタスクへの線も省き、ダブルクリックで畳みを開く**: 畳みは文書に保存される値なので、ダブルクリックが文書を書き換え、取り消しの段が 1 つ増える（送りと拡大は取り消しの対象外、`UN-8`） | **A**。① 逐語 3 は、縮めたために描かれていない先のタスクが在ることを前提にしており、`CR-552` の後その前提を満たすのはグループ LOD の行だけである。B では逐語 3 の後半が空になる。② 倍率はすでに行を足したときに同じ上げ方をしている（`row-tree-entrances.ts:330-338` の `setZoom`、`groupDepthThresholdOf`）—— 新しい送り方を作らない。③ C は、人が畳んだ行（`FR-018` の「倍率ではなく人が決めたこと」、`:4484`）を、線の印の押下で開くことになり、送りより重い。前に立つ者の見立ても「畳んだ行・隠した行は `RT-4a` のまま」 | **「Q2. 「縮小して表示されていなければ拡大して表示しろ → 推奨どおり A」**（2026-09-24。`JDG-445`）—— A |

---

## 12. 台帳

⭐ 貼る行は `<scratchpad>/CR-555-ledger.md` に置いた（前に立つ者が `rulings.md` ・ `defects.md` に足す）。

### 12.1 裁定（`docs/development-records/rulings.md`）

- `JDG-441` 〜 `JDG-443` —— 0.1 節の逐語 3 項。`JDG-444` ・ `JDG-445` —— 11 節の問い 1 ・ 2 への答え。状態はどれも「指示 —— 本書が当てる」。
- 覆す行: `PND-416`（`pending-decisions.md:280`）の状態「裁定済」を「覆された —— `JDG-441`（`CR-555` の `EL-1`）」へ。`JDG-196` は覆さない。

### 12.2 欠陥（`docs/development-records/defects.md`）

- `DFC-860` —— 表 T-241 の `IX-1`「行が多いときに高さだけを伸ばす」と `IX-4`「行が 1000 あれば 28,000px に達し」は、画面より下の行も書き出す読みを示す。`FR-080`「`GRS` が占める画面の全体を … 縮めた絵」と `IX-10`「行を足して埋めてはならない —— 画面に無いものが出る」、コード（`image-exporter.ts:169-176`、絵の高さ ＝ max(`S-81` の高さ, 画面の高さ × 比)）は、画面の中だけを書き出す。仕様の中の食い違い（どちらが正かは選ばない。利用者の裁定 2026-09-13）。
- 欠陥の行は `DFC-860` の 1 つだけ（番号は 2026-09-24 に前に立つ者が `b7a3b76f` で詰めた。帯の残りは返した）。

---

## 13. 測り方の再現

```
# totals (before), 2026-09-24
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   61bbd572 -> tables=187  figures=27  rows=2305  uids=162
#   ebc71984 -> tables=186  figures=27  rows=2282  uids=162   (before CR-551 landed)

# free identifiers, in both trees, and in the untracked sibling drafts CR-556..CR-563
git grep -nE "\bS-36[0-3]\b|\bEL-[0-9]+|\bT-303\b|\bGA-24\b|\bJDG-44[1-5]\b|\bDFC-860\b" <tree> -- . ':!previous-project-result' ':!change-request/CR-555-*'
grep -lE "\bS-36[0-3]\b|\bEL-[0-9]|\bT-303\b|\bGA-24\b|\bJDG-44[1-5]\b|\bDFC-860\b" change-request/CR-55[6-9]*.md change-request/CR-56*.md
#   -> nothing, in ebc71984, in 61bbd572 and in the drafts; again nothing on b7a3b76f after the front session compacted the IDs (2026-09-24)
git grep -ohE "\bGA-[0-9]+\b" -- docs src tests tools change-request | sort -u | sort -t- -k2n | tail -1
#   -> GA-23 (change-request/CR-431:183, an example of a row that does not exist); the table ends at GA-22
git grep -ohE "\bS-[0-9]{3}\b" -- docs/spec | sort -u | tail -1                 # 61bbd572 -> S-341
grep -ohE "\bT-3[0-9]{2}\b|\bS-3[4-9][0-9]\b" change-request/CR-55[6-9]*.md change-request/CR-56[0-3]*.md | sort -u
#   -> S-341 S-354 S-359 S-363 S-364..S-377 S-384 S-385..S-399 T-301 T-304..T-319 (b7a3b76f, after the compaction;
#      S-363 is only cited by CR-556; S-377, S-384, T-306, T-307, T-309 appear only in the returned-band notes)

# every file:line of this CR, re-measured on 61bbd572 by anchor string
#   scratchpad cr555/anchors.py <repo>        (97 anchors; each printed with its current line)

# the route shapes of decision 5 and the precedents of decision 10 (61bbd572)
#   schedule-geometry.ts:624-691 (routeOf), :739-775 (routedDependency), :1250-1262
#   agent-api-members.ts:247-259, :661-694 ; row-tree-entrances.ts:319-354 ; frame-loop.ts:2986-3035
#   image-exporter.ts:167-205 ; svg-renderer.ts:377-389 ; schedule-task-figures.ts:510-595

# graph (every ID of 6.1), 61bbd572
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>
# induced.py was NOT run: it needs scratch/spec-check/sd-out/json/index.json (strictdoc export),
# which this drafting body was told not to produce. Run after check.sh:
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py <group of 6.2>

# rulings reached (6.1): grep docs/development-records/rulings.md for each row reached,
# and for 依存線 / RT-4a / RT-6 / PND-416 / ダブルクリック / 省略
#   -> JDG-196, JDG-03 (and JDG-122.., JDG-140 for T-252 / HT-1); nothing on RT-6 / PND-416 / 省略

# tests that name a touched row or function (section 9), 61bbd572
git grep -lE -- "<ID or name>" tests

# sibling drafts that hold one of this file's old blocks, or name the same rows
#   scratchpad cr555/overlap.py <repo>
#   -> old blocks inside: none, for CR-550..CR-563; CR-560 names MK-13 (see section 1)

# every old block of section 4 occurs exactly once in its file, in both trees; all 18 text
# edits apply in order to a COPY of docs/spec, which md-checks then counts; the JSON edits
# apply to copies and load. E-06, E-11, E-12, E-15 and E-18 are counted without the block's
# final newline (they replace part of a line).
#   scratchpad cr555/verify.py <repo> <scratch> ebc71984
#   edits parsed: 18 E-01 .. E-18
#   WORKTREE (61bbd572): problems 0, applied 18 of 18
#   ebc71984: problems 0, applied 18 of 18
#   md-checks on a copy (before): tables=187  figures=27  rows=2305  uids=162
#   md-checks on a copy (after):  tables=188  figures=27  rows=2321  uids=162
#   settings.json: rows added 4, duplicates 0, S-360..S-363 present True, schema errors 0
#   display-words.json: J-02 old occurs 1; json.loads ok after J-02
#   row-id-prefixes.json: EL absent before; after EG comes EN; inserted as EG, EL, EN; duplicates 0
```
