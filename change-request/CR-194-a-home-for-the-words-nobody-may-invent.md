# CR-194 — 画面に刷る語に置き場を作る（⛔ **語は 1 つも書かない**）

> **閉じるもの**: 裁定 D4（`docs/spec/_source/` に言語別辞書を新設。`ScreenView` は表示言語を運ぶ）。
> **利用者の裁定**（2026-08-21、`previous-project-result/temp/rulings-2026-08-21.md` §7 の案 (a)）:
> **原稿の枠と生成器と `src/` へ運ぶ `.json` を作る。⛔ 語そのものは書かず、空のまま暫定の印を付ける。**
> ⛔ **本 CR は語を 1 つも発明しない。** 根拠は 表 T-109 が自ら「英名の欄を持たない —— 持つと 68 個の
> 確定名を新たに作ることになる」と拒んでいることであり、**エージェントが語を発明した瞬間に同じことが起きる。**
> ⚠️ **`src/framework/dom-screen-surface/dom-screen-surface.ts` には触れていない** —— 同じ回に別のエージェントが
> 書いているためである（規則 05 の 3.「同じファイルを 2 体で書かない」）。

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`（説明を読まずに操作できるようにする）を進める。受ける目標は `GL-006`。**

⛔ **`FR-038` は「メニューとパネルの文字をその言語で示すこと」を MUST で求めるのに、翻訳された文字列を持つ表が
1 つも無かった。** そのため `ScreenRenderer` の 5 ユニットが**語の在るべき場所に空文字を置いている**（`PD-3` / `PD-4`）。
⭐ **アイコンだけで用途を伝える設計（`FR-029`）は、`EZ-2` の説明が出ることを前提にしている** ——
**説明が空文字であるかぎり `CH-4` は成り立たない。**

⭐ **`GL-006` の 3 つ目の MUST も直接進む** —— 「言語の状態は 1 つとし、画面全体に効くこと（MUST）。
ヘルプだけを別の言語にできてはならない（MUST NOT）」。⛔ **これを宣言している値が `ScreenView` に 1 つも無く、
機械が確かめようのない MUST だった。**

⚠️ **正直に分けると、生成器・検査 21 の行・`npm run gen` の配線は `CH-` にも `GL-` にも当たらない** ——
**仕様を真実に保つための基盤であり、②・⑧ の領分である。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1a`**（矛盾する要求が存在しないか）| `FR-038` の 4 つの MUST と `src/` の実物 | ⛔ **満たされていない MUST が 2 本出た。** ① 「押す前に現在どちらの言語かが読めること（MUST）」—— **切替の入口は 2 か所（`IC-21` は `App Header` とヘルプ）なのに、現在の言語を運ぶ値はヘルプ側にしか無い**（`HelpModal.language`）。**ヘッダーの入口は答えられない。** ② 「言語の状態は 1 つ（MUST）」—— **`ScreenView` に宣言が無い** |
| **`R4`**（DRY）| `ScreenSession.language` と `ScreenView.language` | ⭐ **重複ではない。** 前者は殻が持つ**現在値**（`LY-5`。入力）、後者は**その 1 枚がどの言語で書かれているか**（出力）である。⚠️ **`HelpModal.language` のほうは、`ScreenView.language` が立った時点で重複になる** —— §7 に書いた（本 CR では消せない）|
| **`R2.9`**（YAGNI）| 辞書に何を載せるか | ⭐ **仕様が MUST で刷れと言っているものだけを載せた。** ⛔ **`FR-036` のヘルプの中身（表 T-023a / T-023b / T-023c / T-023d / T-036 の 62 行）は載せていない** —— **その面自体がまだ組み立てられていない**（`open-modals.ts` が自ら記録している）。⛔ **`FR-074` と `FR-088` の面の見出しも載せていない** —— **表 T-103 が名を与えていないので、鍵になる名が無い** |
| **`R2`**（Naming）| 辞書の鍵 | ⭐ **鍵は 1 つも発明していない。** 行 ID（`IC-n` / `NT-n` / `MK-n`）・確定名（`Help Modal` …）・**群はその群の最初の行 ID**（`置く` なら `IC-23`）である。⚠️ **群には仕様が ID を与えていない**ので、⛔ `GRP-1` のような番号を発明せず、**表が持っている座席番号で指した** |
| **`R3`**（エラー処理）| 生成器 | ✅ **黙って続行しない** —— 原稿の名簿が仕様の名簿と 1 行でも食い違えば、**1 バイトも書かずに終了コード 1** |
| **`R6`**（試験品質）| 受け入れ条件 | ⭐ **「原稿の語を 1 つ埋めると試験が落ちるか」を合否にした**（規則 04 の 2.）。⚠️ **落ちなければ、その語はどこにも届いていない** |
| **`R2.15`**（注記が「なぜ」を言うか）| 5 ユニットの `STOP` 注記 | ⛔ **本 CR が 6 か所を偽にする** —— 「翻訳された文字列を持つ表は無い」は本 CR のあとで偽である。**偽になった注記を残さない**（CR-191 / CR-193 と同じ扱い）|

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | **辞書は語を持たないときだけ、いまの代役を刷る**（`空なら代役`）| ⭐ **これが「振る舞いを 1 つも変えない」ことの担保である。** ⛔ 逆にすると、群の名（`置く` など）と `FR-072` の見出しが**いま出ているのに消える** —— 語を足す作業が、いったん退行を挟むことになる |
| 2 | 辞書の生成物は `src/adapter/screen-renderer/display-words.json` の **1 本**（`.ts` を作らない）| ⛔ **検査 18 が `src/` と 表 T-075 の 71 ユニットを突き合わせる。** ⭐ 前例は `icon-roster.json` と `startup-template.json` |
| 3 | 原稿自身のスキーマ（`display-words.schema.json`）は**置かない** | ⭐ **生成器が仕様の名簿と突き合わせるほうが強い** —— スキーマは形しか見ないが、生成器は**鍵の全数が表と一致するか**を見る。⚠️ `jsonschema` が入っていない環境では検証が丸ごと飛ぶ（`settings_json_to_md.py` の `NOTE`）ことも理由である |
| 4 | 群の鍵を**その群の最初の行 ID** にした | ⚠️ **行が並べ替わると鍵が変わる。** ⛔ それでも番号の発明よりましだと判断した —— **生成器が毎回計算し直すので、ずれれば黙らずに落ちる** |
| 5 | 表 T-075 の `UF-60` の欄に「表示言語を運ぶ」を足した | ⭐ **CR-193 が `UF-67` に対してした形をそのまま当てた** —— **`ScreenView` のメンバが 1 つ増える根拠は、その表の欄そのものである。** ⛔ **新しいユニットは作らない**（検査 18）|
| 6 | `open-modals.ts` の `NO_WORDS` に `@provisional PD-4` の印を足した | ⛔ **同じ暫定値が印無しで置かれていた** —— `PD-4` の行は本文で「同じ穴に `open-modals.ts` が同じ答えを置いている」と述べているのに、**印が無いので検査 25 から見えなかった** |

---

## 1. 測った（⛔ **2 本とも走らせた**）

```
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py FR-038
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-038 T-103 T-109 T-037 T-016
```

| | 実測 |
|---|---|
| `FR-038` が指している先 | **表 T-016 ただ 1 つ**（行は 0）|
| `FR-038` を指している箇所 | **要求 1 件 ／ 参照 7 箇所** —— `FR-029`、1.7 制限事項、5.3、5.5、A.3、用語辞書 §8、`tbl-settings.md` §8 |
| 誘導部分グラフ（種 5 個）| ⭐ **種 5/5 がすべて解決。** 辺 **1**・**閉路 0** |
| 結論 | ⭐ **閉路が無いので 1 か所ずつ順に書いてよい。** ⚠️ **「空のグラフの閉路 0」ではない** —— 種が 5/5 とも解決していることを確かめた |
| 表の高さ（`specindex`）| `T-109` **68** ／ `T-103` **55** ／ `T-037` **8** ／ `T-023` **14** ／ `T-016` **18** ／ `T-075` **71** |

⭐ **`FR-038` は表を 1 つしか指していない**（`T-016`）**が、それは「訳さないもの」を指す矢印である。**
⛔ **訳すもの側の指し先が 1 本も無いことが、この欠落そのものである。**

---

## 2. 何が足りなかったか

### #1 —— 語の置き場が無い（`PD-3` / `PD-4`）

`FR-038`（`01-04-requirements.md:3748`）:

```
利用者が表示言語を選んだとき、GRS は、メニューとパネルの文字をその言語で示すこと。
対象は ja と en の 2 言語とする。
```

⛔ **どの表も翻訳された文字列を 1 つも持たない。** 実物が置いているもの:

```
app-header-items.ts   CommandItem.label   = ''        @provisional PD-4
command-palette.ts    CommandItem.label   = ''        @provisional PD-4
                      PaletteGroup.name   = 表 T-109 の 群 の欄（日本語のまま）
open-modals.ts        OpenModal.heading   = ''        ⛔ 印が無かった
                      CommandItem.label   = ''
properties-panel.ts   PropertiesPanel.heading = 'selection' / 'documentSettings' / 'selection (none)'
tooltips.ts           Tooltip.text        = 表 T-023 の行 ID（`MK-1` / `MK-5`）@provisional PD-3
```

### #2 —— 表示言語を宣言している値が `ScreenView` に無い

`FR-038` の第 4 段落（`:3756`）:

```
言語の状態は 1 つとし、画面全体に効くこと（MUST）。
ヘルプだけを別の言語にできてはならない（MUST NOT）
```

⛔ **`ScreenView` の 10 メンバに言語が無い。** 在るのは `HelpModal.language` **だけ**であり、
**それは「ヘルプの言語」である** —— ⚠️ **画面全体の言語を宣言している値が無いので、
「ヘルプだけが別の言語になっていない」ことを機械が確かめる相手がいない。**

⛔ **同時に、`FR-038` の第 2 段落の MUST も満たされていなかった:**

```
切替の入口は、画面の上部とヘルプの中の 2 か所に置くこと（MUST）。
押す前に現在どちらの言語かが読めること（MUST）
```

⭐ **ヘルプ側は `HelpModal.language` で答えられるが、`App Header` 側（`IC-21`）は答えられない** ——
`AppHeaderItems` に言語のメンバが無く、`CommandItem` の 3 メンバはどれも運べない
（`isPressed` は「入っているトグル」であり、2 言語の選択に「切」は無い）。

### #3 —— 表 T-016 の項目名は**対象外である**（⭐ **確かめた**）

⛔ **辞書に入れない。** 決めている文は 2 つあり、どちらも逐語で在る:

```
FR-038  :3748  タスク名と行名、および表 T-016 の項目名は翻訳の対象ではない
               （項目名を英語に保つ理由は同表が持つ）
表 T-016 :1396  項目名は英語表記とすること（MUST）
```

⚠️ **表 T-016 の「日本語」の欄は用語辞書からの抜粋であって画面の文字ではない**（同表の前書き）。

### #4 —— 文書自身の値も**対象外である**

```
FR-038  :3748  タスク名と行名 …… は翻訳の対象ではない
FR-038  :3752  日程表の出力に言語は含まれない
```

⭐ **文書名（`AppHeaderItems.documentTitle`）・行の名（`RowTitle.label`）・担当者名
（`RosterResource.name`）・確認が挙げる名前（`ConfirmationItem.name`）は、すべて文書自身の値である。**
⛔ **辞書に 1 つも入れない。**

---

## 3. 何を辞書に載せるか（**名簿は仕様から機械が起こす**）

⭐ **原稿は語を持ち、名簿は持たない。** ⛔ **鍵の全数は生成器が毎回 `docs/spec` から起こし、原稿と突き合わせる**
—— **表が動けば生成器が落ちるので、名簿が黙って古くなる道が無い。**

| 節 | 何の語か | 名簿の出どころ | 数 |
|---|---|---|---|
| `icons` | 入口の語（`CommandItem.label`）と、`EZ-2` が出す説明（`Tooltip.text`）| 表 T-109 の全行 | **68 × 2 = 136** |
| `paletteGroups` | 群の名（`PaletteGroup.name`）| 表 T-109 の 群 の欄のうち、面が `Command Palette` の行に現れるもの | **5** |
| `surfaces` | 面の見出し（`OpenModal.heading`）| 表 T-109 の `IC-52` の 面 の欄 | **4** |
| `notices` | その知らせがどの作法かを語で言う欄 | 表 T-037 の全行 | **8** |
| `confirmation` | `NT-7` が選ばせる 2 つの答え | `NT-7` の MUST | **2** |
| `panelHeadings` | プロパティパネルの見出し 3 つ | `FR-072` の MUST | **3** |
| `assignments` | 割当を語で言う欄（`FR-037` のヒント本文）| 表 T-023 の全行 | **14** |

**合計 172 の欄、言語ごとに 2 つで 344 の空文字。**

### ⭐ なぜ `notices` に「作法の語」が要るのか

⛔ **`NT-1` が「色や枠だけで示してはならない（MUST NOT）」と定め、`NT-5` が「`NT-1` と見分けがつく形にすること（MUST）」
と定めている。** ⭐ **この 2 つを同時に満たす手は、作法を語で言うことしか残らない。**
⚠️ **`PD-157` が同じことを別の面から記録している** —— 件数に添える語が無いので数だけを描いている。

### ⛔ 載せなかったもの（**理由つき**）

```
表 T-016 の項目名        FR-038 と 表 T-016 が英語に保つと定めている（§2 の #3）
文書自身の値            FR-038 が翻訳の対象外と定めている（§2 の #4）
FR-036 のヘルプの中身    表 T-023a / T-023b / T-023c / T-023d / T-036 の 62 行。
                        ⛔ その面がまだ組み立てられていない（open-modals.ts が自ら記録）。
                        ⭐ 名簿の起こし方は本 CR の `assignments` と同じ型紙で足せる
FR-074 / FR-088 の見出し ⛔ 表 T-103 が名を与えていないので、鍵になる名が無い。
                        鍵を作れば、用語集が確定させていない名を確定させることになる
アイコンの図形          図 F-019 が正である（FR-029）。語で書き取らない
```

---

## 4. 編集計画（⛔ **オブジェクトごとに 1 回。同じ行を 2 度書かない**）

### 仕様書

| # | オブジェクト | 何を書くか |
|---|---|---|
| 1 | `01-04-requirements.md` `FR-038` の RATIONALE | **語の置き場を 1 文で指す**（`_source/display-words.json`）。⛔ 語は書かない |
| 2 | `05-07-design.md` Chapter 6.2 | **画面に刷る語の原稿を 1 段落で定める** —— 置き場・言語ごとの辞書・生成物の在り処。⚠️ **`GRS JSON` のスキーマを起こす原稿は 2 つのままである**ことを明記する |
| 3 | `05-07-design.md` 表 T-075 の `UF-60` の行 | 「9 ファイルを束ねて公開する」→ **「…公開し、画面全体に効く表示言語を運ぶ（`FR-038`）」** |
| 4 | `A-appendix.md` | 変更履歴に 1 行（版 **0.71**）|

### 原稿・生成器・配線

| # | ファイル | 何をするか |
|---|---|---|
| 5 | `docs/spec/_source/display-words.json`（**新規**）| 原稿。172 の欄をすべて `{"ja": "", "en": ""}` で置く |
| 6 | `tools/generate_display_words.py`（**新規**）| 名簿を `docs/spec` から起こし、原稿と突き合わせ、`src/` へ書く。⛔ 食い違えば 1 バイトも書かない |
| 7 | `package.json` | `gen:words` / `gen:words:check` を `gen` / `gen:check` に足す |
| 8 | `.claude/skills/spec-graph-check/check-provenance.py` | 検査 21 の名簿に生成物 1 本を足す（11 → **12**）|

### `src/`（⛔ **CR-146 により、署名と型の正は `src/` である。仕様書に書かない**）

| # | ユニット | ファイル | 何をするか |
|---|---|---|---|
| 9 | `UF-60` | `screen-renderer.ts` | `ScreenView` に `language` を足し（**10 → 11 メンバ**）、`screenViewFromRegions` が `session.language` から埋める。3 つの注記が語の置き場を指すようにする |
| 10 | `UF-62` | `app-header-items.ts` | 入口の語を辞書から引く（空なら今の代役）|
| 11 | `UF-65` | `command-palette.ts` | 入口の語と群の名を辞書から引く（空なら今の代役）|
| 12 | `UF-66` | `open-modals.ts` | 面の見出しと入口の語を辞書から引く。`@provisional PD-4` の印を足し、`NT-7` について偽になった注記を直す |
| 13 | `UF-64` | `properties-panel.ts` | 見出し 3 つを辞書から引く（空なら今の 3 つの字句）|
| 14 | `UF-69` | `tooltips.ts` | `EZ-2` の説明と、スクロールバーのヒント本文を辞書から引く（空なら今の代役）|

⛔ **`src/framework/dom-screen-surface/dom-screen-surface.ts` は触らない**（別のエージェントが同じ回に書いている）。

### 記録

| # | オブジェクト | 何を書くか |
|---|---|---|
| 15 | `docs/development-records/pending-decisions.md` | `PD-3` / `PD-4` の文言を**新しい真実**に書き換え、**`PD-160` を足す**（語そのものが未記入であること）|
| 16 | `docs/development-records/W4-adapter.md` | 記録表に段ごとに追記 |

### 数の予測

| | 前 | 予測 |
|---|---|---|
| `tables` | 126 | **126** —— 表は 1 つも増えない |
| `figures` | 11 | **11** |
| `rows` | 1562 | **1562** —— ⛔ **行は 1 つも増えない**（散文とセルの書き換えだけ）|
| `uids` | 145 | **145** —— 要求を 1 本も足さない |
| 表 T-075 の高さ | 71 | **71** |
| 表 T-109 の高さ | 68 | **68** |
| 重複検出 | `A=26 (new 0)` | **`A=26 (new 0)`** |
| 検査 21 の生成物 | 11 | **12** |
| 検査 23 の原稿 | 3 | **4** |
| 裁定待ちの行 | 51（未裁定 50）| **52（未裁定 51）** |
| `npm test` | 2014 / 2016 | **2014 / 2016** —— ⛔ **振る舞いを 1 つも変えない**（辞書が空なので代役がそのまま出る）|

⛔ **⚠️ `ScreenView` が 11 メンバになるので、`uf-60.test.ts` が抱える写しだけは動く**（§6）。

---

## 5. 受け入れ条件（⭐ **規則 04 の 2.**）

```
1. 全部の検査が緑で、npm test が 2014 / 2016 のまま（振る舞いが動いていない証明）
2. 原稿の語を 1 つだけ埋めて npm run gen を走らせると、試験が落ちる
   ⛔ 落ちなければ、その語はどこにも届いていない
3. 原稿の名簿を 1 行壊すと、生成器が 1 バイトも書かずに落ちる
4. 控えから戻すと 1. に戻る（⚠️ ls -la で大きさを見てから戻す）
```

（実測は §8 に書く。）

---

## 6. 試験（**期待値を曲げない**）

⛔ **製品コードに合わせて期待値を書き換えたのではない。** ⭐ **原稿と表が動いたので、試験が抱えている写しを追随させる**（Chapter 1.9）。

| ファイル | 何が偽になったか |
|---|---|
| `tests/unit/uf-60.test.ts` | ⛔ **`T_075_PARTS` の写しが「`ScreenView` のメンバは 表 T-075 の 9 ユニットのぶんで全部である」と主張していた**（「A member beyond the copy above would be a part no unit of that table owns」）。⭐ **表 T-075 の `UF-60` の欄が「…表示言語を運ぶ」になったので、その主張が偽になる** —— **`UF-60` 自身がメンバを 1 つ持つ。** ⚠️ **CR-193 が `UF-67` に対してした追随とまったく同じ形である** |

⚠️ **他の試験は 1 件も動かない見込みである** —— 辞書が空なので、どのユニットの答えも 1 文字も変わらない。
**動いたら、それは振る舞いが変わった証拠なので調べる。**

---

## 7. ⛔ 本 CR が閉じていないもの

| | |
|---|---|
| **語そのもの** | ⭐ **利用者が埋める**（裁定 2026-08-21）。`PD-160` として記録し、生成器に印を置いた |
| ⛔ **`HelpModal.language` が重複になる** | `ScreenView.language` が立った時点で、同じ値が 2 か所に在る。⚠️ **消すと `dom-screen-surface.ts`（`:745`）が型検査で落ちる**が、**そのファイルは同じ回に別のエージェントが書いている**（規則 05 の 3.）。⭐ **次に `UF-71` を触る回に、`modal.language` を `view.language` へ移して消すこと** |
| `FR-036` のヘルプの中身 | 62 行。⭐ **同じ型紙で足せる**が、面そのものが組み立てられていない |
| `FR-074` / `FR-088` の面の見出し | ⛔ **表 T-103 が名を与えていないので鍵が無い。** 名の裁定である |

---

## 8. 実測（**予測と突き合わせた**）

（適用後に書く。）
