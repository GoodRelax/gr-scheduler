# CR-264 — 選ぶかわりに縮めていた全体表示

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-26）** ——
> 「Fit するときは、Fit して入るように LOD の階層を決める。`L3` まで縦に入らなければ `L2`、`L2` でも入らなければ `L1`、`L1` で入らなければ縦スクロール。」
> 「バーを伸ばすのは縦幅を増やすという意味か？ であれば無用に縦幅を増やすな。画面下部に隙間があって良い。」
> 「梯子の一番上は、その文書が持つ最深から。」
> ⭐ **台帳の行は `D-01`**（`D-22` `D-24` も同じ直しで閉じる）。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`（1 画面で全体を読める）。受ける目標は `GL-002`。**
⛔ **起動直後に 100 行の文書が 5 行しか出ず、しかも縦の拡大ボタンがどの値でも効かない。**
⚠️ **`GL-002` を果たすための唯一の操作が、いちばん果たしていなかった。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 縦を合わせる手続き | ⛔ **縦を「倍率を縮めて合わせる」と読める書き方しか無く、「表示量を選んで合わせる」とはどこにも書いていない** |
| **`R2.19`**（驚かせない）| 縮め続けること | ⛔ **`FR-094` の床（`actualMin` ÷ `actualOfPlan`）より下では帯の高さが縮まない。**下げても縦は 1px も縮まず、`FR-018` のしきい値だけを割って行を消す |
| **`R2.20`**（届く）| 縦の拡大ボタン | ⛔ **実測: `zoomMin` 〜 `zoomMax`（0.02〜64）の全域で、起動文書の行数が変わらない** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| 着地する倍率 | ⭐ **「伸ばさない」から導ける** —— その深さを描ける最小の倍率であり、選ぶ余地が無い |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **深さ 1 に着地するときの倍率は、深さ 2 のしきい値の 1 刻み下**（`S-53` で割る）| ⛔ **仕様は「深さ 1 を LOD の対象にしてはならない」と定めるので、深さ 1 を選ぶ倍率の下限が無い。**⭐ **1 刻み下なら、拡大ボタン 1 回で深さ 2 へ届く。**⚠️ 分類 `C`（`layoutEngine` 1 コンポーネント内・公開 API 不動）|
| 2 | ⭐ **1 回目は、帯の高さが床に達する倍率で測る** | ⛔ **その倍率より下では絵が倍率に依らないので、1 回測れば深さ 1〜3 の縦幅が算術で出る。**⚠️ 分類 `C` |
| 3 | ⛔ **`FR-018` は 1 文字も動かさない** | ⭐ **動かす必要が無い** —— Fit が「深さを選び、それを描ける最小の倍率に着地する」なら、`FR-018` の「倍率 → 深さ」はそのまま成り立つ。**Fit はそれを逆に解いているだけである。**⚠️ **`FR-018` と `FR-055` は 2 循環をなす**ので、両方を触るなら 1 つの計画で書く必要があった（規則 02）。**片方で済んだ** |
| 4 | ⛔ **横は本変更の対象外とする** | ⚠️ **横は縮めて合わせてよい** —— 横に床は無く、`FR-017` の段階は横の px/day で決まる。⭐ **縦だけが「床があるのに割り算していた」** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 帯の高さが縮まなくなる倍率 | ⭐ **`zoomY` = 0.7828**（`actualMin` ÷ `actualOfPlan` ÷ `basePlanHeight` ＝ 16 ÷ 0.73 ÷ 28）|
| 深さ 2 / 3 のしきい値 | **0.32 / 0.60**。⛔ **どちらも 0.7828 より下** ⇒ **LOD が効く全域で、絵は倍率に依らない** |
| 各深さの縦幅（着地する倍率で） | `L1` **752px** ／ `L2` 2774 ／ `L3` 6708 ／ `L4` 13216 ／ `L5` 27607 |
| Row Area | 1280×720 で **599px** ／ 1920×1080 で 959 ／ 2560×1440 で 1319 |
| ⇒ 起動文書の答え | ⭐ **1080p 以上なら `L1` が収まる**（隙間 207px）。**720p では `L1` でも収まらず縦スクロール**（1.26 画面）|
| 縦の拡大ボタンの効き | ⛔ **`zoomMin`〜`zoomMax` の全域で行数が変わらない**（16 値で実測、全部 7 行）|
| 割り算の不動点 | ⛔ **無い。** 1 → 0.0223 → 0.0174 → … → 0.0039（8 回目）。⚠️ **`zoomMin` を割る** |
| 素朴な 2 回目 | ⛔ **振動する。** 1.0 → 0.4291 → 0.7322 → 0.3785 → … 永久に往復。⭐ **「3 回目を走らせてはならない」はこれを防いでいた** |
| `impact.py`（`FR-055`）| 要求 9 件 / 参照 24 箇所。塊「占有幅と段」に触れる |
| `induced.py`（`FR-055` `FR-018` `T-068` `S-87` `S-88`）| ⛔ **2 循環が 1 つ**（`FR-018` ↔ `FR-055`）。⭐ **本変更は `FR-055` 側だけを触るので、循環に入る対象は 1 つである** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-055` の RATIONALE | 縦は**表示量（グループ LOD の深さ）を選んで**合わせること（MUST）。収まる最も深い段を採る。深さ 1 でも収まらなければ深さ 1 とし、縦にスクロールを残す。⛔ **選んだ段を描ける最小の倍率より下へ下げてはならない（MUST NOT）** |
| 2 | `05-07-design.md` の 表 T-068 の後の 2 回の表 | 1 回目の測り方を「帯の高さが床に達する倍率で、その文書が持つすべての深さを通して」に改める。2 回目は「選んだ段が床より上の倍率を要するときだけ」 |
| 3 | `A-appendix` | 変更履歴に 1 行（`1.16`）|

⛔ **`FR-018` は 1 文字も動かさない。** ⛔ **`S-87` / `S-88` の値も動かさない。**
⭐ **行の追加は無い。セルと散文の編集だけである。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1700 | **1700** | 0 |
| `uids` | 148 | **148** | 0 |
