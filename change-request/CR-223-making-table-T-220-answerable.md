# CR-223 — 表 T-220 が実際に答えられるようにする

> **閉じるもの**: ⛔ **`scheduleViolations` を書いたら、17 行のうち 3 行が「表には在るが誰も答えない」ことが分かった。** `IV-1` / `IV-2` / `IV-16` は判定に要るものが原稿から生成されていない。
> ⛔ **さらに 表 T-220 は `TaskGroup.parentId` の輪を止める行を持たない** —— `IV-4` は `wbsParentUid` だけ、`HM-4` は画面の移動だけ、`FR-023` は取り込み時の WBS だけである。
> ⭐ **利用者の裁定（2026-08-23、裁定 22 / 23 / 24）**: **関係に列名を足す ／ ε をやめて境界を「開いている」と綴る ／ `IV-18` を足す。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`。受ける目標は `GL-004`。** ⛔ **壊れた文書を受け取っても気づけない経路が 4 つ残っている** —— `CH-1` が約束する「構造化した日程データを出す」は、構造が壊れていないことを言えて初めて成り立つ。

⚠️ **`IV-18` は特に急ぐ** —— **分類 F**（受理するか拒むかの境界）であり、**緩く受理した文書は後から厳しくしても回収できない。利用者の文書がまだ 1 つも存在しないいまが最も安い。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（網羅性）| 表 T-220 の 17 行 | ⛔ **3 行が判定に要るものを持たず、1 つの輪が誰の網にも掛からない** |
| **`R1.2`**（曖昧さの排除）| `1 − ε` / `1 + ε` | ⛔ **ε に値が無い**（仕様書と原稿に 14 回現れ、1 度も定義されない）。⭐ **言いたいのは「1 を含まない」であって特定の数ではない** |
| **`R1.3`**（唯一の正がある）| `_source/erd.json` の `relations` | ⛔ **外部キーの列名が散文の `label` の中にあり、書いてある行と無い行がある。** 機械が読めない |
| **`R3.2`**（境界）| `IV-18` | ⛔ **輪になった行は深さが決まらず、`S-125` の上限も越えず、誰にも報告されない** |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`relations` の新しい欄を `fromColumn` / `toColumn` にした** | ⚠️ **`parent` / `child` が既に在り、向きの語彙がそちらに寄っている。** `from` / `to` は「参照を持つ側」→「指される側」で、`child` → `parent` と一致する |
| 2 | ⭐ **`exclusive` を境界の側に付ける真偽にした** | ⛔ **`max: 0.999` のような値を書かない。** ⭐ **`max: 1` ＋ `exclusive: true` なら、原稿が「1 未満」と言っていることが読んで分かる** |
| 3 | ⭐ **`IV-18` の種別を「構造」にした** | `IV-4`（`wbsParentUid` の輪）が既に「構造」である。**同じ種類の条件は同じ種別に置く** |
| 4 | ⛔ **`IV-1` のための生成器の変更は本 CR に含めない** | ⚠️ **列の `key`（`PK` / `FK`）は `erd.json` に既に在り、`generate_entity_types.py` が出していないだけである。** ⭐ **原稿は 1 文字も動かないので、仕様の変更ではない** |

---

## 1. 測った

```
induced.py  T-057 T-220 IV-1 IV-2 IV-16 IV-4 HM-4 FR-023 （ほか 31 種と同時に）
impact.py   T-220
```

| 測ったこと | 実測 |
|---|---|
| 表 T-220 の行数 | **17 行**（`IV-1` 〜 `IV-17`。⚠️ `IV-17` は `IV-7` と `IV-8` の間に並ぶ）|
| 表 T-220 を指している要求 | **1 件** |
| `erd.json` の `relations` | **22 本** |
| ε を使う境界 | **6 行**（`max: 1 − ε` が 4、`min: 1 + ε` が 2）|
| 他の設定値を名指す境界 | **約 30 本**（`` `fontMin` `` `` `zoomMax` `` `` `thinStrokeMax` `` ほか）|
| 閉路 | ⭐ **0** |

⛔ **`IV-18` が要る理由（3 つの網を逐語で確かめた）:**

```
IV-4     「`Task.wbsParentUid` がたどる親子に輪が無いこと」 ⇒ wbsParentUid だけ
HM-4     禁じるのは画面の上での移動だけ
FR-023   見るのは取り込み時の WBS の輪だけ
⇒ TaskGroup.parentId の輪は、どの網にも掛からない
```

## 2. 編集計画

⛔ **オブジェクトごとに 1 回。**

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-220 | `IV-18` を 1 行足す ——「`TaskGroup.parentId` がたどる親子に輪が無いこと」／ 判定に要るもの「`TaskGroup` の主キーと `parentId`」／ 種別「構造」|
| 2 | 表 T-220 の `IV-2` | 判定に要るものの欄はそのまま（表 T-057 を指しており、正しい）。⛔ **触らない** |
| 3 | `_source/erd.json` の `relations` | 22 本すべてに `fromColumn` / `toColumn` を足す。⚠️ **入れ子の位置が表す関係は `fromColumn` を `null` とし、理由を書く** |
| 4 | `_source/settings.json` | ε を使う 6 行を `exclusive` へ書き換える |
| 5 | `_source/erd.json` のスキーマ注記 ／ 生成器 | ⚠️ **`npm run gen` を走らせ、生成物を作り直す**（表 T-057 の刊行・`grs-document.schema.json`・`tbl-settings.md`）|
| 6 | A-appendix | 変更履歴に 1 行 |

⚠️ **`_source/` は原稿である。生成物（`tbl-settings.md` ／ `fig-erd-*.md` ／ スキーマ）を手で直さない** —— 検査 16 と 21 が落とす。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **132** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1613 | **1614** | **＋1**（表 T-220 に `IV-18`）|
| `uids` | 148 | **148** | 0 |

⚠️ **`relations` と `settings.json` の変更は生成物の中身を動かすが、行を足さない** —— 表 T-057 の刊行は既に 22 本を並べており、欄が増えるだけである。⛔ **もし `rows` が動いたら、欄の追加を行の追加と数え違えている。**
