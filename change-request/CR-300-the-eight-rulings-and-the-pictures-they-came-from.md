# CR-300 — 利用者の 8 裁定と、それを引き出した絵

> **閉じるもの**: 台帳 D-125 ／ D-112 ／ D-109 ／ D-111 ／ D-110（半分）／ D-94 の 6 件。
> **仕様を触らずに閉じたもの**: 台帳 D-137（**今のままとする**という裁定。実測で「不具合ではない」ことを先に示した）。
> **本 CR に含めないもの**: 台帳 D-134（裁定は下りたが、**実装を先に測る**。第 5 節）／ 台帳 D-129（利用者がペンディングとした）。
> ⭐ **8 件のうち 6 件が、実物に描いて撮った絵を見せた結果として下りた。**⛔ **言葉だけで問うた最初の 1 巡では、5 件が「よく分からない」で返ってきた。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

| 裁定 | 進めるもの | なぜ |
|---|---|---|
| 起動時の左端（`OP-10`）| **`CH-2` / `GL-002`** | 起動して最初に見える 1 枚が「全体日程」であることが、ペライチの入口である。注記 1 つで 15 か月ぶん空白から始まる絵は、その入口を潰す |
| 実績の開始点（`GR-5`）| **`CH-1`** | 「作図ソフトと同じ操作感」は、**掴んだ端だけが動く**ことである。両端が動くのは作図ソフトの挙動ではない |
| 選択の不変条件（`SL-9`）| **`CH-4` / `GL-006`** | 利用者が実際に見たのは「受け付けられない操作が含まれていた」だけで、**何をどう直せばよいか画面から読めない**。説明を読まずに使えることの反対である |
| 矩形とパンの追従（`SL-3` / `PD-1`）| **`CH-3`（ぬるサク）／ `CH-4`** | 利用者の逐語（CR-260 の裁定）が「**飛び飛びだと UI 上気持ち悪くて『ぬるサク』を達成できない**」である。握っているあいだ黙る絵は、その逐語がまさに退けたもの |
| パレットの群の線（`S-143`）| **`CH-4` / `GL-006`** | 群の見出しを消して線 1 本にしたのは `FR-029`（用途を言葉ではなくアイコンで伝える）のためである。⛔ **その線が隔てて見えなければ、見出しを消した代償だけが残る** |

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.2`**（曖昧・非検証な表現を排除）| `OP-10` の「その文書が覆う最初の日」 | ⛔ **「覆う」を定める行が 0 件。**3 通りに読め、読みごとに起動時の左端が変わる ⇒ **この MUST は現状のままでは検証できない**（CR-299 が名指しし、本 CR が閉じる）|
| **`R1.3`**（唯一の正がある）| 表 T-023d の `GR-5` | ⛔ **`actualDuration` をどうするかについて正が 0 である。**実装は「据え置き」を選び、利用者の期待は「置き直す」であった。**どちらも仕様から導けなかった** |
| **`R1.4`**（異常系・準正常系を網羅）| 全消しの直後にパレットの図形を押す道 | ⛔⛔ **消えたものを選択から落とす行が、どの表にも 0 件**（`SL-1`・`FR-032`・表 T-050・`SK-2` / `SK-3` を実測）。⇒ 死んだ `uid` へ命令が組み立てられ、束ごと拒まれていた |
| **`R2.7`**（DRY）| 本 CR の `SL-9` の書き方 | ⭐ **入口ごとに「ここで選択を落とす」と書く案を退けた** —— 削除の入口は 表 T-050 の連鎖・行の削除・取り消し／やり直し・**別の文書を開くこと**と複数あり、入口ごとに書けば入口が増えるたびに行が増える。**不変条件 1 行が、入口を 1 つも数えない** |
| **`R2.7`**（DRY）| 本 CR の矩形の見せ方 | ⭐ **`S-174` / `S-175` / `S-151` を新しく写さず、指すだけにした。**⛔ 写せば「選択枠の太さ」の正が 2 つになる |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`Task` が 1 件も無い文書では、`OP-10` の除外そのものが働かない**（＝`FR-055` の全体表示に落ちる）と書いた | ⚠️ **数える対象が無いときの日を発明しない**ためである。⛔ 出荷するテンプレートは `Task` を 1000 件持つので、**今日この枝に入る文書は存在しない** |
| 2 | ⭐ **選択の不変条件を 表 T-023c の「結びの規則」として書いた**（行ではない）| ⛔⛔ **最初は行 `SL-9` として書き、検査に止められた** —— `generate_help_roster.py` が同表の `SL-` 行をヘルプへ刷るので、**不変条件が「利用者ができる操作」として画面に出てしまう**（実測: 「10 件しか無いのに 11 件を求めている」で赤）。⭐ **止められて読み直した結果そちらが正しい** —— `SL-1` 〜 `SL-8` は選択の *操作* の行であり、本件は選択が *満たすべきこと* である。⭐ **表 T-020 が既に同じ形の結びを持っている**（「実績バーが名称ラベルを覆ってはならない」）。⇒ **行は増えない** |
| 3 | ⭐ **矩形を 表 T-020 の `ZO-6` として最前面に置き、`ZO-5` の 順 を「5（最前面）」から「5」へ直した** | ⛔ **直さないと、同じ表に最前面が 2 つ立つ** |
| 4 | ⭐ **`S-143` の値は `1 × 6px` のままとし、2 つ目の数の *意味* を「左右の空き」から「まわりの空き」へ広げた** | ⛔⛔ **最初は 3 つ組（`1 × 6 × 6px`）にしようとして、生成器に止められた** —— `settings.schema.json` の `pair` は `minItems: 2` / `maxItems: 2` である。⭐ **止められて読み直した結果、こちらのほうが正しい** —— 裁定は「上下も左右と同じ 6px」であって、**上下だけの数を新しく決めたのではない。**⇒ **数は 2 つのままでよく、schema も生成器も 1 バイトも動かさない。**⚠️ **新しい行 `S-143a` を立てる手もあったが、同じ理由で退けた** —— 別の行にすると「別々に決められる 2 つの数」に見える |
| 5 | ⛔ **台帳 D-134（確認の面のホイール）を本 CR に含めなかった** | ⚠️ **裁定は下りている**（宿主に委ねる）。⛔ **含めなかったのは、仕様を触らずに閉じられる見込みがあるからである** —— 第 5 節 |

---

## 1. ⛔ 測った

すべて出荷する `dist/index.html` を 1920×1080 で開いての実測（2026-08-29）。

| 測ったこと | 実測 |
|---|---|
| ⭐ **背景を素でドラッグして離す** | **選択枠（`rect` の `stroke-dasharray="2 2"` `stroke-width="2"`）が 0 → 32 件。**⇒ **選択そのものは完全に効いている** |
| ⛔ **握っているあいだ** | **1 件も増えない**（8 件の点線は予実の補助線であって選択枠ではない）|
| ⭐ **`Ctrl`＋ドラッグして離す** | ポインタを 横 −240・縦 −150 動かすと、**一番左のバーの x が 170 → −70**（ちょうど −240。「パンは等倍とすること（MUST）」を満たす）、**一番上の行が `Whole Product` → `Phase Bars`** |
| ⛔ **握っているあいだ** | **縦も横も 1px も動かない** |
| 中ボタンのドラッグ | ⭐ `Ctrl` と同じ（離した時点で縦横とも動く）|
| ⭐ **パレットの群の罫** | **3 本・厚さ 1px・`rgb(217,221,226)`（`S-149`）・左右 6px・上下 0px。**上下は群のボタンが罫に接している |
| ⭐ **上下に 6px 空けたときのパレット** | 高さ **165px → 201px**（罫 3 本 × 上下 6px）|
| ⭐ **起動テンプレートの日付** | `Task.start` の最小 **2026-04-01**（1000 件）／`Task.actualStart` の最小 2026-04-24（294 件）／`HighlightBox` と `CommentBox` は **0 件**／暦の例外日の最小 2026-12-24（7 件）／`BaselineTask` は 0 件 |
| ⛔ **`Panel Divider` の掴み帯** | **x = 166〜174 の 8px**（境界 170 を 4px ずつまたぐ）。⭐ **その上でのポインタは `col-resize`** —— **黙ってはいない** |

### ⛔ 触る前に測った 2 つ（規則の要求どおり）

```
induced.py OP-10 T-024a GR-5 GR-6 FR-011 T-023d SL-1 SL-3 SL-8 T-023c
           SK-3 T-050 T-020 S-174 S-175 S-151 PD-1 PD-5 T-023a
           MK-10 T-023 T-028 T-103 S-143 FR-053
  → 種 25／25、内部の辺 19、輪 2
      輪 A  size 3 : S-174 S-175 SL-8
      輪 B  size 2 : FR-053 S-143
```

⭐ **輪 B は 1 パスで書く**（`S-143` の題と値、`FR-053` の 1 語）。
⭐ **輪 A は触らないことで避ける** —— 矩形の規則は `S-174` / `S-175` を**指すだけ**で、値の側にも `SL-8` にも 1 文字も書かない。

⇒ **5 つの束が、objects を 1 つも共有しない。**

| 束 | objects | 裁定 |
|---|---|---|
| 1 | `OP-10`（表 T-024a）| 起動時の左端 |
| 2 | `GR-5`（表 T-023d）| 実績の開始点 |
| 3 | 表 T-023c（`SL-3` / 新 `SL-9`）・表 T-020（`ZO-5` / 新 `ZO-6`）・表 T-023a（`PD-1`）| 選択の不変条件・矩形・パン |
| 4 | —— | ⛔ **本 CR では立てない**（第 5 節）|
| 5 | `S-143`（表 T-206）・`FR-053` | パレットの群の線 |

---

## 2. 編集計画

| # | 対象 | 何を書くか | 台帳 |
|---|---|---|---|
| 1 | `01-04-requirements.md` 表 T-024a の `OP-10` | 「その文書が覆う最初の日」を **`Task` の `start` と `actualStart` の最小**と定める 1 文。⛔ 注記と暦の例外日を数えることを MUST NOT で退ける。⚠️ `Task` が 0 件なら除外が働かない | D-125 |
| 2 | 同 表 T-023d の `GR-5` | 操作 の欄に「**終了日は据え置き、`actualDuration` を置き直すこと（MUST）**」。⛔ 平行移動を MUST NOT で退ける | D-112 |
| 3 | 同 表 T-023c の**結びの規則** | 「選択は、文書に実在する対象だけを指すこと（MUST）。実在しなくなった対象を選択に残してはならない（MUST NOT）」。⛔ **行にしない**（§0③ の 2）| D-109 |
| 4 | 同 表 T-023c の `SL-3` | 「**握っているあいだ、取ろうとしている矩形を描くこと（MUST）**」。太さ `S-174`・刻み `S-175`・色 `S-151`・重ね順 `ZO-6` を**指す** | D-110 |
| 5 | 同 表 T-020 に **`ZO-6` を新設**、`ZO-5` の 順 を直す | `ZO-6` ＝ 範囲選択の矩形（最前面、握っているあいだだけ）。`ZO-5` は「5（最前面）」→「5」 | D-110 |
| 6 | 同 表 T-023a の `PD-1` | 「**握っているあいだ、ポインタに追従させること（MUST）**」。縦横の両方向 | D-111 |
| 7 | `_source/settings.json` の `S-143` | 題を「線の太さと**まわりの空き**」へ。⭐ **値は `1 × 6px` のまま** —— 2 つ目の数が上下左右のすべてに当たると備考で述べる | D-94 |
| 8 | `01-04-requirements.md` `FR-053` | 「線の太さと**左右の**空きは…」→「線の太さと**まわりの**空きは…」（**#7 と 1 パス**。輪 B）| D-94 |
| 9 | `_assets/tbl-settings.md` | ⛔ **手で書かない。**`npm run gen:settings` で刷り直す | — |
| 10 | `A-appendix.md` 変更履歴 | 版 1 行。⛔ **台帳の行 ID を backtick で書かない**（検査 7）| — |

---

## 3. 数の予測

| | 措置前（実測）| 予測 | 由来 |
|---|--:|--:|---|
| `tables` | 135 | **135** | 表は 1 つも増やさない |
| `figures` | 11 | **11** | 図は触らない |
| `rows` | 1721 | ~~1723~~ → **1722** | ⛔ **予測を外した。**`ZO-6` の 1 行だけである —— 選択の不変条件は行ではなく結びの規則になった（§0③ の 2）。✅ **措置後の実測 1722** |
| `uids` | 150 | **150** | 要求も目標も増やさない |
| アイコン | 78 | **78** | ⛔ **新しい図形を 1 つも起こさない**（`RC-13`）|
| 語 | 355 | **355** | ⛔ **画面に刷る新しい語を 1 つも足さない** |

⭐ **新しい設定値の行は 0 である。**`S-143` は**値も 2 つ組のまま**で、**2 つ目の数が当たる先が上下にも広がっただけ**である。

✅ **措置後の実測（検査 28 本、ALL GREEN）**: `tables=135  figures=11  rows=1722  uids=150`。アイコン 78・語 355 も不変。

---

## 4. 落ちる／緑になる試験

| | 予測 |
|---|---|
| `OP-10`（起動時の左端）| ⭐ **落ちる試験 0 件** —— この MUST を主張する試験が 1 本も無い（それが台帳 D-125 そのものである）|
| `GR-5` | ⚠️ **平行移動を前提にした試験があれば落ちる。**⛔ **緩めずに書き直す** —— 落ちたら、それは仕様が変わったことの証拠である |
| 表 T-023c の結びの規則 | ⭐ 新しい試験が要る（実在しない `uid` を選択に残さないこと）|
| `SL-3` の矩形 ／ `PD-1` の追従 | ⭐ 新しい試験が要る（握っているあいだの絵）。⚠️ **単位試験では「握っているあいだ」を測りにくい** —— e2e 側に置く |
| `S-143` | ⭐ **落ちる試験は無い見込み** —— **値の形は 2 つ組のまま**で、変わったのは当たる先だけである。⚠️ 上下 0px を測っている試験があれば落ちる |
| 検査 28 本 | ⛔⛔ **予測を外した。2 度赤くなった** —— ①`settings.schema.json` が `pair` を 2 個に固定していた ②ヘルプの生成器が 表 T-023c の `SL-` 行を全部刷るので `SL-9` が「利用者ができる操作」として現れた。⭐⭐ **どちらも、赤が正しく、私の設計のほうが誤っていた。**✅ **措置後は ALL GREEN。**⛔ **緑は「参照が解ける」の証拠であって「合っている」の証拠ではない** |

---

## 5. ⛔ 決めずに残したもの（規則 05 の欄）

### (a) ⛔⛔ 台帳 D-134 —— 確認の面のホイール。**裁定は下りたが、仕様を触らない見込みである**

⭐ **裁定**（利用者、2026-08-29）: **浮いた面の上では、素のホイールとその面の中のスクロールバーを宿主に委ねる。**

⛔ **本 CR に含めなかった理由**: **仕様の行が要るかどうかが、まだ測られていない。**

| 申し送りの主張 | 疑っている点 |
|---|---|
| 「直しは `uf-71` の見張り 2 件（聴き手の置き場と数）を赤くする」 | ⛔⛔ **反証しに行く。**ブラウザに任せるとは「**止めるのをやめる**」ことであり、**聴き手を 1 つも増やさない**はずである。増えないなら見張りは赤くならず、**仕様に足す行も 0 である** |
| 「`MK-10` が止めている」 | ⭐ **既に偽と分かっている** —— 同行の主語は両半分とも「**本ツールが割り当てた**修飾キーの付いた入力」で、素のホイールに届かない。⇒ **飲んでいるのは `regionAtPointer` が浮いた面の上の点にも `rowArea` と答えていることである**（`PD-12` が同じ穴を記録済み）|

⇒ ⭐ **先に測り、行が要るなら CR-301 を立てる。**⛔ **要らないなら実装だけで閉じる。**

### (b) ⚠️ 台帳 D-110 の残り半分 —— 矩形の**塗り**

⭐ **利用者へ実物の絵を 4 枚示した**（何も描かない／破線のみ／1px 実線／破線＋12% の塗り）。
⭐ **本 CR は「破線のみ」を書く** —— **新しい値を 1 つも起こさないからである。**
⚠️ **実物を見て「薄く塗ったほうが囲みが読める」となれば、足すのは塗りの濃さ 1 行である。**⛔ **前に立つ者は、絵を見た時点で「塗ったほうが読める」と述べた** —— 下にハイライトボックスが在る場所では、線 1 本が負ける。**利用者の裁定を待つ。**

### (c) ⚠️ `_source/settings.json` に壊れた文字が 2 つある

⛔ **`S-172` ほかの備考に `⛐`（U+26D0 CAR SLIDING）が 2 か所ある。**⭐ **`⛔`（U+26D4）の打ち損じである。**
⛔ **本 CR では直さない** —— 本 CR が触る行ではなく、**触ると 束 5 の object が増える。**⚠️ **別の CR で直す。**
