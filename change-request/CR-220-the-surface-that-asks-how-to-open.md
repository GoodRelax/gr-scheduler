# CR-220 — 開き方を問う面

> **閉じるもの**: ⛔ **`OP-3` は「読んだ内容をどう扱うかを人に選ばせること（MUST）」「3 つとする」「`GRS` が勝手に決めてはならない（MUST NOT）」と定めるのに、その 3 択を問う面が 表 T-103 に無い。**
> ⛔ **表 T-109 は自ら網羅と名乗るので、入口を `src/` で発明できない。**
> ⭐ **利用者の裁定（2026-08-23、裁定 19）**: **「開き方を選ぶ面」を 表 T-103 に立て、表 T-109 に 3 入口を置く。**
> ⚠️ **これが閉じるまで、ファイルを開く経路（`SK-10` / `IC-1`）が丸ごと死んでいる。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`。受ける目標は `GL-001` / `GL-002`。** ⛔ **文書を開けないツールは、`CH-2` のどの課題にも着手できない。**

⚠️ **満たされていない MUST である** —— `OP-3` は要求として立っているが、**それを果たす場所が仕様の中に無い。** 実装した体は推測せず、`frame-loop.ts` に STOP を書いて止まった。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（網羅性）| `OP-3` の 3 択 | ⛔ **要求が求める選択を受け取る面が無い。** 要求だけが在って、着地が無い |
| **`R2.1`**（仕様の名前を使う）| 新しい面の名 | ⭐ **`Export Chooser`（`U-54`）に倣う。**「…を選ぶ面」という同じ形が既に在るので、**新しい概念を作らない** |
| **`R2.9`**（YAGNI）| `Confirmation` を 3 択へ広げる案 | ⛔ **退けた。** `NT-7` が「続けるか取りやめるか」と 2 択で書き、`IC-69` / `IC-70` の対がその具体である。広げると `NT-7` の定義ごと動く |
| **`R1.3`**（唯一の正がある）| 3 つの選択肢の綴り | ⭐ **`PI-10` の `OpenChoice` が既に `replace` / `merge` / `baseline` と綴っている。** 表は日本語だけを持ち、綴りは公開エントリの正に委ねる |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **面の確定名を `Open Chooser` にした** | ⛔ **`U-54` `Export Chooser` の対である。** 書き出しの形式を選ぶ面が `Export Chooser` なら、開き方を選ぶ面は `Open Chooser` が最も驚かない（`R4` の POLA）|
| 2 | ⭐ **3 入口の `群` の欄を `—` にした** | ⚠️ **`IC-69` / `IC-70`（`Confirmation` の 2 択）が既に `—` である。** 面が 1 つの目的しか持たないとき、群は意味を持たない |
| 3 | ⭐ **`正` の欄を 表 T-024a の `OP-3` にした** | ⚠️ **`FR-087` を冠さない形にした** —— 表 T-109 の他の行が表の行 ID を直に書いており、そちらに揃えた |
| 4 | ⭐ **図形は利用者の裁定を受けてから起こした**（2026-08-23）| ⛔ **最初の巡は図形なしで当てようとして差し戻した** —— `generate_icon_glyphs.py` が「図 F-019 が図形を描かない行が 3 つある」と拒み、表 T-026 の `RC-13` が「**本仕様書の中で発明しない**」と定めるからである。⭐ **案を 3 つずつ描いて実際に描画して見比べ、2 巡直してから問うた** —— `IC-71` は端を折ったページの対、`IC-72` は上下から集まる 2 本の矢印、`IC-73` は `IC-4` の対に入りの矢印。⚠️ **3 つとも `RC-13` の一覧に載せた**（後から差し替えられる）|

---

## 1. 測った

```
induced.py  T-103 T-109 OP-3 U-54 U-55 NT-7 FR-087 PI-10 T-230 （ほか 30 種と同時に）
impact.py   T-103 / T-109
```

| 測ったこと | 実測 |
|---|---|
| 種の解決 | **39 / 39** |
| 閉路 | ⭐ **0** —— 1 つずつ書いてよい |
| 表 T-103 の行数 | **56 行**（`U-1` 〜 `U-55`。⚠️ `U-51` と `U-52` が入れ替わって並ぶ）|
| 表 T-103 を指している要求 | **0 件** —— 名前の正であり、規則は持たない |
| 表 T-109 の行数 | **70 行**（`IC-1` 〜 `IC-70`）|
| 表 T-109 を指している要求 | **4 件** |

⭐ **下流は既に用意できている**（触らない）:

```
PI-10  importDocument が OP-3 の答えを引数で取る
T-230  RD-3 / RD-4 が着地を持つ
⇒ 欠けているのは面と入口だけである
```

## 2. 編集計画

⛔ **オブジェクトごとに 1 回。同じ行を 2 度書かない。**

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-103 | `U-56` を 1 行足す —— `Open Chooser` ／「開き方を選ぶ面。開く規則は `FR-087`（表 T-024a の `OP-3`）、3 つの選択肢は `PI-10` の `OpenChoice` が綴る」|
| 2 | 表 T-109 | `IC-71` / `IC-72` / `IC-73` を 3 行足す —— 面は `Open Chooser`、群は `—`、何の入口かは「置き換える」「合流させる」「重ねる」、正は `FR-087`（表 T-024a の `OP-3`）|
| 3 | 表 T-026 の `RC-13` | ⚠️ **未描画の一覧に 3 行を足す** —— `IC-58` 〜 `IC-61` と同じ扱い |
| 4 | A-appendix | 変更履歴に 1 行 |

⛔ **`OP-3` の本文は 1 文字も動かさない** —— 要求は既に正しい。無かったのは着地である。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **132** | ⭐ **0**（新しい表を作らない）|
| `figures` | 11 | **11** | 0 |
| `rows` | 1609 | **1613** | **＋4**（表 T-103 に 1、表 T-109 に 3）|
| `uids` | 148 | **148** | ⭐ **0**（要求を足さない）|

⚠️ **表 T-026 の `RC-13` への追記は、既存の行のセルを広げるだけなので `rows` を動かさない。**

---

## 4. ⭐ 当てた結果（**2026-08-23、第 2 の試み**）

⛔ **1 度目は差し戻した。** 図形が無いまま 表 T-109 に 3 行足したところ、生成器が拒んだ:

```
table T-109 holds 3 row(s) that fig-icons.svg draws no shape for (IC-71, IC-72, IC-73),
and the glossary says every row has one
```

⭐ **図形の裁定を受けたので当て直した。** 実測:

| | 予測 | 実測 |
|---|---|---|
| `tables` | 132 | **132** ⭐ |
| `figures` | 11 | **11** ⭐ |
| `rows` | ＋4 | **1610 → 1614** ⭐ |
| `uids` | 148 | **148** ⭐ |

⚠️ **予測に書いていなかったものが 3 つ出た:**

```
図 F-019 の viewBox が 284 → 328  —— 72 席のうち空きが 2 席しか無く、7 段目が要った
表示語の枠が 177 → 183           —— アイコン 3 行 × label / hint。⛔ 語は空のまま（PD-160）
重複検出に新しい組が 1 つ         —— IC-71 と IC-72。⭐ IC-69 / IC-70 と同じ形なので
                                    その隣へ基準線に載せた（new 0）
```

⭐ **どれも「行を足す」ことの当然の帰結であり、数え落としではない。**⚠️ **それでも予測に書けていなかったのは事実である** —— 次に表へ行を足すときは、**図・表示語・重複基準線の 3 つを予測の欄に入れる。**
