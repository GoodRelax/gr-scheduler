# CR-415 — 上下に隣り合うタスクの掴み代は、描いた形状を先に見て分ける

> **閉じるもの**: 利用者の裁定 `JDG-150`（追加の指示と問 2 への答え）を仕様へ着地させる。`JDG-151`（掴み代の束ねを Entity へ寄せる）はコードの置き場の直しなので、本書は実装への申し送りだけを書く。逐語は `docs/development-records/rulings.md` の `JDG-150` / `JDG-151` が持つ。
> - `JDG-150`: 「なお、行の上下方向のマージン変更によって、上下方向のつかみシロの修正も必要だ。」「① 描いた形を先に見る（推奨）」
> - `JDG-151`: 「フェードのつかみシロ。 Entity へ寄せろ。」
>
> ⭐ **本書は仕様だけを当てる。** 表 T-023d の結びに、上下に積んだ 2 つのタスクの掴み代が縦に重なるときは描いた形状を先に見て 表 T-261（`GS-1`〜`GS-4`）に従う（MUST）段を足し、表 T-261 を 表 T-244 の直前に置いた。表 T-206 の `S-90` の注から 表 T-261 を指した。読んだ木は `1fe521ca`。
> ⚠️ 大きさの波の 4 本目（掴み代）である。8 本（`CR-409`〜`CR-416`）は 1 つの計画で 1 度に当てた（`CR-409` の第 3 節）。
> ⚠️ `src/`・`tests/`・`tools/` は触っていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`**（`CH-1` の受ける目標は `GL-008`）—— 作図ソフトと同じ操作感: 押した形を掴む。隙間を詰めた（`CR-413`）あとも、隣のタスクの掴み代に押しを奪われない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— `CR-413` の隙間（表示の倍率 100 で 2.75px）は、`S-90` の上下 12px、`GR-1` / `GR-2` の `S-92` の半分 7.5px より狭い ⇒ いまの「本表の順は離れているものどうしの優先を決める」だけでは、上の段のタスクの端の掴み代が下の段のタスクの形状の上まで届き、本表の順で先に当たったほうが勝つ。⭐ 表 T-261 で分けた。
- **`R1.3`** —— `CR-399` の「ただし」の段（描いた形状の上では依存線は描いた線だけを取る）と `JDG-141`（依存線が付く端では、端の掴み代のうち線から `S-137` 以内を線が取る）と食い違わないことを、`GS-1`（形状の上は「ただし」の段に従う）と `GS-4`（形状の外で隙間を通る線は先）で確かめた。
- **`R1.4`（境界値）** —— 隣が無い向き（`GS-3`）、両方の掴み代に入る点（`GS-2`）を書いた。⚠️ 2 つの形状から等しい距離の点は書いていない（第 9 節の問い 1）。
- **`R1.6`（否定要求は代替を示す）** —— `GS-1` / `GS-3` の MUST NOT に、それぞれ何を掴むかを並べた。
- **`R2.1`（命名）** —— 接頭辞 `GS`（Grab Split）。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 対象の掴み代を、描いた形状の外へ縦に広がるもの —— `GR-1` / `GR-2` の `S-92`（の半分）と `GR-3` / `GR-4` の `S-90` —— とする | ⭐ `JDG-150` の読み「予定の端の上下 12px の掴み代が隣のバーへ食い込み」。⭐ `GR-5` / `GR-6` の `S-91` は実績バーの帯に閉じ（表 T-023d の結び）、`GR-9` / `GR-17` / `GR-18` は描いた印そのものなので、縦に外へ出ない | `GR-7`（進捗マーカー）と `GR-8`（再開アイコン）の当たり判定の箱が隣の形状に重なる場合は扱っていない（第 9 節の問い 2）|
| 決定 2 | 「隣のタスク」を「押した点の真上か真下にいちばん近い描いた形状のタスク」と書く | ⭐ `JDG-150` 案 ①「形の上ならそのバー、形の外は近い方、隣が無い方向は 12px のまま」 | 無し |
| 決定 3 | `GS-1`: 描いた形状の上では、その形状のタスクの行だけで本表の順に決め、隣の掴み代を届かせない（MUST NOT）| ⭐ 案 ①「形の上ならそのバー」 | 無し |
| 決定 4 | `GS-2`: 形状の外で両方の掴み代に入るときは、縦の距離（押した点から描いた形状の端まで）が近いほう | ⭐ 案 ①「形の外は近い方」 | 等しい距離の点は未決（第 9 節の問い 1）|
| 決定 5 | `GS-3`: 隣が無い向きは `S-90` / `S-92` のまま、縮めない（MUST NOT）| ⭐ 案 ①「隣が無い方向は 12px のまま」。⭐ `S-90` の注「掴み領域は読む人のアクセシビリティに属する」 | 無し |
| 決定 6 | `GS-4`: 形状の外で、隙間を通る依存線の掴み代（`S-137`）に入るときは、`GS-2` / `GS-3` より先に線 | ⭐ 案 ①「隙間に依存線があれば線が先」。⭐ 本表で `GR-13` が `GR-1`〜`GR-4` より上に在る順と同じ | 無し |
| 決定 7 | 表 T-261 を結びの途中ではなく 表 T-244 の直前に置き、規則の段（「表 T-261 に従うこと（MUST）」）だけを「ただし」の段の直後に置く | ⭐ 結びの途中に表を置くと、その後の結びの段（「本表の順は …」）が新しい表のものとして読まれた（重複検出器が `T-261` の段として数えた。実測）| 規則の段と表が離れる |
| 決定 8 | `JDG-151` は仕様を変えない。`05-07-design.md` の 表 T-064 の `PI-7`（`ItemHitArea`）と 表 T-075 の `UF-7` / `UF-48` は、掴み代の大きさをどこで組み立てるかを述べていない ⇒ 第 6.3 節に申し送る | ⭐ `JDG-151` の読み「仕様の変更ではなく層の置き場の直しである」 | 無し |

---

## 1. 読んで確かめた事実

| 事実 | 所（`1fe521ca`） |
|---|---|
| 表 T-023d の順: `GR-1` / `GR-2`（フェード）→ … → `GR-13`（依存線）→ `GR-14` → `GR-3` / `GR-4`（予定の端）→ … → `GR-12`（予定バー本体）| `docs/spec/01-04-requirements.md:3297`〜`:3318`（当てる前）|
| `S-90` ＝ バーの上下と、端点の外側に 12px。`S-92` ＝ 15 × 15px（フェード）| `docs/spec/_assets/tbl-settings.md:287`、`:289`（当てる前）|
| 「ただし」の段: 描いた形状の上では依存線は描いた線そのものだけを取る | `docs/spec/01-04-requirements.md:3364`〜`:3383`（当てる前）|
| 判定の本体は Entity。掴み代の大きさの束ね（`POINTER_SLOP`、`S-92` の半分を使う知識を含む）は Framework に在る | `src/entity/layout-engine/item-hit-area/item-hit-area.ts`（`itemAtPointer`）、`src/framework/single-html-shell/frame-loop.ts:363`〜`:369`（`fadeHandle: NOT_STORED_SIZES['S-92'][0] / 2`）（読むだけ）|
| `05-07-design.md` は掴み代の大きさを組み立てる場所を述べていない | `PI-7`（`:427`）、`UF-7`（`:344`）、`UF-48`（`:383`）を読んだ |
| `T-261` / `GS-` は `docs/spec`・`src/`・`tests/`・`tools/` を引いて 0 件 | `grep -rn`（当てる前）|

## 2. 測った

**隙間と掴み代**（表示の倍率 100。隙間は `CR-413` の第 2 節）:

| | px |
|---|--:|
| 形と形の縦の隙間 | 2.75 |
| `S-90` の上下（`GR-3` / `GR-4`）| 12 |
| `S-92` の半分（`GR-1` / `GR-2`）| 7.5 |
| `S-137` の線の左右（`GR-13`）| 6 |

⇒ 上の段の端の掴み代は、隙間を越えて下の段の形状へ 12 − 2.75 ＝ 9.25px 食い込む（表 T-261 が無ければ、本表の順で `GR-3` / `GR-4` が `GR-12` より先に当たる）。
⚠️ 数は式から計算した。当たり判定は押していない。

## 3. グラフ

**当てる前**: `impact.py T-023d` —— 指す要求 22、2 次 44。`S-90` —— 指す要求 2（`FR-016` / `FR-039`）、参照 13。
**`induced.py`**: `CR-409` の第 3 節と同じ実行。`S-90` と `S-91` の大きさ 2 の閉路（値の行どうしの作法）。本書は `S-90` だけを書いた。
**届いた行を `rulings.md` で引いた**（`S-90` / `掴み代` / `T-023d`）: `JDG-17`〜`JDG-20`（同じ日の端点は終了側、予定の開始日の左右）、`JDG-34`〜`JDG-40`（依存線の優先、線の縦幅は実績より狭い）、`JDG-140` / `JDG-141`（`CR-399`）。⭐ 表 T-261 は、離れた形状どうし（上下の隣）の分け方であり、1 つのタスクの中の端点の分け方（`JDG-17`〜`JDG-20`）と「ただし」の段を変えない ⇒ 衝突 0。

## 4. 当てた中身

| 対象 | 置き場（当てたあと） | 中身 |
|---|---|---|
| 表 T-023d の結びの段 | `docs/spec/01-04-requirements.md:3399`〜`:3400` | 上下に積んだ 2 つのタスクの掴み代が縦に重なるときは、描いた形状を先に見て 表 T-261 に従う（MUST）。隣のタスクの意味 |
| 表 T-261 | `:3446`〜`:3453` | `GS-1` 描いた形状の上 ／ `GS-2` 両方の掴み代に入る ／ `GS-3` 隣が無い向き ／ `GS-4` 隙間を通る依存線 |
| `S-90` の注 | `docs/spec/_source/settings.json`（→ `_assets/tbl-settings.md:287`）| 上下の掴み代が隣の形状に重なるときは 表 T-261、隙間は本値より狭い |
| 接頭辞 `GS` | `docs/spec/_source/row-id-prefixes.json`（→ `_assets/tbl-row-id-prefixes.md`）| Grab Split |

## 5. 数の予測

本書の差: **tables +1**（T-261）／ figures 0 ／ **rows +4**（`GS-1`〜`GS-4`）／ uids 0。8 本の合計は `CR-409` の第 5 節。
⭐ 本書が足した `（MUST）` は 5（結びの段 1、`GS-1`〜`GS-4` に 1 ずつ）、`（MUST NOT）` は 2（`GS-1`、`GS-3`）。

## 6. 影響

### 6.1 仕様

| 所 | 本書で |
|---|---|
| 表 T-023d の「ただし」の段（`CR-399`）| そのまま —— `GS-1` が指す |
| 表 T-023d の予定と実績の境目、同じ日の端点、表 T-244 | そのまま —— 1 つのタスクの中の分け方である |
| `FR-016` の行の軸の上限 | 掴み代は上限に効かない |

### 6.2 記録

| 所 | 書いたこと |
|---|---|
| `docs/development-records/rulings.md` の `JDG-150` | 状態を 適用済 へ、着地先に本書と行 ID を足した |
| `docs/development-records/rulings.md` の `JDG-151` | 変えていない（コードの置き場の直しで、仕様の条項が無い。実装の波で着地）|
| `docs/development-records/changelog.md` | 本書の 1 行 |

### 6.3 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `src/entity/layout-engine/item-hit-area/item-hit-area.ts`（`itemAtPointer`）| 押した点の真上と真下の、いちばん近い描いた形状を求め、表 T-261 の順（`GS-1` → `GS-4` → `GS-2` / `GS-3`）で掴み代を分ける |
| `src/framework/single-html-shell/frame-loop.ts:363`〜`:369` → `item-hit-area.ts`（`JDG-151`）| `POINTER_SLOP` の束ね（`S-90` / `S-91` / `S-92` の半分 / `S-137` / `S-230`）を丸ごと Entity へ移す。フェードの角が `S-92` の半分を使う知識も Entity が持つ。Framework は束ねを組み立てずに `itemAtPointer` を呼ぶ |
| 試験 | 掴み代に触る試験（`handoff.md` §2.8 ③ の 3 の実測で 31 ファイル）のうち、2 段の行で端の上下を押すもの |

## 7. 試験が主張すべきこと

⭐ 名前は本書のとおりに写すこと: 表 T-261（`GS-1`〜`GS-4`、列「行 ID・押した点・掴むもの」）。

1. `GS-1`: 表示の倍率 100、同じ行の 2 段に上下に重なる 2 つの矩形 A（上）と B（下）。A の右端の外側 3px の真下で、B の描いた形状の上の点を押すと、B（`GR-12` か B の端）を掴み、A の `GR-4` を掴まない。⭐ 古い実装（本表の順で A の `GR-4` が勝つ）を赤にする。
2. `GS-2`: A と B のあいだの隙間（2.75px）の中で、A に近い点は A の `GR-4`、B に近い点は B の掴み代（あれば）。
3. `GS-3`: A の上に隣が無いとき、A の右端の外側 3px・描いた上端の上 11px の点は A の `GR-4`（`S-90` の 12px の中）。
4. `GS-4`: A と B のあいだの隙間を依存線が横に通るとき、線から `S-137` 以内の点は依存線（`GR-13`）。
5. 行をまたぐとき（上の行のいちばん下の段と、下の行のいちばん上の段）も 1〜4 が同じ。

## 8. 関係 —— `JDG-150`・`JDG-151`

- ⭐ **`JDG-150` を着地させる** —— 「上下方向のつかみシロの修正」と案 ①「描いた形を先に見る」は 表 T-023d の結びの段と 表 T-261（形の上 `GS-1`、近い方 `GS-2`、隣が無い方向は 12px `GS-3`、線が先 `GS-4`）。
- ⚠️ **`JDG-151` は着地させない** —— 仕様に条項が無い。第 6.3 節の申し送りで、実装の波が着地させる。

## 9. ⛔ この変更でやらないこと ／ 返す問い

- ⛔ `S-90` / `S-92` / `S-137` の値を変えない。
- ⛔ 1 つのタスクの中の端点の分け方を変えない。
- ⛔ `src/`・`tests/`・`tools/` を編集しない（第 6.3 節）。

**返す問い**:

1. **上と下の描いた形状から等しい距離の点（`GS-2`）はどちらを掴むか** —— 推奨: 下（表 T-246 の `HB-5` が行の境目で下を採るのと同じ向き）。⛔ `JDG-150` に無いので本書は決めていない。
2. **`GR-7`（進捗マーカー）と `GR-8`（再開アイコン）の当たり判定の箱（`S-22`）が、上下の隣の形状に重なるときも 表 T-261 で分けるか** —— 推奨: 分ける（形の上ならそのバー）。⛔ 本書は対象を `S-90` と `S-92` に限った（決定 1）。
