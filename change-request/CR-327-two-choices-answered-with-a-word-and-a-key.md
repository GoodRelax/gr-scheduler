# CR-327 — 2 択は語で答え、キーでも答えられる

⭐⭐ **利用者の指示（2026-09-01、逐語）**

> **キャプチャのように 2 択の問いは y / n のキーで回答可能とせよ。アイコンではなく
> [Yes] / [No] のボタンを表示せよ。ここで Y と N は太字にしてショートカットキーを
> アフォーダンスせよ**
>
> **…しますか？ といった表示すれば y/n で回答できるだろ？他の問いも 2 択の場合は
> y/n とせよ。もし違和感がある選択があれば、俺に裁定させよ**

**台帳の行**: `D-174`。⛔ **裁定は下りている。問い直さない。**
⭐⭐ **本要求は `D-147`（透かしのパスワードを問う面）の塞がりも解く** ——
**語のボタンなら新しい図形が要らず、図形の裁定を待たずに済む。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

`GL-006`（アフォーダンスとシグニファイア）。

⛔ **丸囲みの ✓ と ✕ は、どちらが「続ける」でどちらが「取りやめる」かを、
知っている人にしか告げない。**⭐ **語は自ら名乗り、太字の頭文字がキーを名乗る。**

### ② レビュー観点のどの条項を当て、何が出たか

`docs/development-rules/07-review-standards.md` の **`R3.1`**（同じ規則・同じ値を 2 か所に持たない）。

⛔⛔ **当てて、同じ 2 つの答えの語が辞書に 2 組あることが分かった:**

```
confirmation      answer: proceed → 「続ける」 / Proceed
                  answer: cancel  → 「取りやめる」/ Cancel
icons  IC-69      label: 「続ける」 / Proceed     hint: 「問いに続けると答える」
       IC-70      label: 「取りやめる」/ Cancel   hint: 「問いに取りやめると答える」
```

⇒ ⛔ **同じ語が 2 か所に在り、片方だけを直せば必ず離れる。**⭐ **本要求で 1 組にする。**

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

1. ⭐⭐ **`Yes` / `No` は、どの表示言語でも英語で綴ることにした。**
   ⛔ **日本語に訳すと、太字の頭文字が `y` / `n` を指さなくなる** ——
   **アフォーダンスそのものが成り立たない。**
   ⭐ **前例が在る** —— **表 T-037 の `NT-8` が、通知を消す入口の語を
   「どの表示言語でも `OK` と綴ること（MUST）」と定めている**（利用者の裁定 2026-08-25）。
   ⭐ **`No name`（`defaultNames`）も同じ扱いである**（利用者の裁定 2026-09-01）。
2. ⛔⛔ **`IC-69` / `IC-70` を 表 T-109 と 図 F-019 から外すことにした。**
   ⭐ **前例で決めた、好みで決めていない** —— **通知を消す `OK` も語のボタンであり、
   `IC-` の行を 1 つも持たない。その語は `noticeDismiss` が持つ。**
   ⇒ **語のボタンは入口の名簿にも図形の図にも属さない。**
   ⚠️ **図形は 89 → 87 になる。**⛔ **名簿と図は 1 対 1 で突き合わされるので、
   片方だけを外すと生成器が落ちる。**
3. ⭐ **`y` / `n` の規則は `NT-7` が 1 つだけ持つことにした。**
   ⛔ **表 T-036（ショートカット）に行を足さない** ——
   **`NT-8` が `Enter` と `Esc` を自分の行で定めているのと同じ形である。**
   ⚠️ **`NT-8` の消去より後、`IN-4` / `SK-19` の階層より先に置く** ——
   **問いが立っているあいだ、`y` / `n` はほかの何にも当たらない。**
4. ⭐ **表 T-234 の 6 つの文を「…しますか？」の形に書き直した。**
   ⚠️ **`QN-4` と `QN-5` は、条件を述べる文を問いの前に残した** ——
   **「開いたときと同じ文書ではありません」を読まずに `y` を押せてしまうのは、
   問いの形にしたことで生まれる新しい危うさである。**
   ⭐⭐ **利用者の「違和感がある選択があれば裁定させよ」に対して: 6 つとも違和感は無い。**
   ⛔ **ただし 1 つだけ申し送る** —— **`QN-1` / `QN-2` / `QN-3` は取り消しの効かない削除であり、
   一般に「`Yes`」より動詞（「削除する」）のほうが誤答が減るとされる。**
   ⚠️ **利用者の裁定は `[Yes]` / `[No]` なのでそのとおりにした。覆すなら言ってほしい。**

---

## 1. ⛔ 測った —— いま何がどうなっているか

**射程は 2 択の面 1 つだけである**（前の巡が測った）:

```
Confirmation   2 択   ⇒ 本要求が当たる
Open Chooser   3 択   ⇒ 当たらない（表 T-024a の OP-3）
Export Chooser 5 択   ⇒ 当たらない（表 T-024）
```

`IC-69` / `IC-70` を指している所（`impact.py`）:

```
要求        0 件
参照        3 か所 —— A-appendix.md の変更履歴 2 か所と、tbl-glossary.md の U-55
図 F-019    2 図形（丸囲みの ✓ が IC-69、丸囲みの ✕ が IC-70）
辞書        icons 節に 2 項（label と hint）
```

⇒ ⭐ **要求から指されていないので、外しても要求の文は 1 つも動かない。**

---

## 2. ⭐ 書くもの

### 2.1 `NT-7`（続けてよいかを問うとき）に足す

> ⭐⭐ **答えの入口は、図形ではなく語のボタンとすること（MUST）**（利用者の指示 2026-09-01）——
> **`FR-038` の辞書の `confirmation` の語を、どの表示言語でも `Yes` / `No` と綴ること（MUST）。**
> ⛔ **訳してはならない（MUST NOT）** —— **頭文字が下の打鍵を指さなくなる。**
> ⭐ **`NT-8` が消去の入口を「どの表示言語でも `OK`」と定めているのと同じ扱いである。**
> ⭐ **頭の 1 文字（`Y` と `N`）を太字にすること（MUST）** —— **打鍵で答えられることを、
> ボタン自身に名乗らせるためである。**
> ⭐⭐ **`y` と `n` の打鍵でも答えられること（MUST）。** ⛔ **問いが立っているあいだ、
> この 2 つのキーをほかの何にも渡してはならない（MUST NOT）** ——
> **順位は `NT-8` の消去の次、表 T-028 の `IN-4` と 表 T-036 の `SK-19` の階層より先とする（MUST）。**
> ⚠️ **問いの文は、`y` / `n` で答えられる形にすること（MUST）** —— **表 T-234 が持つ。**
> ⛔ **答えの入口に 表 T-109 の行を与えてはならない（MUST NOT）** ——
> **同表と 図 F-019 が持つのは図形の入口であり、語のボタンは図形を持たない。**

### 2.2 表 T-109 と 図 F-019 から 2 行 / 2 図形を外す

| 外すもの | いま | あと |
|---|---|---|
| 表 T-109 の行 | `IC-69` `IC-70` | 無し |
| 図 F-019 の図形 | 89 | **87** |
| 辞書 `icons` の項 | `IC-69` `IC-70` | 無し |
| 用語辞書 `U-55` の参照 | 「2 択の入口は 表 T-109 の `IC-69` / `IC-70` が持つ」 | 「2 択の入口の語は `FR-038` の辞書の `confirmation` が持つ」 |

⛔ **`A-appendix.md` の変更履歴は動かさない** —— **過去に何を決めたかの記録であり、
そこに在る行 ID を消すと、なぜ消したのかが読めなくなる。**

### 2.3 辞書 —— 1 組にする

```
confirmation   proceed → ja: Yes  / en: Yes
               cancel  → ja: No   / en: No
icons          IC-69 / IC-70 の 2 項を削る（同じ語の 2 つ目だったもの）
```

### 2.4 表 T-234 の 6 つの文を、答えられる形に

| 行 | あと（ja） | あと（en） |
|---|---|---|
| `QN-1` | この行と、行に載っているタスクを削除しますか？ 取り消しでは戻せません | Delete this row and the tasks on it? Undo will not bring them back |
| `QN-2` | このタスクと、その配下のタスクを削除しますか？ 取り消しでは戻せません | Delete this task and the tasks under it? Undo will not bring them back |
| `QN-3` | この担当者を削除しますか？ 下のタスクの割り当てが外れます | Delete this resource? It will be unassigned from the tasks below |
| `QN-4` | 開いたときと同じ文書ではありません。上書きしますか？ その間の変更が失われます | This is not the document that was opened. Overwrite it? The changes made since will be lost |
| `QN-5` | 保存していない編集があります。置き換えますか？ 失われます | There are unsaved edits. Replace the document? They will be lost |
| `QN-8` | この操作を続けますか？ 取り消しで戻せない変更が起きます | Go on? This makes a change that undo cannot bring back |

---

## 3. ⭐ 影響

```
induced.py（T-037 NT-7 T-109 IC-69 IC-70 T-234 T-036 SK-19 T-028 IN-4 FR-032 FR-099 FR-031）
  seeds 13 / edges 14 / cycles 1  ——  size 2: FR-031 NT-7
```

⛔⛔ **閉路が 1 つある: `FR-031` と `NT-7`。**⇒ **2 つを 1 つの計画で、1 パスで書く。**
⚠️ **ただし本要求で `FR-031` の文は動かない** —— **同要求が `NT-7` に課しているのは
「問うてよい場面を増やさない」であり、本要求が変えるのは答え方だからである。**
⭐ **動かさないことを、閉路を測ったうえで決めた。**

⚠️ **図形の数が動くので、次のものが同時に動く**（実装の側）:

```
src/adapter/screen-renderer/icon-glyphs.json     89 → 87   （npm run gen）
src/adapter/screen-renderer/icon-roster.json     89 → 87   （同上）
ヘルプの一覧                                     123 entries から 2 減る見込み
tests/unit/uf-62 / uf-65                         名簿の固定の写しを持つ試験
```

⛔ **生成器は 表 T-109 と 図 F-019 を 1 対 1 で突き合わせる。**
**片方だけ外すと `npm run gen` が落ちる** —— **2 つを同じパスで外すこと。**
