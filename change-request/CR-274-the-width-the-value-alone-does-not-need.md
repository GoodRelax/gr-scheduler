# CR-274 — 値のぶんだけ広げても、日付は見切れたままだった

> **閉じるもの**: ⛔ **利用者の指摘 1 件**（`D-58`。プロパティパネルの `start` / `finish` が見切れる）。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作った人の意図どおりに読める）。**⛔ **日付が見切れているパネルは、値を読ませるという役目を果たしていない。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 入力欄の幅 | ⛔⛔ **「操作子が値のほかに何を要するか」を述べる行が 1 つも無い。**⭐ **`FR-093` は概算の式を持つが、それは**ラベル**の幅であって入力欄ではない |
| **`R2.19`**（驚かせない）| 前に立つ者の前提 | ⛔ **覆った** —— 「`FR-093` の概算を満たせばよい」は誤り。**実測: 日付欄は 70.27px あり、概算の 56.0px を既に満たしている。それでも見切れている** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| `labelCoef` の継ぎ目 | ⛔ **`labelCoef`（`S-30`）は文書の設定であり、パネルを描く側は文書を読まない**（表 T-061）。**概算する側が求めて渡すほかに道が無い** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`S-199` の既定を 26px とした** | ⚠️ **参考実装から逆算した値である**（値に 70.27px を与えて全桁が入らない）。🔎 を付けてある。⛔ **測って選び直す義務は残る** |
| 2 | ⭐ **足りないときは行を折り返す**（詰めるのでも縮めるのでもなく）| ⛔ **文字を縮めて収めると、`S-197` / `S-198` で決めた大きさをこの規則が上書きすることになり、パネルだけが読めない字で描かれる** |
| 3 | ⭐ **概算する側が求めて操作子と一緒に渡す**（案 A の (i)）| ⛔ **描く側に係数を持たせる (ii) は採らなかった** —— **同じ概算が 2 か所に現れ、`FR-093` の「係数の 1 値がレイアウト精度を単独で決める」が成り立たなくなる** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 1 つの日付欄の幅 | **70.27px** |
| `FR-093` の概算（`start` の値）| **56.0px** ⇒ ⛔ **既に満たしている。それでも見切れる** |
| ⇒ 足りないもの | **枠・内側の余白・宿主が `date` に付ける暦を開く釦が取るぶん** |
| 値の欄の字の大きさ | **11.9px**（`S-197` 適用後）／ 項目名は **10.71px**（`S-198` = 0.90）|
| `labelCoef` の在処 | ⛔ **`S-30`。文書の設定であり、`dom-screen-surface.ts` は文書を読まない**（表 T-061） |
| 空いている設定値の ID | **`S-199`**（`S-198` が最大。`S-200` は `CR-273` が使う）|
| `induced.py`（`FR-006` `T-016` `T-206` `FR-093` `S-30` `S-197` `S-198`）| **閉路 1 つ: `FR-006` ＋ `S-197` ＋ `S-198`（大きさ 3）。**⇒ ⭐ **`FR-006` は 1 度に書いた。`S-197` / `S-198` は 1 文字も動かしていない** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `_source/settings.json` の 表 T-206 | ⭐ **`S-199`** ——「入力欄が、値のほかに要る幅（`FR-006`）」。`S-198` の直後 |
| 2 | `FR-006` に MUST NOT | ⭐ **要る幅 ＝ `FR-093` の概算 ＋ `S-199`。それより狭い幅を割ってはならない** |
| 3 | 同上 | ⭐ **収まらないときは行を折り返す（MUST）。文字を縮めて収めてはならない（MUST NOT）** |
| 4 | 同上 | ⭐ **継ぎ目**: 概算する側が `labelCoef` を求めて渡す（MUST）。描く側が自前の係数を持ってはならない（MUST NOT）|
| 5 | `A-appendix` の変更履歴 | 1 行（`1.26`）|

⛔ **規則は 表 T-206 に書かない** —— **検査 12 が値の表の MUST を拒む**（`CR-272` で 1 度踏んだ）。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1717 | **1718** | **+1**（`S-199`）|
| `uids` | 148 | **148** | 0 |
| 語の数 | 257 | **257** | 0（画面に刷る語を足していない）|

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `uids` | 135 / 11 / 148 | **135 / 11 / 148** | ✅ |
| `rows` | 1718 | **1718** | ✅ |
| 語 | 257 | **257 word(s)** | ✅ |
| 検査 22 本 | 全緑 | **ALL GREEN** | ✅ |

### ⚠️ 予測から外れて、直しが 1 つ要った

| 何が落ちたか | なぜ | どう直したか |
|---|---|---|
| **検査 9 が 1 件**（`01-04-requirements.md:1414`）| ⛔ **「表 T-016 の 1 行が持つ操作子」と書いたら、検査が「表 T-016 は 1 行だと述べている」と読んだ**（同表は 16 行）| ⭐ **「同じ行に並ぶ操作子」へ言い換え、例として `PR-3` / `PR-12` を名指しした。**規則は 1 文字も変わっていない |

⛔ **次に書く者へ**: **`表 T-nnn の <数> 行` は、たとえ「1 行」でも員数の主張として読まれる。**

## 5. 標準規則への答え

- **①（目的に沿う）**: ⭐ `CH-1`。**値が読めないパネルは役目を果たしていない。**
- **②（影響をグラフで測ってから書く）**: ⭐ `induced.py` を先に走らせ、`FR-006` の閉路を 1 度に書いた。
- **⑧（実測を書く）**: ⭐ **§1 が実測である。**⛔ **前に立つ者の当初の前提が覆ったことも §0 に残した。**

## 6. 残るもの

⛔ **本変更要求は仕様だけである。**⭐ **実装が要るもの**: `properties-panel.ts` が `labelCoef` から概算して
`PropertyControl` の任意の項で運ぶこと（`choiceValues?` と同じ形）と、`dom-screen-surface.ts` がその幅で並べ、
入らない行を折り返すこと。
