# CR-209 — 6 つの呼び手と、それぞれが残すもの

> **閉じるもの**: ⭐ **利用者の裁定（2026-08-22。`docs/review/rulings-2026-08-22/01-whole-document-write-path.md` の 7 問）** ——
> ⭐ **`replaceDocument`（表 T-064 の `PI-8`）が、呼び手ごとに履歴・刻印・取り消しの 1 段をどう扱うかを 表 T-230 に置く。**
> ⭐ **`UndoEdit` は呼ばれる側である** —— コンポーネントの図（図 F-014）が正であり、`CR-196` の記述は誤りだった。⛔ **過去の変更要求は 1 文字も書き換えない。**
> ⛔ **`_source/components.json` の辺は 1 本も動かない** —— **裁定 `Q0` がそのグラフを正としたためである。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作図ソフトと同じ操作感で描きながら、構造化した日程データを出す）。受ける目標は `GL-004`（出し入れできること）。**

⛔ **いま、ファイルを開くことができない。** ⚠️ **押しても何も起きないのではなく、そもそも押せない** ——
**計算した文書を現在値にする手段が公開されていないため、ファイルを開く・自動保存から戻る・起動時の文書・取り込みの 4 経路が 1 つも通らない。**
⭐ **`GL-004` の「出し入れ」のうち「入れ」がまるごと塞がっている。**

⭐ **`CH-4`（説明を読まずに操作できる）にも掛かる。** ⚠️ **取り消しは動くが、唯一の書き込み経路を迂回しているので通知が出ない**（`AG-6` 違反）。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.3`**（唯一の正がある）| コンポーネントの図 ／ `CR-196` ／ いまのコードの 3 者 | ⛔ **本物の違反。** ⭐ **グラフは `ApplyDocumentChange → UndoEdit`（step back）を描き、`CR-196` は取り消しを `replaceDocument` の呼び手と書き、コードはシェルが自分で対を置き換えている。** ⚠️ **`LR-3`（層の中の呼び出しを非巡回に保つこと）により、前 2 者は両立しない** |
| **`R1.3`**（同上）| 裁定 `Q1` の「6 呼び手すべて」と 表 T-027 の `UN-6` | ⛔ **本物の違反が 1 つ残っていた。** ⚠️ **合流の結果は現在の文書から組まれるので、「入ってきた刻印をそのまま持つ」と刻印が動かず、`FR-063` の MUST ／ `AG-2` の楽観ロック ／ `AG-6` の通知の 3 つが同時に破れる。** ⭐ **利用者に問い、「取り込みだけ進める」の裁定を受けた**（本 CR の §2） |
| **`R1.4`**（境界値の明示）| 表 T-067 の 7 つの順 | ⛔ **本物の違反。** ⭐ **`WS-1` `WS-3` `WS-4` `WS-5` の 4 つが、まるごと差し替える道に当てると答えを持たない** —— 素直に読むと**あらゆる差し替えが `WS-1` で拒否され**、**取り消しが自分の歩いている履歴に段を積む** |
| **`R1.2`**（曖昧表現の排除）| 「外で組み立てた文書」 | ⛔ **「外」がどこかを述べていなかった。** ⭐ **グラフが答えを持つ** —— `ApplyDocumentChange` の外、すなわち `ImportDocument` / `UndoEdit` / `RedoEdit` である |
| **`R2.19`**（コンポーネント境界）| `undoEdit` の純粋性 | ⭐ **裏づけとして効いた。** **`undoEdit` は `pure`（表 T-064 の前文と 表 T-075 の両方）であり、純粋関数は現在値を据え付けられない。** ⛔ **据え付けるのは `non-pure` な `replaceDocument` だけである** |
| **`R2.9`**（YAGNI）| 新しい語 | ⭐ **1 つも作らない。** ⛔ **`commitDocument` / `settleDocument` は `CR-196` が明文で退けた**（仕様書に無い語）。**本 CR が足すのは、仕様が既に数え上げた 6 つの呼び手だけである** |
| **`R2.7`**（DRY）| 書く場所 | ⭐ **規則は 表 T-230 の 1 か所に置き、`WS-5` からは指すだけにする。** ⛔ **2 か所に書けば片方が腐る**（規則 02 の 4.）|
| **`R1.1`**（要求に ID）| 表 T-006b の `A-1` | ⛔ **付随して本物の違反が 2 件出た。** ⭐ **`A-1` は「段」に 7 義あるとして「必ずどれかを併記する。『N 段』と単独で書かない」と定めるのに、`PI-8` と `NF-2` が裸で使っている**（裁定 01 の §7 の 3） |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **新しい表を立て、表 T-067 に列を足さなかった** | ⛔ **列を足すと 7 つの順すべてが 6 つの呼び手の欄を持つことになり、命令の道にとって空の欄が 30 個できる。** ⭐ **違うのは 3 つの段だけである**（`WS-1` と `WS-3` は読み替え、`WS-2` `WS-6` `WS-7` はそのまま効く）|
| 2 | ⭐ **行 ID の接頭辞を `RD-` にした** | **`replaceDocument` の頭字である。** ⚠️ **`RP-` は既に使われている**（自分で数えた。使用済みの接頭辞は 89 種）|
| 3 | ⭐ **起動時の文書の「問う先」を「呼び手が持って来る」にした** | ⛔ **裁定 01 の §3 の表は `ChooseStartupDocument` と書いているが、コンポーネントの図の辺は `SingleHtmlShell → ChooseStartupDocument` である。** ⚠️ **`ApplyDocumentChange → ChooseStartupDocument` の辺は存在しない。** ⭐ **`Q0` がグラフを正としたので、グラフに合わせた** —— **辺を足すのは裁定の外である** |
| 4 | ⭐ **「取り込み」の行に合流と重ねの両方を入れた** | **どちらも `ImportDocument` が答え、どちらも現在の文書を捨てない。** ⚠️ **`baselineTasks` は日程データの群の 12 の鍵の 1 つである**（表 T-052 の `DR-2`）ので、**重ねも日程データの群を動かす** |
| 5 | ⭐ **取り消しの 1 段の欄を、取り込みの行だけ「表 T-027 に従う」にした** | ⛔ **合流は `UN-6` で対象、重ねは 表 T-027 に行が無いので対象外である。** ⚠️ **本表に書き写すと、表 T-027 に行が足されたときにここが腐る** |
| 6 | ⭐ **`PI-8` と `NF-2` の「段」を直した** | ⛔ **`A-1` の違反である**（裁定 01 の §7 が名指している）。⚠️ **`PI-8` のセルは本 CR がどのみち書き換えるので、片方だけ直すと双子の片割れが壊れたまま残る** |
| 7 | ⛔ **`pending-decisions.md` に 7 行を遡って足さない** | ⭐ **裁定の記録は `docs/review/rulings-2026-08-22/` が持つ。** ⚠️ **当時考えていなかった理由を後から書き足すのは、穴を見えなくするほうへ働く**（検査 22 の docstring が同じ理由で `CR-175` より前を見ない）|
| 8 | ⛔ **`FrameLoop.replaceDocument` の名前の衝突を本 CR で扱わない** | ⚠️ **シェル側の同名メンバは「履歴を引き継ぐ」と自分で書いており、`PI-8` の同名メンバと意味が違う**（裁定 01 の §7 の 5）。⭐ **本 CR は仕様側を確定させるだけであり、`src/` の改名は実装の回である** |

---

## 1. 測った（⛔ 2 本とも走らせた）

```
impact.py  T-067
impact.py  T-064
impact.py  T-035
impact.py  FR-063
induced.py T-067 WS-1 WS-4 WS-5 AG-2 AG-6 AG-11 FR-063 T-064 PI-8 T-027 UN-6 OP-4 FR-031 FR-087
```

| 測ったこと | 実測 |
|---|---|
| 種が仕様に解決したか | **15 / 15**（空のグラフではない）|
| 種の集合の内側の辺 | **16** |
| 閉路 | ⛔ **1 件（大きさ 5）** —— `AG-11` `AG-2` `AG-6` `FR-063` `WS-5` |
| 表 T-067 を指している要求 | **2 件**（`FR-088` `FR-064`）。2 次は 4 件（`FR-031` `FR-054` `FR-074` `FR-076`）|
| 表 T-067 の行数 | **7 行。** ⛔ **1 行も足さない** |
| 表 T-064 を指している要求 | ⚠️ **0 件**（表が要求から浮いている）。指しているのは 5.3 / 5.5 / 5.6 / 6.1 / A.3 / 表 T-107 の散文だけ |
| `FR-063` を指している箇所 | **要求 6 件 ／ 参照 24 箇所** |
| 表 T-035 を指している要求 | **5 件**（`FR-008` `FR-031` `FR-063` `FR-064` `FR-040`）。2 次は 10 件 |

⛔ **閉路の 5 つのうち 2 つを書く**（`AG-6` と `FR-063`）。⚠️ **`WS-5` も同じ閉路に在る。**
⭐ **したがって `WS-5` ／ `AG-6` ／ `FR-063` は 1 つの計画で 1 度に書く**（規則 02 の 3.）。
⭐ **`AG-2` と `AG-11` は読み直して真のままであることを確かめる** —— **書き換えは要らない**（§4 の 1 に理由）。

---

## 2. 何が食い違っていたか（**逐語**）

### 2.1 3 つの成果物が違うことを言っていた

| | 何と言っているか | どこ |
|---|---|---|
| **(1) コンポーネントの図** | `ApplyDocumentChange` が `UndoEdit` を**呼ぶ**（`step back`）| `_source/components.json`、図 F-014 |
| **(2) `CR-196`** | 取り消し・やり直しは `replaceDocument` の**呼び手**である | `CR-196` の §0① と §4 |
| **(3) いまのコード** | シェルが `undoEdit` を呼び、**自分で**対を置き換える | `frame-loop.ts` の `carryOutAction` |

⭐ **裁定 `Q0` は (1) を正とした。** ⛔ **したがって仕様側で確定させるのは「経路が問う」形である。**

コードが自分でそう告白している（`frame-loop.ts:790`、逐語）:

```
⛔ NOT THROUGH THE ONE WRITE PATH ... the pair is replaced here -- and MS-1 of table T-042
   calls that the second entrance it forbids.
```

### 2.2 表 T-067 の 4 つの順が、まるごと差し替える道に答えを持たない

| 順 | 命令の道 | ⛔ まるごと差し替えるとき |
|---|---|---|
| `WS-1` 刻印を照合 | 読んだ刻印と現在の刻印を比べる | ⛔ **何と比べるのか。** 入ってくる文書の刻印は**定義により**現在のものと違い、素直に読むと**あらゆる差し替えが拒否される** |
| `WS-3` 検証し組み立てる | `editDocument` が組み立てる | ⛔ **組み立てるものが無い。** ⭐ **グラフは答えを持つ** —— `ImportDocument` / `UndoEdit` / `RedoEdit` がこの位置に立つ |
| `WS-4` 取り消しの 1 段を積む | 表 T-027 の対象なら積む | ⛔ **表 T-027 の 17 行に、取り消し・やり直し・復帰・起動・置き換えの行が 1 つも無い。** 素直に読むと**取り消しが自分の歩いている履歴に段を積む** |
| `WS-5` 刻印を進める | 「必ず更新する」 | ⛔ **`FR-063` が逆を書いている** ——「取り消しは以前の文書を**刻印ごと復元する**」 |

⭐ **`WS-2` `WS-6` `WS-7` はそのまま効く。**

### 2.3 ⛔ 裁定の `Q1` だけが、6 呼び手のうち 1 つと噛み合わなかった

⚠️ **裁定 `Q1` は「入ってきた刻印をそのまま持つ（6 呼び手すべて）」であった。**
⛔ **合流だけは、そのまま持つと 3 つの MUST が同時に破れる** —— **合流の結果は現在の文書から組まれるので、入ってきた刻印はいまの刻印そのものである:**

```
① FR-063   「文書が更新されたとき…必ず更新すること（MUST）」
② AG-2     合流の前に読んだ呼び手が、そのまま書き込めてしまう（楽観ロックが利かない）
③ AG-6     裁定 Q6 の判定（前後の scheduleUpdatedUtc の等値）が「日程は動いていない」と答え、
           合流が 1 人にも配られない
```

⭐ **利用者に問い、「取り込みだけ進める」の裁定を受けた（2026-08-22）。**
⚠️ **これは裁定 01 の §3 の表が既に測っていたとおりである**（合流: 刻印 進める ／ 段 積む）。

---

## 3. 編集計画（**オブジェクトごとに 1 回**）

⛔ **4 と 5 と 6 は同じ閉路に在るので、1 度に書く。**

| # | ファイル | オブジェクト | 何をするか |
|---|---|---|---|
| 1 | `05-07-design.md` | 5.5 の冒頭の 1 文 | 表の並びに 表 T-230 を足す |
| 2 | `05-07-design.md` | **表 T-230（新設）** | 6 行（`RD-1` 〜 `RD-6`）と、その前後の散文。`WS-1` `WS-3` `WS-4` の読み替えと、検証し直さないことを MUST で書く |
| 3 | `05-07-design.md` | 表 T-064 の `PI-8` のセル | `replaceDocument` の説明から「段は」を外して「手順は」とし、表 T-230 を足す |
| 4 | `05-07-design.md` | 表 T-067 の `WS-5` のセル | ⭐ **但し書きを 1 文**（まるごと差し替える道では 表 T-230 の刻印の欄に従う）|
| 5 | `01-04-requirements.md` | 表 T-035 の `AG-6` のセル | ⭐ **括弧に 1 文**（まるごと差し替える道での判定は 表 T-230 が持つ）|
| 6 | `01-04-requirements.md` | `FR-063` の 2 段落目 | ⭐ **1 文**（まるごと差し替えは本要求の「更新」ではない。刻印は書かれたときのものが残る）|
| 7 | `05-07-design.md` | 表 T-072 の `NF-2` のセル | 「7 段 / 5 段」を `A-1` に沿って直す |
| 8 | `A-appendix.md` | 変更履歴 | 1 行足す（版 0.86）|

⛔ **触らないもの**（**閉じたと読まれないために明示する**）:

```
_source/components.json   辺は 1 本も動かない（Q0 がグラフを正とした）
表 T-027                  行を 1 つも足さない（Q5。歩くこと自体が対象外）
表 T-078                  行を 1 つも足さない（着地は WS-6 なので FT-2 が覆う。裁定 02）
表 T-064 の PI-11 / PI-12 undoEdit / redoEdit の欄は 1 文字も動かない
CR-196                    過去の変更要求は書き換えない
src/                      本 CR は仕様だけを動かす
```

---

## 4. 数の予測

| | 前 | 予測 |
|---|--:|--:|
| `tables` | 129 | ⭐ **130**（表 T-230）|
| `figures` | 11 | **11** |
| `rows` | 1585 | ⭐ **1591**（`RD-1` 〜 `RD-6` の 6 行）|
| `uids` | 146 | **146** |
| 裁定待ちの行 | 60（55 未裁定）| **60（55 未裁定）**。⛔ **遡って足さない**（§0 の ⑧ の 7）|
| 生成物 | 16 | **16** |
| コンポーネントの図の辺 | 89 | **89** |
| 重複検出 | `A=26 (new 0)` | **`A=26 (new 0)`** |
| 検査 26b の baseline | 2 件 | **2 件**。⚠️ **`PI-8` の行は実装が書くまで閉じない** |

---

## 5. 試験（**期待値を曲げない**）

⛔ **本 CR は仕様だけを動かすので、緑になる試験は 1 つも増えない。**
⭐ **実装の回で書かれるべき試験を、いま名指しておく**（規則 06 の「覆したとき落ちる試験」）:

```
1. 6 つの呼び手それぞれで、履歴・刻印・取り消しの 1 段が 表 T-230 のとおりになる試験
   ⭐ 原稿の行を 1 つ変えると落ちる形にする（表を写した固定データで駆動する。仕様書 1.9）
2. ⛔ 取り消しを N 回押して N 回とも WS-7 の通知が出る試験
   ⚠️ いまは第 2 の入口を通っているので 1 度も出ない
3. ⛔ 申告しない呼び手が WS-1 で拒否されない試験
4. ⛔ 合流の後、合流前に読んだ刻印での書き込みが AG-2 で拒否される試験
   ⭐ 本 CR の §2.3 が閉じた穴は、これが赤で出る形でしか守られない
```

⚠️ **書くのは実装した者ではない**（規則 04 の 1.）。

---

## 6. ⛔ 本 CR が閉じないもの（**黙って閉じたと読まれないために**）

| | 何 | どこへ |
|---|---|---|
| 1 | ⛔ **非同期の答えがフレームへどう戻るか** | ⭐ **`CR-210`**（裁定 02）。⛔ **閉じないと「ファイルを開く」は通らない** |
| 2 | ⛔ **形式の判別器・確認の返り道** | ⭐ **`CR-211`**（裁定 03）|
| 3 | ⛔ **`PI-8` の `replaceDocument` を `src/` が 1 度も書いていないこと** | ⚠️ **検査 26b の baseline に負債として立っている。** ⭐ **本 CR を当てた後の実装で閉じる** |
| 4 | ⚠️ **`PI-3` の `advancedStamp（版を進める）` が「版」のまま** | **`CR-205` が 18 か所を「刻」へ書き換えたが、ここと `CP-3` が残っている**（裁定 01 の §7 の 4）|
| 5 | ⚠️ **`FrameLoop.replaceDocument` が `PI-8` と同名で別物** | ⭐ **`src/` の改名であり、仕様側の作業ではない**（§0 の ⑧ の 8）|
| 6 | ⛔ **`carryOutAction` の取り消し・やり直しの枝が試験 0 件** | ⭐ **表 T-218 の `TS-1`（tests/usecase）か `TS-3`（Playwright）で受ける** |
