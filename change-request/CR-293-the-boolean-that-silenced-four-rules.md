# CR-293 — 4 つの規則を黙らせていた 1 つの真偽値

> **閉じるもの**: ⛔ **台帳 D-89**（`isTextEntryUnsettled` が `false` に固定）と **台帳 D-117**
> （利用者の指摘:「**プロパティーパネルでタスク名や備考のテキストを編集中にコピペができない。
> コピペできるようにしろ**」）。
> ⭐⭐ **利用者の裁定（2026-08-30）: 継ぎ目を広げる。**`ScreenSurface`（`IF-9`）に
> 「入力欄が焦点を持っているか」を答える欄を足す。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`（マニュアルを読まずに使える）。**
⛔ **文字が打てるのに複製だけできないものは、入力欄として壊れている。**
⚠️ **同じ 1 つの欠落が `Delete`・`Esc`・`Agent API` の 3 つも黙らせていた。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 表 T-065 の `IF-9` と `screen-surface.ts` | ⛔⛔ **`IF-9` の欄は 5 つの供給を名乗るのに、宣言は 4 つしかメンバを持っていなかった**（`CR-270` が欄に足した「**まだ確定していない文字入力があるかを答え**」に対応するメンバが無い）|
| **`R2.7`**（同じ問いを 2 か所で解かない）| `isTextEntryUnsettled` / `editingInPlace` / `isEditingInPlace` | ⭐ **3 か所とも 1 つの答えから引く形にした**（`hasUnsettledTextEntry`）|
| **`R2.19`**（驚かせない）| 入力欄での `Delete` | ⛔⛔ **実測: タスク名の欄に焦点があるまま `Delete` を押すと、文字ではなくタスクが消えた**（下の「1. 測った」）|
| **`R3.1`**（同じものを 2 か所に置かない）| `isFieldHeld` と新しい欄 | ⚠️ **2 つの値に分けた** —— `isFieldHeld` は**再描画**を止めるための旗で、選択肢やチェックボックスでも立つ。`IN-5a` が問うのは**文字**であり、チェックボックスに消す文字は無い |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **メンバの名は `hasUnsettledTextEntry`、返すのは真偽 1 つ** | ⭐ **`CR-146` が `src/` の公開エントリを署名の正としている** —— 表 T-065 は「何を供給するか」だけを持ち、名前と署名は書かない。⛔ **どの欄が持っているかは返さない（MUST NOT。表 T-065、利用者の裁定 2026-08-27）** |
| 2 | ⭐ **答えるのは「文字を打ち込む種類の操作子」に焦点があるときだけ** —— `text` / `multiline` / `date` / `number` は真、`boolean` / `choice` / `color` は偽 | ⭐ **`IN-5a` 自身の理由が線を引いている** ——「`SK-3`（選択の削除）と 1 文字消す操作が同じキーに重なる」。⛔ **チェックボックスと選択肢には消す文字が無く、そこで `Delete` を飲むと `SK-3` を理由なく奪う** |
| 3 | ⭐ **`IN-4` の第 1 階層は、描いた側（`DomScreenSurface`）が果たす** | ⭐ **`escapeTarget`（`PI-36`）は既に `'textEntry'` を答えており、それを読むシェルは欄を持たない**（`LR-6`）。⛔ **`'gesture'` / `'confirmation'` と同じ分担である** —— 梯子が階層を名指し、その保持者が果たす |
| 4 | ⛔ **`Esc` の 1 回目では焦点を放さない** | ⛔⛔ **実測して決めた** —— この聴き手はシェルより**先**に走るので、ここで旗を下ろすとシェルはその値を読み、**同じ 1 押しでパネルまで閉じた**（`IN-4` は 1 階層、MUST）。⭐ **1 回目は値を戻して階層を使い切り、2 回目が下の階層へ進む** |
| 5 | ⚠️ **`WS-2` は「確定そのものである書き込み」だけを免れさせる** | ⭐ **`spendFieldCommit` が走る時点で人はまだ欄を握っている**（`Enter` で確定しても焦点は残る）。⛔ **免除しないと `FR-031` が書くはずの値そのものが拒まれ、`readFieldCommit` が既に取り上げているので値ごと消える。**⭐ **身振りが同じ形を先に持っている** —— 押下はシェルが書き込みの**前**に落とす（「身振りは実際に終わっている」から） |
| 6 | ⛔ **`Ctrl`＋`A` は触っていない** | ⛔⛔ **`IN-5a` の例外に挙がっているのは `SK-4` / `SK-5`（`Ctrl+C` / `Ctrl+V`）だけである**（逐語で確認）。⭐ **発明せず、`PD-349` として裁定待ちに載せた** |
| 7 | ⛔ **対話欄（`Dialogue Field`）の入力は答えに入れていない** | ⭐ **`AG-9` が名指すのは「プロパティパネルで入力中など」、`IN-5a` が数えるのは名称・担当者名・行名・文書名・注記の本文** —— どれもこのパネルのものである。⚠️ **`PD-350` として載せた** |

---

## 1. ⛔ 測った（**実物。`dist/index.html` を playwright で**）

| 測ったこと | 措置前 | 措置後 |
|---|---|---|
| 欄を選び `Ctrl`＋`C` → `End` → `Ctrl`＋`V` | ⛔ **`Survey phase` のまま**（何も起きない）| ⭐ **`Survey phaseSurvey phase`** |
| 欄に焦点があるまま `Backspace` | ⛔ **文字が減らない**（`SK-3` が飲む）| ⭐ **1 文字減る** |
| 欄に焦点があるまま `Delete` を 2 回 | ⛔⛔ **タスクが消えた** —— 図形 19 → 16、`Survey phase` が画面から消滅 | ⭐ **図形 19 のまま、タスクは残る** |
| 欄で `P` を含む文字列を打つ | ⛔ **`RenamedByProbe` が `RenamedByrobe` になる**（`SK-14` が `P` を奪う）| ⭐ **`RenamedByProbe`** |
| 打ちかけで `Esc`（1 回目）| ⛔ **編集は取り消されず（`Survey phXYZase` のまま）、パネルが閉じた** | ⭐ **値が `Survey phase` へ戻り、パネルは立ったまま、焦点も残る** |
| 続けて `Esc`（2 回目）| — | ⭐ **パネルが閉じる**（1 押し 1 階層）|
| `Agent API` の `exportSvg`（入力中）| ⛔ **`ok: true`** —— `AG-9` の後半が効いていない | ⭐ **`ok: false`、`reason: editingInPlace`** |
| `Agent API` の `applyCommands`（入力中）| ⛔ **`WS-2` を素通り** | ⭐ **`reason: editingInPlace`** |
| `Agent API` の `applyCommands`（入力していない）| — | ⭐ **`WS-2` は通る**（`WS-3` まで進む）|

⚠️ **`Ctrl`＋`A` は前も後も選択が動かない** —— `SK-2` が `CONSUMED_ELSEWHERE` を返してブラウザの既定動作を止めており、
`IN-5a` の例外に入っていないので、この巡では**触っていない**（`PD-349`）。

⛔ **この巡で直っていないもの（別件、措置前から同じ）**: **プロパティパネルで確定した名称が文書へ届かない。**
措置前・措置後とも、欄を離れても画面の `Survey phase` は変わらない。**`WS-2` の拒否ではない**（知らせは 0 件、
`Agent API` を止めた状態でも同じ）。**本 CR の範囲外として報告する。**

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `src/adapter/screen-renderer/screen-surface.ts` | ⭐ **`ScreenSurface` に 5 つ目のメンバ `hasUnsettledTextEntry(): boolean`** —— `IF-9` の 4 番目の供給に 1 つ |
| 2 | `src/framework/dom-screen-surface/dom-screen-surface.ts` | ⭐ 文字を打つ操作子の焦点を `focusin` / `focusout` で持ち、`Esc` で `IN-4` の第 1 階層を果たす |
| 3 | `src/framework/single-html-shell/frame-loop.ts` | ⭐ 固定 `false` を 3 か所とも置き換える（`InputContext` ／ `WriteMoment` ／ `AgentSnapshot`）|
| 4 | 試験 8 件の雛形 | ⚠️ **型が広がった代償の 1 行**（`hasUnsettledTextEntry: () => false`）|
| 5 | `tests/unit/uf-71.test.ts` ／ `uf-72-screen-part.test.ts` | ⛔ **`IF-9` の供給を数え上げる 3 件が 4 つで固まっていた。**⚠️ **仕様の欄は 5 つを名乗る**（`CR-270` 以来）ので、**名簿に 5 つ目を足し、引用も欄の逐語へそろえた。期待値は緩めていない** |

⛔ **`docs/spec/` は 1 バイトも触らない。**
⭐ **`IF-9` の欄は `CR-270`（2026-08-27）が既に「まだ確定していない文字入力があるかを答え」を持っており、
足りなかったのは実装だけである。**
⭐ **表 T-064 も動かない** —— `ScreenSurface` は既に `PI-37` / `PI-38` に載っており、
**足したのはインターフェースのメンバであって、コンポーネントの公開メンバではない**（検査 26b が新規 0 で確かめる）。
⛔ **`A-appendix.md` の変更履歴にも行を足さない** —— **仕様書は 1 文字も動いていないので、版を上げるのは偽になる。**

## 3. 数の予測

```
tables   135 → 135
figures   11 →  11
rows    1708 → 1708
uids     150 →  150
```

⚠️ **1 つでも動いたら、仕様書を触った証拠である**（本 CR は触らない）。

## 4. 落ちる／緑になる試験

| 試験 | 予測 |
|---|---|
| `uf-71` の「`IF-9` の供給ぶんだけメンバを返す」2 件 | ⛔ **赤くなる** —— 名簿が 4 つで固まっている。**5 つ目を足す** |
| `uf-72-screen-part` の同種 1 件 | ⛔ **赤くなる。**同じく 5 つ目を足す |
| 偽の面を作る 8 ファイル | ⛔ **型が通らなくなる。**1 行ずつ足す |
| `uf-48-write-moment`（`WS-2`）| ⭐ **緑のまま** —— 偽の面は `false` を答える |
| `in-4-escape-closes-the-panel` | ⭐ **緑のまま** —— 同上、入力中でない場合の梯子は動かない |
| そのほか 4670 件 | ⭐ **緑のまま** |

## 5. 裁定待ちへ送ったもの

| PD | 何が未決か | 分類 |
|---|---|---|
| **PD-349** | **`Ctrl`＋`A` を入力中にブラウザへ渡すか** —— `IN-5a` の例外は `SK-4` / `SK-5` だけ | **F** |
| **PD-350** | **対話欄の打ちかけを `IN-5a` / `WS-2` が数えるか** | **F** |
