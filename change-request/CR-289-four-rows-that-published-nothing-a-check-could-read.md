# CR-289 — 検査が読めない名で公開していた 4 行

> **閉じるもの**: ⛔ **台帳 D-121（前に立つ者の実測 2026-08-29）。**
> ⭐ **同じ形が 4 行あったので、まとめて直した** —— 台帳の補足がそう指示している。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⛔ **`CH-1` 〜 `CH-5` のどれも、この変更が直接に進めるものではない。**⭐ **検査の仕事である** —— `GL-001` 〜 `GL-007` のどれを追うにしても、**仕様書が名乗らない名を機械は確かめられない。**
⛔⛔ **「`FileStore` の実装 1 つ」は、公開する名を 1 つも名乗っていない。**
⇒ **検査 26b は読めるものが無いので飛ばし、その越境名は借りとして黙らされる。**
⚠️ **借りの一覧は、それ自体が「見ていない」の記録である。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 表 T-064 の `Framework` の行 | ⛔ **`PI-26` `PI-27` `PI-28` `PI-31` の 4 行が、実装した interface の名しか書いていない** |
| **`R3.4`**（同じものを 2 か所に置かない）| 借りの一覧 | ⭐ **借りを増やさず減らした** —— 行が名を名乗った瞬間、3 本の借りが「払い済み」になり、検査自身がそう言って落ちた |
| **`R2.19`**（驚かせない）| `PI-30` との対称 | ⛔ **`PI-30` だけが CR-281 で直っており、同じ形の 4 行が取り残されていた** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`PI-30` と同じ書き方に揃えた**（`名前`（`Interface` の実装 1 つを返す））| ⭐ **CR-281 が先に選んだ形である。**新しい書き方を発明していない |
| 2 | ⛔ **`PI-25` と `PI-38` は触らない** | ⭐ **`PI-25` は「呼ばれるメンバを持たない」と自ら宣言しており、飛ばされるのが正しい。**`PI-38` は既に 2 つの名を名乗っている |

---

## 1. ⛔ 測った

| 測ったこと | 措置前 | 措置後 |
|---|--:|--:|
| 検査 26b が読んだメンバ | **108** | ⭐ **112** |
| 検査 26b が飛ばした片 | **11** | ⭐ **7** |
| 借りの一覧が黙らせている越境名 | **140** | ⭐ **137** |
| 検査が「払い済み」と言って落ちた借り | —— | ⛔ **3 本**（`domInputSource` ／ `domSvgSurface` ／ `fileSystemAccessFileStore`）|

⭐ **実際に増えた検査は 4 つである** —— 4 行が名を名乗ったので、その 4 つの名が公開エントリに在ることを機械が確かめるようになった。
⚠️ **`canvasRasterizer` は借りの一覧に無かった** —— 越境していなかったからであり、行が名乗ったこと自体は同じ利得である。

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-064 の `PI-26` `PI-27` `PI-28` `PI-31` | ⭐ 公開する名を名乗らせる（`PI-30` と同じ形）|
| 2 | `crossing-names-baseline.txt` | ⭐ **払い済みの 3 本を消す** —— 検査自身が「消さないと借りが許可に腐る」と言う |
| 3 | `A-appendix.md` | 変更履歴に 1 行 |

⛔ **`src/` は 1 文字も触らない** —— 名は既にその形で公開されている。**足りなかったのは仕様書のほうである。**

## 3. 数の予測

```
tables   135 → 135
figures   11 →  11
rows    1704 → 1704   （行を足さない。4 行の中身を厚くするだけ）
uids     149 →  149
```

## 4. 落ちる／緑になる試験

| 試験 | 予測 |
|---|---|
| 検査 26b | ⭐ **一度赤くなり**（払い済みの借りを 3 本告げる）、**消せば緑**。⭐ **それが設計どおりの挙動である** |
| `units.contract` ／ `seams.contract` | ⭐ 動かない（表の行数は変わらない）|
