# CR-430 — 掴み代・応える順・ポインタ・札・押す引く・描く順・依存線を、9 つの表と 7 つの上位要求で持つ

> ⭐ **状態: 適用済（2026-09-20）。** 仕様書へ当て終えた（ブランチ `grab-area-spec`、作業木に未コミットで置いてある）。当てた体が CR の文から変えた所は 12 節にある。
> 読んだ木: `3d342753`（ブランチ `mspdi-both-versions`、`CR-429` が着地した commit）。**行番号はすべてこの木のものである。**
>
> **閉じるもの**: 表 HT を 2 つに割る裁定（利用者、2026-09-20、逐語）。⚠️ 裁定の台帳（`docs/development-records/rulings.md`）へは本書を当てるときに写す（6.2 節）—— 起草の時点では別の巡の裁定が同じ台帳に書かれているためである。
> - 利用者の問い: 「同じPrefixを使う理由は？ 異なるものに同じIDを振るのは絶対にダメだぞ？」
> - 利用者の答え: 「Bで進めろ」
>
> **正本**（本書はここから起こした。⛔ 本書に中身を丸写ししない所は、指し先を読むこと）:
> - `previous-project-result/16-grab-area-sizing/spec-draft-ja.md` —— 表・定数・図・追跡・§8 の答え・§9 の今の仕様との突き合わせ
> - 同フォルダ `old-to-new-map-ja.md` —— Step 1a の対応表（旧 → 新 425 件 ＋ 本書が足した 13 件）
> - 同フォルダ `cr-inventory-ja.md` —— 同じ CR に入れる要素の洗い出し 23 件
> - 同フォルダ `grab-area-sizing-sample.html` —— **振る舞いの正本**（規則はすべて実装して測ってある）
> - 裁定の逐語は `docs/development-records/rulings.md` の `JDG-180`〜`JDG-254`。段取りは `docs/development-records/handoff.md` §1.2
>
> ⭐ **仕様案 §8 の問いは全部答えが出ている。問い直さない。** ⛔ **対応表に行の無いものを 1 つも消さない。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`**（`CH-1` の受ける目標。`01-04-requirements.md:55`・`:119`）—— 画面に描かれたタスクとマイルストーンを直接掴んで動かし、握っているあいだ結果が絵に出る状態。いまは掴み代の規則が 表 T-023d の 22 行と散文（`FR-016` の R:3363〜3564）に散り、対象ごとに独立した値を持たないので、1 つの掴みやすさを直すとほかが動く。

同時に **`GL-002`**（全体日程をスクロールせずに 1 画面で。`:78`）と **`GL-001`**（1 行に複数のタスクを置いて 1 行で見渡す。`:71`）—— 縮めた画面でどれも指せること（`FR-104`）と、札が読める所に出ること（`FR-109`）は、この 2 つの目標が縮小で壊れないための条件である。ポインタの形（`FR-106`）は **`GL-006`**（`CH-4`。マニュアルを読まずに主要な操作へ到達する）を受ける。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正がある）** —— ① 進捗マーカーの縦位置を `FR-002` と `LF-11` が違う場所に書く（`DFC-663`）② 隠した依存線を選べるかを、用語集の `IC-81` の注と `FR-049` が逆に書く（`DFC-660`）③ 名称ラベルの置き場を `FR-002` と、新しい `FR-109` の両方が持ちうる ⇒ `FR-002` を名称の字そのもの（打ち切り・予定日・9 点のアンカー）に縮め、置き場は `FR-109` が持つ（`JDG-211`）。`IC-81` の注は `FR-049` に合わせて直す（`JDG-245`）。
- **`R1.2`（曖昧・非検証な表現を排除）** —— 「掴みシロ」の大きさが、対象ごとではなく `S-90`（予定の端）・`S-91`（実績の端）・`S-92`（フェード）・`S-137`（依存線と基準日線）の 4 つの値に束ねられており、1 つを直すと別の対象が動く ⇒ 表 GA の 22 行・41 の値に割る（`JDG-194`）。
- **`R1.4`（異常系・境界値）** —— 短い実績で開始と終了の掴み代が入れ替わる（見本で測った: 1 日 2px の倍率で左に終了・右に開始が応えた）、`--->` の未着手の印（幅 3.75px）で 2 つの掴み代が重なる、端点スパンの点が予実で重なる ⇒ 頭打ち（実績の幅の半分）・中央で割る（`JDG-232`）・重なりを許す（`JDG-242`）を表に書く。
- **`R1.5`（MECE）** —— 描くもの 31 種のうち、描く順（表 T-020）に行があるのは 7 種だけである（`DFC-662`）⇒ 表 ZO で 13 行に広げる。
- **`R1.6`（否定要求は代替を明示）** —— 「実績はついでに変わらない」（`FR-107`）の例外が散文に散る ⇒ 表 PE が全数を持つ。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 表 HT を **表 HT（手順 1〜4、4 行）** と **表 TY（種別の順、9 行）** の 2 つに割り、接頭辞を `HT` と `TY` の 2 つ立てる | ⭐ 利用者の裁定（冒頭の逐語。「Bで進めろ」）。⭐ 2 つの表は答える問いが違う —— 手順は「どの段で決まるか」、種別の順は「同じ段で誰が先か」（1 表 1 問） | 表が 1 つ増え、`FR-104`・`FR-105`・`FR-044` が 2 つの表を名指す |
| 決定 2 | 接頭辞の字は `HT`（Hit Test）と `TY`（TYpe） | 下の「接頭辞の選び方」で測った | `HT` は 1 字違いの登録済みが 14 ある（`HB`・`HM`・`RT`・`CT` ほか）。`TY` は 1 つ（`TE`）で、その `TE` は本書が退役させる |
| 決定 3 | 新しい上位要求 `FR-104`〜`FR-110` は `FR-103` の直後（`01-04-requirements.md:3578` の要求の後、`FR-051` の `:3613` の前）にまとめて置く | ⭐ 7 つは 1 つの主題（日程の形の直接操作）であり、散らすと読み手が 7 回探すことになる。⭐ **測った: `FR-011`（`:2654`）から `FR-103`（`:3578`）までが直接操作の一続きであり、次の `FR-051`（`:3613`、見出しと目盛）は別の主題である** ⇒ `FR-103` の直後がその族の切れ目であり、既存の要求を 1 つも動かさずに 7 つ続けて置ける | 章の中の並びが主題順であって番号順ではなくなる（既にそうである） |
| 決定 4 | 語「段」は用語集の `A-1` に 1 義足す（**線だけの形（`SH-3`・`SH-4`）の縦の段** —— 表 XS）。表 HT の側は「段」と呼ばず「**手順 1〜4**」と呼ぶ | ⭐ `A-1`（`01-04-requirements.md:373`）は「7 義ある。必ずどれかを併記する」と書く。仕様案は「段」を 96 回使い、そのうち `--->` の 1〜3 段目は既存の 7 義のどれでもない ⇒ 義を足す。⭐ 手順の側は数え方であって縦の位置ではないので、同じ語を当てると `A-1` の「必ずどれかを併記する」が働かない | `A-1` が「8 義ある」になる。表 HT の行の題が「段 1」から「手順 1」へ変わる |
| 決定 5 | `S-181`（`commentBoxPad`、`_assets/tbl-settings.md:130`）の上限の理由を「上限は 表 T-201 が札に置く px の値（`S-31` ほか）と同じ 30px とする —— どれも札の字の周りに置く px の値であり、別の上限を持つ理由が無い」に書き直す（数は 30 のまま） | ⭐ 今の文は「上限は `labelPad`（`S-31`）に揃える —— 同じ『文字の周りの余白』」。本書は `S-31` の意味を「タスク名 開始 → 名前」に替える（4.3 節）ので、「同じ文字の周りの余白」は偽になる。⭐ **替わりの理由は、今の文が本当に主張していること（別の上限を立てない）だけを残して書ける** —— `S-31` の後継も、本書が足す札の px の行も、どれも上限 30 を持つ（`S-31` 0〜30、`S-32` 0〜30 を測った） | 「同じ意味だから」という強い根拠が、「別の上限を立てる理由が無い」という弱い根拠になる。⚠️ 上限そのものを問い直すなら、それは本書の外である |
| 決定 6 | 図 F-023（積んだ行の応え方）の刻みは **1px** | ⭐ 仕様案が測った刻み（x 440〜620 を 1px、y 380〜450 を 0.5px）のうち、図は横に続く同じ答えを 1 つの矩形にまとめるので、横の刻みが矩形の境の精度になる。1px は画面の 1 点であり、それより細かく描いても画面に出ない | 0.5px で分かれる境（あれば）は 1px の矩形に丸まる |
| 決定 7 | 表 PE の行 ID は仕様案の番号をそのまま使い、字の選択の行は `PE-0` とする | ⭐ 仕様案・`handoff.md`・`rulings.md` が「表 PE の 8・9」の形で 0 起点の番号を引いている。1 起点に振り直すと、それらが 1 つずつずれる。⭐ `-0` の行 ID は前例がある（`05-07-design.md:1403` の `MS-0`） | 表の先頭行の ID が `PE-0` になる |
| 決定 8 | 表 T-023d の「日程の形」の 1 行は新しい行 ID `GR-23` とする（`GR-1`〜`GR-18` の欠番は埋めない） | ⭐ 規則 02 の「番号を振り直さない。欠番はそのまま残す」 | `GR` の行 ID が飛ぶ |
| 決定 9 | 表 ZO の新しい 6 行は `ZO-7` から付け、今の `ZO-1`〜`ZO-6`・`ZO-1a` は ID を変えずに順だけ直す | ⭐ 3 つの行がほかから名指されている —— `SL-3` → `ZO-6`（R:2203）、`GD-7` → `ZO-1a`（R:2755）、`MK-9a` → `ZO-4`（R:3312） | 表の並び順と行 ID の順が一致しない |

**接頭辞の選び方（決定 2 の測り）** —— `docs/spec/_source/row-id-prefixes.json` の登録済みは **153**（`CR-429` が `MR` を足した後）。3 つの条件で選んだ。

1. **登録済みの 153 と重ならない** —— `HT`・`TY` とも無い。3 つの木（`docs/spec`・`docs/development-records`・`docs/development-rules`）に `HT-n` ／ `TY-n` の形の行が 1 つも無いことも数えた（0 件）。
2. **包含で紛れない**（`PT` が `PTD` と紛れて `PK` になった前例）—— 登録済みのどれも `HT`／`TY` で始まらず、`HT`／`TY` も登録済みのどれかで始まらない（`H` も `T` も 1 字の接頭辞として登録されていない）。⚠️ `GA`（`G`）・`XS`（`X`）・`LP`（`L`）・`PE`／`PK`（`P`）は 1 字の登録済みを含むが、これは登録簿に既に 11 例あり（`GD`・`XO`・`LC`・`PA` ほか）、`-` で切れるので紛れない。
3. **2 つの新しい接頭辞どうしが 1 字違いでない** —— `HT` と `TY` は 2 字とも違う。ほかの 6 つ（`XS`・`RF`・`LP`・`GA`・`PE`・`PK`）とも 2 字とも違う。⛔ 退けた案: `HO`（`HT` と 1 字違い）・`HS`（同）・`OD`（本書が書き直す `OC`・`OP`・`OR` と 1 字違い）・`RK`（本書が立てる `PK`・`RF` と 1 字違い）。
- ⛔ `GR` ／ `GS` は使い回さない（`defects.md` と `rulings.md` が `GR-7` などを引き続き引くため。`handoff.md` §1.2）。

---

## 1. 読んで確かめた事実（`3d342753` で開いて数えた）

| 事実 | 所 |
|---|---|
| 表 T-023d は 22 行。本書が消すのは 14 行（`GR-1`〜`GR-9`・`GR-12`・`GR-13`・`GR-15`・`GR-17`・`GR-18`）、残すのは 8 行、足すのは `GR-23` の 1 行 | `docs/spec/01-04-requirements.md:3333` |
| 表 T-261 4 行 ／ 表 T-244 4 行 ／ 表 T-264 7 行 ／ 表 T-013 2 行 —— 表ごと退く | `:3479` ／ `:3488` ／ `:6454` ／ `:1374` |
| 表 T-243 は 6 行。消すのは 5 行（`OR-1`・`OR-3`〜`OR-6`）、残すのは `OR-2` | `:1557` |
| 表 T-240 は 13 行。消すのは掴みの 4 行（`DM-5`・`DM-6`・`DM-10`・`DM-13`）だけで、描く規則は残す | `:2798` |
| 表 T-221 は 15 行。消すのは `LF-4` の 1 行 | `docs/spec/05-07-design.md:1007` |
| 表 T-020 は 7 行（`ZO-1`・`ZO-1a`・`ZO-2`〜`ZO-6`）⇒ 13 行に広げる | `01-04-requirements.md:2708` |
| 表 T-021a は 4 行 ⇒ 5 行（◆ の 未着手 ⇄ 完了 を足す） | `:2972` |
| 表 T-245 は 4 行 ⇒ 10 行（引いて置く値を 6 行足す） | `:3587` |
| `A-1` は「**7 義ある**」と書き、7 義のどれにも `--->` の縦の段が無い | `:373` |
| `S-31` は `labelPad`（既定 6、0〜30）、`S-32` は `labelGap`（既定 8、0〜30） | `_assets/tbl-settings.md:102`・`:103` |
| `S-181` の上限の理由が `S-31` を「同じ文字の周りの余白」と呼ぶ | `_assets/tbl-settings.md:130` |
| `S-47`（`spanDotOfStroke`）と `K-47` は、対応表に 1 行も無い（本書で足した） | `_assets/tbl-settings.md:118` ／ `_assets/tbl-glossary.md:231` ／ `_source/display-words.json:1549`〜`:1555` |
| `S-90`・`S-91`・`S-92`・`S-137`・`S-230` は、生成器が**名で**持つ組 `NOT_STORED_SIZES` である | `tools/generate_entity_types.py:1089`、止まるのは `:1622`（`table T-206 has no row %s`） |
| `CM-15`（`cycleTaskPlanActualState`）は `FR-013` を親に持つ | `_assets/tbl-glossary.md:416` |
| `IC-81` の注が「隠しているあいだも別の面に経路が残る」と書く（`FR-049` と逆。`DFC-660`） | `_assets/tbl-glossary.md:562` |
| `CP-36`・`PI-36` が持つ `ScreenState` の全数に、覚えた実績の居場所が無い | `05-07-design.md:141` ／ `:454` |
| 仕様案 §9 と対応表が引く行番号 897 件を `a43cda1d` → `3d342753` へ写した: **SAME 831 ／ SHIFTED 66 ／ 決まらないもの 0**。SHIFTED はすべて `01-04-requirements.md` の 4880 行以降（`CR-429` が足した分） | 写しは本書と同じ巡で対応表・仕様案に当てた |
| ⚠️ **同梱の洗い出し（`cr-inventory-ja.md`）は前の巡の写しから漏れていた** —— 本書の起草で数え直した: 参照 67 件、**SAME 64 ／ SHIFTED 3 ／ 決まらないもの 0**（`EZ-6` R:5861 → `:5874`、`IN-2` R:6429 → `:6443`、`DS-2` R:6388 → `:6402`。3 つとも当たり先を開いて確かめた） | 同じ巡で当てた |

---

## 2. グラフ

**`induced.py`（種 169。`3d342753` で打ち直した）**

```
seeds in the specification : 169 of 169
edges inside the seed set  : 436
cycles in the induced graph: 2
   size 49 : DS-7 FR-001 FR-002 FR-009 FR-011 FR-013 FR-016 FR-019 FR-029 FR-036
             FR-039 FR-043 FR-049 FR-090 FR-103 GO-2 GO-3 GR-3 GR-4 GR-5 GR-6 GR-7
             GR-8 GR-9 GR-12 GR-13 GR-15 GR-17 GR-18 IC-81 LF-11 MK-12 MK-13 OC-2
             OC-10 OR-1 PI-7 PTD-3 PV-1 S-49 S-90 S-91 S-131 S-137 S-180 S-230
             S-233 S-247 TE-3
   size 2  : PC-5 S-249
```

- **束 A（49 対象）は 1 つの計画で 1 度に書く。** 4 つの原稿にまたがる（`01-04-requirements.md`・`05-07-design.md`・`_source/settings.json`・`_assets/tbl-glossary.md`）⇒ **1 体が 1 回の書き入れで、7 節の順に書く。**
- **束 B（2 対象）**: `PC-5` ↔ `S-249` —— 表 T-264 を退けるのと `S-249` の書き直し（24 → 16px）を同じ回に。
- 残る 118 対象は閉路に入らない（1 つずつ書いてよい。それでもオブジェクトごとに 1 回）。
- ⚠️ 新しい ID（`FR-104`〜`FR-110`、表 XS〜TY の行、`GR-23`・`ZO-7`〜・`GO-5`〜）はまだ無いので種にできない ⇒ 当てたあとに `induced.py` を打ち直す（束 A が広がる見込み）。

**`impact.py`（同じ 169 の種）** —— 出力 4091 行。名指された ID のうち、**変わる ID を名指す行で、対応表と仕様案 §9 の場所が覆わないものが 49 行**（機械で絞った）。読んで仕分けた結果が 3.2 節の 13 件である。残り 30 行は、名指す先の ID が残り、変わるのは指し先の中身の一部だけなので変えない。

---

## 3. 対応表 —— 「削除」「書き直す」「ポインタ化」を、新しい表と同じ 1 本で当てる（必須の節）

### 3.1 在り処と数

`previous-project-result/16-grab-area-sizing/old-to-new-map-ja.md`（425 件: A 189 ＋ B 236）。⛔ **本書に丸写ししない。** 当てる体は、対応表の行を 1 つずつ開いて当てる。

| 分担 | 数えたもの | 削除 | ポインタ化 | 意図して捨てる | 書き直す | 残す |
|---|--:|--:|--:|--:|--:|--:|
| A（掴み代・応える順・ポインタ・押す引く） | 189 | 57 | 1 | 1 | 53 | 77 |
| B（札・描く順・依存線・表示・字の選択） | 236 | 21 | 3 | 13 | 67 | 132 |

⭐ **行番号は本書と同じ巡で `3d342753` へ写し直した**（1 節の最後の行）。⚠️ 対応表の 2 件は元から 3 行ずれた引用である（`old-to-new-map-ja.md:854` の R:2934 と、仕様案 264 行目の R:3410〜3411）—— **直さない**（引用の文はその前後にある）。

### 3.2 ⭐ 本書が足した数え落とし 13 件（`impact.py` が拾い、`3d342753` で 1 件ずつ開いて確かめた）

対応表の追補（`old-to-new-map-ja.md` の末尾の節）に同じ 13 件を足した。⛔ 既存の行は 1 つも消していない。

| # | 届き先（確かめた所） | 何が起きるか | 扱い |
|---|---|---|---|
| 1 | `SWS-3`（`05-07-design.md:1155`） | 消える `LF-4` を STATEMENT が名指す | 書き直す —— 引き出し・引き入れ・矢じりの新しい S 行を名指す |
| 2 | 表 T-068 の `LC-4`（`05-07:701`）・`LC-6`（`:703`）と `:711` | 消える 表 T-013 を名指す | 書き直す —— `LC-4` は `FR-002` の打ち切り、`LC-6` と `:711` は 表 RF・LP |
| 3 | `AT-98` の `nameAnchor` の注（生成物 `_assets/fig-erd-detail.md:407`） | 「指定が無いときの置き方は 表 T-013 が決める（`FR-002`）」—— 消える表 | 原稿 `_source/erd.json` を書き直して生成し直す。⛔ md を手で直さない（検査 16） |
| 4 | `SWS-4`（`05-07:1172`） | マーカーと再開アイコンの置き場を `LF-11`・`LF-13` に求める —— 表 LP・XS へ移る | 書き直す —— 表 LP・XS を足す |
| 5 | `FR-091`（`01-04:1344`） | 「名称ラベルをどこへ描くかは `FR-002` が定める」—— 置き場は `FR-109` へ移る | 書き直す —— `FR-109` |
| 6 | 5.5 節の `05-07:680` | 「置く値は 表 T-019 と 表 T-021a、および `FR-043` / `FR-044`」—— 値は 表 T-245 の新しい行へ | 書き直す（`:655`・`:685`〜`:690` と同じ回） |
| 7 | `S-47`（`tbl-settings.md:118`）・`K-40`〜`K-42`・`K-45`・`K-47`（`tbl-glossary.md:231` ほか） | §5 が退役させるのに対応表に行が無い（測った: `S-47`・`K-41`・`K-47` は 0 件、`K-40`・`K-42`・`K-45` は `S-40` 等の行の備考にあるだけ） | ⛔ 行を足してから消す（Step 1a の歯止め）。`K-47` は `_source/display-words.json:1549`〜`:1555` も |
| 8 | `CP-36`（`05-07:141`）・`PI-36`（`:454`） | 覚えた実績（表 PE の押下の巡り）の置き場は「画面の状態」だが、`ScreenState` の全数を持つ 2 行に無い | 書き直す —— メンバーを足す（検査 26b が実装と同じ波を求める） |
| 9 | `CM-15`（`tbl-glossary.md:416`）・`VG-5`（`01-04:1250`）・`LF-2`（`05-07:1012`）・`RC-11`（`01-04:6809`）・表 T-236 | 仕様案 §4〜§9 にはあるが、対応表に行が無い（測った: どれも 0 件） | 対応表に行を足した（本節） |
| 10 | 表 T-006b の `A-1`（`01-04:373`） | 仕様案が「段」を `A-1` の 7 義に無い意味で使う | `A-1` に 1 義足し、表 HT は「手順」と呼ぶ（決定 4） |
| 11 | `S-181`（`tbl-settings.md:130`） | 上限の理由が `S-31` を「同じ文字の周りの余白」と呼ぶ —— `S-31` の意味が変わる | 理由の文を書き直す（決定 5） |
| 12 | `FR-101`（`01-04:5331`） | 「`IN-2` がカーソルの綴りを宿主に渡している」—— `IN-2` は 表 PK の画像を持つようになる | 読み直す（文は偽にならない。`IN-2` は「宿主に渡す」役を残す） |
| 13 | `SWS-2`（`05-07:1139`）・`05-07:1027`（`OC-10`）・`FR-081`（`01-04:2201`、`SL-7`）・`FR-032`（`:2289`、`HM-10`）・`FR-054`（`:3973`、`FR-043`） | 名指す行が書き直される | 読み直すだけ（読んだ限り文は偽にならない） |

### 3.3 ⭐ 対応表の古い行の訂正 5 件（⛔ 行を消さず、追補で覆す）

| 行 | 対応表の扱い | 正しい扱い | 根拠 |
|---|---|---|---|
| `PC-6` | 書き直す | **削除** —— 表 T-264 ごと退き、替わりの形は 表 PK の注が持つ | `JDG-192`（表 T-264 を吸い込む）・`JDG-247` |
| `LF-10` | 書き直す（要確認） | **残す** —— どの図形も一辺 h の正方形に収まる規則は変わらない | `JDG-209` の ⑬ |
| `LF-13` | 残す | **書き直す** —— 表 XS の 10 にだけ当たる文にする | 仕様案 §9（㉖） |
| `V-5` | 残す | **書き直す** —— 「`FR-011` と 表 T-020」を `FR-110` へ指し直す | 仕様案 §2 の `FR-110` |
| `OC-10` | 要確認 | **書き直す** —— 1 段目の高さを字の大きさ f で数える | `JDG-212`・`JDG-221` |

---

## 4. 当てる中身

⛔ **本節が正である。** 仕様案の本文を丸写しせず、当てる文として書いた。数だけの根拠（見本で測った値、測り方）は仕様案 §4・§5 が持つ ⇒ **当てる体は、表の各行の中身を仕様案 §4 の同じ番号の行から取る。**

### 4.1 接頭辞の登録（`docs/spec/_source/row-id-prefixes.json`）

**立てる 8**（`words` ／ `means.ja`）:

| 接頭辞 | words | means（ja） |
|---|---|---|
| `XS` | Cross Section | 形ごとの縦の断面（帯の位置と高さ） |
| `RF` | Reference | 札が付く基準（実績 ／ ダミー ／ 予定） |
| `LP` | Label Placement | 札（マーカー・名前・担当と進捗）の配置 |
| `GA` | Grab Area | 操作対象ごとの掴み代の幾何 |
| `HT` | Hit Test | 応える掴み代を 1 つに決める手順（1〜4） |
| `TY` | Type | 同じ手順の中で、種別の掴み代が応える順 |
| `PE` | Press Effect | 押す・引くの効果 |
| `PK` | Pointer Kind | ポインタの形 |

**退ける 4**: `GS`（表 T-261）・`TE`（表 T-244）・`NL`（表 T-013）・`PC`（表 T-264）。
⚠️ **登録と表の行は同じ commit に置く** —— 行の無い登録は `row_id_prefixes_json_to_md.py` が「dead registration」で拒み、登録の無い行は「unregistered」で拒む（前の巡で実測）。

### 4.2 9 つの表

⭐ **中身は仕様案 §4 の同じ番号の行を取る。** 行 ID は仕様案の番号にそのまま対応する。

| 表 | 行 ID | 行数 | 何に答える表か | 置く所（宿主） |
|---|---|--:|---|---|
| XS 縦の断面 | `XS-1`〜`XS-14` | 14 | 形ごとに、どの帯がどこに何の高さで立つか | `FR-109` の下 |
| RF 札の基準 | `RF-1`〜`RF-3` | 3 | 札はどれに付くか（実績 ／ ダミー ／ 予定） | `FR-109` の下 |
| LP 札の配置 | `LP-1`〜`LP-8` | 8 | マーカーと名前をどこから書き出すか | `FR-109` の下 |
| GA 掴み代 | `GA-1`〜`GA-22` | 22 | 対象ごとの掴み代の形と大きさ（41 の値） | `FR-104` の下 |
| **HT 応える手順** | `HT-1`〜`HT-4` | 4 | どの手順で 1 つに決まるか | `FR-105` の下 |
| **TY 種別の順** | `TY-1`〜`TY-9` | 9 | 同じ手順の中でどの種別が先か | `FR-105` の下 |
| PE 押す・引く | `PE-0`〜`PE-13` | 14 | 掴んだものを押す ／ 引くと何が起きるか | `FR-107` の下 |
| PK ポインタの形 | `PK-1`〜`PK-9` | 9 | ポインタをどんな形にするか | `FR-106` の下 |
| ZO 描く順 | `ZO-1`〜`ZO-6`・`ZO-1a`・`ZO-7`〜`ZO-12` | 13 | 奥から手前へ何をどの順に描くか | `FR-110` の下（今の `FR-013` の下から移す） |

⚠️ **「宿主」は表が**文書のどこに立つか**であって、名指してよい要求は 1 つに限らない（追跡は 4.5 節の表）。表 HT と表 TY は `FR-104` も名指すが、**置き場は `FR-105` の下**とする —— 重なりの解決がその要求の主題そのものだからである。⛔ 2 つの要求が同じ表の中身を持つ形（二重表現）にしない。

**表 HT（応える手順。4 行。⭐ 前の手順で 1 つに決まれば、後の手順は読まない）**

| 行 ID | 手順 | 規則の要点（⭐ 全文は仕様案 §4.5 の同じ段） |
|---|---|---|
| `HT-1` | 手順 1 形の上 | 描いた形の上では、その形のタスクの掴み代だけが応える（今の `GS-1`）。**描いた形は形ごとに読む**（今の R:3408 の「範囲の矩形」を置き換える。`JDG-236`） |
| `HT-2` | 手順 2 形の外の縦 | 形の外で 2 つ以上のタスクの掴み代に入ったら、描いた形が上下に近いタスクだけを残す（今の `GS-2`）。依存線はタスクではないので残る |
| `HT-3` | 手順 3 種別の順 | **表 TY に従う。** ただしマーカーと再開アイコンの描いた箱の上では、実績の端の次にそれらが応え、ほかの種別はその後へ回る（今の C8、R:3428） |
| `HT-4` | 手順 4 同じ種別 | 掴み代の中心までの直線の距離が近い方（`JDG-217`）。同じなら日付の遅い方（今の `TE-1`・`TE-4` を広げる）。⛔ 描いた前後では決めない（`FR-110`） |

**表 TY（種別の順。9 行。上ほど先）**

| 行 ID | 順 | 形の上 | 形の外（隙間・外側の帯） |
|---|---|---|---|
| `TY-1` | 1 | フェード | フェード（`JDG-218`。今の `GS-4` の例外） |
| `TY-2` | 2 | 進捗マーカー（実績の外）・再開アイコン | 依存線（今の `GS-4`） |
| `TY-3` | 3 | ダミー | 進捗マーカー（実績の外）・再開アイコン |
| `TY-4` | 4 | 実績の端（◆ の実績を含む） | ダミー |
| `TY-5` | 5 | 依存線 | 実績の端 |
| `TY-6` | 6 | 予定の端 | 予定の端 |
| `TY-7` | 7 | 進捗マーカー（実績の中） | 進捗マーカー（実績の中） |
| `TY-8` | 8 | ◆ の予定 | ◆ の予定 |
| `TY-9` | 9 | 本体 | 本体 |

**表 T-023d は消えず、日程の形でないものの順を持つ**（新しい並び、9 行）: `GR-19`・`GR-22`（パレットと面の境）→ `GR-10`・`GR-11`（名前・担当、ダブルクリックだけ）→ `GR-14`（注記。日程の形より先へ移す。`JDG-210`）→ **`GR-23`（日程の形 —— 表 HT に従う。新しい行）** → `GR-20`（行見出し）→ `GR-16`（基準日線。掴み代は `S-137` の 6px のまま）→ `GR-21`（スクロールバー）。

### 4.3 定数（仕様案 §5）

⛔ **置く表は、割る今の行の表に従う。** 文書に保存する 表 T-201 の行（保存形の鍵 `K-19`〜`K-32` を持ち、描く比が掛かる）を割るときは T-201 に、製品の決まりである 表 T-206 の行（掴み代・ポインタ）を割るときは T-206 に置く。⚠️ T-201 の行を T-206 へ移すと保存形の鍵が消え、描く比も外れる。

- **正は `docs/spec/_source/settings.json`** であり、表 T-201・T-206、文書の型、既定の値は `npm run gen` で作り直す（検査 21〜22）。名前は 表 T-104 の鍵の行と `_source/display-words.json` が持つ。
- **退役させる行と鍵**: `S-20`・`K-20` ／ `S-23`・`K-23` ／ `S-40`〜`S-42`・`K-40`〜`K-42` ／ `S-45`・`K-45` ／ `S-47`・`K-47`。`S-46`（矢じりの頭打ち）は残す。⛔ **`K-47` は `display-words.json:1549`〜`:1555` の ja／en の対も退ける。**
- ⚠️ **「同上」の頭を退けない** —— 表 T-206 の「保存しない理由」は `S-90` が頭で `S-91`・`S-92` が継ぐ（3 行とも消える）⇒ **GA の新しい行の先頭に、頭の文（アクセシビリティ）を全文で持たせ、`DS-7` の「`S-90` の保存しない理由」をその新しい頭へ付け替える。** `S-199` → `S-137` → `S-230` → `S-134` の継ぎの鎖にも行を割り込ませない。
- 群と既定値の全数は仕様案 §5 の表が持つ（掴み代 41・札・形・依存線・`--->`・端点スパン・再開アイコン・ポインタ）。⭐ `S-22`（マーカーの径）は既定 16 → 22.4、上限 `actualMin` → `actualMin ÷ actualOfPlan`。`S-249`（ポインタの一辺）は 24 → 16。

### 4.4 図

⛔ **手で描かない。見本を固定の設定で描き、SVG を書き出して `docs/spec/_assets` に置く。**

| 図 | ファイル | 描くもの | 持つ表 |
|---|---|---|---|
| F-020 | `fig-grab-map-bar.svg` | `===` の行（掴み代の面を出して） | GA・XS |
| F-021 | `fig-grab-map-arrow.svg` | `--->` の行（3 段。端点スパンの枠を 1 つ添える） | GA・XS |
| F-022 | `fig-grab-map-milestone.svg` | ◆ の行（隣の ◆ を含む） | GA・XS |
| F-023 | `fig-hit-stacked-row.svg` | 積んだ行を `hitAt` で **1px 刻み**に引き、横に続く同じ答えを 1 つの矩形にまとめて掴み代の色で塗る | HT・TY |
| F-024 | `fig-label-placement.svg` | 表 LP の 8 行を 8 つの小窓に | LP・RF |
| F-025 | `fig-pointer-shapes.svg` | 表 PK の 9 行を実寸と 4 倍で | PK |
| F-018（描き直し） | `05-07-design.md`（Mermaid のまま） | 押下の巡り（未着手 → 進行中 → 完了 → 中断 → 未着手、`JDG-239`）。◆ の 未着手 ⇄ 完了 を足す | PE・T-021a |

**書き出しの手順**（当てる体が踏む）:
1. 探り針は追跡される所（`tools/probe/` の下）に置く —— `playwright` が根の `node_modules` へ上向きに解決するので、リポジトリの外の一時フォルダに置くと解決しない。
2. `chromium.launch()` → `page.goto('file://…/previous-project-result/16-grab-area-sizing/grab-area-sizing-sample.html')` → `localStorage.clear()`（ペインの幅を覚えている）→ `reload`。
3. ⭐ **値は `docs/spec/_source/settings.json` を読んで入れ、見本の既定と 1 つでも違えば止める**（図が仕様の値から起きることの担保）。表示の倍率 100、`dayPx` 12、表示の切り替えはすべてオン。
4. ⛔ **名前を中立の語に替える**（見本の業種の例をそのまま書き出さない。タスク A〜F、マイルストーン A、担当 A〜C）。
5. `#stage` を複製し、`viewBox` を行の帯に絞り、ページの CSS の地の色と書体を SVG の `<style>` と背景の矩形へ写し、`<title>` と「生成物・原稿・作り直し方」の注を頭に置く。
6. 置き場は、各図を持つ表の直後（`[![図 F-0xx — …](_assets/fig-….svg)](_assets/fig-….svg)`）。

### 4.5 上位要求と追跡

**新しい 7 つ（`FR-104`〜`FR-110`）は `FR-103` の直後にまとめて置く**（決定 3）。STATEMENT・RATIONALE・ORIGIN の全文は仕様案 §2 が持つ。⭐ **本書で表の名が変わる所**: `FR-104`・`FR-105`・`FR-044` の STATEMENT の「表 HT」は「**表 HT と表 TY**」と書く。

| ID | 題 | 持つ表 | ORIGIN |
|---|---|---|---|
| `FR-104` | 縮小しても、どれも指せる | GA・XS・HT・TY | `GL-008` ／ `GL-002` |
| `FR-105` | 重なっても、狙ったものが答える | HT・TY・PE の `PE-0` | `GL-008` ／ `GL-006` |
| `FR-106` | ポインタの形で、何を掴むかが分かる | GA のポインタ欄・PK | `GL-006` ／ `GL-008` |
| `FR-107` | 実績は事実なので、ついでには変わらない | PE | `GL-008` |
| `FR-108` | 見えないものは、掴めず動かない | GA・RF・PE | `GL-006` |
| `FR-109` | 札は読める所に置く | RF・LP・XS | `UC-001` 手順 4・拡張 4a・4b ／ `UC-003` 手順 4 ／ `UC-005` 手順 3〜4 ／ `GL-001` ／ `GL-002` |
| `FR-110` | 描く前後は 1 つの表で決める | ZO | `UC-001` 手順 4 ／ `UC-004` 手順 4 ／ `GL-001` ／ `GL-002` |

**書き直す 3 つ**: `FR-043`（未着手でも実績を描き始められる。表 RF・GA・PE を指す）／ `FR-009`（依存線は予定に付ける。予定が無い・予定を表示していないときは描かない）／ `FR-002`（名称の字そのもの —— 打ち切り・予定日・9 点のアンカー —— に縮め、置き場は `FR-109` が持つ）。`FR-044` は今のまま、表を指すだけにする。

**追跡（⭐ どの表にも親があり、どの要求も表に届く。Step 4 で機械の検査にする）**

| 上位要求 | XS | RF | LP | GA | HT | TY | PE | PK | ZO | 依存線の定数 |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| `FR-104` | ● | | | ● | ● | ● | | | | |
| `FR-105` | | | | | ● | ● | ● | | | |
| `FR-106` | | | | ● | | | | ● | | |
| `FR-107` | | | | | | | ● | | | |
| `FR-108` | | ● | | ● | | | ● | | | |
| `FR-109` | ● | ● | ● | | | | | | | |
| `FR-110` | | | | | | | | | ● | |
| `FR-043` | | ● | | ● | | | ● | | | |
| `FR-009` | | | | ● | | | | | | ● |
| `FR-044` | ● | | ● | ● | ● | ● | ● | ● | ● | |

### 4.6 足した 2 つ（`handoff.md` §1.2 の 6 つのほか）

1. **表 T-020 を描くものすべての行へ広げる**（表 ZO、13 行、親は `FR-110`）。今の宿主 `FR-011` から `FR-110` へ移し、`V-5` と R:2658 の指し先も直す。⛔ `ZO-*` の ID は変えない。
2. **日程表の上の押す・引くで字の選択を始めない**（`JDG-204`）。`FR-105` の STATEMENT に MUST NOT を 1 文、表 PE の `PE-0` に 1 行、`MK-12`（ブラウザの既定動作に委ねる）に例外を 1 文足す。

### 4.7 理由 —— 5 つの原則（1 原則に 1 つの理由。⛔ 行ごとには書かない）

⛔ **利用者の言葉を仕様に写さない。`JDG` を引く。**

| 原則 | 仕様に書く理由 | 束ねる裁定 |
|---|---|---|
| どれも指せる | どの形も、ズームのどの段でも、それだけが応える領域を持つ。縮めた画面こそ日程を動かす画面だからである | `JDG-187` ／ `JDG-189` ／ `JDG-190` ／ `JDG-194` |
| 実績は事実 | 実績は起きたことの記録である。軽い操作のついでに変わってはならず、変えるのは実績の端を掴む意図した操作に限る | `JDG-187` ／ `JDG-188` |
| 見えないものは掴めない | 画面に無いものが指の下で動けば、利用者は原因を知りようがない | `JDG-184` ／ `JDG-196` |
| 被らせない | 中心線を共有する形（`===`・◆）は札を横へ逃がし、段で分かれた形（`--->`）は札を動かさない。札は線より手前に描く | `JDG-182` ／ `JDG-183` ／ `JDG-185` ／ `JDG-203` |
| ダミーはまだ始まっていない実績 | ダミーに独自の規則を持たせない。規則の数が減る | `JDG-186` |

---

## 5. 数の予測

`3d342753` の実測は **tables=164 ／ figures=11 ／ rows=2048 ／ uids=153 ／ 接頭辞 153**（`md-checks.py` と `row-id-prefixes.json` で数えた）。

| 量 | 式 | 差 | 後 |
|---|---|--:|--:|
| tables | 新しい表 8（XS・RF・LP・GA・**HT**・**TY**・PE・PK。ZO は 表 T-020 を広げる）− 退く表 4（T-261・T-244・T-013・T-264） | **+4** | **168** |
| rows | 新しい表の行 83（XS 14 ＋ RF 3 ＋ LP 8 ＋ GA 22 ＋ HT 4 ＋ TY 9 ＋ PE 14 ＋ PK 9）＋ 今の表 −27（T-023d −14 ＋ `GR-23` 1、T-013 −2、T-261 −4、T-244 −4、T-264 −7、T-243 −5、T-240 −4、T-221 −1、T-020 ＋6、T-021a ＋1、T-245 ＋6）＋ 設定 ＋52（新しい行 62、消す 10）＋ 鍵 ＋5（新 12 − 退役 7） | **+113** | **2161** |
| uids | `FR-104`〜`FR-110` | **+7** | **160** |
| figures | 見本から起こす 6（F-020〜F-025）。F-018 は描き直しなので 0 | **+6** | **17** |
| 接頭辞 | ＋8（XS・RF・LP・GA・HT・TY・PE・PK）− 4（`GS`・`TE`・`NL`・`PC`） | **+4** | **157** |

- 前提: 表 GA の 41 の値は「1 値 1 行」（`JDG-194`）。⚠️ **Step 2（正規化）で行が減ったら、数え直してから当てる。**
- 突き合わせは `md-checks.py` の `tables= figures= rows= uids=` と `python docs/spec/_source/row_id_prefixes_json_to_md.py`。⭐ **外れたら数え落としが出ている**（規則 02 の 2）。

---

## 6. 影響

### 6.1 仕様

**書き換える原稿**: `docs/spec/01-04-requirements.md` ／ `05-07-design.md` ／ `A-appendix.md`（変更履歴 1 行）／ `_source/settings.json` ／ `_source/display-words.json` ／ `_source/row-id-prefixes.json` ／ `_source/erd.json` ／ `_assets/tbl-glossary.md` ／ `_assets/fig-*.svg` 6 枚（新）。
**生成し直すもの**（⛔ 手で直さない）: `_assets/tbl-settings.md`・`tbl-row-id-prefixes.md`・`tbl-property-items.md`・`fig-erd-detail.md`・`grs-document.schema.json`・`src/` の生成域。

### 6.2 記録（当てるときに書く）

- **`rulings.md`**: ⭐ **冒頭の逐語 2 通を裁定の行にする**（表 HT を 2 つに割る問いと答え。次の番号は `JDG-274`・`JDG-275` の見込み —— 当てるときに `rulings.md` の末尾で確かめる）。着地先は本書が当てた行 ID（表 HT・表 TY の行、`row-id-prefixes.json` の 2 つの登録）。
- **`rulings.md` の状態欄**: 本書が着地させるのは **66 行**（`JDG-180`〜`JDG-197`・`JDG-199`〜`JDG-237`・`JDG-239`〜`JDG-242`・`JDG-245`〜`JDG-249`）。⚠️ そのうち `JDG-185`・`JDG-187`・`JDG-209`・`JDG-214`・`JDG-226` は「覆された（一部）」なのに状態欄に「旧の状態: 未着地」の字があり数えられている ⇒ **着地させたら、状態欄から字「未着地」が消える書き方にする**（残す部分が適用済であることを書く）。着地先は `docs/spec` に定義された ID を名指す（検査 43 の証明はそれである）。
- **`defects.md`**: `DFC-659`・`DFC-660` は仕様だけで閉じる（md5 で押さえて `fixed-defects.md` へ）。`DFC-657`・`DFC-658`・`DFC-661`〜`DFC-664` は実装待ちへ（着地先に新しい行 ID を書く。検査 29）。`DFC-449`（`FR-011` の 1 押しの完了が `JDG-239` で消える）と `DFC-450`（`GR-8` が消えて論点が消える）は方針を書き直す。`DFC-649`・`DFC-652`・`DFC-653` は方針の欄の `PC-*`・`LF-11`・`GR-7`・`K-23`・`FR-002` を新しい表へ付け替える。`DFC-636` は結びが書き直されるので読み直す。
- **`pending-decisions.md`**: `PND-476` は裁定済（`JDG-230`）。`PND-167`・`PND-168`・`PND-347`・`PND-360`〜`PND-362` は本書の表が答える ⇒ **体に反証させてから**裁定済へ。`PND-209`・`PND-238`・`PND-312` はコードの STOP が消えるまで未裁定。
- **`changelog.md`** と **`A-appendix.md` の変更履歴**: 1 行ずつ。⚠️ 変更履歴の行に生の `|` を書かない。
- **`.claude/skills/spec-graph-check/retired.py`**: 消える ID のうち、変更履歴・台帳・裁定が名指し続けるものを理由付きで登録する（登録しないと未定義の参照として赤）。

### 6.3 実装と試験で直す所（⛔ 本書は直さない）

| 所 | 直すこと |
|---|---|
| `tools/generate_entity_types.py` | 定数の組を新しい S 行へ付け替える（`NOT_STORED_SIZES`（`:1089`）ほか）、`PUBLISHED_READ_BY_*` |
| `docs/development-rules/03-implementation.md` | 生成された定数の一覧（検査 30） |
| `src/entity/layout-engine/item-hit-area/item-hit-area.ts` | 表 GA の掴み代、表 HT の 4 手順と表 TY の順、注記の枠・四隅・引出し線・線先、依存線の付け先 |
| `schedule-geometry.ts` ／ `schedule-layout.ts` ／ `screen-regions.ts` | 表 XS・RF・LP、`--->` の 3 段と未着手の印、再開アイコン、依存線の経路、マーカーの上限 |
| `edit-task.ts` ／ `edit-document.ts` ／ `input-command-translator.ts` ／ `screen-state.ts` | 表 PE、表 T-021a の輪と覚えた実績、表 T-245 の新しい 6 行、選択を縦に移す（`DFC-664`） |
| `svg-renderer.ts` ／ `frame-loop.ts` ／ `dom-screen-surface.ts` ／ `dom-input-source.ts` | 表 ZO の順（`DFC-661`・`DFC-662`）、表 PK のポインタと替わりの形、日程表の上で字を選ばない |
| 試験 | 消える ID を名指す既存の試験を、対応表の扱いに従って付け替えるか消す。⛔ **実装した者に書かせない** —— 仕様だけを読む別の体が書く |

---

## 7. ⭐ 当てる順と commit の割り方

### 7.1 当てる順（前の段が作る ID を後の段が名指す。⛔ どの行も 2 度書かない）

0. **対応表**に 3.2 の 13 件を足し、3.3 の 5 件を訂正する（本書と同じ commit）。
1. **`_source/row-id-prefixes.json`** —— 8 足し 4 消す（1 回）。生成は 9 でまとめて走らせる。
2. **`_source/settings.json`** を上から 1 回 —— 表 T-201（`S-18`・`S-19` 書き直し、`S-20` 削除、依存線の新しい 3 行、`S-22`、`S-23` 削除、`S-31`・`S-32` と札の新しい 3 行、`S-40`〜`S-42`・`S-45`・`S-47` 削除、`--->` の 3 行と点の径、`S-10`、再開の 2 行、`S-49`・`S-131`・`S-135`・`S-181` の注）→ T-203 の `S-176` → T-206（`S-90`〜`S-92` の位置に GA の 41 行、`S-180`・`S-196`・`S-233`・`S-137`・`S-230` と注記の 3 行、`S-249` とポインタの 4 行、`S-247` の注）→ T-236 の色 2 行。
3. **`_assets/tbl-glossary.md` と `_source/display-words.json`** を 1 回 —— 表 T-104 の鍵（7 消す・`K-19`・`K-31`・`K-32` 書き直し・12 足す）、`U-43`・`U-52`、`IC-81` の注、`CM-15`、`A-1` に 1 義。
4. **図 6 枚を書き出す**（2 の値を読むので 2 の後、5 で差し込むので 5 の前）。
5. **`01-04-requirements.md`** を文書の順に 1 回（**束 A はこの回で全部**）—— T-006b（`A-1`・`A-13`）→ `V-5` → `FR-001` → `FR-002`（縮める・表 T-013 を退ける）→ 表 T-012 の注 → `FR-075` → `FR-091` → `VG-5` → `FR-003` → `FR-009`（`RT-4a`）→ `FR-011` → ⭐ **文書の順の唯一の例外: `FR-103`（表 T-245 の新しい 6 行）を先に書き、それから `FR-043` を縮める**（`JDG-211`。先に縮めると `GR-5` と `FR-043` の値の MUST が行ごと落ちる）→ `FR-044` → `FR-013` → `FR-016`（表 T-023d の新しい並び、T-261・T-244 を退ける）→ **新しい `FR-104`〜`FR-110` と 9 つの表**（`FR-110` の下へ 表 T-020 を移す）→ `FR-019` → `FR-048` → `FR-049` → `FR-090` → `FR-092` → `FR-029` → `FR-036` → `FR-039` → `FR-040`（表 T-264 を退ける）→ `NFR-004` → `CT-4` → `RC-11`。
6. **`05-07-design.md`** を 1 回 —— `CP-36` → `PI-7` → `PI-36` → `:655`・図 F-018・`:680`・`:685`〜`:690` → `LC-4`・`LC-6`・`:711` → `:765` → 表 T-221（`LF-2`、`LF-4` 削除、`LF-7`〜`LF-9`・`LF-11`・`LF-13`）→ `:1027` → 表 T-222 の前書き → `SWS-2`・`SWS-3`・`SWS-4`。
7. **`_source/erd.json`** の `nameAnchor` の注。
8. **`A-appendix.md`** の変更履歴 1 行、**`retired.py`**。
9. `npm run gen` → `gen:check` → `check.sh` → `npm run typecheck` → vitest（**統合の作業木で**）。

### 7.2 commit の割り方 —— ⭐ 仕様だけでは commit できない

**C0 ＝ 本書（CR の文書）＋ 対応表の追補。** 単独で緑。⛔ 仕様書は 1 行も触らない。前例は `CR-429` の起草の commit である。

**C1 ＝ 仕様の適用 ＋ 生成器と規則 03 ＋ `retired.py` ＋ 実装 ＋ 旧い試験の退役 ＋ 仕様だけを読む試験 ＋ 不変条件の検査 ＋ 台帳・裁定** —— 統合の作業木で全部が緑になってから 1 つの commit にする（⛔ 途中の赤い commit を作らない）。

**理由（`3d342753` で測った）** —— 規則 02 の §3.5 は「波を分けるなら、分ける前に『読む側は投げるか』を測れ」と書く。**測った: 読む側は投げる。**

1. **試験が読み込みの時点で投げる。** `tests/unit/fr-043-dummy-drawn.test.ts:134` は**モジュールの頭**（字下げ 0）で `const GR_9 = rowOf('T-023d', 'GR-9')` を評価し、`rowOf` は同ファイル `:126` で `throw new Error('table ${tableId} has no row ${rowId}')` と投げる。**本書が `GR-9` を消すと、この行は import の時点で投げる。**
   - ⭐ **投げたときに何が起きるかを、今の木で測った**（⛔ 仕様書は 1 行も触っていない）: `npx --no-install vitest run tests/unit/uf-36.test.ts` —— このファイルはモジュールの頭（`:131`）で読めない XSD を読むので投げる。結果は **`Test Files 1 failed (1)` ／ `Tests no tests`** —— **1 件も走らない。**赤が 1 件出るのではなく、そのファイルの主張が**全部消える**。
   - ⭐ **同じ形の行を数えた**: 本書が消す行 ID を**モジュールの頭で引いている**のは **27 行 ／ 14 ファイル**（`cr-399`・`cr-415`・`cr-416`・`cr-420`・`cr-421`・`cr-422`・`fr-013-pointer-on-the-figure`・`fr-043-dummy-drawn`・`in-2-pointer-shape`・`t-023d-gr-5-relays-the-actual-duration`・`t-023d-the-hit-box-starts-at-the-day` ほか）。うち引きではなく型の字面である 3 ファイル（`in-2-a`・`in-2-b`・`uf-30-31`）を除くと、**11 ファイルが `(0 test)` になる**。
2. **生成器が止まる。** `tools/generate_entity_types.py:1089` は `'NOT_STORED_SIZES': (['S-90', 'S-91', 'S-92', 'S-137', 'S-230'], …)` と**行 ID を名で**持ち、`:1622` で `raise SystemExit('table T-206 has no row %s' % row_id)` と止まる ⇒ `S-90` を消すと `npm run gen` が止まり、以後の生成器も走らない。
3. **生成された型から鍵が消える。** `DocumentSettings` の鍵が消えると `schedule-geometry.ts` などが型で赤くなる。
4. **検査が同じ波を求める。** 検査 44（`src/`・`tests/` が消える ID を名指す回数）・42・37（鍵の指紋）・43（裁定の着地）・26b（`PI-36` の新しいメンバー）・39（新しい MUST）。

⇒ **① 原稿と ② それを読むコード・試験・生成器を分けられない。** ⛔ 仕様だけを commit すると、関係のない試験が 11 ファイルまとめて `(0 test)` になる。

⚠️ **基準線の動き**（検査 37・39・42・43・44）は **利用者の許しを得てから、前に立つ者が直す**。⛔ 体に触らせない（自動判定は基準線の編集を CI の回避として拒む）。

---

## 8. 試験が主張すべきこと（Step 4。⛔ 実装した者に書かせない）

- **表の 1 行 ＝ 試験 1 件**（約 107 件 ＝ XS 14・RF 3・LP 8・GA 22・HT 4・TY 9・PE 14・PK 9・ZO 13・T-021a 5・T-245 の新しい 6）。
- **掃引を 1 本**（`tests/system/`、DOM なし）: 予定を表示 2 × 実績を表示 2 × マーカー 2 × 入る 2 × 形 4 × ズームの両端 2 × 実績が早い／遅い 2 ＝ **256**。主張: 札・マーカー・再開アイコンが重ならない（`FR-109`）、隠したものは応えない（`FR-108`）。
- **`check.sh` の不変条件 4 つ**（どれも §5.1 の作法: 規則を先に書く・数えて基準線 0・わざと壊して赤を見る）:
  1. ／ 2. **追跡の検査** —— 9 つの表がどれも `FR-104`〜`FR-110`・`FR-043`・`FR-009`・`FR-044` のどれかから「表 X」と名指され、どの要求も表を 1 つ以上名指す。規則は `FR-104` の RATIONALE か 1.9 節に 1 文足す。
  3. **決定表の検査** —— RF・LP・PE の条件の列を「—」「（問わない）」まで展開して、隙間も重なりも無いこと。条件の列と値の域は検査の側に書き、規則は 1.9 節の表記規約に 1 行足す。
  4. **掃引の 1 件**（どの形もズームの両端で応える点を 1 つ以上持つ）を `check.sh` から `npx --no-install vitest run <その 1 本>` で呼ぶ。
  - 壊し: ① `FR-106` から「表 PK」を消す ② `FR-104` から表を全部消す ③ LP の 1 行を消す ④ GA のマーカーの値を 0 にする。

---

## 9. 書くときの作法（⛔ 当てる体はここを読んでから書く）

- ⛔⛔ **置き換える文字列が 1 度しか現れないことを、書き換える前に数える。** `count(...) == 1` を主張してから `replace` する。⛔ `find` で最初の 1 件を取らない。⚠️ **`3d342753` で数えた、2 度以上現れる文字列**（`01-04-requirements.md` での回数）—— 綴りの組のまま: `` `GR-9` / `GR-17` / `GR-18` `` 5、`` `GR-1` / `GR-2` `` 11。1 つずつ: `` `GR-9` `` 35、`` `GR-17` `` 24、`` `GR-18` `` 10、`` `GR-1` `` 14、`` `GR-2` `` 12、`` `S-90` `` 15（`settings.json` に 4）、`` `S-137` `` 10（同 3）、`` `FR-002` `` 8、`` `LF-11` `` 8（`05-07` に 3）。語: 「表 T-023d の結び」15（`settings.json` に 9、`tbl-settings.md` に 9）、「実績バーの右端の外側」5、「表 T-013」9（`05-07` に 3、`settings.json` に 1）、「掴みシロ」26 ⇒ **広い錨を使う。**
- ⛔ **「同・」「同上」で前の行を継ぐ表から、頭の行を退役させない**（4.3 節）。
- **StrictDoc の罠**:
  - ⚠️ **CJK の直後に `**` を開き、その直後にコードスパンを置かない**（例「表 GA の\*\*`GA-3`\*\*」）。句読点や ⚠️ の直後は安全。
  - ⚠️ `****` を作らない（片方が生の `**` になる。表の検査は見えない —— 刊行物の描画検査だけが見る）。
  - ⚠️ **変更履歴の行に生の `|` を書かない**（列が割れる）。
  - ⚠️ **「表 X の N 行」と書くと検査が「X は N 行を持つ」と読む** —— 生成物の何が動いたかは**行 ID を名指す**。
  - ⚠️ **値の表・用語集に `（MUST）` を書かない**（値は表、規則は要求）。
  - ⚠️ **`A-17` が単独で使うことを禁じる語を書かない**（その語は `A-17` の行が持つ。⛔ 本書に写さない。測った: 仕様案も本書も 0 件）。
  - ⚠️ **検査 46（文の切れ目ごとに改行）** —— 書いたあと `python tools/fix_line_breaks.py <file>`。表の中は `<br>`。
  - ⚠️ 9 つの表を差し込んだあと、空行が 2 つ続いていないか見る。
- ⛔ **生成物を手で直さない** —— `fig-erd-detail.md`（検査 16。原稿は `_source/erd.json`）、`tbl-settings.md`・`tbl-row-id-prefixes.md`・`tbl-property-items.md`（検査 21・27）。
- ⛔ **接頭辞に行番号を付けた形（`GA-1` など）を `docs/development-records/` に書かない**（検査 27）—— 本書（`change-request/`）と `docs/spec` は登録するので構わない。
- **道具**: `PYTHONIOENCODING=utf-8`。スクリプトはヒアドキュメントではなくファイルに書いて走らせる（ヒアドキュメントは日本語の手術で逃がし文字を壊す）。**改行は CRLF と LF が混ざる** ⇒ 読みで正規化し、書き戻しは多数派に揃える。⚠️ 本書が触った 3 つの文書（対応表・仕様案・同梱の洗い出し）は測って LF だけだった。

---

## 10. 関係

- `handoff.md` §1.2（段取り。Step 1b ＝ 本書）、`rulings.md` の `JDG-180`〜`JDG-254`。
- 前に入った `CR-429`（MSPDI を両方の版で読み書きする）—— 同じ 6 つのファイルを触るので**同時に走らせない**という約束のもと、先に着地した。本書の行番号はその後の木で測った。
- `CR-421`（ダミーは半分のマーカー幅）・`CR-422`（フェードの掴み点）・`CR-427`（端点のポインタ）—— 返す問いは本書の表で決着させる。
- 台帳: `DFC-449`・`DFC-450`・`DFC-563` の系ではない。`DFC-636`・`DFC-649`・`DFC-652`・`DFC-653`・`DFC-657`〜`DFC-664`（6.2 節）。

---

## 11. ⛔ この変更でやらないこと ／ 返す問い

**やらないこと**:

- 仕様書を、利用者が本書を読んで当ててよいと言う前に変えない。
- **実装と試験を本書では直さない**（6.3 節に所だけ書く）。⛔ ただし commit は分けられない（7.2 節）。
- **基準線ファイルを触らない**（検査 37・39・42・43・44）—— 利用者の許しを得てから、前に立つ者が直す。
- 表 GA の値の正規化（Step 2）を先取りしない。⛔ 行が減ったら 5 節の予測を数え直す。
- 予定の端の**内側**の上限の規則を立てない（既定 0 のまま。上げると決めたときに足す。`JDG-216`）。
- `S-181` の上限の**数**（30）を問い直さない（決定 5 は理由の文だけを直す）。
- キーボードで選択を動かすこと、範囲選択が「囲んだ」と読む外形（同梱の洗い出しの 22・23）—— 掴み代と独立している。
- マイルストーンに再開アイコンを描かない（今の MUST NOT のまま。点は期間を持たない）。

**返す問い**: **0 件。**

- 仕様案 §8 の問い（群 A〜H）は 2026-09-19〜20 にすべて答えが出ている（`JDG-209`〜`JDG-249`）。⛔ 問い直さない。
- 本書の起草で新しく決めた 9 つは、どれも 0 節の ③ に導きと代償を添えて書いた（利用者はあとから覆せる）。
- ⚠️ **許しを求めるもの（問いではない）**: 7.2 節の基準線の動き —— 検査 37・39・42・43（83 → 17 の見込み）・44 を、数を添えて 1 通で。

---

## 12. ⭐ 当てる体が CR の文から変えた所（2026-09-20）

⛔ 中身の判断は 1 つも変えていない。変えたのは、CR が名で呼んでいたものに仕様の番号を与えたことと、
CR が値を与えていなかった欄を仕様の作法から導いたことである。

| # | CR の文 | 当てた形 | なぜ |
|---|---|---|---|
| 1 | 表 GA ・ 表 HT ・ 表 TY ・ 表 PK ・ 表 PE ・ 表 XS ・ 表 RF ・ 表 LP | **表 T-266（`GA`）・表 T-267（`HT`）・表 T-268（`TY`）・表 T-269（`PK`）・表 T-270（`PE`）・表 T-271（`XS`）・表 T-272（`RF`）・表 T-273（`LP`）** | ⭐ 5 節の数の予測（tables 164 → 168）は `md-checks.py` の `tables=` で突き合わせると書いてあり、その数は `**表 T-nnn —` の見出しだけを数える。名だけの見出しでは 4 つしか増えず、168 に届かない。⭐ 番号にすると検査 5 ／ 7 ／ 8 が参照を守り、登録簿の「定義する表」の欄もほかの接頭辞と同じ形になる。行 ID の接頭辞は CR のとおり `GA` 〜 `LP` のままである |
| 2 | 停止日からの破線の「太さ 1.2px」（§5） | `resumeDashWidth` の既定 **1.92**（表示の倍率 100 で描くと 1.2px） | ⭐ 仕様案 §5 の前文が「見本の描く寸法を仕様の単位で持ち、× 0.625 して描く。既定は画面で見た大きさを 0.625 で割った値」と定めている。その作法をそのまま当てた |
| 3 | 新しい設定値の下限・上限 | 置き換える行の範囲を引き継ぐか、同じ群のほかの行と揃えた（札の px は 0 〜 30、`--->` の矢じりと端点スパンの点は同じ範囲、引き入れの下限は `dependencyArrowLength`） | ⚠️ 表 T-201 の行は下限と上限の欄を持たずに存在できない。CR は既定しか与えていないので、⛔ 新しい数を発明せず、置き換える行の範囲か隣の行の範囲を引いた |
| 4 | 「`A-appendix.md` の変更履歴に 1 行」（6.2 節） | **足していない** | ⭐ `A-appendix.md` の `A.3 Changelog` は 2026-09-11 に中身を `changelog.md` へ移しており、「変更履歴は `docs/development-records/changelog.md` が持つ」の 1 文しか無い。足せる行が無い |
| 5 | 「表 T-104 の鍵の `K-19` 書き直し」（7.1 の 3） | **字を変えていない** | ⭐ `K-19` の日本語は既に「矢印の三角形の長さ」であり、対応表が求める「長さだけにする」をそのまま述べている。⚠️ 画面に出る語なので、⛔ 同じ意味の言い換えを発明しなかった。底辺は新しい `K-128`（矢印の三角形の底辺）が持つ |
| 6 | 裁定の逐語の引用（`JDG-nnn`） | 仕様の原稿には 1 つも書いていない | ⛔ 検査 7 は `JDG-nnn` を「定義の無い行 ID」として赤にする（裁定は `docs/development-records` に住み、`docs/spec` の索引に無い）。今の仕様も `JDG-` を 0 件しか持たない |
| 7 | 図 6 枚 | markdown のリンクと図の見出しだけを置いた（`_assets/fig-*.svg` は未作成） | ⭐ 図の書き出しは別の体の持ち場である（本書 4.4 節）。検査 49（刊行物の描画）は見出しと表だけを見るので緑であり、絵が出ないだけである |
