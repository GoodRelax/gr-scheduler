# CR-238 — 告げなければならないのに、運べる理由が無かった 8 つ

> **閉じるもの**: ⛔ **`FR-076` は「通知が運ぶ理由は 表 T-233 の行とすること（MUST）。同表に無い理由を運んではならない（MUST NOT）」と定める。**
> ⛔ **ところが 8 つの場面は、別の要求が MUST で通知を求めているのに、運べる行を持っていない。** 告げる道が仕様の側で塞がっている。
> ⭐ **利用者の裁定（2026-08-24）: 一括で立てる。**
> ⭐ **あわせて `ST-7` の安全弁を、例外ではなく値で告げる形に改める**（`PD-178`。利用者の裁定 2026-08-23）。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`（説明を読まずに操作できるようにする）。受ける目標は `GL-006`。**
⛔ **告げられない失敗は、人から見れば「何も起きなかった」と区別が付かない。**
⚠️ **監査（2026-08-23）が測ったとおり、`schedule.ts` の `throw` は誰にも捕まらず、画面が黙って止まる経路になっていた。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R3.1`**（エラーを握り潰さない）| `notifyChangeWatchers` の返り値 | ⛔ **呼び手が捨てている。** ⚠️ **その単位自身が「黙って飲み込むことは、その答えが唯一排除した選択肢である」と注記している** |
| **`R1.4`**（欠落が無い）| `FR-025` / `FR-026` / `FR-065` / `FR-086` / `FR-088` の通知 | ⛔ **5 要求が MUST で通知を求めるのに、運べる行が 表 T-233 に 1 つも無い** |
| **`R1.1`**（矛盾が無い）| `ST-7` と `FR-028` | ⛔ **一方が「エラーを出して止める（MUST）」、他方が「例外を投げてはならない（MUST NOT）」と定め、本文が調停していない** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **行は `RS-16` の後、`RS-15` の前に置く** | **`RS-15` は落ち先なので末尾に居る。** ⛔ 印刷順は表の裁定なので、数字順に並べ替えない |
| 2 | ⭐ **`RS-23`（監視の配り先が答えを返さなかった）の正を 表 T-035 の `AG-6` とする** | ⚠️ **配信そのものの失敗を述べた MUST は無い。** `AG-6` は監視が何を通知するかを定める行であり、その配達が失敗した場面としてぶら下げた。⛔ **`RS-3` が 表 T-004 の `LM-14`（制限事項）を正に採った先例に倣う** |
| 3 | ⭐ **安全弁の行は 1 本にする** | **表 T-233 が「同じものに 2 つ目の鍵を作らない」と自ら書いている。** `layoutFromSchedule` が達したときと `FR-033` の貼り付けが達したときは、**同じ安全弁（`S-89`）の同じ到達である** |
| 4 | ⭐ **`ST-7` の「黙って切り捨てても、重ねて押し込んでもならない（MUST NOT）」は 1 文字も動かさない** | **変えるのはどう告げるかだけである** |
| 5 | ⭐ **表 T-064 の `PI-5` を動かさない** | **同表の前書きが「本表は名前と、それが何を担うかだけを持つ。引数・戻り値は `src/` の公開エントリが持つ」と定めている。** ⛔ 戻り値を表へ書くと前書きに反する |
| 6 | ⭐ **`FR-033` を動かさない** | 「段が 表 T-014 の `ST-7` の安全弁に達したときは、貼り付けを受け付けずに通知すること（MUST）」は**既に値で答える形である** |

---

## 1. 測った

```
induced.py  NT-7 FR-031 FR-099 UN-17 T-027 FR-038 S-99 T-206 T-233
            NT-4 FR-060 FR-026 FR-065 FR-086 T-014 ST-7 FR-028 FR-033 T-064
```

| 測ったこと | 実測 |
|---|---|
| 種のうち仕様書に在るもの | 19 / 19 |
| 閉路 | **1 つ**（size 2: `FR-031` `NT-7`）|
| ⛔ **本 CR がその閉路を踏むか** | ⭐ **踏まない** —— 8 行はいずれも**告げるだけ**で、続けてよいかを問わない |
| 表 T-233 の既存の行 | 16（`RS-1` 〜 `RS-16`。`RS-15` が落ち先で末尾）|

⛔ **引継書の「表 T-233 に `NT-4` の 4 行 —— `FR-060` / `FR-026` / `FR-065` / `FR-086`」は 4 つとも真ではなかった:**

| 引継書 | 実測 |
|---|---|
| `FR-060` の起動時の復帰 | ⛔ **`RS-1` が既に持つ**（辞書の語も申し出そのものである）。**同じものに 2 つ目の鍵は作らない** |
| `FR-065` の起動時の有効化 | ⛔ **起動時に申し出ると書いた要求本文が 1 行も無い。** ⭐ 立てられるのは「既に渡した参照は取り消せない」の側で、作法は `NT-5` である |
| `FR-026` | ⭐ **1 場面ではなく 3 場面**（復旧の申し出・壊れて復旧できない・履歴は戻らない）。⛔ **うち復旧の申し出は問い側であり、`CR-237` の `QN-7` が持つ**（利用者の裁定）|
| `FR-086` | ⭐ 真である（`UC-009` 拡張 2a）|

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-014 の `ST-7` のセル | 「達したらエラーを出して処理を止めること」を「そこで処理を止め、達したことを判別できる値で返して人に通知すること（MUST）。例外を投げてはならない（MUST NOT）」に改める |
| 2 | 表 T-233 | 8 行（`RS-17` 〜 `RS-24`）を `RS-16` の後・`RS-15` の前に足す |
| 3 | `_source/display-words.json` | `reasons` 節に 8 項目（`text` と `nextStep`）|
| 4 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 134 | **134** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1642 | **1650** | **＋8**（`RS-17` 〜 `RS-24`）|
| `uids` | 148 | **148** | 0 |

⚠️ **辞書の枠は 224 → 240 になる**（8 行 × `text` と `nextStep` の 2 欄）。
⚠️ **`ST-7` はセルの書き換えなので行は増えない。**
