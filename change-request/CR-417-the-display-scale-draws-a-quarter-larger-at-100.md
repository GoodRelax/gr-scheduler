# CR-417 — 表示の倍率 100 の描く比を 0.625 にする（いまの 125% を新しい 100% とする）

> **閉じるもの**: 利用者の裁定 `JDG-169` の ①（2026-09-18）と、問 1 への答え。逐語は `docs/development-records/rulings.md` の `JDG-169` が持つ。
> - 「ちょっとフォントが小さすぎだな...<br>現在の125%をデフォルトの100%のサイズにしろ   i.e., 全体的に1.25倍する」
> - 問 1（倍率を 1.25 倍するとき、ヘッダーとコマンドパレットは変えないか）への答え: 「問1:  ヘッダーやパレットの絶対サイズは変えるな。 今のサイズが一番操作しやすく、画面効率も良い。」
>
> ⭐ **本書は仕様だけを当てる。** 表 T-206 の `S-236` を 0.5 → 0.625 にし、描く比から導いた散文（`FR-039` の描く比の例、`FR-077`、表 T-252 の `DS-1` の理由、`S-234` / `S-235` / `S-34` / `S-236` の注）を数え直した。段の一覧（`S-234`）は変えていない。読んだ木は `b6648920`。
> ⚠️ `CR-417`〜`CR-427` の 11 本は、`induced.py` が 33 員の閉路（第 3 節）を返したので、1 つの計画で 1 度に当てた。各オブジェクトは 1 度だけ書いてある。第 3 節（グラフ）・第 5 節（数の合計）・第 6.3 節（実装と試験の共通の所）は本書が 11 本ぶんを持つ。
> ⚠️ `src/`・`tests/`・`tools/` と基準線は触っていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（全体日程を 1 画面で見られる。`CH-2` の受ける目標）—— 字が小さすぎて読めない画面は、1 画面に収まっていても一覧にならない。⭐ **`GL-006`**（`CH-4` の受ける目標）—— ヘッダーとパレットは「いまの大きさが一番操作しやすい」ので動かさない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— `S-236` の 0.5 から導いた文が 6 か所ある（`FR-039` の描く比の例、`FR-077` の「出荷ビルドの 1/2」と「0.5」、表 T-252 の `DS-1` の「1/2」、`S-234` の注の「1/2」「同じ大きさ」、`S-235` の注の「0.5」、`S-34` の注の字の床 6px と縁取り 0.6px）。6 か所とも数え直した。
- **`R1.3`（唯一の正がある）** —— 描く比の値は `S-236` だけが持つ。`FR-039` は式と例だけを持つ。
- **`R1.2`（非検証な表現を排す）** —— 例に式を添えた（100 ÷ 100 × 0.625、200 ÷ 100 × 0.625 ＝ 1.25）。ヘッダーとパレットが変わらないことを、入口の外形の幅の式（17.3342px）で読めるようにした。
- **`R2.18`〜`R2.20` は当たらない** —— 名前を足していない。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | `S-236` を 0.625 と書き、注に「2/3 × 3/4 × 5/4 ＝ 5/8、丸めていない」と式を添える | ⭐ 逐語「現在の125%をデフォルトの100%のサイズにしろ   i.e., 全体的に1.25倍する」—— いまの 125 の描く比は 125 ÷ 100 × 0.5 ＝ 0.625 | 無し |
| 決定 2 | `S-234` の段（50 … 200）と既定 100 を変えない | ⭐ 前に立つ者の問いの要約「step list unchanged」に利用者は異を唱えていない。段の数字は描く比と独立である（`FR-039` の式）| 200 の描く比が 1.25 になり、2026-09-16 の出荷ビルドより大きく描く |
| 決定 3 | `S-235`（0.6667）と `DS-6`（ヘッダーとパレットに掛けない）を変えず、`FR-039` の描く比の段に「ヘッダーとパレットは描く比を読まない」⚠️ を 1 文足す | ⭐ 問 1 の答え「ヘッダーやパレットの絶対サイズは変えるな」。⭐ `FR-051` / `FR-053` / `FR-029` はヘッダーとパレットを `S-235` だけで描かせている（第 1 節）| 無し |
| 決定 4 | 保存した `displayScale` を読み替えない —— 100 で保存した文書は、新しい描く比 0.625 で開く | ⭐ `JDG-152` の「読み替えは無用」と同じ扱い。`S-236` は保存しない製品の基準である（`S-236` の注）| 以前に保存した文書も 1.25 倍で開く（利用者の求めどおり）|
| 決定 5 | `FR-039` の行見出しパネルの床の段の条件式は、`CR-418` の `S-79` 300 と一緒に 1 度で数え直す | ⭐ 式が描く比と `S-79` の両方を読むので、片方ずつ書くと 2 度書く（規則 02 の 5）| 本書だけを読むと、その段の数え直しが見えない（`CR-418` の第 4 節）|

---

## 1. 読んで確かめた事実

| 事実 | 所（`b6648920`、当てる前）|
|---|---|
| `S-236` ＝ 0.5、注は「2/3 × 3/4 ＝ 1/2」| `docs/spec/_source/settings.json:3321`〜`:3331`（刊行物 `docs/spec/_assets/tbl-settings.md:358`）|
| 描く比 ＝ `S-234` ÷ 100 × `S-236`、例「既定の 100 で … 1/2（100 ÷ 100 × 0.5）、200 で … 同じ大きさ（… ＝ 1）」| `docs/spec/01-04-requirements.md:6297` |
| `FR-077`「出荷ビルドの 1/2 … `S-236` の 0.5」| 同 `:4179` |
| 表 T-252 の `DS-1` の理由「出荷ビルドの 1/2 まで縮められない」| 同 `:6329` |
| `S-34` の注「字の床 6px（`S-8` の 12 × 描く比 0.5）で 0.6px」| `settings.json:895` |
| `S-234` の注「既定の 100 は … 1/2 の大きさ、200 は … 同じ大きさ」、`S-235` の注「`S-236` の 0.5 は既に違う」| `settings.json:1693`、`:3317` |
| `App Header` と `Command Palette` は表示の倍率を掛けず `S-235` を掛けて描く | `01-04-requirements.md:3746`〜`:3748`（`FR-053`）、`FR-051`、表 T-252 の `DS-6`（`:6334`）、`FR-029` の「箱の一辺と隙間と枠の線の太さには、どの面でも `S-235` を掛けて描く」（`:5858`）|
| `S-236` を指すのは `FR-039` と `FR-077` だけ（`impact.py S-236`: 要求 2 件 / 参照 4 箇所）| 第 3 節 |

## 2. 測った

⛔ 描いた絵では測っていない。数はどれも式から `fractions.Fraction` で求めた（丸めていない）。

描く比 ＝ `S-234` ÷ 100 × `S-236`（`FR-039`）。

| `S-234` | 50 | 67 | 75 | 90 | 100 | 110 | 125 | 150 | 175 | 200 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 当てる前（× 0.5）| 0.25 | 0.335 | 0.375 | 0.45 | 0.5 | 0.55 | 0.625 | 0.75 | 0.875 | 1 |
| 当てたあと（× 0.625）| 0.3125 | 0.41875 | 0.46875 | 0.5625 | 0.625 | 0.6875 | 0.78125 | 0.9375 | 1.09375 | 1.25 |

- 新しい 100 の描く比 0.625 は、当てる前の 125 の描く比と等しい（利用者の指定そのもの）。
- 字の床（`FR-077`）: `S-8` 12 × 0.625 ＝ 7.5px。縁取り（`S-34` 0.10）: 0.75px。
- ヘッダーとパレットの入口の外形の幅: （`S-138` 16 ＋ `S-141` 4 × 2 ＋ `S-237` 1 × 2）× `S-235` 0.6667 ＝ 17.3342px —— `S-236` を読まないので、当てる前と同じ。

## 3. グラフ（`CR-417`〜`CR-427` の 11 本ぶん）

**当てる前**（`impact.py`、`b6648920`）: `S-236` —— 要求 2 / 参照 4。`S-234` —— 要求 7 / 参照 23。`S-235` —— 要求 6 / 参照 16。`S-3` —— 要求 2 / 参照 4。`S-2` —— 要求 2 / 参照 5。`S-36` —— 要求 3 / 参照 5。`S-79` —— 要求 4 / 参照 10。`S-23` —— 要求 2 / 参照 6。`S-180` —— 要求 4 / 参照 7。`S-92` —— 要求 2 / 参照 6。`S-109` —— 要求 0 / 参照 1。`S-22` —— 要求 3 / 参照 11。`S-80` —— 要求 1 / 参照 5。`S-171` —— 要求 1 / 参照 3。表 T-252 —— 指す要求 6、2 次 28。表 T-240 —— 2、2 次 10。表 T-036 —— 15、2 次 39。表 T-028 —— 15、2 次 44。表 T-260 —— 1、2 次 18。表 T-253 —— 1、2 次 2。表 T-210 —— 1、2 次 1。`FR-039` —— 要求 18 / 参照 49。`FR-043` —— 10 / 36。`FR-013` —— 8 / 21。`FR-052` —— 6 / 16。`FR-072` —— 6 / 9。`FR-036` —— 5 / 16。`FR-016` —— 3 / 17。`FR-002` —— 6 / 12。`FR-102` —— 2 / 6。`FR-077` —— 2 / 6。

**`induced.py`**（11 本が触る 81 種。⚠️ 当てたあとの木で走らせた —— 当てる前に走らせなかったのは規則 02 の 2 の手順からの逸脱である。すべての編集を 1 つの計画にまとめ、ファイルごとに 1 度の書き込みで当てたので、閉路の員を別々の巡で書いてはいない）: 81 種のうち 80 種が解決（図 F-019 は仕様のオブジェクトとして引けない）、辺 147、閉路 3 —— 大きさ 2（`FR-036` `IC-102`）、大きさ 2（`PC-5` `S-249`）、大きさ 33（`DM-3` `DM-9` `DS-1` `DS-6` `FR-002` `FR-013` `FR-016` `FR-039` `FR-043` `FR-052` `FR-077` `FR-094` `GR-7` `LF-11` `OR-1` `S-131` `S-171` `S-180` `S-2` `S-22` `S-233` `S-234` `S-235` `S-236` `S-245` `S-246` `S-247` `S-248` `S-3` `S-36` `S-79` `S-80` `ZE-1`）。⭐ 33 員の閉路が `CR-417`〜`CR-421`・`CR-423`・`CR-424` にまたがるので、11 本を 1 つの計画で 1 度に当てた。大きさ 2 の 2 つは、値の行とその規則を持つ要求の作法の閉路である。

**当てたあと**（新しいオブジェクト）: 表 T-262 —— 指す要求 1、2 次 2。表 T-263 —— 1、2 次 2。表 T-264 —— 1、2 次 1（要求以外から 1）。`S-245` —— 要求 1 / 参照 3。`S-246` —— 1 / 2。`S-247` —— 1 / 2。`S-248` —— 1 / 5。`S-249` —— 1 / 1。

**届いた行を `rulings.md` で引いた**（`jdgref` の走査: 触る ID を名指す行）:

| 行 | 名指す ID | 本 11 本との関係 |
|---|---|---|
| `JDG-152` | `S-236` | 「0.5 ちょうど」を覆す（`JDG-169`）⇒ 状態を覆された（一部）に |
| `JDG-131` | `S-79` | ② の「名前 → マーカー」を覆す（`JDG-170`、`CR-420`）⇒ 覆された（一部）に。① の `S-79` / `S-37` に倍率を掛けることは変えない |
| `JDG-27` | `GR-7` | 「マーカーは掴みシロの外側」を予定の終了点の掴み代について覆す（`JDG-178`、`CR-421`）⇒ 覆された（一部）に |
| `JDG-36` | `S-180`、`GR-9` | 「ダミーを長さ 1 日の実績バーとして描く」を幅について覆す（`JDG-177`、`CR-421`）⇒ 覆された（一部）に |
| `JDG-156` | `S-109`、`S-92` | 3.5 と「`S-92` は変えない」を覆す（`JDG-179`、`CR-422`）⇒ 覆された（一部）に |
| `JDG-41` | ダミーの当たり判定 | 「印の外へ広げない」を守る —— `CR-421` の決定 3 |
| `JDG-37` / `JDG-89` / `JDG-100` | `GR-9` | 印を左右に割ること、ダミーが予定の開始日に立つことは変えない |
| `JDG-114` | `IN-2` | ②（依存線を構えているあいだは既定の矢印）は変えない —— 表 T-264 の後の段 |
| `JDG-68` | `IF-9` | `IN-5a` の後の段は `AG-9` の状態を広げない（`CR-426` の決定 2）|
| `JDG-88` / `JDG-110` / `JDG-115` | `FR-016` | 字の側の上限の規則は変えない。数（1.173464 → 1.760197）だけが `CR-418` で動く |
| `JDG-159` / `JDG-133` | `FR-052`、`S-80` | 押した時点に描かれていた幅から数えること、床で止めて描くことと同じ形で下限を足す（`CR-424`）|
| `JDG-81` / `JDG-79` / `JDG-73` / `JDG-155` | `FR-036`、`IC-102` | 題の行の並び、凡例、語は変えない（`CR-425`）|

## 4. 当てた中身

| 対象 | 置き場（当てたあと） | 中身 |
|---|---|---|
| `S-236` | `docs/spec/_source/settings.json`（→ `_assets/tbl-settings.md:358`）| 0.625。注に 2/3 × 3/4 × 5/4 ＝ 5/8、100 → 0.625・200 → 1.25・50 → 0.3125、「`App Header` と `Command Palette` の大きさは本行を読まない」|
| `S-234` の注 | 同（→ `:170`）| 「既定の 100 は … 5/8 の大きさ、200 は … 5/4 の大きさ」|
| `S-235` の注 | 同（→ `:357`）| 「`S-236` の 0.625 は既に違う」|
| `S-34` の注 | 同（→ `:105`）| 字の床 7.5px（12 × 0.625）で 0.75px |
| `FR-039` の描く比の文 | `docs/spec/01-04-requirements.md:6353`〜`:6354` | 例を 5/8（100 ÷ 100 × 0.625）と 5/4（200 ÷ 100 × 0.625 ＝ 1.25）に。⚠️ ヘッダーとパレットは描く比を読まない（外形の幅 17.3342px のまま）|
| 表 T-252 の `DS-1` の理由 | `:6386` | 「出荷ビルドの 5/8 まで縮められない」|
| `FR-077` | `:4216` | 「出荷ビルドの 5/8 … `S-236` の 0.625」|

## 5. 数の予測（11 本の合計）

当てる前 `tables=160 figures=11 rows=2018 uids=153`（`b6648920` の `check.sh` の実測）。

| 本 | tables | rows | 新しい行 |
|---|--:|--:|---|
| `CR-417` | 0 | 0 | — |
| `CR-418` | 0 | 0 | — |
| `CR-419` | 0 | +2 | `S-245` `S-246` |
| `CR-420` | 0 | 0 | — |
| `CR-421` | 0 | +1 | `S-247` |
| `CR-422` | 0 | 0 | — |
| `CR-423` | +1 | +5 | 表 T-262 の `ZE-1`〜`ZE-5` |
| `CR-424` | 0 | +1 | `S-248` |
| `CR-425` | 0 | 0 | — |
| `CR-426` | +1 | +4 | `IN-5b`、表 T-263 の `IR-1`〜`IR-3` |
| `CR-427` | +1 | +8 | 表 T-264 の `PC-1`〜`PC-7`、`S-249` |
| 合計 | +3 | +21 | figures 0 ／ uids 0 |

⭐ 予測 `tables=163 figures=11 rows=2039 uids=153` —— 当てたあとの `check.sh` の実測と一致した。
⭐ 足した `（MUST）` は 33、`（MUST NOT）` は 15（本書は 0 と 0）。数え方: `git diff -U0` の `docs/spec/01-04-requirements.md` と `05-07-design.md` の塊ごとに、足した行と消した行の印の数を数えて差を取った（本ごとの内訳は各本の第 5 節）。

## 6. 影響

### 6.1 仕様

| 所 | 本書で |
|---|---|
| `FR-051` / `FR-053` / `FR-029`（ヘッダーとパレットと入口に `S-235`）| そのまま —— 描く比を読まない |
| `S-234` の段と既定 | そのまま |
| `FR-016` の字の側の上限 | そのまま —— 両方の字に同じ比が掛かる |
| `FR-039` の行見出しパネルの床の段の条件 | `CR-418` と一緒に数え直した |
| `S-243` の注の帯の床 24.0012px | そのまま —— `S-235` だけを読む |

### 6.2 記録（11 本ぶん）

| 所 | 書いたこと |
|---|---|
| `docs/development-records/rulings.md` | 節「2026-09-18」に `JDG-168`〜`JDG-179` を足し、`JDG-27` / `JDG-36` / `JDG-131` / `JDG-152` / `JDG-156` の状態を覆された（一部）に変えた |
| `docs/development-records/defects.md` | `DFC-647`〜`DFC-653` を足し、`DFC-610` / `DFC-643` を `取下げ`（`JDG-168`）、`DFC-641` / `DFC-642` / `DFC-644` / `DFC-646` に追記。`tools/ledger_metrics.py` で件数を刷り直した |
| `docs/development-records/pending-decisions.md` の `PND-496` | 答え（`JDG-173`）を書いた。⚠️ 状態は `未裁定` のまま —— `@provisional PND-496` の印が `src/` に残るので、実装の波が印を消してから `裁定済` にする（検査 25）|
| `docs/development-records/changelog.md` | 2.36〜2.46 の 11 行 |

### 6.3 実装と試験で直す所（11 本の共通。本書は直さない）

| 所 | 何が動くか |
|---|---|
| `npm run gen` | ⭐ 本波は `src/` を書く生成器を走らせていない ⇒ 検査 20（`document-settings.ts` / `item-hit-area.ts` / `screen-regions.ts` / `schedule-layout.ts`）と検査 27（`json-codec.ts`、`icon-glyphs.json`、`help-roster.json`）が赤い。`S-236` 0.625、`S-3` の係数、`S-36` 19.5、`S-79` 300、`S-92` 8、`S-109` 2.5（下限 2.5）、`S-245`〜`S-249` を刷る |
| `tools/generate_entity_types.py` の `derived_defaults` | ⚠️ `S-3` の既定の欄は `index` に `times: 1.5` を足した。いまの生成器は `index` の枝で `times` を読まない ⇒ 読むように直さないと、生成された既定値が 14 のまま |
| 検査 37 | `T-036` の `SK-2` / `SK-3` / `SK-4` / `SK-5` / `SK-11` と `T-104` の `K-23` の指紋が動いた（`SK-17` は波の前から赤い）—— 行と辞書の語を読み合わせてから基準を書き直す（前に立つ者） |
| 検査 39 | 印の数が基準線 1401 に対し 1421（当てる前の実測 1370）—— 足した 48 の条項を試験が逐語で握るまで赤い。⚠️ 基準線を上げるかは利用者に問う |
| 検査 42 | 試験のコメントが引く文のうち、仕様から消えた数が 384 → 416 —— 下の試験の引用を直すまで赤い。⚠️ 基準線を上げるかは利用者に問う |
| 試験（逐語や値を持つ候補。`grep` で拾い、中身は読んでいない）| `tests/unit/cr-394-the-chart-has-a-display-scale.test.ts`、`cr-395-…`、`cr-400-…`、`cr-409-…`、`cr-414-…`、`cr-416-…`、`cr-380-381-384-…`、`cr-393-…`、`fr-043-dummy-drawn.test.ts`、`fr-043-b-a-milestone-dummy-box-is-square.test.ts`、`t-023d-the-hit-box-starts-at-the-day.test.ts`、`t-023d-the-plan-start-is-the-boundary.test.ts`、`t-023c-selection-points-at-what-exists.test.ts`、`fr-036-assignment-of-an-entrance.test.ts`、`in-2-pointer-shape.test.ts`、`in-2-a-the-dummies-say-slide.test.ts`、`uf-32.test.ts`、`fr-013-pointer-on-the-figure.test.ts`、`t-038-oc-2-labels-in-the-occupied-width.test.ts`、`tests/integration/schedule-drawing.sws.test.ts`、`tests/fixtures/display-scale.ts`、`tests/fixtures/row-name-font.ts` |
| ⛔ vitest | 本波は `node_modules` を持たない木で書いたので vitest を回していない（規則 02 の 6 から外れる）—— 合流の前に回すこと |

## 7. 試験が主張すべきこと

⭐ 名前は本書のとおりに写すこと: `S-236`（`0.625`）。

1. `S-236`: 生成された値が 0.625 である。
2. `FR-039`: 描く比が `S-234` ÷ 100 × `S-236` —— 100 で 0.625、200 で 1.25、50 で 0.3125。⭐ 古い実装（100 で 0.5）を赤にする。
3. `FR-077`: 表示の倍率 100 の可読の下限が 7.5px（`S-8` 12 × 0.625）。
4. `DS-6`: 表示の倍率を 50 と 200 で切り替えても、`App Header` の入口の外形の幅が 17.3342px のまま（`S-235` だけから導く）。`Command Palette` の入口も同じ。
5. `FR-039`: `displayScale` = 100 を保存した文書は描く比 0.625 で開き、保存値は 100 のまま。

## 8. 関係 —— `JDG-169`

- ⭐ **`JDG-169` の ① を着地させる** —— 「現在の125%をデフォルトの100%のサイズにしろ」は `S-236` と `FR-039` の例、「問1: ヘッダーやパレットの絶対サイズは変えるな」は `DS-6` と `FR-039` の ⚠️ の文。
- ⭐ **`JDG-152` の「100% のときの描く比を 0.5 ちょうど」を覆す** —— 状態を覆された（一部）に変えた。

## 9. ⛔ この変更でやらないこと ／ 返す問い

- ⛔ `S-234` の段と既定を変えない。⛔ `S-235` を変えない。
- ⛔ `src/`・`tests/`・`tools/` と基準線を編集しない（第 6.3 節）。

**返す問い**: 無し（本書の分）。11 本の返す問いは各本の第 9 節と、前に立つ者への報告にまとめた。
