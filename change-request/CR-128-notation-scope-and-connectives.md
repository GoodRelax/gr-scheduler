# CR-128 — 記法の射程を定め、接続語の規則を明文化する

## 1. 変更概要

**1.9 の記法規約に、2 つの穴がある。**

| # | 直すもの | 置き場 |
|---|---|---|
| ① | ⭐ **表 T-006a の射程を明記する** —— 本表が縛るのは**仕様対象のソフトの中の名前**であり、仕様書を組み立てる道具は対象外である | 1.9（表 T-006a の直前） |
| ② | ⭐ **接続語の規則を足す** —— `Of` は比率に限り、変換と導出は `From`、引き当ては `By` を使う | 1.9（略語の規則の直後） |

⭐ **どちらも書き換え 0 箇所である**（§2-3・§2-4 で実測）。**表も要求 `UID` も行 ID も新設しない。散文が 2 か所増えるだけである。**

⚠️ **本 CR は表 T-006a の 11 行に 1 文字も触れない。** 足すのは表の外の散文である。

## 2. 変更の背景・目的

### 2-1. グラフで調べた —— **影響は 1.9 と 5.3 に閉じている**

```bash
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py T-006a W-2 W-11
```

| 測ったもの | 結果 |
|---|---|
| 表 T-006a を指している要求 | **0 件。** 参照は 1.9（2）・5.3（3）・6.2（1）・A.3 変更履歴（1）・用語集の「UI パーツ」節（1） |
| `W-2` を指している箇所 | **要求 0 件 / 参照 2 箇所** —— `01-04-requirements.md:308`（`W-8` が記法を `W-2` に委ねている）と `05-07-design.md:181` |
| `W-11` を指している箇所 | **要求 0 件 / 参照 2 箇所** —— `05-07-design.md:175` と `A-appendix.md:44` |

**要求の側から表 T-006a を指すものが 1 つも無い。** 散文を 2 か所足しても要求は動かない。

### 2-2. 穴① —— **表 T-006a の射程が書かれていない**

`W-2`「関数・変数・**JSON プロパティ**」は 2 通りに読める。

- (a) **一般の JSON の鍵すべて** —— `erd.json` や `model.json` を含む
- (b) **本ソフトウェアが読み書きする JSON だけ**

**兄弟の行が両方の読みを支える。** `W-8`（交換相手由来の列）と `W-9`（`carry` の中身）は本ソフトウェアの文書の内側にしか存在せず (b) を示唆する。一方で `W-1`（型）・`W-3`（定数）・`W-4`（CSS クラス）・`W-11`（ファイル名）はすべて一般のコードの面であり (a) を示唆する。

#### ⭐ 実測が (a) を否定した —— **この仕様書の道具立て自身が表 T-006a に従っていない**

**JSON の鍵**（(a) と読むと違反になるもの。**8 種**）

```
erd.json / erd.schema.json    entity_rows · key_note · row_prefix · container_edge
model.json / overview.json    column_width · node_separation · rank_separation
（別に $schema / $ref / $defs / $id / $comment —— JSON Schema 自身の語彙。改名は不能）
```

**ファイル名**（`W-11` は kebab-case と定めるが、**綴りが 3 通りに割れている**）

```
erd_json_to_md.py                          snake_case
md-checks.py · style-checks.py · dup-check.py   kebab-case
specindex.py · graph.py · impact.py · build.py  単語 1 つ
```

**つまり表 T-006a は、現に道具を縛っていない。** (a) と読むと、この仕様書は自分の道具を 8 鍵ぶん違反させたまま出荷していることになる。**射程を書いていなかっただけである。**

⚠️ **道具を対象外とするのは手抜きではない** —— 表 T-006a が縛るのは**成果物の中の名前**であり、`_assets/source/` の原稿と生成器は本書を作るための足場である。足場の綴りを揃えても、`GRS` を使う者には 1 つも届かない。

### 2-3. 穴② —— 接続語の規則が明文化されていない

**版 0.21 で実際に事故が起きている。** Chapter 5.3 の 10 メンバが `XOfY` の形で書かれ、`XFromY` / `XByY` へ改名された（`A-appendix.md` の版 0.21）。**`xOfY` はこの仕様書では既に「X ÷ Y」の意味を持っており**（表 T-104 に 14 名）、変換に使うと 1 つの形が 2 つの意味を持つ。

**規則が書かれていないので、同じ間違いが再発する。**

### 2-4. `Of` を含む名前の全数（**大文字を仮定せずに数え直した**）

⚠️ **引継書は「39 箇所すべてが比率」と書いていたが、これは `Of` の直後に大文字を仮定した数え方の結果である。** 仮定を外して数え直した。

```bash
grep -oh "[a-zA-Z]\{1,\}Of[a-zA-Z.]*" docs/spec/*.md docs/spec/_assets/*.md | sort | uniq -c
```

| 種別 | 綴り | 出現 | 規則への適合 |
|---|--:|--:|---|
| **比率（`XOfY`）** | 14 | 40 | ✅ `fontOfActual` `actualOfPlan` `markerOfFont` `resumeHeadOfMarker` `labelHaloOfFont` `thinStrokeOfPlan` `starInnerOfOuter` `spanDotOfStroke` `resumeArmOfMarker` `dependencyRunOfArrow` `chevronNotchOfWidth` `chevronNotchOfHeight` `arrowHeadOfStroke` `arrowHeadOfSpan` |
| ⚠️ **`shapeHeightOf.<形状>`** | 5 | 11 | **但し書きで除外**（§2-5） |
| `LevelOfDetail` 系 | 3 | 6 | 略語の展開。但し書きで除外 |
| 記法の見本 | 2 | 2 | `xOfY` / `XOfY` は例示であって名前ではない |
| 偽陽性 | 2 | 4 | `resumeDashOff` / `bodyOffsetPx` —— `Off` `Offset` の一部であり `Of` ではない |

⭐ **書き換えは 0 箇所である。** 比率の 14 名はすべて規則に適合し、残りは但し書きか例示か偽陽性である。

### 2-5. `shapeHeightOf.<形状>` —— **値は比率、文法は引き当て**

| 見たもの | 結果 |
|---|---|
| 値 | **比率である。** `tbl-settings.md` の `S-13` 〜 `S-17` は単位欄が `×`（倍率）で、値は `1.0` / `1.0` / `0.5` / `0.5` / `1.5`。`S-13` の範囲の理由は「基準なので 1 固定」 |
| 要求の呼び方 | **比である。** `01-04-requirements.md:1045` が「形状ごとの縦幅の**比**（`shapeHeightOf.*`）」と書いている |
| ⚠️ 文法 | **引き当てである。** `actualOfPlan` は `Of` の後ろが**除数**だが、`shapeHeightOf.rectangle` の後ろは**鍵**である。除数（矩形の縦幅）は名前に現れない |
| ⚠️ 既存の引き当て | **`By` を使っている** —— `taskByUid`（表 T-064 の `PI-1`。`uid` で `Task` を引く） |

**同じ操作に 2 つの接続語が当たっている。** ただし ⭐ **形は見分けがつく** —— `shapeHeightOf.` は必ず後ろにドットが来て、比率の 14 名は 1 つもドットを持たない。

**改名（`shapeHeightBy.*`）は採らない。** 11 出現・3 ファイルの書き換えが要り、**値の正である `tbl-settings.md` に手を入れることになる**のに対し、得られるのは形で既に見分けられているものの綴りの統一だけである。**但し書きで除外する。**

## 3. 変更箇所

| # | ファイル | 何を | 行 |
|---|---|---|--:|
| 1 | `docs/spec/01-04-requirements.md` | 1.9 に散文 2 か所（表 T-006a の射程 ／ 接続語の規則） | +2 段落 |
| 2 | `docs/spec/A-appendix.md` | 版 0.23 の行 1 本 | +1 |

⛔ **触らないもの** —— 表 T-006a の 11 行、表 T-006b、表 T-104、`tbl-settings.md`、要求の本文、`model.json`、`erd.json`、生成物一式。

## 4. 変更前の仕様

- 1.9 の「命名の規約」は、モデルの語彙への一本化 → 面ごとの記法（表 T-006a）→ 大文字の略語 → 記号のラベル → 曖昧な日本語（表 T-006b）の順に並ぶ
- **表 T-006a に射程の記述は無い。**「面ごとの記法を表 T-006a に示す」とだけ書かれている
- **接続語（`Of` / `From` / `By`）についての規則は 1 つも無い**

## 5. 変更後の仕様

### 5-1. 表 T-006a の射程（**表の直前に置く**）

> **本表が縛るのは、本仕様書が定めるソフトウェアの中の名前である（MUST）** —— `src/` のコードと、本ソフトウェアが読み書きする鍵（`GRS JSON` と、`MSPDI` に由来して `W-8` が記法を与えるもの）である。⚠️ **本仕様書を組み立てる道具は本表の対象外とする** —— `_assets/source/` の原稿と生成器、および検査のスクリプトである。**それらは成果物ではなく、本書を作るための足場だからである。**

### 5-2. 接続語の規則（**略語の規則の直後に置く**）

> **接続語は意味で使い分ける。`Of` は比率（X ÷ Y）に限ること（MUST）。変換と導出は `From`、引き当ては `By` を使うこと（MUST）** —— `Of` を変換にも使うと、`actualOfPlan`（実績 ÷ 予定）と同じ形が別の意味を持つ。例 —— `jsonFromDocument`（変換）／ `taskByUid`（引き当て）／ `fontOfActual`（比率）。
>
> ⚠️ **`LevelOfDetail` は略語を展開した綴りであり、接続語の `Of` ではない**（すぐ上の規則が `LOD` を全部書くよう定めている）。
>
> ⚠️ **`Of` の直後にドットが来る形は、比の値を鍵で引く連想であり、接続語の `Of` ではない** —— 表 T-104 の `K-13` 〜 `K-17`（`shapeHeightOf.<形状>`）がこれである。**比率の名は 1 つもドットを持たないので、形で見分けられる。**

## 6. 影響反映と影響分析結果

### 6-1. 席番号

**行 ID も要求 `UID` も表番号も新設しない。** 足すのは表の外の散文だけである。

### 6-2. 既存の名前との突き合わせ（**全数を機械で当てた**）

| 規則 | 当てた対象 | 違反 |
|---|---|--:|
| `Of` は比率 | `Of` を含む識別子 20 綴り・49 出現 | **0**（14 名は比率、5 名は但し書き、3 名は略語、2 つは例示・偽陽性） |
| 変換は `From` | `documentFromJson` `jsonFromDocument` `documentFromMspdi` `mspdiFromDocument` `svgFromSchedule` `layoutFromSchedule` `geometryFromLayout` `selectionFromInput` `commandFromInput` `derivedFromTaskUid` | **0** |
| 引き当ては `By` | `taskByUid` | **0** |
| 表 T-006a の射程 | 道具の JSON 鍵 8 種・`.py` の綴り 3 通り | **0**（対象外と明記するため） |

### 6-3. 機械検査（**予測**）

| 検査 | 予測 |
|---|---|
| `tables` | **100 のまま**（表を新設しない） |
| `figures` | **9 のまま** |
| `rows` | ⭐ **1165 のまま** —— 足すのは散文であって表の行ではない |
| `uids` | **140 のまま** |
| ゲート 14 本 | すべて緑を維持 |
| 検査 12（値・名前の表に規則） | **0 のまま** —— 規則を表の中に書かず、表の外の散文に置くため |
| 助言 13 / 14 | **4 / 18 のまま**（所有者を指す文も、値を写す文も足さない） |
| 検査 16 | 生成物に触らないので緑のまま |

⚠️ **検査 12 に触れないことが本 CR の要である。** 規則は表の中ではなく表の外に置く（1.9 の「規則と理由の置き場」が定める形である）。

## 7. 最終判断日時

**2026-08-15。** 利用者が決定した。

| 決定 | 結論 | 却下した案と理由 |
|---|---|---|
| **C** 表 T-006a の射程 | ⭐ **仕様対象のソフトだけ。道具は対象外** | 「一般の JSON すべて」（**自前の JSON 8 鍵の改名が要り、`$schema` 系は改名不能。かつ `.py` の綴り 3 通りも統一が要る**）／「`GRS JSON` の鍵だけ」（`W-2` だけ射程が狭くなり、兄弟の `W-1`・`W-3`・`W-4`・`W-11` と揃わない） |
| **D** 接続語の規則 | ⭐ **1.9 に足す** | 足さない（版 0.21 の事故が再発する） |
| **D′** `shapeHeightOf.*` | **但し書きで除外** | `shapeHeightBy.*` へ改名（**11 出現・3 ファイル。値の正である `tbl-settings.md` に手が入る**）／ 何もしない（後から規則を読んだ者が同じ問いを再び立てる） |

## 8. 反映記録（2026-08-15）

### 8-1. §6-3 の予測と実測 —— **外した予測は 0 件**

| 検査 | 予測 | 実測 | |
|---|---|---|:-:|
| `tables` | 100 | **100** | ✅ |
| `figures` | 9 | **9** | ✅ |
| `rows` | ⭐ 1165 のまま | **1165** | ✅ |
| `uids` | 140 | **140** | ✅ |
| ゲート 14 本 | すべて緑 | **すべて緑** | ✅ |
| 検査 12（表の中の規則） | 0 | **0** | ✅ |
| 助言 13 / 14 | 4 / 18 | **4 / 18** | ✅ |
| 検査 16 | 緑のまま | **2 文書とも一致** | ✅ |

⭐ **`rows` が動かなかったことが、規則を表の外に置いた証拠である。**

### 8-2. 実物を見た（`strictdoc export`）

9.60 秒で完走。⚠️ **本 CR は引用ブロックを 1 つ足したので、StrictDoc が拒む「連続する空行」の罠に当たりうる。当たらなかった。**

書き出した HTML を読んだ。

```html
<p><strong>接続語は意味で使い分ける。<code>Of</code> は比率（X ÷ Y）に限ること（MUST）。
   変換と導出は <code>From</code>、引き当ては <code>By</code> を使うこと（MUST）</strong>
   —— … 例 —— <code>jsonFromDocument</code>（変換）／ <code>taskByUid</code>（引き当て）／
   <code>fontOfActual</code>（比率）。</p>
<blockquote>
  <p>⚠️ <strong><code>LevelOfDetail</code> は略語を展開した綴りであり…</strong></p>
  <p>⚠️ <strong><code>Of</code> の直後にドットが来る形は…</strong>
     （<code>shapeHeightOf.&lt;形状&gt;</code>）…</p>
</blockquote>
```

| 見たもの | 実測 |
|---|---|
| 太字・コード・引用 | `<strong>` / `<code>` / `<blockquote>` とも正常 |
| ⚠️ CJK に隣接する `**bold**` | **崩れていない**（`**` の生の文字は 1 つも漏れていない） |
| `shapeHeightOf.<形状>` | `shapeHeightOf.&lt;形状&gt;` と正しくエスケープされた |
| 「同表が縛るのは」（射程） | 1 箇所、表 T-006a の直前 |

### 8-3. 引継書の誤りを 1 件正した

**引継書 `handoff-DESIGN-4.md` §2 の決定 D は「既存の `Of` 名は 39 箇所すべてが比率」と書いていたが、これは `Of` の直後に大文字を仮定した数え方の結果だった。** 仮定を外すと `shapeHeightOf.<形状>`（5 綴り・11 出現）が現れ、**値は比率だが文法は引き当てという境界例**であることが分かった。**規則を書く前に見つかったので、但し書きとして同時に閉じた。**

⚠️ **「機械で数えた」と書かれた数を、同じ問いの別の数え方で当て直す価値がある。** 正規表現の仮定は数の中に見えない。

### 8-4. 残った作業

| # | 件 | どこで |
|---|---|---|
| 1 | ⭐ **一貫性の単位・状態遷移・レイアウトの計算順序・描くときに求める値** | **Chapter 5.5**（未記入。`R7.4` の MUST 違反が残る唯一の箇所） |
| 2 | ADR-000（`R2.18`）／キャッシュの ADR（`R2.20`）／描かなかった図／4 つの代償 | **Chapter 5.6**（未記入） |
| 3 | `SW_SPEC` ノードと JSON Schema | **Chapter 6.1 / 6.2**（未記入） |
