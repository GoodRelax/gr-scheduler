# CR-190 — 作り方の規則を 1 か所に集めた

> **閉じるもの**: 規則が 2 か所に散っていた件。⛔ **`docs/development-rules/` が「入口」で、
> レビュー観点と仕様書の書き方だけ `previous-project-result/` に置かれていた。**
> **利用者の裁定**（2026-08-19）—— **移す。** あわせて **「少なくとも 05 と 06」をやめて全部読ませる**、
> **前に立つ者はオーケストレーションに徹する**。

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⛔ **`CH-1` 〜 `CH-5` と `GL-001` 〜 `GL-007` のどれも進めない。作り方の基盤である。**
⭐ **値打ちは「規則を読ませられる形にした」ことにある** —— 同フォルダの README が測ったとおり、
**原則として置いた規則は 75 件中 7 件しか従われなかった。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R4`**（DRY）| 移すか複製するか | ⛔ **複製は捨てた。** 仕様書が条項番号で引いている文書が 2 部になる。⭐ **移動なら 1 部のままである** |
| **`R4`**（DRY）| 索引の一行説明 | ⭐ **`check.sh` は `README.md` から読んで刷る。** 写しを作っていない |
| **`R2`**（命名）| 新しい番号 | `07` と `08` は空きだった（自分で数えた）。⚠️ **`08` はフォルダである** —— 5 ファイルあり 1 本にできない |
| **`R6`**（試験品質）| 検査 26 | ⭐ **わざと壊して落ちることを確かめた**（`05` へのリンクを 1 文字変えると 2 件で落ちる）|

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | 置き先を `07-review-standards.md` と `08-spec-template/` とした | ⭐ **番号を持たせたのは、`NN` が引用の鍵だからである**（「規則 05 の 2.」の形）。⚠️ 検査 26 が番号の欠落を落とす |
| 2 | ⛔ **過去の変更要求とレビュー在庫のパスを直さなかった** | **過去の記録を書き換えないのが本プロジェクトの規則である。** ⭐ 代わりに `previous-project-result/README.md` に読み替えの節を置いた |
| 3 | `07` の中の**解決しないリンク 2 本を外し、語だけ残した** | ⛔ **移動前から壊れていた**（`full-auto-dev-document-rules.md` と `glossary.md` は一度も存在しない）。⭐ **検査 26 が移動のついでに見つけた** |
| 4 | 検査 26 は **`08` の中身のリンクを見ない** | ⚠️ **取り込んだ一式であり、本プロジェクトが持ってこなかった文書を指す** —— 索引がそれを名乗っていることまでが射程 |

---

## 1. 測った

| | 実測 |
|---|---|
| `21-review-standard` を引くファイル | **22**（うち生きているのは **7**）|
| `20-spec-template` を引くファイル | **11**（うち生きているのは **4**）|
| ⛔ そのうち `docs/spec/` | **2 ファイル・4 か所** —— **だから本 CR が要る**（規則 01 の ④）|
| 直さなかったもの | 変更要求 3 本・`docs/review/` の在庫 4 本・過去の引継書 |

⚠️ **移動を禁じていたのは規則 README 自身である** —— 「⚠️ **移動もしない。** 過去の変更要求・レビュー在庫・
引継書が **10 か所以上**でそのパスを引いており、**過去の記録を書き換えないのが本プロジェクトの規則だからである**」。
⭐ **利用者はこれを覆した** —— **規則が 2 か所に散っていることのほうが高くつく**、という判断である。
⭐ **禁止の理由そのものは生きている**ので、**過去の記録は 1 文字も書き換えていない。**

---

## 2. 直したもの

| # | 場所 | 何をしたか |
|---|---|---|
| 1 | ファイルの移動 | `git mv` で 6 ファイル。⭐ **履歴が繋がる** |
| 2 | `docs/spec/01-04-requirements.md` ／ `05-07-design.md` | パスを 4 か所 |
| 3 | `docs/development-rules/README.md` | 索引に `07` `08` を足し、「なぜ移動しないか」の節を**移動した記録**に書き換えた |
| 4 | 同 `01` ／ `03` | パス |
| 5 | `.claude/skills/spec-graph-check/check-cr-discipline.py` | パス（検査 22 が刷る助言の中） |
| 6 | `previous-project-result/README.md` | 木から 2 つを外し、**読み替えの節**を置いた |
| 7 | `07-review-standards.md` | 解決しないリンク 2 本を外した（語は残した）|
| 8 | 検査 26 | **番号つきフォルダ**を扱えるようにした |
| 9 | `check.sh` の検査 0 | ⛔ **「少なくとも 05 と 06」→「全部読む」** ＋ **オーケストレーションの一文** |
| 10 | `05-working-method.md` | ⭐ **6. 前に立つ者はオーケストレーションに徹する**（新設）|

---

## 3. 数の予測と実測

| | 予測 | 実測 |
|---|---|---|
| 規則の本数 | 6 → **8** | **8**（検査 26 が数える）|
| `tables` / `figures` / `rows` / `uids` | 126 / 11 / 1552 / 145（不変）| **同じ** |
| 重複検出 | 基準線のまま | **`A=25 (new 0)`** |
| 機械検査 | 26 本すべて緑 | **ALL GREEN** |
| 試験 | 741 件（不変。`src/` を触っていない）| **741 件緑** |

---

## 4. ⚠️ 残っていること

1. ⚠️ **過去の変更要求 3 本と `docs/review/` の在庫 4 本は、存在しないパスを指したままである。**
   ⭐ **意図した状態である**（過去の記録は書き換えない）。**読み替えは `previous-project-result/README.md` が持つ。**
2. ⚠️ **`08-spec-template/` の中の文書は、本プロジェクトが持ってこなかった兄弟文書を指している。**
   ⛔ **検査 26 はそこを見ない。** ⭐ 使うときに気づけばよい。
3. ⚠️ **`docs/development-rules/` は 4KB から 290KB になった**（`07` が 52KB、`08` が 233KB）。
   ⭐ **「全部読む」の重さが変わった** —— ⚠️ **次のセッションが本当に読み切れるかは、次のセッションが測ること。**
