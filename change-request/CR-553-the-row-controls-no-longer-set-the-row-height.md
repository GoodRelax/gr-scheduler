# CR-553 — 行の操作子は行の高さを決めない

> 起草の状態: 起草（2026-09-23）。11 節の問い 3 つには前に立つ者が同じ日に答えた（0 節の ③ の決定 12 〜 14）。12 節の台帳の行は同じ日に `rulings.md` と `defects.md` へ写した。
> 読んだ木: `a912f4ea`（`refactor` の内容）。行番号・数・参照は、すべてこの木で測った（測り方は 13 節）。
> ⚠️ **当てる順**: `CR-551`（当てている最中）→ `CR-552`（最小の描画幅）→ 本書。本書の旧の塊は `a912f4ea` で数えた。**`CR-551` と `CR-552` が着地したら、4 節の旧の塊を当てる木で数え直してから当てること**（8 節）。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示 4 件（12 節の `JDG-420` 〜 `JDG-423`。逐語は下の 0.1 節）と、既に書いた `JDG-394`（表示した操作子の群は、群が消えるまで元の行のもの）。
> ⛔ **覆すもの**: 表 T-051 の `HF-19`（格子は帯高の下限）、`05-07-design.md` の 表 T-221 の `LF-3` の格子の床、表 T-259 の `VG-7` の注の格子の床、`JDG-149` の「① 下限を守る」（4 → 1 の `S-243` は残る）、`JDG-238`（`--->` だけの 1 段の行は `===` の行と同じ高さ）。ほかに格子を床と書いている文 7 つ（3 節）。
> ⭐ **形の方針**: 数を 1 つも足さない。1 段だけの行の帯高は、今ある式（`LF-2`: 段の高さ ＋ `VG-2`）から格子の床を外すだけで決まる。操作子の大きさ・並び・置き場（`S-138` ・ `S-243` ・ `S-235` ・ `HF-1` ・ `HF-4` ・ `HF-5`）は変えない。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が体へ渡した写しのまま写した。**Q2 の `…` は写しの側の省略であり、利用者の字ではない。**

| # | 逐語 |
|---|---|
| Q1 | 「1行に1段しかタスクが無いのに1行の縦幅が空きすぎケースがある。1行に1段しかタスクが無い場合は、Rectangle系 タスク/マイルストーンの縦幅+行罫線+依存線の幅程度に行の縦幅が収まるようにしろ。現在その制限となっているのが行の操作子である。」 |
| Q2 | 「もともとやりたかったことは、行の操作子が行の高さのボトルネックにならないようにすることだ。だったら、操作子のホバーを複数行にわたっても表示すればよいのでは？ … Taskaにカーソルを当てたとき、操作子がタスクbやcの上に来てもOK とすれば？ 問題となるのは、画面最下部の場合だけと思う。画面最下部の場合は、その分だけ下にスペースを確保しておけばいい。」 |
| Q3 | 「今のdist/index.htmlのサイズで、1行に行の操作子を入れるという制限を取っ払い、最終行に行の操作子分を確保すればいいのでは？」 |
| Q4 | 「特別ルールは無用だろ？ 表示された行の操作子のホバーの上をマウスカーソルが動くなら、対象とする行は変わらないはずだ。見本は一旦無視でいいだろ？」 |

既に書いてある裁定（`docs/development-records/rulings.md`）: `JDG-394`「一度ある行に対して表示した操作子群は、マウスカーソルが外れてその操作子群が消えるまで元の行に対する操作子とせよ。　というか、それがふつうだろ？」。⭐ Q4 のとおり、これは重なったときの例外ではなく、**群のふだんの規則**として `HF-6` に書く（E-02）。
前に立つ者が渡した読み（利用者の言葉ではない）: 1 段の行は表示の倍率 100 で 24 → 21px（バー 17.5 ＋ 1 ＋ 依存線 1.5 ＋ 罫 1）。⚠️ 本書は式から 21.625px を導き、前に立つ者がそれを採った（決定 12）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2` ／ `GL-002`**（縮めたときに全体を 1 枚で俯瞰する）—— 1 段だけの行が 24.0012px から 21.625px に詰まる（表示の倍率 100。1 行あたり 2.3762px、約 10%）。同じ `Row Area` に描ける行が増え、`FR-055` の全体表示が深い段を採りやすくなる。
⭐ **`CH-4` ／ `GL-006`**（説明を読まずに操作できる）—— Q4 の「ふつう」: 表示した群の上でポインタを動かしても群が指す行が変わらない（開いたメニューと同じ）。重なっても押しが別の行へ届かない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正）** —— 格子を床とする文は 1 つではなかった。`HF-19` ・ `LF-3` ・ `VG-7` の注 ・ `FR-029` の隙間の文 ・ `FR-109` の注 ・ `FR-016` の「第 2 の下限」・ `OP-10` の実測の注 ・ 表 T-071 の `CA-1` の鍵 ・ 表 T-075 の `UF-105` ・ 表 T-064 の `PI-35` ・ `S-12` と `S-243` の注 —— 12 か所をすべて 1 つの計画で書いた（3 節。`induced.py` の閉路 `FR-029` `HF-19` `HF-6` `LF-3` `S-243` を 1 度に書く）。⭐ `HF-6` の頭の MUST（「その行の名前にポインタが乗っているあいだだけ描く」）は、群が下の行に重なると `JDG-394` と両立しない → 頭の文そのものを「名前か、描いた群の上」に書き換えた（E-02）。例外の文を足すと「だけ」と食い違う。
- **`R1.4`（異常系・境界値）** —— ① 最後の行の格子が画面の下で切れる（Q2 の「画面最下部」）→ `LF-16` の余白（E-14）。② 行が 1 つも無い・スクロールする行が 1 つも無い → 余白 0（E-14）。③ 帯が格子以上の行 → 余白 0、重なりも起きない。④ 格子が 2 行以上にまたがる（表示の倍率 50 で帯 11.8125px、格子 24.0012px）→ `HF-19` は「重なる行は 1 つとは限らない」と書いた。⑤ 全体表示の直後に最後の行が下端に座る → 余白を `FR-055` の縦の高さに含めた（E-07）。⑥ 空の行の帯が仕様とコードで違う（格子の床が隠していた）→ 12 節の `DFC-840`、決定 13。⑦ 表示の倍率 50 で `HB-5` の前提（隅の掴み代が帯の半分より狭い）が崩れる → 決定 14（E-16）。
- **`R1.2`（検証できる表現）** —— 「程度に収まる」を式にした: 1 段の帯高 ＝ `LF-2`（段の高さ ＋ `VG-2`）と `LF-3` の床の大きいほう。数は 13 節で原稿から導く。
- **`R1.6`（否定要求は代替を示す）** —— `HF-19` の MUST NOT（格子を下限にしない）に、代わりの 2 つ（重なりは `HF-6` が裁く、最後の行の下は `LF-16`）を同じ行で示した。
- **`R2.1`（命名）** —— `rowControlLatticeFloorPx` は床を答えなくなる → `rowControlLatticeHeightPx` に改めた（E-11）。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる）。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 1 段の帯高は今の式のまま（表示の倍率 100 で 21.625px）。21px にするための規則は足さない | Q1 は「程度」。`VG-5` が枠線の外側から測ることは `JDG-160` b が裁いている（13 節の導き） | 前に立つ者の 21px と 0.625px 違う（決定 12） |
| 決定 2 | `HF-6` の「群」は地（`HF-6` の地の範囲）とする。操作子と操作子のあいだの地の上も群の上に数える | 群の上でポインタを動かしても行が変わらない（Q4）ためには、操作子から操作子へ移る途中で群が消えてはならない | コードは地を当たりに含める（9 節）。いま地は `pointer-events:none` |
| 決定 3 | 地は、行の下端と格子の下端の下にあるほうまで敷く | 敷かないと、はみ出した操作子のあいだから下の行の名前がのぞく（`HF-6` の地の理由と同じ） | 重なっているあいだ、下の行の名前と畳んだ数（`HF-18`）が隠れる |
| 決定 4 | 余白は、スクロールする行（ピン止めの帯を除く）の最後の行の下にだけ置く。量は 格子の高さ − その行の帯高（0 未満なら 0） | Q2「その分だけ下にスペース」、Q3「最終行に行の操作子分を確保」 | ピン止めの帯の最後の行は救わない（10 節） |
| 決定 5 | 余白は縦のスクロールの全体（`GR-21`）と、`FR-055` が収まるかを見る高さの両方に入る | 入れないと、全体表示の直後に最後の行の操作子が下で切れる | 全体表示が段を 1 つ浅く採ることがある（余白は倍率 100 で約 5.9px まで —— 格子 24.0012px − 矩形の床 18.125px） |
| 決定 6 | 余白は `canvasPadding`（`S-56`）と別の量とし、兼ねない | `CR-551` の E-14 は「`canvasPadding` を縦に取る場所は本段が変えない」を残す。兼ねると `S-56` を変えたとき操作子が切れる | 最後の行の下に、余白と `canvasPadding` の 2 つが並ぶ |
| 決定 7 | 描いた格子の高さを測って知らせる経路（`UF-105`、`rowControlsHeightPx`）は残し、床ではなく余白へ流す | 経路は殻・翻訳器・配置を 8 ファイルにわたって通っている。消すのは本書の目的の外 | 格子の高さは式と実測の大きいほうのまま |
| 決定 8 | `HF-19` は退役させず、行 ID のまま中身を書き換える | 6 か所が `HF-19` を指している（6 節）。退役させると指し先を失う | — |
| 決定 9 | `LF-3` の矩形の床と行の名前の床（`JDG-319`）は残す。外すのは格子の床だけ | Q1 が名指したのは操作子だけ | `--->` だけの 1 段の行は矩形の床（倍率 100 で 18.125px）で止まる |
| 決定 10 | `S-243`（4 → 1、`JDG-149`）は残し、注の理由を「床を下げる」から「はみ出しと余白を小さくする」に替える | Q3「今のdist/index.htmlのサイズで」 | — |
| 決定 11 | `OP-10` の実測の注は消さず、過去形にして当時の床を書き添える | 実測は当時の出荷ビルドの記録であり、記録は消さない | — |
| 決定 12 | 1 段の行は 21.625px（式のまま）。`VG-5` を「形の端」から測るように改めて 21.0px にする手（B）は採らない | ⭐ 前に立つ者の答え（2026-09-23。利用者の言葉ではない）: Q1 は「…程度に…収まるように」であり、`JDG-160` b（バーの枠線の外側から測る）は裁かれた行で、写しの側が導いた 21 に勝つ | 前に立つ者の写しの 21px と 0.625px（`S-39` 1 × 描く比 0.625）違う |
| 決定 13 | 空の行（`Task` を 1 つも持たない行）の帯は、仕様どおり矩形 1 段と `VG-2`（表示の倍率 100 で 21.625px）。コードを直す（`DFC-840`、状態は `実装待ち`）。`LF-2` と `SWS-2` は書き直さない | ⭐ 前に立つ者の答え（2026-09-23）: 仕様が勝つ。最初の `Task` を載せたときに行が跳ねない（18.125 → 21.625 の 3.5px の跳ねが、その下のすべての行を動かす） | 空の行は今のコード（18.125px）より 3.5px 高い |
| 決定 14 | `HB-5` の理由の文に「既定の値と既定の表示の倍率では」を添え、表示の倍率 50 では隅が隣の境目へ移りうることを ⚠️ で残す（E-16）。隅の縦の動きに閾値を当てる手（B）は採らない | ⭐ 前に立つ者の答え（2026-09-23）: B は新しい規則であり、利用者は「特別ルールは無用」（Q4）と言っている | 表示の倍率 50 で隅を横にだけ引くと、行が 1 つ動くことがある |

---

## 1. 範囲 —— 要求ごとの行き先

| 要求 | 仕様で変える所 | 編集 | 裁定を戻すとき |
|---|---|---|---|
| R1 格子を床にしない | `HF-19`、`LF-3`、`VG-7` の注、`FR-029` の隙間の文、`FR-109` の注、`FR-016` の序数、`OP-10` の注、`CA-1` の鍵、`UF-105`、`PI-35`、`S-12` ・ `S-243` の注、`HB-5` の理由（決定 14） | E-01 ・ E-04 〜 E-06 ・ E-08 〜 E-13 ・ E-16 ・ J-01 ・ J-02 | `LF-3` に格子の床を 1 文戻し、`HF-19` を旧に戻す |
| R2 大きさ・並び・置き場は変えない | —（`S-138` 16px ・ `S-243` 1px ・ `S-235` 0.6667、`HF-1` ・ `HF-4` ・ `HF-5`） | なし | — |
| R3 重なりと群の持ち主 | `HF-6`（頭の MUST、地の範囲）、`HF-19` | E-02 ・ E-03 ・ E-04 | — |
| R4 最後の行の下の余白 | 表 T-221 の `LF-16`（新）、`SWS-2`、`FR-055` の RATIONALE | E-07 ・ E-14 ・ E-15 | `LF-16` を消す |

**数**: 仕様の文の編集 16（E-01 〜 E-16）、原稿 JSON の編集 2（J-01 ・ J-02）。⚠️ E-16 は 4 節で E-09 の後に置いた（同じファイルの編集をまとめるため）。

⚠️ **`CR-551` と重なる所**（`a912f4ea` で `CR-551` の 4 節の旧 35 塊と突き合わせた。13 節）: 本書の旧が `CR-551` の旧と同じ行を持つものは **0**。隣の行を `CR-551` が書くものが 2 つある —— E-04（`HF-19` の行。すぐ上の `HF-17` の行を `CR-551` の E-26 が `HF-17` ＋ `HF-20` に置き換える）と E-07（`FR-055` の行。すぐ下の行を `CR-551` の E-15 が書く）。`CR-551` の J の塊は `S-12` と `S-243` を持たない。⭐ それでも、**`CR-551` が着地した木で、4 節の旧 18 塊が 1 回ずつ現れることを数え直してから当てること。**
⚠️ **`CR-552` と重なる所**: 本書の起草の終わりに、同じ worktree に現れた `CR-552` の草案（`change-request/CR-552-tasks-the-width-lod-dropped-are-drawn-at-a-minimum-width.md`。未コミット）の旧 11 塊と同じ道具で突き合わせた —— 同じ行も隣の行も **0**。`CR-552` の J の塊は `S-12` と `S-243` を持たない（本書の J-01 ・ J-02 は両行の注の中の文字列だけを書き換える）。コードでは `CR-552` も `schedule-layout.ts` を触る（`keptByLevelOfDetail`、`:604` 付近）—— 8 節の波 1a はその上に乗せる。⚠️ 草案は動くので、当てる者は `CR-552` の着地後に数え直すこと。

---

## 2. 新しい識別子

⚠️ 規則 02 の 2.5 節: 下は 2026-09-23 に `a912f4ea` で測った主張である。**当てる直前に測り直すこと。** 測り方は 13 節。`CR-551` の 2 節が採った識別子（`HF-20` ほか）は取られたものとして数えた。

| 種類 | 採ったもの | 測った最大（2026-09-23、`a912f4ea`） |
|---|---|---|
| 行 | `LF-16` は空いている | `LF-15`（`docs/spec src tests tools change-request` の全体で） |
| 名 | `rowControlLatticeHeightPx` は空いている | — |
| 台帳 | `JDG-420` 〜 `JDG-423`（帯 `JDG-420` 〜 `JDG-429`）、`DFC-840`（帯 `DFC-840` 〜 `DFC-849`） | 帯は前に立つ者が配った |

⚠️ 設定値の行は足さない（帯 `S-355` 〜 `S-359` は使わない）。表も足さない。

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 消すもの | 所（`a912f4ea`） | 置き換わる先 | 編集 |
|---|---|---|---|
| `HF-19` の「`HF-1` の格子が縦に取る高さは、行の帯高の下限であること（MUST）。行の帯がそれを下回ってはならない（MUST NOT）」 | `01-04-requirements.md:1678` | 格子を下限にしてはならない（MUST NOT）＋ 重なりは `HF-6`、最後の行の下は `LF-16` | E-04 |
| `HF-19` の「帯が格子を下回らないことは本行のまま変わらない」（`S-243` の段） | 同 `:1678` | —（`S-243` の注が理由を持つ） | E-04 |
| `HF-19` の「⚠️ 算式は 表 T-221 の `LF-3` が持つ」と「この床を閲覧者の文字サイズに…」の「床」 | 同 `:1678` | 算式は `LF-16`、「格子の高さ」 | E-04 |
| `HF-6` の頭の MUST「その行の名前にポインタが乗っているあいだだけ描く」の「名前だけ」 | `01-04-requirements.md:1666` | 名前か、描いた群の上 ＋ 群は消えるまでその行のもの（`JDG-394`） | E-02 |
| `HF-6` の地の範囲「縦はその行の高さ」 | 同 `:1666` | 行の下端と格子の下端の下にあるほうまで | E-03 |
| `VG-7` の注「行の操作子の格子（`HF-19`）を下回らない」 | `01-04-requirements.md:1281` | 床は矩形と行の名前の字の箱。格子は数えない | E-01 |
| `FR-016` の「`LF-3` の第 2 の下限」（第 2 ＝ 格子の床） | `01-04-requirements.md:3270` | 「`LF-3` の下限」 | E-05 |
| `FR-109` の「1 段だけの行は、行の床（`LF-3`）で矩形の行と同じ高さになる」（`JDG-238`） | `01-04-requirements.md:3833` | 帯は `LF-2` の和と `LF-3` の床の大きいほう | E-06 |
| `OP-10` の「行が既に `LF-3` の下限に座っており、縮める先が無い」（現在形） | `01-04-requirements.md:5622` | 過去形 ＋ 当時の床に格子を数えていたこと | E-08 |
| `FR-029` の「行の帯の床（`HF-19`）は…隙間を詰めて床を下げる」（`JDG-149`） | `01-04-requirements.md:6317` | 隙間を詰めて格子を低くし、はみ出しと余白を小さくする | E-09 |
| `UF-105` の「描いた操作子の高さを測って知らせる（`LF-3`・`HF-19`）」 | `05-07-design.md:474` | （`LF-16`・`HF-19`） | E-10 |
| `PI-35` の `rowControlLatticeFloorPx` と「それが行の帯高の下限であることは `HF-19` が持つ」「床を読む `ScheduleLayout`」 | `05-07-design.md:570` | `rowControlLatticeHeightPx`、下限にしない、余白を求める | E-11 |
| `CA-1` の帯を組む材料の「行の操作子の高さ」 | `05-07-design.md:1023` | —（材料から外す） | E-12 |
| `LF-3` の「かつ、その行の操作子（表 T-051 の `HF-1` の格子）が縦に取る高さも下回らない —— 下回ると、格子の下段が次の行の帯へ落ち、押しがその行の操作子に取られる」と「床を数で書かない…格子はその 2 段ぶん」「同表の値を写してはならない」「この床は閲覧者の文字サイズに追随しない」 | `05-07-design.md:1274` | 格子は床に数えない ＋ 算式と「数で書かない」は `LF-16` へ移す | E-13 ・ E-14 |
| `S-12` の注「帯は中身と操作子の格子の床（表 T-051 の `HF-19`）で既に高さを持ち」 | `_source/settings.json:485`（生成物 `tbl-settings.md:84`） | 中身（`LF-2`）と床（`LF-3`） | J-01 |
| `S-243` の注「行の帯の床（`HF-19`）は、行の操作子の格子が縦に取る高さであり、…`LF-3` は…床は 2 ×（16 ＋ 1 × 2）× 0.6667 ＝ 24.0012px」と「縦を数える `LF-3`」 | `_source/settings.json:4060`（生成物 `tbl-settings.md:411`） | 格子の高さ（床ではない）、`LF-16` | J-02 |
| `HB-5` の理由の「隅の掴み代 `S-230` は既定で帯の高さの半分より狭い」（表示の倍率を限らない言い方） | `01-04-requirements.md:3427` | 既定の値と既定の表示の倍率では ＋ 倍率 50 では崩れうる ⚠️ | E-16 |
| 裁定 `JDG-149` の「① 下限を守る」 | `docs/development-records/rulings.md:261` | 覆された（一部）。4 → 1 は残る | 12 節（当てる者） |
| 裁定 `JDG-238` の「行の床（`LF-3`）は残し、`--->` の効き目は段を積んだ行でだけ出る」 | `docs/development-records/rulings.md:364` | 覆された。格子の床が消え、1 段の行にも効く | 12 節（当てる者） |
| コード（⛔ 本書は直さない。9 節） | `screen-regions.ts:101` の `rowControlLatticeFloorPx`、`schedule-layout.ts:792-794` の `bandFloorOf` と呼ぶ側 `:983`、`row-title-panel-drawing.ts:98-106` の地と `:516` の `rowControlsHeightReporter`、`dom-screen-surface.ts:452-456` の `:hover` と `:686` | 9 節 | 実装する者 |

⭐ **消さないもの**（`grep` で当たるが床の話ではない）: `01-04-requirements.md:4079` ・ `:4084` ・ `:6822` 〜 `:6825` の「床」は行見出しパネルの**幅**の床（`FR-039` の 表 T-252 の後の段）であり、本書は触らない。`HB-5`（`:3427`）の「帯は矩形が縦に取る高さを下回らない —— `LF-3`」は矩形の床を指し、真のまま（E-16 が書き換えるのは同じ文の前半の限定だけ）。

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-551` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（13 節の道具が 18 件すべてで 1 回を確かめ、写しに順に当てた）。旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である。
⚠️ 3 つのファイルは CRLF である。塊は LF で書いてある —— 読むときに `\r\n` を `\n` にしてから数え、書くときに戻すこと。
⚠️ 同じ行の編集は上から順に当てること —— E-02 と E-03 はどちらも `HF-6` の行の中である。E-14 は `LF-15` の行を旧に取り、その後ろに `LF-16` の行を足す（`LF-15` の中身は変えない）。
⚠️ 行末の半角空白 2 つ（`  `）は旧・新の一部である（E-06 ・ E-07）。
⚠️ J-01 ・ J-02 は `settings.json` の `note.ja` の文字列の中だけを書き換える。当てたら `json.loads` で読めることを確かめ、`npm run gen` で `_assets/tbl-settings.md` を刷る（生成物を手で直さない）。

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
R1。行ごと置き換える。旧
```text
| VG-7 | 行の帯の床 | 行をまたぐところは、`VG-2` の大きさを下限とすること（MUST）。<br>⚠️ 行の帯は、矩形が縦に取る高さと、行の操作子の格子（`HF-19`）を下回らない（`LF-3`）ので、段の合計と隙間より床が高い行では、いちばん下の段の下が `VG-2` より広く空く |
```
新
```text
| VG-7 | 行の帯の床 | 行をまたぐところは、`VG-2` の大きさを下限とすること（MUST）。<br>⚠️ 行の帯は、矩形が縦に取る高さと、行の名前の字の箱を下回らない（`LF-3`）ので、段の合計と隙間より床が高い行では、いちばん下の段の下が `VG-2` より広く空く。<br>⚠️ 行の操作子の格子は帯の床に数えない（表 T-051 の `HF-19`）—— 矩形の 1 段だけの行の帯は、矩形が縦に取る高さと `VG-2` の和まで詰まる |
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
R3。HF-6 の頭の 1 文。旧
```text
| HF-6 | **操作子は、その行の名前にポインタが乗っているあいだだけ描くこと（MUST）** —— 常に描くと、日程より操作子が目立ち、行の名前ともぶつかる。
```
新
```text
| HF-6 | **操作子は、その行の名前か、その行のために描いた操作子の群（下の段が地を敷く範囲）にポインタが乗っているあいだだけ描くこと（MUST）** —— 常に描くと、日程より操作子が目立ち、行の名前ともぶつかる。<br>⭐ 一度ある行のために描いた群は、ポインタがその群から外れて群が消えるまで、その行の操作子であり続けること（MUST） —— 群の上でポインタを動かしても、群が指す行は変わらない（開いたメニューと同じ振る舞いである）。<br>⛔ 群が下の行に重なっているとき、群の上にあるポインタを、下の行の名前に乗っていると数えてはならない（MUST NOT） —— 数えると、押そうとして群の下段へ動かしたポインタで群が下の行のものに替わり、押しがその行へ届く（`HF-19` の実測）。<br>⚠️ これは重なったときだけの例外ではなく、群のふだんの振る舞いである —— 群が自分の行の中に収まっているときも、同じ規則で描き、同じ規則で消す。<br>⚠️ ポインタが群からも行の名前からも外れたら群を消す。<br>そのときポインタが別の行の名前に乗っていれば、その行の群を描く。
```

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md -->
R3。HF-6 の地の範囲の文。旧
```text
⭐ 敷く範囲は、いちばん左の操作子の左端から行の右端までとし、縦はその行の高さとすること（MUST） —— 右端は `HF-4` が操作子を留める端と同じものであり、縦を操作子の高さにすると、`HF-5` が名前の上端に揃えるぶん、名前の大きい行で文字の脚が下からのぞく。
```
新
```text
⭐ 敷く範囲は、いちばん左の操作子の左端から行の右端までとし、縦はその行の上端から、行の下端と格子（`HF-1`）の下端のうち下にあるほうまでとすること（MUST） —— 右端は `HF-4` が操作子を留める端と同じものであり、縦を操作子の高さにすると、`HF-5` が名前の上端に揃えるぶん、名前の大きい行で文字の脚が下からのぞく。<br>⭐ 格子が行の下端より下へはみ出すとき（`HF-19`）も、その下端まで敷く —— 敷かないと、はみ出した操作子のあいだから下の行の名前がのぞき、どの行の操作子かが読めない。<br>⚠️ 本行の最初の MUST が言う「群」は、この地の範囲である —— 操作子と操作子のあいだの地の上も、群の上に数える。
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
R1 R3 R4。行ごと置き換える。旧
```text
| HF-19 | `HF-1` の格子が縦に取る高さは、行の帯高の下限であること（MUST）。<br>行の帯がそれを下回ってはならない（MUST NOT）—— ⛔ 下回ると、格子の下段の 2 つが次の行の帯の上に立ち、`HF-6` が「行の名前の上へ重ねて描く」と定めた重なりが、自分の行ではなく隣の行に対して起きる。<br>⚠️ 実測（出荷ビルド）: `Task` を 1 つも持たない行は 22〜28px、格子は 48px。<br>その行の `IC-90` と `IC-58` の中心は次の行のものであり、押しはそちらへ届いた。<br>⛔ **格子の側を縮めて合わせてはならない（MUST NOT）** —— **操作子の大きさは `HF-5` が持つ。<br>**⚠️ 行見出しパネルの入口の隙間を `S-243` として `S-141` より狭くするのは、帯に合わせて格子を縮めることではない —— 隙間は帯の高さによらず設定の行が決め、帯が格子を下回らないことは本行のまま変わらない。<br>⛔ **`HF-1` の 2 × 2 を崩して合わせてはならない（MUST NOT）** —— あちらは理由（上下に読めば動作、左右に読めば範囲）を持つ。<br>⚠️ 算式は 表 T-221 の `LF-3` が持つ。<br>数は本行に書かない —— 操作子 1 つの外形は `_assets/tbl-settings.md` の 表 T-206 の `S-138` と `S-243` が決めており、格子はその 2 段ぶんである。<br>⛔ **この床を閲覧者の文字サイズに追随させてはならない（MUST NOT）** —— `S-138` が入口についてそう定めており、床だけが追随すると、行の帯が入口より高くなったり低くなったりする。<br>⚠️ **`FR-039` は行の名前の大きさを動かすが、入口の大きさは動かさない**（`HF-5`）|
```
新
```text
| HF-19 | `HF-1` の格子（`HF-4` の並び。2 段）が縦に取る高さを、行の帯高の下限にしてはならない（MUST NOT） —— 帯高は `05-07-design.md` の 表 T-221 の `LF-2` と `LF-3` が決める。<br>下限にすると、`Task` が 1 段しかない行でも帯が格子の高さまで広がり、日程ではなく操作子が行の高さを決める。<br>⭐ 帯高が格子より低い行では、描いた格子とその地（`HF-6`）が、下の行の帯の上へはみ出して重なってよい —— 重なる行は 1 つとは限らない。<br>どの行の操作子として描き、押しをどの行へ届けるかは `HF-6` が決める（描き始めた行のまま、群が消えるまで）。<br>⚠️ 実測（出荷ビルド）: `Task` を 1 つも持たない行は 22〜28px、格子は 48px。<br>その行の `IC-90` と `IC-58` の中心は次の行のものであり、押しはそちらへ届いた —— 重なりを許したうえでこれを起こさないのが、`HF-6` の群の規則である。<br>⚠️ 重なっているあいだ、下の行の名前と畳んだ数（`HF-18`）は群の地に隠れる —— 群が消えれば戻る。<br>⭐ 最後の行の格子が画面の下端（行見出しパネルと `Row Area` の下端）で切れないよう、行の並びの下に余白を置くこと（MUST） —— 量は 表 T-221 の `LF-16` が持つ。<br>⛔ **格子の側を縮めて合わせてはならない（MUST NOT）** —— **操作子の大きさは `HF-5` が持つ。<br>**⛔ **`HF-1` の 2 × 2 を崩して合わせてはならない（MUST NOT）** —— あちらは理由（上下に読めば動作、左右に読めば範囲）を持つ。<br>⚠️ 格子の高さの算式は 表 T-221 の `LF-16` が持つ。<br>数は本行に書かない —— 操作子 1 つの外形は `_assets/tbl-settings.md` の 表 T-206 の `S-138` と `S-243` が決めており、格子はその 2 段ぶんである。<br>⛔ **格子の高さを閲覧者の文字サイズに追随させてはならない（MUST NOT）** —— `S-138` が入口についてそう定めている。<br>⚠️ **`FR-039` は行の名前の大きさを動かすが、入口の大きさは動かさない**（`HF-5`）|
```

<!-- EDIT id=E-05 file=docs/spec/01-04-requirements.md -->
R1。`FR-016` の中。`LF-3` から格子の床が消え、「第 2」が指す床が入れ替わるので序数を外す。旧
```text
`LF-3` の第 2 の下限
```
新
```text
`LF-3` の下限
```

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md -->
R1。`FR-109` の RATIONALE。旧
```text
⚠️ 1 段だけの行は、行の床（`05-07-design.md` の 表 T-221 の `LF-3`）で矩形の行と同じ高さになる。  
```
新
```text
⚠️ 1 段だけの行の帯は、`LF-2` の和（段の高さと `VG-2`）と、行の床（`05-07-design.md` の 表 T-221 の `LF-3`）の大きいほうである —— 線だけの形の 1 段が矩形より低ければ、その行は矩形の 1 段だけの行より低くなりうる。  
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md -->
R4。`FR-055` の RATIONALE に 1 行足す。旧
```text
⚠️ **画面の下に隙間が残ることは許す** —— 本要求は収めることを求めており、埋めることを求めていない。  
```
新
```text
⚠️ **画面の下に隙間が残ることは許す** —— 本要求は収めることを求めており、埋めることを求めていない。  
⭐ 収まるかを見る縦の高さには、最後の行の下に置く余白（`05-07-design.md` の 表 T-221 の `LF-16`）を含めること（MUST） —— 含めないと、全体表示の直後に最後の行の操作子（表 T-051 の `HF-19`）が画面の下で切れる。  
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md -->
R1。`OP-10` の中の実測の注。旧
```text
行が既に `LF-3` の下限に座っており、縮める先が無い。
```
新
```text
当時の行は `LF-3` の下限（そのころは行の操作子の格子を数えていた。表 T-051 の `HF-19`）に座っており、縮める先が無かった。
```

<!-- EDIT id=E-09 file=docs/spec/01-04-requirements.md -->
R1。`FR-029` の行見出しパネルの隙間の文。旧
```text
行の帯の床（`HF-19`）は行見出しパネルの入口の外形の 2 段ぶんなので、そこだけ隙間を詰めて床を下げる。
```
新
```text
行の操作子の格子（表 T-051 の `HF-1`）は行見出しパネルの入口の外形の 2 段ぶんなので、そこだけ隙間を詰めて格子を低くし、下の行へのはみ出し（同表の `HF-19`）と、最後の行の下の余白（`05-07-design.md` の 表 T-221 の `LF-16`）を小さくする。
```

<!-- EDIT id=E-16 file=docs/spec/01-04-requirements.md -->
R1。表 T-246 の `HB-5` の理由の欄。表示の倍率の限定と警告を足す（`LF-3` から格子の床が消え、倍率 50 の帯が 12px を下回るため）。旧
```text
横にだけ引いて離したときは行が変わらない —— 隅の掴み代 `S-230` は既定で帯の高さの半分より狭いので（帯は矩形が縦に取る高さを下回らない —— 表 T-221 の `LF-3`）、掴み代の中の点からは、箱自身の辺の境目のほうが隣の境目より近い。
```
新
```text
横にだけ引いて離したときは行が変わらない —— 隅の掴み代 `S-230` は、既定の値と既定の表示の倍率（`_assets/tbl-settings.md` の 表 T-202 の `S-234`）では帯の高さの半分より狭いので（帯は矩形が縦に取る高さを下回らない —— 表 T-221 の `LF-3`）、掴み代の中の点からは、箱自身の辺の境目のほうが隣の境目より近い。<br>⚠️ 表示の倍率を下げると、この前提は崩れうる —— `S-230` は表示の倍率を掛けない（`FR-039` の 表 T-252 の `DS-7`）が帯は縮むので、既定の値では表示の倍率 50 で帯の高さの半分が `S-230` を下回り、横にだけ引いても隅が隣の境目へ移ることがある。
```

<!-- EDIT id=E-10 file=docs/spec/05-07-design.md -->
R1。表 T-075 の `UF-105`。旧
```text
描いた操作子の高さを測って知らせる（`LF-3`・`HF-19`）
```
新
```text
描いた操作子の格子の高さを測って知らせる（表 T-221 の `LF-16`・`HF-19`）
```

<!-- EDIT id=E-11 file=docs/spec/05-07-design.md -->
R1。表 T-064 の `PI-35`。名を変える（床ではなくなった）。旧
```text
`rowControlLatticeFloorPx`（行の操作子の格子（表 T-051 の `HF-1`）を描いたときに縦に取る高さ。<br>算式は 表 T-221 の `LF-3` が持ち、それが行の帯高の下限であることは 表 T-051 の `HF-19` が持つ）—— ⭐ **公開したのは、床を読む `ScheduleLayout` が同じ 1 本を問うためである**
```
新
```text
`rowControlLatticeHeightPx`（行の操作子の格子（表 T-051 の `HF-1`）を描いたときに縦に取る高さ。<br>算式は 表 T-221 の `LF-16` が持ち、それを行の帯高の下限にしないことは 表 T-051 の `HF-19` が持つ）—— ⭐ **公開したのは、最後の行の下の余白（`LF-16`）を求める `ScheduleLayout` が同じ 1 本を問うためである**
```

<!-- EDIT id=E-12 file=docs/spec/05-07-design.md -->
R1。表 T-071 の `CA-1`。帯は操作子の高さから組まれなくなった。旧
```text
段 0 の畳み、行の操作子の高さ、ズームの下限と上限
```
新
```text
段 0 の畳み、ズームの下限と上限
```

<!-- EDIT id=E-13 file=docs/spec/05-07-design.md -->
R1。行ごと置き換える。旧
```text
| LF-3 | 行の縦位置 | 前の行の縦位置に、前の行の帯高と `rowGap` を加える。<br>帯高は矩形が縦に取る高さを下回らず、かつ、その行の操作子（表 T-051 の `HF-1` の格子）が縦に取る高さも下回らない—— ⛔ **下回ると、格子の下段が次の行の帯へ落ち、押しがその行の操作子に取られる。<br>**⭐ 帯高は、その行の名前（行見出しパネルに刷る行名、`FR-085`）の字の箱が縦に取る高さも下回らない —— 行名が見えないと何の行か分からなくなる。<br>⚠️ 表示の倍率（`FR-039`）を上げたまま縦に縮めたときに効く床である。<br>⚠️ **床を数で書かない** —— 操作子 1 つの外形は `_assets/tbl-settings.md` の 表 T-206 の `S-138` と `S-243` が決めており、格子はその 2 段ぶんである。<br>⛔ **同表の値を写してはならない。<br>**⚠️ **この床は閲覧者の文字サイズに追随しない** —— `S-138` がそう定めている |
```
新
```text
| LF-3 | 行の縦位置 | 前の行の縦位置に、前の行の帯高と `rowGap` を加える。<br>帯高は矩形が縦に取る高さを下回らない。<br>⭐ 帯高は、その行の名前（行見出しパネルに刷る行名、`FR-085`）の字の箱が縦に取る高さも下回らない —— 行名が見えないと何の行か分からなくなる。<br>⚠️ 表示の倍率（`FR-039`）を上げたまま縦に縮めたときに効く床である。<br>⚠️ **行の操作子（`01-04-requirements.md` の 表 T-051 の `HF-1` の格子）が縦に取る高さは、帯高の床に数えない** —— 数えると、`Task` が 1 段しかない行も格子の高さまで広がる。<br>格子が帯からはみ出す扱いは同表の `HF-19` と `HF-6` が、最後の行の下の余白は `LF-16` が持つ |
```

<!-- EDIT id=E-14 file=docs/spec/05-07-design.md -->
R4。`LF-15` の次に 1 行足す（`LF-15` の行は変えない）。旧
```text
| LF-15 | コメントボックスのアンカーの置き場 | 横は `anchorDate` の日の列の中央、縦は `anchorGroupId` の行が描かれた帯（`LF-2` / `LF-3`、ピン止めした行は `LF-14`）の中央とする。<br>⚠️ ハイライトボックスの左端と右端は日の列の端に立つ（`01-04-requirements.md` の `FR-019`）が、アンカーは日を 1 つ指す点なので、その日の列の中に置く。<br>⭐ 帯の上端と下端は採らない —— そちらはハイライトボックスが行の範囲を囲むのに使う。<br>⚠️ 本文の箱は留めた点からのずれで置く（`FR-019`）ので、アンカーと共に動く |
```
新
```text
| LF-15 | コメントボックスのアンカーの置き場 | 横は `anchorDate` の日の列の中央、縦は `anchorGroupId` の行が描かれた帯（`LF-2` / `LF-3`、ピン止めした行は `LF-14`）の中央とする。<br>⚠️ ハイライトボックスの左端と右端は日の列の端に立つ（`01-04-requirements.md` の `FR-019`）が、アンカーは日を 1 つ指す点なので、その日の列の中に置く。<br>⭐ 帯の上端と下端は採らない —— そちらはハイライトボックスが行の範囲を囲むのに使う。<br>⚠️ 本文の箱は留めた点からのずれで置く（`FR-019`）ので、アンカーと共に動く |
| LF-16 | 行の並びの下の余白 | スクロールする行（`LF-14` の帯へ上げた行を除く）のいちばん下の行の下に、行の操作子の格子（`01-04-requirements.md` の 表 T-051 の `HF-1`）が縦に取る高さからその行の帯高を引いた長さの余白を加える。<br>引いた長さが 0 を下回るとき（帯高が格子以上のとき）と、スクロールする行が 1 つも無いときは 0 とする。<br>格子が縦に取る高さは、入口 1 つの縦（`_assets/tbl-settings.md` の 表 T-206 の `S-138` ＋ `S-243` × 2）の 2 段ぶんに、`FR-029` のとおり `S-235` を掛けた長さとする —— 枠の線の太さ（`S-237`）は数えない（`FR-029` は `S-237` を外形の幅に加え、外形の縦は定めない）。<br>⚠️ **格子の高さを数で書かない** —— 操作子 1 つの外形は `_assets/tbl-settings.md` の 表 T-206 の `S-138` と `S-243` が決めており、格子はその 2 段ぶんである。<br>⛔ 同表の値を写さない。<br>⚠️ 格子の高さは、表示の倍率（`FR-039` の 表 T-252 の `DS-7`）にも閲覧者の文字サイズにも追随しない —— `S-138` がそう定めている。<br>⭐ 余白は縦のスクロールの全体（`01-04-requirements.md` の 表 T-023d の `GR-21` の「全体」）に入る —— 行見出しパネルは縦を本体と連動させる（同書の 表 T-031 の `SC-1`）ので、行見出しパネルと `Row Area` の両方で、最後の行の操作子が下端で切れない。<br>⭐ `FR-055` が収まるかを見る縦の高さにも入る。<br>⚠️ 余白には何も描かない。<br>行ではないので、その縦位置は行の無い縦位置である（同書の 表 T-239 の `TC-3`）。<br>⚠️ `canvasPadding`（`_assets/tbl-settings.md` の 表 T-201 の `S-56`）とは別の量であり、兼ねない —— `canvasPadding` を縦に取る場所は `FR-051` が持つ |
```

<!-- EDIT id=E-15 file=docs/spec/05-07-design.md -->
R4。`SWS-2` の STATEMENT。旧
```text
**STATEMENT**: 段を割り当てたあと、`GRS` は、行の帯高と縦位置を 表 T-221 の `LF-2` と `LF-3` に従って決めること。
```
新
```text
**STATEMENT**: 段を割り当てたあと、`GRS` は、行の帯高と縦位置を 表 T-221 の `LF-2` と `LF-3` に従って決め、行の並びの下の余白を同表の `LF-16` に従って置くこと。
```

<!-- EDIT id=J-01 file=docs/spec/_source/settings.json -->
R1。`S-12` の `note.ja` の中。旧
```text
帯は中身と操作子の格子の床（表 T-051 の `HF-19`）で既に高さを持ち
```
新
```text
帯は中身（`05-07-design.md` の 表 T-221 の `LF-2`）と床（同表の `LF-3`）で既に高さを持ち
```

<!-- EDIT id=J-02 file=docs/spec/_source/settings.json -->
R1。`S-243` の `note.ja` の中。旧
```text
⭐ 行の帯の床（`HF-19`）は、行の操作子の格子が縦に取る高さであり、`05-07-design.md` の 表 T-221 の `LF-3` は入口 1 つの縦を `S-138` と隙間で数え、格子をその 2 段ぶんとし、描くときは `FR-029` のとおり `S-235` を掛ける —— 床は 2 ×（16 ＋ 1 × 2）× 0.6667 ＝ 24.0012px である（隙間が `S-141` の 4 なら 2 ×（16 ＋ 4 × 2）× 0.6667 ＝ 32.0016px）。⚠️ 縦を数える `LF-3` は枠の線の太さ（`S-237`）を数えない
```
新
```text
⭐ 行の操作子の格子が縦に取る高さは、`05-07-design.md` の 表 T-221 の `LF-16` が入口 1 つの縦を `S-138` と隙間で数え、格子をその 2 段ぶんとし、描くときは `FR-029` のとおり `S-235` を掛ける —— 2 ×（16 ＋ 1 × 2）× 0.6667 ＝ 24.0012px である（隙間が `S-141` の 4 なら 2 ×（16 ＋ 4 × 2）× 0.6667 ＝ 32.0016px）。⚠️ この高さは行の帯の床ではない（表 T-051 の `HF-19`）—— 格子が下の行へはみ出す長さと、最後の行の下の余白（`LF-16`）がこの高さで決まる。⚠️ 縦を数える `LF-16` は枠の線の太さ（`S-237`）を数えない
```

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | 仕様 ↔ 配置（`schedule-layout.ts`） | 帯高 ＝ max（`LF-2` の和、矩形が縦に取る高さ（`drawnExtentOf('rectangle')`）、行の名前の字の箱、`TaskGroup.height`）。⛔ 格子の高さは入れない。`bandFloorOf` は格子を返さない |
| S-2 | 仕様 ↔ 配置（余白） | 格子の高さ `lattice` ＝ max（`rowControlLatticeHeightPx()`、殻が測って渡す `rowControlsHeightPx ?? 0`）。余白 ＝ スクロールする行が 1 つ以上なら max（0、`lattice` − 最後のスクロールする行の `height`）、無ければ 0。`scrollingContentHeight`（＝ `layout.contentHeight`）に余白を足す。ピン止めの帯（`pinnedBandOf` の `height`）には足さない |
| S-3 | 配置 ↔ 全体表示（`fitZoom`） | 収まるかの判定（`schedule-layout.ts:1300` の `run.contentHeight <= …`）は S-2 の余白を含んだ `contentHeight` で行う（`FR-055`、E-07） |
| S-4 | 配置 ↔ 行見出しパネル（`row-title-panel-drawing.ts`） | 行の箱の高さは `RowPlacement.height`（格子に合わせて伸ばさない）。格子と地は行の箱の下へはみ出して描く。地の高さ ＝ max（行の箱の高さ、格子の高さ）。パネルの縦のスクロールの全体は `layout.contentHeight`（S-2 の余白を含む） |
| S-5 | 行見出しパネル ↔ 当たり（`dom-screen-surface.ts` の `:hover`） | 群（地の矩形）の上にポインタがあるあいだ、群を描き始めた行が持ち主のまま。⛔ 群の下にある別の行の `:hover` を立てない。地の上（操作子のあいだを含む）も群の上に数える |
| S-6 | 仕様 ↔ 公開の名（`PI-35`） | `rowControlLatticeFloorPx` → `rowControlLatticeHeightPx`（`src/entity/layout-engine/screen-regions/screen-regions.ts` の入口から出す。検査 26b が名を読む） |

---

## 6. グラフ（`a912f4ea` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 対象 | 要求 / 参照 | 指している要求 | 扱い |
|---|---|---|---|
| `HF-19` | 2 / 6 | `FR-029` ・ `FR-094`（`VG-7`）、`UF-105` ・ `PI-35`、`S-12` ・ `S-243` の注 | 6 か所すべてを本書が書く（E-01 ・ E-09 〜 E-11 ・ J-01 ・ J-02） |
| `LF-3` | 5 / 12 | `FR-004`（`HF-19`）・ `FR-016`（`:3270` ・ `:3427` の `HB-5`）・ `FR-087`（`OP-10`）・ `FR-094`（`VG-7`）・ `FR-109`（`:3833`）・ `SWS-2` | すべて本書が書く（`HB-5` は E-16。矩形の床を指す括弧は真のまま、倍率の限定を足す）。`LF-14` ・ `LF-15` は `LF-3` の連なりと帯を指すだけで床に触れない |
| `HF-6` | 6 / 14 | `FR-004` ・ `FR-013`（未着手のマーカーの濃さ「作法は `HF-6` と同じ」）・ `FR-029`（`:6352` の薄さの別）・ `FR-048`（描き直しの契機）・ `FR-085`（場所の確保）・ `FR-098` | どれも「ポインタが乗っているあいだ描く」の作法と場所の確保を引くだけで、群の持ち主と地の縦の範囲を引かない ⇒ 偽にならない |
| `VG-7` | 0 / 0 | — | 浮いている |
| `S-12` ・ `S-243` | 2 / 4 ・ 3 / 11 | `FR-016` ・ `FR-076` ／ `FR-004` ・ `FR-029` ・ `FR-039` | 注の理由だけを替える。値は変えない |
| `OP-10` ・ `CA-1` ・ `UF-105` ・ `PI-35` | 5 / 12 ・ 0 / 2 ・ 0 / 0 ・ 0 / 0 | — | 1 文ずつ |
| `FR-055` | 10 / 27 | `FR-003` ・ `FR-004` ・ `FR-016` ・ `FR-018` ・ `FR-031` ・ `FR-063` ・ `FR-070` ・ `FR-080` ・ `FR-087` ・ `FR-098` | 足すのは縦の高さに余白を含める 1 文だけで、段の採り方（深い段から見る）は変えない |
| `FR-016` ・ `FR-109` ・ `FR-029` | 4 / 23 ・ 10 / 39 ・ 25 / 65 | — | 1 文ずつ。`FR-016` の行の軸の上限（表 T-253 の `BC-2`）は「いちばん高い行の帯」で測り、余白を数えない ⇒ 変わらない |
| `LF-16`（新。本書を当てた写しで測った） | 3 / 7 | `FR-004`（`HF-19`）・ `FR-055`（E-07）・ `FR-029`（E-09）、`UF-105` ・ `PI-35` ・ `LF-3` ・ `SWS-2` | 7 か所とも本書が書く |

⭐ 導いた条項ごとに、届いた行を `rulings.md` で引いた（規則 02 の 1）:

| 裁定 | 当たる所 | 本書との関係 |
|---|---|---|
| `JDG-149` | `HF-19` ・ `LF-3` ・ `S-243` ・ `HF-5` | ⛔ **覆す（一部）** —— 「① 下限を守る」が覆る。当時退けた案 ③「ボタンはポインタを載せた行にだけ重ねて出す」に近い形になる。「4 → 1」（`S-243`）と「約 24px」（格子の高さ）は残る |
| `JDG-238` | `LF-3` | ⛔ **覆す** —— 格子の床が消え、`--->` だけの 1 段の行は `LF-3` の矩形の床まで下がる（倍率 100 で 18.125px、13 節）。「退けた手 B（`LF-3` の MUST が禁じる）」の前提が消える |
| `JDG-212` | `LF-3`（`JDG-238` 経由） | 狙い（3 段を合わせて `===` より低く）が、1 段の行でも効くようになる。覆さない |
| `JDG-394` | `HF-6` | ⭐ 本書が着地させる（E-02） |
| `JDG-319` | `LF-3` の名前の床 | 残す（決定 9） |
| `JDG-160` b ／ `JDG-148` | `VG-2` ・ `VG-5` ・ `VG-7` | 変えない。21.625px の根拠（決定 1） |
| `JDG-93` ・ `JDG-108` ・ `JDG-113` | `S-12`（`rowGap` 0） | 変えない。`S-12` の注の理由の言い方だけを替える |
| `JDG-06` ・ `JDG-377` | `FR-055` | 変えない。`JDG-377` は横の余白、本書は縦の余白 |
| `JDG-123` ・ `JDG-152` ・ `JDG-169` ・ `JDG-76` | `S-235` ・ `S-138` | 変えない（R2） |

### 6.2 `induced.py`

| 種 | 解決 | 種の中の辺 | 閉路 | 扱い |
|---|---|---|---|---|
| `HF-19 LF-3 HF-6 VG-7 FR-055 SWS-2 FR-029 S-243 S-12 LF-14 FR-016 FR-109 OP-10 CA-1 UF-105 PI-35` | 16/16 | 30 | 1: `FR-029` `HF-19` `HF-6` `LF-3` `S-243` | ⭐ 5 つとも本書が 1 つの計画で書く（E-02 〜 E-04 ・ E-09 ・ E-13 ・ J-02） |
| `LF-3 LF-14 FR-055 FR-098 FR-051 GR-21 SC-1` | 7/7 | 8 | 1: `FR-055` `FR-098` `LF-14` | `FR-055` に 1 文を足すだけ。`FR-098` と `LF-14` は書き直さない |
| `FR-016 FR-109 OP-10 CA-1 LF-3 HB-5 S-12` | 7/7 | 10 | 0 | — |
| `HF-6 FR-013 FR-048 FR-085 FR-098 HF-15 HF-18` | 7/7 | 13 | 1: `FR-085` `HF-15` `HF-18` `HF-6` | `HF-6` だけを書く。`HF-15` の「操作子は掴み代を覆ってはならない（MUST NOT）」は、格子が下の行へはみ出すと、狭いパネル（深さ 4 と 5、同行の実測）で下の行の掴み代も覆いうる —— 同行が既に持つ「幅が足りないときは読む人がパネルを広げる」が当たる。`HF-18` の畳んだ数は重なるあいだ隠れる（E-04 に書いた） |

---

## 7. 数の予測（`a912f4ea` で測った。当てた後に同じ数え方で突き合わせる）

| 数 | 前 | 後 | 内訳 |
|---|--:|--:|---|
| tables | 186 | 186 | — |
| figures | 27 | 27 | — |
| rows | 2282 | 2283 | 足す 1（`LF-16`）。消す 0（`HF-19` は書き換えるだけ） |
| uids | 162 | 162 | — |

⚠️ `CR-551` が先に着地すると、前が 2302（`CR-551` の 7 節の予測）になり、後は 2303 になる。`CR-552` が行を足すなら、その数だけずれる。**予測するのは本書の差分 ＋1 である。**
⭐ 本書の編集を当てた写しの木で `md-checks.py` を走らせた —— `tables=186 figures=27 rows=2283 uids=162`、検査 5 〜 10 ・ 15 ・ 48 はすべて 0（13 節）。

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ 仕様の編集（4 節）は 1 つの体が 1 つの worktree で当てる。⚠️ **検査 26b（`check-published-members.py`）は、E-11 で `PI-35` の名が変わった瞬間に赤くなる** —— `screen-regions.ts` の名を変える波 1a と合わせた木で門を打つこと。

```
wave 0  spec     rebase section 4's old blocks on the tree after CR-551 and CR-552 (count == 1 each),
                 then E-01..E-16, J-01, J-02 + npm run gen + gen:check
                 -> tests that read the spec verbatim go red here, by design (section 9)
wave 1  (parallel, disjoint folders; cut from wave 0's commit)
  1a entity/layout-engine   screen-regions.ts (rename to rowControlLatticeHeightPx),
                            schedule-layout.ts (bandFloorOf drops the lattice; the reserve S-2 on
                            scrollingContentHeight; fitZoom reads it through contentHeight)
                            -- CR-552 edits schedule-layout.ts keptByLevelOfDetail (~:604): rebase on it
  1b framework/dom-screen-surface
                            row-title-panel-drawing.ts (ground height S-4; overflow below the row box),
                            dom-screen-surface.ts (:hover ownership S-5: the ground is hit, the drawn
                            group stays its row's until the pointer leaves the ground)
wave 2  shell comments only if needed: frame-loop.ts:1642 TRAP names "the LF-3 floor" -> the reserve
                            (no function grows; check 60 holds the function sizes)
wave 3  tests by a spec-only tester (never the implementer), on the MERGED tree; then check.sh + vitest
```

- ⚠️ `rowControlsHeightPx` の経路（決定 7）は 8 ファイル（`display-scale-steps.ts` ・ `input-command-translator.ts` ・ `zoom-and-fit.ts` ・ `schedule-layout.ts` ・ `dom-screen-surface.ts` ・ `row-title-panel-drawing.ts` ・ `frame-loop.ts` ・ `single-html-shell.ts`）を通るが、**どれも値を渡すだけ**なので波 1a の `schedule-layout.ts` だけが意味を変える。ほかは触らない。
- ⚠️ 1b は e2e でしか確かめられない振る舞い（`:hover` の持ち主、地の当たり）を持つ。試験の体には「Playwright で、格子が下の行に重なる行（倍率 100 の 1 段の行）で、群の下段の操作子へポインタを動かし、押しが上の行へ届くこと」を書くこと（`HF-19` の実測の逆）。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

| 所 | 何をする |
|---|---|
| `src/entity/layout-engine/screen-regions/screen-regions.ts:99-104`（`rowControlLatticeFloorPx`） | 名を `rowControlLatticeHeightPx` に変える（S-6）。式は変えない。注の `see LF-3, HF-19` を `see LF-16, HF-19` に |
| `src/entity/layout-engine/schedule-layout/schedule-layout.ts:788-795`（`bandFloorOf`）と呼ぶ側 `:983` | 格子（`rowControlLatticeFloorPx()` と `rowControlsHeightPx`）を max から外し、矩形と名前の箱だけにする（S-1） |
| 同 `:1080-1130`（`pinnedBandOf` の `scrollingContentHeight`）と `:1048` | 余白を足す（S-2）。`rowControlsHeightPx` は `bandFloorOf` からここへ移る |
| `src/framework/dom-screen-surface/row-title-panel-drawing.ts:98-106`（`rowControlGroundStyle` の `top:0;bottom:0`、`pointer-events:none`） | 地の高さを max（行の箱、格子）に（S-4）。地を当たりに含める（S-5） |
| 同 `:135-150`（`rowControlGridStyle`）・ `:516-548`（`rowControlsHeightReporter`） | 格子は行の箱の下へはみ出して描く。報告は残す（決定 7）。注の `see LF-3, HF-19` を `see LF-16, HF-19` に |
| `src/framework/dom-screen-surface/dom-screen-surface.ts:452-456`（`[data-group-id]:hover` で操作子を出す CSS）・ `:167`（注）・ `:686` | 群を描いている行を、ポインタが地から外れるまで持ち主とする（S-5）。⚠️ いまは行の要素の `:hover` なので、はみ出した子孫の上では親の `:hover` が続く —— ただし後ろの行の要素が上に重なって描かれると当たりを取られる。群を描いているあいだは、その行の格子と地を後ろの行より上に重ねる |
| `src/entity/layout-engine/schedule-layout/schedule-layout.ts:971-984`（`Task` の無い行の段を 0 と数える） | 空の行にも矩形 1 段を置き、`VG-2` を足す（決定 13、`DFC-840`） |
| `src/framework/single-html-shell/frame-loop.ts:1642`（注「omit the LF-3 floor」） | 注を余白の話に替える |
| `tools/generate_entity_types.py:1225` ・ `:2167-2168`（注「LF-3's SECOND FLOOR」） | 生成物に写る注なら直す（生成器の注だけなら文だけ） |

**試験**（いま格子の床を主張しているもの。仕様だけを読む試験の体が書き直す）:

| 試験 | いま主張していること | 本書の後 |
|---|---|---|
| `tests/unit/lf-3-hf-19-a-row-is-never-shorter-than-its-controls.test.ts`（全 9 件） | 原稿が床を言うこと（`:209-225`）、空の行・床より低い `FR-042` の行が格子まで上がること（`:243-303`） | ⛔ 前提が逆になる。`HF-19` の MUST NOT（格子で上げない）と `LF-16` の余白の試験に置き換える |
| `tests/unit/layout-engine.test.ts`（14 か所） | `CONTROL_LATTICE_FLOOR` を max に入れた期待値（`:51-86` ・ `:571-590` ・ `:761-800` ・ `:817-840` ・ `:999`）。`:835` は「空の行を格子まで上げる」 | 期待値から格子を外す。`:835` は決定 13（空の行 ＝ 矩形 1 段 ＋ `VG-2`、21.625px） |
| `tests/integration/schedule-drawing.sws.test.ts`（14 か所） | `SWS-2` の格子の床（`:184-221` ・ `:959-1057`）、`:1096` の「2 つの床」 | 格子を外す。空の行の試験（`:1004-1050`）は決定 13 の値に。⭐ `:2174` の「表 T-221 のすべての行に試験がある」が `LF-16` で赤くなる —— `SWS-2` の下に `LF-16` の試験を足す（E-15 で `SWS-2` が `LF-16` を名指す） |
| `tests/unit/cr-414-the-row-title-panel-entrances-keep-one-pixel-around-the-glyph.test.ts` | `HF_19_THE_FLOOR`（`:42`、旧 `HF-19` の逐語）、`rowControlLatticeFloorPx`（`:23` ・ `:88-89`）、「`LF-3` counts one control」（`:56`、`05-07` に `HF_19_OUTER_SIZE` の文があること —— E-14 は `LF-16` に同じ文を残すので緑のまま） | `:42` を新しい `HF-19` の逐語に、名を変える |
| `tests/unit/fr-055-vertical-lod-fit.test.ts`（`:187-200` ・ `:497-520`）・ `tests/unit/fr-055-fit-reaches-deep-tiers.test.ts`（`:219-228` ・ `:571-602`） | 各段の帯が格子以上であること、`HF-19` の「行の帯がそれを下回ってはならない（MUST NOT）」の逐語 | 格子を外す。全体表示が余白を数えること（E-07）を足す |
| `tests/unit/dfc-377-fr-016-zoom-ceilings-and-ep-9-thickness.test.ts`（`:134` ・ `:530-554`） | `FR-016` の「`LF-3` の第 2 の下限」の逐語、低い倍率で格子の床が効くという前提 | 逐語を E-05 に、前提を名前の床か `FR-094` の床に |
| `tests/unit/dfc-568-gr-14-corners-anchor-and-body.test.ts:122` | `HB-5` の「横にだけ引いて離したときは行が変わらない」（頭の句だけ） | E-16 はこの句を残すので緑のまま。`:84` の `FR-019` の線先の文（`S-292` が既定で帯の半分より狭い —— `HB-5` の根拠と同じ）も本書は変えない |
| `tests/unit/t-051-hf-6-ground-under-the-row-controls.test.ts`（`:970-1146`） | 地の縦 ＝ 行の高さ（「縦はその行の高さとすること（MUST）」の逐語と、箱の高さ） | 地の縦 ＝ max（行、格子）（E-03） |
| `tests/unit/uf-72-screen-part.test.ts`（`:3916-4048`） | `HF-6` の頭の MUST の逐語 | E-02 の逐語に |
| `tests/unit/t-051-hf-14-the-depth-cap-refuses-a-row.test.ts:282` ・ `tests/unit/t-051-hf-14-a-raised-row-is-visible.test.ts` ・ `tests/unit/uf-30-31.test.ts:1332` ・ `tests/unit/t-024a-op-10-a-chosen-zoom-is-the-place.test.ts:79` ・ `tests/unit/fr-013-pointer-on-the-figure.test.ts:36` ・ `tests/system/user-reported-fixes.test.ts:51` | 注だけ（環境に格子の高さを渡す、旧文の引用） | 注を直す。値に頼る所があれば測り直す |
| 新しく要るもの | — | `JDG-394` ／ E-02: 群の下段の操作子を押すと上の行へ届く（e2e）。E-14: 最後の行まで送ったとき、その行の格子が `Row Area` に収まる。E-07: 全体表示の後も同じ |

---

## 10. ⛔ この変更でやらないこと

- **ピン止めの帯の最後の行**（`FR-098`、`LF-14`）—— 帯が `Row Area` の下端まで届いたとき、その最後の行の格子は下で切れうる。帯は流れないので余白で救えない。帯に載る行は `S-127`（既定 5）までで、帯が下端に届くのは縦に大きく拡げたときだけである。⭐ 申し送り: 起きたら、`LF-14` の「帯に置けない行は落とす」の判定に格子を数えるかを決める。
- **行の罫線（`FR-042` のグループ罫線）の太さ** —— コードの定数 `GROUP_GRID_LINE_WIDTH_PX = 1`（`src/adapter/svg-renderer/svg-renderer.ts:276`）で描き、設定値の行を持たない。帯の境目に中心を置くので帯高に数えない（Q1 の「行罫線」は `LF-2` の式に項を持たない）。本書は触らない。
- 操作子の大きさ・並び・置き場（R2）。`S-138` ・ `S-243` ・ `S-235` ・ `S-237` ・ `S-313` の値。
- 見本（`row-height-sample.html`）—— Q4「見本は一旦無視でいいだろ？」。
- `docs/development-records/` の `rulings.md` ・ `defects.md` ・ `handoff.md` と `A-appendix.md` の変更履歴（当てる者が書く。12 節は最終の文）。

---

## 11. 前に立つ者へ返す問い

⭐ **残る問いは無い。** 起草時の 3 つには前に立つ者が 2026-09-23 に答えた（前に立つ者が既にある裁定から導いた答えであり、利用者の言葉ではない —— だから `JDG-` の行は立てない）。答えは 0 節の ③ の決定 12 〜 14 に移した。

| # | 問い | 答え |
|---|---|---|
| 1 | 1 段の行は表示の倍率 100 で 21.625px（式どおり）。写しの読みは 21px で、違いは矩形の枠線 0.625px（`VG-5` が枠線の外側から測る）。A 式のまま ／ B `VG-5` を形の端から測り 21.0px | **A**（決定 12）—— Q1 は「程度」、`JDG-160` b は裁かれた行で写しの 21 に勝つ |
| 2 | 空の行の帯が仕様（21.625px）とコード（18.125px）で違う（`DFC-840`）。A 仕様どおり、コードを直す ／ B コードどおり、`LF-2` と `SWS-2` を書き直す | **A**（決定 13）—— 仕様が勝つ。`DFC-840` は `実装待ち` |
| 3 | `HB-5` の前提（隅の掴み代が帯の半分より狭い）が表示の倍率 50 で崩れる。A 限定と警告を添える ／ B 閾値の規則を足す ／ C 何もしない | **A**（決定 14、E-16）—— B は「特別ルールは無用」（Q4）に反する |

---

## 12. 台帳（12.1 の節と 12.2 の行は、2026-09-23 に本書を書いた体が台帳へ写した。12.1 の後段の表 —— 古い行の書き換え —— は当てる者の仕事）

### 12.1 裁定（`docs/development-records/rulings.md` の「2026-09-23 —— 使い勝手の指示 16 件（CR-551）」の節の後、「2026-09-07 以前」の節の前に、節ごと足した）

```text
## 2026-09-23 —— 行の縦幅と行の操作子（CR-553）

⭐ 利用者が 2026-09-23 に出した行の縦幅の指示 4 件を、前に立つ者が体へ渡した写しから 1 文字も変えずに写した。Q2 の `…` は写しの側の省略である。前に立つ者が添えた読みは「読み」の欄に書いた。`JDG-394`（同じ日、上の節）と一組である。

| # | 逐語（⛔ 1 文字も変えるな） | 読み | 着地先 | 状態 |
|---|---|---|---|---|
| JDG-420 | **「1行に1段しかタスクが無いのに1行の縦幅が空きすぎケースがある。1行に1段しかタスクが無い場合は、Rectangle系 タスク/マイルストーンの縦幅+行罫線+依存線の幅程度に行の縦幅が収まるようにしろ。現在その制限となっているのが行の操作子である。」**（2026-09-23。Q1） | ⭐ 1 段だけの行の帯高を、形が縦に取る高さと段の下の隙間（`VG-2`）まで詰める。詰まらない原因は、行の操作子の格子を帯高の下限とする規則（表 T-051 の `HF-19`、`05-07-design.md` の 表 T-221 の `LF-3`）である。表示の倍率 100 で 24.0012px → 21.625px（`VG-5` は枠線の外側から測る —— `JDG-160` b。導きは `CR-553` の 13 節）。⚠️ 覆すもの: `HF-19`、`LF-3` の格子の床、`JDG-149` の「① 下限を守る」、`JDG-238` | `docs/spec/01-04-requirements.md` の 表 T-051 の `HF-19` ・ 表 T-259 の `VG-7`、`docs/spec/05-07-design.md` の 表 T-221 の `LF-3`（`change-request/CR-553-the-row-controls-no-longer-set-the-row-height.md` の E-01 ・ E-04 ・ E-13） | 指示 |
| JDG-421 | **「もともとやりたかったことは、行の操作子が行の高さのボトルネックにならないようにすることだ。だったら、操作子のホバーを複数行にわたっても表示すればよいのでは？ … Taskaにカーソルを当てたとき、操作子がタスクbやcの上に来てもOK とすれば？ 問題となるのは、画面最下部の場合だけと思う。画面最下部の場合は、その分だけ下にスペースを確保しておけばいい。」**（2026-09-23。Q2。「…」は写しの省略） | ⭐ 行の操作子の格子とその地が、下の行（1 つとは限らない）の上へはみ出して重なってよい。画面の最下部で切れないよう、最後の行の下に格子の分の余白を置く | `docs/spec/01-04-requirements.md` の 表 T-051 の `HF-6` ・ `HF-19`、`docs/spec/05-07-design.md` の 表 T-221 の `LF-16`（`CR-553` の E-02 〜 E-04 ・ E-14） | 指示 |
| JDG-422 | **「今のdist/index.htmlのサイズで、1行に行の操作子を入れるという制限を取っ払い、最終行に行の操作子分を確保すればいいのでは？」**（2026-09-23。Q3） | ⭐ 操作子の大きさ・並び・置き場は今のまま（`S-138` 16px ・ `S-243` 1px ・ `S-235` 0.6667）。1 行に格子を収める制限を外し、最後の行の下に格子の高さからその行の帯高を引いた余白を置く。余白は縦のスクロールの全体と全体表示の高さに入る | `docs/spec/05-07-design.md` の 表 T-221 の `LF-16` ・ `SWS-2`、`docs/spec/01-04-requirements.md` の `FR-055`（`CR-553` の E-07 ・ E-14 ・ E-15） | 指示 |
| JDG-423 | **「特別ルールは無用だろ？ 表示された行の操作子のホバーの上をマウスカーソルが動くなら、対象とする行は変わらないはずだ。見本は一旦無視でいいだろ？」**（2026-09-23。Q4） | ⭐ 表示した群の上をポインタが動くあいだ、群が指す行は変わらない。これは重なったときの例外ではなく、群のふだんの振る舞いとして書く（`JDG-394` と同じ規則）。群の範囲は地の範囲である。行の縦幅の見本（`row-height-sample.html`）は一旦使わない | `docs/spec/01-04-requirements.md` の 表 T-051 の `HF-6`（`CR-553` の E-02 ・ E-03） | 指示 |
```

⛔ 当てる者が変える古い行（逐語の欄は変えない）:

| 行 | 変える欄 | 変えたあとの文（足す） |
|---|---|---|
| `JDG-394` | 着地先 | 「行の縦幅の変更要求（未起草）」→「`docs/spec/01-04-requirements.md` の 表 T-051 の `HF-6`（`change-request/CR-553-the-row-controls-no-longer-set-the-row-height.md` の E-02 ・ E-03）」 |
| `JDG-149` | 状態 | 末尾に「⚠️ 2026-09-23 に一部が覆された（`JDG-420` 〜 `JDG-423`、`CR-553`）—— ① 下限を守る（`HF-19` の床）は覆り、格子は下の行へ重なってよくなった。4 → 1（`S-243`）と格子の高さ約 24px は残る」 |
| `JDG-238` | 状態 | 末尾に「⚠️ 2026-09-23 に覆された（`JDG-420`、`CR-553`）—— `LF-3` から格子の床が消え、`--->` だけの 1 段の行は `LF-3` の矩形の床（表示の倍率 100 で 18.125px）まで下がる。理由に挙げた「床を下げると格子が次の行へ落ちる」は、`HF-6` の群の規則と `LF-16` の余白で扱う」 |
| `JDG-212` | 状態 | 末尾に「⚠️ 2026-09-23（`CR-553`）から、狙いは 1 段の行にも効く（`JDG-238` が覆された）」 |

⚠️ 検査 43（`check-ruling-landed.py`）が数える状態は `未着地` である。4 行は `指示` で足した。足したあとに走らせた（2026-09-23）—— `未着地` は 17 で基準線どおり、終了コード 0。台帳の件数は `python tools/ledger_metrics.py`（`--start` なし）で刷り直し、`--check` も 0。⚠️ `npm run gen` は `docs/spec/_assets/tbl-row-id-prefixes.md` の `DFC`（767 → 768）と `JDG`（353 → 357）の件数を刷り直す —— 台帳から数える生成物であり、`gen:check` は 0。

### 12.2 欠陥（`docs/development-records/defects.md` の `DFC-793` の後に足した）

```text
| DFC-840 | ⚠️ **空の行の帯高が、仕様とコードで違う** —— `05-07-design.md` の 表 T-221 の `LF-2` は「`Task` を 1 つも持たない段は、矩形が縦に取る高さとする」と段ごとに `VG-2` を加え、`SWS-2` の RATIONALE は「空の段と空の行が矩形 1 段ぶんを取る」とする（表示の倍率 100 で 18.125 ＋ 3.5 ＝ 21.625px）。`src/entity/layout-engine/schedule-layout/schedule-layout.ts:982-984` は `Task` の無い行の段を 0 と数え、帯を矩形の床（`drawnExtentOf('rectangle')`、18.125px）だけにする。行の操作子の格子の床（24.0012px）が両方を隠していたので、`CR-553` が床を外すと表に出る | 空の行の帯は矩形 1 段と `VG-2`（`LF-2` ・ `SWS-2`）—— 表示の倍率 100 で 21.625px、`Task` を 1 つ載せた 1 段の行と同じ高さ | `Task` の無い行にも矩形 1 段を置き、`VG-2` を足す（`schedule-layout.ts:971-984`）。`LF-2` と `SWS-2` は変えない | ⭐ 決定 A（前に立つ者、2026-09-23。`change-request/CR-553-the-row-controls-no-longer-set-the-row-height.md` の決定 13）: 仕様が正 —— `05-07-design.md` の 表 T-221 の `LF-2`「`Task` を 1 つも持たない段は、矩形が縦に取る高さとする」と `SWS-2` の「空の段と空の行が矩形 1 段ぶんを取る」。理由: 最初の `Task` を載せたときに行の高さが跳ねない（18.125 → 21.625 の 3.5px の跳ねが、その下のすべての行を動かす） | `実装待ち` | ⭐ 2026-09-23、`CR-553` を書いた体が、1 段の行の帯高を式から導くときに見つけた（`bandFloorOf` と `LF-2` の読み合わせ）。コードは `CR-553` の波 1a で直す | `LF-2` ／ `LF-3` ／ `SWS-2` ／ `JDG-420` ／ `tests/unit/layout-engine.test.ts:835` ・ `tests/integration/schedule-drawing.sws.test.ts:1004-1050`（いまは格子の床で両方を満たす） | ⛔ まだ押していない。 |
```

---

## 13. 測り方の再現

```
# totals (before), a912f4ea, 2026-09-23
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   -> tables=186  figures=27  rows=2282  uids=162

# free identifiers (2026-09-23, a912f4ea): maximum over docs/spec src tests tools change-request
for p in LF HF S VG; do
  git grep -ohE "\b$p-[0-9]+[a-z]?\b" -- docs/spec src tests tools change-request | sed "s/$p-//;s/[a-z]$//" | sort -n | uniq | tail -1
done
#   -> LF 15, HF 120 (a word in a test comment; the table's last row is HF-19, CR-551 takes HF-20),
#      S 999 (a test sentinel; the last row is S-338 after CR-551), VG 7
git grep -n "\bLF-16\b\|rowControlLatticeHeightPx" -- . ':!previous-project-result'
#   -> nothing but this file
grep -ohE 'JDG-[0-9]+' docs/development-records/rulings.md | sort -t- -k2 -n | tail -1   # -> JDG-399
grep -ohE 'DFC-[0-9]+' docs/development-records/defects.md | sort -t- -k2 -n | tail -1   # -> DFC-793

# the one-lane band at display scale 100, from the manuscript (scratchpad cr553-derive.py reads
# the defaults out of docs/spec/_source/settings.json and mirrors schedule-layout.ts:296-331, :792-794, :982-984)
#   ratio (FR-039)  = S-234 100 / 100 x S-236 0.625                          = 0.625
#   plan (FR-094)   = max(S-6 16 x 0.625 / S-5 0.5715, S-4 28 x 0.625 x S-76 1) x S-13 1.0 = 17.5
#   drawn edge (VG-5, JDG-160 b: the border's outer rim) = 17.5 + S-39 1 x 0.625 = 18.125
#   VG-2            = S-11 1 + S-18 2.4 x 0.625 + S-11 1                     = 3.5
#   LF-2, one lane  = 18.125 + 3.5                                           = 21.625
#   LF-3 floors     = rectangle 18.125 ; row name box S-36 19.5 x 0.625 x S-38 1.3 = 15.84375 (depth 1), 12.1875
#   lattice (LF-16) = 2 x (S-138 16 + 2 x S-243 1) x S-235 0.6667            = 24.0012
#   before: max(21.625, 18.125, 15.84375, 24.0012) = 24.0012   after: 21.625   (S-12 rowGap 0)
#   reserve under the last row: one rectangle lane 2.3762 ; an empty or thin-only row 5.8762
#   display 50: one lane 11.8125, rectangle floor 9.0625 ; display 67: 15.14875, 12.14375 (question 3)
#   a thin-only one-lane row (arrow): label 7.5 + S-196 2 x 0.625 + stroke 1.75 + gap 1.25 + 0.875 + head 1.75
#                                      = 14.375, + VG-2 = 17.875 < 18.125 -> the rectangle floor, 18.125

# graph
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>      # each ID of 6.1
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py <seeds>  # each row of 6.2
#   LF-16 measured on a COPY of the tree with section 4 applied (impact.py reads the Markdown)

# every old block of section 4 occurs exactly once in its file, and all 18 apply in order to a COPY:
PYTHONIOENCODING=utf-8 python <scratchpad>/cr553-edits.py
#   -> edits 18 applied 18 problems 0 (E-16 added 2026-09-23 after the front answered question 3)
#   -> CR-551 old blocks read 35; sharing a line 0; adjacent 2 (E-04 next to CR-551 E-26, E-07 next to E-15)
#   -> the same script pointed at CR-552's draft (untracked, read at the end of drafting):
#      old blocks read 11; sharing a line 0; adjacent 0
#   -> leftovers on the copies 0 (no "行の帯高の下限であること", "第 2 の下限", "格子の床",
#      "行の帯の床（`HF-19`）", "rowControlLatticeFloorPx", "矩形の行と同じ高さになる", "行の操作子の高さ、")
#   -> the settings.json copy parses with json.loads
# md-checks.py on a copy of docs/spec with section 4 applied:
#   -> tables=186 figures=27 rows=2283 uids=162 ; checks 5, 6, 7, 8, 9, 10, 15, 48 all 0

# change-request discipline (check 22) and identifier reservation (check 62), judged by exit code
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/check-cr-discipline.py ; echo "exit=$?"
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/check-identifier-reservation.py ; echo "exit=$?"
```

### 13.1 当てた後の整形（2026-09-24）

4 節を当てた木で `npm run check` が 11 ・ 39 ・ 45 ・ 46 ・ 47 で赤になった（当てる前の `a7336de0` では全部緑）。基準値はどれも動かさず、次のとおり直した。仕様の文は形だけを変え、意味は変えていない。

| 直した所 | 元の編集 | 旧 → 新 | 検査 |
|---|---|---|---|
| `HF-19` の頭の文 | E-04 | 「（`HF-4` の並び。2 段）」→「（`HF-4` の並び、2 段）」 —— 括弧の中の句点が文末に数えられていた | 46 |
| `OP-10` の実測の注 | E-08 | 「（そのころは行の操作子の格子を数えていた。表 T-051 の `HF-19`）」→「（そのころは行の操作子の格子（`HF-19`）を数えていた）」 —— 太字の中に文末が 2 つあり、括弧の中の句点も文末に数えられていた | 46 ・ 47 ・ 11 |
| `VG-7` の注 | E-01 | 「（表 T-051 の `HF-19`）」→「（`HF-19`）」 | 11 |
| `FR-055` の RATIONALE | E-07 | 「最後の行の操作子（表 T-051 の `HF-19`）」→「最後の行の操作子（`HF-19`）」 | 11 |
| `tests/system/cr-553-a-shown-control-group-stays-its-row.test.ts` | E-02 | `HF-6` の ⛔「群が下の行に重なっているとき、…数えてはならない（MUST NOT）」を逐語で持ち、表 T-051 の `HF-6` にその文があることと、既にあった押しの判定の説明にその文を当てた | 39 |
| `src/framework/dom-screen-surface/screen-frame-drawing.ts` | （コード） | 横の `Scrollbars` を探す式が `dom-screen-surface.ts` の `markFrame` と `dialogue-field-drawing.ts` の 2 か所にあった → `horizontalScrollbar` 1 つにまとめ、両方がそれを呼ぶ。振る舞いは変えない | 45 |

⚠️ 11 の 2 群は新しい重複ではなかった。基準値にある 表 T-109 の 4 行の群（`IC-58` ・ `IC-77` ・ `IC-90` ・ `IC-92`）が 2 つに割れたものである。`docs/review/dup-check.py` は 60 を超える単位に現れる 4 文字の並びを捨てる。4 節の編集で「表 T-051 の `HF-n`」を含む単位が差し引き 4 つ増え（`VG-7` ・ `FR-055` ・ `OP-10` ・ `FR-029` ・ `S-243` で 5 つ増え、`S-12` で 1 つ減った）、「1のHF」と「51のH」が 59 から 63 単位になって捨てられ、4 行の似かたが閾値を下回った。同じファイルの中の参照 3 つから「表 T-051 の」を外して 60 に戻した。`tbl-settings.md` の `S-243` の注は別のファイルなので表の名を残した。

⚠️ `HF-19` の文を逐語で持つ試験 3 つ（`tests/unit/cr-414-*` ・ `tests/unit/cr-553-*` ・ `tests/unit/lf-3-hf-19-*`）の引用も同じ整形に合わせた。
