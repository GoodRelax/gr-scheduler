# CR-211 — どちらの復号器へ送るか、そして誰が答えを返すか

> **閉じるもの**: ⭐ **利用者の裁定（2026-08-22。`docs/review/rulings-2026-08-22/03-open-path-owners.md` の 5 問）** ——
> ⭐ **形式の判別（拡張子と先頭 1 文字の両方）・`GRS` は `.json`・MSPDI は `.xml`・`Ctrl+S` は常に `GRS JSON`・確認の 2 択を 表 T-109 の行にする。**
> ⛔ **`FR-023` の順序を守る** —— **BOM は先に捨ててから 1 文字目を見る。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作図ソフトと同じ操作感で描きながら、構造化した日程データを出す）。受ける目標は `GL-004`。**

⛔ **復号器は 4 本とも書けているのに、繋げない。** ⚠️ **`OP-1` は「`GRS JSON` と MSPDI XML を受け付ける」と書くのに、どちらかを決める者がどの表にも無い** ——
`FileGateway` は明文で断り（「形式を嗅ぐ門番は、誰も書かなかった 4 つ目の権限を持つことになる」）、`openDocumentFile` は文字列までしか返さず、
**`ImportFormat` は `importDocument` の引数なのに産む者がいない。**

⭐ **`CH-4`（説明を読まずに操作できる）にも掛かる。** ⛔ **確認の 2 択は、問いの形も画面の値も在るのに、押す入口が 1 つも無い** —— 3 つのファイルが同じ `STOP` を書いている。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.3`**（唯一の正がある）| 形式の判別 | ⛔ **本物の違反。** ⭐ **正がどこにも無い。** ⚠️ **`PD-104` が「どの表も拡張子も MIME 型も持たない」と記録しており、ファイル名からも推測できない** |
| **`R1.4`**（境界条件の明示）| 受理と拒否の境界 | ⛔ **本物の違反。** ⭐ **「両方試す」は、壊れた `GRS JSON` を MSPDI の誤りとして人に見せる。** ⚠️ **拡張子だけでも中身だけでも、片方が嘘をつくと黙って誤った復号器へ流れる** |
| **`R3.3`**（外部入力の検証）| BOM 付きの `GRS JSON` | ⛔ **順序が規則の一部である。** ⚠️ **BOM を捨てる前に 1 文字目を見ると、表計算ソフトが吐いた `GRS JSON` が「JSON として解析できない」と読まれ、`FR-023` の MUST NOT（BOM を理由に拒まない）が破れる** |
| **`R1.3`**（同上）| `Ctrl+S` が MSPDI で開いた文書に何を書くか | ⛔ **`FR-096` の RATIONALE が「ここで決まる」と自ら書いているのに、決めていなかった。** ⭐ **`FR-021` の無損失は「合流させずに、編集せずに書き出したとき」に限られており、`SK-11` が書くのは編集した後の文書である** |
| **`R2.19`**（コンポーネント境界）| 確認の答えの返り道 | ⭐ **表 T-065 の MUST が道を 1 本に定めている** ——「点がどの入口の上かは、その入口を描いた側が答えること」。⛔ **したがって答えは `ScreenPart.entry`、すなわち 表 T-109 の行としてしか返せない** |
| **`R2.9`**（YAGNI）| 新しいコンポーネント | ⭐ **1 つも立てない。** ⛔ **開く道を丸ごと持つコンポーネントは 1 つに 5 つの仕事を負い（`R2.2`）、刷った数（コンポーネント 38・ユニット 71）と図 2 枚が動く** |
| **`R1.2`**（曖昧表現の排除）| 「先頭の 1 文字」 | ⭐ **「先頭の非空白 1 文字」と書いた** —— 改行や空白で始まる `GRS JSON` は正当である |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **値（拡張子と先頭の 1 文字）を 表 T-024 に、規則を 表 T-024a に置いた** | ⛔ **「値は表、規則は要求」**（規則 02 の 4.）。⚠️ **判別の行に値を書き込むと、形式を足したときに 2 か所を直すことになる** |
| 2 | ⭐ **拡張子を書いたのは `IO-1` と `IO-2` だけである** | **裁定が定めたのはこの 2 つだけである。** ⛔ **SVG・PNG・単一 `.html` の拡張子は `suggestedFileName` の話であり、まだ持ち主が無い**（裁定 03 の §6 の 3）。**要らない裁定を発明しない** |
| 3 | ⭐ **`SK-11` の答えは `FR-096` に書き、表 T-036 の `SK-11` からは指すだけにした** | **`FR-096` の RATIONALE が「表 T-036 の `SK-11` が MSPDI から開いた文書に対して何をするかも、ここで決まる」と自ら名乗っている。** ⛔ **鍵の表に規則を書かない** |
| 4 | ⭐ **確認の面に `U-55` `Confirmation` を与えた** | ⛔ **表 T-109 の前文が「`面` の欄は 表 T-103 の確定名である。新しい面の名を作らない」と定める。** ⚠️ **`ScreenView.confirmation` は `CR-193` が置いたが、面の名は無かった** |
| 5 | ⭐ **確認の 2 択を 2 行（`IC-69` / `IC-70`）にした** | ⛔ **`NT-7` が「続けるか取りやめるかを選ばせること（MUST）」と定めており、選択肢は 2 つである。** ⚠️ **`ScreenPart.entry` は 1 行を指すので、2 択には 2 行が要る** |
| 6 | ⭐ **図形は丸で囲んだ ✓ と ✗ にした** | ⛔ **裸の ✗ は `IC-52`（開いている面を閉じる）が既に使っており、四角で囲んだ ✓ は `IC-67` が使っている**（自分で図形を取り出して確かめた）。⚠️ **同じ図形を 2 行に与えると、`FR-029` の「用途を伝える」が果たせない** |
| 7 | ⭐ **`viewBox` を動かさなかった** | **最終行（`y=232`）は 8 個までしか埋まっておらず、12 列目まで空いている**（自分で座標を数えた）。⛔ **高さを変えると 68 個すべての位置が読み直しになる** |
| 8 | ⭐ **`RC-13`（新しい図形を実物と見比べる債務）に 2 つ足した** | **`CR-191` が起こした 12 個と同じ立場である。** ⛔ **図形の決定は利用者の裁定であり、本 CR は「見比べていない」と印すだけである** |

---

## 1. 測った（⛔ 2 本とも走らせた）

```
impact.py  T-024
impact.py  T-024a
impact.py  T-109
impact.py  T-103
induced.py T-024 IO-1 IO-2 T-024a OP-1 OP-5 FR-023 FR-087 T-036 SK-11 FR-060 FR-096 T-103 T-109 T-037 NT-7 FR-029 T-227 DI-4
```

| 測ったこと | 実測 |
|---|---|
| 種が仕様に解決したか | **19 / 19**（空のグラフではない）|
| 種の集合の内側の辺 | **14** |
| 閉路 | ⭐ **0 件。** したがって 1 か所ずつ順に書いてよい —— **ただしオブジェクトごとに 1 回である** |
| ⭐ **塊** | 表 T-024 と 表 T-024a は**同じ塊「開くと合流」**に属する（同じ塊に 表 T-032）。⛔ **本 CR は 表 T-032 を 1 行も触らない** |
| 表 T-024 を指している要求 | **6 件** |
| 表 T-024a を指している要求 | **5 件**。行は **11 行** → ⭐ **12 行** |
| 表 T-109 を指している要求 | **3 件**（`FR-099` `FR-029` `FR-038`）。行は **68 行** → ⭐ **70 行** |
| 表 T-103 を指している要求 | ⚠️ **0 件**（名前の表は要求から浮いている）。行は **55 行** → ⭐ **56 行** |

---

## 2. 何が欠けていたか（**逐語**）

### 2.1 判別器がどの表にも無い

`file-gateway.ts`（逐語）:

```
It does not know what a schedule is ... A gateway that sniffed the format would hold
a fourth authority nobody wrote down
```

`openDocumentFile`（逐語）:

```
⚠️ The text as the bytes encode it. No format has been guessed
```

⛔ **`ImportFormat = 'grsJson' | 'mspdi'` は `importDocument` の引数であり、産む者がいない。**

### 2.2 `FR-096` は「ここで決まる」と書いていた（**決めていなかった**）

`01-04-requirements.md` の `FR-096` の RATIONALE（逐語）:

```
表 T-036 の SK-11（保存する）が MSPDI から開いた文書に対して何をするかも、ここで決まる。
```

⭐ **`FR-021` が答えの根拠を持っていた**（逐語）:

```
1 つの MSPDI を取り込み、合流させずに、編集せずに書き出したとき、GRS は、XML 正規化して
比べたときに元のファイルと同じものを出すこと
```

⛔ **`SK-11` が書くのは「編集した後」の文書である。** ⚠️ **無損失の保証はそこに掛かっていない。**

### 2.3 確認の答えの返り道が無い（**3 つの `STOP` が同じことを書いている**）

```
⭐ 問いの 形 は在る        Confirmation 型（manner / text / items）と ScreenView.confirmation
⛔ 画面は描いていない       dom-screen-surface.ts:「⛔ ONE MEMBER IS NOT DRAWN: view.confirmation」
⛔ 押す入口が無い           表 T-109 に確認の 2 択の行が無く、表 T-103 に名前も無い
⛔ 答えの返り道が無い       notices.ts:「⚠️ STILL OPEN AFTER TABLE T-227」
```

---

## 3. 編集計画（**オブジェクトごとに 1 回**）

| # | ファイル | オブジェクト | 何をするか |
|---|---|---|---|
| 1 | `01-04-requirements.md` | 表 T-024 の `IO-1` の備考 | 拡張子 `.xml` と先頭の非空白 1 文字 `<` を足す |
| 2 | `01-04-requirements.md` | 表 T-024 の `IO-2` の備考 | 拡張子 `.json` と先頭の非空白 1 文字 `{` を足す |
| 3 | `01-04-requirements.md` | 表 T-024a に `OP-12` | **1 行**（両方合致で読む。BOM は先に捨てる）|
| 4 | `01-04-requirements.md` | `FR-096` の散文 | **1 文**（`SK-11` はどの形式から開いた文書でも `GRS JSON` で書く）|
| 5 | `01-04-requirements.md` | 表 T-036 の `SK-11` のセル | `FR-096` を指す |
| 6 | `_assets/tbl-glossary.md` | 表 T-103 に `U-55` | **1 行**（`Confirmation`）|
| 7 | `_assets/tbl-glossary.md` | 表 T-109 に `IC-69` / `IC-70` | **2 行** |
| 8 | `_assets/tbl-glossary.md` | 表 T-109 の前の散文 | 「68 行ある」→「70 行ある」、「全 68 行が」→「全 70 行が」 |
| 9 | `_assets/fig-icons.svg` | 図 F-019 | **2 図形**（丸で囲んだ ✓ と ✗）と `aria-label` |
| 10 | `01-04-requirements.md` | 表 T-026 の `RC-13` | 12 個 → 14 個（`IC-58` 〜 `IC-70`）|
| 11 | — | `npm run gen` | `icon-roster.json` が 68 → 70 |
| 12 | `A-appendix.md` | 変更履歴 | 1 行足す（版 0.88）|

⛔ **触らないもの**:

```
表 T-032          合流の規則は本 CR の対象外（同じ塊だが別の問い）
表 T-227          DI-1 〜 DI-5 は 1 文字も動かない
表 T-037          NT-7 が既に問い方を持つ。行を足さない
src/              icon-roster.json だけが生成で動く
```

---

## 4. 数の予測

| | 前 | 予測 |
|---|--:|--:|
| `tables` | 130 | **130** |
| `figures` | 11 | **11** |
| `rows` | 1592 | ⭐ **1596**（`OP-12` ／ `U-55` ／ `IC-69` ／ `IC-70`）|
| `uids` | 146 | **146** |
| 表 T-024a の行 | 11 | ⭐ **12** |
| 表 T-103 の行 | 55 | ⭐ **56** |
| 表 T-109 の行 | 68 | ⭐ **70** |
| 図 F-019 の図形 | 68 | ⭐ **70**。`viewBox` は **452 × 284 のまま** |
| `icon-roster.json` | 68 | ⭐ **70** |
| 重複検出 | `A=26 (new 0)` | **`A=26 (new 0)`** |

---

## 5. 試験（**期待値を曲げない**）

```
1. ⛔ BOM 付きの GRS JSON（拡張子 .json）が読める試験
   ⭐ 順序を逆にすると落ちる —— FR-023 の MUST NOT がこれで守られる
2. ⛔ 拡張子 .json で中身が < のファイルが拒まれる試験（およびその逆）
3. ⛔ MSPDI で開いた文書の Ctrl+S が GRS JSON を書く試験
4. ⛔ 確認の 2 択を押した答えが ScreenPart.entry で返る試験（IC-69 / IC-70）
```

⚠️ **書くのは実装した者ではない**（規則 04 の 1.）。

---

## 6. ⛔ 本 CR が閉じないもの

| | 何 | どこへ |
|---|---|---|
| 1 | ⛔ **`askToWriteOver` を実装が 1 度も呼ばない** | ⚠️ **`file-system-access-file-store.ts` の欠陥である。** ⭐ **本 CR が入口を開けても、これを直すまで `DI-4` は到達しない** |
| 2 | ⛔ **`suggestedFileName` の出どころ** | ⚠️ **SVG・PNG・単一 `.html` の拡張子はまだ持ち主が無い**（`PD-104`）|
| 3 | ⛔ **`emptyRowTaskUids` を産む者がいない** | `validateImportedDocument` の引数 |
| 4 | ⛔ **二重に開いたときの規則** | `ImportRequest.anotherOpenInProgress` という欄だけが在る |
| 5 | ⛔ **新しい 2 つの図形を実物と見比べること** | ⭐ **表 T-026 の `RC-13` が持つ**（`CR-191` の 12 個と同じ立場）|
