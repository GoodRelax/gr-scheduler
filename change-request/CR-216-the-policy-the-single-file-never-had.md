# CR-216 — 単一ファイルが 1 度も持っていなかった方針

> **閉じるもの**: ⛔ **表 T-003 の `CN-8` は「単一 HTML に対する内容セキュリティ方針（`CSP`）を持つ」と定めるのに、成果物に 1 つも無い。**
> ⭐ **`CN-8` が決めているのは `img-src` だけであり、残りの指令はどこにも無かった。**
> ⭐ **利用者の裁定（2026-08-23）で全数が決まった。表 T-232 を新設し、`SWS-8` を置く。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-5`。受ける目標は `GL-005`。**

⛔ **`NFR-009`（不正な入力によって任意のコードが実行される状態にならないこと、MUST NOT）が、その RATIONALE で「内容セキュリティ方針は表 T-003 の `CN-8` が持つ」と `CN-8` を名指している。** ⚠️ **その `CN-8` が指令を 1 つしか持たないので、`NFR-009` の担保は実質どこにも無かった。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.1`**（計測可能な基準）| `CN-8` | ⛔ **「`CSP` を持つ」は在るか無いかしか測れない。** ⭐ **指令を全数で書けば、成果物と突き合わせられる** |
| **`R3.3`**（外部入力の検証）| `NFR-009` | ⛔ **実証済みの穴。** ⭐ **外部 `img` を仕込んだ成果物を実際に開いたところ、要求がネットワークへ出た**（自分で確かめた）。方針を入れた版では遮断される |
| **`R2.9`**（YAGNI）| 指令の数 | ⭐ **`default-src 'none'` を土台にし、実際に使うものだけを開ける。** ⛔ **`connect-src` / `font-src` / `object-src` を個別に書かない** —— 土台が既に閉じている |
| **`R2.18`**（最小構成との比較）| `nonce` 案 | ⛔ **採れない。** `nonce` はサーバーが要り、`NFR-004` は**ファイルを直接開いた状態での判定を MUST としている** |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`base-uri` と `form-action` を足した** | ⛔ **`default-src` は この 2 つに及ばない**（規格上そうである）。⚠️ 足さないと、注入された `<base>` が相対参照の行き先を変えられる |
| 2 | ⭐ **`img-src` は `data:` だけとし `blob:` を足さなかった** | ⛔ **実装が `blob:` を使っていないことを自分で確かめた** —— `canvas-rasterizer.ts` は `data:` の URL を作り、その理由（`URL.createObjectURL` を掴まないと単体試験が browser 無しで書ける）を自分で書いている |
| 3 | ⭐ **`script-src` にハッシュを 1 つだけ置く** | ⛔ **成果物のスクリプトは 1 つである**（`vite.config.ts` の結線が 1 枚にまとめる）。⚠️ `worker-src` は要らない —— `new Worker` も `eval` も `new Function` も `src/` に 0 件（自分で数えた）|
| 4 | ⭐ **ハッシュの値そのものを仕様に書かない** | ⛔ **ビルドのたびに変わる。** 仕様が持つのは「埋め込んだスクリプトの `sha256` を置くこと」という規則である |

---

## 1. 測った

```
induced.py  CN-8 NFR-009 NFR-004 CN-6 CN-1 T-003
impact.py   NFR-009
```

| 測ったこと | 実測 |
|---|---|
| 種が仕様に解決したか | **6 / 6** |
| **閉路** | ⭐ **0** |
| 表 T-003 は行を足すか | ⛔ **足さない。** `CN-8` の欄も動かさない —— 指令の全数は 表 T-232 が持ち、`CN-8` はそれを指す |

---

## 2. 編集計画

| # | 対象 | すること |
|---|:-:|---|
| 1 | **表 T-232**（新設）| `PO-1` 〜 `PO-6` を置く |
| 2 | **`SWS-8`**（新設）| 親は `NFR-009`。STATEMENT は 表 T-232 を指す |
| 3 | 表 T-003 の `CN-8` | **末尾に「全数は 表 T-232 が持つ」を足すだけ。** ⛔ 指令を書き写さない（1.9）|

---

## 3. 数の予測

| | 前 | 後 | 差 |
|---|--:|--:|--:|
| `tables` | 131 | **132** | +1 |
| `rows` | 1602 | **1608** | +6 |
| `uids` | 147 | **148** | +1 |
| `figures` | 11 | **11** | 0 |

---

## 4. 置く中身

### 表 T-232 — 単一 HTML の内容セキュリティ方針

| 行 ID | 指令 | 値と理由 |
| --- | --- | --- |
| PO-1 | `default-src` | `'none'`。**土台を閉じ、要るものだけを個別に開ける。** ⛔ 開けていない取得元は、すべてこの行が拒む |
| PO-2 | `img-src` | `data:`。**値は 表 T-003 の `CN-8` が持つ。** ⛔ `blob:` を足さない —— 絵を `<img>` へ渡す道は `data:` の URL である |
| PO-3 | `style-src` | `'unsafe-inline'`。⚠️ **成果物は 1 枚なので、様式は要素に直に載る。** ⛔ 外部の様式表は `PO-1` が拒む |
| PO-4 | `script-src` | **埋め込んだスクリプトの `sha256` を 1 つだけ置くこと（MUST）。`'unsafe-inline'` を書いてはならない（MUST NOT）** —— それでは注入されたスクリプトも走る。⛔ `nonce` は採れない —— サーバーが要り、`NFR-004` はファイルを直接開いた状態での判定を MUST としている |
| PO-5 | `base-uri` | `'none'`。⚠️ **`PO-1` はこの指令に及ばない** —— 足さないと、注入された基底 URL が相対参照の行き先を変えられる |
| PO-6 | `form-action` | `'none'`。⚠️ **`PO-1` はこの指令にも及ばない** |

**ハッシュは、埋め込んだスクリプトの本文そのものから作ること（MUST）。** ⚠️ **本文に制御文字が 1 つ混じるとブラウザの読む字面が変わり、ハッシュが合わずに画面全体が出なくなる** —— 文字列の鍵を ASCII に限る規則は 表 T-003 の `CN-5` と同じ理由に立つ。

### `SWS-8` — 単一 HTML の内容セキュリティ方針

**Type**: SW_SPEC
**UID**: SWS-8

**STATEMENT**: 成果物を組み立てるとき、`GRS` は、表 T-232 の指令を持つ内容セキュリティ方針を成果物へ埋め込むこと。

**RATIONALE**: 表 T-003 の `CN-8` が方針を持つことを定め、`NFR-009` がその方針を担保として名指している。⚠️ **`CN-8` が値を持つのは `img-src` だけであり、残りの指令はどこにも無かった。** ⛔ **方針が無いあいだ、成果物に混じった参照は実際に外へ出る**（`NFR-004` の RATIONALE がいう「その開き方でだけ落ちる形」の裏返しである）。**裁定の記録は `docs/review/rulings-2026-08-23/RULINGS.md` が持つ。**

**Relations**:

- **Type**: `Parent`
  **ID**: `NFR-009`
  **Role**: `Satisfies`

---

## 5. ⛔ 本 CR が閉じないもの

| 件 | 状態 |
|---|---|
| **方針が破れたことを人へ告げるか** | ⛔ **開いたまま。** 表 T-037 に行が無い。⭐ **いまは告げない** —— 破れる形は開発中にしか起きない見込みである |
