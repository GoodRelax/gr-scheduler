# CR-258 — 半分しか見えていなかった選択

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-25）: 選択は外接矩形で囲む。**
> ⚠️ **いまは対象自身の輪郭を破線でなぞっており、しかも面のある形状にしか出ない。**
> ⛔ **細線のタスク・依存線・ハイライトボックス・コメントボックス・基準日線は、選択の合図を 1 つも持たない**（`SL-8` の MUST 違反）。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（絵の上で直に編集できる）。受ける目標は `GL-004`。**
⛔ **何を選んでいるか読めなければ、まとめて動かす操作（`SL-7`）はどれに効くか分からない。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `SL-8` の対象 | ⛔ **`SL-1` は 5 種を選べると定めるのに、合図を持つのは 1 種の一部だけである** |
| **`R2.18`**（自己矛盾が無い）| `SL-8` と `FR-030` | ⭐ **どちらも「色以外の手掛かり」を求めており、矛盾はしていない。欠けているのは形である** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| 形状ごとの合図 | ⭐ **外接矩形なら規則は 1 つで済む。**⛔ **形状ごとに描き方を決めると 5 通りになる** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **線の太さと破線の刻みを 表 T-206 に置く**（`S-174` / `S-175`）| ⛔ **文書に保存しない** —— 見せ方だけの選択であり、保存形に痕跡が残らない。⚠️ **いま描く側が自分で選んだ数（2px / 3 2）を暫定で持っており、`PD-1` が付いている** |
| 2 | ⚠️ **推奨値は 2px と `2 2`。⛔ 測って決めた値ではない** | ⭐ **可逆コストは 1 セル ＋ 生成である**（規則 06 の分類 `A`）|
| 3 | ⭐ **色は `S-151` のまま** | ⛔ **発明していない** —— 表 T-236 の同行の用途の欄が逐語で「選択と現在位置」である |
| 4 | ⭐ **矩形は対象の外接枠に、線幅ぶんの隙間を置かず密着させる** | ⚠️ **隙間を置くと、その隙間の値がもう 1 行要る。**⛔ **裁定は「外接矩形で囲む」であって「離して囲む」ではない** |
| 5 | ⛔ **倍率に追随させない** | ⭐ **`FR-094` が依存線・進捗マーカー・再開アイコンに同じことを定めており、選択の枠も画面上の道具である** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| いま描かれている条件 | `selected.has(uid) && task.plan?.form === 'outline'` —— ⛔ **面のある形状（`SH-1` / `SH-2` / `SH-5`）だけ** |
| 描き方 | 予定バー自身の多角形を破線でなぞる（太さ 2、刻み `3 2`）—— ⛔ **どの数も表に無い** |
| 色 | 本日 `S-151` へ繋いだ（それまでは発明した灰色）|
| 合図を持たない対象 | ⛔ **細線のタスク（`SH-3` / `SH-4`）・依存線・ハイライトボックス・コメントボックス・基準日線の 5 種** |
| `SL-8` を指す要求 | `FR-083`（1 件）|
| 誘導部分グラフの閉路 | ⛔ **無し** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-023c の `SL-8` | **選択された対象を、その外接矩形に沿った破線の枠で囲むこと（MUST）。`SL-1` の 5 種すべてに同じ規則を当てること（MUST）。倍率に追随させてはならない（MUST NOT）。** 太さと刻みは 表 T-206 の `S-174` / `S-175`、色は 表 T-236 の `S-151` |
| 2 | `_source/settings.json` の 表 T-206 | 2 行（`S-174` 太さ 2px 🔎、`S-175` 破線の刻み `2 2` 🔎）|
| 3 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1690 | **1692** | ＋2 |
| `uids` | 148 | **148** | 0 |
