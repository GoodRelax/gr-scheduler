# CR-270 — 3 つの規則を黙らせていた 1 つの `false`

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-27。案 D1、暫定）** ——
> 逐語は「**詳細を確認する時間が無いので、一旦推奨通りで進めろ。詳細は defects.md に書いておけ**」。
> ⭐ **台帳の行は `D-89`。**
> ⛔ **仕様には規則が 4 本とも既に在る。無いのは、規則が答えを受け取る継ぎ目だけである。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（作った人の意図どおりに読める）と `GL-006`（読む人が強制されない）。**
⛔ **3 つの MUST が、実装も試験も揃ったまま黙っている** —— `IN-5a`（入力中は `Delete` を飲み込む）・
`IN-4` の第 1 階層（`Esc` がその場の編集を取り消す）・`WS-2` / `AG-9`（編集の最中は Agent API の書き込みを拒む）。
⚠️ **どれも「入力中である」ことを誰も答えないために働かない。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 表 T-065 の `IF-9` | ⛔ **`ScreenSurface` は「確定していない入力があるか」を答えない。**⚠️ **答えを持っている側は既にそれを持っている** —— `dom-screen-surface.ts:3579` の `isFieldHeld` が `focusin` で立ち `focusout` で降りる |
| **`R2.19`**（驚かせない）| `frame-loop.ts:3558` の `isTextEntryUnsettled: false` | ⛔ **これは「まだ早い」ではなく「偽である」。**⚠️ **同所の以前の注は偽であった** —— 「`IN-5a` の『編集入力の確定前』は `editInPlace` が開く欄しか取り得ない状態である」と書いていたが、`AG-9` は逐語で「**プロパティパネルで入力中など**」と名指しており、**パネルの欄は描かれており、焦点も当たる** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| 状態の呼び名 | ⭐ **`IN-5a` が「状態の呼び名は 表 T-035 の `AG-9` と同じ」と自ら書いている。**⇒ **3 つの規則が 1 つの述語を共有しており、答える場所も 1 つでよい** |
| **`R1.4`** | 分類 | ⛔ **規則 06 は継ぎ目を `E` に分類し、`E` は体の判断で動かせない。**⭐ **だから体は正しく止まった。**⚠️ **止まったまま 2 巡が過ぎたことが誤りであった** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`IF-9` に「確定していない入力があるか」を答える 1 項を足す** | ⛔ **`IF-9` は既に「対話欄で確定した発話」「プロパティパネルの欄で確定した値」を返している。**⇒ **欄を持っているのはこの継ぎ目であり、欄の状態を答える先も同じである。**⚠️ **新しい継ぎ目を立てない** —— 立てると `CP-37` / `CP-38` の外に道が 1 本増える |
| 2 | ⛔ **どの欄が保持しているかは返さない。真偽 1 つだけを返す** | ⭐ **3 つの規則はどれも「入力中か」しか問わない**（`IN-4` は階層を消費し、`IN-5a` はキーを飲み、`WS-2` は拒む）。⚠️ **どの欄かを返すと、読む側がそれを使い始めて継ぎ目が太る** |
| 3 | ⛔ **`AG-9` / `IN-4` / `IN-5a` / `WS-2` は 1 文字も動かさない** | ⭐ **4 本とも既に正しい。**⛔ **本件は規則の不足ではなく、規則へ材料が届いていないことである** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 固定値の場所 | `src/framework/single-html-shell/frame-loop.ts:3558` —— `isTextEntryUnsettled: false` |
| 読む側 | ⛔ **4 か所**: `input-command-translator.ts` の 2435（`IN-4` の階層）・2455（確定）・4127（`Ctrl` ＋ `A` の抑止）・4299 |
| 状態の型 | `screen-state.ts:139` が `isTextEntryUnsettled` を持ち、`:197` が `escapeTarget` の最上位（`'textEntry'`）に使う |
| 答えを持っている側 | ⭐ **`dom-screen-surface.ts:3579` の `isFieldHeld`。**`propertiesPanel` の `focusin` で `true`、`focusout` で `false`。⚠️ **どこへも公開されていないローカルである** |
| ⇒ 黙っている規則 | ⛔ **3 本**（`IN-5a` ／ `IN-4` の第 1 階層 ／ `WS-2` と `AG-9`）。⭐ **どれも実装済みで試験も緑である** |
| `induced.py`（`IF-9` `PI-37` `PI-38` `AG-9` `WS-2` `IN-4` `IN-5a` `CP-37` `CP-38`）| **閉路 0。**⇒ **1 つずつ書いてよい** |
| `IF-9` がいま答えるもの | 3 つ（描画の記述を載せる ／ 発話 ／ 欄で確定した値と行 ID ／ 点の上の入口と形式）|

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-065 の `IF-9` | ⭐ **「確定していない文字入力があるかを答える」を 1 項として足す。**⚠️ **どの欄かは返さない旨を添える** |
| 2 | `A-appendix` の変更履歴 | 1 行（`1.22`）|

⛔ **`AG-9` / `WS-2` / `IN-4` / `IN-5a` は 1 文字も動かさない。**
⛔ **表 T-064 の `PI-37` / `PI-38` は動かさない** —— **公開する名前が増えない**（`ScreenSurface` は既に両方に載っており、足すのはその項である）。⚠️ **検査 26b で確かめる。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1718 | **1718** | 0 |
| `uids` | 148 | **148** | 0 |
| 語の数（検査 27）| 260 | **260** | 0 |

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `rows` / `uids` | 135 / 11 / 1718 / 148 | **135 / 11 / 1718 / 148** | ✅ |
| 語の数 | 260 | **260（260 written）** | ✅ |
| 検査 26b | 新規 0 | **108 公開メンバー・new 0 ／ 241 の跨ぎ・new 0** | ✅ |
| 検査 22 本 | 全緑 | **ALL GREEN** | ✅ |

⭐ **予測どおり 表 T-064 は動かなくてよかった** —— 足したのは既に公開されている `ScreenSurface` の項であって、
新しい名前ではない。⛔ **検査 26b はそれを新規 0 で確かめた。**

⚠️ **本変更は仕様だけである。**`frame-loop.ts:3558` はまだ `false` を固定しているので、
⛔ **実装と試験は別の体が当てる**（規則 05 の 7.）。
