# CR-257 — パネルを広げれば済む説明

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-25）** —— 「タスクグループ名にマウスカーソルをあてたときのみ、行の操作子を表示する仕様とする。
> これで足りない分は `…` を表示しておき、`…` で省略された部分を見たい場合はパネルを拡げてもらう。」
> ⭐ **つまり、打ち切った行名の説明そのものを置かない。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`（1 画面で全体を読める）。受ける目標は `GL-002`。**
⛔ **説明は日程の上に立つ。立たなければ、そのぶん日程が見える。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R2.19`**（驚かせない）| 行名に乗せたとき | ⭐ **利用者は「出ない」ことを期待している。**⛔ **仕様が MUST で出させていた** |
| **`R1.4`**（欠落が無い）| 全文への道 | ⭐ **既に在る** —— `FR-085` 自身が「全文を見たい者はパネルを広げる（`FR-052`）」と書いている |
| **`R2.18`**（自己矛盾が無い）| `IN-3` と `FR-048` | ⛔ **どちらも `FR-085` を引き金として名指ししている。落とすなら 3 か所を同時に書く** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **打ち切りの印を `…` と綴ることを `FR-085` に書く** | ⛔ **裁定が逐語で `…` と綴っている。**⚠️ **表示語ではない** —— 言語によらない 1 文字であり、`FR-038` の辞書に入れると言語ごとに持つことになる |
| 2 | ⛔ **`HF-6`（ポインタが乗っているあいだだけ操作子を描く）は 1 文字も触らない** | ⭐ **裁定の前半は既にそのとおりに書かれている** |
| 3 | ⛔ **`S-140`（操作子に確保する場所）も触らない** | ⚠️ **打ち切りが早いのはこの 56px のためだが、裁定は「足りない分は `…`」であって「打ち切りを減らせ」ではない** |
| 4 | ⭐ **`IN-3` と `FR-048` から `FR-085` を落とす** | ⛔ **引き金でなくなったものを引き金の名簿に残すと、次に読む者が実装を探す** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 起動テンプレートの打ち切り | ⭐ **7 行中 6 行**（`Whole Produc…` など）|
| `FR-085` が全文への道を持つか | ⭐ **持つ** —— 「全文を見たい者はパネルを広げる（`FR-052`）」 |
| `FR-085` を引き金として名指しする場所 | **2 か所** —— 表 T-028 の `IN-3`、`FR-048` の名簿（後者は本日 CR-254 で足したばかり）|
| 誘導部分グラフの閉路 | ⭐ **1 つ** —— `FR-085` `HF-6` `S-140`。⛔ **`HF-6` と `S-140` は触らないので、書くのは `FR-085` だけである** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-085` | 全文をツールチップで出す MUST を削る。**末尾を `…` で打ち切ること（MUST）** と、全文はパネルを広げて見ること（既存の文を残す）|
| 2 | 表 T-028 の `IN-3` | 引き金の名簿から `FR-085` を落とす |
| 3 | `FR-048` | 描き直しの名簿から `FR-085` を落とす |
| 4 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1690 | **1690** | 0 |
| `uids` | 148 | **148** | 0 |
