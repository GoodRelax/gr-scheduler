# CR-197 — 何をもって同じ文書とするか、そして「確認を増やすな」の数え違い

> **閉じるもの**: 裁定 `D6`（`previous-project-result/temp/rulings-2026-08-21.md` の §1）。
> ⭐ **文書の同一性を **ファイル名 ∧ プロジェクト名 ∧ プロジェクト ID** の 3 つ組と定め、
> 別の文書の入っているファイルへ上書きしようとしたときに `NT-7` で問う。**
> ⛔ **それ以上作らない（YAGNI）** —— タブ間の持ち主決めも heartbeat も入れない。
> ⚠️ **測ってから分かったこと**: ⛔ **`FR-031` が「確認を求める場面は 2 つだけ。増やさない（MUST NOT）」と書いているが、
> その数は今日すでに合っていない。** 本 CR は数え直すのではなく、**列挙を階級に替える。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-5`（人の手で作った日程表を、壊さずに読み書きできるようにする）を進める。受ける目標は `GL-005`。**
⛔ **仕様書は「同じ文書」という語を 3 か所で使い、1 度も定義していない:**

```
FR-061  同じ機で別の文書や複製を開いても、自動保存が互いを取り違えてはならない（MUST NOT）
T-034   同じ文書で自動保存のほうが新しければ確認を求める。別の文書なら触らない
S-99b   有効化は…文書の識別子と対にして環境側へ置く   ⛔ 仕様書で「文書の識別子」が出る唯一の箇所
```

⭐ **`CH-4`（説明を読まずに操作できるようにする）も進める。受ける目標は `GL-006`。**
⛔ **他人の文書を黙って上書きすることは、取り消しで取り戻せない。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.3`**（矛盾・重複が無い。唯一の正がある）| `FR-031` の「例外は 2 つだけ」 | ⛔ **本物の違反が出た。** `:1560` は **「例外は `FR-032` と `FR-099` が確認を求めると定めた削除だけである…例外を増やさない（MUST NOT）」** と書く。⭐ **数えた** —— `OP-4`（`:3166`。置き換えのときに未保存の編集を捨てる前）と `FR-095`（`:3423`。同じ確認を行う）は**今日すでに問うており、どちらも削除ではない。** ⚠️ **`FR-099` を数え落としていたのを直したのが `CR-193` である**（変更履歴 `:98`）—— **同じ文が 2 度目の数え違いをしている** |
| **`R1.2`**（曖昧・非検証な表現の排除）| 「同じ文書」| ⛔ **本物の違反。** 3 か所が同じ語で判定を求め、**判定の規則がどこにも無い。** `src/` の 2 ファイルが `STOP` として記録している（§2）|
| **`R1.4`**（境界値を明示）| 3 つ組の `null` | ⛔ **`Project.id` も `Project.name` も `nullable` である**（`AT-1` / `AT-2`）。⭐ **「同じと言い切れないときは問う」に倒した** —— **黙って上書きした結果は取り消せず、余分に問うた結果は 1 回の手間である**（§0 ⑧ の 3）|
| **`R1.4`**（同上）| 相手のファイルが読めないとき | ⭐ **同じ理由で問う。** ⚠️ `OP-5`（`:3167`）が「検証は人に問う前」という順序の先例を持つ |
| **`R4`**（YAGNI）| 3 つ組を自動保存の鍵にも使うか | ⛔ **使わない。** ⚠️ **自動保存の鍵はファイル名を持てない** —— **1 度もファイルへ保存していない文書にも自動保存は掛かる。** ⭐ **本 CR は上書きの問いだけを閉じ、`documentKey`（`FR-061` / `T-034`）は開いたままにする**（§6）|
| **`R4`**（YAGNI）| タブ間の持ち主決め | ⛔ **作らない**（裁定 `D6` がそう定めた）。⭐ **前提は確かめてある** —— ブラウザは他のアプリに効くファイルロックを取れず、`localStorage` はオリジンごとに 1 つである |
| **`R1.5`**（MECE。将来の視点で漏れが無いか）| `NFR-012` の `FU-3` / `FU-4` | ⚠️ **「文書が 1 つのプロジェクトに固定された構造でないこと」を壊さないでおく義務がある。** ⭐ **本 CR は `Project` の数を変えない** —— `ET-1` が既に「文書に 1 つしか無い」と定めており、本 CR はその 1 つを**読むだけ**である。⛔ **ただし複数プロジェクトを入れる回には、この 3 つ組を必ず読み直すこと**（§6 に残した）|

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⛔ **`FR-031` の「例外は 2 つ」を、数え直すのではなく**階級**に替えた** | ⛔ **数え直すと 3 度目・4 度目が来る**（実測: 既に 2 度目である）。⭐ **4 つの場面に共通するのは「取り消しで取り戻せないものを失う」ことである** —— `FR-032` / `FR-099` は連鎖して消える範囲が人に見えない、`OP-4` / `FR-095` は**取り消しの履歴ごと捨てる**（`OP-4` 自身が「取り消しの履歴は引き継がない」と書く）、上書きは**他人のファイルであって取り消しの対象ですらない。** ⚠️ **`MUST NOT` は残す** —— それ以外の場面で問うてはならない |
| 2 | 問いを **`FR-096`（書き出す）** に置き、**`FR-060`（開いたファイルへ上書き保存）には置かない** | ⭐ **開いたファイルは、定義によりこの文書のファイルである。** ⛔ **そこで問うと、プロジェクト名を変えただけで毎回問われる** —— `FR-031` が減らそうとしているものを増やすことになる。⚠️ **裁定 `D6` の「同じファイルへ」は、選び直した相手のファイルのことである** |
| 3 | `null` は **「同じとみなさない」** とした（**問う**）| ⛔ **黙って上書きした結果は取り戻せず、余分な問いは 1 回の手間である。** ⭐ **鍵は緩める方向へは後からいつでも動かせるが、失ったファイルは戻らない**（規則 06 の `F` の時間の非対称性）。⚠️ **`FR-035` が `title` の `null` に `Untitled` を当てている先例は採らない** —— **代用の名を当てると、代用どうしが一致して「同じ」になってしまう** |
| 4 | 相手が `GRS JSON` として読めないときも **問う** | ⭐ **読めない相手は「誰のものか分からない相手」である。** ⛔ **読めないことを理由に黙って上書きするのは、いちばん危ない側に倒すことになる** |
| 5 | ⛔ **3 つ組を新しい表に置き、要求は表を指すだけにした** | ⭐ **本プロジェクトの書き方である** —— 要求は表を指し、規則を 1 行ずつ並べない。⚠️ **`tables` が 126 → 127 に増える**（§3 に予測を書いた）|
| 6 | ⛔ **`NT-7` を 1 文字も変えなかった** | ⭐ **`FR-031` ↔ `NT-7` は閉路である**（§1）ので**同じ計画で見た。** **`NT-7` は「問うてよいのは、要求が確認を求めると定めた場面だけ」と書いており、階級に替えた `FR-031` を指したままで正しい。** ⛔ **`NT-7` の「名前を挙げるのは消えるもの・解かれるものがあるとき」という条件付きも、そのままでよい** —— **上書きのときは挙げる名前が無く、`CR-193` がその空を許す形に作ってある** |
| 7 | ⛔ **`Project.id` / `Project.name` の `nullable` を変えなかった** | ⛔ **交換相手のスキーマがどちらも省略できる**（`AT-1` の注記）。**必須にすると、実在する MSPDI を拒むことになる**（規則 06 の `F`）|

⚠️ **決めていないこと**: ⛔ **自動保存の `documentKey` である。** ⭐ **ファイル名を含む 3 つ組は、
ファイルへ 1 度も保存していない文書に使えない。** `src/adapter/autosave-gateway/document-store.ts:36-40` と
`src/use-case/choose-startup-document/choose-startup-document.ts:160-173` の `STOP` は**開いたまま残る。**
⚠️ **本 CR がそれを黙って閉じたと読まれないよう、§6 に明記した。**

---

## 1. 測った（⛔ 2 本とも走らせた）

```
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py FR-031 FR-096
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-031 FR-060 FR-061 FR-062 FR-065 \
    FR-096 FR-024 FR-087 FR-032 FR-099 FR-095 NT-7 T-037 T-024a OP-4 T-034 BT-3 AT-1 AT-2 NFR-012 T-039 FU-3 S-99b
```

| | 実測 |
|---|---|
| 誘導部分グラフ（種 23 個）| 種 **23/23 が解決**。辺 19・**閉路 1 個（大きさ 2: `FR-031` ↔ `NT-7`）** |
| ⛔ その閉路を触るか | ⭐ **触る。** `FR-031` の例外の文を書き替える。**だから 1 つの計画で 1 度に見た** —— **`NT-7` は変えないと決めた**（§0 ⑧ の 6）|
| `FR-031` を指している箇所 | 要求 3 件（`FR-008` / `FR-013` / `FR-076`）／ 参照 16 箇所 |
| `FR-031` が指している先 | 表 10 個・行 18 個。⭐ **例外の文が指すのは `FR-032` と `FR-099` の 2 つだけである** |
| `FR-096` を指している箇所 | 要求 1 件／参照 5 箇所 |
| 結論 | ⭐ **閉路は 1 つ。その 2 つを 1 度に書き、残りは順に書いてよい** |

⚠️ **`induced.py` は種が 1 つも解決しないと拒んで止まる。** 本件は 23 個とも解決しているので、
**閉路 1 個は本物である。**

---

## 2. 何が壊れていたか（**逐語**）

### #1 —— 「同じ文書」が 3 か所で判定を求め、規則がどこにも無い

`docs/spec/01-04-requirements.md:3228`（`FR-061`）:

```
同じ機で別の文書や複製を開いても、自動保存が互いを取り違えてはならない（MUST NOT）。
```

`docs/spec/01-04-requirements.md:3262`（表 T-034 の締め）:

```
同じ文書で自動保存のほうが新しければ確認を求める。別の文書なら触らない。
```

⛔ **`src/` が独立に 2 か所で止まっている。**
`src/use-case/choose-startup-document/choose-startup-document.ts:160-173`:

```
⛔ STOP -- `documentKey` is NOT a value the specification decides. … `Project.id` is not it --
AT-1 of the ERD is nullable and marked 「主キーにしない」 -- and S-99b names a
「文書の識別子」 for the Agent API's records without defining one either.
```

### #2 —— 「確認を増やすな」の数が、今日すでに合っていない

`docs/spec/01-04-requirements.md:1560`（`FR-031` の RATIONALE）:

```
例外は FR-032 と FR-099 が確認を求めると定めた削除だけである …… 例外を増やさない（MUST NOT）。
```

⛔ **ところが、削除ではない確認が 2 つ、今日すでに動いている:**

`docs/spec/01-04-requirements.md:3166`（表 T-024a の `OP-4`）:

```
| OP-4 | 未保存の編集 | 置き換えを選んだときは、捨てる前に確認を求めること（MUST）。黙って捨ててはならない（MUST NOT）。
取り消しの履歴は引き継がない（LM-9 と同じ理由）。… |
```

`docs/spec/01-04-requirements.md:3423`（`FR-095`）:

```
捨てる前に、未保存の編集について表 T-024a の OP-4 と同じ確認を行うこと（MUST）。
```

⚠️ **同じ文は 1 度数え直されている** —— 変更履歴 `A-appendix.md:98`（`CR-193`）が `FR-099` の数え落としを記録している。
⭐ **列挙が 2 度外れたのだから、3 度目を数えるのではなく、列挙をやめる。**

### #3 —— ファイル名は、仕様書のどこにも無い

⛔ **列も、`U-` の行も、表 T-076 の書き出しの行も、打鍵も無い。** 実在するのは `src/` の継ぎ目だけである
（`src/adapter/file-gateway/file-store.ts:112` の `OpenedFileContent.fileName`、`:148` の
`ChosenFileWrite.suggestedFileName`）。⭐ **3 つ組の 1 つに据える以上、仕様書がその存在を認めねばならない。**

---

## 3. 何を書くか（**オブジェクトごとに 1 回**）

| # | オブジェクト | 何をするか |
|---|---|---|
| 1 | `docs/spec/01-04-requirements.md` `FR-096` | **表 T-227 を新設**し、`STATEMENT` の後に「同じファイルへ書き出すときは 表 T-227 に従うこと（MUST）」を置く |
| 2 | 同上・新しい **表 T-227 —— 文書の同一性と上書きの確認** | 行 `DI-1` 〜 `DI-5` |
| 3 | `docs/spec/01-04-requirements.md:1560`（`FR-031` の RATIONALE）| 例外の**列挙を階級に替える**。⛔ **`MUST NOT` は残す** |
| 4 | `docs/spec/01-04-requirements.md` `FR-061` | 「取り違えない」の判定が 表 T-227 の `DI-1` を使わないことを 1 文で書く（⛔ **ファイル名を持てないため**）|
| 5 | `docs/spec/A-appendix.md` | 変更履歴に 1 行 |

⛔ **`NT-7` を触らない**（§0 ⑧ の 6）。⛔ **`AT-1` / `AT-2` を触らない**（`nullable` のまま）。
⛔ **`_source/erd.json` も `_source/settings.json` も触らない** —— **本 CR は列を 1 本も足さない。**

### 数の予測

| | 前 | 後 |
|---|---|---|
| `tables` | 126 | **127** |
| `figures` | 11 | **11** |
| `rows` | 1563 | **1568**（`DI-1` 〜 `DI-5`）|
| `uids` | 145 | **145** |
| 重複検出 | `A=26 new 0` | **`A=26 new 0`** |

⚠️ **要求は 1 本も足さない。** ⭐ **表 1 つと 5 行だけである。**

---

## 4. 表 T-227 の中身（**書く前に置く**）

| 行 ID | 事項 | 規則 |
|---|---|---|
| `DI-1` | 何をもって同じ文書とするか | **ファイル名・`Project.name`・`Project.id` の 3 つがすべて一致するときに限り、同じ文書とすること（MUST）** |
| `DI-2` | `null` の扱い | **`Project.name` または `Project.id` が、どちらかの側で `null` のときは、同じとみなしてはならない（MUST NOT）** |
| `DI-3` | 相手が読めないとき | **書き出す先のファイルが既にあり、その中身を `GRS JSON` として読めないときは、同じとみなしてはならない（MUST NOT）** |
| `DI-4` | 上書きの確認 | **同じとみなせない相手へ書き出そうとするときは、上書きしてよいかを問うこと（MUST）。作法は 表 T-037 の `NT-7`** |
| `DI-5` | 開いたファイルへの上書き保存 | **`FR-060` の経路では問わないこと（MUST）** —— **開いたファイルはこの文書のファイルである** |

---

## 5. `src/` で何を書くか（**仕様書ではない側**）

| ユニット | ファイル | 何をするか |
|---|---|---|
| `UF-41` / `UF-42` | `adapter/file-gateway/` | 書き出す前に相手を読み、`DI-1` 〜 `DI-3` で判定する |
| `UF-67` | `adapter/screen-renderer/notices.ts` | `NT-7` の確認を組み立てる。⭐ **`items` は空でよい**（`CR-193` がそう作ってある）|

⚠️ **答えの返り道は、まだ仕様が持っていない** —— `notices.ts:192` の `STOP` は本 CR では閉じない。
⭐ **`display-words.json` の `confirmation` に `proceed` / `cancel` の鍵は既にある**（`CR-194`。語は空）。

---

## 6. ⛔ 本 CR が閉じないもの（**黙って閉じたと読まれないために**）

| | 件 | 状態 |
|---|---|---|
| 自動保存の `documentKey` | ⛔ **開いたまま。** ⭐ **3 つ組はファイル名を含むので、1 度もファイルへ保存していない文書に使えない。** `document-store.ts:36-40` と `choose-startup-document.ts:160-173` の `STOP` はそのまま残す |
| 確認の答えの返り道 | ⛔ **開いたまま**（`notices.ts:192`）。⚠️ 表 T-109 にどちらの選択肢の行も無い |
| 「はい / いいえ」の語 | ⛔ **開いたまま**（`PD-160`。`display-words.json` は鍵だけを持ち、語は空）|
| 複数プロジェクト（`FU-3` / `FU-4`）| ⚠️ **`DI-1` は「文書の `Project` は 1 つ」を前提にしている。** ⛔ **複数を入れる回には必ず読み直すこと** |
| `Project.id` を主キーにしない件（Chapter 5.4 `:426`）| ⭐ **矛盾しない。** あちらは**文書の中の行**を識別する話であり、`DI-1` は**文書どうし**を識別する話である |
