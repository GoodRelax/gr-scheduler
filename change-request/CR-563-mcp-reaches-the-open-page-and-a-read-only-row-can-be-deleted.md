# CR-563 — MCP は開いているページへ繋ぎ、読み取り専用の行は行ごと消せる

> 起草の状態: 起草（2026-09-24）。**決定のための覚え書き**であり、4 節の書き直しの塊はまだ無い。⛔ **`docs/spec` と `src/` は 1 字も触っていない。** 11 節の問い 3 つには、利用者が 2026-09-24 にすべて答えた（`JDG-503`: 問 1 は A、問 2 と問 3 は推奨どおり）。12 節の台帳の行は、本書を書いた者が同じ日に `rulings.md` ・ `defects.md` ・ `pending-decisions.md` へ写した。
> 読んだ木: `ebc71984`（`refactor`）。行番号・数・参照は、すべてこの木で測った（測り方は 13 節）。⚠️ **当てるのはリファクタ（`CR-551`〜`CR-554`、H2〜H10、段 8）の後である。当てる前に数え直すこと。**
>
> **閉じるもの**: 利用者の 2026-09-23 の指示（12 節の `JDG-500`。逐語は 0.1 節）。`CR-434` の 5 節の ①〜⑨（① と運搬は 11 節の問い、残りは 0 節の ③）。`CR-433` の 10 節の 13 項のうち、まだ開いているもの（5 節）。
> ⛔ **覆すもの**: なし。表 T-275 に `GP-7` を足し、`GP-1` の範囲を「行そのものの削除は含まない」と明かす。`CR-434` は本書の答えで書き直す（本書は `CR-434` を消さない）。
> ⭐ **形の方針**: 文書の唯一の正はページのまま（`GL-005` を動かさない）。MCP は開いているページの `Agent API` へ繋ぐ取次であり、ドメインを 1 行も知らない。行の読み取り専用は、人にも AI にも同じ表（表 T-275）で効く。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が本書の起草者へ渡した写しのまま写した。

| # | 逐語 |
|---|---|
| Q1 | 「MCP連携とタスクグループのアクセス権を実装しろ 読み取り専用のタスクグループは編集不可だが、タスクグループ自体の削除は可能とする。 理由: 自分の業務に関係のないタスクグループを切り離すことはよくあるため。」 |

既に書いてある裁定（`rulings.md`）: `JDG-303`（将来はサーバーと連携して複数人で編集する。行の所有権を設定したい）・`JDG-304`（最低限、行単位で読み取り専用）・`JDG-305`（継承しない）・`JDG-306`（他人の行には挿入も移動もできない。コピーだけ）・`JDG-309`（閲覧は自由。制限は GRSS が持つ）。⭐ **Q1 の「アクセス権」は、この 5 行が既に仕様へ落とした `FR-111` / 表 T-275 の実装を指す** —— 名簿も認証も持たない（`FR-111` の RATIONALE）。⭐ **Q1 が新しく足すのは 1 つだけ**: 行そのものの削除は読み取り専用でも受け付ける。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`**（構造化した日程データを出す）である。受ける目標は `GL-004`。MCP は `Agent API`（`FR-028`: 人間向け UI と同格）に標準の口金を付けるだけで、能力を足さない。行の削除の例外は、複数人の日程から自分の要る行だけを残す道であり、同じく `CH-1` の下にある。
⚠️ **`GL-005`（1 つのファイルだけで動く）とは MCP が正面からぶつかる** —— MCP の相手は待ち受けるプロセスであり、ブラウザのページは待ち受けられない（`CR-434` の 3 節）。⭐ 本書の推奨は「MCP を使う人だけが取次を足し、使わない人の `GL-005` は動かさない」という**回避**であって、衝突が無いという主張ではない。

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた結果 |
|---|---|
| `R1.3`（矛盾が無い） | ⛔ **`GP-1` と `CD-2` は、削除について何も言い合っていない** —— `GP-1` は「行に載るタスクの編集」を断り、`CD-2` は「行を消すと載るタスクも消える」と言う。行を消すことがタスクの編集に当たるなら、読み取り専用の行は消せない。当たらないなら消せる。**どちらとも読める**。⭐ `GP-7` がこの読みを 1 つに決める（2 節） |
| `R1.4`（異常系・権限） | ⭐ 消す連鎖が別の行へ届く場合を 3 つ数えた（2.2 節 の I-2 ・ I-3 ・ I-7）。どれも今は定めが無い |
| `R1.6`（否定要求は代わりの行動を示す） | `GP-1` の断りは、代わりに「行ごと消す」「コピーして自分の行へ」（`FR-033`）を示せる。⭐ `RS-61` の語にこの 2 つを足すかは当てる者が辞書で決める |
| `R2.9`（YAGNI） | ⭐ サーバー（GRSS）・名簿・「自分のグループ」・UID の貸し出しを持たない（10 節） |
| `R4`（並行性） | MCP の書き込みは今ある楽観ロック（`AG-2`）と刻印で足りる。人が確定していない間の拒否（`AG-9`）もそのまま効く |

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | ⭐ **表 T-275 の規則は、人と AI（`Agent API` / MCP）の書き込みに同じく効く。** AI の書き込みが `GP-1` に当たれば、拒否を値で返し、理由は `RS-61` とする | `FR-028` が `Agent API` を人間向け UI と同格と定めている。書き手で規則を変えると、同じ文書が書き手ごとに違って読める | 今の拒否の種類（`AgentRefusalReason`、`agent-api-members.ts:35`〜`:48`）に 1 つ足す |
| 決定 2 | ⭐ **MCP は `Agent API` が有効（`IC-20`、`FR-065`）の間だけ繋がる。** 繋がっている間は `IC-20` の押された見た目がそれを示す。新しいアイコンは足さない | `FR-065` は「人が有効にし、有効だと見える」を求める。MCP は `Agent API` の運び手にすぎない。`FR-029` は入口を増やさない | 繋がっている相手の名は画面に出ない |
| 決定 3 | ⭐ **MCP の道具は 表 T-107 の 18 メンバと 1 対 1、名も同じ**（`CR-434` の ⑦） | 束ねると表が 2 つになる（`R1.3`） | 1 つの作業に往復が増える |
| 決定 4 | ⭐ **`watchChanges`（`AG-6`）は待つ道具として運ぶ**（時間切れを持つ。自分の書き込みでは起きない —— 今の `AG-6` のまま）（`CR-434` の ③） | MCP の購読は相手によって扱いが揃わない。待つ道具ならどの相手でも同じに動く | 待つ間、相手の往復が 1 つ塞がる |
| 決定 5 | ⭐ **`readDocument` は今のまま文書を丸ごと返す**（`CR-434` の ④） | 絞る読み方は表 T-107 に無い能力であり、足せば `Agent API` の変更になる（`R2.9`） | 大きな文書では 1 回の答えが大きい |
| 決定 6 | ⭐ **合流の 3 択は人が答える。** AI の書き込みが合流の問いに当たったら、書き込みを保留して人へ上げる。AI に答えさせない（`CR-434` の ⑤） | 3 択は「どちらを残すか」の判断であり、`JDG-300` 以前から合流の問いは人の面（`OP-3`）が持つ | AI の作業はそこで止まる |
| 決定 7 | ⭐ **取次の守り**: `127.0.0.1` にだけ繋ぐ。WebSocket は取次が配ったページの origin だけを受ける。起動ごとの鍵をページの URL の断片に載せ、鍵の無い接続を断る（`CR-434` の ⑥） | 同じ機械の別のページから `Agent API` を叩かれない。断片（`#`）はサーバーへ送られない | 取次を起動し直すと、ページを開き直す |
| 決定 8 | ⭐ **`CR-434` の ⑧（UID の重なり）と ⑨（版の親）は、問 1 で「ページが持つ」を採るなら起きない。本書では持たない** | 文書に書くのはいつもページ 1 つであり、UID を振るのもページである（`JDG-307`: UID は 1 ファイルで一意）。2 つの文書を合わせるときの UID は、今ある合流（`import-document.ts:319`〜`:432`）が持つ | 問 1 でサーバーを採るなら、⑧ は最初に決め直す（`CR-434` の言う「最大の地雷」） |
| 決定 9 | ⭐ **`CR-433` の ②（検査 39: 逐語の試験が無い MUST が 11）は、案 (b) —— 仕様だけを読む別の体が試験を書く** | `CR-433` 自身の推奨。案 (a) は基準線を上げるので、利用者の許しが要る（`JDG-300` 以後の作法）。(b) は要らない | 試験の体が 1 つ要る |
| 決定 10 | ⭐ **`CR-433` の ⑪（名前の比べ方）は、いま持たない** | 単体の `GRS` が読むのは「`null` か否か」だけである（`GP-1`）。名前どうしを比べる場面（「自分のグループ」）は、サーバーと繋いだときに初めて生まれる（`GP-1` の注） | サーバーと繋ぐ変更要求で決める |
| 決定 11 | ⭐ **`CR-433` の ⑨（「閲覧は自由」を字どおり書くか）は書かない** | `JDG-309` は適用済で、`FR-111` の RATIONALE が「名簿・認証・閲覧の制限は本要求が持たない」と既に言う | — |
| 決定 12 | ⭐ **`CR-433` の ⑦（読み取り専用の行の見せ方）は、行の見出しに鍵の印を 1 つ置き、行を高くしない。** 寸法は触れる見本で決める | 行の縦幅は `CR-553` が詰めたばかりであり、見せ方で高くすれば戻る。数は触れる見本で決めるのがこの木の作法（`JDG-180`〜`JDG-193`） | 見本を 1 つ作る |
| 決定 13 | ⭐ **読み取り専用の行を「上と下の並びで動かす」「別の親の下へ動かす」「開く・閉じる」「ピン止め」は受け付ける** | `GP-2` により値は行ごとで継承しないので、行を動かしても誰の編集可否も変わらない。開閉とピン止めは見え方の値である。行ごと消すことを受け付けるなら、並べ替えを断る理由は無い | 他人の行の位置が、自分の手元で変わる |

---

## 1. 範囲 —— 項目ごとの行き先

| 項目 | 行き先 | 覆すときの戻し方 |
|---|---|---|
| 行そのものの削除 | 表 T-275 に `GP-7`（2.1 節）。`FR-032` の確認に 1 段（2.2 節の I-2 ・ I-3 を挙げる） | 行を消す |
| 個々のタスクの削除 | 問 3 の答えで `GP-1` に 1 文（推奨: 断る） | 文を消す |
| 行の属性 | 問 3 の答えで `GP-1` に 1 文（推奨: 名前と色は断る。`editGroup` は人だけが変えられる） | 文を消す |
| `editGroup` を入れる所（`CR-433` の ⑥） | 表 T-016（プロパティパネル）に行の項目を 1 つ | 項目を消す |
| AI の書き込み | 決定 1。表 T-035 に 1 行、`AgentRefusalReason` に 1 つ | 行を消す |
| MCP | `CR-434` を本書の答えで書き直し、`UC-012` に手順、表 T-035 に運搬の行、新しい要求 1 本（「`Agent API` を MCP として外へ出す」） | `CR-434` のとおり |

### 1.1 予約した識別子（取りまとめ役が 2026-09-24 に押さえた）

⚠️ **使うのは 11 節の答えが求めたときだけ。** 使わなければ返す。コミットのとき、実際に使ったものを取りまとめ役へ告げる。

| 識別子 | 何に使うか | 使う条件 |
|---|---|---|
| `GP-7` | 表 T-275: 行そのものの削除（2.1 節） | いつも（`JDG-500`） |
| `AG-12` | 表 T-035: MCP の運搬の行 | 問 1 で A（ページ） |
| `PR-33` | 表 T-016: 行の `editGroup` をプロパティパネルで入れる項目（`CR-433` の ⑥） | 問 3 の ④ で「人が入れ・消せる」 |
| `FR-150` | 新しい要求「`Agent API` を MCP として外へ出す」 | 問 1 で A |
| `IC-116` | 表 T-109: 読み取り専用の行の鍵の印（決定 12） | 触れる見本が「アイコンの行が要る」と決めたとき |

⭐ 周りの番号は他の変更要求が持つ: `PR-22`（`CR-551`）、`PR-23`〜`PR-32` ・ `FR-137`〜`FR-149` ・ `IC-109`〜`IC-114`（使い勝手の変更要求）、`FR-130`〜`FR-136` ・ `IC-107` ・ `IC-108`（`CR-561`）、`IC-115`（`CR-562`）。

## 2. 新しい行 `GP-7` と、他の行との噛み合い

### 2.1 `GP-7` の案（表 T-275 の末尾に足す）

```text
| GP-7 | ⭐ **行（`TaskGroup`）そのものの削除は、`editGroup` が `null` でなくても受け付けること（MUST）。<br>**
一緒に消えるものは 表 T-050 の `CD-2` に従うこと（MUST）—— 配下の行、載るタスクと各タスクの `CD-1` の連鎖、行を指す注記とピン止め。<br>
⛔ **`GP-1` の断りを当ててはならない（MUST NOT）** —— 自分の業務に関係の無い行を切り離すのが、この道の用途である。<br>
⚠️ **行に載るタスクを 1 つずつ消すことは、`GP-1` の編集として断る** —— 切り離しの単位は行である。<br>
⛔ **`Agent API` からの行の削除は、`editGroup` が `null` でない行では断ること（MUST）** —— AI の書き込みには `FR-032` の確認の面が無く、人が一覧を読まないまま他人の行が消える。断る理由は `RS-61` とする |
```

### 2.2 噛み合い（`ebc71984` で読んだ）

| # | 場面 | 今の定め | `GP-7` を足した後 |
|---|---|---|---|
| I-1 | 消す行の子の行が、別の `editGroup` を名乗る | `CD-2` は子孫の行を深さを問わず消す。`GP-2` は継承しない | ⭐ 子の行も消える。行ごとの削除は誰の行でも受け付けるので、`GP-7` と揃う。確認（`FR-032`）が子の行を名前で挙げる |
| I-2 | 消す行に載るタスクの **WBS の子** が、別の行に載っている（軸 A と軸 B は別。`Task.wbs_parent` と `TaskGroup`） | `CD-1` は WBS の子孫を消す。載る行は問わない | ⛔ **定めが要る**（問 3 の ②）。推奨: 消し、確認がその行とタスクを挙げる。⚠️ 逆向き —— 自分の行のタスクを消すと、別の読み取り専用の行に載る WBS の子が消える —— も同じ扱いにする |
| I-3 | 消す行のタスクを **先行** とする依存が、別の行のタスクに記録されている | `GP-5`: 確認で挙げる。止めない | ⭐ そのまま効く。`GP-5` は `FR-032` の確認全般に掛かるので、行ごとの削除にも掛かる |
| I-4 | 消した結果、別の読み取り専用の行の WBS の親の巻き上げ（期間・進捗）が変わる | `GP-6`: 派生値の作り直しは編集ではない | ⭐ そのまま効く |
| I-5 | ヘッダーの `[x]` で行をすべて消す（`JDG-380`、`CD-6`、`IC-106`。`CR-551` が `7dbd292d` で仕様へ着けた） | `CD-6` は「深さ `L1` のすべての行に `CD-2` を当てた和」と定める（`01-04-requirements.md:2302`、`7dbd292d`）。読み取り専用の行を含むかは言わない | ⭐ 含む。`GP-7` の帰結 |
| I-6 | 行の削除を取り消す（`FR-032` の ②） | 取り消しで戻せる | ⭐ 戻す。戻すことは編集ではない |
| I-7 | AI（`Agent API` / MCP）が読み取り専用の行を消す | 定め無し | ⛔ **定めが要る**（問 3 の ③）。⚠️ AI の書き込みには `FR-032` の確認が無い —— 人が一覧を読む機会が無いまま、他人の行が消える |
| I-8 | 行を切り取る（コピーして消す） | 定め無し | ⭐ `GP-7` に従う（消すことを受け付ける） |
| I-9 | 読み取り専用の行からタスクバーを動かし出す | `HM-11`: 別の `editGroup` の行へは受け付けない。`GP-1`: 読み取り専用の行のタスクは編集できない | 変わらない。持ち出す道はコピー（`FR-033`）だけ |

## 3. `CR-434` の 5 節 ①〜⑨ の行き先

| 項 | 行き先 |
|---|---|
| ① 文書の唯一の正の置き場 | ⛔ **11 節の問 1** |
| 運搬（3 節の A / B / C） | ⛔ **11 節の問 2** |
| ② 有効にする所 | 決定 2 |
| ③ `watchChanges` | 決定 4 |
| ④ `readDocument` の大きさ | 決定 5 |
| ⑤ 合流の 3 択 | 決定 6 |
| ⑥ 接続の守り | 決定 7 |
| ⑦ 道具の数 | 決定 3 |
| ⑧ UID の重なり | 決定 8（問 1 でページを採るなら起きない） |
| ⑨ 版の親 | 決定 8（同上） |

## 4. 書き直す所

⭐ 11 節の 3 問は 2026-09-24 に答えが出た。⛔ **塊は 4.1 節以降に書く。** 書くのは、`GP-7` と `GP-1` の 1 文、`FR-032` の 1 段、表 T-016 の項目、表 T-035 の行、MCP の要求を塊にする。`CR-434` の本文は同じとき書き直す。

## 5. `CR-433` の 10 節の 13 項（`ebc71984` で読んだ）

| 項 | 中身 | 今 |
|---|---|---|
| ① | 検査 23 の免除を 129→130・142→143 | ✅ 閉じた（`JDG-300` の Q1、`JDG-316`） |
| ② | 検査 39: 逐語の試験が無い MUST 11 | 決定 9（案 b） |
| ③ | 複数人の編集に `GL-` を足すか | ✅ 閉じた（`JDG-300` の Q28: 足さない） |
| ④ | リテラルの `editGroup: null` と補い | ✅ 着いた（2026-09-21） |
| ⑤ | `GP-1` ・ `GP-4` ・ `GP-5` が `RS-61` に届くか | 実装の波で確かめる（今は `editGroup` を読むコードが 0 —— 13 節の測り方 2） |
| ⑥ | 値を入れる所が無い | 1 節（表 T-016 に項目）。人だけが変えられるか否かは問 3 の ④ |
| ⑦ | 読み取り専用の行の見せ方 | 決定 12 |
| ⑧ | `rulings.md` ・ `changelog.md` へ写す | ✅ 済んだ（`CR-549`） |
| ⑨ | 「閲覧は自由」を書くか | 決定 11 |
| ⑩ | 「自分のグループ」の置き場 | 持たない（サーバーと繋ぐときに決める。`GP-1` の注） |
| ⑪ | 名前の比べ方 | 決定 10 |
| ⑫ | `RS-61` の指紋の留め（`tests/contract/t-233-...test.ts:493`） | `DFC-678` が持つ。語を場面と照らして人が読む |
| ⑬ | 検査 60: `editTaskGroup` ・ `editTask` が 1 行ずつ | ✅ 許された（`JDG-310`） |

⚠️ **赤い門 4 つ**（`CR-433` の頭）のうち、① ③ ⑫ は上のとおり閉じたか持ち主が居る。残るのは ② だけであり、決定 9 で基準線を動かさずに閉じる。

## 6. グラフ（`ebc71984` で測った）

| 行 | 指している要求 / 参照 |
|---|---|
| `GP-1` | 1 / 1 |
| `GP-5` | 0 / 0 |
| `CD-1` | 4 / 5 |
| `CD-2` | 3 / 8 |
| `HM-11` | 2 / 2（`FR-111` ・ `FR-033`） |

## 7. 数の予測

表 T-275: 6 → 7 行。表 T-233: 行は足さない（`RS-61` を使う）。表 T-107: 変えない（18 のまま）。

## 8. 波

リファクタの後。① 行の権限（表 T-275 の実装: `GP-1`〜`GP-7`、`HM-11`、`FR-033` の例外、表 T-016 の項目、鍵の印）—— 今は `editGroup` を読むコードが 0 なので、ここが本体である。② MCP の取次（問 2 の答えの形）。③ 仕様だけを読む試験の体（決定 9 を含む）。① と ② は持ち場が重ならない。

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

`DFC-560`（書き手の名が `'agent'` に固定）は MCP の書き手の名とかち合う。`DFC-787`（起動時に `Agent API` の有効化を告げる所が無い）は MCP でも同じである。

## 10. ⛔ この変更でやらないこと

- サーバー（GRSS）・名簿・認証・閲覧の制限（`JDG-309`、`FR-111` の RATIONALE）。
- 「自分のグループ」（`GP-1` の注の「いま持ってはならない」）。
- 名前の比べ方（決定 10）。
- 画面を持たない運搬（`CR-434` の C 案）—— `FR-065` ・ `FR-066` ・ `AG-9` ・ `MG-9` が成り立たない。

## 11. 前に立つ者へ返す問い

| 問い | 案 | 推奨 |
|---|---|---|
| 問 1: 文書の唯一の正をどこに置くか（`CR-434` の ①）（✅ A —— `JDG-503`） | **A. ページ（今のまま）** —— AI は開いているページの `Agent API` を MCP で叩く ／ B. GRSS（サーバー上の JSON） | ⭐ **A** —— `GL-005` を動かさず、`JDG-303` の「将来」を今に引き寄せない。B は合流が主題になり、UID の重なり（⑧）と版の親（⑨）を先に決めることになる。⚠️ 代償: AI が編集できるのは、利用者が `GRS` をブラウザで開き、`IC-20` を押している間だけである |
| 問 2: 運び手をどう配るか（A を採るとき）（✅ 推奨どおり —— `JDG-503`） | **A1. 取次を 1 つのファイルで配る**（AI のアプリが MCP の相手を 1 ファイルで入れる形。実行の土台はアプリが持つ）／ A2. `node` で動く取次（`node` を入れた人だけ）／ A3. 既に配っているページから `ws://127.0.0.1` へ繋ぐ（`CR-434` の B。混在コンテンツと `file://` の origin が未実測） | ⭐ **A1 を本筋、A2 を同じ中身の別の配り方** —— 利用者はファイルを落とすだけである（利用者の裁定 2026-09-16）。取次は `GRS` のページを `127.0.0.1` で配り、同じ origin で繋ぐ（`CR-434` の A）。⚠️ 代償: MCP を使うときは、手元のファイルではなく取次が配るページで `GRS` を開く |
| 問 3: 読み取り専用の行の細部（✅ 推奨どおり —— `JDG-503`） | ① 個々のタスクの削除 ② 消す連鎖が別の行のタスクに届くとき（2.2 節の I-2）③ AI が読み取り専用の行を消すこと（I-7）④ 行の名前・色・`editGroup` を変えること | ⭐ ① **断る**（切り離しの単位は行）② **消し、確認がその行とタスクを挙げる**（`GP-5` と同じ「挙げて止めない」）③ **断る** —— AI には確認の面が無い。行を消すのは人の操作とする ④ 名前と色は **断る**。`editGroup` は **人だけが** プロパティパネルで入れ・消せる。AI は書けない —— 読み取り専用の行が、AI に触らせない柵にもなる |

## 12. 台帳（2026-09-24 に本書を書いた者が写した）

- 裁定: `rulings.md` の「2026-09-23 —— MCP 連携・行の削除・画像から GRS JSON（CR-562 / CR-563）」の節の `JDG-500`。
- 答え: `rulings.md` の `JDG-503`（2026-09-24）。
- 裁定待ち（2026-09-24 に `裁定済`）: `pending-decisions.md` の `PND-602`（問 1）・`PND-603`（問 2）・`PND-604`（問 3）。⚠️ `PND-601` は使っていない（`CR-562` の問 1 を分類 C で置くと検査 25 が暫定の印を求めるので、行を置かなかった）。

## 13. 測り方の再現

```bash
# 1. the rows and their reach
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py GP-1 GP-5 CD-1 CD-2 HM-11
# 2. no code reads editGroup to refuse a write yet (10 hits in src/*.ts, all schema / codec / literals; 0 left after the filter)
git grep -n 'editGroup' -- 'src/*.ts' | grep -v -e json-codec -e mspdi-codec -e schedule.ts -e document-change-plan -e task-create -e task-group-naming
# 3. the Agent API reaches the page only as a window global
git grep -n 'grSchedulerAgentApi' -- src
# 4. the refusal reasons an agent write can carry today
sed -n 35,48p src/adapter/agent-api-endpoint/agent-api-members.ts
```
