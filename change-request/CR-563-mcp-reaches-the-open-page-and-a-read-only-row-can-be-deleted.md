# CR-563 — MCP は開いているページへ繋ぎ、読み取り専用の行は行ごと消せる

> 起草の状態: 起草（2026-09-24）。11 節の問い 3 つには、利用者が 2026-09-24 にすべて答えた（`JDG-503`: 問 1 は A、問 2 と問 3 は推奨どおり）。 問 4（辞書の語）は 4 節を書いた後に足し、2026-09-24 に「編集権限」と答えが出た（`JDG-508`）。⭐ **4 節に書き直しの塊を書いた**（E-01 〜 E-12、J-01 〜 J-03）。⛔ **`docs/spec` と `src/` は 1 字も触っていない。** 12 節の台帳の行は、本書を書いた者が同じ日に `rulings.md` ・ `defects.md` ・ `pending-decisions.md` へ写した。
> 読んだ木: `45ba1707`（`claude/goofy-borg-332fd7`。起草の初めは `ebc71984`）。4 節の旧の塊・6 節・7 節の数・行番号は、すべて `45ba1707` で測り直した（測り方は 13 節）。⚠️ **当てるのはリファクタ（`CR-551`〜`CR-554`、H2〜H10、段 8）の後である。当てる前に、旧の塊が 1 回ずつ現れることを数え直すこと。**
> ⭐ **足りなかった予約 2 つは取れた**（1.1 節）: 行の `editGroup` を書く命令 `CM-76`（表 T-108）と、AI が `editGroup` を書こうとしたときの理由 `RS-62`（表 T-233）。どちらも取りまとめ役が 2026-09-24 に予約した。`IC-116` は使わないので返した。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示（12 節の `JDG-500`。逐語は 0.1 節）。`CR-434` の 5 節の ①〜⑨（① と運搬は 11 節の問い、残りは 0 節の ③）。`CR-433` の 10 節の 13 項のうち、まだ開いているもの（5 節）。
> ⛔ **覆すもの**: なし。表 T-275 に `GP-7` を足し、`GP-1` の範囲を「行そのものの削除は含まない」と明かす。`CR-434` は本書の答えで書き直す（本書は `CR-434` を消さない）。
> ⭐ **形の方針**: 文書の唯一の正はページのまま（`GL-005` を動かさない）。MCP は開いているページの `Agent API` へ繋ぐ取次であり、ドメインを 1 行も知らない。行の読み取り専用は、人にも AI にも同じ表（表 T-275）で効く。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が本書の起草者へ渡した写しのまま写した。

| # | 逐語 |
|---|---|
| Q1 | 「MCP連携とタスクグループのアクセス権を実装しろ 読み取り専用のタスクグループは編集不可だが、タスクグループ自体の削除は可能とする。 理由: 自分の業務に関係のないタスクグループを切り離すことはよくあるため。」 |

既に書いてある裁定（`rulings.md`）: `JDG-303`（将来はサーバーと連携して複数人で編集する。行の所有権を設定したい）・`JDG-304`（最低限、行単位で読み取り専用）・`JDG-305`（継承しない）・`JDG-306`（他人の行には挿入も移動もできない。コピーだけ）・`JDG-309`（閲覧は自由。制限は GRSS が持つ）。⭐ **Q1 の「アクセス権」は、この 5 行が既に仕様へ落とした `FR-111` / 表 T-275 の実装を指す** —— 名簿も認証も持たない（`FR-111` の RATIONALE）。⭐ **Q1 が新しく足すのは 1 つだけ**: 行そのものの削除は読み取り専用でも受け付ける。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`**（構造化した日程データを出す）である。受ける目標は `GL-004`。MCP は `Agent API`（`FR-028`: 人間向け UI と同格）に標準の口金を付けるだけで、能力を足さない。行の削除の例外は、複数人の日程から自分の要る行だけを残す道であり、同じく `CH-1` の下にある。
⚠️ **`GL-005`（1 つのファイルだけで動く）とは MCP が正面からぶつかる** —— MCP の相手は待ち受けるプロセスであり、ブラウザのページは待ち受けられない（`CR-434` の 3 節）。⭐ 本書は「MCP を使う人だけが取次を足し、使わない人の `GL-005` は動かさない」という**回避**であって、衝突が無いという主張ではない。`FR-150` の RATIONALE（E-08）がそう書く。

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた結果 |
|---|---|
| `R1.3`（矛盾が無い） | ⛔ **`GP-1` と `CD-2` は、削除について何も言い合っていない** —— `GP-1` は「行に載るタスクの編集」を断り、`CD-2` は「行を消すと載るタスクも消える」と言う。行を消すことがタスクの編集に当たるなら、読み取り専用の行は消せない。当たらないなら消せる。**どちらとも読める**。⭐ `GP-7` がこの読みを 1 つに決める（E-02）。⭐ 重ねて書かないために、次の 3 つは新しく書かず、既にある行を指した: 同じ検証と同じ制限（表 T-035 の `AG-5`）、合流の 3 択を人が答えること（`FR-022` の結び、`01-04-requirements.md:5175`〜`:5177`）、別の行に見えるタスクに印を添えること（`FR-032`、`:2327`） |
| `R1.4`（異常系・権限） | ⭐ 消す連鎖が別の行へ届く場合を 3 つ数えた（2.2 節の I-2 ・ I-3 ・ I-7）。I-3 は `GP-5` が既に持つ。I-2 は、行を消すときの印だけが既にある（`:2327`）。載っている行の名前と、`Task` を消すときの扱いは無かった → E-03 ・ E-05。I-7 は定めが無かった → E-02。⭐ 取次の異常系として、ページが繋がっていない・2 つ目のページが繋ぐ・待つ道具が時間切れになる、の 3 つを数えた → `AG-12` の ② ③ ⑤（E-07） |
| `R1.6`（否定要求は代わりの行動を示す） | `GP-1` の断りには、代わりに「行ごと消す」（`GP-7`）と「コピーして自分の行へ」（`FR-033`、`RS-61` の次の一手）がある。AI が行を消せないことには「人に頼む」を、AI が `editGroup` を書けないことには「人がプロパティパネルで入れる」（`RS-62` の次の一手、J-03）を示した |
| `R2.9`（YAGNI） | ⭐ サーバー（GRSS）・名簿・「自分のグループ」・UID の貸し出しを持たない（10 節） |
| `R4`（並行性） | MCP の書き込みは今ある楽観ロック（`AG-2`）と刻印で足りる。人が確定していない間の拒否（`AG-9`）もそのまま効く |

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | ⭐ **表 T-275 の規則は、人と AI（`Agent API` / MCP）の書き込みに同じく効く。** AI の書き込みが `GP-1` に当たれば、拒否を値で返し、理由は `RS-61` とする（E-01） | `FR-028` が `Agent API` を人間向け UI と同格と定め、表 T-035 の `AG-5` が「UI と同じ検証・同じ制限」を既に求めている。書き手で規則を変えると、同じ文書が書き手ごとに違って読める | 今の拒否の種類（`AgentRefusalReason`、`agent-api-members.ts:35`〜`:48`）に 1 つ足す |
| 決定 2 | ⭐ **MCP は `Agent API` が有効（`IC-20`、`FR-065`）の間だけ繋がる。** 繋がっている間は `IC-20` の押された見た目がそれを示す。新しいアイコンは足さない（`AG-12` の ②） | `FR-065` は「人が有効にし、有効だと見える」を求める。MCP は `Agent API` の運び手にすぎない。`FR-029` は入口を増やさない | 繋がっている相手の名は画面に出ない |
| 決定 3 | ⭐ **MCP の道具は 表 T-107 の 18 メンバと 1 対 1、名も同じ**（`CR-434` の ⑦。`AG-12` の ④） | 束ねると表が 2 つになる（`R1.3`） | 1 つの作業に往復が増える |
| 決定 4 | ⭐ **`watchChanges`（`AG-6`）は待つ道具として運ぶ**（時間切れを持つ。自分の書き込みでは起きない —— 今の `AG-6` のまま）（`CR-434` の ③。`AG-12` の ⑤） | MCP の購読は相手によって扱いが揃わない。待つ道具ならどの相手でも同じに動く | 待つ間、相手の往復が 1 つ塞がる |
| 決定 5 | ⭐ **`readDocument` は今のまま文書を丸ごと返す**（`CR-434` の ④） | 絞る読み方は表 T-107 に無い能力であり、足せば `Agent API` の変更になる（`R2.9`） | 大きな文書では 1 回の答えが大きい |
| 決定 6 | ⭐ **合流の 3 択は人が答える。** AI の書き込みが合流の問いに当たったら、人へ上げる。AI に答えさせない（`CR-434` の ⑤。`AG-12` の ⑥） | ⚠️ **仕様は既にこう定めている** —— `FR-022` の結び（`01-04-requirements.md:5175`〜`:5177`）が「呼ぶ側が機械であっても、選ぶのは人」「`AM-8` が合流にあたるときは `U-61` を立て、人が答えるまで待つ」と言う。`AG-12` はそれを指すだけで、書き写さない | AI の作業はそこで止まる |
| 決定 7 | ⭐ **取次の守り**: `127.0.0.1` にだけ繋ぐ。WebSocket は取次が配ったページの origin だけを受ける。起動ごとの鍵をページの URL の断片に載せ、鍵の無い接続を断る（`CR-434` の ⑥。`AG-12` の ⑦） | 同じ機械の別のページから `Agent API` を叩かれない。断片（`#`）はサーバーへ送られない | 取次を起動し直すと、ページを開き直す |
| 決定 8 | ⭐ **`CR-434` の ⑧（UID の重なり）と ⑨（版の親）は、問 1 で「ページが持つ」を採ったので起きない。本書では持たない** | 文書に書くのはいつもページ 1 つであり、UID を振るのもページである（`JDG-307`: UID は 1 ファイルで一意）。2 つの文書を合わせるときの UID は、今ある合流（`import-document.ts:319`〜`:432`）が持つ | サーバーを採る日が来たら、⑧ は最初に決め直す（`CR-434` の言う「最大の地雷」） |
| 決定 9 | ⭐ **`CR-433` の ②（検査 39: 逐語の試験が無い MUST が 11）は、案 (b) —— 仕様だけを読む別の体が試験を書く** | `CR-433` 自身の推奨。案 (a) は基準線を上げるので、利用者の許しが要る（`JDG-300` 以後の作法）。(b) は要らない | 試験の体が 1 つ要る。⚠️ 本書の塊も MUST を足す（8 節） |
| 決定 10 | ⭐ **`CR-433` の ⑪（名前の比べ方）は、いま持たない** | 単体の `GRS` が読むのは「`null` か否か」だけである（`GP-1`）。名前どうしを比べる場面（「自分のグループ」）は、サーバーと繋いだときに初めて生まれる（`GP-1` の注） | サーバーと繋ぐ変更要求で決める |
| 決定 11 | ⭐ **`CR-433` の ⑨（「閲覧は自由」を字どおり書くか）は書かない** | `JDG-309` は適用済で、`FR-111` の RATIONALE が「名簿・認証・閲覧の制限は本要求が持たない」と既に言う | — |
| 決定 12 | ⭐ **`CR-433` の ⑦（読み取り専用の行の見せ方）は、行の見出しに鍵の印を 1 つ置き、行を高くしない。** 寸法は触れる見本で決める。⛔ **本書の塊は鍵の印を書かない** —— `IC-116` は予約したまま使わない（1.1 節） | 行の縦幅は `CR-553` が詰めたばかりであり、見せ方で高くすれば戻る。数は触れる見本で決めるのがこの木の作法（`JDG-180`〜`JDG-193`）。見本より先に 表 T-109 の行を書くと、見本が決める前に形を決めてしまう | 見本を 1 つ作る。見本が決めるまで、読み取り専用の行は画面で見分けられない（断ったときの通知 `RS-61` だけが告げる） |
| 決定 13 | ⭐ **読み取り専用の行を「上と下の並びで動かす」「別の親の下へ動かす」「開く・閉じる」「ピン止め」は受け付ける**（E-01） | `GP-2` により値は行ごとで継承しないので、行を動かしても誰の編集可否も変わらない。どれも載るタスクの日程を変えない。行ごと消すことを受け付けるなら、並べ替えを断る理由は無い。⚠️ 起草の初めは「開閉とピン止めは見え方の値である」と書いたが、開閉（`AT-56` の `isCollapsed`）は `TaskGroup` の列であり、表 T-052 の `DR-2` で日程データの群に入る。導きは「日程を変えない」のほうで立つ | 他人の行の位置が、自分の手元で変わる |
| 決定 14 | ⭐ **決定 13 の並びに、隠す（`AT-57`、`CM-34`）・開いたままの印（`AT-142`、`CM-75`）・行の高さ（`AT-59`、`PR-20`、`CM-32`）を足す**（E-01）。断るのは、問 3 の ④ が名指した名前と色だけである | 問 3 の ④ は「名前と色は断る」と言い、高さ・隠し・開いたままの印には触れていない。どれも決定 13 と同じく、載るタスクの日程も名乗りも変えない。触れない値を黙らせると、`R1.4` の「どちらとも読める」が残る | 他人の行の高さが、自分の手元で変わる。⚠️ 前に立つ者が覆すなら、E-01 の ⚠️ の 1 文から 3 つを外し、断る側へ移す |
| 決定 15 | ⭐ **待つ道具（`watchChanges`）の待つ長さは、呼ぶ側が道具の引数で渡す。取次は既定の長さを持たない**（`AG-12` の ⑤） | 決定 4 は「時間切れを持つ」とだけ言う。長さを取次が持てば、設定の行（`S-`）が 1 つ要る。呼ぶ側（MCP の客）は自分の往復の時間切れを知っており、取次より正しく選べる | 引数を渡し忘れた呼び出しの扱いを、実装が決める（表 T-107 の前文により引数は `src/` の公開エントリが持つ） |
| 決定 16 | ⭐ **取次に同時に繋ぐページは 1 つとし、2 つ目を断る**（`AG-12` の ③） | 2 つのタブが繋ぐと、書き込みがどちらの文書へ行くかを取次が決めなければならない。取次はドメインを持たない（`AG-12` の結び） | タブを開き直すときは、古いタブを閉じてから開く |
| 決定 17 | ⭐ **問 3 の ③（AI は読み取り専用の行を消せない）を、連鎖にも当てる** —— `Agent API` の削除が、読み取り専用の行か、そこに載る `Task` に届くとき（配下の行、`CD-1` の WBS の子孫）は、束ごと断る（E-02） | 問 3 の ③ の理由は「AI には確認の面が無く、人が一覧を読まないまま他人の行が消える」である。自分の行を消した連鎖で他人の行が消えるときも、その理由がそのまま当たる | AI が自分の行を消せないことがある。そのときは人に頼む |

---

## 1. 範囲 —— 項目ごとの行き先

| 項目 | 行き先 | 編集 | 覆すときの戻し方 |
|---|---|---|---|
| 行そのものの削除 | 表 T-275 に `GP-7`。表 T-050 の `CD-6` に 1 文 | E-02 ・ E-04 | `GP-7` の行と `CD-6` の 1 文を消す |
| 消す連鎖が別の行に届くとき（問 3 の ②） | `FR-032` の 表 T-050 の直下の段（挙げ方）、表 T-234 の `QN-1` ・ `QN-2` | E-03 ・ E-05 | 段を旧に戻す |
| 個々のタスクの削除（問 3 の ①） | `GP-1` に 1 文（断る） | E-01 | 文を消す |
| 行の属性（問 3 の ④） | `GP-1` に 3 文（名前と色は断る。`editGroup` は人だけ。ほかは受け付ける —— 決定 13 ・ 14） | E-01 | 文を消す |
| `editGroup` を入れる所（`CR-433` の ⑥） | 表 T-016 に `PR-33`（原稿 `property-items.json` と辞書）。表 T-108 に命令 1 つ | J-01 ・ J-02 ・ E-09 | 項目と命令を消す |
| AI が `editGroup` を書こうとしたとき | 表 T-233 に理由 1 つ（辞書にも） | E-10 ・ J-03 | 行を消す |
| AI の書き込み（決定 1） | `GP-1` に 1 文。⚠️ 表 T-035 に行は足さない —— `AG-5` が既に持つ | E-01 | 文を消す |
| AI の行の削除（問 3 の ③、決定 17） | `GP-7` の中 | E-02 | 文を消す |
| MCP | 表 T-035 に `AG-12`、新しい要求 `FR-150`、`UC-012` に拡張 1b、要求の数（`05-07-design.md`） | E-06 ・ E-07 ・ E-08 ・ E-11 ・ E-12 | `CR-434` のとおり |

⭐ **本書の塊が書かないもの**: 読み取り専用の行の鍵の印（決定 12）、`05-07-design.md` の構成（取次とページ側の WebSocket の口を持つコンポーネント・ユニット —— 8 節の波 ② が設計の変更要求を先に書く）、`CR-434` の本文（本書を当てるときに書き直す）。

### 1.1 識別子

⚠️ **使うのは 11 節の答えが求めたときだけ。** 使わなければ返す。コミットのとき、実際に使ったものを取りまとめ役へ告げる。

| 識別子 | 何に使うか | 使う条件 | 本書で |
|---|---|---|---|
| `GP-7` | 表 T-275: 行そのものの削除 | いつも（`JDG-500`） | ✅ 使う（E-02） |
| `AG-12` | 表 T-035: MCP の運搬の行 | 問 1 で A（ページ） | ✅ 使う（E-07） |
| `PR-33` | 表 T-016: 行の `editGroup` をプロパティパネルで入れる項目（`CR-433` の ⑥） | 問 3 の ④ で「人が入れ・消せる」 | ✅ 使う（J-01 ・ J-02） |
| `FR-150` | 新しい要求「`Agent API` を MCP として外へ出す」 | 問 1 で A | ✅ 使う（E-08） |
| `IC-116` | 表 T-109: 読み取り専用の行の鍵の印（決定 12） | 触れる見本が「アイコンの行が要る」と決めたとき | ⛔ **使わない。2026-09-24 に取りまとめ役へ返した** —— 決定 12 のとおり、触れる見本が寸法と要否を決めてから、番号を取り直して別の塊で書く |

⭐ **塊を書いてから取った予約（取りまとめ役が 2026-09-24 に認めた）**:

| 識別子 | 何に使うか | なぜ要るか |
|---|---|---|
| `CM-76` | 表 T-108: 行の `editGroup` を入れる・消す命令 `setTaskGroupEditGroup`（E-09） | 表 T-108 の `TaskGroup` の命令（`CM-26`〜`CM-35` ・ `CM-72` 〜 `CM-75`）に `editGroup` を書くものが無い。プロパティパネルの確定は 表 T-108 の命令へ翻訳される（`05-07-design.md` の `UF-100`）ので、命令が無ければ `PR-33` は入れられない |
| `RS-62` | 表 T-233: AI が `editGroup` を書こうとしたときの理由（E-10 ・ J-03） | `RS-61` の語（「この行は読み取り専用です」）は、`editGroup` が `null` の行に AI が名乗りを書こうとした場面に当たらない。当たらない語を運ぶと、AI は何を直せば通るのかを読めない（`AG-9a` の「理由の区分を 1 つの値へ潰さない」） |

⚠️ 塊を書いた体は、`45ba1707` で表の中の最大の次（`CM-75` → `CM-76`、`RS-61` → `RS-62`）を仮に書いた。取りまとめ役が同じ番号を認めたので、塊は書き換えていない。
⭐ 周りの番号は他の変更要求が持つ: `PR-22`（`CR-551`）、`PR-23`〜`PR-32` ・ `FR-137`〜`FR-149` ・ `IC-109`〜`IC-114`（使い勝手の変更要求）、`FR-130`〜`FR-136` ・ `IC-107` ・ `IC-108`（`CR-561`）、`IC-115`（`CR-562`）。

## 2. 新しい行 `GP-7` と、他の行との噛み合い

### 2.1 `GP-7`

⭐ 仕上げた文は 4 節の E-02 にある。起草の初めの案から変えたのは 4 つ:

- ⛔ 案は 1 つの表の行を 5 つの物理行に分けて書いていた。表の行は 1 行でなければ表が切れる → 1 行にした。
- 案は `CD-2` が消すもの（配下の行、載るタスク、注記、ピン止め）を書き写していた（`R1.3`）→ `CD-2` を指すだけにした。
- 「タスクを 1 つずつ消すことは断る」は `GP-1` の範囲なので、`GP-1` へ移した（E-01）。
- 問 3 の ② の答え（連鎖が別の行に届いても消し、挙げる）と、決定 17（AI の削除は連鎖にも断る）を足した。

### 2.2 噛み合い（`45ba1707` で読んだ）

| # | 場面 | 今の定め | `GP-7` を足した後 |
|---|---|---|---|
| I-1 | 消す行の子の行が、別の `editGroup` を名乗る | `CD-2` は子孫の行を深さを問わず消す。`GP-2` は継承しない | ⭐ 子の行も消える。行ごとの削除は誰の行でも受け付けるので、`GP-7` と揃う。確認（`FR-032`）が子の行に載る `Task` を名前で挙げる（表 T-050 の直下の「子孫の行に載る `Task` もその数に入る」） |
| I-2 | 消す行に載るタスクの **WBS の子** が、別の行に載っている（軸 A と軸 B は別。`Task.wbs_parent` と `TaskGroup`） | `CD-1` は WBS の子孫を消す。載る行は問わない。⚠️ 行を消す確認は、別の行に表示されている `Task` に「その旨」を添える（`01-04-requirements.md:2327`、`HM-10`）。**載っている行の名前は添えない。`Task` を消す確認（`QN-2`）には、その旨も無い** | ⭐ 消す（問 3 の ②、`GP-7`）。確認はその `Task` と載っている行の名前を挙げる（E-03 ・ E-05）。逆向き —— 自分の行のタスクを消すと、別の読み取り専用の行に載る WBS の子が消える —— も同じ扱い |
| I-3 | 消す行のタスクを **先行** とする依存が、別の行のタスクに記録されている | `GP-5`: 確認で挙げる。止めない | ⭐ そのまま効く。`GP-5` は `FR-032` の確認全般に掛かるので、行ごとの削除にも掛かる |
| I-4 | 消した結果、別の読み取り専用の行の WBS の親の巻き上げ（期間・進捗）が変わる | `GP-6`: 派生値の作り直しは編集ではない | ⭐ そのまま効く |
| I-5 | ヘッダーの `[x]` で行をすべて消す（`JDG-380`、`CD-6`、`IC-106`。`CR-551` が `7dbd292d` で仕様へ着けた） | `CD-6` は「深さ `L1` のすべての行に `CD-2` を当てた和」と定める（`01-04-requirements.md:2302`、`45ba1707` でも同じ行）。読み取り専用の行を含むかは言わない | ⭐ 含む（E-04）。`GP-7` の帰結 |
| I-6 | 行の削除を取り消す（`FR-032` の ②） | 取り消しで戻せる | ⭐ 戻す。戻すことは編集ではない |
| I-7 | AI（`Agent API` / MCP）が読み取り専用の行を消す | 定め無し | ⛔ 断る（問 3 の ③、`GP-7`）。連鎖が届くときも断る（決定 17） |
| I-8 | 行を切り取る（コピーして消す） | 定め無し | ⭐ `GP-7` に従う（消すことを受け付ける） |
| I-9 | 読み取り専用の行からタスクバーを動かし出す | `HM-11`: 別の `editGroup` の行へは受け付けない。`GP-1`: 読み取り専用の行のタスクは編集できない | 変わらない。持ち出す道はコピー（`FR-033`）だけ |

## 3. `CR-434` の 5 節 ①〜⑨ の行き先

| 項 | 行き先 |
|---|---|
| ① 文書の唯一の正の置き場 | ✅ 11 節の問 1 → A（ページ）。`FR-150` の STATEMENT と `AG-12` の結び |
| 運搬（3 節の A / B / C） | ✅ 11 節の問 2 → A1 と A2。`AG-12` の ① |
| ② 有効にする所 | 決定 2（`AG-12` の ②） |
| ③ `watchChanges` | 決定 4 ・ 15（`AG-12` の ⑤） |
| ④ `readDocument` の大きさ | 決定 5（塊なし —— 今のまま） |
| ⑤ 合流の 3 択 | 決定 6（`AG-12` の ⑥。`FR-022` が既に持つ） |
| ⑥ 接続の守り | 決定 7（`AG-12` の ⑦） |
| ⑦ 道具の数 | 決定 3（`AG-12` の ④） |
| ⑧ UID の重なり | 決定 8（起きない） |
| ⑨ 版の親 | 決定 8（起きない） |

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-553` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（`45ba1707` で 15 塊すべてが 1 回であることを確かめた。13 節の測り方 5）。旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である。
⭐ `CM-76` と `RS-62` は予約で取れた番号である（取りまとめ役が 2026-09-24 に予約した）。E-01 ・ E-02 ・ E-09 ・ E-10 ・ J-01 ・ J-03 に現れる。
⚠️ 当てる 6 つのファイルは、`45ba1707` ではすべて LF である（CRLF は 0）。当てる木で数え直し、CRLF が混じっていたら `\r\n` を `\n` にしてから数え、書くときに多いほうへ戻すこと。
⚠️ 行末の半角空白 2 つ（`  `）は旧・新の一部である（E-03 ・ E-06 ・ E-08 ・ E-11 ・ E-12）。
⚠️ 同じ表の編集は上から順に当てること —— E-01 は `GP-1` の行の末尾、E-02 は `GP-6` の行の後ろ。E-03 と E-04 はどちらも `FR-032` の中である。
⚠️ J-01 〜 J-03 は JSON の原稿である。当てたら `json.loads` で読めることを確かめ、`npm run gen` で `_assets/tbl-property-items.md`（表 T-016）と辞書の写しを刷る（生成物を手で直さない）。
⚠️ **`CR-562` と重なる所**: `CR-562` が書くのは `FR-068`（`01-04-requirements.md` の 6212 行付近）・表 T-109 の `IC-115`・辞書の AI 出力の中身の切替の項・新しい原稿 `_source/image-to-grs-json-prompt.*.md` である。本書の旧は、そのどれとも同じ行を持たない（辞書で本書が触るのは `properties` 節の `PR-19` の前と、`reasons` 節の `RS-61` の後だけ）。⚠️ それでも、`CR-562` が着いた木で数え直すこと。

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
問 3 の ① と ④、決定 1 ・ 13 ・ 14。表 T-275 の `GP-1` の行の末尾に足す。旧
```text
⛔ **いま「自分のグループ」を仕様に持ってはならない（MUST NOT）** —— `GL-005` の単体で動く `GRS` には、名乗る相手が無い |
```
新
```text
⛔ **いま「自分のグループ」を仕様に持ってはならない（MUST NOT）** —— `GL-005` の単体で動く `GRS` には、名乗る相手が無い。<br>⭐ 行に載る `Task` を 1 つずつ消すこと（表 T-108 の `CM-7`）も、本行の編集として断ること（MUST） —— 切り離しの単位は行であり、行ごと消す道は `GP-7` が持つ。<br>⭐ 行そのものの値のうち、名前（`_assets/tbl-property-items.md` の 表 T-016 の `PR-18`）と色（同表の `PR-19`）の書き換えも断ること（MUST）。<br>⚠️ 並び・親・畳み・隠し・開いたままの印・高さ・ピン止めは受け付ける —— どれも載る `Task` の日程も名乗りも変えず、`GP-2` により、行を動かしても誰の可否も変わらない。<br>⭐ **`editGroup` そのもの（表 T-016 の `PR-33`）は、人がプロパティパネルで入れ・消せること（MUST）。<br>** `null` でない行でも入れ・消せる —— 消せなければ、名乗った行を人が戻す道が無い。<br>⛔ **`editGroup` を `Agent API` から書き換えさせてはならない（MUST NOT）** —— `null` の行でも同じである。<br>読み取り専用の行は、AI に触らせない柵を兼ねる。<br>断る理由は 表 T-233 の `RS-62` とすること（MUST）。<br>⭐ **本表の規則は、画面からの書き込みにも `Agent API`（`FR-028`、MCP で運ぶときも同じ —— `FR-150`）からの書き込みにも同じく効くこと（MUST）** —— 同じ検証と同じ制限を通ることは 表 T-035 の `AG-5` が求めている。<br>AI の書き込みが本行の断りに当たったときは、拒否を値で返し（`AG-9a`）、理由を 表 T-233 の `RS-61` とすること（MUST） |
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
`JDG-500`、問 3 の ② と ③、決定 17。表 T-275 の末尾に `GP-7` を足す（`GP-6` の行は変えない）。旧
```text
| GP-6 | 派生値の作り直しは編集ではない。<br>⛔ `GP-1` の対象としてはならない（MUST NOT）—— 対象にすると、読み取り専用の行が 1 つ在るだけで巻き上げが止まる |
```
新
```text
| GP-6 | 派生値の作り直しは編集ではない。<br>⛔ `GP-1` の対象としてはならない（MUST NOT）—— 対象にすると、読み取り専用の行が 1 つ在るだけで巻き上げが止まる |
| GP-7 | ⭐ **行（`TaskGroup`）そのものの削除は、`editGroup` が `null` でない行でも受け付けること（MUST）。<br>** ⛔ **`GP-1` の断りをこの削除に当ててはならない（MUST NOT）** —— 自分の業務に関係の無い行を切り離すのが、この道の用途である。<br>一緒に消えるものは 表 T-050 の `CD-2` に従う —— 配下の行が別の `editGroup` を名乗っていても消える（名乗りは行ごとである —— `GP-2`）。<br>⭐ 消す連鎖（同表の `CD-1` の WBS の子孫）が、`editGroup` が `null` でない別の行に載る `Task` に届くときも、消すこと（MUST） —— 画面から `Task` を消すときも同じである。<br>確認は、その `Task` と、載っている行の名前を挙げる —— 挙げ方は `FR-032` の 表 T-050 の直下の段が持つ。<br>⛔ 削除を止めてはならない（MUST NOT） —— 止めると、`GP-5` と同じく、別のグループの都合で自分の行が消せなくなる。<br>⛔ **`Agent API` からの削除は、消すものが `editGroup` が `null` でない行か、その行に載る `Task` に届くとき、受け付けてはならない（MUST NOT）** —— `Agent API` には `FR-032` の確認の面が無く、人が一覧を読まないまま他人の行が消える。<br>束ごと断り（表 T-035 の `AG-3`）、理由を 表 T-233 の `RS-61` とすること（MUST）。<br>⭐ 行を消すのは人の操作とする —— AI が切り離したいときは、人に頼む |
```

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md -->
問 3 の ②。`FR-032` の 表 T-050 の直下の段。別の行に表示されている `Task` に、載っている行の名前を添え、`Task` を消す確認にも広げる。⚠️ `GP-5`（依存の先行）とは別の場面であり、重ねない。旧
```text
消える `Task` のうち他の行に表示されているものには、その旨を示すこと（MUST） —— 画面上その行に見えていないためである。  
```
新
```text
消える `Task` のうち他の行に表示されているものには、その旨と、表示されている行の名前を示すこと（MUST） —— 画面上その行に見えていないためである。  
⭐ WBS の子孫を持つ `Task` を消す確認でも同じとすること（MUST） —— `CD-1` の WBS の子孫は、載る行を問わず消える。  
⚠️ その行が読み取り専用（表 T-275）でも消えることは、同表の `GP-7` が持つ —— 本段が持つのは挙げ方だけである。  
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
I-5。表 T-050 の `CD-6` の行の中。旧
```text
行を指す注記とピン止め。<br>⚠️ 行が 0 になるので、本表の後の段により
```
新
```text
行を指す注記とピン止め。<br>⭐ `editGroup` が `null` でない行も含めて消す —— 行そのものの削除は、読み取り専用の行でも受け付ける（表 T-275 の `GP-7`）。<br>⚠️ 行が 0 になるので、本表の後の段により
```

<!-- EDIT id=E-05 file=docs/spec/01-04-requirements.md -->
問 3 の ②。表 T-234 の `QN-1` と `QN-2`。E-03 の挙げ方に合わせる（行は足さない。辞書の語 `shownOnAnotherRow` は変えない —— 行の名前は文書の値であり、辞書に持たない）。旧
```text
| QN-1 | 行を削除する前 | 挙げる —— 消える `Task` の名前。<br>別の行に表示されているものにはその旨を添える | `FR-032` |
| QN-2 | WBS の子孫を持つ `Task` を削除する前 | 挙げる —— 一緒に消えるものは 表 T-050 の `CD-1` が持つ | `FR-032` |
```
新
```text
| QN-1 | 行を削除する前 | 挙げる —— 消える `Task` の名前。<br>別の行に表示されているものには、その旨と、表示されている行の名前を添える | `FR-032` |
| QN-2 | WBS の子孫を持つ `Task` を削除する前 | 挙げる —— 一緒に消えるものは 表 T-050 の `CD-1` が持つ。<br>別の行に表示されているものには、その旨と、表示されている行の名前を添える（`QN-1` と同じ） | `FR-032` |
```

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md -->
問 1 ・ 問 2。`UC-012` の拡張に 1b を足す（手順の番号は変えない）。旧
```text
- 1a. `Agent API` が有効化されていない。
  - `GRS` は `Agent API` を公開しない。  
    既定では公開しない。
- 3a. 申し込みが受け付けられない内容である。
```
新
```text
- 1a. `Agent API` が有効化されていない。
  - `GRS` は `Agent API` を公開しない。  
    既定では公開しない。
- 1b. AI エージェントが MCP の客である。
  - 人は、MCP の取次が配るページで `GRS` を開き、`Agent API` を有効にする。  
    AI エージェントは、`Agent API` の各メンバを、同じ名の MCP の道具として呼ぶ。
- 3a. 申し込みが受け付けられない内容である。
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md -->
問 1 ・ 問 2、決定 2 〜 4 ・ 6 ・ 7 ・ 15 ・ 16。表 T-035 の末尾（`AG-9` の行の後ろ）に `AG-12` を足す（`AG-9` の行は変えない）。旧
```text
人が編集入力を確定していない間（プロパティパネルで入力中など）も同じく拒否すること（MUST） |
```
新
```text
人が編集入力を確定していない間（プロパティパネルで入力中など）も同じく拒否すること（MUST） |
| AG-12 | ⭐ **`Agent API` を MCP の道具として運ぶ取次（`FR-150`）と、取次に繋ぐページは、次の ① 〜 ⑦ に従うこと（MUST）。<br>** ① 取次は `GRS` のページを `127.0.0.1` で配り、配ったページの `Agent API` へ、同じ origin の WebSocket で繋ぐこと（MUST）。<br>取次は、AI のアプリが MCP の相手として 1 つのファイルで入れる形で配り、同じ中身をコマンド行の JavaScript の実行環境で動かす形でも配ること（MUST） —— 実行の土台は、前者では AI のアプリが持ち、後者では入れた人が持つ。<br>② 繋ぐのは、ページで `Agent API` が有効（`FR-065`、表 T-109 の `IC-20`）なあいだだけとすること（MUST）。<br>無効のあいだと、ページが繋がっていないあいだは、道具の呼び出しに拒否を値で返すこと（MUST） —— 形は `AG-9a` に従う。<br>繋がっていることは `IC-20` の押された見た目が示す —— ⛔ 入口を足してはならない（MUST NOT）（`FR-029`）。<br>③ 同時に繋ぐページは 1 つとし、2 つ目の接続を断ること（MUST） —— 取次は、2 つの文書のどちらへ書くかを決められない。<br>④ 道具は 表 T-107 のメンバと 1 対 1 とし、名を同じ確定名とすること（MUST）。<br>⛔ 束ねてはならない（MUST NOT） —— 束ねると、呼べることの表が 2 つになる。<br>⑤ `watchChanges`（`AM-17`）は、変更か発話が届くまで待つ道具として運ぶこと（MUST）。<br>待つ長さは呼ぶ側が道具の引数で渡し、切れたときは「まだ無い」を値で返すこと（MUST）。<br>起きる条件は本表の `AG-6` のまま —— 自分の書き込みでは起きない。<br>⑥ 合流の 3 択は人が答える —— 規則は `FR-022` が持ち、道具は人が `U-61` で答えるまで待つ。<br>⑦ WebSocket は取次が配ったページの origin だけを受け、取次が起動ごとに作る鍵をページの URL の断片（`#` の後）に載せ、鍵の無い接続と鍵の違う接続を断ること（MUST） —— 同じ機械の別のページから `Agent API` を叩かせないためである。<br>断片はサーバーへ送られない。<br>⛔ **取次は文書もドメインの規則も持ってはならない（MUST NOT）** —— 検証・拒否・刻印の照合は、繋いだ先のページの `Agent API` が本表のとおり行い（`AG-5`）、取次は呼び出しと答えを運ぶだけである |
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md -->
問 1 ・ 問 2。新しい要求 `FR-150` を `FR-065` の後ろ、`FR-066` の前に置く（`Agent API` の要求 `FR-028` ・ `FR-063` ・ `FR-064` ・ `FR-065` ・ `FR-066` の並び）。規則は `AG-12` が持ち、本要求は指すだけである（表を指して書く作法）。旧
```text
#### AI と画面上で対話する

**Type**: FUNC_REQ
**UID**: FR-066
```
新
```text
#### `Agent API` を MCP として外へ出す

**Type**: FUNC_REQ
**UID**: FR-150

**STATEMENT**: `GRS` は、MCP の客が `Agent API` を MCP の道具として呼べる取次を配り、取次と、取次に繋ぐページを、**表 T-035 の `AG-12`** に従わせること。

**ORIGIN**: UC-012 手順 1 〜 4、拡張 1b

**RATIONALE**: **MCP は `Agent API` に標準の口金を付けるだけであり、能力を足さない** —— 呼べることは 表 T-107 のメンバのままである。  
⭐ **文書の唯一の正は、開いているページのままとする** —— 文書に書くのはいつもページ 1 つであり、UID を振るのもページである。  
取次が文書を持てば、2 つの写しのどちらが正かを決める合流の規則が要る。  
⚠️ **`GL-005`（1 つのファイルだけで動く）とは正面からぶつかる** —— MCP の相手は待ち受けるプロセスであり、ブラウザのページは待ち受けられないので、取次というプロセスが 1 つ要る。  
⭐ **それでも `GL-005` は動かさない** —— 取次を足すのは MCP を使う人だけである。  
使わない人の `GRS` は、今までどおり 1 つの `.html` をブラウザで開くだけで動く。  
⭐ 取次がページを自分で配るのは、ページと取次を同じ origin に置くためである —— 手元のファイル（`file://`）のページから繋ぐ道は、接続がどのページからのものかを取次が確かめる手立てが確かめられていない。  
⚠️ 代償: MCP を使うあいだは、手元のファイルではなく取次が配るページで `GRS` を開き、文書はそのページの「開く」（`FR-087`）で開く。  
⚠️ 代償: 取次を起動し直すと鍵が変わるので、ページを開き直す。  
待ち受ける口の番号が変われば origin も変わり、`FR-065` の有効化の記憶はその origin にしか効かないので、`IC-20` も押し直す。

**Relations**:

- **Type**: `Parent`
  **ID**: `UC-012`
  **Role**: `Satisfies`

#### AI と画面上で対話する

**Type**: FUNC_REQ
**UID**: FR-066
```

<!-- EDIT id=E-09 file=docs/spec/_assets/tbl-glossary.md -->
問 3 の ④。表 T-108 の末尾に、行の `editGroup` を書く命令を足す。`CM-76` は予約済み（取りまとめ役が 2026-09-24 に予約した）。旧
```text
| CM-75 | `TaskGroup` | `setTaskGroupKeptOpen` | — | 行の開いたままの印を立てる・外す | `FR-018`（表 T-254）|
```
新
```text
| CM-75 | `TaskGroup` | `setTaskGroupKeptOpen` | — | 行の開いたままの印を立てる・外す | `FR-018`（表 T-254）|
| CM-76 | `TaskGroup` | `setTaskGroupEditGroup` | — | 行の編集グループを入れる・消す。<br>人だけが使う（表 T-275 の `GP-1`） | `FR-111` |
```

<!-- EDIT id=E-10 file=docs/spec/01-04-requirements.md -->
問 3 の ④。表 T-233 の `RS-61` の後ろに、AI が `editGroup` を書こうとしたときの理由を足す（`RS-61` の行は変えない）。`RS-62` は予約済み（取りまとめ役が 2026-09-24 に予約した）。辞書の項は J-03。旧
```text
| RS-61 | **別の編集グループの行なので、書き換えを受け付けない** | `NT-1` | `FR-111` |
```
新
```text
| RS-61 | **別の編集グループの行なので、書き換えを受け付けない** | `NT-1` | `FR-111` |
| RS-62 | **編集グループは人だけが変えられるので、`Agent API` からの書き換えを受け付けない** | `NT-1` | `FR-111` |
```

<!-- EDIT id=E-11 file=docs/spec/05-07-design.md -->
`FR-150` を足した数の連鎖（`01-04-requirements.md` の要求 121 → 122、`FR` 108 → 109）。旧
```text
`01-04-requirements.md` が持つ要求は 121 件である（`FR` が 108 件、`NFR` が 13 件）。  
```
新
```text
`01-04-requirements.md` が持つ要求は 122 件である（`FR` が 109 件、`NFR` が 13 件）。  
```

<!-- EDIT id=E-12 file=docs/spec/05-07-design.md -->
`FR-150` は起点のユニットをまだ持たない（取次とページ側の口の設計は 8 節の波 ② が書く）。「起点をまだ書いていない要求」の 17 → 18、「仕様が起点のユニットを決めていない」の 12 → 13。⚠️ 99 ＋ 5 ＋ 18 ＝ 122 で E-11 と合う。旧
```text
⚠️ **起点をまだ書いていない要求が 17 件ある。**  
⛔ これは「持ち主が無い」ではなく「まだ決めていない」である。  
理由は 3 つに分かれる。

- **仕様が起点のユニットを決めていない（12 件）** —— `FR-027` ・ `FR-032` ・ `FR-034` ・ `FR-044` ・ `FR-048` ・ `FR-051` ・ `FR-091` ・ `FR-095` ・ `FR-097` ・ `FR-105` ・ `FR-106` ・ `FR-110`。
```
新
```text
⚠️ **起点をまだ書いていない要求が 18 件ある。**  
⛔ これは「持ち主が無い」ではなく「まだ決めていない」である。  
理由は 3 つに分かれる。

- **仕様が起点のユニットを決めていない（13 件）** —— `FR-027` ・ `FR-032` ・ `FR-034` ・ `FR-044` ・ `FR-048` ・ `FR-051` ・ `FR-091` ・ `FR-095` ・ `FR-097` ・ `FR-105` ・ `FR-106` ・ `FR-110` ・ `FR-150`。
```

<!-- EDIT id=J-01 file=docs/spec/_source/property-items.json -->
問 3 の ④、`CR-433` の ⑥。表 T-016 に `PR-33` を足す。⭐ `PR-20`（高さ）の後ろ、`PR-19`（色）の前 —— `FR-006` が色の行を同じ対象の並びの末尾に置くと定めている。表示名は J-02。旧
```text
  {
   "id": "PR-19",
```
新
```text
  {
   "id": "PR-33",
   "columns": [
    "editGroup"
   ],
   "inputKinds": [
    "文字"
   ],
   "appliesTo": "TaskGroup",
   "note": {
    "ja": "行を編集できるグループの名乗り（`fig-erd-detail.md` の `AT-144`）。空の欄は `null` ＝ 誰でも編集できる。⭐ **人だけが入れ・消せる項目である** —— `editGroup` が `null` でない行でも編集でき、`Agent API` からは書けない。規則は `FR-111` の 表 T-275 の `GP-1` が持つ"
   },
   "mspdi": {
    "ja": "無い（`GRS JSON` のみ）"
   }
  },
  {
   "id": "PR-19",
```

<!-- EDIT id=J-02 file=docs/spec/_source/display-words.json -->
`PR-33` の表示名（`properties` 節。並びは J-01 に合わせる）。旧
```text
  {
   "rowId": "PR-19",
```
新
```text
  {
   "rowId": "PR-33",
   "label": {
    "ja": "編集権限",
    "en": "Edit permission"
   }
  },
  {
   "rowId": "PR-19",
```

<!-- EDIT id=J-03 file=docs/spec/_source/display-words.json -->
`RS-62` の語と次の一手（`reasons` 節。`RS-61` の項の後ろ）。`RS-62` は予約済み（取りまとめ役が 2026-09-24 に予約した）。次の一手は `R1.6` の代わりの行動（人がプロパティパネルで入れる）。旧
```text
    "en": "Copy it into a row with no edit group, then edit it"
   }
  },
```
新
```text
    "en": "Copy it into a row with no edit group, then edit it"
   }
  },
  {
   "rowId": "RS-62",
   "text": {
    "ja": "編集権限は人だけが変えられます",
    "en": "Only a person can change the edit permission"
   },
   "nextStep": {
    "ja": "プロパティパネルで、人に入れてもらってください",
    "en": "Ask a person to set it in the properties panel"
   }
  },
```

## 5. `CR-433` の 10 節の 13 項（`ebc71984` で読んだ）

| 項 | 中身 | 今 |
|---|---|---|
| ① | 検査 23 の免除を 129→130・142→143 | ✅ 閉じた（`JDG-300` の Q1、`JDG-316`） |
| ② | 検査 39: 逐語の試験が無い MUST 11 | 決定 9（案 b） |
| ③ | 複数人の編集に `GL-` を足すか | ✅ 閉じた（`JDG-300` の Q28: 足さない） |
| ④ | リテラルの `editGroup: null` と補い | ✅ 着いた（2026-09-21） |
| ⑤ | `GP-1` ・ `GP-4` ・ `GP-5` が `RS-61` に届くか | 実装の波で確かめる（今は `editGroup` を読むコードが 0 —— 13 節の測り方 2） |
| ⑥ | 値を入れる所が無い | ✅ `PR-33`（J-01 ・ J-02）と命令（E-09）。人だけが変えられる（E-01） |
| ⑦ | 読み取り専用の行の見せ方 | 決定 12（塊なし。触れる見本が先） |
| ⑧ | `rulings.md` ・ `changelog.md` へ写す | ✅ 済んだ（`CR-549`） |
| ⑨ | 「閲覧は自由」を書くか | 決定 11 |
| ⑩ | 「自分のグループ」の置き場 | 持たない（サーバーと繋ぐときに決める。`GP-1` の注） |
| ⑪ | 名前の比べ方 | 決定 10 |
| ⑫ | `RS-61` の指紋の留め（`tests/contract/t-233-...test.ts:493`） | `DFC-678` が持つ。語を場面と照らして人が読む。⚠️ `RS-62` を足すと、同じ留めが新しい行を数える |
| ⑬ | 検査 60: `editTaskGroup` ・ `editTask` が 1 行ずつ | ✅ 許された（`JDG-310`） |

⚠️ **赤い門 4 つ**（`CR-433` の頭）のうち、① ③ ⑫ は上のとおり閉じたか持ち主が居る。残るのは ② だけであり、決定 9 で基準線を動かさずに閉じる。

## 6. グラフ（`45ba1707` で測った。13 節の測り方 1）

| 行 | 指している要求 / 参照 | 起草の初め（`ebc71984`） |
|---|---|---|
| `GP-1` | 1 / 1（`FR-111` の `GP-6` の行） | 1 / 1 |
| `GP-5` | 0 / 0（浮いている —— `FR-111` の表の中にだけ在る） | 0 / 0 |
| `CD-1` | 4 / 6（`FR-032` ×3 ・ `FR-033` ・ `FR-023` ・ `FR-076`） | 4 / 5 |
| `CD-2` | 3 / 9（`FR-032` ×3 ・ `FR-033` ・ `FR-098` ×2、節 3） | 3 / 8 |
| `CD-6` | 1 / 3（`FR-004` ×2、表 T-109 の節） | —（測っていない） |
| `HM-11` | 2 / 2（`FR-111` ・ `FR-033`） | 2 / 2 |
| `FR-032` | 指される: 要求 8 件 / 参照 24 箇所。指す: 表 10 ・ 行 15 | —（測っていない） |
| `FR-065` | 指される: 要求 3 件 / 参照 18 箇所。指す: 行 `AT-1` だけ | —（測っていない） |

⭐ 本書の塊が足す辺: `GP-1` → `CM-7` ・ `PR-18` ・ `PR-19` ・ `PR-33` ・ `GP-7` ・ `AG-5` ・ `AG-9a` ・ `RS-61` ・ `RS-62` ・ `FR-150`。`GP-7` → `CD-1` ・ `CD-2` ・ `GP-2` ・ `GP-5` ・ `FR-032` ・ `AG-3` ・ `RS-61`。`CD-6` → `GP-7`。`FR-150` → `AG-12`。`AG-12` → `FR-150` ・ `FR-065` ・ `IC-20` ・ `FR-029` ・ `AM-17` ・ `AG-6` ・ `AG-9a` ・ `FR-022` ・ `U-61` ・ `AG-5`。⭐ `GP-5` は E-02 から初めて指され、浮かなくなる。
⚠️ `FR-150` ⇄ `AG-12` は互いを指す（要求が表を指し、表の行が運ぶ要求を名指す）。`FR-064` ⇄ 表 T-035 と同じ形であり、閉路ではない（`AG-12` は `FR-064` の表の行である）。

## 7. 数の予測（`45ba1707` で数えた。13 節の測り方 4）

| 数 | 今 | 当てた後 |
|---|---|---|
| 表 T-275 の行 | 6 | 7（`GP-7`） |
| 表 T-035 の行 | 12 | 13（`AG-12`） |
| 表 T-016 の行 | 19 | 20（`PR-33`） |
| 表 T-108 の行 | 72 | 73（`CM-76`） |
| 表 T-233 の行 | 57 | 58（`RS-62`） |
| 表 T-234 の行 | 8 | 8（`QN-1` ・ `QN-2` の中身だけ） |
| 表 T-050 の行 | 6 | 6（`CD-6` の中身だけ） |
| 表 T-107 の行 | 18 | 18（変えない） |
| 表 T-109 の行 | 97 | 97（`IC-116` は使わない） |
| `FR` の UID | 108 | 109（`FR-150`） |
| `UC-012` の拡張 | 2（1a ・ 3a） | 3（1b） |
| 表を新しく足す | — | 0 |

## 8. 波

リファクタの後。① 行の権限（表 T-275 の実装: `GP-1`〜`GP-7`、`HM-11`、`FR-033` の例外、表 T-016 の `PR-33` と命令、`RS-62`、鍵の印は見本の後）—— 今は `editGroup` を読むコードが 0 なので、ここが本体である。② MCP の取次（問 2 の答えの形）。⚠️ 取次とページ側の WebSocket の口は `05-07-design.md` の構成（表 T-062 ・ 表 T-075）にまだ無い —— 波 ② の頭で設計の変更要求を書き、`FR-150` の起点のユニットを決める（E-12 の 13 件から外す）。ページの CSP（`connect-src`）が `127.0.0.1` への WebSocket を通すかも、そこで決める。③ 仕様だけを読む試験の体（決定 9 を含む）。① と ② は持ち場が重ならない。
⚠️ **本書の塊は MUST を足す**（E-01 ・ E-02 ・ E-03 ・ E-07 と J-01 の注）。当てた直後、検査 39（逐語の試験が無い MUST）の数が上がる。⛔ 基準線を動かすのは利用者の許しが要る —— 決定 9 のとおり、試験の体が逐語の試験を書いて閉じる。
⚠️ E-09 の命令を仕様に足すと、`src/` の命令の全数と 表 T-108 が食い違う検査が赤くなりうる —— 当てる波で命令を実装するか、当てる者が赤の持ち主を台帳に書く。
⚠️ **写しに当てて走らせた検査**（2026-09-24、`45ba1707` の写しに 15 塊を当て、`property_items_json_to_md.py` で 表 T-016 を刷った）: 検査 46（文の切れ目）・`md-checks.py`・`check-marks.py`・`audit-ch5.py`・`check-spec-id-references.py` は緑。赤は 2 つで、どちらも塊の誤りではなく、当てる者が同じコミットで片づける随伴である:
- `check-dictionary-table-covariance.py` —— `PR-33` は新しい組、`QN-1` と `QN-2` は指紋が動いた。⭐ 辞書の問いの文（「この行と、行に載っているタスクを削除しますか？」「このタスクと、その配下のタスクを削除しますか？」）は、E-05 の後も行の言うことと食い違わない（行の名前は文書の値であり、辞書に持たない）。読み合わせたうえで `--write-baseline` を走らせる。⛔ 基準線を動かすので、当てる前に利用者の許しを取る。
- `check-language-dictionary.py` —— `property-items.json` の `/items[]/inputKinds[]` の免除が 23 のまま、原稿は 24 を持つ。同検査の注が「列を 1 つ足す変更は同じコミットで 1 上げる」と定めているので、23 → 24 に上げ、理由に `PR-33` を書く。

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

`DFC-560`（書き手の名が `'agent'` に固定）は MCP の書き手の名とかち合う。`DFC-787`（起動時に `Agent API` の有効化を告げる所が無い）は MCP でも同じである。

## 10. ⛔ この変更でやらないこと

- サーバー（GRSS）・名簿・認証・閲覧の制限（`JDG-309`、`FR-111` の RATIONALE）。
- 「自分のグループ」（`GP-1` の注の「いま持ってはならない」）。
- 名前の比べ方（決定 10）。
- 画面を持たない運搬（`CR-434` の C 案）—— `FR-065` ・ `FR-066` ・ `AG-9` ・ `MG-9` が成り立たない。
- 読み取り専用の行の鍵の印（決定 12 —— 触れる見本が先）。

## 11. 前に立つ者へ返す問い

| 問い | 案 | 推奨 |
|---|---|---|
| 問 1: 文書の唯一の正をどこに置くか（`CR-434` の ①）（✅ A —— `JDG-503`） | **A. ページ（今のまま）** —— AI は開いているページの `Agent API` を MCP で叩く ／ B. GRSS（サーバー上の JSON） | ⭐ **A** —— `GL-005` を動かさず、`JDG-303` の「将来」を今に引き寄せない。B は合流が主題になり、UID の重なり（⑧）と版の親（⑨）を先に決めることになる。⚠️ 代償: AI が編集できるのは、利用者が `GRS` をブラウザで開き、`IC-20` を押している間だけである |
| 問 2: 運び手をどう配るか（A を採るとき）（✅ 推奨どおり —— `JDG-503`） | **A1. 取次を 1 つのファイルで配る**（AI のアプリが MCP の相手を 1 ファイルで入れる形。実行の土台はアプリが持つ）／ A2. `node` で動く取次（`node` を入れた人だけ）／ A3. 既に配っているページから `ws://127.0.0.1` へ繋ぐ（`CR-434` の B。混在コンテンツと `file://` の origin が未実測） | ⭐ **A1 を本筋、A2 を同じ中身の別の配り方** —— 利用者はファイルを落とすだけである（利用者の裁定 2026-09-16）。取次は `GRS` のページを `127.0.0.1` で配り、同じ origin で繋ぐ（`CR-434` の A）。⚠️ 代償: MCP を使うときは、手元のファイルではなく取次が配るページで `GRS` を開く。⭐ 仕様の文では `node` と書かず「コマンド行の JavaScript の実行環境」と書いた（製品名を書かない作法） |
| 問 3: 読み取り専用の行の細部（✅ 推奨どおり —— `JDG-503`） | ① 個々のタスクの削除 ② 消す連鎖が別の行のタスクに届くとき（2.2 節の I-2）③ AI が読み取り専用の行を消すこと（I-7）④ 行の名前・色・`editGroup` を変えること | ⭐ ① **断る**（切り離しの単位は行）② **消し、確認がその行とタスクを挙げる**（`GP-5` と同じ「挙げて止めない」）③ **断る** —— AI には確認の面が無い。行を消すのは人の操作とする ④ 名前と色は **断る**。`editGroup` は **人だけが** プロパティパネルで入れ・消せる。AI は書けない —— 読み取り専用の行が、AI に触らせない柵にもなる |
| 問 4: 辞書の語（4 節の J-02 ・ J-03。⛔ 辞書の `$comment` は体が起こした語を禁じている —— `CR-562` の問 2 と同じ扱い。2026-09-24 に前に立つ者が足した） | `PR-33` の見出し「編集権限」／ "Edit permission"。`RS-62` の語「編集権限は人だけが変えられます」／ "Only a person can change the edit permission"、次の一手「プロパティパネルで、人に入れてもらってください」／ "Ask a person to set it in the properties panel" | ⭐ 案のまま —— `RS-61` の次の一手（"Copy it into a row with no edit group, then edit it"）と同じく、AI が次に何をすればよいかを 1 文で言う。✅ **2026-09-24 に答えが出た（`JDG-508`）: 画面の語は「編集権限」。列の名 `editGroup` は変えない** |

## 12. 台帳（2026-09-24 に本書を書いた者が写した）

- 裁定: `rulings.md` の「2026-09-23 —— MCP 連携・行の削除・画像から GRS JSON（CR-562 / CR-563）」の節の `JDG-500`。
- 答え: `rulings.md` の `JDG-503`（2026-09-24）。
- 裁定待ち（2026-09-24 に `裁定済`）: `pending-decisions.md` の `PND-602`（問 1）・`PND-603`（問 2）・`PND-604`（問 3）。⚠️ `PND-601` は使っていない（`CR-562` の問 1 を分類 C で置くと検査 25 が暫定の印を求めるので、行を置かなかった）。
- ⚠️ 決定 14 〜 17 は 4 節の塊を書いた者が 2026-09-24 に足した。台帳の行はまだ無い —— 前に立つ者が読んで、覆さなければ `rulings.md` へ写す。

## 13. 測り方の再現

```bash
# 1. the rows and their reach (section 6)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py GP-1 GP-5 CD-1 CD-2 CD-6 HM-11 FR-032 FR-065
# 2. no code reads editGroup to refuse a write yet (10 hits in src/*.ts, all schema / codec / literals; 0 left after the filter)
git grep -n 'editGroup' -- 'src/*.ts' | grep -v -e json-codec -e mspdi-codec -e schedule.ts -e document-change-plan -e task-create -e task-group-naming
# 3. the Agent API reaches the page only as a window global
git grep -n 'grSchedulerAgentApi' -- src
# 4. row counts (section 7): count table rows by prefix
python -c "import re;t=open('docs/spec/01-04-requirements.md',encoding='utf-8').read();g=open('docs/spec/_assets/tbl-glossary.md',encoding='utf-8').read();p=open('docs/spec/_assets/tbl-property-items.md',encoding='utf-8').read();r=lambda s,x:len(re.findall(r'^\| '+x+r'-\d+[a-z]? \|',s,re.M));print({x:r(t,x) for x in ['GP','AG','RS','QN','CD']},r(g,'CM'),r(p,'PR'),len(re.findall(r'^\*\*UID\*\*: FR-',t,re.M)))"
# 5. every old block in section 4 occurs exactly once in its file (15 of 15 on 45ba1707)
#    parse each "<!-- EDIT id=.. file=.. -->" and its first ```text block, then str.count(old) == 1
# 6. no command in table T-108 writes editGroup (section 1.1)
grep -n 'editGroup\|EditGroup' docs/spec/_assets/tbl-glossary.md
```
