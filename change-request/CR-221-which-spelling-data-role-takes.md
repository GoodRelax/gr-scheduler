# CR-221 — `data-role` がどちらの記法を取るか

> **閉じるもの**: ⛔ **表 T-006a の `W-4` は `data-role` を kebab-case と定めるのに、実装は `W-6` の形（PascalCase の複合語・空白あり）を書いている。**
> ⭐ **利用者の裁定（2026-08-23、裁定 20）**: **`W-4` に但し書きを足す。UI パーツの名を運ぶ `data-role` は `W-6` の形とする。**
> ⚠️ **体は「識別の仕様が無い」と報告したが、引いたら無いのではなく二重に決まっていた。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1` 〜 `CH-5` のどれでもない —— 基盤の作業である**（規則 01 の ① が「基盤の作業は正当な答えである」と認める形。⚠️ **`GL-001` 〜 `GL-007` のどれも直接は受けない**）。
⛔ **表 T-006a の中の 2 行が同じ属性について別のことを言っており、どちらでも書ける状態になっている。**
⚠️ **課題を前へ進めるのではなく、課題に取りかかる者が迷わないようにするものである。**

⚠️ **`tests/system` がこれに乗っている** —— `live-app.ts:38` が `[data-role="Schedule Canvas"]` を使う。**どちらが正かが決まらないと、次に書く者がどちらでも書ける。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.3`**（唯一の正がある）| `data-role` の記法 | ⛔ **表 T-006a の中の 2 行が、同じ属性について別のことを言っている。** `W-4` は kebab-case、`W-6` は PascalCase の複合語 |
| **`R2.1`**（仕様の名前を使う）| 確定名を kebab へ訳す案 | ⛔ **退けた。** ⭐ **訳すと同じものに 2 つ目の綴りができる。** 規則 03 の 1. が「仕様が名前を持つ概念に別名を与えてはならない（MUST NOT）」と定める |
| **`R2.7`**（DRY）| 訳語表 | ⛔ **kebab へ訳すなら `Schedule Canvas` → `schedule-canvas` の対応表がどこかに要る。** それは 表 T-103 の写しであり、必ずずれる |
| **`R6.2`**（試験が意図を読める）| `[data-role="Schedule Canvas"]` | ⭐ **確定名がそのまま出ているので、選択子を読めば何を掴んでいるか分かる。** kebab だと表を引き直すことになる |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **但し書きを `W-4` の行の中に書き、新しい行を立てない** | ⚠️ **`W-7` 〜 `W-11` が既に在り、番号を足すと 表 T-006a が 12 行になる。** 但し書きは `W-4` の例外であって、別の面の記法ではない |
| 2 | ⭐ **「UI パーツの名を運ぶとき」と条件を書いた** | ⛔ **`data-role` のすべてを `W-6` にするのではない。** `ROLE.rowExpander` のような**部品の役割**を書く値は、パーツの確定名ではないので kebab のままである |

---

## 1. 測った

```
induced.py  T-006a W-4 W-6 T-006b （ほか 35 種と同時に）
impact.py   T-006a
```

| 測ったこと | 実測 |
|---|---|
| 表 T-006a の行数 | **11 行**（`W-1` 〜 `W-11`）|
| 表 T-006a を指している要求 | **0 件** —— 記法の正であり、要求からは行 ID で引かれる |
| 閉路 | ⭐ **0** |

⭐ **表 T-006a の前文が射程を明言している**（引いて確かめた）:

```
「同表が縛るのは、本仕様書が定めるソフトウェアの中の名前である（MUST）
  —— src/ のコードと、本ソフトウェアが読み書きする鍵」
⇒ data-role の値は射程内である。⛔ 逃げ道は無い
```

⚠️ **実測した食い違い:**

```
表 T-006a W-4              `data-role` ＝ kebab-case（例 `toggle-plan`）
表 T-006a W-6              UI パーツ（散文）＝ PascalCase の複合語（空白あり）
dom-screen-surface.ts:1123 「data-role takes the surface's own name」＝ W-6 の形
tests/system/live-app.ts:38 `[data-role="Schedule Canvas"]`
```

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-006a の `W-4` | 記法の欄に但し書きを足す ——「kebab-case。⚠️ **ただし UI パーツの確定名を運ぶ `data-role` は `W-6` の形とする**（表 T-103 の名をそのまま書く。訳すと 2 つ目の綴りができる）」|
| 2 | A-appendix | 変更履歴に 1 行 |

⛔ **`W-6` は 1 文字も動かさない。** ⛔ **実装も試験も動かさない** —— 本 CR は、既に動いているものを仕様の側で正当化するものである。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **132** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1613 | **1613** | ⭐ **0** —— 既存の 1 セルへの追記 |
| `uids` | 148 | **148** | 0 |

⚠️ **前の欄は `CR-220` の後の値である**（本巡はまとめて当てるので、予測は積み上げで書く）。
