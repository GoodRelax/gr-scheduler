# CR-144 — Chapter 4 の値と経路

> ⛔ **未適用である。** 15 編集は逐語検証済み（すべて `count=1`）。
> ⚠️ **CR-143 と 1 行を共有しているので、単独では当てられない。** §5 を読むこと。

**閉じるもの**: 実装着手前レビューの **F-5** / **F-6** と 付録 A の **C-4** / **C-5**。

## 1. 出どころと、計画からの切り分け

編集一覧の出どころは `docs/review/edit-plan-2026-08-15.md:1041-1238`（同書では「CR-141（版 0.35）」節）である。**日付つきの記録なので同書は書き換えない**（CR-123 の作法）。切り分けはここに記録する。

| 計画の編集 | 扱い | 理由 |
|---|---|---|
| 1・2・3 | ⛔ **撤回** | **F-4 を閉じるのは CR-143 になった。**計画は掴んだときの `actualDuration` を `0` としていたが、利用者の裁定は **1 稼働日**（定数 `actualInitialDuration`）である。編集 1 と 3 は CR-143 と同じ行（`:1937` / `:2255`）を取り合い、編集 2 は置く値が正面から矛盾する |
| 4 | ➡️ **CR-143 へ移管** | `05-07-design.md:496`「置く値は 表 T-019 と 表 T-021a が持つ」は **F-4 の落ち**である。同じ節の `:472` が既に「辺は 表 T-021a の 4 行と `FR-043` / `FR-044`」と書いており食い違っている |
| 5 〜 18 | ✅ **採る**（14 編集） | F-5 / F-6 / C-4 / C-5 |
| 19 | ✅ **採る。ただし書き替える** | 版と CR 番号を **0.37 / CR-144** に。⚠️ **F-4 の記述（「実績期間 ＝ `0`」）を落とす** —— 撤回したので偽になる |

## 2. 何をするか

| | |
|---|---|
| **F-5** | 最上位の行を非表示にすると戻す入口が無い → `HR-6` に**行見出しパネルの根の非表示グループタブ**を足す。UC-002 手順 5 から「親の」を外す。**新しい UI パーツは作らない**（`U-29` を根にも置く） |
| **F-6** | 散文の数え上げ 2 件を実体に合わせる —— `LM-14` の指し先（表 T-206 に ⛔ は 0 個。逐語「別枠。`localStorage` に置く。」は 4 行）／ 1.8 の「5 つの表」→ **表 T-101 〜 T-107**（実体は 7 表） |
| **C-4** | 週の始まりの置き場を `Project.weekStartDay` に正す（`Calendar` は 7 列でその列を持たない）。`Calendar` と `Project` にまたがる編集を 1 回の書き込みで確定させる |
| **C-5** | 稼働日を交換相手の期間へ換算する値を定める —— `Project.minutesPerDay`、空のときは **`S-128`（`480`）** |

## 3. 前回の検証で直したもの

| | |
|---|---|
| **版と CR 番号** | 計画の「0.35 / CR-141」→ **0.37 / CR-144**。追記位置は **0.36 の行の直後**（ファイル末尾） |
| **行番号** | 計画の 場所 は最大 +29 行ずれている。⚠️ **逐語の BEFORE だけで照合する** |
| **`AG-3` の射程** | 計画は暦編集の原子性を 表 T-035 の `AG-3` に帰していたが、**同表は「`Agent API` の規約」**である。人の UI 編集には掛からないので **表 T-067 の `WS-3`** を指す形に改める |
| **編集 2 の理由** | 計画は「`FR-088` は Chapter 5 に 0 回」と書くが、**`UF-66` に 1 回ある**（CR-140 が足した）。`FR-054` の 0 回は正しい |

## 4. ⚠️ 残る 3 件（当てる前に文言を決める）

| | |
|---|---|
| **表 T-209 の題の射程** | 題は「MSPDI を**取り込んでいない**文書が使う」だが、`MinutesPerDay` は XSD で `minOccurs="0"`。**取り込んだ文書でも空になりうる**ので、編集 11 の射程と食い違う。題を広げる必要がある |
| **`FR-063` の例外** | 「見せ方の群の全数は `tbl-settings.md`（**表 T-216 だけ**が日程データ）」に対し、`S-108` / `S-128` は `Project` の列 ＝ 2 つ目の例外になる。⚠️ **表 T-209 は元から暦（日程データ）の既定を持っており、この綻びは本 CR の前からある** |
| **C-5 は半分** | レビューは ⓐ 表 T-059 に換算を明記し `DV-8` / `AT-35` が指す形にする、ⓑ `MinutesPerWeek` / `DaysPerMonth` にも同じ扱いが要るか決める、も求めている。**どちらも編集に無い。**変更履歴を「C-5 は一部」と正直に書くか、編集を足すか |

## 5. ⚠️ CR-143 と 1 行を共有している —— 単独では当てられない

グラフ調査で **2 つの CR をまたぐ閉路**が出た。**EDIT 束縛である。**

```
FR-043 ──(CR-143 が新設)──▶ FR-054 ──(既存)──▶ FR-011 ──(既存)──▶ FR-043
```

**`01-04-requirements.md:2446` の 1 行**が、閉路の辺（前半の `FR-011` / `FR-012` 依存文）と**本 CR の編集 12**（後半の週の始まりの MUST）を**両方持っている**。

⭐ **この 1 行は CR-143 の内容が確定してから、1 回で書く。**

**当てる順**: **CR-144 → CR-143。** 本 CR だけが `erd.json` を触って検査 16 の再生成を強制し、`S-128` を MUST 文に焼き込むためである（CR-143 の席は散文 3 箇所なので、動かすなら CR-143 側が安い）。

## 6. 数の予測

| | 改定前 | 改定後 |
|---|---|---|
| `tables` | 114 | **114**（新しい表を作らない） |
| `rows` | 1324 | **1325**（`S-128` の 1 行） |
| `figures` / `uids` | 10 / 140 | **不変** |
| 生成物 | — | ⚠️ **`erd_json_to_md.py` を走らせる。走らせないと検査 16 が FAIL する**（実測） |
| `components.json` | 38 / 86 | **触らない** |
