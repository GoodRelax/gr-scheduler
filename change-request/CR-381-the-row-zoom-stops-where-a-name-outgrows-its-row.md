# CR-381 — マイルストーンを一回り小さくし、行の軸の上限を矩形の名前の字で置く

> **閉じるもの**: 利用者の申し立て 2026-09-15 の F と、その答え —— 逐語は `docs/development-records/rulings.md` の各行。
> - `JDG-88`（F、★）: タスク／マイルストーン名の字の大きさの最大値が Lv1 のタスクグループ名の字を超えないよう、縦方向の拡大を制限しろ。台帳 `DFC-595`
> - `JDG-99`（方向）: 上限はフォントサイズの計算で求め、実際の条件はタスクやマイルストーンの縦幅で決める
> - `JDG-110`: 「① マイルストーンのサイズを一回り小さくしろ」
> - `JDG-115`: 「よしやれ」 —— 前に立つ者の推奨（`S-17` を 1.5 → 1.25、上限は案「乙」＝矩形で測る）を採る
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた**（初稿は問いを残して止めた。`JDG-110` / `JDG-115` で決まったので当てた）。読んだ木は `87861cf3`。
> ⚠️ `docs/spec/_source/settings.json` を変えて `npm run gen` を回したので、生成物の `document-settings.ts` と `startup-template.json` が動いた（手では書いていない）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`（全体日程を 1 画面で見られる）**。縦に拡げすぎて、タスクの名前が行の名前より大きく、画面がタスクの名前で埋まることを止める。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— `FR-016` の MUST NOT「字の大きさを 2 つ比べて決めてはならない —— どちらかが動くたびに上限も動く」と `JDG-88` が正面から衝突する ⇒ 覆した（決定 2）。
- **`R1.4`（境界値）** —— 形状ごとの縦幅の比（`S-13`〜`S-17`）が名前の字を形状ごとに変える。境界は「どの形状で測るか」にある（第 1 節）⇒ 矩形で測る（決定 3）。
- **`R1.1`（曖昧さがない）** —— 上限の倍率でのマイルストーンの名前は Lv1 を超える。これを代償として条項に明記した。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**: `JDG-88`・`JDG-99`・`JDG-110`・`JDG-115`。

**問うこと**: なし（0 件）。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| 決定 1 | いまの上限（いちばん高い行の帯が `Row Area` に達する倍率）は残し、新しい上限との小さい方をとる | その MUST を外す裁定は無い。`JDG-115` の読み「いまの上限との小さい方」 |
| 決定 2 | **`FR-016` の MUST NOT「字の大きさを 2 つ比べて決めてはならない」を覆す** | 上限が深さ 1 の行の名前の字（`S-36` × `S-38`）から計算される以上、その字が動けば上限も動く。`JDG-99`「上限はフォントサイズの計算で求める」、`JDG-115` がそれを受け入れた。⚠️ **代償**: `S-36` / `S-38` を変えると上限が動き、いまの倍率が上限を超えていれば引き下げられる |
| 決定 3 | **上限は矩形（`S-13`、比 1.0）の名前の字で測る**（案「乙」）。描かれている形状では測らない | `JDG-115`。描かれている形状で測ると、マイルストーンを 1 つ置いただけで上限が動く。⚠️ **代償**: 上限の倍率 1.320 ではマイルストーンの名前が 21.1px と Lv1 の 16.9px を超える —— `JDG-115` で受け入れられた |
| 決定 4 | **`S-17`（`shapeHeightOf.milestone`）を 1.5 → 1.25**。下限（1 超）と上限（4）は据置 | `JDG-110` の「一回り小さく」を `JDG-115` が 1.25 と決めた。等倍の名前の字が 16.0px で Lv1 以下になる |

---

## 1. 測った（既定値。コードの式をそのまま積んだ）

測り方: 字の大きさは `src/entity/layout-engine/schedule-layout/schedule-layout.ts:305-309`（`labelFontSize` ＝ max(`fontMin`, 予定の縦幅 × 形状の比 × `actualOfPlan` × `fontOfActual` ×（矢印・端点スパンなら `thinFontScale`））、深さ 1 の行の名前の字は `src/adapter/screen-renderer/row-title-panel.ts:65`（`rowTitleFont` × `rowTitleTopScale`）。値は `_assets/tbl-settings.md` の既定。計算は scratchpad の Python で行った。

| 形状 | 等倍の名前の字 | 名前の字が Lv1（16.90px）に達する倍率 | 倍率 1.320 のときの字 |
|---|--:|--:|--:|
| 矩形・矢羽根（比 1.0） | 12.80px | **1.320** | 16.90px |
| 矢印・端点スパン（比 0.5、細線 0.85） | 12.00px（床） | 3.106 | 12.00px |
| マイルストーン 前（比 1.5） | 19.20px | 0.880 | 25.35px |
| マイルストーン 後（比 1.25） | **16.00px** | 1.056 | **21.12px** |

## 2. グラフ

**`impact.py FR-016 S-36 S-38 FR-077 FR-094`**（101 行）: `FR-016` は要求 1（`FR-075`）/ 参照 9、`S-36` / `S-38` は `FR-004` の `HF-5` だけ、`FR-094` は要求 3 / 参照 14。
**`impact.py S-17 S-13 S-36 S-38`**（当てた後）: `S-17` と `S-13` を指すのは `FR-016` の新しい段だけ。⭐ **比 1.5 を前提にした条項は 0** —— `grep` で `S-17`・`shapeHeightOf.milestone`・「1.5 倍」を `docs/spec` の原稿・図・`_assets` に引いて、`FR-016` の新しい段と設定値の行のほかに 0 件。表 T-023d の `GR-15` / `GR-18`、表 T-240 の `DM-9`、表 T-221 の `LF-10` は「実績の図形と同じ正方形」「予定の縦幅を一辺」と相対で書いており、比の値を持たない。
**`induced.py`**: 本書に掛かる閉路は `FR-016 ↔ PI-5`（値の行と要求の作法）。`PI-5` の文は変えていない。
**`rulings.md`**: `FR-016` / `zoomY` → `JDG-88`・`JDG-99`、`S-17` → `JDG-110`・`JDG-115`。衝突 0。

## 3. 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| `FR-016` の行の軸の上限の段 | `01-04-requirements.md` | MUST NOT「字の大きさを 2 つ比べて決めてはならない」を外し、上限 ＝ いまの倍率と「矩形（`S-13`）の名前の字が深さ 1 の行の名前の字（`S-36` × `S-38`）に等しくなる倍率」の小さい方（MUST）、形状によらず矩形で測る（MUST）、代償 2 つ（`S-36` / `S-38` で上限が動く、上限の倍率ではマイルストーンの名前が Lv1 を超える）を置いた |
| 表 T-201 `S-17` | `docs/spec/_source/settings.json`（`npm run gen`） | 既定 1.5 → 1.25。範囲の理由に、利用者の指定であること・等倍の名前の字 16.0px・求め方 |

## 4. 数の予測

tables 0 / figures 0 / rows 0 / uids 0（段の文と値だけ）。

## 5. 影響 —— 本書の外で動くもの

| 所 | 何が動くか |
|---|---|
| 生成物 | `_assets/tbl-settings.md`、`grs-document.schema.json`、`document-settings.ts` の生成範囲、`startup-template.json` |
| `src/`（上限を求める所） | 矩形の名前の字で求めた倍率との小さい方を採る（実装の波） |
| 試験 | `tests/unit/fr-016-zoom-bounds.test.ts`、`tests/unit/dfc-377-fr-016-zoom-ceilings-and-ep-9-thickness.test.ts`、マイルストーンの大きさ（比 1.5）を前提にした試験は赤くなりうる（実装の波で直す） |
| `docs/development-records/rulings.md` | `JDG-110` / `JDG-115` を `適用済` |
| `docs/development-records/defects.md` | `DFC-595` を `実装待ち` |

## 6. ⛔ この変更でやらないこと

- ⛔ 日付の軸（`zoomX`）の上限に触れない
- ⛔ `S-17` の下限・上限を変えない
- ⛔ `src/` と `tests/` を手で書かない（生成物だけが動いた）

---

## 7. 当て直し（2026-09-15、実装の体の停止を受けて）

> 実装の体 I-1 が本書の条項で止まった（`DFC-595`）。読んだ木は `5c6efa34`。`src/` と `tests/` には触れていない。
> ⭐ 利用者は留守中で「迷ったら推奨通りでよい」と言った ⇒ 推奨を採って閉じ、決めた点を 7.2 に並べた（覆してよい）。

### 7.1 止まった所と、確かめた結果

| 止まった所 | 確かめたこと | 結論 |
|---|---|---|
| 深さ 1 の行の名前の字が読めない | `row-title-panel.ts` の `rowTitleFontPx` は公開されていない（`image-exporter.ts` にも同じ式の写しが在る）。表 T-064 の `PI-37`（`ScreenRenderer`）に行が無い | 置き場の穴 |
| 矩形の名前の字の式が読めない | `schedule-layout.ts` の `labelFontSize` は公開されていない。`PI-5` の MUST NOT「式を写して持たせてはならない」があるので、上限を求める `input-command-translator.ts` が写せない | 置き場の穴 |
| 字の床で止まっている倍率では、比から上限を出せない | 名前の字 ＝ max（`S-8`、予定の縦幅 × 比 × `S-5` × `S-7`）、予定の縦幅 ＝ max（`S-6` ÷ `S-5`、`S-4` × 倍率）× 比。床が効く倍率では字が倍率に比例しない | 式の段の穴 —— 式を倍率について解き、床だけで超える組の答えを決める（決定 5） |
| （見つけた）いちばん高い行の帯を翻訳側が線形に分けて数えている（`bandGrowthOf`） | `CR-380` の `OC-10` で帯高に倍率に依らない `S-196` と、字の床で止まる名称ラベルの高さが入る | 線形の分け方は成り立たない ⇒ 帯は `PI-5` に問う（決定 7） |

### 7.2 推奨で決めた（覆してよい）

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 5 | 上限の倍率は、矩形の名前の字の式を倍率について解いて求める（MUST）。いまの字といまの倍率の比から求めない（MUST NOT）。床だけで矩形の名前の字が既に深さ 1 の行の名前の字以上になる設定の組では、床が外れて字が大きくなり始める倍率とする（MUST） | 閉じた式は床に依らずに 1 つの倍率を答える。床だけで超える組には「字 ≦ 深さ 1」を満たす倍率が無いので、字を床より大きくしない所で止めるのが最も近い | その組では、上限の倍率でも矩形の名前が深さ 1 の行の名前より大きい（床の値のまま）。既定では起きない（7.3） |
| 決定 6 | 表 T-064 の `PI-5` に `zoomYAtRectangleLabelFont`（与えた字の大きさに矩形の名前の字が等しくなる倍率。床だけで超えるときは床が外れる倍率）を、`PI-37` に `rowTitleFontPxOf`（行の名前の字の大きさ。深さ 1 は `S-36` × `S-38`）を置く | 辺 `InputCommandTranslator` → `ScheduleLayout` と `InputCommandTranslator` → `ScreenRenderer` は既に在る（後者の説明に `FR-016` を足した）。`ImageExporter` → `ScreenRenderer` も在るので、書き出しの写しも同じ 1 本を読める。字の式を 1 か所に置く（`PI-5` の MUST NOT） | 実装の波で 2 つが書かれるまで、検査 26b は公開した名前が `src/` に無いと読む |
| 決定 7 | いちばん高い行の帯は `PI-5` の `rowPlacesAtZoomY` に問うて求める（MUST） | `FR-016` が既に置く「Adapter に自前の割付けをさせてはならない」（表 T-070 の `MN-6`）と同じ向き。翻訳側が `S-196` を読む必要も消える | 上限を求めるのに、倍率を変えて割付けを何度か問うことになる（計算が増える） |

### 7.3 測った（既定値）

| 数 | 値 | 導出 |
|---|--:|---|
| 深さ 1 の行の名前の字 | 16.90px | `S-36` 13 × `S-38` 1.3 |
| 倍率 1 の矩形の名前の字 | 12.8016px | `S-4` 28 × `S-13` 1.0 × `S-5` 0.5715 × `S-7` 0.8 |
| 解いた倍率 | 1.3201 | 16.90 ÷ 12.8016（第 1 節の 1.320 と一致） |
| 予定の縦幅の床が外れる倍率 | 0.99988 | （`S-6` 16 ÷ `S-5` 0.5715）÷ `S-4` 28 |
| `S-8` の床が外れる倍率 | 0.93738 | `S-8` 12 ÷ 12.8016 |
| 行の軸の上限の、字の側の値 | 1.3201 | max（1.3201、0.99988、0.93738）—— 既定では床は効かない |
| 床だけで超える組の例 | `S-8` ＝ 20 のとき 1.5623 | 20 ≧ 16.90 ⇒ 20 ÷ 12.8016 ＝ 1.5623 と 0.99988 の大きい方（字は 20px のまま） |

計算は scratchpad の Python で行った。

### 7.4 グラフ

`CR-380` 第 7 節の 7.4 と同じ測り（`impact.py` 9 種、`induced.py` 15 種）。本節に掛かる閉路は `FR-016 ↔ PI-5` の 1 つで、2 つを同じ 1 回に書いた。`rulings.md` の `JDG-88` / `JDG-99` / `JDG-110` / `JDG-115` と衝突 0 —— `JDG-115` の「矩形で測る」「いまの上限との小さい方」は変えていない。

### 7.5 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| `FR-016` の行の軸の上限の段 | `01-04-requirements.md` | 字の式を倍率について解く（MUST）。比から求めない（MUST NOT）。床だけで超える組は床が外れる倍率（MUST）。式の持ち主は `PI-5`、深さ 1 の字は `PI-37`、どちらも写さない（MUST NOT）。いちばん高い行の帯は `rowPlacesAtZoomY` に問う（MUST） |
| 表 T-064 の `PI-5` | `05-07-design.md` | `zoomYAtRectangleLabelFont` |
| 表 T-064 の `PI-37` | 同 | `rowTitleFontPxOf` |
| `InputCommandTranslator` → `ScreenRenderer` の辺 | `docs/spec/_source/components.json` | 説明に深さ 1 の行の名前の字（`FR-016`）を足した |

### 7.6 数の予測

tables 0 / figures 0 / rows 0 / uids 0。
