# CR-414 — 行見出しパネルの入口だけ、図形と枠の隙間を 1px にする

> **閉じるもの**: 利用者の裁定 `JDG-149`（問 1 への答えと二通目）を仕様へ着地させる。逐語は `docs/development-records/rulings.md` の `JDG-149` が持つ。
> - 問 1 への答え: 「行の操作子枠内のグリフのサイズはそのままにして、枠を小さくしてはどうか？ つまりマージンを減らす。 その上で、① 下限を守るとせよ。」
> - 二通目: 「問 1 の「余白を減らす」 : 行見出しパネルだけを詰めろ。 4 → 1 で、約 24pxでやってみろ。」
>
> ⭐ **本書は仕様だけを当てる。** 表 T-206 に `S-243`（行見出しパネルに載る入口の、図形と枠の最低隙間 1px）を足し、`FR-029` に「行見出しパネルの入口では隙間を `S-243` とする（MUST）」を足し、外形の幅の式・`S-235` を掛ける文・表 T-051 の `HF-5` / `HF-19`・`05-07-design.md` の `LF-3`・`S-141` / `S-237` / `S-79` の注・表 T-252 の `DS-7`・`FR-039` の行見出しパネルの床の段を、`S-243` で数え直した。帯の床（`HF-19`）は外さない。読んだ木は `1fe521ca`。
> ⚠️ 8 本（`CR-409`〜`CR-416`）は 1 つの計画で 1 度に当てた（`CR-409` の第 3 節）。
> ⚠️ `src/`・`tests/`・`tools/` は触っていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（`CH-2` の受ける目標は `GL-002`）—— タスクが 1 本だけの行の帯は、行の操作子の格子（`HF-19`）で高さが決まる。格子を低くすれば、1 画面に入る行が増える。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾がない）** —— `FR-029` の「図形を描く箱の一辺を載る面によって変えてはならない（MUST NOT）」は箱（`S-138`）についての禁止で、隙間は含まない ⇒ 行見出しパネルだけ隙間を変えても両立する。⭐ `FR-029` の新しい文に「箱と枠の線は面によって変えない、変わるのは隙間だけ」と添えた。
- **`R1.3`** —— `JDG-149` の読みは「`S-141` の注「枠の側は動かさない」と `HF-19` の「格子の側を縮めて合わせてはならない」を改める」と書く。⭐ `S-141` の注は「定めるのは隙間だけで、図形の箱は動かさない。隙間を詰めれば枠が小さくなる」に書き直した。⭐ `HF-19` の MUST NOT（帯に合わせて格子を縮めない）は残し、隙間を `S-243` として狭くするのは帯に合わせることではない、と注を足した（決定 3）。
- **`R1.2`（非検証な表現を排す）** —— 利用者の「約 24px」を、`LF-3` の数え方（入口 1 つの縦 ＝ `S-138` と隙間、格子 ＝ その 2 段、`S-235` を掛ける）で数え直し、`S-243` の注に式を書いた（第 2 節）。
- **`R1.3`** —— `FR-039` の行見出しパネルの床の段（深さ 5 の掴み代が操作子に覆われない条件）と `S-79` の注が、外形 26px（隙間 4）で数えていた ⇒ 20px（隙間 1）と新しい描く比（`CR-409`）で数え直した。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 隙間を 表 T-206 の新しい行 `S-243`（1px、保存しない）に置き、`S-141`（4px）は `App Header`・`Command Palette`・ヘルプ・プロパティパネルほか、ほかの面の入口に残す | ⭐ `JDG-149` の読み「ヘッダーとコマンドパレットは `S-141` の 4 のまま ⇒ 行見出しパネル用の値を別の設定行に立てる」 | 無し |
| 決定 2 | 対象を「`Row Title Panel` に載る入口」（行の操作子の格子 `HF-1` と、`HF-4` の対とピン止め、パネルの頭の `HF-10` の入口）とする | ⭐ 二通目「行見出しパネルだけを詰めろ」。⭐ 表 T-109 で面が `Row Title Panel` の行 | パネルの頭の入口も小さくなる |
| 決定 3 | `HF-19` の「格子の側を縮めて合わせてはならない（MUST NOT）」を消さず、「隙間を `S-243` とするのは帯に合わせて縮めることではない」と注を足す | ⭐ 問 1 への答え「① 下限を守るとせよ」—— 帯が格子を下回らないことは変わらない。⭐ MUST NOT が防ぐのは、帯の高さに応じて格子を動かすことである | 無し |
| 決定 4 | 帯の床の数を、`LF-3` のとおり入口 1 つの縦を `S-138` と隙間で数えて 2 ×（16 ＋ 1 × 2）× 0.6667 ＝ 24.0012px と書く。枠の線（`S-237`）は縦に数えない | ⭐ `LF-3`「操作子 1 つの外形は `S-138` と `S-141` が決めており、格子はその 2 段ぶん」（`S-237` を名指さない）。⭐ `src/entity/layout-engine/screen-regions/screen-regions.ts:80`〜`:83` も縦を `S-138` ＋ 隙間 × 2 で数え、`S-237` を幅にだけ足す（`:85`〜`:88`）。⭐ 利用者の「約 24px」と一致する | 無し |
| 決定 5 | `HF-5` / `HF-19` / `LF-3` の「`S-138` と `S-141`」を「`S-138` と `S-243`」に替える | ⭐ 3 つとも行見出しパネルの操作子を述べる | 無し |
| 決定 6 | 表 T-252 の `DS-7`（掛けない）に `S-243` を足す | ⭐ `S-141` と同じ扱い（入口の隙間には表示の倍率を掛けず、`S-235` を掛ける）| 無し |
| 決定 7 | `S-79` の注の「操作子の左端は 幅 − 104、深さ 5 では 幅 ≧ 200」を、外形 × 4 の式に書き直し、本値 200 は外形 26px から決めたこと、いまの外形 20px では 176 で足りることを書く。既定 200 は変えない | ⭐ 104 ＝ 26 × 4 は `S-141` の 4 から出た数。⛔ 既定の値は指示に無い | 無し |
| 決定 8 | `FR-029` に代償（押せる広さも狭くなる）を書き、`S-243` の注はそれを指す | ⭐ `JDG-149` の読み「代償: 押せる広さも同じだけ狭くなる（dist を焼いたあと利用者が手で押して確かめる）」。⭐ 2 か所に同じ文を置くと重複検出器（検査 11）が新しい群にした（実測）| 無し |

---

## 1. 読んで確かめた事実

| 事実 | 所（`1fe521ca`） |
|---|---|
| `S-141` ＝ 4px（注「⛔ 枠の側は動かさない —— 定めるのは隙間だけである」）| `docs/spec/_source/settings.json:2764`〜`:2775`（当てる前）|
| `FR-029`: 外形の幅 ＝ `S-138` ＋（`S-141` ＋ `S-237`）× 2。外形の縦は定めない。どの面でも `S-138` / `S-141` / `S-237` に `S-235` を掛ける | `docs/spec/01-04-requirements.md:5821`〜`:5827`（当てる前）|
| `HF-19`: 格子が縦に取る高さは帯高の下限（MUST）、格子の側を縮めて合わせてはならない（MUST NOT）、外形は `S-138` と `S-141` が決める | 同 `:1695`（当てる前）|
| `LF-3`: 同じく「`S-138` と `S-141` が決めており、格子はその 2 段ぶん」 | `docs/spec/05-07-design.md:1013`（当てる前）|
| コード: 縦 ＝ `S-138` ＋ `S-141` × 2、幅 ＝ 縦 ＋ `S-237` × 2、床 ＝ 縦 × `S-235` × 2 | `src/entity/layout-engine/screen-regions/screen-regions.ts:80`〜`:100`（読むだけ）|
| `FR-039` の床の段: 16 × 5 × 描く比 ＋ 10.67 ≦ 200 × 描く比 − 69.33（外形 26px の 4 列 × `S-235`）| `docs/spec/01-04-requirements.md:6294`（当てる前）|
| `S-243` は `docs/spec`・`src/`・`tests/`・`tools/` を引いて 0 件 | `grep -rn`（当てる前）|

## 2. 測った

**帯の床**（`HF-19`、`LF-3` の数え方、`S-235` ＝ 0.6667）:

| 隙間 | 式 | 床 |
|---|---|--:|
| `S-141` 4（当てる前）| 2 ×（16 ＋ 4 × 2）× 0.6667 | 32.0016px |
| `S-243` 1（本書のあと）| 2 ×（16 ＋ 1 × 2）× 0.6667 | 24.0012px |

- ⭐ 利用者の「約 24px」と一致する。
- ⚠️ 外形の幅（`FR-029`、枠の線を含む）は 16 ＋（1 ＋ 1）× 2 ＝ 20px、描くと 20 × 0.6667 ＝ 13.334px。

**`FR-039` の床の段（深さ 5 の掴み代が操作子に覆われない条件）**: 16 × 5 × 描く比 ＋ 16 × 0.6667 ≦ 200 × 描く比 − 20 × 4 × 0.6667。

| 描く比 | 左辺 | 右辺 | |
|--:|--:|--:|---|
| 0.5（表示の倍率 100、既定）| 50.6672 | 46.664 | 床が無ければ 4.0032px 覆われる ⇒ `FR-039` の床（80 × 0.5 ＋ 10.6672 ＋ 53.336 ＝ 104.0032px ＞ 200 × 0.5）が効く |
| 0.53336（等号）| 53.336 | 53.336 | 120 × 描く比 ＝ 64.0032 |
| 0.55（110）| 54.6672 | 56.664 | 床が無くても覆われない（床 108.0032px ＜ 200 × 0.55 ＝ 110px）|

- 当てる前（外形 26px）は描く比 0.6667 でだけ等号で、新しい既定（0.5）では 50.6672 対 30.6632（100 − 26 × 4 × 0.6667）で 20.004px 覆われていた。

## 3. グラフ

**当てる前**: `impact.py S-141` —— 指す要求 3（`FR-004` / `FR-029` / `FR-039`）、参照 11。`S-138` —— 指す要求 5、参照 35。`S-237` —— 指す要求 2、参照 8。`HF-19` —— 指す要求 0、参照 2（`05-07-design.md:453` の `PI-35`、`S-12` の注）。
**`induced.py`**: `CR-409` の第 3 節と同じ実行。`FR-029` `HF-5` `HF-19` `LF-3` `S-138` `S-141` `S-235` `S-237` `S-79` `FR-039` が 19 員の閉路に入る ⇒ 8 本を 1 度に当てた。
**届いた行を `rulings.md` で引いた**（`S-141` / `HF-19` / `FR-029` / `S-79`）: `JDG-93` / `JDG-108`（行と行の隙間 `S-12`。変えない）、`JDG-131`（行見出しパネルの幅と字下げに表示の倍率を掛ける。`FR-039` の床の式は変えていない）、`JDG-76`（凡例）。衝突 0。

## 4. 当てた中身

| 対象 | 置き場（当てたあと） | 中身 |
|---|---|---|
| `FR-029` の 3 文 | `docs/spec/01-04-requirements.md:5851`〜`:5853` | 行見出しパネルに載る入口では隙間を `S-243` とする（MUST）／ 箱と枠の線は面によって変えない ／ 代償 |
| `FR-029` の外形の幅の式 | `:5855` | 隙間（`S-141`、行見出しパネルでは `S-243`）|
| `FR-029` の `S-235` の文 | `:5858` | 隙間（`S-141` / `S-243`）|
| 表 T-051 の `HF-5` | `:1696` | 大きさの在り処は `S-138` と `S-243` |
| 表 T-051 の `HF-19` | `:1709` | MUST NOT の後に「`S-243` は帯に合わせて縮めることではない」、外形は `S-138` と `S-243` |
| 表 T-252 の `DS-7` | `:6335` | `S-243` を足した |
| `FR-039` の床の段 | `:6343` | 第 2 節の式と数 |
| `LF-3` | `docs/spec/05-07-design.md:1013` | 外形は `S-138` と `S-243` |
| `S-243` | `docs/spec/_source/settings.json`（→ `_assets/tbl-settings.md:365`）| 1px。注に床の式 24.0012px（`S-141` なら 32.0016px）、`S-237` を縦に数えない理由、代償は `FR-029`、`S-141` と兼ねない、`S-235` を掛け表示の倍率を掛けない |
| `S-141` の注 | 同（→ `:314`）| 定めるのは隙間、箱は動かさない、隙間を詰めれば枠が小さくなる。行見出しパネルは `S-243` |
| `S-237` の注 | 同（→ `:315`）| `S-141`（行見出しパネルでは `S-243`）の隙間の外側 |
| `S-79` の注 | 同（→ `:193`）| 幅 −（外形 × 4）、200 は外形 26px から、いまの 20px では 176 |

## 5. 数の予測

本書の差: tables 0 ／ figures 0 ／ **rows +1**（`S-243`）／ uids 0。8 本の合計は `CR-409` の第 5 節。
⭐ 本書が足した `（MUST）` は 1（`FR-029` の文）、`（MUST NOT）` は 0。

## 6. 影響

### 6.1 仕様

| 所 | 本書で |
|---|---|
| `FR-085` の名前に使える幅 | 式は変わらない —— 操作子の場所は `S-140`（0）で、重ねて描く |
| `FR-039` の行見出しパネルの床の式（`S-37` × 描く比 × `S-125` ＋ `S-138` × `S-235` ＋ 外形の幅 × `S-235` × 4）| 式は変わらない —— 外形の幅が `FR-029` から `S-243` で求まる |
| `FR-004`（`S-141` を指す）| そのまま |
| `05-07-design.md` の 表 T-064 の `PI-35`（`rowControlLatticeFloorPx`）| そのまま —— 算式は `LF-3` を指す |

### 6.2 記録

| 所 | 書いたこと |
|---|---|
| `docs/development-records/rulings.md` の `JDG-149` | 状態を 適用済 へ、着地先に本書と行 ID を足した |
| `docs/development-records/changelog.md` | 本書の 1 行 |

### 6.3 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `npm run gen` | `S-243` を読む側へ刷る（`NOT_STORED_ENTRANCE_SIZES` か新しい群。規則 03 の一覧）|
| `src/entity/layout-engine/screen-regions/screen-regions.ts:80`〜`:100` | 行の操作子の縦・幅・格子の床を `S-243` で数える（`rowControlLatticeFloorPx` が 32.0016 → 24.0012、`drawnRowTitlePanelWidthPx` の外形が 26 → 20）|
| 行見出しパネルの入口を描く所（`src/adapter/screen-renderer/row-title-panel.ts`、`src/framework/dom-screen-surface/`）| 隙間 `S-243` × `S-235`。ほかの面は `S-141` × `S-235` のまま |
| `tests/integration/schedule-drawing.sws.test.ts`、`tests/unit/fr-055-fit-reaches-deep-tiers.test.ts`、`tests/unit/fr-055-vertical-lod-fit.test.ts`、`tests/unit/layout-engine.test.ts` | 注が `HF-19` / `LF-3` の旧い文（「`S-138` と `S-141` が決めており」）を引く ⇒ 検査 42 が数える。新しい文へ |

## 7. 試験が主張すべきこと

⭐ 名前は本書のとおりに写すこと: `S-243`（1px）、`FR-029` の「行見出しパネルに載る入口では、その隙間を同表の `S-243` とすること（MUST）」。

1. `S-243` / `HF-19`: タスクを持たない行の帯の高さが 24.0012px 以上で、行の操作子の格子の高さが 2 ×（16 ＋ 1 × 2）× 0.6667。⭐ 古い実装（32.0016px）を赤にする。
2. `FR-029`: 行見出しパネルの入口の図形の箱は 16 × 0.6667 ＝ 10.6672px のまま（`App Header` の入口と同じ）、外形の幅は 20 × 0.6667 ＝ 13.334px。`App Header` の入口の外形の幅は 26 × 0.6667 ＝ 17.3342px のまま。
3. `FR-039` の床: 表示の倍率 100 で、行見出しパネルの描く幅の床が `S-37` × 0.5 × `S-125` ＋ 10.6672 ＋ 13.334 × 4。
4. `HF-19`: 表示の倍率や行の軸のズームを変えても、格子の高さは変わらない。

## 8. 関係 —— `JDG-149`

- ⭐ **`JDG-149` を着地させる** —— 「グリフのサイズはそのままにして、枠を小さく」は `FR-029` の新しい 3 文と `S-141` の注、「① 下限を守る」は `HF-19`（MUST と MUST NOT を残した）、「行見出しパネルだけを詰めろ。 4 → 1」は `S-243`、「約 24px」は `S-243` の注の 24.0012px。

## 9. ⛔ この変更でやらないこと ／ 返す問い

- ⛔ `S-141`（ほかの面の入口）と `S-138`（図形の箱）を変えない。
- ⛔ `HF-19` の床を外さない。
- ⛔ `src/`・`tests/`・`tools/` を編集しない（第 6.3 節）。

**返す問い**: 無し。⚠️ 既定の表示の倍率 100 では `S-79` × 描く比（100px）より `FR-039` の床（104.0032px）が広く、行見出しパネルは床の幅で描かれる —— `S-79` を選び直すかは dist を見てから利用者が決める。
