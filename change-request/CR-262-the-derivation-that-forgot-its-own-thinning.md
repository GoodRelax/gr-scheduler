# CR-262 — 間引きを忘れた導出が、日の段を遠ざけていた

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-26）** —— 「`S-85` を 30 から 8 前後へ下げろ。」
> ⭐ **裁定待ちの行は `PD-196`**（分類 `A`、`裁定済`）。

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`（1 画面で全体を読める）。受ける目標は `GL-002`。**
⛔ **日と曜日の段へ入るのに `zoomX` ≧ 5.83（ホイール約 19 ノッチ）を要していた。**
⚠️ **読める段が事実上 3 段しか無い状態であり、4 段目は在っても届かなかった。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `S-85` の導出 | ⛔ **導出が `1 × ppd ≧ 30` を条件にしている。⭐ これは「毎日に 1 つラベルを置く」という前提であり、`LF-1` がその前提を取り除いている** |
| **`R2.19`**（驚かせない）| 4 段目の到達性 | ⛔ **既定の `zoomX` = 1 では 6 px/day。19 ノッチ回して初めて日が出る。⚠️ 出ないものは無いものと同じである** |
| **`R3.4`**（同じ算術を 2 か所に書かない）| 間引きの式 | ⭐ **`LF-1` が既に持ち、`tickStrideOf` として実装されている。しきい値の側で幅を確保し直す必要が無い** |
| **`R2.20`**（届く）| 段 2・段 3 の導出 | ⭐ **こちらは正しい** —— 除数が `30.4`（月）・`7`（週）であり、**1 ラベルが 1 周期ぶんの幅を占められる**ことを条件にしている。⛔ **段 4 だけが除数 1 である** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`8` ちょうどを採る**（「8 前後」の裁定に対して）| ⛔ **発明していない。** 参考実装の逐語の定数である —— `Ae(e){let t=k(e);return t<1?"year":t<8?"year-month":"month-day"}`。⚠️ **`8` は `S-84`（4.3）と `60` のあいだにあり、行自身の min／max を 1 つも動かさない** |
| 2 | ⭐ **導出表の段 4 の行を書き換え、条件を「間引きが効くので幅の制約にならない」と改める** | ⛔ **古い条件を残すと、次に読む者が「30 が正しい」と読み直す。**⚠️ **`1.12` の教訓（引き金を変えると名簿が 2 か所動く）と同じ形である** |
| 3 | ⛔ **`S-83`（1.4）と `S-84`（4.3）は 1 文字も動かさない** | ⭐ **両者の導出は間引きを前提にしていない場所で正しい** —— 年・月・週の段は `FR-017` が間引きを禁じており、除数が周期そのものである |
| 4 | ⛔ **段の数（4 段）も、判定式も、帯の高さの規則も動かさない** | ⚠️ **参考実装は 3 段・フォント補正なし・帯の高さが段で動く。⭐ 3 つとも仕様が勝つ**（`FR-017` の MUST）。**採るのは定数 1 つだけである** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| `S-85` を指している箇所 | ⭐ **要求 1 件 / 参照 2 箇所**（`FR-017` と 用語集 §4）。`impact.py` |
| 誘導部分グラフの閉路（`S-85` ＋ `FR-017`）| ⛔ **無し**（`induced.py`。種 2 / 内部の辺 1 / 閉路 0）|
| `S-85` の範囲 | min = `rulerTierPxPerDayWeek`（4.3）／ max = 60。⭐ **8 は範囲の内側であり、範囲は動かさなくてよい** |
| 導出表の段 4 | `\| 4 ＋ 日曜 \| 05 月 \| 5 \| 30px \| 1 × ppd ≧ 30 \| 30 \| 30 \|` —— ⛔ **除数が 1 である** |
| 導出表の段 2・段 3 | 除数が `30.4` と `7`。⭐ **1 周期に 1 ラベルなので幅の制約が成り立つ** |
| `LF-1` | 「ラベルの概算幅（`FR-093`）に `rulerLabelGap` を加えた占有幅を、1 日あたりの表示幅で割り、切り上げた日数ごとに 1 つを残す」 |
| `LF-1` の実装 | ⭐ **在る。** `tickStrideOf`（`PI-5` のメンバ。`CR-256` が載せた）。⛔ **日の段だけに掛かっており、年・月・週には掛かっていない** —— `FR-017` の MUST NOT どおり |
| ⇒ 帰結 | ⭐ **日の段では、px/day が小さいほどラベルが疎になるだけで、重ならない。**⛔ **したがって `1 × ppd ≧ 30` は満たす必要の無い条件であった** |
| 参考実装の同じ定数 | ⭐ **8 ちょうど**。⚠️ **かつ参考実装は間引きを `22` と `30` で行っており、その 2 数は `LF-1` を評価した値そのものである**（`rulerFont` 14・`labelCoef` 0.5・`rulerLabelGap` 8 で 2 単位 → 22、3 単位 → 29）|
| 既定の `zoomX` での px/day | ⭐ **6**（`S-1` = 6、`S-75` = 1）。⛔ **30 では 4 段目に入るのに 5 倍の拡大が要る。8 なら 1.33 倍** |

⚠️ **`S-85` の既定値 30 は「実装時に置いて動いたので残った値」ではない。⭐ 導出が記録されている。**
⛔ **その導出の前提だけが、同じ仕様書の別の場所（`LF-1`）と矛盾していた。**

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `_source/settings.json` の `S-85` の `default` | `30` → `8` |
| 2 | `_source/settings.json` の `S-85` の `note` | 間引き（`LF-1`）が効くので、この段では幅が制約にならないことを 1 文足す |
| 3 | `_source/settings.json` の導出の散文（`S-83`〜`S-85` の前置き）| 「3 値とも」を「年・月・週の 2 つのしきい値は」に改める。⛔ **段 4 は別の論であることを明示する** |
| 4 | `_source/settings.json` の導出表の段 4 の行 | 条件の欄を、幅の式ではなく「`LF-1` が間引くので幅の制約は立たない。参考実装の実測値を採る」に改める |
| 5 | `A-appendix` | 変更履歴に 1 行（`1.14`）|

⛔ **`FR-017` は 1 文字も動かさない。** ⭐ **同要求は「しきい値の 3 本は固定値とし、実行時に導出してはならない（MUST NOT）。実装が値を書き換えてはならない（MUST NOT）」と定めており、本変更はまさにその定めに従って原稿の側を動かしている。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1698 | **1698** | 0（セルの編集のみ）|
| `uids` | 148 | **148** | 0 |

⭐ **生成で動くのは 2 か所** —— `document-settings.ts` の既定値と `grs-document.schema.json` の既定値。
⚠️ **`tbl-settings.md` は `S-85` の行と導出の節が動く。**
