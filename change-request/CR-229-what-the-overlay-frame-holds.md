# CR-229 — 重ねの枠に何を入れるかを、2 本の MUST が別々に答えていた

> **閉じるもの**: ⛔ **表 T-024a の `OP-9` は「読んだものを重ね専用の枠へ入れること（MUST）」と言い、
> `FR-015` は「持つのは重ねて描くのに要るものだけとし（MUST NOT）」と言う。**
> ⛔ **`UID` が 1 つも一致しないファイルを重ねると、前者は「入れろ」、後者は「入れるな」と答える。**
> ⭐ **利用者の裁定（2026-08-23）: 一致するものだけを入れる。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`。受ける目標は `GL-004`。** ⛔ **単一 `.html` で配る以上、文書が何を持つかは配布物の大きさに直結する。**
⚠️ **`FR-015` が 5 列に絞った理由がそれである**（「丸ごと持つと成果物の大きさが倍に近づく」）。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.1`**（矛盾が無い）| `OP-9` と `FR-015` | ⛔ **一致 0 件のとき、2 本の MUST が逆を指す。** ⭐ **どちらも誤ってはおらず、射程が書かれていなかった** —— `OP-9` は「合流させるな・専用の枠へ入れろ」を言い、`FR-015` は「その枠に何を入れるか」を言う |
| **`R1.2`**（曖昧さの排除）| `OP-9` の「入れる」| ⚠️ **「読んだファイル」を指すのか「重ねて描く対象」を指すのかが読めない。** 本 CR で後者に定める |
| **`R2.19`**（実装が仕様に対して真である）| `import-document.ts` | ⚠️ **実装は既に一致するものだけを入れており、注記が「これはこのファイルの決定である」と自ら書いていた。** ⭐ **その決定を仕様へ上げる** |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **1 文を `OP-9` の側に置き、`FR-015` は 1 文字も動かさない** | ⛔ **`FR-015` は既に正しい。** 欠けていたのは `OP-9` の「入れる」の射程であり、**直す側は誤っていた側である** |
| 2 | ⭐ **一致 0 件のときに「重ねを行わなかった」とは言わない** | **枠が空になるだけである。** `FR-015` の「対応するタスクが無い重ねる側のタスクは、描かずに知らせること（MUST）」が全数を人に届けるので、**人は何が起きたかを読める** |

---

## 1. 測った

```
induced.py  T-037 FR-076 FR-038 AG-9a T-035 OP-9 FR-015 T-024a T-230
            T-024 FR-096 T-227 FR-031 T-067
```

| 測ったこと | 実測 |
|---|---|
| 閉路 | ⛔ **1 つ。大きさ 2 —— `FR-015` ↔ `OP-9`** |
| ⭐ 帰結 | **本 CR はその閉路の 2 つを触るので、1 つの計画で 1 度に書く**（規則 02 の 3.）|
| 落ちる試験 | `uf-47-48-choosers` の `IC-73`（`OP-9`）1 件 ＋ `CS-4` 1 件 |

### ⚠️ 試験台が正しく食い違いを言い当てていた

⛔ **`uf-47-48-choosers` は `docs/spec` だけを読んで書かれており、`OP-9` の逐語を引いて「読んだものが枠に入る」を期待している。**
⭐ **その読みは `OP-9` の字面としては正しい。** ⛔ **だから「試験台が古い」ではなく「仕様が 2 つに読める」が正しい判じである**（規則 04 の 1.）。
⚠️ **仕様を先に決め、そのうえで試験台を直す。順番が逆だと、仕様に無い形を試験が承認する。**

## 2. 編集計画

⭐ **閉路の 2 つを 1 度に書く。**

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-024a の `OP-9` | 「合流させず、重ね専用の枠へ入れること（MUST）」の後ろに 1 文 ——「**入れるのは、現在の文書のタスクと `UID` が一致するものだけとする（MUST）**」＋ 理由と、`FR-015` の知らせへの受け渡し |
| 2 | `FR-015` | ⛔ **1 文字も動かさない。** 閉路の相手として読み直したうえで、そのままでよいと判じた |
| 3 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **132** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1614 | **1614** | ⭐ **0** —— 既存のセルへの書き足しである |
| `uids` | 148 | **148** | 0 |
