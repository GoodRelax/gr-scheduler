# CR-228 — 理由で引ける節が辞書に無く、MUST が 2 本果たせないままだった

> **閉じるもの**: ⛔ **`RaisedNotice` は理由の鍵を運ぶが、`FR-038` の辞書には理由で引ける節が 1 つも無い。**
> ⛔ **その結果、表 T-037 の `NT-1`（どの項目がなぜ誤りかを文字で示すこと。色や枠だけで示してはならない）と
> `NT-3a`（次に取れる手段を添えること。失敗したことだけを伝えてはならない）の 2 本の MUST が、
> 実装のどこにも果たされていない。**
> ⭐ **利用者の裁定（2026-08-23）: 新表 1 つに全部＋名簿に予備の 1 行。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`。受ける目標は `GL-006`。** ⛔ **失敗を黙って飲むのは、いま最も人を困らせている振る舞いである** ——
書き込みが拒否されても、ファイルが読めなくても、画面には何も出ない。**値は届いているのに、語が無いために捨てられている。**

⚠️ **実害が測れている** —— `single-html-shell/frame-loop.ts` に **`STOP` の注記が 5 か所**あり、
どれも「知らせに語が無いので、ここから先へ運べない」と書いている。`screen-renderer/notices.ts` にもう 2 か所ある。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.1`**（矛盾が無い）| `NT-1` / `NT-3a` と `FR-038` | ⛔ **前者は「文字で示せ」と MUST で言い、後者は「語は辞書 1 か所に持て」と MUST で言うのに、辞書に理由の節が無い。** どちらかが果たせない状態が仕様の中で閉じていた |
| **`R1.3`**（唯一の正がある）| 理由の語彙 | ⛔ **`FileStoreFaultReason` / `DocumentFileFaultReason` / `PlanRefusal` / `FormatMismatch` の 4 つの型が、それぞれ自分の語彙を持っている。** 仕様書側に全数を持つ表が無く、**どれが人へ届く理由なのかを言える場所が 1 つも無かった** |
| **`R2.18`**（公開する契約が自分で意味を成す）| `RaisedNotice.reason` | ⚠️ **型は `string` であり、何を入れてよいかを言う先が無い。** 本 CR で 表 T-233 の行 ID になる |
| **`R3.4`**（誤りを握り潰さない）| シェルの 5 か所 | ⛔ **握り潰してはいないが、届いてもいない。** `FR-028` が値で返させた拒否が、値のまま捨てられていた |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **鍵は 行 ID（`RS-n`）とし、camelCase の語彙を仕様書へ持ち込まない** | **`Notice.manner` が `NT-1` を運んでいるのと同じ形である。** 行 ID なら仕様書の 1 行を指せる（1.9 の「第 1 列は行 ID とする（MUST）」）|
| 2 | ⭐ **`作法` の欄を持たせ、表 T-037 の行を名指す** | ⛔ **持たせないと、語を書く者が「次の手段が要る行かどうか」を判定できない。** ⚠️ **本欄は 表 T-037 を写さない** —— 行 ID を名乗るだけである |
| 3 | ⭐ **`AG-9a` の「理由の区分」は本 CR の射程外とする** | 機械向けの綴りは `AgentRefusalReason` が既に持っており、**そちらへ本表を当てるのは別の仕事である。** ⚠️ 本表は「人へ届く理由」の全数であって、機械向けの区分の全数ではない |
| 4 | ⭐ **`RS-15`（この理由にまだ行が無い）の作法を `NT-3a` にした** | **落ち先は必ず「次の手段」を持つ必要がある** —— `NT-3a` が最も重い MUST を持つ行だからであり、軽い行に落とすと MUST を満たせない場合が出る |

---

## 1. 測った

```
induced.py  T-037 FR-076 FR-038 AG-9a T-035 OP-9 FR-015 T-024a T-230
            T-024 FR-096 T-227 FR-031 T-067
impact.py   T-037 ／ FR-038
```

| 測ったこと | 実測 |
|---|---|
| 種のうち仕様書に在るもの | 14 / 14 |
| 種の内側の辺 | 9 |
| 閉路 | **1 つ**（`FR-015` ↔ `OP-9`。⭐ **本 CR の射程外であり、CR-229 が 1 計画 1 回で書く**）|
| `T-037` を指す要求 | 1 次 8 件 ／ 2 次 7 件 ／ 要求以外 6 か所 |
| `FR-038` を指す箇所 | 要求 2 件 ／ 参照 13 か所 |
| 辞書の節 | **8**（`icons` / `paletteGroups` / `surfaces` / `notices` / `confirmation` / `confirmationMarks` / `panelHeadings` / `assignments`）|
| ⛔ 理由で引ける節 | **0** |

### ⭐ 理由の全数を、いま画面へ届いていない経路から数え直した

⛔ **引継書は「11 鍵」と書いていたが、根拠が無かった。** ⭐ **公開されている型から数え直すと 14 である:**

| 群 | 出どころ | 数 |
|---|---|---|
| ファイル操作の失敗 | `FileStoreFaultReason` の 4 つ ＋ `DocumentFileFaultReason` の 2 つ。**うち `cancelled` は失敗ではない**（人が選択をやめただけで、`IF-3` はそれを報告させないために分けている）| **5** |
| 書き込みが通らなかった | `PlanRefusal`（表 T-067 の `WS-1` / `WS-2` / `WS-3`）| **5** |
| 開こうとしたファイルが読めない | `FormatMismatch`（表 T-024a の `OP-12`）| **3** |
| 受け付けたうえでの注意 | 表 T-024a の `OP-11` | **1** |
| ⭐ 予備 | 本表 | **1** |
| | | **15 行** |

⚠️ **取り込みの検証（`FR-023`）の拒否は本表に入れない** —— 表 T-220 の行が既に行 ID を持っており、
**そちらは自分の行 ID で引ける。** ⛔ **同じものに 2 つ目の鍵を作らない。**

## 2. 編集計画

⭐ **閉路に入らないので、順に書ける。**

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-076` の本文 | 1 文足す ——「知らせが運ぶ理由は 表 T-233 の行とすること（MUST）。表に無い理由を運んではならない（MUST NOT）」 |
| 2 | 表 T-233（新設）| 15 行。欄は 行 ID / 場面 / 作法 / 正 |
| 3 | Chapter 6.2 の原稿の節 | 名簿の出どころに 表 T-233 を足す |
| 4 | `_source/display-words.json` | `reasons` 節（15 項目 × `text` / `nextStep`）|
| 5 | `tools/generate_display_words.py` | 表 T-233 から名簿を起こし、`reasons` 節を検証して書き出す |
| 6 | A-appendix | 変更履歴に 1 行 |

⛔ **表 T-037 は 1 文字も動かさない** —— 作法の表であり、理由の表ではない。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **133** | **＋1**（表 T-233）|
| `figures` | 11 | **11** | 0 |
| `rows` | 1614 | **1629** | **＋15**（`RS-1` 〜 `RS-15`）|
| `uids` | 148 | **148** | 0 —— 要求も目標も足さない |

⚠️ **辞書の枠は 184 → 214 になる**（15 行 × `text` / `nextStep` の 2 欄 × 1 = 30 枠）。
