# CR-397 — 行の操作子の格子の床を、1 つのユニットが答える

> **閉じるもの**: 表 T-064 が、`ScreenRegions` の公開する名前を 1 つ載せていない。
> - 行の操作子の格子の床（表 T-221 の `LF-3` と 表 T-051 の `HF-19` が持つ縦の高さ）を、`ScheduleLayout` が自前の定数で組み立てている。
> - 床を `ScreenRegions` が答える形にすると、その名前はコンポーネントの境を越えるので、表 T-064 が名乗らねばならない（検査 26b）。
>
> ⭐ **本書は 表 T-064 のセルだけを当てる。** 実装は並走する別の体が当てる。読んだ木は `d9230fa2`。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（全体日程を 1 画面で見られる）—— 床は行の帯高の下限であり、`Row Area` に何行が収まるかを決める。⛔ **いまの実装は床を 48px に固定しており**（`S-235` を掛けていない）、表 T-068 の 2 回目の通しが到達できない（台帳の `DFC-618`）。⭐ 床を答える 1 本が公開面に立てば、床を読む側が式を写さずに問える。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R2`（公開インターフェース）** —— 表 T-064 は 36 コンポーネントの公開する名前の**全数**を名乗る（5.3）。床を `ScreenRegions` が答えるなら、その名前は境を越えるので載せねばならない。⚠️ 検査 26b が両方向で見ている。
- **`R4`（DRY）** —— 床の算式は 表 T-221 の `LF-3` が 1 つだけ持つ。⛔ **`src/` の 2 か所に置くと、片方だけが直されて必ず離れる。** いま `ScheduleLayout` が持つ床は、`S-235` を掛けない別の式になっており、まさにそれが起きている。
- **`R1.2`（実装可能性）** —— `S-235`（`NOT_STORED_CHROME_SCALE`）を Entity の層で持つのは `ScreenRegions` である（CR-396 の第 6 節がその置き場を名指した）。⛔ **`ScheduleLayout` が自分で掛けると、その定数が Entity に 2 つ目の席を持つ。**

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 床を答えるメンバを `ScreenRegions`（`PI-35`）が公開し、名前を `rowControlLatticeFloorPx` とする | ⭐ 床は入口の外形の積み上げであり、外形に `S-235` を掛ける側は既に本コンポーネントである（`drawnSettingsOf`）。⛔ `ScheduleLayout` に持たせると `S-235` が Entity の層に 2 席を持つ。⚠️ CR-396 の第 6 節が、床を Entity で組むなら `screen-regions.ts` が閉路を作らない置き場だと既に書いている | 床を読む `ScheduleLayout` が 1 度 `ScreenRegions` を経由する。⚠️ 層は同じ（`layoutEngine`）なので `LR-2` に反しない |
| 決定 2 | 表 T-064 のセルに**算式を書かない**。⭐ 算式は 表 T-221 の `LF-3`、床であることは 表 T-051 の `HF-19` を指す | 同表の結び「本表は名前と、それが何を担うかだけを持つ」。⭐ `PI-5` の `groupDepthLimit` が同じ形の先例である（式を写さず、持つ表を指す） | 床の数を知りたい読み手が 1 度 `LF-3` を経由する |
| 決定 3 | 縦の算式そのものを 1 文字も動かさない | ⭐ CR-396 の決定 3 が「外形の**縦**は定めない」と決め、同書の問い 1 が縦を別の変更要求へ送った。⛔ **本書はその問いに答えない** —— 答えるなら 表 T-051 と 表 T-221 と `FR-029` の閉路を 1 巡で書く別の変更要求である | 縦の外形が `S-237` を含むかは未決のままである（下の問い 1） |
| 決定 4 | セルに `（MUST）` / `（MUST NOT）` の印を足さない | ⚠️ 検査 39 は、印の直前 28 字が `tests/` に逐語で無い条項を数え、`must-clause-coverage-baseline.txt` を超えると赤にする。⛔ **新しい印は必ず未押さえとして数えられ、基準線を 1 つ超える。** ⭐ 禁止は ⛔ の印と平叙文で書く —— `S-138` の注が同じ形を既に採っている | 機械が押さえる条項が 1 つ増えない |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| 床の算式を持つのは 表 T-221 の `LF-3` である。<br>「操作子 1 つの外形は `S-138` と `S-141` が決めており、格子はその 2 段ぶんである」 | `05-07-design.md` の 表 T-221 |
| 床が行の帯高の下限であることと、格子を縮めてはならないことは 表 T-051 の `HF-19` が持つ | `01-04-requirements.md` の 表 T-051 |
| 外形に `S-235` を掛けることは `S-138` の注が定め、表示の倍率を掛けないことは 表 T-252 の `DS-7` が定める | `_assets/tbl-settings.md` の 表 T-206、`FR-039` |
| ⛔ 縦の外形を定めた条項は無い。<br>`FR-029` は「外形の縦は本要求が定めない」と明言する | `FR-029` |
| 実装は床を 48px に固定しており、`S-235` を掛けていない | `src/entity/layout-engine/schedule-layout/schedule-layout.ts` の `ROW_CONTROL_LATTICE_RANKS` と `rowControlLatticeFloorPx` |
| 床・格子・48px を裁いた行は `JDG-93` と `JDG-108` の 2 件あるが、どちらも**行と行のあいだの隙間**（`rowGap`）についての裁定であり、`CR-384` で着地済みである。<br>⭐ 床そのものを裁いた行は 0 件 | `docs/development-records/rulings.md` を `格子` / `床` / `48px` で引いた |
| 表 T-064 が「名前と、それが何を担うかだけを持つ」ことと、引数・戻り値を `src/` が持つことは同表の結びが定める | `05-07-design.md` の 表 T-064 の直前 |

## 2. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| 当てる前の検査 | **ALL GREEN** | `npm run gen` のあと `.claude/skills/spec-graph-check/check.sh` を通した |
| 表 T-064 が名乗り、`src/` に届いているメンバ | 137 | 検査 26b の実測（`0 known gap(s)`、`new 0`） |
| 足したメンバ | 1（1 行のセル） | `PI-35` に 1 つ。<br>行は増えない |
| 足した `（MUST）` / `（MUST NOT）` | 0 | 決定 4 |

## 3. グラフ

**`impact.py T-064 PI-35 LF-3 HF-19 S-235 S-138`**: 155 行。⭐ `PI-35` を指す箇所は 0 件（その行が指す先だけがある）。`LF-3` は `FR-004` / `FR-016` / `FR-087` / `SWS-2` から指されており、いずれも縦の算式を持たない。

**`induced.py T-064 PI-35 LF-3 HF-19 S-235 S-138 S-141 S-237 FR-029 FR-039 DS-7`**: 11 種すべて解決、辺 33、閉路 1 —— 大きさ 7（`DS-7` `FR-029` `FR-039` `S-138` `S-141` `S-235` `S-237`）。

⭐ **本書はその閉路の員を 1 つも編集しない。** 触るのは閉路の外に立つ `PI-35` のセルだけであり、閉路へは「指す」だけである ⇒ 1 巡で書くべき対象は 1 つしかない。

**届いた行を `rulings.md` で引いた**: 第 1 節のとおり `JDG-93` / `JDG-108` の 2 件で、どちらも `rowGap` について既に着地済み ⇒ 衝突 0。

## 4. 当てた中身

| ID | 置き場 | 中身 |
|---|---|---|
| 表 T-064 の `PI-35` | `05-07-design.md` | 決定 1・2・4 |

## 5. 数の予測

当てる前 `tables=151 figures=11 rows=1962 uids=153`。本書の差: tables 0 ／ figures 0 ／ **rows 0**（表 T-064 は行を増やさず、セルを 1 つ直しただけである）／ uids 0 ⇒ 予測 `tables=151 figures=11 rows=1962 uids=153`。

## 6. 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `src/entity/layout-engine/screen-regions/screen-regions.ts` | `rowControlLatticeFloorPx` を公開エントリから export する。<br>⛔ **これが着地するまで検査 26b は赤い** —— 表 T-064 が名乗る名前が `src/` に無いからである |
| `src/entity/layout-engine/schedule-layout/schedule-layout.ts` | 自前の床（`ROW_CONTROL_LATTICE_RANKS` と 48px 固定）を捨て、`ScreenRegions` に問う（台帳の `DFC-618`） |
| `tests/unit/fr-055-fit-reaches-deep-tiers.test.ts` | 定数 `CONTROL_LATTICE_FLOOR` が `S-235` を欠いている（台帳の `DFC-618` が名指す） |

## 7. 試験が主張すべきこと

1. 表 T-064 の `PI-35` が名乗る名前が、`ScreenRegions` の公開エントリから読める（検査 26b が回す）。
2. 床が、表 T-221 の `LF-3` の算式どおり `S-235` に従う。<br>原稿の `S-235` を動かすと床が動く。
3. 表示の倍率を変えても床が動かない（表 T-252 の `DS-7`）。

## 9. ⛔ この変更でやらないこと ／ 返した問い

- ⚠️ **問い 1（前に立つ者へ）**: **床の 1 段は 24px か 26px か。** ⭐ 仕様の側は 24px である —— 表 T-221 の `LF-3` と 表 T-051 の `HF-19` が「外形は `S-138` と `S-141` が決める」と書き、`FR-029` は縦を定めないと明言し、CR-396 の決定 3 がそれを承知で据え置いた ⇒ `(16 + 4 × 2) × 0.6667 × 2 = 32.0016`。⛔ **ところが台帳の `DFC-618` の裁定は 26px を正とし、`34.67` と書いている** —— 幅の式（`S-237` を含む）を縦へ持ち込んだ読みである。⚠️ **どちらかが偽である。** ⭐ **本書は算式を写さないので、どちらに決まっても 表 T-064 のセルは 1 文字も動かない** —— だから本書は答えを待たずに着地できる。⛔ **答えるのは `FR-029` と 表 T-051 と 表 T-221 を 1 巡で書く別の変更要求である**（CR-396 の問い 1 がまさにそれを送っている）。
- ⛔ 表 T-221 の `LF-3` を動かさない —— 縦の算式は同行が持つ。
- ⛔ 表 T-051 の `HF-19` を動かさない —— 床であることは同行が持つ。
- ⛔ `S-138` / `S-141` / `S-235` / `S-237` の行と `FR-029` / `FR-039` を動かさない —— 第 3 節の閉路の員である。
- ⛔ 変更履歴（`docs/development-records/changelog.md`）に行を足さない —— **本体の持ち場の外である。**<br>⚠️ 手順 02 の 7 は履歴を 1 行足せと定めるので、**前に立つ者が足すこと。**
