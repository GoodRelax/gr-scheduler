# CR-362 — 直ったのに「直っていない」と読める実測注記を、直った日ごと書き直す

**台帳の行**: `D-263`（`FR-001` の実測注記が古い）。

⛔ **`induced.py` を先に走らせた**（規則 02）——
**種 `FR-001` `FR-083` `FR-091`：3 of 3・辺 2・閉路 0。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

**`CH-4`（説明を読まずに操作できる）／`GL-006`。**
⭐ **仕様の実測注記は、次に読む者が「まだ壊れている」と判ずる材料である。**
⛔ **直った日を書かない実測は、直った機能を永久に壊れたものとして扱わせる。**

### ② レビュー観点のどの条項を当て、何が出たか

**`R1.3`**（矛盾が無い。唯一の正がある）—— ⭐ **同じ要求の中で、2 つの文が違う現在を述べていた。**
**行の前半は「作ったタスクを選択にすること（MUST）」と命じ、後半の実測注記は
「描いた直後は選ばれておらず、`Delete` を押しても何も消えなかった」と述べている。**
⇒ ⛔ **命令は果たされているのに、注記だけが果たされていない世界を語っていた。**

**`R2.2`**（実測が主張を支えているか）—— ⭐ **支えは在る。日付が古いだけである:**

```
src/framework/single-html-shell/frame-loop.ts
  standOnWhatWasCreated(...) が、作られたものを選択に据える
  呼ばれる場所は 2 つ（作成を伴う命令の枝と、命令の束が created を運ぶ枝）
```

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

1. ⭐ **注記を消さずに、日付を添えて「直った」と書き足す。**
   ⛔ **消すと、なぜこの MUST が置かれたのかが読めなくなる** ——
   **この行は「`FR-083` の RATIONALE が述べているのに、それを求める行がどこにも
   無かった」ことを根拠に置かれている。実測はその根拠の一部である。**

---

## 1. ⭐ 編集計画（**オブジェクトごとに 1 回**）

```
1. FR-001 の実測注記（01-04-requirements.md）
     「実測（2026-09-03、出荷ビルド）: …」のあとに、
     直った日と、直した場所と、まだ押していないことを書き足す
     ⛔ 「作ったタスクを選択にすること（MUST）」の文は 1 文字も動かさない
```

---

## 2. ⭐ 数の予測（**規則 02 の 2.**）

| | 予測 |
|---|--:|
| `tables` / `figures` / `rows` / `uids` | **0** |
| 新しい設定値 | **0** |
| 再生成が要るファイル | **0** |
| 検査 39 の基準線 | **動かない**（MUST を足さない）|

---

## 3. ⛔ 覆したとき落ちる試験

⚠️ **本 CR は散文の日付だけを直すので、落ちる試験は無い。**
⭐ **代わりに残す測り方**: **タスクを 1 つ描き、その直後に `Delete` を押して消えること。**
