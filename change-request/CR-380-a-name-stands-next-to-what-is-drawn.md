# CR-380 — 名前は、描いたものの隣に立つ（最上段の見切れ・マーカーとの重なり・離れすぎ）

> **閉じるもの**: 利用者の申し立て 2026-09-15 の D・I・J —— 逐語は `docs/development-records/rulings.md` の各行。
> - `JDG-86`（D、★）: 矢印のタスクを最上段に作ると、タスク名が見切れて読めない。台帳 `DFC-593`
> - `JDG-91`（I、★）: ステータスマークとタスク名が被る。名前の左にステータスを出す。台帳 `DFC-598`
> - `JDG-92`（J、★）: タスク名が予定や実績とかなり離れた場所に出る。近くに出す。台帳 `DFC-599`
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `87861cf3`。`src/` と `tests/` には触れていない。
> ⭐ 3 件を 1 本にしたのは、3 件とも 表 T-038 と 表 T-243 の結び（名称ラベルの横と縦の置き場）を書き換え、同じ段落の中で互いの数え方を読むからである（`induced.py` の結果は第 2 節）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`（全体日程を 1 画面で見られる）**と **`GL-008`（描いたものを直接掴んで日程を変えられる）**。
名前が見切れる（D）、状態の記号と重なる（I）、どのタスクの名前か分からないほど離れる（J）の 3 つは、どれも「画面で読める」を割っている。

### ② レビュー観点のどの条項を当て、何が出たか

（`docs/development-rules/07-review-standards.md`）

- **`R1.3`（矛盾がない）** —— 表 T-243 の `OR-2`（再開アイコンは並びに従わず `LF-11` の日付位置に立つ）と、同表の結び（並びの中に `OC-4` のぶんを空け続ける）が食い違っていた。結びは「空けたのに何も描かれない場所」と自ら書いていた ⇒ 日付位置に立つアイコンのために場所を空けない、に揃えた。
- **`R1.4`（境界値）** —— 最上段の行（上に行が無い）、形状の中に立つマーカーと形状の外へ出たマーカー、`resume` を持たない中断（表 T-019 の `PA-4`）の 3 つの境目を条項に置いた。
- **`R1.6`（網羅）** —— 表 T-038 は下へずらした実績（`OC-6`）を縦の占有に数えるのに、上へずらした名称ラベルを数える行が無かった ⇒ `OC-10`。
- **`R4` グリッジ（表示の切り替えで位置が動く）** —— `S-63` の切り替えで名称ラベルが動かないこと（`JDG-08` が守っていたもの）は残した。`S-227` / `S-228` でも動かない（決定 6、`JDG-112`）。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**（本書を起こす前）: `JDG-86` / `JDG-91` / `JDG-92`（申し立てそのもの）、`JDG-103`（★ は優先度）。

**問うこと**: なし（0 件）。⭐ 初稿の決定 3 の代償（予定だけ・実績だけの表示で名前が動く）は、利用者の答え `JDG-112`「非表示の計算を非描画ではなく、透明にすればよいではないか？」で消えた（決定 6）。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| 決定 1 | **D は案 ①（ずらしたラベルを帯高に数える）を採る。案 ②（最上段では別の側へずらす）を退ける** | ② は 表 T-012 の結びの MUST「ラベルを上へ、実績を下へ置くことで、予定の線を挟んで三者が重ならない」を割る（下にずらすと実績の線に重なる）。⚠️ さらに `DFC-593` の実測は、1 行下へ置いてもラベルが上の行の帯へ 6.3px 入ることを示している ⇒ 穴は最上段に固有ではなく、② は最上段しか直さない。① は 表 T-038 が `OC-6` で下へずらした実績を縦の占有に数える形と同じであり、新しい設定値も要らない |
| 決定 2 | **ずらしたラベルの高さは名前の有無によらず数える** | `FR-049` が矢印・端点スパンで「実績ぶんの高さを常に確保し、行の高さを変えてはならない —— 見ている場所が動く」と定めている。名前を打つたびに帯高が変わるのは同じ事故である |
| 決定 3 | **J は、形状の外へ出たマーカー（と `PA-4` のときのアイコン）の右端までを空け、形状の中に立つマーカーと、日付位置に立つアイコンには場所を空けない。マーカーの位置は `S-63` によらず「描いたときに立つ位置」で数える** | `JDG-92` は近くに出せ。`DFC-599` の実測では 51.4px のうち 37.44px が描かれていないものの場所だった。`JDG-08`「場所を常に空ける」の理由（`S-63` を切り替えるとタスクの位置が動く事故）は、数える位置を `S-63` に依らせないことで残る。⭐ `S-227` / `S-228` については決定 6。⭐ 退けた案: 再開アイコンのぶん（13.44px）だけ外す —— `OR-2` との食い違いは解けるが、実測の件で 51.4 → 38.0px にしかならない |
| 決定 4 | **I は、形状の中に書くラベルもマーカーの右から書き始める（並びは `OR-1` と同じ）** | `JDG-91` の逐語「タスク名の左にステータスが出るように」。`NL-1` のフェードの規則（「タスクの幅」から引き、`fadeIn` の後から書き始める）と同じ形にした。⚠️ **代償**: 着手済で実績が中ほどまで進んだタスクでは、形状の中のラベルがマーカーの右へ寄り、収まらなければ `NL-3`（形状の右）へ出る。⭐ 退けた案: 重なるときだけ右へ寄せる —— 名前の長さで記号が名前の左にも右にも来ることになり、`JDG-91` の「左に」を満たさない |
| 決定 6 | **隠す ＝ 占有はそのまま・描かない・掴めない。** `S-227` / `S-228` で隠した予定・実績・ダミーは、表 T-038 の算入と名称ラベルの位置（形状の右端・帯高）を描いたときの占有で数え、描かず、当たり判定から外す。マーカーの右端は `S-63` / `S-227` / `S-228` のどの組で立つ位置でも右にあるほうで数える | 利用者の答え `JDG-112`（逐語「非表示の計算を非描画ではなく、透明にすればよいではないか？」）。見た目は透明に描いたときと同じで、表示を切り替えても名前も帯高も段も動かない。マーカーを右にあるほうで数えるのは、`FR-013` が予定だけの表示でマーカーを予定バーへ移すので、どの組でもマーカーが名前に重ならないため（`OR-1` の MUST NOT）。掴めないのは `FR-049` の「隠している要素の掴み領域は残らない」のまま。置き場は `FR-049`（隠したものの扱いの持ち主）、ダミーは 表 T-240 の `DM-14` から同じ扱いを指す |
| 決定 5 | 行 ID は `OC-10`（`OC-9` の次。`grep` で `docs` `change-request` `src` `tests` に 0 件） | 規則 02 の「番号を振り直さない」 |

---

## 1. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| 最上段の矢印のラベルが帯の外へ出る量 | 18px のうち 14.3px | `DFC-593` の実測（出荷ビルド、1920 × 1080、倍率 1）を原典で読んだ |
| 形状の右端から名称ラベルまで | 51.4px（矢印 60.4px） | `DFC-599` の実測。分解 812 + 4 + 16 + 4 + 13.44 + 8 + 6 = 863.44 が実測 863.4 に一致 |
| そのうち描かれていないものの場所 | 37.44px（`S-23` 4 + `S-22` 16 + `S-23` 4 + 16 ×（0.62 + 0.22）= 13.44） | 同上。`_assets/tbl-settings.md` の `S-22` / `S-23` / `S-26` / `S-27` を読んで積んだ |
| 本書の後の同じ件の距離（予測） | 22px（マーカーが形状の外へ 8px 出る件: 820 − 812 + `S-32` 8 + `S-31` 6）／ 14px（マーカーが形状の中の件: `S-32` 8 + `S-31` 6） | 本書の結びの式に `DFC-599` の座標を当てた。⚠️ `S-31`（ラベルの内側の余白）は実装の配置が足している量であり、本書は変えていない |
| マーカーと形状の中のラベルの重なり | 7 件中 3 件（すべて `NL-1`） | `DFC-598` の実測 |
| 表 T-038 の行 | 9 → 10 | `impact.py T-038` の「行: 9 行」 |

---

## 2. グラフ

**`impact.py T-038 OC-1 OC-3 OC-4 OC-6 T-243 OR-1 OR-2 T-013 NL-1 NL-3 SH-3 S-196 LF-2 LF-11`**（出力 327 行）:

| 種 | 指している要求 / 参照 |
|---|---|
| 表 T-038 | 要求 9（`FR-002` `FR-003` `FR-009` `FR-013` `FR-016` `FR-018` `FR-055` `FR-059` `FR-090`）、2 次 26 |
| `OC-1` | 2 / 5 |
| `OC-3` / `OC-4` | 表 T-243 の結びと `FR-016`・`FR-013` から |
| `OC-6` | `FR-049` から |
| 表 T-243・`OR-1`・`OR-2` | `FR-003` の中だけ |
| 表 T-013・`NL-1`・`NL-3` | `FR-002`・`FR-016`（`MK-13`）・表 T-012 の結び |
| `SH-3` / `S-196` | 表 T-012 の結びと 表 T-206 |
| `LF-2` / `LF-11` | 5.5 と 6.1、表 T-023d の `GR-8` の段 |

**`induced.py`**（本書と `CR-382`〜`CR-385` の対象をまとめて 50 種）: 50 種すべて解決、辺 73、閉路 4。本書に掛かる閉路は無い（4 つは `FR-043 GR-17 GR-9`・`GR-20 HF-15`・`FR-049 S-228`・`FR-016 PI-5` で、それぞれ `CR-382`・`CR-385`・`CR-382`・`CR-381` が 1 度で書く）。

**届いた行を `rulings.md` で引いた**:

| 行 | 裁定 | 読んだ結果 |
|---|---|---|
| `OC-3` / `OC-4` | `JDG-27`・`JDG-92` | `JDG-27`（数える幅は掴みシロの幅）は `OR-3` のまま据置。`JDG-92` は本書の出どころ |
| 表 T-038 の結び | `JDG-08`「場所を常に空ける」 | ⛔ **本書が覆す**（決定 3）。守っていた理由（`S-63` で位置が動かない）は残した |
| `LF-11` | `JDG-10`・`JDG-39` | 再開アイコンは日付位置を正とする（`JDG-10`）を強めた側であり、衝突しない。`JDG-10` の読みの欄にある「場所の予約は残す」は本書で外れる（行は書き換えない。規則 ①） |
| `OR-1`・`NL-1`・`NL-3` | `JDG-91`・`JDG-92` | 出どころ |
| `OC-6`・`SH-3`・`S-196`・`LF-2` | `JDG-86`・`JDG-87` | `JDG-86` は出どころ。⛔ `JDG-87`（E、`DFC-594`）は `JDG-107` で保留 —— 本書は E を扱わず、`DFC-594` を吸収しない |

---

## 3. 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| 表 T-038 に `OC-10` | `01-04-requirements.md` | 上へずらした名称ラベル（`SH-3` / `SH-4`）。横は算入しない。縦の占有としてのみ、ラベルが縦に取る高さと `S-196` を予定の上に数え、形状をそのぶん下げて置く（MUST）。名前の有無によらず数える（MUST）。新しい設定値を立てない |
| 表 T-012 の結び | 同 | 量は `S-196` の段に「帯高に数えることは `OC-10` が持つ」を 1 文（指すだけ） |
| 表 T-013 の後の段 | 同 | マーカーの左端が形状の右端より左に立つとき、`NL-1` の「タスクの幅」をマーカーの右端 ＋ `S-32` から形状の右端までとし、そこから書き始める（MUST）。重ねてはならない（MUST NOT）。マーカーの位置は `S-63` によらず数える（MUST）。フェードとは右にあるほうから書き始める（MUST） |
| 表 T-243 の結び | 同 | 名称ラベルの左端 ＝ max（形状の右端、形状の外へ出たマーカーの右端）＋ `S-32`（MUST）。形状の中に立つマーカーのために外に場所を空けない（MUST NOT）。日付位置に立つ再開アイコンのために空けない（MUST NOT）。`PA-4` のときだけアイコンの右端で数える（MUST）。形状の右端は隠したものも描いたときの占有で、マーカーはどの表示の組の位置でも右にあるほうで数える（MUST） |
| `FR-049` の予実の表示の段 | 同 | 隠した予定・実績・ダミーは占有を残し、描かず、当たり判定から外す（MUST）。占有から外してはならない（MUST NOT）（決定 6） |
| `LF-11` | `05-07-design.md` | 「場所の予約は残る」を「並びの中に場所は空けない」へ |
| 表 T-221 の後の注 | 同 | `Task` 1 つの縦の高さの在り処に `OC-10` を足す |

---

## 4. 数の予測

方法: `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py .` の最終行。当てる前（`87861cf3`）は `tables=149  figures=11  rows=1931  uids=153`。

| | 差 | 内訳 |
|---|--:|---|
| tables | 0 | |
| figures | 0 | |
| rows | +1 | `OC-10` |
| uids | 0 | |

⚠️ 同じ波の `CR-382`（`DM-14`、+1）と `CR-385`（`S-231`、+1）を合わせた予測は rows 1931 → 1934。

---

## 5. 影響 —— 本書の外で動くもの

| 所 | 何が動くか |
|---|---|
| `src/entity/layout-engine/schedule-layout/schedule-layout.ts` | `markRoomOf` と `labelX`（一定量を空ける式）、段の縦の置き場（ラベルの高さを足す）、`NL-1` の判定の幅（実装の波） |
| 試験 | 表 T-243 の結びの逐語「描かないときも空けること」を引く試験、`fr-085-room-for-the-name.test.ts`・`t-012-name-label-vertical.test.ts`・`t-038-oc-2-labels-in-the-occupied-width.test.ts` の類は赤くなりうる（実装の波で直す。本書は `tests/` に触れない） |
| `docs/development-records/rulings.md` | `JDG-86` / `JDG-91` / `JDG-92` を `適用済`、`JDG-08` を `覆された` |
| `docs/development-records/defects.md` | `DFC-593` / `DFC-598` / `DFC-599` を `実装待ち` |
| `docs/development-records/changelog.md` | 1 行 |

## 6. ⛔ この変更でやらないこと

- ⛔ `DFC-594`（E、最上段で重なったタスクが見えない）を扱わない —— `JDG-107` で保留
- ⛔ `OC-3` / `OC-4` の「算入してはならない」を変えない
- ⛔ 新しい設定値を立てない
- ⛔ `src/` と `tests/` に触らない

---

## 7. 当て直し（2026-09-15、実装の体の停止を受けて）

> 実装の体 I-1 が本書の条項で 2 か所止まった（`DFC-593` の値の置き場、`DFC-598` の文どうしの矛盾）。読んだ木は `5c6efa34`。`src/` と `tests/` には触れていない。
> ⭐ 利用者は留守中で「迷ったら推奨通りでよい」と言った ⇒ 推奨を採って閉じ、決めた点を 7.2 に並べた（覆してよい）。

### 7.1 止まった所と、確かめた結果

| 止まった所 | 確かめたこと | 結論 |
|---|---|---|
| D: 表 T-038 の `OC-10` が `S-196` を帯高に数えよと言うが、帯高を数えるユニットから `S-196` が読めない | `S-196` は `schedule-geometry.ts` の生成定数にだけ刷られている（`tools/generate_entity_types.py` の `NOT_STORED_LABEL_SIZES` の割り振り）。`components.json` の辺は `ScheduleGeometry` → `ScheduleLayout` の向きであり、逆に読めば `LR-3` の閉路になる。表 T-064 の `PI-5` にも `_assets/tbl-settings.md` の設定値にも無い | 仕様の穴 —— 値を持つコンポーネントと渡す公開エントリが書かれていない |
| D の別件: `labelTopOf` は矢じりの上端から上へずらすが、`OC-10` は「予定の上」に数えると書く | `S-196` の注の逐語は「予定の図形の上端と、ラベルの下端とのあいだの隙間」 | 隙間の基準は図形の上端（いまの `ScheduleGeometry` の読みが正）。`OC-10` は帯に数える基準を名指していなかった |
| I: 表 T-243 の結びはマーカーの右端を「どの表示の組でも右にあるほう」で数え、その組に予定だけの表示の位置（`FR-013`）が入る | 予定だけの表示のマーカーは予定バーの右端の外に立つ ⇒ そう数えたマーカーの左端は形状の右端より左に来ない ⇒ 表 T-013 の後の段の条件は偽のまま。既定の組で形状の中に立つマーカーについて、同じ結びの MUST（右にあるほうで数える）と MUST NOT（外に場所を空けない）がぶつかる | 真の矛盾 —— 数える位置を 2 つに分けて書き直す（決定 9） |

### 7.2 推奨で決めた（覆してよい）

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 7 | `S-196` は `ScheduleLayout` が持ち、表 T-064 の `PI-5` の `NOT_STORED_LABEL_SIZES` で `ScheduleGeometry` へ渡す | 帯高に数えるのが `ScheduleLayout` であり、辺の向き（`ScheduleGeometry` → `ScheduleLayout`）とも合う。`PI-19` の `GROUP_GRID_LINE_WIDTH_PX` と同じ形 | 生成器の割り振り（`tools/generate_entity_types.py`）を実装の波で `schedule-geometry.ts` から `schedule-layout.ts` へ移す。それまで検査 26b は公開した名前が `src/` に無いと読む |
| 決定 8 | `OC-10` は名称ラベルの高さと `S-196` を予定の縦幅の上端の上に数え、ラベルの下端は予定の図形の上端から `S-196` 上に置く | `S-196` の注の逐語が図形の上端である。帯を予定の縦幅で数えれば、`ScheduleLayout` は矢じりと点の式（表 T-221 の `LF-7`）を持たなくてよい。既定の値では図形の上端は予定の縦幅の中にある（導出は 7.3） | 図形の上端と予定の縦幅の上端の差だけ帯に余りが出る（既定・倍率 1 の矢印で 7 − 4.48 = 2.52px）。矢じりや点が予定の縦幅を越える設定の組は数えない（形状そのものも数えていない） |
| 決定 9 | 進捗マーカーは 2 つの位置で数える —— ① 基準の組（`S-227` と `S-228` をともに表示）で立つ位置、② 予定だけの表示で `FR-013` が移す位置。形状の中のラベルは ① が形状の中に立つとき ① の右 ＋ `S-32` から書き始め、外のラベルの左端は 形状の右端・①・② のいちばん右 ＋ `S-32` | 前に立つ者の推奨の方向（基準の組 ① だけで数える）は、予定だけの表示で重なる —— 予定の右端を P とし、実績が P より左で終わるタスクでは、外のラベルの左端が P ＋ `S-32` 8 ＝ P ＋ 8、② のマーカーが P ＋ `S-23` 4 から P ＋ 20 ⇒ 12px 重なる ⇒ `JDG-91`（重ならない）を優先して ② も数える。どの組でもマーカーの位置は ① か ② なので、`JDG-112` の読み「どの表示の組で立つ位置でも右にあるほう」と同じ数え方である。形状の中のラベルに ② は重ならない（ラベルは形状の右端より左で終わる） | 外のラベルは、`PA-4` を除き、形状の右端から常に 34px に立つ（第 1 節の予測 22px / 14px は外れる。7.3）。`JDG-92` の実測 51.4px からは 17.44px 近づくが、マーカー 1 つぶんは常に空く |

### 7.3 数を当て直した

**第 1 節の「22px / 14px」は、決定 6（`JDG-112`）を足す前の式で数えていた。** 決定 9 の式に `DFC-599` の座標を当て直す（値は `_assets/tbl-settings.md` の既定: `S-22` 16、`S-23` 4、`S-26` 0.62、`S-27` 0.22、`S-31` 6、`S-32` 8）。

| 件 | 前の予測 | 当て直し | 導出 |
|---|--:|--:|---|
| マーカーが形状の外へ 8px 出る件（形状の右端 ＝ 予定の右端 812、① の右端 820） | 22px | 34px | ② の右端 812 ＋ 4 ＋ 16 ＝ 832 ＞ 820 ⇒ ラベルの字の始まり 832 ＋ 8 ＋ 6 ＝ 846 ⇒ 846 − 812 |
| マーカーが形状の中の件 | 14px | 34px | ② の右端 ＝ 形状の右端 ＋ 20、同じく ＋ 8 ＋ 6 |
| 実績が予定の右へはみ出した件 | — | 34px | ① の右端 ＝ 実績の右端（＝形状の右端）＋ 20 ＞ ② の右端 |
| `PA-4`（`resume` の無い中断） | — | 51.44px | アイコンの右端 ＝ マーカーの右端 ＋ 4 ＋ 16 ×（0.62 ＋ 0.22）＝ ＋ 17.44 ⇒ 20 ＋ 17.44 ＋ 8 ＋ 6 |

決定 8 の導出（倍率 1 の矢印）: 予定の縦幅 ＝ `S-4` 28 × `S-15` 0.5 ＝ 14px、線の太さ ＝ 14 × `S-40` 0.20 ＝ 2.8px（`S-41` 1.2 〜 `S-42` 4 の内）、矢じりの縦の半分 ＝ 2.8 × `S-45` 3.2 ÷ 2 ＝ 4.48px、端点の点の半径 ＝ 2.8 × `S-47` 1.15 ＝ 3.22px。どちらも縦幅の半分 7px 未満。倍率を上げて線の太さが `S-42` で止まる所（予定の縦幅 20px 以上）でも、矢じりの縦の半分は 4 × 3.2 ÷ 2 ＝ 6.4px で縦幅の半分 10px 以下。倍率を下げても予定の縦幅は `FR-094` の床で 14px に止まる。計算は scratchpad の Python で行った。

### 7.4 グラフ

**`impact.py OC-10 S-196 NL-1 T-243 FR-013 FR-016 PI-5 PI-37 LF-11`**（193 行）:

| 種 | 指している要求 / 参照 |
|---|---|
| `OC-10` | 1 / 2 |
| `S-196` | 2 / 2 |
| `NL-1` | 4 / 12 |
| 表 T-243 | 要求 2（`FR-002` `FR-003`）、2 次 13 |
| `FR-013` | 7 / 18 |
| `FR-016` | 1 / 10 |
| `PI-5` | 1 / 2 |
| `PI-37` | 0 / 0 |
| `LF-11` | 2 / 8 |

**`induced.py`**（上の 9 種に `FR-049` `S-13` `S-36` `S-38` `S-8` `FR-094` を足した 15 種）: 15 種すべて解決、辺 17、閉路 1（`FR-016 ↔ PI-5`、値の行と要求の作法）。2 つとも本節と `CR-381` 第 7 節で同じ 1 回に書いた。
**`rulings.md`**: `JDG-86` / `JDG-91` / `JDG-92` / `JDG-112` は出どころ。`JDG-112` の読みとの衝突は 0（決定 9 の導き）。`LF-11` の `JDG-10` / `JDG-39`、`OR-3` の `JDG-27` は触れていない。

### 7.5 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| 表 T-038 の `OC-10` | `01-04-requirements.md` | 予定の縦幅の上端の上に数える（MUST）。ラベルの下端の基準は予定の図形の上端（MUST）。越える設定の組は数えない。`S-196` の持ち主は 表 T-064 の `PI-5` |
| 表 T-013 の後の段 | 同 | 条件を「基準の組で立つマーカー」とし、表示の組によらず基準の組で数える（MUST）。② は形状の中のラベルに重ならない |
| 表 T-243 の結び | 同 | 左端 ＝ 形状の右端・①・② のいちばん右 ＋ `S-32`（MUST）。2 つを組によらず数える（MUST）。① だけで数えない（MUST NOT）。ほかに場所を空けない（MUST NOT）。`PA-4` は ① ② ともアイコンの右端 |
| 表 T-064 の `PI-5` | `05-07-design.md` | `NOT_STORED_LABEL_SIZES` を足した（`CR-381` 第 7 節の `zoomYAtRectangleLabelFont` と同じ 1 回で書いた） |
| `ScheduleGeometry` → `ScheduleLayout` の辺 | `docs/spec/_source/components.json` | 説明に `S-196` を足し、`build.py --no-figures` で `docs/review/components/components.md` を刷り直した（辺の数・向きは変えていない） |

### 7.6 数の予測

tables 0 / figures 0 / rows 0 / uids 0（セルと段の文だけ）。当てた後の実測は `tables=150  figures=11  rows=1942  uids=153` で、当てる前（`5c6efa34`）と同じ。

---

## 8. 当て直し（2026-09-16、実測を受けて）

> 出荷ビルドの実測（HEAD `178e7369`、1920 × 1080、倍率 1）が `DFC-593` の残りを見つけた。読んだ木は `5bbb2ba4`。`src/` と `tests/` には触れていない（`npm run gen` の生成物だけが動いた）。
> ⭐ 利用者は「すべて推奨通りにやれ。」（`JDG-121`）と答えている ⇒ 推奨を採って閉じ、決めた点を 8.2 に並べた（覆してよい）。

### 8.1 測った・確かめた

| 数・事実 | 値 | 測り方 |
|---|---|---|
| 最上段の矢印の名称ラベルの、描いた `<text>` の箱の上端 | 帯の上端より 0.28px 外（1 行下に置くと上の行の帯へ 0.28px 入る） | 出荷ビルドの実測（前に立つ者） |
| 端点スパンの同じ量 | 0.98px 内側 | 同上 |
| 字のインク | 帯の上端から 4px 内側。見切れは消えている | 同上 |
| 書体を指定しない `<text>` の箱（基線の上 ／ 下 ／ 計） | 12px → 13 ／ 5 ／ 18、12.8px → 14 ／ 6 ／ 20、16px → 17 ／ 7 ／ 24、16.9px → 18 ／ 7 ／ 25、21.12px → 22 ／ 9 ／ 31 | Playwright のヘッドレスのブラウザで `getBBox()` を測った（scratchpad のプローブ `wave6-glyph-box-probe.cjs`）。12px の 18px は前に立つ者の実測と一致 |
| 書体の指定 | 無い —— 名称ラベルの `<text>` は `font-size` だけを書く（`src/adapter/svg-renderer/svg-renderer.ts:295`）。`src/` の `font-family` は `src/framework/dom-screen-surface/dom-screen-surface.ts:415` の等幅 1 か所だけ | grep |
| 字の箱の比の置き場 | 無い —— 表 T-201 と 表 T-206 に行の高さ・字の箱の比の行は無い。`S-33` はベースライン補正、`FR-097` の「1 行の高さは文字の大きさに等しい」はコメントボックスの規則。⇒ 18px を決めているのは木の中ではなく、宿主の既定の書体の上下の張り出しである | `_assets/tbl-settings.md` と `01-04-requirements.md` を grep |
| 当たり判定 | `GR-10` は字の大きさの高さの箱で判じる —— `src/entity/layout-engine/item-hit-area/item-hit-area.ts:204-205` が `task.label` を読み、その高さは `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts:755` の `placed.labelFontSize`。描いた字の箱では判じない。`GR-10` の逐語は「ラベルの描画位置」（`docs/spec/01-04-requirements.md:3230`）で、どちらの箱かを言わない | 読んだ |

⚠️ **前に立つ者の推奨の理由のうち「上の行の当たり判定を奪う」は、いまの実装では起きない** —— 描いた箱の 0.28px は判定の箱の外にある（判定の箱の上端は帯の上端から 2.52px 内側）。起きるのは `GR-10` を描いた箱で判じる読みを採った場合である。
⭐ 本節が閉じる理由は 2 つ: ① `OC-10` 自身の「ラベルは数えた高さの中に収まる」が描いた箱について偽である ② 書体の上の張り出しがもっと大きい宿主では、字のインクが帯を越えうる（最上段では目盛の下に隠れ、ほかの行では上の行の帯へ入る）。

**0.28px の導出**（既定、行の軸の倍率 1 以下の矢印）: 字の大きさ f ＝ 12（`S-8` の床）。予定の縦幅 14px の上端を 0 とすると、図形の上端 ＝ 7 − 4.48 ＝ 2.52（7.3 の導出）、字の大きさの箱の上端 ＝ 2.52 − `S-196` 2 − 12 ＝ −11.48、帯の上端 ＝ −（12 ＋ 2）＝ −14。基線 ＝ 箱の上端 ＋ 12 ÷ 2 ＋ `S-33` 0.35 × 12 ＝ −1.28（`svg-renderer.ts:292` の式）。描いた箱の上端 ＝ −1.28 − 13 ＝ −14.28 ⇒ 帯の上端より 0.28px 外。端点スパンは図形の上端 ＝ 7 − 3.22 ＝ 3.78 なので −13.02 ⇒ 0.98px 内側（実測と一致）。
⚠️ 「上下に 3px ずつはみ出す」は丸めた言い方で、`S-33` のぶん上に 2.8px、下に 3.2px である。
⚠️ 倍率を上げると、線の太さが `S-42` で止まるまで図形の上端と予定の縦幅の上端の差は 2.52 × 倍率で増え、字は床の 12px のままなので、外へ出るのは倍率が 2.8 ÷ 2.52 ＝ 1.111 以下のときだけである。

### 8.2 推奨で決めた（覆してよい）

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 10 | `OC-10` の「名称ラベルが縦に取る高さ」を、描いた字の箱の高さを見積もった値 ＝ 字の大きさ × `S-233` とする（MUST）。字の大きさそのもので数えない（MUST NOT） | 前に立つ者の推奨。`OC-10` の「ラベルは数えた高さの中に収まる」を描いた箱について真にする | 矢印・端点スパンの行の帯が 字の大きさ ×（`S-233` − 1）＝ 6px 高くなる（既定・床の字 12px。字が床を外れた倍率では 0.5 × 字の大きさ） |
| 決定 11 | 比の置き場は 表 T-206 の新しい行 `S-233`（1.5 🔎、保存しない）。表 T-064 の `PI-5` の `NOT_STORED_LABEL_SIZES` が `S-196` と一緒に渡す | 比を持つ行が無い（8.1）。値は宿主の既定の書体が決め、文書は書体を持たないので保存しない。`S-196` と同じ持ち主・同じ定数にすれば辺が増えない。1.5 は 12px と 16px の実測がちょうどその比であり、揺れ（1.46 〜 1.57）の中ほどにある。行 ID は `S-233`（`grep` で `docs` `change-request` `src` `tests` `tools` `.claude/skills` に 0 件。表の最大は `S-232`） | 決定 1 と第 6 節の「新しい設定値を立てない」を、この比についてだけ覆す。生成器の割り振り（`tools/generate_entity_types.py:1238` の `['S-196']`）に `S-233` を足すのは実装の波 —— それまで `PI-5` の文と生成した定数が食い違う |
| 決定 12 | 数えた高さの下端を予定の図形の上端から `S-196` 上に置き（決定 8 のまま。「ラベルの下端」を数えた高さの下端と読む）、字の基線を数えた高さの縦の中央から `S-33` × 字の大きさ下に置く（MUST） | いま `svg-renderer.ts:292` は字の大きさの箱の中央から `S-33` × 字の大きさ下に基線を置く ⇒ 字の大きさの箱を数えた高さの中央に置けば、描く式は変わらない。導出（8.3）で描いた箱が数えた高さに ±0.2px で収まる | ラベルが図形から 字の大きさ ×（`S-233` − 1）÷ 2 ＝ 3px（既定）遠のく。描いた箱の下端は `S-196` の隙間へ 0.2px 入る（隙間は 1.8px 残る）。⭐ 退けた案: 字の大きさの箱の下端を決定 8 のまま置き、帯だけ 6px 高くする —— 実装は 1 行で済むが、描いた箱の下端が図形の上端に 1.2px 重なったままで、「収まる」が下側で偽になる |
| 決定 13 | `GR-10` の当たり判定の箱は変えない（字の大きさの高さの箱のまま）。仕様に「どちらの箱か」を書き足さない | 8.1 のとおり奪いはいま起きず、決定 12 の後も判定の箱は帯の中にある。判定の箱を 1.5 倍にすると、上下の幅を持つ形状の中のラベルでも判定が変わり、本節の外へ波及する | `GR-10` の「描画位置」がどちらの箱かは書かれないまま残る |

### 8.3 数を当てた

値は `_assets/tbl-settings.md` の既定（`S-8` 12、`S-33` 0.35、`S-196` 2、`S-233` 1.5）。予定の縦幅 14px の上端を 0 とする（倍率 1 以下の矢印）。

| 数 | 前 | 後 | 導出 |
|---|--:|--:|---|
| ラベルのぶんの帯高（`OC-10`） | 14px | 20px | 前 12 ＋ 2 ／ 後 12 × 1.5 ＋ 2 |
| 数えた高さ | [−11.48, −0.52]（字の大きさの箱のまま） | [−17.48, 0.52] | 下端 ＝ 図形の上端 2.52 − 2。上端 ＝ 下端 − 18 |
| 字の基線 | −1.28 | −4.28 | 後 ＝ 中央 −8.48 ＋ 0.35 × 12 |
| 描いた箱の上端と帯の上端 | 0.28px 外 | 2.72px 内側 | 後 ＝ −4.28 − 13 ＝ −17.28、帯の上端 −20 |
| 描いた箱の上端と数えた高さの上端 | — | 0.2px 内側 | −17.28 − (−17.48) |
| 描いた箱の下端と図形の上端 | 1.2px 重なる | 1.8px 空く | 前 −1.28 ＋ 5 ＝ 3.72 ＞ 2.52 ／ 後 −4.28 ＋ 5 ＝ 0.72 |
| 端点スパンの描いた箱の上端と帯の上端 | 0.98px 内側 | 3.98px 内側 | 図形の上端 3.78 で同じ式 |

⚠️ 字の大きさ 12.8px（比 1.5625）でも、上端は 14 − 0.35 × 12.8 ＝ 9.52 ≦ 9.6（数えた高さの半分）で 0.08px 内側に収まる。計算は scratchpad の Python で行った。

### 8.4 グラフ

**`impact.py OC-10 S-196 GR-10 S-33 FR-094 S-36 S-38 HF-5 PI-37 FR-085`**（当てる前）:

| 種 | 指している要求 / 参照 |
|---|---|
| `OC-10` | 2 / 4（`FR-001`・`FR-016`・5.3・6.1） |
| `S-196` | 3 / 4 |
| `GR-10` | 3 / 5 |
| `S-33` | 1 / 1（表 T-012 の結び） |
| `FR-094` | 4 / 18 |
| `S-36` / `S-38` | 2 / 5（`FR-004` の `HF-5`・`FR-016`・`PI-37`・`S-17`） |

**`induced.py`**（`OC-10` `S-196` `GR-10` `S-33` `FR-094` `S-36` `S-38` `HF-5` `PI-37` `FR-004` `FR-016` `FR-001` `FR-003` `T-206` の 14 種）: 14 種すべて解決、辺 23、閉路 1（`FR-016 ↔ PI-37`、値の行と要求の作法）。本節はその 2 つのどちらも書かない。
**`rulings.md`**: `OC-10` → `JDG-86`（出どころ）。`S-196` / `S-33` / `GR-10` / `PI-5` / 表 T-206 を名指す裁定は 0 件。衝突 0。

**当てた後**: `impact.py S-233` は要求 1 件（`FR-003` の `OC-10`）/ 参照 2 箇所（`OC-10` と `PI-5`）。`induced.py S-233 OC-10 PI-5 S-196 S-33 FR-094 GR-10 T-206 FR-001 FR-016`: 10 種すべて解決、辺 25、閉路 1（`FR-016 OC-10 PI-5 S-233`、大きさ 4）。そのうち本節が書いた 3 つ（`OC-10`・`PI-5`・`S-233`）は 1 本のスクリプトで 1 度に書いた。`FR-016` は書いていない。

### 8.5 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| 表 T-038 の `OC-10` | `01-04-requirements.md` | 高さ ＝ 字の大きさ × `S-233`（MUST）、字の大きさそのもので数えない（MUST NOT）、下端の基準は数えた高さの下端（決定 8 の読み）、字の基線は数えた高さの中央から `S-33` × 字の大きさ下（MUST）、`S-233` を渡すのも `PI-5`、結びの「新しい設定値を立てない」を置き場の理由へ |
| 表 T-206 の `S-233` | `docs/spec/_source/settings.json`（`npm run gen`） | 1.5 🔎。保存しない理由、実測と揺れ、収まる導出、比 1 で 0.28px 出る導出 |
| 表 T-064 の `PI-5` | `05-07-design.md` | `NOT_STORED_LABEL_SIZES` を「`S-196` と `S-233` を刷った定数」に |

### 8.6 数の予測

tables 0 / figures 0 / rows +1（`S-233`）/ uids 0。当てる前（`5bbb2ba4`）は `tables=150  figures=11  rows=1942  uids=153`。

### 8.7 影響 —— 本書の外で動くもの（実装の波。本書は直さない）

| 所 | 何が動くか |
|---|---|
| `tools/generate_entity_types.py:1238` | `NOT_STORED_LABEL_SIZES` の行に `S-233` を足す（`npm run gen` で `schedule-layout.ts` の生成範囲に刷られる） |
| `src/entity/layout-engine/schedule-layout/schedule-layout.ts:299-302`（`labelLiftOf`） | `labelFontSize × S-233 ＋ S-196` |
| `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts:735-747`（`labelTopOf`） | 矢印・端点スパンで、字の大きさの箱を数えた高さの中央に置く: 図形の上端 − `S-196` − 字の大きさ × `S-233` ÷ 2 − 字の大きさ ÷ 2 |
| `src/adapter/svg-renderer/svg-renderer.ts:292` | 変えない（字の大きさの箱の中央 ＋ `S-33` × 字の大きさ） |
| 試験 | `tests/unit/t-012-name-label-vertical.test.ts`・`tests/unit/cr-380-381-384-name-beside-the-shape-and-row-zoom.test.ts`（`S-196` と帯高を数で主張する）など、帯高やラベルの縦位置を数で持つ試験は赤くなりうる。足すもの: 描いた `<text>` の箱の上端が帯の上端を越えないことを出荷ビルドで測る試験 |
| `docs/development-records/defects.md` | `DFC-593` を `実装待ち` のまま、残りの 0.28px を本節へ結ぶ（前に立つ者） |
