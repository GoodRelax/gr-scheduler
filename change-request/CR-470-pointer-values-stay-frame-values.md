# CR-470 — ポインタとツールチップは領域にしない（段 7 の第 5 領域の棚卸しと閉じ方）

> ⭐ **状態: 波 A を当てた（2026-09-21、ブランチ `sm-pointer-a`、`545eae96` の上）。波 B は無い（決定 6）。**
> ⭐ 当てた体が草案から変えたところは、末尾の改訂の記録の 2 以降に並べた。第 0 〜 10 節と付録は草案の文のまま残した。
> 読んだ木: `545eae96`（ブランチ `refactor`）。作業木の変更は `docs/development-records/measurements/stats.jsonl` の 1 つだけで、本書が引く `src/` と `docs/spec/` は動いていない（`git status --short`）。**本書の行番号と数は、断りが無いかぎりこの木で 2026-09-21 に自分で測ったものである。**
> 型板は `CR-460`（棚卸しの測り直し・分類・波・反証・台帳の候補の形）と `CR-450`。名前の規則は `docs/development-rules/07-review-standards.md` の「状態機械（State Machine）」の節（`:296`〜`:343`）と R4.4（`:605`）。
>
> **結論（先に書く）: この領域に状態機械は作らない。** 残る覚えはすべて、座標・時刻・測った寸法（`SF-5`）か、それらとほかの領域の今の状態から毎回導ける値か、シェルの副作用の把手か、DOM へ書いた値の覚えである。要求が名指す離散の状態は「説明を消したこと」（`IN-3`）の 1 つだけで、それは既に画面の値の `tooltipDisplayStateMachine`（`CR-436` の決定 8、原稿 `docs/spec/_source/state-machines.json:1907`〜`:1954`）が持つ。
>
> **閉じるもの**:
> - 計画の記録 4 の移す順の 5 番目「ポインタとツールチップ。毎秒の出来事が最も多いので、移すたびに性能を測る」（`docs/development-records/refactor-plan-report-2026-09-13.md:252`、領域の例 `pointerAt` `isTooltipStanding` `pointerRestingSince` は `:238`）を、「移すものが無い」で閉じる。
> - `CR-450:114` が「ポインタとツールチップの領域が来るまでシェルの関数のまま」と先送りした `pointerShapeAt` ・ `pointerShapeUnder` の置き場を決める（決定 4）。
>
> **識別子**（規則 02 の 2.5。2026-09-21 に `545eae96` で測った）:
> - 本巡の依頼が配った帯は **表 `T-291`、図 `F-037`、ユニット `UF-120`、変更要求 `CR-470`**。`docs` ・ `change-request` ・ `src` ・ `tests` ・ `tools` ・ `.claude` で `git grep -w` して: `T-291` は `CR-450:375` の改訂の記録（捨てた草案の番号の履歴）1 件だけ、`F-037` ・ `UF-120` ・ `CR-470` は 0 件。見出しが定義する表の上端は `T-290`、図は `F-036`（`grep -ohE "\*\*表 T-[0-9]+[a-z]? —"` ・ 図も同じ）。表 T-075 は 86 行で上端 `UF-89`（`grep -c "^| UF-" docs/spec/05-07-design.md`、`SU-3` も 86 ——`05-07-design.md:245`）。変更要求のファイルの上端は `CR-460`。
> - ⭐ **本書は `T-291` ・ `F-037` ・ `UF-120` を取らない**（決定 7）。前に立つ者へ返す。取るのは `CR-470` だけ。
> - 台帳の上端（`docs/development-records/` の全ファイルを引いた）: `DFC-691`、`PND-500`、`JDG-288`。第 7 節の候補を書くなら `DFC-692` から。
> - 行 ID の接頭辞・要求 ID・状態・出来事・状態機械の名は 1 つも立てない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **どれも前へ進めない —— 基盤の作業の「閉じ」である。** 利用者に見える振る舞いを 1 つも変えない（波 A は表 T-249 の 1 行の文だけ）。
守るもの: **`GL-003`（ぬるサク）** —— ポインタの移動では出来事を 1 つも作らない（表 T-249 の `SF-5`、`05-07-design.md:1011`）。本書の結論は、その上限が「0 件」で満たされることを示す（決定 5）。

### ② レビュー観点のどの条項を当て、何が出たか

- **R4 「状態機械」の節 ／ R4.4** —— 新しい名は無い。既存の 4 領域 18 機械の名に触らない。
- **5.5 の原稿の規則**（`05-07-design.md:726`「原稿に載せるのは、要求が名指す状態・出来事・遷移だけとする（MUST）。実装の都合の細部は状態の種類を増やさず…」）—— この領域の覚え 10 のうち、要求が状態として名指すのは `isTooltipDismissed` だけで、既に原稿に在る（第 2 節）。
- **`SF-5`（des:1011）** —— 座標・時刻・寸法はフレームの値。本書はこれを「それらから毎回導ける値」へ広げて当てた（決定 2 で 1 文を足す）。
- **`R5` パフォーマンス** —— この領域のシェルの道は変わらない。移動 1 回の仕事の測りは、ポインタの道を実際に書き換える `CR-436` の波 B2 と `CR-450` の波 B が `LM-19` で行う（決定 6）。

### ③ 利用者に問うこと・**問わずに決めた**こと

**問うこと**: ⭐ **なし（0 件）。** 候補 6 つに打った 3 つの反証は第 6 節に残した。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| 決定 1 | **領域 `pointer` も状態機械も作らない。** 計画の記録 4 の 5 番目は「移すものが無い」で閉じる | 第 2 節の 10 行の分類。要求が名指す離散の状態は `IN-3` の「消せること」だけで、`tooltipDisplayStateMachine`（原稿 `state-machines.json:1907`）が持つ（`CR-436:73` の決定 8）。残りは座標・時刻（`SF-5`）、導ける値、把手、DOM の覚え。5.5 の規則（des:726）は実装の都合の状態を禁じる。計画自身が「ツールチップの待ち時間のような実装の都合まで仕様に載せると、仕様を直さないとコードを直せない」と書く（`refactor-plan-report-2026-09-13.md:260`） |
| 決定 2 | **表 T-249 の `SF-5`（des:1011）に 1 文を足す**:「フレームの値と、ほかの状態機械の今の状態から毎回導ける値（ポインタの形、指しているパーツと当たり、説明が出ているか）も状態機械に入れず、導く関数で求める。」 | 同じ判断を 3 つの変更要求が別々に導き直している —— `CR-436:272`（`isTooltipStanding` は「描いた結果から導く値」として残す）、`CR-450` の決定 5（`previewDocument` は移動のたびに作り直すフレームの値）、本書。表の行へ上げれば、段 7 の残りの領域（名前付け・選択・操作の記録・Agent API）が同じ判断を行 1 つで引ける。⚠️ 文に「部品」を使わない（`SF-5` と同じ「パーツ」） |
| 決定 3 | **「説明が出ている」（`IN-4` の最後の段「出ている説明」）は状態にしない。** フレームの値として、描き手の `tooltipsFromScreenView`（`src/adapter/screen-renderer/tooltips.ts:97`〜`:139`）が休止の時間・ポインタの下のパーツと当たり・`tooltipDisplayState` から導く | 出る条件は「休止 ≥ `S-124`」（`tooltips.ts:106`）で、休止はポインタが 1 画素でも動くたびに始め直す（`frame-loop.ts:4157`〜`:4159`）。`EZ-6` は「ポインタが動いたら消すこと（MUST）」（req:6232）。⇒ 「出ている」を状態にすると、出ているあいだの移動ごとに出来事が要り、`SF-5` に反する。`CR-436:272` が既に「導く値」と分類している |
| 決定 4 | **ポインタの形（`IN-2` ・ `FR-106`）と、ポインタの下の当たりは、シェルの問い合わせのまま残す。** 結線の後は、読む `pressed` ・ `screenState.armed` ・ `dualCursorFollowing` を根の `gesture` ・ `screen` の今の状態から読む。押した時点の形の覚え `pointerShapeOfPress`（`frame-loop.ts:2850`）は、押した点を鍵にしたシェルの覚えのまま置き、身振りの原稿に運ぶ値を足さない | `pointerShapeUnder`（`:2881`〜`:2905`）は押下（`:2887`）・`Dual Cursor`（`:2890`）・構え（`:2891`）とフレームの値だけの関数で、`grabAtPointer`（`:2838`〜`:2848`）も同じ。前の値に依らない。`CR-450:114` の先送りへの答え。⚠️ 純粋な関数として入力の翻訳係へ出すかは段 5 の持ち物で、本書は決めない |
| 決定 5 | **「指しているパーツや当たりが変わったときだけ作る」（`SF-5` の後半）から作る出来事は、いま 0 件である。** 変わったかの比べ（`owesFrame` の `:4109`〜`:4111`）は、フレームを描き直すかというシェルの規則として残す | ポインタの下の値を使う受け手は、すべて既に出来事の運ぶ値で受け取る: 通知を消す鍵 → `notices/noticeDismissPressed` の `reason`（原稿 `:2027`、いまの `frame-loop.ts:4183`〜`:4184`）、`Esc` の段 → `screen/escapePressed` の `rung`（原稿 `:211`、`:4201`〜`:4208`）、境界の押下 → `gesture/pointerPressed` の `pressedOn`（原稿 `:2320`〜`:2353`、いまの `:4171`）。前例 `CR-450:115`（押しているあいだの移動がフレームを負うのはシェルの規則）。`SF-5` の後半は上限であって割り当てではない |
| 決定 6 | **本書に波 B は無い。** ポインタの道のコードを書き換えるのは `CR-436` の波 B2（`isTooltipDismissed` の移し替え、休止の時計を出来事にする、`Esc` の段）と `CR-450` の波 B（`pressed` を読む所）であり、`LM-19` の測りもそこで行う。第 2.2 節の申し送りを両者へ渡す | 計画の記録 13（`refactor-plan-report-2026-09-13.md:519`「パーツや当たりが変わったときだけ作る」）と `RA-8`（des:1126）。`MC-8` は「ポインタの移動が描き直しを起こしている間」を既に測る区間に含む（req:6841）。⛔ **測る前に利用者を呼ぶ**（`RISK-001` の門）は両者の波の入口の条件のまま |
| 決定 7 | **表 `T-291`・図 `F-037`・ユニット `UF-120` を取らない** | 機械が無いので刷る表と図が無く、`step` を持つユニットも無い。⚠️ 取らずに返すことを依頼の帯の持ち主に知らせる（規則 02 の 2.5 —— 他のセッションが使ってよい） |
| 決定 8 | **計画と棚卸しの記録の直しは本書の本文でしない**（前に立つ者の持ち物）: `docs/development-records/handoff-state-machine.md:103` の次の領域の並び「ポインタ → 名前付けと入力欄 → …」から「ポインタ」を外す（または「`CR-470`: 領域なし」と注す）、棚卸しの領域の数（`refactor-stage1-state-inventory-2026-09-13.md:203` の 9）に `pointerShapeOfPress` が無いこと | `CR-460` の第 9 節と同じ扱い |

---

## 1. 測った事実（`545eae96`、2026-09-21）

| 数 | 値 | 測り方 |
|---|---|---|
| `frameLoop` の範囲 | 1799〜4414 行 | `grep -nE "^(export )?(async )?function frameLoop"` と、その後の最初の `^}`（`awk 'NR>1799 && /^}/'`） |
| `frameLoop` が直に持つ `let` | 66（うちこの領域の候補 8: `:1912`〜`:1918` の 7 と `:2850`） | `sed -n '1799,4414p' … \| grep -c "^  let "` |
| 棚卸しとの差 | 棚卸しは A 節 7 ＋ C 節 1 ＋ D 節 1 ＝ 9（`refactor-stage1-state-inventory-2026-09-13.md:203`）。**`pointerShapeOfPress`（`:2850`）は棚卸しの後に `CR-427`（`FR-106` の「押した時点の形を保つ」）が足した** —— 本書は 10 行 | 名前で照合 |
| 原稿の今 | 4 領域・機械 18・出来事 57（画面の値 31 ・通知 5 ・身振り 5 ・ファイル操作と問い 16） | `state-machines.json` を読んで数えた |
| `tooltipDisplayStateMachine` | 状態 `allowed`（初期）・`dismissed`、升 2（`escapePressed` × `allowed` [`isRungTooltip`] → `dismissed`、`pointerRestElapsed` × `dismissed` → `allowed`） | 原稿 `:1907`〜`:1954` |
| 出来事 `screen/pointerRestElapsed` | 源は時間（`EZ-2`）、運ぶ値なし。コードの型は在る（`src/use-case/advance-screen-session/screen-values.ts:235`）が、いまのシェルは送らない（休止の時計は `ask()` だけを呼ぶ ——`frame-loop.ts:2342`〜`:2345`） | `git grep -n pointerRestElapsed src tests` |
| md-checks の最終行 | `tables=179  figures=23  rows=2225  uids=162` | 第 8 節 |

⭐ **棚卸しの行番号は古い仮説として受け、すべて測り直した**（棚卸しの `pointerAt` は `:1561`、いまは `:1912`）。

---

## 2. 棚卸しの測り直しと分類

分類は `SF-5`（des:1011）、`SF-6`（des:1012）、`SF-10`（des:1016）と 5.5 の原稿の規則（des:726）を 1 つずつ当てた。
**状態** ＝ 原稿の機械の状態。**運ぶ値** ＝ 状態か根の運ぶ値。**フレームの値** ＝ シェルに残る座標・寸法・時刻と、それらから毎回導ける値。**外** ＝ この領域の状態ではない（行き先を書く）。
読み書きは `\bname\b` を `frameLoop` の範囲で行ごとに拾い、注釈の行を除いた（スクラッチの `p-uses.py` と `awk`）。`exportScene` の `:2612` は同名の欄（`isTooltipDismissed: false`）なので数えない。

| # | 覚え | 所在（宣言） | 書き手 ／ 読み手 | 分類 | 名指す仕様の行 |
|---|---|---|---|---|---|
| 1 | `pointerAt` | `frame-loop.ts:1912` | 書き: `receiveInput`:4158。読み: `runFrame`:2210 ・ 2212 ・ 2214 ・ 2216 ・ 2238、`receiveInput`:4157 ・ 4297 ・ 4298 ・ 4300 ・ 4304 | **フレームの値**（座標） | `SF-5` des:1011。`CR-436:222` が `pointer` を既にフレームの値とした |
| 2 | `partUnderPointer` | `:1913` | 書き: `receiveInput`:4161〜4162（`screen.surface.readScreenPartAt` —— 宿主が描いた面への問い合わせ）。読み: `runFrame`:2240、`owesFrame`:4109、`receiveInput`:4164 ・ 4166 ・ 4171 ・ 4183 ・ 4184 ・ 4298 ・ 4300 ・ 4315 | **フレームの値** —— 座標と描いた面から毎回導く（決定 2）。前の値は `owesFrame` の比べ（`:4150`）にだけ要る | `PND-141`（`pending-decisions.md:73`、`未裁定`）、`EZ-2` req:6228、`IN-2` req:6802 |
| 3 | `grabUnderPointer` | `:1914` | 書き: `receiveInput`:4298（`grabAtPointer`、入力の種類を問わず毎回）。読み: `runFrame`:2213 ・ 2242 ・ 2243、`owesFrame`:4099 ・ 4111、`receiveInput`:4151 ・ 4300 ・ 4315 | **フレームの値** —— 座標・幾何・`partUnderPointer` ・ `Dual Cursor` から毎回導く（`:2838`〜`:2848`） | 表 T-023d の `HT-1` 〜 `HT-4`（req:3590〜3593）、`EZ-6` req:6232、`FR-013` の「乗っているあいだだけ濃く」 |
| 4 | `isTooltipStanding` | `:1915` | 書き: `runFrame`:2267（`screenView.tooltips.length > 0`）。読み: `owesFrame`:4110、`receiveInput`:4206、公開の `isBrowserDefaultStopped`:4349 → `escapeTarget`（`src/entity/document-model/screen-state/screen-state.ts:139`） | **フレームの値** —— 描いた説明から導く（決定 3）。⚠️ `IN-4` の段「出ている説明」の判定に使う ⇒ 2.2 の申し送り 1 | `IN-3` req:6803、`IN-4` req:6804 |
| 5 | `isTooltipDismissed` | `:1916` | 書き: `receiveInput`:4160（動いたら戻す）・ 4208（`Esc` の段 `tooltip`）。読み: `runFrame`:2245 | **状態** —— 既に `screen` の `tooltipDisplayStateMachine.dismissed`（`CR-436`）。移し替えは `CR-436` の波 B2 の `let` 12 の 1 つ（`CR-436:272`）。⚠️ 戻る契機が今と違う ⇒ 第 7 節の候補 1 | `IN-3` req:6803、`IN-4` req:6804 |
| 6 | `pointerRestingSince` | `:1917` | 書き: `beginPointerRest`:2338。読み: `runFrame`:2143（`pointerRestedMs` を作る） | **フレームの値**（時刻） | `SF-5`、`EZ-2` ・ `EZ-6` の待ち（`S-124`）。`CR-436:222` が `pointerRestedMs` を既にフレームの値とした |
| 7 | `callOffIconHintWait` | `:1918` | 書き: `beginPointerRest`:2340 ・ 2343 ・ 2346。読み: `:2339` | **外** —— 副作用の把手（時計を止める関数）。時計が満ちたことは既存の出来事 `screen/pointerRestElapsed` の源（⇒ 2.2 の申し送り 2） | `SF-6` des:1012。前例 `callOffScaleMessage`（`CR-436:272` が残す） |
| 8 | `pointerShapeOfPress` | `:2850` | 書き・読み: `pointerShapeAt`:2866〜2874 | **フレームの値の覚え** —— 押した点（`pressed.at`）を鍵にした、押した時点の形。押下から導く（決定 4） | `FR-106`、`IN-2` req:6802、`DFC-652`（`defects.md:190`） |
| 9 | `pointerShapeShown` | `src/framework/single-html-shell/single-html-shell.ts:409`（`boot` の閉包） | 書き・読み: `showPointerShape`:410〜413 | **外** —— DOM へ書いた値の覚え（同じ綴りを 2 度書かない） | 実装の都合。棚卸しの「DOM へ書いた値の覚え」の類（`refactor-stage1-state-inventory-2026-09-13.md:212`） |
| 10 | `anchorsByPart` | `src/framework/dom-screen-surface/dom-screen-surface.ts:2375`（閉包） | 書き・読み: `:2379` ・ `:2381` ほか | **外** —— 説明を添える先の要素の覚え（描き手の差分の覚え） | 実装の都合 |

⇒ **見つけた覚え 10。状態 1（既に `CR-436` の原稿に在る）、運ぶ値 0、フレームの値 6（うち導く値 4: `partUnderPointer` ・ `grabUnderPointer` ・ `isTooltipStanding` ・ `pointerShapeOfPress`）、外 3。**
⇒ **新しい状態機械に入る覚えは 0 である。**

### 2.1 領域をまたぐもの（棚卸しの記録 4 を測り直した）

| 読む側 ／ 書く側 | いま | 移した後 |
|---|---|---|
| `owesFrame` が `partUnderPointer` ・ `grabUnderPointer` ・ `isTooltipStanding` を読む | `frame-loop.ts:4109`〜`:4111`（棚卸し `:223`） | **残る** —— フレームを描き直すかはシェルの規則（決定 5、`CR-450:115` と同じ筋） |
| `pointerShapeAt` ・ `grabAtPointer` が `pressed` ・ `dualCursorFollowing` ・ `screenState.armed` を読む | `:2846` ・ `:2866`〜`:2874` ・ `:2887` ・ `:2890` ・ `:2891`（棚卸し `:238`） | シェルの問い合わせのまま、根の `gesture.pointerPressState`（と運ぶ値 `pressRow` ・ `pressedOn`）、`screen.armModeState` ・ `screen.dualCursorModeState` を読む（決定 4）。書き換えは `CR-450` の波 B と `CR-436` の波 B2 |
| `receiveInput` が `partUnderPointer.dividerPanel` を読む | `:4171`（`PND-451`） | 呼び手が `gesture/pointerPressed` の `pressedOn` に詰める（既存の運ぶ値） |
| `receiveInput` が `partUnderPointer.noticeDismissKey` を読む | `:4183`〜`:4184` | 呼び手が `notices/noticeDismissPressed` の `reason` に詰める（既存） |
| `Esc` の段 `'tooltip'` | `escapeLevelOf` を 2 か所で組む（`:4201`〜`:4207` と `:4343`〜`:4349`）、判定は `screen-state.ts:139` | 呼び手が `screen/escapePressed` の `rung` に詰める（`CR-436` の決定 7）。段の状態のキー ⇒ 2.2 の申し送り 1 |
| 休止の時計 | `beginPointerRest`（`:2337`〜`:2347`）が移動ごとに始め直し、満ちたら `ask()` だけ | 満ちたら `screen/pointerRestElapsed` を送る ⇒ 2.2 の申し送り 2 |

### 2.2 申し送り（⛔ 本書は `CR-436` ・ `CR-450` を書き換えない）

| # | 相手 | 中身 |
|---|---|---|
| 1 | `CR-436` の波 B2 の 表 `T-283`（`SD-4`、des:738「段ごとの状態のキー」） | `IN-4` の最後の段「出ている説明」の行: 状態のキーは `screen.tooltipDisplayStateMachine.allowed`、段に当たるかはフレームの値（描いた説明がある —— いまの `isTooltipStanding`、`frame-loop.ts:2267`）から呼び手が決め、`screen/escapePressed{rung: tooltip}` で運ぶ。⭐ 8 段のうち、状態だけでは決まらずフレームの値を要る段がこれであることを表に書く |
| 2 | `CR-436` の波 B2（シェルへの結線） | 休止の時計（`frame-loop.ts:2342`〜`:2345`）が満ちたら `screen/pointerRestElapsed` を根へ送る。`:4160`（動いたら `isTooltipDismissed = false`）は消す。⚠️ 消すと見え方が変わる所がある ⇒ 第 7 節の候補 1（`JDG-57` の手順で、写すか期待どおりの失敗を留めるかは `CR-436` の波 B2 が決める） |
| 3 | `CR-450` の波 B | `CR-450:114` の答え: `pointerShapeAt` ・ `pointerShapeUnder` は本書の領域へは移らない。`pressed` を読む所（`:2866`〜`:2874` ・ `:2887`）を根の `gesture` の今の状態と押した点（フレームの値）に置き換える。`pointerShapeOfPress` はシェルに残す |
| 4 | `CR-436` の波 B2 と `CR-450` の波 B の `LM-19` | 押さずにポインタを動かす区間（説明が出ている ／ 出ていない）を測りに含める。⭐ 計画の記録 4 の「移すたびに性能を測る」の対象は、本書ではなくこの 2 つの波である |

---

## 3. 波の分け方

| 波 | 入口の条件 | 触る所 | 緑を保つ検査 |
|---|---|---|---|
| **A** | なし（いま当ててよい）。段 5・段 6 と並行してよい | `docs/spec/05-07-design.md` の 表 T-249 の `SF-5`（`:1011`）の 1 文（決定 2）。それだけ | `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py .`、strictdoc の書き出し、spec-graph-check の全数を前後で比べる（⚠️ 検査 11 ・ 12 ・ 33 は `FAIL` の行を出さずに赤くなる —— 出力の全体を当てる前の走りと比べる） |
| B | —— **無い**（決定 6） | —— | —— |
| C | —— 第 7 節の候補は台帳の体が行を書き、直すのは `CR-436` の波 B2 の後の別のコミット（`JDG-57`） | —— | —— |

⭐ **波 A は `src/` ・ `tests/` ・ 原稿に触らない。** 表 T-075 ・ `SU-3` ・ `UT-11` ・ `PI-39` ・ `SS-6` ・ 図 F-028 ・ `changelog.md` の版の数は動かない（`CR-460` の 3.1 の表の行はどれも当たらない）。
⭐ **`frameLoop` の `let` は本書では減らない**（66 のまま）。10 行の行き先: `isTooltipDismissed` は `CR-436` の波 B2 が根へ移す。残りの 7 つ（`frameLoop` の中）はシェルに残る。

### 3.1 波 A の直前の測り直し（⛔ 当てる体が打つ）

1. `SF-5` の行番号と文（並行して動く。動いていたら ID で引き直す）。
2. 足す文が「部品」を含まないこと、変更要求の番号を含まないこと（検査 7）。
3. md-checks の最終行が前後で同じこと（第 8 節）。

---

## 4. この結論を覆すもの

- **`IN-3` の「ポインタを乗せられること」を実装すると決まったとき**（`PND-391`、`pending-decisions.md:255`、`未裁定`）—— 説明は「アイコンの上で休んだ」から「説明の箱の上へ移った」まで出続ける必要があり、今のフレームの値だけからは導けない（前に出ていたかを覚える要がある）。そのときは、どの説明が立っているか（添え先）を状態として持ち、指しているパーツが変わったとき（`SF-5` の後半）の出来事で動かす。⭐ **置き場は新しい領域ではなく `screen` の `tooltipDisplayStateMachine` の広げ**である（同じ `IN-3` を 2 つの機械が持たない）。
- **`PND-141`（ポインタがどのアイコンの上か）の裁定がシェル以外を答える者に決めたとき** —— 導く値の置き場が動くだけで、状態にはならない。

---

## 5. 各覚えを状態にしない理由の出どころ（反証の材料）

| 覚え | 状態にすると何が要るか | 出どころ |
|---|---|---|
| `pointerAt` ・ `pointerRestingSince` | 移動ごとの出来事 | `SF-5` の逐語（des:1011） |
| `partUnderPointer` ・ `grabUnderPointer` | 前の値に依る遷移が 1 つも無いのに、パーツの数だけの種類か、運ぶ値だけを書き換える升 | 決定 5 の受け手の一覧。5.5（des:726）「実装の都合の細部は状態の種類を増やさず」 |
| `isTooltipStanding` | 出ているあいだの移動ごとの出来事 | `EZ-6` req:6232、`frame-loop.ts:4157`〜`:4159`、`tooltips.ts:106` |
| `pointerShapeOfPress` | 身振りの状態に運ぶ値 `pointerShape` を足す（`CR-450` の原稿を変える） | 押下から導ける（`:2872`）。`FR-106` は形を保つことを名指し、覚え方を名指さない |

---

## 6. 問いの候補に打った 3 つの反証（`handoff.md` §0.2）

方法: (1) `docs/development-records/rulings.md`（527 行、2026-09-21）を grep、(2) `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <行>`、(3) 仕様・計画・前の裁定・前の変更要求から導けるか。

| 候補 | (1) `rulings.md` | (2) `impact.py` | (3) 導けるか | 結果 |
|---|---|---|---|---|
| ポインタの領域を作るか | 「ツールチップ」0 件、「フレームの値」0 件、`SF-5` 0 件 | `SF-5` → 要求 0 ／ 参照 1 | **導けた** —— 5.5（des:726）と `SF-5`、計画の `:228` ・ `:260`、`CR-436:222` ・ `:272` | 決定 1 |
| 導ける値をフレームの値とする文を `SF-5` へ上げるか | 同上 | `SF-5` → 要求 0 ／ 参照 1（1 文を足しても届く先は 1） | **導けた** —— 3 つの変更要求が同じ判断を導き直している（決定 2 の導き） | 決定 2 |
| 「出ている説明」を状態にして `SD-4` の段のキーにするか | `IN-3` 0 件、`EZ-6` 0 件 | `IN-4` → 要求 9 件 ／ 参照 35、`SD-4` → **浮いている**（要求 0 ／ 参照 0） | **導けた（しない）** —— `EZ-6` の「動いたら消す」と休止の始め直しで、状態にすると移動ごとの出来事が要る。段は `CR-436` の決定 7 のとおり呼び手が `rung` で運ぶ | 決定 3、申し送り 1 |
| ポインタの形を状態（または身振りの運ぶ値）にするか | `IN-2` 8 件・「ポインタの形」5 件（`JDG-114` ・ `JDG-115` ・ `JDG-121` ・ `JDG-176` ・ `JDG-193` ・ `JDG-200` ・ `JDG-208` ・ `JDG-222` ・ `JDG-223` ・ `JDG-279` —— どれも形の絵の話で、置き場の話は 0） | `IN-2` → 要求 2 件 ／ 参照 3 | **導けた** —— `pointerShapeUnder` は今の値だけの関数（`:2881`〜`:2905`）、`CR-450:114` が本書へ先送りした | 決定 4 |
| 説明を消した後に戻す契機（移動か休止か）を問うか | 0 件（上と同じ） | `IN-3` → 要求 4 件 ／ 参照 8、`FR-037` → 要求 5 件 ／ 参照 7 | **導けた（本書では決めない）** —— 機械は `CR-436` の持ち物で、決定 8（`CR-436:73`）が休止と決めた。今との差は 2026-09-13 の裁定どおり台帳へ、写すかは `JDG-57` の手順で `CR-436` の波 B2 | 第 7 節の候補 1 |
| `IN-3` の「乗せられること」をこの機会に作るか | `PND-391` 0 件、「乗せ」0 件 | `IN-3` → 要求 4 件 ／ 参照 8、`EZ-6` → 要求 4 件 ／ 参照 10 | **導けた（作らない）** —— `JDG-57`（移すのが先、直すのは後）、`PND-391` が `未裁定` | 第 4 節、第 7 節の候補 2 |

⇒ **3 つとも空だった候補は 0 件。問いは無い。**

---

## 7. 台帳の候補（⛔ 問いではない。どちらが正かを選ばない）

2026-09-13 の利用者の裁定（仕様とコードの食い違いは、どちらが正かを選ばずに台帳へ）に従い、台帳の体へ渡す。番号は書く直前に `docs/development-records/` の全ファイルを引いて決める（2026-09-21 の上端 `DFC-691`）。

| # | 食い違い | 片側 | もう片側 | 台帳の今 |
|---|---|---|---|---|
| 1 | **説明を消した後に戻す契機が、`CR-436` の機械と今のコードで違う。** 機械は休止が満ちたとき（`pointerRestElapsed` × `dismissed` → `allowed`）、コードはポインタが動いたとき（`frame-loop.ts:4160`）。休止を待たずに出る説明 —— `FR-037` のスクロールバーの説明（`tooltips.ts:131`〜`:137` は `isHintDue` を見ない）—— で見え方が 2 つ変わる: ① 消した後にスクロールバーの上で動かすと、今はすぐ戻り、機械では `S-124` 待ってから戻る。② 休止の時計が満ちる前に消すと、今は消えたまま、機械では時計が満ちて動かずに戻る。⚠️ **未検証（画面で押していない。コードを読んだだけ）**。`CR-436:73` の決定 8 は「見え方は変わらない」と書いている | `CR-436` の決定 8、原稿 `state-machines.json:1944`〜`:1952`、`IN-3` req:6803（「引き金が外れるまで」「消せること」） | `frame-loop.ts:4157`〜`:4160`、`tooltips.ts:102` ・ `:131`〜`:137`、`FR-037`（req:6637 の STATEMENT「乗せたとき … 示し、離れたら消す」） | 0 件（`defects.md` ・ `pending-decisions.md` を `IN-3` ・「ツールチップ」・`isTooltipDismissed` で引いた。`DFC-463` は 表 T-040 の数の話で別）⇒ 新しい行 **`DFC-692`** の候補。`CR-436` の波 B2 の入口で読むこと |
| 2 | **`EZ-2` のアイコンの説明は、説明の箱の上へポインタを運べない疑い** —— 移動のたびに休止を始め直し（`frame-loop.ts:4159`）、出る条件が休止 ≥ `S-124` とアイコンの上であること（`tooltips.ts:106` ・ `:110`）なので、箱へ向けて動いた時点で消える。⚠️ **未検証** | `IN-3` req:6803「ポインタを乗せられること —— 説明そのものの上へポインタを移しても消えないこと」 | `frame-loop.ts:4157`〜`:4159`、`tooltips.ts:106` ・ `:110` | `PND-391`（`未裁定`）が同じ食い違いを `EZ-6` の説明についてだけ書き、アイコンの説明は「従来どおり」とする。⇒ `PND-391` の広げか新しい行（**`DFC-693`**）かは台帳の体が決める |
| 3 | 台帳の古い記述: `PND-153`（`pending-decisions.md:80`、`裁定済`）は「その `Esc` の経路はまだ実装されていない —— 描く側に STOP が立っている」と書くが、いまは実装されている（`frame-loop.ts:4208`、`screen-state.ts:139`）。`tooltips.ts` に `Esc` の STOP は無い（`grep -n STOP`） | —— | —— | 食い違いではない。行の文の直しとして台帳の体へ |

⚠️ 既に在る関係の行（本書は直さない）: `DFC-556`（依存線の構えの形、`frame-loop.ts:2899`）、`DFC-652`（押しているあいだの形）、`PND-141`、`PND-391`、`PND-445`（`IN-2` が名指さない場所の形、`frame-loop.ts:2878`）、`PND-451`（`frame-loop.ts:4169`〜`:4170`）、`PND-360` ・ `PND-351`（`FR-013` の「乗っている」）。

---

## 8. 数の予測

方法: `PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py .` の最終行。
**起草時（`545eae96`、2026-09-21）: `tables=179  figures=23  rows=2225  uids=162`。**
⭐ 本書の差だけを突き合わせる（「前」は並行する変更で動く）。

| | 波 A の差 | 内訳 |
|---|--:|---|
| tables | 0 | 表を足さない（`T-291` を取らない） |
| figures | 0 | 図を足さない（`F-037` を取らない） |
| rows | 0 | `SF-5` の文を広げるだけ。`UF-120` を取らない |
| uids | 0 | 要求を足さない |

**そのほかの数**: 表 T-075 86 のまま、`SU-3` 86 のまま、原稿は 4 領域 ・ 機械 18 ・ 出来事 57 のまま、`frameLoop` の `let` 66 のまま、`requirement-owner-baseline.txt` の `unfilled=` は動かない。

---

## 9. 影響 —— 本書の外で動くもの

| 所 | 何が動くか | いつ |
|---|---|---|
| `CR-436` の波 B2 | 2.2 の申し送り 1 ・ 2 ・ 4、第 7 節の候補 1 を入口で読む | 波 B2 の入口 |
| `CR-450` の波 B | 2.2 の申し送り 3 ・ 4 | 波 B の入口 |
| `docs/development-records/handoff-state-machine.md:103` | 次の領域の並びから「ポインタ」を外す | 前に立つ者 |
| `docs/development-records/refactor-stage1-state-inventory-2026-09-13.md:203` | 領域の数に `pointerShapeOfPress` が無い（第 1 節） | 前に立つ者が判じる |
| `docs/development-records/defects.md` ・ `pending-decisions.md` | 第 7 節の候補 1 〜 3 | 台帳の体 |
| 依頼の番号の帯 | `T-291` ・ `F-037` ・ `UF-120` を返す | 本書を受けた時 |

---

## 10. ⛔ この変更でやらないこと

- ⛔ 状態機械・出来事・領域を足さない。原稿（`state-machines.json`）に触らない
- ⛔ `src/` ・ `tests/` に触らない
- ⛔ `CR-436` ・ `CR-450` の原稿と本文を書き換えない（申し送りだけ）
- ⛔ `IN-3` の「乗せられること」・`PND-391` ・ `PND-445` ・ `PND-141` を決めない（`JDG-57`）
- ⛔ 新しい異常系・準正常系の扱いを設計しない（`JDG-78`）
- ⛔ 第 7 節の候補を本書で直さない
- ⛔ 基準線を体に触らせない

---

## 付録 —— 波 A の 1 文

表 T-249（`docs/spec/05-07-design.md:1011`）`SF-5` の 2 列目:

| | 文 |
|---|---|
| いま | 連続して変わる値（ポインタの座標・寸法・時刻）は状態機械に入れず、フレームの値として持つ。<br>出来事は、押す・掴む・問いに答える、および指しているパーツや当たりが変わったときだけ作る |
| 案 | 連続して変わる値（ポインタの座標・寸法・時刻）は状態機械に入れず、フレームの値として持つ。<br>**フレームの値と、ほかの状態機械の今の状態から毎回導ける値（ポインタの形、指しているパーツと当たり、説明が出ているか）も状態機械に入れず、導く関数で求める。**<br>出来事は、押す・掴む・問いに答える、および指しているパーツや当たりが変わったときだけ作る |

3 列目（出どころ）は変えない。

## 改訂の記録

| # | 日付 | 何を変えたか | 理由 |
|---|---|---|---|
| 1 | 2026-09-21 | 起草（`545eae96`） | —— |
| 2 | 2026-09-21 | 波 A を当てた（`545eae96` の上、ブランチ `sm-pointer-a`）。表 T-249 の `SF-5`（`docs/spec/05-07-design.md:1011`）へ付録の 1 文をそのまま足した。3 列目（出どころ）は動かしていない | 第 3 節・付録 |
| 3 | 2026-09-21 | 第 7 節の候補 1 ・ 2 を `defects.md` に `DFC-692` ・ `DFC-693` として書いた（どちらが正かは選ばない。状態はどちらも `未検討`）。当てる直前に `docs/development-records/` の全ファイルを `DFC-` で引き直し、上端が `DFC-691` のままであることを確かめてから番号を取った | 第 7 節の候補 1 ・ 2 |
| 4 | 2026-09-21 | 候補 3（`PND-153` の古い記述）は直さなかった —— `docs/development-rules/06-pending-decisions.md` を読み切ったが、`裁定済` の行の記述文を後から訂正してよいとする条は無い（同書が定めるのは分類・波・状態遷移の手順だけで、行は消さないとだけ言う）。⇒ 食い違いの記録は本書のこの候補の行に残し、`pending-decisions.md` の該当行は未変更のまま返す | 候補 3。判断の根拠は 06 の全文を読んだ結果 |
| 5 | 2026-09-21 | `docs/development-records/handoff-state-machine.md` の第 3 節: 「次の領域」の並びから「ポインタ」を外し、`✅ ポインタ CR-470` の行を足した。`T-291` ・ `F-037` ・ `UF-120` を使わず返したことをその行に書いた | 決定 7 ・ 決定 8 |
| 6 | 2026-09-21 | 数の予測を実測と突き合わせた: md-checks の最終行は波 A の直前直後で `tables` / `figures` / `rows`（`SF-5` の 1 文を除く）/ `uids` とも動かなかった（第 8 節の予測どおり、差分 0） | 第 8 節 |
