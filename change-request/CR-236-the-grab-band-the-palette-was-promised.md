# CR-236 — 掴めると書いてあるのに、掴む場所が表にも画面にも無かった

> **閉じるもの**: ⛔ **`FR-053` は「作成者がドラッグで動かせるようにすること」と定め、表 T-109 の `IC-53` は
> 「掴んで動かせることを示す。ボタンではない」と書いている。**
> ⛔ **ところが 表 T-023d（掴み領域と優先順位）にパレットの行が無く、`U-43` の「全数と優先順位は表 T-023d が持つ」に対して落ちている。**
> ⛔ **実測: `IC-53` は DOM に存在せず、`commandPaletteAt` は `Row Area` の角に固定されたまま動かす者がいない。**
> ⭐ **利用者の裁定（2026-08-23）: 掴み帯は最優先に置く。場所は覚えない。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`。受ける目標は `GL-001`。** ⛔ **パレットは日程表の上に浮いており、動かせないと下が読めない。**
⚠️ **`FR-053` の RATIONALE がまさにそれを言っている** ——「固定すると、その幅ぶんだけ日程表が狭くなる」。
**動かせない浮遊物は、固定より悪い。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 表 T-023d | ⛔ **`GR-1` 〜 `GR-18` は日程の要素だけで、パレットの掴み帯が無い。** `U-43` が「全数は表 T-023d が持つ」と宣言しているので、**無い＝存在しないことになっていた** |
| **`R2.19`**（実装が仕様に対して真である）| `command-palette.ts` | ⚠️ **`IC-53` は `NOT_BUTTON_ROWS` に入って入口から外れており、代わりに描かれるものが無い。** 押せないのは正しいが、**掴めもしない** |
| **`R1.1`**（矛盾が無い）| `FR-053` と実装 | ⛔ **`commandPaletteAt` を書き換える者が `src/` に 1 人もいない。** ドラッグの MUST が果たされていない |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **行 ID は `GR-19` とし、印刷順では先頭に置く** | ⛔ **番号を振り直さない**（規則 02 の 4.）。⭐ **優先順は印刷順である**と表自身が定めるので、**番号ではなく位置で優先を表す** |
| 2 | ⭐ **掴み帯の高さは 表 T-206 に 1 行足す**（`S-134` と同じ形）| **掴み代は読む人のアクセシビリティに属する**と `S-90` が既に書いており、**値の置き場はそこである。** 分類 A（原稿にあり生成でコードへ届く）|
| 3 | ⭐ **「最小化」は仕様に足さない** | ⛔ **利用者の裁定で「しまう」と同じものと定まった。** `IC-7`（`App Header`）と `SK-14`（`P`）が既に持ち、**実測で両方とも動いている** —— 押すと消え、`P` で戻る |
| 4 | ⭐ **`commandPaletteAt` に行を足さない** | ⛔ **利用者の裁定で「覚えない」。** 起動のたびに `Row Area` の角へ戻る |

---

## 1. 測った

⛔ **成果物を実ブラウザで開いて測った**（`dist/index.html`、1280×800）。

| 測ったこと | 実測 |
|---|---|
| `Command Palette` | 592 × 274（本巡の `CR-235` で直ったばかり）|
| ⛔ `IC-53`（掴み帯）| **DOM に存在しない** |
| `IC-7`（ヘッダーの切替）| ⭐ **`BUTTON` として在り、動く** —— 押すとパレットが消え、`P` で戻る |
| `commandPaletteAt` を書く者 | ⛔ **0**（`frame-loop.ts` が `Row Area` の角を渡すだけ）|
| 表 T-023d の行数 | 18（`GR-1` 〜 `GR-18`）|

⭐ **求められた 3 つのうち 1 つは既に満たされていた。** ⚠️ **引かずに作っていたら、動くものを作り直していた。**

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-023d | `GR-19` を**先頭に** 1 行 —— パレットの上部の帯。掴めば動かす。⭐ **最優先**（下に何が在っても帯が勝つ）|
| 2 | `_source/settings.json` | 掴み帯の高さを 表 T-206 に 1 行。⛔ **生成物は `npm run gen` で作り直す** |
| 3 | A-appendix | 変更履歴に 1 行 |

⛔ **`FR-053` も `IC-53` も `IC-7` も `SK-14` も 1 文字も動かさない** —— どれも既に正しい。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 133 | **133** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1632 | **1634** | **＋2**（`GR-19` と 表 T-206 の 1 行）|
| `uids` | 148 | **148** | 0 |

⚠️ **`npm run gen` で `tbl-settings.md` と、その値を運ぶ生成物が動く。**
