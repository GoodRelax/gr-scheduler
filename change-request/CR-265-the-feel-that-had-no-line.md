# CR-265 — 手応えを決める行が 1 つも無かった

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-27）** ——
> 「レクタングル系タスクやマイルストーンは予実の中心の高さにラベルを並べる。`*----*` 系のタスクは 予実の線と干渉しないようタスクの上にずらしてラベルを配置する。」
> 「タスクを選択できているのか、マウスカーソルの形状が変わらないからアフォーダンスが無くてわからない。」
> 「タスクやマイルストーンをドラッグする際、マウスカーソルにドラッグ中のタスクやマイルストーンが追従しなくてイライラする。移動も幅変更もどちらもマウスカーソルに追従させろ。ただし、追従中にファイルを保存する必要はない。」
> 「プロパティーパネルのフォントが大きすぎる。プロパティー値の左に右詰めでプロパティー名を表示するレイアウトとしろ。」
> ⭐ **利用者の指示（2026-08-25）**「「知らせ」という日本語が残っている。「通知」に統一しろ。」
> ⭐ **台帳の行は `D-42` / `D-44` / `D-46` / `D-47` / `D-34`。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-1`（掴んで直せる）。受ける目標は `GL-001`。**
⛔ **4 件とも「掴めることが読めない」の一形である** —— 形が変わらないので掴めると分からず、掴んでも動かないので効いたと分からず、ラベルが線に重なるので何を掴んだのか分からず、パネルの字が大きすぎて値の並びが読めない。
⚠️ **どれも実装の粗さではなく、仕様に行が無いことであった。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 名称ラベルの縦位置 | ⛔ **表 T-013 は横の置き場所しか決めておらず、縦位置を決める行が 1 つも無い** |
| **`R1.4`** | プロパティパネルのレイアウト | ⛔ **`S-186` 〜 `S-193` が `FR-006` を名指しているのに、`FR-006` はそれらを 1 度も引いていない。**⭐ **値の行が、自分を使わない要求を指していた** |
| **`R2.19`**（驚かせない）| 掴んで動かすもの | ⛔ **表 T-023d の 17 行のうち、追従する絵を求める行は 5 行だけである**（`GR-1` `GR-2` `GR-9` `GR-17` `GR-18`）。**移動と幅の変更という最も普通の 4 つが、そこに入っていない** |
| **`R2.20`**（届く）| ポインタの形 | ⛔ **`IN-2` は端点しか名指さない。**タスク本体の上でポインタが形を変える根拠が無く、実装側は STOP を残していた（正しい判断）|
| **`R3.4`**（同じ算術を 2 か所に書かない）| パネルの字の大きさ | ⭐ **係数で持ち、px で持たない** —— `fontScale` に追随させれば `NFR-007` の拡大に自動で耐える |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`S-196` ＝ 2px**（線だけの形状でラベルを持ち上げる隙間）| ⛔ **参考実装がこの絵を持たないので測れなかった。**⚠️ 分類 `C`（`screenRenderer` 1 コンポーネント内・公開 API 不動）。🔎 を付けた |
| 2 | ⭐ **`S-197` ＝ 0.70 / `S-198` ＝ 0.90** | ⭐ **どちらも参考実装の実測である**（下の 1.）。⛔ **参考実装の 11.9px そのものは採らない** —— あれは本仕様が持たない 17px の `ui-font` から出た数であり、写せば読む人の文字サイズ設定（`FR-039`）から外れる。⚠️ 分類 `C` |
| 3 | ⭐ **追従する行に `GR-15`（実績のマイルストーン）も入れた** | ⛔ **引継書は `GR-12` / `GR-3` / `GR-4` の 3 つだけを挙げていた。**⚠️ **利用者の逐語は「タスクやマイルストーン」であり、マイルストーンを外す理由が無い。**⚠️ 分類 `B` |
| 4 | ⭐ **`GR-12` は縦にも追従させる** | ⛔ **行の載せ替え（表 T-015a の `HM-3`）は縦の移動そのものである。**横だけ追従させると、どの行へ載るのかが離すまで見えない。⚠️ 分類 `C` |
| 5 | ⛔ **`GR-10`（名称ラベル）と `GR-11`（担当ラベル）は本変更で触らない** | ⚠️ **`D-26` / `D-43` の裁定がそこを別に動かす。**同じ行を 2 つの計画で書かないため、こちらでは触れない（規則 02 の 5.）|

---

## 1. 測った

⛔ **参考実装 `temp/gr-scheduler_current_io.html` を Playwright で開いて測った（2026-08-27、前に立つ者が自分の手で）。**

| 測ったこと | 実測 |
|---|---|
| パネルの `--grsch-ui-font` | **17px**（`.grsch-prop-panel` に載っている）|
| パネルの字の大きさ | **11.9px** ＝ `calc(0.7 × 17px)` ⇒ **係数 0.70** |
| 項目名の字の大きさ | **10.71px** ＝ 11.9 × 0.9 ⇒ **係数 0.90**、`text-align: right` |
| 入力欄 | `font-size 11.9px` ／ `padding 0 2px` ／ `border 2px inset` |
| ⇒ 本仕様に載せた場合 | `fontScaleSizes` は S=12 / M=14 / L=16 なので **8.4 / 9.8 / 11.2px** |
| ツールチップの機構 | ⭐ **`<svg><title>` が 26 個。**宿主の素のツールチップである（`setTimeout` は 4 個で、どれもツールチップではない）|
| ツールチップの文面 | ⭐ **タスク `"Concept Task, 2026-01-01 to 2026-05-01"` ／ マイルストーン `"Kickoff Milestone, 2026-01-01"`** —— **利用者の逐語「タスク名、開始日 to 終了日」そのものである** |
| タスクの上のポインタの形 | ⛔ **`default` である。**バーの上の 15 点（横 5 × 縦 3）をすべて `elementFromPoint` で当てて測った。`pointer` が 227 個あるのはすべて入口のボタンである。⚠️ **利用者の「指マークに変わるぞ？」は参考実装では成り立たない。**⭐ **それでも裁定は裁定なので入れる** —— 参考実装は根拠の 1 つであって正ではない（規則 02 の 3.）|
| 出荷する `dist/index.html` のパネルの字 | ⛔ **宿主の既定のまま**（`src/` に大きさを定める行が 1 つも無い）|
| `PREVIEWED_GRABS` の真の数 | ⛔ **17 行のうち 5 行**（`GR-1` `GR-2` `GR-9` `GR-17` `GR-18`）|
| 表 T-012 の「ラベル」欄 | ⛔ **存在しない。**⚠️ **引継書は「`SH-3`/`SH-4` について『下へずらす』と書いており食い違う」としていたが、それは「実績の置き方」の欄であって実績バーの話である。**⭐ **ラベルの欄そのものが無かった** |
| `impact.py`（`FR-006`）| 要求 6 件 / 参照 18 箇所 |
| `induced.py`（`FR-002` `FR-006` `T-012` `T-013` `T-023d` `IN-2` `S-197`）| ⭐ **閉路 0。**⇒ **4 件を 1 度に書いてよい** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-012 | **6 列目「名称ラベルの縦位置」を足す**（5 行すべてにセル）。閉じの散文 2 段落 |
| 2 | `S-196` を 表 T-206 へ新設 | 線だけの形状でラベルを持ち上げる隙間 |
| 3 | 表 T-028 の `IN-2` | 「タスクの本体とマイルストーンの図形の上は掴めることの合図」を足す |
| 4 | 表 T-023d の閉じの規則 | `GR-3` / `GR-4` / `GR-12` / `GR-15` の追従を求める 1 段落を新設 |
| 5 | `FR-006` | パネルの寸法・余白・配置を `S-186` 〜 `S-193` ＋ `S-197` / `S-198` に縛る 2 段落を新設 |
| 6 | `S-197` / `S-198` を 表 T-206 へ新設 | パネルの字の係数と、項目名の字の係数 |
| 7 | `_source/display-words.json` | `RS-23` の `ja` から「知らせ」を落とし「通知」にする（1 文）|
| 8 | `A-appendix` | 変更履歴に 1 行（`1.17`）|

⛔ **表 T-013 は 1 文字も動かさない。** 縦位置は横の置き場所と独立であり、`NL-1` / `NL-3` の 2 行はそのままで正しい。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1715 | **1718** | **+3**（`S-196` / `S-197` / `S-198`）|
| `uids` | 148 | **148** | 0 |

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` | 135 | **135** | ✅ |
| `figures` | 11 | **11** | ✅ |
| `rows` | 1718 | **1718** | ✅ |
| `uids` | 148 | **148** | ✅ |
| 重複検出器 | 29（new 0）| **29（new 0）** | ✅ |
| 検査 22 本 | 全緑 | **ALL GREEN** | ✅ |

⚠️ **最初の版は重複を 2 件持ち込んだ**（`S-197` と `S-198` の値の欄が似すぎ、`S-196` の備考が 表 T-012 の散文をそのまま繰り返していた）。⭐ **基準線に載せずに書き直した。**
