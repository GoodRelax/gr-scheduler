# CR-231 — 書き出す先が 0 バイトのとき、表 T-227 が何も言っていなかった

> **閉じるもの**: ⛔ **選択面が作ったばかりのファイルと、元から空だったファイルは、どちらも 0 バイトである。**
> ⛔ **表 T-227 はその場合を 1 行も言っておらず、`file-store.ts` の `STOP` が「仕様が決めていない」と書いたまま
> 片側を選んでいた。** ⚠️ **注記は「`occupied` のほうが安全に見える」とも書いており、2 か所で食い違っていた。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`。受ける目標は `GL-006`。** ⛔ **書き出すたびに要らない問いが出るか、要る問いが出ないかのどちらかになる。**
⚠️ **どちらも人の手を余分に取るか、取り戻せないものを失わせる。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 表 T-227 | ⛔ **`DI-3` は「中身を `GRS JSON` として読めないとき」を言うが、0 バイトが読めないに当たるかを言っていない。** 当たると読めば毎回問うことになる |
| **`R1.1`**（矛盾が無い）| `file-store.ts` の注記 | ⛔ **同じ注記の中で「`empty` を選んだ」と「`occupied` のほうが安全に見える」が並んでいた。** ⭐ **どちらが正かは仕様が答えるべきものであり、実装の注記が抱えるものではない** |
| **`R1.2`**（曖昧さの排除）| `DI-4` の射程 | ⚠️ **「同じとみなせない相手」に空のファイルが入るかどうかが読めない** |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

⭐ **分類 C。** ⛔ **保存形に痕跡が残らず、覆すコストは 1 行と 1 か所の分岐である。**

| | 決めたこと | 根拠（⭐ **仕様書の中で閉じている**）|
|---|---|---|
| 1 | ⭐ **0 バイトの書き出し先は「既にある」とみなさず、問わない** | ⛔ **`FR-031` は「確認を求めてよいのは、取り消しで取り戻せないものを失う場面に限る（MUST）。それ以外の場面で確認を求めてはならない（MUST NOT）」と定める。** ⭐ **0 バイトのファイルには失うものが無いので、問うことのほうが `FR-031` の MUST NOT に当たる** |
| 2 | ⭐ **`DI-3` に優先すると明記する** | ⛔ **書かないと「0 バイトは `GRS JSON` として読めない」から `DI-3` を経て `DI-4` へ落ちる。** 順序を書かないと、読む者ごとに答えが変わる |
| 3 | ⭐ **「見分けられない」を理由として書く** | **選択面が作ったファイルと元から空のファイルを、`FileStore` は見分けられない。** ⚠️ **見分けられない 2 つに別々の答えを与える規則は、実装が必ずどちらかを誤る** |

⚠️ **`DI-2` の「黙って上書きした結果は取り戻せず、余分に問うた結果は 1 回の手間である」と逆を向いていない** ——
⭐ **あちらは「中身がある相手」についての天秤である。** ⛔ **本行は中身が無い場合であり、天秤に載るものが片側に無い。**

---

## 1. 測った

```
impact.py  T-227
```

| 測ったこと | 実測 |
|---|---|
| `T-227` を指す要求 | 1 次 2 件（`FR-061` / `FR-096`）／ 2 次 3 件（`FR-031` / `FR-076` / `FR-070`）|
| 要求以外 | 1 か所（A.3 変更履歴）|
| 閉路 | ⭐ **0** |
| 落ちる試験 | ⭐ **無い** —— 実装は既に `empty` を選んでおり、本 CR はそれを正にする |
| ⛔ 消える注記 | `file-store.ts` の `ChosenWriteDestination` の `STOP`（「行が 1 つあればこの注記は消せる」と自ら書いている）|

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-227 | `DI-6` を 1 行足す —— 0 バイトの書き出し先は既にあるとみなさず、`DI-3` に優先する |
| 2 | A-appendix | 変更履歴に 1 行 |

⛔ **`DI-1` 〜 `DI-5` は 1 文字も動かさない。**

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **132** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1614 | **1615** | **＋1**（`DI-6`）|
| `uids` | 148 | **148** | 0 |

## 4. ⭐ 本巡 4 本ぶんの合計

| | 前（版 0.98）| 後 | 差 |
|---|---|---|---|
| `tables` | 132 | **133** | ＋1（表 T-233）|
| `figures` | 11 | **11** | 0 |
| `rows` | 1614 | **1630** | ＋16（`RS-1` 〜 `RS-15` ／ `DI-6`）|
| `uids` | 148 | **148** | 0 |
