# CR-157 — Chapter 7 に系統・レベル・方針・ツール・合格基準を置く

> ⛔ **未適用である。**
> **閉じるもの**: Chapter 7 の未記入行 1 行と、`NFR-013` がゲートを持たないこと。
> ⭐ **利用者の裁定 2 件を受けている** —— (1) **Chapter 9 のケースはテストコードから生成する**、(2) **`NFR-013` は 表 T-043 にゲート行を足して閉じる**。

## 1. 出どころ

直す行は `docs/spec/05-07-design.md:787` の 1 行である。逐語は次のとおり。

```
> 未記入。系統・テストレベル・方針・ツール・合格基準の表を書く。系統は 3 つ（ユースケーステスト / ソフトウェア仕様テスト / 非機能テスト）。ソフトウェア仕様テストの `Unit` は**仕様書の外で実施する**ため Chapter 9 にケースを書かない。文法もそれを強制しており、`SW_SPEC_TEST` の `TEST_LEVEL` は `Integration` と `System` しか受け付けない。テストコードの置き場は `tests/usecase/` `tests/integration/` `tests/system/` `tests/nfr/` で、Chapter 8 〜 10 の `File` 関係が指す先になる。
```

**この席は 2 か所から名指しで委ねられている。**

| 委ねている行 | 逐語 |
|---|---|
| `01-04-requirements.md:110`（1.4） | テストは、値だけで決まる層を Vitest で、利用者の操作を通した確認と性能の計測を Playwright で行う。**テストコードの置き場と Chapter 8 〜 10 の対応は Chapter 7 が定める。** |
| `05-07-design.md:201`（5.3） | **`main.ts` を作らない。**…**テストコードの置き場は Chapter 7 が持つ。本節は `src/` だけを持つ。** |

### ⛔ 実在する 300 件のテストに、仕様書上の居場所が無い

**実測**（本 CR で数え直したものだけを載せる）:

| 調べたもの | 実測 |
|---|---|
| テストの総数 | **300**（契約 **248** ／ 単体 **52**）。`vitest run` の実行数と一致 |
| テストファイル | **4**（`contract/` に 3、`unit/` に 1）。ほかにヘルパ `contract/spec-table.ts` と フィクスチャ `fixtures/grs-document.ts` |
| いま在る置き場 | `tests/contract/` ／ `tests/unit/` ／ `tests/fixtures/` |
| 仕様書が挙げている置き場 | `tests/usecase/` ／ `tests/integration/` ／ `tests/system/` ／ `tests/nfr/` |
| 重なり | ⛔ **0。いま在る 3 つは 1 つも挙げられていない** |
| `tests/e2e/` | ⛔ **実在しない。** それでいて `playwright.config.ts:12`（`testDir`）と `vitest.config.ts:22`（`exclude`）と `tests/README.md` の 3 か所が指している |
| `tests/README.md` の自己申告 | **`This layout is provisional.`**「Chapter 5.3 はテストコードの置き場を Chapter 7 の裁量と述べ、Chapter 7 はまだ空の器である。書かれたらこのファイルを見直せ」 |

⭐ **契約テストが名指しで指す仕様の行は、実測で 3 種**（`spec-table.ts` が実行時に表を読み、行 ID をそのままテスト名に出す）—— 表 T-065 の `IF-1` 〜 `IF-9`、表 T-075 の `UF-1` 〜 `UF-71`、表 T-056 のエンティティ名である。**落ちると仕様の 1 行を名指しする。**

### ⛔ `NFR-013` はゲートを持たない

| 調べたもの | 実測 |
|---|---|
| `NFR-013` の内容 | タスク数 `n` に対して レイアウトの算出・当たり判定・依存線の経路 を `O(n log n)` 以下とする。`O(n²)` を禁じる |
| 測り方 | **定義済み** —— 表 T-025 の `MC-9`（`1×` / `2×` / `4×` / `8×` の 4 段階、1 段階ごとに 3 回測って中央値） |
| 表 T-043 の行 | ⛔ **無い。** 同表の「正」の欄に現れるのは `NFR-001` `NFR-002` `NFR-003` `NFR-004` `NFR-010` `NFR-011` `FR-018` `FR-026` だけである |
| 測れる場所 | `05-07-design.md:66` が「**割っておくと `NFR-013` の計算量をブラウザ無しで測れる**」と述べている |

⚠️ **Chapter 7 は自ら「測る動機は、合否が工程を止めるときにしか続かない」と書いている**（`:744`）。**ゲートに載らない `NFR-013` は、前プロジェクトが踏んだ形をそのまま踏む。**

### 参考にした確定事実

- `SWS-` の実ノードは **0 件**（器の注記 2 箇所にしか現れない）。**Chapter 9 は親を持たないので、いま書けるのは方針だけである**
- `UC-xxx` は **14 本**、`NON_FUNC_REQ` は **13 本**
- 文法 `spec.sgra:127` の `TEST_LEVEL` は `SingleChoice(Integration, System)` であり、**`Unit` は機械が拒む**
- `TEST_LEVEL` の欄を持つのは `SW_SPEC_TEST` だけで、`USE_CASE_TEST` と `NON_FUNC_TEST` は持たない
- 番号の空き —— `T-218` / `T-219` は未使用、行 ID の接頭 `TS-` / `TW-` も未使用（既存の最大は `T-217`）

## 2. 何をするか

**未記入行 1 行を差し替え、表を 2 つ作り、表 T-043 に 1 行足す。図と UID は 1 つも増やさない。**

| | |
|---|---|
| **表 T-218 を作る** | テストの系統。**6 行**（`TS-1` 〜 `TS-6`）。系統 ／ 受け皿 ／ 親に取るもの ／ テストレベル ／ 置き場 ／ ツール |
| **表 T-219 を作る** | 章ごとの方針と合格基準。**3 行**（`TW-1` 〜 `TW-3`） |
| **表 T-043 に 1 行足す** | `PG-14`「規模を変えたときの伸び方」。`M3` から ● とし、合否は **ゲート**、正は `NFR-013` |
| **契約テストと単体テストに席を与える** | ⭐ **受け皿を持たない系統として表に載せる** —— 文法が `Unit` を拒むので仕様書にノードを持てないが、**置き場は Chapter 7 が持つ**（5.3 がそう委ねている） |
| **Chapter 9 を生成物にする** | **テストコードから起こす生成物とすること（MUST）。手で書いてはならない（MUST NOT）**。6.2 のスキーマと同じ守り方を掛ける |
| **席を閉じる** | 引用行を落とす。`grep -n "^> 未記入"` は **10 → 9** に減る |

### 表 T-218（新設）

| 行 ID | 系統 | 受け皿 | 親に取るもの | テストレベル | 置き場 | ツール |
| --- | --- | --- | --- | --- | --- | --- |
| TS-1 | ユースケーステスト | Chapter 8 の `USE_CASE_TEST` | `UC-xxx` | System | `tests/usecase/` | Playwright |
| TS-2 | ソフトウェア仕様テスト | Chapter 9 の `SW_SPEC_TEST` | `SWS-xxx` | Integration | `tests/integration/` | Vitest |
| TS-3 | ソフトウェア仕様テスト | Chapter 9 の `SW_SPEC_TEST` | `SWS-xxx` | System | `tests/system/` | Playwright |
| TS-4 | 非機能テスト | Chapter 10 の `NON_FUNC_TEST` | `NFR-xxx` | —— | `tests/nfr/` | Playwright |
| TS-5 | 契約テスト | 持たない | —— | Unit | `tests/contract/` | Vitest |
| TS-6 | 単体テスト | 持たない | —— | Unit | `tests/unit/` | Vitest |

### 表 T-219（新設）

| 行 ID | 章 | ケースの起こし方 | 合格基準 |
| --- | --- | --- | --- |
| TW-1 | Chapter 8 | 手で書く | `UC-xxx` のすべてが 1 件以上のケースに受けられ、その `TEST_RESULT` が `PASS` であること |
| TW-2 | Chapter 9 | テストコードから起こす | `SWS-xxx` のすべてが 1 件以上のケースに受けられ、その `TEST_RESULT` が `PASS` であること |
| TW-3 | Chapter 10 | 手で書く | `NFR-xxx` のすべてが 1 件以上のケースに受けられ、表 T-043 のゲート行が `PASS` であること |

### 表 T-043 に足す 1 行

```
| PG-14 | 規模を変えたときの伸び方 | — | — | ● | ● | ● | ゲート | `NFR-013`（測り方は表 T-025 の `MC-9`） |
```

⭐ **`M3` から始めるのは、`NFR-013` が挙げる 3 つのうち、当たり判定と依存線の経路がその節目で揃うためである** —— `PG-9`（当たり判定）と `PG-10`（依存線の経路）が同じ節目から始まっている。

## 3. ⚠️ 書かないと決めたもの

| | |
|---|---|
| **テストコードの機械可読な鍵の名前** | ⛔ **仕様に焼かない。** CR-147 §3 が `ts_type` を焼かないと決めたのと同じ理由である。「機械が読める形で持つこと（MUST）」までを書き、**どの鍵をどう足すかはテストコードの作りとする** |
| **生成器のファイル名・検査の番号** | 実装作業である。**CR は「同じ形で守ること」までにする**（CR-147 §3 と同じ）|
| **`tests/fixtures/` を表に載せること** | ⛔ **系統ではない。** テストではなく共有物の置き場であり、載せると「系統の無い行」が表に出る |
| **`tests/e2e/` の 3 か所の食い違い** | 仕様ではなく実装の不整合である。**本 CR を当てたあとの実装作業で直す**（`playwright.config.ts` / `vitest.config.ts` / `tests/README.md`）|
| **テストコードが型検査されていないこと** | ⛔ **仕様の穴ではない**（`tsconfig.json` の `include` に `tests` が無い）。実装の判断であり、別に扱う |
| **CI がテストを回していないこと** | 同上。`.github/workflows/` は `pages.yml` の 1 本だけである。**工程の話であり、仕様書は「いつ回すか」を持たない**（`:761` が同じ線引きをしている）|
| **`UC` が 14 本・`NFR` が 13 本という数** | ⛔ **表に数を書かない。** 要求が増減するたびに腐る。合格基準は「すべてが」と書く |

## 4. 当てる順

**本 CR が触るのは `05-07-design.md` の 2 か所（表 T-043 の直後に 1 行、Chapter 7 末尾の 1 行）である。** CR-156 が触った 6.2 の行とも、6.1 の未記入行とも重ならない。**順序は問わない。**

## 5. 数の予測

| | 改定前 | 改定後 |
|---|---|---|
| `tables` | 116 | **118**（`T-218` と `T-219`） |
| `rows` | 1408 | **1418**（`TS-` 6 ＋ `TW-` 3 ＋ `PG-14` 1） |
| `figures` / `uids` | 10 / 140 | **不変**（要求ノードを 1 つも作らない） |
| `> 未記入` | 10 | **9** |
| 重複 A | 16 (new 0) | **不変** —— `dup-check.py` は `01-04-requirements.md` と 2 つの `_assets` 表を読む。本 CR は `05-07-design.md` しか触らない |
| 生成物 | — | **走らせなくてよい** —— `erd.json` も `tbl-settings.md` も触らない |
| 契約テスト | 300 件緑 | **300 件緑**（`spec-table.ts` は指定された表だけを読む。新しい表は誰も読まない）|
