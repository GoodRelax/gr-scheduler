# CR-259 — 出口を持たない通知

> **閉じるもの**: ⛔ **利用者の裁定（2026-08-25）: 通知は `[OK]` で消す。日本語も `[OK]` でよい。**
> ⛔ **表 T-037 のどの行も「人が消す」手立てを持たず、通知を消す入口が製品のどこにも無い。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-2`（1 画面で全体を読める）。受ける目標は `GL-002` と `GL-006`。**
⛔ **消えない通知は日程の上に永久に残る。**⚠️ **実測で 6 秒経っても消えず、消す手立ても無かった。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| 表 T-037 の 8 行 | ⛔ **`NT-2` は「時間で消える」、`NT-7` は「2 択で問う」。⭐ 人が消す行が無い** |
| **`R2.20`**（届く）| 通知の寿命 | ⛔ **`raisedNotices` は伸びるだけで、短くする道が `src/` に 1 本も無い** |
| **`R2.19`**（驚かせない）| 語 | ⭐ **裁定が「日本語も `[OK]` でよい」と明示したので、言語ごとに分けない** |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⛔ **表 T-109 に行を足さない。図 F-019 も動かさない** | ⭐ **裁定は語であって図形ではない。**⚠️ **`FR-029` の「用途を言葉ではなくアイコンで伝える」はメニューの入口に向いた規則であり、`NT-7` の確認は既に語（続ける / 取りやめる）で答えている。通知の `[OK]` はそちらと同じ形である** |
| 2 | ⭐ **語は辞書（`_source/display-words.json`）が持つ** | ⛔ **画面に刷る語はすべて辞書が持つ**（`FR-038`）。⚠️ **両言語とも `OK` と綴る** —— 裁定が明示した |
| 3 | ⭐ **消せるのは人が消せる通知だけとし、`NT-7` の確認には足さない** | ⛔ **確認は答えを求めるものであり、`[OK]` で消すと「どちらでもない」が生まれる** |
| 4 | ⚠️ **`NT-2`（時間で消える）と併存させる** | ⭐ **同行は「消える前に読めること、または時間を止める手立て」を求めており、人が消せることはそれを弱めない** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| 表 T-037 の行 | **8 行**（`NT-1` 〜 `NT-7` と `NT-3a`）。⛔ **人が消す行は無い** |
| 表 T-109 の通知領域の入口 | ⛔ **0 件**（生成された名簿の面の内訳に `Notification Area` が無い）|
| `IN-4` の `Esc` の階層 | 面 → ドラッグ → 構え → `Dual Cursor` → 出ている説明。⛔ **通知は入っていない** |
| 描く側 | `notices.ts` の `Notice` は `manner` / `mannerText` / `text` / `nextSteps` / `affectedCount` だけを持つ。⛔ **入口を持たない** |
| 消す道 | ⛔ **`raisedNotices` を短くする文が `src/` に 1 本も無い** |
| 誘導部分グラフの閉路 | ⛔ **無し** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-037 | 1 行（`NT-8`。**告げた通知は、人がその場で消せること（MUST）。語は辞書が持ち、両言語とも `OK` とすること（MUST）。確認（`NT-7`）には置いてはならない（MUST NOT）**）|
| 2 | `_source/display-words.json` の原稿と生成器 | 通知を消す語の枠を 1 つ（両言語とも `OK`）|
| 3 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 135 | **135** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1692 | **1693** | ＋1（`NT-8`）|
| `uids` | 148 | **148** | 0 |
