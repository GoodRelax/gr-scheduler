# CR-556 — 期限を緑の下向きの矢印で描く

> 起草の状態: 起草（2026-09-24 に 11 節の問い 3 つへ利用者が答えた）。問い 1 は A、問い 2 は B（推奨と違う —— バーのツールチップに期限の行を足す。E-10 〜 E-12）、問い 3 は提案どおり。4 節の編集を当ててよい。
> 読んだ木: `61bbd572`（`refactor` の内容。`CR-551` は着地済み —— `6f674198` 〜 `7dbd292d`。worktree の枝は `claude/fervent-keller-e96fcc`）。行番号・数・参照は、すべてこの木で測った（測り方は 13 節。起草は `ebc71984` で始め、着地の後に測り直した）。
> ⛔ **当てる順**: `CR-551`（着地済み）→ `CR-552` → `CR-553` → `CR-554`（段 7.5 の分割）→ 本書。本書の旧の塊は `61bbd572`（`CR-551` の後）で数えた。**当てる者は、4 節の旧を、当てる木でもう 1 度数えてから当てること**（`CR-551` 〜 `CR-553` の 4 節の旧と新のどれも、本書の旧の行を含まない —— 13 節）。9 節のコードの所は「関数名 ＋ `CR-554` の前のファイル」で書き、割ったあとの置き場を添えた。
>
> **閉じるもの**: 利用者の 2026-09-24 の指示 1 件と、11 節の問いへの答え 3 件（12 節の `JDG-446` 〜 `JDG-449`。逐語は下の 0.1 節）。`FR-045`（MUST）が期限の印を求めながら、`src/` のどこも描いていない食い違い（12 節の `DFC-861`。`DFC-675` の 28 件のうちの 1 件、`CR-554` の 9 節の候補 `DFC-830`）。
> ⛔ **覆すもの**: 無い。`FR-045` の「別の印として示す」を、形・色・置き場まで決める。表 T-020 の注「期限の印 … まだ描いていないので本表に行を持たない」は、描くので偽になり書き直す（E-05）。
> ⭐ **形の方針**: 利用者は、使い勝手の試しのあとで裁定がまた動くと述べている（`CR-551` の形の方針）。⇒ 描き方は新しい 表 T-304 の 1 行ずつに、値は設定値の 1 行ずつ（`S-364` 〜 `S-367`）に置き、`FR-045` はその表を指すだけにする。裁定を戻すときは、多くが 1 行の書き換えで済む（戻し方は 1 節の最右列）。

### 0.1 利用者の逐語（2026-09-24）

⚠️ 前に立つ者が体へ渡した写しのまま写した。

| 項目 | 逐語 |
|---|---|
| 1 | 「プロパティーパネルで期限/deadlineを手入力したら、緑の下向き矢印アイコンで期限を表示しろ。」 |

⭐ 利用者が求めたのは**表示**だけである。⛔ 印を掴んで期限を動かす操作は求められていないので、本書は作らない（決定 8）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008` ／ `UC-003`**（`FR-045` の ORIGIN は `UC-003`）—— 期限は「この日までに終える」外部との約束であり、予定の終了日と並べて見えないと、終了日が約束の前か後かを日程表の上で読めない。
⭐ **`CH-2` ／ `GL-002`**（全体を 1 枚で俯瞰する）—— 期限をプロパティパネルの欄の中でしか読めないと、全体を見て約束に間に合わないタスクを探せない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正）** —— ① `FR-045`（MUST）は印を求めるが、形・色・置き場を決める行がどこにも無く、`src/` は 0 行である（`git grep -n -i "deadline\|FR-045" -- src/entity/layout-engine src/adapter/svg-renderer` が、`schedule-layout.ts:953` の「OC-8 and OC-9 are not counted yet: their marks are not drawn (MS-4)」の注 1 つだけ）→ 表 T-304 を立て、`FR-045` をそこへ向けた（E-01 ・ E-02）。② 表 T-020 の注（`01-04-requirements.md:4000`）が「期限の印 … まだ描いていないので本表に行を持たない」と書く → 描くので行を足し、注から期限の印を外した（E-04 ・ E-05）。③ 表 T-274 の結び（`:419`）が「本表の「覆う行」の欄にも上の段にも名の無い行を … 表 T-020 へ足してはならない（MUST NOT）」と定める → 足す `ZO-13` を `PP-7` の覆う行に足した（E-06）。④ 表 T-038 の `OC-9`（`:1519`）は既に「左右」に算入すると書くが、どの幅を数えるかが無い → 同じ行に幅を書いた（E-03）。⑤ `FR-094`（`:1236`）は寸法を 表 T-201 に従わせるが、本書は印の比を 表 T-206（保存しない）に置いた —— `CR-551` が遅れの記号の寸法（`S-328` 〜 `S-331`）を 表 T-206 に置いたのと同じ形であり、要求は「表に無い寸法を実装が独自に持つ」ことを禁じるので、表に宣言した行は禁に当たらない（10 節）。
- **`R1.4`（異常系・境界値）** —— 期限が `null`（描かない・場所も取らない、`DA-1`）、期限が予定の開始より前（印はバーの左に出る、`OC-9` が既に書く）、期限が見えている範囲の外（`Row Area` で切る、`DA-7`）、期限が非稼働日（寄せない、`DA-4`）、マイルストーン（同じ規則で描く、`DA-1` ・ `DA-5`）、予定も実績も隠したとき（描く、`DA-1`）、描かない行（畳んだ・隠した・詳しさの段で落とした行、`DA-1`）、予定日の無い `Task`（配置されないので描かない、`DA-1`）、ピン止めした行（帯の中に描く、5 節の S-3）、モノクロ（無彩色で形が担う、`DA-3`）、起動の見本の文書の最初の表示位置（`OP-10` の「覆う最初の日」は `start` と `actualStart` だけであり、期限を数えない —— 利用者の裁定 2026-08-29 のまま。`tests/unit/t-024a-op-10-first-day-covered.test.ts:558` が既に主張する）。
- **`R1.2`（検証できる表現）** —— 「緑」を `S-364` の 2 つの値に、「下向き矢印」を `DA-2` の形と 3 つの比（`S-365` 〜 `S-367`）にした。
- **`R2.1`（命名）** —— UI パーツ `Deadline Mark`（`U-63`、日本語は 表 T-006b の `A-5` が既に定める「期限の印」）。新しい接頭辞 `DA`（Deadline Arrow）—— `DL` は既に「遅れの量の数え方の場合分け」（表 T-021b）を指すので使わず、`DL` と見間違える綴り（`DA` など）も採らない。遅延診断の変更要求が `D` で始まる接頭辞を登録するので、調整役が 2026-09-24 に `DA`（木に 0 件、登録簿に無い）を配った（記録 `one-prefix-one-meaning`）。
- **`R4.x`（状態機械）** —— 当たらない。印は押されも掴まれもしない（`DA-6`）。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる。戻し方は 1 節）。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 印の箱の高さ d は、その `Task` の進捗マーカーの径とし、形は d に掛ける 3 つの比で決める（`DA-2`） | マーカーの径は `FR-094` が「ズームに追随させない」と定め、表示の倍率も 表 T-252 の `DS-1` で既に掛かる。印に別の大きさの行を立てると、同じ 2 つの規則をもう 1 度書くことになる。線だけの形（`SH-3` ／ `SH-4`）の径は名前の字（`FR-094`）なので、1 段目の高さにも合う | マーカーの径（`S-22`）を選び直すと印も動く |
| 決定 2 | 3 つの比と色を、表 T-206（保存しない）と 表 T-236（成果物に埋め込む定数）に 1 行ずつ置く | `CR-551` の遅れの記号（`S-328` 〜 `S-331` と `S-326` ・ `S-327`）と同じ置き場。印の形は見せ方であって日程の内容ではない | 文書ごとに印の形は選べない |
| 決定 3 | 色は `#1f7a3d` ／ `#6fc98d`（`S-364`）。値は `S-152`（良の色）と同じだが、継がずに自分の値を持つ。テーマの色相に追随させない | 逐語「緑」。`S-152` は既に明暗の両方で読める緑である（地に対して 5.37 : 1 ／ 8.95 : 1、13 節）。継がないのは、期限は「順調」の意ではなく、良の色を選び直したときに印が動かないようにするため | 値は 11 節の問い 3 |
| 決定 4 | 形の縁を地の色（`S-146`）で、d × `S-34`（名称ラベルの縁取りの比）の太さに描く（`DA-3`） | 実績バーの塗り（`S-157`）に対して緑は 1.52 : 1（明）／ 1.41 : 1（暗）しかなく、パレットの緑の実績（`#1f8340`）では 1.12 : 1 である（13 節）。縁が無いと、実績バーの上に立つ印が沈む。名称ラベルは同じ理由で地の色の縁を持つ（表 T-273 の結び、`:3927`） | 名称ラベルの縁取り（`S-34`）を選び直すと、印の縁も動く |
| 決定 5 | 横は、`deadline` の日を、予定の端と同じ求め方で時間軸に置いた位置（`DA-4`）。どちらの日の端かを新しく決めない | 予定の右端は `finish` の日を同じ関数で置いた位置である（`schedule-layout.ts:895` の `xOnTimeAxis` と `:909` の `planRight = x + width`、幅は `(finish − start)` 日 —— `:601-606`）。同じ求め方なら `finish` ＝ `deadline` のとき先端が予定の右端に重なり、「間に合うか」を 1 本の縦で読める | 形状の床（`S-49`）で広げた 1 日の予定では、右端と先端がずれる |
| 決定 6 | 縦は、中心線を共有する形とマイルストーンでは予定と同じ中心、線だけの形では 1 段目（`DA-5`）。⭐ 利用者が 11 節の問い 1 に A と答えた（`JDG-447`） | 箱がマーカーと同じ帯に収まるので、帯高も段割当も変わらない（表 T-271 の `XS-3` ・ `XS-4` ・ `XS-8`）。線だけの形では先端が 2 段目の予定の線を指す | 矩形では印がバーの上に重なる（縁で読ませる。決定 4）。⚠️ バーの上に置く案は 11 節の問い 1 |
| 決定 7 | 印は、予定・実績の表示の切り替え（`S-227` ／ `S-228`）にも、進捗マーカーの切り替え（`S-63`）にも従わない。印を隠す切り替えを足さない（`DA-1`） | `FR-045` の RATIONALE「期限は … 予定の終了日とは別物である」。進捗マーカーは既定で隠れる（`JDG-370`）ので、マーカーに従わせると既定で期限が見えない。逐語は切り替えを求めていない | 期限を画面から消すには、期限そのものを空にするしかない |
| 決定 8 | 印を当たり判定の対象にしない。印そのもののツールチップも出さない（`DA-6`、E-07）。⭐ 利用者が 11 節の問い 2 に B と答えた（`JDG-448`、推奨の A と違う）⇒ 期限の日付は、バーのツールチップ（表 T-040 の `EZ-6`）の 2 行目に出す（E-10 〜 E-12）。語はプロパティパネルの期限の欄と同じ `PR-10` の語を引き、辞書の項を足さない | 逐語は「表示しろ」だけ。表 T-023a の結び（`:3324`）「当たり判定の対象は、そのフレームで描いた編集対象の要素に限ること（MUST）」—— 期限はプロパティパネルの `PR-10` で変える値であり、日程表の上では変えない。当たらなければ、印の下にあるもの（バー・何も無い所）が今までどおり応える。逐語「B バーのツールチップに足す」 | 印がバーの外に立つとき、印の上に止めても何も出ない —— 期限の日付はバーに止めて読む。英語の語は `PR-10` のまま小文字の `deadline` になる |
| 決定 9 | 見えている範囲の外の印は、ほかの形と同じく `Row Area` の縁で切る。外に在ることを示す別の印を足さない（`DA-7`） | 全体表示（`FR-055`）は 表 T-038 の占有幅で収めるので、`OC-9` に数えた印は全体表示で必ず見える（`CR-551` の E-15「算入するのは、いま描いているものだけ」）。スクロールの中身は既に `Row Area` で切っている（`svg-renderer.ts:443-464` の `clipPath`） | 拡大して見ている間は、画面の外の期限に気づけない |
| 決定 10 | 占有幅（`OC-9`）は、印の軸を中心とした矢じりの幅を、形状とのあいだを含めて 1 つの占有として数える（E-03） | いまの段割当は 1 つの `Task` を 1 つの区間で数える（`schedule-layout.ts:954-956` の `occupiedX0` ／ `occupiedX1`、`:965-969` の比べ方）。あいだに別の `Task` が入ると、印がどの `Task` のものかが読めない | 遠い期限を持つ `Task` は、あいだの段を塞ぐ（段が増えうる） |
| 決定 11 | マイルストーンにも描く | `deadline` は `Task` の列であり、マイルストーンも `Task` である。MSPDI の `Task/Deadline` も分けない | — |
| 決定 12 | 前後は、依存線（`ZO-4`）の手前、イナズマ線と線の道具（`ZO-8`）・進捗マーカー（`ZO-3`）・札（`ZO-5`）の奥（`ZO-13`、E-04） | 実績バーの奥では印が隠れる。`FR-110` は字とマーカーを線と形より手前に置けと定める —— 押す的（マーカー）と読む字を、押せない印で覆わない | 表 T-020 の「順」の欄が 8 行ずれる |
| 決定 13 | 新しい UI パーツ `Deadline Mark`（`U-63`）を 表 T-103 に立て、表 T-076 の `EP-5` で書き出しにも描く（E-08 ・ E-09） | 期限は文書のデータであり、`EP-5` は「日程そのもの」を描く行である。書き出しと画面が同じになる（`WY-3` の一致は `EP-5` の UI パーツを測る） | — |
| 決定 14 | 印の色はプロパティパネルの色の欄（`CR-551` の選び方、`JDG-383` ・ `JDG-397` ・ `JDG-405`）で選ばせない | 色は製品の定数（表 T-236）であり、文書の列ではない。逐語は色を選ばせることを求めていない | — |
| 決定 15 | 期限の超過を遅れにも、遅延診断にも含めない | `FR-045` の RATIONALE が既に「期限の超過を遅れの判定に含めない」と定める。遅延診断の構想（`docs/review/progress-check-2026-09-23.md`）は期限に 1 度も触れない（`grep -c -i "deadline\|期限"` が 0） | — |

---

## 1. 範囲 —— 項目ごとの行き先

| 何 | 仕様で変える所 | 編集 | 裁定を戻すとき |
|---|---|---|---|
| 要求を表へ向ける | `FR-045` の STATEMENT | E-01 | — |
| 描く条件（`null` で描かない、切り替えに従わない） | 表 T-304 の `DA-1`（新） | E-02 | `DA-1` の 1 行 |
| 形（下向きの矢印） | `DA-2`（新）、`S-365` 〜 `S-367`（新、表 T-206） | E-02 ・ J-02 | 各行の値 ／ `DA-2` の 1 行 |
| 色（緑）と縁 | `DA-3`（新）、`S-364`（新、表 T-236） | E-02 ・ J-01 | `S-364` の値 ／ `DA-3` の 1 行 |
| 横の位置 | `DA-4`（新） | E-02 | `DA-4` の 1 行 |
| 縦の位置 | `DA-5`（新） | E-02 | `DA-5` の 1 行（11 節の問い 1） |
| 当たりと説明 | `DA-6`（新）、表 T-023a の結びの 1 文 | E-02 ・ E-07 | `DA-6` の 1 行と 1 文 |
| バーのツールチップの期限の行（`JDG-448`） | 表 T-040 の `EZ-6` の 3 つの段 | E-10 〜 E-12 | `EZ-6` の 1 行の中で閉じる —— E-10 が足した段（期限の行を出す 5 文）と、E-11 ・ E-12 が足した 1 文ずつを消す |
| 見えている範囲の外 | `DA-7`（新） | E-02 | `DA-7` の 1 行 |
| 占有幅 | 表 T-038 の `OC-9` | E-03 | `OC-9` の 1 セル |
| 前後 | 表 T-020 の `ZO-13`（新）と注、表 T-274 の `PP-7` | E-04 〜 E-06 | `ZO-13` を消して「順」を詰める（1 行では済まない —— 「順」の欄が 8 行ずれる） |
| 書き出し | 表 T-076 の `EP-5`、表 T-103 の `U-63`（新） | E-08 ・ E-09 | `EP-5` の 1 語 |
| 接頭辞 | `row-id-prefixes.json` の `DA`（新） | J-03 | — |

**数**: 仕様の文の編集 12（E-01 〜 E-12）、原稿 JSON の編集 3（J-01 〜 J-03）。辞書（`display-words.json`）の編集は 0 —— 期限の語は既に在る `properties` の `PR-10`（`ja` 「期限」 ／ `en` 「deadline」、`_source/display-words.json:1132-1137`）を引く。

⚠️ **`CR-551` ・ `CR-552` ・ `CR-553` と重なる所**（3 本の変更要求の全文を、本書の旧 12 塊の字で grep した。13 節）:
- 本書の旧の文を、3 本の旧も新も含まない —— 12 件とも 0 回。
- 同じファイルに入る: `01-04-requirements.md`（3 本とも。本書の行は `FR-045` ・ `OC-9` ・ 表 T-020 ・ `PP-7` ・ 表 T-023a の結び ・ `EP-5`。`CR-551` の E-15 が書く `FR-055` と `CR-552` の `FR-018` は隣の要求でもない）、`settings.json`（`CR-551` は 表 T-236 の末 `S-223` の後と 表 T-206 の末 `S-297` の後に足す。本書は `S-311` の後と `S-247` の後に足すので、足す場所が重ならない）、`tbl-glossary.md`（`CR-551` は 表 T-109 ・ `P-20` ・ `CM-25`、`CR-552` は 表 T-104 の `K-55`。本書は 表 T-103 の末）、`row-id-prefixes.json`（`CR-551` は `RK` を `RL` の前に。本書は `DA` を `CV` と `DC` のあいだに）。
- ⇒ **当てる者は、着地した木で本書の旧をもう 1 度数えてから当てること。**
- ⚠️ 同じ日に起草中の兄弟（2026-09-24、作業木に在る草案）: `CR-557`（テーマの色相）が 表 T-236 の色の行の解き方（`S-146` を含む —— `DA-3` の縁の色）と `row-id-prefixes.json`（`TH`）を、`CR-560` が `row-id-prefixes.json`（`DD` を `DC` の後）を触る。足す場所は重ならないが、`CR-557` が `S-146` の値の出し方を変えるなら、`S-364` の備考の実測（地に対して 5.37 : 1 ／ 8.95 : 1）を測り直すこと。

---

## 2. 新しい識別子

⭐ 番号は 2026-09-24 に前に立つ者が `b7a3b76f` で詰めた（`CR-555` 〜 `CR-560` を CR の順に、配られた帯の先頭から隙間なく。帯の外の接頭辞は木の最大 M の次から）。下の「空いている」はその木で測った主張である。⚠️ 規則 02 の 2.5 節: **当てる直前に測り直すこと。** 測り方は 13 節。

| 種類 | 採ったもの | 測った最大（`b7a3b76f`、`CR-555` 〜 `CR-560` の草案を除く） |
|---|---|---|
| 設定値 | `S-364` ・ `S-365` ・ `S-366` ・ `S-367` | `S-363`（`CR-555`）の次（`S-999` は試験の番兵なので除く） |
| 表 | `T-304` | 仕様が使う最大は `T-297`（`CR-551` が着地させた）。`T-306` は古い `CR-110` が引くので使わない |
| 接頭辞 | `DA`（新。登録簿は 163 件（`CR-551` の `RK` を含む）、本書で 164） | `DA-` の行は木のどこにも無い |
| 行 | `DA-1` 〜 `DA-7`、`ZO-13`、`U-63` | `ZO` の最大 12 ・ `U` の最大 62 の次 |
| 台帳 | `JDG-446` 〜 `JDG-449`、`DFC-861` | 帯の中で `CR-555` の次 |
| 名 | UI パーツ `Deadline Mark`、型 `DeadlineGeometry`、欄 `TaskPlacement.deadlineX` ・ `TaskGeometry.deadline`、生成の定数 `NOT_STORED_DEADLINE_MARK_SIZES` | `git grep` で 0 件 |

- 本書が取らないもの: 保留の行（12 節）、`FR-` ・ `IC-` ・ `AM-` ・ `PR-`（要求は `FR-045` を書き直すだけ、期限の欄 `PR-10` は既に在る）、`UC-` ・ `V-` ・ `GL-` ・ `CH-`。
- 6 本を詰めた後に返った帯: `S-377` 〜 `S-384`、`T-309`（`T-306` ・ `T-307` は古い文書が引くので飛ばした）、`DFC-874` 〜 `DFC-889`、`PND-561` 〜 `PND-580`（6 本とも保留の行を起こさない）、`PR-29` 〜 `PR-32`、`FR-137` 〜 `FR-148`、`IC-109` 〜 `IC-114`、`AM-20` 〜 `AM-24`（6 本とも使わない）。

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 消すもの | 所（`61bbd572`） | 置き換わる先 | 編集 |
|---|---|---|---|
| 表 T-020 の注の「期限の印・」（「まだ描いていないので本表に行を持たない」の対象から外す） | `01-04-requirements.md:4000` | 表 T-020 の `ZO-13` | E-05 |
| 表 T-020 の「順」の欄の `ZO-8` 〜 `ZO-6` の 8 つの数（6 〜 13） | `:3987-3994` | 7 〜 14（`ZO-13` が 6 に入る） | E-04 |
| `FR-045` の STATEMENT が形も色も置き場も決めていないこと（文は残し、表への指しを足す） | `:3050` | 表 T-304 | E-01 |
| 表 T-040 の `EZ-6` が名前と 2 つの日付だけを出す読み（文は残し、期限の行を足す）。同じ行の「記号も数字も言語で変わらないので `FR-038` の辞書は持たない」は日付について真のまま残り、期限の語だけが辞書の `PR-10` を引く | `:6309` | `EZ-6` の 2 行目 | E-10 〜 E-12 |
| コードの注「OC-8 and OC-9 are not counted yet: their marks are not drawn (MS-4).」（⛔ 本書は直さない。9 節） | `src/entity/layout-engine/schedule-layout/schedule-layout.ts:953` | 「OC-8 is not counted yet」 | 実装する者 |

⭐ 消す行・消す列・消す設定値は無い。

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-551` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（13 節の道具が 12 件すべてで 1 回を確かめた）。旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である。
⚠️ この木の 4 つの原稿（`01-04-requirements.md` ・ `tbl-glossary.md` ・ `settings.json` ・ `row-id-prefixes.json`）は、作業木では全行が CRLF である（`git grep` は LF で見せる）。読むときに改行を揃え、書くときは多い方（CRLF）で書くこと（記録 `crlf-mixed-in-this-tree`）。
⚠️ 生成物（`_assets/tbl-settings.md` ・ `tbl-row-id-prefixes.md` と `src/` の生成物）は手で直さない。原稿を直して `npm run gen` を打つ。

### 4.1 文の編集（`01-04-requirements.md` ・ `_assets/tbl-glossary.md`）

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
旧
```text
**STATEMENT**: `Task` に期限（`deadline`）が入っているとき、`GRS` は、それを**終了日とは別の印**として日程表の上に示すこと。
```
新
```text
**STATEMENT**: `Task` に期限（`deadline`）が入っているとき、`GRS` は、それを**終了日とは別の印**として日程表の上に示すこと。  
印を描く条件・形・色・置き場・当たりは、表 T-304 に従うこと（MUST）。
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
旧（`FR-045` の RATIONALE の最後の行。この後ろに表を足す）
```text
期限は外部との約束であって進捗の遅れとは別の量であり、混ぜると `UC-006` の判定が二重になる。
```
新
```text
期限は外部との約束であって進捗の遅れとは別の量であり、混ぜると `UC-006` の判定が二重になる。

**表 T-304 — 期限の印**

| 行 ID | 何についてか | 規則 |
| --- | --- | --- |
| DA-1 | 描く条件 | `deadline` が `null` でない `Task` の印を、その `Task` を置いた行を描くときに描くこと（MUST）。<br>マイルストーンも同じである。<br>`deadline` が `null` の `Task` には描かないこと（MUST）。<br>⛔ 予定と実績の表示の切り替え（`_assets/tbl-settings.md` の 表 T-202 の `S-227` ／ `S-228`）と、進捗マーカーの切り替え（同表の `S-63`）に従わせてはならない（MUST NOT） —— 期限は予定とも進捗とも別の約束であり（本要求の RATIONALE）、予定を隠しても約束は残る。<br>⚠️ 畳んだ行・隠した行・詳しさの段で描かない行に載る `Task` の印は描かない —— その `Task` 自身を描かないからである |
| DA-2 | 形 | 下を指す矢印 1 つとし、縦の軸（矩形）と、下を指す二等辺三角形の矢じりを、1 つの塗りの形として描くこと（MUST）。<br>箱の高さ d は、その `Task` の進捗マーカーの径とすること（MUST） —— 径の求め方は `FR-094` が持つ（表 T-201 の `S-22`。表 T-012 の `SH-3` ／ `SH-4` は名称ラベルの字の大きさ）。<br>矢じりの幅は d × `_assets/tbl-settings.md` の 表 T-206 の `S-365`、矢じりの高さは d × 同表の `S-366`、軸の太さは d × 同表の `S-367` とすること（MUST）。<br>軸は箱の上端から矢じりの底辺までとし、矢じりの先端を箱の下端に置くこと（MUST）。<br>⭐ 径をマーカーから借りるのは、ズームに追随させない規則（`FR-094`）と表示の倍率の掛け方（`FR-039` の 表 T-252 の `DS-1`）を、マーカーと 1 つにするためである |
| DA-3 | 色 | 塗りを `_assets/tbl-settings.md` の 表 T-236 の `S-364` とすること（MUST）。<br>形の縁を、地の色（同表の `S-146`）で、d × 表 T-201 の `S-34` の太さに描くこと（MUST） —— 名称ラベルの縁取り（`FR-109` の 表 T-273 の結び）と同じ作法であり、濃い実績バーの上でも印が沈まない（値の実測は `S-364` の備考）。<br>⚠️ モノクロ（`FR-041`）では、ほかの色と同じく描く段で無彩色になる —— 期限であることは形が担う（`FR-030`） |
| DA-4 | 横の位置 | 軸の中心（矢じりの先端）を、`deadline` の日を時間軸に置いた位置に立てること（MUST） —— `start` と `finish` の日から予定の端を置くのと同じ求め方である（`05-07-design.md` の 表 T-064 の `PI-5` の `xFromDay`）。<br>⭐ ⇒ `finish` と `deadline` が同じ日なら、先端は予定の右端に立つ（形状の床 `S-49` で広げた予定を除く）。<br>マイルストーンで `start` と `deadline` が同じ日なら、先端は菱形の中心に立つ。<br>⛔ 非稼働日の期限を稼働日へ寄せてはならない（MUST NOT） —— 置かれた日そのものが約束である |
| DA-5 | 縦の位置 | 中心線を共有する形（表 T-012 の `SH-1` ／ `SH-2`）では、箱の中心を予定と同じ中心に置くこと（MUST） —— `FR-109` の 表 T-271 の `XS-3` でマーカーが立つ帯である。<br>線だけの形（同表の `SH-3` ／ `SH-4`）では、箱を 1 段目（表 T-271 の `XS-4`）に置くこと（MUST） —— 先端が 2 段目の予定の線を指す。<br>マイルストーン（表 T-012 の `SH-5`）では、箱の中心を予定の菱形と同じ中心に置くこと（MUST）（表 T-271 の `XS-8`）。<br>⭐ どの形でも、印のために縦の占有を足さない —— 箱はマーカーと同じ帯に収まる |
| DA-6 | 当たり | ⛔ 印を当たり判定の対象にしてはならない（MUST NOT） —— 印は表示だけのものであり、期限を変える入口は `Properties Panel` の `PR-10` である（表 T-023a の結び）。<br>⇒ 印の上でポインタを押したとき・止めたときは、印が無いときと同じものが応える —— 押したときは 表 T-023a と 表 T-267、止めたときは `FR-092` の `EZ-6` である。<br>⚠️ 印そのものの説明（ツールチップ）は出さない —— 期限の日付は、印の下の `Task` の説明（`FR-092` の `EZ-6` の期限の行）が出す |
| DA-7 | 見えている範囲の外 | `Row Area` の外に立つ印は、ほかの形と同じく `Row Area` の縁で切って描くこと（MUST）。<br>⚠️ 範囲の外に在ることを示す別の印は持たない —— 全体表示（`FR-055`）は印を占有幅に数える（表 T-038 の `OC-9`）ので、全体表示をすれば見える |
```

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md -->
旧
```text
| OC-9 | 期限の印（`FR-045`） | **左右** —— 期限は終了日とは別の約束なので、`finish` より前に置かれることが本来の用途上ふつうに起きる。<br>そのとき印はバーの左に出る |
```
新
```text
| OC-9 | 期限の印（`FR-045` の 表 T-304） | **左右** —— 期限は終了日とは別の約束なので、`finish` より前に置かれることが本来の用途上ふつうに起きる。<br>そのとき印はバーの左に出る。<br>⭐ 数える幅は、印の軸を中心とした矢じりの幅（表 T-304 の `DA-2`）とすること（MUST）。<br>⭐ 形状から離れて立つ印も、形状とのあいだを含めて 1 つの占有として数えること（MUST） —— あいだに別の `Task` を置くと、印がどの `Task` のものかが読めない |
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
旧（`ZO-8` から `ZO-6` までの 8 行）
```text
| ZO-8 | 6 | イナズマ線・基準日線・2 連カーソル・ガイドカーソル・ハイライトボックスの枠 |
| ZO-3 | 7 | 進捗マーカー（**不透明な下地を持つ**）・再開アイコン |
| ZO-5 | 8 | 名称ラベル・担当と進捗の札 |
| ZO-9 | 9 | コメントボックス |
| ZO-10 | 10 | 選択の枠・フェードの掴み点（選んでいるタスクにだけ出す。<br>`FR-075`） |
| ZO-11 | 11 | 構えて引いている仮の依存線 |
| ZO-12 | 12 | 透かし（`FR-020`） |
| ZO-6 | 13（最前面） | **範囲選択の矩形**（表 T-023c の `SL-3`）。<br>⛔ **握っているあいだだけ描き、離したら消すこと（MUST）** —— **離した時点で残るのは選択そのものであり、矩形ではない** |
```
新
```text
| ZO-13 | 6 | 期限の印（`FR-045` の 表 T-304）。<br>形と依存線より手前、イナズマ線と線の道具（`ZO-8`）・進捗マーカー（`ZO-3`）・札（`ZO-5`）より奥とする —— 印は押す的でも読む字でもないので、それらを覆わない |
| ZO-8 | 7 | イナズマ線・基準日線・2 連カーソル・ガイドカーソル・ハイライトボックスの枠 |
| ZO-3 | 8 | 進捗マーカー（**不透明な下地を持つ**）・再開アイコン |
| ZO-5 | 9 | 名称ラベル・担当と進捗の札 |
| ZO-9 | 10 | コメントボックス |
| ZO-10 | 11 | 選択の枠・フェードの掴み点（選んでいるタスクにだけ出す。<br>`FR-075`） |
| ZO-11 | 12 | 構えて引いている仮の依存線 |
| ZO-12 | 13 | 透かし（`FR-020`） |
| ZO-6 | 14（最前面） | **範囲選択の矩形**（表 T-023c の `SL-3`）。<br>⛔ **握っているあいだだけ描き、離したら消すこと（MUST）** —— **離した時点で残るのは選択そのものであり、矩形ではない** |
```

<!-- EDIT id=E-05 file=docs/spec/01-04-requirements.md -->
旧
```text
⚠️ 期限の印・遅れの日数・変更前の予定の重ねは、まだ描いていないので本表に行を持たない —— 描くときに行を足す。  
```
新
```text
⚠️ 遅れの日数・変更前の予定の重ねは、まだ描いていないので本表に行を持たない —— 描くときに行を足す。  
```

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md -->
旧
```text
| PP-7 | 描く前後は 1 つの表で決める | 前後の決まりを 1 か所に集めるためである。<br>理由の全文は `FR-110` の RATIONALE が持つ | `ZO-1` ／ `ZO-1a` ／ `ZO-2` ／ `ZO-3` ／ `ZO-4` ／ `ZO-6` ／ `ZO-7` ／ `ZO-8` ／ `ZO-9` ／ `ZO-10` ／ `ZO-11` ／ `ZO-12` |
```
新
```text
| PP-7 | 描く前後は 1 つの表で決める | 前後の決まりを 1 か所に集めるためである。<br>理由の全文は `FR-110` の RATIONALE が持つ | `ZO-1` ／ `ZO-1a` ／ `ZO-2` ／ `ZO-3` ／ `ZO-4` ／ `ZO-6` ／ `ZO-7` ／ `ZO-8` ／ `ZO-9` ／ `ZO-10` ／ `ZO-11` ／ `ZO-12` ／ `ZO-13` |
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md -->
旧
```text
変更前の予定（`FR-015`）は表示のみで編集できないので対象外とする。  
```
新
```text
変更前の予定（`FR-015`）は表示のみで編集できないので対象外とする。  
期限の印（`FR-045` の 表 T-304 の `DA-6`）も対象外とする —— 期限はプロパティパネルで変える値であり、日程表の上では変えない。  
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md -->
旧（`EP-5` の行の中）
```text
／ `Resume Icon`（`U-6`）／ `Name Label`（`U-7`）
```
新
```text
／ `Resume Icon`（`U-6`）／ `Deadline Mark`（`U-63`）／ `Name Label`（`U-7`）
```

<!-- EDIT id=E-09 file=docs/spec/_assets/tbl-glossary.md -->
旧（表 T-103 の末の行 `U-57`。この後ろに 1 行を足す）
```text
| U-57 | `Notification Area` | 通知が立つ場所。<br>作法は表 T-037 が、運ぶ理由は表 T-233 が持つ。<br>⚠️ **`Confirmation` ではない** —— 通知は答えを求めない。<br>⛔ **`S-99g` が持つ「面」ではない** —— **重ねて開くものではない。<br>**⚠️ **`Esc` では 表 T-028 の `IN-4` の第 1 階層が本行を消す**（表 T-037 の `NT-8`）—— **面より先であって、面として閉じるのではない** —— 重ねて開くものではなく、`NT-4` が起動時の用件を 1 枚に集約させるのもここである。<br>⚠️ **値の型は `Notice` のままである** —— 本行が名づけるのは場所であって、立つものではない |
```
新
```text
| U-57 | `Notification Area` | 通知が立つ場所。<br>作法は表 T-037 が、運ぶ理由は表 T-233 が持つ。<br>⚠️ **`Confirmation` ではない** —— 通知は答えを求めない。<br>⛔ **`S-99g` が持つ「面」ではない** —— **重ねて開くものではない。<br>**⚠️ **`Esc` では 表 T-028 の `IN-4` の第 1 階層が本行を消す**（表 T-037 の `NT-8`）—— **面より先であって、面として閉じるのではない** —— 重ねて開くものではなく、`NT-4` が起動時の用件を 1 枚に集約させるのもここである。<br>⚠️ **値の型は `Notice` のままである** —— 本行が名づけるのは場所であって、立つものではない |
| U-63 | `Deadline Mark` | 期限の印。<br>`Task` の `deadline` を日程表の上に示す印。<br>描き方は `01-04-requirements.md` の `FR-045` の 表 T-304 が持つ。<br>⚠️ 「マーカー」と呼ばない（表 T-006b の `A-5`） |
```

<!-- EDIT id=E-10 file=docs/spec/01-04-requirements.md -->
旧（表 T-040 の `EZ-6` の行の中。`JDG-448`）
```text
そのタスクの名前と、`start` と `finish` の 2 つの日付を出すこと（MUST）。<br>
```
新
```text
そのタスクの名前と、`start` と `finish` の 2 つの日付を出すこと（MUST）。<br>⭐ そのタスクの `deadline` が `null` でないときは、次の行に、期限の語と `deadline` の日付を半角空白 1 つで区切って出すこと（MUST） —— 期限の印（`FR-045` の 表 T-304）は当たりを持たないので、印の日付はバーの説明で読む。<br>期限の語は、プロパティパネルの期限の欄と同じ語（`FR-038` の辞書の `properties` の `PR-10` の語）とすること（MUST）。<br>⛔ 期限の行のために新しい語を辞書に持ってはならない（MUST NOT） —— 同じ値を 2 つの語で呼ぶことになる。<br>`deadline` が `null` のときは、期限の行を出さないこと（MUST）。<br>
```

<!-- EDIT id=E-11 file=docs/spec/01-04-requirements.md -->
旧（同じ行の中）
```text
日付は `YYYY-MM-DD` で書き、開始・終了の順に `/` で並べること（MUST）。
```
新
```text
日付は `YYYY-MM-DD` で書き、開始・終了の順に `/` で並べること（MUST）。<br>期限の日付も `YYYY-MM-DD` で書くこと（MUST）。
```

<!-- EDIT id=E-12 file=docs/spec/01-04-requirements.md -->
旧（同じ行の中）
```text
⚠️ **値を持たない項目は、その場所を空けること（MUST）** —— 名前も 2 つの日付も空を取りうる。
```
新
```text
⚠️ **値を持たない項目は、その場所を空けること（MUST）** —— 名前も 2 つの日付も空を取りうる。<br>⚠️ 期限の行は本段の対象ではない —— 期限が空なら行そのものを出さない（上の段）。
```

### 4.2 原稿 JSON の編集（`_source/`）

J-01（色）`settings.json` —— 表 T-236 の `S-311`（停止日から再開アイコンへ引く破線の色）の後ろに、次の 1 行を足す
```json
{
 "id": "S-364",
 "name": {
  "ja": "期限の印の塗り（`FR-045` の 表 T-304 の `DA-3`）"
 },
 "light": {
  "colour": "#1f7a3d",
  "code": true
 },
 "dark": {
  "colour": "#6fc98d",
  "code": true
 },
 "hue": {
  "ja": "—"
 },
 "note": {
  "ja": "⭐ 緑である。⭐ 値は `S-152`（良の色）と同じだが、継がない —— 期限は「順調」の意ではなく、良の色を選び直しても印の色は動かない。⛔ **テーマの色相に追随させない。** ⭐ 実測（WCAG 2.1 のコントラスト比、テーマの色相 214）: 地（`S-146`）に対し明るいテーマ 5.37 : 1、暗いテーマ 8.95 : 1。実績バーの塗り（`S-157`）に対しては 1.52 : 1 ／ 1.41 : 1 しかない —— 形の縁（`DA-3`）が要る理由である"
 }
}
```

J-02（形）`settings.json` —— 表 T-206 の `S-247`（実績のダミーを描く幅の比）の後ろに、次の 3 行をこの順に足す
```json
[
 {
  "id": "S-365",
  "value": {
   "ja": "期限の印の矢じりの幅の、そのタスクの進捗マーカーの径に対する比（`FR-045` の 表 T-304 の `DA-2`）"
  },
  "default": {
   "num": "0.75",
   "mark": "🔎"
  },
  "note": {
   "ja": "印の見せ方の比であり、日程の内容ではないので保存しない。⭐ 表 T-038 の `OC-9` が占有幅に数えるのは本行の幅である。⭐ 径の求め方は `DA-2` が持つ —— 既定の表示の倍率 100 の矩形では、径 22.4 × 0.625 ＝ 14px に対して 10.5px である。⛔ **測って決めた値ではない** （🔎）"
  }
 },
 {
  "id": "S-366",
  "value": {
   "ja": "期限の印の矢じりの高さの、そのタスクの進捗マーカーの径に対する比（`FR-045` の 表 T-304 の `DA-2`）"
  },
  "default": {
   "num": "0.5",
   "mark": "🔎"
  },
  "note": {
   "ja": "`S-365` と同じ理由で保存しない。⭐ 残り（1 − 本行）が軸の長さになる。⛔ **測って決めた値ではない** （🔎）"
  }
 },
 {
  "id": "S-367",
  "value": {
   "ja": "期限の印の軸の太さの、そのタスクの進捗マーカーの径に対する比（`FR-045` の 表 T-304 の `DA-2`）"
  },
  "default": {
   "num": "0.25",
   "mark": "🔎"
  },
  "note": {
   "ja": "`S-365` と同じ理由で保存しない。⚠️ `S-365` より小さくすること —— 軸が矢じりより太いと、矢の形に読めない。⛔ **測って決めた値ではない** （🔎）"
  }
 }
]
```

J-03（接頭辞）`row-id-prefixes.json` —— 字の順の位置、`CV` の項の後ろ（`DC` の前）に足す
```json
{
 "prefix": "DA",
 "words": "Deadline Arrow",
 "owner": "spec",
 "means": {
  "ja": "期限の印の描き方（描く条件・形・色・置き場・当たり）"
 }
}
```

### 4.3 生成器と生成物（形で示す）

| 何 | 何をする |
|---|---|
| `npm run gen` | 上の原稿から `_assets/tbl-settings.md` ・ `tbl-row-id-prefixes.md` と、`src/` の生成物（`SCHEDULE_COLOURS` ほか）を刷る |
| `tools/generate_entity_types.py` の `SCHEDULE_COLOURS` の名簿（`:1637-1642`。`CR-551` が `S-326` ・ `S-327` を足した後） | `'S-364'` を足す —— 描き手の `themed('S-364')` が引く |
| 同 `NOT_STORED_TARGETS`（`:1102-`） | `'NOT_STORED_DEADLINE_MARK_SIZES': (['S-365', 'S-366', 'S-367'], DRAWN_INTO_THE_EXPORTED_PICTURE)` を足し、刷る先を `schedule-layout.ts`（`OC-9` の幅に `S-365`）と、形を組むファイル（`CR-554` の前は `schedule-geometry.ts`、後は `task-figures.ts`（`UF-144`））の 2 つにする。⚠️ `CR-554` の 3 節は「区画は読む手書きのコードと同じファイルに刷る」—— 兄弟が入口の名を読むと輪になるので、`task-figures.ts` に刷ること |
| `sample-schedule/*.xml` | 触らない。6 つの見本（`sample-large-erp-program.*.xml` ほか）は `<Deadline>` を 4 つずつ持つ（13 節）—— 当てた後に開けば印が見える |

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | 仕様 ↔ 配置（`schedule-layout.ts`） | `TaskPlacement.deadlineX: number \| null` —— `deadline` の日を `xOnTimeAxis`（予定の `start` と同じ関数）で置いた x。`deadline` が `null` か読めないときは `null`。d ＝ `markerDiameterOf(kind, font, settings)`。`occupiedX0 = min(既存, deadlineX − d × S-365 ÷ 2)`、`occupiedX1 = max(既存, deadlineX + d × S-365 ÷ 2)` —— 形状と印を 1 つの区間にする（`OC-9`）。縦の占有は変えない |
| S-2 | 配置 ↔ 形（`CR-554` の前 `schedule-geometry.ts` の `taskGeometryOf`、後 `task-figures.ts`） | `TaskGeometry.deadline?: DeadlineGeometry \| null`（無いときは `null` と読む —— 手で組んだ形は言わないことがある。`hasPlanDates` と同じ）。`DeadlineGeometry = { readonly outline: Path; readonly haloWidth: number }`。`outline` は 7 点（軸の左上 → 軸の左下 → 矢じりの左 → 先端 → 矢じりの右 → 軸の右下 → 軸の右上）、先端 ＝（`deadlineX`、箱の下端）。箱の中心の y: `SH-1` ／ `SH-2` ／ `SH-5` は予定の中心（`markerOf` の中心と同じ y）、`SH-3` ／ `SH-4` は 1 段目の中線（`labelTierMiddleOf`）。`haloWidth` ＝ d × `settings.labelHaloOfFont`（`S-34`） |
| S-3 | 形 ↔ 描き手（`schedule-task-figures.ts` ・ `svg-renderer.ts`） | `<polygon points="…" fill="${themed('S-364')}" stroke="${themed('S-146')}" stroke-width="${haloWidth}" paint-order="stroke" data-figure="${taskKey}-deadline"/>` を `deadlineParts` ／ `deadlinePartsPinned`（ピン止めの行、マーカーと同じ分け方）に積む。`svg-renderer.ts` の重ね順の列（`:497-528`）で `zoLayer('ZO-13', [...figures.deadlinePartsPinned, scrolling(figures.deadlineParts)])` を `ZO-4` と `ZO-8` のあいだに置く。⛔ `planVisible` ・ `actualVisible` ・ `progressMarkerVisible` で止めない（`DA-1`） |
| S-4 | 形 ↔ 当たり（`item-hit-area.ts`） | `TaskGeometry.deadline` を読まない。印の上の点は、印が無いときと同じ答えを返す（`DA-6`） |
| S-5 | 仕様 ↔ 生成器 | 4.3 節の 2 行。定数は行 ID で引く（`NOT_STORED_DEADLINE_MARK_SIZES['S-365']`） |
| S-6 | 仕様 ↔ ツールチップの字（`src/adapter/screen-renderer/tooltips.ts` の `taskHint`、`:84-87`） | 1 行目は今のまま `${name} ${start} / ${finish}`。`task.deadline` が `null` でないときだけ、`\n` の後に `${word} ${dateText(task.deadline)}` を続ける。`word` ＝ `displayWords.properties` の `rowId === 'PR-10'` の `label[language]`（`language` は `tooltipsFromScreenView` が既に持つ `displayLanguageOf(session)`）。`Tooltip.text` の型は `string` のまま |
| S-7 | ツールチップの字 ↔ 描き手（`src/framework/dom-screen-surface/tooltips-drawing.ts`、`:44` の `textContent`） | `tip.text` を `\n` で割り、1 片を 1 行の要素として縦に積む（`:124-126` の読み出しの行と同じ作り）。各行は 表 T-028 の `IN-7` のとおり折り返さない。`\n` を含まない字は今と同じ 1 つの要素 |

---

## 6. グラフ（`61bbd572` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 対象 | 要求 / 参照 | 指している要求と扱い |
|---|---|---|
| `FR-045` | 1 / 2 | `FR-003`（表 T-038 の `OC-9` の行、`:1519` から）と、`05-07-design.md:383` の 表 T-075 の `UF-6`。⭐ どちらも書き直しで偽にならない |
| `OC-9` ・ `PP-7` ・ `A-5` ・ `PR-10` | 0 / 0 | 浮いている。`PP-7` は 表 T-274 の中の行 |
| 表 T-038 | 13 件（2 次 40） | `FR-001` ・ `FR-094` ・ `FR-002` ・ `FR-003` ・ `FR-009` ・ `FR-016` ・ `FR-109` ・ `FR-018` ・ `FR-055` ・ `FR-049` ・ `FR-059` ・ `FR-090` ・ `FR-039` —— 表を指すだけで `OC-9` の中身を引かない。⚠️ `FR-055` の全体表示は印の幅を数えるようになる（決定 9 が頼る） |
| 表 T-020 | 6 件（2 次 19） | `FR-081` ・ `FR-011` ・ `FR-084` ・ `FR-016` ・ `FR-104` ・ `FR-110` —— どれも「表 T-020 に従う」と指すだけで、順の数を引かない。`ZO-8` ・ `ZO-3` を名指すのは 表 T-274 の `PP-7` だけ（`:416`） |
| 表 T-274 | 0 件（要求以外 1） | 結びの MUST NOT が E-06 を求める（0 節 ② の ③） |
| `FR-110` | 3 / 8 | 書き直さない。STATEMENT の「名称・担当と進捗・進捗マーカーは、線と形より手前」を `ZO-13` は守る |
| 表 T-023a | 12 件（2 次 48） | 結びの 1 文を足すだけ。条件と結果の行は変えない |
| `FR-015` | 5 / 12 | 書き直さない（E-07 は同じ段に 1 文を足すだけ） |
| 表 T-076 ・ `EP-5` | 6 件（2 次 25）・ 2 / 4 | `FR-098` ・ `FR-080` が `EP-5` を指す —— 列挙に 1 語を足しても偽にならない |
| 表 T-103 | 8 件（2 次 31） | 行を足すだけ。`FR-080` の「表 T-103 の `U-` 行は、本表に行を持つか、上の 4 つのいずれか」は E-08 で満たす |
| `FR-092` ・ `EZ-6` | 9 / 19 ・ 5 / 13 | `EZ-6` を E-10 〜 E-12 が書く。指しているのは `FR-002`（`:1407-1433`。`ND-5` の注「`EZ-6` の `YYYY-MM-DD` は全文を読ませる場なので 4 桁のまま」—— 期限の日付も同じ書き方なので偽にならない）・ `FR-048`（`:4870`）・ `FR-082`（`:4913`）・ `FR-080`（`:5548`、`EP-15` —— 書き出しにツールチップを描かない）。どれも行の数を引かない |
| 表 T-040 ・ 表 T-028 | 3 件（2 次 13）・ 18 件（2 次 46） | 表 T-028 は書き直さない —— `IN-3`（出し方）と `IN-7`（各行を折り返さない）は 2 行目にもそのまま当たる |
| `PR-10` | 0 / 0 | 辞書の語を引くだけ |
| 表 T-236 ・ `S-146` | 12 件（2 次 45）・ 3 / 10 | 行を足すだけ。`FR-041` の「画面の色は 表 T-236 に従う」が `S-364` を覆う |
| 表 T-206 | 44 件（2 次 43） | 行を足すだけ |
| `FR-094` ・ `S-22` ・ `S-34` | 8 / 32 ・ 3 / 9 ・ 0 / 1 | 書き直さない。`DA-2` ・ `DA-3` が値を引くだけ |
| `FR-003` ・ `FR-055` | 9 / 16 ・ 10 / 28 | 書き直さない |

⭐ 導いた条項ごとに、届いた行を `rulings.md` で引いた（規則 02 の 1、handoff.md 0.2 の 3 段）: `期限` ・ `deadline` の語で 2 行 —— `JDG-282`（無関係）と `JDG-395`（期限の欄を空で確定すると `null`。本書の `DA-1` の「`null` なら描かない」と噛み合う）。`JDG-370`（マーカーの既定を隠す）が決定 7 の根拠の 1 つ。`JDG-383` ・ `JDG-397` ・ `JDG-405`（色の欄）は決定 14 で当たらないと判じた。緑・矢印・印の語で当たる裁定は、ほかに無い。

### 6.2 `induced.py`（6 群）

⚠️ この作業木では `check.sh` を打てない（共通の依頼文）ので、`induced.py` を、作業木の原稿と、根の checkout が持つ StrictDoc の書き出し（`scratch/spec-check/sd-out/json/index.json`、`7dbd292d`）で走らせた（13 節）。Parent の関係は本書の触る要求で `ebc71984` と変わらない。

| 種 | 解決 | 種の中の辺 | 閉路 | 扱い |
|---|---|---|---|---|
| `FR-045 OC-9 T-038 FR-003 FR-055` | 5/5 | 5 | 1: `FR-003` `FR-055` | 本書はどちらも書き直さない |
| `T-020 ZO-8 ZO-3 FR-110 T-274 PP-7` | 6/6 | 4 | 0 | — |
| `T-076 EP-5 FR-080 T-041 WY-3` | 5/5 | 5 | 0 | — |
| `T-023a FR-015 FR-092 EZ-6 T-267` | 5/5 | 0 | 0 | — |
| `FR-094 S-22 S-34 T-271 XS-3 XS-4 XS-8 FR-109` | 8/8 | 7 | 1: `FR-094` `S-22` | 値の行が持ち主の要求を指す作法の輪。本書はどちらも書き直さない |
| `T-236 S-146 S-152 FR-041 FR-030` | 5/5 | 1 | 0 | — |

---

## 7. 数の予測（`61bbd572` で測った。当てた後に同じ数え方で突き合わせる）

| 数 | `ebc71984` | 前（`61bbd572`、`CR-551` の後） | 本書の後 | 内訳 |
|---|--:|--:|--:|---|
| tables | 186 | 187 | 188 | 表 T-304 |
| figures | 27 | 27 | 27 | — |
| rows | 2282 | 2305 | 2318 | 足す 13（`DA-1` 〜 `DA-7`、`ZO-13`、`U-63`、`S-364` 〜 `S-367`）。`CR-552` ・ `CR-553` が動かす数はその 7 節に従う |
| uids | 162 | 162 | 162 | — |
| 接頭辞 | 162 | 163 | 164 | `DA` |

⚠️ 検査 39（MUST の条文の網羅）の数が動く見込み —— 表 T-304 に MUST 13 ・ MUST NOT 3、`OC-9` に MUST 2、表 T-023a の結びに 0（`DA-6` を指すだけ）、`EZ-6` に MUST 4 ・ MUST NOT 1（E-10 〜 E-12）。⛔ 基準線を動かすときは、利用者に先に問うこと（記録 `baseline-moves-need-user-ok`）。

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ 仕様の編集（4 節）は 1 つの体が 1 つの worktree で当てる。⭐ `SCHEDULE_COLOURS` の名簿は生成器にあり、`S-364` を原稿に足しただけでは描き手に届かないので、**生成器の 2 行（4.3 節）は仕様の波に入れる。**

```
wave 0  spec + generator lists (ONE body, one worktree cut after CR-554 landed)
          section 4 (E-01..E-12, J-01..J-03), tools/generate_entity_types.py (4.3: SCHEDULE_COLOURS += S-364,
          NOT_STORED_DEADLINE_MARK_SIZES -> schedule-layout.ts and task-figures.ts)
          npm run gen && npm run gen:check
          -> tests/unit/cr-430-t-020-the-order-things-are-drawn.test.ts goes red here, by design (13 rows -> 14)
wave 1  (parallel, disjoint folders; cut from wave 0's commit; seams S-1..S-7 copied verbatim into the briefs)
  1a entity/layout-engine   schedule-layout.ts (entry: deadlineX, occupiedX0/X1, the :953 comment),
                            task-figures.ts (taskGeometryOf: DeadlineGeometry), schedule-geometry.ts (entry: the type)
  1c tooltip (JDG-448)      src/adapter/screen-renderer/tooltips.ts (taskHint, S-6),
                            src/framework/dom-screen-surface/tooltips-drawing.ts (split on \n, S-7)
  1b adapter/svg-renderer   schedule-task-figures.ts (deadlineParts / deadlinePartsPinned),
                            svg-renderer.ts (zoLayer 'ZO-13' between ZO-4 and ZO-8)
wave 2  tests by a spec-only tester (never a wave-1 body), reading docs/spec only, on the MERGED tree
wave 3  check.sh + vitest on the merged tree; npm run build (dist/) for the user to try by file:// with a sample XML
```

- ⚠️ `CR-554` の前に当てるなら、1a の所は `schedule-layout.ts` の `layoutFromSchedule` の行のループ（`:892-960`）と `schedule-geometry.ts` の `taskGeometryOf`（`:949-1008`）になる。`CR-552` が同じループの直前（`:880` の `.filter`）を、`CR-553` が `bandFloorOf`（`:817`）と `:1002-1006` を消す・変える —— 塊は重ならないが同じファイルである。
- ⚠️ 1a と 1b は `TaskGeometry.deadline` を分けて持つ。継ぎ目 S-2 ・ S-3 を両方の依頼文へ逐語で写す。
- ⚠️ 着地の報告には、枝・commit・sha を添えること（記録 `integration-branch-visibility`）。利用者は根の `dist/index.html` を `file://` で試す。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

| 所（`CR-554` の前 → 後） | 何をする | 試験（いま引いているもの） |
|---|---|---|
| `schedule-layout.ts` の `TaskPlacement`（`:42-77`）→ 入口のまま | 欄 `deadlineX` を足す（S-1） | 手で `TaskPlacement` を組む試験は 0（`occupiedX1:` を書く試験が 0 本） |
| `schedule-layout.ts` の `layoutFromSchedule` の行のループ（`:892-960`。`#9` の閉包）→ 入口のまま（`CR-554` の 4.3 の S3） | `deadlineX` を求め、`occupiedX0` ／ `occupiedX1`（`:954-956`）に印の幅を合わせる。`:953` の注を「OC-8 is not counted yet」に | `tests/unit/t-038-oc-2-labels-in-the-occupied-width.test.ts:254`（注だけ —— 場面の `Task` は期限を持たない。緑のまま）・ `occupiedX` を引く 6 本（`layout-engine.test.ts` ・ `fr-055-vertical-fit.test.ts` ・ `uf-47-48.test.ts` ほか。期限を持つ `Task` が無ければ緑のまま） |
| `schedule-layout.ts` の `markerDiameterOf`（`:400`）→ `shape-tiers.ts`（`UF-137`） | 読むだけ（変えない） | — |
| `schedule-geometry.ts` の `TaskGeometry`（`:102-120`）→ 入口のまま | 欄 `deadline?` と型 `DeadlineGeometry` を足す（S-2）。`MarkerGeometry` と同じく書き出すが、表 T-064 の `PI-6` に足さない（検査 26b は表の名が出ていることだけを見る） | 手で `TaskGeometry` を組む 2 本（`tests/unit/cr-430-cross-section-scene.ts` ・ `t-023d-the-plan-start-is-the-boundary.test.ts`）—— 欄が任意なので緑のまま |
| `schedule-geometry.ts` の `taskGeometryOf`（`:949-1008`）・ `markerOf`（`:526`）・ `labelTierMiddleOf`（`:517`）→ `task-figures.ts`（`UF-144`） | 印の 7 点と縁の太さを組む（S-2） | — |
| `src/adapter/svg-renderer/schedule-task-figures.ts`（`:333-338` の描く物の列、`:443-458` のマーカーの隣） | `deadlineParts` ／ `deadlinePartsPinned` を足す（S-3） | `tests/unit/cr-432-svg-renderer-branches-the-spec-decides.test.ts`（層の中身を数える —— 期限を持つ場面が無ければ緑のまま） |
| `src/adapter/svg-renderer/svg-renderer.ts:497-528`（重ね順の列） | `ZO-13` を `ZO-4` と `ZO-8` のあいだに（S-3） | ⛔ `tests/unit/cr-430-t-020-the-order-things-are-drawn.test.ts:138-143`（「表が 13 行、順が 1 〜 13」）→ 赤。14 に。`:145-157`（表のどの行も描かれた層である）→ 場面（`richScene`、`:67`）に期限を持つ `Task` を足さないと `ZO-13` が描かれず赤 ・ `tests/unit/t-020-zo-layer-membership.test.ts`（`ZO-13` の中身の試験を足す） |
| `src/entity/layout-engine/item-hit-area/item-hit-area.ts` | 触らない（S-4） | 足す: 印の上の点が、印の無いときと同じ答えを返す |
| `src/adapter/screen-renderer/tooltips.ts` の `taskHint`（`:84-87`、`CR-554` は割らない） | 期限の行を足す（S-6）。注 `// see EZ-6` はそのまま | `tests/unit/cr-439-tooltips-text-assignment-and-hover.test.ts:84-87` ・ `:113-116`（`EZ-6` の逐語の一部と、名前が打ち切られないこと —— `toContain` なので緑のまま）・ `tests/unit/cr-541-panels-help-tooltip-and-dialogue.test.ts:276-`（手で組んだ `text: 'A task 2026-09-01 / 2026-09-12'` の置き方 —— 緑のまま）・ ⚠️ `tests/system/user-reported-fixes.test.ts:312-316`（`tellingIn` が説明の全文を「名前 日付 / 日付」の 1 行として `$` まで照らす —— 期限を持つバーに止めると赤。いまの試しの文書が期限を持たなければ緑のまま。期限の行を許す形に直すかは試験の体が決める）・ `tests/unit/cr-550-dual-cursor-readout.test.ts:354-`（モードのあいだ出さない —— 緑のまま） |
| `src/framework/dom-screen-surface/tooltips-drawing.ts:44`（`textContent`） | `\n` で割って行ごとに積む（S-7） | 同上。足す: 期限を持つタスクで 2 行、持たないタスクで 1 行、2 行目の語が表示言語で `期限` ／ `deadline`、日付が `YYYY-MM-DD` |
| 仕様を逐語で引く試験 | — | `tests/contract/cr-441-t-274-principles-and-rows-both-ways.contract.test.ts`（表 T-274 と各表の行を両向きに突き合わせる —— E-04 と E-06 を同じ波で当てれば緑）。`FR-045` を引く 2 本（`tests/unit/edit-task.test.ts:610` ・ `fr-033-the-copy-store-is-inside-the-app.test.ts:20`）は注だけで緑のまま。`EP-5` を引く 4 本（`uf-32.test.ts` ・ `fr-043-dummy-drawn.test.ts` ・ `uf-39-40.test.ts` ・ `uf-54.test.ts`）は列挙の逐語を持たないので緑のまま |
| `tests/unit/t-024a-op-10-first-day-covered.test.ts:558` | — | 期限を数えないことを主張する（`OP-10`）—— 本書はそれを変えないので緑のまま |

**新しく要る試験**（仕様だけを読む試験の体へ）: `DA-1`（`null` で描かない、`S-227` ／ `S-228` ／ `S-63` を偽にしても描く、畳んだ行では描かない）、`DA-2`（箱の高さ ＝ マーカーの径、3 つの比）、`DA-3`（塗り `S-364`、縁 `S-146` と d × `S-34`）、`DA-4`（`finish` ＝ `deadline` で先端が予定の右端、非稼働日のまま）、`DA-5`（形ごとの縦）、`DA-6`（押しても止めても印が無いときと同じ）、`DA-7`（`Row Area` の外で切れる）、`OC-9`（期限が遠い `Task` と、あいだに入る `Task` が別の段になる）、`ZO-13`、`EP-5`（書き出しに印がある、`WY-3` の外接矩形の一致）、`EZ-6`（期限を持つタスクだけ 2 行目、語は `PR-10`、日付は `YYYY-MM-DD`）。

---

## 10. ⛔ この変更でやらないこと

- **印を掴んで期限を動かすこと** —— 逐語は表示だけを求めている（決定 8）。掴ませるなら 表 T-023d ・ 表 T-266 ・ 表 T-270 に行を足す別の変更要求になる。
- **印そのもののツールチップ** —— 利用者の答え（`JDG-448`）はバーのツールチップに足すことであり、印に当たりを持たせることではない。
- **印を隠す切り替え** —— 決定 7。
- **期限の超過の色分け・警告** —— `FR-045` の RATIONALE が遅れと分けている（決定 15）。
- **`FR-094` の STATEMENT に 表 T-206 の寸法を書き足すこと** —— `CR-551` の遅れの記号と同じ扱いにした（0 節 ② の ⑤）。2 本とも揃えて書くなら、別の巡で 1 度に。
- **遅延診断**（別のセッションが起草中）—— ⭐ **共有の継ぎ目の申し送り**: ① 表 T-236 に色の行を足す（本書は `S-311` の後、`CR-551` は `S-223` の後。遅延診断のボトルネックの色も同じ表）。② 表 T-020 に層を足すなら、`ZO-13` の後の「順」をもう 1 度詰め直す（E-04 と同じ段の注 E-05 には「遅れの日数」が残る）。③ 表 T-038 の `OC-8`（遅れの日数）はまだ数えていない —— 本書は `OC-9` だけを書く。④ 期限を診断に使うかは遅延診断の側が決める（本書は `FR-045` の「遅れに含めない」を変えない）。
- `docs/development-records/` の `pending-decisions.md` ・ `changelog.md`、`A-appendix.md` の変更履歴（当てる者が書く）。

---

## 11. 前に立つ者へ返す問い

| # | 問い | 推奨と理由 | 答え |
|---|---|---|---|
| 1 | **印の縦の置き場**（`DA-5`）。例: 矩形のタスク「設計」（予定 3/2 〜 3/13、期限 3/13）で、緑の矢印をどこに立てるか。**A**（本書の形）バーと同じ高さの中心に重ねて描く —— 帯高も段も変わらない。バーの上に重なるので地の色の縁で読ませる（決定 4）。線だけの形では 1 段目に立ち、先端が予定の線を指す。**B** バーの上の隙間に立て、先端をバーの上端に付ける —— バーと重ならず「ここが期限」と指す形になるが、期限を持つ行の帯高が矢印の高さぶん増え（表 T-038 に縦の占有の行が要る。`OC-10` と同じ作り）、期限を入れるたびに行が伸びて見ている場所が動く | **A**。① 帯高と段割当を 1 つも変えない（`FR-003` ・ `FR-055` の数え方がそのまま）。② 期限を入れるたびに行の高さが変わらない —— `OC-10` が「名前を打つたびに行の帯高が変わると、見ている場所が動く」と退けたのと同じ理由。③ 戻すときは `DA-5` の 1 行 ＋ 縦の占有の 1 行。⚠️ 当てた後、見本の XML（`<Deadline>` を持つ 6 本）で実物を見てもらう | **「矢印の縦の置き場 → 推奨どおり A」**（`JDG-447`）⇒ 本書の `DA-5` のまま |
| 2 | **印にポインタを止めたとき、期限の日付を出すか**。例: 期限の矢印が予定の右端の近くに立っていて、3/13 か 3/14 かを目盛で読みにくい。**A**（本書の形）出さない —— 印は当たらず、下のバーに止めれば今の `EZ-6`（名前と `start` ／ `finish`）が出る。**B** `EZ-6` に 3 つ目の日付として期限を足す（期限を持つタスクだけ）。**C** 印そのものに当たりを与え、印だけの説明を出す | **A**。逐語は表示だけを求めている。B は `EZ-6` の 1 行（`FR-092`）を書き換えれば足りるので、使ってみて要ると分かってから足せる。C は `DA-6` と 表 T-023d ・ 表 T-267 に行が要り、細い印に当たりを持たせると下のバーの予定の終了（`GA-2`）を掴みにくくする | **「矢印にポインタを止めたとき → B バーのツールチップに足す」**（`JDG-448`）⇒ 推奨と違う。E-10 〜 E-12 で `EZ-6` に期限の行を足す。印は当たらないまま（`DA-6`） |
| 3 | **緑の値**。`S-364` ＝ 明るいテーマ `#1f7a3d` ／ 暗いテーマ `#6fc98d`（`S-152` の良の色と同じ値、地に対して 5.37 : 1 ／ 8.95 : 1）。形の比 `S-365` ＝ 0.75、`S-366` ＝ 0.5、`S-367` ＝ 0.25（既定の矩形で矢じりの幅 10.5px、全体の高さ 14px） | 提案どおり当て、使い勝手の試しで 1 行ずつ直す（`CR-551` の問い 4 と同じ扱い） | **「色と形の値 → 提案どおり」**（`JDG-449`）⇒ J-01 ・ J-02 のまま |

---

## 12. 台帳

⭐ 行の写しは作業の場の `CR-556-ledger.md` にある（前に立つ者が `rulings.md` ・ `defects.md` へ写す）。

### 12.1 裁定（`docs/development-records/rulings.md`）

- `JDG-446` —— 0.1 節の逐語。状態は「指示」（本書を当てたら「適用済」へ。当てる者の仕事）。
- 覆す古い行は無い。
- `JDG-447` 〜 `JDG-449` —— 11 節の問い 1 〜 3 への利用者の答え（逐語は 11 節の答えの欄）。状態は「指示」。

### 12.2 欠陥（`docs/development-records/defects.md`）

| ID | 何 | 状態 |
|---|---|---|
| `DFC-861` | `FR-045`（MUST）が期限の印を求めるが、`src/` のどこも描かず、表 T-038 の `OC-9`（MUST。左右に算入）も数えていない（`schedule-layout.ts:953` の注が自ら「not counted yet」と書く）。形・色・置き場を決める行が仕様にも無い | `仕様待ち`（本書の E-01 〜 E-03） |

⚠️ 起こさなかったもの: ① `DFC-675`（要求 28 件が `src/` に名を持たない）—— `FR-045` はその 1 件であり、本書を当てれば 27 件になる。行は変えず、当てる者が数を直す。② `CR-554` の 9 節の候補 `DFC-830`（表 T-075 の `UF-6` が `FR-045` を負うのにコードが 0 行）—— `defects.md` にはまだ無い（`grep -c DFC-830` が 0）。本書を当てれば `FR-045` のコードが `task-figures.ts`（`UF-144`）に立つので、`CR-554` の移し替え（`UF-6` に `FR-045` が残る）を当てる者が見直すこと。③ 依頼文の「表 T-012 に `:4000` 近くの注がある」は、実物では 表 T-020（重ね順）の注である。

---

## 13. 測り方の再現

```
# totals (before)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   -> tables=186  figures=27  rows=2282  uids=162   (ebc71984)
#   -> tables=187  figures=27  rows=2305  uids=162   (61bbd572, after CR-551 landed)

# free identifiers
for p in S ZO U XS DL RV EP OC JDG DFC PND T; do
  git grep -ohE "\b$p-[0-9]+[a-z]?\b" -- docs/spec src tests tools change-request docs/development-records \
    | sed "s/$p-//;s/[a-z]$//" | sort -n | uniq | tail -1
done
#   -> S 359 (999 is a test sentinel), ZO 12, U 62, JDG 440, DFC 854, PND 530, T 319 (T-301/T-306/T-319 are
#      old change-request words; the spec's own maximum is T-296, T-297 in CR-551's draft)
git grep -lE "\bS-36[4-7]\b|\bT-304\b|\bDA-[0-9]+|\bZO-13\b|\bU-63\b|\bJDG-44[6-9]\b|\bDFC-861\b" \
  -- . ':!previous-project-result'
#   -> nothing but this file (61bbd572)

# nothing draws the deadline today
git grep -n -i "deadline\|FR-045" -- src/entity/layout-engine src/adapter/svg-renderer
#   -> schedule-layout.ts:953 (the comment) only  (61bbd572)
grep -c -i "deadline\|期限" docs/review/progress-check-2026-09-23.md docs/review/bottleneck-detection-2026-09-20.md
#   -> 0, 0
grep -c "DFC-830" docs/development-records/defects.md
#   -> 0
grep -c "<Deadline>" sample-schedule/*.xml
#   -> 4 in each of the six sample-*.xml, 0 in the seventh XML sample

# graph
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>      # each ID of 6.1
# induced.py needs the StrictDoc export, which check.sh writes and this worktree may not run;
# a scratchpad wrapper imports graph.py, points G.SD_JSON at the root checkout's export
# (scratch/spec-check/sd-out/json/index.json, 7dbd292d) and calls induced.main() per group of 6.2

# contrast (scratchpad cr556_contrast.py): WCAG 2.1 relative luminance of the #rrggbb / hsl() values,
# theme hue 214, ratio = (L1 + 0.05) / (L2 + 0.05)
#   #1f7a3d vs #ffffff 5.37 | vs S-155 hsl(214 46% 80%) 3.17 | vs S-157 hsl(214 62% 34%) 1.52 | vs #1f8340 1.12
#   #6fc98d vs hsl(214 12% 9%) 8.95 | vs S-155 dark hsl(214 32% 26%) 5.28 | vs S-157 dark hsl(214 62% 64%) 1.41
#   | vs #6adc90 1.18

# every old block of section 4.1 occurs exactly once in its file (CRLF normalised), all 12 apply in order to
# COPIES, and J-01..J-03 applied to copies load with json.loads (scratchpad cr556_apply.py)
#   -> ok E-01 .. E-12 on a copy of 61bbd572, problems 0 edits 12; E-02 MUST 13 MUST NOT 3; E-03 MUST 2;
#      on 61bbd572: T-236 rows 36 -> 37, T-206 rows 144 -> 147, prefixes 163 -> 164, duplicate setting ids 0
# overlap with the sibling change requests: each old block of 4.1 searched in CR-551, CR-552, CR-553
#   -> 0 hits each
```
