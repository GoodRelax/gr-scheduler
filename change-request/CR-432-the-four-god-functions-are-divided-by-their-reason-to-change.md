# CR-432 — 神関数 4 つを「変更の理由」で割り、その基準を仕様へ置く

> ⭐ **状態: 一部を当てた（2026-09-21、`60a2e3d7` の上）。**
> 当てたのは 表 T-276・接頭辞 `UD`・`05-07-design.md:272` の文の 3 つである。
> 表 T-075 の行・表 T-074 の `SU-3`・表 T-063 の `UT-8` と `UT-9` は、各波の分割のコミットが足す（8 節・11.1）。
> ⭐ 利用者は 9 節の問いに 2026-09-21 に答えた（3 つの裁定。9 節）。
> 読んだ木: `f73f1ec3`（ブランチ `refactor`）。
> ⭐ 4〜11 節は、2026-09-21 に `2112d0c9` で測り直して書き直した（何を変えたかは 14 節）。
> **行番号・行数・分岐数はすべてこの木で自分で測ったものである。**
>
> **開くもの**: 計画の段 4（`editTask` / `editTaskGroup` / `svgFromSchedule` を割る）の入口。
> ⛔ `builtMerge` は利用者の裁定で段 4 から外した（4.5）。
> 表題の「4 つ」は、起草したときの数である。
> `JDG-48` により、`src/` に新しいファイルを置くには先に 表 T-075 がその行を持たねばならない。
> **本書は割らない。  
> 割ることを合法にし、どの形で割るかを定める。**
>
> **⛔ 本書が絶対にしないこと**: `.claude/skills/spec-graph-check/function-size-baseline.txt` の編集。
> 基準線は分割そのもののコミットが書き換える（7 節）。
>
> **正本**: `docs/development-records/refactor-plan-report-2026-09-13.md` の状態表の段 4 の行と、同書の 2 節（なぜ神が生まれたか）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⚠️ **1 つも前へ進めない。  
利用者が画面で気づく変化は 0 である。**  
表 T-054 の `CH-1` 〜 `CH-6` のどれにも、1.3 の `GL-001` 〜 `GL-008` のどれにも、本書は直に結ばない。  
本書は**作り方の側の手入れ**である。  
隠さずに書く —— 検査 22 は「どれも進めないなら、そう平らに言え」と求めている。

⭐ ただし間接の結びは 1 つある。  
`docs/review/why-a-fix-costs-what-it-does-2026-09-10.md` の測定では、1 つのファイルが全コミットの 4 本に 1 本を占めていた。  
直しの値段は、次にどの課題を選べるかを決める。

### ② レビュー観点のどの条項を当て、何が出たか

- `R2.2`（設計 ・ **MUST** ・ SRP —— 1 ユニットが単一の変更理由）
  —— `docs/development-rules/07-review-standards.md:508`。
  ⛔ **4 つとも違反している。**  
  `editTask` の `switch` は 20 個の `case` を持ち、
  表 T-075 がそのファイルに負わせる 6 件の要求は、5 つのまとまりに分かれる（4.2 節）。
- `R7.9`（構造 ・ **SHOULD** ・ 純粋性でユニットを分割）
  —— 同書 `:551`。
  ⭐⭐ **ここが本件の芯である** —— `docs/spec/05-07-design.md:272` は
  「ユニットを割る基準は純粋性である」と書き、**SHOULD の条項を唯一の基準に据えて、MUST の条項を 1 行も書いていない。**
  計画の 2 節が「ユニットを割る基準を純粋性だけにした」を神が生まれた原因の筆頭に挙げているのは、この 1 文のことである。
- **`R1.3`（唯一の正がある）** —— 割る基準の正が仕様に無いまま、
  表 T-063 の `UT-2` / `UT-4` / `UT-6` / `UT-7` は 4 つとも「**純粋性ではない**」と書いている。
  ⇒ **基準の文と、基準を適用した 4 つの結果が、食い違ったまま 4 版ぶん並んでいる。**
- **`R2.9`（要らないうちは作らない）** —— 6 節でユニット数の増え方に当てた。
- **`R2`（名前を足すときは必ず引く）** —— 接頭辞 `UD` を 1 つ足す。  
既存 160 件と衝突しないことを測った（1 節 (l)。  
2026-09-21 に `2112d0c9` で 3 度目の測り直しをした）。

### ③ 利用者に問わずに決めたこと

⭐ 下の 7 つは根拠を添えて**利用者に問わずに決めた**（あとから覆せる）。

1. **新しい表を立てた**（3 節）—— 表 T-063 は
   「表 T-063 が持つのは、割った理由だけである」と自分で宣言しており（`05-07-design.md:309`）、
   基準そのものを入れるとその宣言が偽になる。
   表 T-074 は語の定義の表である。  
   ⇒ どちらにも入らない。
2. **行 ID の接頭辞を `UD` にした** —— 起草の時点では `UC` `US` `SB` `SX` と同じく、どの木も使っていなかった。
   当てる直前（2026-09-21、`60a2e3d7`）に測り直し、未登録であることを確かめてから登録した。
   「`U`nit `D`ivision」が読める（1 節 (l)）。
3. **表の番号を 表 T-276 にした** —— 木が使う表の最大は `T-277` である（2026-09-21 に `60a2e3d7` で 4 度目の測り直しをした）。
   `T-276` は当てる直前に `docs/spec` で 0 件であり、別のセッションの `CR-435`・`CR-436` もこの番号を取らないと書いている。
4. **`editTask` と `editTaskGroup` を同じ波で並行させてよいとした**（8 節）—— 測った結び（1 節 (h)）は 1 本の輸入であり、
   「`edit-task-group.ts` は再び公開し続ける（MUST）」と「`edit-task.ts:33` の取り込み元を書き換えない（MUST NOT）」の 2 つで、2 体が同じファイルを触らなくなる。
   ⚠️ 前の版はこれを別々の波にしていた。
5. **公開エントリの名前と署名を 1 つも変えない**（5 節）—— 表 T-064 は 1 行も動かない。
6. **共変の数え方と閾値を決めた**（4.1 節）—— 0.5 を超え、小さい側が 5 件以上のときだけ継ぎ目を拒む。
   コメントだけの変更とファイルの誕生を除き、残る元のファイルに対する共変は拒む理由にしない。
   ⭐ 較正は、履歴が本物と言う継ぎ目（目盛 0.14〜0.29）と偽と言う継ぎ目（バーと依存線 0.90）の間である。
7. **どの要求の規則も持たないコードは元のファイルに残した**（4.1 節の S4）—— 残すと元のファイルが 1 つの要求の起点を兼ねて大きくなるときだけ出した（`schedule-overlays.ts`）。

---

## 1. 読んで確かめた事実（`f73f1ec3` で、渡された主張を 1 つずつ測り直した）

⛔ **下は、前に立つ者から渡された主張と、本書が自分で測った数の突き合わせである。**  
⚠️ **1 つは成り立たず、2 つは数が合って意味が違った。**

| # | 渡された主張 | 測った結果 | 判定 |
| --- | --- | --- | --- |
| (a) | 基準線の 4 行の数は**超過**（帯を超えた分）である | ⛔ **実寸である。<br>** 同じファイルは `agent-api-members.ts::agentApiMembers` を `branches=0` と、`mspdi-codec.ts::tasksInWbsOrder` を `lines=31` と書いている。<br>超過なら前者は帯を超えておらず、後者は行で超えていないことになり、どちらも `HELD` 行を持てない | ⛔ **成り立たない** |
| (b) | `svgFromSchedule` は 615 / 126 | 実寸 **615 行 / 126 分岐**（`src/adapter/svg-renderer/svg-renderer.ts:579-1193`）。<br>超過は **565 行 / 111 分岐** | 数は合う。<br>意味が違う |
| (c) | `editTask` は 486 / 101 | 実寸 **486 行 / 101 分岐**（`src/use-case/edit-document/edit-task.ts:516-1001`）。<br>超過は **436 / 86** | 同上 |
| (d) | `editTaskGroup` は 433 / 103 | 実寸 **433 行 / 103 分岐**（`src/use-case/edit-document/edit-task-group.ts:207-639`）。<br>超過は **383 / 88** | 同上 |
| (e) | `builtMerge` は 282 / 56 | 実寸 **282 行 / 56 分岐**（`src/use-case/import-document/import-document.ts:625-906`）。<br>超過は **232 / 41** | 同上 |
| (f) | 検査 60 は 78 の関数を名前で持つ | `grep -c '^HELD'` で **78** | ⭐ **成り立つ** |
| (g) | 段 3 は 4 つの門とも `check.sh` に入った | 検査 19（`:398`）・59（`:626`）・60（`:642`）・61（`:666`） | ⭐ **成り立つ** |
| (h) | `editTask` と `editTaskGroup` は同じコンポーネントに在るだけ | ⛔ **結びがある。<br>** `edit-task.ts:33` が `./edit-task-group` から `tasksRankedByTheRowTree` を取り込み、`edit-task.ts:929`（`moveTaskToTaskGroup`）で使う | ⛔ **弱い。<br>実際の結びは輸入 1 本である** |
| (i) | `src/` に SRP を当てた記録は 0 件 | `docs/development-records/rulings.md` に `SRP` ／ `単一責任` ／ `割る基準` ／ `R2.2` は **0 件** | ⭐ **成り立つ** |

⭐ **渡された表に無く、測って出た事実**

| # | 事実 | 測り方 |
| --- | --- | --- |
| (j) | `R2.2` は **MUST**、`R7.9` は **SHOULD** である | `docs/development-rules/07-review-standards.md:508` と `:551` |
| (k) | 4 つのほかに、同じ 4 ファイルで帯を超える関数が 2 つある —— `svg-renderer.ts::rulerSvg`（79 行 / 13 分岐。<br>`456-534`）と `import-document.ts::mergedDocument`（94 行 / 17 分岐。<br>`516-609`） | 基準線の 51 行目・96 行目と、`function-size.mjs` を 4 ファイルに直に掛けた出力 |
| (l) | 起草の時点で `UD` はどの木にも無かった（`2112d0c9`）。<br>当てたあとの登録の数は、接頭辞 161 件である（2026-09-21 に `60a2e3d7` に `UD` を足して測った）。<br>木が使う表の最大は `T-277` である（2026-09-21 に `60a2e3d7` で 4 度目の測り直しをした） | `docs/spec/_source/row-id-prefixes.json` と、`docs/spec/**/*.md` の `表 T-\d+` の全数 |
| (m) | 基準線の総量は `excess-lines=8412` ／ `excess-branches=952`。<br>`JDG-124` が承認した初期値は 76 関数 ／ 7847 行 ／ 910 分岐である | 基準線の 1 行目と `rulings.md:225`。<br>門を入れる直前の測り直し（`JDG-128`）で動いたものである |
| (n) | 4 つの超過の合計は **1616 行 / 326 分岐**。<br>総量の **19.2% / 34.2%** である | (b)〜(e) を足し、(m) で割った |
| (o) | 割る 4 ファイルを直に取り込む試験は、`edit-task` が 4 本、`edit-task-group` が 1 本、`svg-renderer` が 26 本、`import-document` が 3 本 | `grep -rln` |

### ⛔ 「覆う条項が無い」と書く前に打った 3 つ（規則どおり）

| 打った手 | 結果 |
| --- | --- |
| `grep` で `rulings.md` に 割る基準 ／ SRP ／ `R2.2` を探す | **0 件**。<br>基準についての裁定は無い |
| `impact.py UT-2` | 要求 0 件 ／ 参照 3 箇所（`05-07-design.md:322` `:429` `:916`）。<br>`impact.py SU-3` は 参照 1 箇所（`:461`）、`impact.py UF-19` は **0 箇所** |
| 別の道で届くか | ⭐ **届いた 1 つ** —— 基準線ファイルの書き換えは `JDG-118`（逐語「**(a) Claude Code の許可設定で、この書き換えを許可する。**」）が既に許している。<br>⇒ 7 節はこれを問いにせず、**許しとして引く** |

---

## 2. グラフ（測った先）

⭐ **コンポーネントも辺も図も動かない。**  
割るのはコンポーネントの内側であり、5.3 が「フォルダの外から公開エントリ以外を読んではならない」と定める境界は 1 本も越えない。  
⇒ 検査 19（表 T-061 の依存の規則）と検査 59（コンポーネントをまたぐ辺）は、新しいファイルを 1 つも見ない。  
`docs/spec/_source/components.json` と 図 F-013 は無変更である。

**これは前例と同じ形である** —— `CR-134` が `UT-6` を足したとき、changelog は「コンポーネントも辺も図も動かない —— ユニットはファイルの割り方であってコンポーネントの境界ではない」と書いている。

動く検査は 3 つだけである。

| 検査 | 何が動くか |
| --- | --- |
| 18（`generate_unit_tree.py --check`） | `src/` のファイルの集合が 表 T-075 と一致しなくなるので、**表と `src/` を同じコミットで動かす**（`JDG-48` の言う契機がここである） |
| 60（`check-function-size.py`） | 7 節 |
| 26b（`check-published-members.py`） | 表 T-064 が 1 行も動かないので、**赤にならない**。<br>同検査は表から輸出への一方向しか見ない |

---

## 3. 決定 1 —— 割る基準を仕様のどこへ置くか

### ⛔ いま在る文

`docs/spec/05-07-design.md:272`（逐語）:

> **ユニットを割る基準は純粋性である**（`R7.9`）—— **純粋な側と非純粋な側が同じコンポーネントにあるとき、別のファイルへ出す。**

⚠️ **この 1 文が、本件のすべての原因である。**

- SHOULD の条項（`R7.9`）を唯一の基準に据え、MUST の条項（`R2.2`）を書いていない（1 節 (j)）。
- この文に従うと `InputCommandTranslator` は全部純粋なので割れない —— `CR-125` の §2-4 がそう明示している。
- それでいて 表 T-063 の `UT-2` `UT-4` `UT-6` `UT-7` は 4 つとも「**純粋性ではない**」と書いている。
  ⇒ **基準の文は、それを適用した 4 例すべてに否定されている。**

### 置くもの —— 表 T-276

`:272` の 1 文を、表を指す文に取り替える。  
仕様の作法（要求は表を指し、1 行規則を並べない）に従う。

**当てた文（`05-07-design.md` の 5.3 節）**:

> ユニットは 表 T-276 の基準に従って割ること（MUST）。  
> 基準の筆頭は変更の理由である（`R2.2`）—— 純粋性（`R7.9`）はその次に来る基準であって、唯一の基準ではない。

⭐ **`（MUST）` はこの 1 文の 1 つだけである。**  
表の行は `（MUST）` を持たない —— 規則の強さは表を指す文が 1 つで負う。  
検査 39（逐語の試験を持たない MUST を数える）はすでに赤であり、7 つ書くより 1 つ書くほうが悪化が小さい。

**表 T-276 — ユニットを割る基準**（当てた形）

| 行 ID | 基準 | 規則 |
| --- | --- | --- |
| UD-1 | **変更の理由** | 1 つのユニットは、変更の理由を 1 つだけ負う（`R2.2`）。<br>「変更の理由」とは、そのユニットの中身を書き換えさせうる仕様の出どころ —— 要求 ID・表・裁定 —— である。<br>1 つのユニットの中に、互いに素な出どころから縛られる 2 つのまとまりが在るときは、別のユニットへ出す |
| UD-2 | **純粋性** | 純粋な側と非純粋な側が同じコンポーネントに在るときは、別のファイルへ出す。<br>`R7.9` は SHOULD だが、表 T-060 の `LY-3` が層で同じことを求めるので、本設計では基準とする |
| UD-3 | **純粋性は割らない理由にならない** | 2 つのまとまりの純粋性が等しいことを、`UD-1` に当たるまとまりを割らない理由としない。<br>⚠️ 全部が純粋なユニットでも、変更の理由が 2 つあれば `UD-1` で割る |
| UD-4 | **割り方はコードの位置で裏づける** | 割ることを定める変更要求は、提案するユニットごとに、いま在るコードの `file:line` の範囲を書く。<br>範囲を書けない割り方は採らない。<br>⚠️ 概念だけで割ると、割るはずの処理が思っていたファイルに無いことがある |
| UD-5 | **迷いの試験** | 割ったあと「この欠陥はどのファイルか」に 1 つのファイルで答えられる。<br>2 つ以上のファイルが答になる割り方は `UD-1` を満たしていない |

⭐ **仕様へ写さなかった根拠は、本書に残す**（仕様は変更要求の番号もコミットの sha も持たない。検査 7 と検査 54 が赤にする）。

- `UD-3` の行が要る理由 —— この行が無かったために `InputCommandTranslator` は割られなかった（`CR-125` §2-4）。
- `UD-4` の行が要る理由 —— 概念だけで割って失敗した例が在る。`3d1d9f1` では描く処理が思っていたファイルに無く、3 体が止まった。

⭐ **`UD-2` を基準に据えるのは、いま在る文が既にそう読める形で書かれているからである**（「別のファイルへ出す」に助動詞が無い）。  
**ゆるめていない。**

### ⛔ この表がここに住まねばならない理由

**基準が変更要求の中だけに住むと、同じ忘れ方をもう一度する。**  
`CR-134` は `R2.2` 違反を 1 件見つけ、`UT-6` として割った —— そのとき基準を仕様へ戻していない。  
`CR-140` は `ScreenRenderer` を 10 ユニットに割り、対になる `DomScreenSurface` を 1 ユニットで足した。  
どちらも記録には在るが、**次にファイルが太ったときに誰も読み返さない場所**に在る。

**規則は、機械が読める段に下りるまで守られない。**  
`UD-4` の `file:line` は、変更要求を読む者が確かめられる。  
`UD-1` と `UD-5` は人が判ずる。  
⇒ ⚠️ **本書はこの 3 行に機械の検査を付けない。  
付けられると主張しない。**

---

## 4. 決定 2 —— 3 つをどう割るか（`builtMerge` は外した）

> ⭐ **本節は 2026-09-21 に書き直した。**
> 読んだ木: `2112d0c9`（ブランチ `refactor`）。
> 行番号・行数・共変の数はすべてこの木で測った。
> 書き直す前の本節（16 ファイル）は、4.0 の 2 つの測定で崩れた。
> 書き直しで変えた本書の文は 14 節に並べた。
> ⛔ 利用者の裁定（9 節の ③）で `builtMerge` を段 4 から外した。4.5 はその記録だけを持つ。

⛔ **`UD-4` に従い、提案する全ユニットに `file:line` を添える。**  
⛔ **元のファイルのすべての行が、どこかのユニットに 1 度だけ落ちる。**  
行の割り当ては、4 ファイルそれぞれについて「1 行目から末尾まで、どの行も 1 つのユニットにだけ属する」ことをスクリプトで確かめた（重なり 0、漏れ 0）。

### 4.0 ⛔ 前の 4 節が崩れた 2 つの測定

**(1) 共変（同じコミットで一緒に変わること）が、継ぎ目の多くを偽と言った。**  
前の体は、前の 4 節が提案した行の範囲に `git log -L` を掛けた —— ファイルがまだ無いので、これが手に入る最も近い代わりである。  
本書はそれを測り直した。

| 渡された数 | 測り直した数 | 判定 |
| --- | --- | --- |
| `schedule-bars` と `schedule-links` が 0.86〜0.94 | 4.1 の数え方で **0.90（10 件中 9 件）**。<br>何も除かずに数えると 0.94（18 件中 17 件） | ⭐ **成り立つ** |
| `schedule-ruler` と残りが 0.14〜0.29 | 4.1 の数え方で、兄弟に対して **0.14〜0.29**（依存線 0.14、重ね描き 0.14、バー 0.29）。<br>ただし残る入口に対しては 0.71（7 件中 5 件）、格子に対しては 0.00（4 件中 0 件） | ⭐ **兄弟に対してだけ成り立つ** |
| `task-group-appearance` と `task-group-life` が 1.00（3 件中 3 件） | 何も除かずに 1.00（6 件中 6 件）、コードを変えたコミットだけで 1.00（2 件中 2 件）、そこから誕生を除くと **1.00（1 件中 1 件）** | ⛔ **「3 件中 3 件」はどの数え方でも再現しない。<br>どの数え方でも 6 件以下で、証拠にならない** |
| `merge-*` の組が 0.8〜1.0 | 何も除かずに数えると **0.80〜1.00 を再現した**。<br>⛔ しかし寄与したコミットは、ファイルの誕生（`1d0412b1`・`4a383809`）と、コメントだけを変えた一斉の手入れ（`7361e5e1`・`4d293708`・`be524f4f`・`ac43d02c`）だけである | ⛔ **数は合うが意味が無い。<br>`import-document.ts` は書かれて以来、コードを変えたコミットを 1 つも受けていない** |
| `edit-task` のすべての組が 0.5 以上 | 何も除かずに数えると **再現した**（0.50〜1.00）。<br>4.1 の数え方では、生成と予実が **0.33（6 件中 2 件）**、生成と見た目が **0.00（4 件中 0 件）** | ⛔ **何も除かない数え方でだけ成り立つ** |

⚠️ **渡された 5 つの数は、同じ 1 つの数え方では揃わない。**  
`svg-renderer` の 2 つは誕生とコメントを除いた数え方で、`edit-task` と `merge-*` は何も除かない数え方で再現する。  
⇒ 本書は数え方を 1 つに決め（4.1）、すべての組をそれで測り直した。

**(2) 前の 4 節の範囲は、ファイルを覆っていなかった。**

| ファイル | 前の 4 節のどの範囲にも入らない行 | 渡された主張 |
| --- | --- | --- |
| `edit-task.ts` | **284 行**（`f73f1ec3` の 1,024 行のうち） | 「1,025 行のうち 285 行が割り当て済み」 |
| `svg-renderer.ts` | **350 行**（1,301 行のうち） | 「1,302 行のうち 351 行が割り当て済み」 |
| `import-document.ts` | **642 行**（924 行のうち） | 「925 行のうち 643 行が未割り当て」 |

⚠️ **数は 1 つ違いで再現したが、上の 2 行は向きが逆である** —— 285 と 351 は「割り当て済み」ではなく「どこにも入らない」行の数である。  
⭐ 3 つとも、前の 4 節が関数の外（型・補助・生成された定数）を書いていなかったことを示す。

### 4.1 割り方の手順 —— 要求が継ぎ目を出し、共変が拒み、責務文が確かめる

利用者の目的（2026-09-21 の一通、逐語）は「目的はとにかく変更に強くすること。」である。  
変更要求は要求 ID を名指して来る。  
⇒ ⭐ **割ったあと、要求ごとに AI が最初に開くユニット（表 T-075 の「負う要求」の欄が名指すユニット）が、その要求の規則を持ち、ほかの要求の規則をほとんど持たないこと** —— これを割り方の目的とする。

| 段 | すること | 根拠 |
| --- | --- | --- |
| S1 立てる | 元のファイルが負う要求（表 T-075 の「負う要求」の欄の、区分 `OW-1`・`OW-2` の要求）ごとに、その規則を持つコードのまとまりを 1 つのユニットへ出す案を立てる | `CR-435` の 表 T-277。<br>`OW-4`（横断）は「どう書かれるか」を縛るので継ぎ目を出さない |
| S2 拒む | 2 つの案の間の共変が **0.5 を超え**、かつ小さい側のコミットが **5 件以上**あるとき、その継ぎ目は偽であり、2 つを 1 つのユニットへ戻す | 下の「共変の数え方」 |
| S3 絡み | 1 つの要求のコードが、別の要求のコードが回すループの本体や分岐の中にあるときは、継ぎ目がその本体を横切ることになるので、同じユニットに置く。<br>⭐ 振り分けの `switch` の腕どうしは絡みではない —— 腕は閉じたまとまりであり、`switch` は命令の語彙である | ファイルの境をループの本体の中に引けない |
| S4 残り | どの要求の規則も持たないコード（命令の語彙、兄弟が共有する補助、どの要求も名指さない命令の腕）は、元のファイルに残す | 元のファイルは語彙と公開の面を持ち続ける（5 節） |
| S5 確かめる | 残ったすべてのユニットの責務文が、`R2.2a` の 5 段で PASS すること。<br>畳んだ句と、(a)(b)(c) のどれで畳んだかを各表に書く | `docs/development-rules/07-review-standards.md` の 「SRP の責務文の試験」 |

**共変の数え方（本書のすべての数はこれで測った）**

- **数える単位**: 2 つのユニットの行の範囲それぞれに `git log -L <範囲>:<ファイル>` を掛け、出てきたコミットの集合 A と B を作る。
- **数** = 「A と B の両方にあるコミットの数」÷「A と B の小さいほうの数」。
  ⇒ 「小さい側の変更のうち、もう片側も開かせた割合」である。
- **除くもの 1 —— コメントだけの変更**: その範囲の差分で、コメント行・空行・行末のコメントを除いたコードの行が、消えた側と足された側で同じなら、そのコミットはその範囲について数えない。
  ⚠️ `2026-09-13` のコメントの一斉の手入れ（`7361e5e1`・`4d293708`）が、除かないとすべての組を 1 つに結ぶ。
- **除くもの 2 —— 誕生**: ファイルの行の半分以上にあたる範囲のコードを 1 つのコミットで変えたものは、そのファイルが書かれた手番であり、変更の理由を語らないので除く。
  除いたのは `edit-task.ts` と `edit-task-group.ts` の `5b062a83`、`svg-renderer.ts` の `ce63d668`、`import-document.ts` の `1d0412b1` と `4a383809` である。
- **閾値 0.5 の理由**: 0.5 を超えると、小さい側の変更の過半が 2 つのファイルを開かせる。
  割ったことで 1 ファイルで済むようになる変更より、2 ファイルを開かねばならなくなる変更のほうが多い —— **割ったことが目的に反する。**
  ⭐ 較正: `schedule-ruler` と兄弟の 0.14〜0.29（本物の継ぎ目）と、バーと依存線の 0.90（偽の継ぎ目）の間にある。
- **件数の下限 5 の理由**: 小さい側が 4 件以下だと、コミット 1 件で数が 0.2 以上動き、0.29 と 0.5 を見分けられない。
  ⇒ 下限に届かない組は **証拠なし** とし、S2 で拒まない（S1 の案が立つ）。
- **残る元のファイルに対する共変は、拒む理由にしない。**
  兄弟はどれも元のファイルに自分を登録する（表 T-020 の層の並び、表 T-108 の命令の語彙）ので、層や命令を 1 つ足せば元のファイルも必ず変わる。
  ⇒ 数は各表に載せるが、S2 は兄弟どうしにだけ掛ける。
  ⭐ 裏づけ: 残る `svg-renderer.ts` と `schedule-overlays.ts` が共に変えた 13 件のうち 4 件は「Draw the …」（コメントボックス・二重カーソル・ガイドカーソル・透かしを足した手番）であり、どれも層を 1 つ登録した変更である。
  別の 4 件は `2026-08-19`〜`08-25` の、描画の全体を建てた手番である。
- **輸入の輪**: 兄弟は残る元のファイルから共有の補助を取り込み、元のファイルは兄弟を呼ぶので、同じコンポーネントの中で輸入が輪になる。
  ⭐ 前例がある —— `edit-task.ts:31-32` は `./edit-document` から `refused`・`edited` を取り込み、`edit-document.ts:33` は `./edit-task` を取り込んでいる。
  検査 19 の輪の規則（`tools/check_layer_rules.py:234` の `LR-3`）はコンポーネントの間の輪だけを見るので、赤にならない。
- ⚠️ **`git log -L` の癖**: 範囲の隣にあって、のちに消えた関数の差分を、その範囲の履歴として拾うことがある。
  `edit-task.ts:226-234`（`fadeSpanOf`）の履歴の `8264592b` と `6cf77eab` は、差分を読むと、隣にあった `dummyStartOf`（予実の関数）の追加と削除であった。
  ⚠️ **差分を 1 件ずつ読んで確かめたのはこの 1 か所だけである** —— ほかの組の数は、この癖を含んだまま載せている。

**証拠の量（コードを変え、誕生でないコミットの数）**

| ファイル | 件数 | 共変は継ぎ目を拒めるか |
| --- | --- | --- |
| `svg-renderer.ts` | 60 | ⭐ 拒める |
| `edit-task.ts` | 28 | 一部の組だけ（下限 5 に届く組） |
| `edit-task-group.ts` | 9 | ⛔ どの組も下限に届かない |
| `import-document.ts` | **0** | ⛔ 証拠が 1 つも無い |

⇒ ⚠️ **`edit-task-group.ts` と `import-document.ts` の継ぎ目は、要求の持ち主（S1）と責務文（S5）だけで決めた。  
共変は賛成も反対もしていない。**

### 4.2 `editTask` —— `src/use-case/edit-document/edit-task.ts`（1,025 行）

`editTask`（`:516-1002`、487 行 / 101 分岐）の `switch (command.kind)`（`:535`）は 20 個の `case` を持つ。  
表 T-075 の `UF-11` は `FR-001`・`FR-011`・`FR-012`・`FR-033`・`FR-083`・`FR-103` の 6 件を負わせている（6 件とも `OW-2`）。

| ユニット | 負う要求 | 責務文（S5） | もとの `file:line` | 行数 |
| --- | --- | --- | --- | --- |
| `edit-task.ts`（残る） | — | 「`Task` の命令を受け、要求を負う規則は兄弟のユニットへ委ねて、新しい文書を返す」。<br>「受け」は (a)、「兄弟へ委ねて」は (c) で畳む。<br>命令の語彙（表 T-108）は継ぎ目の宣言なので (c) | `:1-62` `:66-130` `:133-160` `:172-216` `:497-535` `:616-652` `:698-703` `:727-734` `:903-932` `:1001-1025` | 345 |
| `task-create.ts` | `FR-001` | 「構えた形状で、期間を持つタスクを 1 つ立てる」。<br>残る句は 1 つ | `:536-615` | 80 |
| `task-paste.ts` | `FR-033` | 「選んだタスクを WBS の部分木ごと写す」。<br>残る句は 1 つ | `:653-697` | 45 |
| `task-plan-actual.ts` | `FR-011`・`FR-103` | 「予定と実績の日付を、表 T-245・表 T-019・表 T-021a に従って置く」。<br>「置いた日付と食い違う持ち越しの値を残さない」（`EX-11`・`EX-12`）は (c) で畳む | `:63-65` `:260-496` `:704-726` `:735-876` | 405 |
| `percent-complete.ts` | `FR-012` | 「完了率を、稼働日で数えた実績と予定から算出し直す」。<br>残る句は 1 つ | `:217-225` `:235-259` | 34 |
| `task-appearance.ts` | `FR-083` | 「タスクの見た目 —— 形・図形・色・線の太さ・名の置き場・フェードの日数 —— を書き換える」。<br>ダッシュの間の列挙は (b) で畳む | `:131-132` `:161-171` `:226-234` `:877-902` `:933-1000` | 116 |

⇒ **新しいファイル 5。**  
合計 345 ＋ 80 ＋ 45 ＋ 405 ＋ 34 ＋ 116 ＝ 1,025 行。

**残る `edit-task.ts` に何が残るか（S4）** —— 取り込み（`:1-34`）、命令の語彙の型（`:35-62`・`:66-130`）、兄弟が共有する補助（`reject`・`withSchedule`・`sameRow`・`withTask`・`blankVisual`・`visualOf`・`isMilestone`・`checkDay`・`wbsSubtreeOf`）、振り分けの頭（`:514-535`）、どの要求も名指さない腕（`deleteTask` ＝ `CM-7`、`setTaskName`・`setTaskNotes` ＝ `CM-9`・`CM-10`、`setTaskDeadline` ＝ `CM-12`、`setTaskWbsParent` ＝ `CM-18`、`moveTaskToTaskGroup` ＝ `CM-19`）、表 T-108 の行の対応（`:1004-1025`）。

**共変（4.1 の数え方）**

| 組 | 数 | 読み |
| --- | --- | --- |
| `task-plan-actual` の補助（`:260-428`）と置く腕（`:704-876`） | **1.00（5 件中 5 件）** | ⛔ **S2 で拒んだ** —— `FR-011` の床と端の規則と、`FR-103` の置き方は、別々のユニットにすると毎回 2 つを開かせる。<br>⇒ `FR-011` と `FR-103` は 1 つのユニットに同居する |
| `task-plan-actual` と `task-appearance` | 0.60（5 件中 3 件）。<br>⚠️ 4.1 の癖の 2 件を除くと **0.20（5 件中 1 件）** | 残る 1 件は `eac967e2`（フェードの日数を暦日で数えると決めた手番）である。<br>⇒ 継ぎ目を保つ |
| `task-create` と `task-plan-actual` | 0.50（4 件中 2 件） | 証拠なし（下限未満） |
| `task-create` と `task-paste` | 1.00（1 件中 1 件） | 証拠なし（下限未満） |
| `task-create` と `task-appearance` | 0.00（4 件中 0 件） | 証拠なし（下限未満） |
| `task-paste` と `task-plan-actual` | 1.00（1 件中 1 件） | 証拠なし（下限未満） |
| `task-paste` と `percent-complete`・`task-appearance` | 0.00（1 件中 0 件） | 証拠なし（下限未満） |
| `task-plan-actual` と `percent-complete` | 0.50（2 件中 1 件） | 証拠なし（下限未満） |
| `percent-complete` と `task-appearance` | 0.00（2 件中 0 件） | 証拠なし（下限未満） |
| `task-create` と `percent-complete` | 0.50（2 件中 1 件） | 証拠なし（下限未満） |
| 残る `edit-task.ts` に対して | 生成 0.50（4 件中 2 件）・写し 1.00（1 件中 1 件）・予実 0.59（17 件中 10 件）・完了率 0.50（2 件中 1 件）・見た目 0.80（5 件中 4 件） | 4.1 のとおり、拒む理由にしない |

⭐ **`percent-complete.ts` を別にするのは、責務文の試験がそう言うからである。**  
`task-plan-actual.ts` の文に「完了率を算出し直す」を足すと、その句は (a) 手順として畳めない —— `repriced` は `edit-calendar.ts:16` からも呼ばれるので、「その段だけを外から呼ぶことがない」を満たさない。  
そして `FR-012` の式が変わる出来事は、置き方を 1 行も書き換えない。  
⇒ 句が 2 つ残り、段 4 で FAIL する。

⚠️ **`deleteTask`（37 行）は元のファイルに残る。**  
表 T-075 は `UF-11` に `FR-032`（削除）を負わせていない。

### 4.3 `editTaskGroup` —— `src/use-case/edit-document/edit-task-group.ts`（640 行）

`editTaskGroup`（`:207-640`、434 行 / 103 分岐）の `switch`（`:217`）は 13 個の `case` を持つ。  
表 T-075 の `UF-12` は `FR-004`・`FR-005`・`FR-042`・`FR-058`・`FR-085`・`FR-111` の 6 件を負わせている（6 件とも `OW-2`）。

⛔ **このファイルの共変はどの組も下限 5 に届かない**（誕生を除いてコードを変えたコミットは 9 件しかない）。  
⇒ 継ぎ目は S1・S3・S5 だけで決めた。

| ユニット | 負う要求 | 責務文（S5） | もとの `file:line` | 行数 |
| --- | --- | --- | --- | --- |
| `edit-task-group.ts`（残る） | `FR-111` | 「`TaskGroup` の命令を受け、要求を負う規則は兄弟のユニットへ委ねて、新しい文書を返す」。<br>畳み方は `edit-task.ts` と同じ | `:1-126` `:203-217` `:271-472` `:639-640` | 345 |
| `task-group-naming.ts` | `FR-085`・`FR-058` | 「行を、名か由来を持つものとして立て、名を付け替える」。<br>「名も由来も持たない行を拒む」（`AT-54`）は (c) で畳む | `:218-270` `:473-486` | 67 |
| `task-group-look.ts` | `FR-042` | 「行の色と高さを書き換える」。<br>残る句は 1 つ | `:487-518` | 32 |
| `task-group-folding.ts` | `FR-004` | 「行の畳み・隠し・開いたままの印を、1 行または全行について書き換える」。<br>「1 行または全行」は (b) で畳む | `:519-546` `:629-638` | 38 |
| `task-group-order.ts` | `FR-005` | 「兄弟の行の並びと親を、表 T-015a に従って変える」。<br>「WBS の並びを行の並びに追わせる」（`HM-9`）は、並べ替えた結果に掛かる条件なので (c) で畳む | `:127-202` `:547-628` | 158 |

⇒ **新しいファイル 4。**  
合計 345 ＋ 67 ＋ 32 ＋ 38 ＋ 158 ＝ 640 行。

**共変（4.1 の数え方）**: 兄弟どうしの組は、命名と並び 0.00（2 件中 0 件）、命名と開閉 0.50（2 件中 1 件）、開閉と並び 0.00（2 件中 0 件）、色と高さはコードを変えたコミットが 0 件である。  
⇒ **すべて証拠なし。**  
残る `edit-task-group.ts` に対しては、命名 0.50（2 件中 1 件）・開閉 1.00（2 件中 2 件）・並び 0.67（3 件中 2 件）。

⭐ **`FR-085` と `FR-058` の同居は S3（絡み）による。**  
`FR-058` がこのファイルに置く規則は「名も由来も持たない行を拒む」（`AT-54`）の 2 つの検査だけで、どちらも `FR-085` の腕の中にある（`:226-230` と `:478-482`）。

⭐ **`FR-111` は元のファイルに残す。**  
表 T-075 が今日それを負わせているのは `edit-task-group.ts` であり、今日のコードでその規則を持つ行は無い —— あるのは生成の腕の既定値 `editGroup: null`（`:264`。  
`task-group-naming.ts` へ移る）だけである。  
`CR-433` はコードの側をリファクタのあとへ送っており、そこで足す「別のグループからの書き込みを断る」は、すべての命令に先立つ見張りなので、振り分けを持つ元のファイルに住む。

⚠️ **`deleteTaskGroup`（`CM-27`、69 行）と `pasteTaskGroupSubtree`（`CM-28`、133 行）は元のファイルに残る。**  
どちらも表 T-075 が `UF-12` に負わせた要求の規則ではない —— 拒むときに名乗る要求は前者が `FR-032`、後者が `FR-033` であり、`FR-033` は `UF-11` が負っている。

### 4.4 `svgFromSchedule` —— `src/adapter/svg-renderer/svg-renderer.ts`（1,301 行）

`svgFromSchedule`（`:579-1193`、615 行 / 126 分岐）。  
表 T-075 の `UF-32` は `FR-013`・`FR-075`・`FR-080`・`FR-089`（`OW-2`）と `NFR-007`（`OW-4`）を負わせている。

⭐ **割ったあとも、層の並びを決めるのは入口 1 か所だけである**（`:1146-1183` の組み立て）。  
⛔ **兄弟は「層の鍵 → 文字列の列」を返すこと（MUST）。  
入れ物の配列を引数で渡して押させてはならない** —— 押す順が呼ぶ順に化け、表 T-020 の並びが入口の外へ漏れる。

| ユニット | 負う要求 | 責務文（S5） | もとの `file:line` | 行数 |
| --- | --- | --- | --- | --- |
| `svg-renderer.ts`（残る。<br>公開エントリ） | `FR-080`・`NFR-007`（`OW-4`） | 「1 枚の SVG を、表 T-020 の順に層を重ねて組み立てる」。<br>「書き出しには操作の印を載せない」（`EP-12`・`EP-14`）と「色は表 T-236 から引く」は (c)、兄弟が共有する描き方の補助は継ぎ目の宣言なので (c) で畳む | `:1-54` `:71-123` `:144-198` `:288-293` `:374-385` `:575-613` `:628-679` `:887-909` `:1101-1194` `:1198-1301` | 492 |
| `schedule-task-figures.ts` | `FR-013`・`FR-075` | 「タスクごとの図 —— 予定・実績・ガイド・ダミー・選択枠・フェードの掴み点・進捗マーカー・再開アイコン・札 —— と、タスクを結ぶ依存線を描く」。<br>列挙は (b)。<br>依存線も (b) で畳む —— 下の共変 0.90 が、依存線とバーが同じ変更で動くことを示している | `:55-60` `:66-70` `:124-143` `:206-287` `:294-373` `:614-627` `:731-886` `:910-974` `:1195-1197` | 431 |
| `schedule-grid.ts` | `FR-089` | 「チャートの格子 —— 行の帯と横罫、日付の縦罫、目盛の帯 —— を 1 つの刻みから描く」。<br>列挙は (b)、「1 つの刻みから」は (c) | `:199-205` `:386-535` `:680-730` | 208 |
| `schedule-overlays.ts` | — | 「タスクに属さない重ね描き —— 進捗線・状態線・二重カーソル・ガイドカーソル・注記・透かし —— を描く」。<br>列挙は (b) | `:61-65` `:536-574` `:975-1100` | 170 |

⇒ **新しいファイル 3。**  
合計 492 ＋ 431 ＋ 208 ＋ 170 ＝ 1,301 行。

**共変（4.1 の数え方）**

| 組 | 数 | 読み |
| --- | --- | --- |
| バー（`:731-895` ほか）と依存線（`:896-974` ほか） | **0.90（10 件中 9 件）** | ⛔ **S2 で拒んだ** —— 1 つのユニットにする |
| 進捗マーカー（`:225-287` `:836-853` `:1195-1197`）とバー・依存線 | **0.62（8 件中 5 件）** | ⛔ **S2 で拒んだ**。<br>そのうえ S3 —— マーカーはタスクごとのループの本体の中で描かれる |
| フェードの掴み点（`:825-834`） | —— | S3 —— 選択されたタスクの分岐の中、選択枠と同じ本体にある |
| `schedule-task-figures` と `schedule-grid` | 0.42（12 件中 5 件） | 継ぎ目を保つ |
| `schedule-task-figures` と `schedule-overlays` | **0.50（14 件中 7 件）** | ⚠️ **閾値ちょうどである。<br>「超える」ときだけ拒むので、継ぎ目を保つ。<br>**<br>共にした 7 件のうち 4 件は `2026-08-19`〜`08-25` の、描画の全体を建てた手番（どれも 12〜15 か所を同時に変えた） |
| `schedule-grid` と `schedule-overlays` | 0.25（12 件中 3 件） | 継ぎ目を保つ |
| 残る `svg-renderer.ts` に対して | タスクの図 0.71（31 件中 22 件）・格子 0.75（12 件中 9 件）・重ね描き 0.93（14 件中 13 件） | 4.1 のとおり、拒む理由にしない |

⭐ **目盛（`rulerSvg`）の移動は、履歴が裏づける唯一の継ぎ目である**（4.0 の表）。  
⭐ **日付の縦罫を目盛と同じユニットへ置くのは `FR-089` の文そのものによる** —— 「タイムルーラーが出している最も細かい段階と同じ間隔で縦線を引く」。  
その規則は、今日すでに `ticksOfRow` と `ROWS_OF_TIER` を縦罫が借りる形（`:706-718`）で書かれており、2 つを別のユニットに置くと、`FR-089` の規則が継ぎ目をまたぐ。  
格子と目盛の共変は 0.00（4 件中 0 件）で、下限に届かない。

⭐ **`schedule-overlays.ts` はどの要求も負わない**が、出すのは S4 による —— 残る入口は `FR-080` の起点であり、どの要求も名指さない 170 行をそこに残すと、`FR-080` の変更要求を受けた AI が最初に開くファイルが 662 行になる。  
タスクの図とは 0.50、格子とは 0.25 で、どちらも拒む数ではない。

⚠️ **生成された定数の塊（`:1198-1301`）は入口に残す。**  
`tools/generate_entity_types.py` がこのファイルへ書き込む（`:2249` と `:2413`）ので、塊を動かすと生成の道具を同じコミットで直すことになる。  
兄弟は入口から取り込む。

⚠️ **`rulerSvg`（79 行 / 13 分岐）は 4 つの神関数ではないが、本波で `schedule-grid.ts` へ移る。**  
帯を超えているので、基準線の `HELD` 行のファイル名が変わる（7 節）。

### 4.5 `builtMerge` —— ⛔ 段 4 から外した

⛔ **利用者の裁定（2026-09-21、9 節の ③）により、`import-document.ts` は割らない。**  
前の本節が提案した 3 つのユニット（`baselined-document.ts`・`merge-questions.ts`・`merged-schedule.ts`）は取り下げた。  
表 T-075 の `UF-19` の行も、その「負う要求」の欄（`FR-015`・`FR-022`・`FR-056`・`FR-087`）も動かない。

**理由（測った）**

- `git log -- src/use-case/import-document/import-document.ts` は **9 件**である（2026-09-21、`60a2e3d7`）。
  前に立つ者の読みでは、そのうち 1 件が足場、1 件が誕生であり、**残る 7 件はコメントだけを変えた**。
  4.1 の数え方で数えても、誕生でなくコードを変えたコミットは **0 件**である（4.1 の証拠の量）。
  ⇒ このファイルを変更のたびに開かされたことは、履歴の上で 1 度も無い。割っても直しの時間は減らない。
- 前の本節自身が「この分割は `builtMerge` を小さくしない」と書いていた —— `builtMerge` は丸ごと別のファイルへ移るだけであった。

⚠️ **検査 60 の `HELD` 行 `import-document.ts::builtMerge` と `import-document.ts::mergedDocument` は、段 4 では動かない。**  
関数の大きさは引き続きラチェットだけが縛る（`JDG-54`）。

### 4.6 合計

| 割るもの | 前の 4 節 | 本節 | 表 T-075 に足す行 |
| --- | --- | --- | --- |
| `editTask` | 4 | **5** | `UF-72` 〜 `UF-76` |
| `editTaskGroup` | 3 | **4** | `UF-77` 〜 `UF-80` |
| `svgFromSchedule` | 5 | **3** | `UF-81` 〜 `UF-83` |
| `builtMerge` | 4 | ⛔ **0**（段 4 から外した。4.5） | —— |
| **合計** | 16 | **12** | `UF-72` 〜 `UF-83` |

⭐ **行 ID の帯 `UF-72` 〜 `UF-83` は、2026-09-21 に別のセッション（状態機械）と取り決めた**（相手は `UF-84` 以降）。  
⚠️ **行 ID は木の最大（`UF-71`。  
2026-09-21 に測り直した）の続きから採り、表の中では 表 T-062 の `CP-n` の順に置く。**  
表 T-075 は「行は 表 T-062 の `CP-n` の順に並べ」と自分で宣言しており、行 ID の昇順とは宣言していない（`UF-45` が `UF-42` の次に来ている前例がある）。

⭐ **数がほとんど減らなかった理由を隠さずに書く。**  
共変が継ぎ目を拒めたのは `svg-renderer.ts` だけであった（4.1 の証拠の量）。  
そこでは 5 が 3 になった。  
`edit-task.ts` は拒める組が 1 つ（予実の補助と置く腕）だけで、`edit-task-group.ts` と `import-document.ts` は証拠が無い。  
⇒ ⚠️ **この 2 つのファイルの数は、要求の持ち主と責務文だけが決めている。**  
小さいユニット（32〜45 行）を 1 つにまとめる案は 9 節の ① で比べ、利用者はまとめない A を選んだ。

**負う要求の同居 —— 割る前と割ったあと**

| | 割る前 | 割ったあと |
| --- | --- | --- |
| 割る 3 ファイルが負う `OW-1`・`OW-2` の要求 | 16 件 | 16 件（増減なし） |
| そのうち、起点のユニットを別の `OW-1`・`OW-2` の要求と共有するもの | **16 件**（3 ユニットに 6・6・4 件） | **6 件**（3 組） |
| 同居の理由 | —— | `FR-011` と `FR-103` は S2（1.00、5 件中 5 件）。<br>`FR-013` と `FR-075` は S2（0.62、8 件中 5 件）と S3。<br>`FR-085` と `FR-058` は S3 |

### 4.7 段 4 の出口 —— ⭐ 利用者の裁定で決めた（9 節の ②）

計画の状態表（`docs/development-records/refactor-plan-report-2026-09-13.md:95`）は段 4 の出口を「500 行を超える関数が `frameLoop` と `domScreenSurface` だけ」と書いている。  
⛔ **この出口は目的を測っていなかった。**  
`editTask`（487 行）と `editTaskGroup`（434 行）は今日すでに 500 行を下回っており、割らなくても通る。  
また 1 つの関数を 3 つに等分しても通る。

⭐ **段 4 の出口（利用者が選んだ A）: 割る 3 ファイルが今日負う `OW-1`・`OW-2` の要求 16 件のうち、起点のユニットを別の `OW-1`・`OW-2` の要求と共有するものが、4.6 の 3 組 6 件だけであること。**

- **いまの数と目標**: いまは 16 件中 **16 件**（`UF-11` に 6、`UF-12` に 6、`UF-32` に 4）。目標は **6 件** —— `FR-011` と `FR-103`、`FR-085` と `FR-058`、`FR-013` と `FR-075` の 3 組である。
  （2026-09-21 に `60a2e3d7` の 表 T-075 の「負う要求」の欄で数えた。`UF-32` の `NFR-007` は `OW-4` なので数えない。）
- **測り方**: 表 T-075 の「負う要求」の欄を、4.6 の 12 行と残る 3 行（`UF-11`・`UF-12`・`UF-32`）について読み、`OW-1`・`OW-2` の要求 ID を 2 つ以上並べる行を数える。
  それが `task-plan-actual.ts`・`task-group-naming.ts`・`schedule-task-figures.ts` の 3 行だけで、どの要求 ID も 2 つの行に現れないこと。
  ⛔ 行数でも、帯（50 行 ／ 15 分岐）でもない。
- **目的に結ぶ理由**: 変更要求は要求 ID を名指して来る。
  起点のユニットが 1 つの要求の規則だけを持つなら、AI が最初に開くファイルに、ほかの要求の規則が混ざらない。
- **ごまかせない理由**: 関数を小さくしても、ファイルを等分しても、この数は動かない。
  動かす道は、要求の規則を別のユニットへ出すことだけである。
  規則を持たないユニットを起点と書く嘘は、表 T-277 の `OW-1`・`OW-2` の定義（「規則を決めるユニット」「規則の値を持つ最も内側のユニット」）に反し、`CR-435` の判定と同じ手で 1 件ずつ覆せる。
- **関数の大きさとの関係**: 関数の行数と分岐数は、引き続き検査 60 のラチェットだけが縛る（`JDG-54`）。
  本出口は上限を置かないので `JDG-54` と衝突しない。
- **数えるのは前に立つ者の手であり、検査には載せない。**

---

## 5. 公開エントリはどうなるか

### 5.1 表 T-064 は 1 行も動かない

| コンポーネント | 公開エントリ | 割ったあと |
| --- | --- | --- |
| `EditDocument`（`PI-9`） | `edit-document.ts` | ⭐ **無変更。<br>** 割るのは `edit-task.ts` と `edit-task-group.ts` であり、どちらも公開エントリではない |
| `ImportDocument`（`PI-10`） | `import-document.ts` | ⭐ **無変更。<br>** 段 4 は割らない（4.5） |
| `SvgRenderer`（`PI-19`） | `svg-renderer.ts` | ⭐ **無変更。<br>** `svgFromSchedule` は名前も署名もそのまま入口に残る |

⇒ **検査 26b は赤にならない。**

### 5.2 段 5・6 と同じ標準を、段 4 も保てるか

段 5・6 の出口は「公開エントリの名前と署名が不変。試験が無変更で緑」である。

**答: 保てる。  
ただし 1 つ条項を足すことが条件である。**

⛔ **割ったユニットの元のファイルは、フォルダの外から読まれていた名前をすべて再び公開すること（MUST）。**

測った内訳（1 節 (o)）:

| 読み手 | 読んでいる名前 | 割ったあとの要り用 |
| --- | --- | --- |
| `src/use-case/edit-document/edit-document.ts:10-20` `:33` | `editTask` / `TaskCommand` / `TaskMilestoneGlyph` / `TaskShapeKind` / `PlanActualPlacement` / `ActualGrabHold` / `CycleSurroundings` / `CycledPlanActual` / `cycleTaskPlanActualState` | `edit-task.ts` が `CycleSurroundings`・`CycledPlanActual`・`cycleTaskPlanActualState` を `task-plan-actual.ts` から再び公開すること（残りの型は `edit-task.ts` に残る） |
| `src/use-case/edit-document/edit-calendar.ts:16` | `repriced` | `edit-task.ts` が `percent-complete.ts` から再び公開すること |
| `src/use-case/edit-document/edit-task.ts:33` | `tasksRankedByTheRowTree` | `edit-task-group.ts` が `task-group-order.ts` から再び公開すること |
| `src/adapter/image-exporter/image-exporter.ts:14` ／ `src/adapter/screen-renderer/screen-frame.ts:13` | `colourOf` ／ `GROUP_GRID_LINE_WIDTH_PX` | 入口に残るので自動（4.4） |
| `src/framework/single-html-shell/frame-loop.ts:88` ／ `src/use-case/apply-document-change/document-change-plan.ts:27` | `import-document.ts` の公開の型と `importDocument` | 入口に残るので自動（4.5） |
| 試験 4 本（`edit-task` を直に取り込む） | `editTask` / `TaskCommand` / `cycleTaskPlanActualState` | 同上 |
| 試験 1 本（`edit-task-group` を直に取り込む） | `editTaskGroup` | 入口に残るので自動 |
| 試験 26 本（`svg-renderer` を直に取り込む） | 公開エントリそのもの | 自動 |
| 試験 3 本（`import-document` を直に取り込む） | 公開エントリそのもの | 自動 |

⭐ **`tests/unit/cr-430-table-t-021a-the-press-cycle.test.ts:66-80` は継ぎ目を 2 つの道で探す形（`CYCLE_HOMES`）で書かれている。**  
`edit-task.ts` が再び公開していれば緑のままである。

⇒ **試験は 1 本も書き換えない。**  
 それが割ったことの検証になる（計画の「段 4〜6 の共通の型」）。

---

## 6. ユニットが 68 から 80 に増えること —— ⭐ 利用者の裁定で平らのまま（9 節の ①）

表 T-074 の `SU-3` は現在値を **68** と書き、`SU-2`（モジュール）を **0** と書いている（2026-09-21 に `60a2e3d7` で測り直した）。  
4 節のとおり 12 足すと **80** になる。  
`EditDocument` は 9 から **18** ファイル、`SvgRenderer` は 2 から 5 になる。  
`ImportDocument` は 1 のまま動かない（4.5）。

⭐ **利用者は「平らに並べ、モジュールを作らない」を選んだ。**  
⇒ `SU-2` は 0 のままである。  
小さい兄弟もまとめない —— 5 つのうち `baselined-document.ts` は 4.5 で消えたので、50 行に届かないユニットは 4 つである（`task-group-look.ts` 32 行、`percent-complete.ts` 34 行、`task-group-folding.ts` 38 行、`task-paste.ts` 45 行）。

⛔ **`SU-3` の 68 → 80 は、最後の波が着地したときに初めて真になる。**  
⇒ 本書を当てる手番では書かない。  
各波の分割のコミットが、そのコミットで `src/` に在るファイルの数に合わせて書き換える。

⚠️ 5.3 節の「表 T-075 のとおり `ScreenRenderer` と `EditDocument` が最も多くのユニットを持つ」は、波 2 のあとで偽になる（`ScreenRenderer` 11、`EditDocument` 18）。  
波 2 のコミットが「`EditDocument` が最も多くのユニットを持つ」と書き直す。

---

## 7. ⛔ ラチェットが要求すること（分割ごとに、そのコミットで）

検査 60 の文（`function-size-baseline.txt` の 11-20 行目、逐語の要旨）:

> 各 `HELD` 行は帯を超えている関数 1 つである。  
> その行数か分岐数が**上がれば赤**、
> 基準線に無い関数が新たに帯を超えても**赤**、そして
> **`HELD` 行の関数がもう存在しないか帯を出たときも、その行は古びており赤になる** ——
> **上の合計が下がっていても**である。  
> `HELD` 行は名指した 1 つの関数についての主張であり、単独で判ぜられる。
> 帯を超えた関数を割ることは妨げない —— **同じコミットで**このファイルを書き直し、古い行を落として新しい行を足せ。

⛔ **⇒ 3 つの分割は、それぞれ自分のコミットで `function-size-baseline.txt` を書き直さねばならない。**  
⛔ **本書はそのファイルを 1 文字も編集していない。**  
⭐ `HELD` 行の鍵は「ファイル::関数名」なので、関数を丸ごと別のファイルへ移すだけでも、古い行は古び、新しい行が要る。

⭐ **その書き換えは既に許されている** —— `JDG-118`（逐語「**(a) Claude Code の許可設定で、この書き換えを許可する。**」）。  
⚠️ **それでも当日に分類器や許可の確認が止めたら、迂回せずに止まって報告すること。**

下の「足す行」は、4 節の行の範囲から数えた**行数だけの見込み**である。  
⛔ 分岐数は、割ったあとの関数の形が決まるまで数えられないので書かない。

| 分割 | 落とす `HELD` 行（2026-09-21 に基準線で読んだ値） | 足す `HELD` 行の見込み |
| --- | --- | --- |
| `svgFromSchedule` | `svg-renderer.ts::svgFromSchedule`（615 / 126）<br>`svg-renderer.ts::rulerSvg`（79 / 13。<br>ファイルが変わる） | `schedule-grid.ts::rulerSvg`（79 / 13。<br>丸ごと移すなら同じ値）<br>`svg-renderer.ts::svgFromSchedule`（残る本体はおよそ 203 行）<br>タスクごとのループ（`:731-886` の 156 行）<br>依存線（`:910-974` の 65 行）<br>重ね描き（`:975-1100` の 126 行）<br>行の帯と日付の縦罫（`:680-730` の 51 行。<br>帯の縁） ⇒ **最大 6 行** |
| `editTask` | `edit-task.ts::editTask`（487 / 101） | `edit-task.ts::editTask`（振り分けと残る腕。<br>20 個の `case` だけで分岐が 20 あり、帯を出ない）<br>生成の腕（`:536-615` の 80 行）<br>`setTaskPlanActualState` の腕（`:735-815` の 81 行） ⇒ **最大 3 行** |
| `editTaskGroup` | `edit-task-group.ts::editTaskGroup`（434 / 103） | `edit-task-group.ts::editTaskGroup`（残る腕を中に置くなら、およそ 230 行）<br>または同じファイルに切り出した `pasteTaskGroupSubtree`（133 行）と `deleteTaskGroup`（69 行）<br>生成の腕（`:218-270` の 53 行）<br>`moveTaskGroup` の腕（`:573-628` の 56 行） ⇒ **最大 4 行** |

⚠️ **`excess-lines` ／ `excess-branches` の「最良の値」は書かない。**  
前の本節は 8412 → 6796 と書いたが、それは 4 つの関数が帯の中へ消える前提であった。  
本節の割り方では、振り分けの `switch` と大きな腕が帯の外に残る見込みである。  
⛔ **各体は、自分の分割のあとに `function-size.mjs` を掛けて実測した値だけを書くこと。**  
本表の「見込み」を写してはならない。

---

## 8. 3 つの順と、並行してよいもの

⭐ **順を縛るものは 2 つだけである。**

1. **共有の 2 ファイル** —— 3 つとも `function-size-baseline.txt` を書き換え、表 T-075（`docs/spec/05-07-design.md`）に自分の行を足す（検査 18 は `src/` のファイルの集合と表 T-075 が同じコミットで一致することを求める）。
   ⇒ ⛔ **割る体はこの 2 ファイルを書かない。**
   前に立つ者が **1 つずつ** 併合し、併合した木で測り直して基準線と表の行を書き、緑を確かめてから次を併合する。
2. **輸入 1 本** —— `edit-task.ts:33` が `./edit-task-group` から `tasksRankedByTheRowTree` を取り込み、`edit-task.ts:930`（`moveTaskToTaskGroup`）で使う（2026-09-21 に `2112d0c9` で測り直した。
   前の本節の `:929` は 1 行ずれている）。
   4.3 はその関数を `task-group-order.ts` へ移す。
   ⇒ ⛔ **`edit-task-group.ts` は `tasksRankedByTheRowTree` を再び公開し続けること（MUST）。**
   ⛔ **`edit-task` を割る体は、`edit-task.ts:33` の取り込み元を書き換えないこと（MUST NOT）。**
   ⭐ この 2 つを両方の体の依頼文に逐語で書けば、2 体は同じファイルを 1 つも触らない。
   `moveTaskToTaskGroup` は 4.2 で元のファイルに残るので、取り込みも `edit-task.ts` に残る。

⭐ **前の本節は「波 2 と 波 3 を順に打つ」とした。  
本節はそれを取り消す** —— 上の MUST と MUST NOT があれば、どちらの体も動いている足場の上で割らない。  
併合の後で `tasksRankedByTheRowTree` を読む場所が 2 つ（`edit-task.ts:930` と `task-group-order.ts`）あることは、試験が無変更で緑であることが確かめる。

| 波 | 何を割るか | 触るフォルダ | 並行 |
| --- | --- | --- | --- |
| 波 1 | `svgFromSchedule` | `src/adapter/svg-renderer/` | 1 体（`builtMerge` は 4.5 で外した） |
| 波 2 | `editTask` ／ `editTaskGroup` | どちらも `src/use-case/edit-document/`（ファイルは互いに素） | ⭐ **2 体を並行してよい。<br>** 上の MUST と MUST NOT を依頼文に書くこと |

**波 1 を先にする理由**: 履歴が裏づける唯一の継ぎ目（目盛）と、共変が拒んだ継ぎ目（バーと依存線、マーカー）が 波 1 にある。  
⇒ 4.1 の手順が当たっているかを、証拠のある側で先に確かめられる。

**並行させる条件（MUST。  
両方の波に掛かる）:**
1. それぞれ別の worktree で割り、共有の 2 ファイルを**書き換えずに**報告する。
2. 前に立つ者が **1 つずつ** 併合する（上の 1）。
3. ⛔ どちらの体にも `git stash` を打たせない。
4. 割る体は `edit-document.ts` を触らない —— 5.2 の再公開で、その取り込みは 1 行も変わらない。

⚠️ **別のセッション（状態機械）との境**: `docs/development-records/handoff-state-machine.md` 6 節は、本書の 4 ファイルをこちらの持ち場と書いている。  
⛔ 相手の持ち場（`src/use-case/advance-screen-session/` と `docs/spec/_source/state-machines.json`）には触らない。

---

## 9. ⭐ 利用者が答えた問い（2026-09-21）

⭐ **返す問いは 0 件である。**  
起草した本書が返した 2 つの問いと、前に立つ者が足した 1 つの問いに、利用者は 2026-09-21 に答えた。  
3 つとも、前に立つ者が書いた選択肢から利用者が 1 つを選んだ答えである。  
裁定の記録は前に立つ者が `docs/development-records/rulings.md` に積む（本書を当てた体は書かない）。

| # | 問い | 利用者が選んだもの | 本書のどこに当てたか |
| --- | --- | --- | --- |
| ① | ユニットを 68 から増やしてよいか。<br>小さい兄弟をまとめるか。<br>モジュール（`SU-2`）を作るか | 「A 平らに 15 ファイル（推奨）」 | 6 節。<br>平らのまま、モジュールを作らず、小さい兄弟もまとめない。<br>⚠️ 選んだ時の「15」は `builtMerge` の 3 つを含む数である —— ③ と合わせて **12** になる |
| ② | 段 4 の出口を何にするか | 「A 起点の共有（推奨）」 | 4.7。<br>表 T-075 の「負う要求」の欄から数える。<br>⚠️ 選んだ時の「20 件のうち 6 件」は `import-document.ts` の 4 件を含む —— ③ と合わせて **16 件のうち 6 件** になる |
| ③ | `builtMerge` を段 4 で割るか | 「段 4 から外す（推奨）」 | 4.5・4.6・7 節・8 節 |

⚠️ **3 つは互いに数を動かす。**  
③ が `import-document.ts` の 3 つのユニットを消したので、① のファイルの数と ② の要求の数は、`builtMerge` を引いた木から数え直した（2026-09-21、`60a2e3d7`）。  
⛔ 起草のときの数から手で引いてはいない。

**比べたこと（起草のときに書いたもの。答えの根拠として残す）**

- ① の B（50 行に届かない兄弟を隣にまとめる）は、共有する要求を増やし、② の出口を満たせなくする。
  まとめる根拠は「小さいから」だけであり、責務文の試験では、たとえば完了率を予実に入れると句が 2 つ残って FAIL する（4.2）。
- ① の C（`EditDocument` にモジュールの段を作る）は `SU-2` の 0 を崩し、5.3 の「フォルダの外から公開エントリ以外を読まない」が入れ子のフォルダに何を言うかを新たに定めさせる。
- ② の B（500 行のまま）は段 4 をしなくても通る。② の C（帯を出る）は `JDG-54`（逐語「**問 6: 大きさの上限値は、いまはラチェットだけにして、段 8 の実測を見てから決めてよい**」）を覆す。
- ③ —— `import-document.ts` は、誕生でなくコードを変えたコミットを 1 つも受けていない（4.1・4.5）。

---

## 10. 数の予測（当てる体はこれと自分の実測を突き合わせる）

| 何 | いま | 当てたあと |
| --- | --- | --- |
| 表 T-075 の行 | `UF-1` 〜 `UF-71`（欠番 `UF-43` `UF-44` `UF-52`） | `UF-83` まで。<br>足すのは `UF-72` 〜 `UF-83`（各波の分割のコミットで） |
| 表 T-063 の行 | `UT-1` 〜 `UT-7` | `UT-8`（`SvgRenderer`）と `UT-9`（`EditDocument` の第 2 段）を足す（各波の分割のコミットで）。<br>`ImportDocument` の行は要らない（4.5） |
| 表 T-276 | 無い | 立てる。<br>`UD-1` 〜 `UD-5` |
| 接頭辞の登録 | 160 | 161（`UD` を `docs/spec/_source/row-id-prefixes.json` へ） |
| 表 T-074 の `SU-3` の現在値 | 68 | 80（最後の波のあとで。6 節） |
| 表 T-064 の行 | `PI-1` 〜 `PI-38` | ⭐ **無変更** |
| `components.json` の辺 | —— | ⭐ **無変更** |
| 検査 60 の `HELD` | 78 | 7 節の見込みで、落とす 4 行に対して足すのは最大 13 行。<br>⛔ 実測だけを書く |
| `excess-lines` ／ `excess-branches` | 8414 ／ 952（2026-09-21 に `2112d0c9` の基準線で読んだ） | ⛔ 見込みを書かない（7 節）。<br>各体が実測する |
| 負う要求を別の `OW-1`・`OW-2` の要求と共有する要求（割る 3 ファイルの 16 件のうち） | 16 | **6**（4.6 の 3 組。<br>4.7 の出口） |

⚠️ **`UT-2` の文は書き直しが要る** —— いま「集約ごとの 8 ファイル」と書いているが、`edit-task.ts` と `edit-task-group.ts` がさらに割れるので、**「集約ごとに 8 つへ割り、そのうち 2 つをさらに `UT-10` で割った」**と読める形にする。  
⛔ 「8」という数はそのまま残す —— 集約の数は変わらない。

---

## 11. 影響

### 11.1 仕様（`docs/spec/05-07-design.md`）

| 場所 | すること |
| --- | --- |
| `:272` | 1 文を取り替え、表 T-276 を指す。<br>⭐ **当てた**（2026-09-21） |
| 表 T-276 | 新設（3 節）。<br>`:272` の直後。<br>⭐ **当てた**（2026-09-21） |
| 表 T-063 | `UT-8`・`UT-9` を足し、`UT-2` の文を直す。<br>⛔ 各波の分割のコミットで |
| 表 T-075 | `UF-72` 〜 `UF-83` を、`CP-n` の順の位置に差し込む。<br>「負う要求」の欄は 4.2〜4.4 の表のとおりに書き、残る 3 行（`UF-11`・`UF-12`・`UF-32`）の欄も同じ表のとおりに減らす。<br>⛔ 各波の分割のコミットで（検査 18 が表と `src/` を同じコミットで一致させる。2026-09-21 に 1 行だけ足して測ると、終了コード 1 で `MISSING` を刷った） |
| 表 T-074 の `SU-3` | 68 → 80。<br>⛔ 各波の分割のコミットで、その時点のファイルの数に合わせる（6 節） |
| `:250` の「`ScreenRenderer` と `EditDocument` が最も多くのユニットを持つ」 | ⚠️ **測り直す** —— `EditDocument` が 18 で単独の最多になる |

### 11.2 原稿

- `docs/spec/_source/row-id-prefixes.json` に `UD` を足し、`npm run gen` で `_assets/tbl-row-id-prefixes.md` を作り直す。
- ⛔ `_assets/tbl-row-id-prefixes.md` を手で編集しない（生成物である）。

### 11.3 記録（当てるときに書く。⛔ 本書は書かない）

- `docs/development-records/changelog.md` に 1 行。
- `docs/development-records/rulings.md` —— 9 節の 3 つの裁定を積む。⛔ 前に立つ者が書く（本書を当てた体は書かない）。
- `docs/development-records/refactor-plan-report-2026-09-13.md` の状態表の段 4 の行 —— 入口の条件が満たされたことを書く。
  ⚠️ **⛔ 本書を当てる体は、この 2 ファイルを別の体が同時に触っていないことを確かめてから書くこと。**

### 11.4 実装と試験（⛔ 本書は 1 行も直さない。所だけ書く）

- `src/adapter/svg-renderer/` に 3 ファイル、`src/use-case/edit-document/` に 9 ファイル、
  `src/use-case/import-document/` は 0 ファイル（4.5）。
- 試験は **1 本も書き換えない**（5.2 節）。  
書き換えが要ったら、それは割り方が公開の面を動かした徴である。

---

## 12. ⛔ この変更でやらないこと

| やらないこと | なぜ |
| --- | --- |
| 割ること | 本書は入口である。<br>割るのは段 4 の 2 つの波（8 節） |
| `builtMerge` を割ること | 利用者の裁定で段 4 から外した（4.5・9 節の ③） |
| `function-size-baseline.txt` を編集すること | 分割のコミットが書き換える（7 節） |
| `docs/spec/` を編集すること | 本書は変更要求であり、当てるのは別の手番である |
| 振る舞いを変えること | 割るコミットは「試験が無変更で緑」で検証される。<br>直しを混ぜると、ずれの出所が割り違いか直しかを切り分けられない |
| `input-command-translator.ts` と `dom-screen-surface.ts` を割ること | 段 5 と 段 6 である。<br>⭐ ただし 表 T-276 の `UD-3` が、段 5 の「全部純粋だから割らない」（`CR-125` §2-4）を先に無効にする |
| 残る腕を命令の表で引く形に書き換えること | 7 節と 9 節の問い ② の C。<br>振り分けの `switch` を表に替えれば `editTask` の分岐は減るが、それは振る舞いを変えない書き換えであっても、割ることとは別の手番である |
| `frameLoop` を割ること | 段 7 である |

---

## 13. 書くときの作法（⛔ 当てる体はここを読んでから書く）

- **CJK の直後に `**` を開いて、その中を code span で始めない** —— docutils が壊れる。
- `` を作らない。
- 「表 X の N 行」と書かない —— 検査が X の行数についての主張として読む。**  
行 ID を名指す。**
- 表 T-276 を足したら `strictdoc export docs/spec` が通ることを確かめる。
- `python tools/fix_line_breaks.py` を、触ったファイルすべてに掛ける。
- 検査は**終了コード**で判ずる。  
⛔ `FAIL` を `grep` しない —— `check-function-size.py` は `FAIL` と刷るが、
  `check_layer_rules.py` は `VIOLATION` と刷る。

---

## 14. ⭐ 4 節を書き直した体が、本書の文から変えた所（2026-09-21）

### 14.1 4 節を書き直した体が変えた所（`2112d0c9`）

⛔ この書き直しでは、3 節（表 T-276 と `UD-1` 〜 `UD-5`）は 1 字も変えていない（3 節を変えたのは 14.2 の当てる体である）。  
変えたのは、4 節の割り方と、それに引かれて偽になった所だけである。  
測った木は `2112d0c9` である。

| # | 本書の元の文 | 書き直した形 | なぜ |
| --- | --- | --- | --- |
| 1 | 冒頭の「読んだ木: `f73f1ec3`」だけ | 「4〜11 節は `2112d0c9` で測り直して書き直した」を足した | 4〜11 節の行番号はすべて新しい木のものである |
| 2 | 0 節 ② 「`switch` は 18 個の `case` を持ち、そのうち 生成・予実・見た目・帰属 の 4 群は…（4.1 節）」 | 「20 個の `case` を持ち、表 T-075 が負わせる 6 件の要求は 5 つのまとまりに分かれる（4.2 節）」 | `:535-1001` の `case` のラベルを数えると 20 である（`setTaskFadeInDays` と `setTaskFadeOutDays` は 1 つの腕を共有する）。<br>「帰属」の群は 4.2 で元のファイルに残した |
| 3 | 「2026-09-21 に 2 度目の測り直しをした」（0 節 ②、0 節 ③ の 3、1 節 (l) の 2 か所） | 「2026-09-21 に `2112d0c9` で 3 度目の測り直しをした」 | 依頼どおり測り直した —— そのとき `UD` は未登録であった（登録の数は 160）。<br>木が使う表の最大は `T-277` であり、`T-276` は `docs/spec` に 0 件である。<br>`check-identifier-reservation.py` は緑（終了コード 0） |
| 4 | 0 節 ③ 「下の 5 つ」、その 4「`editTask` と `editTaskGroup` を別々の波にした」 | 「下の 7 つ」。<br>4 を「同じ波で並行させてよい」に改め、6（共変の数え方）と 7（残りを元のファイルに置く）を足した | 8 節の書き直しと、4.1 の手順を本書が決めたため |
| 5 | 4 節の全体（16 ファイル） | 4 節の全体（15 ファイル。<br>4.0 〜 4.7） | 4.0 の 2 つの測定 |
| 6 | 5.2 の表の `edit-document.ts:19-20` の行と `repriced` の行 | `edit-document.ts:10-20` の 9 つの名を並べ、`repriced` の再公開元を `percent-complete.ts` にした。<br>入口に残る名の読み手 2 行を足した | `edit-document.ts:10-20` は型 7 つと関数 1 つを `./edit-task` から再び公開している（測った）。<br>`CycleSurroundings` と `CycledPlanActual` は `:297-305` にあり、4.2 で `task-plan-actual.ts` へ移る |
| 7 | 6 節「68 から 84」「`EditDocument` は 9 から 16 ファイル」 | 「68 から 83」「9 から 18」、`SvgRenderer` と `ImportDocument` の数を足した | 4.6 |
| 8 | 7 節の表と「4 回を終えた最良の値 8412 → 6796、626、73」 | 表を 4.2〜4.5 の範囲で数え直し、最良の値を取り下げた | 最良の値は 4 つの関数が帯の中へ消える前提であった。<br>本書の割り方では振り分けの `switch` が 20 分岐を持って残り、`builtMerge` は丸ごと移る |
| 9 | 8 節「波 1〜3」「波 2 と 波 3 を分ける」、`edit-task.ts:929` | 「波 1・2」、`editTask` と `editTaskGroup` を 1 つの波で並行、表 T-075 も前に立つ者が書く共有ファイルとした。<br>`:929` を `:930` にした | `c6407388` が `edit-task.ts` に 1 行足したので、使う場所は `:930` である。<br>表 T-075 は 4 つの分割が共に書き足す 1 本のファイルである（検査 18） |
| 10 | 9 節の問い ① ② | 問い ① に「小さい兄弟をまとめるか」を足し、問い ② を「出口を何にするか」に改めた。<br>3 つの反証の手を 9 節の頭に表で書いた | 4.7 が新しい出口を提案したため |
| 11 | 10 節の `UF-87`・`SU-3` 84・`HELD` 73・`excess` 6796 ／ 626・接頭辞の登録 158 → 159 | `UF-86`・83・見込みの行数・「見込みを書かない」・160 → 161。<br>「負う要求の共有」の行を足した | 4.6 と 7 節。<br>接頭辞は 2026-09-21 に 160 件であった（検査 27 の出力も 160 と刷る） |
| 12 | 11.1 と 11.4 の `UF-87`・84・16・5 ／ 7 ／ 4 ファイル | `UF-86`・83・18・3 ／ 9 ／ 3 ファイル。<br>残る 4 行の「負う要求」の欄を減らすことを足した | 4.6 |
| 13 | 12 節「3 つの波」と「`mergedDocument` を割ること …変更の理由が別である」 | 「2 つの波」、`mergedDocument` は丸ごと移すと改め、「残る腕を命令の表で引く形に書き換えること」を足した | 4.5 と 7 節 |

⚠️ **この書き直しでは 1 節は変えていない** —— `f73f1ec3` で測った記録だからである（(l) の 1 行は 14.2 の #22 で当てる体が変えた）。  
`2112d0c9` では 1 節の数のうち 3 つが動いている: `editTask` は 487 行（1 節 (c) は 486）、`editTaskGroup` は 434 行（(d) は 433）、`excess-lines` は 8414（(m) は 8412）。  
3 つとも `c6407388`（行に編集グループを持たせた変更）が `edit-task.ts` と `edit-task-group.ts` に 1 行ずつ足したことによる。

### 14.2 ⭐ 利用者の 3 つの裁定を受けて、当てる体が変えた所（2026-09-21、`60a2e3d7`）

⛔ 利用者は 9 節の 3 つの問いに、前に立つ者が書いた選択肢を選んで答えた。  
当てる体はそれを本書へ書き込み、`builtMerge` を外した木から数を数え直した。  
測った木は `60a2e3d7` である（`2112d0c9` の上に、別のセッションの `CR-436` が 1 コミット積まれた木）。

| # | 本書の元の文 | 書き直した形 | なぜ |
| --- | --- | --- | --- |
| 14 | 冒頭「状態: 起草。まだ当てていない」、開くもの「`editTask` / `editTaskGroup` / `svgFromSchedule` / `builtMerge` を割る」 | 「一部を当てた」と、当てた 3 つ・波へ送った 4 つを書いた。<br>開くものから `builtMerge` を外した | 裁定 ③ と、11.1 の着地の順 |
| 15 | 0 節 ③ の 2・3 —— 接頭辞 `UD` をどの木も使っていないとした文と、表の番号の根拠（`2112d0c9` で 3 度目の測り直し） | 起草の時点の事実と、当てる直前の測り直し（`60a2e3d7` で 4 度目）に分けて書いた | 当てて `UD` を登録したので、未使用とした文は偽になる。<br>`check-identifier-reservation.py` は、変更要求が未使用・最大・登録の数と書いた文を、当てる日の木で測り直す（`02-changing-the-spec.md` の 2.5 節） |
| 16 | 3 節の 表 T-276 —— 行に `（MUST）` 5 つと `（MUST NOT）` 2 つ、`UD-3` に `CR-125` §2-4、`UD-4` に `3d1d9f1` | `（MUST）` は表を指す 1 文の 1 つだけにし、行から印を外した。<br>`CR-125` §2-4 と `3d1d9f1` は表の下の段落へ移し、仕様には写していない | 検査 39 はすでに赤である（1369 に対して 1381）。7 つ書くより 1 つのほうが悪化が小さい。<br>仕様は変更要求の番号もコミットの sha も持たない（検査 7・検査 54）。<br>2 つとも前に立つ者が認めた |
| 17 | 4 節の見出し「4 つをどう割るか」、4.5 の表（3 ユニット、924 行） | 「3 つをどう割るか（`builtMerge` は外した）」。<br>4.5 は外した記録と理由だけにした | 裁定 ③。<br>`import-document.ts` のコミットは 9 件で、誕生でなくコードを変えたものは 0 件である |
| 18 | 4.6「合計 15、`UF-72` 〜 `UF-86`」、同居の表「20 件 → 6 件」 | 「合計 12、`UF-72` 〜 `UF-83`」、「16 件 → 6 件」 | 裁定 ③ で 3 ユニットと 4 件の要求（`FR-015`・`FR-022`・`FR-056`・`FR-087`）が抜けた。<br>16 件は `60a2e3d7` の 表 T-075 の `UF-11`・`UF-12`・`UF-32` の欄から数えた |
| 19 | 4.6 の行 ID「木の最大の続きから採る」 | 帯 `UF-72` 〜 `UF-83` を 2026-09-21 に別のセッションと取り決めた、と書いた | ⛔ **最初に配られた帯は `UF-69` 〜 `UF-80` であり、いま在る行 `UF-69`・`UF-70`・`UF-71` と重なっていた。**<br>最大を行の数（68）から推して、測っていなかった —— 測った最大は `UF-71` で、欠番が `UF-43`・`UF-44`・`UF-52` にある。<br>当てる体が重なりを見つけて止まり、前に立つ者が帯を `UF-72` 〜 `UF-83`（相手は `UF-84` 以降）に取り決め直した。<br>⭐ 2.5 節の教訓 —— 最大は数えた行の数ではなく、行 ID そのものから測る |
| 20 | 4.7「提案（⛔ 利用者の裁定が要る）」、20 件・3 組 6 件、15 行と残る 4 行 | 「利用者の裁定で決めた」、16 件・3 組 6 件、12 行と残る 3 行。<br>いまの数 16 と目標 6 を書き、「行数でも帯でもない」を足した | 裁定 ② と ③ |
| 21 | 5.1 の `ImportDocument` の行「`builtMerge` は輸出されていない」 | 「段 4 は割らない（4.5）」 | 裁定 ③ |
| 22 | 1 節 (l) の、登録の数 160 と、接頭辞 `UD` を未使用とした文 | 起草の時点の事実と、当てたあとの登録の数（161）に分けた | #15 と同じ |
| 23 | 6 節「68 から 83」、推奨と「9 節の問い ①」 | 「68 から 80 —— 利用者の裁定で平らのまま」。<br>`SU-3` の 80 は最後の波のあとで書くと足した。<br>5.3 節の「最も多くのユニット」の文を波 2 が直すと足した | 裁定 ① と ③。<br>ファイルが無いうちに 80 と書くと、最後の波まで偽の数になる |
| 24 | 7 節の `builtMerge` の行と「4 つの分割」 | 行を落とし、「3 つの分割」にした | 裁定 ③ |
| 25 | 8 節「4 つの順」、波 1「`svgFromSchedule` ／ `builtMerge`、2 体を並行」 | 「3 つの順」、波 1 は `svgFromSchedule` の 1 体 | 裁定 ③ |
| 26 | 9 節「返す問い」（問い ①・②） | 「利用者が答えた問い」—— 3 つの答えの表と、互いに数を動かすことの注意 | ⛔ 利用者が答えた問いを本書が問い続けてはならない |
| 27 | 10 節の `UF-86`・`UT-8` 〜 `UT-10`・`SU-3` 83・`HELD` 落とす 6 ／ 足す最大 15・共有 20 → 6 | `UF-83`・`UT-8`・`UT-9`・80・落とす 4 ／ 足す最大 13・共有 16 → 6 | 裁定 ③。<br>`HELD` の見込みは 7 節の表の 3 行を足した |
| 28 | 11.1 の各行、11.3 の「2 つの問いに答が来たら」、11.4 の `import-document/` に 3 ファイル | 当てた 2 行に「当てた」、波へ送る 3 行に「各波の分割のコミットで」と書いた。<br>11.3 は前に立つ者が裁定を積むと改めた。<br>11.4 は 0 ファイル | 検査 18 は表と `src/` のファイルを同じコミットで一致させる —— 2026-09-21 に 表 T-075 へ 1 行だけ足して `generate_unit_tree.py --check` を走らせると、終了コード 1 で `MISSING` を刷った（試したあと元に戻した） |
| 29 | 12 節の「`mergedDocument` を割ること」の行 | 落とし、「`builtMerge` を割ること」の行を足した | 裁定 ③ |
| 30 | 4.6「小さいユニットを 1 つにまとめる案は 9 節の問い ① で比べる」 | 「9 節の ① で比べ、利用者はまとめない A を選んだ」 | 裁定 ① |
