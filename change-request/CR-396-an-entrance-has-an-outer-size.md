# CR-396 — 入口は外形を持ち、表 T-064 が波の足した名前を載せる

> **閉じるもの**: 表示の大きさの波（`CR-390`〜`CR-395`）が閉じずに残した 2 つ。
> - `FR-039` の `DS-9` の床が、どの設定値の行にも無い「26px」を散文から引いていた（Entity の層のユニットが床を計算できない）。
> - 表 T-064 が、同じ波で `src/` に生まれた公開メンバを 4 つ載せていない（検査 26b が赤）。
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `dca48b79`。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-002`**（全体日程を 1 画面で見られる）—— 床は行見出しパネルの描く幅を決める式であり、`CR-394` の決定 10 がその式を置いた。⛔ **いまの形では実装できない**（下の第 1 節）ので、表示の倍率を小さくしたときのパネルの幅が決まらず、日程に返る幅も決まらない。あわせて **`GL-006`**（読む人が表示を変えられる）—— 入口の外形は倍率を変えても変わらない側の値であり、その外形が数として定まらないと `DS-6` / `DS-7` の「掛けない」を実装が確かめられない。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.2`（実装可能性）** —— `FR-039` の床が引く「`S-138` の注の 26px」は、値の表の**注の散文**にしか無い。⛔ 注は規則ではなく（規則 08 の「値の表は値だけを持ち、規則には行 ID で指す」）、指せる行が無いので Entity の層のユニットが床を組み立てられない。
- **`R1.3`（矛盾がない）** —— 同じ注が「入口の外形は 26 × 24px」と述べる一方、`FR-029` は「⚠️ 一辺が定めるのは図形の箱であって、それを載せる入口の外形ではない」と述べていた。⇒ **外形を持つ側が 2 つに割れていた**（注が数を持ち、要求が「持たない」と言う）。決定 1 と 2 で、規則を `FR-029` に、太さを行に置いた。
- **`R1.1`（曖昧さ）** —— 26 の内訳（16 ＋ 4 × 2 ＋ ? × 2）のうち、最後の 2px を持つ行がどこにも無かった（表 T-206 を「枠」「罫」「線の太さ」で引き、`S-143` / `S-174` / `S-194` / `S-110` の 4 行がいずれも別物であることを読んだ）。
- **`R2`（命名）** —— 表 T-006b と 表 T-104 を引いた。`枠` は `S-141`（図形と入口の枠の最低隙間）と `S-174`（選択の枠）が既に使う語なので、新しい行は「入口の枠の線の太さ」と、掛かる相手（入口）を名に含めて呼ぶ。設定値の鍵は持たない（保存しない行である）。
- **`R2`（公開インターフェース）** —— 表 T-064 は自ら全数を名乗るのに、`displayRatioOf` / `drawnSettingsOf` / `DISPLAY_SCALE_STEPS` / `rowBandCeilingOf` の 4 つがコンポーネントの境を越えて読まれながら載っていなかった（検査 26b）。

### ③ 利用者に問わずに決めたこと

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 入口の枠の線の太さを 表 T-206 の `S-237`（1px、保存しない）として立てる | ⭐ 置き場は 表 T-206 である —— 画面の道具の寸法であり、書き出す絵に入口は出ない（表 T-076 の `EP-1` / `EP-4`）。`S-138` がまさにその理由で同表に居り、枠は同じ入口の一部である。⭐ 値 1px の根拠は 2 つ ——（ⅰ）出荷ビルドの入口の CSS が `border:1px solid` を持つこと（`S-141` の角の R が「出荷ビルドの入口の CSS を読んで求めた」のと同じ求め方）、（ⅱ）既存の注の 26 が 16 ＋ 4 × 2 ＋ 1 × 2 でしか成り立たないこと | ⚠️ 出荷ビルドを読んで起こした値なので、枠を太くする設計変更のときに選び直す行が 1 つ増える |
| 決定 2 | 外形の**幅**の式は `FR-029` が持つ（MUST）。行の側は太さだけを持つ | 規則 08「値の表は値だけを持ち、規則には行 ID で指す」。⛔ 外形を px の数として表に持たせることは同要求の MUST NOT で禁じた —— 導く元（`S-138` / `S-141` / `S-237`）と導いた値（26）の両方を持つと、片方だけが直されて離れる | 外形を知りたい読み手が 1 度 `FR-029` を経由する |
| 決定 3 | 外形の**縦**は定めない | ⭐ `FR-029` は「行送りは入口の側が決める」と既に述べており、床が要るのは幅だけである。⛔ 縦を MUST で書くと、行送りを持たない根拠が無いまま数を 1 つ作ることになる | 注が持っていた「26 × 24px」の 24 は、指し先を持たない主張だったので落とした（下の問い 1） |
| 決定 4 | `FR-039` の床は「`FR-029` が定める外形の幅 × `S-235`」と書き、数を書かない。⚠️ 直後の不等式の 69.33 の由来（16 ＋ 4 × 2 ＋ 1 × 2 ＝ 26px の 4 列 × `S-235`）を式の隣に添えた | 規則 08「散文が抱えた数は、書いた席とは別の場所が動いた日に偽になる」。⭐ 不等式そのものは残す —— 床が要る理由の論証であり、行 ID で組み立て直せる形にした | なし |
| 決定 5 | 表 T-252 の `DS-7` の列に `S-141` と `S-237` を足し、欄を「掴み代と入口（図形の箱・隙間・枠）」とする | ⭐ `S-237` の注が「表示の倍率は掛けない（`DS-7`）」と指すので、`DS-7` がその行を名指していなければ指し先が偽になる。⚠️ `S-141` も同じ立場で既に漏れていた（`FR-029` は `S-138` と `S-141` の両方に `S-235` を掛けると定めている） | なし —— 規則は変わらず、名指しが実体に追いついただけである |
| 決定 6 | 表 T-064 の `PI-35` に `displayRatioOf` / `drawnSettingsOf`、`PI-2` に `DISPLAY_SCALE_STEPS`、`PI-18` に `rowBandCeilingOf` を載せる | 同表は 36 コンポーネントの公開する名前の全数を名乗る（5.3）。4 つとも他のコンポーネントのフォルダから読まれている（検査 26b の実測） | ⚠️ `DISPLAY_SCALE_STEPS` は段の並びが原稿と `src/` の 2 か所に載る形のままである（下の問い 2） |

---

## 1. 読んで確かめた事実

| 事実 | 所 |
|---|---|
| 床が引く「26px」は値の表の**注**にしか無く、行 ID を持たない | `_assets/tbl-settings.md` の 表 T-206 の `S-138` の注 |
| 26 の内訳のうち 16 は `S-138`、4 × 2 は `S-141`、残る 2 を持つ行は無い | 表 T-206 を「枠」「罫」「線の太さ」で引いた —— `S-143`（パレットの群の隔て 1 × 6px）・`S-174`（選択の枠 2px）・`S-194`（2 連カーソル 1px）・`S-110`（掴み点の枠線 1.5px）はいずれも入口の枠ではない |
| 出荷ビルドの入口は `border:1px solid` を持つ | `src/framework/dom-screen-surface/dom-screen-surface.ts` の `entryStyle` / `entryFaintStyle`（`S-141` の角の R を起こしたときと同じ読み方） |
| 入口の外形は要求の側の話であり、一辺は図形の箱だけを定める | `FR-029`（「一辺が定めるのは図形の箱であって、それを載せる入口の外形ではない」） |
| 枠の太さと入口の外形を裁いた行は無い | `docs/development-records/rulings.md` を `border` / `枠線` / `1px` / `外形` で引いて 0 件。`pending-decisions.md` も同じ 4 語で 0 件 |
| 表 T-064 が載せていない公開メンバが 4 つある | 検査 26b（`ScreenRegions.displayRatioOf` / `ScreenRegions.drawnSettingsOf` / `DocumentSettings.DISPLAY_SCALE_STEPS` / `InputCommandTranslator.rowBandCeilingOf`）。いずれも `published-members-baseline.txt` にも `crossing-names-baseline.txt` にも無い |
| 生成された定数を 表 T-064 が名乗る先例が 2 つある | 同表の `PI-5`（`NOT_STORED_LABEL_SIZES`）と `PI-19`（`GROUP_GRID_LINE_WIDTH_PX`） |

## 2. 測った

| 数 | 値 | 測り方 |
|---|--:|---|
| 足した `（MUST）` / `（MUST NOT）` | 節の総数 1835 → 1839（**+4**） | 検査 39 の数えであり、当てる前後の run を突き合わせた。⭐ 内訳: `FR-029` +2 `（MUST）` / +1 `（MUST NOT）`、表 T-064 の `PI-35` +1 `（MUST NOT）`。⚠️ 既存の 1 文（箱と隙間に `S-235` を掛ける MUST）は書き足しではなく書き直しである |
| 足した設定値の行 | 1（`S-237`） | `_source/settings.json` の 表 T-206 に 1 件足した。⭐ `S-237` が空席であることは `docs/` `src/` `tests/` `tools/` `.claude/skills` を `S-237` で引いて 0 件と数えてから使った |
| 波が `src/` に足した公開の要素 | **8 件の `export`、うち公開エントリに立つのが 7 件、コンポーネントの境を越えるのが 4 件** | `git diff ac12202e dca48b79 -- src/` の `+export （function｜const｜class｜interface｜type）` を数え、ファイル名が親フォルダ名と同じ（＝ 5.3 が定める公開エントリ）かで割った。内訳: `screen-regions.ts` 3（`displayRatioOf` / `drawnSettingsOf` / `NOT_STORED_DISPLAY_SCALE_BASE`）、`image-exporter.ts` 1・`dom-screen-surface.ts` 1（`NOT_STORED_CHROME_SCALE`）、`document-settings.ts` 1（`DISPLAY_SCALE_STEPS`）、`input-command-translator.ts` 1（`rowBandCeilingOf`）、公開エントリでない `row-title-panel.ts` 1。境を越える 4 件は検査 26b の実測と一致した |
| 表 T-064 に足したメンバ | 4（3 行） | `PI-2` +1、`PI-18` +1、`PI-35` +2 |

⚠️ **境を越えない 3 件（`NOT_STORED_CHROME_SCALE` の 2 件と `NOT_STORED_DISPLAY_SCALE_BASE`）は本書で載せない** —— 検査 26b が「境を越える名前」だけを全数の対象とし、載せる根拠がその越えだからである。⭐ 越えた日に載せる。

## 3. グラフ

**`impact.py FR-029 FR-039 S-138 S-141 S-237 T-206 T-252 DS-7 DS-9 T-064 PI-2 PI-18 PI-35`**: 485 行。

**`induced.py`（上に `S-235` / `S-236` / `S-79` / `S-37` / `S-125` / `FR-085` を足した 19 種）**: 19 種すべて解決、辺 60、閉路 1 —— 大きさ 11（`DS-7` `DS-9` `FR-029` `FR-039` `FR-085` `S-138` `S-141` `S-235` `S-236` `S-237` `S-79`）。⇒ **閉路の員をすべて 1 巡で書いた**（`S-138` の注・`S-237` の注・`FR-029` の段・`FR-039` の床と不等式・`DS-7` の列）。`FR-085` と `S-79` は読んで確かめ、直す所が無いことを確かめた（式が引くのは描いた幅であり、床の内訳を持たない）。

**届いた行を `rulings.md` で引いた**: 枠の太さ・入口の外形を裁いた行は 0 件。`JDG-131` の ④（どの面の入口も `S-235` の 2/3）は決定 1・5 と同じ向きである ⇒ 衝突 0。

## 4. 当てた中身

| ID | 置き場 | 中身 |
|---|---|---|
| `S-237` | `_source/settings.json`（表 T-206） | 決定 1 |
| `S-138` の注 | 同 | 外形の数を落とし、`S-141` / `S-237` と `FR-029` を指す形にした（決定 2・3） |
| `FR-029` | `01-04-requirements.md` | 枠の太さの MUST、外形の幅の MUST、外形を px で表に持たせない MUST NOT、`S-235` を掛ける相手に `S-237` を足した（決定 1・2・3） |
| `FR-039` の床と不等式 | 同 | 決定 4 |
| 表 T-252 の `DS-7` | 同（`FR-039` の中） | 決定 5 |
| 表 T-064 の `PI-2` / `PI-18` / `PI-35` | `05-07-design.md` | 決定 6 |
| 生成物 | `_assets/tbl-settings.md`、`_assets/tbl-row-id-prefixes.md` | `npm run gen` |

## 5. 数の予測

当てる前 `tables=151 figures=11 rows=1961 uids=153`。本書の差: tables 0 ／ figures 0 ／ **rows +1**（`S-237`。表 T-064 と 表 T-252 は行を増やさずセルを直しただけである）／ uids 0 ⇒ 予測 `rows=1962`。

⭐ **当てたあとの実測**: `tables=151 figures=11 rows=1962 uids=153` —— 予測どおりであった。

## 6. 実装と試験で直す所（本書は直さない）

| 所 | 何が動くか |
|---|---|
| `tools/generate_entity_types.py` と、床を組む側（表 T-064 の `PI-35` の `ScreenRegions`） | `S-237` を保存しない定数として刷り、床（`FR-039`）が `S-138` / `S-141` / `S-237` / `S-235` から外形の幅を組めるようにする。⚠️ **`S-235`（`NOT_STORED_CHROME_SCALE`）はいまアダプタと枠組の 2 か所にしかなく、Entity の層に無い** —— 床を Entity で組むなら、`S-236` と同じく `screen-regions.ts` へ刷るのが閉路を作らない置き場である |
| 行見出しパネルの描く幅（`row-title-panel.ts` と `ScheduleGeometry`） | `DS-9` の床を当てる（いまは当たっていない。`dca48b79` の「Not in this commit」が名指す） |
| 入口を描く側（`dom-screen-surface.ts`） | 枠の太さを `S-237` から読む（いまは CSS に直に 1px を書いている）。⚠️ `S-235` を掛ける相手に枠を足す |
| `DISPLAY_SCALE_STEPS`（`document-settings.ts`） | 段の並びを原稿（表 T-202 の `S-234` の型の欄）から刷る道に替える（下の問い 2） |
| 検査 30 の名簿 | `NOT_STORED_CHROME_SCALE` / `NOT_STORED_DISPLAY_SCALE_BASE` が `docs/development-rules/03-implementation.md` の生成定数の一覧に無い（`dca48b79` から赤のまま。本書の持ち場の外） |

## 7. 試験が主張すべきこと

1. 入口の外形の幅が `S-138` ＋ `S-141` × 2 ＋ `S-237` × 2 に `S-235` を掛けた値である（`FR-029`）。原稿の `S-237` を 1 から 3 へ変えると幅が動く。
2. 表示の倍率を 33 と 100 に変えても、入口の外形の幅が変わらない（`DS-7`）。
3. 描く行見出しパネルの幅が、倍率 33 で床（`S-37` × 描く比 × `S-125` ＋ `S-138` × `S-235` ＋ 外形の幅 × `S-235` × 4）に等しい（`FR-039`）。
4. 表 T-064 の `PI-2` / `PI-18` / `PI-35` が名乗る名前が、それぞれの公開エントリから読める（検査 26b が回す）。

## 9. ⛔ この変更でやらないこと ／ 返した問い

- ⚠️ **問い 1（前に立つ者へ）**: `S-138` の注が持っていた「入口の外形は 26 × 24px」の **24** を落とした。⭐ 落とした理由は、`FR-029` が行送りを入口の側に委ねており、24 を裏づける行が無いこと（出荷ビルドの入口は `min-height` を内容の箱に置くので、枠を数えれば縦も 26px になる）。⛔ **縦を定める必要があるなら、それは別の変更要求である** —— 代償は、書き出しに入口が出ない以上、縦を定めても確かめる場所が画面にしかないこと。
- ⚠️ **問い 2（前に立つ者へ）**: `DISPLAY_SCALE_STEPS` の段の並びを、原稿から**刷る**か、いまのように `src/` に**書く**か。⭐ **推奨は刷る** —— 規則 08 の「同じことを 2 度書きたくなったら置き場を 1 つ決め、他方は指す」と、`PI-2` が既に「原稿を刷った 3 つの定数」を持つ先例に従う。並びの正が 表 T-202 の `S-234` の型の欄であることは本書が `PI-2` のセルに書いたので、**仕様の側はどちらでも真である**。⛔ **代償**: 刷るには `tools/` の生成器を 1 本足す（本体の持ち場の外）。⚠️ 刷らないままなら、`S-234` の段を足した日に `src/` の並びが黙って古くなる（検査は無い）。
- ⛔ `S-116`（ヘッダーの高さの上限）を変えない
- ⛔ 表 T-064 に、境を越えていない 3 つの生成定数を載せない（第 2 節の理由）
- ⛔ 検査 30 と 検査 45 の赤を触らない —— どちらも `dca48b79` から在り、直す所が `docs/development-rules/` と `src/` にある
