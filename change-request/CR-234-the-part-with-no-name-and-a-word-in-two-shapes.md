# CR-234 — 名の無い UI パーツと、2 通りに書かれていた 1 つの語

> **閉じるもの**: ⛔ **通知が立つ場所に確定名が無く、`data-role` が小文字の作り名を運んでいた。**
> **実 DOM の 18 種のうち、これ 1 つだけが 表 T-103 の名を運んでいなかった。**
> ⛔ **同じ概念が仕様書の中で「知らせ」と「通知」の 2 通りに書かれていた。**
> ⭐ **利用者の裁定（2026-08-23）: 確定名は `Notification Area`。語は「通知」に統一する。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`。受ける目標は `GL-006`。** ⛔ **同じものが 2 つの名で呼ばれた瞬間、読む者は「違うものかもしれない」と考えなければならなくなる**（規則 03 の 1）。
⚠️ **`data-role` のほうは実害が測れている** —— **人が探せる名で画面に出ていなかった。**

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.3`**（唯一の正がある）| 通知が立つ場所 | ⛔ **表 T-103 に行が無く、`data-role` は `dom-screen-surface.ts` が作った小文字の名を運んでいた。** `W-4`（MUST）は「表 T-103 の名をそのまま書く」と定めており、**書く名が存在しなかった** |
| **`R1.2`**（曖昧さの排除）| 「知らせ」と「通知」| ⛔ **同じ概念が 2 通りに書かれていた** —— `U-55` の注は既に「通知（`Notice`）ではない」と書き、表 T-037 の見出しは「知らせるときの作法」と書いていた |
| **`R1.1`**（矛盾が無い）| `uf-71` のある件 | ⛔ **「表 T-103 がまだ行を持たない唯一のメンバ」を名乗る件が在った。** 本 CR がその前提を消す |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **過去の記録は掃かない** —— `A-appendix` の変更履歴・`change-request/`・開発の記録 | ⛔ **当時そう書いたという記録である。** 規則 02 の 4. が「変更履歴の過去の行を書き換えない」と定める |
| 2 | ⭐ **開発者向けの散文に残る「知らせなかった」は残した** | **2 か所とも「生成器が開発者に警告しなかった」という話**であり、製品が人に出す通知ではない。⚠️ **どちらも自然な日本語である** |
| 3 | ⭐ **値の型 `Notice` は動かさない** | **表 T-103 が名づけるのは場所であって、立つものではない。** ⚠️ **英語が `Notification`（場所）と `Notice`（値）の 2 語になることは `U-57` の注に書いた** |
| 4 | ⭐ **`ScreenView` の公開メンバ名 `notices` も動かさない** | ⛔ **`W-4` が縛るのは `data-role` であり、`W-6` が縛るのは散文である。公開メンバ名を縛る行はどこにも無い**（体が測って報告した）|

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| `知らせ` の全形 | **83 か所 / 20 ファイル**（生きた散文のみ。過去の記録を除く）|
| 形の内訳 | `知らせること` 29 ／ `知らせる` 24 ／ `知らせと` 7 ／ `知らせ方` 5 ／ `知らせた` 5 ／ ほか |
| ⛔ 小文字の `data-role` | **1**（`notices`）—— ほかの 17 種はすべて 表 T-103 の名 |
| 表 T-103 の行数 | 57 → **58**（`U-56` が末尾だったので `U-57`）|
| 落ちた試験 | **5 件**（`uf-71` 3 ／ `uf-72-screen-part` 2）—— **すべて小文字の名を打ち直していた試験台** |

⚠️ **うち 1 件は空配列に対する `.every(...)` で、名が偽でも決して落ちなかった** —— 体が掃きながら見つけた。

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | 表 T-103 | `U-57` `Notification Area` を 1 行。⛔ **「`Esc` の第 1 階層で閉じる面ではない」と明記**（`S-99g`）|
| 2 | 生きた散文 20 ファイル | `知らせ` → `通知`（順序付きの置換 15 本。長いものから当てる）|
| 3 | `_source/display-words.json` | `RS-9` の文の 1 語。⛔ **生成物は手で直さず `npm run gen` で作り直す** |
| 4 | `dom-screen-surface.ts` | `data-role` を確定名へ |
| 5 | A-appendix | 変更履歴に 1 行 |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 133 | **133** | 0 |
| `figures` | 11 | **11** | 0 |
| `rows` | 1631 | **1632** | **＋1**（`U-57`）|
| `uids` | 148 | **148** | 0 |
