# CR-385 — 行の掴み代の印を、罫より少しはっきり描く

> **閉じるもの**: 利用者の申し立て 2026-09-15 の L と、その答え —— 逐語は `docs/development-records/rulings.md` の各行。
> - `JDG-94`: タスクグループ名を移動するための `::` が見にくい。あと 5% コントラストを上げろ。台帳 `DFC-601`
> - `JDG-109`（同日）: 「提案通りにやれ」 —— 前に立つ者の案 ①（`S-149` の明度を地 `S-150` から 5 ポイント離す）を採る。`JDG-94` の「5% を何で測るか」への答え
>
> ⭐ **起草して、同じ波で `docs/spec` へ当てた。** 読んだ木は `87861cf3`。`src/` と `tests/` には触れていない。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-008`（描いたものを直接掴んで変えられる）**。表 T-051 の `HF-15` 自身が「掴めることが読めなければ、掴もうとする手が動かない」と述べる、その読める度合いを上げる。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（唯一の正）** —— `HF-15` は「掴み代は普段は罫の色で描かれる」と理由の文で述べるだけで、色を値として持つ行が無かった（コードは `S-149` を借りている）⇒ 表 T-236 に行を 1 つ立て、`HF-15` がそれを名指す。
- **`R1.1`（曖昧さがない）** —— 「5%」は明度とも比とも読める ⇒ `JDG-109` が明度（地から 5 ポイント離す）に決めた。数は導出（測り方）と一緒に行の備考へ置いた。

### ③ 利用者に問うたこと・**問わずに決めた**こと

**問うて裁定を得た**: `JDG-94`、`JDG-109`（前に立つ者が案 ① と ② を示し、利用者が「提案通りにやれ」）。

**問うこと**: なし（0 件）。

**問わずに決めた（覆してよい）**:

| # | 決めたこと | 導き |
|---|---|---|
| 決定 1 | 値は 2 つのテーマのセルに色として書き、導き（`S-149` の明度を `S-150` から 5 ポイント遠ざける）を備考に書く | 表 T-236 の列は色の値（`light` / `dark`）であり、式を置く列が無い。`S-151` の備考が明度を選んだ理由と実測を持つ前例に揃えた |
| 決定 2 | 行 ID は `S-231`（設定値の最大 `S-230` の次。`grep` で 0 件）、置き場は表 T-236 の `S-150` の直後 | 規則 02「番号を振り直さない」。導きの元の 2 行（`S-149` / `S-150`）の隣に置く |
| 決定 3 | `GR-20` には値を書かず、`HF-15` を指すだけ | 同じ MUST を 2 か所に置かない（表 T-023d の結びの MUST NOT） |
| 決定 4 | `FR-029` の薄い入口の色（`S-149`）は変えない | 申し立ては掴み代の印だけ。薄い入口は「在るが主張しない」濃さを理由に `S-149` を名指している |

---

## 1. 測った（前に立つ者の数を自分で測り直した）

測り方: Python の `colorsys` で HSL → RGB、WCAG の相対輝度 L、比 ＝ (L1 + 0.05) ÷ (L2 + 0.05)。色相 214。

| テーマ | 地（`S-150`） | いまの印（`S-149`） | 比 | 新しい印（`S-231`） | 比 |
|---|---|---|--:|---|--:|
| 明るい | hsl(214 20% 97%) | hsl(214 14% 87%) | 1.271 | hsl(214 14% 82%) | **1.445** |
| 暗い | hsl(214 14% 13%) | hsl(214 12% 23%) | 1.411 | hsl(214 12% 28%) | **1.711** |

⇒ 前に立つ者の数と一致した。

## 2. グラフ

**`impact.py HF-15 GR-20 S-149 S-150 T-236`**（138 行）: `HF-15` は要求 4（`FR-085` `FR-004` `FR-016` `FR-076`）/ 参照 16、`GR-20` は要求 3 / 参照 5。
**`induced.py`**（`CR-380` と同じ 50 種）: 閉路 `GR-20 ↔ HF-15`（大きさ 2、掴み代の場所と振る舞いを互いに指す作法）⇒ 2 つを 1 度で書いた。
**`rulings.md`**: `HF-15` / `GR-20` / `S-149` → `JDG-94` だけ。衝突 0。

## 3. 当てた中身

| 対象 | 置き場 | 中身 |
|---|---|---|
| 表 T-236 に `S-231` | `docs/spec/_source/settings.json`（`npm run gen`） | 掴み代の印の色。明るい `hsl(H 14% 82%)`、暗い `hsl(H 12% 28%)`、色相追随 ○。備考に導き・実測・測り方 |
| 表 T-051 `HF-15` | `01-04-requirements.md` | 掴んでいないあいだ印を `S-231` で描く（MUST）。握っているあいだ `S-151`（据置）の理由を「普段は `S-231` で描かれるので」に |
| 表 T-023d `GR-20` | 同 | 「印の色は `HF-15` が持つ（`S-231`）」を 1 文 |

## 4. 数の予測

tables 0 / figures 0 / **rows +1（`S-231`）** / uids 0。

## 5. 影響 —— 本書の外で動くもの

| 所 | 何が動くか |
|---|---|
| `tools/generate_entity_types.py` | 表 T-236 の行を ID で選んで読む（`rows of table T-236 one unit needs, by row ID`）ので、`S-231` を読む単位へ足すのは実装の波。⭐ 足さなくても生成は落ちない（`npm run gen` と `gen:check` が通った） |
| `src/framework/dom-screen-surface/dom-screen-surface.ts` の `rowGrabStripStyle` | `S-149` から `S-231` へ（実装の波） |
| `docs/development-records/rulings.md` | `JDG-94` を `適用済`、`JDG-109` を足す |
| `docs/development-records/defects.md` | `DFC-601` を `裁定待ち` → `実装待ち` |

## 6. ⛔ この変更でやらないこと

- ⛔ `S-149`（罫の色）と `FR-029` の薄い入口の色を変えない
- ⛔ 印の形（`⋮⋮`）と場所（`GR-20`）を変えない
- ⛔ `src/` と `tests/` に触らない
