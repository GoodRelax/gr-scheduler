# CR-237 — 問う面が 6 つ立ったのに、問いの文をどこからも読めない

> **閉じるもの**: ⛔ **表 T-037 の `NT-7` は「何が起きるかを示したうえで、続けるか取りやめるかを選ばせること（MUST）」と定める。**
> ⛔ **問う場所は 6 つとも要求が MUST で持っているのに、`FR-038` の辞書には問いの文で引ける節が 1 つも無い。**
> ⚠️ **`src/` の 3 か所（`confirmationOwedBy` ／ `askToWriteOverDestination` ／ `askToDiscardCurrentDocument`）は、どれも `RaisedConfirmation.text` を空文字で上げている。**
> ⭐ **利用者の裁定（2026-08-23 ／ 2026-08-24）: 表 T-234 を立て、行は 8 本とする。**

⚠️ **本 CR は `CR-232` の下書きを引き継ぐ。** ⛔ **下書きのまま当てていない** —— 踏査で 3 つ動いていた（本書 4.）。

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4`（説明を読まずに操作できるようにする）。受ける目標は `GL-006`。**
⛔ **文の無い確認は、何を消すのかを言わずに「続ける／取りやめる」を選ばせる面である。**
⚠️ **`FR-031` が確認の階級を「取り消しで取り戻せないものを失う場面」に絞っている以上、その面は最も説明を要する面である。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `NT-7` の「何が起きるか」| ⛔ **辞書の 9 節はどれも問いの文で引けない。** ⭐ **`CR-228` が 表 T-233 で閉じた穴とまったく同じ形であり、あちらは理由、こちらは問いである** |
| **`R1.1`**（矛盾が無い）| 問う場所の全数 | ⛔ **下書きが 5 つと数えたが、逐語を引き直すと 6 つであった** —— `FR-062` の「自動保存の刻印が開いた文書のそれと 1 つでも違えば確認を求めること（MUST）」と、`FR-026` の「異常終了した次の起動で復旧を申し出ること」が抜けていた |
| **`R2.18`**（公開する契約が自分で意味を成す）| `RaisedConfirmation.text` | ⚠️ **型は `string` で、何を入れてよいかを言う先が無い。** 本 CR で 表 T-234 の行になる |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **鍵は行 ID（`QN-n`）とする** | **表 T-233 と同じ形にする。** ⛔ 同じ仕組みに 2 つの流儀を作らない。⚠️ 接頭辞を全数抽出したところ `Q` で始まるものは 1 つも無く、衝突しない |
| 2 | ⭐ **消えるものの名前は辞書に置かない** | **`NT-7`（MUST）が挙げよと定めるのは対象の名前であり、それは文書の値である。** ⛔ 辞書に置けば文書の中身を翻訳置き場に持つことになる。`RaisedConfirmation.items` が既に運ぶ |
| 3 | ⭐ **`QN-5` は名前を挙げる** | ⚠️ **仕様はどちらとも書いていない。** 実装 `askToDiscardCurrentDocument` は `items` に文書名を 1 件渡しており、`DI-4` のように「挙げる義務は無い」と書いた行も無い。⛔ **挙げないと裁定するなら、`QN-5` のセルと実装の両方が動く** |
| 4 | ⭐ **`FR-095` の行を立てない** | **同要求が「表 T-024a の `OP-4` と同じ確認を行うこと（MUST）」と自ら書いている。** ⛔ 同じ問いに 2 つ目の鍵を作らない |
| 5 | ⭐ **`OP-3` と `MG-1` を入れない** | **どちらも `Confirmation`（`U-55`）ではない** —— `OP-3` は `Open Chooser`（`U-56`）で選ばせる面、`MG-1` の選択肢は `FR-022` が持つ。⚠️ **この一文を前文に置く** —— 次の読み手が足しに来ないようにするためである |
| 6 | ⭐ **辞書の語は空で立てる** | `PD-160` の作法にならう。⛔ **生成器も原稿も語を発明しない** |

---

## 1. 測った

```
induced.py  NT-7 FR-031 FR-099 UN-17 T-027 FR-038 S-99 T-206 T-233
            NT-4 FR-060 FR-026 FR-065 FR-086 T-014 ST-7 FR-028 FR-033 T-064
impact.py   FR-076 T-233 T-014 UN-17
```

| 測ったこと | 実測 |
|---|---|
| 種のうち仕様書に在るもの | 19 / 19 |
| 種の内側の辺 | 12 |
| 閉路 | **1 つ**（size 2: `FR-031` `NT-7`）|
| ⛔ **本 CR がその閉路を踏むか** | ⭐ **踏まない** —— `FR-031` も `NT-7` も 1 文字も動かさない |
| ⛔ 問いの文で引ける辞書の節 | **0**（辞書は 9 節）|
| 空文字で上がっている問い | **3 か所** |
| `NT-7` を MUST で求める要求 | **6 場面**（`FR-032` が 2 ／ `FR-099` ／ `DI-4` ／ `OP-4` ／ `FR-062` ／ `FR-026`）|

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-076` の本文 | 表 T-233 の締めの注の直後に、表 T-234 を宣言する段落を置く |
| 2 | 表 T-234（新設）| 8 行。欄は 行 ID / 場面 / 名前を挙げるか / 正 |
| 3 | Chapter 6.2 の原稿の節 | 名簿の出どころに 表 T-234 を足す |
| 4 | `_source/display-words.json` | `questions` 節（8 項目 × `text`。語は空）|
| 5 | `tools/generate_display_words.py` | 表 T-234 から名簿を起こす（**仕様の外**）|
| 6 | A-appendix | 変更履歴に 1 行 |

⛔ **`FR-032` と `FR-099` と `DI-4` と `OP-4` と `FR-062` と `FR-026` は 1 文字も動かさない** —— どれも既に正しい。欠けていたのは文の置き場である。

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` | 133 | **134** | **＋1**（表 T-234）|
| `figures` | 11 | **11** | 0 |
| `rows` | 1634 | **1642** | **＋8**（`QN-1` 〜 `QN-8`）|
| `uids` | 148 | **148** | 0 |

⚠️ **辞書の枠は 216 → 224 になる**（8 行 × `text` の 1 欄）。

## 4. ⛔ 下書き（CR-232）から動いていた 3 つ

| | 何が違ったか |
|---|---|
| **1** | ⛔ **問う場所は 5 つではなく 6 つ** —— `FR-062` の負けた自動保存の確認（MUST）と、`FR-026` の復旧の申し出が抜けていた。⭐ **後者を問い側に置くのは利用者の裁定である**（表 T-037 の `NT-4` が逐語で「復帰・**復旧の確認**」と書いている）|
| **2** | ⛔ **数の基数が古い** —— 下書きは `rows` 1630 と書くが実測は 1634、辞書は 214 と書くが実測は 216 |
| **3** | ⚠️ **下書きの `R1.1` の根拠が弱い** —— `frame-loop.ts` の「2 か所でありほかに無い」は `FR-032` の 2 か所を指しており、`FR-099` と矛盾してはいない。**実測される事実は「`FR-099` の確認が `src/` に 1 か所も無い」（欠落）である** |
