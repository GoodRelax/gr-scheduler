# CR-219 — CR-217 が「動かない」と予測した 1 メンバ

> **閉じるもの**: ⛔ **CR-217 の §1 は「表 T-064 は動かない」と予測したが、外れた。**
> ⭐ **`PD-169` を実装すると、`pressRowOf` を公開せざるを得なかった。** 表 T-064 の `PI-18` はそれを名乗っていない。
> ⛔ **過去の CR は書き換えない**（2026-08-22 の裁定）。**新しい CR で仕様側を確定させる。**

## 0. ⛔ 立ちどまって答える 3 つ（**検査 22 が空欄を許さない**）

### ① これは `CH-` / `GL-` のどれを前へ進めるか

⭐ **どれでもない。基盤の作業である** —— 規則 01 の ① 自身が「基盤の作業は正当な答えである」と定めている。
⚠️ **親の CR-217 が前へ進めたのは `CH-3`（受ける目標は `GL-003`）であり、本 CR はその公開面の宣言を揃えるだけである。**
⭐ 表 T-064 が公開面の正であるという性質を保つための直しである。

### ② `review-standards` のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R2.19`**（コンポーネント境界）| 表 T-064 の `PI-18` | ⛔ **本物の欠落。** 同条項は「各コンポーネントの公開面が Ch5.3 に宣言されているか」を MUST で問う。**シェルが呼ぶ関数が表に無い状態は、その宣言が 1 つ欠けている状態である** |
| **`R1.3`**（唯一の正がある）| 予測と実測 | ⛔ **予測が外れたことを黙って直さない。** 変更履歴に外したと書く |

### ⑧ 利用者に問わずに決めたこと（**覆してよい**）

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **`pressRowOf` を `PI-18` の欄へ足す** | ⛔ **裁定は「`PI-18` に `pressRowOf` を公開する」案を退けたが、退けたのは「シェルが `pressed` の隣に第 2 の値を持ち、書き込みの時に訊く」形である。** ⭐ **いま在るのは「押下の時にメンバを 1 つ埋めるために呼ぶ」形であり、裁定の「`PointerPress` が運ぶ」をそのまま実装したものである** —— 呼び手が値を解決して運ぶ形は `hit`（`PI-7` の `itemAtPointer` を呼ぶ）と同じである |
| 2 | ⭐ **`PressRow` の型も名乗らせる** | `PI-5` が `ScheduleLayout`（型）を、`PI-9` が `Refusal`（型）を名乗っている前例に従う。⚠️ **`PointerPress` を名乗らせるかは本 CR の射程外**（`PD-169` の調査が挙げた別件であり、いま公開されている型は他にもある）|

---

## 1. 測った

```
impact.py   PI-18
induced.py  PI-18 T-064 IF-2 T-023a AG-9
```

⭐ **表 T-064 の前書き（`:325`）が「本表は名前と、それが何を担うかだけを持つ。引数・戻り値は `src/` の公開エントリが持つ」と定めるので、欄に足すのは名前と担いだけである。**

---

## 2. 編集計画

| # | 対象 | すること |
|---|:-:|---|
| 1 | 表 T-064 の `PI-18` | メンバの列に `PressRow`（型）と `pressRowOf` を足す |

---

## 3. 数の予測

| | 前 | 後 | 差 |
|---|--:|--:|--:|
| `tables` | 132 | **132** | 0 |
| `rows` | 1609 | **1609** | 0 |
| `uids` | 148 | **148** | 0 |
| `figures` | 11 | **11** | 0 |

⭐ **セルを 1 つ厚くするだけである。**
