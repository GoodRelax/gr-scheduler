# CR-355 — `Agent API` を有効にした覚えを、文書ごとからブラウザごとへ移す

⭐⭐ **利用者の裁定 2026-09-05（逐語）**: 「**裁定 5: 提案通り**」
⇒ **案 ④（ブラウザごとに一つ覚える）。⛔ 代償は明示して裁定を受けている** ——
**一度許すと、同じブラウザで開いた別の予定表にも及ぶ。**

**台帳の行**: `D-280`。**未裁定の行**: `PD-422`。

⛔ **`induced.py` を先に走らせた**（規則 02）—— **種 `FR-065` `S-99b` `AT-1`：3 of 3・辺 1・閉路 0。**
⭐ **`impact.py FR-065` は 2 次で 14 か所を返した。**⚠️ **その中に `FR-065` の
「文書ごとに記憶する」を引いている行は無い** —— **引いているのは `S-99b` 1 つだけで、種に入っている。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⛔⛔ **どれでもない。⭐ これは実装できない要求を、実装できる形へ直す変更である。**

⚠️ **`GL-005`（単一の `.html` で完結し、特定の製品に依存しない）を後退させる面がある** ——
**「文書ごと」のほうが締まっているからである。**⭐ **それでも直す理由は、締まっている側が
**成り立たない**ことである**（本 CR の 1 節）。⛔ **成り立たない要求は、締まっていない。**

### ② レビュー観点のどの条項を当て、何が出たか

**`R1.2`**（曖昧・非検証な表現を排除。各要求が検証可能であること）—— ⭐⭐ **当てて出たのは、`FR-065` が
「文書ごとに記憶すること（MUST）」と定めながら、文書を一意に指す手立てを
仕様のどこも持っていないことである。**
**`AT-1`（`Project.id`）は `null` を許し、主キーではないと印がついている。**

**`R3.4`**（境界と資源の扱い）—— ⭐ **当てて出たのは、残る 3 つの候補がどれも成り立たないことである:**

| 候補 | なぜ成り立たないか |
|---|---|
| ファイルのハンドルから導く | ⛔ **ファイルを直接開いた状態では持てない。**`CH-5` の配り方そのものと衝突する |
| 中身のハッシュ | ⛔ **編集するたびに鍵が変わる。**覚えた次の操作で鍵が消える |
| `Project.id` を主キーにする | ⚠️ **成り立つが、`MSPDI` の識別の裁定を開き直す。**費用が桁で違う |

**`R4.3`**（状態遷移の一貫性）—— ⭐⭐ **当てて出たのが本 CR でいちばん大事な点である** ——
**覚える範囲が広がるので、`FR-065` のもう 1 つの MUST（有効であるあいだ画面に示すこと）が
いままでより重くなる。**⛔ **示す側は動かさない。⭐ 動かさないことが、この緩和を支えている。**

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

1. ⭐ **`FR-065` の RATIONALE の中の MUST を書き換えた。**
   ⚠️ **`RATIONALE` に規則を置くのは、この仕様書の流儀である**（規則 02 の 4、利用者の裁定 2026-09-05）。
   ⛔ **だから段を移さず、その場で書き換えた。**
2. ⭐ **「別の文書を開いたときにも効いていてはならない」を、消さずに裏返した。**
   ⛔ **消すと、なぜ広いのかが失われる。**⇒ **いつ・なぜ変えたかを書き残す。**
3. ⭐ **鍵の名前は決めなかった。**⛔ **`localStorage` の鍵の綴りは実装の話であり、
   仕様が持つのは「オリジンごとに 1 つ」までである。**

---

## 1. ⭐ なぜ「文書ごと」が成り立たないか（**実測 2026-09-05**）

```
S-99b        「文書の識別子と対にして環境側へ置く」と定める
その識別子    仕様のどこにも定義が無い
AT-1          Project.id は null を許し、主キーではないと印がついている
コード        frame-loop.ts:2276-2286 が自分でそう書いている。鍵は書かれないままである
```

⛔⛔ **要求が 1 巡以上「実装されていない」のではなく、「実装できない」状態で置かれていた。**

---

## 2. ⭐ 編集計画（**オブジェクトごとに 1 回**）

```
1. FR-065（01-04-requirements.md）の RATIONALE
     「有効化は文書ごとに記憶すること（MUST）」を
     「有効化はブラウザ（オリジン）ごとに記憶すること（MUST）」へ。
     ⭐ 代償を書く —— 1 つの文書で開いたことが、同じブラウザの別の文書にも効く
     ⭐ なぜ広いのかを書く —— 文書を一意に指す手立てが無い
     ⭐ 支えを書く —— だから「有効であるあいだ画面に示す」がいっそう効く

2. S-99b（_assets/tbl-settings.md）
     「文書の識別子と対にして」を「オリジンごとに 1 つ」へ
     ⚠️ ⛔ tbl-settings.md は生成物である。原稿は docs/spec/_source/settings.json
     ⇒ 原稿を直して npm run gen
```

⛔ **`AT-1` は動かさない。**⭐ **`Project.id` は `null` を許したままでよくなる**（本 CR がその依存を外す）。

---

## 3. ⭐ 数の予測（**規則 02 の 2.**）

| | 予測 |
|---|--:|
| `tables` | **0** |
| `figures` | **0** |
| `rows` | **1**（`S-99b`。⛔ 原稿を直して再生成する）|
| `uids` | **1**（`FR-065`）|
| 新しい設定値 | **0** |
| 再生成が要るファイル | **1**（`_assets/tbl-settings.md`）|

---

## 4. ⛔ 覆したとき落ちる試験

⛔ **いま 1 本も無い。**⭐ **仕様だけを読む体に書かせること:**

```
① 有効にしてから、別の文書を開いても有効のままであること（⭐ 裁定の中身そのもの）
② 有効であるあいだ、そのことが画面に出ていること（⛔ この緩和を支える側）
③ 無効にすると、同じブラウザの全部で無効になること
```

⚠️ **`localStorage` は private window では空で返り、文脈によっては読むだけで例外を投げる。**
⛔ **読み書きを try/catch で包み、値が無いときも正しく描くこと。**
