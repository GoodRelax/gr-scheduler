# CR-310 — 部品の境を越えた名前

⛔ **CR-307 の配線が名前を 1 つ部品の外へ出した。**表 T-064 は自らを「公開する名前の全数」と
名乗っているので、**載せないと表が嘘になる。**

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **どれも進めない。**⛔ **本 CR は、既に入った変更が仕様の全数表と食い違ったのを直すだけである。**
`GL-005`（改良に耐える設計）の下働きにあたる。

### ② レビュー観点のどの条項を当て、何が出たか

`R2.9`（層をまたぐ名前は仕様の表が持つ）。⇒ **越えることは正しく、載せていないことが誤りである。**

### ③ 利用者に**問わずに決めた**こと（**覆してよい**）

⭐ **「借りの表へ入れる」ではなく「全数表へ載せる」を選んだ。**
⛔ **これは借りではない** —— `.claude/skills/spec-graph-check/crossing-names-baseline.txt` は
**まだ載せられない debt** を置く場所であり、本件は**正しく公開された名前**である。

---

## 1. ⛔ 測った

検査 26b が出した逐語:

```
PROBLEM  InputCommandTranslator.`SpentEntranceSituation` crosses out of its folder
         -- src/framework/single-html-shell/frame-loop.ts imports it -- and table
         T-064 does not name it, though the table calls itself the full count.
```

### ⭐ なぜ越えるのが正しいか

`FR-029` は「**押されたときに限り、行えない理由を通知すること（MUST）。運ぶ理由は、
押された入口の場面に当たる 表 T-233 の行とすること（MUST）**」と定める。

| 層 | 何を知っているか |
|---|---|
| `InputCommandTranslator` | **押された入口が何を持たないか**（配下に開ける行が無い、選択が空、等） |
| 殻（`SingleHtmlShell`）| **通知の語彙**（`NoticeReason` ＝ 表 T-233 の行） |

⛔ **訳出の側に 表 T-233 の行 ID を持たせると、通知の語彙が 2 か所に住む。**
⭐ **場面だけを名乗らせ、写しは殻が持つ**のが層の向きに合う。⇒ **型が境を越える。**

---

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `05-07-design.md` 表 T-064 の `PI-18` | 公開メンバの並びの末尾に `SpentEntranceSituation` を足す。⛔ **行を増やさない** —— 既存のセルへの追記である |
| 2 | `A-appendix.md` 変更履歴 | 版 1 行（**1.64**。max を数え直して付けた）|

⛔ **触らないもの**: 表 T-062 / 表 T-063 / 表 T-075（コンポーネントとユニットの全数は動かない）、
表 T-065（層をまたぐ 8 本。本件は層をまたがない）、表 T-233、`FR-029`。

---

## 3. 数の予測（**実測で確かめた**）

`tables` 135 ／ `figures` 11 ／ `rows` 1739 ／ `uids` 150 ／ アイコン 85 ／ 語 387 —— **すべて不変。**
⭐ **既存の `PI-18` のセルへの追記だけ**であり、行も表も増えない。

---

## 4. 落ちる／緑になる試験

⭐ **試験は 1 件も動かない。**⛔ **動いたのは検査 26b である**（`PROBLEM` → `OK`）。

---

## 5. ⛔ 実装への影響

**無し。**⭐ **実装は既に正しく、仕様の表が追いついていなかっただけである。**

---

## 6. ⛔ 決めずに残したもの

**無し。**
