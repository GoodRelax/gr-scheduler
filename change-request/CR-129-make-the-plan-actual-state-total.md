# CR-129 — 予実の状態の判別を全域にする

## 1. 変更概要

**表 T-019 は「書くときに置く値」の表なのに、「読むときの判別表」としても使われている。** 書く側は 5 通りでよいが、読む側は**あらゆる列の組み合わせを 5 状態のどれか 1 つに落とす全域関数**でなければならない。そうなっていないため、欠陥が 2 件出ている。

| # | 直すもの | 置き場 |
|---|---|---|
| ① | ⭐ **判別の手順を表 T-019a として新設する**（優先順 5 段。全域） | 1 表を新設 |
| ② | **`FR-010` の STATEMENT を、判別と設置に分ける** —— 「そこから外れる入力を受け付けない」をやめる | `FR-010` |
| ③ | **`FR-043` が着手のときに `resumeValid` を `true` にする** | `FR-043` |
| ④ | **`FR-044` が再開予定日を置くときに `resumeValid` を `true` にする** | `FR-044` |
| ⑤ | **表 T-021a の `PV-3` を明示する** —— `resume` を空にし `resumeValid` を `false` にする | `PV-3` |

⚠️ **本 CR は要求の本文を変える。** CR-127・CR-128 はどちらも要求を 1 文字も変えなかったが、**本件は要求そのものの欠陥なので、要求を直さないと閉じない。**

## 2. 変更の背景・目的

### 2-1. グラフで調べた —— **塊「予実と遅れ」の中に閉じている**

```bash
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py FR-010 T-019 T-021a FR-044
```

| 測ったもの | 結果 |
|---|---|
| 表 T-019 を指している要求 | **1 次 4 件**（`FR-006` / `FR-010` / `FR-044` / `FR-012`）／ **2 次 7 件**（`FR-011` `FR-029` `FR-016` `FR-009` `FR-047` `FR-054` `FR-090`） |
| 表 T-021a を指している要求 | **1 次 3 件**（`FR-011` / `FR-043` / `FR-013`）／ **2 次 9 件** |
| 属する塊 | **「予実と遅れ」** —— 表 T-019・T-021・T-021a・T-021b・T-022 |

⚠️ **CR-127・CR-128 より広い。** 塊の中の表が互いを指すので、**1 パスで編集する**（`SKILL.md` の「object を回せ」）。

### 2-2. 欠陥 D1 —— **`resumeValid` を `true` にする規則が 1 つも無い**

```bash
grep -rn "resumeValid" docs/spec/01-04-requirements.md docs/spec/_assets/*.md
```

**値を置く規則は 2 つだけで、どちらも `false` である** —— 表 T-021a の `PV-1`（未着手 → 完了）と `PV-2`（進行中 → 完了）。ところが表 T-019 は 2 つの状態に `true` を要求している（`PA-2` 進行中 ／ `PA-3` 中断・再開予定あり）。

| 起こる遷移 | 結果 | 表 T-019 のどの行か |
|---|---|---|
| 未着手 →（`FR-043` で掴む）→ 着手 | `actualStart` あり・`actualFinish` 空・`resume` 空・**`resumeValid` は未設定** | ⚠️ **どの行にも当たらない** |
| 中断・再開日未定 →（`FR-044` で再開日を置く）→ 再開予定あり | `resume` 日付・**`resumeValid` は `false` のまま** | ⚠️ **どの行にも当たらない** |

⭐ **`resumeValid` が判別に効いているのは 1 組だけである** —— `PA-2`（進行中）と `PA-4`（中断・再開日未定）は他の 5 列がすべて同一で、この真偽値だけで分かれる。`PA-3` は `resume` の有無で、`PA-5` は `actualFinish` の有無で既に一意に決まる。

### 2-3. 欠陥 D2 —— **途中で中断した完了タスクを取り込むと、どの状態にもならない**

**公式 XSD で意味を確かめた**（`docs/reference/mspdi/mspdi_pj12.xsd`。表 T-003 の `CN-7` に従う）。

| 要素 | 行 | XSD の記述 |
|---|--:|---|
| `Stop` | 1747 | `The date that the task was stopped.` |
| `Resume` | 1753 | `The date that the task resumed.` |
| `ResumeValid` | 1757 | ⭐ `Whether the task can be resumed.`（`xsd:boolean`・`minOccurs="0"`） |

⭐ **`ResumeValid` は「再開日が妥当か」ではなく「そのタスクが再開できるか」である。** 表 T-016 の `PR-8` は「再開可否」と訳しており、**訳は正しい。** 問題は表 T-019 がこれを判別列に使っている点にある。

**`Stop` / `Resume` はスプリット（1 回の中断区間）を表す。** 中断中のタスクなら `Resume` は未来（＝`FR-044` の「再開予定日」で正しい）、**完了したタスクなら過去である。**

#### 実在する形（このプロジェクト自身の MSPDI 資料にある）

```xml
<ActualFinish>2026-07-14T17:00:00</ActualFinish>
<PercentComplete>100</PercentComplete>
<Stop>2026-07-03T17:00:00</Stop>      <!-- 途中で 1 回止まった -->
<Resume>2026-07-08T09:00:00</Resume>  <!-- そこで再開した（過去） -->
<ResumeValid>1</ResumeValid>
```

（`previous-project-result/01-mspdi/mspdi-core-tree.md:250`。同書 §C-5 も「`Stop`/`Resume` は開始/終了ではなく中断/再開」と記録している）

**表 T-019 の `PA-5`（完了）は `resume` 空・`resumeValid` `false`・`Stop` 書かない を要求する。上のタスクは 5 行のどれにも当たらない。** そして `FR-010` が **「そこから外れる入力を受け付けないこと」** と定めているので、`FR-023` の検証がこのファイルを弾く。

### 2-4. ⭐ 同型の欠陥を、この仕様書は一度踏んで手当てしている

**`FR-012` が同じ形の罠に落ち、例外を置いて閉じた。**

> ⚠️ **表 T-033 の `EX-5` が定める「中身のない行」は、この禁止の対象外とすること（MUST）。**（中略）**中身のない行が 1 つあるだけで取込が丸ごと失敗し、`FR-021` の往復無損失も同時に落ちる。**

**D2 は同じ失敗が別の要求で再発したものである。** 「定めた集合から外れる入力を拒む」と書くと、**実在するが想定していなかった形が来たときにファイルごと落ちる。**

### 2-5. 根は 1 つ —— 表 T-019 が 2 つの役を兼ねている

| 役 | 要るもの | 表 T-019 は |
|---|---|---|
| **書く側**（`GRS` が状態を変えるとき置く値） | 5 通りで足りる | ✅ 果たしている |
| **読む側**（列の組み合わせから状態を決める） | **全域関数**。あらゆる組み合わせが 1 つに落ちること | ❌ 果たしていない |

**判別を別の表に分ける。** ⭐ **第 2 段（`actualFinish` があれば完了）は新しい規則ではない** —— `FR-012` が既に散文で「**完了したかどうかは `actualFinish` の有無が表すのであって、完了率が 100 かどうかではない**（表 T-019）」と書いている。**表に畳むだけである。**

## 3. 変更箇所

| # | ファイル | 何を | |
|---|---|---|--:|
| 1 | `docs/spec/01-04-requirements.md` | 表 T-019a を新設（5 行）／ `FR-010` の STATEMENT ／ `FR-043` の STATEMENT ／ `FR-044` の STATEMENT ／ 表 T-021a の `PV-3` ／ 表 T-019 の前書き | +1 表 |
| 2 | `docs/spec/A-appendix.md` | 版 0.24 の行 1 本 | +1 |

⛔ **触らないもの** —— 表 T-019 の 5 行の値、表 T-021、表 T-021b、表 T-022、`FR-012`、`FR-013`、`FR-011`、`model.json`、`erd.json`、生成物一式。

⚠️ **`T-019a` の席は空いている**（機械で確認）。`T-020` / `T-020a` / `T-021` / `T-021a` / `T-021b` は使用済みである。

## 4. 変更前の仕様

- `FR-010` の STATEMENT —— 「`GRS` は、`Task` の予実の状態を**表 T-019 の 5 つ**に保ち、**そこから外れる入力を受け付けないこと。**」
- `FR-043` の STATEMENT —— 「⋯ **掴まれたときに実績開始日を置き**、押されたときに状態を巡らせること（MUST）。」
- `FR-044` の STATEMENT —— 「⋯ 作成者が再開予定日を画面上で置き、置いた後に動かせるようにすること。⋯」
- 表 T-021a の `PV-3` の「置く値」欄 —— 「`actualFinish` を空にする。`resume` は置かない（再開日未定）」
- **判別の手順を持つ表は無い**

## 5. 変更後の仕様

### 5-1. 表 T-019a を新設する（**表 T-019 の直後**）

**表 T-019a — 予実の状態の判別**

| 行 ID | 順 | 条件 | 状態 |
| --- | :-: | --- | --- |
| PS-1 | 1 | `actualStart` が空 | 未着手 |
| PS-2 | 2 | `actualFinish` がある | 完了 |
| PS-3 | 3 | `resumeValid` が `false` | 中断・再開日未定 |
| PS-4 | 4 | `resume` に日付がある | 中断・再開予定あり |
| PS-5 | 5 | 上のどれにも当たらない | 進行中 |

**上から順に当て、最初に当たった行の状態とすること（MUST）。** ⭐ **本表は全域である** —— `PS-5` があるので、どの組み合わせも必ず 1 つの状態に落ちる。**「どの状態でもない `Task`」は存在しない。**

⚠️ **`PS-2` が `Stop` / `Resume` / `ResumeValid` より先に来ることが要である** —— 交換相手は**途中で中断して完了したタスク**にこの 3 つを残す（`Resume` は過去の日付である）。後に置くと、実在するファイルが取り込めなくなる。

### 5-2. `FR-010` の STATEMENT

**STATEMENT**: `GRS` は、`Task` の予実の状態を**表 T-019a の手順**で判別し、状態を変えるときは**表 T-019 の値**を置くこと。

⚠️ **「表 T-019 の 5 つから外れる入力を受け付けない」という形にしてはならない（MUST NOT）** —— 表 T-019 は `GRS` が置く値の表であって、交換相手が書きうる組み合わせの全数ではない。**`FR-012` が「中身のない行」で同じ罠に落ちている。**

### 5-3. `FR-043` の STATEMENT（**下線部を足す**）

「⋯ 掴まれたときに**実績開始日と `resumeValid`（`true`）を**置き、押されたときに状態を巡らせること（MUST）。」

### 5-4. `FR-044` の STATEMENT（**1 文を足す**）

「**再開予定日を置いたとき、`resumeValid` を `true` にすること（MUST）** —— 置かないと、表 T-019a の `PS-3` が先に当たり、日付を置いても状態が変わらない。」

### 5-5. 表 T-021a の `PV-3` の「置く値」欄

| 変更前 | 変更後 |
|---|---|
| `actualFinish` を空にする。`resume` は置かない（再開日未定） | **`actualFinish` と `resume` を空にし、`resumeValid` を `false` にする（再開日未定）** |

⚠️ **明示が要る理由** —— 取り込んだ完了タスクは `resume`（過去の日付）と `resumeValid` = `true` を持ちうる（§2-3）。**空にしないと、完了を解いた瞬間に「中断・再開予定あり（過去の日付）」になる。**

### 5-6. 表 T-019 の前書き（**1 文を足す**）

**本表は、`GRS` がその状態にするときに置く値である。読むときの判別は表 T-019a が持つ。**

## 6. 影響反映と影響分析結果

### 6-1. 席番号

**`T-019a` を新設する。既存の表番号は 1 つも動かさない。** 行 ID は `PS-1` 〜 `PS-5`（`PA-` / `PV-` / `PM-` と衝突しない接頭辞であることを機械で確認する）。

### 6-2. 5 状態がすべて到達可能になることの検算

| 遷移 | 置く値 | 表 T-019a の判定 |
|---|---|---|
| 新規 `Task` | すべて空 | `PS-1` → **未着手** ✅ |
| 未着手 →（`FR-043` 掴む） | `actualStart` ＋ `resumeValid` = `true` | `PS-5` → **進行中** ✅ |
| 進行中 →（`PV-2` 押す） | `actualFinish` ＋ `resumeValid` = `false` | `PS-2` → **完了** ✅ |
| 完了 →（`PV-3` 押す） | `actualFinish` / `resume` 空 ＋ `resumeValid` = `false` | `PS-3` → **中断・再開日未定** ✅ |
| 中断・再開日未定 →（`FR-044` 日付を置く） | `resume` ＋ `resumeValid` = `true` | `PS-4` → **中断・再開予定あり** ✅ |
| 中断 →（`PV-4` 押す） | `actualStart` / `actualDuration` / `actualFinish` / `resume` を空 | `PS-1` → **未着手** ✅ |
| 未着手 →（`PV-1` 押す） | `actualStart` `actualFinish` `actualDuration` ＋ `resumeValid` = `false` | `PS-2` → **完了** ✅ |
| ⭐ 取り込んだ「中断して完了した」タスク | `actualFinish` ＋ `Stop` ＋ `resume`（過去）＋ `resumeValid` = `true` | `PS-2` → **完了** ✅ **D2 が閉じる** |

**5 状態すべてに到達でき、どの組み合わせも 1 つに落ちる。**

### 6-3. 表 T-021 の記号との整合

**記号の対応は 1 つも変わらない。** `PM-1`（進行中）／ `PM-1a`（未着手）／ `PM-2`（完了）／ `PM-3`（中断 2 種）／ `PM-4`（遅れ・導出）。判別の手順が変わっただけで、状態の集合は同じ 5 つである。

### 6-4. 往復（`FR-021`）への影響

| 場面 | 扱い |
|---|---|
| 取り込んで人が触っていないタスク | **原値をそのまま書き戻す**（`resume` / `resumeValid` は出自 `Own`。`Stop` は表 T-019 の注記が Carry として守る）。**本 CR で変わらない** |
| 人が状態を変えたタスク | 表 T-019 / T-021a の値を置く。**本 CR で変わらない** |
| ⭐ 中断して完了したタスクの取り込み | **変更前は `FR-023` が弾いていた。変更後は取り込めて、そのまま書き戻せる** |

### 6-5. 機械検査（**予測**）

| 検査 | 予測 |
|---|---|
| `tables` | **100 → 101**（表 T-019a を新設） |
| `figures` | **9 のまま** |
| `rows` | **1165 → 1170**（`PS-1` 〜 `PS-5` の 5 行） |
| `uids` | **140 のまま**（要求 `UID` を新設しない） |
| 検査 12（値・名前の表に規則） | **0 のまま。**⚠️ 表 T-019a は**判別の表**であり値の表ではないが、「上から順に当てる（MUST）」を**表の外**に置く |
| 助言 13 / 14 | **4 / 18 のまま** |
| 検査 16 | 生成物に触らないので緑のまま |
| ゲート 14 本 | すべて緑を維持 |

## 7. 最終判断日時

**2026-08-15。** 利用者が「まず調べろ」と指示し、公式 XSD とプロジェクト自身の MSPDI 資料を読んだうえで決定した。

| 決定 | 結論 | 却下した案と理由 |
|---|---|---|
| D1 / D2 の直し方 | ⭐ **判別を優先順の手順にする** | **取り込み時に正規化**（完了タスクの `Stop` / `Resume` を捨てることになり `FR-021` の往復が壊れる）／ **D1 だけ直し D2 は別 CR**（遷移図に「どの行にも当たらない組み合わせ」が残る） |

## 8. 反映記録（2026-08-15）

### 8-1. §6-5 の予測と実測 —— **外した予測は 0 件**

| 検査 | 予測 | 実測 | |
|---|---|---|:-:|
| `tables` | 100 → 101 | **101** | ✅ |
| `figures` | 9 | **9** | ✅ |
| `rows` | 1165 → 1170 | **1170** | ✅ |
| `uids` | 140 | **140** | ✅ |
| 検査 12 | 0 | **0** | ✅ |
| 助言 13 / 14 | 4 / 18 | **4 / 18** | ✅ |
| 検査 16 | 緑 | **2 文書とも一致** | ✅ |
| ゲート 14 本 | すべて緑 | **すべて緑** | ✅ |

**`T-019a` は番号付きなので検査 8 が守っている**（無番号の表の注記は `FR-083` の既存 1 件のみで、行番号がずれただけである）。

### 8-2. ⚠️ 実物を見て、本 CR とは別の欠陥が 1 件見つかった

**`strictdoc export` の HTML に、新設した表 T-019a が出ていなかった。** 調べたところ **本 CR が原因ではなく、変更前から在る欠陥だった** —— **表 T-019 も、表 T-021a も、表 T-052 も出ていない。**

#### 相関を機械で当てた —— **例外 0 件**

| 表の位置 | 描画される |
|---|--:|
| 要求ノードの **`**Relations**:` より前** | **6 / 6** |
| 要求ノードの **`**Relations**:` より後** | ⛔ **0 / 40** |

**`**Relations**:` ブロックより後ろに置いた自由文を、StrictDoc は全ビュー（`.html` / `-TABLE` / `-TRACE` / `-DEEP-TRACE`）で黙って捨てる。**

```
HTML の実際の並び:
  … FR-010 の RATIONALE … ORIGIN: UC-005 手順 2 〜 4、拡張 3a
  → FR-011  4.1.29. 実績バーの両端を人が置いた日付に保つ
      ↑ この間にある 表 T-019・表 T-019a・注記がすべて消えている
```

| 測ったもの | 実測 |
|---|--:|
| 後置の自由文を持つ要求ノード | **33** |
| その自由文の実行 | **550** |
| そこに含まれる表 | **40** |

⚠️ **エラーは 1 つも出ない。**`strictdoc export` は終了コード 0 で完走し、16 検査も全緑である。**参照が解決することしか見ていないので、参照先が刊行物から消えていても分からない。**

⚠️ **本 CR の表 T-019a も、この 40 表の 1 つとして現在は刊行物に出ない。** 原稿（Markdown）は正しく、**直し方は機械的である** —— その表を同じ要求の `**Relations**:` の前へ動かす。**別 CR とする。**

### 8-3. 生の `**` が 11 箇所

同じ調査で、**太字が `<strong>` にならず生の `**` のまま出ている箇所が 11 あった**（要求 8 ／ 設計 1 ／ 付録 2）。CJK に隣接する `**bold**` が docutils で開かない既知の罠で、**これもエラーを出さない。** 本 CR が触った `FR-044` の `**再開日を未定のままにもできること。**` もその 1 つだが、**境界（`。**`）は編集前と同じであり、本 CR が作ったものではない。**

### 8-4. 残った作業

| # | 件 | どこで |
|---|---|---|
| 1 | ⭐ **40 表・550 行が刊行物から消えている**（`**Relations**:` より後ろの自由文）＋ 生の `**` 11 箇所 | **別 CR** |
| 2 | 一貫性の単位・状態遷移図・レイアウトの計算順序・描くときに求める値 | **Chapter 5.5**（未記入） |
| 3 | ADR-000 ／ キャッシュの ADR ／ 描かなかった図 ／ 4 つの代償 | **Chapter 5.6**（未記入） |
