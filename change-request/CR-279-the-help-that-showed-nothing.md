# CR-279 — 何も出さなかったヘルプ

> **閉じるもの**: ⛔ **利用者の指摘 1 件**（`D-86`。`D-14` と同じもの）。
> ⭐ **`FR-069`（ライセンス全文・著作権表示・帰属表示）も同じ面に居るので一緒に閉じた。**

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`GL-006`（マニュアルを読まずに使える）。**⛔ **表 T-040 の `EZ-3`「ヘルプ 1 画面で機能を説明する」は、
`GL-006` の合否を決める 5 つの手立ての 1 つであり、**その 1 つが丸ごと欠けていた。**

### ② レビュー観点のどの条項を当て、何が出たか

| 条項 | 当てた対象 | 結果 |
|---|---|---|
| **`R1.4`**（欠落が無い）| `HelpModal` | ⛔⛔ **宣言された 8 つの項のうち 4 つしか埋まっていなかった**（`open-modals.ts`）。`entries` ／ `licenceText` ／ `copyrightNotice` ／ `attributions` が空 |
| **`R1.4`** | 名簿の届き方 | ⛔ **`FR-036` が挙げる 6 つの表は、どれも `src/` へ生成されていなかった。**⭐ **`src/` の `STOP` が「ここに書けば規則 03 の 1. が禁じる写しになる」と正確に述べており、止めた判断は正しい** |
| **`R3.4`**（同じものを 2 か所に置かない）| 語 | ⛔ **6 つの表のうち 4 つ（`T-023a` / `T-023c` / `T-023d` / `T-036`）は辞書に節を持たなかった。**⇒ **語が無いので、書けば `FR-038` の MUST NOT を破るしかない** |
| **`R2.19`**（驚かせない）| 描く側 | ⛔ **既存の描画は各行の**行 ID**を刷っていた。**表 T-023b の結びが行 ID を画面に出すことを禁じており、ヘルプは 110 個それを出すことになる |

### ③ 利用者に問わずに決めたこと（**覆してよい**）

⭐ **利用者は「文言やレイアウトの細かい裁定は一旦推奨で実装しろ」と述べているので、以下は推奨値である。**

| | 決めたこと | 何に載せたか |
|---|---|---|
| 1 | ⭐ **図形を持つのは、表 T-109 がその行にちょうど 1 つの入口を置いている行だけ** | ⛔ **同表の `構え` の欄は `AR-2` に 4 つ、`AR-3` に 8 つの入口を与える。**⇒ **代表を選べば、`FR-029` が図形の正と定める 図 F-019 と 表 T-109 の外で選んだことになる。**⭐ **結果 110 行のうち 36 行が図形を持つ**（パレット 33 ＋ `AR-4` / `AR-5` / `AR-6`）|
| 2 | ⭐ **キーは辞書に入れず、表 T-036 の `割当` 欄から運ぶ** | ⭐ **`Ctrl+S` はどの言語でも同じ** —— 設定値の単位と同じ扱いである。⚠️ **区切り（`/` と `＋`）は表のものをそのまま運ぶ** |
| 3 | ⭐ **キーの欄は、キーを持たない行でも場所を空ける** | ⛔ **ダッシュを書かない** —— 「割当を意図して持たない」と読めてしまい、それは `SK-1` が**語で**述べていることである |
| 4 | ⭐ **行 ID は画面に出さず、`data-row` に残す** | ⛔ **表 T-023b の結びが行 ID を画面から締め出している。**⭐ **読み戻しのための記述は画面ではない** |
| 5 | ⭐ **段組みは `column-count`、大きさは `vw` / `vh` の実寸** | ⭐ **`FR-036` が「基準環境では 1 画面、それより低い画面ではスクロールを許す」と既に定めている。**実寸 ＋ `overflow:auto` はその 2 文をそのまま書いたものである。⚠️ **当初は上限にしたが、§6 のとおり実物で 0.885 にしかならず「その割合で開く」に足りなかった** |
| 6 | ⭐ **帰属表示は空** | ⛔ **`package.json` に `dependencies` が 1 つも無い**（実測）。⭐ **生成器が毎回それを確かめ、実行時依存が足された瞬間に落ちる** —— **空の一覧が嘘になる瞬間に気づく** |

---

## 1. 測った

| 測ったこと | 実測 |
|---|---|
| ヘルプが出すもの | **110 件** —— `T-023a` 6 ／ `T-023b` 6 ／ `T-023c` 10 ／ `T-023d` 19 ／ `T-023` 14 ／ `T-036` 22 ／ パレット 33 |
| 辞書に節が在った表 | **2 つだけ**（`T-023` ＝ `assignments`、`T-023b` ＝ `arms`。どちらも別の面のために立てたもの）|
| ⇒ 語を書いた行 | **57 行 × 2 言語 = 114 語**（語の総数 273 → **330**）|
| キーを持つ行 | **20 行**（`SK-1` / `SK-1a` は `—` ＝ 割当が無いことを述べる行）|
| 図形を持つ行 | **36 行** |
| `LICENSE` | **201 行**（Apache-2.0）|
| 実行時依存 | ⭐ **0 件**（`dependencies` そのものが無い。devDependencies 6 件は `dist` に入らない）|
| `induced.py`（`FR-036` `FR-038` `FR-069` ＋ 6 表 ＋ `T-109`）| ⭐ **閉路 0** |

## 2. 編集計画

| # | 対象 | 何を書くか |
|---|---|---|
| 1 | `FR-036` | ⭐ **割合と段（`S-201` / `S-202`）／ 説明・キー・図形の順と図形の位置 ／ 図形を持つ行の条件 ／ 語は辞書のものであること** |
| 2 | 表 T-206 | ⭐ **`S-201`（0.95）と `S-202`（3）** |
| 3 | `_source/display-words.json` | ⭐ **4 節 57 行**（`pressOrder` / `selecting` / `grabAreas` / `shortcuts`）|
| 4 | `tools/generate_help_roster.py` | ⭐ **6 表 ＋ パレットを 110 件へ。**キーと図形も運ぶ |
| 5 | `tools/generate_licence.py` | ⭐ **`LICENSE` と `NOTICE` から。**帰属は `package.json` に問う |
| 6 | `open-modals.ts` | ⭐ **8 項すべてを埋める** |
| 7 | `dom-screen-surface.ts` | ⭐ **3 段・95%・説明の右に図形** |

## 3. 数の予測

| | 前 | 後 | 差 |
|---|---|---|---|
| `tables` / `figures` / `uids` | 135 / 11 / 149 | **同じ** | 0 |
| `rows` | 1718 | **1720** | **+2**（`S-201` `S-202`）|
| 語（検査 27）| 273 | **330** | **+57** |
| 生成器 | 15 | **17** | +2 |

## 4. 実測（編集後）

| | 予測 | 実測 | |
|---|---|---|---|
| `tables` / `figures` / `uids` | 135 / 11 / 149 | **135 / 11 / 149** | ✅ |
| `rows` | 1720 | **1720** | ✅ |
| 語 | 330 | **330 word(s), 330 written** | ✅ |
| 生成器 | 17 | **17 OK** | ✅ |
| ヘルプの項目 | 110 | **110 entries** | ✅ |
| 検査 22 本 | 全緑 | **ALL GREEN** | ✅ |

### ⚠️ 予測から外れて、直しが 4 つ要った

| 何が落ちたか | なぜ | どう直したか |
|---|---|---|
| **`NFR-004` / `CN-6` の e2e** | ⛔⛔ **Apache ライセンスの本文に URL が 2 つ在る。**`FR-069` が全文を成果物へ入れよと定める以上、**消せば governs しているライセンスでなくなる** | ⭐ **`NON_DEREFERENCED_URI_IDENTIFIERS` へ理由付きで足した** —— **同一覧が自ら定める条件（「参照されないことが示せるものだけ」）を満たす。**⛔ **属性の走査と `file://` の「要求 0 件」は別に効いたままである** |
| **検査 11 が「new 1」** | ⛔ **`S-201` の備考に `FR-036` と同じ理由を書いた** —— 規則は要求が持ち、値の行は指すだけである | ⭐ **備考を「理由は `FR-036` が持つ」へ縮めた** |
| **契約試験が節を鍵にできない／並びが違う** | ⛔ **同ファイルの `KEY_FIELD` は「読める節の全数」であり、しかも**生成物の刷る順**を記録している** | ⭐ **4 節と「置き場」57 件を登記し、生成器が刷る位置（`notices` の後）に置いた** |
| **契約試験が 5 秒で時間切れ** | ⛔ **語 × 場面の総当たりであり、語が 273 → 330 になった** | ⭐ **20 秒へ。**⛔ **総当たりを間引かない** —— 「**書かれた語はすべて画面に届く**」が主張であり、抜き取りでは届かない 1 語を見逃す |

## 5. 標準規則への答え

- **①（目的に沿う）**: ⭐ `GL-006`。**表 T-040 の `EZ-3` が欠けていた。**
- **②（影響をグラフで測ってから書く）**: ⭐ `induced.py` は閉路 0。順に書いた。
- **⑧（実測を書く）**: ⭐ **§1 と §4 が実測である。**⛔ **`STOP` が正確だったことも記録した。**

## 6. ⭐⭐ 実物で確かめた —— **そこで 2 つ直した**

⛔ **「収まる」は試験に測れない。**⇒ **`dist/index.html` を参照ブラウザで開いて実測した（2026-08-29）。**

| 測ったこと | 実測 |
|---|---|
| 1920 × 1080 | ⭐ **110 項目・3 段・幅 0.95 × 高さ 0.95・スクロール無し** |
| キーを持つ行 ／ 図形を持つ行 | **20 ／ 36** |
| 画面に出た行 ID | ⭐ **0 件** |
| 1366 × 768 | ⭐ **折りたたむ**（`FR-036` が MC-6 未満で許す）|
| 英語 | ⭐ **110 項目・スクロール無し**（`保存する` → `Save`、`Ctrl+S`）|
| ライセンス | ⭐ **開くと全文が出る。**著作権行が見出しである |

### ⛔⛔ 試験が 2 つとも捕まえられなかった

| 何が起きていたか | 実測 | 直し |
|---|---|---|
| **スクロールしていた** —— `FR-036` の MUST が破れていた | **一覧 998px ／ ライセンス本文 4824px**（同じ箱）| ⭐ **`FR-069` の 3 つを著作権行の下へ折りたたみ、行送りを 1.6 → 1.35 に。**⇒ **内容 954px** |
| **幅が 0.92 しか無かった** | `STOPPING_BOX` の `max-width:92%` が `width:95vw` に勝っていた | ⭐ **上書き。**⚠️ **高さも当初は上限だったので 0.885 止まりで、「その割合で開く」に足りなかった** ⇒ **実寸へ** |

⭐ **どちらも「試験は緑・実物は違う」の例である**（規則 04）。

## 7. ⛔ 途中で見つけた、本変更要求と無関係な不具合

⛔⛔ **ページを開いてから最初のキー入力が、どのキーでも 1 回まるごと捨てられる**（台帳 D-104）。
⭐ **実測**: `F1` だけ押すとヘルプは出ない ／ `x` を押してから `F1` なら出る ／ `F1` を 2 回でも出る。
⛔ **`Ctrl` ＋ `Shift` ＋ `E` も同じ**なので、**表 T-036 の全行に効く。**
⚠️ **本変更要求の実装とは無関係で、前から在る。**⛔ **試験は `receiveInput` を直に呼ぶので、この経路を通らない。**
