# CR-551 — 2026-09-23 の使い勝手の裁定 16 件を仕様へ下ろす

> 起草の状態: 着地（2026-09-24）—— 仕様は `184dfbdc`（波 0）、コードは `b665c0e5` 〜 `399dd130`（波 1a 〜 2.5 と、つまみの直し）。11 節の問い 1 ・ 4 は利用者が推奨どおりと答えた（`JDG-400` ・ `JDG-401`、調整役が中継）。問い 2 は `JDG-398` が答えた。問い 3（英語の語）は「推奨どおり」で「all rows」に揃えた（`JDG-402`、J-12 ・ J-13）。
> ⭐ 起草の後に下りた裁定 3 件を畳み込んだ: `JDG-396`（ツールチップは 1 行、窓より広いときだけ折り返す。CSS と試験で保証する —— E-13 ・ `S-339`）、`JDG-397`（宿主の色の入力は [任意] を押したときだけ出す —— E-29）、`JDG-398`（読み出しの字は今の 50%、下限 10px —— E-24 ・ `S-340`）。番号 `S-339` ・ `S-340` は前に立つ者の帯（`S-339` 〜 `S-349`）から採った。
> 読んだ木: `73c2bc90`（`refactor`）。仕様の原稿は `98208692` から表 T-064 のほかは動いていない。行番号・数・参照は、すべてこの木で測った（測り方は 13 節）。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示 16 件（`JDG-367` 〜 `JDG-383`。逐語は `docs/development-records/rulings.md` の同日の節と、下の 0.1 節）。
> ⛔ **覆すもの**: `ND-5` の「年（4 桁）」、`JDG-30` のうちモノクロの入口の置き場（`Command Palette`）、`FR-048` と `DC-4` の「3 種は独立に出し分ける」、`CV-9` の並べ方（試作の `selHue` に倣う 1 列）と `JDG-326` ② (1) の欄の示し方（「ライト ■ / ダーク ■（ライトと同じ）」）、`FR-013` の「色は実績バーの色を継ぐ」（遅れだけ）、`S-63` の既定 `true`、`PR-13` の名称ラベルの位置の指定。
> ⭐ **形の方針**: 利用者は、使い勝手の試しのあとで裁定がまた動くと述べている。⇒ 値はすべて設定値の行（`_source/settings.json`）に置き、要求はその行を指す。裁定を戻すときは、多くが 1 行の書き換えで済む（戻し方は 1 節の最右列）。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が体へ渡した写しのまま写した。`…` と、項目 12 の括弧の中（`magenta-leaning, ruling: マゼンタ寄り`）は、写しの側の省略と書き添えであり、利用者の字ではない。

| 項目 | 逐語 |
|---|---|
| 1 | 「パレットにある 6/3 を押して表示する日付は m/d だけにしろ yyyy は無用。また、フォントはタスク名のフォントの85%にしろ」 |
| 2 | 「進捗マーカーの ! が見にくい…線を太くして間を少し開けろ。進捗マーカーの ! は黄色にぬれ。」 |
| 3 | 「矢羽根 >===> タスクのFade Inを操作するとFade Out側の形状まで変わる…独立して変更可能とせよ。」 |
| 4 | 「進捗マーカーはデフォルトを非表示にしろ」 ／ 続けて「遅延診断を実行したときは、マーカーを自動で表示しろ。ただし、ユーザーがマーカーを非表示にできるようにしろ。」 |
| 5 | 「名称アンカー（nameAnchor）と名称の揃え（nameAlign）の機能はドロップしろ。」 |
| 6 | 「モノクロモードのアイコンはパレットからヘッダーのダークモードの左に動かせ」 |
| 7 | 「パレットから担当者名簿を出したとき、名簿を閉じる [x] は右上に配置しろ」 |
| 8 | 「ToolTipsは1行に表示しろ。Helpのように画面の右側にある場合は、右寄せで表示」 |
| 9 | 「横スクロールバーは画面一番下に表示してその領域に日程が入らないようにしろ」 ／ 「縦スクロールバーの左で行の背景…見切れている。スクロールバーのぎりぎりまで表示されろ。ただし、スクロールバー内には表示させるな。」 |
| 10 | 「Fixしたとき、画面左右にマージンが無い。各2.5%のマージン…タスク名が画面右端で見切れることが無いように…担当/進捗/マーカー表示によって表示すべき範囲が変わる。」 |
| 11 | 旗の基準線:「表示している間、パレットのアイコンに色付け／朱色で太さ3px／有効にしたら基準線が中央になるように倍率はそのまま横方向にスクロール／マウスカーソルを当てたら ←&#124;&#124;→ (パネルサイズを変更するアイコンと同じ)」 |
| 12 | カーソル3種:「どれを選択しているのか、パレットアイコンに色付け／カーソルの色は（magenta-leaning, ruling: マゼンタ寄り）、太さ1px／&#124;⇔&#124;を選択した後、他のカーソルに切り替えたとき、2本目の線が残るので消せ／カーソルに追従する日付情報のフォントは今の50%」 |
| 13 | 「タスクグループ操作子について、最上位階層に[x]がない…[x]を押下したら配下のタスクグループを削除…全タスクグループの一括削除が可能」 |
| 14 | 「プロパティーパネルで日付系の入力で編集中に Del または BS を押下すると…すべて削除/クリア」 |
| 15 | 「色の設定はプロパティーの一番下にしろ」 |
| 16 | 「規定の10色を □□□□□ / □□□□□ と並べ、その後に [Custom/任意] [Transparent/透明]」 ／ 透明と未定義の示し方は、前に立つ者の提案どおり（逐語は写しに無い） |

前に立つ者が渡した裁定（利用者の言葉ではない）: 項目 1 は `ND-5` を覆し、暦年をまたぐ文書だけ `yy/m/d` とする。項目 2 は遅れを黄とし、ボトルネックの経路（桃色。ショッキングピンクではない）とボトルネック（赤）は後の遅延診断の変更要求に残す。項目 12 はマゼンタ寄り。項目 16 は提案どおり（透明は語と市松、未定義は空の値と破線、`Light: #AAAAAA / Dark: #050505` の形）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4` ／ `GL-006`**（説明を読まずに操作できる）—— 16 件のうち 12 件は、見て分かる・押して分かることの直しである（効いている入口を塗る 11・12、閉じる入口の場所 7、1 行のツールチップ 8、欄の示し方 14・16、見切れ 9・10）。
⭐ **`CH-2` ／ `GL-002`**（全体を 1 枚で俯瞰する）—— 項目 10（全体表示の余白と見切れ）と項目 4（既定でマーカーを出さない）。
⭐ **`CH-5` ／ `GL-005`** —— 項目 5 は使われない列を文書の形から落とす。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正）** —— ① `FR-048` の「1 つの入口が 2 つを同時に消してはならない」と `DC-4` の「3 種は独立」が項目 12 の排他と両立しない → `DC-9` を 1 行立て、両方をそこへ向けた（E-21 〜 E-23）。② `FR-013` の「色は実績バーの色を継ぐ」が項目 2 の黄と両立しない → 遅れだけの例外として書いた（E-03）。③ 表 T-237 の後の「`S-183` が 5 行に立つ」が `EN-7` を足すと偽になる → 6 に直した（E-20）。④ `S-178` の備考「基準日線と 2 連カーソルは太さの行を持たない」が偽になる（`S-194` は既に在り、`S-333` を足す）→ 書き直した（J-05）。⑤ 3 つの要求（`FR-003` ・ `FR-039` ・ `FR-041`）が同じ置き場の約束を互いに引いている（閉路 4）→ 1 つの計画で 3 か所を書いた（E-09 〜 E-11）。
- **`R1.4`（異常系・境界値）** —— 項目 5: 保存済みの文書は `nameAnchor` / `nameAlign` を必ず持つ（スキーマが `required` にしている）。列を落とすだけでは、今までに保存した文書がすべて開けなくなる → 表 T-297 を立てて「読むときに捨てる」を定めた（E-30）。項目 14: 空で確定したときの `null` を許さない列（`start` / `finish`）を書いた（E-28）。項目 13: 全行を消すと行が 0 になる → 既存の「1 行を作る」段が当たることを `CD-6` に書いた。項目 10: 1 つ浅い段へ下がる分岐（E-15）。
- **`R1.2`（検証できる表現）** —— 「少し開けろ」「小さく」「マゼンタ寄り」「朱色」を、すべて設定値の行の値にした（`S-325` 〜 `S-338`、`S-163` ・ `S-195`）。
- **`R2.1`（命名）** —— 状態機械の効果の名を中身に合わせて変えた（`writePlaceDualCursor` → `writePlaceDualCursorClearingGuide`、J-15）。新しい接頭辞 `RK`（Retired Key）。
- **`R4.4`（状態機械）** —— 項目 12 の排他は `dualCursorModeStateMachine` に出来事 `guideCursorEntryPressed` を 1 つ足して表す（J-15）。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる。戻し方は 1 節）。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 項目 1 の年は西暦の下 2 桁、1 桁は 0 を詰める（`05/3/1`） | 前に立つ者の裁定「yy/m/d」 | 100 年をまたぐ文書は読み分けられない |
| 決定 2 | 項目 1 の 85% は、予定日の文字とその前の半角空白に掛ける。「入る」の判定と `OC-1` の幅は 2 つの大きさで測った和 | 判定を字の大きさで分けないと `FR-002` の既存の MUST（入ると判じたラベルが右端を越えない）が破れる | ラベルの幅の測りが 2 回になる |
| 決定 3 | 項目 2 の「! は黄色にぬれ」は、遅れのマーカーの**地**を黄、記号 `!` を暗い色とする読み（11 節の問い 1） | 白い地に黄の `!` は明るいテーマで読めない（明度差がほとんど無い）。警告の標識と同じ形 | 読みが違えば `S-326` と `S-327` の値を入れ替えるだけ |
| 決定 4 | 項目 2 の「線を太く・間を開けろ」は、`d827da8a` の 4 つの定数（縦棒の太さ 1.7 × `S-24`、下端 0.15、点の中心 0.8、点の半径 0.14）をそのまま 4 行にした | `FR-094` が宣言していない記号の寸法を禁じる。値はコードが既に描いて利用者に見せた値 | 値は 🔎（測って決めていない） |
| 決定 5 | 遅れの色の家族のうち、ボトルネックの経路（桃色）とボトルネック（赤）は行を**立てない**。10 節に申し送る | 読む者の無い行は検査が拾い、意味の定まらない値を先に置くことになる | 遅延診断の変更要求が 2 行を足す |
| 決定 6 | 項目 3 で、フェード長が 0 の端は、フェードの無い矢羽根の深さのまま | いまは片方でもフェードがあると両端を置き換える。0 の端をその深さに戻さないと、片方のフェードがもう一方の形を変え続ける | — |
| 決定 7 | 項目 5 で落とした 2 列を持つ文書は、読むときに黙って捨てる（通知しない、書き戻さない。表 T-297） | 今までに保存した文書はすべて 2 列を持つ（スキーマの `required`）。拒むと全部開けない。保つ（`OP-6` の知らない鍵の扱い）には `TaskVisual` に運び先が要り、落とした機能のために器を残すことになる | 人が `PR-13` か `Agent API` で置いた位置は失われる（画面は既に使っていなかった） |
| 決定 8 | 項目 6 のモノクロの入口は `IC-16` の左（表 T-109 で `IC-105` と `IC-16` のあいだ） | 逐語「ダークモードの左」 | — |
| 決定 9 | 項目 8 の「1 行」は、説明の各行を折り返さないこと。ポインタの点に出す `EZ-6` と `DC-3` の置き方は変えない（`IN-7`） | `DC-3` は 3 行を出すと決まっている。コード（`87458f63`）も同じ形 | — |
| 決定 10 | 項目 9 の `canvasPadding` の場所は変えず、その余白にも行の帯の地を塗る | `FR-052` の `Row Area` の幅の式を動かさずに済む | — |
| 決定 11 | 項目 10 の余白は `Row Area` の幅に対する比（片側 0.025） | 逐語「画面左右に … 各2.5%」。日程を描く場の幅は `Row Area` である | 画面全体の幅ではない |
| 決定 12 | 項目 11 の中央へ送るのは、基準日線を**出す**操作のときだけ（動かすときは送らない）。縦は動かさない | 逐語「有効にしたら」 | — |
| 決定 13 | 項目 11 のポインタは環境の `col-resize`（`PK-10`）とし、`Panel Divider` の掴み帯にも同じ行を当てる | 逐語「パネルサイズを変更するアイコンと同じ」。いまのコードは境界に `col-resize` を当てている（`dom-screen-surface.ts:294`） | — |
| 決定 14 | 項目 12 の「カーソル 3 種」は `IC-45`（`Dual Cursor`）・`IC-47`（十字）・`IC-48`（縦 1 本）とし、基準日線（`IC-44`）は排他に入れない | 逐語の「&#124;⇔&#124;を選択した後、他のカーソルに切り替えたとき」。基準日線は項目 11 で別に扱われ、文書のデータである | — |
| 決定 15 | 項目 12 の色と太さはガイドカーソルにも当てる（`S-195` ・ `S-194` を 2 つに広げる。ガイドカーソルはいま `S-148` と 1 の定数で描いている） | 逐語は 3 種に 1 つの色と太さを求めている | ガイドカーソルの色が控えめの灰からマゼンタ寄りに変わる |
| 決定 16 | 項目 12 の 50% は `S-334` = 0.4375（`S-204` の 0.875 の半分）を独立に持つ | 逐語「今の50%」。`S-204` から導くと、ツールチップの字を変えたときに読み出しも動く | 宿主の地の文字が 16px のとき 7px になる（11 節の問い 2）⇒ `JDG-398` が下限 10px（`S-340`）を足して解いた |
| 決定 17 | 項目 13 の頭の `[x]` は「すべての行を削除」（`IC-106`、`CD-6`）。消したあと空でない規則により深さ `L1` の行が 1 つ作られる | 逐語「全タスクグループの一括削除が可能」と、表 T-050 の後の段（行が 0 になるときの 1 行） | — |
| 決定 18 | 項目 14 の「クリア」は、`null` を許す列では `null`、`start` / `finish` では何も書かずに戻す | `erd.json` の `nullable`。コード（`95f7c3ed` と `field-commit.ts:148-156`）も同じ | — |
| 決定 19 | 項目 15 の「一番下」は、同じ対象の行の並びの末尾（`Task` は `PR-12` を `PR-15` の後、`TaskGroup` は `PR-19` を `PR-20` の後）。文書の描画設定（`FR-072`）の面は表 T-016 の外なので触れない | 表 T-016 の並びが印刷順そのもの | — |
| 決定 20 | 項目 16: 値の `#rrggbb` は英大文字で示す（保存は小文字のまま）。名で選んだ側は名の語。並べない名（ハイライトボックスの透明、行の色の黒）の場所は空けたまま詰めない。未定義の側の「（ライトと同じ）」の語は残す | 逐語の例 `#AAAAAA`。場所で覚えさせる | — |
| 決定 21 | 項目 16 のカスタムカラーの入口の語を `任意` ／ `Custom` に変える（`colourField` の `custom`） | 逐語「[Custom/任意]」 | 英語の語は 11 節の問い 3 |
| 決定 22 | 市松の 2 色（`#ffffff` ・ `#c0c0c0`）と 1 辺 2 ます、1 段 5 つを設定値の行にした | コード（`b359645b` ・ `c45d9065`）の定数そのもの。「値は行に置く」 | — |

---

## 1. 範囲 —— 項目ごとの行き先

| 項目 | 仕様で変える所 | 編集 | `frame-loop.ts` | 裁定を戻すとき |
|---|---|---|---|---|
| 1 予定日の年と字の大きさ | 表 T-251 の `ND-5`、`FR-002`、`S-325`（新） | E-01 ・ E-02 ・ J-07 | 触らない | `ND-5` の 1 行 ／ `S-325` の値 |
| 2 遅れのマーカー | `FR-013`、`S-326` ・ `S-327`（新、表 T-236）、`S-328` 〜 `S-331`（新、表 T-206） | E-03 ・ J-07 ・ J-08 | 触らない | 各行の値 |
| 3 矢羽根のフェード | 表 T-012a の `FD-5` | E-04 | 触らない | `FD-5` の 1 行 |
| 4 マーカーの既定 | `S-63` の既定 | J-01 | 触らない | `S-63` の既定 |
| 5 名称の位置の指定を落とす | `FR-002` ・ 表 T-023d の `GR-10` ・ `FR-029` の注 ・ `PR-13` ・ `AT-98` ・ `AT-99` ・ `CM-25` ・ `P-20` ・ `S-196` の備考 ・ 辞書、表 T-297（新） | E-05 〜 E-08 ・ E-30 ・ E-34 ・ E-35 ・ J-06 ・ J-09 〜 J-11 ・ J-16 | 触らない | 列と行を戻す（1 行では済まない。戻す見込みは低い） |
| 6 モノクロの入口 | 表 T-109 の `IC-100`、`FR-003` ・ `FR-041` ・ `FR-039` | E-09 〜 E-11 ・ E-31 ・ E-32 | 触らない | `IC-100` の面の欄と 3 文 |
| 7 名簿の閉じる入口 | 表 T-257 の `RR-6`（新） | E-12 ・ J-17 | 触らない | `RR-6` の 1 行 |
| 8 ツールチップ | 表 T-028 の `IN-7`（新） | E-13 | 触らない | `IN-7` の 1 行 |
| 9 スクロールバーと帯 | `FR-051` の段 | E-14 | 触らない | 段の 1 文ずつ |
| 10 全体表示 | `FR-055` の RATIONALE、`S-332`（新） | E-15 ・ J-07 | ⚠️ 触る（`:1652` の `scrollDayOffset: 0`） | `S-332` の値 |
| 11 基準日線 | `FR-046` ・ `FR-106` ・ 表 T-269 の `PK-10`（新）・ 表 T-237 の `EN-7`（新）・ `S-163` ・ `S-333`（新）・ `S-178` の備考 | E-16 〜 E-20 ・ J-02 ・ J-05 ・ J-07 | ⚠️ 触る（`:339` の `'GR-16': null`、基準日線を出す命令の後に表示位置を送る所） | 各行の値 ／ `EN-7` ／ `PK-10` |
| 12 カーソル 3 種 | `FR-048` ・ 表 T-029a の `DC-3` の字の大きさ ・ `DC-4` ・ `DC-9`（新）・ `EN-6` ・ `S-194` ・ `S-195` ・ `S-334`（新）・ 状態機械 | E-19 ・ E-21 〜 E-24 ・ J-03 〜 J-05 ・ J-07 ・ J-15 | ⚠️ 触る（`dualCursorEventOf` `:1403-1418` と、ガイドカーソルの入口から新しい出来事を起こす所） | `DC-9` を消し `DC-4` を戻す ／ 各行の値 |
| 13 行の一括削除 | 表 T-051 の `HF-20`（新）・ 表 T-050 の `CD-6`（新）・ 表 T-234 の `QN-10`（新）・ 表 T-109 の `IC-106`（新）・ 辞書 | E-25 〜 E-27 ・ E-33 ・ J-12 ・ J-13 | 入口の割り振りが `frame-loop.ts` を通るかは実装する者が測る（`input-command-translator.ts:711` が `rowDelete: 'IC-82'` を持つ） | `HF-20` ・ `IC-106` を消す |
| 14 日付の欄の消去 | `FR-006` の RATIONALE | E-28 | 触らない | 1 文 |
| 15 色の行を末尾へ | `FR-006` の RATIONALE、表 T-016 の並び | E-28 ・ J-09 | 触らない | 並びと 1 文 |
| 16 色の欄の示し方 | 表 T-017b の `CV-9`、`S-335` ・ `S-338`（新、表 T-206）、`S-336` ・ `S-337`（新、表 T-236）、辞書の `colourField` | E-29 ・ J-07 ・ J-08 ・ J-14 | 触らない | 各行の値 ／ `CV-9` |

**数**: 仕様の文の編集 37（E-01 〜 E-37）、原稿 JSON の編集 17（J-01 〜 J-17）。

⭐ **当てるときに見つかった書き漏れ 3 つ（2026-09-23、波 0 の体が止まり、前に立つ者が反証して足した）**:
- **E-36**（項目 13）—— `FR-004` の表 T-051 の後の段は、頭の入口を 4 つとし「頭にその 3 つの入口（隠す・消す・留める）を置いてはならない（MUST NOT）」と定めていた。これは利用者の裁定ではなく「段 0 は実体が無いので消せない」から導いた文であり、`JDG-380`（頭に [x]、全行の一括削除）が上位である。⇒ 頭の入口を 5 つとし、禁じるのは隠すと留めるの 2 つ、頭の削除は段 0 そのものではなく配下のすべての行を消す（`CD-6`）、と書き直す。`HF-20` は `IC-82` を段 0 に対して行うものとして、残る対応の列に足す。
- **E-37**（項目 12）—— `FR-048` の STATEMENT「表 T-029 の 3 種をそれぞれ独立に出し分けること」は `DC-9` の排他と食い違う。⇒ 「基準日線（`CU-1`）は他と独立に、`CU-2` と `CU-3` は 1 つの排他の選択として（`DC-9`）」と書き直す。
- **J-15 の出来事の定義** —— 状態機械の出来事は領域 `screen` の `events` に定めてから遷移に書く（生成器がそう求める）。⇒ `guideCursorEntryPressed` を `dualCursorEntryPressed` と同じ形で足す（入力、行 `IC-47` ・ `IC-48` ・ `DC-9`、運ぶ値は押したガイドカーソルの値 `guideCursor`）。
- **E-38**（項目 5）—— `MG-8` の「見た目（色・形状・名称ラベルの位置）」は、名称ラベルの位置が保存されなくなったので「見た目（色・形状）」とする（波 1d の体が見つけた）。
- **E-39**（項目 5）—— `GL-001` のシナリオの拡張 4b「作成者が名称ラベルの位置をずらす。`GRS` は指定を保持し、以後その位置に置く。」を消す（落とした機能を生きたものとして書いていた）。`FR-109` の ORIGIN の「拡張 4a・4b」も「拡張 4a」とする。
- **E-40**（項目 11）—— 表 T-274 の `PP-6`（ポインタの形で、何を掴むかが分かる）の「覆う行」に `PK-10` を足す。同表の後の段が、覆う行の無い行を 表 T-269 へ足すことを禁じている（波 2.5 の体が契約の試験で見つけた）。
- **E-41**（項目 14）—— `FR-006` の E-28 の文は `start` ／ `finish` を「`null` を許さない列」と書いたが、`_source/erd.json` では `Task.start` ・ `Task.finish` の `nullable` はどちらも「可」である（閉じの体が見つけた）。⇒ 列の許しで分けず、欄で分ける: 「空のまま確定したときは、`start` ／ `finish` の欄なら何も書かずに欄を元の値へ戻し、それ以外の日付の欄なら `null` を書くこと（MUST） —— 予定の開始と終了を空にすると、形を描く日が無くなる。」
- **E-42**（項目 2）—— `S-341`（新、表 T-206）を足した: 「進捗マーカーの記号の半分の高さの、マーカーの円の半径に対する比（表 T-021 の `PM-2` ・ `PM-3` ・ `PM-4`、`FR-013`）」。描き手が `src/adapter/svg-renderer/schedule-task-figures.ts:164` に持っていた素の定数 `const r = radius * 0.5`（マーカーの円の半径に掛ける、どの行にも無かった 0.5。同じ `r` は `PM-2` ・ `PM-3` の記号も使う）を、この行へ移した。`S-329` ・ `S-330` の値の文は「記号の半分の高さ」を「記号の半分の高さ（`S-341`）」と書き直した（閉じの体が当てた）。
- **E-43**（項目 9）—— 表 T-023d の `GR-21` は、つまみの長さを「見えている範囲 ÷ 全体」とするが、「全体」を定めていなかった。`399dd130` のコードは、内容の範囲といま見えている範囲の和を全体とし、押した時点の値を掴んでいるあいだ保つ（`DFC-802`）。⇒ `GR-21` の欄に 1 文を足した: 「⭐ 全体は、内容の範囲といま見えている範囲の和とし、つまみを掴んでいるあいだは押した時点の全体を保つこと（MUST） —— 全体表示は余白の分だけ見えている範囲を内容の外へ広げるので、内容だけを全体とすると、つまみがポインタから遅れる。」
- **E-44**（項目 16、`PND-531`）—— `CV-9` の並べ方は 2 段目を [カスタムカラーの入口] [透明] で終え、`FR-007` の「指定した色をテーマ追随へ戻せること（MUST）」と `CV-5` の「戻す入口」を押す所が欄に無かった（閉じの体が見つけ、利用者に問うた）。⇒ 利用者の裁定（`JDG-405`、2026-09-24）「推奨は、2 段目を [任意] [透明] [テーマ] にし、呼び名を「テーマ / Theme」にする案を採用する。」に従い、並べ方の文を「その下の段にカスタムカラーの入口、透明、テーマに戻す入口の順に置くこと（MUST）。」と書き直し、1 文を足した: 「⭐ テーマに戻す入口は `CV-5` の「戻す入口」（`FR-007`）であり、押したらその欄の色をテーマ追随（`null`）へ戻すこと（MUST） —— カスタムカラーなら 2 つの値を両方捨てる（`CV-5`）。」ハイライトボックスの枠の文の後に 1 文を足した: 「その欄では透明の場所は空けたままとし、テーマに戻す入口はその後ろに置く（枠の色もテーマ追随へ戻せる）。」を足した。語は辞書の `colourField` の `theme`（「テーマ」／「Theme」）と `themeHint`（「プロジェクトのテーマの色に従う」／「Follow the project theme」）が持つ。
- **生成器** —— 新しい設定値の行 16 個（`S-325` 〜 `S-340`）は `tools/generate_entity_types.py` が群に載せるまで src へ出なかった。波 1a の体が群を足した（`NOT_STORED_LABEL_SIZES` ・ `NOT_STORED_DELAY_MARK_SIZES` ・ `NOT_STORED_DUAL_CURSOR_SIZES` ・ `NOT_STORED_FIT_MARGIN` ・ `NOT_STORED_HELP_SIZES` ・ `NOT_STORED_PROPERTY_FIELD_SIZES` ・ `SCHEDULE_COLOURS` ・ `SCREEN_COLOURS`）。
- ⚠️ 検査 11（重複）: `S-333` と `S-194` の備考、`S-329` と `S-330` の値の文が字面で重なったので、新しい行の側の文を書き換えた（基準線は動かさない）。

---

## 2. 新しい識別子

⚠️ 規則 02 の 2.5 節: 下の「空いている」は 2026-09-23 に `73c2bc90` で測った主張である。**当てる直前に測り直すこと。** 測り方は 13 節。

| 種類 | 採ったもの | 測った最大（2026-09-23、`73c2bc90`） |
|---|---|---|
| 設定値 | `S-325` `S-326` `S-327` `S-328` `S-329` `S-330` `S-331` `S-332` `S-333` `S-334` `S-335` `S-336` `S-337` `S-338` は空いている。⭐ 畳み込みで `S-339` ・ `S-340` を足した（前に立つ者の帯 `S-339` 〜 `S-349`。2026-09-23 に `a912f4ea` で測り直して空き）。⭐ 閉じの体が `S-341` を同じ帯（`S-339` 〜 `S-349`）から足した（E-42） | `S-324`（`S-999` は試験の番兵なので除く） |
| 表 | `T-297` は空いている | 仕様が使う最大の表は `T-296` |
| 接頭辞 | `RK` は空いている（接頭辞の登録簿は 162 件、`RK-` の行は木のどこにも無い） | — |
| 行 | `EN-7` `PK-10` `IN-7` `RR-6` `HF-20` `CD-6` `DC-9` `QN-10` `IC-106` `RK-1` `RK-2` は空いている | `EN-6` ・ `PK-9` ・ `IN-6` ・ `RR-5` ・ `HF-19`（`HF-120` は試験の注記の字） ・ `CD-5` ・ `DC-8` ・ `QN-9` ・ `IC-105` |
| 台帳 | `JDG-367` 〜 `JDG-383`（帯 `JDG-367` 〜 `JDG-389`）、`DFC-789` 〜 `DFC-793`（帯 `DFC-789` 〜 `DFC-799`） | 帯は前に立つ者が配った |
| 名 | 状態機械の出来事 `guideCursorEntryPressed`、効果 `writePlaceDualCursorClearingGuide` ・ `writeClearDualCursorSettingGuide` は空いている | — |

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 消すもの | 所（`73c2bc90`） | 置き換わる先 | 編集 |
|---|---|---|---|
| `ND-5` の「年（4 桁）」 | `01-04-requirements.md:1434` | 年（下 2 桁） | E-01 |
| `FR-013` の「色は実績バーの色を継ぐ」の、遅れについての効き | `:2979` | 遅れは `S-326` ・ `S-327` | E-03（文は残し、例外を足す） |
| `FD-5` の「フェード長が切り込みの深さを置き換える」（端を分けない読み） | `:1327` | 端ごとの置き換え | E-04 |
| `FR-002` の STATEMENT の「`nameAnchor` / `nameAlign` の指定」と「指定があるときはその指定に従うこと」 | `:1387-1389` | 置き場所は `FR-109` だけ | E-05 |
| `FR-002` の「位置の指定は 9 点アンカーと … ずれる。」の 4 行 | `:1436-1439` | 位置を指定させない MUST NOT ＋ 表 T-297 への指し | E-06 |
| `GR-10` の「位置を変える経路は 表 T-016 の `PR-13` だけである」 | `:3380` | 経路を持たない | E-07 |
| `FR-029` の注の「`nameAnchor` / `nameAlign` は `PR-13` と `FR-028` から編集できる」と、次の行の「`GR-10` を本注記の例に挙げてはならない」 | `:6398-6399` | 1 行目だけ残す | E-08 |
| 表 T-016 の `PR-13` | `_source/property-items.json`（生成物 `tbl-property-items.md:44`） | — | J-09 |
| `TaskVisual.nameAnchor` ・ `nameAlign`（`AT-98` ・ `AT-99`） | `_source/erd.json:2309-2347`（生成物 `fig-erd-detail.md:145-146` ・ `:409-410`、`grs-document.schema.json:1598-1599` ・ `:1611-`） | 表 T-297 の `RK-1` ・ `RK-2` | J-10 ・ E-30 |
| 命令 `CM-25` `setTaskVisualNamePlacement` | `tbl-glossary.md:431` | — （欠番） | E-35 |
| 用語 `P-20` | `tbl-glossary.md:72` | — | E-34 |
| 辞書の `properties` の `PR-13` | `_source/display-words.json:1163` | — | J-11 |
| `S-196` の備考の「文書がラベルのアンカー（`PR-13`）を持ち」 | `_source/settings.json` の `S-196` | 文書がラベルの位置を持たない | J-06 |
| `FR-003` ・ `FR-041` ・ `FR-039` の「モノクロの入口は `Command Palette`」 | `:1490` ・ `:2008` ・ `:6765` | `App Header` の `IC-16` の左 | E-09 〜 E-11 |
| 表 T-109 の `IC-100` の面 `Command Palette` | `tbl-glossary.md:569` | `App Header`（`:528` と `:529` のあいだへ移す） | E-31 ・ E-32 |
| `FR-048` の「1 つの入口が 2 つを同時に消してはならない（MUST NOT）」 | `:4855` | 排他（`DC-9`） | E-21 |
| `DC-4` の「ガイドカーソルの「なし」で出てはならない —— 3 種は独立に出し分ける」 | `:4887` | ガイドカーソルの入口の押下で出る | E-22 |
| `DC-3` の字の大きさ `S-204` | `:4886` の中 | `S-334` | E-24 |
| `S-195` の「`S-151` を継ぐ」（色相に追随）と値 | `_source/settings.json:4553-4571` | マゼンタ寄りの固定の色 | J-03 |
| `S-194` の備考の「`S-178` の備考が … 本行が埋める」 | 同 `:3066` | — | J-04 |
| `S-178` の備考の「基準日線と 2 連カーソルは太さの行そのものを持たない」 | 同 `:3635` | `S-333` ・ `S-194` を持つ | J-05 |
| `S-163` の値（灰） | 同 `:4827-4845` | 朱色 | J-02 |
| 表 T-237 の後の「`S-183` が 5 行に立つ」 | `:6381` | 6 行 | E-20 |
| `CV-9` の「並べ方は試作の色の選び（`selHue`）に倣い、名を 1 列に表 T-294 の行の順で並べ」と「（例: `ライト ■ / ダーク ■（ライトと同じ）`）」 | `:1954` | 1 段 `S-338` 個・その下にカスタムと透明、`ライト: ■ #AAAAAA / ダーク: ■ #050505` | E-29 |
| 辞書の `colourField` の `custom` の語「カスタムカラー」 ／ 「Custom color」 | `_source/display-words.json` | 「任意」 ／ 「Custom」 | J-14 |
| 状態機械の効果 `writePlaceDualCursor`（名） | `_source/state-machines.json` の `dualCursorModeStateMachine` | `writePlaceDualCursorClearingGuide` | J-15 |
| コードの定数（⛔ 本書は直さない。9 節） | `schedule-task-figures.ts` の `DELAY_*` 4 つ、`properties-panel-drawing.ts` の `CHECKER_*` と `SWATCH_*`、`schedule-overlays.ts:176` の `guideWidth = 1` と `:175` の `S-148`、`:156` の基準日線の太さ `1` | 設定値の行 | 実装する者 |

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-541` ・ `CR-550` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（13 節の道具が 35 件すべてで 1 回を確かめた）。旧・新は、下の `<!-- EDIT … -->` の直後の 2 つの `text` の塊である。新が空の編集は、旧の行をその改行ごと消す。
⚠️ 同じファイルの編集は上から順に当てること —— E-31 が消した `IC-100` を E-32 が `IC-16` の前へ置き直す。
⚠️ 生成物（`_assets/tbl-settings.md` ・ `tbl-property-items.md` ・ `fig-erd-*.md` ・ `tbl-row-id-prefixes.md` ・ `tbl-state-machines.md` ・ `_source/grs-document.schema.json` と `src/` の生成物）は手で直さない。原稿を直して `npm run gen` を打つ。

### 4.1 文の編集（`01-04-requirements.md` ・ `05-07-design.md` ・ `_assets/tbl-glossary.md`）

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md -->
項目 1。旧
```text
| ND-5 | 年 | 文書のすべてのタスクとマイルストーンの `start` と `finish` が 1 つの暦年に収まるときは書かない。<br>2 つ以上の暦年にまたがるときは、すべての日を 年（4 桁）、`/`、月、`/`、日 の順で書く —— 年の有無を日ごとに変えると、どの日に年が付くかを日ごとに読み分けさせることになる |
```
新
```text
| ND-5 | 年 | 文書のすべてのタスクとマイルストーンの `start` と `finish` が 1 つの暦年に収まるときは書かない。<br>2 つ以上の暦年にまたがるときは、すべての日を 年（西暦の下 2 桁。1 桁のときは 0 を詰めて 2 桁とする）、`/`、月、`/`、日 の順で書く —— 年の有無を日ごとに変えると、どの日に年が付くかを日ごとに読み分けさせることになる。<br>⚠️ 年を 4 桁で書かないのは、名称ラベルが日程の上で幅を取る場だからである（`EZ-6` の `YYYY-MM-DD` は全文を読ませる場なので 4 桁のまま） |
```

<!-- EDIT id=E-02 file=docs/spec/01-04-requirements.md -->
項目 1。旧
```text
名前が空のときは、半角空白を置かず、予定日の文字から書くこと（MUST）。  
```
新
```text
名前が空のときは、半角空白を置かず、予定日の文字から書くこと（MUST）。  
⭐ 予定日の文字（その前に置く半角空白を含む）は、名前の字の大きさに `_assets/tbl-settings.md` の 表 T-206 の `S-325` を掛けた大きさで描くこと（MUST） —— 日付は名前を補う字であり、名前より目立たせない。  
⭐ `FR-109` の 表 T-273 が「入る」を判ずる幅と、表 T-038 の `OC-1` が数える幅は、名前の部分と予定日の部分をそれぞれの大きさで測った和とすること（MUST） —— 1 つの大きさで測ると、入ると判じたラベルが基準の右端を越えるか、入るラベルを外へ出す。  
```

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md -->
項目 2。旧
```text
**色は実績バーの色を継ぎ、独立した色を保存しない**（`FR-041`）。  
```
新
```text
**色は実績バーの色を継ぎ、独立した色を保存しない**（`FR-041`）。  
⭐ ただし遅れ（表 T-021 の `PM-4`）のマーカーは、地を `_assets/tbl-settings.md` の 表 T-236 の `S-326`、記号を同表の `S-327` で塗ること（MUST） —— 遅れは `UC-006` が最も見つけたい対象であり、実績バーの色を継ぐと、淡い色のタスクで遅れが沈む。  
⚠️ 同表は成果物に埋め込む定数であり、文書に保存しないので、上の「独立した色を保存しない」と両立する。  
⭐ 遅れの記号 `(!)` の縦棒の太さは、`S-24` に 表 T-206 の `S-328` を掛けた値とすること（MUST）。  
縦棒の下端・点の中心・点の半径は、同表の `S-329` ・ `S-330` ・ `S-331` とすること（MUST） —— 縦棒の下端と点の上端のあいだに隙間を空け、`!` を 2 つの形として読ませる。  
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
項目 3。旧
```text
| FD-5 | 適用する形状 | **矩形と矢羽根のみ**（表 T-012 の SH-1 / SH-2）。<br>矢羽根では**フェード長が切り込みの深さを置き換える** |
```
新
```text
| FD-5 | 適用する形状 | **矩形と矢羽根のみ**（表 T-012 の SH-1 / SH-2）。<br>矢羽根では、開始側の切り込みの深さを `fadeIn` に、終了側の先端の深さを `fadeOut` に、端ごとに置き換えること（MUST）。<br>⛔ 一方の端のフェード長で、もう一方の端の形を変えてはならない（MUST NOT） —— 2 つの端は別々の日付の曖昧さを表す（`FD-1` ・ `FD-2` と同じ）。<br>フェード長が 0 の端は、フェードの無い矢羽根と同じ深さのままとする |
```

<!-- EDIT id=E-05 file=docs/spec/01-04-requirements.md -->
項目 5。旧
```text
**STATEMENT**: `GRS` は、タスクの名称ラベルの字そのもの —— 打ち切り、添える予定日、`nameAnchor` / `nameAlign` の指定 —— を本要求のとおりに書くこと。  
**指定があるときはその指定に従うこと。**  
**指定が無いときの置き場所は `FR-109` が定める**（表 T-272 ・ 表 T-273 ・ 表 T-271）。
```
新
```text
**STATEMENT**: `GRS` は、タスクの名称ラベルの字そのもの —— 打ち切りと、添える予定日 —— を本要求のとおりに書くこと。  
**置き場所は `FR-109` が定める**（表 T-272 ・ 表 T-273 ・ 表 T-271）。
```

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md -->
項目 5。旧
```text
位置の指定は 9 点アンカーと左詰め / 中央 / 右詰めで持つこと（MUST）。  
既定は自動配置とし、人が動かしたときだけ指定が残る。  
ピクセル座標で持ってはならない（MUST NOT） ⚠️ 「人が動かした」は 表 T-016 の `PR-13`（命令は `CM-25`）と `Agent API`（`FR-028`）を通した指定のことである—— **画面でラベルだけを掴んで動かす経路を 表 T-023d は持たない。**  
—— 縦横独立ズームでずれる。
```
新
```text
⛔ 名称ラベルの位置を人が指定する手段を持ってはならない（MUST NOT） —— 置き場所は `FR-109` だけが決める。  
⚠️ 位置を指定する列を持っていた文書を読むときの扱いは、`05-07-design.md` の 表 T-297 が持つ。
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md -->
項目 5。旧
```text
⛔ **掴んで動かさない（MUST NOT）**—— **位置を変える経路は 表 T-016 の `PR-13` だけである** |
```
新
```text
⛔ **掴んで動かさない（MUST NOT）**—— **位置を変える経路は持たない**（置き場所は `FR-109` だけが決める） |
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md -->
項目 5。旧（2 行目（`GR-10` を例に挙げるなという MUST NOT）は、例に挙げうる編集の面が `GR-10` から消えたので消す）
```text
本書はこれを各所で明示的に許している（`FR-011` / `FR-006` / `FR-049`）—— `start` / `finish` は `PR-3` と掴み領域（表 T-023d）、`nameAnchor` / `nameAlign` は `PR-13` と `FR-028` から編集できる。  
⚠️ **`GR-10` を本注記の例に挙げてはならない（MUST NOT）** —— **掴む面でなくなったので、値として編集する面しか残っていない。**  
```
新
```text
本書はこれを各所で明示的に許している（`FR-011` / `FR-006` / `FR-049`）—— `start` / `finish` は `PR-3` と掴み領域（表 T-023d）から編集できる。  
```

<!-- EDIT id=E-09 file=docs/spec/01-04-requirements.md -->
項目 6。旧
```text
⭐ **その入口を `Command Palette` に置くこと（MUST）** —— ⭐ 文字サイズ（`FR-039`）とモノクロ（`FR-041`）と同じ置き場である。  
```
新
```text
⭐ **その入口を `Command Palette` に置くこと（MUST）** —— ⭐ 文字サイズ（`FR-039`）と同じ置き場である。  
```

<!-- EDIT id=E-10 file=docs/spec/01-04-requirements.md -->
項目 6。旧
```text
モノクロを選ぶ入口を `Command Palette` に置くこと（MUST） —— ⭐ 文字サイズ（`FR-039`）と積む向き（`FR-003`）と同じ置き場である。  
```
新
```text
モノクロを選ぶ入口を `App Header` の、明暗テーマの入口（`_assets/tbl-glossary.md` の 表 T-109 の `IC-16`）の左に置くこと（MUST） —— 明暗とモノクロはどちらも描く色の選び方であり、並べると見比べて選べる。  
```

<!-- EDIT id=E-11 file=docs/spec/01-04-requirements.md -->
項目 6。旧
```text
文字サイズを変える入口を `Command Palette` に置くこと（MUST） —— ⭐ 文字サイズと、モノクロ（`FR-041`）と、積む向き（`FR-003`）の 3 つの入口は、同じ `Command Palette` に置く。  
```
新
```text
文字サイズを変える入口を `Command Palette` に置くこと（MUST） —— ⭐ 文字サイズと積む向き（`FR-003`）の 2 つの入口は、同じ `Command Palette` に置く。  
⚠️ モノクロ（`FR-041`）の入口は `App Header` に置く。  
```

<!-- EDIT id=E-12 file=docs/spec/01-04-requirements.md -->
項目 7。旧
```text
| RR-5 | 罫線 | 一覧のすべての欄のあいだに、縦と横の線を引くこと（MUST）。<br>太さは 表 T-206 の `S-241` とし、画面の px で描くこと（MUST）。<br>⛔ 表示の倍率（`S-234`）も `S-235` も `S-240` も掛けてはならない（MUST NOT） —— 1px の線を縮めると、画面の画素に届かず消える |
```
新
```text
| RR-5 | 罫線 | 一覧のすべての欄のあいだに、縦と横の線を引くこと（MUST）。<br>太さは 表 T-206 の `S-241` とし、画面の px で描くこと（MUST）。<br>⛔ 表示の倍率（`S-234`）も `S-235` も `S-240` も掛けてはならない（MUST NOT） —— 1px の線を縮めると、画面の画素に届かず消える |
| RR-6 | 閉じる入口 | 閉じる入口（`_assets/tbl-glossary.md` の 表 T-109 の `IC-52`）を、面の見出しの行の右端（面の右上）に置くこと（MUST） —— `FR-036` がヘルプの題の行に定める置き方と同じであり、面ごとに閉じる入口の場所を探させない。<br>⚠️ 見出しの行はスクロールしない（`RR-2`）ので、閉じる入口も一覧と一緒に送られない |
```

<!-- EDIT id=E-13 file=docs/spec/01-04-requirements.md -->
項目 8。旧
```text
| IN-6 | 確定していないその場の編集があるとき、その入力欄の外でポインタを押したら、その編集を確定すること（MUST）。<br>取り消してはならない（MUST NOT）—— ⛔ どちらも起きないと、押しても焦点が欄に留まり、`AG-9` の「入力中」が解けない ⇒ 表 T-067 の `WS-2` が `Agent API` の書き込みを拒み続け、`IN-5a` が `Delete` を飲み続ける。<br>⭐ **取り消さないのは、打った内容が押下 1 つで消えてはならないからである** —— 取り消す道は `IN-4` の `Esc` が既に持つ。<br>⛔ **押下そのものの働きを止めてはならない（MUST NOT）** —— 確定は押下に乗るのであって、押下を置き換えない。<br>**押下が何をするかは 表 T-023a と `IN-1` が決める。<br>**⚠️ **`SK-19` の `Enter` と同じ確定である** —— 引き金が 3 つ目になるだけで、確定の意味は 1 つである。<br>⛔ **始めた値と同じ値を書いてはならない（MUST NOT）** —— **何も変えない書き込みを、そもそも道に乗せないためである。<br>**⭐ **ここで止めれば、命令も束も通知も立たない** —— **下流の規則に飲み込ませるより安い。<br>**⚠️ 「同じ値を 2 度書くと取り消しが 2 段になる」を本行の理由にしてはならない —— `FR-031` は値が動かなければ段を残さないので、その結果は起きない。<br>⛔ **段が積まないことと、無駄な書き込みを送ることは別である。<br>**|
```
新
```text
| IN-6 | 確定していないその場の編集があるとき、その入力欄の外でポインタを押したら、その編集を確定すること（MUST）。<br>取り消してはならない（MUST NOT）—— ⛔ どちらも起きないと、押しても焦点が欄に留まり、`AG-9` の「入力中」が解けない ⇒ 表 T-067 の `WS-2` が `Agent API` の書き込みを拒み続け、`IN-5a` が `Delete` を飲み続ける。<br>⭐ **取り消さないのは、打った内容が押下 1 つで消えてはならないからである** —— 取り消す道は `IN-4` の `Esc` が既に持つ。<br>⛔ **押下そのものの働きを止めてはならない（MUST NOT）** —— 確定は押下に乗るのであって、押下を置き換えない。<br>**押下が何をするかは 表 T-023a と `IN-1` が決める。<br>**⚠️ **`SK-19` の `Enter` と同じ確定である** —— 引き金が 3 つ目になるだけで、確定の意味は 1 つである。<br>⛔ **始めた値と同じ値を書いてはならない（MUST NOT）** —— **何も変えない書き込みを、そもそも道に乗せないためである。<br>**⭐ **ここで止めれば、命令も束も通知も立たない** —— **下流の規則に飲み込ませるより安い。<br>**⚠️ 「同じ値を 2 度書くと取り消しが 2 段になる」を本行の理由にしてはならない —— `FR-031` は値が動かなければ段を残さないので、その結果は起きない。<br>⛔ **段が積まないことと、無駄な書き込みを送ることは別である。<br>**|
| IN-7 | ツールチップは、説明の各行を折り返さずに 1 行で出すこと（MUST）。<br>⭐ 説明の幅は中身の幅とし、その上限を閲覧環境の窓の幅から両側に `_assets/tbl-settings.md` の 表 T-206 の `S-339` を引いた幅とすること（MUST）。<br>行が上限より広いときだけ、その行を折り返すこと（MUST） —— 窓より長い 1 行は、折り返さなければ窓の外で切れる。<br>⭐ 対象の左端に揃えて出すと窓の右端から `S-339` の内側に収まらないときは、説明の右端を対象の右端に揃えて出すこと（MUST） —— 画面の右側に在る入口（例: ヘルプの入口、`_assets/tbl-glossary.md` の 表 T-109 の `IC-22`）の説明が、画面の外で切れない。<br>それでも左端が窓の左端から `S-339` の内側に収まらないときは、左端を窓の左端から `S-339` の位置に置くこと（MUST）。<br>⭐ 揃え方は、説明を出したときに 1 度だけ測って決めること（MUST）。<br>⛔ 窓の大きさを繰り返し読みに行ってはならない（MUST NOT） —— 幅の上限は窓の幅に従う書き方で描き手に持たせ、測るのは出した瞬間だけで足りる。<br>⚠️ ポインタの点に出す説明（`FR-092` の `EZ-6` と、表 T-029a の `DC-3`）の置き方は `IN-3` の例外のままであり、本行の揃え方は当たらない |
```

<!-- EDIT id=E-14 file=docs/spec/01-04-requirements.md -->
項目 9。旧
```text
⭐ `canvasPadding`（同書の 表 T-201 の `S-56`）を横に取る場所は、`Row Area` と縦の `Scrollbars` のあいだとすること（MUST） —— `FR-052` の `Row Area` の幅の式は変わらない。  
⚠️ `canvasPadding` を縦に取る場所は本段が変えない。
```
新
```text
⭐ `canvasPadding`（同書の 表 T-201 の `S-56`）を横に取る場所は、`Row Area` と縦の `Scrollbars` のあいだとすること（MUST） —— `FR-052` の `Row Area` の幅の式は変わらない。  
⭐ その余白にも、行の帯の地を、縦の `Scrollbars` の左端まで続けて塗ること（MUST） —— 塗らないと、行の帯が縦の `Scrollbars` の手前で切れて見える。  
⛔ 行の帯の地も日程の形も、縦の `Scrollbars` の帯の中へ描いてはならない（MUST NOT）。  
⭐ 横の `Scrollbars` は、`GRS` が占める画面の下端に接して置くこと（MUST）。  
⛔ 日程（行の帯・形・罫線）を横の `Scrollbars` の帯の中へ描いてはならない（MUST NOT） —— 帯の下に日程が透けると、つまみと日程のどちらを掴むのかが読めない。  
⚠️ `canvasPadding` を縦に取る場所は本段が変えない。
```

<!-- EDIT id=E-15 file=docs/spec/01-04-requirements.md -->
項目 10。旧
```text
⭐ **横はこの限りではない** —— 横に床は無く、段階は `FR-017` が 1 日あたりの幅で定める。
```
新
```text
⭐ **横はこの限りではない** —— 横に床は無く、段階は `FR-017` が 1 日あたりの幅で定める。
⭐ 横は、表 T-038 に従って測った実寸の左右に、`Row Area` の幅に `_assets/tbl-settings.md` の 表 T-206 の `S-332` を掛けた余白を片側ずつ残して収めること（MUST） —— 端の名称ラベルが `Row Area` の縁に接すると、切れたのか終わったのかが読めない。  
⭐ 横の実寸は、採った段と、その段が着地する縦の倍率で描いた絵で測ること（MUST） —— 名称ラベルと札の幅は字の大きさ（縦の倍率）に従い、横の倍率には従わないので、別の段や別の縦の倍率で測った幅では、採った絵が `Row Area` からはみ出す。  
⚠️ 算入するのは、いま描いているものだけである —— 表 T-038 の `OC-2` は表示しているときだけ数え、名称ラベルの書き出しは進捗マーカーを出すかどうかで変わる（`FR-109` の 表 T-273）。  
⭐ 採った段がその着地する縦の倍率で収まらず、1 つ浅い段へ下がるときは、下がった段をその段の着地する縦の倍率で測り直し、収まるかを確かめてから採ること（MUST） —— 返す倍率と、表示位置を決めた絵は、同じ段・同じ倍率のものでなければならない。  
⭐ 左端の表示位置は、日付（`scrollDate`）と日の中の位置（`_assets/tbl-settings.md` の `S-177` の `scrollDayOffset`）の組で持つこと（MUST）。  
日の境目へ切り捨ててはならない（MUST NOT） —— 切り捨てた分だけ絵が右へずれ、右端の名称ラベルが切れる。  
```

<!-- EDIT id=E-16 file=docs/spec/01-04-requirements.md -->
項目 11。旧
```text
⚠️ **表示状態を別に持ってはならない（MUST NOT）** —— 描くかどうかは `statusDate` が `null` かどうかで決まる。
```
新
```text
⚠️ **表示状態を別に持ってはならない（MUST NOT）** —— 描くかどうかは `statusDate` が `null` かどうかで決まる。
⭐ 基準日線を出す操作では、倍率を変えずに、基準日線が `Row Area` の横の中点に来るよう表示位置を横に送ること（MUST） —— 出した線がいま見ている範囲の外に立つと、出たことが読めない。  
縦の表示位置は動かさないこと（MUST）。  
⭐ 基準日線は、色を `_assets/tbl-settings.md` の 表 T-236 の `S-163`、太さを同書の 表 T-206 の `S-333` で描くこと（MUST）。  
⭐ 基準日線を描いているあいだ、基準日の入口（`_assets/tbl-glossary.md` の 表 T-109 の `IC-44`）を `FR-029` の 表 T-237 の `EN-7` で塗ること（MUST）。  
⚠️ ポインタが基準日線の掴み代（表 T-023d の `GR-16`）に入ったときの形は `FR-106` が持つ。  
```

<!-- EDIT id=E-17 file=docs/spec/01-04-requirements.md -->
項目 11。旧
```text
⭐ 押す役と引く役を兼ねる進捗マーカー（表 T-270 の `PE-8`）は、押す役の形（指）とすること（MUST）。
```
新
```text
⭐ 押す役と引く役を兼ねる進捗マーカー（表 T-270 の `PE-8`）は、押す役の形（指）とすること（MUST）。
⭐ 基準日線の掴み代（表 T-023d の `GR-16`）と、パネルの境界の掴み帯（同表の `GR-22`）では、表 T-269 の `PK-10` とすること（MUST） —— どちらも縦の線を左右へ動かす操作であり、同じ形が同じ動きを示す。  
```

<!-- EDIT id=E-18 file=docs/spec/01-04-requirements.md -->
項目 11。旧
```text
| PK-9 | 再開の折れ矢印 | 再開アイコンと同じ折れ矢印（縦棒・腕・矢じり） | 黒 ／ 白 | 角張り | 一辺 `S-296` | 折れ目（縦棒と腕の交わる点） |
```
新
```text
| PK-9 | 再開の折れ矢印 | 再開アイコンと同じ折れ矢印（縦棒・腕・矢じり） | 黒 ／ 白 | 角張り | 一辺 `S-296` | 折れ目（縦棒と腕の交わる点） |
| PK-10 | 左右の境目 | 閲覧環境の、縦の境目を左右へ動かす形（`col-resize`） | — | — | 環境のまま | 環境のまま |
```

<!-- EDIT id=E-19 file=docs/spec/01-04-requirements.md -->
項目 11, 12。旧
```text
| EN-6 | その入口が、排他の選択のうちいま選ばれているものである（`_assets/tbl-settings.md` の `S-66` のガイドカーソル） | `S-183` | `FR-048` |
```
新
```text
| EN-6 | その入口が、排他の選択のうちいま選ばれているものである（`_assets/tbl-settings.md` の `S-66` のガイドカーソルと、`Dual Cursor` のモード。表 T-029a の `DC-9`） | `S-183` | `FR-048` |
| EN-7 | その入口が出す線を、いま描いている（基準日線。`statusDate` が `null` でない） | `S-183` | `FR-046` |
```

<!-- EDIT id=E-20 file=docs/spec/01-04-requirements.md -->
項目 11。旧
```text
⚠️ `S-183` が 5 行に立つのは
```
新
```text
⚠️ `S-183` が 6 行に立つのは
```

<!-- EDIT id=E-21 file=docs/spec/01-04-requirements.md -->
項目 12。旧
```text
⛔ **1 つの入口が 2 つを同時に消してはならない（MUST NOT）** —— **同行が既にそう定めている。**
```
新
```text
⭐ `Dual Cursor` のモード（`CU-2`）とガイドカーソル（`CU-3`）の 2 つの値は 1 つの排他の選択とし、1 つを選ぶと他を消すこと（MUST） —— 規則は 表 T-029a の `DC-9` が持つ。  
⭐ `CU-2` と `CU-3` の線は、色を `_assets/tbl-settings.md` の 表 T-236 の `S-195`、太さを同書の 表 T-206 の `S-194` で描くこと（MUST）。  
```

<!-- EDIT id=E-22 file=docs/spec/01-04-requirements.md -->
項目 12。旧
```text
| DC-4 | 出る | 同じ入口の再押下、または `Esc`。<br>⚠️ **ガイドカーソルの「なし」（`CU-3`）で出てはならない（MUST NOT）** —— 3 種は独立に出し分ける（`FR-048`）ので、1 つの入口が 2 つを同時に消してはならない |
```
新
```text
| DC-4 | 出る | 同じ入口の再押下、`Esc`、またはガイドカーソルの入口（`_assets/tbl-glossary.md` の 表 T-109 の `IC-47` ・ `IC-48`）の押下（`DC-9`） |
```

<!-- EDIT id=E-23 file=docs/spec/01-04-requirements.md -->
項目 12。旧
```text
| DC-8 | どちらが追従中か | 追従している側を、表 T-023c の `SL-8` が線に定める規則と同じやり方で示すこと（MUST） —— 同行の理由もそのまま当てはまる。<br>⚠️ `DC-2` は押すたびに追従する側が入れ替わると定めるので、この印もそのたび入れ替わる。<br>⚠️ **この印を書き出しに出してはならない（MUST NOT）** —— 表 T-076 の `EP-12` が操作の状態を描かないと定めており、どちらが追従中かは操作の状態である。<br>⭐ **`EP-6` が描くのは 2 本の線そのものである** |
```
新
```text
| DC-8 | どちらが追従中か | 追従している側を、表 T-023c の `SL-8` が線に定める規則と同じやり方で示すこと（MUST） —— 同行の理由もそのまま当てはまる。<br>⚠️ `DC-2` は押すたびに追従する側が入れ替わると定めるので、この印もそのたび入れ替わる。<br>⚠️ **この印を書き出しに出してはならない（MUST NOT）** —— 表 T-076 の `EP-12` が操作の状態を描かないと定めており、どちらが追従中かは操作の状態である。<br>⭐ **`EP-6` が描くのは 2 本の線そのものである** |
| DC-9 | カーソルの排他 | 本モードとガイドカーソル（表 T-029 の `CU-3` の `'crosshair'` と `'single-vertical'`）を、1 つの排他の選択とすること（MUST）。<br>本モードに入るときは、`S-66` を `'none'` にすること（MUST）。<br>本モードにいるあいだにガイドカーソルの入口を押したときは、本モードを出て（`DC-7` が 2 本を消す）、押した値を `S-66` に書くこと（MUST） —— 2 つの道具を同時に出すと、ポインタに追従する線がどちらのものか読めない。<br>⚠️ 基準日線（`CU-1`）はこの排他に入らない —— 文書のデータであって、測る道具ではない（`FR-046`） |
```

<!-- EDIT id=E-24 file=docs/spec/01-04-requirements.md -->
項目 12。旧
```text
文字の大きさは `_assets/tbl-settings.md` の `S-204` とする。
```
新
```text
文字の大きさは `_assets/tbl-settings.md` の 表 T-206 の `S-334` とし、同表の `S-340` を下回らせない。
```

<!-- EDIT id=E-25 file=docs/spec/01-04-requirements.md -->
項目 13。旧
```text
| CD-2 | 行（`TaskGroup`） | **その行に載っているすべての `Task`**（`CD-1` が各 `Task` に連鎖する）、配下の行（その行の子孫の `TaskGroup` すべて。<br>深さは問わない）、**その行を指す注記**（`CommentBox` / `HighlightBox`）、**およびその行を指すピン止め**（`_assets/tbl-settings.md` の `S-126`）—— 残すと、指す先の無い参照が文書に残る。<br>⚠️ **その行を指す表示位置（同 `S-78`）は消さず `null` へ戻す** —— `null` は「人がまだ場所を決めていない」を表す値であり、その扱いは表 T-024a の `OP-10` が持つ |
```
新
```text
| CD-2 | 行（`TaskGroup`） | **その行に載っているすべての `Task`**（`CD-1` が各 `Task` に連鎖する）、配下の行（その行の子孫の `TaskGroup` すべて。<br>深さは問わない）、**その行を指す注記**（`CommentBox` / `HighlightBox`）、**およびその行を指すピン止め**（`_assets/tbl-settings.md` の `S-126`）—— 残すと、指す先の無い参照が文書に残る。<br>⚠️ **その行を指す表示位置（同 `S-78`）は消さず `null` へ戻す** —— `null` は「人がまだ場所を決めていない」を表す値であり、その扱いは表 T-024a の `OP-10` が持つ |
| CD-6 | すべての行（行見出しパネルの頭の削除。表 T-051 の `HF-20`） | 深さ `L1` のすべての行に `CD-2` を当てた和 —— 文書のすべての `TaskGroup` と、それらに載るすべての `Task`（`CD-1` が連鎖する）、行を指す注記とピン止め。<br>⚠️ 行が 0 になるので、本表の後の段により、同じ操作の一部として深さ `L1` の行が 1 つ作られる（取り消し 1 回で戻る） |
```

<!-- EDIT id=E-26 file=docs/spec/01-04-requirements.md -->
項目 13。旧
```text
| HF-17 | `HF-10` の操作子の並びに、最も浅い段へ行を 1 つ足す操作子を 1 つ置くこと（MUST） —— **表 T-015 の `HR-8` を段 0 で押すものである。<br>**⭐ 足した行は最も浅い段の末子とすること（MUST）。<br>名前の扱いは `HF-14` に従う。<br>⛔ 足した行が描かれていないときは、その行が見える位置まで表示位置を送ること（MUST）。<br>打ち込み口だけを送ってはならない（MUST NOT） —— **口だけ送ると、確定したあとに行がどこへ行ったか読めない。<br>**⚠️ **送り方は `HF-9` に従う。<br>**⭐ **`HF-14`（配下に足す）も同じとすること（MUST）** —— **押した行が画面の下端に在るときに同じことが起きる。<br>**⛔ **本行が無いと、最も浅い段の行を作る道が画面から消える** —— `FR-085` は最上位の行を作れることを求めており、`HR-8` は足す先を配下と定めるので、**行が 1 つも無い文書では押す相手が存在しない** ⚠️ 本行で行を足すとき、段 0 が畳まれていれば（`S-211`）1 階層だけ開くこと（MUST）。<br>すべて開いてはならない（MUST NOT） —— 開かなければ、本行の MUST NOT（打ち込み口だけを送ってはならない）が破れる。<br>⭐ **すべて開けば `HF-10` と同じになり、人が畳んだ意思を捨てることになる。<br>** |
```
新
```text
| HF-17 | `HF-10` の操作子の並びに、最も浅い段へ行を 1 つ足す操作子を 1 つ置くこと（MUST） —— **表 T-015 の `HR-8` を段 0 で押すものである。<br>**⭐ 足した行は最も浅い段の末子とすること（MUST）。<br>名前の扱いは `HF-14` に従う。<br>⛔ 足した行が描かれていないときは、その行が見える位置まで表示位置を送ること（MUST）。<br>打ち込み口だけを送ってはならない（MUST NOT） —— **口だけ送ると、確定したあとに行がどこへ行ったか読めない。<br>**⚠️ **送り方は `HF-9` に従う。<br>**⭐ **`HF-14`（配下に足す）も同じとすること（MUST）** —— **押した行が画面の下端に在るときに同じことが起きる。<br>**⛔ **本行が無いと、最も浅い段の行を作る道が画面から消える** —— `FR-085` は最上位の行を作れることを求めており、`HR-8` は足す先を配下と定めるので、**行が 1 つも無い文書では押す相手が存在しない** ⚠️ 本行で行を足すとき、段 0 が畳まれていれば（`S-211`）1 階層だけ開くこと（MUST）。<br>すべて開いてはならない（MUST NOT） —— 開かなければ、本行の MUST NOT（打ち込み口だけを送ってはならない）が破れる。<br>⭐ **すべて開けば `HF-10` と同じになり、人が畳んだ意思を捨てることになる。<br>** |
| HF-20 | `HF-10` の操作子の並びに、すべての行を消す操作子を 1 つ置くこと（MUST） —— 行ごとの削除（表 T-109 の `IC-82`）を段 0 で押すものであり、入口は同表の `IC-106` である。<br>⭐ 置き場は `HF-17`（最も浅い段へ足す）の上とすること（MUST） —— `HF-4` が行について定める「消すを上、足すを下」の縦の対と同じ読み方になる。<br>⭐ 消える範囲は 表 T-050 の `CD-6`、問い方は 表 T-037 の `NT-7` と `FR-032` に従うこと（MUST）（示す文は 表 T-234 の `QN-10`） |
```

<!-- EDIT id=E-27 file=docs/spec/01-04-requirements.md -->
項目 13。旧
```text
| QN-9 | **透かしを消す前** | 挙げない —— 消えるものは無い | `FR-020` |
```
新
```text
| QN-9 | **透かしを消す前** | 挙げない —— 消えるものは無い | `FR-020` |
| QN-10 | すべての行を削除する前（表 T-051 の `HF-20`） | 挙げる —— 消える `Task` の名前（`QN-1` と同じ） | `FR-032` |
```

<!-- EDIT id=E-28 file=docs/spec/01-04-requirements.md -->
項目 14, 15。旧
```text
**画面上でタスクを動かしたときは、プロパティの日付を追随させること（MUST）** —— 同じ値を 2 つの面から編集できる以上、片方だけが動く状態を作らない。
```
新
```text
**画面上でタスクを動かしたときは、プロパティの日付を追随させること（MUST）** —— 同じ値を 2 つの面から編集できる以上、片方だけが動く状態を作らない。
⭐ 日付の欄（表 T-016 の入力の型が `日付` の行）を編集しているあいだに `Delete` か `Backspace` を押したときは、欄の字をすべて消して空にすること（MUST） —— 1 字ずつ消すと、途中の字が日付として読めない。  
空のまま確定したときは、その列が `null` を許す列（`_source/erd.json` の `nullable`）なら `null` を書き、許さない列（`start` / `finish`）なら何も書かずに欄を元の値へ戻すこと（MUST）。  
⚠️ これは欄の中の編集であり、表 T-028 の `IN-5a`（確定していないあいだはショートカットが効かない）とは別のことである。  
⭐ 色の行（表 T-016 の入力の型に `色` を含む行）は、同じ対象の行の並びの末尾に置くこと（MUST） —— 日付と名前を先に読ませ、見た目の設定を後ろへ寄せる。  
```

<!-- EDIT id=E-29 file=docs/spec/01-04-requirements.md -->
項目 16。旧
```text
| CV-9 | 色の欄 | プロパティパネルの色の欄は、表 T-294 の名とカスタムカラーの入口を並べて選ばせること（MUST）。<br>並べ方は試作の色の選び（`previous-project-result/08-poc/poc-integrated.html` の `selHue`、`:940` / `:2777-2780`）に倣い、名を 1 列に表 T-294 の行の順で並べ、カスタムカラーの入口をその後に置く。<br>名の見本は、その欄が描く形（`CV-6`）の、いま描いている明暗の値で塗ること（MUST） —— 見本と描かれる色が食い違わない。<br>⭐ 欄には、選んでいる色の明るいテーマと暗いテーマの見本を並べて示すこと（MUST）（例: `ライト ■ / ダーク ■（ライトと同じ）`）。<br>未定義の側は、`CV-3` で描く値の見本に、どちらの側と同じかを添える。<br>ハイライトボックスの枠の欄には透明を並べない（`FR-019`）。<br>行の色の欄には黒を並べない（表 T-294 の `S-315`）。<br>語は `FR-038` の辞書が持つ |
```
新
```text
| CV-9 | 色の欄 | プロパティパネルの色の欄は、表 T-294 の名とカスタムカラーの入口を並べて選ばせること（MUST）。<br>⭐ 並べ方は、透明を除く名を同表の行の順に、1 段に `_assets/tbl-settings.md` の 表 T-206 の `S-338` 個ずつ並べ、その下の段にカスタムカラーの入口、透明の順に置くこと（MUST）。<br>⭐ 閲覧環境の色の入力（`input type=color`）は、カスタムカラーの入口を押したときにだけ出すこと（MUST） —— 常に出すと、並べた名と入力のどちらで選ぶのかが読めない。<br>その欄に並べない名（下の 2 つ）の場所は空けたままとし、後ろの名を詰めてはならない（MUST NOT） —— 欄によって同じ色の場所が変わると、場所で覚えられない。<br>名の見本は、その欄が描く形（`CV-6`）の、いま描いている明暗の値で塗ること（MUST） —— 見本と描かれる色が食い違わない。<br>⭐ 欄には、選んでいる色の明るいテーマの側と暗いテーマの側を 1 行に並べ、側ごとに見本と値を示すこと（MUST）（例: `ライト: ■ #AAAAAA / ダーク: ■ #050505`）。<br>値は、`#rrggbb` の側は英大文字の 16 進、名の側はその名の語とする。<br>⭐ 透明の側は、値を透明の語とし、見本を市松で示すこと（MUST） —— 1 辺を 表 T-206 の `S-335` 個のますに割り、ますを 表 T-236 の `S-336` と `S-337` で交互に塗る。<br>⭐ 未定義の側は、値を空け、見本を縁だけの破線で示し、`CV-3` で描く値がどちらの側と同じかを語で添えること（MUST）。<br>ハイライトボックスの枠の欄には透明を並べない（`FR-019`）。<br>行の色の欄には黒を並べない（表 T-294 の `S-315`）。<br>語は `FR-038` の辞書が持つ |
```

<!-- EDIT id=E-30 file=docs/spec/05-07-design.md -->
項目 5。旧
```text
⭐ 走らせるのは文書を組み立てる側（Chapter 5.2 の `CP-20`）とする（MUST） —— 列の形が違うものは、文書になる前に落ちるべきだからである。  
```
新
```text
⭐ 走らせるのは文書を組み立てる側（Chapter 5.2 の `CP-20`）とする（MUST） —— 列の形が違うものは、文書になる前に落ちるべきだからである。  
⭐ スキーマを走らせる前に、表 T-297 の列を捨てて読むこと（MUST） —— 仕様が落とした列を、保存済みの文書はまだ持っている。  
捨てた列を通知してはならず、書き戻してもならない（MUST NOT） —— 読む人の側に直せることが何も無い。  

**表 T-297 — 退役した列（読むときに捨てる）**

| 行 ID | 実体 | 列 | 捨てる理由 |
| --- | --- | --- | --- |
| RK-1 | `TaskVisual` | `nameAnchor` | 名称ラベルの位置を人が指定する手段を持たない（`FR-002`） |
| RK-2 | `TaskVisual` | `nameAlign` | 同上 |

```

<!-- EDIT id=E-31 file=docs/spec/_assets/tbl-glossary.md -->
項目 6。旧（行ごと消す（E-32 が `IC-16` の前へ置き直す））
```text
| IC-100 | `Command Palette` | 表示 | モノクロで描く・色に戻す（`S-74`）。<br>⚠️ **明暗テーマ（`IC-16`）とは別の値である** —— 理由は `FR-041` が持つ。<br>⛔ **`FR-049` の切り替えではない** —— `S-74` は 表 T-203 の行であり、同要求が見るのは 表 T-202 の真偽の行である | `FR-041`（表 T-108 の `CM-64`）| — |
```
新（空 —— 旧の行を消す）
```text

```

<!-- EDIT id=E-32 file=docs/spec/_assets/tbl-glossary.md -->
項目 6。旧
```text
| IC-16 | `App Header` | 表示 | 明暗テーマを選ぶ（`S-72`）| `FR-039` | — |
```
新
```text
| IC-100 | `App Header` | 表示 | モノクロで描く・色に戻す（`S-74`）。<br>⚠️ **明暗テーマ（`IC-16`）とは別の値である** —— 理由は `FR-041` が持つ。<br>⛔ **`FR-049` の切り替えではない** —— `S-74` は 表 T-203 の行であり、同要求が見るのは 表 T-202 の真偽の行である。<br>⭐ 本行は明暗テーマの入口（`IC-16`）の左に並べる（`FR-041`） | `FR-041`（表 T-108 の `CM-64`）| — |
| IC-16 | `App Header` | 表示 | 明暗テーマを選ぶ（`S-72`）| `FR-039` | — |
```

<!-- EDIT id=E-33 file=docs/spec/_assets/tbl-glossary.md -->
項目 13。旧
```text
| IC-82 | `Row Title Panel` | — | 行を削除する。<br>⭐ 消える範囲は 表 T-050 の `CD-2` が持ち、問い方は 表 T-037 の `NT-7` が持つ（示す文は 表 T-234 の `QN-1`）| `FR-032` | — |
```
新
```text
| IC-82 | `Row Title Panel` | — | 行を削除する。<br>⭐ 消える範囲は 表 T-050 の `CD-2` が持ち、問い方は 表 T-037 の `NT-7` が持つ（示す文は 表 T-234 の `QN-1`）| `FR-032` | — |
| IC-106 | `Row Title Panel` | — | すべての行を削除する（行見出しパネルの頭。表 T-051 の `HF-20`）。<br>⭐ 図形は `IC-82` と同じである。<br>消える範囲は 表 T-050 の `CD-6` が持ち、問い方は 表 T-037 の `NT-7` が持つ（示す文は 表 T-234 の `QN-10`）| `FR-032` | — |
```

<!-- EDIT id=E-34 file=docs/spec/_assets/tbl-glossary.md -->
項目 5。旧（行ごと消す）
```text
| P-20 | `nameAnchor` / `nameAlign` | 名称アンカー / 名称の揃え |
```
新（空 —— 旧の行を消す）
```text

```

<!-- EDIT id=E-35 file=docs/spec/_assets/tbl-glossary.md -->
項目 5。旧（行ごと消す。欠番は残す）
```text
| CM-25 | `TaskVisual` | `setTaskVisualNamePlacement` | ⭐ | 名称ラベルの位置を置く | `FR-002` |
```
新（空 —— 旧の行を消す）
```text

```

### 4.2 原稿 JSON の編集（`_source/`）

⛔ 各塊は、当てる前に原稿を `json.loads` で読み、名指した項が 1 つだけ在ることを確かめてから当てること。当てた後は `npm run gen` の前に、手で書いた検証の形（`*.schema.json`）で確かめること。

J-01（項目 4）`settings.json` の `S-63` —— `default` だけを変える。旧 `{"lit": "true", "code": true}` → 新 `{"lit": "false", "code": true}`
```json
{
 "id": "S-63",
 "key": "`progressMarkerVisible`",
 "type": {
  "ja": "真偽"
 },
 "default": {
  "lit": "false",
  "code": true
 },
 "note": {
  "ja": "進捗マーカー（`FR-013`）と再開アイコン（`FR-044`）。寸法をズームに追随させない規則は `FR-094` が持つ"
 }
}
```

J-02（項目 11）`settings.json` の `S-163` を置き換える。旧:

```json
{
 "id": "S-163",
 "name": {
  "ja": "基準日線の色"
 },
 "light": {
  "colour": "#8b9099",
  "code": true
 },
 "dark": {
  "colour": "#767c86",
  "code": true
 },
 "hue": {
  "ja": "—"
 },
 "note": {
  "ja": "基準日"
 }
}
```

新
```json
{
 "id": "S-163",
 "name": {
  "ja": "基準日線の色"
 },
 "light": {
  "colour": "#eb6101",
  "code": true
 },
 "dark": {
  "colour": "#ff8c42",
  "code": true
 },
 "hue": {
  "ja": "—"
 },
 "note": {
  "ja": "基準日。⭐ **朱色である** —— 本表のほかの線の色（`S-160` のイナズマ線、`S-195` のカーソル）と見分ける。⛔ **測って決めた値ではない**"
 }
}
```

J-03（項目 12）`settings.json` の `S-195` を置き換える。旧:

```json
{
 "id": "S-195",
 "name": {
  "ja": "2 連カーソルの色"
 },
 "light": {
  "colour": "hsl(H 59% 32%)",
  "code": true
 },
 "dark": {
  "colour": "hsl(H 62% 68%)",
  "code": true
 },
 "hue": {
  "ja": "○"
 },
 "note": {
  "ja": "2 本とも同じ色である —— どちらが追従中かは色で示さない（表 T-029a の `DC-8` が 表 T-023c の `SL-8` を引き、同行は線について太さだけを変えると定める）。⭐ **`S-151` を継ぐ** —— 本表が `S-151` を選択と現在位置の色としており、`CU-2` は読む人が置いた現在位置である。⛔ **明度は `S-151` と同じ 32% である**。 42% では `NFR-007` の 3 : 1 を割る色相が在る。⛔ **`S-163` を継がない** —— 基準日線と 2 連カーソルは同時に出るので、同じ色だとどちらの線か読めない"
 }
}
```

新
```json
{
 "id": "S-195",
 "name": {
  "ja": "カーソルの色（表 T-029 の `CU-2` ・ `CU-3`）"
 },
 "light": {
  "colour": "#c2188f",
  "code": true
 },
 "dark": {
  "colour": "#f07ad0",
  "code": true
 },
 "hue": {
  "ja": "—"
 },
 "note": {
  "ja": "⛔ **テーマの色相に追随させない** —— マゼンタ寄りの色であり、選択と現在位置の色（`S-151`）と見分ける。2 本とも同じ色である —— どちらが追従中かは色で示さない（表 T-029a の `DC-8` が 表 T-023c の `SL-8` を引き、同行は線について太さだけを変えると定める）。⭐ ガイドカーソル（`CU-3`）も本行で描く —— 2 つは排他であり（表 T-029a の `DC-9`）、同時には出ない。⛔ **測って決めた値ではない**"
 }
}
```

J-04（項目 12）`settings.json` の `S-194` を置き換える（名を広げ、`S-178` の旧い備考を指す 1 文を消す）。旧:

```json
{
 "id": "S-194",
 "value": {
  "ja": "2 連カーソルの線の太さ（表 T-029 の `CU-2`）"
 },
 "default": {
  "num": "1",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "`S-138` と同じ理由で保存しない。⭐ **`S-178` を掛ける前の、その線自身の太さである** —— 掛ける規則は 表 T-029a の `DC-8` が 表 T-023c の `SL-8` から引く。⚠️ `S-178` の備考が「2 連カーソルは太さの行そのものを持たない」と書いているのは本行の不在を指しており、本行が埋める。 ⛔ **測って決めた値ではない** （🔎）"
 }
}
```

新
```json
{
 "id": "S-194",
 "value": {
  "ja": "カーソルの線の太さ（表 T-029 の `CU-2` ・ `CU-3`）"
 },
 "default": {
  "num": "1",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "`S-138` と同じ理由で保存しない。⭐ **`S-178` を掛ける前の、その線自身の太さである** —— 掛ける規則は 表 T-029a の `DC-8` が 表 T-023c の `SL-8` から引く。⛔ **測って決めた値ではない** （🔎）"
 }
}
```

J-05（項目 11・12）`settings.json` の `S-178` の `note` を置き換える。旧:

```json
{
 "id": "S-178",
 "value": {
  "ja": "選択された線の太さの倍率（表 T-023c の `SL-8`、表 T-029a の `DC-8`）"
 },
 "default": {
  "num": "2",
  "suffix": "×",
  "mark": "🔎"
 },
 "note": {
  "ja": "同上。⭐ **絶対値ではなく、その線自身の太さに掛ける倍率である** —— 依存線は `S-18` を持つが、基準日線と 2 連カーソルは太さの行そのものを持たない。⛔ **測って決めた値ではない** （🔎）"
 }
}
```

新
```json
{
 "id": "S-178",
 "value": {
  "ja": "選択された線の太さの倍率（表 T-023c の `SL-8`、表 T-029a の `DC-8`）"
 },
 "default": {
  "num": "2",
  "suffix": "×",
  "mark": "🔎"
 },
 "note": {
  "ja": "同上。⭐ **絶対値ではなく、その線自身の太さに掛ける倍率である** —— 依存線は `S-18`、基準日線は `S-333`、カーソルは `S-194` を、線自身の太さとして持つ。⛔ **測って決めた値ではない** （🔎）"
 }
}
```

J-06（項目 5）`settings.json` の `S-196` の `note` を置き換える。旧:

```json
{
 "id": "S-196",
 "value": {
  "ja": "線だけの形の 1 段目と予定の線のあいだ（表 T-271 の `XS-5`）"
 },
 "default": {
  "num": "2",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "⭐ **1 段目の下端（字の大きさの箱の下端）と、予定の線の上の縁とのあいだの隙間である** —— 描いた字の箱ではなく、字の大きさで数える。⛔ どの形がこの隙間を使うかは 表 T-271 が持ち、本行は持たない。 ⚠️ **保存しない理由は `S-138` のそれとは違う** —— `S-138` は「書き出しに入口を描かない」ことを根拠にするが、**名称ラベルは 表 T-076 の `EP-5` により書き出しに入る。** ⭐ 本行を保存しないのは、文書がラベルのアンカー（`PR-13`）を持ち、形状の種類から決まる隙間のほうは持たないからである。 ⛔ **測って決めた値ではない** （🔎）"
 }
}
```

新
```json
{
 "id": "S-196",
 "value": {
  "ja": "線だけの形の 1 段目と予定の線のあいだ（表 T-271 の `XS-5`）"
 },
 "default": {
  "num": "2",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "⭐ **1 段目の下端（字の大きさの箱の下端）と、予定の線の上の縁とのあいだの隙間である** —— 描いた字の箱ではなく、字の大きさで数える。⛔ どの形がこの隙間を使うかは 表 T-271 が持ち、本行は持たない。 ⚠️ **保存しない理由は `S-138` のそれとは違う** —— `S-138` は「書き出しに入口を描かない」ことを根拠にするが、**名称ラベルは 表 T-076 の `EP-5` により書き出しに入る。** ⭐ 本行を保存しないのは、隙間が形状の種類から決まり、文書が名称ラベルの位置を持たないからである。 ⛔ **測って決めた値ではない** （🔎）"
 }
}
```

J-07（項目 1・2・8・10・11・12・16）`settings.json` —— `S-297`（表 T-206 の末の行）の後ろに、次の 12 行をこの順に足す（`S-339` ・ `S-340` は `JDG-396` ・ `JDG-398` を畳み込んだもの）
```json
[
 {
  "id": "S-325",
  "value": {
   "ja": "名称ラベルに添える予定日の文字の大きさの、名前の字の大きさに対する係数（`FR-002`、表 T-251）"
  },
  "default": {
   "num": "0.85"
  },
  "note": {
   "ja": "見せ方の比であり、日程の内容ではないので保存しない。⭐ **px ではなく係数である** —— 名前の字は表示の倍率と縦の倍率に従うので、日付の字もそれに付いて行く。⭐ 掛ける範囲は予定日の文字と、その前に置く半角空白である（`FR-002`）"
  }
 },
 {
  "id": "S-328",
  "value": {
   "ja": "遅れの記号 `(!)` の縦棒の太さの、`S-24` に対する係数（`FR-013`）"
  },
  "default": {
   "num": "1.7",
   "mark": "🔎"
  },
  "note": {
   "ja": "見せ方の比であり、日程の内容ではないので保存しない。⭐ 記号の形は `FR-094` の宣言した寸法だけで描く —— 本行と `S-329` 〜 `S-331` がその宣言である。⛔ **測って決めた値ではない** （🔎）"
  }
 },
 {
  "id": "S-329",
  "value": {
   "ja": "遅れの記号 `(!)` の縦棒の下端の位置 —— 記号の半分の高さに対する比、中心から下へ（`FR-013`）"
  },
  "default": {
   "num": "0.15",
   "mark": "🔎"
  },
  "note": {
   "ja": "見せ方の比であり、日程の内容ではないので保存しない。⭐ 縦棒と点のあいだの隙間は本行で開く —— 小さくすると隙間が広がる。⛔ **測って決めた値ではない** （🔎）"
  }
 },
 {
  "id": "S-330",
  "value": {
   "ja": "遅れの記号 `(!)` の点の中心の位置 —— 記号の半分の高さに対する比、中心から下へ（`FR-013`）"
  },
  "default": {
   "num": "0.8",
   "mark": "🔎"
  },
  "note": {
   "ja": "見せ方の比であり、日程の内容ではないので保存しない。⛔ **測って決めた値ではない** （🔎）"
  }
 },
 {
  "id": "S-331",
  "value": {
   "ja": "遅れの記号 `(!)` の点の半径 —— マーカーの円の半径に対する比（`FR-013`）"
  },
  "default": {
   "num": "0.14",
   "mark": "🔎"
  },
  "note": {
   "ja": "見せ方の比であり、日程の内容ではないので保存しない。⛔ **測って決めた値ではない** （🔎）"
  }
 },
 {
  "id": "S-332",
  "value": {
   "ja": "全体表示で横に残す余白の、`Row Area` の幅に対する比（片側。`FR-055`）"
  },
  "default": {
   "num": "0.025"
  },
  "note": {
   "ja": "全体表示の見せ方であり、日程の内容ではないので保存しない。⭐ **左右それぞれに取る** —— 両側で `Row Area` の幅の 2 倍の比になる"
  }
 },
 {
  "id": "S-333",
  "value": {
   "ja": "基準日線の太さ（表 T-029 の `CU-1`、`FR-046`）"
  },
  "default": {
   "num": "3",
   "suffix": "px"
  },
  "note": {
   "ja": "線の太さは画面の見せ方であり、日程の内容ではないので保存しない。⭐ **`S-178` を掛ける前の、その線自身の太さである** —— 選ばれた基準日線は本行に `S-178` を掛けて描く（表 T-023c の `SL-8`）"
  }
 },
 {
  "id": "S-334",
  "value": {
   "ja": "`Dual Cursor` の読み出しの文字の大きさの係数（表 T-029a の `DC-3`）"
  },
  "default": {
   "num": "0.4375"
  },
  "note": {
   "ja": "⛔ **px で持たない理由は `S-204` と同じである。** ⭐ `S-204`（0.875）の半分である —— 読み出しはポインタのそばに出て日程を覆うので、ツールチップより小さくする。⚠️ `S-204` から導かず、本行が値を持つ —— ツールチップの字を変えても読み出しの字は動かない。⭐ 下限は `S-340`"
  }
 },
 {
  "id": "S-339",
  "value": {
   "ja": "ツールチップと窓の縁のあいだに残す余白（表 T-028 の `IN-7`）"
  },
  "default": {
   "num": "8",
   "suffix": "px",
   "mark": "🔎"
  },
  "note": {
   "ja": "説明の見せ方であり、日程の内容ではないので保存しない。⭐ 説明の幅の上限は、窓の幅から両側に本行を引いた幅である —— 描き手はこれを窓の幅に従う書き方（例: `calc(100vw - 2 × 本行)`）で持ち、窓の大きさを読みに行かない。⛔ **測って決めた値ではない** （🔎）"
  }
 },
 {
  "id": "S-340",
  "value": {
   "ja": "`Dual Cursor` の読み出しの文字の大きさの下限（表 T-029a の `DC-3`）"
  },
  "default": {
   "num": "10",
   "suffix": "px"
  },
  "note": {
   "ja": "⭐ `S-334` を掛けた大きさが本行を下回るときは、本行の大きさで描く —— 宿主の地の文字が 16px のとき `S-334` だけでは 7px になり読めない（`JDG-398`）。⚠️ px で持つのは、下限は読める字の大きさの絶対の床だからである"
  }
 },
 {
  "id": "S-335",
  "value": {
   "ja": "透明の見本の市松の、1 辺のますの数（表 T-017b の `CV-9`）"
  },
  "default": {
   "num": "2"
  },
  "note": {
   "ja": "欄の見せ方であり、日程の内容ではないので保存しない。⭐ 見本は `S-335` × `S-335` のますに割る"
  }
 },
 {
  "id": "S-338",
  "value": {
   "ja": "プロパティパネルの色の欄の、1 段に並べる見本の数（表 T-017b の `CV-9`）"
  },
  "default": {
   "num": "5"
  },
  "note": {
   "ja": "欄の見せ方であり、日程の内容ではないので保存しない。⭐ パレットの 10 色（表 T-294 の、透明を除く名）が 2 段に並ぶ"
  }
 }
]
```

J-08（項目 2・16）`settings.json` —— `S-223`（表 T-236 の末の行）の後ろに、次の 4 行をこの順に足す
```json
[
 {
  "id": "S-326",
  "name": {
   "ja": "遅れのマーカーの地の色（`FR-013`）"
  },
  "light": {
   "colour": "#ffd400",
   "code": true
  },
  "dark": {
   "colour": "#e6c229",
   "code": true
  },
  "hue": {
   "ja": "—"
  },
  "note": {
   "ja": "表 T-021 の `PM-4`。⭐ **黄である** —— 遅れを最も見つけたい（`UC-006`）。⛔ **テーマの色相に追随させない。** ⛔ **測って決めた値ではない**"
  }
 },
 {
  "id": "S-327",
  "name": {
   "ja": "遅れのマーカーの記号の色（`FR-013`）"
  },
  "light": {
   "colour": "#16181d",
   "code": true
  },
  "dark": {
   "colour": "#16181d",
   "code": true
  },
  "hue": {
   "ja": "—"
  },
  "note": {
   "ja": "⭐ **両テーマで同じ暗い色である** —— 地（`S-326`）が両テーマで明るい黄なので、`S-161` の暗いテーマの明るい記号色では `!` が地に沈む"
  }
 },
 {
  "id": "S-336",
  "name": {
   "ja": "透明の見本の市松の明るいます（表 T-017b の `CV-9`）"
  },
  "light": {
   "colour": "#ffffff",
   "code": true
  },
  "dark": {
   "colour": "#ffffff",
   "code": true
  },
  "hue": {
   "ja": "—"
  },
  "note": {
   "ja": "⭐ 両テーマで同じ色である —— 市松は「透けている」ことを示す約束の絵であり、地の色に合わせると透明に見えない"
  }
 },
 {
  "id": "S-337",
  "name": {
   "ja": "透明の見本の市松の暗います（表 T-017b の `CV-9`）"
  },
  "light": {
   "colour": "#c0c0c0",
   "code": true
  },
  "dark": {
   "colour": "#c0c0c0",
   "code": true
  },
  "hue": {
   "ja": "—"
  },
  "note": {
   "ja": "`S-336` に同じ"
  }
 }
]
```

J-09（項目 5・15）`property-items.json` —— `PR-13` の項を消し、項の並びを次の順にする（中身は変えない）。旧の順: `PR-1 PR-3 PR-16 PR-4 PR-5 PR-6 PR-9 PR-10 PR-2 PR-7 PR-8 PR-17 PR-12 PR-13 PR-14 PR-15 PR-18 PR-19 PR-20 PR-21`
```json
["PR-1", "PR-3", "PR-16", "PR-4", "PR-5", "PR-6", "PR-9", "PR-10", "PR-2", "PR-7", "PR-8", "PR-17", "PR-14", "PR-15", "PR-12", "PR-18", "PR-20", "PR-19", "PR-21"]
```

J-10（項目 5）`erd.json` —— `TaskVisual` の列から、次の 2 つ（`seat` 98 と 99。`AT-98` / `AT-99`）を消す。欠番は残す
```json
[
 {
  "seat": 98,
  "name": "nameAnchor",
  "type": "整数（0〜8）",
  "nullable": "可",
  "json": {
   "kind": "integer",
   "min": 0,
   "max": 8,
   "null": true
  },
  "key": "",
  "origin": "GRS",
  "exchange": "",
  "meaning": {
   "ja": "名前を置く位置。**外接矩形の 9 点を読み順に数える** —— `0` 左上 / `1` 上中央 / `2` 右上 / `3` 左中央 / `4` 中央 / `5` 右中央 / `6` 左下 / `7` 下中央 / `8` 右下。⚠️ **指定が無いときの置き方は `FR-109` が決める**（表 T-272 と 表 T-273）—— そちらは自動配置の規則であって、この格子ではない"
  }
 },
 {
  "seat": 99,
  "name": "nameAlign",
  "type": "列挙（3 値）",
  "nullable": "可",
  "json": {
   "kind": "enum",
   "values": [
    "left",
    "center",
    "right"
   ],
   "null": true
  },
  "key": "",
  "origin": "GRS",
  "exchange": "",
  "meaning": {
   "ja": "名前の揃え"
  }
 }
]
```

J-11（項目 5）`display-words.json` の節 `properties` —— 次の項を消す
```json
{
 "rowId": "PR-13",
 "label": {
  "ja": "名称アンカー / 名称の揃え",
  "en": "nameAnchor / nameAlign"
 }
}
```

J-12（項目 13）`display-words.json` の節 `icons` —— `IC-82` の項の後ろに足す
```json
{
 "rowId": "IC-106",
 "label": {
  "ja": "すべての行を削除",
  "en": "Delete All Rows"
 },
 "hint": {
  "ja": "すべての行と、載っているタスクを削除する",
  "en": "Delete all rows and the tasks they carry"
 }
}
```

J-13（項目 13）`display-words.json` の節 `questions` —— `QN-9` の項の後ろに足す
```json
{
 "rowId": "QN-10",
 "text": {
  "ja": "すべての行と、行に載っているタスクを削除しますか？",
  "en": "Delete all rows and the tasks on them?"
 }
}
```

J-14（項目 16）`display-words.json` の節 `colourField` の `custom` —— 旧 `{"ja": "カスタムカラー", "en": "Custom color"}` → 新
```json
{
 "part": "custom",
 "text": {
  "ja": "任意",
  "en": "Custom"
 }
}
```

J-15（項目 12）`state-machines.json` の `dualCursorModeStateMachine` —— `transitions.dualCursorEntryPressed` を次に置き換え（`off` の `effect` の名を変え、`DC-9` を根拠に足す）、`guideCursorEntryPressed` を足す。旧の `dualCursorEntryPressed`:

```json
{
 "off": {
  "to": "on",
  "guard": [
   {
    "name": "hasDaysToPlace"
   }
  ],
  "effect": "writePlaceDualCursor",
  "evidence": [
   "DC-1"
  ]
 },
 "on": {
  "to": "off",
  "effect": "writeClearDualCursor",
  "evidence": [
   "DC-4"
  ]
 }
}
```

新
```json
{
 "dualCursorEntryPressed": {
  "off": {
   "to": "on",
   "guard": [
    {
     "name": "hasDaysToPlace"
    }
   ],
   "effect": "writePlaceDualCursorClearingGuide",
   "evidence": [
    "DC-1",
    "DC-9"
   ]
  },
  "on": {
   "to": "off",
   "effect": "writeClearDualCursor",
   "evidence": [
    "DC-4"
   ]
  }
 },
 "guideCursorEntryPressed": {
  "on": {
   "to": "off",
   "effect": "writeClearDualCursorSettingGuide",
   "evidence": [
    "DC-4",
    "DC-7",
    "DC-9"
   ]
  }
 }
}
```

J-16（項目 5）`row-id-prefixes.json` —— 次の項を、字の順の位置（`RL` の前）に足す
```json
{
 "prefix": "RK",
 "words": "Retired Key",
 "owner": "spec",
 "means": {
  "ja": "仕様が落とし、読むときに捨てる列"
 }
}
```

J-17（項目 7）`row-id-prefixes.json` の `RR` —— 旧 `{"ja": "担当者の一覧の字の大きさ・スクロール・罫線"}` → 新
```json
{
 "prefix": "RR",
 "words": "Resource Roster",
 "owner": "spec",
 "means": {
  "ja": "担当者の一覧の字の大きさ・スクロール・罫線・閉じる入口"
 }
}
```

### 4.3 生成器と生成物（形で示す）

| 何 | 何をする |
|---|---|
| `npm run gen` | 上の原稿から `_assets/tbl-settings.md` ・ `tbl-property-items.md` ・ `fig-erd-overview.md` ・ `fig-erd-detail.md` ・ `tbl-row-id-prefixes.md` ・ `tbl-state-machines.md` ・ `_source/grs-document.schema.json` と、`src/` の生成物（`SETTINGS_DEFAULTS` ・ `display-words.json` ・ `property-items.json` ・ `icon-roster.json` ・ `help-roster.json` ・ 状態機械の型 ・ スキーマの検証器）を刷る |
| `tools/generate_icon_glyphs.py` と 図 F-019（`_assets/fig-icons.svg`） | `IC-106` の図形は `IC-82` と同じ ×。生成器が行ごとの図形を要るなら、`IC-82` の図形を `IC-106` にも引く形で足す（⚠️ 新しい図形は起こさない） |
| `tools/generate_startup_template.py` ・ `sample-schedule/Three-Year Product Plan.json` ・ `src/framework/single-html-shell/startup-template.json` | 2 列（各 74 か所）を消す。見本の文書は表 T-297 で読めるが、残すと書き出すたびに捨てられる列を配ることになる |
| `docs/guides/schedule-to-grs-json/prompt-ja.md` ・ `prompt-en.md` | 2 列（各 6 か所）を消す —— AI に書かせる文書の形から落とす |
| `tools/generate_entity_types.py` ・ `tools/generate_json_schema_validator.py` | 2 列の名を 1 か所ずつ持つ。生成器が列を原稿から読むなら直す所は無い。名を手で持っているなら消す |

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | 仕様 ↔ 配置（`schedule-layout.ts`） | 予定日の字の大きさ ＝ 名前の字の大きさ × `settings` の `S-325` の値。「入る」と `OC-1` の幅 ＝ 名前の幅（名前の字）＋ 半角空白と予定日の幅（× `S-325`）。年は `ND-5` の下 2 桁 |
| S-2 | 配置 ↔ 描き手（`schedule-task-figures.ts`） | 名称ラベルは 2 つの `<tspan>`（名前と予定日）で描き、予定日の側の `font-size` を × `S-325` にする。配置が返す箱の幅は S-1 の和 |
| S-3 | 仕様 ↔ 全体表示（`fitZoom`） | 返す `zoomX` と `scrollDate` ／ `scrollDayOffset` ／ `scrollGroupId` は、返す `zoomY` と深さで描いた 1 つの絵から決める。余白は片側 `Row Area.width × S-332`。`scrollDayOffset` は日の中の比（0 以上 1 未満） |
| S-4 | 全体表示 ↔ 殻（`frame-loop.ts:1645-1656`） | `fitZoom` が返す `scrollDayOffset` をそのまま書く（いまの `scrollDayOffset: 0` を置き換える） |
| S-5 | 仕様 ↔ 状態機械 | 出来事 `guideCursorEntryPressed`（`on` → `off`、効果 `writeClearDualCursorSettingGuide`）。`dualCursorEntryPressed` の `off` → `on` の効果は `writePlaceDualCursorClearingGuide`（`S-66` を `'none'` にする書き込みを含む） |
| S-6 | 殻 ↔ 入口（`input-command-translator.ts:694` 付近の `IC-47` ・ `IC-48`） | ガイドカーソルの入口を押したとき、`Dual Cursor` のモードにいれば `guideCursorEntryPressed` を先に起こし、そのあと `S-66` を書く |
| S-7 | 仕様 ↔ 基準日線 | 出す命令のあとに `scrollDate` ／ `scrollDayOffset` を、基準日線の x が `Row Area` の横の中点に来る値へ書く。倍率が保存されているときは `zoomX` ・ `zoomY` ・ `scrollGroupId` を書かない。⭐ 全体表示で描かれていて倍率が保存されていないときは、いま描いている `zoomX` ・ `zoomY` と最上行を、値を変えずに同じ束で書く（「倍率はそのまま」—— 値を変えずに書けば倍率は変わらない。波 2 で前に立つ者が継ぎ目を正した）。ポインタ: `GR-16` と `GR-22` は `PK-10`（`col-resize`） |
| S-8 | 仕様 ↔ パネル（`properties-panel-drawing.ts`） | 1 側の示し方 ＝ 見本 ＋ 値。値は `#RRGGBB`（大文字）／ 名の語 ／ 透明の語 ／ 空。見本は塗り ／ 市松（`S-335` ・ `S-336` ・ `S-337`）／ 破線の縁 |
| S-9 | 仕様 ↔ 読み込み（`json-codec.ts`） | 検証の前に `TaskVisual` の各項から `nameAnchor` と `nameAlign` の鍵を消す（表 T-297）。通知しない |
| S-10 | 仕様 ↔ ツールチップ（`tooltips-drawing.ts` ・ `dom-screen-surface.ts` の `STYLE.tooltip`） | 説明の箱は `width:max-content` ・ `max-width:calc(100vw - 2 × S-339 px)` ・ 行は折り返さない書き方で、上限を超える行だけ折り返す。出した瞬間に 1 度だけ測り、窓の右端 − `S-339` を越えるなら右端を対象の右端へ、それでも左端が `S-339` より左なら左端を `S-339` へ。窓の大きさを定期に読まない（`resize` の監視も置かない） |
| S-11 | 仕様 ↔ パネル（`properties-panel-drawing.ts`） | `input type=color` は、[任意]（`colourField` の `custom`）を押したときにだけ描く。押す前は描かない（`JDG-397`） |
| S-12 | 仕様 ↔ 読み出し（`tooltips-drawing.ts` の `readoutStyle`） | 字の大きさ ＝ `max(S-340 px, S-334 em)`（`JDG-398`） |

---

## 6. グラフ（`73c2bc90` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 対象 | 要求 / 参照 | 指している要求 |
|---|---|---|
| `ND-5` ・ `FD-5` ・ `FD-6b` ・ `S-24` ・ `AT-98` ・ `AT-99` ・ `P-20` ・ `IC-100` ・ `EN-6` ・ `S-195` ・ `S-194` ・ `PR-19` ・ `IC-47` | 0 / 0 〜 1 | 浮いている。⭐ 書き直しで偽になる所が無い |
| 表 T-251 | — | `FR-002` ・ `FR-040` ・ `FR-085` ・ `FR-092` —— 表を指すだけで `ND-5` の中身を引かない |
| `FR-013` | 6 / 17 | `FR-010` ・ `FR-011` ・ `FR-016` ・ `FR-043` ・ `FR-048` ・ `FR-049`。色の例外を引く者は無い |
| `S-63` | 1 / 3 | `FR-003`（名を引くだけ）。表 T-273 は `マーカー` の欄で「出す／出さない」の両方の行を既に持つ（`LP-1` 〜 `LP-7`）ので書き直さない |
| `FR-002` | 3 / 11 | `FR-040` ・ `FR-085` ・ `FR-092` |
| `PR-13` | 3 / 4 | `FR-002` ・ `FR-016` ・ `FR-029` —— 3 つとも本書が書く（E-05 ・ E-07 ・ E-08）。`FR-016` は 表 T-023d の `GR-10` を通して届く |
| `CM-25` | 1 / 1 | `FR-002` |
| `GR-10` | 3 / 5 | `FR-016` ・ `FR-029` ・ `FR-091` |
| `FR-041` ・ `FR-039` ・ `FR-003` | 7 / 27 ・ 20 / 57 ・ 10 / 17 | 置き場の約束を引く者は 3 つ自身だけ（閉路 4、6.2） |
| `IN-3` ・ `EZ-2` | 5 / 11 ・ 5 / 11 | `IN-7` は新しい行であり、両者を書き直さない |
| `FR-051` ・ `S-56` ・ `FR-052` | 6 / 18 ・ 4 / 5 ・ 7 / 18 | `FR-052` の式は変えない（決定 10） |
| `FR-055` | 10 / 27 | `FR-003` ・ `FR-004` ・ `FR-016` ・ `FR-018` ・ `FR-031` ・ `FR-063` ・ `FR-070` ・ `FR-080` ・ `FR-087` ・ `FR-098`。⭐ 足すのは RATIONALE の横の段だけで、縦の段割り（深い段から採る）は変えない |
| `FR-046` ・ `GR-16` ・ 表 T-269 | 9 / 17 ・ 1 / 3 ・ — | 表 T-269 を指す 10 の要求は行を足しても偽にならない |
| `FR-048` ・ `DC-4` ・ `DC-1` ・ `CU-3` ・ `S-66` | 4 / 10 ・ 2 / 3 ・ 2 / 6 ・ 4 / 6 ・ 3 / 4 | `DC-4` を引くのは `FR-048` と `FR-092` —— `FR-092` の `EZ-6` の「モードにいるあいだ出さない」は排他で変わらない |
| `S-204` | 2 / 2 | `FR-082` ・ `FR-092` —— `DC-3` が `S-334` へ移るので `S-204` はツールチップだけのものになる |
| `S-178` | 4 / 6 | 備考の 1 文だけを書き直す |
| `CD-2` ・ `HF-10` ・ `HF-17` ・ `IC-82` ・ `NT-7` ・ `FR-032` | 3 / 8 ・ 4 / 16 ・ 2 / 8 ・ 1 / 3 ・ 7 / 22 ・ 8 / 21 | どれも書き直さない（新しい行が引くだけ） |
| `FR-006` ・ `PR-12` ・ `CV-9` ・ `CV-3` | 5 / 25 ・ 1 / 1 ・ 0 / 2 ・ 1 / 4 | — |

⭐ 導いた条項ごとに、届いた行を `rulings.md` で引いた（規則 02 の 1）: 当たるのは `JDG-30`（本書が覆す。項目 6）、`JDG-326` ②（1）（本書が覆す。項目 16）、`JDG-08` ／ `JDG-92`（`S-63`。`JDG-08` は既に覆されており、残った「名称ラベルの左端は `S-63` によらない」は表 T-273 の「出さない」の行が既に満たす —— `CR-430` の読みと食い違わない）、`JDG-81`（ヘルプの閉じる入口を右端に。`RR-6` はそれに倣うだけ）、`JDG-06`（空の文書。`CD-6` の後は 1 行が残るので当たらない）、`JDG-325`（`DC-3`。字の大きさの行だけを替え、置き方と中身は変えない）。

### 6.2 `induced.py`（9 群）

| 種 | 解決 | 種の中の辺 | 閉路 | 扱い |
|---|---|---|---|---|
| `FR-002 T-251 ND-5 S-232` | 4/4 | 4 | 1: `FR-002` `S-232` | 本書は `S-232` を書き直さない |
| `FR-013 PM-4 S-24 FR-041 FR-094` | 5/5 | 3 | 0 | — |
| `FR-002 PR-13 CM-25 GR-10 FR-029 FR-109 FR-016` | 7/7 | 12 | 1: `CM-25` `FR-002` `PR-13` | ⭐ 3 つとも本書が 1 つの計画で書く（E-05 〜 E-08 ・ E-35 ・ J-09） |
| `FR-041 FR-039 FR-003 IC-100 FR-029` | 5/5 | 9 | 1: `FR-003` `FR-029` `FR-039` `FR-041` | ⭐ 3 つの置き場の文を 1 つの計画で書いた（E-09 〜 E-11）。`FR-029` は書き直さない |
| `FR-051 FR-052 S-56 FR-055 T-038 OC-1` | 6/6 | 4 | 0 | — |
| `FR-046 CU-1 GR-16 T-269 FR-106 T-237 FR-029` | 7/7 | 5 | 1: `CU-1` `FR-046` | `CU-1` は書き直さない |
| `FR-048 DC-1 DC-4 DC-7 CU-3 S-66 FR-082 EZ-6 IN-3 FR-092` | 10/10 | 15 | 1: `CU-3` `DC-4` `EZ-6` `FR-048` `IN-3` `S-66` | ⭐ `FR-048` と `DC-4` を本書が 1 つの計画で書く（E-21 〜 E-23）。`CU-3` ・ `EZ-6` ・ `IN-3` ・ `S-66` は書き直さない —— ⚠️ 当てた commit で `EZ-6` の「モードにいるあいだ出さない」と読み合わせること |
| `T-050 CD-2 FR-032 HF-10 HF-17 IC-82 NT-7 FR-004` | 8/8 | 12 | 1: `FR-004` `FR-032` | 2 つとも書き直さない |
| `FR-006 FR-007 CV-3 CV-9 T-016` | 5/5 | 2 | 0 | — |

---

## 7. 数の予測（`73c2bc90` で測った。当てた後に同じ数え方で突き合わせる）

| 数 | 前 | 後 | 内訳 |
|---|--:|--:|---|
| tables | 186 | 187 | 表 T-297 |
| figures | 27 | 27 | — |
| rows | 2282 | 2304 | 足す 27（設定値 16 —— 畳み込みの `S-339` ・ `S-340` を含む、`EN-7` `PK-10` `IN-7` `RR-6` `HF-20` `CD-6` `DC-9` `QN-10` `IC-106` `RK-1` `RK-2`）、消す 5（`PR-13` `AT-98` `AT-99` `CM-25` `P-20`）。`IC-100` は移すだけ |
| uids | 162 | 162 | — |
| 接頭辞 | 162 | 163 | `RK` |
| 辞書の項 | — | +2 −1 | `IC-106` ・ `QN-10` を足し、`properties` の `PR-13` を消す。`colourField` の `custom` は語を替えるだけ |

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ 仕様の編集（4 節）は 1 つの体が 1 つの worktree で当てる（同じ `01-04-requirements.md` と `settings.json` に 35 ＋ 17 の編集が入る）。⭐ ただし原稿を足すと読む側が投げる名簿がある（表 T-016 は `properties-panel.ts` の `heldByOf` が知らない行で投げる、規則 02 の 3.5 節）ので、**仕様の波とコードの波を合わせた木で門を打つ。**

```
wave 0  spec     all of section 4 (E-01..E-35, J-01..J-17) + npm run gen + gen:check
                 -> tests that read the spec verbatim go red here, by design
wave 1  (parallel, disjoint files; cut from wave 0's commit)
  1a entity/layout   schedule-layout.ts (items 1, 10: planDateText, label measure, fitZoom),
                     schedule-geometry.ts (item 3: chevron notch per end)
  1b svg-renderer    schedule-task-figures.ts (item 2 delay mark rows/colours, item 1 two tspans),
                     schedule-overlays.ts (item 11 S-163/S-333, item 12 S-195/S-194 for the guide)
  1c panel + modals  properties-panel-drawing.ts (item 16), open-modals.ts / open-modals-drawing.ts (item 7),
                     screen-regions.ts / screen-frame.ts / schedule-grid.ts (item 9),
                     app-header-items.ts / app-header-drawing.ts (item 6),
                     tooltips-drawing.ts + STYLE.tooltip in dom-screen-surface.ts (item 8 JDG-396,
                     item 12 readout S-334/S-340), the host colour input behind [Custom] (JDG-397),
                     and the e2e tooltip sweep (every icon tooltip, three window widths)
  1d drop the columns (item 5): json-codec.ts, schedule.ts, edit-task.ts, task-appearance.ts,
                     edit-document.ts, field-commit.ts:230, the two templates, the two prompts, ~38 tests
wave 2  shell (serial, ONE body -- frame-loop.ts is 4760 lines and check 60 holds its functions)
                     frame-loop.ts (item 10 :1652, item 11 :339 + centring, item 12 :1403-1418),
                     input-command-translator.ts (items 12, 13), zoom-and-fit.ts:478 (item 10),
                     edit-task-group.ts (item 13 CD-6)
wave 3  tests by a spec-only tester per wave (never the implementer), then check.sh + vitest on the merged tree
```

- ⚠️ 1d は `field-commit.ts` を触る。項目 14 のコード（`95f7c3ed`）は既に同じファイルに在るので、1d の体に「`:148-160` は触らない」と書くこと。
- ⚠️ 1a と 2 は `FR-055` を分けて持つ（1a が `fitZoom` の値、2 が殻での書き込み）。継ぎ目は 5 節の S-3 ・ S-4 を両方の依頼文へ逐語で写す。
- ⚠️ 項目 4（`S-63`）は波 0 の `npm run gen` だけで効く。
- ⚠️ 項目 14 のコードは既に在る（`95f7c3ed`）。波 3 の試験だけが要る。
- ⚠️ 項目 8 は `87458f63` が 1 行にしたが、上限（`S-339`）も左端の床も無い。`JDG-396` の保証は、1c が CSS で持ち、窓の幅を 3 つ変えて全アイコンのツールチップを開く e2e（1 行の箱であること、窓の内側に収まること）で確かめる。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

| 項目 | 所 | 何をする | 試験（いま引いているもの） |
|---|---|---|---|
| 1 | `src/entity/layout-engine/schedule-layout/schedule-layout.ts:265-277`（`planDateText`）と、ラベルの幅を測る所 | 年を下 2 桁に、予定日の幅を × `S-325` で測る | `tests/unit/cr-386-ic-103-planned-dates-beside-the-name.test.ts` ・ `tests/unit/uf-39-40.test.ts`（`ND-5` を引く） |
| 1 | `src/adapter/svg-renderer/schedule-task-figures.ts` の `labelSvg` | 予定日の `<tspan>` を × `S-325` | 同上 |
| 2 | `schedule-task-figures.ts:143-146` の `DELAY_*` 4 つ ／ `markerSvg` の `ink` | 4 つを `S-328` 〜 `S-331` に、`PM-4` の地と記号を `S-326` ・ `S-327` に | `tests/unit/dfc-407-417-418-the-three-fixes-of-2026-09-09.test.ts` ほか `markerStroke` を引く 6 本 |
| 3 | `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts:448-451`（`Math.max(fade.fadeIn, fade.fadeOut)`）と `chevronOutline`（`:223`） | 開始側の切り込みと終了側の先端に別の深さを渡す（`chevronOutline` の引数を 2 つにする） | `tests/contract/fd-6-iv-12-the-fade-span.contract.test.ts`（`FD-5` を逐語で引く試験は 0） |
| 4 | 生成物だけ | — | `tests/unit/t-038-oc-2-labels-in-the-occupied-width.test.ts` ほか `progressMarkerVisible` を引く 10 本 —— 既定に頼る試験は `true` を明に置く |
| 5 | 1d の表 | 列・命令・欄を消し、読み込みで捨てる（表 T-297） | `nameAnchor` を持つ試験 38 本（`tests/unit/edit-task.test.ts` 10、`uf-19.test.ts` 6 ほか）—— 大半は文書の形の見本として 2 列を書いているだけ |
| 6 | `src/adapter/screen-renderer/app-header-items.ts` ・ `command-palette.ts` | 生成された名簿（`icon-roster.json`）が面を運ぶ。手で並べている所があれば直す | `tests/unit/uf-48-write-moment.test.ts`（`IC-100`） |
| 7 | `src/adapter/screen-renderer/open-modals.ts` ~`:199` ・ `open-modals-drawing.ts` | 名簿の見出しの行の右端に `IC-52` | `tests/unit/cr-406-the-resource-roster-is-a-ruled-grid.test.ts` ・ `cr-439-open-modals-help-roster-report-export.test.ts` |
| 8 | `src/framework/dom-screen-surface/tooltips-drawing.ts`（`tooltipStyle` ・ `keepTooltipsInside`）・ `dom-screen-surface.ts` の `STYLE.tooltip` | 幅を中身に、上限を `calc(100vw - 2 × S-339)` に、右端の返しと左端の床を 1 度だけ測って決める（5 節の S-10、`JDG-396`） | `tests/unit/ez-2-tooltip-one-line-inside-the-window.test.ts` が在る。`IN-7` を逐語で引く試験と、全アイコン × 窓の幅 3 つの e2e を足す |
| 9 | `src/entity/layout-engine/screen-regions/screen-regions.ts:165` ・ `src/adapter/screen-renderer/screen-frame.ts:92-109` ・ `src/adapter/svg-renderer/schedule-grid.ts:226` | 行の帯を `canvasPadding` の余白へ延ばし、縦の帯の中へは描かない。横の帯を画面の下端に置き、日程の下端をその上に | `canvasPadding` を引く 23 本（`tests/unit/dfc-405-gr-21-the-grip-is-a-fraction-of-its-lane.test.ts` ほか） |
| 10 | `schedule-layout.ts:1263-1332`（`fitZoom`）・ `src/adapter/input-command-translator/zoom-and-fit.ts:478` ・ `frame-loop.ts:1652` | 12 節の `DFC-791` 〜 `DFC-793` | `tests/unit/fr-055-vertical-lod-fit.test.ts` ・ `fr-055-fit-discards-the-collapse.test.ts` ・ `cr-403-the-row-zoom-ceiling-follows-t-253.test.ts` ・ `layout-engine.test.ts` |
| 11 | `schedule-overlays.ts:156`（太さ `1`）・ `:160`（`S-163`）・ `frame-loop.ts:339`（`'GR-16': null`）・ `src/framework/dom-screen-surface/dom-screen-surface.ts:294`（境界の `col-resize`）・ 基準日線を出す命令の後 | 太さを `S-333`、ポインタを `PK-10`、出したときに中央へ送る、`IC-44` を `EN-7` で塗る | `tests/unit/t-023c-selected-line-width.test.ts` ・ `tests/unit/fr-029-en-2-a-palette-toggle-that-is-on.test.ts` |
| 12 | `schedule-overlays.ts:175-176`（`S-148`、`guideWidth = 1`）・ 読み出しの字（`tooltips-drawing.ts` の `readoutStyle` —— `max(S-340 px, S-334 em)`、5 節の S-12）・ `frame-loop.ts:1403-1418` ・ `input-command-translator.ts:694` | 色と太さ、`S-334`、排他 | `tests/unit/dfc-295-entering-the-dual-cursor-drops-the-arm.test.ts` ・ `cr-550-dual-cursor-readout.test.ts` ・ `duplicate-paste-and-dual-cursor.test.ts` ・ `fr-092-ez-2-tooltip-text-size.test.ts`（`S-204` を読み出しに当てているなら直る） |
| 13 | `src/adapter/input-command-translator/input-command-translator.ts:711` 付近 ・ `src/use-case/edit-document/edit-task-group.ts` ・ 行見出しパネルの頭の描き手 | `IC-106` を頭に置き、`CD-6` を 1 つの取り消しの単位で | `tests/unit/cr-541-row-title-panel-open-all-and-reset.test.ts` ・ `tests/system/pinned-band-and-the-empty-document.test.ts` ・ `CD-2` を引く 11 本 |
| 14 | 済（`95f7c3ed`、`src/adapter/input-command-translator/field-commit.ts:148-160`） | — | 仕様を逐語で引く試験を足す |
| 15 | 生成物（`property-items.json`）だけ | — | `tests/unit/fr-006-panel-typography.test.ts` ・ `cr-439-properties-panel-fields.test.ts`（並びを主張していれば直す） |
| 16 | `src/framework/dom-screen-surface/properties-panel-drawing.ts`（`CHECKER_LIGHT` ・ `CHECKER_DARK` ・ `SWATCH_SIDE_EM` ・ `CHECKER_TILE_EM`、`b359645b` ・ `c45d9065`） | 定数を `S-335` 〜 `S-338` に、並べ方を 2 段 × `S-338` ＋ 任意 ＋ 透明に、値の示し方を S-8 に、`input type=color` を [任意] の押下の後にだけ出す（S-11、`JDG-397`） | `tests/unit/cv-9-transparent-and-unset-swatches.test.ts` ・ `cr-548-palette-colours-stored-by-name.test.ts` ・ `cr-439-properties-panel-fields.test.ts` ・ `tests/contract/display-words.contract.test.ts` |

⚠️ `frame-loop.ts` を触るのは項目 10 ・ 11 ・ 12（と、測ったうえで 13）である。検査 60 の基準線（`fieldEditingOf` ほか）は段 7.5 の分割を待っており、前に立つ者が扱うので、波 2 の体は関数を太らせず、足す分岐は別の関数に出すこと。

---

## 10. ⛔ この変更でやらないこと

- **遅延診断の自動表示**（項目 4 の続き「遅延診断を実行したときは、マーカーを自動で表示しろ。ただし、ユーザーがマーカーを非表示にできるようにしろ。」）—— 遅延診断そのものがまだ仕様に無い。⭐ **申し送り**: 遅延診断の変更要求は、① 実行したときに `S-63` を `true` にする（文書の値を書くので取り消しの単位と未保存の印を決める）、② その後の非表示は既存の入口（`FR-049` の切替）で行える、の 2 つを要求として書くこと（`JDG-371`）。
- **遅れの色の家族の残り 2 つ** —— ボトルネックの経路（桃色。ショッキングピンクではない）とボトルネック（赤）の行は立てない（決定 5）。⭐ **申し送り**: 遅延診断の変更要求が 表 T-236 に、`S-326` ・ `S-327` と同じ形（地と記号の 2 行ずつ）で足す。
- `EZ-6` の日付の書き方（`YYYY-MM-DD`）、ツールチップの字（`S-204`）。
- 文書の描画設定の面（`FR-072`）の中の色の並び —— 表 T-016 の外である（決定 19）。
- `docs/development-records/` の `pending-decisions.md` ・ `changelog.md`、`A-appendix.md` の変更履歴（当てる者が書く）。

---

## 11. 前に立つ者へ返す問い

| # | 問い | 推奨と理由 |
|---|---|---|
| 1 | 項目 2「! は黄色にぬれ」—— 黄にするのはマーカーの地か、記号 `!` か | **地を黄、`!` を暗い色**（決定 3）。白い地に黄の `!` は明るいテーマでほとんど読めない。逆なら `S-326` と `S-327` の値を入れ替えるだけ |
| 2 | 項目 12「今の50%」—— `S-334` = 0.4375 は、宿主の地の文字が 16px のとき 7px になる | そのまま当てる（逐語どおり）。読めないと分かれば `S-334` の 1 行を上げる。⚠️ 当てた後、`DC-3` の 3 行を実物で見てもらう |
| 3 | 英語の語 —— `IC-106`「Delete All Rows」／「Delete every row and the tasks they carry」、`QN-10`「Delete every row and the tasks on them?」、`colourField` の `custom`「Custom」 | 提案どおり。辞書の `$comment` は体が語を決めることを禁じている（`JDG-324` と同じ扱い） |
| 4 | 色の値 —— 朱色 `#eb6101` ／ `#ff8c42`、マゼンタ寄り `#c2188f` ／ `#f07ad0`、黄 `#ffd400` ／ `#e6c229` | 提案どおり当て、使い勝手の試しで 1 行ずつ直す |

⭐ **答え（2026-09-23、調整役が中継）**: 問い 1 は「提案通り」（`JDG-400`）。問い 4 は「それでよい。」（`JDG-401`）。問い 2 は `JDG-398`（50%、下限 10px。`S-340` を足した）。問い 3 は、利用者が「All Rows」と「every row」の食い違いを問うたあと「推奨どおり」（`JDG-402`）—— 語を「all rows」に揃えた（`IC-106` の説明「Delete all rows and the tasks they carry」、`QN-10`「Delete all rows and the tasks on them?」。`IC-106` の名「Delete All Rows」と `custom`「Custom」は提案のまま）。⭐ `QN-10` の問いは、1 行の規則が作る空の行に触れない（最後の行を消すときと同じ。`NT-7` がそれを数えることを禁じる）。

---

## 12. 台帳

### 12.1 裁定（`docs/development-records/rulings.md` に本書と同じ日に書いた）

`JDG-367` 〜 `JDG-383` —— 項目 1 〜 16（項目 4 は 2 行、`JDG-370` ・ `JDG-371`）。状態はすべて「指示」（本書を当てたら「適用済」へ変える。当てる者の仕事）。

⛔ 当てる者が変える古い行: `JDG-30`（モノクロの置き場を覆された、に）、`JDG-326`（② (1) の示し方を覆された、一部、に）。

⭐ 起草の後に下りた行も本書が当てる: `JDG-396`（項目 8 の保証）・ `JDG-397`（項目 16 の宿主の色の入力）・ `JDG-398`（項目 12 の読み出しの字）、11 節の答え `JDG-400` 〜 `JDG-402`。

### 12.2 欠陥（`docs/development-records/defects.md` に本書と同じ日に書いた）

| ID | 何 | 状態 |
|---|---|---|
| `DFC-789` | 透明の見本が、正しくない CSS（`color:none`）で塗られて黒く見えた | 直した（`b359645b` ・ `c45d9065`）。`実測待ち` |
| `DFC-790` | 名称ラベルの左の余白を 2 回足していた（表 T-273 が足した `S-31` ／ `S-32` に、描き手がもう一度 `labelPad` を足す） | 直した（`1f1893a6`）。`実測待ち` |
| `DFC-791` | `fitZoom` が `zoomX` を、最も深い段を縦の床の倍率で並べた絵から 1 度だけ解き、採った段と着地する縦の倍率で測り直さない —— 字の幅は縦の倍率に従うので、採った絵が `Row Area` より広くなり右端が切れる | `仕様待ち`（本書の E-15） |
| `DFC-792` | `fitZoom` の下がる分岐: 採った段が着地する縦の倍率で収まらないとき、1 つ浅い段の着地の倍率を返しながら、表示位置は床の倍率で並べた絵（`atFloor[depth-1]`）から決め、その段がその倍率で収まるかを確かめない | `仕様待ち`（本書の E-15） |
| `DFC-793` | 全体表示の左端を日の境目へ切り捨てる（`schedule-layout.ts:1323` の `dateAtX` と `frame-loop.ts:1652` の `scrollDayOffset: 0`）—— 切り捨てた分だけ絵が右へずれる | `仕様待ち`（本書の E-15） |

---

## 13. 測り方の再現

```
# totals (before)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   -> tables=186  figures=27  rows=2282  uids=162   (73c2bc90, 2026-09-23)

# free identifiers (2026-09-23, 73c2bc90): every prefix's maximum over docs/spec src tests tools change-request
for p in S PK EN DC HF CD IC EZ IN CV ND PR NT FD QN RR; do
  git grep -ohE "\b$p-[0-9]+[a-z]?\b" -- docs/spec src tests tools change-request | sed "s/$p-//;s/[a-z]$//" | sort -n | tail -1
done
#   -> S 324 (999 is a test sentinel), PK 9, EN 6, DC 8, HF 19 (120 is a word in a test comment), CD 5, IC 105,
#      IN 6, QN 9, RR 5
git grep -lE "\bS-32[5-9]\b|\bS-33[0-8]\b|\bPK-10\b|\bEN-7\b|\bIC-106\b|\bHF-20\b|\bCD-6\b|\bDC-9\b|\bRK-[0-9]+|\bT-297\b|\bQN-10\b|\bRR-6\b|\bIN-7\b" -- . ':!previous-project-result'
#   -> nothing but this file (RK-, T-297 checked the same way)

# graph
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>      # each ID of 6.1
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py <seeds>  # each row of 6.2

# every old block of section 4.1 occurs exactly once in its file, and all 35 apply in order to a COPY:
#   scratchpad build551.py (kind 'line' = the one line starting with the prefix, n lines; kind 'sub' = count == 1)
#   -> problems 0 edits 35
# every JSON edit of section 4.2 applied to COPIES of the six manuscripts, loaded with json.loads,
#   and validated against the hand-written schemas (jsonschema, instance check only):
#   -> settings.json 0 errors, erd.json 0, property-items.json 0, state-machines.json 0
```
