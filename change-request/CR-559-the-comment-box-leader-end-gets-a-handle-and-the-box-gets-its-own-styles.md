# CR-559 — コメントボックス: 線先の取っ手の印、本体と線先の動き方、4 隅から出る引出し線、見た目の列

> 起草の状態: 起草（2026-09-24 に 11 節の問い 2 つへ利用者が答えた。4 節は CR-558 の後に当てられる）。当てる順は `CR-551` → `CR-552` → `CR-553` → `CR-554` → `CR-558`（ハイライトボックス）→ 本書。
> 読んだ木: `ebc71984`（本 worktree）。行番号・数・参照は、断りの無いかぎりこの木で測った（測り方は 13 節）。
> ⚠️ 起草のあいだに `refactor` は `1674c271` へ進み、`CR-551` の仕様とコードが着地した（仕様は `7dbd292d` から動いていない）。⇒ 4 節の旧は、`CR-551` の後の木（`refactor`）の写しに **`CR-558` の 19 の編集を先に当て**、その上で数えた（13 節）。E-01 ・ E-02 ・ E-06 〜 E-08 ・ E-10 ・ E-11 は `CR-558` が足した文を旧にするので、`CR-558` の後の木でだけ 1 回現れる。
>
> **閉じるもの**: 利用者の 2026-09-23 の指示 5 件（`JDG-460` 〜 `JDG-464`。逐語は 0.1 節）と、欠陥 `DFC-870` ・ `DFC-871`（12 節）。
> ⛔ **覆すもの**（2026-09-23 の仕分けで、利用者が覆すことを受け入れた）: ① `FR-019` の RATIONALE の「留めた点と、本文の箱の左下隅とを、1 本の線で結ぶ」（`01-04-requirements.md:4796`）と、線が一意に定まる理由の「箱の基準隅は … 左下と定めている」（`:4798`）。② 同じ段の「線は注記の色で描く」と「新しい設定値を立ててはならない（MUST NOT）」（`:4800-4801`）。③ 表 T-023d の結びの「本体と引出し線を掴めば `CM-51` で、線先を掴めば `CM-50` で書く」（`:3402`）。④ 同じ結びの「箱は不透明に描くので」（`:3403`。MUST NOT は残し、理由を替える）。⑤ `S-291` の備考「引けば本文の箱が動く（`CM-51`）」。⑥ `JDG-249` の読みのうち「引出し線を引けば、本体と同じく箱が動く（`CM-51`）」。
> ⚠️ **覆さないもの**: `FR-019` の「ずれ（`bodyOffsetPx`）の基準隅を左下とする」（`:4752` 〜 `:4755`）。基準隅は箱の置き場を保存する約束であり（2026-09-06 の利用者の裁定「付箋の基準は左下の点とする」、`tests/unit/edit-annotation.test.ts:666` が引く）、線がどこから出るかとは別に持てる。⇒ 文書の形（留めた点は日付と行、箱はずれ）は変えない（決定 6）。
> ⭐ **形の方針**: 利用者は、使い勝手の試しのあとで裁定がまた動くと述べている。⇒ 出る隅は 表 T-221 の 1 行（`LF-17`）、太さと透過率の既定と範囲は注記の既定の表 T-217 の 2 行（`S-374` ・ `S-375`。`CR-558` の `S-369` 〜 `S-371` と同じ表・同じ形）、線先の印の大きさは 表 T-206 の 1 行（`S-376`）に置き、要求はその行を指す。裁定を戻すときは、多くが 1 行の書き換えで済む（戻し方は 1 節の最右列）。

### 0.1 利用者の逐語（2026-09-23）

⚠️ 前に立つ者が体へ渡した写しのまま写した。1 通の中の区切り `／` は写しの字である。項目 1 〜 4 は 1 通、項目 5 は別の 1 通である。

| 項目 | 逐語 |
|---|---|
| 1 | 「コメントボックスを選択した時、コメントボックスの線の先は□で選択可能な旨アフォーダンスしろ。」 |
| 2 | 「コメントボックス本体を動かすとそれに連動して線の先も動け」 |
| 3 | 「逆に線の先だけ動かしたときは、コメントボックス本体を動かすな」 |
| 4 | 「コメントボックスの線は常にコメントボックス本体の4隅の1か所から出るようにしろ。」 |
| 5 | 「コメントボックス: 線色 / 線太さ(数値) / 塗りつぶし (透過率設定可能) / フォント色」 |

前に立つ者が渡した裁定（利用者の言葉ではない）: 覆すもの ① 〜 ⑥ は 2026-09-23 の仕分けで利用者が受け入れた。色の欄は `CR-551` の欄（10 色を 5 つずつ 2 段、その下に [任意] [透明] [テーマ]。`JDG-383` ・ `JDG-397` ・ `JDG-405`）を使い、色の行はプロパティパネルの末尾に置く（`JDG-382`）。
前に立つ者の裁定（2026-09-24、`CR-558` との揃え）: ① 意味 1 つに表 1 つ —— 注記の見た目の既定と範囲は 表 T-217 に置き、本書は新しい表と接頭辞を立てない。② 透過率は利用者の語のとおり `fillTransparencyPercent`（0 ＝ 不透明 〜 100 ＝ 透明、整数の百分率）とし、`CR-558` と同じ形・同じ読み（範囲の外は描くときに範囲へ寄せる）にする —— 利用者の読みが常に正（handoff 0.1）。

---

## 0. ⛔ 立ちどまって答える 3 つ

### ① この変更は `CH-` / `GL-` のどれを前へ進めるか

⭐ **`CH-4` ／ `GL-006`**（説明を読まずに操作できる）—— 項目 1（掴める所を印で示す）と項目 4（線が箱のどこから出るかを読ませる）。
⭐ **`GL-008`**（描いたものを直接掴んで変えられる）—— 項目 2 ・ 3。いまは逆に動く: 本体を引くと線先が残り（`item-grab.ts:108-118` は `CM-51` だけを書く）、線先を引くと本体が付いてくる（`:477-488` は `CM-50` だけを書き、箱は線先からのずれで置かれる）。
⭐ **`CH-1`**（作図ソフトと同じ操作感）と `UC-008`（注記で伝える）—— 項目 5 の線・塗り・字の色と太さと透過率。

### ② レビュー観点のどの条項を当て、何が出たか

- **`R1.3`（矛盾・重複がない。唯一の正）** —— ① `FR-019` の「線は注記の色で描く … 新しい設定値を立ててはならない」が項目 5 と両立しない → `FR-019` の段を書き替え、太さと透過率の既定と範囲は 表 T-217 に置いた（E-09 ・ J-04）。② 見た目の既定が仕様のどこにも無い: 枠と線の太さ `1`（`schedule-overlays.ts:217` ・ `:224` の字）、塗り `S-146`・字 `S-147`（`:224` ・ `:230`）は、コードだけが選んでいた → `DFC-870` ・ `DFC-871` とし、`FR-019` の段と 表 T-217 に書いた。③ 表 T-023d の結びと `S-291` の備考と `JDG-249` の読みが同じ「引けば箱だけが動く」を 3 か所で言っている → 3 つとも本書が 1 つの計画で書く（E-04 ・ J-01 ・ 12 節）。④ 色の列の一覧（`CV-1`）と欄の並べ方（`CV-9`）が `CommentBox` を知らない → 2 か所に 1 句ずつ足した（E-01 ・ E-03）。描く値（`CV-6`）は「どの欄の値かは描く所で決める」と定めているので書き替えず、コメントボックスの 3 つの色の描き方を描く所（`FR-019` の本書の段）に 1 文ずつ置いて `CV-6` を行 ID で引く（E-09）。⑥ `FR-019` と `LF-15` は互いを指す（6.2 節の閉路）→ 本書は両方を 1 つの計画で書く: `FR-019` の段（E-09）は線の出る隅を `LF-17` に、留めた点の置き場を今までどおり `LF-15` に任せ、`LF-15`（E-12）は注だけを「ずれを書かずにアンカーだけを書けば箱も動く。掴んで引いたときの書き方は 表 T-023d の結び」に替える。当てた後の 2 つは食い違わない（`FR-019` の「本文の箱は留めた点からのずれで置く」と `LF-15` の注が同じことを言う）。⑤ 同じ意味（注記の見た目の既定と範囲、読むときに `null` で足すこと、範囲の外を寄せて描くこと）を `CR-558` と別の場所に書かない → `CR-558` が書く 3 つの文に本書の列と命令を足す形にした（E-07 ・ E-08 ・ E-10 ・ E-11）。
- **`R1.4`（異常系・境界値）** —— ① 今までに保存した文書は 5 つの列を持たない。生成するスキーマは列をすべて `required` にするので（`json-codec.ts:607` の `HighlightBox` と同じ形）、足すだけでは今までの文書がすべて開けなくなる → 読むときに `null` として足す（E-10、決定 16）。② 留めた点が本文の箱の中に在るとき、4 隅のどれから引いても線は本文の上を横切る → 線を描かない（`LF-17`、決定 8）。③ 本体を引いてずらした線先の下に行が無いとき → どちらも動かさず `RS-44`（表 T-246 の `HB-3` と同じ。E-04）。④ 太さと透過率の範囲の外 → 命令は拒み、読んだ値は範囲へ寄せて描き、文書は書き換えない（`CR-558` の E-15 の 2 文に列と命令を足す。E-07 ・ E-08、決定 17）。⑤ 太い線でも取っ手の印が掴み代の外にはみ出さない → `S-376` の備考に上限を書いた（J-03）。
- **`R1.2`（検証できる表現）** —— 「□」「連動して」「4隅の1か所」「透過率」を、印の一辺（`S-376`）、束で書く 2 つの命令と置く値（E-04）、隅の選び方（`LF-17`）、透過率の列と範囲（`AT-151` ・ `S-375`）にした。
- **`R2.1`（命名）** —— 命令 5 つの名は `setCommentBox` ＋ 列の名（`CM-55` の `setHighlightBoxStrokeColor` と同じ作り）。`T-217` の鍵は `CommentBox.` を頭に付けた（決定 21。`CR-558` の決定 16a と同じ）。列の名 `strokeWidthPx` ・ `fillColor` ・ `fillTransparencyPercent` は `CR-558` の `HighlightBox` の列と同じ綴り。
- **`R4.4`（状態機械）** —— 当たらない。掴んで離す道は既存の身振りのまま（書き込みの中身だけが変わる）。

⭐ 裁定が要るかを決める前に、規則 handoff.md 0.2 の 3 段を踏んだ: ① `rulings.md` を「コメントボックス」「線先」「引き出し」「掴み」「左下」で引いた —— 当たるのは `JDG-248` ・ `JDG-249` ・ `JDG-252` ・ `JDG-320` と `JDG-399`（掴み代は既存の規則に従え）。`JDG-249` の読みの一部を本書が覆す（12 節）。`JDG-252`（線先はタスクに追従しない）と `JDG-320`（留める日が空の箱は文書の開始日）は変わらない。② 触る行を `impact.py` で引いた（6 節）。③ 別の道: 文書の形を変えずに済む道（ずれの基準隅を保ち、線の出る隅を描く側で選ぶ）を採った。

### ③ 利用者に問わずに決めたこと

⭐ 下は根拠を添えて問わずに決めた（あとから覆せる。戻し方は 1 節）。

| # | 決めたこと | 導き | 代償 |
|---|---|---|---|
| 決定 1 | 「線の先」は、留めた点（`anchorDate` と `anchorGroupId`、表 T-023d の結びの「線先」）である | 仕様が既に同じ所を「線先」と呼ぶ（`01-04-requirements.md:3401-3410`、`S-292`） | — |
| 決定 2 | 本体を引いたとき、本文の箱は引いた量のとおりに動き、線先は「押したときの線先の点 ＋ 引いた量」の下の日と行に置き直す（線先は日の列と行の帯の中央に立つ） | 逐語「本体を動かすと … 線の先も動け」。本文の箱を日と行の格子に吸わせると、拡大したときに箱を細かく置けない（11 節の問い 1） | 線先の動く量は、引いた量から日の列と行の帯の半分まで離れることがある |
| 決定 3 | 引出し線を掴んで引いたときは、本体を引いたときと同じ | `JDG-249` の読み（B: 引出し線を引けば、本体と同じく箱が動く） | — |
| 決定 4 | 線先を引いたとき、本文の箱は画面の上で動かない —— 線先 `CM-50` と、置き直した線先から元の箱の左下隅までのずれ `CM-51` を書く | 逐語「線の先だけ動かしたときは、コメントボックス本体を動かすな」。箱は線先からのずれで置かれるので、`CM-50` だけでは箱が付いてくる | ずれは画面の px なので、拡大率を変えると、箱と線先の見た目の距離は変わらず、線先の日との関係だけが変わる（今までと同じ） |
| 決定 5 | 2 つの命令を 1 つの束で書く。新しい命令を立てない | 表 T-035 の `AG-3`（束は原子的）。`changed([...])` が既に 1 束を作る（`input-command-translator.ts:302-306`）。1 度の取り消しで 2 つとも戻る | — |
| 決定 6 | 文書の形は変えない: 留めた点は日付と行、箱は左下隅へのずれのまま。線が出る隅は描く側で選ぶ | 2026-09-06 の裁定「付箋の基準は左下の点とする」は置き場の約束であり、線の出る所の約束ではない。形を変えると、今までの文書の置き場を読み替える段が要る | 保存する基準隅と線の出る隅が別になる（`FR-019` に 1 行で断る） |
| 決定 7 | 出る隅は、4 隅のうち留めた点に最も近い隅。等しいときは左と下を採る（`LF-17`） | 逐語「4隅の1か所」。最も近い隅は、横と縦をそれぞれ箱の中点と比べるだけで決まる。左下は基準隅と同じ | 読みが違えば `LF-17` の 1 行を書き換える |
| 決定 8 | 留めた点が本文の箱の中（縁を含む）に在るときは、引出し線を描かない。取っ手の印はそのときも描く | 箱の外へ出る線が無く、隅から中の点へ引くと本文の上を横切る（塗りを透かしたときに読める） | 線が消えるので、箱が何を指すかは印でしか読めない（選んでいるときだけ） |
| 決定 9 | 印は一辺 `S-376`（8px）の正方形。中は地の色 `S-146`、縁は強調の色 `S-151`、縁の太さは選択の枠の `S-174`。重ね順は `ZO-10`。選んでいるときだけ描く。倍率を掛けない | 逐語「□」「選択した時」。色と太さは選択の枠（`SL-8`）が既に読む行であり、新しい色を起こさない | — |
| 決定 10 | 印は掴み代を変えない —— 線先は選んでいてもいなくても `S-292` で応える | `JDG-399`「掴める幅の扱い: 今回の対応で特別ルールを設けるな。既存のルールに従え。」 | — |
| 決定 11 | 線先の掴み代に入ったポインタは `PK-11`（`CR-558` が立てる、環境の `move`） | 線先は日と行の両方へ動く。`CR-558` の 5.1 節の申し送り「箱を動かす形が要るなら `PK-11`（`move`）を使い、同じ形の行を足さない」 | 本体と引出し線のポインタは今のまま（10 節） |
| 決定 12 | 線の色と太さは、本文の箱の枠と引出し線の両方に当てる（1 つの色、1 つの太さ） | 利用者は引出し線を「コメントボックスの線」と呼んだ（項目 4）。枠と線は付箋の縁から指す先まで続く 1 本として読まれる | 枠と線で色を分けるには列を 1 つ足す（1 行では済まない） |
| 決定 13 | 線と字の欄には透明を並べない。塗りの欄には並べる | 引出し線が消えると、どれについてのコメントかが読めない（`JDG-249` の逐語「どのタスクやマイルストーンに対するコメントなのかを明らかにできる」）。字が消えると本文が消える | 枠の無い付箋は作れない（11 節の問い 2） |
| 決定 14 | パレット色の描く値: 線と字は 表 T-294 の縁の値、塗りは塗りの値 | 縁の値は地に対して `CT-4` ・ `CT-5` を満たす（`CV-8`）。塗りの値は淡く、付箋の地に向く。`CR-558` の `HighlightBox.fillColor` も塗りの値 | — |
| 決定 15 | 5 つの列が `null` のときは、今までの絵と同じ値で描く: 太さと透過率は 表 T-217 の `S-374`（1）・ `S-375`（0）、色は線 `S-312`・塗り `S-146`・字 `S-147`。置くときも列は `null` のまま | コードが今まで描いていた値（`schedule-overlays.ts:217` ・ `:224` ・ `:230`）。既定の 1 行を替えれば `null` の箱がすべて追随する（`CR-558` の決定 11a と同じ持ち方） | 色の 3 列の既定は 表 T-217 に行を立てず、画面の色の 表 T-236 の既存の行を `FR-019` が名指す —— `HighlightBox.strokeColor` の `null`（`S-312`、`FR-019`）と同じ扱い。表 T-217 に色の行を立てるのは、既定が新しい値のとき（`CR-558` の `S-370` の透明）だけ |
| 決定 16 | 5 つの列を持たない `CommentBox` を読むときは、形式の版によらず `null` として足す。通知しない | `CR-558` の決定 14 と同じ段（E-10 はその段の文に `CommentBox` を足す）。`json-codec.ts:1392` が `editGroup` を持たない行を版によらず埋めているのと同じ形 | 今の版で鍵を欠いた文書も黙って開く |
| 決定 17 | 太さと透過率の範囲（`S-374` ・ `S-375`）の外: 命令は拒み、読んだ値は範囲へ寄せて描き、文書は書き換えない | `CR-558` の E-15 の 2 文（範囲へ寄せて描く ／ 命令は拒む）と同じ規則。同じ文に本書の列と命令を足す（E-07 ・ E-08） | 範囲の外の値を持つ文書は、保存した値と描いた値が食い違ったまま残る |
| 決定 18 | 線の太さは整数の px（倍率に追随させない）。下限・上限は 表 T-217 に置き、原稿の型（`erd.json` の `min` / `max`）には書かない | 本文の箱の大きさと字は画面の px（`FR-097`）、ずれも画面の距離（`FR-019`）。範囲の置き場は `CR-558` の決定 9 と同じ | 生成したスキーマは範囲の外を拒まない（読む側が寄せる） |
| 決定 19 | パネルの行は 3 つ: `PR-26`（線の太さ）・ `PR-27`（塗りの透過率（%））・ `PR-28`（線・塗り・字の色）。`PR-21`（本文）の後、色の行を末尾に | `JDG-382`（色の行は末尾）。数値の 2 行の語は `CR-558` の `PR-23` ・ `PR-24` と同じ（「線の太さ」「塗りの透過率（%）」） | 透過率の行が塗りの色の行と離れる（`CR-558` と同じ代償） |
| 決定 20 | 値は `S-374` 既定 1（整数、下限 1、上限 8）、`S-375` 既定 0（整数、0 〜 100）、`S-376` 8px（🔎 測って決めていない） | 既定は今の絵（不透明な箱、1px の線）。線の範囲は `CR-558` の `S-369` と同じ。下限 1 は、1px を割ると画面の画素に届かず線が消えるから（表 T-257 の `RR-5` と同じ理由）。8px は掴み代 `S-292`（6px、差し渡し 12px）の中に収まる | 11 節の問い 2 |
| 決定 21 | `T-217` の 2 行の鍵は `CommentBox.strokeWidthPx` ・ `CommentBox.fillTransparencyPercent` と実体名を頭に付ける | `CR-558` の決定 16a（`HighlightBox.strokeWidthPx` ほか）と揃え、同じ表の中で鍵が重ならない | 既存の `S-132` の鍵（`cornerRadiusPx`）と綴りの流儀が揃わない |
| 決定 22 | 塗りの透け具合は、利用者の語のとおり透過率 `fillTransparencyPercent`（整数、0 ＝ 不透明 〜 100 ＝ 透明）で持ち、描く不透明度は 1 − 値 ÷ 100。パネルの語は「塗りの透過率（%）」／「fill transparency (%)」 | 利用者の逐語「透過率設定可能」（利用者の読みが常に正、handoff 0.1）。`CR-558` の決定 11 と同じ列の形 | SVG と CSS の `fill-opacity`（不透明度）とは向きが逆 —— 描く所で 1 回だけ 1 から引く。戻すなら 表 T-217 の `S-375` の 1 行と列の名 |

---

## 1. 範囲 —— 項目ごとの行き先

| 項目 | 仕様で変える所 | 編集 | 裁定を戻すとき |
|---|---|---|---|
| 1 □ の印 | 表 T-023d の結び（印の段）・ 表 T-020 の `ZO-10` ・ `S-376`（新、表 T-206）・ `FR-106`（ポインタ `PK-11`） | E-04 ・ E-05 ・ E-06 ・ J-03 | 印の段の 6 文を消す ／ `S-376` の値 ／ `FR-106` の 1 文 |
| 2 本体を引くと線先も動く | 表 T-023d の結び（本体の段）・ `S-291` の備考 | E-04 ・ J-01 | 本体の段の 4 文を「`CM-51` で書く」の 1 文へ戻す |
| 3 線先を引いても本体は動かない | 表 T-023d の結び（線先の段） | E-04 | 線先の段の 2 文を「`CM-50` で書く」の 1 文へ戻す |
| 4 4 隅の 1 か所から出る | `FR-019` の RATIONALE ・ 表 T-221 の `LF-17`（新）と `LF-15` の注 ・ `SWS-4` | E-09 ・ E-12 ・ E-13 | `LF-17` の 1 行（「いつも左下」にすれば元の絵） |
| 5 見た目の列 | `FR-019` の RATIONALE（本書の段と、`CR-558` の範囲の 2 文）・ 表 T-017b の `CV-1` ・ `CV-9`（`CV-6` は行 ID で引くだけ）・ 表 T-108 の `CM-80` 〜 `CM-84`（新）・ `05-07-design.md` の読む段（`CR-558` の文）・ `erd.json` の 5 列（`AT-148` 〜 `AT-152`）・ 表 T-217 の `S-374` ・ `S-375`（新）と同表の段 ・ `S-312` の備考 ・ 表 T-016 の `PR-26` 〜 `PR-28`（新）・ 辞書 | E-01 ・ E-03 ・ E-07 〜 E-11 ・ E-14 ・ J-02 ・ J-04 〜 J-07 | 太さと透過率の既定と範囲は `S-374` ・ `S-375` の 1 行ずつ、色の `null` のときの値は `FR-019` の 1 文、色ごとの描き方は `FR-019` の 1 文ずつ（3 文）、透明を並べない欄は `CV-9` の 1 文 |

**数**: 仕様の文の編集 13（E-01 〜 E-14。E-02 は欠番）、原稿 JSON の編集 7（J-01 〜 J-07）。

⚠️ **`CR-551` 〜 `CR-554` ・ `CR-558` と重なる所**（13 節の道具で、`CR-551` の後の木に `CR-558` を当てた写しで数えた）:
- `CR-558`: E-01（`CV-1` の列の一覧。`CR-558` が `AT-146` を足した後ろに足す）・ E-06（`ZO-10`。`CR-558` が書き替えた行の後ろに足す）・ E-07 ・ E-08（`FR-019` の RATIONALE。`CR-558` の E-15 が足す「範囲へ寄せて描く」「命令は拒む」の 2 文に列と命令を足す）・ E-10 ・ E-11（`05-07-design.md` の読む段。`CR-558` の E-18 が足す 2 文に `CommentBox` を足す）は、`CR-558` の後の木でだけ 1 回現れる。
- `CR-551`: E-03（`CV-9`）は `CR-551` が書き直した行の中の、`CR-551` が変えなかった句を旧にする。E-05（`FR-106`）の旧は `CR-551` と `CR-558` が次に足した行の前の行である。
- `CR-553`: E-12 は 表 T-221 の `LF-15` の行を旧にする。`CR-553` の E-14 も `LF-15` の行を旧にして直後に `LF-16` を足すが、`LF-15` の行そのものは変えない ⇒ 本書は `LF-15` を書き換え、その直後（`LF-16` の前）に `LF-17` を置く。
- `CR-552` ・ `CR-554`: 仕様の旧は重ならない。`CR-554` はコードの置き場を動かす（9 節に「割る前の関数名 in ファイル」で書いた）。

---

## 2. 新しい識別子

⭐ 番号は 2026-09-24 に前に立つ者が `b7a3b76f` で詰めた（`CR-555` 〜 `CR-560` を CR の順に、配られた帯の先頭から隙間なく。帯の外の接頭辞は木の最大 M の次から）。下の「空いている」はその木で測った主張である。⚠️ 規則 02 の 2.5 節: **当てる直前に測り直すこと。** 測り方は 13 節。

| 種類 | 採ったもの | 測った最大（`b7a3b76f`、`CR-555` 〜 `CR-560` の草案を除く） |
|---|---|---|
| 設定値 | `S-374` ・ `S-375`（表 T-217）、`S-376`（表 T-206） | `CR-558` の `S-369` 〜 `S-373` の次（`S-999` は試験の番兵なので除く） |
| 表 | 立てない（前に立つ者の裁定: 注記の既定と範囲は 表 T-217 に 1 つ） | — |
| 接頭辞 | 立てない（`CB` は取り下げた） | — |
| 行 `LF` | `LF-17`（調整役が取り置いた。位置は `LF-15` と `LF-16` のあいだで、番号は位置に従わない） | `LF-16`（`CR-553` の草案） |
| 行 `CM` | `CM-80` 〜 `CM-84` | `CM-75`、`CR-563` の `CM-76`、`CR-558` の `CM-77` 〜 `CM-79` の次 |
| 行 `PR` | `PR-26` ・ `PR-27` ・ `PR-28` | 表 T-016 の行は調整役の帯 `PR-23` 〜 `PR-32` に限る。`PR-22` は `HighlightBox.strokeColor`（`CR-551` のセッション）、`PR-23` 〜 `PR-25` は `CR-558`。`PR-13` は退役しており二度と使わない |
| 列 `AT` | `AT-148` 〜 `AT-152`（`erd.json` の `seat` 148 〜 152） | `AT-144`、`CR-558` の `AT-145` 〜 `AT-147` の次 |
| 台帳 | `JDG-460` 〜 `JDG-464`（0.1 節の項目 1 〜 5）、`JDG-465` ・ `JDG-466`（11 節の問い 1 ・ 2 の答え）、`DFC-870` ・ `DFC-871` | 帯の中で `CR-558` の次。保留の行は起こさない（12.3 節） |
| 名 | 関数 `commentAnchorPointOf`（5 節の S-1）・ `withCommentBoxLookColumns`（S-6）、図の鍵 `comment-<id>-handle`（S-3）、命令 `setCommentBoxStrokeColor` ・ `setCommentBoxStrokeWidth` ・ `setCommentBoxFillColor` ・ `setCommentBoxFillTransparency` ・ `setCommentBoxTextColor`、`CommentBox` の列 `strokeColor` ・ `strokeWidthPx` ・ `fillColor` ・ `fillTransparencyPercent` ・ `textColor` は空いている | — |

⚠️ `PK` ・ `IV` ・ `ZO` ほかの行は立てない（`PK-11`（`CR-558`）・ `ZO-10` を使う）。
⚠️ `FR` ・ `IC` ・ `AM` ・ `UC` ・ `V` ・ `GL` ・ `CH` のどれも立てない（新しい要求を立てず、`FR-019` ・ `FR-106` と表の行に書く）。
- 6 本を詰めた後に返った帯: `S-377` 〜 `S-384`、`T-309`（`T-306` ・ `T-307` は古い文書が引くので飛ばした）、`DFC-874` 〜 `DFC-889`、`PND-561` 〜 `PND-580`（6 本とも保留の行を起こさない）、`PR-29` 〜 `PR-32`、`FR-137` 〜 `FR-148`、`IC-109` 〜 `IC-114`、`AM-20` 〜 `AM-24`（6 本とも使わない）。

---

## 3. ⛔ 消すものを先に列挙する（旧 → 新）

| 消すもの | 所（`ebc71984`） | 置き換わる先 | 編集 |
|---|---|---|---|
| `FR-019` の「留めた点と、本文の箱の左下隅とを、1 本の線で結ぶこと」 | `01-04-requirements.md:4796` | 4 隅のうち `LF-17` が選ぶ 1 隅 | E-09 |
| `FR-019` の「箱の基準隅は本要求の上の段が左下と定めている」（線が一意に定まる理由として） | `:4798` | 出る隅は `LF-17` が選ぶ。基準隅は保存の約束として別に残る | E-09 |
| `FR-019` の「線は注記の色で描くこと（MUST）」と「新しい設定値を立ててはならない（MUST NOT） —— 色は … 固定色と同じである」 | `:4800-4801` | 5 つの列と、`null` のときの値（表 T-217 の `S-374` ・ `S-375`、表 T-236 の `S-312` ・ `S-146` ・ `S-147`） | E-09 |
| 表 T-023d の結びの「本体 … と引出し線 … を掴めば `CM-51` で、線先 … を掴めば `CM-50` で書くこと（MUST）」 | `:3402` | 本体と引出し線は `CM-50` ＋ `CM-51` の束（箱と線先が一緒に動く）、線先も `CM-50` ＋ `CM-51` の束（箱は動かない） | E-04 |
| 同じ結びの「箱は不透明に描くので、素通しにすると見えないタスクを掴むことになる」（MUST NOT の理由） | `:3403` | 塗りの透過率によらず本体として応える、に替える（MUST NOT は残す） | E-04 |
| `S-291` の備考「引けば本文の箱が動く（`CM-51`）」 | `_source/settings.json:3286`（生成物 `tbl-settings.md:351`） | 箱と線先が同じ量だけ動く（束） | J-01 |
| `S-312` の備考「コメントボックスの枠と引出しの線」（無条件に注記の色で描く読み） | `_source/settings.json:4749`（生成物 `tbl-settings.md:457`） | 線の色を指定しないコメントボックスの枠と引出し線 | J-02 |
| `LF-15` の注「本文の箱は留めた点からのずれで置く（`FR-019`）ので、アンカーと共に動く」 | `05-07-design.md:1285` | ずれを書かずにアンカーだけを書けば共に動く。掴んで引いたときに何を書くかは 表 T-023d の結び | E-12 |
| `T-217` の段の「収まる先が `HighlightBox` の列だからである」 | `_source/settings.json:6416` | 注記（`HighlightBox` と `CommentBox`）の列 | J-04 |
| コードの定数（⛔ 本書は直さない。9 節） | `schedule-overlays.ts:217` ・ `:224` の `stroke-width="1"`、`:224` の `S-146`、`:230` の `S-147`、`:216` の左下隅、`schedule-geometry.ts:152-154` の `leaderOf` の左下隅、`item-grab.ts:108-118` ・ `:477-488` の 1 命令ずつの書き込み | 表 T-217 ・ `FR-019` ・ `LF-17` ・ E-04 | 実装する者 |

---

## 4. 書き直す所（当てる体がそのまま使う文）

⛔ **作法**（`CR-551` と同じ）: 各編集は「旧」を「新」で置き換える。**置き換える前に、旧がそのファイルに 1 回だけ現れることを数えること**（13 節の道具が 13 件すべてを、`CR-551` の後の木に `CR-558` を当てた写しで数え、写しに順に当てた）。旧・新は、`<!-- EDIT … -->` の直後の 2 つの `text` の塊である。`kind=sub` の編集は、長い 1 行の中の部分の字だけを置き換える。
⚠️ 同じファイルの編集は上から順に当てること。
⚠️ 生成物（`_assets/tbl-settings.md` ・ `tbl-property-items.md` ・ `fig-erd-*.md` ・ `_source/grs-document.schema.json` と `src/` の生成物）は手で直さない。原稿を直して `npm run gen` を打つ。
⚠️ `CR-558` の文が当てた後で変わっていて旧が 1 回にならないときは、当てる者が前に立つ者に返す。

### 4.1 文の編集（`01-04-requirements.md` ・ `05-07-design.md` ・ `_assets/tbl-glossary.md`）

<!-- EDIT id=E-01 file=docs/spec/01-04-requirements.md kind=sub -->
項目 5。表 T-017b の `CV-1` の末尾（色の列の一覧。`CR-558` の E-04 の後の文）。旧
```text
`AT-121` ・ `AT-146` が持つ |
```
新
```text
`AT-121` ・ `AT-146` ・ `AT-148` ・ `AT-150` ・ `AT-152` が持つ |
```

⚠️ E-02 は取り下げた（欠番）—— 表 T-017b の `CV-6` は「どの欄の値かは描く所で決める」と定める（`CR-564` の後の文）。⇒ コメントボックスの 3 つの色の描き方は、描く所である `FR-019` の本書の段（E-09）に書き、`CV-6` は行 ID で引く。

<!-- EDIT id=E-03 file=docs/spec/01-04-requirements.md kind=sub -->
項目 5。表 T-017b の `CV-9`（`CR-551` の後の文）。旧
```text
<br>行の色の欄には黒を並べない（表 T-294 の `S-315`）。
```
新
```text
<br>コメントボックスの線の欄と字の欄には透明を並べない（`FR-019`）。<br>その 2 つの欄でも透明の場所は空けたままとし、テーマに戻す入口はその後ろに置く。<br>行の色の欄には黒を並べない（表 T-294 の `S-315`）。
```

<!-- EDIT id=E-04 file=docs/spec/01-04-requirements.md -->
項目 1 ・ 2 ・ 3。表 T-023d の結びの `GR-14` の段のうち、コメントボックスの 3 行（`CR-558` はこの 3 行を触らない —— 同書の 5.1 節）。旧
```text
コメントボックスは本体・引出し線・線先を持つ。  
本体（本文の箱の全体）と引出し線（線から左右へ同表の `S-291`）を掴めば `CM-51` で、線先（線先から同表の `S-292`）を掴めば `CM-50` で書くこと（MUST）。  
⛔ 本文の箱の中を素通しにしてはならない（MUST NOT） —— 箱は不透明に描くので、素通しにすると見えないタスクを掴むことになる（`FR-108`）。  
```
新
```text
コメントボックスは本体・引出し線・線先を持つ。  
⭐ 本体（本文の箱の全体）か引出し線（線から左右へ同表の `S-291`）を掴んで引いたときは、本文の箱と線先を一緒に動かすこと（MUST） —— 本文の箱は引いた量だけ動かし、線先は、押したときに描いていた線先の点を引いた量だけずらした点で読む（読み方は下の「線先は離した位置で読む」と同じ）。  
その 2 つは、`_assets/tbl-glossary.md` の 表 T-108 の `CM-50`（線先）と `CM-51`（ずれ）を 1 つの束にして書くこと（MUST） —— `CM-51` のずれは、置き直した線先を描く点（`05-07-design.md` の 表 T-221 の `LF-15`）から、動かした本文の箱の左下隅までとする。  
⚠️ 線先は日の列の中央と行の帯の中央に立つので、線先の動く量は、引いた量から日の列と行の帯の半分まで離れることがある —— 本文の箱は引いた量のとおりに動く。  
ずらした点の下に描かれた行が無いときは、どちらも動かさず、表 T-233 の `RS-44` を告げる —— 表 T-246 の `HB-3` と同じ扱いである。  
⭐ 線先（線先から同表の `S-292`）を掴んで引いたときは、線先だけを動かし、本文の箱を画面の上で動かさないこと（MUST） —— `CM-50` で線先を、`CM-51` で、置き直した線先を描く点から押したときの本文の箱の左下隅までのずれを、1 つの束にして書く。  
⚠️ 本文の箱は線先からのずれで置く（`FR-019`）ので、`CM-50` だけを書くと箱が線先と一緒に動く —— `CM-51` を同じ束に入れるのは、それを打ち消すためである。  
⭐ どちらも 1 つの束なので、1 度の取り消し（`FR-031`）で 2 つとも戻る。  
⭐ コメントボックスを選んでいるあいだ（表 T-023c の `SL-8`）は、線先に四角の印を描き、線先を掴めることを示すこと（MUST）。  
印は、線先の点を中心とする一辺 `_assets/tbl-settings.md` の 表 T-206 の `S-376` の正方形とし、中を同書の 表 T-236 の `S-146`、縁を同表の `S-151` で、縁の太さを 表 T-206 の `S-174` で描くこと（MUST）。  
⛔ 印に倍率を掛けてはならない（MUST NOT） —— 印が示す掴み代 `S-292` は画面の px である。  
重ね順は 表 T-020 の `ZO-10` とする —— 線先が本文の箱の中やタスクの上に在っても、印は隠れない。  
⛔ 選んでいないコメントボックスの線先に印を描いてはならない（MUST NOT） —— 選んでいない付箋の線先にまで四角が並ぶと、日程の形と見分けにくい。  
⚠️ 印は見せ方だけであり、掴み代を変えない —— 線先は、選んでいてもいなくても `S-292` で応える。  
⛔ 本文の箱の中を素通しにしてはならない（MUST NOT） —— 箱の中は、塗りの透過率（`_assets/fig-erd-detail.md` の `AT-151`、`FR-019`）によらず本体として応える。  
透過率で応え方を変えると、同じ所を押しても、本文を掴むか下のタスクを掴むかが塗りの濃さで変わる。  
⚠️ 透過率が 0（不透明）のまま素通しにすると、見えないタスクを掴むことになる（`FR-108`）。  
```

<!-- EDIT id=E-05 file=docs/spec/01-04-requirements.md -->
項目 1。`FR-106` の STATEMENT（`CR-551` の後の木では、この行の次に `PK-10` の行、`CR-558` の後はさらにハイライトボックスの行が続く。本書の行はこの行の直後に入る）。旧
```text
⭐ 押す役と引く役を兼ねる進捗マーカー（表 T-270 の `PE-8`）は、押す役の形（指）とすること（MUST）。
```
新
```text
⭐ 押す役と引く役を兼ねる進捗マーカー（表 T-270 の `PE-8`）は、押す役の形（指）とすること（MUST）。
⭐ コメントボックスの線先の掴み代（表 T-023d の `GR-14`、`_assets/tbl-settings.md` の 表 T-206 の `S-292`）では、表 T-269 の `PK-11` とすること（MUST） —— 線先は日と行の両方へ動く取っ手であり、上下左右へ動かす形が、掴んで動かせることを示す。  
```

<!-- EDIT id=E-06 file=docs/spec/01-04-requirements.md kind=sub -->
項目 1。表 T-020 の `ZO-10`（`CR-558` の E-14 が書き替えた行の末尾）。旧
```text
・ハイライトボックスの掴み点（選んでいる箱にだけ出す。<br>`FR-016` の 表 T-246） |
```
新
```text
・ハイライトボックスの掴み点（選んでいる箱にだけ出す。<br>`FR-016` の 表 T-246）・コメントボックスの線先の印（選んでいるコメントボックスにだけ出す。<br>表 T-023d の結び） |
```

<!-- EDIT id=E-07 file=docs/spec/01-04-requirements.md kind=sub -->
項目 5。`FR-019` の RATIONALE（`CR-558` の E-15 が足した「範囲へ寄せて描く」文の列の一覧）。旧
```text
表 T-217 の数値の列（`AT-122` ・ `AT-145` ・ `AT-147`）
```
新
```text
表 T-217 の数値の列（`AT-122` ・ `AT-145` ・ `AT-147` ・ `AT-149` ・ `AT-151`）
```

<!-- EDIT id=E-08 file=docs/spec/01-04-requirements.md kind=sub -->
項目 5。同じく `CR-558` の E-15 が足した「命令は拒む」文の命令の一覧。旧
```text
命令（表 T-108 の `CM-77` ・ `CM-79`）は拒むこと（MUST）
```
新
```text
命令（表 T-108 の `CM-77` ・ `CM-79` ・ `CM-81` ・ `CM-83`）は拒むこと（MUST）
```

<!-- EDIT id=E-09 file=docs/spec/01-04-requirements.md -->
項目 4 ・ 5。`FR-019` の RATIONALE の引出しの段（`CR-558` はこの 6 行を触らない —— 同書の 5.1 節）。旧
```text
⭐ 描き方はこうである（MUST）: 留めた点と、本文の箱の左下隅とを、1 本の線で結ぶこと。  
⛔ **途中に曲がりを置いてはならない（MUST NOT）** —— 曲がりを置けば「どこで曲がるか」を別に決めることになり、1 種に絞った理由（決めることを増やさない）が失われる。  
⭐ **端点が 2 つとも既に決まっているので、この線は一意に定まる** —— 留めた点は本要求が日付と行の識別子で持ち、箱の基準隅は本要求の上の段が左下と定めている。  
⛔ **吹き出しの尻尾にしてはならない（MUST NOT）** —— **尻尾は「箱のどの辺のどこから出るか」を別に決める必要がある。**  
⭐ 線は注記の色で描くこと（MUST）。  
新しい設定値を立ててはならない（MUST NOT） —— **色は本段がハイライトボックスについて述べる固定色と同じである。**  
```
新
```text
⭐ 描き方はこうである（MUST）: 留めた点と、本文の箱の 4 隅のうち `05-07-design.md` の 表 T-221 の `LF-17` が選ぶ 1 隅とを、1 本の線で結ぶこと。  
⛔ **途中に曲がりを置いてはならない（MUST NOT）** —— 曲がりを置けば「どこで曲がるか」を別に決めることになり、1 種に絞った理由（決めることを増やさない）が失われる。  
⭐ **端点が 2 つとも決まるので、この線は一意に定まる** —— 留めた点は本要求が日付と行の識別子で持ち、出る隅は `LF-17` が留めた点と箱の位置から選ぶ。  
⚠️ 出る隅は、ずれの基準隅（上の段の左下）とは別のものである —— 基準隅は箱の置き場を保存する約束であり、線がどこから出るかを決めない。  
⛔ **吹き出しの尻尾にしてはならない（MUST NOT）** —— **尻尾は「箱のどの辺のどこから出るか」を別に決める必要がある。**  
⭐ コメントボックスの線（本文の箱の枠と引出し線）の色と太さ、本文の箱の塗りの色と透過率、本文の字の色も指定できること（MUST） —— 列は `_assets/fig-erd-detail.md` の `AT-148` 〜 `AT-152`、太さと透過率の既定と範囲は `_assets/tbl-settings.md` の 表 T-217 の `S-374` ・ `S-375`、入口は `_assets/tbl-property-items.md` の 表 T-016 の `PR-26` 〜 `PR-28` が持つ。  
⭐ 太さと透過率の列が `null` のときは 表 T-217 の同じ列の既定で、色の 3 列が `null` のときは、線を注記の色（`_assets/tbl-settings.md` の 表 T-236 の `S-312`）、塗りを地の色（同表の `S-146`）、字を文字の色（同表の `S-147`）で描くこと（MUST） —— 5 列がすべて `null` の箱は、列を足す前と同じ絵になる。  
⭐ 置くとき（`_assets/tbl-glossary.md` の 表 T-108 の `CM-46`）は 5 列を `null` とすること（MUST） —— 既定の 1 行を替えれば、値を置いていない箱がすべて追随する。  
⭐ 枠と引出し線は 1 つの色と 1 つの太さで描く —— 2 つは付箋の縁から指す先まで続く 1 本の線として読まれる。  
⛔ 線の太さに倍率を掛けてはならない（MUST NOT） —— 本文の箱は画面の px で置き（`FR-097`）、ずれも画面の距離で持つ（上の段）。  
⭐ `CommentBox.fillColor`（`AT-150`）は、本文の箱の中を、その色の塗りの値で、不透明度を 1 − `AT-151` ÷ 100 として塗ること（MUST） —— 描く値の選び方は 表 T-017b の `CV-6` に従い（パレット色は `_assets/tbl-settings.md` の 表 T-294 の塗りの値、カスタムカラーは `CV-3` で決まった値そのもの）、`AT-151` は透過率（0 は不透明、100 は透明）である。  
⭐ `CommentBox.strokeColor`（`AT-148`）は、本文の箱の枠と引出し線を、その色の縁の値で描くこと（MUST） —— 描く値の選び方は 表 T-017b の `CV-6` に従う（パレット色は 表 T-294 の縁の値、カスタムカラーは `CV-3` で決まった値そのもの）。  
⭐ `CommentBox.textColor`（`AT-152`）は、本文の字を、その色の縁の値で描くこと（MUST） —— 描く値の選び方は 表 T-017b の `CV-6` に従う（同上）。  
⚠️ 3 つとも実績の塗りではないので、`CV-10`（カスタムカラーの実績の塗りを導く）は当たらない。  
透過率は塗りにだけ掛け、枠・引出し線・字には掛けない。  
⛔ コメントボックスの線の色と字の色に透明を選ばせてはならない（MUST NOT） —— 引出し線が消えると、どのタスクやマイルストーンについてのコメントかが読めず、字が消えると本文が消える。  
塗りには透明を選ばせてよい。  
```

<!-- EDIT id=E-10 file=docs/spec/05-07-design.md kind=sub -->
項目 5。表 T-220 の前の段（`CR-558` の E-18 が足した文）に `CommentBox` を足す。旧
```text
`AT-145` ・ `AT-146` ・ `AT-147` の列を持たない `HighlightBox` の項には、その列を `null` として足して読むこと（MUST） —— 3 列を足す前に保存した文書は、その鍵を持たない。
```
新
```text
`AT-145` ・ `AT-146` ・ `AT-147` の列を持たない `HighlightBox` の項と、`AT-148` 〜 `AT-152` の列を持たない `CommentBox` の項には、その列を `null` として足して読むこと（MUST） —— 列を足す前に保存した文書は、その鍵を持たない。
```

<!-- EDIT id=E-11 file=docs/spec/05-07-design.md kind=sub -->
項目 5。同じ段の次の文（`CR-558` の E-18）—— コメントボックスの色の `null` は 表 T-217 の既定でなく 表 T-236 の色で描くので、読みを広げる。旧
```text
`null` は `_assets/tbl-settings.md` の 表 T-217 の既定で描く値なので（`01-04-requirements.md` の `FR-019`）、
```
新
```text
`null` は `_assets/tbl-settings.md` の 表 T-217 の既定か、`01-04-requirements.md` の `FR-019` が名指す色で描く値なので、
```

<!-- EDIT id=E-12 file=docs/spec/05-07-design.md -->
項目 4。表 T-221 の `LF-15` を書き換え、その直後に `LF-17` を置く（`CR-553` の後の木では、`LF-17` の次に `LF-16` が続く）。旧
```text
| LF-15 | コメントボックスのアンカーの置き場 | 横は `anchorDate` の日の列の中央、縦は `anchorGroupId` の行が描かれた帯（`LF-2` / `LF-3`、ピン止めした行は `LF-14`）の中央とする。<br>⚠️ ハイライトボックスの左端と右端は日の列の端に立つ（`01-04-requirements.md` の `FR-019`）が、アンカーは日を 1 つ指す点なので、その日の列の中に置く。<br>⭐ 帯の上端と下端は採らない —— そちらはハイライトボックスが行の範囲を囲むのに使う。<br>⚠️ 本文の箱は留めた点からのずれで置く（`FR-019`）ので、アンカーと共に動く |
```
新
```text
| LF-15 | コメントボックスのアンカーの置き場 | 横は `anchorDate` の日の列の中央、縦は `anchorGroupId` の行が描かれた帯（`LF-2` / `LF-3`、ピン止めした行は `LF-14`）の中央とする。<br>⚠️ ハイライトボックスの左端と右端は日の列の端に立つ（`01-04-requirements.md` の `FR-019`）が、アンカーは日を 1 つ指す点なので、その日の列の中に置く。<br>⭐ 帯の上端と下端は採らない —— そちらはハイライトボックスが行の範囲を囲むのに使う。<br>⚠️ 本文の箱は留めた点からのずれで置く（`FR-019`）ので、ずれを書かずにアンカーだけを書くと、箱もアンカーと共に動く —— 掴んで引いたときに何を書くかは、同書の 表 T-023d の結びが持つ |
| LF-17 | コメントボックスの引出し線が本文の箱から出る隅 | 横は、留めた点（`LF-15`）が本文の箱の横の中点より右に在れば右、そうでなければ左の隅とする。<br>縦は、留めた点が箱の縦の中点より上に在れば上、そうでなければ下の隅とする。<br>⭐ この 2 つで選ぶ隅は、4 隅のうち留めた点に最も近い隅である —— 横の差と縦の差をそれぞれ最も小さくする隅が、直線の距離も最も小さくする。<br>⭐ 中点に等しいときは左と下を採る —— 等しい 2 隅のどちらを採っても線の長さは同じであり、左下はずれの基準隅（`01-04-requirements.md` の `FR-019`）と同じ隅である。<br>⭐ 留めた点が本文の箱の中（縁を含む）に在るときは、引出し線を描かない —— 箱から外へ出る線が無く、隅から中の点へ引くと本文の上を横切る。<br>⚠️ 線先の印（同書の 表 T-023d の結び）はそのときも描く（重ね順は同書の 表 T-020 の `ZO-10`）ので、選んでいれば箱の中の線先も見えて掴める |
```

<!-- EDIT id=E-13 file=docs/spec/05-07-design.md kind=sub -->
項目 4。`SWS-4` の STATEMENT。旧
```text
コメントボックスのアンカーの置き場を 表 T-221 の `LF-6` 〜 `LF-11` と `LF-13` と `LF-15` に、
```
新
```text
コメントボックスのアンカーと引出し線の置き場を 表 T-221 の `LF-6` 〜 `LF-11` と `LF-13` と `LF-15` と `LF-17` に、
```

<!-- EDIT id=E-14 file=docs/spec/_assets/tbl-glossary.md -->
項目 5。表 T-108 の `CommentBox` の群の末尾（`CM-51` の後）に 5 行を足す。命令の名は `CR-558` の `CM-77` 〜 `CM-79` と同じ作り。旧
```text
| CM-51 | `CommentBox` | `setCommentBoxBodyOffsetPx` | — | 本文のずれを変える | `FR-016` |
```
新
```text
| CM-51 | `CommentBox` | `setCommentBoxBodyOffsetPx` | — | 本文のずれを変える | `FR-016` |
| CM-80 | `CommentBox` | `setCommentBoxStrokeColor` | — | 枠と引出し線の色を置く | `FR-019` |
| CM-81 | `CommentBox` | `setCommentBoxStrokeWidth` | — | 枠と引出し線の太さを置く | `FR-019` |
| CM-82 | `CommentBox` | `setCommentBoxFillColor` | — | 本文の箱の塗りの色を置く | `FR-019` |
| CM-83 | `CommentBox` | `setCommentBoxFillTransparency` | — | 本文の箱の塗りの透過率を置く | `FR-019` |
| CM-84 | `CommentBox` | `setCommentBoxTextColor` | — | 本文の字の色を置く | `FR-019` |
```

### 4.2 原稿 JSON の編集（`_source/`）

⛔ 各塊は、当てる前に原稿を `json.loads` で読み、名指した項が 1 つだけ在ることを確かめてから当てること。当てた後は `npm run gen` の前に、手で書いた検証の形（`*.schema.json`）で確かめること（13 節で、`CR-558` の J-03 を当てた写しに当てて 0 件を確かめた）。

J-01（項目 2）`settings.json` の `S-291` —— `note` だけを変える。旧 `"同上。⭐ 引けば本文の箱が動く（`CM-51`）。⭐ 3px は利用者が指定した値である"` → 新:
```json
{
 "ja": "同上。⭐ 引けば本文の箱と線先が同じ量だけ動く（`CM-50` と `CM-51` の束。表 T-023d の結び）。⭐ 3px は利用者が指定した値である"
}
```

J-02（項目 5）`settings.json` の `S-312` —— `note` だけを変える。旧の頭の句 `"線の色を指定しないハイライトボックスの枠と、コメントボックスの枠と引出しの線（`FR-019`）。"` → 新（後ろの句は変えない）:
```json
{
 "ja": "線の色を指定しないハイライトボックスの枠と、線の色を指定しないコメントボックスの枠と引出し線（`FR-019`）。テーマの色相に追随させない（`FR-041`）。⚠️ 色相 26 は依存線（`S-159`）と同じであり、離すことは `FR-019` が SHOULD に留めている。⚠️ 暗いテーマも明るいテーマと同じ値である —— 暗いテーマで選び直していない"
}
```

J-03（項目 1）`settings.json` の 表 T-206 の `S-292` の直後に 1 行を足す（`CR-558` の `S-372` ・ `S-373` は `S-293` の直後に入り、重ならない）:
```json
{
 "id": "S-376",
 "value": {
  "ja": "コメントボックスの線先の印の一辺（表 T-023d の結び）"
 },
 "default": {
  "num": "8",
  "suffix": "px",
  "mark": "🔎"
 },
 "note": {
  "ja": "見せ方の寸法であり、日程の内容ではないので保存しない。⭐ 印は掴み代の中に収める —— 一辺は `S-292` の 2 倍を超えないこと（超えると、描いた印の縁を押しても掴めない）。⛔ **倍率に追随させない** —— `S-292` と同じ画面の px である。⛔ **測って決めた値ではない** （🔎）"
 }
}
```

J-04（項目 5）`settings.json` の 表 T-217 —— `rows` の `S-371`（`CR-558` の J-03 が足す最後の項）の後に 2 行を足し、後ろの段の文を替える。足す 2 行（形は `CR-558` の `S-369` ・ `S-371` と同じ）:
```json
[
 {
  "id": "S-374",
  "key": "`CommentBox.strokeWidthPx`",
  "type": {
   "ja": "整数"
  },
  "default": {
   "num": "1",
   "code": true,
   "mark": "🔎"
  },
  "min": {
   "num": "1"
  },
  "max": {
   "num": "8"
  },
  "note": {
   "ja": "コメントボックスの本文の箱の枠と引出し線の太さ（px、`AT-149`）。列が `null` のときに描く値である。ズームによらず一定に描く規則は `FR-019` が持つ。既定は、本行より前のコードが描いていた太さ。下限は、1px を割ると画面の画素に届かず線が消えるからである（表 T-257 の `RR-5` と同じ理由）。上限は測って決めた値ではない"
  }
 },
 {
  "id": "S-375",
  "key": "`CommentBox.fillTransparencyPercent`",
  "type": {
   "ja": "整数"
  },
  "default": {
   "num": "0",
   "code": true
  },
  "min": {
   "num": "0"
  },
  "max": {
   "num": "100"
  },
  "note": {
   "ja": "コメントボックスの本文の箱の塗りの透過率（`AT-151`）。列が `null` のときに描く値である。0 は不透明、100 は透明（`FR-019`）。⭐ 既定の 0 は、本行より前の不透明な箱と同じ絵である"
  }
 }
]
```
後ろの段（`kind: prose` の 2 行目）の旧 `"⚠️ **本表の値は日程データの群に入る** —— 収まる先が `HighlightBox` の列だからである。**どちらの群に属するかの判定は表 T-052 が持つ** （表 T-209 と同じ扱いである）。"` → 新:
```json
{
 "ja": "⚠️ **本表の値は日程データの群に入る** —— 収まる先が注記（`HighlightBox` と `CommentBox`）の列だからである。**どちらの群に属するかの判定は表 T-052 が持つ** （表 T-209 と同じ扱いである）。⚠️ `S-132` は置くときに列へ写す。`S-369` 〜 `S-371` と `S-374` ・ `S-375` は写さない —— 列は `null` のまま置き、描くときに本表の既定で描く（`FR-019`）"
}
```

J-05（項目 5）`erd.json` の `CommentBox`（`seat` 13）—— `columns` の `bodyOffsetPx`（`seat` 115）の後に 5 列を足す。`min` / `max` / `default` は書かない（範囲と `null` のときの値は 表 T-217 と `FR-019` が持つ —— `CR-558` の J-04 と同じ）:
```json
[
 {
  "seat": 148,
  "name": "strokeColor",
  "type": "文字列",
  "nullable": "可（`null` = 注記の色 `S-312`）",
  "json": {
   "kind": "color",
   "null": true,
   "transparent": false
  },
  "key": "",
  "origin": "GRS",
  "exchange": "",
  "meaning": {
   "ja": "本文の箱の枠と引出し線の色。形は `AT-102` と同じ。ただし透明は取らない（`FR-019`）"
  }
 },
 {
  "seat": 149,
  "name": "strokeWidthPx",
  "type": "整数（px）",
  "nullable": "可（`null` = 表 T-217 の既定）",
  "json": {
   "kind": "integer",
   "null": true
  },
  "key": "",
  "origin": "GRS",
  "exchange": "",
  "meaning": {
   "ja": "枠と引出し線の太さ。`null` と範囲は `_assets/tbl-settings.md` の表 T-217 の `S-374`。ズームによらず一定に描く（`FR-019`）"
  }
 },
 {
  "seat": 150,
  "name": "fillColor",
  "type": "文字列",
  "nullable": "可（`null` = 地の色 `S-146`）",
  "json": {
   "kind": "color",
   "null": true
  },
  "key": "",
  "origin": "GRS",
  "exchange": "",
  "meaning": {
   "ja": "本文の箱の塗りの色。形は `AT-102` と同じ。透明も取る（`FR-019`）"
  }
 },
 {
  "seat": 151,
  "name": "fillTransparencyPercent",
  "type": "整数（0〜100）",
  "nullable": "可（`null` = 表 T-217 の既定）",
  "json": {
   "kind": "integer",
   "null": true
  },
  "key": "",
  "origin": "GRS",
  "exchange": "",
  "meaning": {
   "ja": "本文の箱の塗りの透過率。**0 は不透明、100 は透明**。`null` と範囲は `_assets/tbl-settings.md` の表 T-217 の `S-375`（`FR-019`）"
  }
 },
 {
  "seat": 152,
  "name": "textColor",
  "type": "文字列",
  "nullable": "可（`null` = 文字の色 `S-147`）",
  "json": {
   "kind": "color",
   "null": true,
   "transparent": false
  },
  "key": "",
  "origin": "GRS",
  "exchange": "",
  "meaning": {
   "ja": "本文の字の色。形は `AT-102` と同じ。ただし透明は取らない（`FR-019`）"
  }
 }
]
```

J-06（項目 5）`property-items.json` —— `items` の `PR-21` の後に 3 行を足す（`CR-558` の `HighlightBox` の行が近くに在っても、`appliesTo` が面を分けるので並びは食い違わない。`CR-558` の 5.1 節の申し送りどおり `PR-21` の後、`PR-23` より前）:
```json
[
 {
  "id": "PR-26",
  "columns": [
   "strokeWidthPx"
  ],
  "inputKinds": [
   "数値"
  ],
  "appliesTo": "CommentBox",
  "note": {
   "ja": "本文の箱の枠と引出し線の太さ（px）。既定と範囲は `_assets/tbl-settings.md` の表 T-217 の `S-374`"
  },
  "mspdi": {
   "ja": "無い（`GRS JSON` のみ）"
  }
 },
 {
  "id": "PR-27",
  "columns": [
   "fillTransparencyPercent"
  ],
  "inputKinds": [
   "数値"
  ],
  "appliesTo": "CommentBox",
  "note": {
   "ja": "本文の箱の塗りの透過率。0 は不透明、100 は透明。既定と範囲は同表の `S-375`"
  },
  "mspdi": {
   "ja": "無い（`GRS JSON` のみ）"
  }
 },
 {
  "id": "PR-28",
  "columns": [
   "strokeColor",
   "fillColor",
   "textColor"
  ],
  "inputKinds": [
   "色",
   "色",
   "色"
  ],
  "appliesTo": "CommentBox",
  "note": {
   "ja": "枠と引出し線・本文の箱の塗り・本文の字の色。`null` ＝ `FR-019` が名指す色（`S-312` ・ `S-146` ・ `S-147`）。⭐ 色の行なので、同じ対象の行の並びの末尾に置く（`FR-006`）"
  },
  "mspdi": {
   "ja": "無い（`GRS JSON` のみ）"
  }
 }
]
```

J-07（項目 5）`display-words.json` の `properties` —— `PR-21` の後に 3 項を足す（`PR-26` ・ `PR-27` の語は `CR-558` の `PR-23` ・ `PR-24` と同じ）:
```json
[
 {
  "rowId": "PR-26",
  "label": {
   "ja": "線の太さ",
   "en": "stroke width"
  }
 },
 {
  "rowId": "PR-27",
  "label": {
   "ja": "塗りの透過率（%）",
   "en": "fill transparency (%)"
  }
 },
 {
  "rowId": "PR-28",
  "label": {
   "ja": "線 / 塗り / 文字",
   "en": "stroke / fill / text"
  }
 }
]
```

### 4.3 生成器と生成物（形で示す）

| 何 | 何をする |
|---|---|
| `tools/generate_entity_types.py:1777-1810`（`annotation_defaults_block`） | ⛔ いまは 表 T-217 がちょうど 1 行（`S-132`）でないと `SystemExit` で止まる（`:1796-1798`）。`CR-558` の手番が全行を `S-` の行 ID で読む形に直す（同書の 4.3 節）⇒ 本書の手番は、`NOT_STORED_ANNOTATION_SIZES` に `'S-374'` ・ `'S-375'` が刷られることを確かめるだけ。⚠️ `CR-558` の直しが無い木に当てると `npm run gen` が止まる |
| `npm run gen` | 上の原稿から `_assets/tbl-settings.md` ・ `tbl-property-items.md` ・ `fig-erd-overview.md` ・ `fig-erd-detail.md` ・ `_source/grs-document.schema.json` と、`src/` の生成物（`NOT_STORED_ANNOTATION_SIZES` ・ `property-items.json` ・ `display-words.json` ・ `schedule.ts` の `CommentBox` の型・ `json-codec.ts` のスキーマの区画・ 検証器）を刷る |
| `tools/generate_startup_template.py` ・ `sample-schedule/*.json` ・ `startup-template.json` | コメントボックスを持たない（`leaderShapeKind` が 0 か所 —— 13 節）ので直す所は無い |

---

## 5. 継ぎ目 —— 両側の依頼文にこのまま写すこと

| # | 間 | 継ぎ目（逐語） |
|---|---|---|
| S-1 | 仕様 ↔ 幾何 ↔ 翻訳（線先の点） | `commentAnchorPointOf(layout: ScheduleLayout, day: CalendarDay, groupId: string): Point \| null` を配置の側に 1 つ置き、`LF-15` の点（日の列の中央、行の帯の中央）を返す。描く側（`commentGeometry`）と翻訳（`item-grab.ts`）の両方がこれを呼ぶ。⛔ 式を 2 か所に書かない |
| S-2 | 仕様 ↔ 幾何（引出し線） | `leaderOf(comment: CommentGeometry): Path \| null`。返す経路は `[anchor, corner]`、`corner` は `LF-17`（横: `anchor.x > body.x + body.width / 2` なら右、縦: `anchor.y < body.y + body.height / 2` なら上、等しいときは左と下）。`anchor` が箱の中（縁を含む）なら `null`。当たり（`noteHitOf`）と描き手は `null` を「線なし」と読む |
| S-3 | 仕様 ↔ 描き手（`schedule-overlays.ts`） | 線の色 ＝ `strokeColor` を縁の値で（`null` なら `S-312`）。太さ ＝ `strokeWidthPx ?? S-374 の既定` を `S-374` の下限と上限へ寄せた値。塗り ＝ `fillColor` を塗りの値で（`null` なら `S-146`）、`fill-opacity` ＝ 1 − （`fillTransparencyPercent ?? S-375 の既定` を 0 〜 100 へ寄せた値）÷ 100。字 ＝ `textColor` を縁の値で（`null` なら `S-147`）。枠と引出し線は同じ色と太さ。印は選んでいる箱だけ、`selectionParts` に `<rect>`（中心 ＝ 線先、一辺 `S-376`、`fill` ＝ `S-146`、`stroke` ＝ `S-151`、`stroke-width` ＝ `S-174`）、図の鍵 `comment-<id>-handle` |
| S-4 | 仕様 ↔ 翻訳（`item-grab.ts`、引いて離したとき） | 本体と引出し線（`boxPart.kind` が `body` か `leader`）: `d = 離した点 − 押した点`、線先' ＝ `commentAnchorAt(layout, 線先の点.x + d.x, 線先の点.y + d.y)`、ずれ' ＝ （箱の左端 ＋ d.x − 線先'の点.x、箱の下端 ＋ d.y − 線先'の点.y）。線先（`anchor`）: 線先' ＝ `commentAnchorAt(layout, 離した点)`、ずれ' ＝ （箱の左端 − 線先'の点.x、箱の下端 − 線先'の点.y）。どちらも `changed([{ kind: 'setCommentBoxAnchor', … }, { kind: 'setCommentBoxBodyOffsetPx', … }])` の 1 束。行が無ければ `nothingToDo('noRowToPutTheAnnotationOn')`（`RS-44`）で何も書かない。「箱の左端・下端」「線先の点」は押した時点の幾何の値 |
| S-5 | 仕様 ↔ 命令（`edit-annotation.ts`） | `{ kind: 'setCommentBoxStrokeColor', id, strokeColor: string \| null }` ・ `{ kind: 'setCommentBoxStrokeWidth', id, strokeWidthPx: number \| null }` ・ `{ kind: 'setCommentBoxFillColor', id, fillColor: string \| null }` ・ `{ kind: 'setCommentBoxFillTransparency', id, fillTransparencyPercent: number \| null }` ・ `{ kind: 'setCommentBoxTextColor', id, textColor: string \| null }`（形は `CR-558` の S-6 と同じ）。拒み: 無い箱 `reject('CM-80' 〜 'CM-84' のそれぞれ, 'AT-110', …)`、線と字の透明 `reject('CM-80' / 'CM-84', 'FR-019', …)`、名でもカスタムカラーでもない色 `reject(…, 'CV-1', …)`、整数でない ／ 範囲の外 `reject('CM-81', 'S-374', …)` ／ `reject('CM-83', 'S-375', …)`。`createCommentBox` は 5 列を `null` で置く |
| S-6 | 仕様 ↔ 読み込み（`json-codec.ts`） | `withCommentBoxLookColumns(parsed)`: スキーマの前に、`schedule.commentBoxes` の各項で 5 つの鍵（`strokeColor` ・ `strokeWidthPx` ・ `fillColor` ・ `fillTransparencyPercent` ・ `textColor`）のうち無いものを `null` で足す。版を見ない。通知しない。⭐ `CR-558` の `withHighlightBoxLookColumns` と同じ所で呼ぶ |
| S-7 | 仕様 ↔ パネル（`properties-panel.ts` ・ `field-commit.ts`） | `PR-26` ・ `PR-27` は数値の欄（`min` / `max` は `S-374` ・ `S-375` の下限・上限）、`PR-28` の 3 欄は `CR-551` の色の欄。線と字の欄は透明の場所を空け、塗りの欄は透明を並べる。確定は列で振り分ける: `strokeWidthPx` → `CM-81`、`fillTransparencyPercent` → `CM-83`、`strokeColor` → `CM-80`、`fillColor` → `CM-82`、`textColor` → `CM-84`、`text` → `CM-48`（今のまま） |
| S-8 | 仕様 ↔ ポインタ（`frame-loop.ts`、`CR-554` の後は `pointer-shape.ts`） | 当たりが `GR-14` かつ `item.kind === 'commentBox'` かつ `boxPart.kind === 'anchor'` のとき `PK-11`（`CR-558` の S-5 が足す `move`）。コメントボックスのほかの部分（本体・引出し線）は今のまま `null`。ハイライトボックスは `CR-558` の S-5 のとおり（本書は触らない） |

⚠️ **`CR-558`（ハイライトボックス、先に当てる）と共有する行**（同書の 5.1 節と突き合わせた）: 表 T-017b の `CV-1`（E-01。`CV-6` は行 ID で引くだけ）、表 T-020 の `ZO-10`（E-06）、`FR-019` の RATIONALE の `CR-558` の 2 文（E-07 ・ E-08。本書の段 E-09 は `CR-558` が触らない 6 行）、`05-07-design.md` の読む段の `CR-558` の 2 文（E-10 ・ E-11 —— 段は 1 つに畳まれ、2 つ並ばない）、表 T-023d の結び（E-04 は `CR-558` が触らないコメントボックスの 3 行）、表 T-217 の `rows` と後ろの段（J-04）、`S-312` の備考（J-02）、`erd.json`（本書は `CommentBox` だけ、`seat` 148 〜 152）、`property-items.json` ・ `display-words.json`（`PR-26` 〜 `PR-28`）、`tools/generate_entity_types.py`（`CR-558` が直す）、ポインタ `PK-11`（`CR-558` が立てる）。

---

## 6. グラフ（`ebc71984` で測った）

### 6.1 `impact.py` —— 触る行ごとの届く先（要求 / 参照）

| 対象 | 要求 / 参照 | 指している要求 |
|---|---|---|
| `FR-019` | 7 / 30 | `UC-008` ・ `FR-001` ・ `FR-007` ・ `FR-016` ・ `FR-033` ・ `FR-041` ・ `FR-076`。⭐ 書き直すのは引出しの段と `CR-558` の 2 文の一覧だけ。置き方（日付と行の識別子）・ハイライトボックスの段・`RS-44` の段は変えないので、引く側の読みは偽にならない |
| `GR-14` | 2 / 10 | `FR-016` ・ `FR-019`。⭐ `FR-016` の中の参照は、表 T-023d の結び（E-04 が書く所）と追従の段（`:3430`）。追従の段「掴んでいるあいだ、置くことになる姿を … 描いて示す」は、本書の後も束を当てた姿で満たす（試験に書く。9 節） |
| 表 T-023d | 17 の要求が指す（2 次 53） | ⭐ 本書は `GR-14` の結びの 3 行だけを旧にする。行（`GR-*`）の並びも優先順も変えない |
| `S-291` ・ `S-292` | 2 / 3 ・ 2 / 4 | `FR-016` ・ `FR-039`。`FR-039` は掴み代に表示の倍率を掛けるかを引くだけで、値と意味は変わらない |
| `S-312` | 1 / 1 | `FR-019` |
| `S-146` ・ `S-147` | 3 / 10 ・ 1 / 3 | `FR-007` ・ `FR-009` ・ `FR-029`。どれも書き直さない（`FR-019` が名指すだけ） |
| `CV-1` ・ `CV-6` ・ `CV-9` | 0 / 2 ・ 1 / 5 ・ 0 / 2（`ebc71984`） | `FR-007`。`CV-1` ・ `CV-9` は句を足すだけ。`CV-6`（`CR-564` の後の文）は本書が書かず、`FR-019` の 3 文が行 ID で引く。`CV-10` は実績の塗りだけに当たり、本書の 3 列には当たらない |
| 表 T-017b | 3 の要求が指す（2 次 9） | — |
| `ZO-10` ・ 表 T-020 | 0 / 1 ・ 6 の要求が指す（2 次 19） | 重ねの順は変えず、`ZO-10` に 1 つ足すだけ |
| `LF-15` ・ `SWS-4` ・ 表 T-221 | 2 / 3 ・ — ・ 9 の要求が指す（2 次 34） | `FR-016` ・ `FR-019` ・ `SWS-4`。`LF-15` の点の式は変えない |
| `FR-106` ・ 表 T-269 | 1 / 5 ・ 3 の要求が指す（2 次 7） | `FR-040` ・ `FR-104`。`PK-11` は `CR-558` が立てる行を使うだけ |
| `CM-50` ・ `CM-51` ・ 表 T-108 | 1 / 1 ・ 1 / 2 ・ 8 の要求が指す（2 次 39） | `FR-016`。命令の中身は変えない（束にして使うだけ） |
| 表 T-217 ・ `S-132` | 1 の要求が指す（2 次 7）・ 0 / 0 | `FR-019`。`S-132` は変えない。`CR-558` の後は 表 T-217 を `FR-019` の 2 か所（`CR-558` の段と本書の段）が指す |
| `AT-110` ・ `AT-115` | 1 / 1 ・ 1 / 1 | `FR-016` |
| 表 T-016 ・ `PR-21` ・ `FR-006` | 17 の要求が指す（2 次 51）・ 1 / 6 ・ 5 / 25 | 行を足すだけ。`FR-006` の「色の行は末尾」（`CR-551` の E-28）を `PR-28` が満たす |
| `FR-097` | 2 / 7 | `FR-016` ・ `FR-040`。本文の箱の大きさの規則は変えない |

⭐ 導いた条項ごとに、届いた行を `rulings.md` で引いた（規則 02 の 1）: `JDG-248` ・ `JDG-249`（掴み代の値。`S-291` の備考の「箱が動く」だけを本書が覆す）、`JDG-252`（線先はタスクに追従しない —— 変わらない）、`JDG-320`（留める日が空の箱 —— 本体を引けば `anchorDate` に日が入るが、置き場の規則は変わらない）、`JDG-399`（掴み代は既存のまま —— 決定 10）、`JDG-382` ・ `JDG-383` ・ `JDG-397` ・ `JDG-405`（色の欄 —— 本書はそれに従うだけ）。

### 6.2 `induced.py`（3 群。群 1 ・ 3 は `cbf5ba1a`、群 2 は `c32b13d7` の check.sh の書き出しで測った）

⚠️ 群 1 の `LF-17` と群 3 の `PK-11` は本書自身（と兄弟の `CR-558`）が新しく立てる行で、当てる前の仕様にはまだ無い。⇒ 種から外して測った（下の 2 群はその形で走らせた行を載せる）。

| 種 | 解決 | 種の中の辺 | 閉路 | 扱い |
|---|---|---|---|---|
| `FR-019 GR-14 LF-15 SWS-4 CM-50 CM-51 S-291 S-292`（`LF-17` を外した） | 8/8 | 7 | 1: `FR-019` `LF-15` | ⭐ 本書は 2 つとも 1 つの計画で書く（E-09 と E-12）。`LF-15` の点の式は変えず、注だけを 表 T-023d の結びへ向ける。`FR-019` は置き場を `LF-15` に任せたまま、出る隅を `LF-17` に向ける ⇒ 当てた後も 2 つは同じ置き方を述べる（0 節 ② の ⑥） |
| `FR-019 T-217 CV-1 CV-6 CV-9 CV-10 FR-007 T-017b`（`c32b13d7` の書き出しで測り直した。`CR-564` の後） | 8/8 | 8 | 0 | — 1 つずつ書いてよい |
| `FR-106 GR-14 T-269`（`PK-11` を外した） | 3/3 | 1 | 0 | — |

閉路が出た群 1 は、その閉路の要求を 1 つの計画で書いたか（本書の E-04 ・ E-07 〜 E-09 は 1 つの計画である）を確かめた。

---

## 7. 数の予測（`ebc71984` で測った。当てた後に同じ数え方で突き合わせる）

⚠️ 当てる時の前の数は、`CR-551` 〜 `CR-554` と `CR-558` の後の木で測り直す。下の「差」だけが本書の予測である。

| 数 | `ebc71984` | 差 | 内訳 |
|---|--:|--:|---|
| tables | 186 | 0 | 表を立てない |
| figures | 27 | 0 | — |
| rows | 2282 | +17 | 設定値 3（`S-374` ・ `S-375` は 表 T-217、`S-376` は 表 T-206）、`LF-17`、`CM-80` 〜 `CM-84`、`PR-26` 〜 `PR-28`、列 5（`AT-148` 〜 `AT-152`、`fig-erd-detail.md` の行）。消す行は 0 |
| uids | 162 | 0 | — |
| 接頭辞 | 162（`refactor` で 163） | 0 | — |
| 辞書の項 | — | +3 | `properties` の `PR-26` 〜 `PR-28` |

---

## 8. 波 —— 持ち場で割る（規則 02 の 3.5 節: 原稿と読む側は同じ波）

⛔ 仕様の編集（4 節）は 1 つの体が 1 つの worktree で当てる。⭐ `CommentBox` の型に 5 列が足されると、`edit-annotation.ts:140-147` の `createCommentBox` の字が型を満たさず `tsc` が落ちる ⇒ 波 0 に入れる（5 列を `null` で置くだけ。S-5 の最後の文）。⚠️ 表 T-217 の生成器の止まり方は `CR-558` の波 0 が直している前提である（4.3 節）。

```
wave 0  spec + the reader that stops tsc (ONE body, one worktree cut after CR-558 landed)
          section 4 (E-01..E-14, J-01..J-07), recount every old block on that tree (count == 1)
          src/use-case/edit-document/edit-annotation.ts createCommentBox: the five columns as null
          npm run gen && npm run gen:check && npx tsc --noEmit (src only)
          -> tests that quote the spec verbatim or build a CommentBox literal go red here, by design (section 9)
wave 1  (parallel, disjoint files; cut from wave 0's commit; seams S-1..S-8 copied verbatim into both briefs)
  1a layout-engine   schedule-geometry.ts (before CR-554; comment-box.ts UF-148 after): commentAnchorPointOf (S-1),
                     leaderOf with the LF-17 corner and null inside (S-2);
                     item-hit-area.ts noteHitOf: a null leader answers nothing
  1b use-case        edit-annotation.ts CM-80..CM-84 (S-5), edit-document.ts the command list
  1c document-codec  json-codec.ts withCommentBoxLookColumns before the schema, beside CR-558's (S-6)
  1d svg-renderer    schedule-overlays.ts: the five columns, clamped, and the handle (S-3)
  1e translator+panel item-grab.ts the two bundles (S-4) -- calls 1a's commentAnchorPointOf by the S-1 name;
                     field-commit.ts :391-393 dispatch by column, properties-panel.ts colour and number controls (S-7)
wave 2  shell        frame-loop.ts POINTER_BY_GRAB / pointerRowOf (pointer-shape.ts UF-157 after CR-554): S-8,
                     on top of CR-558's S-5 (which already reads boxPart for GR-14). No function grows (check 60)
wave 3  tests by a spec-only tester (never an implementer of waves 0-2), reading docs/spec only,
        on the MERGED tree; then check.sh + vitest; npm run build (dist/) for the user to try by file://
```

- ⚠️ 1a と 1e は S-1 の名だけで繋がる（1e は 1a の関数を呼ぶ）。⭐ 両方の依頼文に S-1 を逐語で写し、1e の体には「1a が未着地の間は S-1 の署名の仮の関数を自分の worktree に置き、合わせるときに 1a の側を採る」と書く。
- ⚠️ 1d と `CR-558` の体は `schedule-overlays.ts` の隣り合う 2 つの `for`（ハイライトボックスとコメントボックス、`:196-239`）を触る。`CR-558` の着地の上で数え直すこと。範囲へ寄せる計算は `CR-558` の形（同書の S-3）に倣い、1 つの関数にまとめられるならまとめる。
- ⚠️ 1c ・ 1e の `json-codec.ts` ・ `properties-panel.ts` ・ `field-commit.ts` は `CR-558` も触る。同じく着地の上で。
- ⚠️ `dist/` は利用者が `file://` で試す。着地の報告には枝・commit・sha を添えること。

---

## 9. 仕様の外で直すもの（⛔ 本書は直さない）

`CR-554` が割るファイルは「割る前の関数名 in ファイル」で書き、割ったあとの置き場を添えた（`CR-554` の 4.1 ・ 4.4 ・ 4.5 ・ 8 節）。

| 項目 | 所（`ebc71984`） | 何をする | 試験（いま引いているもの） |
|---|---|---|---|
| 2 ・ 3 | `commandFromGrab` in `src/adapter/input-command-translator/item-grab.ts:96-99`（線先の枝）・ `:106-118`（本体と引出し線の枝）、`commentBoxAnchorWrite` in 同 `:477-488` | S-4 の 2 束 | `tests/unit/dfc-568-gr-14-corners-anchor-and-body.test.ts`（`setCommentBoxAnchor` ・ `setCommentBoxBodyOffsetPx` を 2 か所、`S-291` ・ `S-292` を引く）・ `tests/unit/t-023d-follows-the-pointer.test.ts`（4 か所。追従の姿が束で変わる）・ `tests/unit/edit-annotation.test.ts`（命令を 5 か所） |
| 4 | `leaderOf` in `src/entity/layout-engine/schedule-geometry/schedule-geometry.ts:152-154`（`CR-554` の後は `comment-box.ts`、`UF-148`）、`commentGeometry` in 同 `:1144-1186`（同） | S-1 ・ S-2 | `tests/unit/edit-annotation.test.ts:626-670`（「⭐ FR-019 (MUST) puts the far end on the BODY’s bottom-left corner」—— 左下を主張する。`LF-17` に書き直す）・ `tests/unit/dfc-568-gr-14-corners-anchor-and-body.test.ts:88`（`FR_019_LEADER` に E-09 の旧を逐語で持つ）・ `:1127`（`comment-<id>-leader`） |
| 4 | `noteHitOf` in `src/entity/layout-engine/item-hit-area/item-hit-area.ts:898-912`（`CR-554` の後も入口に残る） | `leaderOf` が `null` の箱は引出し線で応えない | `tests/unit/pnd-235-gr-14-comment-over-highlight.test.ts` ・ `tests/unit/cr-438-pointer-chart-presses-grabs-and-arms.test.ts`（`boxPart`） |
| 1 ・ 5 | `src/adapter/svg-renderer/schedule-overlays.ts:213-239`（引出し線 `:214-218`、箱 `:219-224`、字 `:226-232`、選択の枠 `:235-238`） | S-3。`stroke-width="1"` ・ `S-146` ・ `S-147` の字を列と設定値へ。印を足す | `tests/unit/fr-097-comment-box-drawn.test.ts`（`GR-14` 4 か所）・ `tests/unit/cr-432-svg-renderer-branches-the-spec-decides.test.ts`（`T-217` を 2 か所）・ `tests/unit/uf-32.test.ts:664`（注で `S-146` を注記の地と呼ぶ） |
| 1 | `POINTER_BY_GRAB` in `src/framework/single-html-shell/frame-loop.ts:311-345`（`:337` の `'GR-14': null`）と `pointerRowOf` in 同 `:535-539`（`CR-554` の後は `pointer-shape.ts`、`UF-157`） | S-8 | `tests/unit/cr-430-*`（`pointerRowOf` を動的に取り込む）・ `tests/unit/uf-30-31.test.ts`（`GR-14` 4 か所） |
| 5 | `src/use-case/edit-document/edit-annotation.ts:27-48`（命令の型）・ `:133-149`（`createCommentBox`）・ `:50-58`（`boxStrokeRefusal` の形に倣う）、`src/use-case/edit-document/edit-document.ts:197-202`（命令の名の一覧） | S-5 | `tests/unit/edit-annotation.test.ts` |
| 5 | `documentFromJson` の前段 in `src/adapter/document-codec/json-codec.ts`（`withTaskGroupColumnsOfAnOlderVersion` `:1383-1398` の隣、呼ぶ所 `:1473`）。スキーマの区画 `:570-603` は `npm run gen` が刷る | S-6 | `CommentBox` を字で組む試験 26 本 35 か所（`git grep -l leaderShapeKind -- tests`）—— 型に 5 列が足されるので、試験の字にも 5 列が要る（試験の体が直す） |
| 5 | `src/adapter/input-command-translator/field-commit.ts:391-393`（コメントボックスの欄の確定はいつも `setCommentBoxText`）・ `controlsOfCommentBoxItem` in `src/adapter/screen-renderer/properties-panel.ts:604-620`・ `heldByOf` in 同 `:162-165` | S-7。⚠️ `:391-393` は列を見ていないので、`PR-26` 〜 `PR-28` の値を本文に書いてしまう | `tests/unit/cr-439-properties-panel-fields.test.ts` ・ `tests/unit/fr-006-panel-typography.test.ts` ・ `tests/unit/cv-9-transparent-and-unset-swatches.test.ts` |
| 5 | 表 T-217 を読む試験 | —（生成器は `CR-558` が直す） | `tests/system/three-rows-read-from-the-spec-alone.test.ts`（`T217` を 9 か所）・ `tests/system/divider-colour-corner-and-sticky-field.test.ts`（7 か所）・ `tests/unit/settings-defaults-follow-the-manuscript.test.ts` ・ `tests/contract/seams.contract.test.ts` —— `CR-558` の後の行数に本書の 2 行を足す |
| 1 | 仕様を逐語で引く試験 | — | `tests/unit/pnd-235-gr-14-comment-over-highlight.test.ts:30`（E-04 の旧「箱は不透明に描くので」を逐語で持つ）→ 赤。E-04 の新の文に |

⚠️ 新しく要る試験（仕様だけを読む試験の体が書く）: ① 本体を引くと箱が引いた量だけ動き、線先が「元の線先 ＋ 引いた量」の日と行に移る（取り消し 1 回で 2 つとも戻る）。② 線先を引いても箱の画面の位置が動かない。③ 箱の 4 つの向きに留めた点を置き、線が最も近い隅から出る。中点ちょうどでは左下。箱の中では線が無い。④ 選んだ箱だけに印（一辺 `S-376`）が出て、倍率を変えても一辺が変わらない。⑤ 5 列を持たない文書が開き、今までと同じ絵になる。⑥ 範囲の外の太さと透過率、線と字の透明を命令が拒む。範囲の外の値を持つ文書は、寄せた値で描かれ、書き換えられない。⑦ 線先の上のポインタが `PK-11`。

---

## 10. ⛔ この変更でやらないこと

- 本体と引出し線の上のポインタ（コメントボックスの `GR-14` のほかの部分は今のまま `null`）。利用者が求めたのは線先の印だけである。
- `SL-7`（選んだものをまとめて動かす）にコメントボックスが入るときの動き方 —— 今のまま。
- `S-291` の掴み代を線の中心から測るか縁から測るか —— 今のまま（`JDG-399`）。⚠️ 太さを上限 8px にすると、描いた線の外側の 1px は掴み代の外になる（`CR-558` の `HB-12` は縁から測る —— 揃えるかは別の変更要求）。
- 枠と引出し線の色を分けること（決定 12）、角の丸み、本文の字の大きさ。
- 線先をタスクに追従させること（`JDG-252`）。
- `leaderShapeKind` の列と列挙の退役（`FR-019` が別の段に回している）。
- `NOT_STORED_ANNOTATION_SIZES` の名を変えること。
- ハイライトボックスの見た目（`CR-558`）。
- 表 T-017b の `CV-6` ・ `CV-10` の文（本書は行 ID で引くだけ）。
- `docs/development-records/` の `pending-decisions.md` ・ `changelog.md`、`A-appendix.md` の変更履歴（当てる者が書く）。

---

## 11. 前に立つ者へ返す問い

| # | 問い | 推奨と理由 | 答え |
|---|---|---|---|
| 1 | **本体を引いたときの線先の置き方** —— 線先は日の列と行の帯の中央にしか立たない。使う場面: 拡大して 1 日が 100px のとき、付箋を 30px だけ右へずらす。A 本文の箱は 30px 動き、線先は元の日のまま（線先は半日を越えるまで動かない）／ B 箱も線先と一緒に日の格子で動く（30px では動かず、50px を越えると 100px 跳ぶ） | **A**（決定 2）。逐語「本体を動かすと … 線の先も動け」は箱が主語であり、箱を思った所へ置けることが先に立つ。B では拡大したときに箱を細かく置けない。代償は、引いた量と線先の動く量が半日・半行までずれること | 「Q13. 本体を引いたときの線の先 → 推奨のA」（2026-09-24、`JDG-465`）—— A を採る。決定 2 のまま |
| 2 | **値と透明の扱い** —— 線の太さ `S-374` 既定 1px（1 〜 8、`CR-558` のハイライトボックスと同じ）、塗りの透過率 `S-375` 既定 0%（不透明 ＝ 今の箱）、線先の印 `S-376` 8px。線の欄と字の欄には [透明] を並べない（決定 13）。使う場面: 枠の無い付箋を作りたいとき、線の色を塗りと同じ色にする | **提案どおり**。既定は今の絵と同じ。透明を並べないのは、線が消えると付箋がどこを指すかが読めなくなるから（`JDG-249`）。使い勝手の試しで 1 行ずつ直す（透明を許すなら `CV-9` と `FR-019` の 1 文と `erd.json` の `transparent: false` を消す） | 「Q14. 値と、透明を出さない欄 → 提案どおり」（2026-09-24、`JDG-466` —— 仮の ID。前に立つ者が最後に振り直す）—— 提案どおり。決定 13 ・ 決定 20 のまま |

⚠️ 前に立つ者への申し送り（利用者に問わない）: 共通の依頼文は `FR-019` の「左下」（`~4752`）も覆すものに挙げていたが、本書はそれを覆さない（決定 6）—— 覆すと文書の形が変わり、今までの文書の箱の置き場を読み替える段が要る。線の出る隅は描く側で選べるので、逐語はそれで満たされる。

---

## 12. 台帳

### 12.1 裁定（前に立つ者が `docs/development-records/rulings.md` に写す。行の写しは台帳の下書き `CR-559-ledger.md`）

`JDG-460` 〜 `JDG-464` —— 項目 1 〜 5。`JDG-465` ・ `JDG-466` —— 11 節の問い 1 ・ 2 への答え（2026-09-24）。状態はすべて「指示」（本書を当てたら「適用済」へ変える。当てる者の仕事）。

⛔ 当てる者が変える古い行: `JDG-249`（読みの「引出し線を引けば、本体と同じく箱が動く（`CM-51`）」を、`JDG-461` が「箱と線先が一緒に動く（`CM-50` と `CM-51` の束）」へ覆した、に）。

### 12.2 欠陥（前に立つ者が `docs/development-records/defects.md` に写す）

| ID | 何 | 状態 |
|---|---|---|
| `DFC-870` | コメントボックスの枠と引出し線の太さ `1` が、仕様のどの行にも無い（`schedule-overlays.ts:217` ・ `:224` の `stroke-width="1"`） | `仕様待ち`（本書の `FR-019` の段と 表 T-217 の `S-374`） |
| `DFC-871` | コメントボックスの本文の箱の塗り（`S-146`、`schedule-overlays.ts:224`）と字（`S-147`、`:230`）を、仕様はコメントボックスについて名指していない —— 表 T-023d の結びが「箱は不透明に描く」とだけ言い、色の行を持たない | `仕様待ち`（本書の `FR-019` の段） |

### 12.3 未決（前に立つ者が `docs/development-records/pending-decisions.md` に写す。答えを待つ）

書かない —— 11 節の問い 2 つは 2026-09-24 に利用者が答えた（`JDG-465` ・ `JDG-466`）。保留の帯は使わず返す。

---

## 13. 測り方の再現

```
# totals (before)
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/md-checks.py | grep "tables="
#   -> tables=186  figures=27  rows=2282  uids=162   (ebc71984, 2026-09-24)

# free identifiers (2026-09-24): every prefix's maximum over docs/spec src tests tools change-request docs/development-records
for p in S AT CM PR LF PK IV JDG DFC PND; do
  git grep -ohE "\b$p-[0-9]+" -- docs/spec src tests tools change-request docs/development-records \
    | sed "s/$p-//" | sort -n | uniq | grep -v '^999$' | tail -1
done
#   -> ebc71984:  S 359, AT 144, CM 75, PR 21, LF 16, PK 10, IV 21, JDG 440, DFC 854, PND 530
#   -> refactor (same command with the ref): the same, but PND 532
#   (CR-558's draft numbers S-369..373, AT-145..147, CM-77..79, PR-23..25, PK-11..15 are untracked drafts)
git grep -nE "\bS-37[4-6]\b|\bAT-(14[89]|15[0-2])\b|\bCM-8[0-4]\b|\bPR-2[6-8]\b|\bLF-17\b|\bJDG-46[0-6]\b|\bDFC-87[01]\b" -- . ':!previous-project-result'
#   -> nothing but this file (and the same on refactor)

# graph
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/impact.py <ID>     # each ID of 6.1
# induced.py, cbf5ba1a (check.sh's export, scratch/spec-check/sd-out/json/index.json).
# LF-17 (group 1) and PK-11 (group 3) do not exist yet -- this CR (and sibling CR-558) creates them --
# so they are dropped from the seeds:
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-019 GR-14 LF-15 SWS-4 CM-50 CM-51 S-291 S-292
#   -> 8/8, 7 edges, 1 cycle: FR-019/LF-15
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-019 T-217 CV-1 CV-6 CV-9 FR-007 T-017b
#   -> 7/7, 6 edges, 0 cycles
PYTHONIOENCODING=utf-8 python .claude/skills/spec-graph-check/induced.py FR-106 GR-14 T-269
#   -> 3/3, 1 edge, 0 cycles

# old blocks: copies of the refactor tree (CR-551 landed), then CR-558's 19 edits applied in order, then this CR's 14
#   (scratchpad k5_check_edits.py with CR-558 first; every edit is a substring replace whose old must occur once)
#   -> CR-558: 19 of 19 applied; CR-559: 13 of 13 occur exactly once (E-02 withdrawn) and apply in order: problems 0
# JSON edits: the same copies, CR-558's T-217 rows (S-369..371) added first, then J-01..J-07,
#   json.loads + the hand-written schemas (jsonschema 4.26, instance check only)
#   (scratchpad k5_check_json.py)
#   -> settings.json 0 errors (no duplicate S id; T-217 = S-132 S-369 S-370 S-371 S-374 S-375),
#      erd.json 0 errors (no duplicate seat), property-items.json 0 errors, display-words properties tail PR-21 PR-26 PR-27 PR-28

# tests that construct a CommentBox literal
git grep -l leaderShapeKind -- tests | wc -l       # -> 26 files
git grep -c leaderShapeKind -- tests               # -> 35 lines
git grep -c leaderShapeKind -- sample-schedule src/framework/single-html-shell/startup-template.json docs/guides tools  # -> 0
```
