---
type: Working Note
title: handover 整理計画
description: "敵対的レビュー 5 本の統合結果。整理が完了したら to-del/ へ移す。"
tags: [working]
phase: packaging
status: draft
---
# handover 整理計画 — 敵対的レビュー 5 本の統合結果

- 日付: 2026-08-01
- **この文書は作業用である。整理が完了したら `to-del/` へ移す。**
- 出所: `handover/` に対する敵対的レビュー 5 本（網羅性 / 重複・陳腐化 / 用語 / 初見の誤解 / 理由の欠落）。
  重複を除いて統合し、**主張は可能な限り一次資料で裏を取った**。

## この文書の読み方

| 印 | 意味 |
|---|---|
| **✓** | 本セッションで**一次資料に当たって確認済み**。そのまま着手してよい |
| **—** | レビューの指摘のまま。**着手前に確認が要る** |

**A〜C は着手順。D 以降は分類。** 件数は「直す箇所」であってレビューの指摘数ではない。

---

## 0. 根因（これを直さないと同じことが再発する）

最後のレビューが言い当てた 1 文:

> **`07 §11` のような「改訂 → 反映先」表を作る運用が、2026-07-30 の改訂にしか適用されていない。**

2026-07-31（テーマ・縁取り・目盛キー分割・本日線廃止）と 2026-08-01（マイルストーン横ずらし・
イナズマ線・目盛 4 段階・`Esc`・パン・スクロールバー・テーマ色）には同等の表が無く、
**陳腐化の指摘のほぼ全部がこの 2 日ぶんに集中している**。

### 構造対処（個別修正より先に効く）

| # | 対処 | 効く範囲 |
|---|---|---|
| **J1** | 各文書の冒頭に**どの段階の産物か**を明記（先行調査 / 企画 / 原理証明 / 成果物整理） | D 群のほぼ全部。`user-order.md` を「最新の正」と誤読させない |
| **J2** | **「改訂 → 反映先」表**を 2026-07-31 / 08-01 分についても作る（`07 §11` と同じ形） | A・D 群の再発防止 |
| **J3** | `grs-document-settings-ja.md` §3 に**「既定値の理由」列**を追加し、由来不明の値に印を付ける | H 群 |
| **J4** | **用語の正は 4 つ**であることを全文書で統一（現在「2 つだけ」と書いた文書が 3 本ある） | G7・矛-5 |

---

## A. 本セッションの変更による波及漏れ ✓

**これは今日の作業で作った defect である。** 07 §2-2 と §2-4 を書き換えたとき、同一ファイル内および
下流の記述を掃かなかった。

### A1. マイルストーンの実績「下にずらす」が残存 ✓

正: `07 §2-2`「マイルストーンの実績は**横**にずらす — 確定 2026-08-01」（`actualPlacement = 'atActualDate'`）

| # | ファイル:行 | 残っている記述 |
|---|---|---|
| 1 | `07-plan-actual/handover-plan-actual-decisions-ja.md:351` | 「幅のない **3 タスク形状**は…そのタスク形状だけ実績を下にずらす」← **正が自ファイル内で矛盾** |
| 2 | 同 `:1139` | 「幅のないタスク形状で実績を下にずらすと行が高くなる」 |
| 3 | `03-ui-naming/handover-ui-detail-spec-ja.md:354` | 「幅がないタスク形状（矢印 / 端点スパン / **マイルストーン**）は…下にずらす」 |
| 4 | `04-performance/handover-performance-notes-ja.md:281` | 「（`arrow` / `endpointSpan` / **`milestone`**）は実績バーを下にずらす」 |
| 5 | `user-order.md:368` | 同上 |
| 6 | `user-order.md:542` | 「幅がないタスク形状（…**マイルストーン**）だけ、実績を下にずらす」 |
| 7 | `08-poc/POC-SPEC-ja.md:52` | 凍結記録。**冒頭の対照表に 1 行足す**（本文は書き換えない） |

> `07:210-211`（`arrow` / `endpointSpan` の行）と `07:220` は**正しいので触らない**。

### A2. `(!)` = 「期限超過」が残存 ✓

正: `07 §2-4`「`(!)` = **遅れ**。判定は 3 条件 — 確定 2026-08-01」

| # | ファイル:行 | 残っている記述 |
|---|---|---|
| 1 | `02-data-model/grs-native-erd-ja.md:1102` | 「マーカーは**完了・中断・期限超過**を形で示す唯一の手段」 |
| 2 | `user-order.md:543` | 「`(✓)` 完了 / `( \ )` 中断 / `(!)` **期限超過**」 |

---

## B. 設計判断が要るもの（人が決める）

**私が決めてはならない。** 決まらないと下流が書けない順に並べた。

### B1. WBS 6 段以上のクランプ ✓ 【最優先】

| 側 | 記述 |
|---|---|
| **クランプする** | `grs-native-erd-ja.md` §5.5e「6 段以上は 5 段にクランプし、**export でも復元しない**」「クランプ後の木が唯一の真実」／§8D「**唯一の明示的な Drop**」／正規化式 `lv = min(lv, 5)`／`grs-data-model-ja.md` §4.6・§8 #18／`grs-mspdi-field-ledger-ja.md` §B |
| **クランプしない** | `07 §5-2`「**7 段の階層が来れば 7 段で戻る。丸める処理が存在しない**」／`handover-ui-detail-spec-ja.md` §6-1-1「export で返す＝**クランプしない**」／`grs-mspdi-field-ledger-ja.md` 冒頭表・§7.4「**クランプしない**」／`08-poc/OPEN-QUESTIONS-ja.md` Q5／`user-order.md` 項 36「**書き戻す値は頭打ちにしない**」 |

**ERD 側の根拠は軸を取り違えている** ✓ — 「推奨 3 段に対して 5 段はマージン」は `user-order.md` 項 27 ＝
**軸B（`TaskGroup`・非 export）**の要求。それを**軸A（外部マスタの WBS・export する）**へ流用している。
`07 §5-3` が「軸A と 軸B は別物。**混同しないこと**」と明記しているまさにその混同。

**推奨**: 誤っているのは ERD 側。`Drop=0` は本設計の看板要件（項 56 往復無損失）であり、
`grs-mspdi-field-ledger-ja.md` は自ファイル内でも割れている（冒頭表・§7.4 は「しない」、§B は「Drop 2 件」）。

### B2. `milestoneGlyph` の値集合に既定の ◇ が無い ✓

- 値集合（5 箇所とも同一）: 〇 △ ▽ □ ☆ 五角形 六角形 —— `ui-parts §2-1-2` / `erd:1323` / `ui-detail:131` / `user-order` 項 10 / `07 §2-2-2` 本文
- しかし `07 §2-2-2` のコード欄は `'diamond' | 'circle' | 'triangleUp' | ...`、`07 §2-2`（08-01）は「予定 **◇** / 実績 **◇**」、`entry` の JSON 実例も `"milestoneGlyph": "diamond"`

**◇ を値集合に足すのか、既定を別の形にするのか。**

あわせて `07 §2-2-2` は同一節内で矛盾 — 「`milestoneGlyph` は **GRS 専用・非 export**、`TaskVisual` に置く」と
3 行後の「マイルストーンの形状も **`shapeKind` の同じ列に入る**」。

### B3. 「4 状態」か「5 状態」か —

見出しは「4 状態」、表は 5 行（未着手 / 進行中 / 中断・再開予定あり / 中断・再開日未定 / 完了）。
`07 §1-3` は同節内で「上の **5 状態**のどれとも並ばない」とも書く。
`user-order` 項 50 / `ui-detail §4-0` / `property-mspdi §3` / `README §1` も「4 状態」。
**列挙型の値数**なので実装に直結する。

### B4. 担当・完了率を既定で表示するか —

`POC-RESULTS-ja.md` 書き戻し候補 #2「既定で出すと直列チェーンが常時 2 段になる」。
`grs-document-settings-ja.md` §4-1 の既定値列が「—」のまま。**行高が既定値で倍増するか**が決まらない。

### B5. `stack_direction` と ALIGN-L2-004 の矛盾 —

`grs-native-erd-ja.md` §5.6 に「要 change-manager（要求改訂か本設定の撤回かの判断）」が未決のまま残り、
`OPEN-ITEMS-ja.md` に載っていない。

### B6. `statusDate` の置き場所 —

`ui-parts §2-1-6` は「設定値のキー」に載せるが、`grs-document-settings-ja.md` §3 の注記は
「PoC の都合の値。製品では `Project.status_date` ＝ **`documentSettings` ではなく文書のデータ**」。

---

## C. 事実の誤り（判断不要・機械的に直せる）

| # | 検証 | ファイル:行 | 誤り → 正 |
|---|:--:|---|---|
| C1 | ✓ | `01-mspdi/mspdi-core-tree.md:302` | `Unit` enum `{0=m,1=h,2=d,3=w,4=mo,5=y}` → **正本 XSD は `0,1,2,3,5,8`（`5=mo, 8=y`。4・6・7 は欠番）**。信じると月・年が壊れる |
| C2 | ✓ | `01-mspdi/mspdi-tables.md:253` | 「通貨（4）→ 全 ×」に `CurrencyCode` を含む → **XSD 必須**（`Project` 直下の必須は `SaveVersion` と `CurrencyCode` の 2 つだけ）。従うと XSD 非妥当な XML を出す |
| C3 | ✓ | `README.md` §1 行10・§2 木 | 「07 の **§9** に差分の全数」→ **§11**（§9 は「命名」） |
| C4 | ✓ | `03-ui-naming/handover-ui-parts-ja.md:202,208` | 所属の**手本**が `Task.stackOrder` → **`TaskGroupMember.stack_order`**（`erd:174,312,856`）。教える例が誤りなので初見は疑えない |
| C5 | ✓ | `02-data-model/grs-native-erd-ja.md:417` | C-3 の括弧書き「`source_uid` 列を**持たない**」→ 列は存在する（`:317,393,436`）。§5.3（`:371`）は既に「突合専用・export で復元しない」と整合済みなので、**この括弧書きだけが古い** |
| C6 | — | `README.md` §2 木 | 「MS Project **2012** MSPDI スキーマ」→ **2007**（`pj12` = Project 12 = 2007。XSD ヘッダで確認可） |
| C7 | — | `07 §11` | 反映先「`ui-detail` **§4-6（新設）**」→ 実際は **§4-5**（§4-6 は「進捗の編集入口」） |
| C8 | — | `handover-ui-detail-spec-ja.md` §4-9 | 「掴み領域の定義（**§3**）」→ **§4-1**（§3 は Command Palette） |
| C9 | — | 同 §4-10-3 | 「`actualMin`（**§2-4-1** の 16px）」→ 本書に §2-4-1 は無い。**`07 §2-4-1`** を指している（ファイル名なし） |
| C10 | ✓ | `grs-document-settings-ja.md` §3・§7 | 「`grs-native-erd-ja.md` **§1228** / `grs-mspdi-field-ledger-ja.md` **§508**」= **行番号を `§` で参照**。編集で即無効になる。節番号へ直す |

---

## D. 陳腐化（2026-07-31 / 08-01 の未反映）

| # | 検証 | ファイル | 内容 |
|---|:--:|---|---|
| D1 | ✓ | `user-order.md` 項 33 | 目盛が **3 段階（しきい値 1・8）**のまま → 4 段階（1・4・14。週の段が入る）。`README` が user-order を「これが正」と書いているので誤読が確定的 |
| D2 | ✓ | `user-order.md` 項 37 | 「素の左ドラッグでパンしてはならない」→ **背景の上ではパンする**（`ui-detail §5-2`・08-01 改訂）。`perf` E-12 も同じ |
| D3 | — | `user-order.md` 項 53 | イナズマ線が「**行ごと**に 1 頂点」→ **段ごと**（07-30）＋**途切れさせない**（08-01）。user-order に「段ごと」「途切れ」は 0 件 |
| D4 | — | `handover-data-model-entry-ja.md` §2・§3 | JSON 実例に廃止済み `"todayLineVisible": true` が残存／注記「テーマ・ズーム・スクロールは保存しない」が真逆（07-31 に保存へ移動）／`documentSettings` が 16 キーのみで「**全項目を書く**」規約（`settings §2`）に違反 |
| D5 | — | 同 §3 | 実例 uid=3 が `resumeValid: false` かつ `resume` 空 ＝ 状態表では「**中断・再開日未定**」なのに、検算表は**進行中**として扱う |
| D6 | ✓ | `04-performance/handover-performance-notes-ja.md` §4-1 | 依存線が「**候補列挙＋辞書式採点**」のまま。正は `ui-detail §4-9`「候補列挙も採点も探索も要らない」。撤回告知が §4-1-4 末尾 1 行に埋没。**`user-order` 項 48 がこの旧方式を名指しで読めと指している** |
| D7 | — | `05-security-a11y/security-design.md` | 取下げ済みの SVG/PNG 取込サニタイズが「生きている要求」の節（§1.1・§2.4・§3.6・§4・§10）に残存。CSP の `img-src data:` の根拠も消滅している |
| D8 | — | `05-security-a11y/a11y-wcag21-aa-checklist.md` | SC 一覧に **1.4.11 / 1.4.4 / 2.5.5** が無い（いずれも後発の確定が根拠に使っている）。残存ギャップ #2（ラベル縁取り）は `07 §2-7` で解決済み、#3（背景色変更）は `themeHue` で既に実装 |
| D9 | — | `grs-data-model-ja.md` 冒頭表 / `handover-stale-spec-audit-ja.md` / `refactor-gui-data-separation-ja.md` | `percentComplete`「**整数 0〜100**」が残存。`07 §1-4` が「**「0〜100」と書いてはならない**」と明示。しかも**上書き表そのものが禁止された書き方をしている** |
| D10 | — | `04-performance` §2-4 | 廃止済み定数が「基準線」として残る（`MAX_STACK_LANES = 64` / 行 44px / 段 18px）。同書 §4-2-4 が自ら「廃止した」と書いている |
| D11 | — | `08-poc/POC-RESULTS-ja.md` | 「未承認の書き戻し候補」に採用済みのものが混在（§6-3・§6-4）。`POC-SPEC` にはある「現在の正」対照表が**無い** |
| D12 | — | `06-background/refactor-gui-data-separation-ja.md` §5 | 存在しない `CR-017` の起票と、凍結済みプロセス（strictdoc / playwright ゲート）の実行を指示 |

---

## E. 重複（同じ事実が 2 か所以上）

| # | 検証 | 内容 |
|---|:--:|---|
| E1 | — | `handover-property-mspdi-mapping-ja.md` §3 が `07 §1/§3/§4` の**全文複製**。冒頭は「07 が上書きする」と書きながら §3 は「この節は仕様である」と主張。**既に矛盾 2 件（下の E1a/E1b）を生んでいる** |
| E1a | — | 完了時の `PercentComplete`: §3-3「**100**」⇔ `07 §1-4`「**要素そのものを書かない**」⇔ 同ファイル §3-4「**書かない**」（自ファイル内でも割れている） |
| E1b | — | 仕分け: §2-1「`OutlineLevel` = **Reconstruct**」「`Resource/Name` = **Consume**」⇔ `ledger §7.4`「**Consume**」/ §7.5「**Own**」 |
| E2 | — | **UI パーツ木が 3 本**あり全部違う（`ui-parts §3`「これが正」/ `07 §9-6` / `ui-detail §1`）。`07 §11` が「§1 の木を §9-6 に合わせる」と指示しているが未実施 |
| E3 | — | 予実の表が **4 系統**に複製。5 状態表は `user-order:346` / `property-mspdi:179` / `ui-detail:165` が**行単位で完全一致**。掴み代ブロックは `07 §3-2` と `ui-detail §4-1` が注記まで一字一句同じ。`ui-detail §4-5` だけ 08-01 に参照へ置換済みで、**同じ処置が §4-0/§4-1/§4-2/§4-7/§6-6/§6-1-2/§6-1-3 に未適用** |
| E4 | — | `documentSettings` の全リストが `erd §5.7-1` と `settings §4` に二重（前者は「代表例」と称しながら全数を持つ） |
| E5 | — | JSON 往復検査 6 件が `property-mspdi §4-2 C-8` と `settings §8-2` に二重。**どちらが正か書かれていない** |
| E6 | — | 実機確認 3 件が `OPEN-ITEMS-ja.md` と `07 §12` で**番号が入れ替わっている**（`07 §1-4` は自番号で「§12-3」と参照） |
| E7 | — | `mspdi-tables.md` §A-2 と `grs-mspdi-field-ledger-ja.md` §6.1 が同じ 29 テーブルに**別々の別名体系**を使い、双方「正」を主張 |

---

## F. リンク切れ・参照切れ

| # | 検証 | 内容 |
|---|:--:|---|
| F1 | — | `mspdi-declutter-erd-ja.md` が**不在**なのに 5 箇所から参照（`mspdi-tables:6,14` / `grs-data-model:479` / `ledger:657` / `erd:1447`）。**`mspdi-tables` の「断捨離」列の根拠がこの不在文書**にある |
| F2 | — | `../_assets/` という不在パス（`mspdi-pitfalls:7` ＝ **読む順 2 の最優先文書** / `mspdi-tables:95`） |
| F3 | — | `01-mspdi/mspdi/README.md` が「XSD は git-ignored・未再配布」と書くが**実際は追跡済み**。`learn-docs/` と `../../40-data-format.sdoc` も不在。**公開前に再配布可否の判断が要る** |
| F4 | — | `POC-RESULTS-ja.md:323` が `poc-layout-C-reserved-stackIndex.html`（実名は `-lane`）。なお**実ファイル名が廃止語 `lane` を含む** |
| F5 | — | `a11y-wcag21-aa-checklist.md` が不在の実装ファイル約 20 本を参照（本文の過半） |
| F6 | — | `handover-user-order-diff-ja.md` §5 の根拠 2 件が不在 |

---

## G. 数の不一致

| # | 検証 | 対象 | 実測 | 文書の主張 |
|---|:--:|---|--:|---|
| G1 | ✓ | 描画の設定 | **66**（描画 60 ＋ LOD 6） | `settings §3`「60」/ `erd §5.7-1`「59」/ `ui-detail §4-10-2`「59 個」/ `08-poc/README`「64」 |
| G2 | — | 保存しない項目 | 9 キー | 「全 **8** 項目」（`settings §5` / `erd §5.7-1` / `settings §8-2` の検査 #5） |
| G3 | ✓ | `.md` 本数 | **30**（README は 3 本） | `README §1-1`「全 **26** 文書」「README は **2 つ**」 |
| G4 | ✓ | handover のファイル数 | **38** | `DISCARDED §1`「22 / 25 ファイル」。§2 の内訳に `07-plan-actual/` `08-poc/` `OPEN-ITEMS-ja.md` `grs-document-settings-ja.md` の行が無い |
| G5 | — | エンティティ数 | 14 | `DISCARDED` / `stale-spec-audit`「**12** エンティティ」 |
| G6 | ✓ | `ui-parts §2-1-6`「全数。ここが正」 | 69 キー | `settings §4-1〜4-4` の **24 キー**が §2-1-6 に無い（`assigneeVisible` `zoomX` `exportCanvas` `stackSafetyCap` ほか） |
| G7 | ✓ | 用語の正 | **4 つ** | `ui-parts §6-2` / `user-order` 用語節 / `DISCARDED §3・§4-1`「**2 つだけ**」 |

---

## H. 根拠の欠落（決定はあるが理由が無い）

| # | 検証 | 対象 | 内容 |
|---|:--:|---|---|
| H1 | ✓ | `actualOfPlan = 0.73` | **導出が全文書に無い**。数値が一致する唯一の記述は `ui-detail:731`「基準ページは 22 : 16（比 0.73）」＝**統合前に各ページが割れていたときの実測値**。因果は未確認だが、**縦寸法の鎖の最上流に由来不明の値がある**のは事実 |
| H2 | ✓ | `settings §3` | 表に「**既定値の理由**」列が無い。`markerTextBaseline 0.36` / `labelBaseline 0.35` / `resumeArmOfMark 0.62` / `chevronNotchOfH 0.45` / `arrowHeadOfStroke 3.2` / `spanDotOfStroke 1.15` / `starInnerOfOuter 0.45` ほか由来ゼロが多数。§2 が「凍結は目的」と決めているので**理由の無い値が永久に残る構造** |
| H3 | — | 個別の値 | `pxPerDayAt1x = 6`（「根 4 つ」の 1 つ）／`markerGap = 4px`（理由が §3-2 のどの掴み代とも接続しない・下限=上限で調整不能）／`labelHaloOfFont = 0.17`／打ち切り「全角 12 / 半角 24」／`labelCoef = 0.5`（実測しない方針下で**レイアウト精度を単独で決める**）／掴み代 6px・15×15px（WCAG 2.5.5 を根拠に据えながら基準との突合が無い）／`rowLodBase 0.32`／`dependencyArrowLength 10px`・倍率 2／ズーム ×1.1・0.02〜64／Undo 50 回 |
| H4 | — | `markerOfText` ほか 3 キー | 「**3 桁が円に収まる比**」が根拠だが、`07 §2-4` はマーカーの中身を**記号のみ**と定義し数字に一言も触れない。**存在しない設計に依存した設定が 3 つある** |
| H5 | — | `07 §7` / `ui-detail §4-6` | 進捗の編集入口「右クリックのクイック設定」「キーボードでの進捗増減」が**「不採用」の 2 文字だけ**。真の理由（率は導出値で直接入力させない）が書かれていないので、次の担当者が復活させるとモデルが壊れる |
| H6 | — | 12px | 「**和文がこれを割ると読めない**」の出典が無い。「根 4 つ」の 1 つで、LOD のレベル削減の引き金 |
| H7 | — | 「6 段以上は実務でほぼ無い」 | 3 箇所とも出典なし。**LOD 用の一文が B1 の Drop 正当化に流用されている** |
| H8 | — | 「調査した描画ライブラリ」 | 製品名・版・ファイル名が中立化で消え**再検証不能**。`07 §4-3` ではこれが「完了タスクを左に尖らせない」（日本の作図慣行を意図的に外す判断）の唯一の「強」根拠。`NEUTRALIZED-TERMS §5` が課した「どこまで検証したか併記」が未適用 |
| H9 | ✓ | `07 §2-4-1` | 「**なぜ固定ピクセルをやめたか**」の理由が PoC 追試で**反証済み**（段数膨張の主因は担当＋完了率ラベル。本セッションでも 693px→592px と実測）。当該節に警告が無い |

---

## I. 網羅性の欠落（次期の実開発ステップ別）

実開発の順: **要望ヒアリング → UI モック → アーキ設計 → データモデル設計 → JSON/MSPDI 入出力設計 → UI×データ複合動作**

| # | 検証 | ステップ | 欠けているもの |
|---|:--:|---|---|
| I1 | ✓ | 1・3・6 | **対象環境の定義がゼロ**（ブラウザ / OS / タッチ・モバイルの要否）。性能ゲートの測定条件も CSP も決められない |
| I2 | — | 2 | **視覚モックが 1 枚も無い**。PoC にも `App Header` / `Command Palette` / `Properties Panel` / `Watermark` / `Comment` / `Highlight` が無い（キャンバス描画のみ） |
| I3 | — | 2 | **アイコン定義が無い**（必要数 約 35）。`ui-detail §3` は「前プロジェクトのアイコン定義が参考になる」と書くが**それは handover に無い** |
| I4 | — | 2 | **i18n の表示文字列カタログと英訳がゼロ**。項 21 / 6-4 が要求 |
| I5 | — | 2 | **Properties Panel の中身が未定義**。`07 §7` が WCAG 2.1.1 適合の根拠をこの未設計パネルに預けている |
| I6 | — | 3 | **レンダリング方式の結論（DEC-001）が本文に無い**。`perf §0-2` は「決めた」とだけ書く |
| I7 | — | 3 | **状態管理／不変更新ストアの設計が無い**。`07 §8` がそれを前提に Undo 方式を選んでいる |
| I8 | — | 3 | **モジュール／ディレクトリ構成が無い**（アーキ図 7 本は「参考」で不在） |
| I9 | — | 3 | **技術スタックが handover に無い**（`TypeScript` / `ESLint` は 0 件）。`README` の「このフォルダだけ持っていけばよい」と両立しない |
| I10 | ✓ | 5 | **JSON Schema が存在しない**（`.json` 0 件）。項 57 が JSON を主データ形式、項 62 が厳格検証を要求 |
| I11 | — | 5 | **完全な MSPDI XML 実例がゼロ**。要素順（`xsd:sequence` 47 箇所）・名前空間・`elementFormDefault`・最小妥当文書が未記載 |
| I12 | — | 4 | **既定値未定が 9 ＋ 8 ＋ 1 項目**あるのに「常に全項目を書き出す」が確定。**新規テンプレート（項 63）が作れない** |
| I13 | — | 6 | **LOD のレベル減の量の式が未確定**（`POC-RESULTS §B-6`「提案であって確定ではない」） |
| I14 | — | 6 | **行をまたぐ依存線が未解決**。段が 2 つ以上離れる / マイルストーン端点 / FF・SF・SS も未定。項 46・48・49 は核機能 |
| I15 | — | 6 | **Fit の仕様が断片的**。`perf` T-11「描画実寸で測る」と `POC-RESULTS §B-5`「Fit と LOD 閾値の衝突」が未反映 |
| I16 | — | 2 | **通知（トースト）の設計が無い**のに通知が 5 系統以上散在 |
| I17 | — | 2 | **初期テンプレート（項 63）の中身が未指定** |
| I18 | — | 3 | **localStorage のキー設計・スキーマ移行手順が無い**（`schema_version` は「移行に必須」と書かれている） |

> **I6〜I9 は意図的な空白**である可能性が高い（アーキは実開発のステップ 3 でやる）。
> その場合は `README.md` に**「ここは意図的に空白。実開発で決める」と明記する**だけでよい。

---

## 着手順（推奨）

1. **A**（本セッションの波及漏れ 9 箇所）— 判断不要。今日壊したものの修復
2. **J2**（07-31 / 08-01 の「改訂 → 反映先」表）— これが無いと A を直しても同じことが起きる
3. **B1**（WBS クランプ）— 決まらないとデータモデルもアダプタも書けない
4. **C**（事実の誤り 10 件）— 判断不要。C1・C2 は**実装すると壊れる**ので優先
5. **J1**（各文書に段階を明記）— D 群の大半が効かなくなる
6. **D・G**（陳腐化・数の不一致）
7. **E・F**（重複・リンク切れ）
8. **B2〜B6**（残りの設計判断）
9. **H・J3**（根拠の欠落・「既定値の理由」列）
10. **I**（網羅性）— 実開発のどのステップで埋めるかを決める
